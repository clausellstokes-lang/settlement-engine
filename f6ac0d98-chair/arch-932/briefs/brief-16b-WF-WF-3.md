# THE LANE — PRICING §C lane 16, sitting 16b · WF-3 STANCE CONSEQUENCES (`faithStanceConsequencesEnabled`; car 0 the five-producer reconciliation)

Architect: the Fable seat (family WF), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED = opened at the cited line by me or a recon skeptic; PLAUSIBLE = reasoning. Line numbers rot; re-measure by SYMBOL. Protocols `…/f6ac0d98…/scratchpad/arch-932/ARCH-PROTOCOLS.md` (PROTO §n) via `…/arch-932/ARCHITECTS-CARD.md`; `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` (R1–R6) WINS. Family plan `…/arch-932/plans/plan-WF.md` §1 row 16b, §3 C0-C, §5 "16b"; where the code contradicts it this brief says so (one place: the durability read's lawful site, below).

**Cars: 5 (0, 1, 2, 3, 4) + 1 repair allowance = 5–6.** PRICING §B 3–5 C; HISTORY-WF §A.4 **4–6** ("+1: MANDATORY CAR 0 — a written reconciliation (own / gate / declare) of FIVE live deity→world producers, inheriting `faithField.js:30-38`'s no-double-count law"); +1 because the LIT act is a car of its own under the built-lit regime.

**Prerequisites (anchors):** lane 16a on the consist (the `SIM_LIT_FAITH_SPREAD` cohort exists; the bearing fixture `faith_bearing_v1`; `faithUnseatingEnabled` lit — `brief-16a-WF-WF-0-WF-1.md`); lane 5 (WF-6) LANDED on the consist — this lane's durability seam touches the pact readers WF-6 edits (`treatyEnforcement.js`/`pactAmendment.js`), NEVER `peaceTerms.js`, and the pact files are SERIAL TR-5 → WF-6 → WF-3 → POP-5b → GR-5 (plan §4); GR-2 `a18fdcfae` landed (mounted, CONFIRMED: `settlementLifecycleKernel.js:564`). Phase 4 W-F2 (`deityStance.js`) and W-F5 (`deityStanceLane.js`) are landed and LIT under `faithSpreadEnabled` (`religiousContest.js:553`).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** NEEDS lit first — nothing beyond the door train and lane 16a car 0's cohort: `faithStanceConsequencesEnabled` is SPREAD-lane (its door ANDs `isFaithSpreadEnabled(rules)` — a DEFAULT boolean, not a virtual conjunct; no `requires*` row exists for WF until lane 22's evaluator) and its consumers (`deityStanceLane.js`'s betrayal lane; GR-2's pact compliance) are lit by `faithSpreadEnabled` (dramatic_campaign/full_simulation) and `pactFormationEnabled` (TR-5 7a, all four) respectively — the durability arm therefore MOVES only where a GR-2 pact exists between two patron-bearing courts (`full_simulation` on the bearing fixture; `dramatic_campaign` only for `shared_threat` pacts, which need no belief). WHAT IT UNBLOCKS: WF-5b (the covert seam consumes the FOOTHOLD SEED this lane publishes, lane 19b); WF-8 (the stance voices, lane 21). Roster: TWO presets via `SIM_LIT_FAITH_SPREAD` (`dramatic_campaign`, `full_simulation`) — lane 16a car 0's O-12 ruling; the reason recorded in the key's register comment.

**Shared files this lane declares (PROTO §7.3 rule 1), eff/ceiling at the cut:** `src/domain/worldPulse/deityStance.js` 60/800 (READ-ONLY except the deferral COMMENT at `:160-173` — the fields `aggression`/`treatyDurability` STAY; the comment says "consumed by WF-3 (lane 16b)") · `src/domain/worldPulse/deityStanceLane.js` 235/800 (two seam lines under the door, car 2) · `src/domain/worldPulse/peaceTermsAppraisal.js` (`evolveCompliance` `:271` — the compliance LEAF the head calls at `peaceTerms.js:705`; the preferred durability site, car 2 — VERIFY-AT-BUILD) · `src/domain/worldPulse/pactAmendment.js` 140/800 (the FALLBACK durability site — the renewal/amendment crossing `amendPactInstrument` `:303`, `treatyRenewalActive` `:136`) · `src/domain/worldPulse/treatyEnforcement.js` 113/800 (READ — `grantedRightStateFor` `:262`) · `src/domain/worldPulse/peaceTerms.js` **797/800 — ZERO EDITS** (if the only way to hand `durability01` to `evolveCompliance` is a head line, car 2 STOPs and proposes the extraction of the head's compliance block into `peaceTermsAppraisal.js`, byte-neutral, its own tests, the baseline entry created LOWER — PROTO §4.3) · `src/domain/worldPulse/strategicPosture.js` 136/800 (READ — `courtRiskAppetiteOf` `:303`) · `src/domain/worldPulse/faithField.js` 109 (header cure, car 0) · `src/domain/worldPulse/deityAxes.js` 28 (header cure, car 0) · `src/domain/worldPulse/simulationRules.js` 352 (register tail + cohort line) · `src/domain/certification/subsystemRowsFaith.js` (append a row — created by lane 5; CHECK-GIT-FIRST: if this lane composes first, IT mints the leaf + the tail spread) · `couplingRegistryFaith.js` (append rows) · NEW `src/domain/worldPulse/stanceConsequences.js` (≤ 200) · NEW `tests/lint/stanceConsequencesNoDoubleCount.walker.test.js` · NEW `tests/property/faithStanceDormancyFence.test.js` · the flag-mint test surfaces · the witness golden · `docs/GOLDEN_SHIFT_LEDGER.md` (dock draft). NOT touched: `disposition.js` 237, `dispositionProfile.js` 122, `faithChannelBindings.js` 18, `causalState.js`, `moralInstitutionPressure.js` 321, `religionState.js`'s `applyDivineMandate` (the five producers are RECONCILED, never edited), `sacredClaim.js` 85 (the volume's "threshold-modifier seam, ~6 lines" is REFUSED here — see car 1), `pulseKernel.js`, `beliefMap.js`.

---

## THE STATE OF THE RECORD

- **The history pass HAS run for WF** and WINS over PRICING §B. Its WF-3 row (HISTORY-WF §A.2): UNBUILT + a MANDATORY CAR 0. The premise holds exactly — the deferral comment is verbatim at `deityStance.js:160-173` ("DEFERRED CONSUMPTION ([worldpulse-religion-trade-3], formalized not wired): `aggression` and `treatyDurability` are a DELIBERATE build-ahead … Only betrayalHazard + cooperation are consumed today (deityStanceLane). Wiring the other two is an OWNER-GATED new-capability / deferred-wave resurrection"); `treatyDurability` has NO reader outside its file (`grep -rn treatyDurability src` = the file only; the one `.aggression` hit outside is `npc/paradigmAxisCatalog.js:530`, a different object — re-run by me); `GOLDEN_SHIFT_LEDGER.md:283` `[worldpulse-religion-trade-3]` records the FORMALIZED-NOT-WIRED deferral; `FABLE_VALIDATION_QUEUE.md:315` CR-4 and `SOL_QUEUE.md:348-349` hold the seatBooks stance-choices seam DEFERRED "revisit at WF-3 drafting". **BUT FIVE live deity→world producers now price what WF-3 proposes**: (1) the warlike drive `disposition.js:195 W_DEITY = 0.35` → `:208 deityTemperDrive` × `:281 pietyLocalMultOf`, plus `:469 W_LAW_DEITY = 0.3` → `:554`; (2) WR-2's domain pressure `dispositionProfile.js:168 deityPressureOf` under §851's one-arm-per-deity (`authoredAspectsOf` `:124-131`; `supportedDomainOf` `:135` returns null when a boon/bane is authored); (3) W-FAITH's channel lift `faithChannelBindings.js:81-88 FAITH_CHANNEL_BINDINGS` (six channels → `causalState.SYSTEM_VARIABLES` members) consumed at `src/domain/causalState.js:78/:157-159` UNFLAGGED ("DORMANT BY ABSENCE" `:132` — no shipped deity authors a boon); (4) `moralInstitutionPressure.js` mounted `pulseKernel.js:114` (`evaluateMoralInstitutionPressure` `:275`; `evaluateMoralInstitutionFounding` `:441` — patron axes × piety ABOLISH and FOUND institutions under `institutionLifecycleEnabled`); (5) `applyDivineMandate` (`religionState.js:762` ← `pulseKernel.js:1807`) → `publicLegitimacy`. And the law to inherit: `faithField.js:30-38` ("Feeding the temper into a `war_readiness` channel here would be the DOUBLE-COUNT W-FAITH D1 forbids"). The volume's Q2 hazard (`riskToleranceOf` collision) is CURED in the safe direction: SP-4 landed as `strategicPosture.js` (`:9-18` records the ruling; the court read is `courtRiskAppetiteOf` `:303`; `tests/lint/postureNameCollision.walker.test.js` keeps `riskToleranceOf` to ONE definition) and has a live caller (`pactFormation.js:110/:367`). Grep traps adopted verbatim: `courtRiskAppetiteOf` not `riskToleranceOf`; `schism` has three non-faith senses; `faith_pact_formed` is the stance lane's NEWS-ONLY record (`deityStanceLane.js:238-260`), not GR-2's pact.
- **The volume** `docs/DESIGN_FP_ARCH_WF.md` §4 WF-3 (`:497-523`): leaf `stanceConsequences.js` ≤ 200 eff (JUDGMENT: leaf, the lane file's betrayal machinery untouched); "aggression as threshold colour never selector — E3; durability → SP-3's fraying clock WHEN SP-3 exists — until then the durability consumption builds against the pact's existing news-only cadence with the SP-3 hook declared-dark" — ⚠ SUPERSEDED BY CODE: GR-2's pact lifecycle IS landed (the compiled programme §5 #35 `:1966-1972` already says "GR-2's substrate landed by order; the SP-3 hook now binds against real pacts"), so the durability consumer binds against `evolveCompliance`'s real compliance evolution, not a news cadence; the pins (REVERSAL both directions on one fixture; COLOUR-NEVER-DROWN cap; the G1c fence captured FIRST — dark ⇒ the two consumed fields and every receipt byte-identical; departed-minister lifecycle; guard-absoluteness regression); the mutants (cap removal; fraying-scale zero); Req 13 "THE ONE PLACE faith reads authored truth — stances read the deities' authored axes … consequences land on believer state only"; Req 14 no new state; the volume's `sacredClaim.js` "threshold-modifier seam, ~6 lines" (`:502`) — REFUSED in car 1 (a stance colour on the casus-belli threshold would be a SIXTH deity→war coupling beside `W_DEITY`; the reconciliation forbids it — recorded). §1b V12 (`:143`) and V42 (`:173`, the collision, now cured). ODQ §78.2 F-5 THE WF TUNING-HOME LAW (every dial inside `STANCE_CONSEQUENCES_TUNING` with an executed derivation or the compile STOPs); `docs/DESIGN_FP_ARCHITECTURE.md:1006` "WF-3/5/7, POP-1/2/5b, INT-1 — lighting seatBooksEnabled later is a …" (the seatBooks seam stays DEFERRED — CR-4 re-affirmed here, recorded).
- **RECORD≠CODE rows this lane cures in code (R3/R4 of the fold):** `faithField.js:12-22` "THE FIELD IS DARK. Nothing in production calls it" — FALSE since F4c (`religionState.js:43/:669` and `causalState.js:78` import it; the fence `faithFieldDormancyFence.test.js:146-170` was correctly amended to the three-consumer roster, the header was not); `deityAxes.js:35-42` "every consumer that reads an EMBED — the whole engine — still derives" — FALSE since F3c (`deityTemperConsumerCensus.walker.test.js:28-35` records "the ten embed consumers now hear an authored word"). Two comment edits, zero behaviour — car 0.
- **The owner's laws:** everything built lands LIT; a lit key whose consumers cannot reach the behaviour is a FALSE CLAIM (this key's consumers are live: the betrayal lane under faithSpread; GR-2's compliance under pactFormation); tuning raw + derivation; nothing pushes/deploys/applies/deletes/spends; the golden master never re-recorded.

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/deityStance.js` | 60/800 | `stanceOf(source, target)` returning `{ aggression, cooperation, betrayalHazard, treatyDurability }` `:174`; `treatyDurability = clampSigned(T.DURABILITY_LAW * signedMinLawfulness − T.EVIL_BRITTLE * (pos(mA) + pos(mB))) + 0` `:159-161` (a SIGNED decay-resistance modifier, 0 = no signal); `STANCE_TUNING` (`:53-73` per the volume); the deferral comment `:163-173` |
| `src/domain/worldPulse/deityStanceLane.js` | 235/800 | `STANCE_LANE_TUNING` `:51-66` (`BETRAYAL_COOLDOWN_TICKS 8`, `PACT_COOLDOWN_TICKS 6`, `FOOTHOLD_COOLDOWN_TICKS 6`, `MAX_BETRAYALS_PER_TICK 2`, `MAX_PACTS_PER_TICK 3`, `BETRAYAL_MIN_HAZARD 0.05`, `PACT_MIN_COOP 0.45`, `BETRAYAL_SEVERITY 0.6`); `pactBetrayalHazard(source, target)` `:84`; `betrayalCooldownPairs` `:100`; `pactCooldownPairs` `:126`; `pactOutcome` `:238` (NEWS-ONLY `faith_pact_formed`, "the positive relationship-WEIGHT coupling is the deferred half") |
| `src/domain/worldPulse/peaceTermsAppraisal.js` | — | `evolveCompliance({ loserCapacity01, monitorReach01 })` `:271` — THE compliance evolution leaf; called from `peaceTerms.js:705` with `role.capacity01`/`role.reach01` |
| `src/domain/worldPulse/peaceTerms.js` | **797/800** | `:705 const comp = evolveCompliance(...)`; the stream loop `:710-735`. ZERO EDITS — VERIFY-AT-BUILD where `role.capacity01`/`role.reach01` are built (`termObligationOf`, `treatyOrientation.js:256-275`, or the head loop); if `role` is built in a LEAF, `durability01` rides it without a head line |
| `src/domain/worldPulse/pactAmendment.js` | 140/800 | `treatyRenewalActive(worldState)` `:136` (GR-5's door — CHECK-GIT-FIRST); `amendPactInstrument({ treaty, terms, tick, act })` `:303`; `closeTermsBrokenByWar` `:353`; `absorbWarEndIntoStandingPact` `:406` — the FALLBACK durability site (a durability-scaled `expiresTick` extension at the amendment crossing) |
| `src/domain/worldPulse/treatyEnforcement.js` | 113/800 | `grantedRightStateFor` `:262-265` ("honored on parchment and harassed on the road" — the OBSERVED word) — READ |
| `src/domain/worldPulse/strategicPosture.js` | 136/800 | `POSTURE_ACTOR_KINDS = ['settlement']` `:97` (SP-4a's widening is lane 55's, R4); `strategicPostureActive(rules)` `:143`; `courtPostureOf` `:222`; `courtRiskAppetiteOf(actorRef, dispositionEntry, options)` `:303` — the posture read WF-3 consumes as a DECLARED ARM (dark where `strategicPostureEnabled` is dark — neutral fallback receipted) |
| the five producers | — | `disposition.js:195/:208/:281/:283/:469/:554`; `dispositionProfile.js:124-131/:135/:168`; `faithChannelBindings.js:81-88` + `causalState.js:78/:132/:157-159`; `moralInstitutionPressure.js:275/:441` (mounted `pulseKernel.js:114/:1416`); `religionState.js:762` (mounted `pulseKernel.js:1807`) — READ ONLY; the walker imports none of them |
| `src/domain/worldPulse/faithField.js` · `deityAxes.js` | 109 · 28 | the two stale headers (`faithField.js:12-22`; `deityAxes.js:35-42`) — comment edits, car 0 |
| `src/domain/worldPulse/religiousContest.js` | 493/800 | the targeted-foothold lane (spread) — the FOOTHOLD SEED this lane publishes (car 3) is a pure read the lane consults; `FOOTHOLD_COOLDOWN_TICKS` governs its cadence |
| `src/domain/worldPulse/simulationRules.js` | 352 | `faithStanceConsequencesEnabled` ZERO hits (NEW mint); `SIM_LIT_FAITH_SPREAD` exists (16a car 0), carrying `faithTermsEnabled` if lane 5 composed (CHECK-GIT-FIRST) |
| `tests/lint/couplingInclusion.walker.test.js` | — | `LAYER_PATTERNS` faith regex `:187` does NOT match `stanceConsequences.js` (no `faith|sacred|religion|pantheon|conversion|piety|deity|temple` prefix) — ⚠ EITHER name the leaf `deityStanceConsequences.js` (matches `deity`) OR add an exact-path row as `patronFall.js` did (`:203-206`); RULING: name it `deityStanceConsequences.js` — zero walker edits, and the name says whose stance |
| ABSENT | — | `stanceConsequences.js`/`deityStanceConsequences.js`, `faithStanceConsequencesEnabled`, the two new test files |

**Budgets:** worker 1,404,493 EXACT untouched (quote); engine ±0 (worldPulse leaves ride the advance worker); first-paint +1 register string + 1 cohort line (≈ 50–100 B PLAUSIBLE) — quote.

## ALREADY DONE (do not re-land)

Phase 4 W-F2's pure stance model (`deityStance.js` whole, owner-tuned) · W-F5's stance lane (betrayal + pact + foothold, LIT under faithSpread) · the `[worldpulse-religion-trade-2]` PACT 6 cooldown cure · GR-2's pact lifecycle and compliance evolution · SP-4/SP-C's posture read with the collision cured · W-FAITH's channel bindings and §851's precedence · `moralInstitutionPressure`'s two moral lanes · the divine mandate · the five-producer census itself (HISTORY §A.3 rows 4–6, 14 — this lane WRITES it into the tree, it does not rediscover it).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned files; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: 16b`; PACKET_STANDARD caps; the packet `docs/implementation/packets/fp/WF-3.md` in the dock.

### Car 0 — THE RECONCILIATION: five live deity→world producers ruled own / gate / declare, the no-double-count walker pointed inward, two stale headers cured (docs + one walker; zero behaviour)

**In plain words:** five pieces of the engine already let a town's god change the world — its temper tilts the court toward war, its domain leans the court's mood, an authored blessing lifts harvest or trade, its character founds or abolishes institutions, and its favour props the throne. Before this wave adds a sixth and seventh, this car writes down for each of the five whether the new wave owns it, gates it or leaves it be — and plants a permanent test that the new leaf never reaches into any of them, so the same divine influence is never counted twice.

**The change.** THE RECONCILIATION TABLE (the packet + the volume's dock copy §4 WF-3 + `deityStanceConsequences.js`'s header when car 1 lands): (1) `W_DEITY`/`W_LAW_DEITY` — DECLARED, untouched: the temper→war coupling is disposition's; WF-3's `aggression` colour touches the STANCE LANE's escalation between two DEITIES (inter-deity), never the court's war drive — a different plane; (2) `deityPressureOf` — DECLARED, untouched (§851's one-arm-per-deity law inherited: WF-3 reads the same embed the ten consumers read and never re-derives a domain); (3) the channel lift — DECLARED, untouched (`faithField.js:30-38`'s law: the temper is never fed to `war_readiness`; WF-3 writes no `SYSTEM_VARIABLES` member); (4) `moralInstitutionPressure` — DECLARED, untouched (WF-5a forks its founding lane; WF-3 reads nothing from it); (5) `applyDivineMandate` — DECLARED, untouched (legitimacy is not stance). WHAT WF-3 OWNS: the two UNCONSUMED fields `aggression` and `treatyDurability` — nothing else. NEW `tests/lint/stanceConsequencesNoDoubleCount.walker.test.js` (the K3 idiom pointed INWARD): (arm 1) `src/domain/worldPulse/deityStanceConsequences.js` (or its intended path before car 1 — the walker asserts the path when present and its future absence is declared by a `it.skipIf`-free explicit "the leaf is not yet landed" branch REFUSED: the walker lands in car 0 asserting on `deityStance.js`'s CURRENT consumer set and gains the leaf arm in car 1) imports NONE of `disposition.js`, `dispositionProfile.js`, `faithChannelBindings.js`, `moralInstitutionPressure.js`, `religionState.js` (a comment-stripped import scan); (arm 2) it writes NONE of `causalState.SYSTEM_VARIABLES` (a token scan over the leaf for each member name in write position); (arm 3) the positive control — a planted import of `disposition.js` in a temp copy is SEEN (guard-the-guard); (arm 4) the consumer census of `deityStance.js`'s four fields at this tip: `betrayalHazard`/`cooperation` read by `deityStanceLane.js`; `aggression`/`treatyDurability` read by NOTHING in `src/` (the FIRED state; car 2 updates the expectation to `deityStanceConsequences.js` in the same commit as its reads — the deferral closes VISIBLY). THE TWO HEADER CURES: `faithField.js:12-22` → "THE FIELD HAS THREE DECLARED CONSUMERS (F4c/F7c): `causalState.js`, `display/faithDeepening.js`, `religionState.js` — the fence `faithFieldDormancyFence.test.js` pins the closed set; dormant by DATA (no shipped deity authors a boon)"; `deityAxes.js:35-42` → "the embed carry landed at F3c (ODQ §866): the ten embed consumers hear the authored word; `deityTemperConsumerCensus.walker.test.js` records the closing". `deityStance.js:163-173` deferral comment: UNCHANGED at car 0 (car 2 amends it when the reads exist).

**Files (≈5):** the walker (NEW), `faithField.js` (comment), `deityAxes.js` (comment), the packet, the volume dock copy. **Pins:** the walker's four arms; the positive control executed. **Census bills:** refreeze own commit; manifest `kind:'mutation'` (the planted import). **Byte rule:** comment-only src bytes — build quoted, dist byte-identical stated. **Gate:** full list + `faithFieldDormancyFence` (unchanged, green) + `deityTemperConsumerCensus.walker`. **LIT act:** none. **Exit:** the table written; the walker green in its fired state; the two headers true.

### Car 1 — `deityStanceConsequences.js`: the leaf with its door, its tuning home, the two pure consumers as functions, and the DARK MINT

**In plain words:** a small new module reads the two numbers the stance model has computed for months and nobody used — how aggressive one god is toward another, and how durable a pact between their followers should be — and turns each into a bounded modifier, behind a new switch that is registered but off. Nothing is wired yet; the functions exist, the switch exists, and the certification row for the switch exists in the same commit because the estate's walkers demand it.

**The change.** NEW `src/domain/worldPulse/deityStanceConsequences.js` (≤ 200 eff; the name matches `couplingInclusion.walker`'s `deity` prefix — no walker edit): `stanceConsequencesActive(rules)` — THE ONE by-name read `rules?.faithStanceConsequencesEnabled === true && isFaithSpreadEnabled(rules)`; `STANCE_CONSEQUENCES_TUNING = Object.freeze({ AGGRESSION_COLOUR_CAP: <raw>, DURABILITY_RESIST_CAP: <raw> })` — each value RAW with an EXECUTED DERIVATION in the comment (the WF TUNING-HOME LAW; e.g. the colour cap derived as a fraction of `BETRAYAL_SEVERITY 0.6` so the colour can never exceed the base term — COLOUR-NEVER-DROWN by construction; the resist cap derived against `evolveCompliance`'s measured per-tick slip so a pact never becomes immortal); `aggressionColour(stance)` → a signed bounded modifier in `[−CAP, +CAP]` from `stance.aggression` (E3: a COLOUR on an existing threshold/severity, never a selector — the function returns a delta, never a branch); `durabilityResist(stance)` → `clamp01(0.5 + 0.5 * stance.treatyDurability) * DURABILITY_RESIST_CAP` (the SIGNED field: +1 both lawful → full resistance, −1 chaotic → none; 0 → the neutral centre, byte-identical to no signal); both PURE (no store, no Date, no Math.random). THE DARK MINT in the same commit (ADDENDUM R3 as amended — `scanGateReads` sees the by-name read): `simulationRules.js` register TAIL gains `'faithStanceConsequencesEnabled'` (comment: WF-3 · SPREAD lane · two-preset roster + O-12 by symbol · DARK at this car); `subsystemRowsFaith.js` gains the row (CHECK-GIT-FIRST: lane 5 created the leaf and the tail spread; if THIS lane composes first it mints both — the FIRST WF mint to compose creates the leaf) — 25–40 eff; THE EVIDENCE LAW: `eventTypes: []` DELIBERATELY EMPTY AND MEASURED (this lane mints no candidate; the stance lane's `faith_pact_formed`/betrayal candidates are W-F5's and declaring them would grade this row off another wave's vocabulary — WF-1a's row says exactly this); `stateKeys: []`; `other` prose names the two consumed fields and the five declared producers; the three `subsystemRowsVirtual.test.js` edits (const; `VIRTUAL_RULES` IN POSITION at the tail — after `faithTermsEnabled` if lane 5 composed first; `LANE_LEAVES` entry `faithStanceConsequencesEnabled: 'src/domain/worldPulse/deityStanceConsequences.js'`); BOTH `contributionLedgerShape.test.js:118-119` literals READ OFF HEAD (+1); the covering-array `union` re-measured; the four edge-shared files regenerated. NEW `tests/property/faithStanceDormancyFence.test.js` in the DARK shape (fence 1: `stanceOf` + the lane driven on the bearing fixture with `faithSpreadEnabled: true` but the key ABSENT ⇒ every stance-lane candidate and every pact compliance state byte-identical to a run with the two functions stubbed to identity — computed in-file; fence 2 absent ≡ false; fence 3 the two functions' call counts ZERO dark (a spy at the leaf boundary) and MOVING lit — the lit twin is car 2's once the mounts exist, so at car 1 fence 3 asserts ZERO both ways with the lit arm marked "mounted at car 2"; fence 4 polarity — exactly ONE `=== true` site, the truthy-non-true probes dark). `tests/domain/deityStanceConsequences.test.js` (NEW): the two functions over the `STANCE_TUNING` corner cases (both-lawful-good, chaotic-evil, neutral centre → exact 0 / `+0`), the caps hold at the extremes, purity. The no-double-count walker (car 0) gains the leaf arm (the import scan over the real path).

**Files (≈11):** the leaf (NEW), `simulationRules.js`, `subsystemRowsFaith.js`, `subsystemRowsVirtual.js` (only if minting the leaf), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, `faithStanceDormancyFence.test.js` (NEW), `deityStanceConsequences.test.js` (NEW), the walker (arm), the manifest; regenerated: four edge files. **Census bills:** refreeze; manifest ×2; bundles. **Byte rule:** `simulationRules.js` EAGER (+≈30 B) — quote; worker unchanged; engine ±0. **Gate:** full list + engine-gated walker (directions 1–3; `ENGINE_GATED_DORMANT_RULE_KEYS` CONTAINS the key), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage`, the two freshness tests, the totality walker. **LIT act:** none. **Exit:** the leaf pure and pinned; the mint registered dark with its row; literals stated.

### Car 2 — the two mounts: aggression colours the betrayal lane's escalation; durability resists compliance decay on GR-2 pacts — with the REVERSAL pin, the COLOUR-NEVER-DROWN cap, the departed-minister lifecycle and the guard-absoluteness regression; the deferral comment closed

**In plain words:** the two numbers start to matter. An aggressive god's followers escalate a betrayal a shade harder (and a placid god's a shade softer) — a colour on the existing machinery, never a new trigger — and a pact sworn between the followers of two lawful gods frays more slowly under the existing compliance clock, while one touching a chaotic or cruel god frays faster. Both effects are capped so they can never drown the thing they colour, and both reverse cleanly when the stance reverses.

**The change.** MOUNT 1 (`deityStanceLane.js`, 2 seam lines under the door): at the betrayal severity/hazard composition (`pactBetrayalHazard` `:84`'s consumer in the lane body; the `BETRAYAL_SEVERITY 0.6` base — locate by symbol), `severity = clamp01(base + (stanceConsequencesActive(rules) ? aggressionColour(stance) : 0))` — the colour never crosses `BETRAYAL_MIN_HAZARD`'s dead-band on its own (E3: it cannot CREATE a betrayal, only colour one that already fires — pinned). MOUNT 2 (the durability read): PREFERRED SITE `peaceTermsAppraisal.js evolveCompliance` widened with an OPTIONAL `durability01 = 0` (byte-identical when absent — the default is the neutral centre) that scales the decay step; the value is supplied per GR-2 pact from `durabilityResist(stanceOf(patronA, patronB))` where the two courts' patrons are read from the settlement snapshots (`config.primaryDeitySnapshot`; no patron on either side ⇒ 0 — the volume's guard) — VERIFY-AT-BUILD whether `role` (the object the head hands `evolveCompliance` at `peaceTerms.js:705`) is built in a LEAF (`treatyOrientation.js termObligationOf` or a sibling) so `durability01` rides it with zero head bytes; if the ONLY road is a `peaceTerms.js` line, this car STOPs and proposes the extraction recipe (the head's compliance block → `peaceTermsAppraisal.js`, byte-neutral, its own tests, the `.size-baseline.json` entry created LOWER — PROTO §4.3) — the FALLBACK site is `pactAmendment.js` (140/800): a durability-scaled term-duration extension at `amendPactInstrument` (GR-5's renewal window; CHECK-GIT-FIRST whether GR-5 landed `treatyRenewalActive`'s trigger) — the lane REPORTS which site it took. `deityStance.js:163-173` comment → "CONSUMED by WF-3 (§932 lane 16b car 2): aggression → `deityStanceConsequences.aggressionColour` (the betrayal lane's severity colour); treatyDurability → `durabilityResist` (GR-2 pact compliance decay); [worldpulse-religion-trade-3] CLOSED — named in the shift ledger". The no-double-count walker's arm 4 expectation → the two fields now read by `deityStanceConsequences.js` (same commit).

**Pins (`tests/domain/deityStanceConsequences.mount.test.js`, NEW):** THE REVERSAL PIN — one fixture, stance flipped (patron A's alignment mirrored) → the colour and the resist flip sign/magnitude symmetrically; COLOUR-NEVER-DROWN — the coloured severity never exceeds `base + CAP` and never falls below `base − CAP` over the whole `STANCE_TUNING` corner grid; E3 — a pair below `BETRAYAL_MIN_HAZARD` mints NO betrayal even at `+CAP`; THE DURABILITY ARM — a GR-2 pact between two lawful-patron courts on the bearing fixture (signed through `signPactProposal`) shows a strictly slower `complianceState` slip than the same pact with the key dark, and a chaotic-patron pair a faster one (the seeded fixture drains capacity so the slip is reachable in-horizon); NEUTRAL CENTRE — a pair with `treatyDurability === 0` is byte-identical lit vs dark; THE DEPARTED-MINISTER LIFECYCLE — a betrayal coloured on tick t whose named minister dies on t+1 leaves no dangling reference (the lane's own `pulseHistory` read); GUARD-ABSOLUTENESS REGRESSION — a court with no patron gets colour 0 and resist 0 (never a charitable kindred default — `sacredClaim.js`'s doctrine). **Mutants:** the cap removed → COLOUR-NEVER-DROWN reds; `durabilityResist` returning a constant (fraying-scale zero) → the durability arm reds; the door removed → fence 1 reds. `faithStanceDormancyFence` fence 3's LIT TWIN now asserted (the counts MOVE under a lit literal bag on the bearing fixture).

**Files (≈7):** `deityStanceLane.js` (2), `peaceTermsAppraisal.js` (the optional parameter) or `pactAmendment.js` (fallback), the feeder (in the leaf, ≤ 20), `deityStance.js` (comment), the mount test (NEW), the fence (lit twin), the walker (arm 4), `couplingRegistryFaith.js` (FAITH→GRAMMAR compliance row; FAITH→FAITH stance-lane row — `flags: ['faithStanceConsequencesEnabled']` ONLY). **Census bills:** refreeze; manifest ×2. **Byte rule:** build quoted; worker 0; engine ±0; `peaceTerms.js` byte count UNCHANGED (asserted with the enforcer's Linter at the publishing commit — 797). **Gate:** full list + `pactFormationDormancyFence` (fence 1 dark identity holds — the durability read is inside `stanceConsequencesActive`) + the T9 STOP check (no other lane in flight on `peaceTerms*.js`). **LIT act:** none. **Exit:** both mounts driven both ways; the deferral comment closed; `peaceTerms.js` at 797.

### Car 3 — the posture arm declared and the WF-5b FOOTHOLD SEED published (a pure read, degraded-dark until lane 19b)

**In plain words:** how boldly a court acts on its god's stance depends on the court's own appetite for risk — a read another family already built — so this car wires that read in as a declared modifier with an honest neutral fallback where the posture layer is dark, and publishes a small "where a rival creed could gain a foothold" signal that the underground-congregation wave will consume later.

**The change.** `deityStanceConsequences.js` (+≤ 40): `postureScaledColour(actorRef, entry, rules)` → `aggressionColour × courtRiskAppetiteOf(actorRef, entry, { rules })` where `strategicPostureActive(rules)` — the IMPORT-SOURCE PIN: the read resolves to `src/domain/worldPulse/strategicPosture.js#courtRiskAppetiteOf` and NEVER to `src/domain/roads/state.js#riskToleranceOf` (the cured collision — pin the SOURCE, not the collision; `postureNameCollision.walker` stays green); dark posture ⇒ ×1 (neutral fallback, receipted `posture_dark`); `footholdSeedFor(worldState, cid, rivalRef)` → a bounded 0..1 read composed from the stance lane's foothold candidates (the cadence `FOOTHOLD_COOLDOWN_TICKS` governs) × the colour — PUBLISHED as an export the WF-5b covert seam reads (lane 19b); until then it has no consumer — declared in the leaf header ("consumer: WF-5b lane 19b; a read with no reader at this car, stated"). `POSTURE_ACTOR_KINDS` is `['settlement']` (`strategicPosture.js:97`) — the seed reads at the settlement grain only; R4's widening is lane 55's and this lane neither needs nor touches it.

**Pins:** the import-source pin (a source scan of the leaf's import specifier); posture dark ⇒ the colour equals the unscaled colour (byte-identical); posture lit on the bearing fixture ⇒ an insular seat's colour is damped and a bold one's amplified within the cap; the seed is in `[0,1]` and 0 for a settlement with no rival creed. **Mutant:** the import re-pointed to `roads/state.js` → the source pin reds (the walker `postureNameCollision` would not — the names differ — which is why the pin is the source).

**Files (≈3):** the leaf, its test, `couplingRegistryFaith.js` (FAITH→SPINE posture row). **Census bills:** refreeze if a title changed; manifest. **Byte rule:** build quoted. **Gate:** full list + `postureNameCollision.walker`. **LIT act:** none. **Exit:** the arm both ways; the seed published and documented.

### Car 4 — THE LIT CAR: `faithStanceConsequencesEnabled` → `SIM_LIT_FAITH_SPREAD` (two presets); the fence to the LIT shape; the witness; the shift ledger — BUILT AT LANDING ON THE CONSIST, rebased, after lane 16a and lane 5

**In plain words:** the stance-consequences switch is turned on in the two living presets where faith already spreads between towns, with proof that a world without gods is unchanged, proof on the god-bearing world that the colour and the resistance now move, and the shift written down with its movers named — including the honest note that the pact-durability half moves only where a pact between two god-bearing courts exists.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 (the two-preset cohort is lane 16a car 0's recorded extension of `SIM_LIT`'s law; the `:367` walker is D0's — run, quoted, never edited); R2 (composes after D0, lane 16a, lane 5; `pactFormationEnabled` lit by TR-5 7a is the durability arm's mechanism precondition — stated, not a lighting-table row; a dock-green LIT car is a FALSE GREEN); R3 (the dark mint was car 1; this car lights and re-reads); R6 (the report lists the volume rows below).

**The change:** (1) `simulationRules.js`: `SIM_LIT_FAITH_SPREAD` gains `faithStanceConsequencesEnabled: true, // WF-3 · lane 16b car 4 · SPREAD lane — two presets, O-12`; the register comment gains the LIT paragraph. (2) THE MOVERS BY NAME: the betrayal colour in `dramatic_campaign` and `full_simulation` on deity-bearing worlds with ≥ 2 patron-bearing courts whose stance clears `BETRAYAL_MIN_HAZARD`; the durability resist wherever a GR-2 pact stands between two patron-bearing courts in those presets (`shared_threat` pacts in both; belief-fed pacts in `full_simulation` only); the posture scale where `strategicPostureEnabled` is lit (CHECK: its roster). (3) `faithStanceDormancyFence.test.js` → LIT shape: the roster arm (VIRTUAL; REGISTERED; `true` in exactly the presets where `SIM_LIT_FAITH_SPREAD` is spread == `isFaithSpreadEnabled` true — computed; absent from the other five); fence 1's dark world → `quiet_local.rules`; fence 3's lit twin under `full_simulation.rules` on the bearing fixture MOVES both call counts. (4) The row text in `subsystemRowsFaith.js` amended LIT; the deferral's ledger row `[worldpulse-religion-trade-3]` named CLOSED in the shift draft. (5) THE WITNESS by hand: `rulesSha256` + `litFlagCount` move on `dramatic_campaign` and `full_simulation` only; every pulse hash UNMOVED (deity-free born worlds — `stanceOf` is never reached without two patrons); a moved pulse hash on ANY row is a STOP. (6) 525/525 unchanged; espionage 21/21; `prose-manifest-cells.mjs --out` zero rows. (7) Covering-array figures re-measured; both literals re-read off HEAD; bundles rebuilt. (8) THE SHIFT-LEDGER DRAFT (`docs/implementation/GOLDEN_SHIFT_LEDGER-WF-3.draft.md`): *"`faithStanceConsequencesEnabled` lit on `dramatic_campaign`, `full_simulation` at landing <sha> (the SPREAD-lane roster; O-12); `rulesSha256`/`litFlagCount` moved on those two rows; every pulse hash UNMOVED (deity-free born worlds); the G1c deferral `[worldpulse-religion-trade-3]` CLOSED — the two fields are consumed as a capped colour and a capped resist; the durability half moves only on GR-2 pacts between patron-bearing courts; the generation dump 525/525; espionage 21/21; closure +<n> B; no installed campaign re-labelled; no tuning value moved (the two caps are RAW with derivations, ratified at the soak)."*

**Files (≈8):** `simulationRules.js`, the fence, the witness golden, `subsystemRowsFaith.js` (text), `coveringArrayCoverage.test.js` (if moved), `contributionLedgerShape.test.js` (if HEAD moved), the shift draft, the packet; regenerated: four edge files. **Census bills:** refreeze; manifest `kind:'rationale'`; bundles. **Byte rule:** closure +1 line quoted; worker unchanged; engine ±0. **Gate:** full list + the witness, the stability test, the two freshness tests, the engine-gated walker (`ENGINE_GATED_DORMANT_RULE_KEYS` LACKS the key), the totality walker, `tradeConvergenceContract.walker` (D0 + 16a's amendment), `faithSpreadCohortFence` (the key is on its allowlist), the espionage fence, the soak suites alone at load < 8. **LIT act:** the key. **Exit:** lit per roster; fence LIT-shape green with the twin moving; witness diff quoted; shift draft; zero UNDECLARED lines.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

Law 2's LAST car is car 4 (the LIGHTING). The DARK MINT is car 1 (the by-name read is the leaf's door; `scanGateReads` reads every `src/` file; `manifestWithoutRow` forbids the entry without the row; the pinned `VIRTUAL_RULES`/`VIRTUAL_PENDING_RULE_KEYS`/both lengths drag the three test edits and both literals into car 1) — the forced departure from R3-as-first-written, recorded. **At car 1**: `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length`; literals = that + 1 (36 → 37 if lane 5 composed first and no TR mint; NEVER inherited). **At car 4** (rebased) the same two commands re-run; the register tail, `SIM_LIT_FAITH_SPREAD` (lane 5's `faithTermsEnabled` line may precede), `subsystemRowsFaith.js`'s tail, the literals and the census are CHECK-GIT-FIRST and resolved by MEASUREMENT (PROTO §5.2). The report states the numbers written, re-read, and expected.

## THE VOLUME ROWS THIS LANE CHANGES (ADDENDUM R6)

`§4 WF-3` → "LANDED-LIT (§932 lane 16b; two-preset roster)" with the cars by sha; the RECONCILIATION TABLE (five producers, own/gate/declare) written into the row; "durability → SP-3's fraying clock WHEN SP-3 exists" → "binds against GR-2's `evolveCompliance` (the site taken: `peaceTermsAppraisal.js` or the `pactAmendment.js` fallback — as reported)"; the `sacredClaim.js` threshold seam → REFUSED (recorded); the leaf's name `deityStanceConsequences.js` (not `stanceConsequences.js`) with the `couplingInclusion` reason; `§1b V10` → "temper is a TWO-ARM read (derived + authored, F3c)" and V12 → "CONSUMED"; `§2` the flag row → LANDED-LIT; the seatBooks CR-4 deferral RE-AFFIRMED (FVQ/SOL rows unchanged — recorded as deliberately deferred); the history fold's WF-3 row folded; the two in-code header cures listed as RECORD≠CODE R3/R4 CURED.

## OWNER-GATED, KEPT OUT

- The seatBooks stance-choices seam (CR-4, SOL §15) — stays DEFERRED; "revisit at WF-3 drafting" is discharged by this record: deferred again, with the reason (INT-1's `booksOf` is unbuilt).
- Tuning: `AGGRESSION_COLOUR_CAP`, `DURABILITY_RESIST_CAP` — RAW with derivations inside `STANCE_CONSEQUENCES_TUNING`, ratified at the soak; the G1c golden's lighting-day disclosure (J-WF-8) is the shift draft's.
- Any `peaceTerms.js` byte (the STOP + extraction recipe if the durability seam has no leaf road); any raise of a ceiling; any edit to the five producers; SP-4a's `POSTURE_ACTOR_KINDS` (lane 55's R4); push/deploy/migration/deletion/spend; the golden master.
- Persisted shapes widened: NONE (the colour and the resist are transient reads; no new key) — recorded.

## RISKS

**Grep traps (HISTORY-WF + RECHECK §5 + HISTORY-TRADE, VERBATIM by reference; the ones that bite here):** `riskToleranceOf` is the ROADS npc read (`roads/state.js`) — the court read is `courtRiskAppetiteOf`; `.aggression` matches `npc/paradigmAxisCatalog.js:530` (a different object); `faith_pact_formed`/`pactOutcome` is the stance lane's NEWS record, not GR-2's pact — the durability read applies to the TREATY ledger; `schism` has three non-faith senses; `W-F2`/`W-F5` (Phase 4) ≠ `WF-3`; `couplingInclusion.walker`'s faith regex needs a `deity`-prefixed leaf name.

**Collision surfaces:** the pact files (TR-5 → WF-6 → THIS LANE → POP-5b → GR-5; the T9 STOP; CHECK-GIT-FIRST for GR-5's `treatyRenewalActive` trigger if the fallback site is taken); the WF chain (`deityStanceLane.js`, `religiousContest.js` — ONE WF lane in flight); `simulationRules.js` and the flag-mint surfaces; `subsystemRowsFaith.js` (lane 5 creates it, this lane appends — or the reverse; MEASURE); `couplingRegistryFaith.js` (append-only).

**The addendum, by path (cite, never re-derive):** `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R1 + lane 16a car 0's two-preset cohort (the fallback if the chair reads PROTO §1.1 strictly: this key lands DARK with its lighting priced to the successor-id act); R2 (after D0, 16a, lane 5); R3 (dark mint at car 1); R5 (car 4 ≈ 8 files — no split); R6 (the volume rows above).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls; dist-gated suites skip without `dist/`; `deno.lock` re-freeze; `git grep` reads the INDEX; 120 faith suites in one vitest = exit 137 — halve the run; a moved DRIVEN GOLDEN (`worldpulseDeityGolden.test.js` drives literal rules — a preset lighting must not move it; if it moves, the mover is a kernel change under a neighbour's key: STOP) is the chair's bisect.

**Design hazards:** E3 — the colour must never be able to CREATE a betrayal (pin the dead-band); COLOUR-NEVER-DROWN by construction (the cap derived from the base); the durability read's site — `peaceTerms.js` takes ZERO bytes, STOP + extraction if the leaf has no road; the SIXTH-COUPLING trap — the volume's `sacredClaim.js` threshold seam is REFUSED; the no-double-count walker must be pointed INWARD (the leaf imports none of the five); the neutral centre `+0` (a `−0` breaks `toBe(0)` — `deityStance.js:161`'s own `+ 0`); the seed with no reader is DECLARED, not hidden.

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (NONE — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title (cars 0, 1, 2, 3, 4); the mutation manifest per new invariant file; no wiring census stamp (no prose byte in this lane); the edge bundles at cars 1 and 4; the Seat/Lane trailer `Seat: Opus 5 — Fable-unvalidated` · `Lane: 16b`; porcelain 0. At car 1 (the dark mint) additionally the engine-gated walker (directions 1–3), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two freshness tests, the totality walker. At car 2 additionally `pactFormationDormancyFence` and the `peaceTerms.js` 797 count. At car 4 (built on the consist after D0, lane 16a and lane 5 — ADDENDUM R2(iii); never gated in the dock) additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, `tradeConvergenceContract.walker`, `faithSpreadCohortFence`, the espionage fence 21/21, `prose-manifest-cells.mjs --out` base/tip, the generation classified diff 525/525 unchanged, the soak suites alone at load < 8.
