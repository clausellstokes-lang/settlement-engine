# THE LANE — PRICING §C lane 56b · TR-8 THE TRAVELING FACTOR (`factorErrandsEnabled`; two slices) — the second sitting of lane 56

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED / PLAUSIBLE as marked; re-measure by SYMBOL. Protocols: `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 6 (1–6), range 5–6** (PRICING §B 4–5; HISTORY §A.4 4–5 "spelling ruling `factorErrand.js` vs `commercialErrands.js` at charter"; +1 for the LIT car). Slice 1 = cars 1–2; slice 2 = cars 3–4; news car 5; mint/LIT car 6.

**Prerequisites:** TR-5 (lane 2 — `petition_pact`'s transport switch), TR-7 (lane 56a — the supercargo binding), SP-D landed (`errandMint.js`, `errandSpineActive` `:76-78`, the four live consumers), WR-7b's hold writer landed (`foreignGuestHold.js` — `openForeignGuestHold` `:414`, `closeForeignGuestHold` `:478`, `restoreForeignGuestHold` `:522`), `errandSpineEnabled` LIT — ⚠ it is a DORMANT register member today (`simulationRules.js:260-270`) and PROTO §3.1A says "the first lane lighting any errand consumer (TR-8, WF-2, IN-4, ES-7, lane B's W-OPS car B)" re-charters `errandSpineDormancyFence.test.js` (BOTH polarities pinned: `espionageGate.js:80` reads `!== true` as a LIGHTING-ORDER precondition); by this lane's boarding lane B / IN-4 / WF-2 have EXPECTEDLY lit it — CHECK-GIT-FIRST; if not, car 6 lights it and re-charters the fence (the espionage gate's `!== true` polarity pin must stay green — lighting the spine does NOT light espionage). SERIAL on `envoyErrandVocabulary.js` (PROTO §7.1: lane B (wiring B) → IN-4 → WF-2 → **TR-8** → ES-6b/ES-7; each adds one row).

**THE SPELLING RULING (chair, vetoable, recorded in car 1):** the CODE's reserved row spells the consumer module `src/domain/worldPulse/factorErrand.js` (`envoyErrandVocabulary.js:315-321`, `built: false`, `purposeClass: 'commercial'`, `wave: 'TR-8'`); the volume says `commercialErrands.js`. **The code's spelling wins** (the registry row IS the landing surface and its `module` address is a single string a walker resolves); the volume is amended (dock copy) with a dated bracket. ⚠ `factorErrand.js` matches NO TRADE `LAYER_PATTERNS` stem — car 1 adds `factor[A-Z]|factorErrand` to the TRADE pattern (`tests/lint/couplingInclusion.walker.test.js:180-184`) in the same commit (the CR-FP-11 arm-A precedent), because renaming the leaf would break the registry row's address.

**Shared files this lane declares, slack at the cut:** `src/domain/worldPulse/envoyErrandVocabulary.js` 367/800 (the ONE row flip) · `src/domain/worldPulse/errandMint.js` 87/800 (read; the mint head — a factor errand mints THROUGH it) · `src/domain/worldPulse/envoyErrandTransit.js` 180/800 (read — the ONE transit kernel; the week floor) · `src/domain/worldPulse/pactProposals.js` 161/800 (`pactTransportFor` `:250` — the TR-5 transport switch, ≤4 lines, GRAMMAR's file, a coupling row) · `src/domain/worldPulse/foreignGuestHold.js` 424/800 (read; its writer is CALLED) · `src/domain/worldPulse/ransomClaim.js` 178/800 (read; `mintRansomClaim` `:178` — NOT persisted, owner item 8) · `src/domain/worldPulse/merchantVentureLegs.js` (TR-7's; the supercargo binding) · `src/domain/worldPulse/commercialReceiptPools.js` · `tests/lint/namedPersonTransitTotality.walker.test.js` (the speed-floor walker — extended) · the flag-mint surfaces + `SIM_LIT` + `subsystemRowsTrade.js` · `tests/property/errandSpineDormancyFence.test.js` (car 6, only if this lane lights the spine).

## THE STATE OF THE RECORD

- **The history fold** (`…/recon-32-34/HISTORY-TRADE-2026-09-15.md`; `HISTORY-TR-…` does not exist under that name): TR-8 **UNBUILT (spine landed as SP-D)** — "the commercial consumer row is reserved IN CODE at `built: false` (`envoyErrandVocabulary.js:316-321`), spelled `factorErrand.js` where the volume says `commercialErrands.js`; `commercialErrands.js`/`factorErrand.js` absent." Also ODQ §46c/OWNER-QUESTIONS item 8: NPC ransom is BUILT and dark (`ransomClaim.js:133-215`), "NOTHING IS PERSISTED HERE, AND THAT IS AN OWNER GATE" (`envoyRansomStage.js:39-42`) — the ransom ARM of slice 2 is DECLARED DEGRADED (capture → hold → release) until the owner rules the key (the chair's recommendation: "rule the key on the `foreignGuestHolds` pattern").
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §4 TR-8 (two slices; "the speed-floor walker extends to commercial errands (every movement site through WR-7a's one transit kernel — totality)"; "capture lands in `foreignGuestHold.js` through ITS one writer (S34 — consumed, import pin proves no second hold writer)"; "any WR lane in flight on envoy files is a STOP-and-report"), §5a seam 4. Design law `docs/DESIGN_FP_TRADE.md` §5 TR-8 (`:1162-1231`): slice 1 kinds {pact proposal/renewal (the TR-5 transport switch — Seam One's lit arm: the crossing mints an errand; the abstract rounds path suppresses at the call site, dark byte-identical — the WR-7a suppression idiom verbatim), venture supercargo, fair circuit (terms-shopping N markets, carrying home believed bands as ARRIVAL-grade writes)}; THE SNAPSHOT (K.2) decays by rumour, never truth-refreshed (K3 structural set); slice 2 the compromised factor (declared vs true purpose on the covert seam; "the TRUE stock book handed over — treachery transmits what loyalty cannot") + the prize (interception weights band above plain travellers; capture into WR-7b's holds; ransom on the I2 person-subject claim; the ransomed factor returns carrying what captors let them believe). Compiled programme §5 #30 (line 1710). The dependency clause `DESIGN_FP_TRADE.md:283-286` `TR-8 ⇒ SP-1 landed + TR-5 + TR-7 (…) + WR-7b's hold writer landed (…)` → `tradeConvergenceContract.js:362-…` (`requiresTradeFlags: ['tradePactsEnabled','venturesEnabled']`, `requiresForeignFlags: ['errandSpineEnabled']`, landed work WR-7b).
- **The owner's laws:** built lit; tuning raw (purpose weights, circuit cap, prize weights, snapshot mutation, vetting — REUSE WR-7d's vetting band, never mint) ratified at the soak; no new persisted state (errands ride SP-D's ledger, holds ride WR-7b's, the term sheet rides the errand record — the volume's lifecycle line; the RANSOM key is the owner's); nothing pushed/deployed/applied/deleted/spent.

## THE STATE OF THE CODE (CONFIRMED)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/envoyErrandVocabulary.js` | 367/800 | `ERRAND_CONSUMERS` rows `:295-360`: WR-7a embassies (built), GR-2 `pact proposals` (built — "there is no separate `pactErrand.js` and there must not be one"), **`factors` / `commercial` / `factorErrand.js` / `TR-8` / `built: false`** `:315-321`, legates + pilgrims (WF-2b), covert missions (ES-1 built), couriers (IN-4), ambitious (INT-3b built); `PURPOSE_CLASS_SET`, `purposeClassOf` |
| `src/domain/worldPulse/errandMint.js` | 87/800 | `errandSpineActive(worldState)` `:76-78` — THE ONE door; the mint head every consumer mints through; the W-OPS envoy-task-catalog door `:81-…` |
| `src/domain/worldPulse/envoyErrandTransit.js` | 180/800 | the ONE transit kernel (WR-7a; the week floor law M); `normalizeRoutePlan` |
| `tests/lint/namedPersonTransitTotality.walker.test.js` | — | the speed-floor TOTALITY walker over every movement site — car 2 extends its census to the factor consumer |
| `src/domain/worldPulse/envoyErrandRecords.js` | — | the errand DTO (`errandSpineBlock`, `normalizeCovertMission`); the term sheet in transit rides the record (`:555/:590` the DTO widening PRICING names as ES-7's OWNER gate — TR-8 adds NO DTO field: the snapshot rides the existing `payload`/`evidence` slot — verify by symbol) |
| `src/domain/worldPulse/pactProposals.js` | 161/800 | `pactTransportFor({worldState, fromId, toId, tick, arrivalTick})` `:250` — the abstract bounded-rounds transport; the LIT arm (a commercial errand carries the proposal) switches HERE with the WR-7a call-site suppression idiom |
| `src/domain/worldPulse/foreignGuestHold.js` | 424/800 | `openForeignGuestHold` `:414` / `closeForeignGuestHold` `:478` / `restoreForeignGuestHold` `:522`, `foreignGuestHoldForErrand` `:404` — THE ONE hold writer (an import pin proves no second) |
| `src/domain/worldPulse/ransomClaim.js` | 178/800 | `mintRansomClaim({hold, tick, homeId, worthBand})` `:178` (the I2 person-subject claim, BUILT, not persisted — owner item 8); `ransomAgainstSilence` `:297` |
| `src/domain/worldPulse/espionage/espionageGate.js` | — | `:80-81` `errandSpineEnabled !== true` ⇒ false — the LIGHTING-ORDER precondition (lighting the spine does not light espionage) |
| `tests/property/errandSpineDormancyFence.test.js` | — | SP-D's fences; BOTH polarities pinned (the mint's `=== true`, the gate's `!== true`) |
| `src/domain/worldPulse/commercialBeliefScarcity.js` (TR-3), `merchantHouseFactor.js` (TR-2), `merchantVentureLegs.js` (TR-7), `commercialDemandTrigger.js` (TR-5) | — | the belief read, the cast factor, the supercargo binding, the pact crossing |
| ABSENT | — | `factorErrand.js`, `commercialErrands.js`; `factorErrandsEnabled` (zero hits) |

**Budgets:** worker EXACT; `engine` ±0; first-paint: one (or two, if the spine) `SIM_LIT` lines, quoted.

## ALREADY DONE (do not re-land)
SP-D's spine (the mint head, the transit kernel, the records, the vocabulary with the reserved row); WR-7b's hold writer; the I2 ransom claim (dark, unpersisted); GR-2's abstract transport; the K3 belief seam tests (`tests/domain/envoyK3BeliefSeam.test.js`); the send-two corroboration ladder (K4, INFO's).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-8`; PACKET caps.

### Car 1 — slice 1: `factorErrand.js` — the commercial consumer on SP-D's spine (pact carriage · venture supercargo · the fair circuit), the row flipped `built: true` in the SAME commit

**In plain words:** a house's factor can now travel on the same road machinery envoys use — carrying a pact proposal to a neighbouring court, riding along as the supercargo of a venture, or touring several markets and bringing home what he believes they were paying — and the registry that lists every kind of traveller flips its reserved "factors" row to built in the very same commit.

**The change.** `factorErrand.js` (≤250 eff): `factorErrandsActive(rules)` — THE ONE by-name read `rules.factorErrandsEnabled === true`; the closed errand kinds `{pact_carriage, venture_supercargo, fair_circuit}` on purpose class `commercial` (`PURPOSE_CLASS_SET`), minting THROUGH `errandMint.js`'s head (never a private clock; the transit kernel is WR-7a's); the factor CAST per TR-2's read (`castHouseFactor`); THE SNAPSHOT (K.2): believed market bands (TR-3's read) + the house's term sheet frozen at departure into the errand record's existing payload slot, MUTATING by rumour along the route (SP-D's rumour hooks), NEVER refreshed from truth (the K3 structural set: import pin — `factorErrand.js` imports no truth-side scarcity module; token scan; guard-the-guard); `fair_circuit` visits N markets (`FACTOR_TUNING.CIRCUIT_CAP`) and on each arrival writes the market's TRUE band home as an ARRIVAL-grade report through SP-B's `foldSubjectAxes` reports channel (the richest honest write — the existing machinery, no new writer); `pact_carriage`: the TR-5 TRANSPORT SWITCH — `pactProposals.pactTransportFor` (+≤4 lines, GRAMMAR's file, a CW-0 coupling row) returns the errand transport when `factorErrandsActive` and a house factor is castable, else the abstract rounds; the abstract path SUPPRESSES at the call site when the errand carries (the WR-7a suppression idiom verbatim; dark byte-identical); `venture_supercargo`: the factor RIDES the venture's legs (TR-7's `merchantVentureLegs` binds the errand to the legs' clock — interceptable, ransomable). `envoyErrandVocabulary.js:319` `built: false` → `built: true` in THIS commit (the both-ways walker: an unregistered minter reds; a consumerless row reds). The LAYER_PATTERNS stem added (the spelling ruling). The volume's `commercialErrands.js` amended to `factorErrand.js` (dock copy).

**Pins / negatives:** each kind mints through the head and appears in the ledger with the right purpose class; the suppression-at-call-site pin (lit ⇒ the abstract rounds do NOT run for a carried proposal; dark ⇒ byte-identical to GR-2's fixtures); the K.2 snapshot decays (a rumour on the road moves the carried band; a truth change at the destination does NOT); the letters-home write moves the origin's belief on the next fold; the vocabulary walker green (`tests/lint/…errand…walker`); dark identity. Mutants: a truth refresh planted in the snapshot → the K3 scan reds; the row left `built: false` → the walker reds. **Files:** the leaf, `envoyErrandVocabulary.js` (one word), `pactProposals.js` (+≤4), `couplingRegistryTrade.js`, `tests/lint/couplingInclusion.walker.test.js` (the stem), `tests/domain/factorErrand.test.js`, the volume (dock). **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + the errand vocabulary walker, `errandSpineDormancyFence` (its polarity census: this leaf reads `factorErrandsEnabled` by name and `errandSpineActive` through the head — never `errandSpineEnabled` by name). **LIT act:** none (the by-name read ⇒ THIS car pays the MINT half dark — PROTO §5.1; the row in `subsystemRowsTrade.js`; car 6 lights). **Exit:** three kinds mint; the row built; the switch suppresses.

### Car 2 — the speed-floor walker extended + the stale-arrival T-8 TELLABLE + the fair-circuit cap

**In plain words:** the permanent test that every travelling person moves through the one transit kernel at no faster than a week per hop now covers factors too; and the story of Factor Maren, who rode for the fair with one price in her head and found another at the gate, is pinned as a test.

**The change.** `tests/lint/namedPersonTransitTotality.walker.test.js`: the census gains `factorErrand.js` as a movement-site consumer (totality — every movement through `envoyErrandTransit.js`); T-8 (`tests/domain/factorErrandTellables.test.js`): snapshot band ≠ gate band on arrival, receipted ("rode for the fair with one price in her head, and found another at the gate") — the wrong-market tragedy at person grain; the circuit cap binds; the week floor holds for a factor (a fixture that would move faster reds the kernel's own floor). Mutants: a movement site outside the kernel planted in `factorErrand.js` → the totality walker reds. **Files:** the walker, the tellables test, `factorErrand.js` (≤4 lines if the receipt needs a slot). **Census bills:** manifest; refreeze own commit. **Byte rule:** build quoted if src moved. **Gate:** full list. **LIT act:** none. **Exit:** totality green with the factor; T-8 pinned.

### Car 3 — slice 2a: the COMPROMISED FACTOR — declared vs true purpose on the covert seam

**In plain words:** a factor sent for the house may truly travel for a rival or for the corruption web's patron — tilting the terms he carries and handing over the house's true stock book — and a careful seat that vets its people catches him where a hurried seat that takes the volunteer bleeds.

**The change.** `factorErrand.js` (+≤60, or a sibling `factorErrandCovert.js` ≤120 if the cap binds — `factor[A-Z]` matches the stem): the DECLARED vs TRUE purpose rides SP-D's covert seam (`normalizeCovertMission`'s shape — READ; ES-1's `covert missions` row is the sibling consumer; the factor's covert arm is TRADE's, minted on the commercial row, never on ES's); the true purpose tilts the carried terms (a banded tilt) and hands the TRUE stock book (the one honest channel — a betrayal: the K3 paradox inherited) to the patron; the vetting read REUSES WR-7d's vetting band (`VETTING_QUALITIES` in `espionageMath.js` — READ by symbol; never a second ladder) — careful seat vets and catches (the factor is recalled, receipt), hurried seat takes the volunteer and bleeds (the tilt lands); the corruption web receives the traitor's purpose as material (`corruptionWeb.js`'s existing intake — by call). Pins: both arms on two fixtures; the tilt is banded (prose-numerics); dark identity. Mutant: the vetting arm severed (careful seat never catches) → reds. **Files:** the leaf(s), `couplingRegistryTrade.js` (TRADE→INFO corruption row), tests. **Census bills:** manifest; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + the ES fences (`espionageDormancyFence` 21/21 — TRADE's covert arm must not add an importer of `missionDispatcher.js`; the chain arm is lane B's). **LIT act:** none. **Exit:** both arms pinned.

### Car 4 — slice 2b: THE PRIZE — capture into WR-7b's hold through ITS one writer; the ransom arm DECLARED DEGRADED (owner item 8)

**In plain words:** a factor on the road is a rich prize — term sheets and market knowledge — so interception weights him above a plain traveller; a captured factor is held by the existing foreign-guest machinery through its one writer; and because the owner has not yet ruled how ransom claims are saved, the capture resolves as hold-then-release with the ransom arm written down as deferred.

**The change.** `factorErrand.js` (+≤40): interception prize weight band (`FACTOR_TUNING.PRIZE_WEIGHT_BAND`) applied in the transit kernel's interception read (by the existing hook — READ the kernel's interception shape at charter; ≤2 lines in `envoyErrandTransit.js` only if no hook exists — then a coupling row); capture → `openForeignGuestHold` (WR-7b's writer, BY CALL; the import pin: `factorErrand.js` imports the hold module's writer and NOTHING mints a hold elsewhere — `tests/lint/foreignGuestHoldWriters…` if it exists, else authored here as a census); the RANSOM ARM: `mintRansomClaim` (BUILT, dark, unpersisted) is CALLED to price the worth band and the RECEIPT is minted, but NO claim is persisted — capture resolves `hold → release` on the existing release road (`closeForeignGuestHold` with reason `release`) — DECLARED DEGRADED with the anchor (OWNER-QUESTIONS item 8; `envoyRansomStage.js:39-42`), and a pre-pin: the day a persisted ransom key lands (the owner's `foreignGuestHolds`-pattern ruling), the factor's claim joins it or the pin reds; the ransomed/released factor returns carrying what captors let him believe (the plant-exposure idiom — his snapshot mutated by the captor's rumour). Pins: capture → hold → release round-trip on war's machinery (no fork); the import pin (no second hold writer); JSON-alias round-trip (the factor IS a roster npc); DM-freed captive closes the hold through the hold's writer; DM-KILL closes the errand `lost` through SP-D's writer and (TR-7) the venture `failed`. Mutant: a second hold writer planted → the census reds. **Files:** the leaf, the hold-writer census (new or extended), tests. **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list. **LIT act:** none. **Exit:** the round-trip; the degraded arm declared with its anchor.

### Car 5 — `factorErrandNews.js`: the Herald's factor sentences with the five joins and their OWN walker

**In plain words:** departures, silences ("No word from the salt road; House Verren fears for its factor"), captures and returns in the commercial voice — each with its pool, desk, address chain and walker; the K.7 inference receipts ("the court fears the worst") at commercial stakes.

**The change.** Kinds `commercial_factor_departed`, `commercial_factor_silent` (K.7 inference), `commercial_factor_captured`, `commercial_factor_returned` — registry leaf (`factor[A-Z]` stem), pool rows in `commercialReceiptPools.js` (SPLIT into `commercialReceiptPoolsFactors.js` if the file nears 800 — by this lane's boarding TR-5/6/7 have each added rows: MEASURE FIRST; teach the walker both leaves), the TRADE annex rows, `WHAT_PHRASES`, `heraldRouting.js` `trade` desk, the address chain, `tests/lint/commercialFactorKindPools.walker.test.js` (NEW), `heraldContaminationFence`, pacing-registered, the metronome latch; the dossier house entry gains the factor line ("Factor Maren Verren — on the road to Threeways, expected by midsummer" / "overdue; no word since the spring" — ONE user-facing surface, TR-2 car 4's leaf). **Files:** ≈8. **Census bills:** wiring census stamp; prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + the kind walkers, `heraldContaminationFence`, `proseWiringCensus.walker`, `metronomeCooldownLint`, `tests/components`. **LIT act:** none. **Exit:** four kinds × five joins; the dossier line.

### Car 6 — THE FLAG-MINT / LIT CAR: `factorErrandsEnabled` lit in the four lit presets (+ `errandSpineEnabled` if this lane is first)

**The change (PROTO §1.2; the mint half was car 1):** `SIM_LIT` gains `factorErrandsEnabled: true, // TR-8 car 6`; conjuncts `tradePactsEnabled` + `venturesEnabled` EXPECTED lit (lanes 2, 56a); `errandSpineEnabled` — CHECK-GIT-FIRST: EXPECTED lit by lane B / IN-4 / WF-2; if ABSENT, this car lights it (`errandSpineEnabled: true, // SP-D's key — lit by TR-8 car 6 (the first errand consumer to light)`) and RE-CHARTERS `tests/property/errandSpineDormancyFence.test.js` into the LIT shape with BOTH polarity pins kept (the `espionageGate.js:80` `!== true` read is a lighting-order precondition and STAYS; `espionageEnabled` stays dark — assert `espionageDormancyFence` 21/21 and the ES chain arm untouched); the four live SP-D consumers (embassies, pact proposals, covert missions, ambitious) LIGHT with the spine where their own doors are lit — a substantive GR/WR/INT movement this car DECLARES by name in the shift ledger and the chair relays to those families. `tests/property/factorErrandsDormancyFence.test.js` (NEW at car 1, dark) → LIT shape (site census: `factorErrand.js` the ONE `=== true`; the lit twin under `full_simulation` MOVES the mint count). The witness re-recorded by hand (PLAUSIBLE: a factor needs a house + a pact crossing or venture in 52 ticks — likely zero; the spine, if lit here, WILL move `worldStateSha256` on all four rows through embassies — attribute per key by a settling experiment). The shift-ledger draft; census literals; `contributionLedgerShape` off HEAD; the edge bundles; the row text; the espionage fence 21/21 and prose cells zero quoted. **Files (≈9).** **Gate:** full list + the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence, `errandSpineDormancyFence`, the soak suites alone at load < 8. **Exit:** the key(s) absent from `ENGINE_GATED_DORMANT_RULE_KEYS`; the fence(s) LIT-shape green; the witness diff per key; the shift draft; the family notices.

---

## THE FLAG-MINT CAR (last; the count from HEAD)
MINT at car 1 (dark; literals off HEAD, +1), LIGHTING at car 6 (+1 more literal if the spine is minted here — it is ALREADY registered, so NO literal moves for the spine; only its `SIM_LIT` line and fence), REBASED at landing; conflicts resolved by MEASUREMENT (PROTO §5.2).

## OWNER-GATED, KEPT OUT
The ransom persistence key (OWNER-QUESTIONS item 8) — declared degraded with its anchor; the chartered arbitrary-target DM verb / the errand DTO widening (`envoyErrandRecords.js:555/:590` — ES-7's OWNER gate; TR-8 adds NO DTO field) · tuning (`FACTOR_TUNING`) raw, ratified at the soak; the vetting band REUSED from WR-7d, never minted · push/deploy/migration/deletion/spend · the golden master · no new persisted state (recorded).

## RISKS
**Grep traps:** `commercialErrands.js` (the volume's spelling) finds NOTHING — the row says `factorErrand.js`; `couriers` rows are IN-4/ES-1's (split — "IT TAKES ITS OWN ROW AND NEVER SHARES ONE", `DESIGN_FP_ARCHITECTURE.md:2620`); `roaming` (72 files) is the roaming-stressor twin, not captivity; `Emergency extraction` is a catalog toggle with no reader; `INSTR-1≠TR-1`. **Collision surfaces:** `envoyErrandVocabulary.js` (SERIAL lane B → IN-4 → WF-2 → TR-8 → ES-6b/ES-7 — one row each; CHECK-GIT-FIRST); `espionageGate.js`/`missionDispatcher.js` (never touched — the ES chain arm is lane B's); `pactProposals.js` (GRAMMAR's; GR-5 lanes); `foreignGuestHold.js` (WR-7b's — by CALL only; any WR lane in flight on envoy files is a STOP-and-report); the flag-mint surfaces; `subsystemRowsTrade.js`; `commercialReceiptPools.js` (the SPLIT — measure first). **Load hazards:** `gate-mutex.sh` false green; dist-gated suites skip; `deno.lock`; `git grep` reads the INDEX; soak suites alone at < 8; a moved DRIVEN GOLDEN is a STOP. **Design hazards:** the spine's TWO polarities (lighting `errandSpineEnabled` must leave `espionageGate.js:80`'s `!== true` precondition green and espionage dark); the factor never reads `errandSpineEnabled` by name (it goes through `errandMint`'s head); one transit kernel (the totality walker); one hold writer (the import pin); the snapshot is never truth-refreshed (K3); the ransom claim is minted as a RECEIPT, never persisted.

## GATES (per car, verbatim)
focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file; the wiring census stamp at car 5; prose-numerics at cars 3 and 5; the edge bundles at cars 1 and 6; the errand vocabulary walker and `namedPersonTransitTotality.walker` at cars 1–2; the Seat/Lane trailer; porcelain 0. At car 6 additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence 21/21, `errandSpineDormancyFence`, the soak suites alone at load < 8; at car 5 additionally `tests/components`.
