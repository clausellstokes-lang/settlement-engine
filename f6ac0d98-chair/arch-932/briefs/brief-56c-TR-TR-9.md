# THE LANE — PRICING §C lane 56c · TR-9 TRADE CONVERGENCE INSTRUMENTATION (no flag; the family terminus) — the third sitting of lane 56

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED / PLAUSIBLE as marked; re-measure by SYMBOL. Protocols: `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 5 (1–5), range 4–6** (PRICING §B 4–6; HISTORY §A.4 unchanged "WR-9 took five commits after its contract"). **No flag, no flag-mint car, no LIT act** — TR-9 is measurement; PRICING §B: "DONE-WHEN is the owner's soak". **THE LANE-56 OVERRUN (the skeptic's minor gap; stated in full in brief 56a, which boards first, and pointed at from here):** 56a (5) + 56b (6) + 56c (5) = 16 cars at centre against PRICING §C lane 56's 11–15 for TR-7 → TR-8 → TR-9 — ONE over the top of the range; the excess is the LIT cars the built-lit regime adds to the two flagged waves (56a car 5, 56b car 6), and the family summary's "+5 overall" already counts them; nothing else in the three briefs grew. **CORRECTED 2026-09-15 22:3x at the skeptic's findings, under the chair's `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` (read it before car 5):** car 1's corpora bill was WRONG and would have broken a green pin — it is REPLACED (the `rumorFallbackPhrasePools.test.js:220-221` literals are BOTH 107, describe RECEIPT_POOLS_LEGACY.md §4, and do NOT move); car 5's lighting-law assertion is rewritten against the evaluator's REAL vocabulary and against door D0 (all seven presets `ok: true`; `DORMANT_BY_CONFIG` is not an evaluator output); the walker's `:367` VIRTUAL arm is D0 car A's to re-cut (ADDENDUM R1) — car 5 ASSERTS the re-cut arm, never edits it. **Prerequisites:** door D0 (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` §3 — car A re-cut the `:367` virtuality arm of THIS lane's walker file and minted the `SIM_LIT` cohort; car B lit `commodityFlowEnabled` in all four lit presets); TR-1..TR-8 on the consist (lanes 7, 55, 1, 2, 8, 56a, 56b — the family in build order); the eight TR keys LIT through `SIM_LIT` (each lane's LIT car — the LAST car of each lane under ADDENDUM R3); the convergence-wave siblings for a non-degenerate sky are CW-3's concern (PRICING §C lane 15), not this lane's.
**Shared files this lane declares, slack at the cut:** `src/domain/certification/tradeConvergenceContract.js` 398/800 (the envelopes + the tuning table; +≤150) · `src/domain/display/settlementRumors.js` 555/800 (five `route_*` WHAT_PHRASES rows) · `src/domain/realm/heraldRouting.js` 273/800 (five rows) · `src/domain/worldPulse/routeNetworkCharter.js` 308/800 + `routeNetworkDecay.js` 335/800 (READ — the candidate kinds) · `scripts/audit/behavioral-observation.mjs` (the deciding-component histogram + trend envelope, `:988-1058`) · `scripts/audit/whole-world-soak.mjs` (the v5 `tradeConvergence` slot; the honest-empty precedent) · `tests/perf/tickScanBudget.test.js` (new lanes) · `src/domain/certification/subsystemRowsTrade.js` (READ; totality asserted) · `tests/lint/tradeConvergenceContract.walker.test.js` (extended). NOT touched: `simulationRules.js` (no key), `subsystemRowsVirtual.js`.

## THE STATE OF THE RECORD

- **The history fold** (`…/recon-32-34/HISTORY-TRADE-2026-09-15.md`; `HISTORY-TR-…` does not exist under that name): TR-9 **PART-BUILT (9c only)** — "the contract module (736 lines) + walker test (804 lines) (`c7933e845`, 2026-08-04). Nothing in src imports it — three comment mentions only; the sole importer is its walker test. Its own owed list is still owed: the five `route_*` candidate kinds (`routeNetworkCharter.js:138`, `routeNetworkDecay.js:123-137`) have ZERO `WHAT_PHRASES` and ZERO `heraldRouting` rows, so the 17-module route estate (J1 `ed5ac49dd`, dark behind `routeLifecycleEnabled: false`) still does not speak." HISTORY §A.3: J1 is TR-9's Herald debt; `commodityFlowEnabled` lit at §903 is "the largest live trade-behaviour change in the estate".
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §4 TR-9 ("SLICE TR-9c LANDED 2026-08-05 — THE CONTRACT MODULE ONLY … STILL OWED at the collector waves: every envelope and its tuning table, the receipt joins, the Herald T-11 rows, the tickScanBudget lanes and the eight certification rows. Bands were deliberately NOT authored here: an acceptance band for an instrument that has never run is a signature this slice has no authority to give"; the three cross-program preconditions resolved in the module: SP-1 = `errandSpineEnabled`, SP-2 = `believedScarcityEnabled`, SP-3 = `pactFormationEnabled`); "Certification rows for all eight flags (converting §2's declared-pending entries — Q4)" — Q4 is DEAD (`subsystemRowsVirtual.js:595`): every row was AUTHORED at its mint; THIS lane VERIFIES totality. Design law `docs/DESIGN_FP_TRADE.md` §5 TR-9 (`:1232-1305`): `TRADE_ENDING_KEYS = {fortune, ruin, monopoly, collapse, severance, cornered}` CLOSED; `collapse` an HONEST PERMANENT ZERO while `routeLifecycleEnabled` is dark; the deciding-component histogram (provision/circulation/sustenance moving realmSelfSufficiency each year) + the SELF-SUFFICIENCY TREND ENVELOPE; health guards (house Gini, corner frequency/duration, pact formation/breach mix, route endings mix); the v5 receipt section `tradeConvergence` with honest-empty emission (the `whole-world-soak.mjs:632` precedent); the scan ratchet (tickScanBudget lanes for flows/supplyKernel/commodityFlow/entrepotKernel + every TR kernel); every window INTERVAL_WEEKS-derived (seasons 13 / years 52), SP-7-asserted; "Declared empty, with reasons"; "This wave is the acceptance harness: the program is DONE when these envelopes hold on the owner-ordered soak, and not before." Compiled programme §5 #31 (line 1727); the §5 CQ2 note ("the CQ2 set stays exactly GR-0 + GR-1 + TR-1 + TR-9-contract"). PRICING §B TR-9: "WR-9 took five commits after its contract".
- **The owner's laws:** tuning bands (envelope shapes: endings shares, corner frequency/duration, house Gini ceiling, trend tolerance) land RAW with executed derivations and are ratified ONLY at the soak under THE PROMISE; the soak itself and its grading are the OWNER's (the terminal soak, #24); nothing pushed/deployed/applied/deleted/spent; no persisted world state (envelopes, rows, receipt fields only).

## THE STATE OF THE CODE (CONFIRMED)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/certification/tradeConvergenceContract.js` | 398/800 | `TRADE_RULINGS_FLAG_KEYS` (eight) `:137-146`; `TRADE_FLAG_RULE_STATES`, `TRADE_FLAG_CERTIFICATION_VERDICTS`; `TRADE_FLAG_LIGHTING_ROWS` `:305-363` (the eight rows with `requiresTradeFlags/requiresForeignFlags/requiresLandedWork` — NOTE lane 1 car 6 narrows TR-4's row to `['commodityFlowEnabled']`); `evaluateTradeFlagLighting` `:493-525` (the out-of-order law, EXECUTABLE: two violation kinds ONLY — `trade_precondition_dark` `:497-510`, `foreign_precondition_dark` `:511-522`; returns `{ ok, violations }`; a non-object `rules` is the all-dark config); `TRADE_FLAG_CERTIFICATION_VERDICTS` `:149-154` (`ALIVE`/`DORMANT_BY_CONFIG`/`SILENT`/`UNOBSERVED` — an OBSERVATION row's words, never the evaluator's); `TRADE_DEGRADED_ARMS` `:392-405` (two declarations, not gates); `tradeEndingAvailability` (kind-availability); `TRADE_ENDING_KEYS`; the ending mint table with its one honest-permanent-zero field; the v1 observation shape with its totality wall; ZERO src importers |
| `tests/lint/tradeConvergenceContract.walker.test.js` | 804 lines | the census parses `DESIGN_FP_TRADE.md` §3's dependency ruling (`:148-175` `VOLUME_DEPENDENCY_CLAUSES`, exactly-once) and requires the table to match — the INDEPENDENT DENOMINATOR; `:428` asserts each built flag's `=== true` read exists in the module its row names; `:577` reads `DESIGN_FP_SPINE.md` for the three preconditions |
| `src/domain/worldPulse/routeNetworkCharter.js` | 308/800 | `CHARTER_CANDIDATE_TYPE = 'route_chartered'` `:138`, `CHARTER_NEWS_TYPE` `:141` |
| `src/domain/worldPulse/routeNetworkDecay.js` | 335/800 | `PROMOTION_CANDIDATE_TYPE = 'route_promoted'` `:123`, `DEMOTION… = 'route_demoted'` `:126`, `ABANDONMENT… = 'route_abandoned'` `:134`, `REVIVAL… = 'route_revived'` `:137` |
| the 17 route modules | — | `routeNetworkCharter*` (4), `routeNetworkConsumers*` (5), `routeNetworkDecay*` (2), `routeNetworkFlows*` (4), `routeNetworkGenesis.js`, `routeNetworkLedger.js` — NO `routeNetworkNews.js` registry leaf (the five kinds have no L6 registry row anywhere) |
| `src/domain/display/settlementRumors.js` | 555/800 | `WHAT_PHRASES` `:123`; `route_disruption: 'the roads gone bad'` `:334` (an OLD kind, not J1's). **THE TWO LEGACY CORPORA ARE CLOSED AND THE FIVE ROUTE KINDS JOIN NEITHER (CONFIRMED — the first cut's "63 canonical + 107 fallback … 63 → 68 / 107 → 102" was WRONG):** `docs/content/RECEIPT_POOLS_LEGACY.md` (header `:1-11`: "the kinds the engine ALREADY MINTS AND ROUTES today") holds §3 = the 63 legacy kinds that carry an authored `WHAT_PHRASES` row (`tests/domain/rumorPhrasePools.test.js`, "CLOSED AT 63 OF 63" `:8/:96`; its only count pin is a FLOOR, `Object.keys(PARSED).length > 50` `:136`) and §4 = the 107 legacy kinds with NO row (`tests/domain/rumorFallbackPhrasePools.test.js:220-221` — BOTH literals are `toBe(107)`: `Object.keys(PARSED).length` and `FALLBACK_WIRED_KINDS.length`, `PARSED` parsed out of the doc at test time, so editing a literal without the doc reds the corpus join instantly). The five `route_*` kinds are NEW kinds with rows in the TRADE (or route) annex — neither literal moves, neither doc is edited. The live `WHAT_PHRASES` size pins are FLOORS: `tests/lint/informationKindPools.walker.test.js:219` `> 1`, `tests/lint/newsSubjectVocabulary.walker.test.js:380` `> 200`; `tests/lint/heraldRouting.walker.test.js:153` is a ROUTING obligation (every `WHAT_PHRASES` key explicitly routed), discharged by the five `heraldRouting.js` rows in the same commit |
| `src/domain/realm/heraldRouting.js` | 273/800 | `SECTION_OF`: `route_disruption: 'trade'` `:204`; no `route_chartered/promoted/demoted/abandoned/revived` |
| `scripts/audit/behavioral-observation.mjs` | 1,233 lines | `observeRealmSelfSufficiency` `:988`; the conditional `realmSelfSufficiency` slot `:1058`; the trade word list `:39` |
| `scripts/audit/whole-world-soak.mjs` | — | the restore probe / honest-empty precedent around `:628-640` (the `:632` cite rotted — locate `tradeConvergence`'s neighbours by the `soakRules.mjs:98 full_simulation` build) |
| `tests/perf/tickScanBudget.test.js` | — | `describe('tick SCAN BUDGET — the per-advance graph scans stay sub-quadratic (Wave 4)')` `:161`; `SEED` `:47`; per-lane budgets (the shape to extend) |
| `src/domain/certification/subsystemRowsTrade.js` (lanes 1/2/7/8/55/56a/56b) | — | the TR rows authored at each mint (+ TR-1's in `subsystemRowsCompact.js`) |
| `src/domain/certification/warConvergenceContract.js` | 515 eff | the TEMPLATE (envelopes, mutants, non-vacuity gates) |
| `docs/content/RECEIPT_POOLS_TRADE.md` | 107/790 (compiled programme line 2488) | the TRADE annex — the route kinds' pools land HERE (T-11 is TR-9's debt) or in the ROUTE_LIFECYCLE annex if one exists (check `tests/helpers/receiptAnnex.js`'s URL roster at charter) |

**Budgets:** worker EXACT (no generator byte); `engine` ±0 (the contract is a certification leaf; the news rows are display/realm — lazy); first-paint ±0 (assert: none of the touched files is in the eager closure table); `settlementRumors.js` 555/800 and `heraldRouting.js` 273/800 have room.

## ALREADY DONE (do not re-land)
TR-9c the contract module + its walker (`c7933e845`); the three cross-program preconditions resolved IN the module; the eight certification rows (AUTHORED at each mint by lanes 1/2/7/8/55/56a/56b — this lane VERIFIES); the J1 route estate (17 modules, dark) and its five candidate kinds; the whole-world soak's honest-empty precedent; `warConvergenceContract.js` as template; `commodityFlowEnabled` lit at §903 (the substrate's largest live change — the observation lane's first real signal).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-9`; PACKET caps.

### Car 1 — THE HERALD DEBT PAID (T-11): the five `route_*` kinds get the L6 five joins and ONE walker file of their own

**In plain words:** the road network built in August has been mute — a road chartered, promoted, demoted, abandoned or revived produces no Herald sentence anywhere; this car gives each of the five events its phrase pool, its desk, its address chain and a permanent test that reds if any of the five ever falls silent again.

**The change.** A registry leaf `src/domain/worldPulse/routeNetworkNews.js` (≤120 eff; `routeNetwork` matches the TRADE stem) — the `commercialReasonsNews.js` registry idiom for the five kinds (`route_chartered` 'notable', `route_promoted` 'routine', `route_demoted` 'routine', `route_abandoned` 'notable' — "Herald-worthy mourning", `route_revived` 'notable'), each row joined to the candidateType literal the route estate ALREADY mints (`routeNetworkCharter.js:138`, `routeNetworkDecay.js:123-137` — READ; the emitters are not edited); pool rows in the TRADE annex (or the route annex if `receiptAnnex.js` names one — the ONE reader) with `requiredSlots` (the road's two towns; the flow class); `WHAT_PHRASES` five rows — **THE CORPORA BILL, CORRECTED (the first cut's "63 → 68 / 107 → 102" would have BROKEN A GREEN PIN):** the five kinds are NEW and join NEITHER legacy corpus of `docs/content/RECEIPT_POOLS_LEGACY.md` (§3's 63 with-row kinds, "CLOSED AT 63 OF 63" — `rumorPhrasePools.test.js:8/:96`; §4's 107 no-row kinds — `rumorFallbackPhrasePools.test.js:220-221`, BOTH literals `toBe(107)`, `PARSED` parsed from the doc at test time), so those literals are NOT touched and that doc is NOT edited; the pools live in the TRADE annex (or the route annex, if `receiptAnnex.js` names one). THE MEASURED CHECK, replacing the guessed literals: after the five rows land, run every `tests/lint/*KindPools.walker.test.js`, `phrasedKindPools.walker`, `heraldRouting.walker`, `newsSubjectVocabulary.walker`, `candidateTypeVoicePhrasing.walker`, `settlementRumors.test.js`, `rumorPhrasePools.test.js`, `rumorFallbackPhrasePools.test.js` and CLASSIFY each red before touching any literal — the known `WHAT_PHRASES` size pins are FLOORS (`informationKindPools.walker.test.js:219` `> 1`; `newsSubjectVocabulary.walker.test.js:380` `> 200`; `rumorPhrasePools.test.js:136` `> 50`) and pass; `heraldRouting.walker.test.js:145-155` are ROUTING obligations (every scanned `candidateType` literal and every `WHAT_PHRASES` key explicitly routed — the five `route_*` literals ALREADY exist in src, so if they are not routed by a family prefix today that arm is red at HEAD and this car cures it; MEASURE which) and its `:157` SINGLE-PRODUCER arm must be run; an EXACT count pin that moves is REPORTED with its file:line and edited as a declared instrument edit in the same commit — none is expected; `heraldRouting.js` five `'trade'` rows beside `route_disruption` (even if a `route_` prefix already routes them, the exact rows are authored so each kind's desk is stated by name); the address chain (id + the road's settlementIds + typed action + recorded reason + audience — the emitters already carry `CHARTER_NEWS_TYPE`-shaped actions; the registry maps them); `tests/lint/routeNetworkKindPools.walker.test.js` (NEW — ONE walker for the family of five: the template `commercialKindPools.walker.test.js`, frequency-scaled floors; the T-11 TOTALITY pin: exactly the five candidate kinds parsed off the route modules' `*_CANDIDATE_TYPE` exports have a WHAT_PHRASES row, a heraldRouting row and a registry row — both directions); `heraldContaminationFence` green; pacing-registered.

**Pins / negatives:** totality both ways (a sixth `route_*` candidate without rows reds; a row without a candidate reds); the five joins per kind; the two legacy corpora UNTOUCHED and their pins green unchanged (`rumorFallbackPhrasePools.test.js:220-221` still `107`/`107`; `rumorPhrasePools.test.js`'s desk-roster equality `:173-175` still exact — the five kinds are NOT added to `WHAT_PHRASE_POOLS`, their variant pools live in the registry idiom's annex reader). Mutant: one WHAT_PHRASES row deleted → the totality pin reds; one `heraldRouting.js` row deleted → `heraldRouting.walker.test.js:152-155` reds (the routing obligation has eyes). **Files:** the registry leaf, `settlementRumors.js` (+5), `heraldRouting.js` (+5), the annex (docs/content — TRADE or route, by `receiptAnnex.js`'s roster), the walker; NO edit to `tests/domain/rumorFallbackPhrasePools.test.js`, `tests/domain/rumorPhrasePools.test.js` or `docs/content/RECEIPT_POOLS_LEGACY.md` (a red in any of those at this car is a DEFECT in the car, never a literal to move). **Census bills:** the wiring census stamp (a prose byte); prose-numerics (no digits in the phrases); manifest; refreeze own commit. **Byte rule:** build quoted (lazy); first-paint ±0 asserted. **Gate:** full list + every `*KindPools.walker`, `phrasedKindPools.walker`, `heraldRouting.walker`, `newsSubjectVocabulary.walker`, `candidateTypeVoicePhrasing.walker`, `heraldContaminationFence`, `proseWiringCensus.walker`, `rumorPhrasePools`, `rumorFallbackPhrasePools` (both green UNCHANGED — quoted). **Exit:** five kinds speak; totality reds its plant; every count pin that moved is named with its file:line and classified (floor passed / exact set edited as a declared instrument edit) — none expected. (The kinds fire only when `routeLifecycleEnabled` lights — J1's own declared false; this car makes the road estate ABLE to speak, and says so.)

### Car 2 — THE ENVELOPES and their tuning table: endings shares, corner frequency/duration, house Gini, pact mix, route endings mix — each with an executed mutant and a non-vacuity gate

**In plain words:** the auditor's yardsticks — how the six trade endings share the mass, how often and how long corners run, whether one house runs away with everything, how pacts form and break, how roads end — each written as a window measured in seasons or years, each raw and unsigned until the owner's soak, each proved able to red by a planted regression, and each refusing a verdict without evidence.

**The change.** `tradeConvergenceContract.js` (398 → ≤550): `TRADE_ENVELOPE_TUNING` RAW with derivations (endings share bands per `TRADE_ENDING_KEYS`; corner frequency per 13-week season and duration in seasons; house Gini ceiling over holdings bands — the `centralityGini` precedent, `entrepots.js:370`; pact formation/breach mix; route endings mix over the five kinds); every window INTERVAL_WEEKS-derived (`INTERVAL_WEEKS.one_season`/`one_year` — the SP-7 walker asserts it: run it); `evaluateTradeEnvelopes(observation)` → per-envelope `{verdict, evidence, window}` with NON-VACUITY (`'unobserved'` when the window carries no samples — a verdict requires measured evidence, the war gate's shape); `collapse` reports HONEST PERMANENT ZERO with its reason while `routeLifecycleEnabled` is dark (the field already exists — wire the reason). The walker (`tradeConvergenceContract.walker.test.js`, +arms): every envelope carries an executed MUTANT (a planted regression per envelope in `scripts/mutation-sweep.sh` reds its arm); every window is INTERVAL_WEEKS-derived (a hand-typed `13` reds). **Files:** the contract, the walker, `scripts/mutation-sweep.sh` + the manifest. **Census bills:** manifest (one label per envelope); refreeze own commit. **Byte rule:** build quoted (the contract is lazy certification; assert). **Gate:** full list + the SP-7 walker (locate by `INTERVAL_WEEKS` in `tests/lint`). **Exit:** every envelope reds its mutant; non-vacuity refuses an empty window; bands raw.

### Car 3 — THE SOAK JOINS: the deciding-component histogram, the trend envelope, the v5 `tradeConvergence` receipt slot (honest-empty from day one), SC-9 self-sampling

**In plain words:** the long-run observation scripts learn to record which of provision, circulation or sustenance moved a realm's self-sufficiency each year, whether it improves over time absent shocks, and a trade-convergence section that emits honestly-empty histograms until the mechanisms have run — plus the rule that this wave samples its OWN receipt rows so the coupling measure can tell "never sampled" from "sampled and silent".

**The change.** `scripts/audit/behavioral-observation.mjs` (+≤60, beside `observeRealmSelfSufficiency` `:988`): the deciding-component histogram over 52-week years (the data is in receipts today and reaches no field — the volume; the reader is a pure fold over the existing `realmSelfSufficiency` observation) and the SELF-SUFFICIENCY TREND ENVELOPE (monotone improvement absent shocks — `DESIGN_ROUTE_LIFECYCLE.md:113`'s prose made a check); `scripts/audit/whole-world-soak.mjs` (+≤40): the additive `tradeConvergence` envelope slot with honest-empty emission (the existing honest-empty precedent's shape — zero histograms are sequencing evidence, not failures); SC-9: `tradeConvergenceContract.js` names the receipt rows it samples (the `fortune`/`ruin` fields TR-7 car 4 declared; the corner/monopoly receipts; the pact receipts) — `CW-0w`'s helper samples them (PROTO: "each convergence wave samples its OWN rows; CW-3 reds never-sampled rows as UNOBSERVED"). ⚠ THE OSR QUESTION: a receipt SHAPE read by the audit scripts is not persisted world state; `tests/lint/observedShapeReaders.walker.test.js` inventories READERS of observed shapes in `src/` — the scripts are outside `src/`; if the walker's scope reaches `scripts/audit`, the new reader is REPORTED to the chair for #20's one trailing mint, never added to the baseline by this lane. **Files:** the two scripts, the contract (+the sampled-rows manifest), tests under `tests/audit`/`tests/scripts` (locate the scripts' existing test homes). **Census bills:** manifest; refreeze own commit. **Byte rule:** no dist byte (scripts) — state so; the contract's edit is lazy. **Gate:** full list + the scripts' own tests + `observedShapeReaders.walker` (expected unchanged; a change is a REPORT). **Exit:** the histogram and trend fold pinned on a fixture observation; the slot emits honest-empty; SC-9 rows named.

### Car 4 — THE SCAN RATCHET EXTENDED: tickScanBudget lanes for `flows`/`supplyKernel`/`commodityFlow`/`entrepotKernel` and every TR kernel

**In plain words:** the per-tick cost test that keeps the engine from growing quadratic gains lanes for the trade machinery — the caravan, commodity, entrepôt and route-flow kernels and every kernel this programme added — so "cheap by prose" becomes a measured promise.

**The change.** `tests/perf/tickScanBudget.test.js` (+lanes in the file's own shape at `:161-…`): budgets for `routeNetworkFlows.js`, `supplyKernel.js` (`advanceSettlementSupply` + `advanceCommodityContinuity`), `spatial/commodityFlow.js`, `entrepotKernel.js`, and the TR kernels (`commercialReasons.advanceCommercialReasons`, `merchantHouseLedger.advanceMerchantHouses`, `merchantVentureLedger.advanceMerchantVentures`, the corner stage, the pact lane's TR arm) — each a per-test BUDGET measured on the file's `SEED` fixture, NEVER a bank across tests (memory: "a slow test gets a per-test BUDGET never a bank"); the `neutralNeighborsEnabled` lesson (`simulationRules.js:677-735`): a lane that measures a red like it RECORDS the refusal and reports it, it does not raise the budget. ⚠ MEASURE ALONE at load < 8 (PROTO §1.4 row 8) before any figure is written. **Files:** the perf test. **Census bills:** manifest (`kind:'rationale'` — a perf budget is not a mutation-testable invariant; or the file's existing entry extended); refreeze own commit. **Byte rule:** no src byte. **Gate:** tests/lint · tests/copy · tests/property · tests/perf (alone) · check:quick. **Exit:** every named kernel has a lane with a measured budget quoted at load < 8.

### Car 5 — THE EIGHT ROWS VERIFIED + the lighting law exercised under the built-lit regime + the family's DONE-WHEN recorded

**In plain words:** the last car checks that every one of the eight trade switches has its certification row where the rules say, that the "no switch may be lit before the ones it depends on" law is actually exercised now that all eight are lit in the same four presets, and writes down that this programme is finished only when the owner's long soak says the yardsticks hold — not before.

**The change.** `tests/lint/tradeConvergenceContract.walker.test.js` (+arms): TOTALITY — every `TRADE_RULINGS_FLAG_KEYS` member has an AUTHORED row in `VIRTUAL_SUBSYSTEM_ROWS` (TR-1's in `subsystemRowsCompact.js`, the seven in `subsystemRowsTrade.js`) with the evidence law met (every `stateKey` a v5 container; every `eventType` a `candidateType:` literal); THE LIGHTING LAW EXERCISED — REWRITTEN AGAINST THE EVALUATOR'S REAL VOCABULARY AND AGAINST DOOR D0 (the skeptic: the first cut's assertion could not pass and cited a word the function does not emit): `evaluateTradeFlagLighting` (`tradeConvergenceContract.js:493-525`) emits exactly two violation kinds, `trade_precondition_dark` (`:497-510`) and `foreign_precondition_dark` (`:511-522`), and returns `{ ok, violations }`; `DORMANT_BY_CONFIG` belongs to `TRADE_FLAG_CERTIFICATION_VERDICTS` (`:149-154`), the vocabulary of an OBSERVATION row, and is never an evaluator output — the car's assertion uses neither the word nor the idea. The car asserts, for EVERY preset id in `SIMULATION_RULE_PRESETS` (all seven), `evaluateTradeFlagLighting(preset.rules).ok === true` with `violations` `[]` — the four lit presets because ALL EIGHT TR keys are lit there in build order AND every foreign precondition is lit there too (`commodityFlowEnabled` in all four after door D0 car B — ADDENDUM §3; `commonsVoiceEnabled` after lane 8's 7a; `pactFormationEnabled`/`believedScarcityEnabled` after lane 2's 7a; `errandSpineEnabled` after lane 56b's car 6), the three dark presets because nothing is lit (the all-dark config is trivially ordered, `:487-490`). The POSITIVE control: `litFlagCount` of TR keys per lit preset is 8 (so the `ok` is not vacuous). The NEGATIVE control, executed: a throwaway rules object `{ corneringEnabled: true }` yields exactly FOUR violations — three `trade_precondition_dark` (`merchantHousesEnabled`, `believedMarketsEnabled`, `foodCaravansEnabled`) and one `foreign_precondition_dark` (`commonsVoiceEnabled`) — in declared order (`:483-485` "TOTAL AND DETERMINISTIC"); a second throwaway `{ venturesEnabled: true, merchantHousesEnabled: true, believedMarketsEnabled: true }` yields exactly one `foreign_precondition_dark` (`commodityFlowEnabled`) — the shape the first cut would have shipped on two presets. `tradeEndingAvailability` under each lit preset matches the declared arms: `collapse` UNMINTABLE (`permanentZeroWhileDark: 'routeLifecycleEnabled'`, the `collapse` row at `:437-442` — CONFIRMED; `requiresFlags: ['foodCaravansEnabled', 'routeLifecycleEnabled']`), the other five mintable; under the dark presets none. ⚠ THE BOARDING CONSEQUENCE, STATED: this assertion is the one place the whole family's lighting is checked TOGETHER, so this car is built AT LANDING on the consist after every TR LIT car and door D0 (ADDENDUM R2(iii)); in a dock cut at `a5876c0ea` it reds two presets and that red is the DOCK's, not a defect. THE VIRTUAL ARM, ASSERTED NOT EDITED: `:367`'s arm is D0 car A's re-cut (ADDENDUM R1: absent from the defaults; present in a preset spread ONLY through `SIM_LIT`; every `SIM_LIT` key registered with a row) — this car adds the totality twin over the SAME denominator: every `TRADE_RULINGS_FLAG_KEYS` member IS in `SIM_LIT` (eight of eight) and absent from `ENGINE_GATED_DORMANT_RULE_KEYS`, so a TR key silently un-lit at a later rebase reds here. The family's DONE-WHEN: `docs/DESIGN_FP_ARCH_TR.md` (dock copy) §4 TR-9 gains the dated record "TR-9 landed §932 lane 56c; the programme is DONE when the envelopes hold on the owner-ordered terminal soak (#24) — the chair grades, the owner signs; every band in `TRADE_ENVELOPE_TUNING` is RAW until then"; `docs/A_PLUS_ROADMAP.md` (dock) the family row. **Files:** the walker, the two docs. **Census bills:** refreeze own commit. **Byte rule:** no src byte. **Gate:** full list. **Exit:** totality green; the lighting law's verdict per preset quoted; the negative control reds; the record written.

### Repair allowance (the sixth car)
WR-9 took five commits after its contract; the kind-walker classification at car 1 (a count pin that turns out EXACT rather than a floor — none is expected, but the walkers are run and each red classified before any literal is touched) and the perf budgets at car 4 are the likeliest spenders.

---

## THE FLAG-MINT CAR
NONE — TR-9 mints no key (the volume: "no flag"). This lane's cars are composable in any order relative to other lanes' mint cars; it never touches the register, `SIM_LIT`, the two literals or the census figures. It DOES read them (car 5) — so it boards LAST in the family, after every TR mint is on the consist.

## OWNER-GATED, KEPT OUT
The envelope bands (`TRADE_ENVELOPE_TUNING` entire) — RAW, ratified only at the soak · the terminal soak itself, its grading and the golden freeze (the owner's keystrokes; PRICING §D) · lighting `routeLifecycleEnabled` (J1's declared false) — the route kinds are made ABLE to speak, not made to speak · the OSR baseline (#20's ONE trailing mint — any new observed-shape reader is REPORTED) · push/deploy/migration/deletion/spend · the golden master · persisted shapes: NONE.

## RISKS
**Grep traps:** `route_disruption` (`settlementRumors.js:334`, `heraldRouting.js:204`) is an OLD trade kind, not J1's five; `endingsMix` is trade/war (PROGRAMME §A); `T8` in the ledger is the SHIFT window; `whole-world-soak.mjs:632` is a rotted address — locate by symbol; `INSTR-1≠TR-1`; the walker parses `DESIGN_FP_TRADE.md` §3 clause heads EXACTLY ONCE — lane 1 car 6 re-worded TR-4's head: the walker's `VOLUME_DEPENDENCY_CLAUSES['TR-4']` MUST already match at this lane's boarding (CHECK-GIT-FIRST; a mismatch here is lane 1's defect, not this lane's edit). **The addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R1 (the virtuality law amended; the `:367` arm is D0 car A's — this lane's car 5 asserts it and adds a totality twin, never re-cuts it), R2 (this lane boards LAST in the family, after door D0 and every TR LIT car; car 5 built at landing on the consist), R3 (no mint here — nothing to split). ⚠ The addendum's `src/config/simulationRules.js` is `src/domain/worldPulse/simulationRules.js` at the consist. **Collision surfaces:** `settlementRumors.js`/`heraldRouting.js` (every lane minting a kind adds rows — union); the two legacy corpora and their pins are NOT this lane's surface (`rumorFallbackPhrasePools.test.js:220-221` stays `107`/`107`; POP-6's phrase estate, if it moves them, does so on its own doc — never inherit or "declare" a number here); `tradeConvergenceContract.js` (lane 1 car 6's TR-4 row edit — must be on the consist); the audit scripts (POP-7/WF-9/GR-7/IN-6 add their own observation lanes — disjoint functions, same files: expected textual conflicts). **Load hazards:** `gate-mutex.sh` false green; the perf suite under load lies — measure alone at < 8; `deno.lock`; `git grep` reads the INDEX; a moved DRIVEN GOLDEN is a STOP. **Design hazards:** the envelopes are unsigned until the soak — a band typed as a verdict threshold before the soak is a signature this lane has no authority to give (the 9c slice's own law); a hand-typed window reds SP-7; a verdict without samples must be `unobserved`, never `pass`; the totality pin must parse the candidate kinds off the route modules (the independent denominator), never off the registry it checks.

## GATES (per car, verbatim)
focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file (one label per envelope at car 2); the wiring census stamp at car 1; prose-numerics at car 1; tests/perf ALONE at load < 8 at car 4; the SP-7 INTERVAL_WEEKS walker at car 2; `observedShapeReaders.walker` at car 3 (a change is a REPORT); the Seat/Lane trailer; porcelain 0.
