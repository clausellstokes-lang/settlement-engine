# THE LANE — PRICING §C lane 16, sitting 16a · WF-0 THE OBSERVATION FLOOR + WF-1 THE UNSEATING'S LIGHTING — THE FAITH FAMILY'S DOOR SITTING

Architect: the Fable seat (family WF), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`, porcelain 0) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED = opened at the cited line by me or by a recon skeptic; PLAUSIBLE = reasoning. Line numbers rot inside a day — re-measure by SYMBOL. Protocols: `…/f6ac0d98…/scratchpad/arch-932/ARCH-PROTOCOLS.md` (PROTO §n), distilled in `…/arch-932/ARCHITECTS-CARD.md`; the addendum `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` (R1–R5, door train D0) WINS wherever they differ. The family plan `…/arch-932/plans/plan-WF.md` §1/§3/§5 seeds this sitting; where the code contradicts it, the code wins and this brief says so.

**Cars: 4 (0, 1, 2, 3) + 1 repair allowance = 4–5.** PRICING §B priced WF-0 at 2–3 C and carried NO WF-1 row; the history fold (`…/recon-32-34/HISTORY-WF-2026-09-15.md` §A.4) adds WF-1's lighting as a NEW 1–2 C row ("already landed and therefore already inside the 09-15 built-lit directive's scope, with no row to carry it"); the family plan adds ONE car the fold did not price — car 0, THE FAITH DOOR (plan §3 C0-F). The lane is the family's DOOR SITTING: every later WF lane needs the two-preset cohort it mints, the deity-bearing fixture it authors, and the first lit WF key it declares.

**Prerequisites (anchors):** door train D0 cars A + B on the consist (ADDENDUM §3 — the EMPTY `SIM_LIT` cohort in the four lit presets and the re-cut `tests/lint/tradeConvergenceContract.walker.test.js:367` arm); nothing else. WF-1's six cars are ancestors (`66fda66d4` · `39d0ad7c8` · `adb5faf3f` · `1a90b5639` · `3ac279db4` · `5d18b4a0c` — HISTORY §A.2, CONFIRMED by the folder's `git merge-base`); WF-8A `47ea9c9ba` is an ancestor. TR-5 is NOT a prerequisite of this sitting (it is lane 5's).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** what this sitting NEEDS lit first — nothing: `faithUnseatingEnabled` is a LOCAL-lane key with a single conjunct that is DATA not config (`religiousContest.js:547 isSubsystemActive(snapshot, 'religion')` returns before any fork; the register comment at `simulationRules.js:283-295`); it has no `requires*` row in any contract evaluator (WF has none until lane 22). What it UNBLOCKS: every later WF LIT car (they append to the cohort car 0 mints, drive the fixture car 1 authors, and cite car 3's declared-shift sentence); the two blocked certification rows (`subsystemRowsBaseline.js:60` FAITH_SPREAD_OTHER, `subsystemRowsGrowth.js:91`) become observable; WF-8A's `faith_last_altar_dark` becomes mintable by inheritance (`religiousContest.js:819 if (unseating)`).

**Shared files this lane declares (PROTO §7.3 rule 1), eff/ceiling at the cut (measured with `arch-932/eff-any.mjs`, eslint's own counter):** `src/domain/worldPulse/simulationRules.js` 352/800 (the cohort + the `SIM_LIT` line + one register comment; EAGER first-paint file — every car quotes the closure table) · `tests/lint/tradeConvergenceContract.walker.test.js` 636/800 (ONE arm amended once, car 0, after D0's re-cut) · `scripts/audit/whole-world-soak.mjs` **780/800 (20 slack)** (≤ 6 grid lines, car 1; STOP if slack < delta — extract a case-table body first) · `src/domain/worldPulse/religiousContest.js` 493/800 (one `export` word, car 1) · `src/domain/certification/subsystemRowsBaseline.js` 252 · `subsystemRowsGrowth.js` 241 · `subsystemRowsVirtual.js` 284 (text edits only, the WF-1 row at `:460` NEVER moves) · `tests/property/faithUnseatingDormancyFence.test.js` 121 (car 3 adds arms) · the witness golden `tests/fixtures/preset-lighting-witness-golden.json` (car 3, by hand). NOT touched: `pulseKernel.js` 1581/1581, `warTermination.js` 818/818, `applyWorldPulse.js` 907/907 (WF-0's fixture READS the `primaryDeitySnapshot` writer path `reEmbedPrimaryDeity` `:233-242`, never edits it), `peaceTerms*.js`, `patronFall.js`, `religionState.js`. Worker bundle: 0 B (no WF file is on the generation worker graph — PRICING §A.3; quote the `stat` anyway).

---

## THE STATE OF THE RECORD

- **The history pass HAS run for WF** (`…/recon-32-34/HISTORY-WF-2026-09-15.md`, four seats + the folder's re-execution) and WINS over PRICING §B. For this sitting: **WF-0 is UNBUILT and re-ranked as the family's DATA CAR** (§A.2 row WF-0: deity presence is the outer gate of every faith mechanism; the release soak generates with `customContent: {}` at `whole-world-soak.mjs:248` and `:713`, then the `dark` variant deletes `primaryDeityRef`/`primaryDeitySnapshot`/`cultDeitySnapshots`/`latentPantheon` at `:271-277`; the premade pool is DELETED at `5f8dc7830` and walker-guarded by `tests/lint/noPremadeDeityPool.walker.test.js`; two certification families wait on this one case). **WF-1 is LANDED-DARK-WHOLE**, not PROGRAMME §B's "BUILT — code wins": `grep -c 'faithUnseatingEnabled: true' src/domain/worldPulse/simulationRules.js` = 0 (re-run by me; the register carries it at `:295`, no preset does). The fold's grep traps after its §B are adopted VERBATIM by reference (`feat(WF` is the only clean landing door; `W-F<n>` ≠ `W-FAITH F<n>c` ≠ `WF-<n>`; `WF-1` vs `WF-1A`/`WF-1a` alternate — count members by commit; line numbers rot inside a day).
- **The volume** `docs/DESIGN_FP_ARCH_WF.md` (846 lines on the ledger): §4 WF-0 (`:374-397` the observation floor — the bearer-count invariant, the v5 `deityBearers` receipt field, ONE authored deity-bearing soak case "spatial canon + two opposed-quadrant deities + a qualifying grand observance scaleBand ≥ 3 with act ∈ {procession, offering, fair} at the patron host + ≥1 mapped reachable neighbour — pilgrimage.js:31-38's own bar", pins both arms, the cult-embed mutant, Req 13/14 "engine-only, instrument"); §4 WF-1 (`:399-440`); §2 the flag family (`:181-235` — `faithUnseatingEnabled` LOCAL; the four-fence set; "LOCAL-lane flags additionally sit behind isSubsystemActive(snapshot,'religion') by construction — the deity-free world is byte-identical even lit"); §1b V29 (`:160` "release corpus deity-free; certification names the missing observation + bearer census"). ⚠ RECORD≠CODE rows the chair carries to the ledger (not lane work): the volume's WF-0 sentence "every WF fixture hereafter is deity-bearing through SET_PRIMARY_DEITY/IMPOSE_CULT (mutateEntities.js:820/:864)" — at the consist the verbs are composed in `src/components/settlement/eventComposer/buildEvent.js` and the PRODUCTION writer of `config.primaryDeitySnapshot` is `applyWorldPulse.js:233-242 reEmbedPrimaryDeity` (the pulse's convert re-embed) with the embed built by `src/domain/deityCommitEmbed.js:129 commitDeityEmbed` (CONFIRMED); the volume's `:406-412` "NEW leaf patronFall.js … budget ≤ 200 eff" (landed, 28 eff); the volume's §2 "nothing in this program lights a flag" (superseded by the owner's 16:5x order).
- **The compiled programme** `docs/DESIGN_FP_ARCHITECTURE.md` §5 #32 WF-0 (`:1921-1928`) and #33 WF-1 (`:1930-1940`); the flag table row 23 (`:484` `faithUnseatingEnabled` WF-1); L2 at `:92` ("Every FP flag is VIRTUAL: absent from DEFAULT_SIMULATION_RULES and every preset spread") — AMENDED by ADDENDUM R1; this brief CITES the addendum, never re-derives it.
- **The ODQ anchors** (`docs/OWNER_DECISION_QUEUE.md` on the ledger; locate with `grep -n '^## §N'`): §78 (`:2987`) THE WF FABLE ROUND ACCEPTED — §78.2 F-3 "WF-1 stamps all THREE suppression sites", F-4 "the four persisted-addition waves re-derive their lifecycle clauses against the EXISTING normalizer", F-5 THE WF TUNING-HOME LAW; §78.3 (`:3014`) Q1 and Q4 left open; §215 (`:7778`) TC32 — WF-1 REFUSED IN PART AND SPLIT; §215.2(1) THE FOUR-CAUSE STOP (`abandoned` cut — `patronFall.js:76` is four tokens, `:63` "IS DELIBERATELY ABSENT, AND THE ABSENCE IS A MEASUREMENT"); §227/§228 (`66fda66d4` WF-1a); §308 (`:12908`) THE WF FAMILY LAW SIGNED (trains cap at EIGHT); §309 (`:12970`) the settlement obituary RE-FILED to WF-8; §313/§314.1 WF-1b; §324 WF-1C; §329 WF-1d; §333 (`:14194`) WF-1E; §333.2 Q4 RULED (the DS-FTH corpus binds as reader-side spelling law); §352 WF-1F; §350 (`:14676`) R-4 "no new flag — darkness is inherited (no preset declares faithUnseatingEnabled)" — THE SENTENCE THIS SITTING'S CAR 3 MAKES FALSE, recorded; §376 (`:15470`) WF-8A LANDED; §400 (`:16227`) THE PRODUCER DOOR STAYS OPEN (a fifth cause lands in the same commit as its producer — this sitting mints NONE); §925.5(1) (`:32616`) the deity pool still called an open constitutional violation — RECORD≠CODE, the pool is GONE (row 7 of the fold's §B).
- **O-12, the record that shapes car 0** (`simulationRules.js:797-809`, CONFIRMED verbatim): lighting a `RULE_COMPARISON_KEYS` member on a LEGACY preset id "makes rulesMatchPreset miss, presetIdForRules fall through to 'custom', and every installed Dramatic Campaign silently re-label itself at its next ensureWorldState with no receipt minted … The lighting therefore belongs on a LIT SUCCESSOR id (row O-1's birth form)". `faithSpreadEnabled` is such a member: a `DEFAULT_SIMULATION_RULES` boolean at `:101` → `BOOLEAN_KEYS` (`:1156`) → `RULE_COMPARISON_KEYS` (`:1162`), compared at `:1291`. It resolves TRUE in exactly `dramatic_campaign` (`:811`) and `full_simulation` (`:875`) and FALSE in `realistic_regional` and `living_realm` (CONFIRMED by `normalizeSimulationRules` over every preset at the consist — my `node -e` run: `quiet_local false · realistic_regional false · dramatic_campaign true · static_campaign false · narrative_campaign false · living_realm false · full_simulation true`). So the D0-car-B move (light the conjunct in the two remaining lit presets) is BLOCKED for faith; the SPREAD-lane WF keys (`faithTermsEnabled`, `faithStanceConsequencesEnabled`, `pilgrimageEnabled`) can lawfully light in TWO presets only.
- **The owner's laws that bind:** everything built lands LIT (16:5x); the lit regime reverts to built-dark after the terminal soak (17:0x) — `SIM_LIT`/`SIM_LIT_FAITH_SPREAD` emptied is the single reverting act; nothing pushes/deploys/applies/deletes/spends; tuning values raw with derivations, ratified at the soak; the golden master stays 525/525 red and is NEVER re-recorded (the classified base-vs-tip diff is the claim).

## THE STATE OF THE CODE (CONFIRMED at the consist unless marked)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/simulationRules.js` | 352/800 | `DEFAULT_SIMULATION_RULES` with `faithSpreadEnabled: false` `:101` and the legacy mirror `religionDynamicsEnabled: false` `:108` (lockstep normalizer `:1341-1345`; the tolerant reader `isFaithSpreadEnabled` `:1414-1425`); `ENGINE_GATED_VIRTUAL_RULE_KEYS` 35 members (`node -e` CONFIRMED) — `beliefAxesEnabled` `:242`, `believedDevotionEnabled` `:251`, `errandSpineEnabled` `:271`, `faithUnseatingEnabled` `:295` (the WF-1a entry, comment `:283-294`), `pactFormationEnabled` `:324`; `WAVES` `:644` (nine keys, PINNED) and `ONE_REGEN` `:666`, spread at `:771-772` (realistic_regional), `:822-824` (dramatic_campaign), `:864-865` (living_realm), `:1072-1073` (full_simulation); the O-12 record `:797-809`; `faithSpreadEnabled: true` at `:811` and `:875` ONLY; `RULE_COMPARISON_KEYS` `:1162-1166`, `rulesMatchPreset` `:1291`. ⛔ NO `SIM_LIT` exists at this tip — D0 car A mints it; this lane appends to it and NEVER re-mints it (CHECK-GIT-FIRST) |
| `src/domain/worldPulse/religiousContest.js` | 493/800 | header law `:17-36`; `isSubsystemActive` import `:48`, `isFaithSpreadEnabled` import `:49`; `deityBearers(snapshot)` PRIVATE at `:265`, used once at `:591`; `advanceReligionStates` `:544` returning at `:547` on `!isSubsystemActive(snapshot, 'religion')`; `const spread = isFaithSpreadEnabled(rules)` `:553`; `const unseating = worldState?.simulationRules?.faithUnseatingEnabled === true` `:692`; `if (unseating) {` `:819` → the WF-8A obituary mint `faithReceipt` `:855`; `resolvePatronContest` `:893` |
| `src/domain/worldPulse/subsystemActivation.js` | 17 | `isSubsystemActive(snapshot, gate)` `:68-72` over `SUBSYSTEM_GATES` — the `'religion'` predicate is deity PRESENCE (`config.primaryDeitySnapshot` or a cult embed) |
| `src/domain/worldPulse/religionState.js` | 373/800 | `suppressDeity` `:271-280` (the ONE writer of `suppressed`/`suppressedAtTick`; `suppressed: true` at `:276`, the stamp `:277`); `pruneSuppressed` `:431-…` (`KEEP = 3` `:434`, the `narrativePrune` fork `:437`); `projectReligionStateOntoSettlement` `:614` with THE read `simulationRules?.faithUnseatingEnabled === true` at `:649` (the projection carries `patronFall` only lit AND on a real ring); `applyDivineMandate` `:762` |
| `src/domain/worldPulse/patronFall.js` | 28 | `PATRON_FALL_CAUSES` frozen FOUR `:76` (`discredited`, `displaced`, `imposed`, `suppressed`); `FALL_RING_CAP = 3` `:83`; `classifyPatronFall` `:102` (first-match precedence `imposed` > `suppressed` > `discredited` > `displaced`); `recordPatronFall` `:138` refusing out-of-vocabulary causes `:141` |
| `src/domain/worldPulse/pulseKernel.js` | **1581/1581** | `advanceReligionStates` called `:1261`; `advancePantheon({ …, unseating: simulationRules.faithUnseatingEnabled === true })` `:1634-1637`; `applyDivineMandate` `:1807`. NO WF LINE EVER |
| `src/domain/worldPulse/pantheon.js` | 179 | `advancePantheon({ pantheon, snapshot, faithDeltas, unseating })` `:466-474` — `lastSeatLosses` joins `changes` only under `unseating === true` |
| `src/domain/worldPulse/realmEvents.js` | 309 | `synthesizePantheonArcs` `:287` — `pantheon_extinction` "The Last Altar of {name}" `:305-315` (WF-1C's, reached only through `lastSeat`); `pantheon_ascendancy` `:338` / `pantheon_twilight` `:361` UNGATED (lane 21's concern, not this sitting's) |
| `src/domain/worldPulse/warTermination.js` | **818/818** | `:538` `rules.faithUnseatingEnabled === true ? asObject(state.religionStates) : {}` (WF-1d's join). NO WF LINE |
| `src/components/settlement/faithPanelModel.js` | 140 | `:225-230` reads `profile.patronFall?.cause` (the PROJECTION's field, not the key by name) — NOT a fifth gate site |
| `src/domain/certification/subsystemRowsVirtual.js` | 284/800 | `VIRTUAL_SUBSYSTEM_ROWS` `:105` (…`...BELIEF_SUBSYSTEM_ROWS` `:110` … `...COMPACT_SUBSYSTEM_ROWS` `:302` … the INLINE WF-1a row `rule: 'faithUnseatingEnabled'` `:460` with `eventTypes: []`, `moverFamilies: []`, `stateKeys: []` (all "DELIBERATELY EMPTY AND MEASURED") and the `other` text containing "no preset declares this key, so a soak receipt carries no entry for it and this row can only ever grade UNOBSERVED" — the sentence car 3 amends … `...ENCOUNTERS_SUBSYSTEM_ROWS` `:584`); `VIRTUAL_PENDING_RULE_KEYS = Object.freeze([])` `:595`; `VIRTUAL_SUBSYSTEM_ROWS.length` = 35 (`node -e`) |
| `src/domain/certification/subsystemRowsBaseline.js` | 252 | `FAITH_SPREAD_OTHER` `:60` — "MEASURED 2026-07-31 … the soak fixture is DEITY-FREE … THE OBSERVATION NEEDED to certify this row: one soak case whose settlements carry an embedded config.primaryDeitySnapshot, at a horizon long enough for a patron seat to change, plus a v5 subsystems.stateKeys census over worldState.religionStates. Until such a case exists the honest verdict is UNOBSERVED" (its `whole-world-soak.mjs:112` cite has rotted to `:248`) |
| `src/domain/certification/subsystemRowsGrowth.js` | 241 | `:91` "institution_founding is ZERO in every case and every year … the soak fixture carries no deities … THE OBSERVATION NEEDED … one soak case whose settlements carry config.primaryDeitySnapshot" |
| `scripts/audit/whole-world-soak.mjs` | **780/800** | `buildFixture(seed, { variant })` `:244` — `generateSettlementPipeline(config, null, { seed, customContent: {} })` `:248`; the `dark` variant deleting the four deity keys `:271-277`; the isolated-worker measurement `:705-713` with `customContent: {}`; the v5 envelope `subsystems: buildSubsystemConfiguration({…})` `:1138` (v5 ADDS the `subsystems` section `:1014`). No `soakCases/` directory exists (`ls scripts/audit` CONFIRMED — the sidecar is NEW) |
| `src/domain/traditions/pilgrimage.js` | 41 | `PILGRIM_ACTS {procession, offering, fair}`, `GRAND_SCALE_MIN = 3`, `SPECTACLE_SCALE = 5`, `PILGRIM_MAX = 0.1` (six bare dials `:32-37`); `drawsPilgrims(rec)` `:59-63`; `pilgrimageDraw` `:82` (aspatial ⇒ 0; unmapped host ⇒ 0) — the WF-2 precondition pin reads `drawsPilgrims` |
| `src/domain/deityCommitEmbed.js` | — | `commitDeityEmbed(ref, snapshot)` `:129`; `authoredCharacterEmbedKeys` `:86` — THE embed writer the fixture's deities go through (never config poking) |
| `src/domain/worldPulse/applyWorldPulse.js` | **907/907** | `reEmbedPrimaryDeity` `:233-242` — the ONE production writer of `config.primaryDeitySnapshot` (and the one file `noPremadeDeityPool`'s allowlist permits the `deity:core:` literal in — READ ONLY here) |
| `tests/property/faithUnseatingDormancyFence.test.js` | 121 | FENCE 1 the polarity census over the projection with its lit mutant (seven dark spellings byte-identical, the lit one moves); FENCE 2 the flag AND the ring; FENCE 3 the site census — `DECLARED_GATE_SITES` FOUR modules (`pulseKernel.js`, `religionState.js`, `religiousContest.js`, `warTermination.js`) found by the regex `faithUnseatingEnabled\s*===\s*true` over `src/`; FENCE 4 "every rules object here is built in this file, so no arm inherits a default". ⚠ **THERE IS NO PRESET ARM AT THIS TIP** — no `toContain(FLAG)` on a dormant list, no "declared in no preset" assertion — so car 3 ADDS the lit-roster arm (the LIT form) rather than re-chartering a dark one; and the `SIM_LIT` line `faithUnseatingEnabled: true,` does NOT match FENCE 3's `===` regex, so the four-site census does not move |
| `tests/simulation/presetLightingWitness.test.js` + `tests/fixtures/preset-lighting-witness-golden.json` | — | one row per preset + `__birth_default__`; `measureWitness()` `:76/:86`; fields `rulesSha256`, `litFlagCount`, `worldStateSha256`, `settlementUpdatesSha256`, `wizardNewsSha256` (`:146-162`); the golden's `birthDefaultRow` / `birthSuccessorPresetIdWhenRecorded` name the birth preset — read it, never assume |
| `tests/domain/contributionLedgerShape.test.js` | — | `:118-119` `toHaveLength(35)` ×2 — UNCHANGED by this sitting (WF-1's key is already registered; state so) |
| `tests/soak-harness/coveringArrayCoverage.test.js` | — | `:185-222` the four figures (`union`, `governed`, `ungoverned`, `nonBoolean`) — a PRESET declaration of a virtual key may move `governed`/`ungoverned`: MEASURE by calling `flagDomainCensus` out of band at car 3, never inherit |
| `tests/lint/tradeConvergenceContract.walker.test.js` | 636 | `PRESET_RULE_KEYS` `:141-144` (= every key of every preset's `rules`, so a `...SIM_LIT`/`...SIM_LIT_FAITH_SPREAD` spread IS a preset key); the `:367` arm ("every TRADE flag is VIRTUAL — absent from the defaults and from every preset spread") — D0 car A re-cuts it to R1(a)–(c); car 0 amends ONE arm ONCE after that |
| `tests/lint/noPremadeDeityPool.walker.test.js` | — | four scans + the anti-vacuity denominator `:90`; `deityPool.js` and `seedStartingPantheon.js` ABSENT (`ls`) — the fixture's deities must be `deity:custom:*`-scoped refs, never `deity:core:` |

**Budgets (PROTO §4.1):** worker 1,404,493 EXACT and untouched (no WF file on the graph — quote the `stat`); `engine` chunk ±0 expected (quote the `engine-*.js` line); first-paint closure: car 0's empty cohort string + car 3's one `SIM_LIT` line ≈ 30–90 B (PLAUSIBLE) against an UNMEASURED margin (PRICING 795 B vs lane A0's 5,828 B) — quote the real table at both cars.

## ALREADY DONE (do not re-land)

WF-1 whole and dark (six cars; `patronFall.js`, the one suppression writer, the flag-forked prune, the realm last-seat beat, the war join, the panel sentence, the creed-name cure) · WF-8A `faith_last_altar_dark` (one-row `FAITH_KIND_REGISTRY`, `faithNews.js:118-124`, riding `if (unseating)`) · the WF-1 fence in its polarity/site-census shape (LGT-P13) · the row at `subsystemRowsVirtual.js:460` · the register entry `:295` · the two certification rows' SHELLS with the exact observation owed written into them · the deity-pool deletion and its walker · the DS-FTH corpus (`warFaith.generated.js` DS-FTH-3 binds `patronFalls[]` × `templeWealth`) — VERIFY-AT-BUILD whether a WF-1 car carried the doc pin (`grep -rn 'DS-FTH-3' tests/`); if none did, car 3 lands it.

---

## THE CARS

Every car: one gated commit; anchored negatives (`// anchored:`); executed mutants manifest-rowed; NUL-scanned files; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: 16a`; PACKET_STANDARD caps (≤1 behaviour family, ≤1 new persisted record family, ≤1 user-facing surface, ≤2 leaves ≤250 eff, ≤12 handwritten files); the packet `docs/implementation/packets/fp/WF-0.md` (car 1–2) and `WF-1G.md` (car 3, the lighting) in the dock — WF runs packets (seven exist).

### Car 0 — THE FAITH DOOR: the empty two-preset cohort `SIM_LIT_FAITH_SPREAD`, the D0 walker's one amendment, the cohort fence

**In plain words:** three of the faith family's switches only make sense where the wider "faith spreads between towns" switch is already on, and that switch is on in only two of the four living presets — and turning it on in the other two would silently re-label every player's installed campaign as "custom". So this car builds a second, smaller switch-panel that lives only in those two presets, empty for now, and writes down in the code exactly why it is two and not four.

**The change.** `src/domain/worldPulse/simulationRules.js`: a second frozen cohort `SIM_LIT_FAITH_SPREAD = Object.freeze({})` declared beside `SIM_LIT` (D0 car A's) and spread as `...SIM_LIT_FAITH_SPREAD` immediately after `...SIM_LIT` in EXACTLY `dramatic_campaign` and `full_simulation` — the two presets where `faithSpreadEnabled` resolves `true` — and nowhere else; its header quotes O-12 (`:797-809`, by symbol: the paragraph beginning "THE LIT HOME IS NOT THIS ENTRY") and states the roster and the reason: "a SPREAD-lane WF key ANDs with `isFaithSpreadEnabled(rules)`; `faithSpreadEnabled` is a `RULE_COMPARISON_KEYS` member resolved false in `realistic_regional` and `living_realm`; lighting it there re-labels installed campaigns (O-12); the lawful four-preset home is a LIT SUCCESSOR id (O-1's birth form), owner-gated and not taken here". ⚠ `WAVES` (nine keys, `:644`) and `ONE_REGEN` (`:666`) are NOT touched. `tests/lint/tradeConvergenceContract.walker.test.js`: ONE amendment to D0's re-cut `:367` arm, once — VERIFY-AT-BUILD the re-cut's scope first: if the re-cut arm walks only `TRADE_RULINGS_FLAG_KEYS`, WF keys never hit arm (b) and the amendment is to arm (c)'s cohort roster (admit `SIM_LIT_FAITH_SPREAD` as a second registered cohort whose every key must be registered and rowed); if the re-cut walks every `SIM_LIT`-resident key, arm (b) is amended to read "present in a preset spread ONLY through `SIM_LIT`, or through a NAMED conjunction cohort whose preset roster EQUALS the presets where its conjunct resolves `true`" — the roster computed from `SIMULATION_RULE_PRESETS` + `normalizeSimulationRules` in-test, never a literal. NEW `tests/property/faithSpreadCohortFence.test.js`: (arm 1) the cohort's preset roster == `{ id : isFaithSpreadEnabled(normalizeSimulationRules(SIMULATION_RULE_PRESETS[id].rules)) }` — computed both sides; (arm 2) every key in `SIM_LIT_FAITH_SPREAD` is a member of a FROZEN in-file allowlist `SPREAD_LANE_KEYS = ['faithStanceConsequencesEnabled', 'faithTermsEnabled', 'pilgrimageEnabled']` AND of `ENGINE_GATED_VIRTUAL_RULE_KEYS` AND has a row in `VIRTUAL_SUBSYSTEM_ROWS`; (arm 3) the inverse — no `SPREAD_LANE_KEYS` member appears in `SIM_LIT` or directly in any preset object; (arm 4) THE O-12 TRIPWIRE — `faithSpreadEnabled` resolves `true` in exactly `['dramatic_campaign', 'full_simulation']` (the set is spelled as the fence's expectation of the CURRENT record; a change here is a chair act, the message names O-12 and the successor-id road); (arm 5) the anti-vacuity control — a rules bag with `faithSpreadEnabled: true` planted resolves true through `isFaithSpreadEnabled` (the reader is live). ⚠ THIS FENCE CARRIES THE FAMILY'S LIGHTING LAW UNTIL LANE 22 lands `faithConvergenceContract.js` (ADDENDUM R2: "each family's own, where one exists; ARCH §3's fences otherwise") — lane 22 folds it and leaves a pointer.

**Pins and negative controls (executed, restored md5-exact):** (1) deep-equal every preset's RESOLVED rules base-vs-tip via `normalizeSimulationRules` — the empty cohort moves nothing (the car's receipt, quoted for all seven presets); (2) a `SPREAD_LANE_KEYS` member planted into `SIM_LIT` → arm 3 reds; (3) a key planted into `SIM_LIT_FAITH_SPREAD` without a register entry → arm 2 reds (and D0's arm (c) reds — quote both); (4) `faithSpreadEnabled: true` planted into `living_realm` → arm 4 reds with the O-12 message; (5) the cohort spread planted into `realistic_regional` → arm 1 reds.

**Files (≈4 handwritten):** `simulationRules.js`, `tests/lint/tradeConvergenceContract.walker.test.js` (one arm), `tests/property/faithSpreadCohortFence.test.js` (NEW), `scripts/mutation-coverage-manifest.json` (a `kind:'mutation'` entry with a planted regression); regenerated: the four edge-shared files (`supabase/functions/_shared/aiCharterBundle.js` + `.meta.json`, `aiOutputSchemaBundle.js` + `.meta.json` — any `simulationRules.js` byte stales them: clone immer+seedrandom with `cp -Rc`, `node scripts/build-edge-shared.mjs`, `grep -rl 'Users/cstokes' supabase/functions/_shared/` EMPTY, relink before any vite build). **Census bills:** the lighting-census refreeze on a clean tree as its OWN commit (a new test file); the manifest entry. **Byte rule:** `simulationRules.js` is EAGER — quote the first-paint table, the `engine-*.js` line and the worker `stat` from a real `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist`. **Gate:** the full list (GATES below) + `tests/lint/tradeConvergenceContract.walker.test.js` in D0's re-cut form + the two edge freshness tests + `tests/domain/simulationRulesPreset.stability.test.js` (its identity arms must stay green — the cohort is empty). **LIT act:** none (the cohort is EMPTY). **Exit:** the D0 walker green with the amended arm; every preset byte-identical in resolution (seven quoted equalities); the bundles fresh; the new fence 100 % green with its five arms and its executed mutant. **Prerequisite:** D0 car A ON THE CONSIST; if D0 has not composed when this lane cuts, the chair may fold this car into D0 as car C — it is priced ONCE, here, either way (CHECK-GIT-FIRST; report which case).

### Car 1 — THE DATA CAR: the deity-bearing soak case as a sidecar, `deityBearers` exported into the v5 receipt

**In plain words:** every faith mechanism in the engine is asleep in the shipped test worlds because those worlds have no gods at all, so nothing faith-shaped can ever be observed there. This car authors one test world with two gods of opposite character, a grand festival at the patron's town and a reachable neighbour, and teaches the soak's receipt to count how many towns carry a god — so that from here on "the faith layer is lit" can be seen rather than assumed.

**The change.** NEW `scripts/audit/soakCases/faithBearing.mjs` (the SIDECAR — the estate's first `soakCases/` module; ≤ 250 eff): exports one case `{ id: 'faith_bearing_v1', build(seed) }` that builds a `REGION`-shaped fixture with the spatial canon (`whole-world-soak-spatial-fixture.mjs`'s digest — the pilgrimage draw needs `isMapped` + `pathCost`), TWO custom deities authored through the custom-content path (`customContentSchema.js` deity shape `:309-420`; refs `deity:custom:<slug>` minted by `customRegistry.js mintDeityRef`; embedded by `commitDeityEmbed(ref, snapshot)` — NEVER a `deity:core:` literal, NEVER a hand-poked `config.primaryDeitySnapshot` object; the two deities sit in OPPOSED alignment quadrants so `faithAlignmentQuadrant`'s kindred default is not the whole story; NO `boonChannel`/`baneChannel`/`authoredTemper` authored — W-FAITH's channel lift stays dormant by data, DECLARED in the case header; a boon-bearing variant is lane 22's grid), the patron host carrying ONE qualifying grand observance (`traditions[]` rec with `scaleBand ≥ 3` and `coreMotif.act ∈ {procession, offering, fair}` — `pilgrimage.js:59-63 drawsPilgrims`'s own bar) and ≥ 1 mapped reachable neighbour. `scripts/audit/whole-world-soak.mjs` (780/800): ≤ 6 grid lines importing the sidecar and registering the case beside the existing variants (if the measured slack at the cut is < the delta, the car FIRST extracts a case-table body into the sidecar to make room — never a squeeze); `src/domain/worldPulse/religiousContest.js:265`: `function deityBearers` → `export function deityBearers` (one word; the call at `:591` unchanged); the v5 receipt `subsystems` section (`whole-world-soak.mjs:1138 buildSubsystemConfiguration`) gains `deityBearers: <count>` computed from the fixture snapshot through the exported helper (VERIFY-AT-BUILD the exact receipt writer — the PRICING cite `:1105-1121` has rotted; locate `buildSubsystemConfiguration` by symbol); the receipt JSON schema/coverage manifest for v5 gains the field (VERIFY the totality walker for receipt fields — `tests/soak-harness/` reads the envelope; a new field without a manifest row reds).

**Pins (NEW `tests/soak-harness/faithBearingCase.test.js`):** (1) the bearer count on the bearing case equals the number of settlements carrying `config.primaryDeitySnapshot` (2 of N — both arms: the exact count AND non-zero); (2) ZERO on the deity-free corpus fixture (`buildFixture(seed)` today) — the negative arm is non-vacuous because arm 1 is positive on the same helper; (3) THE DOCTRINE PIN — the fixture's deity refs round-trip the custom-content/embed path (`authoredCharacterEmbedKeys` parity; `noPremadeDeityPool.walker` stays green — run it and quote the denominator `:90`); (4) THE WF-2 PRECONDITION PIN — `drawsPilgrims(hostRec) === true` for the case's grand observance AND `pilgrimageDraw({ hostId, hostRec, settlements, digest }) > 0` (the draw is live; a mapped neighbour exists); (5) `isSubsystemActive(snapshot, 'religion') === true` on the bearing case and `false` on the corpus (the outer gate, driven both ways). **Mutants:** the bearer predicate changed to count cult embeds only (`cultDeitySnapshots`) → pin 1 reds; the observance's `scaleBand` lowered to 2 → pin 4 reds.

**Files (≈6 handwritten):** the sidecar (NEW), `whole-world-soak.mjs` (≤ 6 lines), `religiousContest.js` (1 word), the receipt manifest/schema file (VERIFY name), `tests/soak-harness/faithBearingCase.test.js` (NEW), `scripts/mutation-coverage-manifest.json`. **Census bills:** refreeze own commit; manifest; the v5 receipt coverage row. **Byte rule:** `religiousContest.js` is pulse-side (advance worker, no ceiling) — build + verify:dist quoted; worker unchanged; engine ±0; closure ±0. **Gate:** full list + `tests/soak-harness/` + `noPremadeDeityPool.walker`. **LIT act:** none (an instrument). **Req 13/14 recorded:** alignment-empty with reason (a count reads no axes); "engine-only, instrument" — the certification-surface render pin is car 2's. **Exit:** the case builds deterministically on two seeds (same-seed byte-identical receipts); pins 1–5 green; both mutants convict; `whole-world-soak.mjs` at or under 780 (the number quoted).

### Car 2 — the two blocked certification rows learn the observation exists

**In plain words:** two rows of the world's self-certification table have said for six weeks "this cannot be observed because the test worlds have no gods"; now that a god-bearing world exists, the rows say which case observes them and what count proves it — and the table still renders on the settlement's certification panel.

**The change.** `src/domain/certification/subsystemRowsBaseline.js` `FAITH_SPREAD_OTHER` (`:60`): the "MEASURED 2026-07-31 … Until such a case exists the honest verdict is UNOBSERVED" clause gains a dated sentence naming the case (`faith_bearing_v1`, `scripts/audit/soakCases/faithBearing.mjs`) and the bearer-count invariant ("`subsystems.deityBearers` ≥ 1 on the bearing case; 0 on the release corpus"); the rotted `whole-world-soak.mjs:112` cite → the symbol `buildFixture` (`:248` at this tip — cite the symbol). `src/domain/certification/subsystemRowsGrowth.js` `:91`: the same dated sentence. TEXT ONLY — no row's INDEX moves (`VIRTUAL_RULES` at `tests/domain/subsystemRowsVirtual.test.js:646` is ordered equality; these rows are baseline/growth rows, not virtual, but the same discipline holds). The certification-surface render pin: `tests/components/…WorldCertificationPanel` (the surface is `src/components/settlement/WorldCertificationPanel.jsx` reading `certificationRead.js`) renders the two rows from a fixture receipt carrying `deityBearers: 2` without a thrown render — the dossier landing the volume's WF-0 clause owes (Req 14's escape). `soakEvidence` stays `'unobserved'` on both rows until a soak has actually graded them (the terminal soak's business).

**Files (≈3):** the two row leaves (text), the panel test (one arm). **Census bills:** none new (no new test file if the arm joins an existing panel test; else refreeze). **Byte rule:** certification leaves are LAZY (VERIFY by build — quote); worker 0. **Gate:** full list + `tests/lint/subsystemCertificationTotality.walker.test.js`. **LIT act:** none. **Exit:** both rows carry the case id; the panel renders; totality walker green.

### Car 3 — WF-1 LIT: `faithUnseatingEnabled` declared in the four lit presets through `SIM_LIT`; the fence gains its lit-roster arm and its lit twin over the bearing fixture; the row text and register comment say LIT; the witness re-recorded; the declared shift written (BUILT AT LANDING ON THE CONSIST, rebased — ADDENDUM R2(iii))

**In plain words:** the "a town remembers which god lost its seat, and why" feature has been finished for three weeks but switched off in every preset; this car switches it on for every world born into the four living presets, proves in the same commit that a world without gods is byte-for-byte unchanged, proves on the new god-bearing world that a real fall now writes a real record, and writes the change down in the shift ledger with its movers named.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 — the virtuality law is amended; `SIM_LIT` is the one lawful home of a lit FP key; the `:367` arm is D0 car A's (car 0 amended one arm; this car edits it NOT AT ALL — it runs it and quotes it green). R2 — this key has NO lit-before precondition (LOCAL lane; the data gate does the second gate's work); the car is built AT LANDING on the consist after D0 (a dock-green LIT car against the slot's dark presets is a FALSE GREEN). R3 — the DARK MINT of this key was WF-1a (`66fda66d4`: register entry, gate read, row, literals) — this car is LIGHTING ONLY and moves NO literal (`contributionLedgerShape.test.js:118-119` stays 35 unless another lane's dark mint composed between — re-read off HEAD and state the number).

**The change (PROTO §1.2 items 2, 3, 10–12; the mint half is WF-1a's):**
1. `simulationRules.js`: `SIM_LIT` gains `faithUnseatingEnabled: true, // WF-1 · lit at §932 lane 16a car 3 (LOCAL lane; the data gate isSubsystemActive(snapshot,'religion') is the second gate)` (CHECK-GIT-FIRST for lines other lanes appended — write only what is absent; never re-mint the cohort); the register comment `:283-294` gains a LIT paragraph naming this car, the four presets, and the two-gate fact.
2. `tests/property/faithUnseatingDormancyFence.test.js` gains **FENCE 5 — THE LIT ROSTER** (the LIT form, PROTO §3.2 arm 1): `faithUnseatingEnabled` is ABSENT from `DEFAULT_SIMULATION_RULES`; is a member of `ENGINE_GATED_VIRTUAL_RULE_KEYS`; is `true` in `SIMULATION_RULE_PRESETS[id].rules` for EXACTLY the four ids where `SIM_LIT` is spread and absent from the other three — the roster COMPUTED from the presets object (`Object.entries(SIMULATION_RULE_PRESETS).filter(([, p]) => p.rules.faithUnseatingEnabled === true)`) and compared to the set of presets whose rules carry `SIM_LIT`'s own marker, never a literal list of ids; and **FENCE 6 — THE LIT TWIN OVER THE BEARING FIXTURE**: `advanceReligionStates` driven on car 1's case under `SIMULATION_RULE_PRESETS.full_simulation.rules` through a real fall (reuse `tests/domain/patronFall.test.js`'s printed-execution drivers for the four causes — ODQ §227 — the cheapest is the `imposed` arm via `cultImpositionApply`) yields a `patronFalls` ring of length ≥ 1 whose `cause` is one of the four tokens; the SAME fixture under `quiet_local.rules` (dark) yields NO `patronFalls` key on any state (absent, never `[]`); FENCE 3's `DECLARED_GATE_SITES` stays exactly four (the `SIM_LIT` line is `: true,` and does not match `\s*===\s*true` — assert the census still returns the four, quoted).
3. `subsystemRowsVirtual.js:460` the WF-1a row's `other` text: "no preset declares this key, so a soak receipt carries no entry for it and this row can only ever grade UNOBSERVED" → "LIT at §932 (lane 16a car 3) in the four lit presets through `SIM_LIT`; observable on the deity-bearing soak case `faith_bearing_v1` (WF-0); a deity-free world is byte-identical even lit" — TEXT ONLY, the row's INDEX unchanged (`VIRTUAL_RULES` ordered equality at `subsystemRowsVirtual.test.js:646`).
4. WF-8A rides by inheritance: `faith_last_altar_dark` becomes mintable (`religiousContest.js:819`) — `tests/lint/faithKindPools.walker.test.js` is UNCHANGED (its arms census the pool, not the mint) — run and quote; `kindPoolFloors.walker` unchanged.
5. THE WITNESS re-recorded BY HAND (`measureWitness()`; PROTO §1.4 row 1) against the post-D0 golden: EXPECTED `rulesSha256` + `litFlagCount` move on the four lit rows (and on `__birth_default__` iff the golden's `birthDefaultRow` names a lit preset — read it); `worldStateSha256` / `settlementUpdatesSha256` / `wizardNewsSha256` UNMOVED on EVERY row because the witness's born worlds are DEITY-FREE and `isSubsystemActive(snapshot,'religion')` returns before any fork — **a moved pulse hash on ANY preset is a STOP** (the key leaked past the data gate; find the mover before landing); a moved DARK row is a STOP.
6. THE GENERATION DUMP: the classified base-vs-tip run (`$SC/kit/c931-f6ac0d98/declared/run-declared.sh` shape) reports `configs 525 · unchanged 525 · changed 0` — the ZERO-MOVEMENT receipt for an engine key; the golden master is NEVER re-recorded. `tests/property/espionageDormancyFence.test.js` 21/21 quoted (an engine key cannot move the generator-side constant; no header block). `node scripts/prose-manifest-cells.mjs --out` base and tip → zero rows moved, quoted.
7. THE SHIFT-LEDGER DRAFT (`docs/implementation/GOLDEN_SHIFT_LEDGER-WF-1G.draft.md` in the dock, PROTO §2.5 shape: HEADLINE · why a shift not a repair · goldens re-recorded (the witness: file | assertions | before | after | cause) · non-golden registers moved (the lighting census) · future-shift conditions (the terminal soak's revert) · THE DECLARED-SHIFT SENTENCE): *"`faithUnseatingEnabled` lit on `realistic_regional`, `dramatic_campaign`, `living_realm`, `full_simulation` at landing <sha>; `rulesSha256` and `litFlagCount` moved on those four witness rows (and `__birth_default__` where applicable); `worldStateSha256` / `wizardNewsSha256` / `settlementUpdatesSha256` UNMOVED on every preset because the witness's born worlds are deity-free and the outer gate `isSubsystemActive(snapshot,'religion')` returns before any fork; the generation dump 525/525 unchanged; the espionage fence 21/21; the first-paint closure +<n> B; no installed campaign re-labelled (the key is VIRTUAL — absent from `RULE_COMPARISON_KEYS`); no tuning value moved; WF-8A's `faith_last_altar_dark` becomes mintable by inheritance on deity-bearing worlds only."*
8. The DS-FTH doc pin (volume §5-7; exactly-once discipline; `patronFalls[]` spelling matched against `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`'s DS-FTH-3 block) — ONLY if `grep -rn 'DS-FTH-3' tests/` finds none at the cut (VERIFY-AT-BUILD; `3ac279db4`/`5d18b4a0c` may have carried it); a doc-reading pin touches no prose byte (wiring census `--check` only).
9. The covering-array figures (`coveringArrayCoverage.test.js:185-222`) re-measured out of band by calling `flagDomainCensus` — a preset declaration may move `governed`/`ungoverned`; state the four numbers before and after; the edge bundles rebuilt (four files) — the `simulationRules.js` byte stales them.
10. `docs/implementation/packets/fp/WF-1G.md` (the lighting packet) + the ODQ line for the chair: "§350 R-4's 'no preset declares faithUnseatingEnabled' is FALSE from §932 lane 16a car 3 — recorded, not edited".

**Negative controls (executed, restored md5-exact):** FENCE 5 with the `SIM_LIT` line removed → reds (the roster is three-of-seven → zero-of-seven; the arm sees it); FENCE 6 with the fixture's deities deleted (the `dark` variant's four `delete`s) → the lit run yields NO ring (the data gate; this is the FALSE-CLAIM guard — a lit key on a deity-free world moves nothing, and the fence proves the fence can tell); the witness with `faithUnseatingEnabled` planted into `quiet_local` → the dark row moves → the STOP arm reds.

**Files (≈9 handwritten):** `simulationRules.js` (one `SIM_LIT` line + one comment), `tests/property/faithUnseatingDormancyFence.test.js` (two arms), `tests/fixtures/preset-lighting-witness-golden.json` (by hand), `subsystemRowsVirtual.js` (text), `tests/soak-harness/coveringArrayCoverage.test.js` (only if a figure moved), `tests/domain/contributionLedgerShape.test.js` (only if HEAD moved — expected UNCHANGED), the DS-FTH pin test (only if owed), the shift-ledger draft, the packet; regenerated: the four edge-shared files. **Census bills:** refreeze own commit (arms added to an existing file change titles); manifest `kind:'rationale'` for the two new arms (or `mutation` with the planted `SIM_LIT`-line removal); no prose byte. **Byte rule:** closure +1 line (≈30–60 B, PLAUSIBLE) — quote; worker `stat` unchanged; engine ±0 quoted. **Gate:** full list + the witness + `tests/domain/simulationRulesPreset.stability.test.js` (its identity assertions stay green — a VIRTUAL key is not a comparison key) + the two edge freshness tests + `tests/lint/engineGatedRuleKeys.walker.test.js` (three directions; `ENGINE_GATED_DORMANT_RULE_KEYS` LACKS `faithUnseatingEnabled` at the tip — measured) + `tests/lint/subsystemCertificationTotality.walker.test.js` + `tests/lint/tradeConvergenceContract.walker.test.js` (D0's re-cut arm green with the key in `SIM_LIT`) + the espionage fence 21/21 + the preset-driven soak suites measured ALONE at load < 8. **LIT act:** `faithUnseatingEnabled` → `SIM_LIT` (four presets). **Exit:** the key in `SIM_LIT`; FENCES 1–6 green with the lit twin moving on the bearing fixture and the four-site census unchanged; the witness diff quoted row by row with zero pulse-hash movement; 525/525 unchanged; the shift draft written; the row text and register comment say LIT; zero UNDECLARED lines; no red this car did not cause (a moved driven golden is the chair's bisect, not this lane's edit).

**Handoff to lane 5:** the cohort `SIM_LIT_FAITH_SPREAD` exists (EMPTY); the bearing fixture `faith_bearing_v1` exists; `faithUnseatingEnabled` is lit; the family's declared-shift sentence template is car 3's.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

This sitting MINTS NO NEW KEY. Its one LIGHTING act (car 3) lights a key WF-1a registered on 2026-08-16, so `contributionLedgerShape.test.js:118-119` and `VIRTUAL_SUBSYSTEM_ROWS.length` are EXPECTED UNCHANGED (35 at this tip — `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length`) and the car states the numbers it read off HEAD at its base and at the rebase; if a TR lane's dark mint composed first the numbers are higher and the literals are THAT lane's to have moved — this car touches them only if HEAD says so. Law 2's "last car" is car 3: built at landing, rebased, composed after D0. The covering-array figures ARE expected to move (a preset declaration of a registered key) and are re-measured, never inherited.

## THE VOLUME ROWS THIS LANE CHANGES (ADDENDUM R6 — the chair writes the dated LANDING BLOCK into `docs/DESIGN_FP_ARCH_WF.md` on the ledger from this list; `collect-932.sh` refuses a landing whose volume lacks it)

`§2` the flag table row `faithUnseatingEnabled` → "LANDED-LIT (four presets via `SIM_LIT`, §932 lane 16a car 3)"; `§2` "Nothing in this program lights a flag" → superseded by the 16:5x order (dated); `§2` a new paragraph THE FAITH DOOR (the two-preset `SIM_LIT_FAITH_SPREAD` cohort, O-12, the successor-id road) — the SPREAD-lane rows of the table gain "roster: `dramatic_campaign`, `full_simulation`"; `§4 WF-0` → "LANDED (§932 lane 16a cars 1–2): the case `faith_bearing_v1` at `scripts/audit/soakCases/faithBearing.mjs`; `deityBearers` exported into the v5 receipt; the two certification rows name the case" and the RECORD≠CODE cure of "SET_PRIMARY_DEITY/IMPOSE_CULT (mutateEntities.js:820/:864)" → the embed path (`commitDeityEmbed`; `reEmbedPrimaryDeity`); `§4 WF-1` → "LANDED-LIT-WHOLE" with the six shas + the lighting car; `:406-412` "NEW leaf patronFall.js … ≤ 200 eff" → "landed, 28 eff"; `§1b V29` → the release corpus is deity-free AND one bearing case now exists; the history fold's §A.2 WF-0/WF-1 rows folded as the first landing block.

## OWNER-GATED, KEPT OUT

- The LIT SUCCESSOR preset id (O-1's birth form) that would let the SPREAD-lane WF keys light in all four presets — an owner-gated public-surface act; named to the chair, not priced. Lighting `faithSpreadEnabled` on a legacy id is REFUSED by O-12 and by car 0's arm 4.
- A fifth fall cause (`abandoned`) — §215.2(1)/§400's producer door; not this sitting's.
- Tuning: none authored here (WF-0 mints no dial; WF-1's are landed and chair-signed).
- Push/deploy/migration/deletion/spend; the golden master; any raise of a ceiling (`whole-world-soak.mjs` 780/800 is extraction-first).
- Persisted shapes widened: NONE (the v5 receipt field is an INSTRUMENT, not world state — J-WF-12; `patronFalls` is WF-1a's, landed) — recorded.

## RISKS

**Grep traps (HISTORY-WF's paragraph after §B + RECHECK §5 + HISTORY-TRADE's twelve, VERBATIM by reference; the ones that bite this sitting):** `faithUnseatingEnabled: true` appears ONLY in tests at the base — a grep that finds it in `tests/` is not a preset; `feat(WF` is the only clean landing door (`--grep='WF-[0-9]'` matches bodies); `W-F<n>` (Phase 4) ≠ `W-FAITH F<n>c` ≠ `WF-<n>`; `deityBearers` is a private helper (one caller) — the export is the whole edit; `whole-world-soak.mjs:112`/`:131` cites have rotted to `:248`/`:713` — navigate by `buildFixture`; `SET_PRIMARY_DEITY`/`IMPOSE_CULT` at `mutateEntities.js:820/:864` (the volume) are now composed in `eventComposer/buildEvent.js` and the production writer is `applyWorldPulse.js reEmbedPrimaryDeity`; `religionState` (singular) is a DEAD POINTER (lane 5's trap, but a fixture author reaches for it); `faithProfile` is the pulse-minted projection on `settlement.config`, not WF state; the addendum's `src/config/simulationRules.js` is `src/domain/worldPulse/simulationRules.js` at the consist.

**Collision surfaces:** `simulationRules.js` (every #32 lane's mint/LIT car + D0 — parallel-safe ONLY because LIT cars are last and rebased; the register tail and BOTH cohorts are CHECK-GIT-FIRST, resolved by MEASUREMENT never OURS/THEIRS — the §931 half-carried-mechanism hazard); `tests/lint/tradeConvergenceContract.walker.test.js` (D0 car A → this car 0, one edit, once); `whole-world-soak.mjs` (WF-0 ↔ TR-9 car 3 ↔ WF-9 — SERIAL, one at a time; the first to board creates `soakCases/`); the witness golden (every LIT car re-records by hand — the composed tree's golden is the base, never the slot's).

**The addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R1 (the `SIM_LIT` home; car 0 EXTENDS it with a second, two-preset cohort under a recorded reason — a vetoable ruling of the family plan §3 C0-F, and the fallback if the chair reads PROTO §1.1's "do not light half the presets" strictly is that the three SPREAD-lane WF keys land DARK with their lighting priced to the successor-id act); R2 (this sitting's LIT car has no lit-before precondition and lands after D0; it unblocks every later WF LIT car); R3 (lighting-only here; no literal moves); R5 (car 3 counts ≈9 handwritten — under the cap; no 7a/7b split needed).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls — a gate line with no PRINTED test count DID NOT RUN; both size suites skip silently without `dist/`; `deno.lock` re-freeze before `--frozen`; `git grep` without a rev reads the INDEX; the soak suites under load — measure alone at load < 8; a moved DRIVEN GOLDEN (`*DormancyGolden` drives literal rules) is a STOP and the chair's bisect.

**Design hazards:** THE FALSE-CLAIM SHAPE — a lit key that moves nothing in a shipped world: this sitting states the DATA ceiling once (the plan §1's declared-shift sentence) and proves the lit twin on the bearing fixture rather than on the corpus; the O-12 relabel — never light `faithSpreadEnabled` in a third preset; the `SIM_LIT` line must be `key: true,` (a `===` spelling in the register file would join FENCE 3's four-site census); the WF-1 row at `subsystemRowsVirtual.js:460` NEVER moves (index = identity); the `soakCases/` sidecar is the extraction — `whole-world-soak.mjs` at 780/800 takes ≤ 6 lines or extracts first; the fixture's deities go through `commitDeityEmbed`, never a hand-built snapshot (the doctrine pin and the `noPremadeDeityPool` walker both bite).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (NONE in this lane — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title (cars 0, 1, 3); the mutation manifest per new invariant file (cars 0, 1) and per new arm (car 3); no wiring census stamp (no prose byte in this sitting — the DS-FTH pin READS the corpus); the edge bundles at cars 0 and 3; the Seat/Lane trailer `Seat: Opus 5 — Fable-unvalidated` · `Lane: 16a`; porcelain 0. At car 3 (built on the consist after D0 — ADDENDUM R2(iii); never gated in the dock) additionally: the witness, the stability test, the two freshness tests, the engine-gated walker (directions 1–3), the totality walker, `tradeConvergenceContract.walker` (the D0 arm), the espionage fence 21/21, `prose-manifest-cells.mjs --out` base/tip zero rows, the generation classified diff 525/525 unchanged, the soak suites alone at load < 8.
