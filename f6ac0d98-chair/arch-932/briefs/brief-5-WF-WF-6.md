# THE LANE — PRICING §C lane 5 · WF-6 FAITH TERMS (`faithTermsEnabled` + the lighting of `believedDevotionEnabled`; car 0 the crossing repair + the rung ruling)

Architect: the Fable seat (family WF), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED = opened at the cited line by me or a recon skeptic; PLAUSIBLE = reasoning. Line numbers rot; re-measure by SYMBOL. Protocols `…/f6ac0d98…/scratchpad/arch-932/ARCH-PROTOCOLS.md` (PROTO §n) via `…/arch-932/ARCHITECTS-CARD.md`; `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` WINS. Family plan `…/arch-932/plans/plan-WF.md` §1 row 5, §3 C0-D, §5 "5 — WF-6"; where the code contradicts it this brief says so (one place: `selfBandsOf`'s signature, below).

**Cars: 6 (0, 1, 2, 3, 4, 5) + car 5b if the LIT car exceeds 12 files (ADDENDUM R5) + 1 repair allowance = 6–8.** PRICING §B 4–7 C; HISTORY-WF §A.4 **6–9** ("+2: the CROSSING REPAIR and the RULING CAR … a term that has never fired, not one that already does; the executor car does not narrow"); ⚠ VETOABLE SEAM (HISTORY §A.4, plan §6 item 2): `pactFormation.js` is GRAMMAR's — if the chair re-homes the `:217` repair to GR, this lane returns to 5–8 and GR gains the car; the work exists either way.

**Prerequisites (anchors):** lane 16a ON THE CONSIST (the `SIM_LIT_FAITH_SPREAD` cohort exists EMPTY; the deity-bearing fixture `faith_bearing_v1` exists; `faithUnseatingEnabled` lit — `brief-16a-WF-WF-0-WF-1.md`); **TR-5 car 1 on the consist** (PRICING §A.4.3: "WF-6 boards lane 5 when TR-5's catalog car has landed"; the T9 STOP — no second lane in flight on `peaceTerms*.js`; `brief-2-TR-TR-5.md` car 1's exit names this handoff); GR-2 `a18fdcfae`, GR-3a `0be4800db`, GR-3b, SP-B `4c0f2f38b` landed (CONFIRMED ancestors). For the LIT car ONLY: **TR-5 car 7a on the consist** (it lights `pactFormationEnabled` + `beliefAxesEnabled` + `believedScarcityEnabled` in the four lit presets — R2's lit-before for this lane's two keys).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** NEEDS lit first — (i) `pactFormationEnabled` (GR-2's door, `pactProposals.js:132-134 pactFormationActive` — the ONE `=== true`; every faith term is drafted by `advancePeacetimePacts` behind it, `pactFormation.js:625`); (ii) `beliefAxesEnabled` (SP-B's host; `beliefAxes.js:89-97 subjectAxesActive` refuses without it and `believedDevotionEnabled` is a CONJUNCTION with it — `simulationRules.js:247`); both lit by TR-5 car 7a; (iii) door D0 and lane 16a car 0 (the cohort). WHAT IT UNBLOCKS: WF-5b's `toleranceGuaranteeFor` surfacing arm (lane 19b); WF-2a's `pilgrimageRightFor` flip from refusal to live grant (lane 17a); GR-5 renewal, POP-5b (`migrationRightFor` `treatyEnforcement.js:300`) and WF-3 (lane 16b) on the pact files — all board AFTER this lane on `peaceTerms*.js`/`pactFormation.js`. ⚠ THE PRESET-INTERSECTION FACT, DECLARED (a mechanism fact, not a lighting-table violation): `faithTermsEnabled` is SPREAD-lane → lit in `dramatic_campaign` + `full_simulation` only (lane 16a car 0's O-12 ruling); the faith crossing reads the counterparty through the belief map, which exists only where `infoMode !== 'omniscient'` (`beliefMap.js:204-208 beliefsActive`) — `living_realm` (`perfect_delayed`) and `full_simulation` (`full`); `dramatic_campaign` is omniscient. So a faith pact DRAFTS in exactly ONE lit preset, `full_simulation`; in `dramatic_campaign` the crossing returns `NO_CROSSING` on the belief side by construction and the lit key moves only the EXECUTORS over hand-signed or DM-imposed instruments. The lit twin drives `full_simulation`; the declared-shift sentence says this in words.

**Shared files this lane declares (PROTO §7.3 rule 1), eff/ceiling at the cut:** `src/domain/worldPulse/pactFormation.js` 426/800 (GRAMMAR's — the `selfBandsOf` widening, ~5 lines, car 1) · `src/domain/worldPulse/pactTriggers.js` 133/800 (READ, not edited) · `src/domain/worldPulse/peaceTermsCatalog.js` 115/800 (ONE comment cure, car 0; NO row minted) · `src/domain/worldPulse/peaceTerms.js` **797/800 — ZERO EDITS** (the stream loop `:715-735` already executes any `stream: true` row; every executor is leaf-first) · `src/domain/worldPulse/treatyEnforcement.js` 113/800 (READ — the four readers are consumed, not edited) · `src/domain/worldPulse/religiousContest.js` 493/800 (the `missionary_access` CHANNEL read in the targeted-foothold lane, car 2, ≤ 6 lines) · `src/domain/worldPulse/religionState.js` 373/800 (the `tolerance_guarantee` breach seam reads `suppressDeity`'s receipt — READ; if a receipt field is owed it lands through the one writer, ≤ 3 lines) · `src/domain/worldPulse/peaceReasons.js` (the `common_rite` scorer — one gated read, car 2) · `src/domain/worldPulse/simulationRules.js` 352 (register tail, `SIM_LIT_FAITH_SPREAD` line, `SIM_LIT` line) · `src/domain/certification/subsystemRowsVirtual.js` 284 (the TAIL spread `...FAITH_SUBSYSTEM_ROWS`, car 1 — CHECK-GIT-FIRST: lane 16b mints the leaf if it composed first) · NEW `src/domain/certification/subsystemRowsFaith.js` · NEW `src/domain/certification/couplingRegistryFaith.js` (+ its import in `couplingRegistry.js:40-50`/the spread at `:151-159`) · `tests/property/believedWorldAxesDormancyFence.test.js` (the `believedDevotionEnabled` arms re-chartered at the LIT car — SPINE's fence, edited by the lane that lights its key, PROTO §3.3; TR-5 7a re-charters the scarcity/host arms first — SERIAL) · `tests/domain/subsystemRowsVirtual.test.js` · `tests/domain/contributionLedgerShape.test.js` · `tests/soak-harness/coveringArrayCoverage.test.js` · the four edge-shared files · the witness golden · `docs/GOLDEN_SHIFT_LEDGER.md` (dock draft). NOT touched: `sovereigntyBundle.js` (derives `TERM_FAMILIES` at call time; `faith` already exists so `catalogGrewSinceWr10()` does NOT move — VERIFY, never re-run), `negotiationPictures.js` (consumed), `beliefMap.js` 781/800, `pulseKernel.js`, `settlementLifecycleKernel.js:564` (the stage is mounted).

---

## THE STATE OF THE RECORD

- **The history pass HAS run for WF** and WINS over PRICING §B. Its WF-6 row (HISTORY-WF §A.2, re-executed by the folder): PART-BUILT — landed under GRAMMAR's name (the five rows `peaceTermsCatalog.js:228/:231/:236/:240/:245`; the crossing `pactTriggers.js:226 scoreFaithCommunion`; the four-rung lens `pactFormation.js:174-179`; the mounted peacetime lifecycle) — **and the landed half is LANDED-INERT**: `pactFormation.js:212-218 selfBandsOf` passes `row.religionState` to `devotionGroundTruth`, and `settlement.religionState` (singular) has ZERO writers repo-wide in any form (object-key, shorthand, assignment — the folder closed the census) because the pulse projects to `settlement.config.faithProfile` (`religionState.js:614-671`) and the belief side correctly hands over `worldState.religionStates[id]` (`beliefMap.js:503`, `beliefAxes.js:174`) — so `devotionGroundTruth(undefined)` → `null` → `text()` → `''` → `rungOf` = −1 → `mine < 0` → `NO_CROSSING` (`pactTriggers.js:230-232`) on every call in every world, lit or dark. **No faith term has ever been drafted** (PLAUSIBLE as a universal, CONFIRMED as a static derivation). **S2:** the attainable score set is {1/3, 2/3, 1} (`COMMUNION_RUNG_FLOOR: 2` `:80`; `DEVOTION_BANDS` five `beliefAxisSubjects.js:115`; score = (floor − 2 + 1)/(4 − 2 + 1)); `CROSSING_FLOOR01: 0.5` (`:91`, applied at `:175 crossed: score >= T.CROSSING_FLOOR01`) cuts 1/3; `rungTermsFor` picks the single highest reached rung (`pactFormation.js:285-296`; non-accumulation pinned at `pactFormation.test.js:435`) — so 2/3 reaches the 0.45 rung (`pilgrimage_right`, `tolerance_guarantee`) and 1 reaches the 0.9 rung (`temple_restitution`); **`shared_rite` (min 0) and `missionary_access` (min 0.7) can NEVER be selected**; no faith row is in `CLASS_TERM` (`peaceTermsCatalog.js:385-394`, folder-confirmed), so the catalog's own `:238-239` "the row a war's end can EXTRACT (Augsburg's darker half)" is RECORD≠CODE in both doors. Also owed: the four GRANT readers `treatyEnforcement.js:270/:278/:285/:293` have ZERO callers outside their file (only the comment `couplingRegistryGrammar.js:148` names them — re-run by me). The fold's grep traps are adopted verbatim: `religionState` singular is a DEAD POINTER; a populated `PACT_DRAFT_LENS` rung is not a reachable rung; `restitution` mostly matches WR's `restitutionClaimBand`; `pilgrim` is TRADITIONS + the term + the reserved errand row; `covert` is espionage vocabulary.
- **The volume** `docs/DESIGN_FP_ARCH_WF.md` §4 WF-6 (`:596-639`, CORRECTED WF-F round): "WF-6 compiles as a CONSUMER and mints NO catalog row"; `peaceTerms.js` ZERO-HEADROOM (net-zero seam lines only; any growth = STOP + extraction recipe); "ALL executors land in NEW leaf siblings (`faithTermExecutors.js` or per-family leaves; budget ≤ 300 eff total)"; the pins (the GUARD pin — `sacredClaim.js:39-49`'s both-patrons gate imported verbatim as the formation gate; executor honesty — `missionary_access` moves the CHANNEL never share; breach reachability under `tolerance_guarantee`; dictated + negotiated arms both reachable; Law One walker over executor writes); §5 seam 1 (`catalogGrewSinceWr10()` FIRED and ASSERTED at `sovereigntyBundleWr10.test.js:336/:341` with the grown set EXACTLY `['commercial','faith','population']` — DONE; WF-6's half: derive family membership from `TERM_FAMILIES` at call time, spell no family literal outside the catalog); §6 Q3 COMPOSE (a faith term composes in the sovereignty bundle; WF-6 adds ONE herald sentence for the faith-consideration sale on the faith desk, `heraldRouting.js:151 sovereignty_sale_judged`). ODQ §78.2 F-2 ("WF-6 re-scopes to CONSUME the already-landed GR-3 rows — headroom is 3 lines, not ~24"). The compiled programme §5 #38 WF-6 (`DESIGN_FP_ARCHITECTURE.md:1990-1999`). ⚠ THE VOLUME'S §2 ROW for this key: "`faithTermsEnabled` | SPREAD + SP-3 lit + GR-3 rows landed | WF-6 | **the formation-trigger gate in the WF-6 executor leaf**" — the door sits at the CROSSING, which is exactly where car 1 puts it.
- **The GRAMMAR fence's law that binds every WF file this lane writes:** `tests/property/pactFormationDormancyFence.test.js:198-211` asserts an EXACT FIVE-FILE set of `src/` files whose CODE (comments stripped) spells `pactFormationEnabled` (`couplingRegistryGrammar.js`, `subsystemRowsCompact.js`, `tradeConvergenceContract.js`, `pactProposals.js`, `simulationRules.js`). THE SIXTH-NAMER TRAP (brief-2's, verbatim for WF): `faithTermExecutors.js` never spells the token in code (import `pactFormationActive` if it needs the host door); the WF-6 row in `subsystemRowsFaith.js` never spells it in ANY string field (say "GR-2's pact-formation key" in prose or name it in a comment); the FAITH→GRAMMAR coupling row's `flags` column lists `['faithTermsEnabled']` ONLY (the GR key goes in the comment). `grep -rlw pactFormationEnabled src | wc -l` = 5 asserted at every car's exit.
- **THE PACT_DRAFT_LENS BOUNDS ARE TUNING** (`pactFormation.js:165-171`: "THE SIX BOUNDS ARE AUTHORED LITERALS AND THEY ARE UNSOAKED … The owner signs them at the soak redo under THE PROMISE; this file does not tune them") — RULING (plan §6 item 3, vetoable): the rung re-band that would make `shared_rite`/`missionary_access` attainable is a TUNING act; car 0 lands the ATTAINABILITY WALKER in its FIRED state (asserting BY NAME which rungs are unattainable at the current bounds) and the owner's soak signature re-bands; the missing WAR DOOR (a faith `CLASS_TERM` asset class) is a DESIGN ROW in car 0, not built here.
- **The owner's laws:** everything built lands LIT; a persisted-shape widening is TAKEN and recorded (this lane widens NONE — terms ride the treaties artifact, the peaceTerms family's own serialization); nothing pushes/deploys/applies/deletes/spends; tuning raw + derivation, ratified at the soak; the golden master never re-recorded.

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/pactFormation.js` | 426/800 | `PACT_DRAFT_LENS.faith_communion` `:174-179` — `[{min:0, terms:['shared_rite']}, {min:0.45, terms:['pilgrimage_right','tolerance_guarantee']}, {min:0.7, terms:['missionary_access']}, {min:0.9, terms:['temple_restitution']}]`; the bounds law `:165-171`; `selfBandsOf(settlement)` `:212-218` — **takes ONLY the settlement** (`const row = recordOf(settlement); … devotion: text(devotionGroundTruth(row.religionState))`) — ⚠ the plan's "the call shape carries the world state — it does: crossingsFor({worldState})" is about `crossingsFor`, NOT `selfBandsOf`; the repair WIDENS `selfBandsOf` to a second argument; `believedBandsOf(worldState, observerId, subjectId)` `:223-232` (`devotion: text(record.devotionBand)`); `crossingsFor` `:252-266` calling `scoreFaithCommunion({ ladder: DEVOTION_BANDS, proposerWord: self.devotion, counterpartyWord: believed.devotion })` `:261`; `rungTermsFor` `:285-296`; `draftPactSheet({ trigger, fromId, toId, reciprocal, tick, score01 })` `:318`; `advancePeacetimePacts` `:617` — the gate `pactFormationActive(worldState)` `:625`; `settlementOf(id)` `:632-633` (freshest update or the snapshot item's settlement); the THREE `selfBandsOf(settlementOf(…))` call sites `:714` (the responder), `:749` (`fromId`), `:763` (`toId`) — `state` (the working worldState) is in scope at all three |
| `src/domain/worldPulse/pactTriggers.js` | 133/800 | `PACT_TRIGGER_TUNING` `:73` (`COMMUNION_RUNG_FLOOR: 2` `:80`, `COMMUNION_RUNG_SPREAD: 1` `:81`, `CROSSING_FLOOR01: 0.5` `:91`); `rungOf` `:147`; `crossing()` `:175 crossed: score >= T.CROSSING_FLOOR01`; `NO_CROSSING` `:181`; `scoreFaithCommunion` `:226-245` (three refusals: no belief `:230-232`; spread `:234-236`; floor `:239-241`; the score `:243`) |
| `src/domain/worldPulse/beliefAxisSubjects.js` | 179 | `DEVOTION_BANDS` `:115` (`secular, lukewarm, observant, faithful, devout`); `STANDING_RUNG` `:186` (`cult→lukewarm, established→observant, ascendant→faithful`); `devotionGroundTruth(religionState)` `:356-373` — reads `worldState.religionStates[subjectId]` (its own JSDoc), the top unsuppressed deity's standing, +1 rung when it holds the patron seat — LIVE ground truth, rewritten every tick by `advanceReligionStates` (`pulseKernel.js:1261`) — NO join car owed (HISTORY §C.1 item 12) |
| `src/domain/worldPulse/beliefAxes.js` | 112 | `subjectAxesActive(worldState)` `:89-97` — `beliefAxesActive` ∧ any of the three family keys `=== true`; the axis ground truth handed `opts.religionState` `:174` |
| `src/domain/worldPulse/beliefMap.js` | **781/800** | `beliefsActive` `:204-208` (marker ∧ `infoMode !== 'omniscient'`); `beliefRecord` `:225-231`; the handover `religionState: asObject(ctx.worldState?.religionStates)[String(subjectId)]` `:503` — THE CORRECT SPELLING the repair copies. NOT edited |
| `src/domain/worldPulse/peaceTermsCatalog.js` | 115/800 | the five faith rows `:228-245` (`shared_rite` grant 0.4/6/15; `pilgrimage_right` grant 0.5/5/12; `tolerance_guarantee` grant 0.6/8/20; `missionary_access` grant 0.8/4/10 with the RECORD≠CODE sentence `:238-239`; `temple_restitution` transfer 0.9/3/8 `baseMag: 0.35, stream: true`); `CLASS_TERM` `:385-395` (nine classes, ZERO faith); `termLabel` faith cases `:416-420` |
| `src/domain/worldPulse/treatyEnforcement.js` | 113/800 | `grantedRightFor(w, type, grantorId, granteeId, tick)` `:246-251` (the compliance door: `complianceState !== 'defaulted'`); `grantedRightStateFor` `:262-265`; the four faith readers `missionaryAccessFor` `:270`, `sharedRiteFor` `:278`, `pilgrimageRightFor` `:285`, `toleranceGuaranteeFor` `:293` ("WF-5b's underground surfacing arm and the eviction/purge lanes read this") — ZERO callers |
| `src/domain/worldPulse/peaceTerms.js` | **797/800** | the stream loop `:710-735` (`const spec = TERM_CATALOG[term.type]; if (spec?.stream) { computeTreatyGrainDraw({ payer, payee, takeFraction: streamInstallmentFraction(...) }) … }`) — generic over any `stream: true` row, so `temple_restitution` EXECUTES the day it is drafted; `evolveCompliance` called `:705`. ZERO EDITS |
| `src/domain/worldPulse/sacredClaim.js` | 85 | the two guards `:36-50` (`isFaithSpreadEnabled` + both patrons named — "Absence of a faith is not a shared faith") — the formation gate the volume imports verbatim |
| `src/domain/worldPulse/religiousContest.js` | 493/800 | `isFaithSpreadEnabled(rules)` `:553`; the targeted-foothold candidates in the spread lane (the CHANNEL `missionary_access` opens — locate the candidate builder by symbol at the cut: the foothold lane that `deityStanceLane`'s `FOOTHOLD_COOLDOWN_TICKS` governs) |
| `src/domain/worldPulse/religionState.js` | 373/800 | `suppressDeity` `:271-280` — the ONE writer; a `tolerance_guarantee` BREACH is "a suppression of the guaranteed creed by the grantor" — the breach seam reads the suppression outcome (the fold's receipts) and never a second suppression path |
| `src/domain/worldPulse/deityStanceLane.js` | 235 | `pactOutcome` `:238-260` mints `faith_pact_formed` — a NEWS-ONLY record of two kindred faiths ("no stressor, no state mutation") — ⚠ NOT GR-2's pact; WF-6's executors read the TREATY ledger (`grantedRightFor`), never this record |
| `src/domain/worldPulse/simulationRules.js` | 352 | `believedDevotionEnabled` registered `:251` ("a CONJUNCTION with beliefAxesEnabled"); `beliefAxesEnabled` `:242`; `pactFormationEnabled` `:324`; `faithTermsEnabled` ZERO hits (NEW mint); `SIM_LIT` (D0) and `SIM_LIT_FAITH_SPREAD` (16a car 0) both exist and are EMPTY of WF keys at this lane's cut (CHECK-GIT-FIRST) |
| `src/domain/certification/subsystemRowsBelief.js` | 252 | the `believedDevotionEnabled` row `:285-292` ("ONE GATE, A CONJUNCTION … THE OBSERVATION NEEDED is the same per-field belief census") — text amended LIT at the LIT car |
| `src/domain/certification/subsystemRowsVirtual.js` | 284 | the tail spreads through `...ENCOUNTERS_SUBSYSTEM_ROWS` `:584`; NO faith leaf exists — this lane MINTS `subsystemRowsFaith.js` (CHECK-GIT-FIRST vs lane 16b) |
| `src/domain/certification/couplingRegistryGrammar.js` · `couplingRegistry.js` | — | GR-3's GRAMMAR→FAITH forward reference `:26-28` ("FAITH's WF-6 is the CONSUMER wave — a pointer, never a second"); the aggregate spreads family leaves `couplingRegistry.js:151-159` — no `couplingRegistryFaith.js` exists (`ls` CONFIRMED) |
| `tests/property/pactFormationDormancyFence.test.js` · `believedWorldAxesDormancyFence.test.js` | — | the five-member namer set `:198-211`; SP-B's fence over the three family keys (`FLAGS` `:73`; the independence goldens `:338-357` map `believedDevotionEnabled → 'devotionBand'`; fence 3's spy at the `beliefAxisSubjects` boundary `:294`) |
| `tests/domain/pactFormation.test.js` | — | `grep -c religionState` = 0 (every ladder test injects a synthetic `score01`, `:416-421`; the single-rung pin `:435`) — the seam no suite sees |
| ABSENT | — | `faithTermExecutors.js`, `faithTermsEnabled`, `subsystemRowsFaith.js`, `couplingRegistryFaith.js`, `faithTermsDormancyFence.test.js`, `faithCommunionAttainability.walker.test.js` (zero hits each) |

**Budgets:** worker 1,404,493 EXACT — none of these files is on the generation graph (quote the `stat`); `engine` chunk ±0 (worldPulse leaves ride the advance worker — quote); first-paint: the LIT car's two cohort lines ≈ 60–120 B (PLAUSIBLE) — quote the table, the margin being contested.

## ALREADY DONE (do not re-land)

GR-2 the whole peacetime lifecycle (mounted at `settlementLifecycleKernel.js:564`, dark under `pactFormationEnabled`) · GR-3a the five faith rows + `executor:'grant'` + `termLabel`s · GR-3b the four-rung `faith_communion` lens · GR-3b's four faith readers over the generic `grantedRightFor` (the SHAPE this lane consumes, never re-implements) · `catalogGrewSinceWr10()` FIRED and pinned with `faith` in the grown set — VERIFY (`sovereigntyBundleWr10.test.js:336/:341`), never re-run · SP-B's devotion family with a LIVE ground truth · the `temple_restitution` stream physics (the head's generic loop) · the `restitution` precedent (`peaceTermsCatalog.js:168`) · `termObligationOf`'s negotiated arm (`treatyOrientation.js:256-275`) · the compliance door (`grantedRightFor`'s `defaulted` refusal).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned files; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: 5`; PACKET_STANDARD caps; the packet `docs/implementation/packets/fp/WF-6.md` in the dock; `grep -rlw pactFormationEnabled src | wc -l` = 5 at every exit.

### Car 0 — THE RULING CAR: the attainability walker in its FIRED state, the catalog's false sentence cured, the war door and `temple_restitution` ruled as design rows (no behaviour byte)

**In plain words:** the treaty engine already knows five faith clauses and a rule for choosing among them by how devout two courts believe each other to be — but the arithmetic means two of the five can never be chosen, and a comment claims one of them can be forced at a war's end when no war ever drafts it. This car writes a permanent test that says out loud which clauses are unreachable at today's numbers (so the day the owner retunes them the test asks to be updated), fixes the false comment, and records the design decisions the rest of the lane builds on.

**The change.** NEW `tests/lint/faithCommunionAttainability.walker.test.js` (≤ 120 eff): computes the ATTAINABLE SCORE SET from the live constants — `for floor in [COMMUNION_RUNG_FLOOR .. DEVOTION_BANDS.length-1]: (floor − F + 1)/(top − F + 1)` filtered by `>= CROSSING_FLOOR01` — and, for each `PACT_DRAFT_LENS.faith_communion` rung, whether ANY attainable score selects it under `rungTermsFor`'s highest-reached rule; asserts BY NAME in the FIRED state: `attainable = [2/3, 1]`, `reachableTerms = ['pilgrimage_right', 'tolerance_guarantee', 'temple_restitution']`, `unreachableTerms = ['missionary_access', 'shared_rite']` with the message "a TUNING act (the six bounds law, pactFormation.js) — the owner re-bands at the soak; update this walker in the SAME commit"; an anti-vacuity arm plants `COMMUNION_RUNG_FLOOR = 1` in-test and shows `shared_rite` becomes reachable (the walker can see a re-band). `peaceTermsCatalog.js:238-239` COMMENT cure: "the row a war's end can EXTRACT (Augsburg's darker half)" → "priced like a concession; ⚠ at this tip NO `CLASS_TERM` asset class drafts any faith row and the peacetime rung (min 0.7) is unattainable under the current bounds — see tests/lint/faithCommunionAttainability.walker.test.js; a faith war door is a WF-6 design row" (a `src/` byte — build + verify:dist, byte-identical dist expected; `peaceTermsGrantTerms.test.js` + `sovereigntyBundleWr10.test.js` re-run green). DESIGN ROWS written into the packet and the dock's `docs/DESIGN_FP_ARCH_WF.md` §4 WF-6 (amended, never deleted): (D1) THE WAR DOOR — a faith asset class `sanctity` in `CLASS_TERM` mapping to `missionary_access` is NOT built in this lane (a `CLASS_TERM` row is `peaceTermsAppraisal`'s victor-lens and needs an appraisal of the loser's BELIEVED faith holdings — a GRAMMAR/WAR seam; recorded as a WF-6b design row, owner-visible, vetoable; the dictated arm this lane builds is car 3's READ of a faith term that arrives on a treaty by any door); (D2) `temple_restitution` SEMANTICS — a conserved GRAIN installment stream from grantor to grantee (the `restitution` precedent verbatim — `computeTreatyGrainDraw`), re-labelled by the catalog as "the repair of an impaired temple-class institution"; the grain is the honest denomination (W-COIN's treasury is STATE money — HISTORY-TRADE (e); no coin is moved, no `treasury` field is read — pinned by an import-source scan on the executor leaf); (D3) `shared_rite`'s consumer is `peaceReasons`' existing `common_rite` scorer (the catalog's own words `:225-226`); (D4) the ONE-PRESET drafting fact (the header of this brief).

**Files (≈3):** the walker (NEW), `peaceTermsCatalog.js` (comment), the packet/volume dock copies. **Pins:** the walker's FIRED assertions + its anti-vacuity plant; the two GR pins green. **Census bills:** refreeze own commit (new test file); manifest `kind:'mutation'` (the planted re-band). **Byte rule:** comment-only src byte — build quoted, dist byte-identical stated. **Gate:** full list. **LIT act:** none. **Exit:** the walker green in its fired state; the sentence cured; the four design rows written; `catalogGrewSinceWr10()` unchanged (asserted).

### Car 1 — THE CROSSING REPAIR + THE DARK MINT: `faithTermExecutors.js`'s door, the self side of `faith_communion` reads the LIVE religion state under that door, the key registered dark with its row

**In plain words:** the treaty engine asks each court "how devout are you?" by reading a field that nothing has ever written, so the answer is always blank and no faith pact has ever formed. This car hands it the real answer — but only when the new faith-terms switch is on, so nothing changes for any world until the switch is thrown — and registers that switch, dark, with the certification row the estate requires in the same commit.

**The change.** NEW `src/domain/worldPulse/faithTermExecutors.js` (≤ 300 eff total across the lane; this car ≤ 80): `export function faithTermsActive(rules)` — THE ONE by-name read `rules?.faithTermsEnabled === true && isFaithSpreadEnabled(rules)` (SPREAD lane: `religiousContest.js:536-553`'s idiom — the conjunct is the reader, not a second flag); `FAITH_TERMS_TUNING = Object.freeze({})` (THE WF TUNING-HOME LAW — the table exists from the first car even if empty; every later dial lands inside it with an executed derivation). `pactFormation.js` (GRAMMAR's; ~5 lines, a CW-0 coupling row lands in the SAME commit): `selfBandsOf(settlement, religionState)` — the second parameter; `devotion: text(devotionGroundTruth(religionState))` (the dead `row.religionState` read REMOVED — it was a key nothing writes); the three call sites `:714/:749/:763` pass `faithTermsActive(state.simulationRules) ? asObject(state.religionStates)[id] : undefined` (the belief side's spelling at `beliefMap.js:503`, mirrored; `asObject` is the file's `recordOf`) — under a dark door the argument is `undefined` and the crossing is BYTE-IDENTICAL to today's `NO_CROSSING` (GR-2's fence 1 dark identity holds; TR-5 7a's re-chartered LIT arm still moves through `shared_threat`). Import `faithTermsActive` from the WF leaf (the TR-5 car 3 precedent: `pactFormation.js` importing `tradePactsActive` from `commercialDemandTrigger.js`). THE DARK MINT in the same commit (ADDENDUM R3 as amended — `engineGatedRuleKeys.walker.test.js:538-541 scanGateReads` reads every `src/` file, so the by-name read without its register entry reds `unaccountedReads` `:501-502`; `manifestWithoutRow` `:518` forbids the entry without its row; `subsystemRowsVirtual.test.js:646-647` pin the ordered `VIRTUAL_RULES` and `VIRTUAL_PENDING_RULE_KEYS = []`; `contributionLedgerShape.test.js:118-119` pin both lengths): `simulationRules.js` register TAIL gains `'faithTermsEnabled'` (comment: WF-6 · SPREAD lane · the two-preset roster + O-12 by symbol · DARK at this car); NEW `src/domain/certification/subsystemRowsFaith.js` (≤ 250; the FAITH tail leaf) with the `faithTermsEnabled` row (25–40 eff; THE EVIDENCE LAW: `eventTypes` traceable to a `candidateType:` literal in the lane leaf — `pact_proposed`/the signing receipts are GR-2's, so declare `eventTypes: []` DELIBERATELY EMPTY AND MEASURED as WF-1a's row does, or name ONLY a `candidateType` this leaf mints in car 3 (the herald sentence's receipt) — state which; `stateKeys: []` — terms ride the treaties artifact, not a v5 container; `other` prose names "GR-2's pact-formation key" and NEVER spells `pactFormationEnabled`); `subsystemRowsVirtual.js` gains `...FAITH_SUBSYSTEM_ROWS` at the TAIL (after `...ENCOUNTERS_SUBSYSTEM_ROWS` `:584`; CHECK-GIT-FIRST — if lane 16b composed first the leaf exists and this car APPENDS a row); the three `tests/domain/subsystemRowsVirtual.test.js` edits (the key const; the `VIRTUAL_RULES` member IN POSITION at the tail; the `LANE_LEAVES` entry `faithTermsEnabled: 'src/domain/worldPulse/faithTermExecutors.js'`); BOTH `contributionLedgerShape.test.js:118-119` literals READ OFF HEAD at this car's base (+1; 35 → 36 if first, else N+1 — NEVER inherited); the covering-array `union` re-measured (`flagDomainCensus` out of band; `union` +1, `governed`/`ungoverned` UNMOVED — a virtual key is never normalizer-governed); the four edge-shared files regenerated. NEW `tests/property/faithTermsDormancyFence.test.js` in the DARK shape (fence 1: `advancePeacetimePacts` on the bearing fixture under `pactFormationEnabled: true` + the belief keys lit LITERALLY but `faithTermsEnabled` ABSENT ⇒ the pact ledger byte-identical to the pre-car engine — computed in-file, no 64-hex; fence 2 absent ≡ explicit false; fence 3 the `devotionGroundTruth` call count from the self side is ZERO dark and MOVES lit — a spy at the leaf boundary, the SP-B fence's `:294` shape; fence 4 gate polarity — every read `=== true`, exactly ONE site `faithTermExecutors.js`, the truthy-non-true probes `1`/`'true'`/`{}` dark; the LIT-MUTANT CONTROL: the same fixture with `faithTermsEnabled: true` and `faithSpreadEnabled: true` yields a `faith_communion` crossing whose `crossed === true` and a drafted sheet on the 0.45 or 0.9 rung). NEW `src/domain/certification/couplingRegistryFaith.js` with the FAITH→GRAMMAR row (`read: 'src/domain/worldPulse/pactFormation.js#selfBandsOf'`, `flags: ['faithTermsEnabled']` ONLY — the GR key in the comment; `owningWave: 'WF-6'`) + the import/spread in `couplingRegistry.js` (append-only); `tests/domain/faithTermExecutors.test.js` (NEW): the POSITIVE CONTROL FIRST — a two-court fixture from `faith_bearing_v1` with real `religionStates` entries (both courts `established`/`ascendant` on unsuppressed deities, the observer's belief record carrying `devotionBand`) crosses at 2/3 or 1; the NEGATIVE CONTROL — the repair reverted (self side `undefined`) → `NO_CROSSING` every call.

**Files (≈12 handwritten — AT the cap):** `faithTermExecutors.js` (NEW), `pactFormation.js`, `simulationRules.js`, `subsystemRowsFaith.js` (NEW), `subsystemRowsVirtual.js`, `couplingRegistryFaith.js` (NEW), `couplingRegistry.js`, `tests/domain/subsystemRowsVirtual.test.js`, `tests/domain/contributionLedgerShape.test.js`, `tests/soak-harness/coveringArrayCoverage.test.js`, `tests/property/faithTermsDormancyFence.test.js` (NEW), `tests/domain/faithTermExecutors.test.js` (NEW); regenerated: the four edge-shared files. ⚠ If the count exceeds 12 at the cut, the coupling row + `couplingRegistry.js` move to car 2 (their own same-commit law binds to the car that adds the READ they describe — `selfBandsOf` is this car's; prefer keeping them here and moving the covering-array edit to a 1b bills commit — R5's split shape). **Census bills:** refreeze own commit (three new test files); manifest ×2 (the fence with a planted regression — the door removed; the executor test); the two edge bundles + metas. **Byte rule:** `simulationRules.js` is EAGER (the register string ≈ 20–40 B) — quote the table; worker unchanged; engine ±0 quoted. **Gate:** full list + the engine-gated walker (directions 1–3; `ENGINE_GATED_DORMANT_RULE_KEYS` CONTAINS `faithTermsEnabled` — measured), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two freshness tests, the totality walker, `pactFormationDormancyFence` (fence 1 dark identity + the five-member set unchanged), `believedWorldAxesDormancyFence` (unchanged at this car). **LIT act:** none (DARK mint). **Exit:** a real fixture crosses lit and refuses dark; the mint registered dark with its row; both literals read off HEAD and stated; the five-namer set unchanged.

### Car 2 — the four idle readers bite: `toleranceGuaranteeFor` → a breach, `missionaryAccessFor` → the channel, `sharedRiteFor` → `common_rite`, `pilgrimageRightFor` → the refusal arm (declared dark until lane 17a)

**In plain words:** the four faith promises a treaty can carry — "we won't suppress your creed", "your preachers may enter", "we keep the rites together", "your pilgrims may travel our roads" — have been readable for six weeks and read by nothing. This car makes each one matter: breaking the first becomes a treaty breach, the second lets a creed try to gain a foothold where it could not before, the third gives the peace-reasons scorer a signed rite to point at, and the fourth is honestly refused until the pilgrim season exists.

**The change.** `faithTermExecutors.js` (+≤ 160): `toleranceBreachFor(worldState, grantorId, granteeCreedRef, suppressionReceipt, tick)` — when `toleranceGuaranteeFor(w, grantorId, granteeId, tick)` holds and the grantor's fold SUPPRESSED the guaranteed creed (`suppressDeity`'s outcome — read from the fold's receipts, `religiousContest.js`'s `outcomes[]`; never a second suppression path), the term's `complianceState` is moved to `defaulted` THROUGH the compliance family's existing writer (`treatyBreach.js`/the peaceTerms compliance road — VERIFY-AT-BUILD which leaf writes `complianceState` on an observed breach; if the only writer is the head `peaceTerms.js:705` block, the car STOPs and proposes the extraction, never a head line); `missionaryChannelFor(worldState, grantorId, granteeId, tick)` — a boolean the targeted-foothold candidate builder in `religiousContest.js`'s spread lane consults (≤ 6 lines there, one gated read behind `faithTermsActive`): a creed holding `missionary_access` is ADMITTED to the grantor's foothold candidates (the CHANNEL); it moves NO `share`, NO `standing`, NO `patronRef` (EXECUTOR HONESTY — pinned by a source scan of the leaf for `share`/`standing`/`patronRef` writes: zero); `sharedRiteFor` → `peaceReasons.js`'s `common_rite` scorer gains one gated read (+2 lines): a signed `shared_rite` between the pair scores as a live common rite (the catalog's own promise `:225-226`); `pilgrimRoadFor(worldState, grantorId, granteeId, tick)` — returns `{ granted, refusal: 'pilgrimage_season_dark' }` while `pilgrimageEnabled` is not lit/landed (the volume's "refusal receipted, no dead guarantee certified"; lane 17a flips this arm to a live grant and re-runs THIS pin as its positive control). THE GUARD PIN: `sacredClaim.js:39-49`'s both-patrons gate is imported as `formationGuard(a, b)` — a pair where either court names no patron drafts NO faith term even lit (the crossing's `secular` rung already refuses below the floor, but the guard is the volume's pin and the doctrine's: "absence of a faith is not a shared faith").

**Pins (`tests/domain/faithTermExecutors.test.js` + `tests/domain/faithTermBreach.test.js`):** each reader true/false on a drafted-and-signed pair (through `signPactProposal` `pactFormation.js:444`, never a hand-built ledger); the compliance door (a `defaulted` guarantee grants nothing); THE BREACH: a suppression of the guaranteed creed under a live guarantee defaults the term and mints the breach grievance; the dark twin (no term ⇒ the suppression is policy, not breach — byte-identical receipts); the CHANNEL: a creed with `missionary_access` appears in the foothold candidates where the same creed without it does not, and its `share` is unchanged after the tick (executor honesty); `common_rite` scores on a signed `shared_rite` and not otherwise; the refusal receipt for `pilgrimage_right`. **Mutants:** the compliance door severed (a defaulted guarantee still bites) → reds; the channel executor writing `share` → the honesty scan reds; the guard dropped → the mixed-fixture negative reds.

**Files (≈6):** the leaf, `religiousContest.js` (≤ 6), `peaceReasons.js` (+2), two test files, the manifest; the coupling rows FAITH→GRAMMAR (`peaceReasons` read) in `couplingRegistryFaith.js`. **Census bills:** refreeze; manifest ×2. **Byte rule:** build quoted; worker 0; engine ±0. **Gate:** full list + `pactFormationDormancyFence` fence 3's spy (the dark count ZERO — the seams sit inside the lit path). **LIT act:** none. **Exit:** four readers consumed; honesty scan green; the refusal arm receipted.

### Car 3 — the two arms and the bundle: the negotiated arm end to end (`temple_restitution` streams grain), the dictated arm as a READ of a faith term that arrives by any door, the sovereignty-bundle composition fixture, one herald sentence

**In plain words:** with the crossing repaired, two devout courts believed devout of each other can now negotiate a temple-restitution pact and grain actually moves between their granaries in conserved installments; a faith clause that lands on a treaty by any other road (a DM's hand, a future war door) is honoured the same way; a settlement sold for missionary access composes in the existing sovereignty bundle without a single new literal; and the town crier learns one sentence for the sale.

**The change.** THE NEGOTIATED ARM (tests + ≤ 2 leaf lines): on `faith_bearing_v1` under `full_simulation` rules with `pactFormationEnabled`, `beliefAxesEnabled`, `believedDevotionEnabled`, `faithTermsEnabled`, `faithSpreadEnabled` lit LITERALLY (the mechanismLitCoverage credit rule), two courts with a devotion crossing of 1 (both `ascendant` on the patron) → `draftPactSheet` drafts `temple_restitution` (the 0.9 rung) → `signPactProposal` → the stream loop moves `storageMonths` through `computeTreatyGrainDraw` (`deliveredToVictor > 0`, `extractedFromLoser ≥ deliveredToVictor` — the spoilage sink) — receipts at every link; the SAME fixture with `faithTermsEnabled` absent byte-identical. THE DICTATED ARM: a treaty carrying `tolerance_guarantee` installed through `materializeCarriedTermSheet` (`peaceTerms.js:211` — the existing carried-sheet boundary, READ not edited) is honoured by car 2's breach executor exactly as a negotiated one (the executors read the ledger, not the door the term came through); the D1 design row (a faith `CLASS_TERM` class) stays a row. THE BUNDLE FIXTURE (Q3 COMPOSE): a `sovereigntyBundle` composition where the consideration is `missionary_access` composes through the DERIVED `TERM_FAMILIES` — the source-scan pin both directions: `faithTermExecutors.js` spells NO family literal (`'faith'`) outside `TERM_CATALOG` reads, and `sovereigntyBundle.js` is UNTOUCHED (`catalogGrewSinceWr10()` value asserted unchanged). ONE HERALD SENTENCE: the faith-consideration sale on the existing `sovereignty_sale_judged` kind (`heraldRouting.js:151` — an EXISTING desk-bearing kind; this car adds a VARIANT to its pool, not a new kind — so no new walker file; the annex `docs/content/RECEIPT_POOLS_*` row the pool reads is amended and `node scripts/wiring-census.mjs` re-stamped; prose-numerics: words, no digits). K3 twin: the self side is NEVER read through the belief map (`selfBandsOf`'s own header law — pin: `beliefRecord(x, x)` is called zero times during formation).

**Files (≈6):** `tests/domain/faithTermTellables.test.js` (NEW), the leaf (≤ 2 lines), the pool/annex row, `tests/domain/sovereigntyBundleWr10.test.js` (the composition arm — an ADDED test, no existing anchor dropped), `couplingRegistryFaith.js` (the FAITH→WAR bundle row), the manifest. **Census bills:** refreeze; manifest; **the wiring census stamp** (`node scripts/wiring-census.mjs` — a prose byte); prose-numerics. **Byte rule:** build quoted. **Gate:** full list + `heraldContaminationFence` + `proseWiringCensus.walker` + `sovereigntyKindPools.walker`. **LIT act:** none. **Exit:** grain moves on the fixture lit; dark identical; the bundle composes; the sentence passes its pool walker.

### Car 4 — the K3 absurdity twin, the refusal receipts, the GRAMMAR→FAITH coupling rows closed, the DECLARATIONS written

**In plain words:** one more story is pinned — a court that signs a temple-restitution pact for grain it no longer has defaults and the default is read as a grievance rather than a silence — and the lane writes down, where the next reader will find them, the facts it cannot change: the pact drafts in one preset only, two clauses are unreachable until the owner retunes, the war door is a design row.

**The change (tests + docs):** the K3 twin (`temple_restitution` signed, the grantor drained to the reserve floor → the installment returns null → `complianceState` slips → the breach grading mints `contract_default` — the treatyBreach family's existing road; CHECK-GIT-FIRST: TR-5 car 4 may have created a `commercialSeamsFor` registry — WF adds no seam producer, the faith breach is a receipt only); the refusal receipts census (`pilgrimage_right` refused with `pilgrimageEnabled` dark; a secular pair refused by the guard; a `dramatic_campaign` pair refused on the belief side — the one-preset fact, asserted with the preset's own rules object); the `couplingRegistryFaith.js` rows complete (FAITH→GRAMMAR ×2, FAITH→WAR ×1) with `tests/lint/couplingInclusion.walker.test.js` green — `faithTermExecutors.js` matches `LAYER_PATTERNS`' faith regex `:187` (`/^src\/domain\/worldPulse\/(?:faith|sacred|religion|pantheon|conversion|piety|deity|temple)/`) so no pattern edit is needed (state so). THE DECLARATIONS in the packet, the volume's dock copy and the register comment: (1) the one-preset drafting fact; (2) the two unreachable rungs (the walker's names); (3) the war door as design row D1; (4) `temple_restitution` = conserved grain, no coin (the import-source scan: the leaf imports nothing from `treasury.js` — a pin); (5) the executors are READERS not a second evaluator (`negotiationPictures.js` is the one — seam 3).

**Files (≈4):** the K3 test arm, `couplingRegistryFaith.js`, the packet, the volume dock copy. **Census bills:** refreeze if a title changed. **Byte rule:** no src byte (state so) or build quoted. **Gate:** full list + `couplingInclusion.walker`. **LIT act:** none. **Exit:** K3 mints; the refusal census green; the rows complete.

### Car 5 — THE LIT CAR (5a; 5b the bills if > 12 files — ADDENDUM R5): `faithTermsEnabled` → `SIM_LIT_FAITH_SPREAD` (two presets); `believedDevotionEnabled` → `SIM_LIT` (four presets); the fences to the LIT shape; the witness; the shift ledger — BUILT AT LANDING ON THE CONSIST, rebased, after TR-5 7a and lane 16a

**In plain words:** two switches. The faith-terms machinery is turned on in the two living presets where faith already spreads between towns; and a court's ability to believe how devout its neighbours are — a spine feature another family built — is turned on in all four living presets, because without it the faith crossing has nothing to compare. Both are written down with their movers named, and the fact that a faith pact can actually form in only one of the presets is said in plain words.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 — `SIM_LIT` (four presets) and lane 16a car 0's `SIM_LIT_FAITH_SPREAD` (two presets, the O-12 reason recorded) are the lawful homes; the `:367` walker is D0's — run and quoted, never edited. R2 — composes AFTER TR-5 7a (`pactFormationEnabled` + `beliefAxesEnabled` lit) and after lane 16a; a dock-green LIT car is a FALSE GREEN. R3 — the dark mint was car 1; this car is the LIGHTING and RE-READS every count off the composed HEAD. R5 — if the handwritten count exceeds 12, 5b carries the bills and literals.

**The change (PROTO §1.2 items 2, 3, 10–12):**
1. `simulationRules.js`: `SIM_LIT_FAITH_SPREAD` gains `faithTermsEnabled: true, // WF-6 · lane 5 car 5 · SPREAD lane — two presets, O-12`; `SIM_LIT` gains `believedDevotionEnabled: true, // SP-B's key — lit by WF-6 car 5 (the first lane on the devotion axis); conjunct beliefAxesEnabled lit by TR-5 7a` (CHECK-GIT-FIRST: POP-1 or another lane may have written it — write only what is absent); both register comments gain a LIT paragraph.
2. THE MOVERS, DECLARED BY NAME: (a) the faith crossing + the four executors in `full_simulation` (the only lit preset where the belief side exists AND spread is lit); (b) in `dramatic_campaign` the executors only (the crossing is belief-blind there — omniscient); (c) `believedDevotionEnabled` in all four — the `devotionBand` field on belief records in `living_realm` and `full_simulation` only (`beliefsActive`); INERT by construction in the two omniscient presets — the witness must show ZERO pulse-hash movement from this key there; (d) `pactFormationEnabled`'s pact lane was already lit by TR-5 7a — no movement attributed to it here; (e) WF-5b's and WF-2a's arms remain dark (declared).
3. `tests/property/faithTermsDormancyFence.test.js` → the LIT shape: the roster arm (VIRTUAL; REGISTERED; `true` in exactly the two presets where `SIM_LIT_FAITH_SPREAD` is spread == the presets where `isFaithSpreadEnabled` resolves true — computed, never a literal; absent from the other five); fence 1's dark world → `quiet_local.rules`; fence 3's lit twin under `full_simulation.rules` requires the self-side `devotionGroundTruth` count to MOVE on the bearing fixture and the `dramatic_campaign` twin to stay at ZERO crossings on the belief side (the fog-gate fact); the polarity census unchanged. `tests/property/believedWorldAxesDormancyFence.test.js`: the `believedDevotionEnabled` arms → LIT form (the roster off `SIMULATION_RULE_PRESETS`); the `believedConditionsEnabled` arms UNCHANGED (still dark — POP-1's); the three-family independence goldens (`:338-357`) must still hold (`devotionBand` moves only where devotion is lit; the host arms were re-chartered by TR-5 7a — VERIFY, do not re-edit). `tests/property/pactFormationDormancyFence` — VERIFY ONLY (TR-5 7a re-chartered it; the five-member set unchanged — `simulationRules.js` is already a member).
4. `subsystemRowsFaith.js` the WF-6 row's `other` amended LIT (two presets, the one-preset drafting fact); `subsystemRowsBelief.js:285` the devotion row gains one sentence "LIT at §932 by WF-6 car 5 (the first lane on the axis)" — text only; the SPINE family informed by the chair at collection.
5. THE WITNESS by hand (`measureWitness()`): `rulesSha256` + `litFlagCount` move on the four lit rows (two keys in `dramatic_campaign`/`full_simulation`, one in `realistic_regional`/`living_realm`); pulse hashes UNMOVED on every row — the born worlds are deity-free (`isSubsystemActive` returns before the fold; `devotionGroundTruth` yields `null` for a subject with no religion state — "NO KEY"); a moved pulse hash on ANY row is a STOP; a moved DARK row is a STOP.
6. The generation classified diff 525/525 unchanged; the espionage fence 21/21 (ES-0's three-door conjunction is untouched — `beliefAxesEnabled` was TR-5 7a's); `prose-manifest-cells.mjs --out` base/tip zero rows (the herald variant landed in car 3 with its own stamp).
7. The covering-array figures re-measured (`governed`/`ungoverned` may move on the preset declarations); BOTH `contributionLedgerShape` literals re-read off the composed HEAD (car 1 paid them at its base; a lane composed between moves them — re-derive the note); the edge bundles rebuilt.
8. THE SHIFT-LEDGER DRAFT (dock, `docs/implementation/GOLDEN_SHIFT_LEDGER-WF-6.draft.md`): *"`faithTermsEnabled` lit on `dramatic_campaign`, `full_simulation` (the SPREAD-lane roster — `faithSpreadEnabled` resolves true there only; O-12 forbids the other two) and `believedDevotionEnabled` lit on the four lit presets at landing <sha>; `rulesSha256`/`litFlagCount` moved on those rows; every pulse hash UNMOVED (deity-free born worlds); a faith pact DRAFTS in `full_simulation` only (belief maps need `infoMode !== 'omniscient'`); two lens rungs (`shared_rite`, `missionary_access`) remain unattainable until the owner re-bands (the walker); the generation dump 525/525; espionage 21/21; closure +<n> B; no installed campaign re-labelled; no tuning value moved."*

**Files (≈10 handwritten):** `simulationRules.js` (two lines + two comments), `faithTermsDormancyFence.test.js`, `believedWorldAxesDormancyFence.test.js`, the witness golden, `subsystemRowsFaith.js` (text), `subsystemRowsBelief.js` (text), `coveringArrayCoverage.test.js` (if moved), `contributionLedgerShape.test.js` (if HEAD moved), the shift draft, the packet; regenerated: the four edge files. If > 12: 5b = the census bills + literals. **Census bills:** refreeze own commit; manifest `kind:'rationale'` for the re-charters (or the LIT twin as a `mutation` plant); no prose byte. **Byte rule:** closure +2 lines (≈60–120 B PLAUSIBLE) quoted; worker unchanged; engine ±0. **Gate:** full list + the witness, the stability test, the two freshness tests, the engine-gated walker (directions 1–3; `ENGINE_GATED_DORMANT_RULE_KEYS` LACKS both keys — measured), the totality walker, `tradeConvergenceContract.walker` (D0's arm + lane 16a's amendment green with `faithTermsEnabled` in the two-preset cohort), `faithSpreadCohortFence` (lane 16a's — `faithTermsEnabled` is on its allowlist and now in the cohort; arm 3 must still hold), the espionage fence 21/21, the soak suites alone at load < 8; `evaluateTradeFlagLighting` `ok: true` ×7 QUOTED (this lane lights no TRADE key, but `pactFormationEnabled` is a TRADE precondition — the evaluator must still be green at the composed tip). **LIT act:** the two keys. **Exit:** both keys lit per roster; both fences LIT-shape green with the `full_simulation` twin moving and the `dramatic_campaign` twin zero on the belief side; the witness diff quoted; the shift draft; zero UNDECLARED lines; no red this lane did not cause.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

Law 2's "the flag-mint car is LAST" means car 5 (the LIGHTING). The DARK MINT of `faithTermsEnabled` is car 1 because the by-name read is the crossing repair's own door and `scanGateReads` reads every `src/` file regardless of callers (`engineGatedRuleKeys.walker.test.js:538-541`; `unaccountedReads` `:501-502`); the estate's one-commit law drags the row, the three test edits, both literals and the covering-array census into car 1 with the register entry (`manifestWithoutRow` `:518`; `VIRTUAL_PENDING_RULE_KEYS` pinned `[]` `:647`; the ordered `VIRTUAL_RULES` `:646`; the two lengths `:118-119`) — recorded as the forced departure from R3-as-first-written, exactly as every TR brief records it. `believedDevotionEnabled` is ALREADY registered (`:251`) and rowed (`subsystemRowsBelief.js:285`) — its lighting moves NO literal. **At car 1**, before editing: `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length`; both literals = that + 1 — 35 → 36 if this is the first dark mint on the consist, higher if TR lanes 1/2/7 minted first; NEVER inherited from this brief. **At car 5** (rebased at landing after TR-5 7a and lane 16a) the same two commands are RE-RUN and the literals, the census figures, the `subsystemRowsFaith.js` tail (lane 16b may have appended `faithStanceConsequencesEnabled` — CHECK-GIT-FIRST; the FIRST WF mint to compose creates the leaf, the second appends) and the two cohorts are re-verified against the composed tree; conflicts on the register tail, the cohorts, the literals and the census are EXPECTED and resolved by MEASUREMENT (PROTO §5.2), never OURS/THEIRS. The lane's report states the numbers it wrote at car 1, re-read at car 5, and expects at the consist.

## THE VOLUME ROWS THIS LANE CHANGES (ADDENDUM R6 — the chair writes the dated LANDING BLOCK into `docs/DESIGN_FP_ARCH_WF.md` on the ledger from this list)

`§4 WF-6` → "LANDED-LIT (§932 lane 5; `faithTermsEnabled` on `dramatic_campaign`/`full_simulation`; `believedDevotionEnabled` on the four)" with the cars by sha; the substrate rows it corrects: the `:217` dead read (GR-2's crossing was LANDED-INERT until car 1 — the premise "the peacetime producer is landed" was true and could not fire), the two unattainable rungs (the walker), the war door as design row D1, the one-preset drafting fact, `temple_restitution` = conserved grain; `§2` the flag row `faithTermsEnabled` → LANDED-LIT (two-preset roster); `§1b V32` unchanged; `§5 seam 1` → "WF-6's half DISCHARGED (call-time family derivation pinned)"; `§6 Q3` → "COMPOSED (the bundle fixture)"; the history fold's WF-6 row folded. Also for GRAMMAR's volume (the chair's): `DESIGN_FP_ARCH_GR.md` GR-2's row gains "the self-side devotion read was a dead pointer from `a18fdcfae` to §932 lane 5 car 1".

## OWNER-GATED, KEPT OUT

- THE RE-BAND of `PACT_DRAFT_LENS.faith_communion` (and `COMMUNION_RUNG_FLOOR`/`CROSSING_FLOOR01`) — a TUNING act under the six-bounds law; the walker lands FIRED; the owner signs at the soak.
- THE WAR DOOR (a faith `CLASS_TERM` asset class) — a design row (D1), a GRAMMAR/WAR appraisal seam; not built here.
- Any `peaceTerms.js` byte; any `beliefMap.js` byte (781/800, net-zero law); any `pulseKernel.js` byte; a ceiling raise; push/deploy/migration/deletion/spend; the golden master; the LIT SUCCESSOR preset id.
- Persisted shapes widened: NONE (terms ride the treaties artifact; `couplingRegistryFaith.js` and `subsystemRowsFaith.js` are certification leaves) — recorded.
- The `:217` repair's HOME (GRAMMAR's file) — the vetoable seam: if the chair re-homes it, car 1's `pactFormation.js` lines become a GR car and this lane's car 1 keeps only the door + the mint; the count returns to 5–8.

## RISKS

**Grep traps (HISTORY-WF's paragraph after §B + RECHECK §5 + HISTORY-TRADE's twelve, VERBATIM by reference; the ones that bite here):** `religionState` (singular) is a DEAD POINTER — the live spellings are `worldState.religionStates[cid]` and `settlement.config.faithProfile`; a populated `PACT_DRAFT_LENS` rung is not a reachable rung — compute the attainable set; `restitution` mostly matches WR's `restitutionClaimBand` — the term is `peaceTermsCatalog.js:168/:245`; `faith_pact_formed`/`pactOutcome` in `deityStanceLane.js` is a NEWS-ONLY stance record, not GR-2's pact; `pilgrim` is TRADITIONS + the term + the reserved errand row; `covert` is espionage vocabulary; `contributionLedgerShape.test.js`'s title says "at 28", the literals say 35; `catalogGrewSinceWr10` — ALREADY FIRED, verify never re-run; `toll_exemption`'s one consumer reads treaty terms (HISTORY-TRADE); W-COIN is state money — `temple_restitution` moves grain.

**Collision surfaces:** `peaceTerms*.js`/`pactFormation.js`/`pactTriggers.js` (TR-5 → THIS LANE → WF-3 (16b) → POP-5b → GR-4/GR-5; the T9 STOP on any second lane in flight; CHECK-GIT-FIRST for GR-5's renewal trigger and TR-5 car 3's `crossingsFor` seam line at `:260` — the `selfBandsOf` widening must merge with it by MEASUREMENT); `believedWorldAxesDormancyFence.test.js` (TR-5 7a → THIS LANE → POP-1 — SERIAL by landing order; each re-charters ITS arm); `simulationRules.js` and the flag-mint surfaces (every #32 lane); `subsystemRowsFaith.js` (created by whichever WF mint composes first — lane 16b is the other candidate); the WF chain (`religiousContest.js` — ONE WF lane in flight); `sovereigntyBundleWr10.test.js` (TR-5 car 1 re-points its set; this lane ADDS an arm, drops no anchor).

**The addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R1 (the cohort home; the two-preset faith cohort is lane 16a car 0's vetoable extension under a recorded reason; the fallback if the chair reads PROTO §1.1 strictly is that `faithTermsEnabled` lands DARK with its lighting priced to the successor-id act while `believedDevotionEnabled` still lights in four); R2 (this lane's LIT car lands after door D0, lane 16a and TR-5 7a — "`casusCommercii` before `tradePacts`" is TR's; WF's own order is the plan §1's: 16a → 5 → 16b → …); R3 (the dark mint at car 1; the lighting re-reads); R5 (5a/5b if > 12).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls — a gate line with no PRINTED count DID NOT RUN; dist-gated size suites skip without `dist/`; `deno.lock` re-freeze before `--frozen`; `git grep` without a rev reads the INDEX; 120 faith suites in one vitest = exit 137 (W-FAITH's resume caution — run the faith property suites in two halves); a moved DRIVEN GOLDEN is a STOP.

**Design hazards:** THE SIXTH-NAMER TRAP (the five-file `pactFormationEnabled` set — the row's string fields, the coupling row's `flags`, the leaf's code); the repair MUST sit behind `faithTermsActive` or GR-2's fence 1 dark identity breaks and faith terms draft in `living_realm` where the SPREAD lane is dark (the volume's invalid config); `selfBandsOf` takes ONE argument today — widen it, never reach for `worldState` inside it (the K3 law: the self side is knowledge, never belief); `peaceTerms.js` takes ZERO bytes; the executors are READERS (`grant`) not a second evaluator; `missionary_access` moves the CHANNEL never `share`; the herald sentence is a pool VARIANT on an existing kind (a NEW kind would owe its own walker file and an exact `heraldRouting` row — the `faith_` prefix trap); `TERM_FAMILIES` must NOT grow (`faith` exists).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (NONE — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title (cars 0, 1, 2, 3, 5); the mutation manifest per new invariant file; the wiring census stamp at car 3 (the herald variant — a prose byte); prose-numerics at car 3; the edge bundles at cars 1 and 5; the Seat/Lane trailer `Seat: Opus 5 — Fable-unvalidated` · `Lane: 5`; porcelain 0. At car 1 (the dark mint) additionally the engine-gated walker (directions 1–3), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two freshness tests, the totality walker, `pactFormationDormancyFence` (fence 1 dark identity; the five-member set), `grep -rlw pactFormationEnabled src | wc -l` = 5. At car 5 (built on the consist after D0, lane 16a and TR-5 7a — ADDENDUM R2(iii); never gated in the dock) additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, `tradeConvergenceContract.walker` (D0's arm + 16a's amendment), `faithSpreadCohortFence`, the espionage fence 21/21, `prose-manifest-cells.mjs --out` base/tip, the generation classified diff 525/525 unchanged, the soak suites alone at load < 8, `evaluateTradeFlagLighting` `ok: true` ×7 quoted.
