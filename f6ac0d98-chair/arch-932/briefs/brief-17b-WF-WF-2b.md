# THE LANE — PRICING §C lane 17, sitting 17b · WF-2b LEGATES + PILGRIMS, THE MOVERS (no new key; the movers arm of `pilgrimageEnabled`'s door; the two reserved `ERRAND_CONSUMERS` rows flip `built: true`)

Architect: the Fable seat (family WF), 2026-09-16, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED = opened at the cited line by me or by a recon skeptic; PLAUSIBLE = reasoning. Line numbers rot; re-measure by SYMBOL. Protocols `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n) via `…/arch-932/ARCHITECTS-CARD.md`; `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` WINS. Family plan `…/arch-932/plans/plan-WF.md` §1 row 17b, §2, §4, §5 "17b"; where the code contradicts it this brief says so — and it does so in the ONE place that shapes the whole sitting (THE MOVERS' LEDGER, below).

**Cars: 5 (1, 2, 3, 4, 5) + 1 repair allowance = 5–6.** PRICING §B WF-2 7–10 C across both slices (17a took 4); the plan seated 17b at 4 + 1; this brief adds the KINDS CAR (car 4 — seven mover kinds, the annex §E `:320-411`, by the same "each wave registers its own kinds" law brief 17a records) and DROPS the plan's separate interception car by folding the road counterforce into car 3 — net 5.

**Prerequisites (anchors):** sitting 17a LANDED on the consist (`pilgrimageEnabled` lit in two presets; the season's feast/flood bands exist to dispatch from; the FAITH registry at eight rows; `faithPilgrimSeason.js`); lane B's W-OPS wiring cars and IN-4 (lane 6) on `envoyErrandVocabulary.js` per ARCH §7.1's row (lane B → IN-4 → **WF-2b** → TR-8 → ES-6b/ES-7) — CHECK-GIT-FIRST: if IN-4 has not boarded when this lane cuts, the chair re-orders (the row is one `built:true` flip per lane; the collision is a one-word merge); SP-D landed (`errandMint.js`, `mintErrandSpine` `:257-300`, CONFIRMED); WR-7b's `foreignGuestHold.js` landed (424 eff — READ ONLY, and per THE MOVERS' LEDGER below, UNCONSUMED). The WF chain: ONE lane in flight (plan §4).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** this sitting mints NO key. Its door is `pilgrimMoversActive(rules) = pilgrimSeasonActive(rules) && rules?.errandSpineEnabled === true` — the movers need the SP-D spine lit. `errandSpineEnabled` is a DORMANT register member at the consist (`simulationRules.js:271`, no preset; CONFIRMED) and PROTO §3.1A makes "the first lane lighting any errand consumer (TR-8, WF-2, IN-4, ES-7, lane B's W-OPS car B)" the re-charterer of `tests/property/errandSpineDormancyFence.test.js`. RULING (vetoable): THIS LANE DOES NOT LIGHT `errandSpineEnabled`. Reason, measured: lighting the spine moves GRAMMAR's pact-proposal transport (`pactProposals.js:253 pactTransportFor` returns `'envoy'` instead of `'abstract'` the moment `mintErrandSpine` answers `'spine'`) and the INT emigre seam in every lit preset — a cross-family shift this faith lane has no standing to declare; TR-8 car 6 already prices it (`brief-56b-TR-TR-8.md` car 6: "if ABSENT, this car lights it … a substantive GR/WR/INT movement this car DECLARES by name"), and lane B / IN-4 board before both. So: NEEDS lit first — `pilgrimageEnabled` (17a) and, for the movers to MOVE, `errandSpineEnabled` by whichever errand consumer lights it first (lane B car B / IN-4 / TR-8 car 6). WHAT IT UNBLOCKS: TR-8 boards after on `envoyErrandVocabulary.js`; WF-8's Great Pilgrimage arc (lane 21b) reads the movers' receipts; ES-6b/ES-7 follow on the vocabulary file. THE DECLARED CONSEQUENCE: at this sitting's landing the movers arm is DORMANT-BY-CONJUNCT in every preset; the day the spine lights (another lane's car), the legates and pilgrims WAKE in `dramatic_campaign` and `full_simulation` on deity-bearing worlds — that lane's shift ledger MUST name WF-2b among the consumers it wakes (the chair relays; this brief's car 5 writes the sentence that lane copies).

**⛔ THE MOVERS' LEDGER — WHERE THE CODE CONTRADICTS THE VOLUME, AND WHAT THIS BRIEF DOES ABOUT IT (CONFIRMED at source):** the volume says legate/pilgrim errands ride "SP-1's ledger under SP-1's lifecycle" (`DESIGN_FP_ARCH_WF.md` §3 "Everything else …", §5 seam 3 "the ledger name is resolved: `worldState.envoyErrands` (CR-FP-10)"). At the consist the ONLY writer of `worldState.envoyErrands` is the war head `envoyErrand.js` (`mintEnvoyErrand` `:156`; `tests/lint/envoyErrandLedgerSingleWriter.walker.test.js` forbids a second), and the ONLY mover of its rows is `advanceEnvoyErrands` `:777` — BOTH gated by `envoyDiplomacyActive` (`envoyErrandOffer.js:38-44` = `ENVOY_REQUIRED_RULES.every(=== true)` over `warLayerEnabled · warTerminationEnabled · peaceEngineEnabled · envoyDiplomacyEnabled · npcConsequencesEnabled · routeLifecycleEnabled`, `envoyErrandVocabulary.js:33-40`), and `envoyDiplomacyEnabled` is declared ONLY as `false` in `full_simulation` (`simulationRules.js:920`), `npcConsequencesEnabled: false` `:962`, `routeLifecycleEnabled: false` `:976` — so the envoy layer is DARK IN EVERY PRESET and a legate row in `envoyErrands` would never be created (`mintEnvoyErrand` returns `reason: 'dark'` `:176-177`) nor advanced. The plan's "interception rides WR-7b's `foreignGuestHold.js` (read-only + close-through-the-writer)" inherits the same wall: a hold keys on an envoyErrands row (`foreignGuestHoldForErrand(worldState, errandId)` `:404`, `foreignGuestHoldForNpc` `:398`; the hold's `positionRef`/`journeyLegs` are the row's own `:280-306`). THE ESTATE'S OWN PRECEDENT for a NON-WAR spine consumer is GR-2: `pactTransportFor` (`pactProposals.js:250-271`) hands `mintErrandSpine` an INJECTED plan on a dwell the geometry already priced and keeps the RESULT on ITS OWN record (the proposal row carries the transport word and the arrival tick) — "there is no separate `pactErrand.js` and there must not be one" (`envoyErrandVocabulary.js:303-307`); INT-3b's `emigreErrandFor` (`emigreErrand.js`, 21 eff) does the same and is the registry's first built consumer "that NOTHING CALLS". RULING (vetoable, recorded for R6 as a code-forced deviation): the WF-2b movers are RECEIPT-PARTIES — a legate or a pilgrim party is a pulse OUTCOME minted by the faith fold through the WF leaf, carrying the spine's class fields (`purposeClass 'religious'` / `'personal'`, `purpose`, the plan) from `mintErrandSpine`, an `arrivalTick` priced on the same dwell read the pact lane uses, and its ARRIVAL is applied by the fold on a later tick by reading the party's dispatch receipt back off `pulseHistory` (the `footholdCooldownKeys(worldState, tick)` idiom at `deityStanceLane.js:162-172` — the faith fold already reads its own prior outcomes this way). No row in `envoyErrands` (the single-writer law holds); no NPC state write ("per-head pilgrims are NOT modelled — a mover is a party"; the never-resolve walker); no WR-7b hold (there is no row to hold — the volume's "interception rides foreignGuestHold" is REFUSED on measurement, and the degraded harassment/turn-back arm the volume ALREADY declares for `envoyDiplomacyEnabled`-dark is the ONE LIVE ARM everywhere). The ARCH §7.1 row "`foreignGuestHold.js`: WF-2b ↔ TR-8" is STRUCK for WF-2b (TR-8's factors may still use it — its own brief's business; the chair relays). If the chair re-homes the movers to `envoyErrands` instead, this sitting is BLOCKED until the envoy stack lights (a WAR act on five dark keys) and its cars 1–3 become that act's — the work exists either way.

**Shared files this lane declares (PROTO §7.3 rule 1), eff/ceiling at the cut:** `src/domain/worldPulse/envoyErrandVocabulary.js` 367/800 (TWO one-word flips `built: false → true` at the legates row `:322-327` and the pilgrims row `:328-333` — each in the SAME commit as its module; SERIAL lane B → IN-4 → THIS → TR-8) · `tests/lint/errandConsumerRegistry.walker.test.js` 270 (the EXACT `builtModules`/`minters` lists `:421-430` gain the two paths — the "FOUR built consumers" title `:392` re-worded to the measured count; direction 2 `:368-378` reds a `built:true` row whose module does not call `mintErrandSpine` and vice versa) · `src/domain/worldPulse/legateErrand.js` (NEW — the PATH IS PINNED by the registry row; ≤ 120 eff) · `src/domain/worldPulse/pilgrimErrand.js` (NEW — pinned path; ≤ 120 eff) · `tests/lint/couplingInclusion.walker.test.js` (TWO exact-path rows in the FAITH list `:186-207` — `legateErrand.js`/`pilgrimErrand.js` match no FAITH prefix and their paths are pinned by another leaf, so the `patronFall.js` precedent applies verbatim: "AN EXACT-PATH REGEX, NOT A PREFIX") · `src/domain/worldPulse/religiousContest.js` 493/800 (≤ 10 lines: the dispatch call and the arrival read, both behind the door, inside the spread lane) · `src/domain/worldPulse/faithPilgrimSeason.js` (17a's leaf — the `pilgrimMoversActive` door lives beside `pilgrimSeasonActive`, ≤ 4 lines) · `src/domain/worldPulse/faithNews.js` (seven registry rows — under the REGISTRY GROWTH RULE, brief 17a car 3) · `settlementRumors.js` 555 · `heraldRouting.js` 273 · `kindRegistryRoster.js` · `kindPoolFloors.walker` · `impactKindWalkers` · `tests/property/faithPilgrimageDormancyFence.test.js` (17a's — gains the movers arms) · `couplingRegistryFaith.js` (append). NOT touched: `errandMint.js` (the mint head — called, never edited), `envoyErrand.js` 714 (the war head — never), `foreignGuestHold.js` (UNCONSUMED — the ruling above), `namedPersonTransit.js`/`envoyErrandTransit.js` (the leaves price NO leg — see THE DWELL below), `pulseKernel.js`, `simulationRules.js` (NO register byte this sitting — the door is a conjunct on an existing key + 17a's key; ⚠ therefore the four edge bundles are NOT stale here). Worker bundle: 0 B.

**THE DWELL (how a party's arrival is priced without becoming a MOVEMENT SITE):** `tests/lint/namedPersonTransitTotality.walker.test.js` discovers every file that calls `openNamedPersonLeg|namedPersonLegTicks|namedPersonLegPosition|namedPersonPathPosition|namedPersonArrivalTick|advanceWanderer|…` or imports `namedPersonTransit.js`, and every discovered site must be in its closed `MOVEMENT_SITES` manifest (`errandMint.js` is there as `injected` via `normalizeRoutePlan`). The pact lane prices its dwell WITHOUT any of those tokens (VERIFY-AT-BUILD the exact helper `pactTriggers.js`/`pactFormation.js` hand `pactTransportFor` as `arrivalTick` — the digest's `pathCost`-derived dwell); the WF leaves copy THAT read (`pathCost` + the same tick-per-cost law the pact lane uses — import the pact lane's exported dwell helper if one exists, else re-derive with the identical constants and pin equality in-test) so they are NOT movement sites and need NO manifest row; `mintErrandSpine` validates the injected plan (`normalizeRoutePlan`) exactly as it does for pacts. If the ONLY lawful pricer at the cut is `namedPersonArrivalTick`, the leaf takes a `route: 'direct'` manifest row in the SAME commit (the `ransomClaim.js` shape) — VERIFY-AT-BUILD and report which road was taken.

---

## THE STATE OF THE RECORD

- **The history pass HAS run for WF** and WINS over PRICING §B. Its WF-2 row: "SP-1's errand spine is built and both WF-2b rows are reserved IN CODE by exact path and type with `built: false` (`envoyErrandVocabulary.js:322-335`; §78.2 F-1 discharged the precondition)" — CONFIRMED (re-read by me: `legates`/`religious`/`legateErrand.js`/`WF-2b`/`false` at `:322-327`; `pilgrims`/`personal`/`pilgrimErrand.js`/`WF-2b`/`false` at `:328-333`). The fold's grep traps adopted VERBATIM: `pilgrim` three senses; `covert` is espionage vocabulary; `oracle` is the dormancy oracle + the `'oracle'` clergy label (`npcAgency.js:44`); `roaming` is the roaming-stressor twin; `couriers` rows are IN-4/ES-1's. ⚠ THE FOLD DID NOT OPEN THE ENVOY LAYER'S GATE — the finding above (the envoy stack dark in every preset) is THIS BRIEF's, made by reading `envoyDiplomacyActive` and the preset declarations; the chair should carry it to the WAR/WY families (WR-7a embassies, WR-7b holds and TR-8's factors all sit behind the same six-key conjunction).
- **The volume** `docs/DESIGN_FP_ARCH_WF.md` §4 WF-2 (`:442-495`): the HARD PRECONDITION DISCHARGED clause (`legates → legateErrand.js` purposeClass `religious`; `pilgrims → pilgrimErrand.js` purposeClass **`personal`** — NEVER `religious`; "each module lands WITH its row's `built:true` flip in the SAME commit"; `ENVOY_PURPOSES` stays closed at `['sue','self_parlay']` — "the class layer (`ENVOY_PURPOSE_CLASSES:130`, `PURPOSE_CLASS_BY_PURPOSE:147`) is the lawful join"); WF-2b deltas ("interception rides WR-7b's foreignGuestHold.js; `envoyDiplomacyEnabled` dark ⇒ the declared harassment/turn-back degraded arm (receipted, no hold). Never-resolve absolute") — the first half REFUSED on measurement (THE MOVERS' LEDGER), the second half IS the live arm; pins ("war-on-the-road counterforce"); Req 14 ("named movers' verbs are SP-1's own (DM KILL/ASSIGN semantics ride SP-1's registered verbs)" — with receipt-parties there is no NPC to KILL/ASSIGN: Req 14 is RECORDED as "no state ⇒ no verb", the season's own answer). §5 seam 3 SP-1 DISCHARGED ("the registry row, not a probe, is the seam"); seam 6 WR-7b ("foreignGuestHold.js is the ONE writer; WF-2b consumes read-only") — STRUCK for WF-2b by the ruling above (R6 row). §3 the JSON-alias trap ("shepherdRef/clergy are `npcs[]`") — a legate's `{npc}` slot names a minister by DISPLAY NAME read off the roster, never a ref persisted.
- **The compiled programme** `docs/DESIGN_FP_ARCHITECTURE.md` §5 #34 (`:1949-1953`): "2b THE MOVERS: legates/pilgrims mint through SP-D's writer with purpose `religious` (the red-until-built existence probe from the WF side is GREEN by order — war's closed ENVOY_PURPOSES was never widened); interception rides WR-7b's foreignGuestHold; envoyDiplomacyEnabled dark = the declared harassment/turn-back degraded arm. Never-resolve absolute (no named fate)." — ⚠ "with purpose `religious`" is loose: the PILGRIM row's class is `personal` (the volume and the code agree; the programme's sentence is a RECORD≠CODE row for R6).
- **The annex** `docs/content/RECEIPT_POOLS_FAITH.md` §E (`:320-411`): `faith.legate.dispatched` (`:320`, routine) · `faith.legate.arrived` (`:334`, notable) · `faith.legate.withheld` (`:345`, posture receipt, routine) · `faith.legate.returned` (`:359`, routine) · `faith.pilgrim.named_departs` (`:373`, routine) · `faith.pilgrim.intercepted` (`:387`, notable) · `faith.pilgrim.turned_back` (`:398`, "the WR-7b-dark degraded arm", routine) — seven Herald `faith` kinds. CANONICAL SPELLINGS RULED HERE: `faith_legate_dispatched`, `faith_legate_arrived`, `faith_legate_withheld`, `faith_legate_returned`, `faith_pilgrim_named_departs`, `faith_pilgrim_intercepted`, `faith_pilgrim_turned_back`. ⚠ `faith.pilgrim.intercepted` presumes a HOLD; under the ruling the "interception" is the road counterforce's HARASSMENT arm (the party is delayed/thinned, not held) — the pool's variants are authored generally enough (VERIFY-AT-BUILD by reading `:387-397`); if a variant names custody, it is an INELIGIBLE variant (the WF-8A eligibility discipline), never a hold minted to satisfy prose.
- **The ODQ anchors:** §78.2 F-1 (`:2987`+) WF-2b on the LANDED errand spine; §308 (trains cap at EIGHT); CR-FP-10 (the spine's ledger keep-in-place ruling — cited by the volume; the ruling stands for the WAR head and is not contradicted: this lane WRITES NO ledger).
- **The owner's laws:** everything built lands LIT — this sitting's behaviour is DORMANT-BY-CONJUNCT at landing (declared; the false-claim shape avoided by saying so — ARCHITECTS-CARD §1 last bullet: "a lit key that would be a FALSE CLAIM lands DARK with the lighting priced to the producer car"); nothing pushes/deploys/applies/deletes/spends; tuning raw + derivation.

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/envoyErrandVocabulary.js` | 367/800 | `ENVOY_REQUIRED_RULES` `:33-40` (six keys); `ENVOY_PURPOSE_CLASSES` `:130-137` (`commercial, covert, diplomatic, factional, personal, religious`); `PURPOSE_CLASS_BY_PURPOSE` `:147-150` (`self_parlay`/`sue` → `diplomatic` — TOTAL over `ENVOY_PURPOSES` by pin; a NEW PURPOSE WORD (`legation`, `pilgrimage`) is NOT a war purpose and does NOT join `ENVOY_PURPOSES` — the class is written explicitly on the mint call, the `errandSpineFields` road `:117-121`: "`purposeClass` — written ONLY when it differs from what the mapping row already"…); `ERRAND_CONSUMERS` rows `:295-360` |
| `src/domain/worldPulse/errandMint.js` | 87/800 | `errandSpineActive(worldState)` `:74`; `errandSpineFields({ purpose, purposeClass, declaredPurpose, truePurpose, covert })` `:141`; `mintErrandSpine({ worldState, purpose, purposeClass, …, routePlan, fromId, toId, journey, notBeforeTick })` `:257-300` → `{ ok, fields, plan, reason: 'invalid_route_plan' | 'dark' | <covert refusal> | 'invalid_purpose_class' | 'spine' }` — DARK returns `{ ok: true, fields: {}, plan, reason: 'dark' }` (the byte-identity arm) |
| `src/domain/worldPulse/pactProposals.js` | 161/800 | `pactTransportFor({ worldState, fromId, toId, tick, arrivalTick })` `:250-271` — THE TEMPLATE: an injected one-leg plan on a priced dwell; reads the mint's `reason`, never the field block ("SP-D's one-reader law forbids reaching for it outside the errand family") |
| `src/domain/worldPulse/emigreErrand.js` | 21/800 | `emigreErrandFor({ worldState, fromId, toId, departTick, arrivalTick })` → `{ ok, lit: reason === 'spine', reason, plan }` — the second template (a built consumer nothing calls) |
| `src/domain/worldPulse/envoyErrand.js` | 714/800 | `mintEnvoyErrand` `:156` gated `envoyDiplomacyActive` `:176`; `advanceEnvoyErrands` `:777` gated `:784` — DARK in every preset (THE MOVERS' LEDGER) |
| `src/domain/worldPulse/foreignGuestHold.js` | 424/800 | `FOREIGN_GUEST_HOLD_CAUSES` `:37-43`; `openForeignGuestHold({ worldState, hold })` `:414` — keyed on an errand row; UNCONSUMED by this lane (ruling) |
| `src/domain/worldPulse/religiousContest.js` | 493/800 | the spread lane `:598-`; the targeted-foothold block `:961-996` (`targetedFootholds(settlement, patronForFoothold, rivals)` `:972`; `footholdOutcome` `:980`) — the LEGATE's arrival effect ADMITS the legate's creed to the foothold candidates for the host (the `missionary_access` CHANNEL lane 5 built — REUSE `missionaryChannelFor`'s admission shape, never a second channel); `footholdCooldownKeys(worldState, tick)` (`deityStanceLane.js:162-172`) — THE PULSE-HISTORY READ IDIOM this lane copies for arrivals |
| `src/domain/worldPulse/faithPilgrimSeason.js` | 17a's (≤ 250) | `pilgrimSeasonActive(rules)`; `pilgrimSeasonFor(...)` → `{ band, scalar, reasons }` — the feast/flood band is the PILGRIM PARTY's dispatch trigger |
| `src/domain/worldPulse/faithTermExecutors.js` | lane 5's | `missionaryChannelFor(worldState, grantorId, granteeId, tick)` (car 2) — the channel a LEGATE opens for a tick-window on arrival (the legate is the channel's MOVER: `missionary_access` grants the road; the legate walks it) |
| `tests/lint/errandConsumerRegistry.walker.test.js` | 270 | direction 1 `:355-360` (a module that mints with no row); direction 2 `:368-378` (`built` vs `isMinter` both ways); the partition non-vacuity `:383-388`; the EXACT lists `:421-430` (`emigreErrand.js`, `envoyErrand.js`, `espionage/espionageMissions.js`, `pactProposals.js`) — this lane's two paths join BOTH lists (codepoint order: `legateErrand.js` after `espionage/…`, `pilgrimErrand.js` after `pactProposals.js`) |
| `tests/property/errandSpineDormancyFence.test.js` | — | "the flag is read by name, only in strict form, at exactly the two known doors" `:230` — the WF leaves read `errandSpineEnabled` NEVER by name (they call `mintErrandSpine`; the door `pilgrimMoversActive` reads it by name ONCE in `faithPilgrimSeason.js` — ⚠ that IS a third by-name site: the fence's site census `:230-260` must gain `faithPilgrimSeason.js` in the SAME commit as the door (car 1), with the fence's own instruction followed; alternatively the door reads `errandSpineActive(worldState)` (the head's own gate, `errandMint.js:74`) and adds NO by-name site — RULING: read through `errandSpineActive` (one reader, the head's), so the fence census is UNTOUCHED and the polarity stays the head's) |
| `tests/lint/couplingInclusion.walker.test.js` | — | the FAITH list `:186-207`; exact-path precedent `:207` |
| `src/domain/spatial/distanceRead.js` | 468 | `pathCost`, `isMapped`, `distanceWeight` — the dwell reads |
| ABSENT | — | `legateErrand.js`, `pilgrimErrand.js`, `faithMoverReceiptPools.js`, `faithMoverKindPools.walker.test.js` |

**Budgets:** worker 1,404,493 EXACT untouched; engine ±0 (pulse-side leaves); first-paint: SEVEN `WHAT_PHRASES` strings (≈ 250–350 B EAGER, PLAUSIBLE — `settlementRumors.js` reaches first paint) against the UNMEASURED margin — quote the table at car 4 and STOP if the closure is exceeded; NO `simulationRules.js` byte this sitting.

## ALREADY DONE (do not re-land)

SP-D's spine (`errandMint.js`, `errandSpineFields`, `mintErrandSpine`), the class layer, the two reserved rows · GR-2's `pactTransportFor` and INT-3b's `emigreErrandFor` (the two consumer templates) · WR-7a's transit family and WR-7b's holds (built; NOT consumed here) · 17a's season, door, kinds and registry growth · lane 5's `missionaryChannelFor` (the channel a legate walks) · the seven mover pools AUTHORED in the annex (2026-08-02) — transcribed, never authored.

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned files; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: 17b`; PACKET_STANDARD caps; the packet `docs/implementation/packets/fp/WF-2B.md` in the dock.

### Car 1 — `legateErrand.js`: the legate party minted through SP-D's head with the `built: true` flip in the same commit; the dispatch trigger in the faith fold; the arrival read off `pulseHistory`; the movers door

**In plain words:** a court whose creed holds a treaty right of missionary access, or whose god's followers are on cooperative terms with a neighbour's, sends a legation — a party of ministers — down the road. This car builds the module that says when a legation sets out, how long the road takes, and what it does when it arrives (its creed gains a foothold opening at the destination for a while); the estate's registry of "who travels on the shared errand spine" is told the legates are now real, in the same commit, because its test refuses either half alone.

**The change.** NEW `src/domain/worldPulse/legateErrand.js` (≤ 120 eff; the path is PINNED by `ERRAND_CONSUMERS:325`): `LEGATE_ERRAND_TUNING = Object.freeze({ LEGATION_COOLDOWN_TICKS: <raw>, ARRIVAL_CHANNEL_TICKS: <raw> })` (THE WF TUNING-HOME LAW — each RAW with an executed derivation: the cooldown derived from `STANCE_LANE_TUNING.FOOTHOLD_COOLDOWN_TICKS 6` (`deityStanceLane.js:59`) so a legation never re-announces inside the foothold's own metronome; the channel window derived from the same); `legateErrandFor({ worldState, fromId, toId, tick, arrivalTick })` → `{ ok, lit: reason === 'spine', reason, plan, fields }` — `mintErrandSpine({ worldState, purpose: 'legation', purposeClass: 'religious', fromId, toId, journey: 'outbound', routePlan: { legs: [{ fromId, toId, departTick, arrivalTick, journey: 'outbound' }], expectedReturnTick: <arrival + the same dwell> } })` (the `emigreErrandFor` shape verbatim; `purposeClass` written explicitly because `legation` is not a war purpose and `PURPOSE_CLASS_BY_PURPOSE` carries no row for it — `errandSpineFields`' documented road); `legateReceipt({ kind, cid, toId, settlementName, counterpartName, creedName, tick, arrivalTick, fields })` — the RECEIPT-PARTY outcome (`type: 'faith_legation'`, `candidateType: 'faith_legate_dispatched'` AS A LITERAL, `ruleId: 'religious_legation'`, `ruleFamily: 'religion'`, `applyMode: 'auto'`, `probability: 1`, `curationClass` per the metronome's transition class, `targetSaveId: cid`, `affectedSettlementIds: [cid, toId]`, `legation: { toId, arrivalTick, purposeClass: fields.purposeClass ?? 'religious' }` — the spine fields ride the receipt, never a ledger); `legateArrivalsDue(worldState, tick)` — the PULSE-HISTORY READ (the `footholdCooldownKeys` idiom `deityStanceLane.js:162-172`, copied: scan `worldState.pulseHistory`'s recent outcomes for `ruleId === 'religious_legation'` with `legation.arrivalTick <= tick` and not yet consumed — "consumed" = an arrival receipt for the same `id` already in the history; VERIFY-AT-BUILD `pulseHistory`'s retention horizon covers the longest dwell — if not, the party is dropped with a `faith_legate_returned` receipt and the reason `road_too_long`, never a phantom arrival). THE DOOR: `faithPilgrimSeason.js` gains `export function pilgrimMoversActive(worldState)` = `pilgrimSeasonActive(worldState?.simulationRules) && errandSpineActive(worldState)` (the head's own gate — NO third by-name site; the fence census untouched). THE DISPATCH TRIGGER in `religiousContest.js` (≤ 6 lines inside the spread lane, behind `pilgrimMoversActive(worldState)`): for the settlement's patron creed, a counterparty court where `missionaryChannelFor(worldState, toId, cid, tick)` holds (the treaty road) OR the stance lane recorded `faith_pact_formed` between the two patrons within `PACT_COOLDOWN_TICKS` (`pactCooldownPairs`), not on `LEGATION_COOLDOWN_TICKS`, dispatches ONE legation: `outcomes.push(legateReceipt(...))`; THE ARRIVAL (≤ 4 lines beside the foothold block `:961`): for each due arrival, the legate's creed is ADMITTED to the destination's foothold candidates for `ARRIVAL_CHANNEL_TICKS` through the SAME admission `missionaryChannelFor` opens (lane 5's channel — one admission shape, two openers) and `faith_legate_arrived` is minted; a destination that has SUPPRESSED the legate's creed since dispatch yields `faith_legate_withheld` (the posture receipt) instead. THE FLIP: `envoyErrandVocabulary.js:326` `built: false` → `built: true` and `tests/lint/errandConsumerRegistry.walker.test.js:421-430` both exact lists gain `'src/domain/worldPulse/legateErrand.js'` in codepoint position; the test title `:392` re-worded ("the FIVE built consumers today … and the legates"); `tests/lint/couplingInclusion.walker.test.js` FAITH list gains `/^src\/domain\/worldPulse\/legateErrand\.js$/` with a comment in the `patronFall.js` idiom (the path is pinned by `ERRAND_CONSUMERS`, so a prefix is refused).

**Pins (`tests/domain/legateErrand.test.js`, NEW):** the mint is SPINE-shaped — `legateErrandFor` on a world with `errandSpineEnabled: true` returns `lit: true` and `fields.purposeClass` resolves `'religious'` (through `purposeClassOf`, never a hand read outside the family — the one-reader law); DARK spine ⇒ `lit: false, reason: 'dark'` and the fold DISPATCHES NOTHING (the movers door is closed — `pilgrimMoversActive` false); an invalid plan (`arrivalTick <= departTick`) ⇒ `ok: false, reason: 'invalid_route_plan'`; THE TELLABLE on the bearing fixture under a LITERAL bag `{ faithSpreadEnabled, pilgrimageEnabled, errandSpineEnabled, pactFormationEnabled, faithTermsEnabled, beliefAxesEnabled, believedDevotionEnabled: true }` with a signed `missionary_access` between the two deity-bearing courts (through lane 5's `signPactProposal` road): a legation dispatches at tick t, `faith_legate_arrived` mints at `t + dwell`, and the destination's foothold candidates include the legate's creed for the channel window (drive `targetedFootholds` on the destination with the legate's creed among `rivals`); the WITHHELD arm (suppress the creed at the destination between dispatch and arrival ⇒ `withheld`, no admission); the COOLDOWN (a second legation on the same pair inside the window is refused); the never-resolve walker arm (no `npcs[]` write, no `envoyErrands` write — a source scan of the leaf for `envoyErrands`/`npcs` write tokens: zero; plus the runtime pin that `worldState.envoyErrands` is byte-identical before/after). **Mutants:** the flip reverted → the registry walker direction 2 reds ("`legateErrand.js` now mints — flip built:true"); the `purposeClass` omitted → `errandSpineFields` resolves NO class for `legation` ⇒ `reason: 'invalid_purpose_class'` ⇒ the mint pin reds (the class layer is the join — proven by removing it); the arrival read's "consumed" guard removed → the same arrival mints twice ⇒ the tellable's count pin reds.

**Files (≈9):** `legateErrand.js` (NEW), `envoyErrandVocabulary.js` (one word), `errandConsumerRegistry.walker.test.js`, `couplingInclusion.walker.test.js` (one row), `faithPilgrimSeason.js` (the door), `religiousContest.js` (≤ 10), `tests/domain/legateErrand.test.js` (NEW), `couplingRegistryFaith.js` (FAITH→SPINE `read: 'src/domain/worldPulse/errandMint.js#mintErrandSpine'`, `flags: ['pilgrimageEnabled']` — the GR key `pactFormationEnabled` spelled ONLY in the comment (the five-namer trap, `pactFormationDormancyFence.test.js:198-211`); FAITH→GRAMMAR `missionaryChannelFor` row), the manifest. **Census bills:** refreeze own commit; manifest `kind:'mutation'` (the flip plant). **Byte rule:** build quoted; worker 0; engine ±0; NO `simulationRules.js` byte (bundles fresh — state so). **Gate:** full list + `errandConsumerRegistry.walker` (both directions + the exact lists), `errandSpineDormancyFence` (the site census UNTOUCHED — asserted; the espionage `!== true` precondition at `espionage/espionageGate.js` untouched), `envoyErrandLedgerSingleWriter.walker` (the leaf writes no ledger), `namedPersonTransitTotality.walker` (the leaf is NOT a movement site — or takes its manifest row, THE DWELL), `couplingInclusion.walker`, `pactFormationDormancyFence` (fence 1 dark identity — the dispatch sits behind the movers door), `grep -rlw pactFormationEnabled src | wc -l` = 5. **LIT act:** none. **Exit:** a legation mints, travels and arrives on the bearing fixture under the literal bag; both walker directions green with the list at five; dark ⇒ byte-identical.

### Car 2 — `pilgrimErrand.js`: the named pilgrim party dispatched from the season's feast/flood bands, `purposeClass 'personal'` — NEVER `religious`; the flip; the arrival as attendance, never a person

**In plain words:** when the season at a festival town runs to a feast or a flood, a party of pilgrims sets out from a neighbouring town of the same faith — named in the news for one of its own (a name read off the town's roster, nothing more), travelling as a party, arriving to swell the festival. This is ordinary people's business, not the church's, so the errand spine is told it is a personal errand, and the registry learns pilgrims are real in the same commit.

**The change.** NEW `src/domain/worldPulse/pilgrimErrand.js` (≤ 120 eff; pinned path `ERRAND_CONSUMERS:331`): `PILGRIM_ERRAND_TUNING` (the party-size BAND vocabulary — a closed set `['a handful', 'a score', 'a company']` derived from the season's band floors; the dispatch cooldown = one festival year); `pilgrimErrandFor({ worldState, fromId, toId, tick, arrivalTick })` — `mintErrandSpine({ …, purpose: 'pilgrimage', purposeClass: 'personal', … })` (⛔ `'personal'` — the volume and the registry row both say so; `'religious'` here would mis-type the whole half and the walker cannot see it — the pin below does); `pilgrimReceipt({ kind, cid, toId, settlementName, hostName, creedName, npcName, band, tick, arrivalTick, fields })` — `candidateType: 'faith_pilgrim_named_departs'` AS A LITERAL, the `{npc}` slot a DISPLAY NAME read off `settlement.npcs[]` (the JSON-alias trap: "shepherdRef/clergy are `npcs[]`" — a name is read, no ref is persisted, no NPC state moves — the never-resolve law); THE DISPATCH in `religiousContest.js` (≤ 4 lines beside car 1's, behind the movers door): when 17a's `seasonRead.band ∈ {feast, flood}` for a host this tick (read back off the SEASON RECEIPT in `pulseHistory` — the season is minted in `traditionsKernel.js`, which runs LATER in the tick than the religion fold (`pulseKernel.js:2619` vs `:1261`), so the fold reads LAST tick's season receipt: a one-tick lag, DECLARED — the pilgrims set out the tick after the feast is called), each kindred reachable neighbour (the season's own neighbour set — re-derived through 17a's exported `reachableNeighbours`) dispatches ONE pilgrim party per festival year; THE ARRIVAL: the party's arrival is ATTENDANCE — it lifts nothing beyond what the season already counted (the draw already priced these pilgrims; the party is the NARRATED body of a number the season computed — the K3-shape honesty: no double-count) and mints `faith_pilgrim_named_departs` at dispatch only (arrival is silent unless intercepted/turned back — car 3); a party dispatched to a host whose festival was CANCELLED (`TRADITION_OUTCOME.CANCELLED`) returns with `faith_legate_returned`? — no: pilgrims have no `returned` pool; the party is simply not narrated further (the annex has no such kind — record the silence, never invent a beat). THE FLIP: `:332` `built: false → true`; the walker's exact lists gain `pilgrimErrand.js`; the title re-worded to six; `couplingInclusion.walker` FAITH list gains the exact-path row.

**Pins (`tests/domain/pilgrimErrand.test.js`, NEW):** THE CLASS PIN — `pilgrimErrandFor` on a lit spine yields `fields.purposeClass === 'personal'` and NEVER `'religious'` (both assertions, anchored: the negative is anchored by the legate test's positive on `'religious'`); the dispatch fires only on feast/flood (a `steady` season dispatches nothing — the negative); one party per neighbour per festival year (the cooldown); the name is a roster display name and the roster is byte-identical after (the never-resolve pin); dark spine ⇒ nothing; the one-tick lag asserted (a feast at tick t ⇒ departure receipts at t + 1). **Mutants:** `purposeClass: 'religious'` planted → the class pin reds; the cooldown removed → the one-per-year pin reds.

**Files (≈7):** `pilgrimErrand.js` (NEW), `envoyErrandVocabulary.js` (one word), `errandConsumerRegistry.walker.test.js`, `couplingInclusion.walker.test.js`, `religiousContest.js` (≤ 4), `tests/domain/pilgrimErrand.test.js` (NEW), `couplingRegistryFaith.js` (FAITH→TRADITIONS read of the season receipt — a pulse-history read, `read: 'src/domain/worldPulse/pilgrimErrand.js#pilgrimDispatchesDue'`). **Census bills:** refreeze; manifest. **Byte rule:** build quoted; worker 0. **Gate:** full list + the registry walker (the lists at six), `errandSpineDormancyFence`, `envoyErrandLedgerSingleWriter.walker`, `namedPersonTransitTotality.walker`. **LIT act:** none. **Exit:** both rows `built: true`; the walker's partition still a real measurement (three rows unbuilt: factors, couriers, + IN-4's — `:383-388` non-vacuity holds — state the count); a pilgrim party departs on the bearing fixture's feast.

### Car 3 — THE ROAD: the war-on-the-road counterforce as the ONE LIVE ARM (harassment thins, a closed road turns back), the never-resolve walker, WR-7b declared UNCONSUMED

**In plain words:** a legation or a pilgrim party crossing a road that runs through a war is harassed — it arrives late and smaller — or turned back outright when the road is closed; nobody is taken prisoner and nobody dies, because the estate's prisoner machinery keys on named travellers and these parties carry no named traveller. That refusal is written down as a deliberate one, with the road back named.

**The change.** In the two leaves (+≤ 30 lines each) the ROAD READ: `roadStateFor({ worldState, snapshot, fromId, toId })` → `'open' | 'harassed' | 'closed'` from the relationship graph and the war layer the fold already holds (an active war edge or an occupation on the destination ⇒ `harassed`; a `hostile`/`cold_war` relationship with a blockade or siege stressor on either end ⇒ `closed` — VERIFY-AT-BUILD the exact snapshot fields; the TRADE seam law: read the RELATIONSHIP and `warPosture`, never `tradeWar.js`); `harassed` ⇒ the party's `arrivalTick` is pushed by a derived fraction and the pilgrim band drops one rung (`faith_pilgrim_intercepted` — the annex's "intercepted" is THIS harassment: VERIFY the pool's variants name no custody; ineligible ones are skipped); `closed` ⇒ the party does not depart (`faith_pilgrim_turned_back` / for legates the party returns — `faith_legate_returned` with reason `road_closed`); the SAME road read gates 17a's season contribution (the roads-closed counterforce — one read, two consumers; export it from `faithPilgrimSeason.js` and have both leaves import it). THE NEVER-RESOLVE WALKER: NEW `tests/lint/faithMoversNeverResolve.walker.test.js` — a comment-stripped source scan of `legateErrand.js`, `pilgrimErrand.js`, `faithPilgrimSeason.js` for any write into `npcs`, `envoyErrands`, `foreignGuestHolds`, `deaths`/`killed`/`alive:` tokens (zero), with the guard-the-guard positive control (a planted `npcs[0].alive = false` in a temp copy is SEEN); THE WR-7b DECLARATION in both leaf headers and the packet: "WR-7b's `foreignGuestHold.js` is UNCONSUMED by design: a hold keys on an `envoyErrands` row (`foreignGuestHoldForErrand`) and this party has none — the envoy layer is dark in every preset (`envoyDiplomacyActive`'s six keys) and the single-writer law forbids a second ledger writer; the day the envoy stack lights, a NAMED legate (a minister on a real errand row) is a design row for the WAR family's envoy consumers, not this leaf's."

**Pins:** the three road states driven on the bearing fixture (open ⇒ on time; harassed ⇒ late + one band down + `intercepted` receipt; closed ⇒ `turned_back`/`returned`, no departure); the never-resolve walker's arms; `foreignGuestHoldsOf(worldState)` byte-identical across every fixture run (the UNCONSUMED pin — a positive statement, not a silence). **Mutants:** the road read hard-wired `'open'` → the harassed/closed arms red; a planted hold write → the walker reds.

**Files (≈5):** the two leaves, `faithPilgrimSeason.js` (the shared road read), the walker (NEW), the manifest. **Census bills:** refreeze; manifest `kind:'mutation'`. **Byte rule:** build quoted. **Gate:** full list + the walker + `heraldContaminationFence` (the road read is TRUTH-side). **LIT act:** none. **Exit:** three arms driven; the walker green; the declaration written.

### Car 4 — THE KINDS CAR: seven mover kinds join the FAITH registry with their annex pools, their exact desk rows, their phrases, their voice classes and their own walker

**In plain words:** the legations and pilgrim parties now speak in the Herald — set out, arrived, withheld, returned; a named pilgrim departs, is harassed on the road, is turned back — in sentences authored and verified in August. Each kind gets its desk row, its rumour phrase, its crier voice decision and its test walker.

**The change.** NEW `src/domain/worldPulse/faithMoverReceiptPools.js` (≤ 130 eff; annex-VERBATIM §E `:320-411`); `faithNews.js` `FAITH_KIND_REGISTRY` gains SEVEN rows (significance/audience/desk/slots read off the annex headings; all Herald `faith`, all `public`); THE REGISTRY GROWTH RULE: `faithNews.js` ≈ 130 → ≈ 175 eff (under 250 — no extraction here; predicted at 19b); the receipt builders in the two leaves already spell their `candidateType` literals (cars 1–3); `settlementRumors.js` `WHAT_PHRASES` + seven rows; `heraldRouting.js` `EXACT_SECTION` + seven exact rows on `'faith'` (desk-bearing registered kinds — the refusal of the prefix door, `kindPoolFloors.walker:163-169`); `kindRegistryRoster.js` `routedTokens` MEASURED (+7); `kindPoolFloors.walker` `REGISTERED_KIND_COUNT` +7 with the `+1 at WF-2b:` comment rows; the divergence UNMOVED (all seven desk-bearing); NEW `tests/lint/faithMoverKindPools.walker.test.js` (the L6 shape — brief 17a car 3's template; `SECTION '## §E WF-2'`/`UNTIL '## §F WF-3'`, the seven-kind exact census, the annex identity, the sentinel probe, the eligibility both arms — `faith.pilgrim.intercepted`'s custody-naming variants INELIGIBLE and pinned so; the five joins on a driven receipt); `impactKindWalkers` `EXPECTED_VOICE` + seven; the `subsystemRowsFaith.js` `pilgrimageEnabled` row's `eventTypes` gains the seven literals (17a's row — the SAME key; append the tokens the lane leaves spell — `LANE_LEAVES[pilgrimageEnabled]` gains `legateErrand.js` and `pilgrimErrand.js` so the MINT_RE scan finds them).

**Files (≈11):** the pool (NEW), `faithNews.js`, `settlementRumors.js`, `heraldRouting.js`, `kindRegistryRoster.js`, `kindPoolFloors.walker.test.js`, the walker (NEW), `impactKindWalkers.test.js`, `subsystemRowsFaith.js`, `tests/domain/subsystemRowsVirtual.test.js` (the `LANE_LEAVES` widening — a test edit, no count moves), the manifest. **Census bills:** refreeze; manifest; no wiring census (src pools). **Byte rule:** `settlementRumors.js` EAGER — quote the closure table; STOP if exceeded. **Gate:** full list + `faithKindPools.walker`, `faithSeasonKindPools.walker`, `faithMoverKindPools.walker`, `phrasedKindPools.walker`, `wizardNewsAuthoring.walker`, `kindPoolFloors.walker`, `heraldRouting.walker`, `heraldContaminationFence`, `newsSubjectVocabulary.walker`, `impactKindWalkers`, `subsystemRowsVirtual.test.js`. **LIT act:** none. **Exit:** fifteen FAITH rows registered; seven routed; the freezes re-measured and stated.

### Car 5 — THE MOVERS' LIT ARM (no new key): the `pilgrimageEnabled` fence gains the movers arms; `errandSpineEnabled` CHECK-GIT-FIRST and NOT lit here; the witness unchanged; the shift-ledger ADDENDUM naming the arm DORMANT-BY-CONJUNCT and the road back — BUILT AT LANDING, rebased

**In plain words:** no new switch. The proof that the pilgrim-season switch, already on, now also governs the legations and pilgrim parties — and that in every shipped world they sit still until the shared errand spine is switched on by the lane that owns that decision — is written into the existing fence, the witness is shown unchanged, and the ledger records exactly which future switch wakes them.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION (`…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R2 — the movers' precondition `errandSpineEnabled` is lit by lane B car B / IN-4 / TR-8 car 6 (CHECK-GIT-FIRST at landing; if lit, the lit twin below MOVES on the composed tree and the shift sentence says WAKE; if dark, the twin is asserted under a LITERAL bag and the sentence says DORMANT-BY-CONJUNCT) — never lit here (the ruling in the header); a dock-green LIT arm is a FALSE GREEN. R3 — no mint, no lighting line; this car RE-READS the counts and touches no literal unless HEAD moved. R7 — the witness is EXPECTED UNMOVED; if a row moved, the mover is another lane's (STOP and report), never a re-record here.

**The change:** (1) `tests/property/faithPilgrimageDormancyFence.test.js` gains THE MOVERS ARMS: (a) dark-by-conjunct — under `SIMULATION_RULE_PRESETS.full_simulation.rules` AS COMPOSED (read off the presets object at landing), if `errandSpineEnabled !== true` there, the bearing fixture over a full year mints ZERO `religious_legation`/`faith_pilgrim_*` receipts and `worldState.envoyErrands` is byte-identical — the arm is written to READ the preset and assert the matching branch, so it stays true after the spine lights (then it asserts the WAKE branch: ≥ 1 legation on the fixture with a signed `missionary_access`); (b) the LIT TWIN under a LITERAL bag (`pilgrimageEnabled`, `faithSpreadEnabled`, `errandSpineEnabled: true` + lane 5's keys) — receipts mint (the fence can see); (c) the polarity census UNCHANGED (the movers door reads through `errandSpineActive` — zero new `errandSpineEnabled` by-name sites; assert the head's fence census still names exactly its two doors). (2) `subsystemRowsFaith.js` the row's `other` gains the movers sentence (dormant-by-conjunct; the road back). (3) THE WITNESS: `measureWitness()` diff EXPECTED EMPTY on every row (no key lit, no preset byte); quoted. (4) 525/525; espionage 21/21; `prose-manifest-cells.mjs --out` zero rows. (5) Counts re-read off HEAD (no literal expected to move — state). (6) THE SHIFT-LEDGER ADDENDUM (`docs/implementation/GOLDEN_SHIFT_LEDGER-WF-2b.draft.md`): *"WF-2b LANDED under `pilgrimageEnabled` (lit at 17a); its movers arm is DORMANT-BY-CONJUNCT in every preset at <sha> because `errandSpineEnabled` is dark; the day it lights (lane B car B / IN-4 / TR-8 car 6 — whichever composes first), legations and pilgrim parties WAKE in `dramatic_campaign` and `full_simulation` on deity-bearing worlds — THAT lane's declared-shift sentence names WF-2b among the consumers it wakes; no witness row moved; no installed campaign re-labelled; no tuning value moved; WR-7b's holds are UNCONSUMED by design (the envoy stack is dark in every preset; a hold keys on an `envoyErrands` row)."* (7) The handoff line for TR-8's brief: the registry's built list stands at SIX (`emigreErrand.js`, `envoyErrand.js`, `espionage/espionageMissions.js`, `legateErrand.js`, `pactProposals.js`, `pilgrimErrand.js`) — `brief-56b-TR-TR-8.md` expects to find the two WF-2b rows flipped and re-measures its own.

**Files (≈5):** the fence, `subsystemRowsFaith.js` (text), the shift addendum, the packet, `contributionLedgerShape.test.js` only if HEAD moved. **Census bills:** refreeze (new fence arms); manifest `kind:'rationale'`. **Byte rule:** no src byte expected (state so) else build quoted. **Gate:** full list + the witness (UNMOVED), the stability test, the engine-gated walker, the totality walker, `errandSpineDormancyFence` (both polarities green; `espionageDormancyFence` 21/21), `errandConsumerRegistry.walker`, the soak suites alone at load < 8. **LIT act:** none (the arm's lighting is the spine's owner's). **Exit:** the fence's movers arms green in the branch the composed presets select; the witness empty diff quoted; the addendum written; the six-row built list stated for TR-8.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

This sitting MINTS NO KEY and LIGHTS NO KEY. `ENGINE_GATED_VIRTUAL_RULE_KEYS.length`, `VIRTUAL_SUBSYSTEM_ROWS.length` and both `contributionLedgerShape` literals are EXPECTED UNCHANGED (read off HEAD at car 1 and re-read at car 5; stated). The registers this lane DOES move are the kinds freezes (`REGISTERED_KIND_COUNT`, `routedTokens`, the divergence — car 4) and the errand-consumer partition (cars 1–2: built 4 → 6) — re-measured at the compose against sibling kinds cars, never inherited. Law 2's "last car" is car 5 (the arm's LIT act is another lane's; car 5 records that).

## THE VOLUME ROWS THIS LANE CHANGES (ADDENDUM R6)

`§4 WF-2` slice 2b → "LANDED (§932 lane 17b), the movers DORMANT-BY-CONJUNCT until `errandSpineEnabled` lights" with the cars by sha; THE MOVERS' LEDGER correction: the movers are RECEIPT-PARTIES (the GR-2 `pactTransportFor` / INT-3b `emigreErrandFor` road), NOT rows in `worldState.envoyErrands` — the envoy layer (`mintEnvoyErrand`/`advanceEnvoyErrands`) is dark in every preset behind `envoyDiplomacyActive`'s six keys, three of them declared false (`envoyDiplomacyEnabled :920`, `npcConsequencesEnabled :962`, `routeLifecycleEnabled :976`); `§5 seam 6` (WR-7b hold consumption) → STRUCK for WF-2b (no row to hold; a named legate is a WAR-family design row); `§2` the flag row's conjunct `errandSpineEnabled === true` → the MOVERS door only (`pilgrimMoversActive`, read through `errandSpineActive`); the compiled programme's "with purpose `religious`" → the pilgrims are `personal`; the volume's Req 14 "named movers' verbs are SP-1's own" → "no state ⇒ no verb" (receipt-parties); the ARCH §7.1 `foreignGuestHold.js` row loses WF-2b; the history fold's WF-2 row folded; ⭐ A CROSS-FAMILY FINDING FOR THE CHAIR: WR-7a embassies, WR-7b holds and TR-8's factors sit behind the same dark six-key conjunction — the WAR/WY/TR families' lighting tables owe a row.

## OWNER-GATED, KEPT OUT

- Lighting `errandSpineEnabled` — NOT this lane's (the ruling; TR-8 car 6 / lane B / IN-4).
- Lighting the envoy stack (`envoyDiplomacyEnabled` + `npcConsequencesEnabled` + `routeLifecycleEnabled` …) — a WAR/WY act; named, unpriced.
- A NAMED legate (a minister on a real `envoyErrands` row, holdable by WR-7b) — a WAR-family envoy-consumer design row; not built.
- `ENVOY_PURPOSES` — closed at `['sue','self_parlay']`; never widened (the class layer is the join).
- Tuning: `LEGATE_ERRAND_TUNING`, `PILGRIM_ERRAND_TUNING` — RAW with derivations, ratified at the soak.
- Persisted shapes widened: NONE (receipt-parties; no ledger, no NPC field) — recorded. Push/deploy/migration/deletion/spend; the golden master.

## RISKS

**Grep traps (VERBATIM by reference: HISTORY-WF's paragraph after §B; RECHECK §5; HISTORY-TRADE's twelve; the ones that bite here):** `pilgrim` three senses; `couriers` is IN-4/ES-1's split row; `roaming` is the stressor twin; `covert` is espionage vocabulary (a `purposeClass 'covert'` row exists — never ours); `oracle` is the test discipline + a clergy label; `envoyErrands` in a grep is the WAR ledger — this lane WRITES it nowhere (a hit in the leaves is a defect); `personal` vs `religious` — the pilgrim row's class is the trap the walker cannot see; `INSTR-1 ≠ TR-1`.

**Collision surfaces:** `envoyErrandVocabulary.js` (SERIAL lane B → IN-4 → THIS → TR-8 → ES — one word per lane; CHECK-GIT-FIRST); `errandConsumerRegistry.walker.test.js`'s EXACT lists (every consumer lane edits them — resolved by MEASUREMENT of `builtModules` at the compose, never by copying a brief's list); `couplingInclusion.walker`'s FAITH list (append-only exact-path rows); `religiousContest.js` (ONE WF lane in flight); the kinds freezes (every family's kinds car); `faithNews.js` (append-only; the growth rule).

**The addendum, by path:** `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R2 (the movers' precondition is the spine; the lane that lights it declares the wake); R3 (no mint here); R5 (car 4 ≈ 11 files — under the cap); R6 (the rows above); R7 (the witness expected unmoved).

**Load hazards:** `gate-mutex.sh` false green; dist-gated suites skip; `deno.lock`; `git grep` reads the INDEX; the faith property suites in two halves; a moved DRIVEN GOLDEN is a STOP.

**Design hazards:** THE SINGLE-WRITER LAW — the leaves NEVER write `envoyErrands` (the walker + the runtime pin); THE ONE-READER LAW — never read `purposeClass` off a field block outside the errand family (read the mint's `reason`); the class is EXPLICIT on both mint calls (`religious`/`personal`) because neither purpose word is a war purpose; the arrival read's "consumed" guard (a phantom double arrival is the WF-8A false-receipt shape — 25 obituaries for 3 creeds); the one-tick lag between the season receipt and the pilgrim dispatch is DECLARED; THE DWELL is priced by the pact lane's read, never by a transit token (or the leaf takes a manifest row); the movers door reads through `errandSpineActive`, adding no by-name site to the spine fence's census; the harassment arm never mints a hold; the `{npc}` slot is a display name, never a persisted ref.

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (NONE — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title (cars 1, 2, 3, 4, 5); the mutation manifest per new invariant file; no wiring census stamp (no prose desk byte); prose-numerics at car 4 (band words, never digits); NO edge bundle rebuild (no `simulationRules.js` byte — state so at every car); the Seat/Lane trailer `Seat: Opus 5 — Fable-unvalidated` · `Lane: 17b`; porcelain 0. At cars 1–2 additionally `errandConsumerRegistry.walker` (both directions + the exact lists), `errandSpineDormancyFence` (site census untouched), `envoyErrandLedgerSingleWriter.walker`, `namedPersonTransitTotality.walker`, `couplingInclusion.walker`, `pactFormationDormancyFence`, `grep -rlw pactFormationEnabled src | wc -l` = 5. At car 3 additionally `faithMoversNeverResolve.walker`, `heraldContaminationFence`. At car 4 additionally the three FAITH kind walkers, `phrasedKindPools.walker`, `wizardNewsAuthoring.walker`, `kindPoolFloors.walker`, `heraldRouting.walker`, `newsSubjectVocabulary.walker`, `impactKindWalkers`. At car 5 (built on the consist, rebased — ADDENDUM R2(iii)) additionally: the witness (UNMOVED, quoted), the stability test, the engine-gated walker, the totality walker, `errandSpineDormancyFence` + `espionageDormancyFence` 21/21, `prose-manifest-cells.mjs --out` base/tip, the generation classified diff 525/525 unchanged, the soak suites alone at load < 8.
