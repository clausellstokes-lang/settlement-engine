# THE LANE — PRICING §C lane 7 · TR-1 THE MOUNT (`casusCommerciiEnabled` lit) → TR-3 BELIEVED MARKETS (`believedMarketsEnabled`; the JOIN CAR first)

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED / PLAUSIBLE as marked; line numbers rot — re-measure by SYMBOL. Protocols: `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 5 (1 + 4), range 5–6** (PRICING §C lane 7: 3–4 C "the mount line, then TR-3 cars A/B/C"; HISTORY §A.4: TR-1 1 + TR-3 **3–5** "+1–2: the JOIN CAR"; +1 here for the dossier line as its own user-facing car). Waves: TR-1's mount and LIT (car 1); TR-3's join (car 2), composer (car 3), dossier line (car 4), mint/LIT (car 5).

**Prerequisites:** nothing unbuilt (PRICING §C: "forced: TR-6 depends on TR-3; TR-3 boards before TR-6"); TR-1 landed (`d7ea69a4b` + `93c118b6d`); SP-B landed (`4c0f2f38b`); **lane 2 (TR-5) car 7 EXPECTED on the consist first** — it lights `beliefAxesEnabled` + `believedScarcityEnabled`; if it is not, car 5 of THIS lane lights them (CHECK-GIT-FIRST — the protocol's "first lane on the spine pays"). TR-4 not required. Boards beside lane 6 (IN-4) and lane 8 waits on this lane.

**Shared files this lane declares (PROTO §7.3 rule 1), slack at the cut:** `src/domain/worldPulse/settlementLifecycleKernel.js` 733/800 (+≤12, car 1) · `tests/domain/pactKernelMount.test.js` (car 1 — the stage-order pin) · `src/domain/certification/subsystemRowsCompact.js` 237/800 (TR-1's row TEXT only; no index moves) · `src/domain/worldPulse/beliefAxisSubjects.js` 179/800 (SP-B's leaf; car 2 — SERIAL with any SP/IN lane on it; PROTO §7.1 row "beliefMap.js · beliefAxisSubjects.js · beliefAxes.js: SERIAL TR-3 → HB-6 → HB-7; IN-2 not beside") · `src/domain/worldPulse/beliefAxes.js` 112/800 (one line, car 2) · `src/domain/worldPulse/beliefMap.js` **781/800 — NET-ZERO in this lane** (J-FP-4/Q3: every TR-3 consumer is a sibling leaf) · `src/domain/spatial/dispatchEV.js` 118/800 (the destination seam, car 3) · `src/domain/worldPulse/supplyKernel.js` 489/800 (the composer threaded into `buildDispatchEV`, car 3 — SERIAL after lane 1's car 3 on this file) · `src/domain/display/marketPrices.js` 134/800 + `src/components/new/tabs/EconomicsTab.jsx` **596/600 — 4 lines slack** (car 4) · the flag-mint surfaces + `SIM_LIT` + `subsystemRowsTrade.js` · `tests/property/casusCommerciiDormancyFence.test.js` (car 1) · `tests/property/believedWorldAxesDormancyFence.test.js` (car 5 only if this lane lights the axes) · `tests/lint/couplingInclusion.walker.test.js` (no pattern edit: the new leaves are `commodityScarcityTruth.js` (matches `commodity`), `commercialBeliefScarcity.js`, `commercialDispatchDestination.js` (match `commercial[A-Z]`)).

---

## THE STATE OF THE RECORD

- **The history fold.** `HISTORY-TR-2026-09-15.md` does not exist under that name; `…/recon-32-34/HISTORY-TRADE-2026-09-15.md` is the TR fold and wins over PRICING §B. TR-1: **PART-BUILT — whole but inert; two independent inertness counts, only one priced**: (a) unmounted — `advanceCommercialReasons` (`commercialReasons.js:457`) has NO caller in src (the tree says so: `subsystemRowsCompact.js:283-293` "A DARK INSTRUMENT WITH NO MOUNT"; `src/lib/spatialUsage.js` EXEMPT row "advanceCommercialReasons has NO CALLER IN src/ … Do not revisit this row when the mount lands"); (b) **only 6 of 16 reasons can ever be non-zero** — three pairs score live state (`scoreToll/scoreMarketAccess/scoreDependency` at `:345-347`), five are `scoreRegisteredSeam(evidence.seam_*)` (`:348-352`) returning `ZERO_PAIR` because `buildCommercialEvidence`/`commercialEvidenceFor` (`:283-335`) is fed `seams = {}` (`:457-458`) — the five producers belong to TR-5 (`contract_default`), TR-6 (`cornering`, `famine_profiteering`), M7 (`contraband_injury`) and TR-9/WY (`route_predation`). **The mount car DECLARES 6/16** and each producer lane raises the count as it lands (TR-5 car 4 → 7/16; TR-6 → 9/16). TR-3: **PART-BUILT (belief half only), GENERATION-FROZEN ground truth** — `foodRatio` has exactly two writers (`foodGenerator.js:337/:424`); `goodsByCategory` memoises generator lists (`beliefAxisSubjects.js:229-250`); the file imports no commodity state; the TRUTH side already reads live stocks on the product (`marketPrices.js` over M6a bands + M6d drift, `EconomicsTab.jsx:16/:263/:462`). "The join car is the wave's precondition, not polish."
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §4 TR-1 (the T9 seam edit at `tradeWar.js`; the volume said "one commit" — the MOUNT was never in that commit), §4 TR-3 (`beliefScarcity.js` the SIBLING leaf, `dispatchDestination.js` the WHERE composer; "beliefMap.js … receives NET-ZERO seam lines only"), §5b seam 6 (toward SP-2), §6 Q3 (ruled: sibling leaves). The design law `docs/DESIGN_FP_TRADE.md` §3 SEAM RULING THREE (the two scarcities: "NO MERGING, not no-co-reading … the whitelist is enumerated HERE and only here: (1) the CORNER COMPOSER (TR-6) … (2) the DISPATCH COMPOSER (TR-3's dispatchEV rewiring: believed dearness is the belief side; the need premium is a TRUTH-side physical-need read, `dispatchEV.js:12-31` — `needPremium` reads storageMonths/need state, NEVER a scarcity band)"), §5 TR-3 (the pins; T-1 TELLABLE; the dossier crown "Grain: fair here; dear in Aldenmoor, they say"; "tradeWar's truth-scored contest is DECLARED OUT of this wave (T9 fence)"). The compiled programme §5 #23 (TR-1) and #25 (TR-3, line 1605). The dependency clause: `DESIGN_FP_TRADE.md:267` `TR-3 ⇒ SP-2 landed + TR-1;` → `tradeConvergenceContract.js:322-328` `requiresTradeFlags: ['casusCommerciiEnabled']`, `requiresForeignFlags: ['believedScarcityEnabled']` (the walker couples them — untouched by this lane).
- **The record's frozen pending door** (`subsystemRowsVirtual.js:595`) and the stale register comment `simulationRules.js:255-258` ("declared-pending certification entry (TR architecture Q4 …)") — car 1 amends the comment in passing.
- **The owner's laws:** built lit; tuning raw (belief update weights per writer class arrival ≫ rumor ≫ plant, decay, destination weight, staleness bands — `DESIGN_FP_TRADE.md` §7) and ratified at the soak; the §776 PRICE-HEURISTICS LAW binds car 4 (bands and words, never a coin figure — `marketPrices.js:1-40`); nothing pushes/deploys/applies/deletes/spends; ALWAYS LAYMAN'S TERMS on the dossier line.

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/commercialReasons.js` | 289/800 | `casusCommerciiActive(rules)` (the door); `advanceCommercialReasons({snapshot, worldState, tick, rules, seams = {}})` `:457-…` — "THE ONE WRITER. Dark ⇒ an immediate no-op returning the world's own reference"; `makeCommercialPressureRead` (consumed by `tradeWar.js:335-337` under `rules.casusCommerciiEnabled === true` — the SECOND by-name read, the T9 seam); `commercialEvidenceFor` `:283-335`; `scoreRegisteredSeam` `:244-250` |
| `src/domain/worldPulse/settlementLifecycleKernel.js` | 733/800 | `advanceSettlementLifecycle({snapshot, worldState: hostWorldState, settlementUpdates, pIndex, rng, tick, now})` `:516`; the three own-flag stages: `advanceSovereigntyMarket` `:538`, `advancePeacetimePacts` `:564`, `advanceDemographics` `:571`; the DORMANCY GATE `:588` with the dark return `:589-604` (`changed: demo.changed \|\| market.changed \|\| pacts.changed` `:603` — "EVERY STAGE THAT RAN ABOVE THIS GATE VOTES ON `changed`, AND THE DARK PATH IS THE ONE THAT MATTERS MOST"); the lit return `:1160-1170` (`let changed = cloned \|\| demo.changed \|\| market.changed \|\| pacts.changed`) — TWO return paths, both vote |
| `tests/domain/pactKernelMount.test.js` | — | recorders for the three stages `:45-69`; order pins `expect(run.order).toEqual(['market','pacts','demo'])` `:167/:174/:181`; per-stage vote pins on BOTH paths `:165-203`; the identity chain `:205-…` |
| `src/domain/certification/subsystemRowsCompact.js` | 237/800 | TR-1's row `:265-305` (`stateKeys: Object.freeze([])` with the paragraph "The declaration lands the day the writer is mounted, not before" `:283-292`; `other` names the WIRING WAVE; `soakEvidence: 'unobserved'`) — a BODY leaf: TEXT edits only, never a re-order |
| `src/lib/spatialUsage.js` | 185/800 | the `commercialReasons` EXEMPT row `:349` ("…the classification here therefore must survive the wiring wave … Do not revisit this row when the mount lands") — UNTOUCHED |
| `src/domain/certification/couplingRegistryTrade.js` | 16/800 | `TR1_SEVERANCE_PRESSURE_COUPLING` (flags `['warLayerEnabled', 'casusCommerciiEnabled']`) — unchanged by the mount |
| `tests/property/casusCommerciiDormancyFence.test.js` | — | the four fences + lit mutant; the DORMANT/preset arm `:213-241` (`expect(ENGINE_GATED_DORMANT_RULE_KEYS).toContain(FLAG)` `:239`); fence 3's spy on `makeCommercialPressureRead` from OUTSIDE `tradeWar.js` `:52-66`; the adversarial fixture `:71-101` drives `advanceCommercialReasons` DIRECTLY (not through the kernel) — the mount adds a second caller the fence must also see: extend fence 3 with a spy on `advanceCommercialReasons` counted through `advanceSettlementLifecycle` |
| `src/domain/worldPulse/beliefAxisSubjects.js` | 179/800 | `SCARCITY_BANDS` `:85`; `goodsByCategory` `:229-250` (memoised per settlement object; generator lists `localProduction/primaryExports/primaryImports/necessityImports`); `scarcityGroundTruth(settlement)` `:269-291` (the four-rule rung + the `foodRatio` shift with `SUBJECT_AXIS_TUNING.FOOD_DEFICIT_RATIO 0.95 / FOOD_SURPLUS_RATIO 1.10`); `subjectGroundTruth(item, sources, gates)` `:383-400` — `sources` today carries `{religionState}` only; imports: `data/constants.js`, `region/goodsCatalog.js` (worker-graph file — IMPORTED, not edited) |
| `src/domain/worldPulse/beliefAxes.js` | 112/800 | `subjectAxesActive(worldState)` `:88-97` (the three by-name reads); the ground-truth call `:174` `subjectGroundTruth(item, { religionState: opts?.religionState }, subjectAxes)`; the fold `:273` |
| `src/domain/worldPulse/beliefMap.js` | 781/800 | `beliefsActive` `:204-208` (marker ∧ `infoMode !== 'omniscient'`); `beliefRecord` `:225-231`; the record typedef `:345-349` (`scarcityBands` "only under believedScarcityEnabled ∧ beliefAxesEnabled") |
| `src/domain/spatial/dispatchEV.js` | 118/800 | the header `:1-45` ("THE DETERRENT reads BELIEF … nothing yet chooses WHERE"); `believedDestinationDanger` `:206`, `needPremium(stock, target)` `:224` (a TRUTH-side NEED read — the Seam Three line is drawn AT this read), `dispatchDecision({short, needPremium, appetite, believedDanger, caution, priorWillingness, now, override})` `:332` |
| `src/domain/worldPulse/supplyKernel.js` | 489/800 | `buildDispatchEV({worldState, graph, itemById, localSettlements})` (the ev context threaded into `advanceCommodityFlow` `:479-482`) — the seam where a destination weight enters |
| `src/domain/spatial/commodityFlow.js` | 448/800 | `commodityBand(stock, target)` `:187-193` (shortage/adequate/surplus over `SHORTAGE_FRAC/SURPLUS_FRAC × target`); `stockOf` `:225`; `spatialLedgers.commodityStocks` the sparse `{sid: {gid: units}}` map; the dispatch loop consumes `ev` at `:700-760` |
| `src/domain/display/marketPrices.js` | 134/800 | `commodityBandForGood(worldState, settlementId, goodId)` `:143-152` (the display's own band read over `commodityStocks`), the MOVEMENTS enum, the §776 header |
| `src/components/new/tabs/EconomicsTab.jsx` | **596/600** | renders `marketPrices` at `:16/:263/:462` (HISTORY) |
| `src/domain/worldPulse/simulationRules.js` | 352 eff | `casusCommerciiEnabled` `:253-259` (registered 2026-08-04; the stale Q4 sentence); `warLayerEnabled: true` only in `dramatic_campaign` `:809` and `full_simulation` `:873`; `infoMode` omniscient by default `:56`, `perfect_delayed` `:857` (living_realm), `full` `:930` (full_simulation) |
| the fixture goldens | — | `tests/property/intelTradeDormancyGolden.test.js`, `informationStatecraft…`, `corruptionWeb…` (PROTO §3.1B "TR notes intelTrade, informationStatecraft, corruptionWeb") — literal rules; none may move |
| ABSENT | — | any src caller of `advanceCommercialReasons`; `beliefScarcity.js`, `dispatchDestination.js`, `believedMarketsEnabled` (zero hits); any live-stock input to `scarcityGroundTruth` |

**Budgets (PROTO §4.1):** worker EXACT — `goodsCatalog.js` is imported, never edited (worker `stat` unmoved, quoted); `engine` chunk ±0 (worldPulse/spatial leaves ride the advance worker; `marketPrices.js` is lazy display); first-paint: the mint car's `SIM_LIT` line(s) only, quoted; `EconomicsTab.jsx` 596/600 — car 4 adds ≤3 effective lines or extracts first.

## ALREADY DONE (do not re-land)

TR-1 whole (taxonomy · writer · scorers · news · walkers · the T9 seam in `tradeWar.js` · the coupling row · the four-fence set · the register entry — the MINT is done; only the MOUNT and the LIGHTING remain) · SP-B's scarcity family, gate and field (`beliefAxes.js:91`, `beliefMap.js:347`, 11 src reads) · M6c's GO/NO-GO (`dispatchEV.js`) · the truth side's display (`marketPrices.js` under §776) · `tradeConvergenceContract.js`'s TR-3 row.

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-1→TR-3`; PACKET caps.

### Car 1 — TR-1 MOUNTED AND LIT: the fourth stage in the lifecycle kernel, 6/16 declared, `casusCommerciiEnabled` in the four lit presets

**In plain words:** the commercial-grievance ledger that has existed since August but that nothing ever ran is wired into the settlement-lifecycle step of every tick, its certification row finally names the container it fills, and the switch is turned on for worlds born into the four living presets — with the honest declaration that only six of its sixteen grievance kinds can fire until the neighbouring lanes land their producers.

**The change.** In `advanceSettlementLifecycle` (`settlementLifecycleKernel.js:516`), a FOURTH own-flag stage AFTER `advanceDemographics` (`:571-586`) and BEFORE the dormancy gate (`:588`): `const reasons = advanceCommercialReasons({ snapshot, worldState: demo.worldState, tick, rules: asObject(hostWorldState).simulationRules, seams: commercialSeamsFor(demo.worldState) });` — reading the FRESHEST world (post-market, post-pact, post-demographics: a grievance about this tick's own pact default is this tick's, not next week's — JUDGMENT, vetoable); `seams` from a registry `commercialSeamsFor(worldState)` exported by `commercialReasons.js` (+~10 eff) that returns `{}` today — the FIVE producers register there as they land (TR-5 car 4, TR-6, M7's contraband, TR-9's route predation); the world threaded onward is `reasons.worldState`; BOTH return paths vote `|| reasons.worldState !== demo.worldState` (the writer returns the world's own reference when dark or when nothing changed — `:462-464` — so reference inequality IS the vote). Delta in the kernel ≤ 12 effective lines (733 → ≤745). `pulseKernel.js`: ZERO edits. ⚠ THE VOTE LAW (`:592-604`): a stage that wrote under a `false` vote has its whole worldState DISCARDED at `applyPulseMover` — pin it on both paths.

**The declaration:** the kernel comment and TR-1's certification row say, in words, "MOUNTED at §932 lane 7 car 1; `seams` is `{}` at this mount, so SIX of the sixteen reasons (toll_extortion/toll_relief, market_exclusion/market_opened, dependency_fear/dependency_comfort) can score and TEN are structurally zero until their producers register (TR-5: contract_default/honored · TR-6: cornering/provision, famine_profiteering/famine_relief · M7 wiring: contraband_injury/honest_gates · TR-9/WY: route_predation/route_wardenship)". The row's `stateKeys` becomes `Object.freeze(['spatialLedgers.commercialReasons'])` (the row's own instruction), the DELIBERATELY EMPTY paragraph amended (never deleted) to record the mount date; `soakEvidence` stays `'unobserved'`; the `simulationRules.js:255-258` Q4 sentence amended: "(the Q4 pending door was later STRUCK — the row is AUTHORED in subsystemRowsCompact.js)".

**The LIT act (PROTO §1.2 — the key is already REGISTERED with its row and its by-name reads, so this car is items 2, 3, 10–12):** the `SIM_LIT` cohort minted if absent (CHECK-GIT-FIRST: lanes 1–3 may have) with `casusCommerciiEnabled: true, // TR-1 — lit at §932 lane 7 car 1 (the mount)`; the register entry's comment gains the LIT paragraph: "LIT in realistic_regional · dramatic_campaign · living_realm · full_simulation; the ledger writer runs in all four; the T9 seam (tradeWar's escalation deposit) bites only where `warLayerEnabled` is lit — dramatic_campaign and full_simulation — and is dormant in the other two by conjunction". The fence `casusCommerciiDormancyFence.test.js` RE-CHARTERED (PROTO §3.2): the `:213-241` arm → "VIRTUAL, REGISTERED, LIT in exactly the four (roster from `SIMULATION_RULE_PRESETS`), absent from the three"; fence 1's dark world stays the adversarial fixture with `quiet_local.rules`; fence 3 gains a SECOND spy — `advanceCommercialReasons` counted through `advanceSettlementLifecycle` (the mount's call path): dark ZERO, lit under `full_simulation` MOVES (the spy sits on `commercialReasons.js`'s export, imported by the kernel — the correct direction, `:29-35`'s WR-10 lesson); the truthy-non-true probe added. The witness re-recorded BY HAND with the per-preset diff quoted: `rulesSha256`/`litFlagCount` on five rows; `worldStateSha256` on the four lit rows IF the 52-tick birth worlds carry an entrepôt or a closed market pair (PLAUSIBLE — the ledger is recomputed from state each pulse and DROPPED when empty, so a calm born world may move NOTHING: say so). The GOLDEN_SHIFT_LEDGER draft with the DECLARED-SHIFT sentence ("`casusCommerciiEnabled` lit on the four presets at landing `<sha>`; 6/16 reasons reachable; the T9 seam live in dramatic_campaign and full_simulation; …"). `pactKernelMount.test.js`: a fourth recorder (`reasons`), the order pins → `['market','pacts','demo','reasons']`, the per-stage vote pins on BOTH paths for the new stage, the identity chain extended (reasons receives demo's world). The census literals re-measured out of band (already a registered key — `union` unchanged; `governed` may move with a preset declaration: MEASURE). The edge bundles rebuilt (any `simulationRules.js` byte). `spatialUsage.js`'s EXEMPT row: UNTOUCHED (its own text forbids revisiting). The espionage fence 21/21 quoted; prose-manifest cells zero moved, quoted.

**Pins / negatives:** the mount pin (the fourth stage runs in order with the identity chain); the vote pin on both paths (a lit fixture that writes a ledger under a dark host — the ordinary configuration — survives `applyPulseMover`; NEGATIVE CONTROL: force the vote false → the ledger vanishes at the mount, the pin reds); the 6/16 pin — on the adversarial fixture lit, exactly the three live pairs can score and every seam pair is `ZERO_PAIR` (anchored on `toll_extortion` scoring); the dark twin — the kernel's dark return is the SAME references (the J1 identity idiom). Mutant: the stage removed → the mount pin reds (the point: the stage is now load-bearing).

**Files (≈10):** `settlementLifecycleKernel.js`, `commercialReasons.js` (the registry), `subsystemRowsCompact.js` (text), `simulationRules.js` (`SIM_LIT` + two comments), `tests/property/casusCommerciiDormancyFence.test.js`, `tests/domain/pactKernelMount.test.js`, `tests/domain/commercialReasons.test.js` (the 6/16 arm), `tests/fixtures/preset-lighting-witness-golden.json`, `tests/soak-harness/coveringArrayCoverage.test.js` (if moved), `docs/GOLDEN_SHIFT_LEDGER.md` (dock); regenerated: the four edge-shared files. **Census bills:** refreeze own commit; manifest (a `kind:'mutation'` for the stage-removed plant); the witness; the shift draft. **Byte rule:** build + verify:dist quoted; worker unchanged; engine ±0; closure +1 `SIM_LIT` line in bytes. **Gate:** the full list + the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence, the soak suites alone at load < 8. **Exit:** `ENGINE_GATED_DORMANT_RULE_KEYS` lacks `casusCommerciiEnabled` (measured); the fence green in LIT shape with two spies; the mount and vote pins green; the witness diff quoted; the row declares 6/16.

### Car 2 — THE JOIN CAR: `commodityScarcityTruth.js` wires live M6a stocks into the believed-scarcity ground truth

**In plain words:** what a town "really has" of each good class stops being a fact frozen at world creation and starts tracking the live warehouse stocks the caravan engine already moves — so beliefs about scarcity can finally be wrong, corrected, and surprised.

**The change.** `src/domain/worldPulse/commodityScarcityTruth.js` (NEW, ≤120 eff; TRADE pattern `commodity` — the truth-side scarcity derivation of Seam Three's three-module topology): `liveScarcityShiftsFor(worldState, settlementId)` → `Record<category, -1|0|1>` over `spatialLedgers.commodityStocks[sid]` — for each good the settlement holds a stock for, `commodityBand(stock, target)` (imported from `spatial/commodityFlow.js`; the consumer target `COMMODITY_TUNING.STOCKPILE_TARGET`, the producer cap where the settlement is a producer — PLAUSIBLE: the leaf reads the target the way `marketPrices.commodityBandForGood` does, `:143-152`) → shortage `−1`, surplus `+1`, adequate `0`, folded per CATEGORY via `normalizeGood(goodId).category` (the catalog is IMPORTED — worker-graph file, never edited) with the sharper shift winning a tie; a settlement with no stock rows yields `{}`. `beliefAxisSubjects.js` (+~8): `scarcityGroundTruth(settlement, liveShifts = {})` applies `shiftRung(SCARCITY_BANDS, band, liveShifts[category])` AFTER the generator-list rung and BEFORE the `foodRatio` shift — THE FROZEN DERIVATION IS THE FLOOR (a class with no live stock keeps its frozen rung: declared in the docblock as the wave's floor); `subjectGroundTruth(item, sources, gates)` reads `sources.liveScarcityShifts`. `beliefAxes.js:174` (+1 line): passes `liveScarcityShifts: liveScarcityShiftsFor(worldState, item.id)` in `sources`. Cross-layer read INFO(`belief[A-Z]`)→TRADE(`commodity`): a CW-0 coupling row in `couplingRegistryTrade.js` (owningWave `TR-3`, read `beliefAxisSubjects.js#scarcityGroundTruth`, receiptField the belief record's `scarcityBands`, counterforce the arrival write) in the SAME commit or `tests/lint/couplingInclusion.walker.test.js` reds the new pair. Persisted shape: NONE (belief records already carry `scarcityBands`).

**Pins / negatives:** the floor pin — a settlement with no `commodityStocks` row yields the SAME bands as the pre-car derivation (byte-identical over the 525 golden configs' economicState — an executed corpus pin, generator-side inputs only); the live pin — a `shortage` stock on a `plentiful` class moves it to `sufficient`, a `surplus` on `pinched` to `sufficient`; the food term still applies after the shift; `tests/domain/beliefAxisSubjectsFoldPin.test.js` green unchanged; `believedWorldAxesDormancyFence` fence 1 green (its fixture carries no stocks); the SP-B dormancy golden fixture (`tests/fixtures/beliefAxes-dormancy-golden.json`, literal rules) MUST NOT MOVE — a move is a STOP. Mutant: the floor severed (an absent stock read as shortage) → the corpus pin reds.

**Files:** the new leaf, `beliefAxisSubjects.js`, `beliefAxes.js` (+1), `couplingRegistryTrade.js`, `tests/domain/commodityScarcityTruth.test.js` (NEW), `tests/domain/beliefAxisSubjects.test.js` (the live arm). **Census bills:** manifest; refreeze own commit. **Byte rule:** build quoted; worker unchanged (imports only); engine ±0. **Gate:** full list. **LIT act:** none (the join runs under SP-B's keys — lit by lane 2 or by car 5; the join's effect is visible only where belief maps exist). **Exit:** floor pin + live pin green; no fixture golden moved; the coupling row present; lane 2's ONE-SHOT declaration is now DISCHARGED — the lane's report says so by name so the chair can amend lane 2's declaration at collection.

### Car 3 — `commercialBeliefScarcity.js` + `commercialDispatchDestination.js` (the WHERE composer) + the dispatchEV seam + the K3/no-merge pins + T-1

**In plain words:** a merchant town now chooses WHERE to send its wagons by what it BELIEVES the markets are paying, weighed against how badly the buyer needs it and how dangerous the road is believed to be — and a permanent scan proves that belief and truth are read side by side but never averaged into one number.

**The change.** `commercialBeliefScarcity.js` (NEW, ≤120 eff): the belief-side READ facade — `believedScarcityBandOf(worldState, observerId, subjectId, category)` and `believedDearness01Of(...)` (rung distance below `sufficient` on `SCARCITY_BANDS`) over `beliefRecord(...).scarcityBands` ONLY (never a truth read — the per-side import pin); `believedMarketsActive(rules)` — THE ONE by-name read `rules.believedMarketsEnabled === true`. `commercialDispatchDestination.js` (NEW, ≤150 eff; Seam Three's whitelisted composer #2): `destinationAttraction01({believedDearness01, needPremium01, believedDanger01, caution, appetite})` — believed dearness is the belief side; `needPremium` (from `dispatchEV.needPremium`, a TRUTH-side NEED read of stock vs target) is the truth side; the two enter as SEPARATE terms of the attraction (`believedDearness01 · DESTINATION_WEIGHT + needPremium · appetite`) and NO expression combines a truth BAND with a belief BAND (the truth band read `commodityBand` is never imported here — need is need, not scarcity); returns `{attraction01, receipt}`. THE SEAM: `dispatchEV.js` (+≤6): `dispatchDecision` accepts an optional `destinationAttraction01` that, when present, replaces `needPremium · appetite` on the attraction side (absent ⇒ M6c verbatim, byte-identical); `supplyKernel.js buildDispatchEV` (+≤6): threads the composer under `believedMarketsActive` — SERIAL after lane 1's car 3 on this file. THE LETTERS-HOME WRITE: a caravan ARRIVAL writes the destination's TRUE band home as an ARRIVAL-grade report through SP-B's existing `foldSubjectAxes` reports channel (`beliefAxes.js:273`; `SUBJECT_AXIS_TUNING.SCARCITY_ADOPT 0.7`) — the existing update machinery, no new writer; the report is minted in the kernel's arrival loop under the flag (≤4 lines in `supplyKernel.js`). THE PINS: the K3 structural set per consumer (import pin: `commercialDispatchDestination.js` imports `commercialBeliefScarcity.js` and `dispatchEV.js` and NEVER `commodityScarcityTruth.js`/`beliefAxisSubjects.js`; a token scan for `scarcityGroundTruth|liveScarcityShiftsFor|commodityBand(` in the composer → zero; guard-the-guard: the positive control seeds a deliberate `commodityBand(` import in a throwaway copy → the scan reds); THE NO-MERGE PIN (`tests/lint/scarcityNoMerge.walker.test.js`, NEW): a token scan over `src/` for ARITHMETIC COMBINATION of a truth-band value and a belief-band value in one expression (identifiers from both sides — `scarcityGroundTruth|liveScarcityShifts` vs `scarcityBands|believedDearness` — within one arithmetic expression) → zero; the whitelist census: EXACTLY the two co-importing modules named in `DESIGN_FP_TRADE.md` §3 Seam Three — `commercialDispatchDestination.js` (this car) and the corner composer (TR-6 — ABSENT today; the census admits it BY NAME so TR-6 joins at N+1 without widening; a third co-importer reds); guard-the-guard: seed a band-average in a throwaway module → the scan reds. STALENESS REACHABLE: a fixture where the believed band is ≥ two rungs from truth after an unheard glut, receipted (the `hostilityInference`-style receipt on the record). T-1 TELLABLE (`tests/domain/believedMarketsTellables.test.js`): a caravan dispatched on a stale `scant` belief arrives after the correction into a `plentiful` truth — the arrival receipt names BOTH bands ("came for the famine and found the harvest"), and the letters-home write moves the origin's belief to the true band on the next fold. Dormancy: the omniscient dispatch path byte-identical (`beliefsActive` false ⇒ the composer is never consulted — pinned on `realistic_regional` rules); the dark twin under `full_simulation` with the flag absent byte-identical to M6c. Mutants: whitelist-widen plant (a third co-importer) → census reds; merge-expression plant → the no-merge scan reds.

**Files (≈9):** two leaves, `dispatchEV.js`, `supplyKernel.js`, `tests/lint/scarcityNoMerge.walker.test.js`, `tests/domain/commercialDispatchDestination.test.js`, `tests/domain/believedMarketsTellables.test.js`, `tests/domain/commercialBeliefScarcity.test.js`, `tests/domain/metronomeCooldownLint.test.js` untouched (no `candidateType`). **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted; worker unchanged; engine ±0. **Gate:** full list (every `*DormancyGolden` unmoved — `roads`, `seaRoads`, `intelTrade` drive literal rules; the M6a `commodityFlow.kernel.integration` fixtures unmoved: the composer is absent in them). **LIT act:** none (`believedMarketsEnabled` read by name here ⇒ THIS car pays the MINT half dark — PROTO §5.1 items 1, 4–8, 13–14; the row AUTHORED in `subsystemRowsTrade.js`; car 5 pays the LIGHTING). **Exit:** the composer live under the lit fixture; both scans red their plants; T-1 pinned; the mint registered dark.

### Car 4 — the dossier market line: "Grain: fair here; dear in Aldenmoor, they say" (ONE user-facing surface)

**In plain words:** a town's economics page gains one sentence per traded good class saying how the market stands at home and what the town BELIEVES its neighbours are paying — the "they say" is structural honesty, not flavour — with a DM toggle that shows the truth beside the belief.

**The change.** `marketPrices.js` (134 → ≤190 eff): `believedMarketLines(worldState, settlementId, {includeGroundTruth})` → one line per category the town holds beliefs about, composed from the LOCAL band's MOVEMENT word (existing) and the neighbour beliefs' band WORDS via `commercialBeliefScarcity.js` (never a coin figure — §776; never a digit — prose-numerics); `EconomicsTab.jsx` (**596/600**: the render is ≤3 effective lines — one map over the lines inside the existing market section — or an `EconomicsMarketBeliefs.jsx` leaf is extracted FIRST and the tab gains ONE line; measure with `eff-any.mjs` at the publishing commit); the `includeGroundTruth` DM toggle rides the existing DM-only plumbing of the tab (fail-closed upstream). Dark (flag absent or omniscient) ⇒ the section renders nothing new (byte-identical DOM on the existing `tests/components` snapshots).

**Pins:** the line's words come from `SCARCITY_BANDS` verbatim (a closed vocabulary — `tests/copy` bans a float); the omniscient/dark twin renders nothing; the DM toggle shows both when set; prose-numerics: the walker `tests/lint/proseNumerics.test.js` green with NO new debt row (bands and words). **Files:** `marketPrices.js`, `EconomicsTab.jsx` (or the extracted leaf), `tests/domain/marketPrices.beliefs.test.js`, `tests/components/economicsTab.beliefs.test.jsx`. **Census bills:** wiring census `--check` (the display line is not a composed-prose desk — expected untouched; stamp if it reds); prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted — `marketPrices.js` and the tab are LAZY display; closure ±0 (assert: the tab is not in the first-paint table); engine ±0. **Gate:** full list + `tests/components`. **LIT act:** none. **Exit:** the line renders under the lit fixture, nothing dark; 596/600 respected or the extraction landed.

### Car 5 — THE FLAG-MINT / LIT CAR: `believedMarketsEnabled` lit in the four lit presets; the fog gate declared

**In plain words:** believed markets are switched on for worlds born into the four living presets; because beliefs only exist in the two presets where news travels by road, the lane says plainly that the layer moves worlds in `living_realm` and `full_simulation` and is inert by construction in the two omniscient presets.

**The change (PROTO §1.2 — the mint half landed in car 3; this car is items 2, 3, 10–12):** `SIM_LIT` gains `believedMarketsEnabled: true, // TR-3 — lit at §932 lane 7 car 5`; the conjuncts `beliefAxesEnabled` + `believedScarcityEnabled` — CHECK-GIT-FIRST: EXPECTED already in `SIM_LIT` from lane 2 car 7 (or lane 3); if ABSENT, THIS car lights them and re-charters `believedWorldAxesDormancyFence.test.js`'s host + scarcity arms exactly as lane 2's brief specifies (its car 7 step 4), and the lane's report says which case it hit; `casusCommerciiEnabled` lit at car 1. The register comment's LIT paragraph names the four presets and the FOG GATE: "belief maps exist only under `infoMode !== 'omniscient'` (`beliefMap.js:204-208`): living_realm (`perfect_delayed`) and full_simulation (`full`); in realistic_regional and dramatic_campaign the composer is never consulted and the key is inert by construction". `tests/property/believedMarketsDormancyFence.test.js` (NEW in car 3, dark shape) → LIT shape (the site census: `commercialBeliefScarcity.js` the ONE `=== true`; fence 3's lit twin under `full_simulation` MOVES the composer's call count; under `realistic_regional` lit it stays ZERO — the fog-gate proof; truthy-non-true probe; no 64-hex literal). The witness re-recorded by hand: `rulesSha256`/`litFlagCount` on five rows; `worldStateSha256` may move ONLY on `living_realm`/`full_simulation` and only if the born worlds carry a spatial canon with ≥2 trading towns (PLAUSIBLE they do not — say so honestly); a moved omniscient-preset world hash from THIS key is a STOP. The GOLDEN_SHIFT_LEDGER draft with the DECLARED-SHIFT sentence naming the fog gate. Census literals re-measured out of band; `contributionLedgerShape.test.js` re-read off HEAD; the edge bundles rebuilt; TR-3's row `other` amended for LIT; the espionage fence 21/21 and the prose-manifest zero movement quoted.

**Files (≈8):** `simulationRules.js`, the fence, the witness golden, `coveringArrayCoverage.test.js` (if moved), `subsystemRowsTrade.js` (text), `docs/GOLDEN_SHIFT_LEDGER.md` (dock), `contributionLedgerShape.test.js` (only if HEAD moved), + `believedWorldAxesDormancyFence.test.js` only in the ABSENT case. **Census bills:** refreeze own commit after; manifest rationale/plant for the fence; the witness; the shift draft. **Byte rule:** build + verify:dist; closure delta of the `SIM_LIT` line(s) in bytes. **Gate:** the full list + the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence, the soak suites alone at load < 8. **Exit:** `ENGINE_GATED_DORMANT_RULE_KEYS` lacks the key (measured); the fence green in LIT shape with the fog-gate twin; the witness diff quoted; the shift draft written.

### Repair allowance (the sixth car)
TR-1, SP-B and GR-2 each took a repair commit; the kernel's two return paths and `EconomicsTab.jsx`'s four lines of slack are the likeliest spenders. Budget it.

---

## THE FLAG-MINT CAR (last; the count from HEAD)

`casusCommerciiEnabled` is already minted — car 1 is a LIGHTING-only act (no literal moves). `believedMarketsEnabled`: MINT at car 3 (dark; the literals read off HEAD immediately before editing — `node -e` on `ENGINE_GATED_VIRTUAL_RULE_KEYS.length` and `VIRTUAL_SUBSYSTEM_ROWS.length`, both + 1; NEVER inherited — by the time this lane boards, lanes 1–3 may have moved 35 to 38 or 39), LIGHTING at car 5, REBASED at landing; conflicts on the register tail, `SIM_LIT`, the two literals, the census figures and `subsystemRowsTrade.js` are EXPECTED and resolved by MEASUREMENT (PROTO §5.2).

## OWNER-GATED, KEPT OUT

- Tuning (belief update weights per writer class, decay half-life, `DESTINATION_WEIGHT`, staleness bands, `SCARCITY_ADOPT` — SP-B's, untouched) — raw + derivation, ratified at the soak.
- `tradeWar.js`'s truth-scored contest — DECLARED OUT (T9); no edit.
- Lighting `infoMode` on `realistic_regional`/`dramatic_campaign` (a preset IDENTITY axis, a RULE_COMPARISON_KEY — re-labels installed campaigns) — never; the fog gate is declared, not cured.
- Any `beliefMap.js` byte (781/800, net-zero law); any `pulseKernel.js` byte; ceiling raises; push/deploy/migration/deletion/spend; the golden master.
- Persisted shapes widened: NONE — recorded.

## RISKS

**Grep traps (verbatim from RECHECK §5 + HISTORY):** `INSTR-1≠TR-1` (17 of the ledger's 20 `TR-1` hits are INSTR-1); `cornering` is TR-1's reason name; `market` in ODQ §87/§576/§629/§632 is the map programme; `scarcity` hits in `demographicsWar.js` (`perceivedScarcityOf`) are the WAR-motive read, not SP-B; `riskToleranceOf` in `roads/state.js:319` is NOT the SP-4 posture read; `commodityBand` lives in `spatial/commodityFlow.js` AND is re-derived in `display/marketPrices.js:143` (`commodityBandForGood`) — the composer imports NEITHER; `contributionLedgerShape.test.js`'s title says "at 28".

**Collision surfaces:** `settlementLifecycleKernel.js` + `pactKernelMount.test.js` (this lane only; GR-4's mount ruling is on `pulseKernel.js`, not here); `beliefAxisSubjects.js`/`beliefAxes.js` (SERIAL TR-3 → HB-6 → HB-7; IN-2 never beside); `supplyKernel.js`/`dispatchEV.js` (after lane 1 car 3; before TR-6/WY-3/WY-6); `EconomicsTab.jsx` (W-COIN-4, lane B — CHECK-GIT-FIRST; 4 lines of slack shared); the belief-spine `SIM_LIT` lines (lanes 2 and 3 — the first-composed wins); the flag-mint surfaces; `subsystemRowsTrade.js`.

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls (no printed count = did not run); dist-gated size suites skip without a build; `deno.lock` re-freeze; `git grep` reads the INDEX; the soak suites under load — alone at < 8; a moved DRIVEN GOLDEN (the SP-B fixture golden, `intelTrade`, `informationStatecraft`, `corruptionWeb`) is a STOP by its own law.

**Design hazards:** the vote — a stage writing under a `false` vote is silently discarded at `applyPulseMover` (GR-2's repair round found exactly this); the two return paths of the kernel must BOTH carry the new stage's world and vote; the composer must never import a truth-band read (the K3 import pin) and the no-merge scan's whitelist must name the corner composer BY NAME before TR-6 exists; `beliefMap.js` net-zero; `EconomicsTab.jsx` 596/600 — extraction first if the line costs more than the slack; the 6/16 declaration must be RAISED by each producer lane (a stale "6/16" after TR-5 lands is a record≠code row in the making — name the owner of the update: the producer lane).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file; the wiring census `--check` (car 4); prose-numerics (car 4); the edge bundles at cars 1, 3 and 5; the Seat/Lane trailer; porcelain 0. At cars 1 and 5 additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence 21/21, the soak suites alone at load < 8; at car 4 additionally `tests/components`.
