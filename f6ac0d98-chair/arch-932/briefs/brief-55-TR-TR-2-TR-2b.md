# THE LANE — PRICING §C lane 55 · TR-2 THE HOUSE (`merchantHousesEnabled`) + TR-2b THE SYNDICATE (reserved shape, rides dark); ⚠ boards BEFORE lane 8 (TR-6)

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED / PLAUSIBLE as marked; line numbers rot — re-measure by SYMBOL. Protocols: `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 6 (0–5), range 5–7** (PRICING §B 4–6; HISTORY §A.4 4–6 "re-derive house books against W-COIN's coin before drafting"; +1 here for the LIT car). **Boarding:** PRICING §C parks TR-2 at lane 55 "⏳ SP-4a constitution amendment (SPINE, not TR) or Req-13 struck by chair"; lane 8's brief (TR-6) shows the corner CANNOT board before TR-2 (`tradeConvergenceContract.js:337-352` requires `merchantHousesEnabled`). **Ruling (chair under the 14:4x grant, vetoable, veto window open until car 0 lands): the SP-4a amendment is TAKEN by this lane's car 0 — `POSTURE_ACTOR_KINDS` widens to `['house', 'settlement']`, recorded at the constitution (`docs/DESIGN_FP_SPINE.md` SP-4 seam 9, dock copy) exactly as its own text demands ("widening this set is an explicit SP-4a amendment at the constitution, never a volume-local mint", `strategicPosture.js:90-96`) — and TR-2 boards as soon as lane 7 (TR-1 mount → TR-3) is on the consist, AHEAD of lane 8.** A refusal falls back to PRICING's alternative (Req 13 struck for TR-2: acts priced by the OWNING SETTLEMENT's posture as a declared degraded read) — car 0 is written so either outcome is one edit.

**Prerequisites:** TR-1 mounted and lit (lane 7 car 1 — `casusCommerciiEnabled` is the conjunct); TR-3 for house MOTIVES (believed bands) — car 2 reads `commercialBeliefScarcity.js` (lane 7 car 3); SP-C landed (`strategicPosture.js`); W-COIN's `treasury.js` READ for the re-derivation (no edit). OWNER-QUESTIONS item 18(a) recommends "TR-2b rides TR-2's lane, dark" — adopted: TR-2b lands as RESERVED SHAPE in car 3's docblock and the contract's note; no key, no code, no lighting (there is nothing built to light — the owner's sequencing question stays open).

**Shared files this lane declares, slack at the cut:** `src/domain/worldPulse/strategicPosture.js` 136/800 (+≤12, car 0/2) · `tests/domain/strategicPosture.test.js` (`:137-145` the closed-set pin) · `src/domain/factionArchetypes.js` 73/800 — ⛔ IN THE GENERATION WORKER GRAPH (`worker-graph.txt:58`): READ ONLY, never edited · `src/lib/spatialUsage.js` 185/800 (the `houses` ledger-key classification, car 1) + `tests/lib/spatialUsage.test.js` + `tests/lib/spatialLedgerCoverage.walker.test.js` · `src/domain/worldPulse/commercialReceiptPools.js` 386/800 (car 3; ~4 kinds → ~460) · `tests/lint/couplingInclusion.walker.test.js` (add `merchantHouse[A-Z]`? — NO: `merchant` is ALREADY a TRADE stem (`:180`), so the leaves are named `merchantHouseLedger.js`, `merchantHouseActs.js`, `merchantHouseFactor.js`, `merchantHouseNews.js` and no pattern edit is needed) · the flag-mint surfaces + `SIM_LIT` + `subsystemRowsTrade.js`. NOT touched: `factionCompetition.js` 775/800 (its `inferFactionArchetype` is a SIBLING detector — TR-2 reads `factionArchetype()` from `factionArchetypes.js`, the canonical one), `generosityKernel.js` 800/800 (the `relief_grant` verb CALLS its exports).

---

## THE STATE OF THE RECORD

- **The history fold** (`…/recon-32-34/HISTORY-TRADE-2026-09-15.md`; `HISTORY-TR-…` does not exist under that name): TR-2 **UNBUILT** — "No house exists as an actor with books under any name. `houseLedger/houseActs/houseFactorCasting/houseNews` absent; `spatialLedgers.houses`, `houseBooks`, `MAX_HOUSES_PER_SETTLEMENT`, `booksBandOf` = 0 hits; faction wealth is the word high/medium/low (`factionProfile.js:161-217`)". Two FALSE POSITIVES discounted: the T8 "trades train" (`64c344615`, a generator TAXONOMY car: ~240 "Merchant Guilds" nameplates, no simulation body) and `brokerageServices.js:457 houseId` (INFO's news brokerage). Finding (e): the volume's "no money" premise is FALSE since W-COIN (`treasury.js` — conserved integer coin, STATE money). **THE RE-DERIVATION (ruled, vetoable):** house BOOKS stay BANDED words (holdings/credit ladders — the faction `wealth` word's grain, `factionProfile.js`) and are NOT coin-denominated: (i) the treasury is the STATE's stock with ONE writer (`advanceTreasury`) and a conservation walker (`treasuryConservation.walker.test.js`) — a house coin stock would be a SECOND conserved money family (a new persisted stock family + a second walker), which THE PROMISE and the §776 price-heuristics law both weigh against; (ii) the volume's acts deposit into EXISTING machinery (dispatch warrants, the credit lever `LEVER_NUDGE` `settlementStrategy.js:198`, the plan grammar, SP-3 triggers, generosity EV — all band-priced today); (iii) the treasury's `coinOf`/`treasuryBandOf` (`treasury.js:647/:538`) may be READ by a house act as a COLOUR of the seat's means (the forced-sale arm in TR-6) — a read, never a house write. Recorded in car 1's docblock and the TR volume (dock copy).
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §3 (the `spatialLedgers.houses` shape: `{ [factionId]: { books: { holdings, credit, interests[] }, appetite, credibility, updatedTick, dormantSince? } }`; the lifecycle clause: "regenerate: books are event-accrued, NOT re-derivable — regen PRESERVES books (the J-TR-3 disclosed exception, second instance of J-WR-3's idiom)"; "a load-time normalizer LANDS WITH THE WRITER"; single-writer census + third-writer plant), §4 TR-2 (four leaves; the pins; "Reads eligibility ONLY via `factionArchetype()` (S26 — rename-sensitivity is real)"; Req 13 "ENGAGED, thin — acts are posture-priced (SP-4a house instance)"), §5b seams 8 (INT-1 seatBooks) and 9 (FAITH reads house bands READ-ONLY). The design law `docs/DESIGN_FP_TRADE.md` §5 TR-2 (`:533-…`: the closed verb set `{sponsor_caravan, take_route_interest, extend_credit, corner_attempt (TR-6), venture_stake (TR-7), petition_pact (TR-5), relief_grant}`; FORMATION / THE RUIN LATCH / ELIGIBILITY LOSS → DORMANT-WITH-BOOKS; "This formation rule is also the BUILDER for entry wherever this program names entry as a counterforce"; T-4 TELLABLE). Compiled programme §5 #24 (line 1595). The dependency clause `DESIGN_FP_TRADE.md:266` `TR-2 ⇒ TR-1;` → `tradeConvergenceContract.js:313-320`. `docs/DESIGN_FP_SPINE.md:163-168` SP-4 (the closed actor set; "an explicit SP-4a amendment HERE").
- **The owner's laws:** built lit; tuning raw (holdings/credit ladders, act thresholds, `MAX_HOUSES_PER_SETTLEMENT`, the ruin-cooldown latch, SP-4a/SP-5 instance rates) ratified at the soak; a persisted-shape widening is TAKEN and recorded (`spatialLedgers.houses` — THIS lane's one new persisted record family; every world is test data; a veto is a code revert); nothing pushed/deployed/applied/deleted/spent.

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/strategicPosture.js` | 136/800 | `POSTURE_ACTOR_KINDS = Object.freeze(['settlement'])` `:97`; `assertActor` throws on any other kind `:151-163` ("widening it is an SP-4a amendment at the constitution"); `courtPostureOf(actorRef, …)` / `courtRiskAppetiteOf` — a live caller at `pactFormation.js:110`; the header `:36` "THE RULER'S BOOKS are unbuilt (`grep seatBooksEnabled src` → zero hits)" |
| `tests/domain/strategicPosture.test.js` | — | `:137-145` "the closed actor set REDS rather than guessing": `expect(POSTURE_ACTOR_KINDS).toEqual(['settlement'])`; `read({ kind: 'house', id: 'h1' })` throws `/closed/` — car 0 re-points BOTH |
| `src/domain/factionArchetypes.js` | 73/800 (worker graph) | `factionArchetype(faction)` `:103` — category-first, then the MERCHANT name regex `:81` (`/merchant\|trade\|caravan\|bazaar\|market\|broker\|oligarch\|bank\|guild/i`) — the rename-sensitivity is REAL (a DM rename that drops every keyword loses the archetype) |
| `src/domain/factionProfile.js` | 146/800 | `ARCHETYPE_TEMPLATES.merchant.resources.wealth: 'high'` `:178-184` — the faction wealth WORD |
| `src/domain/worldPulse/scoringObjective.js` | 50/800 | `MERCHANT_OBJECTIVE` `:72` (S26 substrate) |
| `src/domain/worldPulse/settlementStrategy.js` | 760/800 | `LEVER_NUDGE = 0.05` `:198` (the credit lever the `extend_credit` act deposits into) |
| `src/lib/spatialUsage.js` | 185/800 | `TRACKED_LEDGER_KEYS` `:238-…` / `EXEMPT_LEDGER_KEYS` `:334-…` — every `spatialLedgers.<key>` writer must be classified or `tests/lib/spatialLedgerCoverage.walker.test.js` reds; the GR-2 `pactProposals` TRACKED paragraph `:254-262` is the form for a flag-gated ledger with no tracked flag |
| `src/domain/spatial/distanceRead.js` | — | `getSpatialLedger`/`setSpatialLedger`/`dropSpatialLedger` `:166/:180` (S43 — conditional materialisation) |
| `src/domain/worldPulse/treasury.js` | 438/800 | `treasuryActive` `:595`, `coinOf` `:647`, `treasuryBandOf` `:538`, `TREASURY_BANDS` `:528` — READ for the seat's means colour; never written by TR |
| `src/domain/worldPulse/commercialBeliefScarcity.js` (lane 7 car 3) | — | `believedDearness01Of` — every house MOTIVE reads believed bands (TR-3) |
| `src/domain/worldPulse/generosityKernel.js` | 800/800 | the relief/purchase lanes the `relief_grant` verb deposits into — CALL its exports, ZERO lines |
| `src/domain/worldPulse/npcLadderKernel.js` / `npcBank.js` | — | the ladder/standing planes the factor is CAST from (read-wired; codepoint tie-break) |
| `src/domain/certification/tradeConvergenceContract.js` | 398 | TR-2's row `:313-320` (`requiresTradeFlags: ['casusCommerciiEnabled']`); the `syndicateHousesEnabled` note (TR-2b reserved) |
| ABSENT | — | every house leaf; `merchantHousesEnabled`, `syndicateHousesEnabled` (zero src hits); `spatialLedgers.houses`; `MAX_HOUSES_PER_SETTLEMENT` |

**Budgets (PROTO §4.1):** worker EXACT — `factionArchetypes.js` READ ONLY (the worker `stat` must not move; a natural-looking "tidy the regex" edit is the trap); `engine` ±0; first-paint: the mint car's `SIM_LIT` line, quoted. `generosityKernel.js` 800/800 — ZERO lines.

## ALREADY DONE (do not re-land)

The canonical archetype detector (`factionArchetype()`), `MERCHANT_OBJECTIVE`, the credit lever, the generosity relief/purchase lanes, the plan grammar (`demographicsPlans.js` — its `:21` fence forbids generalising ITS writer; TR-7 re-implements the grammar, TR-2 only DEPOSITS into it), SP-C's posture read with a live caller, the T8 faction nameplates (generator-side — the ~240 "Merchant Guilds" are the ELIGIBLE POPULATION this lane's formation rule reads, never edits), W-COIN's treasury (read).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-2`; PACKET caps.

### Car 0 — THE SP-4a AMENDMENT: the posture read admits a `house` actor (recorded at the constitution)

**In plain words:** the engine's "how bold is this court" reader only knows settlements; the constitution says widening that list is a constitutional act, so this car performs it in the open — a house becomes a legal actor whose boldness can be read — with the house's own books arriving in car 2 and a declared stand-in until then.

**The change.** `strategicPosture.js` (+≤12): `POSTURE_ACTOR_KINDS = Object.freeze(['house', 'settlement'])` (codepoint order); `courtPostureOf({kind:'house', id})` / `courtRiskAppetiteOf` gain a house arm: UNTIL car 2, the arm resolves the house's OWNING SETTLEMENT (the faction's settlement id — the `houses` ledger does not exist yet, so the arm reads `{kind:'house', id, settlementId}` and delegates to the settlement read, receipt naming the delegation — the DECLARED DEGRADED ARM, byte-identical for every existing caller since none passes a house). `tests/domain/strategicPosture.test.js:137-145` re-pointed: the closed set is `['house','settlement']`; `temple` still throws; a house ref WITHOUT `settlementId` throws (the degraded arm needs it). `docs/DESIGN_FP_SPINE.md` (dock copy) SP-4 seam 9: the amendment recorded as a dated bracket ("SP-4a AMENDED 2026-09-15 (§932 lane 55 car 0, chair under the 14:4x grant, vetoable): the closed actor set is {house, settlement}; the house instance's books land at TR-2 car 2; the temple instance stays FAITH's to amend"); `docs/DESIGN_FP_ARCH_TR.md` §4 TR-2 (dock copy) records the same. If the owner VETOES: revert the code lines and switch the volume's Req-13 line to "STRUCK for TR-2: acts priced by the owning settlement's posture" — one edit, and cars 1–5 are unchanged.

**Pins / negatives:** the two reads on a house ref return the settlement's posture with the delegation receipt; the fence `tests/property/strategicPostureDormancyFence.test.js` green unchanged (SP-C's key is not read here); mutant: `settlementId` dropped from the house arm → the throw pin reds. **Files:** `strategicPosture.js`, its test, the two docs. **Census bills:** manifest; refreeze own commit. **Byte rule:** build quoted; worker unchanged; engine ±0. **Gate:** full list. **LIT act:** none. **Exit:** the closed set is two; the docs record it; the veto path is one edit.

### Car 1 — `merchantHouseLedger.js`: THE ONE WRITER — formation, the ruin latch, dormant-with-books, the load-time normalizer; `spatialLedgers.houses` TAKEN

**In plain words:** a merchant or guild faction with a working commercial institution gains a set of BOOKS — banded holdings, banded credit, a list of interests — created by one rule, closed by ruin with a cooling-off period, frozen (never deleted) when the faction stops qualifying, and repaired on load from any older save.

**The change.** `src/domain/worldPulse/merchantHouseLedger.js` (≤250 eff; TRADE pattern `merchant`): `merchantHousesActive(rules)` — THE ONE by-name read `rules.merchantHousesEnabled === true`; `HOUSE_TUNING` RAW with derivations (`MAX_HOUSES_PER_SETTLEMENT`, `HOLDINGS_BANDS`/`CREDIT_BANDS` ladders — BORROWED BY SPELLING where a ladder exists (the SP-A `bandFamilies` law J-FP-8: borrow before minting; the faction `wealth` words `low/medium/high` are too coarse — mint a five-rung ladder and RECORD why nothing existed), `RUIN_COOLDOWN_SEASONS` INTERVAL_WEEKS-derived, the decay-toward-prosperity rate); `eligibleHouseFactions(settlement)` — `factionArchetype(f) === 'merchant'` (imported READ from the worker-graph file) AND a live commercial institution (the `institutionServices`/category read — by symbol at charter); FORMATION — deterministic: rank by institution seniority, factionId codepoint tie-break, under the cap; THE RUIN LATCH — `books` collapse ⇒ entry closed + `ruinedAtTick`, re-eligible only after the cooldown, re-entry a NEW entry with a lineage receipt; ELIGIBILITY LOSS ⇒ `dormantSince` (books frozen, no acts, receipt), restoration wakes the SAME entry; dissolution closes through the writer; `advanceMerchantHouses({snapshot, worldState, tick, rules})` the ONE writer (dark ⇒ the world's own reference; lit-but-empty ⇒ `dropSpatialLedger`); `normalizeMerchantHouses(raw)` the LOAD-TIME NORMALIZER (the dormant-with-books shape round-trips from any save; members ARE `npcs[]` ids — JSON-alias round-trip). The shape (`docs/DESIGN_FP_ARCH_TR.md` §3) as the closed key set; conditional fields drop-when-absent (T4: an empty array is a key and a key is a byte). **THE PERSISTED SHAPE IS TAKEN AND RECORDED:** `spatialLedgers.houses` — classified in `src/lib/spatialUsage.js` as TRACKED (the GR-2 `pactProposals` form: a flag-gated ledger whose flag is not in `TRACKED_FLAGS` and whose activity no tracked mover signals) with its paragraph, `tests/lib/spatialUsage.test.js` gains the row, the coverage walker green; the wizard's OSR walker (`observedShapeReaders.walker`) — a NEW persisted shape is exactly what #20's ONE trailing mint must swallow: the lane's report NAMES `spatialLedgers.houses` for the #20 batch (PRICING §D). THE SINGLE-WRITER CENSUS: `tests/lint/merchantHouseWriters.walker.test.js` (the T4/storageMonths shape) — `setSpatialLedger(…, 'houses'` in exactly `merchantHouseLedger.js`; third-writer plant executed.

**Pins / negatives:** formation determinism (two eligible, one slot — codepoint decides, same-seed identical); the cap binds; ruin reachable from the TOP band on a real fixture (T5 reversal); no-undead-house (a ruined house's still-eligible faction does NOT re-open inside the cooldown); rename round-trip (rename the faction to break the regex → dormant-with-books; JSON round-trip; restore → the SAME entry wakes; never orphaned); JSON-alias round-trip; dark identity (the writer returns the world's reference); regen PRESERVES books (the regen path leaves `spatialLedgers.houses` untouched — the J-TR-3 exception, pinned and RECORDED in the row's `other`); undo/import round-trip through the normalizer. Mutants: the cooldown removed → the undead pin reds; the third-writer plant → the census reds; the normalizer dropped → the old-save round-trip reds. **Files:** the leaf, `spatialUsage.js` (+paragraph), `tests/lib/spatialUsage.test.js`, the census walker, `tests/domain/merchantHouseLedger.test.js`, `docs/DESIGN_FP_ARCH_TR.md` (dock: the W-COIN re-derivation bracket). **Census bills:** manifest ×3; refreeze own commit. **Byte rule:** build quoted; worker unchanged (imports only); engine ±0. **Gate:** full list + `tests/lib/spatialLedgerCoverage.walker.test.js`, `tests/lint/observedShapeReaders.walker.test.js` (expected: no new READER of the shape yet — the OSR inventory is unchanged; a reader lands at car 2 and is REPORTED, not minted into the baseline: the OSR baseline is #20's one act). **LIT act:** none (`merchantHousesEnabled` read by name ⇒ THIS car pays the MINT half dark — PROTO §5.1 items 1, 4–8, 13–14; the row AUTHORED in `subsystemRowsTrade.js` with `stateKeys: ['spatialLedgers.houses']`; car 5 lights). **Exit:** the writer pinned on every lifecycle path (create · read · persist · regenerate · undo · import · migrate); the shape recorded for #20; the mint registered dark.

### Car 2 — `merchantHouseActs.js` (the threshold chooser over the closed verb set) + `merchantHouseFactor.js` (read-wired casting) + the posture house arm made real

**In plain words:** a house decides what to do — sponsor a caravan, take an interest in a road, extend credit, petition for a pact, grant relief — by comparing its books, its boldness and what it believes about the markets against thresholds; it never picks a victim and never owns a physics lane of its own, it only deposits into machinery that already exists; and the person who speaks for it is cast fresh from the roster each time, never stored.

**The change.** `merchantHouseActs.js` (≤250 eff): the CLOSED verb set `HOUSE_VERBS = ['sponsor_caravan', 'take_route_interest', 'extend_credit', 'petition_pact', 'relief_grant', 'corner_attempt', 'venture_stake']` (the last two RESERVED — `built: false` rows in a small registry the way `ERRAND_CONSUMERS` does it, flipped by TR-6/TR-7 in THEIR commits); `chooseHouseActs({house, books, appetite, believed, thresholds})` THRESHOLD-style (T4: books + appetite + believed bands colour; NO relationship graph, NO target lists — the no-hidden-governor IMPORT PIN: the module imports no `relationship*`/`graph` module; guard-the-guard: a planted graph import reds the scan); execution DEPOSITS: `sponsor_caravan` → a dispatch warrant into `commodityFlow`'s dispatch (the `smuggleDispatchWarrant` shape's legal twin — by symbol), `take_route_interest` → an `interests[]` row, `extend_credit` → the credit lever (`LEVER_NUDGE`, by CALL into `settlementStrategy.js`'s export — 760/800, ZERO lines there), `petition_pact` → an SP-3 trigger deposit (`pactProposals.openPactProposal`'s road — GRAMMAR's, by call), `relief_grant` → the generosity EV lane by call. `merchantHouseFactor.js` (≤120 eff): `castHouseFactor(house, roster)` — deterministically the house faction's highest-ranked member scored by the EXISTING planes (temperament W0/H3, goals, mercantile facets; codepoint tie-break), never stored, never new NPC state; rivalries read the existing rivalry plane. The posture house arm: `courtPostureOf({kind:'house', id})` now reads the house's `appetite`/`credibility` from the ledger (car 0's degraded delegation retired; its pin re-pointed). Req 13 ENGAGED, thin: acts are posture-priced; no alignment GATE anywhere (T6). Cross-layer reads (TRADE→GRAMMAR for `petition_pact`, TRADE→INTERIOR for the lever) → CW-0 coupling rows in `couplingRegistryTrade.js` in the SAME commit.

**Pins / negatives:** the import pin + guard-the-guard (positive control at a legitimate truth reader — `supplyKernel`); the no-micro-agent SOURCE SCAN over the diff (no per-NPC trade state anywhere); each verb deposits into its named machinery on a fixture and NOTHING else moves; casting determinism (same roster, same factor; codepoint on a tie); DM-KILL of the cast factor → the next cast picks another (never orphaned state — nothing was stored). Mutants: hidden-governor plant → scan reds; the cap on acts-per-tick removed → the tempo pin reds. **Files:** two leaves, `strategicPosture.js` (the real arm), `couplingRegistryTrade.js`, two tests, `tests/lint/merchantHouseGovernor.walker.test.js` (NEW). **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list. **LIT act:** none. **Exit:** every verb pinned; the governor scan reds its plant; the posture arm real.

### Car 3 — `merchantHouseNews.js` + the L6 five joins + own walker + T-4 TELLABLE; TR-2b RESERVED

**In plain words:** the Herald learns the house's sentences — "House Verren took an interest in the river road", "House Verren is broken; its factors scattered to the quays", "the Verren name trades again" — each with its pool, desk, address chain and a permanent walker; the story of a house that keeps its road when a demotion threatens it is pinned end to end; and the syndicate variant is written down as a reserved shape with nothing built.

**The change.** Kinds `commercial_house_formed` ('routine'), `commercial_house_interest` ('routine'), `commercial_house_ruined` ('notable'), `commercial_house_revived` ('routine'), `commercial_house_dormant` ('routine') — registry leaf, pool rows in `commercialReceiptPools.js` (386 → ~480) mirroring `docs/content/RECEIPT_POOLS_TRADE.md` (the ONE reader), `WHAT_PHRASES`, `heraldRouting.js` `trade` desk, the address chain, `tests/lint/commercialHouseKindPools.walker.test.js` (NEW), `heraldContaminationFence` green, pacing-registered, the metronome law (the ruin/revival emitter names its latch). T-4 TELLABLE (`tests/domain/merchantHouseTellables.test.js`): a house whose route interest is threatened by a route demotion event (J1's `route_demoted` — dark behind `routeLifecycleEnabled: false`; the fixture drives the demotion RECORD directly) petitions/patrols (act receipts) — "the house that keeps its road" on one fixture. **TR-2b:** the `merchantHouseLedger.js` docblock and `tradeConvergenceContract.js`'s note record "TR-2b THE SYNDICATE — RESERVED SHAPE (ODQ `fdf437907`, OWNER-QUESTIONS item 18a): rides TR-2's lane as vocabulary only; `syndicateHousesEnabled` is minted by NO lane until the owner sequences it; nothing here is built to light" — no key, no code, no lighting. **Files:** ≈8. **Census bills:** wiring census stamp; prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + the kind walkers, `heraldContaminationFence`, `proseWiringCensus.walker`, `metronomeCooldownLint`. **LIT act:** none. **Exit:** five kinds × five joins; T-4 pinned; TR-2b recorded.

### Car 4 — the read surfaces toward the neighbours: `booksBandOf` (FAITH reads, seam 9), the INT-1 seatBooks absence pin (seam 8), the dossier house entry (ONE user-facing surface)

**In plain words:** other programmes get exactly the read they were promised — FAITH's future tithe may read a house's wealth band and nothing else — the interior programme's ruler's-books colour is recorded as absent-not-zero until it exists, and the town page's power section gains one line per house with its books in words.

**The change.** `merchantHouseLedger.js` exports `booksBandOf(worldState, factionId)` → band words (READ-ONLY; no stream-physics API for external consumers, ever — the fp-audit's ownership ruling); `tests/lint/merchantHouseReaders.walker.test.js` — a consumer import census on `merchantHouseLedger.js` admitting FAITH's executor as READER only when it lands (a writer import reds). The INT-1 pre-pin (seam 8): an absence-then-presence pair — `seatBooksEnabled` has zero src hits (`strategicPosture.js:36` says so) → the house appetite's `booksOf` colour term is ABSENT (the pin asserts the ledger carries no `seatBooksColour` key), and the twin reds the day `seatBooksEnabled` appears in `simulationRules.js` without the colour landing (anchored on `strategicPostureEnabled` being present). The dossier: the town page's power/factions section gains "House Verren — holdings: deep; credit: sound; interests: the river road" (bands and words) — locate the factions panel component at charter (`src/components/new/tabs/*.jsx` — measure its slack FIRST; extract if under 10 lines); DM-only fields none (books are public words). **Files:** the leaf (+≤15), the reader walker, the seat-books pin test, the tab or its leaf, a component test. **Census bills:** wiring census `--check`; prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted (lazy display; closure ±0 asserted). **Gate:** full list + `tests/components`. **LIT act:** none. **Exit:** the read surface pinned; the seam-8 pair; the line renders lit, nothing dark.

### Car 5 — THE FLAG-MINT / LIT CAR: `merchantHousesEnabled` lit in the four lit presets

**In plain words:** merchant houses are switched on for every world born into the four living presets; because eligible factions are authored at generation, houses form in all four — a same-seed shift for born worlds that the preset fingerprint golden records by hand with the movers named.

**The change (PROTO §1.2 — the mint half landed in car 1; this car is items 2, 3, 10–12):** `SIM_LIT` gains `merchantHousesEnabled: true, // TR-2 car 5`; the conjunct `casusCommerciiEnabled` — EXPECTED lit (lane 7 car 1); if not, STOP boarding (lane 7 is a prerequisite). The register comment's LIT paragraph. `tests/property/merchantHousesDormancyFence.test.js` (NEW at car 1, dark shape) → LIT shape (site census: `merchantHouseLedger.js` the ONE `=== true`; fence 3's lit twin under `full_simulation` MOVES the writer's count; the truthy probe; no 64-hex literal). The witness re-recorded BY HAND: `rulesSha256`/`litFlagCount` on five rows; `worldStateSha256`/`settlementUpdatesSha256`/`wizardNewsSha256` EXPECTED to move on ALL FOUR lit rows (a born world with a merchant faction and a commercial institution forms a house on tick 1 — the formation receipt is `wizard_news`); a moved DARK row is a STOP. The shift-ledger draft (PROTO §2.5) with the DECLARED-SHIFT sentence ("`merchantHousesEnabled` lit on the four presets at landing `<sha>`; `spatialLedgers.houses` TAKEN (the #20 batch); houses form in <n> of the 8 witness rows; the generation arm 525 of 525 bit-identical (the formation reads generator output, never writes it); …"). Census literals out of band; `contributionLedgerShape` re-read off HEAD; the edge bundles; TR-2's row text for LIT; the espionage fence 21/21 and prose cells zero quoted.

**Files (≈8):** `simulationRules.js`, the fence, the witness golden, `coveringArrayCoverage.test.js` (if moved), `subsystemRowsTrade.js` (text), `docs/GOLDEN_SHIFT_LEDGER.md` (dock), `contributionLedgerShape.test.js` (only if HEAD moved). **Census bills:** refreeze own commit after; manifest for the fence; the witness; the shift draft. **Byte rule:** build + verify:dist; the `SIM_LIT` line in bytes. **Gate:** full list + the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence, the soak suites alone at load < 8. **Exit:** `ENGINE_GATED_DORMANT_RULE_KEYS` lacks the key; the fence LIT-shape green; the witness diff quoted; the shift draft.

### Repair allowance (the seventh car)
The normalizer (a lifecycle-path bug class — regen · undo · import) and the SP-4a veto path are the likeliest spenders. Budget it.

---

## THE FLAG-MINT CAR (last; the count from HEAD)

MINT at car 1 (dark; both literals read off HEAD immediately before editing, +1 — NEVER inherited: by this lane's boarding the register is well past 35), LIGHTING at car 5, REBASED at landing; conflicts on the register tail, `SIM_LIT`, the two literals, the census figures and `subsystemRowsTrade.js` EXPECTED and resolved by MEASUREMENT (PROTO §5.2).

## OWNER-GATED, KEPT OUT

- Tuning (`HOUSE_TUNING` entire) — raw + derivation, ratified at the soak.
- `syndicateHousesEnabled` / TR-2b's sequencing (OWNER-QUESTIONS 18a) — reserved shape only; nothing built.
- A coin-denominated house treasury — REFUSED by the re-derivation ruling (vetoable); books stay bands.
- `factionArchetypes.js` (worker graph) — READ ONLY; `generosityKernel.js` 800/800 and `settlementStrategy.js` 760/800 — by CALL; push/deploy/migration/deletion/spend; the golden master.
- Persisted shape TAKEN and recorded: `spatialLedgers.houses` (ONE new family; the #20 batch); no migration is written or applied — every world is test data; a veto is a code revert.

## RISKS

**Grep traps (verbatim):** the T8 "trades train" (`64c344615`) is a generator TAXONOMY car — ~240 "Merchant Guilds" nameplates are the eligible POPULATION, not a house; `houseId` in `brokerageServices.js` is INFO's news brokerage; `merchant_guild` is a faction-archetype responder key (`factionResponses.matchArchetype`); `inferFactionArchetype` in `factionCompetition.js` is a SIBLING detector — read `factionArchetype()` from `factionArchetypes.js` only; `syndicate` in tests is the undercity crime layer; `wealth` on a faction is a WORD; W-COIN is STATE money; `INSTR-1≠TR-1`.

**Collision surfaces:** `strategicPosture.js` (INT-1/INT-2 read the posture books — PROTO §7.1 "strategicPostureDormancyFence … INT-1/INT-2 (the books the posture reads)": CHECK-GIT-FIRST); `spatialUsage.js`'s key lists (every lane minting a ledger edits them — expected conflicts); `commercialReceiptPools.js` (TR-5/TR-6/TR-7 — the SPLIT rule near 800); the flag-mint surfaces; `subsystemRowsTrade.js`; the factions panel component (W-COIN-4's `EconomicsTab.jsx` is a different tab — verify the panel's file and slack).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls; dist-gated suites skip without a build; `deno.lock` re-freeze; `git grep` reads the INDEX; soak suites alone at < 8; a moved DRIVEN GOLDEN is a STOP.

**Design hazards:** the rename round-trip (a DM rename that drops every regex keyword must yield DORMANT-WITH-BOOKS, never a deletion); regen PRESERVES books (the disclosed J-TR-3 exception — pin the regen path); the normalizer lands WITH the writer (an old save without `houses` must load; a save with a dormant entry must round-trip); no hidden governor (the import pin is mandatory); the factor is CAST, never stored; `factionArchetypes.js` untouched.

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file; the wiring census stamp at cars 3 and 4; prose-numerics at cars 3 and 4; the edge bundles at cars 1 and 5; `tests/lib/spatialLedgerCoverage.walker.test.js` + `observedShapeReaders.walker` at car 1; the Seat/Lane trailer; porcelain 0. At car 5 additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence 21/21, the soak suites alone at load < 8; at car 4 additionally `tests/components`.
