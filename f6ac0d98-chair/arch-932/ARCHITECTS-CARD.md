# THE ARCHITECT'S CARD — ARCH-PROTOCOLS.md + ADDENDUM 1, distilled (the chair, Fable 5.1, 2026-09-15 23:2x EDT)

Read THIS instead of the 77 KB protocols; open `ARCH-PROTOCOLS.md` only at the section a car needs (§ numbers below point there). Every figure was CONFIRMED in the protocols at the consist tip `315080928` unless marked PLAUSIBLE; line numbers rot — a brief re-measures by symbol. `ARCH-PROTOCOLS-ADDENDUM-1.md` (R1–R5, the door train D0) is the authority wherever it and the protocols differ.

## 0. Paths
`$S` the consist `…/4e3d2f70…/scratchpad/kit/laneCONSIST-932` (the code); `$L` the ledger worktree `…/f6ac0d98…/scratchpad/ledger-a803dee7a` (the volumes `docs/DESIGN_FP_ARCH_<CODE>.md`, the ODQ); `$MY/arch-932/` (protocols, addendum, plans/, briefs/); `$MY/recon-32-34/` (PROGRAMME · RECHECK · PRICING · `HISTORY-<CODE>-2026-09-15.md` — a history fold, where it exists, WINS over PRICING §B's rows for that family).

## 1. THE LIT ACT (protocols §1; addendum R1–R3)
- Every #32/#33 flag is a VIRTUAL simulation-rule key: appended to `ENGINE_GATED_VIRTUAL_RULE_KEYS` in `src/domain/worldPulse/simulationRules.js` (35 members at HEAD, append at the tail, never deleted), ABSENT from `DEFAULT_SIMULATION_RULES` (a boolean there is a comparison key and re-labels installed campaigns `custom` — never), read by exactly ONE by-name strict gate `rules.<key> === true` wrapped as `<x>Active(rules)` in the family's door module (never a frozen-list `.every()`, never `!!`).
- LIT = declared `true` in the FOUR lit presets (`realistic_regional`, `dramatic_campaign`, `living_realm`, `full_simulation`) and absent from the three dark (`quiet_local`, `static_campaign`, `narrative_campaign`) THROUGH the `SIM_LIT` frozen cohort spread after `...ONE_REGEN` — never into `WAVES` (nine, pinned) or `ONE_REGEN`. The cohort is minted EMPTY by the door train D0 (addendum §3); no lane mints it or edits the virtuality walker `tests/lint/tradeConvergenceContract.walker.test.js:367` (D0 re-cuts it to R1: absent from defaults; in presets only via SIM_LIT; every SIM_LIT key registered with a row). L2 (`DESIGN_FP_ARCHITECTURE.md:92`) is amended by R1; a brief CITES the addendum by path at its LIT car, never re-derives it.
- CONJUNCTS LIGHT TOGETHER: `espionageEnabled` needs `errandSpineEnabled` (`espionageGate.js:80`); the SP-B subject keys need `beliefAxesEnabled`; `interventionEnabled`/`peaceEngineEnabled`/`supplyWebWarfareEnabled` need `warLayerEnabled`. The first lane on a spine pays for the spine. THE LIGHTING-ORDER LAW (R2): a LIT car lights nothing while the family's contract evaluator (`evaluateTradeFlagLighting`, `tradeConvergenceContract.js:493-525`, for trade) or its fences report a violation for ANY lit preset; every brief states its lighting preconditions in BOTH directions; every LIT car is built AT LANDING on the consist (rebased), never in the dock against the slot's dark presets (a dock-green LIT car is a FALSE GREEN).
- THE MINT/LIGHTING SPLIT (R3 as amended): the DARK-MINT car carries, in one commit, the register entry + the one gate read + the certification row (the family's TAIL leaf under `src/domain/certification/subsystemRows<Family>.js`; a NEW family's leaf is spread at the tail of `VIRTUAL_SUBSYSTEM_ROWS`; 25–40 effective lines; the evidence law: every `eventType` traceable to a `candidateType:` literal, every `stateKey` a v5-census container) + the three `tests/domain/subsystemRowsVirtual.test.js` edits (the key const; `VIRTUAL_RULES` member IN POSITION; `LANE_LEAVES` entry) + BOTH `tests/domain/contributionLedgerShape.test.js:118-119` literals read off HEAD at its base (35 → 36 for the first) + the `tests/soak-harness/coveringArrayCoverage.test.js:185-222` four figures re-measured + the two edge bundles + metas (any byte in `simulationRules.js` stales `supabase/functions/_shared/aiCharterBundle*` and `aiOutputSchemaBundle*`: clone immer+seedrandom with `cp -Rc`, `node scripts/build-edge-shared.mjs`, `grep -rl 'Users/cstokes' supabase/functions/_shared/` EMPTY, relink before any vite build). The LAST car (rebased at landing) carries the LIGHTING: the `SIM_LIT` line, the fence re-charter (§3), the preset-lighting witness re-record, the shift-ledger draft, the register comment saying LIT — and RE-READS every count off the composed HEAD. `VIRTUAL_PENDING_RULE_KEYS` is `[]` and shrink-only: a row is AUTHORED at the mint, never declared pending. A key leaving `BACKLOG_RULE_KEYS` (`engineGatedRuleKeys.walker.test.js:193-210`, ≤17) deletes its row and lowers the ceiling 17 → 16 in the same commit.
- A lit key that would be a FALSE CLAIM (nothing can reach the behaviour) lands DARK with the lighting priced to the producer car, said in the body. A key lit in a subset of presets needs a recorded reason in its comment (the `neutralNeighborsEnabled` form).

## 2. WHAT REDS WHEN A KEY LIGHTS, AND THE CURE (protocols §1.4)
`tests/simulation/presetLightingWitness.test.js` + `tests/fixtures/preset-lighting-witness-golden.json` (RE-RECORD BY HAND via `measureWitness()`; the row diff quoted; a field that moved on a DARK preset is a STOP) · the family's dormancy fence (`toContain(FLAG)` on `ENGINE_GATED_DORMANT_RULE_KEYS` and "declared in no preset" → re-chartered to the LIT form, §3) · `coveringArrayCoverage` (measure all four figures out of band first) · `contributionLedgerShape` · `subsystemRowsVirtual` · `engineGatedRuleKeys.walker` (three directions) · `subsystemCertificationTotality.walker` · the two edge freshness tests · the lighting census (`tests/lint/.lighting-census-baseline.json`: a new test file/title → the refreeze bill as its OWN commit on a clean tree) · `mutationCoverageManifest` (a new invariant test file → a `kind:'mutation'` entry with a planted regression in `scripts/mutation-sweep.sh`, or `kind:'rationale'`; never `uncovered`) · the preset-driven soaks (behavioural; a red at load > 8 is measured alone) · the test ratchet (the chair's totals car) · the generator golden master (525/525 RED; NEVER re-recorded; the classified diff is the claim) · the per-family dormancy GOLDENS drive LITERAL rules and do not move for a preset lighting (a move = a kernel change under a neighbour's key: find the mover, then re-record THROUGH THE SIGNED DOOR — ADDENDUM-1 R7: `UPDATE_GOLDEN=1` alone writes NOTHING since the 2026-09-16 genesis freeze; `GOLDEN_SHIFT_SIGNED=docs/shift-records/<record the chair cuts>.json` is the armament, and fixture + register + record commit together with an `Owner-Signed: §<row>` trailer).

## 3. FENCES (protocols §3)
The FOUR-FENCE dormancy set: fence 1 driven byte-identity dark (+ the anti-vacuity twin that drives the LIT preset and requires movement); fence 2 absent ≡ explicit false; fence 3 call-path count zero under dark (the lit twin requires it to MOVE under `full_simulation`); fence 4 gate-polarity census (every read `=== true`, + the truthy-non-true probe); the lit mutant = the lit preset; a site census of the modules that read the key. LIT FORM of the preset arm: "VIRTUAL (absent from DEFAULT_SIMULATION_RULES), REGISTERED, LIT in exactly the four lit presets, absent from the other three" — the roster spelled from `SIMULATION_RULE_PRESETS`, never a literal. Copy the LGT-P13 trio shape (`conquestDoctrine`, `faithUnseating`, `warCirculation` fences). ⛔ No 64-hex literal in a NEW fence (the freeze register is UNFROZEN); both sides computed in-file. WHO RE-CHARTERS: the lane whose car LIGHTS the key, in that car; a foreign lane that breaks a REACHABILITY arm (lane B on `missionDispatcher.js`'s importer set) pays that arm's own replacement instruction and leaves the dormant arms to the family lane. A lane that meets a red it did not cause STOPS and reports the mover. Family fences: TR `casusCommerciiDormancyFence` (lane 7) · SP-B `believedWorldAxesDormancyFence` ×3 (the first lane lighting the axes: TR-3, POP-1 or ES-7) · SP-D `errandSpineDormancyFence` (the first errand consumer: TR-8, WF-2, IN-4, ES-7, lane B) · ES `espionageDormancyFence` (fence 1 = the driven constant `PRE_COUPLING_CORPUS_SHA` + the reachability chain) and eight ES sibling fences (one lighting act over the whole conjunction) · WF `faithFieldDormancyFence` · `faithUnseatingDormancyFence` (lit shape) · HB `habitNeutralIdentity` · GR `oathHolder`/`pactFormation`(five-member namer set at `:198-214` — spelling `pactFormationEnabled` in code adds a sixth namer)/`treatyLifecycleVoice` · IN `secondOrderBelief`/`secrecyTrade` · WR-10 `sovereigntyTrade` (TR-5 touches `sovereigntyBundle.js`) · SP-C `strategicPosture` (INT-1/2) · WC `warCirculationDormancyFence` · POP `demographicsLifecycleGolden`; the byte-identity fences under `tests/domain/` (irregularForce, foreignSeat, legitimacyUpheaval, treasury, routeNetwork, …) keep their byte-identity arm and rewrite the preset-absence arm at lighting.

## 4. THE DECLARED-SHIFT RECEIPT (protocols §2)
- THE ESPIONAGE FENCE CONSTANT (`tests/property/espionageDormancyFence.test.js:263`, generator-side; an engine key cannot move it; generator text/structure/data can): run at tip → green = write the dated zero-movement block anyway; red → the anti-vacuity arms must have passed (360 rows, 0 errors, 360 distinct) → NAME THE MOVER by a settling experiment BEFORE landing (base + suspect cars alone reproduce the sha; count moved rows) → residue zero on every other train → the dated header block `── <date>, <WINDOW> (<old 8>… → <new 8>…) ──` with the scope line naming ALL remaining chartered windows ("THE BUILT-LIT WINDOW: one re-record per landing from §932 until the terminal soak") → replace the constant (one line) → the plain 21/21 re-run is the proof.
- THE PROSE-MANIFEST CELLS (`tests/fixtures/dossier-prose-manifest-golden.json`, 1,050 rows): `sh scripts/gate-mutex.sh --run -- node scripts/prose-manifest-cells.mjs --record` only when a cell or the recorder's own files move; the moved rows listed BY POOL KEY (`--out` at base and tip, `prose-manifest-diff.mjs`).
- THE REGISTERS AND THEIR RECORDERS: wiring census `node scripts/wiring-census.mjs` (any composer/prose byte) · lighting census refreeze (env-driven run that FAILS BY DESIGN; the plain 34/34 re-run is the proof; its own commit) · `scripts/mutation-coverage-manifest.json` by hand · `tests/lint/.prose-numerics-baseline.json` (shrink-only; a moved debt row re-keyed exactly) · `scripts/.writer-reach-baseline.json` (`check-writer-reach.mjs` dry then `--write`; the chair re-stamps at the consist) · the witness by hand · the capsule (the chair).
- THE CLASSIFIED DIFF (`$SC/kit/c931-f6ac0d98/declared/`: `__chair_dump.test.js`, `run-declared.sh`, `classify.py`): every `-`/`+` line matches a declared pattern anchored to the exact sentence/field family; never a bare `.*`; exit code is the gate; the extended classifier lives under the lane dir in `$MY`, never in the tree. For an engine key the generator dump is the ZERO-MOVEMENT receipt (`configs 525 · unchanged 525 · changed 0`).
- THE SHIFT-LEDGER ENTRY (`docs/GOLDEN_SHIFT_LEDGER.md`, append-only; the lane drafts its section under `docs/implementation/` in the dock): HEADLINE · why a shift not a repair · goldens re-recorded (file | assertions | before | after | cause) · non-golden registers moved · future-shift conditions · the DECLARED-SHIFT sentence ("<keys> lit on <presets> at landing <sha>; <hashes moved>; <what did not move>; no installed campaign re-labelled; no tuning value moved").

## 5. BUDGETS (protocols §4)
| Budget | Ceiling | Measured | Rule |
|---|---|---|---|
| generation worker `WORKER_BUNDLE_CEILING_BYTES` (`tests/build/generationWorkerLazy.test.js:78`, monotone-down) | 1,404,493 | EXACTLY 1,404,493 at the slot (0 B) | lane A0 lowers it (car 4 parked: −80,981 B); only generator-side code counts; no generator car adds a byte before A0 lands |
| lazy `engine` chunk (`vendorPdfLazy.test.js:746-769`) | < 676,000 | 675,099 at the slot (901 B) | every pulse-side leaf adds here — A0's car 3 (narrativeData.js out, ~36 KB) is the door; a car quotes the `engine-*.js` line from a real build |
| eager first-paint closure `CLOSURE_BUDGET_BYTES` (`vendorPdfLazy.test.js:565`) | 1,048,000 | CONTESTED: PRICING 1,047,205 (795 B) vs lane A0's plain-build 1,042,172 (5,828 B) — treat as UNMEASURED until the chair re-measures | each lit key costs ~30–60 B eager (PLAUSIBLE); INT-3a's `stressorsCore.js` 0.6–1.2 KB; a closure-headroom act (lane A0b) is owed before INT-3a and before ~the twentieth key |
| shared-file line ceilings (`tests/lint/sizeBaseline.test.js`; eslint max-lines 600 `.jsx` / 800 else; `scripts/.size-baseline.json` frozen at zero slack) | per file | §4.3 | EXTRACTION CAR FIRST (a whole stage body to a pure leaf, byte-neutral, its own tests, the baseline LOWERED), never a raise; PACKET_STANDARD: delta in a shared/hot file ≤ 15; a car that shrinks a baselined file lowers its number in the same car |
⛔ The dist-gated skip: both size suites skip silently without `dist/` and under-report on a stale one — every src car runs `sh scripts/gate-mutex.sh --run -- npm run build` then `npm run verify:dist` and quotes the worker stat, the engine line and the first-paint table.
AT ZERO SLACK: `pulseKernel.js` 1581 (GR-4/GR-6; ONE lane at a time; extraction first) · `roadsKernel.js` 838 (WY-3 car 1 extracts) · `applyWorldPulse.js` 907 (TR-4 car 0 CR-2 LOWERS it; lands before any other writer) · `warTermination.js` 818 (WF-1, GR-7) · `explanation.js` 827 · `App.jsx` 650 · `settlementSlice.js` 824 · `npcGenerator.js` 1345 · `generosityKernel.js` 800/800 (TR-4's grain credit is a NEW leaf) · `OutputContainer.jsx`/`EventComposer.jsx` 600. WITHIN 20: `WorldMap.jsx` 599 (WY-0 extracts a layer-manifest leaf) · `generationReceiptJudgments.js` 799 · `armyTransitKernel.js` 798 (WC-6 extraction first) · `institutionLifecycle.js` 798 (POP-5b) · `peaceTerms.js` 797 (TR-5 → WF-6 → GR-5, ZERO edits by TR-5) · `EconomicsTab.jsx` 596 · `behavioralContract.js` 796 · `npcAgency.js` 795 (HB, INT) · `economicState.js` 791 (TR-3/TR-6; worker graph) · `WorldMapToolbar.jsx` 590 · `HeraldCommandBody.jsx` 586 (IN-5, ES-7) · `realmItemReadModel.js` 784 · `beliefMap.js` 781 (TR-3 → HB-6 → HB-7) · `informationStatecraft.js` 781 (IN-2/3/4). Measure with `node $MY/arch-932/eff-lines.mjs`/`near-ceiling.mjs <dock>`.

## 6. THE MINT CAR'S FILES (protocols §5) and PACKET_STANDARD
`simulationRules.js` (register entry; the `SIM_LIT` line at the LIT car) · the family door module (the one gate read) · `subsystemRows<Family>.js` (the row) · `subsystemRowsVirtual.test.js` (three edits) · `contributionLedgerShape.test.js` (two literals) · `coveringArrayCoverage.test.js` (four figures) · the four edge bundle files (regenerated) · `tests/property/<family>DormancyFence.test.js` (LIT shape) · the witness fixture (by hand) · `engineGatedRuleKeys.walker.test.js` only if the key leaves BACKLOG or joins EXEMPT · a family importer allowlist if one exists · then the census bill commit · the shift-ledger draft · packets where the family runs them. Handwritten ≈ 12–14 against the cap of 12 → where a LIT car exceeds 12, the 7a/7b split IS THE PLAN (R5). Per car: ≤1 behaviour family, ≤1 new persisted record family, ≤1 user-facing surface, ≤2 leaves ≤250 eff, ≤12 handwritten files; a lane ≤ 8 cars (a longer lane is split into sittings a/b by the family plan, once). Counts are READ OFF HEAD (`node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length`), never inherited from a brief; the chair re-measures at the compose and resolves the conflict by measurement.

## 7. THE LANDING, AS IT TOUCHES A BRIEF (protocols §6)
The lane never composes or lands. Its cars are cherry-picked in order onto `laneCONSIST-932` (cut at `a5876c0ea`); the LIT car is composed LAST and rebased; the chair runs the register pass (every moved register RE-STAMPED at the consist tip: lighting refreeze, writer-reach `--write`, the ratchet `--update`), the totals car, the capsule, the gate under the quiet law (load < 4, zero other workers), chair-verify, the CAS. A brief therefore: declares its shared files in its header with each ceilinged file's slack; predicts `totalFiles` and `entries` for the ratchet in its report; names every register its cars move and by which recorder; and STOPS on any red it did not cause (a moved driven golden, a foreign fence, a worker byte it did not add) — the chair bisects (`worker-measure.sh` for bytes; a settling experiment for a golden).

## 8. SERIALIZATION (protocols §7; the family plan adds rows)
The flag-mint surfaces are parallel-safe ONLY because the LIT car is last and re-measured · `peaceTerms*.js`: TR-5 → WF-6 → POP-5b → GR-5, one lane in flight (the T9 STOP) · `applyWorldPulse.js`: TR-4 car 0 first · `commodityFlow.js`/`supplyKernel.js` (+ the worker-graph tripwires `foodImportRates.js`, `goodsCatalog.js`, `tradeGoodsData.js`: read, never edit): TR-4 → WY-3 → {TR-6, WY-6} · `roadsKernel.js`/`distanceRead.js`: WY-3 (extraction first) → WY-5 → WY-8a; LG-0/1 beside WY-0/1 but not WY-3 · `pulseKernel.js`: one lane at a time · migration files: POP-1 → POP-3 → POP-5a → POP-5b → WY-4 (after WY-3 too) · `beliefMap.js`: TR-3 → HB-6 → HB-7; IN-2 not beside them · `informationStatecraft.js`/brokerage: IN-2 → IN-3; IN-4 beside IN-2 · `envoyErrandVocabulary.js`/`errandMint.js`/`espionageGate.js`: lane B → IN-4 → WF-2 → TR-8 → ES-6b/ES-7 · Herald sections: lane B → IN-5; kinds parallel (each kind its own walker file) · `treasury.js`: lane B → WF-7 → WC-1 · the WF chain (`religiousContest.js`, `religionState.js`, `deityStance.js`, `faithNews.js`): ONE WF lane at a time · `settlementStrategy.js`/`strategyMoves.js`: HB-1 → HB-5 · `WorldMap.jsx`: WY-0 → WY-7 → WY-9/10; EP-4 s2 · `stressorsCore.js`/`rulingPower.js` (EAGER): INT-3a waits for the closure act; INT-4 ∥ INT-5 the only INT parallelism · the generator graph/worker: ONE generator lane at a time (two green alone can be red composed) · `couplingRegistry*.js`: append-only per family leaf · lane E (Scribe) after A0, never beside A. FOUR build trains at a time; the critical path ES-6b ∥ IN-5 → ES-7 (a+b) → ES-7 (c+d) → SC-6 walker → CW-1 → CW-2x → CW-3.

## 9. THE ADDENDUM'S RULINGS, ONE LINE EACH (cite `ARCH-PROTOCOLS-ADDENDUM-1.md` by path; never re-derive)
R1 L2 amended: FP flags are virtual in the defaults and absent from presets EXCEPT the `SIM_LIT` cohort of the four lit presets; D0 car A re-cuts the walker and mints the empty cohort · R2 the lighting-order law binds the boarding order: D0 lands FIRST; `casusCommercii` before `tradePacts`; `commodityFlow` (D0 car B lights it in `dramatic_campaign` and `living_realm`, a declared shift) before `foodCaravans` and `ventures`; `merchantHouses` before `cornering`; every LIT car built at landing · R3 (amended) the dark-mint car carries row + three test edits + both literals off HEAD at its base + covering array + bundles; the LAST car lights and re-reads the counts · R4 SP-4a (`POSTURE_ACTOR_KINDS`, `strategicPosture.js:97`) taken as a declared change, lane 55 car 0, the veto = its code revert · R5 a LIT car over 12 handwritten files splits 7a (mint + lighting) / 7b (bills + literals) as the plan · R7 THE REGISTER IS FROZEN (genesis 2026-09-16): every golden/fixture/witness/in-file-constant move goes through the signed door with a chair-cut record under docs/shift-records/ and an `Owner-Signed:` trailer; the walker binds every row to its bytes · R6 every landing writes a dated LANDING BLOCK back into each family volume it touched (the chair's act; `collect-932.sh` refuses a landing without it) — a brief's REPORT lists the volume rows its cars change so the block can be written.

## 10. THE OWNER'S WORDS THAT BIND EVERY BRIEF
"everything that we built is built lit on" (16:5x) · "we revert back to built dark after the terminal soak" (17:0x) · "fable is architect · architect only one at a time" (18:1x) · "don't start the implementation until all of the architecture has been made" (22:3x) · TIME IS NOT THE CONSTRAINT, QUALITY IS — never present a cheaper option as the best · ALWAYS LAYMAN'S TERMS in the one-paragraph car descriptions, exact identifiers everywhere else · nothing pushes, deploys, applies a migration, deletes data or spends; the golden/tuning signatures, the Scribe's key and price, the legal sitting stay the owner's.
