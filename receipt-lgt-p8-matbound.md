# RECEIPT — LGT-P8-MATBOUND (L-HOMES CAR 8) — ✅ COMPLETE (two commits), with ONE REFUTED BRIEF PREMISE and ONE PRICED REFUSAL

**Lane** L-HOMES-8 / `LGT-P8-MATBOUND` · **Seat** Opus 5 — Fable-unvalidated · **Chair** Fable 5.1
**Dock** `$SC/laneLH8`, opened detached at `38474a59eba460f30d6596dcb65efda3a446738a` (HEAD and porcelain 0 both
verified on arrival; `node_modules/vitest` a symlink into the owner tree).
**Dock tip: `9f7378a555b8a8cdb7d768549fb368924d266053`** · porcelain **0** at close · no rebase, no push, no ref write,
no register act, no `git stash`, no `git checkout --`, no `git show HEAD:<path> >`, no `node_modules` materialisation.
Scratch lives OUTSIDE the measured tree at `$SC/lh8/`.

---

## 0 · THE HEADLINE

⛔ **THE BRIEF'S CENTRAL PREMISE IS REFUTED BY LANDED CODE AT MY OWN BASE.** The brief (and PLAN §6 POSITION 2 row 8,
and the sealed docket row) all say *"the generate action is both BIRTH and REGEN today"*. It is not.
`src/domain/density/densityCreateBoundary.js` exists at `38474a59e`, classifies every module that can reach the
settlement pipeline as BIRTH / DERIVED / PREVIEW, is wired at **both** birth call sites, and is held to the tree by
`tests/lint/densityCreateBoundary.walker.test.js`. **Its own header corrects the very sentence the brief repeats**
(car D2, ODQ §822). Deciding which store path is a BIRTH is not owed. I did not rebuild it.

⛔⛔ **AND THE MAT WIRING IS A HARD RED THAT NEEDS NO BUILD — EXECUTED, NOT PREDICTED.** Planting the naive wiring
(the living-content mint spread by `birthConfig`) reds `tests/build/engineChunkLazy.test.js` by name:
`first-paint-reachable but UNPINNED … /src/domain/content/livingContentLawVersion.js`. **This is the finding the car
exists to produce**, and it re-prices L-MAT.

⭐ **WHAT WAS ACTUALLY OWED, AND IS NOW BUILT:** the boundary answered "which CALLER is a birth" and nothing about
"which LAW a birth mints". There are two generation laws and only one is on it. Every mint arm of the walker was
spelled with a density-only `MINT_SYMBOLS` and was therefore **blind to `newSettlementLivingContentLaw`** — a car
could have wired that mint into a PREVIEW module in silence. That is closed.

⛔ **A STANDING FALSE-GREEN, CURED AT CAUSE, AND ELEVEN SIBLINGS REPORTED:** the walker's `stripCommentsAndStrings`
ran its single-quote pass BEFORE its template-literal pass. Measured: **181 of 2,174 src files mis-stripped, 521,714
characters of LIVE CODE invisible to the scan**, `settlementSlice.js` alone losing 32,723.

---

## 1 · PREMISES RE-DERIVED BEFORE ACTING (every brief figure is the chair's claim, not a fact)

| # | premise as stated | measured at `38474a59e` | verdict |
|---|---|---|---|
| 1 | "the generate action is both BIRTH and REGEN today" | `densityCreateBoundary.js` exists (8,024 B), `settlementSlice.js:379` and `composeInstantWorld.js:121` both call `birthConfig`, walker landed | ⛔ **REFUTED** |
| 2 | "a dial minted there would restamp lived worlds on their next regen" | `regenSection` (`settlementSlice.js:709`) reads `settlement.config \|\| config` — settlement FIRST — and calls `regenNPCsPipeline`, **not** `generateSettlementPipeline`; it is not in the walker's denominator at all | ⛔ **REFUTED as a present-tense fact**; true only of a boundary that does not exist |
| 3 | "Decide which store path is BIRTH" | decided by car D2 and recorded with four executed reads in the module header | ⛔ **ALREADY DISCHARGED** |
| 4 | "make the boundary explicit" | `BOUNDARY_CLASSES` + `PIPELINE_REACHERS` (5 rows: 2 BIRTH, 3 PREVIEW, 0 DERIVED) | ⛔ **ALREADY DISCHARGED** |
| 5 | L-MAT's STOP: `_livingContentLawVersion` stamped on an EXISTING world on regen | the law resolves from the world's own config and **never** from the dial (`livingContentLawVersion.js:106`, pinned at `livingContentMaterialization.test.js:359`) | ✅ the STOP is real as a FUTURE risk, already fail-closed on the read side |
| 6 | "The dial stays dormant here" | `NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION === DEFAULT_LIVING_CONTENT_LAW_VERSION` (1) | ✅ **HOLDS — and it is untouched by this car** |
| 7 | (common law) plan path `refs/preserve/light-plan-2026-09-05:PLAN.md` | the file is at `…:lightingwave/PLAN.md`; the bare `PLAN.md` address does not resolve | ⚠ **brief error** (CORRECTIONS §4 already says so) |
| 8 | (common law) `simulationRules.js` comment-minting hazard | not touched by this car; the flag denominator cannot move | n/a — `mechanismLitCoverage` green |

**The residue, and it is the whole car:** `newSettlementLivingContentLaw()` has **no caller anywhere**
(`git grep` = its own file and its own test), the walker cannot see it, and `livingContentLaw.js:126–134` carried
the refuted reason in prose — which would have sent L-MAT to build a boundary that already exists.

---

## 2 · WHAT LANDED — two commits

| step | outcome | sha |
|---|---|---|
| 1/2 — the generation-law register, the byte refusal, the regen pin, the stripper cure | ✅ green (one census red, owed) | `8776fae10` |
| 2/2 — the two defects the plant-out found in this car's own machinery | ✅ green (same one census red) | `9f7378a55` |

**Files touched — three, all pre-existing. No new file, no new `src/domain` leaf, no fixture, no golden-adjacent
spelling** (`grep -cE '[0-9a-f]{64}|UPDATE_GOLDEN|GOLDEN_SHIFT_SIGNED|REFREEZE|UPDATE_[A-Z_]+'` = **0** on both test
files, and neither is in `.golden-freeze-register.json` — so neither a row nor an exclusion is owed).

- `src/domain/density/densityCreateBoundary.js` — `LAW_WIRING_STATES` + `GENERATION_LAWS` (2 rows: density **WIRED**,
  living content **UNWIRED** with the measured byte reason and the wiring car's exact instruction). Append-only:
  `BOUNDARY_CLASSES`, `PIPELINE_REACHERS` and `birthConfig` are **byte-unchanged**.
- `src/domain/content/livingContentLaw.js` — the refuted paragraph replaced by what is true, with the byte blocker,
  its instrument by name, and the address of the row a wiring car must flip. **Comments only; no executable change.**
- `tests/lint/densityCreateBoundary.walker.test.js` — the stripper order cured, `LAWS_OFF_THE_BOUNDARY` declared, and
  **+7 tests / +1 suite**.

---

## 3 · THE SEVEN NEW ARMS, AND WHAT EACH REFUSES

| # | arm | what it makes impossible |
|---|---|---|
| 1 | finds a real denominator | the symbol scan going vacuous (≥3 mints, ≥3 dials, ≥2 rows) |
| 2 | every mint and every dial in `src/` is accounted for | a THIRD generation law arriving unclassified |
| 3 | every off-boundary row is EARNED | the exclusion roster outliving its subject and becoming a rubber stamp |
| 4 | every row is well-formed, and points at a law that really exists | a row drifting from the law it governs; two laws on one config key |
| 5 | ⭐ **a law that is not WIRED is named by NO module that reaches the pipeline — nor by the mint's own home** | ⛔ **the materialization dial becoming reachable from the product before somebody answers the byte question.** This is the car's charter, made structural |
| 6 | every WIRED law is spread by `birthConfig`, in the mint's own home | a law CLAIMING to be on the boundary while it is not |
| 7 | ⭐ the re-derivation reads the world's OWN config first, and mints once | ⛔ **regen stamping a law onto a world that already exists** — at the one place a module-level manifest structurally cannot look, because `settlementSlice.js` is classified BIRTH as a whole while holding both the birth action and `regenSection` |

---

## 4 · RECEIPTS, EVERY EXIT CAPTURED IN-SHELL

| instrument | exit | figure |
|---|---|---|
| `npx eslint` (3 changed files, both commits) | **0** | zero problems |
| `npm run typecheck:domain:strict` (the real script, not the test) | **0** | `no strict-type regressions (1120 errors, ceiling 1120)` |
| `npm run check:observed-shape-readers` | **0** | `1993 finding(s), exactly matching the frozen inventory` |
| `node scripts/check-writer-reach.mjs` | **0** | judged 6520 · LIT 550 · LIT-NAME 4644 · DARK 1326 — identical to CAR 1's |
| `node scripts/count-tuning-inventory.mjs` | **0** | 233 declared rows · 147 no-importer tables · 0 signed — unmoved |
| `node scripts/count-domain-any.mjs` | — | neither touched file appears; no `any` moved |
| focused: `densityCreateBoundary.walker` + `settlementMapSurfaceAllowlist.walker` | **0** | `2 passed · 19 tests` |
| guards (9 files: voiceMechanics · proseLeak · mechanismLitCoverage · campaignRuntimeLazy · engineChunkLazy · livingContentSeamLazy · densityLaw · livingContentMaterialization · the boundary walker) | **0** | `Test Files 9 passed (9)` |
| ⭐ **`tests/lint/` WHOLE, at the final tip** | **1** | `Test Files 1 failed | 138 passed (139)` |

**The single `tests/lint/` red, attributed by re-running at base:**
`sovereigntyLightingContract.walker.test.js` > "THE CENSUS IS AN ASSERTION" — `expected 23211 to be 23204`.
**The tree measures 23211**; 23204 is the frozen figure. It is the lighting census owed to the chair, caused by this
car's 7 new titles and by nothing else. **Every other arm of the 138-file scanner family is green.**

⚠ **AND ONE RED I CAUSED AND CURED MID-CAR, recorded because the cure is a law:** the first whole-directory run also
red `settlementMapSurfaceAllowlist.walker.test.js` — my `GENERATION_LAWS` row for the layout law named a retired
capability's module and mint **inside `src/`**, which that terminal census convicts as an **owner-gated** question
(ODQ §725). I did **not** widen the allowlist. The row moved to the walker, which that census deliberately does not
scan, with its reason and an earned-ness arm beside it. **No allowlist row was spent.**

---

## 5 · PLANT-OUT — six mutants, all killed, all restored byte-identical

Each plant took a backup FIRST; restore is `cp` from that backup verified by `cmp` **and** md5 in the same shell.
The checkout family was never used. `git status --porcelain --untracked-files=all` was **0** after every restore.

| # | mutant | reds |
|---|---|---|
| M1 | the stripper's unsound order restored | the regen arm — **it cannot even see the expression it asserts on** |
| M2 | the naive MAT wiring (import + spread in `birthConfig`) | ⭐⭐ `engineChunkLazy` > *"every excised module the first paint reaches is PLACED by manualChunks"*, naming `/src/domain/content/livingContentLawVersion.js` |
| M2c | the same wiring, after the arm was cured | the starred arm, naming `densityCreateBoundary.js` |
| M3 | the mint wired into a PREVIEW reacher (`customContentPreview.worker.js`) | the starred arm, naming the worker |
| M4 | a SECOND `birthConfig(` call, inside the re-derivation | the regen arm |
| M4b | the re-derivation reading the wizard config FIRST (`config \|\| settlement.config`) | the regen arm |
| M5b | the register no longer naming the second law | the denominator arm **and** the well-formed arm |

⭐ **M1 is the one that earns the stripper fix**: with the unsound order restored, the regen arm is **vacuously
green** in the very file this car was written to protect.

⭐⭐ **M2 IS THE CAR'S CENTRAL RESULT.** It converts "the MAT wiring might cost bytes" into an executed red with a
named module — and it needed no build, which is why this lane could take it at all.

---

## 6 · ⛔⛔ TWO DEFECTS THE PLANT-OUT FOUND IN THIS CAR'S OWN MACHINERY (both cured, both re-planted)

1. **THE ARM COULD NOT SEE THE WIDEST POSSIBLE WIRING.** "No module that REACHES the pipeline names it" scanned the
   five reachers — and M2, the mint spread straight into `birthConfig`, **passed green**. The boundary module does
   not reach the pipeline: it is what the reachers CALL. A mint added there is minted by every BIRTH at once. The
   mint's own home is now in the scan. *(A scan's denominator must contain the thing every member of it calls.)*
2. **AND THEN THE REGISTER CONVICTED ITSELF.** With the home in the scan the walker went red **at rest**, because
   `GENERATION_LAWS` was keyed by the mint's IDENTIFIER — and to a source scan **an identifier used as an object key
   is indistinguishable from a call site**. The row declaring a law UNWIRED read as this module naming that law's
   mint. ⭐ **A declaration must not be spelled the way the thing it declares is spelled.** The table is keyed by a
   law id now; the mint lives in a `mint` string field, which the scan strips. **Worth a standing row.**

---

## 7 · EAGER BYTES — the prediction, its bound, and the one thing I could not execute

**Predicted first-paint delta: 0 B. NO IMPORT EDGE WAS ADDED TO ANY MODULE** — the only edge this car priced is the
one it refused. Measured with the estate's own closure walker (`$SC/lh8/closure.mjs`, `overlay.mjs`, the algorithm
copied term-for-term from `campaignRuntimeLazy.test.js:64`):

```
src/main.jsx static closure, at rest        : 237 modules
src/main.jsx static closure, naive wiring   : 239 modules   (+livingContentLaw.js, +livingContentLawVersion.js)
```

⚠ **THE ONE PREDICTION I COULD NOT EXECUTE, AND IT IS THE CHAIR'S TO CLOSE.** `densityCreateBoundary.js` **is
eager**, and this car adds **1,422 B of source** to it (two exported constants, comments excluded). If Rollup
tree-shakes them the delta is **0 B**; if it retains them it is **at most 1,422 B pre-minification**. Two reasons to
expect 0, neither of them a receipt:
- `Object.freeze` is in Rollup's known-pure globals, so an unused `export const X = Object.freeze({…})` is
  shakeable; nothing under `src/` imports either new symbol — only the walker does.
- ⭐ **THE PRECEDENT IS IN THE SAME FILE**: `PIPELINE_REACHERS` is **1,520 B of the identical shape**, equally
  test-only, and it landed with car D2 against the same three-figure margin.

⇒ **CONFIRMED: the closure MEMBERSHIP is unmoved. PLAUSIBLE: the byte delta is 0.** The chair's hashed-chunk listing
diff is the proof; **if the consist's listing diff moves by ~1.4 KB, this car is the first place to look**, and the
cure is to shorten the two `why` strings, which costs the car nothing but prose.

---

## 8 · PREDICTED REGISTER DELTAS — recorded, NOT taken

| register | frozen | this tip | delta | how |
|---|---|---|---|---|
| lighting census `files` | 2523 | **2523** | 0 | MEASURED (`lprobe/census-probe.mjs`, read-only farm, at the final tip) |
| lighting census `parked` | 371 | **371** | 0 | MEASURED |
| lighting census `credited` | 2152 | **2152** | 0 | MEASURED |
| lighting census `titles` | 23204 | **23211** | **+7** | MEASURED — twice: the probe, and the walker's own red |
| lighting census `suiteTitles` | 6217 | **6218** | **+1** | MEASURED — the walker throws on `titles` first, so this figure is invisible to it |
| test ratchet `totalTests` | — | — | **+7** | DERIVED; a 90 % collapse floor, so a rise cannot red it |
| test ratchet `totalFiles` | — | — | **0** | no file added or deleted |
| test ratchet `entries` | — | — | **0** | no new known failure |
| four censuses per new `src/domain` leaf | — | — | **0** | no new leaf; both src edits are to existing modules |
| golden-freeze register | — | — | **0** | no new test file, no fixture, and no golden-adjacent env spelling or 64-hex literal anywhere in the diff ⇒ neither a row nor an exclusion is owed |
| `sizeBaseline` | — | — | **0** | neither src file is baselined; effective lines are far under the 800 layer ceiling and `tests/lint/sizeBaseline.test.js` is green inside the whole-dir run |
| voice magnitudes | — | — | **0** | `voiceMechanics` + `proseLeak` green; the car adds no user-facing string |
| writer-reach · OSR · domain-any · tuning | — | — | **0** | each measured above, exit 0, figures identical to CAR 1's |
| engine-gated walker · `mechanismLitCoverage` | — | — | **0** | green; `simulationRules.js` untouched, so no flag can be minted by a comment |

---

## 9 · FINDINGS RECORDED, NOT BUILT (each with its address)

1. ⛔⛔ **ELEVEN SIBLING INSTRUMENTS CARRY THE SAME UNSOUND STRIPPER.** Every one puts its single-quote pass before
   its template-literal pass, so an apostrophe in a template literal swallows code:
   `tests/lint/seedLoopTotality.walker.test.js` · `tests/lint/worldGenerationClockSeam.walker.test.js` ·
   `tests/property/sovereigntyTradeDormancyFence.test.js` · `tests/property/oathHolderDormancyFence.test.js` ·
   `tests/property/dispositionChannelsDormancyGolden.test.js` · `tests/domain/townCartographyDeterminism.test.js` ·
   `tests/domain/npc/characterConsumers.test.js` · `tests/domain/npc/livedExperienceFunnel.test.js` ·
   `tests/domain/npc/characterDrift.test.js` · `tests/domain/npc/paradigmAxisCatalog.test.js` ·
   `tests/domain/npc/livedExperienceSources.test.js`. (Two files — `infiltrationDrift.test.js`,
   `operationsVoice.test.js` — already order it correctly, so the sound spelling exists in-house.) **The cure is a
   two-line reorder per file, but each one's verdicts must be measured both ways before it lands** — mine flipped
   nothing; another's might, and a flip is a real finding surfacing, not a regression. **NOT TAKEN: a 11-file sweep
   is its own car.**
2. ⭐ **THE MAT WIRING'S SHAPE IS NOW KNOWN, AND IT IS A BUILD.** Mint on the LAZY engine side the birth caller
   already awaits (`loadEngine()` resolves before `generateSettlementPipeline` is called at `settlementSlice.js:411`,
   while `birthConfig` runs at `:379`), and add `'/src/domain/content/livingContentLaw.js'` to
   `ENGINE_SHARED_DOMAIN_EXCISIONS` beside the leaf. ⛔ `settlementSlice.js` is `sizeBaseline`d at **976, tolerance
   0**, so the store-side edit must be line-neutral — a WRAP, exactly as car D2's own comment says it did.
3. ⚠ **A CONCURRENT LANE IS EDITING MY FILE.** `$SC/BACKUP-densityCreateBoundary.js` (09:27) is a *larger* version of
   `densityCreateBoundary.js` carrying a **fourth boundary class, `EXECUTOR`**, `src/store/settlementGenerateAction.js`
   and `src/workers/generationRequest.js` — a worker-transport split. **No ref in the repo carries that file**
   (`git cat-file -e` over every `refs/preserve` and `refs/heads`), so it is an in-flight dock, not a landed tip.
   **My edits are append-only and touch neither `BOUNDARY_CLASSES` nor `PIPELINE_REACHERS` nor `birthConfig`**, which
   should make the replay conflict a pure additive one — but the chair should expect to hand-merge this file, and
   ⛔ **that lane's `EXECUTOR` row must also be checked against arm 5**: an executor that names a non-WIRED law's
   mint reds, correctly.
4. **`BOUNDARY_CLASSES` has a member with no rows.** `DERIVED` is declared and unused — re-derivation does not reach
   the pipeline at all at this base. Left as-is (it is the vocabulary a future transport needs, and finding 3 says
   one is being built), but it is headroom by the estate's own earned-row law and worth one sentence at the desk.
5. **PLAN §6 POSITION 2 row 8's "new test file? no · same-seed shift: no" both hold.** Row 8's "files it touches: the
   create boundary" is the only accurate half of the row; its invariant sentence describes a tree from before D2.

---

## 10 · RETROVALIDATION ROW (owed to the Fable 5.1 chair)

| # | what an Opus seat JUDGED | what the chair must RE-DERIVE | receipts by path | priority |
|---|---|---|---|---|
| 1 | **refusing to rebuild the create boundary**, on the measured ground that it landed at D2 and the brief's premise is dead | that the residue I built (the LAW axis) is the right reading of the car, and that no part of the brief's boundary work is still owed | `src/domain/density/densityCreateBoundary.js` header (D2's own correction) · §1 of this receipt | **HIGH** — it re-cuts the brief |
| 2 | **refusing the MAT wiring and pricing it instead**, on an executed red rather than a byte estimate | that "declare the gap, hand up the shape" beats "wire it and let the listing diff decide", given the wiring needs a `vite.config.js` excision and a build | `$SC/lh8/m2.log` (the orphan-arm red, naming `livingContentLawVersion.js`) · `$SC/lh8/overlay.mjs` | **HIGH** — it re-prices L-MAT |
| 3 | **the eager-byte prediction is PLAUSIBLE, not CONFIRMED** — 1,422 B of source added to an eager module, predicted shaken to 0 on Rollup's pure-`Object.freeze` rule and the 1,520 B `PIPELINE_REACHERS` precedent in the same file | the hashed-chunk listing diff. ⛔ **If the consist's listing moves ~1.4 KB, look here first**; the cure is to shorten two `why` strings | §7 · `git show 8776fae10 -- src/domain/density/densityCreateBoundary.js` | **HIGH** — it is the consist's STOP |
| 4 | **curing the stripper inside this car** rather than deferring it with the other eleven | that a cure whose verdicts were measured both ways (0 reacher flips, 0 mint flips over 2,174 files) belongs in the car that needs it, and that the eleven siblings are their own car | `$SC/lh8/blindspot.mjs` · `$SC/lh8/stripdiag.mjs` · `$SC/lh8/m1.log` | MEDIUM |
| 5 | **moving the layout law's exclusion out of `src/` into the walker** after the terminal settlement-surface census convicted the row | that relocating rather than allowlisting was right, and that a `tests/`-side exclusion with an earned-ness arm is an acceptable home for owner-gated vocabulary | `$SC/lh8/lintdir.log` (the red) · `LAWS_OFF_THE_BOUNDARY` · §4 | MEDIUM |
| 6 | **keying the register by a law id, with the mint as a string** — the second self-conviction | that "a declaration must not be spelled the way the thing it declares is spelled" is the right general rule, and whether it deserves a standing hazard row | `$SC/lh8/walker3.log` (the at-rest red) · commit `9f7378a55` | MEDIUM |
| 7 | **the regen contract pinned by SOURCE**, because the behavioural half needs the store and the module-level manifest structurally cannot see inside a BIRTH module | that a source contract is acceptable here, and that M4/M4b are enough to prove it kills | `$SC/lh8/m4.log`, `$SC/lh8/m4b.log` | LOW |
| 8 | **committed with the hook**, not `--no-verify` | nothing — md5 of every touched file is identical before and after both commits, so no `eslint --fix` re-stage could have hidden anything | `$SC/lh8/pre-commit-md5.txt` vs `post-commit-md5.txt` (and `…2-md5.txt`) | LOW |
| 9 | ⚠ **a self-reported defect I chose NOT to rewrite history to fix.** Both commits carry `Seat:` / `Lane:` / `Co-Authored-By:` exactly as the brief spells them, but with a BLANK LINE before `Co-Authored-By`, so git's contiguous-block parser (`%(trailers:key=Seat)`) sees only the last line. The landed cars (`408d2e2b1`, `4a2e1110c`) use a tight block. `^Seat:` / `^Lane:` greps still find mine, and I found **no chair tool that parses trailers** — so I recorded it rather than amending a cited sha or rewriting two commits, both of which the dock law disfavours | whether the chair's landing tooling parses rather than greps; if it parses, the fix is the chair's at composition, where the messages are rewritten anyway | `git log -2 --format='%(trailers:only=true)'` in `$SC/laneLH8` | LOW |

**Dock tip: `9f7378a555b8a8cdb7d768549fb368924d266053`.**
