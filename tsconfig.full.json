{
  "//": [
    "Full-tree tsconfig — extends the baseline (tsconfig.json) and includes the",
    "whole src logic tree (domain/store/lib/hooks/generators/components/pdf).",
    "",
    "This IS the gate's typecheck config: `npm run typecheck` (and therefore",
    "`npm run check`) run `tsc --noEmit -p tsconfig.full.json`, currently at",
    "ZERO errors. The old domain-only punch-list closed, so the gate switched",
    "from tsconfig.json to here; `npm run typecheck:domain` keeps the fast",
    "domain-only check.",
    "",
    "Don't include `tests/` — the test files use vitest globals + JSX in JS",
    "and would need their own includes/types config to typecheck cleanly."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {},
  "include": [
    "src/**/*.d.ts",
    "src/domain/**/*.js",
    "src/domain/**/*.ts",
    "src/store/**/*.js",
    "src/lib/**/*.js",
    "src/hooks/**/*.js",
    "src/generators/**/*.js",
    "src/components/**/*.{js,jsx}",
    "src/pdf/**/*.js"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "public",
    "src/data",
    "src/utils",
    "tests"
  ]
}
