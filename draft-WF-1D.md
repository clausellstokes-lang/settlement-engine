# WF / WF-1D — the war-dissolution join that names the fall (stage 4 of the `wf-1` split promotion)

- **Status:** DRAFT
- **Compile note:** compiled by lane TC-WF1D under ODQ §291.5; the chair lands. ⚠ The status value
  above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line — trailing prose
  there leaves the status unparsed and makes the manifest disagree with the Markdown
  (J-TEWF1B-1, paid once already).
- **Verified base:** `claude/composite-r4` at `2cdb87fac566b3d6803a0dce9d59df13f07c1c9e`
- **Preamble:** `docs/implementation/preambles/WF-PREAMBLE.md`, cited **BY SHA-256
  `ca02c8a165ddbc18ab3f254bebce8cca5dd945e71e074b526227164e9b0cabfd`** (re-executed at this base:
  `git show 2cdb87fa:docs/implementation/preambles/WF-PREAMBLE.md | shasum -a 256`; 757 lines,
  56,878 bytes). This is the post-§315 hash the whole live family cites. Every refutation,
  disposition, register, STOP and census law in that file binds here and is not restated.
- **Substrate annex:** `docs/implementation/preverification/WF-SUBSTRATE.md`. ⛔ **No absolute is
  inherited.** Every row this packet depends on was re-executed at `2cdb87fa` against a
  `git archive` tree, never against the working tree, which matches no branch.
- **Design authority:** `docs/DESIGN_FP_ARCH_WF.md` §4-WF-1 (the NET-ZERO join rule) and §5 seam **5**
  (the WR-1 dissolution join, pinned from both sides) · `docs/DESIGN_FP_FAITH.md` §5-WF-1 (the
  `pin:dissolution-names-the-fall` two-flag fixture and the legacy arm) ·
  `docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md` §1/§10 · `docs/DESIGN_FP_SPINE.md` reqs 13/14.
  ⭐ At `2cdb87fa` the BUILD copy of the WF volume **IS** the §78-cured text (ODQ §308.2 re-folded it
  to ledger blob `169c75e7`; `git show 2cdb87fa:docs/DESIGN_FP_ARCH_WF.md | wc -l` → **846**), so
  R-WF-1's nine-hunk divergence is CLOSED at this base and the preamble's warning is historical.
- **Train:** `wf-1`, family **WF**, member **d**. WF-1A is LANDED (`66fda66d`); WF-1B is LANDED;
  **WF-1C is built and terminalized at `253f2028` awaiting the chair's CAS** and is treated
  throughout as a LANDED ancestor. ⛔ **This packet promotes only once WF-1C is live on the branch**
  — not for a path collision (there is none) but because two of its inherited figures move in that
  landing (§11 step 8).
- **Member cap:** WF is **STAMPED** (ODQ §78.1; preamble §P10 stamp line) ⇒ engine trains cap at
  EIGHT. This member uses none of the headroom.
- **Dispatch + census authority:** ODQ **§320**, which dispatched this compile and is the
  **authorizing decision** for the census move named in §6 (§299.4; §303.7 for the block schema).
  Chair instructions folded in: ODQ **§316.2A**'s two zero-headroom hazards (both re-measured at
  §2.1; **neither STOP fires** and the reason is executed, not argued) and **§258.2**'s substitution
  doctrine (measured NOT NEEDED for the join — §2.2).
- **Constitutional law binding every line below:** the DEITY DOCTRINE — faith is CULTURAL, never
  theological; no premade deity pool; typed buckets per FINITE-SEMANTICS; AI is clerk, never writer.
  This join makes a war receipt say **why the believers left the seat** — a political and social
  fact about people — and never that a god fell, failed or departed. See §5.

---

## §0 · WHAT THIS MEMBER IS, AND WHAT IT REFUSES

`WF-1A` §0 defers to WF-1d three deliverables: *"the `warTermination` dissolution-names-the-fall
join and the FaithSection cause-chain line"* in §0, and *"the FaithSection cause-chain line, the
Chronicle obituary row"* in its §5 exclusion table. `DESIGN_FP_ARCH_WF.md` §4-WF-1 states the join's
whole engineering rule in one sentence:

> *"warTermination.js **FROZEN 818** — the dissolution-names-the-fall join lands as a PURE READ (the
> receipt site calls the WF-1 leaf's `fallCauseFor(...)`) at NET-ZERO effective lines in
> warTermination.js, or the wave STOPS and proposes the extraction recipe (L8) — no silent growth
> against a frozen ceiling."*

⭐⭐ **THE JOIN LANDS AT NET ZERO, AND §2 IS THE EXECUTED MEASUREMENT RATHER THAN THE ARGUMENT.**
`warTermination.js` goes **818 → 818 EXACT** with **zero lines added and zero lines removed**,
`eslint` clean, and the four landed war suites **50/50** green.

⛔ **BUT THE THREE CHARTERED DELIVERABLES DO NOT FIT ONE MEMBER, AND ONE OF THEM NO LONGER HAS A
PRODUCER.** §7 prices all of it with numbers. In short:

- **THE JOIN — this packet.** The `warTermination` receipt names the typed fall.
- **THE FAITHSECTION CAUSE-CHAIN LINE — DEFERRED to a sibling member, priced in §7.** Together with
  the join it breaks **three** `PACKET_STANDARD` hard caps at once, and it carries a DS-FTH-1 corpus
  amendment plus a generator run that the join carries not at all. Compile seed:
  `laneTCWF1D-WF-1E-seed.md`.
- **THE CHRONICLE OBITUARY ROW — REFUSED ON A MEASUREMENT (§4).** Both of its candidate producers
  were re-filed out from under it by later chair rulings: the settlement-scoped obituary went to
  WF-8 at ODQ §309, and the realm last-seat beat's own `KIND_SECTION` row lands with WF-1C. A WF-1d
  row would be a registration row for a kind nothing mints, and ⛔ **nothing in the estate would
  red on it** — which is exactly why it is refused at compile.

---

## §1 · THE BEHAVIOUR THIS MEMBER ADDS

Today a holy war can outlive the faith that raised it and **the receipt can only say that it did,
never why**. Executed at this base, and this is the wave's whole premise:

```
warTermination.js:320-333   sacred_claim dissolves when the pinned patron anchor moved
warTerminationCauseTables.js:79
  sacred_claim: 'a god named when the banners rose is no longer worshipped from the same throne'
```

⛔ **THAT CLAUSE IS THE ONLY THING THE ENGINE CAN SAY, AND IT SAYS THE SAME WORDS WHETHER THE CREED
WAS EVICTED BY A GARRISON, LOST A CONTEST, OR SIMPLY LOST THE PEWS.** WF-1a built the typed record
that knows the difference — `religionStates[cid].patronFalls` and the pure read `fallCauseFor` —
and left it deliberately unread: *"`fallCauseFor` is WF-1d's whole consumption surface. It is built
here and deliberately has no production caller until then"* (`WF-1A` §13).

⭐⭐ **THIS PACKET IS THE CHARTERED SECOND CALLER, AND IT SAYS SO EXPLICITLY.** Preamble **§P2.11**
reads: *"`fallCauseFor` IS WF-1d's WHOLE CONSUMPTION SURFACE … ⛔ No WF wave may give it a second
caller ahead of WF-1d without re-pricing WF-1a's producer-first identity claim."* **WF-1d IS that
wave.** Executed census of `fallCauseFor` in `src` at this base: **exactly one definition
(`patronFall.js:146`) and ZERO callers** — the prohibition held through WF-1b and WF-1c, both of
which name it in their own non-goals. This member takes the caller the law reserved for it, and
WF-1a's producer-first claim is discharged rather than re-priced.

WF-1d makes the dissolution **named**: when a sacred war's founding claim dies because the patron
anchor moved, and the settlement's own ring records why that patron fell, the receipt carries the
typed token and the reader sentence says it in the house voice. Dark — and lit with nothing
recorded — the receipt is byte-identical.

---

## §2 · THE CURE

### 2.1 ⛔⛔ THE TWO ZERO-HEADROOM HAZARDS, RE-MEASURED AT THIS BASE — AND A THIRD AND FOURTH

Re-executed with **eslint's own `Linter`** under `max-lines {skipBlankLines:true,
skipComments:true}` — the same engine `tests/lint/sizeBaseline.test.js` uses for its own
`effectiveLines()`, so the measurer and the enforcer cannot disagree. Never `wc -l`, never inherited.

| file | effective | frozen literal | verdict |
|---|---:|---:|---|
| `src/domain/worldPulse/warTermination.js` | **818** | **818** | ⛔ **EXACT — ZERO HEADROOM** |
| `src/domain/worldPulse/pulseKernel.js` | **1581** | **1581** | ⛔ **EXACT — ZERO HEADROOM** |
| `src/domain/worldPulse/applyWorldPulse.js` | **941** | **941** | ⛔ **EXACT — a FOURTH, unrecorded (RAISED-2)** |
| `src/domain/worldPulse/peaceTerms.js` | 797 | — | 3 under the 800 layer ceiling |
| `src/domain/worldPulse/warTerminationCauseTables.js` | **71** | — | **729 headroom — the extraction home** |
| `src/domain/certification/couplingRegistry.js` | 109 | — | 691 headroom |
| `src/domain/worldPulse/patronFall.js` | 28 | — | the landed WF-1a leaf |

⭐ **BOTH DISPATCH-NAMED HAZARDS REPRODUCE EXACTLY, AND NEITHER STOP FIRES.**

1. **`warTermination.js` is EXACT at 818 and this member holds it there** — §2.2 is the executed
   before/after. The §258.2 substitution doctrine is measured **NOT NEEDED**, and the extraction
   recipe (L8) is **NOT PROPOSED**, because the join fits inside the ceiling as chartered.
2. ⭐ **`pulseKernel.js` IS NOT TOUCHED BY THIS MEMBER AT ALL.** The dispatch's STOP-and-RAISE for a
   kernel touch beyond net-zero packing does not arise: the manifest names no line in that file,
   **no PRNG call is added, moved or removed, and the fork order is untouched**, so the stream
   identity THE PROMISE protects is not in play. Stated affirmatively rather than by silence.
   ⚠ This is a property of the shape CHOSEN, not of the deliverable — J-TCWF1D-1 records the
   alternative that would have touched it.
3. ⛔ **A FOURTH ZERO-HEADROOM FILE IS MEASURED AND NO FAMILY DOCUMENT NAMES IT.**
   `applyWorldPulse.js` is **941 against a frozen 941**. Preamble §P7.8 names `peaceTerms.js` and
   `warTermination.js`; WF-1C's RAISED-A adds `pulseKernel.js`; this is the fourth. It matters here
   because it was the natural second home for the join's injection seam and is as frozen as the
   first. **RAISED-2.**

### 2.2 ⭐⭐ THE JOIN FITS AT NET ZERO — EXECUTED, NOT REASONED

Applied to a throwaway `git archive 2cdb87fa` tree outside the repo and measured on both sides:

```
--- BEFORE ---
src/domain/worldPulse/warTermination.js            eff=818  baseline=818  EXACT
src/domain/worldPulse/warTerminationCauseTables.js eff=71   baseline=—    no-baseline
--- AFTER ---
src/domain/worldPulse/warTermination.js            eff=818  baseline=818  EXACT
src/domain/worldPulse/warTerminationCauseTables.js eff=83   baseline=—    no-baseline
--- npx eslint on both files ---   TRUE_EXIT=0
--- npx vitest run tests/domain/{warTermination,warTerminationPulse,warCostsIntegration,warCoalitionWr6}.test.js ---
 Test Files  4 passed (4)        Tests  50 passed (50)
```

**THE MECHANISM, IN ONE SENTENCE: the prose selector pays for the faith import.**

`warTermination.js` reads `DISSOLVED_CAUSE_PROSE` at **exactly one site** — `:431`, inside
`terminationReason` — and does **not** re-export it (only `WAR_CAUSE_DISSOLUTION` is re-exported,
at `:94`). Replacing that raw-table read with a `dissolvedClauseFor(type, fallCause)` selector that
lives in `warTerminationCauseTables.js` lets the four-line import block collapse to two, which buys
the `patronFall.js` import outright and leaves exactly one line of change budget for the lookup.
Everything else is a key or a parameter appended to a line that already exists.

⭐ **THE EXTRACTION HOME IS NOT AN INVENTION OF THIS PACKET — IT WAS BUILT FOR THIS PRESSURE.**
`warTerminationCauseTables.js`'s own header says so: *"`warTermination.js` … is FROZEN there by
`scripts/.size-baseline.json` — zero headroom … Rather than raise a frozen number — which the
baseline's own instructions forbid — the tables move DOWN into a leaf that has nothing but them."*
WF-1d takes the door the WR family already cut for exactly this reason, which is why this is a
**seam** and not a squeeze.

### 2.3 THE EXACT EDIT — seven sites, symbol-first, two anchors each

| # | site | edit | Δ eff |
|---|---|---|---:|
| 1 | the cause-table import block (`:90-93`, symbols `DISSOLVED_CAUSE_PROSE` / `TERMINATION_PEACE_PROSE`) | **4 lines become 2** — `import { dissolvedClauseFor, TERMINATION_PEACE_PROSE } from './warTerminationCauseTables.js';` then `import { fallCauseFor } from './patronFall.js';`. The faith import IS one of the two | **−2** |
| 2 | `readWarTerminations`, immediately above `const deployments = asObject(state.deployments)` (`:508`; second anchor — the next line is the `byId` Map guard) | `const unseating = rules.faithUnseatingEnabled === true;` — the BY-NAME strict gate | **+1** |
| 3 | `terminationReason` parameter list, the `dissolvedCauseTypes,` line (`:424`; second anchor — the next line is `trajectory = 'even',`) | append `fallCause = null,` to that existing line | **0** |
| 4 | `terminationReason`, the `dissolvedCase` line (`:431`) | `DISSOLVED_CAUSE_PROSE[type]` → `dissolvedClauseFor(type, fallCause)` | **0** |
| 5 | immediately above `const attackerName = settlementName(attackerItem, 'The attacking court')` (`:824`) | the per-deployment lookup, one statement on one line | **+1** |
| 6 | the `terminationReason({…})` argument line `dissolvedCauseTypes: effectiveCause.dissolvedCauseTypes,` (second anchor — the next line is `trajectory: trajectoryRead.trajectory,`) | append `fallCause,` | **0** |
| 7 | the receipt's `decidingTerm,` line (`:892`; second anchor — the next two lines are `causeState: …` then the `dissolvedCauseTypes` spread) | append `...(fallCause ? { patronFallCause: fallCause } : {}),` | **0** |
| | | **TOTAL** | **0** |

⭐ **VERIFIED TWICE OVER, BY TWO INDEPENDENT MEASURES.** eslint's `Linter` reports **818 → 818**, and
a plain `wc -l` reports **1045 → 1045** — the physical diff is `-4 +2` in the import block and `+2`
statement lines, so the file does not move by either count. The executed `diff -u` is quoted in
`laneTCWF1D-receipt.md` §2.2.

⛔ **THE DISCIPLINE THAT MAKES THIS A SEAM AND NOT A SQUEEZE, STATED SO A REVIEWER CAN HOLD ME TO
IT.** Exactly ONE edit (#1) reformats existing code, and it reformats an import whose specifier list
genuinely shrinks by a member that is no longer read. Edits #2 and #5 are ordinary one-statement
lines, each on its own line — **there is no two-statement cramming anywhere in this member.**
#3/#6/#7 append a key or a parameter to a line that already exists, which is verbatim the idiom
WF-1C priced for `pulseKernel.js` M5 and which that file's own `:307`/`:331` established long before
either wave.

⚠ **`:892` IS AN ADDRESS AND ADDRESSES ROT** (§P0). Every row above leads with its SYMBOL and
carries a second anchor; the implementer navigates by symbol and treats the line numbers as a
cross-check that must agree, not as the target.

### 2.4 THE TYPED CLAUSE, AND WHY THE TOKEN AND THE PROSE BOTH LAND

`warTerminationCauseTables.js` gains a closed four-member table and one selector:

```js
export const FALL_CAUSE_PROSE = Object.freeze({
  discredited: …, displaced: …, imposed: …, suppressed: …,
});
export function dissolvedClauseFor(type, fallCause = null) { … }
```

- ⛔ **THE VOCABULARY IS THE LANDED ONE AND IT IS NOT WIDENED.** Four keys, exactly
  `PATRON_FALL_CAUSES`, in codepoint order. `abandoned` is absent because WF-1a CUT it on a
  measurement, and §P2.12 forbids a fifth cause without its producer in the same commit. A6 asserts
  the two vocabularies are the same set, so a later fifth cause reds here rather than rendering a
  blank clause.
- ⛔ **EVERYTHING BUT A TYPED `sacred_claim` FALL READS EXACTLY TODAY'S CLAUSE, BYTE FOR BYTE.** The
  selector returns `DISSOLVED_CAUSE_PROSE[type]` unchanged for the other fifteen casus types and for
  `sacred_claim` with no recorded fall. That is why the 50/50 green above is a receipt and not a
  coincidence.
- ⭐ **BOTH the typed token AND the sentence land, and the reason is FINITE-SEMANTICS.** The receipt
  carries `patronFallCause` — a closed bucket a later reader can switch on — and the reader sentence
  carries the house prose. A packet that landed only the prose would put a fact into free text; one
  that landed only the token would leave the charter's *"the dissolution receipt prints the typed
  cause"* unmet.
- ⚠ **THE CONDITIONAL SPREAD IS A BYTE CLAIM.** `...(fallCause ? { patronFallCause: … } : {})` means
  a receipt with no named fall carries **no key** — absent, never `null` — on the
  `dissolvedCauseTypes` spread's own precedent three lines below it. A3 asserts it at the byte level.

### 2.5 ⛔ THE COUPLING IS THE CHARTERED ONE, AND ITS BILL IS EXECUTED RATHER THAN PREDICTED

`DESIGN_FP_FAITH.md` §5-WF-1 declares it in terms: *"**Couplings (declared in
DESIGN_FP_COUPLINGS.md):** FAITH×WAR — the dissolution receipt prints the fall cause (this wave's
export)."* This member makes that coupling REAL, and the walker priced it in its own words. Running
`tests/lint/couplingInclusion.walker.test.js` against the patched tree:

```
AssertionError: expected [ Array(1) ] to deeply equal []
+ "src/domain/worldPulse/warTermination.js imports src/domain/worldPulse/patronFall.js (FAITH→WAR)
   — add its couplingRegistry row in THIS commit (the same-commit obligation,
   DESIGN_FP_COUPLINGS.md §0.3), naming the importer as the row's read address."
 Test Files  1 failed (1)      Tests  1 failed | 15 passed (16)
```

**BOTH HALVES OF THE INSTRUMENT ARE MEASURED** (§P2.8 — a packet naming one half is defective on
its face):

- **(a) PAIR half — INCURRED, exactly one pair, one row.** `licensingRows(pair)`
  (`couplingInclusion.walker.test.js:830`) matches on `direction` **and** importer module. Executed
  at this base: `COUPLING_REGISTRY` holds **49** rows, of which **ZERO** carry `direction:
  'FAITH→WAR'` and **ZERO** name `warTermination.js` as `read` or `counterforce`. Nothing licenses
  the pair, so the row is genuinely owed and **M3 is the estate's first `FAITH→WAR` registry row.**
  ⚠ The four `FAITH→WAR` pairs that exist today — `warTermination.js → sacredClaim.js` among them —
  are grandfathers in `tests/lint/.coupling-inclusion-baseline.json` (**152** entries), and a
  grandfather never reaches the licensing filter.
- **(b) UNLAYERED-MODULE half — NOT INCURRED.** No module is created, so `LIVE_UNLAYERED` does not
  move and `UNLAYERED_BASELINE_CEILING` (**179**, `:646`, `.toBe`, exact in BOTH directions) stands
  still; `ARGUED_ROSTER_CEILING` (**20**, `:612`) likewise, since no `ARGUED_UNLAYERED` entry is
  added or removed. Both named files are already claimed by `LAYER_PATTERNS.WAR`'s
  `/^src\/domain\/worldPulse\/war[A-Z]/`, and `patronFall.js` by FAITH's landed exact-path row.
- ⭐ **NO EXACT-COUNT PIN BLOCKS THE ROW, AND THIS WAS MEASURED RATHER THAN HOPED.**
  `tests/domain/couplingRegistry.test.js` pins `COUPLING_REGISTRY_SCHEMA_VERSION` `.toBe(4)` and
  otherwise asserts only `toBeGreaterThan(0)` / `toBeGreaterThanOrEqual(5)` and derived identities;
  `tests/lint/couplingDesk.walker.test.js` uses `DECLARING_ROW_FLOOR` / `DECLARED_KIND_FLOOR` /
  `AUTHORED_ROW_FLOOR` floors plus a `DISPUTED` `toHaveLength(4)` this row does not join.
  ⚠ `tests/domain/couplingReceiptSample.test.js:116` asserts `addresses > COUPLING_REGISTRY.length`
  — a row carrying **both** a `read` and a `counterforce` address adds 2 to the left and 1 to the
  right, so the inequality widens. **M3 must therefore carry both addresses**, and the battery names
  every one of the eleven suites that import the registry.

### 2.6 ⛔⛔ THE LEAF'S OWN HEADER SAYS THIS WAVE FALSIFIES IT — DECLARED, NOT DISCOVERED

`patronFall.js:21-25`, verbatim: *"⛔ THE FILE NAME IS LOAD-BEARING. It sits under
`src/domain/worldPulse/` beside its ONLY importer (`religiousContest.js`) precisely so
`scanCrossLayerPairs` yields zero pairs and both coupling-registry ceilings stand still."*

This member gives the leaf its **second** importer, in another layer. **Both ceilings still stand
still** — measured above, neither `.toBe` figure moves — because the lawful cure for a new pair is a
LICENSING ROW, not a baseline bump. But that paragraph now describes a tree that no longer exists,
and a WF compiler reading it would price a later wave wrong. **M4 re-words it in the same commit**,
on WF-1C's M2 precedent of correcting a production comment whose claim its own wave falsifies.
⚠ M4 is comment-only and `warTerminationCauseTables.js` / `patronFall.js` appear in **no** edge-shared
closure (§6), so §104.4's comment-only trigger does not fire.

### 2.7 THE LIFECYCLE CLAUSE (§78 F-4), RE-DERIVED — AND IT IS SHORT BECAUSE NOTHING PERSISTS

⛔ The dead "no load-time normalizer" premise is used nowhere in this packet. Re-executed at
`2cdb87fa`:

1. **ZERO new persisted material.** `patronFallCause` lives on a `war_termination_read` receipt,
   which is a per-tick projection. `readWarTerminations` **mutates nothing** — the WR-1 no-mutation
   law its own suite already pins — and the receipt reaches `worldState` only through the existing
   `pulseHistory.warTerminationReads` path that carries every other receipt field today.
2. **ZERO new top-level keys** ⇒ `CONDITIONAL_LEDGER_KEYS` does not move and the array-order-IS-
   serialized-key-order law is untouched. **Asserted, not assumed** (A5).
3. **No dedicated normalizer is owed and none is added.** No field joins `religionStates`,
   `spatialLedgers` or any conditional ledger. `normalizeEnvoyRows` remains the precedent for a wave
   that needs one; this wave does not.
4. **The `{ to: 2 }` `religionStates` migration is untouched.** This member READS `patronFalls`
   through the landed pure `fallCauseFor` and writes nothing there; WF-1a already round-trip-pinned
   the ring and WF-1b already proved the migration's `...rest` spread carries added sub-keys.
5. **REGEN / UNDO.** The receipt is derived: a regen re-runs the fold and re-derives the clause from
   the ring it rebuilds; the pulse undo ring restores `preWorldState` wholesale. ⭐ **And the
   derivation is honest across the boundary** — a regen that DROPS `religionStates` drops the ring
   with it, at which point the join finds nothing and the receipt falls back to today's clause,
   which is the A3 arm rather than a defect.
6. **IMPORT — traced, and answered by execution.** `scrubImportedConfig` (`src/lib/importScrub.js:36`)
   **drops `faithProfile` outright** on import, and `religionStates` is worldState rather than
   settlement config. ⇒ this member adds no import-validation surface. The volume's *"import
   validates cause tokens against the closed set"* binds WF-1a's ring, not this receipt.
7. **THE VEIL, asserted rather than assumed.** `religionStates` is a member of
   `WORLD_SNAPSHOT_HARD_DENY` (`src/domain/display/worldSnapshotPublic.js:68`) and is **not** in
   `WORLD_SNAPSHOT_PUBLIC_LEDGER_ALLOWLIST` (which holds `pantheon` only). The receipt carries a
   four-token bucket and **no deity ref and no settlement-private material**, so nothing crosses the
   veil that did not cross it yesterday. A5 asserts it.
8. **§103.2 — ungated persistence: NOT INCURRED, stated affirmatively.** No normalizer, no cleanup
   arm, nothing to sit inside the flag.

---

## §3 · SCOPE AND BOUNDARY

WF-1D gives `fallCauseFor` its chartered first production caller, joins the typed fall to the war
dissolution at net-zero effective lines in a frozen file, lands the closed reader clause in the
already-built cause-table leaf, and licenses the FAITH×WAR coupling the faith volume declares.

**It does not** (named affirmatively, never by silence): touch `pulseKernel.js`, `applyWorldPulse.js`,
`peaceTerms.js`, `religionState.js`, `religiousContest.js`, `realmEvents.js`, `pantheon.js`,
`settlementRumors.js`, `heraldRouting.js`, `chroniclersLetter.js`, `simulationRules.js`,
`subsystemRowsVirtual.js`, `worldState.js` or **any component**; mint a flag, a news kind, an
`impactKind`, a Herald desk row, a kind REGISTRY, a receipt pool, a `WHAT_PHRASES` row, an
`EXACT_SECTION` row, a `KIND_SECTION` row (**refused on a measurement — §4**), a `spatialLedgers`
sub-ledger, an errand row, an annex URL, a persisted field, a top-level key, a normalizer, a
migration arm, or a test FILE; author a threshold, weight, rate, window or curve of any kind; add a
`LAYER_PATTERNS` row or an `ARGUED_UNLAYERED` entry; add or move a PRNG draw; touch `package.json`
or `package-lock.json`; light either flag anywhere; or move a same-seed golden.

⭐ **THE WF-1C BOUNDARY IS CLEAN AND MEASURED.** WF-1C's five production paths are `pantheon.js`,
`realmEvents.js`, `pulseKernel.js`, `settlementRumors.js` and `chroniclersLetter.js`
(`laneTEWF1C-receipt.md` §1). **This manifest names none of them.** The only file the two members
share is the lighting-census walker, and WF-1C reaches it first: this packet re-reads its post-WF-1C
value rather than inheriting the pre-WF-1C one (§11 step 8).

⭐ **THE WF-1D-ii BOUNDARY IS DECLARED IN ADVANCE.** The deferred FaithSection member's surface is
`religionState.js`, `pulseKernel.js`, `faithPanelModel.js`, `FaithSection.jsx` and
`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`. **This manifest names none of them either**, so the
two are path-disjoint apart from the census walker and may ride one train in either order (§7.3).

---

## §4 · ⛔⛔ THE CHRONICLE OBITUARY ROW IS REFUSED, AND THE REFUSAL IS ARITHMETIC

`WF-1A` §5 files *"the FaithSection cause-chain line, the Chronicle obituary row"* under WF-1d. That
pointer was written at `0cf18bed`, **before two later chair rulings moved its producers.** Executed
at `2cdb87fa`:

| measured | result |
|---|---|
| the Chronicle's row mechanism | `KIND_SECTION` (`src/domain/display/chroniclersLetter.js:80`), keyed on **`impactKind`** |
| its faith rows today | `pantheon_ascendancy` and `pantheon_twilight`, both `'traditions'`, at `:120` |
| candidate producer (a) — the SETTLEMENT extinction obituary | ⛔ **RE-FILED TO WF-8 at ODQ §309.** `WF-1B` §4 carries the ruling verbatim; its compile seed is `draft-WF-1B-II.md` |
| candidate producer (b) — the REALM last-seat beat | ⛔ **WF-1C's OWN M4** lands `pantheon_extinction: 'traditions'` beside its two siblings, executed and digested in `laneTEWF1C-receipt.md` §1 |
| what WF-1d itself produces | a **receipt** — `kind: 'war_termination_read'` (`warTermination.js:841`), **not an `impactKind`**. `KIND_SECTION` cannot key on it |

⇒ **A WF-1d `KIND_SECTION` ROW WOULD BE A REGISTRATION ROW FOR A KIND NOTHING MINTS**, which is
exactly the consumerless-row shape the estate forbids (ODQ §73.3 F-1; the GR-5A / INT-3B law the
family already applied to `abandoned` and to `pantheon_extinction`).

⛔⛔ **AND IT WOULD LAND SILENTLY, WHICH IS WHY IT MUST BE REFUSED AT COMPILE RATHER THAN DISCOVERED.**
Executed over the instrument: `tests/lint/heraldRouting.walker.test.js` carries *"every
`KIND_SECTION` key is itself explicitly routed"* (`:224`) and *"every recorded divergence names a
real `KIND_SECTION` key"* (`:229`) — but **no arm requires a `KIND_SECTION` key to have a minter**.
A key spelled `faith_*` or `pantheon_*` routes for free through the prefix rule
(`heraldRouting.js:357-358`), so a dead row would sit in a frozen table with every walker green.
⭐ **A6 asserts the absence instead** — no `KIND_SECTION` key this member could have added exists —
so the refusal is structural rather than a sentence in prose. **RAISED-1** puts the deliverable's
disposition in front of the chair.

---

## §5 · DEITY-DOCTRINE AND SPINE COMPLIANCE (binding, per member)

- **Faith is cultural, never theological — and this member is where the doctrine does the most
  work in the whole family, because the surface it writes to is a WAR receipt.** The existing clause
  already says *"a god named when the banners rose is no longer worshipped from the same throne"* —
  a sentence about worship, not about a god's fate — and every one of the four clauses this member
  adds continues it in the same register: a contest lost, pews turned, a garrison seated, a creed
  put out of the light. ⛔ **No clause says a god fell, failed, died, departed or was defeated**, and
  none reads a deity's authored axes. `imposed` records what the garrison did, never what the god
  failed to do.
- **No premade deity pool.** This member mints no deity, reads no deity catalogue, adds no allow-list
  entry, and **prints no deity name at all** — the clause names the act, and the deity name it might
  have wanted comes from nowhere. ⛔ **Fixtures acquire deities ONLY through the doctrine path** — the
  `SET_PRIMARY_DEITY` / `IMPOSE_CULT` verbs — never by poking config (§P2.2).
- **Typed buckets (FINITE-SEMANTICS).** `patronFallCause` is exactly one member of the landed closed
  four-token vocabulary; `FALL_CAUSE_PROSE` is a frozen four-key table whose keys A6 asserts EQUAL
  `PATRON_FALL_CAUSES`. The only free text is authored house prose in the reader clause, exactly as
  its fifteen siblings carry.
- **AI is clerk, never writer.** No AI surface reads or writes any of it, and ⭐ **no edge-shared
  bundle moves** — §6, derived from the committed metas' own `inputs`.
- **Spine req 13 (alignment engagement): alignment-EMPTY, WITH REASON, recorded rather than
  omitted.** The join is a lookup of a landed token by a ref that a landed anchor comparison already
  computed; no arm reads a deity's authored axes and no threshold anywhere is alignment-sensitive.
  ⚠ The `niche` that decides suppression upstream IS alignment-derived (`nicheOf`,
  `cultImpositionApply.js:35-37`, WF-1B's finding), but that is the EXISTING producer's input and
  this member adds no new reader of it.
- **Spine req 14 (the DM edit story): RECORDED — engine-only.** The clause is a derived reading of
  history, not editable state. The DM's lever is the EXISTING `SET_PRIMARY_DEITY` verb, which
  creates an `imposed` fall through WF-1a's classifier and therefore **can now change what a war
  receipt says** — the verb story is the existing verb, with a longer reach. Vetoable at the chair
  under the req-14 escape.
- ⚠ **DS-FTH corpus (§P2.7) — NOT REACHED BY THIS MEMBER, AND MEASURED.** DS-FTH-3 binds
  `worldState.religionStates[cid] {…, patronFalls[]}` and WF-1a discharged it by agreement; this
  member spells nothing differently and adds no field to that record. DS-FTH-1 binds
  `faithPanelModel`'s return signature — **which is why the FaithSection line is a separate member**
  (§7.2), not this one. ⇒ **no corpus row is amended and `npm run gen:dossier-prose` is NOT run.**
  Recorded, not assumed. ⚠ Volume Q4 remains OPEN at the chair; this is the engineering fact, not
  the ruling.

---

## §6 · PRICED OBLIGATIONS

| obligation | verdict |
|---|---|
| **§49/§50/§148 — the flag mint, FIVE obligations** | ⛔ **NOT INCURRED — THIS MEMBER MINTS NO FLAG.** It rides `faithUnseatingEnabled` (LANDED, WF-1a) and `warTerminationEnabled` (LANDED, the war program's). Re-executed: `ENGINE_GATED_VIRTUAL_RULE_KEYS` holds **24** members; the key is **absent from `DEFAULT_SIMULATION_RULES`** (VIRTUAL); its certification row is at `subsystemRowsVirtual.js:1248`. **No registry row moves**, so `virtual` 24, `union` 81 and `union − governed` 56 in `tests/soak-harness/coveringArrayCoverage.test.js` are untouched and `contributionLedgerShape.test.js`'s literal stays **24**. ⚠ **The by-name read census over `src` moves ONE**: WF-1a minted the key with one read, WF-1B hoisted the fold's read into a local, WF-1C adds a second in `pulseKernel.js`, and **this member adds one more, in `warTermination.js`, strict `=== true`, not reached through a frozen-list `.every()`** — the conjunction-gate hole is closed by construction (§P1 R-WF-3) |
| **§104.4 — edge-shared bundle closures** | **NOT INCURRED, DERIVED AT COMPILE from the committed metas' own `inputs` arrays, never re-typed.** Executed over all five at `2cdb87fa`: `aiCharterBundle` **110** inputs, `aiOutputSchemaBundle` **111**, `aiGroundingBundle` **66**, `analyticsEventsBundle` **2**, `intentAtlasBundle` **2**. ⭐ **Not one of this packet's four production paths appears in ANY closure** — and this was derived rather than waved through precisely because M4 is a COMMENT-ONLY edit, which is the trigger shape §104.4 fires on |
| **test census (the lighting contract)** | **INCURRED, TITLES ONLY.** ⛔ **THE FIGURE THIS PACKET STARTS FROM IS POST-WF-1C AND MUST NOT BE INHERITED FROM EITHER PACKET.** At `2cdb87fa` the live pinned tuple is **`2485/364/2121/20611/5768`** (`tests/lint/sovereigntyLightingContract.walker.test.js:4870`, re-read from the walker's own trailing record); WF-1C's executed delta is `+0/+0/+0/+7/+0`, so this member's starting tuple is **`2485/364/2121/20618/5768`** — **re-read at the implementing tip, never inherited** (§11 step 8). ⭐ **This member mints NO test file**: the battery extends `tests/domain/warTermination.test.js`, already credited, adding **six** `it(` calls to an EXISTING `describe`. **Predicted delta `+0/+0/+0/+6/+0`.** ⚠ Because `files` does not move, the SEQUENCED census cannot stop at its first figure and hide the rest (§P5). ⛔ **Authorizing decision: ODQ §320** (§299.4) |
| **§102.2 / §P3b — mutation coverage** | **NOT INCURRED.** No file is added under `tests/lint/`; M7 amends an existing one and the battery lands in `tests/domain/` |
| **§85.4 — seeded chooser / pool registry** | **NOT INCURRED.** The lookup and the selector are total functions of already-computed values and draw nothing. **Zero new PRNG streams, dark AND lit.** ⭐ **`pulseKernel.js` is not touched, so the kernel's 22 `rng.fork` sites and their order are provably unmoved** — the stream identity THE PROMISE protects is not in play |
| **size baseline / hot files** | ⛔⛔ **INCURRED, AND IT IS THIS MEMBER'S SHARPEST CONSTRAINT.** `warTermination.js` **818 against a frozen 818 — ZERO HEADROOM**, and §2.2 proves the edit holds it there with `eslint` clean. `warTerminationCauseTables.js` 71 → 83 against an 800 layer ceiling with **no** baseline entry. `couplingRegistry.js` 109, `patronFall.js` 28 (comment-only). ⛔ No `PACKET_STANDARD` hot-file is named. ⛔ The other three ZERO-HEADROOM-class files are untouched and re-measured for the record: `pulseKernel.js` **1581/1581**, `applyWorldPulse.js` **941/941**, `peaceTerms.js` **797** |
| **`negativeAssertionAnchor`** | **INCURRED, CEILING ZERO for any NEW file — and this member adds none.** Every negative arm added to `warTermination.test.js` carries an `// anchored:` line or routes through `tests/helpers/anchoredNegatives.js` **by name on the same line**. This member's signature pins are absences — *a receipt with no named fall carries NO key* (A3), *a legacy record gets NO invented cause* (A4), *no `KIND_SECTION` row exists* (A6) — so **three arms owe anchors**. ⚠⚠ **A multi-line `// anchored:` counts only by its LAST line, and WF-1C's landing paid this bill THREE TIMES in one battery** (`laneTEWF1C-receipt.md` §8 item 4): put the marker on the line immediately above the assertion as that comment's LAST line, and hoist a continuation-style `expect(x)\n  .not.toContain(y)` into a local first. Preflight is §11 step 1 |
| **coupling registry — BOTH halves** | ⛔ **INCURRED, PAIR HALF ONLY, AND THE BILL IS EXECUTED.** §2.5 carries the walker's own message, the 49-row / zero-`FAITH→WAR` census, and the measurement that no exact-count pin blocks the row. The UNLAYERED half is NOT incurred: `UNLAYERED_BASELINE_CEILING` **179** and `ARGUED_ROSTER_CEILING` **20** both stand still |
| **hand-keyed address rot (`.prose-numerics-baseline.json`)** | ⭐ **NOT INCURRED, AND MEASURED RATHER THAN ASSUMED.** The baseline holds **413** rows; executed, **ZERO** of them key `warTermination.js`, `warTerminationCauseTables.js`, `patronFall.js` or `couplingRegistry.js`. ⚠ **It is INCURRED for the deferred sibling and the figure is recorded here so that member does not discover it**: `FaithSection.jsx` carries **TWELVE** rows at lines `49`, `53`, `174`, `176`, `179`, `188` |
| **wizard-news authoring walker** | **NOT INCURRED.** This member authors no news entry and no `impactKind`; the debt ledger stays at **19** entries with none for any named file |
| **declared shift** | **NOT INCURRED, stated affirmatively.** Dark is byte-identical by construction and the fixture proves it at the byte level (§9 A1/A3). The lit path is exercised by no preset, no soak case and no golden corpus — executed: **no preset declares `faithUnseatingEnabled`** — so no same-seed golden can reach it. ⛔ **Any motion in a same-seed golden is a STOP, never a re-record** (§P7.11) |
| **chair value signature (§42/§43, §78 F-5)** | **NOT INCURRED.** This member authors **no number**. The four clauses are reader prose, the gate is a strict boolean read, and the lookup spells no literal. ⛔ No `<NAME>_TUNING` table is minted and no existing table gains a member, so F-5's one-table-per-leaf arm is not reached |
| **§77 premise map** | **INCURRED.** WF's train inherits the obligation from birth (ODQ §78.3). Rows in §10 |
| **`docs/**.md` naked-claim debt** | **INCURRED — PER CLAIM.** This packet is a `docs/**.md` write and a new claim key reds a test that passes today. ⛔⛔ **TRANSCRIBING THE VOCABULARY IS ITSELF AN OCCURRENCE** — the recorded class that convicted the chair's own signed preamble at ODQ §313.2 and this family's own §6 once already (J-TEWF1B-3). The cure is to paraphrase, never to tag. ⭐ **The live claim regex (`tests/docs/enforcement-claims.test.js:40`) was run over this packet's bytes BEFORE writing finished and returns no match**; the implementer re-runs it at its own tip. ⚠ **The suite is RED at this base on a BANKED arm that is not this member's** — see §12 item 6 |

---

## §7 · THE SPLIT, PRICED — ONE MEMBER vs TWO, WITH NUMBERS

*Every figure executed at `2cdb87fa`. `PACKET_STANDARD` "Default hard scope budget" binds:
"If the work cannot fit, the agent stops and proposes the smallest split. The agent may not quietly
renegotiate the budget."*

### 7.1 The three chartered deliverables, and what each actually costs

| deliverable | production files it needs | why |
|---|---|---|
| **D1 · the dissolution join** | `warTermination.js` · `warTerminationCauseTables.js` · `patronFall.js` (comment) · `couplingRegistry.js` (registration) | §2 |
| **D2 · the FaithSection cause-chain line** | `religionState.js` · `pulseKernel.js` · `faithPanelModel.js` · `FaithSection.jsx` | the panel model reads **only** `settlement.config` — its own header states the invariant — so the fall must arrive through `config.faithProfile`, whose sole writer is `projectReligionStateOntoSettlement` (`religionState.js:603`); and the flag is not in that function's scope, so it rides a defaulted sixth parameter injected from the kernel's single call site (`pulseKernel.js:1773`) |
| **D3 · the Chronicle obituary row** | **NONE — no producer exists** | §4 |

⭐ **D2's KERNEL TOUCH IS MEASURED NET ZERO AND THE MEASUREMENT IS RECORDED HERE SO THE SIBLING DOES
NOT RE-DISCOVER IT.** Packing `simulationRules.faithUnseatingEnabled === true` onto the existing
single-line call at `pulseKernel.js:1773` leaves the file at **1581/1581 EXACT** with `eslint`
TRUE_EXIT=0, and `simulationRules` (`:307`) is in scope — verified by an executed top-level
brace scan showing `simulateCampaignWorldPulse` (opening `:241`) is still open at `:1773`.

### 7.2 The two shapes

| limit (`PACKET_STANDARD` default) | **Shape 1** — ONE member (D1+D2) | **Shape 2** — TWO members (D1 / D2) |
|---|---:|---:|
| behavior families 1 | 2 ⛔ | 1 / 1 ✓ |
| new persisted record families ≤1 | 0 ✓ | 0 / 0 ✓ |
| named writers per state 1 | 0 ✓ | 0 / 0 ✓ |
| feature flags ≤1 | 0 ✓ | 0 / 0 ✓ |
| user-facing surfaces ≤1 | 1 ✓ | 0 / 1 ✓ |
| direct production consumers ≤2 | 3 ⛔ | 1 / 2 ✓ |
| new logic-bearing leaves ≤2 | 0 ✓ | 0 / 0 ✓ |
| **existing logic files modified ≤3** | **6 ⛔ +3** | **3 (AT CAP) / 3 (AT CAP)** ✓ |
| registration-only files ≤3 | 1 ✓ | 1 / 0 ✓ |
| **handwritten files ≤12** | **14 ⛔ +2** | **7 ✓ / 9 ✓** |
| new/changed effective production lines ≤400 | ~75 ✓ | ~35 / ~40 ✓ |
| shared/hot-file delta ≤15 | 0 hot named ✓ | 0 / 0 ✓ |
| **acceptance cases ≤8** | **≥10 ⛔ +2** | **6 ✓ / 6 ✓** |
| generated artefacts | `warFaith.generated.js` | — / `warFaith.generated.js` |
| **overrides needed** | **THREE** | **NONE** |
| census delta | `+0/+0/+0/+12/+0` | `+0/+0/+0/+6/+0` each |

⛔ **SHAPE 1 IS REFUSED BY ARITHMETIC ON THREE HARD CAPS SIMULTANEOUSLY** — existing logic files
6 > 3, handwritten 14 > 12, acceptance ≥10 > 8 — exactly as chartered WF-1 was refused on five
(preamble R-WF-8; `WF-1A` §0). ⭐ **AND THE SPLIT LINE IS NOT ARBITRARY**: the two halves share **no
production path at all**, no data structure, no flag read site and no obligation. D1 is a WAR-file
seam that owes a coupling row; D2 is a display projection that owes a DS-FTH-1 corpus amendment and
a `npm run gen:dossier-prose` run in the same commit (Lane P's standing law). Neither obligation
touches the other's files.

### 7.3 What the deferred member owes, recorded so it is not re-discovered

`laneTCWF1D-WF-1E-seed.md` carries it in full. The five figures that would otherwise be found at a
terminal:

1. ⛔ **DS-FTH-1 BINDS `faithPanelModel`'s RETURN SIGNATURE VERBATIM.** Executed at
   `src/data/dossierStateProse/warFaith.generated.js:2113`: *"`faithPanelModel(settlement) →
   {patron, cults, ranks, piety, unaffiliated, mandate, sinkSentence, live}`"*. A new key **amends**
   that row ⇒ the corpus source `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` is edited and
   `npm run gen:dossier-prose` runs in the SAME commit. ⚠ Volume Q4 is OPEN at the chair.
2. ⛔ **`FaithSection.jsx` CARRIES TWELVE HAND-KEYED `.prose-numerics-baseline.json` ROWS**, at lines
   `49`, `53`, `174`, `176`, `179`, `188`. ⭐ **The render belongs in the existing `<Cause>` block at
   `:195-198`, which sits BELOW every one of them**, and the model destructure at `:75` takes a name
   on an EXISTING line. ⇒ **add no line above `:174` and the bill is zero**; add one and six rows
   rot. **RE-ADDRESS such rows; NEVER delete them.**
3. `pulseKernel.js` **1581/1581**, net zero by packing — §7.1, executed.
4. `scrubImportedConfig` **drops `faithProfile` entirely** on import, so the panel key needs no
   import-validation arm.
5. ⚠ **The dark fence for D2 is NOT the ring's presence.** Gating on `state.patronFalls` alone would
   render the line in a world that was lit once and then darkened. The flag read is what makes the
   fence honest, which is why the kernel touch is owed rather than optional.

**⭐ THIS COMPILE RECOMMENDS SHAPE 2, AND RAISES THE MEMBER'S NAME RATHER THAN CHOOSING IT.**
⛔ **THE PATH IS `docs/implementation/packets/fp/WF-1D.md`, WITHOUT A `-I` SUFFIX** — J-TEWF1B-2 is
the recorded precedent: a suffix names a sibling that may never open, and a LANDED packet whose own
manifest names a path it does not occupy is a false record. Whether the deferred half lands as
`WF-1D-II`, as a new `WF-1E`, or re-filed (the §309 shape) is a chair act; **RAISED-3**.

---

## §8 · EXACT CHANGE MANIFEST

⚠ **Change-path collision check, EXECUTED at `2cdb87fa`:** `docs/implementation/PACKET_MANIFEST.json`
holds **131 rows, of which 130 are LANDED and 1 SUPERSEDED — ZERO non-terminal.** No live packet
reserves any path below. ⛔ **A change path is reserved at EVERY non-terminal status, DRAFT
included** (§45.2; §P7.14), so the promoting lane **re-runs this check at its own tip** — where
WF-1C's landing will have taken the count to 132. ⭐ WF-1C's five production paths and the deferred
sibling's four do not intersect this manifest at all (§3).

### Handwritten (7 files)

| # | action | path | symbol / region | max eff Δ | instruction |
|---|---|---|---|---:|---|
| M1 | MODIFY | `src/domain/worldPulse/warTermination.js` | the cause-table import block · `readWarTerminations` · `terminationReason` · the receipt literal | **0 — NET ZERO, MANDATORY** | ⛔⛔ **ZERO-HEADROOM FILE: 818 effective against a frozen `scripts/.size-baseline.json` literal of 818, pinned EXACT IN BOTH DIRECTIONS by `tests/lint/sizeBaseline.test.js` and re-enforced by a generated per-file `max-lines` override. GROWTH HAS NO LAWFUL CURE.** Apply the seven sites of §2.3 exactly: collapse the 4-line cause-table import to 2 and add the `patronFall.js` import; add the BY-NAME `unseating` gate; give `terminationReason` a `fallCause = null` parameter on the existing `dissolvedCauseTypes,` line; swap the `dissolvedCase` map callback to `dissolvedClauseFor(type, fallCause)`; add the one-line per-deployment lookup above `attackerName`; pass `fallCause` on the existing argument line; append the conditional `patronFallCause` spread to the receipt's `decidingTerm,` line. ⛔ **Add no other line, delete no other line. No two statements share a line.** §11 step 7 makes re-measuring 818 a STOP on BOTH sides of the edit |
| M2 | MODIFY | `src/domain/worldPulse/warTerminationCauseTables.js` | `FALL_CAUSE_PROSE` (new), `dissolvedClauseFor` (new) | **≤14** | The closed four-key table and the one selector of §2.4, placed ABOVE `TERMINATION_PEACE_PROSE` so the three module-load totality assertions keep their position at the file's foot. ⛔ Keys EXACTLY `PATRON_FALL_CAUSES`, codepoint order, no fifth. ⛔ The selector returns the base clause unchanged for every type but a typed `sacred_claim` fall. ⚠ **A DOCBLOCK IS POSITIONAL** — placing a new declaration directly above an existing export orphans that export's JSDoc onto the new one; WF-1B's landing paid this as nine `typecheck:domain:strict` errors against a GREEN `typecheck:ratchet`. Base measured **71/800** |
| M3 | MODIFY | `src/domain/certification/couplingRegistry.js` | `COUPLING_REGISTRY` | **≤14** | ⛔ **ONE row, and it lands in the SAME COMMIT as its live reader** (ODQ §73.3 F-1; both directions red otherwise). `direction: 'FAITH→WAR'`; `read` and `counterforce` **BOTH** addressed into `src/domain/worldPulse/warTermination.js` (the walker's `licensingRows` matches on direction + importer, and `couplingReceiptSample.test.js:116` needs the address count to outgrow the row count); `owningVolume: 'WAR'`; `owningWave: 'WF-1d'`; `flags` naming BOTH `warTerminationEnabled` and `faithUnseatingEnabled`; `receiptField` naming the receipt key. ⚠ Re-read `COUPLING_REGISTRY_SCHEMA_VERSION` (**4** at this base) and shape the row to the live schema, never to this cell. Base measured **109/800** |
| M4 | MODIFY | `src/domain/worldPulse/patronFall.js` | the `THE FILE NAME IS LOAD-BEARING` header paragraph | **0 — COMMENT ONLY** | ⛔ **CORRECT THE FALSE CLAIM.** The paragraph says the leaf sits *"beside its ONLY importer"*; this member makes that untrue. Re-word to name BOTH importers and to say that the cross-layer read is LICENSED by M3's registry row rather than absent — so a later WF compiler prices a third importer correctly. ⛔ Change no code. ⚠ §104.4 fires on a comment-only edit to a closure member; §6 derives that this file is in none. Base measured **28** |
| M5 | TEST | `tests/domain/warTermination.test.js` | the EXISTING `describe` covering opening pins and dissolution | — | The six acceptance cases of §9, as exactly **six** straight-line `it(` calls with string-literal titles, added INTO an existing `describe`. ⛔ **No new `describe`, no `.each`, no `runIf`, no nesting** (§P5) — so `suiteTitles` does not move. ⛔ The ring is seeded **through the landed `recordPatronFall` writer**, never by poking the object, and the drive spells the LITERAL `faithUnseatingEnabled: true` |
| M6 | TEST | `tests/lint/sovereigntyLightingContract.walker.test.js` | the census tuple (`:4870`) | — | Re-derive the WHOLE five-figure tuple in THIS commit. ⛔ Never inherit; never patch one figure. Predicted `+0/+0/+0/+6/+0` from the **post-WF-1C** tuple, which §11 step 8 re-reads from the live file. Authorizing decision **ODQ §320** |
| M7 | DOC | `docs/implementation/packets/fp/WF-1D.md` | whole file | — | This packet, landed at promotion. ⛔ The path carries **no `-I` suffix** (§7.3). ⚠ Any `docs/**.md` write is naked-claim gate risk — run the live claim regex BEFORE committing, and paraphrase rather than transcribe its vocabulary |

### Generated

**NONE.** No edge-shared bundle closure contains any named path (§6, derived from the metas' own
`inputs`). No generator runs. `npm run gen:dossier-prose` is NOT run (§5).

### Budget reconciliation

| limit | this packet |
|---|---|
| behavior families | **1** |
| new persisted record families | **0** |
| named writers for state that changes | **0 — nothing is persisted** |
| feature flags | **0 new, 0 new conjunctions** (both flags are landed) |
| user-facing surfaces | **0** |
| direct production consumers | **1** (`warTermination.js` consuming `patronFall.js`) of 2 |
| new logic-bearing production leaves | **0** |
| existing logic-bearing production files modified | **3** of 3 (AT CAP: M1, M2, M4) |
| registration-only production files | **1** of 3 (M3) |
| handwritten files total | **7** of 12 |
| new/changed effective production lines | **≤35** of 400 |
| shared/hot-file deltas | **0 — no `PACKET_STANDARD` hot file is named; M1 is NET ZERO against a frozen baseline** |
| acceptance cases | **6** of 8 |

**Overrides approved before dispatch: NONE — and none is needed.**

---

## §9 · ACCEPTANCE (closed denominator — 6 cases)

⚠⚠ **RUN THE FIXTURE AND PRINT WHAT THE ENGINE DID BEFORE WRITING ANY PIN.** This lane did, at
`2cdb87fa`, against a `git archive` tree rather than the working tree. The executed transcript is in
`laneTCWF1D-receipt.md` §3; its load-bearing results are quoted per case, and **one of them names a
hazard that would have made two of these pins silently vacuous.**

| id | case |
|---|---|
| A1 | ⭐ **MAIN BEHAVIOUR — the receipt names the fall, and it is the LIT-MUTANT CONTROL.** Lit on both flags, a sacred war whose pinned patron anchor moved, against a settlement whose ring records that patron's fall, carries `patronFallCause: 'discredited'` on the receipt AND the typed clause in `reason`. ⭐ **MEASURED:** ring `{"patronFalls":[{"ref":"sun-old","cause":"discredited","atTick":7}]}` → `patronFallCause = "discredited"`, `reason = "…no longer worshipped from the same throne — the creed lost its rightful claim in the town it was named from. Still, …"`. ⛔ The **LITERAL** `faithUnseatingEnabled: true` spelling is load-bearing: `mechanismLitCoverage` grants AUTO credit only on a literal and a computed key attributes to NO key. ⛔ The deity-free corpus golden is NOT this fence and is vacuous for faith (§P2.2); the fixture's deities arrive through the doctrine verbs and its ring through `recordPatronFall` |
| A2 | **ABSENT vs FALSE vs LIT, at the byte level.** `{}` and `{faithUnseatingEnabled:false}` produce a **byte-identical receipt** over the same fixture; only the literal lit drive moves a byte. ⭐ **MEASURED:** the dark receipt serializes identically to the lit-with-no-recorded-fall receipt (`true`), and differs from the lit-with-fall receipt (`false`) — so the fence has something to see and the differential is not blind. The case also asserts the **other fifteen** casus clauses are byte-identical across the landing, which is the control on M2's selector refactor |
| A3 | ⛔⛔ **NO INVENTED CAUSE, AND NO KEY — the honesty arm.** Lit, with a ring that records nothing for the pinned ref, the receipt carries **NO `patronFallCause` key at all** (absent, never `null`) and the clause is exactly today's. ⭐ **MEASURED:** `patronFallCause = undefined`, `reason` byte-identical to the dark arm. ⚠ **Spectacle-aware per §P2.9:** this member reads no `drawsPilgrims`-class predicate and no scale band, so no negative here can be silently satisfied at `scaleBand >= SPECTACLE_SCALE`. Stated affirmatively. **anchored** |
| A4 | ⭐ **THE CHARTERED LEGACY ARM — `anchor_unavailable` GETS NO INVENTED CAUSE.** A deployment predating the anchors reaches `causeState: 'anchor_unavailable'` with `dissolvedCauseTypes: []`, so the lookup's own guard is false and nothing is named. ⭐ **MEASURED, and the finding matters:** the honesty law is satisfied by the EXISTING control flow rather than by a new guard, which is why MUTANT (c) is shaped to delete the guard and prove it still reds rather than to prove an unreachable branch (§P2.9's first vacuity). *"missing history is not evidence"* — `DESIGN_FP_FAITH.md` §5-WF-1, honoured verbatim. **anchored** |
| A5 | **LIFECYCLE AND THE VEIL — nothing persists, and the ledger shape is unchanged.** `readWarTerminations` mutates nothing (the landed WR-1 no-mutation pin, re-asserted across this landing); `CONDITIONAL_LEDGER_KEYS` is unchanged (zero new top-level keys, ASSERTED not assumed); a field-absent and a field-present world round-trip through `ensureWorldState` byte-identically; `religionStates` is in `WORLD_SNAPSHOT_HARD_DENY` and not in the public allowlist, and the receipt carries a closed token and **no deity ref**, so the VEIL claim is asserted rather than reasoned |
| A6 | ⛔⛔ **THE REGISTRATION AND VOCABULARY TOTALITY, WITH THE REFUSED ROW ASSERTED AS AN ABSENCE.** `Object.keys(FALL_CAUSE_PROSE)` EQUALS `PATRON_FALL_CAUSES` exactly (so a fifth cause reds here rather than rendering blank); the FAITH→WAR pair is licensed by exactly one `COUPLING_REGISTRY` row whose `read` module is `warTermination.js`; `UNLAYERED_BASELINE_CEILING` **179** and `ARGUED_ROSTER_CEILING` **20** both stand still; and **`chroniclersLetter.js`'s `KIND_SECTION` gains NO key** — asserted as an absence, because §4's measurement is that nothing in the estate would red on a dead row. **anchored** |

---

## §10 · §77 PREMISE-MAP ROWS

| id | premise | grade at `2cdb87fa` | how it dies |
|---|---|---|---|
| P-1 | `warTermination.js` is 818 effective against a frozen literal of 818 | EXECUTED — eslint `Linter`; `scripts/.size-baseline.json` | any landing moves it ⇒ re-measure at the implementing tip; **never inherit this figure** |
| P-2 | `DISSOLVED_CAUSE_PROSE` has exactly ONE reader in `warTermination.js` and is not re-exported from it | EXECUTED — one hit at `:431`; only `WAR_CAUSE_DISSOLUTION` is re-exported, `:94` | a second reader appears ⇒ the import swap stops being net-zero ⇒ **STOP and re-price** |
| P-3 | `fallCauseFor` has ZERO production callers | EXECUTED — one definition (`patronFall.js:146`), zero `src` callers | another wave calls it first ⇒ §P2.11 is breached and WF-1a's producer-first claim needs re-pricing |
| P-4 | `COUPLING_REGISTRY` carries ZERO `FAITH→WAR` rows and zero rows naming `warTermination.js` | EXECUTED — 49 rows enumerated | a row appears first ⇒ M3 becomes an amendment, not a mint; re-derive `licensingRows` |
| P-5 | no exact-count pin bounds `COUPLING_REGISTRY.length` | EXECUTED — `couplingRegistry.test.js` / `couplingDesk.walker.test.js` arms read | a count pin lands ⇒ M3 owes it; re-read the live arms, never this row |
| P-6 | no path this packet names is in any edge-shared closure | EXECUTED over five committed `*.meta.json` `inputs` (110/111/66/2/2) | a bundle's inputs grow ⇒ §104.4 becomes INCURRED, and M4's comment-only edit alone would trigger it |
| P-7 | the lighting tuple is `2485/364/2121/20611/5768` at this base and `+0/+0/+0/+7/+0` after WF-1C | EXECUTED — walker `:4870`; WF-1C's own landing receipt | ⛔ **re-read at the implementing tip. This member's starting figure is WF-1C's ENDING figure, and inheriting either packet's number is the recorded failure** |
| P-8 | `PACKET_MANIFEST.json` has ZERO non-terminal rows | EXECUTED — 131 rows, 130 LANDED + 1 SUPERSEDED | WF-1C's landing takes it to 132; a sibling DRAFT opens ⇒ re-run the collision check |
| P-9 | `.prose-numerics-baseline.json` keys none of this packet's files | EXECUTED — 413 rows, zero matches on all four | a landing adds a row against one ⇒ re-address it, NEVER delete it |
| P-10 | `KIND_SECTION` has no arm requiring a key to have a minter | EXECUTED — `heraldRouting.walker.test.js:224`/`:229` read in full | such an arm lands ⇒ §4's refusal gains a second, machine reason; the refusal itself does not change |
| P-11 | `scrubImportedConfig` drops `faithProfile` on import | EXECUTED — `importScrub.js:36` | the scrub narrows ⇒ the DEFERRED member owes an import arm; this member is unaffected |

---

## §11 · PREFLIGHT (run before the first edit; each is a STOP if it fails)

0. `git rev-parse HEAD` is **WF-1C's landed tip**, or an accepted descendant with every §15 symbol
   and every §8 target byte-identical across the window — discharged by an **EXECUTED
   `git diff --name-only`, quoted**, never by an assertion that the window "looks docs-only" (§P8).
   ⛔ **WF-1C must be LANDED on `claude/composite-r4` first.** At this compile it was terminalized at
   `253f2028` in a detached worktree awaiting the chair's CAS, and the branch tip was still
   `2cdb87fa` — re-read the ref, do not trust this sentence.
1. ⛔ **§31 ANCHOR PREFLIGHT, MANDATORY, BEFORE THE MEMBER PROOF IS DECLARED GREEN** — bare,
   in-shell, never piped:
   `npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?`
   Measured at this base: **9 passed, TRUE_EXIT=0.** ⚠⚠ Put every `// anchored:` marker on the line
   immediately above its assertion, as that comment's **LAST** line; WF-1C's landing paid this bill
   three times in one battery.
2. ⛔ **`docs/implementation/BASE_STATE.json` admissibility is RE-READ AND RE-DISCHARGED HERE**, at
   this member's own tip. WF-1A, WF-1B and WF-1C each measured it **NOT CITABLE** independently;
   **that finding is inherited not at all.** Every figure here is re-executed regardless —
   capsule-only compilation is refused (§P8).
3. Packet status is **READY**. It is DRAFT as written — do not dispatch.
4. All four §8 production targets are clean; no foreign dirt on them. ⛔ **Preserve any foreign WIP
   elsewhere — it is the owner's.** Never `git add -A`/`-u`/`.`; stage explicit paths.
5. Every §15 symbol resolves in the live tree.
6. `npm run validate:packets` exits 0 at the tip, and the §8 collision check re-runs.
7. ⛔⛔ **RE-MEASURE `warTermination.js` WITH ESLINT'S OWN `Linter` BEFORE THE EDIT AND AGAIN AFTER
   IT. BOTH READINGS MUST BE THE SAME NUMBER, AND THAT NUMBER MUST EQUAL THE LIVE
   `scripts/.size-baseline.json` LITERAL** (818 at this base — re-read it, never inherit it). A
   difference in either direction is a STOP: growth has no lawful cure, and a shrink owes the
   literal lowered in the same commit. Re-measure `warTerminationCauseTables.js` (71),
   `couplingRegistry.js` (109) and `patronFall.js` (28) the same way. ⛔ Also confirm `pulseKernel.js`
   **1581** and `applyWorldPulse.js` **941** are UNTOUCHED — this member names neither.
8. ⛔ **RE-READ THE LIGHTING TUPLE FROM M6's OWN RECORD AT THE POST-WF-1C TIP.** The figure in §6 is
   this compile's arithmetic across another member's landing, and **an inherited census figure is
   the recorded failure**. Re-read the coupling ceilings (`179`, `20`), the registry schema version
   (`4`) and the `.prose-numerics-baseline.json` row count (`413`) from the live files too.
9. ⛔ **Capture the own-footprint golden BEFORE wiring**, on a deity-BEARING fixture built through
   `SET_PRIMARY_DEITY` / `IMPOSE_CULT`. The deity-free corpus golden is NOT this fence — it is
   vacuous for faith (§P2.2).
10. ⛔⛔ **RUN THE A1, A3 AND A4 FIXTURES AND PRINT WHAT THE ENGINE DID, BEFORE WRITING THEIR PINS.**
    Standing chair law. §9 carries this lane's executed transcript; the implementer re-runs at its
    own tip and compares. ⚠⚠ **THIS MEMBER'S OWN FIXTURE HAZARD, MEASURED:** a LIT world whose ring
    records **no fall for the pinned ref** is byte-identical to dark (A3's whole point), so a
    dormancy fixture that lights the flag and never records a fall produces **base == cure** and a
    silently vacuous pin. Seed the ring through `recordPatronFall` with the ref the deployment
    actually pinned, and PRINT the ring before asserting on it.

---

## §12 · CHECKS

```
npx vitest run tests/domain/warTermination.test.js \
  tests/domain/warTerminationPulse.test.js \
  tests/domain/warCostsIntegration.test.js \
  tests/domain/warCoalitionWr6.test.js \
  tests/domain/patronFall.test.js \
  tests/domain/couplingRegistry.test.js \
  tests/domain/couplingReceiptSample.test.js \
  tests/domain/subsystemRowsWar.test.js \
  tests/domain/brokeragePlantHandoffPins.test.js \
  tests/domain/religionDormancy.byteIdentity.test.js \
  tests/lint/couplingInclusion.walker.test.js \
  tests/lint/couplingDesk.walker.test.js \
  tests/lint/ruinFilterRoster.walker.test.js \
  tests/lint/allianceWebRiskConsumers.walker.test.js \
  tests/lint/heraldRouting.walker.test.js \
  tests/lint/sizeBaseline.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/property/pactFormationDormancyFence.test.js \
  tests/property/worldpulseDeityGolden.test.js \
  tests/scripts/implementationPackets.test.js
```

⚠ **The eleven suites that import `COUPLING_REGISTRY` are all named above** — enumerated by
execution, not by memory, because M3's row is visible to every one of them.

1. ⚠ **Gate-reading law.** ⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** —
   `test:ratchet` re-acquires and self-deadlocks, and **exit 3 is the mutex giving up, not a red.**
   Run `check:tail` **bare**, from a fresh shell, with `; echo TRUE_EXIT=$?`, and **outlast it in
   your own turn.**
2. ⚠ **Trust no exit status you did not capture**, and read only SELF-NAMED logs — a shared log
   directory lies by recency. ⛔ **This lane proved the point on itself**: a `vitest` run piped
   through `tail` reported TRUE_EXIT=0 over a suite that was RED; re-run bare, it reported
   TRUE_EXIT=1.
3. **Report BOTH typecheck configurations** (`typecheck:ratchet` / `typecheck:domain:strict`) at
   their exact floors, naming the config AND the window. A per-file `tsc` is a vacuum. ⚠ M2 adds a
   new exported function and M1 widens a receipt literal's shape — **exactly the shape that produced
   nine `typecheck:domain:strict` errors against a GREEN `typecheck:ratchet` at WF-1B's landing.**
   Type the selector's return explicitly rather than letting it infer.
4. ⚠ **The lighting census is SEQUENCED and stops at its first red figure.** `files` does not move in
   this member, which is what keeps the later figures reachable — but verify, do not assume.
5. ⛔ `npx eslint src/domain/worldPulse/warTermination.js` must be clean — the generated per-file
   `max-lines: { max: 818 }` override is the second enforcer of §11 step 7.
6. **A pre-existing-red list is derived from `scripts/.test-ratchet-baseline.json` WHOLE, never from
   memory.** Measured at this base: **11** banked entries against `tests/lint/testRatchet.test.js`'s
   `CEILING = 17`; `measuredAtSha` `4deb4f02`; `totalTests` **28274** and `totalFiles` **2387** are
   FLOORS transcribed from the terminal's own executed receipt, never predicted.
   ⛔ **`tests/docs/enforcement-claims.test.js` IS RED AT THIS BASE AND IT IS NOT THIS MEMBER'S.**
   Executed bare: `1 failed | 20 passed (21)`, TRUE_EXIT=**1**, on the arm *"every completeness claim
   carries an @enforced-by tag with ≥1 target"* — which is **banked entry 5 of 11**, so
   `test:ratchet` does not red on it. ⭐ **The second arm WF-1B found unbanked is now GREEN**, which
   is this lane's independent confirmation that the ODQ §313 paraphrase cure landed and held. Compare
   failure identities against a committed-base run and **do not repair unrelated rows.**

---

## §13 · JUDGMENT CALLS (vetoable) AND RAISED MATTERS

### Judgment calls

- **J-TCWF1D-1 — the join imports `fallCauseFor` DIRECTLY and lands a coupling row, rather than
  taking an injected reader.** Both shapes were BUILT and MEASURED and both hold `warTermination.js`
  at 818 EXACT with `eslint` clean and 50/50 war suites. Chose the direct import because the injected
  variant costs **two extra production files** — `pulseKernel.js` (1581/1581, ZERO HEADROOM) for the
  import specifier and `religiousContest.js` for a `fallCauseFor` re-export to keep the kernel at
  +0 — which takes the member to **four** existing logic files (over the cap of 3), puts it back onto
  a path WF-1C reserves, and hides behind an injection seam the very coupling
  `DESIGN_FP_COUPLINGS.md` exists to declare. The registry row the direct import owes is the
  chartered FAITH×WAR coupling made visible. *Say "veto" for the injected shape; §8 then gains
  `pulseKernel.js` and `religiousContest.js`, the member needs a scope override, and §6's coupling
  row becomes NOT INCURRED.*
- **J-TCWF1D-2 — the prose selector moves to `warTerminationCauseTables.js` rather than staying
  inline.** Chose it because it is what makes the join net-zero at all: the freed import specifier
  buys the faith import. It also puts the fall clause beside the table it decorates, in the leaf
  whose own header says it exists to absorb exactly this pressure. *Say "veto" to keep
  `DISSOLVED_CAUSE_PROSE` inline; §2.2's arithmetic then loses its −1 and the member owes an
  extraction recipe (L8) instead of a seam.*
- **J-TCWF1D-3 — the attacker's ring is consulted before the defender's.** When BOTH pinned anchors
  moved in the same tick, the receipt names the ATTACKER's fall. Chose it because the receipt is
  attacker-centric by construction — *"One physical deployment still yields one attacker-centric
  receipt"* (`warTermination.js:618`) — and its `attackerName` already frames the sentence. *Say
  "veto" for defender-first or for naming both; naming both widens the clause to a list and A1's
  assertion becomes an ordering pin.*
- **J-TCWF1D-4 — the receipt carries the TOKEN and the sentence carries the PROSE, both.** Chose
  landing both over landing either alone: a token-only join leaves the charter's *"the dissolution
  receipt prints the typed cause"* unmet, and a prose-only join puts a closed-vocabulary fact into
  free text against FINITE-SEMANTICS. *Say "veto" to drop the receipt key; A1 then asserts only the
  sentence and a later consumer has to parse prose.*
- **J-TCWF1D-5 — the acceptance battery extends `tests/domain/warTermination.test.js`.** Chose it
  over `tests/domain/patronFall.test.js` or a new `tests/lint/**` walker: the dissolution fixtures
  and the landed `sacred_claim` pins already live there, a new `tests/lint/` file would red two
  censuses and owe a mutation-coverage row it cannot honestly earn without a sweep this tree
  forbids, and the census sits at its pinned ceiling where *"just census it"* is unavailable. *Say
  "veto" to split the battery; the census prediction then decomposes across two files.*

### RAISED for the chair — four matters this lane may not decide

- ⛔⛔ **RAISED-1 — WF-1d's THIRD CHARTERED DELIVERABLE HAS NO PRODUCER LEFT, AND THE PACKET REFUSES
  IT RATHER THAN DROPPING IT SILENTLY.** §4 is the measurement: `WF-1A` §5 files the Chronicle
  obituary row under WF-1d; ODQ §309 re-filed the settlement obituary to WF-8 and WF-1C's own M4
  takes the realm beat's `KIND_SECTION` row, so both candidate producers are gone, and WF-1d's own
  output is a receipt rather than an `impactKind`. ⛔ **And nothing in the estate would have reddened
  on a dead row** — there is no "every `KIND_SECTION` key has a minter" arm. **This lane refuses the
  row and asserts its absence (A6); it does not re-scope a chartered deliverable across waves, which
  is a chair act.** If the chair reads the deliverable as still owed, it belongs with WF-8's beat.
- ⛔⛔ **RAISED-2 — `applyWorldPulse.js` IS A FOURTH ZERO-HEADROOM-CLASS FILE AND NO FAMILY DOCUMENT
  SAYS SO.** Measured **941 against a frozen 941**, pinned exact in both directions by
  `tests/lint/sizeBaseline.test.js` and re-enforced by a generated per-file `max-lines` override.
  Preamble §P7.8 names `peaceTerms.js` and `warTermination.js`; WF-1C's RAISED-A adds
  `pulseKernel.js`; this is the fourth, and `WF-1A` §3 lists it among the files WF waves avoid —
  which is why nobody has measured it. **WF-1D is safe because it names neither kernel file, but the
  deferred FaithSection member touches `pulseKernel.js` and a later WF wave may reach
  `applyWorldPulse.js`.** Amending a chair-signed preamble is a chair act; offered with its
  measurement rather than made. ⭐ **RAISED-A (WF-1C) and this row should land as one preamble edit.**
- ⚠ **RAISED-3 — THE DEFERRED MEMBER'S NAME AND PLACEMENT.** §7 recommends a two-member shape and
  compiles the seed (`laneTCWF1D-WF-1E-seed.md`), but whether the FaithSection line lands as
  `WF-1D-II`, as a new `WF-1E` inside the ladder, or re-filed to a display member is a re-scope
  across waves — the §309 shape, and a chair act. ⛔ **This packet's own path carries no suffix
  either way** (J-TEWF1B-2). ⚠ The deferred member also carries a **DS-FTH-1 corpus amendment** and
  therefore touches volume **Q4, which is OPEN at the chair** — that half may need the Q4 ruling
  before it can promote, and this half does not.

- ⚠ **RAISED-4 — A LANDED CERTIFICATION ROW MAKES A PREDICTION ODQ §309 FALSIFIED, AND IT SITS IN
  PRODUCTION SOURCE.** `subsystemRowsVirtual.js:1268`'s `aliveness.other` for `faithUnseatingEnabled`
  closes: *"WF-1d lands the war-dissolution join that consumes `fallCauseFor`, and WF-1b the obituary
  beat, at which point a receipt can grade the layer."* Executed at this base — the first clause is
  what THIS packet makes true; **the second is wrong**, because §309 re-filed the obituary beat to
  WF-8 and WF-1B landed the mechanism without the voice. ⛔ **This lane does not amend it**: the row
  is not in this packet's closed §8 manifest, out-of-manifest editing invalidates the session, and
  the error was introduced by a chair ruling rather than by any wave. It is offered with its
  measurement. ⚠ The row would still grade UNOBSERVED after this landing — no preset declares the
  key — so nothing about the layer's certification actually moves; only the sentence is stale.
  ⭐ **If the chair takes it, the cheapest home is the §309 bookkeeping, not a WF member**, and
  `aliveness.other` must stay above 400 characters (`contributionLedgerShape.test.js`).

### ⭐ Inherited dispositions this compile re-verified rather than assumed

- **`deityNameForRef` (WF-1C RAISED-B) — REFUSED IN-MEMBER, STILL OPEN, AND WF-1D IS UNAFFECTED.**
  `laneTEWF1C-receipt.md` §6.2 records the chair's §316 refuse-arm firing: a live, unflagged surface
  shares the helper. Re-verified here for this member's own surfaces: the executed census in that
  receipt shows **exactly ONE caller of `deityNameForRef` in `src` — `realmEvents.js:273`**. ⛔ **No
  file this packet names reads it, and this member prints no deity name at all** (§5) — the clause
  names the act, not the god. ⇒ **if the chair takes RAISED-B, WF-1D re-records nothing.**
- **WF-1C's fixture-refutation lesson is folded into §11 step 10.** Its §6.1 found a pin that would
  have passed while asserting nothing because it was handed the wrong snapshot. The analogue here is
  A3's hazard: the wrong RING makes the whole battery vacuous.

---

## §14 · MUTANTS AND HAZARDS

- **MUTANT (a) — the untyped join.** Force `dissolvedClauseFor` to ignore its `fallCause` argument
  and always return the base clause. Must red **A1 alone** — the receipt key still lands but the
  sentence stops naming the cause. This is the mutant that proves the prose half is real rather than
  decorative.
- **MUTANT (b) — the ungated join.** Force `unseating` true regardless of the flag. Must red **A2
  alone** — the dark byte-identity fence. This is the lit-mutant control's counterpart and it is what
  proves the fence can see.
- **MUTANT (c) — the invented cause.** Delete the `includes('sacred_claim')` guard from the lookup so
  every dissolution consults the ring. Must red **A4** — the legacy `anchor_unavailable` record then
  acquires a cause it has no right to. ⛔ If it also reds A3, the guard and the ring-miss path are
  entangled and one of the two is vacuous — **a STOP and a re-shape, not a pass** (§P2.10).
- **MUTANT (d) — the eager key.** Write `patronFallCause: null` unconditionally instead of the
  conditional spread. Must red **A3 alone** — an absent key and a null key are different bytes.
- **MUTANT (e) — ⭐ the UNLICENSED IMPORT.** Delete M3's `COUPLING_REGISTRY` row while leaving the
  import. Must red **A6**, and it is expected to red `couplingInclusion.walker.test.js`'s
  new-cross-layer-import arm directly with the message quoted in §2.5. ⛔ This mutant exists because
  the row is the one piece of this member a reasonable implementer might treat as bookkeeping — it
  makes the same-commit obligation falsifiable rather than merely written down.
- ⚠⚠ **IF (a) AND (b) CONVICT THE SAME ARMS, ONE OF THE TWO GUARDS IS VACUOUS** — a STOP and a
  re-shape, not a pass. They should not: (a) leaves the receipt key intact and (b) removes the whole
  feature.
- Every mutant is planted by `cp` backup, `node --check`ed in its host, convicted, and restored
  **`cmp`/`md5`-exact** — the restore is proved by digest, never by eye. ⛔ **NEVER the
  `git checkout` family**: it discards uncommitted work, and on this shared tree that work is the
  owner's.
- ⚠⚠ **THE FIXTURE MUST BE ABLE TO EXECUTE ITS OWN DEFECT — AND THIS MEMBER ADDS A FOURTH MEASURED
  INSTANCE TO THE FAMILY'S LIST.** Beyond `PATRON_FLIP_TICKS` 3 / `SHARE_STEP_MAX` 6 (WF-1a),
  `nicheOf`'s computed niche (WF-1b) and `TIER_HOLD_TICKS` 2 / `MAX_TIER_CHANGES_PER_TICK` 2
  (WF-1c): **a LIT world whose ring records no fall for the PINNED ref is byte-identical to dark.**
  Executed, both arms. ⛔ **RUN THE FIXTURE AND PRINT THE RING AND THE RECEIPT.**
- ⚠ **A DOCBLOCK IS POSITIONAL.** Placing `FALL_CAUSE_PROSE` or `dissolvedClauseFor` directly above
  an existing export in M2 orphans that export's JSDoc onto the new declaration — WF-1B's landing
  paid this exact bill as nine `typecheck:domain:strict` errors against a GREEN `typecheck:ratchet`.
- ⚠ **`warTerminationCauseTables.js` THROWS AT MODULE LOAD** on three totality assertions. M2 must
  place its additions so those three checks remain the file's last statements, and must not join a
  taxonomy the assertions enumerate — `FALL_CAUSE_PROSE` is keyed on FALL causes, not war reasons,
  and is deliberately outside all three.
- ⚠ **A DEPENDENCY BUMP IS A MINT TRIGGER.** `package.json` / `package-lock.json` are governed paths;
  this packet touches neither and must not.
- ⚠ **VERIFY THE LANDED PINS SURVIVE, DO NOT ASSUME IT.** `warTermination.test.js`'s dissolution test
  asserts `toContain('a god named when the banners rose is no longer worshipped from the same
  throne')` and `not.toContain('sacred_claim')`. The selector returns the base clause unchanged dark
  and `base + ' — ' + named` lit, so both hold on both arms — ⭐ **executed: 50/50 green with the
  patch applied.** **This is a priced check at the implementing tip, not a conclusion.**
- ⚠ **THE WRAPPER LIES.** A backgrounded ratchet can report the wrapper's exit 0 over a red gate, and
  so can a `vitest` run read through a pipe — **this lane reproduced it while writing this packet**
  (§12 item 2). Read the log; never believe an exit you did not capture in-shell.

---

## §15 · REQUIRED SYMBOLS (present at THIS status; the deliverable must PRESERVE them)

⚠ Per `PACKET_STANDARD` "Change manifest", a `requiredSymbols` row names only what exists NOW —
symbols and anchors, ⛔ **never a line and never a figure.** A re-recorded figure inside a landed
packet's `requiredSymbols` traps every train that follows. No row below names an exact line that the
deferred FaithSection member or the WF-8 beat member is chartered to edit, and no row names
`pulseKernel.js`, `applyWorldPulse.js` or any component.

| symbol | home |
|---|---|
| `readWarTerminations` | `src/domain/worldPulse/warTermination.js` |
| `pinDeploymentCasusReasons` | `src/domain/worldPulse/warTermination.js` |
| `WAR_TERMINATION_TUNING` | `src/domain/worldPulse/warTermination.js` |
| `WAR_TERMINATION_BANDS` | `src/domain/worldPulse/warTermination.js` |
| `WAR_CAUSE_DISSOLUTION` | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `DISSOLVED_CAUSE_PROSE` | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `TERMINATION_PEACE_PROSE` | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `WAR_REASON_TYPES` | `src/domain/worldPulse/warReasonTaxonomy.js` |
| `PEACE_REASON_TYPES` | `src/domain/worldPulse/warReasonTaxonomy.js` |
| `isWarReasonType` | `src/domain/worldPulse/warReasonTaxonomy.js` |
| `patronRefOf` | `src/domain/worldPulse/sacredClaim.js` |
| `fallCauseFor` | `src/domain/worldPulse/patronFall.js` |
| `recordPatronFall` | `src/domain/worldPulse/patronFall.js` |
| `classifyPatronFall` | `src/domain/worldPulse/patronFall.js` |
| `PATRON_FALL_CAUSES` | `src/domain/worldPulse/patronFall.js` |
| `COUPLING_REGISTRY` | `src/domain/certification/couplingRegistry.js` |
| `COUPLING_REGISTRY_SCHEMA_VERSION` | `src/domain/certification/couplingRegistry.js` |
| `KIND_SECTION` | `src/domain/display/chroniclersLetter.js` |
| `CONDITIONAL_LEDGER_KEYS` | `src/domain/worldPulse/worldState.js` |
| `WORLD_SNAPSHOT_HARD_DENY` | `src/domain/display/worldSnapshotPublic.js` |
| `faithUnseatingEnabled` | `src/domain/worldPulse/simulationRules.js` |
| `ENGINE_GATED_VIRTUAL_RULE_KEYS` | `src/domain/worldPulse/simulationRules.js` |
| `dissolvedClauseFor` *(created; add at the flip to LANDED, not before)* | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `FALL_CAUSE_PROSE` *(created; add at the flip to LANDED, not before)* | `src/domain/worldPulse/warTerminationCauseTables.js` |

`retiredSymbols`: **none.**

### Flag and dormancy statement

⛔ **THIS MEMBER MINTS NO FLAG AND LIGHTS NONE.** It rides two LANDED keys and requires **both**:
`warTerminationEnabled` (the war program's, already gating `readWarTerminations` at its head) and
`faithUnseatingEnabled` (WF-1a's, read BY NAME and strict `=== true` at the new `unseating` gate).
Neither is declared by any preset — executed — so no soak case and no golden corpus can reach the
lit path, and the `faithUnseatingEnabled` certification row can still only grade UNOBSERVED.
**DORMANCY IS TOTAL AND TRIPLE**: the war flag returns an empty read before any deployment is
examined; the faith gate is false so the lookup never runs; and even lit-on-both, a world whose ring
records nothing produces a byte-identical receipt (A3, executed). ⛔ **Lighting either flag is
owner-gated, always.**
