# RECEIPT — lane WARMS-CAR0 — **PARTIAL** (three acts complete; every build-gated figure OWED)
**Seat: Opus 5 — Fable-unvalidated · Lane: WARMS-CAR0 · 2026-09-05**

> **PARTIAL, and the header says why.** A1 (the five probes), B3 (the re-measure) and B2 (the skeptic pass) are
> **complete as far as a read-only lane can take them**. Eight figures require a build or vitest and were **NOT
> RUN** — each is listed in `CAR0.md` §5 with its exact command. Nothing in this receipt asserts a build outcome.

**Dock:** `$SC/laneANCH2` detached at **`272dbd2da416343a6eac14bc50ac09abb69d53b8`** — porcelain **0** on arrival,
**0** after the lighting probe, **0** after the OSR gate, **0** at close. HEAD unchanged throughout. (CONFIRMED.)
**Product bytes written: ZERO. Repo writes: ZERO. Refs created: ZERO. Register acts: ZERO.**
**Fences honoured:** no vitest, no `npm run`, no build, no `npm install`, no `node_modules` materialisation
(`esbuild` invoked via the existing **symlink** at `node_modules/.bin/esbuild`, output to stdout only), one process
at a time, `${sha}:path` braces throughout.

⚠ **One fence tension, declared:** the chair's `lighting-probe.mjs` — named verbatim in my brief — writes a
transient `tests/lint/.lighting-probe.<pid>.mjs` into the tree and unlinks it in a `finally`. I ran it because the
brief names it, and verified porcelain 0 and no stray file after. Flagging it because "no writes to any git working
tree" and "run this probe" are in tension, and the chair should know which it meant.

**Deliverables:** `$SC/warms/CAR0.md` (probes + B3 table) · `$SC/warms/SKEPTIC.md` (B2, claim by claim) · this file.
Working copy of the volume at `$SC/warms/_DESIGN_W_ARMS.md` (read from `refs/preserve/train-warms-2026-09-05`).

---

## 1 · WHAT WAS DONE, WITH LABELS

| act | status | evidence |
|---|---|---|
| **A1 probe 1 — OSR** | **CONFIRMED** | gate `EXIT=0`, *"1993 finding(s), exactly matching the frozen inventory"*; full per-file inventory table derived |
| **A1 probe 2 — `EAGER_FIRST_PAINT_MODULES`** | **CONFIRMED** | `Set`, **size 265**; every W-ARMS seam file's membership measured; no build needed |
| **A1 probe 3 — exporter call sites** | **CONFIRMED** | four entry points read; two thread no `campaign` |
| **A1 probe 4 — esbuild bytes** | **CONFIRMED (existing files)** / **REFUSED (the `arms.js` stub)** | `parentRefOf` = 80,334 B; other four = 2,712 B; substrate = 6,642 B. The stub cannot be honestly measured — the file does not exist. **OWED to Car 1** |
| **A1 probe 5 — `wizardNewsAuthoring`** | **CONFIRMED CLEAR** | rc=1 for all three candidate paths |
| **B3 — re-measure** | **CONFIRMED, 40 rows** | 5 DECAYED · 1 WRONG · 3 OWED · rest HOLD |
| **B2 — skeptic pass** | **COMPLETE** | 11 claims graded RATIFY/REFUTE/THIN + both mandated questions answered |
| **B1 — status** | **CONFIRMED OPEN** | volume ABSENT from `master`, `review-fixes-2026-07-08`, `29e7bf1e5`, `272dbd2da`; PRESENT on `refs/preserve/train-warms-2026-09-05` only |

**CONFIRMED figures** (executed at `272dbd2da`, output quoted in `CAR0.md`): samples 16 · snap 17 lines · charges
104 / 279,799 B · ornament dir 5 files · zero heraldry code · `domain/display` 103 files · lighting live tuple
2521/371/2150/23184/6214 (live probe == frozen baseline, exactly) · OSR 1993/1409/schema 16/minRows 40/388 files ·
voice ceilings 34/0/15/382/69/9/770 (+ JSX total 37) · enforcement-claims 6 · ratchet entries 6 of CEILING 17 ·
`totalTests` 31489 / `totalFiles` 2468 · full typecheck 173/38/1400 · `EAGER_FIRST_PAINT_MODULES` **265** ·
`SHARED_FIELDS` **11** · `viewModel.js` **924** lines, 0 heraldry mentions · `lineageClaim.js` 605 lines ·
`parentRefOf` bundle **80,334 B** · `eventProse.js` 64,222 B raw / 83,271 B bundled · import-closure walk (6 entries).

**PLAUSIBLE (reasoned, not executed):** "measured == ceiling" on the seven voice arms (needs `voiceMechanics`) ·
the volume's zero `CLAIM_RE` hits (inherited from WARMSSCOPE at `f537ce47e`; bytes unchanged since) · the marginal
dossier-chunk delta being ≈ 0 by Rollup chunk co-location (needs a build) · Rollup co-locating `arms.js` with its
importers.

**REFUSED, and why:** the design's literal probe 1 (a scratch adapter in a **detached worktree** — a git write this
lane is fenced from; the per-file OSR table is the stronger pre-answer) · the `arms.js` esbuild stub (measuring an
invented stub would be a fabricated receipt) · §713.2's 525/525 and fence 21/21 (**no comparator located by name**;
I will not quote a count I did not derive).

---

## 2 · THE FIVE DECAYS AND THE ONE WRONG FIGURE

| # | figure | recorded | measured at `272dbd2da` |
|---|---|---|---|
| 1 | D1 lighting `titles` | 23178 | **23184** |
| 2 | D1 lighting `suiteTitles` | 6213 | **6214** |
| 3 | domain-strict typecheck `total` | 1121 | **1120** |
| 4 | `viewModel.js` lines | 917 | **924** |
| 5 | line citations `SettlementCard.jsx:144` / `shippedAssetLicence.test.js:311` | — | **:160 / :324** |
| **W** | **`SHARED_FIELDS` rows** | design §4.2: **20** | **11** — not decay, **wrong when written** |

Decays 1–2 are **UB-4 firing again one consist later**, exactly as `FABLE_RETROVALIDATION_QUEUE.md:2280` predicts.
**D1's five figures must be re-derived at the landing base, never quoted from any plan — including mine.**

---

## 3 · CORRECTIONS TO THE CHAIR

Offered under the preamble's standing instruction to verify rather than comply.

1. ⛔ **My brief's "a NEW test file reds THREE censuses" is wrong on two of three.** `totalFiles` and `totalTests`
   are **90 % collapse floors** (`SCOPE_FLOOR_RATIO = 0.9`, `check-test-ratchet.mjs:97/:1118/:1138`), which a new
   file raises and cannot breach; the known-failure list is at **6 of CEILING 17**. Only the **lighting census**
   binds (plus the anchor walker). This is the same correction memory already carries and SCOPE-MEASURE records as
   D-2 — the brief re-introduced the stale framing.
2. ⛔ **The design cites the wrong graph for its own central safety claim.** `EAGER_FIRST_PAINT_MODULES` is
   `computeEagerModuleGraph()` (`:300`/`:309`), not `computeEngineSharedDomain()` (`:31`). The zero-first-paint
   conclusion **survives on a measured ground**; two of its three stated grounds are false.
3. ⛔ **The design's OSR inference is backwards** ("0 rows ⇒ plausibly clean" — 0 rows is ceiling 0, the harshest
   state). Its **cure is right**; its ground needs restating or the next lane re-derives it wrong.
4. ⛔ **§8.3's chartered Car 3 obligation is already discharged**, and the real exposure is larger and different —
   a **PDF↔PDF drift across four paid entry points**, two of which thread no campaign at all.
5. ⚠ **B1 is OPEN**, refuting the handoff's "volume landed". It should ride a **docs-only car on the ledger branch,
   alone**, so a src-consist's gate cannot bounce the one item that survives the C1 veto.
6. ⚠ **A twelfth taste row is owed** (SKEPTIC §Q2 item 4): arms make an unopened political relationship visible on
   a dossier. Not one of the eleven. **Owner's, not mine — I do not rule it.**

**Not ruled on, by fence:** the eleven §9 taste rows, the C1 one-word veto, and every owner-gated class.

---

## 4 · RETROVALIDATION ROW

| field | content |
|---|---|
| **What was judged** | (a) B1 is OPEN and belongs on a docs-only ledger car; (b) the design's first-paint safety conclusion holds but on a **different graph** than it cites; (c) the design's OSR inference is **backwards** while its cure is sound; (d) §8.3's chartered obligation is discharged and the **real paid-surface risk is a PDF↔PDF drift across four entry points**; (e) Car 1's sample count **N is unresolved** — the design's own three statements give 16, 24 and 64; (f) my own brief's three-census warning is **two-thirds refuted**; (g) a **twelfth owner taste row** is owed. |
| **What the Fable chair must re-derive** | 1. That `EAGER_FIRST_PAINT_MODULES` (`vite.config.js:309`, size **265**) — not `computeEngineSharedDomain()` — is the graph the pin must assert against. 2. That `SettlementCard.jsx:115` and `SingleDossierSuccessPage.jsx:183` thread **no `campaign`**, and rule which of the two remedies Car 3 takes (**owner-visible: paid surfaces**). 3. That `OutputContainer.jsx` is **already in the OSR inventory (5 identities)** and therefore at ceiling 0 for anything new. 4. Whether the **80,334 B** `parentRefOf` closure is genuinely free — needs the Car 2 build; the static walk says the modules are already resident on the parent paths but **not** in `DossierHeaderRow.jsx` (25 modules) or `Cover.jsx` (15 modules). 5. **N**, fixed from `gen-organic-ornament.mjs`'s one-per-thing rule before Car 1 authors a byte. 6. The volume's zero-`CLAIM_RE` result at the actual landing base (the arm is at **6/6**). 7. §713.2's **525/525 + 21/21** — I could not locate the comparator; **chair-owned**. |
| **Receipts by path** | `$SC/warms/CAR0.md` (§1 B1 · §2 five probes · §3 the 40-row B3 table · §4 the brief correction · §5 the eight OWED commands) · `$SC/warms/SKEPTIC.md` (§1 C1–C11 · §2 Q1/Q2 · §3 what was not settled) · `$SC/warms/_DESIGN_W_ARMS.md` (working copy, from `refs/preserve/train-warms-2026-09-05`) · this receipt. |
| **Priority** | **HIGH — before any W-ARMS build car is dispatched.** Items 2 and 3 are **unbankable STOPs** (a paid-surface behaviour change; an OSR red with no lawful bank), and item 5 decides how many byte-pinned goldens Car 1 mints. Item 1 changes what Car 2's pin asserts. Items 6–7 are landing-gate acts. |

---

## 5 · CLOSING STATE

`git -C $SC/laneANCH2 status --porcelain` → **0 lines**. `git rev-parse HEAD` → **`272dbd2da4163…`**.
No branch, ref, index or working tree was modified anywhere. No register act was taken. No product byte was written.
