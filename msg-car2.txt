SEAT-78 car 2: the sealed car's landing bill, measured and paid — and the D10 bridge turns out to owe a coupling row nobody priced

⛔ THE FINDING THAT MADE THIS CAR NECESSARY. The sealed SEAT-7a car's own RECEIPTS block names
sixteen suites and NOT ONE `tests/lint/` directory run, and `tests/lint/` is where the scanner
family that a src-adding train reds lives entirely. Run WHOLE at the replayed tip, it convicts the
car SEVEN ways in fifteen arms. Every one of them was green at `6c49ecbbf` too — the walkers all
existed there — so this is a bill the sealed car never paid, not a bill the new base created. A
single-file green is structurally blind to this family, exactly as the lane law says.

ATTRIBUTION IS MEASURED, NOT ARGUED. `tests/lint/` was run at the BASE `5e28d5c83` on the eight
failing files (dock detached, run, restored — HEAD and porcelain verified both ways) and came back
**1 failed | 7 passed**. So:
· MINE, 15 arms across 7 files — couplingInclusion(2) · domainAnyCastBaseline(4) · ruinFilterRoster(2)
  · wizardNewsAuthoring(1) · writerReach(3) · tuningRegister(2) · sovereigntyLightingContract(1).
· NOT MINE, 1 arm — `clampPrimitiveBaseline`: its baseline names 72 files, the tree defines 62, and
  all ten in the delta (townCartography ×2, conquest ×3, razing ×3, dispositionLedger,
  warAllianceRisk) are OUTSIDE this lane's nine-file diff. PRE-EXISTING at the tip; reported, untouched.

WHAT THIS CAR CURES, all four at cause:
1. **The two `any` holes are TYPED AWAY, not baselined** — the ratchet's own message refuses both a
   baseline row and a `DECLARED_OVERRUN` ("both ledger ceilings are MONOTONE-DOWN literals"), so the
   only cure is the code. Both casts go through `unknown` in two steps, which is the verdict file's
   own live idiom at `ladderEffectivePowerFactor`, and `s` now names `RulingPowerSettlement` through
   a typedef rather than laundering the shape through `any`. `count-domain-any.mjs` no longer reports
   the file at all.
2. **`irregularForce.js` gets a LAYER family home — INTERIOR, on SUBJECT.** Its subject is whether a
   settlement's own people would rise, composed from three INTERIOR readings (legitimacy deficit,
   commons grievance, challenger roster weight) and handed to a verdict INTERIOR's first regex already
   claims by the `rulingPower` spelling. It is NOT an ARGUED_UNLAYERED case (that roster is for modules
   owning no subject), and the `deriveMilitaryCapacity` read does not make it WAR: it consumes
   precomputed 0..100 FACETS as scalars and touches no army, deployment, transit or headcount.
   Exact-path regex, never an `irregular[A-Z]` prefix — the file's own standing rule.
3. **The ruin-filter roster exempts it on the settled facet-scalar shape** — `facets.institutions` off
   `deriveMilitaryCapacity`, never `settlement.institutions`, which is the third instance of the
   exemption `attrition.js` and `warDeployment.js` already hold. Its census literal moves 93 → 94,
   read from the arm's own failure message as that arm instructs, and it is a re-measurement rather
   than a ceiling raise: the discovery set counts exempt readers too, by design.
4. **The Wizard-News ledger is RE-PINNED for a pure relocation, with the arithmetic derived from the
   diff rather than copied from the failure.** Ceiling stays 19, no row added or removed, all five
   COLUMNS, SIGNATURES and ISSUE SETS byte-identical. deploymentReturn.js takes +1 at :37 and +6 at
   ~:562, so the three rows above the block move by exactly +1 and the one below it by 7 — the SPLIT
   shift is itself the proof that the six-line block landed between the third row and the fourth;
   commonsVoiceKernel.js takes one 29-line insertion at :79 and 167+29 = 196 exactly. The prior note
   is preserved, as the two earlier re-pins preserved theirs.

⛔⛔ WHAT THIS CAR DELIBERATELY DOES NOT DO, AND IT IS THE REAL FINDING. Giving the leaf a family made
a coupling VISIBLE that was previously invisible on both sides, and it is a genuine one:
`deploymentReturn.js` (WAR) imports `irregularForce.js` (INTERIOR), direction `INTERIOR→WAR`. That is
not an artefact of the home I chose — it is D10's own design showing through. `participation01` is
chartered as "the single politics↔force bridge", so the leaf sits BETWEEN a WAR importer and an
INTERIOR import by construction, and every possible home mints the same one pair: WAR mints
`irregularForce → commonsVoiceKernel`, INTERIOR mints `deploymentReturn → irregularForce`, any third
family mints TWO, and leaving it unlayered mints none only by making the bridge invisible — which
this walker's header calls "the most expensive kind of wrong". So the minimum is one row, and the
walker says whose it is, verbatim: **"minting a registry row declares a coupling's direction, desk,
flags and receipt address. That is a chair declaration, not a walker repair."** The bill is larger
than a row besides — `CHARTERED_VOLUME_PREFIXES` is a closed set of thirteen and carries NO W-SEAT
prefix, and a row demands a `receiptField` and an `intendedDesk` for a car whose certification row
COMMITS to minting zero news kinds. Improvising that would be a false claim on a real registry.
⇒ ONE ARM STAYS RED AT THIS TIP BY CHOICE: `couplingInclusion.walker :: a NEW cross-layer import is
either licensed by a registry row or REDS`. It is handed up with its derivation, not laundered.

RECEIPTS, every exit captured in-shell: `typecheck:domain:strict` **EXIT=0** (1120 errors, ceiling
1120 — ⚠ the sealed car's message says 1121/1121; the ceiling moved on the mainline, so that figure
is stale and this is the live one) · `typecheck:ratchet` **EXIT=0** (173/173) · `eslint` on all three
edited JS files **EXIT=0**, zero output · vitest over `irregularForce` + `irregularForceDormancy` +
`ruinFilterRoster` + `wizardNewsAuthoring` + `domainAnyCastBaseline` + `couplingRegistry` +
`couplingDesk`: **7 files, 77 tests, 0 failed, EXIT=0**.

NO REGISTER ACT WAS TAKEN. The four remaining red arms are all register-side or chair-side and are
enumerated with predicted figures in the lane receipt.

Seat: Opus 5 — Fable-unvalidated
Lane: SEAT-78

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
