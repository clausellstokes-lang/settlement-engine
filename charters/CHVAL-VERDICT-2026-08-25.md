# TE-CHVAL — retro-validation of `charters/draft-CATALOG-HYGIENE-PLAN.md` (CH-3 sections + CH-2B + coherence)

- **Object:** the plan at preserve ref `029268fe579b2cbd64e9941b66e77639133f072e` (86,536 B, 1,165 lines, read whole).
- **Slot:** `claude/composite-r4` = `e4ed27f48424eb2d016128ee1b16753e6b1ad53d` (verified by `rev-parse`; both packet bases `b2852ccc3` and `86794b5d2` are ancestors).
- **Method:** §291 retro-validation. Plain-node instruments in this directory (`inst-a` catalog census · `inst-b` source-line scan · `inst-c` behavior probes · `inst-d`/`inst-d2` real-pipeline dead-magic corpus, 2,520 settlements, 0 errors · `inst-e` coexistence corpus, 840 settlements), all run against a `git archive` of the slot with its own `npm ci` node_modules (468 entries, slot `.npmrc`). Every instrument carries a positive control and every control fired (planted minTier rows classified 1/1; string-scan control found the known `downstream` desc; `ctrl-miss` pattern returned zero; dead-corpus control produced 33 Magic/Exotic instances at default magic; the world-law control allowed a plain bakery; the ladder control classified `Academy of magic → guild`).
- **Executed-state note that frames every verdict:** at the slot, **CH-3's code is fully merged** (commits `308c7ee0d`…`da2c7085c` in first-parent history; packet `MF-CH3` is **DRAFT** in the manifest awaiting its landing act) and **CH-2B's code is NOT merged** (only its packet record landed in `86794b5d2`; the commit subject "MF-CH2B LANDED" describes the record, not the code — the diff `79b78881c..86794b5d2` contains no production file).

Verdict vocabulary: **RATIFIED** (claim re-derived true) · **AMENDED** (true with a stated correction) · **REVERSED** (false, with evidence) · **DECAYED** (true at its base, false or unmeasurable at this slot — both states stated) · **EXECUTED** (the claim's prescribed change is now live and verified).

---

## §1 · CH-3 SECTION VERDICTS

| plan claim | verdict | executed evidence at the slot |
|---|---|---|
| §3.1 "311 rows, 36 carry minTier, 26 REDUNDANT, 10 ABOVE-BLOCK, 0 BELOW" | **RATIFIED then, DECAYED now (by execution of its own ruling)** | INST-A: 311 rows; carriers **10 / 0 redundant / 10 above / 0 below** — the 26 no-ops were deleted by ruled, merged commit `31445ad90` (§538.5 third ruling). The classes were disjoint (DW-0 §B's correction) — arithmetic confirmed: 26+10=36 then, 10 alone now |
| §3.1 the ten-row table (names/blocks/shelves/gates) | **RATIFIED** | INST-A reproduces all ten exactly: village Smuggling network→city; city Entertainment ×4 and city Exotic ×5 →metropolis |
| §3.1/H13 the UI offers what generation refuses (3 readers unfiltered) | **RATIFIED then, EXECUTED now** | INST-C: `getInstitutionalCatalog('city')` leaks **0** of the 9 gated city rows; metropolis view carries all 9; `'all'` exemption carries all 9. Import census over `src`: `getInstitutionalCatalog` consumed only by `InstitutionalGrid.jsx`, `CatalogTabs.jsx`, `selectors.js` — zero generation-path importers, so the zero-shift claim is structural. One AMENDMENT: the shipped form filters two readers and **relabels** the third (`getFullCatalogWithTierMeta` keeps rows, reports effective `nativeTier`) — the plan said all three filter |
| H12 sharpening: "a user force-requiring an above-block row at its own tier gets silence" | **REVERSED** (as MF-CH3 §0.2 C1 found) | INST-B: `minTier` is consulted at exactly **one** place in `assembleInstitutions.js` (`:256`, the probabilistic loop); the forced-toggle loops (`:652-654`, `:680-682`) never read it, so a force-required gated row appears. The 70/70 behavioural half is packet-claimed (C1, with its own vacuity-caught control) |
| §3.1 resolutions measured: DELETE 97/420 · MOVE 108/420 · READER 0/420 | **CONSUMED BY RULING; no longer measurable as specified** | J-CH-3-1 approved at §538.5 on these figures. At the slot the 26 redundant keys no longer exist, so the delete/move experiments cannot be re-run as specified; the 0/420 reader claim is proven structurally (import census above) |
| §3.2 seven `religiousCenter` carriers; per-tier thorp 1 / hamlet 2 / village 1 / town 1 / city 2; metropolis pair carries none | **RATIFIED** | INST-A: exactly 7 carriers, exact per-tier split; metropolis `Great cathedral` and `Major monasteries (5-10)` carry NONE |
| §3.2 "at city the group holds two members, so a city gets one or the other, never both" | **AMENDED** (MF-CH3 C4) | The exclusivity return is real (`:347-348` precedes the draw at `:380`), but at base `Multiple monasteries` reached a final city roster 0/70 — `coherenceRepairPass` deleted it as unsupported (packet-measured). At the slot the shipped cure is live: INST-E, 420 cities — Cathedral 120, Multiple monasteries 120, **both together 120** (co-occurrence pattern corroborates the dependency mechanism C4 named) |
| §3.2 fix = delete the two city `exclusiveGroup`s; shift 81/420, ~130 names; digest `6127eae7…` | **SUPERSEDED by the chair's own mid-build revision** | §538.5 first ruled the deletion (81/420 declared); the chair then revised (recorded in MF-CH3 §4.2): shipped form = `exclusiveGroupCoexists: true` + side-stream `rng.fork('exclusiveCoexist::…')` — INST-A shows both city rows carry the key; INST-B shows the branch at `:336-349`. Shift **30/420** (packet), `Monastery or friary` injection **0** (INST-E: 0 at city AND metropolis — the C5 guard holds). The `6127eae7…` digest is base-bound and can never land on the branch; MF-CH3 §0.1 reproduced it in a detached worktree at `00e7af612` (packet-claimed receipt) |
| §3.3 the run of four + `Contract killer`; CATEGORY_AFFINITY consequence | **RATIFIED then, EXECUTED now** | INST-A: all five read honest values (`economy` ×4, `criminal`); city Criminal shelf is 11/11 `criminal`. INST-B: `CATEGORY_AFFINITY` `economy → ['merchant','civic','craft','residential','other']`, `criminal → ['criminal','residential','foreign','merchant','other']` — precisely the plan's proposed rankings. H29's self-refutation (hasCriminalInst unaffected) statically corroborated: `corruption.js` keys on tags/category, never `priorityCategory` |
| §3.4 the tanner overstatement has FIVE spellings; no engine reader enforces the desc | **RATIFIED then, EXECUTED now** | INST-A/B: all five homes rewritten to the honest register (catalog town row; `institutionDescVariants.js:778,779`; `institutionVocabulary.js:87,107`); village `Placed downstream` register intact. Reader claims exact at slot: `townLayoutV2.js:209` regex reads a QUARTER's `location` prose (`locationBias`, fed by `spatialGenerator.js:57-61`); `townMapModel.js:516` haystack is name+priorityCategory+category+tags; `tannery` still matches the layout regex by NAME, so the placement weight survived the rewrite |
| §3.5 exactly three Eberron strings; pick-index stability | **RATIFIED then, EXECUTED now** | INST-A: Eberron 0 in catalog; INST-B: 0 in variants; the landed texts match the plan's proposed rewrites **verbatim** (catalog `:2224`, variants `:242,243`). Pick mechanism confirmed at `assembleInstitutions.js:743-751`: pure fnv over `[canonicalDesc, ...variants]`, text-blind |
| §3.6 two subterranean rows, tags `['criminal','underground']`, no facets; "an affordance already landed on three sibling rows" | **RATIFIED then, EXECUTED in the C2-corrected ONE-KEY form** | INST-A: facets carriers now **5** — the three `Underground network` rows keep the two-key `{clandestine, subterranean}` shape; `Underground city` and `Black market bazaar` carry **`{subterranean}` only**. The plan's "same affordance as the siblings" was AMENDED by MF-CH3 C2: the sibling two-key shape would have activated the live `clandestine` D6 couplings (an unpriced behaviour change, settled by digest at the charter's base). The forbiddance union ("tunnels flood") mechanism confirmed present (`strataExistence.js:129-160`) |
| §3.7 `Kidnapping ring` desc vs `exclusionConditions`; rewrite the desc | **RATIFIED then, EXECUTED with two amendments** | INST-A: `exclusionConditions: ['Slave market','Slave market district']` unchanged; desc rewritten. AMENDED (C3): the claim had **three** homes (two variants), not one — all rewritten. AMENDED (J-TECH3-C): the plan's proposed replacement dropped the authored "forged provenance documents" mechanism; the landed text preserves it. Exclusion check drifted `:324 → :351`, mechanism identical |
| §3.8 the shift-summary table | **SUPERSEDED** | by MF-CH3 §1's per-commit table (five separable commits + bundle rebuild, each with its own digest at base `86794b5d2`) |

## §2 · CH-2B SECTION VERDICTS (plan §2(b)/(c)/(e) + the §4.3 stub)

| plan claim | verdict | executed evidence at the slot |
|---|---|---|
| P1 (multiplier `:88-96`), P2 (`hiMagicInsts` `:176-183`), P3 (`NON_MAGIC_EXOTICS` `:186-195`) as quoted, un-executed | **RATIFIED — still live, byte-identical** | INST-B at slot: P1 `:88`, P2 `:176-181` (all 11 members incl. the three dead ones), P3 `:187-189`. `git diff f1e4d515..slot` over `institutionProbability.js`, `generationContext.js`, `magicFilter.js` = **empty** — all three gate files unchanged since the charter's base |
| P4 (`:302` magicExists gate) → "a catalog row's declared licence decides; `isArcaneInstitution` stays the fallback" | **EXECUTED (by landed MF-CH2A)** | `arcaneInstitutionIdentity.js:215-236`: licence first (`none` ⇒ mundane), tag second, own-tags/`magical` third, `bucket === 'magic'` fallback (`@non-catalog-fallback MF-CH2`), name last. INST-C: all nine `none` rows → false; `Healer (divine, 1st level)` → true; all three fallback controls behaved |
| P5 the world law: un-named by §491, "strikes 26 catalog rows" | **RATIFIED (the fifth path is real, still un-executed) · figure AMENDED to 28 · call-site census AMENDED** | `generationContext.js:132-138` still the unanchored `ARCANE_INST_KW` scan, no licence read. Strike census by execution (INST-C, both `magicExists:false` and `priorityMagic:0`): **35 struck = 28 Magic/Exotic rows (magic-attributable) + 7 struck by the OTHER laws in the same predicate** (maritime ×2 config-dependent, content-profile ×5) — confirming MF-CH2B/§520.4's "28, not 26". The keyword arm alone now hits **23** names (was 26 at the charter base — CH-6 removed the faith keywords, §541.8 territory). NEW correction (mine): the plan's call-site census was wrong **at its own base** — `assembleInstitutions.js` had **five** `allowsInstitution` sites at `f1e4d515` (`268/410/484/546/667`; slot: `268/442/516/578/699`), not the three the plan names, plus `cascadeGenerator.js:180`. No consequence for the fix shape (the cure edits the one predicate), but the claim inventory under-counted consumers |
| the UI half `magicFilter.js:37` shelf test | **RATIFIED — still live at exactly `:37`** | INST-B; no licence token anywhere in the file |
| H16/G4: "the hall generates 16× at `magicExists:false` while `filterCatalogForMagic` strips it — the estate's one measured generation-versus-UI divergence" | **REVERSED** | INST-D (real pipeline, 2,520 dead-magic settlements): hall instances 94, Magic/Exotic-shelf instances **0**. INST-D2: every dead-world hall instance is the **town/Adventuring** row; the UI **shows** that row in a dead world and hides the hamlet/village Magic rows; the world law refuses the hamlet/village rows and allows the town row — **the two surfaces agree at every hall tier; there is no divergence to cure**. The gate files are byte-identical to the charter base, so the refutation reaches back to `f1e4d515`: the plan's count was real, its attribution was wrong then. Independently converges with §520.4's row-level refutation (which my brief did not hand me) |
| H8/§2(b): "CH-2 discharges the `DESIGN_REALM_MAGIC_TOGGLE.md:514-519` chair-to-schedule item" | **REVERSED (by the packet's own §5, confirmed at slot)** | The residual text is present, unchanged, at the slot (verified in the doc at the slot: "magicFilter's own list is still unanchored… Recorded, not fixed… Chair to schedule"). The built CH-2B narrows its surface to the unlicensed remainder; it does not discharge it |
| §2(c) G7: the three dead `hiMagicInsts` keywords match 0 catalog rows | **RATIFIED** | INST-C: `magical banking` 0 · `enchanting quarter` 0 · `magic item consignment` 0 over 311 names; control `teleportation` = 2 |
| §2(e) the 361/2,100 model table + J-CH-2-1 (split P2 out) | **SUPERSEDED / not reproducible as stated** | §520.4: "The charter's 361-of-2,100 is not reproducible as stated. The denominator is 504." The built packet measured Design A at **1,025/2,520** and Design B at 545/2,520 at ITS base; J-TECH2-4 carried the P2 split to the chair as the plan recommended. All of these are now themselves stale at the slot (next section) |
| §2(e) "the NONE-licensed rows that survive a dead-magic world are the two Adventurers' charter hall rows and Underground city" | **AMENDED** | Survivor NAMES confirmed at slot (INST-D: hall 94, Underground city 126, everything else licensed/struck = 0) — but the surviving hall instances are the **town/Adventuring** row (which merely *reads* licence `none` by name-keyed lookup), not "the two [hamlet/village Magic] rows", which the world law strikes. The load-bearing conclusion — **"restoring the mundane chemical trade requires P5, not P4"** — is **RATIFIED by execution**: P4 is licence-routed at the slot and `Alchemist shop`/`quarter` are still 0 in 2,520 dead-magic settlements |
| §4.3 CH-2b packet stub (2 production files: `generationContext` + `magicFilter`) | **SUPERSEDED** | The built MF-CH2B is three production files (P1/P2/P3 needed `institutionProbability.js` since MF-CH2A took only P4) + CREATE `tests/lint/magicShelfGateCensus.walker.test.js`; the stub's TEST rows survive in spirit (`mundaneRealmAcceptance` is in the packet's bill) |

## §3 · MF-CH2B PACKET COHERENCE (brief item 3)

1. **The ⛔ §0.0 STOP is RESOLVED at the slot — by events, exactly as §541.6 predicted.** Ruled as SHAPE F (ODQ §541.3), landed as MF-CH5 (`alchemy` → `TRADE_INST_TAGS`; redundant `arcane` tags deleted) + MF-CH6 (faith rows). Executed at the slot: `ARCANE_INST_TAGS = ['arcane','planar','enchanting']`, `TRADE_INST_TAGS = ['alchemy']`; the three ex-conflict rows carry tags `['alchemy']`/`['military']`; INST-C's ladder-conflict census over all `none` rows: **0 conflicts** (controls: Academy→guild, Golem→foundry still classify). The packet's "not mintable until one of those is ruled" is discharged; §0.0 is historical text.
2. **Its manifest absence is chair-directed, not an omission** — MF-CH2A's INDEX row: "its sibling MF-CH2B is NOT minted by this act and stands outside the manifest at the chair's direction (§520.5)". Consequence stands regardless: its three production files and its walker path are **unreserved** in the manifest today; nothing mechanical prevents a sibling lane from claiming them until the mint.
3. **Serial-mint collision at dispatch:** MF-CH3 (DRAFT, non-terminal) reserves `tests/fixtures/generator-golden-master.json`, `tests/property/generatorGoldenMaster.test.js` and `scripts/mutation-coverage-manifest.json` — all of which CH-2B's landing also owes (its golden re-record; its new walker's E-A entry). **MF-CH2B can mint only after MF-CH3 lands.** The plan's §4.1 order (CH-2b before CH-3) is inverted in execution.
4. **Every measured figure in the packet is stale at the slot — re-derivation owed at the dispatch base.** Its base `b2852ccc3` is ~40 first-parent commits behind; since then the catalog moved under it six ways (two licence values → `none` for `Druid Circle`/`Elder Grove Council`; three `arcane` tags deleted; 26 `minTier` keys deleted; five `priorityCategory` values; two `facets` keys; two `exclusiveGroupCoexists` keys + prose). Named stale figures: the 1,025/2,520 headline shift and the 545 Design-B price; the §2 row table; arm A7's re-point ("admits exactly the SEVEN `none` rows… strikes the other 21" — the slot has **nine** `none` rows / 8 distinct names, so 9-and-19); the 92-of-525 golden subset claim (MF-CH3 re-recorded 272 rows since); the census delta; the mundane-realm census `134→139/22→24/193→195`; the 13-red sweep classification (three of its "pre-existing" reds may have moved).
5. **⛔ THE BUILT TREE EXISTS ONLY AS DANGLING COMMITS — zero refs, gc-exposed.** Recovered by sweeping all 481 dangling commits for the walker file: the complete build tip is **`2ccb20d598c93a263f9bd48a9c86a92264d1a81e`** ("MF-CH2B: the packet — DRAFT, not READY…"), four CH2B commits (`a38cd3bb5` gates · `1a6ff1298` census · `70bb64f23` golden · `2ccb20d59` packet) atop the lane's own pre-landing MF-CH2A build, merge-base `b2852ccc3`; all three gate files carry licence reads there (5/9/7 hits). Three stash-shaped WIP siblings also match. **This lane is forbidden refs — the chair should pin it (e.g. `refs/preserve/holding-ch2b`) before any gc/cleanup.** It is the receipt-of-record for every packet figure; the dispatch will still rebase/rebuild on the current slot (its CH2A underlay is the pre-stack form).
6. Carried bills that ride CH-2B, re-confirmed in the ruling trail: the `effectReachability.coverage` loss (2 of 12 authored effects, §520.6 — "wants a corpus widening or a stated reason") and the re-cut B4 arm ("they STILL agree after both stop reading the shelf", §520.4).

## §4 · CORRECTED FIGURES THE CH-3 / CH-2B DISPATCH BRIEF MUST USE (all slot-executed)

- Catalog: **311 rows** · minTier carriers **10** (0 redundant — deleted) · the ten gated rows as listed · `religiousCenter` carriers **7**, the two city rows carrying `exclusiveGroupCoexists: true` · facets carriers **5** (3 two-key + 2 one-key) · licence carriers **28** on-shelf, off-shelf 0, values low 3 / medium 4 / high 12 / **none 9** (8 distinct names).
- Vocabulary: `ARCANE_INST_TAGS` 3 members; `TRADE_INST_TAGS = ['alchemy']`; `ARCANE_INST_KW` **32 members, 23 catalog-name hits**.
- World law (still unrouted): strikes all **28** Magic/Exotic rows in a dead world (record-shape, confirmed by real pipeline: 0 Magic/Exotic instances in 2,520 dead settlements); consumers = **five** sites in `assembleInstitutions` (`268/442/516/578/699`) + `cascadeGenerator:180`.
- Un-executed gate homes at the slot: P1 `institutionProbability.js:88` · P2 `:176` · P3 `:187` · P5 `generationContext.js:132` · UI `magicFilter.js:37`.
- Dead-world survivors of the catalog's magic surface: town/Adventuring hall (94 instances in my 2,520-grid) and `Underground city` (126) — there is **no** gen-vs-UI hall divergence to cure or to pin.
- CH-3 landing-act state: code merged; packet DRAFT at base `86794b5d2` with the census delta `+1/+0/+1/+10/+1` (carry the delta, never the tuple); `metropolisCatalogReachable` amended per J-CH-3-1 and no longer a red; the re-homed `Priest (resident)` `faith` override is **not** in the tree (facets null at slot) and remains its own chartered car.

## §5 · CLAIMS NO INSTRUMENT CAN NOW VERIFY (named, not buried)

1. The charter-base corpus digests `1a4a8d3f…`, `6127eae7…`, `a542cc2e…` — permanently unreproducible on the branch (every later car moved records); reproducible only in a historical worktree at `00e7af612` with its own `npm ci` (MF-CH3 §0.1 did this for the first two — packet-claimed, receipts in that lane's transcript).
2. The §3.1 alternative-resolution figures (97/420 delete, 108/420 move) — the experiment's precondition (the 26 redundant keys) no longer exists at the slot; consumed by the §538.5 ruling.
3. The §2(e) 361/2,100 model — refuted as-stated by §520.4 ("not reproducible; the denominator is 504"); no instrument can rehabilitate it.
4. MF-CH2B's own measured figures — verifiable only by rebuilding at the pinned dangling tip; all owe re-derivation at the dispatch base regardless (§3.4 above).
5. MF-CH3's behavioral receipts I did not independently re-run (C1's 70/70 forced-row probe, the 30/420 revised shift, the 272-row golden totality) — corroborated at the slot by my static and corpus instruments (single minTier read; coexistence live 120/420 city-pairs on my grid; injection 0/840) but the packet's exact figures bind to its base and grid.

## §6 · WHOLE-DOCUMENT COHERENCE (still-live claims outside the two sections)

- Line 5 "landing before DW-0" — stale; ruled in the pickup card's favour (DW-0 §B, conflict C-13). The correction is owed "at CH's next touch".
- §4.1 train order — inverted in execution (CH-3 built first); binding consequence in §3.3 above.
- §4.2 manifest snapshot — decayed: 182 packets at the slot, 179 LANDED / 1 SUPERSEDED / 2 DRAFT (`MF-CH3`, `MF-CG2`). The law it carries (re-read the live manifest at the build base) is what survives.
- §4.4 census tuple — decayed as expected; the carried-delta law is honoured by both packets.
- H24 (lowercase tokens) and H25 (28 rows, not 32) — EXECUTED exactly (INST-A: 28 carriers, four lowercase values).
- The §2(a) licence-value table — AMENDED since: `Druid Circle` and `Elder Grove Council` moved low→none by the CH-6 deity-doctrine work (§541.8 lineage).
- H36 — RATIFIED at slot: `institutionalCatalog` is an input to exactly the three AI bundles, zero in analytics/intentAtlas.
- H37 — AMENDED: the size baseline now holds 16 rows (was ten); still zero CH files, so the conclusion stands.
- H39/H40 — tuple decayed / **37 test trees RATIFIED** at the slot (38 entries, one file).
- Instrument note for successors: my INST-B function-regex missed `export const getInstitutionalCatalog = (tier) =>` (arrow form) and reported a false ZERO; caught by a whole-tree token search. A zero from a shape-specific pattern is not an absence.

## §7 · RECOMMENDATION (the chair rules and executes)

**JUDGMENT (recommendation only): discharge the plan's `[OPUS-RUN · FABLE-VALIDATION OWED]` marker to a dated FABLE-VALIDATED form covering the CH-3 and CH-2B sections**, with this document (or its ledger digest) attached as the §623-required dated addendum, because: every load-bearing CH-3 claim is either re-derived true at the slot, executed, or superseded by an execution-backed chair ruling that my instruments corroborate; every CH-2B claim is either re-derived true (P1/P2/P3/P5/UI still live; G7 zeros; strike census 28), executed by CH-2A (P4), or reversed with executed evidence (H16/G4; H8's discharge claim; the 361/2,100 model). The two REVERSED headline claims (G4's divergence, H8's discharge) and the DECAYED base-bound digests must appear in the discharge text — a quiet deletion would re-open them. **The MF-CH2B packet's own FABLE-VALIDATION marker is a separate debt and is NOT discharged by this validation** — its figures are stale by construction and its re-derivation is the dispatch's first act, per §620.2's own scheduling. Say "veto" to hold the marker instead.

**Two immediate mechanical items for the chair, outside my write authority:** pin the dangling CH-2B build tip `2ccb20d598c93a263f9bd48a9c86a92264d1a81e` before any gc; and note the §3.3 serial-mint ordering (MF-CH3 lands before MF-CH2B can mint).
