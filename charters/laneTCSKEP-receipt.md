# laneTCSKEP receipt (TC-CH-SKEPTIC)

STARTED 2026-08-23T20:11:09Z

Lane: TC-CH-SKEPTIC (adversarial panel on draft-CATALOG-HYGIENE-PLAN.md)
Mode: READ-ONLY on repo; private worktree for probes; no sub-agents; no Workflow.

## RESUME POINT (see timestamp line below)
(a) PROVEN:
  - disk: 19,909,860 KB available on / (threshold 3,000,000) — PASS
  - claude/composite-r4 re-derived independently = 00e7af612d428078634d52ea37054bd00b773ca6
    subject: "MF-UC2 landing: pay the seventh naked claim the sweep found ..."
  - f1e4d5150748f13cc27fbbb2d34db614d49d7620 IS an ancestor (merge-base --is-ancestor EXIT 0); 6 commits in range.
  - TEN CH-touched files: blob ids IDENTICAL at both ends (FILES=10 SAME=10); git diff --name-only over
    the ten = 0 lines. NON-VACUITY CONTROL: the same range diff over ALL paths names 9 files
    (incl. src/domain/institutionFounding.js, tests/lint/sovereigntyLightingContract.walker.test.js).
    => charter's "diff is EMPTY over the ten" = CONFIRMED, probe non-vacuous.
  - NOTE for the chair: src/domain/institutionFounding.js DID move in the range and is NOT in the ten.
(b) IN FLIGHT: worktree creation at the CURRENT tip.
(c) NEXT: CH-1 933-cell independent re-derivation.

TIMESTAMP 2026-08-23T20:12:27Z

## RESUME POINT (timestamp below) — CH-1 ATTACK COMPLETE
(a) PROVEN (all executed at 00e7af612 in $SP/laneTCSKEP-tree; npm ci EXIT 0, 468 node_modules entries):
  P1  933-cell table RE-DERIVED INDEPENDENTLY from the charter's §1(a) PROSE (SKEP-facet933.mjs):
      ROWS 311 / KINDS 3 / CELLS 933; CHANGED_CELLS 4 — exactly the four the charter names.
      institutionSubstructure moves 0. HAS_TYPE_KEY 0. 3 rows carry `facets`. => CH-1 4/933 CONFIRMED.
  P2  NAIVE uniform leading-\b: CHANGED_CELLS 9, not 4 rows' craft alone. The regression is SIX cells
      over FOUR rows — Blacksmith and Blacksmiths (3-10) ALSO lose institutionFunction:'arms'.
      Charter §1(a)/H5 says only "both lose craft". => UNDERSTATED.
  P3  Corpus digest at the CURRENT TIP reproduces the charter's base figure exactly:
      1a4a8d3f85e6424a427167ddc44a5e3d7a0e63a9316dab2894632883cf9c9c84 (420 settlements, 0 errors).
  P4  CH-1 patch applied (15 ins / 15 del, matches charter): digest IDENTICAL.
  P5  ⚠⚠ NON-VACUITY CONTROL FAILS. Deliberately breaking the `faith` regex to /./i — which moves an
      interior kind in ALL 420 settlements — leaves the corpus digest BYTE-IDENTICAL. Same for
      breaking the crypt substructure regex. THE DIGEST CANNOT DETECT ANY FACET_INFERENCE CHANGE.
  P6  MECHANISM: facetOf is called ZERO times across 420 pipeline generations (counter instrumented in
      cohesionWeave.js; COUNTER_CONTROL_AFTER_ONE_DIRECT_CALL = 1 proves the counter live).
      => the charter's "strongest receipt" is a TAUTOLOGY. Conclusion right, evidence vacuous.
  P7  NEW facetOf CONSUMER LANDED IN THE RANGE: src/domain/undercity/monotoneComponents.js does not
      exist at f1e4d515, exists at 00e7af612, and reads facetOf(inst,'institutionNature'|'institutionFunction').
      The charter's §1(d) derived-read table and §4.5 CH-1 grep list both omit it, and
      tests/domain/undercityMonotoneComponents.test.js is a live grep hit at the tip.
  RESTORES: cohesionWeave.js cmp-clean against SKEP-cohesionWeave.PRISTINE.js EXIT 0 after every probe.
(b) IN FLIGHT: the religiousCenter rng-preserving probe (§5) + the digest positive control.
(c) NEXT: minTier dossier (§4), Charlatan/fort (§3), P5 26-row census (§6), car splits (§7).

TIMESTAMP 2026-08-23T20:21:34Z

## RESUME POINT (timestamp below) — §5 religiousCenter dossier COMPLETE
(a) PROVEN:
  R1 Charter's fix (delete the two city exclusiveGroup keys) reproduces the charter's digest EXACTLY:
     6127eae762dc8b4c4ef41b6ea1006d527c238cd679e45020fe9c3b776b600c18. 81/420 rosters, 113/420 records.
     This is ALSO the digest instrument's POSITIVE control — it does move for a real generation change.
  R2 ⚠⚠ THE CHARTER'S CITY PREMISE IS WRONG. `Multiple monasteries` reaches a city's FINAL roster
     0 times of 70 today — not "one or the other". Traced: 26/70 blocked by the exclusive group;
     21/70 PUSHED by the generator and then REMOVED by coherenceRepairPass as
     "unsupported_institution :: Multiple monasteries had no compatible, non-excluded dependency at
     this tier." (21/21, logged at the splice). 49/70 never reach the draw or lose it.
  R3 ⚠ THE FIX INJECTS AN OUT-OF-TIER ROW THE CHARTER NEVER NAMES: `Monastery or friary` (a TOWN row,
     itself exclusiveGroup religiousCenter) goes 0 -> 21 at city and 0 -> 5 at metropolis, because
     dropping the group unblocks coherenceRepairPass's isCompatible dependency add.
  R4 RNG-PRESERVING CURE WORKS. Side-stream variant (rng.fork per suppressed religiousCenter row,
     main stream untouched): 34/420 rosters vs 81; digest 4e259048edd95418fb4ab712a55fd3bac482b24e...;
     `Monastery or friary` stays 0 at city; monasteries city 18 / metropolis 48.
  R5 NAME SHIFT MEASURED, and the chair's framing is HALF WRONG: settlement names change 0/420 under
     BOTH variants. NPC name LISTS change 73/140 city+metropolis under A but only 5/140 under B;
     NPC counts 26 vs 2. The "~130 names" of the charter are institution + derived SERVICE names.
  R6 Each pipeline STEP gets rng.fork(name) (pipeline.js:156), so the reshuffle is confined to
     assembleInstitutions' own stream; downstream steps move only because the ROSTER moved.
  RESTORES: institutionalCatalog.js, assembleInstitutions.js, coherenceRepairPass.js, pipeline.js,
  cohesionWeave.js all cmp-clean against their PRISTINE copies (EXIT 0 each); git status -- src/ empty.
(b) IN FLIGHT: §4 minTier per-row dossier.
(c) NEXT: P5 26-row census + G5/G6 refutation probes; car splits vs the live manifest; unexecuted-claim sweep.

TIMESTAMP 2026-08-23T20:32:42Z

## RESUME POINT (timestamp below) — ALL PROBES EXECUTED, WRITING THE REPORT
(a) PROVEN since the last point:
  M1 minTier: 311 rows / 36 minTier / 26 REDUNDANT / 10 ABOVE / 0 BELOW — CONFIRMED.
     All 10 UI-visible via getInstitutionalCatalog + getInstitutionsForTier + getFullCatalogWithTierMeta
     at their block tier; generator eligible = false for all 10. H13 CONFIRMED.
     ⚠ NO copy-paste RUN: city/Entertainment holds them at positions 5,6,8,9 of 10 (7 is un-gated) and
     city/Exotic at 1,2,5,6,7 of 7 (3,4 un-gated). The pattern is INTERLEAVED = deliberate, not a slip.
     DELETE resolution re-run: 97/420 rosters (charter figure EXACT), digest 9d710694977db1ff77ff...
     (charter figure EXACT); Colosseum +28 / Multiple theaters +27 / Fighting pits +23 / Opera +21 EXACT.
     ⚠ Only 5 of the 10 move anything at default magic (4 Entertainment + Dragon resident +7).
     Planar traders / Dream parlors / Airship docking / Message network stay 0 (hiMagicInsts hard-zero),
     and village/Smuggling network stays 0/70 even with its minTier deleted.
  M2 ⚠⚠ CH-1's HEADLINE IS REFUTED FOR THE CAR AS SPECIFIED. With J-CH-1's declared override in the
     tree (the shipped CH-1, §1(f) names institutionalCatalog.js as a production file), the corpus
     digest is 20f4c34915681e1c1567843edbc86c0f738cb6de10e5327b44f4f0eb4bef5f5f, NOT base; 70/420
     records move; and tests/property/generatorGoldenMaster.test.js FAILS.
     THREE-WAY CONTROL, each exit captured with no pipe:
       base                     TRUE_EXIT=0  Test Files 1 passed / Tests 3 passed
       anchoring only           TRUE_EXIT=0  Test Files 1 passed / Tests 3 passed   <- what the charter ran
       anchoring + override     TRUE_EXIT=1  Test Files 1 failed / Tests 1 failed | 2 passed
     Drift key set = every village row, all cultures x all terrains.
  M3 ⚠⚠ hasCharityFacet REFUTED. base 417/420 TRUE, full-CH-1 417/420 TRUE, NEWLY charity-capable = 0.
     Village rows were already 70/70 at base. The charter's "70 villages become charity-capable that
     were not" is false by execution.
  M4 CONFIRMED exactly: J-CH-1 blast radius 41/420; hasVice 287 -> 217 (70 flips); CH-1 interior-kind
     111/420 with Priest 70 / Charlatan 26 / Warden's 17 / Dragon 6.
  M5 Other CH-1 battery files GREEN at the full patch: 10 files / 142 tests passed alongside the one
     golden-master failure (incl. tests/domain/undercityMonotoneComponents.test.js, the new consumer).
  M6 P5 census = EXACTLY 26 rows; mid-word matches 0; Magic+Exotic shelf = 28 (21+7);
     magical banking / magic item consignment / enchanting quarter = 0 catalog hits each.
  M7 G6/G5 refutations CONFIRMED WITH A POSITIVE CONTROL: Dragon resident 0 at dead and 0 at pm0 but
     11 at pm50; Great library 0/0 but 50 at pm50 and 70 at pm80. Adventurers' charter hall = 16 at
     magicExists:false (charter figure EXACT). Underground city survives dead-magic (12) and pm0 (21).
  M8 Manifest at tip: 168 entries, 167 LANDED + 1 SUPERSEDED, 0 NON-TERMINAL — CONFIRMED.
     Every non-CREATE packet path named in §4.3 EXISTS at the tip; the two walkers correctly do not.
     No production-file-count rule exists in scripts/implementation-packets.mjs — the <=3 ceiling is a
     chair convention, not machine-enforced.
  M9 R-INST-6-1 facets digest a542cc2e9470ed95468b648e2763b7832214a5c9f7f2d2431a0111b5632240de
     (charter figure EXACT), 0 rosters, 54/420 records. priorityCategory variant: 0 rosters, digest moves.
  RESTORES: every touched file cmp-clean; git status -- src/ EMPTY at every boundary.
(b) IN FLIGHT: writing $SP/draft-CH-SKEPTIC-REPORT.md.
(c) NEXT: the FINAL block.

TIMESTAMP 2026-08-23T20:43:12Z

## FINAL (timestamp below)

**DELIVERABLE:** `$SP/draft-CH-SKEPTIC-REPORT.md`, 35,054 bytes, 348 lines, **C0 = 0**, no emojis
(the `⚠` sign is the estate's own house vocabulary, as used throughout the charter).
Receipt: this file, **C0 = 0**. Charter re-scanned: **C0 = 0**.

**SIZES.** report 35,054 B · receipt (this file) 13,318 B · charter under attack 86,536 B, unmodified
(re-measured at hand-off; I never wrote to it).

**COUNTS BY VERDICT (54 rows).** 39 CONFIRMED · 4 REFUTED · 2 UNDERSTATED · 1 NEW defect the charter
does not name · 1 cosmetic mismatch · 4 untested-or-plausible · 3 confirmed-by-source-only.

**EXECUTED PROBE COUNT: 43.** 16 full 420-settlement corpus generations · one 2,100-settlement
five-case magic axis · three 140-settlement NPC sweeps · five instrumented 70-settlement traces ·
4 mutexed vitest batteries (GATE_MUTEX_LOCK_DIR inline, TMPDIR=/tmp/skep, exits captured with NO pipe)
· 14 static / source-census probes.

**MUTEXED BATTERY RECEIPTS (exit captured directly, never through a pipe):**
  golden master at BASE                       TRUE_EXIT=0   Test Files 1 passed (1) / Tests 3 passed (3)
  golden master at ANCHORING ONLY             TRUE_EXIT=0   Test Files 1 passed (1) / Tests 3 passed (3)
  golden master at FULL CH-1 (with override)  TRUE_EXIT=1   Test Files 1 failed (1) / Tests 1 failed | 2 passed
  11-file CH-1 blast-radius battery at FULL CH-1:  Test Files 1 failed | 10 passed (11);
      Tests 1 failed | 142 passed (143)  — the single failure IS the golden master.

**TREE HYGIENE.** Final proof taken against the COMMITTED BLOBS, not against my own copies:
`git show HEAD:<path> | cmp` EXIT 0 for cohesionWeave.js, institutionalCatalog.js,
assembleInstitutions.js, coherenceRepairPass.js, pipeline.js. `git status --porcelain` in the worktree
has **0 tracked-dirty lines**; only untracked `SKEP-*.mjs` probes remain (all copied to $SP).
The main repo was never written. Disk 18,429,196 KB free.
⚠ THE WORKTREE `$SP/laneTCSKEP-tree` IS LEFT IN PLACE, detached at 00e7af612 (which is the live
`claude/composite-r4` tip, so no ref is at risk). Reap it when the chair is done re-checking figures.

**THE THREE THINGS THE CHAIR MUST DECIDE**
 1. **CH-1's shape.** The charter's byte-identical-digest headline is TRUE of the anchoring and FALSE
    of the car: with J-CH-1's `facets` override the digest is 20f4c349..., 70/420 records move, and
    generatorGoldenMaster REDS (proved three ways). Either dispatch CH-1 as `cohesionWeave.js` ONLY and
    move the override to CH-3a beside R-INST-6-1's two identical keys (RECOMMENDED — restores the
    headline truthfully and frees CH-1/CH-3a from serial-minting), or re-declare CH-1 as a
    golden-re-record car with a SHIFT RECORD row.
 2. **`religiousCenter` (J-CH-3-2) at the right number.** The premise is wrong (`Multiple monasteries`
    is 0/70 at city today, 21 of them killed by coherenceRepairPass, not by the group), the fix injects
    a town-tier `Monastery or friary` into 21 cities + 5 metropolises that the charter never priced,
    and an rng-preserving side-stream cure cuts the shift from 81/420 to 34/420 with the NPC-name
    movement falling from 73/140 to 5/140. Rule against ~34 intended + ~47 collateral, not against 81.
 3. **`minTier` (J-CH-3-1) is not one class.** Take the READER fix for eight rows — the gated rows are
    INTERLEAVED with un-gated ones inside both blocks, which refutes the copy-paste-slip reading and is
    the evidence §491's framing lacks — but DELETE the village `Smuggling network` row outright (dead at
    0/70 with AND without its gate, and duplicated at city) and rule `Dragon resident` explicitly, since
    its own desc says "living in city" (cost +7 city settlements).

TIMESTAMP 2026-08-23T20:48:08Z
