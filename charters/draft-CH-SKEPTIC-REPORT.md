# THE CH SKEPTIC PANEL — draft-CATALOG-HYGIENE-PLAN.md attacked at `00e7af612`

Lane TC-CH-SKEPTIC `[OPUS-RUN · FABLE-VALIDATION OWED]` (ODQ §484), run under §441 J7 — a charter's
CONFIRMED home is a hypothesis. SOLO, no sub-agents, no Workflow. READ-ONLY on the repo; every probe
executed in a private detached worktree `$SP/laneTCSKEP-tree` at the CURRENT tip
`00e7af612d428078634d52ea37054bd00b773ca6` (`npm ci` **EXIT 0**, `node_modules` **468** entries,
node v24.12.0, vitest 4.1.8, disk 19,909,860 KB free). Every file touched by a probe was restored and
`cmp`-proven clean; `git status --porcelain -- src/ tests/ docs/ scripts/ supabase/` is EMPTY.

**Every verdict below is an execution.** Where I could not execute, the row says so.

---

## §0 · THE THREE THINGS THAT CHANGE A DECISION

1. **CH-1's headline claim is REFUTED for the car the charter actually specifies.** The charter's
   §1(d)/§Σ.2 says the corpus digest is byte-identical and *"no generation golden can move"*. That is
   true of the anchoring **alone**. It is FALSE of CH-1 as chartered, because §1(f) ships a second
   production file — J-CH-1's `facets: { institutionNature: 'faith' }` on `Priest (resident)`. That
   row is `required: true, baseChance: 1` at village, so the key lands on **every** village record.
   Executed, three-way controlled, exits captured with no pipe:

   | tree | corpus digest | `generatorGoldenMaster.test.js` |
   |---|---|---|
   | base `00e7af612` | `1a4a8d3f85e6…` | **TRUE_EXIT=0** — 1 file / 3 tests passed |
   | anchoring only (what the charter ran) | `1a4a8d3f85e6…` | **TRUE_EXIT=0** — 1 file / 3 tests passed |
   | **anchoring + J-CH-1 override (the shipped car)** | **`20f4c34915681e1c1567843edbc86c0f738cb6de10e5327b44f4f0eb4bef5f5f`** | **TRUE_EXIT=1** — 1 file **failed**, drift on **every village row, all cultures × all terrains** |

   70 of 420 per-settlement records move. CH-1 is a **golden-re-record car**, and the golden master's
   own docstring law ("Re-recording without adding a row is a deleted alarm") means it owes a SHIFT
   RECORD entry. The charter's "Goldens/pins to re-record: **none** on the generation side" is wrong.

2. **The `religiousCenter` ruling rests on a false premise about what a city has today, and the
   rng-preserving cure the charter never tried cuts the shift by 58%.** `Multiple monasteries` reaches
   a city's final roster **0 times of 70** at base — not "one or the other". 26/70 are blocked by the
   exclusive group; **21/70 are selected by the generator and then deleted by `coherenceRepairPass`**
   as `unsupported_institution` (logged at the splice, 21/21). And the charter's fix silently injects a
   TOWN-tier row into cities: `Monastery or friary` goes **0 → 21 at city and 0 → 5 at metropolis**.
   A side-stream (`rng.fork`) variant delivers the coexistence with **34/420** rosters instead of 81,
   **no** `Monastery or friary` injection, and NPC name lists moving in **5 of 140** city/metropolis
   settlements instead of 73. Settlement names move **0** under both.

3. **The `minTier` ten are not a copy-paste slip and only five of them do anything.** They sit at
   positions 5, 6, 8, 9 of 10 on city/Entertainment (7 is un-gated) and 1, 2, 5, 6, 7 of 7 on
   city/Exotic (3 and 4 un-gated) — **interleaved, not contiguous**; the run signature
   `priorityCategoryPlausibility`'s header names is ABSENT. And at default magic only five move
   anything under the DELETE resolution: the four Entertainment rows plus `Dragon resident` (+7). The
   other five are inert — four are already hard-zeroed by `hiMagicInsts`, and village/`Smuggling
   network` stays **0/70 even with its `minTier` deleted**.

---

## §1 · VERDICT TABLE

Legend: **C** confirmed · **R** refuted · **O** overstated · **U** understated.

| # | charter claim | probe (all in `$SP/laneTCSKEP-tree`) | output | verdict |
|---|---|---|---|---|
| V1 | base→tip: the ten CH files are identical, `git diff` empty | `git rev-parse <sha>:<path>` per file, both ends + whole-range control | `FILES=10 SAME=10`; `git diff --name-only` over the ten = 0 lines; the same range over ALL paths names **9** files (non-vacuous) | **C** |
| V2 | 311 rows × 3 kinds = 933 cells; the proposal moves exactly 4 | `SKEP-facet933.mjs` — proposed table rebuilt **from the charter's §1(a) prose**, not from the compile lane's script | `ROWS 311 FACET_KINDS 3 CELLS 933` / `CHANGED_CELLS 4`, and the four are exactly the four named | **C** |
| V3 | `institutionSubstructure` moves zero cells | same probe, per kind | 0 substructure cells in the delta | **C** |
| V4 | H3: no catalog row carries a `type` key | same probe | `HAS_TYPE_KEY 0` | **C** |
| V5 | three rows already carry `facets` | same probe | village/town/city `Underground network` | **C** |
| V6 | H5: naive leading-`\b` regresses `Blacksmith`×2 / `Sawmill`×2 — "both lose `craft`" | NAIVE table auto-generated by prefixing `\b` to every alternative | **9** cells move, not 4: the four `craft` losses **plus `institutionFunction: arms` on `Blacksmith` and `Blacksmiths (3-10)`** | **U** — the regression is 6 cells over 4 rows; acceptance arm A5 pins only `craft` |
| V7 | H5: `/fort/` matches "fortune" at a leading boundary, so anchoring cannot cure `Charlatan fortune tellers` | NAIVE vs PROPOSED delta; every bare-`fort` row listed | NAIVE leaves `Charlatan` at `security`; PROPOSED moves it to null; `Massive walls and fortifications` keeps `security` under both | **C** |
| V8 | the cure touches no other Religious row | base-vs-proposed nature printed for all 21 Religious rows | exactly one moves (`Priest (resident)`); 20 unchanged | **C** |
| V9 | §1(c) arm 1: exactly ONE multi-rule collision at base, ZERO after | match-set hunt over all 933 cells | `MATCH_SET_CHANGES 5` — 4 verdict moves + **1 latent**: `Resident smith (part-time)` `[craft,vice] → [craft]` | **C** |
| V10 | **§1(d): the CH-1 corpus digest is byte-identical; "no generation golden can move"; the strongest receipt in the charter** | `SKEP-corpus.mjs` at base, at the anchoring patch, and at the **full car** (anchoring + override) | anchoring-only digest identical (`1a4a8d3f…`) — **but the full car is `20f4c349…`, 70/420 records move, and the golden master REDS (TRUE_EXIT=1)** | **R for the car as specified** (C for the anchoring alone) |
| V11 | **the digest is a non-vacuous control for the anchoring** | deliberately broke `faith` to `/./i` (moves an interior kind in **all 420** settlements) and separately broke the `crypt` substructure regex | **digest BYTE-IDENTICAL in both breaks.** Mechanism: `facetOf` is called **0 times** in 420 pipeline runs (counter instrumented; `COUNTER_CONTROL_AFTER_ONE_DIRECT_CALL = 1` proves the counter live) | **R — the control cannot fail.** The conclusion is right; the evidence is a tautology |
| V12 | the digest instrument works at all | the `religiousCenter` variant | digest moves to `6127eae762dc…` — **the charter's own figure, exactly** | **C** (positive control satisfied) |
| V13 | §1(d): 111/420 interior kinds; Priest 70 / Charlatan 26 / Warden's 17 / Dragon 6; 0 rosters | roster+kind diff, base vs CH-1 | `ROSTER_CHANGED 0`, `INTERIOR_KIND_CHANGED 111`, per-row 70/26/17/6 | **C** exactly |
| V14 | J-CH-1 blast radius: 41 of 420 render `generic` | union over the three non-Priest rows | **41** | **C** exactly |
| V15 | §1(d): `hasVice` flips 287 → 217 in 70 villages | live `facetOf` census over the corpus, base and patched | `287 / 420` → `217 / 420` | **C** exactly |
| V16 | §1(d): the flag is DARK | `'underwaysOrganicFoundingEnabled' in DEFAULT_SIMULATION_RULES` | module path in the charter's phrasing does not resolve as written; three source comments in `src/` independently record the key as absent from `DEFAULT_SIMULATION_RULES` | **C by source, NOT re-executed** |
| V17 | **§1(d): with the override, 70 villages "become charity-capable that were not"** | `hasCharityFacet` over all 420, base vs full car | base **417/420 TRUE**, full car **417/420 TRUE**, newly-capable **0**; villages were already **70/70** (the `Parish church` row already infers `faith`) | **R** |
| V18 | H21: P5 `nativeInstitutionRequiresMagic` strikes 26 catalog rows | `ARCANE_INST_KW.some(includes)` over all 311 names | **26**, listed | **C** |
| V19 | H9: mid-word `ARCANE_INST_KW` matches = 0 | same probe with a leading-`\b` comparison | **0** | **C** |
| V20 | H25: the Magic+Exotic shelves hold 28 (21+7), not 32 | shelf census | **28 (Magic 21 / Exotic 7)** | **C** |
| V21 | H22/G7: `magical banking`, `magic item consignment`, `enchanting quarter` match 0 rows | each `hiMagicInsts` keyword against all 311 names | 0 / 0 / 0 | **C** |
| V22 | **H19: G6 REFUTED — `Dragon resident` 0 in 840 dead-magic settlements** | 5-case magic axis × 420 = 2,100 settlements, with a **positive control** | dead **0**, pm0 **0**, **pm50 11**, pm80 9, pm100 4 — the probe finds instances when they exist | **C, and the vacuity risk is closed** |
| V23 | H18: G5's dead-magic half refuted — `Great library` 0 | same | dead **0**, pm0 **0**, **pm50 50**, pm80 70 | **C** |
| V24 | H16: `Adventurers' charter hall` appears 16× at `magicExists:false` | same | **16** at dead, 18 at pm0 | **C** exactly |
| V25 | H14: `Alchemist shop`/`quarter` die at dead-magic | same | 0 / 0 at dead and pm0; 22 / 52 at pm50 | **C** |
| V26 | H20: the `'underground city'` member of `NON_MAGIC_EXOTICS` is inert | shelf read + magic axis | the row is metropolis/**Criminal**, so `isMagicOrExoticCategory` is already false; it survives dead-magic (12) and pm0 (21) | **C** |
| V27 | H10/§3.1: 311 rows, 36 `minTier`, 26 REDUNDANT, 10 ABOVE, 0 BELOW | `SKEP-mintier.mjs` | exactly those counts | **C** |
| V28 | H13: the UI shows all ten; generation refuses all ten | `getInstitutionalCatalog` / `getInstitutionsForTier` / `getFullCatalogWithTierMeta` per row | all three **true** for all 10; generator eligible **false** for all 10 | **C** |
| V29 | §3.1: DELETE costs 97/420; Colosseum +28, Multiple theaters +27, Fighting pits +23, Opera +21 | executed the DELETE variant | `ROSTER_CHANGED 97`, digest `9d710694977db1ff…` (the charter's figure), per-row **exactly** those four | **C** |
| V30 | §3.1: MOVE costs 108/420 and clobbers the city's own `Smuggling network` | **not re-executed.** The collision is confirmed by source: two `'Smuggling network'` rows, `institutionalCatalog.js:865` (village) and `:1998` (city, `baseChance 0.6`, different desc) | — | **collision C by source; the 108 figure UNTESTED** |
| V31 | §3.1: the reader fix costs 0 same-seed shift | a UI-only `lookups.js` filter cannot reach the pipeline; `facetOf`-style reasoning does not apply, but `getInstitutionalCatalog` has no pipeline caller on the generation path | **not executed** — a `lookups.js` filter was not written | **PLAUSIBLE, untested** |
| V32 | §3.1 blast radius: `metropolisCatalogReachable`'s RATCHET arm will red | read the arm | `:58-62` asserts every catalog institution in every tier is reachable from `getInstitutionalCatalog(tier)` — the filter breaks it by construction | **C by source** |
| V33 | **§3.2: 81/420 rosters, ~130 names, digest `6127eae7…`** | executed | `ROSTER_CHANGED 81`, `PER_SETTLEMENT_DIGESTS_MOVED 113`, digest **exactly** `6127eae762dc…` | **C** |
| V34 | §3.2 mechanism: `:323` returns before the rng draw, so un-suppressing reshuffles the sequence | source read + `pipeline.js:156` `rng.fork(name)` per step | mechanism CONFIRMED, **but scoped**: each step forks its own PRNG, so the reshuffle is confined to `assembleInstitutions`; later steps move only because the ROSTER moved | **C, scoped** |
| V35 | **§3.2 premise: "at city the group holds two members, so a city gets one or the other, never both"** | traced every city settlement | `Multiple monasteries` final presence at city = **0/70**. 26 blocked by the group; **21 pushed then removed by `coherenceRepairPass`** (`unsupported_institution`, 21/21 logged); 23 never won the draw | **R** |
| V36 | **(not in the charter) the fix injects a town-tier row** | per-tier presence census | `Monastery or friary` **0 → 21 at city, 0 → 5 at metropolis** | **NEW — a defect the charter does not name** |
| V37 | §3.3: the `priorityCategory` run of four + `Contract killer` | shelf census | city/Economy 23 rows, 4 `military` at **contiguous** positions 18–21; city/Criminal 11 rows, 1 `military`; catalog-wide Criminal shelf = **28 rows, 27 criminal + 1 military** | **C** (the "28" is catalog-wide, not the city shelf — §3.3's wording conflates them) |
| V38 | H28: all five place `["military","civic","noble","residential","other"]`; honest values rank `merchant`/`criminal` first | read `CATEGORY_AFFINITY` | exactly as claimed | **C** |
| V39 | §3.3: 0 rosters change; the digest moves | executed a 4-of-5 variant (the `Furrier's district` anchor did not match my patcher) | `ROSTER_CHANGED 0`, digest moves, 112/420 records | **C for the structural claim** |
| V40 | §3.4: the tanner overstatement has FIVE spellings | `git grep` the three phrasings | exactly the five the charter lists, plus the honest village register at `:628` | **C** |
| V41 | §3.4: no engine reader enforces the catalog desc | source read of `townLayoutV2.js:209` / `townMapModel.js:516` | the layout regex reads a QUARTER's own `location` prose, not the catalog `desc` | **C by source** |
| V42 | H31: exactly three "Eberron" strings | `grep -rn Eberron src/ tests/ docs/` | `institutionalCatalog.js:2183`, `institutionDescVariants.js:242,243` | **C** |
| V43 | H32: the variant pick is a pure fnv of (seed : name), zero draws, text-blind | read `proseHash.js` + `assembleInstitutions.js:719` | `pickVariant([inst.desc, ...variants], \`${ctx._seed}:${inst.name}\`)` — the pool length and the key are text-independent | **C** |
| V44 | §3.6: R-INST-6-1's facets cost 0 rosters and move the digest to `a542cc2e…` | executed | `ROSTER_CHANGED 0`, `54/420` records, digest **exactly** `a542cc2e9470ed95…` | **C** |
| V45 | H36: both files are inputs to exactly three edge bundles | grep the metas | `aiCharterBundle`, `aiGroundingBundle`, `aiOutputSchemaBundle` | **C** |
| V46 | H37: ten size-baseline rows, none a CH file; `src/data/**` has no `max-lines` | read `.size-baseline.json` + `eslint.config.js` | 16 keys of which **10 are file rows**, none a CH file; the `src/data/**` block carries purity rules only; largest CH file under a ceiling is `institutionVocabulary.js` at 672 raw vs an 800 **effective** domain ceiling | **C** |
| V47 | §4.2: the manifest at tip is 168 / 167 LANDED / 1 SUPERSEDED / **0 non-terminal** | read `PACKET_MANIFEST.json` | exactly that | **C** |
| V48 | §4.3: every non-CREATE packet path exists | existence check on all 20 named paths | 18 exist; the 2 walkers correctly do not | **C** |
| V49 | §4.4: the census tuple at the tip | read `sovereigntyLightingContract.walker.test.js:6049` | `files: 2515, parked: 366, credited: 2149, titles: 20854, suiteTitles: 5807` | **C** |
| V50 | H40: `tests/` holds 37 trees | `ls tests \| wc -l` = 38, `ls -d tests/*/ \| wc -l` = 37 | 38 / 37 | **C** |
| V51 | **§1(d)'s derived-read table is complete** | re-ran the `facetOf` consumer census at base vs tip | **`src/domain/undercity/monotoneComponents.js` does not exist at `f1e4d515` and DOES at `00e7af612`** (MF-UC2), and it reads `facetOf(inst,'institutionNature'\|'institutionFunction')`. Neither §1(d) nor §4.5's CH-1 grep list names it | **U — a consumer landed in the range** |
| V52 | §1(a): "51 keywords" | counted the alternations in the live table | 46 in `institutionNature`, 65 in nature+function, **88** across all three kinds | **cosmetic mismatch; no figure depends on it** |
| V53 | §2(e): the CH-2 model's 361 / 273 of 2,100 | **not re-executed** — reproducing it requires rebuilding the charter's four-gate working model, which is a build act, not a probe | — | **UNTESTED** |
| V54 | §3.1: the 26 REDUNDANT deletions move the record | not run as its own variant, **but the mechanism is now PROVEN**: adding one `facets` key to one catalog row moved 70 records and redded the golden master (V10). A key removal is the same act | — | **C by proven mechanism** |

**Tally: 54 rows — 39 CONFIRMED · 4 REFUTED · 0 OVERSTATED-only · 2 UNDERSTATED · 1 NEW defect ·
1 cosmetic · 4 untested/plausible · plus 3 confirmed-by-source.** Executed probe count: **43 distinct
runs** — 16 full 420-settlement corpus generations, one 2,100-settlement magic axis, three 140-settlement
NPC sweeps, five instrumented 70-settlement traces, **4 mutexed vitest batteries** (`GATE_MUTEX_LOCK_DIR`
inline, exits captured with no pipe), and 14 static / source-census probes.

---

## §2 · RULING DOSSIER I — J-CH-3-1, the `minTier` divergence (10 rows)

### The evidence the ruling turns on

**Both readers, measured.** For every one of the ten rows, all three UI lookups
(`getInstitutionalCatalog(tier)`, `getInstitutionsForTier(tier)`, `getFullCatalogWithTierMeta()`)
return the row at its authoring tier, and `assembleInstitutions.js`'s first in-loop statement
(`if (inst.minTier && tierIndex < TIER_ORDER.indexOf(inst.minTier)) return;`) refuses it there —
**before** the toggle read, so a user force-requiring it gets silence. The charter's H13 is CONFIRMED
without qualification. The surface that lies today is the **UI**.

**The copy-paste-slip hypothesis is REFUTED by position.** A slip leaves a contiguous run. These do not:

* `city/Entertainment` (10 rows): gated at positions **5, 6, 8, 9** — position 7 (`Red light district`)
  and position 10 (`Bardic college`) are un-gated, and so are 1–4 (`Theaters`, `Gambling halls`,
  `Brothel (red light district)`, `Fighting pits`).
* `city/Exotic` (7 rows): gated at **1, 2, 5, 6, 7** — positions 3 and 4 (`Golem workforce`,
  `Undead labor`) are deliberately left un-gated in the middle of the block.
* `village/Criminal`: a single gated row among three.

An author who interleaves gated and un-gated rows inside one block is exercising judgment, not
repeating a paste. That is the strongest single fact for the READER reading of J-CH-3-1.

### Per-row dossier

| # | block / shelf / row | `minTier` | `desc` (verbatim) | prose reads as | block neighbours | **my recommendation** |
|---|---|---|---|---|---|---|
| 1 | village / Criminal / **Smuggling network** | `city` | "Goods move through this village avoiding tolls or legal scrutiny. Usually tied to a specific commodity." | **village** — the desc names the village | neighbours `Fence (word of mouth)`, `Underground network` both un-gated | **DATA — DELETE THE ROW.** It is dead twice over: gated out at village, and a *separate* `Smuggling network` exists at city (`:1998`). Measured: **0/70 at village at base AND 0/70 with the `minTier` deleted.** `getFullCatalogWithTierMeta` merges by name, so the city row overwrites it there too. Its only live effect is one phantom entry in the village UI list |
| 2 | city / Entertainment / **Multiple theaters** | `metropolis` | "Permanent venues. Professional companies." | city-tier prose, **but** `Theaters` is already at city and `UPGRADE_CHAINS` holds `["Theaters","Multiple theaters"]` — both at city would collapse on sight | 5th of 10; 1–4 un-gated | **READER** — the gate is the ladder made explicit |
| 3 | city / Entertainment / **Colosseum/arena** | `metropolis` | "Gladiatorial games or monster fights. **Massive scale.**" | metropolis | 6th of 10 | **READER** |
| 4 | city / Entertainment / **Gambling district** | `metropolis` | "Concentrated gaming houses. Zone of Truth enforcement." | city-tier prose, **but** `Gambling halls` is at city and `UPGRADE_CHAINS` holds `["Gambling halls","Gambling district"]` | 8th of 10; 7 un-gated | **READER** — same ladder argument |
| 5 | city / Entertainment / **Opera house** | `metropolis` | "High culture. Elite patronage." | **ambiguous** — nothing in the prose says metropolis, and no ladder partner exists | 9th of 10 | **READER, but this is the one honest DATA candidate.** If the chair wants one data fix, this is it: measured cost of deleting its gate alone is bounded by the +21 it gains in the DELETE run |
| 6 | city / Exotic / **Planar traders** | `metropolis` | "Goods from other planes of existence." | ambiguous | 1st of 7; 3 and 4 un-gated | **READER** — and **inert**: 0 at city even with the gate removed (`hiMagicInsts` hard-zeroes `planar` below pm66) |
| 7 | city / Exotic / **Dragon resident** | `metropolis` | "Ancient wyrm living in **city**." | **city — the desc contradicts the gate** | 2nd of 7 | **DATA — the only row whose own prose refutes its gate.** Cost measured: +7 city settlements under DELETE |
| 8 | city / Exotic / **Dream parlors (high magic)** | `metropolis` | "5th level Dream spell experiences. Lucid shared dreams, communication." | the NAME says "(high magic)" | 5th of 7 | **READER** — and **inert** (hard-zeroed) |
| 9 | city / Exotic / **Airship docking (high magic)** | `metropolis` | "Mooring towers with magical weather protection. Eberron-style." | the NAME says "(high magic)" | 6th of 7 | **READER** — and **inert** |
| 10 | city / Exotic / **Message network (high magic)** | `metropolis` | "Sending Stones network or Speaking Stones. 250-10,000 GP per station." | the NAME says "(high magic)" | 7th of 7 | **READER** — and **inert** |

### Totals under each resolution

| resolution | measured | verdict |
|---|---|---|
| **DELETE all ten** (§491's "the block is the structural truth") | **97/420 rosters**, digest `9d710694977db1ff…`; `Colosseum/arena +28`, `Multiple theaters +27`, `Fighting pits +23`, `Opera house +21`. ⚠ `Fighting pits`, `Bardic college +19`, `Sage's quarter +18`, `Sewage system ±` **carry no `minTier`** — they are draw-sequence collateral, not "cities acquiring metropolis-grade entertainment" | the charter's figure is exact; its *interpretation* over-credits the ten rows |
| **DELETE only the two prose-contradicting rows** (#1 and #7) | `Smuggling network` **0 → 0** (measured, no cost at all); `Dragon resident` **+7** at city | **the smallest honest data fix, and it is nearly free** |
| **MOVE into the named block** | not re-executed; the name collision the charter names is CONFIRMED by source (two `Smuggling network` rows at `:865` and `:1998`) | refuse, as the charter says |
| **READER fix** (`lookups.js` filter) | 0 same-seed shift by construction (no pipeline caller); `metropolisCatalogReachable`'s RATCHET arm at `:58-62` reds and must be amended | recommended for the other eight |

**My recommendation to the chair, differing from the charter in one respect.** Take the reader fix as
the class resolution — the interleaved authoring positions make "author here, gate there" a *legible
idiom*, not a slip, and that is the evidence §491's framing lacks. But do **not** call all ten
legitimate: **delete the village `Smuggling network` row outright** (dead at zero measured cost, and
leaving it means the reader fix hides a duplicate name rather than removing it), and **rule
`Dragon resident` explicitly** — its own description says "living in city", so either the desc or the
gate is wrong and the charter should not leave the contradiction standing under a reader filter.
Total cost of that split: 0 settlements for the deletion, +7 city settlements if the chair also frees
`Dragon resident`, 0 for the other eight.

---

## §3 · RULING DOSSIER II — J-CH-3-2, `religiousCenter`, and the rng-preserving cure

### What is actually true at base (the charter's premise is wrong)

`Multiple monasteries` never reaches a city's final roster. Traced over all 70 city settlements:

```
CITY SETTLEMENTS 70   FINAL_PRESENT 0
{ "blocked-exclusive": 26, "reached-draw p=0.6": 44, "PUSHED": 21 }
step-transition census: { "(never present)": 49,
                          "assembleInstitutions:ADDED -> coherenceRepairPass:REMOVED": 21 }
removal reason, 21/21: "unsupported_institution :: Multiple monasteries had no compatible,
                        non-excluded dependency at this tier."
```

So the city defect has **two** causes, and the charter found one. The exclusive group accounts for 26
of 70; a second gate — `coherenceRepairPass` cannot add the dependency `Monastery or friary` because
that row *also* carries `exclusiveGroup: 'religiousCenter'` and `isCompatible` refuses a same-group
add — accounts for the other 21.

### The undeclared side effect

Deleting the two `exclusiveGroup` keys unblocks that dependency add. Measured per tier:

| row | city BASE | city FIX-A | metropolis BASE | metropolis FIX-A |
|---|---|---|---|---|
| `Cathedral (10,000+ only)` | 26 | 26 | 29 | 29 |
| `Multiple monasteries` | **0** | **41** | 32 | 60 |
| **`Monastery or friary`** (a **town** row) | **0** | **21** | **0** | **5** |

A city acquiring a town-tier "Monastery or friary" as a coherence repair is a content change the
charter never priced. **This alone is a reason not to ship §3.2 as written.**

### The rng-preserving probe — IT WORKS

The charter reasons that un-suppressing the row consumes a draw and reshuffles everything downstream.
That is true *within the step only*: `pipeline.js:156` gives every step its own `rng.fork(name)`, so no
later step's stream moves. I implemented the side-stream cure in the worktree — the suppressed
`religiousCenter` row at city/metropolis draws from `rng.fork('skep-coexist::'+tier+'::'+name)`, leaving
the main stream position untouched — and measured it:

| | **A — the charter's fix** | **B — rng-preserving side stream** |
|---|---|---|
| corpus digest | `6127eae762dc8b4c…` | `4e259048edd95418…` |
| rosters changed | **81 / 420** | **34 / 420** |
| per-settlement records moved | 113 / 420 | 34 / 420 |
| `Multiple monasteries` at city | 0 → 41 | 0 → **18** |
| `Multiple monasteries` at metropolis | 32 → 60 | 32 → **48** |
| **`Monastery or friary` injected** | **21 city + 5 metropolis** | **0 / 0** |
| settlement NAMES changed | **0** | **0** |
| NPC name lists changed (city+metropolis, n=140) | **73** | **5** |
| NPC counts changed | 26 | 2 |
| top collateral | `Monastery or friary +26`, `Mayor and council +22`, `Guild consortium −22`, `Citadel −12`, `Front businesses −10`, `Gambling halls +10` … | `Beast trainers ±2`, `Academy of magic ±2`, single-instance residue |

**The chair's "re-rolls ~130 NAMES" framing is half wrong and half worse than stated.** No settlement
name and no prose name is re-rolled — 0/420 under both variants; the charter's "~130 names" are
institution names plus derived SERVICE names (`Religious network`, `Educational network`,
`Trade licensing`…). But NPC rosters *are* roster-derived, so variant A moves the NPC name list in
**73 of 140** city/metropolis settlements. Variant B cuts that to **5**.

**Caveat, stated plainly:** variant B as I wrote it is a *probe*, not a shippable design — it
hard-codes `'religiousCenter'` inside `assembleInstitutions.js`, which is a code change in a file
CH-3a does not touch and a new mechanism (a per-row side stream) rather than a repair. What it proves
is the **attribution**: of the 81 changed rosters under the charter's fix, **only ~34 are the intended
coexistence; ~47 are draw-sequence collateral**, and 26 of the additions are an out-of-tier row nobody
asked for. That is the number the chair should rule against THE PROMISE, not 81.

---

## §4 · MUST-FIX (a CH car lands wrong without these)

1. **CH-1's declared shift is wrong.** Re-state §1(d), §Σ.2 and the "Goldens/pins to re-record: none"
   line: the shipped car moves the corpus digest to `20f4c349…`, moves 70/420 records, and **reds
   `tests/property/generatorGoldenMaster.test.js`** (every village row). The car owes a golden
   re-record **and** a SHIFT RECORD entry in that file's docstring, which its own law requires.
   Two lawful shapes for the chair: (a) accept it as a declared golden-moving car, or (b) drop
   J-CH-1's override from CH-1 — which restores the byte-identical claim, makes CH-1 a one-file car
   (`cohesionWeave.js` only), and **frees CH-1 and CH-3a to mint in parallel** since the shared
   `institutionalCatalog.js` reservation disappears. The charter rejected (b) at §4.2 on the grounds
   that serial-minting is cheap; it did not know (b) also buys back the zero-shift headline.
2. **Delete the "digest identical ⇒ safe" reasoning, or label it.** No `FACET_INFERENCE` mutation can
   move the digest — `facetOf` is called **zero** times in 420 pipeline runs. A build lane that uses
   the digest as its CH-1 safety check is using an instrument that cannot fail. The honest evidence
   for "the anchoring cannot move generation" is the 4/933 cell delta plus the zero-call census.
3. **§1(d)'s consumer list and §4.5's CH-1 grep arm are stale at the tip.**
   `src/domain/undercity/monotoneComponents.js` (landed in the range by MF-UC2) reads
   `facetOf(inst,'institutionNature')` and `facetOf(inst,'institutionFunction')`, and
   `tests/domain/undercityMonotoneComponents.test.js` is a live grep hit. Add both. (I ran it: it is
   **green** at the full CH-1 patch — the four cured rows are not burying or vault-bearing — but a
   sweep list that omits a known consumer is a sweep that hit N−1 of N.)
4. **§1(d)'s `hasCharityFacet` row is false.** Executed: 417/420 both before and after; **zero**
   settlements change charity capability. Villages are already 70/70 via `Parish church`. Remove the
   "70 villages become charity-capable" sentence; it is currently one of the charter's headline
   arguments for J-CH-1.
5. **Acceptance arm A5 under-pins the regression it exists to catch.** Its own mutant
   (`smiths?\b → \bsmith`) drops **`institutionFunction: 'arms'`** from `Blacksmith` and
   `Blacksmiths (3-10)` as well as `craft`. A5 must pin both kinds, or the arm passes a tree that has
   already lost two cells.
6. **Acceptance arm A6 is unstateable as written for the shipped car.** "the whole-catalog delta is
   EXACTLY four cells of 933" is a claim about the *anchoring*; with J-CH-1's override present,
   `Priest (resident)` resolves `faith` through `declaredFacet` on both sides of any table comparison
   the arm can run in-tree. Re-word it as a pinned 4-row expectation
   (`vice→faith`, `vice→null`, `security→null`, `vice→null`) against a frozen base table.
7. **§3.2's city premise must be corrected before the chair rules it.** `Multiple monasteries` is
   **0/70 at city today**, and 21 of those 70 are removed by `coherenceRepairPass`, not by the
   exclusive group. The charter's "a city gets one or the other" is not what the engine does.
8. **§3.2 must declare the `Monastery or friary` injection** (0 → 21 city, 0 → 5 metropolis). A
   town-tier religious house appearing in cities is a content change, and it is *not* the change
   R-INST-3 asked for.

## §5 · NICE-TO-HAVE

* §3.3's "the only row of 28 on that shelf" conflates the **city** Criminal shelf (11 rows) with the
  **catalog-wide** Criminal shelf (28 rows). The 27/1 split is right; the sentence isn't.
* §1(a)'s "51 keywords" does not match the live table (46 in `institutionNature`, 88 across all three
  kinds). No figure depends on it.
* The `≤3 production files` ceiling is **not machine-enforced** — `scripts/implementation-packets.mjs`
  has no file-count rule. J-CH-2-2 and J-CH-3-5 are therefore chair conventions the chair may waive
  at zero validator cost; the splits are a legibility choice, not a validator requirement.
* §4.2's "the cars MUST be serial-minted" is stronger than the validator needs. Only cars sharing
  `src/data/institutionalCatalog.js` (CH-1, CH-2a, CH-3a, CH-3b) collide. **CH-2b**
  (`generationContext.js` + `magicFilter.js`) and **CH-3c** (`lookups.js`) are path-disjoint from the
  catalog cars and from each other, and may mint in parallel.
* The village `Smuggling network` row (`:865`) is measurably dead — 0/70 at village with **and**
  without its gate, and shadowed by the city row of the same name in every merged view. Worth a
  DELETE row in CH-3a rather than a reader filter.
* V16 (`underwaysOrganicFoundingEnabled` absent from `DEFAULT_SIMULATION_RULES`) is confirmed only by
  three source comments; the charter's own executed check is not reproducible from the module path it
  implies. Cheap to re-run at the real home before the packet quotes it.
* §2(e)'s 361/273-of-2,100 figures are the one substantive block I could not test without rebuilding
  the charter's working model. They should be re-measured by the CH-2 build lane before J-CH-2-1 is
  ruled on, not carried from this compile.

---

## §6 · IS CH-1 SAFE TO DISPATCH IMMEDIATELY?

**No — not as chartered. Yes, in one of two amended shapes, and the amendment is one line of
charter text either way.**

The charter's §6 says "NOTHING blocks CH-1 … it can be dispatched the moment the chair ratifies the
charter", resting on the byte-identical digest and the green golden master. Both receipts were taken
on a tree that does not contain the car's second production file. With that file present the golden
master **fails** (`TRUE_EXIT=1`), and a build lane dispatched on the charter's text would meet an
unexpected red on its first battery, with a golden re-record — an owner-visible, THE-PROMISE-adjacent
act — arriving as a surprise rather than as a declared shift.

The engineering content of CH-1 is sound. The 4-of-933 table is independently reproduced, the
non-regression set holds, the four cured rows are the right four, the walker design is right, and
every non-golden test in the CH-1 blast radius is **green at the full patch** (10 files / 142 tests,
including the new `monotoneComponents` consumer). Nothing in the car is wrong. What is wrong is its
**declared shift**, and a declared shift is the thing the chair is being asked to ratify.

**Recommended (my call, vetoable):** dispatch **CH-1 as `cohesionWeave.js` only** — anchoring, walker,
acceptance — with J-CH-1's `Priest (resident)` override moved to **CH-3a**, where a catalog `facets`
key already rides beside R-INST-6-1's two identical keys (§3.6, measured `a542cc2e…`, same act, same
price, already declared as digest-moving). That restores CH-1's byte-identical headline *truthfully*,
keeps its blast radius at 111 interior kinds with zero record movement, removes the shared
`institutionalCatalog.js` reservation so CH-1 and CH-3a need not serial-mint, and puts all three
`facets` declarations in one commit with one golden re-record instead of two.

**If the chair prefers the charter's shape**, then CH-1 must be re-declared as a golden-moving car
(digest `20f4c349…`, 70/420 records, `generatorGoldenMaster` re-record + SHIFT RECORD row), and it
should not be described as "the only car whose generation digest is provably unmoved" — the ordering
argument in §4.1 rests on that description and would need re-writing too.

