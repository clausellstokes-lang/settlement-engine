# TE-ADVAL — THE AD CHARTER'S FABLE VALIDATION AT THE JUDGMENT LAYER

**Lane:** TE-ADVAL (Fable seat, solo), dispatched under ODQ §623.2's validation split: judgments do not decay — figures do.
**Object:** `charters/draft-AD-CHARTER.md` at `refs/preserve/ad-charter-2026-08-24` = `f23e7978ed1f825a7af137aeca9e51df8065caac`.
**Provenance chain, executed this lane:** `git rev-parse` on the ref → `f23e7978e…` · `git cat-file -s` on the blob → 122,346 B · extracted copy sha256 `e4ddb509cb7e564208cb4374abb5e7b8a688325e9d29f6eeb84abbde043815ca`, matching §526.1's recorded `sha256 e4ddb509…` and §544.1's byte-identical rescue proof. The document under validation is CONFIRMED to be the document the ledger consumed.
**Method:** the whole charter read (1,377 lines) and the whole register program read (`docs/DESIGN_REGISTER_PROGRAM.md`, 483 lines, working tree at HEAD `3c1c505f6` — body + amendments A1–A6). Current-reality checks are structural reads at the slot branch tip `claude/composite-r4` = `e4ed27f48` (67 commits past the charter's slot `5055990a3`) and at the ledger (§§526, 530, 586, 596, 619–623). READ-ONLY throughout; no repo write, no ref, no gate, no memory write.
**What this lane did NOT do (per §623.2):** re-measure any slot-stamped figure. §V3 inventories them for the dispatch window instead.

---

## §V1 · THE JUDGMENT VERDICT TABLE

Each verdict is re-derived from the charter's own cited evidence plus current reality. "RATIFIED" means the reasoning re-derives cleanly and nothing landed since contradicts it; annotations name what the dispatch window must carry.

| Judgment | Verdict | Re-derivation |
|---|---|---|
| **J-AD-1** — a DW vocabulary member addresses its motif by ENUM MEMBER through a registry, never by string match; the 18 name regexes keep their one existing job | **RATIFIED** | The evidence chain holds: DW-1's vocabularies are closed enums; `glyphAssign.js`'s regex waterfall exists only because the town-map path has no enum to key on (the file's own header, quoted in charter §3.2); and the prose-token-pin hazard class is real and estate-bitten (standing memory row). The reversal clause ("delete the registry indirection and add regexes; nothing else depends on the choice") is stated correctly and is genuinely cheap to execute — a well-formed vetoable judgment. **Register-era annotation:** REG-3's shape code is a second class→visual-form resolution system ("every map-visible class resolves to exactly one family"). Nothing in A2/A3 states its addressing discipline. J-AD-1's law should be named as binding on REG-3's family resolution too — recommend the chair extend it explicitly rather than let a name-match creep in there. |
| **J-AD-2** — AD mints ZERO feature flags; dark by reachability; anything needing a switch inherits DW-7a's | **RATIFIED** | The one-flag rule and the six-surface bill still stand; the register program's own light-up mechanism (sandbox-first, owner-eyes cutover, REG §5) is flag-free and thus compatible. The reversal is correctly marked owner-class ("a new gate and therefore an owner-class act"). No landing since contradicts it. |
| **J-AD-3** — no `ROOF_FORMS` enum; a roof form is derived at draw time from parti × material × tier | **RATIFIED, with a scope clarification** | The core (derive-don't-store, DW law 4) re-derives cleanly. But the charter's supporting fact ("there is no ROOF_FORMS enum anywhere in src") is slot-stamped and WILL decay: L-REG-8/A3.1 mint roof forms as part of shape families ("footprint + compound arrangement + roof form", slots "rolled by the building's own seed"). That is *compatible* — A3's drawVariant idiom is exactly draw-time derivation, not a stored field — but the judgment's text should be read as forbidding a *persisted* roof-form store, not forbidding a drawing-side roof vocabulary. REG-3 will lawfully create the latter. The clarification costs J-AD-3 nothing; without it a future reader could bill REG-3 as a J-AD-3 violation, or read REG-3 as licensing a stored enum. |
| **J-AD-4** — `burned` draws the broken silhouette plus a char stroke; no new state, no new enum member | **RATIFIED** | Reads a condition the engine already carries; B14's signed mapping untouched; vetoable at sitting row A5. Register-era note: REG-D consumes "the wear grid" as a LANDED mechanism (A6.2), so the char stroke's zero-data posture matters more now, not less — it must stay a draw-time read wherever the wear grid is consumed. |
| **J-AD-5** — the register is HAND-DRAWN CARTOGRAPHIC; painterly refused on mechanism, not taste | **RATIFIED, with two register-era annotations** | See §V1.1 below — this is the judgment the register program consumes as binding law and it gets its own subsection. |
| **J-AD-6** — B13's three density bands are three cut points on ONE ordered motif list per trade | **RATIFIED, with a one-writer seam note** | The simplification re-derives: `BUSY` = `WORKING` + the layer the wear ladder sheds first; density and decay share one mechanism. **Register-era annotation:** A2.2 has the chair minting "B13's per-rung fixture-count band … before REG-4 opens", and REG-4's exit includes "furniture density inside B13 band" (market furniture). B13 density constants are now consumed by TWO programs (REG-4's markets, AD-4b's yards) and sit on the tuning surface (A5.4 lists "B13 densities"). One writer for those constants; AD-4b's dispatch brief must name REG-4's minted band as its input, not re-mint it. |
| **J-AD-7** — nothing AD authors goes in `public/` except a generated exhibit plate with an `ASSET_MANIFEST.json` row naming its generator | **RATIFIED, and strengthened by events** | The AD-1 audit proved this law's failure mode in production: NC-licensed charges commercially served since April (§530.6). **Two annotations:** (i) the manifest the judgment names does not exist — `public/ASSET_MANIFEST.json` and `tests/lint/assetManifestWalker.test.js` are both ABSENT at tip `e4ed27f48` (checked this lane); the landed machinery is `tests/lint/shippedAssetLicence.test.js` + `scripts/ai-media-provenance.json` + notices §§1.7/6/7 — a different (licence-keyed, prose-section) shape. The judgment's obligation therefore currently binds against machinery that is only partially built; §V2-R7 carries the delta. (ii) REG-11 will regenerate the landing assets ("the landing assets regenerate from the new pipeline") — that swap is exactly J-AD-7's territory and must land with its provenance rows. |

### §V1.1 · J-AD-5, the one the register leans on — verified against its own text

**What the register program consumes.** A2.1 blocker 5 (and §586.1/§596.1 of the ledger): *"Q2 is constrained by the AD charter's J-AD-5 ONE-GEOMETRY law (named as binding): baked raster qualifies only if one geometry feeds every surface with PDF and thumbnail identical, else a priced PDF-side geometry expansion."* **Correction of record for the dispatch brief: the citation lives in AMENDMENT A2 (the first panel's rulings), not A6** — A6 does not mention Q2; the ledger row confirming the seam verification is §586.1/§596.1, and §620.2 says "the A5 panel verified J-AD-5 against its text".

**Does the charter's own text support that reading? YES — from three mutually reinforcing places:**

1. J-AD-5's own refutation clause (§8.1): *"Refusing painterly is a mechanism claim — refute it by showing a raster path that keeps the PDF and thumbnail identical, and the refusal falls."*
2. The ONE-GEOMETRY TEST (§1.3.2): assets *"must reach the pane, the file exports, the PDF and the thumbnail through `buildTownMapDrawList` (2D) or the manifest (3D) — never through a parallel path only one surface knows about"*; *"a raster overlay that only the browser can composite breaks the PDF and the print page silently."*
3. The §2.1 painterly row: refused because it *"cannot survive the one-geometry test (§1.3.2) — the PDF plate and the token raster would need a second path."*

The A2 reading — one geometry feeds every surface, with PDF-and-thumbnail identity as the qualifying condition for a baked raster, else a priced expansion — is a faithful composition of (1) and (2). The refutation clauses are stated correctly in the charter, and they are stated in the falsifiable direction (a demonstrated raster path fells the refusal; nothing vaguer).

**Annotation 1 — the anchor must be re-pointed at cutover.** The charter's law is anchored by name to `buildTownMapDrawList` as "the ONE geometry source". The register program builds the fabric render stack (L0–L5) and at the owner cutover the legacy town-map paths stop being the ones a user sees ("the legacy octagon wall and v1/v2 paths stay byte-frozen until it", REG §5). The LAW transfers — one geometry source feeding pane, exports, PDF, thumbnail — but its named anchor becomes the fabric stack's draw list at that moment. The dispatch window (and REG-7's print-path car) should restate J-AD-5 with the anchor generalized: *one geometry source per surface family, whatever module embodies it at the tip*.

**Annotation 2 — the refutation clause cuts both ways, and REG-7 may trip it deliberately.** If REG-7's print answer is the baked-raster branch and it demonstrates exactly what the clause demands (one geometry → raster, PDF and thumbnail identical), then by the charter's own terms the *mechanism* half of painterly's refusal falls. That is not a defect — it is the clause working — but the charter's phrase "refused on mechanism, not taste" should then be read as *refused on mechanism as of the charter's slot*. The practical refusal survives on register grounds regardless: the register question is now settled ABOVE the charter by the owner's north star (corpus dress over Watabou/FTG structure), and the corpus register is ink-first with wash — inside the charter's own "hand-drawn cartographic … restrained wash" definition, not painterly ("line vestigial"). Also note the register program actively *enforces* J-AD-5's two consequences: L-REG-9 is "line before colour"'s sibling (a stroke may carry only meaning the geometry earns), and REG-3's exit pins "one NW light everywhere (zero divergent shadow ops)".

### §V1.2 · The five-wave structure and the standing refusals

| Structure | Verdict | Re-derivation |
|---|---|---|
| The five waves + 17 cars + dependency graph (§5.1–§5.3) | **RATIFIED as dependency logic; the schedule layer is SUPERSEDED by execution and by the reprioritization** | The graph's edges all re-derive (AD-4b needs a landed parcel surface; AD-5a..d need the DW enums and panes; AD-5e last-before-regen; AD-7 after DW-7a). What moved: **Wave A is substantially discharged out-of-band** (§V2-R7); **AD-4b's MP-1 gate is now satisfied** (§530.5 — the painter emits a `parcel` op; confirmed present at tip `e4ed27f48` in `cartographyPaint.js`, though the layer is dark: `townCartographyEnabled` has no default entry); **waves C and E recede** because A2.3 places DW-2b (and with it the DW arc's remainder) after the REG arc; wave D is untouched and still owner-gated on AD-6a's number. |
| The §1.5 "last responsible moment" derivation (audit before G1, else a post-regen shift) | **RATIFIED as a conditional; its operative urgency is DISCHARGED by AD-1's own result** | The reasoning was sound: IF a finding touches drawn output THEN replacing it is a same-seed shift the tail cannot absorb. The executed audit resolved the antecedent FALSE: §530.6 — *"no remedy forces a second regeneration — nothing is inside the deterministic derivation and there is no PDF byte golden."* The binding clock is now counsel turnaround (§530.6's own finding), not G1. The dispatch brief should not re-inherit the G1 deadline as the provenance driver. |
| The storey refusal (§2.5.1: AD draws no storey until DW-2b mints `Storey[]` and the chair rules Q-B) | **RATIFIED, and it binds longer now** | DW-2b runs after the REG arc (A2.3), so the refusal's window widened. Nothing in REG draws storey lines (A3's family grammar buys "the tower and transept" — forms, not floor counts), so no program pressure against it. |
| The refusal to answer the tuning signature (§6 last row) | **RATIFIED** | Unchanged; the register program says the same (§9, A5.4: tuning signature owner-by-nature, accumulating for the soak's end). |
| The anti-scope table (§6) | **RATIFIED row by row** | Spot-checked against register law: "no op added to `cartographyPaint`" — intact (MP-1 paid the parcel identity; §530.5 measured the real cost at 14 pins against the MP-1 charter's 13 — a figure-class correction to a sibling charter that only confirms the AD charter's "every test pinning it moves" warning). "No paid surface / no lens / no SKU" — agrees with REG §10 ("No new entitlement gates; the §523 trichotomy stands"). "Gate viewing" — the charter's trichotomy citation (§524.4) already matches A2.4's corrected home. "AI never authors geometry" — agrees with REG §10's "No runtime AI imagery, ever." |
| The five asset tests (§1.3) | **RATIFIED; test 5 sharpened by events** | Tests 1–4 re-derive unchanged. Test 5 ("no row, no ship") is now backed by a landed walker of a different shape (`shippedAssetLicence.test.js`) — and by the §530.6 lesson that the *served* obligation matters too: the production notices page 404s until a deploy, so "a row in the tree" is not yet "a notice served". The dispatch brief should carry the served-state check beside the tree-state check. |

---

## §V2 · THE REGISTER RECONCILIATION — what the charter could not have seen

The charter predates the entire register program (body §§570–581 + A1–A6, all 2026-08-24 evening). Every collision, overlap, and superseded assumption found, with both texts quoted.

### R1 ⛔ THE THREE-PAINTER PROBLEM — the charter's one-geometry ground vs the fabric cutover (the largest finding)

**The charter:** §0.1 — *"`buildTownMapDrawList` … is the ONE geometry source, and it feeds every 2D surface"*, called *"the single most consequential architectural fact in this charter"*. §0.2 — `cartographyPaint.js` is *"a different, parallel painter from the live `townMapDraw` one"*, the DW-6 seam AD lands at (§514.4). AD-4a generalizes `townGlyphs`' registry; AD-4b modifies "the yard draw path MP-1 landed" (the cartography stack).

**The register:** §5 sequencing — *"the map-program cutover (owner eyes) remains the single point where any of this reaches shipped `src/` — the legacy octagon wall and v1/v2 paths stay byte-frozen until it."* §8 — *"**AD (17 cars)**: the art-direction slot at the DW-6/`cartographyPaint` seam is unchanged; REG supplies the abstract tier AD elevates."*

**The finding:** the estate now has THREE town-scale geometry producers — `townMapDraw` (live), the `townCartography` stage (dark behind `townCartographyEnabled`, the DW-6 seam, MP-1 landed here), and the fabric sandbox (sealed, the register's object). REG §8's AD row asserts the seam "unchanged" but does not say which painter survives the cutover as AD's canvas: the charter's wave B is written against the first two, and the cutover demotes "v1/v2 paths" by name. This is not a law contradiction — it is a superseded machinery assumption the seam row papered over. **The dispatch brief must rule, per car: does wave B (a) land pre-cutover against the legacy stacks and get carried by REG-P's re-port, or (b) re-point at the fabric stack's shape-code/glyph layer (which is REG Q3's territory — see R9)?** Until that ruling, AD-4a/4b are compiled against a canvas with a scheduled demolition date.

### R2 · The abstract-tier premise is inverted; A0's core verdict survives it

**The charter:** §0.5 defines the abstract map AD elevates; A0's honest cost — *"It will never be a painted map. A working cartographer … would see a competent, legible, consistent hand — not a beautiful one."*

**The register:** §1 — *"The first thing is a map that is beautiful, intuitive, and relatable"*; the north star — corpus aesthetics as the UPGRADE, *"read just as good or better than Watabou and FTG"*; §7.5 — "indistinguishable at arm's length" as the judging loop's convergence target.

**The finding:** the owner's §570.2 order supersedes the charter's *positioning* (the ceiling claim "never a painted map" is no longer the estate's accepted ceiling — the register program exists to disprove it procedurally), while **ratifying the charter's *mechanism*** — the register program builds beauty exactly the way A0 recommends: procedural, estate-authored, deterministic, nothing bought (L-REG-12, REG §10). A0's verdict stands; its consolation prose is superseded. Consequences: **Q-AD-5 / D-AD-4** (illustrated as default) are **superseded-in-substance** — the default look now changes via the fabric cutover, not via `DEFAULT_STYLE_ID`, and the `parchment === legacy` golden-pin rationale holds only until that cutover. **A2 (paid cartographer's critique)** survives as an open taste row but overlaps A1.4's owner-docket "external eyes" option and the register's owner-judged corpus loops — the sitting sheet should present them together, not as independent asks.

### R3 · The DW-6/cartographyPaint seam vs the register's L4 paint layer

**The charter:** §0.2 — cartographyPaint emits *"colour-free"* role-and-tone ops, *"no glyph path here at all"*; §6 — AD adds no op to it. The charter assumes AD is the program that will give the DW-6 surface its illustrated dress.

**The register:** §3 L4 — *"PAINT — the recovered MF-A1 pass (`refs/preserve/mf-a1-recovered-2026-08-24`): paper grain, stains, vignette, tidemark rims, wash mis-registration, mass shadows, three-tone water — applied last over L2/L3"*; REG-7 charters its pipeline integration. (Ref existence CONFIRMED this lane: `5c524197a`.)

**The finding:** these are different layers on different stacks — cartographyPaint is geometry ops; L4 is a post-geometry dress pass over the fabric's SVG — so there is no direct collision. But the *division of dress labor* the charter assumed is redrawn: at town scale, ink and paint dress belong to the register program (L2/L4); AD's surviving exclusive dress territory is **the interior plan plane (DW-6a–e), the yard/fixture motif families, and the optional 3D branch**. The shared laws bind both programs identically (one NW light — charter §2.1 and REG-3's exit; line-before-colour — charter §2.1 and L-REG-9; one geometry — J-AD-5 and A2.1.5). Recommend the marker discharge state this division explicitly so no AD car re-derives town-plate dress the register already owns.

### R4 · The print-path constraint — verified (see §V1.1)

The charter's text supports A2.1.5's reading of J-AD-5; the citation is A2, not A6; the anchor needs re-pointing at cutover; the refutation clause is live in both directions. One inherited seam the register program did NOT reconcile: **D-EXPORT-1** (charter §2.4/D-AD-8 — `hasDrawableMap` self-gates the PDF plate; a paid dossier can ship without a map). REG-7's parity matrix and REG-11's export surfaces sit directly on that seam and neither names it. Recommend it be added to REG-7/REG-11's acceptance the same way the charter bound AD's DW-6-dependent cars ("must not widen it").

### R5 · The §613 film-last order and the L-REG-26 carry-note — an obligation the charter predates

**The register:** L-REG-26 — *"every wave from A5 forward stamps its elements' timeline annotations (appearance epoch, accretion order, beat events) in its changeManifest"*; A5.2 — *"the L-REG-26 timeline carry-note is a standing changeManifest obligation from this amendment forward"*; A6.1 scopes the totality walker by op-class roster with *"timeless dress (paint, chrome, lettering, legend) exempt by name"*.

**The charter:** no AD car carries any timeline arm — it could not have; all 17 acceptance sets predate §607–§613.

**The finding:** whether AD's cars are bound is UNRULED — the law's letter covers the REG program's waves, but AD cars will land after the law exists and will add drawn elements. The likely dispositions differ by family: building/fixture *motifs* are dress on elements that already carry timeline data (exempt-by-name candidates for the A6.1 roster); **yard fixture elements are the hard case** — a tenter strip or kiln is a dated consequence of a trade's founding, and if the chronicle film should show a tannery's yard filling in, those elements owe appearance data. **The dispatch brief must add a per-car timeline-disposition row (owing vs exempt-by-name) and get it ruled** — the cheap moment is before authoring, exactly per L-REG-26's own rationale ("the final assembly consumes emitted data and re-opens nothing").

### R6 · Two wear systems and two density consumers — lawful, but the seams need names

- **Wear:** B14's owner-signed 3×3 grid (charter §2.3) governs buildings/fixtures; L-REG-18 mints a separate wall-wear ladder (kept → weathered → crumbling), derived — and already BUILT in-wave at REG-2 (§621.1: `exposure × (1 − upkeep)`). Coexistence is lawful (different objects, different derivations), and REG-D consumes "the wear grid" as a landed mechanism (A6.2) — consuming, not forking. The seam to name: **B14 is owner-SIGNED; wall-wear thresholds are tuning-class** — no lane may "unify" them without the owner, and AD-4c's shed-order derivation must not silently absorb wall wear.
- **Density:** J-AD-6's seam note (§V1) — B13's band constants get ONE writer (the chair's pre-REG-4 mint) and two consumers (REG-4 markets, AD-4b yards).

### R7 · Wave A has been substantially executed out-of-band — the charter's plan vs what actually landed

Executed since the charter (all CONFIRMED at tip `e4ed27f48` or in the ledger): **TE-AD-1** ran (§530.6, `draft-AD1-PROVENANCE-AUDIT.md`, 1,000 lines) — charges 179× BY-NC-SA + 37× BY-SA self-documenting; heightmaps DOCUMENTED (Tangrams/Mapzen, attribution owed); fonts NOT clean (re-cut in June against three "unmodified" claims; Lora RFN under OFL cl. 3); production notices page 404. **TE-IP-1** remediation landed: charges 338 → 104 files and textures 23 → 2 at the tip (counted this lane); notices grew §1.7 (fork art), §6 (art/media inventory incl. the 7,413,151 B unknown cohort), §7 (AI media) — 727 lines vs the charter-measured 521. **TE-AIP-1/AIP-2** landed the AI-provenance floor (`scripts/ai-media-provenance.json`, `tests/build/aiMediaProvenance.test.js`, C2PA honesty language). **The walker landed as `tests/lint/shippedAssetLicence.test.js`** — licence-keyed, not the charter's manifest shape.

**The deltas still open from the charter's wave-A spec, for the dispatch brief:** (i) `public/ASSET_MANIFEST.json` + `assetManifestWalker` (every-file enumeration, SHA-256 pins, orphan-row control, unattributed-count down-ratchet) — NOT built; notices §6 is authored prose, not rendered from a manifest, so it can drift from the tree (the charter's §4.4 point). Whether the landed licence-walker shape suffices is a chair call — the charter's fuller shape still has unclaimed value (drift-proofing, the plate-generator rows J-AD-7 requires, REG-11's incoming asset swap). (ii) The AI-vendor ToS question (Google/Higgsfield/BytePlus commercial-use terms) — named open at §530.7 and again in notices §6 ("no lane has yet examined"). (iii) The unknown 7.4 MB painting cohort — waiting on the owner's own records. (iv) The 404'd notices page — undischarged LIVE until a deploy. **AD-3\*'s scope, which the charter left as `*`, is therefore now mostly known and mostly done; what remains is the manifest machinery, the ToS question, and the served-state proof.**

### R8 · Corpus references and AD's provenance laws — aligned, one hygiene row to carry

The register program's references (313-plate corpus, hf-plates, Watabou/FTG) are judging references, never shipped assets — consistent with the charter's five tests. The ledger already carries the register-side hygiene consequence (§595: hf34 bears a real-name credit, *"must never be copied into a product plate"*; drawn scale numerals never trusted). AD's test 3 (setting-agnostic/no named settings) and test 5 (notices) apply unchanged to anything the register dress work ever emits into the tree; no conflict found.

### R9 · The glyph-population trichotomy — REG Q3 decides the fate of AD's ground

**The register:** §4 — *"v2 glyph library (26 type-shapes, NW shadows) + `massing.js` (dormant)"* (A2.1.2 corrected massing.js to LIVE); Q3 — *"do the 26 oblique glyphs survive as the illustrated lens's building layer above the fused plan masses, or retire into the shape families?"*

**The charter:** §0.1 — `medieval.js`, *"27 glyph kinds"*, the ILLUSTRATED TOWN's ground.

**The finding:** a grep for "type-shapes" across `src` at the tip returns NOTHING — the "v2 glyph library" row cites the fabric sandbox's own tree, not `composite-r4`. So the 26 and the 27 are **probably two different glyph populations** (the sandbox's v2 library vs `townGlyphs/medieval.js`) — but neither document says so, and Q3's ruling will decide whether AD-4a's registry-generalization plan has a live substrate or a retiring one. **Recommendation: AD wave B does not dispatch before Q3 rules** (or its brief carries both branches priced); and the dispatch window's figure re-measure should establish the real population count(s) — see §V3, which also records why this lane's own quick count is not trustworthy enough to adjudicate.

### R10 · Accessibility — the charter's rule now has an instrument arriving

Charter §2.6 ("line before colour"; "never a fill alone"; MP-1's border-plus-tint precedent) and L-REG-28 (the accessible lens joins every wave's instrument run) agree in law. A6.2 charters the accessible-lens arm with bindingness dated from the arm's landing. **AD cars landing after that arm owe the lens run in their instrument sets** — add to every AD car's acceptance at dispatch; the charter's blind-read gate (§4.2) is unaffected and complements it.

### R11 · What the register program consumes from the charter beyond J-AD-5 — nothing, and that is a gap in one direction

The register program names the AD charter exactly once (§8's seam row) and consumes exactly one judgment (J-AD-5, via A2.1.5). It does NOT consume: the one-light law's charter statement (§2.1 — REG-3 re-derives it independently, harmlessly), the five asset tests (§1.3 — which bind any asset the register work ships, e.g. REG-11's landing assets), or J-AD-7's public/-manifest law (same exposure). Harmless duplication except at REG-11, where the charter's laws genuinely bind incoming work and nothing in the register says so. **Recommend the chair stamp REG-11's brief with J-AD-7 + test 5 explicitly.**

### R12 · The sequencing reality for the marker discharge

§619.1 counts "AD's 17" among the fully-compiled surfaces (no just-in-time recompile owed on structure); §620.2 + §623.1 pull only the *validation* forward — this document. The arc order at HEAD: REG arc (REG-2 sealed §621; REG-3/REG-T dispatched §622) → DW → AD at the DW-6 seam → G1 → regen → soak → tuning → push. The charter's "dispatched deliberately early" framing (§514.4/§523.6) aged into: wave A consumed early exactly as designed; the rest waits, correctly, behind programs the charter could not name.

---

## §V3 · THE SLOT-STAMPED FIGURE INVENTORY — the dispatch window's re-derivation checklist

Per §623.2, none of these were re-measured. Grouping: **[S]** slot-stamped code/tree fact (decays with landings — re-derive at dispatch), **[D]** document-sourced (stable unless the source document is amended), **[X]** externally sourced (re-fetch if load-bearing). ⚠ marks rows this lane's *structural* checks incidentally proved ALREADY MOVED — proof of decay, not replacement values.

**Identity/reachability pins [S]** — all file:line cites in §0.1/§0.5 (`townMapStyles.js:74/:78/:97/:311-331`, `SettlementMapPane.jsx:763/:474/:481-590/:617-619`, `SettlementMapEditControls.jsx:79`, `entitlementLadder.js:37` `LENS_COUNT=5`, `glyphAssign.js:39-52/:90-109/:120-151` incl. the **18** regex count and **12**-category default, `mapDress.js:132`, `interiorDraw.js:80-81`, `cartographyPaint.js:15-17/:19-21/:52-58`, `cartographyBuildings.js:74`, `cartographyTuning.js:284-290` incl. `DWELLING_HEIGHT_PERMILLE {100,120,140,180,220,260}` / `HEIGHT_PLAN_CEILING 60`, `pricing.js:297-304`). ⚠ the `cartographyPaint` cluster is PROVEN moved: the identity now includes `parcels.length` and the header rewrote the "placement SLOT" clause (MP-1, §530.5 — 14 pins, not 13).

**Byte/file counts [S]** — `medieval.js` 15,837 B / **27 glyph kinds** / 269 effective lines (~10/motif); `glyphCompiler.js` 8,547 B; `groundDress.js` 33,423 B; `townMapStyleWall.js` 13,530 B; `townMapDraw.js` 27,761 B; arch/ **37 files / 331,627 B / 19 test files / zero importers**; `interiorTemplates.js` 28 cell kinds / 22 fixtures / 8 interior kinds. ⚠ the glyph-kind count needs a REAL count method at dispatch: this lane's naive top-level-entry regex returned 24 (helper-composed entries evade it — the ugrep/false-negative class), the charter says 27, REG Q3 says 26 of a possibly different library. Do not let three documents carry three numbers into the Q3 ruling.

**The public/ payload table [S] ⚠ WHOLESALE MOVED** — 176,499,727 B total; landing-maps 68,558,483/80; media 44,601,456/13; map 24,738,225/636; realm-journey 20,715,193; backgrounds 15,427,767/55; fonts 1,401,748/17; textures 11,646,263/23; charges 3,094,160/338; heightmaps 1,224,885/24. PROVEN moved: charges now 104 files, textures now 2 (TE-IP-1's removals, counted at tip this lane). Every row re-derives at dispatch; notices grew 521 → 727 lines with three new sections.

**Build/test budgets [S]** — first-paint closure 1,040,000 B (one measurement 1,039,975); gzip 337,000 / brotli 283,000; index.html 8,600; CSS 19,800; fonts 90,000 (42,124 + 46,036); per-file caps 800/600; census `files 2517 / parked 366 / credited 2151 / titles 20882 / suiteTitles 5814`; ratchet 28,924 / ceiling 11; K-1 sheet 108/396/2,172 triangles, 42 draw calls, +0 eager bytes. ⚠ census/ratchet figures are 67 commits stale by construction (the lighting census alone re-stamped 21,033/5,849 → 21,049/5,852 in CH-4); every AD car walks its own delta anyway (charter's own rule, §5.2 preamble — which is the correct posture and is re-ratified here).

**Vocabulary counts [D]** — partis 48; cell kinds 83; fixture kinds **82** (the §Σ AR-7 amended figure — the charter correctly used the post-amendment number); circulation 9/~55; yard vocabulary ~39. Stable while the ratified DW charter stands; re-confirm only that no DW amendment has landed since.

**Dossier/PDF facts [S]** — plate ships in 2 of 4 dossier variants (§524.3); `$2.99` / `requiresAccount: false`. Re-verify at dispatch; the trichotomy ruling itself is ledger law [D].

**External [X]** — the Unity EULA FAQ reading (single primary source, flagged as such in §8.3 — and now materially superseded as the operative risk by AD-1's *executed* findings: the NC charges were a live breach, not a licence-shape hypothesis); the CC0-pack claim (search digest only); commissioned-art cost/lead-time (lane estimates, never load-bearing). §8.3's own honesty about all three is RATIFIED — every row correctly predicted what would confirm or refute it, and AD-1 then did.

---

## §V4 · THE RECOMMENDATION ON THE MARKER (judgment-labeled; the chair rules)

**JUDGMENT (vetoable by a word): discharge `[OPUS-RUN · FABLE-VALIDATION OWED]` to a dated `[FABLE-VALIDATED-AT-THE-JUDGMENT-LAYER 2026-08-25 · FIGURES PROVISIONAL-ON-DISPATCH]`, landed as a new preserve-ref version with `refs/preserve/ad-charter-2026-08-24` kept as provenance (the §623.2 r2 precedent), carrying this verdict as the charter's register-era amendment layer — the preserved original untouched.**

Grounds: all seven J-AD judgments RATIFIED on re-derivation (J-AD-3 with a scope clarification; J-AD-5 with the re-anchoring and both-ways-refutation annotations of §V1.1; J-AD-6/-7 with one-writer/machinery annotations); the five-wave dependency logic RATIFIED with its schedule layer superseded by execution (wave A) and reprioritization (waves C/E); no judgment REVERSED — the one materially discharged derivation (§1.5's G1 deadline) was a conditional whose antecedent the executed audit resolved false, which is the system working, not the charter failing. The figures section decays exactly as §623.2 predicted and is inventoried, not repaired, so the dispatch window re-measures once, at the moment it binds.

**What the discharge must carry forward (the dispatch brief's obligations, consolidated):** the R1 painter ruling per wave-B car · the R5 per-car timeline-disposition row · the R6 one-writer seams (B13 band, B14-vs-wall-wear) · the R7 wave-A deltas (manifest machinery decision, AI-vendor ToS, the 7.4 MB cohort, the served-404 proof) · the R9 Q3-before-wave-B gate and the glyph-count settlement · the R10 accessible-lens arm per car · the R11 stamp on REG-11 (J-AD-7 + test 5) and the R4 D-EXPORT-1 line on REG-7/REG-11 · the §V3 figure re-measure.

**PLAUSIBLE (stated as such):** that the sandbox v2 glyph library and `townGlyphs/medieval.js` are distinct populations (R9) — settled by one look inside the fabric worktree, which this read-only lane did not enter; and the R5 disposition guesses (motifs exempt, yard elements owing) — the chair's call, flagged, not assumed.
