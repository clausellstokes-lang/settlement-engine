# Lane MF-W0 — THE VERIFICATION DEBT (§5 W0, ODQ §262.3 / §264): receipt

**Lane MF-W0 (Opus 5), 2026-08-17. THE FIRST BUILD LANE OF THE NEW ORDER.**
**Touched:** `mf-proto/build-out/**` (4 source files, 2 test files, 1 harness file) and the
scratchpad under `MFW0-*` / `laneMFW0-*`. ⛔ **NO git tree write, NO repo gate, NO branch move,
NO memory write, NO state-mutating git command.** The only git commands used were the read-only
`git show refs/heads/review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md`.

⚠ **THE BUILD SHEET MOVED UNDER ME MID-LANE AND I RE-READ IT.** `GENERATION-SPEC.md` grew from
3,187 to 4,364 lines at 06:47 (the §264 fold), adding **G-40's three instruments** and **the
derived-vs-drawn grain census** to W0 and expanding exit criteria 3, 6, 7, 8, 9. Everything
below is measured against the **current** sheet, not the one I opened with.

---

## §0 · THE VERDICT, STATED FIRST

> ⭐⭐⭐ **§205 A's STREET ARM FALLS FROM 127 VIOLATIONS TO 13, AND THE UNEXEMPTABLE SHARE FROM
> 72% TO 0%.** `waterViolations` **165 → 61** over 16 leaves (**63 → 25** over 10 distinct
> sites). `bridges` **15 → 28**: the towns gained **13 crossing works they always needed**.
> Suite **173/173** green, determinism **10/10**, all four drawn censuses **0**, **96/96** under
> ceiling, and every moved pixel is attributed to one of two declared shifts.

> ⚠⚠ **AND THE HONEST SPLIT OF THAT FALL, BECAUSE THE CHAIR'S OWN WARNING DEMANDS IT.** Of the
> 127 street convictions: **13 remain convicted**, **21 channels are now decked crossings (13 of
> them newly built)**, and **101 are reclassified as named `bankside adjacency`**. So **11% of
> the fall is crossings GAINED and 89% is the named exemption of §262.2(e)**. That exemption is
> a topological fact, not a tolerance, and it is pinned non-vacuous four ways (§3.4) — but its
> SIZE is a chair-visible number and I am putting it in the first paragraph rather than the
> appendix. **J-W0-4.**

> ⭐⭐ **THE MILL CURE IS RE-PROVED PIXEL-FREE AGAINST THE TREE THAT LANDS.** Isolated at stage B:
> **32 of 32 plates BYTE-IDENTICAL** to MF-ARCH-2, and the four named run-1 parchment SHAs
> reproduce MF-W1's exactly — `town 85fe7726…`, `city ae2eb05f…`, `highwater ed8a78f0…`,
> `fjord 8c083827…`. Violations **165 → 159**, exempt **28 → 34**, street arm untouched.

> ⛔ **ONE FIGURE MOVED THE WRONG WAY AND EXIT 9 IS WHY YOU ARE READING ABOUT IT.** G-33's grain
> re-pin costs **+16 §205 A BODY violations** (32 → 48), all of them on the two coastal leaves,
> and buys **−524 `zoneUnwashedBodies`** (2,137 → 1,613) and **+440 / +1,339 op-ceiling headroom**
> at city / metropolis. Attributed by a dedicated stage-C measurement (§2).

---

## §1 · THE FOUR-STAGE DECLARED SHIFT — every moved byte has a named cause (§110.3)

**The three-stage method MF-ARCH-2 used, run with FOUR stages because W0 carries TWO independent
declared shifts and they must not be attributed to one another.** Each stage is a separate tree,
each measured with the same battery after the final edit of that stage.

| stage | what landed | 32-plate SHAs vs MF-ARCH-2 | `waterCrossings` | `waterExempt` | `waterViolations` | `bridges` | drawn bodies |
|---|---|---|---|---|---|---|---|
| **base** | MF-ARCH-2's tip | — | 193 | 28 | **165** | 15 | 23,391 |
| **A · behaviour-neutral** | `fabric.waterCensus` published whole · the distinct-site harness line · `UNVALIDATED-BY-INSTRUMENT` labels | ⭐ **32 of 32 BYTE-IDENTICAL** | 193 | 28 | 165 | 15 | 23,391 |
| **B · census-only, pixel-free** | **(d)** the dead-`anchorKey` mill cure, ALONE | ⭐⭐ **32 of 32 BYTE-IDENTICAL** | 193 | **34** | **159** | 15 | 23,391 |
| **C · geometry (G-34)** | **(a)(b)(c)(e)** the one predicate, the passage plank, the loop, the coast exemption | **18 of 32** — the **14** that moved are the 7 **river** leaves × 2 lenses | 193 | **148** | **45** | **28** | 23,391 |
| **D · geometry (G-33)** | the metropolis `GRAIN_BAND` re-pin + re-derived seams | **12 of 32** — the **6** further are city · metropolis · migration × 2 lenses | **215** | **154** | **61** | 28 | **22,067** |

⭐⭐ **THE ATTRIBUTION IS EXACT AND IT IS THE POINT OF THE FOUR STAGES.**
- **Stage A moved nothing.** A whole new published surface on the fabric, a new harness export
  and three metadata labels, and **not one byte of any plate changed** — which is what licenses
  every later comparison.
- **Stage C moved EXACTLY the river leaves.** town · highwater · siege · plague · famine ·
  year-018 · year-100, and no other. That is precisely right: G-34 builds **crossing works**, a
  crossing work exists only where a street **transits**, and only a river has a far bank. The
  coastal leaves gained **census exemptions and no pixels**; the six waterless leaves are inert.
- **Stage D moved EXACTLY the occupancy-tier city and metropolis leaves.** city · metropolis ·
  migration. The town family is untouched by the re-pin because its rung did not move.

⛔ **ZERO UNEXPLAINED RENDER CHANGES (exit 9).** 20 of 32 plates moved; 14 are G-34's, 6 are
G-33's, and the two sets are disjoint.

---

## §2 · G-33 · THE STALE FIGURES (C-1, C-2, C-3)

### §2.1 · C-2 — the metropolis rung, re-pinned, with the seams re-derived BY CONSTRUCTION

`GRAIN_BAND.metropolis` **100–130 → 80–120**. The old rung rested on **n=1** (hf34 on MF-S1's
own window); the corrected ATLAS T-01 rung rests on **n=9**. `GRAIN_SEAMS` is *computed* from
the band table, so it re-derived itself:

```
GRAIN_SEAMS  [8, 16, 28, 47.5, 70, 97.5, 130]  →  [8, 16, 28, 47.5, 70, 87.5, 120]
```

⚠ **AND THE CITY RUNG MOVED TOO, WHICH IS NOT OBVIOUS AND IS WORTH THE SENTENCE.** The
city→metropolis seam is the *mean of the two facing endpoints*, so re-pinning the metropolis
floor moved the seam **97.5 → 87.5** and with it every city-tier leaf's derived grain.

| leaf | derived grain, base → final | band (new) | in band |
|---|---|---|---|
| city · migration | **86.9 → 80.8** | 60–95 | ✔ |
| metropolis | **111.9 → 101.9** | 80–120 | ✔ |
| every other leaf | unchanged | — | ✔ |

**THE DECLARED CONSEQUENCE, MEASURED (stage C → stage D):**

| figure | stage C | stage D | direction |
|---|---|---|---|
| drawn bodies | 23,391 / 13,086 | **22,067 / 12,084** | −1,324 (city −322, metropolis −680, migration −322) |
| op ceiling headroom, city | 92 | **532** | ⭐ +440 |
| op ceiling headroom, metropolis | 83 | **1,422** | ⭐ +1,339 |
| `zoneUnwashedBodies` | 2,137 / 1,639 | **1,613 / 1,301** | ⭐ −524 |
| `zoneMajorityOutside` | 217 / 134 | 209 / 127 | −8 |
| `waterCrossings` | 193 / 71 | 215 / 82 | +22 |
| §205 A **body** arm | 32 / 12 | ⛔ **48 / 20** | **+16 (city +8, migration +8)** |
| `waterGates` | 22 / 10 | 20 / 9 | −2 |

⛔ **THE +16 IS A REAL COST AND I AM NOT SMOOTHING IT.** All 16 are cause `body in the claim` on
the two coastal leaves: the coarser grain gives larger institution footprints, and more of them
now reach the shore claim. It is inside the census's **known un-widened subject set** (92 bodies,
MF-ARCH's measurement) — a new instance of an old class, not a new class. ⭐ **Against it stands
the largest single improvement in the wave, `zoneUnwashedBodies` −524, and 1,339 primitives of
metropolis headroom that §252.3a's third circuit will want.**

### §2.2 · C-3 — a WITHDRAWN GRADING TARGET IS STILL A GENERATION INPUT

Per §257.3(b), the thorp and hamlet rungs are **`UNVALIDATED-BY-INSTRUMENT`, not withdrawn**.
They were withdrawn *from grading* because the instrument counts hedges as buildings at that
tier, not because the derivation is wrong. A new exported table states it so a grader cannot
miss it:

```js
export const GRAIN_VALIDATED = Object.freeze({
  thorp: false, hamlet: false, village: true, town: true, city: true, metropolis: true,
});
```

⭐ **THE GENERAL RULE, WRITTEN DOWN WHERE IT BINDS: a census that reads `GRAIN_BAND` without
reading `GRAIN_VALIDATED` is issuing a verdict the atlas withdrew.** Pinned (3 titles), and the
derived-vs-drawn census below honours it — the thorp and hamlet rows are **reported, not graded**.

### §2.3 · C-1 — paper warmth 37: THE FIGURE IS INSTALLED, THE PIXELS ARE **W7's**

**MEASURED FIRST, and the measurement changed what I did.** Running MFS1-measure.py's own method
(the lightest of eight median-cut clusters) over all 16 parchment plates:

```
paper L median 220   against band 226–244   ⛔ 0 of 16 in band
paper warmth median 34   against TARGET 37 (§249.4a, conflict C-1)
per leaf: warmth 33–36, L 215–225
```

⛔ **I DID NOT MOVE THE PALETTE, DELIBERATELY (J-W0-6).** W0's G-33 item is a **stale-figure**
fix; **"paper centroid within band at warmth 37" is W7's exit criterion 5**, not W0's. Moving ten
role colours to land a measured centroid is a tuning-signature change, which is an owner carve-out,
and it would spend W7's declared shift early for no measured structural gain. What W0 owes is that
the superseded 34 can never be re-derived: the ruled target and its citation are now in the code
beside the palette, and the distance to it is measured above rather than assumed.

⭐ **HANDED TO W7 WITH ITS NUMBER: the gap is +3 warmth and +6 L, and the lever is the PALEST
ROLE, not `FOLIO.paper`** — the instrument reads the lightest cluster, which on our page is a mix
dominated by `roads #F3EBD6` (warmth 29) rather than by `paper #E9DEC3` (warmth 38). A W7 lane
that warms `paper` alone will not move the measured figure.

---

## §3 · G-34 · THE FIVE ARMS, AS §262.2 RULED THEM

### §3.1 · The predicate, and the one thing this section is really about

`waterWorks.js` now exports **one** answer to **one** question, and `deriveBridges` **computes it
once and hands it to the census on `bridges2.meetings`**. There is no second computation to keep
in step — the census literally cannot ask a different question, because it is not asking.

⭐⭐⭐ **AND THE UNIT OF THE ANSWER IS A *RUN*, NOT A SEGMENT AND NOT A CHANNEL.** I sized all
three before writing any of it (`MFW0-sim{1,2,3}.mjs`) and only the run survived contact:

| formulation | what it costs | verdict |
|---|---|---|
| asked of a **SEGMENT** | **68 decks** corpus-wide against 15, **five of them under block seams** — a bridge under every field seam that grazes its own bank | ⛔ rejected |
| asked of a **CHANNEL** | a high street that crosses once then runs 200 units along the bank owes decks over the whole run; passed only because the de-dup radius happened to cover it | ⛔ rejected as fragile |
| asked of a **RUN** — one continuous stretch inside the claim | **28 decks**; and **a street meeting a meander twice has two runs and therefore two bridges** | ⭐ adopted |

**§262.2(c) — "fix the loop" — therefore falls out of the formulation rather than being patched
on.** The four verdicts a run can carry are `transit` · `bankside` · `in-channel` · `open-water`.

### §3.2 · Arm by arm, with the measurement each one owes (exit 3)

| arm | ruled | landed | measurement |
|---|---|---|---|
| **(a)** predicate mismatch, 72 | one predicate, one home, + a check that reds on a second spelling | `waterMeetings` exported from `waterWorks.js`; **exactly ONE caller in the whole fabric layer**; the census consumes the deriver's own object | **72 → 0.** 4 pins incl. 2 ⛔ counterfactuals |
| **(b)** `rank === 'passage'` refused, 20 | build the bridge at passage scale | the rank refusal is **deleted**; `deckKindFor` gives `plank` / `footbridge` / `bridge` | **20 → 0.** ⚠ all 20 prove **bankside** on this corpus; a **planted** transiting passage earns a `plank` |
| **(c)** first-crossing-only, 35 | fix the loop | the `break` is gone; **every** transit run earns a candidate deck | **35 → 0.** bridges **15 → 28**; **zero** channels with an untaken crossing |
| **(d)** dead `anchorKey`, 6 | land the mill cure | both strings read | **165 → 159**, exempt +6, **32/32 plates byte-identical** (stage B) |
| **(e)** coast-vs-river | name the exemption in the census | `bankside adjacency` is a **published `byExemption` key**; `in-channel` and `open-water` are **published violation causes** | 101 channels exempted **by name**; 13 still convicted |

⭐ **THE HEADLINE MEASUREMENT (exit 3): the unexemptable share falls from 72% (92 of 127) to
0% (0 of 13).** "Unexemptable by construction" meant *the census convicts a crossing the bridge
deriver is structurally unable to see or deck*. After the cure that count is **zero by
construction**, because both consumers read one classification. The 13 survivors are not
unexemptable — they are **convicted, with a published cause, because there is nothing lawful to
build**:

| cause | n (16 leaves / 10 sites) | what it is |
|---|---|---|
| `in-channel` | 7 / 2 | a block seam lying **wholly inside the river channel**, dry ground on neither bank. The defect is the LINE, not a missing bridge |
| `open-water` | 6 / 3 | a road out past the strand **into the sea** on `city` / `migration` — visible on the plate as the high street and the wall striding across the bay. **There is no far shore to bridge to** |

### §3.3 · CROSSINGS GAINED vs FORGIVEN, stated plainly

The street arm's 135 crossings, before and after:

```
BASE   135 = 127 violations  +   8 exempt (bridge/water-gate deck)
FINAL  135 =  13 violations  +  21 decked crossing  + 101 bankside adjacency
```

⚠⚠ **13 OF THE 114-VIOLATION FALL IS NEW WORKS (11%); 101 IS THE NAMED EXEMPTION (89%).** The
chair's warning — *"a fall produced by a widened exemption is a failed wave wearing a passing
number"* — is the right test to apply, so here is the defence and the evidence for it, and the
chair should overrule me if it does not hold:

1. **It is a topological fact, not a threshold.** A run is `bankside` iff the channel's dry,
   out-of-claim ground lies on **one** side of the water. Nothing is tuned; there is no tolerance
   to widen later.
2. **A coast HAS ONE BANK, and that is the whole of the coastal half.** `waterClaims` already
   says a coast's claim is *"the strand between the ink of the shoreline and the dry ground,
   where a hut on the beach would stand"*. MEASURED: `street.road.approach.corridor.2~frame.2`
   scored **775 "crossings" on `city` and 555 on `fjord`** with **`maxOut = 0.0`** — *every point
   of it lies inside the strand*. It does not cross the shore; **it IS the shore**. Convicting it
   was the instrument reading a shore road as a river crossing.
3. **It is non-vacuous, pinned four ways.** A planted transiting passage is `transit` and earns a
   plank; the same passage laid along its bank is `bankside` and earns nothing; a line lying in a
   river is `in-channel` and stays convicted; a road past the strand is `open-water` and stays
   convicted. **6 real coastal convictions survive on the very leaves the exemption is largest.**
4. **The river half is small and checkable**: 41 of the 101 are riverside — 6 per town-family
   leaf and 5 on highwater — and every one is a seam or block-cross touching the bank inside the
   drawn half-width with all its dry ground on one side.

### §3.4 · The check that reds when a second module re-spells the question

⚠⚠ **THE FORM OF THIS CHECK IS ITSELF A FINDING, AND THE OBVIOUS GUARD WAS MEASURED USELESS.**
I wrote the token scan first — *forbid a second spelling of the intersection kernel, or of "read
the water centreline"* — and measured it over the fabric layer:

```
INTERSECTION-KERNEL present in 5 modules  (reservedGround · fabricGeometry · accessLaw · groundLaw · waterWorks)
WATER-LINE read in 13 modules             (waterMode 31 sites, terraform 9, routes 6, seating 6, buildFabric 5, …)
```

**A scan over read sites convicts all of them or none.** ⭐ **THE CURE IS §241.5a's, APPLIED A
SECOND TIME: a scan over read sites must solve aliasing; a guard at the publication point does
not have to.** The guard therefore asserts, with **8 new pins** in
`tests/lint/derivationGraph.walker.test.js` (no new test file):

1. **`waterMeetings` has exactly ONE caller in the entire fabric layer**, and `deckReach` exactly
   one home — so a module that decides to ask the question itself reds immediately.
2. **The assembly hands the census the deriver's own object** — asserted on the AST: the
   `meetings:` property of the `censusLeaf(...)` call must come from the same identifier as
   `bridges:`.
3. ⛔ **COUNTERFACTUAL: `deriveBridges` no longer calls `crossPoint`, and `deriveWaterGates`
   still does.** ⭐ *A wall is a closed ring the water CUTS and its gate stands at the cut — a
   true centreline intersection and nothing else. Giving the wall the street's predicate would
   mint a water gate wherever the curtain merely grazed the bank. The two questions are kept
   apart deliberately and the walker names each one's lawful home.*
4. ⛔⛔ **COUNTERFACTUAL, AND EXIT 3(a) NAMES IT: PLANT A SECOND SPELLING AND THE SCAN
   CONVICTS.** The scan is factored out and run twice — once over the real fabric source
   (`['waterWorks.js']` ✔) and once over source in which `leafCensus.js` re-spells the question
   for itself (`['leafCensus.js', 'waterWorks.js']` ⛔). ⭐ *A guard that has never been shown
   convicting is a guard nobody has tested.*
5. ⛔ **COUNTERFACTUAL: the two spellings really ARE different questions.** The pre-cure
   `crossPoint` is reconstructed in the test and shown to see **nothing** where `waterMeetings`
   sees a crossing. Without this arm the guard would be proving nothing.
6–8. The three behavioural arms of §3.2 (plank / meander / bankside-and-coast).

---

## §4 · THE DISTINCT-SITE TOTAL — one line in the harness (exit 4)

`harness/exemplars.mjs` now exports `siteKey`, `siteRepresentatives` and `bothTotals`. **The key
is `seed | terrain | demoted`, and it is chosen because it reproduces MF-W1's published
distinct-site figures to the unit** (114 → 55 at MF-ARCH's tip; 165 → 63 at MF-ARCH-2's).

```
DISTINCT SITES (10): thorp · hamlet · village · town · city · metropolis · polycentric · highwater · mountain · fjord
```

⭐ **NO FIGURE ANYWHERE IN THIS RECEIPT IS QUOTED CORPUS-WIDE WITHOUT IT.** The authoritative set
at the landing tip:

| §205 A / §203 | 16 leaves | 10 distinct sites |
|---|---|---|
| `waterCrossings` | **215** | **82** |
| `waterExempt` | **154** | **57** |
| `waterViolations` | **61** | **25** |
| · street arm | **13** | **5** |
| · body arm | **48** | **20** |
| `bridges` | **28** | **8** |
| `waterGates` | **20** | **9** |
| `riverClaimWidth` | 16.3 (town; per-leaf — no corpus total is meaningful) | |
| `zoneOutside` | 227 | 136 |
| `zoneMajorityOutside` | 209 | 127 |
| `zoneOffBand` | 65 | 34 |
| `zoneUnwashedBodies` | **1,613** | **1,301** |
| `landlocked` · `orphanStreets` | 0 · 0 | 0 · 0 |
| `physicalViolations` | 1 (polycentric, unchanged five waves) | 1 |
| drawn bodies | **22,067** | **12,084** |

**EXEMPTIONS BY NAME:** `bank-rooted work` 26 · `bankside adjacency` 107 · `decked crossing` 21.
**VIOLATIONS BY CAUSE:** `body in the claim` 48 · `in-channel` 7 · `open-water` 6.

⚠ **AND THE PUBLICATION DISCIPLINE §0.3a DEMANDS (exit 2), stated once:** MF-ARCH's predicate
corrections account for `waterCrossings` **+16**, `waterViolations` **+14** and
`zoneMajorityOutside` **−186** on a **byte-identical fabric**; MF-ARCH-2's fabric change accounts
for the rest. ⛔ **`100 → 165` IS NOT ONE MOVEMENT AND IS NOT PUBLISHED AS ONE.**

---

## §5 · THE PLATES — 32 of 32 VIEWED, EACH WITH A WRITTEN VERDICT (exit 1)

**THE THREE-NUMBER RECONCILIATION: files 32 · previews 32 · ledger 32.** ✔
(32 `.svg` in `mf-proto-out/arch2/`; 32 `.png` rasterized to `MFW0-plates/` at 1,400 px by
`MFW0-raster.mjs` with a per-file sha manifest; 32 rows in `arch2/manifest.json`.) Six 2×2
contact sheets at 1,000 px/cell were used for 24 of them and full-size reads for 8; **every one
of the 32 was rendered and seen** (§231.2).

⚠ **THESE ARE MF-ARCH-2's PLATES — the tip W0 inherited, which is what §5 W0 asked to be judged.**
14 of them have since moved under G-34 and 6 more under G-33 (§1); the causes are declared and
the *new* plates are W1's first sight.

### §5.1 · Parchment (16)

| plate | verdict |
|---|---|
| **thorp** `Dar-al-kala` 27 souls | Reads as **an incident in the countryside** — 9 buildings on a crossroads, correctly tiny. ⛔ **The plate is 95% field patchwork and the patchwork is the weakest thing on it**: flat polygon washes, one hedge weight, no hachure. G-15's case in one image |
| **hamlet** `Sundfoss` 220 | The green and the two rows read; the road fork is legible. Same countryside verdict. Building bars already read **mechanically parallel** at this size |
| **village** `Mingguan` 512 | Two named quarters, a proper green, a market hole. ⭐ The best of the low tiers. Still 85% flat field |
| **town** `Mahabagh` 3,502 | ⭐ **The strongest plate in the corpus.** Circuit, ditch, 2 gatehouses, 2 water gates, river through the middle, market and government quarters legible. ⛔ **Streets cross the river with no bridge in at least four places** — G-34's defect, visible at a glance |
| **city** `Kitaqiao` 20,091 | Large, plural, legible districts. ⛔⛔ **The wall and the high street STRIDE ACROSS THE OPEN BAY** to a spit in the lower-left. This is the `open-water` conviction made visible and it is the single worst thing on any plate |
| **metropolis** `Warmgrund` 71,325 | Two circuits; the inner one clearly visible cutting the north-west grid. ⛔ **The grid runs through the old wall line completely unchanged** — same grain, same bearing both sides. The epoch axis is correct and **inert** |
| **polycentric** `Strathgarth` 3,334 | The dumbbell reads; one circuit, ditch dashes clean. Two centres are discernible but not *distinct* |
| **highwater** `Mahabagh` demoted | ⭐ **Two rings, and their shapes genuinely differ** (§5.3). City extent, town life. Bricked gates present |
| **mountain** `Mingguan` relief 0.66 | ⛔ **NO RELIEF IS DRAWN.** A mountain village on a plate with no hachure, no rock hatch, no terracing — only scrub ticks. §9.5b acceptance **FAIL** by eye, confirmed |
| **fjord** `Beiyuan` relief 1.00 | ⛔ **Same failure and worse**: a "fjord" that reads as a plain bay because there is no relief on either shoulder. The reconciliation is *structurally* right and *visually* absent |
| **siege** / **plague** / **famine** `Mahabagh` | ⚠ **Indistinguishable from the town at plate scale.** Famine's market is marginally barer. **The §10 state expressions are not legible at glance range** — they exist in the geometry and do not survive the zoom-out |
| **migration** `Kitaqiao` | As city, plus a camp at a gate. Same bay-crossing defect |
| **year-018** `Mahabagh` yr 18 | ⭐ **NO CIRCUIT, correctly** — the wall was raised in year 49. The single cleanest proof that the snapshot axis works. River crossings undecked |
| **year-100** `Mahabagh` yr 100 | Wall stands, market place still open. Differs from the present-day town visibly and correctly |

### §5.2 · darkFantasy (16)

The night lens preserves the §9.7 value hierarchy on every leaf — roads stay the palest role and
roofs sit several steps darker — so the reskin claim holds structurally on all sixteen.

| plates | verdict |
|---|---|
| **thorp · hamlet · village · mountain** | ⚠ **Too dark to read at plate scale.** The countryside collapses to a near-black field and the settlement is a small grey smudge. The hierarchy is intact; the *contrast* is not |
| **town · siege · plague · famine · year-018 · year-100** | ⭐ Strong. The fabric reads, the wall reads, the river reads as a dark ribbon. The best night plates in the set |
| **city · migration** | Good; the bay reads as deep water — which makes the road **crossing** it more obviously wrong, not less |
| **metropolis** | Good; the second circuit is *more* visible at night than on parchment |
| **polycentric · highwater** | Good; highwater's two rings separate cleanly |
| **fjord** | Sea reads; land still has no relief |
| **all 16** | ⚠ **The cartouche and legend are close to illegible** — dark text on a dark panel. A real legibility defect of the night lens, not of the geometry |

### §5.3 · THE THREE NAMED JUDGMENTS — evidence and my view. ⚠ **THESE ARE CHAIR CALLS.**

**(1) Does the epoch-derived old core read as an OLDER town rather than a smaller copy?**
⭐ **EVIDENCE FOR:** on `highwater` the two rings are demonstrably *different shapes* — the outer
is a large rounded polygon, the inner an angular pentagon with its own bays; they are not a scaled
copy. §240.1's containment residual is 0 on every ring and no two rings share a bay-and-lobe
signature. ⛔ **EVIDENCE AGAINST, and it is the stronger:** on the `metropolis` the inner circuit
crosses a **planned grid that does not change across it** — identical grain, identical bearing,
identical plot rhythm inside and outside. **A reader sees a line, not a town.**
**MY VIEW: the WALL reads older; the FABRIC does not, and the fabric is what a reader looks at.**
This is exactly G-9's "structurally correct and visually inert" and it is W4's whole brief.
**Not a defect in §240 — a missing consumer of it.**

**(2) Does the outer ring at 0.899 of the city's extent leave a suburb that reads as a suburb?**
⭐ **EVIDENCE FOR:** there IS extramural fabric outside the outer ring on `city`, `metropolis`
and `highwater`, and it is visibly thinner than the intramural fabric.
⛔ **EVIDENCE AGAINST:** it reads as **the same town continuing past a line**, not as a suburb —
equal ribbon extents at every gate, the same plot rhythm, no junction band, no intake line.
**MY VIEW: it reads as SPILL, not as a SUBURB.** The named lever is W5's (G-23's unequal ribbons
and the extramural ladder), not W2's. **The 0.899 extent is not the problem; the sameness is.**

**(3) Does the ditch on the working circuit read right?**
⭐ **EVIDENCE:** the dashed ditch-and-bank line is present, legibly outside the curtain, on every
walled leaf, and on the two-ring leaves it sits on the **working** circuit — which is J-A2-7's
intended correction and it is visibly the right ring.
**MY VIEW: YES — this one reads right and I would not touch it.** The one reservation is that the
ditch is a single dash weight everywhere; the corpus varies ditch grammar by run type, which is
W2's G-3 business.

### §5.4 · ⭐ THE ONE NEW PLATE I DID VIEW — because building 13 bridges and looking at none is not finishing

Rendered at 2,600 px with a zoom on the river corridor (`MFW0-newplates/town-river-zoom.png`).
**`town` now carries 4 works: 3 `high/bridge` + 1 `artery/bridge`** (`highwater` the same).

⭐ **VERDICT: THEY READ AS BRIDGES AND THE TOWN READS AS A TOWN, NOT AS A BRIDGE FARM.** Each
deck draws as a hatched rung spanning the channel **at the road's own width**, sited exactly
where a road visibly meets the water; the two water gates keep their own distinct hatched-bar
glyph on the circuit, so the two works do not read as the same mark. Four crossings over a river
that snakes the full width of the settlement is a plausible count, and the places they sit are
the places a reader's eye already expected one.
⚠ **ONE RESERVATION, HANDED ON:** every deck on this leaf is `kind: bridge` — the `plank` and
`footbridge` rungs exist and are pinned but **the corpus never reaches them**, because no lane or
passage on any leaf actually transits. The rank ladder is built and untested by output.

---

## §6 · THE §264 FOLD's TWO ADDITIONS

### §6.1 · ⭐ THE DERIVED-vs-DRAWN GRAIN CENSUS — first run (exit 8)

Drawn grain measured with **MF-S1's own kernel** (`HFM1-grain2.py`'s scanline dark-run counter,
ported verbatim) at 3,000 px, inside a window taken from the **built umbrella's own bbox** — never
hand-set, so it cannot rot (the hand-keyed-address class).

| tier | n | derived ÷ drawn — min / med / max | graded? |
|---|---|---|---|
| thorp | 1 | 2.42 / 2.42 / 2.42 | ⚠ **UNVALIDATED-BY-INSTRUMENT — reported, NOT graded** |
| hamlet | 1 | 2.20 / 2.20 / 2.20 | ⚠ **UNVALIDATED-BY-INSTRUMENT — reported, NOT graded** |
| village | 2 | 1.62 / 2.33 / 2.33 | yes |
| **town** | 9 | **0.92 / 0.94 / 1.12** | yes |
| city | 2 | 1.43 / 1.43 / 1.43 | yes |
| metropolis | 1 | **1.63** | yes |

⭐⭐ **THE SHAPE OF THE LOSS IS THE FINDING, AND IT IS NOT WHAT THE HEADLINE SUGGESTED.** The gap
is **smallest at town (0.94 — the town DRAWS FINER than it derives)** and grows at **both** ends:
×1.4–1.6 above and ×2.2–2.4 below. The metropolis's famous "113 derived vs 70 drawn" now reads
**101.9 vs 62.6, ratio 1.63** after the re-pin. ⭐ **The census is the point, not the number:
after this the loss between intent and output has a home and cannot be discovered as a surprise
again.** **PROPOSED TOLERANCE for W3 to bind: `1.0 ≤ derived ÷ drawn ≤ 1.3` at town+, with the
low tiers reported and ungraded until the roof-count instrument exists.** ⟦chair's to ratify⟧

### §6.2 · G-40 · THE THREE MISSING INSTRUMENTS (exit 7)

**(ii) PER-EPOCH FILL-TONE SEPARATION — ⭐ PRODUCED, ON THE CORPUS.** Measured on the four
within-plate epoch pairs the compendiums isolated. Windows are **[E]** (eye-set against the
plates' **own labels**); everything inside them is **[M]** — MF-S1's own convention.

| plate | regions (the plate's own names) | Δ median fill L | Δ warmth | fill-tone IQR |
|---|---|---|---|---|
| **hf26** fire-rebuild | ZONA BRUCIATA vs QUARTIERE NUOVO | ⭐ **42.7** | **45.0** | 29.4 vs **21.4** |
| **hf26** | QUARTIERE NUOVO vs the organic core | 23.3 | 18.0 | **21.4** vs **48.3** |
| **hf274** Oldbank/Newcharter | OLDBANK vs NEWCHARTER | **8.4** | 4.0 | **39.4** vs **23.8** |
| **hf239** Castra Hiberna | CASTRA vs VICUS | **10.0** | 8.0 | 39.7 vs 36.0 |
| **hf347** Wealdbury | inner circuit vs outer burgages | **4.7** | 2.0 | 12.8 vs 16.9 |

⭐⭐ **TWO READINGS, AND THE SECOND IS THE SHARPER ONE.**
**(1) A VINTAGE DIFFERENCE ALONE IS DRAWN AT Δ median L ≈ 5–10; AN EVENT IS DRAWN AT ≈ 40.** The
corpus separates ordinary epochs by tone only *weakly* and reserves a large tone separation for a
catastrophe. **A generator that paints its epochs 40 apart will look like a fire, not like age.**
**(2) ⭐ THE PLANNED EPOCH IS MEASURABLY MORE *UNIFORM*, NOT MERELY DIFFERENT: fill-tone IQR
21.4 vs 48.3 (hf26) and 23.8 vs 39.4 (hf274).** That is a within-region **variance** discriminator
and it is more robust than the between-region median, because it survives any global tone shift.
**W4's label-free district census should use it.** ⟦lane finding, vetoable⟧

**(iii) FOOTPRINT RECTANGULARITY — ⭐ PRODUCED ON OUR OUTPUT; ⛔ NO INSTRUMENT ON THE CORPUS.**
area ÷ min-area-rectangle area, exact rotating calipers on each body's convex hull, over
**15,199 drawn bodies (8,387 over 10 distinct sites)**:

```
EVERY LEAF: p50 = 1.000.  p25 = 0.786–0.941 (the wings and L-plans).  p75 = 1.000.
```

⛔⛔ **THE MEDIAN DRAWN FOOTPRINT IS A PERFECT RECTANGLE ON ALL SIXTEEN LEAVES.** This is the
first *number* behind §252.3c's chair-zoom finding that the building bars "read MECHANICALLY",
and it is a harder fact than the zoom: more than half of every leaf's bodies are exactly
rectangular. ⛔ **The CORPUS side cannot be produced in this lane and must not read as a pass:**
it needs footprint *vectors*, the corpus is raster, and the 47-field register carries no
footprint-shape column at all. **⛔ NO INSTRUMENT (§249.2's fifth verdict).** *What it needs: a
building-outline segmenter over the 313 plates, validated against a hand-traced sample.*

**(i) TOWER SPACING CV + TOWERS PER UNIT CIRCUIT LENGTH — ⛔ NO INSTRUMENT.** It requires two
vision tasks that no existing instrument performs: **tracing the drawn circuit polyline** off a
raster plate, and **detecting tower glyphs on it**. The register has no wall or tower column, so
it is a new instrument rather than a query. **It may not read as a pass and the mechanism it
gates (G-3's per-run tower policy) stays out of the ranking until it exists.** *Cheapest first
version: a hand count with stated windows on the ~8 walled studiable plates at native
resolution — the same evidential grade as T-01's own n=1 rungs, clearly labelled [E]-windowed.*

---

## §7 · THE STANDING FLOOR, RE-QUOTED AT THE TIP (exit 6)

```
vitest (lane config, bare)               7 files / 173 tests passed   TRUE_EXIT=0
                                         (was 162 — +11 titles, 0 new test FILES)
cross-process determinism                10 processes, identical=10 mismatched=0
                                         98a29755cbb7db6564dab80c9f621a6cd2eea1de3d25451df06f75ecca980bc1
§17 / §17.4 / §205A / §200 drawn census  0 / 0 / 0 / 0 over 22,067 bodies (12,084 distinct-site), AREA-TRUE
§202 landlocked · §201B orphan streets   0 · 0
op ceiling (§217 per-tier ratchet)       96/96 renders under their tier ceiling
purity scan (comments stripped)          Math.random/Date/localeCompare/Math.pow/trig — NONE
sizeBaseline                             MAX 791 (buildFabric.js) against 800
GRAIN SWEEP (exit 5)                     14,600 populations, 1 → 200,000 — ZERO decreases
                                         every tier seam CONTINUOUS to 2 d.p.
```

⚠⚠ **THE SEAM TEST LIED BEFORE IT WAS FIXED, AND THE LESSON IS THE INSTRUMENT'S.** My first
spelling sampled the sweep's own 10-population grid and compared the two samples *straddling*
each seam — it read `70.00 → 70.29` at town→city and cried ⛔ JUMP. **Nothing had jumped.**
`bandPosition` is steepest at the *foot* of a band, so the curve's **slope** changes at a seam
while its **value** does not, and a straddling sample measures the slope change.
⭐ **THE CLASS: A CONTINUITY TEST TAKEN ON A GRID MEASURES THE GRID.** The seam is now found
*exactly* by binary search and the law is asserted where it is actually made — the curve reaches
`GRAIN_SEAMS[i+1]` from below at the last population of tier *i* and leaves it from above at the
first population of tier *i+1*:

```
thorp  → hamlet      pop     61   below 16.00 · seam 16.00 · above 16.00   CONTINUOUS
hamlet → village     pop    401   below 28.00 · seam 28.00 · above 28.00   CONTINUOUS
village→ town        pop    901   below 47.50 · seam 47.50 · above 47.50   CONTINUOUS
town   → city        pop   8001   below 70.00 · seam 70.00 · above 70.00   CONTINUOUS
city   → metropolis  pop  40001   below 87.50 · seam 87.50 · above 87.50   CONTINUOUS
```

---

## §8 · WHAT CHANGED, FILE BY FILE

| file | eff lines | what |
|---|---|---|
| `fabric/waterWorks.js` | 284 → **384** | `WATER_MEETING`, `waterMeetings`, `deckReach`, `runIsCovered`, `deckKindFor`; `deriveBridges` rewritten to consume the predicate and to loop every transit run; `crossPoint` retained **for the wall only** |
| `fabric/leafCensus.js` | 275 → **277** | the census consumes `meetings`; `bankside adjacency` / `decked crossing` exemptions named; `in-channel` / `open-water` / `body in the claim` causes published; the mill cure |
| `fabric/buildFabric.js` | 790 → **791** | `waterCensus` published whole; `meetings: bridges2.meetings` threaded to the census |
| `fabric/tierGrammar.js` | 194 → **197** | `GRAIN_BAND.metropolis` 100–130 → **80–120**; `GRAIN_VALIDATED` exported |
| `harness/exemplars.mjs` | — | `siteKey`, `siteRepresentatives`, `bothTotals` |
| `tests/lint/derivationGraph.walker.test.js` | — | **+8 titles** — the one-predicate guard, **3 counterfactuals incl. a PLANTED second spelling that convicts**, 3 behavioural arms |
| `tests/domain/townMapFabricBuildOut.test.js` | — | **+3 titles** — grain monotonicity, seam derivation, the UNVALIDATED rule |

---

## §9 · WHAT I DID NOT DO — stated plainly

1. ⛔ **NO GIT WRITE OF ANY KIND.** The repo tree is untouched; everything is in the sandbox.
2. ⛔ **I DID NOT MOVE THE PALETTE FOR C-1** (§2.3). The figure is installed and the distance to
   it measured; the pixels are W7's exit criterion 5 and a tuning-signature carve-out.
3. ⛔ **G-40(i) IS NOT PRODUCED** and is reported ⛔ NO INSTRUMENT with its design and cost (§6.2).
   **G-40(iii)'s CORPUS side likewise.** Neither may read as a pass.
4. ⛔ **I DID NOT EXTEND THE BRIDGE DERIVER TO COASTS.** The three `open-water` roads on
   `city` / `migration` reach **143 units past the strand**; a deck the width of the strand cannot
   span a bay, and building one was not among the chair's five arms. **Named, measured, handed on.**
5. ⛔ **I DID NOT WIDEN THE §205 A SUBJECT SET.** The body arm still walks institution solids only
   (92 bodies, MF-ARCH's measurement). The +16 at stage D sits inside that known set.
6. ⚠ **THE NEW PLATES ARE ONLY PARTLY VIEWED.** 20 of 32 moved. I viewed MF-ARCH-2's full set
   (which is what §5 W0 asked for) **plus the new `town` plate at 2,600 px with a river-corridor
   zoom, because a lane that builds 13 bridges and looks at none has not finished** (§5.4). The
   other 19 moved plates are unviewed and are **W1's first act**. ⚠ *This is the same debt W0
   existed to discharge, one wave on — it is cheap and it must not accumulate again.*
7. ⛔ **NO MEMORY WRITE, NO ODQ EDIT, NO SPEC EDIT.**

---

## §10 · JUDGMENTS (all vetoable)

- **J-W0-1 · THE ANSWER IS COMPUTED ONCE AND CARRIED, NOT COMPUTED TWICE FROM ONE MODULE.**
  "One predicate, one home" could have meant one *exported function* called by both consumers.
  It would have doubled the census's cost and left the two free to pass different arguments.
  `deriveBridges` computes the meetings and the assembly hands that object to the census, so a
  second reading is not merely forbidden — it is *absent*. *Veto and I will make the census call
  the predicate itself, at ~2× the water-census cost.*
- **J-W0-2 · A MEETING IS A RUN.** Sized against segment and channel formulations first
  (§3.1); the segment reading wanted 68 decks including five under block seams, the channel
  reading was fragile. *The sims are in `MFW0-sim{1,2,3}.mjs` and the numbers are quoted.*
- **J-W0-3 · A COAST HAS ONE BANK, AND THE STRAND IS WALKABLE GROUND.** This is the load-bearing
  half of §262.2(e). It follows `waterClaims`' own words and it is what makes the coastal
  exemption structural rather than a threshold. *Veto and the alternative is a parallelism
  threshold on |cos| — which I rejected because it is a tuning knob wearing a law's clothes.*
- **J-W0-4 · I PUT "89% OF THE FALL IS THE NAMED EXEMPTION" IN THE FIRST PARAGRAPH.** The chair's
  own test for this wave is whether the fall was bought by building or by forgiving. It was
  mostly forgiving, the forgiveness is defensible (§3.3), and burying that ratio would have been
  the failure the warning describes.
- **J-W0-5 · `deriveWaterGates` KEEPS `crossPoint`.** The wall's question is not the street's; a
  ring is *cut* by water at an intersection. The walker pins both homes so neither drifts.
  *Veto and the gates move to the run predicate, which will site gates wherever the curtain
  grazes a bank.*
- **J-W0-6 · C-1 IS A FIGURE FIX IN W0 AND A PIXEL FIX IN W7** (§2.3). *Veto and I will re-fit
  the palette to a measured warmth of 37 — but it is a tuning-signature change and the owner has
  carved that out.*
- **J-W0-7 · THE DISTINCT-SITE KEY IS `seed | terrain | demoted`.** Chosen to reproduce MF-W1's
  published figures exactly. *The defensible alternative is `seed | terrain` alone (9 sites),
  which would fold `highwater` into the town family and silently restate every W1 figure.*
- **J-W0-8 · I ADDED 11 TITLES TO TWO EXISTING FILES AND NO NEW TEST FILE**, deliberately — a new
  test file reds TWO censuses at landing and three waves of titles are already queued behind one
  landing.
- **J-W0-9 · THE PROPOSED DERIVED÷DRAWN TOLERANCE (1.0–1.3 at town+) IS A PROPOSAL, NOT A PIN.**
  Binding a tolerance on n=1 at metropolis would be tuning wearing measurement's clothes. *The
  census runs and publishes; the chair binds the band.*

---

## §11 · HAZARDS FOR THE NEXT LANE

- ⚠⚠ **`buildFabric.js` IS AT 791 EFFECTIVE LINES AGAINST 800.** Nine lines of headroom. MF-B7's
  standing warning now has a number: **the next member added to the assembly breaks the ratchet.**
  W1's substrate cannot land inside it.
- ⚠⚠ **`censusLeaf`'s SIGNATURE GAINED `meetings` AND IT IS NOT OPTIONAL.** A lane that makes it
  default to a recomputation "for safety" restores G-34 silently and the walker will *not* catch
  it — the walker forbids a second *caller*, and a fallback inside `waterRightOfWay` would be a
  second caller and would red. It is the *shape* of the temptation that matters: **do not add a
  fallback.**
- ⚠⚠ **THE BUILD SHEET IS BEING EDITED WHILE LANES RUN.** `GENERATION-SPEC.md` grew by 1,177
  lines mid-lane. **Re-read §5 before believing any exit criterion you cached.**
- ⚠ **THE ACORN MINT-TRIGGER GREW AGAIN.** `derivationGraph.walker.test.js` now carries 21 pins
  parsing source with `acorn`. The landing executor still owes an explicit devDependency, and a
  dependency bump is a MINT TRIGGER.
- ⚠ **THE TEST CENSUS MOVED BY EXACTLY 11 TITLES AND 0 FILES.** Four waves of titles are now
  queued behind one landing (b8b + MF-ARCH + MF-ARCH-2 + W0).
- ⚠ **`fabric.waterCensus` IS A REPORT, NOT A LAW SURFACE.** Nothing in the pipeline reads it
  back. A consumer that starts to would be reading an unverified copy of the meta.
- ⚠ **THE OP-CEILING RATCHET NOW HAS 1,422 PRIMITIVES OF METROPOLIS HEADROOM** (was 83). ⭐ That
  is §252.3a's metropolis third circuit becoming affordable — **and the ratchet only shrinks, so
  W2 must re-measure and re-pin rather than spend it.**
- ⚠ **`waterGates` FELL 22 → 20** at stage D, on the coastal leaves, as a consequence of the grain
  re-pin moving the circuit's relationship to the shore. Small, declared, not investigated.

---

## §12 · WHAT W1 INHERITS

1. ⭐⭐ **LOOK AT THE 19 STILL-UNVIEWED MOVED PLATES.** I answered the sharpest question myself
   (§5.4: **4 bridges on one river town read as a town, not a bridge farm**), but 19 of the 20
   moved plates have still not been seen — including every one of the 6 that moved under the
   grain re-pin, where **1,324 bodies vanished** and nobody has looked at the result.
2. ⭐⭐ **THE SUBSTRATE IS UNBLOCKED AND THE TWO PLATES THAT PROVE IT ARE ALREADY DAMNING**:
   `mountain` (relief 0.66) and `fjord` (relief 1.00) draw **no relief at all**. §9.5b's
   acceptance test fails by eye today, on a plate anyone can look at.
3. ⭐ **THREE MEASURED FACTS W1–W4 CAN SPEND IMMEDIATELY**: the derived÷drawn grain ratios
   (§6.1); the corpus's epoch tone separations — **≈5–10 for a vintage, ≈40 for a catastrophe**,
   with the planned epoch measurably *more uniform* (§6.2); and **footprint rectangularity p50 =
   1.000 on every leaf**, which is G-6/G-7's target in one number.
4. ⚠ **THREE §205 A RESIDUALS, EACH NAMED AND NONE CURED**: 6 `open-water` roads across the bay;
   7 `in-channel` seams lying in the river; 48 `body in the claim` on the un-widened subject set.
5. ⚠ **THE INTEGRATION SPIKE §265.3 RULED IN RUNS AFTER W0 SEALS.** Nothing in this lane touched
   the product and the sandbox diverged further — by four source files and one harness export.

---

## §13 · ARTIFACTS (scratchpad, `MFW0-*` / `laneMFW0-*`)

| file | what it is |
|---|---|
| `MFW0-battery.mjs` · `-A0/-stageB/-stageC/-FINAL.log` | the proof battery: 32-plate SHA diff vs MF-ARCH-2, the authoritative water set with both denominators, four drawn censuses, op ceiling, grain, purity, sizeBaseline |
| `MFW0-water.mjs` · `MFW0-water-{stageA,C,FINAL}.json` | §205 A by leaf, by arm and by the rule that refuses the exemption, with the meta-equality assert (`spyDisagreements 0`) |
| `MFW0-coastprobe.mjs` · `.json` | how deep each "crossing" actually goes; the measurement that produced the one-bank rule |
| `MFW0-sim{1,2,3}.mjs` · `.json` | the three formulations sized before anything was written |
| `MFW0-grain.mjs` · `mf-proto-out/w0/MFW0-grain.json` | the population sweep, the derived-vs-drawn census, footprint rectangularity |
| `MFW0-epochtone.py` · `.json` · `.log` | G-40(ii) on the four corpus epoch-pair plates |
| `MFW0-paper.py` | C-1's measurement of the plates' own paper against the ruled target |
| `MFW0-det.mjs` · `MFW0-det-FINAL.log` | cross-process determinism, 10 processes |
| `MFW0-raster.mjs` · `MFW0-plates/` · `MFW0-sheet-*.png` | the 32 previews and the six contact sheets that made §5 possible |
| `MFW0-sync.sh` · `laneMFW0-{base,stageB,stageC,tip}` | the four disposable trees the declared shift was measured on |
