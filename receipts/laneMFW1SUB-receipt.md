# Lane MF-W1 · **THE SUBSTRATE** (§5 W1, ODQ §269.4/§269.5): receipt

**Lane MF-W1 (Opus 5), 2026-08-17. THE SUBSTRATE WAVE.**
**Touched:** `mf-proto/build-out/**` (5 source files) and the scratchpad under `MFW1S-*` /
`laneMFW1S-*`. ⛔ **NO git tree write of any kind, NO repo gate, NO branch move, NO memory
write, NO ODQ or spec edit, NO stash.** Neither git tree was touched; the only repo access was
read-only `grep`/`sed` over `src/` to recover the engine's own canonical tables.

---

## ⚠⚠ §-1 · READ THIS FIRST — A FILENAME COLLISION THE BRIEF WALKED INTO

**My brief names my deliverable `laneMFW1-receipt.md` and my scratch prefix `MFW1-`. BOTH ARE
ALREADY TAKEN BY A DIFFERENT LANE.** `laneMFW1-receipt.md` (28,780 bytes, 05:27 today) is the
receipt of **lane MF-W1, the `waterViolations` 114 → 165 CHARACTERIZATION lane** — and it is
**cited by name** in `GENERATION-SPEC.md` §5 W0 exit criterion 2 and in `laneMFW0-receipt.md`.
Writing this file to that name would have destroyed a receipt the build sheet points at.

⛔ **I DID NOT OVERWRITE IT.** This receipt is `laneMFW1SUB-receipt.md`; my scratch and tips
carry `MFW1S-` / `laneMFW1S-`. ⚠ **A successor grepping "lane MF-W1" finds TWO lanes** — the
water one (05:27) and this one (the substrate wave). **J-W1S-0.** *The chair should rename one
of the two; I will not rename another lane's sealed artifact.*

---

## §0 · THE VERDICT, STATED FIRST

> ⭐⭐⭐ **THE EXEMPLAR CORPUS IS BUILT BY THE REAL GENERATOR, AND THREE WHOLE MECHANISMS WERE
> DARK ON EVERY LEAF OF IT.** `harness/exemplars.mjs` calls `generateSettlementPipeline` — these
> are real dossiers, not fixtures — and MEASURED at the base tip: `readResourceWords()` returned
> `[]` on **16 of 16 leaves**, so the entire §161a RESOURCE_GROUND contract machinery had
> **never once run**; `stressors` is a bare OBJECT on **7 of the 10 distinct sites** and the
> fabric's `Array.isArray` test could not see any of them; and the active-condition bridge was
> keyed on a vocabulary that **does not exist**. This is not a fixture-vs-product gap. **The
> corpus this programme has been grading for nine waves was drawing settlements whose economy
> and whose state the fabric could not read.**

> ⭐⭐ **THE ARCHETYPE BRIDGE IS WORSE THAN "ONE MAPPING TABLE AWAY FROM LIVE" (MF-INT1's
> reading): THE TABLE EXISTS AND MAPS A THIRD VOCABULARY.** Measured against the engine's own
> `CONDITION_ARCHETYPE_TEMPLATES` (46 keys): **1 of 9 bridge keys is real.**
> `plague_outbreak`, `famine_risk`, `food_shortage`, `siege`, `war_footing`,
> `monster_incursion`, `civil_unrest`, `migration_pressure` are names no kernel in the estate
> has ever written — while **13 of the 14 archetypes a generated settlement can actually
> carry** had no row at all. ⭐ **THE CLASS: A BRIDGE BETWEEN TWO VOCABULARIES CAN BE WRITTEN IN
> A THIRD.** Both endpoints were correct; nothing could red, because a key that never occurs
> never fires. `MFW1S-vocab.log`.

> ⭐⭐ **THE ACCEPTANCE TEST'S CAUSE IS FOUND, NAMED AND HALF-CURED — AND IT WAS NEVER A MISSING
> DERIVATION.** MF-W0 wrote *"NO RELIEF IS DRAWN"* on a `mountain` leaf that had derived **204
> hachures and 72 crag marks**. The marks existed, were passed to the renderer and were drawn.
> **The defect was the SELECTION: `cells.sort((a,b) => a.pri - b.pri)` — a HASH — and each mark
> family took the first N off the front.** That is a uniform random sample of every eligible
> cell, so the ration was spread evenly over all the steep ground instead of massed on any of
> it, and a hachure field that does not crowd reads as scrub. ⭐ **THE CLASS: A RATION SPENT
> UNIFORMLY OVER AN ELIGIBLE SET IS A RATION SPENT ON NOTHING.**

> ⛔⛔ **AND THE HONEST HEADLINE ON SCOPE: THIS WAVE IS NOT COMPLETE. Of six exit criteria, ONE
> is met, ONE is partial, and FOUR are not built** — the `buildable` refusal mask and its
> census, `waterBearing`, the two-scale coastline, and every new pin including the consistency
> pin and its counterfactual. **The suite is green at 178 tests, which is exactly PERF1's count:
> I added ZERO test titles, and the exit criteria are written in pins.** §6 states each one.
> ⛔ **The op ceiling is 91/96, not 96/96** — `hamlet` 1102 against 1100. §5.

---

## §1 · THE FIRST ACT — 19 OF 19 MOVED PLATES VIEWED, WITH WRITTEN VERDICTS

**THE THREE-NUMBER RECONCILIATION: files 19 · previews 19 · ledger 19.** ✔
Rasterized by `MFW0-raster.mjs` from `mf-proto-out/perf1/` (PERF1's set, sha-identical to
MF-W0's). 4 seen full-size at 1,600 px; 15 in three contact sheets at 900 px/cell. **Every one
was rendered and seen** (§231.2). ⚠ These are the 20 plates MF-W0's declared shift moved, minus
the one it viewed itself.

| plate | verdict |
|---|---|
| **city** parchment | ⛔⛔ **The wall and high street still STRIDE ACROSS THE OPEN BAY** to the lower-left spit, with a bridge tick on the water. W0's `open-water` conviction is unmistakable at 1,600 px and is still the worst single thing on any plate. The grain re-pin's **−322 bodies is INVISIBLE** — the fabric still reads full and plural; five districts legible. Relief 0.36 declared in the cartouche and **nothing on the ground answers it** |
| **metropolis** parchment | ⭐ Two circuits, the inner clearly visible. ⛔ **CONFIRMED AT SIZE: the NW planned grid crosses the inner wall line with identical grain, identical bearing and identical plot rhythm.** A reader sees a line, not an older town. The −680 bodies are not legible as thinning |
| **migration** parchment | **Indistinguishable from `city` at plate scale** but for a few marks at the north gate. The §10 migration expression does not survive the zoom-out |
| **highwater** parchment | ⭐ **Two rings whose shapes genuinely differ**; 4 white ladder decks over the river plus a hatched water gate, all sited where a reader expects them. The best evidence in the corpus that §240 works. Relief 0.30 declared, none drawn |
| **siege · plague · famine** parchment | ⚠ **Mutually indistinguishable, and indistinguishable from `town`.** Confirms W0 exactly: the §10 state expressions exist in the geometry and do not survive plate scale |
| **year-018** parchment | ⭐ **NO CIRCUIT, correctly** — the cleanest proof in the corpus that the snapshot axis works. Reads immediately as a different, younger place |
| **year-100** parchment | Wall stands, market place still open; visibly and correctly different from the present-day town |
| **town · highwater · siege · plague · famine · year-018** darkFantasy | ⭐ Value hierarchy holds on all six — roads stay the palest role. ⚠ **The river reads as a dark ribbon barely separable from the ground**, weaker than parchment. ⚠ **The cartouche is close to illegible** (dark text on a dark panel) on every one |
| **year-100 · metropolis** darkFantasy | metropolis's **inner circuit is MORE visible at night than on parchment** — the one place the night lens outperforms |
| **city · migration** darkFantasy | The bay reads as deep water, which makes the road crossing it **more** obviously wrong, not less |

⭐ **NOTHING UNEXPLAINED.** Every difference I can see against W0's own descriptions is
accounted for by G-34's crossings or G-33's grain re-pin. **No plate showed a change W0's
declared shift does not name** — which is exit 10 of W0 discharged from the other side.

---

## §2 · THE DECLARED SHIFT — FOUR STAGES, EACH MEASURED AFTER ITS OWN FINAL EDIT (§110.3)

Four stages because this wave carries **three independent causes** and they must not be
attributed to one another. Each is a separate tree; the SHA arm compares against
`mf-proto-out/arch2/manifest.json`, so the **base's own 20 moved plates are the zero point**.

| stage | what landed | 32-plate SHAs vs MF-ARCH-2 | moved vs the stage before |
|---|---|---|---|
| **base** | PERF1's tip | 12 identical / **20 moved** | — |
| **A · behaviour-neutral** | the `accessFreed` figure correction + its meta rows | ⭐⭐ **12 identical / 20 moved — THE IDENTICAL SET, PLATE FOR PLATE** | **0** |
| **B · geometry** | §161a resource cure (`availableResources`) | 4 identical / 28 moved | **+8** (hamlet, polycentric, mountain, fjord × 2) |
| **C · geometry** | §10 state cure (canonical shape + real bridge + typed danger set) | 2 identical / 30 moved | **+2** (thorp × 2) |
| **D · geometry** | the relief-mark ranking cure + the two derived rations + `reliefField` | 2 identical / 30 moved | further movement on already-moved leaves |

⭐⭐ **STAGE A MOVED NOTHING, AND THAT IS WHAT LICENSES EVERY LATER COMPARISON.** A rewritten
published figure, three new `meta` rows and a rewritten reason string — **not one byte of any
plate changed.**

⭐ **STAGE B's ATTRIBUTION IS EXACT AND IT IS A MECHANISM COMING ALIVE, NOT A REGRESSION.**
Relief moved on **6 of the 10 distinct sites**, and on two of them **the §5.-1c reconciliation
solver fired for the first time in the programme's history** — with the resource words dead,
`forcedConstraints` only ever saw terrain and water, so `reconcileLandform` had always returned
the declared family and `forced` was **false on every leaf ever measured**:

| leaf | relief base → tip | family base → tip | the dossier fact that did it |
|---|---|---|---|
| thorp | 0.124 → 0.124 | plains | (no contract-bearing resource) |
| hamlet | 0.154 → **0.327** | plains | `iron_deposits, iron, metalwork` ⇒ ore, reliefFloor 0.34 |
| village | 0.158 → 0.158 | plains | (foraging/honey/herbs carry no ground contract) |
| town · highwater · siege · plague · famine · year-018/100 | 0.299 → **0.345** | riverside | `gemstone_deposits, timber` |
| **city · migration** | 0.360 → **1.000** | **coastal → FJORD** | `precious_metals, gems` ⇒ ore+relief, which `coastal` cannot hold with sea+shore |
| metropolis | 0.149 → **0.317** | plains | `stone_quarry, gems, wool, livestock` |
| polycentric · mountain · fjord | unchanged | hills / mountain / fjord | already at or above the floor |

---

## §3 · THE THREE REAL-DOSSIER CONTRACT FIXES (§269.5 item 1)

### §3.1 · `readResourceWords()` — six spellings, zero hits ⇒ **LANDED**

The live writer is `src/generators/resourceGenerator.js:372`, `availableResources:
nearbyResources`, a plain string array. It was not among the six. **`resourceAnalysis.availableResources`
is now the FIRST row read**, and `RESOURCE_SPELLINGS` is exported so a walker can hold the
reader to its list. ⭐ **The lesson banked in the source: TOLERANCE TO SIX WRONG SPELLINGS IS
NOT TOLERANCE** — the comment above the function was written to prevent exactly the failure it
then suffered, and no seventh name fixes that; only a pin that reds when the reader goes dead
does. ⛔ **That pin is NOT WRITTEN.** §6.

### §3.2 · `stressors` is an OBJECT ⇒ **LANDED, and cured by delegation not by widening**

`settlement.schema.js:62` declares `stressors: ['stress','stresses']` as ONE alias group and
`canonStressors` has always handled the bare-object case. **The fabric had a second opinion
about the shape, in two places** (`stateMarks.js:90` and `buildFabric.js:724`), and both were
wrong the same way. The cure is `stressorKeysOf()` — **one home, delegating the SHAPE question
to the estate's own accessor** and keeping only the key-extraction, which is genuinely the
map's own question. G-34's ruling applied verbatim rather than re-derived.

⚠ **The array-of-keys shape is deliberately still accepted** — the four synthetic state leaves
are the programme's declared-shift baseline and migrating them would restate every published
figure for no gain. **Both arms are filtered through `STRESSOR_DISPOSITION`**, because an
unfiltered token would reach the danger test as free text and §5.0 forbids a prose regex
standing in for a typed fact.

### §3.3 · The archetype bridge ⇒ **LANDED, and it was the real defect**

`CONDITION_TO_STRESSOR`'s nine keys, checked against `CONDITION_ARCHETYPE_TEMPLATES`:

```
1 of 9 bridge keys are archetypes the engine produces; 8 are not.
⛔ FICTIONAL  civil_unrest · famine_risk · food_shortage · migration_pressure
              monster_incursion · plague_outbreak · siege · war_footing
    REAL      regional_migration_pressure
UNBRIDGED (13 of the 14 archetypes conditionPromotion.js can actually produce):
  plague · famine · war_pressure · vassal_extraction · rebellion · faction_challenge
  regional_tax_revenue_disruption · regional_criminal_pressure · regional_religious_pressure
  regional_authority_instability · dominant_npc_removed · regional_export_market_loss
  magical_instability
```

The table is rebuilt from `conditionPromotion.js`'s **own** `STRESSOR_ARCHETYPE_RULES`,
inverted — a re-reading of an existing decision, never a new one. **Where the inverse is not a
function** (siege, wartime and monster pressure all promote to `war_pressure`) it maps to the
**widest honest expression**, `wartime`, rather than guessing a siege. ⭐ *An inverse that is
not a function must return the fact both preimages share, never the more dramatic one.*
`CONDITION_UNBRIDGED` names the nine ruled-out archetypes with a reason each, so none can
quietly acquire an expression later without an argument.

### §3.4 · A THIRD instance of the same class, found in passing ⇒ **LANDED**

The assembly's `danger` test was `/monster_raider_pressure|wartime|siege|occupation|
insurgency|rebellion/` **over catalog keys**. Three of its six alternatives **cannot match any
catalog key that exists**: `monster_raider_pressure` (the catalog says `monster_pressure`),
`occupation` (`occupied`), and `rebellion` (an archetype name, never a catalog key). ⛔ **So a
settlement under monster pressure or under occupation did not nucleate** — two of the three most
obviously nucleating pressures in the catalog. `under_siege` matched only by the accident of
containing the substring `siege`. Replaced by `DANGER_STRESSORS`, six keys each with its
argument. **MEASURED consequence on `thorp`:** `wartime` now reads, `tenurePattern` flips
`dispersed → nucleated`, `tenureScore` 0.565 → 0.225 — incastellamento arriving for the first
time on a real dossier.

### §3.5 · `history.founding.kind` ⇒ ⛔ **NOT MINE TO FIX — OWNER-GATED, AND I DID NOT DECIDE IT**

MF-INT1's own finding is that the cure is a **generator** change (`+95/−0` lines in
`narrativeGenerator.js`), not a map change, and the sandbox already carries that patch — the
real pipeline produced `"kind":"organic"` on village/mountain/metropolis in my own probe. **The
fabric side is already correct**: `readFoundingKind`'s backfill law is right and THE PROMISE
depends on it. ⛔ **A generator change is an owner-gated class** (§269.5 lists three "contract
fixes"; this one is a fourth thing wearing the same coat). **I left it alone and did not
publish a provenance token either**, because a new `meta` row asserting "this kind was
defaulted" is a claim about the generator that only the generator can make honestly.
**J-W1S-6.**

---

## §4 · THE §202 REPAIR-LADDER DEFECT (§269.4) — **THE FIGURE IS CURED; THE LADDER IS NOT**

⭐ **THE FIGURE, CURED AND PROVED PIXEL-FREE (stage A).** `freed = shrunk − dropped` was an
arithmetic **between two different units**: `shrunk` counts SHRINK OPERATIONS (one per body per
round) and `dropped` counts BUILDINGS. It published `town 45 − 15 = 30`, `city 102 − 34 = 68`,
`metropolis 234 − 78 = 156` under a name that says *freed*, when PERF1 measured the true figure
as **ZERO buildings on every walled leaf in the corpus**. Now:

- `freed` is a **definition**: sealed at entry, not sealed at exit. Countable, in the unit its
  name claims, bounded above by `sealedPerRound[0]`.
- `shrinkOps` is published beside `shrunk` so a consumer wanting operations asks by name.
- `sealedPerRound` and `sealedAtEntry` are published, so the ladder **audits itself**.
- The reason string **says so out loud** when `freed === 0 && shrinkOps > 0`.
- `meta` gains `accessFreed`, `accessSealedAtEntry`, `accessSealedPerRound`.

⭐⭐ **THE CLASS, NOW FOUND AT THREE SURFACES IN THIS ESTATE: A FIGURE WHOSE NAME NAMES ONE
QUANTITY AND WHOSE BODY COMPUTES ANOTHER IS NOT A WRONG NUMBER — IT IS A NUMBER NOBODY CAN
CHECK**, because the name is what a reader checks it against. It survived four waves because it
was never zero, and a plausible non-zero number is the hardest kind of wrong to see.

⛔⛔ **THE LADDER ITSELF IS NOT FIXED, AND I HAVE THE DIAGNOSIS.** Reading the code beside
PERF1's `sealed[15,15,15,15]`: for an INSTITUTION the repair correctly shrinks the
**neighbours** or cuts a corridor (*"the houses give way"*). For an ORDINARY body it shrinks
**the sealed body itself** — and the thing sealing it is its neighbours. ⭐ **SHRINKING THE
PRISONER DOES NOT OPEN THE CELL:** an intact ring of neighbours still encloses a body that has
given up 14% of its own ground. **The institution arm already knows the law the ordinary arm
breaks.** The fix is to apply one law to both arms. ⛔ **NOT LANDED — it moves geometry and it
would ADD drawn bodies (town +15, city +34, metropolis +78) against an op ceiling this wave has
already broken.** **PLAUSIBLE, not CONFIRMED** — I did not build the counterfactual. **J-W1S-5.**

---

## §5 · THE PROOF FLOOR, RE-QUOTED AT MY TIP

```
vitest (lane config, bare)          7 files / 178 tests passed          TRUE_EXIT=0
                                    ⚠ EXACTLY MF-PERF1's COUNT — I ADDED ZERO TITLES (§6)
cross-process determinism           10 processes, identical=10 mismatched=0
                                    b6609732424a891bc804b883cb710d3989722ea71d6a4968c6cb14b76d6711de
                                    (MF-W0/PERF1's was 98a29755…980bc1 — moved, declared, §2)
§17 / §17.4 / §205A / §200 drawn    0 / 0 / 0 / 0 over 22,760 bodies (12,227 distinct-site)
§202 landlocked · §201B orphan      0 · 0
SCC instrument                      binding granularity 0 non-trivial · field granularity 0
                                    ⭐ THE SUBSTRATE ADDED NO CYCLE, at BOTH granularities
purity scan (comments stripped)     Math.random / Date / localeCompare / Math.pow / trig — NONE
sizeBaseline                        MAX 737 (buildFabric.js) against 800 — UNDER (was 733)
T-01 grain                          every leaf IN BAND
op ceiling (§217 per-tier ratchet)  ⛔ 91 of 96 renders under · hamlet 1102 / 1100 ⛔ OVER (−2)
```

### ⛔ THE ONE BROKEN FLOOR ITEM, WITH ITS EXACT CAUSE

`hamlet` carries `iron_deposits`. The §161a cure raises its relief 0.154 → 0.327, the mark
rations scale with the relief spread, and the leaf lands **2 primitives over its pinned
ceiling**. I reduced it from **−13 to −2** with two *derived* rations (below) and **stopped**:

- ⛔ **`MARK_BUDGET`'s constants are a tuning-signature value** — an owner carve-out.
- ⛔ **§217's ratchet only shrinks and raising it is owner-gated.**
- ⚠ The remaining lever is folding relief marks into `renderFolio`'s existing mid-pass ration
  against `CEIL`, which they are currently outside of — **that is W7's surface (THE HAND)**, and
  spending W7's declared shift from W1 to buy two primitives is the wrong trade.

**J-W1S-4: I report 91/96 rather than tune to 96/96.** *The chair's options are (a) ration
relief marks inside the renderer's CEIL pass, (b) re-derive `MARK_BUDGET.hill` — a hill sign
costs THREE primitives and is the dominant term, (c) accept `hamlet`'s ceiling at 1102.*

### ⚠ TWO CENSUS FIGURES MOVED THE WRONG WAY AND I AM NOT SMOOTHING THEM

| figure | base | tip | direction |
|---|---|---|---|
| `waterViolations` | 61 / 25 | **44 / 16** | ⭐ better (street arm 13 → 6) |
| `waterGates` | 20 / 9 | 22 / 10 | ⭐ better |
| drawn bodies | 22,067 / 12,084 | 22,760 / 12,227 | +693 |
| `zoneUnwashedBodies` | 1,613 / 1,301 | ⛔ **2,264 / 1,453** | **+651 worse** |
| `zoneMajorityOutside` | 209 / 127 | ⛔ **318 / 145** | **+109 worse** |
| `physicalViolations` | 1 (polycentric, five waves) | ⛔ **7 / 7** | **+6, NOT INVESTIGATED** |

⛔⛔ **`physicalViolations` 1 → 7 IS THE SHARPEST UNEXPLAINED ROW IN THIS RECEIPT.** It had been
1 for five waves. It is almost certainly the relief rise putting bodies on ground the physical
law refuses — which is **exactly the mask this wave was supposed to build and did not**. ⚠ *It
is a finding, not a smoothed number, and it is the first thing W2 or a W1-completion lane should
open.* **J-W1S-7.**

---

## §6 · THE SIX EXIT CRITERIA, GRADED HONESTLY

| # | criterion | verdict |
|---|---|---|
| **1** | every leaf publishes a **relief field**, not a scalar | ⭐ **MET (CONFIRMED)** for the field; ⛔ **the consistency pin over every §161a input and its counterfactual are NOT WRITTEN** |
| **2** | zero drawn bodies on `buildable == false`, censused per leaf, planted violation reds | ⛔ **NOT BUILT.** There is no `buildable` mask, no census and no counterfactual |
| **3** | `waterBearing` on every water-bearing leaf, pinned DERIVED not stored | ⛔ **NOT BUILT.** The symbol does not exist in the tree |
| **4** | the fjord shows a fjord and the mountain shows relief, by eye at 3000 px | ⚠ **PARTIAL** — see below |
| **5** | determinism 10/10; ceilings 96/96; SCC 0 non-trivial at both granularities | ⭐ determinism **10/10 CONFIRMED** · ⭐ SCC **0/0 CONFIRMED** · ⛔ ceilings **91/96** |
| **6** | two-scale coastline with the detail term damped at the worked waterfront | ⛔ **NOT BUILT** |

### §6.1 · CRITERION 1 — what the field publishes, and the row that justifies it

`substrate.reliefField(sub)` → `spread · bands[8] · steepShare · cragShare · flatShare ·
wetShare · slopeP50 · slopeP90 · localMax · reason`, on `meta.reliefField` for every leaf.
Derived on every read, never stored (§161 LAYER ZERO).

⭐⭐ **THE FIELD EARNED ITS KEEP ON ITS FIRST RUN, and the row that did it is `localMax`:**

```
mountain  spread 0.661 · 23.5% scarp · 46.3% level · slope p50 0.184 p90 0.523 · localMax 0.08737
fjord     spread 1.000 · 63.2% scarp · 10.4% level · slope p50 0.428 p90 0.672 · localMax 0.02665
```

**The mountain's ground is 3.3× steeper in absolute terms and reports a LOWER normalized slope
p50 than the fjord's**, because `sub.slope` is normalized to each leaf's own steepest cell
(`buildSubstrate`). ⛔ **`slope = 0.5` means "half as steep as the steepest thing HERE" and is
not comparable across leaves at all** — and every threshold in `RELIEF_BANDS` is stated in those
units, so *the same share of cells clears `hachure = 0.38` on a pancake and on a mountain.* **A
scalar could never have shown this; the field shows it in one row.** ⚠ *This is a live defect
in the substrate, now measured and published, and it is the reason criterion 4 is only partial.*

### §6.2 · CRITERION 4 — the acceptance test, judged by eye at 3000 px

**BEFORE (`MFW1S-acc-base-png/`, PERF1's tip):** on `mountain`, 204 hachures and 72 crag marks
scattered as **isolated single ticks across a flat field patchwork**, each one alone in a green
polygon with a field boundary running through it. W0's "only scrub ticks" is exact. On `fjord`,
the same, and the right half of the plate is a uniform tan wash with X-marks on it.

**AFTER (`MFW1S-acc-tip-png/`):**
- ⭐ **`mountain` — the crag marks now MASS INTO RIDGES.** Four legible chains of crag hatching
  (a diagonal ridge top-centre, one on the left flank, a strong one lower-left, and a long one
  sweeping bottom-centre to right), with hachures visibly denser against them. An arc of high
  rocky ground now reads, with the settlement in the gentler ground along the road.
  **The "terrain drama invisible" fail condition is CLEARED.**
- ⭐ **`fjord` — the crags mass into a shoulder along the north-east shore** and the headland
  carries a crag ridge; the scattered-tick reading is largely gone.

⚠⚠ **BUT NEITHER LEAF YET READS AS ITS LANDFORM, AND I WILL NOT GRADE THIS A PASS.**
- `mountain` reads as **rocky ridges in farmland**, not as mountain country — because **the
  field patchwork still tiles the entire leaf including the steep ground.** That is criterion
  2's refusal mask, which is not built. *The ploughland runs over the crags.*
- `fjord` reads as **a bay with a rocky shoulder**, not as a drowned steep valley — because
  nothing organises relief at the LARGE scale, and because the shoreline's detail term is
  uniform everywhere (criterion 6, not built).

**MY GRADE: criterion 4 is PARTIAL. The cause is found and named, the half of it that lives in
the relief pass is cured and visible, and the half that lives in the refusal mask is not built.**

---

## §7 · WHAT CHANGED, FILE BY FILE

| file | eff lines | what |
|---|---|---|
| `fabric/substrate.js` | 531 → **~590** | `readResourceWords` reads `resourceAnalysis.availableResources` FIRST; `RESOURCE_SPELLINGS` exported; **`reliefField()` NEW**; `slopeLocalMax` carried on the substrate |
| `fabric/stateMarks.js` | — | **`stressorKeysOf()` NEW** (delegates shape to `canonStressors`); `CONDITION_TO_STRESSOR` rebuilt on the real vocabulary (20 rows); **`CONDITION_UNBRIDGED` NEW** (9 ruled-out archetypes); **`DANGER_STRESSORS` NEW** (6 keys, each argued) |
| `fabric/relief.js` | — | the ration is sorted **steepest-first** instead of by hash; `steepRation` and `hillRation` derived from the ground each mark family is a picture of |
| `fabric/accessLaw.js` | 342 → **~360** | `freed` redefined as buildings; `shrinkOps`, `sealedPerRound`, `sealedAtEntry` published; the reason string convicts a ladder that frees nobody |
| `fabric/buildFabric.js` | 733 → **737** | one reader for stressors; `danger` via `DANGER_STRESSORS`; `meta.reliefField` + three §202 audit rows |
| **tests** | — | ⛔ **NOTHING. Zero new titles.** §6 |

⚠ **ONE NEW MODULE EDGE:** `fabric/stateMarks.js → src/domain/canonicalAccessors.js`.
`canonicalAccessors.js` is a **leaf** (no imports of its own) and is purity-clean; the SCC
instrument confirms **0 non-trivial cycles at both granularities** after it.

---

## §8 · JUDGMENTS (all vetoable)

- **J-W1S-0 · I DID NOT WRITE TO `laneMFW1-receipt.md`** — it is another lane's sealed receipt
  and the build sheet cites it by name. §-1. *The chair should rename one of the two lanes.*
- **J-W1S-1 · THE STRESSOR SHAPE QUESTION IS DELEGATED TO `canonStressors`, NOT RE-SPELLED.**
  A fabric-local `typeof x === 'object'` would be a second answer to a question the estate
  already answers, free to drift on the next alias the schema gains. *Veto and it is three lines
  inline — but it is the exact shape G-34 was ruled against.*
- **J-W1S-2 · THE ARCHETYPE BRIDGE IS INVERTED FROM `conditionPromotion.js`, NOT AUTHORED.**
  Every row is a re-reading of an existing engine decision. Where the inverse is not a function
  it returns the **widest honest** expression, never the most dramatic. *Veto the `war_pressure
  → wartime` row and the alternative is guessing a siege that may not be there.*
- **J-W1S-3 · THE MARK RATIONS ARE DERIVED FROM THE GROUND, NOT TUNED.** `steepRation` and
  `hillRation` are "a mark family's ration is proportional to the ground that family is a
  picture of". *That they also bought 11 of hamlet's 13 over-ceiling primitives is a consequence,
  and I am naming it as one rather than presenting the ceiling as the reason.*
- **J-W1S-4 · I REPORT 91/96 RATHER THAN TUNE TO 96/96.** The three remaining levers are a
  tuning-signature value, an owner-gated ratchet, and W7's surface. §5.
- **J-W1S-5 · THE §202 LADDER'S BEHAVIOUR IS DIAGNOSED AND NOT FIXED.** The diagnosis is
  labelled **PLAUSIBLE**: I read it off the code beside PERF1's counts and did **not** build the
  counterfactual. *It also adds ~127 drawn bodies against a ceiling already broken.*
- **J-W1S-6 · `history.founding.kind` IS OWNER-GATED AND I LEFT IT ALONE**, including the
  tempting provenance token. §3.5.
- **J-W1S-7 · `physicalViolations` 1 → 7 IS PUBLISHED UNINVESTIGATED**, in the first table a
  reader reaches, rather than found later. It is the strongest single argument that the refusal
  mask is the missing piece.
- **J-W1S-8 · I SPENT THE WAVE ON THE THREE CONTRACT FIXES AND THE ACCEPTANCE DIAGNOSIS RATHER
  THAN ON THE MASK.** *Defensible because the contract fixes are the input every later criterion
  reads and because the acceptance failure's cause was unknown; but the chair should know the
  trade was made, and that the mask — criterion 2, the one with a census — is the casualty.*

---

## §9 · HAZARDS FOR THE NEXT LANE

- ⚠⚠ **THE EXEMPLAR CORPUS IS REAL-DOSSIER, NOT SYNTHETIC.** `exemplars.mjs` calls
  `generateSettlementPipeline`. Every "the fixtures synthesize X" claim in the inherited
  receipts is true only of the **four** state leaves' `stressors` array. **Any other dead read
  found by INT1's census is dead on the corpus too**, and curing it will move plates.
- ⚠⚠ **A THIRD VOCABULARY IS THE FAILURE MODE TO SWEEP FOR.** Three instances found in this one
  lane (`CONDITION_TO_STRESSOR`, the `danger` regex, `readResourceWords`). **A table whose keys
  never occur cannot red.** The cheap census is: enumerate every literal key set in the fabric
  layer and check each against the engine table it claims to bridge.
- ⚠⚠ **`sub.slope` IS NORMALIZED TO ITS OWN LEAF'S MAXIMUM AND IS NOT COMPARABLE ACROSS
  LEAVES.** Every `RELIEF_BANDS` threshold is stated in those units. `reliefField().localMax`
  is now published so an absolute reading can be recovered — **use it, or your cross-leaf
  comparison is measuring two different units.**
- ⚠ **THE RELIEF FLOOR IS APPLIED AFTER THE RECONCILIATION SOLVER, UNCONDITIONALLY.** `hamlet`
  is now a `plains` leaf with relief 0.327 because `iron` raised `reliefFloor` to 0.34 while the
  solver kept `plains` (the declared family's +5 tie bonus won). **That is a "plains" family
  with hill-country relief — the incoherence §5.-1c exists to refuse, arriving through a door
  the solver does not watch.** Not investigated.
- ⚠ **THE OP CEILING IS BROKEN BY 2 ON `hamlet`** and the relief marks are **outside**
  `renderFolio`'s mid-pass ration against `CEIL`. Any wave that raises relief again pays this.
- ⚠ **`meta` GAINED FOUR ROWS** (`reliefField`, `accessFreed`, `accessSealedAtEntry`,
  `accessSealedPerRound`). `reliefField` is an OBJECT with an 8-element array — a consumer that
  serializes `meta` naively will see it.
- ⚠ **THE TEST CENSUS MOVED BY EXACTLY 0 TITLES AND 0 FILES.** Five waves of titles remain
  queued behind one landing; **this wave added nothing to that queue and owes the pins in §6.**

---

## §10 · WHAT W2 INHERITS

W2 is **THE WALL, WHOLE**, and it is defined by two rulings: **§251.4b — §240 and the run chain
are ONE piece of work**, and **§250.5 — without `circuitDemotion`, every new ring ERASES the
history the epoch model was adopted to express.** Against that:

1. ⭐⭐ **A LIVE §161a SUBSTRATE FOR THE FIRST TIME.** Relief is dossier-caused on 6 of 10
   distinct sites and the reconciliation solver fires. **W2's terrain-surrender wall run has
   ground to surrender to** — but ⛔ **there is still no `buildable` mask**, so the run cannot
   yet be derived from a refusal, only from slope.
2. ⭐ **A PUBLISHED RELIEF FIELD ON EVERY LEAF** (`meta.reliefField`), with `steepShare`,
   `cragShare`, `flatShare` and the `localMax` divisor. **Two of the nine run types are keyed on
   exactly these rows.**
3. ⭐ **A LIVE §10 STATE ARM.** `siege`/`occupied`/`monster_pressure` now read from real
   dossiers, which is what W2's barred-gate and invested-gate marks consume.
4. ⛔⛔ **THE OP CEILING IS ALREADY BROKEN (`hamlet` −2) AND W2 ADDS THE METROPOLIS'S THIRD
   CIRCUIT.** ⚠ **The metropolis headroom PERF1 and W0 published as 1,422 is now 1,210** — the
   relief cure spent 212 primitives of it. **§252.3a's op-ceiling arithmetic must be re-taken
   against 1,210, not inherited.**
5. ⛔ **FOUR OF SIX W1 EXIT CRITERIA ARE OPEN** and the mask (criterion 2) is the one W2's
   terrain-surrender run and W7's §214 terrain arm both wait on. **W1 is not dischargeable on
   this receipt.**
6. ⚠ **`physicalViolations` 1 → 7**, uninvestigated, and most likely the mask's absence
   speaking. Open it before adding a circuit.
7. ⚠ **The determinism digest moved to `b6609732…6711de`.** Every same-seed comparison against
   `98a29755…980bc1` is now a comparison across a declared shift.

---

## §11 · ARTIFACTS (scratchpad, `MFW1S-*` / `laneMFW1S-*`)

| file | what it is |
|---|---|
| `MFW1S-moved/` · `MFW1S-moved-png/` · `MFW1S-sheet-{state,dark1,dark2}.png` | §1's 19 plates and three contact sheets |
| `MFW1S-sheet.mjs` · `MFW1S-raster` (via `MFW0-raster.mjs`) | the contact-sheet and raster instruments |
| `MFW1S-vocab.mjs` · `MFW1S-vocab.log` | §3.3's archetype-vocabulary census against the engine's own tables |
| `MFW1S-probe.mjs` | derived relief marks vs drawn output, per leaf |
| `MFW1S-live.mjs` | §2's per-leaf attribution of the two live-data cures |
| `MFW1S-diff.mjs` | single-leaf SVG + meta diff between two tips (found the `tenurePattern` flip) |
| `MFW1S-battery-{base,sA,sB,C,D,E}*.log` | the four-stage declared-shift battery |
| `MFW1S-det.log` · `MFW1S-det-raw.log` | 10-process determinism |
| `MFW1S-suite.log` | 7 files / 178 tests |
| `MFW1S-acc-base-png/` · `MFW1S-acc-tip-png/` | §6.2's acceptance plates at 3,000 px, before and after |
| `MFW1S-sync.sh` · `laneMFW1S-{base,sA,sB,tip}` | the four disposable trees |
