# TE-CH-6 RECEIPT — the ARCANE_INST_KW deity-doctrine cure

SLOT: claude/composite-r4 = 233c35a69ccb67bb3ce2612427ad918a42fc4af8 (62 cars)
TREE: .../scratchpad/laneCH6-tree (detached HEAD). `.husky/_` PRESENT — pre-commit re-stages.
TIP: 1f0e88503 (five commits; see the RESUME POINT)

## RESUME POINT (2026-08-24 ~12:32 CDT)
DONE: everything except the gate verdict. FOUR COMMITS, tree clean:
  d2a59f481 the cure · 06c3a45f7 shift + census · f688c3105 edge bundles (8 files, one set)
  · 618a2b0e4 the MF-CH6 mint (182 packets, 0 READY, validator exit 0)
GATE 1 (laneCH6-gate.log): TRUE_EXIT=1 · `[gate-tail] exit: 1` — RED, ONE failure outside the
  frozen census and it was MINE: tests/lint/negativeAssertionAnchor.walker.test.js flagged
  magicLicenceCensus.walker.test.js:388, a bare `not.toContain` (frozen ceiling 0). PAID at the
  instrument's lawful path — `expectAbsentWithAnchor(ARCANE_INST_KW, word, 'wizard')` — with no
  ceiling, baseline or census widened, and re-proved with two mutants (the emptied-collection
  mutant, which the bare form would have passed, now reds by name). Commit 1f0e88503.
GATE 2 (laneCH6-gate2.log) — **GREEN**, verbatim:
  TRUE_EXIT=0
  [test-ratchet] OK — no test regressions (11 known failure(s) of 29070 tests, ceiling 11).
  [test-ratchet] STRICT DIST OK — 53 discovered/reported file(s), 467 test(s), zero
    failed/non-run/uncollected/missing/extra/duplicate rows.
  [gate-tail] end: 13:02  up 20 days,  2:33, 1 user, load averages: 17.53 30.39 35.63
  [gate-tail] exit: 0 (the gate's own status, not a pipe's)
  disk at end: 17,954,576 KB free (~17.1 GB). Tree clean at 1f0e88503.
LANE COMPLETE — then REBASED UNDER ME BY ANOTHER LANE, and re-verified (2026-08-24 ~13:10):
  My five commits were rebased onto 714a398b7 (TE-DEPLOYDOC-188) INSIDE this worktree.
  NEW TIP e4ed27f48; my gated tip 1f0e88503 is still a live object via reflog.
  · ALL FIVE COMMITS PRESERVED BYTE-FOR-BYTE — patch-id identical on every pair
    (d2a59f481->17c4d75a1, 06c3a45f7->c1b93b657, f688c3105->85a20b0b7,
     618a2b0e4->8c07bc7e5, 1f0e88503->e4ed27f48), with a NEGATIVE CONTROL proving the
    comparison is live (two different commits compare non-identical). ⚠ The first attempt
    at this control was VACUOUS — zsh does not word-split `set -- $pair`, so it compared
    empty strings and printed IDENTICAL five times. Re-run under node.
  · THE THREE STACKED-LANDING SHARED ARTIFACTS ARE INTACT at the new tip: census tuple
    still 2525/366/2159/21052/5852 (no conflict resolved to the slot side), packet manifest
    still 182 with MF-CH6 present, golden fixture 525 rows with ZERO hashes differing from
    my gated tip.
  · THE ONLY DELTA between the tree I gated and the current tree is docs/DEPLOY.md +11
    lines (DEPLOYDOC-188's). No test file, no source file, no fixture moved, so the
    collected-test count and the ratchet verdict transfer.
  · CHECKED THE ONE INSTRUMENT THAT COULD HIDE A REGRESSION UNDER A BANKED ENTRY: a
    docs/**.md write can add a naked claim invisibly, because enforcement-claims is banked
    failure #5. Re-run at the new tree it lists exactly the SAME SIX claims, none in
    docs/DEPLOY.md. Nothing hidden.
  ⚠ FOR THE CHAIR: the GREEN verdict was earned at 1f0e88503, not at e4ed27f48. It
    transfers on the evidence above; a stack normally re-gates at its own tip, and that
    call is the chair's, not this lane's.
COLLECTED-TEST COUNT read off gate 1's own runner report
  ($TMPDIR/test-ratchet-last-red.json): numTotalTests **29,070** = 29,067 + this lane's 3
  titles, numTotalTestSuites 9,442, numFailedTests 12 = the 11 banked + the one anchor bill.
  Gate 2 should therefore read 11. The 11 banked failures are the frozen census; entry #5 is
  tests/docs/enforcement-claims.test.js, whose six naked claims are all in
  docs/FABLE_VALIDATION_QUEUE.md / docs/GOLDEN_SHIFT_LEDGER.md / packets/foreign-policy/IN-0C.md
  — files this lane never opened (verified by reading the failure list, not by assumption).
Scripts: scratchpad/ch6work/{m2-classifier-census,m3-cause-attribution}.mjs · sweep.mjs
  (ROOT=<tree>) · paths.mjs (ROOT_A/ROOT_B) · receipt-probe.mjs (ROOT) · compare.mjs
Snapshots: ch6work/base-src (slot), mid-src (data half), before/mid/after/final.json

## THE MEASURED VIOLATION (by execution at the slot; totality control 525/525)
Instrument: the REAL classifiers driven with the exact entity shape assembleInstitutions.js:268
builds. Negative control: the same rows at magicExists:true — 7 rows struck in BOTH (maritime
and content-profile arms), so the differential is the magic arm alone.
- 311 rows / 276 distinct names. ARCANE_INST_KW had 39 members.
- The magic arm strikes 28 of 311 at magicExists:false — exactly the 28 licensed rows.
- ⛔ THE KEYWORD LIST ALONE DECIDES ONE ROW (`Dragon resident`). All others are
  over-determined by the `Magic` SHELF (carriesExplicitMagicMetadata), the `arcane` TAG and
  the declared licence. Removing the faith keywords frees NOTHING.
- §541.9 CONFIRMED: 24 of 276 names keyword-matched, 24/24 declare a licence (fully
  redundant), 5 contradict it (the `none`-licensed rows).
- Two faith keywords matched NO catalog row: 'divine healer', 'wandering healer' — live only
  over FREE TEXT (the fallback + filterServicesForMagic), which is the surface this car cures.

## WHY THE GATE HALF WAS BUILT, MEASURED, AND THEN REVERTED
A full `declared ?? inferred` reroute of the world law + institutionProbability P1/P3 +
magicFilter was implemented and swept. It works — Druid Circle 0 -> 36, Healer 0 -> 48, Elder
Grove 0 -> 17 at magicExists:false — but it puts `Magic`-SHELF rows into magic-free worlds and
the shipped `world_law_magic` coherence receipt convicts the record's own taxonomy fields:
**264 of 504 magic-free settlements FAIL, against 0 at the slot** (probe measured at base, mid
and after). Plus `textAssertsFunctionalMagic` convicts the NAME "Druid Circle" and the AUTHORED
DESC of two rows. That is a train (gates + the receipt's vocabulary + authored prose), and
PACKET_STANDARD says to stop and propose the smallest split. Reverted; scoped in the packet.

## WHAT SHIPPED
- `src/domain/arcaneInstitutionVocabulary.js` — six faith words leave ARCANE_INST_KW (39->33)
- `src/data/institutionalCatalog.js` — Druid Circle + Elder Grove Council: `arcane` tag
  dropped, `low` -> `none`. Healer (divine, 1st level) HELD at `low` (premise refuted).
- pins in two existing test files (no new file), 4 convicting mutants all red.

## DECLARED SHIFT (final, measured base -> tip)
golden master 30/525 (18 town, 12 village) · six field-level path templates · ZERO rosters and
ZERO instance-count moves in all five magic cases · hashes: mgExists:false 0, pm0 0, pm20 0,
pm50 30, pm80 139.
