# RECEIPT — LANE SEAT-78 · the irregular-force topology (`irregularForceEnabled`), LGT-P10
## STATUS: COMPLETE FOR THIS DISPATCH · 3 cars landed · 2 cars REFUSED with measurement
Seat: Opus 5 (implementer/verifier) under the Fable 5.1 chair · 2026-09-05
Dock tip: **`027cf668d42c8ee9fce8998e31dc239e00d23c11`** · porcelain 0 · detached · 3 cars over `5e28d5c83`

Dock verified on arrival: HEAD `5e28d5c8376b2c7333ffc8b911b378f04629da8f`, porcelain 0, `node_modules`
present as the dock's own link tree (NOT materialised by me). No hooks active in this dock
(`core.hooksPath` unset; `.git/worktrees/laneSEAT78/hooks` carries no non-sample hook), so no
invisible pre-commit re-staging was possible and `git diff HEAD` was never blind.

## OUTCOME TABLE
| # | Step | Outcome | sha |
|---|------|---------|-----|
| 0 | D10/D11 mini-panel FOLD (§745.2) | **ALREADY DISCHARGED — owed by nobody.** ODQ **§746** (2026-08-29) folded it as `DESIGN_W_SEAT.md` **AMENDMENT A2** (panel `wf_aa485d16-8ab`, 3 lenses, 10 rulings; A2 outranks F1–F7). Recorded; no commit needed. | — |
| 1 | Replay sealed SEAT-7a `ce9910469` onto `5e28d5c83` | **LANDED.** One append collision resolved (both keys kept); 4 auto-merges read, not trusted | `2ef249ed5` |
| 2 | The sealed car's unpaid `tests/lint/` landing bill | **LANDED** — 4 of 7 red files cured at cause; 1 arm handed up by the walker's own rule | `b8aab0a59` |
| 3 | The per-key dormancy FENCE (L-HOMES P13's shape) | **LANDED** — the SECOND verdict caller fenced and proven alive; it had **zero tests in the estate** before this | `027cf668d` |
| 4 | "SEAT-7b — the above-threshold half" | ⛔ **REFUSED, with measurement.** See STOP 1 | — |
| 5 | "SEAT-8 — Q-S3's third leg" | ⛔ **REFUSED, with measurement — the brief mis-identifies both.** See STOP 2 | — |

---
## ⛔ STOP 1 — the above-threshold half cannot be built under this brief's own fences
The brief orders it and, in the same brief, forbids the only shape it can be built on. That is not a
judgment call; it is a contradiction, and it is measurable in the design text:
- `DESIGN_W_SEAT.md` **F3** specifies the above-threshold half as *"**Ledger** `civilContests`, keyed by
  typed contest id … drop-when-empty; row = the `seedDeploymentState`-SHAPED irregular record + heads
  bank + `participation01` + provenance"*, with a `ledgerOwnershipManifest` row landing beside it.
- **A2.1.1** makes persistence load-bearing rather than incidental: *one open contest per settlement*,
  and a second gate-crossing *"feeds the OPEN contest's `participation01`"* — a contest that must
  survive across ticks. **A2.1.2** adds a `splintered` field on the homecoming metadata, itself
  flagged as a *"persisted-summary shape"*. **F7**: *"Zero new persisted fields **beyond the
  contest-window ledger row**."*
- My brief's fences: *"No persisted shape (F3's ledger stays deferred — owner-gated class)"* and
  *"No new persisted shape."* The sealed SEAT-7a car deferred it for the same reason, by name.
⇒ The above-threshold half **is** a new persisted ledger. A lesser version — threshold routing with
no ledger to route into — would mint a classification with no consumer, which is the dead-arm class
this car's own suite header is written against. **Not built. Owner-gated: the `civilContests` ledger.**
⚠ **BRIEF ERROR:** the brief calls the above-threshold half *"(D11)"*. It is not. D11 is F6 — the
intervention market and covert instigation. The above/below-threshold split lives **inside D10**, at
F3's *"Threshold routing"*.

## ⛔ STOP 2 — Q-S3's third leg is `irregularForceEnabled` itself, so SEAT-8 is not it
Re-derived from three independent places, none of them the brief:
- **A1.2.12** rules Q-S3 as `foreignSeatEnabled` + `legitimacyUpheavalEnabled` (D5 rides the seat key).
- **F6** adds the third: *"REC: a sibling `irregularForceEnabled` key beside `foreignSeatEnabled`/
  `legitimacyUpheavalEnabled` (three keys, each certified for exactly what it lights)"*.
- **ODQ §820** rules Q-S3 per A1.2.12 as exactly those **three** keys.
⇒ Q-S3's third leg is the key **car 1 already mints**. SEAT-8 is **D11**, and it mints no Q-S3 key at
all — F6: *"D11 rides the seat key AND the covert substrate's own flags."*
Its own gates, all measured:
- **A2.2.9** conditions SEAT-8 on W-COIN-2 landed. That precondition **is met** — `treasury.js`
  `computeUpkeep` is already ledger-parameterized and both `treasury.js:490/:1036` and
  `treasuryConservation.walker:197/:205` name SEAT-8's `civilContests`/`interventions` widening by
  name as future work. So this is *not* what blocks it.
- What blocks it: **A2.2.9** puts *"Both new coin flows … on the standing Q4 amendment (the owner sees
  them by name)"* → owner-visible enumeration. **A2.1.3** rules the rally onto
  `applyLegitimacyDeltasToUpdates` **with the re-band** → a cross-court **persisted-score write**.
  **F6** forbids riding `interventionEnabled` because it is preset-LIT and widening autonomous
  minting there *"is a DECLARED lit shift deferred until the owner sees it."*
⇒ Three owner-gated classes (new coin flows, a persisted-score write, a declared lit shift). **Not
built.**

---
## BRIEF ERRORS FOUND (all measured)
1. **§745.2's fold is stale.** The brief makes step 0 a live obligation with two dispositions. Neither
   applies: ODQ §746 folded it on 2026-08-29 (AMENDMENT A2). Nothing was owed.
2. **READ FIRST §4's path does not exist.** `refs/preserve/light-plan-2026-09-05:PLAN.md` →
   `fatal: path 'PLAN.md' does not exist`. The real path is
   `refs/preserve/light-plan-2026-09-05:**lightingwave/**PLAN.md`; LGT-P10 is the table row at :54 and
   "IN PARALLEL, FROM DAY ONE" is a §POSITION-5 successor section, not a numbered §6.
3. **"the above-threshold half (D11)"** — D11 is the intervention market + instigation (F6). See STOP 1.
4. **"SEAT-8 — Q-S3's third leg"** — the third leg is `irregularForceEnabled`. See STOP 2.

## ⛔ ERRORS IN THE SEALED SEAT-7a CAR ITSELF (it is a good car with a false receipt)
1. **It never ran `tests/lint/`.** Its RECEIPTS block names sixteen suites and no directory run. Run
   WHOLE at the replayed tip it convicts the car **seven ways in fifteen arms**. Every one of those
   walkers existed at its own base `6c49ecbbf`, so this is a bill it never paid — not one the new base
   created. `git show ce9910469:tests/lint/ruinFilterRoster.walker.test.js | grep -c irregularForce` = 0.
2. **`typecheck:domain:strict 1121/1121` is stale** — the live ceiling is **1120/1120** (green).
3. **Its lighting-census prediction is stale** — it says files 2515→2517; measured 2521→2523.
4. **Its tuning-register prediction is stale in both figures** — it says tables 225→226 and keys
   2099→2100; measured at this dock's own ends, 232→233 and 2108→2109.
5. **It claims both verdict callers are wired but fences only one.** `grep -rln
   'return_coup\|deployment_return_coup' tests/` returned **ZERO FILES** — the return-army caller had
   no test of any kind. Car 3 cures this.

---
## REGISTER DELTAS I CAUSE — every figure MEASURED at both ends, NO register act taken
I took no register act: no lighting refreeze, no `--update`, no `writer-reach --write`, no tuning
refreeze, no `--genesis`, no `--rebank`.

### 1 · Tuning register + inventory (`tuningRegister.walker`, 2 arms red — the DESIGNED handoff)
`node scripts/count-tuning-inventory.mjs` run at `5e28d5c83` **and** at my tip (report-only CLI; it
has no `--update` by design; porcelain 0 after both):
| figure | base `5e28d5c83` | tip `027cf668d` | delta |
|---|---|---|---|
| P1 tables | 232 | **233** | +1 (`irregularForce.js#IRREGULAR_TUNING`) |
| top-level keys | 2108 | **2109** | +1 (`SHARE_WEIGHT`) |
| spans machine-closed | 232 / 232 | **233 / 233** | +1 |
| declared rows | 232 | 232 | **0 — the row is OWED** |
| P2 unregistered named | 535 (222 files) | 535 (222 files) | **0** |
| P3 bare decimals | 6985 (373 files) | 6985 (373 files) | **0 — CONFIRMED, not predicted: the one decimal is housed inside the table** |
| tables no FILE imports | 8 | 8 | 0 |
| tables no IMPORTER NAMES | 146 | **147** | **+1 — flag this: the leaf is `IRREGULAR_TUNING`'s only consumer, so no importer names it** |
⚠ The walker's own message: *"Register it in `tests/lint/.tuning-register.json` as a DRAFT row —
registering is not signing."* ⛔ The row **must carry a unit**; `SHARE_WEIGHT` is a dimensionless
fraction of political standing. The inventory refreeze rides `TUNING_INVENTORY_REFREEZE` inside the
walker — chair only.

### 2 · Lighting census (`sovereigntyLightingContract.walker`, 1 arm red)
`TEST_FILES` **2521 → 2523** (executed; the arm's own message, actual-first). Cause: car 1's two new
test files. ⚠ **The other census figures (parked / credited / titles / suiteTitles) are OWED** — they
need the walker, and lane vitest is held. Car 3 adds **+1 `describe` and +6 `it`** to an existing
file, so the chair should expect **suiteTitles +1, titles +6** on top of car 1's two files. Predicted,
not measured — do not bank it without the walker.

### 3 · Writer-reach register (`writerReach.walker`, 3 arms red) — CAUSE FULLY ISOLATED
| figure | base | tip |
|---|---|---|
| `shapesDigest` | `8f634f2750f0a0d12b533a6cfeb4340b9597ee2b9c19dc4457a5a5856e066738` | `7bbf6a11de643320c545ebf48e598ce003bcfe53f654f43daf74c50cf460a8cc` |
| `population.thinKeys` | 9143 | **9144** |
| shapes / thinShapes | 1299 / 962 | 1299 / 962 (**both UNCHANGED**) |
⭐ **The cause is exactly one key, isolated by dumping `buildObservedCorpus()`'s shapes at both ends
and diffing:** the ONLY difference in the entire corpus is `simulationRules` (25 rows, below
`MIN_ROWS` 40, therefore a THIN shape) **gaining the single key `irregularForceEnabled`**. No shape
appeared, none vanished, no row count moved. Hence +1 thinKey and a moved digest, and nothing else.
⚠⚠ **AND THE MECHANISM BY WHICH IT ARRIVES IS WORTH THE CHAIR'S ATTENTION:**
`scripts/lib/observed-shape-corpus.mjs:91` discovers flags by source-scanning
`/simulationRules[^\n]{0,40}?\.([A-Za-z][A-Za-z0-9]*Enabled)\b/`, and `:751` builds
`simulationRules = Object.fromEntries(flags.map((f) => [f, true]))` — **the observed corpus runs with
every discovered flag LIT** (only `advanceEpochEnabled` is forced false at `:774`). So SEAT-7a's gate
read is discovered the moment it lands and **the force ratio is LIVE inside the OSR / writer-reach
corpus**, a world no customer receives. The movement is legitimate — this is precisely what the
corpus exists to observe — but "dark at launch" is a statement about presets, never about this corpus,
and the lighting wave should know it.
`node scripts/check-writer-reach.mjs --report` = **EXIT=0** (no violations; HOLD line is the
pre-existing WRW-2 owner debt), porcelain 0 after — so the DARK COHORT DID NOT GROW and only the
population figures moved. The door is `--write` (SHRINK-ONLY) and it is the chair's.

### 4 · Golden-freeze register — **NO ROW OWED, and this was measured rather than assumed**
The walker derives its roster by **espree AST** over `process.env.UPDATE_*` MemberExpressions plus
enrolled fixture paths. Both files car 1 adds carry **zero** `process.env` and zero `UPDATE_` hits and
mint no fixture; car 3 adds no file at all. `frozenAt` is `null` — the register is still UNFROZEN, and
`surfaces` are fixture paths with a `recordEnv`. So neither ENROL nor EXCLUDE is owed, and
`goldenFreeze.walker` was **green** in the whole-directory run. (This is the LGT-P14-WITNESS law
discharged by measurement, not skipped.)

---
## THE ONE RED I HAND UP BY THE WALKER'S OWN RULE
`couplingInclusion.walker :: a NEW cross-layer import is either licensed by a registry row or REDS`
→ `src/domain/worldPulse/deploymentReturn.js` imports `src/domain/worldPulse/irregularForce.js`
   (`INTERIOR→WAR`).
⭐ **The coupling is D10's own design showing through, not an artefact of the home I chose.** F4 makes
`participation01` *"the single politics↔force bridge"*, so the leaf sits between a WAR importer and an
INTERIOR import by construction. Measured over every option: **INTERIOR** mints `deploymentReturn →
irregularForce`; **WAR** mints `irregularForce → commonsVoiceKernel`; **any third family** mints
**two**; **unlayered** mints none only by making the bridge invisible, which this walker's header calls
*"the most expensive kind of wrong."* One row is the floor.
⛔ Why I did not mint it: the walker says, verbatim — *"minting a registry row declares a coupling's
direction, desk, flags and receipt address. That is a chair declaration, not a walker repair"* … *"The
register never grows without a chair ruling."* And the bill is bigger than a row:
`CHARTERED_VOLUME_PREFIXES` in `tests/domain/couplingRegistry.test.js:106` is a **closed set of
thirteen** — `['WR','TR','GR','WF','POP','IN','INT','SP','CW','ES','WY','HB','ENC']` — **with no
W-SEAT prefix**, and `couplingRow` demands a `receiptField` and an `intendedDesk` for a car whose
certification row **commits to minting zero news kinds and no mover family**. Improvising those would
be a false claim on a live registry.
**Chair's decision, drafted:** which volume prefix W-SEAT rows carry (a fourteenth prefix, or homing
under `WR`), whether a `couplingRegistrySeat.js` leaf is minted, and what `receiptField` /
`intendedDesk` a receipt-less political→force bridge names. The alternative that needs no row at all
is to keep the leaf unlayered — which I recommend against, for the reason the walker gives.

## PRE-EXISTING RED AT THE TIP — reported, untouched
`clampPrimitiveBaseline.test.js :: baseline exactly matches the files that still define a local
clamp/clamp01` — baseline names **72**, the tree defines **62**. **RED AT THE BASE TOO** (executed: the
eight failing files re-run at `5e28d5c83` came back `1 failed | 7 passed`), and all ten files in the
delta are outside this lane's diff: `townCartography/cartographyBuildings.js`,
`townCartography/cartographyMultiplicity.js`, `worldPulse/conquestExecution.js`,
`conquestFeasibility.js`, `conquestIntent.js`, `dispositionLedger.js`, `razing.js`,
`razingExecution.js`, `razingWitness.js`, `warAllianceRisk.js`. **Not mine. Not cured.**

---
## EXECUTED PROOFS (every exit captured in-shell; none taken from a task notification)
⚠ **A task notification reported this lane's own proof run as "exit code 0" while the captured
`PROOF1_EXIT` was 1.** The recorded hazard, live again today. Every figure below is from a captured exit.
- `tests/lint/` **WHOLE** + 14 named domain/property suites at car 1's tip, through
  `sh scripts/gate-mutex.sh --run --`: **152 files, 2309 tests, 8 files / 16 arms failed, `PROOF1_EXIT=1`**.
- **BASE COMPARISON** of the eight failing files at `5e28d5c83` (dock detached, run, restored; HEAD and
  porcelain verified in both directions): **`1 failed | 7 passed`, `BASE_EXIT=1`**. This is what makes
  the attribution above evidence rather than argument.
- After car 2: `typecheck:domain:strict` **EXIT=0** (1120/1120) · `typecheck:ratchet` **EXIT=0**
  (173/173) · `eslint` **EXIT=0** on every touched file, zero output · vitest over `irregularForce`,
  `irregularForceDormancy`, `ruinFilterRoster`, `wizardNewsAuthoring`, `domainAnyCastBaseline`,
  `couplingRegistry`, `couplingDesk`: **7 files, 77 tests, 0 failed, `CURE2_EXIT=0`**.
- `node scripts/count-domain-any.mjs` no longer names `irregularForce.js` at all.
- Module evaluation of the merged graph: both sides of the append conflict live together
  (`COUP_RISK_LABELS`/`coupRiskLabel` from the mainline **and** `resolveCoupVerdict`'s
  `forceRatioFactor`); dark factor is the literal `1`; garbage worlds return `1`, never NaN.
- **Car 3's aliveness sweep (node, 3,600 points):** 189 differ; min factor 0.917517; the pinned point
  `seed 's8'` / legitimacy 0 / incumbent 55 / challengers 20 / factor 0.945284 → **dark emits NO
  outcome, lit emits `coup_succeeded` + `deployment_return_coup`**. The first fixture drawn was
  VACUOUS (factor 0.984466, arms byte-identical at 1040 = 1040) and was discarded by measurement.

## ⛔ OWED-UNTIL-RESUME (the chair held all lane vitest for a gate-class run)
Car 3's suites were executed **verbatim under a node stand-in for `vitest`** (a `describe`/`it`/`expect`
shim; the files' own text with only import specifiers rewritten; run from the dock):
**`irregularForceDormancy.byteIdentity` PASS 11 FAIL 0** · **`irregularForce` PASS 9 FAIL 0**, exit 0
both. That is executed evidence that every assertion holds. **It is NOT a vitest green and is not
offered as one.** Run on RESUME, exactly:
```
sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint tests/domain/irregularForce.test.js \
  tests/domain/irregularForceDormancy.byteIdentity.test.js tests/domain/foreignSeatCoupAdj.test.js \
  tests/domain/rulingPower.test.js tests/domain/coupEvents.test.js \
  tests/domain/subsystemCertificationCorpus.test.js tests/domain/convergence.test.js \
  tests/domain/warDisposition.p2.test.js tests/domain/round3WaveF3Couplings.test.js \
  tests/domain/deploymentReturnHeraldWords.test.js tests/domain/deploymentReturnLevyConservation.test.js \
  tests/domain/deploymentReturnOccupied.test.js tests/property/commonsVoiceDormancyGolden.test.js \
  tests/property/mechanismLitCoverage.test.js
```
Expected at the tip: green **except** the four register/chair arms enumerated above
(`tuningRegister` ×2, `sovereigntyLightingContract` ×1, `writerReach` ×3, `couplingInclusion` ×1) and
the pre-existing `clampPrimitiveBaseline` ×1.
⚠ Also OWED: `tests/property/` fences as a directory (the brief names them; never run this dispatch).

## FENCES HONOURED
No register act. No new persisted shape. No tuning VALUE moved — the additive sixth-adj slot stays
**OPEN and unclaimed**, and `IRREGULAR_TUNING.SHARE_WEIGHT` is the sealed car's own dial, unchanged.
No `--amend`. Explicit staging only, verified against `git diff --cached --name-only` before each
commit. No `git stash`, no `git checkout --`, no `git show HEAD:<path> > <path>`. No rebase, no push,
no ref writes. No subagents. `simulationRules.js` gains no flag name in a comment but the one minted
in car 1's own commit. Backups taken before every hand edit (`$SC/*.pre*.bak`).
⚠ Two `git checkout --detach <sha>` round trips were used for the BASE comparisons; both verified
porcelain 0 before and HEAD + porcelain after, and both restored exactly.

---
## RETROVALIDATION ROW (for the Fable 5.1 chair)
| # | What was judged | What the chair must re-derive | Receipts | Priority |
|---|---|---|---|---|
| R1 | **The above-threshold half is refused as a persisted shape** | That F3's `civilContests` ledger + A2.1.1's one-open-contest law + A2.1.2's `splintered` field are jointly unbuildable without persistence, so the brief's car 2 contradicts its own fence | `$SC/wseat-seat78.md` F3/F7/A2.1.1/A2.1.2 | ⭐⭐ HIGH — an owner row (the ledger) |
| R2 | **Q-S3's third leg is `irregularForceEnabled`, and SEAT-8 is D11** | A1.2.12 + F6's flag paragraph + ODQ §820's ruling; and that D11's three gates (Q4 coin flows, the persisted-score rally, the `interventionEnabled` lit shift) are owner classes | `$SC/wseat-seat78.md`; `$SC/odq-seat78.md:31929` (§820) | ⭐⭐ HIGH — re-charter SEAT-8 |
| R3 | **INTERIOR is `irregularForce.js`'s layer home** | The subject argument against WAR, and that every home mints ≥1 cross-layer pair | `tests/lint/couplingInclusion.walker.test.js` (my entry, argued in place); `$SC/cure1.log` | ⭐⭐ HIGH — it gates R4 |
| R4 | **The CW-0 registry row is NOT minted, by the walker's own rule** | The volume prefix (a fourteenth, or `WR`), whether a `couplingRegistrySeat.js` leaf is minted, and what `receiptField`/`intendedDesk` a receipt-less bridge names | `couplingInclusion.walker` REACH_OWED_ROWS note; `couplingRegistry.test.js:106` | ⭐⭐ HIGH — one arm red until ruled |
| R5 | **The ruin-filter census literal moved 93 → 94** | That a facet-scalar `.institutions` read is the settled `attrition.js`/`warDeployment.js` shape and that growing the discovery set is a re-measurement, not a raise | `tests/lint/ruinFilterRoster.walker.test.js` (arm + exemption, argued in place) | ⭐ MEDIUM |
| R6 | **The Wizard-News ledger re-pin** | The split-shift arithmetic (+1 above the block, +7 below; 167+29=196) and the five unchanged signatures | `.wizard-news-authoring-baseline.json` `rePinNote` (prior note preserved) | ⭐ MEDIUM |
| R7 | **The two `any` holes typed away rather than baselined** | That the double cast through `unknown` is the verdict file's own idiom and that `RulingPowerSettlement` is the honest type | `count-domain-any.mjs`; `typecheck:domain:strict` 1120/1120 | ⭐ LOW |
| R8 | **Car 3's fixture, chosen by sweep** | That the first fixture was vacuous and the pinned point is a real outcome flip through the real writer | `$SC/probe-return-sweep.mjs`; the commit message's figures | ⭐⭐ HIGH — the vitest green is OWED |
| R9 | **The observed corpus runs ALL FLAGS LIT** | `observed-shape-corpus.mjs:91`/`:751`/`:774` — and what that means for the lighting wave's "dark at launch" claims | `$SC/shapes-base.json`, `$SC/shapes-tip.json`, the one-key diff | ⭐⭐ HIGH — it bears on LGT-P14 |
| R10 | **The sealed SEAT-7a car's receipt block is false in five places** | Each of the five, especially the missing `tests/lint/` run | this receipt's own section; base comparison | ⭐⭐ HIGH — it is a committed claim |

## SCRATCH ARTEFACTS (all under `$SC`, none in the tree)
`receipt-seat-78.md` (this) · `proof1-seat78.log` (the whole-directory run) · `base-seat78.log` (the
base comparison) · `cure1.log` · `cure2.log` · `tcstrict.log` · `tcratchet.log` · `wr-report.log` ·
`tuning-tip.log` · `shapes-base.json` / `shapes-tip.json` (the corpus diff) · `probe-shapes.mjs` ·
`probe-return-coup.mjs` · `probe-return-sweep.mjs` · `probe-layers.mjs` · `vitest-shim.mjs` +
`shimrun-*.mjs` · `msg-car1.txt` / `msg-car2.txt` / `msg-car3.txt` · `*.pre*.bak` (pre-edit backups) ·
`seat7a.patch` · `wseat-seat78.md` · `odq-seat78.md` · `lightplan-seat78.md`.

## DOCK TIP
**`027cf668d42c8ee9fce8998e31dc239e00d23c11`** — 3 cars over `5e28d5c83`, porcelain 0, no rebase, no push.
