Desk landing (fix car A): the dial-gated roster shrinks 31 → 30 by the walker's own measurement — `source on stressors` turned ordinary when the corpus configs took `stressTypes`

The census-totals run refused on five writer-reach arms; three were this one fact. At the composed tip the observed-shape corpus builds a config carrying `stressTypes` (`observed-shape-corpus.mjs:834`; writer-reach's register froze `stressTypes on _config` as a new LIT identity at register car 2), so `resolveStress.js:65` stamps `source: 'forced'` in the CONTROL world too, and `source on stressors` is written on both sides of the dial — no longer dial-gated. The walker's diff is the measurement (expected 31, received 30; confounded 55 → 54; gated 31 → 30; live.dialGated 31 → 30; the pack-attributable arithmetic 21 → 20; the control-removed list gains it, 3 → 4); the pins move to it and the roster loses that one line with a dated note. A shrink, no row rowed, no register act. Walker 54/56 with the two OSR-lineage arms left for fix car B.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
