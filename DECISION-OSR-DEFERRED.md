# DECISION — the OSR resolution is DEFERRED to its own act. The desk consist waits.
2026-09-05 · Opus 5 · ⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧

## WHY I AM NOT RULING AGAIN TONIGHT
Three lanes have now corrected me on this one surface in a row:
- **DEADREAD** refuted 2 of 3 "dead arms" — one was LIVE and my brief would have deleted it.
- **OSRMINT** refuted 2 of 3 legs of my mint ruling, and found that my own fences made it
  unexecutable (no schema-17 rung exists).
- **OSRCORPUS** refuted the replacement ruling's central premise: the corpus is a governed
  **DETECTOR SOURCE**, so editing it does not trade a finding for a re-freeze — **the plain gate
  takes the same branch and returns 1 without scanning at all.** A widened corpus DARKENS THE WHOLE
  INSTRUMENT. And widening is not a shrink: it clears 33 rows and **mints 20 new ones** across 15
  files, because 20 saves instead of 16 re-rolls the pulse past `MIN_ROWS = 40`.

⭐ Each refusal was measured, and each was right. **A fourth ruling from me on this surface, at this
hour, under pressure to unblock a 23-car consist, is the worst-quality decision available.** The
program's own doctrine settles it: TIME IS NOT THE CONSTRAINT.

## THE STATE, SO THE NEXT ACT STARTS FROM FACTS
- The read is **legitimate**: `criminalIncomePoolKey` fires on 52–83 % of stress-loaded worlds, and
  its docblock records the deliberate rule *"keyed on the FLAG, never on the LABEL"* — the
  label-trap law, fourth instance. Re-pointing is out; deleting a working feature is out.
- The finding is a **false positive of corpus reach** — 0 of 118 `incomeSources` rows carry the flag
  in the current corpus, and the observed key list matches the exemption's recorded list key for key.
- ⭐ **A strictly better cure is MEASURED AND READY** (OSRCORPUS): push the stress settlements into
  `roots` but NOT into `generated`, leaving producer 2's pulse untouched.

| | briefed (+1 CONFIG) | **topology-only** |
|---|---|---|
| clears the target | yes | yes |
| new rows minted | 20 | **0** |
| frozen rows cleared | 32 | 12 |
| corpusMeta fields moved | 6 of 9 | 2 of 9 |
| corpus cost | +27 % | **≈free** |

  It also clears both sibling `isCriminal` reads (`EconomicsTab.jsx`, `treasury.js`). It is a **pure
  shrink** — but it still edits a detector source, so it needs a **schema-17 rung**. Precedent is
  exact: **schema 15 was a rung cut for a change in this very file.**

## DECIDED
1. ⛔ **Do NOT bank the two arms.** The census has headroom (6 of 17), and the instrument sanctions a
   hand-attributed entry — but banking DISARMS the guard, and these two arms are precisely the ones
   that catch new dead reads. That is the pathology recorded at
   `a-banked-census-row-keeps-accruing-the-thing-it-forbids`, and taking it here to save a night
   would be knowingly repeating it.
2. ⛔ **Do NOT revert the read or the block.** The lens is real and the ECF lane's work is sound.
3. ✅ **The schema-17 mint + the topology-only corpus change is its own act**, with the measurement
   above already done. It is not a landing step.
4. ✅ **The desk consist stays sealed and blocked** at `refs/preserve/train-desk-integrated-2026-09-04`
   (23 cars, 4 of 6 registers in). It loses nothing by waiting.
5. ✅ **Land the UNBLOCKED work meanwhile** — clamp wave 1, clamp repair (incl. the spun-off
   `planUnitCm` floor), and anchors are all sealed, independent, and owe no OSR act.

## ONE HAZARD TO FILE
⚠ **An inverse edit is not self-verifying.** OSRCORPUS's inverse edit removed its block but left a
stray blank line; `git diff` looked innocuous and `cmp` against a pre-taken backup caught it.
**Take the backup BEFORE the plant, and verify the restore with `cmp`, not with eyes.**
