# RECEIPT — LANE SOAK-HONEST-909
**Seat: Opus 5 — Fable-unvalidated · Chair: Fable 5.1 (re-dispatch session b43943b4) · dock `$SC/laneB6` · implementation lane, FIVE cars**

**STATUS: COMPLETE — FIVE CARS LANDED (`ec265d24c`, `268d53605`, `d58b489b8`, `68c3a1618`, `864e76be9`); dock porcelain 0; typecheck ratchet 173/173 (re-measured at the car-5 tip).** Cars 1-3 written Mon Sep  7 03:41:48 EDT 2026; **CAR 4** landed Mon Sep  7 04:11:47 EDT 2026; **CAR 5 (chair-chartered from the nine-skeptic + critic pass) landed Mon Sep  7 04:52:42 EDT 2026**, receipt re-written Mon Sep  7 04:56 EDT 2026.

> **CAR 5 IS THE SKEPTIC PASS'S CAR.** Nine Opus skeptics and a completeness critic read cars 1a-3 against their own messages and measurements. TWO findings were PRODUCT defects of the class this lane exists to close — an empty top-level series graded as CARRIED, and a row whose detector is unreadable skipping its `requires` grading — both reproduced at `68c3a1618` before being cured. The chair's horizon ruling, the ordered twin guard, the fixture year-1 correction and the cite-by-symbol law ride the same car. See **CAR 5** below, **R17-R24**, and **DEFERRED**.

> **CAR 4 IS A FOLLOW-ON, NOT A NEW LANE.** The chair's whole-suite proof at the car-3 tip (`$SC/whole-910.log`) found ONE unpredicted red - `tests/lint/negativeAssertionAnchor.walker.test.js`, arm *no NEW un-anchored negative assertion anywhere in the test corpus*, counting **seven** bare negatives this lane's own cars 1-3 wrote into `tests/soak-harness/tripwireRegistry.test.js` (lines 471, 472, 518, 519, 590, 723, 724; frozen ceiling **0**). All seven are now anchored. See CAR 4 below and **R15**.

## ARRIVAL CHECK — the RE-DISPATCH (Mon Sep  7 03:02:16 EDT 2026)
| line | expected | measured | verdict |
|---|---|---|---|
| `$SC/HOLD-VITEST` | absent | `No such file or directory` | **CONFIRMED — no chair gate running** |
| `git -C $SC/laneB6 rev-parse HEAD` | `6ebe0ef3b` | `6ebe0ef3bd2e7fa0f5d0ede5c93780ecfbf3aada` | **CONFIRMED** |
| `git -C /Users/cstokes/Desktop/settlement-engine rev-parse claude/composite-r4` | product tip | `6ebe0ef3bd2e7fa0f5d0ede5c93780ecfbf3aada` | **CONFIRMED — dock == tip** |
| `git -C $SC/laneB6 status --porcelain` | 2 (predecessor's car-1 work) | `M scripts/audit/whole-world-soak.mjs`, `M tests/soak-harness/tripwireRegistry.test.js` | **CONFIRMED — inherited, not discarded** |
| dock branch | detached | `git rev-parse --abbrev-ref HEAD` = `HEAD` | **CONFIRMED — DETACHED** |
| inherited diff re-proved BEFORE any edit | green | `npx vitest run tests/soak-harness/tripwireRegistry.test.js` → **EXIT=0, 9/9** at 03:03:45 | **CONFIRMED — the predecessor's work was already green** |

## CARS
- **CAR 1 — ship the series: DONE. `ec265d24c`** (3 files, +103/−31).
- **CAR 2 — dotted-path `requires` + walker extension: DONE. `268d53605`** (3 files, +409/−57).
- **CAR 3 — constant lift + arm `capacity_envelope_30y`: DONE. `d58b489b8`** (6 files, +326/−62).
- **CAR 4 — the seven negative assertions anchored: DONE. `68c3a1618`** (1 file, +36/−7; test bytes only).
- **CAR 5 — the weak zero's last two floors, the horizon channel, the twin guard, the citations: DONE. `864e76be9`** (9 files, +533/−116).

> ⚠⚠ **THE DURABLE RECORD OF CAR 1a STANDS FALSE IN FOUR FIGURES, DELIBERATELY (chair ruling, §909 car 5).** `ec265d24c`'s commit message carries `tripwireCaller 4/4, soakRegister 6/6, curveBandFreeze 10/10, demographicsEnvelope 9/9` where the lane's own per-file logs read **10, 8, 4, 6** — the alphabetical-order mis-attribution R8 discloses. It is **NOT amended**: an amend rewrites every car above it, the sha is already cited in the ledger and in this receipt, and the estate's own hazard says an amend after a register is taken orphans it. The correction lives here, in R8, and in the §910 ledger row; the per-file logs under `$SC/soak909/` are the receipts. The completeness critic concurs (G4: "record, do not amend").

### CAR 1 — the measurements
| what | measured | verdict |
|---|---|---|
| focused suites, one file at a time, exit captured in-shell | tripwireRegistry **9/9**, tripwireCaller **10/10**, soakRegister **8/8**, soakScriptSeams **8/8**, curveBandFreeze **4/4**, demographicsEnvelope **6/6** — every `EXIT=0`. ⚠ **These figures were first written mis-attributed** (a `grep -h … *.log` prints in ALPHABETICAL order, not loop order); re-read per file at 03:26 and corrected — see R8 | **CONFIRMED, after correction** |
| eslint on the three touched files | `EXIT=0`, zero output | **CONFIRMED** |
| the reachability walker's banked baseline | rows `2 → 0`, pairs `4 → 0`; `reach.unreachable` = `[]`, `reach.unreadable` = `[]` | **CONFIRMED** |
| the zero is CAUSED, not weak | the test reconstructs the pre-§909 writer by deleting the two shipped lines, proves the reconstruction non-vacuous (`legacyWriter.length < WRITER_SOURCE.length`, neither field in its key set), and re-runs the walker: **the old 2 rows / 4 pairs come back** | **CONFIRMED** |
| body-only false-positive list | `['non_finite_ledger_figure', 'capacity_plateau', 'capacity_floor_thaw']` → **`['non_finite_ledger_figure']`** (both capacity fields live inside `receiptBody` and survive the truncation) | **CONFIRMED** |
| fresh 30-year receipt, DEFAULT (`--years 30 --settlements 4`) | `fullInstrument: **true**`, `notExecutable: **[]**`, `deterministicFirings 0`, eval `EXIT=0`; series shipped 30 rows × 4 columns, last row == `finalPopulations` | **CONFIRMED** |
| …but the two capacity rows did **NOT** execute on it | `subsystems.rules.demographicsEnabled` = **false** on the default preset (`full_simulation`), so both rows are NOT-APPLICABLE — `evaluateTripwires` (`tripwires.mjs:474`) skips on `gate` **before** it reads `requires` | **REFUSAL — see the retrovalidation row** |
| fresh 30-year receipt, **LIT** (`--lighting demographicsEnabled=true`) — the run that actually proves the cure | gate **true**; `yearlyPopulations`/`yearlyDiedFlags` present (30 × 4); `capacity_plateau` **EXECUTED, verdict `[]`**; `capacity_floor_thaw` **EXECUTED, verdict `[]`**; `notExecutable: []`; `fullInstrument: **true**` | **CONFIRMED** |
| which guard the plateau row declares on a 30-year run | **its OWN horizon guard**, `if (pops.length < 150) return []` (`tripwires.mjs:324`) — 30 < 150. Its sibling declares `if (pops.length < 100) return []` (`:364`). Neither appears in `notExecutable`, which is exactly the addendum's prediction | **CONFIRMED — positively, the series is 30 rows long, so the detector was entered** |
| the lit 30-year run's one firing | `capacity_realm_load` — realm load **0.5449** outside `[0.6, 1.05]`; eval `EXIT=1`. Pre-existing engine reading, untouched by §909 (the archived receipt fires the same row at 0.4787) | **CONFIRMED — declared, not silent** |
| ARCHIVED B6 receipt `research-lit-4s-300y-4s-seed1.json` (schema 5, pre-series) | still `fullInstrument: **false**`; `notExecutable` = **both** rows, `"requires receipt.yearlyPopulations, receipt.yearlyDiedFlags — absent from this receipt"`; `capacity_realm_load` fires at 0.4787; eval `EXIT=1` | **CONFIRMED — an archived receipt still grades honestly** |
| the detectors are NOT vacuous at their own horizon | fed the 300-row series rebuilt from `behavioral.yearly[].stateVectors[id].population`, `capacity_plateau` convicts **all four** settlements — 8319@149 → 10869@299; 202 → 1017; 324 → 244; 38 → 159 — reproducing §907 M1-F1 exactly; `capacity_floor_thaw` correctly silent (nothing frozen) | **CONFIRMED (on a rebuilt series; a shipped 300-row series would need a fresh 300-year lit run, ~25 min, not in this lane's charter)** |
| register cells | **no edit, none owed.** `receiptSchemaVersion` is computed at mint (`register.mjs:195`, `finite(receipt?.schemaVersion) ?? 0`); both genesis cells scaffold `0`, not 5 | **CONFIRMED — the brief's premise refuted by the predecessor, re-affirmed here** |

### CAR 2 — the measurements
| what | measured | verdict |
|---|---|---|
| `capacity_realm_load` now declares its real dependency | `requires: ['behavioral.yearly[last].realmDemography']`; `receiptCarries` walks the envelope segment by segment (`[last]` = the final row, an empty array is absent) | **CONFIRMED** |
| a lit receipt without the reading is NOT-EXECUTABLE, not clean | reason on its face: `requires receipt.behavioral.yearly[last].realmDemography — absent from this receipt`; proved for a missing `behavioral`, an empty `yearly`, a null `yearly`, a null `realmDemography`, and a series carrying the reading on an EARLIER year but not the last | **CONFIRMED** |
| the walker grades the path | `tripwireFieldReach(rows, writerSource, nestedSource)` — first segment against the receipt writer, the rest against the nested writer's shipped keys, in a new `unreachablePaths` channel. On the live tree: `unreachable []`, `unreachablePaths []`, `unreadable []` | **CONFIRMED** |
| a nested path with no nested source is REFUSED, not passed | 2-arg call ⇒ `unreadable` names `capacity_realm_load` with the path in the reason | **CONFIRMED** |
| the zero is CAUSED, on the OBSERVER this time | reconstruct the pre-P4 observer by deleting the one shipping line (non-vacuity proved first): `unreachablePaths` = `[{capacity_realm_load, behavioral.yearly[last].realmDemography, segment realmDemography}]` | **CONFIRMED** |
| ⛔ a FALSE UNREACHABLE found in the walker itself and fixed | the literal scanner could not read a CONDITIONAL SHORTHAND SPREAD — `...(realmDemography ? { realmDemography } : {})`, the exact form `behavioral-observation.mjs:1054` uses — so its first reading was that NO writer ships `realmDemography`. Scanner extended | **CONFIRMED — caught by measurement before the test was written** |
| …and the extension moves no receipt-writer key | old scanner (`git show HEAD:` at `ec265d24c`, imported from scratch) vs new, same writer source: **43 keys, identical set**, added `[]`, lost `[]` | **CONFIRMED — the car-1 ratchet is untouched** |
| the "unreachable == declares requires" identity | **restated.** It held only while every requirement was a top-level field of ONE writer. Asserted now as the subset law plus a totality control that blinds BOTH writers and recovers the whole roster | **declared, see R9** |
| GRADE MOVEMENT over the four receipts named below ⚠ **the row's original title claimed "every `whole_world_soak` receipt on disk" and the census taken was FOUR — corrected at car 5 (critic G1/J3). 32 non-fixture receipts exist under `$SC` plus one fixture, 33 documents; the missing 21 are the §905 L-PROBE battery, graded by the critic and re-graded by car 5: every one NOT APPLICABLE by gate (`demographicsEnabled` undefined on 18, false on 3). The conclusion survives the wider set; the sentence did not.** | **ZERO real receipts change grade.** Archived 300-year `research-lit-4s`: lit, reading present → unchanged (`fullInstrument false`, the two series rows NOT-EXECUTABLE). Fresh 30-year DARK: gate dark → unchanged. Fresh 30-year LIT: reading present → unchanged. `tests/fixtures/simSoakReceiptFixture.json`: not a soak receipt shape, dark → unchanged | **CONFIRMED by census** |
| the ONE shape that moved is a synthetic FIXTURE | `soakRegister.test.js`'s `researchReceipt()` claimed `lit` and carried no realm reading — a shape the writer cannot produce (`observeRealmDemography` returns non-null on exactly the worlds whose demographic term runs). Corrected to the writer's shape; `realmReading: false` reconstructs the pre-P4 shape so the mint door's refusal is proved | **CONFIRMED — declared, see R10** |
| no register figure moves | `deriveRegisterFigures` never reads `realmDemography`: same figure KEY SET and identical VALUES with and without it, measured on the real 300-year receipt | **CONFIRMED** |
| focused suites, each run alone, exit captured in-shell | tripwireRegistry **10/10**, tripwireCaller **10/10**, soakRegister **8/8**, soakScriptSeams **8/8**, demographicsEnvelope **6/6**, soakInvariants **16/16**, realmScaleCertification **10/10**, simMetricEmitter **7/7**, curveBandFreeze **4/4** — every `EXIT=0` | **CONFIRMED (each count read from its OWN log file, per R8)** |
| eslint on the three touched files | `EXIT=0` | **CONFIRMED** |

### CAR 3 — the measurements
| what | measured | verdict |
|---|---|---|
| the constant lift — ALL THREE NAMED | `MOVING_SHARE_FLOOR` (0.05), `MOTION_FLOOR_01` (0.0025) and `CAMPAIGN_HORIZON_YEARS` (30) now live in `scripts/audit/soakInvariants.mjs` beside `LIVENESS_FLOOR`; `behavioral-observation.mjs` and `demographicsEnvelope.test.js` both IMPORT. `soakInvariants.mjs` still imports nothing. **The third was lifted BEYOND the brief's charter (it chartered two) — RATIFIED by the chair at car 5, conditional on the guard now shipped (R18)** | **CONFIRMED** |
| one spelling, checked in the tree | zero live `0.0025` literals remain in either former home (comment-stripped scan, asserted in the pin); the suite's own `CAMPAIGN_YEARS` now reads the shared home. ⚠ **"the lift REMOVED a spelling of 30" IS CORRECTED AT CAR 5 (critic G5/J1, by count): TWO definitions before (`demographicsEnvelope.test.js`'s own `const`, `behavioralContract.js:129`) and TWO after (`soakInvariants.mjs`, `behavioralContract.js:129`) — the lift MOVED the spelling. What it genuinely bought is that the SUITE stopped typing the figure (`CAMPAIGN_YEARS` is now an alias, measured at `demographicsEnvelope.test.js:57`). The certification twin is now GUARDED — see R18.** | **CONFIRMED for `0.0025`; the 30 claim CORRECTED** |
| ⭐ SAME-SEED PROOF that the lift moved nothing | a lit 30-year soak re-run at the same seed AFTER the change vs BEFORE. ⚠ **THE BYTE FIGURE IS WITHDRAWN AT CAR 5 AND REPLACED BY WHAT RE-EXECUTES.** Two skeptics could not reproduce "303334 bytes each"; nor could this car. RE-DERIVED at the car-5 tip: **on-disk 539,995 bytes EACH** (`wc -c`, exact match, and the skeptics' own figure). Every *stripped* figure depends on WHICH keys are stripped — this car's own seven-key strip reads **537,042 vs 537,043** and compacts to **303,439 vs 303,440**, and the skeptics' reads 305,121/305,120 — so no stripped byte count is quoted here as a receipt: a byte count without its strip is not one, which is the whole lesson. ⭐ **AND THE ONE-BYTE DIFFERENCE IS ITSELF THE ARGUMENT**: it is a host timing one digit longer, i.e. exactly the class of movement a byte count cannot tell from a behaviour shift. THE SUBSTANCE IS UNTOUCHED and is what the row was for, RE-EXECUTED at car 5: `finalHash` equal (`1a6f8477f596564bd47f5264dc8615eb7ee3242d45df6c9ea5dde505bb4fd3b5`), all **30** yearly composite hashes equal, population series equal, motion series equal | **CONFIRMED — no behaviour shift, none to declare; the byte figure corrected, the proof re-executed** |
| the roster | `TRIPWIRE_IDS` **11 → 12**; deterministic **9 → 10**; host-observability 2. `capacity_envelope_30y` lands after `capacity_realm_load` and before both host-observability rows | **CONFIRMED — the design's own §2.5 C3 prediction** |
| `tripwireRegistryDefects()` | `[]` | **CONFIRMED** |
| the walker at the final tip | `unreachable []`, `unreachablePaths []`, `unreadable []` | **CONFIRMED** |
| ⭐ the armed row's verdict on the ARCHIVED B6 300-year receipt | **EXECUTED, SILENT.** 110 of 120 transitions moved over the customer horizon (**0.9167**); at year 30 exactly 3 of 4 (**0.75**); across all 300 years 1079 of 1200 (**0.8992**); **0** yearly rows at zero transitions, **0** rows below the 0.05 bar | **CONFIRMED — reproduces the C3/§907 figures exactly** |
| the armed row's verdict on the FRESH LIT 30-year receipt | **EXECUTED, SILENT.** 110 of 120 (**0.9167**) | **CONFIRMED** |
| the armed row on the FRESH DARK 30-year receipt | **NOT APPLICABLE** (gate false). Its share would have been 84 of 120 (**0.70**), above the bar — so the gate costs no finding, only a claim the row is not entitled to make | **CONFIRMED** |
| the zero-denominator hole | closed as a FINDING, not a silence. Measured unreachable on an honest run: 4 transitions per year, **0** rows at zero across 300 years and across 30 | **CONFIRMED** |
| `npm run typecheck:ratchet` at the composed tip | `OK — no type regressions (**173** error(s), ceiling **173**)`, `EXIT=0` | **CONFIRMED — at the ceiling, no regression** |
| final suite sweep at `d58b489b8`, each file run alone | tripwireRegistry **11/11**, tripwireCaller **10/10**, soakRegister **8/8**, soakInvariants **16/16**, soakScriptSeams **8/8**, curveBandFreeze **4/4**, demographicsEnvelope **6/6**, demographicsCure **4/4**, demographicsFloor **32/32**, engineTelemetryWall.walker **8/8**, realmScaleCertification **10/10**, simMetricEmitter **7/7**, behavioralCertificationContract **39/39** — every `EXIT=0` | **CONFIRMED** |
| eslint on all six car-3 files | `EXIT=0` | **CONFIRMED** |

### CAR 4 — the measurements (Mon Sep  7 04:05:52 -> 04:11:59 EDT 2026)
| what | measured | verdict |
|---|---|---|
| the guard's reading BEFORE, re-derived in the dock at `d58b489b8` | the file's own scan: **7** un-anchored sites, at lines **471, 472, 518, 519, 590, 723, 724** — the chair's seven exactly | **CONFIRMED — the finding reproduced, not taken on trust** |
| the ceiling this file is held to | `grep tripwireRegistry` over the walker returns **nothing**: no row in `FROZEN_UNANCHORED_NEGATIVES`, none in `READMITTED_GENERATION_FACING`, so `ceilingFor` answers **0**. `tests/soak-harness` holds no row at all | **CONFIRMED — an exact zero, and the walker was right** |
| :471/:472 anchors, measured before choosing | `receiptWriterFields(legacyWriter)` = **41 keys**; `finalPopulations` present, `finalDiedFlags` present; `yearlyPopulations` absent, `yearlyDiedFlags` absent | **CONFIRMED — each anchor is the excluded key's own FINAL-state sibling, same object literal, same read** |
| :518/:519 anchors, measured before choosing | `nestedWriterFields(legacyObserver)` (BOTH blinded) = **78 keys**; `realmSelfSufficiency` present, `motionRenamedForControl` present; `realmDemography` absent, `motion` absent. Live observer = **79 keys** | **CONFIRMED** |
| ⭐ why `realmSelfSufficiency` and not any sibling | it ships at `behavioral-observation.mjs:1058`, ONE LINE ABOVE `realmDemography` at `:1059`, in the same literal and through the same **conditional shorthand spread** branch of `literalKeysFrom` (`tripwires.mjs:671`) — the exact branch car 2 measured blind. A scanner that went blind to spreads again reds on the anchor instead of reporting a deletion it never made | **CONFIRMED — the anchor catches the one drift that could make the absence meaningless** |
| ⭐⭐ `motionRenamedForControl` closes a hole the control already had | the `motion` blinding is a **rename**, and a rename LENGTHENS the source, so `legacyObserver.length < OBSERVER_SOURCE.length` (`:517`) is satisfied by the DELETION alone. A dead rename regex would have left `motion` present and the negative silently false. The renamed twin is the only reading that proves the rename matched | **CONFIRMED — a real strengthening, not compliance** |
| :590's anchor, and the candidate refused | `realmSelfSufficiency` again, for the spread-branch reason. **`motion` was refused**: it is a plain `key: {`, read by a different scanner branch, and would survive the exact drift the anchor exists to catch | **CONFIRMED — declared, see R15** |
| :723/:724 — the helper's contract admits a STRING | `assertContainable` accepts `typeof subject === 'string'` (`anchoredNegatives.js:51`), so **no `// anchored:` marker was needed anywhere**; the helper form is used at all seven sites | **CONFIRMED — the brief's fallback was not reached** |
| :723/:724 anchors, measured before choosing | `MOTION_FLOOR_01` present in `codeOnly(OBSERVER_SOURCE)` (48893 -> 42504 chars) **and** in `codeOnly(ENVELOPE_SUITE_SOURCE)` (14226 -> 11795) — live code, not prose, so the comment-stripper keeps it; `0.0025` absent from both, raw and stripped | **CONFIRMED — it is the identifier that REPLACED the literal in car 3, so it travels the same path** |
| ⭐⭐ NEGATIVE CONTROL — an anchor that cannot fail anchors nothing | each of the seven anchors broken in turn (sibling renamed to a non-existent key), file re-run each time: **7 of 7 exit 1, all seven carrying the helper's `LIVENESS ANCHOR` message**. File then restored from the pre-probe copy, `diff` **clean**, and both gates re-run at the restored bytes | **CONFIRMED — `$SC/soak909/car4-anchor-negative-control.log`** |
| the guard's reading AFTER | this file **7 -> 0**; **no** `tests/soak-harness/` file offends at all; 504 files offend corpus-wide, every one at or under its frozen ceiling (the arm is green) | **CONFIRMED** |
| `tests/soak-harness/tripwireRegistry.test.js`, shared tier, exit captured in-shell | `EXIT=0`, Test Files 1 passed (1), **Tests 11 passed (11)** | **CONFIRMED** |
| `tests/lint/negativeAssertionAnchor.walker.test.js`, whole corpus, shared tier | `EXIT=0`, Test Files 1 passed (1), **Tests 9 passed (9)** — up from `1 failed / 8 passed (9)` at the chair's proof | **CONFIRMED** |
| eslint on the one file | `EXIT=0`, **zero bytes** of output | **CONFIRMED** |
| ⛔ TITLE COUNTS — the lighting census must not move | `it()`/`test()` **11 before, 11 after**; `describe()` **1 before, 1 after**; total **12 -> 12**, and the twelve title LINES are byte-identical under `diff`. Anchoring added assertions, never an arm | **CONFIRMED — the predicted 23664 is undisturbed by this car** |
| `npm run typecheck:ratchet` at the CAR-4 tip | `OK — no type regressions (**173** error(s), ceiling **173**)`, `EXIT=0` — unmoved by the car | **CONFIRMED** |
| the committed bytes are the proved bytes | `git show HEAD:tests/soak-harness/tripwireRegistry.test.js` vs the copy the gates ran on: `diff` **clean** | **CONFIRMED** |
| the consist and the tree | `6ebe0ef3b` -> `ec265d24c` -> `268d53605` -> `d58b489b8` -> **`68c3a1618`**; one file in the commit; porcelain **0**; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: SOAK-HONEST-909` present | **CONFIRMED** |

### CAR 5 — the measurements (Mon Sep  7 04:26:52 -> 04:52:42 EDT 2026)
**Every figure in the chair's charter is a skeptic's measurement at `d58b489b8`; every one below is RE-DERIVED at `68c3a1618` before the edit, and re-measured at `864e76be9` after it.**

| what | measured | verdict |
|---|---|---|
| ⛔⛔ **DEFECT 1 REPRODUCED FIRST** — an EMPTY top-level series grades as CARRIED | the skeptic's own probe `$SC/skeptic-909/empty.mjs` re-run at `68c3a1618`: a lit receipt with `yearlyPopulations: []`, `yearlyDiedFlags: []` and a motion + realm reading answers **`notExecutable []`, `findings []`** — both series rows EXECUTABLE, both taking their own `pops.length < 150` early return, `fullInstrument` reading TRUE over two rows that could not observe | **CONFIRMED — the finding reproduced, not taken on trust** |
| …and CURED, same probe, same command | at `864e76be9` the same receipt answers **`notExecutable ['capacity_plateau','capacity_floor_thaw']`**, reason `requires receipt.yearlyPopulations[last], receipt.yearlyDiedFlags[last] — absent from this receipt`; `findings []`; the detectors are never called | **CONFIRMED** |
| the UNREADABLE-DETECTOR CONTROL (defect 2) — before | a planted row with a computed `receipt[…]` detector AND `requires: ['noSuchSection']`, graded against a two-line writer: `unreadable` names it, **`unreachablePaths` is `[]`** — the requirement was never graded, while the walker's header claimed it was | **CONFIRMED — reproduced at `68c3a1618`** |
| …the same control, after | `unreadable` = `[{opaque-and-blind, a computed …}]` **AND** `unreachablePaths` = `[{opaque-and-blind, noSuchSection, noSuchSection}]`; `unreachable` stays `[]` — the READ channel is still refused rather than graded on a partial key set | **CONFIRMED — both channels, curing one did not open the other** |
| ⭐ **"CARRIED" IS THE SHAPE THE ROW READS** — the three readings the charter names | `realmDemography = 42` → `capacity_realm_load` **NOT-EXECUTABLE** (was a silent clean); `realmDemography = {}` → **NOT-EXECUTABLE** (was a *non-finite FINDING* against a world whose instrument wrote nothing — a declared movement, see R17); `yearlyPopulations = 42` → both series rows **NOT-EXECUTABLE** (was a silent clean) | **CONFIRMED, executed** |
| the shape vocabulary is CLOSED and convicted | `tripwireRegistryDefects()` = **`[]`** on the live registry; `behavioral.yearly[last].realmDemography[]` and `yearlyPopulations{last}` each convict with 1 defect; `yearlyPopulations`, `yearlyPopulations[last]` and `behavioral.yearly[last].realmDemography{}` do not | **CONFIRMED — a rule, not a wall** |
| the LIVE walker at the car-5 tip | `written` **43** keys, `unreachable []`, `unreachablePaths []`, `unreadable []` — unmoved by the horizon reader being scanned (it reads `yearlyPopulations` and `behavioral`, which both rows' detectors already read) | **CONFIRMED — the car-1 ratchet at zero is untouched** |
| ⭐⭐ **THE HORIZON RULING, ON THE 30-YEAR LIT RECEIPT** (`$SC/soak909/fresh-30y-4s-lit.json`, through the real CLI) | `horizon  w0-soak · capacity_plateau — horizon: 30 years observed, 150 required`; `horizon  w0-soak · capacity_floor_thaw — horizon: 30 years observed, 100 required`; **`capacity_envelope_30y` is ABSENT — 30 observed is exactly its bar**; `deterministicFirings 1 · fullInstrument true`, eval `EXIT=1` on the pre-existing `capacity_realm_load` firing at 0.5449 | **CONFIRMED — exactly the chair's prediction** |
| the 29-ROW receipt | all **THREE** rows report: `horizon: 29 years observed, 150 required` · `…, 100 required` · `…, 30 required`; `findings []`; `notExecutable []` | **CONFIRMED** |
| the ARCHIVED 300-YEAR receipt | **NONE.** `capacity_envelope_30y` is long enough (300 ≥ 30); the two series rows never reach the horizon check because `requires` refuses them first — which is the ORDER the evaluator asserts: a row that could not run has nothing to say about how long the run was. `deterministicFirings 1 · fullInstrument false`, unchanged | **CONFIRMED** |
| ⛔ **THE 33-DOCUMENT GRADE CENSUS** (`$SC/soak909/car5/census.mjs`, the critic's `grade.mjs` widened to print all three channels; run over the **32** non-fixture `whole_world_soak` receipts under `$SC` **plus** `tests/fixtures/simSoakReceiptFixture.json`; the 14 other copies of that fixture are ONE md5) | **`deterministicFirings` IDENTICAL on all 33. `notExecutable` IDENTICAL on all 33 ⇒ `fullInstrument` unchanged on every receipt.** Exactly **4** documents move, all four the lit 30-year receipts (`fresh-30y-4s-lit`, `fresh-30y-4s-lit-AFTER-car3`, and the two annotated copies), each gaining exactly `capacity_plateau` + `capacity_floor_thaw` in the horizon channel. **NO real receipt carries an empty series, a `42` reading or an empty-object reading, so defect 1's cure moves no real grade** — the charter's prediction, measured rather than assumed | **CONFIRMED — `$SC/soak909/car5/census-BEFORE.txt` vs `census-AFTER.txt`** |
| the census SET, re-derived | `grep -rl whole_world_soak --include='*.json' $SC` = **46** paths; minus the 14 byte-identical `simSoakReceiptFixture.json` copies = **32**, in four directories (1 archived · 21 §905 L-PROBE · 10 this lane's own); + the repo fixture = **33 documents** | **CONFIRMED — the critic's 32 reproduced** |
| ⭐ THE ORDERED TWIN GUARD | `soakInvariants.test.js` gains ONE arm asserting `CAMPAIGN_HORIZON_YEARS === CERTIFICATION_HORIZONS.useful.years` (**30 === 30**), proving the twin is a real reading on both sides (not an `undefined` equality), and pinning all three lifted bars as controls read from the module (30, 0.05, 0.0025) plus the (0,1] share bound. The import reaches `src/domain/certification/behavioralContract.js` — precedent: `soakScriptSeams.test.js` already reaches into `src/` from this directory | **CONFIRMED — 17/17, EXIT=0** |
| the "lift REMOVES a spelling" claim, re-counted | **2 definitions before, 2 after** — MOVED, not removed. `demographicsEnvelope.test.js:57` is now `const CAMPAIGN_YEARS = CAMPAIGN_HORIZON_YEARS;`, an ALIAS: what the lift bought is that the SUITE stopped typing the figure | **CONFIRMED — the critic's count reproduced; the comment corrected in the tree** |
| ⭐ THE FIXTURE'S YEAR 1, and the observer's actual mechanism | the observer compares each year's OWN before-saves and after-saves (`before = settlementMap(beforeSaves)` / `after = settlementMap(afterSaves)`, then `if (!previous) continue` — which skips a settlement absent from the BEFORE map, i.e. one founded mid-year, **not a first year**). So year 1 is not exempt. Measured: `behavioral.yearly[0].year = 1` carries `populationTransitions: 4` on the fresh 30-year lit receipt AND on the archived 300-year one, and **0 rows at zero transitions in 30 years and in 300**. The fixture now derives year 1 the observer's way: its before-state is its own year-1 head count, so 4 transitions and 0 moved — the conservative reading, which can only lower the moved share | **CONFIRMED** |
| …and the fixture correction moves NO verdict | `capacity_envelope_30y` over the register fixture's first 30 years: **56 of 116 (0.4828) → 56 of 120 (0.4667)** on the plateau and runaway shapes, **27 of 116 (0.2328) → 27 of 120 (0.2250)** on the floored shape — every one an order of magnitude above the 0.05 bar, silent before and after. `deriveRegisterFigures` reads no `motion` at all | **CONFIRMED — soakRegister 8/8** |
| the TYPED `motion` plants (critic G7), declared | car 3 planted `motion: { populationTransitions: 4, populationMoved: 4 }` TYPED into `tripwireRegistry.test.js`'s passing corner and into the `planted` fixture. **⛔ REFUSAL WITH A MEASUREMENT: no derivation exists to replace them.** Both plants carry a ONE-row `behavioral.yearly` envelope against a 201-row population series — the two are not aligned, so computing motion from the fixture's own numbers would require inventing an alignment the writer does not produce. Both are now DECLARED in the source with what the figures stand for (4 of 4 = share 1.0, twenty times the bar: a reading the row can execute on, never a verdict it could not have reached) | **DECLARED — see R21** |
| the stale counts inside the file car 3 edited | "what ALL THREE require" → **FOUR** rows declare `requires` since car 3 (executed: `notExecutable` lists four on a bare lit receipt); the passing corner's "every capacity row RAN on it: the third channel is empty, so this `[]` is a measurement" is **re-cut** — `notExecutable []` AND `capacity_envelope_30y` named in the horizon channel at `1 years observed, 30 required`; "THREE SINCE §909" re-cut to say what it measures (three of the four rows are blind ON THAT PLANT) | **CONFIRMED — corrected in the tree** |
| ⭐ **THE CITE-BY-SYMBOL SWEEP, with each cite MEASURED before it was moved** | `tripwires.mjs`: `:1054` ×3 (the conditional spread is at **1059**) · `whole-world-soak.mjs:1050-1052` for `rules: runA.simulationRules` (**1128**) · and THREE INHERITED `home:` cites this car measured stale for the first time — the remnant law cited `:559` and `everyAlive` is at **:872** (**313 lines**), the byte ceiling cited `:578-582` and is at **:941**, the wall trend cited `:590` and is at **:951**. `whole-world-soak.mjs`: `tripwires.mjs:314,:355` (the two `requires` are at **357/399**, and the cite was ALREADY WRONG at the commit that shipped it — 319/361 there) · `behavioral-observation.mjs:1202` (**1207/1208**). `soakInvariants.mjs`: `behavioral-observation.mjs:957` (**962**). Every one replaced by its SYMBOL | **CONFIRMED — seven citations, each re-derived, none guessed** |
| "five pinned fixtures" re-derived | **17** files pin `schemaVersion: 5` across `tests`, `src`, `scripts` — 16 in the JS form, 1 (`tests/fixtures/simSoakReceiptFixture.json`) in the JSON form. The skeptic's 17 reproduced exactly; the shipped figure of five was inherited and never re-derived | **CONFIRMED — the count now in the source** |
| the M1-F1 narrative in the writer's header | re-written to describe the defect AT `4243bdc61` (where both rows declared NO `requires` and the silence was a silent clean) and to say so — CAP-TRIP-907 had already turned that silence into a positive refusal before this line landed, so the un-anchored narrative described a path the code could not take at its own parent | **CONFIRMED — corrected** |
| the misplaced JSDoc block | the block documenting `receiptWriterFields` headed `literalKeysFrom` (car 2 inserted the helper between them). Moved back to its function; `literalKeysFrom` given its own | **CONFIRMED** |
| ⛔ NO VALUE MOVED | 150, 100, 30, 0.05, 0.0025, 900_000 and the 800 MB band all keep their exact values. The two capacity horizons are hoisted to ONE spelling **inside `tripwires.mjs`**, where the band prose and the detector had spelled each twice — the file's own header law. This collapses no cross-file spelling and does not claim to | **CONFIRMED — no tuning move, no golden, no register `--write`, no signature touched** |
| focused suites, EACH RUN ALONE, exit captured in-shell, EACH COUNT READ FROM ITS OWN LOG BY NAME (the R8 law, obeyed) | tripwireRegistry **11/11** · soakInvariants **17/17** · soakRegister **8/8** · tripwireCaller **10/10** · soakScriptSeams **8/8** · demographicsEnvelope **6/6** · curveBandFreeze **4/4** · demographicsCure **4/4** · realmScaleCertification **10/10** · determinismUnderWorkers **4/4** · centuryLegSoak **9/9** — every `EXIT=0`. The last two are there because they are the remaining importers of the six modules this car edited (`grep -rln` over `tests/`) | **CONFIRMED — `$SC/soak909/car5/car5-*.log`** |
| the negative-assertion walker, file alone, at the end | `EXIT=0`, Test Files 1 passed (1), **Tests 9 passed (9)** — zero NEW un-anchored negatives from this car's test bytes | **CONFIRMED** |
| eslint on all nine touched files | `EXIT=0`, **zero bytes** of output | **CONFIRMED** |
| `npm run typecheck:ratchet` at the car-5 tip | `OK — no type regressions (**173** error(s), ceiling **173**)`, `EXIT=0` | **CONFIRMED — at the ceiling, no regression** |
| ⛔ TITLE COUNTS, before and after, per touched test file | `tripwireRegistry.test.js` **11 it + 1 describe = 12 → 12** · `soakRegister.test.js` **8 + 1 = 9 → 9** · `soakInvariants.test.js` **16 + 2 = 18 → 19** (+1: the ordered twin guard is ONE new arm, as the charter ordered). **THE CENSUS MOVES 23664 → 23665 — the chair must re-predict** | **CONFIRMED — declared, not smuggled** |
| `$SC/HOLD-VITEST` | checked before **every** vitest invocation; absent throughout (04:26:52, 04:45:58, 04:47, 04:48, 04:49, 04:51) | **CONFIRMED** |
| the consist and the tree | `6ebe0ef3b` → `ec265d24c` → `268d53605` → `d58b489b8` → `68c3a1618` → **`864e76be9`**; 9 files, +533/−116; porcelain **0**; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: SOAK-HONEST-909` present; staged by explicit path with `--only`; no push, no stash, no `git add -A`/`-u`/`.` | **CONFIRMED** |

**ONE COMMIT, AND WHY NOT TWO.** The charter allowed splitting the comment-only work. It is NOT split: every comment corrected here is false in the same way the two code defects are false — a shipped statement the tree refutes — and two of them are promises the code half keeps (the `soakInvariants.mjs` header now says the twin is guarded, and the guard ships in the same car; the `receiptCarries` header now states the emptiness law it previously claimed). Splitting would have put a rationale in one commit and its subject in another.

### THE FINAL GRADE CENSUS at `864e76be9` (the three headline receipts; the full 33-document census is CAR 5's row and `$SC/soak909/car5/census-AFTER.txt`)
⛔ **`deterministicFirings` and `notExecutable` — and therefore `fullInstrument` — are IDENTICAL on all 33 documents before and after car 5.** The only column that moves is the horizon channel, on exactly the four lit 30-year receipts.

| receipt | fullInstrument | notExecutable | findings | horizon (car 5) | the four capacity rows |
|---|---|---|---|---|---|
| ARCHIVED 300y LIT `research-lit-4s-300y-4s-seed1` | **false** (unmoved) | `capacity_plateau`, `capacity_floor_thaw` (both: `requires receipt.yearlyPopulations[last], receipt.yearlyDiedFlags[last] — absent` — the reason string gains the declared shape at car 5) | `capacity_realm_load` @ 0.4787 (unmoved) | **none** — 300 years is long enough for the envelope row, and the two series rows never reach the horizon check because `requires` refuses them first | plateau NOT-EXEC · floor_thaw NOT-EXEC · realm_load EXECUTED (fires) · envelope_30y EXECUTED, silent |
| FRESH 30y DARK | **true** (unmoved) | `[]` | none | **none** — the gate is read first, so a dark cell says nothing about horizons either | all four NOT APPLICABLE (gate false) |
| FRESH 30y LIT (post-car-3 re-run) | **true** (unmoved) | `[]` | `capacity_realm_load` @ 0.5449 (unmoved) | ⭐ **`capacity_plateau` — `horizon: 30 years observed, 150 required`; `capacity_floor_thaw` — `horizon: 30 years observed, 100 required`. `capacity_envelope_30y` ABSENT: 30 observed IS its bar** | all four EXECUTED; plateau and floor_thaw now say COMPLETE BUT INCONCLUSIVE instead of a silent `[]`, envelope_30y silent at 0.9167, realm_load fires |

## RETROVALIDATION ROW

> **⭐⭐ THE CHAIR'S RULINGS ON R1–R16, recorded here as the car-5 charter directs (the §910 ledger row carries them too).** **RATIFIED:** R1, R2, **R3 (named the lane's best act)**, R4, R6, R7, **R9 (strictly stronger than the equality it replaced)**, R10, R12, R14, **R15 (the strongest verification act in the consist)**, R16. **RATIFIED WITH AN ORDERED GUARD:** the `CAMPAIGN_HORIZON_YEARS` lift — the guard ships in car 5 (**R18**). **RATIFIED, REVERSIBLE, THE COST ON THE RECORD:** R11 (`capacity_envelope_30y` gated on `demographicsEnabled`) — a category error otherwise; the chair keeps the wider net as an owner-visible one-line option. **RATIFIED AS TO ITS TWO CORRECTIONS, ITS CLASS RE-CURED BY CAR 5's CITE-BY-SYMBOL LAW:** R5. **RATIFIED, ITS CLASS CLOSED BY READING EACH COUNT FROM ITS OWN LOG BY NAME** — which car 5's proof section obeys: R8. **REOPENED AND CURED BY CAR 5:** the empty-array asymmetry (critic J2 = defect 1), the census sentence (J3), the missing deferral ledger (J4), and R13's year-1 claim (**R19**).

| # | call | ground | who owns it |
|---|---|---|---|
| R1 | **The `schemaVersion` 5 → 6 bump is WITHDRAWN; the series ships ADDITIVE on v5.** | **CHAIR RULING** (addendum §2), on the predecessor's measurement, re-verified here: `SOAK_RECEIPT_SCHEMA_VERSION` has ONE home — `src/domain/certification/behavioralContract.js:117` — inside the certification contract and outside this lane's charter; `SUPPORTED_SOAK_RECEIPT_SCHEMA_VERSIONS = [4, 5]` at `:125` and re-declared at `scripts/telemetry/simMetricEmitter.mjs:50`; three further stamps share the constant (`whole-world-soak.mjs:519` checkpoint identity, `:644` restore probe, `:1017` the receipt) plus `behavioral-observation.mjs:1202`; five fixtures pin 5. The constant's own header documents the additive-v5 precedent (WR-9's `warConvergence`, "no existing field changes meaning"). A pre-series receipt is told apart POSITIVELY by the `requires` channel — **proved above**: the archived receipt names both absent fields on its face. | **CHAIR ruling — the owner may veto.** Not a lane refusal. |
| R2 | **The one-line schema note went in as the chair directed**, above `schemaVersion:` in the writer (`whole-world-soak.mjs:1017-1020`, above `schemaVersion:` at `:1021`), naming §907 M1-F1 as the cause and WR-9's `warConvergence` as the additive-v5 form. No version integer, no fixture, no `SUPPORTED_…` edit. | addendum §2. | lane, under the ruling |
| R3 | **⛔ REFUSAL WITH A MEASUREMENT — a DEFAULT 30-year soak does not prove what the acceptance line asks.** The addendum requires `capacity_plateau`/`capacity_floor_thaw` **EXECUTED** on the fresh run. Measured on the default `--years 30 --settlements 4` receipt: `subsystems.rules.demographicsEnabled` = **false** (the flag ships dark — `simulationRules.js:1027`, a VIRTUAL flag declared FALSE by wave P), so both rows are skipped by their `gate` and never reach the `requires` check. `notExecutable: []` there is a GATE fact, not an instrument fact. So a **second, LIT** 30-year run was taken (`--lighting demographicsEnabled=true` — a run-scoped overlay, exactly how `research-lit-4s` produced the archived receipt; `realm-scale-certification.mjs:97-112`), and it is that run which shows both rows executing. Both figures are reported above; the default run's `fullInstrument: true` / `notExecutable: []` stand, with their real cause named. | measured, this lane, 03:08–03:12. | lane |
| R4 | **⛔ REFUSAL — the "~80 s" figure in the addendum is wrong.** Measured wall clock: default 30 y × 4 s = **2 m 34 s** (03:06:02 → 03:08:36); lit 30 y × 4 s = **2 m 14 s** (03:10:06 → 03:12:20). Both include run A + byte-identical replay + divergence run + the isolated worker. | `date` either side of the `gate-mutex --run`. | lane |
| R5 | **Two inherited comment defects corrected against measurement before the commit.** (a) The predecessor's block cited `behavioral-observation.mjs:1222`; the `soak_subsystem_configuration` stamp is at **:1202** (a 20-line error) — corrected, and `:644` added since it shares the constant too. (b) The block called the bump a lane REFUSAL; per addendum §2 it is recorded as the **chair's withdrawal on the lane's measurement**. The stale sentence "The exact diff is handed to the chair" was removed — nothing is handed; the bump is withdrawn. | `grep -n SOAK_RECEIPT_SCHEMA_VERSION` across `scripts src tests docs`. | lane |
| R6 | **The two capacity rows' own headers in `tripwires.mjs` were updated in the same commit.** They asserted a LIVE blindness ("no written receipt has ever carried `yearlyPopulations`… until it lands the row is NOT-EXECUTABLE") which car 1 makes false. A source comment that claims a cured defect is still live is the same false-report class this lane exists to close. `requires` is UNCHANGED on both rows, and the new text says why it stays: every receipt already on disk still lacks both fields. | the honesty program; in-charter (`scripts/soak/*`). | lane |
| R7 | **No register `--write`, no tuning value moved, no golden re-recorded, no signature touched, no push, no stash, no `git add -A`/`-u`/`.`.** Staged by explicit path; porcelain 0 after the commit. | STATE NEVER FATE. | lane |
| R8 | **⚠ A FIGURE THIS LANE FIRST WROTE WAS FALSE, CAUGHT AND CORRECTED.** Car 1's six suite counts were recorded from `grep -h 'Tests  ' $SC/soak909/*.log`, which prints in ALPHABETICAL filename order, not the loop's order — so five of six were attributed to the wrong file (tripwireCaller was written 4/4 and is 10/10; curveBandFreeze 10/10 and is 4/4). Every count is now read from its own log by name. The estate's "a verdict keyed by POSITION breaks when the list is reordered" hazard, live, in a receipt. | re-read per file, 03:25. | lane |
| R9 | **The reachability identity was RESTATED rather than deleted.** `unreachable rows == rows declaring requires` was a coincidence of a single writer; `capacity_realm_load` declares a requirement it can reach, so the equality now fails harmlessly. Kept as the subset law it was protecting (whatever the walker convicts announced its dependency) plus a stronger totality control that blinds BOTH writers and recovers the full roster. Weakening it to nothing was the alternative and was refused. | measured, car 2. | lane |
| R10 | **A synthetic fixture was corrected to the writer's shape, and the correction is DECLARED because it is the class this lane exists to refuse.** `researchReceipt()` claimed `lit` while carrying no `realmDemography` — the estate's "a fixture can be the only writer of the SHAPE" hazard inverted: here the fixture was the only writer of a shape the writer CANNOT produce, and it made the row's silence look like a pass. The reading planted sits INSIDE the row's band on both clauses, so it adds an instrument and not a green; `realmReading: false` keeps the refusal provable; no register figure moves. | measured, car 2. | lane |
| R11 | **`capacity_envelope_30y` is GATED on `demographicsEnabled` like its three siblings — a judgment, and not a symmetry one.** Its bar's derivation home runs the DEMOGRAPHIC kernel, so applying it to a world whose head counts move by `populationDynamics`' raw proportional growth would be a category error dressed as a wider net. The cost is measured and small: the dark 30-year run's share is 0.70, well above the bar, so today the gate suppresses no finding. Ungating it is a one-line, reversible act if the chair wants the wider net. | measured, car 3. | lane — reversible, flagged for the chair |
| R12 | **A ZERO DENOMINATOR IS A FINDING in the new row, not a silence.** `moved / 0` would have answered `[]` — this lane's own defect class arriving through an arithmetic guard. It is named instead. The risk of a false red was weighed against it and measured away: the observer counts one transition per settlement per year with a previous-year record, 4 per year on both real receipts, 0 rows at zero in 300 years and in 30. | measured, car 3. | lane |
| R13 | **The register fixture gained a DERIVED `motion` block, on the same ground as R10.** Every receipt since `37459391a` (2026-07-28) carries it, so a fixture without it was again a shape the writer cannot produce. It is computed from the fixture's OWN series at the observer's own floor — nothing typed. ⛔ **BUT ITS YEAR-1 CLAIM WAS FALSE AND IS CURED AT CAR 5 (R19): "year 1 carries zero transitions exactly as the observer produces" is refuted by every real receipt** — `behavioral.yearly[0].year === 1` carries `populationTransitions: 4` on all three, and ZERO rows sit at zero in 30 years or in 300. `motionReading: false` keeps the mint door's refusal provable. | measured, car 3; **corrected by measurement, car 5**. | lane |
| R14 | **The `instrumentComplete` helper was KEPT, against its own note.** Its comment said it "becomes the identity function and should be deleted" once the series shipped. The writer ships them now, but `researchReceipt()` is synthetic and deliberately does not, because the arm below it needs a pre-§909-shaped receipt to prove the mint door refuses a blind instrument. Deleting the helper would have left that arm with no subject. The note is corrected in place. | measured, car 2. | lane |
| R15 | **⛔ THE GUARD FIRED AND WAS READ — the seven bare negatives cars 1-3 wrote are anchored, and every anchor was chosen by MEASUREMENT and then broken to prove it can fail.** The chair's whole-suite proof found the red; this lane re-derived it in the dock before touching anything (7 sites, the same seven lines) rather than trusting the report. What each anchor PROVES: **:471/:472** — `finalPopulations`/`finalDiedFlags` are the excluded keys' own FINAL-state siblings in the same writer literal, so a `receiptWriterFields` read that returned NOTHING now reds instead of certifying a deletion (the existing `legacyWriter.length <` check catches a replace that matched nothing; it could not catch an empty read). **:518/:590** — `realmSelfSufficiency` ships one line above `realmDemography` through the SAME conditional-shorthand-spread scanner branch car 2 found blind, so the one drift that would make the absence meaningless reds on the anchor; **`motion` was offered as :590's anchor and refused**, because it is a plain `key: {`, read by a different branch, and would survive that exact drift. **:519** — `motionRenamedForControl` is the renamed twin, and it closes a hole the control already had: the rename LENGTHENS the source, so the length check was satisfied by the deletion alone and a dead rename regex would have left `motion` present and the negative silently false. **:723/:724** — `MOTION_FLOOR_01` is the identifier that REPLACED the literal in car 3's lift and survives `codeOnly` in both sources, so "no `0.0025` here" can no longer be true of a file that went missing or a stripper that ate the code with the comments. **No `// anchored:` marker was used at any site**: the helper's `assertContainable` admits a string collection, so the two string sites took the helper too and the brief's marker fallback was never reached. **No assertion was weakened or deleted** — all seven negatives survive inside the helper, each now preceded by a positive that must hold first. | measured, this lane, 04:05-04:11; negative control 7/7 red with the `LIVENESS ANCHOR` message, restored `diff`-clean. | lane |
| R16 | **⛔ REFUSAL WITH A MEASUREMENT — the dispatched gate command was refused by the mutex itself.** `GATE_MUTEX_TIER=shared sh scripts/gate-mutex.sh --run -- npx vitest run ...` as written exits **2** with `gate-mutex: SHARED tier REFUSED — the command declares no worker cap.` The shared tier admits a run concurrently only because it cannot saturate the box (`gate-mutex.sh:34-36`; `SHARED_MAX_WORKERS` defaults to 2 at `:118`), so `--maxWorkers=2` was added and **every** run reported for car 4 carries it. No tier was downgraded to exclusive and no gate was skipped. | `gate-mutex.sh` read, then re-run; exit captured in-shell. | lane |
| R17 | **⚠ ONE DECLARED GRADE MOVEMENT ON A SHAPE NO REAL RECEIPT HAS: `realmDemography = {}` moves from a FINDING to NOT-EXECUTABLE.** An empty object used to reach `capacity_realm_load`'s detector, read `Number(undefined)`, and mint `realm load non-finite outside the plateau window` — a *finding* against a world whose instrument had written nothing. Under the `{}` shape it is an absence. NOT-EXECUTABLE is the honest channel (both are non-silent; only one is true), and `deterministicFirings` therefore drops 1 → 0 on such a receipt. Measured over all 33 documents on disk: **none has this shape**, so nothing real moves. Declared because a movement nobody can see is exactly what this lane refuses. | measured, car 5. | lane |
| R18 | **The CHAIR RATIFIED car 3's third, uncharterd constant lift ON CONDITION of a guard, and the guard ships here.** `CAMPAIGN_HORIZON_YEARS` was lifted beyond the brief's two, creating a stated-but-unguarded twin of `CERTIFICATION_HORIZONS.useful.years`; nothing in the estate reddened when they drifted. `soakInvariants.test.js` now asserts the equality (30 === 30), proves the twin is a real reading on both sides so the equality cannot go vacuous, and pins all three lifted bars as controls. The commit's "the lift REMOVES a spelling" is corrected by count in the tree: 2 before, 2 after — MOVED. | chair ruling (car-5 charter), implemented and measured. | **CHAIR ruling — the owner may veto.** |
| R19 | **⛔ A FIXTURE CLAIM THIS LANE SHIPPED WAS FALSE AGAINST EVERY REAL RECEIPT, AND THE MECHANISM WAS MIS-READ.** Car 3's register fixture derived `motion` from a *previous-YEAR* record and said year 1 "carries zero transitions — exactly what the observer produces". The observer compares each year's OWN before- and after-saves; its `if (!previous) continue` skips a settlement absent from the BEFORE map (founded mid-year), not a first year. Measured: `yearly[0].year === 1` carries 4 transitions on all three receipts, 0 rows at zero in 30 and in 300. Corrected to the observer's derivation; no verdict moves (0.4828 → 0.4667, still twenty times the bar). **This is the estate's "a fixture can be the only writer of the SHAPE" hazard inverted — the fixture was writing a shape the writer cannot produce, in a lane whose whole subject is that class.** | measured, car 5. | lane |
| R20 | **THE HORIZON RULING'S MECHANISM NEEDED ONE THING THE RULING DID NOT NAME, AND IT IS DECLARED RATHER THAN SMUGGLED.** The chair ruled the horizon-short rows report through "the registry's third channel — `observability`". That channel was keyed on the row's CLASS (host-observability firings only) and its printer labelled every entry `(host-observability; never a finding)`. Reporting a DETERMINISTIC row there under that label would have been the false-report class arriving in the one place a reader looks. So each entry carries `inconclusive: true`, and the three readers that name the channel (`evaluate-receipt.mjs`'s printer and summary, `run.mjs`'s console line, `evaluate.mjs`'s contract) say what it now holds. **The ruling stands; its mechanism was measured, per "charter the permission, measure the mechanism".** A fourth channel was the alternative and was refused: it would have moved the shape of `run.mjs`, `rolling.mjs`, `ladder.mjs`, `summarizeEvaluations` and their pins to say something the flag says in one field. | measured, car 5. | lane — under the chair's ruling |
| R21 | **⛔ REFUSAL WITH A MEASUREMENT — the TYPED `motion` plants cannot be derived, so they are declared instead.** The charter asked for a derivation "if a derivation exists". Both plants sit in fixtures whose `behavioral.yearly` is ONE row while their population series is 201 rows: the two are not aligned, and manufacturing an alignment would be the fixture asserting a shape the writer does not produce — R19's own defect, committed on purpose. Both plants now state in the source that they are typed, why no derivation is available, and what the figures stand for. | measured, car 5. | lane |
| R22 | **THE SHAPE IS DECLARED IN THE PATH AND NEVER INFERRED FROM THE DETECTOR — a judgment, and the alternative is named.** The cheaper cure was a blanket law: refuse every primitive at a terminal segment. It closes the `42` case in one line and buys a false report — a future row keyed on a scalar (`settlements`, whose `Number(…) || 0` guard is the identical weak zero) would be permanently NOT-EXECUTABLE and read exactly like an honest instrument gap. Inferring the shape by text-scanning the detector was refused for the same reason the walker refuses to resolve a call graph: a mechanism that lies. The vocabulary is CLOSED (`[last]`, `{}`), convicted by `tripwireRegistryDefects`, and a bare segment still means "present and not empty". | measured, car 5. | lane |
| R23 | **FOUR INHERITED `home:`/comment CITATIONS WERE MEASURED STALE AND MOVED TO SYMBOLS; THE REST ARE A DECLARED DEFERRAL.** The charter's cite-by-symbol law names the false-report class, and `tripwires.mjs`'s three inherited cites are members of it (`:559` vs `:872`, `:578-582` vs `:941`, `:590` vs `:951`) — fixed, in the file this car is chartered in and whose own header forbids exactly this. `scripts/audit/soakInvariants.mjs`'s header cites are ALSO stale (`whole-world-soak.mjs:842` is now `sourceSettlementId`, the envelope verdict is at `:878`; `:886-896` for `frozenTail`, which is at `:972`) and are NOT fixed here: that is a header sweep, not a car, and fixing it inside this consist would trade a bounded car for an unbounded one. Recorded in DEFERRED. | measured, car 5. | lane |
| R24 | **⛔ REFUSAL WITH A MEASUREMENT — the charter's "state it as the identical-after-strip figure the skeptic measured" could not be obeyed, because the strip is not reproducible from the figure.** The on-disk 539,995 bytes each REPRODUCES exactly. Every stripped figure depends on which keys are stripped: this car's own seven-key strip reads 537,042 vs 537,043, the skeptics' reads 305,121 vs 305,120, and neither is derivable from the other. So no stripped byte count is quoted as a receipt — the substance (`finalHash`, all 30 yearly hashes, both series) is RE-EXECUTED instead. The one-byte difference is itself the argument: it is a host timing one digit longer, the exact class a byte count cannot tell from a behaviour shift. | measured, car 5. | lane |

## DEFERRED — documented, not a bug to re-find
| # | the item | why it is deferred, and what would discharge it |
|---|---|---|
| D1 | **`capacity_plateau` has never graded a SHIPPED 300-row series** — only one rebuilt from `behavioral.yearly[].stateVectors[].population` (CAR-1 table). Every receipt on disk that is long enough predates the series; every receipt that carries the series is 30 years long. | The first receipt that will carry the series AT the horizon this row grades is **CAP-HORIZON-909's 600-year run under the new writer** (the chair's §909 ruling that a 600-year run precedes any re-cut). Until it runs, the cure of §907 M1-F1 **at the row's own horizon** is PLAUSIBLE-by-reconstruction, not CONFIRMED-on-a-shipped-series. Nothing else discharges it: a 300-year lit run is ~25 minutes and outside every brief this lane held. |
| D2 | **A DARK run certifies `fullInstrument: true` with all four capacity rows NOT APPLICABLE by gate** (critic G9; car 3 widened the set from three to four). | **By design, and recorded as an open DESIGN question rather than a defect:** the gate is a property of the run's LIGHTING, and the register's cell identity carries `lighting`, so a dark cell's certificate is not claiming the demographic instrument ran. The question — whether a dark world's population term should be graded by *something* — belongs to the tuning sitting, not to a lane. R11 keeps the wider net available at one line if the chair wants it. |
| D3 | **The nested half of the walker grades later path segments against a FLAT union of every `return {` key in the observer (79 keys)** — so a leaf the observer ships ANYWHERE passes even where it cannot nest. The header admits it in writing. | A per-return-site nested walk is **a chair follow-on for a later consist**. The runtime channel is not built on it: `receiptCarries` walks the REAL receipt segment by segment, so a path this walker admits and the envelope does not carry still grades NOT-EXECUTABLE with the path on its face — the weakness is toward silence in the guard and never in the verdict. |
| D4 | **`scripts/audit/soakInvariants.mjs`'s header carries at least three stale line citations** (`whole-world-soak.mjs:842`, `:858-862`, `:886-896`; measured at car 5 — the subjects are at `:878`, the byte-ceiling check, and `:972`). | The cite-by-symbol law applies, but a two-header sweep is not this car (R23). Discharged by a comment-only car over `soakInvariants.mjs`'s and `whole-world-soak.mjs`'s module headers, each cite re-derived. |
| D5 | **`home:` on `capacity_envelope_30y` still points at the test file** while the bar now lives in `soakInvariants.mjs` (critic J5). The prose carries the truth and the pin asserts only that the string contains `demographicsEnvelope`. | MINOR, and left as the critic recommended. The estate's "a charter's CONFIRMED home is a hypothesis" hazard argues for pinning the real home; nothing is wrong today, and moving it churns a pinned string for no measured gain. |


## PROVENANCE
- Scratch: `$SC/soak909/` — three real soak receipts (`fresh-30y-4s.json` dark, `fresh-30y-4s-lit.json`, `fresh-30y-4s-lit-AFTER-car3.json`) with their run logs, the annotated receipts, the eval transcripts, `typecheck-ratchet.log`, and every vitest log (`car2-*`, `car3-*`, `final-*`).
- **THREE soaks**, each through `sh scripts/gate-mutex.sh --run -- …` (exclusive tier), each under the quiet-window law — load-1 **3.37**, **2.97**, **2.03** (all < 4.0), **0** vitest processes each time, `gate-mutex.sh` inspect = `FREE`. Wall clock: dark 30 y **2 m 34 s**, lit 30 y **2 m 14 s**, lit 30 y re-run **2 m 07 s**.
- `$SC/HOLD-VITEST` checked before **every** vitest invocation; absent throughout.
- **CAR 4 scratch:** `$SC/soak909/car4-*.log` — the two pre-commit gate logs, the seven-probe negative-control transcript (`car4-anchor-negative-control.log`) and its driver (`anchor-probe.py`), the eslint log, the commit log and message. Pre-edit and post-edit copies of the file at `$SC/tripwireRegistry.BEFORE-car4.js` / `.AFTER-car4.js`, and the committed bytes at `$SC/soak909/at-HEAD.js`.
- `$SC/HOLD-VITEST` checked before **every** vitest invocation in car 4 as well; absent throughout (04:05:52, 04:09:44, 04:10:02, 04:10:10, 04:11:00).
- **CAR 5 scratch:** `$SC/soak909/car5/` — `census.mjs` (the census instrument, the critic's `$SC/skeptic-909/grade.mjs` widened to all three channels), `census-BEFORE.txt` / `census-AFTER.txt` (33 documents each), the twelve per-file vitest logs (`car5-*.log`), `car5-eslint.log`, `car5-typecheck-ratchet.log`, the two real-CLI evaluations (`eval-30y-lit.log`, `eval-300y.log`), the commit message (`msg-car5.txt`) and `car5-commit.log`. The skeptics' own probes were RE-RUN rather than paraphrased: `$SC/skeptic-909/empty.mjs` at `68c3a1618` and again at `864e76be9`.
- Dock `$SC/laneB6`, DETACHED, `6ebe0ef3b` -> `ec265d24c` -> `268d53605` -> `d58b489b8` -> `68c3a1618` -> **`864e76be9`**; porcelain **0** at the end; nothing staged that this lane did not write; no push, no stash, no `git add -A`/`-u`/`.`; car 4 staged by explicit path and committed with `--only`.

---

## ORIENTATION — the brief's figures re-derived before any edit (predecessor 02:50:57, re-derived by this lane 03:03)
| brief cites | measured at `6ebe0ef3b` | verdict |
|---|---|---|
| `receiptBody` ~:1013 | `const receiptBody = {` at **:1013** | **CONFIRMED exactly** |
| written ~:1163 | `writeFileSync(file, …JSON.stringify(receipt…` at **:1163** (pre-car-1). ⚠ **"…; :1197 after" IS WRONG AND IS CORRECTED AT CAR 5:** `:1197` is `if (RECEIPT_PATH) {`; the WRITE is at **`:1200`** at `ec265d24c`. The tree is right and the receipt was wrong | **CONFIRMED, after correction** |
| `behavioral-observation.mjs` ~:957 inline `0.0025` | `:957` | **CONFIRMED exactly** |
| `demographicsEnvelope.test.js` ~:45 for `MOTION_FLOOR_01` | `MOTION_FLOOR_01` at **:53**, `MOVING_SHARE_FLOOR` at **:55** | **brief off by eight lines; immaterial** |
| `capacity_realm_load` keys on `behavioral.yearly[last].realmDemography` | `tripwires.mjs:394` (pre-car-1) | **CONFIRMED** |
| `behavioral-observation.mjs` writes `realmDemography` | `:998` computes, `:1054` ships `...(realmDemography ? { realmDemography } : {})` | **CONFIRMED — conditional, so a dark run drops the key** |
| register cells' scaffolded `receiptSchemaVersion` "must move 5 → 6" | both genesis cells scaffold **`0`**; computed at mint (`register.mjs:195`) | **BRIEF PREMISE REFUTED — no cell edit owed, none made** |
| the series builders | `yearlyPopulations`/`yearlyDiedFlags` declared **:389-390**, pushed **:489-490**, returned **:555-556** | **CONFIRMED exactly** |
