# whole-900e — the 102-car proof at 7ef501df9 (laneDESKPROOF5, launched 14:07 under the mutex, machine load 20–34 with four agents running)
Classification of the failing files as they appear (the FAIL details print at the END of the run; re-check each against the detail block before acting):
| file | arms red | class | disposition |
|---|---|---|---|
| tests/lint/sovereigntyLightingContract.walker.test.js | 1 (the census) | REGISTER — lighting files 2537 vs frozen 2523 (+14 from the desk docks' new test files; VIS +1 title) | the lighting door, FIRST, in run-registers-900.sh |
| tests/lint/tuningRegister.walker.test.js | 1 (inventory exact) | REGISTER — IRREGULAR_TUNING draft row + the two-band CHANCE_MEETING row + TUNEREG-2's tables | the tuning-inventory refreeze door |
| tests/property/generatorGoldenMaster.test.js | 1 | BANKED owner-gated (the declared prose text shift; register row in the ratchet baseline) | none — the ratchet knows it |
| tests/lint/proseNumerics.test.js | 2 (debt exact; identities) | REGISTER — relocations (GEN3 22 rows, DEF2, ECON2) | prose-numerics re-key (dry then --write) |
| tests/lint/writerReach.walker.test.js | 13 | REGISTER — desk leaves gain readers; corpusMeta/shapesDigest; the dial roster 30→31; ⚠ `forcedByConfig on stress/stressors` DARK-UNREGISTERED and `hasMilitaryInst on inst` stale-banked may REFUSE the --write → a fix car before the registers | writer-reach --write; read its refusal text |
| tests/docs/enforcement-claims.test.js | 1 (@enforced-by ≥1 target) | BANKED (the ratchet's known-failure row) — verify the row still matches this arm's title | none if banked; else a fix |
| tests/copy/voiceMechanics.test.js | 2 (per-file debt; per-file JSX debt) | REGISTER — the Tier-2 per-file row (banked as debt, OWED_CEILING 3) + the JSX refreeze (legal, a FALL) | the voice refreeze doors (Tier-2 banked; JSX --write) |
| tests/domain/livingContentLawVersion.test.js | 1 ("every moved symbol is DECLARED exactly once") | FALSE RED UNDER LOAD — the arm scans 2,187 src files and took 21,011 ms against `testTimeout: 20000` (vite.config.js:904); the chair's replicated scan finds exactly ONE declarer per symbol at BOTH 7ef501df9 and e3d240911; green in whole-900a–d | re-run the file alone under the mutex on a quiet machine before the registers; if green, record as load-timeout |
Rule (memory FOLD 41 / DOOR 3): a proof run beside four agents at load 30 can time out arms that scan the tree; every timeout-class red is re-run quietly, never banked and never "fixed".
- livingContentLawVersion: RE-RUN GREEN (Tests  8 passed (8)) with --testTimeout=60000 under the mutex; the proof's red was a load timeout (21,011 ms vs 20,000).
