# W-ARMS — THE ENUMERATION, WITH ITS REAL DENOMINATOR
Lane WARMSSCOPE · 2026-09-04 · Seat Opus 5 · measurement-and-design only, zero writes.
Product base: `laneKERNELMARK-tree` @ **`f537ce47e`** (porcelain 0). Ledger: `review-fixes-2026-07-08` @ `88be66ab8`.

## 0 · WHAT "W-ARMS" DENOTES — QUOTED, NOT INFERRED

The chartering line (CONFIRMED, `DESIGN_FMG_WEAVE.md` §3, quoted in the design volume):
> "W-ARMS (L, last, §728-ratified, vetoable): generative heraldry with kinship inheritance (vassal
> quarters the liege's charge) over the relationship vocabularies and the 8-emblem substrate;
> web/PDF seam honored; only the 104 pruned CC0 charges may be referenced."

Amendment 14: *"W-ARMS owes determinism/dormancy/test-bill rows at its dispatch."*

The owner's ratification (CONFIRMED, ODQ §728.1, 2026-08-29):
> "'It all' is read as the whole review INCLUDING the owner-gated generative-heraldry item (this
> directive is its ratification; it is sequenced last and stays vetoable)."

The charter (CONFIRMED, ODQ §879.11 R2, read at `docs/OWNER_DECISION_QUEUE.md:32312`):
> "**R2 W-ARMS — RULED: NOT DROPPED, NOT NOW, MINTED AS ITS OWN ARCHITECTURE-FIRST CAR AFTER T13.**
> … bill = determinism pin · the §713.2 bit claim (525/525 + fence 21/21 at its tip) · a DECLARED UI
> shift · four censuses if a domain leaf · imported from components/pdf only (the engine margin is
> ~266 B). Slot: **a short Fable design note + skeptic pass**, then 2–3 Opus cars on their own gate
> after T13 and BEFORE L9 iff the PDF seam moves `goldenViewModel`; otherwise lawfully after L9 and
> before the walk. ⚠ OWNER: your one-word veto drops it; it sits beside the T13 REC on the desk."

So W-ARMS is **not** a name for a code file. It denotes a CAPABILITY (generative heraldry with
kinship inheritance) plus a two-part named bill (design note **and** skeptic pass) plus 2–3 build
cars on their own gate, held open under a standing one-word owner veto.

## 1 · THE AUTHORITATIVE HOME (CONFIRMED)

`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/825f209c-0e84-4a1e-b6f0-79a46de834dc/scratchpad/DESIGN_W_ARMS.md`
· 55,812 B · 558 lines · md5 `58e1e8dc6f9aebe55b3eeed7758524b2` · mtime 2026-09-01 21:21 · sections 0–9.

⛔ **It is in NO git ref, on NEITHER branch.** CONFIRMED: `git log --all --oneline -- '*DESIGN_W_ARMS*'`
returns empty; `git ls-tree -r --name-only master | grep -i w_arms` empty; same for `HEAD`.

The ledger's own record of it (CONFIRMED, ODQ §880.5): *"**W-ARMS-DESIGN COMPLETE:** `DESIGN_W_ARMS.md`
(55.8 KB, §§0–9, 74 CONFIRMED / 6 PLAUSIBLE)."*

Other homes, all secondary and all consistent with the volume: `docs/recon/R-WEAVE-READY.md:56` (the
`HELD-LAST` row), `docs/briefs/TRAIN-PACKETS.md:150,157`, `docs/HANDOFF_CURRENT.md` (14 arc lines),
`docs/FABLE_RETROVALIDATION_QUEUE.md:2280,2657`, `docs/OWNER_DECISION_QUEUE.md` (21 lines).
The prior scoping pass lives at `$SC/pending/WARMS.json` (10 items) and `$SC/pending/RECONCILED.json`
stage **H** (6 items) + 3 `owner_gated` rows.

## 2 · WHERE THE "ROUGHLY 9" COMES FROM, AND WHY IT UNDERCOUNTS

The 9 is derivable, not invented: `RECONCILED.json` stage H holds **H1…H6** and its `owner_gated`
array holds **three** further W-ARMS rows (`[12]` Car 3, `[13]` Car 4, `[30]` the veto + the eleven
taste rows as ONE row). 6 + 3 = **9**. `WARMS.json` independently lists **10** (it splits the veto
from the taste rows).

Both undercount, because neither enumerates the **landing-register acts** the design's own §7
"Landing sequence" paragraph names as obligations of the gate. Those are work, they are owed, they
belong to the chair, and nobody has listed them as items.

**MEASURED DENOMINATOR: 14 items.** 9 (or 10) as previously scoped + **4** never enumerated.
Of the 14: **0 are done**, **14 remain**. Of the 5 build cars, **1 (Car 4) is explicitly out of this
landing**, so the landable build is **4 cars**.

## 3 · THE ITEMS

Classification key: **NEW CODE** · **WIRING** (something already authored, never connected) ·
**CONTENT/PROSE** · **REGISTER** (a chair act at a landing) · **OWNER-GATED**.

### TIER A — BUILD CARS (5)

| # | item | class | done | concretely requires |
|---|---|---|---|---|
| **A1** | **Car 0 — MEASURE** (rider, 0 product bytes, read-only) | NEW CODE (probe only, nothing committed) | NO | Five named probes, quoted: (1) write the adapter's reads on a scratch copy in a detached worktree and run `scripts/check-observed-shape-readers.mjs`, quote the identity delta; (2) import `EAGER_FIRST_PAINT_MODULES` from `vite.config.js` (CONFIRMED exported at `vite.config.js:309`) and quote its size + that no ornament file is in it — **no build needed**; (3) grep every `<SettlementPDF`/`generateSettlementPDF` caller for what `campaign.settlements`/`nameById` carry; (4) `npx esbuild --bundle --minify` a 40-line `arms.js` stub, quote bytes; (5) whether `wizardNewsAuthoring` pins any line in `pools.js`/`DossierHeaderRow.jsx`/`Cover.jsx`. STOP: any kinship read whose OSR identity is banked with no exported reader to route through ⇒ STOP to the chair. |
| **A2** | **Car 1 — THE LEAF + PINS** | NEW CODE | NO | `src/design/organic/ornament/arms.js` (new); `pools.js` += `CADENCY_MARKS` (append-only, 4 entries); `emblemPaths.js` += `CADENCY_PATHS` mirror; `scripts/gen-organic-ornament.mjs` += arms samples; `docs/samples/organic-craft/ornament/arms-*.svg` (design estimates ~24 new; **measured base = 16 committed samples**); `tests/design/organicOrnament.test.js` EXTENDED — **no new test file**. Proofs: samples byte-stable, serializer parity, rule-of-tincture exhaustive over 7×6, integer-geometry over every emitted coordinate, existing `EMBLEMS` samples byte-IDENTICAL. |
| **A3** | **Car 2 — THE ADAPTER + THE WEB SEAM** | NEW CODE + WIRING | NO | `src/domain/display/armsKinship.js` (new); `tests/domain/display/armsKinship.test.js` (**the ONE new test file**); `Ornament.jsx` += `<Arms/>`; `DossierHeaderRow.jsx` (medallion → arms, two optional props); `OutputContainer.jsx` threads `owningWorldState` + `allSavedSettlements`; placement `describe`s into `domainGeneratorsBoundary.test.js` + `vendorPdfLazy.test.js`; the lighting refreeze + anchors; the DECLARED UI shift record; **one full build** (eager chunks hash-identical, first-paint closure byte-identical). |
| **A4** | **Car 3 — THE PDF SEAM** | NEW CODE + WIRING · **OWNER-GATED by nature (paid surface)** | NO | `src/pdf/primitives/HouseArmsBlock.jsx` (new); `Cover.jsx` (arms block beside the title, counterseal kept, `armsInputsFor` at the call site = option (b)); `tests/pdf/countersealStructuredPath.test.js` EXTENDED. Proofs: `goldenViewModel` `.snap` byte-identical (**measured base = 17 lines**), `viewModelParity` green, `pdfEntropyGuard` + `src/pdf` lint green, and the cross-surface parity pin (the top surviving refutation, §8.3). STOP: the PDF path unable to resolve the liege's NAME ⇒ STOP until `nameById` is threaded; never fall back to `String(id)` as a seed. |
| **A5** | **Car 4 — THE CC0 CHARGE ROSTER** | NEW CODE + CONTENT · **OWNER-GATED (taste row §9.8)** | NO | Explicitly **"not in this landing"** in the design's own car table. Would owe: copied charges as path data, `src/design/organic/ornament/CHARGES-CC0.md`, a `THIRD-PARTY-NOTICES.md` row, and a new pin that each copied `d` equals its `public/map/charges/<name>.svg` source. Substrate re-measured CONFIRMED at `f537ce47e`: **104 files, 279,799 B** in `public/map/charges/`. |

### TIER B — MACHINERY DEBT / UNMET TERMS OF THE CHARTER (3)

| # | item | class | done | concretely requires |
|---|---|---|---|---|
| **B1** | ⛔ **Commit `DESIGN_W_ARMS.md` to a git ref** | WIRING (content authored, never wired) | NO | One commit onto the ledger line's `docs/`. **Measured CHEAP:** the volume carries **ZERO** `enforcement-claims.test.js` `CLAIM_RE` hits (re-measured tonight, `grep -E` over all 8 alternates → rc=1), so it does not touch that census — which matters because that arm is a **banked known-failure at ceiling 6 with zero headroom**. No docs test freezes a docs/*.md file count (`docCounts.test.js` scans `supabase/migrations` + `supabase/functions`, not docs). `docs/` is outside both voice arms and outside the lighting census's subject. |
| **B2** | **The independent skeptic pass** | REGISTER / process act, 0 bytes | **NO — CONFIRMED NOT RUN** | §879.11's bill names it as half of a two-part obligation ("a short Fable design note **+ skeptic pass**"). §880.5 records only the note. See §5 of the receipt for the four-way negative. ⚠ The volume's §8 is a five-lens refutation **authored by the design lane itself** — self-administered, so it does not discharge an INDEPENDENT pass. |
| **B3** | **Re-measure the point-in-time figures at the boarding base** | REGISTER / measurement, 0 bytes | **PARTIALLY DONE BY THIS LANE** | This lane re-measured 20 figures at `f537ce47e` (receipt §4). Nine decayed, one is materially REFUTED (the first-paint premise). A boarding-base re-run is still owed for anything a build settles. |

### TIER C — OWNER ACTS (2)

| # | item | class | done | concretely requires |
|---|---|---|---|---|
| **C1** | **The one-word veto** | OWNER-GATED | OPEN | §879.11 verbatim: *"⚠ OWNER: your one-word veto drops it; it sits beside the T13 REC on the desk."* Nothing since narrows it. A veto drops A1–A5 entirely; B1 (committing the volume) survives a veto as a record. |
| **C2** | **The eleven §9 taste rows** | OWNER-GATED | NO | §9.1 seed root (`settlement.name` REC vs `_seed`) · §9.2 shield (banner REC vs escutcheon) · §9.3 tinctures (hatching REC vs colour — **re-asks the fixed-ink-palette constant itself**) · §9.4 kind bias · §9.5 sizes/placement · §9.6 the blazon sentence · §9.7 defaulted subordination · §9.8 the CC0 roster · §9.9 sibling cadency collisions + the sacred/politics vocabularies · §9.10 campaign PDF / World Book · §9.11 the on-screen foot colophon stays removed. Each carries the chair's recommendation first and each is vetoable. §9.1 and §9.3 are the only two that would MOVE a landed golden family if answered against the REC. |

### TIER D — LANDING-REGISTER ACTS (4) — ⭐ NEVER PREVIOUSLY ENUMERATED AS ITEMS

Named by the design's own §7 "Landing sequence" paragraph; every one is a chair act at the gate.

| # | item | class | done | concretely requires |
|---|---|---|---|---|
| **D1** | **The lighting census refreeze, taken ONCE for the whole consist** | REGISTER | NO | Five figures. Measured base at `f537ce47e` (`tests/lint/.lighting-census-baseline.json`, `measuredAtSha 450f7dbb7`, §891 landing): **files 2515 · parked 371 · credited 2144 · titles 22922 · suiteTitles 6153**. ⚠ The §882.15 self-refusing-refreeze defect is a live standing hazard for any lighting-refreezing lane. |
| **D2** | **The DECLARED UI shift record** | REGISTER + CONTENT | NO | The design supplies the sentence verbatim (§5.3). Written in the landing's commit body AND mirrored to the ODQ collection row. Precedent: VAR-3 landed as *"a DECLARED UI shift; generation untouched"* (§826/§879.11). |
| **D3** | **The §713.2 bit claim + the espionage fence at the landing tip** | REGISTER | NO | The charter is explicit that this is **NOT by construction — it must be PROVEN**. §879.11 and the design both cite **525/525 goldens + fence 21/21**. ⚠ I could not re-derive either count at `f537ce47e` (no comparator located by name) — see the receipt's REFUSALS. |
| **D4** | **The size/hash build** | REGISTER | NO | Eager chunks (`kernel`, `engine-core`, `index`, `data`, `vendor-*`) HASH-identical; first-paint closure byte-identical; dossier-chunk delta quoted. ⛔ A build is forbidden to this lane, and the design's own closure premise is STALE (receipt §4, DECAY-10). |

## 4 · THE UNWIRED-BUT-AUTHORED CHECK, PER ITEM

The shape: *a ruling made, the content authored, and the WIRING STEP NEVER TAKEN* (four instances
tonight per `$SC/RULINGS-893-CENSUS-HEADROOM.md` §2: `respect`→`BOND_KINDS`; `rivalry`→`GRUDGE_KINDS`;
`errandSpineEnabled`→`PREVIEW_OVERLAY`; eight annex families→`WAR_RECEIPTS`).

**In the product code: ZERO instances. CONFIRMED.** `git ls-tree -r --name-only HEAD | grep -iE
"ornament/arms|armsKinship|HouseArmsBlock|CADENCY"` returns **empty**. `src/design/organic/ornament/`
holds exactly five files (`compose.js`, `emblemPaths.js`, `fnv.js`, `palette.js`, `pools.js`) — no
`arms.js`. `src/domain/display/` holds 103 files — no `armsKinship.js`. §879.11 measured *"ZERO
heraldry code"* at its tip and that is still true. **Nothing was half-built.**

**At the DOCUMENT layer: ONE HARD INSTANCE, and it is item B1.**
A ruling was made (§728.1 ratification → §879.11 R2 charter → §880.5 slot ruling), the content was
authored (55,812 B, 74 CONFIRMED / 6 PLAUSIBLE, recorded COMPLETE in the ODQ), and the wiring step —
**committing it to any git ref** — was never taken. This is **instance five of tonight's pattern**,
at the document layer rather than the pool layer. Every other item in this block depends on it.

**ONE SOFT INSTANCE: item B2.** §879.11's bill named ONE slot with TWO halves — "a short Fable design
note **+ skeptic pass**". The note half landed and was collected; the skeptic half was never taken and
no row records the omission. Same shape: a ruling made, half the named act performed.

**Total: 1 hard + 1 soft = 2 unwired-but-authored items, both at the document layer, zero in code.**
