L-CHAIR-901 fix car B: the W-OPS mint's fifth surface — the covering-array census re-measured 31 → 35 out of band, and the closure is the proof

The composed-tip proof (whole-901.log at d75e807a6) reddened
`tests/soak-harness/coveringArrayCoverage.test.js` › "the flag domain is ENUMERATED FROM
THE REGISTRY, and its arithmetic closes": `census.virtual.length` measured 35 against the
literal 31 frozen at SEAT-78 (§900). LGT-P5-WOPS (six cars, replayed from 427b85c63) mints
four engine-gated virtual keys — infiltrationDepthEnabled, missionDispatcherEnabled,
operationsVoiceEnabled, envoyTaskCatalogEnabled — and paid the manifest, the certification
rows, the family-door reads and (at the trial composition) contributionLedgerShape's pair;
this soak-harness census was not on its battery, exactly as it was not on SEAT-78's, and
the first assertion's red blinded the three literals after it, exactly as the file's own
note warns.

MEASURED OUT OF BAND BEFORE ANY LITERAL MOVED, by calling flagDomainCensus with the
suite's own arguments at d75e807a6: governed 25 (unmoved), ungoverned 32 (unmoved),
virtual 35, union 92 (88 → 92 in lockstep), union − governed 67 (63 → 67), nonBoolean 13
(unmoved), overlap [], sum closes. All four keys absent from DEFAULT_SIMULATION_RULES and
from all seven preset spreads — checked directly. Three literals moved (31 → 35, 88 → 92,
63 → 67) and the history paragraph is written in the file's own form. Executed under the
mutex: tests/soak-harness/coveringArrayCoverage.test.js — 6 tests, exit 0 (fixB-901.log).

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
