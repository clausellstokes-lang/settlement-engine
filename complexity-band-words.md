# THE SIX COMPLEXITY BAND WORDS — chair words act (Fable 5.1, 2026-09-05), for `src/domain/display/labelBands.js`'s `complexityBandOf` map at §900
DOCKET-2 measured: `deriveEconomicComplexity` (src/generators/economy/prosperity.js:296–314) emits ELEVEN labels; five carry `"<Band> — <gloss>"` and their band is the head before the dash; six carry no dash, so the Summary tile rendered the whole phrase on 174 of 360 driven settlements. The map declares `null` for those six (byte-identical to today) and the words are the chair's (§0c-3 class).

RULE FOLLOWED: the band word is the label's own HEAD WORD, exactly as the five dashed labels already do (`Highly diversified`, `Diversified`, `Concentrated`, `Limited`, `Subsistence`), so the tile's vocabulary stays one family and no new word enters the reader's page that the label did not already carry.

| label (verbatim, the map key) | band word |
|---|---|
| `Diversified market economy` | `Diversified` |
| `Specialized production and trade` | `Specialized` |
| `Mixed subsistence and market` | `Mixed` |
| `Agricultural surplus with trade links` | `Agricultural` |
| `Subsistence with minor surplus` | `Subsistence` |
| `Subsistence with surplus` | `Subsistence` |

Consequences to pin in the same car: `complexityBandOf` returns a word for 11/11 labels (the walker's totality arm moves from 5 to 11 with attribution); `Subsistence` now names THREE labels (the dashed survival-economy one and these two) — that is honest, the tile shows the band and the gloss stays in the label; no display label changes, so `economyInputFingerprint` (which hashes `safetyLabel`/`foodLabel`, not this) does not move — prove by the fingerprint's freshness assertion staying green. Voice: single words, no digit, no dash. STATE, never FATE.
