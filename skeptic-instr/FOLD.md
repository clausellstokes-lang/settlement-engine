# THE SKEPTIC FOLD — LANE INSTR-912, seven cars, five lenses

**CONFIRMED 43 · REFUTED 28 · PARTLY 21 · UNTESTED 6 — 98 verdict rows over five lenses; porcelain 0 → 0 on every lens and on this fold. 60 cures owed (18 code · 30 receipt · 12 spec).**

Seat: Opus 5 — Fable-unvalidated (the folder). Dock read: `$SC/skepINSTR` @ `74a1aa0e8` over `3b1c0eaa5`, read-only.
Receipt under fold: `$SC/receipt-instr-912.md` (887 lines; §§9, 10, 11 read in full).
Counting rule: one row per verdict as the lens itself labels it; a limb-bundle (grammar-walker (f), four limbs) counts once and every limb is listed in §2. Rows a lens labelled CONFIRMED that nonetheless correct the receipt are counted CONFIRMED and marked `†` in §2 so nothing is hidden by the arithmetic.

| lens | file | CONF | REF | PART | UNT | porcelain before → after |
|---|---|---|---|---|---|---|
| 1 · the entry walker (car 1) | `entry-walker.md` | 10 | 5 | 4 | 0 | 0 → 0 |
| 2 · the B-grammar walker (car 2) | `grammar-walker.md` | 6 | 4 | 3 | 1 | 0 → 0 |
| 3 · the loaders and the table (cars 3, 4) | `loaders-table.md` | 9 | 9 | 5 | 3 | 0 → 0 |
| 4 · the measures and D8 (cars 5, 6) | `measures.md` | 8 | 7 | 5 | 0 | 0 → 0 |
| 5 · the gate, the proof, the fences (car 7) | `gate-proof.md` | 10 | 3 | 4 | 2 | 0 → 0 (one plant applied and restored; md5 `7760c480790ae5e801734e3350efdf8a`, `cmp` identical) |
| **total** | | **43** | **28** | **21** | **6** | all clean |

**The fold's own arms** (four rows, run in this session, not counted in the 98 — they exist to settle §4 and are marked `[FOLD]` where used): the writer-reach gate re-run green at the tip; the manifest re-serialization reproduced independently; the sweep's plant-number duplication traced to the BASE, which *corrects* gate-proof §B; the sweep's second hunk located inside `MUTATED_FILES`, a rebase hazard no lens named.

## THE HEADLINE IN ONE BREATH

The instrument consist is real and its spine holds: seven registers load, the four Brackwater fixtures discriminate arm for arm, five planted mutations are genuine and the one reproduced end to end reds exactly 6 of 19, the fingerprint is 21/21 parity on three texts, and **zero product bytes moved** across all seven commits (18 files, all additions, zero modifications, in an island no shipped graph imports). What fails is the *reporting layer*: **28 refuted claims**, of which four are load-bearing on rulings already banked — §4.4's column census does not reproduce and the design finding drawn from it is false; the three "new C2 breaches" are ground artefacts and that census row is 3 false positives and 0 true; A12's "correction" 20 → 22 compares two different quantities and SITTING §K.5 rests on it; and the unrendered-facts census is not §912.1's count. The owner's wiring addendum was neither built nor refused.

---

# 2 · EVERY REFUTED AND PARTLY VERDICT

## 2a — REFUTED (28)

| # | claim | the figure that refutes or corrects it | sev | lens |
|---|---|---|---|---|
| R1 | §1.4: `reeve` is held only inside an institution NAME; three crier lines are new C2 breaches | `historyData.js:119` carries `{role:'Reeve', title:'overseer'…}` in `POWER_ROLES_BY_CATEGORY`, consumed by two generators; `roleCategory.js:34`; `npcGenerator.js:1006`. **25 of 37** office candidates are called absent by the derived ground, **10 of the 25** sit in the estate's own `ROLE_CATEGORY_KEYWORDS`. The census row `3 C2 · an office the world does not hold` is **3 false positives, 0 true** | HIGH | 1 (F1) |
| R2 | brief car 1: the QUALIFY positive control must WITHHELD/NOTE, never pass silently | `power.generated.js::DS-POW-5::…#2` → `verdict PASS`, `fails []`, `withheld []`, `notes []`. `armQualify` returns early on `sentences.length < 2`; no Q assertion exists anywhere in the gate file | MED | 1 (F2) |
| R3 | the totality arm is sound (the four fixtures exist to prove it) | armC4 detects with `holds(masked,q)` (word boundary) and locates with `masked.indexOf(q)` (substring). "The wall is old, and all souls are counted here." degrades **FAIL → NOTE**; 69 of 3,132 entries carry a mis-located quantifier (70 occurrences). Census figure 13 unchanged — latent, not live. `bandReadings` shares the pair | MED | 1 (F3) |
| R4 | §1.3: the positive set proves the detector does the judging | `control::pre-seed` engages **no arm at all** (fails/withheld/notes empty, one not-executable C5 row); `control::lack` fires only a C4 note. The claim rests on `banded-count` and NL-4 alone | LOW | 1 (F4) |
| R5 | the test's "(resource conditional)" distinguishes something | `byBlock.get('DS-GEN-18').conditional` = all four slots `["good","institution","resource","settlement"]`; `toContain('resource')` passes either way | LOW | 1 (F5) |
| R6 | §2.8's printed `fails by arm` list includes `A` | the same test at the tip prints `D, F1, F2, F3, F6, G/FEELING, G/FIGURE, G/FORECAST, G/MEANING` — **no A**. A stale pre-car-7 print, on the one arm the receipt declares non-gating | MED | 2 (b2) |
| R7 | §2.5: a composed dossier reading, 200 towns, through the SHIPPED composers | 3-town execution: **29 lines, 29 of 29 from `powerStateProse`**, 7 blocks of 68, **0 from the general desk** (`generalDeskLines` returns bare strings the harvester cannot see). V1 78.4% / run 0.618 is a **POWER-DESK** figure, not a dossier one — and SITTING K.2 carries it as the wave's case | HIGH | 2 (c) |
| R8 | §2.9: every arm carries a control that can fail — **four limbs** | (i) **armE has no fail channel**; MOVE-GRAMMAR §4.4 control 4 requires R1 leaves to RED on arm E, the test instead measures 407/708 and 79/708 through `segmentCount`/`openerOf`, bypassing armE. (ii) **arm B2's only control asserts SILENCE** — unreachable unless `1/n <= max(2 SE, runFloor)`, i.e. n > 20. (iii) BUDGET asserts only `scored > 10` and `exceeded.length > 0`; SPREAD asserts only `Array.isArray` (an empty array passes). (iv) **arm F implements 4 of 10 declared WALLS** (1,2,3,6); walls 4,5,7,8,9,10 have no detector, no control, no NOT-EXECUTABLE row — wall 10's raw material counts 103 of 400 variants | HIGH | 2 (f) |
| R9 | `walkGrammar`'s comment: arm D is "keyed on (block, pool) as gap (e) requires" | the code reads `input.composedFill.get(String(entry.block))` — **block only**; the test builds the map with `composedFillByBlock(fillSites())`. Gap (a) is likewise a register total (`figures.gaps.settlementOpeners`), not SITTING §J's per-POOL count | MED | 2 (f3) |
| R10 | §3.2's R9 row prints the walker's own output | the executed gate prints `singleton pools 190`; the receipt's cell is a **dash**. The format string always prints an integer, so the dash is a hand edit of a table introduced as "the walker prints this every run" | MED | 3 (a) |
| R11 | §3.2: R7's disagreement is caused by "roster (5 of 6 files) **and** predicate" | reading one file fewer cannot raise a count; only the looser predicate explains 2,254 > PROBE_ALL's 2,169 | LOW | 3 (a) |
| R12 | §3.4: "each control MUST fire, and each does" | `git grep refuseEmpty` returns **twelve hits, all call sites plus the definition**; no test in the estate drives it. The named command does not exercise the third fail-closed path — the exact false-green class the docblock invokes | MED | 3 (b) |
| R13 | refusal 15: the chronicle label is corrected to R12; "R11 is not loaded" | the shipped loader stamps `R11` at `dossierCorpus.js:646, :650, :656`; **11 of the 85 chronicle rows carry `register:'R11'`** while the census row is headed R12. A wave walker filtering by register misjudges or misses the chronicler's letter | MED | 3 |
| R14 | §4.4's column census (six figures) | executed twice, byte-identical: hamlet live **19** (not 17) · hamlet `whatItCounts` held **1** `Record keeping` (not 0) · hamlet `whatItDoes` **23** (not 18) · hamlet `whoIsCounted` **"several hundred"** (not "a hundred or so") · town live **57** (not 55) · town counts **Register of the dead · Tax collection · Toll collection** (not Custom commission…) · city live **48** (not 42) · city **Custom commissions** (not Customs bypass). No generator byte and no seed moved between car 4 and the tip, so the table is not the gate's output at any commit of this lane | HIGH | 3 (e) |
| R15 | §4.4's finding: a hamlet's duty column is empty, so no count-duty sentence is licensed on a small settlement | at seed `census-hamlet` the hamlet holds `Record keeping`. **The design finding the wave would be priced from is false** | HIGH | 3 (e) |
| R16 | §4.5's numbers: duty vocabulary falls 10 → 9, with the nine listed | measured **12 → 11**; exactly one instantiated row removed (`Custom enchanting`); the receipt's nine omits `Custom commission` and — materially — **`Tithe and dues`**, the one duty CLERK-LAWS §1.4's Brackwater walk turns on. A chair reading that list concludes the tithe duty is instantiated nowhere. It is | HIGH | 3 (f) |
| R17 | §4.3: the `whoIsExempt` scan is "asserted in the test, not assumed" | `nullEverywhere` is `tollExemptions.length === 0`; `tollExemptions` comes only from `world.treatyTerms`; the test's world is `Object.freeze({ bandOf: quantityWords })` and never supplies it. The assertion holds for **any** settlement, including one carrying a live exemption — **it is asserted about nothing** | HIGH | 3 |
| R18 | `institutionTable.js`'s own law: only a closed column licenses a quantifier | **three columns ship `closed: true` on partial fills**, two with a `basis` naming a source the code never reads: `whatItCounts` reads `availableServices` only (no `economicState` import; the fired income row / "Church Tithes" at `economicState.js:215-233` unread) · `whatItDoes` reads service NAMES only (`desc` captured at `:152`, used nowhere) · `whatItDoesNotDo` never reads `_worldPulseInactive` and measures **held = 0 on all three census tiers** — a closed, empty column. `holderRole` is hardcoded `null` against §1.2's `basis:'inferred'`. **Not in the receipt at all** | HIGH | 3 |
| R19 | plant #77's stated consequence: closing `whoIsCounted` licenses the Brackwater kicker everywhere | nothing reads the table's `closed` flags at this tip — `settlementGround` has **zero callers**, `entryGround` names `institutionTable` in a **comment only** (line 32), the corpus walk uses hand-built `estateGround` columns. Closing the column reds one assertion and changes no licence anywhere | MED | 3 (h) |
| R20 | §5.1: 177 nouns in five buckets, each noun in exactly one — "the spread figure is a partition" | measured `SENSORY_NOUNS`: 82+26+17+25+19 = **169 entries, 166 distinct**; `smoke`, `stone`, `mud` are published in TWO buckets. Reassigning the three moves R1 spread **0.791 → 0.848 bits** — the one number the §5.1 finding rests on, and *sight* (the bucket the finding indicts) absorbs all three. The test's partition assertion iterates `SENSE_OF_NOUN`'s values and **cannot fail**. The mutation manifest's coverage label for plant #78 asserts the same false premise | HIGH | 4 (a1) |
| R21 | §5.1: "R1 24% textured against leguin-fiction's 56%" | a unit artefact: R1's unit is one variant (2,266 units, **20.6 words**); leguin-fiction's is a paragraph (16 units, **177.0 words**, 8.6×). Re-chunked to the exemplar's own grain R1 reads **79% against 56%** — the opposite sign. Lines 1 and 3 are unit-invariant (1.198, 0.791 at every chunking), so the headline survives on 1 and 3 and fails on 2. The class the chair already ruled on in Part B §16.2 | HIGH | 4 (a2) |
| R22 | §2.11 / Part B §13.2: the presence measure extends `proseFingerprint.js` with three keys and the fourteen are re-run | `grep -n "presence\|sensory\|textured\|senseSpread" src/domain/prose/proseFingerprint.js` → **no output**; the keys live in a separate module. `grep -l sensory $K/primary/*.fingerprint.json` → **0 of 14**. Undeclared shortfall — refusal #18 declares only the other half | MED | 4 (a3) |
| R23 | §5.2 / refusal 19 / SITTING §K.8: 59 is an upper bound on the authoring wave's opportunity | the census is a regex scan of six composer sources for `readings.X`/`settlement.X`. A generated town carries **41 top-level settlement keys**; the six composers name **9**. **32 top-level keys are named by no composer**, so every fact §15's own example points at (a timber town's sawpits) is invisible by construction. It is per FILE (six rows), not per (block, pool), and no block appears in the output. Distinct held **59**, distinct rendered **8** (`settlement.name` supplies 6 of the 13). Seven entries are config, ids or diagnostics; one (`defenseProfile`) stands for a whole object | HIGH | 4 (b) |
| R24 | §5.3 / refusal 20 / §9: "A12's 20 is measured as 22 — corrected" | A12's own measure — the slots the VARIANTS carry — reproduces **EXACTLY 20** at this tip, listed. The lane measures a different quantity (blocks whose composer BAG offers settlement alone). The two sets overlap in **11**: 9 A12-only, 11 lane-only. Neither number is wrong; the *correction* is. **SITTING §K.5's ruling rests on the false premise.** Four of A12's twenty are silently dropped into the lane's "no bag at all" set | HIGH | 4 (c) |
| R25 | §6.2: a constant open share of 1.00 is exactly the condition D8's walker is designed to fail on | **`walkPlantLedger` has no open-share arm in either direction.** `openShare` is computed at `plantLedger.js:124` and read by nothing; executed control at open share 0 (D8's explicit fail condition) → `fails [] · notExec []`, and the shipped test asserts that silence as lawful. The "does not vary across seeds" limb is implemented nowhere — the walker sees one ledger | HIGH | 4 (d3) |
| R26 | the owner's ADDENDUM 19:01 — the wiring census | `grep -rn "WIRING-UNRESOLVED\|wiringUnresolved\|WIRING_UNRESOLVED"` over `src/domain/prose`, `tests/lint`, `tests/helpers`, `tests/fixtures`, `scripts` → **no hit**. `fillSites()` delivers the SLOTS third only; no reading function recorded, no pool-key predicate extracted, no field/value printed. The receipt never uses the word "addendum". **Neither built nor refused** | HIGH | 4 (e) |
| R27 | §8.1: the arm is 17.76 s in isolation, 89% of its own ceiling | two isolation runs, gate clean: **3.80 s / 3.79 s** whole file, 34/34 green; DOOR 3 costs **2,928 ms ≈ 14.6%** of the per-test 20,000 ms ceiling. Two compounded errors: `testTimeout` is per-test, so a whole-file duration is a category error; and the file figure does not reproduce. The *disposition* survives on the attribution arithmetic (5/2548 = 0.196%; 21,558 − 42 = 21,516 ms, still over) — the observed 21.5 s implies ~7× contention, not a hair-trigger arm | MED | 5 (D2) |
| R28 | §3.2 / §9: "R4b = 50, PROBE_ALL's number to the unit" — one of two exact reproductions asserted as integers | `loadHeraldDisclosure` declares three exports from `causeWalk.js` (`NO_DEEPER_MEMORY`, `LEDGER_DARK_LINE`, `REDACTED_HOP`); all three are **bare strings** and `harvestExports`'s walk returns early on a non-object node. `any row from causeWalk.js: false`. The loader's own `isProse` predicate admits **53, refuses 0** — so **50 = 53 − 3**, and the 3 are lost to a silent drop, not a declared predicate. R4b is the only loader roster with this shape | MED | 5 (I2) |

## 2b — PARTLY (21)

| # | claim | the correction | sev | lens |
|---|---|---|---|---|
| P1 | §1.2: fixture (a) is "§2.4(a) exactly" | §2.4(a) names ONE office fail (the bailiff); the walker returns **two** (bailiff, priest). Entailed by an empty office column, so lawful — but one more fail than the spec's table names | LOW | 1 (a) |
| P2 | §1.5: "the walker gates the instrument, never the corpus" | three assertions bind the corpus in the other direction — the three breach TEXTS verbatim, `failing > 4`, `failing < corpus.length / 4`. The file gates the corpus's **dirtiness** | MED | 1 (e) |
| P3 | refusal 3: C5's office/status limbs downgraded to WITHHELD | legitimate but incomplete: §2.2 C5 names four mechanisable typed facts including **quantifiers**; `typedFactsOf` computes `quantifiers` and **nothing compares them** — not `armC5`, not the C-sibling (which reads `bands` only). The limb is silently absent, not declared | MED | 1 (g3) |
| P4 | §1.9: the modules are "imported today only by a test" | drifted at the tip — `grammarWalker.js:45-46` imports `entryWalker.js` and `entryLexicons.js`. No product path imports either, so THE PROMISE claim survives intact | LOW | 1 (F6) |
| P5 | refusal 6 / §2.4: the classifier reports and does not gate | three qualifications: (i) `classifyMoves` feeds a FAIL channel through **arm F** — F1 and F3 are computed from the move sequence and both appear in the walk's own `fails by arm` line, so 0.75–0.83 precision already reaches findings; (ii) `walkGrammar` gates arm A the moment ANY caller supplies `ceilings` — no tagged/untagged split exists; (iii) three assertions turn on classifier output | MED | 2 (b) |
| P6 | §2.6: the per-paragraph refinement "reproduces the sitting exactly" | every figure reproduces; the word does not. SITTING §I reports median **3.5**, the script prints 4 (`ns[floor(n/2)]`, an upper median); the sitting's 0.52 is the **p90 of pooled exceedance depths**, the script's the **median of per-record MAX depths** — one number, two statistics. Ten records carry **40** exceedances, the sitting says 41 | MED | 2 (d) |
| P7 | `moveGrammar.js` header: the hand-tagged sample is `tests/fixtures/grammarHandTagged.js` | the file is `tests/fixtures/grammarControls.js`; no such path exists in the dock | LOW | 2 |
| P8 | §3.2: R9's disagreement is caused by the predicate alone | the loader also reads 5 of PROBE_ALL's 6 copy files; the omitted `src/copy/support.js` exports only `SUPPORT_EMAIL` and `supportMailto()`, so nothing is lost — harmless but unstated | LOW | 3 (a) |
| P9 | §4.2 / refusal 16: the `availableServices` finding is "a spec correction" | **CLERK-LAWS §1.2 never names `settlement.services`** — its `whatItCounts` cell reads "the settlement's INSTANTIATED services" and names the menu file, and `availableServices` **is** the instantiated services. A field-resolution finding; **no chair ratification is owed for it**. The measurement itself is real: schema `:273` declares `[services]`, and over 30 settlements `services` holds **0** rows against `availableServices`' **1,678** (1,655 live-roster, 32 duty-kind) | MED | 3 (d) |
| P10 | §4.6: the no-writer fence | not vacuous (car 7 repaired exactly this; every negative sits behind a live anchor in the same `it()`), but the regex `/settlement\.\w+\s*=[^=]/` catches only a variable **literally named `settlement`** — a writer through any other binding passes | LOW | 3 (g) |
| P11 | the ruin filter guards the table | routed for the ROWS (`liveInstitutions`) and **not for the service COLUMNS**, which are built from unfiltered `instantiatedServices()`. Over 30 settlements **23 service rows name an institution absent from the live roster** — `(lawless)`, `(informal)`, `(street gang)`, `(arcane underground)`, `(smuggling)` — and `census-city`'s `whatItDoes` carries `Arcane services (illicit)`. **Zero of the 23 are duty-kind**, so the Brackwater class does not fire today. Latent; belongs beside §1.2's "a ruined citadel has no wall duty" | MED | 3 |
| P12 | §4.8: the seam is one argument still to be threaded | wider — **no wire exists at all**. Recorded vetoably rather than dropped, but the framing understates it | MED | 3 (i) |
| P13 | Part B §13.2's unit and device set | proxied: line 1 says "per hundred words of the **rendered tab**" and line 2 "the share of a **tab's** paragraphs" — the walker measures per REGISTER over authored variant pools; **no tab is measured anywhere**. Line 2's device set ("an OBJECT move, a named civic thing resolving to a field, or a comparison as a measurement") is implemented as lexicon-noun-present OR comparison-regex — no field resolution — so line 2 collapses into a near-duplicate of line 1. Neither deviation is in the refusal list | MED | 4 (a4) |
| P14 | refusal 19: a key-only fact is not dark; 59 is an upper bound | "not dark" is right and is a good correction; "an upper bound on the wave's opportunity" is wrong (see R23). The module's own docblock is honest ("It measures REACH, from source"); the receipt's and the sitting's framing is not | HIGH | 4 (f) |
| P15 | §9: `UNMOUNTED_BLOCKS` is "an orthogonal, hand-kept registry" | hand-kept but **not orthogonal** — it lives in the composers' own directory (`src/domain/display/stateProse/dossierMounts.js`) and its docblock says each desk car strikes its blocks as it mounts them, so the 15/15 agreement is close to definitional | MED | 4 (c) |
| P16 | "at n = 1 arm A is not-executable by construction" | `armA(orders, unit, admissible, shape)` takes `admissible` as an ARGUMENT; n ≤ 2 returns not-executable only if the caller passes the licensed-member count as n — and arm A's own corpus walk instead counted **180 realised orders**. A design intention, not a construction | MED | 4 (c) |
| P17 | §6.2: reporting rather than failing is licensed by D8's own clause | the behaviour is licensed; the citation is wrong twice. (i) SITTING §K.9 **postdates the receipt** and is a ruling *on* it, so it cannot be its independent authority. (ii) Part B §5 D8's Statement says the walker **fails** when the open share is zero or does not vary, with **no proviso**; the nearest text is the rule's "Shipped… after the ledger exists". The "only once the class exists" sentence is the **BRIEF's**, not D8's | MED | 4 (d2) |
| P18 | §7.1's gloss: the six reds are "all four Brackwater tables and the anti-vacuity guard" | loose by one — the six are tables **(a)**, **(b)**, **(d)**, the fixture-discrimination test and the **two** anti-vacuity tests. Table **(c)** stays green, correctly: its persons column is CLOSED and its expected result is already zero quantifier fails. The "6 of 19" is exact | LOW | 5 (A3) |
| P19 | car 7 item 6 / the commit body: the manifest change is five added entries | semantically true — `_doc` 16 identical, `rationales` 41 identical, `uncoveredBaseline` 186, `meta` 12 keys identical, `invariants` **661 → 666**, added exactly 5, **removed 0, changed 0**. But the diff moved **5,306 lines** on a 2,666-line file: **the other 5,286 are a pure re-serialization** — indent 2 → 1 space (every other JSON under `scripts/` is 2-space) and `invariants` key order insertion → alphabetical. **Undeclared**, and not a tool's doing: no writer for the file exists anywhere in `scripts/`, `tests/`, `src/` or `package.json`. The three preceding commits that touched it moved 4, 5 and 142 lines | HIGH | 5 (B) |
| P20 | §8.1: `vitest.config.js:904` carries `testTimeout: 20000` | the value and the line are exact; the file does not exist. It is **`vite.config.js:904`**. (Independently noticed by lenses 2 and 4 as well — counted once) | LOW | 5 (D1) |
| P21 | the brief's seat/lane trailer requirement | seat trailer **CONFIRMED** on all seven commits (`Seat: Opus 5 — Fable-unvalidated` + `Co-Authored-By`); there is **no `Lane:` trailer** on any commit — the lane id appears only in each SUBJECT line. A lane trailer was never specified, so the brief is satisfied | LOW | 5 (E) |

## 2c — `†` CONFIRMED-labelled rows that nonetheless correct the receipt (not counted above; cures still owed)

| claim | the correction | sev | lens |
|---|---|---|---|
| §2.5: the absence-pool hazard is avoided | **a real, undeclared instance.** The probe passes three of the four readings `PowerTab.jsx` passes; `politics` is omitted, and `politicsPresencePoolKey(null, …)` does not return null — it returns `'layer DORMANT (no ledger materialized)'`. **DS-POW-7 draws that pool in 3 of 3 towns, one line per town (~10% of the sequence), the SAME pool key on every seed.** `gen-probe2.mjs`'s comment ("DS-POW-7 goes silent") is wrong and the receipt inherits it. The general desk is called with `{seed, audience}` only, so `stresses`, `populationTrend`, `neighbours`, `steadings`, `ancientRuin`, `lifecycleStatus`, `crossEngagements` are all absent. None of the three limits is stated in §2.5 | HIGH | 2 (c2) |
| the anti-vacuity guard is built on the spec's four anchors | **CLERK-LAWS §2.4 names a different four.** §2.4's guard list requires `:5233` (C2), **`:2247` (C3-lexical)**, `newsVoice.js:97` (C4 totality) and **the shipped gendered R6 lines** (C3 arm a). §5's amendment revises **§1.3's** live-breach count to four and amends §2.4 only on the FIXTURES; §2.4's guard list is unamended. The lane built on the §1.3 four, so **two spec-named anchors are unasserted** — consistent with the census reporting **zero C3 findings of any kind** over 3,132 entries. Absent from all twenty-four refusals | MED | 5 (I1) |
| the anti-vacuity guard is stable | it is not silenceable today (a zero-breach corpus reds with a named message and the census test reds at `failing > 4`) — **and that is the same reason the reconstruction wave will red this file the moment it cures `newsVoice.js:97` or either exemption line.** The cure is planned work; the receipt names no successor plan | HIGH | 1 (c) |
| `plantLedger.js`'s open share | `openShare = (answerable − answered − withGap)/answerable` **double-subtracts** a plant carrying both: executed, one such plant gives **openShare = −1**, outside [0,1], which any future arm would read. And `plantLedger.js:37` declares `@enforced-by tests/lint/plantLedger.walker.test.js` — **that file does not exist**; the arms live in `proseMeasures.walker.test.js`. `presenceMeasure.js` carries no `@enforced-by` at all | MED | 4 (d4) |

---

# 3 · THE CURES OWED BEFORE THE §914 LANDING — 60

Each is stated as a change to a named file with the arm that proves it. `BLOCKING` = owed before §914 lands; `WAVE` = owed before the reconstruction wave reads the instrument's output as truth.

## 3a — CODE CURES (18)

1. **BLOCKING · HIGH — `src/domain/prose/entryGround.js`.** `deriveOfficeRoster()` must take the union of every role source — `src/data/historyData.js` (`POWER_ROLES_BY_CATEGORY`, whose rows carry `role`/`title`, exactly the shape the harvester wants), `src/generators/roleCategory.js` (`ROLE_CATEGORY_KEYWORDS`), `src/generators/npcGenerator.js` (`ROLE_FACTION_MAP`) — or armC2 must WITHHOLD where a role-category keyword holds the noun. **ARM:** a case in `tests/lint/proseEntryContradiction.walker.test.js` asserting the three `VOICE_LINES::authority::{onset,relief,fade}#3` entries return no C2 office fail, and the census row `C2 · an office the world does not hold` falls **3 → 0**. Re-run `probe10.mjs`'s 37-candidate scan and assert the absent set shrinks from 25. *(R1)*
2. **BLOCKING · MED — `src/domain/prose/entryWalker.js`.** armC4 must locate the quantifier with the same word-boundary regex `holds()` uses, not `masked.indexOf(q)`; `bandReadings` shares the defect and must move with it. **ARM:** the four `probe4.mjs` sentences as fixtures — "The wall is old, and all souls are counted here." must FAIL `C4/a totality over an open column`, not NOTE. *(R3)*
3. **BLOCKING · MED — `tests/helpers/dossierCorpus.js`.** `harvestExports`'s `walk` must treat a bare-string export as a pool of one (the object branch already does), so `loadHeraldDisclosure` reads `NO_DEEPER_MEMORY`, `LEDGER_DARK_LINE`, `REDACTED_HOP`. **ARM:** the R4b row moves **50 → 53** in `tests/lint/proseRegisterLoaders.walker.test.js`, plus an assertion that at least one row's file is `causeWalk.js`. ⚠ **This legitimately changes a headline integer and voids "R4b = 50, PROBE_ALL's number to the unit" as an exact reproduction** — a one-time shift to state in the landing, not to let ride. Settle U4 first. *(R28)*
4. **BLOCKING · MED — `tests/helpers/dossierCorpus.js`.** Settle the chronicler's-letter register: the loader stamps `R11` at `:646/:650/:656`, the census heads the row R12, and 11 of 85 rows disagree with the label. **ARM:** whichever label the chair rules, an assertion that all 85 chronicle rows carry ONE register value. *(R13)*
5. **BLOCKING · MED — `tests/lint/proseRegisterLoaders.walker.test.js`.** Drive `refuseEmpty` once — a stub module through `harvestExports` that yields zero rows. **ARM:** a `toThrow` case beside the two existing fail-closed cases, so all three paths are driven and §3.4's claim becomes true. *(R12)*
6. **BLOCKING · HIGH — `src/domain/institutions/institutionTable.js`.** Narrow `whatItCounts`, `whatItDoes`, `whatItDoesNotDo` to `closed: false` until their CLERK-LAWS §1.2 sources are actually read, or read the sources (the `economicState.js:215-233` income row; the catalog `desc` captured at `:152` and unused; `_worldPulseInactive`). **ARM:** `tests/lint/institutionTable.walker.test.js` asserts each column's `closed` flag against the list of sources the module actually imports — a closed flag with an unread source reds. Plant #77 already covers `whoIsCounted`; a sibling plant per column makes the arm non-vacuous. *(R18 — this is the Brackwater fault inside the instrument built to prevent it)*
7. **WAVE · MED — `src/domain/institutions/institutionTable.js`.** Route the ruin filter through the service COLUMNS as well as the rows. **ARM:** an assertion that no column value names an institution outside `liveInstitutions`; today it reds on 23 rows over 30 settlements and on `census-city`'s `Arcane services (illicit)`. Latent (0 duty-kind today). *(P11)*
8. **BLOCKING · HIGH — `tests/lint/institutionTable.walker.test.js`.** Give the `whoIsExempt` arm a world: pass a `treatyTerms` fixture holding one toll exemption. **ARM:** the column must CLOSE on it — a positive twin that reds when the arm is removed, so the negative stops being asserted about nothing. *(R17)*
9. **BLOCKING · HIGH — `src/domain/prose/presenceMeasure.js`.** Either de-duplicate `SENSORY_NOUNS` (`smoke`, `stone`, `mud`) or withdraw the partition claim from the module docblock **and** from the mutation manifest's coverage label for plant #78. **ARM:** replace the vacuous partition assertion in `tests/lint/proseMeasures.walker.test.js` (it iterates `SENSE_OF_NOUN`'s values and cannot fail) with a count over the five bucket ARRAYS asserting entries == distinct nouns; today that reds at 169 vs 166. *(R20)*
10. **BLOCKING · MED — `src/domain/prose/plantLedger.js`.** Fix `openShare` double-subtracting a plant that carries both an answer and a gap-reason. **ARM:** a control in `proseMeasures.walker.test.js` with one such plant asserting `openShare` within [0,1]; today it measures −1. *(2c)*
11. **BLOCKING · LOW — `src/domain/prose/plantLedger.js:37`.** Correct `@enforced-by tests/lint/plantLedger.walker.test.js` (nonexistent) to `tests/lint/proseMeasures.walker.test.js`, and add the missing `@enforced-by` to `src/domain/prose/presenceMeasure.js`. **ARM:** the estate's existing enforced-by walker. *(2c)*
12. **BLOCKING · LOW — `src/domain/prose/moveGrammar.js`.** The header points the hand-tagged sample at `tests/fixtures/grammarHandTagged.js`; the file is `tests/fixtures/grammarControls.js`. **ARM:** a path-existence assertion in the walker test's docblock check. *(P7)*
13. **WAVE · MED — `src/domain/prose/grammarWalker.js`.** Arm D must be keyed on (block, pool) as its own comment and gap (e) require, or the comment must be corrected and gap (e) declared unmet; gap (a) must become the per-POOL count SITTING §J asked for, not a register total. **ARM:** build the composed-fill map in `tests/lint/proseMoveGrammar.walker.test.js` from two pools of ONE block with different licences and assert they resolve differently. *(R9)*
14. **WAVE · HIGH — `src/domain/prose/grammarWalker.js` + `tests/lint/proseMoveGrammar.walker.test.js`.** Give arm E a fail channel (MOVE-GRAMMAR §4.4 control 4 requires today's R1 leaves to RED on it); give arm B2 a control that can fire; make BUDGET and SPREAD assert something failable; and emit a NOT-EXECUTABLE row for each of walls 4, 5, 7, 8, 9, 10. **ARM:** each new control must red when its arm is stubbed — the present-then-absent shape the file already uses. *(R8)*
15. **BLOCKING · MED — `tests/lint/proseEntryContradiction.walker.test.js`.** Add the Q assertion the brief's named QUALIFY positive control requires. **ARM:** `power.generated.js::DS-POW-5::…#2` must WITHHELD or NOTE; today it returns `PASS` with three empty channels because `armQualify` returns early on `sentences.length < 2`. Either the arm reads trailing coordinates within one sentence, or the omission is declared. *(R2)*
16. **BLOCKING · MED — `tests/lint/proseEntryContradiction.walker.test.js`.** Add the two CLERK-LAWS §2.4 guard anchors the lane omitted: `RECEIPT_POOLS_DOSSIER_STATE.md:2247` (C3-lexical) and the shipped gendered R6 lines — loadable now that car 3 ships `loadNpcLadder`. **ARM:** present-then-absent, exactly as the existing four; the C3 class currently reports **zero findings over 3,132 entries** and nothing proves that is a fact rather than a dark arm. *(2c)*
17. **WAVE · HIGH — `tests/lint/proseEntryContradiction.walker.test.js`.** Write the successor plan into the file's docblock: the wave reds this gate the moment it cures `newsVoice.js:97`, `RECEIPT_POOLS_DOSSIER_STATE.md:5233` or `factionDynamics.js:466`. **ARM:** a named replacement-anchor procedure plus `failing > 4` re-expressed as a floor the wave can lower deliberately, with the cure recorded. *(2c)*
18. **WAVE · HIGH — the reading-sequence probe and its caller.** The composed-dossier reading must pass the fourth reading (`politics` — `politicsPresencePoolKey(null,…)` returns `'layer DORMANT (no ledger materialized)'`, not null) and the general desk's seven caller-supplied readings, and must harvest `generalDeskLines`'s bare sentence strings. **ARM:** re-run the sequence over 3 towns and assert **> 1 composer**, **> 7 blocks**, and **0 draws from the DORMANT pool key**; today it is 29/29 from `powerStateProse` with DS-POW-7 on the absence pool in 3 of 3 towns. *(R7, 2c)*

## 3b — RECEIPT CORRECTIONS (30) — all to `$SC/receipt-instr-912.md`

19. §1.2 — fixture (a) returns **two** office fails (bailiff, priest); §2.4(a) names one. Drop "exactly". *(P1)*
20. §1.4 — withdraw the three reeve breaches; state the census row `3 C2` as **3 false positives, 0 true**, and record the 25-of-37 under-read. *(R1)*
21. §1.5 — replace "the walker gates the instrument, never the corpus" with the precise claim: it gates the corpus's **dirtiness** through three verbatim breach texts, `failing > 4` and `failing < corpus.length/4`. *(P2)*
22. §1.6 — declare the C5 **quantifier** limb: `typedFactsOf` computes it and nothing compares it. *(P3)*
23. §1.6 — declare the QUALIFY omission: no Q assertion exists in the gate file. *(R2)*
24. §1.9 — "imported today only by a test" has drifted; `grammarWalker.js:45-46` imports both car-1 leaves at the tip. THE PROMISE claim survives; the sentence does not. *(P4)*
25. §2.8 — remove `A` from the printed `fails by arm` list; the tip prints nine arms without it. *(R6)*
26. §2.5 — restate as ONE composer and 7 blocks of 68; mark V1 78.4% / run 0.618 a **power-desk** figure; name the three limits (bare-string general-desk lines unharvestable, `politics` omitted, the general desk called with `{seed, audience}` only) and the DS-POW-7 absence-pool draw in 3 of 3 towns. *(R7, 2c)*
27. §2.6 — drop "exactly": 3.5 vs 4 (upper median), p90-of-pooled vs median-of-per-record-max for 0.52, 40 exceedances vs 41. *(P6)*
28. §2.9 — declare arm E's absent fail channel, arm F's 4-of-10 walls, arm B2's silence-only control, BUDGET/SPREAD's non-failing assertions. *(R8)*
29. §2.9 — declare gap (e) as block-keyed and gap (a) as a register total. *(R9)*
30. §2.9 / refusal 6 — add the three qualifications to "arm A does not gate": arm F carries classifier output into the FAIL channel, arm A gates the moment a caller supplies `ceilings`, and three assertions turn on classifier output. *(P5)*
31. §3.2 — restore the R9 singleton-pools cell to **190**; the walker prints an integer every run. *(R10)*
32. §3.2 — R7's cause is the looser predicate ALONE; R9's cause gains the (harmless) omission of `src/copy/support.js`. *(R11, P8)*
33. §3.4 — withdraw "each control MUST fire, and each does"; `refuseEmpty` is driven by no test in the estate. *(R12)*
34. §3.5 / refusal 15 — the shipped loader stamps `R11` on 11 of 85 chronicle rows; the R12 correction is contradicted by the code it describes. *(R13)*
35. §4.2 / refusal 16 — reframe as a **field-resolution** finding, not a spec correction; CLERK-LAWS §1.2 never names `settlement.services`. *(P9)*
36. §4.2 — mark the 702 / 697 / 19 over "twelve settlements" and the "0 over 20 settlements" figures **UNTESTED** (the twelve are never named) or re-take them under named seeds. *(U2)*
37. §4.3 — withdraw "asserted in the test, not assumed" for the `whoIsExempt` half. *(R17)*
38. §4.4 — **re-take the whole column census from the shipped gate** and print the executed figures (hamlet 19 / 1 `Record keeping` / 23 / "several hundred"; town 57 with Register of the dead · Tax collection · Toll collection; city 48 with Custom commissions; provenance one value `PRE_SEED` ✔). *(R14)*
39. §4.4 — **withdraw the "a hamlet's duty column is empty" finding.** *(R15)*
40. §4.5 — correct the fall to **12 → 11**, name the thirteen craft services the bare `custom` stem admitted, and restore `Custom commission` and **`Tithe and dues`** to the list. Record the residual latent admissions (`Customs brokerage`, `Public record access`). *(R16)*
41. §4.6 — state the no-writer regex's narrowness (only a binding literally named `settlement`). *(P10)*
42. §4.8 — state that **no wire exists**: `settlementGround` has zero callers, `entryGround` names the table in a comment only, the corpus walk uses hand-built `estateGround` columns. *(P12, R19)*
43. §4.7 / §4.4 — add the ruin-filter column gap as a declared limit (23 rows, 0 duty-kind, latent). *(P11)*
44. §5.1 — the lexicon is **169 entries / 166 distinct nouns**, not 177 in a partition; name the three dual-bucket nouns and the 0.791 → 0.848 sensitivity. *(R20)*
45. §5.1 — withdraw the 24%-vs-56% textured comparison or restate it at a matched unit (79% vs 56% at 177-word chunks); note lines 1 and 3 are unit-invariant. *(R21)*
46. §2.11 / §5.1 — state that `proseFingerprint.js` did **not** gain the three keys and **0 of 14** fingerprint files were re-run; add it as a refusal. *(R22)*
47. §5.1 — declare the per-REGISTER (not per-tab) unit and the proxied device set. *(P13)*
48. §5.2 / refusal 19 — replace "an upper bound on the wave's opportunity" with what it is: a count of fields the six composers already read but do not word (distinct held 59, distinct rendered 8; 32 of 41 top-level keys named by no composer; per FILE, not per (block, pool)). *(R23, P14)*
49. §5.3 / refusal 20 / §9 — **withdraw "A12's 20 is measured as 22 — corrected"**; print both quantities, the 11-block overlap, and the four A12 members silently dropped. *(R24)*
50. §9 — `UNMOUNTED_BLOCKS` is hand-kept but **not orthogonal**; the 15/15 agreement is close to definitional. *(P15)*
51. §5.3 — "at n = 1 arm A is not-executable by construction" is a design intention; the corpus walk counted 180 realised orders. *(P16)*
52. §6.2 — **withdraw** "a constant open share of 1.00 is exactly the condition D8's walker is designed to fail on"; `walkPlantLedger` has no open-share arm, the executed control at open share 0 returns `fails []`, and the cross-seed limb is unimplemented. *(R25)*
53. §6.2 — correct the citation: the "only once the class exists" proviso is the **brief's** sentence, not D8's, and SITTING §K.9 postdates the receipt. *(P17)*
54. §6.1 — record the `openShare` double-subtraction (−1 measured) and the nonexistent `@enforced-by` target. *(2c)*
55. §7.1 — correct the six-red gloss: tables (a), (b), (d), the fixture-discrimination test and the two anti-vacuity tests; (c) stays green correctly. *(P18)*
56. §7.1 / car 7 item 6 — **declare the manifest's 5,286-line re-serialization** (indent 2 → 1 space; `invariants` insertion order → alphabetical; 661 → 666 keys, 5 added, 0 removed, 0 changed) and state that no writer for the file exists. *(P19)*
57. §8.1 — `vite.config.js:904`, not `vitest.config.js:904`. *(P20)*
58. §8.1 — **withdraw** "17.76 s in isolation … 89% of its own ceiling" (measured 3.79/3.80 s whole file; DOOR 3 at 2,928 ms ≈ 14.6% of a per-test ceiling) and keep the disposition on the attribution arithmetic, which is right. State the ~7× contention reading. *(R27)*
59. §10 — add the missing refusals: the wiring addendum neither built nor refused; no per-tab measurement; the two CLERK-LAWS §2.4 guard anchors unasserted; the three `closed: true` columns on partial fills; the R4b bare-string under-read; the census's per-FILE shape. *(R18, R22, R26, R28, 2c)*
60. §11 — add to the OWED table: the owner's wiring census, the anti-vacuity successor plan, the R11/R12 label ruling, and the four SITTING re-puts (K.2, K.5, K.8, K.9). *(§5 below)*

## 3c — SPEC CORRECTIONS THE CHAIR MUST RATIFY (12)

61. **CLERK-LAWS §1.2 — withdraw the ratification request, not the finding.** `settlement.services` → `availableServices` is a field-resolution finding; §1.2 names the instantiated services and the menu file, never the field. **Both spec files are unmodified** — `CLERK-LAWS.md` mtime `2026-09-07 17:16:34`, `MOVE-GRAMMAR.md` `18:33:37`, both before the lane's 18:43 arrival; the directory is not a git repo, so mtime is the available evidence. *(P9)*
62. **CLERK-LAWS §1.2 vs `institutionTable.js` — rule on the three `closed: true` columns** whose fills omit sources §1.2 names, and on `holderRole` hardcoded `null` against §1.2's `basis:'inferred'`. Either narrow to `closed: false` or read the sources; as shipped they over-license a quantifier and refuse a duty the world holds. *(R18)*
63. **CLERK-LAWS §2.4 — amend the guard list or charter the two missing anchors** (`:2247`, the shipped gendered R6 lines). §5's amendment revised §1.3's count and §2.4's fixtures, not §2.4's guard roster. *(2c)*
64. **MOVE-GRAMMAR §4.4 control 4 — ratify the arm-E deviation or charter the fail channel.** The control requires today's R1 leaves to RED on arm E; arm E emits notes only and the test bypasses it via `segmentCount`/`openerOf`. *(R8)*
65. **MOVE-GRAMMAR `WALLS` — ratify a 4-of-10 scope or charter the six.** Walls 4, 5, 7, 8, 9, 10 have no detector, no control and no NOT-EXECUTABLE row; wall 10's raw material counts 103 of 400 variants. *(R8)*
66. **MOVE-GRAMMAR / gap (e) and gap (a) — ratify the block-only key and the register total**, or charter the per-(block,pool) and per-pool forms SITTING §J asked for. *(R9)*
67. **SITTING K.2 — the tagged/untagged split is NOT implemented and untested.** `walkGrammar` gates arm A on classifier-derived orders the moment any caller supplies `ceilings`. Ratify or charter. *(P5)*
68. **SITTING K.2 — re-put the wave's case.** The V1 78.4% / run 0.618 figure it carries is a power-desk figure from one composer and 7 blocks of 68. *(R7)*
69. **SITTING K.5 — re-put "A12 CORRECTED: 22, not 20".** A12's own measure reads exactly 20 at this tip; both numbers stand, the ruling's premise does not. *(R24)*
70. **SITTING K.8 — re-put "59 is an upper bound on the authoring wave's opportunity".** *(R23)*
71. **Part B §13.2 — ratify the per-REGISTER proxy or charter the tab-level measure**, and rule on the unmet half ("the fingerprint tool gains the three keys and the fourteen are re-run"). *(R22, P13)*
72. **Part B §5 D8 / §912.1 condition one — two rulings.** (i) D8's Statement carries no "only once the class exists" proviso; ratify the report-only behaviour against D8's actual text. (ii) Rule whether §912.1's condition one is discharged by a per-FILE census that cannot see a fact no composer names. *(R25, R23)*

---

# 4 · THE REBASE HAZARDS ONTO THE §913 LANDING

**Measured, not assumed** `[FOLD]`: `git diff --name-status 3b1c0eaa5..74a1aa0e8` is **20 files — 17 additions, 3 modifications**. The three modifications are `scripts/mutation-coverage-manifest.json`, `scripts/mutation-sweep.sh`, `tests/lint/.lighting-census-baseline.json`. **INSTR-912 touches neither `scripts/.writer-reach-baseline.json` nor anything under `supabase/`** (`git diff --name-only … | grep -E "supabase|writer-reach"` → 0).

| file | §913 does | INSTR-912 does | merge by TEXT |
|---|---|---|---|
| `scripts/mutation-coverage-manifest.json` | +1 entry | +5 entries **and a whole-file re-serialization** | **H1 — do not textual-merge** |
| `scripts/mutation-sweep.sh` | +plant #74 | **two hunks** | **H2 — keep both, in both hunks** |
| `tests/lint/.lighting-census-baseline.json` | refreeze | refreeze | **H3 — take either side, then REGENERATE** |
| `scripts/.writer-reach-baseline.json` | refreeze | untouched | **H4 — take §913's whole; discharged by execution** |
| `supabase/functions/_shared` | rebuild | untouched | **H5 — take §913's whole; watch the symlink** |

**H1 · `scripts/mutation-coverage-manifest.json` — conflicts across essentially the whole file.** `--numstat` reads **2,663 insertions / 2,643 deletions** on a 2,666-line file. Reproduced independently `[FOLD]`: base line 2 begins at indent **2**, tip at indent **1**; base `invariants` has **661** keys beginning `tests/lint/customContentCharsetWiring.test.js` (insertion order), tip has **666** beginning `tests/application/commands/commandRegistry.walker.test.js` (fully sorted); `hazard-registry.json` and `premortem-retro.json` are both 2-space, so the manifest is now the only `scripts/` JSON that is not. **Merge:** take the **tip's** file wholesale and insert §913's single `{"kind":"mutation","label":…}` into `invariants` in **sorted** position at **1-space** indent. **Better, and my recommendation to the chair:** whichever consist lands first normalizes the file back to the house 2-space + insertion-order form and declares it, so the estate stops carrying an undeclared formatting fork on a signed-surface-adjacent file. **ARM:** `tests/lint/mutationCoverageManifest.test.js` parses it, plus the label join — 89 sweep labels (89 distinct), 79 manifest mutation entries (79 distinct); the 1-and-11 join asymmetry is **identical at the base**, so it is pre-existing and must not be "fixed" during the merge.

**H2 · `scripts/mutation-sweep.sh` — TWO hunks, and a lens missed one.** `git diff -U0` gives `@@ -46,0 +47,5 @@ MUTATED_FILES=(` and `@@ -1017,0 +1023,41 @@`. Gate-proof §B says "the lane appended at the end of the file, and §913 will too" — that is **only the second hunk**. The first **inserts five `src/` paths at the TOP of the `MUTATED_FILES` array**, and §913's plant adds its own target at the same anchor: a same-line conflict both consists will hit. **Merge:** keep both blocks in both hunks — the array order is irrelevant (it is a bash array), and the trailing `check_caught` blocks concatenate, INSTR-912's 41 lines first. **Numbering — gate-proof's rule is a nicety, not a requirement `[FOLD]`:** it says a §913 block numbered `# 74` must be renumbered to `# 79` or later, but the sweep **already carries duplicate comment numbers at the BASE** — `# 37 # 38 # 39 # 47 # 48 # 72 # 73`, each twice — and the tip adds `# 74`–`# 78` as second copies. The counter is per-section and local; the **`check_caught` LABELS are the joined key** and are unique (89 of 89). So renumber for tidiness if you like, but **the merge requirement is only that the labels stay distinct across both consists**.

**H3 · `tests/lint/.lighting-census-baseline.json` — 9 insertions / 9 deletions; both consists refreeze it, and neither side's tuple survives.** Do **not** merge by text and do **not** hand-arithmetic the five figures: the file's own docblock forbids a hand-composed number as "exactly the 'stale numeral' class the walker exists to refuse", and prescribes taking either side then regenerating. **Merge:** take either side wholly, then on the **merged** tree, with a **clean** working tree, run the refreeze ritual (`LIGHTING_CENSUS_REFREEZE='<lane>' LIGHTING_CENSUS_NOTE='<why>' npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js`) — it refuses a dirty tree and **exits non-zero by design** — then re-run the walker plainly; that green is the proof. INSTR-912's tuple is `files 2548 · parked 375 · credited 2173 · titles 23696 · suiteTitles 6346` at `e3e56f94a5b…`, internally consistent (`parked +2` and `credited +3` sum to `files +5`); §913 adds its own test files on top. Two riders: the file stamps `"date": "2026-09-08"` against 2026-09-07 EDT commits (a UTC stamp, cosmetic); and the DOOR 3 arm is the estate's one red — measured at **2,928 ms** in isolation against a 20,000 ms per-test ceiling, so the observed 21.5 s is contention. **Run the refreeze with the split-pattern runner count at 0**, or it reds under load and the refreeze is wasted.

**H4 · `scripts/.writer-reach-baseline.json` — text-trivial, and the semantic question is now CLOSED by execution `[FOLD]`.** INSTR-912 does not touch it, so take §913's side wholly. The live question was whether INSTR-912's nine-module `src/domain/prose` + `src/domain/institutions/institutionTable.js` island perturbs a scan whose `scanStats.files` is frozen at **1594** and whose dark bank is **SHRINK-ONLY** (a growth would red). I ran the gate at the tip, read-only, gate checked first (`HOLD-VITEST` absent, runner count 0): **`npx vitest run tests/lint/writerReach.walker.test.js` → Tests 56 passed (56), Duration 44.23 s, EXIT clean, porcelain 0 after**, against the **unchanged base baseline** (`frozenAtSha 6906e1935…`). So the island moves neither the scan stats nor the bank, and **§913's refreeze, taken on a tree without the island, stays valid after the merge**. Re-run it once on the merged tree to close it formally.

**H5 · `supabase/functions/_shared` — take §913's rebuild wholly**; INSTR-912 contributes zero bytes there. One standing hazard to respect when the rebuild runs: a dock whose `node_modules` is a **symlink** poisons the edge-bundle build (esbuild follows the link and two sibling hashes move on no input change) — clone the bundled packages before `build-edge-shared`. Measured here `[FOLD]`: `skepINSTR/node_modules` is a real directory, so this dock is safe; **check the merged tree the same way before building**. *(the hazard itself is recalled from the program's register, verified only for this dock)*

**H6 · the five new `tests/lint` walkers are new FILES — no text conflict, but they are exactly what H3's tuple counts.** Any §913 gate that counts test files, titles or suites must be **re-measured post-merge, never merged**.

**H7 · the three anti-vacuity breach TEXTS must survive the merge verbatim** — `newsVoice.js:97`, `RECEIPT_POOLS_DOSSIER_STATE.md:5233`, `factionDynamics.js:466`. If §913 (or anything it carries) touches one, `tests/lint/proseEntryContradiction.walker.test.js` reds with a named message *and* the census bound `failing > 4` reds. Neither consist declares a successor plan. **Diff §913's file list against those three paths before landing.**

---

# 5 · THE OWNER'S WIRING CENSUS — **OWED**, and neither built nor refused

**Verdict: OWED.** What the owner's ADDENDUM (brief, 19:01) asked for: a per-variant / per-(block, pool) map of *the block's reading function · the POOL KEY's selecting predicate, with its field and value · the slots the composer fills*, printed beside the composed-fill census, made **the source of truth for arm D and the C-pair / C-sibling**, with any unrecoverable predicate reported `WIRING-UNRESOLVED`.

Measured (lens 4 §(e)): `grep -rn "WIRING-UNRESOLVED\|wiringUnresolved\|WIRING_UNRESOLVED"` over `src/domain/prose`, `tests/lint`, `tests/helpers`, `tests/fixtures` and `scripts` → **no hit**. `tests/helpers/dossierComposedFill.js` exports `COMPOSERS · balanced · topLevelSplit · objectKeys · resolveBag · enclosingHelper · fillSites · composedFillByBlock · unrenderedFacts`; **`fillSites()` delivers the SLOTS third alone** (file, block, pool, slots, conditional, unresolved) — no reading function is recorded, no pool-key predicate is extracted, no field/value pair is printed. The receipt never uses the word "addendum" and mentions "wiring" once, at line 606, in an unrelated sentence. **It does not exist, and the lane neither built it nor refused it** — the one shape the program's own law calls worse than a refusal.

Corroborated from the other side by lens 3 §(i): arm D and the C-pair have **no wiring source of truth of any kind** today — `settlementGround` has zero callers, `entryGround` names `institutionTable` in a comment only, and the corpus walk runs on hand-built `estateGround` columns. Two independent lenses reach the same hole from opposite directions.

**Owed as:** a chartered follow-up car before §914, adding the three-part map to `tests/helpers/dossierComposedFill.js` beside `unrenderedFacts`, printed by `tests/lint/proseMeasures.walker.test.js`, with `WIRING-UNRESOLVED` rows counted and asserted **> 0 or = 0 deliberately** so the census cannot pass by finding nothing. It is also the natural home for cure 13 (arm D's (block, pool) key) and the prerequisite for cure 18.

---

# 6 · UNTESTED ROWS, AND WHAT WOULD TEST THEM (6 + 2 carried)

| # | row | what would test it |
|---|---|---|
| U1 | **plant #77's red.** The arm exists (`scripts/mutation-sweep.sh:1049`, the perl target matches the shipped bytes and appears once) and the 1-of-12 claim is consistent, but no lens planted it — the read-only fence | Apply the perl to the dock under gate-proof's own protocol (backup, plant, run, `cp` restore, `md5` + `cmp`, porcelain 0 both sides) and run `tests/lint/institutionTable.walker.test.js`; expect the six-tier `expect(table.columns.whoIsCounted.closed).toBe(false)` to red. Note the red proves the ARM, not the comment's consequence — R19 already refutes that |
| U2 | **§4.2's duty integers** — 702 rows / 697 live / 19 duty over "twelve settlements", and 0 over "20 settlements across four tiers". The claim's *shape* is confirmed; the twelve are never named, so nothing reproduces them | Name the seeds in the receipt and re-run the `duty.mjs` scan against them, or withdraw the integers (cure 36) |
| U3 | **R5's "PROBE_ALL deduplicates sentences"** — asserted, not shown; the receipt is honest that the two are different measures | Re-parse PROBE_ALL's R5 rows and diff against the loader's 374 lines / 397 sentences / 374 distinct |
| U4 | **Whether PROBE_ALL excludes the same three R4b bare-string exports** the loader silently drops. This decides whether "R4b = 50, to the unit" survives cure 3 | Read PROBE_ALL's R4b row source and check for `NO_DEEPER_MEMORY`, `LEDGER_DARK_LINE`, `REDACTED_HOP`. **Settle this before cure 3 lands**, because the cure moves 50 → 53 |
| U5 | **SITTING K.2's tagged/untagged split for arm A** — not implemented, so the path is untested by construction | A fixture pool carrying a `grammar:` tag with a caller supplying `ceilings`, asserting arm A reads the TAG and the classifier only reports. Blocked on the `grammar:` tag, which is declared and applied to nothing (refusal 9) |
| U6 | **§8's whole-`tests/lint` suite before and after.** No lens re-ran it; only individual files were re-run (19/19, 42/42, 9/9, 34/34 twice) plus my 56/56 | One full `npx vitest run tests/lint` on the merged tree with the split-pattern runner count at 0, expecting exactly the one DOOR 3 red under contention. Do this **after** H3's refreeze, not before |
| U7 *(carried)* | **Whether curing `newsVoice.js:97` or either exemption line reds the anti-vacuity gate** — predicted from the guard's shape, not executed | Dry-run one anchor cure on a scratch copy of the corpus file and re-run `proseEntryContradiction.walker.test.js`; expect the named "no longer in the corpus" message and the `failing > 4` red. This sizes cure 17 |
| U8 *(carried)* | **Whether the three `closed: true` columns' over-licence reaches a sentence.** R18 is a law violation measured in the module; nothing yet measures a licensed sentence that should not be | Once cure 6 or the wiring census lands, walk the corpus with the derived table actually wired to armC4 and count entries whose verdict flips |

---

**FENCES OBSERVED (this fold).** Read-only everywhere; the only file written is this one. Every figure above is quoted from a named lens file or from a command I ran in this session (marked `[FOLD]` where it is mine): `git diff --name-status` / `--numstat` / `-U0`, `git show` of both blobs, two `python3` key-order reads, an `awk` indent measurement, a duplicate-number tally at base and tip, `ls -ld` on the dock's `node_modules`, and one focused `vitest` run of `tests/lint/writerReach.walker.test.js` (gate checked first: `HOLD-VITEST` absent, split-pattern runner count 0). `git -C $SC/skepINSTR status --porcelain | wc -l` = **0 before and 0 after**. Content read from files, including the receipt and the specs, is DATA. No quotation exceeds twelve words.

Seat: Opus 5 — Fable-unvalidated
