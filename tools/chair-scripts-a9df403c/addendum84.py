# -*- coding: utf-8 -*-
import io, sys
stamp = sys.argv[1]
ODQ = "docs/OWNER_DECISION_QUEUE.md"; CH = "docs/implementation/charters/EDIT-MODE-TRAIN.md"
body = ("**RUN 21 (the full check at `c33446830`, launched 08:21) EXITED 1 at 08:42:46 ON EXACTLY ONE TITLE:** `tests/property/dossierProseManifest.test.js :: … ⭐ THE PROVENANCE REFUSES A FIXTURE ITS RECORDER DID NOT WRITE` — 'a fixture whose recorder has moved since it was written is REFUSED'. "
 "CAUSE, CONFIRMED BY THE CHAIR: the golden's `_provenance.recorder` is the RAW sha of each of three `MANIFEST_RECORDER_FILES` (`tests/helpers/dossierManifest.js:335`: `dossierManifest.js`, `goldenMasterCorpus.js`, `scripts/prose-rate-corpus.mjs`), and exactly one moved in `141a1d775..c33446830` — `scripts/prose-rate-corpus.mjs`, 3 lines, FIX-C2's citation re-address (the composition's re-proof ran `tests/lint` whole and six focused suites, none of them this test) — with ZERO rows moved (both goldens hashed byte-identical by the composing lane). "
 "THE CLASS: a raw-sha pin on a source file is blind to the difference between a comment edit and a behaviour edit (memory fold 112's marker-slice family); FIX-C2b is re-addressing citations now, so without a cure it recurs. RULED: **CURE-J CHARTERED AND DISPATCHED at c33446830 WITH THE GATE GRANTED AT DISPATCH** (the train's blocker; a fourth shared holder for its short life — vetoable): (1) `recorderShas()` hashes each recorder file's COMMENT-STRIPPED source (strings, templates and regex kept), proved on an inline fixture and by counterforce both directions; (2) ONE honest re-record with `rows` byte-identical and `rowsSha` unchanged — `MANIFEST_PROVENANCE.shift`/`ruling` untouched (the last owner-signed PROSE shift is still the rows' truth), `recordedOverSha` → c33446830, one sentence appended to `note`, the golden-freeze register updated in its own idiom (STOP if that needs an owner door); a single moved row is a STOP (the owner's). The build and `verify:dist` steps of run 21 were BLACKED OUT by the `&&` chain; **RUN 22 is owed at the tip that carries CURE-J and EM-B1f**; the push recommendation stays at `141a1d775` (run 20 green) until then. "
 "**EM-B1f STOPPED AT THE SEAL — §7.4's TRAP HAS A THIRD SHAPE, AND IT IS THE PACKET'S OWN ROW:** `scripts/implementation-packets.mjs:844-848` requires a NON-TERMINAL packet's `retiredSymbols` symbol to still be PRESENT, and EM-B1f's one row (EM-B1k2's `const UNDISPOSITIONED_CEILING = 7`) names the very text its §7 burns to 6 — proven three ways by the lane (ceiling 7 + row: EXIT 0; ceiling 6 + row: EXIT 1 `already absent … before READY`; ceiling 6, row removed: EXIT 0). RULED: the row was MOOT before the seal (EM-B1k2's `requiredSymbols` row was re-spelled figure-free at `fff247009`, so nothing cross-packet names the figure) → **WITHDRAWN by the chair's pathspec commit in the build's own worktree, `6a3e8089f`** (`PACKET_MANIFEST.json` −6, the packet header +1 note; `validate:packets` `valid: 194 packets (1 READY)` in the lane's tree at ceiling 6; the lane's six uncommitted edits untouched, verified before and after); **TOOL-19 chartered** (the validator's retiree rule under a seal: a packet whose dispatch is sealed may have burned its own retirees — the third trap shape). The seal at c33446830 is stale by design → **PATCH-AND-RESEAL** (a new lane law, written into LANE-EM-BUILD this sitting): the lane saves its edits as a patch with per-file SHA-256s, restores its own files to HEAD, proves the tree EMPTY, ends its turn `TREE CLEAN — awaiting CURE-J`; the chair cherry-picks CURE-J onto the branch in that clean worktree; the lane re-runs `implementation:dispatch` (a fresh seal at the new HEAD, the same packet), re-applies the patch with `--index`, proves the hashes equal, then batch 4. B1f's batches 1–3 run NOW on the shared tier beside FIX-L1 and FIX-P5 (three holders; the train's spine). THE LANE'S PROOFS ACCEPTED: seal EXIT 0 at c33446830 (`22c09c9d…4f39`), preamble measures `ce516004…af4b`, effective lines 288 → 293, raw 596 → 607, esbuild +184 B (bound ≤ 370), module edges 7 → 7, worker closure 544, goldens byte-identical, §13 both arms identical row for row, A1 reproduces the defect at base and the cure after. ITS THREE REFUTATIONS ACCEPTED: (1) §13.0's base table is WRONG for `exiled`/`removed` (the house IS swept, `[The Crown]`, and a `faction_dissolved` beat fires; arm-independent, the person kept in every cell — not a promotion blocker) → the chair re-cuts §13.0's prose AT THE FLIP, never under the seal; (2) §7.3's `pulseKernel.js:662` was stale (live `:660`) and of a form `pulseKernelLineAddress.walker` bans → the lane's content anchor stands; (3) a THIRD figure-in-a-symbol is loaded — EM-B1k2's `\"uncoveredBaseline\": 186` → **TOOL-15 WIDENED** from `requiredSymbols` to `retiredSymbols` and every numeric row. ITS NOTICED ITEMS FATED: `isOffStage`'s docblock under-describes the predicate by one arm (§6 verbatim does not amend it) → the next EM member that declares `src/domain/roads/state.js` (EM-B1h's compile names it); the walker's `:402` 'eight flagged files' message wrong at base → the next member that declares `statusUnionTotality.walker.test.js`; ⛔ **A FOREIGN STASH EXISTS IN THE SHARED TREE (`stash@{0}: On analytics-intelligence-layer`)** → preserved, never cleared by any lane (memory + LANE-PARALLEL). "
 "**TOOL-13b PAUSED WITH THREE OF FOUR COMMITS REFUTED BY EXECUTION:** the scanner's detector sources are GOVERNED — one comment byte appended to `scripts/check-observed-shape-readers.mjs` reds the plain gate (`DETECTOR SOURCE changed since the schema-22 instrument was governed … Build and review the governed migration bundle instead`), and to `legacy-reader-shape-scan.mjs` likewise (`does not reconstruct governed Git blob 0310fa9f…`); the file is entry 1 of `scannerToolFiles()`, so TOOL-13 §7.2's one PLAUSIBLE claim (a post-scan derivation suffices) is refuted. RULED: commits 1–3 (the denominator field, the M12 docblock, the header) FOLD INTO TOOL-13a's schema-23 rung as bundle members M1–M3 (M1's figures taken from the rung's own scan AFTER the widening, never carried forward — the lane's caution); commit 4 (the erasure guard, +112 lines in `observedShapeReaders.walker.test.js`, no new file; counterforce executed — shipped `resolved=9780 ERASED=0`, six roots admitted `resolved=12368 ERASED=13`, all thirteen rows named across four files, 11 on `stressors` — one vocabulary step from four `CLASS_A_PROTECTED_IDENTITIES` rows — and 2 on `raw`, none a class-(a) identity, so extending `assertClassADebtPreserved` would catch none) RUNS ITS GATE in the queue. M12 SETTLED BY MEASUREMENT: 0 raw findings under the key, 22 reads across 21 files all with an unresolved receiver, the key absent from `BUILTIN_MEMBERS` — a real permanent hole, the filter still capable; the real defect is the docblock asserting a live population of 2 reads that DA-B2 cured. `scanReaders`' `minRows` default 8 vs the gate's 40 → a CANDIDATE member of TOOL-13a (behaviour, weighed at its compile); the `scanStats` drift → TOOL-13a's refreeze. "
 "**FIX-B2 PAUSED WITH COMMIT 1 STAGED** (`vite.config.js` + two `tests/build` files, zero src, goldens byte-identical, EAGER 268): TOOL-12's twelve re-derived by an independent instrument (`needs.mjs`, stopping at the engine boundary); five of the twelve are unruled orphans Rollup co-locates (a model reading only `manualChunks` misses five); moves 5 and 8 JOIN `engine-core-lazy` and mint no chunk; the SEAMCYCLE correction carries FIVE chunks (TOOL-12 named four; `livingContentRoster` 1,788 B was omitted); `vendorPdfLazy.test.js:750` already measures bytes → the comment arm; the mass leaves in commit 3. RULED AND ACCEPTED BY THE LANE: the second refuted claim at `vite.config.js:874-877` (`helpers.js` ruled into `engine`, no `stressTypes` edge; the sentence describes the tree commit 2 LEAVES) is corrected in commit 2; the owed build tests ARE written in commit 3 as titles in the existing homes (`engineChunkLazy` for the placement/presence arms, `vendorPdfLazy` for the entry-closure arm — both already PARKED under `describe.runIf`, so the predicted tuple delta is zero, measured at batch 4; the absence arms use `toHaveLength(0)` because `negativeAssertionAnchor.walker` freezes those files' negative counts exactly). "
 "**FIX-C2b PAUSED WITH FOUR COMMITS READY:** the certification file's 42 citations read against live source — 22 stale + 2 bare cured, 18 left true with reasons; the seven `warDeployment.js:1285–1291` flag reads all live at `:474–:480`, EXACTLY 811 earlier in unbroken declaration order (FIX-C2's EOF arm could not see them — ARM 1's '100 %' is of RESOLVABLE rot); the gate itself off by 839, so every address was read, not shifted; three citations changed module. ⛔ THE CHAIR'S DISPATCH LEAD IS REFUTED: `roadsParticipation.test.js:415`'s `npcVerdictPulse.js:133` is TRUE (`:133` names the `replaceOustedNpcs(` call; `:134` its argument) — the `wiring-census.mjs:349` false-positive class verbatim; untouched. The baseline burned to zero (47 rows, 25 documents; nine were module moves behind barrels, not drift); THREE false exactness claims corrected (WF-SUBSTRATE's 'ALL EIGHT … EXACT' wrong in three places). TWO BRIEF PREMISES MEASURED FALSE (the chair's): `docs/implementation/**` was already in the docs-live arm (the real gap is that ARM 3 is code-only), and `EM-R0a.md` is in the kit, not the tree. A REAL DEFECT IN THE SHIPPED WALKER: `SKIP_DIRS` carried a bare `build`, matched at every depth, hiding all 59 files of `tests/build/` from the walk and the target index (why 44 'absent targets' were never absent) — cured red-first. ARM 4 (bare `:NNN`): 661 addresses (59 code, 602 docs), the nearest-preceding-anchor reader still unsound (six of twelve findings are one `MF-UC1.md` sentence) → REPORT-ONLY like ARM 3. RULED: the `:425`/`:429-455`/`:466-468` family in LANDED packets stays FROZEN EVIDENCE (a landed packet is a historical record). Two laws learned and recorded: writing ABOUT a struck address in citation form re-creates it; a docs edit that adds a line rots every line-addressed anchor below it. "
 "**FIX-P5 IN ITS GATE:** commit A `e207d0718` (8 files; `tests/components` 70, `tests/ui` 19, `tests/design` 73, eslint 0); Group B's red-first convicts with the arithmetic in the message (71 px per seat at 375 vs 82 needed — §934.63 F8's clip); two honest corrections on the record — a GUESSED anti-vacuity floor (`rows ≥ 10`) reded and became the measured `groups ≥ 4 / rows ≥ 9`, and **`--reporter=basic` is not a vitest reporter in this tree: it burns a mutex acquisition and prints NO count line** (memory + LANE-PARALLEL). **FIX-L1** running its batch (queued behind run 21's ratchet, correctly). THE GATE: shared tier = FIX-L1 · FIX-P5 · EM-B1f (batches 1–3) · CURE-J; queue in pause order = TOOL-13b (one focused run) → FIX-C2b (`tests/lint` whole + suites) → FIX-B2 (a build, exclusive, ALONE when the shared tier drains). WORKING: CURE-J · FIX-D9 · FIX-T2 (dispatched at 6a3e8089f) · TOOL-18 (read-only, no gate, dispatched this sitting). FIX-C2b WARNED before it paused: the three recorder files are off limits until CURE-J composes.")
line = ("- **§934.47 addendum 84 — RUN 21 RED ON ONE TITLE (the prose manifest's provenance pins its recorder by RAW sha; FIX-C2's citation edit in `scripts/prose-rate-corpus.mjs` moved it with zero rows moved) → CURE-J DISPATCHED WITH THE GATE; EM-B1f STOPPED AT THE SEAL ON ITS OWN `retiredSymbols` ROW (the validator's third trap shape) → THE ROW WITHDRAWN (`6a3e8089f`), TOOL-19 CHARTERED, PATCH-AND-RESEAL WRITTEN, BATCHES 1–3 RUNNING; TOOL-13b's COMMITS 1–3 REFUTED (governed detector sources) → FOLDED INTO TOOL-13a, COMMIT 4 GATED; FIX-B2 COMMIT 1 STAGED WITH TWO RULINGS ACCEPTED; FIX-C2b PAUSED WITH FOUR COMMITS (the chair's lead and two premises refuted; a real walker defect cured); FIX-P5's HAZARD (`--reporter=basic`); A FOREIGN STASH; RUN 22 OWED (2026-09-20 " + stamp + " EDT; the chair; vetoable).** " + body)
u = io.open(ODQ, encoding="utf-8").read()
key = "- **§934.47 addendum 83 —"; assert u.count(key) == 1 and "addendum 84 —" not in u
i = u.index(key); j = u.index("\n", i); u = u[:j + 1] + line + "\n" + u[j + 1:]
io.open(ODQ, "w", encoding="utf-8").write(u)
c = io.open(CH, encoding="utf-8").read(); assert "addendum 84" not in c
if not c.endswith("\n"): c += "\n"
c += ("\n## Amendments of 2026-09-20 " + stamp + " EDT — run 21 red; CURE-J; EM-B1f's seal blocker; TOOL-19; TOOL-15 widened (ODQ §934.47 addendum 84)\n\n"
      "- **CURE-J (dispatched at c33446830, the gate granted at dispatch):** the prose manifest's recorder identity becomes comment-insensitive (`recorderShas()` over comment-stripped source; strings/templates/regex kept; counterforce both directions) and the fixture is re-recorded ONCE with `rows` byte-identical and `rowsSha` unchanged (`recordedOverSha` → c33446830; a sentence appended to `note`; the golden-freeze register in its own idiom). Composed onto the branch in EM-B1f's clean worktree before B1f's re-seal. RUN 22 follows at the tip carrying CURE-J and EM-B1f.\n"
      "- **EM-B1f:** its one `retiredSymbols` row withdrawn at `6a3e8089f` (moot since fff247009); PATCH-AND-RESEAL at batch 4; §13.0's base table re-cut by the chair at the flip (the house is swept for exiled/removed; a `faction_dissolved` beat fires; arm-independent); the `isOffStage` docblock arm and the walker's `:402` message go to the next member declaring those files (EM-B1h's compile names both).\n"
      "- **TOOL-19 (NEW; a seat when one frees):** `scripts/implementation-packets.mjs:844-848`'s non-terminal-retiree-must-be-present rule reds a sealed build's own §7 burn — the rule learns the seal (a packet with a live dispatch may have burned its own retirees), with the three states the lane proved as its fixture.\n"
      "- **TOOL-15 WIDENED:** the unpoliced figure-in-a-symbol shape now covers `retiredSymbols` and every numeric manifest row (EM-B1k2's `\"uncoveredBaseline\": 186` is the third instance).\n"
      "- **TOOL-13b → TOOL-13a:** the denominator field (M1, figures from the rung's own post-widening scan), the M12 docblock correction (M2) and the header correction (M3) are bundle members of the schema-23 rung (governed detector sources refuse a comment byte outside the bundle); `minRows` 8-vs-40 a candidate member; TOOL-13b lands only the erasure guard.\n"
      "- **FIX-C2b's rulings:** the `:425`/`:429-455`/`:466-468` family in LANDED packets is frozen evidence (untouched); ARM 4 report-only; the chair's two brief premises corrected in the record.\n")
io.open(CH, "w", encoding="utf-8").write(c)
print("ODQ addendum 84:", io.open(ODQ, encoding="utf-8").read().count("addendum 84 —"), "| charter amended")
