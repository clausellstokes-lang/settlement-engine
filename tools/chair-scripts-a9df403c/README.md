# chair scripts, session a9df403c (2026-09-20)
- place_b1k_v2.py: EM-B1k v2 placement with the chair's cuts (model: promote_b3c.py)
- docs_corrections.py: the hot-file list + EM-B1d flip-note retraction
- eslint here is FLAT-config only: `new Linter({configType:"flat"})` + `verify(src, [{files:["**/*.js"], languageOptions:{...}, rules:{...}}], {filename})`; the message's `(N)` is the effective-line count.
