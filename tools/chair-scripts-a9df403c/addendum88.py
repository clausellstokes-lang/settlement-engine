# -*- coding: utf-8 -*-
import io, sys
stamp = sys.argv[1]
ODQ = "docs/OWNER_DECISION_QUEUE.md"; CH = "docs/implementation/charters/EDIT-MODE-TRAIN.md"
body = ("**TOOL-18 DONE (read-only; acorn 8.16.0 over 1,172 files, 0 parse failures):** the premise held and the measurement redirects the question — `src/generators/**` is the estate's MOST STABLE prose (+6,819 B, +5.1 % in sixty days; `npcStructure.js` itself SHRANK 171 B; its 26,939 B reproduced exactly, 231/231 literals; rendered-vs-source explained: 41,135 − 3,122 comments + ~1,908 Rollup = 39,921); the growth is all `src/domain/**`. POPULATION by the brief's rule (M1): 1,169,148 B over the two roots; estate-wide with `src/data` 1,770,386 B, of which the ratchet reaches 366,240 (20.7 %). THE CERTIFICATION REGISTER IS A FALSE POSITIVE AT SCALE: `src/domain/certification/**` holds 416,965 B of M1 prose and ships 162 B of it (0.0 %), grown from 495 B in sixty days; product-side prose is 752,183 B, +25.0 % / 60 d. REACH TODAY: ZERO byte instruments over the two roots (the baseline intersects them at 0 files); the only instrument is eslint `max-lines` 800, which leaves ~712,098 unused effective lines (~102 MB of headroom, an order-of-magnitude claim). A top-ten growth entry was a FILE SPLIT (`causeConjunctionRoleContent.js` −203,036 / twelve new files +203,036, identical to the byte) — reported by group. RECOMMENDED AND RULED: **shape (b), synthesised — TOOL-24 chartered:** three SHRINK-ONLY prose-byte TOTALS (generators / domain-product / certification) with the existing declared-row door and a NON-ASSERTED per-file diagnostic printed on the red (via `process.stdout.write` — item 10); (a) declined (RAW-EXACT is built for generated artefacts; forty rows on files that move on every fix would make every lane a declared-row author, cover 72.2 % and miss the new files that carried 97.5 % of the growth); (c) refuted as an accounting cure (`src/data` already holds 234,998 B of prose the ratchet does not name); (a) and (b) cost TOOL-12's 365 B margin exactly zero. THE TEN ITEMS FATED: (1) `src/data` leaks 234,998 B past its own ratchet (`proseLeavesOnDisk()` derives its roster from one directory + named leaves; `institutionDescVariants.js` 66,835 B, `institutionServices.js` 47,868 B and siblings unnamed) → TOOL-24's FIRST row; (2) ⛔ THE CEILING'S AUTHORITY IS A PHANTOM — five files (`.prose-byte-baseline.json`, `proseCorpusBytes.test.js`, `vendorPdfLazy.test.js`, `mutation-sweep.sh`, `mutation-coverage-manifest.json`) cite **ARCH-COMPOSED-PROSE §10–§13**, a document that exists nowhere in the repo, its history or the kits; the law lives only in the instrument's docblock and the baseline's `_ceilingArithmetic` → **DOC-1 chartered (the chair's, at the composition sitting):** materialise `docs/ARCH_COMPOSED_PROSE.md` §10–§13 from the instrument's own text and the baseline's arithmetic (a restatement, no new law), or re-cite the five to the docblock — never leave a law citing a document that is not there; (3) the certification register's 416,965 B → TOOL-24's separate certification total (an engineering register, not a product cost; governed by the domain layer's 800-line ceiling and `voiceMechanics` — the row keeps it visible, nothing else); (4) M1 under-counts template-heavy modules by up to ~40 % (M3 − M1 = 75,073 B; `commercialReceiptPools.js`, `warReceiptPools.js`, `narrativeText.js`, `eventProse.js`) → TOOL-24 measures by M3 (templates included) — a brief line; (5) `src/generators/data/deityPool.js` DELETED within the window (−1,094 B) with the DEITY DOCTRINE constitutional → the chair read the deleting commit this sitting (its message quoted in the kit's TOOL-18 folder); if it records the doctrine's reason it is CLOSED, else **DOC-2** (a one-line reason recorded beside the doctrine memory); (6) seven files over the 800-line ceiling with `.size-baseline.json` overrides → CLOSED (the overrides exist for exactly these files; the arithmetic is consistent); (7) 51.2 % of the two roots' bytes are COMMENTS (11,150,178 B — 9.5× the declared prose corpus; the largest body of authored English in the estate; invisible to every instrument; does not ship) → CLOSED with reason (design-in-code is the estate's idiom; not a bundle question; the owner's moat is depth — no instrument is owed); (8) `causeConjunctionRole*`'s 203,036 shipping bytes never attributed to a chunk (not among the engine's 114) → TOOL-17's brief gains a second measurement (which chunk carries them and its budget); (9) the generator golden master sees prose CHANGES but not ADDITIONS on unreached branches (525 seeds × 64 facets) → CLOSED with reason (TOOL-24's totals bound additions by bytes; branch reach is the seed corpus's design, the owner-signed golden's scope); (10) the report-only hazard → TOOL-24 writes its diagnostic with `process.stdout.write`. SEATS: TOOL-18 done → **TOOL-15 DISPATCHED at c127cdfb2** (EM-B1f's entry under seal is off limits to it).")
line = ("- **§934.47 addendum 88 — TOOL-18 DONE: `src/generators` IS THE ESTATE'S MOST STABLE PROSE, THE GROWTH IS `src/domain`; THE CERTIFICATION REGISTER SHIPS 0 % OF ITS 416,965 B; NO BYTE INSTRUMENT REACHES THE TWO ROOTS → TOOL-24 CHARTERED (three shrink-only totals + a diagnostic); ⛔ THE PROSE CEILING'S AUTHORITY (ARCH-COMPOSED-PROSE §10–§13) IS A PHANTOM DOCUMENT → DOC-1 CHARTERED; ITS TEN ITEMS FATED; TOOL-15 DISPATCHED (2026-09-20 " + stamp + " EDT; the chair; vetoable).** " + body)
u = io.open(ODQ, encoding="utf-8").read()
key = "- **§934.47 addendum 87 —"; assert u.count(key) == 1 and "addendum 88 —" not in u
i = u.index(key); j = u.index("\n", i); u = u[:j + 1] + line + "\n" + u[j + 1:]
io.open(ODQ, "w", encoding="utf-8").write(u)
c = io.open(CH, encoding="utf-8").read(); assert "addendum 88" not in c
if not c.endswith("\n"): c += "\n"
c += ("\n## Amendments of 2026-09-20 " + stamp + " EDT — TOOL-18's fates; TOOL-24, DOC-1, DOC-2 chartered (ODQ §934.47 addendum 88)\n\n"
      "- **TOOL-24 (NEW; a build lane when a seat frees):** three SHRINK-ONLY prose-byte totals — `src/generators/**`, `src/domain/**` minus certification, `src/domain/certification/**` — measured by TOOL-18's M3 rule (templates included; the parse, never a regex), with the existing declared-row door for a raise and a NON-ASSERTED per-file diagnostic printed via `process.stdout.write` on the red; its first row names the `src/data` prose `proseLeavesOnDisk()` misses (234,998 B).\n"
      "- **DOC-1 (NEW; the chair's, at the composition sitting):** the prose ceiling's authority — ARCH-COMPOSED-PROSE §10–§13, cited by five files and existing nowhere — materialised as `docs/ARCH_COMPOSED_PROSE.md` §10–§13 from `proseCorpusBytes.test.js`'s docblock and the baseline's `_ceilingArithmetic` (a restatement, no new law), the five citations left true.\n"
      "- **DOC-2 (NEW; the chair's, one line):** `src/generators/data/deityPool.js`'s deletion within the window recorded with its reason beside the deity doctrine, if the deleting commit does not already carry it.\n"
      "- **TOOL-17's brief gains a second measurement:** which chunk carries `causeConjunctionRole*`'s 203,036 shipping bytes and what that chunk's budget is.\n")
io.open(CH, "w", encoding="utf-8").write(c)
print("ODQ addendum 88:", io.open(ODQ, encoding="utf-8").read().count("addendum 88 —"), "| charter amended")
