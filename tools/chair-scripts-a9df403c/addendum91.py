# -*- coding: utf-8 -*-
import io, sys
stamp = sys.argv[1]
ODQ = "docs/OWNER_DECISION_QUEUE.md"; CH = "docs/implementation/charters/EDIT-MODE-TRAIN.md"
body = ("**FIX-C2b COMPLETE on `fix-citations-2b-2026-09-20`** (base ee2406191; four commits `0bc7503b8` the war certification file's 42 citations by symbol (1 file) · `a7cd336bb` the docs-live baseline burned 47 → 0 and three false exactness claims (35 files) · `7330a89e9` the town-map citations struck with the deleting commit, MF-UC4 re-cut (2) · `b1c1ac182` the instrument: the `tests/build` blind spot cured, ARM 4 added (2); tree clean; none amended). EVERY COUNT LINE: subsystemRowsWar 27; roadsParticipation 8; dossierStateProseProjection.contract 79; `tests/build` ×6 56 passed | 42 skipped; implementationSession 10; `tests/lint` WHOLE 2793/2794 (the lighting red alone — `dossierProseManifest` not swept); voiceMechanics 30; eslint 0; `validate:packets` `valid: 193 packets (1 READY)`; `wiring-census --check` verified 708 / 2266 / 165. LIGHTING: the walker threw at `files` (`expected 2656 to be 2653` — its tree's register predates two refreezes), so parked/credited/titles/suites were NEVER evaluated; the lane's delta measured from the committed diff: 0 files, +3 titles, 0 suites, 0 markers (against the fifth refreeze: `titles` 25074 → 25077). ITS SHARPEST FINDINGS: the nine flag-read addresses were off by exactly 811 while the parent gate moved by 839 — an arithmetic cure would have written seven right answers and two wrong ones; `SKIP_DIRS`' bare `build` hid all 59 files of `tests/build/` from the walk AND the target index (red-first 5,208 files / 0 findings → 5,268 / 1 with a planted mutant); WF-SUBSTRATE's 'ALL EIGHT, EACH AT AN EXACT ANCHOR' false in THREE of eight; `writers.js` never entered history — the atlas pinned an uncommitted tree. WHERE IT REFUSED THE BRIEF, ACCEPTED: ARM 4 ships REPORT-ONLY (a shrink-only baseline over a half-false set — `MF-UC1.md:178`'s six bare numbers are one sentence; a single-path restriction leaves all six, 485 of 661 — freezes the heuristic and reds on correct behaviour; CLOSED with reason); `docs/implementation/**` was ALREADY in the docs-live arm and `EM-R0a.md` is in the kit, not the tree (the chair's two premises corrected); the defect that does exist cured (`implementation-packets.mjs:165`'s status-row anchor, wrong in three packets). RECORDER HAZARD: vacuous and measured (ARMS 1/3/4 = 0 inside the three; a ⛔ refusal in the walker's header). ITS TEN ITEMS FATED: (1) ARM 3 scoped to `CODE_TREES` (a stale address in a packet header unseen), (8) 170 citations resolving to an AMBIGUOUS basename (29 distinct) silently skipped, (9) the fourth UNRESOLVED sub-class (a target that existed only in a pinned uncommitted tree), (6) `safetyProfile.js:234` cited by `COHESION_REMEDIATION_PLAN.md:120` in-range and stale, (7) `DESIGN_FP_COUPLINGS.md:510` + `DESIGN_FP_INTERIOR.md:187` citing `coup.js:113-114` → **FIX-C2c chartered and its brief written** (after C2b composes; ARM 3's docs scope report-only; the ambiguous-basename class named and counted; `PINNED-UNCOMMITTED` classified; the five re-addressed by symbol); (2) ARM 4's baseline → CLOSED (report-only accepted); (3) `WC-PREAMBLE.md:70`'s `:1360`/`:1366` both stale → **DOC-3 (the chair's; a family preamble edit re-stamps its members — LANDED members keep their historical hash; at a docs sitting)**; (4) `ES-5D.md:677`'s `:2031`/`:2563` a LANDED packet's ratified deviation record → CLOSED (frozen evidence); (5) `MF-UC1.md:178` → CLOSED (ARM 4's documented false-positive family); (10) the provenance red → §934.71. THE GATE: FIX-C2b done → shared tier = EM-B1f (batch 4 — its re-dispatch at c127cdfb2 took, the capsule `EM-B1f` fresh beside `EM-B1f.sealed-at-c33446830`, its six edits re-applied under the new seal) · FIX-P1b; FIX-B2 (exclusive) runs ALONE when the tier drains; then FIX-D9 → FIX-T2 → FIX-K1 → … THE COMPOSITION ORDER gains FIX-C2b's four after TOOL-13b's one.")
line = ("- **§934.47 addendum 91 — FIX-C2b COMPLETE (four commits; the war certification file's 42 citations by symbol — nine off by exactly 811 while the gate moved 839; the docs-live baseline burned to zero; `SKIP_DIRS`' bare `build` hid `tests/build/` from the walker; ARM 4 report-only ACCEPTED; the chair's two premises corrected); ITS TEN ITEMS FATED — FIX-C2c CHARTERED (brief written), DOC-3 CHARTERED (2026-09-20 " + stamp + " EDT; the chair; vetoable).** " + body)
u = io.open(ODQ, encoding="utf-8").read()
key = "- **§934.47 addendum 90 —"; assert u.count(key) == 1 and "addendum 91 —" not in u
i = u.index(key); j = u.index("\n", i); u = u[:j + 1] + line + "\n" + u[j + 1:]
io.open(ODQ, "w", encoding="utf-8").write(u)
c = io.open(CH, encoding="utf-8").read(); assert "addendum 91" not in c
if not c.endswith("\n"): c += "\n"
c += ("\n## Amendments of 2026-09-20 " + stamp + " EDT — FIX-C2b complete; FIX-C2c and DOC-3 chartered (ODQ §934.47 addendum 91)\n\n"
      "- **FIX-C2b is complete on its lane** (`0bc7503b8`, `a7cd336bb`, `7330a89e9`, `b1c1ac182`); composed after TOOL-13b at the composition sitting.\n"
      "- **FIX-C2c (NEW; brief `briefs/launch/FIX-C2c.md`; after FIX-C2b composes):** ARM 3 reaches the docs scope (report-only; LANDED packets' bodies excluded by the manifest's status); the 170 ambiguous-basename citations resolved or named as a class; `PINNED-UNCOMMITTED` classified; the five stale docs citations re-addressed by symbol.\n"
      "- **DOC-3 (NEW; the chair's, at a docs sitting):** `WC-PREAMBLE.md:70`'s stale `:1360`/`:1366` corrected — a family preamble's SHA re-stamps its members, so the LANDED members' historical hashes are noted as such.\n"
      "- **ARM 4 stays report-only** (a shrink-only baseline over a half-false set is refused by measurement — the chair's standing ruling for heuristic arms).\n")
io.open(CH, "w", encoding="utf-8").write(c)
print("ODQ addendum 91:", io.open(ODQ, encoding="utf-8").read().count("addendum 91 —"), "| charter amended")
