You are the implementer for §932 LANE E — THE SCRIBE RE-CUT (launch-path: the owner 2026-09-15 15:4x "the scribe is pre-launch"; the prose rewrite is POST-launch by the owner's 15:3x word). The Scribe's cars (W0–W5b, the seal `refs/preserve/scribe-w5b-2026-09-15` = `600771812`, in the dock /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-scribe — READ ONLY for you) descend from the banked REWRITE dock (`7992713d0`, 57 REWRITE cars that will NOT land before launch). Your dock is /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-scribe-recut, cut at a5876c0ea (THE NEW BUILD SLOT; porcelain 0; node_modules symlinked). Read first: /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/briefs/PLAN-932-2026-09-15.md (lane E), `docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md` at HEAD of the ledger, and `git -C <lane-scribe> log --format='%h %s' 7992713d0..600771812` — the Scribe's own cars are the ones whose subjects name W0/W1/W2/W3a–d/W4/W5b/the fence car; list them.

CAR 1 — the census: for every Scribe car, `git cherry-pick -x -n` it onto your dock in order and record whether it applies clean, conflicts, or DEPENDS ON REWRITE CONTENT (a file the rewrite cars created or moved, a corpus row the rewrite rewrote, the DEF-2 clarity re-cut's faces); write the table to docs/implementation/SCRIBE_RECUT_CENSUS.md BEFORE resolving anything (commit it). CARS 2… — the Scribe cars replayed onto the slot in order, each conflict resolved by hand against the SHIPPED corpus (the slot's `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` and the prose kernel as they stand, never the rewrite's), each proven by the Scribe's own suites (`tests/domain/prose/` scribe*, the Deno bundle tests under `supabase/functions/scribe-render`, `tests/scripts/baseStateCapsule` if touched) and by the harness's simulate path (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/scribe-harness/simulate.mjs` builds the exact prompt — run it for the Spitzplatz defense cell and diff the prompt against the same run in the lane-scribe dock; the difference must be ONLY corpus-row text the rewrite had changed). ⛔ The migrations 201–203 are WRITTEN, NEVER APPLIED — carry the files, apply nothing; `FLAGS.scribe` stays default false; the worker bundle ceiling (the Scribe's worker sat at ZERO HEADROOM on the rewrite dock) — measure at every car, and if a car cannot fit, HOLD it and report the bytes (lane A0's headroom car may land meanwhile — check `refs/preserve/lane-932-headroom-*`). Report per car: applied clean / resolved (how) / HELD (why), the suite lines, the prompt diff summary.

Rules that bind absolutely: never `git stash`, `git checkout -- <file>`, `git reset --hard`, never push, never deploy, never `supabase db push`, never edit package.json or add a dependency, never raise any size or build ceiling (`tests/lint/sizeBaseline.test.js`, the `tests/build/` ceilings incl. `WORKER_BUNDLE_CEILING_BYTES`, the first-paint and CSS budgets are exact and frozen — lower them when a car shrinks, never raise), never touch /Users/cstokes/Desktop/settlement-engine or any other dock; commit by explicit pathspec after `git diff --cached --stat`; porcelain 0 after every car; pay every census bill a changed or new test file owes (the lighting census re-freeze on a CLEAN tree as its own commit; the mutation-coverage manifest; the wiring census stamp; the prose-numerics baseline). EVERY commit message carries, above the Co-Authored-By line, exactly `Seat: Opus 5 — Fable-unvalidated` and `Lane: <your lane>`. THE GATE per car: the focused vitest files · `tests/lint/` whole · `tests/copy/` whole · `tests/property/` whole · `tests/edgeFunctions/` whole · `tests/security/` whole · `tests/generators/` for any generator touched · `npm run build && npm run verify:dist` under `sh scripts/gate-mutex.sh --run --` for ANY car that touches src/ (quote the worker-bundle line and the first-paint byte table verbatim) · `npm run check:quick` — ⛔ `scripts/gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0 when another lane holds the mutex; a gate line with no printed test count DID NOT RUN — re-run until it prints a count. ⛔ HOLD RULE: before EVERY vitest / npm run / build, if the file /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/HOLD-VITEST exists, sleep 60 and re-check — run nothing while it exists. Quote `uptime` with every gate line; a timeout at load > 8 is measured alone before it is called a defect. Every pin gets an executed negative control. No em dash (U+2014) and no exclamation point in any new user-facing string. Do not seat model agents; no subagents; no Agent tool. If a gate stays red after three distinct hypothesis-driven attempts, stop and report the exact red. When you finish, write your final report ALSO to /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/report-<lane>.md and append one line per car to progress-<lane>.txt beside it.
