You are the implementer for §932 LANE A — THE DECLARED-SHIFT LANE, the ONE priced window in which the long tail's output-moving cars land before the golden freeze (the owner's 14:4x grant; §931.6b). Read /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/briefs/PLAN-932-2026-09-15.md (lane A) first, then `brief-LT41b-the-rulings.md` (RULING 3) and `brief-LT17-catalogue-hygiene.md` (the five held output-moving cars, whose exact strings are in the LT17 lane report in the same briefs dir if present — else in the commit bodies of `lane-LT17-hygiene`). Your dock is /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-declared, cut at __SLOT__ (THE NEW BUILD SLOT; porcelain 0; node_modules symlinked). ⚠ Wait for lane A0 (the worker-headroom car) to land before ANY car that adds generator bytes — check `refs/preserve/lane-932-headroom-*` or the chair's message; until then do the re-record READS and the dedupe/claim-freeze proofs only.

CAR 1 — LT41b car 6 + 6b cherry-picked (`git cherry-pick -x`) from the seal `refs/preserve/lt41b-rulings-2026-09-15` (`a76fe8cdf`, `919c37da5`): then the espionage fence's driven golden RE-RECORDED — read `tests/property/espionageDormancyFence.test.js`'s golden header first and do it the way the last two windows did: name the mover (RULING 3, the nine labels + the annotation), the count of the 360 rows that moved, and attribute the residue to zero; the prose-manifest cells carrying a stability label re-recorded by `node scripts/prose-manifest-cells.mjs --record` (the moved rows listed by pool key in the body); the aiGrounding edge bundle rebuilt (its input `simulationSpine.js` moves) by the recipe: clone `immer` and `seedrandom` with `cp -Rc` from /Users/cstokes/Desktop/settlement-engine/node_modules, `node scripts/build-edge-shared.mjs`, verify no `Users/cstokes` path leaked into supabase/functions/_shared/*, RELINK the two packages (`rm -rf node_modules/<pkg> && ln -s <repo>/node_modules/<pkg> node_modules/<pkg>`) BEFORE any vite build. CAR 2… — LT17's five held output-moving cars implemented under the claim freeze (the words move, the fact does not), each priced by name. CAR N — `docs/GOLDEN_SHIFT_LEDGER.md` gains ONE entry naming every mover of this window. EVERY car carries the declared-scope proof: dump every golden config at base and tip with the chair's probe (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/c931-f6ac0d98/declared/__chair_dump.test.js`, run in throwaway worktrees as `run-declared.sh` there shows) and classify EVERY differing line with an extended `classify.py` (zero UNDECLARED, or the car is HELD). The golden master stays 525/525 red — never re-record it; the claim is the classified diff. ⚠ #37 car 5's food-deficit cure is REFUSED on LT37's measurement (band word identical 360/360) — do not implement it; record the refusal in the ledger entry you draft for the chair.

Rules that bind absolutely: never `git stash`, `git checkout -- <file>`, `git reset --hard`, never push, never deploy, never `supabase db push`, never edit package.json or add a dependency, never raise any size or build ceiling (`tests/lint/sizeBaseline.test.js`, the `tests/build/` ceilings incl. `WORKER_BUNDLE_CEILING_BYTES`, the first-paint and CSS budgets are exact and frozen — lower them when a car shrinks, never raise), never touch /Users/cstokes/Desktop/settlement-engine or any other dock; commit by explicit pathspec after `git diff --cached --stat`; porcelain 0 after every car; pay every census bill a changed or new test file owes (the lighting census re-freeze on a CLEAN tree as its own commit; the mutation-coverage manifest; the wiring census stamp; the prose-numerics baseline). EVERY commit message carries, above the Co-Authored-By line, exactly `Seat: Opus 5 — Fable-unvalidated` and `Lane: <your lane>`. THE GATE per car: the focused vitest files · `tests/lint/` whole · `tests/copy/` whole · `tests/property/` whole · `tests/edgeFunctions/` whole · `tests/security/` whole · `tests/generators/` for any generator touched · `npm run build && npm run verify:dist` under `sh scripts/gate-mutex.sh --run --` for ANY car that touches src/ (quote the worker-bundle line and the first-paint byte table verbatim) · `npm run check:quick` — ⛔ `scripts/gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0 when another lane holds the mutex; a gate line with no printed test count DID NOT RUN — re-run until it prints a count. ⛔ HOLD RULE: before EVERY vitest / npm run / build, if the file /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/HOLD-VITEST exists, sleep 60 and re-check — run nothing while it exists. Quote `uptime` with every gate line; a timeout at load > 8 is measured alone before it is called a defect. Every pin gets an executed negative control. No em dash (U+2014) and no exclamation point in any new user-facing string. Do not seat model agents; no subagents; no Agent tool. If a gate stays red after three distinct hypothesis-driven attempts, stop and report the exact red. When you finish, write your final report ALSO to /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/report-<lane>.md and append one line per car to progress-<lane>.txt beside it.
