You are the implementer for §932 LANE D — THE Q10 BAND-WALKER PREFIX, the one infrastructure car that blocks item #33's bands (ODQ §99.3: "docketed to the next infra member"). Read /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/briefs/PLAN-932-2026-09-15.md (lane D), then the ledger sections §99.3, §49 and §92.3 (`git -C /Users/cstokes/Desktop/settlement-engine show HEAD:docs/OWNER_DECISION_QUEUE.md | grep -n '^## §99\|^## §49\|^## §92' ` then sed -n the ranges) and `docs/DESIGN_FP_ARCH_LG.md` at HEAD of the ledger for what a band is and what the prefix walker must prove. Your dock is /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-q10, cut at a5876c0ea (THE NEW BUILD SLOT; porcelain 0; node_modules symlinked). ONE CAR under `tests/lint`: the band-walker prefix as the record specifies it, with an executed negative control (a planted band that the walker must refuse), a docblock naming §99.3 as its charter and #33 as its first consumer, and its census bills paid. No src/ byte unless the walker's contract requires a registry file (if so, dark and display-free). Report the exact arm titles.

Rules that bind absolutely: never `git stash`, `git checkout -- <file>`, `git reset --hard`, never push, never deploy, never `supabase db push`, never edit package.json or add a dependency, never raise any size or build ceiling (`tests/lint/sizeBaseline.test.js`, the `tests/build/` ceilings incl. `WORKER_BUNDLE_CEILING_BYTES`, the first-paint and CSS budgets are exact and frozen — lower them when a car shrinks, never raise), never touch /Users/cstokes/Desktop/settlement-engine or any other dock; commit by explicit pathspec after `git diff --cached --stat`; porcelain 0 after every car; pay every census bill a changed or new test file owes (the lighting census re-freeze on a CLEAN tree as its own commit; the mutation-coverage manifest; the wiring census stamp; the prose-numerics baseline). EVERY commit message carries, above the Co-Authored-By line, exactly `Seat: Opus 5 — Fable-unvalidated` and `Lane: <your lane>`. THE GATE per car: the focused vitest files · `tests/lint/` whole · `tests/copy/` whole · `tests/property/` whole · `tests/edgeFunctions/` whole · `tests/security/` whole · `tests/generators/` for any generator touched · `npm run build && npm run verify:dist` under `sh scripts/gate-mutex.sh --run --` for ANY car that touches src/ (quote the worker-bundle line and the first-paint byte table verbatim) · `npm run check:quick` — ⛔ `scripts/gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0 when another lane holds the mutex; a gate line with no printed test count DID NOT RUN — re-run until it prints a count. ⛔ HOLD RULE: before EVERY vitest / npm run / build, if the file /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/HOLD-VITEST exists, sleep 60 and re-check — run nothing while it exists. Quote `uptime` with every gate line; a timeout at load > 8 is measured alone before it is called a defect. Every pin gets an executed negative control. No em dash (U+2014) and no exclamation point in any new user-facing string. Do not seat model agents; no subagents; no Agent tool. If a gate stays red after three distinct hypothesis-driven attempts, stop and report the exact red. When you finish, write your final report ALSO to /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/report-<lane>.md and append one line per car to progress-<lane>.txt beside it.
