You are the implementer for §932 LANE A0 — THE WORKER-HEADROOM CAR. Read /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/briefs/PLAN-932-2026-09-15.md (lane A0) first, then the memory note the chair wrote for you: the generation worker bundle (`dist/assets/generation.worker-*.js`) measures EXACTLY its frozen ceiling `WORKER_BUNDLE_CEILING_BYTES = 1404493` in `tests/build/generationWorkerLazy.test.js` at the build slot, so every generator-side code car reds STRICT DIST; LT17 car 2 (+205 B) was held at §931 for exactly this. Your dock is /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-headroom, cut at __SLOT__ = claude/composite-r4, THE NEW BUILD SLOT (porcelain 0; node_modules SYMLINKED — never materialise packages).

CAR 1 — measure first: `npm run build` under the mutex and record the worker's size and its top contributors (esbuild's metafile via vite's `build.rollupOptions` is NOT available — use `npx vite build --mode production` with a one-off `--config` that adds `build.reportCompressedSize`… no: read the built worker with `npx source-map-explorer` if installed, else a byte census by module banner comments in the minified chunk; report the ten largest modules by bytes). CAR 2 — buy ≥ 20 KB of headroom with ZERO OUTPUT BYTES MOVED, in this order of preference: (a) a data table or generator family the pipeline only needs behind a flag becomes a lazy `import()` awaited where it is used (the worker already lazy-loads: read `tests/build/generationWorkerLazy.test.js`'s docblock for the boundary law and copy its shape); (b) code proven unreachable by the estate's own walkers (writer-reach, the OSR inventory, the coupling registry) removed with the walker's receipt quoted. Prove nothing moved: the composed-instrument lines byte-identical base-vs-tip (run `tests/property/dossierProseManifest.test.js` and the golden master at base and tip — the golden is 525/525 RED already; extract the drift set as LT40 car 1 did and diff it: EMPTY), the generator determinism suites green. CAR 3 — LOWER `WORKER_BUNDLE_CEILING_BYTES` to the new measurement (monotone-down) and re-run the build test. CAR 4 — cherry-pick LT17 car 2 from the sealed 67-car tip `refs/preserve/consist-931-with-lt17car2-2026-09-15` (the commit whose subject starts `LT17 car 2 (CH-6b car 1 / J-TECH2-10)`; `git cherry-pick -x`) on top, rebuild, and prove the worker stays under the lowered ceiling. Report the byte table before/after for every car.

Rules that bind absolutely: never `git stash`, `git checkout -- <file>`, `git reset --hard`, never push, never deploy, never `supabase db push`, never edit package.json or add a dependency, never raise any size or build ceiling (`tests/lint/sizeBaseline.test.js`, the `tests/build/` ceilings incl. `WORKER_BUNDLE_CEILING_BYTES`, the first-paint and CSS budgets are exact and frozen — lower them when a car shrinks, never raise), never touch /Users/cstokes/Desktop/settlement-engine or any other dock; commit by explicit pathspec after `git diff --cached --stat`; porcelain 0 after every car; pay every census bill a changed or new test file owes (the lighting census re-freeze on a CLEAN tree as its own commit; the mutation-coverage manifest; the wiring census stamp; the prose-numerics baseline). EVERY commit message carries, above the Co-Authored-By line, exactly `Seat: Opus 5 — Fable-unvalidated` and `Lane: <your lane>`. THE GATE per car: the focused vitest files · `tests/lint/` whole · `tests/copy/` whole · `tests/property/` whole · `tests/edgeFunctions/` whole · `tests/security/` whole · `tests/generators/` for any generator touched · `npm run build && npm run verify:dist` under `sh scripts/gate-mutex.sh --run --` for ANY car that touches src/ (quote the worker-bundle line and the first-paint byte table verbatim) · `npm run check:quick` — ⛔ `scripts/gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0 when another lane holds the mutex; a gate line with no printed test count DID NOT RUN — re-run until it prints a count. ⛔ HOLD RULE: before EVERY vitest / npm run / build, if the file /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/HOLD-VITEST exists, sleep 60 and re-check — run nothing while it exists. Quote `uptime` with every gate line; a timeout at load > 8 is measured alone before it is called a defect. Every pin gets an executed negative control. No em dash (U+2014) and no exclamation point in any new user-facing string. Do not seat model agents; no subagents; no Agent tool. If a gate stays red after three distinct hypothesis-driven attempts, stop and report the exact red. When you finish, write your final report ALSO to /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/report-<lane>.md and append one line per car to progress-<lane>.txt beside it.
