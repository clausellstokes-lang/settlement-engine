# LONG TAIL #40 — engine defects recorded and deliberately not fixed — LANE REPORT (resumed session)

Dock: `kit/lane-LT40-engine`, cut at `f73bdbf16` (= the build slot `claude/composite-r4`).
**Dock tip: `6b4cc8f43` · porcelain 0 · 12 commits · nothing pushed, deployed, migrated or deleted.**
Resumed after the previous implementer (Opus 5) was killed at ~06:12 with car 6 staged but uncommitted.

## Cars 1–5 (landed before this session, by sha and subject only)

| car | sha | subject |
|---|---|---|
| 1 | `c1792e11a` | the seed-join cure COLLECTED onto the build slot |
| 1b | `71c16dae8` | census bill: the prose-numerics baseline follows PowerTab's row |
| 2 | `2d841ad53` | `settlement.services` PINNED permanently absent |
| 2b | `43335c380` | census bill: the lighting census regenerated through its ritual |
| 3 | `5f9cd858a` | the razing spoils PINNED inert — one reader, no writer |
| 4 | `f6961265c` | the four banked dark-pool dispositions written down AT THE SLOT |
| 5 | `b771ccfa7` | VERDICT: `fires(n)`'s 65-ceiling is a RARITY DIAL, not an off-by-one |

## CAR 6 — `cf0365942` — the ruined-court civic snapshot, reported, cured nowhere

**Files:** `docs/ENGINE_DEFECT_DISPOSITIONS.md` (§4) · `src/domain/display/stateProse/defenseStateProse.js` · `src/generators/economy/economicState.js` (both docblock-only).

The dead lane's stage was read whole, checked against the brief's CAR 6, its probe RE-EXECUTED
at this tip (figures reproduced exactly: live roster 42 of 46, recompute over `liveInstitutions`
false/false, the stamp still true/true), and amended only where it was wrong: its line cites were
the recon's PRE-EDIT numbers, and the car's own docblocks shift them. Corrected to `:62`, `:883`,
`:578`/`:616`, `:269`, `:1369`, with a line saying the cites are read at this commit's tip.

**Gate.** focused 3 files/116 tests · `tests/generators` 111/1010 · `tests/lint` 1 failed of 155
(its own census bill, below) · `tests/docs` the six pre-existing naked-claim rows · lint 0 errors
· check:quick exit 0. No behaviour byte, proved by diff scope (zero non-comment additions).

**Judgment:** the report lands in `ENGINE_DEFECT_DISPOSITIONS.md`, not the ODQ the brief names,
because neither ledger doc exists at this slot — car 4's recorded call, kept identical.

## CAR 6b — `9b1c55ecd` — the census bill

11 comment lines moved every producer write below them by 11, staling **nine** line-keyed cites.
Re-derived from the walker's own `producerCitations()` (never +11'd by hand): 7 rows in
`holderTable.js`, 2 in `institutionTable.js`. `docs/content/wiring-census.json` regenerated by
`node scripts/wiring-census.mjs`; a structural JSON diff shows **exactly one field moved** — the
stamp hash of the single file car 6's docblock touched. `tests/lint` back to 155/2600 green.
**Deliberate deferral:** `mutation-coverage-manifest.json:2772` keeps the same stale string because
it is a DATED narrative of an executed plant, not a live pointer.

## CAR 7 — `d33d95882` — `history.age` pinned as a generation-time constant

**Files:** `historyGenerator.js` + `worldState.js` (docblocks) · `tests/domain/worldPulseTickClock.test.js`
(ONE arm) · `observedShapeSentinel.test.js` (one cite) · `ENGINE_DEFECT_DISPOSITIONS.md` §5.

CONFIRMED: twelve real `advanceCampaignWorld` advances — `calendar.year` 2→13, `history.age`
215 throughout, 0 of 12 ticks reusing the object, and population/powerStructure/populationHistory/
activeConditions all moving. One writer in all of `src/`.

**NEGATIVE CONTROL — the finding of the car.** The plant had to be placed THREE times:
`advanceInterval.js foldUpdatesOntoSaves` → arm GREEN; `demographicsKernel.js` → arm GREEN;
`populationDynamics.js` → **RED: `expected [ 216, 217, 218, …(6) ] to deeply equal [ 215 ]`**.
Only the third is on the path the fixture exercises. Every plant reverted by file copy, `cmp`-verified.

**SECOND FINDING — a cite that may not be corrected.** `scripts/check-observed-shape-readers.mjs`
is a HASH-PINNED DETECTOR SOURCE: editing three digits in one of its COMMENTS reds
`observedShapeReaders.walker` with *"a DETECTOR SOURCE drifted since the mint — that needs a
governed migration, not a re-freeze"*. Proved both ways (red with the edit; 44/44 green on restore).
The number stays wrong at `:888` and §5 is its correction.

**Judgments:** the arm lands in `tests/domain/worldPulseTickClock.test.js` because NO
`tests/property` suite drives `advanceCampaignWorld`; ONE behavioural arm, the single-writer fact
stated in the docblock, because every title bills the lighting census.

**Gate.** focused 4/55 · `tests/generators` 111/1010 · `tests/lint` 5 reds, each resolved ALONE
(one was this car's bill; one a named DOOR 3 stray; two load strays green alone; one REAL and mine,
cured before the commit) · `tests/copy` the two pre-existing voice rows · lint 0 errors · check:quick exit 0.

## CAR 7b — `1cd710bd7` — the lighting census bill

Ritual run on a CLEAN tree at car 7's own tip, exit 1 BY DESIGN:
`titles 24170 -> 24171, suiteTitles 6452 -> 6453` (car 7's arm brought its own describe, which is
why two figures moved where car 2's bill moved one). Receipt run 34/34 green, which also discharged
the DOOR 3 load stray.

## CAR 8 — `6b4cc8f43` — D-CH-1 closed on the code

Premise REFUTED by reading AND by execution: over the estate's 30-settlement scan, 2 of 2 removals
carry `{type, subject, reason}` plus a coherence trace; the receipt dates to `c1ea091f7` (2026-07-26),
a month BEFORE §503.4.

**Two additions the brief did not have.** (1) The reader-facing aggregate is **two** surfaces, not
one: `generationContracts.js:132` → `IdentityDailyLife.jsx:337-338`, and `ViabilityTab.jsx:195`.
(2) The unreceipted deletion D-CH-1 imagined **does exist, in a sibling**: `assembleInstitutions.js`
exclusive-group displacement at `:290`, `:453`, `:529` splices the displaced row and traces only the
incoming one. Different act (replacement in a declared group), so recorded and PRICED, not cured.
Coverage read, not claimed: no existing arm asserts a removal's subject and reason; recorded as owed.

**Gate.** focused + `tests/docs` (six pre-existing rows) · `tests/generators` 2 reds, both 20s
timeouts, green alone 9/9 · `tests/lint` 9 reds across 8 files under load 115–226, **all eight files
green alone: 279/279, EXIT 0** · `tests/copy` unchanged · lint 0 errors · check:quick exit 0.

## Chair addendum compliance (from car 7 on)

- `tests/copy` WHOLE run in cars 7 and 8, and retroactively measured for car 6.
- **No em dash or exclamation point added to any user-facing string** — every src byte this lane
  added since car 6 is a comment, and the E2 ratchet reads string literals.
- ⛔ **REPORTED, NOT ACTED ON:** `voiceMechanics` E2 is RED AT THE BASE —
  `labelBands.js` em:0→5 and `generalStateProse.js` em:0→3 against the baseline. **Neither file is
  touched by any LT40 car** (`git diff --name-only f73bdbf16..HEAD` does not list them); they were
  last written by `e5e3b2647` and `efb5111cd`, both ancestors of the slot. The debt GREW, so the
  addendum says rewrite — but those are another desk's RENDERED PROSE, so a rewrite moves output on
  every town (§764.3, owner-gated), and banking a rise would raise a baseline. **Left for the chair.**
- Seat/Lane trailers carried on cars 7, 7b and 8. Cars 6 and 6b landed before the addendum arrived.

## Nothing in OWNER-GATED was touched

No cure of the `compound.inst` snapshot; no `history.age` cure; no `fires(n)` constant; no
`Fish market` relabel; no `settlement.services` populate/retire; no lifting of the banked dark-pool
cures; no golden re-record and no "N of 525" claim anywhere; no push, deploy, migration, deletion;
no ceiling raised (economicState.js stands at 897 against the frozen 900, two lines of headroom).
