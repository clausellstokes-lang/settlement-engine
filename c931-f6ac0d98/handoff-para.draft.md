**⭐⭐⭐ STATE AT 09-15 __STAMP__ EDT, THE COMMIT CLOCK (refreshed by the Fable 5.1 chair, session f6ac0d98, a NEW window after f86a239c died on the WEEKLY LIMIT at ~06:1x — "resets Sep 20 at 2pm"): THE LONG TAIL'S FIRST LANDING IS DOWN (§931) — `claude/composite-r4` ADVANCED `f73bdbf16` → `__CAS__` (__N__ cars: LT15 · LT42 · LT16 · LT17 · LT29 · LT30 · LT36 · LT41 cars 1–3 · LT28 · LT39 · LT40 + the chair's fix car, the register car, the totals car, the capsule car; sealed `landing-longtail-2026-09-15`); the gate green on a quiet box (`<f6ac0d98>/c931/gate-931.log`: __GATE__); chair-verify green; the ledger through this commit.** ⛔⛔ THE LINEAGE IS UNCHANGED: product code lives on `claude/composite-r4` (NOW `__CAS__` — THE NEW BUILD SLOT); this branch's `src/` is a fossil; cut every code dock from the NEW slot; land ledger docs here by `chair-commit.sh` only after regenerating each file from `git show HEAD:`. WHAT THE LANDING PAID FOR (§931.3, memory `a-per-file-ours-theirs-cherry-pick-rule-…`): a per-file OURS/THEIRS cherry-pick rule half-carried LT28's two-file voice cure and dropped two earlier cars' hunks from `testRatchet.test.js` — three-way-check every file against the car's parent after any scripted compose, then fast gate + negative control BEFORE the registers; and every car needs a strict `Seat:` trailer (`Seat: Opus 5 — Fable-unvalidated` + `Lane:` on a lane car) — put it in every dispatch, or add it by a message-only rewrite (`seat-msg-filter.py`) after the compose. IN FLIGHT from f6ac0d98 (Opus, four build lanes, each writing `<f6ac0d98>/report-LT<N>.md` and `progress-LT<N>.txt`, each obeying `$SC/HOLD-VITEST`): __INFLIGHT__. THEIR CARS LAND FROM THE NEW SLOT at the next landing (cut `laneCONSIST-932` at `__CAS__`; `compose.sh` + the trailer filter; the docks stay cut at `f73bdbf16` — cherry-picks, never rebases). THE OWNER'S DESK is unchanged and grows only by pointers: `docs/OWNER_SITTING_2026-09-15.md` (+ the 118 security verdicts; rung 19's one indivisible commit; the §6.8a disclosure); the voice rulings are the chair's (03:1x, LT41b in flight). THE REWRITE STAYS BANKED; the Scribe lineage stays sealed and out. Diary: `$SC/kit/RESUME-NOTE.md` 11:4x → __STAMP__ EDT.
