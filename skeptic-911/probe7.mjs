import fs from 'node:fs';
const SC='/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad';
const R=f=>JSON.parse(fs.readFileSync(f,'utf8'));
const M1=R(`${SC}/capacity-horizon/artifacts/horizon-600y-4s-lit.json`);
const B3=R(`${SC}/capacity/artifacts/research-lit-4s.cases/research-lit-4s-300y-4s-seed1.json`);
const pop=j=>j.behavioral.yearly.map(y=>y.realmDemography.population);
const p1=pop(M1),p3=pop(B3);
const mean=a=>a.reduce((x,y)=>x+y,0)/a.length;
console.log('M1 pop mean y1-100:',mean(p1.slice(0,100)).toFixed(0),' B300 pop mean y1-100:',mean(p3.slice(0,100)).toFixed(0),' ratio',(mean(p1.slice(0,100))/mean(p3.slice(0,100))).toFixed(4));
console.log('M1 pop mean y1-300:',mean(p1.slice(0,300)).toFixed(0),' B300 y1-300:',mean(p3).toFixed(0),' ratio',(mean(p1.slice(0,300))/mean(p3)).toFixed(4));
console.log('M1 pop range:',Math.min(...p1),Math.max(...p1),' B300 range:',Math.min(...p3),Math.max(...p3));
console.log('cost level ratio century1:',(2708.8/2147.6).toFixed(4));
