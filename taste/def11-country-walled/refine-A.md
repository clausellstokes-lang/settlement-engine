1. `[plain]` On the books the country about {settlement} is marked beset.
   - `[face]` Beset is the grade {settlement}'s own country takes in the record.
   - `[face]` Around {settlement} the country is set down beset.
   - `[face]` Entered beset, the country about {settlement} keeps that grade in the town's standing entry.
2. `[plain]` Beset country about {settlement} stands as the ordinary case.
   - `[face]` About {settlement} the country is harried in the common course.
   - `[face]` For the country about {settlement}, beset is the plain state.
   - `[face]` Harried is the country about {settlement} in the way that holds.
3. `[plain]` Settled is a grade the country around {settlement} does not reach.
   - `[face]` In the entry the country about {settlement} falls short of settled.
   - `[face]` The country about {settlement} misses the settled grade on the town's own books.
   - `[face]` A settled grade the country about {settlement} does not bear.

--- NOTES

**Packet.** DS-DEF-11 · `country: pressed (walled)` · REFINE, arm A (Opus refiner; a different author from the drafter). Refined one for one from `draft-round-4.md`: 3 variants, 12 faces, none added, none dropped, none merged (§22). Card re-read by `node scripts/prose-licence-card.mjs DS-DEF-11 'country: pressed (walled)'` in `$SC/laneTASTE`; the block's annex re-read at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:6018-6107` — both `WALLED-STRAINED` spines and the two sibling modifier pools mounted beside this one. Every face still asserts exactly one claim, the card's own: `settlement.config.monsterThreat` holds, as a STANDING fact of the record. **No claim was added, sharpened into a second fact, or moved.**

**Per face — what changed and why.**

1. `1-plain` — `is entered at the beset grade` → `is marked beset`: the grade NOUN was doing nothing the grade WORD was not, so the line loses three words and lands flat on the fact instead of the rubric; the pool's plainest row is now also its hardest.
2. `1-f1` — tail cut from `in the town's standing record` to `in the record`: the long frame was the weakest half of the strongest rhythm in the pool, and cutting it lets the inversion (`Beset is the grade …`) carry the whole line; the modality word `standing` moves to `1-f3`, where it earns its place on a record noun.
3. `1-f2` — `is marked beset in the entry` → `is set down beset`: `in the entry` was a vague locative doing rubric work, and `set down` is the clerk's own idiom for the act of recording, so the shortest face in the packet (8 words) now names the act in the verb and needs no frame at all.
4. `1-f3` — `wherever the books are kept` → `in the town's standing entry`: the old tail implied custodians and copies the card licenses nothing about, and repeated `kept … keeps`; the new tail is the card's own modality (`STANDING`) fixed to one named record of one named town, and the face stays the packet's longest (14 words).
5. `2-plain` — `the ordinary state` → `the ordinary case`: `state` moves to `2-f2` so that variant 2's four tails are four different nouns (case · course · state · way) instead of four synonyms in one frame, which is the refuter test §23 hands out.
6. `2-f1` — `harried in the plain course of things` → `harried in the common course`: `of things` is filler and `plain` was needed elsewhere; the truncated idiom is denser and, per §21.4, a reader who completes it himself is being rewarded, not obstructed.
7. `2-f2` — `beset is the common case` → `beset is the plain state`: the fronted-dative rhythm is kept whole and only its noun and adjective move, so this face and `2-plain` stop reading as one sentence written twice.
8. `2-f3` — `in the case that stands` → `in the way that holds`: the old tail repeated `2-plain`'s noun and `2-plain`'s verb in one breath; the new one lands the full inversion on a standing verb and takes the variant's fourth distinct noun. **This is the face I could lift least** — the inversion is right, and every denser complement I tried either brought a record noun into the one wholly world-facing variant or reached for a second fact.
9. `3-plain` — `the country about {settlement}` → `the country around {settlement}`: the smallest change in the packet, and deliberately so — the inversion and `does not reach` (which encodes the tier's direction, where `outside` and `below` do not) are already at the ceiling; what was left to fix was the pool's `about` habit, now 7 faces of 12 rather than 8.
10. `3-f1` — `Outside the settled grade the country about {settlement} is held in the record` → `In the entry the country about {settlement} falls short of settled`: the passive `is held` was the limpest verb in the packet and the line closed on an abstraction; it now opens on the record and closes on the sibling band word itself, which is the sharpest landing the licence allows (R-DA-02 wall 5: the rejected alternative is a sibling band of this pool's own read field).
11. `3-f2` — `on the town's books` → `on the town's own books`: two words that turn a generic record into this town's record, the one specificity the card's `of the record` clause licenses free, and the face becomes variant 3's long line so the variant is not flat.
12. `3-f3` — `does not carry` → `does not bear`: `carry` reads as conveyance and `bear` as what an entry bears, so the fronted-object rhythm now closes on the fourth distinct verb of the variant (reach · falls short of · misses · bear) with no softening of the negation.

**What was refused, and held at the draft's number.**

- **The SOURCE citation stays at 0 of 12.** The card prints `source: road · standing LICENSED`, but S3 licenses a citation only where a clerk would cite — two accounts that disagree, a count from an interested party, a record whose keeper is a power — and none of the three holds on this field. MOVE-GRAMMAR §4.4.3 budgets the move and refuses the habit. §21.3 settles it: where the sharper wording and the licensed one part, the licensed one wins. Chair-vetoable; the lawful form and its ration are in the draft's R1 and are unchanged by this round.
- **No coordinate tail anywhere.** The packet contains no `and` at all. This is deliberate: the 12 WITHHELD rows this pool carries are raised on the spine's own trailing coordinate, and a face that grew one would put the same finding on a writer's line.
- The draft's R2–R14 all stand as written; nothing this round reopened one.

**The shapes, re-checked face by face against their detectors.** One sentence each · `{settlement}` exactly once, never initial (T-F8), never final (`…ment`, `closers.abstractNounRate`) · no semicolon, colon, em dash, question, exclamation, parenthesis, digit or percent · no `which` · no `-ly` adverb · no `-ing` opener (`Entered` is a past participle) · no `There is` / `It is` opener · no doubled-adjective pair · no comma triad · zero pronoun closers and zero `closers.abstractNounRate` hits (last words: beset · record · beset · entry | case · course · state · holds | reach · settled · books · bear) · one `CLOSE_KINDS.abstraction` closer in twelve, down from two · no `QUANTIFIERS` member (`not`, never `no`) · no `FUTURE_INDICATIVE`, no past tense, no `armC3` temporal · no `DUTY_PREDICATES` or `DUTY_STEM_NOUNS` · no `PROVENANCE_LEXICONS` member of any of the four classes · no `SUPPLY_CLAIM_LEXICONS` member (`pressed` above all) · no `RECORD_CITATION` (no record noun is the subject of a citation verb; the record never speaks) · no `SPECIFICATIONAL_COPULA` on either limb and no wh-cleft · no `BARE_RELATIVE` · no `CONTRAST_SHAPES` match · no `RELATION_LEMMAS` member.

**Measured position of the set, reported as information only (§21.1).** Words per face 10 · 11 · 8 · 14 | 9 · 10 · 10 · 11 | 11 · 11 · 13 · 10 — mean 10.67, sd 1.55, range 8 to 14, against the draft's mean 11.50, sd 1.50, range 9 to 14. The spread is wider and the mean is LOWER: no face was padded toward the middle of the 8-to-30 window (§21.4; the draft's §5 measurement stands — the band arm scores the face alone and returns the identical row for every one-sentence face in the sample, so length buys no band position and is a craft matter only). `{settlement}` sits at word 7 · 5 · 2 · 6 | 4 · 2 · 5 · 6 | 8 · 7 · 4 · 7 — mean 5.25, maximum 8: the town's name is handed back early in every face, which is the thread this modifier can carry, the spine's own nouns being barred to it (the wall, the watch, the muster, the wages are the fields the spine tests). All twelve openers remain pairwise distinct on their first two words, the three `[plain]` rows included (`On the` · `Beset country` · `Settled is`), so `synonymSwaps` is expected at 0 as before. Record nouns stand in 5 faces of 12, unchanged: variant 2 remains wholly world-facing.

**Expected gate delta: none on the scored channels.** The walk's 12 WITHHELD are the spine's post-semicolon segment and no wording of a modifier reaches them; every face still carries `{settlement}`, so `armQualify`'s `consider` returns early on each of them exactly as it did on the draft. If any scored verdict moves, this refinement should revert (§21.2), and the draft is lawful and intact.
