1. `[plain]` The purse at {settlement} falls short of the standing charge on its defences.
   - `[face]` What {settlement} lays out on its defences does not run to their upkeep.
   - `[face]` Against what its defences come to, {settlement}'s means run thin.
   - `[face]` Coin set aside at {settlement} for its defences does not reach their keeping.
2. `[plain]` The charge of keeping {settlement}'s defences is met in part.
   - `[face]` What the defences at {settlement} take in their keeping outruns the coin laid by.
   - `[face]` Above the purse at {settlement} stands the upkeep of its defences.
   - `[face]` The keeping of {settlement}'s defences runs dearer than its purse bears.
3. `[plain]` The defences at {settlement} are kept on a purse short of their standing upkeep.
   - `[face]` For the keeping of its defences, {settlement} does not find the charge in full.
   - `[face]` The paid defence at {settlement} stands short of its upkeep.
   - `[face]` Between the purse and the keeping of its defences, {settlement} is found short.

--- NOTES

Seat: Fable 5.1, arm B refiner, a different author from the drafter by design. Block `DS-GEN-3`, pool `purse: short`, refined one for one from `draft-round-4.md` (the lawful floor, measured at `measure-draft-base.json` pool index 6: FAIL 0, owned PASS 72 of 72, every face budgetOk and depthOk, the twenty-four WITHHELD all inherited from the two spines). Three variants, twelve faces, none added, none dropped. The typed lines (ROLE `modifier`, FORM `sentence`, MOVE `PRESENT`, READS `readings.economicGates.military`, RELATION `addition`, ATTACH `scores.military: CRITICAL` `scores.military: WEAK`) stand in the annex and are not repeated here. The licence card was re-read first in the read-only dock (`node scripts/prose-licence-card.mjs DS-GEN-3 'purse: short'`): the one claim is that the gate holds as a STANDING fact; may NOT a count, a cause, a season, a future, a standpoint, a second fact, any spine field; `source: muster` prints LICENSED on the card but arm A13 withholds every citation on this pool because the holder kind resolves to no institution, so no face cites a holder. Every face carries `{settlement}` once and never first, and every face carries the defences forward from the spine, so each reads as the passage's second sentence after any of the six attached spines and after any sibling modifier.

Per face, what changed and why (word count after):

- **1a** (13w) `of its defences` became `on its defences`: a standing charge ON a thing is the liability that attaches to it, which is exactly the upkeep gate on the defence stack; `of` read as the defences' own charge, which is looser. The key's two words `purse` and `short` are kept in the canonical row.
- **1b** (13w) `runs under their upkeep` became `does not run to their upkeep`: `run to` is the purse's own idiom (a sum that does not run to a charge), denser and more specific than the spatial `runs under`, and it lands on the civic noun.
- **1c** (10w) `the means at {settlement} run thin` became `{settlement}'s means run thin`: the possessive tightens the line by two words and sharpens the rhythm of the fronted comparison; the cataphoric `its` still resolves forward to the town, which is the thread.
- **1d** (13w) `set aside at {settlement}` became `set aside at {settlement} for its defences`, and the close became `their keeping`: the earmark now names its object, which is the field's exact scope (the funded outlay on the defences, not the town's coin at large), and closes the poverty reading a sibling field owns.
- **2a** (10w) `is not met in full` became `is met in part`: the same gate stated flat and positive (the lack as the fact itself, never the negation of a fuller state), and it is the truer fact, since the gate's floor means part of the charge is always found.
- **2b** (14w) `in their upkeep outruns the coin` became `in their keeping outruns the coin laid by`: `laid by` fences the coin to what is put by for the defences, so the face no longer reads as a claim about the town's coin in general; `keeping` keeps the head noun off `upkeep`, which 2c holds.
- **2c** (11w) kept byte-identical: the locative inversion is the set's strongest rhythm and lands on the civic noun; every move I tried was either a synonym swap, which arm A5 refuses as a face, or a plainer line, which the density law refuses as the regression.
- **2d** (11w) `runs dearer than its purse` became `runs dearer than its purse bears`: the purse now bears (carries) what it can, and the keeping runs past that, which states the comparison as a measurement in words with both ends named; present, no modal, no edge.
- **3a** (14w) kept byte-identical: the canonical STANDING form with the defences in the subject seat, `standing` being the card's own word; the alternatives I measured either repeated a verb another variant owns (`outruns`, `comes to`) or added a `that` clause that says the same thing in more words.
- **3b** (14w) `does not find the charge` became `does not find the charge in full`: the sharper licensed fact, since the town does find part of the charge; `in full` is a completeness word, not a fraction, and no quantifier enters (round 1's `whole` stays retired).
- **3c** (10w) `Short of its upkeep is how the paid defence at {settlement} stands` became `The paid defence at {settlement} stands short of its upkeep`: the `is how` frame pointed at the fact instead of stating it and was the set's one summarising cadence; the plain indicative lands harder in two fewer words, keeps the scope phrase `the paid defence`, and gives variant 3 a fourth distinct opener.
- **3d** (13w) `stands short` became `is found short`: the audit's own verb, the record finding the account short between the two named things; it also takes the doubled `stands` out of variant 3 now that 3c carries it.

What was checked, outside every dock, from standard input with every regex and word list re-declared by hand: word count 10 to 14 on all twelve (none under 8, none over 30); twelve distinct two-word openers; zero participial openers; zero abstract-suffix, pronoun or slot closers; zero contrast shapes on all eight limbs; zero triads, doubled adjectives, -ly adverbs, expletive openers, specificational copulas, record citations; zero quantifiers, magnitudes, count nouns, duration words, rationed words, carve-out tokens (R-DST-W4-c); zero hits on the ABSENCE, CONTRADICTION, PROVENANCE, OPEN, CONSEQUENCE, HISTORY, INSTITUTION, PERSON, TRADITION, GEOGRAPHY and OBJECT detectors, so every face falls through to PRESENT, the declared move; zero spine nouns (watch, garrison, muster, arms, hall, gate, households, walls) and zero spine-field tokens; no digit, semicolon, colon, em dash, which-clause, question, exclamation or modal. Within-variant content-token overlap runs 0 to 3,333 bp, the widest pair being 3a/3d, as in the draft. Every face carries exactly the parent's slot set. No file was written anywhere but this packet; no dock byte moved; nothing executed in a dock but the licence-card script.
