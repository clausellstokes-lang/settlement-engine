1. `[plain]` The purse at {settlement} falls short of what its defences cost to keep.
   - `[face]` What {settlement} lays out does not answer the upkeep of its defences.
   - `[face]` Less coin goes to the defences at {settlement} than their keeping.
   - `[face]` Set against the charge of holding its defences, what {settlement} pays is the smaller sum.
2. `[plain]` The upkeep of the defences {settlement} pays for is not met in full.
   - `[face]` Where {settlement} pays for its defence, it pays under the keep.
   - `[face]` Whatever {settlement} keeps by paying for, it keeps on short means.
   - `[face]` Short of what it costs is how the paid defence at {settlement} stands.
3. `[plain]` So far as paying goes, the defences at {settlement} are kept short of their upkeep.
   - `[face]` For the keeping of its defences, {settlement} does not find the whole charge.
   - `[face]` The defence {settlement} pays for is paid for in part.
   - `[face]` By the muster roll's own account, {settlement} keeps its defences under their charge.

--- NOTES

**The card this set was written against** (read at the dock by `node scripts/prose-licence-card.mjs DS-GEN-3 'purse: short'`): role `modifier` · reads `readings.economicGates.military` (measured) · bag `{settlement: proper}`, FILLED `{settlement}` · relation `addition` · seat/form `sentence / sentence` · move `PRESENT` · angle `plain` · attach `scores.military: CRITICAL`, `scores.military: WEAK` (narrowed by hand in the annex) · covert `no` · audience `player (no mark)` · **source `muster · standing LICENSED`** · may claim: **that `military` holds, as a STANDING fact of the record** · may NOT: a count, a cause, a season, a future, a standpoint, a second fact, any field the attached spine tests (axis · score).

**The predicate, grounded in the producing code.** The card does not recover the block's key-function branch, so the class was read off the leaf that owns the predicate and the generator that writes the field.

- `src/domain/display/stateProse/generalStateProseCandidates.js:56-59` — `purseIsShort` fires when `readings.economicGates.military` is a finite number **and** is `< 1`. The leaf's own docblock states that PRESENT is half the predicate: the generator writes the key only under `hasAnyDefense`, so an absent gate is an ABSENCE and never `1.0`.
- `src/generators/defenseGenerator.js:189-192` — `milUpkeepMult = min(1, 0.6 + (econOutput / 50) * 0.4)`, and where `hasAnyDefense` and the multiplier is below one, the military score is recomputed as the community baseline plus the gated remainder. The comment above it is the field's own definition: the gate is the **economic-upkeep gate (garrison wages, wall maintenance)**; **the community baseline (armed households, terrain alarm) is unpaid and exempt, and only the funded portion above it is gated.**
- `src/generators/defenseGenerator.js:467` — the value written is `milUpkeepMult` rounded to two places, present only under `hasAnyDefense` (walls **or** garrison **or** militia **or** watch **or** mercenary **or** charter hall).

So the ONE claim every face carries is: **the outlay {settlement} makes on the defences it pays for is below what keeping them comes to** — a standing fact of the record, no count, no cause, no capability. That reading is why the set never names men, arms, a wage or a garrison (see refusal 3): `hasAnyDefense` fires on **walls alone**, so a face asserting paid men would be false on a walls-only town and would carry a different typed claim from its siblings (arm C; MOVE-GRAMMAR §3.4).

**Why every face carries `{settlement}`, and why none opens on it.** ARCH §8.3: a modifier whose surface names neither a slot nor a band word is not licensed; it is the summarising beat (arm Q; S14). The bag offers `{settlement}` alone, so it is the only lawful naming, and it is carried exactly once by all twelve faces, so the `{slot}` set is identical inside each variant (ARCH §2.5, T-F8). None opens on it: a sentence face opening on a `proper`-typed slot is refused by the same table.

**The three semantic variants** all assert the one licensed claim (arm C bars a claim difference inside a pool) and differ in construction, never in assertion: **1** sets the purse against the charge (money as subject); **2** states the upkeep as the thing unmet (the cost as subject); **3** states the paid defence's standing condition (the thing kept, as subject). Close kinds vary across the set — a condition, an object, a measurement in words, a standing fact (R-DA-04's close-kind rule); **no face closes on a pronoun** (`closers.pronounRate`).

**Per face — the clause that licenses each claim**

*Variant 1 — the purse against the charge.*
- `[plain]` *The purse at {settlement} falls short of what its defences cost to keep.* — the shortfall: `may claim: that military holds, as a STANDING fact`; the town named: `bag {settlement}` FILLED. "falls short" is the register's plain measurement idiom and is the pool key's own word; it is named here for the refuter as the nearest idiom risk in the set.
- `[face]` *What {settlement} lays out does not answer the upkeep of its defences.* — one claim, stated as a negation of sufficiency; "lays out" is the outlay, not a sum, so no count is asserted (`may NOT: a count`).
- `[face]` *Less coin goes to the defences at {settlement} than their keeping.* — a comparison written as a measurement in words, the form R-DA-11 licenses in place of a figure; the ellipsis after "keeping" is the clerk's compression, lawful under §21.4.
- `[face]` *Set against the charge of holding its defences, what {settlement} pays is the smaller sum.* — a fronted comparison; "sum" is a magnitude class, never a stated count (R-DA-16's COUNT move is not taken).

*Variant 2 — the upkeep as the thing unmet.*
- `[plain]` *The upkeep of the defences {settlement} pays for is not met in full.* — "the defences {settlement} pays for" is the field's own scope (the funded portion above the unpaid community baseline, `defenseGenerator.js:186-192`), which is exactly what the gate multiplies and nothing wider.
- `[face]` *Where {settlement} pays for its defence, it pays under the keep.* — the fronted adjunct is the frame of the same fact, not a second one; the antique air sits in the noun ("the keep"), never in the syntax (R-DA-18).
- `[face]` *Whatever {settlement} keeps by paying for, it keeps on short means.* — concessive fronting; the concession runs over the field's own scope, never over an unheld fact; the short line the register card licenses.
- `[face]` *Short of what it costs is how the paid defence at {settlement} stands.* — a fronted predicate closing on a standing condition; present, no future indicative, no subjunctive edge (R-DA-07; A2; STATE never FATE).

*Variant 3 — the paid defence's standing condition.*
- `[plain]` *So far as paying goes, the defences at {settlement} are kept short of their upkeep.* — the adjunct fences the claim to the paying, so no reader takes it for a capability claim, which is the spine's field and barred to this pool.
- `[face]` *For the keeping of its defences, {settlement} does not find the whole charge.* — "the whole charge" is a quantity of money, not a totality over persons; the REFUSED COLUMN (a totality over persons) is untouched, and no `closed` column is quantified (R-DA-15).
- `[face]` *The defence {settlement} pays for is paid for in part.* — the shortest line in the set; the chiastic repetition is rhythm, not a second fact.
- `[face]` *By the muster roll's own account, {settlement} keeps its defences under their charge.* — **the set's one PROVENANCE move** (MOVE-GRAMMAR §4.4.3; REGISTER-CARD amendment S3). Claim one: the gate holds, as above. Claim two: the muster is the holder of this record — `src/domain/prose/holderTable.js:195` maps `economicGates` to `kind: 'muster'`, citing `defenseGenerator.js:467`, and `holderKindOfField`'s right-to-left walk resolves `settlement.defenseProfile.economicGates.military` on `economicGates` because `military` names no row (`holderTable.js:433-435`). Both claims are licensed, which is what S3 requires of a citation. The office does not cite its own books here: the compiling office is the record, the muster is a distinct in-world holder, and the register card's S3 names the muster roll among the holders by that name.

**THE THREAD (owner, ~21:4x).** The composer orders modifiers by salience with the spine first, and this pool's attach set spans six differently-worded spines (`scores.military: WEAK` ×3, `CRITICAL` ×3), which share exactly one noun: `{settlement}` itself. Every face therefore re-anchors on `{settlement}` and hands that noun back, which is what makes a face safe in any position the composer chooses — after any of the six spines, and after the sibling modifier `roll: falling`. A11's echo bound counts facts, not nouns, so the deliberate `{settlement}` echo is lawful by §1.4.1's own sentence. No face changes subject in the middle of the passage; none needs to be the passage's turn outward, because each is a single sentence that lands back on the town. The one face that carries a **second** spine noun forward is the provenance face, which picks up the muster from the `CRITICAL` ledger variant.

**Restatement and contradiction, checked against the six shipped spines** (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:5316-5321`, arms A1 and A11). The spines assert the military axis band — the watch against a garrison, arms counted in households rather than issued from the hall, no muster worth the name, nothing that would stop a comer, nobody having wanted the place. Every one of those is a **capability** claim on `scores.military`. This pool asserts a **funding** claim on `economicGates.military`. No face names the axis, the band, the muster's size, the arms, the watch, the gate or the households, so nothing is said twice. Nothing contradicts either: the gate is present only under `hasAnyDefense`, so on every town that draws this pool at least one paid defence exists to be underfunded, including at `CRITICAL` where that may be the walls alone.

**Refusals — no face was refused; six WORDINGS were, and four findings are carried up**

1. **REFUSED wording: any face naming how short the purse is** ("half its keep", "two parts of three", "sixty in the hundred"). `may NOT: a count`; and a digit in prose is a wall (A4, R-DA-16). The gate's own arithmetic (`0.6 + (econOutput / 50) * 0.4`) is a number the record does not state.
2. **REFUSED wording: any face naming the reason** ("the town's trade cannot carry it", "a thin revenue leaves the wall unpaid"). `may NOT: a cause`. This one is refused **against the world, not by it**: `econOutput` genuinely drives the gate and the gate genuinely drives the score (`defenseGenerator.js:189-192`), so a causal face would be TRUE and still unlicensed — `econOutput` is a second field this pool does not read, and MOVE-GRAMMAR §1.2 row 2 gives HISTORY/CAUSE only to an event-provenance field. Named so no later author re-proposes it as an obvious improvement.
3. **REFUSED wording: any face naming paid men, wages, the garrison or the watch** ("the garrison's wages are behind", "the watch is paid late"). Two grounds, either sufficient: `hasAnyDefense` fires on **walls alone**, so a wages claim is false on a walls-only town and would break the face contract inside the variant (arm C); and the garrison and the watch are institutions the attached spines' own text works in, so naming them risks the restatement arm A11 as well as `may NOT: any field the attached spine tests`.
4. **REFUSED wording: any face naming a season or a term** ("this year the purse is short", "since the last levy", "by the spring"). `may NOT: a season`; and a term boundary is a HISTORY move with no event-provenance field behind it (R-DA-19).
5. **REFUSED wording: any face taking the edge** ("unpaid men would drift off", "the walls would go unrepaired in a bad year"). `may NOT: a future`; and a subjunctive edge here would assert a capability outcome, which is the spine's field. THE PROMISE: state never fate.
6. **REFUSED wording: any citation of a holder other than the muster** ("the treasury's books show", "by the hall's own reckoning", "the reeve's count"). The holder table resolves this token to `muster` and to nothing else (`holderTable.js:195`); a treasury citation would be an inference wearing a citation, which is the exact failure the table's own docblock names. Naming the hall or a reeve would additionally be an INSTITUTION move with no institution-table row behind it (R-DA-15; MOVE-GRAMMAR §1.2 row 5).

**BANKED, not shipped — the non-citing replacement for face 3d.** If the sitting sets the provenance budget to zero for a player-audience pool (see F2), the lawful substitute is: *`[face]` Under the charge of keeping them is where the defences at {settlement} are paid.* (13 words; first two words "Under the", distinct from all twelve.) It is recorded here so the family stays at four with no trim (§22 (b)).

**Findings carried up**

- **F1 — the sentence band for a modifier is UNPINNED in the documents read.** ARCH §6.1 pins `FRAGMENT length ≤ 12 words (ESTIMATE; measured at car 6)` and the unit's `≤ 2 sentences · ≤ 3 facts · one sentence seat`, but no word band for `FORM: sentence` at the modifier grain. R-DA-05/R-DA-06's figures (`wordsPerSentence.sd ≥ 8.5`; `> 30 words ≤ 0.340`; `< 8 words ≥ 0.030`) are REGISTER-grain, measured over whole variants, and cannot be read onto a twelve-face add-on pool. This set was written to a declared working band of **8 to 16 words, interior 10 to 14**, taken from the load a one-sentence addition can carry beside spines that run 15 to 26 words. **Measured: 13 · 12 · 11 · 15 / 13 · 11 · 11 · 13 / 15 · 13 · 10 · 13 — mean 12.5, min 10, max 15, sd 1.50.** The sd is narrow by the form's nature (one sentence, one claim, one object); **the modifier sentence band wants a number at car 6, and a spread floor wants deciding with it.** The sibling packet at `taste/def2-stores-short/draft-round-1.md` declares the same working band from the same absence, which is two writers arriving at it independently and not a ratification.
- **F2 — the PROVENANCE rate is shipped at 1 of 12 (0.083) with the budget unset.** MOVE-GRAMMAR §4.4.3 says the rate is measured at the taste (car 6) and set at the sitting (car 7), so there was no number to obey; the card says the citation is licensed "where the provenance budget allows", and the owner's directive says provenance is a **move, never a habit**. One face in twelve is the reading of "a move" this set took, and it is the taste's job to price it. **Two frictions, named rather than smoothed:** (i) REGISTER-CARD S3 says the player's page states the fact **as compiled** and the referee's page names the keeper — that sentence is written about an *interested* fact with two faces, and this pool is not covert and has no DM twin, but a refuter may read it as barring any player-audience citation; the card's own `source` line is the licence this face stood on. (ii) The `CRITICAL` ledger spine says the town keeps no muster worth the name, so a citation of the muster roll on that one cell of six reads with friction; it is not a contradiction, because the gate's presence requires a paid defence to exist and the roll is a record, not a boast. **The chair's call, and the banked replacement is written above so the refusal costs no face.**
- **F3 — ARCH §6.5's relation ground is WRONG on this pool; the card's is right; both land at `addition`, so no byte of this set moves.** ARCH §6.5 states that "the source-(c) row `(economicGates.military, scores.military)` HAS the spine's field as an endpoint". The shipped table holds no such row: `src/data/dossierRelations.generated.js:1132-1138` carries `src/generators/defenseGenerator.js::econOutput|economicGates.military` — a `consequence`, source `c`, direction `a→b` — whose endpoints are **`econOutput` and `economicGates.military`**, and **`scores.military` appears nowhere in the file** (grep over all 166 relation rows: zero hits). So the card's stated ground — *no relation-table row joins this field to the spine's primary field, and `addition` is the claim-free floor (ARCH §4.5)* — is the correct one, and ARCH §6.5's parenthetical should be struck or re-derived. The set is unaffected because both routes seat the modifier at the sentence seat under `addition`, which is what was written.
- **F4 — the ATTACH sets disagree between the architecture and the shipped annex, and this set is lawful under either.** ARCH §6.5's table narrows `purse: short` to `scores.military: WEAK`, `CRITICAL`, **`ADEQUATE`**; the annex row at `RECEIPT_POOLS_DOSSIER_STATE.md:5382` and the licence card both name **two** keys, `CRITICAL` and `WEAK`. The set was written against the card's two. It was then read against the three `ADEQUATE` spines as well (enough men to be taken seriously and not enough to be feared; the muster covering the gates and not stretching past them; holding against what it expects): every face is true, none restates, none contradicts, because the funding claim is orthogonal to the band at every rung. **So widening the attach to three costs no re-authoring**; the chair should reconcile the two documents in either direction. The card's own echo line records that the echo table is keyed on the producer-token root `readings.economicGates`, coarser than this pool's read, and that `readings.economicGates.military` is absent from the census's fact index — so the **echo bound is NOT-EXECUTABLE here and the ATTACH set is the only guard**, which is what makes the disagreement worth a ruling rather than a shrug.

**Marks.** `covert: no`, `audience: player (no mark)` — no variant carries a `dm-only` mark. No `[grammar: Vn]` tag is written: ARCH §2.5's tag row scopes that tag to spine and turn variants, not to a modifier pool.

**Mechanical checks run on the twelve faces** (by script, over this file's variant block): zero digits, zero percent signs, zero em dashes, zero exclamation marks, zero question marks, zero semicolons, zero colons, zero occurrences of `which`, `will`, `there is`, `it is` as an opener, zero second-person pronouns; every face opens on a capital that is not the `{settlement}` slot and closes on a full stop; every face carries `{settlement}` exactly once; all twelve first-two-word pairs distinct.

**What was executed.** `node scripts/prose-licence-card.mjs DS-GEN-3 'purse: short'` in the read-only dock; reads of `REGISTER-CARD.md` whole, `RULES-V2-PART-B.md` §1 and §16–§23, `sweep/MOVE-GRAMMAR.md` §1–§3 and §4.4.1–§4.4.3 and §1.4.1, `sweep/CLERK-LAWS.md` §2.4.1 and §2.6.1, `arch-prose/ARCH-COMPOSED-PROSE-v2.md` §2.5, §6.1, §6.5 and §8.3; and in the dock `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:5307-5384`, `src/domain/display/stateProse/generalStateProseCandidates.js:1-90`, `src/generators/defenseGenerator.js:155-200, 440-475, 615-655`, `src/domain/prose/holderTable.js:150-215, 420-450`, `src/data/dossierRelations.generated.js:1128-1200` plus a grep for `scores.military` over that file. **No dock byte was written; no test was run; no file outside this packet was created or modified.**
