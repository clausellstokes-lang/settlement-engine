# VALIDATION ET-2 — desks P (power), S (stressors), WF (war & faith) — Fable seat, 2026-09-12

**Seat.** Fable validation seat, retrospective review of `items-ET-2.md` rows `P-*`, `S-*`, `WF-*` (main + REFUSED sub-tables), the APPENDIX rows those desks point at (NF-1…NF-17), and `CONTRADICTION-TABLE.md` §D (the deity rows) as they bind the WF desk. Read-only on the repo. Every file:line resolved in the dock checkout at `f2da5a3ee` (`scratchpad/dock-f2da5a3ee`), never on the ledger branch.

**Method.** Every row of the three desks was read whole in `items-ET-2.md`, compared against `evidence/convert-ET-2.md` (the fold's working), and the load-bearing ground of every row was resolved in the dock by reading the enclosing function. Two things were EXECUTED rather than read: `deriveTemper` over all nine alignment×law cells, and the `deitySnapshotFrom → deityTemper` round trip. Labels: **CONFIRMED** = I executed or read the cited code at the cited line and it reads so; **PLAUSIBLE** = reasoning only.

**Coverage.** 123 / 123 rows in the three desks validated (P 27 · S 45 · WF 51): CONTRADICTION 37 · MODEL 7 · SCOPE 1 · SPLIT 74 · STRUCK 4. All 42 `[archivist's reading]` cells in these desks (P 10 · S 11 · WF 21) were argued as the archivist's, not the fold's. All 18 §D deity rows and the 13 appendix rows that touch these desks were read.

**Mark vocabulary.** `OK` · `CHALLENGE: <argument>` · `REVERSE: <should be | field:line | floor>`. A REVERSE against a *permission cell* or a *struck half* leaves the verdict token standing but changes what a writer may do; the table says which.

---

## 2.4 POWER — 27 rows

| id | verdict | (b) ground real & engine behaves so | (c) permission safe | VALIDATION |
|---|---|---|---|---|
| P-1 | SPLIT | CONFIRMED `cohesionWeave.js:171-177` ARCHETYPE_TO_RULING (`military→autocrat`, `government→council`); the lens word can oppose the seat label | Kept half safe. Struck half hands back "how often it meets" — a RATE | **REVERSE (struck half):** "how often it meets" survives on floor 2 · F2-06 (the chair's own withdrawn rate example) · false sentence: "The council sits every seventh day." |
| P-2 | SPLIT | CONFIRMED `institutionServices.js:1448-1453` consensus rows; `Town hall` is `required: true` only at town (`institutionalCatalog.js:1550`, inside the town block 923-1588); village has no hall row | Under-fenced | **CHALLENGE:** "never put one on a thorp" should read "below town" (F1-20): hamlet is consensus too and village carries no hall row. |
| P-3 | SPLIT | CONFIRMED `safetyProfile.js:296-310` prints the no-force denial only in the Unsafe/Dangerous branches; `hasWatch` `priorityHelpers.js:48` | Safe | OK |
| P-4 | SPLIT | CONFIRMED `holderTable.js:357-366` office kind = City administration / City hall / Town hall | "a chase for arrears" struck as texture is the elapsed non-payment F2-05 names ("three months' arrears" is F2-02's own example, minus the number) | **CHALLENGE:** arrears is floor-2 grammar; licensed form is a present standing condition ("someone is behind and nobody will say who"). |
| P-5 | SPLIT | PLAUSIBLE (holder kind `court` vs the political noun) | Safe; §R-4 agrees the WORD is free | OK |
| P-6 | SPLIT | CONFIRMED `settlementPolitics.js:149` floor 0.4, `:580-592` ruling bloc, `:604-606` dormant ⇒ 0 | Two defects: "a count beyond two as colour" struck (F2-01); "Name the bloc" — a `compromise` bloc is `covert: true` (`settlementPolitics.js:434-436`) and a minted proper name is F1-126 | **REVERSE (struck half):** a count survives on floor 2 · F2-01 · false: "Five houses sit in the combination" on a two-member bloc. **CHALLENGE:** add the F4-13 audience rider (covert bloc = DM face) and read "describe", not "name", per F1-126. |
| P-7 | SPLIT | CONFIRMED `:439-441` threat detail "at the walls"; `settlementUnderThreat` `:510-526` reads no wall row | Same covert rider missing (compromise glue is covert) | **CHALLENGE:** F4-13 rider — the compromise-glued bloc's leash is "a leash the bloc does not name aloud" and its flag is `covert: true`. |
| P-8 | SPLIT | CONFIRMED `GOAL_END_HINT` `:469-473`, fail-soft `:495` | Struck "a temple as a place" is a roster item (F1-11), not silence | **CHALLENGE:** fence "a temple as a place where a temple-class row stands"; F1-11 binds ALL and the row should say so. |
| P-9 | SPLIT | CONFIRMED `npcLadderState.js:108-110` RUNG_CAP_BY_TIER thorp 1 … metropolis 5 | "who is climbing" — only office-holders hold rungs and the rungs print NAMED NPCs via `{npc}` | **CHALLENGE:** a climber on the ladder is a predicate on a named person (F3-01; F3-06's singular-office rider). Licensed only as an unnamed person NOT on the printed ladder. |
| P-10 | SPLIT | CONFIRMED `rulingPowerCoup.js:103-105` criminal archetype filtered out of contenders | Safe | OK |
| P-11 | SPLIT | CONFIRMED `factionDynamics.js:113-118` govMultiplier bands; fractured = score<30 (F1-59) | Struck "a cause" collides with P-12 / F1-60: the four contributions PRINT (EngineSections) | **CHALLENGE:** "a cause" is struck at P-11 and kept at P-12 for the same field; P-11 should carry P-12's fence (a cause against the printed weight is floor 1). |
| P-12 | SPLIT | CONFIRMED `factionDynamics.js:33-41` famine −20, quarantined −8, controlled −5 | Safe | OK |
| P-13 | CONTRADICTION [archivist] | CONFIRMED `powerStateProse.js:519-524` (>1 → "backing hardens"; <1 → "rejection is breaking") vs `EngineSections.jsx:208-214` (1.15 → "lends them weight"; 0.80 → "doubt loosens") | The cell mis-describes the defect. Both mismatches are DEGREE overruns in the SAME direction, never "opposite"; the "never" clause bars an impossible event (1.15 cannot draw the rejection pool: `govMultiplier > 1`) and misses the real one. And the collision is the KEY's, not the writer's (§R-1 → WIRING, as F1-52's own ⚠ says). "take the old list back entire — a public act, a vote, a protest" is unfenced: narrated as happened it is F2-04 | **CHALLENGE (strong):** re-cut the permission to an INTENSITY bar (in the hardens pool never write the top band's certainty; in the breaking pool never write collapse — 0.80 towns draw it) and fence the list to PRESENT hooks, never events that have happened. Fidelity Finding 3's P-13 fence was NOT applied. |
| P-14 | SPLIT | CONFIRMED `factionCapture.js:136-146` rollup of the worst faction; `powerStateProse.js:962` `faction: governing` | "the unnamed hand in it" is the unexposed-stooge route — `corruption.js:668-690` builds a `covert` set the DM line names by OFFICE only (F4-13) | **CHALLENGE:** the permission needs the audience rider — a captured, unexposed officer is DM-face only. |
| P-15 | SPLIT | CONFIRMED `institutionDescVariants.js:81` city smuggling row with no port gate | Safe (profit/age are R-α floor 2) | OK |
| P-16 | CONTRADICTION [archivist] | CONFIRMED `cohesionWeave.js:275-287` `structuralLensOf` picks MAX power, never `isGoverning`; `stressFactions.js:28-29` doubles Military/Guard? — the dock shows ×0.3 under `occupied` at `:29`; the siege doubling is at `:13-16` PLAUSIBLE (not re-read) | The cell lifts "its fear, its war style" — but `RULING_POWER_LENS` (`:161-168`) carries only `legitimacyNerve`; `fear`/`warStyle` belong to `ECONOMIC_BASE_LENS` (`:141-147`), the lens P-17 says is dark. And the seat-label collision is the KEY's (§R-1 WIRING) | **CHALLENGE:** the permission lifts vocabulary from the wrong table; correct to "its nerve" (coup · unrest · heresy · insolvency · betrayal), and file the label collision as WIRING with F1-53's "take the seat's kind from the seat's own label". |
| P-17 | STRUCK [archivist] | CONFIRMED `cohesionWeave.js:286` passes no base; `powerStateProse.js:668-681` the five pools are dark BY DECLARATION | Safe today ("however the Economy tab supports it" fences it). The chair's own doubt stands: the return is silent if the base field is ever produced | OK — with a recorded RETURN TRIGGER: any writer of `economicState.economicBase` re-lights the row and the lens's fear/warStyle become liftable at P-16's terms. |
| P-18 | SPLIT | CONFIRMED `powerStateProse.js:303-330` first-word ladder + floor; `governanceNarrative.js:294-295` `Fractured — no stable governing authority` | Struck half hands back "duration" and "a trend" — both floor 2 by the fold's own standing rule R-γ and F2-02 | **REVERSE (struck half):** duration and trend survive on floor 2 · R-γ / F2-02 / F2-08 · false: "Unstable for a year now, and worse each season." |
| P-19 | CONTRADICTION [archivist] | CONFIRMED `governanceNarrative.js:499-506` fallback `The guard`; `includesInstitution` `:74-75` substring ⇒ `Watchtower` mints "The watch"; `safetyProfile.js:300/:310` denial (Unsafe/Dangerous branches only — the cell is correctly conditional) | The cell hands back "pay, NUMBERS, training" — a headcount is F2-01 | **REVERSE (permission cell):** strike "numbers" · F2-01 · false: "Forty men on the watch." |
| P-20 | SPLIT | CONFIRMED `rulingPower.js:398-401` demotion needs Register VII | Safe | OK |
| P-R1 | CONTRADICTION [archivist] | CONFIRMED `RULING_CONSOLIDATION_FLOOR: 0.4` `:149`; `:588-590` | Safe (F1-51) | OK |
| P-R2 | CONTRADICTION [archivist] | CONFIRMED `:515-518` the town's OWN posture satisfies the predicate | Safe | OK |
| P-R3 | CONTRADICTION [archivist] | CONFIRMED `:495` `base \|\| 'commerce'` | Safe | OK |
| P-R4 | CONTRADICTION [archivist] | CONFIRMED `:470` `break_vassalage: 'survival'` | Safe | OK |
| P-R5 | CONTRADICTION [archivist] | CONFIRMED `npcLadderKernel.js:683` "first-lit derivation from structural order"; `powerStateProse.js:610` folds a ≤ gap | "Write the climbing" — same F3-01 exposure as P-9 (rungs are named) | **CHALLENGE:** as P-9. |
| P-R6 | CONTRADICTION [archivist] | PLAUSIBLE (`governanceNarrative.js:372-487` strings not re-read line by line; the precedence-ladder header at `:370-372` confirms the shape) | Safe | OK |
| P-R7 | SPLIT | CONFIRMED the roster grep (`priorityHelpers.js:45-66`) | Safe | OK |

## 2.5 STRESSORS — 45 rows

| id | verdict | (b) ground | (c) permission | VALIDATION |
|---|---|---|---|---|
| S-1 | SPLIT | CONFIRMED walls are a probability modifier (`stressGenerator.js:107`, `:123`); port lifeline (`computeActiveChains.js:756-761` "naval route only"); `Gates (if walled)` bc 0.5 (`institutionalCatalog.js:1356-1358`) | Safe | OK |
| S-2 | SPLIT | ⚠ The quoted denial "This famine is not a failure of harvest" is NOT at `stressorDynamics.js:469` — line 469 is the famine×siege synergy note (`siege: { blocksResolution: true, note: 'the blockade stands…' }`). The sentence lives at `src/data/dossierStateProse/stressors.generated.js:1353` — a shipped CORPUS face. The generation summary itself asserts "second failed harvest season" (`stressNarrative.js:81`). Grain Holders `stressFactions.js:137-142` CONFIRMED | The harvest half of the kept denial rests on corpus prose, which §R-1 says is not a record | **CHALLENGE (strong):** the "blockade famine denies a harvest cause" ground is mis-cited and, once located, is a face not a field; only the Grain Holders (rule-based rationing) half has a field. F1-76 in the CONTRADICTION-TABLE inherits the same mis-citation. Also floor 2: "a season" is F2-02 and the record's own "second failed harvest season" is the only licensed season word. |
| S-3 | SPLIT | CONFIRMED `priorityHelpers.js:324` ×0.40; `defenseGenerator.js:387-389` −35/−20; `stressFactions.js:28-29` military ×0.3; `stressTypes.js:38-39` "Local institutions continue under oversight" | "never leave the town's own force standing" contradicts the model it cites: the multipliers REDUCE, they do not remove — and the table's own F1-78 makes "the town's OWN bodies removed under occupation" the finding | **REVERSE (permission):** the force STANDS, diminished; never write it removed and never at full strength · `stressTypes.js:38-39`, `priorityHelpers.js:324`, `defenseGenerator.js:387-389` · floor 4 / floor 1 (the watch row still prints) · false: "The town's own watch was disbanded the day the occupier came." |
| S-4 | SPLIT | PLAUSIBLE (`stressorDynamics.js:49` admin-class resolve not re-read) | Safe | OK |
| S-5 | SPLIT | CONFIRMED `stressFactions.js:76-92` Crown Creditors (Noble Coalition) under Royal Authority | NOT ENTAILED "whether payment is current" is F2-05 (elapsed) and is not on R-α's list; the struck half leaves it unstated | **CHALLENGE (minor):** name "payment is behind/current" as floor 2. |
| S-6 | SPLIT | CONFIRMED `stressNarrative.js:93` "the wound hasn't closed"; `stressTypes.js:70` "The betrayer may still be here" | Struck "longer agreements as a hook" and the permission "Write longer, warier agreements" — "longer" is a comparative against a past state (F2-05) | **REVERSE (struck half):** floor 2 · F2-05 · false: "Since the betrayal every agreement is written twice as long as it used to be." Licensed: "the agreements are long and wary." |
| S-7 | SPLIT | CONFIRMED `stressNarrative.js:96`; `stressFactions.js:105-107` | Safe | OK |
| S-8 | SPLIT | CONFIRMED `stressNarrative.js:99` vs `activeConditions.js:59-61` (plague archetype, label 'Plague'); `safetyProfile.js:134-143` informal-enforcement fallback | Pointer "see NEW FINDING 1" now resolves to NF-1 — but NF-1's reading over-bars (see appendix) | OK on the row; see NF-1. |
| S-9 | SPLIT | CONFIRMED `stressFactions.js:111` `'vacant'` modifier on the still-existing governing faction; `SummaryTab.jsx:32` + `:40` "Power rests with …" | Safe | OK |
| S-10 | SPLIT | CONFIRMED `safetyProfile.js:30-35` 'local watch' below town; `stressNarrative.js:105` "Whether wolves, raiders, or worse" | R-β is applied to a disjunction the record PRINTS as not-knowing; naming an arm is the F1-70 shape ("the absence IS the fact"), and the summary is a persisted record field, not a gloss | **CHALLENGE:** the struck "wolves / raiders" half is the D-2 family the chair itself doubts (STRIKES §1); on the generation record the summary prints the uncertainty an inch away. |
| S-11 | SPLIT | CONFIRMED `stressNarrative.js:115`; `stressFactions.js:188-204` neither opposition armed | Safe | OK |
| S-12 | SPLIT | CONFIRMED `stressNarrative.js:121` `name.length % 3`; `stressFactions.js:301` `N.length % 3` (governing faction name), independent | Safe; "never predicate anything of a deity" is consistent with ADDENDUM 15 (followers act) | OK |
| S-13 | SPLIT | CONFIRMED `stressorsCore.js:175-180` "no slavery substrate"; `stressGenerator.js:250-254` tilt only | Safe | OK |
| S-14 | SPLIT | CONFIRMED `stressNarrative.js:42` 45 % coin; `:145-152` "The garrison is reinforced"; `stressTypes.js:156` conscription on the type row | Safe | OK |
| S-15 | SPLIT | CONFIRMED summary always arrivals (`stressNarrative.js:158-165`, the dead-branch comment explains why); faction gate `economyOutput >= 50` (`stressFactions.js:209-210`) with `getInstFlags` imported live (`:7`) | Safe | OK |
| S-16 | SPLIT | CONFIRMED `stressors.js:770-776` origin-driven; `stressorsCore.js:364-367` peakSeverity; `:740-759` escalation | Safe | OK |
| S-17 | SPLIT | CONFIRMED F5 `stressors.js:616-623` keeps `unattributed` beside a stamped attacker; F6 `stressorDynamics.js:877` `\|\| 'palace_coup'`; `:826-838` servile_uprising off two scores | Safe; `WorldPulseData.js:239` prints Attacker = "unnamed" (DM surface) | OK |
| S-18 | SPLIT | CONFIRMED `archetypeCatalog.js:31` lifts; `activeConditions.js:680-684` no sort | Safe | OK |
| S-19 | SPLIT | CONFIRMED `activeConditions.js:859` window 2; `:878-882` forced easing; `:472/:480/:488` polarity | Safe | OK |
| S-20 | SPLIT | CONFIRMED `conditionPromotion.js:203-208` `'GENERATION'` sentinel | Safe | OK |
| S-21 | SPLIT | CONFIRMED `WIND_DOWN_FRACTION = 0.25` (`stressorsStateProse.js:259`) vs the engine's 2-tick window (`activeConditions.js:859`) | The KEY draws the pool at 25 % remaining; the writer inside it cannot satisfy "never say 'nearly over' outside the engine's window" — the disagreement is the desk's cut, i.e. WIRING under §R-1 | **CHALLENGE:** the kept half charges the face for the key; file a WIRING row (desk 25 % vs engine ≤2 ticks) and let the pool write "running down" without a clock. |
| S-22 | SPLIT | PLAUSIBLE | Safe | OK |
| S-23 | MODEL | CONFIRMED `stressGenerator.js:339-346` independent rolls; `stressorDynamics.js:458-460` "no interaction" outside `STRESSOR_SYNERGIES` — which DO list famine×siege (`blocksResolution`) and famine×disease (`:467-469`) | The permission drops the ground's qualifier: on a listed pair "never say the crises compound" is itself the F4 breach | **CHALLENGE:** restore "outside the listed synergies"; on famine×siege the engine positively models "cannot lift while the siege stands". |
| S-24 | STRUCK [archivist] | CONFIRMED a fill definition | Safe | OK |
| S-R1 | CONTRADICTION [archivist] | CONFIRMED as S-1 | Safe | OK |
| S-R2 | CONTRADICTION [archivist] | Grain Holders CONFIRMED; the harvest half mis-cited (see S-2) | The cell's "never blame a harvest on a blockade famine whose own summary says it was not a failure of harvest" cites a corpus face as the summary | **CHALLENGE:** as S-2 — the harvest half of the denial has no field. |
| S-R3 | CONTRADICTION [archivist] | CONFIRMED as S-3 | "never leave the town's own force standing — occupation strips it" | **REVERSE (permission):** as S-3. |
| S-R4 | SPLIT | PLAUSIBLE | Safe — "clerk" is not a tier-mandatory singular office (`npcGenerator.js:1511-1517`) | OK |
| S-R5 | SPLIT | CONFIRMED `stressNarrative.js:89-90` prints the four kinds as an open disjunction ("or something older") | R-β applies; the faction row fences it | OK |
| S-R6 | SPLIT | as S-6 | "Write longer, warier agreements" | **REVERSE (struck half):** as S-6. |
| S-R7 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| S-R8 | SPLIT | CONFIRMED | Safe | OK |
| S-R9 | SPLIT | CONFIRMED `stressorsCore.js:146` `abandoned_roads` residual | "Say monsters" — see S-10 | **CHALLENGE:** as S-10. |
| S-R10 | CONTRADICTION [archivist] | CONFIRMED `stressTypes.js:119` tier null; `priorityHelpers.js:48` | "below the tier that carries a watch row" — §R-6: the test is the FLAG on THIS town, never the tier (custom rosters, F1-30) | **CHALLENGE:** re-word to "where no watch row stands". |
| S-R11 | SPLIT | CONFIRMED `stressNarrative.js:130-132`; `stressFactions.js:336-340` | Safe | OK |
| S-R12 | SPLIT | CONFIRMED `stressGenerator.js:343` tier gate in Mode 3 only | Safe | OK |
| S-R13 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| S-R14 | SPLIT | PLAUSIBLE (no camp/origin field) | "give the newcomers a story" licenses history elsewhere (F2-04) | **CHALLENGE:** the licensed form is a standing condition ("they do not say where from"), not an origin narrated. |
| S-R15 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| S-R16 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| S-R17 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| S-R18 | SPLIT | CONFIRMED the kept half | Struck "spending on repair rather than on holding" splits the ONE purse the table's own F4-02/F4-03 keep (`defenseGenerator.js:182`, `:189-192`) | **REVERSE (struck half):** floor 4 · F4-02 · false: "The coin goes to the repairs now and not to the men who hold the wall." |
| S-R19 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| S-R20 | SPLIT | PLAUSIBLE | Safe | OK |
| S-R21 | SPLIT | CONFIRMED | Safe | OK |

## 2.6 WARFAITH — 51 rows

| id | verdict | (b) ground | (c) permission | VALIDATION |
|---|---|---|---|---|
| WF-1 | SPLIT | CONFIRMED `warStatus.js:45-53` bare-hostile front; `:103-116` deployments counted before a front; `warDeployment.js:109-113`, `:497-502` `atWalls` gate; `warStatusPoolKey` returns null when besieged (`warFaithStateProse.js:199-200`) | Safe (no besieged pool exists today) | OK |
| WF-2 | SPLIT | PLAUSIBLE (`activeConditions.js:332-347` not re-read) | Safe | OK |
| WF-3 | SPLIT | CONFIRMED `occupation.js:119-125` ladder with `liberated` exit; `:883-894` `occupierStillPresent` | Safe | OK |
| WF-4 | SPLIT | CONFIRMED benefit 0.45 / burden 0.55 (`:135-156`); `occupationStatus.js:102` burdened | Safe | OK |
| WF-5 | SPLIT | CONFIRMED `RESISTANCE_BANDS` floor 0 = 'largely quiescent' (`occupationStatus.js:59-64`) | Safe | OK |
| WF-6 | SPLIT | CONFIRMED `mobilization.js:315,343,349,375` covert false off the ramp | Safe | OK |
| WF-7 | SPLIT | CONFIRMED `EXHAUSTION_DECAY_PER_TICK = 0.03` (`warHomeCosts.js:69`); `:432` levied vassal | Struck "what the town will not do again" is F2-05 grammar (modal future + "again") | **CHALLENGE (minor):** name it floor 2. |
| WF-8 | SPLIT | CONFIRMED `treatyOrientation.js:171-176`; `warFaithStateProse.js:420-426` declared defect | Safe | OK |
| WF-9 | SPLIT | CONFIRMED `peaceTermsCatalog.js:160-183` executors; `burden01` is written on the raw term (`peaceTerms.js:708`) but NOT projected on `treatyDocument.js` (grep empty) — so "its burden" is silence | Safe | OK |
| WF-10 | SPLIT | CONFIRMED executor `transfer` moves granary months (`peaceTerms.js:108-111`) | Safe | OK |
| WF-11 | SPLIT | CONFIRMED `peaceTerms.js:706-707` observed state; `treatyDocument.js:160` normalizer; `:218-240` true-state chip DM-only | Safe | OK |
| WF-12 | MODEL | CONFIRMED `AIRSHIP_RE` (`blockadeTransport.js:20`), "impaired, never inoperable" (`:25-26`) | Safe | OK |
| WF-13 | SPLIT | CONFIRMED `war_spoils` is the OCCUPIER-side benefit, `affectedSystems: []` (`activeConditions.js:451-461`) | KEPT names "a before/after comparison" as floor 2, then the permission says "Write the town flush AFTER the fighting" — the comparative it just kept | **CHALLENGE:** "flush" as a standing condition is free; "after the fighting … more than it went in with" is F2-05; and the condition sits on the occupier, not the fought-over town. |
| WF-14 | SPLIT | PLAUSIBLE | Safe | OK |
| WF-15 | STRUCK [archivist] | CONFIRMED `liveWorld.js:91-97` module-private; `livingWorldSignals.js:152-162` two spellings differ and `null` for the dead band | Safe ("whatever words the page supports" fences F1-107) | OK |
| WF-16 | CONTRADICTION [archivist] | CONFIRMED `peaceTermsCatalog.js:170-172` (`overlay` vs `readiness_cap`); `mobilization.js:302-306` cap as a ramp rung | Safe | OK |
| WF-17 | SCOPE | CONFIRMED `warFaithStateProse.js:56-64` | Safe; consistent with ADDENDUM 15 | OK |
| WF-18 | SPLIT | CONFIRMED `priorityHelpers.js:65` `hasChurch` fires on `shrine`, `priest` | Safe | OK |
| WF-19 | SPLIT | CONFIRMED `deityCommitEmbed.js:135` default `'minor'`; `pantheon.js:54-64` hysteresis 4/2 with `TIER_HOLD_TICKS = 2`; `:383-388` tier written to the ledger only | Safe | OK |
| WF-20 | SPLIT | CONFIRMED `standingFor` (`religionState.js:149-154`): `established` holds down to share 11; suppressed refs dropped by `activeRefs` (`:124-125`) so the routed member is share<15 | Safe | OK |
| WF-21 | CONTRADICTION [archivist] | CONFIRMED `patronFall.js:76`, `:113-116` | Safe | OK |
| WF-22 | SPLIT | CONFIRMED `religionState.js:626` contested, `:629` ×0.65 | Safe | OK |
| WF-23 | CONTRADICTION [archivist] | CONFIRMED six-point margin (`:56`, `:626`); 34 vs 27 is not contested | Safe | OK |
| WF-24 | CONTRADICTION [archivist] | CONFIRMED `faithPanelModel.js:249` cults from the STATIC snapshot list; ranks from the live profile | Safe — and this is the real plumbing behind §D D-12 (see deity section) | OK |
| WF-25 | SPLIT | CONFIRMED `pietyBandLabel` floor band 'secular' (`faithPanelModel.js:77`) | Safe | OK |
| WF-26 | SPLIT | CONFIRMED `pietyTrend` is `target − local01` (`:81-86`), prospective | Safe | OK |
| WF-27 | MODEL | CONFIRMED `religiousContest.js:932-934`; `applyUnaffiliatedSink` `:386-396` | Safe | OK |
| WF-28 | SPLIT | CONFIRMED `classifyPatronFall` precedence (`patronFall.js:110-119`); `suppressDeity` (`religionState.js:276`); `abandoned` absent by measurement (`patronFall.js:62-71`) | Safe | OK |
| WF-29 | MODEL | Half CONFIRMED, half FALSE in the dock. Cross-niche contest (`religionState.js:68-71`, `:283-285`) CONFIRMED. But `authoredTemper` IS carried into the embed: `DEITY_AUTHORED_CHARACTER_KEYS` (`deityCommitEmbed.js:54-61`) lists it, `deitySnapshotFrom` spreads it (`deitySnapshot.js:66`), and `deityTemper` returns it before deriving (`deityAxes.js:150-151`). EXECUTED: `deityTemper(deitySnapshotFrom({alignmentAxis:'evil', authoredTemper:'peacelike'}))` → `peacelike`; with the key removed → `warlike` | "never treat temper and alignment as two independent choices" is false for any deity carrying a valid `authoredTemper` | **REVERSE (verdict → SPLIT):** keep the cross-niche half; the derived-temper half binds only where `authoredTemper` is absent · `deityCommitEmbed.js:54-61`, `deitySnapshot.js:66`, `deityAxes.js:150-151` · floor 4 · false FINDING licensed: a refuter fails a lawful face about a gentle following of an authored-peacelike, evil-aligned creed. The `deityAxes.js` header (`:35-42`) and the walker test's docblock ("STRIPPED by the embed writers") are stale text; F1-93's parenthetical repeats them. |
| WF-30 | SPLIT | CONFIRMED `divineMandateStatus` returns null at `mandateGovWeight <= 0` (`religionState.js:789`) | "Write the clergy's hold on the seat" — the clergy as a body needs a church/cathedral/parish row (F1-23) | **CHALLENGE (minor):** add the F1-23 fence. |
| WF-31 | MODEL | CONFIRMED projection copies exactly seven keys, no `tenure` (`religionState.js:620`) | Safe | OK |
| WF-32 | STRUCK | CONFIRMED no producer: `templeWealth` grep in `src/domain` empty; faith-side `covert` only in `religionLegitimacy.js:127,132` comments and on `warPosture` (`mobilization.js:128,147`); the pools exist in the corpus (`warFaith.generated.js:5024-5170`) against those absent fields | "Write a rich temple or a pilgrim season as flavour" is unfenced twice over: a temple where the only faith row is a `Wayside shrine` / `Access to parish church` (F1-11, `priorityHelpers.js:65`, `institutionalCatalog.js:42, :50-54`), and a "season" is F2-02 / F2-06 (a recurring annual event is a rate) | **REVERSE (permission cell):** the STRUCK verdict stands for the three fields; the permission must read "a rich house of the faith WHERE a temple-class row stands; pilgrims as present traffic, never as a season" · F1-11 + F2-02/F2-06 · false: "Each spring the pilgrims come through and the temple's coffers fill" on a hamlet whose faith row is a wayside shrine. The chair's push 3 was right and the archive did not act on it. |
| WF-33 | CONTRADICTION [archivist] | CONFIRMED `faithPanelModel.js:194` `live` = profile projected | Safe | OK |
| WF-34 | SPLIT | CONFIRMED `WarTab.jsx:513` `hasPatron: !!faithPanelModel(settlement).hasEmbed`; `religionState.js:196-209` cult-led seeding then `selectPatron` | Safe | OK |
| WF-35 | CONTRADICTION [archivist] | CONFIRMED `counterpartName` (`warFaithStateProse.js:930-932`): occupier else `war.counterpart`; the treaty document's sides never consulted | Safe | OK |
| WF-36 | SPLIT | CONFIRMED `warFaithStateProse.js:282-294` inverted roles; `occupationStatus.js:145-147` | Safe | OK |
| WF-R1 | CONTRADICTION [archivist] | CONFIRMED as WF-1 | Safe | OK |
| WF-R2 | CONTRADICTION [archivist] | CONFIRMED as WF-3 | Safe | OK |
| WF-R3 | MODEL [archivist] | CONFIRMED as WF-4 | Safe | OK |
| WF-R4 | CONTRADICTION [archivist] | CONFIRMED `mobilizationStatus.js:29` 'on alert'; `mobilization.js:311-320` deployed pins; `warHomeCosts.js:432` | Safe | OK |
| WF-R5 | CONTRADICTION [archivist] | CONFIRMED as WF-11 | Safe | OK |
| WF-R6 | CONTRADICTION [archivist] | CONFIRMED `WarTab.jsx:486-492` `hasWar` fires on `exhaustionRaw > 0`, `!!war.mobilization`, `!!war.occupied`, `!!war.holdings`; `warStatusPoolKey` residue (`warFaithStateProse.js:204-206`) | The composed cell is faithful to the ground (fidelity nomination checked) | OK |
| WF-R7 | CONTRADICTION [archivist] | CONFIRMED as WF-19 | Safe | OK |
| WF-R8 | CONTRADICTION [archivist] | CONFIRMED | Safe | OK |
| WF-R9 | CONTRADICTION [archivist] | CONFIRMED `SHARE_STEP_MAX: 6` (`religionState.js:48`) | Safe | OK |
| WF-R10 | CONTRADICTION [archivist] | CONFIRMED as WF-24 | Safe | OK |
| WF-R11 | CONTRADICTION [archivist] | CONFIRMED as WF-26/27 | Safe | OK |
| WF-R12 | MODEL [archivist] | CONFIRMED `religionState.js:68-71`, `:283-285` | Safe | OK |
| WF-R13 | CONTRADICTION [archivist] | CONFIRMED as WF-33 | Safe | OK |
| WF-R14 | CONTRADICTION [archivist] | CONFIRMED as WF-35 | Safe | OK |
| WF-R15 | SPLIT | CONFIRMED `warStatus.js:304-310` conquest transfer | Safe | OK |

## APPENDIX rows touching these desks (archivist's reading throughout)

| id | VALIDATION |
|---|---|
| NF-1 | **CHALLENGE:** "neither 'plague' nor 'it is not yet a plague' can be asserted" over-bars against the table's own F1-63 ("state the record you read, deny neither; §R-1") and F1-36 ("the WORD plague is the engine's own"). A DS-STR-1 face may say "not yet a plague" (its record's words); a condition face may say "plague". The bar is on DENYING the other surface, not on using either word. |
| NF-2 | OK — CONFIRMED (`stressNarrative.js:158-165`; `stressFactions.js:209-241`, gate `economyOutput >= 50`). |
| NF-3 | OK — CONFIRMED (`stressNarrative.js:42`; `stressFactions.js:251`). |
| NF-4 | OK — CONFIRMED (`stressNarrative.js:121`; `stressFactions.js:301`). |
| NF-6 | **CHALLENGE:** title "prints the OPPOSITE of its band" is false — see P-13; it is a degree overrun, and an engine fault (§R-1 WIRING). |
| NF-7, NF-8, NF-9 | OK — CONFIRMED (`warFaithStateProse.js:930-932`, `:420-426`, `:282-294`; `treatyOrientation.js:171-176`). |
| NF-11 | OK — CONFIRMED (`SummaryTab.jsx:32`, `:40`). |
| NF-12 | OK — CONFIRMED (`governanceNarrative.js:74-75`, `:499-506`; `institutionServices.js:1561`). |
| NF-14 | OK — CONFIRMED. |
| NF-16 | OK on the finding; its permission inherits WF-32's reversal (fence the temple on F1-11; no "season"). |
| NF-17 | OK — the chair minted `[F2]` at §R-10. |

## §D — THE DEITY ROWS (CONTRADICTION-TABLE.md, ADDENDUM 15 as corrected) as they bind WF

| id | VALIDATION |
|---|---|
| D-01, D-02 | OK (constitutional; consistent with WF-17, S-12, P-7, P-8, WF-30). |
| D-03 | **CHALLENGE:** F3-02's own worked example ("an unnamed priest — all may act") collides with F3-06, which D-03 cites: `TIER_MANDATORY_ROLES` (`npcGenerator.js:1511-1517`) seats exactly one `Parish Priest` at hamlet and one `High Priest` at town/city/metropolis, each a NAMED NPC with a disposition. "The priest" in the singular reads as that person at every tier but thorp/village. The carve-out needs the rider on its own example. |
| D-04 | OK — CONFIRMED `deityTemper` (`deityAxes.js:148-153`). |
| D-05 | OK — CONFIRMED the stored `temperamentAxis` is read by neither arm (`:148-153`; executed: the snapshot's `temperamentAxis:'warlike'` was ignored). |
| D-06 | OK — **CONFIRMED BY EXECUTION** (this seat re-ran it): all nine alignment×law cells give good→peacelike, neutral→neutral, evil→warlike; with `W_CHAOS = 0.3` and `THRESHOLD = 0.15`, the law axis contributes at most ±0.15 and never crosses the dead band. `lawAxis` is inert to the derived temper — as an OUTCOME fact (the coefficient is non-zero; the threshold absorbs it). |
| D-07 | OK — follows from D-06. |
| D-08 | OK — CONFIRMED `DEITY_CORRUPTION_TUNING.axisSign.evil = −1` (`corruption.js:195`); "at birth the clock has not run" PLAUSIBLE. |
| D-09 | OK, with a caveat: F1-92 calls `rankAxis` "the deity's place in the PANTHEON (`DEITY_RANK_AUTHORITY`)" while WF-19/WF-R7 prove it is the AUTHORED embed rank the ledger never writes back (`pantheon.js:383-388`). Both true; a refuter reading F1-92 alone will take `rankAxis` for a seat count. Add WF-19's sentence to D-09. |
| D-10 | **CHALLENGE:** the ground conflates the two facts D-09 separates — `DEITY_RANK_AUTHORITY.cult = 5` (`deityConstants.js:29`) is keyed on `rankAxis` (the pantheon rank), not on `standing:'cult'` (share < 15). Strike "and the authority lift of 5"; share < 15 is the whole ground. |
| D-11 | OK — CONFIRMED `patronRef` is a scalar (`religionState.js:453`, `:623`). |
| D-12 | **REVERSE (mechanism):** "the marker MUST surface `suppressed` BESIDE `standing`" cannot be done — the projection is built over `activeRefs` (`religionState.js:124-125`, `:618-620`), so a suppressed creed has NO row and no standing on the page; the D-10/D-12 collision cannot arise on the projected record. The real leak is WF-24's: `cults[]` is the STATIC embed list (`faithPanelModel.js:249`) never reconciled against suppression, so a suppressed cult still prints there. The mark belongs on `cults[]` entries absent from the live `ranks`. Floor 1 · `religionState.js:276`, `:124-125`; `faithPanelModel.js:249`. |
| D-13 | OK — CONFIRMED (`religionState.js:616-618` returns the settlement untouched with no active deities). |
| D-14, D-15 | OK — CONFIRMED `tenure` unprojected (`:620`). |
| D-16, D-17 | OK — CONFIRMED (`patronFall.js:76`; `faithPanelModel.js:39-44`). |
| D-18 | **REVERSE:** "two deities in one niche" is a REAL engine state in the patron's niche — `religionState.js:195-198`: `if (nicheCount >= (niche === patronNiche ? 2 : 1)) continue;` — "One deity per niche — EXCEPT the patron's niche, which tolerates a single contestant". That contestant is the SCHISM (`CONTEST_*`, `resolvePatronContest`) WF-29/WF-22 describe. The header line 6 D-18 quotes is the stale half-sentence. Should be: two deities in one niche OUTSIDE the patron's niche · `religionState.js:195-198` · floor 1 · false effect: a refuter FAILS a lawful face ("two creeds claim the same calling and the seat between them"). |

---

## FIDELITY-REPAIR CHECK (findings 1–5 of `fidelity-ET-2.md`, as they touch P/S/WF)

| finding | repaired? | evidence |
|---|---|---|
| 1 — the 71 archivist cells tagged | **yes** | grep over `^\| (P\|S\|WF)-…` rows: P 10 / S 11 / WF 21 cells carry `[archivist's reading]` = 42, exactly the CONTRADICTION rows + P-17, S-24, WF-15, WF-R3, WF-R12. Zero `[corrected]` in these desks. The preamble's new "Who wrote the permission cell" paragraph states the rule. |
| 2 — the six code preambles reproduced | **no** | No `§0 — THE CODES` block exists; the preamble still says the `source` cell "is the only thing dropped". In these desks the rows still cite `F2`, `F3`, `F4`, `F5` ×3, `F6` ×3, `0.2` ×4, `0.4` ×3 with no definition inside the archive (S-15 "F3", S-16 "F4", S-17 "F5/F6", S-R16, WF-9 "0.1", WF-19, WF-35, WF-R6 "0.3"…). A reader of S-17's KEPT half ("`palace_coup` is the FALLBACK (F6)") must open the ratified table. |
| 3 — P-13 fenced | **no** | The cell still reads "take the old list back entire — a public act, a vote, a protest, a rival's move are all yours" with no fence; the `[archivist's reading]` tag was added, the fence was not. (D-24 is the sibling seat's.) |
| 5c — R-α mapped to the right column | **no** | Preamble line 17 still says floor-2 survivors "are named in the permission column only where they are that row's principal survivor"; in this archive they sit in the GROUND column (e.g. P-9, S-5, WF-2 "+ floor 2 (…)"). |
| 5d — R-β at full strength | **no** | Preamble line 17 still carries the short form; the three quoted disjunctions and the casualty list (D-2, E-R14, half of S-10/S-R9) are absent from the rule's statement, though S-10/S-R9's own cells cite R-β. |
| "also noted" — S-8 / WF-32 dangling pointers | **yes** | Both resolve inside the archive: NF-1 (S-8, "see NEW FINDING 1") and NF-16 (WF-32, "see NEW FINDING 16"), each stating the pointer it closes. |
| `IT POLICED:` on STRUCK rows | present | P-17, S-24, WF-15, WF-32 all carry it (4/4). |

---

## SLICE VERDICT

The grounds are real. Of 123 rows, every CONTRADICTION and MODEL ground I resolved in the dock reads as the row says it does, with one material exception: the "blockade famine" denial at S-2 / S-R2 (and F1-76) quotes a sentence that is not at `stressorDynamics.js:469` and, once found, is a shipped corpus face (`stressors.generated.js:1353`) — not a field, and by §R-1 not a record. The fold's REAL errors are not in what it kept but in what it handed back: the struck halves and the composed permissions repeatedly return items the fold's own instrument bars — a rate (P-1), a count (P-6), a duration and a trend (P-18), a comparative against the past (S-6/S-R6, WF-13), a split purse (S-R18), a headcount (P-19), a recurring season and an unfenced temple (WF-32), and a force "stripped" where the model and F1-78 say it stands (S-3/S-R3). That is eleven reversals, all on the side the protocol warned is invisible. The deity carve-out (ADDENDUM 15 as corrected) is safe in its doctrine and in D-04–D-08 (D-06 I re-executed: nine cells, three outcomes, `lawAxis` inert at the threshold), and unsafe in three of its rows as written: D-18 bars a real engine state (the patron's niche tolerates a contestant — the schism), D-12 prescribes a mark on a field the projection never carries for a suppressed creed (the real leak is the static `cults[]` list, WF-24), and D-10 grounds a standing bar on the rank's authority lift — the very conflation D-09 forbids. And one premise of F1-93 is false in the dock: `authoredTemper` DOES reach the embed (`deityCommitEmbed.js:54-61`, executed round trip → `peacelike` on an evil-aligned deity), which also turns WF-29 from MODEL to SPLIT and makes the `deityAxes.js` header and the walker test's docblock stale text a reviewer must not trust.

## THE ONE THING

The re-cut was applied to the verdicts but not to the fold's own standing rules: R-α, R-γ, F2-06 and F4-02 exist in the instrument, yet eleven struck halves and permissions in these three desks hand back exactly the rate, count, duration, comparative, purse-split and headcount those rules bar. Run one mechanical pass over every STRUCK half and every `[archivist's reading]` cell against §F2 and §F4 before any of this becomes the refuters' instrument — that pass, not another reading of the grounds, is where the false sentences are hiding.
