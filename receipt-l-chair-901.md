# RECEIPT — lane L-CHAIR-901 (Opus 5 implementer; chair Fable 5.1, 2026-09-05)

Dock `$SC/laneLIGHTINT`, 18 cars over the §900 CAS `04bb92d19`, base tip `871c61468`, sealed
`light-int-2026-09-05`. **One commit: `f2ab6c802`.** Dock tip and porcelain at the foot.

---

## 1 · OUTCOME TABLE

| # | Item | Measured premise at the composition | Cure / refusal | Sha |
|---|---|---|---|---|
| — | **RED A** (base) | `densityCreateBoundary.walker.test.js:440` demands `settlementSlice.js` call `birthConfig(` once. **It calls it ZERO times** — 0 at `af17639a8` (the arm's own landing) and 0 at `04bb92d19`. The mint left the slice at `c9611da70`. **A car shipped a red; not merge damage.** | CURED. Mint count taken at `settlementGenerateAction.js` (1 call, measured); the slice's zero is now asserted too. Claim strength unchanged, only its address. | f2ab6c802 |
| — | **RED B** (base) | `subsystemRowsVirtual.test.js` `VIRTUAL_RULES` = **39 members, 4 duplicates** vs 35 authored rows. The chair's own conflict class was set-merged on the manifest (30→35, clean) and **concatenated** here. | CURED mechanically (the ordered equality admits exactly one union). Named **duplicate guard** added so the next concatenation is told what it did. | f2ab6c802 |
| 1 | No-fence set vs 35 keys | Manifest = **35** (chair's figure CONFIRMED). No-fence set re-derived = **12** (was 7 at 30 keys; `treasuryEnabled` dropped out, the 5 new keys joined). All five new keys **DO** carry source-level fence arms. | LANDED as an **executable derivation** + `NO_FENCE_DISPOSITIONS` roster (key → dark-proof address), checked from the tree both directions. | f2ab6c802 |
| 2 | `missionDispatcher.js` "NO FLAG IS MINTED HERE" | **FALSE** — the flag is manifest member 33 with an authored row in `subsystemRowsOps.js`. Its *literal* half (no gate read in this leaf) is still true and load-bearing. | CURED. Fence arm run **before (pass) → naive cure (RED) → after (pass)**. | f2ab6c802 |
| 3 | Walker's "zero live instances of the computed-access class" | **FALSE, not merely stale.** 4 live frozen-list conjunctions + 3 single-key computed gates. | Prose corrected **and replaced by an executed non-emptiness arm** at named addresses. **Register act refused.** | f2ab6c802 |
| 4 | `readerCorpus.mjs:113` "no gate anywhere reads it" | **FALSE** — `characterDriftActive` reads it strictly; 4 live call sites. | CURED in **both** places the sentence lived; true reason (no production entry point) written in. | f2ab6c802 |
| 5 | `characterDrift.test.js` over-broad `not.toContain` | TRUE (at **:831**, not :791; path is `tests/domain/npc/`). | NARROWED to the brace-matched literal's declared keys — **stronger**, and anchored. | f2ab6c802 |
| 6 | `subsystemRowsCompact.js` dead citation | TRUE, and **worse than reported: FOUR citations were dead**, not one. | 3 re-cited to real pins; the 4th (tier floor) has **no pin anywhere** → row now EXPOSES the gap and names the owed arm. | f2ab6c802 |
| 7 | Density walker's stripper newline half | TRUE — the tip commit says so itself ("The thirteenth … this car deliberately does not touch"). | CURED + both mutants planted with controls. **271 files / 1,322,927 chars recovered, 0 verdict flips in 4,382.** | f2ab6c802 |
| 8 | Golden roster closed BY NAME | TRUE, and quantified: **65 fixtures, 44 claimed, name-arm population = exactly those 44.** | CONTENT-keyed arm added beside the unchanged name arm + enrolled-rot mirror + mis-named plant. | f2ab6c802 |
| + | L-HOMES-9 addendum | Both instruments compute per-preset 52-tick hashes and invite unification. | Non-comparability ruling written into **both** headers. ⚠ `lprobe/pulse-hashes.mjs` is **scratchpad tooling, not in the repo** — that half is uncommitted by necessity. | f2ab6c802 (witness half) |

---

## 2 · PER ITEM — the measured premise, and the cure or the refusal

### RED A and RED B — the dock was red before this lane touched it
Measured at `871c61468`, porcelain 0, **before any edit**: `tests/domain/subsystemRowsVirtual.test.js`
and `tests/lint/densityCreateBoundary.walker.test.js` both failed. Both are cured above. RED A's
provenance was established by `git show <sha>:src/store/settlementSlice.js | grep -c` at two commits,
which is why it is recorded as *a car that shipped a red* rather than as composition damage.

### Item 1 — the no-fence set, re-derived and made executable
The derivation rule is L-HOMES-3's: *register minus keys with a `*DormancyFence` arm*, then each
survivor must carry an equivalent (explicit dark state, bit-level dark claim, lit control). At 35
keys the survivor set is **12**. The five new keys, measured file by file:

| key | dark proof | verdict |
|---|---|---|
| `irregularForceEnabled` | `irregularForceDormancy.byteIdentity.test.js` :56/:65/:78 | fenced |
| `infiltrationDepthEnabled` | `infiltrationDepth.test.js` :289/:751 | fenced at the family door |
| `missionDispatcherEnabled` | `missionDispatcher.test.js` :419/:421/:438 | fenced, + conjunction arm |
| `operationsVoiceEnabled` | `operationsVoice.test.js` :829/:836 | fenced, + truthy-non-true census |
| `envoyTaskCatalogEnabled` | `envoyTaskCatalog.test.js` :594/:596/:615 | fenced at the errand door |

**Why the family gate IS the fence** for the four W-OPS keys: none of the four leaves holds its own
gate. The reads live at `espionageGate.js` :116/:147/:177 and `errandMint.js`:108. A `tests/property/`
fence on a leaf would pin a module with no gate in it, and each suite already drives the key false
against a lit world *and* drives the conjunction — the arm a leaf-local fence could not write.

### Item 2 — and the constraint the brief was right to warn about
The first cure named the door module's path. `espionageDormancyFence`'s reachability arm is a
**raw-source** `not.toContain('espionageGate.js')` — comments are not stripped — so it went RED.
Re-cured with the estate's own file-free idiom (`operationsVoice.js`'s door prose). Executed:
**pass → RED → pass**, and the leaf now carries 0 occurrences of either forbidden token.

### Item 3 — refusal recorded
Opening a fourth receiver arm would pull `characterDriftEnabled`, `magicEconomyEnabled` and
`townCartographyEnabled` into the census, and each would owe an authored certification row in the
same commit. **That is a register act and a chair call. Measured, refused, written down.**

### Item 6 — the fourth dead citation, and a walker blind to the class
`subsystemCertificationTotality.walker.test.js` checks only that a row's `module` PATH resolves. It
never reads prose citations, so a citation naming a live file that says nothing about the claim is
invisible. A census over all 91 rows found **18 content-dead citations and 2 that name files which
do not exist at all** (`tests/domain/informationStatecraft.test.js`,
`tests/domain/settlementPolitics.test.js`). **A content-keyed citation guard is therefore NOT
landable in this lane** — it would red on 20 rows. Reported, not forced.

### Item 7 — zero verdict flips, checked against a parser
2,188 src files. 271 differ between the landed and cured strips; 1,322,927 characters recovered.
**0 reach flips, 0 mint flips, 0 flips on the re-derivation arm's six probes — 4,382 verdicts.**
Every verdict computed a third time with the estate's own character scanner (`codeOnly`): **0
disagreements on all 2,188 files.**

---

## 3 · ADVERSARIAL PASS — every new arm proven able to red

Ten mutants, **10/10 RED as required**, all restored byte-identical by md5.

| plant | mutation | result |
|---|---|---|
| P1 | roster drops an unfenced key | RED |
| P2 | a disposition address rots | RED |
| P3 | computed-access scan goes blind | RED |
| P4 | conjunction anchor moves | RED |
| P5 | content sweep widened past the enrolled set | RED |
| P6 | every enrolled spelling reads as rotted | RED |
| P7 | the concatenation returns | RED |
| P8 | mint home reverts to the module that never held it | RED |
| P9 | a real `DEFAULT_SIMULATION_RULES` entry appears | RED |
| P10 | the newline bound removed again | RED |

⛔⛔ **HAZARD, RE-BITTEN — REPORT THIS ROW TO THE CHAIR.** The first plant harness restored with
`git checkout -- <file>` on a tree dirty with this lane's own uncommitted work. It restored HEAD and
**destroyed four files of finished work** (items 1, 3, 7, 8 and both base-red cures). Detected by the
harness's own md5 comparison, re-applied from context, re-verified, and the harness is now
**copy-based**. The estate's oldest standing hazard is still live and a plant harness is a place it
hides.

---

## 4 · REGISTER DELTAS — **NOTHING MOVES**

- **Lighting titles: 0.** Zero new `it`/`test`/`describe` titles. Every assertion rides an existing
  title — the convention `engineGatedRuleKeys.walker.test.js` states for itself (a title added there
  costs **thirteen**, through its twelve importers). The ten-file suite set measures **263 tests
  before and after**.
- **New test files: 0.** No item needed one.
- **Golden-freeze register: 0 rows, 0 exclusions.** No golden fixture, no env spelling, no 64-hex
  literal in the diff.
- **Voice ratchet: neutral.** `subsystemRowsCompact.js` em dashes 60 → 60, bangs 0 → 0, deliberately.
- **Typecheck ratchet: unmoved.** 173 errors, ceiling 173.

### Gates
| gate | result |
|---|---|
| `npx eslint` (all 10 committed files) | **exit 0** (no hook runs in this repo) |
| `npm run typecheck:ratchet` | **exit 0** — "no type regressions (173 error(s), ceiling 173)" |
| `tests/lint/` WHOLE | exit 1 — **138 of 140 files pass, 2,190 of 2,194 tests**; 4 failures, all PRE-EXISTING |

### The four remaining reds are the chair's, and it is proved rather than asserted
All ten of this lane's files were replaced with their HEAD content (copy-backed, restored
byte-identical) and the suites re-run: **the identical four fail with identical figures.**

1. **Lighting census stale at the composition** — `files` 2,543 vs a frozen 2,537. The 18 cars added
   test files and nobody refroze. *(A refreeze is a register act; the brief forbids one.)*
2–4. **`writerReach` frozen register stale** — `corpusMeta.simulationFlagsLit` 81 vs 80, the
   `shapesDigest` equality, and the `surfaceReach` grammar, which has gained a `world-book` term the
   frozen string does not carry.

**A fifth, not in `tests/lint/`: the voice ratchet is red at the tip** for two display files this lane
never touched — `src/domain/display/labelBands.js` (baseline em 0 → current 5) and
`src/domain/display/stateProse/generalStateProse.js` (0 → 3). Both are clean in porcelain, so the red
is a property of the tip. Curing it means rewriting user-facing prose or banking a baseline growth —
both register/voice acts. **Refused with measurement.**

---

## 5 · BRIEF ERRORS (measured, not inferred)

1. **Dock line** — corrected by the dispatcher; the composition is `$SC/laneLIGHTINT`, 18 cars over
   `04bb92d19`, tip `871c61468` (not `laneLIGHTTRIAL` / `edef0b219`).
2. **Item 5's address is wrong twice** — the file is `tests/domain/npc/characterDrift.test.js`, not
   `tests/domain/characterDrift.test.js`, and the assertion is at **:831**, not :791.
3. **Item 6 understates the damage** — four citations were dead in that row, not one; and the
   "certification citation walker that convicts a dead citation" **cannot convict this class** (it
   checks only `row.module` path existence).
4. **The addendum's second file is not in the repo** — `lprobe/pulse-hashes.mjs` lives in the
   scratchpad (`$SC/lprobe/`). Its header was written as instructed but **cannot be committed**.
5. **"No register act" and "record deltas: lighting titles per new `it`" are in tension.** The
   lighting census pins five figures by EXACT equality, so any new `it` reds it and demands a
   refreeze — a register act. Resolved by landing every arm inside an existing title, which is also
   the estate's own stated convention.
6. **Item 8's plant description is inverted** — "a mis-named fixture WITH a register row" would not
   red. The discriminating plant is a mis-named fixture with **no** row, which is what was built.

---

## 6 · WHAT THIS LANE STOPPED ON

Nothing was left half-done inside the eight items. Work stops at three **chair/owner-gated register
acts**, each measured and refused rather than taken: the **lighting census refreeze** (files
2,537 → 2,543), the **`writerReach` frozen-register refresh** (`simulationFlagsLit` 80 → 81 plus the
`world-book` reach term), and the **voice-ratchet baseline** for the two display files. A fourth item
is docketed rather than built: the **certification citation guard** is not landable today because 18
rows carry content-dead citations and 2 name files that do not exist.

---

## 7 · RETROVALIDATION ROW — what this Opus seat JUDGED, for the Fable chair to re-derive

1. ⭐⭐ **THE COMPOSITION WAS RED AND THIS LANE CURED TWO OF ITS REDS.** Neither was in the eight
   items. Judged in scope because item 7 owns one of the two files and could not have been proven
   over a red one, and because both cures are mechanically determined by measurement rather than
   taste. **Priority: HIGHEST — if the chair wants composition reds handled by the composer, these
   two cures are separable and should be re-homed.**
2. ⭐⭐ **THE "FIVE NEW KEYS ARE FENCED" VERDICT IS A JUDGMENT, NOT A COUNT.** It applies L-HOMES-3's
   test (explicit dark state + bit-level claim + lit control) to five suites. The chair should
   re-derive at least `operationsVoiceEnabled`, whose dark proof is a predicate-level arm rather than
   a byte-identity one. **Priority: HIGH.**
3. ⛔ **AN EXECUTABLE ROSTER WAS BUILT WHERE THE BRIEF ASKED ONLY FOR A MEASUREMENT.**
   `NO_FENCE_DISPOSITIONS` now reds when a manifest key gains an entry without a fence or a written
   disposition. That is structural prevention the brief did not request. If the chair wants the
   derivation to stay a receipt, the roster is one deletion. **Priority: HIGH.**
4. ⛔ **ITEM 6 GREW: a fourth citation was corrected, and one row now declares an UNPINNED
   invariant.** Saying "not pinned anywhere" in a certification row is a stronger claim than
   re-citing, and it is the honest one — but it is a row edit the brief did not scope.
   **Priority: HIGH.**
5. **The certification citation guard is refused on measurement** (20 rows would red). The chair may
   want it landed as a shrink-only ratchet over a frozen baseline instead. **Priority: MEDIUM.**
6. **Item 4 was corrected in TWO files**, not the one the brief named, because the false sentence was
   duplicated verbatim in `readerCorpusManifest.test.js`. **Priority: LOW.**
7. ⚠ **The `lprobe/pulse-hashes.mjs` header edit is uncommittable** (scratchpad, not repo). If the
   chair wants that ruling durable it needs a repo home. **Priority: MEDIUM.**

---

## 8 · DOCK TIP AND PORCELAIN (last, as required)

```
commit  f2ab6c802   L-CHAIR-901: eight chair items land, and the composition's
                    own two reds were the first finding
parent  871c61468   (STRIPPER-UNIFY, the §901 composition tip)
files   10 changed, 557 insertions(+), 36 deletions(-)
```

**Dock tip: `f2ab6c802`.  Porcelain: 0.**
