# WF / WF-8A — the settlement extinction obituary, the lawful resurrection of the refused Chronicle row, and the §321.2a minter-totality arm (stage 1 of the WF-8 train)

- **Status:** LANDED
- **Compile note:** compiled by lane TC-WF8 under ODQ §291.5 / §347.1(2); chair-ruled at ODQ
  **§350** (R-1 the second small family, R-2 the discharge shape, R-3 the severity signature,
  R-4 no new flag); BUILT AND VERIFIED by lane TE-WF8. ⚠ The status value above stands ALONE on
  its line because `parsePacketHeader` anchors the status row at end-of-line — trailing prose
  there leaves the status unparsed and makes the manifest disagree with the Markdown
  (J-TEWF1B-1, paid once already).
- **Verified base:** `claude/composite-r4` at `27c250f94bb7c5c799693a62985c5ddecd4b6c7c`
- **Preamble:** `docs/implementation/preambles/WF-PREAMBLE.md`, cited **BY SHA-256
  `f4cd39fee756c3a192710ab9aa3fc208e18ef1f08ea9e9073bcdc8bfb55d871c`** (780 lines). Recomputed
  at this verified base and byte-identical to the compile's citation. Every refutation,
  disposition, register, STOP and census law in that file binds here and is not restated.
- **Substrate annex:** `docs/implementation/preverification/WF-SUBSTRATE.md`. ⛔ **No absolute is
  inherited.** Every figure the compile carried was re-derived at this base before the first
  edit; the three substrate corrections and the one design divergence are recorded at §7.
- **Design authority:** `docs/DESIGN_FP_ARCH_WF.md` at the **LEDGER blob `169c75e7`** (the copy
  of record; the build copy `f2e5c935` is pre-cure) · `docs/DESIGN_FP_FAITH.md`, THE EXTINCTION
  BEAT, obituary (a) — the settlement-scoped half · **`docs/content/RECEIPT_POOLS_FAITH.md`
  `## §D WF-1`, the GOVERNED CORPUS, whose `faith.extinction.last_altar` pool this member wires
  for the first time** · `docs/DESIGN_FP_SPINE.md` §2 SP-6 and reqs 13/14 · GR-PREAMBLE §P2 ·
  `docs/implementation/packets/foreign-policy/IN-1C-A.md` (the one-row registry precedent).
- **Train:** `wf-8`, family **WF**, member **A**. This is the train's FIRST member; WF-8
  (NARRATION) remains a multi-member train by arithmetic, and this member **does not touch**
  `faithNarrationEnabled` — the beat rides WF-1A's landed `faithUnseatingEnabled`.
- **Member cap:** WF is **STAMPED** (ODQ §78.1) ⇒ engine trains cap at EIGHT.
- **Census authority:** ODQ **§309.3** (the beat re-filed to WF-8), **§321.2(a)** (the refused
  row's absence pin, the ordered minter-totality arm), **§347.1(2)** and **§350** (the dispatch
  and its rulings), under §299.4's binding-forward rule.
- **Constitutional law binding every line below:** the DEITY DOCTRINE — faith is CULTURAL, never
  theological; typed buckets under FINITE-SEMANTICS; AI is clerk, never writer. ⛔ The obituary
  records that **the people stopped keeping the rite**. It never says a god died, left or failed.

---

## §1 · WHAT THIS MEMBER IS

1. **THE PRODUCER.** The settlement extinction beat `faith_last_altar_dark` is minted at the one
   deletion path: the fold reads the pre-prune creed names, consumes WF-1B's landed
   `advanceShares` return, and — behind the already-hoisted `unseating` local — mints ONE outcome
   per creed that has genuinely left the parish. No literal bar is authored; `pruneSuppressed`'s
   own `KEEP` decides what is deleted.
2. **THE CORPUS IS THE ANNEX'S.** `faithReceiptPools.js` transcribes the five authored variants
   of `faith.extinction.last_altar` from the governed FAITH annex byte for byte, and the family
   walker re-derives the whole pool from the document on every run through the shared
   fail-closed reader. This member authors no sentence of the corpus.
3. **THE LAWFUL RESURRECTION.** `chroniclersLetter.js`'s `KIND_SECTION` gains the row WF-1D
   refused dead — now with a LIVE MINTER in the same commit, which is what §321.2(a) ruled the
   row always needed. WF-1D's own refused keys stay absent and its assertions do not move.
4. **THE §321.2a MINTER-TOTALITY ARM** joins the walker estate with the written-ruling escape
   the ruling ordered, the hygiene arms that keep the escape honest, and in-suite controls that
   drive the predicate both ways on every run.

**Nothing else changes.** No flag is minted; dark is byte-identical twice over. No zero-headroom
file is touched — measured, not asserted (§4).

---

## §2 · THE CURE

### 2.1 `faithNews.js` (CREATE) — the FAITH kind registry, the cured picker, the builder

`FAITH_KIND_REGISTRY` carries ONE row, `faith_last_altar_dark`, significance **`major`**,
audience `public`, section `faith`, with `requiredSlots` parallel to the pool. The picker copies
`informationNews.js`'s CURED keyed pick — `hash01` avalanched before the multiply — and carries
NO unseeded arm, because the seam always supplies a settlement id and a deity ref.
`faithReceipt` returns a pulse OUTCOME carrying `candidateType: 'faith_last_altar_dark'`,
`curationClass: 'transition'`, the settlement scope, the tuned severity, the picked authored
headline and the address chain. **No deltas, no condition, no patch, no `deityReembed`** — it is
a record, not a mutation, and `collectFaithDeltas` filters it out by construction.

`FAITH_NEWS_TUNING.LAST_ALTAR_SEVERITY = 0.5` is the member's ONE authored value, chair-signed
under §42/§43 at ODQ §350.2 R-3. Derivation: the conversion outcome in the same fold reads
exactly 0.5 where no piety record exists, and 0.5 sits deliberately below
`newsEntryForOutcome`'s major-entry line so the obituary surfaces as a `notable` RECORD rather
than an alarm. The KIND's cadence class stays `major` — a different instrument on a different
axis, and the battery proves nothing ties them.

### 2.2 `faithReceiptPools.js` (CREATE) — the annex's corpus, transcribed

Five variants, byte-identical to `## §D WF-1`'s `faith.extinction.last_altar` block. One names a
`{temple}` the deletion seam cannot supply, so the eligible set at this base is FOUR — exactly
the derived `major` floor. ⛔ The pool is NOT trimmed to what is reachable, on the CR-IN1C-2
precedent, and the walker pins the eligible set in BOTH directions.

### 2.3 The fold edit (`religiousContest.js`, MODIFY)

Inside `advanceReligionStates`, at the `// 3b. advance shares` block: the pre-prune names are
captured (the prune DELETES the entries, so they are unreadable afterwards), the discarded
return is consumed, and one receipt is pushed per deleted creed **that has no road back**.

⛔ **THE TWO ROADS BACK ARE THE MEMBER'S CENTRAL FINDING AND THEY WERE FOUND BY EXECUTION**
(§7 R-5). A deletion is not an extinction: a creed with a road back re-enters next tick and is
deleted again. Road one is a neighbour still CARRYING the creed; road two is the settlement's
own EMBEDDED snapshots, which `ensureReligionState` re-installs as the dominant patron. Both are
read at the seam from data already in scope, and each is convicted separately in B4.

Dark, the name map is never built, the mint is skipped, and the fold's behaviour is the landed
one — proven by an own-footprint golden captured BEFORE wiring, not assumed.

### 2.4 The three routing homes (+1 row each)

`heraldRouting.js` `EXACT_SECTION`: `faith_last_altar_dark: 'faith'`. The `faith_` family prefix
would route it free; the exact row is the refusal to take the cheap door, and it is what keeps
the estate-wide registered-minus-routed honesty check still at its measured value.
`chroniclersLetter.js` `KIND_SECTION`: `'traditions'` — the letter's own vocabulary, and a
documented SPLIT, so its Herald desk of `faith` is a split outcome rather than a divergence row.
`settlementRumors.js` `WHAT_PHRASES`: one phrase, distinct from the realm-scale sibling's.

### 2.5 The §321.2a arm and the escape

`chroniclersLetter.js` mints `KIND_SECTION_MINTER_RULINGS` (empty at mint, by measurement), and
`tests/lint/heraldRouting.walker.test.js` gains ONE arm asserting that every `KIND_SECTION` key
is minted OR ruled, that no ruling names a minted key, that no ruling names an absent key, that
every ruling spells a §-ref, and that every section value is one of the letter's frozen six.
C2's block in `tests/domain/chroniclersLetter.test.js` is removed with a pointer — a DECLARED
re-record, stated here and in the diff.

---

## §3 · ACCEPTANCE (closed denominator — 8 cases, all in the existing `patronFall.test.js` describe)

⚠ Every fixture was RUN and PRINTED before its pin was written, and the first run REFUTED the
compile's mint rule (§7 R-5).

| id | case |
|---|---|
| B1 | a carrier withdrawal darkens the last altars, each creed named EXACTLY ONCE, through the REAL pulse; the creed carries its authored snapshot name and the town its own |
| B2 | the same ring with NO withdrawal produces no obituary at all — the near-miss negative, with the withdrawal arm as the positive companion travelling the identical filter |
| B3 | per DELETED CREED, never per tick: three altars, three beats, one pulse |
| B4 | both ROADS BACK fenced, each convicted with its deletion proven to have happened first |
| B5 | dormancy: absent and false byte-identical in state AND outcomes; the LITERAL lit drive moves a fenced byte; the rendered feed carries no obituary dark |
| B6 | registration totality both directions, from the kind the fixture ACTUALLY produced |
| B7 | the surfaced entry is a RECORD, address-complete and game-grade, on the applied path |
| B8 | the pick is deterministic on its key and the pool genuinely spreads |

---

## §4 · THE ZERO-HEADROOM CLASS — UNTOUCHED, AND MEASURED BOTH WAYS

| file | before | after | proof |
|---|---:|---:|---|
| `warTermination.js` | 818 | 818 | EXACT, and `git diff` reports zero changed bytes |
| `pulseKernel.js` | 1581 | 1581 | EXACT, and `git diff` reports zero changed bytes |
| `applyWorldPulse.js` | 941 | 941 | EXACT, and `git diff` reports zero changed bytes |
| `peaceTerms.js` | 797 | 797 | three under the layer ceiling; zero changed bytes |

The kernel-wiring accounting is ZERO: the fold's outcomes already flow through the landed generic
curation seam, so the beat reaches the feed with none of the four touched. No PRNG draw was
added, moved or removed anywhere.

---

## §5 · CENSUS

The lighting tuple was re-derived WHOLE at this base by placeholder-and-convict, five figures in
sequence, each read from its own assertion message. The TRUE BASE was itself re-measured with
every one of this member's test edits held aside, and it reproduced the compile's figures
exactly. The new test file is the NAMED interior red the train plan carries.

⚠ **THE SUITE-TITLE PREDICTION OF NET ZERO WAS WRONG, AND THE CORRECTION IS MEASURED.** The
compile predicted the new walker's one suite title against the removed C2 block's one. Measured
by isolation — the C2 block reverted with everything else in place — the block contributes ZERO
titles and ZERO suite titles, because its file is not credited by the census at all. The delta
is therefore the new walker's alone.

## §6 · CHECKS

The focused battery, the §31 anchor preflight run bare before member-green, both typecheck
configurations at their exact floors named with their configs, eslint over every changed path,
`validate:packets` at every status flip, and the one terminal bare gate.

## §7 · RAISED FOR THE CHAIR

- **R-5 · THE MINT RULE IS AMENDED BY TWO CONJUNCTS, on executed evidence.** As drafted, the
  member announced the same creed extinct twenty-five times in one sixteen-tick village, and
  twice more through the real pulse for a creed the town's own record kept reinstating. The
  receipt asserts the rite is kept by nobody; that must be true past the tick that says it. The
  fence is proven by an independent cross-check rather than by the count falling. Veto restores
  a metronome.
- **S-6 · The governed FAITH corpus already existed and nothing read it.** The pool is taken
  from the annex verbatim rather than authored, on the finite-semantics law.
- **S-7 · The §85.4a row costs one row PLUS one constant.** The defer ceiling moves by one in
  the same commit, on the landed IN-1c-a precedent; the packet's "no edit" verdict is refuted.
- **S-8 · The §85.4b lit-coverage row is NOT INCURRED.** Both new leaves are directly imported
  by the family walker and are therefore covered rather than owed as debt — measured, with the
  shrink-only baseline untouched.
- **R-6 · A SECOND TRANSCRIPTION OF THE KIND CENSUSES WAS FOUND AT THE REBASE, and it costs the
  override one file.** `tests/domain/pantheon.test.js`'s A5 re-derives the same six figures from
  its OWN hand-copied registry list, so a chair-authorized registration reds it. No packet named
  the file and the compile's register table listed only `kindPoolFloors.walker.test.js`; it
  surfaced because WF-1F touched `pantheon.test.js` and the rebase put it in this member's
  battery. Four figures are re-recorded as a DECLARED act, the FAITH registry joins the local
  list, and the title takes the rename door so census cardinality stands still. WF-1c's own claim
  — its kind routes by prefix and files no exact row — is untouched and still asserted.
  ⇒ **the handwritten count is 17 files + this packet = 18, one above the ruled 12 → 17.** The
  override's stated grounds cover it (a standing-law obligation, not a design act), but the
  NUMBER is the chair's, so it is raised rather than absorbed. ⛔ The structural repair — A5
  importing the one registry list instead of re-typing it — is RECORDED AS OWED and deliberately
  not taken inside a faith member.
- **R-7 · THREE MORE FULL-GATE-ONLY RATCHETS FIRED, and the pre-slot sweep is what found them.**
  All three were GREEN at the pristine base and RED with this member — measured in a base-proof
  worktree at `a09138d7`, 87/87 — so all three are this member's own.
  (a) `.prose-numerics-baseline.json`: SIX rows key `religiousContest.js` and the fold edit moved
  both addresses. The compile ruled this NOT INCURRED on a scan that found none; refuted. They are
  the same six rows WF-1a paid for. Re-addressed, never deleted, with `line` the only field that
  moved on exactly six of 413 rows.
  (b) `entropyRootCensus`: the `hash01` CALLER census 14 → 15, a declared arrival — the picker
  copies the CURED spelling rather than the uncured one nine sites still carry.
  (c) `observedShapeReaders`: the reader-with-no-writer ratchet caught the fence's first spelling
  — a NEW `primaryDeityRef` identity at ceiling zero plus two reads over their ceilings. ⭐ IT WAS
  CURED IN THE CODE, NOT IN THE BASELINE, which is the instrument's own first instruction: the
  fence now reads through `deitySnapshotFor` (the same embedded snapshot, resolved through the
  `snapshot.byId` that `settlement` itself came from) and through a HOISTED cult list, and the
  redundant denormalized `primaryDeityRef` read is simply gone. **No observed-shape baseline was
  touched at all.** That matters twice over: `--write` is refused by a pre-existing
  instrument-migration block that reproduces at the base with zero of this member's changes, and
  §P7.13 makes any detector change a STOP — so the only lawful cure was the code, and it was
  available. The three behavioural proofs were re-run after the swap and reproduce byte-for-byte.
- **THE FILE COUNT AGAINST THE RULED OVERRIDE**: 19 files + this packet = **20**, against the
  ruled 12 → 17. Every file beyond the ruling is a standing-law ratchet this member's authorized
  registration moved, none is a design act, and each is named above. The NUMBER is the chair's.
- M15 was dropped (S-8) and the chooser ceiling added (S-7), which is the one exchange that nets
  to zero.

## §8 · REQUIRED SYMBOLS

`advanceReligionStates` · `advanceShares` · `isSubsystemActive` · `SECTION_OF` /
`EXACT_SECTION` / `PREFIX_RULES` / `KIND_SECTION_CORRESPONDENCE` · `KIND_SECTION` ·
`WHAT_PHRASES` / `whatPhrase` · `newsEntryForOutcome` / `isDriftOnlyOutcome` ·
`newsVoiceCategory` · `hash01` · `stablePart` · `deityDisplayNameFromRef` ·
`HABIT_FORK_REGISTRY` · `INFORMATION_KIND_REGISTRY` · `FREQUENCY_FLOORS` / `floorFor`.
*Created at the flip to LANDED:* `FAITH_KIND_REGISTRY`, `FAITH_KINDS`, `FAITH_NEWS_TUNING`,
`faithLine`, `faithReceipt`, `FAITH_RECEIPTS`, `KIND_SECTION_MINTER_RULINGS`.
`retiredSymbols`: none.

## §9 · censusAuthorization

`{ movedInstruments: [the lighting tuple, the kindPoolFloors registry counts and small-family
list, the chooser defer ceiling], authorizingDecisions: ['ODQ §309.3', 'ODQ §321.2a',
'ODQ §347.1(2)', 'ODQ §350'], newTestFiles: ['tests/lint/faithKindPools.walker.test.js'] }`
