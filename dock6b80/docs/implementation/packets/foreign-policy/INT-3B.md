# Interior / INT-3B — the émigré mint seam (no flag, no ledger, no number)

- **Status:** LANDED
- **Train:** `refs/trains/int-3b`, **ONE member**. Plan: `laneTC12-TRAIN-PLAN.md`.
- **Landed:** `28d2824b488937e0b51fdd69920574aa2327f9fa`. Chain: promotion `8401efbc` →
  this implementation commit → the terminal. ⛔ **THE IMPLEMENTATION COMMIT STOPPED BEFORE IT
  LANDED, AND THE STOP WAS RATIFIED.** Mutant M2's declared SECOND conviction door — the
  one-reader walker arm — was measured to SURVIVE, so lane TE12 refused to paper over it,
  parked the green implementation at `refs/trains/int-3b-i1-stopped`, and reported.
  `OWNER_DECISION_QUEUE.md` §58 ratifies that stop as correct procedure, dockets the detector
  gap to the machinery queue (§58.3), and ORDERS THE RELAND AS-IS on the measured ground that
  the refutation touches the GUARD's reach and never this member's behaviour. The commit
  relanded byte-identical; only this packet's M2 claim was amended, in the terminal.
- **Chair authority:** ⭐ **RULED — `OWNER_DECISION_QUEUE.md` §54.** The one-member
  REFUSED-IN-PART shape is SIGNED; the five beyond-annex refutations BIND; the quoted-literal
  `purposeClass`, the exact-path INTERIOR regex and the `builtModules`+`minters` exact pin are
  all SIGNED; X5 is RESOLVED-NOT-PAID; and the OSR receipt gap is accepted as declared with
  **any movement at execution a STOP**. `J-TC12-1..7` are ratified on the report.
  ⚠ **`Q-TC12-1` (where an émigré record persists) STAYS OPEN AND BATCHED** with the other
  five questions — including `Q-TC12-2`, the formal family-preamble signature. This packet is
  executable without any of them: it authors zero numbers, mints zero flags, persists zero
  state, and is built so that leaving the record's home open costs nothing.
- **Volume:** INT — the INTERIOR family. Wave INT-3b (the émigré), **REFUSED IN PART** (§2.3).
- **Family preamble, cited BY SHA-256** (the §P-header citation law):
  `docs/implementation/preambles/INT-PREAMBLE.md`
  sha256 `cb86055e8db99c17c3f7da2aff5acede57f49fb15b33edb17727c54bf31b7809`
  ⭐ RE-COMPUTED at P1 by the executing lane over the **landed bytes at that path**, not over
  the scratchpad draft it was compiled from (whose stamp was `cb9923ff…`); the two differ
  because landing rewrote the draft's own status blockquote. A preamble edit re-stamps this
  line, which is the whole point of citing by digest rather than by name.
- **Branch:** `claude/composite-r4`
- **Verified base:** `claude/composite-r4` at `fc8451c44c21032009ac9dfba8c8589ac7eb6de0`
- **Base-state capsule:** `e93376ec`; the verified base is its **docs-only child**, so the capsule
  is citable as executed under its `consumptionLaw` / chair judgment **J-T1** — and every row
  this manifest touches is re-executed in §4 regardless.

---

## 1. Reconciled authority

| Source | What it supplies |
|---|---|
| `docs/DESIGN_FP_INTERIOR.md` §5 (INT-3b block, `:694-780`) | the DESIGN authority for the émigré: mechanism, host decision, endings, pins, bands |
| `docs/DESIGN_FP_ARCH_INT.md` §4 (INT-3 block) | the implementation compilation. ⛔ **363 commits stale**; its INT-3b paragraph ("BLOCKED as specced", "STOP-and-report is the SUCCESS mode here") is REFUTED whole |
| SPV annex `INT-SUBSTRATE.md` (lane INT-S) | the 284-claim re-measurement that supersedes the ARCH volume's §1. Rows consumed: **X1 X2 X5 · C63 C181 C182 C184 C201 C211 C244 C274 C275 C284** |
| `docs/implementation/preambles/INT-PREAMBLE.md` @ the sha above | §P1–§P9 in full: the layer-claim law, the coupling-leaf cost, the errand-consumer flip, the census law, the OSR law, the hazard dispositions, the gate law, the STOP conditions, the §38.4 delete-clause law |
| `OWNER_DECISION_QUEUE.md` §41–§45 | §42/§43 the value-derivation law · §44 `R35` path reservation · §45.2 the mandatory validator simulation |
| `OWNER_DECISION_QUEUE.md` §49/§50 | the flag-mint law's three obligations — **all three with EMPTY SUBJECTS here** (§6.2) |
| `docs/DESIGN_FP_COUPLINGS.md` §0.3 | the same-commit coupling obligation — **no subject** (§11) |

**Authority notice.** Only this packet, once READY, defines coding authority. Design files,
queues, progress notes, commit subjects and briefs cannot expand it.

---

## 2. Outcome and boundary

### 2.1 What lands

**One pure leaf that prices a defeated claimant's flight through the estate's ONE
purposeful-travel substrate, and the registry flip that makes the estate's own frozen claim
true.**

`src/domain/worldPulse/emigreErrand.js` exports `emigreErrandFor(...)`. It calls
`mintErrandSpine` — and nothing else — with a free-form purpose and the quoted literal
`'factional'` class the frozen `ERRAND_CONSUMERS` row already reserved for this wave, and it
returns the mint's own answer narrowed to `{ ok, lit, reason, plan }`. `ERRAND_CONSUMERS`'
`ambitious` row flips `built: false → true` in the same commit, because
`tests/lint/errandConsumerRegistry.walker.test.js` measures that claim **in both directions** and
would red either way round.

### 2.2 What it deliberately does NOT build — the non-goals, named

- ⛔ **No caller is wired.** Nothing in `src/` calls the export. That is the wave's identity claim
  rather than an omission (the landed HB-2 precedent, verbatim: *"`writeHabits` exists and
  nothing in `src/` calls it"*), and **A6** pins it.
- ⛔ **No flag.** No `interiorVetoEnabled`, no `emigreErrandEnabled`, no `simulationRules.js` edit,
  no certification row, no edge-shared bundle regeneration. Dormancy here is STRUCTURAL: with no
  caller, no seed, flag, preset or lifecycle path can reach the module.
- ⛔ **No number.** Not a share, band, cap, window, threshold or tuning key. **A7** pins it as a
  property of the module's executable source.
- ⛔ **No persisted state.** No ledger key, no `worldState` write, no sub-ledger, no DTO change.
- ⛔ **No vocabulary widened.** `ENVOY_PURPOSES`, `ENVOY_PURPOSE_CLASSES`,
  `ENGINE_GATED_VIRTUAL_RULE_KEYS`, `PURPOSE_CLASS_BY_PURPOSE`, every endings and news vocabulary
  — all untouched.
- ⛔ **No coupling row and no registry leaf.** `couplingRegistryInterior.js` is **not** minted:
  the module reads across no layer (§11).
- ⛔ **No UI.** Neither the host's guest mark nor the home town's away-mark.

### 2.3 ⛔ INT-3b AS CHARTERED IS REFUSED IN PART

The volume's INT-3b block charters six behaviours, five bands, three endings and two UI surfaces
under one wave's name — the GR-5 shape. Six successors are split out, each carrying the measured
reason it cannot compile at this base: **the record's home is owner-gated and the volume's own
answer ("NO NEW LEDGER — SP-1 errands") is refuted (R-INT3B-1); the departure trigger needs two
chair-signed values; the host decision needs INT-1, which has not landed, and reads a
`securityBand` word that does not exist; the harboring grievance needs the record; the endings
vocabulary is forbidden to lead its producer (J-INT-12) and its envelopes are INT-7's; the two
marks are projections over a record that does not exist.** Full table: plan §11.

---

## 3. ⭐⭐ THE REFUTED PREMISES THIS PACKET IS BUILT ON

### R-INT3B-0 (from the annex, X2) — INT-3b is UNBLOCKED, and its host was pre-ruled

The volume declares INT-3b blocked, recommends HOLD, and calls STOP-and-report *"the SUCCESS mode
here."* At the verified base **SP-1 has landed**: `src/domain/worldPulse/errandMint.js` is the
generalized mint head, `errandSpineEnabled` is manifest member `simulationRules.js:215` with
`tests/property/errandSpineDormancyFence.test.js` behind it, and the frozen `ERRAND_CONSUMERS`
map (`envoyErrandVocabulary.js:357-363`) already carries

```js
{ consumer: 'ambitious', purposeClass: 'factional',
  module: 'src/domain/worldPulse/emigreErrand.js', wave: 'INT-3b', built: false }
```

with `errandMint.js`'s own header naming *"INTERIOR's emigres."* The module path, the purpose
class and the mint seam are pre-ruled; the wave's only remaining question was whether the
substrate would admit it.

### R-INT3B-1 (STOP-CLASS, this compile) — the errand ROW DTO is war-welded, so *"NO NEW LEDGER — SP-1 errands"* is FALSE

`DESIGN_FP_INTERIOR.md:392` directs the émigré to persist as an SP-1 errand. It cannot.
`normalizeErrand` (`envoyErrandRecords.js:555-588`) returns `null` unless the row carries a valid
`offer` (a **peace offer**), a matching `acceptance`, a departure `snapshot`, an `id` equal to
`envoyErrandIdForOffer(offer)` or `envoyAttemptIdForOffer(offer)`, a `purpose` inside
`PURPOSE_SET` (= `ENVOY_PURPOSES` = `['sue','self_parlay']`), and a payload whose `offererId`/
`targetId` equal the row's `from`/`to`. `envoyErrand.js:184` refuses the mint on the same set.
An exile is not suing for peace, and the vocabulary may not be widened from the interior.

⇒ **The wave's record home is an open owner-gated question (`Q-TC12-1`), and this member is built
so that leaving it open costs nothing: it persists nothing at all.**

### R-INT3B-2 — and GR-2 already showed what "minting through the spine" actually means

`pactProposals.js:250-266` (`pactTransportFor`) calls `mintErrandSpine` with
`purpose: 'pact_proposal'` — **not an `ENVOY_PURPOSES` member** — and `purposeClass: 'diplomatic'`,
injects a one-leg plan, and reads back **only `minted.reason`** (`'spine'` vs `'dark'`) to choose
a transport word. Nothing enters `worldState.envoyErrands`. The head is a **pricer and class
validator**, not a row writer, and `purposeClassOf` accepts a WRITTEN class regardless of purpose
(`envoyErrandVocabulary.js:612-620`). This member is that precedent, one volume over.

### R-INT3B-3 — `emigreErrand.js` matches NO layer pattern

`tests/lint/couplingInclusion.walker.test.js`'s `LAYER_PATTERNS.INTERIOR` claims by name prefix
(`faction|legitimacy|relationship|institution|commons|disposition|generosity|grievance|
rulingPower|npcLadder|seatBooks`) plus an exact path for `strategicPosture.js`; GRAMMAR claims
`envoy…`, not `emigre…`. The registry-mandated filename therefore arrives UNLAYERED and reds three
arms at once (the new-unlayered-module census, the exact-set equality against
`.coupling-unlayered-baseline.json`, and `UNLAYERED_BASELINE_CEILING` = 179). **Path 4 is the
cure**, and it is a LAYER HOME rather than an argued entry because the walker's own table rules
the distinction by SUBJECT.

### R-INT3B-4 — the third walker test's exact-equality arms and its COUNT-BEARING TITLE

`errandConsumerRegistry.walker.test.js`'s third test pins `builtModules` **and** `minters` by
`toEqual` against three-element arrays, under the title *"the THREE built consumers today are the
war errand head, the pact proposals and the covert missions."* A fourth consumer reds both arrays
and falsifies the title. **The flip alone is not the price; path 3 is.**

### R-INT3B-5 (executed, and NARROWED at the reland) — a NAMED CONSTANT is an offender to the one-reader law's PATTERN, but only where no sibling brace nests

⛔ **NARROWED BY EXECUTION AT THE RELAND (OQ §58.1/§58.3).** The four rows below were driven as
FLAT snippets and every one of them still reproduces. What does NOT follow — and what the original
heading claimed — is that a named constant in THIS MODULE would red the law: `readsByPattern` scans
only INNERMOST brace groups (`/\{[^{}]*\}/g`), so the same offending element inside an object
literal that CONTAINS a nested brace is invisible to it. This leaf's mint call carries a nested
`routePlan`, so mutant M2 SURVIVED the walker (§10). The quoted literal is still the right and
signed spelling — it is now the ONLY thing keeping the module clean, which is a stronger reason
than the one this premise gave. The detector gap is docketed to the machinery queue; ZERO live
evaders were measured estate-wide, so it is latent rather than live.

`readsByPattern` matches any object-literal or binding element that IS one of
`purposeClass|declaredPurpose|truePurpose`, optionally renamed **to an identifier**. Driven at the
verified base:

```
TRUE   purposeClass: EMIGRE_CLASS          ← a named constant is an OFFENDER
FALSE  purposeClass: 'factional'           ← a quoted literal is clean
FALSE  purposeClass: 'diplomatic'          ← GR-2's landed spelling, clean
TRUE   const { purposeClass } = row;       ← the shorthand the law exists for
```

⇒ **the class rides the mint call as a quoted literal, and the module defines no constant for it** —
and A8's positive pin `toContain("purposeClass: 'factional'")` is what actually holds it there,
which is why that pin is the single door M2 is convicted through.

---

## 4. Verified tree contract and executed preflight receipts

### 4.1 Base state

Simroot built by `git archive HEAD | tar -x` (HEAD objects only; the working tree was never
read, because an executor may be live).

```
$ node scripts/implementation-packets.mjs validate ; echo TRUE_EXIT=$?
[implementation-packets] valid: 45 packets (0 READY)
TRUE_EXIT=0                          ← reproduces the capsule's "45 packets / 0 READY"

LIVE statuses: {"LANDED":44,"SUPERSEDED":1}
non-terminal rows (the ONLY rows that reserve change paths): (NONE)
⇒ all six of this packet's paths are FREE; R35 has no live subject
```

```
scripts/.observed-shape-readers-baseline.json  → 387 files, rows summing to exactly 1998
scripts/.test-ratchet-baseline.json            → 16 frozen entries; NONE names
                                                  errandConsumerRegistry / couplingInclusion /
                                                  sovereigntyLightingContract
scripts/.size-baseline.json                    → 10 baselined path entries; none is ours
scripts/.domain-strict-baseline.json           → {total: 1134}; the check is a per-file ceiling
                                                  ("New/worsened files must be strict-clean")
```

### 4.2 Hot files — measured with eslint's own `Linter`, `max-lines` `{skipBlankLines, skipComments}`

| File | raw | **effective** | ceiling | this packet |
|---|---:|---:|---:|---|
| `envoyErrandVocabulary.js` | 674 | **367** | 800 | **+0** (one word) |
| `errandMint.js` | 263 | **82** | 800 | untouched |
| `envoyErrand.js` | 900 | **714** | 800 | untouched ⚠ its header still claims 695 |
| `pactProposals.js` | 380 | **161** | 800 | untouched (precedent only) |

The capsule's four hot files — `OutputContainer.jsx` 599/600, `convergence.js` 798/800,
`peaceTerms.js` 797/800, `informationStatecraft.js` 780/800 — are **not in this manifest**.

### 4.3 Required live symbols (navigate by symbol, never by line)

| Path | Symbol | State |
|---|---|---|
| `src/domain/worldPulse/emigreErrand.js` | `export function emigreErrandFor(` | **CREATED by this wave** |
| `src/domain/worldPulse/errandMint.js` | `export function mintErrandSpine(` | preserved |
| `src/domain/worldPulse/envoyErrandVocabulary.js` | `export const ERRAND_CONSUMERS` | preserved |

⚠ Naming the created symbol is what produces this packet's single declared P1 red (§7). It is
kept deliberately: a packet naming only preserved symbols would exit 0 at P1 and prove nothing.

### 4.4 Live and post-change censuses

| Figure | Base | Predicted at T |
|---|---|---|
| `lightingCensus` | `2429/366/2063/20115/5654` | **`2430/366/2064/20123/5655`** |
| `runtimeTests` | 28138 | **28146** (FLOOR) |
| `validatePackets` | `45 / 0 READY` | **`46 / 0 READY`** (executed, §7) |
| `flagManifestRows` | 20 | **20** |
| `osrFindings` | 1998 | **1998** |
| `ARGUED_ROSTER_CEILING` / `UNLAYERED_BASELINE_CEILING` | 19 / 179 | **19 / 179** |
| `frozenKnownFailures` | 16 | **16** |
| `ERRAND_CONSUMERS` built / unbuilt | 3 / 4 | **4 / 3** |

Arithmetic: `366 + 2064 = 2430` ✓ · `20115 + 8 = 20123` ✓ · `5654 + 1 = 5655` ✓.

---

## 5. Exact behaviour and lifecycle contract

### 5.1 The function

```
emigreErrandFor({ worldState, fromId, toId, departTick, arrivalTick })
  → { ok: boolean, lit: boolean, reason: string, plan: object|null }
```

It calls `mintErrandSpine` exactly once, with: the caller's `worldState`; a free-form
`purpose` naming an exile's flight; `purposeClass` as the **quoted literal** `'factional'`;
`fromId`/`toId`; `journey: 'outbound'`; and an injected one-leg `routePlan` carrying the caller's
own `departTick`/`arrivalTick` unchanged. `lit` is `reason === 'spine'`.

**It does not compute the ticks.** Arithmetic on a leg clock is what
`namedPersonTransitTotality.walker.test.js`'s bypass detectors exist to catch, and a leaf that
priced its own journey would be claiming a speed law it does not own. The transit owner prices;
this leaf forwards.

### 5.2 Exact transitions

| World | Answer |
|---|---|
| `errandSpineEnabled === true`, plan priceable | `{ ok:true, lit:true, reason:'spine', plan }` — the class resolves to `factional` |
| flag absent or `false`, plan priceable | `{ ok:true, lit:false, reason:'dark', plan }` — the head ignores the class cargo entirely; **no class is written** |
| plan unpriceable, **either** world | `{ ok:false, lit:false, reason:'invalid_route_plan', plan:null }` — refused BEFORE the gate, identically |

### 5.3 Lifecycle paths (L4, written before the writer builds)

**There is no state, so every path is the same path.** Create / read / persist / regenerate /
undo / clone / migrate / import: the module holds nothing, writes nothing, and is reachable from
nothing. Serialized bytes and the rng stream are identical to a pre-wave build on every seed and
every lifecycle path — **not by a fence, but because no code path reaches the module at all**.
That is a stronger claim than dormancy and a weaker capability, and it is exactly what a seam
that is waiting for its trigger should be.

⚠ When `Q-TC12-1` is ruled and INT-3b-ii/iii land the trigger and the record, the lifecycle
triad the design volume names (REGEN closes `lost`; UNDO reverts both projections with the
record; IMPORT drops a dangling host reference to absent) becomes THEIR obligation and is
carried in their packets, not retro-fitted here.

---

## 6. Exact SIX-path implementation manifest

| # | Action | Path | Cost |
|---|---|---|---|
| **1** | **CREATE** | `src/domain/worldPulse/emigreErrand.js` | ≤ **90 effective** / 800. One import (`errandMint.js`). One exported function. Zero numeric literals. Zero `x.y` chains on an unresolvable receiver |
| **2** | **REGISTER** | `src/domain/worldPulse/envoyErrandVocabulary.js` | **ONE WORD** at `:362`: `built: false` → `built: true`. 367 → 367 effective |
| **3** | **TEST** | `tests/lint/errandConsumerRegistry.walker.test.js` | both `toEqual` arrays 3 → 4 entries (`builtModules` **and** `minters`, in sorted order — `emigreErrand.js` sorts first among the four), the count-bearing title renamed THREE → FOUR, and one comment paragraph recording the fourth. **Zero net titles** |
| **4** | **TEST** | `tests/lint/couplingInclusion.walker.test.js` | one **EXACT-PATH** regex appended to `LAYER_PATTERNS.INTERIOR` with its written reason. **Zero titles** |
| **5** | **CREATE** | `tests/domain/emigreErrand.test.js` | ONE `describe`, **EIGHT** literal titles, straight-line registration |
| **6** | **TEST** | `tests/lint/sovereigntyLightingContract.walker.test.js` | the census tuple re-derived **WHOLE** with its cause stated |

### 6.1 Why path 4 is a LAYER HOME and not an argued entry

The walker's table draws the line by SUBJECT, in its own words, three times: `strategicPosture.js`
took INTERIOR because it composes a court's memories; `secrecyTradeFactor` took INFO because
secrecy is INFORMATION's subject; `errandMint.js` took the argued roster because it *owns no
subject* and five ports speak it. A defeated claimant's flight is INTERIOR's own subject. And the
argued door is the expensive one: an argued module's edges are deleted from the pair scan, and
`ARGUED_ROSTER_CEILING` is an **exact equality in both directions** since 2026-08-10 —
*"a number that only has to be 'not exceeded' is not a ratchet, it is a budget."*

⛔ **EXACT PATH, never an `emigre[A-Z]` prefix.** The table's own `secondOrderBelief.js` note:
*"a prefix would claim files nobody has designed and silently widen a frozen family."*

### 6.2 ⭐⭐ THE FLAG-MINT LAW — THREE OBLIGATIONS, ALL WITH EMPTY SUBJECTS

OQ §49 ruling 3 as amended by §50 ruling 2 makes three costs standing law for every volume, and
this packet states affirmatively that it pays **none of them, because it mints no key**:

| Obligation | Subject here |
|---|---|
| (a) the `subsystemRowsVirtual.test.js` ordered-equality pin | **none** — no manifest row |
| (b) the SEVEN generated edge-shared bundle artefacts `simulationRules.js` feeds | **none** — `simulationRules.js` is not in the manifest |
| (c) the flag drive spelled as a LITERAL `<flag>: true` in the acceptance file | **none** — no flag to drive |

⚠ **A2/A3 still spell `errandSpineEnabled: true|false` as literals** — not for lit-coverage credit
(that flag is SP's and already covered), but because a computed key attributes to no key and this
estate has already paid for learning that once.

### 6.3 The mandatory implementation order

1. Path 4 (the layer claim) **and** path 1 (the module) — in that order, so the module never
   exists unclaimed even for one edit.
2. Path 2 (the flip) **immediately** — DIRECTION 2 reds between paths 1 and 2.
3. Path 3 (the walker's two arrays and its title) — the exact-equality arms red between 2 and 3.
4. Path 5 (the acceptance file).
5. Path 6 (the census, re-derived WHOLE) — **last**, and in this same commit, because it is the
   train's only `tests/`-moving commit and a patched figure measures nothing.
6. The §31 anchor preflight (§9), **then** green is declared.

⛔ All six paths are ONE commit (`I1`). Four instruments red each other's absence; there is no
intermediate state in which the tree is green.

---

## 7. The §45.2 validator status sequence — EXECUTED

| Commit | Status | packets / READY | exit | reds |
|---|---|---|---:|---|
| base `fc8451c4` | — | **45 / 0** | **0** | none |
| **P1** | READY | 46 / 1 attempted | **1** | **EXACTLY ONE**, verbatim below |
| **I1** | READY | **46 / 1** | **0** | none |
| **T** | LANDED | **46 / 0** | **0** | none |

```
=== P1  promote (docs-only)  ::  INT-3B @ READY ===  exit=1
   [implementation-packets] INT-3B.requiredSymbols[0].path does not exist: src/domain/worldPulse/emigreErrand.js
```

Negative control (LANDED with the CREATE targets absent) fires on this packet's own paths, so the
green `T` is a measurement: see plan §6.2.

⚠ **The error KIND differs from GR-5A's.** GR-5A's P1 reds were `.symbol is missing from` an
existing file; this wave CREATES its file, so the red is `.path does not exist`. ⛔ Any SECOND row,
or a row of another kind, is a STOP.

⚠ **Two authoring shapes the validator enforces**, both found by execution: `checks` rows are
argv **string arrays**, and the packet Markdown must carry `**Status:**`, `**Branch:**` and
`**Verified base:**` lines or every state reds `status disagrees with packet Markdown`.

---

## 8. Acceptance matrix — exactly EIGHT titles, ONE file, ONE `describe`

`tests/domain/emigreErrand.test.js`

| # | Case |
|---|---|
| **A1** | **LIT.** `errandSpineEnabled: true` (a LITERAL) + a priceable pair ⇒ `reason:'spine'`, `ok:true`, and the returned plan's single outbound leg carries the caller's own depart/arrival ticks unchanged. The main-behaviour control every fence below is anchored against |
| **A2** | **FENCE 1 — dormancy.** Flag absent ⇒ `reason:'dark'`, `ok:true`, empty field block, **no class written anywhere**. Asserted only AFTER A1 has shown the lit path writes |
| **A3** | **FENCE 2 — absent vs false.** `errandSpineEnabled: false` (a LITERAL) and the key absent produce byte-identical answers |
| **A4** | **The road is not the flag's to change.** An unpriceable plan ⇒ `reason:'invalid_route_plan'`, identically lit and dark, refused before the gate |
| **A5** | **THE REGISTRY IS TRUE.** The `ERRAND_CONSUMERS` row whose `consumer` is `'ambitious'` reads `built:true`, `purposeClass:'factional'`, and a `module` that resolves to an existing file |
| **A6** | **THE IDENTITY CLAIM — NO CALLER IS WIRED.** A source walk of all of `src/` finds zero call sites of the export outside its own module. ⚠ The loop runs INSIDE one named test (the SP-D registry-walker idiom), never as a test generator |
| **A7** | **ZERO AUTHORED NUMBERS.** The module's executable source (comments stripped) contains no numeric literal — this member spends nothing the chair has not signed |
| **A8** | **THE ONE-READER LAW FROM THE INSIDE.** The module's executable source never spells `purposeClass`/`declaredPurpose`/`truePurpose` as a member read, a computed read, or a binding-pattern element; the class rides the call as a quoted literal (R-INT3B-5) |

⛔ Literal titles, straight-line, one `describe`. No `.each`, no `runIf`, no conditional
registration — a `.each` case is invisible to the lighting census and a `runIf` parks the file
whole, either of which closes the census arithmetic while measuring nothing.

---

## 9. §31 ANCHOR PREFLIGHT — MANDATORY, BEFORE GREEN IS DECLARED (ceiling ZERO on the new file)

`tests/domain/emigreErrand.test.js` is NEW, and the frozen worklist in
`negativeAssertionAnchor.walker.test.js` is **shrink-only and exact** — a new row for a new file
is precisely the edit the ratchet forbids. The three scanned matchers are `not.toContain`,
`not.toMatch`, `not.toHaveProperty`. Each occurrence in A2/A6/A7/A8 must either route through
`expectPresentThenAbsent` / `expectAbsentWithAnchor` **on the same line**, or carry
`// anchored: <why this cannot go vacuous>` on the assertion line or the one immediately above it.
⚠ On a split assertion the annotation sits above the `.not.` line, never above the `expect(`.

```
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js  ; echo TRUE_EXIT=$?
```

**§38.4 — NO SUBJECT.** This packet deletes, retires and replaces nothing. The INT volume's two
delete instructions belong to INT-6 and INT-8; both censuses are pre-executed in SPV annex §2 and
neither is discharged here.

**The tuning gate — NO SUBJECT.** Zero numbers authored (A7). The soak manifest stays at nine
ratified bands; `SOAK_COUPLINGS` names no interior émigré family. The five values the volume's
INT-3b block names are §42/§43 chair acts and every one is deferred to a named successor.

---

## 10. Mutation proof — six disposable mutants, isolated immutable candidate

Every mutant is planted, executed, reverted, and never committed.

| # | Mutant | Must red |
|---|---|---|
| **M1** | drop `purposeClass` from the mint call | **A1** (the class no longer resolves — `purposeClassOf` finds no mapping row for the free-form purpose and the mint refuses `invalid_purpose_class`) |
| **M2** | spell the class as a module constant instead of a literal | **A8**, by its POSITIVE LITERAL PIN (`toContain("purposeClass: 'factional'")`) — a SINGLE-DOOR conviction. ⛔ **AMENDED AT THE RELAND (OQ §58.2).** This row originally also claimed `errandConsumerRegistry.walker.test.js`'s one-reader arm, "driven separately", on R-INT3B-5. **That claim is STRUCK: executed, the walker arm SURVIVED M2** — `readsByPattern` scans only INNERMOST brace groups, so an offending element inside an object literal that CONTAINS a nested brace (this module's `routePlan`) is invisible to it. The flat spelling R-INT3B-5's table drove IS caught; this module's real shape is not. The detector gap is DOCKETED to the machinery queue (§58.3, ZERO live evaders measured — latent, not live) and was refused in-wave as a seventh path |
| **M3** | flip `built` back to `false` while the module exists | `errandConsumerRegistry.walker.test.js` DIRECTION 2, by name |
| **M4** | delete the exact-path regex from `LAYER_PATTERNS.INTERIOR` | `couplingInclusion.walker.test.js`'s new-unlayered-module arm **and** its exact-set equality |
| **M5** | return `lit: minted.ok` instead of `minted.reason === 'spine'` | **A2** — a dark world answers `ok:true`, so the substitution reports a lit spine on a dark world |
| **M6** | add a caller in `src/` | **A6** |

**Immutable candidate (declared, un-mutated):** the `journey: 'outbound'` literal. Mutating it to
`'return'` reds nothing this wave owns — the head's `normalizeRoutePlan` would refuse the mismatched
plan and A1/A4 would both still red, so the mutant proves the head's guard rather than this leaf's.
Named here so its absence is a decision rather than a gap.

⚠ **M1 and M2 must be driven SEPARATELY through the composed predicate.** `readsAField` is a
three-door disjunction, and this estate has measured a deleted door hiding behind a surviving one.
⭐ **AND DRIVING THEM SEPARATELY IS EXACTLY WHAT CAUGHT THE OVERCLAIM.** Had M2 been driven only
against the acceptance file it would have read as a clean two-door kill; driven against the walker
as well, its second door was measured to SURVIVE, which is how the `readsByPattern` nesting gap was
found at all. The method held even though the claim it tested did not — and the estate's own rule
about a deleted door hiding behind a surviving one is the same shape one rung up: an ASSERTED door
that never fires hides behind a real one just as well.

---

## 11. Wave-specific hazards, coupling, and OSR

**Coupling — ZERO rows, and that is measured rather than claimed.** After path 4 the module is
INTERIOR-layered, so `scanCrossLayerPairs` iterates it. Its only import is `errandMint.js`, which
carries `kind: 'substrate'` in `ARGUED_UNLAYERED` and therefore has **no layer**; the scan skips
any dependency with no layer. ⇒ zero pairs, zero baseline entries, **no `couplingRegistryInterior.js`,
and no subject for the validator's coupling-registration template check.**
⛔ **A second import is a STOP**, not a budget: any layered dependency mints the volume leaf, the
head import, the head re-export, the exact-list pin and a baseline entry, and changes the wave's
shape entirely.

**OSR.** Per-identity ceiling **ZERO** on the new file; any movement off **1998** is a STOP.
Executed cohort read: `errandMint.js`, `envoyErrandVocabulary.js`, `envoyErrand.js` and
`pactProposals.js` carry **zero rows each** (only `envoyErrandOffer.js` carries three in the whole
family), because the detector grounds a finding by a receiver that must resolve to exactly one
shape and these modules forward named parameters. Build the leaf the same way.

**Transit totality — no manifest row.** `DISCOVERY_RE` matches `normalizeRoutePlan(` and eight
named physics symbols. The leaf spells none of them: it injects a plan into the head, exactly as
`pactProposals.js` does and is likewise not a `MOVEMENT_SITES` member. ⛔ Spelling
`normalizeRoutePlan(`, `legArrivalTick:`, or any local `Math.max(1, hopWeeks(...))` would discover
the file and demand a row **and** subject it to the bypass detectors.

**`spTermLiteral` — no membership.** `SP_MODULES` is a closed six-file list of SP-owned modules;
an INT leaf does not join it.

**Mutation manifest — no row.** `tests/domain/emigreErrand.test.js` is outside the seven enforcer
dirs and its basename carries none of the sixteen invariant tokens. ⛔ **Do not rename it** to
`…Contract`, `…Pins`, `…Coverage` or `…Baseline`: each silently enumerates the file and buys a
manifest row whose target battery does not exist.

**Domain strict.** `check-domain-strict.mjs` is a per-file ceiling — *"New/worsened files must be
strict-clean."* The new leaf carries full JSDoc types and adds **zero** errors; total stays 1134.

---

## 12. Completion receipt and STOP conditions

### Checks (argv string arrays, as the validator requires)

```
npx vitest run tests/domain/emigreErrand.test.js
npx vitest run tests/lint/errandConsumerRegistry.walker.test.js
npx vitest run tests/lint/couplingInclusion.walker.test.js
npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js
npx vitest run tests/lint/namedPersonTransitTotality.walker.test.js
node scripts/check-observed-shape-readers.mjs
node scripts/implementation-packets.mjs validate
npm run typecheck:domain:strict
```

…then `check:tail` **bare, from a fresh shell**, with `; echo TRUE_EXIT=$?`.
⛔ Never wrap `npm run check*` in `gate-mutex.sh --run` — `test:ratchet` re-acquires and
self-deadlocks, and **exit 3 is the mutex giving up, not a red.**

### STOP conditions

1. ⛔ A **second** red at P1, or a red of a kind other than
   `requiredSymbols[0].path does not exist`.
2. ⛔ `osrFindings` ≠ **1998**; either typecheck ratchet off `173/173` / `1134/1134`;
   `kindPoolFloorsRegistries` ≠ 9.
3. ⛔ `ARGUED_ROSTER_CEILING` ≠ 19 or `UNLAYERED_BASELINE_CEILING` ≠ 179 — either means the layer
   claim was written as an argued entry or was missed.
4. ⛔ A **second import** in `emigreErrand.js`.
5. ⛔ `parked` ≠ 366 — a title was registered by a loop or a conditional.
6. ⛔ Any temptation to persist a record, mint a flag, author a number, widen a vocabulary, or add
   a seventh path. Each is a successor's, and each has a named owner in plan §11.
7. ⛔ Any temptation to widen `ENVOY_PURPOSES` or to touch `envoyErrandRecords.js`'s
   `normalizeErrand`. That is the cross-program write the estate forbids, and R-INT3B-1 is the
   reason it looks tempting.
