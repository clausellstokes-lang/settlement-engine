### 934  src/data/tradeGoodsData.js  ::  INSTITUTION_SERVICES
  paths: INSTITUTION_SERVICES.Household elder.Dispute mediation.desc | INSTITUTION_SERVICES.Household elder.Communal decisions.desc | INSTITUTION_SERVICES.Household elder.Record of custom.desc
  ex: "Settles quarrels between households by custom"
  ex: "Calls and chairs the gathering of families"
  ex: "Keeps the memory of boundaries, debts, and old agreements"
  consumers(24):
    src/components/ServicesTogglePanel.jsx:2|import {INSTITUTION_SERVICES} from '../data/tradeGoodsData';
    src/data/institutionServices.js:8|export const INSTITUTION_SERVICES = {
    src/data/servicesData.js:5|// Maps institution name variants → canonical INSTITUTION_SERVICES key.
    src/domain/display/institutionProfile.js:9|*   - services  → the institution's INSTITUTION_SERVICES catalog entry (count)
    src/domain/goods.schema.js:145|*     vocabulary. Both reach `tradeGoodsData.js` only for `INSTITUTION_SERVICES`,
    src/generators/services/institutionServices.js:2|import { INSTITUTION_SERVICES } from '../../data/tradeGoodsData.js';
    src/lib/customRegistry.js:26|import { INSTITUTION_SERVICES } from '../data/institutionServices.js';
    src/store/toggleSlice.js:48|// INSTITUTION_SERVICES). Toggles a user persisted under the OLD form are now
    tests/data/informationBrokerageCatalog.test.js:31|import { INSTITUTION_SERVICES } from '../../src/data/institutionServices.js';
    tests/data/underwaysInstitutionParity.test.js:21|import { INSTITUTION_SERVICES } from '../../src/data/institutionServices.js';

### 274  src/store/selectors.js  ::  selectCurrentCatalog()
  paths: selectCurrentCatalog().Government.Informal elder consensus.desc | selectCurrentCatalog().Government.Head-of-household consensus.desc | selectCurrentCatalog().Government.Lord's reeve.desc
  ex: "A free hamlet with no lord's representative. Communal elder consensus governs shared affairs."
  ex: "All family heads hold equal voice on shared concerns. Slower but more egalitarian."
  ex: "A steward appointed by a distant lord to oversee the settlement. Authority is formal but often resented."
  consumers(6):
    src/components/InstitutionalGrid.jsx:7|import { selectTierForGrid, selectCurrentCatalog, selectTierInstitutionNames, selectIsManualTier } from '../store/selectors.js';
    src/components/ServicesTogglePanel.jsx:8|import { selectTierForGrid, selectCurrentCatalog } from '../store/selectors.js';
    src/generators/lookups.js:83|* `filterCatalogForMagic`, which runs immediately after this in `selectCurrentCatalog`
    tests/ui/servicesTogglePanelKey.test.jsx:51|selectCurrentCatalog: () => MOCK_CATALOG,
    docs/implementation/PACKET_MANIFEST.json:28879|"_note": "THE READER FIX (J-CH-3-1, chair-ruled). Ten catalog rows are AUTHORED in one tier's block and GATED to a higher tier by `minTier` - nine city r
    docs/implementation/packets/catalog-hygiene/MF-CH3.md:123|`selectCurrentCatalog`.

### 199  src/domain/compendium/searchIndex.js  ::  COMPENDIUM_INDEX
  paths: COMPENDIUM_INDEX[0].keywords | COMPENDIUM_INDEX[1].keywords | COMPENDIUM_INDEX[2].keywords
  ex: "smallest single institution subsistence 8-60"
  ex: "local subsistence minimal trade 61-400"
  ex: "surplus weekly market guilds begin 401-900"
  consumers(9):
    src/components/CompendiumPanel.jsx:22|import { COMPENDIUM_INDEX } from '../domain/compendium/searchIndex.js';
    src/lib/seoCompendium.js:32|* domain/compendium/searchIndex.js COMPENDIUM_INDEX) always yields the same
    tests/build/prerenderRoutes.test.js:27|import { COMPENDIUM_INDEX } from '../../src/domain/compendium/searchIndex.js';
    tests/build/sitemap.test.js:18|import { COMPENDIUM_INDEX } from '../../src/domain/compendium/searchIndex.js';
    tests/domain/compendiumSearch.test.js:21|COMPENDIUM_INDEX,
    tests/domain/compendiumSearchDrift.test.js:13|import { COMPENDIUM_INDEX, searchCompendium } from '../../src/domain/compendium/searchIndex.js';
    tests/lib/seoCompendium.test.js:10|import { COMPENDIUM_INDEX } from '../../src/domain/compendium/searchIndex.js';
    scripts/generate-sitemap.mjs:34|import { COMPENDIUM_INDEX } from '../src/domain/compendium/searchIndex.js';
    scripts/prerender-routes.mjs:54|import { COMPENDIUM_INDEX } from '../src/domain/compendium/searchIndex.js';

### 198  src/domain/cultureProfiles.js  ::  CULTURE_PROFILES
  paths: CULTURE_PROFILES.germanic.scope | CULTURE_PROFILES.germanic.builtForm[0] | CULTURE_PROFILES.germanic.builtForm[1]
  ex: "A timber-and-stone, guild-and-estate design grammar."
  ex: "Timber-framed street houses cluster around a market green, with stone reserved for halls, bridges, and defenses."
  ex: "Farmsteads and craft yards form compact wards around a hall, church, or fortified manor."
  consumers(11):
    src/components/ConfigurationPanel.jsx:5|CULTURE_PROFILES,
    src/components/new/dailyLifeLogic.js:5|CULTURE_PROFILES,
    src/data/cultureProfiles.js:50|export const CULTURE_PROFILES = Object.freeze({
    src/generators/narrativeGenerator.js:44|CULTURE_PROFILES,
    tests/components/culturalTraditionPresentation.test.js:12|CULTURE_PROFILES,
    tests/data/categoryGovernance.test.js:30|import { CULTURE_PROFILES } from '../../src/data/cultureProfiles.js';
    tests/domain/cultureProfiles.test.js:12|CULTURE_PROFILES,
    tests/generators/narrativeArrival.test.js:26|import { CULTURE_PROFILES } from '../../src/data/cultureProfiles.js';
    docs/SITE_COHERENCE_AUDIT.md:33|**Corpus** 462 pipeline settlements, 0 generation errors = (6 settTypes × 7 `terrainOverride` × 5 seeds @ `tradeRouteAccess:'road'`) + (6 × 7 × 6 route values × 1 seed)
    docs/SITE_COHERENCE_PLAN.md:64|**Corpus spec** (fixed for the whole program so every wave's numbers are comparable): **462 settlements** = (6 settTypes × 7 `terrainOverride` × 5 seeds @ `tradeRouteAcc

### 189  src/data/stressInstitutionEffects.js  ::  STRESS_INSTITUTION_EFFECTS
  paths: STRESS_INSTITUTION_EFFECTS.under_siege[0].secret | STRESS_INSTITUTION_EFFECTS.under_siege[0].stakes | STRESS_INSTITUTION_EFFECTS.under_siege[1].secret
  ex: "Secretly negotiating surrender terms with the besieging force, without the council's knowledge"
  ex: "Treason. The garrison would execute them on the spot if they found out"
  ex: "Has been rationing their own private food stores while publicly enforcing the communal rationing order"
  consumers(7):
    src/generators/npcGenerator.js:31|import { STRESS_INSTITUTION_EFFECTS } from '../data/stressInstitutionEffects.js';
    tests/build/vendorPdfLazy.test.js:344|// integrated together. FP-G6 found the ~24KB NPC-secret STRESS_INSTITUTION_EFFECTS table
    tests/data/stressTypeRegistration.test.js:24|import { STRESS_INSTITUTION_EFFECTS } from '../../src/data/stressInstitutionEffects.js';
    tests/generators/stressTypeContentWave.test.js:13|import { STRESS_INSTITUTION_EFFECTS } from '../../src/data/stressInstitutionEffects.js';
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:1048|- Evidence: STRESS_DESCS keys end at monster_pressure (lines 192-293; no insurgency/mass_migration/wartime/religious_conversion/slave_revolt), so generateA
    docs/GOLDEN_SHIFT_LEDGER.md:626|legitimacy_crisis/demographic_pressure/trade_dispute; dt3: 5–8 STRESS_INSTITUTION_EFFECTS secret/
    docs/PHASE55_EXECUTION_PLAYBOOK.md:115|| **W-UPSWING MERGES + RATCHET #8 — recovery becomes an event; first paint breaks 1.13 MB** | 45eb5387..7ed60013 (7 commits on claude/w-upswing), 9aa95107 (FP-G6

### 151  src/domain/worldPulse/commercialReasonsNews.js  ::  COMMERCIAL_KIND_REGISTRY
  paths: COMMERCIAL_KIND_REGISTRY[0].pool[0]() | COMMERCIAL_KIND_REGISTRY[0].pool[1]() | COMMERCIAL_KIND_REGISTRY[0].pool[2]()
  ex: "{counterpart} took the wagons and sent nothing back; the compact is broken."
  ex: "The wharf at {settlement} waited for a cargo that never came, and stopped waiting in the autumn."
  ex: "The factors' book at {settlement} carries {counterpart} in the column of debts unanswered."
  consumers(2):
    tests/helpers/kindRegistryRoster.js:35|import { COMMERCIAL_KIND_REGISTRY } from '../../src/domain/worldPulse/commercialReasonsNews.js';
    tests/lint/commercialKindPools.walker.test.js:42|COMMERCIAL_KIND_REGISTRY,

### 86  src/domain/worldPulse/grammarNews.js  ::  GRAMMAR_KIND_REGISTRY
  paths: GRAMMAR_KIND_REGISTRY[0].pool[0]() | GRAMMAR_KIND_REGISTRY[0].pool[1]() | GRAMMAR_KIND_REGISTRY[0].pool[2]()
  ex: "The peace of {settlement} and {counterpart} has run its course: {band} years, and no hand that signed it still holds a s"
  ex: "The {term} {settlement} and {counterpart} kept between them came to its last day and was filed as closed; neither court "
  ex: "In {settlement} the market kept its hours as always; the pact with {counterpart} ended that week, and the carters heard "
  consumers(19):
    tests/domain/treatySuccessionDossier.test.js:38|import { GRAMMAR_KIND_REGISTRY } from '../../src/domain/worldPulse/grammarNews.js';
    tests/domain/treatySuccessionOpeningVoice.test.js:31|import { GRAMMAR_KIND_REGISTRY } from '../../src/domain/worldPulse/grammarNews.js';
    tests/domain/treatySuccessionReaffirmedVoice.test.js:27|import { GRAMMAR_KIND_REGISTRY } from '../../src/domain/worldPulse/grammarNews.js';
    tests/helpers/kindRegistryRoster.js:36|import { GRAMMAR_KIND_REGISTRY } from '../../src/domain/worldPulse/grammarNews.js';
    tests/lint/grammarLifecycleKindPools.walker.test.js:35|GRAMMAR_KIND_REGISTRY, GRAMMAR_SLOT_ROLES, grammarReceipt, grammarSlotRoles,
    tests/lint/sovereigntyLightingContract.walker.test.js:4423|// updates move NO registration — the three `test.each(GRAMMAR_KIND_REGISTRY)` sites
    tests/scripts/baseStateCapsule.test.js:151|grammarKindRegistryRows: news.GRAMMAR_KIND_REGISTRY.length,
    scripts/base-state-capsule.mjs:92|grammarKindRegistryRows: figure('MEASURED', 'src/domain/worldPulse/grammarNews.js', 'GRAMMAR_KIND_REGISTRY.length'),
    docs/content/RECEIPT_POOLS_INFORMATION.md:187|**`treaty_disclosure_opened` → pool lives in `RECEIPT_POOLS_GRAMMAR.md` under `# GR-0 — THE LIFECYCLE VOICE`.** Relocated by IN-0C under chair ruling CR-I
    docs/implementation/PACKET_MANIFEST.json:2996|"symbol": "GRAMMAR_KIND_REGISTRY"

### 68  src/domain/worldPulse/habitForkRegistry.js  ::  HABIT_FORK_REGISTRY
  paths: HABIT_FORK_REGISTRY[0].closeOwed | HABIT_FORK_REGISTRY[0].reason | HABIT_FORK_REGISTRY[1].closeOwed
  ex: "identify what a migration draw is graded against — arrival versus attrition on the chosen destination — before this row "
  ex: "a keyed race inside the migration advance; the acting settlement and the action it is choosing between have not been rea"
  ex: "identify the graded outcome of a demographic plan, and whether the fork is a choice at all"
  consumers(5):
    tests/domain/habitForkRegistry.test.js:31|HABIT_FORK_REGISTRY,
    tests/lint/chooserTotality.walker.test.js:53|HABIT_FORK_REGISTRY,
    docs/implementation/PACKET_MANIFEST.json:6349|"symbol": "export const HABIT_FORK_REGISTRY"
    docs/implementation/packets/foreign-policy/IN-1C-A.md:767|`HABIT_FORK_REGISTRY` classification for the seeded pick (`informationNews.js#informationReceipt`
    docs/implementation/packets/fp/WF-8A.md:233|`HABIT_FORK_REGISTRY` · `INFORMATION_KIND_REGISTRY` · `FREQUENCY_FLOORS` / `floorFor`.

### 67  src/data/economicData.js  ::  TRADE_DEPENDENCY_NEEDS
  paths: TRADE_DEPENDENCY_NEEDS.Mills (2-5).detail | TRADE_DEPENDENCY_NEEDS.Town granary.detail | TRADE_DEPENDENCY_NEEDS.City granaries.detail
  ex: "grain and mill sites for water-powered milling"
  ex: "grain reserves for storage"
  ex: "grain reserves at scale"
  consumers(10):
    src/data/finishedGoodsCategory.js:7|// larger TRADE_DEPENDENCY_NEEDS raw-material table (~21 kB of the source). Moving
    src/data/goods/chains.js:24|* would stop dragging the 21 kB TRADE_DEPENDENCY_NEEDS table along with it.
    src/domain/goods.schema.js:31|*     • src/data/economicData.js          TRADE_DEPENDENCY_NEEDS
    src/generators/economy/economicState.js:9|import { TRADE_DEPENDENCY_NEEDS } from '../../data/goods/chains.js';
    src/generators/economy/finishedGoodsDemand.js:16|// Builds on top of TRADE_DEPENDENCY_NEEDS (raw resources) without replacing it.
    tests/build/vendorPdfLazy.test.js:359|// economicData.js dragging the 21KB TRADE_DEPENDENCY_NEEDS raw-material table eager to
    tests/joins/goods.test.js:9|import { TRADE_DEPENDENCY_NEEDS } from '../../src/data/economicData.js';
    docs/COHESION_REMEDIATION_PLAN.md:102|- every `TRADE_DEPENDENCY_NEEDS` / goods `requiredInstitution` key matches a catalog institution (case-exact)
    docs/GENERATION_COHERENCE_AUDIT.md:145|1. ORPHAN (consumer, no producer): 'Major port' — gates Port Duties income (economicGenerator.js:1646), TRADE_DEPENDENCY_NEEDS, subsumption rule 29, spatial entr
    docs/PHASE55_EXECUTION_PLAYBOOK.md:105|| THE DOCTRINE BLOCK — SUPPLY-WEB + INFORMATION STATECRAFT + THE CORRUPTION WEB (3 of 4 waves) + FP-G4 −12,730B + RATCHET #6 | 12554bd3 (D1), eb1537dc/642c28d8 (

### 64  src/lib/instantWorld/factionDedup.js  ::  candidateNames()
  paths: candidateNames()[4] | candidateNames()[5] | candidateNames()[6]
  ex: "The Greater Independent Bloc"
  ex: "The Greater Free Alliance"
  ex: "The Greater Common Interest"
  consumers(1):
    tests/lib/instantWorld/factionDedup.test.js:13|dedupeWorldFactionNames, candidateNames, hasBannedStack,

### 52  src/data/goods/chains.js  ::  SUPPLY_CHAIN_NEEDS
  paths: SUPPLY_CHAIN_NEEDS.food_security.desc | SUPPLY_CHAIN_NEEDS.food_security.chains[2].entrepotNote | SUPPLY_CHAIN_NEEDS.food_security.chains[7].resourceSubstitutes[1]
  ex: "What the settlement eats, how it stores food, and what it can export"
  ex: "Salt is among the most-traded commodities. Port and crossroads settlements profit from redistribution."
  ex: "Date Palms and Orchards"
  consumers(23):
    src/components/new/SupplyChainsPanel.jsx:5|import { SUPPLY_CHAIN_NEEDS } from '../../data/goods/chains.js';
    src/data/supplyChainData.js:10|export const SUPPLY_CHAIN_NEEDS = {
    src/data/supplyChainResourceIndex.js:6|// first-paint code. supplyChainData.js keeps SUPPLY_CHAIN_NEEDS, which is reached
    src/domain/goods.schema.js:36|*     • src/data/supplyChainData.js       SUPPLY_CHAIN_NEEDS
    src/domain/supplyChainState.js:239|// Keys mirror the SUPPLY_CHAIN_NEEDS groups in data/supplyChainData.js
    src/domain/worldPulse/institutionLifecycle.js:34|import { SUPPLY_CHAIN_NEEDS } from '../../data/goods/chains.js';
    src/domain/worldPulse/tierResourceDynamics.js:4|import { SUPPLY_CHAIN_NEEDS } from '../../data/goods/chains.js';
    src/generators/cascadeGenerator.js:8|import { SUPPLY_CHAIN_NEEDS } from '../data/goods/chains.js';
    src/generators/computeActiveChains.js:6|import {SUPPLY_CHAIN_NEEDS} from '../data/goods/chains.js';
    src/generators/economy/viability.js:7|import { SUPPLY_CHAIN_NEEDS } from '../../data/goods/chains.js';

### 51  src/data/goods/chains.js  ::  GOODS_MODIFIERS_BY_TIER
  paths: GOODS_MODIFIERS_BY_TIER.thorp.Eggs.desc | GOODS_MODIFIERS_BY_TIER.thorp.Small game.desc | GOODS_MODIFIERS_BY_TIER.hamlet.Grain surplus.desc
  ex: "Fresh eggs from household chickens"
  ex: "Rabbits, fowl from local hunting"
  ex: "Wheat, barley, oats beyond subsistence needs"
  consumers(20):
    src/components/TradeDynamicsPanel.jsx:6|import {GOODS_MODIFIERS_BY_TIER} from '../data/tradeGoodsData';
    src/components/settlement/EventComposer.jsx:30|import { GOODS_MODIFIERS_BY_TIER } from '../../data/goods/chains.js';
    src/data/tradeGoodsData.js:140|export const GOODS_MODIFIERS_BY_TIER = {
    src/domain/display/institutionProfile.js:26|import { GOODS_MODIFIERS_BY_TIER } from '../../data/goods/chains.js';
    src/domain/goods.schema.js:33|*                                         GOODS_MODIFIERS_BY_TIER, COMMODITY_CATEGORY_MAP
    src/generators/economy/tradeGoods.js:7|import { COMMODITY_CATEGORY_MAP, GOODS_CATEGORIES, GOODS_MODIFIERS_BY_TIER } from '../../data/goods/chains.js';
    src/generators/institutionProbability.js:6|import {GOODS_MODIFIERS_BY_TIER} from '../data/goods/chains.js';
    src/generators/structuralValidator.js:10|import {GOODS_MODIFIERS_BY_TIER} from '../data/goods/chains.js';
    src/lib/customRegistry.js:28|import { GOODS_MODIFIERS_BY_TIER, IMPORT_GOODS_BY_TIER } from '../data/goods/chains.js';
    src/lib/dependencyEngine.js:580|* legacy bare name (prebuilt GOODS_MODIFIERS_BY_TIER form) or a refId from

### 47  src/domain/autonomy/index.js  ::  signalRegistryEntries()
  paths: signalRegistryEntries()[0].description | signalRegistryEntries()[1].description | signalRegistryEntries()[2].description
  ex: "The food security causal score (0–100)."
  ex: "The food security causal band (surplus/adequate/strained/critical/collapsed)."
  ex: "The labor capacity causal score (0–100)."
  consumers(10):
    src/components/surveyor/AutonomyPanel.jsx:31|signalRegistryEntries, signalById,
    src/domain/aiCharter.js:40|import { signalRegistryEntries } from './autonomy/signalRegistry.js';
    src/domain/aiOutputSchema.js:100|import { signalRegistryEntries } from './autonomy/signalRegistry.js';
    src/domain/autonomy/signalRegistry.js:178|export function signalRegistryEntries() {
    src/lib/surveyorWrite.js:277|signalRegistryEntries, NUDGE_TYPES, validateStopCondition, validateNudge,
    tests/components/surveyorAutonomyPanel.test.jsx:34|import { signalRegistryEntries, STANDING_INSTRUCTIONS_KEY } from '../../src/domain/autonomy/index.js';
    tests/domain/aiCharter.test.js:40|import { signalRegistryEntries } from '../../src/domain/autonomy/signalRegistry.js';
    tests/domain/aiOutputSchema.test.js:50|import { signalRegistryEntries } from '../../src/domain/autonomy/signalRegistry.js';
    tests/domain/autonomy/signalRegistry.walker.test.js:20|signalRegistryEntries, signalById, registerSignal, resetMintedSignalsForTest,
    tests/edgeFunctions/autonomyCore.test.js:30|import { signalRegistryEntries } from '../../src/domain/autonomy/signalRegistry.js';

### 41  src/data/goods/identity.js  ::  RESOURCE_DATA
  paths: RESOURCE_DATA.fishing_grounds.desc | RESOURCE_DATA.fishing_grounds.warning | RESOURCE_DATA.salt_flats.desc
  ex: "Deep-water or reef fishing close to shore; reliable fish and seafood supply"
  ex: "Fishing grounds require water access."
  ex: "Coastal evaporation pans or inland salt deposits; the settlement produces its own salt"
  consumers(43):
    src/components/settlement/EventComposer.jsx:31|import { RESOURCE_DATA } from '../../data/goods/identity.js';
    src/data/resourceChains.js:9|* paint. They were extracted out of resourceData.js (whose RESOURCE_DATA /
    src/data/resourceData.js:14|export const RESOURCE_DATA = {
    src/domain/certification/subsystemRowsWaves.js:78|check: 'For every satellites ledger record carrying a site, digest.territory[site.cell] resolves to the record parentId and digest.costField[site.cell
    src/domain/events/mutateWorld.js:17|import { RESOURCE_DATA } from '../../data/goods/identity.js';
    src/domain/goods.schema.js:28|*     • src/data/resourceData.js          RESOURCE_DATA, SPECIAL_RESOURCES
    src/domain/resourceSemantics.js:4|* RESOURCE_DATA describes presentation and economic outputs. It does not answer
    src/domain/resourceSites.js:43|import { RESOURCE_DATA } from '../data/goods/identity.js';
    src/domain/resourceTerrainCompatibility.js:7|* src/generators/terrainHelpers.js. It answers a pure question about RESOURCE_DATA
    src/domain/worldPulse/institutionLifecycle.js:37|import { RESOURCE_DATA } from '../../data/goods/identity.js';

### 41  src/domain/display/warAndRoadNames.js  ::  WAR_NAME_POOLS
  paths: WAR_NAME_POOLS.grievance[0] | WAR_NAME_POOLS.grievance[1] | WAR_NAME_POOLS.grievance[3]
  ex: "the War of the Old Grievance"
  ex: "the War of the Unanswered Wrong"
  ex: "the War of the Long Complaint"
  consumers(1):
    tests/domain/display/warAndRoadNames.test.js:4|WAR_NAME_POOLS,

### 35  src/domain/worldPulse/stressorDynamics.js  ::  VARIANT_HOOKS
  paths: VARIANT_HOOKS.foreign_sponsored[0] | VARIANT_HOOKS.foreign_sponsored[1] | VARIANT_HOOKS.foreign_sponsored[2]
  ex: "A courier carries coin that traces back across the border."
  ex: "Exposing the sponsor would be a casus belli, if anyone dares name them aloud."
  ex: "Someone local is living slightly too well for their station."
  consumers(2):
    src/domain/display/stateProse/stressorsStateProse.js:369|*   `VARIANT_HOOKS` — asserted in the desk test, which can import that roster because a
    tests/domain/stressorsStateProseDesk.test.js:37|import { VARIANT_HOOKS } from '../../src/domain/worldPulse/stressorDynamics.js';

### 31  src/lib/flags.js  ::  FLAGS
  paths: FLAGS.discordOauth.description | FLAGS.googleOauth.description | FLAGS.dossierFiveTabs.description
  ex: "Discord OAuth sign-in button. Safe flag-on: an unconfigured provider degrades to a calm message."
  ex: "Google OAuth sign-in button. Safe flag-on: an unconfigured provider degrades to a calm message."
  ex: "P102 / D-1: consolidate 14 dossier tabs into 5 thematic groups. PROMOTED default-on; flag retained as soak killswitch."
  consumers(28):
    src/components/dev/DevFlagPanel.jsx:21|import { FLAGS, flag, setFlagOverride } from '../../lib/flags.js';
    src/domain/certification/subsystemRowsBelief.js:204|// THREE ROWS, NOT ONE, BECAUSE THERE ARE THREE FLAGS (J-SP-3). Per-family flags are
    src/domain/certification/subsystemRowsVirtual.js:303|// ── WC-0E · THE WAR-CIRCULATION FLAGS (docs/DESIGN_FP_ARCH_WC.md §7.A.2) ──────
    src/domain/certification/tradeConvergenceContract.js:156|* THE FOREIGN PRECONDITION FLAGS — every non-TRADE key the lighting table reads,
    src/domain/certification/warConvergenceContract.js:307|* preconditions are FLAGS, and two of these three are LANDED WORK rather than a key
    src/domain/worldPulse/contributionLedger.js:33|* ⚠ BOTH FLAGS ARE VIRTUAL AND LIT IN NO PRESET, so no seeded world reaches this lane. The
    src/hooks/useAdvanceSession.js:11|* FLAG BEHAVIOUR: with FLAGS.advanceMultiTick OFF the single-tick path never returns
    src/lib/flagRegistry.js:30|// the human-facing FLAGS object (default + description) from these keys.
    src/pdf/sections/AIAppendix.jsx:92|RED FLAGS: WATCH FOR
    tests/domain/beliefAxisSubjectsFoldPin.test.js:68|* fence's lit control — build their rules with `Object.fromEntries(FLAGS.map(...))`, a

### 28  src/components/compendium/customContentEditorCopy.js  ::  CUSTOM_CONTENT_FIELD_HINTS
  paths: CUSTOM_CONTENT_FIELD_HINTS.category | CUSTOM_CONTENT_FIELD_HINTS.authority | CUSTOM_CONTENT_FIELD_HINTS.defenseRole
  ex: "Which part of settlement life this belongs to and where it appears in the dossier. This classifies presentation; it does"
  ex: "Describes the kind of power associated with this content. It is currently presentation-only and does not silently change"
  ex: "Describes a defensive role for presentation and future registered operations. It does not change defense totals unless a"
  consumers(1):
    src/components/compendium/CustomContentEditor.jsx:43|CUSTOM_CONTENT_FIELD_HINTS as FIELD_HINTS,

### 25  src/domain/display/engagementNarrative.js  ::  ENGAGEMENT_PHASES
  paths: ENGAGEMENT_PHASES.conquest[0] | ENGAGEMENT_PHASES.conquest[1] | ENGAGEMENT_PHASES.conquest[3]
  ex: "the lines close around the walls"
  ex: "the fields outside are eaten bare"
  ex: "the gate gives, and the town changes hands"
  consumers(1):
    tests/domain/display/engagementNarrative.test.js:6|ENGAGEMENT_PHASES,

### 25  src/domain/worldPulse/demographicsRates.js  ::  DEMOGRAPHIC_TUNING_COVERAGE
  paths: DEMOGRAPHIC_TUNING_COVERAGE[2].note | DEMOGRAPHIC_TUNING_COVERAGE[3].note | DEMOGRAPHIC_TUNING_COVERAGE[4].note
  ex: "What the ground does to the density ceiling, per terrain."
  ex: "The unsuppressed birth rate per tick, per tier."
  ex: "The natural mortality floor per tick, per tier. It exists at zero pressure forever."
  consumers(2):
    tests/domain/demographicsRates.test.js:26|DEMOGRAPHIC_TUNING_COVERAGE,
    scripts/count-tuning-inventory.mjs:38|* ⚠ CAPACITY's 16-row `DEMOGRAPHIC_TUNING_COVERAGE` is NOT here, and its absence is

### 23  src/domain/factionRename.js  ::  FACTION_RENAME_SURFACES
  paths: FACTION_RENAME_SURFACES[0].why | FACTION_RENAME_SURFACES[1].why | FACTION_RENAME_SURFACES[2].why
  ex: "the canonical display name"
  ex: "the legacy alias every tolerant reader falls back to"
  ex: "the exact name of the faction holding the governing seat"
  consumers(4):
    src/store/settlementPendingEdits.js:326|// FACTION_RENAME_SURFACES cascade (the count lives in that list, never in a
    src/store/settlementRenameHelpers.js:21|* denominator declares — FACTION_RENAME_SURFACES and NPC_RENAME_SURFACES in
    src/store/settlementSlice.js:740|// FACTION_RENAME_SURFACES and into every neighbour save, then persists and
    tests/domain/factionRename.test.js:11|* measured FACTION_RENAME_SURFACES against a hand-written PROBES map that named

### 22  src/domain/stressorPicker.js  ::  buildStressorPickerItems()
  paths: buildStressorPickerItems()[0].name | buildStressorPickerItems()[0].desc | buildStressorPickerItems()[1].desc
  ex: "Beast & Raider Threat"
  ex: "The attacks are following a pattern that suggests coordination, not desperation. Someone is directing this (whether a ri"
  ex: "Campaign crisis: pressures public legitimacy, faction stability, social trust."
  consumers(3):
    src/components/settlement/EventComposer.jsx:29|import { buildStressorPickerItems } from '../../domain/stressorPicker.js';
    tests/domain/coupEvents.test.js:8|import { buildStressorPickerItems, pulseTypeForStressorKey, GEN_TO_PULSE_TYPE } from '../../src/domain/stressorPicker.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:3711|- inputs: stressor from the FULL vocabulary (generation types + campaign-only types + user custom stressors, buildStressorPickerItems — EventComposer.jsx:174-1

### 22  src/domain/supplyChainState.js  ::  NEED_HEURISTICS
  paths: NEED_HEURISTICS.food_security.failureConsequence | NEED_HEURISTICS.raw_extraction.victims[0] | NEED_HEURISTICS.raw_extraction.failureConsequence
  ex: "bread prices climb; relief queues lengthen; legitimacy strains."
  ex: "miners and quarry workers"
  ex: "raw inputs run short; downstream chains feel the squeeze first."
  consumers(6):
    tests/domain/supplyChainState.test.js:126|// NEED_HEURISTICS used to key 'trade'/'arcane'/'energy' — no such
    tests/joins/chains.test.js:27|import { NEED_HEURISTICS } from '../../src/domain/supplyChainState.js';
    docs/COHESION_REMEDIATION_PLAN.md:34|> carry), needKey/NEED_HEURISTICS vocabulary, dead-magic leaks sealed (divine food, magicProfile,
    docs/GENERATION_COHERENCE_AUDIT.md:157|7. ORPHAN (consumer, no producer): NEED_HEURISTICS and trace-switch keys 'trade'/'energy'/'arcane' (real keys: trade_entrepot, arcane_magical; no energy category
    docs/SIMULATION_LOGIC_AUDIT.md:367|Strengths: Single source of truth: CONDITION_ARCHETYPE_TEMPLATES drives presentation defaults, lifecycle expiry, default status/severity, and the affectedSystems wir
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:491|headlines, `CRIER_FRAMES`, `NEED_HEURISTICS` consequences, the market-prices

### 22  src/domain/worldPulse/stressorDynamics.js  ::  STRESSOR_SYNERGIES
  paths: STRESSOR_SYNERGIES.famine.disease_outbreak.note | STRESSOR_SYNERGIES.famine.siege.note | STRESSOR_SYNERGIES.siege.wartime.note
  ex: "the sick cannot work the fields"
  ex: "the blockade stands. No relief can arrive"
  ex: "the wider war keeps the besiegers supplied"
  consumers(1):
    tests/domain/rulingPower.test.js:15|import { STRESSOR_COUNTERFORCES, STRESSOR_SYNERGIES } from '../../src/domain/worldPulse/stressorDynamics.js';

### 21  src/domain/content/contentVocabulary.js  ::  CUSTOM_CONTENT_MANIFEST
  paths: CUSTOM_CONTENT_MANIFEST.purpose | CUSTOM_CONTENT_MANIFEST.categories[0].fields[4].condition | CUSTOM_CONTENT_MANIFEST.categories[0].fields[8].condition
  ex: "The icon-free, category-aware authority for custom-content authoring, compiler admission, and effect truth."
  ex: "When a settlement is generated."
  ex: "Only while the institution is present in a settlement."
  consumers(12):
    src/components/compendium/customCategories.js:10|import { CUSTOM_CONTENT_MANIFEST } from '../../domain/content/customContentManifest.js';
    src/domain/content/customContentManifest.generated.js:12|export const CUSTOM_CONTENT_MANIFEST = deepFreeze({
    src/domain/content/customContentManifest.js:11|CUSTOM_CONTENT_MANIFEST,
    src/domain/content/livingContentRoster.js:40|* `CUSTOM_CONTENT_MANIFEST`: admitted authorable fields, in the manifest's own
    src/lib/contentPacks.js:31|CUSTOM_CONTENT_MANIFEST,
    tests/domain/customContentManifest.contract.test.js:10|CUSTOM_CONTENT_MANIFEST as CLIENT_MANIFEST,
    tests/domain/customContentMechanicalClaims.test.js:17|CUSTOM_CONTENT_MANIFEST,
    tests/domain/customContentPresentationClaims.test.js:24|CUSTOM_CONTENT_MANIFEST,
    tests/domain/customContentTierGates.test.js:51|import { CUSTOM_CONTENT_MANIFEST } from '../../src/domain/content/customContentManifest.js';
    tests/domain/livingContentMaterialization.test.js:56|CUSTOM_CONTENT_MANIFEST,

### 21  src/domain/npc/characterReadModel.js  ::  EXPERIENCE_CLAUSES
  paths: EXPERIENCE_CLAUSES.abandoned_unransomed | EXPERIENCE_CLAUSES.bereavement_close | EXPERIENCE_CLAUSES.captured_held
  ex: "a captivity nobody paid to end"
  ex: "a death close to home"
  ex: "a season held captive"
  consumers(1):
    tests/domain/npc/characterReadModel.test.js:54|EXPERIENCE_CLAUSES,

### 20  src/components/organic/samples/fixtures.js  ::  dossierFixture
  paths: dossierFixture.tradeAccess | dossierFixture.character | dossierFixture.arrival
  ex: "On the coast road; a tidal harbour"
  ex: "A wall town that learned to trade with the sea it once feared."
  ex: "You come to Thornwall by the coast road, and the wall shows first: grey stone the height of four men, salt-stained on it"
  consumers(1):
    src/components/organic/samples/DossierSample.jsx:5|import { dossierFixture as d } from './fixtures.js';

### 20  src/domain/tuning/proposedSoakBands.js  ::  RATIFIED_SOAK_BANDS
  paths: RATIFIED_SOAK_BANDS[0].label | RATIFIED_SOAK_BANDS[0].unit | RATIFIED_SOAK_BANDS[0].rationale
  ex: "Court successions per faction, per sim-decade"
  ex: "successions per faction per decade"
  ex: "The cadence dial targets \"a succession per faction every few sim-years, NEITHER stasis NOR churn.\" A few years is roughl"
  consumers(4):
    tests/domain/proposedSoakBands.test.js:12|RATIFIED_SOAK_BANDS,
    scripts/check-tuning-bands.mjs:28|RATIFIED_SOAK_BANDS,
    docs/FABLE_VALIDATION_QUEUE.md:4739|numeric ceiling, exactly like `RATIFIED_SOAK_BANDS`. A repetition vocabulary would be a second
    docs/implementation/packets/fp/HB-0B.md:99|`RATIFIED_SOAK_BANDS.namesHabit = false`; its seven `constantModule` values are npcLadder×3,

### 19  src/domain/worldPulse/index.js  ::  DRAMA_CLASS_REGISTRY
  paths: DRAMA_CLASS_REGISTRY.stressor_birth_siege.rationale | DRAMA_CLASS_REGISTRY.stressor_birth_wartime.rationale | DRAMA_CLASS_REGISTRY.stressor_birth_disease_outbreak.rationale
  ex: "A besieging force is an independent war-arc birth."
  ex: "Wartime pressure spawns a spontaneous war arc."
  ex: "A disease outbreak is the plague class."
  consumers(8):
    src/domain/display/chronicleGraph.js:55|* A stressor TYPE → drama class map, mirroring DRAMA_CLASS_REGISTRY's
    src/domain/worldPulse/decisionTier.js:187|export const DRAMA_CLASS_REGISTRY = Object.freeze({
    src/domain/worldPulse/narrativeTempo.js:19|* The DRAMA_CLASS_REGISTRY + DRAMA_CLASS_PRIORITY (the taxonomy) live in
    tests/domain/dramaClassRegistry.contract.test.js:18|DRAMA_CLASS_REGISTRY,
    docs/DESIGN_FP_ARCH_HB.md:816|| 5 | `faith_contested` | schism, conversion pressure, or a tradition claim is live | `religious_authority` band + `DRAMA_CLASS_REGISTRY` `'schism_contest'` |
    docs/DESIGN_FP_POPULATIONS.md:1124|candidate seam over the DRAMA_CLASS_REGISTRY taxonomy, while demographic
    docs/PHASE55_E0_TEMPO_GOVERNOR_PLAN.md:73|- **BUILD the registration mechanism** (design §6): the `DRAMA_CLASS_REGISTRY` + a **walker/contract test**
    docs/implementation/packets/foreign-policy/GR-4B.md:804|`DRAMA_CLASS_REGISTRY` (⚠ that machinery throttles **spontaneity, never causality** — a

### 18  src/components/organic/samples/fixtures.js  ::  pricingFixture
  paths: pricingFixture.noHiddenFees | pricingFixture.tiers[0].tagline | pricingFixture.tiers[0].features[0]
  ex: "The price on this page is the price at checkout. No setup fees, no seat math, no surprise renewals."
  ex: "For the curious DM trying things out."
  ex: "Generate any size, from hamlet to metropolis"
  consumers(1):
    src/components/organic/samples/PricingSample.jsx:3|import { pricingFixture as pr } from './fixtures.js';

### 17  src/data/economicData.js  ::  INSTITUTION_FINISHED_GOODS_DEMAND
  paths: INSTITUTION_FINISHED_GOODS_DEMAND.military.importLabels[0] | INSTITUTION_FINISHED_GOODS_DEMAND.military.importLabels[1] | INSTITUTION_FINISHED_GOODS_DEMAND.military.importLabels[2]
  ex: "Replacement arms and basic equipment"
  ex: "Quality weapons and armour"
  ex: "Advanced weapons and armour (bulk contract)"
  consumers(8):
    src/data/finishedGoodsCategory.js:6|// needs INSTITUTION_FINISHED_GOODS_DEMAND (via the derived index) but NOT the far
    src/data/goods/chains.js:40|*   INSTITUTION_FINISHED_GOODS_DEMAND Record<string, FinishedGoodsDemandRecord>
    src/domain/customContentSchema.js:88|//   • demandLive (military…alchemical): keys MUST match INSTITUTION_FINISHED_GOODS_DEMAND
    src/domain/goods.schema.js:34|*     • src/data/finishedGoodsCategory.js INSTITUTION_FINISHED_GOODS_DEMAND,
    src/generators/economy/finishedGoodsDemand.js:6|import { INSTITUTION_FINISHED_GOODS_DEMAND } from '../../data/goods/chains.js';
    tests/joins/finishedGoodsDemand.test.js:4|import { INSTITUTION_FINISHED_GOODS_DEMAND } from '../../src/data/economicData.js';
    tests/joins/goods.test.js:252|INSTITUTION_FINISHED_GOODS_DEMAND,
    docs/GENERATION_COHERENCE_AUDIT.md:502|Strengths: The chain-first derivation spine is genuinely coherent: nearbyResources → RESOURCE_TO_CHAINS activation → computeActiveChains (tier gates, magic=0 sup

### 16  src/domain/display/warAndRoadNames.js  ::  WAR_REASON_CLAUSES
  paths: WAR_REASON_CLAUSES.grievance | WAR_REASON_CLAUSES.revanchism | WAR_REASON_CLAUSES.resource_pressure
  ex: "an old wrong that was never answered"
  ex: "ground lost in an earlier war"
  ex: "fields and goods the realm can no longer do without"
  consumers(1):
    tests/domain/display/warAndRoadNames.test.js:5|WAR_REASON_CLAUSES,

### 16  src/domain/worldPulse/envoyChanceMeetingNews.js  ::  CHANCE_MEETING_KIND_REGISTRY
  paths: CHANCE_MEETING_KIND_REGISTRY[0].pool[0]() | CHANCE_MEETING_KIND_REGISTRY[0].pool[1]() | CHANCE_MEETING_KIND_REGISTRY[0].pool[2]()
  ex: "{npc} of {home}, passing through {settlement}, {outcome_phrase} in {counterpart}."
  ex: "Word from {settlement}: {npc} and {counterpart} {outcome_phrase}, and neither court sent them to."
  ex: "On the road to {settlement}, {npc} of {home} fell in with {counterpart} and {outcome_phrase} before either reached the g"
  consumers(2):
    tests/helpers/kindRegistryRoster.js:34|import { CHANCE_MEETING_KIND_REGISTRY } from '../../src/domain/worldPulse/envoyChanceMeetingNews.js';
    tests/lint/chanceMeetingKindPools.walker.test.js:81|CHANCE_MEETING_KIND_REGISTRY,

### 16  src/lib/galleryHubs.js  ::  GALLERY_HUBS
  paths: GALLERY_HUBS[0].blurb | GALLERY_HUBS[1].blurb | GALLERY_HUBS[2].blurb
  ex: "Public D&D settlements raised on plains terrain: browse, react, and import them into your own campaign."
  ex: "Public D&D settlements raised on hills terrain: browse, react, and import them into your own campaign."
  ex: "Public D&D settlements raised on forest terrain: browse, react, and import them into your own campaign."
  consumers(6):
    src/components/gallery/GalleryHubPage.jsx:21|import { GALLERY_HUBS, resolveHub } from '../../lib/galleryHubs.js';
    tests/build/prerenderRoutes.test.js:28|import { GALLERY_HUBS } from '../../src/lib/galleryHubs.js';
    tests/build/sitemap.test.js:17|import { GALLERY_HUBS } from '../../src/lib/galleryHubs.js';
    tests/lib/galleryHubs.test.js:15|import { GALLERY_HUBS, HUB_BY_PATH, resolveHub } from '../../src/lib/galleryHubs.js';
    scripts/generate-sitemap.mjs:33|import { GALLERY_HUBS } from '../src/lib/galleryHubs.js';
    scripts/prerender-routes.mjs:55|import { GALLERY_HUBS } from '../src/lib/galleryHubs.js';

### 14  src/lib/seo.js  ::  VIEW_DESCRIPTIONS
  paths: VIEW_DESCRIPTIONS.generate | VIEW_DESCRIPTIONS.compendium | VIEW_DESCRIPTIONS.pricing
  ex: "Generate a living tabletop-RPG settlement in seconds: economy, factions, NPCs, institutions, and history, ready for the "
  ex: "The SettlementForge compendium: settlement tiers, trade, power and faction archetypes, religion, stress, and the institu"
  ex: "SettlementForge pricing. Generate and save settlements for free, or unlock the Realm, AI narration, and cross-settlement"
  consumers(1):
    tests/build/prerenderRoutes.test.js:25|import { headForView, VIEW_DESCRIPTIONS, DEFAULT_DESCRIPTION } from '../../src/lib/seo.js';

### 13  src/components/compendium/customCategories.js  ::  CATEGORY_BY_KEY
  paths: CATEGORY_BY_KEY.institutions.dependencies[0].hint | CATEGORY_BY_KEY.institutions.dependencies[1].hint | CATEGORY_BY_KEY.institutions.dependencies[2].hint
  ex: "Trade goods or services this institution generates when present."
  ex: "Resources, goods, or services this institution consumes."
  ex: "Institutions this one represents. When present, smaller institutions are not listed separately."
  consumers(6):
    src/components/compendium/CustomContent.jsx:47|import { CUSTOM_CATEGORIES, CATEGORY_BY_KEY } from './customCategoryDefs.js';
    src/components/compendium/FactionEventBanner.jsx:11|* The faction accent is read from the shared CATEGORY_BY_KEY data (not a fresh
    src/components/compendium/customCategoryDefs.js:19|export const CATEGORY_BY_KEY = Object.freeze(
    src/domain/content/customContentManifest.js:72|const CATEGORY_BY_KEY = new Map(
    tests/components/customContentEditorManifest.test.jsx:9|import { CATEGORY_BY_KEY } from '../../src/components/compendium/customCategoryDefs.js';
    tests/domain/customFoundingTraditions.test.js:14|import { CATEGORY_BY_KEY } from '../../src/components/compendium/customCategoryDefs.js';

### 13  src/data/roadmapLedger.js  ::  ROADMAP_LEDGER
  paths: ROADMAP_LEDGER[0].title | ROADMAP_LEDGER[0].summary | ROADMAP_LEDGER[1].title
  ex: "Same seed, same world, forever"
  ex: "A world is stable and reproducible. The same seed always builds the same place, down to the last street, so what you sha"
  ex: "A settlement that explains itself"
  consumers(1):
    tests/data/roadmapLedger.test.js:13|ROADMAP_LEDGER,

### 13  src/domain/display/armyStrength.js  ::  ARMY_STRENGTH_PHRASES
  paths: ARMY_STRENGTH_PHRASES.STRENGTH_BANDS[0].phrase | ARMY_STRENGTH_PHRASES.STRENGTH_BANDS[1].phrase | ARMY_STRENGTH_PHRASES.STRENGTH_BANDS[2].phrase
  ex: "a formidable host, few in the region could match it in the field"
  ex: "a strong army, ready to take the field with confidence"
  ex: "a capable militia, enough to defend its own and raid a neighbour"
  consumers(2):
    tests/domain/display/forceComposition.test.js:12|import { ARMY_STRENGTH_PHRASES } from '../../../src/domain/display/armyStrength.js';
    docs/DESIGN_FP_ARCH_WY.md:729|| ARMIES | `armyTransit` + `navalTransit` (same record shape, `mode:'sea'`) | force-BANDED marker: the glyph's SIZE encodes the strength band (ARMY_STRENGTH_PHRASES ladde

### 13  src/domain/display/deityEffects.js  ::  DEITY_AXIS_EFFECTS
  paths: DEITY_AXIS_EFFECTS.alignment.evil.effect | DEITY_AXIS_EFFECTS.alignment.good.effect | DEITY_AXIS_EFFECTS.temperament.warlike.effect
  ex: "Evil-aligned worship lets corruption take root even without organized crime"
  ex: "Good-aligned worship exposes corruption and favors incorruptible successors"
  ex: "A warlike creed raises the realm's aggression"
  consumers(6):
    src/components/compendium/CatalogTabs.jsx:169|deity is authored on these four axes, projected from DEITY_AXIS_EFFECTS). */}
    tests/domain/display/deityEffects.test.js:5|DEITY_AXIS_EFFECTS,
    tests/ui/compendiumFaith.test.jsx:11|*      projected from the engine's DEITY_AXIS_EFFECTS single source (never re-typed).
    scripts/generate-compendium-data.mjs:49|import { DEITY_AXIS_EFFECTS } from '../src/domain/display/deityEffects.js';
    docs/COMPENDIUM_COMPLETION_PLAN.md:13|> - Wave C1 (shipped, `52a4d8c1`) — the deity FOUR AXES (from DEITY_AXIS_EFFECTS, no roster) lead the arcane tab + Pantheon Rank ladder (seats, PANTHEON_TUNING-bo
    docs/UIUX_AUDIT_AND_PLAN.md:1279|- WHAT WORKS — PRESERVE: the single-source-of-truth wiring (DeityEffectPreview and the Arcane axis effects both read describeDeityEffects/DEITY_AXIS_EFFECTS, the same 

### 13  src/domain/dossier/powerSupport.js  ::  SUPPORT_BASIS
  paths: SUPPORT_BASIS.founded | SUPPORT_BASIS.aligned.merchant | SUPPORT_BASIS.aligned.military
  ex: "Raised by this power"
  ex: "A commercial house of this power"
  ex: "An armed body under this command"
  consumers(2):
    tests/components/powerTabSupport.test.jsx:27|import { SUPPORT_BASIS } from '../../src/domain/dossier/powerSupport.js';
    tests/domain/dossier/powerSupport.test.js:19|import { deriveFactionSupport, SUPPORT_BASIS } from '../../../src/domain/dossier/powerSupport.js';

### 13  src/domain/worldPulse/faithTuningSurface.js  ::  FAITH_TUNING_COVERAGE
  paths: FAITH_TUNING_COVERAGE[0].note | FAITH_TUNING_COVERAGE[1].note | FAITH_TUNING_COVERAGE[2].note
  ex: "How much louder the patron is than a co-resident cult."
  ex: "The kernel reading of the three authored strength bands."
  ex: "The channel backstop; the reachable band uses 63 percent of it (pack row 30)."
  consumers(5):
    tests/domain/faithTuningSurface.test.js:33|*   5. THE COVERAGE PARTITION holds `FAITH_TUNING_COVERAGE` to the tables both
    tests/lint/.tuning-inventory.json:4016|"src/domain/worldPulse/faithTuningSurface.js#FAITH_TUNING_COVERAGE": {
    tests/lint/.tuning-register.json:84|"FAITH_TUNING_COVERAGE"
    scripts/count-tuning-inventory.mjs:74|exports: ['FAITH_TUNING_COVERAGE'],
    scripts/lib/tuning-inventory.mjs:228|// frozen ARRAY — `TIER_ORDER`, `IMPORTANCE_ORDER`, `FAITH_TUNING_COVERAGE`. Searching only

### 12  src/components/generate/characterPresets.js  ::  ARCHETYPES
  paths: ARCHETYPES[1].desc | ARCHETYPES[2].desc | ARCHETYPES[3].desc
  ex: "Trade hub; guild security"
  ex: "Major overland hub; active guilds"
  ex: "Resource extraction; high military"
  consumers(10):
    src/components/generate/CharacterPresetCard.jsx:37|import { ARCHETYPES, ARCHETYPE_GROUPS, archetypePatch } from './characterPresets.js';
    src/data/foundingSeeds.js:34|* @property {string} archetype   a characterPresets ARCHETYPES key (resolved via archetypePatch)
    src/domain/compendium/catalogData.js:32|export const ARCHETYPES = [
    src/domain/worldPulse/negotiationPictures.js:29|const ARCHETYPES = Object.freeze(['unknown', 'merchant', 'military', 'religious', 'other']);
    tests/components/characterPresetCard.test.jsx:17|ARCHETYPES,
    tests/data/foundingSeeds.probe.test.js:13|import { archetypePatch, ARCHETYPES } from '../../src/components/generate/characterPresets.js';
    tests/data/foundingSeeds.test.js:9|import { ARCHETYPES } from '../../src/components/generate/characterPresets.js';
    tests/docs/compendiumDataFreshness.test.js:29|import { ARCHETYPES, REL_TYPES } from '../../src/domain/compendium/catalogData.js';
    scripts/generate-compendium-data.mjs:32|*   ARCHETYPES and the 8 neighbour RELATIONSHIP types live in catalogData.js. They
    docs/DESIGN_FP_ARCH_CENSUS.md:208|Two more closed vocabularies live beside them but are NOT in the bands map: `ARCHETYPES`

### 12  src/design/organic/index.js  ::  MOTION_GRAMMAR
  paths: MOTION_GRAMMAR.layDown.means | MOTION_GRAMMAR.unfold.means | MOTION_GRAMMAR.ruleDraw.means
  ex: "page arrival — the document is laid on the desk and settles"
  ex: "panel reveal — a folded leaf opens from its crease"
  ex: "a rule draws itself once at first paint, left to right"
  consumers(2):
    src/design/organic/motion.js:35|export const MOTION_GRAMMAR = Object.freeze({
    tests/design/organicMotion.test.js:7|*      spelling (MOTION_GRAMMAR is canonical; CSS is the projection).

### 12  src/domain/factionRename.js  ::  NPC_NON_CASCADED_SURFACES
  paths: NPC_NON_CASCADED_SURFACES[0].why | NPC_NON_CASCADED_SURFACES[1].why | NPC_NON_CASCADED_SURFACES[2].why
  ex: "the secret is generated prose that names the characters it implicates; prose policy for people is owner-gated (see above"
  ex: "the stakes sentence names who would pay to bury the secret; prose policy for people is owner-gated"
  ex: "the grouping copy of the same secret prose; it moves if and only if its twin does"
  consumers(2):
    tests/domain/npcRename.test.js:23|* or NPC_NON_CASCADED_SURFACES (a written ruling), so a FUTURE field carrying a
    docs/FABLE_VALIDATION_QUEUE.md:46|| 2026-08-01 | factionRename cascade extended to `role`, `factionGoal` and `relationships[].npc1Role/npc2Role` | TREATED AS REPAIR, not new capability, so taken rathe

### 12  src/domain/region/goodsCatalog.js  ::  GOOD_CATALOG
  paths: GOOD_CATALOG.grain.aliases[1] | GOOD_CATALOG.livestock.aliases[5] | GOOD_CATALOG.stone.aliases[7]
  ex: "bulk grain and foodstuffs"
  ex: "raw hides and animal products"
  ex: "cut stone and masonry"
  consumers(1):
    tests/domain/marketPrices.test.js:26|import { GOOD_CATALOG } from '../../src/domain/region/goodsCatalog.js';

### 12  src/domain/townMap/arch/cathedralSection.js  ::  buildCathedralSection()
  paths: buildCathedralSection().elements[0] | buildCathedralSection().elements[2] | buildCathedralSection().elements[3]
  ex: "pierced nave wall (thickness + reveals)"
  ex: "pointed archivolt (voussoir ring)"
  ex: "central mullion + jamb shafts"
  consumers(2):
    tests/architecture/k0bMesh.test.js:21|import { buildCathedralSection } from '../../src/domain/townMap/arch/cathedralSection.js';
    scripts/generate-k0b.mjs:14|* DETERMINISM: buildCathedralSection + encodeGlb are pure -- no clock, no rng. This script renders

### 12  src/domain/worldPulse/index.js  ::  PARTY_IMPACT_KINDS
  paths: PARTY_IMPACT_KINDS.resolve_stressor.note | PARTY_IMPACT_KINDS.ease_stressor.note | PARTY_IMPACT_KINDS.worsen_stressor.note
  ex: "The party ended an active stressor (broke the siege, cured the plague)."
  ex: "The party blunted but did not end a stressor."
  ex: "The party (or their failure) deepened a stressor."
  consumers(16):
    src/domain/aiOutputSchema.js:16|* buildConstructVocabulary, buildOpVocabulary, PARTY_IMPACT_KINDS, the
    src/domain/events/partyEventLinkage.js:27|import { PARTY_IMPACT_KINDS } from '../worldPulse/partyImpactKinds.js';
    src/domain/intent/applyDispatch.js:13|*     world-pulse party-input lane (worldState-scoped; action.kind ∈ PARTY_IMPACT_KINDS).
    src/domain/intent/opVocabulary.js:18|import { PARTY_IMPACT_KINDS } from '../worldPulse/partyImpactKinds.js';
    src/domain/worldPulse/partyImpact.js:34|import { PARTY_IMPACT_KINDS } from './partyImpactKinds.js';
    src/domain/worldPulse/partyImpactKinds.js:21|export const PARTY_IMPACT_KINDS = Object.freeze({
    tests/domain/aiOutputSchema.test.js:49|import { PARTY_IMPACT_KINDS } from '../../src/domain/worldPulse/partyImpactKinds.js';
    tests/domain/events/partyEventLinkage.test.js:3|import { PARTY_IMPACT_KINDS } from '../../../src/domain/worldPulse/partyImpact.js';
    tests/domain/interpretReview.test.js:22|import { PARTY_IMPACT_KINDS } from '../../src/domain/worldPulse/partyImpactKinds.js';
    tests/domain/namedFateConsentBarrier.test.js:36|import { PARTY_IMPACT_KINDS } from '../../src/domain/worldPulse/partyImpactKinds.js';

### 11  src/components/map/WorldMapTourSteps.js  ::  WORLD_MAP_TOUR_STEPS
  paths: WORLD_MAP_TOUR_STEPS[0].body | WORLD_MAP_TOUR_STEPS[1].title | WORLD_MAP_TOUR_STEPS[1].body
  ex: "Switch between View, Terrain, Annotate, and Routes. Each mode swaps the toolbar below for tools specific to that task."
  ex: "Place & select settlements"
  ex: "Drag a saved canon settlement from the palette onto the map to place it, then click any placed settlement to select and "
  consumers(2):
    src/components/map/WorldMapOverlays.jsx:16|import { WORLD_MAP_TOUR_STEPS } from './WorldMapTourSteps.js';
    tests/components/worldMapTourSteps.test.js:13|import { WORLD_MAP_TOUR_STEPS } from '../../src/components/map/WorldMapTourSteps.js';

### 11  src/data/institutionServiceKeys.generated.js  ::  INSTITUTION_SERVICE_KEYS
  paths: INSTITUTION_SERVICE_KEYS[99] | INSTITUTION_SERVICE_KEYS[106] | INSTITUTION_SERVICE_KEYS[117]
  ex: "Airship docking (high magic)"
  ex: "Aqueduct or water system"
  ex: "Bowyers & fletchers (guild)"
  consumers(3):
    src/store/toggleSlice.js:39|INSTITUTION_SERVICE_KEYS,
    tests/store/toggleSlice.servicesNormalize.test.js:28|INSTITUTION_SERVICE_KEYS,
    scripts/generate-institution-service-keys.mjs:42|+ `export const INSTITUTION_SERVICE_KEYS = Object.freeze(${

### 11  src/domain/display/stateProse/defenseStateProse.js  ::  DEF10_DARK_POOLS
  paths: DEF10_DARK_POOLS[0] | DEF10_DARK_POOLS[1] | DEF10_DARK_POOLS[2]
  ex: "badge STRONG (any arm)"
  ex: "badge ADEQUATE (any arm)"
  ex: "badge WEAK (any arm)"
  consumers(2):
    src/domain/display/stateProse/dossierMounts.js:244|* `defenseStateProse.js` at `DEF7_DARK_POOLS` and `DEF10_DARK_POOLS`, pinned by desk arms so
    tests/domain/defenseStateProseDesk.test.js:31|DEF7_DARK_POOLS, DEF10_DARK_POOLS, DEF10_FACT_SPOKEN_AT,

### 11  src/domain/display/warRemembrance.js  ::  WAR_ENDING_TELLINGS
  paths: WAR_ENDING_TELLINGS.punitive_sack_vengeance | WAR_ENDING_TELLINGS.punitive_sack_initiation | WAR_ENDING_TELLINGS.conquest
  ex: "It ended in a sack, taken as the answer to a burning of its own."
  ex: "It ended in a sack, and that burning was the opening of the reckoning, not its answer."
  ex: "It ended in conquest: {victor} held what it had taken."
  consumers(1):
    tests/ui/warRemembranceReader.test.jsx:50|WAR_ENDING_TELLINGS,

### 10  src/components/home/landingFixture.js  ::  fixture
  paths: fixture.town.prose | fixture.town.pressure | fixture.town.hooks[1].text
  ex: "The road into Cnocby becomes a street at a point you can't precisely identify. The settlement grows around you gradually"
  ex: "Rónnat Sullivan is about to call it in."
  ex: "A neutral figure is being pressured by both The Free Alliance and The Establishment to take a side before the next counc"
  consumers(1053):
    src/components/ChroniclePanel.jsx:235|Real key FIRST, legacy `.description` kept as a tail fallback (fixture-shaped
    src/components/SettlementDetail.jsx:309|// fixture). SettlementDetail is the one surface that owns this edit session,
    src/components/compendium/FactionEventBanner.jsx:7|* Rather than wire them in (fixture / byte-identity risk), we RELABEL: a faction
    src/components/contentStudio/ContentSampleReceipt.jsx:44|function FixtureResult({ fixture }) {
    src/components/generate/PlaceInRegionCard.jsx:12|* golden fixture on (the golden CFGs set neither). It does NOT change any
    src/components/generate/WizardEmptyState.jsx:15|* (not duplicates of the landing's own fixture artifacts), so the item's delete-only-
    src/components/home/HomeSampleDossier.jsx:6|* fixture from Pillar G so the entities the callouts reference are
    src/components/home/LandingArtifacts.jsx:14|* exact town" button replays the fixture's recorded { seed, config } through the
    src/components/home/LandingBelowFold.jsx:8|* W-L2/1) and live in ./LandingArtifacts.jsx with their fixture; §05 renders up
    src/components/home/RegionWakeReplay.jsx:5|* visitors. It scrubs a small CANNED fixture (domain/display/regionWakeReplay.js)

### 10  src/data/securityQuestions.js  ::  SECURITY_QUESTIONS
  paths: SECURITY_QUESTIONS[0].text | SECURITY_QUESTIONS[1].text | SECURITY_QUESTIONS[2].text
  ex: "What was the name of the first street you lived on?"
  ex: "What was the first name of your oldest childhood friend?"
  ex: "What was the name of your first pet?"
  consumers(2):
    src/components/auth/SecurityQuestionsFields.jsx:16|import { SECURITY_QUESTIONS } from '../../data/securityQuestions.js';
    tests/security/securityQuestions.test.js:6|* exports SECURITY_QUESTIONS[].id and migration 066 hard-codes the same id set in

### 10  src/domain/display/mobilizationStatus.js  ::  MOBILIZATION_STATUS_PHRASES
  paths: MOBILIZATION_STATUS_PHRASES.POSTURE_PHRASE.war_preparation | MOBILIZATION_STATUS_PHRASES.POSTURE_PHRASE.mobilized | MOBILIZATION_STATUS_PHRASES.POSTURE_PHRASE.war_exhaustion
  ex: "gearing for war, the economy shifting to a war footing"
  ex: "fully mobilized, a war economy ready to march"
  ex: "war-weary, its army spent"
  consumers(0):

### 10  src/domain/worldPulse/envoyChanceMeeting.js  ::  CHANCE_MEETING_TUNING_PROVENANCE
  paths: CHANCE_MEETING_TUNING_PROVENANCE.status | CHANCE_MEETING_TUNING_PROVENANCE.ownerRows[0] | CHANCE_MEETING_TUNING_PROVENANCE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (§12 row 7; enrolled in TUNEREG at its landing)"
  ex: "the three base rungs 3 / 4 / 2 and the plus-or-minus-one modifiers"
  ex: "the approach die: base 2, plus 2 at an at-odds target, plus 1 covert"
  consumers(2):
    src/domain/worldPulse/envoyChanceMeetingStage.js:119|* ENC-1's `CHANCE_MEETING_TUNING_PROVENANCE` names it in so many words: "the exposure
    tests/domain/envoyChanceMeeting.test.js:29|CHANCE_MEETING_TUNING_PROVENANCE,

### 10  src/domain/worldPulse/factionCompetition.js  ::  FACTION_VERB_PHRASES
  paths: FACTION_VERB_PHRASES.faction_government_challenge.may | FACTION_VERB_PHRASES.faction_government_challenge.did | FACTION_VERB_PHRASES.faction_institution_suppression.may
  ex: "press a challenge to the government"
  ex: "presses a challenge to the government"
  ex: "move to suppress an institution"
  consumers(7):
    src/domain/certification/subsystemRowsBaseline.js:152|// FACTION_VERB_PHRASES table (factionCompetition.js:557), which the module's
    tests/domain/reasonHeadlineRegister.test.js:19|import { FACTION_VERB_PHRASES } from '../../src/domain/worldPulse/factionCompetition.js';
    tests/domain/subsystemRowsFaithPolitics.test.js:38|import { FACTION_VERB_PHRASES } from '../../src/domain/worldPulse/factionCompetition.js';
    tests/lint/newsHeadlineContract.walker.test.js:22|import { FACTION_VERB_PHRASES } from '../../src/domain/worldPulse/factionCompetition.js';
    docs/content/RECEIPT_POOLS_INTERIOR.md:1088|`FACTION_VERB_PHRASES` derives BOTH the hedged candidate headline (`may {may}`)
    docs/implementation/PACKET_MANIFEST.json:3861|"symbol": "export const FACTION_VERB_PHRASES"
    docs/implementation/packets/corpus-coverage/AO-4.md:164|- any summary or `reasons` rewrite, producer wording change, `FACTION_VERB_PHRASES` edit,

### 9  src/domain/cultureProfiles.js  ::  materializeCulturalIdentity()
  paths: materializeCulturalIdentity().scope | materializeCulturalIdentity().builtForm | materializeCulturalIdentity().civicPattern
  ex: "A locally blended design grammar; both traditions remain visible rather than collapsing into a generic fallback."
  ex: "Timber-framed street houses cluster around a market green, with stone reserved for halls, bridges, and defenses. Masonry"
  ex: "Household heads, landed patrons, and craft associations negotiate authority through customary rights. Councils, magistra"
  consumers(3):
    src/data/cultureProfiles.js:586|export function materializeCulturalIdentity(culture, rng = null) {
    src/generators/steps/resolveConfig.js:16|materializeCulturalIdentity,
    tests/domain/cultureProfiles.test.js:14|materializeCulturalIdentity,

### 9  src/domain/display/labelBands.js  ::  COMPLEXITY_LABEL
  paths: COMPLEXITY_LABEL.HIGHLY_DIVERSIFIED | COMPLEXITY_LABEL.DIVERSIFIED | COMPLEXITY_LABEL.CONCENTRATED
  ex: "Highly diversified — multiple major revenue streams"
  ex: "Diversified — broad institutional economic base"
  ex: "Concentrated — fewer revenue streams than scale suggests"
  consumers(1):
    src/domain/display/stateProse/economyStateProse.js:80|import { COMPLEXITY_LABEL } from '../labelBands.js';

### 9  src/domain/fieldManifest.js  ::  FROZEN_VS_LIVE
  paths: FROZEN_VS_LIVE[1].displayRule | FROZEN_VS_LIVE[2].displayRule | FROZEN_VS_LIVE[6].displayRule
  ex: "Generation verdicts with NO live sibling — display-as-generated is honest today. Whichever package gives any of them a p"
  ex: "Displays read the tick-advanced stock, never a cached generation value."
  ex: "Zeroed while an effective deficit holds; restored from the structural base."
  consumers(8):
    src/components/new/tabs/OverviewTab.jsx:331|{/* First-survey framing (G5 / FROZEN_VS_LIVE): the five score bars and
    src/pdf/sections/Overview.jsx:138|(FROZEN_VS_LIVE grammar). One string, vetoable. */}
    tests/components/frozenTenseDefenseCopy.test.js:5|* src/domain/fieldManifest.js FROZEN_VS_LIVE declares defenseProfile.scores.
    tests/components/g5FirstSurveyCopy.test.js:15|*     FROZEN_VS_LIVE grammar), Structural Issues, Coherence Notes (its sole
    tests/components/g5FirstSurveyPdfTwins.test.js:16|*     Security carve-out, FROZEN_VS_LIVE grammar), and the WARNINGS & NOTES
    tests/joins/fieldManifest.test.js:34|FROZEN_VS_LIVE,
    docs/CAPABILITY_REMEDIATION_PLAN.md:22|> - **Wave R-5b BATCH 1 SHIPPED (2026-07-27 ~14:45; folded same session)** — the seven owner-ratified implement items, all landed and gated: #3 G9/G10 polarity b
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:15|> **Staleness warning:** every reach, exposure, recovery, and gating claim below is a code-traced SNAPSHOT of 2026-07-26, not a declared contract. It goes stale 

### 9  src/domain/worldPulse/informationNews.js  ::  INFORMATION_KIND_REGISTRY
  paths: INFORMATION_KIND_REGISTRY[0].pool[0] | INFORMATION_KIND_REGISTRY[0].pool[1]() | INFORMATION_KIND_REGISTRY[0].pool[2]()
  ex: "Less than we fear, and the reckoning is a season stale."
  ex: "{counterpart} has been shown {band}; nothing since {season}."
  ex: "Given {band} to see, and given it late."
  consumers(8):
    tests/domain/neighbourMirror.test.js:25|import { INFORMATION_KIND_REGISTRY } from '../../src/domain/worldPulse/informationNews.js';
    tests/helpers/kindRegistryRoster.js:38|import { INFORMATION_KIND_REGISTRY } from '../../src/domain/worldPulse/informationNews.js';
    tests/lint/informationKindPools.walker.test.js:45|INFORMATION_KIND_REGISTRY,
    tests/ui/neighbourMirrorLine.test.js:40|import { INFORMATION_KIND_REGISTRY } from '../../src/domain/worldPulse/informationNews.js';
    docs/implementation/INDEX.md:1125|| LANDED-31 | [`IN-1c-a`](./packets/foreign-policy/IN-1C-A.md) | **LANDED** at `24c61190731687952924de5c146046c07845ebda`; do not redispatch | The INFORMATION registr
    docs/implementation/PACKET_MANIFEST.json:21994|"symbol": "export const INFORMATION_KIND_REGISTRY"
    docs/implementation/packets/foreign-policy/IN-1C-A.md:227|- the estate's **fifth phrased-kind registry family** — `INFORMATION_KIND_REGISTRY`, one row,
    docs/implementation/packets/fp/WF-8A.md:233|`HABIT_FORK_REGISTRY` · `INFORMATION_KIND_REGISTRY` · `FREQUENCY_FLOORS` / `floorFor`.

### 9  src/domain/worldPulse/pulseStageManifest.js  ::  PULSE_STAGE_TOPOLOGY
  paths: PULSE_STAGE_TOPOLOGY[0].purpose | PULSE_STAGE_TOPOLOGY[1].purpose | PULSE_STAGE_TOPOLOGY[2].purpose
  ex: "Normalize the incoming world and mint deterministic pulse identity."
  ex: "Age and update slow actor state before conditions and movers read it."
  ex: "Advance existing pressures and convert resolved conditions into consequences."
  consumers(3):
    tests/domain/pulseStageTopology.test.js:5|PULSE_STAGE_TOPOLOGY,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:12157|**Declared exclusion [added-by-critic-pass]:** `src/domain/worldPulse/pulseStageManifest.js` — `PULSE_STAGE_TOPOLOGY`, the declared 9-stage advance ordering (
    docs/implementation/PACKET_MANIFEST.json:1399|"symbol": "PULSE_STAGE_TOPOLOGY"

### 8  src/domain/factionRename.js  ::  NON_CASCADED_SURFACES
  paths: NON_CASCADED_SURFACES[0].why | NON_CASCADED_SURFACES[1].why | NON_CASCADED_SURFACES[2].why
  ex: "regime lineage is recorded history, not a live reference"
  ex: "the durable identity undo keys on; a rename changes the label only"
  ex: "recorded history, and unreachable: renames are refused in canon phase"
  consumers(1):
    tests/domain/factionRename.test.js:22|*      FACTION_RENAME_SURFACES (it cascades) or NON_CASCADED_SURFACES (a written

### 8  src/domain/worldPulse/npcVerdictApply.js  ::  RELINQUISHED_FIELDS
  paths: RELINQUISHED_FIELDS[0].why | RELINQUISHED_FIELDS[1].why | RELINQUISHED_FIELDS[3].why
  ex: "the influence stock itself; a jailed or roaming person contributes none"
  ex: "the numeric power score the faction and ladder reads consume"
  ex: "the propagation weight a named figure carries into other reads"
  consumers(4):
    src/domain/worldPulse/disposition.js:94|// (npcVerdictApply.RELINQUISHED_FIELDS) is a hasOwnProperty-guarded STRIP that
    tests/domain/npcVerdictApply.test.js:30|RELINQUISHED_FIELDS,
    docs/COMPREHENSIVE_REVIEW_2026-08-01.md:452|- Relinquishment is the estate's alias-trap discipline done right: RELINQUISHED_FIELDS declared once with per-field rationale (including all five npcInFacti
    docs/FABLE_VALIDATION_QUEUE.md:7458|| 2026-08-11 | ✅ FABLE-VALIDATED 2026-08-11 (chair session c42c8924) — the NPC ladder rungs `dots`/`notability` REFUSED IN BOTH DIRECTIONS and escalated; nothing ed

### 7  src/components/generate/nextSteps.js  ::  buildNextSteps()
  paths: buildNextSteps().steps[0].label | buildNextSteps().steps[0].hint | buildNextSteps().steps[1].hint
  ex: "Save it to your library"
  ex: "Keep it for campaigns, inline editing, and export."
  ex: "A print-ready dossier for the table."
  consumers(3):
    src/components/PostGenCoach.jsx:43|import { buildNextSteps } from './generate/nextSteps.js';
    tests/components/wizardNextSteps.test.js:3|* builder. buildNextSteps() derives the ordered "what's next" checklist
    docs/review-r2/VERIFY_DIMS_RESULTS.json:61|"reasoning": "Every specific claim checks out at current HEAD c1efa6c5 (finding tip 91a0c409 has moved; verified against HEAD). (1) Only 2 of 9 registered ho

### 7  src/components/organic/samples/fixtures.js  ::  deitiesFixture
  paths: deitiesFixture.intro | deitiesFixture.entries[0].portfolio | deitiesFixture.entries[1].portfolio
  ex: "The twelve deities of the core pantheon, as a surveyor records them: the name a town swears by, the portfolio it prays o"
  ex: "The sea, safe harbour, and the toll of drowning"
  ex: "Smithing, honest weights, and the guild oath"
  consumers(1):
    src/components/organic/samples/CompendiumSample.jsx:3|import { deitiesFixture as pantheon } from './fixtures.js';

### 7  src/components/settlement/eventComposer/EventComposerConstants.js  ::  _TYPE_ICONS
  paths: _TYPE_ICONS.REMOVE_INSTITUTION.render().props.iconNode[1][1].d | _TYPE_ICONS.REMOVE_INSTITUTION.render().props.iconNode[2][1].d | _TYPE_ICONS.DAMAGE_INSTITUTION.render().props.iconNode[0][1].d
  ex: "M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"
  ex: "M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"
  ex: "M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-"
  consumers(0):

### 7  src/data/namingData.js  ::  NAMING_DATA
  paths: NAMING_DATA.germanic.description | NAMING_DATA.arabic.description | NAMING_DATA.east_asian.description
  ex: "Northern European, medieval German/Austrian style"
  ex: "Middle Eastern/North African influenced"
  ex: "Chinese, Japanese and Korean medieval influenced - Song Dynasty, Heian Period, Goryeo"
  consumers(26):
    src/domain/npc/npcOps.js:212|* @param {NamingPool} [args.namingData]           the culture's NAMING_DATA entry (names)
    src/domain/townCartography/cartographyWards.js:125|* @param {unknown} namingPools the injected NAMING_DATA, by reference
    src/domain/townScene/compileTownSceneManifest.js:513|+ 'the caller above the lazy boundary must supply NAMING_DATA from src/data/namingData.js',
    src/domain/worldPulse/satellitesLedger.js:55|* @property {string} name          seeded from NAMING_DATA on the satellite fork
    src/domain/worldPulse/settlementLifecycleKernel.js:68|import { NAMING_DATA } from '../../data/namingData.js';
    src/generators/npcGenerator.js:26|import { NAMING_DATA } from '../data/namingData.js';
    src/lib/townScene/townSceneExport.js:161|? (await import('../../data/namingData.js')).NAMING_DATA
    src/workers/townScene.worker.js:64|? (await loadNamingPools()).NAMING_DATA : null;
    tests/components/gallery/facetAlignment.test.js:13|import { NAMING_DATA } from '../../../src/data/namingData.js';
    tests/data/namingDataCharset.test.js:9|* WHAT IT FOUND, AND WHAT CLOSED IT. Walking every string in NAMING_DATA against

### 7  src/domain/aiGrounding.js  ::  forbiddenChanges()
  paths: forbiddenChanges()[0] | forbiddenChanges()[1] | forbiddenChanges()[2]
  ex: "Adding NEW factions, institutions, NPCs, threats, conditions, or chains."
  ex: "Renaming proper nouns (settlement name, faction names, NPC names, district names)."
  ex: "Changing numerical or categorical facts (population, tier, prosperity band, faction power)."
  consumers(9):
    src/store/settlementSlice.js:774|// grounding sees it via `forbiddenChanges`, and the verifier
    tests/domain/aiGrounding.test.js:30|forbiddenChanges,
    tests/domain/userEdits.grounding.test.js:7|*   - forbiddenChanges() emits one "MUST PRESERVE user-edited field"
    tests/edgeFunctions/aiGroundingBundle.freshness.test.js:90|'forbiddenChanges',
    tests/edgeFunctions/aiGroundingContract.test.js:35|forbiddenChanges,
    tests/store/userEdits.actions.test.js:289|test('an edit appears in forbiddenChanges as a MUST PRESERVE line', async () => {
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:1437|- reach: `_authored: true` promotes the entity to `source:'user', locked:true` via canonStatus tagger; AI grounding sees it via `forbiddenChanges`; NPC reroll 
    docs/abuse-model.md:235|`forbiddenChanges(settlement)` so every refinement-pass prompt
    docs/settlement-schema.md:195|`forbiddenChanges(settlement)` returns the per-call MUST PRESERVE

### 7  src/domain/customContentSchema.js  ::  DEFENSE_ROLES
  paths: DEFENSE_ROLES[0].label | DEFENSE_ROLES[1].label | DEFENSE_ROLES[2].label
  ex: "Does not contribute to defense"
  ex: "Fortification — walls, towers"
  ex: "Garrison — standing troops"
  consumers(1):
    src/components/compendium/CustomItemAttributes.jsx:13|CRITICALITY, ECONOMIC_WEIGHT, DEFENSE_ROLES, FOOD_IMPACT, POWER_AUTHORITIES,

### 7  src/domain/display/neighbourMirror.js  ::  MIRROR_BASIS_WORDS
  paths: MIRROR_BASIS_WORDS.intercept:caught | MIRROR_BASIS_WORDS.plant:strengthBand | MIRROR_BASIS_WORDS.posture:sealed
  ex: "an errand of ours they took off the road"
  ex: "a story we seeded in their court"
  ex: "a silence we have kept"
  consumers(3):
    tests/domain/neighbourMirror.test.js:23|MIRROR_BASIS_WORDS, MIRROR_BAND_WORDS, MIRROR_STALENESS_WORDS, neighbourMirrorLines,
    docs/implementation/packets/foreign-policy/IN-1B.md:609|MIRROR_BASIS_WORDS                // Object.freeze({...})  basis token -> one DM-facing phrase
    docs/implementation/packets/foreign-policy/IN-1C-A.md:96|| **IN-1b** | `src/domain/display/neighbourMirror.js` (**79 eff / 188 raw**) exports `NEIGHBOUR_MIRROR_HEADING`, `MIRROR_BAND_WORDS`, `MIRROR_S

### 7  src/domain/worldPulse/deityFlaws.js  ::  DEITY_FLAWS
  paths: DEITY_FLAWS[0].note | DEITY_FLAWS[1].note | DEITY_FLAWS[2].note
  ex: "The packet-anchored modulation: the boon weakens as pantheon share is contested. Reads the religion state shares only."
  ex: "The packet-anchored modulation: the temper pull sharpens while faith fortunes fall. Reads the pantheon ledger wins and l"
  ex: "D2 chartered variance, not mean: the boon and bane magnitude band widens. A variance needs a seeded draw, and the field "
  consumers(4):
    src/domain/display/faithDeepening.js:50|import { FLAW_EFFECTS, DEITY_FLAWS, viceLevelOf } from '../worldPulse/deityFlaws.js';
    src/domain/worldPulse/faithTuningSurface.js:247|* `deityFlaws.DEITY_FLAWS` — a coverage row below, not a resident here.
    tests/domain/deityFlaws.test.js:23|DEITY_FLAWS,
    tests/domain/faithTuningSurface.test.js:296|'DEITY_FLAWS',

### 7  src/domain/worldPulse/espionage/operationsVoice.js  ::  OPERATIONS_VOICE_PROVENANCE
  paths: OPERATIONS_VOICE_PROVENANCE.status | OPERATIONS_VOICE_PROVENANCE.ownerRows[0] | OPERATIONS_VOICE_PROVENANCE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (every sentence in this file is voice-taste awaiting the pen; the vocabularies are closed, the"
  ex: "every line and reason here is the pen's: the words are candidates, and none is signed"
  ex: "the going-native reversal line is DELIBERATELY UNWRITTEN. Writing it is a ruling on the unsigned reversal fork, never a "
  consumers(1):
    tests/domain/operationsVoice.test.js:35|OPERATIONS_VOICE_PROVENANCE,

### 7  src/lib/instantWorld/composerPipeline.js  ::  COMPOSER_COUPLINGS
  paths: COMPOSER_COUPLINGS[0].subject | COMPOSER_COUPLINGS[0].rule | COMPOSER_COUPLINGS[1].subject
  ex: "the shared id counter"
  ex: "Any step that mints an id must keep its position relative to every other id-minting step; the campaign id is minted FIRS"
  ex: "the placements ⇄ members join"
  consumers(1):
    tests/lib/instantWorld/composerPipeline.test.js:19|COMPOSER_COUPLINGS,

### 7  src/lib/spatialUsage.js  ::  EXEMPT_LEDGER_KEYS
  paths: EXEMPT_LEDGER_KEYS.merchantAppetite | EXEMPT_LEDGER_KEYS.bufferDiscipline | EXEMPT_LEDGER_KEYS.generosityWillingness
  ex: "supply-web economic pressure STOCK, not a distinct exercised mover"
  ex: "E1 generosity reserve-discipline STOCK (give-side motive substrate), not a distinct exercised mover"
  ex: "E1 generosity give-side WILLINGNESS STOCK (motive substrate feeding the generosity verbs), not a distinct exercised move"
  consumers(16):
    src/domain/worldPulse/commercialReasons.js:92|* src/lib/spatialUsage.js#EXEMPT_LEDGER_KEYS — a recomputed reason-ANNOTATION over a
    src/domain/worldPulse/envoyChanceMeetingLedger.js:26|* invisible to it. Both are registered in `spatialUsage.js` EXEMPT_LEDGER_KEYS beside
    src/domain/worldPulse/provenanceKernel.js:72|/** The spatialLedgers sub-key this writer owns (registered in EXEMPT_LEDGER_KEYS). */
    tests/domain/institutionStatusCertification.test.js:38|import { EXEMPT_LEDGER_KEYS, TRACKED_LEDGER_KEYS } from '../../src/lib/spatialUsage.js';
    tests/domain/vengeanceLicenseWr8.test.js:44|import { EXEMPT_LEDGER_KEYS } from '../../src/lib/spatialUsage.js';
    tests/lib/spatialLedgerCoverage.walker.test.js:13|* exempts it (add it to EXEMPT_LEDGER_KEYS with a reason).
    tests/lib/spatialUsage.test.js:15|EXEMPT_LEDGER_KEYS,
    docs/DESIGN_FP_ARCHITECTURE.md:709|| `advanceEpoch` = {byYear} | EP-3 Arm A ONLY | the EP-3 writer, ONE call site inside `pulseKernel.js` immediately after the calendar advances | CONTINGENT on Arm A;
    docs/DESIGN_FP_ARCH_EP.md:85|## accessor calls · `provenance`'s `EXEMPT_LEDGER_KEYS` row · the `memoryState` seam sitting
    docs/implementation/PACKET_MANIFEST.json:11716|"symbol": "import { TRACKED_LEDGER_KEYS, EXEMPT_LEDGER_KEYS }"

### 6  src/data/goods/chains.js  ::  INDUSTRY_WATER_NEEDS
  paths: INDUSTRY_WATER_NEEDS.Tannery.reason | INDUSTRY_WATER_NEEDS.Dyers' guild.reason | INDUSTRY_WATER_NEEDS.Fulling mill.reason
  ex: "Tanning requires large amounts of water for processing hides"
  ex: "Dyeing textiles requires significant water for preparation and rinsing"
  ex: "Fulling uses water-powered hammers to clean and thicken cloth"
  consumers(7):
    src/data/resourceChains.js:262|export const INDUSTRY_WATER_NEEDS = {
    src/domain/goods.schema.js:30|*     • src/data/resourceChains.js        RESOURCE_CHAINS, INDUSTRY_WATER_NEEDS
    src/generators/economy/viability.js:8|import { INDUSTRY_WATER_NEEDS } from '../../data/goods/chains.js';
    tests/build/vendorPdfLazy.test.js:353|// lazy-only RESOURCE_CHAINS + INDUSTRY_WATER_NEEDS tables eager → resourceChains.js
    tests/joins/goods.test.js:250|import { RESOURCE_CHAINS, INDUSTRY_WATER_NEEDS } from '../../src/data/resourceChains.js';
    docs/GENERATION_COHERENCE_AUDIT.md:499|The 'No Milling Facility' check fires only for 1000<population<5000 (economicGenerator.js:535) — a 50,000-person metropolis grinding grain with no mill (and no '
    docs/PHASE55_EXECUTION_PLAYBOOK.md:111|| **THE SM-3 WINDOW CLOSES + RATCHET #7** — the map becomes editable; the guidance layer lands; first paint drops again | e275349f (FP-G5 merge), dd9b01a2 (SM-3)

### 6  src/domain/display/occupationStatus.js  ::  OCCUPATION_STATUS_PHRASES
  paths: OCCUPATION_STATUS_PHRASES.STATE_PHRASE.contested | OCCUPATION_STATUS_PHRASES.STATE_PHRASE.unstable | OCCUPATION_STATUS_PHRASES.STATE_PHRASE.extractive
  ex: "a contested occupation; the occupier has not yet taken hold"
  ex: "an unstable occupation; control is precarious"
  ex: "an extractive occupation; the occupier bleeds it for everything it holds"
  consumers(0):

### 6  src/domain/display/stateProse/defenseStateProse.js  ::  DEF6_C3_BLOCKED_POOLS
  paths: DEF6_C3_BLOCKED_POOLS[4] | DEF6_C3_BLOCKED_POOLS[6] | DEF6_C3_BLOCKED_POOLS[7]
  ex: "Magical Capability: Arcane support"
  ex: "Legal Infrastructure: Court + Prison"
  ex: "Legal Infrastructure: Court only"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:29|DEF6_C3_BLOCKED_POOLS, DEF6_FACT_SPOKEN_AT, defenseSupportingProse,

### 6  src/domain/display/stateProse/defenseStateProse.js  ::  defenseThreatProse()
  paths: defenseThreatProse().invasion.sentence | defenseThreatProse().invasion.provenance.poolKey | defenseThreatProse().internal.sentence
  ex: "The town's plan for an army is to not be interesting to one, and everybody here can state the plan."
  ex: "Invasion & War: neither walls nor force"
  ex: "The town settles things itself, quickly, and does not always settle them well."
  consumers(2):
    src/components/new/tabs/DefenseTab.jsx:11|import { defenseCriminalProse, defenseForcesProse, defenseMilitaryStatusProse, defensePostureProse, defenseStateProse, defenseSupportingProse, defenseThreatPr
    tests/domain/defenseStateProseDesk.test.js:19|defenseForcesProse, defenseStateProse, defenseThreatProse, disasterRowPoolKey,

### 6  src/domain/npc/characterDrift.js  ::  DRIFT_PROVENANCE
  paths: DRIFT_PROVENANCE.status | DRIFT_PROVENANCE.ownerRows[0] | DRIFT_PROVENANCE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (the magnitudes are derived, not authored)"
  ex: "a TIGHTER per-axis offset clamp than the full spectrum span (F11 fixes the property, not the number)"
  ex: "the materialization floor: F9 offers a quarter-band as a CANDIDATE"
  consumers(2):
    src/domain/npc/livedExperienceFunnel.js:754|* owner row (carried in L2's DRIFT_PROVENANCE), and answering it here would answer
    tests/domain/npc/characterDrift.test.js:38|DRIFT_PROVENANCE,

### 6  src/domain/realm/realmItemAttention.js  ::  REALM_ATTENTION_DEFINITIONS
  paths: REALM_ATTENTION_DEFINITIONS.blocking_decision.reason | REALM_ATTENTION_DEFINITIONS.lapsed_order.reason | REALM_ATTENTION_DEFINITIONS.critical_condition.reason
  ex: "A realm decision is unresolved and awaits the GM."
  ex: "A staged order no longer passes its current legal preconditions."
  ex: "A significant condition is active in the current realm state."
  consumers(1):
    tests/domain/realmItemAttention.test.js:5|REALM_ATTENTION_DEFINITIONS,

### 6  src/domain/summary/settlementQuickGuide.js  ::  composeSettlementQuickGuide()
  paths: composeSettlementQuickGuide().identitySentence | composeSettlementQuickGuide().definingTruths[0].text | composeSettlementQuickGuide().definingTruths[1].text
  ex: "This settlement is a settlement."
  ex: "Its founding purpose is not recorded."
  ex: "No governing authority is recorded."
  consumers(6):
    src/components/TableView.jsx:29|import { composeSettlementQuickGuide } from '../domain/summary/settlementQuickGuide.js';
    src/components/new/SummaryTabV2.jsx:27|import { composeSettlementQuickGuide } from '../../domain/summary/settlementQuickGuide.js';
    src/components/session/SessionMode.jsx:37|import { composeSettlementQuickGuide } from '../../domain/summary/settlementQuickGuide.js';
    src/pdf/sections/TonightAtTheTable.jsx:21|import { composeSettlementQuickGuide } from '../../domain/summary/settlementQuickGuide.js';
    tests/domain/settlementQuickGuide.test.js:10|import { composeSettlementQuickGuide } from '../../src/domain/summary/settlementQuickGuide.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:396|- authority: `composeSettlementQuickGuide(settlement)` — `src/domain/summary/settlementQuickGuide.js` (shared composer with Table View / Session Mode / PDF, per

### 6  src/domain/worldPulse/espionage/infiltrationDrift.js  ::  PARKED_CUSTODY_EXITS
  paths: PARKED_CUSTODY_EXITS[0].unblockingAct | PARKED_CUSTODY_EXITS[1].unblockingAct | PARKED_CUSTODY_EXITS[2].unblockingAct
  ex: "a linking field on the hold row, or a close reason that names the agent: a persisted schema change, owner-gated"
  ex: "an elected-exposure door on the web's creation seam, which F8 rules applies verbatim to placed seats and therefore needs"
  ex: "a reachable vice-ward FIDELITY source: a design act on the W-LIVES table, not a tuning row"
  consumers(1):
    tests/domain/infiltrationDrift.test.js:44|PARKED_CUSTODY_EXITS,

### 6  src/domain/worldPulse/foundingCatalog.js  ::  FOUNDING_INSTITUTIONS
  paths: FOUNDING_INSTITUTIONS[0].desc | FOUNDING_INSTITUTIONS[1].desc | FOUNDING_INSTITUTIONS[2].desc
  ex: "A house of care for the dying and the incurably ill poor, kept by the faithful. No one is turned away for want of coin."
  ex: "A charitable house feeding and sheltering the destitute poor, the aged, and the disabled who cannot work. Endowed by the"
  ex: "A house that takes in parentless children and raises them until they can be apprenticed or placed. Order and mercy, kept"
  consumers(5):
    src/domain/worldPulse/moralInstitutionPressure.js:37|import { FOUNDING_INSTITUTIONS } from './foundingCatalog.js';
    tests/data/institutionVocabulary.test.js:30|import { FOUNDING_INSTITUTIONS } from '../../src/domain/worldPulse/foundingCatalog.js';
    tests/domain/moralInstitutionFounding.test.js:16|import { FOUNDING_INSTITUTIONS, foundingEntryByName } from '../../src/domain/worldPulse/foundingCatalog.js';
    scripts/audit/institution-ecology-soak.mjs:30|import { FOUNDING_INSTITUTIONS, foundingEntryByName } from '../../src/domain/worldPulse/foundingCatalog.js';
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:478|- Evidence: moralMartialLean.js:53 codes `[/slave\s*market|.../, { cruelty: 0.9, disorder: -0.4 }]` and the design examples repeat "CG abolishes the slave m

### 6  src/lib/supplyChains.js  ::  CHAIN_DEFS
  paths: CHAIN_DEFS[0].description | CHAIN_DEFS[1].description | CHAIN_DEFS[2].description
  ex: "Ore extraction to metalworking institutions"
  ex: "Agricultural production to food processing"
  ex: "Forestry to construction and shipbuilding"
  consumers(6):
    src/components/new/SupplyChainsPanel.jsx:9|const CHAIN_DEFS = {};
    src/lib/computeMapChains.js:18|import { CHAIN_DEFS, buildChainEdges } from './supplyChains.js';
    src/lib/roadNetwork.js:28|CHAIN_DEFS,
    src/pdf/sections/SupplyChainFlow.jsx:24|const CHAIN_DEFS = {};
    tests/lib/roadNetworkMst.test.js:477|test('a pair sharing multiple chains lists all chain ids (CHAIN_DEFS order)', () => {
    tests/lib/supplyChains.test.js:6|import { CHAIN_DEFS, buildChainEdges } from '../../src/lib/supplyChains.js';

### 5  src/components/settlements/useCampaignAdvance.js  ::  ADVANCE_REFUSAL_TEXT
  paths: ADVANCE_REFUSAL_TEXT.world_not_canonized | ADVANCE_REFUSAL_TEXT.no_settlements | ADVANCE_REFUSAL_TEXT.busy
  ex: "The realm advances only after you canonize this campaign world."
  ex: "Add at least one settlement to this campaign before advancing."
  ex: "The realm is already advancing. Give it a moment."
  consumers(1):
    tests/components/useCampaignAdvance.test.jsx:13|import { useCampaignAdvance, ADVANCE_REFUSAL_TEXT } from '../../src/components/settlements/useCampaignAdvance.js';

### 5  src/data/goods/chains.js  ::  IMPORT_GOODS_BY_TIER
  paths: IMPORT_GOODS_BY_TIER.village.basic[1].name | IMPORT_GOODS_BY_TIER.village.fromHigher[2].desc | IMPORT_GOODS_BY_TIER.town.restricted[0].desc
  ex: "Quality cloth and clothing"
  ex: "Wide variety of crafts"
  ex: "Slaves imported from war fronts, pirate suppliers, and distant slave-holding regions."
  consumers(11):
    src/data/tradeGoodsData.js:26|export const IMPORT_GOODS_BY_TIER = {
    src/domain/goods.schema.js:32|*     • src/data/tradeGoodsData.js        GOODS_CATEGORIES, IMPORT_GOODS_BY_TIER,
    src/lib/customRegistry.js:28|import { GOODS_MODIFIERS_BY_TIER, IMPORT_GOODS_BY_TIER } from '../data/goods/chains.js';
    tests/data/dataVocabularyCoverage.test.js:32|IMPORT_GOODS_BY_TIER,
    tests/joins/goods.test.js:255|import { IMPORT_GOODS_BY_TIER, COMMODITY_CATEGORY_MAP } from '../../src/data/tradeGoodsData.js';
    tests/joins/tradeGoodsRegistry.test.js:8|*   - IMPORT_GOODS_BY_TIER    (import): { tier: { groupKey: [{ name, … }] } }
    docs/COMPREHENSIVE_REVIEW_2026-07-15.md:1261|- **[HIGH|confirmed] data-tables-1** — Registry ingest reads IMPORT_GOODS_BY_TIER with the wrong shape — phantom prebuilt goods minted, all real import goo
    docs/GENERATION_COHERENCE_AUDIT.md:451|getUpgradeChain branches on `route === 'city' || route === 'metropolis'` and `route === 'metropolis'` (economicGenerator.js:1392-1395), but route is a trade-rout
    docs/briefs/W_R2_DATA_SURFACE_BRIEFS.md:7|## Base symbols: src/lib/customRegistry.js has an ingest reading IMPORT_GOODS_BY_TIER; src/data/supplyChainResourceIndex.js mentions the retired 'fish' chain.
    docs/review-r2/RAW_SURVEY_RESULTS.json:1994|"holistic": "The data tables are the best-governed layer I have reviewed in this codebase, and the round-1 fix program visibly landed here: the 33-key resou

### 5  src/data/goods/identity.js  ::  SPECIAL_RESOURCES
  paths: SPECIAL_RESOURCES.ancientRuins.description | SPECIAL_RESOURCES.magicalNode.description | SPECIAL_RESOURCES.holySite.description
  ex: "Remnants of a previous civilization"
  ex: "Intersection of magical energy"
  ex: "Location of religious significance"
  consumers(8):
    src/data/resourceChains.js:10|* SPECIAL_RESOURCES tables ARE eagerly reached by customRegistry /
    src/data/resourceData.js:371|export const SPECIAL_RESOURCES = {
    src/domain/goods.schema.js:28|*     • src/data/resourceData.js          RESOURCE_DATA, SPECIAL_RESOURCES
    src/generators/resourceGenerator.js:9|import {RESOURCE_DATA, SPECIAL_RESOURCES} from '../data/goods/identity.js';
    src/generators/structuralValidator.js:26|export { SPECIAL_RESOURCES as RELATION_TYPES } from '../data/goods/identity.js';
    src/lib/customRegistry.js:27|import { RESOURCE_DATA, SPECIAL_RESOURCES } from '../data/goods/identity.js';
    tests/joins/goods.test.js:249|import { RESOURCE_DATA, SPECIAL_RESOURCES } from '../../src/data/resourceData.js';
    docs/GENERATION_COHERENCE_AUDIT.md:167|12. ORPHAN (producer, no consumer): dead config/data strata — _INCOME_TO_CHAINS, _isolationInfraType, _isolationFoodStress, ITEM_CATEGORIES, SPECIAL_RESOURCES fe

### 5  src/domain/display/parityContract.js  ::  PARITY_EXEMPT
  paths: PARITY_EXEMPT[0].reason | PARITY_EXEMPT[3].reason | PARITY_EXEMPT[5].fact
  ex: "The PDF reads this prose from the AI overlay only. The settlement's own canonical scalar exists, is generator-written, s"
  ex: "AI-narrative path only; absent from the data dossier."
  ex: "tone color hex / bar widths"
  consumers(10):
    src/pdf/lib/viewModelBodySlices.js:340|// PARITY_EXEMPT contract in domain/display/parityContract.js.)
    src/pdf/theme.js:9|* PARITY_EXEMPT) already declares tone COLOR per-surface (web RGB scale vs this print
    tests/pdf/parityContractExemptions.test.js:7|* audited surface is the OTHER half of the contract — PARITY_EXEMPT — which the
    tests/pdf/viewModelDocReference.test.js:7|* about. The doc comment used to point at "display/PARITY_EXEMPT", a path that
    tests/pdf/viewModelParity.test.js:66|// documented PARITY_EXEMPT entry), and this harness asserts it automatically.
    docs/A_PLUS_ROADMAP.md:241|- SHARED_FIELDS registry + PARITY_EXEMPT allowlist (test.each) [high/L]
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:2882|- Corrected: The golden's single-seed value snapshot and Overview-only render-leaf are real but narrow gaps. Two material misstatements: (1) the pinned fie
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:5687|**PROVENANCE:** **NONE NEW.** This surface mints no prose today; it re-exposes the same strings to react-pdf under an explicit *parity by default* cont
    docs/implementation/PACKET_MANIFEST.json:23421|"symbol": "export const PARITY_EXEMPT"
    docs/review-r2/VERIFY_SUBSYSTEMS_RESULTS.json:1156|"reasoning": "Refutation failed on every avenue. (1) Code path: summarySlice (viewModel.js:267-268) and overviewSlice (398-399) both compute arrivalS

### 5  src/domain/npc/characterReadModel.js  ::  CHARACTER_READ_PROVENANCE
  paths: CHARACTER_READ_PROVENANCE.status | CHARACTER_READ_PROVENANCE.ownerRows[0] | CHARACTER_READ_PROVENANCE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (two vocabularies and four frames; nothing here is taste the pen has ruled)"
  ex: "THE THIRTY REASON CLAUSES: one per teaching kind. They are the reader-facing name of every experience the funnel can bin"
  ex: "THE THREE LEVEL PHRASES (a little / notably / above all): the only adverbs the read model owns"
  consumers(1):
    tests/domain/npc/characterReadModel.test.js:51|CHARACTER_READ_PROVENANCE,

### 5  src/domain/npc/knownCharacter.js  ::  KNOWN_CHARACTER_PROVENANCE
  paths: KNOWN_CHARACTER_PROVENANCE.status | KNOWN_CHARACTER_PROVENANCE.ownerRows[0] | KNOWN_CHARACTER_PROVENANCE.ownerRows[2]
  ex: "CANDIDATE, OWNER-UNSIGNED (the belief weight is derived from two existing ladders)"
  ex: "the observer TRUST mapping: carried as a linear read across the spectrum's own span, so neutral lands on exactly one hal"
  ex: "whether a DISPLACEMENT should move belief at all — it is on record and names no band, so today it marks the axis as spok"
  consumers(1):
    tests/domain/npc/knownCharacter.test.js:40|KNOWN_CHARACTER_PROVENANCE,

### 5  src/domain/npc/npcFacets.js  ::  NPC_FACET_REGISTRY
  paths: NPC_FACET_REGISTRY.temperament.consumers[1].read | NPC_FACET_REGISTRY.role.source.reads | NPC_FACET_REGISTRY.role.consumers[1].read
  ex: "the NPC card renders the normalized trait chips (temperament included)"
  ex: "the role/category mix is weighted by institution presence + stressors"
  ex: "the NPC card reads the declared role archetype while preserving the authored office/title"
  consumers(1):
    tests/domain/npc/npcFacetConsumer.walker.test.js:22|NPC_FACET_REGISTRY, EXEMPT_FACETS, FACET_EXEMPT_CEILING,

### 5  src/domain/resourceTerrainCompatibility.js  ::  getCompatibleResources()
  paths: getCompatibleResources()[0].incompatibleReason | getCompatibleResources()[1].incompatibleReason | getCompatibleResources()[4].incompatibleReason
  ex: "Only available in coastal terrain"
  ex: "Only available in coastal or desert terrain"
  ex: "Only available in riverside terrain"
  consumers(13):
    src/components/ConfigurationPanel.jsx:8|import {getCompatibleResources} from '../generators/terrainHelpers';
    src/domain/worldPulse/resourceDynamicsKernel.js:50|import { getCompatibleResources, getTerrainType } from '../../generators/terrainHelpers.js';
    src/domain/worldPulse/steadingTopography.js:59|import { getCompatibleResources } from '../resourceTerrainCompatibility.js';
    src/generators/steps/resolveResources.js:12|import { getCompatibleResources, getDefaultResources } from '../terrainHelpers.js';
    src/generators/terrainHelpers.js:7|// getCompatibleResources MOVED DOWN A LAYER (W-E). The terrain/route legality
    tests/domain/resourceDynamicsKernel.test.js:21|import { getCompatibleResources } from '../../src/generators/terrainHelpers.js';
    tests/generators/resourceAnalysisCoupling.test.js:21|import { getCompatibleResources } from '../../src/generators/terrainHelpers.js';
    tests/generators/terrainOverrideResolution.test.js:76|// filter on RESOURCE_DATA[k].terrain; getCompatibleResources only pre-screens).
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:895|- Evidence: resolveResources.js:116 `const allCompatible = getCompatibleResources(tradeRoute).filter(r => r.compatible)` — terrain param omitted, and terrai
    docs/GENERATION_COHERENCE_AUDIT.md:271|resolveConfig can roll plains/hills/forest/riverside/coastal/mountain/desert (resolveConfig.js:15-18), and resolveResources reserves 2 selection slots for terrai

### 5  src/domain/worldPulse/causeVocabulary.js  ::  CAUSE_CLASSES
  paths: CAUSE_CLASSES[1].label | CAUSE_CLASSES[4].label | CAUSE_CLASSES[6].label
  ex: "a broken supply chain"
  ex: "the strength levied away"
  ex: "the pressure of the war"
  consumers(1):
    docs/PHASE5_CONTENT_ARCHITECTURE.md:23|(causeVocabulary CAUSE_CLASSES) x 6 lifecycle stages (attributed, re-caused, reformed,

### 5  src/domain/worldPulse/envoyChanceMeetingNews.js  ::  CHANCE_MEETING_NEWS_TUNING_PROVENANCE
  paths: CHANCE_MEETING_NEWS_TUNING_PROVENANCE.status | CHANCE_MEETING_NEWS_TUNING_PROVENANCE.ownerRows[0] | CHANCE_MEETING_NEWS_TUNING_PROVENANCE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (ROAD B; enrolled in the tuning register at the §899 landing; the major band at §900)"
  ex: "the recorded meeting's Herald severity, the notable band's own weight"
  ex: "the recorded meeting's Herald score, an integer the feed sorts by"
  consumers(0):

### 5  src/domain/worldPulse/envoyTaskCatalog.js  ::  PARKED_ENVOY_TASKS
  paths: PARKED_ENVOY_TASKS[0].unblockingAct | PARKED_ENVOY_TASKS[1].unblockingAct | PARKED_ENVOY_TASKS[2].claimedFamily
  ex: "mint a casus kind whose producer is the delivery of a demand: a new receipt family, therefore owner-visible"
  ex: "a close reason, or a linking field, that makes a swap distinguishable from two releases: a persisted vocabulary change, "
  ex: "legitimacy / standing displays"
  consumers(1):
    tests/domain/envoyTaskCatalog.test.js:40|PARKED_ENVOY_TASKS,

### 5  src/domain/worldPulse/espionage/infiltrationDepth.js  ::  ENROLLMENT_GATE_DISPOSITION
  paths: ENROLLMENT_GATE_DISPOSITION[0].note | ENROLLMENT_GATE_DISPOSITION[1].note | ENROLLMENT_GATE_DISPOSITION[2].note
  ex: "one live asset per (patron, target) pair. The web enforces it as a candidate SKIP with no word of its own, because it sc"
  ex: "the realm-wide per-patron live-asset cap, verbatim: F8's named clause"
  ex: "the rising-upkeep affordability floor, verbatim: F8's named degradation"
  consumers(2):
    src/domain/worldPulse/corruptionWeb.js:720|// disposition ENROLLMENT_GATE_DISPOSITION already records for L4 in infiltrationDepth.js.
    tests/domain/infiltrationDepth.test.js:32|ENROLLMENT_GATE_DISPOSITION,

### 5  src/domain/worldPulse/espionage/infiltrationDepth.js  ::  INFILTRATION_LEVELS
  paths: INFILTRATION_LEVELS[0].access | INFILTRATION_LEVELS[1].access | INFILTRATION_LEVELS[2].access
  ex: "waypoint hearing on a journey: the host's own beliefs"
  ex: "the target dwell: counting granaries, seeing the place"
  ex: "the gather-or-govern dwell: re-sampling on a ramping catch"
  consumers(4):
    src/domain/worldPulse/espionage/infiltrationDrift.js:95|* The authority is car O3's `./infiltrationDepth.js` (`INFILTRATION_LEVELS`), which
    tests/domain/infiltrationDepth.test.js:34|INFILTRATION_LEVELS,
    tests/domain/infiltrationDrift.test.js:16|* `infiltrationDepth.INFILTRATION_LEVELS`. If any of them moves, the row that leaned
    tests/domain/operationsVoice.test.js:58|INFILTRATION_LEVELS,

### 5  src/domain/worldPulse/espionage/infiltrationDrift.js  ::  INFILTRATION_DRIFT_PROVENANCE
  paths: INFILTRATION_DRIFT_PROVENANCE.status | INFILTRATION_DRIFT_PROVENANCE.ownerRows[1] | INFILTRATION_DRIFT_PROVENANCE.ownerRows[2]
  ex: "CANDIDATE, OWNER-UNSIGNED (breadth is derived from the rung index; no rate is authored here)"
  ex: "whether the ambient milieu term is meant to be capable of a paradigm reversal at all, or whether reversals are events-on"
  ex: "the two custody exits that are admitted are BOTH closed by a human act, and no hold in this engine closes by simulation."
  consumers(1):
    tests/domain/infiltrationDrift.test.js:42|INFILTRATION_DRIFT_PROVENANCE,

### 5  src/domain/worldPulse/faithNews.js  ::  FAITH_KIND_REGISTRY
  paths: FAITH_KIND_REGISTRY[0].pool[0]() | FAITH_KIND_REGISTRY[0].pool[1]() | FAITH_KIND_REGISTRY[0].pool[2]()
  ex: "The last altar of {creed} in {settlement} went dark; none there now keep the rite."
  ex: "The undercroft at {temple} was cleared for grain, and the word is that nobody objected."
  ex: "{settlement}'s roster carries {creed} no longer: not suppressed, not sleeping; gone from the parish entirely."
  consumers(5):
    tests/domain/patronFall.test.js:40|import { FAITH_KIND_REGISTRY, FAITH_KINDS, faithLine } from '../../src/domain/worldPulse/faithNews.js';
    tests/helpers/kindRegistryRoster.js:37|import { FAITH_KIND_REGISTRY } from '../../src/domain/worldPulse/faithNews.js';
    tests/lint/faithKindPools.walker.test.js:56|FAITH_KIND_REGISTRY,
    docs/implementation/PACKET_MANIFEST.json:21836|"_note": "THE REGISTRY LEAF. FAITH_KIND_REGISTRY (one row), the cured hash01 keyed pick copied verbatim from informationNews.js with NO unseeded arm, fai
    docs/implementation/packets/fp/WF-8A.md:64|`FAITH_KIND_REGISTRY` carries ONE row, `faith_last_altar_dark`, significance **`major`**,

### 5  src/domain/worldPulse/npcCirculation.js  ::  rejectionNewsItem()
  paths: rejectionNewsItem().headline | rejectionNewsItem().summary | rejectionNewsItem().reasons[0]
  ex: "A stranger is turned away at the gates of {settlementName}."
  ex: "The story that put them on the road reached {settlementName} before they did."
  ex: "The gate was shut against them over the matter at their last home."
  consumers(1):
    tests/domain/npcCirculation.test.js:34|rejectionNewsItem,

### 5  src/domain/worldPulse/npcVerdictApply.js  ::  verdictHeraldItem()
  paths: verdictHeraldItem().headline | verdictHeraldItem().summary | verdictHeraldItem().reasons[0]
  ex: "{npcName} is imprisoned in {settlementName}."
  ex: "The office is forfeit and the seat at {factionName} stands open."
  ex: "The corruption was proven and the office was forfeit."
  consumers(1):
    tests/domain/npcVerdictApply.test.js:29|verdictHeraldItem,

### 5  src/domain/worldPulse/simulationProfile.js  ::  PROFILE_COERCION_LAWS
  paths: PROFILE_COERCION_LAWS[0].apply().message | PROFILE_COERCION_LAWS[1].apply().message | PROFILE_COERCION_LAWS[2].apply().message
  ex: "That world-progression setting isn’t recognized: defaulting to advancing it yourself."
  ex: "That autonomy setting is not recognised; the realm keeps its current approval custom."
  ex: "The realm does not yet reckon distance. Every settlement is a neighbour."
  consumers(2):
    tests/domain/simulationProfileValidate.test.js:17|PROFILE_COERCION_LAWS,
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:334|- Fix shape: Update the comment block (and the sibling note in the PROFILE_COERCION_LAWS 'progression_unrecognized' law already has the correct M10b wording

### 5  src/domain/worldPulse/temperamentPresets.js  ::  TEMPERAMENTS
  paths: TEMPERAMENTS[0].description | TEMPERAMENTS[1].description | TEMPERAMENTS[2].description
  ex: "A gentle age. The world keeps its own counsel and turns slowly; change waits to be asked for, and the great powers hold "
  ex: "The measured middle. Settlements rise and fall at a believable pace, trade and rumor cross the region, and no single han"
  ex: "A steady realm that must reckon with the year. The seasons turn, the granary is watched, and a hard winter is a hardship"
  consumers(12):
    src/components/temperament/TemperamentPicker.jsx:15|import { TEMPERAMENTS } from '../../domain/worldPulse/temperamentPresets.js';
    src/domain/undercity/jointVocabulary.js:13|* TEMPERAMENTS is the other closed enum every component row carries (§311.8.2, §441.5(b)): the
    tests/domain/temperamentPresets.test.js:10|TEMPERAMENTS, temperamentById, applyTemperament, overridesAreExistingKeysOnly,
    tests/domain/undercityColonization.test.js:25|import { TEMPERAMENTS, isJointKind } from '../../src/domain/undercity/jointVocabulary.js';
    tests/domain/undercityMonotoneComponents.test.js:44|import { JOINT_KINDS, TEMPERAMENTS } from '../../src/domain/undercity/jointVocabulary.js';
    tests/domain/undercitySewerDerivation.test.js:43|import { JOINT_KINDS, TEMPERAMENTS } from '../../src/domain/undercity/jointVocabulary.js';
    tests/domain/undercityStaticComponents.test.js:35|import { JOINT_KINDS, TEMPERAMENTS } from '../../src/domain/undercity/jointVocabulary.js';
    tests/domain/undercityStrataExistence.test.js:28|import { JOINT_KINDS, TEMPERAMENTS, isJointKind, isTemperament } from '../../src/domain/undercity/jointVocabulary.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:1452|- authority: NPC facet values in `settlement.npcs[]`; vocabulary owned by `src/domain/npc/npcBank.js` (NPC_ALIGNMENTS/TEMPERAMENTS/ROLE_ARCHETYPES/GOALS)
    docs/implementation/PACKET_MANIFEST.json:26016|"_note": "THE CLOSED JOINT VOCABULARY AS DATA (J-R2-2): JOINT_KINDS grate|stair|sealed_door|sluice|breach (§311.9.2(iii)) and TEMPERAMENTS STATIC|MONOTON

### 5  src/pdf/variants.js  ::  PDF_VARIANTS
  paths: PDF_VARIANTS.draft_brief.description | PDF_VARIANTS.canon_dossier.description | PDF_VARIANTS.timeline_packet.description
  ex: "Quick prep doc: no timeline, no canon-only sections."
  ex: "Full campaign-ready document with current state and timeline."
  ex: "Lean recap: cover, current state, timeline only. For reviewing what changed."
  consumers(7):
    src/components/settlement/ExportSheet.jsx:23|import { PDF_VARIANTS } from '../../pdf/variants.js';
    src/foundry/journalPages.js:8|* PDF_VARIANTS chapter map via shouldInclude. The Faith & War page is gated by
    src/pdf/SettlementPDF.jsx:45|import { PDF_VARIANTS, shouldInclude, faithChapterVisible } from './variants.js';
    tests/config/entitlementLadder.enforcement.test.js:90|import { PDF_VARIANTS } from '../../src/pdf/variants.js';
    tests/pdf/faithWarGate.test.js:16|import { faithChapterVisible, PDF_VARIANTS } from '../../src/pdf/variants.js';
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:1229|- Evidence: ExportSheet.jsx:66-70 builds cards from Object.entries(PDF_VARIANTS) and only disables timeline_packet on non-canon phase; campaign_state's tim
    docs/briefs/SESSION_FOUNDRY_SCOPE.md:60|chapter list and honors the **same `PDF_VARIANTS` chapter-inclusion map** via

### 4  src/application/commands/adapters/customContentApply.js  ::  customContentCommandSpecs
  paths: customContentCommandSpecs[0].description | customContentCommandSpecs[2].description | customContentCommandSpecs[3].description
  ex: "Create a definition or append one immutable content revision."
  ex: "Restore an archived custom-content definition."
  ex: "Import or update one validated content-pack version atomically."
  consumers(2):
    src/application/commands/standardCommandRegistry.js:17|import { customContentCommandSpecs } from './adapters/customContentApply.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:7215|**Enumeration method**: read `standardCommandRegistry.js` + all 5 adapter files in full (specs enumerated statically: 5 named + 6 via `customContentCommandSpec

### 4  src/components/dossier/proseFieldLabels.js  ::  PROSE_FIELD_LABELS
  paths: PROSE_FIELD_LABELS.settlement[7].label | PROSE_FIELD_LABELS.settlement[11].label | PROSE_FIELD_LABELS.settlement[12].label
  ex: "How the challenge was overcome"
  ex: "Safety, as first surveyed"
  ex: "Guard effectiveness, as first surveyed"
  consumers(4):
    src/components/dossier/WorkbenchProseEditor.jsx:24|import { PROSE_FIELD_LABELS } from './proseFieldLabels.js';
    src/components/settlement/VersionDiffView.jsx:22|*      (PROSE_FIELD_LABELS, the same words the Workbench prose editor offers).
    src/domain/display/stateProse/dmFieldProjection.js:23|* read (QUEUE_WIRED_PROSE_PATHS / PROSE_FIELD_LABELS). It is duplicated here as a pure
    tests/components/workbenchProseEditor.test.jsx:34|import WorkbenchProseEditor, { PROSE_FIELD_LABELS } from '../../src/components/dossier/WorkbenchProseEditor.jsx';

### 4  src/components/settlement/faithPanelModel.js  ::  FALL_SENTENCE
  paths: FALL_SENTENCE.discredited | FALL_SENTENCE.displaced | FALL_SENTENCE.imposed
  ex: "The patron fell. Discredited: the creed lost its rightful claim, and the town let another take the seat."
  ex: "The patron fell. Displaced: the town’s devotion drifted to another creed until the seat changed hands."
  ex: "The patron fell. Imposed: the seat changed hands by decree of those who rule, not by the drift of belief."
  consumers(7):
    src/components/new/tabs/FaithTab.jsx:30|import { FALL_SENTENCE, faithPanelModel, shareBandLabel } from '../../settlement/faithPanelModel.js';
    tests/components/faithPanelModel.test.js:12|import { faithPanelModel, legitimacyBand, pietyTrend, FALL_SENTENCE } from '../../src/components/settlement/faithPanelModel.js';
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:4516|`FALL_SENTENCE` strings behind `patronFallSentence` — one per member of
    docs/implementation/INDEX.md:1075|| CT-3 | [`CT-3`](./packets/content-train/CT-3.md) | **LANDED**; the chair CASes the TE-CT3 landing | Car 3 of the `content` train, and the chapter on why a town keep
    docs/implementation/PACKET_MANIFEST.json:21658|"_note": "THE READ-MODEL. A frozen four-key FALL_SENTENCE table whose key set the acceptance battery asserts EQUALS PATRON_FALL_CAUSES, plus patronFallSe
    docs/implementation/packets/content-train/CT-3.md:167|carries `patronFallSentence` over `FALL_SENTENCE`, four authored strings keyed to
    docs/implementation/packets/fp/WF-1E.md:77|| E4 | `faithPanelModel.js` | the frozen four-key `FALL_SENTENCE` map + `patronFallSentence` derived and returned on the existing closing line | **117 → 124*

### 4  src/design/boundBook.js  ::  SEAM_KINDS
  paths: SEAM_KINDS.letterbox.means | SEAM_KINDS.feather.means | SEAM_KINDS.plate.means
  ex: "art held between hard chrome rails"
  ex: "art resolves into page through a tokenized scrim"
  ex: "art is hard-framed inside the house border ring"
  consumers(3):
    tests/design/boundBookSubstrate.test.js:25|SEAM_KINDS,
    tests/lint/boundBookReport.test.js:52|SEAM_KINDS: ['letterbox', 'feather', 'plate', 'edge'],
    scripts/audit/bound-book-report.mjs:362|const seams = new Set(enumValues(config?.SEAM_KINDS));

### 4  src/domain/ai/suggestedQuestions.js  ::  suggestedQuestions()
  paths: suggestedQuestions()[0] | suggestedQuestions()[1] | suggestedQuestions()[2]
  ex: "What is happening in my world right now?"
  ex: "Which settlements are under the most pressure?"
  ex: "What can I share with my players?"
  consumers(5):
    src/components/AiAnalystPanel.jsx:22|import { suggestedQuestions } from '../domain/ai/suggestedQuestions.js';
    src/components/InterviewPanel.jsx:21|import { suggestedQuestions } from '../domain/ai/suggestedQuestions.js';
    tests/domain/surveyorShell.test.js:18|import { suggestedQuestions } from '../../src/domain/ai/suggestedQuestions.js';
    scripts/.observed-shape-readers-baseline.json:7193|"path": "src/domain/ai/suggestedQuestions.js",
    docs/DESIGN_AI_CHAT_SURFACE.md:129|shell adopts it (AnchorChip from surveyorPanelKit). src/domain/ai/suggestedQuestions.js

### 4  src/domain/content/customSupplyChainPresentation.js  ::  CUSTOM_CHAIN_STATE_PRESENTATION
  paths: CUSTOM_CHAIN_STATE_PRESENTATION.active.summary | CUSTOM_CHAIN_STATE_PRESENTATION.blocked.summary | CUSTOM_CHAIN_STATE_PRESENTATION.ineligible.summary
  ex: "Every reviewed component materialized; trade endpoints are live."
  ex: "This settlement is eligible, but one or more components are unavailable."
  ex: "One or more reviewed components fall outside this settlement tier."
  consumers(0):

### 4  src/domain/density/densityCreateBoundary.js  ::  PIPELINE_REACHERS
  paths: PIPELINE_REACHERS.src/workers/generationRequest.js.why | PIPELINE_REACHERS.src/components/surveyor/ConstructionPanel.jsx.why | PIPELINE_REACHERS.src/workers/customContentPreview.worker.js.why
  ex: "the generation core runs a request whose density law was already minted by the BIRTH caller that built it; minting again"
  ex: "generates a dossier only to compare it against the surveyor's construct constraints and report deviations; the result is"
  ex: "renders a preview of what homebrew content would do to a generation; the output is shown, never persisted."
  consumers(1):
    tests/lint/densityCreateBoundary.walker.test.js:18|* pipeline and is not classified in `PIPELINE_REACHERS` REDS, and a classified

### 4  src/domain/display/stateProse/defenseStateProse.js  ::  DEF7_DARK_POOLS
  paths: DEF7_DARK_POOLS[5] | DEF7_DARK_POOLS[6] | DEF7_DARK_POOLS[7]
  ex: "live band FALLS SHORT of the built profile"
  ex: "live band EXCEEDS the built profile"
  ex: "a contributor with a RECORDED reason, adverse"
  consumers(2):
    src/domain/display/stateProse/dossierMounts.js:244|* `defenseStateProse.js` at `DEF7_DARK_POOLS` and `DEF10_DARK_POOLS`, pinned by desk arms so
    tests/domain/defenseStateProseDesk.test.js:31|DEF7_DARK_POOLS, DEF10_DARK_POOLS, DEF10_FACT_SPOKEN_AT,

### 4  src/domain/display/visibilityAudit.js  ::  default()
  paths: default().checks[0].label | default().checks[2].label | default().checks[3].label
  ex: "Covert mobilizer omitted from player standings"
  ex: "A covert-only world reports no live mobilization to players"
  ex: "No covert smuggling tie in a player trade-pressure read"
  consumers(1982):
    src/App.jsx:106|export default function App() {
    src/AppViews.jsx:123|icons-off via the default (false) IconsContext. */}
    src/application/commands/sessionCommandRuntime.js:7|* default app-session bucket. This runtime itself promises only session replay;
    src/components/AccountMenu.jsx:50|export default function AccountMenu({
    src/components/AccountPage.jsx:53|export default function AccountPage({ onNavigateAdmin, routeSection, routeMessageId }) {
    src/components/AdminPanel.jsx:61|export default function AdminPanel({ onBack }) {
    src/components/AiAnalystPanel.jsx:34|export default function AiAnalystPanel({ open = false, onClose, initialQuestion = '' }) {
    src/components/AnonTierTeaser.jsx:19|export default function AnonTierTeaser({ onSignIn }) {
    src/components/AuthModal.jsx:22|export default function AuthModal({ onClose, isMobile = false }) {
    src/components/BuyThisDossier.jsx:69|export default function BuyThisDossier({ settlement, saveId = null, onSignIn, onSaveFirst, onNavigate, size = 'sm' }) {

### 4  src/domain/display/warAndRoadNames.js  ::  ROUTE_NAME_POOLS
  paths: ROUTE_NAME_POOLS.land.addressed[3]() | ROUTE_NAME_POOLS.land.paired[1]() | ROUTE_NAME_POOLS.water.addressed[3]()
  ex: "the carters' road to {dest}"
  ex: "the road between {a} and {b}"
  ex: "the water road to {dest}"
  consumers(1):
    tests/domain/display/warAndRoadNames.test.js:7|ROUTE_NAME_POOLS,

### 4  src/domain/factionRename.js  ::  NPC_RENAME_SURFACES
  paths: NPC_RENAME_SURFACES[0].why | NPC_RENAME_SURFACES[1].why | NPC_RENAME_SURFACES[2].why
  ex: "the character record's own display name at the canonical home"
  ex: "the grouping copy the dossier renders as a faction member chip; a separate object on every reloaded save"
  ex: "the first end of an NPC relationship edge, joined by display name"
  consumers(2):
    src/store/settlementRenameHelpers.js:21|* denominator declares — FACTION_RENAME_SURFACES and NPC_RENAME_SURFACES in
    tests/domain/npcRename.test.js:22|* test. Every shape it finds must be covered by NPC_RENAME_SURFACES (it cascades)

### 4  src/domain/instantWorld/genesisDiplomacy.js  ::  GENESIS_RELATION_CAUSES
  paths: GENESIS_RELATION_CAUSES.tier_dominance.phrase | GENESIS_RELATION_CAUSES.mutual_defence.phrase | GENESIS_RELATION_CAUSES.market_exchange.phrase
  ex: "the older seat has long overshadowed the younger"
  ex: "both seats agreed to stand together from the first"
  ex: "goods have moved between the two since the founding"
  consumers(1):
    tests/lib/instantWorld/genesisDiplomacy.test.js:19|GENESIS_RELATION_CAUSES,

### 4  src/domain/npc/characterConsumers.js  ::  CONSUMER_SEAM_PROVENANCE
  paths: CONSUMER_SEAM_PROVENANCE.status | CONSUMER_SEAM_PROVENANCE.ownerRows[1] | CONSUMER_SEAM_PROVENANCE.ownerRows[2]
  ex: "CANDIDATE, OWNER-UNSIGNED (every magnitude below is derived from the spectrum, none authored)"
  ex: "R6's centre gives its three terms an EQUAL derived share. A signed weighting (a desperate man's nerve counting for more "
  ex: "R8's breadth runs from one band's share of the window to half of it. The two ends are derived from the spectrum; a signe"
  consumers(1):
    tests/domain/npc/characterConsumers.test.js:27|CONSUMER_SEAM_PROVENANCE,

### 4  src/domain/relationships/canonicalRelationship.js  ::  RELATIONSHIP_SELECTIONS
  paths: RELATIONSHIP_SELECTIONS[7].label | RELATIONSHIP_SELECTIONS[8].label | RELATIONSHIP_SELECTIONS[9].label
  ex: "Current settlement is patron"
  ex: "Current settlement is client"
  ex: "Current settlement is overlord"
  consumers(13):
    src/components/settlement/eventComposer/EventComposerLinkNeighbourField.jsx:17|import { RELATIONSHIP_SELECTIONS, relationshipDefinition, directionalRelationshipLabel } from '../../../domain/relationsh
    src/components/settlementDetail/SettlementDetailLinkNeighbourCard.jsx:2|import { RELATIONSHIP_SELECTIONS } from '../../domain/relationships/canonicalRelationship.js';
    src/domain/instantWorld/genesisDiplomacy.js:54|RELATIONSHIP_SELECTIONS,
    src/domain/roads/thirdPartyRansom.js:72|// is in RELATIONSHIP_SELECTIONS, so no author can mint one. `cordial` remains load-bearing as
    src/domain/worldPulse/conquestDoctrineStage.js:107|// RELATIONSHIP_SELECTIONS. The survivors are reachable: 'allied' from the authoring
    src/domain/worldPulse/convergence.js:800|// RELATIONSHIP_SELECTIONS offers `allied`, never `ally`. The set therefore admitted a value
    tests/domain/convergence.test.js:515|// `alliance` and `allies` ALL onto `allied`, and RELATIONSHIP_SELECTIONS offers `allied`
    tests/lib/instantWorld/genesisDiplomacy.test.js:22|import { RELATIONSHIP_SELECTIONS } from '../../../src/domain/relationships/canonicalRelationship.js';
    tests/lint/vocabularyTotality.walker.test.js:221|* `RELATIONSHIP_SELECTIONS` offers it — OR declared EXPECTED-DEAD with a written reason.
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:902|- Evidence: generatePower.js:41 `const ADVERSARIAL_REL = new Set(['hostile_rival', 'Hostile rival', 'cold_war', 'Cold war', 'tense']);` — but the live vocab

### 4  src/domain/state/bands.js  ::  BAND_HINT
  paths: BAND_HINT.Stable | BAND_HINT.Strained | BAND_HINT.Vulnerable
  ex: "Healthy. Shocks are absorbed without crisis."
  ex: "Functional but stretched. A bad season would hurt."
  ex: "One real shock away from failure."
  consumers(9):
    src/components/settlement/SystemStateBar.jsx:13|import { BAND_COLOR, BAND_HINT, dimensionPolarity } from '../../domain/state/bands.js';
    src/domain/compendium/bandLadders.js:23|*   - Stability .... glossary 'stability-band' (state/bands.js BAND_HINT).
    src/domain/display/glossary.js:12|* already carries the interpretation (BAND_HINT, a verb's label, a dial's
    src/pdf/sections/SystemStateSnapshot.jsx:16|import { BAND_COLOR, BAND_HINT, dimensionPolarity } from '../../domain/state/bands.js';
    tests/components/surveyorGlossary.test.jsx:41|// The definition IS the code BAND_HINT (never invented).
    tests/docs/glossaryFreshness.test.js:19|import { BAND_HINT } from '../../src/domain/state/bands.js';
    tests/ui/compendiumBandLadders.test.jsx:15|*      table exactly. A new band added to PROSPERITY_TIERS / BAND_HINT /
    docs/CAPABILITY_REMEDIATION_PLAN.md:20|> - **R-5b batch 2 · item #22 — ReadSystemStateBar REMOUNTED, not deleted (2026-07-27 ~16:30, UNCOMMITTED increment; resumed after the prior lane died on the ses
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:9642|- notes/gaps: Why surface exists (click-to-open drivers/risks disclosure, SystemStateBar.jsx:128-153) but is reachable only in edit mode since the read twin is

### 4  src/domain/state/compareSystemState.js  ::  compareSystemState()
  paths: compareSystemState()[0].explanation | compareSystemState()[1].explanation | compareSystemState()[2].explanation
  ex: "Resilience fell slightly; pressure increased"
  ex: "Volatility fell slightly; pressure increased"
  ex: "External Threat fell slightly; pressure increased"
  consumers(20):
    src/components/settlement/VersionDiffView.jsx:14|*      composes compareSystemState / compareCausalState / compareCapacityStates
    src/domain/capacityModel.js:1074|// by absolute ratio change descending. Mirrors compareSystemState /
    src/domain/causalState.js:1553|// compareSystemState so consumers can render the two side-by-side.
    src/domain/counterfactual.js:51|import { compareSystemState } from './state/compareSystemState.js';
    src/domain/events/batch.js:34|import { compareSystemState } from '../state/compareSystemState.js';
    src/domain/events/eventPipeline.js:22|*   6. Compute deltas at both layers (compareSystemState +
    src/domain/regenerationDelta.js:28|import { compareSystemState } from './state/compareSystemState.js';
    src/domain/state/bands.js:43|* separate places (compareSystemState's local POLARITY, SystemStateBar's
    tests/domain/bandPolarity.test.js:37|import { compareSystemState } from '../../src/domain/state/compareSystemState.js';
    tests/domain/systemState.test.js:14|import { compareSystemState } from '../../src/domain/state/compareSystemState.js';

### 4  src/domain/worldPulse/brokerageServicesRules.js  ::  INTERCEPT_BAND_REASONS
  paths: INTERCEPT_BAND_REASONS.legible | INTERCEPT_BAND_REASONS.clouded | INTERCEPT_BAND_REASONS.fogged
  ex: "The town keeps no secrets worth the name, so the whole of the traffic read plainly."
  ex: "Gates half-raised cost the watcher the subtlest of it, and left the rest standing."
  ex: "Behind closed gates only the bare shape survived: that a contract exists at all."
  consumers(1):
    tests/domain/brokerageIntercept.test.js:54|INTERCEPT_BAND_REASONS,

### 4  src/domain/worldPulse/demographicsPushPull.js  ::  pushScoreOf()
  paths: pushScoreOf().drivers[0].guard | pushScoreOf().drivers[1].guard | pushScoreOf().drivers[2].guard
  ex: "the walls are the wall"
  ex: "a threat stands at the gate"
  ex: "the fields can be counted"
  consumers(2):
    src/domain/worldPulse/demographicsMigration.js:85|pushScoreOf,
    tests/domain/demographicsMigration.test.js:63|pushScoreOf,

### 4  src/domain/worldPulse/envoyTaskCatalog.js  ::  ENVOY_TASK_PROVENANCE
  paths: ENVOY_TASK_PROVENANCE.ownerRows[0] | ENVOY_TASK_PROVENANCE.ownerRows[2] | ENVOY_TASK_PROVENANCE.ownerRows[4]
  ex: "the four admitted rows are receipt-verified, not taste, but WHICH of them a court may charter, and at what cadence, is t"
  ex: "tribute: the same fork one row along, and it was hidden inside a slash in §4's prose"
  ex: "the four method words and their nerve fits. The fits are derived from the volume's own sentence about threatening coward"
  consumers(1):
    tests/domain/envoyTaskCatalog.test.js:36|ENVOY_TASK_PROVENANCE,

### 4  src/domain/worldPulse/envoyTaskCatalog.js  ::  NEGOTIATION_METHODS
  paths: NEGOTIATION_METHODS[0].note | NEGOTIATION_METHODS[1].note | NEGOTIATION_METHODS[2].note
  ex: "a court that thinks well of its own nerve will spend to keep the reputation; a frightened one hears the appeal as a dema"
  ex: "the honest middle. It misfires on nobody, which is exactly why it is not free: it reads the smallest amount about the ot"
  ex: "the volume's own line: threaten the cowardly court, never the brave one. Pressed against a dominant court it hardens the"
  consumers(1):
    tests/domain/envoyTaskCatalog.test.js:38|NEGOTIATION_METHODS,

### 4  src/domain/worldPulse/espionage/infiltrationDepth.js  ::  STANDING_COVER_GATE
  paths: STANDING_COVER_GATE.owes[0] | STANDING_COVER_GATE.owes[2] | STANDING_COVER_GATE.candidateShapes[1].cost
  ex: "a persisted cover schema, owner-admitted"
  ex: "an undo/restore round-trip for the new key space"
  ex: "the cover can assert only what is RECEIPTED"
  consumers(1):
    tests/domain/infiltrationDepth.test.js:40|STANDING_COVER_GATE,

### 4  src/domain/worldPulse/index.js  ::  EXEMPT_STRESSOR_TYPES
  paths: EXEMPT_STRESSOR_TYPES.mass_migration | EXEMPT_STRESSOR_TYPES.infiltration | EXEMPT_STRESSOR_TYPES.criminal_corridor
  ex: "A migration is consequence-adjacent (it is usually the OUTCOME of a famine/war arc, not an independent birth)."
  ex: "Infiltration is a covert, ambiguous seed: safer un-governed than mis-paced."
  ex: "A criminal corridor is a slow structural condition, not a spontaneous major-arc birth."
  consumers(3):
    src/domain/worldPulse/decisionTier.js:250|export const EXEMPT_STRESSOR_TYPES = Object.freeze({
    tests/domain/dramaClassRegistry.contract.test.js:11|*      registry as stressor_birth_<key> OR is on the EXEMPT_STRESSOR_TYPES allow-list
    docs/PHASE55_E0_TEMPO_GOVERNOR_PLAN.md:179|`stressorsCore.js`) either appears in the registry or is on an explicit `EXEMPT_STRESSOR_TYPES` allow-list

### 4  src/domain/worldPulse/institutionStatusModel.js  ::  INSTITUTION_CAUSE_CURES
  paths: INSTITUTION_CAUSE_CURES.supply_shortage | INSTITUTION_CAUSE_CURES.corruption_exposed | INSTITUTION_CAUSE_CURES.damage
  ex: "the road reopens or the stores refill"
  ex: "the house is cleaned out and the scandal closes"
  ex: "the repairs are finished"
  consumers(2):
    src/domain/worldPulse/institutionStatusLifecycle.js:66|INSTITUTION_CAUSE_CURES,
    tests/domain/institutionStatusModel.test.js:26|INSTITUTION_CAUSE_CURES,

### 4  src/domain/worldPulse/magicRegimeModel.js  ::  MAGIC_REGIME_LABELS
  paths: MAGIC_REGIME_LABELS.subsistence | MAGIC_REGIME_LABELS.funded | MAGIC_REGIME_LABELS.patronized
  ex: "what the practitioners can do unpaid"
  ex: "what the town itself pays for"
  ex: "what a patron underwrites"
  consumers(1):
    src/domain/worldPulse/magicRegimeLifecycle.js:60|MAGIC_REGIME_LABELS,

### 4  src/domain/worldPulse/npcCirculation.js  ::  arrivalNewsItem()
  paths: arrivalNewsItem().headline | arrivalNewsItem().summary | arrivalNewsItem().reasons[0]
  ex: "A stranger takes up residence in {settlementName}."
  ex: "They enter {factionName} at its lowest rung."
  ex: "{factionName} had a seat open at the bottom."
  consumers(1):
    tests/domain/npcCirculation.test.js:24|arrivalNewsItem,

### 4  src/domain/worldPulse/routeNetworkCharterEvents.js  ::  charterHeraldItem()
  paths: charterHeraldItem().headline | charterHeraldItem().summary | charterHeraldItem().reasons[0]
  ex: "A road is chartered between {a} and {b}"
  ex: "The way opens as a track, and it will be as good a road as it is walked."
  ex: "The realm cannot close this material loop by any other road."
  consumers(1):
    tests/domain/routeNetworkCharter.test.js:56|charterHeraldItem,

### 4  src/domain/worldPulse/routeNetworkDecay.js  ::  gradeStepHeraldItem()
  paths: gradeStepHeraldItem().headline | gradeStepHeraldItem().summary | gradeStepHeraldItem().reasons[0]
  ex: "The road between {a} and {b} is abandoned"
  ex: "The way is not struck from the map. It is only overgrown, and it remembers."
  ex: "Nothing has walked the way in {quietTicks} weeks."
  consumers(2):
    tests/domain/routeNetworkDecay.test.js:50|gradeStepHeraldItem,
    docs/COMPREHENSIVE_REVIEW_2026-08-01.md:162|- **Finding:** sweepRouteDecay applies a grade step for verdict 'garrison_floor' (line 651: `verdict.verdict === 'garrison_floor' || verdict.steps`), and wi

### 4  src/domain/worldPulse/warCoalitionRefusal.js  ::  buildCoalitionRefusalOutcome()
  paths: buildCoalitionRefusalOutcome().headline | buildCoalitionRefusalOutcome().summary | buildCoalitionRefusalOutcome().reasons[0]
  ex: "{coalitionNameFor} refuses {coalitionNameFor}'s call"
  ex: "{coalitionNameFor} will not send its army against {coalitionNameFor}; the refusal now stands between the allied courts."
  ex: "The court weighed the wider retaliation that a march against {coalitionNameFor} could awaken."
  consumers(2):
    src/domain/worldPulse/warDeployment.js:57|import { buildCoalitionRefusalOutcome } from './warCoalitionRefusal.js';
    tests/domain/warDeploymentLeaves.test.js:41|buildCoalitionRefusalOutcome,

### 3  src/components/new/economyDeskRead.js  ::  economyDeskRead()
  paths: economyDeskRead().foodSecurityRung.sentence | economyDeskRead().exportPosture.sentence | economyDeskRead().catalogStanding.sentence
  ex: "The town is meant to be shut and the market is not empty. Nobody local will explain that, and everybody local has notice"
  ex: "The town's living is the road. Goods come, goods go, and the town takes its part of both."
  ex: "The town can answer what a town this size ought to answer, and does not think about it further."
  consumers(8):
    src/components/new/tabs/DailyLifeTab.jsx:12|import { economyDeskRead } from '../economyDeskRead.js';
    src/components/new/tabs/EconomicsTab.jsx:24|import { economyDeskRead } from '../economyDeskRead.js'; // the desk's ONE caller + the §885.3 gate (walker ARM 2)
    src/components/new/tabs/ResourcesTab.jsx:6|import { economyDeskRead } from '../economyDeskRead.js';
    src/components/new/tabs/ServicesTab.jsx:12|import { economyDeskRead } from '../economyDeskRead.js';
    src/domain/display/stateProse/dossierMounts.js:251|* therefore moved out of `EconomicsTab.jsx` into `components/new/economyDeskRead.js`,
    tests/lint/.tuning-inventory.json:737|"src/components/new/economyDeskRead.js"
    tests/lint/economyReadModelCoverage.walker.test.js:184|'src/components/new/economyDeskRead.js':
    scripts/.observed-shape-readers-baseline.json:5198|"path": "src/components/new/economyDeskRead.js",

### 3  src/data/sampleSettlements.js  ::  SAMPLE_SETTLEMENTS
  paths: SAMPLE_SETTLEMENTS[0].teaser | SAMPLE_SETTLEMENTS[1].teaser | SAMPLE_SETTLEMENTS[2].teaser
  ex: "A rain-grey harbour town where the council and the temple have stopped speaking, and the merchant guilds quietly profit "
  ex: "A mountain city built on iron, ruled by its guild-masters, whose mines now give up too little ore."
  ex: "A forest village a week from the nearest road, where every cottage bars its door twice and the hunter's lodge kills more"
  consumers(4):
    src/components/generate/FoundingWorlds.jsx:25|import { SAMPLE_SETTLEMENTS, forkSeedFor } from '../../data/sampleSettlements.js';
    src/components/settlements/SampleDashboard.jsx:4|import { SAMPLE_SETTLEMENTS } from '../../data/sampleSettlements.js';
    tests/components/foundingWorlds.test.jsx:42|const { SAMPLE_SETTLEMENTS } = await import('../../src/data/sampleSettlements.js');
    tests/data/sampleSettlements.test.js:12|import { SAMPLE_SETTLEMENTS, forkSeedFor } from '../../src/data/sampleSettlements.js';

### 3  src/design/organic/index.js  ::  MOTION_EASE
  paths: MOTION_EASE.ink | MOTION_EASE.settle | MOTION_EASE.press
  ex: "cubic-bezier(0.33, 0, 0.2, 1)"
  ex: "cubic-bezier(0.22, 1, 0.36, 1)"
  ex: "cubic-bezier(0.4, 0, 0.6, 1)"
  consumers(2):
    src/design/organic/motion.js:28|export const MOTION_EASE = MOTION_PRIMITIVES.easing;
    tests/design/boundBookSubstrate.test.js:30|import { MOTION_DURATION, MOTION_EASE } from '../../src/design/organic/motion.js';

### 3  src/domain/aiGrounding.js  ::  assemblePromptSections()
  paths: assemblePromptSections().system | assemblePromptSections().developer | assemblePromptSections().format
  ex: "You are a structural narrator. The dossier below is a single source of truth — every name, faction, institution, number,"
  ex: "Voice: confident, unhurried. Specific over generic. Tie every descriptive line to a fact already present in the dossier."
  ex: "Output MUST preserve every proper noun from the dossier and every numerical / categorical fact. Restructure and polish f"
  consumers(4):
    tests/domain/aiGrounding.test.js:28|assemblePromptSections,
    tests/domain/userEdits.grounding.test.js:11|*   - assemblePromptSections puts the locked entity list inside the
    tests/edgeFunctions/aiGroundingBundle.freshness.test.js:89|'assemblePromptSections',
    tests/edgeFunctions/aiGroundingContract.test.js:34|assemblePromptSections,

### 3  src/domain/customContentSchema.js  ::  DEITY_TIER
  paths: DEITY_TIER[0].label | DEITY_TIER[1].label | DEITY_TIER[2].label
  ex: "Major — a pillar of the pantheon"
  ex: "Minor — a lesser god"
  ex: "Cult — a fringe or secret following"
  consumers(5):
    src/components/compendium/CustomContentEditor.jsx:13|DEITY_TIER,
    src/components/settlement/DeityAssignmentPanel.jsx:38|DEITY_TIER,
    docs/GAME_GRADE_AUDIT.md:312|- **V-CUS3** src/components/settlement/DeityAssignmentPanel.jsx:56-62 (snapLine, in the patron/cult summary and lapsed read-only view) — Joins raw axis keys with ' · ' (sn
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:8144|- authority: `customContent.deities` (account library or campaign pinned binding — activeCustomContentContext); schema src/domain/customContentSchema.js:154-19
    docs/SUBSYSTEM_INTEGRATION_PLAN.md:325|- **Files:** `customContentSchema.js` (frozen `DEITY_ALIGNMENT`/`DEITY_TEMPER`/`DEITY_TIER`

### 3  src/domain/display/forceComposition.js  ::  FORCE_COMPOSITION_TABLES
  paths: FORCE_COMPOSITION_TABLES.UNIT_WORDS.heavy_horse.one | FORCE_COMPOSITION_TABLES.UNIT_WORDS.heavy_horse.many | FORCE_COMPOSITION_TABLES.UNIT_WORDS.war_mages.many
  ex: "troop of heavy horse"
  ex: "troops of heavy horse"
  ex: "circles of war mages"
  consumers(1):
    tests/domain/display/forceComposition.test.js:5|FORCE_COMPOSITION_TABLES,

### 3  src/domain/display/hegemonyRead.js  ::  HEGEMONY_TUNING
  paths: HEGEMONY_TUNING.SHARE_BANDS[0].phrase | HEGEMONY_TUNING.SHARE_BANDS[1].phrase | HEGEMONY_TUNING.SHARE_BANDS[2].phrase
  ex: "commands the lion’s share of the realm’s strength"
  ex: "holds a great part of the realm’s strength"
  ex: "holds a regional preponderance"
  consumers(5):
    src/domain/rulingPowerSeat.js:86|* The floors idiom is `HEGEMONY_TUNING.SHARE_BANDS` verbatim (descending floors,
    src/domain/worldPulse/hegemony.js:7|* / HEGEMONY_TUNING / SUBORDINATING_TERM_TYPES) is unchanged; the display wrapper's tests
    tests/domain/hegemonyRead.test.js:17|HEGEMONY_TUNING,
    tests/lint/.tuning-inventory.json:4272|"src/domain/worldPulse/hegemony.js#HEGEMONY_TUNING": {
    tests/lint/.tuning-register.json:1204|"src/domain/worldPulse/hegemony.js#HEGEMONY_TUNING": {

### 3  src/domain/display/stateProse/defenseStateProse.js  ::  defenseForcesProse()
  paths: defenseForcesProse().force.sentence | defenseForcesProse().force.provenance.poolKey | defenseForcesProse().arcane.sentence
  ex: "The town would defend itself as a crowd defends itself, which is to say briefly and in several directions at once."
  ex: "NO organized force at all"
  ex: "The town has no answer to anything it cannot see, and is unprepared rather than protected in that respect."
  consumers(2):
    src/components/new/tabs/DefenseTab.jsx:11|import { defenseCriminalProse, defenseForcesProse, defenseMilitaryStatusProse, defensePostureProse, defenseStateProse, defenseSupportingProse, defenseThreatPr
    tests/domain/defenseStateProseDesk.test.js:19|defenseForcesProse, defenseStateProse, defenseThreatProse, disasterRowPoolKey,

### 3  src/domain/generationContentProfile.js  ::  GENERATION_CONTENT_PROFILES
  paths: GENERATION_CONTENT_PROFILES.heroic.description | GENERATION_CONTENT_PROFILES.grounded.description | GENERATION_CONTENT_PROFILES.grim.description
  ex: "Adventure, danger, and political conflict without generated trafficking, slavery, torture, or hard-drug themes."
  ex: "Serious conflict and social pressure without generated trafficking, slavery, or torture; vice and narcotics may appear."
  ex: "The complete governed dark-theme vocabulary, including systems of coercion and exploitation."
  consumers(1):
    scripts/audit/generation-certification-soak.mjs:35|import { GENERATION_CONTENT_PROFILES } from '../../src/domain/generationContentProfile.js';

### 3  src/domain/historyBeats.js  ::  LIKELY_FUTURE_BEAT_TEXT
  paths: LIKELY_FUTURE_BEAT_TEXT.crisis | LIKELY_FUTURE_BEAT_TEXT.test | LIKELY_FUTURE_BEAT_TEXT.continuity
  ex: "A crisis is imminent if no one intervenes."
  ex: "The next year will test whoever holds the chair."
  ex: "Continuity, with the usual slow erosion of any settlement."
  consumers(1):
    tests/domain/historyBeats.test.js:16|LIKELY_FUTURE_BEAT_TEXT,

### 3  src/domain/instantWorld/worldPlan.js  ::  REALM_SIZES
  paths: REALM_SIZES.small.blurb | REALM_SIZES.medium.blurb | REALM_SIZES.large.blurb
  ex: "A compact frontier: one market town and its hamlets."
  ex: "A settled region anchored by a walled city."
  ex: "A whole realm under a great metropolis."
  consumers(4):
    src/components/instant/InstantWorldEntry.jsx:25|import { REALM_SIZES, TONES, MAP_KINDS, MAGIC_CHOICES, DEFAULT_REALM_SIZE, DEFAULT_TONE, DEFAULT_MAP_KIND, DEFAULT_MAGIC, isMagicChoice } from '../../do
    src/domain/construct/configVocabulary.js:22|import { isRealmSize, isTone, isMapKind, REALM_SIZES, TONES, MAP_KINDS } from '../instantWorld/worldPlan.js';
    tests/lib/instantWorld/composeInstantWorld.test.js:14|import { REALM_SIZES, MAP_KINDS, TONES, deriveWorldPlan } from '../../../src/domain/instantWorld/worldPlan.js';
    tests/lib/instantWorld/planLaw.test.js:15|REALM_SIZES,

### 3  src/domain/instantWorld/worldPlan.js  ::  TONES
  paths: TONES[0].blurb | TONES[1].blurb | TONES[2].blurb
  ex: "Ties hold; change is local and slow."
  ex: "A balanced, believable region."
  ex: "War, faith, and upheaval run hot."
  consumers(13):
    src/components/AccountMenu.jsx:111|// ⚠️ THE CHIP'S TONES ARE A FUNCTION OF THE HEADER'S GROUND, AND THE GROUND HAS
    src/components/instant/InstantWorldEntry.jsx:25|import { REALM_SIZES, TONES, MAP_KINDS, MAGIC_CHOICES, DEFAULT_REALM_SIZE, DEFAULT_TONE, DEFAULT_MAP_KIND, DEFAULT_MAGIC, isMagicChoice } from '../../do
    src/components/primitives/Badge.jsx:9|const TONES = {
    src/components/primitives/IconButton.jsx:45|const TONES = {
    src/components/primitives/Toast.jsx:6|const TONES = {
    src/components/theme.js:868|// ⚠️ THE NAMES CHANGED WITH THE TONES on purpose. SHAFT_GREEN/SHAFT_SLATE named a
    src/design/organic/lampTones.js:8|* as a label even on white) but with LAMP TONES: warm, luminous successors that
    src/domain/construct/configVocabulary.js:22|import { isRealmSize, isTone, isMapKind, REALM_SIZES, TONES, MAP_KINDS } from '../instantWorld/worldPlan.js';
    src/lib/foundersHall.js:114|* The ring TONES are named design tokens (founderRingDeveloper /
    tests/components/designPrimitives.test.jsx:132|expect(el.style.getPropertyValue('--sf-btn-hover-bg'), `tone ${tone} applies its TONES.hover`).toBeTruthy();

### 3  src/domain/npc/acceptanceCharacterReads.js  ::  ACCEPTANCE_READ_PROVENANCE
  paths: ACCEPTANCE_READ_PROVENANCE.status | ACCEPTANCE_READ_PROVENANCE.ownerRows[0] | ACCEPTANCE_READ_PROVENANCE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (this leaf authors no magnitude; every number it returns is the register's)"
  ex: "whether a court that holds NO known chart should vet as though the man were ordinary, or refuse to let character speak a"
  ex: "the §802 R1 willingness door is NOT wired here and its symbol is deliberately unnamed in this file: it has landed nowher"
  consumers(2):
    tests/domain/npc/acceptanceCharacterReads.test.js:29|ACCEPTANCE_READ_PROVENANCE,
    tests/domain/npc/characterDrift.test.js:814|['acceptanceCharacterRead', 'vettingInputFor', 'ACCEPTANCE_READ_TERMS', 'ACCEPTANCE_READ_PROVENANCE'],

### 3  src/domain/npc/characterEdit.js  ::  CHART_EDIT_PROVENANCE
  paths: CHART_EDIT_PROVENANCE.status | CHART_EDIT_PROVENANCE.ownerRows[1] | CHART_EDIT_PROVENANCE.consumers
  ex: "STRUCTURE DECIDED; the axis roster itself is owner-taste and UNSIGNED"
  ex: "whether clearing a chart back to all-neutral should un-canon the NPC. This writer NEVER clears the marker: it cannot kno"
  ex: "NONE by design; the mount is a later car"
  consumers(1):
    tests/domain/npc/characterEdit.test.js:26|CHART_EDIT_PROVENANCE,

### 3  src/domain/npc/livedExperienceFunnel.js  ::  FUNNEL_TUNING
  paths: FUNNEL_TUNING.status | FUNNEL_TUNING.ownerRows[0] | FUNNEL_TUNING.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (every figure DERIVED from the floor or from the pack ladder)"
  ex: "the three pull magnitudes (derived: faint = the floor, heavy = one full band)"
  ex: "the per-family learn ladder: carried as a mechanical tercile over the pack order until real rates are signed"
  consumers(6):
    src/domain/worldPulse/envoyChanceMeeting.js:169|* THE TUNING REGISTER — every row a DRAFT, `signedBy: null`, on the `FUNNEL_TUNING` idiom.
    tests/domain/infiltrationDrift.test.js:74|FUNNEL_TUNING,
    tests/domain/npc/livedExperienceFunnel.test.js:47|FUNNEL_TUNING,
    tests/domain/npc/livedExperienceLoops.test.js:41|FUNNEL_TUNING,
    tests/lint/.tuning-inventory.json:881|"src/domain/npc/livedExperienceFunnel.js#FUNNEL_TUNING": {
    tests/lint/.tuning-register.json:315|"src/domain/npc/livedExperienceFunnel.js#FUNNEL_TUNING": {

### 3  src/domain/npc/livedExperienceSources.js  ::  SOURCE_PROVENANCE
  paths: SOURCE_PROVENANCE.status | SOURCE_PROVENANCE.ownerRows[3] | SOURCE_PROVENANCE.consumers
  ex: "CANDIDATE, OWNER-UNSIGNED (the registry is measurement; the blocks are a chair row)"
  ex: "whether an affiliation-plane realm event should teach EVERY roster member of a settlement or only its seated/important o"
  ex: "NONE in production by design; the pulse call site is car L5's"
  consumers(1):
    tests/domain/npc/livedExperienceSources.test.js:46|SOURCE_PROVENANCE,

### 3  src/domain/npc/paradigmAxisCatalog.js  ::  CATALOG_PROVENANCE
  paths: CATALOG_PROVENANCE.status | CATALOG_PROVENANCE.ruling | CATALOG_PROVENANCE.consumers
  ex: "CANDIDATE, OWNER-UNSIGNED (words and leans are drafts; legacy columns are measured)"
  ex: "ODQ 806 F1/F2: consolidation, not derivation; corruptibility is per-word"
  ex: "NONE by design; car L1 lands dark, car L5 re-points consumers"
  consumers(3):
    src/domain/worldPulse/deityFlaws.js:49|* paradigmAxisCatalog CATALOG_PROVENANCE idiom).
    src/domain/worldPulse/faithTuningSurface.js:180|* DEITY_FLAW_PROVENANCE / CATALOG_PROVENANCE idiom).
    tests/domain/npc/paradigmAxisCatalog.test.js:31|CATALOG_PROVENANCE,

### 3  src/domain/regenerationMode.js  ::  MODE_DESCRIPTIONS
  paths: MODE_DESCRIPTIONS.nudge | MODE_DESCRIPTIONS.rebalance | MODE_DESCRIPTIONS.reforge
  ex: "Preserve most. Minor cosmetic / narrative variation only."
  ex: "Preserve user canon + locked entities. Recalculate affected subsystems."
  ex: "Keep only hard anchors (seed, name, tier). Reroll almost everything."
  consumers(1):
    tests/domain/regenerationMode.test.js:8|MODE_DESCRIPTIONS,

### 3  src/domain/simulationSpine.js  ::  SPINE_RUNGS
  paths: SPINE_RUNGS[3].frame | SPINE_RUNGS[4].frame | SPINE_RUNGS[6].frame
  ex: "Its real power lies with"
  ex: "It is currently strained by"
  ex: "Its likely future is"
  consumers(3):
    src/domain/historyBeats.js:377|* frame word ("Its likely future is") is prepended by SPINE_RUNGS while a beat
    tests/domain/simulationSpine.test.js:35|SPINE_RUNGS,
    scripts/mutation-coverage-manifest.json:319|"what": "Standing plant restores the frame word INSIDE deriveStrainedBy, which is the exact shape that shipped \"It is currently strained by / Strained by u

### 3  src/domain/spatial/cohesionWeave.js  ::  ECONOMIC_BASE_LENS
  paths: ECONOMIC_BASE_LENS.agrarian.warStyle | ECONOMIC_BASE_LENS.trade_hub.warStyle | ECONOMIC_BASE_LENS.craft.warStyle
  ex: "seasonal army (the harvest imperative bites)"
  ex: "route wars; folds to embargoes"
  ex: "limited; guards its supply"
  consumers(1):
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2189|**RECEIPT:** `src/domain/spatial/cohesionWeave.js:130-197` (`ECONOMIC_BASES`, `RULING_POWERS`, `ECONOMIC_BASE_LENS`, `RULING_POWER_LENS`, `ARCHETYPE_TO

### 3  src/domain/traditions/politics.js  ::  reexpressed()
  paths: reexpressed().expression.trappings[0] | reexpressed().expression.trappings[1] | reexpressed().expression.epithet
  ex: "a whole ox turned on the spit"
  ex: "the guild ovens working through the night"
  ex: "older than any charter the town can show"
  consumers(3):
    src/domain/traditions/relations.js:42|import { reexpressed } from './politics.js';
    tests/lint/entropyRootCensus.walker.test.js:287|['src/domain/traditions/politics.js', 'export function reexpressed(rec, seedKey)'],
    docs/DESIGN_FP_ARCH_EP.md:365|| 9 | `traditions/politics.js` → `reexpressed(rec, seedKey)` | `` `${rngSeed}::tradition:reexpress:${id}:${year}:${kind}` `` | YEAR-KEYED |

### 3  src/domain/worldPulse/calamityKernel.js  ::  forceCalamityStrike()
  paths: forceCalamityStrike().settlement.populationHistory[0].reason | forceCalamityStrike().settlement.calamityHistory[0].name | forceCalamityStrike().settlement.activeConditions[0].description
  ex: "Killed in the calamity."
  ex: "{name}'s Great Calamity, year {year}"
  ex: "The calamity has overwhelmed the seat of power; the response is contested and legitimacy bleeds."
  consumers(7):
    src/domain/worldPulse/realmVerbExecution.js:50|import { forceCalamityStrike, buildExodusOutcome, applyExodusToUpdates } from './calamityKernel.js';
    src/domain/worldPulse/settlementLifecycleKernel.js:1178|// W-COMPOSER-2 lift, the forceCalamityStrike/forceCalamityEntry precedent).
    tests/domain/calamity.kernel.integration.test.js:20|import { advanceCalamity, forceCalamityStrike, forceCalamityEntry } from '../../src/domain/worldPulse/calamityKernel.js';
    tests/domain/highWater.test.js:38|import { forceCalamityStrike } from '../../src/domain/worldPulse/calamityKernel.js';
    docs/implementation/PACKET_MANIFEST.json:25560|"_note": "THE ACCEPTANCE AND THE TWO ROSTERS. One literal describe, seven straight-line it: guard-the-guard (the whole-directory scans are live and DESCE
    docs/implementation/packets/town-cartography/MF-T2R.md:361|| A2 | **the lived interval, with and without the strike** (charter acceptance 1+2, the §306.2 input-trap law; channel 3 via the calamity led
    docs/review-r2/RAW_SURVEY_RESULTS.json:540|"Force ≡ organic is genuinely implemented: every realm arm resolves through its wave's own kernel function (declareCasus, planConvoy/planBlockade, forceCalam

### 3  src/domain/worldPulse/espionage/espionageTap.js  ::  TAP_RECEIPTS
  paths: TAP_RECEIPTS.beliefs | TAP_RECEIPTS.delta | TAP_RECEIPTS.performance
  ex: "as X itself believes"
  ex: "X speaks one thing and believes another"
  ex: "as told in X's markets"
  consumers(1):
    tests/domain/espionageProducts.test.js:45|TAP_RECEIPTS,

### 3  src/domain/worldPulse/npcDmVerbRecords.js  ::  deathNewsItem()
  paths: deathNewsItem().summary | deathNewsItem().reasons[0] | deathNewsItem().reasons[1]
  ex: "The realm strikes their name from the register of the living. {where} was the last place that held them."
  ex: "{who} is recorded among the dead at {where}."
  ex: "Any order standing against them dies with them."
  consumers(1):
    src/domain/worldPulse/npcDmVerbs.js:78|deathNewsItem,

### 3  src/domain/worldPulse/operations/operationGrammar.js  ::  ACCEPTANCE_SEAMS
  paths: ACCEPTANCE_SEAMS[0].ruling | ACCEPTANCE_SEAMS[3].ruling | ACCEPTANCE_SEAMS[4].ruling
  ex: "ODQ §806 ⟨F8⟩ — one vetting home, importance-DESCENDING; neither program forks a second"
  ex: "ODQ §802 R2 + §802.1 — CHOOSE ON KNOWN, RESOLVE ON TRUE; mortals read reputation, deities keep true-sight"
  ex: "W-LIVES L5 — the chokepoint every consumer re-routes through; core + drift, sparse"
  consumers(1):
    tests/domain/operationGrammar.test.js:18|ACCEPTANCE_SEAMS,

### 3  src/domain/worldPulse/pulseKernel.js  ::  RESIDUE_STRIP_SITES
  paths: RESIDUE_STRIP_SITES[2].banks | RESIDUE_STRIP_SITES[3].banks | RESIDUE_STRIP_SITES[4].banks
  ex: "occupation seed + conquest disposition ratchet"
  ex: "vassal promotion + advance-win disposition residue"
  ex: "the concluded-war record (W-MEM): staged or sealed out of band"
  consumers(9):
    src/domain/worldPulse/residueStripGuard.js:6|* The RESIDUE_STRIP_SITES registry catches a strip that DRIFTS from the code; the
    src/domain/worldPulse/roadsKernel.js:28|* NEVER faction.power, NEVER ladder standing. NO RESIDUE_STRIP_SITES entry.
    tests/domain/concludedWarsWriter.test.js:4|* This file is the `concluded_wars` residue-strip site's named coverage (RESIDUE_STRIP_SITES),
    tests/domain/residueStripRegistry.test.js:6|import { RESIDUE_STRIP_SITES } from '../../src/domain/worldPulse/pulseKernel.js';
    tests/domain/worldPulseResidueSweep.test.js:5|* per layer. The machine-enforced RESIDUE_STRIP_SITES registry catches a strip that
    tests/property/roadsCharter.test.js:29|import { RESIDUE_STRIP_SITES } from '../../src/domain/worldPulse/pulseKernel.js';
    docs/COMPREHENSIVE_REVIEW_2026-07-15.md:327|**Coverage:** Read HEAD 91a0c409 (expected). Scope = 13 files / 11,740 LOC, ALL fully read: pulseKernel.js (2,454), applyWorldPulse.js (1,434), worldState.j
    docs/DESIGN_FP_ARCH_EP.md:2511|| 4 | **Resume a paused advance with different verdicts** | `resolveIntervalMajors` → `runResolveIntervalMajors` | **THE CONSTRAINT, NOT THE OPPORTUNITY: the SAME epoch 
    docs/review-r2/RAW_SURVEY_RESULTS.json:13|"coverage": "Read HEAD 91a0c409 (expected). Scope = 13 files / 11,740 LOC, ALL fully read: pulseKernel.js (2,454), applyWorldPulse.js (1,434), worldState.js (

### 3  src/domain/worldPulse/supplyWebWarfare.js  ::  WEBWAR_VETO_PROSE
  paths: WEBWAR_VETO_PROSE.webwar_self | WEBWAR_VETO_PROSE.webwar_no_web | WEBWAR_VETO_PROSE.webwar_plan_exists
  ex: "A court cannot open a supply-war campaign against itself."
  ex: "That target has no readable supply web to strangle (no reachable outside suppliers)."
  ex: "That court already runs a live indirect campaign; one plan per aggressor."
  consumers(2):
    src/domain/events/realmManifest.js:31|import { supplyWebWarfareActive, WEBWAR_VETO_PROSE } from '../worldPulse/supplyWebWarfare.js';
    tests/domain/supplyWebWarfare.test.js:27|orderSupplyRaid, declareTradeEmbargo, WEBWAR_VETO_PROSE, WEBWAR_TUNING, INSTRUMENTS,

### 3  src/domain/worldPulse/warReasons.js  ::  CASUS_VETO_PROSE
  paths: CASUS_VETO_PROSE.casus_unknown_type | CASUS_VETO_PROSE.casus_engine_derived | CASUS_VETO_PROSE.casus_self
  ex: "That is not a typed reason for war this engine tracks."
  ex: "That cause must arise from the world’s own record. A lineage claim requires a surviving founding edge and a live inversi"
  ex: "A court cannot hold a casus belli against itself."
  consumers(2):
    src/domain/events/realmManifest.js:29|import { peaceCausalActive, DECLARABLE_WAR_REASON_TYPES, CASUS_VETO_PROSE } from '../worldPulse/warReasons.js';
    tests/domain/peaceCausalVerbs.test.js:24|declareCasus, CASUS_VETO_PROSE, warReasonsFor, warReasonFactor, REASON_TUNING,

### 3  src/lib/aboutMapping.js  ::  ABOUT_UNITS
  paths: ABOUT_UNITS[1].heading | ABOUT_UNITS[3].heading | ABOUT_UNITS[4].heading
  ex: "What if the town remembered?"
  ex: "One tick, in dependency order"
  ex: "Caged by mechanism, not by promise"
  consumers(1):
    tests/components/aboutSplit.test.jsx:32|ABOUT_UNITS, ABOUT_WHAT_VIEW, ABOUT_GUIDE_VIEW, LEGACY_ABOUT_TABS,

### 2  src/components/compendium/customCategories.js  ::  AUTHORING_LANES
  paths: AUTHORING_LANES[0].blurb | AUTHORING_LANES[1].blurb
  ex: "Institutions, services, resources, trade goods, and the supply chains they form: the static building blocks a generation"
  ex: "Deities, traditions, and factions. Deities gain mechanics after assignment; factions enter through authored events; trad"
  consumers(3):
    src/components/compendium/CustomContent.jsx:10|import { AUTHORING_LANES } from './customCategories.js';
    tests/domain/customFoundingTraditions.test.js:15|import { AUTHORING_LANES } from '../../src/components/compendium/customCategories.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:8940|- authority: `customContent.{institutions, services, resources, tradeGoods}` — the semantic authority is `schema/custom-content.manifest.json`, projected throu

### 2  src/components/gallery/galleryMapsUtils.js  ::  KIND_OPTIONS
  paths: KIND_OPTIONS[0][2] | KIND_OPTIONS[1][2]
  ex: "Bare terrain and geography. No settlements, no world state."
  ex: "The map populated with its settlements, plus the living world you choose to reveal: the in-world clock, the chronicle, t"
  consumers(3):
    src/components/gallery/MapShareEditor.jsx:28|KIND_OPTIONS, suggestedTagsForCampaign, campaignFacets, } from './galleryMapsUtils.js';
    src/components/tableLedger/TableLedgerPanel.jsx:68|const KIND_OPTIONS = TABLE_EVENT_KINDS.map((id) => ({ id, label: KIND_LABELS[id] }));
    tests/components/gallery/galleryMapsUtils.test.js:4|KIND_OPTIONS,

### 2  src/components/map/heraldCommandSourceParity.js  ::  HERALD_COMMAND_SOURCE_PARITY
  paths: HERALD_COMMAND_SOURCE_PARITY[2].owner | HERALD_COMMAND_SOURCE_PARITY[5].owner
  ex: "LiveWarStatus, RealmIntrigue, BeliefDivergenceBand, WarResolveSection"
  ex: "RealmItem archive and HeraldHeadline"
  consumers(1):
    tests/components/heraldCommandBrief.test.jsx:26|import { HERALD_COMMAND_SOURCE_PARITY } from '../../src/components/map/heraldCommandSourceParity.js';

### 2  src/components/map/heraldRegister.js  ::  REMNANT_GRADE
  paths: REMNANT_GRADE.relic_ruin.epitaph | REMNANT_GRADE.abandoned_site.epitaph
  ex: "A great city that dwindled to a final thorp and died. Its stones stand as a landmark and a warning."
  ex: "It dwindled and was abandoned. A quiet site marks where it stood."
  consumers(0):

### 2  src/components/organic/samples/fixtures.js  ::  libraryFixture
  paths: libraryFixture.rows[2].signal | libraryFixture.rows[3].signal
  ex: "Grain surplus; a new mint"
  ex: "Raiders on the upland road"
  consumers(1):
    src/components/organic/samples/LibrarySample.jsx:3|import { libraryFixture as lib } from './fixtures.js';

### 2  src/data/galleryReactionVocab.js  ::  REACTION_LABELS
  paths: REACTION_LABELS.worth_walking | REACTION_LABELS.run_campaign
  ex: "A world worth walking"
  ex: "I'd run a campaign here"
  consumers(2):
    src/components/gallery/GalleryReactionChips.jsx:16|* No free text anywhere: unknown keys never render (REACTION_LABELS lookup).
    tests/data/galleryReactionVocab.test.js:16|import { REACTION_VOCAB, REACTION_KEYS, REACTION_LABELS } from '../../src/data/galleryReactionVocab.js';

### 2  src/design/organic/index.js  ::  MATERIAL_GRAMMAR
  paths: MATERIAL_GRAMMAR.grain | MATERIAL_GRAMMAR.inkBite
  ex: "paper tooth — an alpha overlay on grounds, never under body text at more than ~4% peak"
  ex: "plate edge weighting — the pressed line breathes; frames read printed, not drawn by tool"
  consumers(2):
    src/design/organic/material.js:23|export const MATERIAL_GRAMMAR = Object.freeze({
    tests/design/organicMaterial.test.js:20|import { inkBiteFrameSvg, GRAIN_TILES, MATERIAL_GRAMMAR } from '../../src/design/organic/material.js';

### 2  src/domain/aiGrounding.js  ::  summarizeGroundingPayload()
  paths: summarizeGroundingPayload()[0] | summarizeGroundingPayload()[11]
  ex: "Identity: {name} ({tier}, pop. {population})."
  ex: "Relationship memory entries: 1."
  consumers(3):
    tests/domain/aiGrounding.test.js:29|summarizeGroundingPayload,
    tests/edgeFunctions/aiGroundingBundle.freshness.test.js:93|'summarizeGroundingPayload',
    tests/edgeFunctions/aiGroundingContract.test.js:38|summarizeGroundingPayload,

### 2  src/domain/autonomy/accelerationOps.js  ::  validateNudge()
  paths: validateNudge().errors[0] | validateNudge().errors[2]
  ex: "unknown stressor type \"(empty)\": a nudge may only raise a catalogued pressure"
  ex: "severity must sit in [0.05, 0.85]"
  consumers(4):
    src/domain/autonomy/index.js:22|validateNudge, buildNudgeOp, describeNudge,
    src/lib/surveyorWrite.js:277|signalRegistryEntries, NUDGE_TYPES, validateStopCondition, validateNudge,
    tests/domain/autonomy/accelerationOps.test.js:13|validateNudge, buildNudgeOp, describeNudge,
    tests/security/aiSurfaceSourceScan.test.js:87|'surveyor-autonomy':   { disposition: 'walled', seam: 'src/lib/surveyorWrite.js', walls: ['validateStopCondition', 'validateNudge'] },

### 2  src/domain/content/customContentVersioning.js  ::  validateVersionedContent()
  paths: validateVersionedContent().errors[0] | validateVersionedContent().errors[1]
  ex: "Unsupported custom-content category \"{a}\"."
  ex: "Custom content must be a plain object."
  consumers(3):
    src/domain/content/customContentCommands.js:11|validateVersionedContent,
    src/lib/contentPacks.js:16|validateVersionedContent,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:7010|- inputs: entries[] (category + data; ≥1 entry required for entry-kinds, `customContentCommands.js:181-185`); each entry passes `validateVersionedContent` + `a

### 2  src/domain/customContentSchema.js  ::  CRITICALITY
  paths: CRITICALITY[0].label | CRITICALITY[2].label
  ex: "Critical — food, water, timber"
  ex: "Discretionary — luxury / comfort"
  consumers(1):
    src/components/compendium/CustomItemAttributes.jsx:13|CRITICALITY, ECONOMIC_WEIGHT, DEFENSE_ROLES, FOOD_IMPACT, POWER_AUTHORITIES,

### 2  src/domain/customContentSchema.js  ::  DEITY_EFFECT_CHANNELS
  paths: DEITY_EFFECT_CHANNELS[3].label | DEITY_EFFECT_CHANNELS[5].label
  ex: "Healing and resistance to pestilence"
  ex: "Order and the keeping of law"
  consumers(2):
    src/domain/causalState.js:1065|// `war_readiness` is both a `DEITY_EFFECT_CHANNELS` key and a `DEITY_DOMAIN_PRESSURE`
    src/domain/worldPulse/dispositionProfile.js:103|* `war_readiness` are BOTH a `DEITY_DOMAIN_PRESSURE` key and a `DEITY_EFFECT_CHANNELS`

### 2  src/domain/customContentSchema.js  ::  DEITY_LAW
  paths: DEITY_LAW[0].label | DEITY_LAW[1].label
  ex: "Lawful — upholds order and oaths"
  ex: "Chaotic — erodes order, tolerates corruption"
  consumers(4):
    src/components/compendium/CustomContentEditor.jsx:12|DEITY_LAW,
    src/components/settlement/DeityAssignmentPanel.jsx:37|DEITY_LAW,
    docs/GAME_GRADE_AUDIT.md:312|- **V-CUS3** src/components/settlement/DeityAssignmentPanel.jsx:56-62 (snapLine, in the patron/cult summary and lapsed read-only view) — Joins raw axis keys with ' · ' (sn
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:8144|- authority: `customContent.deities` (account library or campaign pinned binding — activeCustomContentContext); schema src/domain/customContentSchema.js:154-19

### 2  src/domain/customContentSchema.js  ::  FOOD_IMPACT
  paths: FOOD_IMPACT[1].label | FOOD_IMPACT[2].label
  ex: "Produces food — raises supply"
  ex: "Consumes food — raises demand"
  consumers(2):
    src/components/compendium/CustomItemAttributes.jsx:13|CRITICALITY, ECONOMIC_WEIGHT, DEFENSE_ROLES, FOOD_IMPACT, POWER_AUTHORITIES,
    docs/GAME_GRADE_AUDIT.md:311|- **V-CUS2** src/components/compendium/CustomItemAttributes.jsx:36 — `Food · ${item.foodImpact}` prints the raw stored key (e.g. 'surplus'/'drain') rather than keyLabel(FO

### 2  src/domain/display/dossierViewModel.js  ::  deriveGranaryOutlook()
  paths: deriveGranaryOutlook().detail | deriveGranaryOutlook().display
  ex: "the granary is nearly empty (0.0 of 0.0 months)"
  ex: "{season} — the granary is nearly empty (0.0 of 0.0 months)"
  consumers(9):
    src/components/new/tabs/EconomicsTab.jsx:14|import { deriveFoodBalance, deriveGranaryOutlook, deriveTreasuryGlance } from '../../../domain/display/dossierViewModel.js';
    src/data/dossierStateProse/economy.generated.js:140|"title": "DS-ECO-2: Economics › At-a-glance tiles · `foodBalance{dailyProduction,dailyNeed,surplus,deficit}` + `deficitPct` + `deriveGranaryOutlook(
    src/domain/display/stateProse/economyStateProse.js:52|* number a canonical reader already owns — `deriveFoodBalance` and `deriveGranaryOutlook`
    src/domain/fieldManifest.js:378|// seasonsEnabled; deriveGranaryOutlook is its display reader).
    src/domain/traditions/almanac.js:11|* PURITY (the dossierViewModel.deriveGranaryOutlook precedent): a true leaf — no store/
    src/domain/worldPulse/foodStockpile.js:424|// dossier's seasonal read (deriveGranaryOutlook) is self-contained on
    tests/domain/display/dossierViewModel.test.js:9|deriveGranaryOutlook,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:630|- authority: `economicState` (prosperity, economicComplexity, compound.economyOutput, situationDesc); food from `economicViability.metrics.foodBalance` with def
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:836|### DS-ECO-2: Economics › At-a-glance tiles · `foodBalance{dailyProduction,dailyNeed,surplus,deficit}` + `deficitPct` + `deriveGranaryOutlook(s){availab

### 2  src/domain/display/economyFreshness.js  ::  ECONOMY_FRESHNESS_SENTENCES
  paths: ECONOMY_FRESHNESS_SENTENCES.tallies | ECONOMY_FRESHNESS_SENTENCES.catalog
  ex: "Events recorded after this settlement's last survey may not be fully counted in these tallies yet."
  ex: "Events recorded after this settlement's last survey may not be fully counted in this catalog yet."
  consumers(6):
    src/components/new/EconomyFreshnessNote.jsx:8|* domain/display/economyFreshness.js (ECONOMY_FRESHNESS_SENTENCES) and the
    src/data/dossierStateProse/economy.generated.js:6|import { ECONOMY_FRESHNESS_SENTENCES } from '../../domain/display/economyFreshness.js';
    tests/components/economyFreshnessNote.test.jsx:60|import { ECONOMY_FRESHNESS_SENTENCES } from '../../src/domain/display/economyFreshness.js';
    tests/lint/economyReadModelCoverage.walker.test.js:49|* `ECONOMY_FRESHNESS_SENTENCES.tallies` where the literal used to sit. The
    scripts/generate-dossier-state-prose.mjs:36|import { ECONOMY_FRESHNESS_SENTENCES } from '../src/domain/display/economyFreshness.js';
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:492|footnote and `ECONOMY_FRESHNESS_SENTENCES` are existing authored strings with

### 2  src/domain/display/stateProse/economyStateProse.js  ::  COMPLEXITY_NOUN
  paths: COMPLEXITY_NOUN.Diversified — broad institutional economic base | COMPLEXITY_NOUN.Mixed subsistence and market
  ex: "broad base of trades"
  ex: "mix of field and market"
  consumers(2):
    tests/domain/economyStateProseDesk.test.js:20|COMPLEXITY_NOUN,
    docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:179|| `{complexity}` | bare-common | the economy's complexity as a bare common-noun phrase. **NOT the generator's `economicComplexity` display string**, whi

### 2  src/domain/display/tradePressure.js  ::  TRADE_PRESSURE_PHRASES
  paths: TRADE_PRESSURE_PHRASES.SALIENCE_BANDS[0].phrase | TRADE_PRESSURE_PHRASES.SALIENCE_BANDS[1].phrase
  ex: "a vital, hard-to-replace tie"
  ex: "a valuable trade tie"
  consumers(0):

### 2  src/domain/display/warAndRoadNames.js  ::  UNTYPED_WAR_NAME_MOLDS
  paths: UNTYPED_WAR_NAME_MOLDS[1]() | UNTYPED_WAR_NAME_MOLDS[2]()
  ex: "the War of {a} and {b}"
  ex: "the War between {a} and {b}"
  consumers(1):
    tests/domain/display/warAndRoadNames.test.js:6|UNTYPED_WAR_NAME_MOLDS,

### 2  src/domain/instantWorld/worldPlan.js  ::  MAGIC_CHOICES
  paths: MAGIC_CHOICES[0].blurb | MAGIC_CHOICES[1].blurb
  ex: "Mages, arcane orders, enchanted trade, and magical events all belong here."
  ex: "No working magic anywhere in the realm. Gods and temples remain."
  consumers(3):
    src/components/instant/InstantWorldEntry.jsx:25|import { REALM_SIZES, TONES, MAP_KINDS, MAGIC_CHOICES, DEFAULT_REALM_SIZE, DEFAULT_TONE, DEFAULT_MAP_KIND, DEFAULT_MAGIC, isMagicChoice } from '../../do
    src/store/displayPrefsSlice.js:33|*     by domain/instantWorld/worldPlan.js MAGIC_CHOICES and deliberately NOT
    tests/domain/instantWorldMagicKnob.test.js:12|MAGIC_CHOICES,

### 2  src/domain/npc/characterConsumers.js  ::  CORRUPTION_DEPTH_GATE
  paths: CORRUPTION_DEPTH_GATE.status | CORRUPTION_DEPTH_GATE.ownerRows[1]
  ex: "CANDIDATE, OWNER-UNSIGNED (pack row 13(a); F13 restored the call to the pen)"
  ex: "the reach: a drifted vice opens a door on 7 of 17 axes, because ten axes carry no legacy flaw column on their vice side "
  consumers(1):
    tests/domain/npc/characterConsumers.test.js:26|CORRUPTION_DEPTH_GATE,

### 2  src/domain/npc/livedExperienceCatalog.js  ::  LIVED_EXPERIENCE_PROVENANCE
  paths: LIVED_EXPERIENCE_PROVENANCE.status | LIVED_EXPERIENCE_PROVENANCE.ownerRows[0]
  ex: "CANDIDATE, OWNER-UNSIGNED (pack Register III, first draft)"
  ex: "the whole kind roster, every plane assignment, every family assignment and every pull vector"
  consumers(1):
    tests/domain/npc/livedExperienceCatalog.test.js:44|LIVED_EXPERIENCE_PROVENANCE,

### 2  src/domain/region/wizardNews.js  ::  IMPACT_LABELS
  paths: IMPACT_LABELS.criminal_pressure | IMPACT_LABELS.religious_pressure
  ex: "a rise in lawlessness"
  ex: "a stir among the faithful"
  consumers(4):
    src/domain/realm/heraldRouting.js:203|// wizardNews IMPACT_LABELS (dynamic `impactKind: impact.kind`) — the trade-side.
    tests/lint/newsSubjectVocabulary.walker.test.js:67|import { IMPACT_LABELS, createWizardNewsEntryFromImpact } from '../../src/domain/region/wizardNews.js';
    scripts/mutation-coverage-manifest.json:83|"rationale": "The news-subject vocabulary walker. Its catching power is EXECUTED IN THE TEST BODY rather than claimed, and by a control no other walker in th
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:2190|- Fix shape: Add a what-token → in-world phrase vocabulary in the display layer (the IMPACT_LABELS pattern, extended over the candidateType families: war/c

### 2  src/domain/townMap/asymmetrySources.js  ::  extractAsymmetrySources()
  paths: extractAsymmetrySources()[0].cause | extractAsymmetrySources()[1].cause
  ex: "waterfront (the town meets the water)"
  ex: "market accretion (stalls crowd the trade gate)"
  consumers(1):
    src/domain/townMap/townLayoutV2.js:54|extractAsymmetrySources, netDisplacement, regionalGrain, leanPolygon, desirePaths,

### 2  src/domain/tuning/autoTunableRegistry.js  ::  CANDIDATE_RAILS
  paths: CANDIDATE_RAILS[0].note | CANDIDATE_RAILS[1].note
  ex: "SQL k-floor; monotone UP only (§4). Owner-ratify if auto-tightening is wanted."
  ex: "SQL k-floor; monotone UP only (§4)."
  consumers(0):

### 2  src/domain/tuning/proposedSoakBands.js  ::  validateSoakBand()
  paths: validateSoakBand().reasons[4] | validateSoakBand().reasons[5]
  ex: "min and max must both be finite numbers"
  ex: "status must be exactly 'RATIFIED' (bands are owner-signed targets)"
  consumers(2):
    tests/domain/proposedSoakBands.test.js:15|validateSoakBand,
    scripts/check-tuning-bands.mjs:38|// The RATIFIED law, asserted independently of validateSoakBand so a future

### 2  src/domain/worldPulse/convergence.js  ::  interventionNews()
  paths: interventionNews().headline | interventionNews().summary
  ex: "{patronId}'s banners march on {targetId}'s succession"
  ex: "{patronId} threw an army into {targetId}'s contest to raise the challengers. {receipt}"
  consumers(0):

### 2  src/domain/worldPulse/deityFlaws.js  ::  DEITY_FLAW_PROVENANCE
  paths: DEITY_FLAW_PROVENANCE.status | DEITY_FLAW_PROVENANCE.ruling
  ex: "CANDIDATE, OWNER-UNSIGNED (the D2 flaw words are drafts mapped onto the chart; frozen only by the owner pen)"
  ex: "ODQ 800.3 J2: flaws are vice-pole positions on the shared chart; the D2 modulation hooks survive, attached to vice poles"
  consumers(2):
    src/domain/worldPulse/faithTuningSurface.js:180|* DEITY_FLAW_PROVENANCE / CATALOG_PROVENANCE idiom).
    tests/domain/deityFlaws.test.js:21|DEITY_FLAW_PROVENANCE,

### 2  src/domain/worldPulse/deityStanceLane.js  ::  footholdOutcome()
  paths: footholdOutcome().headline | footholdOutcome().summary
  ex: "{rivalName} finds an ear in {cityName}"
  ex: "{npcName}, whose temper leans to {rivalName} and away from {patronName}, has become the creed's foothold within {cityNam"
  consumers(3):
    src/domain/worldPulse/religiousContest.js:70|import { evaluateDeityStanceLane, betrayalCooldownPairs, pactCooldownPairs, footholdCooldownKeys, footholdCooldownKey, footholdOutcome } from './deityStanc
    tests/domain/deityStanceLane.test.js:22|footholdOutcome,
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:429|- Evidence: religiousContest.js:791-814 emits footholdOutcome per tick with no cooldown (targetedFootholds is deterministic over static NPC traits); deitySt

### 2  src/domain/worldPulse/envoyChanceMeetingStage.js  ::  CHANCE_MEETING_STAGE_TUNING_PROVENANCE
  paths: CHANCE_MEETING_STAGE_TUNING_PROVENANCE.ownerRows[1] | CHANCE_MEETING_STAGE_TUNING_PROVENANCE.ownerRows[2]
  ex: "the doubling of that grievance when the approacher wore a covert face"
  ex: "the residents floor, which is the estate's own notable line rather than a number this lane chose"
  consumers(0):

### 2  src/domain/worldPulse/espionage/infiltrationDepth.js  ::  PLACEMENT_MISSION_KINDS
  paths: PLACEMENT_MISSION_KINDS[0].sourceNote | PLACEMENT_MISSION_KINDS[1].sourceNote
  ex: "the L3 placement operation; its FAILURE writes the covert-cause custody hold, which is a live record family with a live "
  ex: "the L4 seating operation; F8 rules the placed seat IS the web's foreign-leashed seat reached by a second door, so its ex"
  consumers(2):
    tests/domain/envoyTaskCatalog.test.js:567|expect(/PLACEMENT_MISSION_KINDS/.test(code)).toBe(false);
    tests/domain/infiltrationDepth.test.js:38|PLACEMENT_MISSION_KINDS,

### 2  src/domain/worldPulse/espionage/infiltrationDrift.js  ::  DEPTH_PRICING_LAW
  paths: DEPTH_PRICING_LAW.ruling | DEPTH_PRICING_LAW.rejected
  ex: "depth prices BREADTH (how many of the host chart's axes an embedding exposes a person to), and never the per-axis quantu"
  ex: "the registers pack drafts the five depth words as a MILIEU MULTIPLIER; a multiplier is exactly the arithmetic the cliff "
  consumers(1):
    tests/domain/infiltrationDrift.test.js:40|DEPTH_PRICING_LAW,

### 2  src/domain/worldPulse/espionage/operationsVoice.js  ::  GOING_NATIVE_VOICE_LAW
  paths: GOING_NATIVE_VOICE_LAW.rule | GOING_NATIVE_VOICE_LAW.measured
  ex: "the voice speaks strain and never a turning: no line states or presupposes that dwelling can reverse a fidelity while th"
  ex: "ambient dwelling rests at a whisper (one floor quantum per axis at every rung), and the axis the arc runs on carries zer"
  consumers(1):
    tests/domain/operationsVoice.test.js:32|GOING_NATIVE_VOICE_LAW,

### 2  src/domain/worldPulse/feasibilityGate.js  ::  classifyFeasibility()
  paths: classifyFeasibility().reasons[0] | classifyFeasibility().reasons[1]
  ex: "Weighed against the walls, the attacker is no contest: the defender's home ground counted."
  ex: "Utterly outmatched, a thorpe against a fortified city; the siege cannot succeed."
  consumers(16):
    src/domain/display/mobilizationStatus.js:19|classifyFeasibility,
    src/domain/worldPulse/conquestDoctrineStage.js:565|* gate, `classifyFeasibility` itself — still runs exactly as before. A hopeless
    src/domain/worldPulse/warCapacityReads.js:32|import { classifyFeasibility, verdictPermitsSiege } from './feasibilityGate.js';
    src/domain/worldPulse/warDeployment.js:67|import { classifyFeasibility, verdictPermitsSiege, siegeMatchupWordFor, feasibilityVerdictClause } from './feasibilityGate.js';
    src/domain/worldPulse/warIntent.js:36|*              the keystone — classifyFeasibility. A HOPELESS WAR STILL DOES NOT OPEN.
    src/domain/worldPulse/warSiegeVerdict.js:20|import { classifyFeasibility, verdictPermitsSiege, verdictAllowsHarassment } from './feasibilityGate.js';
    tests/domain/feasibilityGate.test.js:4|classifyFeasibility,
    tests/domain/warIntentJoin.test.js:359|// waiver of a heuristic pre-filter, never of classifyFeasibility.
    tests/domain/warMagicGate.test.js:22|*   L2 — classifyFeasibility could return `require_magic` ("arcane force could tip an
    tests/domain/warVerdictHeraldWords.test.js:9|*       Proved by DRIVING the real `classifyFeasibility` and checking the pairing, not

### 2  src/domain/worldPulse/npcGoalBranches.js  ::  GOAL_BRANCH_PROVENANCE
  paths: GOAL_BRANCH_PROVENANCE.status | GOAL_BRANCH_PROVENANCE.ownerRows[0]
  ex: "CANDIDATE, OWNER-UNSIGNED (the nerve column is the conversion's one authored addition)"
  ex: "the `nerve` column: thirteen ordinal values saying how bold each branch is. They are the only numbers this conversion au"
  consumers(1):
    tests/domain/npcGoalBranches.test.js:27|GOAL_BRANCH_PROVENANCE,

### 2  src/domain/worldPulse/operations/operationGrammar.js  ::  MISSION_KIND_CATALOG
  paths: MISSION_KIND_CATALOG[1].sourceNote | MISSION_KIND_CATALOG[2].sourceNote
  ex: "the ACQUIRE arm — the only product that may CREATE a belief; it crosses exactly the appraisal legs the mission named and"
  ex: "the REFUTE arm — the sweeping branch, which crosses every readable slot so the contradiction term can bite, then lands t"
  consumers(3):
    src/domain/worldPulse/espionage/infiltrationDepth.js:315|* THE PLACEMENT MISSION KINDS, in sibling car O1's `MISSION_KIND_CATALOG` ROW SHAPE so the
    tests/domain/infiltrationDepth.test.js:60|// does not — arm 6's `expectAbsentWithAnchor` on `MISSION_KIND_CATALOG` is what keeps the
    tests/domain/operationGrammar.test.js:22|MISSION_KIND_CATALOG,

### 2  src/domain/worldPulse/pactFormation.js  ::  signPactProposal()
  paths: signPactProposal().worldState.spatialLedgers.treaties.{from}>{to}.receipts[0] | signPactProposal().receipt
  ex: "Agreed in peace between {from} and {to}, and extracted from nobody."
  ex: "{from} and {to} have set their names to it, in peace and not at a war's end."
  consumers(8):
    src/domain/worldPulse/ledgerOwnershipManifest.js:57|ref('src/domain/worldPulse/pactFormation.js', 'signPactProposal', "setSpatialLedger(worldState, 'treaties'"),
    tests/domain/peaceTerms.test.js:46|import { draftPactSheet, signPactProposal } from '../../src/domain/worldPulse/pactFormation.js';
    tests/domain/treatyOrientationWr10g.test.js:47|import { draftPactSheet, signPactProposal } from '../../src/domain/worldPulse/pactFormation.js';
    tests/lint/oathStampTotality.walker.test.js:85|doors: Object.freeze(['signPactProposal']),
    docs/FABLE_VALIDATION_QUEUE.md:2788|`pactFormation.js#signPactProposal` — the FOURTH transport into the one instrument — which
    docs/implementation/packets/foreign-policy/GR-3B-ORIENT.md:136|Method: drive the real `draftPactSheet` / `signPactProposal` / `ensureWorldState`
    docs/implementation/packets/foreign-policy/GR-3B.md:167|| Named state writers | `0` | no new writer; `signPactProposal` unchanged |
    docs/implementation/packets/foreign-policy/IN-0C.md:180|| Negotiated door (signature re-base) | `pactFormation.js:385` `signPactProposal` | ✅ re-stamped to the **signature** tick |

### 2  src/domain/worldPulse/peaceReasons.js  ::  PEACE_VETO_PROSE
  paths: PEACE_VETO_PROSE.peace_no_deployment | PEACE_VETO_PROSE.peace_already_ordered
  ex: "That court has no army in the field against that foe. There is no war of theirs to wind down."
  ex: "The recall order is already given; the army marches home."
  consumers(3):
    src/domain/events/realmManifest.js:30|import { PEACE_VETO_PROSE } from '../worldPulse/peaceReasons.js';
    tests/domain/peaceCausalVerbs.test.js:27|import { sueForPeaceOrder, PEACE_VETO_PROSE } from '../../src/domain/worldPulse/peaceReasons.js';
    docs/review-r2/VERIFY_DIMS_RESULTS.json:580|"reasoning": "Refutation lens failed. All claims verified against current HEAD c1efa6c5 (finding tip 91a0c409 moved; defect unchanged).\n\nBUILT: warCausalB

### 2  src/domain/worldPulse/razingExecution.js  ::  razingDecisionFor()
  paths: razingDecisionFor().verdict.receipt | razingDecisionFor().extremity.receipt
  ex: "the conquest doctrine is not lit in this world; no town burns by it."
  ex: "{razerId} is not at the extreme with {victimId}: the border is not hostile, the resentment has not reached what hostilit"
  consumers(3):
    tests/domain/razingDeterrenceWr8.test.js:31|razingDecisionFor,
    tests/domain/razingExecutionWr8.test.js:36|razingDecisionFor,
    docs/FABLE_VALIDATION_QUEUE.md:189|| 2026-08-03 | ⛔ LANE WZ-2 — THE RAZING IS WIRED AT ITS MOUTH and WR-8 does **NOT** close: three commits, two chair rulings implemented, one FALSE ruling corrected i

### 2  src/domain/worldPulse/reinforcement.js  ::  REINFORCEMENT_FLOW_WORDS
  paths: REINFORCEMENT_FLOW_WORDS[0] | REINFORCEMENT_FLOW_WORDS[2]
  ex: "A thin trickle of men and materiel"
  ex: "Everything the home could send"
  consumers(1):
    tests/domain/warCostHeraldWords.test.js:32|REINFORCEMENT_FLOW_WORDS,

### 2  src/domain/worldPulse/routeNetworkConsumersInterdiction.js  ::  severanceReasons()
  paths: severanceReasons()[0] | severanceReasons()[1]
  ex: "The road from {b} carried {join} into {b}."
  ex: "Traffic on that way stood at {band}."
  consumers(1):
    tests/domain/routeNetworkConsumersInterdiction.test.js:34|severanceReasons,

### 2  src/lib/importReconciliation.js  ::  previewImportReconciliation()
  paths: previewImportReconciliation().error | previewImportReconciliation().diagnostic.message
  ex: "This reconciliation session is malformed or unsupported."
  ex: "The reconciliation session was rejected."
  consumers(5):
    src/components/settlements/StructuredCampaignReconciliation.jsx:28|previewImportReconciliation,
    src/lib/importReconciliationExecution.js:100|export function previewImportReconciliation(session) {
    tests/lib/importReconciliation.test.js:17|previewImportReconciliation,
    tests/lib/importReconciliationRecovery.test.js:17|previewImportReconciliation,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:11918|- preview: import-reconciliation has previewImportReconciliation (importReconciliation.js:29-32); account import surfaces per-record notices

### 2  src/store/aiRequestLifecycle.js  ::  NARRATIVE_FIELD_LABELS
  paths: NARRATIVE_FIELD_LABELS.thesis | NARRATIVE_FIELD_LABELS.connectionsMap
  ex: "Writing the settlement’s identity"
  ex: "Mapping the political web"
  consumers(2):
    src/store/aiSlice.js:58|NARRATIVE_FIELD_LABELS,
    tests/store/aiSliceHelpers.test.js:11|NARRATIVE_FIELD_LABELS,

### 2  src/store/settlementDeityHelpers.js  ::  DEITY_WRITE_REFUSAL
  paths: DEITY_WRITE_REFUSAL.reasons[0] | DEITY_WRITE_REFUSAL.unlocks[0]
  ex: "Deities come from your custom Compendium."
  ex: "Requires premium custom content."
  consumers(3):
    tests/store/deityClearCults.test.js:27|DEITY_WRITE_REFUSAL,
    tests/store/deityRestoreFromWorld.test.js:34|import { setPrimaryDeityImpl, DEITY_WRITE_REFUSAL } from '../../src/store/settlementDeityHelpers.js';
    tests/store/deityWriteGate.test.js:42|DEITY_WRITE_REFUSAL,

### 1  src/application/commands/adapters/customContentApply.js  ::  customContentArchiveSpec
  paths: customContentArchiveSpec.description
  ex: "Archive a custom-content definition without deleting its revisions."
  consumers(0):

### 1  src/application/commands/adapters/importReconciliationApply.js  ::  importCampaignAttachExistingSpec
  paths: importCampaignAttachExistingSpec.description
  ex: "Rehome one existing settlement into the reviewed target campaign."
  consumers(2):
    src/application/commands/standardCommandRegistry.js:14|importCampaignAttachExistingSpec,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:6988|- operations: spec `importCampaignAttachExistingSpec` (`adapters/importReconciliationApply.js:111-132`); same factory/runtime/entry chain

### 1  src/application/commands/adapters/importReconciliationApply.js  ::  importSettlementCreateAndAttachSpec
  paths: importSettlementCreateAndAttachSpec.description
  ex: "Create one dormant imported settlement and attach it atomically."
  consumers(2):
    src/application/commands/standardCommandRegistry.js:15|importSettlementCreateAndAttachSpec,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:6967|- operations: spec `importSettlementCreateAndAttachSpec` (`adapters/importReconciliationApply.js:91-109`); envelope factory `importReconciliationCommand` (line

### 1  src/application/commands/adapters/partyImpactRecord.js  ::  partyImpactRecordSpec
  paths: partyImpactRecordSpec.description
  ex: "Record one reviewed party impact through the World Pulse input lane."
  consumers(2):
    src/application/commands/standardCommandRegistry.js:11|import { partyImpactRecordSpec } from './adapters/partyImpactRecord.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:6924|- operations: command spec `partyImpactRecordSpec` (`adapters/partyImpactRecord.js:25-75`); envelope factory `partyImpactCommand` (lines 77-111)

### 1  src/application/commands/adapters/pendingEditCommit.js  ::  pendingEditCommitSpec
  paths: pendingEditCommitSpec.description
  ex: "Commit one exact owner-scoped set of reviewed settlement edits."
  consumers(2):
    src/application/commands/standardCommandRegistry.js:12|import { pendingEditCommitSpec } from './adapters/pendingEditCommit.js';
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:2901|- surveyor: none — and structurally so: `pendingEditCommitSpec.surveyor === false` (adapters/pendingEditCommit.js:34)

### 1  src/application/commands/commandEnvelope.js  ::  validateCommandEnvelope()
  paths: validateCommandEnvelope().message
  ex: "command must be a plain object"
  consumers(4):
    src/application/commands/canonEventCommandRecovery.js:20|validateCommandEnvelope,
    src/application/commands/executeCommand.js:15|validateCommandEnvelope,
    tests/application/commands/commandEnvelope.test.js:6|validateCommandEnvelope,
    tests/application/commands/commandRegistry.walker.test.js:41|* whatever object it is handed with validateCommandEnvelope(). One production

### 1  src/components/founders/hallRegister.js  ::  covenantProseStyle
  paths: covenantProseStyle.fontFamily
  ex: "\"Crimson Text\", Georgia, serif"
  consumers(4):
    src/components/founders/ChairDrawer.jsx:23|import { HALL, RING_TONE, RING_LABEL, numeralStyle, covenantProseStyle, quietLineStyle } from './hallRegister.js';
    src/components/founders/FoundersHallPage.jsx:37|import { HALL, HALL_CEREMONY_CSS, covenantProseStyle, quietLineStyle, numeralStyle } from './hallRegister.js';
    src/components/founders/HallCovenant.jsx:15|import { HALL, covenantProseStyle, quietLineStyle } from './hallRegister.js';
    src/components/founders/RequestChairLetter.jsx:28|import { HALL, covenantProseStyle, quietLineStyle } from './hallRegister.js';

### 1  src/components/map/gatheredDocket.js  ::  heldDocketPromise()
  paths: heldDocketPromise()
  ex: "Every matter is settled."
  consumers(2):
    src/components/map/GatheredAdjudication.jsx:28|* the way out (heldDocketPromise) rather than leaving the DM to infer it. That is
    tests/components/gatheredDocketModel.test.js:25|heldDocketPromise,

### 1  src/components/map/lifecycleGlyphStyle.js  ::  LIFECYCLE_GLYPH_STYLE
  paths: LIFECYCLE_GLYPH_STYLE.ruin.label
  ex: "Relic ruin / abandoned"
  consumers(2):
    src/components/map/MapLegend.jsx:22|import { LIFECYCLE_GLYPH_STYLE } from './lifecycleGlyphStyle.js';
    src/components/map/PlacementsLayer.jsx:17|import { LIFECYCLE_GLYPH_STYLE } from './lifecycleGlyphStyle.js';

### 1  src/components/new/tabConstants.js  ::  SERVICE_META
  paths: SERVICE_META.criminal.note
  ex: "Available if you know where to look"
  consumers(0):

### 1  src/components/theme.js  ::  dropShadow()
  paths: dropShadow()
  ex: "drop-shadow(NaNpx NaNpx {b}px {c})"
  consumers(2):
    src/components/nav/FletchBand.jsx:188|FLETCH_VANE, GILT_LIGHT, HEADER_RIDERS, contactShadow, dropShadow, lightOffset,
    tests/components/navFletching.test.jsx:648|'components/nav/FletchBand.jsx': /contactShadow|dropShadow/,

### 1  src/config/entitlementLadder.js  ::  DEFERRED_LADDER_ROWS
  paths: DEFERRED_LADDER_ROWS[0].reason
  ex: "InteriorView has no product importer and no per-settlement sample allowance exists — unshipped, so the claim had nothing"
  consumers(4):
    tests/config/entitlementLadder.enforcement.test.js:87|import { ENTITLEMENT_LADDER, DEFERRED_LADDER_ROWS, VIEWING_PAYWALLS_PENDING_514 } from '../../src/config/entitlementLadder.js';
    docs/implementation/INDEX.md:1268|| WEB-8 | [`WEB-8`](./packets/website/WEB-8.md) | **LANDED**; the chair CASes the TE-STACK-2 tip — car 1 of a two-car stack through ONE gate, rebased onto slot `7009f
    docs/implementation/PACKET_MANIFEST.json:28389|"symbol": "export const DEFERRED_LADDER_ROWS = Object.freeze(["
    docs/implementation/packets/website/WEB-8.md:85|| interiors | viewing | `1 sample per settlement` / true | YES | **NONE.** `InteriorView.jsx` has ZERO real importers in `src` (the six textual hits are

### 1  src/data/goods/chains.js  ::  RESOURCE_CHAINS
  paths: RESOURCE_CHAINS.grain.processingInstitutions[3]
  ex: "Access to external mill"
  consumers(14):
    src/data/resourceChains.js:19|// RESOURCE_CHAINS — raw-resource → processing → export chains.
    src/domain/display/institutionProfile.js:23|import { RESOURCE_CHAINS } from '../../data/goods/chains.js';
    src/domain/display/stateProse/generalStateProse.js:1317|* RESOURCE_CHAINS and name theirs `livestock`, `wool`, `hides`, `timber`. MEASURED over 24
    src/domain/goods.schema.js:30|*     • src/data/resourceChains.js        RESOURCE_CHAINS, INDUSTRY_WATER_NEEDS
    src/generators/resourceGenerator.js:10|import {RESOURCE_CHAINS} from '../data/goods/chains.js';
    src/pdf/lib/viewModelBodySlices.js:271|// Entries are RESOURCE_CHAINS objects — resource in `rawResource`,
    tests/build/vendorPdfLazy.test.js:353|// lazy-only RESOURCE_CHAINS + INDUSTRY_WATER_NEEDS tables eager → resourceChains.js
    tests/data/terrainInstitutionModifiersReachable.test.js:11|* Ratchet (mirrors the RESOURCE_CHAINS F32 pin): EVERY named terrain modifier must
    tests/joins/goods.test.js:242|// holds RESOURCE_CHAINS-shaped entries), so a field rename would be a
    tests/joins/resourceChainCatalog.test.js:2|* tests/joins/resourceChainCatalog.test.js — F32 join harness for RESOURCE_CHAINS.

### 1  src/design/tokens.js  ::  fontFamily
  paths: fontFamily.mono
  ex: "\"JetBrains Mono\", \"Fira Code\", Consolas, monospace"
  consumers(374):
    src/App.jsx:617|fontSize: FS.sm, fontFamily: sans,
    src/AppViews.jsx:99|<div style={{ padding: 40, textAlign: 'center', color: MUTED, fontFamily: sans }}>
    src/components/AccountPage.jsx:325|<div style={{ textAlign: 'center', padding: '60px 20px', color: MUTED, fontFamily: sans }}>
    src/components/AdminPanel.jsx:49|<h2 style={{ margin: 0, fontFamily: serif_, fontSize: FS.lg, fontWeight: 600, color: INK, flex: 1 }}>
    src/components/AiAnalystPanel.jsx:109|const dockPos = { position: 'fixed', left: SP.lg, bottom: SP.lg, zIndex: 60, fontFamily: sans };
    src/components/AnonTierTeaser.jsx:66|fontFamily: serif_, fontSize: FS.lg, fontWeight: 600,
    src/components/AuthModal.jsx:68|<h2 id="auth-modal-title" style={{ margin: 0, fontSize: FS.xl + 1, fontFamily: serif_, fontWeight: 600 }}>
    src/components/BuyThisDossier.jsx:302|gap: 6, fontFamily: sans,
    src/components/CampaignSyncBanner.jsx:76|fontFamily: sans, fontSize: FS.sm, fontWeight: 700,
    src/components/ChroniclePanel.jsx:106|fontFamily: 'Nunito, sans-serif', letterSpacing: '0.06em', textTransform: 'uppercase',

### 1  src/domain/autonomy/accelerationOps.js  ::  describeNudge()
  paths: describeNudge()
  ex: "{type} at {originSettlementId} (severity 0%)"
  consumers(3):
    src/components/surveyor/AutonomyPanel.jsx:33|buildNudgeOp, describeNudge,
    src/domain/autonomy/index.js:22|validateNudge, buildNudgeOp, describeNudge,
    tests/domain/autonomy/accelerationOps.test.js:13|validateNudge, buildNudgeOp, describeNudge,

### 1  src/domain/autonomy/index.js  ::  evaluateStopCondition()
  paths: evaluateStopCondition().errors[0]
  ex: "a StopCondition carries version: 1"
  consumers(5):
    src/domain/autonomy/stopConditions.js:218|export function evaluateStopCondition(condition, frame) {
    src/domain/worldPulse/npcLadderGoals.js:9|* a goal's condition IS a StopCondition; evaluateStopCondition gives the non-short-
    src/lib/surveyorAutonomy.js:19|clampAutonomousWeeks, prepareSignalFrame, evaluateStopCondition,
    tests/domain/autonomy/autonomousRun.test.js:17|import { evaluateStopCondition } from '../../../src/domain/autonomy/stopConditions.js';
    tests/domain/autonomy/stopConditions.test.js:12|validateStopCondition, evaluateStopCondition, describeStopCondition,

### 1  src/domain/briefs/citations.js  ::  ENGINE_DOES_NOT_RECORD
  paths: ENGINE_DOES_NOT_RECORD
  ex: "the engine does not record this"
  consumers(2):
    src/domain/briefs/index.js:13|ENGINE_DOES_NOT_RECORD,
    tests/domain/aiAnalyst.test.js:25|buildAnalystPrompt, aiOperationLogRecord, fnv1a32, bundleIsPlayerSafe, ENGINE_DOES_NOT_RECORD,

### 1  src/domain/content/contentEffectProjection.js  ::  contentFieldTruth()
  paths: contentFieldTruth().explanation
  ex: "This field is outside the registered content vocabulary."
  consumers(3):
    src/components/compendium/CustomContentEditor.jsx:18|contentFieldTruth,
    src/components/contentStudio/ContentDraftEntry.jsx:11|import { contentFieldTruth } from '../../domain/content/contentEffectProjection.js';
    tests/domain/contentEffectProjection.test.js:3|contentFieldTruth,

### 1  src/domain/content/customSupplyChainPresentation.js  ::  customSupplyChainReasonText()
  paths: customSupplyChainReasonText()
  ex: "{component} is not currently available."
  consumers(0):

### 1  src/domain/customContent.js  ::  classifyCustomEntity()
  paths: classifyCustomEntity().contributors[0].reason
  ex: "\"{name}\" classified as other institution."
  consumers(3):
    src/domain/settlement.schema.js:1609|* domain/customContent.js#classifyCustomEntity. Lets user prose flow
    tests/domain/customContent.test.js:10|classifyCustomEntity,
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:669|- Evidence: classifyCustomEntity/classifyCustomInstitution/inferCustomEntityType have zero src consumers (grep: only settlement.schema.js doc text and its o

### 1  src/domain/customContentSchema.js  ::  ECONOMIC_WEIGHT
  paths: ECONOMIC_WEIGHT[3].label
  ex: "Backbone of the economy"
  consumers(1):
    src/components/compendium/CustomItemAttributes.jsx:13|CRITICALITY, ECONOMIC_WEIGHT, DEFENSE_ROLES, FOOD_IMPACT, POWER_AUTHORITIES,

### 1  src/domain/customContentSchema.js  ::  POWER_AUTHORITIES
  paths: POWER_AUTHORITIES[4].label
  ex: "Civic / legal authority"
  consumers(1):
    src/components/compendium/CustomItemAttributes.jsx:13|CRITICALITY, ECONOMIC_WEIGHT, DEFENSE_ROLES, FOOD_IMPACT, POWER_AUTHORITIES,

### 1  src/domain/customContentSchema.js  ::  validateDeity()
  paths: validateDeity().errors[4]
  ex: "portfolio must be free text (a string)."
  consumers(12):
    src/domain/content/customContentManifest.generated.js:1489|"validateDeity"
    src/domain/worldPulse/deityAxes.js:141|* write-time walls (validateDeity, the admission projection, migration 200's CHECK)
    tests/domain/customContentConsumerEvidence.walker.test.js:148|validateDeity: 'src/domain/customContentSchema.js',
    tests/domain/customContentTraditions.test.js:3|* custom-content bucket: the validator (validateTradition, the validateDeity template),
    tests/domain/deityAuthoredCharacter.test.js:22|*      store actually calls) and the compat validator `validateDeity`.
    tests/lib/contentPacks.test.js:6|import { validateDeity } from '../../src/domain/customContentSchema.js';
    tests/security/customContentDeities.pglite.test.js:12|*      axis (mirroring validateDeity) and ACCEPTS a valid one. NON-deity rows are
    docs/PHASE4_FAITH_DELTA.md:905|authoring UI in W-F5/W-F6; validateDeity gains the optional field, no CHECK constraint).
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:8144|- authority: `customContent.deities` (account library or campaign pinned binding — activeCustomContentContext); schema src/domain/customContentSchema.js:154-19
    docs/UX_OVERHAUL_PLAN.md:243|- **Reuse & sharing** — export/import content packs (re-run `validateDeity` + re-namespace refIds on import) + premium "Share to Gallery as a content pack".

### 1  src/domain/customContentSchema.js  ::  validateTradition()
  paths: validateTradition().errors[2]
  ex: "epithet must be free text (a string)."
  consumers(2):
    tests/build/customRegistryLazy.test.js:18|*      keeps; 'motifElement must be one of', validateTradition's error prose).
    tests/domain/customContentTraditions.test.js:3|* custom-content bucket: the validator (validateTradition, the validateDeity template),

### 1  src/domain/display/deityEffects.js  ::  deityPressureOf()
  paths: deityPressureOf().receipt
  ex: "No supported local patron domain changes this court's bar for war."
  consumers(10):
    src/domain/causalState.js:1067|// through `dispositionProfile.deityPressureOf` AND lift defense readiness here — one
    src/domain/worldPulse/deityFlaws.js:216|* `dispositionProfile.deityPressureOf` reads loss history as losses exceeding wins.
    src/domain/worldPulse/dispositionNews.js:10|import { deityPressureOf } from './dispositionProfile.js';
    src/domain/worldPulse/dispositionProfile.js:98|* of `deityPressureOf`'s already-computed output. So the law is enforceable as a
    src/domain/worldPulse/settlementStrategy.js:53|import { deityPressureOf, thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/warTermination.js:53|import { deityPressureOf, thresholdFactorOf } from './dispositionProfile.js';
    tests/domain/customContentMechanicalClaims.test.js:34|import { deityPressureOf } from '../../src/domain/worldPulse/dispositionProfile.js';
    tests/domain/deityTemperConsumerCensus.walker.test.js:631|expect(news).toContain('deityPressureOf');
    tests/domain/dispositionChannels.test.js:15|deityPressureOf,
    tests/domain/mandateTemperReach.test.js:20|import { deityPressureOf } from '../../src/domain/worldPulse/dispositionProfile.js';

### 1  src/domain/display/discourseKernel.js  ::  CONNECTIVE_LEXICON
  paths: CONNECTIVE_LEXICON.anticipatory[1]
  ex: "Against what was coming:"
  consumers(3):
    tests/domain/discourseKernel.test.js:22|CONNECTIVE_LEXICON, realizationCandidateId,
    tests/lint/discourseLexiconCoverage.test.js:20|KNOWN_RELATION_TYPES, RELATION_FOR_TYPE, CONNECTIVE_LEXICON, DEFAULT_CONNECTIVE, ALL_CONNECTIVES,
    tests/simulation/discourseParity.test.js:38|import { realizeCauseWalk, ALL_CONNECTIVES, CONNECTIVE_LEXICON, realizationCandidateId } from '../../src/domain/display/discourseKernel.js';

### 1  src/domain/display/dossierViewModel.js  ::  deriveBlockadeRelief()
  paths: deriveBlockadeRelief().display
  ex: "The blockade is biting: no magical channel runs it, and the import share of need goes unmet."
  consumers(9):
    src/components/dossier/EngineSections.jsx:8|* All read pure display read-models (deriveBlockadeRelief, deriveCausalState,
    src/domain/display/defenseDisplay.js:253|// reuses the food-stockpile blockade-relief plumbing (deriveBlockadeRelief).
    src/domain/fieldManifest.js:374|consumers: ['src/domain/display/dossierViewModel.js'], // Wave 8: deriveBlockadeRelief
    src/domain/worldPulse/foodStockpile.js:312|// blockade did or didn't bite (deriveBlockadeRelief reads it).
    tests/domain/display/dossierViewModel.test.js:7|deriveBlockadeRelief,
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:364|- Why: Fail-closed so no leak, but every public/gallery/anon dossier silently loses the food-deficit attribution (magicFoodNote) and the chain notes (magicN
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:2859|- destination: retired — with rationale recorded (G-2b inventory rule: "retired-with-rationale", GAME_GRADE_PROGRAM.md:668-671); its read-models (deriveBlockad
    docs/UX_OVERHAUL_PLAN.md:5|> **Author's correction to the source audits:** the audits repeatedly cite `src/components/settlement/CausalViewTabs.jsx` as a "fully built but mounted-nowhere" orphan to wi
    docs/review-r2/VERIFY_DIMS_RESULTS.json:444|"correction": "Two minor imprecisions, neither material: (1) the finding lists 'navalLayer' as an engine consumer, but navalLayer only references navalStren

### 1  src/domain/display/dossierViewModel.js  ::  deriveExportPosture()
  paths: deriveExportPosture().label
  ex: "Entrepôt: re-exports transit goods"
  consumers(11):
    src/components/new/economyDeskRead.js:28|* own. `deriveExportPosture` (the §1d export read-model) and `deriveNotableAbsences` (the
    src/data/dossierStateProse/economy.generated.js:1557|"title": "DS-ECO-10: LADDER › export posture, market scarcity, and the freshness caveat · `deriveExportPosture(settlement){status, exports, count, 
    src/domain/display/exportPosture.js:7|* pipeline — needs exactly one display derivation: deriveExportPosture. While
    src/domain/display/stateProse/economyStateProse.js:614|* DS-ECO-10 lens A — the EXPORT POSTURE, keyed on `deriveExportPosture`'s own status token.
    src/domain/state/deriveSystemState.js:30|import { deriveExportPosture } from '../display/exportPosture.js';
    tests/build/vendorPdfLazy.test.js:269|//   • dossier read-model split     −33,487 → 1,361,308. deriveExportPosture —
    tests/domain/display/dossierViewModel.test.js:4|deriveExportPosture,
    tests/domain/economyStateProseDesk.test.js:43|import { deriveExportPosture } from '../../src/domain/display/exportPosture.js';
    docs/A_PLUS_ROADMAP.md:907|- **Change:** src/domain/state/deriveSystemState.js:109-111 — replace `const exportCount = flag('canonicalViewModel') ? deriveExportPosture(s).count : canonExports(s).length
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:9630|- economicState.primaryExports/exports count via deriveExportPosture — deriveSystemState.js:155-164; src/domain/display/exportPosture.js:45-65

### 1  src/domain/display/dossierViewModel.js  ::  deriveViability()
  paths: deriveViability().summary
  ex: "VIABLE: economically self-sufficient and historically plausible."
  consumers(9):
    src/components/new/tabs/ViabilityTab.jsx:6|import { deriveViability } from '../../../domain/display/dossierViewModel.js';
    src/domain/display/parityContract.js:102|// (both render deriveViability().summary, behind canonicalViewModel). What
    src/domain/fieldManifest.js:245|+ 'and every render site says so (first-survey framing). deriveViability reconciles the '
    src/pdf/lib/viewModel.js:27|import { deriveFoodBalance, deriveViability } from '../../domain/display/dossierViewModel.js';
    tests/domain/display/dossierViewModel.test.js:5|deriveViability,
    tests/pdf/parityContractExemptions.test.js:208|{ kind: 'settlement', path: 'economicViability.summary', expect: 'EXCLUDED', why: 'Read-time reconciliation replaces this field on BOTH surfaces (viabili
    docs/A_PLUS_ROADMAP.md:674|- **Change:** The divergence SOURCE is that the web reads the raw engine struct while the PDF reads the convergence layer. EconomicsTab.jsx:189 `const fb = via?.metrics?.foo
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:1701|- authority: `settlement.economicViability` + `settlement.structuralViolations`; behind `flag('canonicalViewModel')` the verdict routes through `deriveViabilit
    docs/review-r2/VERIFY_SUBSYSTEMS_RESULTS.json:920|"reasoning": "The finding's premise — that fb.deficitPercent is the \"gross, pre-import\" number diverging from the canonical residual — is stale. It 

### 1  src/domain/display/humanizeEngineTokens.js  ::  tickCalendarDetailLabel()
  paths: tickCalendarDetailLabel()
  ex: "week 1 of spring, year 1"
  consumers(4):
    src/components/gallery/CampaignStatePanel.jsx:25|import { tickCalendarDetailLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/components/map/ChronicleScrollback.jsx:27|import { tickCalendarDetailLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/components/map/WizardNewsPanel.jsx:5|import { humanizeToken, tickCalendarDetailLabel } from '../../domain/display/humanizeEngineTokens.js';
    tests/domain/humanizeEngineTokens.test.js:17|tickCalendarDetailLabel,

### 1  src/domain/display/humanizeEngineTokens.js  ::  tickCalendarLabel()
  paths: tickCalendarLabel()
  ex: "the spring of year 1"
  consumers(19):
    src/components/map/AdvanceReport.jsx:31|tickCalendarLabel,
    src/components/map/CauseWalkPanel.jsx:18|import { tickCalendarLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/components/map/ChroniclersLetterPanel.jsx:34|import { tickCalendarLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/components/map/WarFaithMapOverlay.jsx:37|import { tickCalendarLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/components/map/gatheredDocket.js:36|import { tickCalendarLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/domain/display/chroniclersLetter.js:27|import { humanizeFlagKey, tickCalendarLabel } from './humanizeEngineTokens.js';
    src/domain/display/discourseKernel.js:29|*      register) with tickCalendarLabel for the spoken opening time.
    src/pdf/sections/FaithWar.jsx:25|import { tickCalendarLabel, tickDurationLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/pdf/sections/PowerStructure.jsx:19|import { tickCalendarLabel } from '../../domain/display/humanizeEngineTokens.js';
    src/utils/generateWorldBook.js:36|import { tickCalendarLabel } from '../domain/display/humanizeEngineTokens.js';

### 1  src/domain/display/neighbourMirror.js  ::  MIRROR_BAND_WORDS
  paths: MIRROR_BAND_WORDS.unknown
  ex: "nothing we can account for"
  consumers(4):
    tests/domain/neighbourMirror.test.js:23|MIRROR_BASIS_WORDS, MIRROR_BAND_WORDS, MIRROR_STALENESS_WORDS, neighbourMirrorLines,
    tests/ui/neighbourMirrorLine.test.js:37|NEIGHBOUR_MIRROR_HEADING, MIRROR_BAND_WORDS, neighbourMirrorLines,
    docs/implementation/packets/foreign-policy/IN-1B.md:607|MIRROR_BAND_WORDS                 // Object.freeze({...})  MIRROR_BANDS member -> one player-facing phrase
    docs/implementation/packets/foreign-policy/IN-1C-A.md:96|| **IN-1b** | `src/domain/display/neighbourMirror.js` (**79 eff / 188 raw**) exports `NEIGHBOUR_MIRROR_HEADING`, `MIRROR_BAND_WORDS`, `MIRROR_S

### 1  src/domain/display/neighbourMirror.js  ::  MIRROR_STALENESS_WORDS
  paths: MIRROR_STALENESS_WORDS.unknown
  ex: "on no date our record holds"
  consumers(3):
    tests/domain/neighbourMirror.test.js:23|MIRROR_BASIS_WORDS, MIRROR_BAND_WORDS, MIRROR_STALENESS_WORDS, neighbourMirrorLines,
    docs/implementation/packets/foreign-policy/IN-1B.md:608|MIRROR_STALENESS_WORDS            // Object.freeze({...})  MIRROR_STALENESS_BANDS member -> one phrase
    docs/implementation/packets/foreign-policy/IN-1C-A.md:96|| **IN-1b** | `src/domain/display/neighbourMirror.js` (**79 eff / 188 raw**) exports `NEIGHBOUR_MIRROR_HEADING`, `MIRROR_BAND_WORDS`, `MIRROR_S

### 1  src/domain/display/neighbourMirror.js  ::  NEIGHBOUR_MIRROR_HEADING
  paths: NEIGHBOUR_MIRROR_HEADING
  ex: "What the neighbours have been shown"
  consumers(4):
    src/components/new/tabs/RelationshipsTab.jsx:11|import { NEIGHBOUR_MIRROR_HEADING, neighbourMirrorLines } from '../../../domain/display/neighbourMirror.js';
    tests/ui/neighbourMirrorLine.test.js:37|NEIGHBOUR_MIRROR_HEADING, MIRROR_BAND_WORDS, neighbourMirrorLines,
    docs/implementation/packets/foreign-policy/IN-1B.md:606|NEIGHBOUR_MIRROR_HEADING          // 'What the neighbours have been shown'   (CR-IN1B-3, exact)
    docs/implementation/packets/foreign-policy/IN-1C-A.md:96|| **IN-1b** | `src/domain/display/neighbourMirror.js` (**79 eff / 188 raw**) exports `NEIGHBOUR_MIRROR_HEADING`, `MIRROR_BAND_WORDS`, `MIRROR_S

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  DEF8_UNREACHABLE_POOLS
  paths: DEF8_UNREACHABLE_POOLS[0]
  ex: "multiple stresses, one posture shown"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:24|militaryOverridePoolKey, viabilityUnderStressPoolKey, DEF8_UNREACHABLE_POOLS,

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  charterPoolKey()
  paths: charterPoolKey()
  ex: "charter hall PRESENT (specialist monster response)"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:18|beastsRowPoolKey, charterPoolKey, contractedForcePoolKey, countryWarrantsCharter,

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  contractedForcePoolKey()
  paths: contractedForcePoolKey()
  ex: "mercenary / contracted forces PRESENT"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:18|beastsRowPoolKey, charterPoolKey, contractedForcePoolKey, countryWarrantsCharter,

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  disasterRowPoolKey()
  paths: disasterRowPoolKey()
  ex: "Disasters & Famine: granary AND hospital"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:19|defenseForcesProse, defenseStateProse, defenseThreatProse, disasterRowPoolKey,

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  internalRowPoolKey()
  paths: internalRowPoolKey()
  ex: "Internal Security: full legal chain (court AND prison)"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:21|fortificationPoolKey, internalRowPoolKey, invasionRowPoolKey, isCompoundSafetyLabel,

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  invasionRowPoolKey()
  paths: invasionRowPoolKey()
  ex: "Invasion & War: walls AND professional garrison"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:21|fortificationPoolKey, internalRowPoolKey, invasionRowPoolKey, isCompoundSafetyLabel,

### 1  src/domain/display/stateProse/defenseStateProse.js  ::  navalDefensePoolKey()
  paths: navalDefensePoolKey()
  ex: "Naval Defense: Under blockade"
  consumers(1):
    tests/domain/defenseStateProseDesk.test.js:30|navalDefensePoolKey, supplyLogisticsPoolKey,

### 1  src/domain/display/stateProse/economyStateProse.js  ::  prosperityHeaderPoolKey()
  paths: prosperityHeaderPoolKey()
  ex: "COMBINATION C3: the middle rungs"
  consumers(1):
    tests/domain/economyStateProseDesk.test.js:40|prosperityHeaderPoolKey,

### 1  src/domain/display/stateProse/generalStateProse.js  ::  originTierPoolKey()
  paths: originTierPoolKey()
  ex: "tier overlay: other tiers"
  consumers(1):
    tests/domain/generalStateProseDesk.test.js:41|originTierPoolKey,

### 1  src/domain/display/stateProse/generalStateProse.js  ::  situationPoolKey()
  paths: situationPoolKey()
  ex: "ordinary (route road and the default)"
  consumers(1):
    tests/domain/generalStateProseDesk.test.js:61|situationPoolKey,

### 1  src/domain/display/stateProse/generalStateProse.js  ::  viabilityVerdictPoolKey()
  paths: viabilityVerdictPoolKey()
  ex: "the MARGINAL arm: neither verdict returned"
  consumers(1):
    tests/domain/generalStateProseDesk.test.js:48|viabilityVerdictPoolKey,

### 1  src/domain/display/stateProse/powerStateProse.js  ::  legitimacyReadingPoolKey()
  paths: legitimacyReadingPoolKey()
  ex: "neutral baseline (nothing pulling either way)"
  consumers(1):
    tests/domain/powerStateProseDesk.test.js:45|capturePoolKey, legitimacyHoldPoolKey, legitimacyReadingPoolKey,

### 1  src/domain/display/stateProse/powerStateProse.js  ::  politicsPresencePoolKey()
  paths: politicsPresencePoolKey()
  ex: "layer DORMANT (no ledger materialized)"
  consumers(1):
    tests/domain/powerStateProseDesk.test.js:47|politicsPresencePoolKey, powerLadderRung, riskPoolKey, rulingPowerPoolKey,

### 1  src/domain/display/stateProse/stressorsStateProse.js  ::  conditionDirectionPoolKey()
  paths: conditionDirectionPoolKey()
  ex: "DIRECTION: stable, and the FLAT case (no valid directional status)"
  consumers(1):
    tests/domain/stressorsStateProseDesk.test.js:24|conditionDirectionPoolKey, conditionDurationPoolKey, conditionProvenancePoolKey,

### 1  src/domain/display/warAndRoadNames.js  ::  deriveWarName()
  paths: deriveWarName().line
  ex: "the {nameFor}–{nameFor} War: {nameFor} against {nameFor}."
  consumers(2):
    src/domain/display/warRemembrance.js:67|import { deriveWarName, pinnedWarReasonOf } from './warAndRoadNames.js';
    tests/domain/display/warAndRoadNames.test.js:9|deriveWarName,

### 1  src/domain/npc/characterEdit.js  ::  CHART_WRITER_SEAM
  paths: CHART_WRITER_SEAM.note
  ex: "UNLANDED on this base and therefore NAMED, never imported. At the reconcile the reader and this writer are the one modul"
  consumers(1):
    tests/domain/npc/characterEdit.test.js:28|CHART_WRITER_SEAM,

### 1  src/domain/npc/characterReadModel.js  ::  DECAY_CLAUSE
  paths: DECAY_CLAUSE
  ex: "the slow work of years"
  consumers(1):
    tests/domain/npc/characterReadModel.test.js:53|DECAY_CLAUSE,

### 1  src/domain/npc/livedExperienceCatalog.js  ::  EXPERIENCE_TABLE
  paths: EXPERIENCE_TABLE.ordinary_day.receipt
  ex: "n/a - teaches nothing"
  consumers(9):
    src/domain/npc/livedExperienceFunnel.js:592|// Behaviour-neutral at this commit: NO row in EXPERIENCE_TABLE carries the field, so
    src/domain/npc/livedExperienceSources.js:77|EXPERIENCE_TABLE,
    src/domain/worldPulse/faithWitnessSource.js:17|* `foldLivedExperience`, `EXPERIENCE_TABLE` and `livedExperienceSources.js` live on
    tests/domain/faithWitnessSource.test.js:26|EXPERIENCE_TABLE,
    tests/domain/npc/characterDrift.test.js:523|'EXPERIENCE_TABLE', 'LIVED_EXPERIENCE_KINDS', 'AMBIENT_EXPERIENCE_KINDS',
    tests/domain/npc/livedExperienceCatalog.test.js:39|EXPERIENCE_TABLE,
    tests/domain/npc/livedExperienceFunnel.test.js:72|import { SOURCE_UNVERIFIED_KINDS, EXPERIENCE_TABLE } from '../../../src/domain/npc/livedExperienceCatalog.js';
    tests/domain/npc/livedExperienceSources.test.js:63|EXPERIENCE_TABLE,
    tests/domain/npc/paradigmAxisCatalog.test.js:43|EXPERIENCE_TABLE,

### 1  src/domain/npc/npcBank.js  ::  validateNpcFacet()
  paths: validateNpcFacet().reason
  ex: "unknown facet kind \"{a}\""
  consumers(6):
    src/domain/npc/npcFacetContract.js:129|export function validateNpcFacet(facetKind, value) {
    src/domain/npc/npcOps.js:28|validateNpcFacet, goalChainForRole, npcFacetOf,
    src/domain/pendingEditIntents.js:20|import { validateNpcFacet } from './npc/npcFacetContract.js';
    src/store/settlementPendingEditWriters.js:11|import { validateNpcFacet } from '../domain/npc/npcFacetContract.js';
    tests/domain/npc/npcBank.test.js:14|npcFacetOf, isBankValid, validateNpcFacet, bankVocabulary,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:2460|- authority: enum vocabulary src/domain/npc/npcFacetContract.js (`validateNpcFacet`, imported by both admission and writer — pendingEditIntents.js:20, settleme

### 1  src/domain/regionalGraph.js  ::  deriveRegionalLink()
  paths: deriveRegionalLink().contributors[0].reason
  ex: "Neighbour \"{name}\" classified as other."
  consumers(2):
    tests/domain/regionRelationshipB06Fixes.test.js:28|import { deriveRegionalLink } from '../../src/domain/regionalGraph.js';
    tests/domain/regionalGraph.test.js:8|deriveRegionalLink,

### 1  src/domain/regionalGraph.js  ::  summarizeRegional()
  paths: summarizeRegional()[0]
  ex: "No structured regional neighbours."
  consumers(1):
    tests/domain/regionalGraph.test.js:11|summarizeRegional,

### 1  src/domain/roads/migrationReason.js  ::  MIGRATION_CAUSE
  paths: MIGRATION_CAUSE
  ex: "A column of folk leaving a shed settlement"
  consumers(0):

### 1  src/domain/roads/ops.js  ::  applyRoadsPartyRelease()
  paths: applyRoadsPartyRelease().reason
  ex: "unknown roads op \"{c}\""
  consumers(1):
    tests/domain/roadsParty.test.js:14|applyRoadsPartyRelease, isRoadsHostage, captorOfHostage, roadsRescueImpactAction, ROADS_RELEASE_MODE,

### 1  src/domain/rulingPowerCoup.js  ::  COUP_RISK_LABELS
  paths: COUP_RISK_LABELS[3]
  ex: "Critical. The seat could fall"
  consumers(2):
    src/domain/display/stateProse/powerStateProse.js:505|* @param {string|null|undefined} riskLabel a value of COUP_RISK_LABELS
    tests/domain/powerStateProseDesk.test.js:37|import { COUP_RISK_LABELS, coupRiskLabel } from '../../src/domain/rulingPowerCoup.js';

### 1  src/domain/spatial/calamity.js  ::  stampTitle()
  paths: stampTitle()
  ex: "The Great Calamity of {a}, year {b}"
  consumers(4):
    src/domain/worldPulse/eventProse.js:204|(x) => `The Great Calamity of ${x.name}, year ${x.year}`,          // canonical (== stampTitle)
    tests/domain/calamity.test.js:28|stampTitle,
    tests/domain/eventProse.test.js:21|import { stampTitle } from '../../src/domain/spatial/calamity.js';
    docs/GENERATION_TIME_SHIFT_MAP.md:105|- `calamity.test.js:119-120` pin the pure `stampTitle` — **left UNCHANGED (canonical)**;

### 1  src/domain/spatial/dispatchEV.js  ::  emboldenedReceipt()
  paths: emboldenedReceipt()
  ex: "The merchants are emboldened by the {a} run. A risky delivery paid off."
  consumers(2):
    src/domain/worldPulse/supplyKernel.js:42|believedDestinationDanger, merchantBaseline, emboldenedReceipt,
    tests/domain/dispatchEV.test.js:21|appetiteOf, stepAppetite, emboldenedReceipt, dispatchDecision,

### 1  src/domain/spatial/generosityEV.js  ::  GENEROSITY_INSTRUMENTS
  paths: GENEROSITY_INSTRUMENTS.warning.note
  ex: "Statecraft §2.4 GIVE lane: warning an ally, priced by the SACRIFICE of the telling (strategic advantage spent + eyes exp"
  consumers(1):
    tests/domain/generosityEV.test.js:13|GENEROSITY_TUNING, VERDICTS, GENEROSITY_INSTRUMENTS,

### 1  src/domain/spatial/generosityEV.js  ::  generosityReceipt()
  paths: generosityReceipt()
  ex: "Grain went to {receiverName}: their ground shields {giverName}'s flank: the granary opened as defense spending."
  consumers(1):
    tests/domain/generosityEV.test.js:16|generosityEV, generosityReceipt, triageAllocation, refugeAcceptance, purchaseFallThrough,

### 1  src/domain/spatial/index.js  ::  PORT_ADVISORY_PHRASE
  paths: PORT_ADVISORY_PHRASE.unpartnered_harbour
  ex: "a harbour with no counterpart across the water"
  consumers(2):
    src/domain/spatial/seaLanes.js:570|export const PORT_ADVISORY_PHRASE = Object.freeze({
    tests/domain/seaLanes.test.js:20|PORT_ADVISORY_PHRASE,

### 1  src/domain/spatial/supplyShipments.js  ::  starvationReceipt()
  paths: starvationReceipt()
  ex: "{institutionName} starves: the {input} road is cut (every supplier severed) under {cause}."
  consumers(4):
    src/domain/spatial/commodityFlow.js:77|import { supplyActive, routeIntercepted, routeWeeks, pickSource, starvationReceipt } from './supplyShipments.js';
    tests/domain/supplyShipments.test.js:28|starvationReceipt,
    docs/DESIGN_FP_ARCH_TR.md:32|| S4 | M2 shipments: K-ranked producers, hostile-gate cut, starvation latch + mandatory receipt | `supplyShipments.js:227 starvationReceipt` (exact); total-cut latch :261-
    docs/DESIGN_FP_COUPLINGS.md:297|**RECEIPTS + DOSSIER:** starvationReceipt is mandatory and causal ("the smithy

### 1  src/domain/state/deriveSystemState.js  ::  deriveSystemState()
  paths: deriveSystemState().volatility.drivers[0]
  ex: "Few factions: concentrated power"
  consumers(90):
    src/components/library/LibraryToolbar.jsx:45|// severity is the worst 4-dim health band (deriveSystemState via healthPip);
    src/components/new/SummaryTabV2.jsx:109|there is no settlement at all — deriveSystemState is total, so a
    src/components/settlement/ReadSystemStateBar.jsx:5|* dimensions from the settlement itself (deriveSystemState — pure, no store, no
    src/components/settlements/HealthPip.jsx:4|* health band (from deriveSystemState via healthPip()), paired with the
    src/components/settlements/livingWorldSignals.js:15|*   - deriveSystemState                   (4-dim resilience → health pip)
    src/domain/causalState.js:25|*   - This file does NOT replace `domain/state/deriveSystemState.js`.
    src/domain/construct/configVocabulary.js:75|/** The four constraint dimensions (the systemState axes deriveSystemState produces). */
    src/domain/construct/intentComparator.js:9|* here is where the draft deviates." It reuses the existing read-models (deriveSystemState),
    src/domain/counterfactual.js:50|import { deriveSystemState } from './state/deriveSystemState.js';
    src/domain/display/dossierViewModel.js:14|*   - Current State (src/domain/state/deriveSystemState.js) read

### 1  src/domain/tableEvents.js  ::  tableEventToNewsEntry()
  paths: tableEventToNewsEntry().headline
  ex: "{kind} at the table ({band})"
  consumers(5):
    src/store/campaignSlice.js:40|import { tableEventToNewsEntry } from '../domain/tableEvents.js';
    tests/domain/tableEvents.test.js:23|tableEventToNewsEntry,
    tests/domain/tableEventsNoFreeText.test.js:33|describe('the news-projection apply path (tableEventToNewsEntry)', () => {
    tests/security/aiSurfaceSourceScan.test.js:294|const applyBody = bodyOfFn(tableEvents, 'tableEventToNewsEntry');
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:10471|3. **user-driven table backfill** — `importTableEvents` (`src/store/campaignSlice.js:899-913`) maps confirmed `TableEventRecord`s through `tableEventToNewsEnt

### 1  src/domain/tableLedger.js  ::  validateTableEvent()
  paths: validateTableEvent().errors[0]
  ex: "kind must be one of: incident, stressor-relief, obligation, exposure, structure-harm, structure-restored, supply-loss, s"
  consumers(8):
    src/components/tableLedger/TableLedgerPanel.jsx:23|validateTableEvent, buildTableEffect, exposureTargets,
    src/domain/tableEvents.js:112|* value for an unknown band — but validateTableEvent rejects unknown bands before
    src/lib/campaignImport.js:22|validateTableEvent,
    tests/domain/tableEvents.test.js:18|validateTableEvent,
    tests/domain/tableLedger.test.js:13|validateTableEvent, buildTableEffect, reviewClerkProposals, exposureTargets,
    tests/store/pendingEditTransaction.test.js:18|import { buildTableEffect, validateTableEvent } from '../../src/domain/tableLedger.js';
    tests/store/tableEventCommit.test.js:24|validateTableEvent, buildTableEffect, TABLE_EVENT_SOURCE,
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:10725|- authority: `payload.record.flavor` on a `table-event` pending edit; schema wall `validateTableEvent` (`src/domain/tableLedger.js:92-153`)

### 1  src/domain/threatProfile.js  ::  summarizeThreats()
  paths: summarizeThreats()[0]
  ex: "No threats currently pressing the settlement."
  consumers(1):
    tests/domain/threatProfile.test.js:21|*     summarizeThreats.

### 1  src/domain/townMap/siteGenesis.js  ::  generateSite()
  paths: generateSite().prov[0].sourceRef
  ex: "the coast (sea trade)"
  consumers(7):
    src/domain/townMap/townLayoutV2.js:57|generateSite, responseModeFor, nucleateCore, latentAdvantageMap, reconciliationDisplacement,
    tests/lint/siteCoherenceRatchet.test.js:32|* drift from production silently. The reconstructed `generateSite` bag this file also needs
    scripts/mutation-coverage-manifest.json:2142|"supersededRationale": "SITE COHERENCE WAVE 0 — the identity-keyed contradiction ratchet. Its assertion is an EXACT BIDIRECTIONAL comparison of 43 (terrain
    docs/SITE_COHERENCE_AUDIT.md:3|**Trigger** TCD-4 (`2c1ec70f`) routed `townLayoutV2.js` through `canonExports()`, so `economicState.primaryExports` reaches `siteGenesis.generateSite()` for the first ti
    docs/SITE_COHERENCE_PLAN.md:188|- **Exit criteria** The **47/462 purely-false-positive water cases reach 0** (decisive strings `Preserved foods` 28, `Milled flour` 21, `Milled timber` 6, `Hewn timber`
    docs/implementation/PACKET_MANIFEST.json:2633|"symbol": "generateSite"
    docs/implementation/packets/site-coherence/SCW-0.md:208|| **The site deriver (the subject)** | `src/domain/townMap/siteGenesis.js` | `generateSite(arg)` `:206` | CONFIRMED it takes ONE destructured ba

### 1  src/domain/townScene/index.js  ::  validateTownSceneCompileInput()
  paths: validateTownSceneCompileInput().errors[0]
  ex: "input must be a plain object"
  consumers(3):
    src/domain/townScene/sceneCompileInput.js:300|export function validateTownSceneCompileInput(candidate) {
    tests/domain/townSceneCartography.test.js:44|validateTownSceneCompileInput,
    tests/security/townScenePlayerSafe.test.js:15|validateTownSceneCompileInput,

### 1  src/domain/traditions/genesis.js  ::  describeTraditionWindow()
  paths: describeTraditionWindow()
  ex: "Seedtime, the first week"
  consumers(6):
    src/components/new/tabs/TraditionsTab.jsx:23|import { deriveFoundingTraditions, describeTraditionWindow, motifGlyph } from '../../../domain/traditions/genesis.js';
    src/pdf/sections/Traditions.jsx:22|import { describeTraditionWindow } from '../../domain/traditions/genesis.js';
    tests/architecture/engineWorkerDomFree.test.js:163|expect(bindsLocally(codeOnly('function describeTraditionWindow(window) { return window?.weeks; }'), 'window')).toBe(true);
    tests/components/traditionsTab.test.jsx:11|import { deriveFoundingTraditions, describeTraditionWindow } from '../../src/domain/traditions/genesis.js';
    tests/domain/traditionGenesis.test.js:11|import { deriveFoundingTraditions, describeTraditionWindow } from '../../src/domain/traditions/genesis.js';
    tests/pdf/traditionsSection.test.jsx:14|import { describeTraditionWindow } from '../../src/domain/traditions/genesis.js';

### 1  src/domain/traditions/prose.js  ::  traditionBeatProse()
  paths: traditionBeatProse().summary
  ex: "{name} went well this year in {town}. A town's observances carry its identity forward, and every year they hold or fail "
  consumers(2):
    src/domain/worldPulse/traditionsKernel.js:74|import { traditionBeatProse } from '../traditions/prose.js';
    tests/domain/traditionProse.test.js:10|import { traditionBeatProse, traditionHook, traditionHookCondition } from '../../src/domain/traditions/prose.js';

### 1  src/domain/tuning/autoTunableRegistry.js  ::  validateRegistryEntry()
  paths: validateRegistryEntry().reasons[2]
  ex: "surface must be one of display/rollup/analytics (never sim/generation)"
  consumers(2):
    src/domain/tuning/laneClassifier.js:23|import { AUTO_TUNABLE, validateRegistryEntry } from './autoTunableRegistry.js';
    tests/domain/autoTunableRailsWalker.test.js:20|validateRegistryEntry,

### 1  src/domain/worldPulse/applyWorldPulseOccupationAuthority.js  ::  conquestExtractionCondition()
  paths: conquestExtractionCondition().causes[0].reason
  ex: "{toPowerName} extracts wealth, troops, and authority from the conquered settlement."
  consumers(2):
    src/domain/worldPulse/applyWorldPulse.js:75|import { conquestExtractionCondition, installOccupationAuthority, liftOccupationAuthority } from './applyWorldPulseOccupationAuthority.js';
    tests/fixtures/mechanism-lit-coverage-baseline.json:26|"why": "SEAT-5 gave the module its first standing flag-ON lit proof. tests/domain/occupationLiberationUninstall.test.js drives liftOccupationAuth

### 1  src/domain/worldPulse/applyWorldPulseOccupationAuthority.js  ::  installOccupationAuthority()
  paths: installOccupationAuthority().powerStructure.factions[0].desc
  ex: "A foreign occupation authority installed by conquest."
  consumers(5):
    src/domain/rulingPowerSeat.js:302|* the occupation administration (`installOccupationAuthority` cuts its power and
    src/domain/worldPulse/applyWorldPulse.js:75|import { conquestExtractionCondition, installOccupationAuthority, liftOccupationAuthority } from './applyWorldPulseOccupationAuthority.js';
    tests/domain/occupationLiberationUninstall.test.js:5|installOccupationAuthority,
    tests/fixtures/mechanism-lit-coverage-baseline.json:26|"why": "SEAT-5 gave the module its first standing flag-ON lit proof. tests/domain/occupationLiberationUninstall.test.js drives liftOccupationAuth
    docs/PROPOSAL_ASSESSMENT.md:39|| 8 | occupation states; capped/delayed occupier **benefit** loop + burden | `installOccupationAuthority` (power-transfer) + `vassal_extraction` condition + military dis

### 1  src/domain/worldPulse/applyWorldPulseRelationshipGraph.js  ::  cascadeNewsEntry()
  paths: cascadeNewsEntry().headline
  ex: "{nameFor} and {nameFor}: {fromType} becomes {toType}"
  consumers(1):
    src/domain/worldPulse/applyWorldPulse.js:73|import { relationshipOutcomeDisposition, openCoalitionEnemyRelationship, applyRelationshipLabelToGraph, relationshipEdgeForOutcome, roleOrientedEdge, writeR

### 1  src/domain/worldPulse/applyWorldPulseRelationshipGraph.js  ::  openCoalitionEnemyRelationship()
  paths: openCoalitionEnemyRelationship().worldState.relationshipStates.edge.enemyid.partyid.history[0].reason
  ex: "An allied court answered a live call and opened its own bilateral war edge."
  consumers(1):
    src/domain/worldPulse/applyWorldPulse.js:73|import { relationshipOutcomeDisposition, openCoalitionEnemyRelationship, applyRelationshipLabelToGraph, relationshipEdgeForOutcome, roleOrientedEdge, writeR

### 1  src/domain/worldPulse/attrition.js  ::  applyAttritionToRecord()
  paths: applyAttritionToRecord().reasons[0]
  ex: "The attacking host bled lightly this turn in a {band} engagement, though supply, food and heart between them took much o"
  consumers(4):
    src/domain/certification/subsystemRowsWar.js:292|other: 'Nested under warLayerEnabled (read at warDeployment.js:1280). The ledger seeds from the target\'s fresh homeDefense on the first besieged tick,
    src/domain/worldPulse/warDeployment.js:85|import { applyAttritionToRecord, fortificationStrength } from './attrition.js';
    src/domain/worldPulse/warSiegeVerdict.js:57|* `applyAttritionToRecord` and is never persisted, which is why it was free to fix. The
    tests/domain/statefulArmies.test.js:8|applyAttritionToRecord,

### 1  src/domain/worldPulse/brokerageServices.js  ::  answerBrokerageQuery()
  paths: answerBrokerageQuery().refusal.detail
  ex: "No house in this world keeps a register worth paying for."
  consumers(4):
    src/domain/worldPulse/brokerageServicesRules.js:50|import { answerBrokerageQuery, interceptOutboundRecord } from './brokerageServices.js';
    tests/domain/brokerageIntercept.test.js:47|answerBrokerageQuery,
    tests/domain/brokerageServices.test.js:30|answerBrokerageQuery,
    docs/COMPREHENSIVE_REVIEW_2026-08-01.md:77|- **Finding:** answerBrokerageQuery builds its ReliabilityStamp from a FABRICATED provenance — `{ hopCount: 0, independentSources: 1, credibility01: 1 }` whe

### 1  src/domain/worldPulse/brokerageServices.js  ::  brokerageQueryPrice()
  paths: brokerageQueryPrice().detail
  ex: "The house wants real coin for this, and will want it before it speaks."
  consumers(1):
    tests/domain/brokerageServices.test.js:31|brokerageQueryPrice,

### 1  src/domain/worldPulse/brokerageServices.js  ::  interceptOutboundRecord()
  paths: interceptOutboundRecord().refusal.detail
  ex: "Nothing legible left that house this week."
  consumers(2):
    src/domain/worldPulse/brokerageServicesRules.js:50|import { answerBrokerageQuery, interceptOutboundRecord } from './brokerageServices.js';
    tests/domain/brokerageIntercept.test.js:48|interceptOutboundRecord,

### 1  src/domain/worldPulse/brokerageServicesPlant.js  ::  commissionPlant()
  paths: commissionPlant().refusal.detail
  ex: "No market in this world sells that."
  consumers(3):
    src/domain/worldPulse/brokerageServicesRules.js:51|import { commissionPlant, marketHouseOf } from './brokerageServicesPlant.js';
    tests/domain/brokeragePlant.test.js:28|commissionPlant,
    docs/COMPREHENSIVE_REVIEW_2026-08-01.md:154|- **Finding:** brokerageServicesPlant.js honestly records (PLANT_WIRING, lines 64-87) that the one edit folding commissionPlants() into informationStatecraf

### 1  src/domain/worldPulse/conquestExecution.js  ::  conquestMarginVerdict()
  paths: conquestMarginVerdict().receipt
  ex: "the margin between the victor and the held settlement cannot be measured, and an unmeasured margin is not an overwhelmin"
  consumers(2):
    src/domain/worldPulse/occupation.js:99|conquestMarginVerdict,
    tests/domain/conquestExecutionWr8.test.js:41|conquestMarginVerdict,

### 1  src/domain/worldPulse/conquestExecution.js  ::  inheritedHunger()
  paths: inheritedHunger().receipt
  ex: "nothing the victor holds can be weighed for hunger."
  consumers(2):
    src/domain/worldPulse/occupation.js:102|inheritedHunger,
    tests/domain/conquestExecutionWr8.test.js:44|inheritedHunger,

### 1  src/domain/worldPulse/conquestFeasibility.js  ::  collapseSeatBooksUnderThreat()
  paths: collapseSeatBooksUnderThreat().receipt
  ex: "the seat keeps its own books: the war is not yet its own survival."
  consumers(3):
    tests/domain/conquestFeasibilityWr8.test.js:35|collapseSeatBooksUnderThreat,
    tests/domain/warSeatBooksPartition.test.js:38|collapseSeatBooksUnderThreat,
    docs/FABLE_VALIDATION_QUEUE.md:186|| 2026-08-03 | ⭐ LANE W8-C — the W8-A verifier's six findings, and SLICE 3 (N, CONQUEST EXECUTION) LANDED WHOLE | **TWO COMMITS. THE REPAIRS @ `49e8fd23`, SLICE 3 @ 

### 1  src/domain/worldPulse/conquestFeasibility.js  ::  conquestTermsRange()
  paths: conquestTermsRange().receipt
  ex: "the court has no bargaining range: it cannot judge the war."
  consumers(3):
    tests/domain/conquestFeasibilityWr8.test.js:38|conquestTermsRange,
    tests/domain/warVerdictHeraldWords.test.js:39|import { conquestTermsRange } from '../../src/domain/worldPulse/conquestFeasibility.js';
    docs/FABLE_VALIDATION_QUEUE.md:186|| 2026-08-03 | ⭐ LANE W8-C — the W8-A verifier's six findings, and SLICE 3 (N, CONQUEST EXECUTION) LANDED WHOLE | **TWO COMMITS. THE REPAIRS @ `49e8fd23`, SLICE 3 @ 

### 1  src/domain/worldPulse/conquestFeasibility.js  ::  mistakenFeasibilityReceipt()
  paths: mistakenFeasibilityReceipt().receipt
  ex: "the court held no judgement to be wrong about."
  consumers(4):
    src/domain/worldPulse/razingExecution.js:22|* `conquestMercyReceipt`, `mistakenFeasibilityReceipt`) and nothing in the belief
    tests/domain/conquestFeasibilityWr8.test.js:39|mistakenFeasibilityReceipt,
    tests/domain/razingExecutionWr8.test.js:220|for (const symbol of ['readConquestFeasibilityFor', 'readConquestIntentFor', 'conquestMercyReceipt', 'mistakenFeasibilityReceipt']) {
    docs/FABLE_VALIDATION_QUEUE.md:186|| 2026-08-03 | ⭐ LANE W8-C — the W8-A verifier's six findings, and SLICE 3 (N, CONQUEST EXECUTION) LANDED WHOLE | **TWO COMMITS. THE REPAIRS @ `49e8fd23`, SLICE 3 @ 

### 1  src/domain/worldPulse/conquestFeasibility.js  ::  readConquestFeasibility()
  paths: readConquestFeasibility().receipt
  ex: "the court cannot judge a conquest of its rival: the pair is not two named settlements."
  consumers(4):
    src/domain/worldPulse/conquestDoctrineStage.js:37|readConquestFeasibility,
    tests/domain/conquestFeasibilityWr8.test.js:40|readConquestFeasibility,
    tests/domain/conquestIntentWr8.test.js:46|import { readConquestFeasibility } from '../../src/domain/worldPulse/conquestFeasibility.js';
    docs/DESIGN_FP_ARCH_HB.md:2603|| 1 | `conquestFeasibility.readConquestFeasibility` | as a FOURTH banded ARGUMENT with its own weight, the three legs re-normalized — ⚠ **it CANNOT be imported**: the mo

### 1  src/domain/worldPulse/conquestFeasibility.js  ::  termsRangesOverlap()
  paths: termsRangesOverlap().receipt
  ex: "neither side can price the war, so no range can be compared."
  consumers(2):
    tests/domain/conquestFeasibilityWr8.test.js:41|termsRangesOverlap,
    docs/FABLE_VALIDATION_QUEUE.md:186|| 2026-08-03 | ⭐ LANE W8-C — the W8-A verifier's six findings, and SLICE 3 (N, CONQUEST EXECUTION) LANDED WHOLE | **TWO COMMITS. THE REPAIRS @ `49e8fd23`, SLICE 3 @ 

### 1  src/domain/worldPulse/conquestIntent.js  ::  readConquestIntent()
  paths: readConquestIntent().receipt
  ex: "no conquest intent can be read: the court has no martial disposition on record."
  consumers(4):
    src/domain/worldPulse/conquestDoctrineStage.js:39|import { readConquestIntent } from './conquestIntent.js';
    src/domain/worldPulse/espionage/espionageDoctrine.js:5|* docs/DESIGN_FP_ARCH_ES.md §3.9. The `readConquestIntent` house idiom: closed word
    tests/domain/conquestIntentWr8.test.js:44|readConquestIntent,
    docs/DESIGN_FP_ARCH_ES.md:916|The per-court espionage doctrine is a `readConquestIntent`-shaped PURE leaf

### 1  src/domain/worldPulse/convergence.js  ::  engagementOptions()
  paths: engagementOptions().receipt
  ex: "Committed to the field. The prize is worth the blood."
  consumers(2):
    tests/domain/convergence.test.js:24|deriveSides, engagementOptions, resolveSideBattle, reliefFlipsSiege,
    docs/DESIGN_FP_ARCH_HB.md:2196|| **argmax / argmin over scores** | `convergence.engagementOptions` (⚠ see the addressability note) · `embattlement.chooseRoute` (an ARGMIN over `effectiveCost`, MEASURE

### 1  src/domain/worldPulse/convergence.js  ::  interventionLegitimacy()
  paths: interventionLegitimacy().receipt
  ex: "An army sent to raise up rebels. Every neighbor now reads a ring tightening, and arms."
  consumers(4):
    src/domain/certification/subsystemRowsSeat.js:57|other: 'THE COUNTERPART SEAT: the owner directive (ODQ 735.2) that an occupied or vassal settlement\'s ruling power has "an equal or greater counterpar
    src/domain/worldPulse/seatIntervention.js:203|* entry", and it closes a measured hole: `interventionLegitimacy` computes, rounds and
    tests/domain/convergence.test.js:19|interventionLegitimacy, interventionTilt, interventionObligationMint,
    tests/domain/seatIntervention.test.js:13|*   2. `interventionLegitimacy` computes and rounds a `legitimacyCost` whose only consumer

### 1  src/domain/worldPulse/convergence.js  ::  prizeRivalryCasus()
  paths: prizeRivalryCasus().reason
  ex: "Two banners raced for one crown. The prize itself is now the quarrel between them."
  consumers(1):
    tests/domain/convergence.test.js:25|prizeRivalryCasus, overstayOccupation, canReEngage,

### 1  src/domain/worldPulse/convergence.js  ::  reactiveResponse()
  paths: reactiveResponse().receipt
  ex: "No word of banners on the roads: the muster stands down."
  consumers(3):
    src/domain/worldPulse/convergenceReactive.js:60|export function reactiveResponse({ believed = null, relation = REACTIVE_RELATIONS.ENEMY_COLUMN, myStrength01 = 0.5, exhaustion01 = 0 } = {}) {
    tests/domain/convergence.test.js:27|reactiveResponse, REACTIVE_RELATIONS, contestSideLookup,
    docs/DESIGN_FP_ARCH_HB.md:93|## carried-vs-recomputed (§3a's audit table); `convergence.reactiveResponse` is REFUTED

### 1  src/domain/worldPulse/convergence.js  ::  scoreMotives()
  paths: scoreMotives().receipt
  ex: "A tribute binds the seat to us. A client that falls is an empire that shrinks."
  consumers(1):
    tests/domain/convergence.test.js:17|scoreMotives, MOTIVE_TYPES, INTERVENTION_SIDES,

### 1  src/domain/worldPulse/demographicsLand.js  ::  landLine()
  paths: landLine()
  ex: "Every opening left to {b} lies out beyond the reach of any road."
  consumers(1):
    src/domain/worldPulse/demographicsPlans.js:76|import { chooseLegalSite, landLine, legalFoundingSites } from './demographicsLand.js';

### 1  src/domain/worldPulse/demographicsPlans.js  ::  planLine()
  paths: planLine()
  ex: "{b} does nothing in particular. The work stands at 100 percent and 0 of the stores it needs are raised (100 percent)."
  consumers(0):

### 1  src/domain/worldPulse/demographicsRates.js  ::  DEMOGRAPHIC_TUNING_PROVENANCE
  paths: DEMOGRAPHIC_TUNING_PROVENANCE.status
  ex: "CANDIDATE, OWNER-UNSIGNED (wave P landed the values as the lane's claim, not the owner's word)"
  consumers(1):
    tests/domain/demographicsRates.test.js:27|DEMOGRAPHIC_TUNING_PROVENANCE,

### 1  src/domain/worldPulse/demographicsResponses.js  ::  responseLine()
  paths: responseLine()
  ex: "{b} does nothing in particular."
  consumers(1):
    src/domain/worldPulse/demographicsPlans.js:83|responseLine,

### 1  src/domain/worldPulse/demographicsRisk.js  ::  demographicRiskOf()
  paths: demographicRiskOf().raidReasons[1]
  ex: "and the holding is scattered over open ground"
  consumers(3):
    src/domain/worldPulse/pressureModel.js:13|import { demographicRiskOf, diseaseCouplingReason, raidCouplingReason } from './demographicsRisk.js';
    tests/domain/demographicsWorldsHand.test.js:42|import { demographicRiskOf } from '../../src/domain/worldPulse/demographicsRisk.js';
    docs/COMPREHENSIVE_REVIEW_2026-08-01.md:466|- No hidden governor: demographicsRisk is structurally isolated from realm totals (no import path to the observation module, every term a function of one se

### 1  src/domain/worldPulse/demographicsWar.js  ::  perceivedScarcityClause()
  paths: perceivedScarcityClause()
  ex: "The court believes their granaries are bare and the court is wrong about it."
  consumers(0):

### 1  src/domain/worldPulse/dispositionProfile.js  ::  thresholdFactorOf()
  paths: thresholdFactorOf().receipt
  ex: "No supported disposition channel changes this court's action bar."
  consumers(16):
    src/domain/worldPulse/disposition.js:49|import { thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/dispositionLedger.js:191|* dispositionProfile's `thresholdFactorOf` to its 0.8 floor: the lowest possible bar on
    src/domain/worldPulse/peaceTerms.js:107|import { thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/peaceTermsCoalition.js:17|import { thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/settlementStrategy.js:53|import { deityPressureOf, thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/sovereigntyIntent.js:60|import { thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/strategicPosture.js:49|* `thresholdFactorOf` returns a bounded factor for ONE channel under its own cap, plus a
    src/domain/worldPulse/warCoalitionDecision.js:4|import { thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/warCoalitionRefusal.js:23|import { thresholdFactorOf } from './dispositionProfile.js';
    src/domain/worldPulse/warTermination.js:53|import { deityPressureOf, thresholdFactorOf } from './dispositionProfile.js';

### 1  src/domain/worldPulse/envoyChanceMeetingNews.js  ::  CHANCE_MEETING_EXPOSED_REASON
  paths: CHANCE_MEETING_EXPOSED_REASON
  ex: "a refusal that did not stay private."
  consumers(2):
    tests/domain/impactKindWalkers.test.js:188|// instrument itself (`CHANCE_MEETING_EXPOSED_REASON`), filed at a registry section with its own
    tests/lint/chanceMeetingKindPools.walker.test.js:79|CHANCE_MEETING_EXPOSED_REASON,

### 1  src/domain/worldPulse/envoyChanceMeetingNews.js  ::  CHANCE_MEETING_REASON
  paths: CHANCE_MEETING_REASON
  ex: "a guest and a notable met by chance; what passed between them is now a tie across two courts, and it will fade as ties d"
  consumers(1):
    tests/lint/chanceMeetingKindPools.walker.test.js:83|CHANCE_MEETING_REASON,

### 1  src/domain/worldPulse/envoyTaskCatalog.js  ::  COUNTERPART_SIGHT_LAW
  paths: COUNTERPART_SIGHT_LAW.rule
  ex: "the menu reads what the ENVOY'S COURT KNOWS, never the true chart and never the counterpart's own ledger"
  consumers(1):
    tests/domain/envoyTaskCatalog.test.js:34|COUNTERPART_SIGHT_LAW,

### 1  src/domain/worldPulse/envoyTaskCatalog.js  ::  ENVOY_TASK_CATALOG
  paths: ENVOY_TASK_CATALOG[2].sourceNote
  ex: "WR-7d mints a real claim against a real custody hold, and the hold closes through its own closed reason vocabulary. The "
  consumers(2):
    tests/domain/envoyTaskCatalog.test.js:35|ENVOY_TASK_CATALOG,
    tests/domain/subsystemRowsVirtual.test.js:235|const ENVOY_TASK_CATALOG = 'envoyTaskCatalogEnabled';

### 1  src/domain/worldPulse/espionage/espionageDoctrine.js  ::  readEspionageDoctrine()
  paths: readEspionageDoctrine().receipt
  ex: "No espionage doctrine can be read for this court: no court was named."
  consumers(8):
    src/domain/certification/couplingRegistryEspionage.js:312|* THE COUNTERFORCE is the doctrine leaf's own resolution gate. `readEspionageDoctrine`
    src/domain/worldPulse/espionage/espionageDoctrineStage.js:7|* `readEspionageDoctrine` is a PURE leaf that takes two WORDS and returns words, and it
    tests/domain/espionageDoctrine.test.js:36|readEspionageDoctrine,
    tests/domain/espionageDoctrineStage.test.js:5|* `readEspionageDoctrine` a word: the words come out of `settlementAlignment` over a
    tests/domain/espionageGauntlet.test.js:49|import { readEspionageDoctrine } from '../../src/domain/worldPulse/espionage/espionageDoctrine.js';
    tests/property/espionageDormancyFence.test.js:104|readEspionageDoctrine: (...args) => { calls.doctrine += 1; return actual.readEspionageDoctrine(...args); },
    docs/DESIGN_FP_ARCHITECTURE.md:1275|future claim on the file) + `readEspionageDoctrine`; `espionageMath.js` —
    docs/DESIGN_FP_ARCH_ES.md:922|readEspionageDoctrine({ courtId, orderWord, natureWord })

### 1  src/domain/worldPulse/espionage/espionageDoctrine.js  ::  unknownDoctrine()
  paths: unknownDoctrine().receipt
  ex: "No espionage doctrine can be read for this court: {a}."
  consumers(3):
    src/domain/certification/couplingRegistryEspionage.js:313|* refuses any word outside its two closed tables and mints `unknownDoctrine(why)` instead —
    tests/domain/espionageDoctrine.test.js:37|unknownDoctrine,
    docs/DESIGN_FP_ARCH_ES.md:919|`unknownDoctrine(why)` at the RESOLUTION gate):

### 1  src/domain/worldPulse/espionage/espionageGauntlet.js  ::  captorLeniencyRead()
  paths: captorLeniencyRead().receipt
  ex: "The captor court has no readable doctrine, so its caught guest is treated as any other."
  consumers(2):
    src/domain/worldPulse/operations/missionDispatcher.js:126|* `captorLeniencyRead`, which reads the word to grade a captor's temper and never asks
    tests/domain/espionageGauntlet.test.js:36|captorLeniencyRead,

### 1  src/domain/worldPulse/espionage/espionageProductStage.js  ::  standoffDecisionFor()
  paths: standoffDecisionFor().receipt
  ex: "Pride carried him through the gate: the approach read no worse than his nerve, and only an entered man hears what a cour"
  consumers(1):
    tests/domain/espionageProducts.test.js:68|standoffDecisionFor,

### 1  src/domain/worldPulse/faithField.js  ::  FAITH_UNBOUND_CHANNELS
  paths: FAITH_UNBOUND_CHANNELS.hearth
  ex: "No fertility or household causal variable exists. The word appears only in a founding-myth vocabulary token and in inter"
  consumers(5):
    src/domain/content/customContentManifest.generated.js:1627|"condition": "The settlement causal channel this deity blesses. Pairs with boonStrength; both or neither. MECHANICAL at W-FAITH F4c (ODQ 851)
    src/domain/worldPulse/faithChannelBindings.js:74|* ⚠ THE UNBOUND HALF STAYS IN `faithField.js` (`FAITH_UNBOUND_CHANNELS`), and that is
    src/domain/worldPulse/faithTuningSurface.js:319|Object.freeze({ kind: 'vocabulary', exportName: 'FAITH_UNBOUND_CHANNELS', home: 'src/domain/worldPulse/faithField.js', note: 'The three channels that na
    tests/domain/faithChannelWiring.test.js:41|FAITH_CHANNELS, FAITH_UNBOUND_CHANNELS, FAITH_FIELD_TUNING, faithFieldProjection,
    tests/domain/faithTuningSurface.test.js:300|'FAITH_UNBOUND_CHANNELS',

### 1  src/domain/worldPulse/faithNews.js  ::  faithReceipt()
  paths: faithReceipt().headline
  ex: "{creedName} lost its last altar in {settlementName}; no one there keeps the rite, and the parish roster carries the cree"
  consumers(7):
    src/domain/worldPulse/envoyChanceMeetingNews.js:471|* docblock reserves ("ENC-4 owns those: it exports a single builder on the `faithReceipt(...)`
    src/domain/worldPulse/envoyChanceMeetingStage.js:566|* builder on the `faithReceipt(...)` model and this stage calls it once per seed through
    src/domain/worldPulse/envoyPulse.js:268|// the `faithReceipt(...)` model and this stage calls it once per seed through
    src/domain/worldPulse/religiousContest.js:63|import { faithReceipt } from './faithNews.js';
    tests/lint/faithKindPools.walker.test.js:60|faithReceipt,
    docs/implementation/PACKET_MANIFEST.json:21836|"_note": "THE REGISTRY LEAF. FAITH_KIND_REGISTRY (one row), the cured hash01 keyed pick copied verbatim from informationNews.js with NO unseeded arm, fai
    docs/implementation/packets/fp/WF-8A.md:68|`faithReceipt` returns a pulse OUTCOME carrying `candidateType: 'faith_last_altar_dark'`,

### 1  src/domain/worldPulse/faithTuningSurface.js  ::  FAITH_TUNING_PROVENANCE
  paths: FAITH_TUNING_PROVENANCE.status
  ex: "CANDIDATE, OWNER-UNSIGNED, DRAFT-LIT (row O-17, 2026-09-03: the door is open over candidate numbers; every table below i"
  consumers(1):
    tests/domain/faithTuningSurface.test.js:47|FAITH_TUNING_PROVENANCE,

### 1  src/domain/worldPulse/feasibilityGate.js  ::  feasibilityVerdictClause()
  paths: feasibilityVerdictClause()
  ex: "the matchup alone made a storm worth trying"
  consumers(2):
    src/domain/worldPulse/warDeployment.js:67|import { classifyFeasibility, verdictPermitsSiege, siegeMatchupWordFor, feasibilityVerdictClause } from './feasibilityGate.js';
    tests/domain/warVerdictHeraldWords.test.js:11|*   `feasibilityVerdictClause` — total over the closed verdict vocabulary. A seventh

### 1  src/domain/worldPulse/index.js  ::  STRESSOR_CATALOG
  paths: STRESSOR_CATALOG.monster_raider_pressure.label
  ex: "Monster or raider pressure"
  consumers(25):
    src/domain/autonomy/accelerationOps.js:21|import { STRESSOR_CATALOG, catalogFor } from '../worldPulse/stressorsCore.js';
    src/domain/certification/subsystemRowsPeople.js:40|* The stressor catalog's type keys (STRESSOR_CATALOG, worldPulse/stressorsCore.js),
    src/domain/crisisLifecycle.js:46|import { STRESSOR_CATALOG, normalizeStressor } from './worldPulse/stressorsCore.js';
    src/domain/stressorPicker.js:8|*   - STRESSOR_CATALOG (worldPulse/stressors.js): the CAMPAIGN vocabulary —
    src/domain/worldPulse/decisionTier.js:245|* "safer un-governed than mis-governed"). The walker requires every STRESSOR_CATALOG
    src/domain/worldPulse/stressors.js:13|STRESSOR_POLICIES, STRESSOR_CATALOG, idFor, catalogFor,
    src/domain/worldPulse/stressorsCore.js:54|export const STRESSOR_CATALOG = Object.freeze({
    tests/domain/autonomy/accelerationOps.test.js:15|import { STRESSOR_CATALOG } from '../../../src/domain/worldPulse/stressorsCore.js';
    tests/domain/coupEvents.test.js:10|import { STRESSOR_CATALOG } from '../../src/domain/worldPulse/stressors.js';
    tests/domain/dramaClassRegistry.contract.test.js:10|*  (c) the CATALOG-COVERAGE guard: every STRESSOR_CATALOG key either appears in the

### 1  src/domain/worldPulse/index.js  ::  canRecoverResource()
  paths: canRecoverResource().reason
  ex: "Renewable or managed resource can recover when pressure drops."
  consumers(3):
    src/domain/worldPulse/resourceTaxonomy.js:158|export function canRecoverResource(resource, settlement, context = {}) {
    src/domain/worldPulse/tierResourceDynamics.js:21|import { canRecoverResource, classifyResource } from './resourceTaxonomy.js';
    docs/GENERATION_COHERENCE_AUDIT.md:776|magicLevelScore (resourceTaxonomy.js:31-38) scores 'pervasive'=4, 'high'=3, 'common'=2, 'moderate'=1, else 0. The generator emits none/low/medium/high — so 'medi

### 1  src/domain/worldPulse/institutionStatusModel.js  ::  INSTITUTION_CAUSE_LABELS
  paths: INSTITUTION_CAUSE_LABELS.damage
  ex: "damage to the building"
  consumers(2):
    src/domain/worldPulse/institutionStatusLifecycle.js:67|INSTITUTION_CAUSE_LABELS,
    tests/domain/institutionStatusModel.test.js:27|INSTITUTION_CAUSE_LABELS,

### 1  src/domain/worldPulse/ledgerOwnershipManifest.js  ::  LEDGER_OWNERSHIP_MANIFEST
  paths: LEDGER_OWNERSHIP_MANIFEST[1].ordering
  ex: "no global writer order; every writer owns its local deterministic ordering"
  consumers(1):
    tests/domain/pulseStageContracts.test.js:16|LEDGER_OWNERSHIP_MANIFEST,

### 1  src/domain/worldPulse/lineageClaim.js  ::  scoreKinshipBond()
  paths: scoreKinshipBond().receipt
  ex: "The lineage was cited by both sides to opposite ends, and the quieter reading held."
  consumers(1):
    tests/domain/warReasonsPredationFaith.test.js:40|import { scoreLineageClaim, scoreKinshipBond } from '../../src/domain/worldPulse/lineageClaim.js';

### 1  src/domain/worldPulse/magicBufferModel.js  ::  EXPLOITATION_GATE_CONTRACT
  paths: EXPLOITATION_GATE_CONTRACT.absentMeaning
  ex: "no regime is known, so nothing can be exploited and the magic term is zero"
  consumers(1):
    tests/domain/magicBufferModel.test.js:33|bufferReceiptLine, bufferShortfallLine, EXPLOITATION_GATE_CONTRACT,

### 1  src/domain/worldPulse/magicBufferModel.js  ::  bufferReceiptLine()
  paths: bufferReceiptLine()
  ex: "The wards blunted the worst of it, and it cost nothing anyone can name."
  consumers(2):
    src/domain/worldPulse/magicBufferApply.js:64|bufferReceiptLine, bufferShortfallLine,
    tests/domain/magicBufferModel.test.js:33|bufferReceiptLine, bufferShortfallLine, EXPLOITATION_GATE_CONTRACT,

### 1  src/domain/worldPulse/magicBufferModel.js  ::  bufferShortfallLine()
  paths: bufferShortfallLine()
  ex: "The wards reached further than the stores could pay for, and stopped where the coin stopped."
  consumers(2):
    src/domain/worldPulse/magicBufferApply.js:64|bufferReceiptLine, bufferShortfallLine,
    tests/domain/magicBufferModel.test.js:33|bufferReceiptLine, bufferShortfallLine, EXPLOITATION_GATE_CONTRACT,

### 1  src/domain/worldPulse/magicFormsPractitioner.js  ::  defaultPractitionerName()
  paths: defaultPractitionerName()
  ex: "The Hedge wizard of a"
  consumers(1):
    tests/domain/magicFormsPractitioner.test.js:23|defaultPractitionerName,

### 1  src/domain/worldPulse/magicSubstitution.js  ::  magicDependencySentence()
  paths: magicDependencySentence()
  ex: "The settlement takes a little of its bread from the arcane houses: about 0 percent of what it eats each day is conjured,"
  consumers(1):
    tests/domain/magicSubstitution.test.js:34|magicDependencySentence,

### 1  src/domain/worldPulse/opportunism.js  ::  perceivedVulnerabilityClause()
  paths: perceivedVulnerabilityClause()
  ex: "The court believes their walls are thinly held and the court is wrong about it."
  consumers(0):

### 1  src/domain/worldPulse/pactFormation.js  ::  answerPactProposal()
  paths: answerPactProposal().receipt
  ex: "This court's posture reads settled: nothing was heard from its remembered standing, its disposition channels, its learne"
  consumers(3):
    src/domain/certification/couplingRegistryGrammar.js:96|counterforce: 'src/domain/worldPulse/pactFormation.js#answerPactProposal',
    tests/domain/pactFormation.test.js:61|answerPactProposal,
    docs/FABLE_VALIDATION_QUEUE.md:4167|supplied and its body never destructured; `answerPactProposal` narrows a term magnitude with

### 1  src/domain/worldPulse/pactFormation.js  ::  reserveFor()
  paths: reserveFor().receipt
  ex: "This court's posture reads settled: nothing was heard from its remembered standing, its disposition channels, its learne"
  consumers(7):
    src/domain/certification/couplingRegistryGrammar.js:65|counterforce: 'src/domain/worldPulse/pactFormation.js#reserveFor',
    src/domain/worldPulse/treatyBreachCredibility.js:6|* with a LIVE reader chain — `credibilityScoreOf` → `reserveFor` →
    tests/domain/pactFormation.test.js:63|reserveFor,
    tests/domain/treatyBreachCredibility.test.js:20|* chain — `credibilityScoreOf` → `reserveFor` → `OATHBREAKER_PENALTY` — and no producer
    docs/implementation/INDEX.md:1117|| LANDED-23 | [`GR-4c`](./packets/foreign-policy/GR-4C.md) | **LANDED** at `cd2ab894`; do not redispatch | The oath that finally costs something: a torn-up treaty cha
    docs/implementation/packets/foreign-policy/GR-4A.md:916|| **D4** | *"open repudiation prices NO credibility delta (coalition fracture does)"* | `spatialLedgers.credibility` has **five** delta producer
    docs/implementation/packets/foreign-policy/GR-4C.md:9|- **Depends on:** GR-4a at `a53ef7c6` (the succession answer, the shared shell, `SUCCESSION_REPUDIATION_TYPE`); GR-4b-α at `dd457b9a` (widened `an

### 1  src/domain/worldPulse/pactProposals.js  ::  openPactProposal()
  paths: openPactProposal().receipt
  ex: "There was nothing here that could be written into an instrument."
  consumers(3):
    src/domain/worldPulse/pactFormation.js:95|openPactProposal, pactFormationActive, pactProposalsOf, prunePactProposals, settlePactProposal,
    tests/domain/pactProposals.test.js:10|* `pactProposalsOf` over state produced by `openPactProposal` — never over a hand-built
    docs/FABLE_VALIDATION_QUEUE.md:2855|What IS here is everything the verb would need: `openPactProposal` is the ONE creation

### 1  src/domain/worldPulse/pactTriggers.js  ::  dependencyFearOf()
  paths: dependencyFearOf().receipt
  ex: "This court can carry what the bargain would cost it in reliance."
  consumers(3):
    src/domain/worldPulse/pactFormation.js:98|dependencyFearOf, scoreFaithCommunion, scoreMigrationPressure, scoreSharedThreat, scoreTradeDemand,
    tests/domain/pactTriggers.test.js:17|dependencyFearOf,
    docs/FABLE_VALIDATION_QUEUE.md:4163|deleted quietly. The four `clamp01(unknown)` calls in `dependencyFearOf` pass through a local

### 1  src/domain/worldPulse/pactTriggers.js  ::  scoreFaithCommunion()
  paths: scoreFaithCommunion().receipt
  ex: "One of these courts holds no belief about the other's observance."
  consumers(2):
    src/domain/worldPulse/pactFormation.js:98|dependencyFearOf, scoreFaithCommunion, scoreMigrationPressure, scoreSharedThreat, scoreTradeDemand,
    tests/domain/pactTriggers.test.js:20|scoreFaithCommunion,

### 1  src/domain/worldPulse/pactTriggers.js  ::  scoreMigrationPressure()
  paths: scoreMigrationPressure().receipt
  ex: "One of these courts holds no belief about where people are going."
  consumers(2):
    src/domain/worldPulse/pactFormation.js:98|dependencyFearOf, scoreFaithCommunion, scoreMigrationPressure, scoreSharedThreat, scoreTradeDemand,
    tests/domain/pactTriggers.test.js:21|scoreMigrationPressure,

### 1  src/domain/worldPulse/pactTriggers.js  ::  scoreSharedThreat()
  paths: scoreSharedThreat().receipt
  ex: "No court either of these believes in is armed enough to be worth fearing together."
  consumers(3):
    src/domain/worldPulse/pactFormation.js:98|dependencyFearOf, scoreFaithCommunion, scoreMigrationPressure, scoreSharedThreat, scoreTradeDemand,
    tests/domain/pactFormation.test.js:28|*   whose `band` is read by `scoreSharedThreat`, whose FLOOR BAND is `'quiet'`
    tests/domain/pactTriggers.test.js:22|scoreSharedThreat,

### 1  src/domain/worldPulse/pactTriggers.js  ::  scoreTradeDemand()
  paths: scoreTradeDemand().receipt
  ex: "No scarcity ladder was supplied to price a demand against."
  consumers(3):
    src/domain/worldPulse/pactFormation.js:98|dependencyFearOf, scoreFaithCommunion, scoreMigrationPressure, scoreSharedThreat, scoreTradeDemand,
    tests/domain/pactTriggers.test.js:23|scoreTradeDemand,
    tests/property/pactFormationDormancyFence.test.js:62|scoreTradeDemand: (/** @type {any[]} */ ...args) => {

### 1  src/domain/worldPulse/peaceReasons.js  ::  scoreBeliefConvergence()
  paths: scoreBeliefConvergence().receipt
  ex: "Both courts have been schooled to the same truth by the fighting. No offer is an insult any more."
  consumers(3):
    tests/domain/informationStatecraftPins.test.js:43|import { scoreBeliefConvergence } from '../../src/domain/worldPulse/peaceReasons.js';
    tests/domain/peaceReasons.test.js:18|scoreExhaustion, scoreBeliefConvergence, scoreEconomicStrangulation,
    tests/domain/warReasonsPredationFaith.test.js:28|scoreExhaustion, scoreBeliefConvergence, scoreEconomicStrangulation,

### 1  src/domain/worldPulse/peaceReasons.js  ::  scoreObligationDischarged()
  paths: scoreObligationDischarged().receipt
  ex: "The compact or original quarrel no longer supplies a borrowed cause for war."
  consumers(1):
    tests/domain/warReasonsPredationFaith.test.js:31|scoreObligationDischarged, scoreAtrocityAtoned,

### 1  src/domain/worldPulse/peaceReasons.js  ::  scoreRealignment()
  paths: scoreRealignment().receipt
  ex: "A common enemy stands at both gates. The peace is signed in haste, for the horde was in the passes."
  consumers(2):
    tests/domain/peaceReasons.test.js:19|scoreCoalitionFracture, scoreMediation, scoreHarvestPressure, scoreRealignment,
    tests/domain/warReasonsPredationFaith.test.js:29|scoreCoalitionFracture, scoreMediation, scoreHarvestPressure, scoreRealignment,

### 1  src/domain/worldPulse/peaceTermsDrafting.js  ::  signingReason()
  paths: signingReason()
  ex: "{c} accepts the {type} demanded by {b}."
  consumers(1):
    src/domain/worldPulse/peaceTerms.js:165|import { draftReceipt, draftTerms, signingReason } from './peaceTermsDrafting.js';

### 1  src/domain/worldPulse/razing.js  ::  compareSpoils()
  paths: compareSpoils().receipt
  ex: "burning yields 0 once against 0 from holding. This was a place worth more dead than alive, which is the rarer and darker"
  consumers(1):
    tests/domain/razingWr8.test.js:37|compareSpoils,

### 1  src/domain/worldPulse/razing.js  ::  conservedSack()
  paths: conservedSack().receipt
  ex: "the settlement cannot be counted, and an uncounted place cannot be sacked."
  consumers(2):
    src/domain/worldPulse/razingExecution.js:109|conservedSack,
    tests/domain/razingWr8.test.js:38|conservedSack,

### 1  src/domain/worldPulse/razing.js  ::  razingDeparture()
  paths: razingDeparture().receipt
  ex: "They burned the settlement and rode home. The victor took no ground, left no garrison, and asked for nothing."
  consumers(2):
    src/domain/worldPulse/razingExecution.js:112|razingDeparture,
    tests/domain/razingWr8.test.js:40|razingDeparture,

### 1  src/domain/worldPulse/razing.js  ::  razingGate()
  paths: razingGate().receipt
  ex: "the victor has not taken the defeated: there is no razing without a won siege."
  consumers(7):
    src/domain/worldPulse/razingExecution.js:107|razingGate,
    src/domain/worldPulse/vengeanceLicense.js:267|* This is the boolean `razingGate` consumes as `licenseHeld`, and the separation
    tests/domain/razingDeterrenceWr8.test.js:35|import { RAZING_REFUSALS, razingGate } from '../../src/domain/worldPulse/razing.js';
    tests/domain/razingExecutionWr8.test.js:48|RAZING_TUNING, razingEdgeFlips, razingGate, readRelationshipExtremity, razingOutcomeIdFor,
    tests/domain/razingWr8.test.js:42|razingGate,
    tests/domain/vengeanceLicenseWr8.test.js:40|import { RAZING_TUNING, razingGate, readRelationshipExtremity } from '../../src/domain/worldPulse/razing.js';
    docs/DESIGN_FP_ARCH_HB.md:1520|- **guard chains with no weighted arm** — `razingGate`, `captorRansomChoice`,

### 1  src/domain/worldPulse/razing.js  ::  razingSpoils()
  paths: razingSpoils().receipt
  ex: "there is nothing in the ash: the army carries off what a ruin has, which is nothing."
  consumers(2):
    src/domain/worldPulse/razingExecution.js:113|razingSpoils,
    tests/domain/razingWr8.test.js:44|razingSpoils,

### 1  src/domain/worldPulse/razing.js  ::  readRelationshipExtremity()
  paths: readRelationshipExtremity().receipt
  ex: "the court is not at the extreme with its neighbour: the border is not hostile, the resentment has not reached what hosti"
  consumers(4):
    src/domain/worldPulse/razingExecution.js:108|readRelationshipExtremity,
    tests/domain/razingExecutionWr8.test.js:48|RAZING_TUNING, razingEdgeFlips, razingGate, readRelationshipExtremity, razingOutcomeIdFor,
    tests/domain/razingWr8.test.js:45|readRelationshipExtremity,
    tests/domain/vengeanceLicenseWr8.test.js:40|import { RAZING_TUNING, razingGate, readRelationshipExtremity } from '../../src/domain/worldPulse/razing.js';

### 1  src/domain/worldPulse/razing.js  ::  tierFallDescriptor()
  paths: tierFallDescriptor().receipt
  ex: "what the settlement was and what it is now cannot both be read, so no fall can be counted."
  consumers(1):
    tests/domain/razingWr8.test.js:46|tierFallDescriptor,

### 1  src/domain/worldPulse/razingExecution.js  ::  razingDeterrenceFor()
  paths: razingDeterrenceFor().receipt
  ex: "nobody would inherit the right to answer the burning"
  consumers(1):
    tests/domain/razingDeterrenceWr8.test.js:32|razingDeterrenceFor,

### 1  src/domain/worldPulse/razingExecution.js  ::  razingSeverityFrom()
  paths: razingSeverityFrom().receipt
  ex: "no quarrel stands hot enough to price a burning."
  consumers(1):
    tests/domain/razingExecutionWr8.test.js:41|razingSeverityFrom,

### 1  src/domain/worldPulse/reinforcement.js  ::  computeReinforcement()
  paths: computeReinforcement().reasons[0]
  ex: "Origin is itself besieged/occupied. It cannot spare reinforcements."
  consumers(2):
    src/domain/worldPulse/warHomeCosts.js:41|import { computeReinforcement, applyReinforcementToRecord } from './reinforcement.js';
    tests/domain/statefulArmies.test.js:12|computeReinforcement,

### 1  src/domain/worldPulse/relationshipCompatibility.js  ::  validateRelationship()
  paths: validateRelationship().issues[0].reason
  ex: "'{a}' is not a canonical relationship type."
  consumers(1):
    tests/domain/relationshipCompatibility.test.js:11|*   - validateRelationship flags every incoherent combination with a code.

### 1  src/domain/worldPulse/settlementLifecycleFirstClass.js  ::  forceResettleEntry()
  paths: forceResettleEntry().dials[0].label
  ex: "New settlement name (optional)"
  consumers(5):
    src/domain/events/realmManifest.js:41|import { forceAbandonEntry, forceResettleEntry } from '../worldPulse/settlementLifecycleFirstClass.js';
    tests/domain/settlementLifecycleVerbs.test.js:26|forceResettleEntry,
    docs/COMPREHENSIVE_REVIEW_2026-07-15.md:549|- Evidence: calamityKernel :424/:459 ("NOT registered; W-COMPOSER-2 lift"), settlementLifecycleKernel :840, settlementLifecycleFirstClass :605 all assert th
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:3630|- authority: `REALM_MANIFEST.FORCE_RESETTLE` (realmManifest.js:427-439) — candidateType/authority `'settlement_resettled'`, module `settlementLifecycleFirstCla
    docs/review-r2/RAW_SURVEY_RESULTS.json:487|"evidence": "calamityKernel :424/:459 (\"NOT registered; W-COMPOSER-2 lift\"), settlementLifecycleKernel :840, settlementLifecycleFirstClass :605 all assert 

### 1  src/domain/worldPulse/settlementLifecycleKernel.js  ::  forceFoundSteading()
  paths: forceFoundSteading().refusal
  ex: "only town-or-higher parents seed steadings"
  consumers(3):
    src/domain/worldPulse/realmVerbExecution.js:52|import { settlementLifecycleActive, forceFoundSteading } from './settlementLifecycleKernel.js';
    tests/domain/settlementLifecycleVerbs.test.js:18|forceFoundSteading,
    docs/review-r2/RAW_SURVEY_RESULTS.json:540|"Force ≡ organic is genuinely implemented: every realm arm resolves through its wave's own kernel function (declareCasus, planConvoy/planBlockade, forceCalam

### 1  src/domain/worldPulse/settlementLifecycleKernel.js  ::  forceFoundSteadingEntry()
  paths: forceFoundSteadingEntry().dials[1].label
  ex: "Resource key (optional: the vein the camp exists for)"
  consumers(5):
    src/domain/events/realmManifest.js:40|import { settlementLifecycleActive, forceFoundSteadingEntry } from '../worldPulse/settlementLifecycleKernel.js';
    tests/domain/settlementLifecycleVerbs.test.js:19|forceFoundSteadingEntry,
    docs/COMPREHENSIVE_REVIEW_2026-07-15.md:549|- Evidence: calamityKernel :424/:459 ("NOT registered; W-COMPOSER-2 lift"), settlementLifecycleKernel :840, settlementLifecycleFirstClass :605 all assert th
    docs/SETTLEMENT_CAPABILITY_ATLAS.md:3652|- authority: `REALM_MANIFEST.FORCE_FOUND_STEADING` (realmManifest.js:402-413) — candidateType/authority `'steading_forced'`, module `settlementLifecycleKernel.
    docs/review-r2/RAW_SURVEY_RESULTS.json:487|"evidence": "calamityKernel :424/:459 (\"NOT registered; W-COMPOSER-2 lift\"), settlementLifecycleKernel :840, settlementLifecycleFirstClass :605 all assert 

### 1  src/domain/worldPulse/sovereigntyAppraisal.js  ::  appraiseSettlementAsset()
  paths: appraiseSettlementAsset().receipt
  ex: "the court cannot price the holding: the pair is not an asset and a court."
  consumers(12):
    src/domain/worldPulse/sovereigntyMarketStage.js:61|* `appraiseSettlementAsset` still returns `known: false` and this stage still emits an
    tests/domain/envoyK3BeliefSeam.test.js:357|// WR-10 (amendment S). "Valuation (K3 governs)" names `appraiseSettlementAsset` by
    tests/domain/espionageDistantSourceEs4.test.js:81|* → `appraiseSettlementAsset`. JUDGMENT J-ES4-A, vetoable; the cost of a veto is a market
    tests/domain/sovereigntyAppraisalWr10.test.js:6|* THE CONJUNCTION-BLIND GUARD PAIR. `appraiseSettlementAsset` has FOUR legs and any
    tests/domain/sovereigntyBundleWr10.test.js:38|import { appraiseSettlementAsset, sovereigntyValueBand } from '../../src/domain/worldPulse/sovereigntyAppraisal.js';
    tests/domain/sovereigntyMarketStageWr10w.test.js:38|SOVEREIGNTY_TRAJECTORY_BANDS, appraiseSettlementAsset,
    docs/DESIGN_FP_ARCHITECTURE.md:1339|a completed mission and `appraiseSettlementAsset` prices it end to end; THE
    docs/DESIGN_FP_ARCH_CENSUS.md:245|> position have no surface at all. Three legs missing ⇒ `appraiseSettlementAsset` returns
    docs/DESIGN_FP_ARCH_ES.md:1611|and `appraiseSettlementAsset` prices it end-to-end (the market clears on
    docs/DESIGN_FP_ARCH_SP.md:464|the SUPPLY side's spelling. The consumer, `appraiseSettlementAsset`

### 1  src/domain/worldPulse/sovereigntyAssets.js  ::  readSovereigntyAsset()
  paths: readSovereigntyAsset().receipt
  ex: "no holding was named, so none can be judged."
  consumers(7):
    src/domain/worldPulse/occupation.js:486|* instead of the holding it bought, and `readSovereigntyAsset` would stop calling the
    src/domain/worldPulse/realmVerbExecution.js:59|import { readSovereigntyAsset, sovereigntyTradeActive } from './sovereigntyAssets.js';
    src/domain/worldPulse/sovereigntyTransfer.js:53|import { readSovereigntyAsset, sovereigntyTradeActive } from './sovereigntyAssets.js';
    tests/domain/sovereigntyDmVerbWr10w.test.js:54|SOVEREIGNTY_REQUIRED_RULES, readSovereigntyAsset,
    tests/domain/sovereigntyMarketReadsWr10.test.js:25|readSovereigntyAsset, tradeableAssetsOf,
    tests/domain/sovereigntyTransferWr10w.test.js:12|*     buyer a fight instead of the holding it paid for — and `readSovereigntyAsset`
    tests/domain/sovereigntyWaveCloseIntegration.test.js:42|SOVEREIGNTY_REQUIRED_RULES, readSovereigntyAsset,

### 1  src/domain/worldPulse/sovereigntyBundle.js  ::  valueBundleThroughNeeds()
  paths: valueBundleThroughNeeds().receipt
  ex: "no court was named, so the bundle has no valuer and no value."
  consumers(2):
    src/domain/worldPulse/sovereigntyMarketStage.js:107|import { bundleComponentFamilies, clearSovereigntyTrade, valueBundleThroughNeeds } from './sovereigntyBundle.js';
    tests/domain/sovereigntyBundleWr10.test.js:31|bundleComponentFamilies, catalogGrewSinceWr10, valueBundleThroughNeeds,

### 1  src/domain/worldPulse/sovereigntyIntent.js  ::  judgeSovereigntySale()
  paths: judgeSovereigntySale().receipt
  ex: "No verdict is recorded on this sale: no buyer is named."
  consumers(4):
    src/domain/worldPulse/espionage/espionageDoctrine.js:122|* `judgeSovereigntySale` pattern). Every closed-word field is null rather than a
    src/domain/worldPulse/espionage/espionageDoctrineStage.js:17|*   `judgeSovereigntySale` resolution-gate pattern, minted by the leaf and never here.
    tests/domain/sovereigntyIntentWr10.test.js:40|judgeSovereigntySale,
    docs/DESIGN_FP_ARCH_ES.md:941|— the judgeSovereigntySale gate pattern); the axes are total and finite,

### 1  src/domain/worldPulse/sovereigntyIntent.js  ::  readSovereigntySaleIntent()
  paths: readSovereigntySaleIntent().receipt
  ex: "weighs selling to . No court is named as the seller, so no appetite is recorded. The sale would serve the realm's books,"
  consumers(3):
    src/domain/worldPulse/sovereigntyMarketStage.js:109|import { readSovereigntySaleIntent } from './sovereigntyIntent.js';
    tests/domain/sovereigntyIntentWr10.test.js:41|readSovereigntySaleIntent,
    tests/domain/sovereigntyMarketStageWr10w.test.js:31|import { readSovereigntySaleIntent } from '../../src/domain/worldPulse/sovereigntyIntent.js';

### 1  src/domain/worldPulse/sovereigntyIntent.js  ::  saleAppetiteOf()
  paths: saleAppetiteOf().receipt
  ex: "No court is named as the seller, so no appetite is recorded."
  consumers(1):
    tests/domain/sovereigntyIntentWr10.test.js:42|saleAppetiteOf,

### 1  src/domain/worldPulse/sovereigntyReach.js  ::  sovereigntyReach()
  paths: sovereigntyReach().receipt
  ex: "a reach question needs a buyer and a holding that are two different places."
  consumers(12):
    src/domain/certification/subsystemRowsCompact.js:166|module: 'src/domain/worldPulse/sovereigntyTransfer.js,src/domain/worldPulse/sovereigntyAssets.js,src/domain/worldPulse/sovereigntyAppraisal.js,src/
    src/domain/worldPulse/realmVerbExecution.js:414|// measured rather than assumed. `sovereigntyReach` is the MARKET's candidate-set
    src/domain/worldPulse/sovereigntyMarketStage.js:108|import { reachableAssetsFor } from './sovereigntyReach.js';
    src/domain/worldPulse/treasury.js:65|* receipts }` whose `flows` carries `goods`, and `sovereigntyReach.js` reads
    tests/domain/envoyK3BeliefSeam.test.js:432|'./sovereigntyReach.js',
    tests/domain/sovereigntyMarketReadsWr10.test.js:29|sovereigntyReach, reachableAssetsFor,
    tests/domain/sovereigntyMarketStageWr10w.test.js:173|/** THE REACH IS STUBBED, DELIBERATELY AND VISIBLY. `sovereigntyReach` reads the frozen
    tests/lint/.coupling-unlayered-baseline.json:153|"src/domain/worldPulse/sovereigntyReach.js",
    tests/lint/.tuning-inventory.json:1199|"src/domain/worldPulse/sovereigntyReach.js",
    tests/lint/.tuning-register.json:1802|"src/domain/worldPulse/sovereigntyReach.js#SOVEREIGNTY_REACH_TUNING": {

### 1  src/domain/worldPulse/stressorGates.js  ::  STRESSOR_SPAWN_GATES
  paths: STRESSOR_SPAWN_GATES.religious_conversion_fracture().reasons[0]
  ex: "Few congregations here to fracture."
  consumers(6):
    src/domain/worldPulse/stressors.js:5|import { STRESSOR_SPAWN_GATES } from './stressorGates.js';
    tests/domain/coalitionDissentCoupSoak.m9d.test.js:32|import { STRESSOR_SPAWN_GATES } from '../../src/domain/worldPulse/stressorGates.js';
    tests/domain/foreignSeatResolver.test.js:36|import { STRESSOR_SPAWN_GATES, activeTypesAt } from '../../src/domain/worldPulse/stressorGates.js';
    tests/domain/legitimacyUpheaval.test.js:3|import { STRESSOR_SPAWN_GATES, UPHEAVAL_TUNING } from '../../src/domain/worldPulse/stressorGates.js';
    tests/domain/rulingPower.test.js:16|import { STRESSOR_SPAWN_GATES } from '../../src/domain/worldPulse/stressorGates.js';
    tests/domain/stressorGates.test.js:6|STRESSOR_SPAWN_GATES,

### 1  src/domain/worldPulse/successorNpc.js  ::  successorNpc()
  paths: successorNpc().goal.short
  ex: "Newly installed after a corruption scandal. Determined to stay above suspicion."
  consumers(18):
    src/domain/events/mutateEntities.js:37|import { successorNpc } from '../worldPulse/successorNpc.js';
    src/domain/npc/npcOps.js:82|*     (successorNpc / seatNpcsIntoFactions on the next tick) refills.
    src/domain/worldPulse/npcVerdictPulse.js:9|import { replaceOustedNpcs } from './successorNpc.js';
    tests/domain/roadsParticipation.test.js:250|'src/domain/worldPulse/successorNpc.js',
    tests/domain/successorNpc.test.js:2|import { successorNpc, replaceOustedNpcs } from '../../src/domain/worldPulse/successorNpc.js';
    tests/lint/.coupling-unlayered-baseline.json:165|"src/domain/worldPulse/successorNpc.js",
    tests/lint/.domain-any-baseline.json:582|"src/domain/worldPulse/successorNpc.js": {
    tests/lint/allowlistRebuilderRegistry.walker.test.js:958|module: 'src/domain/worldPulse/successorNpc.js', fn: 'successorNpc', status: 'not-a-rebuilder',
    scripts/.observed-shape-readers-baseline.json:1547|"src/domain/worldPulse/successorNpc.js": {
    docs/COMPREHENSIVE_REVIEW_2026-07-13.md:639|- Why: One-liners: (1) ENGAGE_WINLOSS_W is a dead tunable (winloss hardwired 0). (2) coup.js draws from a single rng.fork('coup-verdict') consumed in resolv

### 1  src/domain/worldPulse/supplyWebWarfare.js  ::  scoreCampaignEV()
  paths: scoreCampaignEV().receipt
  ex: "The target can be taken directly, and the slow strangulation would cost more time than it is worth."
  consumers(1):
    tests/domain/supplyWebWarfare.test.js:25|supplyWebWarfareActive, readSupplyWeb, scoreCampaignEV, atrocityBrakeFor,

### 1  src/domain/worldPulse/warReasons.js  ::  scoreAllianceObligation()
  paths: scoreAllianceObligation().receipt
  ex: "A sworn ally remains in the field under the same living cause."
  consumers(1):
    tests/domain/warReasonsPredationFaith.test.js:24|scoreIngratitudeDebt, scoreDependencyByDesign, scoreAllianceObligation, scoreAtrocityAnswer,

### 1  src/domain/worldPulse/warReasons.js  ::  scoreGrievance()
  paths: scoreGrievance().receipt
  ex: "A ledger of grievances stands open: resentment runs hot, and old wrongs have not faded."
  consumers(12):
    src/domain/worldPulse/corruptionWeb.js:199|*  the people-held grievance that feeds scoreGrievance the same tick (× magnitude). */
    src/domain/worldPulse/grievanceRead.js:7|* ten-value relationship rung. It clones the warReasons.scoreGrievance /
    src/domain/worldPulse/informationStatecraft.js:581|// incident machinery (applyRelationshipPatch) so it feeds scoreGrievance/revanchism
    src/domain/worldPulse/pulseKernel.js:712|// a people-held grievance on the (corrupted↔patron) edge (feeds scoreGrievance the same
    src/domain/worldPulse/traditionsKernel.js:846|// scoreGrievance/revanchism + the D-4 fixation grievanceLean (law 14's honest lag).
    tests/domain/grievanceRead.test.js:4|* The pure memory-weave substrate read: the scoreGrievance clone (resentment +
    tests/domain/informationStatecraftPins.test.js:403|// The exposure banked resentment on the pair edge (fed to scoreGrievance the same tick).
    tests/domain/warReasons.test.js:18|scoreGrievance, scoreRevanchism, scoreResourcePressure, scoreTreatyDefault,
    tests/domain/warReasonsPredationFaith.test.js:22|scoreGrievance, scoreRevanchism, scoreResourcePressure, scoreTreatyDefault,
    docs/DESIGN_FP_COUPLINGS.md:450|info survey) — IN-5 gives scoreGrievance the incident-naming receipt variant.

### 1  src/lib/accountSettlementContentPortability.js  ::  remapAccountSettlementContentProvenance()
  paths: remapAccountSettlementContentProvenance().error
  ex: "Settlement content provenance has an invalid shape."
  consumers(2):
    src/store/accountImportBody.js:56|remapAccountSettlementContentProvenance,
    tests/lib/accountSettlementContentPortability.test.js:15|remapAccountSettlementContentProvenance,

### 1  src/lib/customContentArchive.js  ::  validateCustomContentArchive()
  paths: validateCustomContentArchive().message
  ex: "archive must be an object."
  consumers(10):
    src/lib/accountData.js:28|validateCustomContentArchive,
    src/lib/accountImport.js:61|validateCustomContentArchive,
    src/lib/customContentArchiveImport.js:28|validateCustomContentArchive,
    src/lib/customContentArchiveMerge.js:21|validateCustomContentArchive,
    src/lib/customContentArchiveService.js:17|validateCustomContentArchive,
    tests/domain/customContentReferencePackLifecycle.test.js:32|validateCustomContentArchive,
    tests/lib/customContentArchive.test.js:23|validateCustomContentArchive,
    tests/lib/customContentLocalLedger.test.js:23|validateCustomContentArchive,
    tests/security/customContentArchiveTransfer.pglite.test.js:24|validateCustomContentArchive,
    tests/security/reviewedSupplyChainPersistence.pglite.test.js:67|validateCustomContentArchive,

### 1  src/lib/importReconciliation.js  ::  ingestSettlementForgeExport()
  paths: ingestSettlementForgeExport().diagnostic.message
  ex: "This export is too large to reconcile safely."
  consumers(4):
    src/components/settlements/StructuredCampaignReconciliation.jsx:27|ingestSettlementForgeExport,
    src/lib/importReconciliationAdmission.js:109|export function ingestSettlementForgeExport(text, meta = {}) {
    tests/lib/importReconciliation.test.js:16|ingestSettlementForgeExport,
    tests/lib/importReconciliationRecovery.test.js:16|ingestSettlementForgeExport,

### 1  src/lib/instantWorld/factionDedup.js  ::  FACTION_DESCRIPTORS_EXTRA
  paths: FACTION_DESCRIPTORS_EXTRA.crafts[3]
  ex: "The Guild of Artificers"
  consumers(5):
    src/data/powerData.js:80|export const FACTION_DESCRIPTORS_EXTRA = Object.freeze({
    src/domain/arcaneIdentity.js:15|*      a faction name in FACTION_DESCRIPTORS / FACTION_DESCRIPTORS_EXTRA, an institution
    tests/domain/arcaneIdentity.test.js:39|import { FACTION_DESCRIPTORS, FACTION_DESCRIPTORS_EXTRA } from '../../src/data/powerData.js';
    tests/lib/instantWorld/factionDedup.test.js:14|COLLECTIVE_NOUNS, DISTINGUISH_MODIFIERS, FACTION_DESCRIPTORS_EXTRA,
    docs/DESIGN_REALM_MAGIC_TOGGLE.md:299|`FACTION_DESCRIPTORS` ∪ `FACTION_DESCRIPTORS_EXTRA`. Exact-match then containment, so the

### 1  src/lib/seo.js  ::  DEFAULT_DESCRIPTION
  paths: DEFAULT_DESCRIPTION
  ex: "SettlementForge generates living tabletop-RPG settlements with economies, factions, NPCs, and history, then simulates th"
  consumers(1):
    tests/build/prerenderRoutes.test.js:25|import { headForView, VIEW_DESCRIPTIONS, DEFAULT_DESCRIPTION } from '../../src/lib/seo.js';

### 1  src/lib/seo.js  ::  OG_IMAGE_ALT_DEFAULT
  paths: OG_IMAGE_ALT_DEFAULT
  ex: "SettlementForge: living settlements for game masters"
  consumers(0):

### 1  src/lib/seoCompendium.js  ::  compendiumEntryHead()
  paths: compendiumEntryHead().description
  ex: "{term}, a {category} in the SettlementForge Compendium. Every entry renders from the deterministic engine's own registri"
  consumers(3):
    tests/build/prerenderRoutes.test.js:9|*      the runtime SPA's head (headForView / compendiumEntryHead — the single
    tests/lib/seoCompendium.test.js:9|import { compendiumEntryHead, compendiumEntryPath } from '../../src/lib/seoCompendium.js';
    scripts/prerender-routes.mjs:29|* seo.js) and compendiumEntryHead (src/lib/seoCompendium.js) — the SAME pure

### 1  src/pdf/lib/headlines.js  ::  overviewHeadline()
  paths: overviewHeadline()
  ex: "A {toLowerCase}, {toLowerCase} {toLowerCase} under active strain (1 crisis chip)."
  consumers(2):
    src/foundry/journalPages.js:33|overviewHeadline, powerHeadline, economicsHeadline, defenseHeadline,
    src/pdf/sections/Overview.jsx:23|import { overviewHeadline, overviewTone } from '../lib/headlines.js';

### 1  src/store/aiRequestLifecycle.js  ::  DAILY_LIFE_FIELD_LABELS
  paths: DAILY_LIFE_FIELD_LABELS.night
  ex: "Walking the night watch"
  consumers(2):
    src/store/aiSlice.js:57|DAILY_LIFE_FIELD_LABELS,
    tests/store/aiSliceHelpers.test.js:10|DAILY_LIFE_FIELD_LABELS,
