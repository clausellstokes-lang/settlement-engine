§912.7: the wave's instruments are built (lane INSTR-912, seven commits in laneINSTR, no prose byte moved) and two of the day's rulings are re-ruled on what they measured — the three numbers are unit-specific (a third and half a band for a register, tab or pool; two thirds and 1.75 band-widths for an entry, provisional on one author's texts because seven registers' raw texts no longer exist), arm A gates on the authored grammar tag and reports on untagged variants, and the shipped breaches are seven

The entry walker with its four Brackwater tables and an anti-vacuity guard that reds on every shipped breach; the grammar walker with arms A–J, the three numbers and the ten gaps, a control per arm, the classifier measured at 0.75–0.83 (it reports, never gates); the loaders for seven registers with every count printed against PROBE_ALL's; the derived, unpersisted institution table; the presence measure; the unrendered-facts census (72 held, 13 rendered as a word, 59 key-only — the authoring wave's upper bound); D8's ledger walker (the class does not exist). First-run findings: one shape holds 78.4 % of the record across 200 towns against an 11 % ceiling; 22 blocks compose only the town's name; 33 of 68 license one shape; 128 bare futures (the wave's cure list under A2); 114 unreachable variants; settlement.services empty on every town; three more live breaches. The lane's skeptic pass and its landing (§914, rebased onto the §913 CAS) follow the §913 gate. Zero product bytes.

Enrols: none — a ledger-only chair-ruling row (§912.7); INSTR-912 enrols its retrovalidation row at its own landing (§914).

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
