# SLICE wiring
## facts
- `(worktree)/laneB6:1` — TIP MISMATCH: the brief names 6ebe0ef3b, but `git log --oneline -1` in laneB6 returns `ec265d24c §909 car 1a: the per-year population series ships…`. 6ebe0ef3b is HEAD~1 (`git log --oneline -1 6ebe0ef3b` = 'Register (capsule car): the base-state capsule regenerates at the §908 tip'). A §909 car landed into this dock after the brief was written. Worktree is clean (`git status --porcelain` empty) and I wrote nothing to it.
- `src/domain/content/livingContentLaw.js:154` — PATH MISMATCH: the brief places livingContentLaw.js and livingContentLawVersion.js under src/domain/density/. They do not exist there. The real homes are src/domain/content/livingContentLaw.js and src/domain/content/livingContentLawVersion.js. `export function newSettlementLivingContentLaw() {` is at line 154.
- `src/domain/density/densityCreateBoundary.js:279` — The livingContent row of GENERATION_LAWS carries `wiring: 'UNWIRED',` (line 279), inside the frozen row opened at line 274 with `mint: 'newSettlementLivingContentLaw',` (275), `module: 'src/domain/content/livingContentLaw.js',` (276), `dial: 'NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION',` (277), `configKey: '_livingContentLawVersion',` (278).
- `src/domain/density/densityCreateBoundary.js:204` — The header's two-law table reads ` *   living content `newSettlementLivingContentLaw()`  NO CALLER ANYWHERE`. CONFIRMED by census: `grep -rn newSettlementLivingContentLaw` over the whole tree returns exactly six hits — the definition (src/domain/content/livingContentLaw.js:154), two prose mentions (livingContentLaw.js:16, densityCreateBoundary.js:204), the register's string field (densityCreateBoundary.js:275), and two test references (tests/domain/livingContentMaterialization.test.js:61 and :311). Callers in src/ = 0.
- `src/domain/density/densityCreateBoundary.js:163` — `export function birthConfig(config) {` (163) returns `{ ...(config || {}), ...newSettlementDensityLaw() };` (164) — the density mint only. LAW_WIRING_STATES is `Object.freeze(['WIRED', 'UNWIRED']);` at line 189; there is deliberately no third member (the reason is given at 176-186: an OUT_OF_SCOPE row was written and withdrawn because it put retired vocabulary into src/).
- `src/domain/density/densityCreateBoundary.js:249` — The recorded prescription for the wiring car: the mint must happen on the lazy side, and 'that move needs `livingContentLaw.js` added to `ENGINE_SHARED_DOMAIN_EXCISIONS` beside the leaf, plus the hashed-chunk listing diff to prove it free. That is a build, which is why this car declares the gap instead of taking it.' (lines 249-251).
- `src/domain/density/densityCreateBoundary.js:232` — The header prices the block as: livingContentLaw.js and livingContentLawVersion.js are both outside the first-paint closure, the leaf excised and 'deliberately UNPINNED', and 'The excision's own note measures the alternative at engine-core +214 B with 112 chunks re-hashed.' (lines 230-237). It states the closure size as 237 modules at line 231.
- `src/domain/content/livingContentLaw.js:94` — The dial: `export const NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION =` (94) `  DEFAULT_LIVING_CONTENT_LAW_VERSION;` (95). newSettlementLivingContentLaw() (154-162) returns `{}` when the dial equals the default, so a wiring car that does NOT flip the dial writes zero config bytes. Its only import is the leaf pair LIVING_CONTENT_LAW_CONFIG_KEY / DEFAULT_LIVING_CONTENT_LAW_VERSION (74-77); lines 79-87 record that a convenience re-export was measured and refused (~1,047 B of engine chunk against a 677 B ceiling margin).
- `src/domain/content/livingContentLawVersion.js:106` — `export function resolveLivingContentLawVersion(config) {` (106) returns `readLivingContentLawVersion(config?.[LIVING_CONTENT_LAW_CONFIG_KEY]);` (107) — a CONFIG-ONLY read. Lines 97-101 warn it must not fall back to the dial, because an existing world's persisted config carries no marker and a dial fallback would re-birth every old world the moment the dial flipped. readLivingContentLawVersion (86-92) is a closed membership test over LIVING_CONTENT_LAW_VERSIONS = Object.freeze([1, 2]) (69), DEFAULT = 1 (72), ROSTER = 2 (75), key = '_livingContentLawVersion' (63).
- `src/generators/generateSettlementPipeline.js:185` — The customContentRoster write (re-derived, brief said ~:185): `const livingContentRoster = livingContentRosterFor(` at 180, taking `options.customContent || {}` (181) and `finalCtx.settlement.config` (182); `if (livingContentRoster) {` at 184 and `finalCtx.settlement.customContentRoster = livingContentRoster;` at 185. The pipeline reaches the family only through the seam — `livingContentRosterFor,` (42) `} from '../domain/content/livingContentSeam.js';` (43).
- `vite.config.js:218` — ENGINE_SHARED_DOMAIN_EXCISIONS opens at line 173. `'/src/domain/content/livingContentSeam.js',` is line 200 and `'/src/domain/content/livingContentLawVersion.js',` is line 218 (re-derived; the brief's ~:200-203 is the seam row, not the leaf row). The leaf's note (201-217) records the measurement: without the row, engine-core +214 B, first-paint closure 1,046,564 -> 1,046,778, margin 436 B -> 222 B, 112 chunks re-hashed; with it, all 483 chunks byte- and content-hash-identical. Line 223 applies the list: `for (const frag of ENGINE_SHARED_DOMAIN_EXCISIONS) ENGINE_SHARED_DOMAIN.delete(frag);`
- `vite.config.js:58` — computeEngineSharedDomain() seeds ONLY from generators: `for (const g of walk(join(SRC, 'generators')))` (58) `for (const dep of importsOf(g)) if (dep.startsWith(DOMAIN)) seed.add(dep);` (59), with `const DOMAIN = `${join(SRC, 'domain')}/`;` at 56, then closes forward within src/domain (62-65). A store→domain or lib→domain static edge therefore does NOT feed ESD.
- `vite.config.js:309` — `export const EAGER_FIRST_PAINT_MODULES = EAGER_MODULES;` (309). EAGER_MODULES (300) is computed from seeds that include the main.jsx graph, generators/lookups.js (286), the whole kernel walk (288), AND every ENGINE_SHARED_DOMAIN member (290) — so it is a strict superset of the main.jsx-only walk the engineChunkLazy guard uses.
- `tests/build/engineChunkLazy.test.js:238` — The orphan-excision arm: `it('every excised module the first paint reaches is PLACED by manualChunks', () => {` (238), computing `const orphans = reachable.filter(frag => !manualChunks(join(ROOT, frag.slice(1))));` (240). `reachable` (220-221) is ENGINE_SHARED_DOMAIN_EXCISIONS filtered by membership in eagerSourceGraph(), which walks static edges from src/main.jsx only (203-217). It runs in plain `npm test`, no build.
- `tests/build/livingContentSeamLazy.test.js:134` — A SECOND, harder guard the boundary header does not name: `expect(eager.has(LAW)).toBe(false);` (134), where LAW = src/domain/content/livingContentLaw.js (66) and `eager` is vite's own EAGER_FIRST_PAINT_MODULES (124). Any wiring that makes livingContentLaw.js first-paint reachable reds here directly, with no build and no excision-list involvement. Line 102-105 separately forbids the pipeline statically importing livingContentLaw.js.
- `tests/lint/densityCreateBoundary.walker.test.js:452` — THE HOLE IN THE UNWIRED ARM. The ⭐ arm at 429 ('a law that is not WIRED is named by NO module that reaches the pipeline') scans `const scanned = [...reachers, MINT_HOME];` (452), where reachers = modules naming generateSettlementPipeline in stripped source and MINT_HOME = 'src/domain/density/densityCreateBoundary.js' (325). MEASURED by replicating pipelineReachers() over the tree: the reacher set is exactly 5 files — ConstructionPanel.jsx, composeInstantWorld.js, campaignContentBindingSession.js, customContentPreview.worker.js, generationRequest.js. `src/store/settlementGenerateAction.js` — the estate's actual birth-mint home — is NOT among them, so a living-content mint placed there today would be invisible to this arm.
- `tests/lint/densityCreateBoundary.walker.test.js:470` — THE STRUCTURAL BLOCKER ON FLIPPING THE ROW. `it('every WIRED law is spread by birthConfig, in the mint's own home', …)` (470) reds unless `names(MINT_HOME, row.mint)` is true (472) — and names() strips comments and string literals (328-331). So declaring livingContent WIRED REQUIRES src/domain/density/densityCreateBoundary.js to name newSettlementLivingContentLaw in executable code, i.e. a real static import, unless the car amends this arm. MINT_SYMBOLS is `['birthConfig', 'newSettlementDensityLaw']` (43) and MINT_HOMES is the three-entry allowlist at 48-52 including src/store/settlementSliceHelpers.js.
- `src/store/settlementSliceHelpers.js:44` — THE SOLE EAGER EDGE INTO THE BOUNDARY MODULE: `export { birthConfig } from '../domain/density/densityCreateBoundary.js';` (44), written as a re-export leaf because settlementSlice.js 'is size-ratcheted at tolerance 0 and keeps one helper import home' (40-41). Census: only two static importers of densityCreateBoundary.js exist in src/ — this line and src/lib/instantWorld/composeInstantWorld.js:56 (`import { birthConfig } from '../../domain/density/densityCreateBoundary.js';`). The only consumer of this re-export is src/store/settlementGenerateAction.js:47.
- `src/domain/density/densityCreateBoundary.js:231` — MEASURED (executed replication of engineChunkLazy's eagerSourceGraph over laneB6 at ec265d24c): the main.jsx static closure is 239 modules, not the 237 the header states at line 231. Membership: densityCreateBoundary.js EAGER, densityLaw.js EAGER, settlementSliceHelpers.js EAGER, settlementSlice.js EAGER; livingContentLaw.js, livingContentLawVersion.js, livingContentSeam.js, livingContentRoster.js, settlementGenerateAction.js, composeInstantWorld.js, generationRequest.js and generateSettlementPipeline.js all NOT eager.
- `src/store/settlementGenerateAction.js:116` — BOTH BIRTH CALLERS ARE ALREADY LAZY — the measurement that reframes the whole car. The store's birth mint is at line 116: `const fullConfig = geographyLockedConfig(state.locks, state.settlement, birthConfig({`, importing birthConfig from settlementSliceHelpers.js at line 47. The module is reached only through settlementSlice.js:59's `(_generateLanePromise ||= import('./settlementGenerateAction.js'))`, so it is NOT in the eager closure. The other BIRTH, src/lib/instantWorld/composeInstantWorld.js, mints at line 121 (`...birthConfig({}),`) and is also not eager.
- `src/store/settlementSliceHelpers.js:44` — COUNTERFACTUAL, EXECUTED: re-running the eagerSourceGraph derivation with the single edge settlementSliceHelpers.js -> densityCreateBoundary.js suppressed drops the closure from 239 to 238 modules and moves densityCreateBoundary.js from EAGER to not-eager. densityLaw.js stays EAGER (it has an independent eager path), so the density mint's cost is unchanged either way. Deleting/relocating that one re-export is therefore the measured lever that takes the boundary module off first paint.
- `vite.config.js:223` — THE PRESCRIBED CURE IS MISADDRESSED, MEASURED: replicating computeEngineSharedDomain() gives 68 pre-excision members, and src/domain/content/livingContentLaw.js is NOT one of them (livingContentLawVersion.js and livingContentSeam.js are; densityCreateBoundary.js and livingContentRoster.js are not). Because ESD seeds only from src/generators and densityCreateBoundary.js is not in ESD, a new static edge from the boundary module to livingContentLaw.js would not put it in ESD either — so adding livingContentLaw.js to ENGINE_SHARED_DOMAIN_EXCISIONS is an inert `.delete()` of a non-member. The real red would come from EAGER_MODULES membership, not from ESD.
- `tests/build/vendorPdfLazy.test.js:565` — The live first-paint ceilings the wiring car's build receipt is judged against: `const CLOSURE_BUDGET_BYTES = 1_048_000;` (565), `const CLOSURE_GZIP_BUDGET_BYTES = 337_000;` (595), `const CLOSURE_BROTLI_BUDGET_BYTES = 283_000;` (596). The comment at 585-586 records the cured-tip margins as gzip 332,064 / 337,000 (4,936 B, 1.486 %) and Brotli 278,569 / 283,000 (4,431 B, 1.590 %), and at 585 cites the raw closure as 1,046,369. Raises are owner-signed; monotone-down only.
- `scripts/lib/writer-dark-register.mjs:90` — The dark-register row for customContentRoster (opened line 80) is keyed on the dial: `configKey: '_livingContentLawVersion',` (88), `dialModule: 'src/domain/content/livingContentLaw.js',` (89), `dialExport: 'NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION',` (90), `litExport: 'ROSTER_LIVING_CONTENT_LAW_VERSION',` (92). Its lighting prose (94-99) states that when the owner rolls the dial the row 'must die with its premise, which is exactly what clause S convicts'. Wiring alone (dial left at 1) does not trip it; a wiring car that also flips the dial does.
- `src/domain/density/densityCreateBoundary.js:246` — STALE MECHANISM IN THE HEADER: line 246-248 says 'The store's generation lane already awaits `loadEngine()` before it hands the config to the pipeline'. Measured, it does not. loadEngine() is defined in src/store/settlementSlice.js:42 and dynamic-imports generateSettlementPipeline.js (45); settlementGenerateAction.js never calls it — it calls `runGeneration` (imported at :40 from ../lib/generationClient.js) and, for the in-thread half, `const core = await import('../workers/generationRequest.js');` at :61. The prescription's premise names a seam the lane does not use.
- `src/domain/density/densityCreateBoundary.js:120` — THE WIRED PRECEDENT'S SHAPE, for the living-content wiring to mirror: the density law is WIRED (269) because birthConfig spreads its mint (164) and every classified BIRTH calls birthConfig, while the EXECUTOR class (120-126, defined at 78-87) exists precisely because the generation transport split the mint from the reach — src/workers/generationRequest.js runs a request whose law was already minted on the main thread and must never mint one of its own. Any living-content mint is therefore also barred from the worker/engine far side and must happen main-thread, in a BIRTH caller or in birthConfig.
## hazards
- The brief's tree address is stale twice over: laneB6 is at ec265d24c (one §909 car past the stated 6ebe0ef3b), and the two named law files are under src/domain/content/, not src/domain/density/ — a car briefed on those paths would open nothing.
- The header's prescribed cure — add livingContentLaw.js to ENGINE_SHARED_DOMAIN_EXCISIONS — is misaddressed: livingContentLaw.js is not an ESD member (measured, 68 members), so that row is an inert .delete() and buys no byte relief; the red would come from EAGER_MODULES membership instead.
- The ⭐ arm that is advertised as making the materialization dial safe scans only [...reachers, MINT_HOME], and src/store/settlementGenerateAction.js — the estate's own birth-mint home — is not in the measured 5-file reacher set, so a per-caller wiring there would land silently green.
- Flipping the row to WIRED is structurally coupled to an eager static import: the arm at :470 requires densityCreateBoundary.js to NAME the mint in executable code, and names() strips comments and strings, so the register row's string field cannot satisfy it.
- Two independent guards fire on an eager import, not one: engineChunkLazy's orphan arm (the leaf at vite.config.js:218 is excised and unpinned) and livingContentSeamLazy.test.js:134's flat assertion that livingContentLaw.js is absent from vite's own EAGER_FIRST_PAINT_MODULES.
- De-eagering densityCreateBoundary.js by relocating the settlementSliceHelpers re-export leaves it an orphan (not in ESD, not eager), and Rollup co-locates orphans into the big lazy engine chunk — the FP-G11/FP-G17 incident class — so the car must PIN placement in manualChunks rather than rely on excision.
- The header's stated closure size (237) is 239 as measured at this tip, and its 'the lane awaits loadEngine()' premise is false — the lane uses runGeneration and a dynamic import of workers/generationRequest.js — so a car that reasons from the header's mechanism rather than re-measuring will design against a seam that is not there.
- First-paint headroom is three-figure and owner-signed: raw budget 1_048_000 against a 1,046,369 measured closure, gzip margin 4,936 B, Brotli 4,431 B, and raising any of them is owner-gated, so a wiring car that costs bytes has no room to absorb them.
- Wiring and lighting must stay separate acts: the dial at livingContentLaw.js:94 sits at the default so the mint returns {} and writes zero bytes, but if the same car also flips the dial, the writer-dark-register row for customContentRoster dies by its own clause S and same-seed output moves.
- settlementSliceHelpers.js exists specifically because settlementSlice.js is size-ratcheted at tolerance 0, so any move of the re-export must be checked against that ratchet and against the walker's MINT_HOMES allowlist, which still lists the helpers file as a legitimate mint home.
## carShape
For the wiring-boundary slice I would put six things in the car, in this order.

1. RE-MEASURE FIRST, then relocate one edge. Delete the re-export at settlementSliceHelpers.js:44 and have settlementGenerateAction.js import birthConfig directly from densityCreateBoundary.js. Measured: that is the only eager edge into the boundary module, and suppressing it drops the closure 239 to 238 and takes densityCreateBoundary.js off first paint. Both BIRTH callers are already lazy, so nothing else needs to move.

2. Only then add the static import densityCreateBoundary.js to livingContentLaw.js and spread newSettlementLivingContentLaw() inside birthConfig — the WIRED shape the :470 arm demands, and the widest wiring, so every BIRTH mints and no PREVIEW can.

3. PIN, do not orphan. densityCreateBoundary.js and livingContentLaw.js both become non-ESD, non-eager orphans; pin them in manualChunks (FP-G14/G16 convention). Drop the header's ENGINE_SHARED_DOMAIN_EXCISIONS instruction: measured inert.

4. Close the walker hole — extend the :429 arm's scanned set from [...reachers, MINT_HOME] to include declared BIRTH rows and their reachesVia executors.

5. Correct the two stale header facts (237 modules; "the lane awaits loadEngine()") in the same car, with the measurement beside each.

6. Receipts: two npm run build runs, hashed-chunk listing diff (count, per-chunk bytes, content hash), EAGER_FIRST_PAINT_MODULES membership, raw/gzip/Brotli against 1_048_000 / 337_000 / 283_000, and a same-seed golden proving zero movement with the dial left at 1.

Dial stays at 1. Lighting is a separate owner act.
## openQuestions
- Does de-eagering densityCreateBoundary.js re-parent the big lazy `engine` chunk into composeInstantWorld's chunk (which statically imports it today at :56 while it is eager and therefore free)? Only a hashed-chunk listing diff from two builds can answer this, and it is the single largest unpriced risk in the car.
- Is src/store/settlementGenerateAction.js itself under a size or coupling ratchet that a new direct domain import would trip? settlementSliceHelpers.js:40-41 records the tolerance-0 ratchet on settlementSlice.js specifically; I did not establish whether the generation lane carries one of its own.
- Should the chair prefer amending LAW_WIRING_STATES / the :470 arm to admit a declared LAZY mint home (letting the mint live in the two lazy BIRTH callers with no eager import at all), rather than the de-eager-then-import route? That avoids touching first paint entirely but breaks the one-mint-in-birthConfig architecture the boundary was built on, and the header at 176-186 records that adding a wiring state has already cost one red.
- The brief frames L-MAT as 'the migration file lands in the tree UNAPPLIED; production application rides the owner's push'. This slice found no migration artifact anywhere on the wiring boundary — the living-content law explicitly has NO migration by design (livingContentLaw.js:22-24, 'THERE IS NO MIGRATION, DELIBERATELY'). Is the 'migration' the ODQ §904 row means the dial flip itself, a persistence-layer file owned by another slice, or a mislabel?
- Does the chair want the walker's MINT_HOMES entry for settlementSliceHelpers.js (walker :51) removed in the same car once the re-export leaves, or kept as a harmless allowlist row?
- Is the 12-word documentation-prose limit intended to bind the car's own header rewrites, or only this orientation return? Several corrections I would make quote the existing header at length by necessity.

# SLICE publicSafe
## facts
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:382` — The hashed core is EXACTLY five keys and nothing else: lines 382-388 read `const core = {` / `schemaVersion: SETTLEMENT_CONTENT_PROVENANCE_SCHEMA_VERSION,` / `scope: contextRecord.scope === 'campaign' ? 'campaign' : 'standalone',` / `environment,` / `bindingHash,` / `materializedDefinitions: definitions,` / `};`, and line 392 is `receiptHash: fingerprintContent(core),`. Neither `customContentRoster` nor the living-content law marker `_livingContentLawVersion` nor the settlement `_seed` is inside the hash.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:21` — `const RECEIPT_KEYS = new Set([` at line 21 holds exactly six strings — 'schemaVersion', 'scope', 'environment', 'bindingHash', 'materializedDefinitions', 'receiptHash' (lines 22-27) — and line 411 admits with `!hasExactKeys(source, RECEIPT_KEYS)`, whose helper at 109-111 is `const actual = Object.keys(value); return actual.length === keys.size && actual.every(key => keys.has(key));`. Exact-cardinality, not superset.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:412` — Line 412 is `|| source.schemaVersion !== SETTLEMENT_CONTENT_PROVENANCE_SCHEMA_VERSION` — a strict inequality against the constant declared 1 at line 18, so a schemaVersion bump is not a migration path: it rejects every persisted receipt at the first branch.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:392` — MEASURED (node probe importing the module read-only from the worktree; nothing written into the tree). A receipt built from one materialized institution plus a bindingHash hashes to `4adb7702437fb669c951c6d76fdc32e5927b4eafca9bd69229dc69293391809a`. Adding an ALWAYS-PRESENT sixth core key whose dormant value is `null` moves it to `e3ee3f1884cd02fec2ca3ce7aa5326873b080bf1ae95461fceb85832092584eb` — every existing receipt moves, 100%. Adding the same key OMITTED-WHEN-DORMANT reproduces `4adb7702…` byte-for-byte (canonical JSON identical), so zero existing receipts move; only a lit receipt gets a new digest.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:411` — MEASURED: passing today's admit a receipt with one extra key returns `{ok:false}` with message 'Settlement content provenance has an invalid shape.' — so a widened writer's output is rejected by an un-widened reader, and symmetrically a widened RECEIPT_KEYS would reject every existing six-key receipt.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:44` — `const MATERIALIZED_CATEGORIES = Object.freeze([` at line 44 already lists 'deities' (45), 'factions' (46), 'stressors' (50) and 'traditions' (53) — the four LIVING_CONTENT_BUCKETS. The receipt's category vocabulary is already wide enough to hold roster content; what keeps living content out is only that no roster definition ever reaches a mechanical surface.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/settlementContentProvenance.js:264` — `if (!definitionId || !revisionId || !SHA256_RE.test(contentHash)) return null;` — a materializedDefinitions row demands all three identity fields, and the admit repeats the rule at 466-468 plus `surfaces.length === 0` at 471.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:130` — `if (!localUid && !text(identity.customDefinitionId)) return null;` — a roster row is admitted on a localUid ALONE. MEASURED on the un-identified reference pack: the first roster row is `{"source":"custom","isCustom":true,"customDefinitionCategory":"deities","localUid":"reference-aurora-kept-oath","name":"Aurora of the Kept Oath",…}` with no definitionId, revisionId or contentHash at all.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:170` — MEASURED via `buildLivingContentRoster`: with the IDENTIFIED reference pack and a lit config the roster is 4 rows across all four buckets, 3,061 canonical-JSON bytes, `fingerprintContent` = `cf9681425f9811507b8f4a8eebe7a915de3223ade6cb7d0914408a9f03c0d0a7`, and every row does carry customDefinitionId + customDefinitionRevisionId + a valid sha256 customDefinitionContentHash. With a dark config it returns `null` (line 175, `if (!materializesLivingContent(config)) return null;`).
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/generators/generateSettlementPipeline.js:147` — The receipt is built FIRST — `const contentProvenance = buildSettlementContentProvenance(` at 147, attached at 153 — and the roster is built AFTER, `const livingContentRoster = livingContentRosterFor(` at 180, attached at 185 as `finalCtx.settlement.customContentRoster = livingContentRoster;`. The receipt therefore cannot see the roster today even in principle, and the roster is built from `options.customContent` and `finalCtx.settlement.config` (181-182), not from the settlement body.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/domain/livingContentMaterialization.test.js:560` — The existing pin: `it('the provenance receipt is untouched by the roster', () => {` at 560, asserting `expect(lit.customContentProvenance).toEqual(dark.customContentProvenance);` at 570 and an empty living-content category list at 571-577 whose message calls a widening 'a widening of a persisted, hash-validated receipt' that is 'owner-gated, not a side effect'. Its comment at 563-565 says widening 'belongs with the lighting decision, not with the machinery.' This test goes RED the moment the receipt becomes roster-aware and must be re-cut deliberately.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/store/accountImportBody.js:439` — `delete preparedResult.entry.settlement.customContentProvenance;` — when `remapAccountSettlementContentProvenance` returns not-ok the import DELETES the receipt and pushes a warning (440-443). A widening that breaks admission does not red; it silently strips receipts off imported worlds.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountSettlementContentPortability.js:182` — The remap rebuilds its own `const core = {` at 182 with the SAME five keys (183-191) and re-hashes at 194 (`receiptHash: fingerprintContent(core),`), then re-admits its own candidate at 196. So the core shape is written in TWO places — any widening must land in both or the remap's output fails its own admit.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountSettlementContentPortability.js:96` — `admitSettlementContentProvenance` has exactly ONE production consumer in the whole tree — `const admitted = admitSettlementContentProvenance(rawProvenance);` here (grep over src/ returns only lines 17, 96 and 196 of this file plus the declaration itself). Nothing re-admits a persisted receipt on ordinary save/load, so a hash break surfaces only on the account-import path.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/store/accountImportBody.js:424` — The import remaps `customContentProvenance` (424-445) and NOTHING remaps `customContentRoster`: a grep for that key across src/ returns only generateSettlementPipeline.js:185 and two documentation mentions. A lit world imported into a receiving account would carry source-namespace roster ids beside a remapped receipt.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentLawVersion.js:72` — `export const DEFAULT_LIVING_CONTENT_LAW_VERSION = 1;` (72) and `export const ROSTER_LIVING_CONTENT_LAW_VERSION = 2;` (75); `materializesLivingContent` at 112-114 is a strict `=== ROSTER_LIVING_CONTENT_LAW_VERSION`. The dial is held dormant at livingContentLaw.js:94-95 (`export const NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION =` / `  DEFAULT_LIVING_CONTENT_LAW_VERSION;`).
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/density/densityCreateBoundary.js:279` — The livingContent GENERATION_LAWS row carries `wiring: 'UNWIRED',` at 279 and `configKey: '_livingContentLawVersion',` at 278; line 204 states `living content `newSettlementLivingContentLaw()`  NO CALLER ANYWHERE`. Combined with the dormant dial this is the measured ground for the claim that NO world the product can mint today carries a roster — so the population of existing LIT receipts is zero, and an omitted-when-dormant widening moves nothing that exists.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/domain/customContentTierGates.test.js:245` — PIN 5 uses the digest as its sharpest instrument: `expect(withFaction.customContentProvenance?.receiptHash)` / `.toBe(without.customContentProvenance?.receiptHash);` (245-246), with the comment at 244 'The receipt is the sharpest single byte — it is a hash over the blob-derived core.' A receipt that starts covering roster content changes what this pin measures.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/domain/customContentPresentationClaims.test.js:80` — `'customContentProvenance',` sits inside `NON_MECHANICAL_SETTLEMENT_SURFACES` (77-93), so the neutrality projection strips it — but `changedMechanicsPaths` at 452 walks the whole settlement unstripped and feeds `reached:` at 664, and the header at 640-643 already warns that `reached` alone is 'satisfiable by a provenance digest moving while the definition itself materializes nowhere'. The instrument generates with the DARK `CONFIG` (line 201, `function generated(pack, seed, config = CONFIG) {`), so it is unaffected today.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/vite.config.js:184` — `'/src/domain/content/settlementContentProvenance.js',` is an entry in `ENGINE_SHARED_DOMAIN_EXCISIONS` (173-186) and line 728 pins the same module into `return 'engine-core-lazy';` (736). The provenance module is deliberately OUT of first paint, so any new static import it acquires is a byte question, not a style question.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/lint/writerReach.walker.test.js:911` — `expect(live.dialGated.size).toBe(30);` at 911, and `'customContentRoster on settlement',` is already row 103 of `FROZEN_DIAL_GATED` (96-127, thirty entries). Line 1165 reads `expect(litShapes.settlement.keys).toContain('customContentRoster');` — the MAT retro-control. Any new dial-gated persisted key from this car moves the literal 30.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/scripts/.observed-shape-readers-baseline.json:1` — The frozen inventory row for `src/domain/content/settlementContentProvenance.js` lists exactly nine shape-reader entries (confirmedCustomSupplyChains/customCategoryExports/customCategoryImports/customTradeEndpoints/customTradeLabels on economicState, cultDeitySnapshots/primaryDeitySnapshot on config, nearbyResourceDefinitions/nearbyResourcesCustom on settlement) and no roster row; the file records `frozenAtSha: a7ca033a67e5dfe1b8cd24506c9da330105a3dc3`, `minRows: 40`, `originMinRows: 8`. A receipt that READ `customContentRoster on settlement` would add a row here.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/lint/.tuning-inventory.json:7406` — `"src/domain/content/settlementContentProvenance.js": 1,` — the module declares one tuning constant (the `|| source.materializedDefinitions.length > 2_000` cap at settlementContentProvenance.js:415). A new numeric bound added by the car moves this ratchet row.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/migrations/185_custom_content_versions.sql:1` — Neither `receiptHash` nor `customContentProvenance` appears anywhere in supabase/migrations (grep over the whole directory returns nothing for either token; 185 and 186 match only on `_content_canonical_json`). The settlement usage receipt has no server-side mirror to widen in step.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/settlement.schema.js:297` — Neither `customContentProvenance` nor `customContentRoster` is declared in settlement.schema.js (grep returns no hits; the neighbouring receipt IS declared, `@property {GenerationCoherenceReceipt} [generationCoherenceReceipt]` at 297). Both content keys are undocumented additive surfaces on the persisted settlement typedef.
## hazards
- The worktree HEAD is ec265d24c ("§909 car 1a"), not the briefed 6ebe0ef3b — 6ebe0ef3b is two commits back — so any line number carried over from a §908-era note may already have moved and must be re-read before it is cited.
- Adding a seventh key to RECEIPT_KEYS makes `hasExactKeys` reject every existing six-key receipt, and the only production reader responds by DELETING the receipt (accountImportBody.js:439), so the failure mode is silent loss of persisted provenance on import rather than a red test.
- An always-present new core key moves 100% of existing receipt hashes even when its dormant value is `null` (measured 4adb7702… → e3ee3f18…), which is precisely the persisted-identity re-labelling THE PROMISE forbids.
- Bumping SETTLEMENT_CONTENT_PROVENANCE_SCHEMA_VERSION is not an escape hatch: the admit pins `!==` against the constant, so every persisted v1 receipt fails at the first branch with no migration path in the module.
- The receipt's MATERIALIZED_CATEGORIES already contains all four living-content buckets, so any car that routes roster rows into `materializedDefinitions` performs the presentation→mechanical promotion the roster module exists to refuse, and does it without looking like a promotion.
- A roster row may legitimately carry only a `localUid` (livingContentRoster.js:130) while `exactDefinition` demands definitionId + revisionId + sha256 (:264), so a naive join would drop such rows and produce a receipt that claims a completeness it does not have.
- The provenance module is excised from ENGINE_SHARED_DOMAIN and pinned to `engine-core-lazy`, so giving it a static import of any living-content module risks dragging the content-manifest closure into first paint against a three-figure ceiling margin — the exact measured failure recorded in livingContentSeam.js's header.
- `changedMechanicsPaths` walks the unstripped settlement, so once the receipt hashes roster content, a neutrality run performed under a LIT config would light `reached` for all 26 living-content cases from the digest alone — the false positive that file's own header warns about; it is dormant only because the instrument still generates with the dark CONFIG.
- The core's five-key shape is written in two places — settlementContentProvenance.js:382 and accountSettlementContentPortability.js:182 — and a widening applied to only one produces a remap whose output fails its own admit at line 196.
- `customContentRoster` is remapped by nothing on the account-import path, so a lit world imported into a receiving account would carry source-namespace roster identity beside a correctly remapped receipt, which is a worse lie than the current honest silence.
- Three frozen instruments sit downstream of any change here — writerReach's `live.dialGated.size` pinned to 30, the observed-shape-readers inventory row of nine entries, and the tuning-inventory count of 1 — and each moves for a different reason, so a car that budgets for one will be surprised by the others.
- The `customContentProvenance` key is absent from settlement.schema.js, so there is no typedef gate that would catch a shape widening, and the persisted surface can grow without any declaration changing.
## carShape
Slice O-11 Path 3 — the honest-receipt car, four commits, zero movement on any existing receipt.

1. WIDEN THE CORE, OMITTED-WHEN-DORMANT. Add one optional core key, `livingContentRosterHash`, emitted only when the run produced a roster — the `newSettlementLivingContentLaw()` idiom of writing nothing while dormant. Measured: the dormant canonical JSON is byte-identical, so `fingerprintContent` returns the same digest and zero existing receipts move; an always-present key, even `null`, moves all of them.

2. MAKE ADMISSION OPTIONAL-AWARE, NOT EXACT-WIDER. `hasExactKeys` must become a required-plus-optional test, and the recomputed core must include the key only when the source carried it. Measured: today's admit rejects a seven-key receipt outright, and accountImportBody.js:439 answers a failed admit by deleting the receipt. schemaVersion stays 1. The same widening lands in the portability remap's own core (accountSettlementContentPortability.js:182), or its output fails its own admit.

3. WIRE IT WITHOUT AN IMPORT. Build the roster before the receipt in the pipeline and pass the digest through the existing `context` bag, so settlementContentProvenance.js acquires no new edge and stays out of first paint.

4. RE-CUT THE PIN DELIBERATELY. livingContentMaterialization.test.js:560 asserts lit-equals-dark provenance; it must be rewritten to assert the new, narrower truth — the receipt moves for a lit world and is byte-identical for every dark one — with the dark half kept as the control.

Explicitly NOT in this car: roster rows in `materializedDefinitions`, the roster's own import remap, the migration.
## openQuestions
- Sibling receipt or widened receipt? A `customContentRosterReceipt` beside the existing one would move zero bytes of the existing artifact and need no optional-key admission at all, at the cost of a second validator and a second thing to remap. The chair should rule before the car is written, because the two shapes share almost no code.
- Which digest goes in: `fingerprintContent(roster)` over the whole roster (measured 3,061 canonical bytes for the identified reference pack) makes every authored presentation field hash-load-bearing, so editing a deity's epithet would move a tamper-evident receipt; an identity-only projection (definitionId/revisionId/contentHash triples) would not. The wide one is more honest about what was in scope; the narrow one is more honest about what is immutable.
- Should the receipt record the law version itself, not just the roster digest? `buildLivingContentRoster` returns null at zero rows, so a world that is LIT but whose pack held no living content is indistinguishable in the receipt from a dark world — which is the same silent gap one level up.
- Is `customContentProvenance` persisted server-side inside a settlement blob? Nothing in supabase/migrations names it, but I did not open the settlements table definition, and the answer decides whether this widening has a server half at all.
- Is `finalCtx.settlement.config` (read by the roster at pipeline line 182) identical to the `effectiveConfig` in scope at line 147? Moving the roster build above the receipt build requires that they be the same object or provably the same law version.
- Does L-MAT's unapplied migration touch this path at all, or is it purely the dial's own persistence elsewhere? Nothing I read connects the receipt to any SQL, so the migration may be entirely outside this slice.
- Is a roster-aware receipt wanted for a world that was lit and later unlit? The seam's dormancy claim is that such a world is indistinguishable from one never lit; a receipt key that persists would break that indistinguishability deliberately, and that is a PROMISE-adjacent call rather than a mechanical one.

# SLICE publicSafe
## facts
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/display/publicSafe.js:52` — PUBLIC_TOPLEVEL_KEYS is `Object.freeze([...])` holding 38 keys (lines 53-60). Neither `customContentRoster` nor `customContentProvenance` is on it, so both additive root keys are dropped today by the fail-closed root gate.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/display/publicSafe.js:328` — The default projection is `for (const key of PUBLIC_TOPLEVEL_KEYS) { if (!Object.prototype.hasOwnProperty.call(src, key)) continue; const sanitized = sanitizePublicValue(src[key], [key]); ... }` — the allowlist is the ONLY root gate, and each allowed subtree is seeded at path=[key].
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/display/publicSafe.js:101` — PRIVATE_KEY_RE = /(secret|private|\bdm|\bgm|guidance|dossierNotes|tabNotes|\bnotes?\b|plotHook|plot_hooks|hook|compass|chronicle|pinnedNpc|aiData|aiSettlement|aiDailyLife|narrativeNotes|identityMarkers|frictionPoints|connectionsMap|latentPantheon|seed|_config)/i . I checked every key a roster subtree can carry (schemaVersion, buckets, deities/factions/stressors/traditions, source, isCustom, customDefinitionCategory, localUid, the five customDefinition* identity keys, and every manifest field incl. name/description/agenda/methods/portfolio/domain/epithet/severity/affects/controls/rivals) — NONE matches any token, so an allowlisted roster would ride through the deeper denylist entirely un-stripped.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/security/gallerySanitizeAllowlist.contract.test.js:26` — The drift pin's SQL source is a HARDCODED path: `const MIGRATION = resolve(process.cwd(), 'supabase', 'migrations', '123_money_and_public_projection_hardening.sql');` — it never reads the net-current sanitizer.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/security/gallerySanitizeAllowlist.contract.test.js:118` — The pin itself: `it('SQL 123 allowlist == JS PUBLIC_TOPLEVEL_KEYS', () => { expect(parseSqlAllowlist(sql).sort()).toEqual([...PUBLIC_TOPLEVEL_KEYS].sort()); });` (line 119). A JS-side addition without an identical edit inside migration 123's array reds this.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/security/gallerySanitizeAllowlist.contract.test.js:37` — `const CANONICAL_ALLOWLIST = [...]` is a THIRD hand-copy of the same 38 keys, and line 76 pins the parsed SQL to it: `expect(sqlKeys).toEqual([...CANONICAL_ALLOWLIST].sort());`. So a new key must be added in three places at once (JS export, this constant, migration 123's array) or one of two arms goes red.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/security/gallerySanitizeAllowlist.contract.test.js:124` — The round-trip arm's fixture is `generateSettlementPipeline({}, null, { seed: 'allowlist-round-trip', customContent: {} })` — an EMPTY config, so no `_livingContentLawVersion` marker, so a DORMANT v1 world. Every existing round-trip arm is vacuous with respect to the roster: the key is never written in this fixture.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/migrations/123_money_and_public_projection_hardening.sql:219` — `public_toplevel constant text[] := array[` opens at 219 and the 38 quoted keys run 220-227, closing `];` at 228 — five lines of five keys plus a final line of three, in the same grouping the JS export uses.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/migrations/189_scrub_faction_member_privacy.sql:61` — `create or replace function public._gallery_sanitize_public_json(value jsonb, path text[] default '{}')` — 189 is the NET-CURRENT server definition (latest of 123/128/130/142/189), and its own header calls itself a net-current recreate of the 142 body plus one predicate. Its public_toplevel array (lines 78-87) is byte-identical to 123's.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/migrations/128_latent_pantheon_public_denylist.sql:60` — The SAME 38-key public_toplevel literal is copied verbatim into five migrations: 123:219, 128:60, 130:66, 142:79, 189:78. The estate's convention for changing the sanitizer is a NEW migration that recreates the whole net-current body (189's header states `create or replace` recreates it so callers re-point with no signature change), which means the array is re-copied every time.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/migrations/195_civility_guard_and_public_identity.sql:41` — 195 is the newest migration that mentions the sanitizer and explicitly declines to touch it: `-- _gallery_sanitize_public_json too, that is its own wave and its own ruling.` So no migration after 189 has redefined the public dossier projection.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/generators/generateSettlementPipeline.js:185` — `finalCtx.settlement.customContentRoster = livingContentRoster;` — the ONE root write of the roster, guarded by `if (livingContentRoster)` at 184, fed by `livingContentRosterFor(options.customContent || {}, finalCtx.settlement.config)` at 180-183. Reached through livingContentSeam.js, never a direct import (the header at 172-179 records that the direct import moved the first-paint static closure 1,045,910 -> 1,095,584 against a 1,047,000 ceiling).
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/generators/generateSettlementPipeline.js:153` — `finalCtx.settlement.customContentProvenance = contentProvenance;` — a SECOND additive custom-content root key that already ships on every run with a pack, and that appears in NO migration (grep for customContentProvenance across supabase/migrations returns nothing) and on no allowlist. It is the live precedent for an additive content root key deliberately dropped by the fail-closed gate.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentLawVersion.js:63` — `export const LIVING_CONTENT_LAW_CONFIG_KEY = '_livingContentLawVersion';` — the marker is a CONFIG key, never a settlement-root key. Its header (59-62) calls it a resolved generation input carried on the persisted `settlement.config`, underscore-prefixed like `_seed`.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/domain/livingContentMaterialization.test.js:285` — MEASURED, not assumed: the marker rides TWO config echoes — `settlement.config` AND `settlement._config` — and worldBytes (292) strips both. The header records that a first cut stripped only `config` and failed by exactly 29 bytes, the literal length of the appended key/value. `config` IS allowlisted (publicSafe.js:53) and `_config` is not (and is a denylist token), so on a lit world `config._livingContentLawVersion` survives to the public projection while `_config` is dropped.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentLaw.js:94` — `export const NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION = DEFAULT_LIVING_CONTENT_LAW_VERSION;` — the dial ships at 1 (dormant); the roster law is 2 (livingContentLawVersion.js:75). newSettlementLivingContentLaw() (155-163) returns `{}` while the dial is at the default, and livingContentLaw.js:150-151 records that a v2 world can today only be produced by passing `_livingContentLawVersion: 2` in a config explicitly.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:184` — `for (const bucket of LIVING_CONTENT_BUCKETS) { const projected = array(content[bucket]).map(definition => rosterRow(bucket, definition)).filter(row => row !== null); ... }` — the roster records EVERY living-content definition present in the run's customContent snapshot for deities/factions/stressors/traditions, adopted or not. It is not a record of what the settlement shows; livingContentRoster.js:22 states the roster records and does not activate, adopt, enrol or empower.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:133` — A roster row is `{ source: 'custom', isCustom: true, customDefinitionCategory: bucket, ...(localUid ? { localUid } : {}), ...identity }` plus every manifest-admitted authorable field copied verbatim from the author's definition (145-151). The roster object itself is `{ schemaVersion, buckets }` (200-203), schemaVersion pinned to 1 at line 72.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/customDefinitionIdentityProjection.js:16` — CUSTOM_DEFINITION_IDENTITY_KEYS = ['customDefinitionId', 'customDefinitionRevisionId', 'customDefinitionContentHash', 'customDefinitionVersion', 'customDefinitionFingerprint'] — stable, account-scoped definition identifiers (up to 240 chars each, MAX_IDENTITY_LENGTH at line 14) that every roster row carries.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/customContentManifest.generated.js:1328` — Parsed the generated manifest: the four living-content buckets' authorable fields are stressors {name, description(2000), severity, affects, disablesInstitutions, disablesGoods}; deities {name, alignmentAxis, temperamentAxis, lawAxis, rankAxis, portfolio(500), domain(300), authoredTemper, characterAxes, boonChannel, boonStrength, baneChannel, baneStrength}; traditions {name, motifElement, motifAct, epithet(300)}; factions {name, authority, archetype(300), agenda(2000), scale, methods(2000), magical, criminal, defenseRole, description(2000), tierMin, controls, rivals}. Every field is labelled effect presentation or mechanical; there is NO secret/goal/plotHooks field in any of the four, so the roster carries no generator-minted DM secret — but factions.agenda and factions.methods are 2000-char author-written motivation/tactics prose, the closest analogue to an NPC's stripped `goal`.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/applied-head.json:2` — `"appliedHead": 121` while the repo head is 200, so migrations 122-200 are all committed-but-unapplied. Landing a migration UNAPPLIED is the documented norm here, and 189's own header (lines 50-54) records that the applied-head ledger is deliberately NOT bumped, the migration being committed for the owner's deploy batch.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/ops/migrationRehearsal.test.js:77` — Hard pins that any new migration file breaks: `expect(plan.appliedHead).toBe(121);` (77), `expect(plan.repoHead).toBe(200);` (78), `expect(plan.pendingCount).toBe(79);` (79), the exact wave list ending `[200, 200]` (80-100), `expect(MIGRATION_WAVES.at(-1).to).toBe(MIGRATION_TRAIN_REPO_HEAD);` (101), and `expect(covered).toEqual(Array.from({ length: 79 }, (_, index) => 122 + index));` (105-107).
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/scripts/ops/migrationRehearsalCore.mjs:19` — `export const MIGRATION_TRAIN_REPO_HEAD = 200;` (with MIGRATION_TRAIN_BASE_HEAD = 121 at line 18), and line 667 raises when `repoHead !== MIGRATION_TRAIN_REPO_HEAD`. A wave row is a frozen object {id, from, to, purpose, rollback:{mode, reason}, expectedObjects:[{kind, name}]} — the 199 row at 459-490 is the smallest current template.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/ARCHITECTURE.md:269` — `- **migrations/** (200) — prod applied head tracked in \`supabase/applied-head.json\`,` and tests/docs/docCounts.test.js:77-81 asserts every stated `migrations/** (N)` equals the real file count. Adding a migration file reds that test unless this line is bumped in the same commit.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/scripts/check-migration-head.mjs:70` — `migrationNumbers()` (70-75) accepts any `^\d+.*\.sql$` file; `contiguityGaps` (78-84) and `duplicateNumbers` make a gap or a duplicate numeric prefix a hard exit(1) (126-140). So the new file must be exactly 201_<name>.sql — no gap, no collision with a parallel worktree.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/security/gallerySanitizer.pglite.test.js:31` — The three SIBLING server pins all use a latest-wins locator across all migration files — gallerySanitizer.pglite.test.js:31, factionMemberPublicParity.pglite.test.js:37 and snapshotDenylistDrift.test.js:28 each say they return the LAST definition so the NET-CURRENT body is what executes. Only the allowlist contract test in my slice is frozen to one historical file.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/security/townMapEditsPublicDrop.test.js:33` — The estate's established idiom for an additive root key deliberately kept OFF the allowlist: `expect(PUBLIC_TOPLEVEL_KEYS).not.toContain('mapEdits');` plus a behavioural arm `expect('mapEdits' in pub).toBe(false)` alongside `expect(pub.name).toBe('Publicton')`. townMapFogPublicDrop.test.js:34 is the same shape for `fogSessions`, and both headers name the gallery opt-in as owner-gated and needing an SQL-sanitizer twin migration.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/store/lifecycleRoundTrip.test.js:1203` — `expectAbsentWithAnchor([...PUBLIC_TOPLEVEL_KEYS], 'fogSessions', 'spatialLayout', 'the public top-level key allowlist');` — the stronger form, with the in-file note that a bare not.toContain would pass just as happily if the allowlist had drifted away to nothing. The helper is tests/helpers/anchoredNegatives.js:111, which refuses an anchor equal to the member and fails loudly when the anchor sibling is missing.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/scripts/lib/writer-dark-register.mjs:81` — WRITER_DARK_REGISTER (79) row 1 is `identity: 'customContentRoster on settlement'`, reason 'dark-by-construction', door kind 'generation-dial' keyed to _livingContentLawVersion / NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION / ROSTER_LIVING_CONTENT_LAW_VERSION. Its lighting prose states that when the owner rolls the dial the key becomes an ordinary written fact and the row must die with its premise. tests/lint/writerReach.walker.test.js:790 and :1224 both pin the register to exactly ['customContentRoster on settlement', 'densityRungRole on npcs'].
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/supabase/migrations/200_deity_authored_character.sql:5` — The header convention for a new migration, from the newest file: a `-- Settlement Generator — Migration NNN` title line, then a NOT-DEPLOYED-BY-THE-LANE block stating migrations are owner-gated at deploy (`supabase db push` plus a bump of supabase/applied-head.json in the same act) and that the pending state is reported by scripts/check-migration-head.mjs rather than as a failure. 199 uses an alternate boxed header with an OPERATOR NOTE section; both are current.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/domain/livingContentMaterialization.test.js:214` — The lit-run call shape a new contract arm would copy: `function generated(pack, seed, config) { return generateSettlementPipeline(config, null, { seed, customContent: identifyCustomContentPack(pack) }); }` with LIT_CONFIG (106-109) = CONFIG spread plus `[LIVING_CONTENT_LAW_CONFIG_KEY]: ROSTER_LIVING_CONTENT_LAW_VERSION`, and the pack from tests/fixtures/customContentReferencePack.js. ROSTER_KEY = 'customContentRoster' is declared at line 112.
## hazards
- The drift pin parses migration 123 by a hardcoded path (gallerySanitizeAllowlist.contract.test.js:26) while the live server function is 189's net-current recreate, so a key added only in a new migration reds the pin, and a key added only to 123 leaves the sanitizer production will actually run un-widened.
- The 38-key allowlist exists in SEVEN copies — the JS export, CANONICAL_ALLOWLIST in the contract test, and five migration literals (123/128/130/142/189) — and only three of the seven are pinned to each other, so a copy that silently diverged would never be caught by the pin in my slice.
- Adding any migration file moves the repo head off 200 and hard-fails tests/ops/migrationRehearsal.test.js's repoHead / pendingCount / wave-list / covered-range pins plus scripts/ops/migrationRehearsalCore.mjs:667's MIGRATION_TRAIN_REPO_HEAD guard, unless a wave row with purpose, rollback and expectedObjects lands in the same car.
- Adding any migration file also reds tests/docs/docCounts.test.js unless ARCHITECTURE.md:269's `migrations/** (200)` is bumped to 201 in the same commit.
- No PRIVATE_KEY_RE token matches any key a roster subtree can carry, so allowlisting customContentRoster publishes the WHOLE roster with zero deeper-denylist defense — the fail-closed second layer that has saved every previously-added top-level key does not apply to this one.
- The roster records every living-content definition that was IN SCOPE for the run rather than what the settlement adopted (livingContentRoster.js:184), so a public roster exports the author's unadopted homebrew library, not the world the viewer is looking at.
- Every roster row carries stable account-scoped identifiers (customDefinitionId / RevisionId / ContentHash / Fingerprint), so publishing them lets an observer correlate the same private definition across two separately published worlds — a linkage no other public key currently offers.
- veilPublicPayload masks identifiers deterministically and publicSafe.js:434-443 records that only public_slug and the uuid row id are known not to carry a flaggable token, so any client/server byte-parity claim about author-written roster names or agenda prose must be measured against the SQL twin, never assumed.
- The lit marker rides settlement.config._livingContentLawVersion, `config` is allowlisted at root and no denylist token matches the key, so a world's lit-ness reaches the public projection today even if the roster itself is denied.
- Every existing arm of the allowlist contract test generates a DORMANT v1 settlement (customContent: {}, no marker), so any new arm about the roster that reuses that fixture proves nothing and must pass LIT_CONFIG plus the reference pack.
- The WRITER_DARK_REGISTER row 'customContentRoster on settlement' and the two writerReach walker pins at :790 and :1224 are keyed to the roster staying dark, so a car that rolls the dial without retiring that row reds the walker.
- supabase/applied-head.json must stay at 121 — bumping it to claim an unapplied migration is live is the exact lie the ledger exists to prevent (tests/docs/migrationAppliedHead.test.js:48-53 accepts 'pending' precisely so nobody bumps it early).
- Migration numbers must be unique and contiguous (scripts/check-migration-head.mjs:126-140), so a parallel session landing its own 201 in the shared tree produces a duplicate-prefix hard failure rather than a merge conflict.
- The main tree's git status shows ARCHITECTURE.md already carrying foreign modifications, so the doc-count bump this car requires lands into a file another session may be editing.
## carShape
My car is one consist, zero behaviour change, migration unapplied. FIRST, the ruling I would put in it: keep customContentRoster OFF the allowlist and PIN that, in the townMapEditsPublicDrop idiom but with the stronger anchored form the estate already requires — expectAbsentWithAnchor([...PUBLIC_TOPLEVEL_KEYS], 'customContentRoster', 'config', ...) — plus a behavioural arm that generates with LIT_CONFIG and the reference pack, asserts the roster key IS written, and asserts toPublicSafe drops it while allowlisted siblings survive. Reason: nothing in PRIVATE_KEY_RE strips anything inside the roster, and the roster carries unadopted homebrew definitions plus stable content hashes. SECOND, if the owner wants it public instead, the migration is a 201 that CREATE OR REPLACE recreates 189's net-current body verbatim with one key added, paired in the same car with publicSafe.js's export, the test's CANONICAL_ALLOWLIST, and the drift pin re-pointed from the frozen 123 path to the latest-wins locator its three sibling pglite pins already use — otherwise the pin certifies a migration production does not run. THIRD, the toll every migration pays, in the same car: ARCHITECTURE.md 200→201, MIGRATION_TRAIN_REPO_HEAD 200→201, a wave row, and migrationRehearsal.test.js's repoHead/pendingCount/wave-list/covered pins; applied-head.json stays 121. FOURTH, the measurement I would not skip: whether config._livingContentLawVersion reaches the public projection on a lit world, since a denied roster still announces lit-ness.
## openQuestions
- Which of the two lawful shapes does ODQ §904 owner row 2 actually name for the roster — ALLOWLIST it with a SQL twin, or PIN IT OUT with an anchored negative? I found no L-MAT design doc in this worktree, and 'the MAT dial's public-safe path' is compatible with both readings.
- If the roster is to be public, should it be REDUCED to a public projection (name + category + presentation fields) the way NPCs are reduced by publicNpc, rather than shipped whole with the five customDefinition* identity keys and 2000-char agenda/methods/description prose?
- Should the drift pin be re-pointed from the hardcoded 123 path to a net-current latest-wins locator as part of this car, or is that a separate structural-prevention car? It changes what the pin certifies, so it is arguably owner-gated as a security-posture change.
- Is editing migration 123's array in place lawful given appliedHead=121 (123 has never run in production), or does the estate treat any committed migration as immutable regardless of whether it has been applied? The five verbatim copies suggest the latter, but nothing I read states the rule.
- Does the new migration need a supabase/rollback/201_*.down.sql, or does a function-replacing migration take the forward-only wave policy the way 199 does with an @rollback annotation instead?
- Should config._livingContentLawVersion be added to the deeper denylist — a lawful GROWTH in the private direction, mirroring what 099 did for seed/_config — or is a law-version marker considered public like schemaVersion and generatorVersion?
- Does the L-MAT car cover only the dossier path (_gallery_sanitize_public_json) as 142 and 189 did, or also _gallery_dm_full_json and _gallery_world_snapshot_is_safe? The world-snapshot scanner is a key-presence gate and a new root key may need a decision there too.
- Is customContentProvenance's absence from the allowlist deliberate-and-unpinned, or an accident? It already ships on every run with a pack and has no negative pin anywhere; the same car could pin it for the same price.
- Who owns bumping ARCHITECTURE.md:269 and MIGRATION_TRAIN_REPO_HEAD given a parallel session may be editing both — should the L-MAT car reserve migration number 201 by landing the file first, or coordinate through the chair?

# SLICE publicSafe
## facts
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/importScrub.js:42` — THE IMPORT SCRUB IS A DENYLIST, NOT AN ALLOWLIST, AND IT IS SCOPED TO `settlement.config`. The whole strip is one destructure: `    _seed, primaryDeityRef, primaryDeitySnapshot, cultDeitySnapshots, faithProfile,` — five named keys dropped, `...rest` kept. No settlement-ROOT key is stripped here, so `settlement.customContentRoster` is untouched by the dormancy strip on every one of the three import paths the module's header claims to be the single writer for.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/importScrub.js:80` — The SECOND strip is settlement-level but single-key: `scrubImportedTreasury` returns early unless `Object.hasOwn(economicState, 'treasury')` and drops only that one key. So the only settlement-root strip that exists drops a coin ledger and nothing else — there is no generic settlement-root key policy anywhere on the import path.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountImport.js:617` — `prepareSettlementEntry` builds the persisted settlement by SPREADING the whole normalized blob and overwriting exactly five keys: `const settlement = scrubImportedTreasury({` … `...normalized,` `neighbourNetwork: []`, `neighborRelationship: null`, `interSettlementRelationships: []`, `_seed: undefined`, `config`, `importedFrom: {…}` (lines 617–631). Every other settlement-root key — `customContentRoster` included — is carried verbatim from the user-supplied file.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountImport.js:48` — The module header's exhaustive statement of the boundary — "What is NEVER restored on either surface: `id` / `user_id` / owner / `public_slug` / `is_public` / gallery flags" plus `serviceRecords` and "the `_seed` / `scrubImportedConfig` dormancy strip" (lines 48–51) — names no content-identity key at all. The roster is outside the stated contract, not deliberately admitted by it.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:23` — THE ROSTER'S SHAPE AND ITS ONE KEY: the module header states "It is written to ONE additive key, `settlement.customContentRoster`." `buildLivingContentRoster` returns `{ schemaVersion, buckets }` (lines 200–203) where `buckets` is keyed by the four LIVING_CONTENT_BUCKETS (`deities`, `factions`, `stressors`, `traditions`) and each value is a frozen array of projected rows.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:137` — THE ROW CARRIES ACCOUNT-SCOPED IDS. `rosterRow` builds `{ source: 'custom', isCustom: true, customDefinitionCategory: bucket, ...(localUid ? { localUid } : {}), ...identity }` (lines 137–142), where `identity` is `projectCustomDefinitionIdentity(definition)` and `localUid` is `text(definition.localUid)`. Line 130 refuses a row with neither: `if (!localUid && !text(identity.customDefinitionId)) return null;` — i.e. every persisted row is joined to the AUTHORING account's content identity, which is exactly what an import must re-resolve.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/customDefinitionIdentityProjection.js:16` — The five identity keys a roster row can carry are frozen as `CUSTOM_DEFINITION_IDENTITY_KEYS`: `'customDefinitionId', 'customDefinitionRevisionId', 'customDefinitionContentHash', 'customDefinitionVersion', 'customDefinitionFingerprint'` (lines 16–22). Four of these have a 1:1 counterpart in the account-import identity map; the fifth (`customDefinitionVersion`) may be a revision NUMBER.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/customDefinitionIdentityProjection.js:97` — THE FINGERPRINT TRAP: `customDefinitionFingerprint` falls back to the content hash — `source.customDefinitionFingerprint ?? nested.fingerprint ?? source.definitionFingerprint ?? contentHash` (lines 93–99). A remap that rewrites `customDefinitionContentHash` but leaves `customDefinitionFingerprint` alone leaves the SOURCE account's hash living in a destination row under a second key.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:189` — ROW ORDER IS DERIVED FROM THE IDS THAT WOULD BE REMAPPED: rows are sorted with `compareCodepoint(text(left?.customDefinitionId) || text(left?.localUid), …)` (lines 189–194), stated at line 143 as "Manifest declaration order, so two runs of the same pack serialize identically". After an id remap the source order is no longer the order the builder would produce, so a remap must re-sort.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentRoster.js:33` — THE ROSTER HAS NO READER: "Nothing in `src/` reads this key. It is regression-grade by construction, and saying so plainly is the honest reading rather than a gap." (lines 33–34). Confirmed by grep: the only `src/` occurrences of `customContentRoster` are the pipeline write and two prose comments.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/generators/generateSettlementPipeline.js:185` — THE ONE WRITER TODAY is `      finalCtx.settlement.customContentRoster = livingContentRoster;`, guarded at line 184 by `if (livingContentRoster) {`, itself fed by `livingContentRosterFor(options.customContent || {}, finalCtx.settlement.config)` at lines 180–183. It sits three lines below the provenance write at line 153 (`finalCtx.settlement.customContentProvenance = contentProvenance;`) — the two keys are minted side by side, and only one of them is re-resolved on import.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/store/accountImportBody.js:424` — THE EXISTING ID-RESOLUTION SEAM, AND THE SHAPE THE ROSTER'S MUST COPY. Phase 4 reads `const sourceProvenance = preparedResult.entry.settlement.customContentProvenance;` (424–425), calls `remapAccountSettlementContentProvenance(sourceProvenance, contentIdentityMap, bindingDestinations)` (427–432), assigns the remapped value on success (433–435) and on failure does `delete preparedResult.entry.settlement.customContentProvenance;` plus `settlementContentWarnings.push({ name, reason: `${remappedProvenance.error} Its source provenance was removed.` })` (439–443). THE ESTATE'S LAW FOR AN UNRESOLVABLE FOREIGN CONTENT IDENTITY IS THEREFORE: DROP THE ARTIFACT AND SURFACE A PER-SETTLEMENT WARNING — never carry the source ids.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountSettlementContentPortability.js:107` — The remapper's per-definition core is four lookups against the identity map — `identityMap.definitionIds.get(definition.definitionId)`, `identityMap.revisionIds.get(definition.revisionId)`, `identityMap.revisionContentHashes.get(definition.revisionId)` and `identityMap.localUids.get(definition.localUid)` (lines 107–116) — and any miss returns `{ ok:false, code: 'settlement_content_provenance_identity_incomplete' }` (123–128). The roster row's four identity fields key on exactly the same source values, so the same four lookups resolve it.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountContentPortability.js:330` — The archive-backed identity map exposes `definitionIds`, `revisionIds`, `localUids`, `packIds`, `environmentIds`, `environmentRevisionIds` (330–341) plus `revisionContentHashes` and `revisionNumbers` built from the same `record.revisionIds` array keyed by `sourceId` (342–360). `revisionNumbers` is the map that could resolve a roster row's `customDefinitionVersion` when that value came from a revision number.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/store/accountImportBody.js:186` — THE ORDERING HAZARD, MEASURED. `let contentIdentityMap = emptyIdentityJoin.identityMap;` (186) is the default; the v3 ARCHIVE path replaces it in Phase 3 at line 349, BEFORE the Phase 4 settlement loop at 409. The legacy content-PACK path replaces it at line 631 — inside Phase 8, AFTER Phase 4. So on any v1/v2 envelope the Phase-4 remap runs against an EMPTY, `archiveBacked:false` map.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/accountData.js:208` — THE EXPORT IS A PASSTHROUGH: `const settlements = Array.isArray(state.savedSettlements) ? state.savedSettlements : [];` (208–210), placed into the envelope unmodified as `settlements,` at line 345. There is no export-side allowlist over the settlement blob, so a lit world's roster — foreign ids and all — is written into the account-transfer file verbatim.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/normalizeSettlement.js:9` — The migration/canonicalization step cannot drop the key either: the design contract says "Tolerant: missing fields default to safe values; unknown fields pass through untouched (forward compatibility)." (lines 9–10).
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/lib/saveAdmission.js:136` — The live save-admission wall applies no settlement-key allowlist: `recordFields` is `['settlement','config','aiData','campaignState','institutionToggles','categoryToggles','goodsToggles','servicesToggles','toggles']` (136–146) and each is only type-checked with `!isRecord(entry[field])`. Nothing between `prepareSettlementEntry` and `savesService.save` inspects settlement-root keys.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/display/publicSafe.js:52` — PATH 1 IS ALREADY SATISFIED BY CONSTRUCTION, AND THAT IS THE CROSS-CHECK FOR PATH 2: `PUBLIC_TOPLEVEL_KEYS` (52–60) is a fail-closed allowlist of 38 names and `customContentRoster` is not among them, so the roster already drops from every public/DM share. The account-import path has no equivalent gate — which is precisely why ODQ names it as a separate persistence path.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentLawVersion.js:63` — The lit marker is `export const LIVING_CONTENT_LAW_CONFIG_KEY = '_livingContentLawVersion';` and it rides on `settlement.config`. Grep across `src/lib/`, `src/store/` and `publicSafe.js` returns ZERO hits, and the string matches no token of `PRIVATE_KEY_RE` (publicSafe.js:103) — so the marker survives `scrubImportedConfig`, survives the public projection, and an imported world stays LIT under its own config even if its roster is dropped.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/store/accountImportSlice.test.js:749` — THE CAR'S TEST FIXTURE ALREADY EXISTS. The `importAccountData — export→import round-trip` suite (opens line 595) mocks `importCustomContentArchive` returning a full `identityMap` with `definitionIds`/`revisionIds`/`localUids` (81–110), and already asserts the remapped provenance `materializedDefinitions: [{ definitionId: 'definition-0', revisionId: 'revision-1', localUid: 'lu_import_0', … }]` (749–765) plus `expect(harbor.settlement.customContentProvenance.receiptHash).not.toBe(harborSettlement.customContentProvenance.receiptHash);` (766–767). A roster arm is a sibling assertion in the same suite.
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/lint/negativeAssertionAnchor.walker.test.js:607` — EXACT-EQUALITY PER-FILE BUDGETS bind the three files this car would touch: `'tests/lib/accountImport.test.js': 6,` (607), `'tests/lib/importScrub.test.js': 1,` (629) and `'tests/store/accountImportSlice.test.js': 9,` (734). The header states the rule the car must obey: the scan "reads EXACTLY TWO LINES for a marker: the assertion's own line, and the single line above it", and "A MARKER WRAPPED ACROSS TWO COMMENT LINES IS UN-ANCHORED".
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/lint/sovereigntyLightingContract.walker.test.js:546` — The lighting census is an exact-equality five-figure tuple in `const CENSUS_BASELINE_REL = 'tests/lint/.lighting-census-baseline.json';` over `CENSUS_FIGURE_KEYS = ['files','parked','credited','titles','suiteTitles']` (548). The baseline file's own `_doc` says "NEVER HAND-EDIT THE FIVE FIGURES" and that the refreeze "REFUSES to run on a dirty tree". The WEB-5 precedent block (5717–5735) shows the required discipline: `+13 titles, +3 suites` for accountImport.test.js and `+5 titles, +1 suite` for accountImportSlice.test.js, "ATTRIBUTED BY ISOLATION, NOT BY ARITHMETIC".
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/scripts/lib/writer-dark-register.mjs:81` — The roster is enrolled as a dark-by-construction writer row: `identity: 'customContentRoster on settlement'`, `key: 'customContentRoster'`, `shape: 'settlement'`, `writer: 'src/generators/generateSettlementPipeline.js'`, `reason: 'dark-by-construction'`, with a `door` naming `configKey: '_livingContentLawVersion'`, `dialExport: 'NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION'` and `litExport: 'ROSTER_LIVING_CONTENT_LAW_VERSION'` (81–93). Its own lighting prose (94–99) says that when the dial rolls "this row must die with its premise".
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/tests/lint/writerReach.walker.test.js:1164` — The MAT retro-control asserts the SHIPPED corpus does not carry the key: `expect(live.corpus.shapes.settlement.keys.includes('customContentRoster')).toBe(false);` (1166) and the same for the control corpus (1167), against a lit corpus that does contain it (1165). This is the instrument that will react to any change in who writes the key.
- `/Users/cstokes/Desktop/settlement-engine/docs/OWNER_DECISION_QUEUE.md:32260` — THE LAW FOR MY SLICE, ON THE LEDGER BRANCH `review-fixes-2026-07-08`. The three paths are named separately and asymmetrically: "three persistence paths traced at lighting (publicSafe allowlist drops the roster from DM-shares · accountImportBody id-resolution · provenance receiptHash widening = persisted-shape)". The public path DROPS; the import path RESOLVES. The ruling is at line 32362 (§882.13): "**LIGHTING O-11 SIGN** the three persistence paths (publicSafe allowlist, accountImport id-resolution, provenance receiptHash)." O-11 is listed there among "THE FIVE HELD, each the owner's by nature" — but §882.13's own boundary reads "pre-launch … persisted shape is CHAIR-CLASS in this window".
- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneB6/src/domain/content/livingContentLaw.js:94` — The dial is at the dormant default today — `export const NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION = DEFAULT_LIVING_CONTENT_LAW_VERSION;` (94–95) — so no shipped world carries a roster and the id-resolution car is provably byte-inert on every existing save. Lines 137–150 record that `newSettlementLivingContentLaw()` still has NO CALLER and that "a v2 world can only be produced by passing `_livingContentLawVersion: 2` in a config explicitly".
## hazards
- THE ROSTER IS INVISIBLE TO BOTH IMPORT SCRUBS — `scrubImportedConfig` is a five-key denylist over `config` and `scrubImportedTreasury` touches only `economicState.treasury` — so once the dial flips, an account import lands another account's `customDefinitionId`/`revisionId`/`contentHash`/`localUid` values inside the importer's own saved settlement with no strip, no remap and no notice.
- THE PHASE-4/PHASE-8 ORDERING SPLIT: `contentIdentityMap` is the empty, `archiveBacked:false` map at the Phase-4 settlement loop for every legacy content-PACK envelope (it is only replaced at line 631, inside Phase 8), so a roster remap written into Phase 4 will resolve nothing on v1/v2 imports and must degrade to the documented drop-with-warning rather than appear to work.
- THE FINGERPRINT ALIAS: because `projectCustomDefinitionIdentity` falls `customDefinitionFingerprint` back to `contentHash`, a remap that rewrites the content hash but not the fingerprint leaves the SOURCE account's hash persisted in a destination row under a second key — an exactness claim that is false while reading as correct.
- THE SORT KEY IS THE REMAPPED FIELD: rows are ordered by `compareCodepoint` over `customDefinitionId || localUid`, so a remap that preserves source order persists a roster the builder would never produce, silently breaking the module's own determinism claim that "two runs of the same pack serialize identically".
- A SECOND WRITER OF A SINGLE-WRITER KEY: `customContentRoster` has exactly one writer today (`generateSettlementPipeline.js:185`) and both the roster module's header and the WRITER_DARK_REGISTER row rest on that; an import-path write makes a second one, and any single-writer scan, walker or register row built on the old claim must be amended in the same car or it will red — or worse, stay green while lying.
- THE MAT INSTRUMENTS REACT TO THIS KEY: `writerReach.walker.test.js:1166` asserts the shipped corpus does NOT contain `customContentRoster`, and the register row is `reason: 'dark-by-construction'` with prose saying the row "must die with its premise" when the dial rolls — so L-MAT must retire that row, and an import-path write is a way to light the key without the dial moving.
- THE LIGHTING CENSUS IS EXACT EQUALITY over five figures in `tests/lint/.lighting-census-baseline.json`, its own `_doc` forbids hand-editing and its refreeze refuses a dirty tree, so every new `it`/`test`/`describe` this car adds must be re-derived by execution at the LANDING base and attributed by isolation — never carried from the authoring base and never computed.
- THE NEGATIVE-ASSERTION BUDGETS ARE EXACT PER FILE (accountImport.test.js 6, importScrub.test.js 1, accountImportSlice.test.js 9), and the marker scan reads only the assertion's own line and the ONE line above it — a wrapped two-line `// anchored:` comment reads correctly to a human and still counts as a violation.
- A LIT WORLD WITH NO ROSTER IS A SHAPE NO GENERATOR PRODUCES: `_livingContentLawVersion` is stripped by no scrub and matched by no `PRIVATE_KEY_RE` token, so if the car drops the roster the imported world still carries its v2 marker — the lifecycle path (import → save → regen) must be traced before choosing drop over resolve.
- EVERYTHING ABOVE IS PLAUSIBLE, NOT CONFIRMED: this was a read-only pass over a detached worktree at 6ebe0ef3b with no build, no vitest and no git mutation, so every claim is source-reading rather than executed evidence.
## carShape
O-11 PATH 2 is one car, ~120 product lines, modelled beat-for-beat on the provenance seam three lines above it.

Add `remapAccountSettlementLivingContentRoster(rawRoster, identityMap)` to `src/lib/accountSettlementContentPortability.js` — the module whose header already says it exists to keep this projection beside the binding remapper. Per row: the same four lookups the provenance remapper makes (`definitionIds`, `revisionIds`, `revisionContentHashes`, `localUids`), plus `revisionNumbers` for `customDefinitionVersion`; re-derive `customDefinitionFingerprint` from the DESTINATION hash rather than carrying it; keep `source`/`isCustom`/`customDefinitionCategory` and the authored manifest fields untouched; then re-sort each bucket with `compareCodepoint` over the NEW id, because the source order was keyed on the id we just changed. Fail closed per settlement, not per row.

Wire it in `accountImportBody.js` immediately after line 445, in the same `if (preparedResult.ok === true)` block: on failure `delete …settlement.customContentRoster` and push a `settlementContentWarnings` row. That is the estate's stated law for an unresolvable foreign content identity, and it makes the legacy-pack ordering gap (empty map at Phase 4) a documented drop rather than a silent pass-through.

Proof: two arms in the existing `export→import round-trip` suite (already-credited file, mocked identityMap in place — titles-only census delta) and unit arms in `accountSettlementContentPortability.test.js`, including the negative control that an UNMAPPED roster is removed with a warning.

Byte-inert today: the dial is at 1, so no shipped world carries the key.
## openQuestions
- DROP OR RESOLVE — the crux, and it is genuinely open. ODQ's tracing line reads the public path as DROP and the import path as "accountImportBody id-resolution", but the roster has NO reader in `src/` and says so in its own header. A one-line drop (add `customContentRoster` to a settlement-level strip) is cheaper, needs no identity map, works identically on the archive and legacy-pack paths, and costs nothing anyone can observe. Resolution is the right answer only if the roster is meant to survive a user moving their own estate between accounts. Which does the chair want signed?
- LIFECYCLE: does REGENERATION re-mint the roster after import? `_seed` is stripped but `_livingContentLawVersion` is not, so an imported world stays v2. If a later regen runs `livingContentRosterFor` against the IMPORTER's own `customContent`, it will overwrite the imported roster with importer-local ids — which would make the import-time remap redundant and the drop clearly correct. This was not traced in my slice and should be before the car is cut.
- THE RECONCILIATION BOUNDARY: `importReconciliationAdmission.js` declares `campaignState`/`aiData`/`versionHistory` UNSUPPORTED on its own boundary, and `prepareSettlementEntry`'s other caller relies on that. Must `customContentRoster` be declared there too, or does the reconciliation path get the roster silently because it shares `prepareSettlementEntry`?
- PER-ROW OR PER-SETTLEMENT FAILURE: the provenance remapper is all-or-nothing per settlement because a receipt is an exactness claim. The roster is explicitly INERT and regression-grade, so dropping only the unresolvable ROWS is defensible and loses less. Which failure grain?
- `customDefinitionVersion` can be a revision number, a definition version, or a `_schemaVersion` (identity projection lines 83–90). Only the revision-number case has a map. Remap when resolvable and drop otherwise, or drop the field unconditionally on import?
- SINGLE-WRITER AMENDMENT: does adding an import-path write require amending the roster module's "ONE additive key" header and the WRITER_DARK_REGISTER row in the SAME car, and does the writerReach MAT retro-control need a new arm to distinguish "written by the pipeline" from "carried by an import"?
- SEQUENCING AGAINST THE DIAL: L-MAT flips `NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION` to 2. Path 2's car is byte-inert while the dial sits at 1, so it can land BEFORE the flip at zero product risk — but should it, given §882.13's CH-11 ("dark-inert cars may START ahead of owner words and are dropped at zero product cost if vetoed")?
- IS O-11 STILL HELD? §882.13 lists O-11 among "THE FIVE HELD, each the owner's by nature", yet its own boundary rules persisted shape CHAIR-CLASS pre-launch, and the 2026-09-07 delegation hands the open rows to the chair's judgment. The brief treats it as decided; the chair should record which reading it is signing under, by name.

