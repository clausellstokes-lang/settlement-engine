# RECEIPT — LANE ENC-4 — **PARTIAL: BUILT AND PROVEN, ONE PROOF CLASS OWED**

⚠ **STATUS: PARTIAL, AND THE PARTIAL IS EXACTLY ONE THING — NO VITEST RAN.**
The train is complete, committed and green on every instrument reachable without vitest. The
chair's gate and two sibling lanes held this machine continuously from 07:44 to the end of this
lane (load-1 peaking at **55.25** with 16 workers; at the last probe, 5 workers and load 14.8).
The quiet-window law (load-1 < 4.0 AND zero vitest/dist-worker processes for three consecutive
60-second probes) **never opened once** — best streak 0. A background waiter and a Monitor were
armed for the lane's whole life.

⛔ **I DID NOT RUN VITEST, AND THAT WAS THE FENCE WORKING, NOT AN OMISSION.** Running under load
risks the recorded DOOR-3 under-load timeout — a FALSE RED — and would have disturbed the chair's
own register act. Every proof below was chosen and built to be executable WITHOUT vitest for that
reason, including a 42-assertion re-execution of the walker's own logic in plain node.

**WHAT IS OWED:** `tests/lint/` whole-directory, the stage + meeting suites, `tests/copy/`, and
the walker under vitest — the REGISTRATION proof (the lighting census credits suites, not logic),
plus the four `.lighting-census-baseline.json` figures predicted below.

- Lane: ENC-4 (Opus 5 under Fable 5.1 chair)
- Dock: `$SC/laneENC4` — verified on arrival: HEAD `fd8b6df0013b749e24435450931618fbe78a3f13`, porcelain **0**, detached, node_modules symlinked (untouched).
- Sealed source: `refs/preserve/enc5-words-2026-09-04` = `2c1eb302db84d158b7db1e6180aa17375f5ae183` (two files: `RECEIPT_POOLS_CHANCE_MEETING.md`, `RULING-ENC5-ROAD-B.md`)

## Progress
- [x] Dock verified
- [ ] Sealed words + ruling read
- [ ] Precedent re-derived (faithNews registry, pool leaf, annex walker)
- [ ] Bill re-derived
- [ ] Car 1: data leaf + annex
- [ ] Car 2: 7th registry family
- [ ] Car 3: walker
- [ ] Car 4: writer
- [ ] Proofs
- [ ] RETROVALIDATION ROW

---

## THE BILL, RE-DERIVED (PREDICTED before any instrument ran)

The ruling's guess: *"six ratchet figures moving in two files — likely
`tests/lint/.news-headline-contract-baseline.json` and `.news-voice-baseline.json`"*.

| # | Instrument | File | Ruling's guess | RE-DERIVED (predicted) |
|---|---|---|---|---|
| 1 | routed-token census | `tests/lint/kindPoolFloors.walker.test.js` `ROUTED_TOKENS` | not named | **379 → 380** |
| 2 | registered-kind census | same file, `REGISTERED_KIND_COUNT` | not named | **113 → 114** |
| 3 | registry-roster length | same file, `expect(REGISTRIES).toHaveLength(11)` | not named | **11 → 12** |
| 4 | small-family exact list | same file, `.toEqual(['INFORMATION','FAITH'])` | not named | **+ `'CHANCE_MEETING'`** (a THIRD one-row family — the reviewed act the list exists to force) |
| 5 | lighting census `files` | `tests/lint/.lighting-census-baseline.json` | not named | **2521 → 2522** |
| 6 | lighting census `credited` | same file | not named | **2150 → 2151** |
| 7 | lighting census `titles` | same file | not named | 23184 → 23184 + (my walker's test count) |
| 8 | lighting census `suiteTitles` | same file | not named | 6214 → 6214 + (my walker's describe count) |
| — | news headline contract | `tests/lint/.news-headline-contract-baseline.json` | **"likely"** | **ZERO — the guess is WRONG** |
| — | news voice contract | `tests/lint/.news-voice-baseline.json` | **"likely"** | **ZERO — the guess is WRONG** |

**WHY THE TWO NAMED FILES DO NOT MOVE.** Both baselines are built from `buildObservedCorpus`
(`scripts/lib/observed-shape-corpus.mjs`) — a corpus of GENERATED WORLDS, not a source scan:
`newsHeadlineContract.walker.test.js:64` and the `corpus: {pulseRoots:12, introductions:272,
retirements:32, finalEntries:240, homes:50}` block in each baseline. This car is DARK by
construction (`chanceEncountersEnabled` off in every preset), so it introduces no entry into any
generated world and neither corpus figure can move. The ruling named the one pair of files whose
figures a dark car provably cannot touch.

**NET:** at least **8 figures across 2 files**, and NEITHER file is one the ruling named.

---

## THE CARS AS LANDED

| car | sha | what |
|---|---|---|
| 1 | `382de8d66` | the annex verbatim from the seal + the pool leaf |
| 2 | `35575bc19` | the 7th registry family, the exact row, the four census figures |
| 3 | `30b779fba` | the per-program walker |
| 4 | `92ea5daa0` | the writer: the seed's completed address, the pulse wiring, two id cures |

⚠ Cars 3 and 4 were COMMITTED in the order 4-then-3 (the walker imports the builder's final
signature, so committing the walker first would have left a self-inconsistent commit). Brief
numbering is kept in the messages.

## NEW FILES BY NAME

| path | kind |
|---|---|
| `docs/content/RECEIPT_POOLS_CHANCE_MEETING.md` | the annex — byte-identical to the seal (sha256 `ca6b8608387ffcdb7742f4b562502e11952c998d6eecee895fddd6832d5b7347`, 5794 bytes, `cmp` clean) |
| `src/domain/worldPulse/chanceMeetingReceiptPools.js` | the annex-verbatim corpus + the closed band table |
| `src/domain/worldPulse/chanceMeetingNews.js` | the registry row, the cured keyed pick, the Herald entry builder |
| `tests/lint/chanceMeetingKindPools.walker.test.js` | **the one new TEST FILE** — 13 tests, 1 describe |

Files EDITED: `src/domain/realm/heraldRouting.js`, `src/domain/display/settlementRumors.js`,
`src/domain/worldPulse/envoyChanceMeetingStage.js`, `src/domain/worldPulse/envoyPulse.js`,
`tests/helpers/kindRegistryRoster.js`, `tests/lint/kindPoolFloors.walker.test.js`.

## THE BILL AS MEASURED (against the prediction above)

Every predicted figure in `kindPoolFloors.walker.test.js` was measured by execution BEFORE the
edit and matched the prediction EXACTLY:

```
REGISTRIES length      : 12        (predicted 11 → 12)
REGISTERED_KIND_COUNT  : 114       (predicted 113 → 114)
ROUTED_TOKENS          : 380       (predicted 379 → 380)
unvoiced               : 274       (predicted UNMOVED)
registered-minus-routed: 8         (predicted UNMOVED)
small families         : ["INFORMATION","FAITH","CHANCE_MEETING"]
WHAT_PHRASES size      : 245, unrouted rows: 0
chance_meeting_recorded explicitly routed: true
```

## THE LIT-ARM PROOF (flag FORCED on)

Driver `$SC/enc4-tmp/lit-arm.mjs` — a real minted errand world, `chanceEncountersEnabled: true`
(plus `npcLadderEnabled`/`memoryWeaveEnabled`, without which every meeting resolves `nothing`
on `no_home_record` and mints no seed at all — measured, not assumed):

```
receipts     : 1
heraldSeeds  : 1
  beat            : meeting          outcome : bond
  npcIds          : ["ashford:npc.envoy.1","irontown:npc.host.1"]
  npcNames        : ["A Named Legate","Host Notable"]
  settlementNames : {"irontown":"Irontown","ashford":"Ashford"}
  ENTRY.headline  : Host Notable spent a market day in Irontown with A Named Legate of Ashford
                    and found a friend; the two courts have a thread between them now that
                    neither one spun.
  ENTRY.summary   : A guest and a notable met by chance; what passed between them is now a tie
                    across two courts, and it will fade as ties do.
  ENTRY.impactKind: chance_meeting_recorded   section: events
  ENTRY.id        : wizard_news.12.chance_meeting_recorded.9e679d03e8446e58
  ENTRY.severity  : 0.56   score: 58   significance: notable
```

## THE DARK-ARM BYTE IDENTITY

Driver `$SC/enc4-tmp/dark-arm.mjs` — the LANDED dormancy harness
(`tests/property/chanceEncountersDormancyFence.test.js`) re-run in plain node against its own
frozen parent hashes, taken at `6c49ecbbf`, a commit that predates ENC-3 and therefore this lane:

```
ticks hashed         : 10   distinct: 3   errands really moved: true
THIS TREE  : ["4566c86da28f90c6","98efa7c906fe0ae1","c6d37517dc9122c8", …]
PARENT     : ["4566c86da28f90c6","98efa7c906fe0ae1","c6d37517dc9122c8", …]
BYTE-IDENTICAL TO PARENT: YES
  refusal spelling explicit false / the number one / the string "true" / an empty object /
  the string "yes"  →  identical: YES (all five)
ALL DARK ARMS IDENTICAL: YES     exit 0
```

**Zero golden movement is therefore not a hope but a measurement:** the car is dark by
construction and ten pulses of a really-moving dark run reproduce a pre-ENC-3 world byte for byte.

## TWO DEFECTS FOUND IN MY OWN CAR AND CURED BEFORE LANDING

Both measured by execution at the dock, on the FIRST end-to-end lit run:

1. **THE ID COLLISION.** A meeting key is built from the errand id and runs past `stablePart`'s
   80-character cap. Two DIFFERENT meetings between long-named towns slugged identically
   (`…northharrowfield_southharrowfield_7_northharro` for both). `appendWizardNewsEntries`
   dedupes by id, so the second line would have been swallowed **in silence** — the estate's own
   recorded hazard (`momentum.js:1152`, `supplyWebWarfare.js:892`).
2. **THE ERRAND-HANDLE LEAK.** The same slug published `envoy_errand:17` on a reader surface.
   `envoyNews.js#publicSourceRef` hides exactly this handle, in exactly those words.

Cured with envoyNews's own landed idiom (a two-part `fnv1a32` digest). Re-probed after the cure:
`COLLIDE: false`, `LEAK: false`, and the same meeting still publishes the same stable handle.

## REGISTER FIGURES — PREDICTED IN WRITING BEFORE ANY INSTRUMENT RAN

⛔ This lane takes NO register act. These are the deltas the chair must bank at the landing.

| file | figure | frozen | PREDICTED | basis |
|---|---|---|---|---|
| `tests/lint/.lighting-census-baseline.json` | `files` | 2521 | **2522** | +1 test file |
| " | `parked` | 371 | **371** | straight-line literal `describe`/`test`, nothing skipped ⇒ credited, not parked |
| " | `credited` | 2150 | **2151** | +1 credited file |
| " | `titles` | 23184 | **23198** | +14 `test(` calls, counted in the file |
| " | `suiteTitles` | 6214 | **6215** | +1 literal `describe(` |

### Ratchets measured NOT to move (each by execution, not by reasoning)

| instrument | verdict | how measured |
|---|---|---|
| `tests/lint/.news-headline-contract-baseline.json` | **UNMOVED** | corpus is `buildObservedCorpus` over generated worlds; the car is dark in every preset |
| `tests/lint/.news-voice-baseline.json` | **UNMOVED** | same corpus |
| `tests/fixtures/mechanism-lit-coverage-baseline.json` | **UNMOVED** | derivation re-run in node: both new worldPulse modules are AUTO-covered by the walker's direct imports; 0 new uncovered, 0 baseline entries newly covered |
| `scripts/.size-baseline.json` | **UNMOVED** | none of the six touched files is baselined and all are far under the 800 domain ceiling (measured on eslint's own Linter: 330 / 476 / 272 / 554 / 127 / 15) |
| `tests/lint/.wizard-news-authoring-baseline.json` (19-row debt ledger) | **UNMOVED (predicted)** | the one new governed site carries `id` + `settlementIds` + `severity` and routes on a LITERAL `impactKind` that `isExplicitlyRouted` accepts ⇒ `issues: []`, no debt row; `candidateSites === sites + 1` holds because the site is governed, not excluded |
| `src/domain/display/chroniclersLetter.js#KIND_SECTION` | **UNMOVED** | no letter row is claimed (the annex files a Herald desk only) |
| `LEGACY_UNVOICED_TOKENS` (274) / registered-minus-routed (8) | **UNMOVED** | measured 274 and 8 after the edit |

---

## SOURCE-SCAN CENSUSES RUN DIRECTLY IN NODE (no vitest needed)

**The Wizard News authoring wall** (`censusNewsAuthoringSites`, the walker's own shared census):

```
files 1101 (floor 999) · candidateSites 109 (floor 99) · sites 108 (floor 98) · paths 63 (floor 58)
candidateSites === sites + 1 : true        excludedSites: 1
MY SITE: src/domain/worldPulse/chanceMeetingNews.js:330:10
         routeField "impactKind"  routeTokens ["chance_meeting_recorded"]  issues: []
debt ledger rows: 19 (ceiling 19)   my rows in the debt ledger: 0
```

**The §886 fence over every new reader string** (all 11: six pool sentences, three band fills, the
reason, the noun phrase):

```
§886 adjacency violations: 0 · em-dash: 0 · exclamation: 0 · digit: 0
whatPhrase('chance_meeting_recorded') = "a meeting no court arranged"
phantom mints in comments: none — exactly ONE `impactKind: 'chance_meeting_recorded'` site in the tree
```

---

## FINDINGS BACK TO THE CHAIR

### F1 — ⛔ THE RULING'S BILL NAMED THE ONE PAIR OF FILES A DARK CAR CANNOT MOVE
The ruling guessed "six ratchet figures moving in two files — likely
`.news-headline-contract-baseline.json` and `.news-voice-baseline.json`". Both are built from
`buildObservedCorpus` — GENERATED WORLDS — and this car is dark in every preset, so neither figure
can move. The real bill is **8 figures across 2 files**: four in
`tests/lint/kindPoolFloors.walker.test.js` and four in `tests/lint/.lighting-census-baseline.json`.
The RULING (Road B) stands; only its mechanism was wrong — the "charter the permission, measure the
mechanism" pattern for the fourth time in this arc.

### F2 — ⛔ THE §B KIND CANNOT BE WIRED, AND HERE IS THE SLOT THAT BREAKS
`chance_meeting_exposed` has authored words, an authored `WHAT_PHRASES` phrase (§C, kept), and a
live producer (ENC-3 maps `outcome === 'exposed'` → beat `approach_exposed`). It is NOT registered,
and the reason is a slot, not a preference:

> §B every variant reads **"{counterpart} … refused what {npc} … offered"** — `{npc}` is the
> APPROACHER and `{counterpart}` is the TARGET.

The stage's typed seed carries `npcIds` / `settlementIds` in PARTY order (traveller first), and
nothing else. The approach direction lives only on the receipt, in
`envoyChanceMeeting.js`'s `lead` (`directions.sort(...)[0]`) and, indirectly, in
`receipt.grievance.{fromSid,toSid}` — none of which reaches the seed. Assigning `{npc}` and
`{counterpart}` from party order would name the wrong person as the one who made the offer roughly
half the time. **This is a finding, not an edit:** the cure is either (a) the seed gains an
`approacherNid` limb (one line in `heraldSeedsOf`, since `receipt.grievance` is right there), or
(b) the chair rules the assignment. Variant 1's "{counterpart} of {settlement}" also strains in a
traveller×traveller meeting, where neither party is of the host town — a second, smaller row for
the same ruling.

### F3 — ⛔ THE SEAM'S SEED DID NOT CARRY WHAT ITS OWN DOCBLOCK PROMISED (cured in car 4)
`envoyChanceMeetingStage.js`'s seam says the seed carries "both courts, both NAMES". It carried
IDS only. Worse, the round-trip a consumer would have had to make is WRONG:
`envoyCasting.js#rosterPersonById` matches a durable H1 id or a bare roster id, while the stage's
nids are `npcAgency.js#npcId`'s `sid:rosterId` composite (measured: `ashford:npc.envoy.1`). A
consumer that reached for the obvious reader would have got `null` for every person and failed
closed **silently, forever**. Cured by resolving both names in the stage off the seats the census
already holds.

### F4 — ⛔ ENC-3's `appendWizardNewsEntries` MECHANISM CLAIM IS WRONG (measured)
The stage docblock says: *"MEASURED, not assumed: `wizardNewsAuthoring.walker.test.js:148` pins
`candidateSites.length === sites.length + 1` EXACTLY, so a mint site whose entries come from a
variable rather than an inline governed literal breaks that relation the day it lands."* It does
not. The relation is `governed + excluded`, an append CALL is not an object literal and is not a
site at all, and `envoyPulse.js` already carried two append calls with variable arguments while
the relation was green. Re-measured with the car landed: `109 === 108 + 1`, still true. The
DEFERRAL was still right (ENC-4 owns the mint); the reason given for it was not.

### F5 — ⚠ A PHANTOM MINT IN A LANDED CAR: `faith_last_altar_dark`'s ONLY MINTER IS A COMMENT
`heraldRouting.walker.test.js`'s minter-totality arm scans RAW source for
`candidateType:\s*['"]token['"]`. Executed over `src/domain`, the single match for
`faith_last_altar_dark` is **`src/domain/display/chroniclersLetter.js:125`, a comment line**
("// mints `candidateType: 'faith_last_altar_dark'` at the one deletion path"). The real producer
(`faithNews.js`'s `candidateType: kind`) is invisible to that regex. Rewording that comment would
red the arm on a kind that IS minted. This is the estate's recorded
"a flag name in a comment is minted into the denominator" family. **Not cured here** — it is
WF-8a's row and the cure is a chair call (a written ruling, or spelling faithNews's mint as a
literal). Recorded so it is not re-found.

### F6 — TWO DEFECTS OF MY OWN, FOUND AND CURED (detail above)
The public news id collided across distinct meetings and leaked an errand handle. Both measured,
both cured with `envoyNews.js#publicSourceRef`'s landed idiom, both re-probed in the
false direction afterwards.

---

## NON-VITEST GATE STEPS — ALL RUN, EVERY EXIT CAPTURED IN-SHELL

| step | exit | note |
|---|---|---|
| `node scripts/check-domain-strict.mjs` | **0** | `✓ no strict-type regressions (1120 errors, ceiling 1120)` |
| `npx eslint src/ tests/ scripts/` | **0** | 31 pre-existing warnings, 0 errors |
| `node scripts/check-hazard-registry.mjs` | **0** | 29 classes; MACHINERY 13/9, OWED 16/18 |
| `node scripts/premortem.mjs --self-check` | **0** | 28 predicates |
| `node scripts/implementation-packets.mjs validate` | **0** | 182 packets |
| `node scripts/check-tuning-bands.mjs` | **0** | 9 ratified soak bands — ⛔ this lane moved no tuning value |
| `node scripts/check-migration-head.mjs` | **0** | pre-existing undeployed-train note, not this lane's |

⚠ **THE STRICT RATCHET CAUGHT A REAL RED IN CAR 4** and it is recorded rather than hidden:
`envoyPulse.js` went 14 → 15 strict errors on
`TS2345: Argument of type 'unknown[]' is not assignable to parameter of type
'RawWizardNewsEntry[]'`. Cured in `f5f3ccee1` by annotating the collector from the SINK's own
signature (`NonNullable<Parameters<typeof appendWizardNewsEntries>[1]>`, `wizardNews.js:1131`'s
spelling), never a hand-written entry type. Re-run captured: **EXIT 0**, total unchanged at 1120.

Car sha list now: `382de8d66` · `35575bc19` · `92ea5daa0` · `30b779fba` · `f5f3ccee1`

---

## RETROVALIDATION ROW — ⟦Seat: Opus 5 — Fable-unvalidated · Lane: ENC-4⟧

The Fable chair must re-derive each of these. They are Opus judgments taken inside the delegation,
recorded so they can be vetoed.

| # | what was JUDGED | what the chair must re-derive | receipts (absolute paths) | priority |
|---|---|---|---|---|
| R1 | **ONE registry row, not two.** The §B kind's words are sealed and good and its producer is live, but the seed carries no approach direction, so `{npc}`/`{counterpart}` have no honest assignment. I registered `chance_meeting_recorded` alone and reported §B as a finding. | Whether §B should ship now with an `approacherNid` limb added to the seed (a one-line change in `heraldSeedsOf`, since `receipt.grievance.{fromSid,toSid}` is already in scope), or wait for a ruling on the traveller×traveller "{counterpart} of {settlement}" strain. | `/private/tmp/.../laneENC4/src/domain/worldPulse/chanceMeetingNews.js` (registry docblock) · `…/src/domain/worldPulse/envoyChanceMeeting.js:892-960` (the `lead`) | **HIGH** — it is the difference between a half-family and a whole one |
| R2 | **The stage's seed gained two ADDRESS fields** (`npcNames`, `settlementNames`). I judged names to be address rather than prose, so the ENC-3 seam ruling (no kind, no significance, no sentence) is untouched. | That "names are address" is the right reading of the seam ruling, and that resolving them in the stage — rather than at the caller — is the right home. | `…/src/domain/worldPulse/envoyChanceMeetingStage.js` (`heraldSeedsOf` + the name book) · commit `92ea5daa0` | **HIGH** — it edits a landed ENC-3 file |
| R3 | **`CHANCE_MEETING_PRESENTATION = {severity: 0.56, score: 58}`** — `envoyNews.js`'s `notable` pair, transcribed under GR-4B §6.2's written ruling, as a single frozen pair rather than a seventh three-branch `presentationWeight`. | Whether transcribing a landed presentation pair counts as "no tuning value" under this lane's fence, and whether refusing the ruled FORM (to avoid two unreachable branches) is the right call. ⚠ The estate now carries SIX hand-rolled `presentationWeight` copies; this is a live instance of the accruing-copies family. | `…/src/domain/worldPulse/chanceMeetingNews.js` (the `CHANCE_MEETING_PRESENTATION` docblock) · `docs/implementation/packets/foreign-policy/GR-4B.md:651,653,714,971` | **MEDIUM** |
| R4 | **No `KIND_SECTION` row.** The annex files a Herald desk and names no Chronicle section, and the routing walker asserts KIND_SECTION → routed and never the reverse. | That the beat genuinely files no Chronicler's-Letter section. If it should, the row needs a QUOTED `candidateType:` literal too (see F5 — the minter scan is raw-text). | `…/src/domain/worldPulse/chanceMeetingNews.js` (the "WHY THERE IS NO CHRONICLER'S-LETTER ROW" block) | **MEDIUM** |
| R5 | **The third one-row family.** CHANCE_MEETING joins INFORMATION and FAITH on `kindPoolFloors`'s exact small-family list, with the shrink-back recorded as an obligation. | That a third exception is acceptable rather than a signal the floor-of-five rule needs re-cutting. | `…/tests/lint/kindPoolFloors.walker.test.js` (the amended `.toEqual([...])` and its comment) | **MEDIUM** |
| R6 | **The local block-split extractor** in the walker, rather than the shared `receiptAnnexPool`. Driven by a measured fact: this annex writes a second numbered list inside the kind block, so the shared extractor reads twelve rows where six are the pool. | Whether the split belongs in `tests/helpers/receiptAnnex.js` as a declared option instead, so the next volume with a slot declaration does not fork again. | `…/tests/lint/chanceMeetingKindPools.walker.test.js` (`annexBlocks` + the three MUTANT arms) | **LOW** |
| R7 | **The public-ref cure** (a two-part `fnv1a32` digest) applied to my own new id, on `envoyNews.js#publicSourceRef`'s precedent. | Nothing about the cure — it is measured in both directions. But: are OTHER news ids in the tree built by slugging a long key through `stablePart`'s 80-char cap? This lane looked only at its own. | `…/src/domain/worldPulse/chanceMeetingNews.js#publicMeetingRef` · commit `92ea5daa0` | **MEDIUM** — the class may be wider than one car |
| R8 | **F5, reported not cured:** `faith_last_altar_dark`'s only minter for `heraldRouting.walker.test.js`'s minter-totality scan is a COMMENT. | Whether to cure it (a quoted literal in `faithNews.js`, or a `KIND_SECTION_MINTER_RULINGS` row) or accept it. It is WF-8a's row, not this lane's. | `…/src/domain/display/chroniclersLetter.js:125` · `…/tests/lint/heraldRouting.walker.test.js:73-85` | **MEDIUM** |

---

## WALKER DRY RUN (plain node, no vitest — the gate held the machine)

Every load-bearing assertion of `chanceMeetingKindPools.walker.test.js` re-executed outside vitest,
21 of 21 PASS, including all three MUTANT arms:

```
PASS  pool === annex lines            PASS  pool is the corpus module array
PASS  requiredSlots === annex declared PASS  sentinel witness agrees
PASS  sentinel can disagree (6)        PASS  registrationReasons []
PASS  depth === notable floor          PASS  unique sentences
PASS  desk events + explicitly routed  PASS  unfiled token: same desk, NOT routed
PASS  WHAT_PHRASES row exact           PASS  no KIND_SECTION row
PASS  reach all six                    PASS  reach [1] without home
PASS  probes                           PASS  unknown kind null / known truthy
PASS  MUTANT dup heading throws        PASS  MUTANT rotted kind throws
PASS  MUTANT lost marker throws        PASS  live split is 6 and 6
PASS  positive controls (names in lines)
```

## VITEST PROOFS — **PENDING THE QUIET WINDOW**

⛔ NOT YET RUN. The chair's gate held the machine for this lane's whole build (load 1 peaked at
**55.25** with 16 vitest workers). The quiet-window law (load-1 < 4.0 and zero vitest/dist-worker
processes for three consecutive 60-second probes) has not opened once; a background waiter is
armed. Every proof above was chosen to be executable WITHOUT vitest for that reason.

---

## ⛔ THE BILL, CORRECTED — A FIFTH INSTRUMENT THE RULING NEVER CONTEMPLATED

My own first bill (above) was itself incomplete. Reading the scanner family a src-adding train
reds — rather than waiting for a red — found a FIFTH instrument, and it is a **SOURCE registry**,
not a test file, which the ruling's "two files" framing could not have covered.

`tests/lint/chooserTotality.walker.test.js` scans `src/domain/worldPulse` for the KEYED_RACE
signature `\bhash01\s*\(` and attributes each hit to its nearest preceding top-level declaration.
Replicated at the dock BEFORE any edit:

```
UNCLASSIFIED (must be []): ["src/domain/worldPulse/chanceMeetingNews.js#chanceMeetingLine"]
DEFER rows now: 35 (ceiling 35)
```

Cured in `258125e72`:

| file | figure | frozen | now | note |
|---|---|---|---|---|
| `src/domain/worldPulse/habitForkRegistry.js` | a new `fork({...})` row | 41 rows | **42 rows** | HBF-42, the FOURTH member of the HBF-07 / HBF-33 / HBF-34 prose-pick family — siblings' disposition and closeOwed taken verbatim |
| `tests/lint/chooserTotality.walker.test.js` | `DEFER_CEILING` | 35 | **36** | the same move IN-1c-a and WF-8a each made for their own family |

Re-measured after: `UNCLASSIFIED: []` · `GHOSTS: []` · 42 rows · 36 DEFER · `domain` rows still 14
(`NAMED_DOMAIN_ROWS` pin unmoved) · 42 unique forkIds · 0 LEARN rows.

### FINAL BILL

| # | file | figures |
|---|---|---|
| 1 | `tests/lint/kindPoolFloors.walker.test.js` | 4 |
| 2 | `tests/lint/.lighting-census-baseline.json` | 4 (a REGISTER ACT — the chair's, at the landing) |
| 3 | `tests/lint/chooserTotality.walker.test.js` | 1 |
| 4 | `src/domain/worldPulse/habitForkRegistry.js` | 1 row (a source registry mint) |
| — | `.news-headline-contract-baseline.json`, `.news-voice-baseline.json` | **0 — the two the ruling named** |

**10 movements across 4 files, and the ruling named 6 across 2 — neither of which is on this
list.** The RULING (Road B) is sound; its mechanism was wrong in place, in count and in kind.

---

## ⛔ THE BILL, CORRECTED A SECOND TIME — A SIXTH INSTRUMENT, AND A RENAME

Reading the scanner family again (rather than waiting for a red) found one more, of exactly the
shape that caught car 5: a walker TOTAL over a directory that reds on any member it cannot
classify.

`couplingInclusion.walker.test.js` is total over `CENSUS_SCOPE_RE =
/^src\/domain\/(?:worldPulse|spatial)\//` and its header rules that the census "reds on any OTHER
unlayered module, so this map is the only door". Its own `LAYER_PATTERNS`, evaluated at the dock
against real paths:

```
** UNLAYERED **    src/domain/worldPulse/chanceMeetingNews.js
** UNLAYERED **    src/domain/worldPulse/chanceMeetingReceiptPools.js
GRAMMAR            src/domain/worldPulse/envoyChanceMeeting.js
GRAMMAR            src/domain/worldPulse/envoyChanceMeetingStage.js
GRAMMAR            src/domain/worldPulse/envoyPulse.js
```

**The cure was my own naming, not a roster row.** Every other ENC module is `envoyChanceMeeting*`;
mine were the only two that were not, and the GRAMMAR pattern
`/^src\/domain\/worldPulse\/(?:treaty|peaceTerms|…|envoy)/` reads exactly that prefix. Renamed in
`080c4412d`:

| old | new | now |
|---|---|---|
| `src/domain/worldPulse/chanceMeetingNews.js` | `src/domain/worldPulse/envoyChanceMeetingNews.js` | **GRAMMAR** |
| `src/domain/worldPulse/chanceMeetingReceiptPools.js` | `src/domain/worldPulse/envoyChanceMeetingReceiptPools.js` | **GRAMMAR** |

Both now sit in the SAME layer as the stage and the pulse, so **no cross-layer pair is minted at
all** — no `LAYER_PATTERNS` edit, no `ARGUED_UNLAYERED` row, no growth of the frozen unlayered
baseline. `LAYER_FLOORS.GRAMMAR` is a floor (`toBeGreaterThanOrEqual`), so +2 members is lawful.

### FINAL FILE LIST (superseding the earlier table)

| path | kind |
|---|---|
| `docs/content/RECEIPT_POOLS_CHANCE_MEETING.md` | the annex, byte-identical to the seal |
| `src/domain/worldPulse/envoyChanceMeetingReceiptPools.js` | the annex-verbatim corpus + the band table |
| `src/domain/worldPulse/envoyChanceMeetingNews.js` | the registry, the picker, the Herald entry builder |
| `tests/lint/chanceMeetingKindPools.walker.test.js` | **the one new TEST FILE** — 1 describe, 14 tests |

EDITED: `heraldRouting.js` · `settlementRumors.js` · `envoyChanceMeetingStage.js` · `envoyPulse.js`
· `habitForkRegistry.js` · `kindRegistryRoster.js` · `kindPoolFloors.walker.test.js` ·
`chooserTotality.walker.test.js`.

### FINAL BILL — 10 movements across 4 files

| # | file | figures | ruling named it? |
|---|---|---|---|
| 1 | `tests/lint/kindPoolFloors.walker.test.js` | 4 | no |
| 2 | `tests/lint/.lighting-census-baseline.json` | 4 (REGISTER ACT — the chair's) | no |
| 3 | `tests/lint/chooserTotality.walker.test.js` | 1 (`DEFER_CEILING` 35→36) | no |
| 4 | `src/domain/worldPulse/habitForkRegistry.js` | 1 row (HBF-42) | no |
| — | `.news-headline-contract-baseline.json` | **0** | **YES — wrongly** |
| — | `.news-voice-baseline.json` | **0** | **YES — wrongly** |

Instruments the car was steered AROUND rather than paying: `couplingInclusion` (cured by the
rename), `.size-baseline.json`, `.domain-any-baseline.json`, `.wizard-news-authoring-baseline.json`,
`mechanism-lit-coverage-baseline.json`, `KIND_SECTION`, `LEGACY_UNVOICED_TOKENS`.

### CAR SHAS

`382de8d66` · `35575bc19` · `92ea5daa0` · `30b779fba` · `f5f3ccee1` · `258125e72` · `080c4412d`

## RETROVALIDATION ROW — ADDENDUM (cars 5 and 6)

| # | what was JUDGED | what the chair must re-derive | receipts | priority |
|---|---|---|---|---|
| R9 | **`DEFER_CEILING` 35 → 36 and HBF-42 minted DEFER.** I judged ROAD B to be the chair authorization the ceiling comment requires ("a chair-authorized registry mint landing its fork row, never a lane absorbing a discovery"), on the exact precedent of IN-1c-a (HBF-33) and WF-8a (HBF-34). | That ROAD B authorizes the fork row, and that DEFER — not STAY — is right. ⚠ The prose-pick family is now FOUR rows wide across four waves asking one question; that width is itself an argument for closing it, and closing it is a chair act, not a lane's. | `…/src/domain/worldPulse/habitForkRegistry.js` (HBF-42) · `…/tests/lint/chooserTotality.walker.test.js` (the ceiling ledger comment) · commit `258125e72` | **HIGH** — a ceiling the volume says may only move by chair act |
| R10 | **The two modules were RENAMED** to `envoyChanceMeeting*` so the coupling census layers them GRAMMAR, instead of adding a `LAYER_PATTERNS` row or an `ARGUED_UNLAYERED` entry. | That taking the volume's own naming is the right cure rather than arguing a new layer home for an ENCOUNTERS news leaf — note that `faithNews.js` and `informationNews.js` DO carry their own layer patterns (FAITH, INFO), so a `CHANCE_MEETING`/`ENCOUNTERS` layer was an available alternative and was NOT taken. | measured layer table + commit `080c4412d` | **MEDIUM** |

### F7 — ⚠ A LANDED WALKER'S JUSTIFICATION COMMENT IS WRONG ABOUT FOUR OF ITS FIVE EXAMPLES

`couplingInclusion.walker.test.js` justifies giving `treasuryNews.js` its own exact-path layer row
with this, verbatim:

> "no other `*News.js` module in the estate carries a layer — sovereigntyNews, faithNews,
> informationNews, grammarNews and commercialReasonsNews all sit in the argued/unlayered roster."

Measured by evaluating the file's OWN `LAYER_PATTERNS` against real paths:

```
sovereigntyNews         layer=NONE      in-unlayered-baseline=True    ← the claim holds
faithNews               layer=FAITH     in-unlayered-baseline=False   ← FALSE
informationNews         layer=INFO      in-unlayered-baseline=False   ← FALSE
grammarNews             layer=GRAMMAR   in-unlayered-baseline=False   ← FALSE
commercialReasonsNews   layer=TRADE     in-unlayered-baseline=False   ← FALSE
```

Four of the five DO carry a layer — not by an exact-path row, but by their family prefix
(`faith[A-Z]`, `grammar[A-Z]`, `commercial[A-Z]`, and the INFO family's own). The RULING
(`treasuryNews.js` → INTERIOR) may still be right; the reason recorded beside it is not. Fifth
instance in this arc of a right ruling carrying a wrong mechanism.

⚠ **It bears directly on R10.** Because every other family's news leaf takes its FAMILY's layer,
a `CHANCE_MEETING` / `ENCOUNTERS` layer was a live alternative for these two modules and I did
NOT take it — I took the `envoy` prefix, because the ENC volume's other three modules are already
GRAMMAR and the pulse that mounts them is GRAMMAR too. That is a judgment, and it is R10's.
**Not cured here** — the comment is `couplingInclusion`'s, not this lane's.

---

## THE NEW TEST FILE, CHECKED AGAINST THE WALKERS THAT POLICE TEST FILES

Replicated in plain node rather than assumed:

| walker | rule | measured |
|---|---|---|
| `negativeAssertionAnchor.walker.test.js` | every `not.toContain` / `not.toMatch` / `not.toHaveProperty` needs `// anchored:` on its own line or the one above; the frozen list is exact-equality shrink-only, so a NEW file with an un-anchored site must take a row | **12 scanned matchers, 0 un-anchored**, file correctly absent from the frozen list |
| `contractTestAntiVacuity.walker.test.js` (scope includes `tests/lint/*.test.js`) | Rule 1a bare skip-guard before an `expect(` | none — no `continue`/`return` guards in any test |
| " | Rule 1b an extractor binding (`*Body` / `*Block` / `*Def` callee) in a negative matcher with no non-empty guard | `annexBlocks` ends in **Blocks**, and `EXTRACTOR_CALLEE_RE` anchors on `Block$` — no match; every extract is additionally guarded by a throw and pinned non-empty |
| " | Rule 2 an exhaustive-claim title backed by a local pure literal in a file that derives NOTHING from source | the file derives from source (`readFileSync` + `matchAll`), so the file gate exempts it — and the claims really are derived from the document |
| `sizeBaseline.test.js` | new file over its layer ceiling | src files measured 127 / 15 effective lines against a domain ceiling of 800 |

### F8 — ⛔⛔ `tests/copy/voiceMechanics.test.js` IS RED AT THE LANDED TIP, 69 FILES AND 100 OVER BUDGET

The brief anticipated this shape ("expect a shrink-only refusal if a magnitude must RISE: that is
a FINDING for the chair, not a re-record"), and it is real and much larger than one lane.

Measured with the walker's OWN `stringLiteralContents` extracted verbatim (it skips line and block
comments, so this counts STRING LITERALS, not docblocks), over the walker's own scan set
(`src/data` + `src/domain`, `.js`, excluding `*.test.*`):

```
DIFF ROWS AT THE LANE TIP: 69
TOTALS: em 770 (budget 670) · bang 15 (budget 15)
```

The assertion is an EXACT per-file match (`b.em !== c.em || b.bang !== c.bang`), and a file absent
from the baseline counts as zero — so all 69 are failures. The total is **100 over** `EM_BUDGET`,
and `bang` sits exactly ON its budget of 15 with no headroom.

**68 of the 69 name files this lane never touched.** The one that does is
`src/domain/worldPulse/habitForkRegistry.js` (baseline 0 → current 34), and this lane's
contribution to it is proven ZERO: the dock base `fd8b6df00` measures `{"em":34}` and the lane tip
measures `{"em":34}` after `4fdc05f00` gave back the two HBF-42 introduced. None of
`envoyChanceMeetingNews.js`, `envoyChanceMeetingReceiptPools.js`, `envoyChanceMeetingStage.js`,
`envoyPulse.js`, `settlementRumors.js` or `heraldRouting.js` appears in the diff at all.

⛔ **NOT CURED, DELIBERATELY.** Curing it is a content program across 69 files; re-recording it is
a register act (and `writeShrinkOnlyBaseline` THROWS on a growth by design, so `--update` cannot
bank it either). The lawful path is the chair's. Reported, with the measurement, so it is not
re-found.

⭐ The lane's own eleven authored reader strings carry ZERO em-dashes and ZERO exclamations by
construction — the annex forbids both and the walker scans for both on rendered prose.

## WALKER DRY RUN — PART TWO (the writer and the id cure), plain node

The 21 arms the first dry run did not cover, all PASS:

```
PASS address: settlementIds / settlementNames / npcIds     PASS sourceEventId shape
PASS typed action (routed)      PASS names in headline      PASS reason cased
PASS governed metadata          PASS presentation pair      PASS id shape
PASS fail-closed: three silent outcomes                     PASS fail-closed: §B beat + empty beat
PASS fail-closed: holes (6 shapes)                          PASS fail-closed: no args
PASS deterministic              PASS three bands reach their fill
PASS no patch fields            PASS both built / ids differ / no leak / ref stable
```

**42 of the walker's assertions re-executed outside vitest, 42 PASS.** The vitest run remains
owed as the registration proof (the lighting census credits suites, not logic), not as the
correctness proof.

---

## FINAL STATE

- Dock `$SC/laneENC4`, **porcelain 0**, `node_modules` still symlinked (never materialised).
- **9 cars**, all trailered `Seat: Opus 5 — Fable-unvalidated` / `Lane: ENC-4`:

```
382de8d66  car 1    the annex verbatim from the seal + the pool leaf
35575bc19  car 2    the 7th registry family, the exact row, four census figures
92ea5daa0  car 4    the writer; the seed's completed address; two id cures
30b779fba  car 3    the per-program walker
f5f3ccee1  car 4.1  the domain-strict cast, taken from the sink's own signature
258125e72  car 5    HBF-42 + DEFER_CEILING 35 -> 36 (a bill item the ruling missed)
080c4412d  car 6    the rename that layers both modules GRAMMAR
4fdc05f00  car 6.1  HBF-42 gives back its two em-dashes
7be568047  car 6.2  the one address name that may be blank says so
```

- **NO register act taken.** No `--update`, no `--write`, no `--genesis`, no refreeze env var.
- **No word changed**, no tuning value moved, no flag flipped, no `--amend`, no `git stash`,
  no `git add -A`, no ref written, no rebase, no push.
