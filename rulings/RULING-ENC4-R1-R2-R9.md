# RULING — ENC-4's three high-priority rows (2026-09-05, Fable 5.1; the lane's receipt is sealed `enc4-receipt-2026-09-05`)
**R1 — ONE registry row now; §B (`chance_meeting_exposed`) is DEFERRED to its own car, with the words untouched.** The §B
sentences ("{counterpart} refused what {npc} offered") assign a direction the stage's seed does not carry (parties arrive in
census order). RATIFIED: registering `chance_meeting_recorded` alone and reporting §B was right — a sentence that names an
offerer it cannot know would be fluent and false about half the time (the wrong-ROLE law from the warFaith desk). The cure is
NOT the one-line seed limb by itself: `approacherNid` is determinable for traveller × resident (the traveller approaches)
and NOT for traveller × traveller (symmetric). RULED: a follow-up car ENC-4b adds `approacherNid` to `heraldSeedsOf` for the
traveller × resident kind ONLY, registers §B for that kind, and keeps traveller × traveller on the recorded row; the annex
gains the one-sentence rule "§B speaks only where the approach is known". The sealed words do not change.
**R2 — names are ADDRESS, not prose. RATIFIED.** `npcNames` / `settlementNames` on the stage's seed carry the NEWS ADDRESS
LAW's chain (who, where) and mint no kind, no significance, no sentence — the ENC-3 seam ruling stands untouched; resolving
them in the stage (the one place that holds the roster) rather than at every caller is the right home. The lane's F3 (the
seam never carried the names its docblock promised; the obvious id round-trip would have failed silently because
`rosterPersonById` cannot resolve `npcId`'s composite spelling) is a defect it cured on the way — recorded, retrovalidation owed.
**R9 — `DEFER_CEILING` 35 → 36 with `HBF-42` minted DEFER. RATIFIED.** The ceiling's own comment demands "a chair-authorized
registry mint landing its fork row, never a lane absorbing a discovery"; ROAD B is that authorization (the chair ruled the
seventh family and the exact row), on the precedent of IN-1c-a (HBF-33) and WF-8a (HBF-34). DEFER, not STAY: the chooser
totality walker must see the kind as a deliberate deferral until §B lands. A raise by exactly the measured residual (one row,
one kind), attributed. ⚠ The lane's "prose-pick" caveat on R9 is read at the landing.
**Landing note:** ENC-4's vitest registration proof is OWED (no quiet window opened under the gate); it runs at the ENC-4
landing before the registers — the walker, `kindPoolFloors.walker` (four figures move: 379→380, 113→114, 11→12, the small
family gains `CHANCE_MEETING`), `chooserTotality.walker` (35→36), `tests/lint/` whole. The lighting census moves +1 file / +14
titles / +1 suite (a new test file) — the chair re-takes it. The two news-contract baselines the ruling guessed cannot move
(both are built from generated worlds and the car is dark in every preset) — the ruling's mechanism was wrong, the class right
(the seventh instance of charter-the-permission-measure-the-mechanism).
