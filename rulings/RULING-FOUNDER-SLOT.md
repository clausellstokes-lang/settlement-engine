# RULING — the `{founder}` slot: the annex moves to the producer; a founder NAME is an owner row (Fable 5.1 chair, 2026-09-05)
GEN2 measured (finding 2): the annex declares `{founder}` `proper` (a NAME) while the only producer, `history.founding.foundedBy`, writes a lowercase DESCRIPTIVE PHRASE. The lane rightly left the slot unfilled.
RULED: **the annex moves.** `{founder}` is re-declared as the descriptive phrase class the producer actually writes (the same bare-common family DOCKET-2 item 8 is measuring), and every sentence that reads honestly with a phrase ("founded by a guild of masons") keeps it; any variant whose grammar NEEDS a name is marked name-only and WITHHELD until a name exists. Rationale: a producer that mints founder NAMES is a generation-side persisted-shape change (a new field on `history.founding`) — an OWNER row, not a chair's, and THE PROMISE's immutable-history clause makes retro-naming a settled world's founder a tuning-class act. The words bend to the tree; the tree does not bend to the words without the owner.
Owner row opened (ODQ at the §900 collection): a founder-name producer for new worlds (never re-derived for existing seeds).
Sibling findings from the same receipt, for §900: (4) the two sibling desks' `{timeband_age}` declination is STALE — `heraldCausalGrammar.js`'s `timeBandOf`/`timeBandWord` is the duration former, a zero-import leaf; a DESK car (lane) may fill it on both desks. (5) the duration ladder tops out below the history chapter (89 of 108 generated events are older than the sixth band's adverbial reach) — a ladder + corpus act: the chair authors the adverbial form of the sixth band; a lane wires it.
