# RULING — the clamp program's FINAL wave: the ceiling rises 62 → 69 by exactly the measured residual, with seven recorded reasons (2026-09-05, Fable 5.1)
**The mechanical work is exhausted.** Three waves migrated 6 + 3 = 9 hand-rolled clamps onto `src/kernel/math.js`
(wave 1: six; wave 2: `dispositionLedger.js`, `cartographyBuildings.js`, `cartographyMultiplicity.js`). Of the ten copies
outside the 62-file baseline, **seven remain, every one with a recorded reason that the estate's own doctrine protects:**
| file | why it does not move | recorded at |
|---|---|---|
| `worldPulse/warAllianceRisk.js` | excluded by name, wave-0 fence 3 | `clamp-wave0-ruling-2026-09-04` |
| `worldPulse/conquestFeasibility.js` | excluded by name, wave-0 fence 3 | same |
| `worldPulse/conquestIntent.js` | import pin `envoyK3BeliefSeam.test.js:356` (`[]` imports) — a belief pin outranks a bookkeeping ratchet | `clamp-w1-rulings-2026-09-05` |
| `worldPulse/conquestExecution.js` | header "NO IMPORTS AT ALL"; `warSeatBooksPartition.test.js:313` rules such a header WINS | same |
| `worldPulse/razing.js` | header "NO IMPORTS AT ALL"; `razingWr8.test.js:616` executes `not.toMatch(/^\s*import\s/m)` | `clamp-w2-receipt-2026-09-05` |
| `worldPulse/razingWitness.js` | the same header AND the executed pin at `razingWitnessWr8.test.js:494` | same |
| `worldPulse/razingExecution.js` | wave-0 FENCE 1: its local `clamp01` IS the `num()`-style screen at nine `unknown`-typed reads — `typecheck:domain:strict` +9 refuted a migration that had passed a 1,155-row differential (a differential proves VALUES, not TYPES) | same |
**RULED:** `BASELINE_CEILING` 62 → **69**, the seven files ENTER the baseline list, and the test's docblock records the
seven reasons by name (the `sovereigntyAppraisal` / CR-WR10-A(b) form — the one raise the ceiling ever took, for exactly
this class). The owner's 09-04 line lists "ceiling raises by exactly the measured residual" under TAKEN; this raise is
exactly the residual, named file by file. Overruling the header pins is REFUSED: twice the estate ruled an older belief
declaration wins, and a bookkeeping ratchet must not be the thing that overrules it. **Consequence:** the equality arm
turns green (69 == 69) and the banked `clampPrimitiveBaseline` known-failure row RETIRES at the landing (census 6 → 5
on that row alone; the ratchet `--update` removes it).
**Not absorbed:** `cartographyMorphology.js:62` defines a passthrough `clamp01` under the name `unit`, invisible to
`DEF_RE` and load-bearing for the cartographyBuildings proof — it is NOT a copy to migrate and NOT a baseline row; it is a
detector blind spot, docketed (DOCKET item 6: the walker's DEF_RE vs a renamed clamp). `planUnitCm` has a validated floor
and NO ceiling (the `storageCapacityMonths` sibling) — docketed with it.
**Register act owed at the wave-2 landing:** `dispositionLedger.js` is one of the edge-shared bundle's 113 inputs; the
hash moves `d3d8ea6d14ba2375` → `28c68442afa0446b` — the chair re-mints with the ritual (materialise ONLY immer+seedrandom
→ `npm run build:edge-shared` → commit → RESTORE the symlinks), on the composed tip, once.
