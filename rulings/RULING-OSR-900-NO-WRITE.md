# RULING — the §900 OSR `--write` is NOT taken; the migration receipt's subject sha lies outside the landing lineage (chair, Fable 5.1, 2026-09-05)
FINDING: `node scripts/check-observed-shape-readers.mjs --write` REFUSED at 21fe864fa: `validateBaselineHistory` requires `migrationReview.subjectSha` (0742f8ff5, the schema-17 genesis SUBJECT commit in OSR-SCHEMA17's own dock, laneINTEG-tree) to be a committed ancestor of HEAD, then reconstructs the receipt (predecessor baseline text, scan/source/detector/execution tree digests) FROM THAT COMMIT'S TREE. The genesis car was cherry-picked into the desk composition as fea88e5b1 — a new sha over a different base — so no commit in this lineage carries the subject tree, and the receipt cannot reconstruct from any in-lineage sha. This is the register-provenance law's THIRD COSTUME at the OSR level (a register taken in another dock points outside the landing).
WHAT IS TRUE AT THE TIP: the vitest walker (`tests/lint/observedShapeReaders.walker.test.js`) was GREEN in the 102-car whole-suite proof (no OSR arm among the 22 reds); the register is exact at the tip — nothing to shrink; the OSR door reads 4 → 5 landed as a chair car. Only the CLI's history validation refuses.
RULED: no `--write` at §900 (nothing to write, and the door cannot open). The re-anchoring is RUNG 18's first act (lane OSR-SCHEMA18, already owed for the ninth exemption + `_doc` + the register rename): re-execute the governed migration with a SUBJECT commit inside the product lineage so the receipt reconstructs, and record the §900 refusal in `migrationReview` history. ⚠ Until then any CLI `--write` refuses; a chair must not "fix" the subjectSha by hand (the digests would not reconstruct and the receipt would become a lie).
