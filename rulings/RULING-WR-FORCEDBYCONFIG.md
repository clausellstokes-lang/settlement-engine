# RULING — writer-reach: `forcedByConfig on stress` and `forcedByConfig on stressors` (chair, Fable 5.1, 2026-09-05, the §900 register phase)
FINDING (measured at 7ef501df9, `wr-dry-900e.log`): two DARK, unregistered identities — the engine writes `forcedByConfig: true` on a stress entry when the config's `intendedStressTypes` names it (`src/generators/steps/resolveStress.js:66`, since c1ea091f7), and no counting surface reads it. Pre-existing at a2af55cde (DESK-TIMEBAND handed it up); the §900 `--write` REFUSES growth, so the register phase cannot proceed without a decision.
THE THREE DOORS: (1) LIGHT IT — a stress-desk sentence ("this pressure was chosen at the founding") is a NEW capability and chair words: not at a landing. (2) A REGISTER ROW — `dark-by-construction` needs a dormancy BIT (none: no flag gates the stamp); `engine-internal` needs a READER behind the STOP (none: grep finds no reader of `forcedByConfig` anywhere; its twin `source: 'forced'` is the field downstream checks read); `pending-surface` is the honest class but its population is a shrink-only ceiling that admits rows only through `--rebank`. (3) THE GOVERNED GROWTH DOOR `--rebank --charter=§900` — the walker's own message offers it; it is chartered for the MIN_ROWS flood class, and using it for TWO identities is a declared stretch of that charter, recorded in `rebankHistory` and in the §900 row.
RULED: door (3), with the pending surface named (DS-STR: the stressors desk may one day say a pressure was chosen) so the bank is a ceiling somebody is watching. The two STALE members (`hasMilitaryInst on inst`, `hasWatch on inst`, now LIT by the desks) fold out in the same act. Owner veto by name: "light forcedByConfig" (a words + capability car) or "strike the key" (a persisted-shape change: the stress entry is generated state — owner-gated, and it moves the OSR).
