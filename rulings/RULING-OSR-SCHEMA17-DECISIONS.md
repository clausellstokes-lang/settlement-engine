# RULING — OSR-SCHEMA17's three handed-up decisions (Fable 5.1 chair, 2026-09-05)
Lane OSR-SCHEMA17 landed the topology-only corpus pass, the 16 → 17 rung and the governed shrink re-freeze (3 cars over 940d161ca, tip a2af55cde, sealed `osr-schema17-2026-09-05`): 12 frozen rows + the unfrozen target cleared, 0 minted, every vanished row a named stress-gated writer. Its vitest is OWED (the chair's HOLD) and runs at the §900 composition.

1. **The walker's deliberate literal 64/43 → 62/41 and the banked roster 9 → 8 — ACCEPTED.** The withdrawn ruling refused a RAISE that would have sold silence; this is a LOWERING forced by rows the rung genuinely cleared. A pin that cannot fall when its population falls is a ratchet with no downward door, and the rung cannot both clear the rows and leave the pin. Declared stays 9 until (2).
2. **The ninth exemption now banks nothing and its `why` was falsified by the rung — RETIRE IT at §900 as a chair roster car** (a roster change is the chair's; the lane rightly kept the declaration and corrected the prose). The retirement moves declared 9 → 8 with the banked count; both in one car, with the walker's arm proving the roster == the tree.
3. **`_doc` in the register still says "SCHEMA 10 … eight-identity bank" — six rungs stale, pre-existing.** Cure it in the §900 OSR baseline `--write` (plan step 3e), the ONE further write that step already owes; never as a separate write.
Cost correction recorded: the corpus scan is +3 s (≈ +20 %) per whole scan, not the "≈ free / +1 %" the brief inherited; build-only timings on a four-lane machine are not trustworthy and were rightly not chased.
The desk landing's PRECONDITION (OSR-SCHEMA17 landed in laneINTEG-tree) is MET: `land-desk-prep.sh <post-§899 CAS>` may run once §899 lands.
