# RULING — PDFDRIFT is WITHDRAWN (2026-09-05, Fable 5.1); the residue is an OWNER question, and a docblock becomes machinery
The §896.2/§897 ruling "route the two campaign-less PDF export sites through `resolveExportSeam` (option (a), a declared
paid-surface repair)" rested on WARMS-CAR0's C5 finding that one settlement exports different artefacts by button. Lane
PDFDRIFT measured the premise FALSE: `SettlementCard.jsx:399` renders its export ONLY on plan-inactive (retention-frozen)
saves; every route into `SettlementDetail` is `isSaveActive`-gated (`SettlementsPanel.jsx:270/:294/:581`); the
anonymous `SingleDossierSuccessPage` holds no save id on any path. The two settlement sets are DISJOINT. Implementing (a)
would have handed a lapsed account the live `worldState` the AUDIT-2.2 docblock forbids and flipped `faithUnlocked` on
a $2.99 anonymous artefact — with the whole suite green (only one test observes `generateSettlementPDF`'s options).
**WITHDRAWN.** Option (b) is moot. The sixth instance of "charter the permission, measure the mechanism": the ruling's
class (a paid-surface repair is chair-class) was right; the mechanism it named was a skeptic pass's reading, unmeasured.
## What survives
1. **OWNER ROW (product scope):** does the paid-rights FLOOR (what a frozen, lapsed save may still export) include the
   W-ARMS arms? Reachable only across time (export while live → plan lapses → export frozen). Opened on the desk, not ruled.
2. **DOCKET item 5 (chair-class machinery):** turn the AUDIT-2.2 docblock (`SettlementCard.jsx:97-101`: the frozen
   card's export never receives the live store, worldState, or the faith chapter) into a test arm — this brief proved the
   exposure is live: a chair ruled exactly the forbidden change and nothing would have gone red.
