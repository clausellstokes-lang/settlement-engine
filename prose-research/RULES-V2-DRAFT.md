# RECONSTRUCTION RULES v2 — DRAFT (chair, Fable 5.1, 2026-09-05 evening)
Status: PART A is derived from the corpus's OWN laws and stands on its own; PART B (the exemplar-derived rules) WAITS for the exhaustive sweep (Tolkien/Martin/D&D by four angles + primary-text fingerprints) and PROBE-ALL (the full-corpus measurement by register). v1's R1–R12 are WITHDRAWN (the refuter's U1–U12 in `taste-sample-refutation.md` are the acceptance tests any Part B rule must pass). Nothing here changes any sentence.

## PART A — THE LAWS EVERY RECONSTRUCTION OBEYS (from RECEIPT_POOLS_DOSSIER_STATE.md §0b–§0f and the estate's standing laws; mechanised where a script can hold them)
A1 THE REGISTER (§0b): present tense, a condition that STANDS; **the state first, the cause second where a cause exists — never the cause first with the state as its payoff** (this is the law v1's R1 contradicted); one flowing sentence or two short ones — never a paragraph, a cascade or a list (`check-pair.mjs`: THREE+ SENTENCES); woven — droppable mid-section without a seam (no typed action verb lead, no headline turn, no desk or dateline gesture); the town's own voice about the town.
A2 THE ANGLE IS THE STANDPOINT, NOT A FLAVOUR (§0b): [ledger] the books; [street] the town's talk; [visitor] what a stranger notices first; [elder] the memory frame — a length to it; [unfolding] the movement still running; [counterforce] what did NOT happen, plainly; [threshold] the edge and what would tip it, **in the conditional or subjunctive** — a bare future indicative is a FATE breach anywhere (`check-pair.mjs`: FUTURE INDICATIVE ADDED / SUBJUNCTIVE REMOVED). Two variants differing only in slot fills are ONE for the repetition envelope.
A3 SLOTS ARE A CONTRACT (§0c): the slot SET of a variant is fixed (`check-pair.mjs`: SLOTS differ); each slot's fill SHAPE (`proper` / `phrase` / `bare-common`) decides its casing and article — a rewrite may not move a slot from mid-sentence to sentence-initial if its shape is `phrase` (lowercase) or `bare-common` (the seam supplies the article), and may not put a `proper` fill after an article.
A4 BANDS ONLY (§0d): no digits; durations only through the six-band table (attributive / span / adverbial / predicate forms; "older than its bearers" PREDICATE-ONLY); counts only in QUANTITY_BANDS words or an authored magnitude word (a handful, a score, most, all but); proportions in words never percent. A rewrite may not add a duration or count word the BEFORE did not carry, nor change the count NOUN (souls → households moved the band) (`check-pair.mjs`: DURATION/TIME words ADDED; COUNT words ADDED/LOST). T5: a band-blind variant asserts no duration.
A5 AUDIENCE IS INHERITED (§0e): a `dm-only` mark carries to the AFTER unchanged, and a rewrite of a covert seam may not leak a hint into the player projection (`check-pair.mjs`: MARKS).
A6 R-DST-B, THE CENTRAL LAW: a standing configuration field licenses a STRUCTURAL clause (what the town is arranged like) and never a HISTORICAL one (what happened); only an event-provenance field licenses history; a prosperity rung, a food label, a posture word and a market band carry NO provenance and get plain description only. A rewrite may not introduce a cause, an actor, a capacity fact, or a spatial fact the field does not carry ("the town is arranged around it", "can house them", "reopens it" all failed this). NOT mechanisable — the refuter's job, per item, against the block's STATE-KEY.
A7 THE POOL IS A SEED INPUT: variants are rewritten IN PLACE — none added, removed or reordered (the draw is `hash % length` and index 0 is canonical-at-zero); the pool KEY is part of the draw and never changes; a sentence that must say something new goes in a NEW pool key by a chair act, never appended.
A8 THE CONTRAST TEST (replaces v1's R4, which was two tests under one name): a contrast ("rather than", "not X but Y", "X, not Y") is KEPT when the rejected alternative names a SIBLING POOL KEY or a sibling band of the same block (the corpus's own discriminating fact — "departure rather than decline" beside `CANDIDATE KIND: decline`); it may be CUT only when no sibling key or band names it (`check-pair.mjs`: R4 CUT a word that names a SIBLING pool key — the executed half; the band half is the refuter's).
A9 THE GLOSS TEST, GUARDED (replaces v1's R3): a second sentence or a ", which is / which means" tail is cut only AFTER A8 has cleared it — a "That is / This is" sentence that carries the pool key's discriminating claim is not a gloss. A gnomic closer (a general truth the family states: "a decree binds only the hall that made it") is a device the corpus uses on purpose and gets its own rule in Part B; it is never created where the BEFORE had none.
A10 THE RATION IS A NET TEST AT THE EDIT (replaces v1's R11): none of {rather than · which is/means · nobody/no one/nothing · its own · whatever · enough to · kind of/sort of · quiet(ly) · still/yet/already} may be ADDED by a rewrite; the rubric counts the antithesis SHAPE, not the phrase (`check-pair.mjs`: RATIONED WORD ADDED; ANTITHESIS SHAPE ADDED; the NOTE when "rather than" becomes a bare "not").
A11 POOL SPREAD IS MEASURED AT THE EDIT (replaces v1's R5): a rewrite may not make two variants of one pool share their first two words, nor make every variant of a pool the same sentence count where they differed (the pool's other variants are inputs to the check, not an afterthought).
A12 A RATIONED ADVERB IS CHECKED AGAINST ITS SIBLING POOL (from U8): before cutting "quiet(ly)", ask whether a sibling pool (a `dm-only` COVERT twin, a `mobilization` pool) exists that the adverb distinguishes the variant from; if so it is the band's word and stays.
A13 VOICE PILLAR 8 CUTS BOTH WAYS: "a vivid image, a fragment, a turn of phrase all stay" — the corpus's deliberate devices (the tautological figure "the hall's decisions are the hall's", the echo across a pair of variants, the civic act named — "suing for peace") are not tics; the refuter's §3 list is the catalogue of what v1 over-cut.
A14 THE SAMPLE IS VERIFIED BEFORE THE OWNER SEES IT: every before/after pair passes `check-pair.mjs` mechanically AND an adversarial refuter on A6/A8/A9 claim-preservation; the receipts state the counts they measured, never the author's memory.
A15 THE HERALD AND EVERY OTHER REGISTER GET THEIR OWN PART A (the news address law, the two-sentence read-aloud law, the kind-pool floors, the seeded-pick laws) after PROBE-ALL names them; the dossier-state laws above are NOT assumed to transfer.

A16 THE ENGINE/DISPLAY LINE DECIDES THE PRICE OF A REWORDING (inventory §7.5): R1 dossier-state, R2 causal join, R4 the Herald causal grammar, R5 the crier voice, R6 the cause-conjunction ladder and R9 chrome copy are DISPLAY-SIDE side-cars generation never imports — byte-inert to every golden, a rewording is a declared text shift and nothing more. R3 (the Herald RECEIPT POOLS, `eventProse.js`, `roadsProse.js`, `traditionProse.js`) and R7/R8's generation inputs PERSIST into save and golden data — a rewording there moves same-seed picks and is a park-red, goldens-regen-once surface: OWNER-GATED at the fixture (the §898 precedent). The wave is sequenced display-side first; the engine-side registers wait for the owner's signature at the freeze.
A17 THE POOL KEY AND THE BLOCK ID ARE HASH INPUTS (inventory §7.3): renaming either re-rolls every draw in the pool; a reconstruction touches TEXT only, never a key, never an id, never the order (A7).

## PART A′ — PER-REGISTER NOTES FROM THE FULL-CORPUS PROBE (`PROBE_ALL.md`, 20 registers × 47 metrics; every figure re-derivable by its printed extractor). These name where each register's OWN failure lives; the rules for each are written against these numbers, not transferred from R1.
A′-R1 dossier-state (2,262): the register the dossier laws govern — antithesis 0.139 (of which "rather than" 0.113), gloss tails 0.066, semicolon 0.17, pronoun closers 0.133, "There/It is" openers 0.051, rationed pet words 0.536 per variant, 57.6% of pools uniform in sentence count, 11.2% with a repeated opener. The Part A rules apply as written.
A′-R2 causal join (467): the long register by law (one flowing sentence): 49% of sentences over thirty words, semicolon 0.36 AND colon 0.364, pet words 0.713 per variant (the highest), 23.1% of pools with a repeated two-word opener, 51% uniform. Its rule: the sentence stays one but the joint is chosen (a colon for the cause, a period where the semicolon was a staple); the opener spread is the first cure.
A′-R3 Herald receipt pools (1,212; ENGINE-SIDE, persisted, golden-bound — A16): the HEALTHIEST register: 12.2 words a sentence, 20% under eight, antithesis 0.012, gloss 0.034, pet words 0.176; 84% of pools uniform in segment count, but its unit is the headline fragment (terminal-stop share 0.847). Its rule is the read-aloud law (two sentences, who/what/where first) and it already keeps it; rewordings here are OWNER-GATED text shifts — measure, do not touch before the freeze.
A′-R5 crier voice (373; display-side): a FORMULA register — "The matter of X … and the country/the halls of rule/the careful …" (the top ten tics are all frames of one mould; "and the country" in 7% of lines), semicolon 0.198, 22.4% of pools share an opener, 67% uniform. Its rule: the crier is a quoted in-world VOICE (R-DST-I) and may keep a formula, but a pool's variants must not share the frame — vary the frame across the pool, not the vocabulary inside it.
A′-R6 NPC cause-conjunction ladder (1,659; read aloud at the table; ungated by any pool walker): the estate's STRONGEST tic — "It is public / It is known / It is public that" opens 184 lines (18× the median), NO short sentence (p10 = 19 words, under-eight share 0.005 against a 0.381 median), semicolon 0.54 (every second line), 95.5% of pools uniform. Its rule is the opposite of R1's: this register needs SHORT sentences and OPENER spread before anything else, and the two-sentence read-aloud law applies because it is spoken.
A′-R7 institution gazetteer (2,169): short units (8.8 words), 47% under eight, 6.9% three-plus segments (the entry shape), abstract closers 0.086 (the highest with R10/R11), digits-in-prose 0.03 (the bible's numeral rule is contextual here — a chair ruling, not a rewrite). Rule: the gazetteer entry closes on the thing, not the quality.
A′-R8/R9/R16 (world-data, chrome copy, JSX): nearest the bible's own exemplar set by the style axis; the hard rules hold at zero breach everywhere; ⛔ ONE violation: `historyData.js:1326` "the PCs" (a generation input — a text shift in persisted history; a chair car declared at the golden).
A′-UNWIRED annex (4,626 rows in docs only): carries R1's gloss tic at the same rate (0.062) — wiring imports the debt; the reconstruction reads the annex, not the projection, so the unwired rows are rewritten WITH their wired siblings (they are one corpus in the doc).

A′-FINGERPRINTS OF THE HERALD (chair, 17:50; `herald-pools/herald-crier/npc-ladder.fingerprint.json`, same tool as the exemplars): the receipt pools (R3) sit INSIDE the exemplar band on length, tics and closers but below it on rhythm (neighbour variation 0.33 vs 0.50–0.73; sd 5.0 vs 8.8–16); the crier (R5) has the estate's worst opener repetition (same opener as the previous line 0.42 vs 0.03–0.10) and the flattest rhythm after the ladder (0.18); the NPC ladder (R6) has variation 0.17, semicolon 0.53 (four times the exemplar ceiling), "It is" openers 0.11 and no short sentences. So the Herald's cure is RHYTHM and OPENER SPREAD, not vocabulary or tics — the opposite of R1's.

## PART B0 — MEASURED, NOT CRITIC-DERIVED: what the primary-text fingerprints license today (`receipt-primary-samples.md`; 70,223 words; estate control `estate-state.fingerprint.json`). Each is a direction with its number; the SIZE of the move is Part B's to set after the sweep.
B0.1 THE "RATHER THAN" TIC GOES FIRST: 5.33 per 1,000 words against 0.00–0.32 in every exemplar; 259 of our 323 antithesis hits are this one phrase. Under A8 the contrast stays where a sibling key or band names the alternative; everywhere else the phrase is rewritten as two facts or one — and NOT as "X, not Y" (A10 counts the shape).
B0.2 A QUALIFICATION GETS ITS OWN SENTENCE: Martin's 12,422-word chronicle has ZERO which-tails against our 0.051; his long sentences accumulate, ours hedge. The A9 gloss test keeps the claim and moves it out of the subordinate clause. (The lane's em-dash alternative is REFUSED: V-26a, zero em dashes, is the owner's law — the period is the estate's instrument.)
B0.3 LAND ON A NOUN: 253 of our sentences end on "it" (pronoun closers 0.134 vs 0.021–0.108); Martin's chronicle lands on nouns and names (0.054). Where the referent is a civic thing, name it at the close (v1's R6, now with its number; the refuter showed it was cited eight times and delivered once — so it is checked, not claimed).
B0.4 RHYTHM IS THE WIDEST GAP: neighbour variation 0.399 vs 0.50–0.73, sd 7.2 vs 8.8–16.0, long sentences 2% vs 9–34%, same-opener-as-previous 0.132 vs 0.03–0.10. The cure is NOT longer sentences by default (A1's two-sentence law stands); it is that consecutive variants and consecutive sentences differ — a short fact beside a long chain — and that a pool's variants do not open alike (A11).
B0.5 THE TRIAD IS A DEVICE, NOT A TELL, IN THIS COMPANY: 0.009 vs 0.045–0.213; Martin's chronicle argues by lists of three concrete nouns. Part B decides where a triad is the exact form (a chain of three links, a visitor's three sights) — never as scaffolding (the AI-failure list's "lists of three" is about REFLEXIVE triads; Gorrie's "taste").
B0.6 THE VOCABULARY IS THE NARROWEST IN THE TABLE (TTR 0.211 vs 0.24–0.35 at 4k tokens): the reconstruction may vary the civic noun and the verb across a pool's variants (A2's standpoints already ask for it) — within the finite-semantics law, never a new claim.
B0.7 THE D&D RULES REGISTER IS THE COLUMN WE MOST RESEMBLE, and it is the most metronomic (runs-of-three 0.339 vs our 0.320) with the most abstract closers (0.088): scannable by design, and paid for exactly the way we pay. dnd-flavor shows the evocative move inside ONE house voice is four dials (triads 0.045→0.144, doubled adjectives 0.013→0.041, adverbs 0.140→0.228, pronoun closers 0.050→0.021) — the estate's dossier prose should sit between the two D&D columns, nearer flavor than rules, while the news line stays nearer rules (the read-aloud handle).
B0.8 WHAT WE DO THAT THEY DO NOT, kept on purpose: zero em dashes, zero questions, zero exclamations, zero digits (the voice bible's laws) — the exemplars' punctuation freedoms are NOT imported.

## PART B — DEFERRED: the exemplar rules (Tolkien register-by-standpoint and earned elevation; Martin's concrete sensory noun and functional default; the read-aloud two-sentence handle; Le Guin's exact plainness; the AI-failure avoidance list) — written only from the verified sweep and the fingerprint tables, each rule carrying its source count and the corpus register it applies to, and each tested against U1–U12 before it enters.
