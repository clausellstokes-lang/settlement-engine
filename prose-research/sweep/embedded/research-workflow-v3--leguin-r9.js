export const meta = {
  name: 'prose-exemplar-sweep-v3',
  description: 'Prose exemplar sweep v3 (Opus finds and verifies; Fable synthesises and criticises), file-backed: resume verification of a sweep from its state chunks (Opus verifies), or run fresh finders for a new exemplar/probe; then synthesize and criticise (seat)',
  phases: [
    { title: 'Find', detail: 'fresh runs only: four angles per exemplar, or one finder per technique probe' },
    { title: 'Verify', detail: 'every unverified claim refuted against its source, 15 per Opus agent; each agent writes its verdict file' },
    { title: 'Synthesize', detail: 'the section from kept-<name>.json, then the completeness critic' },
  ],
}
// v2 (chair, 2026-09-05 evening, after the reboot). SAME research design as research-workflow.js (finder prompts, angles, exemplars,
// probes, verdict vocabulary, seat law: every VERIFY chunk is `model: 'opus'`, SYNTH and CRITIC inherit the seat). What changed is the
// DATA PATH: a successor session cannot resume a run id, and the v1 successor path asked one agent to return a 400 KB state file
// VERBATIM as structured output, which exceeds an agent's output ceiling. So: the chair pre-splits the unverified claims into chunk
// FILES (args.chunks = [{file, indices}]), each verifier READS its file and WRITES `verdicts-<name>-chunk-<k>.json` (claims + verdicts,
// the checkpoint), and synthesis reads `kept-<name>.json` produced by `sweep-state.mjs merge` — nothing large ever crosses an agent's
// structured return. Owner's cap: FOUR running agents of any kind; args.cap bounds this run's concurrency.
// ⛔ ARGS FROM A FILE — DISABLED 2026-09-06 00:20 after one launch: the low-effort "return this 13 KB JSON verbatim" agent returned an object
// with 3,605 hashes, 4,348 extra angles and 221 chunks where the file held 330, 7 and 1 — a hallucinated expansion that would have run
// thousands of finders. A verbatim structured return of a file is NOT a read; paste the args inline (`cat sweep/args-<name>.json`),
// which is what the pickup card now says. Left here as the record; a checksum-verified loader may replace it later.
// v3.4 (09-06 14:35): the verify RULE hard-caps trueWording at twelve words (copyright); the merger flags longer rows fullSentence.
// v3.1 (08:20): A.angleHints = {angleKey: text} appends a PRIOR PROGRESS line to that angle's finder prompt (built by mk-round.py --inflight from sweep/inflight/).
// v3 (chair, 2026-09-06 08:10, owner: "architect how opus should research … then switch all find phases for each research to Opus"): every FINDER is
// `model: 'opus', effort: 'high'` and works under OPUS-FINDER-METHOD.md (read by path; essentials inline); the FINDINGS schema gains optional
// per-claim fields (kind, polarity, date, routeHint, registerHint, confidence, modelEra, page) and a top-level `complete` flag the finder writes
// false every five sources and true at the end. Verify, regrade, synth and critic are byte-identical to v2.
// EMBEDDED ARGS from args-leguin-r9.json (mk-embedded.py; existingHashes 0)
let A = {"name":"leguin","cap":4,"verifiedCount":681,"stateFile":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/state-leguin.json","outDir":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep","kitDir":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research","chunks":[{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-00.json","indices":[388,391,393,395,398,400,401,402,403,404,405,407,408,417,419]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-01.json","indices":[420,421,422,423,424,425,426,427,428,429,430,433,435,437,438]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-02.json","indices":[439,440,441,442,445,446,448,449,450,455,456,457,458,459,461]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-03.json","indices":[462,463,464,465,467,473,474,480,481,482,483,484,486,488,489]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-04.json","indices":[490,496,497,499,500,502,504,505,506,507,508,509,510,511,512]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-05.json","indices":[514,515,525,535,541,543,546,555,556,558,568,569,570,571,572]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-06.json","indices":[588,590,591,592,593,594,595,596,597,617,631,632,634,635,636]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-07.json","indices":[637,638,642,643,644,645,647,648,649,652,653,655,656,658,659]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-08.json","indices":[660,661,662,668,670,671,672,673,674,676,677,678,679,683,684]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-09.json","indices":[686,687,691,696,697,712,713,714,715,716,717,718,719,720,721]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-10.json","indices":[722,723,724,726,727,729,730,731,732,733,734,739,740,741,743]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-11.json","indices":[744,745,746,747,748,749,753,754,755,757,758,760,761,762,764]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-12.json","indices":[766,768,769,770,771,772,773,775,776,777,779,780,782,785,786]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-13.json","indices":[787,789,791,795,834,835,836,837,838,845,846,847,850,854,857]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-14.json","indices":[858,860,871,873,876,877,878,879,880,881,882,883,884,886,887]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-15.json","indices":[888,897,900,901,902,903,904,905,906,907,909,910,911,912,915]},{"file":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep/chunks/leguin-16.json","indices":[917,918,921]}],"regrade":true,"regradeNotes":"R9 REGRADE (the verify-only completion; the r8 critic \u00a78.2 and \u00a72): [32] \u2192 PARTIAL 'relay' unless [433] (Kirkus) lands verified this round; fill the nine empty trueWording fields from their verifier notes where the note carries the confirmed wording; flag [140] as the critic describes; any VERIFIED_VERBATIM whose quotation was found only in a PDF or full-text render rather than at the cited URL is graded to the standard applied to the sweep's [13]-style rows (VERIFIED_SUBSTANCE with the location in the note) unless re-pointed; the 243 rows verified this round were SKIPPED_TRIAGE sole witnesses or second sources (untriage-leguin.json names each row's feature and the critic's group a\u2013d): grade each on its own page like any row.","synthNotes":"R9 SYNTHESIS \u2014 THE VERIFY-ONLY COMPLETION (owner delegation 2026-09-07; the 29-feature form kept; no new Part). Fold every row verified this round into the feature untriage-leguin.json ties it to \u2014 the record-register set (features 1\u20135), the relay cures, the dissent and period-scoping rows, the second sources for the single-source limbs \u2014 and re-mark every feature that gained a second or third independent voice (the SINGLE SOURCE flags fall only when the new witness is a distinct source, not a distinct claim). Then the r8 critic's \u00a78.3 list: strike 'short' from feature 6's rule; resolve feature 26's rhyme ban against [213]; label feature 1's calendar limb and feature 19's detached author as doctrine or contract, not measured practice; scope feature 11 to the early register and admit the late-style dissent; meet Cummins [635] in feature 5; correct the Coverage paragraph's 'still not reached' list; describe the verbless record syntax [712, 776, 777, 786] wherever features 1 and 21 meet; write the Elfland p. 5 'sentences beginning with And' sentence and the Craig 'taut and clean' sentence as verified rows if this round verified them. Register map: which rules bind the settlement dossier's archivist, which the Herald's pools, which the chronicle line. ADD to the register map the OWNER RULING of 2026-09-07 as it bears on Le Guin's economy rules (features 8, 9, 20): 'only the relevant belongs' governs HOW an entry is written, never WHICH elements of the dossier exist \u2014 every element gets its full form because any player may explore any of it; leaving out is a sentence-level art, not a slot-level one. No quotation over twelve words; PARTIAL rows cited only for their supported limb.","regradeTag":"r9"}
if (A.argsFile) throw new Error('argsFile is DISABLED (it hallucinated on 2026-09-06): paste the JSON of ' + A.argsFile + ' inline as args')
const CAP = (A && A.cap) || 1
const NAME = A && A.name
if (!NAME) throw new Error('A.name is required (tolkien | martin | dnd | ai | kay | leguin | wolfe | hobb)')
const OUT = (A && A.outDir) || '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research/sweep'
const KIT = (A && A.kitDir) || '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/prose-research'
const STATE_FILE = (A && A.stateFile) || null
const METHOD = `${KIT}/OPUS-FINDER-METHOD.md`
async function batched(items, fn) {
  const out = []
  for (let i = 0; i < items.length; i += CAP) {
    const group = items.slice(i, i + CAP)
    const r = await parallel(group.map((it, j) => () => fn(it, i + j)))
    out.push(...r)
  }
  return out
}
const FINDINGS = { type: 'object', required: ['claims', 'sourcesRead'], properties: {
  complete: { type: 'boolean' }, coverage: { type: 'string' },
  sourcesRead: { type: 'array', items: { type: 'object', required: ['title', 'url', 'kind', 'substantive'], properties: { title: { type: 'string' }, url: { type: 'string' }, kind: { type: 'string' }, substantive: { type: 'boolean' }, date: { type: 'string' }, route: { type: 'string' } } } },
  claims: { type: 'array', items: { type: 'object', required: ['feature', 'claim', 'source', 'url', 'quote'], properties: { feature: { type: 'string' }, claim: { type: 'string' }, source: { type: 'string' }, url: { type: 'string' }, quote: { type: 'string', description: 'under twelve words, verbatim and contiguous from the page as fetched, or empty' }, page: { type: 'string' }, kind: { type: 'string' }, polarity: { type: 'string' }, date: { type: 'string' }, routeHint: { type: 'string' }, registerHint: { type: 'string' }, confidence: { type: 'string' }, modelEra: { type: 'string' } } } } } }
const EXEMPLARS = [
  { key: 'tolkien', name: 'J. R. R. Tolkien', notes: 'prose style of The Lord of the Rings and The Hobbit: register modulation, archaism, per-speaker register, cadence, deep time, the annalist voice; critics Shippey, Rosebury, Walker, Turner, Flieger, Le Guin, Lewis, Auden; corpus/stylometric studies' },
  { key: 'martin', name: 'George R. R. Martin', notes: 'A Song of Ice and Fire prose: tight third person, sensory concrete nouns, Anglo-Saxon diction, compounds, chapter openings, sentence rhythm, repetition, dialogue; both praise and criticism; interviews in his own words' },
  { key: 'kay', name: 'Guy Gavriel Kay', notes: 'historical-fantasy prose (Tigana, The Lions of Al-Rassan, Under Heaven, the Sarantine Mosaic): restrained lyrical historical atmosphere; history as residue on ordinary people; competing memories of one event; melancholy and consequence without ornament; his own essays and interviews on writing history-shaped fantasy; critics on his register' },
  { key: 'leguin', name: 'Ursula K. Le Guin', notes: 'the prose of Earthsea and the essays on style (From Elfland to Poughkeepsie; Steering the Craft; The Language of the Night): economy, rhythm, ordinary vocabulary carrying worldbuilding weight; distance without costume; her own stated rules on sentence sound, adjectives, and the plain noble style; critics and stylometric studies' },
  // OWNER 09-05 evening: Wolfe and Hobb get the FULL four-angle sweep, the same as Martin and Tolkien — the one-device probes are superseded; the device stays in the brief so every angle looks for it as well as for the register at large.
  { key: 'wolfe', name: 'Gene Wolfe', notes: 'the prose of The Book of the New Sun, The Fifth Head of Cerberus, Peace and the Soldier books: plain-seeming sentences carrying withheld information; unreliable and limited narrators; incidental detail as evidence; omission as information; THE DEVICE this program wants above all — prose that lets the reader infer the gap between public belief, institutional record and physical evidence without the narrator announcing it; Wolfe\'s own essays, interviews and letters on craft; critics (Clute, Wright, Aramini, Le Guin, Gaiman, Borski) and close readers' },
  { key: 'hobb', name: 'Robin Hobb', notes: 'the prose of the Farseer, Liveship and Tawny Man books: first-person interiority in ordinary vocabulary; slow consequence; household economy; THE DEVICE this program wants above all — converting a state change into a lived consequence for a person or household (who lost something, what they kept, what it meant); Hobb\'s own essays, interviews and blog posts on writing consequence and character; critics and close readers on her register and pacing' },
  { key: 'dnd', name: 'D&D official content', notes: 'Wizards of the Coast adventure and sourcebook prose: read-aloud/boxed text form, DM-facing text, terminology consistency, the 2014 vs 2024 house style, Adventurers League and DMs Guild guidance, designers (Perkins, Crawford, Decker, Noonan) on writing, community analyses; no published style guide — say so where sources agree' },
]
const ANGLES = [
  { key: 'academic', prompt: 'academic and scholarly criticism (journals, monographs, Wikipedia syntheses with their cited critics, conference papers, stylometric or corpus studies)' },
  { key: 'craft', prompt: 'craft essays and writing-advice pieces by novelists, editors and game designers that analyse HOW the prose works (sentence shape, diction, rhythm, register)' },
  { key: 'voice', prompt: 'the author\'s or designers\' OWN words: interviews, forewords, letters, blog posts, talks, panels' },
  { key: 'close', prompt: 'close readings and line-level analyses on forums, blogs, video essays (transcripts), reading groups — the kind that quote a sentence and explain it' },
]
const AI_ANGLES = [
  { key: 'studies', prompt: 'peer-reviewed and arXiv studies of AI-generated NARRATIVE text: stylometry, burstiness, discourse features, expert vs non-expert judgments, homogenisation, lexical overuse (Kobak, Juzek & Ward, StoryScope, Chakrabarty, Ismayilzada, Doshi & Hauser, O\'Sullivan, Jakesch and any you find beyond these)' },
  { key: 'craft', prompt: 'novelists, fiction editors and developmental editors on what AI prose gets wrong in FICTION: subtext, specificity, rhythm, tidy endings, reflexive antithesis, the gloss, pet words' },
  { key: 'industry', prompt: 'publishing and games industry: magazine editors on AI slush, DMs Guild / RPG publishers on AI submissions, style-guide bans, what readers report noticing' },
  { key: 'counter', prompt: 'the case AGAINST the common tells: sources arguing em dashes, triads and antithesis are legitimate devices, that detection heuristics fail, that human prose shares the features — the disconfirming evidence' },
]
const CHUNK = (A && A.chunk) || 15
const RESUME = !!(A && A.chunks)            // state chunks (pre-split, file-backed) to verify
// TOP-UP MODE (owner 09-05 evening, "the full workflow for every author"): a sweep whose state carries claims from fewer than
// four angles (dnd: 1 of 4 before the cutoff) re-runs the named angles' FINDERS and appends only the NEW claims — deduped
// against A.existingKeys (lowercase source|feature|claim[:60] of every claim already in the state) — at indices from
// A.baseIndex (= the state's claim count), so the state file grows by index and nothing already verified is touched.
const FIND_ANGLES = (A && A.findAngles) || (RESUME ? [] : ANGLES.map(a => a.key))
// ROUND-N TOP-UP: a completeness critic's "recommended next round" becomes A.extraAngles = [{key, prompt}] — custom finder
// prompts run beside (or instead of) the four standard angles, their claims deduped against the state and appended from baseIndex.
const EXTRA = (A && A.extraAngles) || []
const BASE_INDEX = (A && A.baseIndex) || 0
const keyOf = (c) => (c.source + '|' + c.feature + '|' + String(c.claim || '').slice(0, 60)).toLowerCase()
// existing claims travel as SHORT HASHES of their content key (djb2, hex) so a 330-claim state costs ~4 KB of A, not 50 KB;
// the same hash is computed by the chair's Python when the A are built (sweep/mk-A.py). Full keys are still accepted.
const djb2 = (str) => { let h = 5381; for (let i = 0; i < str.length; i++) h = ((h * 33) ^ str.charCodeAt(i)) >>> 0; return h.toString(16) }
const EXISTING = new Set([...((A && A.existingKeys) || []).map(k => djb2(String(k).toLowerCase())), ...((A && A.existingHashes) || [])])
const seenKey = (c) => djb2(keyOf(c))
// ---------------------------------------------------------------- Find (fresh runs and top-ups)
phase('Find')
let freshClaims = []
if (FIND_ANGLES.length || EXTRA.length) {
  const ex = EXEMPLARS.find(e => e.key === NAME)
  let found = []
  const finderPrompt = (subject, an, key) => `You are an OPUS research FINDER for the prose-reconstruction program (the seat law: Opus finds and verifies, Fable synthesises and criticises). FIRST read ${METHOD} in full — the finder method binds you; its essentials are repeated here. Subject: ${subject}. Angle: ${an.prompt}.
DISCOVERY: fetch every source the angle NAMES first (each was chosen because a critic found it absent), logging each as fetched / not found / blocked; then expand by bibliography chasing, "cited by" routes (Semantic Scholar, OpenAlex, Crossref) and lateral searches (the subject beside feature terms, quoted phrases, site: operators); stop only when the named roster is exhausted AND two consecutive searches surface nothing new; aim for ten to twenty substantive sources and write one sentence explaining any shortfall.
FETCHING: READ every source RAW (WebFetch the page; never a search snippet, never a summariser, never an aggregator quote page). Recovery ladder when a fetch fails: a browser user agent → the Wayback raw capture (https://web.archive.org/web/<ts>id_/<url>; find <ts> at https://archive.org/wayback/available?url=<url>; gunzip if gzip-encoded) → archive.ph → for a PDF fetch the binary and extract its text → for a DOI the OpenAlex / Crossref / Semantic Scholar record (mark abstract-only) → scribe.rip for Medium → Google Books search-inside for a book (mark snippet-only). Put the route that worked in the claim's routeHint. Wikipedia is a RELAY: fetch the primary it cites; if unreachable, source = "Wikipedia relaying <primary>" with kind "relay". Date every source and name the edition or version it describes.
CLAIM-WRITING (an adversarial Opus verifier grades EVERY LIMB; an overreaching claim yields nothing): ONE LIMB PER CLAIM — one assertion whose every noun is on the page; a two-part finding is two claims. ATTRIBUTION names the actual speaker — the author's own words, a critic's analysis, a reporter's paraphrase, a parody or demonstration passage, a reader's comment and a vendor's line are six different sources; a paraphrase is never transcribed speech. POLARITY: say whether the source asserts, applies, rejects, disputes or merely mentions the feature; never cite an application of a rule for its opposite. QUOTE: at most twelve words, verbatim and contiguous from the page AS FETCHED, or an empty string, with its section or page in "page"; never let the sentence continue past the quotation marks with the source's own next words unquoted. FEATURE from the method's controlled vocabulary (or "other: <label>"). kind ∈ analysis | own-words | measurement | reception | reader | vendor | relay. registerHint ∈ dossier-archivist | herald-pools | chronicle-line | dm-page | none (a hint only). confidence high | medium | low with the reason when not high. ${key === 'ai' ? 'modelEra is MANDATORY on every claim: the model and the month and year of the observation, and the register measured (fiction, verse, news, essays, archival). The gap worth most is any source that measured the archival or encyclopedic register, and any evidence on mechanism and steerability rather than the tell itself.' : 'The claims worth most are what the source says about describing a PLACE and its institutions, and about the register a record, chronicle, annal or gazetteer uses.'} Report a source's own figures and units; never round or re-express them. Never reproduce more than twelve words of a copyrighted passage.
PRE-VERIFY before you write: search each quotation string in the text you fetched; blank any quotation that does not match exactly; drop any claim you cannot point to one sentence for.
CHECKPOINT: write raw notes to ${OUT}/find-${key}-${an.key}.md as you go, and REWRITE ${OUT}/found-${key}-${an.key}.json (JSON: {"complete": false, "coverage": "...", "sourcesRead": [...], "claims": [...]}) after EVERY FIVE SOURCES with everything so far; write it a last time with "complete": true BEFORE you return — the file is the record that survives a session cutoff, the return is a courtesy. Then return the same structured result with complete true and the coverage sentence (roster fetched / not found / blocked; substantive sources per route).${(A.angleHints && A.angleHints[an.key]) ? ('\nPRIOR PROGRESS (a predecessor finder on this angle was cut off by a session limit; its checkpoint claims are already in the state and deduplicated by hash, so do not re-extract them): ' + A.angleHints[an.key]) : ''}`
  if (ex) found = await batched([...ANGLES.filter(a => FIND_ANGLES.includes(a.key)), ...EXTRA], (an) => agent(
    finderPrompt(`the prose STYLE of ${ex.name} (${ex.notes})`, an, ex.key),
    { label: `find:${ex.key}:${an.key}`, phase: 'Find', schema: FINDINGS, model: 'opus', effort: 'high' }))
  else if (NAME === 'ai') found = await batched([...AI_ANGLES.filter(a => FIND_ANGLES.includes(a.key)), ...EXTRA], (an) => agent(
    finderPrompt('where generated (LLM) prose FAILS against skilled human fiction and game writing', an, 'ai'),
    { label: `find:ai:${an.key}`, phase: 'Find', schema: FINDINGS, model: 'opus', effort: 'high' }))
  else throw new Error(`unknown sweep name ${NAME}`)
  const all = found.filter(Boolean).flatMap(r => r.claims || [])
  const seen = new Set(EXISTING)
  for (const c of all) { const k = seenKey(c); if (!seen.has(k)) { seen.add(k); freshClaims.push(c) } }
  log(`found ${all.length} claims across ${FIND_ANGLES.length + EXTRA.length} angle(s), ${freshClaims.length} NEW after dedupe (${EXISTING.size} already in the state), from ${found.filter(Boolean).reduce((n, r) => n + (r.sourcesRead || []).length, 0)} sources read`)
}
// ---------------------------------------------------------------- Verify
phase('Verify')
const VERDICTS = { type: 'object', required: ['verdicts', 'wroteFile'], properties: { wroteFile: { type: 'boolean' }, verdicts: { type: 'array', items: { type: 'object', required: ['index', 'verdict', 'trueWording', 'note'], properties: { index: { type: 'integer' }, verdict: { type: 'string', enum: ['VERIFIED_VERBATIM', 'VERIFIED_SUBSTANCE', 'PARTIAL', 'NOT_FOUND', 'CONTRADICTED', 'BLOCKED'] }, trueWording: { type: 'string' }, note: { type: 'string' }, unsupportedLimb: { type: 'string' } } } } } }
// v3.2 (12:55): a chunk may be given COMPACT as {f: 'chunks/<name>-NN.json', i: [first, last]} (relative to outDir, an inclusive index range) —
// the legacy {file, indices} form still works. Halves the args a chair must paste inline after a cutoff.
const expandChunk = (c) => c.f ? { file: OUT + '/' + c.f, indices: Array.from({ length: c.i[1] - c.i[0] + 1 }, (_, j) => c.i[0] + j), inline: null } : { file: c.file, indices: c.indices, inline: null }
const chunks = RESUME ? A.chunks.map(expandChunk) : []
for (let i = 0; i < freshClaims.length; i += CHUNK) { const part = freshClaims.slice(i, i + CHUNK); chunks.push({ file: null, indices: part.map((_, j) => BASE_INDEX + i + j), inline: part.map((c, j) => ({ index: BASE_INDEX + i + j, ...c })) }) }
chunks.forEach((ch, k) => { ch.k = k; ch.tag = `i${ch.indices[0]}-${ch.indices[ch.indices.length - 1]}` })   // verdict files are named by INDEX RANGE, so a top-up never overwrites an earlier run's file
log(`verification: ${chunks.reduce((n, c) => n + c.indices.length, 0)} claims to verify in ${chunks.length} chunks of up to ${CHUNK} (${RESUME ? 'state chunks; ' + (A.verifiedCount || 0) + ' already verified; ' : ''}${freshClaims.length} fresh)`)
const RULE = 'For each claim report its index and: VERIFIED_VERBATIM only if the quotation appears on the page word for word AND every limb of the claim sentence is supported by the page; VERIFIED_SUBSTANCE if the page supports the whole claim in other words (give the true wording, under twelve words); PARTIAL if the quotation is on the page but some limb of the claim is NOT (name that limb in unsupportedLimb — a PARTIAL is a visible grade, never a caveat buried in a note); NOT_FOUND if the page does not support it; CONTRADICTED if it says otherwise; BLOCKED only if the page cannot be fetched by any route — before you write BLOCKED you MUST have tried all three recovery routes: (1) a browser user agent on the live URL, (2) the Wayback Machine raw capture (`https://web.archive.org/web/<timestamp>id_/<url>`, gunzip it if it arrives gzip-encoded; find a timestamp via `https://archive.org/wayback/available?url=<url>`), (3) for a PDF, fetch the binary and extract its text — plus every routeHint the claim carries (OpenAlex/Crossref abstract routes for paywalled DOIs, scribe.rip for Medium, archive.ph, Internet Archive full text); a verdict formed through a summarizer or a search snippet rather than the raw page is PARTIAL with unsupportedLimb "raw page not reached", never VERIFIED. Default to NOT_FOUND when uncertain. Return one verdict per index, every index. COPYRIGHT HARD CAP (chair, 2026-09-06 14:35, after a critic measured 162 of 342 kept rows over the limit): trueWording is AT MOST TWELVE WORDS on every verdict, VERIFIED_VERBATIM included — quote at most twelve contiguous words and name the section, heading or paragraph for the rest; a longer trueWording is a breach the merger flags as fullSentence and no downstream document may copy.'
const results = await batched(chunks, (ch) => agent(
  (ch.file
    ? `Read the JSON file ${ch.file} — it holds ${ch.indices.length} attributed claims (fields: index, feature, claim, source, url, quote; some carry routeHints — alternative routes to the same source that MUST be tried before BLOCKED, and a claim marked reverify=true was NOT_FOUND or BLOCKED once already: find it by the hinted route or say precisely why it cannot be found). `
    : `Here are ${ch.indices.length} attributed claims as JSON: ${JSON.stringify(ch.inline)}\n\n`)
  + `Adversarially verify EACH claim by fetching its source as RAW text (WebFetch the url; group the claims by url and fetch each url ONCE; if a host blocks, try the Wayback Machine once, else BLOCKED). ${RULE}\n\nBEFORE you return, WRITE the checkpoint file ${OUT}/verdicts-${NAME}-${ch.tag}.json as JSON of the form {"name":"${NAME}","chunk":${ch.k},"claims":[the ${ch.indices.length} claims exactly as given, each with its index],"verdicts":[your ${ch.indices.length} verdicts: {index, verdict, trueWording, note}]} — the file is the record that survives a session death; then return the same verdicts as your structured output with wroteFile true (false only if the write failed).`,
  { label: `verify:${NAME}:${ch.tag}`, phase: 'Verify', schema: VERDICTS, model: 'opus' }).then(v => ({ ch, v })))
const verdictMap = {}
let unwritten = []
const returned = results.filter(r => r && r.v && Array.isArray(r.v.verdicts))   // a verifier that DIED (session limit, API error) yields {ch, v: null}: it is a MISSING chunk, never a crash
for (const r of returned) { for (const v of (r.v.verdicts || [])) verdictMap[v.index] = v; for (const i of r.ch.indices) if (!verdictMap[i]) verdictMap[i] = { index: i, verdict: 'NOT_FOUND', trueWording: '', note: 'no verdict returned for this index' }; if (!r.v.wroteFile) unwritten.push(r) }
const missing = chunks.filter(ch => !returned.some(r => r.ch.k === ch.k))
if (missing.length) log(`⚠ ${missing.length} verifier chunk(s) returned nothing (skipped or died): ${missing.map(c => c.k).join(', ')} — their indices stay unverified in the state; re-run to cover them`)
if (unwritten.length) {
  log(`${unwritten.length} verifier(s) reported wroteFile=false — writing their checkpoint files from the returned verdicts`)
  await batched(unwritten, (r) => agent(`Write the file ${OUT}/verdicts-${NAME}-${r.ch.tag}.json with EXACTLY this JSON content (create the directory if needed) and return the byte count you wrote:\n${JSON.stringify({ name: NAME, chunk: r.ch.k, claims: r.ch.inline || null, claimsFile: r.ch.file, verdicts: r.v.verdicts })}`, { label: `checkpoint:${NAME}:${r.ch.tag}`, phase: 'Verify', effort: 'low' }))
}
const kept = Object.values(verdictMap).filter(v => v.verdict === 'VERIFIED_VERBATIM' || v.verdict === 'VERIFIED_SUBSTANCE').length
log(`verified this run: ${kept} of ${Object.keys(verdictMap).length} survive (${returned.length} of ${chunks.length} verifier chunks returned)`)
// REGRADE (a critic's finding, 2026-09-05: "the VERIFIED label attaches to the quote, not the claim"): re-read every kept verdict's own
// note; where the verifier itself wrote that a limb is not supported / not on the page / appears nowhere / FLAG, the grade becomes
// PARTIAL with the limb named. Written as a regrade verdict file the merger applies LAST. Runs when A.regrade is true.
if (A && A.regrade) {
  await agent(`First run, in a shell: \`cd ${KIT} && node sweep-state.mjs merge ${NAME} --update-state\` then read ${OUT}/kept-${NAME}.json (in pieces). For EVERY row, read the verifier's note and trueWording. If the verifier's own words say some limb of the claim is not supported — "NOT SUPPORTED", "not on the page", "appears nowhere", "FLAG", "does not link", "the page does not say", "overstates", "the X half is not", or any equivalent — the row's grade is PARTIAL and you name the unsupported limb in one clause. Be strict and literal: a VERIFIED grade may stand only where the note supports every limb.${A.regradeNotes ? ' ALSO apply these findings from the completeness critic, each as a regrade (PARTIAL, NOT_FOUND or CONTRADICTED, as the finding says) with the finding as the note: ' + A.regradeNotes : ''} Write ${OUT}/verdicts-${NAME}-regrade-${A.regradeTag || 'r'}.json as {"name":"${NAME}","regrade":true,"claims":[each regraded claim exactly as in kept-${NAME}.json with its index],"verdicts":[{index, verdict:"PARTIAL" | "NOT_FOUND" | "CONTRADICTED", trueWording, note: the verifier's original note or the critic's finding, unsupportedLimb}]} — ONLY the rows you downgrade. Return the count downgraded and the count left standing.`, { label: `regrade:${NAME}`, phase: 'Verify', model: 'opus' })
}
// ---------------------------------------------------------------- Synthesize
phase('Synthesize')
const ABOUT = NAME === 'ai' ? 'the catalogue of where generated prose FAILS against skilled human fiction and game writing' : `the prose style of ${(EXEMPLARS.find(e => e.key === NAME) || {}).name || NAME}`
// v3.3 (09-06 14:05): A.skipSynth — the previous round's synth already wrote section-<name>.md AFTER its regrade and only the critic died; run the critic alone.
const section = (A && A.skipSynth) ? (log(`skipSynth: section-${NAME}.md stands from the previous round (written after its regrade) — critic only`), '') : await agent(
  `First run, in a shell: \`cd ${KIT} && node sweep-state.mjs merge ${NAME} --update-state\` — it merges the state file${STATE_FILE ? ` (${STATE_FILE})` : ''} with every verdicts-${NAME}-*.json the verifiers wrote (this run's and any earlier run's) and writes ${OUT}/kept-${NAME}.json (the VERIFIED_VERBATIM / VERIFIED_SUBSTANCE claims only, each with its index, source, url, quote and verdict.trueWording). Read that file (with Read, in pieces if it is long). Begin the section with one line naming the exact files it was built from and their byte counts and mtimes (\`ls -l\` them), so a later reader can tell whether the inputs moved. Then write the dossier section for "${NAME}" — ${ABOUT} — from those VERIFIED claims only. Structure: numbered concrete features, each with the critics/sources that support it (count them; cite by index), the true wording of any quotation (under twelve words), and the reconstruction rule it implies for a settlement dossier written as a calm archivist (present tense, concrete civic nouns, no digits, no em dash). Mark disagreements between sources explicitly. A feature resting on ONE source is flagged as such. A row flagged fullSentence: true carries a trueWording over twelve words — cite it by its claim and by book/chapter/page and NEVER copy its trueWording (quote at most twelve words). PARTIAL rows (in ${OUT}/partial-${NAME}.json) may be cited ONLY for their verbatim quotation, never for the unsupported limb, and they get their own table at the end (index · quotation · the unsupported limb). Also state, in a short paragraph after the features: how many rows carry a second verifier's verdict (the \`alt\` field) and which disagree; the aggregate source concentration (share of kept rows from the top three hosts); and a REGISTER MAP naming which rules bind a settlement dossier's archivist, which the Herald's short pools, which the chronicle line, and which are for the DM page only.${A.synthNotes ? ' THE CHAIR\'S NOTES FOR THIS SYNTHESIS (apply each, and say where you did): ' + A.synthNotes : ''} End with a coverage table: sources per angle, and the verdict counts (verified / not found / contradicted / blocked). Write it to ${OUT}/section-${NAME}.md and return the markdown.`,
  { label: `synth:${NAME}`, phase: 'Synthesize' })
const critic = await agent(`You are the completeness critic for the sweep "${NAME}". Read ${OUT}/section-${NAME}.md, ${OUT}/kept-${NAME}.json and the find-${NAME}-*.md notes in ${OUT} (if present). List what is MISSING: an angle not run, a well-known critic or study absent (name them), a claim that rests on one source, a copyright risk (a quotation over twelve words), a verdict that looks wrong on its face (a VERIFIED claim whose quote is not on its page or whose note flags an unsupported limb — those are PARTIAL; a NOT_FOUND or BLOCKED that a second route would find — name the route: OpenAlex/Crossref for DOIs, scribe.rip for Medium, archive.ph, Internet Archive full text), and any feature the section contradicts itself on. Return a markdown list with a recommended next round of searches. Write it to ${OUT}/critic-${NAME}.md.`, { label: `critic:${NAME}`, phase: 'Synthesize' })
return { name: NAME, resumed: RESUME, regraded: !!(A && A.regrade), findAngles: FIND_ANGLES, extraAngles: EXTRA.map(a => a.key), freshClaims: freshClaims.length, chunks: chunks.length, chunksReturned: returned.length, verifiedThisRun: Object.keys(verdictMap).length, keptThisRun: kept, missingChunks: missing.map(c => c.k), sectionChars: (section || '').length, critic }
