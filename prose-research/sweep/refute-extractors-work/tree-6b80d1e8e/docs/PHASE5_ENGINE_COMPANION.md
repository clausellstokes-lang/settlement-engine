# Phase 5 — Engine Companion Wave (W-C): the owner's work order

OWNER SEQUENCING MANDATE (2026-07-11, verbatim intent): do ALL of the following first,
THEN the Reunification merge, THEN everything else continuing from Phase 5 (W2
multiplication onward). Chaos font stays HOLSTERED by evidence — correctly dormant,
no action in this wave.

Verification result folded in: `supplyCompleteness` feeds trade only (tradeWar.js,
tradeSalience.js). NO consumer wires it into deployed force quality; martialReadiness
has no supply input. The supply-gap quality penalty is confirmed MISSING → W-C1 scope.

Standing laws for every sub-wave: same-seed byte-identity is constitutional; dormancy
neutrality (no record ⇒ exact 1.0 / inert, conditional materialization only); strict
endogeneity (war/trade records feed mechanics; user/party actions NEVER feed fit);
golden corpus byte-identical or STOP and report (no silent regens); cause chains pinned
(every new mechanic emits its why); shrink-only ratchets + verify:dist post-build;
weekly-tick temporal constitution (all new constants justified at week scale).

Execution: sub-waves run SEQUENTIALLY in the shared checkout (two-writer doctrine;
the tree is the only truth). Opus implements; the manager reviews diffs, runs the
index review, and commits. W-C1..C3 wait for the calendar Option-A agent to clear the
worldPulse fence; W-C4 waits for the landing implementer to clear components/copy.

## W-C1 — The war cluster (one implementer)
Fence: martialReadiness.js, fidelityNoise.js, the war deployment/peace resolution
modules + a new supplyQuality leaf + their tests. NOT advanceInterval/worldState/
pulseKernel-calendar seams.
1. Rust at the two remaining sites, via the existing fidelityNoise pattern (seeded
   per-decision forks, capped): (a) DEPLOYMENT SIZING — rustMagnitude perturbs
   committed force size (over/under-commit, symmetric error, cap consistent with
   RUST_MAX_ERROR geometry); (b) SUING-FOR-PEACE — rust distorts the risk-calculator
   reading at the peace threshold (delayed or premature suit, alignment-scaled like
   the war-entry site: lawful reads true, chaos mis-reads).
2. Readiness THREAT-ENVIRONMENT input: warFooting01 gains a bounded threat term
   derived ONLY from world records — hostile stance lanes, active war fronts in
   region, recent raids/captures against self or neighbors. Peace with no threats
   decays readiness; a menacing border sustains it. Zero-threat worlds must produce
   bit-identical prior behavior (neutrality theorem).
3. SUPPLY-GAP QUALITY PENALTY (verified missing): a deployed-quality multiplier from
   supplyCompleteness over the WAR_SUPPLY chain commodities (iron→smelting→weapons,
   leather, horses, provisioning...). Equipment quality scales deployed stats and
   attrition; floored so a chainless settlement fields a degraded force, never zero.
   It does NOT feed readiness accrual (readiness = training/posture; supply = kit).
4. Readiness DOWN_DECAY weekly retune: 0.04/tick is month-contaminated (~4 months to
   zero). Retune to the ~0.001 class per the temporal constitution with soak evidence
   (religion-plane-soak pattern) proving decades-scale rust emergence, not seasons.
Evidence bundle: soak envelopes for threat-input on/off, quality-penalty gradient,
retuned decay half-life; docs/evidence/phase5-wc1/.

## W-C2 — Conquest feeds + compensating markets (one implementer, after C1)
Fence: war resolution outcome application, tierResourceDynamics economy seams,
institution demand/strength, + tests.
1. CONQUEST FEEDS: capture/occupation outcomes yield (a) captives → slave-market
   institutions ONLY where such institutions exist AND the CONQUEROR's moral plane
   permits (ABOLITION_FLOOR machinery is the gate; conscience FORECLOSES the revenue —
   the yield is lost, never laundered through another channel); (b) loot → market/
   prosperity pulses, temporary and cause-chained to the specific conquest.
2. MERCENARY / ADVENTURER-GUILD COMPENSATING MARKET: war demand where readiness or
   supply is short raises mercenary+adventurer-guild institution activity (demand-
   driven strength, W1 catalog tags exist); rented force costs prosperity and carries
   its own fidelity penalty (hired steel reads the risk calculator worse than sworn
   steel). A settlement can BUY readiness it didn't train — at a price and a noise.
Both: all inputs from world records; deterministic per-decision forks; conquest with
no eligible institutions produces exactly the prior outcome bytes.

## W-C3 — Institutional ecology + the moral founding lane (one implementer, after C2)
Fence: institution founding/lifecycle seams, moralInstitutionPressure.js, trade/faith
propagation touchpoints, institution catalog side-car additions, + tests.
1. MORAL FOUNDING LANE + EMERGENCE WEIGHTING: the founding seam exists; weight
   emergence by the settlement's moral plane and patron axes — good planes found
   almshouses/hospices/orphanages/houses-of-healing; cruel planes found fighting
   pits/debtors' yards. Catalog entries needed for the benevolent set: lifecycle-only
   entries (worldPulse-time founding), golden-inert like W1's side-car. If any entry
   must enter the GENERATION catalog: STOP and report (golden trap).
2. INSTITUTIONAL ECOLOGY + PROPAGATION: (a) TRADE NORMALIZES — sustained trade pulls
   partners' institutional tolerance profiles toward each other (slow, capped,
   cause-chained); (b) FAITH PRESCRIBES — extend moralInstitutionPressure across
   settlements via the existing faith-carrier graph (a proselytizing patron's plane
   presses its converts' institutions); (c) EMBARGO AS CONSCIENCE — a settlement whose
   moral plane abhors a partner's institutions (slave markets under a good plane)
   applies a standing penalty in trade-partner selection (tradeWar standing term),
   emitting the cause. Conscience costs the embargoer real trade score — morality is
   not free.

## W-C4 — Deity authoring UI (one implementer; may run parallel to C2/C3 once the
landing implementer clears components/copy)
Fence: custom-content components + copy registry + component tests. Engine untouched.
Adopt the export-lineage picker cluster (PrimaryDeityPicker / CultPicker /
DeityEffectPreview / PantheonActivationStrip patterns — reference checkout + export
source) onto OUR latent-pantheon architecture:
- Authoring lives in premium custom content (canUseCustomContent); the PORTFOLIO
  FLAVOR input gets its home in the authoring form (feeds the deity's domain/flavor
  fields; NEVER generation).
- Assign = the canon-event path mapped to our patron/activation seam (embed-on-assign
  snapshot, undo-clean), not their pre-latent semantics; the dormancy/activation strip
  reflects OUR milestones (latent baked → activated → dynamics on).
- Constitutional walls verified in-component: FREE/ANON never see live faith surfaces;
  lapsed = read-only owned embeds; tier never touches generation. The export lineage's
  two known leaks (ungated live-world dossier hook; public gallery deity facet) MUST
  NOT ride in.

## After the wave
1. REUNIFICATION MERGE — the parity-ledger worklist (74 absent / 39 partial / top-20
   ranked), landing spec already built, dossier three-band handoff (pending owner
   sign-off) as the dossier organizing frame, quick-win punch list, OAuth flip.
2. Phase 5 continues: W2 conjunction multiplication + age bands (THE regen), temporal
   audit/registry/normalization sweep, W4 session mode + exports, W5 polish.
