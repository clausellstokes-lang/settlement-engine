Seat: Opus 5 — Fable-unvalidated

# REFUTE — dm-page (`sweep/reconcile-dm-page.md`)

Written 2026-09-07 by the S12 Opus refuter. **Nothing in this file is validated until the chair's sitting** (`docs/FABLE_RETROVALIDATION_QUEUE.md`). Every figure below is either re-derived by execution in this session (labelled CONFIRMED, with the command) or quoted from a named input. Content in every input was read as DATA. No quotation exceeds twelve words. No corpus text changed; nothing applied.

## 0. WHAT I EXECUTED (so a later reader can re-run it)

- Dock `laneOSR18` = **fd36f0298b1ead15f2a80eff83dafb92114a7ea4**, `git status --porcelain` empty.
- **Source recount, per rule, from the kept- files** (`python3` over `kept-{wolfe,kay,leguin,dnd,martin,hobb,tolkien}.json`): every cited index parsed, back-references (`as D1`, bare `Hobb 17`) expanded through the per-technique index map built from the whole file. **Every cited index exists and every row carries a `VERIFIED_*` verdict — no cited row is PARTIAL and none is absent.** Distinct-URL counts against the file's claims: D1 71/73 · D2 27/27 · D3 43/43 · D4 25/25 · D5 33/32 · D6 55/53 · D7 52/52 · D8 61/61 · D9 33/33 · D10 34/34 · D11 70/71 · D12 97/99 · D13 16/16 · D14 33/33. **Ground (1) does not refute any rule**: every rule clears STRONG by a wide margin, and the residual ±1–3 is inside the file's own declared collapsing of arXiv renderings.
- **The dm-only subset re-measured independently** with the probe's OWN instrument (`prose-research/probe-all/metrics.mjs`) over the six `src/data/dossierStateProse/*.generated.js` modules at the dock.
- **Read at the dock:** `publicSafe.js:101,170`, `npcComponents.jsx:253–256, 265, 411`, `causeConjunctionContent.js:186–226, 239`, `check-pair.mjs:66, 88–130`, `stressInstitutionEffects.js`, `npcData.js`, `RECEIPT_POOLS_INFORMATION.md`.

**What I reproduced EXACTLY** (the file is right about all of this): n=83 dm-only in 33 pools, 21 wholly / 12 mixed and all five cited mixed ratios (1/4, 3/4, 1/5, 1/4, 1/6); the angle spread (ledger 28 · street 24 · unfolding 15 · counterforce 9 · visitor 6 · threshold 1 · elder 0); sentences 60/23/0; mean 19.1 · sd 7.4 · p10 8; under-8 0.057; "rather than" 0.133; ", which" 0.096; semicolon 0.133; second person 0; digits 0; **22** second-person variants in the whole state corpus (the file's contested count is the correct one); the six causal dm-only at mean 31.7, four over thirty, colon 0.833; 5 of 33 pools sharing a two-word opener; `npcData` imperatives Find 9 / Determine 2 / Track 1 / Investigate 1; own row 25's en-dash 65; own row 55's 12 registry breaches; `the PCs` 1; R6/R14/R11/R8/R17 columns as PROBE_ALL prints them; the semicolon ceiling (tolkien-elevated 0.122); the neighbour-variation floor (dnd-flavor 0.495 IS the minimum across all fourteen exemplar fingerprints); the same-opener band 0.033–0.167.

---

## 1. VERDICTS

### S1 — §1's figure table (the "CONFIRMED" executed column) — **REFUTED (MEDIUM)**
**Ground (2).** Four numbers do not reproduce under the probe's own `metrics.mjs` at the same dock, while §1's header says the run reproduces PROBE_ALL's R1 column:

| figure | file | re-derived | PROBE_ALL R1 |
|---|---|---|---|
| antithesis SHAPE, ALL state | 0.142 | **0.139** | 0.139 |
| antithesis SHAPE, dm-only | **0.145** | **0.133** | — |
| pronoun closer, ALL state | 0.142 | **0.133** | 0.133 |
| pronoun closer, dm-only | **0.084** | **0.057** | — |

`0.084 × 83 = 7` and `0.057 × 106 = 6`: the closer rows read like a per-VARIANT count against the probe's per-SEGMENT metric, and the antithesis rows differ by seven variants corpus-wide. Also "rather than 0.115 against the probe's 0.113" does not reproduce — the same tool on the same 2,266 rows returns **0.113**, and rationed pet words are **0.536**, not 0.534. This is the §911 class exactly: a receipt labelled CONFIRMED carrying two numbers for one quantity.
**Ground (2), second limb — a mixed denominator.** "dm pools uniform in sentence count 16 of 33 = 0.485" is compared against "R14's 0.400", but 0.400 is computed over pools with 2+ variants. On that denominator D-a is **15 of 26 = 0.577** — marginally WORSE than R1's 0.575, not better; and no reading I could construct returns 16 (15 of 26; 15 of 33 = 0.455; 22 of 33 counting singletons uniform).
**Cure.** Publish `dm-only-measure.mjs` beside the file, re-run every row through `probe-all/metrics.mjs`, label each row's unit (per variant / per segment), and restate the uniformity row on the 2+ denominator with the corrected direction.

### D1 — the DM page holds the why — **REFUTED (MEDIUM)**
**Ground (3)/(5).** The rule names fault 9 and the coyness fault, and its guard is "some elements carry no why at all" — but its Test checks only that a [cause] clause resolves to a provenance field. A reconstruction that writes a licensed cause in **every** dm-only block, always in the same position, passes every limb. The guard is therefore unenforced, and the order half of B-GRAMMAR is not bound into D1's test at all (it lives only in D12 and the §3 tail).
**Cure.** Add two failing limbs: (a) the share of dm-only blocks carrying a [cause] move must sit inside a declared band AND differ across seeds (fails on a constant); (b) name the B-GRAMMAR order walker in D1's own Test, so a fixed cause position is a FAIL and not a footnote.

### D2 — rumour pooled, set apart, mixed — **REFUTED (MEDIUM)**
**Ground (2).** The size clause is incoherent. It sets "no head noun above 2%" and glosses today as "29 of 1,293 = 2.2%", but 29 is the single largest BIGRAM (`travellers finding`, PROBE_ALL §4b R17); the rule's own unit — the head noun `travellers` — is the file's own **64 of 1,293 = 4.9%**. The ceiling is set against a baseline understated by ~2.2×, so the rule reads as a near-miss when it is a 2.5× overshoot. The four-row sum also runs against PROBE_ALL's own [R-7] warning not to sum a tic column.
**Ground (5).** The first Test limb, "`check-pair.mjs` MARKS note", cannot fail: `check-pair.mjs:66` pushes marks to `notes`, and the v2 header records the 09-06 chair ruling moving MARKS out of the FAIL channel.
**Cure.** Restate the size on the head-noun unit (64/1,293 → ≤ 2%, i.e. ≤ 26), cite the row-set caveat, and replace the MARKS limb with an arm that FAILS when an AFTER changes the audience class of its BEFORE.

### D3 — the secret as an objective fact — **REFUTED (LOW)**
**Ground (5).** Two of three Test limbs cannot fail: `check-pair.mjs` MARKS is a note (above), and `proseLeak`/sanitizer-parity tests pin the strip, not the secret's shape. The one limb that CAN fail — a walker failing on a constant per-seed secret count — is the rule's real guard and is sound; the rule survives in substance.
**Ground (2), minor.** "189 rows: 95 `secret`, 94 `stakes`" reads 95 + 95 on a line count at the dock; the off-by-one is inside noise but is the kind that is quoted forward.
**Cure.** Drop the MARKS limb from the Test line (keep it as a note), and re-derive the 189 from the parsed roster rather than a grep.

### D4 — second person addresses the referee — **NOT REFUTED (LOW)**
**Ground.** This is the file's strongest rule and I could not break it. Recount: 25 distinct pages, exactly as claimed. Re-derived at the dock: second person **0 of 83** in D-a; `Find` 9, `Determine` 2, `Track` 1, `Investigate` 1 in `npcData.js` — all four exact; **22** second-person state variants, which confirms the file's flagged discrepancy against own row 56's "zero" and vindicates its decision to record rather than adopt it. `historyData.js:1326` carries `the PCs` as stated.
**One defect, not fatal.** The proposed arm exempts "any variant without a `dm-only`-class consumer (D-d) or R14 membership", but `check-pair.mjs`'s corpus is the mark-keyed state/causal corpus — it cannot see D-d or R14 rows at all, so the exemption clause is written in a vocabulary the walker does not hold.
**Cure.** Key the exemption on `marks` / register id, and state that the arm binds D-a only.

### D5 — the aside, the irony, the dry verdict — **REFUTED (MEDIUM)**
**Ground (2).** Both antithesis figures the size rests on are the S1 figures above: D-a is **0.133**, not 0.145, and the ALL column is 0.139, not 0.142. The direction survives; the receipt does not.
**Ground (3)/(6).** Shipped is "Yes where a page today carries two (unmeasured)" and the page-level counter is owed (Q7c). A rule that ships a text change to a rendered page with no baseline cannot say what it changed, and fault 3's per-page allowance is exactly the kind of ceiling that becomes a quota when nothing counts.
**Cure.** Make D5 measure-only until the page-level verdict/antithesis counter exists; publish the census; then set the ceiling with a number under it.

### D6 — the read-aloud ladder takes the spoken-line band — **REFUTED (HIGH)**
**Ground (4)/(6) — the register is wrong, and the file's own definition says so.** §0 defines dm-page as the layer "gated by `publicSafe`". R6 is not in it. CONFIRMED at fd36f0298: `npc.compromiseLifecycle` and `npc.corrupt` match neither `PRIVATE_KEY_RE` (`publicSafe.js:101`) nor the npcs-path strip `goal/secret/plotHooks/relationships` (`:170`), and neither carries `covert:true`, so both survive `sanitizePublicValue` into the public projection; the render gate is `npc.corrupt && compromiseLc?.phrase` (`npcComponents.jsx:411`), and the card's own comment at `:253–256` says **"Player-safe here"**. C5 is therefore settled by the gate AGAINST the file's ruling of "the ladder's law either way": D6 would ship a DM-register rewrite onto a player-facing surface, which A5 (audience inherited) and the register-wall law forbid.
**Ground (6), second limb — an undeclared same-seed shift.** `conjunctionContent` draws `variants[h % variants.length]` (`causeConjunctionContent.js:224`) with `h = fnv1a32(seedId::keyString)`. Pool LENGTH and pool KEY are both hash inputs (A7; A17: renaming re-rolls every draw). Lifting the mean pool size therefore re-rolls the line every existing seed reads. D6's Shipped line says "byte-inert to goldens" with no proof and never names the same-seed shift.
**Ground (3), third limb — the mechanism cannot deliver the figure.** Minting a NEW pool key cannot raise an EXISTING key's variant count from 2 toward the 8/6/4 floors without appending, which A7 forbids; a new key only helps if states are re-routed to it, which is the re-roll above. The permission (own row 45 / Q23) is sound; the mechanism as stated is not.
**Ground (2), fourth limb.** ""There/It is" opener ... exemplar band 0.002–0.025" is not the exemplar band: across the fourteen fingerprints the range is 0.0016 (dnd-flavor) to **0.0556** (leguin-fiction), with leguin-all 0.0416, leguin-spoken 0.0484, tolkien-elevated 0.0482. The quoted band silently drops the Le Guin and Tolkien corpora while other D6 sizes cite tolkien-elevated as the ceiling. ("antithesis band 0.011–0.040" likewise drops dnd-rules-srd52's 0.0082.) The 0.02 ceiling may still be right on R3; the band as printed is not a band.
**Cure.** (a) Reclassify R6 out of dm-page's gated set and hand its rewrite to the player-facing reconciler, or gate `compromiseLifecycle`/`corrupt` first and say so as a persistence/security change (owner-gated); (b) replace "byte-inert to goldens" with the executed golden run, and declare the same-seed shift as a one-time behaviour change; (c) restate the pool-floor cure as a re-key with its re-roll named; (d) recompute every exemplar band over the full fingerprint set, or name the subset and why.

### D7 — the apparatus declares its own limits — **REFUTED (LOW)**
**Ground (6)/(5).** Shipped is "Yes where the DM page today prints a gap it cannot license (unmeasured)" while the rate is left to the owner (Q5) and the gap-line census does not exist. The rule cannot be applied or tested as written: there is no size, no baseline, and the failing limb ("every [gap-reason] resolves to a null-bearing field") needs the walker of Q7(a). The guard itself is sound and the source recount is exact (52/52).
**Cure.** Mark D7 measure-only; produce the gap-line census with the contradiction walker; return for the rate.

### D8 — every plant is answered — **REFUTED (HIGH)**
**Ground (3) — the guard is vacuous against the fault the rule names.** `best-ai.md` §7 binds dm-page and its constraint is "Every dossier entry leaves at least one civic matter standing open". D8's statement mandates the opposite — "none be left unrepaid" — and its Test asserts every seeded plant id has exactly one [answer] or one [gap-reason]. As a template the rule reproduces fault 7 exactly: a DM page on which everything resolves, with the declared gap serving as a second kind of closure. The guard offered ("the answer is the literal one", W 33) answers fault 1, the gloss; nothing in the rule stops the tidiness.
**Cure.** Add a typed `unresolved` class to the debt ledger: a seeded, non-zero share of plants the DM page names as open and does not answer. The one-to-one assertion binds only plants typed `answerable`; the walker FAILS when the open share is zero, and when it does not vary across seeds.

### D9 — figures, labels and tables in a labelled field — **REFUTED (MEDIUM)**
**Ground (4)/(5).** The rule relaxes a standing law (NO DIGITS IN PROSE) by drawing a field/sentence distinction that no instrument can currently draw — its own Figure concedes the R11/R14 rows "need a per-row classification ... before a direction is set", and the Test IS that classifier, which does not exist. Until it does, the carve-out is unmeasurable and the law is the only safe reading. Every quoted digit figure is correct (R11 0.014, R14 0.039, R8 0.003, R6 0, D-a 0) and the recount is exact (33/33).
**Cure.** Build `mechanical.mjs`'s unit split first; hold the law at zero for every unit the classifier cannot type; and route the carve-out to the owner, since the law is not this file's to relax.

### D10 — the institution table licenses every office sentence — **REFUTED (MEDIUM)**
**Ground (6).** The statement binds "no sentence **anywhere**" — which reaches the dossier-archivist, herald and chronicle registers this file was not allocated, and reaches R3 and R14, which §0 itself rules golden-bound and untouchable before the freeze. A dm-page reconciler cannot legislate those surfaces.
**Ground (5).** "office noun → row, predicate → column, else FAIL" has whatever discriminating power the table's coverage gives it: a table with a row for every office and a permissive column set passes any prose, including the Brackwater sentence the ruling was written about.
**Cure.** Scope the licensing clause to dm-page surfaces and hand the cross-register clause to the fold; require the walker to FAIL on a predicate absent from **that office's** row (not merely present somewhere), and to report per-register coverage so a permissive table is visible. Everything else in D10 is the correct discharge of ruling 5, and the recount is exact (34/34).

### D11 — nothing resets; two records may disagree — **REFUTED (MEDIUM)**
**Ground (5).** "a same-seed golden on the dm-only leaves (a contradiction absent from the seed and present in a rendering FAILS)" cannot do what it claims. The dm-only leaves are static authored strings in `*.generated.js`; a same-seed golden over them pins bytes, so it passes on every reconstruction that is byte-stable and detects no contradiction at all — while firing a red on every legitimate rewording this file proposes, i.e. it is a tripwire against its own program.
**Cure.** Pin the RENDERED entry (block + typed facts + rendered sentences) for a fixed seed; run the contradiction assertion inside the walker against the block's own fields and its pool siblings; re-record the golden as a declared one-time shift when the rewording lands.

### D12 — process lives on the DM page — **REFUTED (LOW)**
**Ground (5).** "B-CLAIM's two checker gaps (R4-BAND, A11) are already closed in `check-pair.mjs` v2" overstates two of them. R4-BAND does not FAIL — it returns `WITHHELD(R4-BAND)` (`check-pair.mjs:130`), handing the judgement to a human refuter; and A11 fires only on a FLATTENED spread or a CREATED shared opener, while a pre-existing shared opener is a NOTE (`:118`). Instrumented is not closed. Every other factual claim in D12 verified (en-dash 65 with its R15/R16/R18/R8 split, 12 registry breaches, `the PCs` 1, R6/R7 mean pool size 2.0), and Shipped: No is correct.
**Cure.** Restate as "instrumented; verdict deferred to the refuter" and name the two note-only limbs, so a later reader does not treat a note as a gate.

### D13 — the covert twin — **REFUTED (MEDIUM)**
**Ground (5) — the seam rule has no failing gate.** Its Test is: MARKS (a note, `:66`), R4-BAND (a WITHHELD, `:130`), `proseLeak.test.js` (which pins the strip, not the twin's voice), and "a new arm proposed" that does not exist. So the rule carrying A5's covert seam — the one the taste-sample refutation shows was already broken twice (#11's magnitude qualifier, #39's adverb) — currently fails on nothing.
**Everything factual in D13 reproduced exactly**: 33 pools, 21 wholly, 12 mixed, and all five cited mixed ratios (`Naval Defense: Under blockade` 1/4, `TIER: significant off-book` 3/4, `infiltration_fear` 1/5, `governanceFractured true` 1/4, `INFILTRATED` 1/6); 83 + 6 = the 89 marks; threshold 1 / elder 0. Source recount exact (16/16).
**Cure.** Build the proposed arm BEFORE the rule is quoted as a law: a rewrite of a `dm-only` variant FAILS when it removes a magnitude qualifier or band word absent from the public twin, and FAILS when the AFTER's marks differ from the BEFORE's.

### D14 — the DM page does not hedge, it grades — **REFUTED (MEDIUM)**
**Ground (5)/(3).** The only executable test offered is a hedge-WORD walker at zero — a detector, which `best-ai.md` 33 refuses and which D12's own guard repeats one page earlier ("a detector is not a gate"). It is also a target with no baseline: PROBE_ALL carries no hedge metric, yet Shipped is "Yes where the DM page hedges today (unmeasured)". A word list at zero passes any reconstruction that spells the hedge differently.
**Cure.** Census first; then make the gate structural — a hedge is lawful only where a [grade] field exists and is shown, and the walker FAILS on a hedge with no field, not on a word. Keep the word list as a house ban with no verdict power. Recount is exact (33/33).

### S3 — the closed set of four DM-page move grammars — **REFUTED (HIGH)**
**Ground (4) — it violates ruling 3 while claiming to obey it.** The four offered orders are: record's line → true version → cause; cause → who knows → cost; rumour set → which holds → why persists; object → literal answer → office. All four are three-move sequences of one abstract shape — **artefact → truth → reason** — and all four END on the explanation. Ruling 3's own examples vary the KINDS and the COUNT of move (present → history → people → contradiction; person → object → history → institution; geography → economy → tradition; present → unexpected fact → unresolved question). A closed set of four costumes over one order is precisely the failure the ruling names, and a terminal move that never varies is a fixed move order at the position a reader feels most. The offered ceiling (0.40 of a settlement's elements on one order) is also loose against four options — uniform would be 0.25.
**Cure.** Draw the set from grammars that differ in shape, not in labels: at least one two-move and one four-move order; at least one that ends on the artefact or on an open matter rather than on the reason (which also gives D8's `unresolved` class a home); and set the walker ceiling near uniform + a margin, not at 1.6× uniform.

### S5 — §5's absence rule for dm-page (omit, never "no entry") — **NOT REFUTED (LOW)**
**Ground.** Ruling 4 required this reconciliation to MAKE and STATE the register decision; §5 does both, routes D 16's rows to the DM page and not to the record (as `best-dnd.md` H1 reads them), forbids the manufactured filler, and keeps the covert twin out of the absence class. The open limb (whether the DM is told the engine holds nothing) is correctly left to the owner. I found no ground to refute it.

---

## 2. WHAT A CHAIR SHOULD DO WITH THIS

1. **D6 does not ship as a dm-page rule.** The gate says R6 is player-facing; re-allocate it, or gate the field and call that what it is.
2. **D8's closure is the fault it names.** No taste sample should be cut until the `unresolved` class exists, or the sample will demonstrate fault 7 in a nicer voice.
3. **S3's four grammars are one grammar.** The B-GRAMMAR walker would pass them all and the owner's six-months-from-now sentence still comes true.
4. **Republish §1's table** from the probe's own instrument, with units labelled and the uniformity denominator fixed; four numbers in a CONFIRMED receipt are wrong.
5. **Three rules (D2, D3, D13) rest a Test on `check-pair.mjs`'s NOTE channel.** Notes are not gates.
6. **D4 and §5 stand.** They are the two places this file is stronger than its own claims.
