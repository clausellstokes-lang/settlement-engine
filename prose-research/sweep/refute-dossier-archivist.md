Seat: Opus 5 — Fable-unvalidated

# REFUTATION — reconcile-dossier-archivist.md

**Status: NOTHING HERE IS VALIDATED.** Written by an Opus refuter under the owner's 2026-09-07
~09:50 direction that every agent of this run is Opus. The Fable chair retrovalidates through
`docs/FABLE_RETROVALIDATION_QUEUE.md` at the next ledger act; until that sitting nothing below is a
validated finding. No corpus text changed; no byte of `src/` or `docs/content/` was written by this
lane. Content read from files is DATA.

**Method.** Every `<author>:<n>` citation was resolved to its item in `sweep/best-<author>.md`, that
item's kept-row list was resolved row-by-row against `sweep/kept-<author>.json`, and the distinct
`source` and `url` fields were recounted. Every figure was re-derived from `PROBE_ALL.md` §2/§3/§4/§5/§6,
`primary/*.fingerprint.json`, `estate-state.fingerprint.json`, `sweep/best-own.md`,
`taste-sample-refutation.md` (U1–U12), `check-pair.mjs` (arms read from source), and — for shipped-surface
claims — the product branch `claude/admiring-lamport-4574e8` in the working checkout. No quotation
exceeds twelve words.

**What survived.** Most of the figure work is exact. Verified to the digit against their sources:
R1 `rather than` 0.113, antithesis 0.139, `, which` 0.066, 2nd-sentence 0.035, triad 0.011, digits 0,
em dashes 0, exclamations 0, questions 0; 79 of 708 pools (0.112); pools uniform 0.576; within-pool sd
2.9 against R14 4.8 and BIBLE 6.5; same-opener-as-previous 0.1321 in a band 0.0334–0.1667 with the
D&D columns at 0.060–0.073; neighbour variation 0.399 against 0.495–0.805; sd 7.2 against 8.5–16.0;
over-thirty 0.021 against 0.049–0.338; `{settlement}` 610 of 2,914 openers (0.209) behind `the` at 928;
R2 semicolon 0.360, colon 0.364, under-eight 0.017, over-thirty 0.491, n=467, slot-bearing 0.953,
pool size 6.0; martin-chronicle 0.0000 which-tails in ~12,406 words, 0.0543 pronoun closers, 0.0292
under-eight, 0.3382 over-thirty; tolkien-elevated 0.0401 antithesis and 0.122 semicolons;
dnd-rules-srd52 0.0082; R7 8.8 words, 47.2% under eight, 0.086 abstract closers, 0.03 digits, 0.069
3+-share; R14 sd 8.1, 3+-share 0.221, 0.400 uniform; R17 terminal-stop 0, 1.000 segments, 7.5 words,
29/14/11/10 = 64 of 1,293 = 0.050; A-U 0.062 / 0.287 / 0.123 over 4,626; TTR 0.211 lowest of eight
against a 0.242 floor; `[R-5]`'s 8 vs 129 (17.2%) and 13 truncated rows; ai faults 1/3/5/9/10/11/24/26
graded and counted exactly as §1h reports them; U1–U12 quoted faithfully. **`historyData.js:1326`
carries `the PCs` at that exact line on the product branch** — the one line-number citation in the
file is correct.

Sixteen rules were tested. **Fifteen are refuted on at least one ground; DA-16 is not.**

---

## THE VERDICTS

### DA-1 — SPEND THE CONTRAST ONLY WHERE A SIBLING NAMES THE ALTERNATIVE — **REFUTED (MEDIUM)**

**Ground 3 — the fault guard does not gate.** The rule's guard is that the rubric counts the shape,
not the phrase. `check-pair.mjs:85` fails on `ANTITHESIS SHAPE ADDED` only when the count *rises*;
the U4 case the rule cites by name — `rather than` removed, the antithesis surviving as a bare `not`
— lands at line 86–87 as a **NOTE**, and line 130 makes the verdict `FAIL` only from the `r` list.
Six of U4's eight removals are exactly that case. So at the point of edit the named failure mode
passes.

**Ground 5 — the only gate that can fail is the aggregate.** What remains is the fingerprint delta on
`shapes.antithesisRate`, a per-file measure taken after the wave. That is the precise error U3 names
and DA-16 quotes in its own acceptance: a ration must be a per-variant net test at the point of edit.
DA-1 does not adopt it.

**Ground 2 — the baseline and the test are different instruments.** The stated baseline 0.139 is
PROBE_ALL's per-variant rate over 2,262 variants; the named test reads
`estate-state.fingerprint.json`, whose `shapes.antithesisRate` is **0.1088** per sentence. A reader
tracking "0.139 → ≤0.040" against the named instrument will never see 0.139.

**Cure.** Promote the `rather than` → bare-`not` NOTE to a FAIL arm; state the baseline as the
fingerprint's 0.1088 (or name the probe as the instrument and drop the fingerprint delta); add a
per-variant net test at the point of edit.

### DA-2 — THE QUALIFICATION GETS ITS OWN SHORT SENTENCE — **REFUTED (LOW)**

**Ground 2 — the SIZE's justification is wrong twice.** The file writes that 0.010 is *the midpoint*
no exemplar's record register exceeds. The midpoint of martin-chronicle's 0.0000 and the stated
ceiling 0.037 is **0.0185**, not 0.010; and two exemplar rules columns exceed 0.010 —
dnd-rules-srd52 `whichTailRate` **0.0274** and dnd-rules **0.0237**. The target is defensible; the
sentence that justifies it is not.

**Ground 6 — see S-SHIPPED-R1.**

**Cure.** Restate the SIZE as below every record-register exemplar except the D&D rules columns,
anchored on martin-chronicle's measured zero; drop the word midpoint.

### DA-3 — LAND ON A CIVIC NOUN — **REFUTED (MEDIUM)**

**Ground 2 — the parenthetical target is derivable from no instrument.** The rule writes
`0.133 → ≤0.055 (253 of 2,914 sentences end on "it" → ≤120)`. Three quantities are welded together
here and none of them yields 120:

| quantity | instrument | value |
|---|---|---|
| 0.133 pronoun closers | PROBE_ALL, per **variant**, n=2,262 | ≈ 301 closers; at 0.055 → ≈ 124 |
| 0.1335 pronoun closers | `estate-state.fingerprint.json`, per **sentence**, n=2,914 | ≈ 389 closers; at 0.055 → ≈ 160 |
| 253 | best-own.md, sentences ending on the word **"it" alone** | at the same ratio → ≈ 104 |

`≤120` matches none of the three. The 2,914 denominator belongs to the fingerprint, the 0.133 rate to
the probe, and the 253 to an "it"-only subset of a rate that also counts *them / there / this / that /
one* (the closer set is confirmed at `check-pair.mjs:129`). An acceptance number that no instrument
produces will be enforced anyway.

**Cure.** Pick one instrument. If the fingerprint is the test, write `0.1335 → ≤0.055 (389 → ≤160
sentences)`; keep 253 as the "it"-only sub-figure, labelled as such.

### DA-4 — THE POOL IS THE UNIT OF RHYTHM — **REFUTED (LOW)**

**Ground 1 — `dnd:46`'s three "documents" are not independent sources.** All fourteen cited kept rows
resolve to two publisher PDFs measured by the lane's own finder: eleven rows carry the source string
"Finder measurement over SRD 5.1" and three "Finder measurement over D&D Basic Rules v1.0". That is
one in-house instrument over two texts by one publisher. It may support a SIZE as a measurement; it
may not enter a union of *independent sources*, and it inflates the stated 30 by three.

Everything else in this rule verified exactly, and the file's own PROVISIONAL flag on the SIZE is
honest. The device stands on five sources without dnd:46.

**Cure.** State the union as 27 and carry dnd:46 as an instrument reading, not as three sources.

### DA-5 — THE TOWN'S OWN NAME IS NOT THE DEFAULT OPENER — **REFUTED (MEDIUM)**

**Ground 5 — the two limbs of the SIZE cannot both bind.** The statement is a per-pool rule ("no more
than one variant in five in any pool"); the Figure is a corpus rate (≤0.200 hard, ≤0.150 target).
R1's **mean pool size is 3.2** and 708 pools carry two or more variants (PROBE_ALL §3). One in five
of a three- or four-variant pool floors to **zero**, so the per-pool limb forbids any settlement
opener in most of the register — under which the corpus rate lands far below 0.150 and the published
hard ceiling of 0.200 is a near-no-op against a measured 0.209.

**Ground 1 — `dnd:21`'s five sources are one publisher.** The nine kept rows resolve to: two D&D
Beyond *Encounter of the Week* columns (Wizards of the Coast), four rows of the Wizards Settlement
Tracker handout, one more Settlement Tracker sheet, and Ghosts of Saltmarsh relayed by Blackman.
The file's own naming counts the Tracker twice ("the Settlement Tracker, the tracker's coin field")
and the publisher's two columns as two voices. Honest independent count: **one publisher plus one
relay**, not five.

**Cure.** Make the per-pool limb "at most one settlement opener per pool, and never in two adjacent
variants", and let the corpus ceiling carry the rate; recount dnd:21 as 2 and the union as 30.

### DA-6 — THE APPARATUS SPEAKS ONLY WHERE THE ENGINE HOLDS A RECORDED NULL — **REFUTED (HIGH)**

**Ground 5 — the acceptance cannot fail, because it tests something else.** The rule claims U11 and
U12. U11's finding is that the closed **count and duration vocabularies** are absent from the rule
set (its breaches are #4's souls→households, #23's "every year", #32's "the two or three places").
U12's finding is **R-DST-B**, structural licence versus historical licence (#14, #19, #37, #34, #32).
DA-6's walker asserts that an apparatus sentence resolves to a *recorded null* field. It would catch
none of those eight items. The rule's acceptance is satisfied by construction.

**Ground 4/6 — it is owner-gated and says the opposite.** DIRECTION is "up from a measured zero", the
licensing field is a "null-with-provenance" class, and no register is shown to hold one. Building a
new typed field class on world state is a persistence/schema change — an owner-gated class — and the
rule is marked **Shipped surface: no**.

**Ground 4 — the ration contradicts §4 tier 1.** §4 rules that where the engine holds a recorded null
the absence **is written** — "a sentence like any other" — and licensed by that field exactly as
every other sentence is. DA-6 then caps the family at "no more than one dossier in three carrying one at
all". Two dossiers in three must therefore suppress a field-licensed absence — which is the ration
overriding the licence, and it is ruling (4)'s own mechanism being switched off by a rate.

**Cure.** Split the ration from the licence: a **recorded** null is always reported (that is ruling
4); the ceiling governs only the *elaborated* apparatus move (the lost roll, the uncorrected count) and
its shape-variety. Re-assign acceptance to a U that tests absence, or state honestly that DA-6 has no
U. Mark the field class owner-gated and route it beside DA-7.

### DA-7 — THE INSTITUTION TABLE LICENSES EVERY SENTENCE ABOUT OFFICE, COUNT AND EXEMPTION — **REFUTED (LOW)**

This is the strongest rule in the file and its substance survives. All five exemplar citations
resolve: wolfe:24 (8 claimed, 10 distinct sources across 11 kept rows), hobb:20 (7 / 8), tolkien:F3
(3 / 4), dnd:27 (6 / 9), kay:7 (6 / 9), ai fault 24 (15 documents, STRONG, archival YES). The
owner-signed marking is **correct and confirmed in the checkout**: on the product branch
`src/data/institutionalCatalog.js` is imported by `src/generators/cascadeGenerator.js`,
`src/generators/steps/assembleInstitutions.js` and `src/generators/lookups.js` — a generation input,
therefore golden-bound.

**Ground 2 — the acceptance figure contradicts DA-8's.** DA-7 writes "four field-licence breaches in
forty items, two of them in KEEPs". U12 lists **five** (#14, #19, #37 and the KEEPs #34, #32), and
DA-8's own Figure writes "5 field-licence breaches in 40 items (12.5%)" — the arithmetically
consistent reading. The same measure appears twice in one file with two values.

**Cure.** Restate DA-7's acceptance as five in forty (12.5%), matching DA-8 and U12.

### DA-8 — THE SAME-ENTRY CONTRADICTION WALKER GATES EVERY MOVE — **REFUTED (MEDIUM)**

**Ground 5 — the test is a reporter and cannot fail.** The Test line makes the walker its own test and
gives it one verb: it runs over the 2,734 variants plus A-U **"reporting" per-variant** the fields
each assertion resolves to. A report has no assertion. The SIZE (zero unresolved contradictions) names no arm, no
threshold and no failure channel, and the file itself records at §6 that the instrument does not
exist.

**Ground 1 — the stated union is not any of the numbers on the page.** 5+8+5+6+1+5+9 = **39**, and the
rule states **34**. Every other rule in the file states the exact arithmetic sum of its components, so
the declared "union of DISTINCT independent sources" method is not what produced 34 either. Where a
deduction is genuinely owed it is unexplained: wolfe:31 and wolfe:41 share Wolfe's own written reply
to Nigel Price, and tolkien:E1 and tolkien:G4 overlap.

**Cure.** Give the walker an assertion arm and a failure channel before calling it a test; show the
34's derivation row by row, or restate it as 39 minus the named duplicates.

### DA-9 — A CONSEQUENCE SENTENCE NEEDS AN EVENT-PROVENANCE FIELD — **REFUTED (LOW)**

Sources verified: hobb:8 (10 claimed, 13 distinct across 18 rows — the best-evidenced import in the
matrix, correctly reported), kay:14 (7 / 10), kay:16 (6 / 6), martin:38 (4 / 6). Figures verified.

**Ground 3 — the guard names two wrong faults.** It cites "ai fault 3" for *the pairing becoming a
metronome* — the metronome is **fault 5**; fault 3 is reflexive antithesis. It cites "fault 1, the
stamped equation" — fault 1 is **the gloss, the summary second sentence**. The mechanisms described
are real; the catalogue numbers attached to them are not, and a guard is only as executable as the
fault it points at.

**Cure.** Re-cite: the pairing-as-cadence is fault 3, the every-block rhythm is fault 5, the stamped
equation is fault 8 (flat pressure, swappable sentences) or fault 17, not fault 1.

### DA-10 — THE STATE STANDS FIRST AND THE EDGE IS SUBJUNCTIVE — **REFUTED (MEDIUM)**

**Ground 3 — the fault guard cites a fault with no bearing on the rule.** "ai fault 2's register form
— no assigned reaction, no prediction". best-ai.md item 2 is **"The glossary tail — the participial
significance close"**, 3 documents / 2 independent, **MODERATE**. It says nothing about assigned
reactions or predictions; that content is dnd:2's. The rule's only named anti-AI guard therefore
guards nothing, and it is the weakest-graded item in the catalogue.

**Ground 5 — the test cannot catch half of its own acceptance.** DA-10 claims U7, whose finding is
two-sided: #19's subjunctive **deleted** and #7's future-indicative necessity **kept without
comment**. `check-pair.mjs:123` fires only when `will|shall` appears in the AFTER and not the BEFORE
— a future indicative present in both passes silently. The SIZE ("zero future-indicative necessity
forms across the wave") is an absolute census; the named instrument is a delta arm.

**Cure.** Re-cite the guard to ai fault 24 (fabrication/forecast) or drop it; add a census arm that
counts future-indicative necessity forms in the AFTER regardless of the BEFORE.

### DA-11 — THE GNOMIC CLOSER IS A LICENSED DEVICE WITH A CEILING — **REFUTED (LOW)**

**Ground 2 — the ceiling has no baseline.** DIRECTION is "a ceiling where none exists" and SIZE is
0.010 per variant in R1, "measured by a new walker arm before any is written". No current rate
is measured, so 0.010 may sit above the live rate — in which case the ceiling *licenses an increase*
in a device the rule itself calls a claim about the world, which B-CLAIM forbids. The rule's own
MODERATE grade and its prohibition on creating one in a rewrite limit the damage; the number remains
unanchored.

**Cure.** Measure the live gnomic-closer rate in R1 first, then set the ceiling at or below it.

### DA-12 — VARY THE CIVIC NOUN ACROSS THE POOL — **REFUTED (LOW)**

Sources are the strongest in the file and all verified (wolfe:3 10/14, hobb:10 9/13, martin:36 7/11,
leguin:21 7/14, dnd:8 9/11, kay:20 7/9). TTR 0.211, the 0.242 floor and the triad guard 0.011 ≤ 0.020
all verified.

**Ground 5 — the named test does not exist in the named instrument.** "A fingerprint delta on
type-token ratio at a fixed 4,000-token window" — no `*.fingerprint.json` carries a type-token field;
the keys are exhausted by `wordsPerSentence`, `shapes`, `closers`, `openers`, `punctuation`,
`sentences`, `paragraphs`, `sentencesPerParagraph`, `runsOfThreeSameLengthBand`. TTR is measured by
`primary/extras.py`, best-own's instrument **I6**.

**Cure.** Name `primary/extras.py` (I6) as the instrument, or add TTR to `fingerprint.mjs` first.

### DA-13 — THE JOINT IS CHOSEN, AND EVERY POOL CARRIES ONE SHORT RELIEF — **REFUTED (MEDIUM)**

**Ground 1 — the union double-counts, and one component contributes nothing.** `leguin:39` is cited
as one source; **both** of its kept sources are already inside `leguin:24`'s set (Le Guin's own
B-Sides annals piece and 'Mountain Ways'), so it adds **zero** to the union. `kay:44` and `kay:24`
share Paul O'Neill's Goodreads review of Under Heaven. The stated 31 is the arithmetic sum, not the
"union of DISTINCT independent sources" the file's own method declares; the honest floor is 29 or
lower.

**Ground 2 — the semicolon ceiling is the cured register's own live rate, mislabelled.** "the estate
control's own 0.1325" is `estate-state.fingerprint.json`, which **is R1** — the register this same
file is curing. PROBE_ALL's R1 semicolon rate is 0.17. Using one dossier register's current rate as
another's ceiling is a choice; calling it a control is not accurate.

**Cure.** Restate the union at 29 with the duplicates named; anchor the semicolon ceiling on
tolkien-elevated 0.122 alone and say that R1's own 0.1325 is a live rate, not a control.

### DA-14 — ONE TERM FOR ONE THING, ONE NAME FORM — **REFUTED (MEDIUM)**

`historyData.js:1326` is **CONFIRMED exact** on the product branch, and 5+4+3 = 12 is arithmetic.

**Ground 2 — "all in JSX" contradicts the cited source.** best-own.md row 55 records `DM-private` 5
(all JSX chrome), `publicly visible`/`public-safe` 4 (all JSX chrome) and `profit enormously` 3 —
**"world-data plus the long tail"**. Three of the twelve are R8/R15, not chrome, which changes their
class: R8 is a generation input and golden-bound.

**Ground 6 — the published ratchet cannot be moved by the wave that publishes it.** The Figure sets
"12 → 0"; the Shipped-surface line then says chrome bytes are the surface-text program's, not this
wave's. Nine of the twelve are chrome. The wave can move at most three.

**Ground 6 — the scope line under-states the rule's own binding.** The statement binds R1, R2, R7,
R8, R14, R15, R17, R18 and the annex. R14 is `src/generators/**` and R15/R18 are `src/**` — all
generation-side. The Shipped-surface note names only R8 and JSX.

**Cure.** Split the ratchet: freeze the nine chrome breaches as owed to the surface-text program and
target the three R8/R15 breaches plus `the PCs` in this wave; extend the shipped-surface line to
R14/R15/R18.

### DA-15 — THE RUMOUR PHRASE REGISTER GETS HEAD-NOUN DISPERSION — **REFUTED (LOW)**

Every figure verified exactly, including the four tic rows summing to 64 of 1,293 (0.050) and the
phrase-register shape (terminal-stop 0, 1.000 segments, 7.5 words, 1.000 uniform, 0.006 repeated
opener). The refusal of a fingerprint delta here is correct and well argued.

**Ground 1 — two of the three cited items carry no limb of the rule.** `kay:11` is "Omission as
record: the four forms of what is not held" and `wolfe:14` (SINGLE, one kept row) is "a voice quoted
inside the record may lie outright". Neither speaks to head-noun dispersion, to the sentence-law
import, or to the fence. The union of 10 rests on `dnd:43`'s six alone, which is what the MODERATE
grade should say.

**Ground 6 — the owed route check is answerable in one command and is not free.**
`settlementRumors.js` sits at `src/domain/display/settlementRumors.js` on the product branch —
display-side, as the rule guesses. But it is listed in `scripts/.clamp-primitive-baseline.json`, so
an edit is not byte-inert to the gate even if it is inert to the goldens.

**Cure.** State the union as 6 (dnd:43), carry kay:11 and wolfe:14 as context not evidence, and close
the §6 owed check with the two facts above.

### DA-16 — THE ANNEX IS THE ORIGINAL; IT IS REWRITTEN BESIDE ITS WIRED SIBLING — **NOT REFUTED**

**Confirmed against the product branch.** `tests/data/dossierStateProseProjection.contract.test.js`
states in its own header that the corpus lives in `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` and
`RECEIPT_POOLS_CAUSAL_DOSSIER.md` and is projected into `src/data/dossierStateProse/*.generated.js`
by `scripts/generate-dossier-state-prose.mjs`, with `--check` re-deriving and byte-comparing so that
a stale leaf reds. The six leaves exist (defense, economy, general, power, stressors, warFaith) and
`dossierCausalProse.generated.js` beside them. The rule's write-target ruling is exactly what the
repository enforces. `[R-5]`'s 8-versus-129 (17.2%) and the 13 truncated line-wrapped rows are
verified verbatim in PROBE_ALL's corrections table. The rule's MODERATE grade is if anything
conservative.

---

## CROSS-CUTTING VERDICTS

### S-UNION — the declared `sourceCount` method is not the method used — **REFUTED (MEDIUM)**

The preamble to §2 defines `sourceCount` as "the union of DISTINCT independent sources across the
kept rows cited". In **fifteen of sixteen** rules the printed total is the exact arithmetic **sum** of
the component counts; only DA-8 differs, and its 34 is neither the sum (39) nor a derivation shown
anywhere. Where the sum and the union genuinely diverge, the divergence is not taken: leguin:39 ⊂
leguin:24 (DA-13), kay:44 ∩ kay:24 = O'Neill (DA-13), dnd:2 ∩ dnd:1 = Merwin + Living Greyhawk
(DA-10), martin:24 ∩ martin:45 = Laura Miller (DA-14), wolfe:31 ∩ wolfe:41 = Wolfe's reply to Price
(DA-8), dnd:21 ∩ dnd:42 = D&D Beyond (DA-5), dnd:22 ∩ dnd:27 and dnd:42 ∩ dnd:14 = the Alexandrian
(DA-7, DA-5). **Cure.** Either compute the unions (they are cheap — the row lists are printed) or
rename the field `sourceSum` and say the union is lower.

### S-UNITS — the file compares per-variant rates with per-sentence rates — **REFUTED (MEDIUM)**

PROBE_ALL's rates are **per variant**; the fingerprint JSONs' rates are **per sentence**. The file
mixes them inside single comparisons and inside single rules:

| where | probe (per variant) | fingerprint (per sentence) | the file's comparison |
|---|---|---|---|
| §1.1 / DA-1 | antithesis 0.139 | estate-state 0.1088 | 0.139 against a fingerprint exemplar band 0.0082–0.0401 |
| §1.4 / DA-3 | pronoun closer 0.133 (n=2,262) | 0.1335 (n=2,914) | 0.133 with a 2,914 denominator |
| §1i | doubled adjectives 0.009 | estate-state 0.0144 | 0.009 against fingerprint 0.041 / 0.042 |
| DA-13 | R1 semicolon 0.17 | estate-state 0.1325 | 0.1325 used as the ceiling |

Every direction survives the correction; no target is shown to be wrong by it. What is wrong is that
each rule's stated baseline is not the number its named test will print. **Cure.** Print both units
in one column of the rules table, or restate every baseline in the unit of its own named test.

### S-SHIPPED-R1 — "byte-inert to every golden" is unestablished — **REFUTED (MEDIUM)**

Eleven rules (DA-1 to DA-6, DA-9 to DA-13) carry `Shipped surface: no` on the ground that R1/R2 are
display-side side-cars byte-inert to every golden. On the product branch,
`src/data/dossierStateProse/economy.generated.js` opens with the generator's own note that a
canonical row which **is** a live engine string is imported rather than inlined, and it imports
`ECONOMY_FRESHNESS_SENTENCES` from `src/domain/display/economyFreshness.js`. That module's own
comment records that re-wording there re-words them everywhere, and the two sentences are pinned by
literal assertions in `tests/components/economyFreshnessNote.test.jsx` and
`tests/lint/economyReadModelCoverage.walker.test.js`. The generator carries a pin
(`LIVE_STRING_BINDINGS` / `BOUND_LITERALS`) that fails when a binding stops binding.

So the R1 corpus is *mostly* inert, and the rows carrying bound live strings are **not**: rewriting
one is a live product-copy edit that reds two tests and can break the generator's pin. No golden
fixture was found carrying them, so the narrow words "byte-inert to every golden" may hold; the broad
claim `Shipped surface: no` does not.

**Cure.** Fence the LIVE_STRING_BINDINGS rows out of the wave by name, or mark them owner-signed
alongside DA-7 and DA-14, and restate the eleven shipped-surface lines as "no, except the canonical
rows bound to live engine strings".

### S1-MEDIAN — §1.1's "0.028 corpus median" is the field MEAN — **REFUTED (LOW)**

PROBE_ALL §5 line 810 gives `antithesis shape 0.139 (field 0.028)` as a **field mean**, in a table
whose own column heading is "field mean". PROBE_ALL §6, which is where the corpus median lives,
divides every antithesis row by **0.019** (R1 7.32×, R2 4.05×, R14 2.74×). The reconciliation
inherits best-own.md's wording verbatim and calls 0.028 the corpus median. The error is in the
conservative direction — the true outlier is 7.32×, not 4.96× — and no target moves.
**Cure.** Write "0.139 against a 0.019 corpus median (7.32×) and a 0.028 field mean".

### S4-ABSENCE — the two-tier absence ruling — **NOT REFUTED**

§4 makes the register decision the owner's ruling (4) demands and states it: a recorded null is
written as an honest absence in the clerk's third person; a slot with no field at all is omitted
silently and the omission is not announced. It is correctly grounded in ai fault 24 (15 documents,
STRONG, archival YES — verified), dnd:23's pretext rule and leguin:4's placement of the gap in the
record. Its one defect is not its own: the rate it hands to DA-6 turns the tier-1 licence into a
ration. See DA-6.

---

## WHAT THE REFUTATION DOES NOT TOUCH

The allocation in §1 is genuinely allocated and not averaged: no weights appear anywhere in the file,
the Kay 35 / Le Guin 20 / Wolfe 5 / Hobb 10 proposal is named once and never used, and each author is
attached to a technique family against a measured failure. §3's refusal list names a law for every
refusal and correctly excludes the three SINGLE-graded grammar candidates (leguin:45, kay:28,
wolfe:26b — all confirmed SINGLE in their own files) and D&D N6 under the owner's 09-07 ruling. §5
labels the two largest unsupported choices as the lane's own, and §6 declares that no figure was
re-derived. That honesty is what made this refutation cheap; it is not a substitute for the fifteen
cures above.

**No rule here is applied. No corpus text changed. Nothing is validated until the Fable chair's
retrovalidation sitting.**
