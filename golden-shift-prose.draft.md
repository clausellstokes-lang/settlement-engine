# GOLDEN SHIFT LEDGER — LIGHTING POSITION 0 "THE SRC/ PROSE CAR" (§898, 2026-09-05; SEAT: Fable 5.1 — validated)

## HEADLINE RESULT — a DECLARED same-seed shift on EMITTED TEXT; no rules value, no preset, no flag moved
The sealed prose car (`refs/preserve/srcprose-2026-09-03` = `8f4d5c648`, merge-base `30c1667bc`) was rebased onto
`df7cdd37e` (then replayed onto the ANCHORS tip `fd8b6df00`) by lane PROSE-REBASE (Opus 5) and lands as its own consist at `__CAS_SHA__`. It cures 1,001 reader sentences
across 200 paths; 113 of those paths are `src/domain/` modules outside `display/` that EMIT text, so **emitted text moves
on 94 engine-side paths** for the same seed. This entry is the declaration; the ODQ row §898 is the authority.

## Why this is a shift and not a repair of a defect
The cures are sentence-level punctuation substitutions — 28 hunks across the 7 overlapping paths, every one inside an existing sentence: em dash → period (13), → colon (10), → comma (3), two clause re-joins — and the same class across the other 193 paths: em dashes, agreement, register — the FACTS a sentence states are unchanged; the
BYTES are not. THE PROMISE is kept: a saved world's lived history is immutable; what moved is how the reader is told it.)

## The eight overlapping paths, resolved by keeping the mainline change AND the cure
| path | prose side | mainline side | resolution |
|---|---|---|---|
| `src/domain/display/defenseDisplay.js` | +4/−4 | +4/−16 | both kept; disjoint lines (PROSE-OK 8 · MAINLINE-OK 20 · eval OK) |
| `src/domain/display/dossierViewModel.js` | +5/−5 | +15/−7 | both kept; disjoint (10 · 22 · eval OK) |
| `src/domain/display/settlementRumors.js` | +1/−1 | +4/−0 | both kept; the frame pool grew, the cure moved one frame (2 · 4 · eval OK) |
| `src/domain/region/propagation.js` | +1/−1 | +7/−2 | both kept; disjoint (2 · 9 · eval OK) |
| `src/domain/rulingPowerCoup.js` | +2/−2 | +47/−0 | both kept; a pure 47-line addition beside two cured verdict sentences (4 · 45 · eval OK) |
| `src/domain/worldPulse/npcLadderKernel.js` | +6/−6 | +6/−5 | both kept; disjoint (12 · 11 · eval OK) |
| `src/domain/worldPulse/warReceiptPools.js` | +5/−5 | +13/−0 | both kept; the mainline adds rows, the cure moves five (10 · 13 · eval OK) |
| `docs/content/RECEIPT_POOLS_LEGACY.md` | +1/−1 | +81/−35 at the ANCHORS tip | the de-slugging kept wholesale, the one annex row re-applied at the replay |
| **the fix car `dac3b15a8`** | 13 structural `"<Band> — <Condition>"` separators RESTORED (prosperity 10, governanceNarrative 12, safetyProfile 2, foodGenerator 2 lines) | — | a field separator is a contract, not prose: three tiles parse it (186/360 broke) and two labels feed the persisted `economyInputFingerprint` (22/360 moved) — both at 0/360 after the fix |

## Goldens / driven-corpus constants re-recorded — every one named, with its cause
| file | assertions | before | after | cause |
|---|---|---|---|---|
| `tests/fixtures/generator-golden-master.json` (525 rows) | 525 | the base golden | **NOT re-recorded** — __GOLDEN_ROWS__ rows move (__GOLDEN_UNMOVED__ unmoved), measured by `golden-count.mjs` | the declared shift's OWN fixture; the door is closed before the freeze act (`[NO_SIGNATURE]`; walker arm 1 :358), so the arm is BANKED owner-gated with magnitude ceiling 525 until the genesis signing — car __GOLDEN_CAR__ (`RULING-GOLDEN-PRE-FREEZE.md`) |
| the edge-shared bundles (`aiCharter`, `aiGrounding`, `aiOutputSchema`) | 3 hashes | the base metas | re-minted | three bundle inputs carry cured reader text; the ritual re-mint, car `73a6f0c22` |
| `tests/property/espionageDormancyFence.test.js:262` `PRE_COUPLING_CORPUS_SHA` | 1 | `72acacd8…` | `cda5ec877…` | the cured emitted text of the corpus (anti-vacuity arms hold: rows 360, distinct 360, errors 0) — TAKEN as car `3ab6fd9d3` at residue zero |
Register state: `tests/fixtures/.golden-freeze-register.json` is UNFROZEN (`frozenAt` null); the door is CLOSED
before the freeze act for every registered surface (measured; CORRECTIONS.md row 7) — the ledger row + this entry are the
declaration, and the fixture regenerates at the genesis. Fingerprinted fixtures touched:
none — the 15 prose-side test files are not register surfaces; the fence-1 pin is a corpus sha, not a fixture (expected: none).

## Non-golden registers moved at the landing (chair acts, at the composed tip)
- voice magnitudes (`UPDATE_VOICE_BASELINE=1`): 102 entries / em 770 (budget 670, a standing RED) / bang 10 → 55 entries / em 311 / bang 8 (unchanged by the fix car: the voice scanner reads `src/data` + `src/domain` only) (a FALL — shrink-only, banked)
- writer-reach: plain `--write` — cohort unchanged, `frozenAtSha` re-taken at the composed tip (provenance only; never `--genesis`)
- test ratchet: totalTests 31491 → __TT_AFTER__, totalFiles unchanged, entries 6
- prose-numerics: 10 re-keyed snippets at 5 addresses (activeConditions.js:983, moralDrift.js:315, stressConfirmPass.js:126/:127), 0 fell, 0 new relocations, net zero rows
- lighting census: +0 (probe == register at the fixed tip: __LIGHTING_TUPLE__) (no new test file; expected +0)

## Conditions under which a FUTURE golden will legitimately shift (for the next session)
L-PROBE (POSITION 1) measures its dark-arm control AFTER this landing, so this car's text movement is never mis-attributed
to lighting. Any later same-seed text movement on these 94 paths needs its own declaration.

## The DECLARED-SHIFT sentence owed to LGT-REG-DECL (cause (0), verbatim into the eleven-declarations record)
> "1,001 reader sentences across 200 paths were cured; emitted text moves on 133 engine-side paths (94 src/domain outside display + 39 src/generators); no rules
> value, no preset and no flag moved."
