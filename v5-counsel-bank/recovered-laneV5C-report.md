# Lane V5-C — REPORT (Ribbon V5 counsel resumed under D5, 2026-08-15)

## Headline

**The V5 photoreal program is fully specified and fully recoverable: the purged SOUND
spec was reconstructed byte-faithfully from transcripts, the 13 executed 4K
generations are mapped prompt-by-prompt to the cull's KEEP/CUT/HOLD verdicts, and the
remaining work is three owner-run re-generations plus a six-wave dark build slotted
after the `da` display family.** Zero src changes; all deliverables scratchpad-only.

Deliverables (this directory, laneV5C- prefix):
- `laneV5C-V5-SPEC.md` — the consolidated spec: surviving structural layer (cited by
  section), the D5 composition/crop/shadow law, warp-as-acceptance-bounds, the
  pendant criterion as four testable clauses, the texture-budget re-ruling (vetoable).
- `laneV5C-ASSET-MANIFEST.md` — disposition of all 13 generations, three full
  re-generation prompts in the owner's own discipline (+1 contingency), the
  processing pipeline, glance-cull checklists, retina/export ladder.
- Recovered evidence: `laneV5C-recovered-v5-spec-draft.md` / `-decisions.md` /
  `-REVISION-NOTES.md` / `-prompts.json`; reference downscales `laneV5C-ref-*.jpg`.

## What changed vs the paused pass

1. **The paused pass's deliverables no longer existed on disk.** The c44e5d99
   scratchpad `v5/` and `v5-photo/` directories (SOUND spec, decisions rows 1–19,
   revision notes, four mockup PNGs, asset plan) were purged from /tmp. This lane
   reconstructed the three documents by replaying the recorded Write/Edit operations
   from the counsel and revision agent transcripts — 8 writes + 59/59 edits applied,
   zero failures — so the chair-verified SOUND text is recovered verbatim, not
   re-derived. The four mockup PNGs are gone for good; their measured receipts
   survive in the revision journal and are quoted in the spec.
2. **The asset program had already advanced past the brief's snapshot.** The brief
   described the manifest as future work; in fact the 13 nano_banana_pro 4K
   generations were EXECUTED on 2026-08-05 (prompts recovered verbatim) and a
   2026-08-06 read-only lane already delivered the cull recommendation (KEEP 5
   slots/6 files · CUT 5 · HOLD 2, with per-asset reasons). This pass therefore
   consolidates cull → manifest instead of drafting a from-scratch request list:
   only THREE re-generations remain owner-owed (mid-tone feather, dark-feather top
   pose, tabs re-shoot), plus one contingency.
3. **The pendant problem moved from open law to composition rule.** The cull found
   it solved in the single-vane evidence (`01f373de`) and narrowed the residue to
   the three-vane composite; the spec turns that into four measurable clauses
   (bound run ≥ Lb, return kiss, daylight ≤ Sg, separation pin at crossings).
4. **Two open sign-offs gained evidence-backed amendments**: OQ1 now proposes a
   two-value gold ladder (#D4AF45 cartouche / #E9CD70 tabs — `54727e25`'s value
   finding); OQ2 now requires the seal's middle rung to be a measured downscale of
   the one wax master (`a396fc06`).
5. **A model-pin discrepancy was resolved by receipts**: the cull lane reported the
   library batch as `nano_banana_2`; the executed calls (recovered verbatim) all say
   `nano_banana_pro` @ 4k. The manifest trusts the executed calls.

## The build-wave plan (v5-1..v5-6, slotted after the `da` display family)

Slotting: after da-1..da-6 (the display-adoption family, sibling lane TC-21's
deliverable set) lands — V5 touches the same header/display surface and the
concurrency law (TWO lanes/ONE landing) forbids overlap. All waves DARK until the
owner's glance (§27 build-everything law). One commit per wave. **No new test
files** — the test census sits at its ceiling and a new file reds two censuses;
every proof extends the four landed design-test suites (contrast / organicLogo /
brandLockup / navFletching), checking the lighting census's exact-title pin before
any new title.

| wave | contents | proof shape |
|------|----------|-------------|
| v5-1 | Asset intake: owner runs R1–R3, glance-culls; processing per manifest §3; assets + size-manifest land (no component changes) | spelling-gate receipts; A6/A7 glyph-overlay check; light-direction glance; per-asset manifest rows (sha256, bytes, tone stats, crop-line, exact text) |
| v5-2 | The ground: A1 shaft strip composited behind the existing header, dark (unreferenced component) | navFletching: absolute/inset-0/zero-intrinsic-height pins on every photo layer (the viewBox-law generalization); ANCHOR_OFFSET derivation pins re-run; two-load byte determinism; contrast.test.js re-census v1 on recorded tone stats + negative-control list EXTENDED |
| v5-3 | Cartouche + seal: A2 band seated, A5 rung ladder wired | brandLockup: manifest text equivalence + COPY_ONLY; organicLogo census admission; seam ΔL receipt (A2↔A1); seal register selection on CSS px |
| v5-4 | Vanes + two-state: six sprites, controller (40/8 hysteresis, 240ms crossfade, reduced-motion swap, loaded-gate) | pendant clauses 1–3 as sprite-geometry pins (navFletching); separation pin ≥1.15:1 both states on composited screenshots; hit-target + focus-law pins; no-active-paint-below-vane pin |
| v5-5 | Tabs + states + rider close-out: R3 band, post-derived active/hover, drawn quill-line; drawn-texture layers retired | HEADER_RIDERS full re-census (ground-move law — fourth move of this class); contrast floors per spec §5.2; byte-budget exact ceilings recorded |
| v5-6 | Mobile + breakpoints: mobile composition, THE OWED 640 RECEIPT (the 640 composition is decided here, at build, per F9), full three-width pass | breakpoint law at 1440/640/390; mobile hang caps 24/8; final two-state owner glance at all widths |

## Open questions for the owner (visual calls only they can make)

- **OQ1 (carried, amended)** — sign the branded-then-gilded lettering grammar WITH
  the two-value gold ladder: #D4AF45 in the char-framed cartouche, #E9CD70 for the
  burned tabs. Evidence: 8.5–11.5:1 measured gold-on-bed; `54727e25`'s small-size
  value finding.
- **OQ2 (carried, amended)** — sign the three-rung seal ladder (dimple <22 / strike
  22–40 / device ≥40) with the middle rung as a measured downscale of `a396fc06`.
- **OQ3 (new)** — the isolated triangle-in-circle at large registers sits close to
  the Alcoholics Anonymous mark (cull observation). Embedded in the O it reads as
  estate work; the standalone large-register rendition is the owner's call before
  any public surface uses it.
- **OQ4 (new)** — glance the three-vane composite at v5-4: the pendant failure only
  ever appeared at three vanes; every solved image is one vane. The four clauses
  gate it mechanically; the owner's eye is the final authority.
- **OQ5 (new)** — Sign In stays the existing drawn gold button (the generated brass
  plate was cut as the retired maker's plate reborn). Confirm, or request a subdued
  re-shoot (patinated, smaller, no mounting pins).
- **OQ6 (new)** — the label rest/active pair comes from two separate generations;
  if the glyph-overlay check shows visible drift, the active variant is derived
  from the rest master by gold remap instead. Accept that fallback order?

## Judgments (J-V5C-n, all vetoable)

- **J-V5C-1** — Recovered the purged deliverables by transcript replay rather than
  re-deriving from directives: the SOUND text carries chair-verified receipts that a
  re-derivation would orphan. Risk: replay fidelity — mitigated by 59/59 edit
  anchors matching exactly (a drifted document would have failed edits).
- **J-V5C-2** — Model pin resolved by executed calls over the cull lane's library
  metadata read: `nano_banana_pro` @ 4k stays pinned.
- **J-V5C-3** — Sign In remains a drawn object (recovered decisions row 15 +
  `e2cf5c2a`'s failure); no photo asset requested.
- **J-V5C-4** — Post-mirroring photo assets is FORBIDDEN: a horizontal flip
  reverses the baked upper-left light, breaking the one-light law. The dark
  feather's wrong-way top pose gets a re-generation (R2), not a flip.
- **J-V5C-5** — State variants whose glyph geometry must not jiggle (tabs, and
  labels if OQ6's check fails) are derived in post from ONE master; generation-time
  variant pairs are used only where one sheet delivered both poses (the feathers —
  the owner's two-pose-one-sheet discipline is what makes the crossfade morph-free).
- **J-V5C-6** — The warp constants (WARP_RATIO/FORESHORT) retire as authored
  transforms and re-enter as intake acceptance bounds (bow > 0 and ≤ 0.021; the
  measured counter-crush point); no perspective "fixes" in post.
- **J-V5C-7** — The texture-budget re-ruling (spec §5): byte budget with exact
  per-asset ceilings replaces the ink ratchet; the contrast law survives via
  recorded tone stats per asset region; determinism becomes asset byte identity +
  the existing two-load pin. The program ceilings (220KB/550KB) are proposals.
- **J-V5C-8** — The pendant criterion binds at composite as four clauses (bound
  run, return kiss, daylight bound, separation pin), placement-geometry-testable.
- **J-V5C-9** — Label sprites are matted as paint strokes, never rectangular
  patches (a patch's own feather texture seams on a different feather photo).
- **J-V5C-10** — Wave slotting after da-1..da-6, proofs confined to existing suites
  (census ceiling + exact-title lighting census respected).

## Not done, deliberately

- No src changes, no commits to either tree (charter). No new generations executed —
  the owner drives Higgsfield; R1–R3 are written ready-to-run.
- No composite mockup rendered this pass: the D5 composite IS the build's first
  receipt (v5-2..v5-4), and the drawn-geometry mockups it would supersede are gone;
  their measured receipts are preserved in the recovered documents instead.
- The 640 receipt remains owed at build (F9 disposition carried; the 640
  composition is decided at v5-6, not by counsel).
- The `da` family slotting is by the parent chair's designation, verified against
  lane TC-21's deliverables (da-1..da-6 exist there); the family is not yet a row in
  the build branch's packet INDEX — the slot is a statement about lane order, not a
  queue row this lane could cite.
- The two Fable-era sign-offs (OQ1/OQ2) remain OPEN and are restated above in their
  amended form; per the exhaustion-boundary law this lane's judgments carry
  ⏳ FABLE VALIDATION OWED where they are utilized.
