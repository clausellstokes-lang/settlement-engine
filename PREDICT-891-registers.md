# PREDICTIONS — the §891 landing's register acts, written BEFORE any instrument runs
Chair: Opus 5 seat (session d5b9a39f), 2026-09-04 ~00:5x ET. Dock `450f7dbb7`, 19 cars over `ca651d54b`, porcelain 0.
Base register state, read at the dock tip: lighting `2515/371/2144/22919/6152` (measuredAtSha `3d3ad217b`) · ratchet `totalTests 31141 / totalFiles 2458 / entries 10` (measuredAtSha `69bd44f65`, a §890 commit) · mounts `UNMOUNTED_BLOCKS.blocks 68`.

## 1. LIGHTING CENSUS REFREEZE — CERTAIN on four figures, one measured
- `files` **2515 → 2515** CERTAIN. O2GATE added NO new test file; it extended `tests/ui/economicsTabFlow.test.js`.
- `parked` **371 → 371** CERTAIN · `credited` **2144 → 2144** CERTAIN (closure identity 2144 + 371 = 2515 holds).
- `titles` **22919 → 22922** CERTAIN (+3). O2GATE measured the walker saying `expected 22922 to be 22919`.
- `suiteTitles` **6152 → 6153** CERTAIN (+1). Its diff adds exactly one `describe` and three `test`.
⚠ The walker asserts `titles` BEFORE `suiteTitles` and throws on the first mismatch, so the second figure is invisible to a single run. Both are predicted here because a first-mismatch throw is a LOWER BOUND, never the whole drift (the §892 law).

## 2. MOUNTS DARKNESS BASELINE — CERTAIN
- `UNMOUNTED_BLOCKS.blocks` **68 → 64** CERTAIN. DESK CAR 1 mounted four blocks; the shrink-only baseline still records 68, so the ratchet is SLACK by four and a later car could re-park four blocks unseen. Door: `UPDATE_MOUNT_BASELINE=1`.

## 3. VOICE BASELINE — PREDICTED TO **REFUSE**, and that is the point of running it
- The four arms are red at the tip (E2 per-file 69 rows · E2 total 770 > 670 · JSX per-file 17 rows · JSX total 40 > 6) and all four are among the ten BANKED census entries by `file :: test` identity, so the gate passes either way.
- ⛔ **PREDICTION: `UPDATE_VOICE_BASELINE=1` REFUSES.** The JSX drift is a MOVE, not a fall: DESK CAR 1's extraction took `EconomicsTab em 3 → 0` and gave `EconomicsGlance em 0 → 3`. A shrink-only per-file ratchet reads the destination as GROWTH and must refuse. If it refuses, the act is NOT taken and the two banked slots stay spent; the alternative (curing the three component em dashes) is a cure car, not a landing act, and is docketed rather than smuggled in.
- If it SUCCEEDS instead, the prediction was wrong and the win is banked — and the refusal mechanism owes an explanation.

## 4. TEST RATCHET `--update` — one CERTAIN, one refused in advance
- `totalFiles` **2458 → 2462** CERTAIN (+4). The train adds four new test files; O2GATE adds none.
- `totalTests` **REFUSED IN ADVANCE** — a title and a test are different units and the count is not derivable from the diff. Measured, never guessed.
- `entries` **10, removed 0** PREDICTED — unless act 3 succeeds, in which case up to two voiceMechanics entries retire and the census gains headroom.
