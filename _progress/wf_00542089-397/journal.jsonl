{"type":"launched"}
{"type":"started","key":"v2:947d48a9daaef0fe226825bbca514986cecb37f2d366043eb516b4d4c170951c","agentId":"a0745eb265497e2f3","label":"read:entitlement-and-billing-code","phase":"Read"}
{"type":"started","key":"v2:3d1aff2e32ea8c35a1a7c8827f4cf28896a6cd67daa3c11237ca01fd64d7c3cf","agentId":"a534045ec29905f4d","label":"read:design-rulings-directives","phase":"Read"}
{"type":"result","key":"v2:3d1aff2e32ea8c35a1a7c8827f4cf28896a6cd67daa3c11237ca01fd64d7c3cf","agentId":"a534045ec29905f4d","result":{"summary":"The record holds ONE owner sentence that opens a Scribe subscription tier — 2026-09-14 ~13:2x, \"Because I'm thinking that this could actually be a new paid subscription tier in and of itself\" — and it is a THOUGHT, not a signature. Everything else about the Scribe's economics is the chair's own, recorded vetoably under the 09-13 total-delegation grant, and every one of it is INERT: `dossierProse: 5` credits sits in `pricing.js` behind `FLAGS.scribe = false`, migration 202 is written and NOT applied, and the owner's five acts (the key · `db push` 201+202 · the tier's price and shape · item 13 · the flag) are all outstanding. The design's §12 owner-gated list runs 1–15; §11c points at \"§12 item 16, the tier\" — AN ITEM THAT WAS NEVER WRITTEN. There is no tier proposal anywhere in the record: no price, no entitlement shape, no render allowance, no cap. Three facts should drive the proposal. (1) The 13:2x amendment removed the Scribe from the free AND paid subscription product entirely — the corpus is every subscription's prose until the rewrite is finished — so the tier is additive, not a repricing. (2) The chair's 5-credit default is worth $0.65–$1.00 at shipped retail pack rates, and my arithmetic over the MEASURED RUN-5 prompt puts one epoch render at roughly $0.73–$1.32 of Opus 5 COGS excluding adaptive-thinking output — i.e. the per-render SKU is at or below cost at the best pack rate. (3) The design's own cost paragraph (§6: \"a few tens of thousands of input tokens per settlement … cents per settlement on Opus 5, not dollars\") PREDATES the prompt that now ships: the measured cached prefix alone is ~28 K tokens read on every one of ~10 tab calls, ~390 K input tokens per settlement for the writer alone and the same again for the tier-1 reader. That paragraph is the single largest unmeasured part of the design, and the design says so itself (\"to be MEASURED in the pilot, not trusted\"). Also: the \"daily render cap (three saves, one render a day)\" that the 08:5x memory calls a temporary economics EXISTS NOWHERE ELSE — not in the design, not in the rulings, not in code; `saveLimit: 3` is the Wanderer tier's save cap, and the chair appears to have read a render cap into it. The closest precedent for the tier's shape is SURVEYOR_PLAN ($14.99/mo, `monthlyCredits: 0` as a dial, BYOK, an ENTITLEMENT rather than a `TIERS` key) — deliberately outside `TIERS` so `getVisibleTiers()` stays three-way.","facts":[{"topic":"Owner directive — the tier (the ONLY statement that opens it)","fact":"VERBATIM, 2026-09-14 ~13:2x, AMENDING the 08:5x frame: \"okay then with that, i ammend what i said before. the scribe is the AI narrative layer/overlay. Free and paid subscription generation uses the full corpus only until the full rewrite is finished. I still want you to continnue the scribe work to completion however. Because I'm thinking that this could actually be a new paid subscription tier in and of itself\". Note the modality — \"I'm thinking that this COULD actually be\". It licenses the proposal; it signs nothing.","where":"/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/owner-directive-2026-09-14-the-scribe-is-the-ai-narrative-layer-and-a-tier-of-its-own-the-corpus-is-every-subscriptions-prose.md"},{"topic":"Owner directive — what 13:2x SUPERSEDED","fact":"The memory records the amendment as striking, explicitly: the 08:5x frame (Scribe as the intermediate for cash flow), \"the Scribe as the DEFAULT render for the product, the once-a-day render cap as temporary economics, the per-render credit SKU as the Scribe's price, the free first render (chair ruling 2). Those are now design inputs for a SUBSCRIPTION TIER, not the free/paid product's defaults.\" So ruling 1's 5-credit SKU and ruling 2's free first render are NO LONGER the product's defaults — they are raw material for the tier design and must be re-decided inside it.","where":"same file, \"What it supersedes\" paragraph"},{"topic":"Owner directive — how 13:2x says to apply it","fact":"VERBATIM from the same memory's How-to-apply: \"`FLAGS.scribe` stays false for every tier until the tier exists; no Scribe render is ever the default for free or paid subscribers; W4/W5 design the Scribe as a SUBSCRIPTION entitlement (an account tier gate at `scribe-render`, replacing or wrapping the per-render credit spend) — a paid-surface design that is the owner's to sign; the daily cap and the free first render are re-decided inside that design.\" The word ENTITLEMENT is the chair's read, and it is the same word the shipped Surveyor plan uses.","where":"same file"},{"topic":"Owner directive — the end state (08:5x, still standing)","fact":"VERBATIM 2026-09-14 ~08:5x: \"Quite the opposite. The corpus prose is the hopeful final version because then it unlocks every free version to truly be free. The AI is the intermediate to get something workable and to get cash flow so I can keep working on the prose.\" And, same hour: \"when the corpus is done, the AI is going to move to a higher tier for payment.\" The 13:2x amendment struck the INTERMEDIATE half; this second sentence — the Scribe as a higher paid tier once the corpus lands — is what 13:2x brought forward and made immediate rather than eventual.","where":"/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/owner-directive-2026-09-14-the-scribe-is-the-critical-path-the-corpus-is-what-exists-until-then.md"},{"topic":"Owner directive — the chair's own gloss on the end state","fact":"The chair recorded (not the owner's words): \"So W5's paid surface is designed as a TIER feature from the start (a Surveyor-like entitlement with renders bundled), with per-credit metering only as the intermediate's bridge; the price row and the tier are owner-signed.\" This is the only place in the record where a tier SHAPE is named, and it is the chair's proposal, dated 09-14 ~08:5x, written before the 13:2x amendment.","where":"same file, THE END STATE paragraph"},{"topic":"THE DAILY CAP DOES NOT EXIST","fact":"The sentence \"The free tier's daily render cap (three saves, one render a day) is a TEMPORARY economics of the intermediate\" appears in exactly ONE place in the whole record — that memory file. It is absent from the design doc, absent from SCRIBE-RULINGS (all 36), and absent from the code: greps for daily cap / renders per day / DAILY_LIMIT across src/ and supabase/functions/scribe-render return only the ANONYMOUS GENERATION cap (HomeHero.jsx:442 `DEFAULT_DAILY_CAP`) and the BYOK key caps. \"Three saves\" is `TIERS.wanderer.saveLimit: 3`. No Scribe render cap was ever designed, ruled or built; the chair appears to have narrated one from the save limit.","where":"memory owner-directive-…-the-scribe-is-the-critical-path…md:14; vs. lane-scribe/src/config/pricing.js (TIERS.wanderer.saveLimit) and lane-scribe/src/components/HomeHero.jsx:27,442"},{"topic":"Owner directive — the billing unit (the render trigger)","fact":"VERBATIM 2026-09-14 ~06:3x: \"but generation happens only once a settlement's dossier is opened and frozen until next advance time.\" Read into the design as: nothing renders on save or on advance by itself; an advance only makes the artefact stale; the next OPEN of that settlement's dossier renders; \"A settlement never opened in an epoch is never rendered and never billed — in a realm of thirty towns advanced a year, only the ones the game master looks at are scribed.\" This is the load-bearing economics of any tier: the billable unit is opened-settlement × epoch, not settlement and not advance.","where":"ruling 14, SCRIBE-RULINGS.md; design §5 CREATE and §5b THE TRIGGER IS THE OPEN"},{"topic":"Owner directive — the advance rule (what makes a render recur)","fact":"VERBATIM 2026-09-14 ~06:1x: \"new rule. when advance time happens, the program reads teh new town card, the history of town cards, and what happened last. or however we have it now. This is essentially the narrative overlay set as default and without prose already constructed for it to refine. so think of it like that in terms of how it reads past settlements data to make things remain coherent.\" Consequence for the tier: the artefact is write-once PER EPOCH, so a campaign advanced twelve times over a year bills up to twelve renders per settlement the DM actually opens. Design §5b: \"The pilot (§10) measures a 12-advance campaign end to end, not only epoch 0.\"","where":"ruling 13, SCRIBE-RULINGS.md; design §5b including its COST bullet"},{"topic":"Owner directive — what else is billed (REDO)","fact":"VERBATIM 2026-09-14 ~06:5x, among four product rules: \"and there should be an option to redo the AI narrative, to redraw based on the current settlement's facts. lastly, remove the Narrative overlay option because it is now redundant. But do key in the AI instructions box in the dossier to be additional instructions that is actually read and used by the AI for the prose for the dossier. And lastly, with this, it is automatially default that the daily life tab be populated rather than on command. let's just get it all out there.\" Ruling 16 reads REDO as \"billed per render\"; ruling 18 makes editing the AI-instructions box REDO-ELIGIBLE but never an automatic render. So a DM who edits guidance and redraws pays again — a second billable event the tier design must price or absorb.","where":"rulings 16–19, SCRIBE-RULINGS.md; design §5c rules 1 and 3"},{"topic":"Owner directive — the retirements that fund the SKU","fact":"The same 06:5x order retires the narrative overlay AND (ruling 19) daily life's on-command button: \"DAILY LIFE renders by default as the seventh tab call of every epoch render … its on-command button and SKU retire. Ruling 1 AMENDED: one render SKU, chair's default 5 credits (the retired narrative's), owner-signed.\" So the Scribe's SKU is priced AT the SKU it replaces, and two existing SKUs (`narrative: 5`, `dailyLife: 4`) go away — revenue the tier must replace, not add to.","where":"ruling 19, SCRIBE-RULINGS.md; design §5c rules 2 and 4, and the \"Owner-gated item 14 (recorded)\" note after §5c"},{"topic":"Chair ruling 1 (as amended by 19) — THE PRICE","fact":"Ruling 1 first set `dossierProse: 4` credits \"between narrative 5 and dailyLife 4\"; ruling 19 amended it to \"one render SKU, chair's default 5 credits (the retired narrative's), owner-signed.\" Rationale on the record: \"priced like the feature it most resembles; the pilot's measured COGS re-prices it before launch.\" `TIER_MULTIPLIERS` left untouched because \"all-1 is the owner-signed activation switch.\" The re-pricing the ruling promises has never happened — the pilot has never run.","where":"rulings 1 and 19, SCRIBE-RULINGS.md"},{"topic":"The price row AS BUILT","fact":"`NEW_AI_COSTS` carries `dossierProse: 5` with the comment: \"⭐ THE SCRIBE'S ONE RENDER SKU (chair ruling 1 as amended by ruling 19; migration 202's `spend_credits` arm is the server mirror). ONE PRICE FOR A WHOLE RENDER of a settlement's dossier — every tab AND the daily-life beats, per epoch — set at the retired narrative's own five credits. INERT: nothing spends this feature until FLAGS.scribe is lit, which is an owner act.\" `FAST_AI_COSTS` carries the same 5 deliberately, \"so `getAiCostForModel('dossierProse', anyPreference)` answers the true price instead of `undefined`\" — there is no cheaper model tier to price because ruling 8 fixed one model.","where":"/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-scribe/src/config/pricing.js:64-86"},{"topic":"What a credit is worth (the retail anchor for the SKU)","fact":"Shipped `NEW_PACKS`: credits_25 $4.99 → $0.20/credit; credits_60 $9.99 → $0.17/credit; credits_150 $19.99 → $0.13/credit. A 5-credit render therefore retails at $1.00 / $0.85 / $0.65 depending on which pack the user bought. This is the ceiling any per-render tier economics has to clear.","where":"lane-scribe/src/config/pricing.js:48-52 (NEW_PACKS)"},{"topic":"The shipped subscription ladder (what a Scribe tier would sit beside)","fact":"`TIERS`: wanderer — $0/forever, `saveLimit: 3`, no pdf/json export, `maxSize: 'capital'`. cartographer — `priceCents: 599` ($5.99/mo, \"reconciled to the displayed price (owner sign-off 2026-07-17)\"), `monthlyCredits: 30`, unlimited saves, all exports. founder — lifetime, NO SKU (\"A chair is given, never sold\"), `oneTimeCredits: 30`, seatLimit 30. Plus `SINGLE_DOSSIER` at `priceCents: 299` ($2.99, claimable without signup). At 5 credits a render, Cartographer's 30 monthly credits buy SIX renders a month.","where":"lane-scribe/src/config/pricing.js:246-320 (TIERS) and :322-330 (SINGLE_DOSSIER)"},{"topic":"THE PRECEDENT for a Scribe tier — SURVEYOR_PLAN","fact":"`SURVEYOR_PLAN`: `priceCents: 1499` ($14.99/mo, \"§464.2 ruled the band $14.99–19.99 at its floor\"), `billing: 'monthly'`, `monthlyCredits: 0`, `byok: true`, `entitlement: 'surveyor_entitlements'`. Its header is the architectural ruling a Scribe tier should copy or consciously break: \"NOT a `TIERS` key, deliberately. `TIERS` keys are `profiles.tier` shapes, and Surveyor is an ENTITLEMENT (`surveyor_entitlements`, migration 139) rather than a billing tier — the same reason 139 declined to widen the tier CHECK. Keeping it outside `TIERS` is what lets `getVisibleTiers()` stay three-way and leaves every existing TIERS consumer untouched.\" Its `monthlyCredits: 0` is documented as \"THE DIAL — WEB-10 sets it when the webhook can grant\", i.e. the allowance is minted by the Stripe webhook, which does not know about the constant yet. A Scribe tier with bundled renders inherits exactly that unbuilt granting path.","where":"lane-scribe/src/config/pricing.js:332-361 (SURVEYOR_PLAN, with its full docblock)"},{"topic":"The activation switch is owner-signed","fact":"`TIER_MULTIPLIERS` is `{scout: 1, journeyman: 1, master: 1}` with the comment: \"⚠ EVERY VALUE HERE IS 1 ON PURPOSE. This map is the ACTIVATION SWITCH for the quoted price, and pricing activation is owner-signed (design §5; owner queue M5, the pricing-sheet sign-off) … Changing a number here without the owner's signature silently reprices a paid surface.\" Any tier proposal that scales price by capability tier touches this map and therefore needs the owner's pricing-sheet signature, not just a tier sign-off.","where":"lane-scribe/src/config/pricing.js:130-158"},{"topic":"Chair ruling 2 — THE FREE FIRST RENDER","fact":"\"FREE FIRST RENDER — YES, one per ACCOUNT on the migration-118 pattern (`claim_free_scribe`), released on failure. RATIONALE: the same product logic as the free first narrative; unfarmable by construction.\" Built into migration 202 as a conditional UPDATE whose `found` is the answer. The 13:2x amendment explicitly puts this back on the table: \"the daily cap and the free first render are re-decided inside that design.\" So ruling 2 is BUILT but no longer decided.","where":"ruling 2, SCRIBE-RULINGS.md; lane-scribe/supabase/migrations/202_scribe_claim.sql §1"},{"topic":"Migration 202 — written, NOT applied","fact":"Header verbatim: \"⚠️ INERT UNTIL TWO SEPARATE ACTS. Applying this migration changes no existing behaviour: the claim functions have no caller until the Scribe's flag is lit, and the `dossierProse` arm is reachable only from a feature name nothing spends today. `FLAGS.scribe` defaults to FALSE and lighting it is the owner's act; so is `supabase db push`.\" The SKU arm is added by recreating `spend_credits` from migration 192 VERBATIM with one line — `when 'dossierProse' then 5` — because \"`spend_credits` RAISES on an unknown feature, by design, so a new SKU is a new arm or it is a failed spend.\" Rollback documented: recreate from the 192 body and drop the three claim functions, \"Safe at any time while the Scribe's flag is dark.\"","where":"lane-scribe/supabase/migrations/202_scribe_claim.sql:1-38 (and 201_scribe_prose_dm_full_strip.sql beside it)"},{"topic":"The flag","fact":"`scribe: false` in the flag registry. Its description: \"THE SCRIBE: AI-written dossier prose, rendered once per epoch when a settlement's dossier is opened, refuted by the tier-0 instruments, and persisted on the settlement. Default OFF and INERT: with the flag off nothing renders, nothing is billed, no Scribe module enters the first-paint closure, and every tab draws the hand corpus. Lighting it opens a PAID surface, so it needs the render SKU seeded and the provider key in place first.\"","where":"lane-scribe/src/lib/flagRegistry.js:137 and lane-scribe/src/lib/flags.js:88"},{"topic":"Chair ruling 10 — BYOK","fact":"\"BYOK — a BYOK user's Scribe runs on THEIR key at THEIR model's probe tier (the ladder's no-flattening rule), the tier-0 refuter unchanged, the tier-1 refuter on the same model as their writer (the conflicted-witness rule).\" Design §8 adds the retention consequence: \"the managed path uses Opus 5; BYOK users' model choice inherits their own org's retention — recorded, not decided.\" For a tier proposal BYOK is the natural zero-COGS arm — and it is the one Surveyor already sells at $14.99/mo with `byok: true` and a zero credit allowance.","where":"ruling 10, SCRIBE-RULINGS.md; design §8; SURVEYOR_PLAN byok:true in pricing.js"},{"topic":"Chair ruling 8 — MODEL (a cost input)","fact":"\"MODEL — `claude-opus-5` (the API skill's default) for the writer and the tier-1 refuter on the managed path; adaptive thinking, `effort: 'high'`, structured outputs, streaming, server-side `fallbacks: \\\"default\\\"`. A Fable selector arm runs in the PILOT only (design §6) and is adopted only if the blind read prefers it. The `opus-4-8` defaults elsewhere are NOT moved by the Scribe.\" Note the tension with the shipped `DEFAULT_MODEL_PREFERENCE = 'anthropic_claude_opus_4_8'`: the Scribe runs a model the product's own model-preference UI does not list, so per-user model choice does not reduce its COGS.","where":"ruling 8, SCRIBE-RULINGS.md; design §6 Model paragraph; lane-scribe/src/config/pricing.js:160 (DEFAULT_MODEL_PREFERENCE) and :162-227 (AI_MODEL_OPTIONS)"},{"topic":"Model choice, measured (the cheaper-tier question is closed for now)","fact":"RUN 5: Opus 87 % of pools shipped vs Sonnet 84 %; contradicting lines Opus 6 vs Sonnet 6. RUN 4: Opus 89 / Sonnet 85, contradictions 2 vs 8, FAILs 10 vs 14. RUN 3's ruling 4: \"Opus stays the writer; a per-tab Sonnet seat is not supported by this run. Re-open with live usage receipts.\" So the obvious COGS lever — a cheaper writer — was measured and declined on quality, with re-opening explicitly conditioned on live receipts the programme does not have.","where":"SCRIBE-SIM-2026-09-14.md, RUN 3 §4, RUN 4 Model line, RUN 5 table"},{"topic":"Chair ruling 3 — the public projection (a distribution, not a price, decision)","fact":"\"THE PUBLIC PROJECTION — NOT SHOWN to gallery viewers and NOT copied on gallery import in W2 (absent from `PUBLIC_TOPLEVEL_KEYS`; migration 196 adds the claim RPC only). RATIONALE: fail-closed is the estate's rule; showing paid, unrefuted-by-a-human prose on the public gallery is a reversible LATER decision, hiding it is the safe first state. Re-open at W5.\" A tier that bundles renders should decide whether a subscriber's rendered prose is visible to non-subscribers in the gallery — the re-open is scheduled for exactly the wave the tier lands in.","where":"ruling 3, SCRIBE-RULINGS.md; design §5 THE PUBLIC PROJECTION / SQL TWIN and §12 item 3"},{"topic":"Chair ruling 7 — re-scribe (a recurring-revenue lever, default OFF)","fact":"\"RE-SCRIBE — the growth opt-in's shape exactly: a campaign-level, versioned, explicit 're-scribe this world under Scribe vN' that re-renders EVERY block (never a partial mix) and keeps the prior artefact in `versionHistory` as one entry … Default OFF. Nothing re-renders on its own.\" Under THE PROMISE a world opened under a newer scribeVersion does NOT re-render; only an advance renders a new epoch. So there is no version-churn revenue and no version-churn cost without an explicit campaign opt-in.","where":"ruling 7, SCRIBE-RULINGS.md; design §7 second bullet; §12 item 7"},{"topic":"Chair ruling 5 — the residual error rate (the tier's quality promise)","fact":"\"THE RESIDUAL ERROR RATE — the pilot's gate (design §10): ship behind the flag only at or below the corpus's audited defect rate; WITHHELDs ship (the corpus's own rule); FAILs never ship (the unit falls back to the corpus). No number is set by hand.\" Design §10's proposed decision rule: ships \"if its post-refute FAIL rate is at or below the hand corpus's own audited defect rate … AND the blind read prefers it or ties\" — and the blind read is specified as \"a 40-unit blind read: the owner and one Fable seat.\" The owner has never sat that read.","where":"ruling 5, SCRIBE-RULINGS.md; design §10 including its Decision rule"},{"topic":"Owner standard — what the tier actually sells","fact":"VERBATIM 2026-09-14 ~14:4x: \"okay, to clarify again, the goal is that it doesn't contradict the settlement not that it invents when it comes to prose.\" Ruling 36 makes this law for both seats and SUPERSEDES ruling 35's six invention bars. This is why the measured ship rate moved from 26 % to 84 %+ in one afternoon — RUN 2's \"70 % refused\" was a licence reading, not the product.","where":"ruling 36, SCRIBE-RULINGS.md; memory owner-directive-2026-09-14-the-scribe-is-held-to-non-contradiction-not-non-invention.md"},{"topic":"MEASURED — the shipped share (RUN 5, the current ceiling)","fact":"RUN 5 (workflow `wf_00f13b26-496`, dock `f350d7ed5`, 64 seats, 8.9 M tokens, 16 cells = hamlet/village/town/city × defense/economics/overview/power): Opus 198 pools → 188 returned → 178 tier-0 kept → 5 tier-1 dropped → **173 shipped (87 %)**; Sonnet 188 → 172 → 6 → **166 (84 %)**. Contradiction LINES: Opus 6 (field 3 · page 3), Sonnet 6 (field 4 · page 2) — 12 in ~1,400; roster 0, model 0, record 0, forecast 0. The chair's 20:3x ruling: \"The Scribe without a key is at its natural ceiling: 84–89 % of pools ship as the Scribe's, ~0.5–0.7 % of lines contradict.\"","where":"SCRIBE-SIM-2026-09-14.md RUN 5 section; raw rows /private/tmp/…/kit/scribe-harness/out/sim/RUN-wf_00f13b26-496.json"},{"topic":"MEASURED — the arc that produced it","fact":"Over one afternoon of one 16-cell grid: RUN 2 26 % (Opus) / 30 % (Sonnet) under the licence reading → RUN 3 84 % / 78 % (the owner's non-contradiction standard + the honest card) → RUN 4 89 % / 85 % (the page's machine lines in the writer's turn, `town.bodies`) → RUN 5 87 % / 84 % (the card's reads resolved). RUN 5 is explicitly read as \"flat against RUN 4 within the writer's own draw-to-draw variance (the craft withholds swing ±20 between runs of one prompt: Q on Opus 36 → 22 → 41 across RUNS 3–5)\" — so 84–89 % is a band, not a point.","where":"SCRIBE-SIM-2026-09-14.md, RUN 2/3/4/5 tables"},{"topic":"MEASURED — the prompt, cached global brief","fact":"`brief.md` = **96,795 chars**, BYTE-IDENTICAL across all four seeds (hamlet/village/town/city) and all twelve non-daily-life tabs → ≈24.2 K tokens at ~4.0 chars/token. This is cached block 1, shared by every settlement of the hour. The daily-life tab's brief is **98,480 chars** (the daily-life register section) — a DIFFERENT prefix, therefore a SECOND cache write per hour, which the design's one-cached-brief economics does not account for.","where":"/private/tmp/…/kit/scribe-harness/out/sim/{render-town,sim-hamlet,sim-village,sim-city}/*/brief.md (RUN 5 build, 2026-09-14 18:45; daily_life built 20:32 at W4)"},{"topic":"MEASURED — the prompt, cached town block","fact":"`town.md` (cached block 2, shared across one settlement's tabs, constant per seed): hamlet (Warmholz) **6,568** · village (Hochhausen) **10,089** · town (Spitzplatz) **15,964** · city (Rundgate) **16,236** chars → ≈1.6 / 2.5 / 4.0 / 4.1 K tokens. The cached prefix a town-tier tab call carries is therefore ≈28.2 K tokens (brief + town).","where":"/private/tmp/…/kit/scribe-harness/out/sim/<seed>/<tab>/town.md"},{"topic":"MEASURED — the prompt, volatile turn, per tab","fact":"Spitzplatz (town), all 13 tabs, `turn.md` chars: defense 25,695 · overview 20,772 · power 9,963 · plot_hooks 9,314 · economics 8,420 · history 7,079 · daily_life 5,023 · viability 4,644 · services 2,287 · faith 1,676 · relationships 402 · resources 398 · war 392. **Total 96,065 chars ≈ 24.0 K tokens per settlement.** Across tiers the big four tabs scale: hamlet defense 28,016 / overview 23,380; city defense 30,947 / overview 17,970 — a hamlet's turn is NOT smaller than a city's, because the turn carries the page and the pools, not the town's size.","where":"/private/tmp/…/kit/scribe-harness/out/sim/<seed>/<tab>/turn.md; output schema is 740 chars (≈185 tokens) on every tab"},{"topic":"MEASURED — the prompt, tier-1 checklist","fact":"`tier1-opus.md` chars: defense **36,710** · overview **35,811** · power **26,139** · economics **25,569** = 124,229 chars ≈ 31.1 K tokens over four tabs. NOTE A FIDELITY GAP: the harness's tier-1 prompt is SELF-CONTAINED — it re-embeds \"THE TOWN: {armedForces: …}\" and the two-ladders / funding-note law inline. Ruling 31 and RUN 3's finding 5 say that in the PRODUCT \"the checklist rides under the same cached brief\", so the product's tier-1 input is mostly a cache read while the harness's is full-price. Which of the two the built `scribe-render` actually sends has never been measured against the API.","where":"/private/tmp/…/kit/scribe-harness/out/sim/render-town/{defense,overview,power,economics}/tier1-opus.md; ruling 31, SCRIBE-RULINGS.md; SCRIBE-SIM RUN 3 §5"},{"topic":"MEASURED — how many calls a render actually makes","fact":"The built transport calls `firingTabs(settlement, renderTabPage, SCRIBE_TABS)` and keeps only tabs whose page has a `kind === 'composed'` line: `const tabs = firingTabs(...); if (tabs.length === 0) return { ok: false, reason: 'no-tabs' };`. On Spitzplatz that is ~10 of 13 tabs (war/resources/relationships have ~400-char turns and no pools). `SCRIBE_TABS` is derived from `DOSSIER_MOUNTS`, and `townCard.js:398` records \"Over the pinned town's THIRTEEN TABS\". The design §6 still says \"six tab calls\" and §5c rule 4 calls daily life \"a SEVENTH TAB CALL\" — the design's call count is stale by roughly a factor of 1.7 against the built code.","where":"lane-scribe/src/store/scribeTransport.js:56-64,220-221; lane-scribe/src/domain/prose/scribePage.js:131-133; design §6 Wall-clock paragraph and §5c rule 4"},{"topic":"COST ESTIMATE — the rates","fact":"From the claude-api skill (model table cached 2026-06-24): Claude Opus 5 `claude-opus-5`, 1M context, **$5.00 /MTok input, $25.00 /MTok output**; Sonnet 5 $2/$10. Prompt caching: **cache reads ~0.1× base input ($0.50/MTok on Opus 5); cache writes 1.25× for 5-minute TTL, 2× for 1-hour TTL ($10/MTok on Opus 5)**; break-even on a 1h TTL needs at least three requests. Message Batches run **asynchronously at 50 % cost**. The design pins `ttl: '1h'` on the block brief (§3.1) and names the Batches path for unopened worlds (§6, \"renders overnight at half price\").","where":"claude-api skill — Current Models table, shared/prompt-caching.md:144, and the Batches line in the Quick Task Reference; design §3.1 item 1 and §6 Wall-clock"},{"topic":"COST ESTIMATE — one epoch render, PLAUSIBLE not confirmed","fact":"Arithmetic over the RUN-5 prompt at ~4.0 chars/token (the chair's own RUN-1 ratio: 148,890 chars ≈ 37 K tokens), town tier, ~10 firing tabs, Opus 5 first-party rates. WRITER, cold hour (both brief variants written at the 1h 2× rate): writes 52.8 K ≈ $0.53 + cache reads ≈230 K ≈ $0.12 + volatile turns 23.7 K ≈ $0.12 + visible output ≈6 K ≈ $0.15 ⇒ **≈ $0.92**. WRITER, warm hour (only the town block written): $0.04 + $0.14 + $0.12 + $0.15 ⇒ **≈ $0.45**. TIER-1 READER, 10 calls riding the same cached prefix ⇒ **≈ $0.28–$0.40**. **TOTAL ≈ $0.73 (warm) to $1.32 (cold) per epoch render** — EXCLUDING adaptive-thinking output tokens, which the design pins at `effort: 'high'`, which bill at $25/MTok, and which no artefact on disk measures; a 2–5× multiple on visible output would add $0.15–$0.60. Batches halve the whole figure. Label: PLAUSIBLE (arithmetic over measured chars and published rates), never CONFIRMED — no API call has ever been made.","where":"derived from the measured files above + claude-api rates; the chars/token ratio is from SCRIBE-SIM-2026-09-14.md §\"The run\" (\"brief 115,307 chars (cached) + turn 33,583 chars (volatile) ≈ 37 K tokens\")"},{"topic":"COST — the SKU is at or below COGS","fact":"5 credits retails at $1.00 (25-pack) / $0.85 (60-pack) / $0.65 (150-pack). The estimate above puts one render at ≈$0.73–$1.32 of Opus 5 COGS before thinking tokens, before Stripe's cut, before the free first render, and before any repair round. At the 150-pack rate the chair's default SKU is UNDERWATER; at the 25-pack rate it is roughly break-even. This is the strongest argument in the record for a subscription tier over per-render credits — and it is arithmetic nobody has run before now.","where":"pricing.js NEW_PACKS :48-52 and NEW_AI_COSTS :73 against the cost estimate above"},{"topic":"THE DESIGN'S COST MODEL IS STALE — the central unmeasured part","fact":"Design §6 verbatim: \"**Caching and cost, per settlement (to be MEASURED in the pilot, not trusted):** six tab calls; the block brief cached after the first settlement of the hour (`cache_read_input_tokens` is the receipt — the runsheet's 'one economic claim no test can prove', verified by reading it on two consecutive calls); the town card ~3–6 K tokens per tab; the output ~60–70 units × ~40 tokens ≈ 3 K per settlement; the tier-1 refute the same size again. Order of magnitude: a few tens of thousands of input tokens per settlement, mostly cached, and a few thousand output. At the skill's listed first-party rates that is cents per settlement on Opus 5, not dollars — and it is the owner's paid surface, so the price row (§12) is signed, not derived.\" MEASURED AGAINST THAT: ~10 tab calls not six; a 24.2 K-token cached prefix read on EVERY call ⇒ ~280–370 K cache-read tokens per settlement for the writer alone, plus ~24 K volatile — roughly 390 K input tokens, an order of magnitude above \"a few tens of thousands\", and the cache reads are 0.1× not free. The §6 paragraph was written before the prompt it prices existed.","where":"ledger HEAD docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md §6, final paragraph"},{"topic":"WHAT THE SIMULATION CANNOT TELL US (the design says so twice)","fact":"SCRIBE-SIM §\"What the simulation cannot tell us\": \"Tokens by the API's own count, cache-hit rate, dollars, wall-clock, and whether `parse` with `zodOutputFormat` behaves as written. Those are the key's.\" Repeated at RUN 5's close: \"the key first — the live pilot measures tokens, cache reads, dollars and wall-clock, which no simulation can.\" And the programme memory: \"BUILT DARK, UNVERIFIED LIVE: no render, bill or landed artefact has run end to end.\" Every dollar figure in any tier proposal is therefore an estimate over chars, not a receipt.","where":"SCRIBE-SIM-2026-09-14.md; memory the-scribe-generation-time-prose-programme-2026-09-14.md STATE paragraph; design §11c"},{"topic":"THE §12 GAP — the tier item was never written","fact":"§11c says \"W5 is a PROPOSAL (§12 item 16, the tier).\" §12 contains items 1–11, 14, 13, 12, 15 — **there is no item 16**. The tier has no entry in the owner-gated list, no price, no entitlement shape, no render allowance, no cap, no BYOK arm, and no position relative to Cartographer or Surveyor anywhere in the design, the rulings, or the code. The §11 wave plan's only line on it is \"**W5 — THE PAID SURFACE**: the price row, the free-first-render claim, BYOK, the Batches path for unopened worlds, the re-scribe opt-in, DM edits over `_userEdits`. Every item owner-signed.\"","where":"ledger HEAD docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md §11c vs §12 (items 1–15) and §11 W5"},{"topic":"WHAT THE OWNER HAS SIGNED","fact":"(a) The Scribe is built to completion, dark (05:0x \"let's build both in parallel. Comprehensively architect out this newest ai model integration\"; 05:5x \"build it in! this will likely happen first before the handwritten prose lands\"). (b) The corpus is every subscription's prose, free and paid, until the rewrite is finished (13:2x). (c) The four product-surface rules — REDO, the narrative overlay retired, the AI-instructions box read on every render, daily life by default (06:5x). (d) The render trigger is the OPEN, frozen until the next advance; unopened towns are never billed (06:3x). (e) An undone epoch is SAVED, never dropped (06:4x). (f) The standard the prose is held to: non-contradiction, not non-invention (14:4x). (g) Total delegation over the programme's judgment (09-13 14:0x: \"okay continue with the work all the way. i give all permissions you need now or would need in the future and leave all decisions to you, fable.\").","where":"memory owner-directive files of 2026-09-13 and 2026-09-14; rulings 13–19 and 36, SCRIBE-RULINGS.md"},{"topic":"WHAT THE OWNER HAS NOT SIGNED","fact":"Every one of design §12's items 1–15 remains RECORDED, NOT DECIDED — the price row, the free first render, the public projection, licensed-vs-rendered on import, the residual error rate, refused-unit marking, re-scribe, the model, retention and training consent, BYOK behaviour, the checkout reconciliation, the FINITE-SEMANTICS pin wording, past-epoch retention (item 13), the single render SKU's price (item 14), and the two readiness ladders (item 15). Plus the five acts nothing moves without: the API key, `supabase db push` of 201+202, the tier's price and shape, item 13, and the flag — flag LAST. The chair has issued vetoable defaults for most of these under the delegation grant; a default is not a signature.","where":"ledger HEAD docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md §12; memory the-scribe-generation-time-prose-programme-2026-09-14.md final paragraph; memory MEMORY.md START HERE row"},{"topic":"Owner law that governs HOW the proposal must be written","fact":"2026-08-27 verbatim: \"I want the absolute best product not compromises.\" / \"I could've launched some version of all of this a year ago or a month ago. I will keep waiting on launch for the best product. Just keep that in mind.\" How-to-apply, verbatim: \"**Never present a cheaper option as though it were the best available.** Name the highest-ceiling route first, price it honestly, and let the owner choose to scale down.\" And the standing hazard on the same page: \"⛔ **A self-imposed constant silently becomes a design constraint unless someone re-asks whether it should be.**\" — the 5-credit SKU, the 55 s invocation budget, and the once-per-epoch trigger are exactly that class of constant.","where":"/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/time-is-not-the-constraint-quality-is.md"},{"topic":"Owner law — risk appetite inside the delegated scope","fact":"2026-07-27 verbatim: \"always choose the best option even if it introduces risk.\" How-to-apply: \"In delegated judgment calls, pick the best-engineering option and record it vetoably per judgment-ledger; do not default to the timid variant to avoid risk. This does NOT unlock owner-gated classes (pushes/deploys, migrations, persistence shape, data deletion, security posture, **paid surfaces**, R-5/parked queue items) — those stay gated.\" A subscription tier IS a paid surface, so the directive governs how boldly the chair may PROPOSE, never whether it may decide.","where":"/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/owner-risk-appetite-directive.md"},{"topic":"Security and retention (constraints on any tier's fine print)","fact":"Design §8: player units from the player projection, DM notebook units from the DM read — \"**two calls, never one context**\". Keys stay server-side; BYOK through the vault (\"never logged, fail-closed on vault error per the owner's 2026-07-30 ruling; the probe-tier guard\"). \"`probeProfile` stays NULL on the managed path so the server key's cached prefix is byte-identical for every user — the block brief must not vary per user either.\" That last clause is load-bearing economics: a per-user or per-tier brief would destroy the cache sharing the whole cost model rests on, so a tier CANNOT differentiate by prompt content. Ruling 9: \"NO Scribe prose enters any training/research corpus without the campaign-level toggle.\"","where":"ledger HEAD docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md §8; ruling 9, SCRIBE-RULINGS.md"},{"topic":"Storage economics the tier must price (owner-gated item 13)","fact":"Design §5b JUDGMENT (vetoable): past epochs kept COMPACT (units only) and rotated into `ai_data.chronicle` under `CHRONICLE_LIMITS` tier-based rotation, \"rather than growing the blob without bound — a year-by-year campaign of fifty advances at ~150 KB an epoch would otherwise be 7.5 MB on one save row and 375 MB across its undo snapshots. … if the owner wants every epoch forever on the blob, that is a storage decision (§12, item 13).\" Note the retention is ALREADY tier-based via the chronicle's limits — so item 13 is not merely storage, it is a tier feature the proposal must name. Also §5: the account export ceiling must rise from `MAX_IMPORT_BYTES = 5 MB` to 64 MB or an account of ~30 towns \"cannot re-import its own export — a silent data-loss path with no test today.\"","where":"ledger HEAD docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md §5b THE ARTEFACT bullet, §5 EXPORT bullet, §12 item 13"},{"topic":"Pre-existing blockers the tier's launch inherits","fact":"Ruling 24: \"PRE-EXISTING DEFECTS SURFACED BY W2, THE OWNER'S: the account-import NODE BUDGET (`MAX_NODES` 20,000) refuses 4 of 7 real generated towns today, artefact or no artefact (security posture — owner-gated); `verify:dist`'s first-paint closure is over its 1,048,000 B ceiling at the BASE (1,058,427; W2 adds 162 B) and the generation worker is over its ceiling too (1,416,656 vs 1,404,493) — both pre-existing reds, neither W2's.\" Ruling 20 also defers W2's commit 6 (the SKU/overlay retirements) to W5 because retiring the `narrative`/`dailyLife` SKUs by editing the client table \"reds the money pins over SHIPPED migrations\" — \"the SKUs retire by a NEW migration that re-seeds `ai_credit_costs` (never by contradicting a shipped seed), the overlay's UI and requests retire at the FLAG FLIP (W4).\"","where":"rulings 20 and 24, SCRIBE-RULINGS.md"},{"topic":"The checkout hazard that gates any product byte","fact":"Design header, verbatim: \"⛔ **THE CHECKOUT IS NOT THE CODE.** The main working tree at `/Users/cstokes/Desktop/settlement-engine` is an older snapshot: 4,236 deletions and 1,270 modifications against HEAD … Every line reference in this document is `git show HEAD:<path>`. A build started from the checkout targets a codebase that no longer exists. Reconciling the checkout is the owner's act (it is their WIP) and the first prerequisite of W2.\" The pricing figures above were read from the `lane-scribe` dock at `9a8ff7280`, the consist lineage (ruling 11), NOT from the checkout.","where":"ledger HEAD docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md status block; ruling 11, SCRIBE-RULINGS.md; memory the-main-checkout-is-a-stale-snapshot-read-the-repo-at-head.md"}],"openQuestions":["THE TIER'S SHAPE IS UNWRITTEN. §11c points at \"§12 item 16, the tier\" and §12 stops at 15. Is the Scribe tier (a) a fourth `TIERS` key beside wanderer/cartographer/founder, or (b) an ENTITLEMENT outside `TIERS` on the SURVEYOR_PLAN pattern — which pricing.js documents as the deliberate choice that \"lets `getVisibleTiers()` stay three-way and leaves every existing TIERS consumer untouched\"? Nothing in the record decides this, and it determines whether the tier CHECK constraint has to widen (migration 139 declined to).","WHAT DOES THE TIER METER? The owner's 13:2x how-to-apply says \"an account tier gate at `scribe-render`, replacing OR WRAPPING the per-render credit spend\" — the chair wrote both options and chose neither. Unlimited renders under a flat fee is uncapped COGS at ~$0.73–$1.32 a render; a bundled allowance keeps `dossierProse: 5` and needs the Stripe webhook to grant, which SURVEYOR_PLAN's `monthlyCredits: 0` docblock says is UNBUILT (\"WEB-10 flips the dial in the same commit that teaches the webhook to grant\").","IS THE 5-CREDIT SKU SOLVENT? At the 150-pack rate 5 credits is $0.65 against an estimated $0.73–$1.32 of COGS before thinking tokens. Ruling 1 promised \"the pilot's measured COGS re-prices it before launch\" and the pilot has never run. Should the proposal price the tier off this estimate, or refuse to name a number until the key produces `cache_read_input_tokens` and dollar receipts — which the design itself calls \"one economic claim no test can prove\"?","HOW MANY RENDERS DOES A REAL MONTH OF PLAY BUY? The billable unit is opened-settlement × epoch, plus every REDO. Nobody has modelled a realistic month: a DM with one campaign of thirty towns advancing monthly and opening six of them bills six renders; a DM who tunes AI guidance and redraws bills more. Cartographer's 30 monthly credits buy exactly six renders today. Without this model, any allowance number is arbitrary.","DOES ADAPTIVE THINKING AT effort:'high' DOUBLE THE BILL? Ruling 8 pins `effort: 'high'` with adaptive thinking; thinking tokens bill at $25/MTok and NOTHING on disk measures them — the harness files capture only the visible JSON. The claude-api skill's cost guidance says effort is the first quality-trading lever and that lower effort on current models often matches higher effort on prior ones. Is `high` load-bearing for the Scribe, or an untested constant of the kind the owner's 08-27 hazard note warns about?","WHY IS THE 1-HOUR TTL RIGHT? The design pins `ttl: '1h'` (2× write) on the block brief. The TOWN block is read by ~10 tab calls seconds apart, where a 5-minute TTL at 1.25× would do — and the caching doc says a 1h TTL \"needs at least three requests\" to pay off and \"buys nothing\" for requests under five minutes apart. Nobody has separated the two blocks' TTLs.","IS DAILY LIFE A SECOND CACHE PREFIX? `daily_life/brief.md` is 98,480 chars against every other tab's 96,795 — a different prefix, hence a second 24.6 K-token cache WRITE per hour on top of the main brief. Was that intended, or should the daily-life register live inside the one shared brief so the cache is written once?","DOES THE PRODUCT'S TIER-1 READER RIDE THE CACHE? Ruling 31 and RUN 3's finding 5 say the checklist rides the same cached blocks in the product; the harness's `tier1-*.md` is self-contained and re-embeds the town. If the built `scribe-render` sends it the harness's way, tier 1 roughly doubles the bill. This is a code question nobody has answered and a live-run question the key would settle.","SIX TABS OR TEN? Design §6 costs \"six tab calls\" and §5c rule 4 calls daily life \"a SEVENTH\"; the built `firingTabs` walks `SCRIBE_TABS` (thirteen tabs, ~ten firing on the pinned town). Which is the design's intent — and does the proposal price ten or trim the tab set?","THE DAILY CAP: DOES THE OWNER BELIEVE ONE EXISTS? The 13:2x directive says \"the daily cap … [is] re-decided inside that design\", but no cap was ever designed, ruled or built — it appears only in one chair memory sentence that seems to have read a render cap out of `saveLimit: 3`. The proposal should either surface this as a chair error or propose a cap deliberately; it should not silently inherit one.","WHAT HAPPENS TO THE TIER WHEN THE CORPUS LANDS? The 08:5x end state is that a finished corpus makes the free tier truly free and the Scribe \"moves to a HIGHER PAID TIER\"; 13:2x says the corpus is every subscription's prose \"until the full rewrite is finished\" — and the rewrite is PAUSED on the owner's 12:05 word (\"focus solely on the scribe\"). Is the Scribe tier priced for the world where the corpus is unfinished, the world where it is finished, or both?","THE FREE FIRST RENDER, RE-OPENED. Ruling 2 (one per ACCOUNT, migration-118 pattern) is BUILT in migration 202 but explicitly re-decided by 13:2x. Inside a subscription tier is the right free sample a render per account, a trial epoch, a single free settlement, or nothing at all?","GALLERY VISIBILITY IS A TIER QUESTION. Ruling 3 hides Scribe prose from every gallery viewer fail-closed and schedules the re-open \"at W5\" — the same wave as the tier. If a subscriber's rendered prose is invisible to everyone else, the tier has no shop window; if it is visible, non-subscribers read paid prose for free. Undecided.","BYOK AS THE ZERO-COGS ARM. Ruling 10 runs a BYOK user's Scribe on their key, and Surveyor already sells BYOK at $14.99/mo with a zero managed allowance. Should the Scribe tier be BYOK-first (near-zero COGS, a real price ceiling problem) or managed-first (COGS-bound)? The record contains the mechanism and no decision.","THE BLIND READ THE DECISION RULE REQUIRES HAS NEVER HAPPENED. Design §10 gates the ship on \"a 40-unit blind read: the owner and one Fable seat, Scribe vs corpus, same pools, shuffled\" AND a post-refute FAIL rate at or below the corpus's audited defect rate. Five simulated runs measured the instruments; nobody has measured whether the OWNER prefers the prose. Should the tier proposal be written before that read, or should the read be the proposal's first ask?"]}}
{"type":"result","key":"v2:947d48a9daaef0fe226825bbca514986cecb37f2d366043eb516b4d4c170951c","agentId":"a0745eb265497e2f3","result":{"summary":"All paths in `where` are RELATIVE TO THE WORKTREE ROOT `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-scribe` (HEAD = 9a8ff7280, \"SCRIBE W4 car 4\"). Read-only; nothing was written or mutated.\n\nTHE HEADLINE FOR A SUBSCRIPTION PROPOSAL, in four sentences.\n\n(1) THERE IS NO TIER GATE ON THE SCRIBE AT ALL. The product has exactly two billing tiers in the database — `profiles.tier` is `text not null default 'free' check (tier in ('free','premium'))` — plus one ENTITLEMENT table (`surveyor_entitlements`) that migration 139 created *specifically to avoid widening that CHECK*. Eight paid AI edge functions gate on `has_surveyor_entitlement()`. `scribe-render` gates on NONE of them: its whole admission chain is bot-guard → JWT → session-not-superseded → per-IP bucket → `account_is_active` → body validation → BYOK → free-claim → credits. `account_is_active` is a MODERATION predicate (banned/disabled/soft-deleted), not a billing one. So today the Scribe's only \"who may use it\" answer is \"anyone signed in, not banned, with 5 credits.\"\n\n(2) ⛔ THE PRICE THE CODE CHARGES IS NOT THE PRICE THE CODE CLAIMS. Migration 202's header states \"there is no second SKU and no per-tab charge: one render of a settlement's dossier costs five credits whatever it draws,\" and `ScribeRedrawButton` quotes \"Redraw the survey (5 credits)\". But a render is ONE EDGE INVOCATION PER FIRING TAB — `renderScribe` loops the tabs and POSTs each one — and every invocation runs the full `runCreditedCall` with its own `spend_credits('dossierProse')`. The design's own number is seven tab calls. **One render therefore spends ~30–35 credits, not 5**, and the \"free first render\" (one per ACCOUNT, `profiles.free_scribe_claimed_at`) is consumed by the FIRST TAB, leaving the other ~6 charged. Both modules say so in their own headers (\"its own credited spend decision on the server\"); migration 202 and the UI button contradict them. Nothing in the test suite pins the whole-render total — the edge test's \"one render is ONE credited act\" is scoped to a single invocation. This is the single most load-bearing fact for pricing a subscription.\n\n(3) THE RECURRING MACHINERY ALREADY EXISTS AND IS PROVEN, but it is wired to Cartographer only. A subscription invoice (`billing_reason` = subscription_create|cycle) whose first line price id equals `STRIPE_PRICE_PREMIUM` grants 30 credits of source `monthly_allowance`, EXPIRING at the invoice period end; `spend_credits` deliberately allocates `monthly_allowance` grants FIRST. The Surveyor SKU bills as a subscription and grants an entitlement but ZERO credits (`SURVEYOR_PLAN.monthlyCredits: 0` is documented as a dial awaiting \"WEB-10\"). The annual plan is registered dark: `ANNUAL_FACTOR = 0`, and `premium_annual` is absent from create-checkout's `PRICE_MAP` entirely.\n\n(4) THERE IS NO PER-DAY RENDER CAP, and the one-render-per-epoch promise is CLIENT-SESSION MEMORY ONLY. Server-side the only ceilings are a per-user 60-calls/86400s DB limiter (fail-OPEN at the edge), a fail-CLOSED 40/hour per-IP token bucket, and a global USD cap. A per-account daily render cap would need new machinery — though `surveyor_usage_precheck` (migration 144: daily/weekly token caps, USD caps, per-task caps, warn threshold, pause) is an existing, eight-caller DB function that the Scribe simply does not call.","facts":[{"topic":"account_is_active is moderation, not billing","fact":"`account_is_active(p_uid uuid)` returns true only when the profile EXISTS and carries none of `banned_at`/`disabled_at`/`deleted_at`; a missing profile is inactive (fail-closed). It is SECURITY DEFINER, granted to `authenticated` and (explicitly, at 087) to `service_role`. It encodes NO tier, plan, entitlement or subscription fact whatsoever.","where":"supabase/migrations/057_enforce_account_status_writes.sql:40-60; grant to service_role at supabase/migrations/087_review_money_hardening.sql:70"},{"topic":"scribe-render's account gate","fact":"scribe-render calls `account_is_active` through the ADMIN client and fails closed on `isActive !== true` (403 'Account is not active'). This is the ONLY account-state gate in the function — there is no entitlement check and no tier check anywhere in the file.","where":"supabase/functions/scribe-render/index.ts:190-192"},{"topic":"The only billing tier vocabulary in the DB","fact":"`profiles.tier` is `text not null default 'free' check (tier in ('free','premium'))`. The constraint is re-stated at 003 and re-validated in 017's setter (`if new_tier not in ('free','premium') then ...`). There are exactly TWO tier values; no 'surveyor', no 'annual', no scribe tier.","where":"supabase/migrations/001_initial_schema.sql:7; supabase/migrations/003_admin_rls_policies.sql:48; supabase/migrations/017_fix_credit_auth_integrity.sql:314"},{"topic":"Why 139 refused to widen the tier CHECK","fact":"Migration 139's header states the rule verbatim: 'Rather than widen profiles.tier's CHECK (free,premium) — which ripples into TIER_GATE, tierFacts, and every CHECK-pinned RLS clause — this adds a SEPARATE entitlement table + a has_surveyor_entitlement() definer read gate.' This is the established precedent any new paid Scribe tier would follow.","where":"supabase/migrations/139_surveyor_entitlement_and_byok.sql:6-16"},{"topic":"The Surveyor entitlement table and gate","fact":"`surveyor_entitlements(user_id pk, status active|revoked, source, granted_at, revoked_at)` with RLS owner-read-own and service-role writes only; `has_surveyor_entitlement()` returns true iff the CALLER has an active row. Migration 159 adds `stripe_subscription_id` / `stripe_customer_id` columns plus service-role-only `grant_surveyor_entitlement` / `revoke_surveyor_entitlement` / `revoke_surveyor_entitlement_by_subscription`.","where":"supabase/migrations/139_surveyor_entitlement_and_byok.sql:48-88; supabase/migrations/159_surveyor_provisioning.sql:31-38 and :45-78"},{"topic":"Eight AI surfaces gate on the entitlement — the Scribe does not","fact":"`has_surveyor_entitlement` is called by ai-analyst, custom-content, interpret-session, surveyor-autonomy, parley, interview, construct-realm and construct-settlement, each fail-closed on `!== true` with a graceful refusal naming the plan. `scribe-render/index.ts` contains no call to it. Grep for the RPC across supabase/functions returns those eight and never scribe-render.","where":"supabase/functions/ai-analyst/index.ts:198-204; supabase/functions/custom-content/index.ts:181-182; supabase/functions/interpret-session/index.ts:184-185; supabase/functions/surveyor-autonomy/index.ts:161-162"},{"topic":"The graceful-refusal copy a tier gate produces today","fact":"ai-analyst's 403 body is the house pattern for a plan boundary: 'The analyst is part of the Surveyor plan. You can still open the World Pulse and map panels — they read the same standings, factions, and rumors.' (design §3d: name the boundary and the nearest door the user CAN use).","where":"supabase/functions/ai-analyst/index.ts:203-204"},{"topic":"How a client learns the account's tier","fact":"The tier is read from `profiles` at sign-in (`.select('role, display_name, tier, is_founder, avatar_url, email_notifications, model_preference, email')`), normalized, and held in the eager auth store as `'anon' | 'free' | 'premium'`. Elevated roles (developer/admin) are forced to 'premium' by `resolveTier`.","where":"src/lib/auth.js:54, :79, :95; src/store/authSlice.js:106, :73-75, :149"},{"topic":"How a client learns the Surveyor entitlement (separately from the tier)","fact":"The entitlement bit is fetched LAZILY by a `has_surveyor_entitlement()` RPC, duplicated on purpose across two lazy chunks (the door and the account page) so no shared chunk rebalances the first-paint closure, and combined by one pure predicate: entitled OR founder OR admin/developer. Cartographer 'premium' is explicitly NOT sufficient — 'the client tier field conflates it with Surveyor, so the entitlement bit ... not the tier, decides.'","where":"src/components/surveyor/useSurveyorEntitled.js:23-36; src/components/account/useAccountSurveyorGate.js:30-35; src/components/surveyor/surveyorGate.js:16-22"},{"topic":"TIER_GATE gates world features, never AI","fact":"`TIER_GATE` covers maxTier/maxSaves/neighbour/export/mapChains/customContent for anon/free/premium. The module header states the doctrine directly: 'AI features are gated by credits (creditsSlice), not tier — except developers get unlimited.'","where":"src/store/authSlice.js:20, :47-51, :647-694"},{"topic":"The three shipped tiers in pricing.js","fact":"`TIERS` = wanderer (legacyKey 'free', stripeProduct null, 0 cents, saveLimit 3), cartographer (legacyKey 'premium', stripeProduct 'premium', 599 cents/mo, monthlyCredits 30, saveLimit Infinity), founder (lifetime, stripeProduct null — 'a chair is given, never sold', priceCents 9900 retained only for refund/replay rendering). `getVisibleTiers()` returns exactly these three.","where":"src/config/pricing.js:251-317, :494-496"},{"topic":"SURVEYOR_PLAN is deliberately outside TIERS","fact":"`SURVEYOR_PLAN = { key:'surveyor', stripeProduct:'surveyor', priceCents:1499, billing:'monthly', monthlyCredits:0, byok:true, entitlement:'surveyor_entitlements' }`, landed dark. Its comment is explicit: 'NOT a TIERS key, deliberately. TIERS keys are profiles.tier shapes, and Surveyor is an ENTITLEMENT ... the same reason 139 declined to widen the tier CHECK.' `monthlyCredits: 0` is documented as A DIAL — 'the allowance is minted by the Stripe webhook, which does not know about this constant yet; WEB-10 flips the dial'.","where":"src/config/pricing.js:337-361"},{"topic":"The annual plan exists only as a zero dial","fact":"`ANNUAL_FACTOR = 0`, so `CARTOGRAPHER_ANNUAL` derives to priceCents 0 and credits 0, and `ACTIVE_CHECKOUT_SKUS` includes `premium_annual` only `if (ANNUAL_FACTOR > 0)`. 'Two months free' would be factor 10. `premium_annual` is also absent from create-checkout's PRICE_MAP, so it is unsellable on two independent counts.","where":"src/config/pricing.js:363-405; supabase/functions/create-checkout/index.ts:63-79"},{"topic":"runCreditedCall's seven invariants, verbatim","fact":"1. RESERVE the global cap BEFORE the spend (fail-closed on !allowed). 2. Consume the per-user rate limit AFTER reserve; a reject RELEASES the reservation. 3. SPEND is captured (spendId); insufficient/error RELEASES the reservation. 4. On model FAILURE: meter(false) → refund(spendId) → release. No double-spend. 5. On model SUCCESS: meter(true) → release. NEVER refund a successful paid call. 6. ELEVATED (dev/admin) spends are never refunded. 7. The reservation is RELEASED on EVERY post-reserve exit.","where":"supabase/functions/ai-analyst/creditFlow.ts:10-17 (contract) and :64-107 (implementation)"},{"topic":"scribe-render's exact gate order","fact":"OPTIONS/405 → `botGuard` → Authorization header (401) → `auth.getUser` (401) → `isSessionSuperseded` (401) → `aiIpRateGuard` (429/503) → `account_is_active` (403) → body ≤ 512KB (413) → JSON parse (400) → saveId / card.pools / renderedFor present (400) → `resolveProviderKey` with BYOK fail-closed (503) → `claim_free_scribe` → `runCreditedCall`{ `reserve_ai_spend` → `consume_ai_generate_rate_limit` → `spend_credits` → provider (writer + tier-1 reader) → refund / release / meter } → `scheduleAutoReload` → 200.","where":"supabase/functions/scribe-render/index.ts:171-192, :194-212, :217-223, :236-240, :286-448, :475"},{"topic":"The dossierProse SKU is a hard-coded CASE arm","fact":"Migration 202 recreates 192's `spend_credits` body verbatim plus one line, `when 'dossierProse' then 5`, placed 'at the retired narrative's own price'. The function raises on an unknown feature by design, so a new SKU is a new arm or it is a failed spend.","where":"supabase/migrations/202_scribe_claim.sql:20-30, :179-182"},{"topic":"Neither the config-first price path nor the tier multiplier can reach the Scribe","fact":"scribe-render calls `supabaseUser.rpc('spend_credits', { feature: SCRIBE_FEATURE })` — passing NO `p_profile` and NO `p_tier`. 202's config-first block only runs `if p_profile is not null`, and the 192 tier multiplier only runs when `p_tier in ('scout','journeyman','master')`. Both are structurally unreachable for dossierProse, so the price is the literal 5 in the CASE and nothing else.","where":"supabase/functions/scribe-render/index.ts:304; supabase/migrations/202_scribe_claim.sql:150-199, :201-235"},{"topic":"The Scribe has NO runtime repricing lever","fact":"`get_ai_pricing()` is documented as 'the client's ONLY pricing read path', and its net-current body (174) enumerates exactly three features — `foreach feat in array array['narrative','dailyLife','progression']` — over eight model profiles. `dossierProse` is absent, so `validLiveAiCost('dossierProse', …)` always returns null and the client falls back to the frozen constant 5. Repricing the Scribe requires a new migration AND a client deploy; the operator's `ai_credit_costs` config cannot touch it.","where":"supabase/migrations/174_pricing_optimal_margins.sql:240ff (defaults map and the three-feature loop); src/config/pricing.js:104-108, :444-449"},{"topic":"The client mirror of the SKU","fact":"`NEW_AI_COSTS.dossierProse = 5` and `FAST_AI_COSTS.dossierProse = 5` (same number, deliberately: 'The Scribe runs on ONE model by ruling 8, so there is no cheaper tier to price'). Client/server parity is pinned by a test that regexes the migration for `when 'dossierProse' then (\\d+)` and compares it to the client block, and asserts `SCRIBE_FEATURE = 'dossierProse'` in the edge source.","where":"src/config/pricing.js:68-74, :80-85; tests/edgeFunctions/contracts.test.js:487-500"},{"topic":"The free first render: one per ACCOUNT, atomic, releasable","fact":"202 adds `profiles.free_scribe_claimed_at` and three definer functions modelled on migration 118's `claim_free_narrative`: `claim_free_scribe(uuid)` is a conditional UPDATE whose `found` is the answer (service_role only); `release_free_scribe(uuid)` nulls it back, idempotent (service_role only); `free_scribe_available(uuid)` is a self-or-privileged read granted to `authenticated`.","where":"supabase/migrations/202_scribe_claim.sql:38-113"},{"topic":"free_scribe_available has ZERO callers","fact":"A grep of src/ and supabase/functions/ for `free_scribe_available` returns only the migration itself. The client therefore has no way to tell a user whether their one free render is still unclaimed, even though the RPC was built and granted to `authenticated` for exactly that purpose.","where":"supabase/migrations/202_scribe_claim.sql:89-113 (defined); no caller in src/ or supabase/functions/"},{"topic":"How the free claim is wired into the render","fact":"The claim runs BEFORE the credit flow; `usedFree` makes `spend()` return `{ok:true, spendId:null}` so the orchestrator's refund arm has nothing to refund and the release arm still runs; `releaseFree()` fires on every non-ok outcome. The response carries `free: usedFree`.","where":"supabase/functions/scribe-render/index.ts:225-240, :300-309, :450-451, :498"},{"topic":"The global USD cap (reserve_ai_spend)","fact":"Two-phase reservation accounting against `system_config.ai_spend_cap` (defaults enabled, daily_usd 50, monthly_usd 750 when the row is missing). Admission is serialized by `pg_advisory_xact_lock(hashtext('ai_spend_cap'))` and the admission test lives inside the INSERT…SELECT…WHERE. Reservations sit in `ai_spend_reservations` with a 10-minute `expires_at` so a crashed run cannot leak headroom. An operator-disabled cap allows WITHOUT inserting a reservation.","where":"supabase/migrations/086_ai_spend_reservation.sql:43-65, :80-140"},{"topic":"What the Scribe reserves per call","fact":"`SCRIBE_SPEND_ESTIMATE_USD = 0.08`, reserved on EVERY tab invocation regardless of BYOK. Metered COGS is computed as (input + cacheRead + cacheWrite)/1e6 × $5 + output/1e6 × $25, conservatively folding cache reads and writes in at full input price 'so the global cap trips no later than reality'.","where":"supabase/functions/scribe-render/index.ts:82, :288, :430-437"},{"topic":"The per-user daily rate limit is a DB function, and fails OPEN at the edge","fact":"`consume_ai_generate_rate_limit(p_user, p_window_seconds default null, p_user_limit default null)` reads `system_config.ai_user_rate_limit`, falling back to a 86400-second window and a 60-request per-user limit; it is a fixed-window upsert counter in `ai_generate_rate_limits`. In scribe-render's effect an RPC error is logged and returns `{allowed: true}` — fail OPEN, by the same design note that says a limiter outage still runs into the reservation and the hard cap.","where":"supabase/migrations/087_review_money_hardening.sql:227-296; supabase/functions/scribe-render/index.ts:295-299"},{"topic":"The per-IP gate is a DB token bucket and fails CLOSED","fact":"`checkAiIpRate` consumes one token from `aiip:<ip>` via `consume_token_bucket` (migration 156) with server fallbacks capacity 40 and refill 40/hour, operator-tunable through the private `system_config.ai_ip_rate_limit` row. A definite over-limit is 429; a limiter-infrastructure error is a 503 DENY, never a silent open. It SKIPS entirely for a missing/'0.0.0.0' IP. `x-forwarded-for` is spoofable, so the dimension is advisory; `cf-connecting-ip` is the trustworthy value.","where":"supabase/functions/_shared/rateLimit.ts:116-200 (constants at :130-131, checkAiIpRate at :179-200); supabase/functions/scribe-render/index.ts:187-188"},{"topic":"BYOK resolution and its fail-closed rule","fact":"`resolveProviderKey(admin, userId, provider, serverKey, onLookupError, owner)` reads `surveyor_byok_get` (service-role-only decrypt of a pgcrypto-encrypted row). A successful lookup answering NULL means 'no key on file' and takes the house path. Any ERROR/throw consults a second witness, the user's own `surveyor_byok_status`; only a confident empty result reopens the house path, otherwise it returns a typed `{code:'byok_vault_unavailable', status:503, retryable:true}` refusal. The key is never logged (pinned by tests/security/byokNeverLogged.test.js).","where":"supabase/functions/ai-analyst/byok.ts:20-40, :135-216"},{"topic":"BYOK does NOT waive the Scribe's credits","fact":"scribe-render resolves the key BEFORE constructing the credit flow so a vault failure costs nothing, but a BYOK user still runs the identical `spend_credits('dossierProse')` path and still reserves $0.08 against the global cap. The ONLY thing `providerKey.byok` changes is the metered `estimated_cost_usd`, which becomes 0. There is no BYOK discount, waiver or bypass on this surface.","where":"supabase/functions/scribe-render/index.ts:214-223, :300-309, :437"},{"topic":"Two provider calls per invocation, one credited act","fact":"Each invocation makes the writer call and then, only where tier 0 kept units, a tier-1 checklist call on the SAME two cached system blocks. ONE 55-second AbortController spans both ('Two timers would let a slow writer plus a slow reader spend 110 s'). Usage from both is SUMMED into one `ai_usage_events` row and the response's `calls` field reports 1 or 2. A tier-1 failure is NOT a render failure.","where":"supabase/functions/scribe-render/index.ts:75-87, :265-284, :315-319, :357-409, :428-447, :496"},{"topic":"credit ledger tables","fact":"`credit_ledger(id, user_id, kind check in ('grant','spend'), amount check > 0, source, metadata jsonb, expires_at, reversed_by, created_at)` is authoritative; `credit_spend_allocations(spend_id, grant_id, amount)` records which grants funded which spend; `credit_transactions(user_id, amount signed, reason, settlement_id)` is the legacy mirror that `spend_credits` still dual-writes, and `profiles.credits` is a denormalized cache updated at the end of every spend.","where":"supabase/migrations/007_credit_ledger.sql:36-50; supabase/migrations/001_initial_schema.sql:45-52; supabase/migrations/202_scribe_claim.sql:250-284"},{"topic":"spend_credits burns the subscription allowance FIRST","fact":"The allocation loop orders grants `case when g.source = 'monthly_allowance' then 0 else 1 end, g.expires_at nulls last, g.created_at` — so an expiring monthly allowance is consumed before purchased packs. This is the existing, proven shape for any Scribe subscription allowance.","where":"supabase/migrations/202_scribe_claim.sql:255-278 (order-by at :266-267)"},{"topic":"The existing recurring-grant path (Cartographer)","fact":"On `invoice.paid`, the webhook proceeds only for `billing_reason` of `subscription_create` or `subscription_cycle`; then a PRICE-ID GATE requires the invoice's first line price to equal `STRIPE_PRICE_PREMIUM` (added because 'a Surveyor subscription invoice would otherwise mint the Cartographer allowance'), FAIL-OPEN when the env var or line price is absent. It then idempotency-checks `credit_ledger` on `metadata->>stripe_invoice_id` and grants 30 credits of source `monthly_allowance` with `expires_at` = the invoice line's period end.","where":"supabase/functions/stripe-webhook/index.ts:350-357, :359-375, :395-401, :403-412"},{"topic":"Subscription products and the checkout catalogue","fact":"`SUBSCRIPTION_PRODUCTS = new Set(['premium','surveyor'])`; everything else uses Stripe payment mode. `PRICE_MAP` holds credits_25/60/150, premium, single_dossier, surveyor, plus legacy credit SKUs kept resolvable for refunds/replays. An unset env var yields '' which makes the SKU unpurchasable (LAW 1). `founder_lifetime` is in `ABOLISHED_PRODUCTS` and refused outright on the outbound path.","where":"supabase/functions/create-checkout/index.ts:63-79, :97, :113, :333-337"},{"topic":"The Surveyor checkout grants an entitlement and no credits","fact":"On `checkout.session.completed` with product 'surveyor', the webhook retrieves the live subscription (skipping an out-of-order delete), then calls `grant_surveyor_entitlement(p_user, p_source, p_subscription_id, p_customer_id)`. It mints NO credits. The money-spine kinds `surveyor_start` / `surveyor_renewal` exist and renewals are discriminated from Cartographer by comparing the renewal line's price id to `STRIPE_PRICE_SURVEYOR`.","where":"supabase/functions/stripe-webhook/index.ts:2890-2913, :1496, :1508-1509, :3057-3063"},{"topic":"Auto-reload can charge the card after a Scribe render","fact":"`scheduleAutoReload(supabaseAdmin, user.id)` runs after every successful render, exactly as every other credited surface does. It reads the credits_25 Stripe price, calls the atomic `claim_auto_reload_attempt` (migration 158 — re-reads settings+balance, gates below-threshold, enforces cooldown and a monthly cap, one-open unique index), and on a claim creates an off-session PaymentIntent whose SUCCESS IS NOT TRUSTED — the webhook confirms and grants. Never-throw contract: a reload failure can never fail a paid generation.","where":"supabase/functions/scribe-render/index.ts:473-475; supabase/functions/_shared/autoReload.ts:1-29"},{"topic":"The narrative and dailyLife SKUs the Scribe is meant to replace are STILL LIVE at this HEAD","fact":"`spend_credits` still prices `narrative` 5 and `dailyLife` 4, and the client still spends them: `aiSlice.requestNarrative` (cost via `getAiCostForModel('narrative', …)`) and `aiSlice.requestDailyLife` (`getAiCostForModel('dailyLife', …)`) are both present and registered in the operation registry. scribe-render's own header says 'THE NEXT COMMIT GUTS THAT MODULE. The owner's rule 17 retires the narrative overlay, and requestNarrative with it' — that commit has not landed in this worktree.","where":"supabase/migrations/202_scribe_claim.sql:176-178; src/store/aiSlice.js:292, :311, :574, :596; src/store/operationRegistry.js:138-139; supabase/functions/scribe-render/index.ts:16-18"},{"topic":"The OPEN trigger's four conditions","fact":"A render is enqueued only when (1) the dossier is ON SCREEN — not a save, not an advance, not a tick; (2) it has a durable home (`saveId`) — 'no credit is ever spent on a settlement that cannot keep what it paid for'; (3) the artefact is STALE or ABSENT (`advanceSeq`, seed, or engine version mismatch) — a current artefact is FROZEN, so opening the same dossier ten times in one epoch renders once; (4) the flag is lit and a transport is registered. The decision returns a reason string always.","where":"src/store/scribeOpenTrigger.js:1-43, :106-149"},{"topic":"The one-per-epoch guarantee is CLIENT SESSION MEMORY ONLY","fact":"`ATTEMPTED` is a module-scope `Set` keyed `saveId::advanceSeq::renderedFor`, added BEFORE the await so two effects in one tick cannot both send, and deleted on failure so a reopen can retry. It is session state DELIBERATELY ('it must not survive a reload, because a render that failed and left no artefact has to be retryable by reopening'). There is NO server-side equivalent: scribe-render never reads saveId/advanceSeq for dedup — it only echoes them back in the response.","where":"src/store/scribeOpenTrigger.js:21-27, :65-73, :176-182, :193-200; supabase/functions/scribe-render/index.ts:199-202, :476-482"},{"topic":"The REDO bypasses the freeze and the ledger by design","fact":"A redo skips the frozen test and the ATTEMPTED ledger ('the ledger's whole job is to refuse a second render of an epoch, and a second render of an epoch is what a redo IS'), and answers instead to two new conditions: there must BE a current render to retire (`currentAdvanceSeq(prose) !== null`), and a separate `REDRAWING` in-flight set refuses a double-click. It still answers to the flag, the settlement, the durable home, the seed and the transport. There is NO cap on how many redraws one epoch may buy.","where":"src/store/scribeOpenTrigger.js:29-38, :75-83, :131-140, :176-203, :221-223"},{"topic":"A redraw is billed identically to an open","fact":"`reason` is 'open' or 'redo' and 'it changes EXACTLY ONE THING: on redo the prior render is retired into the past lane before the answers land... It changes NO pricing byte: a redraw is a render and is billed through the same SKU by the same server call (pricing.js untouched).' A test pins the absence: no `redraw: <n>` in the transport and no `dossierRedraw` key in pricing.js.","where":"src/store/scribeTransport.js:197-200; tests/store/scribeTransport.test.js:196-202"},{"topic":"⛔ ONE RENDER IS N CREDITED CALLS, ONE PER TAB — the per-tab vs per-render contradiction","fact":"`renderScribe` computes `firingTabs` then loops `for (const tab of tabs) { … await postTabRender(tabRequestBody(request, tab, card, record), token) … }`. Each POST hits scribe-render, which runs the full `runCreditedCall` including its own `spend_credits('dossierProse')`. Both modules say so in their headers: scribeTransport — 'each tab is its own call, its own credited spend decision on the server, and its own landing'; scribe-render — 'the Scribe is ONE INVOCATION PER TAB (§6) ... Seven small calls, not one large one.' MIGRATION 202 SAYS THE OPPOSITE: 'The daily-life beats are the seventh tab call of the same epoch render (§5c rule 4), so there is no second SKU and no per-tab charge: one render of a settlement's dossier costs five credits whatever it draws.' At 5 credits per invocation and the design's own seven calls, ONE RENDER COSTS ~35 CREDITS, not 5. CONFIRMED by reading both paths end to end; no test pins the whole-render total (the edge test's 'one render is ONE credited act and ONE usage row' is scoped to a single invocation).","where":"src/store/scribeTransport.js:8-12, :56-64, :247-259; supabase/functions/scribe-render/index.ts:19-21, :286-309; supabase/migrations/202_scribe_claim.sql:28-30; supabase/functions/scribe-render/index.test.ts:396"},{"topic":"How many tabs a render covers","fact":"`SCRIBE_TABS` is every distinct tab named by `DOSSIER_MOUNTS` — THIRTEEN: overview, economics, defense, power, faith, war, history, viability, daily_life, plot_hooks, relationships, resources, services. `firingTabs` narrows that to the tabs whose page actually composes a line for THIS settlement (a fact about the town, not a maintained list). The design's stated working number is seven calls, with daily life as the seventh.","where":"src/domain/prose/scribePage.js:130-133, :793-811; src/domain/display/stateProse/dossierMounts.js:257ff; src/store/scribeTransport.js:56-64"},{"topic":"⛔ The 'free first render' is in practice a free first TAB","fact":"`claim_free_scribe` is attempted on EVERY tab invocation and is once per ACCOUNT (`profiles.free_scribe_claimed_at`). On a user's very first render, tab 1 claims it and renders free; tabs 2..N each run `spend_credits` and charge 5. So the advertised free first render actually costs a new user ~30 credits under the current wiring.","where":"supabase/migrations/202_scribe_claim.sql:13-18, :43-61; supabase/functions/scribe-render/index.ts:236-240, :300-309"},{"topic":"What the UI promises the user before it spends","fact":"`ScribeRedrawButton` is self-gating on four conditions (flag lit, durable home, DM on their own editable dossier, a current render exists) and labels itself `Redraw the survey (${cost} credits)` with `cost = getCost('dossierProse')` = 5, plus a tooltip 'Spends 5 credits.' Its header claims 'the number on the label is the number the server charges' — true per invocation, false per render.","where":"src/components/dossier/ScribeRedrawButton.jsx:6-24, :59, :84-94"},{"topic":"What an advance bills","fact":"Nothing. An advance only makes the artefact stale; the NEXT OPEN renders. Unopened towns are never billed ('A realm of thirty towns advanced a year renders the ones the game master actually looks at and no others'). The epoch record is computed at advance time and parked on the artefact, read once per render and sent with every tab.","where":"src/store/scribeOpenTrigger.js:3-19, :142-148; src/store/scribeTransport.js:240-246"},{"topic":"The flag is dark and lighting it is owner-gated","fact":"`FLAGS.scribe` description: 'Default OFF and INERT: with the flag off nothing renders, nothing is billed, no Scribe module enters the first-paint closure, and every tab draws the hand corpus. Lighting it opens a PAID surface, so it needs the render SKU seeded and the provider key in place first.' Migration 202's header adds that applying it changes no behaviour and that `supabase db push` is likewise the owner's act.","where":"src/lib/flags.js:88; supabase/migrations/202_scribe_claim.sql:8-11"},{"topic":"A per-user cap machine ALREADY EXISTS in the DB — the Scribe just does not call it","fact":"`surveyor_user_settings` carries `daily_token_cap`, `weekly_token_cap`, `daily_usd_cap`, `weekly_usd_cap`, a `per_task_caps` jsonb map, `warn_pct` and a `paused` switch; `surveyor_usage_precheck(p_user, p_provider, p_feature)` is 'the SINGLE edge door' returning {allowed, paused, warn, reason_class} before anything is spent. Eight edge functions call it (ai-analyst, custom-content, interpret-session, surveyor-autonomy, parley, interview, construct-realm, construct-settlement). `scribe-render` does not.","where":"supabase/migrations/144_surveyor_usage_governors.sql:6-33, :37-58; callers at supabase/functions/ai-analyst/index.ts:290, custom-content/index.ts:263, interpret-session/index.ts:259, surveyor-autonomy/index.ts:235, parley/index.ts:178, interview/index.ts:226, construct-realm/index.ts:240, construct-settlement/index.ts:239"},{"topic":"The tier multiplier machinery is built, inert, and owner-gated","fact":"Migration 192 taught `spend_credits` a third argument (`p_tier`) that scales a resolved cost by a multiplier read from `system_config.ai_tier_multipliers`, clamped to the 0.5–3 band and to a 1..12 cost floor/ceiling. There is deliberately NO seed row — 'absence of the row is the inert state'. An unrecognized tier string degrades to the ordinary price rather than raising, because 'the tier names are the design's WORKING names and the owner has not made the taste pick'. The client half (`TIER_MULTIPLIERS` = {scout:1, journeyman:1, master:1}) is frozen at identity and is documented as THE ACTIVATION SWITCH for the quoted price, owner-signed.","where":"supabase/migrations/192_tier_credit_multiplier.sql:1-40; supabase/migrations/202_scribe_claim.sql:201-235; src/config/pricing.js:133-158, :451-483"},{"topic":"The activation checklist 192 wrote for itself","fact":"Turning the multiplier on requires, explicitly and in this order: (1) the owner signs the pricing sheet and seeds `ai_tier_multipliers`; (2) a later wave extends `surveyor_usage_precheck` from its NET-CURRENT body so warn/cap quotes the same tier-scaled number that will be charged; (3) a later wave threads the tier from the edges into the RPC — 'No edge function is touched here; the 11 live callers keep their 2-arg shape'; (4) the client display half ships inert. This is the ready-made path a Scribe subscription tier would walk.","where":"supabase/migrations/192_tier_credit_multiplier.sql:24-39"},{"topic":"The single-session belt and the elevated bypass on every spend","fact":"`spend_credits` calls `assert_current_session()` first ('a superseded JWT can never move credits'), then `account_is_active`. A `developer`/`admin` role or `current_user_is_privileged()` writes an `elevated:true` ledger row and returns balance -2 without touching grants — and creditFlow invariant 6 means such a spend is never refunded. scribe-render independently rejects a superseded session before it reaches any of this.","where":"supabase/migrations/202_scribe_claim.sql:141-148, :237-243; supabase/functions/scribe-render/index.ts:184-186, :416-422"}],"openQuestions":["⛔ THE PRICE CONTRADICTION IS THE FIRST THING THE PROPOSAL MUST SETTLE. Migration 202 and ScribeRedrawButton both assert 5 credits per render; the transport and the edge function both bill 5 credits PER TAB (~7 tabs = ~35 credits). Three possible resolutions, all with different subscription economics: (a) the per-tab charge is the true intent and 202's header plus the button label are wrong — then a render is ~$0.35-worth of credits and a subscription's allowance must be sized for 35, not 5; (b) the ONE-PRICE ruling is the true intent and scribe-render needs per-(saveId, advanceSeq) spend dedup — a NEW server-side idempotency key, since nothing durable exists today; (c) the SKU is repriced to ~1 credit/tab. This is owner-gated (paid-surface behaviour) and cannot be decided in-lane.","NO TIER CONCEPT EXISTS FOR THE SCRIBE. `profiles.tier` is CHECK-constrained to ('free','premium') and 139 recorded in terms why it must not be widened. A paid Scribe tier therefore needs a NEW entitlement table + a `has_*_entitlement()` definer gate following the surveyor_entitlements pattern (139/159), plus a gate call in scribe-render that does not exist today. Does the Scribe tier reuse `surveyor_entitlements` (one AI plan, many surfaces) or get its own table (two plans, separately priced)? The owner's frame — 'the Scribe moves to a HIGHER PAID TIER' — implies a ladder above Surveyor, which the existing binary entitlement cannot express.","NO PER-DAY OR PER-ACCOUNT RENDER CAP EXISTS. The owner's 'daily render cap is temporary economics' has no implementation. The nearest existing machinery is `surveyor_usage_precheck` (migration 144: daily/weekly token caps, USD caps, a per_task_caps map, warn_pct, paused) which eight surfaces call and the Scribe does not. Extending it is the cheap path — but 192's own activation checklist warns that precheck must be recreated from its NET-CURRENT body carrying EVERY delta, not forked from 144's source (the 159 accident). A render-COUNT cap (as opposed to a token/USD cap) has no column anywhere and would need a new one.","THE SCRIBE CANNOT BE REPRICED AT RUNTIME. `get_ai_pricing()` — 'the client's ONLY pricing read path' — enumerates only narrative/dailyLife/progression, and scribe-render passes no `p_profile`, so `system_config.ai_credit_costs` cannot reach dossierProse either. Every price change is a migration plus a client deploy. If a subscription is meant to carry a different per-render price (or a zero price with an allowance), that lever has to be built.","THE SUBSCRIPTION ALLOWANCE PATH IS WIRED TO CARTOGRAPHER ONLY. `SURVEYOR_PLAN.monthlyCredits` is a documented DIAL at 0 awaiting 'WEB-10', and the webhook's price-id gate grants the 30-credit `monthly_allowance` ONLY when the invoice's first line price equals STRIPE_PRICE_PREMIUM — a Surveyor invoice is explicitly skipped. A Scribe subscription that mints credits needs a new branch there (and a decision: does its allowance also expire at period end, as Cartographer's does, and does it sort ahead of purchased packs in the allocation loop?).","release_free_scribe IS NOT KEYED TO AN INVOCATION (PLAUSIBLE hazard, untested). It is an unconditional `set free_scribe_claimed_at = null where id = p_user and ... is not null`. Two concurrent renders — two dossiers open in two panes, or an open racing a redo — can have invocation A consume the free claim successfully while invocation B fails and RELEASES it, handing the account a second free tab. The transport awaits tabs sequentially within one render, so the single-render case is safe; the cross-render case is not obviously so. Worth a targeted test before the flag is lit.","THE REDRAW IS UNBOUNDED. Nothing caps how many times one epoch may be redrawn; the only guard is an in-memory in-flight Set that a page reload clears. At ~35 credits a redraw this is the largest uncapped spend surface in the feature, and it is exactly the button a frustrated DM presses repeatedly.","BYOK DOES NOT WAIVE CREDITS ON THE SCRIBE (unlike its role on the Surveyor surfaces, where the plan is explicitly `byok: true` with `monthlyCredits: 0`). A BYOK user pays their provider AND 5 credits per tab, and still holds $0.08 per call against the shared global cap. Is that the intended posture for a Scribe subscription, or should a BYOK subscriber render free (and, if so, should their calls stop reserving against the house cap)?","THE 'narrative' (5) AND 'dailyLife' (4) SKUs ARE STILL LIVE at this HEAD — requestNarrative and requestDailyLife both still spend. scribe-render's header says the next commit retires the narrative overlay. Until it lands, the proposal is pricing against a catalogue that still sells the thing the Scribe replaces, and a user can pay for both.","NO ANNUAL PLAN EXISTS TO EXTEND. ANNUAL_FACTOR is 0 and `premium_annual` is absent from create-checkout's PRICE_MAP, so the 'two months free' shape is registration-only on two independent counts. An annual Scribe tier would be the FIRST working annual SKU, not a variation on an existing one.","free_scribe_available HAS NO CALLER, so no surface can tell a user whether their one free render remains — and after the per-tab finding above, the honest copy for that affordance is unclear anyway ('your first tab is free' is not a sentence the product wants to write)."]}}
{"type":"started","key":"v2:3da21fa9aaaf31f706396fa4e713e24289ff79006d8a1e7bc217392239039bdf","agentId":"afc8022687833c725","label":"draft:the-tier-proposal","phase":"Draft"}
{"type":"result","key":"v2:3da21fa9aaaf31f706396fa4e713e24289ff79006d8a1e7bc217392239039bdf","agentId":"afc8022687833c725","result":{"markdown":"# THE SCRIBE TIER PROPOSAL\n\n*File written at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f86a239c-ba8e-464d-a904-e4b70a0c4b2e/scratchpad/proposal/SCRIBE_TIER_PROPOSAL.md` (2,565 words; no em dash or en dash anywhere in the document, verified by grep).*\n\n---\n\nFor the owner's signature. Written by the chair, 2026-09-14, on the owner's word of ~13:2x: \"Because I'm thinking that this could actually be a new paid subscription tier in and of itself.\" Nothing here is decided. Everything here is a default the owner can strike.\n\n## 1. What the Scribe is, as a product\n\nThe Scribe is an AI writer that writes a settlement's dossier in prose, using only what the settlement actually is. It writes once per epoch, when a game master opens that settlement's dossier, and the writing then freezes until the next time the world advances, so a realm of thirty towns only ever writes the towns somebody looks at. Every line it writes is checked by the same instruments that audit the hand-written corpus, and any line that contradicts the settlement is thrown away and replaced by the corpus line, which means the product never gets worse than it is today. Measured without a live key over five simulated runs, 84 to 89 percent of the writing survives that check and about half of one percent of surviving lines still contradict something (SCRIBE-SIM-2026-09-14, RUN 5). It is built and dark: no render has ever run against a real model, no bill has ever been charged, and the flag that turns it on has never been lit.\n\n## 2. What exists today for gating and billing\n\n**There is no tier gate on the Scribe.** The render function's whole admission chain is bot guard, sign-in, session check, per-address rate limit, then `account_is_active`, then credits (`supabase/functions/scribe-render/index.ts:171-192`). `account_is_active` answers \"is this person banned or deleted\", not \"has this person paid\" (`supabase/migrations/057_enforce_account_status_writes.sql:40-60`). Eight other paid AI surfaces check a paid entitlement; the Scribe checks none.\n\n**The database knows only two tiers.** `profiles.tier` is constrained to `free` or `premium` (`001_initial_schema.sql:7`). Migration 139 deliberately refused to widen that constraint and invented a separate entitlement table instead (`139_surveyor_entitlement_and_byok.sql:6-16`). The Surveyor plan follows that pattern: $14.99 a month, sold as a subscription, granting an entitlement and zero credits, kept outside the visible tier ladder on purpose (`src/config/pricing.js:337-361`).\n\n**The price the code charges is not the price the code claims.** Migration 202 and the redraw button both say a render costs five credits (`202_scribe_claim.sql:28-30`; `ScribeRedrawButton.jsx:59`). But the client sends one separate paid call per dossier tab (`src/store/scribeTransport.js:56-64`), and each call runs its own credit spend (`scribe-render/index.ts:300-309`). The built code fires roughly seven to ten tabs on a real town, so one render actually charges thirty-five to fifty credits, which is $4.55 to $10.00 of retail credit. **This is a defect, not a design, and it is the first thing that has to be settled.** The same defect makes the advertised \"free first render\" a free first *tab*: the claim is once per account (`202_scribe_claim.sql:43-61`), so a new user's first render still charges for the other tabs.\n\n**There is no daily render cap anywhere.** Not in the design, not in the rulings, not in the code. A sentence in one chair memory called a daily cap \"temporary economics\"; that cap was never built and appears to have been misread out of the free tier's three-save limit. I am correcting that on the record here.\n\n**What does exist and is proven:** a monthly subscription that mints an expiring credit allowance through the Stripe webhook, wired only to the Cartographer price (`stripe-webhook/index.ts:350-412`); a credit ledger that always spends the monthly allowance before purchased packs (`202_scribe_claim.sql:266-267`); a per-account usage governor with daily and weekly caps that eight surfaces call and the Scribe does not (`144_surveyor_usage_governors.sql`); and a redraw button with no limit on how many times one epoch may be redrawn.\n\n## 3. The options\n\n**Prices assumed throughout:** Claude Opus 5 at $5.00 per million input tokens and $25.00 per million output tokens, cache reads at $0.50 per million, cache writes at the one-hour rate of $10.00 per million, batch processing at half price. These are the rates the design itself cites (claude-api model table, cached 2026-06-24).\n\n**Cost of one render, measured prompt, ten tab calls, town-sized settlement.** The shared instruction brief is 96,795 characters (about 24.2 thousand tokens) and is read on every call; the town's own block adds about 4 thousand; the per-tab volatile material totals about 24 thousand across the render; visible output is about 6 thousand. Writer, first settlement of the hour: about $0.92. Writer, warm hour: about $0.45. The second checking call on each tab adds $0.28 to $0.40. **One render therefore costs about $0.73 warm to $1.32 cold.** Thinking tokens are pinned at high effort, bill at the output rate, and have never been measured; a two to five times multiple on visible output would add $0.15 to $0.60. Every figure here is arithmetic over measured character counts, not a receipt. The design's own cost paragraph (\"cents per settlement, not dollars\") was written before the prompt that now ships and is wrong by roughly an order of magnitude.\n\nFor reference, five credits retails at $1.00, $0.85 or $0.65 depending on which pack the user bought (`pricing.js:48-52`).\n\n### Option A. A standalone monthly subscription\n\n**How a user gets it:** a new Stripe subscription product, added to `SUBSCRIPTION_PRODUCTS` and `PRICE_MAP` in `create-checkout`.\n\n**How the product knows:** a new `scribe_entitlements` table and a `has_scribe_entitlement()` check, copied from migrations 139 and 159. `scribe-render` calls it right after `account_is_active` and refuses politely if absent, in the house style already used by the analyst. The client reads the bit lazily through the same RPC pattern as `useSurveyorEntitled.js`, never from the tier field.\n\n**Cost to the owner:** $0.73 to $1.32 a render, so a subscriber who renders twenty times a month costs $15 to $26 before Stripe's cut.\n\n**What it charges:** chair default $19.99 a month, unlimited renders under a fair-use daily cap of five, with a bring-your-own-key arm at $9.99 that costs the owner nothing per render.\n\n**Cap and free render:** the daily cap becomes real machinery for the first time. The free first render becomes one complete free render per account, used as the shop window.\n\n**Code:** new migration (table, grant and revoke functions, the entitlement check), the gate in `scribe-render`, a render-scoped charge so one render is one event, a new daily counter (nothing existing counts renders), a webhook branch for start and cancel, and the client's lazy read.\n\n**Risks:** unlimited is uncapped cost at a fixed price, and the daily cap is the only brake. A heavy user with three campaigns can lose money every month. The redraw button is uncapped and is exactly the button a frustrated game master presses.\n\n### Option B. An add-on billed per render through the existing credit machine\n\n**How a user gets it:** they do not buy anything new; they spend credits, as today's design intends.\n\n**How the product knows:** no entitlement at all, or at most a check that the account is a paying Cartographer. This is what the code does now.\n\n**Cost to the owner:** the same $0.73 to $1.32.\n\n**What it charges:** five credits a render, which is $0.65 to $1.00. **That is at or below cost.** To clear cost with any margin the SKU has to move to roughly ten credits, which is $1.30 to $2.00. Note that the Scribe has no runtime price lever at all: `get_ai_pricing()` does not list it, so every price change is a migration plus a client deploy (`174_pricing_optimal_margins.sql`; `pricing.js:104-108`).\n\n**Cap and free render:** the credit balance is the only cap; there is no daily cap. The free first render stays as ruling 2 built it, once it is corrected to be a render rather than a tab.\n\n**Code:** the least of the three. Fix the charge so it is once per render, reprice the SKU by migration, mirror it in the client. No webhook change, no new table.\n\n**Risks:** no recurring revenue, and it retires two existing SKUs (narrative at five credits, daily life at four) without replacing their income. The price is not legible: the user cannot be told what a render will cost until the tab count is known. It also answers the owner's question with \"no tier\", which is not what was asked.\n\n### Option C. A subscription with a render allowance, overage in credits\n\n**How a user gets it:** as Option A, a new subscription product.\n\n**How the product knows:** as Option A, an entitlement check at `scribe-render` and a lazy client read. The difference is what happens after the check: the function reads a monthly render counter, and if the allowance is spent it falls through to the existing credit spend, which already works.\n\n**Cost to the owner:** bounded by the allowance. Twenty renders is $15 to $26 of cost; anything past that is paid for at the overage price.\n\n**What it charges:** chair default $19.99 a month including twenty renders that do not roll over, then ten credits a render beyond that, with a bring-your-own-key arm at $9.99 for unlimited renders on the subscriber's own key.\n\n**Cap and free render:** a fair-use daily cap of five renders, tunable by the operator without a deploy. One complete free render per account, kept.\n\n**Code:** all of Option A, plus a render counter reset each billing period, plus the fall-through to the credit spend. The allowance could instead be minted as credits through the proven webhook path, but credits are fungible and would leak into other AI features, so a counted allowance is the better shape.\n\n**Risks:** the most machinery of the three, and two meters to explain to a user. The counter is new and untested. The webhook must grant and revoke correctly or a cancelled subscriber keeps rendering.\n\n## 4. The chair's recommendation\n\n**Option C, at $19.99 a month with twenty renders included, a five-a-day fair-use cap, credit overage, and a $9.99 bring-your-own-key arm.** The reasons, in order of weight.\n\nThe per-render SKU alone is insolvent at the price the record already set. Five credits is worth less than one render costs at every pack rate but the smallest, and the promise in ruling 1 that the pilot would reprice it has never been kept because the pilot has never run. A subscription is the only shape that survives that arithmetic without making the user do mental math before every click.\n\nA subscription also matches what the feature actually is. The billable act is opening a town after an advance, which is bursty: a game master prepares a session and opens six towns in an hour, then opens nothing for three weeks. Metering that by the click punishes exactly the behaviour the product wants.\n\nUnlimited, Option A's shape, is the wrong first bet while the true cost of a render has never been observed once. An allowance with overage gives the same feel to almost every user while keeping the worst month bounded.\n\nThe owner's standing law is to name the highest-ceiling route first. That route is this one with the quality dials untouched: Opus 5, high effort, both checking passes, no trimming of the tab set to save money. If the pilot's receipts say the render costs more than this estimate, the honest response is to raise the price, not to quietly cheapen the writing.\n\n**What it would take, in order.**\n\n*Owner acts first, and nothing moves without them:* an Anthropic API key; applying migrations 201 and 202; the price and shape signed from the list in section 5; a decision on how long past epochs are kept; and last of all, lighting the flag.\n\n*Wave 5a, the pilot.* Light the Scribe for the owner's account alone and render real settlements end to end. This produces the four numbers no simulation can produce: tokens by the model's own count, how much of the prompt is actually served from cache, dollars, and how long a render takes. It also settles whether the second checking call rides the cache or pays full price, which is worth roughly a doubling of the bill.\n\n*Wave 5b, the correctness fix, before any money moves.* Make one render one charge, keyed to the settlement and the epoch, so the free render is a real free render and the price on the button is the price on the bill. Cap the redraw. Close the race in the free-claim release.\n\n*Wave 5c, the blind read.* The design's own ship gate requires the owner to read forty units of Scribe prose against forty of corpus prose, shuffled and unlabelled, and prefer the Scribe or tie. That has never happened. It should happen before the price is set, because it is the only test of whether the tier is worth buying.\n\n*Wave 5d, the tier.* Entitlement table, gate, counter, webhook, client read, checkout SKU, refusal copy.\n\n*Wave 5e, the flag.* Last.\n\n## 5. Decisions only the owner can make\n\nEach carries the chair's default. Strike, change, or initial.\n\n1. **Does the Scribe become a paid tier at all?** Default: yes, build it; it is additive and does not touch what free and paid subscribers get today. ______\n2. **One render, one charge.** Default: yes, corrected before any money moves. ______\n3. **Tier shape.** Default: its own entitlement table, outside the visible tier ladder, following Surveyor's precedent, not shared with Surveyor. ______\n4. **Price.** Default: $19.99 a month, set after the pilot's receipts, not before. ______\n5. **Included renders.** Default: twenty a month, no rollover. ______\n6. **Daily fair-use cap.** Default: five renders a day, operator-tunable. ______\n7. **Overage.** Default: ten credits a render past the allowance. ______\n8. **Free sample.** Default: one complete free render per account, once. ______\n9. **Bring-your-own-key arm.** Default: $9.99 a month, unlimited under the same daily cap, no house credits. ______\n10. **Gallery visibility.** Default: a subscriber's Scribe prose is visible to gallery viewers and marked as machine-written, because a tier with no shop window cannot be sold. ______\n11. **Past epochs kept.** Default: compact rotation, not every epoch forever, to keep save files from growing without bound. ______\n12. **When it ships.** Default: it ships whether or not the hand-written corpus is finished; the corpus stays every subscriber's prose either way. ______\n13. **Quality gate.** Default: the owner sits the forty-unit blind read before the price is signed. ______\n14. **The two retiring SKUs.** Default: narrative and daily life retire at the flag flip, by a new migration, never by editing a shipped one. ______\n15. **Model and effort.** Default: Opus 5 at high effort, unchanged, until the pilot prices the thinking tokens. ______\n\n## 6. What is assumed and unmeasured\n\nEvery dollar in this document is arithmetic over measured character counts and published list prices. No API call has ever been made. The pilot, and only the pilot, can produce: the model's own token counts, the real cache hit rate, real dollars, and wall-clock time per render. Also unmeasured: what thinking at high effort actually costs; whether the second checking call rides the cache or pays full price; whether a real month of play is six renders or sixty, which is the number every allowance in section 5 rests on; whether the owner prefers the Scribe's prose to the corpus, never tested; and whether the daily-life tab's slightly different brief is writing a second expensive cache entry every hour for no reason. The quality figures (84 to 89 percent surviving, about half a percent of lines still contradicting) come from five simulated runs with a Claude seat standing in for the model, not from the live product.\n\n---\n\n## Chair's notes to the parent seat (not part of the owner document)\n\nTwo things surfaced in the drafting that belong in the ledger rather than in the proposal.\n\nFirst, **the daily render cap the 08:5x memory calls \"temporary economics\" does not exist and never did.** It appears in exactly one chair memory sentence and nowhere in the design, the rulings, or the code; it looks to have been narrated out of `TIERS.wanderer.saveLimit: 3`. The proposal corrects it on the record in section 2 rather than silently inheriting it. That memory line needs amending.\n\nSecond, **§12 item 16 (the tier) was pointed at by §11c and never written.** This document is the missing item, and if the owner signs any part of it, §12 should gain item 16 pointing here.\n\nThe single hardest fact for the owner's decision remains the per-tab charge: `renderScribe` loops tabs and each POST runs its own `spend_credits('dossierProse')`, so migration 202's header and `ScribeRedrawButton`'s label are both false about the whole-render price, and no test pins the total. Nothing about the tier's economics can be trusted until that is settled, which is why it is decision 2 and wave 5b.","decisionsForOwner":["Does the Scribe become a paid tier at all? Chair default: yes, build it as an additive tier; it does not change what free or paid subscribers get today.","One render must be one charge. Chair default: yes, fix the per-tab spend before any money moves (today one render charges 35 to 50 credits, not 5).","Tier shape. Chair default: its own entitlement table outside the visible tier ladder, on the Surveyor precedent from migration 139, not shared with Surveyor.","Price. Chair default: $19.99 a month, signed after the live pilot's receipts rather than before.","Included renders. Chair default: twenty a month, no rollover.","Daily fair-use cap. Chair default: five renders a day, operator-tunable. Note: no daily cap exists today and none was ever designed.","Overage past the allowance. Chair default: ten credits a render through the existing credit machine.","Free sample. Chair default: one complete free render per account, once (ruling 2 re-decided, and corrected from a free first tab to a free first render).","Bring-your-own-key arm. Chair default: $9.99 a month, unlimited under the same daily cap, no house credits.","Gallery visibility of Scribe prose (ruling 3's scheduled re-open). Chair default: visible to gallery viewers and marked as machine-written, because a tier with no shop window cannot be sold.","Retention of past epochs (design item 13). Chair default: compact rotation rather than every epoch forever on the save blob.","When it ships relative to the corpus. Chair default: it ships either way; the corpus remains every subscriber's prose.","Quality gate. Chair default: the owner sits the forty-unit blind read (design §10) before the price is signed. It has never happened.","The two retiring SKUs (narrative 5, daily life 4). Chair default: retire at the flag flip by a new migration, never by editing a shipped seed.","Model and effort. Chair default: Opus 5 at high effort, unchanged, until the pilot prices the thinking tokens.","The five standing owner acts, in order: the API key; db push of migrations 201 and 202; the price and shape; item 13; and the flag last."],"assumptions":["Prices assumed: Claude Opus 5 at $5.00 per million input tokens and $25.00 per million output, cache reads at $0.50 per million, cache writes at the one-hour rate of $10.00 per million, batches at half price. These are the rates the design itself cites (claude-api model table, cached 2026-06-24).","Token counts assumed at roughly 4.0 characters per token, the chair's own RUN-1 ratio, applied to measured character counts of brief.md (96,795), town.md (15,964 at town tier) and the per-tab turn files (96,065 total).","Ten firing tab calls per render assumed, from the built firingTabs over SCRIBE_TABS on the pinned town. The design still says six or seven; the code says roughly ten. Pricing used the code's number.","Per-render cost of $0.73 (warm hour) to $1.32 (cold hour) is PLAUSIBLE arithmetic, never CONFIRMED. No API call has ever been made in this programme.","Thinking tokens at effort high are entirely unmeasured and excluded from the headline figure; a two to five times multiple on visible output would add $0.15 to $0.60 per render.","Assumed the product's tier-1 checklist rides the same cached blocks (ruling 31, RUN 3 finding 5). If the built scribe-render sends it self-contained as the harness does, the tier-1 half of the bill roughly doubles.","Assumed a subscriber renders about twenty times a month. Nobody has modelled a real month of play; every allowance number rests on this unmeasured figure.","Quality figures (84 to 89 percent of pools shipping, about 0.5 to 0.7 percent of lines contradicting) come from five simulated runs with a Claude seat standing in for the model, not from live renders.","Assumed the render counter for an allowance is new machinery; nothing in the database counts renders today (surveyor_usage_precheck counts tokens and dollars, and the Scribe does not call it).","The daily render cap referenced in one chair memory as temporary economics does not exist in the design, the rulings or the code; the proposal treats it as a chair misreading of TIERS.wanderer.saveLimit and proposes a cap deliberately instead.","All code citations were read from the lane-scribe dock at 9a8ff7280, not from the main checkout, which is a stale snapshot."]}}
