{"type":"launched"}
{"type":"started","key":"v2:1f354a8bd3b1a33b9f843fd42a1aaca3a83dcf656ec734e27eb0f3482e13f9c4","agentId":"ac77ce7a59f610151","label":"price:TR","phase":"Price"}
{"type":"started","key":"v2:84241913df335df363c7a13d2045b538b9183214eb2132e8a16ef104b4616f88","agentId":"ad4826378b84e1f98","label":"price:WF","phase":"Price"}
{"type":"started","key":"v2:bee2dd01b0b04c23d075778b4ee0cfad77380b04ea72c62db3c8f1bf7025446b","agentId":"a6f9c673d465f8062","label":"price:POP","phase":"Price"}
{"type":"started","key":"v2:3f806c566f56657816c1b2031ec98e22bacbeee073726b28b0a056e8c1b1537c","agentId":"a174c368f476923cc","label":"price:WY","phase":"Price"}
{"type":"started","key":"v2:4934367803f18257888f090a26f1719d686429289960719cce6731330a9f1ce6","agentId":"aa82e67da3850db14","label":"price:IN","phase":"Price"}
{"type":"started","key":"v2:ea0521eca4b77a2b95fa79931045c68745b3953762eb5786d7a427d5f8b84608","agentId":"a07425c376ba8faad","label":"price:ES","phase":"Price"}
{"type":"result","key":"v2:bee2dd01b0b04c23d075778b4ee0cfad77380b04ea72c62db3c8f1bf7025446b","agentId":"a6f9c673d465f8062","result":{"family":"POP","summary":"POP is 8 of 8 UNBUILT, re-confirmed at the build slot `a5876c0ea`: all six flags (`believedMigrationEnabled`, `commonsArcEnabled`, `departureMemoryEnabled`, `calamityArcEnabled`, `roadDramaEnabled`, `moverPermitsEnabled`) return zero hits across src and tests, and neither `departureMemory.js`, `calamityArc.js`, `columnEvents.js`, `arrivalClearing.js`, `believedMigrationRead.js`, `commonsAnswer.js`, `populationEditor.js` nor `populationLines.js` exists. Family price: 22-34 cars. Four things the volume says are WRONG at the slot and change the price, each measured today rather than inherited. (1) POP-4's SUBSTRATE GATE IS DISCHARGED. The volume (DESIGN_FP_ARCH_POP.md §4, POP-4) says the decline double-shed is \"still armed\" and orders STOP-and-report; `src/domain/worldPulse/populationDynamics.js:407-408` now reads \"WAVE P4, THE RECONCILIATION (ODQ §219.3). THE DEFERRAL ABOVE IS DISCHARGED\", and :424-426 rules that when lit this lane writes population ONLY as a conserved transfer, bare decline and bare growth belonging to demographicsKernel.js as \"the design's one writer\". POP-4's \"attribution covers KERNEL deaths only, legacy decline sheds keep the existing voice\" carve-out collapses — every lit death is a kernel death — which removes a whole split-attribution arm and drops POP-4 from ~3-4 cars to 2-3. The volume's own check-first order has fired; the implementer must re-read, not inherit. (2) NO POP FILE IS SIZE-BLOCKED, and the volume's raw-line budgets mislead. The enforced ceiling is 800 EFFECTIVE lines (eslint max-lines, skipBlankLines+skipComments) and `scripts/.size-baseline.json` holds no entry for any POP-touched file. Measured effective today: demographicsMigration 380/800, spatial/migration 247, commonsVoiceKernel 214, demographicsPlans 375, demographicsResponses 232, demographicsHerald 262, migrationKernel 264, heraldRouting 273, demographicsLadder 101, simulationRules 352, subsystemRowsVirtual 284, populationDynamics 294, demographicsObservation 96. The tightest is `display/settlementRumors.js` at 555/800 (245 headroom), and that is POP-6's file. The estate comments at roughly 1.7-2x, so the volume's raw figures (e.g. demographicsMigration 658) read as near-ceiling when the enforcer sees less than half that. No decomposition car is owed anywhere in this family. (3) THE FLAG-MINT BILL IS 35, NOT 28, AND ITS OWN TEST TITLE LIES. The programme doc tells every #32 packet to carry \"the 28-literal fourth obligation\"; `tests/domain/contributionLedgerShape.test.js:49` is still TITLED \"holds the triple bijection at 28\" while its literals at :119-120 assert `ENGINE_GATED_VIRTUAL_RULE_KEYS` and `VIRTUAL_SUBSYSTEM_ROWS` both `toHaveLength(35)`. A POP implementer grepping for 28 edits nothing and reds at the terminal gate. The manifest is 35 members at this slot (never the volume's five, never the 20 at `a6fd6395`), so each POP flag mint is six exact edit sites in one commit: the manifest array, an AUTHORED row in a `subsystemRows*` leaf, `VIRTUAL_RULES` in position at `tests/domain/subsystemRowsVirtual.test.js:255`, the `LANE_LEAVES` entry, and both 35-to-36 literals. C-POPF-1's struck pending door is real: `VIRTUAL_PENDING_RULE_KEYS` is frozen empty at `subsystemRowsVirtual.js:595` and `PENDING_MANIFEST_KEYS` empty at the walker's :288, and the BACKLOG is full at 17/17 shrink-only with `commonsVoiceEnabled` and `assizeEnabled` both still in it. An authored certification row in this estate is not a line — the neighbouring `conquestDoctrineEnabled` row runs ~60 lines of aliveness prose, stateKeys channels and three named invariants with checks. (4) THE WORKER CEILING THE BRIEF WARNS ABOUT IS NOT POP'S CEILING. `WORKER_BUNDLE_CEILING_BYTES = 1404493` at `tests/build/generationWorkerLazy.test.js:78` governs `generation.worker.js`, the settlement-GENERATION entry. POP touches no generator: `git grep -ln 'demographicsMigration|commonsVoiceKernel|demographicsLadder|demographicsHerald' -- src/generators src/store` returns empty, and `generation.worker.js` imports only generationRequest.js and the living-content seam. Every POP byte rides `advanceInterval.worker.js` (`src/workers/advanceInterval.worker.js:23` imports the worldPulse leaf; Vite builds each worker entry as its own rollup build) and the lazy `engine` chunk — and NO byte-ceiling test exists for the advanceInterval worker at all. POP-6 is the single exception worth watching, because `display/settlementRumors.js` is imported by React components (OutputContainer.jsx, OverviewTab.jsx, RumorsTab.jsx) and so can reach the first-paint closure that `vendorPdfLazy.test.js:565` caps at `CLOSURE_BUDGET_BYTES = 1_048_000`. Substrate the family reuses is genuinely strong and re-verified: the reception machinery is landed (`MIGRATION_REFUSALS` at demographicsMigration.js:108, the refusal mint at :231-242), `destinationMenuFor` is still the one menu builder at :276 with viability at :293 and the spare read at :302, the unconditional landing survives at :464-478, the 300-tick conservation identity AND its leak-detecting negative control are live at `tests/domain/demographicsMigration.test.js:211/:234` for POP-1 and POP-5a to EXTEND rather than write, `columnOf`'s conditional carry-through idiom is a working precedent at `spatial/migration.js:543-558`, `moverPermitted` and the 5x6 table already exist at `demographicsLadder.js:79/:209` with exactly one consumer at `demographicsResponses.js:244`, GR-3's term family is LANDED (`peaceTermsCatalog.js:250/:254`, reader `treatyEnforcement.js:301`), and SP-2's `believedConditionsEnabled` plus `migrationRumorsEnabled` are both already in the 35-member manifest, so POP-1's lit-preconditions are satisfied and nothing blocks it today. One structural cost the volume understates: no POP coupling desk leaf exists (the registry at `couplingRegistry.js:40-50` composes Trade, Grammar, Info, Espionage, Encounters, War), so POP-1 must mint `couplingRegistryPopulations.js` and its composition edit — though the POP prefix is already inside the closed twelve-prefix alternation (DESIGN_FP_ARCHITECTURE.md:2645), which spares the four-edit-site admission ceremony. Every wave lands DARK behind its own flag with the four-fence dormancy set; the only wave that moves same-seed output AT LANDING is POP-6, whose J-POP-12 live-kind prose rows are a declared shift needing a recorded owner ruling.","waves":[{"wave":"POP-1 — THE BELIEVED ROAD (believedMigrationEnabled)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"The family's anchor and its largest wave. Spec demands two new leaves (believedMigrationRead.js ≤250, arrivalClearing.js ≤300), a seam swap plus a clearing insertion in demographicsMigration.js, TWO new conditional fields carried through columnOf, and an extension of the frozen DEMOGRAPHIC_COLUMN_CLASSES set. RF-1 is the expensive truth and it holds at the slot: columnOf (spatial/migration.js:543-558) is a WHITELIST that rebuilds every record as {originId,destId,arrivals,departTick,arrivalTick,...travelClass?} and runs at three sites, so believedBand and the returning class are silently deleted by the first same-tick M4 dispatch unless carried through — pinned by a round-trip-THROUGH-ENQUEUE test, not a save/load test. RF-2 compounds it: isDemographicColumn (:483-485) fails closed toward M4, so a mis-classed return column is landed by releaseArrivals and narrated as a flight. The pin list is the heaviest in the family — omniscience collapse, the gold rush pin, conservation RE-DERIVED and re-executed at 300 ticks over both new arms, never-outruns-the-road, the wrong-lane negative with an executed mutant that reverts the class-set extension and watches M4 steal the column — plus three manifest-entered mutants, the six-site flag mint, a NEW coupling desk leaf, and the beliefSubjectsGrewSincePop1() source-scan tripwire. Natural car split: (1) the class extension plus both columnOf carry-throughs with the RF-1/RF-2 pins, no flag; (2) believedMigrationRead.js, the seam swap, the flag mint and the four fences; (3) arrivalClearing.js, the return column and the conservation re-derivation; (4) disappointment and letters home with the gold-rush and omniscience pins; (5) the coupling desk leaf, the three CW-0 rows and the SP-2 tripwire; (6) the mutant battery and the full gate if the sittings run long. Chair ruling Q1 (extend the closed set with 'returning' rather than a marker field) is signed under the grant and recorded in the wave's ledger row, as is Q3's acceptance that a homeward column wears the migration_flight rumour face for v1.","dependsOn":"Nothing blocks it today — all three lit-preconditions are satisfied at the slot. demographicsEnabled exists as a virtual key (simulationRules.js:1027, declared false in full_simulation); SP-2's believedConditionsEnabled LANDED at SP-B and is in the 35-member manifest; migrationRumorsEnabled is also manifested. POP-1 is itself a hard predecessor: DESIGN_FP_ARCHITECTURE.md:2043-2044 slots WY-4 in PHASE 6 IMMEDIATELY AFTER POP-1 because the shared columnOf amendments compose BY ORDER, POP-1 first, and POP-5a's events[] carry-through composes onto the same surface. The first pass corrected the old claim that POP-1 blocks LG-9..12 (PROGRAMME-32-34.md:371): the reception machinery landed, and what LG still needs is the smaller individual suspicion refusal, which can ride LG-9 itself.","ownerGate":"None for the landing — signed under the 14:4x grant. The family-wide SECOND DECLARED LIGHTING is owner-gated (sitting item 1's §764.3 amendment), but that gates lighting, not this wave.","outputMoving":"NO at landing — dark behind believedMigrationEnabled with the four-fence dormancy set plus the lit-mutant control, the dormancy golden captured at the pull seam BEFORE wiring, and stage-1 bytes proved identical. YES at lighting.","reuse":"The reception machinery is landed and CONFIRMED (MIGRATION_REFUSALS at demographicsMigration.js:108, the refusal mint at :231-242). destinationMenuFor is still the ONE menu builder (:276) with tierViabilityOf at :293 and the spare/inbound read at :302, so the belief arm swaps one resolver and leaves the over-send guard truth-side. The unconditional landing survives at :464-478 exactly where the clearing inserts. THE BIG ONE: the 300-tick conservation identity AND its leak-detecting negative control already exist at tests/domain/demographicsMigration.test.js:211/:234 — POP-1 EXTENDS a working instrument instead of writing one. The travelClass conditional-spread at :558 is a working precedent to copy verbatim. migrationRumors.js's carrier filters only origin≠dest and arrivals>0 with no direction or class filter, so homeward carriage is automatic once a return column exists (S29 answered). The POP couplingId prefix is already admitted in the closed twelve-prefix alternation (DESIGN_FP_ARCHITECTURE.md:2645), sparing the four-edit-site ceremony.","workerKb":"ZERO to generation.worker.js (ceiling 1,404,493 B) — verified: no generator or store module imports any POP file. Two new leaves at ~550 effective lines land ~22-28 KB of raw source, ~9-12 KB code-only, roughly 5-7 KB minified into advanceInterval.worker.js and the lazy engine chunk. No byte-ceiling test exists for the advanceInterval worker, so no measured ceiling is threatened. It CANNOT live in a lazy chunk behind its flag: the gate is a runtime `=== true` read inside the pulse tick, so the bytes ride the worker dark or lit and dead-code elimination cannot shed them. Never in first paint (the engine-absent-from-closure pin holds worldPulse out of the 1,048,000 B budget)."},{"wave":"POP-2 — THE COMMONS ARC (commonsArcEnabled)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Extends a live kernel rather than building one, but the pins are the family's most fixture-expensive because every single one must seed live grievance state before it asserts. commonsVoiceKernel.js is roomy (214 effective, 586 headroom) and takes the refusal rung, the jump rule, LEGIT_DIP widened from four slots to five and the targetKind/targetRef fields; demographicsPlans.js and demographicsResponses.js take the live-rung read and the refused_by_the_commons vocabulary word; and the genuinely new work is commonsAnswer.js (≤200), THE MISRULE ANSWER — a receipted seat verb with legitimacy repair and stressor stand-down. That answer is real new capability, because the built assize coupling reaches only named-accused and corruption-kind grievances (S22), so a reversal pin written only on the corruption fixture is vacuous BY CONSTRUCTION and the wave owes both arms. Two new herald kinds (commons_refusal, commons_answered) must register in ALL THREE walkers in the same commit, copying the existing three-kind shape exactly — WHAT_PHRASES in display/settlementRumors.js, EXPECTED_VOICE at tests/domain/impactKindWalkers.test.js:60 (where commons_petition sits at :152 as a deliberate null), and heraldRouting.js:289 — and the wizard-news skew prohibition must be restated at the mint, since S20's beat ids are wizard_news.*-shaped and the observation classifier files every one of them under moverFamily knowledge. Cars: (1) the rung, the dip re-author and the jump-rule pin; (2) commonsAnswer.js with both reversal arms and the revealed-only pin; (3) the plan/levy refusal pricing with the priced-not-forbidden and refused-levy fixtures; (4) the three-walker registration and the flag mint; (5) the mutant battery if the answer arm runs long.","dependsOn":"commonsVoiceEnabled must be lit to extend its ladder — and that flag sits in the walker's BACKLOG (17/17, shrink-only) with no certification row, which POP-7 is slated to clear under Q2. demographicsEnabled lit for the plan-refusal arm ONLY, declared arm by arm. The corruption input premise is VERIFIED live (corruptionSignalOf requires corrupt===true, ousted!==true, timesExposed>0, or a live exposedCorruption entry), so the revealed-only pin has real ground. ⚠ VERIFY-AT-BUILD: the volume's levy-shortfall collision cite (warDeployment.js:1259) is DEAD; the muster moved, and deployedPopulation is handled at warHomeCosts.js:354-362 — the seam is READ, never edited, and the shortfall must arrive as a smaller REAL muster number upstream of the WR-4 conscript-share read, never a parallel multiplier.","ownerGate":"None for the landing — signed under the grant.","outputMoving":"NO at landing — dark behind commonsArcEnabled, with the dormancy fence INCLUDING the skip-path shape, and the jump-rule pin proving lit-no-target time-to-riot equals dark tick-for-tick against S19's one-rung-per-dwell baseline. YES at lighting.","reuse":"The whole three-rung ladder, its DWELL_TICKS 3 and its rung thresholds 0.35/0.55/0.75 are live at commonsVoiceKernel.js:59-64; LEGIT_DIP's four-slot array at :63 is exactly the shape the wave widens. The three-kind walker registration is a copy-the-shape job with a working template in all three files. commonsVoiceKernel runs BEFORE the assize in the same tick (assizeKernel.js combined mover) so the petition it deposits is answerable that tick — the ordering POP-2 needs already exists and needs no kernel edit.","workerKb":"ZERO to generation.worker.js. One new leaf at ≤200 effective plus roughly 150 effective of in-place edits: ~8-12 KB raw source, ~3-5 KB minified into advanceInterval.worker.js. The two WHAT_PHRASES rows are the only bytes that can reach the main graph, and they are two authored strings. No lazy chunk is possible — same runtime-gate reason as POP-1."},{"wave":"POP-3 — DEPARTURE MEMORY (departureMemoryEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"The cheapest of the new-state waves and the cleanest shape in the family: ONE new writer leaf (departureMemory.js ≤300 carrying the write, the decay and the spent-tie), two feed-site calls of ≤15 lines each at the P2 landing and the M4 arrivals, one new spatialLedgers key, and the flag mint. No seam surgery, no new rng, no display work. What makes it two-to-three cars rather than one is the L4 lifecycle bill, which is exactly this owner's most-bitten bug class and must be discharged per path: CREATE at four feed sites all through the one writer, READ on the kin-pull arm and the remembrance crossings, PERSIST with a JSON round-trip and import validation of all three closed sets (kinBand, causeClass, rememberedAs), REGENERATE proving a same-seed replay re-derives identical bands with zero PRNG, UNDO through the pulse ring, and MIGRATE as a no-op because the key is new and drop-when-empty. Add the freeze-at-death and reopen-at-resettlement arms extending S27's lifecycle laws, the BYTE-ABSENT drop-when-empty proof (no migration ⇒ no key at all), the writer/reader spelling pin that boots the REAL writer, the kin-cap negative proving the pull is never unreachable and never outweighs crisis push, and the adoption double-count negative showing traditions' influx checkpoints identical lit versus dark. Cars: (1) the leaf, the two feed sites, the flag mint and the lifecycle/dormancy pins; (2) the kin-pull arm, the spent-tie loop and its convergence pin; (3) the mutant pair and the coupling read API if the first two run long.","dependsOn":"demographicsEnabled lit — nothing else. Independent of POP-1 and POP-2 and schedulable in parallel with either. It PRE-PINS a seam toward FP-COUPLINGS' diaspora-reconquest pair by fixing a read API in band words only, with no raw ticks crossing the seam. The ledger API needs no registration work: getSpatialLedger/setSpatialLedger are generic (distanceRead.js:131-132), there is no key registry to edit, and the v5 stateKeys census walks spatialLedgers one level so a new key is census-visible for free (S32, VERIFIED).","ownerGate":"None for the landing — signed under the grant. RF-4 fences one temptation: the ledger does NOT write mass_migration stressors; the existing reads at capacityModel:633 and stressorGates:476 keep their non-arrival writers, and the arrival-side coupling is CR-FP-6's owner-queue deferral, not this wave's.","outputMoving":"NO at landing — dark behind departureMemoryEnabled, and stronger than the usual fence: the drop-when-empty proof is BYTE-ABSENT, so a world with no migration carries no key at all and serializes identically to the pre-wave engine. YES at lighting.","reuse":"The generic spatialLedgers accessor pair means zero registry plumbing. The census sees the new key automatically (S32). traditions/relations.js already captures influx from the live column ledger with ADOPTION_THRESHOLD 0.12 over a 3-year window, so the double-count negative has a real existing checkpoint to compare against rather than a fixture to invent. settlementLifecycleKernel.js's TERMINAL_DWELL and resettlement-as-privileged-birth laws are live and simply extend to the pairs.","workerKb":"ZERO to generation.worker.js. One leaf at ≤300 effective plus two call sites: ~12-15 KB raw source, ~4-6 KB minified into advanceInterval.worker.js. Zero main-graph bytes (no display surface until the dossier panel, which rides the governed projector). No lazy chunk — the writer is called inside the pulse tick."},{"wave":"POP-4 — THE PLAGUE ARC (calamityArcEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"REPRICED DOWN from the volume's own framing because its substrate gate has since discharged — this is the finding that most changes POP's cost. The volume orders the implementer to treat the decline double-shed as OPEN and to confine attribution to KERNEL deaths while legacy decline sheds keep the existing departure/decline voice, with a STOP-and-report if a ruling has landed. The ruling HAS landed: populationDynamics.js:407-408 now reads 'WAVE P4, THE RECONCILIATION (ODQ §219.3). THE DEFERRAL ABOVE IS DISCHARGED', and :424-426 rules that when the engine is lit this lane writes population ONLY as a conserved transfer, because bare decline and bare growth are deaths and births by another name and belong to demographicsKernel.js, 'the design's one writer'. So the split-attribution arm collapses: under a lit engine every death is a kernel death and attribution has ONE source, not two. Remaining work is a new leaf (calamityArc.js ≤300 for staging, prune and attribution reads), a burial-line generalization in demographicsHerald.js (262 effective, 538 headroom), one new spatialLedgers key, and the flag mint. The defining pin is ZERO-NEW-MORTALITY — lit and dark death totals identical — which is now easier to write honestly than the volume expected, since there is no second shed to reconcile against. THE BELLS PIN (a plague year without famine yields the sickness line, not the hunger line, with the food gate preserved for hunger) and the stamp-monotonicity/no-zombie-arc pins carry the rest. Cars: (1) the leaf, the ledger key, the flag mint, the lifecycle and monotonicity pins; (2) the burial-line generalization with the bells pin, the attribution negatives including the mixed case, and the first-market-day walk-backward fixture; (3) the mutant pair plus the named-cast pin under arc conditions if needed.","dependsOn":"demographicsEnabled lit. Independent of POP-1, POP-2 and POP-3. Its evidence source is already built and needs no edit: pestilenceKernel.js is a genuine traveling front that materializes the ONE canonical disease_outbreak stressor, and the burial line is food-gated today in demographicsHerald.js — POP-4 READS that evidence and mints no disease of its own. ⛔ The implementer must re-read populationDynamics.js §P4 at build time rather than inherit the volume's 'still armed' sentence; the volume's own check-first order has already fired and its answer is the opposite of what the volume assumes.","ownerGate":"None for the landing — signed under the grant. One artefact travels to the owner untouched rather than as a gate: the 193-year zero-fire counterforce fact is carried to the owner's tuning table UN-retuned, since tuning is owner-signed under THE PROMISE.","outputMoving":"NO at landing — dark behind calamityArcEnabled, with the ZERO-NEW-MORTALITY pin proving lit and dark death totals identical, so even a lit arc moves narration and not population. YES at lighting, and only in voice.","reuse":"pestilenceKernel.js (305 raw) supplies the whole evidence stream — arcs open on existing outbreak evidence, so the wave stages and narrates rather than simulates. demographicsHerald.js's QUANTITY_BANDS and its existing food-gated burial line are the surface the generalization edits. The generic spatialLedgers accessor and the free census visibility apply exactly as for POP-3. The named-cast mercy machinery (pool = before − named) is live at demographicsKernel.js and composes structurally under arc conditions.","workerKb":"ZERO to generation.worker.js. One leaf at ≤300 effective plus ~60 lines of herald edits: ~12-16 KB raw source, ~4-6 KB minified into advanceInterval.worker.js. Negligible main-graph bytes. No lazy chunk — staging runs inside the pulse tick."},{"wave":"POP-5a — ROAD DRAMA (roadDramaEnabled)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"Priced above its file count because it mints the program's ONE new rng surface, and in this estate a new PRNG fork family is the expensive kind of change: columnEvents.js (≤350) draws on per-column keyed forks `column:<id>`, every draw must be accounted against the wave-E draw-accounting instrument, and PRNG call order IS stream identity under THE PROMISE, so the accounting is not paperwork. The conservation bill is the second cost: the identity must be EXTENDED-UNWEAKENED, with every event kind closing into it, split sums exact, and the existing 300-tick pin gaining event fixtures — a leaked half-column is the failure mode and its mutant is mandatory. The third is THE RE-ROUTE PIN, which must be written as a ROUTE-CHOICE assertion with populationLegCost output byte-identical, because the leg cost is grade and season only: belief re-ranks routes, physics does not, and a pin that let danger price into legCost would quietly make this wave output-moving. Add the lost-column pin in both arms (M4's graves line; P2's inference then honest resolution), the no-new-mortality negative on P2 — S10 is explicit that P2 columns carry ZERO road mortality BY DESIGN — the split double-count negative at the menu's spare read, RF-1's round-trip-through-enqueue for the events array, and the flag mint. Cars: (1) columnEvents.js with the typed banded kind set and the fork accounting; (2) the columnOf carry-through, the RF-1 pin and the conservation extension; (3) the lost-column inference in both arms with the re-route byte-identity pin; (4) the mutant pair and the flag mint if the fork accounting runs long.","dependsOn":"POP-1 FIRST, and strictly: the events carry-through composes onto the same columnOf whitelist POP-1 amends, and DESIGN_FP_ARCHITECTURE.md:2043-2044 states the shared columnOf amendments compose BY ORDER. Also demographicsEnabled lit, migrationRumorsEnabled lit (already manifested), and the spatial column lane live — ⚠ VERIFY-AT-BUILD the exact predicate migrationActive uses, which the volume leaves open. Seam 3 declares the FP-TRADE dependency honestly: `tolled` ships DORMANT unless J4 charters expose a toll read, the dependency is recorded in the event-kind registry comment, and a source-scan pin asserts POP-5a prices no toll number of its own.","ownerGate":"None for the landing — signed under the grant.","outputMoving":"NO at landing — dark behind roadDramaEnabled. The re-route pin's byte-identity arm is what keeps it honest: even lit, populationLegCost output is unchanged, so physics never moves and only ranking and narration do. YES at lighting.","reuse":"columnOf's conditional-spread idiom (spatial/migration.js:558) is the exact template for the events carry-through, and POP-1 will already have exercised it twice. The 300-tick conservation identity plus its leak-detecting negative control at tests/domain/demographicsMigration.test.js:211/:234 is the instrument this wave extends rather than writes. The rumour carrier is class- and direction-agnostic (S29 VERIFIED), so event-bearing columns reach the rumour lane with no carrier edit. demographicsPlans.js already imports the keyed hash01 idiom, which is the fallback for every POP draw that is NOT this declared fork surface.","workerKb":"ZERO to generation.worker.js. One leaf at ≤350 effective plus ~15 lines in spatial/migration.js: ~14-18 KB raw source, ~5-7 KB minified into advanceInterval.worker.js. Zero first-paint bytes. No lazy chunk — the event pass runs per-tick inside the column lane."},{"wave":"POP-5b — THE PERMIT TABLE GOES LIVE (moverPermitsEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"The cheapest wave in the family by construction: NO new leaf at all, because every read composes existing pure functions. The work is four read insertions at the calling surfaces (levy at the muster, institution seeding, trade/caravan assignment, and promotion), one gate narrowing at destinationMenuFor's viable read so the wired nonviable refusal narrows to the grades the TABLE refuses (J-POP-11 — failing MAY receive, which is how a failing settlement recovers), and two treaty-input reads at the founding mint and the destination lane. Budget ≤30 lines per touched file against 500-plus lines of headroom everywhere. The cost is not code, it is fixtures: BOTH arms must be proved per consumed column on REAL corpora with a seeded failing-town fixture, the recovery in-migration pin must demonstrate J-POP-11's whole point, and the levy/consent double-bind fixture must produce ONE muster receipt naming TWO distinct reasons (grade-refused AND commons-refused) without collapsing them — its mutant is exactly that collapse. Add the treaty reachability pins, the flag mint, and the CW-0 rows CPL-3/CPL-8/CPL-17/CPL-18 landing this commit. Cars: (1) the four read insertions with the gate narrowing and both-arm corpora pins; (2) the two treaty reads with required reachability pins and the coupling rows; (3) the double-bind fixture and the mutant pair if the corpora work runs long.","dependsOn":"demographicsEnabled lit. GR-3 is LANDED, which COLLAPSES seam contract 2 to a single state and REMOVES the degraded arm entirely: migration_right and settlement_provision are live at peaceTermsCatalog.js:250/:254 with the reader at treatyEnforcement.js:301, so POP-5b's reachability pins are REQUIRED from birth rather than declared-degraded (C-POPF-7, and DESIGN_FP_ARCHITECTURE.md:2636 row 23 orders exactly that for the GR-3→POP-5b grant read). Two ceremonies the volume describes are STRUCK and must not be performed: the catalogGrewSinceWr10 'must not be the one that trips it' ritual is unfireable because POP-5b touches TERM_CATALOG never and only READS live terms, and the degraded-pair posture is history. ⚠ VERIFY-AT-BUILD the muster site — the volume's warDeployment.js:1259 conscription cite is dead. INT-1's seatBooks coupling (CPL-18) is declared dark behind BOTH flags with its reachability pin written INTERIOR-side.","ownerGate":"None for the landing — signed under the grant.","outputMoving":"NO at landing — dark behind moverPermitsEnabled, and the narrowing at destinationMenuFor is inside the gate so the dark path keeps today's refusal behaviour byte-identically. YES at lighting, where a failing settlement starts receiving in-migration it is refused today.","reuse":"The largest reuse ratio in the family. The 5x6 permit table and moverPermitted already exist, authored and closed on both axes, at demographicsLadder.js:79/:209, with the grade set {viable, failing, evacuating, remnant_occupied, remnant_empty} and the authored comment declaring failing.destination=true because 'that is how it recovers'. Exactly ONE consumer exists today (demographicsResponses.js:244's foundingPermitted) and the table is already directly pinned in tests/domain/demographicsWorldsHand.test.js:533-585, so the wave inherits a tested pure function and only wires it. The promotion response's gate at demographicsResponses.js:296-297 gains a grade read beside the existing no_next_tier and no_headroom. GR-3's landed term family means the treaty reads resolve at call time with no term literal spelled in the gate composition.","workerKb":"ZERO to generation.worker.js. No new leaf and no new state: roughly 100-150 effective lines of reads spread over four files, ~4-6 KB raw source, ~1.5-2.5 KB minified into advanceInterval.worker.js. The smallest byte cost in the family. No lazy chunk needed or possible."},{"wave":"POP-6 — THE HOPEFUL HALF (rides its machinery's flags)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"Priced high not for code volume but because it is the only POP wave that MOVES SAME-SEED OUTPUT AT LANDING, and the law forces that shift into its own commit. Two new leaves (populationLines.js ≤400 for the mint list a-f, and populationEditor.js for the per-tick per-settlement significance budget over SP-6 classes) plus eleven authored WHAT_PHRASES rows, three-walker registrations, and the R-28 repair. The editor is architecturally careful: it wraps the RETURN of demographicNewsEntries rather than editing the kernel, so the kernel's direct-mint site keeps its line and S3's herald bypass is left intact. R-28 is CONFIRMED STILL OPEN and has MOVED — the volume cites demographicsHerald.js:257, but at the slot it is :420, `headline: quantityWords(departures || unplaced) + ' left ' + origin`, and the departures=0 arm still says 'left' about people who never left. That is live honesty work, not a no-op, and the wave owes R-28-class honesty pins on EVERY headline verb it mints. The J-POP-12 discipline is what sets the car count: prose rows for LIVE kinds land WR-0c-style in their OWN commit, as a disclosed same-seed prose shift, with goldens re-recorded ONLY under a recorded ruling. Measured at the slot, tier_change and steading_founded have ZERO hits in display/settlementRumors.js and are falling back to de-underscored engine tokens today, so authoring their phrases changes printed prose on existing seeds. Cars: (1) populationLines.js and the mint list; (2) populationEditor.js with THE FLOOD PIN and its quiet-world-single-line negative; (3) the dark-kind prose rows and the three-walker registrations; (4) the LIVE-kind prose rows as the disclosed-shift commit, alone; (5) the R-28 repair with the honesty pins; (6) the totality walkers and the dwindling-window arithmetic if they do not fit.","dependsOn":"Rides the machinery flags of POP-1 through POP-5b rather than minting its own, so it lands LAST among the building waves — its mint list spans the whole family's kinds and its totality walkers assert the program's FULL kind roster, which cannot be written until the roster exists. ⚠ VERIFY-AT-BUILD the dwindling-window arithmetic: the volume flags a possible mismatch between populationHistory's 12 rows and the 13-tick season window, and orders the implementer to read the receipts stream and REPORT which, not to guess. ⚠ Check the R-33 register lane at build time: narration honesty is pinned here, but the behaviour fix belongs there. The Herald's existing ridden kinds (hungry_gap, migration_flight) keep their own gates.","ownerGate":"⭐ THE ONE GATED WAVE IN THE FAMILY. The dark-kind rows, the editor and the R-28 repair are all signed under the grant. But the LIVE-kind prose rows are a DECLARED same-seed output shift on existing worlds, which is the class CLAUDE.md non-negotiable 10 and the §764.3 amendment reserve to the owner: the shift must be stated explicitly, its cause recorded, and the goldens re-recorded only under a recorded ruling. Land the other cars under the grant and put the disclosed-shift commit on the sitting.","outputMoving":"⭐ YES AT LANDING for the J-POP-12 live-kind prose rows — the only POP wave of which this is true, and the reason that commit is separated. tier_change and steading_founded print de-underscored engine tokens today; authoring their phrases changes same-seed prose. Every other car is NO at landing (dormancy per underlying flag). The R-28 repair is also arguably output-moving in narration and should be classified explicitly in the commit story rather than assumed benign.","reuse":"The fallback arm is documented and live at display/settlementRumors.js:111 ('Any UNKNOWN token is REFUSED by whatPhrase(), never de-underscored into prose'), so the wave authors into a working closed-vocabulary machine. The three-walker registration has a proven template in the commons kinds (WHAT_PHRASES, EXPECTED_VOICE at tests/domain/impactKindWalkers.test.js:60 with its deliberate nulls, heraldRouting.js:289). demographicsHerald.js's QUANTITY_BANDS and its floors are live. SP-6's significance classes already exist for the budget to range over.","workerKb":"ZERO to generation.worker.js. ⚠ THE ONE POP WAVE WITH A FIRST-PAINT BYTE QUESTION: display/settlementRumors.js is imported by React components (OutputContainer.jsx, OverviewTab.jsx, RumorsTab.jsx), so eleven authored prose rows can reach the main graph against CLOSURE_BUDGET_BYTES = 1,048,000 (vendorPdfLazy.test.js:565). The rows are authored strings, so expect well under 1.5 KB minified — small, but this is the wave that must MEASURE rather than assume, and it is also the tightest file in the family at 555/800 effective (245 headroom), comfortable for ~25 effective lines of rows. The two leaves at ~500 effective add ~20-25 KB raw source, ~6-9 KB minified, split between advanceInterval.worker.js (the editor and mint list) and the display graph (the phrase rows)."},{"wave":"POP-7 — CONVERGENCE INSTRUMENTATION (no flag)","status":"UNBUILT","carsLow":2,"carsHigh":4,"reason":"No flag and no new behaviour — this is the family's ACCEPTANCE HARNESS, and the volume is explicit that the program is DONE when its envelopes hold on the owner-ordered soak and not before. Work is endings-mix envelopes over the seventeen producer-mapped tokens, six health metrics (the rush-to-bust band, the no-permanent-rush horizon, the rung pyramid, THE PARITY METRIC, memory-decay stationarity, and conservation UNCHANGED with every flag lit), the convergence certification rows with dispositive stateKeys channels, and additive null-on-dark censuses joining measureRealmDemography. Every envelope carries its own mutant negative control, which is the bulk of the cost: an envelope that cannot fail is the false-instrument class this estate refuses. Two clarifications that keep the price down. First, the SIX per-flag certification rows do NOT land here — C-POPF-1 struck the pending door, so each flag's authored row lands in ITS OWN wave's commit; POP-7 owns the convergence rows and the channel declarations, not the six. Second, the commonsArcEnabled row declares stateKeys channels ONLY, because S20's wizard-news ids would skew any moverFamily claim. Q2's judgment adds one real item: POP-7 also lands the commonsVoiceEnabled certification row and shrinks the walker BACKLOG by one — CONFIRMED still needed, the backlog is full at 17/17 shrink-only with commonsVoiceEnabled in it. Check-first per the R-28 discipline: if the certification register lane lands that row first, POP-7's item is a no-op. Cars: (1) the envelopes and health metrics with their mutant controls; (2) the convergence certification rows, the channel declarations and the commonsVoiceEnabled backlog shrink; (3) the v5 census additions to measureRealmDemography; (4) the soak read-out and acceptance write-up, which is a sitting of its own if the envelopes do not hold first time.","dependsOn":"ALL SEVEN preceding waves, and then some — the envelopes are measured with EVERY POP flag LIT, so POP-7 cannot complete until the SECOND DECLARED LIGHTING has happened. That lighting is owner-gated (sitting item 1's §764.3 amendment), and the lighting wave §898-§906 is spent, so every flag this family mints lands dark and waits. The soak itself is owner-ordered. POP-7's instrument work can be BUILT ahead of the lighting; only its acceptance read-out must wait.","ownerGate":"⭐ TWO, and they are different things. The wave's own landing is signed under the grant. But (a) the lighting its envelopes require is the owner's §764.3 amendment, and (b) the soak is owner-ordered. Separately, CR-FP-6 is already RULED at DESIGN_FP_ARCHITECTURE.md:2938 — 'ACCEPT NO — deliberately deferred beside J-POP-14; ONE owner ruling covers both arrival-side couplings; this goes on the owner queue, not into any wave' — so Q4's arrival-side mass_migration coupling is a recorded owner-queue item and POP-7 must NOT quietly absorb it. Record it as deliberately deferred rather than re-find it.","outputMoving":"NO — an instrument act throughout. It mints no flag, adds no behaviour, and its censuses are additive and null-on-dark. Its conservation envelope must show the identity UNCHANGED with every flag lit, which is itself the assertion that the family moved narration and not arithmetic.","reuse":"measureRealmDemography is live at demographicsObservation.js:103 and already wired into scripts/audit/behavioral-observation.mjs, with 704 lines of headroom — the v5 rush, commons and arc censuses are additions to a working instrument, not a new one. The v5 stateKeys census walks spatialLedgers one level, so departureMemory, calamityArcs, the commons rung field and the column event arrays are census-visible for free (S32, VERIFIED). The 300-tick conservation identity and its negative control are the existing spine of the conservation envelope.","workerKb":"ZERO to generation.worker.js and near-zero to advanceInterval.worker.js — the census additions are a few hundred lines in demographicsObservation.js (~2-3 KB raw, ~1 KB minified). The envelopes and health metrics live in tests and scripts/audit, which ship in no bundle at all. The certification rows live in src/domain/certification, reached from the audit path rather than first paint. This is the one POP wave with effectively no bundle cost."}],"firstCar":"POP-1 car 1: THE COLUMN CARRY-THROUGH, landed with no flag at all. Amend `src/domain/spatial/migration.js` alone — extend the frozen `DEMOGRAPHIC_COLUMN_CLASSES` at :474 from `['refugee','voluntary']` to `['refugee','voluntary','returning']` (Q1's ruling, chair-signed under the grant, recorded as a decision in the wave's ledger row so it is never re-litigated as drift), and add TWO conditional carry-throughs to `columnOf` at :543-558 beside the existing `travelClass` spread — `believedBand` for POP-1 and `events` for POP-5a — each in the present-and-valid-else-no-key idiom the file already uses, so a record lacking the field is rebuilt byte-identically. Land it first, before any belief resolver or clearing exists, for four reasons that are all measured rather than argued. First, RF-1 is the family's quietest killer and it is TRUE at this slot: `columnOf` is a WHITELIST that rebuilds every record as exactly `{originId, destId, arrivals, departTick, arrivalTick, ...travelClass?}`, and it runs at THREE sites — `enqueueColumns`, which rebuilds the WHOLE ledger on every M4 dispatch tick, `releaseArrivals` at :629-641, and the demographic stage-0 read — so a frozen departure belief written by a later car would survive serialization perfectly and then be silently deleted by the first same-tick M4 dispatch, with no error anywhere. Second, that failure is provable NOW with executed mutants and no new behaviour to confound them: strip either spread, run a column through a real enqueue rebuild, and watch the field ghost; restore by `cmp`, never by a checkout-family command. The pin must be a round-trip-THROUGH-ENQUEUE assertion, not a save/load test — save/load passes while the bug is live, which is exactly why the volume calls it out. Third, RF-2's wrong-lane hazard rides the same edit and must be closed in the same commit: `isDemographicColumn` at :483-485 FAILS CLOSED toward M4, so until `'returning'` is inside the closed set a homeward column is landed by M4's `releaseArrivals`, credited with M4's own refugee story and never routed through the arrival clearing — conservation attribution breaks and a homecoming is narrated as a flight. The negative control is an executed mutant that reverts the class-set extension and watches M4 steal the column. Fourth, this is the surface WY-4 composes onto: `DESIGN_FP_ARCHITECTURE.md:2043-2044` slots WY-4 in PHASE 6 immediately after POP-1 precisely because the shared `columnOf` amendments compose BY ORDER, POP-1 first, and POP-5a's `events` carry-through wants the same spread — so landing both fields in one careful commit pays the shape tax once for three waves. The car carries no flag, so it owes none of the six-site mint bill (and must not touch the 35-to-36 literals at `tests/domain/contributionLedgerShape.test.js:119-120` — note the test's TITLE still says 28 and is stale; read the literals, never the title). Its gate is the existing conservation instrument re-executed unchanged: the 300-tick identity at `tests/domain/demographicsMigration.test.js:211` with its leak-detecting negative control at :234, which must stay green at exactly its current reading, because at this point nothing behavioural has changed and any movement in that number means the carry-through is not byte-neutral on records that lack the fields. Headroom is not a concern — `spatial/migration.js` measures 247 effective lines against the 800 ceiling and holds no size-baseline entry — and the byte cost to `advanceInterval.worker.js` is on the order of a few hundred bytes, with ZERO bytes to `generation.worker.js`, whose 1,404,493-byte ceiling no POP wave touches at all. Output-moving: NO, and provably so — every existing world's columns lack both new fields, so every rebuild is byte-identical and the car is a pure shape widening."}}
{"type":"started","key":"v2:769848521bee58048a769df9cf2ad6395dae5305a3217b459ef42cd78f39326a","agentId":"a7f3c891f5d9a1bf5","label":"price:WC","phase":"Price"}
{"type":"result","key":"v2:3f806c566f56657816c1b2031ec98e22bacbeee073726b28b0a056e8c1b1537c","agentId":"a174c368f476923cc","result":{"family":"WY — wayfare (the inhabited roads, the physical flows, the mirror law)","summary":"All 13 WY rows (12 waves + WY-8a) re-confirmed UNBUILT at the NEW slot a5876c0ea: all eight flags plus `kmScale` return zero hits across src and tests, and none of the seven greenfield modules the volume names (modeSpeeds, severityDurability, flowRegimeContract, moverTidings, armyCaravanSeizure, armySupply, the five projections) exists. Priced at 34-55 cars for the family. Three measurements at the slot change the price and must ride the first ledger row. (1) ⛔ THE ONE HARD BLOCKER IS A SIZE RATCHET, NOT A GATE: `src/domain/worldPulse/roadsKernel.js` measures 838 effective lines against a FROZEN baseline of exactly 838 in scripts/.size-baseline.json — ZERO slack — and eight of WY-3's nine F3 stamps land in that file, so WY-3 owes a behaviour-identical pure-leaf decomposition car (the npcAgency/settlementStrategy idiom with the stripped-block declaration diff) or net-zero `;`-joined stamps before a single stamp can land. (2) ⛔ THE WORKER CLAUSE DOES NOT BIND WY AT ALL, AND THAT IS A CONFIRMED NEGATIVE: a static-import BFS from `src/workers/generation.worker.js` reaches 227 modules and ZERO of `domain/worldPulse/` or `domain/spatial/` — the generation worker at its 1,404,493 B ceiling never sees WY engine code. Every WY engine wave lands in `src/workers/advanceInterval.worker.js`'s graph (560 modules, 375 worldPulse + 25 spatial), which carries NO byte ceiling in tests/build. WY's real byte constraint is elsewhere: `src/store/mapSlice.js` is EAGER first paint (store/index.js:49 statically imports it) and the first-paint raw closure sits at ~1,047,205 against CLOSURE_BUDGET_BYTES = 1,048,000 — about 795 B of margin — so WY-0's and WY-9's store-side bytes are the ones that must be measured, not the worker's. (3) THREE VOLUME PREMISES DRIFTED AND ONE IS ALREADY DISCHARGED: `WY` is ALREADY in CHARTERED_VOLUME_PREFIXES (tests/domain/couplingRegistry.test.js:108, one const feeding both the regex and the toEqual), so §5b item 7's prefix-admission obligation is PAID and no WY wave carries it; `hasMartialRecord` is now EXPORTED from martialReadiness.js:256 beside readinessOf/experienceOf/rustOf and consumed at WarTab.jsx:48, so C-WYF-2 item 2 is part-discharged and what remains is collapsing opportunism.js:122's duplicate local spelling; and `WarFaithTab.jsx` no longer exists — the surface is now WarTab.jsx / FaithTab.jsx / WarFaithDesk.jsx, so WY-7's and WY-8b's host-file budget notes must navigate by symbol. Two premises re-confirmed EXACT: the F3 write census is still NINE writes in TWO modules (roadsKernel.js lines 487/510/706/720/731/749/812/1028 plus roads/state.js:189; seaRoads.js only reads), and the WarFaithSection phantom census is still TWELVE files. One premise loosened: C-WYF-3's CONDITION_BANDS rename is called \"measured FREE (zero external consumers)\" but armyStrength.js:237 re-exports it inside ARMY_STRENGTH_PHRASES, which tests/domain/display/forceComposition.test.js:12 imports — still near-free, no longer literally zero. Only TWO of the thirteen rows move output at landing (WY-0's deletions, WY-7's declared unifications) and one moves it by record presence rather than flag (WY-8b's F6/F7); the other ten are dark behind their own flag or dark by data. No WY row is a push, deploy, migration, deletion or spend: every field is drop-when-absent with MIGRATE: none, so the §2a batch signed under the 14:4x grant clears the whole family.","waves":[{"wave":"WY-0 the lawful canvas (surface, no flag)","status":"UNBUILT","carsLow":1,"carsHigh":2,"reason":"Two cheap halves that should not share a commit. Car 1 is the registration machinery: NEW tests/ui/mapLayerManifest.walker.test.js scanning DEFAULT_LAYERS keys against LayersPanel rows against the overlay mounts against MapLegend entries in both directions, with the structural config-key exemption class (non-boolean value implies config, so the *Filter family and regionalMinSeverity exempt themselves) and an anti-vacuity floor MEASURED AT BUILD from the boolean row set — DEFAULT_LAYERS carries 23 declared keys at mapSlice.js:88, so the r1 'thirteen' is wrong on both axes as the volume warns; plus C-WYF-4's cure, since travelersFilter is CONFIRMED still undeclared (written on demand by setLayerFilter with no declared-key guard, read at LayersPanel.jsx:95-103 and TravelersLayer.jsx:40) — declare `travelersFilter: null` in DEFAULT_LAYERS or give setLayerFilter toggleLayer's guard, and make the walker's census domain the union of declared keys and source-scanned setLayerFilter-written keys; plus the persisted-layers merge pin tracing the spread order so a new default key reaches old campaigns with its default. Car 2 is the deletions: TravelersLayer chevron/flip/tooltip (C1) and the WarFaithMapOverlay deployment arrows (Q3), both halves together, plus the one-time move of the two shipped test files travelersLayer.test.jsx and roadsTravelersGeometry. Splitting them keeps the owner-visible half revertible on its own and lands the census surface every later WY surface wave is scanned by first. One car if the chair is content to land the deletions with the walker.","dependsOn":"Nothing. The volume calls it 'buildable NOW; the program's first commit in either lane' (§5 LANE S) and SOL_QUEUE §2 Lane B orders it as the early opener. It is the prerequisite for WY-9 and, through the walker, for every later layer edit — toggleLayer's silent no-op on undeclared keys is what makes the census mandatory before any layer work.","ownerGate":"Signed under the 14:4x grant — no field, no flag, no migration. But the Q3 WarFaithMapOverlay arrow deletion is a chair ruling (§7 Q3, DELETED under the ambiguity law) that is VETOABLE with the owner's veto window open UNTIL THIS WAVE LANDS, so car 2's ledger row must name it as owner-visible and name the two moved test files as a declared one-time shift against directive 2c. Not a push, deploy, migration, deletion of data, or spend.","outputMoving":"YES at landing, and it is one of only two WY rows that are. There is no flag: the chevrons, flips, tooltips and deployment arrows simply stop drawing on the canvas the day car 2 lands, and two shipped test files move with them. Car 1 alone moves zero output — a walker, a declared default and a merge pin.","workerKb":"Zero generator code; nothing WY-0 touches is in the generation worker's 227-module graph. The binding budget is the FIRST-PAINT EAGER closure, because store/index.js:49 statically imports mapSlice.js: the raw closure measures ~1,047,205 against CLOSURE_BUDGET_BYTES = 1,048,000, about 795 B of margin. Declaring travelersFilter costs roughly 20 B eager (mapSlice.js:109's own comment prices that key at ~20 B); the walker and the deletions are lazy-side or test-side and should net NEGATIVE. Measure the closure at the publishing commit rather than inheriting this figure.","reuse":"DEFAULT_LAYERS (mapSlice.js:88, 23 keys), setLayerFilter (mapSlice.js:328-330), LayersPanel.jsx (364 eff), MapLegend.jsx (180 eff), WorldMapStage.jsx (390 eff), TravelersLayer.jsx (154 raw), WarFaithMapOverlay.jsx (229 raw) all exist; the both-direction walker idiom is estate-standard (premiumGateSingleSource is the named model). No file WY-0 touches carries a size-baseline entry, so the component layer ceiling of 600 governs and all of them sit well under it."},{"wave":"WY-1 the scale charter (engine, no flag — data-gated by spatialDigest.kmScale)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"Car 1 is the mechanism: NEW src/domain/spatial/modeSpeeds.js (~60 eff), the kmScale arm plus the spectrum surface on distanceRead.js's hopWeeks/calibration, buildSpatialDigest stamping the datum and campaignSpatialCanonize supplying it — creation-supplied on first canonize, PRIOR-DIGEST CARRY-FORWARD on every receipted re-canonize, riding the same prior-worldState read the canonize body already performs for the biome flag. Car 2 is the instrument family, which is where the money is: the legacy-absent byte-identity golden, the re-canonize ROUND-TRIP (the volume's own r2 HIGH pin, THE PROMISE class — canonize scaled, founding edit, receipted re-canonize, kmScale identical, week spectra identical, spatialCanonVersion bumped), the absolute-arm fixture (two scales one geometry giving different week spectra with law-M's floor holding at both), the shrink-only spectrum-derivation lint census over the named mover-estate consumers, the MAX_MARCH_WEEKS clamp receipt, the invariant pin that march cap stays below siege max age in both worlds, and the three mutants (kmScale-ignored, carry-forward-severed — the ghost-write mutant that is this user's most-bitten class — and floor-severed). Car 3 is the creation-flow disclosure screen, a Lane B surface slice that can ride separately or wait. Priced at the low end only if the chair lets the golden family share the mechanism commit.","dependsOn":"§5b item 3 lands it at the PHASE 1 tail after FP #7 SP-E, which the first pass records as LANDED/BUILT (tests/helpers/kindPoolWalker.js, components/map/heraldFeed.js). So WY-1 is unblocked today. Seam 6 binds it to the transit kernel's documented gradeMultiplier socket (namedPersonTransit.js:44) — ONE position model, no second speed floor anywhere, or law M's walker reds.","ownerGate":"Signed under the grant. The MODE_SPEEDS km/week table and the default kmScale are §8 TUNING rows, owner-signed AT THE SOAK and raw-authored until then, never entering proposedSoakBands before ratification — that is the soak's signature, not a build gate. The §1a flag-home supersession (decoupled from portOpportunityEnabled, chair-ruled, vetoable) rides this wave's ledger row, and §5b item 9's two pointer sentences in DESIGN_WAR_RULINGS_ARCHITECTURE must land in the same fold commit so the two binding volumes never disagree.","outputMoving":"NO at landing — dark by DATA, not by flag, which is the stronger form. Every existing world lacks spatialDigest.kmScale and the legacy arm is the world's identity forever, not a migration, so the absent-datum arm is the dark arm by construction and the byte-identity golden is its proof. Output moves only when the creation flow ships the datum, which is car 3's screen.","workerKb":"Roughly 3-5 KB of source into the advanceInterval (pulse) worker graph, which carries NO byte ceiling — distanceRead.js and spatialDigest.js are both already in that 560-module graph, so the delta is the new module plus the arms. ZERO bytes into the generation worker (confirmed absent from its graph). The eager first-paint closure must still be re-measured, because a store-reachable static edge into spatial/distanceRead.js was severed once before (the vendorPdfLazy WAVE-D seam repair) and a new one would re-drag it into the 795 B margin.","reuse":"hopWeeks already exists at distanceRead.js:639 (468 eff, well under the 800 domain ceiling, so the volume's 'budget the leaf split if near ceiling' does NOT fire); buildSpatialDigest at spatialDigest.js:580 (415 eff); campaignSpatialCanonize.js is tiny at 98 eff; MAX_MARCH_WEEKS: 52 is already the clamp at armyTransit.js:59 with its termination docstring at :662-664 already naming the below-SIEGE_MAX_AGE invariant this wave pins; the gradeMultiplier socket is already injected at namedPersonTransit.js:44. The only greenfield is modeSpeeds.js."},{"wave":"WY-2 the durable truth (engine, severityDurableRumorsEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"Car 1 is the mechanism as REBUILT in r2 on the keyed-hash01 discipline: NEW src/domain/spatial/severityDurability.js (~90 eff carrying both the rung multipliers AND the closed-outcome-table degrade — ONE hash01 roll per hop over the fork-key tuple, zero conditional draws, the name-swap index by sub-interval rescale), plus the two-arm gate at rumorNetwork.js's weathering site (dark arm = fork plus degradeTelling VERBATIM; lit arm = no fork, the table, both distorting modes, hiFi never weathering), plus the score-to-rung derivation pinned BY INDEX with the grave-collision anchored negative — magnitude faint/notable/serious/grave against SEVERITY_LADDER glancing/telling/grave/ruinous, never joined by word. Car 2 is the stream-identity instrument, which is the expensive half and the reason this is not one car: THE FORK-KEY-SET CENSUS proving the module's rng touches enumerate to exactly the one dark-arm `rumor-organic:` family in BOTH flag states (fork isolation IS the stream guarantee; the r2 note that the draw-count pin is DELETED as wrong-mechanism must be honoured or a builder re-lands a refuted instrument), plus THE OTHER-CONSUMER STREAM GOLDEN (a non-rumor consumer's same-seed stream byte-identical across flag states), plus the four-fence, the lit-mutant, the ruinous-near-lossless multi-hop fixture, the glancing-drifts fixture, the plant-bypass pin on the disinfo lineage, and the big-lie emergent-exposure fixture executed. The two mutants (multiplier-inverted; a sequential-draw plant in the lit arm) split across both.","dependsOn":"§5b item 3 lands it in PHASE 3 immediately before ES-1, so the manipulation seam composes — and ES-1 is already LANDED/BUILT (espionageGate.js:80-81), so that composition point has passed and WY-2 slots freely. Seam 5 binds it to the IN estate; seam 7 says WY-2's durability floor is the thing ES-I's manipulation bypasses, priced at the plant site ES-side. Nothing blocks it today. WY-5 depends on it by MECHANISM (one weathering spelling, two namespaces) even though §5b orders WY-5 later.","ownerGate":"Signed under the grant. The severityDurability rung multipliers are a §8 tuning row, owner-signed at the soak. No field is minted (this wave appears in no §2a row), no key, no migration.","outputMoving":"NO at landing — dark behind severityDurableRumorsEnabled, with the dark arm running degradeTelling VERBATIM and the other-consumer stream golden standing as the executed proof that no neighbouring consumer's same-seed stream moved.","workerKb":"Roughly 3-4 KB into the pulse worker graph (rumorNetwork.js is already there); NO ceiling binds it. Zero bytes into the generation worker, zero into the eager first-paint closure.","reuse":"rumorNetwork.js already holds degradeTelling, pickBetterTelling and SEVERITY_LADDER (it is one of five files spelling them) and measures 364 eff against the 800 ceiling, so the two-arm gate has ample headroom and needs no decomposition. hash01 and the rng.fork discipline are estate-standard; the fork-census idiom already exists in the tree (pulseKernel's 22 fork sites are pinned by count in the size-baseline's R-BLD-10 note). Only severityDurability.js is greenfield."},{"wave":"WY-3 the mover bodies (engine, caravanBodiesEnabled; slices 3a fields+normalizers, 3b cadence+contract)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"The family's heaviest engine wave and the one with a measured structural blocker. ⛔ THE BLOCKER: F3 must be re-stamped beside every legArrivalTick write, and the r2 census of NINE writes in TWO modules is CONFIRMED STILL EXACT at a5876c0ea — eight in src/domain/worldPulse/roadsKernel.js (lines 487, 510, 706, 720, 731, 749, 812, 1028) plus consumeMissionRecall at src/domain/roads/state.js:189, with seaRoads.js holding three READS only. But roadsKernel.js measures 838 effective lines against a FROZEN baseline of exactly 838 in scripts/.size-baseline.json — zero slack, and eslint reds the file the moment it grows — so eight added stamps cannot land as eight added lines. That buys a car of its own: either a behaviour-identical pure-leaf extraction in the settlementStrategy/npcAgency idiom (stripped-block declaration diff proving every top-level declaration byte-identical, all exports still exported from the head so no consumer import path moves, strict-neutral measurement, then the baseline number LOWERED) or `;`-joined net-zero stamps in the EP-1/EP-3 idiom, which is achievable across eight assignment sites but fragile and should be argued in the ledger row rather than assumed. Beyond that: car for slice 3a (F1/F2 at the commodityFlow dispatch write where both values are already in hand, AND its OWN arrive-loop reconstruction taught the two fields in the SAME commit — commodityFlow.js:558's in-file comment documents the exact loss class, 'a multi-tick smuggle run loses smuggle:true on reconstruction', so departTick and path are carried across in-transit ticks precisely as smuggle is, plus any load-time spatialLedgers normalizer the build-day census finds); car for slice 3b (the quantization cadence — sub-MIN_SHIP persistent deficits accruing dispatch debt into a MIN_SHIP caravan every ceil interval with doubled credit, lit-only — plus NEW src/domain/spatial/flowRegimeContract.js at ~120 eff carrying the per-good-class regime declaration, 2i's bidirectional test shape, v1 M6a-physical rows and grain rows pre-pinned toward TR-4's T7 and tripwired); car for the walkers and scans (the TREE-WIDE legArrivalTick pairing walker navigating by symbol so a tenth write anywhere without its twin reds, plus THE MARKER-TRUTH SOURCE SCAN proving no render-path module calls a route solver — C4's lie fence, guarding WY-10 from birth); and a car for the pin family (four-fence, lit-mutant, the serialize/regen/undo/import round-trip for the shipment family THROUGH at least one in-transit tick so the arrive-loop rebuild is inside the loop — measured absent and landed here, not inherited from TR-4's promise — the conservation identity extended-unweakened with the debt bookkeeping proven OUTSIDE the stock identity, and the cadence pin). Four mutants: re-solve-at-render, cadence-starved, a tenth legArrivalTick write without its twin, and a field-dropping arrive-loop plant.","dependsOn":"Nothing upstream inside WY — it is the engine lane's true root. It must land BEFORE FP #26 TR-4 (still UNBUILT: no grainFlow, spec rows only) so grain inherits the caravan body, and it owes TR-4 a one-line delta recording that the shipment round-trip pin LANDED HERE and TR-4 verifies and extends rather than re-lands. Downstream it blocks WY-5, WY-6, WY-8a and WY-10. ⚠ Collision: commodityFlow.js is TRADE's estate — CHECK-GIT-FIRST and serialize with the TR lanes.","ownerGate":"Signed under the grant: F1, F2 and F3 are §2a rows on EXISTING records, all drop-when-absent, all MIGRATE: none (old records simply render no caravan markers rather than lying ones). SOL_QUEUE row 18b's rule applies — an unsigned §2a row stalls exactly one wave, never the program. The quantization cadence bounds and dispatch-debt cap are §8 tuning at the soak. Not a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — everything is lit-only behind caravanBodiesEnabled, and F3 is explicitly inert bookkeeping under the same flag with readers preferring-it-when-present and falling back to today's reconstruction when absent, so old records degrade to exactly today's render. The four-fence plus lit-mutant is the proof.","workerKb":"Roughly 6-9 KB into the pulse worker graph (commodityFlow.js, roadsKernel.js and roads/state.js are all already in it), plus whatever a roadsKernel leaf extraction relocates WITHIN that same graph at net zero. NO ceiling binds the pulse worker. ZERO bytes into the generation worker and zero eager first-paint delta. The size ratchet, not the bundle, is this wave's byte constraint.","reuse":"The dispatch write already computes both F1 and F2 values (dispatchPick.route.path is in hand — no new solve); commodityFlow.js measures 448 eff against the 800 ceiling with room; the arrive-loop's own in-file smuggle:true comment is the exact carry-through precedent to copy; supplyShipments.js exports no record normalizer at this HEAD (it is the supply-LINK module, not the shipment-record keeper — the volume's premise holds, re-verify by symbol at build); commodityFlow's ONE stock-write seam already exists and is the door WY-6's prize credit and WY-8a's supply debits both reuse. Only flowRegimeContract.js is greenfield."},{"wave":"WY-4 the population cargo + the fence (engine, flowMigrationPhysicalEnabled + migrationCargoEnabled)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Two flags, two §2a rows, four mutants and a cross-family writer, which is why it does not compress. Car 1 is the fence itself: flows.js populationFlows gains the lit physical fork sending columns through the M4 writer path with travelClass ABSENT so M4 lands them and the RF-2 mutex stays untouched — and it must cover BOTH APPLY ARMS per the r2 correction, the auto arm at mint AND the proposal-accept arm where a candidate over MIGRATION_PROPOSAL_FRACTION applies at DM acceptance, which becomes the dispatch tick under the lit fork, receipted. Car 2 is migration.js's columnOf carry-through for F4/F5 in the SAME commit, composing with POP-1's own carry-through by order, with the THROUGH-ENQUEUE round-trip pin (RF-1: the enqueueColumns whole-ledger rebuild plus releaseArrivals plus the P2 stage-0 read, never merely save/load). Car 3 is the faith credit arm in the religiousContest writer family per the Q1 BLEND ruling — arrival pulls the destination mix by arrivals/(destPop+arrivals), renormed, through religionState's own writer family — additionally gated on the faith-spread flag pair WITH the legacy-mirror handling in both spellings exactly as the UI writes, with the WF×POP tellable executed and occupationFaithPull as the shape precedent. Car 4 is the fences and censuses: NEW exactly-one rows in flowRegimeContract.js (netted flow_migration XOR physical columns per settlement per tick in BOTH flag states, and the same walker finally covering populationDynamics' bare spatial fork), THE PRODUCER-OVERLAP CENSUS measured at build because M4-shed and DISPLACEMENT_STRESSORS triggers are NOT disjoint by construction, and the DEDUP pin with deterministic precedence where the fence yields to a same-tick M4 dispatch. Car 5 is the remaining pin family (four-fence twice, M4 conservation extended-unweakened with fence columns inside the identity, the omniscience collapse golden) plus the four mutants: both-arms-live, double-send, cargo-stripped, wrong-lane.","dependsOn":"⛔ AFTER POP-1, which the first pass records UNBUILT (believedMigrationEnabled zero hits; every POP-1 src hit is a DS-POP-1 dossier id, a grep trap) — §5b item 3 places WY-4 in PHASE 6 immediately after FP #42 POP-1 because the two share columnOf amendments that compose BY ORDER. That is WY-4's one hard predecessor and it is outside the family. It also needs flowRegimeContract.js, which arrives from WY-3. Downstream: WY-5 and WY-10 wait on it. Note the first pass's finding that POP-1 is no longer a hard predecessor of #33 does NOT relax this: POP-1 stays the WY-4 ordering anchor in as many words.","ownerGate":"Signed under the grant: F4 (faithCargo, banded composition, band words only) and F5 (causeReceipt) are §2a rows on the existing MigrationColumn record, drop-when-absent, MIGRATE: none. Q1's BLEND shape is a chair ruling (§7), VETOABLE, and the HEADS re-founding is recorded owner-elective — an implementer never re-rules it silently. The faith-credit pull cap is a §8 tuning row at the soak. Seam 4 binds the credit to write ONLY through religionState's writer family or the single-writer census reds.","outputMoving":"NO at landing — two flags, both dark, with the omniscience collapse golden standing as the dark-arm proof and a four-fence on each flag. The lit arms move output only at lighting.","workerKb":"Roughly 4-6 KB into the pulse worker graph (flows.js at 121 eff and migration.js at 246 eff are both already there and both have wide headroom under the 800 ceiling); no ceiling binds it. Zero generation-worker bytes, zero eager first-paint delta.","reuse":"flowRegimeContract.js comes from WY-3 — WY-4 adds rows, mints no module. The M4 writer path, travelClass, the RF-2 mutex, MIGRATION_PROPOSAL_FRACTION, columnOf, enqueueColumns/releaseArrivals and the religionState writer family with occupationFaithPull as the single-writer precedent all exist. WY-4 is entirely amendment work on existing writers — no greenfield module at all, which is why its cost is pins and censuses rather than code."},{"wave":"WY-5 the carried word (engine, moversCarryNewsEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":4,"reason":"Car 1 is NEW src/domain/spatial/moverTidings.js (~150 eff): at caravan arrival and column release, reconstruct the origin's telling set as-at departTick from arrival records with arrivalTick <= departTick — fully derived, ZERO new fields, which is why this wave appears in no §2a row — and offer it to the destination through pickBetterTelling at hopCount+1 with ONE mover-hop weathering roll reusing WY-2's closed table under its own `mover-tidings:` key namespace, keyed hash01, zero sequential draws (the r2 law: one mechanism, two namespaces, never a second weathering spelling), with THE TAP-ORDER LAW binding — movers carry what places TOLD them, the outbound performance fidelity, except events the origin itself witnessed, which carry at the root's fidelity — and severity-durability composing. Mounted at the two arrival sites, own-flag-before-host-gate. Car 2 is THE GOING-DARK READ: lastWordFrom(dest, origin) over rumor arrivals plus the M6d tally yielding the computed fact, a Herald kind `no_word_from` with the FULL FIVE JOINS per L6 and the news address law, plus the dossier and popup line, plus the kind's own walker file per seam 5. Car 3 is the pin family: four-fence, lit-mutant, the fidelity-injection pin (a mover-carried telling arriving fresher or more complete than ambient at equal hop depth), arrival-order determinism (the merge is total-ordered), the tap-order-inversion mutant, and THE BLACKOUT TELLABLE — one cut route starving grain AND news on the same interruption, executed ACROSS WY-3's physical lane, which is a cross-wave executed fixture and the reason the high end reaches four.","dependsOn":"§5 orders it after WY-3 (caravan bodies) and POP-1/WY-4 (column estate settled), and §5b item 3 lands it after WY-4 — so it inherits WY-4's POP-1 block. It additionally depends on WY-2 BY MECHANISM rather than by queue position: the mover-hop weathering roll IS WY-2's closed table under a second namespace, and building WY-5 before WY-2 would mint the second weathering spelling the r2 revision exists to forbid. Seam 5 asks that IN-5's desk routing be honoured (IN-5 is UNBUILT — informationNews.js:24 says 'seventh is IN-5's to mint'), which shapes where the kind routes but does not block the mint.","ownerGate":"Signed under the grant — zero fields, zero keys, so there is not even a §2a row to sign. The mover-hop weathering magnitude is a §8 tuning row at the soak. Not a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — dark behind moversCarryNewsEnabled, and the Herald kind lands dark with it, four-fenced. Output moves at lighting, when arrivals start injecting fidelity and the going-dark sentence becomes sayable.","workerKb":"Roughly 5-6 KB into the pulse worker graph; no ceiling binds it. Zero generation-worker bytes. The dossier and popup line is display-layer and rides the lazy chunks, so zero eager first-paint delta.","reuse":"WY-2's severityDurability module and its closed outcome table are reused wholesale under a second key namespace — that reuse is the wave's central design constraint, not an optimisation. pickBetterTelling already exists in rumorNetwork.js; the arrival ledgers the going-dark read scans already exist; the Herald five-joins machinery is built out across heraldJoinMolds.js, heraldIndex.js, heraldCausalGrammar.js and demographicsHerald.js, so the new kind rides an established mint path. Only moverTidings.js is greenfield."},{"wave":"WY-6 the prize arm + the pairs table (engine, caravanSeizureEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"Car 1 is the arm: NEW src/domain/spatial/armyCaravanSeizure.js (~120 eff, E14 as specced in §4), the armyTransit normalizer taught F8, and the home-credit release at column arrival through commodityFlow's ONE stock-write seam — never a same-tick teleport, which is what the timing pin executes. Car 2 is NEW tests/lint/encounterPairs.walker.test.js, and its subtlety is the whole reason it deserves its own commit: it is BORN SEEING FIFTEEN. E15 (covert operative against host settlement watch, resolved in espionageGauntlet.js) was admitted by CR-ES-6 and MOUNTED at ES-2 a phase earlier, so this walker VERIFIES that row and never re-closes the table at fourteen — a walker authored against fourteen reds a resolver the estate consciously admitted, which is the wave's own tripwire firing on its own author. ES-2 is CONFIRMED LANDED (espionageGauntlet.js:7-10 per the first pass, and `caught_spying` is live in the tree), so the E15-PRESENT pin's resolver-side scan PASSES rather than waits — the volume's contingency clause does not fire. The walker scans in both directions: an untabled resolver reds, and a table row without a resolver reds. Car 3 is the Herald kind `caravan_seized` with the five joins plus the pin family: four-fence, lit-mutant, conservation extended (carried = prize + lost, prize credits home at arrival), both conscience arms executed (lawful loots little, evil near-all), the passage negative (an allied caravan passes untouched), the co-location snap pin with both sides Math.round documented, and the two mutants (sixteenth-arm, prize-teleport).","dependsOn":"After WY-3 (it reads `path` for co-location, which only exists once F1/F2 land). §5b item 3 additionally lands it after FP #26 TR-4, which is UNBUILT. Seam 7 pins the E15 relationship with ES-2, already landed. ⚠ Collision: the home credit moves real stores through commodityFlow's seam, so CHECK-GIT-FIRST against the TR lanes.","ownerGate":"Signed under the grant: F8 (prizeCargo, an integer map, conserved) is a §2a row on the existing armyTransit record, drop-when-absent, MIGRATE: none. ⭐ No tuning signature is owed — §8 states in terms that the seizure conscience bands ride the EXISTING seizureTake table, no new bands, reuse. Not a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — dark behind caravanSeizureEnabled with a four-fence and a lit-mutant; the Herald kind lands dark with it. The E14/E15 walker is an instrument and moves nothing.","workerKb":"Roughly 4-5 KB into the pulse worker graph (armyTransit.js and the espionage modules are already in it); no ceiling binds it. Zero generation-worker bytes, zero eager first-paint delta.","reuse":"The existing seizureTake conscience table (explicitly reused, no new bands); armyRecordOf at src/domain/spatial/armyTransit.js:327 is the normalizer to teach, and armyTransit.js sits at 340 eff with wide headroom; commodityFlow's one stock-write seam already exists; espionageGauntlet.js and its `caught_spying` outcome are already in the tree so the fifteenth row is verifiable on day one; the Herald five-joins mint path is established. Only armyCaravanSeizure.js and the walker are greenfield."},{"wave":"WY-8a the supply train (engine slice of WY-8; armySupplyEnabled, F9)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"The heaviest WY engine slice after WY-3, because it is the one that writes into the war estate and mints a cross-program coupling. Car 1 is the writer: NEW src/domain/worldPulse/armySupply.js (~140 eff) homed beside supplyWebWarfare.js per C-WYF-2's re-home ruling — src/domain/war/ does not exist and is NOT minted for a one-module family, which the slot confirms (armyStrength.js lives at src/domain/display/) — as THE ONE WRITER for every F9 mutation, carrying the march-dispatch mint that debits origin stores through commodityFlow's one stock-write seam (surplus-gated, so a bled home sends empty wagons and the manifest says so) and the per-tick consumption with time/march booking to `consumed`. Car 2 is the coupling arm and it carries the wave's sharpest contract: low supply accrues the ARMY'S OWN accumulatedAttrition and drags the readiness band — the field-side twin of WR-4's home-front drain — and NEVER the realm's warExhaustion, because §3f's three-exhaustions labeling IS the contract (the army's wear is 'condition', the realm's is 'the home front', the road's is 'march fatigue'). Car 3 is the REST arm: resting in a town debits the HOST's real stores and credits supplyCargo, with the OCCUPIED arm minting a requisition receipt that feeds occupation resistance through the war program's existing writer (giving WR-10's durable fragility a feeding schedule) and the FRIENDLY arm booking an ally-hosting burden; reinforcement columns carrying supply as cargo from origin surplus fold in here. Car 4 is the pin family: four-fence, lit-mutant, the conservation identity (debited = carried + consumed, EVERY tick, executed), the empty-wagons pin, the requisition-resistance coupling executed and receipted with the dark arm byte-identical, the F9 round-trip THROUGH a consumption tick, the starvation tellable (cut road, supply falls, condition degrades in weeks, executed), and the three mutants — conjured-supply, free-lunch, and a second-writer plant that must red a single-writer SOURCE SCAN. Car 5 if the twin-note pairing in supplyWebWarfare.js and the closed-goods-vocabulary fence (food = grain class, armor = crafted class, no new resource type, and this slice mints NO sixteenth encounter arm) are landed separately.","dependsOn":"After engine WY-3 — it reuses the stock-write seam and the cargo idiom, and F8's normalizer teaching at WY-6 is its shape precedent (WY-6 is not a hard block: F9 can teach armyRecordOf itself if WY-6 has not landed, but the volume asks that the normalizer be taught in the SAME commit as the first stamp, RF-1). Downstream, WC-10 HARD-GATES on this BUILD per the first pass, so WY-8a sits on the WC family's critical path, not just WY's. 8b's supply facet renders declared-absent until 8a lands. ⚠ Collision: commodityFlow stores are TRADE's estate — CHECK-GIT-FIRST.","ownerGate":"Signed under the grant, WITH A DOC CATCH-UP OWED. F9 (supplyCargo) is a §2a row; the ledger copy of DESIGN_FP_ARCHITECTURE.md §4:669 reads '⏱ SIGNED — released at its queue position' while the BUILD-SLOT copy of the same doc still says UNSIGNED at :669 and :3043 and contradicts itself at :1891. The first pass flags this as a doc catch-up owed, not an open gate, and under the 14:4x grant the §2a batch is signed — so this slice is clear to build, and correcting the slot copy should ride its ledger row so the two branches stop disagreeing. Separately, the r3 chair translation (attrition to the army's condition, never the realm's exhaustion) has its ONE-CLAUSE VETO SURFACE at the WY-8a ledger row and must be restated there verbatim. The supply consumption rates per march/rest tick, the supply band edges, and the rest-debit and requisition rates are §8 tuning at the soak. Not a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — lit-only behind armySupplyEnabled, with a four-fence, a lit-mutant and an explicitly byte-identical dark arm on the requisition coupling. Old records and dark worlds render supply DECLARED-ABSENT, never full, so lighting is what moves output.","workerKb":"Roughly 6-8 KB into the pulse worker graph (supplyWebWarfare.js at 538 eff, armyTransit.js at 340 eff, attrition.js and warArmyRecord.js are all already in it and all under the 800 ceiling with room). NO ceiling binds the pulse worker. ZERO bytes into the generation worker, zero eager first-paint delta. No size-baseline entry is touched.","reuse":"commodityFlow's ONE stock-write seam (the same door F8's prize credit uses); armyRecordOf at armyTransit.js:327 as the record normalizer; accumulatedAttrition already live across attrition.js, warArmyRecord.js and warCoalitionExpenditure.js, so the coupling writes into an existing quantity rather than minting one; supplyQuality already rides the transit record (armyTransitKernel.js:575/:719); supplyWebWarfare.js as the twin home with its pressure-instrument deferral left untouched; the occupation-resistance writer exists war-side. Only armySupply.js is greenfield, and the §4 encounter table stays CLOSED at fifteen — this slice's supply-line strategy is EMERGENT BY COMPOSITION (cut the road behind an army and its reinforcement cargo stops arriving) with zero new arms, which is a real saving."},{"wave":"WY-11 the lifeblood envelope (engine, caravanFloorEnabled + instrument)","status":"UNBUILT","carsLow":2,"carsHigh":4,"reason":"Car 1 is THE FLOOR LAW (directive 2h): per-region subsistence circulation, a peddler-grade minimum dispatch eligibility derived from settlement quantity times tier diversity on an owner-signed band — and the discipline that makes it cheap is that it is ECONOMIC, NEVER PHYSICAL: it raises dispatch EV and eligibility floors and never conjures arrivals through interdiction, which the executed pin proves by showing an interdicted region's floor delivering zero. Car 2 is THE DENSITY ENVELOPE soak instrument: caravan density against activity times tier diversity times war state, with both lively AND lean regions reachable, the dead-band law applied to an emergent quantity, plus 2j verified riding (war suppression via seizure-EV, partnership via risk/cost/information, the market_access and toll_exemption term families' route-cost effect measured once TR-5 is landed, and the payoff named — pair-wise caravan density is the ambient relationship read, no label, consistent with the ambiguity law). Car 3 is the certification-row AMENDMENTS for the eight WY flags in subsystemRowsVirtual.js, and the ⛔ C-WYF-5 discipline (the §92.2 instance) governs it: each row LANDS AT ITS MINTING WAVE with its twin, and WY-11 AMENDS the landed rows with envelopes and dispositive channels — it must NEVER land them as a batch, which is the shape an implementer will reach for by default. The high end reaches four because the 2j half is only executable once TR-5 exists, so a chair building earlier must declare the degraded arms as the volume permits, which is itself a recorded-deferral car.","dependsOn":"⛔ THE FAMILY'S MOST-BLOCKED ROW. §5b item 3 lands it after FP #31 TR-9, which the first pass records PART-BUILT — only 9c landed (tradeConvergenceContract.js whole; simulationRules.js:257 shows none of the eight converted) — so the EV/pact substrate is incomplete. The 2j route-cost measurement additionally needs TR-5 (UNBUILT; sovereigntyBundle.js:68/:72, peaceTermsCatalog.js:274 are spec rows only). The volume's own escape is explicit: 'degraded arms declared if built earlier'. It also needs WY-3's caravan bodies for density to be a real quantity, and it can only amend certification rows that its eight sibling waves have already minted, so in practice it is the family's LAST engine wave.","ownerGate":"Signed under the grant. The floor law's per-region minimum band and the density envelope edges are §8 tuning rows, owner-signed at the soak and raw-authored until then; §1a.3's PER-MAP derivation obligation binds every band derived from hopWeeks spectra — the dead-band law applied per map, forever. No field, no key, no migration. Not a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — dark behind caravanFloorEnabled, and the density envelope is a SOAK INSTRUMENT, which by the estate's own convention observes rather than moves. The floor-never-beats-interdiction pin is the executed guarantee that the lit arm cannot invent arrivals.","workerKb":"Roughly 4-6 KB into the pulse worker graph; no ceiling binds it. Zero generation-worker bytes, zero eager first-paint delta. subsystemRowsVirtual.js measures 284 eff with wide headroom under the 800 ceiling, so the certification amendments cost no decomposition.","reuse":"dispatchEV.js is already in the pulse graph; the dead-band law and the proposedSoakBands machinery are estate-standard; subsystemRowsVirtual.js already carries the certification row shape and its twin convention; WY-3's flowRegimeContract and F1/F2 supply the density quantity. No greenfield module at all — this wave is law, instrument and amendment."},{"wave":"WY-7 the projection spine (surface, no flag; behavior-preserving except declared unifications)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"WY's biggest surface wave, because it is a tree-wide unification carrying disclosed output shifts and no flag to hide behind. Car 1 is the registry prerequisite plus the treaty projection: the ONE-LINE EXPORT of KIND_PREFIX, CONFIRMED still module-local at src/domain/dossier/entityLinks.js:13 (`const KIND_PREFIX = Object.freeze({`, consumed at :38) — C-WYF-2 item 3 is right that 'share one registry' is unbuildable without it — extended for army/caravan/population/treaty; plus NEW treatyProjection.js consuming treatyDocument.decorate() whole so BOTH surfaces stop post-shaping and D3/D4/D5 die, with the PDF scope/role/term shifts DISCLOSED and each named in the commit. Car 2 is NEW personProjection.js: heraldWanderers relocated as a pure move out of src/components/map/heraldWanderers.js, plus ONE whereabouts vocabulary via src/domain/roads/whereaboutsDisplay.js (which already exists) curing D7's two truths — a visible dossier wording shift, disclosed — plus NEW resolveOwningCampaign and settlementNameFor (D9/D10, each surface keeping its current eligibility, now declared). Car 3 is NEW tests/lint/wayfareMirror.walker.test.js seeded with treaty and person rows, copied from premiumGateSingleSource's exact-in-both-directions census rather than viewModelParity's value-equality pin, with the r2 CONSUMPTION-EVIDENCE CONVENTION bound: the default evidence is the facet KEY LITERAL at the per-facet render site on both surfaces, and where a kind's two surfaces deliberately share one renderer module the walker instead asserts SHARED-RENDERER IMPORT IDENTITY, declared per kind in the walker's own manifest — a kind with zero literals and no declared shared-renderer row reds, and so does a declared row whose two surfaces do not both import the module. Car 4 is PHANTOM RETIREMENT (C5): the retirement clause is the FULL WarFaithSection grep census AT BUILD, never a fixed count — ⭐ RE-MEASURED at a5876c0ea and still EXACTLY TWELVE files, matching the r2 census (dossierLazyTabs.js, LivingWorldTab.jsx, FaithTab.jsx, faithPanelModel.js, livingWorldSignals.js, entityLinks.js, pdf/lib/liveWorld.js, pdf/sections/FaithWar.jsx, and four test files including entityIndex.test.js, liveWorld.test.js, liveWorldBTrack.parity.test.js and summaryTabPhase2.test.jsx) — every member re-anchored to a real surface or SURFACE_EXEMPT'd, liveWorldBTrack's screen half re-pointed or print-only-exempt until WY-8, and a retirement-completeness grep pin landing in the same commit. Car 5 is the before/after parity goldens on every touched surface with the declared unifications listed and re-recorded under this ruling.","dependsOn":"Nothing in WY blocks it — SOL_QUEUE §2 Lane B orders it after WY-0 as the second surface item, but the dependency is practical (one quiet window at a time) rather than structural. It BLOCKS WY-8b (which needs the mirror walker and KIND_PREFIX) and the persons half of WY-9. ⚠⚠ THE VOLUME CALLS THESE THE DIRTIEST FILES IN THE LIVE TREE and makes a quiet window MANDATORY, with CHECK-GIT-FIRST before the wave. ⚠ Navigate by symbol: WarFaithTab.jsx NO LONGER EXISTS at the slot — the surface is now WarTab.jsx (407 eff), FaithTab.jsx and WarFaithDesk.jsx (92 eff), so the volume's host-file budget note must be re-derived at build.","ownerGate":"Signed under the grant — no field, no flag, no key, no migration. But this wave carries DECLARED ONE-TIME OUTPUT SHIFTS that must be named in the commit and the ledger row rather than riding silently: D3/D4/D5's post-shaping death changes PDF scope, role and term rendering; D7's whereabouts unification is a visible dossier WORDING shift; and the parity goldens must be re-recorded with the legitimate cause stated. Not a push, deploy, migration, deletion or spend.","outputMoving":"YES at landing, and it is the second of only two WY rows that are. It is behavior-preserving EXCEPT the declared unifications, and there is no flag: the PDF term/scope/role shifts and the dossier whereabouts wording change the day the wave lands. Every shift is disclosed and goldened, never silent.","workerKb":"ZERO generator code — the projections are pure display-layer builders (no store, no React, no clock, worker-safe data-only returns per §3e) riding the lazy dossier and map chunks, and none of it is in the generation worker's graph. ⚠ ONE EAGER MEASUREMENT IS OWED: relocating heraldWanderers.js OUT of src/components/map/ moves a module across vite.config.js's derived map-only lucide-icon boundary (an icon imported only by components/map/** rides vendor-icons-map; one shared with a non-map surface returns to first-paint vendor-icons), so the eager closure must be re-measured against its ~795 B margin at the publishing commit.","reuse":"treatyDocument.decorate() is ~90% built and consumed whole; whereaboutsDisplay.js already exists as the one vocabulary; heraldWanderers.js and npcLedgerProjection.js/envoyErrandProjection.js exist; armyStrength.js is the seed for 8b's builder; premiumGateSingleSource is the walker's stated model; the WarFaithSection census is exactly the twelve the r2 pass measured. ⭐ ONE OBLIGATION IS ALREADY PAID: §5b item 7's prefix admission is DISCHARGED — `WY` is already in CHARTERED_VOLUME_PREFIXES at tests/domain/couplingRegistry.test.js:108, and the file builds the id-shape regex from that same const (:128) and iterates it in the shape describe (:586-588), so the two-site hazard the volume warns about does not exist here. ⚠ Budget: src/components/OutputContainer.jsx measures ~650 effective against the 600 components layer ceiling with NO size-baseline entry, so the volume's near-ceiling warning is live — every consumption lands in tab-body leaves, never the host."},{"wave":"WY-8b the army mirror (surface slice of WY-8; F6/F7, no flag)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Car 1 is armyProjection.js grown FROM armyStrength.js (never beside it): the audience parameter, the frozen UNIT_SOURCES registry, the declared-absent provenance shape, the retreat JOIN, the belief block and the commander-name anchored negative. Car 2 is M1's two conditional stamp lines for F6/F7 — and the idiom is already in the tree to copy verbatim: warArmyRecord.js:114 and :165 stamp `readiness` conditionally (`...(rdy > 0 ? { readiness: rdy } : {})`) and armyTransitKernel.js:575 is the existing copy site, so experience rides beside readiness at two lines, byte-identical for every world with no martial record. Car 3 carries two collision cures the slot re-measures. ⭐ C-WYF-2 item 2 is PART-DISCHARGED: the volume says hasMartialRecord exists only module-local at opportunism.js:122 and prescribes EXPORT-AND-REUSE, but at a5876c0ea it is ALREADY EXPORTED from src/domain/worldPulse/martialReadiness.js:256 beside readinessOf, experienceOf and rustOf and already consumed at WarTab.jsx:48 — so the export is free, and what actually remains is collapsing opportunism.js:122's duplicate local spelling rather than minting a third. ⛔ C-WYF-3 is CONFIRMED LIVE and slightly more expensive than the volume priced it: armyStrength.js:133 exports a supply-worded CONDITION_BANDS deriving from a DIFFERENT quantity, three layers deep in name, quantity and model — and the rename is called 'measured FREE (zero external consumers)', but :237 re-exports it inside ARMY_STRENGTH_PHRASES, which tests/domain/display/forceComposition.test.js:12 imports (reading STRENGTH_BANDS, not CONDITION_BANDS). Still near-free, no longer literally zero; the M4 mint executes the cure and never lands a second same-name vocabulary. Car 4 is the SUPPLY band (2k): F9 through the WY-8 ladder for field armies with the home arm deriving fresh from the town's own stores, joining the army popup facet table AND the mirror-law war tab IN THE SAME COMMIT so the mirror walker's army row gains the facet on both surfaces at once, never one. Car 5 is the pin family: mirror-walker army rows including supply, the deity-free inversion negative (experience declared-absent, never veteran — H3), the 0-vs-0.5 fork pin (a recordless world's column renders absent, never half-ready — H2), the home-unit completeness map pin, the supply declared-absent pin, and the decoy-facet mutant.","dependsOn":"After WY-7 (it needs the mirror walker, the exported KIND_PREFIX and the projection home). The supply facet additionally needs WY-8a landed, or it renders declared-absent by design. Seam 1 binds it to the war estate: F6/F7 ride war-owned records with the war lane's normalizers taught same-commit, and the UNIT_SOURCES census reds if a sixth army-bearing ledger appears. ⚠ The B-track pin re-anchoring needs re-measurement: the volume says deployedArmyStatus gains its FIRST screen consumer here so 'the vacuity dies', but it is already consumed at WarTab.jsx:420 — re-derive what the pin actually guards before writing it.","ownerGate":"Signed under the grant: F6 and F7 (experience on worldState.deployments[homeId] and on spatialLedgers.armyTransit[]) are §2a rows that need NO FLAG — each is a conditional beside an existing conditional and is byte-identical for every world without a martial record — and both are MIGRATE: none. Q2 (home units — DECLARE-ABSENT, no owner-gated home-unit record minted) is a chair ruling, VETOABLE. The ARMY CONDITION ladder edges are a §8 tuning row at the soak. Not a push, deploy, migration, deletion or spend.","outputMoving":"PARTLY AT LANDING, and this is the one WY row whose gate is record presence rather than a flag — say so plainly rather than calling it dark. F6/F7 carry no flag by design, so for any world that HAS a martial record the war tab gains real army facets the day the wave lands; for every world without one the stamps are omitted and the render is byte-identical. The supply band is the exception within the exception: it is gated by armySupplyEnabled and renders declared-absent while that flag is dark.","workerKb":"Zero generator code and zero generation-worker bytes; armyProjection is a pure display-layer builder riding the lazy tab chunks. The F6/F7 stamps add two conditional lines to warArmyRecord.js and armyTransitKernel.js, both already in the pulse worker graph, which has no ceiling. No eager first-paint delta expected; measure at the publishing commit.","reuse":"armyStrength.js at 94 eff is small and is the explicit seed; the conditional-stamp idiom is already written twice in warArmyRecord.js; armyTransitKernel.js:575 is the existing copy site; hasMartialRecord/experienceOf/readinessOf/rustOf are already exported from martialReadiness.js; the existing CONDITION_BANDS is a cure target rather than a rewrite; WarTab.jsx (407 eff) and WarFaithDesk.jsx (92 eff) both sit well under the 600 components ceiling, so unlike OutputContainer they have room — but the volume's leaf discipline still binds."},{"wave":"WY-9 the clickable canvas (surface, store-gated)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"The wave with the most user-facing surface per commit and an accessibility done-bar, which is why it does not compress below four. Car 1 is the Wayfare layer family restructure per §3a: four sub-layers inside the existing `travelers` registration — ARMIES absorbing navalTransit (same record shape, mode:'sea', the naval record's TWO affiliations both linking), CARAVANS bound to F1/F2-bearing shipments only, PERSONS widened from missions alone to all three person ledgers (roads.missions, envoyErrands, npcLedger roamers/placed transit, plus DM-assigned legs, with one-hop legs drawing on the fromId-to-toId chord as a declared judgment), and POPULATIONS positioned from rec.departTick, which is the census-4 defect cure at one line. Car 2 is the filter-value migration normalize per §2c ('migrants' to 'populations', 'envoys' to 'persons') in the mapSlice merge, pinned because mapState.layers is persisted PER CAMPAIGN and merged on load — the census-1 hazard: verify the spread order gives new sub-layer defaults their default, and pin it. Car 3 is the pointer seam per §3d: hit-slop, drag-vs-click, keyboard reach, sr-only widening, the pan-preserved pointer test and the a11y row (keyboard open/close, focus trap, sr-only counts). Car 4 is the popup family over PortablePopup with the projection renderers (armies and persons live; caravans and populations rendering 'no bodies to draw' until WY-10 binds them) plus the marker-to-Herald query linesForEntity and the declared-absent rendering pin. Car 5 is THE COVERT TIGHTENING per §3c: ownerUserId and viewerUserId supplied at EVERY map call site, covert markers DM-only with the covert ring, FAIL-CLOSED — with the covert-leak plant (a hidden leg rendered without DM context) as its mutant, and this is the car that must not share a commit with anything, because a partial landing is a privacy leak.","dependsOn":"After WY-0 (the lawful canvas and its registration walker — the volume says toggleLayer's silent no-op makes the census mandatory before any layer work) and after WY-8 (the army projection); persons need WY-7. Seam 7 binds the covert chain to the espionage law — ONE covert predicate chain, zero second spellings — and the persons layer renders covert operatives DM-only the day ES lands with no ES-side surface work.","ownerGate":"Signed under the grant. Q4 (the popup primitive — PortablePopup MODAL v1, clicking is reading, canvas occlusion while reading ACCEPTED, the anchored popover RECORDED-NOT-BUILT until the modal measurably fails) is a chair ruling, VETOABLE. No field, no flag, no migration — the persisted-filter normalize is a load-time value rename inside an existing key, not a data migration. Not a push, deploy, migration, deletion or spend.","outputMoving":"AT LIGHTING, where lighting means a reader turning the layer on — it is STORE-gated, not flag-gated, so what moves at landing is entirely decided by the DEFAULT layer state WY-0 declared, and that default choice must be named in this wave's ledger row rather than left implicit. With defaults off, landing moves nothing visible; with defaults on, markers appear immediately.","workerKb":"Zero generator code and zero generation-worker bytes; the layers, popups and renderers ride the lazy map chunk. ⚠ THE BINDING BUDGET IS EAGER, NOT WORKER: the sub-layer set widening, the travelersFilter handling and the filter-value normalize all land in src/store/mapSlice.js, which is EAGER first paint (store/index.js:49), against roughly 795 B of raw closure margin (~1,047,205 of 1,048,000). Keep the normalize small, keep renderers out of the slice, and measure the closure at the publishing commit. The performance posture is recorded, not built: no virtualization exists in the SVG layers and the zoom-gated LOD escape hatch is DEFERRED.","reuse":"PortablePopup.jsx already exists (4,321 B) and Q4 rules it the primitive; TravelersLayer.jsx, LayersPanel.jsx (364 eff), MapLegend.jsx (180 eff) and WorldMapStage.jsx (390 eff) all exist and all sit under the 600 components ceiling; the existing travelers key and travelersFilter are KEPT and widened rather than replaced, which preserves every old campaign's toggle state and avoids four new DEFAULT_LAYERS keys; the projections come from WY-7/WY-8b; the covert predicate chain comes from the espionage estate. No greenfield domain module."},{"wave":"WY-10 the flow layers (surface, store-gated)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"The last surface wave and the cheapest, because WY-9 has already built the layer family, the popup family and the pointer seam, and the mirror walker already exists from WY-7. Car 1 is the bindings and the two greenfield map-only projections: the caravan sub-layer binding ONLY to F1/F2-bearing shipments, so recordless shipments simply do not draw (a marker is never a lie — WY-3's marker-truth source scan already fenced re-solves at birth, which is exactly why this wave is cheap), and the populations popup facets reading F4/F5, with caravanProjection.js and populationProjection.js landing greenfield (the mirror law is cheap BEFORE first drift — no second spelling exists to reconcile). Car 2 is the map-only enforcement: §3e's import census plus the publicSafe contract rows, executed, with mirror-walker rows carrying map-only exemptions from the closed reason set. Car 3, which may fold into car 1, is the legend rows plus the lie-negative pin (a bodiless shipment renders nothing) — and the legend's design is the wave's nicest property: it says what the layer shows NOW (non-food goods), and grain caravans join AUTOMATICALLY when TR-4 lights, because the layer binds to the LEDGER and not to the flag, so no surface edit is owed when new shipment classes appear.","dependsOn":"⛔ The most-dependent surface row: WY-9 (the layer family and popup family), engine WY-3 (F1/F2 or nothing draws) and engine WY-4 (F4/F5 for the populations facets) — and through WY-4 it inherits the POP-1 block. §5b item 4 orders it last in the Lane B WY sub-block. Q5 rules that the caravan layer ships on WY-3 against the M6a NON-FOOD shipments with an honest legend.","ownerGate":"Signed under the grant. Q5 (the caravan layer ships on WY-3 with an honest legend) is a chair ruling, VETOABLE. No field, no flag, no key, no migration. Not a push, deploy, migration, deletion or spend.","outputMoving":"AT LIGHTING ONLY, and doubly so: the layer is store-gated like WY-9, AND it is dark BY DATA independently of that, because it draws nothing until caravanBodiesEnabled and the migration cargo flags are lit and records actually carry bodies. A reader who turns the layer on before the engine flags light sees an empty layer with an honest legend, which is the designed behaviour, not a defect.","workerKb":"Zero generator code, zero generation-worker bytes, and no expected eager first-paint delta — the two projections are pure map-only display builders in the lazy map chunk, and §3e's import census is the structural instrument that keeps them there. Nothing lands in the store slice, so the ~795 B eager margin is untouched.","reuse":"Everything structural arrives from earlier waves: the layer family, sub-layer registration and popup family from WY-9; the mirror walker and its exemption census from WY-7; the marker-truth source scan from WY-3; the legend's shared style module from WY-0's MapLegend law. The two projections are greenfield only in the trivial sense that nothing to reconcile exists — which §3e names as the point of landing them now rather than later."}],"firstCar":"The first car is WY-0's registration half, landed WITHOUT the deletions: author tests/ui/mapLayerManifest.walker.test.js as a render-free source scan asserting DEFAULT_LAYERS keys against LayersPanel rows against the map overlay mounts against MapLegend entries in BOTH directions, with the config-key exemption declared STRUCTURALLY (a non-boolean value is a panel control, not a layer row, so the *Filter family and regionalMinSeverity exempt themselves rather than being named) and with the anti-vacuity floor MEASURED AT BUILD from the boolean row set rather than inherited — src/store/mapSlice.js:88 carries 23 declared keys at a5876c0ea, so the volume's r1 figure of thirteen is wrong on both axes exactly as its own r2 note warns; in the same commit cure C-WYF-4, which is confirmed still live (travelersFilter is written on demand by setLayerFilter at mapSlice.js:328-330 with no declared-key guard, read at LayersPanel.jsx:95-103 and TravelersLayer.jsx:40, and absent from DEFAULT_LAYERS so the walker cannot see it) by declaring `travelersFilter: null` in DEFAULT_LAYERS — the file's own :103 comment already documents null as all sub-layers — or by giving setLayerFilter toggleLayer's guard, and make the walker's census domain the UNION of declared keys and source-scanned setLayerFilter-written keys either way; and land the persisted-layers merge pin that traces the spread order and proves a NEW default key reaches an OLD campaign with its default, since mapState.layers is persisted per campaign and merged on load. This is the right first car for four reasons an implementer can check: it is the only WY row with no dependency of any kind, WY-0 being the program's declared first commit in either lane; the volume states in terms that toggleLayer's silent no-op on undeclared keys makes this census mandatory BEFORE any layer work, so every later WY surface wave is scanned by a surface this car mints; it moves ZERO output, which keeps the Q3 deployment-arrow deletion and the C1 chevron deletion — the only owner-visible half, and the one whose veto window stays open until WY-0 lands — in a second, independently revertible commit; and it costs almost nothing in bytes while touching the one file that matters, since mapSlice.js is EAGER first paint (store/index.js:49) against roughly 795 B of raw closure margin (~1,047,205 of CLOSURE_BUDGET_BYTES 1,048,000), and mapSlice.js:109's own comment already prices a key of this kind at about 20 B. Two things must be in the commit and the ledger row: the eager closure re-measured at the publishing commit rather than quoted from here, and an explicit note that §5b item 7's prefix-admission obligation is ALREADY DISCHARGED — `WY` is in CHARTERED_VOLUME_PREFIXES at tests/domain/couplingRegistry.test.js:108, which feeds both the id-shape regex at :128 and the iterated shape assertion at :586-588 from one const, so the two-site hazard the volume warns about does not exist and no WY wave should pay for it again."}}
{"type":"started","key":"v2:4e6dc6aaac5d2de894ae6b996455f4dfb8b01556c73b1a8766ed058125d6bc6f","agentId":"a07029ba4c63c2954","label":"price:HB","phase":"Price"}
{"type":"result","key":"v2:ea0521eca4b77a2b95fa79931045c68745b3953762eb5786d7a427d5f8b84608","agentId":"a07425c376ba8faad","result":{"family":"ES — espionage (covert confirmation missions)","summary":"Two waves remain: ES-6b (the vetting quality — the unbuilt slice of PART-BUILT ES-6) and ES-7 (the voice + the measure, with ES-6c folded in). Priced against the build slot at a5876c0ea, not against the record. THE RECORD'S HEADLINE IS STALE AND THE CODE SAYS SO: docs/implementation/INDEX.md:1107 and :1564 hold that \"ES-7 is refused — five of the six espionage Herald kinds have no reachable producer\", but that was measured BEFORE ES-Da landed (6dbb76c2). At this tip the gauntlet and the product stage are BOTH mounted in production (envoyPulse.js:380 advanceEspionageGauntlet, :519 advanceEspionageProducts) and envoyPulse hands back espionageDetections / espionageGatherings / espionageLandings (:567-:573), while espionageRider.js is mounted at envoyDiplomacy.js. So THREE of the six kinds now have a reachable producer in a lit world — espionage_departed (the rider's covert sub-record), espionage_confirmed (a landed CONFIRM), espionage_caught (a detection row with caught:true) — and three still do not: espionage_refuted (espionageRider.js:140 hard-codes product: 'confirm', so REFUTE and ACQUIRE have no producer at all), espionage_exposed (no §3.5 exposure road; corruptionWeb returns newsEntries empty on every path) and espionage_betrayal (the FRIEND-betrayal mint ES-2 charters is absent from espionageGauntlet.js — grep finds no betrayal/grievance/trust term in the file). ES-7 as chartered is at least five behavior families against the estate's cap of one and will be refused-and-split exactly as ES-5 and ES-6 were; it is priced as that split. THE ONE STRUCTURAL BLOCKER THAT DID NOT MOVE: normalizeErrand (envoyErrandRecords.js:555) refuses any errand row without a valid peace offer AND acceptance, requires the row id to equal envoyErrandIdForOffer(offer), and binds from/to to payload.offererId/payload.targetId at :590 — so there is no free-standing errand in this estate and a court can still only spy on the court it is suing. Widening that DTO is a persisted-shape act and stays owner-gated; the DM verb narrowed to \"attach a watcher to an embassy already going out\" is buildable now without it. WORKER BUNDLE, CONFIRMED BY MEASUREMENT: neither wave adds generator code. A static closure trace from src/workers/generationRequest.js reaches 152 modules and ZERO worldPulse, zero herald, zero news modules, so the 1,404,493 B monotone-down generation-worker ceiling (tests/build/generationWorkerLazy.test.js:78) is untouched by both waves; the ES code rides advanceInterval.worker.js, which carries no byte ceiling, and realmVerbExecution.js is already engine-chunk lazy with zero first-paint bytes. THE LARGEST UNPRICED REUSE IS OUTSIDE THE ES VOLUME: W-OPS car O7 already landed src/domain/worldPulse/espionage/operationsVoice.js (308 effective, dark, zero src importers, signedBy: null) carrying mission_accepted / mission_refused / operative_unmasked / ransom_price_named / operative_pardoned / operative_died_in_custody beats with their address chains, explicitly awaiting \"the wiring car that first mounts these beats on a feed\" — which is W-OPS wiring car A (feed envelope + desk registration, subsystemRowsOps.js:193, UNBUILT and free in the first pass's lane 2). Its exposure roster overlaps ES-7's espionage_caught and espionage_exposed, so the two voices must be reconciled by a chair ruling the ES volume never anticipated; landing W-OPS wiring A first buys ES-7 most of its feed plumbing.","waves":[{"wave":"ES-6b — the vetting quality (the unbuilt slice of PART-BUILT ES-6; ES-6a landed 53d538b4)","status":"PART-BUILT","carsLow":1,"carsHigh":2,"reason":"One behavior family and the spec demands almost nothing new. The wave is: (1) a pure vettingQualityFor derivation — home internal security x own-web health, i.e. readCorruptionClimate(settlement).security banded against (1 - the compromised-institution share from compromisedSecurityInstitutions(settlement)) — thresholded onto the two-member closed set VETTING_QUALITIES (sendTwoDivergence.js:39, ['hurried','careful']); (2) the threshold itself, which is the only real cost — a continuous product has to become one of two words and no code answers that, so it is CHAIR-AUTHORED on the family's own 0.05 grid with BOTH sides pinned, on the LEAK_TUNING.FIDELITY_W = 0.7 precedent ES-6a set; (3) one wiring line replacing the silent default at espionageMissions.js:132 (quality = 'careful'). THE SHARPEST REASON THIS WAVE EXISTS: today the covert cast ALWAYS vets carefully, so the hurried arm — the arm sendTwoDivergence.js's own header says 'Q's whole betrayal depends on' — is structurally unreachable through the covert road. ES-6b is what makes it reachable, and both arms then need executed reachability fixtures plus a mutant that flattens the security term and shows a fixture's quality word move. 1 car if the derivation rides espionageMath.js as an export; 2 if the chair wants a separate leaf and the dead-head declaration / census row taken as its own act. The ES-6A packet already measured the budget headroom: espionageMissions.js 105/800, corruption.js 277/800, sendTwoDivergence.js 103/800 — none is near a ceiling, and neither corruption.js nor sendTwoDivergence.js needs an edit at all.","dependsOn":"NOTHING to build. DESIGN_FP_ARCH_ES.md §3.13's C6 clause, as corrected by C-TLF-11, rules its three deliverables 'build-and-pin NOW against the live call site' — that call site is espionageMissions.js:165. The blocker is the DISPATCHER DEAD-HEAD, not an absent consumer: castCovertOperative (espionageMissions.js:129) and mintCovertMission (:244) have ZERO src importers, re-verified at this tip, so the derivation lands correct and unreached. To be REACHED it needs a production caller of castCovertOperative, and the cheapest one in the estate is ES-7's own DM-verb arm — which inverts the first pass's ES-6b -> IN-5 -> ES-7 ordering on that one point: ES-6b can be built first, but ES-7 is what releases it.","ownerGate":"None. Signed under the 14:4x grant. The threshold is dark tuning, which by CR-ES5B-6 / CR-ES5C-O4 / ES-5d O2 an implementer may never author — the CHAIR authors it and the implementer pins both sides. No persisted key, no schema change, no push/deploy/migration/deletion/spend.","outputMoving":"NO at landing and NO at lighting — this is the family's one doubly-dark wave. It sits behind espionageEnabled (absent from every preset) AND behind a consumer with zero production callers, so a lit world still executes none of it. It becomes output-moving only in the commit that first gives castCovertOperative a caller, and that commit — not this one — owes the declared shift.","reuse":"Everything the derivation reads is already built and already inside the family's import closure: readCorruptionClimate (corruption.js:588, returning {crime, security, prosperity, ...}) is ALREADY imported by espionageMath.js:261 for covertCompetence01, and compromisedSecurityInstitutions (corruption.js:663) is built and consumed elsewhere (causeVocabulary.js:129). vetVolunteerEnvoy (sendTwoDivergence.js:188) was built spec-complete at WR-7d with both arms real, its refusal reasons typed, and its one-home law already scan-pinned by seam row 9 (F8). VETTING_QUALITIES is a live frozen closed set. No new module is strictly required.","workerKb":"0 KB to the generation worker — CONFIRMED by a static closure trace from src/workers/generationRequest.js: 152 modules reached, zero under worldPulse, zero herald, zero news, so the 1,404,493 B monotone-down ceiling (tests/build/generationWorkerLazy.test.js:78) cannot move. Roughly +1-2 KB of source into the advance-interval worker graph (advanceInterval.worker.js), which carries no byte ceiling test anywhere in tests/build. No lazy chunk needed and none would buy anything."},{"wave":"ES-7 — the voice + the measure (the family's and the programme's terminus; ES-6c folded in)","status":"UNBUILT","carsLow":6,"carsHigh":11,"reason":"As chartered (DESIGN_FP_ARCH_ES.md §4, ES-7) it is at least five behavior families against a cap of one — six Herald kinds with five joins each, the tap register in the pools, the DM verb with its compendium regen, the certification row's dispositive literals, seven measured envelopes, plus ES-6c's retroactive clause — so it will be refused-and-split the way ES-5 and ES-6 were, and it is priced as that split. (a) THE REACHABLE KINDS, 2-3 cars: pool rows, registry rows with requiredSlots + slotless fallback, WHAT_PHRASES, section authority and the full address chain for espionage_departed / espionage_confirmed / espionage_caught, plus a new tests/lint/espionageKindPools.walker.test.js and a docs/content receipt-pool annex. The content is not free: kindPoolFloors.walker.test.js reds a NEW kind that lands under its frequency-scaled floor, and FREQUENCY_FLOORS is routine 8 / notable 6 / major 4 (tests/helpers/kindPoolWalker.js), so three kinds alone want 18-24 authored variants and six want 36-48. The ENC-5 precedent makes THE WORDS their own car. (b) THE DM VERB DISPATCH_CONFIRMATION_MISSION, 1-2 cars: a realmManifest row + a build arm + an apply arm in realmVerbExecution.js (608/800 effective) + an operationRegistry row + npm run gen:compendium-data in the same commit, routed ALWAYS-PROPOSAL per J-ES-12 through the landed stageRealmVerb / mintRealmVerbProposal road so it mints zero new apply paths. (c) THE EXPOSURE ROAD, 2-3 cars (ES-6c folded in): the §3.5 taint cascade, the FRIEND-betrayal mint ES-2 could not land, espionage_exposed with the double-agent clause, and the news producer corruptionWeb does not have. (d) THE MEASURE, 1-2 cars: seven envelopes (mission mix by product, catch-rate band, standoff share, leak share, grade mix, rooted-dwell share, tap mix), each INTERVAL_WEEKS-derived with an executed mutant, each needing a registered entry in tests/fixtures/distribution-envelopes.manifest.json whose schema demands baseMeasurementN >= 400 — and since espionageEnabled lights in NO preset, every base rate has to come from a purpose-built LIT corpus, which is the single most underpriced line in this wave. Plus the small producer widening (espionageRider.js:140's hard-coded product: 'confirm') that REFUTE and ACQUIRE need before their kinds may be voiced at all. 6 cars is the honest floor if the exposure road narrows to the catch and the refuted/exposed/betrayal kinds are declared-deferred; 11 if all six kinds ship voiced and measured.","dependsOn":"ES-6a (53d538b4) and ES-Da (6dbb76c2) — both LANDED; ES-Da is the one traffic road and is what makes any of this reachable. IN-5, the seventh Herald section, for the confirmations/refutes section authority: HERALD_SECTIONS is the frozen six at heraldRouting.js:64 and informationNews.js:24 records that 'the seventh is IN-5's to mint'. THAT DEPENDENCY IS AVOIDABLE FOR ONE SLICE: heraldRouting.js:133 already carries the interim precedent — IN-0a's plant_took is filed under 'war' with J-IN0A-3 declaring that IN-5 will RE-FILE it — but informationNews.js's own header rules that 'the interim declaration belongs to the wave that mints a routed INFORMATION beat', so taking it is a CHAIR act, not a lane's. W-OPS wiring car A (feed envelope + desk registration, subsystemRowsOps.js:193) is the natural predecessor for the feed plumbing and for reconciling operationsVoice.js's overlapping exposure roster. The chartered arbitrary-target DM verb additionally needs the errand DTO widened at envoyErrandRecords.js:555/:590 — see the owner gate.","ownerGate":"TWO items, everything else signed under the 14:4x grant. (1) OWNER — the DM verb in its chartered 'go and look at any court' form: normalizeErrand (envoyErrandRecords.js:555) refuses any row without a valid peace offer AND acceptance, forces the id to envoyErrandIdForOffer(offer), and binds from/to to payload.offererId/payload.targetId (:590). Widening that is a persisted-DTO shape change and falls squarely in the chair's own standing exceptions. THE NARROWED VERB — propose attaching a watcher to an embassy already going out, i.e. the rider road under DM authorship — needs none of it and is buildable today. (2) OWNER — opening CUSTODY on a caught spy (the errand DTO refuses the held state without an army-carried ENCOUNTER; subsystemRowsBelief.js records 'relaxing either shape is a persisted-schema question and this estate gates those on the owner'). ES-7 DOES NOT NEED IT: the §3.5 cascade fires off the detection row's caught flag, not off a hold, so exposure can land while custody stays shut. NOT an owner gate: the pool prose, which lands CANDIDATE with signedBy: null and its owner rows named, on the operationsVoice and ENC-5 precedents.","outputMoving":"NO at landing — every kind is minted under espionageEnabled, which is absent from every preset, and the dm-only kinds are covert:true fail-closed upstream (L6), so a dark world composes no line and writes no byte. YES at the second declared lighting, and loudly: this is the wave that gives the espionage layer its first reader-visible surface. ONE THING THAT MOVES IN THE COMMIT ITSELF AND MUST NOT BE MISREAD AS OUTPUT: the registry and routing edits move frozen estate-wide numerals — the registered-minus-routed honesty count, kindPoolFloors' shrink-only backlog figures, and the 274-token unvoiced backlog. Those are instrument movements, they must be RE-DERIVED rather than restated, and the walker family reds in both directions if the pools and the counts do not move in one commit.","reuse":"Large, and mostly outside the ES volume. src/domain/worldPulse/espionage/operationsVoice.js (W-OPS O7, 308 effective, dark, zero importers) already composes mission_accepted / mission_refused / operative_unmasked / ransom_price_named / operative_pardoned / operative_died_in_custody with typed vocabulary and full address chains, and its own header says the feed envelope and desk registration belong to a wiring car — so ES-7's caught/exposed voice is largely authored already, subject to a chair ruling on the two overlapping spellings. The §3.5 cascade rides built machinery end to end: SIGHT_TUNING's EXPOSE_CHARGE01 0.8 / EXPOSE_GRIEVANCE_W 0.3 pair and the incidentType 'spy_exposed' at informationStatecraft.js:1026-1044, npcCredibility.freshLieExposureFor into maintainMarks, and infowar_spy_exposed already routed at heraldRouting.js:129 with phrase pools at rumorPhrasePools.js:177 — ES reuses that incidentType rather than minting one. The DM verb reuses mintRealmVerbProposal (applyWorldPulse.js:1305) and the stageRealmVerb proposal road, so it opens zero new apply paths and touches neither pulseKernel.js (banked at 1581) nor applyWorldPulse.js. Nine sibling kind-pool walkers and one shared tests/helpers/kindPoolWalker.js give the ES walker its whole shape; informationKindPools.walker.test.js at 252 lines is the size to copy, not chanceMeetingKindPools' 1,188. The producers are mounted and already hand their receipts back at envoyPulse.js:567-573, so no kernel plumbing is owed. Headroom is comfortable on every collision file measured at this tip: heraldRouting 273/800, realmVerbExecution 608/800, realmManifest 422/800, operationRegistry 242/800, envoyPulse 332/800, subsystemRowsBelief 252/800 — and envoyDiplomacy.js now measures ~762/800, not the 797/800 the ES-1 charter warned of, so that stale three-lines-of-headroom warning should not be inherited.","workerKb":"0 KB to the generation worker — CONFIRMED by the same closure trace (152 modules from src/workers/generationRequest.js, zero worldPulse / herald / news), so the 1,404,493 B monotone-down ceiling is untouched and no lazy chunk is required for it. The pools and news modules ride the advance-interval worker and the main graph: estimate +12-20 KB of source for six pools at floor depth (envoyChanceMeetingReceiptPools.js is 156 lines, sovereigntyNews.js 492) plus +8-12 KB for the registry/routing/walker side and +2-4 KB for the verb rows. The verb arm costs ZERO first-paint bytes because realmVerbExecution.js is already engine-chunk lazy (its own header: 'imported only by applyWorldPulse.js and tests — zero first-paint bytes'). advanceInterval.worker.js carries no byte ceiling. The compendium regen adds one operation row to the generated data file, which rides the DATA_LAZY ceilings in tests/build/vendorPdfLazy.test.js (3,098,110 raw / 771,897 gzip) — tens of bytes, immaterial. If the chair wants belt-and-braces, the pool corpus is the natural lazy-chunk candidate behind the flag, on the livingContentRoster seam precedent, but nothing measured today demands it."}],"firstCar":"ES-6b, in one car: land `vettingQualityFor` and give the covert cast a derived vetting quality instead of the silent `quality = 'careful'` default it carries today at `src/domain/worldPulse/espionage/espionageMissions.js:132`. Export the derivation from `espionageMath.js` (106 effective lines against an 800 ceiling — no new file is needed, and the module already imports `readCorruptionClimate` at :261 for `covertCompetence01`): it takes the home settlement, reads `readCorruptionClimate(settlement).security` (corruption.js:588) and the compromised-institution share from `compromisedSecurityInstitutions(settlement)` (corruption.js:663), composes internal rot into external blindness exactly as §3.13 words it, and thresholds the result onto the two live members of `VETTING_QUALITIES` (sendTwoDivergence.js:39). The threshold is the wave's one real decision and an implementer MAY NOT author it — under CR-ES5B-6 / CR-ES5C-O4 / ES-5d O2 the chair sets it, on the family's own 0.05 grid, with the ES-6a `LEAK_TUNING.FIDELITY_W = 0.7` docblock as the model for recording WHY the number means what it means; the implementer's job is to pin both sides of it. Then one wiring line: `castCovertOperative` (:129) derives `quality` when the caller supplies none, and `vetVolunteerEnvoy` at :165 is left untouched — it is the estate's one vetting home under seam row 9 ⟨F8⟩ and this wave adds no second reader. The proofs: executed reachability fixtures for BOTH arms (a secure, clean-institution court reaching `careful`; a rotten one reaching `hurried`) — this is the point of the wave, because today the covert road can only ever vet carefully and the `hurried` arm that `sendTwoDivergence.js`'s own header calls the one \"Q's whole betrayal depends on\" is structurally unreachable through it; a mutant that flattens the security term and moves a fixture's quality word; an anchored negative that the derivation reads no `covert.*` or `truePurpose` field; the four-fence dormancy arm (`espionageActive` dark ⇒ the derivation is not called and nothing is composed); and a DECLARED, receipted statement in the file header and the certification row that the consumer is still dead-headed — `castCovertOperative` and `mintCovertMission` have zero `src/` importers, re-verified at `a5876c0ea` — so a later reader cannot mistake a correct-and-unreached derivation for a live one. Nothing pushes, deploys, migrates, deletes, spends, or lights a flag."}}
{"type":"started","key":"v2:1c43dd0ff944e4a57a083faabeaf74465ee2cada79912f72641ee65e36407841","agentId":"a193b042571c32a0c","label":"price:EP","phase":"Price"}
{"type":"started","key":"v2:46acf5564157d531c9094b302a8b8bfff0d43412b0873e7a72ee09809bf4ece2","agentId":"a8cdffdba57fa0dbf","label":"price:INT","phase":"Price"}
{"type":"result","key":"v2:1f354a8bd3b1a33b9f843fd42a1aaca3a83dcf656ec734e27eb0f3482e13f9c4","agentId":"ac77ce7a59f610151","result":{"family":"TR","summary":"TRADE is 1 landed + 1 part-landed of 9. TR-1 (casus commercii) landed whole in ONE commit — `d7ea69a4b`, 18 files, +2,933 lines, 4 new src leaves (the volume predicted 3; `commercialReceiptPools.js` at 460 raw was unpriced) and 5 test/walker files. That commit is this family's CAR CALIBRATION: one car ≈ 3–4 new pulse leaves + 2–3 shared-surface edits + 3–5 test/walker files. TR-9c landed the contract module only (`tradeConvergenceContract.js`, 398 eff). Everything else measured ABSENT at the slot `a5876c0ea`: no `houseLedger.js`, `beliefScarcity.js`, `dispatchDestination.js`, `grainFlow.js`, `grainArrivalCredit.js`, `commercialTermExecutors.js`, `tradeDemandTrigger.js`, `cornerGate.js`, `cornerComposer.js`, `warehouseSeizure.js`, `monopolyDwell.js`, `ventureLedger.js`, `ventureLegs.js`, `commercialErrands.js`/`factorErrand.js`. Family total 24–33 cars (PLAUSIBLE, priced from the TR-1 calibration; the substrate figures below are CONFIRMED by reading the slot).\n\nFIVE MEASURED FACTS THAT CHANGE THE FAMILY'S PRICE, none of them in the volume:\n\n(1) THE WORKER BUNDLE IS A NON-ISSUE FOR THIS FAMILY — CONFIRMED BY TRACE. I walked the static+dynamic import graph from `src/workers/generation.worker.js`: 153 modules, containing ZERO `src/domain/worldPulse/**` and ZERO `src/domain/spatial/**`. Every TR wave lands in exactly those two directories, so every row below is ~0 KB against the 1,404,493 B monotone-down ceiling (`tests/build/generationWorkerLazy.test.js:78`). THREE TRIPWIRES, all in the worker's graph and all READ-ONLY for TR: `src/domain/factionArchetypes.js` (TR-2's eligibility read), `src/data/foodImportRates.js` and `src/domain/region/goodsCatalog.js` (435 eff; TR-4/TR-6). An edit to any of those three is the only way a TR wave pays worker bytes — so TR-4's authored `GRAIN_UNITS_PER_MONTH` must live in `grainArrivalCredit.js`, never in `foodImportRates.js`.\n\n(2) `pulseKernel.js` IS AT 1581/1581 EFFECTIVE — ZERO HEADROOM, banked permanently by chair ruling R-BLD-10 (`scripts/.size-baseline.json`). `applyWorldPulse.js` is 907 against a 907 entry. S44's \"pulseKernel/applyWorldPulse are ZERO-EDIT\" is therefore not a preference, it is the only affordable path: every TR kernel mounts inside an existing in-cycle host (`advanceSettlementSupply` pulseKernel.js:772, `advanceEntrepotLayer` :795, `assizeKernel`, `settlementLifecycleKernel` at 733/800).\n\n(3) THE DECLARED-PENDING CERTIFICATION DOOR IS STRUCK AND FROZEN. `VIRTUAL_PENDING_RULE_KEYS = Object.freeze([])` at `subsystemRowsVirtual.js:595`. The TR volume's Q4 (\"each wave lands PENDING, TR-9 converts all eight\") is DEAD in code — the comment at `simulationRules.js:256-258` still asserts it and is stale. Every TR wave now owes a full AUTHORED certification row in the same commit as its flag (TR-1's is at `subsystemRowsCompact.js:265`, ~30 lines of authored prose). This adds ~0.3 car per wave and SHRINKS TR-9 by the same amount.\n\n(4) TR-1 LANDED BUT ITS WRITER IS UNMOUNTED. `advanceCommercialReasons` (`commercialReasons.js:457`) has ZERO callers in `src/`; the only cross-module import is `tradeWar.js:69`'s reader. `subsystemRowsCompact.js:293` says it in words: \"A DARK INSTRUMENT WITH NO MOUNT … THE OBSERVATION NEEDED is not a better receipt but a WIRING WAVE: mount advanceCommercialReasons on the pulse.\" This is recorded, not a bug to re-find — but it is a half-car that TR-2/TR-3/TR-6 all inherit, and it is the cheapest car in the family (one mount line inside `advanceSettlementSupply` or `settlementLifecycleKernel`, dormancy golden, the census channel then grades itself). Recommend it as car 2 of the family.\n\n(5) TWO LIVE RECORD-vs-RECORD CONTRADICTIONS AT TR-4, both of which stall an implementer at charter. (i) `DESIGN_FP_ARCHITECTURE.md` §5 #26 handles the `applyWorldPulse` second storageMonths fold per Q9's pre-ruling — \"REPORTED-NOT-DEFECT; the queue row filed; the sixth-writer scan fences it\" — while `docs/FABLE_VALIDATION_QUEUE.md:303` (CR-2) and `docs/SOL_QUEUE.md:342` row 14 both order \"FIRST consolidate the two folds into the one applicator … behind a no-behavior-change pin\". Two different acts; one must be struck. (ii) §5 says the `commodityStocks`/`supplyShipments` serialize→regen→undo→import round-trip pin \"LANDED AT WY-3, ordered immediately before; TR-4 VERIFIES and extends it, never re-lands it\" — but WAYFARE is 12 of 12 UNBUILT and WY-3 needs unsigned owner §2a rows. The TR volume's own TR-4 lifecycle clause says slice 1 LANDS it. With WY unsigned, TR-4 lands it and WY-3 later verifies. §5 also corrects the T7 row's shape: ROUTED CENSUS-SHAPED over the food ledger's `importDependency` consumer census, shrink-only — NEVER a count-of-N row.\n\nTHE FAMILY'S LARGEST REUSE WIN IS ALREADY IN THE TREE: GR-3 minted `exclusivity`/`market_access`/`toll_exemption` at `peaceTermsCatalog.js:285-287` with `family:'commercial', executor:'seam'` and display strings at `:425-427`. TR-5 mints only `grain_provision` (zero hits tree-wide, measured) plus the executors. Its two designed red-flips are live and confirmed: `tests/domain/peaceTermsGrantTerms.test.js:272` and `tests/domain/sovereigntyBundleWr10.test.js:486`. And `peaceTerms.js` measures EXACTLY 797/800 effective at this base — the volume's figure holds to the line, three lines of headroom, so any TR-5 head seam of ≥4 effective lines is leaf-first or the wave STOPs.","waves":[{"wave":"TR-2 — THE HOUSE (merchantHousesEnabled; TR-2b the syndicate variant rides dark)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"CONFIRMED absent: no houseLedger.js / houseActs.js / houseFactorCasting.js / houseNews.js anywhere under src. The volume prices ~890 effective lines of new leaf (400+250+120+120) against TR-1's measured ~540-predicted/1,468-raw-actual, so this is ~1.6x TR-1's one car before the extras. The extras are what make it three: a SECOND persisted ledger (spatialLedgers.houses) whose books are event-accrued and therefore NOT re-derivable — the disclosed J-TR-3 regen-PRESERVES exception, the second instance of J-WR-3's idiom — plus a load-time normalizer that must land WITH the writer (the dormant-with-books shape has to round-trip from old saves), a JSON-alias round-trip (members ARE npcs[]), a shrink-only single-writer source-scan census with an EXECUTED third-writer plant (cp backup, md5-exact restore, never checkout-family), the rename round-trip pin (mandatory, because factionArchetypes.js:81's MERCHANT fallback is a name/description regex and rename-sensitivity is real), formation determinism by codepoint tie-break, the no-undead-house cooldown negative, MAX_HOUSES_PER_SETTLEMENT, the T4 no-hidden-governor import pin with a guard-the-guard positive control, the four-fence dormancy set plus the lit-mutant control, and — new since the volume — a full AUTHORED certification row rather than a declared-pending entry. Car A: houseLedger + normalizer + flag mint + fences + the writer census and plant. Car B: houseActs + houseFactorCasting + houseNews + commercialReceiptPools rows + WHAT_PHRASES/heraldRouting + the own walker file + the no-micro-agent scan. Car C: the T-4 TELLABLE fixture, formation determinism, cap, cooldown, rename round-trip. Car D (the high end) is TR-2b riding dark. Label: PLAUSIBLE, priced from the TR-1 calibration.","dependsOn":"TR-1 (LANDED, d7ea69a4b) — the intra-TR chain's first link. No spine blocker: every substrate premise TR-2 names measured BUILT (factionArchetypes.js resolution order :52-56/:81/:133-142; scoringObjective.js MERCHANT_OBJECTIVE; distanceRead.js:166/:180 getSpatialLedger/setSpatialLedger; region/contestMath.js:45 hash01). TR-2b's one substrate widening — admitting a syndicate house as a corruption-web patron — has its home built: worldPulse/corruptionWeb.js, 510 eff. ⚠ TR-2 does not strictly need TR-1's unmounted writer, but it inherits the blind census channel until that mount lands (subsystemRowsCompact.js:293).","ownerGate":"NONE. TR-2b's sequencing was the family's one recorded owner gate — SOL_QUEUE.md:346 and the first pass's B.1 row both carry 'UNSCHEDULED until the owner sequences it' — and it is signed under the 14:4x grant, so TR-2b rides TR-2 dark behind its own syndicateHousesEnabled flag with its own four-fence set. Nothing here pushes, deploys, applies a migration, deletes data or spends.","outputMoving":"NO at landing — dark behind merchantHousesEnabled (and syndicateHousesEnabled), with the dormancy golden landing BEFORE the wiring and an absent-vs-false differential proving the absent key is byte-identical. YES only at the second declared lighting, which has no door until sitting item 1's §764.3 amendment is initialled.","reuse":"commercialReceiptPools.js (386/800 eff) takes this wave's receipt rows — the family pool already exists, so no new pool module; that headroom (414 eff) is the family's shared budget and TR-2 spends first. factionArchetypes.js READ-ONLY via factionArchetype() (⚠ it is in the generation worker's graph — read it, never edit it). corruptionWeb.js (510 eff) for TR-2b's patron widening. distanceRead.js's conditional-materialization primitives + supplyKernel.js:577-581's drop pattern. deterministicSort.js and contestMath.hash01 for the codepoint tie-break. The existing includeCovert veil seam, widened not rebuilt. Mount rides an existing in-cycle host — settlementLifecycleKernel.js (733/800) is the natural one; pulseKernel is at 1581/1581 with zero headroom.","workerKb":"~0 KB. CONFIRMED by tracing the generation worker's 153-module graph: no src/domain/worldPulse/** member is in it, and all four TR-2 leaves land there. The wave needs no lazy chunk of its own — it is already outside the worker. ⛔ THE ONE WAY THIS ROW GOES NON-ZERO: src/domain/factionArchetypes.js IS in that graph. If the eligibility predicate forces an edit there rather than a read, the wave pays worker bytes against a ceiling with no slack, and the cure is a pulse-side leaf that reads the archetype rather than a new branch inside it."},{"wave":"TR-3 — BELIEVED MARKETS (believedMarketsEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"CONFIRMED absent: no beliefScarcity.js, no dispatchDestination.js. ~500 effective lines of new leaf (300+200) — slightly under TR-1's one-car load — but it carries the family's most delicate pin set, which is what buys the second and third car. dispatchEV.js (118 eff) gains the destination-consumer seam and nothing else. The pins: the K3 structural set PER CONSUMER (import pin + token scan + guard-the-guard with a positive control at the truth-side scarcity derivation); the NO-MERGE pin (an arithmetic-combination token scan plus the two-composer whitelist enumerated in FP_TRADE §3 Seam Three and nowhere else), whose guard-the-guard seeds a deliberate band-average and asserts the scan reds; staleness reachable across at least two bands with a receipt; the T-1 TELLABLE (came for the famine, found the harvest); the four-fence + lit-mutant set; two named mutants (a third co-importing module widening the whitelist ⇒ census reds; a merge-expression plant). Car A: beliefScarcity as a sibling leaf + flag + fences + the SP-2 registry-membership tripwire. Car B: dispatchDestination + the dispatchEV seam + the no-merge scan and whitelist + both mutants. Car C (the high end): the T-1 tellable and the staleness fixtures if the corpora prove heavy. Label: PLAUSIBLE.","dependsOn":"TR-1 (LANDED) for the intra-TR chain, and SP-2 — which is SATISFIED IN CODE, not merely claimed: believedScarcityEnabled is manifest member 5 of the 35 in ENGINE_GATED_VIRTUAL_RULE_KEYS (simulationRules.js:252), joined by SP-B on 2026-08-05 with its authored certification row. The volume's '⚠ RE-DERIVE the blocker' is discharged: the only remaining gate is the intra-TR chain. TR-3 is itself the gate on TR-6 and TR-7, so it is the family's highest-leverage unbuilt wave. ⚠ beliefMap.js measures 781/800 effective at this base (the volume's 771 and 778 are both stale) — 19 lines of headroom, so the sibling-leaf law (Q3) holds and beliefMap takes net-zero seam lines.","ownerGate":"NONE — signed under the 14:4x grant. No push, deploy, migration, deletion or spend. The one cross-programme obligation is a NOTE, not a gate: the pre-pin toward SPINE SP-2 (seam 6) asserts the subject-family name and banded shape from the consumer side, and reds if SP-2's export surface moves.","outputMoving":"NO at landing — dark behind believedMarketsEnabled, and the dormancy golden's specific claim here is that the OMNISCIENT path stays byte-identical, i.e. dispatchEV's existing WHETHER decision and its rng draw count are untouched when the flag is absent. YES only at the second declared lighting, where destination choice starts moving caravans.","reuse":"SP-B's whole belief family — decay, update and forgetting laws — consumed, never rebuilt; beliefScarcity is an instance of an existing subject family, not a new belief engine. dispatchEV.js's netDanger formula (:12-14) and per-origin appetite ledgers (supplyKernel.js:566-581 merchantAppetite/dispatchWillingness) are the truth-side reads already in place. beliefMap.js's export surface as-is. The whitelist idiom and the guard-the-guard control come from GR-2's and TR-9c's already-executed mutant rounds (scripts/mutation-coverage-manifest.json), so the harness shape is precedent, not invention.","workerKb":"~0 KB. CONFIRMED: neither beliefMap.js nor dispatchEV.js nor anything under src/domain/worldPulse/** or src/domain/spatial/** appears in the generation worker's 153-module graph. No lazy chunk needed — the wave is already outside the worker entry's rollup build."},{"wave":"TR-4 — THE GRAIN ROAD (foodCaravansEnabled; two slices; EARLY-ELIGIBLE)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"CONFIRMED absent: no grainFlow.js, no grainArrivalCredit.js. This is the family's most expensive non-terminal wave, and the cost is not the ~400 effective lines of leaf — it is the proof surface around them. The wave must land: THE CR-2 ACT (see the gate column) as its own no-behavior-change commit; the commodityStocks/supplyShipments serialize→regen→undo→import round-trip pin, MEASURED ABSENT — the volume says 'do not inherit' and it is right; the T7 walker (exactly one of {netted rate share, physical arrivals} live per settlement per tick, in BOTH flag states, forever), which the compiled §5 re-shapes as a ROUTED CENSUS over the food ledger's importDependency consumers, shrink-only, never a count-of-N; the five-site storageMonths shrink-only census with an EXECUTED sixth-writer plant — and the five sites re-measured at this base are foodStockpile.js:406 (+:479), generosityUpdates.js:65, applyWorldPulse.js:106 (the second fold; the volume's :322 and :92-110 are both address-rotted), magicBufferApply.js:357, mutateWorld.js:1153; THE CALM-EQUIVALENCE PIN (within one authored band of the old rate — the disclosed, bounded lit-path shift); the BYPASS-PRESERVATION pin with all three arms EACH executed (teleport 0.3, airshipBesieged 0.15, underways trickle); grain-months conservation at the one applicator; a belief-cannot-starve negative; the T-6 siege TELLABLE week-by-week plus its open-route negative; three mutants. Cars: (0) CR-2; (1) the round-trip pin; (2) grainFlow + grainArrivalCredit + the equal-and-opposite offset + flag + fences; (3) the T7 walker + the storageMonths census + the sixth-writer plant; (4) calm-equivalence + the three bypass arms + T-6; (5) slice 2's treaty fork + the robbery claim. Label: PLAUSIBLE.","dependsOn":"NOTHING UNBUILT — this is the early-eligible wave and the first pass's own lane-3 head. Its lit preconditions both exist: commodityFlowEnabled (simulationRules.js:769/:938, true in full_simulation) and demographicsEnabled (:1027, virtual); the collapse arm additionally wants routeLifecycleEnabled, measured false, which yields an honest permanent zero rather than a bug. Its three substrate anchors re-measured green: commodityFlow.js:260 assertGoodsConservation extends to grain for free (J-TR-8); foodStockpile.js:288's generation stash is the offset's source and is NEVER rewritten (GENERATION IS SACRED); treatyTransfer.js:11-32 names generosityUpdates.applyFoodDeltasToUpdates as the existing single applicator. ⚠ TWO RECORD CONTRADICTIONS TO SETTLE AT CHARTER, both named in the summary: the CR-2-vs-Q9 disagreement, and whether the round-trip pin is TR-4's or WY-3's (WAYFARE is 12 of 12 UNBUILT with unsigned §2a owner rows, so it is TR-4's — WY-3 later verifies rather than the reverse).","ownerGate":"NONE for the build — signed under the 14:4x grant; nothing pushes, deploys, migrates, deletes or spends. But CR-2 is a PRE-RULED CHAIR ACT with a STOP clause, and it is the wave's first commit: FABLE_VALIDATION_QUEUE.md:303 and SOL_QUEUE.md:342 both order the two storageMonths folds consolidated into the one applicator FIRST, behind a same-seed byte-identical pin, and BOTH say that if consolidation shifts behaviour the lane STOPS and reports to the chair — a live two-writer defect is adjudicated, never silently normalized. ⚠ DESIGN_FP_ARCHITECTURE.md §5 #26 instead carries Q9's pre-ruling (REPORTED-NOT-DEFECT, fenced by the sixth-writer scan). The chair must strike one before an implementer starts. Separately: WY-3's own §2a field-mint rows ARE the owner's, and TR-4 must not wait on them.","outputMoving":"NO at landing — dark behind foodCaravansEnabled, dormancy golden proving the rate arithmetic byte-identical. ⚠ AND THE HONEST HALF: this is the ONE TR wave whose LIT path is a DECLARED same-seed shift rather than a pure addition — the calm-equivalence pin exists precisely because physical arrivals replacing the netted rate share moves the food line within one authored band. That shift is bounded, stated and pinned, but it must be declared at the second lighting and never allowed to ride silently. CR-2's own commit is byte-identical in BOTH flag states or it stops.","reuse":"commodityFlow.js's five-term integer conservation and MAX_SHIP (:117) — grain joins the balance and assertGoodsConservation covers it for free. generosityUpdates.applyFoodDeltasToUpdates (90 eff) as THE single applicator, already named by treatyTransfer.js's own header ruling. foodStockpile.js's six-term effective-deficit sum (:347) and its measured bypass rates in src/data/foodImportRates.js (:18-20). supplyShipments.js (210 eff) K-ranked producers and the starvation latch. goodsCatalog.js:30-36 — grain is ALREADY canonical with criticality 0.95, so no catalog edit is needed (which is also what keeps this wave off the worker bundle). supplyKernel.js (489/800) is the mount seam, own-flag-before-host-gate. pulseKernel and applyWorldPulse take ZERO edits — not a preference, a necessity at 1581/1581 and 907/907.","workerKb":"~0 KB for the pulse leaves — no worldPulse or spatial module is in the generation worker's graph. ⛔ TWO REAL TRIPWIRES HERE AND THEY ARE THIS WAVE'S ONLY WORKER RISK: src/data/foodImportRates.js and src/domain/region/goodsCatalog.js (435 eff) are BOTH in the worker's 153-module graph. The single authored GRAIN_UNITS_PER_MONTH constant must live in grainArrivalCredit.js, NOT beside the bypass rates in foodImportRates.js where it would look at home; and grain needs no goodsCatalog row because it already has one. Land it that way and the wave adds zero bytes; land it the natural-looking way and it pays against a ceiling that was last re-minted for 245 B and had to be argued in the ledger."},{"wave":"TR-5 — THE PACT LANE (tradePactsEnabled)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"CONFIRMED absent: no commercialTermExecutors.js, no tradeDemandTrigger.js, and grain_provision returns zero src hits. ~500 effective lines of new leaf, but the wave is priced up by three things the leaves do not show. First, THE DEAD CONSUMER FINALLY LANDS: realmPressure01 is half-wired today — demographicsObservation.js:137 produces it and only the war path consumes it (demographicsWar.js:172/:295-312, warReasons.js:801-853) — and tradeDemandTrigger is the trade-agreement consumer that has never existed. Second, THE T-5 TELLABLE IS FIVE SUBSYSTEMS DEEP with receipts at every link (pact forms → delivery lands → realmPressure falls → war-motive weight falls) AND the same fixture must be dark byte-identical. Third, the wave carries two DESIGNED RED-FLIPS whose re-pointing is its own work, plus a docs amendment across eleven consumer files. Also: every term names duration in 52-week years with the INTERVAL_WEEKS identity pin (treatyClock.js:6-7), the K3 absurdity twin (signed for grain the seller no longer has ⇒ the mismatch becomes the next grievance), the lean-year suspension with a buyer-belief fork, amendment-shaped formation (one instrument per pair — a proposal AMENDS, minting only when absent), and the four-fence + lit-mutant set. Cars: (a) grain_provision + the five executors as lazy peaceTerms-family siblings + the TERM_CATALOG membership pin + the two pin re-pointings; (b) tradeDemandTrigger + the realmPressure01 landing + flag + fences; (c) the T-5 end-to-end tellable and its dark twin + the K3 absurdity twin + the lean-year fork; (d) the CR-WR10-B amendment sweep. Label: PLAUSIBLE.","dependsOn":"TR-1 (LANDED); GR-2 and GR-3 BOTH BUILT — the first pass confirms GR-3 against the record's own stale 'GR-3b remains owed' (tests/domain/peaceTermsGrantTerms.test.js:38 says 'GR-3b HAS LANDED' in words), and I re-verified the substance: peaceTermsCatalog.js:285-287 carries exclusivity / market_access / toll_exemption at family 'commercial', executor 'seam', with display strings at :425-427. SP-3 is satisfied via GR-2/GR-3 (pactFormationEnabled is manifest member 15). TR-4 is wanted only for grain_provision's PHYSICAL arm — dark-TR-4 gives the named conserved-rate degraded read, so TR-5 can land before TR-4 with that arm declared degraded rather than blocked. ⚠ THE COMPILED §5 SUPERSEDES THE TR VOLUME'S OWN C-TLF-2 DISCHARGE BLOCK (J-FP-1): catalogGrewSinceWr10's discharge was EXECUTED at GR-3, so TR-5 VERIFIES it and does NOT re-run it. What still binds from that block: the CR-WR10-B twin-degradation-sentence law (byte-equal across both volumes, never a second sentence), and the amendment of the 'HALF DISCHARGED… TR-5 STILL OWED' blockquote to 'DISCHARGED' — never deleted — across its eleven consumer files plus DESIGN_FP_ARCHITECTURE.md:2349, on TR-5's OWN commit.","ownerGate":"NONE — signed under the 14:4x grant. No push, deploy, migration, deletion or spend. The wave's only STOP condition is a collision one: peaceTermsCatalog.js is WR-shared substrate, so a WR lane in flight on it is STOP-and-report (T9), and peaceTerms.js head seams are size-fenced (below).","outputMoving":"NO at landing — dark behind tradePactsEnabled, with the T-5 fixture pinned byte-identical in the dark arm. ⚠ TWO PINS GO RED THE DAY THIS LANDS, BY DESIGN, and that is not a regression: tests/domain/peaceTermsGrantTerms.test.js:272 ('the three trade-rights rows are SEAMS and stay producer-less') and tests/domain/sovereigntyBundleWr10.test.js:486 ('the trade-rights rows stay producer-less until TR-5 lands'). Both are live at this base — I read them. Their re-pointing, with the reachability obligation moving into TR-5's own pins, is priced into car (a). A TR-5 that reds them without re-pointing them has not landed.","reuse":"THE BIGGEST REUSE WIN IN THE FAMILY: three of TR-5's five term rows already exist (GR-3's, above), so the wave mints ONE row — grain_provision — and spends its budget on executors and producers instead of vocabulary. peaceTermsCatalog.js is at 115/800 effective (the volume's 80 is stale; it grew and is still cheap), so the rows land there and NEVER back in peaceTerms.js. negotiationPictures.js (303 eff) is the ONE terms evaluator and is CONSUMED — a second evaluator anywhere is a design defect (seam 3). strategicPosture.js (136 eff) supplies postureOf and already has a real caller in pactFormation.js, so SP-C's recorded 'zero callers' is stale. treatyOrientation.js and the treaties artifact carry the pacts — zero new top-level worldState keys. ⛔ THE HARD FENCE: peaceTerms.js measures EXACTLY 797/800 effective at this base — the volume's figure to the line, three lines of headroom. Any head seam of ≥4 effective lines is leaf-first or the wave STOPs; re-measure with the enforcer's own Linter at the publishing commit, never inherit my number.","workerKb":"~0 KB. CONFIRMED: peaceTerms.js, peaceTermsCatalog.js, negotiationPictures.js, strategicPosture.js and treatyTransfer.js are none of them in the generation worker's 153-module graph — the worker reaches generators plus a thin domain rim, and the whole treaty estate sits outside it. No lazy chunk needed."},{"wave":"TR-6 — THE CORNER + FAMINE SPECULATOR (corneringEnabled)","status":"UNBUILT","carsLow":4,"carsHigh":5,"reason":"CONFIRMED absent: no cornerGate.js, cornerComposer.js, warehouseSeizure.js, monopolyDwell.js or cornerNews.js. ~920 effective lines of new leaf — the family's largest leaf load, roughly 1.7x TR-1 — and it carries three loads no other TR wave does. (1) NEW CONSERVED MACHINERY: warehouseSeizure.js is a real writer in the commodityFlow family with a five-term balance, months only at the one applicator, serving BOTH the riot counterforce and the seat's forced sale. (2) TR-6 AUTHORS THE commodityFlow WRITER-FAMILY CENSUS — C-TLF-3 is right and I verified it: no such shrink-only source scan exists under tests/lint today (only couplingInclusion and the trade-contract walker mention commodityFlow at all), so the wave writes the census in the T4/storageMonths shape, asserting exactly the measured set, with an executed third-writer plant, in the same commit as the writer it admits. (3) FIVE COUNTERFORCE FIXTURES, EACH WINNING ON ITS OWN — arrivals break, smuggle undercuts, riot seizes (with commonsVoiceEnabled lit), the seat forces sale, the carry ruins — plus NO PHANTOM CORNER (the independent census negative), corner-at-glut impossible, NO PHANTOM MONOPOLY, monopoly reachable-but-rare, cornered reachable-but-rarest, ENTRY breaks the monopoly via TR-2's formation rule, appetite reversal after ruin, T-2/T-3 tellables, and the alignment pair (a good house corners and pays conscience+credibility+commons; an evil one pays less morally and exactly as much physically — ALIGNMENT NEVER GATES either arm). Two mutants: the self-referential-denominator plant and a combining-expression plant in the composer. Cars: (a) cornerGate + the self-referential mutant; (b) cornerComposer + the no-merge scan + flag + fences; (c) warehouseSeizure + the commodityFlow writer-family census + third-writer plant; (d) monopolyDwell + cornerNews + receipt rows + walker; (e) the five counterforce fixtures + the alignment pair + the tellables. Label: PLAUSIBLE.","dependsOn":"TR-2 + TR-3 + TR-4 — the deepest intra-TR chain in the family, which is why it sits late in the §3j order despite its consumer being ready. Plus commonsVoiceEnabled, BUILT/DARK (commonsVoiceKernel.js:41-50, mounted inside assizeKernel.js). ⚠ MEASURED AND WORTH KNOWING BEFORE TR-9: commonsVoiceEnabled is NOT in ENGINE_GATED_VIRTUAL_RULE_KEYS (I read all 35) and has ZERO hits in simulationRules.js — it is a pure virtual with no preset row. The riot fixture lights it in its own rules object, which is fine; but TR-9's kind-availability walker encodes §2's precondition table, and that table names a key the manifest does not carry. Reconcile the spelling when TR-9's walker lands, not after it reds. The underlying spine blockers the volume flags are all satisfied at this base.","ownerGate":"NONE — signed under the 14:4x grant. No push, deploy, migration, deletion or spend. Two STOP-and-report conditions rather than gates: a WR or WC lane in flight on commodityFlow (the writer-family census must assert the measured set, so a concurrent writer invalidates it), and the commonsVoiceKernel header law — TR-6 CONSUMES its rung reads and adds no new effect vocabulary and no new writer there; the seizure writer is TR's own leaf.","outputMoving":"NO at landing — dark behind corneringEnabled, dormancy golden before any wiring. ⚠ ONE SUBTLETY THE FENCE MUST COVER: TR-6 is the wave that finally makes TR-1's fifth registered seam non-undefined. commercialReasons.js:349 carries a LIVE scorer over seam_cornering that nothing writes today, so the TR-1 ledger's famine_profiteering family starts producing entries the day corneringEnabled lights — which is correct and designed (:51 names TR-6 as the owed producer), but it means the lighting moves TR-1's output too, and the second-lighting declaration must say so for both flags together, not for corneringEnabled alone.","reuse":"THE CONSUMER IS ALREADY BUILT AND WAITING — that is this wave's headline reuse: commercialReasons.js:349's scorer over seam_cornering, with :51 naming TR-6 as its owed producer. commonsVoiceKernel.js (214 eff) RUNG_KIND petition/gathering/riot consumed under its no-new-writer header. spatial/moralDrift.js (159 eff) + worldPulse/moralInstitutionPressure.js + moralMartialLean.js READ for the moral pricing — S32's path is resolved and nothing is rebuilt. commodityFlow.js's five-term conservation and its ONE stock-write seam. smuggle.js (119 eff) CONTRABAND_TABLE and the worstGate for the undercut counterforce. entrepots.js (151 eff) TOLL_MAX and centralityGini for the corner envelope. commercialReceiptPools.js for the news rows — but note TR-2 and TR-5 spend that 414-eff headroom first, so re-measure it here rather than assuming it.","workerKb":"~0 KB. CONFIRMED: all five leaves land in src/domain/worldPulse/** or src/domain/spatial/**, neither of which the generation worker's graph contains. ⛔ THE TRIPWIRE: src/domain/region/goodsCatalog.js (435 eff) IS in the worker graph, and the truth-side supply census is exactly the kind of code that reaches for a goods taxonomy edit. cornerGate must READ goodsCatalog and derive its denominator from live M6a stocks — which is also what the independent-denominator law demands anyway, so the size fence and the correctness fence point the same way here."},{"wave":"TR-7 — VENTURES (venturesEnabled)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"CONFIRMED absent: no ventureLedger.js, ventureLegs.js or ventureNews.js. ~670 effective lines of new leaf, a little above TR-1's one-car load, and the pin set is narrower than TR-2's or TR-6's — which is why this is the cheapest multi-car wave in the family. What it must prove: the stake is GENUINELY at risk (a failed venture drops the house's books band — a real consequence, not a receipt); the one-active-venture cap; the trigger is a BAND CROSSING, never drift; the joint-venture split and the default arm both executed; appetite reversal executed; the T-10 Bardi arc TELLABLE on ONE fixture with receipts at each step (fortune → failed venture → ruin); DM-KILL of the supercargo closes the errand 'lost' and the venture 'failed' through their OWN writers, never a second one; a dangling shipmentRef at load closes 'failed' with a receipt rather than surviving as a dangling reference — the lifecycle-bug class this user is bitten by most; the third persisted ledger (spatialLedgers.ventures) with regen PRESERVING in-flight ventures because they are event-accrued; the four-fence + lit-mutant set; the AUTHORED certification row. Cars: (a) ventureLedger + flag + fences + the lifecycle/round-trip pins; (b) ventureLegs + ventureNews + receipt rows + walker; (c) the Bardi tellable + DM-KILL + dangling-ref fixtures if they prove heavy. Label: PLAUSIBLE.","dependsOn":"TR-2 (the books the stake rides) + TR-3 (via the intra-TR chain) + commodityFlowEnabled, which is lit in full_simulation (simulationRules.js:769/:938). The new_route venture kind additionally wants routeLifecycleEnabled, measured FALSE at this base (routeNetwork's 17 modules are dark), so that kind suppresses to {far_market, joint_venture} — an honest declared absence, not a defect, and the kind-availability read in tradeConvergenceContract.js already encodes exactly this. The INT-1 seam is RESERVED here, not required: seatBooks is UNBUILT and strategicPosture.js:36 says so in its own words, so TR-7's venture-appetite books term is ABSENT-not-zero and the absence-then-presence pin PAIR lands in this wave (seam 8).","ownerGate":"NONE — signed under the 14:4x grant. No push, deploy, migration, deletion or spend. One absolute fence rather than a gate: demographicsPlans.js takes ZERO edits. Its :21 header says verbatim 'This is not a general planning system and must not become one', and TR-7's whole design is the plan grammar RE-IMPLEMENTED at house grain precisely so that file is never touched (J-TR-12).","outputMoving":"NO at landing — dark behind venturesEnabled, dormancy golden proving the absent key byte-identical. YES only at the second declared lighting. Nothing in this wave touches an existing decision surface: ventures are a new ledger with a new trigger, and the one existing consumer they reach (M6a/M8 dispatch, for the legs) is read for duration pricing, not rewritten.","reuse":"supplyShipments.js (210 eff) and seaLanes.js (629 raw; SEA_LANE_CAPACITY recorded-unenforced) supply the legs — the road prices duration, and the venture binds to shipment refs rather than minting a second transport. commodityFlow.js's dispatch machinery for the M6a binding. TR-2's houseLedger books are the stake's home, so TR-7 adds no wallet of its own — the Universality Clause's whole point is that books are earned state, and ventures spend them rather than multiplying them. The errand spine (errandMint.js, errandSpineEnabled, manifest member 8) is what the supercargo will later ride at TR-8 — TR-7 reserves the seam and does not build it. commercialReceiptPools.js for the news rows.","workerKb":"~0 KB. CONFIRMED: all three leaves land in src/domain/worldPulse/**, and the generation worker's traced 153-module graph contains none of that directory. The ledger is a spatialLedgers sub-key written through distanceRead.js's conditional-materialization primitives, so it also adds zero eager bytes to the world payload when absent."},{"wave":"TR-8 — THE TRAVELING FACTOR (factorErrandsEnabled; two slices)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"CONFIRMED absent: neither commercialErrands.js nor factorErrand.js exists. ~300 effective lines of leaf for slice 1, which understates the wave badly because slice 2 is a four-system round-trip. Slice 1: the commercial purpose class with three kinds — pact proposal/renewal (which carries the TR-5 call-site SUPPRESSION SWITCH in the WR-7a idiom verbatim), venture supercargo, and the fair circuit with ARRIVAL-grade belief writes — plus the K.2 rule that a snapshot DECAYS and is never truth-refreshed. Slice 2: the compromised factor (declared purpose vs true purpose across the covert seam, both arms executed) and the prize — capture landing in foreignGuestHold.js through ITS one writer, with an import pin proving no second hold writer exists after the wave, then ransom on the I2 person-subject claim, then return. Pins: the speed-floor walker EXTENDS to commercial errands (every movement site through WR-7a's one transit kernel — totality, not sampling); the stale-arrival pin (T-8); suppression-at-call-site proven in both flag states; JSON-alias round-trip; the four-fence + lit-mutant set; the AUTHORED certification row. Cars: (a) commercialErrands + the ERRAND_CONSUMERS row flip + flag + fences + the speed-floor extension; (b) the TR-5 suppression switch + the K.2 stale-arrival pin; (c) the compromised factor, both arms; (d) capture→hold→ransom→return on the war machinery. Label: PLAUSIBLE.","dependsOn":"TR-5 + TR-7 (both UNBUILT — this is the family's terminal build wave and it cannot start until both land) + SP-D, which is BUILT (errandMint.js, errandSpineEnabled manifest member 8) + WR-7b's hold writer, BUILT (foreignGuestHold.js, 424 eff, one writer, cause caught_spying, close reasons release/escape/death/pardon). THE LANDING SURFACE IS ALREADY RESERVED AND SAYS SO IN WORDS: ERRAND_CONSUMERS at envoyErrandVocabulary.js:295 carries a 'factors' row at built:false, and envoyTaskCatalog.js:285-293 records the unblocking act verbatim — 'TR-8 landing factorErrand.js and flipping that registry row to built:true. No design decision is owed; this row is waiting on a dated wave, not on a ruling.' ⚠ A SPELLING COLLISION TO SETTLE AT CHARTER: the registry names factorErrand.js; the TR volume names commercialErrands.js. The registry's spelling is the pinned one and is quoted in a test's evidence string, so either the leaf takes that name or the registry row and its evidence prose move in the same commit. ⚠ AND ONE INHERITED DEFECT: ransomClaim.js:181 requires a home settlement as debtor, so an untied captured factor cannot be priced — slice 2's ransom arm inherits that, and the ransom persistence ruling is on the owner queue.","ownerGate":"NONE for slice 1 — signed under the 14:4x grant. ⚠ SLICE 2's RANSOM ARM TOUCHES AN OPEN OWNER ROW: the ransom persistence ruling is listed among the items awaiting an owner line (OWNER-QUESTIONS-32-34.md). Slice 2 can land the capture→hold→return legs on built machinery and declare the ransom pricing arm degraded-and-dark until that ruling arrives, rather than stalling the wave. STOP-and-report on any WR lane in flight on the envoy files — WR-7 substrate is consumed throughout.","outputMoving":"NO at landing — dark behind factorErrandsEnabled, with the suppression-at-call-site pin's dark arm asserting byte-identity at the TR-5 call site. YES only at the second declared lighting. Note the shape of the lit change for the declaration: lighting TR-8 SUPPRESSES abstract pact rounds in favour of travelled ones, so it removes events as well as adding them — a substitution, not an addition, and the declaration should say that rather than 'new news kinds appear'.","reuse":"SP-D's errand spine consumed whole — the purpose class, the mint, the transit kernel, the speed floor. WR-7's envoy estate (envoyErrandOffer/Records/Evidence/Vocabulary.js) consumed, never duplicated. foreignGuestHold.js (424 eff) is THE hold writer and TR-8 proves by import pin that it remains the only one. The I2 claim shapes for both the person-subject ransom and TR-4's goods robbery. envoyNews.js is the walker-file template the volume names for every new kind. The declared/true purpose split on the covert seam is the Q amendment's existing machinery, which TR-2b also rides — so if TR-2b lands with TR-2, its factor arm stays absent-until-TR-8 and the two waves share one seam rather than minting two.","workerKb":"~0 KB. CONFIRMED: errandMint.js, envoyErrandVocabulary.js, envoyTaskCatalog.js and foreignGuestHold.js all live in src/domain/worldPulse/**, and none of that directory is in the generation worker's traced graph. The whole envoy estate is pulse-side."},{"wave":"TR-9 — TRADE CONVERGENCE INSTRUMENTATION (no flag)","status":"PART-BUILT","carsLow":3,"carsHigh":4,"reason":"SLICE 9c LANDED AND I READ IT: src/domain/certification/tradeConvergenceContract.js, 398 effective / 736 raw — the closed six-key TRADE_ENDING_KEYS vocabulary, the eight-flag lighting table with named degraded arms, the mint table with its one honest-permanent-zero field, evaluateTradeFlagLighting (the out-of-order law made executable) and tradeEndingAvailability, pinned by tests/lint/tradeConvergenceContract.walker.test.js against an INDEPENDENT denominator parsed out of the TRADE volume — a denominator added only after an executed mutant proved the first draft's both-arms block was self-referential (scripts/mutation-coverage-manifest.json:30 records all five mutants and their red counts). Bands were deliberately NOT authored: 'an acceptance band for an instrument that has never run is a signature this slice has no authority to give.' WHAT IS STILL OWED, each verified absent: (i) THE T-11 HERALD DEBT — settlementRumors.js's WHAT_PHRASES carries ZERO of the five route_* kinds (it has route_disruption, a different kind), while all five candidates exist in code: route_chartered (routeNetworkCharter.js:138/:141), route_promoted / route_demoted / route_abandoned / route_revived (routeNetworkDecay.js:123-137). Five WHAT_PHRASES rows + five heraldRouting rows + walker files + a totality pin over exactly those five, and a 17-module estate finally speaks. (ii) the kind-availability walker enforcing §2's precondition table, with out-of-order lighting reding. (iii) the four health envelopes — house-Gini, corner, pact-mix, endings-share — each with an INTERVAL_WEEKS-derived window and an EXECUTED mutant negative control, plus their tuning tables. (iv) tickScanBudget lanes for flows/supplyKernel/commodityFlow/entrepotKernel and every new TR kernel (tests/perf/tickScanBudget.test.js is the home). (v) the soak observation joins on behavioral-observation.mjs's realmSelfSufficiency lane, where collapse reports an HONEST PERMANENT ZERO while routeLifecycleEnabled is dark (measured false). ⭐ AND THE PRICE DROPS: the eight certification rows are NO LONGER TR-9's to convert — see the gate column. Cars: (a) the T-11 route_* Herald debt; (b) the kind-availability walker + lighting-order reds + the tickScanBudget lanes; (c) the four envelopes + tuning tables + per-envelope mutants; (d) the soak joins. Label: PLAUSIBLE.","dependsOn":"The contract module was early-eligible and is landed. Cars (b)–(d) depend on the waves they audit — an envelope over house-Gini needs TR-2's houses, a corner envelope needs TR-6, a pact-mix envelope needs TR-5 — so they are the family's terminus. ⭐ BUT CAR (a) DEPENDS ON NOTHING IN THIS FAMILY. The five route_* kinds already exist in the route estate; the Herald debt is a pure display/registry car that could land TODAY, independent of every other TR wave, and it is the only TR work available before TR-4. ⚠ ONE RECONCILIATION OWED BEFORE CAR (b): §2's precondition table names commonsVoiceEnabled (TR-6's riot arm), and I measured that key ABSENT from ENGINE_GATED_VIRTUAL_RULE_KEYS and absent from simulationRules.js entirely — so the walker must either admit a pure virtual with no manifest row or the key must join the manifest. Settle it when the walker is written, not when it reds.","ownerGate":"NONE — signed under the 14:4x grant; the whole wave is instrument work. ⭐ ONE MEASURED FINDING RE-ALLOCATES WORK OFF THIS WAVE AND ONTO EVERY OTHER TR WAVE, AND IT IS A CHAIR RULING WAITING TO BE RECORDED: the TR volume's Q4 answer — 'each TR wave lands its flag with a DECLARED-PENDING entry; TR-9 converts all eight to real rows' — is DEAD IN CODE. VIRTUAL_PENDING_RULE_KEYS = Object.freeze([]) at subsystemRowsVirtual.js:595, and subsystemRowsVirtual.js:160-162 and :503 both say in words 'AUTHORED, NEVER PENDING… this lane's pending array is empty, so there is nowhere to defer to'. The comment at simulationRules.js:256-258 still promises Q4's road and is stale prose. Consequence: each TR wave owes a full authored certification row in its own flag commit (TR-1's precedent is subsystemRowsCompact.js:265), and TR-9 has nothing to convert. Record the amendment rather than letting an implementer discover it at the gate.","outputMoving":"NO at landing, including car (a) — and the reason is worth stating precisely rather than waving at a flag. The five route_* kinds become MINTABLE the moment their WHAT_PHRASES and heraldRouting rows land, but nothing can mint them: routeLifecycleEnabled measures FALSE (the 17-module route estate is dark), so the Herald rows sit behind an estate that produces no candidates. That is an HONEST PERMANENT ZERO, exactly the shape 9c already encodes for the collapse ending, and the totality pin should assert the zero rather than assume it. Cars (b)–(d) are pure instruments and move nothing by construction. YES only when routeLifecycleEnabled itself lights, which is not this family's decision.","reuse":"tradeConvergenceContract.js's whole landed surface — the vocabulary, the lighting table, the availability read and its independent denominator — is the spine cars (b)–(d) hang on; nothing there is rebuilt. warConvergenceContract.js remains the format precedent. envoyNews.js is the walker-file template for the five route_* kinds. settlementRumors.js (555 eff) and heraldRouting.js (273 eff) take the rows — both well under their ceilings. tests/perf/tickScanBudget.test.js already exists and takes new lanes rather than a new harness. The mutant-plant harness idiom is precedent from 9c's own five executed mutants and GR-2's twelve: plant with a script that asserts its anchor occurs EXACTLY ONCE, compare md5 before and after so an inert plant cannot green, restore from a cp backup and prove it byte-identical.","workerKb":"~0 KB. CONFIRMED: settlementRumors.js is display-layer and heraldRouting.js is realm-layer, and neither appears in the generation worker's traced 153-module graph — the worker reaches generators plus a thin domain rim (arcane identity, culture profiles, faction refs, magic filter, priority bands), not the Herald or certification estates. The certification modules are likewise outside it."}],"firstCar":"Land the CR-2 consolidation car — the flagless, state-free, byte-identical act that both SOL_QUEUE row 14 and FABLE_VALIDATION_QUEUE.md:303 make TRADE's first, and that TR-4 cannot honestly start behind. The work: fold the second storageMonths applicator in `src/domain/worldPulse/applyWorldPulse.js` (`applyFoodStockpileOutcomeToSettlement`, the conquest fold at :88-109 whose write lands at :106 — note the volume's `:322` and `:92-110` are both address-rotted, navigate by symbol) into the estate's ONE applicator, `applyFoodDeltasToUpdates` in `src/domain/worldPulse/generosityUpdates.js:46-65` (90 effective lines, ample room), which `treatyTransfer.js:11-32` already names as the single applicator by ruling. The done bar is a same-seed byte-identical corpus pre/post on a comparator PROVED ABLE TO SEE — plant a one-word change and show it moves rows and the final hash, the way the settlementStrategy CEIL lane did — and if the consolidation shifts behaviour by one row, STOP AND REPORT TO THE CHAIR rather than normalizing it: both records say a live two-writer defect is adjudicated, never silently absorbed, and a measured blocker reported is a success mode here. Land in the same commit the shrink-only storageMonths writer census that this act reduces from five sites to four — re-measured at this base as `foodStockpile.js:406` (+`:479`), `generosityUpdates.js:65`, `applyWorldPulse.js:106`, `magicBufferApply.js:357`, `mutateWorld.js:1153` — with the executed extra-writer plant (cp backup, md5-exact restore, never a `git checkout --` revert: this is a shared tree and a foreign lane's untracked work is the owner's). Two size obligations ride along: removing the fold shrinks `applyWorldPulse.js` below its 907 baseline, so LOWER the `scripts/.size-baseline.json` entry in the same commit — the ratchet reds a shrink that is not banked — and do not touch `pulseKernel.js`, which measures 1581/1581 effective with zero headroom and is banked permanently by R-BLD-10. Before writing a line, get the chair to strike one of the two contradictory records: `DESIGN_FP_ARCHITECTURE.md` §5 #26 carries Q9's pre-ruling that this fold is REPORTED-NOT-DEFECT and merely fenced by the sixth-writer scan, while CR-2 and SOL_QUEUE row 14 order the consolidation — executing the consolidation settles it either way, but the ruling should be recorded, not inferred. Verify the gate really ran (`gate-mutex.sh` exits 0 after 40 polls and a gate line with no printed test count did not run), and re-freeze `deno.lock` before any `--frozen` run. The wave's cheapest follow-on, and the family's second car, is the TR-1 mount: `advanceCommercialReasons` (`commercialReasons.js:457`) has zero callers in `src/`, `subsystemRowsCompact.js:293` names the cure in words, and it rides an existing in-cycle host — `settlementLifecycleKernel.js` at 733/800 or `advanceSettlementSupply` at `pulseKernel.js:772` — never a new kernel line."}}
{"type":"result","key":"v2:84241913df335df363c7a13d2045b538b9183214eb2132e8a16ef104b4616f88","agentId":"ad4826378b84e1f98","result":{"family":"WF — FAITH (docs/DESIGN_FP_ARCH_WF.md; FP program 5 of 6)","summary":"Nine wave-units priced: eight UNBUILT (WF-0/2/3/4/5/6/7/9) and one PART-BUILT (WF-8, whose 8a slice landed). WF-1 is BUILT and not re-litigated. Total band 30-49 cars, centre ~38. The price is anchored on this family's OWN landed cars rather than on the volume's \"one commit each\" sentence, which its own landing refuted: WF-1 shipped as SIX cars (66fda66d4 WF-1a 21 files/1,335 ins · 39d0ad7c8 WF-1b 5/341 · adb5faf3f WF-1C 8/420 · 1a90b5639 WF-1d 8/271 · 3ac279db4 WF-1E 12/852 · 5d18b4a0c WF-1F 7/532) and WF-8A alone (47ea9c9ba) cost 22 files / 1,771 insertions for ONE narration kind, because it minted a registry family. Read the volume's slice counts as roughly one third of the car count. Three measured facts move prices off the record, all CONFIRMED at the slot a5876c0ea. (1) THE WHOLE NARRATION CORPUS IS ALREADY WRITTEN: docs/content/RECEIPT_POOLS_FAITH.md (1,411 lines) carries 103 phrased kinds across nine waves, authored 2026-08-02, verifier-passed, and its §C per-wave counts verify exactly against its own §D-§M section bodies (WF-1 9 · WF-2 14 · WF-3 6 · WF-4 11 · WF-5 10 · WF-6 13 · WF-7 10 · WF-8 22 · WF-9 8). No WF wave pays for authoring; it pays for producers. (2) WF-6 IS CHEAPER THAN THE RECORD: GR-3a landed not only the five faith catalog rows (peaceTermsCatalog.js:228-245) but the four grant-right READERS over the generic pair — missionaryAccessFor:270, sharedRiteFor:278, pilgrimageRightFor:285, toleranceGuaranteeFor:293 in treatyEnforcement.js (113 eff/800) — and all four have ZERO src consumers today, so WF-6 mints consumers and a formation trigger, not executors. The catalog is now 26 terms / 13 families, not the volume's 12/8 (R1) nor its own WF-F \"24/11\" correction. (3) WF-7's STOP DOES NOT FIRE: warDeployment.js measures 684 eff/800 (116 lines of headroom) and has fallen out of scripts/.size-baseline.json entirely, so the volume's \"STOP if warDeployment's headroom is gone at build\" is green today. Two zero-headroom files are live and must be honoured verbatim: peaceTerms.js at 797/800 eff and warTermination.js at its frozen 818 — net-zero seam lines or a STOP with an extraction recipe. THE WORKER BUNDLE IS A NON-ISSUE FOR THIS ENTIRE FAMILY: the generation worker's import closure is 127 modules and contains ZERO faith, worldPulse, peaceTerms, heraldRouting, realmEvents or settlementRumors modules; faith rides advanceInterval.worker.js (513-module closure), which carries no byte-ceiling test in tests/build/. Every WF wave adds 0 KB to WORKER_BUNDLE_CEILING_BYTES (1,404,493 B, monotone-down). Every wave lands DARK behind its own virtual flag and is output-moving only at the SECOND DECLARED LIGHTING, which is owner-gated on sitting item 1's §764.3 amendment. None of the eight faith flags exists in src or tests yet except faithUnseatingEnabled (WF-1's, 9 src / 9 tests).","waves":[{"wave":"WF-0 — THE OBSERVATION FLOOR (no flag; instrument-side)","status":"UNBUILT","carsLow":1,"carsHigh":2,"reason":"The spec demands three things: a `deityBearers` field on the v5 receipt schema, a bearer-count invariant on the two faith rows in subsystemRowsBaseline.js (252 eff/800, ample headroom), and ONE authored deity-bearing soak case beside the whole-world grid. The case is the real work — it must carry two opposed-quadrant deities plus a qualifying grand observance (scaleBand >= 3, act in {procession, offering, fair}) at the patron host with >= 1 mapped reachable neighbour, which is pilgrimage.js:31-38's own bar, and run to a horizon long enough for a patron seat to change. No flag, no world state, no engine code — receipts and a fixture only. Two cars if the v5 schema field and the soak case are split; one if a single implementer takes both, which is the shape WF-1a proved at 21 files.","dependsOn":"Nothing. It is the family floor and the volume's §4 dependency order starts here. Every later WF wave's own-footprint fence is 'a deity-BEARING fixture world advanced dark is byte-identical', and no deity-bearing soak case exists — CONFIRMED: whole-world-soak.mjs builds the grid with `customContent: {}` at :248 and again at :713, and subsystemRowsBaseline.js:60 records the deity-free measurement of 2026-07-31 and names the missing observation verbatim.","ownerGate":"None for the build — signed under the 14:4x grant (no push, deploy, migration, deletion or spend). The `deityBearers` v5 RECEIPT-schema field is a schema widening that #20's ONE trailing OSR mint must swallow (§482.2, §739.2), which is a programme obligation, not an owner signature.","outputMoving":"NO, and uniquely it is never output-moving even at lighting: it mints no flag (J-WF-12's receipt-field ruling) and no world state. The new soak case is ADDITIVE beside the grid, so the four historical archetypes stay byte-identical (whole-world-soak.mjs:170 pins them byte-stable for the first four ids) and no golden moves. It needs no lighting door at all, which is why it can land while sitting item 1 is still open.","workerKb":"0 KB. Nothing ships: the soak case is a script fixture and the field is a receipt. The `deityBearers` promotion touches religiousContest.js, which is in advanceInterval.worker's closure and absent from the ceilinged generation worker's 127 modules.","reuse":"Large. `deityBearers` ALREADY EXISTS as a private helper — `function deityBearers(snapshot)` at religiousContest.js:265, consumed at :591 — so the counting logic is written and the car promotes it to a published read plus a receipt field. The doctrine fixture path is built (mutateEntities.js:820 SET_PRIMARY_DEITY / :864 IMPOSE_CULT). Five dedicated religion soaks already exist under scripts/audit/ (religion-balance, religion-coup-soak, religion-plane-soak, religion-soak, simulate-religion) as the case's ancestors. The certification row's own prose already names the exact observation required, so the acceptance bar is pre-written."},{"wave":"WF-2 — PILGRIMS + LEGATES (`pilgrimageEnabled`; SPREAD; slices 2a season, 2b movers)","status":"UNBUILT","carsLow":5,"carsHigh":8,"reason":"Two slices with genuinely different substrates. 2a mints `pilgrimSeason.js` (<= 250 eff), the co-location gate (genuinely new — V24 verified the tree has NO faith-site linkage; pilgrimage.js's draw is deity-agnostic and discards who-from-where), an injectable belief-leg seam so SP-2 can supply legs later without touching the leaf, a ~5-line subsumption fork in pilgrimage.js (41 eff), the THREE-WAY split negative (no-observance / aspatial / unmapped-or-no-neighbour, each its own seeded arm), and the subsumption-seam disclosure pin measuring the lit-vs-dark observance-success delta on one seed — call it 2-3 cars. 2b mints TWO new leaves, `legateErrand.js` (purposeClass `religious`) and `pilgrimErrand.js` (purposeClass `personal`, never `religious`), each flipping its own registry row's `built:true` in the SAME commit, plus interception through foreignGuestHold.js and the envoyDiplomacyEnabled-dark harassment/turn-back degraded arm — 3-4 cars. The corpus supplies 14 kinds (4 season bands, 2 arc transitions, 4 legate, 3 pilgrim, 1 dossier line), already written.","dependsOn":"WF-0 (its authored observance preconditions are what 2a's roads-fill-at-the-feast pin asserts against). SP-1 the errand spine — DISCHARGED: errandMint.js is BUILT (87 eff, exporting mintErrandSpine/errandSpineFields/errandSpineActive) on `worldState.envoyErrands` per CR-FP-10, and the TWO reserved rows sit in envoyErrandVocabulary.js ERRAND_CONSUMERS at :322-335 with `built: false`, walker-policed both directions. WR-7b's foreignGuestHold.js BUILT (424 eff) — §5 seam 6. ENVOY_PURPOSES stays closed at ['sue','self_parlay']; the lawful join is the class layer, never the war leaf. SP-2 believed-devotion is ABSENT, so the pull's belief leg reads beliefAxes.js's observanceLabel substrate as a declared-dark arm.","ownerGate":"None at build — signed under the 14:4x grant. Lighting is owner (the second declared lighting). J-WF-13's traditions-lane disclosure is a lighting-day act, not a landing act.","outputMoving":"NO at landing — dark behind `pilgrimageEnabled` ANDed with the SPREAD lane, and the traditions golden (the pilgrimage scalar) is the own-footprint fence. YES at lighting, declared and quantified in advance: the subsumption seam changes the observance-success scalar, and the wave's own disclosure pin measures that delta on one seed precisely so the lighting-day shift is a number, not a surprise.","workerKb":"0 KB to the ceilinged generation worker. pilgrimage.js is in advanceInterval.worker's 513-module closure and absent from the generation worker's 127. The two new errand leaves sit under src/domain/worldPulse/ and ride the same pulse graph. No lazy chunk is needed and none is available to move the cost to.","reuse":"Substantial. errandMint.js supplies the whole mint, the clock and the transit seam — the movers are consumers, not a spine. foreignGuestHold.js is the one writer for the hold and is consumed read-only plus close-through-the-writer. pilgrimage.js's dials and `drawsPilgrims` bar are built and become the season's inner math. routeNetworkFlows.js stays UNTOUCHED — the `pilgrim` source row is the volume's declared deferral and ROUTE_FLOW_SOURCES stays frozen this program (V33)."},{"wave":"WF-3 — STANCE CONSEQUENCES (`faithStanceConsequencesEnabled`; SPREAD)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"The cheapest unbuilt wave in the family, because its inputs already exist and are merely unconsumed. One new leaf `stanceConsequences.js` (<= 200 eff) carries the three consumptions so deityStanceLane.js's betrayal machinery (235 eff/800) stays untouched; deityStance.js (60 eff) is READ-ONLY and its deferral comment at :160-174 stays verbatim; sacredClaim.js (85 eff) takes a ~6-line threshold-modifier seam. The pins are the cost: the REVERSAL pin in both directions on one fixture, the COLOUR-NEVER-DROWN cap, the departed-minister lifecycle, the guard-absoluteness regression, and the G1c fence captured FIRST (before wiring, the J1 precedent). Six corpus kinds, pre-written.","dependsOn":"GR-2 peacetime formation — BUILT (pactFormation.js 426 eff, pactProposals.js), which is a real price reduction: the volume wrote the durability->fraying clock against an absent SP-3 with a declared-dark hook, and it can now bind against real pacts. WF-5b's foothold seed is consumed where lit and is a declared degraded-dark read otherwise. No hard blocker. INTERIOR's seatBooks seam is deliberately NOT reserved — the volume's own declared decision, re-affirmed.","ownerGate":"None at build — signed under the 14:4x grant. The G1c golden shift is a lighting-day disclosure (J-WF-8), so the owner meets it at lighting, not at landing.","outputMoving":"NO at landing — the four-fence set plus lit-mutant, and dark means the two consumed fields and every receipt stay byte-identical. YES at lighting: the G1c golden moves, and J-WF-8 already declares it.","workerKb":"0 KB. deityStance.js, deityStanceLane.js and sacredClaim.js are all pulse-graph modules absent from the generation worker's closure.","reuse":"Very high. The two fields this wave consumes — `aggression` and `treatyDurability` — already exist on deityStance.js and are deliberately UNCONSUMED (V12, the deferral comment verbatim at :160-174), so the wave spends nothing minting them. deityStanceLane.js's cooldowns (BETRAYAL 8, PACT 6, FOOTHOLD 6), realm caps and pact-outcome machinery are built. sacredClaim.js's quadrant tables and both-patrons guard are built. STANCE_TUNING and STANCE_LANE_TUNING are two of the eight measured tuning homes, so new dials have a frozen table waiting."},{"wave":"WF-4 — OMEN READS (`omenReadsEnabled`; LOCAL)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"The second-largest green field. One new leaf `omenReading.js` (<= 300 eff) is the ONE writer for a NEW sub-ledger `worldState.spatialLedgers.omenReadings`, carrying five reading kinds each with its opposable arm ON THE SAME ROW, the patron-temper-by-quadrant lens, cooldowns and caps; heraldRouting.js (273 eff/800) takes two token rows. The pins are what push it past three cars: the failed-prophecy arm must be seeded ONCE PER KIND (five fixtures), the NO-PHYSICS byte-diff runs over every physical ledger with an inward-pointed import pin excluding every physical-state writer, the damped-lens echo is its own pin, the razed-mid-expectation close path needs its own seeded lifecycle fixture, and the new sub-ledger owes a full lifecycle re-derivation against CONDITIONAL_LEDGER_KEYS plus a byte-level drop-when-empty fence. Eleven corpus kinds, pre-written.","dependsOn":"WF-0's deity-bearing fixtures. No hard predecessor otherwise — this is the most independently schedulable unbuilt WF wave after WF-0 and WF-3. INFO's false-omen LURE builds THERE and is consumed here, deferred-declared. POP's flight coupling is read-only. WF-8 voices the resolution beats and therefore follows.","ownerGate":"None at build — signed under the 14:4x grant. The Req-14 decision (omenReadings is DM-visible but engine-only v1, on the reasoning that a reading is the pulpit's speech and the DM's existing event verbs already author calamities upstream) is recorded in the volume and vetoable at the CHAIR under the spine's req-14 escape clause, not at the owner. The new persisted sub-ledger is a shape widening #20's trailing OSR mint must swallow. Lighting is owner.","outputMoving":"NO at landing — dark means no ledger key materializes at all, asserted at the byte level against the ensureWorldState loop's measured behaviour for an absent spatialLedgers.omenReadings. YES at lighting.","workerKb":"0 KB. The leaf lands under src/domain/worldPulse/ and heraldRouting.js is in NEITHER worker's closure (it is realm/display-side). Nothing reaches the ceilinged generation worker.","reuse":"Good. The conditional-materialization idiom is mature — spatialLedgers already carries ~20 sub-ledgers (beliefMaps, commitments, commodityStocks, credibility, disinfo, migration, npcLadder, pactProposals and more), so the new key is a well-worn path with `normalizeEnvoyRows` as the dedicated-normalizer precedent. The `divination` desk is LIVE: HERALD_SECTIONS at heraldRouting.js:64 already lists it and its docblock records that divination holds only the forecast tokens, so WF-4's routing split needs no new section. deityAxes' temper-by-quadrant reads are built (V10). The write side moves EXISTING state through EXISTING writers — religionState, piety and religionLegitimacy are all UNTOUCHED."},{"wave":"WF-5 — SCHISM + UNDERGROUND (`faithSchismEnabled`; LOCAL + covert seam; slices 5a split, 5b underground)","status":"UNBUILT","carsLow":5,"carsHigh":8,"reason":"The hardest wave in the family and the one most likely to overrun. It lands TWO persisted additions (`institutions[].creedRef` minted only by the split's reduced-ceremony founding call, and `deities[ref].covert {share band, sinceTick, shepherdRef?}` in place, never a parallel ledger), a new leaf `covertCongregation.js` (<= 300 eff) with four evaluators (growth, assimilation, surfacing, exposure), a creed-aware backing fork in religionLegitimacy.js (227 eff/800), and two named clocks. Its pins are the family's most expensive: the PREMIUM-ISOLATION pin audits covert receipts as absent from all four audience renders (free, shared, lapsed, premium-player); the ANTI-ENTRENCHMENT pin must show the winner's backing does NOT rise against the pre-split baseline while the loser's creedRef row backs the loser only; never-prune-the-cellar must sequence against WF-1's ALREADY-LANDED prune; exposure-at-zero-covert needs a seeded lower bound; and the covert share must render in the closed banded vocabulary, not a number. Two VERIFY-AT-BUILD surfaces (the reduced-ceremony founding call, the reusable covert seam) mean the first car may discover work the volume could not price. Ten corpus kinds, pre-written.","dependsOn":"WF-1 — BUILT, and the dependency is live rather than nominal: the prune cure sequences against religionState.js:353-357 (KEEP = 3, codepoint sort) and the landed suppression writer at :277 that already stamps `suppressedAtTick`, and religionState.js:436 already reads that stamp as the dormancy key. GR-3's `tolerance_guarantee` row — LANDED (peaceTermsCatalog.js:236) — so the surfacing arm has a real term and the volume's declared fallback is not needed. WF-3's foothold seed feeds covert where lit, degraded-dark otherwise. INFO's informant exposure arm is consumed when INFO lights. institutionFounding.js (26 eff) and src/domain/institutions/institutionRoster.js are the VERIFY-AT-BUILD call surface.","ownerGate":"None at build for the code — signed under the 14:4x grant. But the two named clocks (growth half-life 520w, assimilation 156w) are UNSOAKED TUNING: they land as recommendations inside a frozen COVERT_CONGREGATION_TUNING table with executed derivations from measured substrate or the compile STOPs at promotion (§43's rationale-or-STOP), and RATIFICATION rides the owner's tuning signature — THE PROMISE makes tuning owner-SIGNED. Both persisted additions go on #20's trailing OSR mint. Lighting is owner.","outputMoving":"NO at landing — four fences plus a lit-mutant PER SLICE, and both new fields are conditional drop-when-absent. YES at lighting.","workerKb":"0 KB to the generation worker. religionState.js, religionLegitimacy.js and institutionTolerance.js are all in advanceInterval.worker's closure only. Note the creedRef field rides settlement.institutions[] rows, which serialize through the roster — still not the generation worker's graph.","reuse":"Moderate. The suppression and resurgence arms the covert field folds through are built and verified (V4: pruneSuppressed keeps <= 3, deletes in codepoint order, writes {suppressed:true, share:0}). institutionTolerance.js (123 eff) is consumed read-only. INTERVAL_WEEKS has a canonical home at intervalWeeks.js:7 so the two clocks derive from it rather than minting a fourth local copy. The includeCovert / includeGroundTruth double-gated projection discipline already exists for the veil. What is NOT reusable and the volume says so: institution rows are creed-AGNOSTIC presence records today with no congregation concept (V25), and clandestineFacet.js is criminal-only with zero faith references (V26) — the creedRef, the creed-aware backing and the covert seam are all new."},{"wave":"WF-6 — FAITH TERMS (`faithTermsEnabled`; SPREAD) — EARLY IN §3j ORDER","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Priced DOWN against the record, on two measured facts. First, GR-3a landed more than the record credits: beyond the five catalog rows at peaceTermsCatalog.js:228-245 it landed the four grant-right READERS in treatyEnforcement.js — missionaryAccessFor:270, sharedRiteFor:278, pilgrimageRightFor:285, toleranceGuaranteeFor:293 — over the generic grantedRightFor/grantedRightStateFor pair, and every one has ZERO src consumers today (only a couplingRegistryGrammar.js:148 docblock names them). Second, `temple_restitution` is the `restitution` precedent verbatim (executor 'transfer', stream true, baseMag 0.35), so it runs on existing stream physics with no executor at all. So the volume's '<= 300 eff of executor leaves' is an over-price: WF-6 mints CONSUMERS and a FORMATION TRIGGER, not executors. What remains: the formation gate (sacredClaim.js:39-49's both-patrons guard imported verbatim), draft-lens membership — today the faith rows carry NO CLASS_TERM entry, the `non_intervention` precedent, so nothing drafts them; the four consumers, of which missionary_access moving the CHANNEL and never the share is the executor-honesty pin; breach reachability under tolerance_guarantee; the dictated and negotiated arms both reachable; and the bundle-composition fixture plus one herald sentence for the faith-consideration sale. Thirteen corpus kinds, pre-written.","dependsOn":"GR-2 peacetime formation — BUILT (pactFormation.js, pactProposals.js). GR-3a faith rows and readers — BUILT. §5 seam 1's tripwire half is DONE: TERM_FAMILIES is derived at peaceTermsCatalog.js:328 and catalogGrewSinceWr10() already FIRED, with the pin asserting its fired state and naming the grown set exactly ['commercial','faith','population']. WF-6's remaining half of that seam is only its own: derive family membership from TERM_FAMILIES at call time and spell no family literal outside the catalog. WF-2 is NOT a blocker — pilgrimage_right simply cannot form with pilgrimageEnabled dark, and the refusal is receipted with no dead guarantee certified, which is exactly why §3j can schedule WF-6 early. ⛔ THE ZERO-HEADROOM RULE IS LIVE AND RE-MEASURED: peaceTerms.js is 797/800 effective at the slot, so the head takes net-zero seam lines or the wave STOPS and proposes an extraction recipe — never a squeeze (the WF-1 warTermination rule verbatim).","ownerGate":"None at build — signed under the 14:4x grant. Q3 (compose faith families into the WR-10 sovereignty bundle, versus excluding them) is a CHAIR ruling with a recorded recommendation to compose, on the reasoning that exclusion would require the first family literal in sovereigntyBundle.js and break its own source-scan pin; take it vetoably rather than sending it up. Lighting is owner.","outputMoving":"NO at landing — dark behind faithTermsEnabled ANDed with the SPREAD lane; the catalog rows are already landed and flag-shaped per GR-3's own dormancy idiom, consumed here. YES at lighting.","workerKb":"0 KB. treatyEnforcement.js, peaceTerms* and sacredClaim.js are all pulse-graph modules; the generation worker's 127-module closure contains no peaceTerms module at all.","reuse":"THE HIGHEST IN THE FAMILY, and the record does not carry it. The four grant readers are built and idle. The generic grantTermFor / grantedRightFor / grantedRightStateFor trio already encodes the compliance door, the beneficiary resolution and the NEVER-GRANTED vs GRANTED-THEN-VOIDED distinction, in a file at 113 eff of an 800 ceiling. The per-term compliance union {honored, strained, defaulted, expired} plus its trueState fog twin is peaceTermsCatalog.js:122-123's own vocabulary — cited, never re-minted. negotiationPictures.js (303 eff) is BUILT and consumed, never forked. The catalog is DERIVED, so landing nothing widens TERM_FAMILIES further."},{"wave":"WF-7 — THE TITHE (`titheEnabled`; LOCAL)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"One new leaf `tithe.js` (<= 250 eff: draw, wealth fold, disposition), one persisted banded field `religionStates[cid].templeWealth {band, movedTick}` per-settlement through the one writer, a quarterly fold at 13 weeks off the canonical intervalWeeks.js:7 (the peace-terms 12-tick trap is cured law and the pin must name 13), and three small reads: the splendor term riding INSIDE W_INSTITUTION's existing 0.12 slot and never as a new additive (~4 lines in religionLegitimacy.js), wealth-band scaling of generosityEV.js's existing templeMediated faithTerm at :243-253 (403 eff/800), and the sack-loot weight the war SACK arithmetic consumes with conservation pinned. The pins are the price: the SPLENDOR CAP (gold gilds, never buys — it cannot push past the institution slot ceiling), conservation through sack, remission both arms priced, fall-to-coffer both arms, and a non-vacuous secular/temple-less negative. Ten corpus kinds, pre-written.","dependsOn":"WF-1 — BUILT, and load-bearing: the coffers-at-the-fall disposition (inherit named / dispersal conserved, J-WF-15) reads the landed patronFalls ring. The war-family sack coupling needs a COUPLING_REGISTRY row in the same commit as the import, exactly as WF-1d landed CPL-23.FAITH_TO_WAR.WF-1d.dissolution_names_the_fall in couplingRegistryWar.js. ⭐ THE VOLUME'S STOP CONDITION IS GREEN TODAY AND THE RECORD DOES NOT SAY SO: warDeployment.js measures 684 effective of an 800 ceiling — 116 lines of headroom — and has fallen out of scripts/.size-baseline.json entirely (the frozen map now holds only App.jsx 650, explanation.js 827, applyWorldPulse.js 907, pulseKernel.js 1581, roadsKernel.js 838, warTermination.js 818, npcGenerator.js 1345, settlementSlice.js 824). Re-measure at the compile's own base before relying on it. TRADE's CPL-7 port is consumed only when lit.","ownerGate":"None at build — signed under the 14:4x grant; the sack read is a cross-family read with a coupling row, not a war-family edit. The tithe dials, the wealth band words and the splendor cap are UNSOAKED TUNING landing in a frozen TITHE_TUNING table with executed derivations, and ratification rides the owner's tuning signature (THE PROMISE). The persisted templeWealth goes on #20's trailing OSR mint. Lighting is owner. Req-14 is recorded engine-only v1 with the remission lever as the seat's ACT through posture rather than a DM verb; a DM wealth-set verb is a recorded deferral.","outputMoving":"NO at landing — dark means no templeWealth key ever materializes, fenced at the byte level. YES at lighting, and with a reader-visible consequence worth declaring in advance: templeWealth has ZERO writers anywhere in src/domain today (the only hits are the DS-FTH-3 and DS-FTH-4 prose TITLES in warFaith.generated.js and the two comments recording the absence at dossierMounts.js:504-505 and warFaithStateProse.js:96-98), so lighting WF-7 un-darkens DS-FTH-4's four already-authored dossier pools, which are unreachable prose today.","workerKb":"0 KB to the ceilinged generation worker. tithe.js, religionState.js, religionLegitimacy.js, generosityEV.js and warDeployment.js are all in advanceInterval.worker's closure only; the generation worker's 127 modules include none of them.","reuse":"Good on the physics, zero on the concept. V27 is still exact at the slot: the only 'tithe' in the tree is the granary reserve in foodStockpile.js:12-21 and `tithe_rights` as a factionCompetition prize token — and the record's own warning holds, `tithe` matching 38 files is the old economy layer and a grep trap. What IS reusable: the banded-stock idiom from SP-A (bandedStock.js), the canonical INTERVAL_WEEKS leaf, generosityEV's existing faithTerm arm, W_INSTITUTION's existing 0.12 legitimacy slot, warDeployment's existing conserved sack arithmetic at :803-812, and the one-writer fold in religionState.js (373 eff/800, 427 lines of headroom)."},{"wave":"WF-8 — NARRATION 3x (`faithNarrationEnabled`; LOCAL; lights LAST)","status":"PART-BUILT","carsLow":5,"carsHigh":8,"reason":"The 8a slice LANDED (47ea9c9ba, 22 files / 1,771 insertions): faithNews.js (73 eff) carrying ONE governed row `faith_last_altar_dark`, faithReceiptPools.js (9 eff), the EXACT_SECTION desk row, and the kindPoolFloors exception. Its own docblock at :103 concedes the shape — 'the FAITH volume charters roughly twenty further narration kinds and each needs its own producer, so WF-8 is a multi-member train by arithmetic.' Owed: the corpus's §K (15 silent-machinery voices) and §L (7 realm arcs, of which Ascendancy and Twilight already exist as realmEvents.js:270-294 tier-change beats, so FIVE are new work — R2 confirmed, COMPOUND_SIGNATURES enumerates exactly five keys and none is a schism); the >= 3x census walker measuring the LIVE phrased-token baseline at its own landing commit and never inheriting 9 (R6, L8's never-inherit-a-figure applied to a count); the Reformation detector as a pure derived read over WF-1's landed patronFalls rings (window 260w, >= 3 settlements, one cause token, banded); the GOVERNOR flood pin; and the RECORDED SHRINK-BACK OBLIGATION at faithNews.js:107-112 — the member that takes FAITH to five rows or more must remove FAITH from kindPoolFloors.walker.test.js's exact exception list. Rows are cheap once a family exists (grammarNews.js carries 13 rows in 118 eff); PRODUCERS are the cost. Budget 1-2 cars for the WHAT_PHRASES and routing batch, 2-3 for the five realm arcs with seeded-cluster reachability pins, 1 for the Reformation detector, 1 for the census walker plus the shrink-back.","dependsOn":"EVERY other WF wave — it voices their machinery and the volume says it lights LAST. The season voices need WF-2's producers, the stance voices WF-3's, the reading beats WF-4's, the covert beats WF-5's, the coffer beats WF-7's. The Reformation detector needs WF-1 only, which is BUILT, so that car can ride early. Headroom is real but has moved: settlementRumors.js measures 555 eff/800 at the slot, not the volume's 482 — 245 lines for ~20 WHAT_PHRASES rows, with extraction of the faith pool to a leaf as the budgeted fallback; realmEvents.js is 309 eff/800, and the realmFaithArcs.js (<= 250 eff) fallback stays budgeted against the ~700 eff trigger. SP-6's pacing governor is a SPINE deliverable that does NOT exist (V40), so the caps land as program-local constants registered for adoption, declared. ⚠ DOCKET HAZARD, not a code one: this FP wave shares its spelling with the launch-tail's WF-8 / WF-8A doc cars — name the volume when scheduling it.","ownerGate":"None at build — signed under the 14:4x grant. It lights LAST within WF and lighting is owner. Nothing here is a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — flag-dark means zero new WHAT_PHRASES entries and byte-identical output, and J-WF-9 binds dark worlds to stay silent with no retroactive narration. YES at lighting, and more than any other WF wave: this is the one a reader actually sees, so it carries the family's largest declared same-seed text shift.","workerKb":"0 KB to the ceilinged generation worker. settlementRumors.js, heraldRouting.js and realmEvents.js are in NEITHER worker closure — settlementRumors and heraldRouting are display/realm-side and realmEvents rides advanceInterval.worker. The generation worker's 127-module closure contains none of them, so no lazy chunk is needed.","reuse":"Very high, and it is the reason this wave is 5-8 rather than 15+. The ENTIRE corpus is pre-authored and verifier-passed: RECEIPT_POOLS_FAITH.md §K gives all 15 silent-machinery voices and §L all 7 realm arcs, written 2026-08-02. The registry family, its picker, its cured keyed-pick spelling (hash01 avalanching before the multiply, grammarNews' cure copied verbatim) and its corpus leaf all landed at 8a. The three live walkers (heraldRouting.walker, phrasedKindPools.walker, wizardNewsAuthoring.walker) are the registration machinery. `sovereignty_sale_judged` at heraldRouting.js:143 is the war-minted faith-routed precedent the census must count ONCE, and the row that proves the counting rule already exists."},{"wave":"WF-9 — FAITH CONVERGENCE INSTRUMENTATION (no flag; the acceptance harness)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"CONFIRMED absent: there is no faith convergence contract anywhere under src/domain/certification/ (the directory holds warConvergenceContract.js, warConvergenceForces.js, warEndingClassifier.js and tradeConvergenceContract.js, and no faith sibling). The two siblings price it: warConvergenceContract.js is 515 eff and tradeConvergenceContract.js is 398 eff. The content is the volume's §5-WF-9 whole — the eight-token endings walker with `faith_converted` qualified, instrument-arc vocabularies held OUTSIDE the mix with `roads_closed` and `plundered` floors, the tenure tail, per-mechanism occurrence envelopes including WF-2's subsumption-seam delta and WF-4's per-kind reading ratios, the NINE-row counterforce audit plus the sack lure, the realm-arc envelope covering five arcs, the invalid-config walker (a SPREAD flag lit while faithSpreadEnabled is dark must red), and the WF-0 case grown to a small grid. Every envelope carries its own mutant negative control (L7) and the certification rows grown from WF-0's shells must render on the certification surface from a fixture. The eight closed endings tokens are pre-written in the corpus's §M.","dependsOn":"EVERY other WF wave — it measures them, and WF-0's authored case is the grid's seed. TR-9 is the shape precedent and is itself only PART-BUILT (tradeConvergenceContract.js whole, none of eight converted at simulationRules.js:257), so expect the same converted-versus-declared split here. Lighting order must equal build order, which the invalid-config walker is what enforces.","ownerGate":"⭐ THE ONE REAL OWNER GATE IN THE FAMILY, and it is on CLOSURE rather than on build. The volume's own sentence: 'The program is DONE when these envelopes hold on the owner-ordered soak, and not before.' The walk plus ONE regen is owner-held in the endgame (TAIL RULED 08-06), so WF-9 can be BUILT under the 14:4x grant but cannot be CLOSED without the owner ordering the soak. Do not let an implementer report the family complete on a green harness alone.","outputMoving":"NO — it is an instrument act, alignment-empty with reason, no state and no verb, all recorded. It mints no flag and therefore needs no lighting door of its own; what it needs is the owner's soak.","workerKb":"0 KB. Certification and envelope modules ship on the certification surface, not in either worker.","reuse":"High on shape, zero on content. warConvergenceContract.js and tradeConvergenceContract.js are the two working templates and the WR-9 instruments sit beside them. subsystemRowsVirtual.js (284 eff/800) already carries WF-1a's row at :458-480 as the authored precedent for how a faith row reads, including its honest UNOBSERVED verdict and its named closing observation. The mutant-negative-control discipline and the mutationCoverageManifest register are built (V38). What is absent is every faith envelope and the faith contract module itself."}],"firstCar":"Land WF-0 car 1 — THE DEITY-BEARING SOAK CASE AND THE BEARER COUNT — and land it before any other faith work, because it is the only WF wave that mints no flag and no world state, so it needs no lighting door and can ship while sitting item 1's §764.3 amendment is still unsigned, and because every later WF wave's own-footprint fence is worded \"a deity-BEARING fixture world advanced dark is byte-identical\" against a corpus that has none: whole-world-soak.mjs builds its grid with `customContent: {}` at :248 and again at :713, which is exactly the deity-free measurement subsystemRowsBaseline.js:60 recorded on 2026-07-31 and has been grading UNOBSERVED on ever since. The car does three things. First, it promotes the private helper `deityBearers(snapshot)` at religiousContest.js:265 (today called once, at :591) to a published read and adds the `deityBearers` field to the v5 receipt schema, which is a promotion rather than a green field — the counting is already written and doctrine-clean. Second, it authors ONE soak case BESIDE the existing grid, never replacing an archetype, so the four historical ids stay byte-stable (whole-world-soak.mjs:170) and no golden moves: the case carries the spatial canon, two opposed-quadrant deities and a qualifying grand observance — scaleBand >= 3 with act in {procession, offering, fair} at the patron host plus at least one mapped reachable neighbour, which is pilgrimage.js:31-38's own bar and is simultaneously WF-2a's precondition pin — at a horizon long enough for a patron seat actually to change, and every deity is seated through the doctrine path (mutateEntities.js:820 SET_PRIMARY_DEITY / :864 IMPOSE_CULT), never by poking config, which is the fixture law every WF wave after this one inherits. Third, it pins BOTH ARMS non-vacuously — the bearer count correct on the bearing fixture AND exactly zero on the deity-free corpus — plus the doctrine round-trip through the custom-content path, and it carries ONE executed mutant with a mutationCoverageManifest row: flip the bearer predicate to count cult embeds only and the pin must red. Never derive the count from wizard_news.* ids (the moverFamily skew law). Req 13 is recorded alignment-empty WITH REASON, since a count instrument reads no axes; Req 14 is recorded engine-only instrument under the spine's own escape, with the certification-surface render pin as its dossier landing. The acceptance bar is not this car's to invent — subsystemRowsBaseline.js:60 already spells it out verbatim (\"one soak case whose settlements carry an embedded config.primaryDeitySnapshot, at a horizon long enough for a patron seat to change, plus a v5 subsystems.stateKeys census over worldState.religionStates\"), so the car is done when those two faith rows can grade on evidence instead of grading UNOBSERVED. Three hazards bind it: re-freeze `deno.lock` before any `--frozen` run or the 656-case edge suite stays invisible; a gate line printing no test count DID NOT RUN, because gate-mutex.sh gives up after 40 polls and exits 0; and the receipt-schema field must be added to the #20 trailing OSR mint's bill in the same breath, not discovered by it later."}}
{"type":"started","key":"v2:e49f79256dec9d8f0df6fe59802ec79ade8debf7ae53d87266f773836e05a1e3","agentId":"a28674c8ca3bfe988","label":"price:GR-CW","phase":"Price"}
{"type":"result","key":"v2:4934367803f18257888f090a26f1719d686429289960719cce6731330a9f1ce6","agentId":"aa82e67da3850db14","result":{"family":"IN — INFORMATION (docs/DESIGN_FP_ARCH_IN.md, governing volume DESIGN_FP_INFORMATION.md; SOL_QUEUE A2 row 13)","summary":"IN-0 is COMPLETE at the slot — all four slices, including IN-0c which the record still calls \"owed/BLOCKED\" (`peaceTermsDisclosure.js` exists, the signing credit is wired at `informationStatecraft.js:1442` through the pre-plumbed `provenTrue` param, `couplingRegistryInfo.js:128-143` carries `owningWave: 'IN-0c'` with kind `treaty_disclosure_opened`). IN-1 is further built than the first pass's row says: 1a, 1b AND 1c-a are all landed (`secondOrderBelief.js` 299 lines, `neighbourMirror.js` read-model, `informationNews.js` + `informationReceiptPools.js` as the estate's fifth phrased-kind registry); only IN-1c-b remains, and it is blocked on a chair sitting (ODQ §55 Q3+Q5), not on code. The remaining family is IN-1c-b + five unbuilt waves, priced at 19–28 cars total.\n\nFOUR MEASURED FACTS RE-PRICE THIS FAMILY AGAINST ITS OWN VOLUME:\n\n(1) THE WORKER BUNDLE DOES NOT BIND IN AT ALL — and this is the cleanest answer available. A BFS over `src/workers/generation.worker.js`'s static+dynamic import graph reaches 153 modules and ZERO under `domain/worldPulse`, `domain/realm`, `domain/display`, `domain/certification`, `domain/spatial` or `components/`. Every file every IN wave touches is outside that graph. So `WORKER_BUNDLE_CEILING_BYTES = 1404493` (`tests/build/generationWorkerLazy.test.js:78`, zero headroom) is not a constraint on any IN wave: each adds ~0 KB, and no lazy chunk is needed. IN adds no generator code.\n\n(2) THE TWO HOT HEADS ARE AT THE CEILING AND THE VOLUME SAYS THEY ARE NOT. `informationStatecraft.js` measures **781 effective lines against the 800 domain ceiling (eslint.config.js:710-713, skipBlankLines+skipComments) — 19 lines of headroom**; `beliefMap.js` is also at 781/800. The volume asserts \"1428 physical, effective well under 800\". That is now false, and it changes the SHAPE of IN-2 and IN-3: neither can put its law in the statecraft head, both must land as delegation-to-leaf (the `errandMint.js` recipe). `eventProse.js` is at 750/800 (50 lines), so IN-2's NAMED LIE variant lands in a prose leaf. `settlementStrategy.js` is at 760/800 with its baseline entry DELETED — the volume's \"812, AT ceiling\" is stale, though IN touches it nowhere.\n\n(3) IN-4'S STATED BLOCKER IS REFUTED AT THE SLOT. The volume says the courier migration \"still waits on SP-1's GENERALIZED errand spine\". SP-D built it: `errandMint.js` (\"THE GENERALIZED ERRAND MINT HEAD\", gate `errandSpineEnabled` read exactly once at :76, `mintErrandSpine` at :257), and its header names \"ES-1/IN-4's couriers\" as a chartered consumer. The walker-held consumer registry at `envoyErrandVocabulary.js:350-355` carries IN-4's row BY NAME with its file address pre-declared: `{consumer:'couriers', purposeClass:'covert', module:'src/domain/worldPulse/covertErrand.js', wave:'IN-4', built:false}` — split out of the shared ES-1 row precisely so ES-1's landing would not falsely declare IN-4 built. Combined with `routeNetworkConsumersRace.js` (a fully built, tuned, 7-export pure leaf with `RACE_OUTCOMES` frozen at :104 and `reputationRace` at :322, whose only importers are one certification row and tests), IN-4 is the cheapest unbuilt wave in the family and has no IN-internal dependency.\n\n(4) IN-5 IS THE ONLY IN WAVE THAT MOVES SAME-SEED OUTPUT AT LANDING. `belief_misjudgment: 'faith'` at `heraldRouting.js:143`; the refile moves it to the new knowledge desk. Its producer is gated on `beliefsActive` (spatial canon marker AND `infoMode !== 'omniscient'`), and `infoMode: 'full'` ships in the Full Simulation preset (`simulationRules.js:930`) with `'perfect_delayed'` in Living Realm (`:857`). IN-5 has no flag by design, so nothing hides it. That refile is a declared same-seed shift and belongs at the §764.3 door, not under the standing grant — it is the family's one genuine owner gate.\n\nRE-MEASURED CENSUSES (never quote the volume's): `EXACT_SECTION` now carries **381 routed kind tokens** (war 91, trade 129, events 134, faith 19, divination 8) with zero knowledge section — the volume's 354 and the original 269 are both stale and the starvation is worse than either. `HERALD_SECTIONS` has only THREE src consumers (`heraldRouting.js`, `components/map/heraldFeed.js:20/:145/:226`, `worldPulse/brokerageStamps.js`) plus seven test files — IN-5's consumer census is cheaper than the volume feared. `PLANT_REFUSALS` is SIX today (`brokerageServicesPlant.js:90-92`), so IN-3's pin asserts SEVEN after `too_hot`; `QUERY_REFUSALS` is five and stays untouched. The engine-gated manifest has grown from 5 keys to 24, and `intelTradeEnabled` is STILL absent from it despite three real `=== true` gate reads (`intelActs.js:98`, `generosityKernel.js:1009-1015`, `informationStatecraft.js:1364`) — J-INA-4's cure is still owed and still correct.\n\nCAR CALIBRATION from this family's own landed work: IN-1a = 1 car (15 files, 1,091 insertions), IN-1b = 1 car (20 files, 863), IN-0a = 2 cars (17 files / 1,230 for the build, plus a repair car), IN-0d = 1 car (3 files, 523). An honest certification row costs 25–40 effective lines (`subsystemRowsVirtual.js:41`), a flag mint costs one dormancy-fence file (60 such files exist to copy from), and `subsystemRowsBelief.js` at 252/800 has room for all three remaining IN rows.","waves":[{"wave":"IN-1c-b — the interim knowledge desk + the three certification-surface movements (the mirror's last slice)","status":"PART-BUILT","carsLow":1,"carsHigh":2,"reason":"IN-1 is three-quarters landed and the first pass's row understates it. BUILT: IN-1a (`src/domain/worldPulse/secondOrderBelief.js`, 299 phys / 139 eff, pure, no worldState in the derivation half — the K3 fence is structural), the flag `secondOrderBeliefEnabled` already in ENGINE_GATED_VIRTUAL_RULE_KEYS with its authored row at `subsystemRowsBelief.js:314-338`, plus `tests/property/secondOrderBeliefDormancyFence.test.js`; IN-1b (`src/domain/display/neighbourMirror.js`, 224 phys / 84 eff, the standing line mounted at `RelationshipsTab.jsx:41`, with `tests/ui/neighbourMirrorLine.test.js` as a driven rendered-surface golden); IN-1c-a (`informationNews.js` 182 + `informationReceiptPools.js` 62 — the estate's fifth phrased-kind registry, one registered kind `mirror_standing_line`, a section-null DOSSIER row, walker `tests/lint/informationKindPools.walker.test.js`). IN-1c-c is REFUSED (no producer). What is left is exactly what ODQ §55 blocked: Q3, the interim-desk decision — an `EXACT_SECTION` row for the INFORMATION beats to `events` carrying a DATED IN-5 re-point comment, so the silent catch-all fallback becomes a recorded decision — and Q5, three certification-surface movements ruled together as one charter. That is the smallest landed IN shape (cf. IN-0d at 3 files / 523 insertions), so 1 car. It is 2 if the chair also lands J-INA-5's SEVENTH input family, which never shipped: `mirrorInputsAt` reads plants (`spatialLedgers.disinfo`), `intelTransfers`, `secrecyPostures`, `envoyErrands` (the interception leg) and our own belief record — the WR-7b NEGOTIATION PICTURES are not among them, so the volume's 'seventh ledgered family' is still owed and would carry its own degradation pin and allow-list import fence. Two other IN-1 legs are RECORDED DEFERRALS, not gaps to re-find: the exposure-receipt degradation leg (one-shot news with a sidecar drained next tick) and the ally-share channel (no persisted source at this HEAD), both named in the leaf's own header.","dependsOn":"The §55 Q3+Q5 chair charter sitting, still open (RESUME_STATE.md:1560 lists 'IN-1c-b charter' among chair-owed sittings; :1677 'IN-1c-b blocked on the Q3+Q5 charter'). Nothing in code blocks it. ORDERING CONSTRAINT: it must land BEFORE IN-5, because Q3's own ruling makes the interim desk row a DATED IN-5 re-point comment — land it after IN-5 mints the seventh section and the comment is born stale.","ownerGate":"None. Chair sitting only (§55's five questions were already SIGNED; this is the deferred half). Signed under the standing grant.","outputMoving":"NO, at landing and at lighting. `mirror_standing_line` files no Herald desk and routes no token today — `informationNews.js:18-22` records that the row 'registers WITHOUT routing', which is exactly what raises the registered-minus-routed honesty check by one; the interim EXACT_SECTION row routes a token nothing currently files. The read-model's identity check against the frozen unknown short-circuits above the composition on a dark path, so the three aliveness channels stay empty on the merits. The certification movements are receipt-surface.","workerKb":"0 KB. `secondOrderBelief.js`, `neighbourMirror.js`, `informationNews.js`, `heraldRouting.js` and the certification leaves are all outside the generation worker's 153-module graph (measured by BFS from `src/workers/generation.worker.js`). No lazy chunk needed; the 1,404,493 B ceiling is untouched."},{"wave":"IN-2 — THE LURE (`infoLureEnabled`)","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"`infoLureEnabled` has ZERO hits in src and tests. Seven distinct obligations, and the ceiling re-measure changes the shape of two of them. (1) THE AXIS WIDENING is not one key list but FIVE frozen ones in `disinformationPlant.js`: `PAID_PLANT_KEYS` (4), `PAID_PLANT_RECORD_KEYS` (7: assertedBand, audienceId, liarId, lineageId, seededTick, subjectId, trueBand), `PAID_PLANT_OVERRIDE_KEYS` (6), `PAID_PLANT_RECEIPT_KEYS` (9), `PAID_PLANT_TARGET_KEYS` (10). The `axis` / `assertedValue` / `trueValue` fields must enter at least RECORD and RECEIPT, and the negative pin is MANDATORY and already named by the estate: `subsystemRowsBelief.js` records that ES-3 routed its products away from 'the paid-plant override road, whose exact six-key envelope would have SILENTLY rejected an axis payload and left a product looking landed while writing nothing'. That is this wave's sharpest failure mode, pre-diagnosed. (2) J-INA-3's DEVOTION BAIT is buildable NOW and cheaper than the volume knew: `faithLabel` is already in `PAID_PLANT_OVERRIDE_KEYS` and `CHANNEL_BELIEF_AXES.faith = ['faithLabel']` (`brokerageServices.js:122-129`), so the override channel is open and only the disinfo-side typing, comparator and receipt are absent. (3) SCARCITY/CONDITIONS stay HARD-BLOCKED — `CHANNEL_BELIEF_AXES.trade` is frozen EMPTY — so the wave closes axis ∈ {strengthBand, faithLabel} with `axisFamiliesGrewSinceIn2()` as the SP-2 tripwire. (4) The TWO-CHANNEL source scan against `ENVOY_PICTURE_PLANT_FIELDS` (ten frozen fields at `brokerageServicesPlant.js:95-106`, enforced at :178) in both directions. (5) A bait-selection leaf aiming through IN-1's built `mirrorOf`. (6) The flag mint: manifest join + a 25–40-effective-line certification row + the four-fence dormancy set with its lit mutant + at least one BY-NAME gate read (the conjunction-gate hole). (7) The DM LIE-COMMISSION VERB through the store-action lane — a row in `src/store/operationRegistry.js` (640 lines, dense typed table with opType/klass/slice/targetScope/receiptRef/undoToken/undoState), a store slice action, `gen:compendium-data`, an approval-routed typed AI proposal, and its UI; this discharges INFO's named verb gap from CR-7 and is a car in its own right. THE CEILING RE-PRICE: the fold-side axis law cannot land in `informationStatecraft.js` (781/800, 19 lines) and comparator work cannot land in `beliefMap.js` (781/800), so both go to leaves; the NAMED LIE grievance variant goes to a prose leaf because `eventProse.js` is at 750/800. Cars: 1 flag+manifest+row+fences · 2 the axis widening through the five key lists with the absent-axis⇒legacy-strength-semantics migration pin and the JSON round-trip · 3 the axis-typed comparator + devotion bait + two-channel scan · 4 the bait leaf and the volume's pin set (THE RESISTED LURE, THE UNATTRACTIVE BAIT, BLUFF-VS-BLUFF on the expiry arm with EXPOSE_MAX_AGE_TICKS 8, spring reachability under BOTH J-INF-14 restraint states) · 5 the DM verb · 6 receipts and prose. Low end 4 if the verb and the prose ride cars 3–4.","dependsOn":"IN-1a/1b, LANDED (the bait aims through `mirrorOf`; §2 names `secondOrderBeliefEnabled` a lit-precondition). `informationBrokeragesEnabled` × `infoStatecraftEnabled`, both dark/declared-false today so dormancy is trivially fenceable. SP-2's trade families for the scarcity/conditions baits — NOT a blocker, they are deliberately deferred with a tripwire (volume §5 pre-pin 2). Serializes against IN-0a and IN-3 on `brokerageServicesPlant.js` (the volume's collision map: 'serialize within IN'). WY-2 owns the severity-durable rumour fidelity law — IN-2 consumes it, never re-implements (and `severityDurableRumorsEnabled` is 0 in src today, so that arm degrades).","ownerGate":"Owner-gated CLASS, signed on precedent. The axis fields are a PERSISTED-SHAPE WIDENING of `spatialLedgers.disinfo[]` records. It mints no new ledger and moves no state-key census (the estate's own rule: `censusWorldStateKeys` counts ledger ENTRIES and never the fields inside them), but persisted shape is the owner's class by standing law and the shape must ride #20's ONE trailing OSR mint (first pass §C.1). The controlling precedent is OWNER-QUESTIONS-32-34 row 4 on `civilContests`: 'Grant; lands dark; the shape rides #20's one mint.' Signed under the standing grant on that precedent; the OSR mint itself stays the owner's act. The DM verb is a new player-facing surface, not a paid-surface change.","outputMoving":"NO at landing — dark behind `infoLureEnabled` with the four-fence set, and both preconditions (`informationBrokeragesEnabled`, `infoStatecraftEnabled`) are dark in every shipped preset, so the dormancy golden is byte-identical including the ONE_REGEN preset. The DM verb adds a surface, not a same-seed mover. YES at the second declared lighting.","workerKb":"0 KB. `disinformationPlant.js`, `informationStatecraft.js`, `brokerageServicesPlant.js`, `eventProse.js`, `operationRegistry.js` — none is in the generation worker's graph. No lazy chunk needed."},{"wave":"IN-3 — THE COUNTER-GAME (`counterIntelEnabled`)","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"`counterIntelEnabled` has ZERO hits in src and tests. It is the family's largest wave by count of distinct new mechanisms: five verbs (sweep, vet, send-two, HIDE-as-answer, false accusation), a `suspicionOf` derivation, a house sub-program with its own producer, two DM surfaces and a DM verb. WHAT IT CONSUMES RATHER THAN BUILDS (R2 verified at the slot): `sendTwoDivergence.js` (230 lines) is built and its header IS the one-home contract — IN-3 imports it and a source scan proves no second reader; `envoyTestimony.js` (415) is built and equality-pinned to `brokerageStamps`' RELIABILITY_LADDER, so the vet composes the ladder and never re-spells it. WHAT IS GENUINELY ABSENT AND MEASURED: `INTERCEPT_ENDINGS = ['read','refused']` with `brokerageServices.js:587` stating in words that 'caught is IN-3's sweep, declared not built'; `PLANT_REFUSALS` is SIX at `brokerageServicesPlant.js:90-92` (the volume's 'frozen five growing to six' is stale — the pin asserts SEVEN after `too_hot`, and `QUERY_REFUSALS` stays five and untouched); `projectPatronBindings(bindings, { audience='dm', exposed=[] })` at `brokeragePatronage.js:319` still has NOTHING supplying `exposed` — IN-3 is the wave that finally closes that gap with a producer AND a surface; `sightPostures` is written at `informationStatecraft.js:1321` and has zero UI consumer (the Watch panel is its first). Substrate with room: `corruptionWeb.js` at 510/800 (290 headroom) hosts the organic exposure producer; `npcCredibility.js` (289) supplies the mouthpiece leg of the blowback triple (R6). THE CEILING RE-PRICE: HIDE-as-answer wants the statecraft head, which has 19 lines — it must be a delegation call into a leaf, not an inline arm. Cars: 1 flag+manifest+row+four fences · 2 the suspicion leaf (with the import pin, the token scan and guard-the-guard proving suspicion never reads ground truth) · 3 vet + send-two as consumers with the no-second-reader source scan · 4 sweep + `caught` + `too_hot` + HIDE-as-answer through the leaf · 5 the house sub-program (exposure producer supplying `exposed`, `false_accusation` registered at mint, the witch-hunt counterforce with the no-fates phrase scan biting on fate verbs) · 6 the Watch panel + Houses block (two components, ≤600 effective each) · 7 the SWEEP DM verb. Low end 5 if the surfaces fold into cars 5–6 and the DM verb rides IN-2's already-cut store-action lane.","dependsOn":"IN-1 for the mirror-gap evidence arm of `suspicionOf` — NOT a hard block, it degrades dark by design (a named degraded read). `infoStatecraftEnabled` only. IN-2 for FILE SERIALIZATION on `brokerageServicesPlant.js`, not for behaviour — the volume's collision map requires IN-0a/IN-2/IN-3 to serialize on that file. NO war-side edit: Q2's ruling stands — read-only consumers behind IN flags with a war-chair queue row per touch, never a behavioural edit to a war path, never an IN-side fork.","ownerGate":"None new. §3 is explicit that `sightPostures` gains NO fields and IN-3 mints no ledger, so there is no persisted-shape question here. Signed under the standing grant. J-INF-17's fail-open default (`projectPlants(records, { audience = 'dm' })`) stands as ruled; the Watch panel and Houses block are DM-only fail-closed projections passing `veilPublicPayload`.","outputMoving":"NO at landing — dark behind `counterIntelEnabled`, a virtual key absent from DEFAULT_SIMULATION_RULES and every preset. YES at the second declared lighting. The two new DM surfaces are player-side empty by construction.","workerKb":"0 KB. `brokerageServices*.js`, `brokeragePatronage.js`, `corruptionWeb.js`, `npcCredibility.js`, `informationStatecraft.js` and the two new components are all outside the generation worker's graph. No lazy chunk needed."},{"wave":"IN-4 — THE ROAD (couriers, double agents, the race) (`reputationRaceEnabled`)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"The cheapest unbuilt IN wave, and the volume's own blocker for its courier half is REFUTED at the slot. `reputationRaceEnabled` is 0 in src and tests, but BOTH halves sit on built, tested, pre-declared substrate. THE RACE HALF: `routeNetworkConsumersRace.js` (364 phys / 147 eff) is a complete pure leaf with seven exports — `ROUTE_RACE_TUNING` :85, `RACE_OUTCOMES` frozen ['person','story','together','neither'] :104, `newsGradeImpedance` :119, `newsHopTicks` :138, `listenerShare01` :175, `storyArrivalTicks` :209, `reputationRace` :322 — carried in `tests/lint/.tuning-inventory.json` and `.tuning-register.json` and pinned by `tests/domain/routeNetworkConsumersRace.test.js` and `namedPersonTransitTotality.walker.test.js`, with ZERO engine consumers (the only src importer is `subsystemRowsWaves.js`, a certification row). This is wiring, not a green field. THE COURIER HALF: SP-D's generalized errand spine is BUILT — `errandMint.js` ('THE GENERALIZED ERRAND MINT HEAD, and the errand spine's one gate'), `errandSpineEnabled` read exactly once at :76, `mintErrandSpine` :257, `errandSpineFields` :141, `covertFaceFor` :189, `covertMissionRefusal` :221 — and its header names 'ES-1/IN-4's couriers' among the five volumes that mint through it. The walker-held consumer registry at `envoyErrandVocabulary.js:350-355` carries IN-4's row BY NAME with its file address already chosen: `{consumer:'couriers', purposeClass:'covert', module:'src/domain/worldPulse/covertErrand.js', wave:'IN-4', built:false}` — and the source comment records that this row was SPLIT OUT of a shared 'ES-1/IN-4' row precisely because landing ES-1 against it 'would have declared IN-4 built and pointed the registry at a file that does not exist'. So IN-4's file address is pre-declared and the walker is already holding the slot open. J-INA-4 IS STILL OWED AND STILL CORRECT: the engine-gated manifest has grown to 24 keys and `intelTradeEnabled` is absent from every one of them, despite three real `=== true` gate reads (`intelActs.js:98`, `generosityKernel.js:1009-1015`, `informationStatecraft.js:1364`) — exactly the invisible-key defect `simulationRules.js` warns about in its own text. A SEQUENCING FINDING THE VOLUME MISSES: §2 requires the manifest walker to assert EXACTLY a one-key delta per commit, so the J-INA-4 cure and the `reputationRaceEnabled` mint CANNOT share a commit — the cure needs its own car first. Cars: 1 the J-INA-4 manifest cure alone with its certification row and one-key-delta assertion · 2 the flag mint + four fences (captured against the ONE_REGEN preset too, since `distancePricedNewsEnabled: true` at `simulationRules.js:667` makes the story leg live machinery in a shipped config) + the race-consumer leaf reading `reputationRace` at the arrival joins, carrying THE TRUTH THAT ARRIVED TOO LATE (the jewel) and the trivial-race-silent negative with its seeded twin · 3 `covertErrand.js` on the built spine, the D-3 deposit-courier retirement pinned dark-byte-identical with DM-KILL closing `lost` through SP-1's one writer, and the JSON-round-trip alias trap on courier fixtures · 4 only if the interception cross-pins into WAR-owned `envoyInterceptionStage.js` need their own dock under Q2's queue-row discipline. NOTHING in IN-4 touches the chooser — `settlementStrategy.js` (760/800) is not read or written, and the volume records that explicitly.","dependsOn":"NOTHING inside IN — this is the one IN wave with no intra-family predecessor, which is why §3j's order already places it ahead of the critical path. `errandSpineEnabled` (built, dark) for the courier arm; `distancePricedNewsEnabled` (LIT in the ONE_REGEN preset spread) for the story leg; `intelTradeEnabled` × SP-1's flag for the D-3 retirement, whose dark arm is the landing state. Person legs degrade to arrivals the existing ledgers already stage (armies, exiles, refugee columns, and envoy returns — the last is substrate the volume could not name and WR-7 has since built).","ownerGate":"None. J-INA-4's manifest join is a chair ruling with a reason, recorded and vetoable — the volume's own Q3 already carries the recommendation and its alternative. Signed under the standing grant.","outputMoving":"NO at landing — dark behind `reputationRaceEnabled`, dormancy captured against BOTH the defaults and the ONE_REGEN preset. ONE HONEST CAVEAT that must be declared in the row rather than discovered: the J-INA-4 cure makes `intelTradeEnabled` CENSUSABLE, which moves the certification receipt's key partition. That is a receipt-surface movement, not a same-seed one, and it is the point of the cure — but it must be stated, not smuggled.","workerKb":"0 KB. `routeNetworkConsumersRace.js`, `errandMint.js`, `covertErrand.js`, `intelActs.js`, `generosityKernel.js`, `simulationRules.js` — none is in the generation worker's graph. No lazy chunk needed."},{"wave":"IN-5 — THE VOICE (the seventh Herald section; no flag)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"`informationNews.js:24` states the contract in words: 'the section vocabulary is a frozen six and the seventh is IN-5's to mint.' RE-MEASURED AT THE SLOT, and every figure in the volume is stale: `HERALD_SECTIONS` is the frozen six at `heraldRouting.js:64`; `EXACT_SECTION` carries **381 routed kind tokens** (war 91, trade 129, events 134, faith 19, divination 8) and ZERO knowledge section — the volume's 354 and the original 269 are both wrong and the starvation diagnosis is STRONGER, not weaker. The one cost the volume overestimates: the HERALD_SECTIONS consumer census is SMALL — three src readers (`heraldRouting.js`, `components/map/heraldFeed.js` which builds its section map at :145 and its counts at :226, and `worldPulse/brokerageStamps.js`) plus seven test files including `tests/lint/heraldRouting.walker.test.js` (totality both ways + single-home + KIND_SECTION consistency) and `couplingDesk.walker.test.js`. Growing a frozen six to a seven touches all ten, but none is a rewrite. Scope: the seventh section token + its correspondence rows + the walker update + an EXECUTED consumer census (measured, never assumed); the arc composer (pure, no state — a save/load mid-arc recomposes identically); the hums; the SCANDAL retrospective join (CPL-20); the inaction receipt `court_sat_still` with its negative (LOW-confidence sitting still mints NOTHING); the significance ceiling raised 'notable' → 'major' for exactly three banded shapes (J-INF-8 — today's ceiling verified live at `informationStatecraft.js:584/:1007/:1338`); one walker file per new kind on the envoy template, and the envoy walkers now exist to copy. `intel_transfer` STAYS under trade (the authoring ruling at `heraldRouting.js:120` is unoverturned). Cars: 1 the seventh section + correspondence + walker + the executed consumer census · 2 the arc composer + hums + the SCANDAL join · 3 `court_sat_still` + the significance-ceiling raise for the three banded shapes · 4 the REFILE as its own car, goldens first, held at the door · 5 only if the consumer census turns up a reader the frozen-six shape breaks.","dependsOn":"**ES-6b**, which is UNBUILT — the first pass measures ES-6 as PART-BUILT (`espionageLeak.js:2` is ES-6a, mounted at `espionageProductStage.js:753`; ES-6b has zero hits). IN-5 sits mid-critical-path ES-6b → IN-5 → ES-7 → CW-3, and ES-7 is explicitly held for it: `subsystemRowsBelief.js:169` reads 'the six Herald kinds are ES-7's, in ES-7's commit', with `missionDispatcher.js:9/:402`'s refusal standing. ALSO **IN-1c-b must land first** — ODQ §55 Q3 makes IN-1c-b's interim desk row a DATED IN-5 re-point comment, so the reverse order is born stale. The critical path is strictly serial and this wave is its narrowest point.","ownerGate":"**YES — the family's one real owner gate, and it is car 4 only.** The `belief_misjudgment` REFILE is NOT dark-safe and IN-5 has no flag to hide it: the kind routes to 'faith' today at `heraldRouting.js:143`, the refile moves it to the new knowledge desk, and its producer is gated on `beliefsActive` (a spatial canon marker AND `infoMode !== 'omniscient'`) — with `infoMode: 'full'` shipping in the Full Simulation preset (`simulationRules.js:930`) and `'perfect_delayed'` in Living Realm (`:857`). So the refile moves Herald output in shipped configurations at landing. That is a DECLARED SAME-SEED SHIFT and belongs at the §764.3 door / the second declared lighting, NOT under the standing grant — and the first pass records that such shifts have no door today until sitting item 1 is initialled. THE PRACTICAL ANSWER: land cars 1–3 dark-safe under the grant (registration + composers that nothing files until the desk routes), and hold car 4 — goldens for BOTH non-omniscient infoModes captured FIRST, the one-time shift recorded by name — at the door. Cars 1–3 unblock ES-7 without paying the gate.","outputMoving":"**YES at landing, for car 4 (the refile) — the only IN wave of which this is true.** NO for cars 1–3: minting a section token and authoring composers moves nothing until a kind routes to the new desk, and `informationNews.js`'s registered-without-routing posture is the existing precedent for exactly that.","workerKb":"0 KB. `heraldRouting.js`, `informationNews.js`, `informationStatecraft.js` and `components/map/heraldFeed.js` are all outside the generation worker's graph. No lazy chunk needed."},{"wave":"IN-6 — THE MEASURE (the acceptance harness; no flag)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"`subsystemRowsCoin.js:58` carries a forward reference only. No flag, no engine state — four instruments, and three of them have half their substrate already in the tree, which is what holds this to 3–4 cars. (1) THE EARNED-CLASSIFICATION WALKER: `scripts/audit/behavioral-observation.mjs` is the named edit site — `FAMILY_TOKENS` at :25, the family match at :167-171, and the file's own text at :559-561 records why the bare `news` token must come out ('knowledge: 28 over thirty years proves nothing about the belief lane at all'), restated four times in `subsystemRowsWaves.js`. (2) THE DECONTAMINATION RATCHET re-measures its baselines AT BUILD and must never quote the volume — `knowledgeLaneEvidence.js:62` still holds ONE dispositive literal (`KNOWLEDGE_LANE_EVENT_TYPES = ['belief_misjudgment']`), and the routed census is 381 now, not 269 or 354. (3) THE LIT/DARK DIFFERENTIAL HARNESS the certification row asks for BY NAME: `subsystemRowsRegen.js:60` IS the spec ('What would close the gap is a PAIRED run, not a richer single receipt: two soaks on one seed, one with the flag lit and one dark, compared over the new per-year beliefDivergence series... Half of that instrument now exists; the differential harness does not'), with :85 reading `soakEvidence: 'unobserved'`. Both halves of the reuse exist: `BELIEF_DIVERGENCE_RECEIPT_PATH = 'behavioral.yearly[].beliefDivergence'` (`knowledgeLaneEvidence.js:161`) is already in the soak receipt, and `scripts/dormancy-bit-compare.mjs` is the COMMITTED cross-build same-seed comparator whose two-invocations-one-diff ritual this harness copies (its own header argues exactly why such an instrument must be a repo file and not a scratchpad script). (4) THE DISPLAY-SAFE BANDED TRUTH PROVIDER completes `BeliefDivergenceBand.jsx`'s (138 lines) documented v1 follow-up. Plus: ALL the envelopes of the volume's CORRECTED list, each cross-checked mechanically against every §4 Endings entry so no promised envelope is unauthored, each with a tuning-constant mutant negative that REDS it; lit/dark pair byte-identical at tick 0; and certification rows for all four flags naming their dispositive literals (25–40 effective lines each, and `subsystemRowsBelief.js` at 252/800 has room). Cars: 1 the earned-classification walker + the ratchet at re-measured baselines · 2 the paired-soak differential harness, flipping `subsystemRowsRegen`'s row off `unobserved` · 3 the banded truth provider + BeliefDivergenceBand's v1 join · 4 the envelope cross-check with its per-envelope tuning mutants and the four flags' rows. ONE OPERATIONAL HAZARD the harness must design around: `gate-mutex.sh` exits 0 after 40 polls and a soak under box load can read ZERO tests, so the harness must print its own case count or it is a false green by construction.","dependsOn":"ALL FOUR IN FLAGS must exist first — `secondOrderBeliefEnabled` (landed), `infoLureEnabled` (IN-2), `counterIntelEnabled` (IN-3), `reputationRaceEnabled` (IN-4) — because car 4's certification rows name each flag's dispositive literal. And on IN-5 for the Endings entries IN-5 mints, since the envelope cross-check is mechanical against every §4 Endings row. IN-6 is the family's terminus by construction, not by preference.","ownerGate":"None. Instrumentation; Spine 13 and 14 both declared-empty with reason, recorded. Signed under the standing grant.","outputMoving":"NO. An instrument act — the #20 precedent ('no OSR movement'). The earned-classification walker changes what the behavioural audit CLASSIFIES, not what the engine produces; the paired soak reads two existing arms and writes nothing into a world.","workerKb":"0 KB. `scripts/audit/behavioral-observation.mjs`, `scripts/dormancy-bit-compare.mjs`, `knowledgeLaneEvidence.js`, `subsystemRowsRegen.js` and `BeliefDivergenceBand.jsx` are all outside the generation worker's graph — two of them are node scripts that never enter a bundle at all. No lazy chunk needed."}],"firstCar":"IN-4 CAR 1 — THE INVISIBLE KEY IS CURED, AND THE MANIFEST STOPS LYING ABOUT A MEASURED GATE. Land J-INA-4 alone, before any IN flag is minted: join `intelTradeEnabled` to `ENGINE_GATED_VIRTUAL_RULE_KEYS` in `src/domain/worldPulse/simulationRules.js` (24 keys today, alphabetical, between `infoStatecraftEnabled` and `migrationRumorsEnabled`) with its authored certification row in the SAME commit, and assert the exactly-one-key delta in `tests/lint/engineGatedRuleKeys.walker.test.js`. It qualifies on the walker's own membership test today — three real `=== true` by-name gate reads already exist at `src/domain/spatial/intelActs.js:98`, `src/domain/worldPulse/generosityKernel.js:1009-1015` and `src/domain/worldPulse/informationStatecraft.js:1364` — so this adds no behaviour whatsoever; it makes a fully wired flag visible to the census that is supposed to see it, which is exactly the defect `simulationRules.js` warns about in its own text and which the file's own history records shipping once before. Choose this as the family's first car for four reasons that are all measured rather than argued. First, it is the only IN work that is unblocked TODAY: IN-1c-b waits on the §55 Q3+Q5 chair sitting, IN-5 waits on ES-6b, IN-6 waits on all four flags, and IN-2/IN-3 are multi-car waves whose shape changed when the statecraft head turned out to be at 781 of 800 effective lines. Second, it is a SEQUENCING PREREQUISITE, not merely a nice cleanup: §2's flag law makes the manifest walker assert exactly a one-key delta per commit, so `reputationRaceEnabled` and the `intelTradeEnabled` cure physically cannot share a commit — landing the cure first is what clears the path for IN-4's flag mint rather than forcing a contrived two-key ruling. Third, it is the smallest possible car with a real proof: a certification row costs 25–40 effective lines in a leaf with 548 lines of headroom (`subsystemRowsBelief.js` at 252 of 800), and the proof is the walker's delta assertion plus the existing five dormancy fences re-run — no new mechanism, no new state, no new prose. Fourth, it moves no same-seed output and adds 0 KB to the generation worker (whose 153-module graph contains no `domain/worldPulse` file at all, so the 1,404,493 B zero-headroom ceiling is simply not this family's problem and the car can say so with a receipt). The one thing this car must DECLARE rather than let ride: making the key censusable moves the certification receipt's key partition — a receipt-surface movement that is the entire point of the cure, and the row must name it so a later reader does not rediscover it as drift. It needs no owner: a manifest join is a chair ruling with a reason, vetoable, and the volume's own Q3 already carries both the recommendation and the rejected alternative."}}
{"type":"started","key":"v2:f631fa2cee5df5fcbb43e3814dcca35eb31639815a7166ddf059fd8b6d12f9e1","agentId":"aeee29db4ed7352ac","label":"price:LG0","phase":"Price"}
{"type":"result","key":"v2:1c43dd0ff944e4a57a083faabeaf74465ee2cada79912f72641ee65e36407841","agentId":"a193b042571c32a0c","result":{"family":"EP","summary":"EP-0 through EP-3 are BUILT and the first pass's \"code wins\" verdict holds for BOTH of EP-3's slices, which the first pass's evidence row did not establish: slice B is landed too (`src/domain/advanceEpochLedger.js:213 yearStreamSeedOf`, the `byYear` first-wins writer at :111-121, the nine family-2 re-roots live at `mapDress.js:175`, `generosityKernel.js:1033`, `pulseKernel.js:497`, `npcLadderKernel.js:589`, `roadsKernel.js:382`, `traditionsKernel.js:263/732`, `politics.js:328`, `relations.js:171`, and `tests/lint/advanceEpochSingleWriter.walker.test.js:106` re-records the kernel import line \"2026-08-16 BY EP-3 SLICE B\"). Everything from EP-4 on is UNBUILT, verified by symbol at the slot a5876c0ea, not inferred: `configSlice.js:86` still carries the literal `seed` in CONFIG_PATCH_EXTRA_KEYS and `generateSettlementPipeline.js:114` still throws on it (H1 is a LIVE product break today); `InstantWorldEntry.jsx:38/67` and `ConstructionPanel.jsx:30` still hand-roll `Math.random` seeds; `WorldMap.jsx:626` still calls `bridge.resetMap()` with zero arguments; no `SEED_DOOR_DEBT`, `ARGUED_EXCLUSION`, `regenAffordances.walker.test.js` or `scripts/regen-affordance-manifest.json` exists anywhere in the tree; `VISION_IDEALIZED_FINAL_PRODUCT.md:135`, `roadmapLedger.js:45` and `AutoplacementConsent.jsx:234` still carry the un-amended promise; and `realmVerbExecution.js:824` carries the epoch but no tick term, with J-EP-10's in-file note at :818-823 saying in the source that EP never adds one. Eight waves are priced: EP-4's three slices, EP-5, and all four §7a parked rows. Total 11-17 cars. THE WHOLE FAMILY IS FREE OF THE GENERATION WORKER'S BYTE CEILING EXCEPT ONE ROW: the worker graph is `generation.worker.js:17` -> `generationRequest.js:36` -> `generateSettlementPipeline.js` + `stepMetadata.js`, and only §7a row 3 (H5) touches a file on it, against a ceiling of 1,404,493 B that is MONOTONE-DOWN and whose last rise (251 B, §917) needed a chair ruling with a measured build. Three cross-cutting facts the schema rows repeat but that are easier to see once: (1) only two waves in the family are output-moving at landing and both are deliberate — EP-4 slice 1 (a break repair, unflagged by chair ruling P5) and §7a row 4 (a declared same-seed shift in BOTH flag states, which is J-EP-10's whole reason for parking it); (2) EP-1 already bought the flag, so no later wave owes manifest, certification-lane or gate-read work — `advanceEpochEnabled` is registered at `simulationRules.js:241` and read at `pulseKernel.js:313`, `campaignAdvanceSession.js:328/803`, `clock.js:79` and `subsystemRowsVirtual.js:405`; (3) slice 3's GUARD-THE-GUARD fixture, which the volume assigned to EP-0, was NOT captured when EP-0 landed — it can still be taken from `sampleSettlements.js:138`, but only until §7a row 1 removes the live instance, so slice 3 must precede row 1.","waves":[{"wave":"EP-4 slice 1 — the H1 repair (state.config.seed poisons generation)","status":"UNBUILT","carsLow":1,"carsHigh":1,"reason":"One gated commit. The edit is tiny and fully specified: strip `seed` at `settlementGenerateAction.js:135`'s fullConfig build, remove the one `seed` token from the space-joined `CONFIG_PATCH_EXTRA_KEYS` string at `configSlice.js:86`, and amend `tests/store/updateConfigPatchValidation.test.js` in the SAME commit by REPLACING its two admission assertions (`:103` `expect(store.getState().config.seed).toBe('mossgate-004-user1')` and `:204` `expect(isAllowedConfigKey('seed')).toBe(true)`) with their inverses plus a fork-shape pin that the fork's OTHER keys still land — never by deleting them. No new module, no flag, no persisted shape, and §3e's five-part values-only argument is already written and re-measurable. The car's real weight is the pins, not the edit: `state.config`'s serialized key set before and after, an executed pin that a PRE-EXISTING `config.seed` SURVIVES (proving `updateConfig`'s patch loop cannot delete), and the end-to-end regression through the real store (fork click then wizard generate succeeds). ONE THING THE VOLUME DID NOT MEASURE AND THIS CAR MUST RECONCILE: `tests/generators/configPatchAllowlistWalker.test.js`'s own docblock carries a live 'RECORDED, NOT PRUNED' paragraph naming `seed` among five entries whose pruning 'would change saved-config LOAD behaviour, which is a product decision and not a landing one'. That walker will NOT red (verified — `seed` is written in shorthand form, which its documented blind spot skips, and it asserts census SUBSET-OF allowlist only), but a car that lands the prune without amending that paragraph to cite chair ruling P5 ships a tree whose instrument docblock contradicts its own allowlist.","dependsOn":"Nothing. §5 item 3: 'EP-4 is an INDEPENDENT LANE and may interleave anywhere after EP-0', and EP-0 is landed. Slice 1 in particular is unflagged, so it does not even wait on EP-1. §3e argument 5 proves it stands alone if §7a row 1 is declined, so no ordering constraint against the parked rows either.","ownerGate":"SIGNED under the 14:4x grant. Ruled VALUES-ONLY, NOT A PERSISTED-SHAPE CHANGE by chair ruling P5 (EP volume §3e), with the ruling's two honest residuals recorded rather than argued away: an already-poisoned device keeps a dead `config.seed` in localStorage forever, and the test file's assertions are inverted, not deleted. No migration, no deploy, no push, no deletion, no spend.","outputMoving":"YES AT LANDING, and deliberately unflagged — this is the one repair in the family that is a LIVE PRODUCT BREAK rather than a behaviour change. Today one sample-fork click writes `state.config.seed`, which is persisted, and `generateSettlementPipeline.js:114`'s `if (config && typeof config === 'object' && 'seed' in config)` then THROWS on every subsequent generation on that device until the config is cleared. The only user-visible difference after the car is that generation stops throwing. The ledger row must say this out loud rather than let it read as a dark landing.","workerKb":"0 KB. Both touched product files (`src/store/configSlice.js`, `src/store/settlementGenerateAction.js`) are store modules and neither is on the generation worker's graph, which runs `generation.worker.js:17` -> `generationRequest.js:36` -> `generateSettlementPipeline.js` + `stepMetadata.js`. No lazy chunk owed, and the 1,404,493 B monotone-down ceiling is untouched.","reuse":"`configSlice.js` is 116 approx-effective lines against an 800 ceiling with no baseline row — ample headroom. `tests/store/updateConfigPatchValidation.test.js` (223 lines) already exists and already exercises the exact fork shape at `:87-103`, so the inverses land in a suite that already drives the path. `isAllowedConfigKey` is already exported for the walker, so the predicate is already test-reachable. The `{ ok: true, ignoredKeys: [...] }` dev-report return shape the volume specifies is the store's existing idiom."},{"wave":"EP-4 slice 2 — the flag-gated user-visible repairs (A3 instant-world replay + H7 world-map regenerate)","status":"UNBUILT","carsLow":1,"carsHigh":2,"reason":"One car if A3 and H7 ride one commit — both are two-line edits behind the same flag with a dark pin and a lit pin each: A3 makes `instantWorldBody.js:116`'s `options.seed || generateSeed()` actually mint by not always passing the mount-time seed from `InstantWorldEntry.jsx:67`'s `useState(freshSeed)`, and H7 hands `bridge.resetMap()` at `WorldMap.jsx:626` a recorded `String(seed)`, with A12's owed recorded clause landing beside it. TWO THINGS PUSH IT TO TWO CARS, and both are measurable before the first line is written. (a) `WorldMap.jsx` is 605 approx-effective against the 600 component layer ceiling with NO baseline row of its own, so it is at or within a few lines of its ceiling; the enforcer must be run on it BEFORE the edit and a pure-leaf extraction car lands first if it is over. (b) The A3 half's flag read has no measured precedent: every landed read of `advanceEpochEnabled` is engine- or store-side off a campaign's `worldState.simulationRules`, and an instant world is PRE-campaign, so where the component layer reads the gate for A3 is a genuine open seam the car must settle first. H7's half does have one — `WorldMap.jsx:822` already reaches `activeCampaign?.worldState?.simulationRules?.presetId`, and `LivingWorldGates.jsx:42`, `AdminSimTuningPanel.jsx:40` and `SimulationRulesDialog.jsx:6` already import `normalizeSimulationRules` into components. Whatever spelling is chosen must match §2.2's census-visible gate spelling or the entropy-root census walker's classification will not see it.","dependsOn":"EP-1 (LANDED). §5 item 3, chair ruling R7: slice 2 'may not LIGHT before EP-1' — the generator half of the living-futures claim rides the same flag as the engine half, so the generator half becomes true at exactly the moment the engine half does and never before. Nothing else: the flag, its manifest join, its certification lane `subsystemRowsEpoch.js` and its first by-name gate read were all bought in EP-1's single commit.","ownerGate":"SIGNED under the 14:4x grant. No owner-gated class is touched: no persisted shape (§4's dark-state note, strengthened in revision 3, states that with A11 and H5 extracted NO slice in EP-4 changes a persisted shape at all), no paid surface, no migration, no deploy.","outputMoving":"NO at landing — it is dark behind `advanceEpochEnabled` and that is the point of chair ruling P8. Dark, `bridge.resetMap()` is still called with zero arguments and is byte-identical to today, and the instant-world seed still mints once at mount. Lit, the world map's regenerate yields a recorded fresh draw and two consecutive instant-world Generate clicks compose DIFFERENT realms — which is the flag's own lit-mutant and the car's sharpest pin. H7 in particular CANNOT be argued byte-identical and the volume refutes rather than declines that route: FMG's internal draw is opaque across the iframe boundary, so no byte-identity argument is executable from this side, which is itself why the flag is the honest instrument.","workerKb":"0 KB to the generation worker. The touched files are two components plus `src/store/instantWorldBody.js`, none on the worker graph. Main-graph weight is roughly 1 KB. If (a) forces a `WorldMap.jsx` leaf extraction, that is a main-graph move with no worker consequence either.","reuse":"The repair precedent for H7 is live and exact: `src/hooks/useInstantWorldMaterialize.js:51` already calls `await bridge.resetMap(String(ms.seed));`, and `src/lib/mapBridge.js:318` already declares `resetMap: (seed) => call('settlementEngine:resetMap', { seed }, { timeout: 30000 })` — the door already accepts the argument nobody passes. `generateSeed()` is the sanctioned mint with five real call sites. The flag needs no new wiring."},{"wave":"EP-4 slice 3 — the two entropy doors (H4) and the standing affordance instrument","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"The door swaps are trivial and the instruments are the wave. The swaps: `InstantWorldEntry.jsx:38 freshSeed()` and `ConstructionPanel.jsx:30 newSeed` both route through `generateSeed()` — pure door swaps, no flag owed, same freshness before and after, and the only two rows in the wave carrying that argument. The instruments are three separately-budgeted, separately-provable pieces: the `ARGUED_EXCLUSION` map of SEVENTEEN `Math.random` sites in `src/components`/`src/store` each with a written reason (13 id mints, 2 non-entropy UI, 1 documented negative in `ShaftWrap.jsx` whose own comment says 'never Math.random' and which is therefore the free positive control that the detector reads comments correctly), the `SEED_DOOR_DEBT` shrink-only baseline frozen at TWO, and the standing `tests/lint/regenAffordances.walker.test.js` over `scripts/regen-affordance-manifest.json` with its 18-row non-empty floor, its five row types, its both-directions fixture controls and `// anchored:` on every absence assertion. Budgets are stated: walker <=250, H4 scan arm <=180. TWO ORDERING FACTS THE PRICE DEPENDS ON. First, the debt baseline must be frozen at TWO and GREEN with the debt COUNTED on day one BEFORE the swaps drop it to zero — so the instrument car lands first and the swap car ratchets it down; a single car would have to freeze a baseline at a number it immediately invalidates. Second, GUARD-THE-GUARD requires A4's `forkSeedFor` call shape captured as a frozen fixture BEFORE the repair that removes the live instance, and the volume assigned that capture to EP-0 — EP-0 LANDED WITHOUT IT (no affordance fixture exists anywhere in the tree). It is still takeable from `src/data/sampleSettlements.js:138`, but only while §7a row 1 has not landed. Three cars if the walker and the exclusion map are separated from the manifest; two if the instrument is one commit.","dependsOn":"EP-0 (LANDED) for the census-walker idiom the exclusion map copies. MUST LAND BEFORE §7a row 1 — that row removes `forkSeedFor`'s live call shape, which is the only member GUARD-THE-GUARD can prove the detector's reach against. Not flag-gated and not dependent on EP-1, slice 1 or slice 2.","ownerGate":"SIGNED under the 14:4x grant. Pure door swaps plus instruments; no persisted shape, no paid surface, no migration.","outputMoving":"NO. The door swaps are freshness-identical in both directions — `Math.random`-derived and `generateSeed()`-derived seeds are both fresh per click, no seed reaches a stored or displayed surface differently, and no flag is owed precisely because there is no behaviour change to gate. The walker, the exclusion map and the manifest are instruments and move nothing.","workerKb":"0 KB. Two component lines change; the walker is a test and `scripts/regen-affordance-manifest.json` is an instrument file on the `scripts/mutation-coverage-manifest.json` precedent, which is explicitly 'an instrument file, not product weight'. Nothing enters the generation worker's graph.","reuse":"The shape is already in the tree and named: `tests/lint/couplingInclusion.walker.test.js` (1,618 lines) is the `ARGUED_UNLAYERED` precedent to copy — a frozen module->reason map, asserted total, every entry required to carry a non-empty reason string, and a map entry whose site no longer exists REDS. 97 `.walker.test.js` files exist as the house idiom. `scripts/mutation-coverage-manifest.json` is the manifest-home precedent. ⭐ ONE REUSE THE VOLUME DOES NOT NAME AND THIS CAR MUST TAKE: `tests/helpers/codeOnlySource.js`'s `codeOnly` strip must run INSIDE both detectors before every regex. `tests/generators/configPatchAllowlistWalker.test.js`'s docblock records the exact class from its own 2026-09-01 cure — a detector making a USE claim must read CODE, because a key cannot be read from inside a comment — and this wave's detectors make precisely that kind of claim over a layer thick with prose about `Math.random`."},{"wave":"EP-5 — the promise amendment, forty-five in-tree homes","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"Forty-five homes across four tiers (tier 1 user-facing copy 7, tier 2 docs/law 15, tier 3 code headers 10, tier 4 tests 13), and the exact new sentences are written verbatim in §8.5 for every load-bearing one — this is transcription under a sweep discipline, not authorship, which is why 45 homes price at two to three cars rather than a car per tier. The natural split: one car for tier 2 plus the two MACHINE-READ homes (`docs/FABLE_VALIDATION_QUEUE.md`'s R-BLD-10 row and `scripts/.size-baseline.json`'s `_r_bld_10_pulsekernel_banked_permanently_2026_08_03` rationale key, which `tests/lint/sizeBaseline.test.js` reads, so that one is a real diff and not a comment); one car for tier 1 copy, which carries the owner checkpoint; one for tiers 3 and 4 — compressible to two if tiers 3 and 4 ride with tier 2. The sweep discipline is the cost, and it is priced from named traps rather than caution: every doc-reading pin must assert its target appears EXACTLY ONCE (the first-match retargeting hole, which this estate's memory records as LIVE with a second instance found at LT36), the sweep runs on the LOOSE anchor `seed` AND (`world`|`town`|`realm`) and never the punctuated phrase because at least eight spellings exist including one with no comma in `tests/domain/routeNetworkDormancy.test.js`, `SETTLEMENT_CAPABILITY_ATLAS` has TWO occurrences that must move together, `GOLDEN_SHIFT_LEDGER.md`'s two entries are amended by APPENDING and never by rewriting the reasoning, and three tier-2 homes plus one tier-1 home are enumerated as UNAFFECTED so a sweep leaves them alone.","dependsOn":"§5 item 3: EP-5 lands LAST, at the terminal-phase gate, beside the marketing pass — late enough that its lit soak rides the terminal soak, early enough that the constitutional amendment is not the last thing anyone touches. It does NOT gate on EP-4. §8.6 discharged the one external blocker: `memory/the-promise-ratified.md` is already amended by the chair and EP-5 carries NO memory obligation — its charter is the forty-five in-tree homes and nothing else, and §5 item 8 is struck.","ownerGate":"SIGNED for 43 of the 45 homes. OWNER-GATED for exactly two, and the gate is a red rather than a question: `src/copy/landing.js`'s `brief.deterministic` and `map.provenance` are TRUE unchanged (a TOWN is a starting world) and are edited only if the owner wants the fuller claim, in which case `tests/copy/landingClaimsParity.test.js` — which binds `landing.brief.deterministic` verbatim — REDS BY DESIGN, and §8.3 says that red IS the owner's review checkpoint and must not be routed around. Separately and standing: the marketing claim itself unlocks only when `advanceEpochEnabled` is LIT IN A SHIPPED BUILD, which is a deploy and a permanent carve-out — not EP-5's to discharge.","outputMoving":"NO code behaviour at all; the charter says so. It moves document and copy text. `src/data/roadmapLedger.js:45`'s title edit is free — MEASURED, `tests/data/roadmapLedger.test.js` validates shape and does not pin the title. The two shipped copy strings §8.3 names (`ForgeExactDemo.jsx` 'Same seed, same world. No re-roll behind your back.' and `AboutManifesto.jsx`'s no-hidden-re-roll sentence) both remain TRUE under the amended promise and are re-read, never rewritten.","workerKb":"0 KB. No generator code. The only machine-read file touched is `scripts/.size-baseline.json`, an instrument, and the amendment is to a rationale string key, not to any ceiling number.","reuse":"The verbatim replacement sentences are already authored in §8.5 for covenant 1, the four spelled tier-1 homes, `prng.js`'s `fork` docblock and the size-baseline rationale key. The 'point, don't restate' rule means every other tier-2 home takes a POINTER to covenant 1 rather than new prose. `tests/domain/advanceWorkerByteIdentity.test.js`'s 'a seed IS a world, bit-for-bit' is identified as the one phrasing in the estate that stays exactly right and gets no edit — a home the sweep must actively skip."},{"wave":"§7a row 4 — FORCE_RESETTLE's missing tick term (J-EP-10)","status":"UNBUILT","carsLow":1,"carsHigh":1,"reason":"The code edit is one token. `src/domain/worldPulse/realmVerbExecution.js:824` reads ``const seed = `${tickStreamSeedOf(state, { base: String(state.rngSeed ?? 'realm') })}:realm_verb`;`` — the epoch landed with EP-3 slice A and the tick did not, while the file's own two sibling verb seeds at `:681` and `:751` already append `:${nowTick}`, so the target spelling exists ten lines away. THE CAR IS THE SHIFT, NOT THE EDIT, and four obligations make it a full commit. First, the in-file note at `:818-823` currently states in the source, verbatim, that 'EP ADDS THE EPOCH AND NEVER A TICK TERM (J-EP-10)' and that adding a tick 'would be a same-seed shift for every world that has ever resettled, in BOTH flag states, which is a THE PROMISE event this directive does not authorize' — a car that lands the tick without rewriting that block ships a file that contradicts itself and re-arms the next auditor to revert it. Second, a `GOLDEN_SHIFT_LEDGER.md` entry naming the owner signature and the one-time cause, per the standing rule that a legitimate behaviour shift is never re-recorded silently. Third, every golden, fixture and soak corpus that reaches a second FORCE_RESETTLE re-records with the cause stated. Fourth, §7a row 4 is struck with its landing sha and the three row-16 dispositions in the EP volume (§3b.2's rows at lines 1911, 1986 and 2115, each of which says in the record that EP adds no tick here) are amended in the same act.","dependsOn":"EP-3 slice A (LANDED) — the epoch term is already in this key, which is why the remaining work is one token. Nothing else in the family gates it, and it gates nothing.","ownerGate":"OWNER-SIGNED, and the volume asks for the signature by name: 'Add the tick term as its OWN owner-signed change with a recorded golden shift.' The parent's grant signs it and it is in none of the carve-outs (no push, deploy, migration, deletion or spend). But it is a declared same-seed shift under THE PROMISE — the constitutional class — so the chair should RE-STATE the signature in the car's own ledger row rather than inherit it from the grant line, and the shift must be recorded, never allowed to ride.","outputMoving":"⭐ YES, AT LANDING, IN BOTH FLAG STATES — the only wave in the EP family that cannot be dark, and that is precisely why J-EP-10 parked it instead of taking it in passing. MEASURED consequence at HEAD, independent of the epoch program: every FORCE_RESETTLE in a campaign's entire life draws from ONE stream, so the second resettle in a world currently re-reads the first's draws in the same order. Adding the tick changes the outcome of every second-and-later resettle in every existing world, dark and lit alike. There is no flag that makes this dark, because the term enters the key the dark arm composes too.","workerKb":"0 KB to the GENERATION worker — `realmVerbExecution.js` lives under `src/domain/worldPulse/` and rides the advance path, not `generation.worker.js`'s graph. File headroom is fine on its own terms: 608 approx-effective against the 800 domain layer ceiling with no baseline row, and the change is +0 lines (a token appended to an existing template literal).","reuse":"The exact target spelling is already in the same file twice (`:681`, `:751`), so there is no design question about how the tick enters the key — only whether it may. `tickStreamSeedOf` is already imported and already supplying the epoch at this site. `GOLDEN_SHIFT_LEDGER.md` is the established home for a declared one-time shift and already carries entries in the amend-by-appending idiom."},{"wave":"§7a row 1 — A4/A5 the sample-fork living door (paid/founder surface)","status":"UNBUILT","carsLow":1,"carsHigh":2,"reason":"MEASURED unchanged at the slot: `src/data/sampleSettlements.js:138 forkSeedFor(sample, userId)` still returns ``\\`${sample.config.seed}-${suffix}\\``, a pure function of (card, user), so the same user forking the same card gets the same town forever. Two consumers, both live and both tier-reading: `generate/FoundingWorlds.jsx:53` (reads `s.auth.tier`; the fork is tier-gated above town) and `SettlementsPanel.jsx:136` (whose own comment says a tier-gated sample 'opens the purchase modal'). The recommended disposition is LIVING DOOR — the fork button mints fresh, and the sample's own seed stays typeable in the `SeedField`, which is the address the directive preserves. One car for the mint plus its pins if the chair takes the flag route; two if the car must also carry a declared-shift ledger entry and a re-record of any fork-reaching golden, which is the unflagged route's cost. The design question the car settles first is exactly that: §4's governing rule that every user-visible behaviour change rides `advanceEpochEnabled` was written for EP-4 and this row LEFT EP-4, so as its own wave it must either ride the flag (dark at landing, symmetric with slice 2, recommended) or land as a declared shift.","dependsOn":"EP-4 slice 3 MUST LAND FIRST — GUARD-THE-GUARD requires A4's `forkSeedFor` call shape captured as a frozen fixture BEFORE the repair removes the live instance, so the affordance walker's reach survives its own subject. If the flag route is taken, EP-1 (LANDED) for the gate. No dependency in the other direction: §3e argument 5 proves H1's repair stands on its own if this row is declined, which is exactly what §7a row 1's own warning asks to be confirmed.","ownerGate":"PAID / FOUNDER SURFACE — an owner-gated class on the canonical list. The volume's §7a authority reconciliation records rows 1-3 as SIGNED by the 2026-08-06 blanket sign-off, with the sign-off winning on AUTHORITY and the volume winning on SEQUENCING ('signed rows build as their OWN waves in owner-queue order, never as EP-4 slices'). Signed, therefore — but changing what a paid fork click yields is the class the owner reserved, so the chair re-confirms in the car's row rather than inheriting, and Q5's recommendation travels with the row verbatim.","outputMoving":"YES at landing on the unflagged route — a fork click yields a different town than it does today. NO at landing on the recommended flagged route: dark, `forkSeedFor` composes exactly as today and the existing docstring's justification ('so two users forking the same sample get mechanically-different towns', which is about different USERS and never a second click) still describes the dark behaviour truthfully.","workerKb":"0 KB. `src/data/sampleSettlements.js` (76 approx-effective) and the two components are main-graph; none is on the generation worker's graph.","reuse":"`generateSeed()` is the sanctioned mint and both call sites already route the seed to generation through `generate(seed)`'s `seedOverride` door rather than through config — MEASURED at `FoundingWorlds.jsx`, which does `updateConfig({ ...normalizeConfig(sample.config), seed, _forkedFromSample: sample.id }); await generate(seed);`. The `SeedField` address door in `LayeredConfigurationPanel.jsx` already exists and needs no work — it is what makes the living-door ruling safe."},{"wave":"§7a row 2 — A11 the town-map layout reroll (mint a seed, do not widen the counter)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"MEASURED unchanged: the reroll is still a counter, and `doReset` still zeroes it, so reset-then-reroll replays variant 1 forever. The counter is read at four addresses — `townLayoutV2.js:249` and `:395`, `townMapModel.js:358` and `:619`, `spatialSubstrateDerive.js:51` — and normalized into the persisted blob by `normalizeMapEdits` in `mapEdits.js`, whose own docblock exists 'for a stable stringify … byte-identity'. That last fact is the whole price: this is a PERSISTED SHAPE change in a normalizer that a byte-identity claim is anchored on, which is why the volume calls it 'not a passing repair'. Two to three cars: one to design and prove the shape (a `mapEdits` KEY-ORDER byte-identity proof, and the migration story for existing `layoutVariant` values — the design question being whether an old counter value maps to a seed that reproduces the same layout or whether the car accepts a one-time shift for worlds that have rerolled), one to move the four readers and the normalizer with `townMapModel.js:23`'s documented invariant ('Absent mapEdits / layoutVariant 0 ⇒ the base fork key ⇒ byte-identical to no edits') as the pin surface, and a third if the migration is written as its own commit.","dependsOn":"Nothing in the EP family. It is independent of EP-1..EP-4 and of the other parked rows. Its only real dependency is its own migration design, which must be settled before the reader moves.","ownerGate":"PERSISTED SHAPE — owner-gated class, recorded as SIGNED by the 2026-08-06 blanket sign-off per §7a's authority reconciliation, and building as its own wave in owner-queue order. ⛔ THE CARVE-OUT BINDS INSIDE THE SIGNATURE: the migration may be WRITTEN and must NOT be APPLIED, on the §929 precedent (migration 203 written, never applied). A car that applies it has crossed a standing exception the grant does not lift.","outputMoving":"YES at landing for any existing world carrying `layoutVariant > 0`, UNLESS the migration maps the old counter to a seed that reproduces the same layout — which is the design question above and the reason the shape car comes first. For a world that has never rerolled (`layoutVariant` absent or 0) the base fork key is documented byte-identical and nothing moves. If the chair wants a dark landing, the seed field can ride `advanceEpochEnabled` the way EP-4 slice 2's rows do, but the volume does not require it since this row left EP-4's flag rule with the extraction.","workerKb":"0 KB to the generation worker. `src/domain/townMap/` rides the townScene worker and the main graph, not `generation.worker.js`. `mapEdits.js` is 362 approx-effective against the 800 domain ceiling with no baseline row — ample headroom for a seed field and its normalizer branch.","reuse":"A seed field 'is the shape the rest of the estate already uses' — `generateSeed()`, the `_regenSeed` root-record idiom and `_seed` rider convention are all in place, so the shape needs inventing nowhere. `townMapModel.js:356-359`'s existing comment ('layoutVariant salts the fork') documents exactly where the salt enters, so the reader move is mechanical once the shape is ruled."},{"wave":"§7a row 3 — H5 `_regenSeed` holds only the LAST reroll (widen to a per-action list)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"MEASURED unchanged at the slot: `src/generators/generateSettlementPipeline.js:638`'s own docstring still reads 'ROOT `_regenSeed` HOLDS THE LAST SECTION REROLL, whichever section it was', and the scalar is still written at `:595` and returned at `:695`, so the second reroll in a chain overwrites the first and an earlier reroll is unreplayable. Widening it to a per-action list at the ROOT is a persisted-shape change on a DM-SHARE BLOB whose gallery strip is a top-level KEY list — migration 121 plus `publicSafe.js` — and `src/domain/townScene/manifestContract.js:91` carries `_regenSeed` as a manifest key that moves with it. The volume's own instruction is that the gallery key-list impact is measured FIRST, which makes the measurement its own car: one car to measure the share/gallery impact and write (not apply) the migration, one to move the shape with its round-trip and replay pins, and a third if the manifest-contract row and the public-safe projection warrant separating. This is the largest of the four parked rows and the only one that reaches the generation core.","dependsOn":"Nothing in the EP family — it is chair-gated since revision 1 and was never an EP-4 slice. It is independent of EP-1..EP-4 and of the other three parked rows.","ownerGate":"PERSISTED SHAPE ON A DM-SHARE BLOB — owner-gated class, recorded as SIGNED by the 2026-08-06 blanket sign-off per §7a's authority reconciliation. ⛔ TWO CARVE-OUTS BIND INSIDE THE SIGNATURE: migration 121's successor is WRITTEN, NEVER APPLIED (§929 precedent), and the share/gallery surface is public-facing, so any change to what `publicSafe.js` projects is re-confirmed in the car's row rather than inherited.","outputMoving":"NO draw changes — no seed composition moves, so no world generates differently, dark or lit. What moves is the persisted SHAPE and, with it, a capability: an earlier reroll in a chain becomes replayable, which is the point of the row. The DM-share blob's serialized key list changes, which is the surface the measurement car exists to bound before anything is written.","workerKb":"⭐⭐ THE ONE ROW IN THE FAMILY THAT PAYS THE WORKER, and it must be priced from a measured build, not from this estimate. `src/generators/generateSettlementPipeline.js` IS on the generation worker's graph (`generation.worker.js:17` -> `generationRequest.js:36` imports it directly), so a scalar-to-list change plus the accumulate logic and the widened docstring puts roughly +0.3 to +0.8 KB of source into a bundle whose ceiling is 1,404,493 B and MONOTONE-DOWN, where the last rise — 251 B at §917, for lighting the living-content dial — needed a chair ruling with a receipt from a real build. ⛔ IT CANNOT LIVE IN A LAZY CHUNK: the regen path is the worker's own hot path, and the lazy-edge table's own words are that 'every row is one more cold fetch sitting inside the user's generation wait', with adding a row called a byte decision and not a formality. So the car must either find the bytes back elsewhere in the worker graph or land a declared, reasoned ceiling re-mint measured from a real build with the log cited — and HORIZON-B2's precedent is that refusing to write the arm without a build was RIGHT. Budget a build into this wave.","reuse":"The root-record idiom is already established and already documented at `:464` and `:631-636` ('Replay a persisted reroll with `{ seed: settlement._regenSeed }`'), so the replay contract needs no invention — only widening. `manifestContract.js` already carries the key, so the manifest surface is one row rather than a new registration. `publicSafe.js` and migration 121 are the two named homes, both existing."}],"firstCar":"The first car is EP-4 SLICE 1, THE H1 REPAIR, and it is first because it is the only wave in the family that fixes something broken in the product today: one sample-fork click writes `state.config.seed`, `state.config` is persisted through `partialize`, and `src/generators/generateSettlementPipeline.js:114`'s guard `if (config && typeof config === 'object' && 'seed' in config)` then throws on every subsequent generation on that device until the config is cleared. The implementer makes three edits in one commit. (1) In `src/store/settlementGenerateAction.js`, strip `seed` from the `fullConfig` build at `:135` so the key can never reach the pipeline's first argument. (2) In `src/store/configSlice.js`, remove the single `seed` token from the space-joined `CONFIG_PATCH_EXTRA_KEYS` string at `:86` (the token sits between `routeRequired` and `specialResources`); `isAllowedConfigKey` at `:100` then drops the key on write with the store's existing dev report and `{ ok: true, ignoredKeys: ['seed'] }` return. (3) In `tests/store/updateConfigPatchValidation.test.js`, REPLACE — never delete — the two assertions that pin the admission today: `:204`'s `expect(isAllowedConfigKey('seed')).toBe(true)` becomes its inverse, and `:103`'s `expect(store.getState().config.seed).toBe('mossgate-004-user1')` becomes an assertion that the key is absent, joined by a new fork-shape pin that the fork's OTHER keys (`_forkedFromSample` and the normalized sample config) still land, because a deleted assertion is a silently weakened pin. The proofs the car must execute and quote are four: `state.config`'s serialized key set before and after with no other key moved; an executed pin that a PRE-EXISTING `config.seed` SURVIVES the change, which is what proves `updateConfig`'s patch loop cannot delete and is the load-bearing half of §3e argument 2; the end-to-end regression through the real store (a fork click then a wizard generate succeeds rather than throws); and a negative control that the guard at `generateSettlementPipeline.js:114` still throws when a `seed` key is planted directly, so the repair is proved to have removed the poison rather than the detector. Two things the implementer must not skip. First, reconcile the live contradiction this edit creates in `tests/generators/configPatchAllowlistWalker.test.js`: its 'RECORDED, NOT PRUNED' docblock paragraph names `seed` among five entries whose pruning 'would change saved-config LOAD behaviour, which is a product decision and not a landing one'. That walker will not red — verified, because `seed` is written in shorthand form (`updateConfig({ ...cfg, seed, ... })`), which the walker's documented blind spot skips, and it asserts census SUBSET-OF allowlist only, never the reverse — but the paragraph must be amended in the same commit to cite chair ruling P5 and record that the product decision has now been made, or the tree ships an instrument that argues against its own allowlist. Second, the ledger row must state plainly that this landing is OUTPUT-MOVING and unflagged by ruling, with the user-visible difference named exactly ('generation stops throwing after a sample fork') and P5's two honest residuals carried forward: an already-poisoned device keeps a dead `config.seed` in localStorage forever, and the test assertions were inverted rather than removed. The car adds zero bytes to the generation worker, needs no flag, owes no migration, and depends on nothing — EP-0 is landed and slice 1 does not even wait on EP-1."}}
{"type":"result","key":"v2:769848521bee58048a769df9cf2ad6395dae5305a3217b459ef42cd78f39326a","agentId":"a7f3c891f5d9a1bf5","result":{"family":"WC — war circulation (DESIGN_FP_ARCH_WC.md, 431 KB, 17 waves in four arcs)","summary":"WC-0 is PART-BUILT and effectively COMPLETE on the slot — it landed as five implementer cars (WC-0A `511d2ceb7` warStance+peopleLedger · WC-0B `4c87bae45` the column-class union and the three-way release fork · WC-0C `cf7e64660` lawBandModulation + its singular-exporter walker · WC-0D `e47d67631` amnesty/jubilee + the TERM_FAMILIES census re-derived · WC-0E `9602952ba` both flags, the ledger shape, the blocks[] arm) plus a sixth fence car from another lane (`05b43ec7b` LGT-P13-FENCES step 2). All four modules are dark by charter (zero importers). WC-1..WC-16 are UNBUILT: every named writer is absent from the slot (contributionReads, callInErrand, joinPreference, contributionDispatch, relayNetwork, warBlockRoster, blockForks, serviceIntegrals, enduranceEnvelope, freeUnits, freeUnitKernel, brigandContest, moverAbsorption, residentCohorts, homecomingEffects, veteranReads, diasporaBonds, demobilizationPulse, contributionMoves, armyTransitEnvoy — all ABSENT), and `warContributions` appears in src/ only in comments. THE IN-FAMILY PRICING ANCHOR IS MEASURED, not guessed: WC-0 — the cheapest wave in the volume (no writer, total dormancy, \"TUNING: none\" proven by a no-numeric-literal scan) — cost 5 packets for ~258 effective production lines (warStance 50 · peopleLedger 130 · lawBandModulation 33 · contributionLedger 45) against ~2,184 lines of pins and walkers. That is ~52 eff of production per car, an 8:1 test-to-production ratio, under PACKET_STANDARD's hard budget (≤400 changed eff, ≤2 new leaves ≤250 eff each, ≤1 flag, ≤1 new persisted record family, ≤12 handwritten files, ≤8 acceptance cases). Priced at that rate against §2.2's 5,090 eff of genuinely-new leaf code, the remaining family is 85–124 cars — WC alone exceeds the first pass's 100–200-car band for the whole of #32 (PROGRAMME-32-34.md §D row 3, which labels its own figure PLAUSIBLE). ⛔ THE WORKER BUNDLE IS NOT WC's PROBLEM, MEASURED: a BFS of src/workers/generation.worker.js reaches 153 modules and ZERO src/domain/worldPulse modules; migration.js, peaceTermsCatalog.js, defenseLedger.js, supplyShipments.js, entrepots.js, migrationKernel.js, pressureModel.js, stressorsCore.js, traditions/relations.js and mercenaryMarket.js are all absent from that graph. No WC wave adds a byte to WORKER_BUNDLE_CEILING_BYTES = 1,404,493 (tests/build/generationWorkerLazy.test.js:78/:387, monotone-down). WC's ~690 KB of new source (~150 KB bundled) lands in the LAZY pulse/engine chunk; §5.8 binds every leaf off first paint, so the budget WC actually spends is the first-paint static-closure byte budget (tests/build/vendorPdfLazy.test.js) and it spends zero of it as long as no WC leaf acquires an eager edge. Every wave lands DARK behind the two-flag conjunction (warCirculationEnabled root + arc flag, `contributionLedger.js:78`); the two same-seed shifts the volume declares (WC-2's coalitionShares contribution split, CR-WC-7; WC-6's bank-moves-to-arrival) are LIT-ONLY and move nothing at landing. CR-WC-9 — the four-ledger persisted batch — is SIGNED under the 14:4x grant, so no landing gate remains anywhere in the family; the live blockers are a BUILD gate (WY-8a's armySupply.js, ABSENT, hard-gating WC-10/WC-11) and THE PROMISE's standing tuning ratification at soak.","waves":[{"wave":"WC-0 REGISTRATION (the vocabulary wave)","status":"PART-BUILT","carsLow":0,"carsHigh":1,"reason":"Everything the LANDS block names is on the slot and verified: warStance.js (50 eff), peopleLedger.js (130 eff, EVENT_SIGNATURES with the DERIVED block-touching set), lawBandModulation.js (33 eff, no numeric literal), contributionLedger.js shapes + fail-closed normalizer + the two-flag gate (45 eff), MILITARY_COLUMN_CLASSES and the frozen union (migration.js:504/:509) with isMilitaryColumn (:521) and the release guard widened three-way (:641), the amnesty and jubilee TERM_CATALOG rows (peaceTermsCatalog.js:320-321), the normalizeDeployments blocks[] fail-closed arm (worldState.js:163-174), the certification pending rows (subsystemRowsVirtual.js:305/:355), both CQ5 flag trios (simulationRules.js:460/:484), and BOTH single-exporter fences (tests/lint/strategyMoveVocabulary.walker.test.js, tests/lint/lawBandTable.walker.test.js). The only chartered residue is the spatialUsage TRACKED rows and the certification stateKeys, which ODQ §73.3's F-1 deliberately sends to WC-1 with the writer — a TRACKED row for a key nothing writes reds tests/lib/spatialLedgerCoverage.walker.test.js on the day it lands, so this is a deferral recorded in the module header, not a hole. A zeroth docs car could discharge the CR-HB1′-VOL prose micro-act the preamble §P9 lists as owed by no wave (R28 most urgently: WC-5's body text still asserts errandMint.js is absent, and errandMint.js is BUILT at src/domain/worldPulse/errandMint.js) plus the C1 INBOUND arbitration row in FABLE_VALIDATION_QUEUE.md; that is one ledger-docs car, not engine work.","dependsOn":"nothing (7.F: WC-0 <- nothing). Landed as the wc-0 and wc-0e trains; its residue rides WC-1.","ownerGate":"none. Both flags landed under the CQ5 trio law with no owner signature owed; the volume's own TUNING: none is a measurement (the no-numeric-literal scan), so no soak ratification is pending either.","outputMoving":"NO at landing — the seeded world was byte-identical, proven by the four-fence dormancy pin. It DID move declared ratchet and tripwire surfaces: TERM_FAMILIES grew by two members and sovereigntyBundleWr10's PIN 1b was consumed as the instruction its own docstring says it is. That motion is bills, not output.","workerKb":"0 KB to the generation worker (measured: not in its 153-module graph). ~34 KB of source landed in the lazy pulse chunk (7,011 + 15,042 + 6,028 + 6,770 B), ~8 KB bundled; no first-paint edge.","reuse":"Already consumed: lawWord.js's LAW_WORDS (the recorded narrowing of §8.1 row 2's zero-import claim), peaceTermsCatalog's TERM_CATALOG and its DERIVED TERM_FAMILIES, migration.js's existing class-stamp and class-in-the-key predicates, worldState.js's normalizeDeployments, subsystemRowsVirtual's virtual-row shape."},{"wave":"WC-1 THE ARRIVAL LEDGER","status":"UNBUILT","carsLow":6,"carsHigh":9,"reason":"The densest ARC A wave and the family's first writer. It must turn contributionLedger.js from a 45-eff shape into the ~300-eff SINGLE WRITER of spatialLedgers.warContributions (~255 new eff) and add contributionReads.js (~150 eff) for the one balance function §1.1.4 and WC-5's source-scan pin require. It mints a NEW PERSISTED LEDGER FAMILY (its own packet by the ≤1-per-packet rule) plus a SECOND persisted surface, the edge archive, whose three symbols (appendRelationshipContributionClose, contributionCloseWasRecorded, RELATIONSHIP_CONTRIBUTION_CLOSE_CAP = 24) attach to relationshipState.js's existing three appenders and its recorded-action predicate. Separately budgeted: the war_contribution obligation deposit with decayPerTick: 0 at EVERY mutation site (generosityReactions' obligationKey family + advanceObligationDecay, with the decay-single-owner pin); CONTRIBUTION_DELIVERY_GRADES (the three-member WC-1 grade — the four-member CONTRIBUTION_CLOSE_GRADES is explicitly NOT minted here); Herald arrival and close beats with wc_*-prefixed kinds through heraldRouting + newsVoice; the K7 authored sentence families; DM annotate/forgive-row verbs through the operationRegistry + gen:compendium-data store-action lifecycle; and a display credit chip. Five named hardest-negative pin families each carry executed mutants, and the exact-once close pin is TWO pins because exact-once is two functions here (the predicate call deleted at the caller; the dedupe deleted inside the appender). The spatialUsage TRACKED row and the certification stateKeys WC-0 deferred must ride the first setSpatialLedger commit or the coverage walker reds in the other direction. ⛔ A CORPUS CONSTRAINT BINDS EVERY PIN: war_levy is MEASURED SILENT (subsystemRowsWar.js:55 — zero fires in every one of seven release cases while army_deployed fires 7 and war_conscription 8), so no pin may be authored against levy-produced rows; each fixture is built through this volume's own dispatch road or an explicitly CONSTRUCTED levy fixture asserting non-emptiness first.","dependsOn":"WC-0 (landed). 7.F: WC-1 <- WC-0. Nothing else; buildable today.","ownerGate":"None at landing. CR-WC-9's note says WC-0/WC-1 touch only the new ledgers, which the blanket sign-off's queue coverage reaches — and the batch is in any case SIGNED under the 14:4x grant. TUNING is owner-signature surface at soak under THE PROMISE (CONTRIBUTION_ARRIVALS_CAP 24, four troop bands, four supply bands, the obligation deposit magnitude curve); nothing enters proposedSoakBands.js before ratification.","outputMoving":"NO — dark behind warCirculationEnabled && contributionLedgerEnabled, with the FOUR-FENCE dormancy proof (ledger keys, rng stream, news kinds, display surface fenced separately) and rng draw-count parity dark vs lit. Output moves only at lighting.","workerKb":"0 KB to the generation worker. ~55 KB of new source (~12 KB bundled) in the lazy pulse chunk; the display chip rides the already-lazy World page. No baselined file grows.","reuse":"Large. relationshipState.js's three appenders (:189/:273/:294) and its recorded-action predicate (:266) — the fourth archive is built on the appendRelationshipCoalitionSettlement shape, not invented; generosityReactions.js's obligationKey family and advanceObligationDecay; supplyShipments.js's arrival edge for the supplies road; warHomeCosts.js's accrual site; heraldRouting + newsVoice for the beats; subsystemRowsWar.js and src/lib/spatialUsage.js for the certification and manifest rows; and contributionLedger.js's landed record shapes, normalizer and gate — the file exists, the writer is what is missing."},{"wave":"WC-2 THE DERIVED STANCE","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"Modest new code over a wide consumer surface. warStanceOf goes live as a derivation over deployments and credit (the vocabulary and rank map already landed in warStance.js; the derivation is ~120 new eff, and for non-composite worlds the share integral degenerates to own-army = 1.0, so ARC A needs no roster), stanceScalars attach as multipliers at three EXISTING sites (warHomeCosts accrual, warFactorForCasusRead, coalitionShares at peaceTermsCoalition.js:261), the K5 termsWeightRead lands in contributionReads.js, priced shielding rides peaceTermsAppraisal's existing consumer chain, and the wave adds a stance chip, war-document rows and the crossing beat. The car count is driven by pins, not lines: STANCE TOTALITY keyed per (partyId, warEpisodeKey) with the anchor-keyed mutant that must red on a two-deployment fixture; AT MOST ONE PRINCIPAL PER SIDE (the 0.40/0.35/0.25 three-way-split fixture proving a legal no-principal side, with the at-least-one mutant redding); the COMMENSURABILITY pin whose roster-denominator mutant reds; a DERIVED-NOT-DECLARED grep fence plus a companion scan asserting no consumer calls warStanceOf with an anchor key; and the coalitionShares fork pinned with a DIFFERENTIATED fixture where the strength and contribution orderings INVERT, so a consumer reading the wrong split cannot pass. Tuning is a full cluster: the auxiliary/belligerent share edges, the majority edge, and three scalar curves of five values each.","dependsOn":"WC-1. 7.F: WC-2 <- WC-1. Parallel with WC-3 and WC-5 after WC-1.","ownerGate":"None at landing. The lit-only coalitionShares behavior shift is DECLARED here under CR-WC-7 and any seeded lit corpus re-records ONCE with the cause stated — an owner-facing declaration at lighting, not a landing gate. The eighteen tuning values are owner-signed at soak.","outputMoving":"NO at landing — dark behind the conjunction. The coalitionShares split from strength to contribution is a real same-seed shift but only on a LIT corpus; it must be named in the lighting bill under THE PROMISE, never allowed to ride silently.","workerKb":"0 KB to the generation worker. ~16 KB of new source (~4 KB bundled) in the lazy pulse chunk.","reuse":"warStance.js's landed ladder and WAR_STANCE_RANK (the whole point of WC-0A); the three existing multiplier sites take no new writers; peaceTermsAppraisal's existing consumer chain carries priced shielding with no new road; peaceTermsCoalition.js:261's coalitionShares; the war document and status surfaces already exist (src/domain/display/warStatus.js, warResolve.js)."},{"wave":"WC-3 THE RELAY","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"One ~250-eff leaf (relayNetwork.js) but an unusually heavy conservation and belief surface. It composes staging over the existing candidateRoutes/entrepots/supplyShipments machinery rather than inventing routing, adds per-hop law-band efficiency through lawWordFor and registers the RELAY CURVE ROW into WC-0's empty table, credits skim to staging stores while booking it as shortfall, carries the comradeship term PRESENT AND NEUTRAL (zero bond = today's arithmetic exactly, lighting at WC-9), forks troops from stores (troop columns take the same interdiction exposure but neither stage nor skim), and teaches the relay conservation identity to the shipment rebuild IN THE SAME COMMIT. Two beats, not one: wc_relay_shortfall fires immediately with the typed reason `unknown_hands`, and wc_relay_skim is the NAMING beat that fires only when the naming rumor arrives — the SKIM-ATTRIBUTION LAG pin needs a lagged fixture (skim at T, rumor at T+k) asserting no attribution write in [T, T+k) plus an omniscient-mode twin, and the omniscient-attribution mutant that names the thief at T must red. The per-consignment conservation walker (debited = arrived + consumed + skimmed + destroyed, integer-exact, through at least one in-transit tick — never save/load only) and the PEOPLE-ARE-NOT-STORES pin (two separate mutants: per-hop-loss-on-people and skim-on-people) are each their own car's worth of proof.","dependsOn":"WC-1. 7.F: WC-3 <- WC-1, with the E-row slice gated on the WY encounter TABLE. WY-6 is unbuilt, so the physical-interdiction rows land as a PRE-PINNED SEAM that reds when the table lands without them; the pressure-side shortfall lands now. Feeds WC-9 (`betrayed` needs the skim input) and WC-10.","ownerGate":"None at landing. Tuning at soak: three per-band loss fractions, one skim fraction, the staging search depth.","outputMoving":"NO — dark behind the conjunction; the comradeship term is arithmetically neutral at zero bond so even the lit path is byte-identical to today until WC-9.","workerKb":"0 KB to the generation worker. ~34 KB of new source (~8 KB bundled) in the lazy pulse chunk. supplyShipments.js is 517 lines with headroom; the new logic is a leaf, not a growth of it.","reuse":"Very high: candidateRoutes (distanceRead.js, already used by routeNetworkCharterBypass.js and npcCirculationTransit.js), entrepots.stepEntrepot's existing crossing accountant — relay crossings move the counter that already exists — supplyShipments.pickSource's ranking for the WORSE-OF law band, lawWord.js's lawWordFor, WC-0's registered-but-empty modulation table, and supplyWebWarfare.js for the pressure-vs-physical twin note."},{"wave":"WC-4 STRATEGY AND COERCION","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"Two leaves plus the R6 coercion gradient in full. contributionMoves.js (~150 eff, the CHOOSER: send-reinforcements-to / send-supplies-to / recall-support weighted by relationship, treaty obligation, hopWeeks-derived distance bands, own threat, favor balance, law band and habit) and contributionDispatch.js (~180 eff, the EXECUTOR §1.1.5 — the ONLY writer of the sender's capacity reservation, landing its ASPATIAL arm here and its SPATIAL arm at WC-6). Its call site is warDeployment.js STEP 5c, after applyHomeWarCosts (:1263) and before the tick return, re-exported through the :158-162 block so the 1,338-line head stays thin. THE COERCION GRADIENT IS A SECOND BEHAVIOR FAMILY and cannot share a packet: occupied requisition reads occupation.js's STATE_LADDER (:117), and elective OVERSHOOT writes loyalty standing into all three named payoffs — better terms via the K5 read, conveyance value via the WR-10 market appraisal, and RELEASE, composed into stabilizationSuitability (:386) so a loyal town climbs sooner to `vassalized` (:724), with the REGRESSION and LIBERATION arms staying resistance's and never written. Pin load: the three-executor event pins, the step-5c ordering pin (a contribution chosen at T never credits at T in a spatial world; the before-home-costs mutant reds because the sender's own conscription then draws against reserved capacity), the reservation trio, THE THREE-DELIVERER COERCION PIN (over/neutral/under against one occupier, with the standing-deleted and standing-into-regression mutants), and a DISTRIBUTION distance-weight pin — not single-seed, per the wave-E vacuity lesson.","dependsOn":"WC-2. 7.F: WC-4 <- WC-2. The HB-1 vocabulary arbitration is DISCHARGED in WC's favour by build order: HB-1 already minted src/domain/worldPulse/strategyMoves.js with STRATEGY_MOVES and ALL_MOVE_TOKENS, so WC-4 AMENDS that leaf rather than minting a list — its dispatched-but-unemitted moves join ALL_MOVE_TOKENS and the fence's DISPATCHED_NOT_EMITTED list, never STRATEGY_MOVES, and the leaf takes no import (the paste-ready inbound row is in WC-PREAMBLE.md §P9). WC-6 depends on this wave for the executor's spatial arm.","ownerGate":"None at landing. Tuning at soak: seven move weights, overshoot and under-delivery edges, CONTRIBUTION_RESERVATION_CAP, RESERVATION_HORIZON.","outputMoving":"NO — dark behind the conjunction. The coercion arm composes into an EXISTING input (stabilizationSuitability) rather than adding a writer, so the dark path is unchanged arithmetic.","workerKb":"0 KB to the generation worker. ~45 KB of new source (~10 KB bundled) in the lazy pulse chunk. warDeployment.js takes only the leaf call and the re-export — well inside the 15-eff shared-file delta cap.","reuse":"strategyMoves.js (HB-1's landed leaf and its single-exporter fence — WC-0's structural half already discharged); warDeployment.js's threaded capacityFor and the :158-162 re-export block; occupation.js's STATE_LADDER, stabilizationSuitability, advanceOccupationState and vassalizationOutcomes — every coercion payoff is a composition into machinery that already runs; the WR-10 sovereignty market's existing stages for the debt-to-vassalage road."},{"wave":"WC-5 CALL-INS, EXITS, AND THE DOCTRINE PAGE","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"One ~200-eff leaf (callInErrand.js: the CALL_IN_REQUEST record with its cap, CALL_IN_ANSWERS, the loaded answer fork over six weights, the lapse horizon, call-in RIGHTS as a read, and the OUTCOME axis of the close off readWarTerminations) plus K8's exits as separate behavior families: the forgiveness gesture (typed, extinguishes the obligation, archive row, relationship patch through the one writer), the debt-to-vassalage intent road through the sovereignty market's existing stages, and THE JUBILEE PRODUCER ALONE — amnesty's producer moved to WC-11, where its subject population is first minted, which is why this wave owes an AMNESTY-STAYS-DARK vacuous-term negative on the seeded treaty corpus. The SP-D premise the volume carried is REFUTED and the discharge is cheaper than the text implies: errandMint.js is BUILT on the slot and tests/lint/errandConsumerRegistry.walker.test.js exists, so WC-5 takes its own ERRAND_CONSUMERS row ({ consumer, purposeClass, module, wave: 'WC-5', built: false }) and flips built:true in the SAME COMMIT as the carriage, with the walker redding both directions. ENVOY_PURPOSES stays closed at two (envoyErrandVocabulary.js:119). The mechanism does not wait for the carriage. THE ONE-BALANCE PINS are the wave's hardest: a source scan asserting the answer fork, the breach grade and the K5 terms weight all read contributionReads' single balance function and never reach into warContributions or the edge archive, PLUS the differentiated behavioral pin — forgiving the obligation must move the answer fork AND the terms weight in the SAME tick, and the forgiveness-invisible-to-the-fork mutant is precisely what a second reading of the balance would have shipped.","dependsOn":"WC-1, WC-2. 7.F: WC-5 <- WC-1, WC-2. ⚠ WC-5 IS INSIDE ARC A's CRITICAL PATH — CR-WC-16 re-derived in round 2: WC-9 needs this wave for two of CONTRIBUTION_CLOSE_GRADES' four members (`partial`'s askedBand and `unanswered`'s live call-in), so a queue that defers call-ins as a nice-to-have stalls the entire composite arc. The doctrine reliability page is a pre-pinned seam on HABIT r5's believed-doctrine axes.","ownerGate":"None at landing. Tuning at soak: the breach-grade threshold band, jubilee appraisal weights (raw and unconsumed until drafted), CALL_IN_REQUESTS_CAP 24, CALL_IN_HORIZON_TICKS, and the six answer-fork weights.","outputMoving":"NO — dark behind the conjunction, and the AMNESTY-STAYS-DARK pin proves the treaty corpus does not move: with only the jubilee producer live, amnesty is never drafted anywhere.","workerKb":"0 KB to the generation worker. ~27 KB of new source (~6 KB bundled) in the lazy pulse chunk.","reuse":"errandMint.js and envoyErrandVocabulary.js (SP-D landed — the volume's absence premise is refuted by its own header); the errandConsumerRegistry walker, which already exists to take the row; treatyBreach's existing grading for indebted refusal; readWarTerminations' existing close for the OUTCOME axis; the WR-10 sovereignty market's stages; peaceTermsCatalog's jubilee row, already registered producer-less at WC-0D."},{"wave":"WC-6 BLOCK ROSTERS AND THE CONSERVATION WALKER","status":"UNBUILT","carsLow":8,"carsHigh":12,"reason":"The volume's largest and most structural wave, and the one everything in ARC B waits on. It OPENS with a pure-motion commit that lands no behavior: the armyTransitEnvoy.js extraction (~200 eff MOVED, not added — createArmyCommandPicture, projectArmiesForEnvoyEncounters, applyArmyEnvoyIntent, applyEnvoyInterceptionDecision out of armyTransitKernel.js, which the ceiling ledger measured at 798/800 eff and which is 1,129 raw lines today) with goldens before and after, buying the kernel the headroom the roster projection and arrival edge need. Then warBlockRoster.js at ~320 eff — OVER the 250-eff-per-leaf packet cap, so it splits — carrying block mint/normalize/reconcile, the MANDATORY SELF-ORIGIN BLOCK holding the `conscripted` remainder, blocks[].drawnCohorts banking, the share-integral accumulator and largest-remainder apportionment. Then blocks[] on deployment records and the transit projection field (persisted shape), the SPATIAL arm of contributionDispatch.js over a `reinforcement_column` — the first producer of any military column, the class WC-0B registered — share-integral accumulators at BOTH altitudes from one arithmetic with the anchor→episode map, peopleLedger.js going live, the compositeArmiesEnabled CQ5 trio (which serializes against every other flag wave), and THE CONSERVATION WALKER v1 over nine events with each TAG ARM naming a TAG_SELECTIONS member. The walker alone is a car or two: its red-capability is proven by executing a one-count-drop mutant on EVERY branch INCLUDING the birth source (a walker that can see a stolen death but not an invented birth is a guard with one eye). The reconciliation pin had to be RESTATED because both of its old clauses were unpassable — the live form is total equality on sum(blocks[].headcount) === deployedPopulation plus a per-origin SUBSET law with exact equality only on rows the sweep wrote, and the old equality-everywhere form is recorded as UNPASSABLE so no later wave restores it as a tightening.","dependsOn":"WC-1 and WC-4 (7.F: WC-6 <- WC-1, WC-4 — WC-4 is a real edge, not an ordering coincidence, because this wave takes the executor's spatial arm). Blocks WC-7, WC-8, WC-9, WC-10 and, transitively, all of ARC B/C/D.","ownerGate":"CR-WC-9, the persisted field batch — SIGNED under the 14:4x grant (DESIGN_FP_ARCHITECTURE.md §4:686: \"An unsigned batch stalls WC-6 onward, never WC-0/WC-1\"). The batch is deployment.blocks[] including blocks[].drawnCohorts, the transit blocks projection, shipment relay buckets, the column cohort tag and military classes, FOUR new spatialLedgers (warContributions, freeUnits, residentCohorts, serviceBonds) and one edge archive; CR-WC-9's own recommendation is to sign the full four-ledger list up front rather than let serviceBonds carry a one-row addendum at WC-9. No landing gate remains. Not a push, deploy, migration, deletion or spend.","outputMoving":"NO at landing — dark behind warCirculationEnabled && compositeArmiesEnabled. The BANK-MOVES shift (muster-time bank → arrival-time bank) is the volume's DECLARED LIT-ONLY shift and is pinned as a pair: dark/aspatial byte-identical to today, lit+spatial at arrival, both executed. The sweep's arm of that pair runs on a CONSTRUCTED levy fixture with a non-emptiness assertion first, because war_levy is measured silent.","workerKb":"0 KB to the generation worker. ~43 KB of new source (~10 KB bundled) in the lazy pulse chunk; armyTransitEnvoy.js MOVES bytes rather than adding them, and the display/armyRoster.js leaf (~150 eff) rides the already-lazy World page. No baselined file grows — the decomposition is budgeted BEFORE behavior.","reuse":"seedDeploymentState (warArmyRecord.js) for the conditional blocks[] stamp and normalizeDeployments (worldState.js) for the fail-closed arm — the arm already landed at WC-0E, rejecting, because nothing writes it; ArmyTransitRecord's typedef (armyTransit.js:305) for the projection field; deploymentReturn.js:273/:302's deployedPopulation and `conscripted` arithmetic, which is exactly why the self-origin block had to exist; peopleLedger.js's landed EVENT_SIGNATURES and derived block-touching set — the walker derives its checks from that frozen object rather than holding a copy; migration.js:553's class-in-the-key, already verified, which is what makes the column key-collision pin passable."},{"wave":"WC-7 REALIZED STANCE AND THE CROSSING","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"Little new module code — the time-integrated share feeds the warStanceOf that WC-2 built, and the reads land in existing leaves — but a very high pin-to-line ratio. It adds the crossing edge-detect and mission-creep beat, the dependency-risk forks over own-sheet truth (the outboundImpression zero-import template), the enemy's BELIEVED share as hop-worn composition news entries at crossings, and the ES composition-intelligence tap as a new PRODUCT row (never a new tap level — espionageMath's TAP_LEVELS is closed), gated on the existing espionageEnabled conjunction. THE RUNG-SWAP PIN is the flagship negative of the whole derived ladder: a reinforcer's integrated share crosses 0.50 against a declarer-owner, the two rungs EXCHANGE, and all four scalars re-derive from that one read for BOTH parties in the same tick — the declarer-disjunct mutant that restores \"or is the war's original declarer with a fielded army\" IS the priced-shielding hole the ladder exists to close. THE UPDATE-SET TOTALITY PIN is derived from EVENT_SIGNATURES and asserted with toEqual against a named list, so no later wave can mint a block-moving event that skips the integral and `dispatch` cannot creep in and let an interdicted column BUY STANCE. Plus the event-updated-only integration pin, the crossing-beat exact-once pin, the BELIEF-LAG pin with its omniscient twin, and a proseNumerics grep fence proving no surface renders the raw integral.","dependsOn":"WC-2 and WC-6. 7.F: WC-7 <- WC-2, WC-6. Parallel with WC-8 after WC-6.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant (this wave reads the WC-6 fields rather than minting new ones). Tuning at soak: the crossing margin (the hysteresis dead zone, sharing K2's shape) and the composition-news significance class, which is BORROWED from SIGNIFICANCE_CLASSES and never minted.","outputMoving":"NO — dark behind warCirculationEnabled && compositeArmiesEnabled.","workerKb":"0 KB to the generation worker. ~10 KB of new source (~3 KB bundled) in the lazy pulse chunk — the cheapest wave in ARC B by bytes and the most expensive per byte in proofs.","reuse":"warStanceOf and the scalar sites from WC-2; the rumor/news-arrival records and hop-wearing already built for belief lag; espionageMath's closed TAP_LEVELS (a new product, not a new level); the outboundImpression own-sheet-truth template for the dependency-risk forks; SIGNIFICANCE_CLASSES."},{"wave":"WC-8 THE BLOCK FORKS","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"Two leaves, one of them over the packet cap: blockForks.js at ~300 eff (news-gated fork evaluator, BLOCK_FORK_OUTCOMES, law-band cohesion across whole/fragment/majority, loading factors as typed reasons) and joinPreference.js at ~120 eff, minted HERE because WC-8 is the first consumer and consumed UNCHANGED by WC-11 and by the merge arm — one scorer, three consumers, with a source-scan fence so WC-11 cannot grow a second one. The four outcomes wire to people-ledger events (stay; return = depart + arrive_home; defect = a conserved block transfer; resist = the same return pair plus the resistance feed), and the wave adds occupied-block WATCHED taint as an ES read, occupied-home credit continuation, fork receipts and beats, and registers the COHESION CURVE ROW into WC-0's table. Pin load: the NEWS-GATE pin constructs the in-flight window explicitly (occupation at T, hop-worn arrival at T+k, no fork in [T, T+k), fork at T+k) with an omniscient twin; the COHESION pin requires a lawful block to be single-valued and a chaotic block's fragments to sum exactly by largest-remainder, with a non-member law word NULLing the row upstream rather than mis-grading; the CONSERVED-DEFECTION pin's defection-as-sink mutant must red the WC-6 walker; and THE DEFECT-TARGET PIN — without which the wave is unbuildable — needs a deterministic receiving army drawn from the closed candidate set with reasons pinned by toEqual, and an empty candidate set that re-normalizes the fork rather than silently staying.","dependsOn":"WC-6 only (7.F: WC-8 <- WC-6; WC-7 is NOT required — forks read stance through banked facts). ⚠ WC-8 MUST PRECEDE WC-11, a round-2 correction: the old graph let WC-8 be deferred indefinitely, and WC-11 consumes the joinPreference.js this wave mints.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant. Tuning at soak: six fork loading weights and the cohesion fragmentation curve (the law-band table row).","outputMoving":"NO — dark behind warCirculationEnabled && compositeArmiesEnabled.","workerKb":"0 KB to the generation worker. ~57 KB of new source (~13 KB bundled) in the lazy pulse chunk.","reuse":"The rumor-arrival and army-staleness records for the news gate — a joined read, with no second latency law minted; lawWordFor and the closedValue discipline; occupation.js's advanceResistance as a consumer input with its existing writer untouched; WC-6's blocks[] and the conservation walker, which grades the defection; WC-0's registered modulation table for the cohesion row."},{"wave":"WC-9 THE SERVICE INTEGRALS","status":"UNBUILT","carsLow":7,"carsHigh":10,"reason":"The volume's densest tuning cluster and the wave where four deferred rows all come due. serviceIntegrals.js at ~290 eff (over the per-leaf cap, so it splits) carries blend absorbed/imparted with share-scaled asymmetry, the comradeship pair integrals, the drift accumulator with K2 hysteresis — AND becomes THE ONE WRITER of spatialLedgers.serviceBonds, a FOURTH persisted ledger that had no declared home anywhere in the volume before round 3 despite four consumers reading it MID-WAR, landing with its certification row and its spatialUsage manifest row in the same commit per the FINAL FOLD LAW. Betrayal is priced by intimacy inside the EXISTING rule bodies (resentment scaled by banked comradeship), the war-close fold writes the relationship patch plus the `shared_service` archive SUMMARY row, and K5's termsWeightRead finally gains its comradeship term. Three rows that waited for this wave's inputs land here: CONTRIBUTION_CLOSE_GRADES is MINTED here and not at WC-1 (`betrayed` needs WC-3's skim and this wave's comradeship, `unanswered` and `partial` need WC-5's request record), completing CALL_IN_CLOSE_GRADES' honour axis; the relay comradeship term LIGHTS with RELAY_COMRADE_BONUS; and the 1.6.4 COMRADE FIDELITY RIDER lands as a bounded error reduction capped by COMRADE_FIDELITY_MAX, gated on HABIT r5's axes as a pre-pinned seam with an executed red-capability proof. Pin load is the largest in the volume: the asymmetry pin, hardship-not-victory, hysteresis, betrayal-intimacy ordering, event-updated-only, FOUR separate consumer pins for directive (h) (relay loss, answer weight, alliance odds through warCoalitionDecision.js:108-109 asserted as a real read, and a WY-3 caravan seam row), the comrade-fidelity pin with THREE mutants (term deleted, cap removed, depth raised), the service-bond ledger pins (drop-when-empty, symmetric by key normalization, event-updated-only, and a source scan proving bondOf reads THIS ledger and not the relationship axes), and a four-member grade-totality dead-arm census.","dependsOn":"WC-3, WC-5 AND WC-6 — corrected in round 2 (7.F). The old single WC-6 edge contradicted this wave's own LANDS text. It can no longer run beside WC-5, which is correct, because it consumes WC-5's request record. Feeds WC-2's K5 read back its comradeship term and WC-11's fission cutter its bondOf.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant, and serviceBonds is explicitly inside the batch (CR-WC-9's note: sign the full four-ledger list up front rather than in two pieces, which is the shape a later wave cites without re-reading). Tuning at soak is the heaviest in the volume: blend and comradeship accrual curves, THE LAW-BAND TABLE'S DRIFT CURVE ROW, hysteresis margins, the betrayal intimacy multiplier curve, RELAY_COMRADE_BONUS, COMRADE_FIDELITY_MAX, and the call-in answer's comradeship weight — every constant with its in-file one-liner.","outputMoving":"NO — dark behind warCirculationEnabled && compositeArmiesEnabled. It LIGHTS WC-3's neutral relay term, which is a lit-path change only.","workerKb":"0 KB to the generation worker. ~39 KB of new source (~9 KB bundled) in the lazy pulse chunk.","reuse":"applyRelationshipPatch as the one relationship writer (unchanged — the archive row becomes the war-close SUMMARY of the new ledger, never its storage); relationshipState's existing appenders; warCoalitionDecision.js:59/:108-109's join appetite via pactStrength/trust for the alliance-odds pin; the existing rule bodies for betrayal pricing; WC-5's request record and WC-3's skim accounting for two of the four close grades."},{"wave":"WC-10 ENDURANCE AND CONCENTRATION","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"One ~200-eff leaf (enduranceEnvelope.js: the envelope read over carried supply plus relay throughput against mass-and-season-scaled burn, the carrying-capacity network read, the dueling-envelopes siege read, massing-tell emission) plus the temporal strategist fork, PARTIAL WICK-APART as a stated total selector order (weakest bonds, nearest homes, believed homes-under-threat, longest-served), K4's siege document surface, and the freeCompaniesEnabled CQ5 trio (a flag car that serializes against every other flag wave). Before WC-13 exists, wick-apart routes blocks home whole through the EXISTING return road rather than inventing an exhale. Pins: the envelope monotonicity family (more relay throughput never shortens; more mass never lengthens; seasonal severity never lengthens), the supply-consequence one-clause veto executed as a WRITE-SET assertion (envelope exhaustion drags the army's own accumulatedAttrition and NEVER realm warExhaustion), the massing-tell pin against the rumor ledger at the concentration tick, and the wick-apart selector ordering pin whose shuffle mutant reds.","dependsOn":"WC-3 and WC-6 (7.F). ⛔ HARD BUILD GATE: WY-8a — armySupply.js / F9 supplyCargo — must be BUILT, because an envelope needs carried supply to exist. MEASURED: armySupply.js is ABSENT from the slot and WY's armySupplyEnabled is 0. This is a build dependency on a sibling family, not an owner signature: F9 supplyCargo is owner-SIGNED on the ledger (DESIGN_FP_ARCHITECTURE.md §4:669, \"⏱ SIGNED — released at its queue position\") while the BUILD-SLOT copy of §4 still reads UNSIGNED — a doc catch-up the first pass already docketed. CR-WC-14's recommendation is to HOLD the gate (ARC A plus WC-6..WC-9 deliver value without it); if WY-8a slips badly the chair may re-scope this wave to a relay-throughput-only envelope declared as v1.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant. No further owner gate; the WY-8a dependency is a build gate the chair holds under CR-WC-14. Tuning at soak: the burn-rate curve, mass scaling, and the never-hit renderer guard ceiling (documented as a guard with its not-load-bearing pin).","outputMoving":"NO — dark behind warCirculationEnabled && freeCompaniesEnabled.","workerKb":"0 KB to the generation worker. ~27 KB of new source (~6 KB bundled) in the lazy pulse chunk.","reuse":"WC-3's relay throughput as the envelope's second input; the existing rumor-carrier shape for the massing tell (an additive carrier entry, not a new carrier law); the existing block return road for pre-WC-13 wick-apart; the siege surfaces already in display."},{"wave":"WC-11 FISSION AND FREE UNITS","status":"UNBUILT","carsLow":7,"carsHigh":10,"reason":"Two leaves totalling ~570 eff — freeUnits.js at ~370 (well over the 250-eff per-leaf cap, so at least two packets: FREE_UNIT_STATES, the comradeship-graph fission cutter, unit records and normalizer, drift-weighted join preference, the `merge` reconsolidation, forage escalation) and freeUnitKernel.js at ~200 (the mover adapter and the estate's SECOND counts stepper) — plus a wave OPENER that is its own car: THE COUNTS-MOVER MANIFEST, a SECOND totality manifest with its own discovery signature over armyTransitKernel.js's stepArmyPosition sites (:467/:502/:698/:742) and migrationKernel.js's releaseMigrationArrivals (:336), gaining freeUnitKernel.js BEFORE the stepper lands. It is NOT a widening of law M: MOVEMENT_SITES is a named-person leg-physics census by its own header and signature, and pushing a counts-mover into it would contradict CR-WC-2 — the manifest pins must prove the two censuses stayed DISJOINT (a planted named-person token must NOT enter the new manifest). The wave also carries THE AMNESTY PRODUCER AND ITS EXECUTOR TOGETHER, moved here from WC-5 so producer and subject population arrive in one commit, gated on freeCompaniesEnabled by name in conjunction with the arc flag. Pins: the fission-graph pin reading bondOf off WC-9's service-bond ledger (origin blocks never split — the block-split mutant reds the walker AND the pin); THE MERGE PIN with a differentiated should/should-not fixture, the older unit's key surviving, per-origin headcounts summing exactly at the KEY level while the pool-level identity provably does not move; the war-ended-unknown pin (a free unit beyond news reach keeps operating after termination until the arrival record exists); the join-preference slider pin run against the SAME leaf WC-8 pinned with a second-scorer plant; and the amnesty gate's vacuous-term negative and its positive, both executed.","dependsOn":"WC-8, WC-9 AND WC-10 — corrected in round 2 (7.F): WC-8 mints the one joinPreference.js this wave consumes unchanged and lay on no path here under the old edges. ⛔ Inherits WC-10's HARD BUILD GATE on WY-8a (armySupply.js ABSENT). Blocks WC-12 and WC-13, hence all of ARC C and ARC D.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant (freeUnits is one of the four ledgers in the batch). CR-WC-10 — the second counts-mover manifest — is a CHAIR ask, already RULED in the affirmative and recorded as standing authority in WC-PREAMBLE.md §P9 (\"mint the SECOND counts-mover manifest; do not widen law M\"); coordinate its existence with WY, but no edit to WY's guard is requested. Tuning at soak: fission threshold slack, forage escalation bands, join-preference drift weighting.","outputMoving":"NO — dark behind warCirculationEnabled && freeCompaniesEnabled, and the amnesty gate pin proves the seeded treaty corpus does not move while the flag is dark.","workerKb":"0 KB to the generation worker. ~77 KB of new source (~17 KB bundled) in the lazy pulse chunk — the family's largest single-wave addition.","reuse":"joinPreference.js from WC-8, consumed unchanged (one scorer, three consumers, fenced by source scan); bondOf and the serviceBonds ledger from WC-9; the WC-0 amnesty catalog row, registered producer-less and waiting; armyTransitKernel and migrationKernel's existing steppers, which the new manifest REGISTERS rather than rewrites; the fission cutter is reused for dissolution at war end rather than a second cutter."},{"wave":"WC-12 BRIGANDAGE AND THE EMBATTLED STATE","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"One ~200-eff leaf (brigandContest.js: nearest-settlement targeting by road and warmth, the repel/embattled contest over defenseLedger military and internal reads, the embattled stressor injection, EMBATTLED_RESOLUTIONS, buy-off as a graded close) plus three separate attachment families that cannot share a packet: embattled crime coupling (a new CRIME_ARCHETYPES member, walked, plus the smuggling-to-besiegers loop, with espionage porosity composing through the EXISTING crime term), NETWORK_CARRYING_CAPACITY going live, and suppression demand into the mercenary market. The two-caps distinction is the wave's subtlest obligation: excess world mass shortens envelopes network-wide ONLY by reducing relay throughput, and THE TWO-CAPS COMPOSITION PIN must prove it — raising world mass with an army's own carried supply held constant must SHORTEN that army's envelope, with the network-cap-cuts-armies-directly mutant redding, plus a companion arm proving FISSION_SLACK is grace against the ENVELOPE alone on a fixture where the two orderings disagree. Encounter rows E16 (brigand × settlement) and E17 (column × host) land in the SAME COMMIT as their resolvers if the WY table exists, else as pre-pinned seam rows. ⚠ This wave's E16/E17 claim is the one place the volume moves a sibling's guard: WY §4's \"anti-vacuity at fifteen\" and WY-6's sixteenth-arm mutant go stale the day WC-12 builds, and WY-6's anti-vacuity arm must DERIVE the count from the table rather than restate a literal. Other pins: a dead-arm census proving repel AND embattled are both reachable with band edges derived from the MEASURED defense distribution (the POINTS-GAP lesson, §5.5); the ONE-STRESSOR pin whose parallel-stressor mutant reds; the buy-off-teaches pin; and a worse-of crime-composition source scan forbidding a blended fourth spelling.","dependsOn":"WC-11 (7.F). Inherits the WY-8a build gate transitively through WC-10/WC-11.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant. ⚠ ONE CONDITIONAL CHAIR RETURN: CR-WC-11 recommends embattled be expressed as the existing APPLY_STRESSOR/RESOLVE_STRESSOR pair (an ordinary catalog stressor with a typed source reference); if the build finds otherwise, a third event type is a STORE-CONSUMER change and comes back to the chair BEFORE landing. Tuning at soak: contest thresholds (derived from distribution, then frozen), buy-off price bands, embattled trade-interdiction severity.","outputMoving":"NO — dark behind warCirculationEnabled && freeCompaniesEnabled.","workerKb":"0 KB to the generation worker. ~27 KB of new source (~6 KB bundled) in the lazy pulse chunk.","reuse":"stressorsCore's normalizeStressor and the pestilenceKernel precedent for an injected ordinary stressor; pressureModel's CRIME_ARCHETYPES (one new member) and its existing crime read for espionage porosity; defenseLedger's military and internal reads; mercenaryMarket's supply side for suppression demand; WY's encounter table and resolver shape."},{"wave":"WC-13 HOP-AND-SHED AND THE ONE ABSORPTION MARKET","status":"UNBUILT","carsLow":7,"carsHigh":10,"reason":"Three leaves totalling ~640 eff — moverAbsorption.js ~270 (over the cap; parking search, shed apportionment town by town, the host rejection fork loaded by density/relationship/law/fear, the shared absorption market read, the `reclass` producer and the razed-destination rule), residentCohorts.js ~250 (THE ONE WRITER of the residency TAG ledger, landing HERE and not at WC-15 because the tag credit must ride each shed IN THE SAME EVENT — the earlier plan's census→cohort transfer appears in no event list this volume has, and §0.2's ruling closed it), and homecomingEffects.js ~120 — plus the moverAbsorptionEnabled CQ5 trio and a new persisted ledger family. THE TAG-LEDGER TOTALITY WALKER is a car on its own: three laws, not the single sum an earlier drafting stated, with FOUR executed mutants — a tag written without its census credit (the double-count), a census credit without its tag (the lost cohort), a census DEBIT run without its tag arm (the phantom garrison, which used to be the DESIGN and made WC-16's fork pin unpassable), and a second overlay planted against LAW 3. That walker is what makes WC-15 a read-only wave over people. The `reclass` TAG EVENT (`veteran_return` → `shed_column`) must land as a before/after headcount identity with the reclass-as-drop-and-recreate mutant redding both it and the conservation walker; without it WC-6's key-collision re-run would pass over a class nothing ever stamped. Plus the RAZED-DESTINATION pin (a column whose destination is razed mid-transit reclasses, hops, and sheds at exactly ONE addressable host, with both the credited-to-a-razed-row and the dropped-on-razing mutants redding), the competition pin, the rejection-hunger pin ending in a brigand arrival, and integer shed conservation.","dependsOn":"WC-11 (7.F). Consumes the COLUMN CLASS union WC-0 already minted — it no longer mints it, a round-2 correction that removed a wave-order violation the dependency graph could not express. Blocks WC-14, WC-15, WC-16.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant (residentCohorts is the third of the four ledgers in the batch). CR-WC-20 is standing authority and shapes the design rather than gating it: a cohort is a TAG on census, never a disjoint pool, because the disjoint arm would change the MEANING of settlement.population for every existing consumer. Tuning at soak: density-capacity edges derived from the measured absorption distribution, rejection loading weights, shed quantum bands.","outputMoving":"NO — dark behind warCirculationEnabled && moverAbsorptionEnabled.","workerKb":"0 KB to the generation worker. ~86 KB of new source (~19 KB bundled) in the lazy pulse chunk — the family's heaviest wave by bytes.","reuse":"Very high on the migrant side: demographicsMigration's competeForDestinations and destinationMenuFor take military columns into the EXISTING competition rather than a second market; migration.js's class-in-the-key (:553) makes collision-free coexistence free; the WC-0 column union and the three-way release fork; WC-6's conservation walker, which the tag walker composes with rather than duplicates; WC-12's brigand branch as the rejection escalation's destination."},{"wave":"WC-14 HOMECOMING AND THE PULSE","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"One ~180-eff leaf (demobilizationPulse.js: institution supply writes into mercenaryMarket and the INT leaves, free-lance pool feeds, orphan branch routing, founding and peace decay) plus three distinct behavior families each needing its own packet: the full proportional-homecoming double effect (ONE function, two consumers — destabilization = share × drift depth and defense recontribution = the same share — pinned so that both effects read the SAME value from ONE call, with the two-computations mutant redding); introduction attempts as a FOURTH traditions relation pass (typed attempts, a receptivity contest, most of them failing, adoptedFrom = the origin mix, plus faith exposure as a bounded sanctioned nudge through traditionsKernel's existing one writer); and the demographic-signature-of-a-peace receipts, addressed and archived under the news address law. THE ORPHAN PIN is re-aimed at the routing §1.12.5 now states: a destroyed-home block becomes a FREE UNIT, walks, and reaches a settlement ONLY through `shed` — census credit plus a free-lance tag at exactly one addressable host, with the pin asserting addressability (no counts are ever written to a razed settlement's row, which does not exist) and the walker closing at every hop; the block-straight-to-free_lance mutant reds because that arm is no longer in the event table. The most-attempts-fail pin is a corpus-wide banded assertion, not a single fixture.","dependsOn":"WC-13 (7.F). Also lights directive (d)'s landing and (e).","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant. No further gate. Tuning at soak: receptivity weights, pooling thresholds, peace-decay bands, the destabilization severity curve. ⚠ Note the standing fence the volume keeps here: mercSupplementOf and SUPPLEMENT_MAX 0.35 stay a census-free CAPABILITY read and are NOT converted — the force market is the owner's parked widening and this wave must not touch it.","outputMoving":"NO — dark behind warCirculationEnabled && moverAbsorptionEnabled.","workerKb":"0 KB to the generation worker. ~24 KB of new source (~5 KB bundled) in the lazy pulse chunk.","reuse":"traditions/relations.js's advanceRelations takes a fourth pass rather than a new machine; traditionsKernel's sanctioned religionState share nudge (one writer, bounded); mercenaryMarket's supply side and institutionCatalog/Roster from INT for the founding road; WC-13's shed event and tag ledger, which the homecoming arithmetic writes through unchanged."},{"wave":"WC-15 RESIDENT COHORTS AND DIASPORA BONDS","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"Deliberately cheap in people-arithmetic and expensive in proofs. residentCohorts.js gains NO new people arithmetic here — WC-13 already wrote the ledger and WC-13's totality walker already conserves it — so this wave adds the year-fold mortality debit pro-rata by the settlement's own realized death fraction (in the SAME fold as the census `mortality` event it partitions), the COHORT_AGE_BANDS steps, the `untag` and `demobilize` tag events (`untag` carrying its RESIDENCY_TAGS kind: both the naturalization an earlier drafting called `absorb` AND the free-lance retirement that had no named event at all), diasporaBonds.js at ~150 eff, the four bond expressions as READ-TIME modulations (trade affinity, war-reluctance strategist weight, aid warmth, and an espionage-ease term composed into the existing infiltration read), cohort chips and tables, and the veteranCohortsEnabled CQ5 trio — which gates the READS, not the ledger. THE CR-WC-21 RULED PIN SET is three pins that replace two variants and are strictly stronger than either: the count term is MORTALITY-DRIVEN (zero-mortality world, arbitrary elapsed time, byte-identical cohort count; the calendar-count-decay mutant reds); THE FLOOR IS REAL (elapsed time pushed arbitrarily past the last band edge still reads a nonzero STABLE bond forever, with two mutants — a fourth lower band appended, and the floor weight set to 0); and MORTALITY IS THE ONLY ZEROING ROAD (an emptied cohort reads ABSENT). Plus the sixty-year embodied-vs-institutional fixture (cohort retired, bond neutral, the introduced tradition STILL present), a no-new-decay-law source scan (no Math.pow half-life, no bandedStock call in the cohort path), and THE SELF-ORIGIN PIN with both halves in one fixture — veteranReads sees a nonzero living cohort while hostReadsResidents(S, S) is ABSENT, not zero, so no consumer can average it in.","dependsOn":"WC-13 (the sheds and the tag LEDGER itself) and WC-14 (the returns). 7.F: WC-15 <- WC-13, WC-14.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant. CR-WC-21 is RULED Position B and UNBLOCKS this wave (WC volume header:29-30; WC-PREAMBLE.md §P9 records it as standing authority): calendar bands are kept and RECENCY_WEIGHTS is reclassified a BOUNDED, FLOORED band modulation rather than a decay law. The honest cost of that arm is recorded — the no-new-decay-law fence keeps its source-scan shape but its DOC claim narrows. Tuning at soak: age-band edges (year-denominated, inside the sub-century ruling CR-WC-4), recency weights, the espionage-ease magnitude.","outputMoving":"NO — dark behind warCirculationEnabled && veteranCohortsEnabled.","workerKb":"0 KB to the generation worker. ~20 KB of new source (~5 KB bundled) in the lazy pulse chunk.","reuse":"Maximal — this is the volume's cheapest wave precisely because WC-13 pre-paid for it: the tag ledger, its one writer and its three-law totality walker all exist, so WC-15 is read-only over people. The espionage-ease term composes into the EXISTING infiltration read; the four expressions are read-time modulations of existing consumers, not new writers; the mortality debit rides the census `mortality` event's own fold."},{"wave":"WC-16 VETERANS AND THE ORDER DIVIDEND","status":"UNBUILT","carsLow":4,"carsHigh":6,"reason":"One ~150-eff leaf (veteranReads.js: the readiness term at warCapabilityOf and martialReadiness, the defense term at the defenseLedger read sites, and the crime-suppression term at the live crime read — all three over the SELF-ORIGIN cohort row WC-13 already writes) plus veterans-first muster with its selection AND its tag arm stated (MUSTER_VETERAN_PREFERENCE drawn from `veteran` cohort rows first, largest-remainder in codepoint origin order, balance from untagged census, then pro-rata, with those rows DEBITED in the same event plus the `untag` of any drawn person's free-lance overlay), the field-or-garrison strategist fork, comradeship reactivation on re-muster seeded from shared_service archive rows, and directive (p)'s symmetry surfaced in prose. Greener-armies falls out of conservation rather than being computed. The pins are what price it: the three-reads-one-cohort pin whose stale-read mutant is one read caching independently; a LIVE-NOT-FROZEN source-scan fence proving no veteran term touches defenseProfile.scores; the greener-armies pin over TWO consecutive seeded wars; THE FIELD-OR-GARRISON DIFFERENTIATED PIN, passable at last because the FIELD arm debits the cohort row and the GARRISON arm holds it — and the mutant that must red is THE OLD DESIGN, a `muster` with its tag arm deleted, under which both arms read identically and the pin collapses; and THE MUSTER-AND-RETURN ROUND-TRIP PIN, which asserts residentCohorts is BYTE-IDENTICAL PER ROW after a muster and a return, not merely equal in total — the composition-discarded mutant laundered foreign-origin cohorts into the host on every cycle while LAW 1 closed throughout, which is why only a round trip can see it.","dependsOn":"WC-15 (7.F). The terminal wave of the volume.","ownerGate":"CR-WC-9 — SIGNED under the 14:4x grant. No further gate. Tuning at soak: the three read magnitudes and the muster preference weights.","outputMoving":"NO — dark behind warCirculationEnabled && veteranCohortsEnabled.","workerKb":"0 KB to the generation worker. ~20 KB of new source (~5 KB bundled) in the lazy pulse chunk.","reuse":"demographicsWar's warCapabilityOf and martialReadiness take a new TERM at live read points only; defenseLedger's existing read sites and pressureModel's crime read take compositions, never a write to the frozen defenseProfile.scores; WC-13's self-origin cohort row and blocks[].drawnCohorts from WC-6 make the round-trip identity expressible; WC-9's serviceBonds ledger and shared_service archive rows seed reactivation."}],"firstCar":"WC's first car is WC-1's opener: turn `src/domain/worldPulse/contributionLedger.js` from the 45-eff shape WC-0E landed into THE ONE WRITER of `spatialLedgers.warContributions`, scoped to the ASPATIAL bank-tick credit only — troops-lent credit at the levy/reinforcement effect edge, the `CONTRIBUTION_ARRIVALS_CAP` 24 arrivals cap, and nothing else (supplies-at-shipment-arrival, shortfall booking, the close fold and the obligation deposit are later cars; the spatial arrival edge is WC-6's mover-kernel detector). The car MUST carry the `src/lib/spatialUsage.js` TRACKED row and the certification `stateKeys` declaration in the SAME commit as the first `setSpatialLedger` call, because `tests/lib/spatialLedgerCoverage.walker.test.js` asserts `classified` EQUALS `written` in BOTH directions — that symmetry is exactly why WC-0 deliberately deferred the row (the module's own header at :14-21 cites ODQ §73.3's F-1: \"`warContributions` travels to WC-1 with its writer\"), and it is why the row cannot be split off into a follow-up. The two virtual certification rows at `src/domain/certification/subsystemRowsVirtual.js:305` and `:355` each name this exact commit as \"THE OBSERVATION THAT WOULD CLOSE IT\", so both flip from virtual to a real container declaration here. ⛔ NO PIN IN THIS CAR MAY BE AUTHORED AGAINST LEVY-PRODUCED ROWS ON THE SEEDED CORPUS: `war_levy` is MEASURED SILENT (`src/domain/certification/subsystemRowsWar.js:55` — zero fires in every one of the seven completed release cases while `army_deployed` fires 7 and `war_conscription` 8, because LEVY_POP_RATE_PER_TICK 0.004 against LEVY_POP_FLOOR 300 means a source under roughly 550 people levies nobody), so every credit fixture is an explicitly CONSTRUCTED levy fixture that asserts non-emptiness BEFORE it asserts anything else. The pins are the aspatial twin of the ARRIVAL-NOT-DISPATCH pin (credit lands at the bank tick, exactly one row, with the write-at-dispatch mutant executed and red), the FOUR-FENCE dormancy proof — the existing `tests/property/warCirculationDormancyFence.test.js` gains its ledger-key arm beside the rng, news-kind and display fences it already carries — rng draw-count parity dark vs lit, and the coverage walker green in both directions. Budget it against PACKET_STANDARD's hard caps: one behavior family, one new persisted record family, one named writer, ~150–200 new effective production lines, at most eight acceptance cases, and a header line citing `docs/implementation/preambles/WC-PREAMBLE.md` by SHA-256. Re-measure the ceiling ledger (§2.1) at the building commit rather than inheriting the volume's `cbd348a5`-era figures, and expect the car to move the census bills (test totals, the lighting census, the mutation-coverage manifest row every new `tests/lint/**` file owes under §P3b) while the seeded world stays byte-identical — declare that motion, never report a green you did not run."}}
{"type":"result","key":"v2:4e6dc6aaac5d2de894ae6b996455f4dfb8b01556c73b1a8766ed058125d6bc6f","agentId":"a07029ba4c63c2954","result":{"family":"HB","summary":"HB — habit conditioning, priced against the volume at /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a/docs/DESIGN_FP_ARCH_HB.md (§4 the waves, §7 the chair questions) and re-measured against the code at /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-headroom (claude/composite-r4 @ a5876c0ea). Nine waves remain: HB-1 and HB-2 part-built, HB-3..HB-9 unbuilt. The honest band is 21-39 cars, centred near 27.\n\nTHE FOUR THINGS THAT MOVE THE PRICE, all measured rather than read off the volume.\n\n(1) THE WORKER BUNDLE IS NOT A CONSTRAINT ON THIS FAMILY, and that is CONFIRMED by execution rather than argued. A BFS over the import graph from src/workers/generation.worker.js reaches 153 modules and ZERO src/domain/worldPulse modules; the same BFS from src/workers/advanceInterval.worker.js reaches 559 modules of which 375 are worldPulse. Every line of HB code lives in worldPulse (or, for HB-8's dossier arm, in src/domain/display and src/components/map, both main-thread), so no HB wave adds a single byte to the generation worker sitting at its MONOTONE-DOWN ceiling. The whole landed HB substrate is ~13 KB of effective source (habitCurve 4.3 KB code-bytes, habitLedger 6.6 KB, habitVocabulary 1.6 KB, habitGate 0.3 KB, strategyMoves 0.5 KB); the 36.8 KB habitForkRegistry.js has ZERO src importers — only tests — so it is in no bundle at all. The whole remaining programme adds roughly 20-25 KB of effective source to the ADVANCE worker graph, all of it dark behind flags lit in no preset. No lazy chunk is needed, and none is possible: the advance worker is one bundle.\n\n(2) THE ONE REAL OWNER GATE IS Q1, AND IT SITS ON HB-3. Two stamped fields on persisted campaign records — deployment.habitEpisode and treaty.habitEpisode (DESIGN_FP_ARCH_HB.md:3881-3882). Persisted shape is an owner-gated class and the volume refuses to decide it. Everything else in the family is signed under the 14:4x grant. The cheapness evidence re-measures clean today: DeploymentRecord is a deliberately untyped bag, TreatyRecord is `Record<string, unknown>` declared exactly once in src/domain/worldPulse/peaceTermsCatalog.js, neither mint path validates key shape, and the only treaty-key assertions in the tree are negative-and-specific (tests/domain/sovereigntyMarketStageWr10w.test.js's not.toContain arms). A veto costs close 1 and the sue-arm join alone; close 2 survives on its recomputed road.\n\n(3) TWO PIECES THE VOLUME CHARTERS AND NO WAVE HOMES. `circumstanceClassOf(actor, worldState)` is ruled TOTAL at §1.2(a) and returns zero hits anywhere in src; the cert row at src/domain/certification/subsystemRowsVirtual.js:177 says so in its own words — \"the classifier has no lawful home yet, which is a blocking question for HB-4\". And habitCurve.js exports only the pure `habitFactor(stock, span)`; the world-reading `habitFactorFor(worldState, actorId, cls, action)` that every §3c load point calls does not exist. Both are priced into HB-4, but a credit write is keyed on `<class>.<action>`, so whichever of HB-3/HB-4 lands first pays for the classifier.\n\n(4) THE RECORD IS STALE AGAINST THE CODE IN THREE PLACES A WAVE BRIEF WOULD TRIP ON. HB-1's `'fortify'` disjunct deletion was RE-REFUTED at build — src/domain/worldPulse/settlementPolitics.js:621 records the token as a registered foreign move with two ratified same-seed goldens (1.02 dark / 1.0168 lit), the opposite of the volume's ruling. HB-5's \"bought headroom\" is gone: scripts/.size-baseline.json records settlementStrategy.js 812 -> 760 ENTRY DELETED, done by lane TE-CEIL rather than by HB-1, and the file measures 775 effective against the 800 domain ceiling — 25 lines of room for four additions. And HB-8's \"(b) and (c) SHIP DARK-FOREVER UNTIL SP-D LANDS\" is false: errandSpineEnabled is registered at src/domain/worldPulse/simulationRules.js:271, read by name at errandMint.js:76, and mintErrandSpine is imported by pactProposals.js:65 and envoyErrand.js:86. SP-D landed; waves.md already carries the erratum.\n\nNothing in this family is output-moving at landing. Every wave rides habitConditioningEnabled, believedDoctrineEnabled, habitAnticipationEnabled or doctrineTapEnabled, and all four are virtual and lit in no preset — src/lib/spatialUsage.js:322 states that limit in source. Output moves at lighting, which is a separate owner act.","waves":[{"wave":"HB-1 — the action vocabularies + the fork registry (residual: the promotion shape)","status":"PART-BUILT","carsLow":1,"carsHigh":2,"reason":"Landed and verified: src/domain/worldPulse/strategyMoves.js mints STRATEGY_MOVES with ALL_MOVE_TOKENS, NON_MARTIAL_HISTORY_MOVES and the derived MARTIAL_HISTORY_MOVES, consumed live by settlementStrategy.js:79; src/domain/worldPulse/habitForkRegistry.js carries 42 fork rows (36 DEFER, 6 STAY, ZERO LEARN) with FORK_DISPOSITIONS, FORK_ARITIES, NAMED_DOMAIN_LABELS (the closed eight) and OWNER_DOMAIN_MAPPING (the owner's seven); tests/lint/chooserTotality.walker.test.js runs with the root-set self-assertion and a DEFER_CEILING of 36 exact in both directions; tests/lint/strategyMoveVocabulary.walker.test.js freezes MOVE_TOKEN_BRANCHERS. What is NOT built is the promotion shape, and it blocks every downstream wave: fork() at habitForkRegistry.js:101 hard-codes `circumstanceClasses: Object.freeze([])` at line 109 AFTER the row spread, so no row can ever declare its classes; `dyadicActions` returns ZERO hits in src and tests although §3c makes it mandatory on the one per-action row (HBF-17, settlementStrategy) and walker-asserted against enumerateMoves' own conditional; and HB-4's site 3, coalitionRatification.castRatificationBallot (live at src/domain/worldPulse/coalitionRatification.js:209), has NO registry row at all — zero hits for coalitionRatification in the registry — and is not idiom-discoverable, so it needs a hand-maintained checklist row plus its own totality arm before any load can sit there. One car covers all three plus the walker arms; a second only if the coalitionRatification row's discovery arm proves contentious.","dependsOn":"Nothing new. The registry is a live shared file other families keep minting into (HBF-33 from IN-1c, HBF-34 from WF-8a, HBF-42 and HBF-36..41 from ENC-4), so CHECK-GIT-FIRST is mandatory and the DEFER_CEILING moves in whatever commit shrinks the list.","ownerGate":"None — signed under the 14:4x grant. One record debt to raise vetoably at the same sitting: the volume's HB-1 ruling 'delete the `move === 'fortify' ||` DISJUNCT ALONE' was re-refuted at build (settlementPolitics.js:621) and the code now registers the token instead; the volume still carries the deletion.","outputMoving":"NO — registry shape and walker arms only, zero src consumers, no behaviour anywhere.","reuse":"The whole registry, its four walkers and the frozen move vocabulary are landed; this car edits one helper, adds one field and one row.","workerKb":"0 KB to either worker. habitForkRegistry.js has ZERO src importers (tests only), so its 31 KB of effective source is in no bundle; strategyMoves.js is already in the advance graph at 507 code-bytes and does not grow."},{"wave":"HB-2 — the ledger + the flag (residual: the twelfth coupling prefix's leaf)","status":"PART-BUILT","carsLow":1,"carsHigh":2,"reason":"Landed and verified: src/domain/worldPulse/habit/habitLedger.js carries HABIT_LEDGER_KEY, the single writer writeHabits (with stocks and open), the normalizer, drop-when-neutral at four levels and two deterministic evictions with codepoint tiebreaks; habitGate.js is door one and the ONE `habitConditioningEnabled === true` by-name read in the tree; the flag is registered at simulationRules.js:307; spatialUsage.js:304-322 carries the TRACKED row; subsystemRowsVirtual.js:159-201 carries the AUTHORED cert row stating the writer is uncalled by design; tests/lint/habitLedgerSingleWriter.walker.test.js, tests/property/habitNeutralIdentity.test.js and the engineGatedRuleKeys registration at line 955 are all in place, and all four habit leaves have ARGUED_UNLAYERED rows at tests/lint/couplingInclusion.walker.test.js:785-810. Residual, one car: `HB` IS the twelfth member of CHARTERED_VOLUME_PREFIXES (tests/domain/couplingRegistry.test.js:108) but src/domain/certification/couplingRegistryHabit.js DOES NOT EXIST and there is no CPL-*.HB-* row anywhere in src/domain/certification — Q4's ruling is half-executed, the document act done and the leaf owed. Second residual, deliberately NOT priced here: §1.4's measured bound (median and p95 live rows per actor in the module header) returns zero hits for median/p95 in habitLedger.js, and it is structurally unmeasurable while writeHabits has no caller, so it rides HB-4 rather than a car of its own — record it as deferred-and-documented, not as a gap.","dependsOn":"HB-1 only if the coupling rows name fork ids. Otherwise free. CQ5-class shared files (spatialUsage, simulationRules, subsystemRowsVirtual) serialize against every other flag and ledger wave.","ownerGate":"None — signed.","outputMoving":"NO — dark by charter; the writer is uncalled and the cert row makes that the wave identity claim rather than a blind spot.","reuse":"Everything the wave chartered except the coupling leaf; the leaf has seven worked siblings (couplingRegistryWar/Trade/Espionage/Grammar/Info/Encounters/Schema) to copy the shape from.","workerKb":"0 KB to the generation worker (CONFIRMED by BFS). ~7 KB of effective source already resident in the advance graph and unchanged by this car; the coupling leaf is certification-side and main-thread."},{"wave":"HB-3 — the credit fold","status":"UNBUILT","carsLow":3,"carsHigh":6,"reason":"The heaviest single-module wave and the flagship. habitCredit.js (budget 300 effective) carrying the EIGHT frozen grade maps, each TOTAL over its own imported closing vocabulary (close 6 is six rungs with cancelled ⇒ LAPSE; close 8 is a BANDING of a continuous progress into SEVERITY_LADDER by HABIT_TUNING edges authored at HB-0, never here); the authored complianceToSourceKind bridge reconciling evolveCompliance's THREE observed states against close 2's FOUR disposition source kinds, with totality asserted on both sides; the episodeKeyFor family split by audited road — CARRIED for closes 1, 4, 6 and 8, RECOMPUTED for 3 and 7, and BOTH for close 2, which rides site 8's recomputed terms:<treatyId> and site 7's carried treaty.habitEpisode and therefore owes BOTH pins; the credit application through writeHabits; the lapse road and its receipt; EIGHT close-side hooks at 6 lines each at the sites' own outcome seams, never in pulseKernel.js or applyWorldPulse.js (both banked, zero headroom); and the two foreign stamped fields, deployment.habitEpisode copied at warDeployment step 4 and treaty.habitEpisode stamped WRITE-WHEN-KEYED on emitMove's bilateralOffer gate. Four cars is the honest centre — (1) grade maps plus the compliance bridge and their totality pins, (2) the episode-key family with both roads and one seeded fixture per close, (3) the credit application plus the lapse road plus the observedState omniscience-leak mutant and the four-rungs/both-directions reachability measurement, (4) the eight hooks plus the two stamped fields plus the gated-join anchored negative. Eight hooks across eight different host modules is what pushes the top of the band to six.","dependsOn":"HB Q1 (the owner signature, §7, DESIGN_FP_ARCH_HB.md:3881-3882) — hard blocker on close 1 and the sue-arm join. HB-2's ledger (landed). The unhomed classifier circumstanceClassOf: a credit row is keyed on <class>.<action> and the function exists nowhere, so this wave homes it or every hook takes a class it cannot compute. ⚠ SEQUENCING HAZARD the volume does not flag: the pledge OPEN lives at the chooser, which is HB-4 and HB-5, two waves later — so HB-3's carry-road round-trips must seed their own opens or the wave lands with pins that cannot execute.","ownerGate":"YES — Q1, a persisted-shape change on TWO records. This is the family's one true blocker and it is NOT covered by the 14:4x grant, because persisted shape is an owner-gated class the volume explicitly refuses to decide. Decline price, re-parsed against the eight-close table: seven of eight closes survive, deploy reads a stock nothing writes, and the sue arm loses its keyed join.","outputMoving":"NO at landing — everything rides habitConditioningEnabled, virtual and lit in no preset.","reuse":"HB-0's applyCredit, severityWeightOf, seatAdjusted and roundToUnits (the one rounding door) and HB-2's writeHabits and pledge book are all landed, so the write path costs nothing. DeploymentRecord is an untyped bag and TreatyRecord is Record<string, unknown> declared once in peaceTermsCatalog.js with no key validator anywhere, so neither field costs a typedef, a schema-version bump or a migration — absent-field records ARE the legacy shape.","workerKb":"~4-6 KB of effective source into the ADVANCE worker graph (habitCredit at its 300-line budget plus eight 6-line hooks). ZERO into the generation worker — CONFIRMED, its graph reaches no worldPulse module. No lazy chunk: the advance worker is one bundle and the code is dark-but-resident behind the gate."},{"wave":"HB-4 — the chooser read, wave one: outside the war lane","status":"UNBUILT","carsLow":2,"carsHigh":4,"reason":"The charter reads cheap — three sites at 8 lines each plus a registry row — and it is not, because two substrate pieces it depends on do not exist. The three sites are site 1 demographicsResponses.selectResponse (MONADIC), site 3 coalitionRatification.castRatificationBallot (DYADIC, verify-at-build on whether the term sheet exposes a stable offerer id) and site 6 tradeWar.evaluateTradeWar into contestOverThirdParty (DYADIC). The unpriced weight: the circumstance CLASSIFIER circumstanceClassOf, total over the 12-member CIRCUMSTANCE_PRECEDENCE array by first match, with the sorted-array-read-as-precedence trap pinned as a permutation assertion; the habitFactorFor world-reading wrapper, because habitCurve.js exports only the pure habitFactor(stock, span) and nothing that touches a world; and the pledge OPEN at each of the three sites, which is where the episode key is minted. ⛔ AND THE INSTRUMENT CONTRADICTS THE CHARTER: HB-4 says demographics is deliberately first 'because it is the one site whose open→close join is already complete in the tree', but HB-1's own landed row HBF-03 carries closeOwed 'grade a chosen response against the overflow band it was meant to relieve, over the horizon the band takes to move' — the close is owed, not complete — and HBF-32 owes the contest FLIP close 'together or not at all' with the trade-partnership row. tests/lint/chooserTotality.walker.test.js:313 enforces a non-empty closeOwed on every DEFER row and an exact DEFER_CEILING of 36, so a DEFER→LEARN promotion without a minted close cannot be committed. Either this wave mints two or three closes (a car each) or it lands behind HB-9 with that slip recorded.","dependsOn":"HB-3 (a pledge with no close grades nothing). HB-1's promotion-shape car (circumstanceClasses pass-through, the coalitionRatification checklist row). HB-9's closes for sites 1, 3 and 6 — or a close-minting car inside this wave. HB-2's deferred measured bound lands here, because this is the first wave that supplies a caller and makes the corpus measurement possible.","ownerGate":"None — signed. No persisted shape, no new flag, no tuning value (the span the loads multiply is HB-0's).","outputMoving":"NO at landing — dark behind habitConditioningEnabled; the per-site pins are same-seed lit/dark differentials, and the dark path is byte-identity fenced.","reuse":"The vocabulary, the curve, the ledger, the gate and the neutral-identity property are all landed and pinned; the twelve classes and their precedence array are frozen and walker-checked.","workerKb":"~3-4 KB into the advance graph (the classifier leaf, the habitFactorFor reader leaf, and 24 lines across three existing hosts). ZERO into the generation worker."},{"wave":"HB-5 — the chooser read, wave two: the war-chooser joint","status":"UNBUILT","carsLow":3,"carsHigh":6,"reason":"Two declared slices, but the file budget sets the price, not the logic. The work: the renormalized multiplicand on the POST-SOFTMAX weights array at settlementStrategy.js:1229 (per-action, dyadicActions: ['deploy'], because both of enumerateMoves' emission paths condition bestTargetId on the deploy token), warPeaceDecision's accept threshold, npcLadderGoals.mintGoal's goal-kind mint, and the signed CR-ES-4 spy-before-decision read at the same candidate-scoring block. ⚠ THE BOUGHT HEADROOM THE CHARTER COUNTS ON IS GONE. scripts/.size-baseline.json records settlementStrategy.js 812 -> 760 with the ENTRY DELETED — executed by lane TE-CEIL, not by HB-1 — and the file measures 775 effective lines today against the 800 domain layer ceiling, leaving 25 lines for four additions plus their reason clauses. The same baseline note names five instruments pinning symbols to that address (the warFrontReads routing, the applyMode ternary read as source text, the softmax sample's discoverability to chooserTotality and to HBF-17, the exact-both-directions move-literal branch set, and the observed-shape-readers baseline addressing the file by name), so a naive addition either breaks the ceiling or moves a pinned symbol. HB-5 pays a second behaviour-identical pure-leaf extraction, exactly as TE-CEIL did, and that is a car on its own. Four cars: (1) the extraction that buys room; (2) the post-softmax load plus the positional scan with its planted pre-softmax mutant, the pairwise odds-ratio pin at STRATEGY_K = 3.5 with the hand-walked [0.4815, 2.0769] fixture, the return_home hard-override negative and the stream-identity pin; (3) warPeaceDecision plus the gated-join anchored negative with all four bilateralOffer disjuncts falsified in their own arms and an unconditional stamp as its mutant; (4) the ladder slice plus the four-fence re-run against the war lane's goldens.","dependsOn":"HB-3 (close 1 and the sue-arm join). HB Q1 for the treaty.habitEpisode half. HB-1's dyadicActions field, without which HBF-17 cannot promote. Serialization against any in-flight war wave — this is the estate's most-contended module and CHECK-GIT-FIRST is mandatory.","ownerGate":"None beyond Q1's inheritance. The spy read is SIGNED: CR-ES-4's deferred arm was released by the owner's blanket sign-off of 2026-08-05, recorded at §0.3 R1 and treated as released at §4, §5 clause 5, §8.1 row 5 and §3d seam 4'. The habit read itself rides the directive's TOTAL SCOPE clause, which postdates the grant and is broader.","outputMoving":"NO at landing — both reads are gated, and beliefsActive orthogonality is pinned so the habit term behaves identically in both belief states.","reuse":"The softmax caller, stableSampleByWeight, the six existing enumerateMoves loads and the clamp01 idiom all stay untouched — the load sits at the caller, not inside the enumerator, so nothing upstream of softmaxWeights( moves. TE-CEIL's pure-leaf extraction method is a worked precedent in the same file.","workerKb":"~2-3 KB net into the advance graph; the extraction moves bytes sideways rather than adding them. ZERO into the generation worker."},{"wave":"HB-6 — the believed-doctrine axis","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"The most mechanical wave in the family, because SP-B's five-join spine is landed and has three worked precedents. The work: doctrineBands as the FOURTH SP-B subject family — the derivation in beliefAxisSubjects.js (190 effective today, growth budget 90), the by-name line in subjectAxesActive (beliefAxes.js:88), the opts member through axisGroundTruth (beliefAxes.js:165, the one truth door), the arm in foldSubjectAxes, the BeliefRecord @property, a fourth member on SUBJECT_AXIS_FIELDS (beliefAxisSubjects.js:130, currently exactly three: conditionsBands, devotionBand, scarcityBands), the manifest key, the AUTHORED cert row in the SP-B shape with aliveness channels deliberately empty, doctrineSheetOf on the truth side, and the second flag believedDoctrineEnabled with its full CQ5 trio. ⚠ THIS IS THE SECOND TUNING-BEARING WAVE and both its bands are owed a reachability MEASUREMENT rather than an authored guess: the doctrine adoption bar, because refinement 13's whole mechanism is the arithmetic claim that a tap clears it and hop-worn news does not, and the believed-law-band decay contribution, because it composes into the existing per-pair decayKeep01 alongside the SEE/HIDE posture and the pair must not saturate clamp(k, -1, 1). Two cars: (1) the five joins, the flag, the cert row and the extended SP-B vocabulary walker executing the derivation rather than comparing a transcription; (2) the adopt-bar reachability measurement on generated worlds plus the per-observer WeakMap memoization, without which the fold is quadratic in a realm's size.","dependsOn":"SP-B — LANDED and verified in code (beliefAxes.js:38 imports subjectGroundTruth and foldSubjectAxes from beliefAxisSubjects.js; beliefMap.js:66 imports the four-symbol set). Independent of HB-3, HB-4 and HB-5, which makes it the family's best parallel lane.","ownerGate":"None — signed. Two band values, which are tuning under §8.2 rather than owner-gated shape; the CQ5 trio is the ordinary flag ritual.","outputMoving":"NO — second flag, virtual, lit in no preset; the fold's dark path is byte-identity pinned with the spread SKIPPED rather than an object of undefineds.","reuse":"The entire SP-B join spine, its three existing families as shape precedents, the per-pair decayKeep01 modifier (so no second decay law and no per-target parameter), and the SP-B cert row shape for a conditional field inside another subsystem's container.","workerKb":"~2 KB into the advance graph (beliefAxisSubjects growth capped at 90 effective lines, 6 lines each into beliefAxes and beliefMap). ZERO into the generation worker."},{"wave":"HB-7 — the anticipation term + the deviation damper","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"One new leaf and two seams. beliefDoctrineAnticipation.js (budget 260 effective) carrying the counterWeight tables — AUTHORED design knowledge over closed vocabularies, one per DYADIC joined vocabulary only (STRATEGY_MOVES and the pact answers; never RESPONSES or the goal kinds, so an authored-cell census over a monadic vocabulary can never become an undischargeable obligation) — plus anticipationFactorFor, awareness01 and the effective-span damper. Seams 2 (the beliefMap injected closure) and 4' (the war chooser, where the term is simply the SECOND factor of the dyadic product HB-5 already built with subjectId = bestTargetId, so this wave adds a multiplicand and no new seam); seam 4 is STRUCK (convergence.reactiveResponse, forkless, worldless and uncalled in src); seams 1 and 3 pre-pinned from both sides, 5 and 6 declared-deferred with reasons. Third flag habitAnticipationEnabled with its CQ5 trio. Two cars: (1) the leaf, the tables, the flag and the cert row; (2) the two seams plus the pin set — the ZERO-IMPORT scan proving the leaf cannot import habitLedger and therefore structurally cannot read the subject's truth (with a guard-the-guard positive control pointing at a legitimate truth reader outside the set), the level-one negative, the counterWeight totality where a missing cell reds rather than defaulting to zero, the damper's both-sides-live mutant proving awareness01 is a product and not a truth read, the infoStatecraftEnabled-dark declared-degradation pin, and the closure null ⇒ byte-identical anchor on the sightOf precedent.","dependsOn":"HB-5 (the dyadic product at the war chooser is the multiplicand's host) and HB-6 (the believed-doctrine axis is what the term reads). Strictly after both.","ownerGate":"None — signed. Bands NONE BY DESIGN: ANTICIPATION_SPAN and DEVIATE_W are declared in HABIT_TUNING at HB-0, and the counterWeight cells are authored design knowledge over a closed vocabulary, which is not a band and never enters a soak manifest.","outputMoving":"NO — third flag, dark; and the leaf is named INTO the belief* spelling deliberately so LAYER_PATTERNS.INFO's existing /belief[A-Z]/ pattern claims it, which is what keeps it visible on both sides of the inclusion ratchet.","reuse":"HB-5's dyadic product shape, the existing /belief[A-Z]/ INFO family (so no eighth LAYER_PATTERNS family is owed — Q4's corollary), the outboundImpression geometry and the sightOf closure precedent.","workerKb":"~3 KB into the advance graph (one 260-line leaf plus two small seams). ZERO into the generation worker."},{"wave":"HB-8 — the doctrine sheet: dossier + tap + plant","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Four lettered arms and the family's only wave touching a .jsx or owing a DM edit-verb rationale. (a) THE DOSSIER — the sheet row family inside src/domain/display/settlementBeliefs.js (116 effective) and src/components/map/BeliefDivergenceBand.jsx's existing premium/elevated band, plus the OWN-SHEET TRUTH row; display read 80 added, band component 60; no new panel, no new gate. (b) THE TAP — doctrine as the SEVENTH ES legRef, an ES-volume vocabulary amendment landed in THIS commit, three tap mappings, products through reconcileBelief as synthetic reports. (c) THE PLANT — planted doctrine on the REPORT road with the disinfo lineage row. (d) THE VOICE — the doctrine_shifted Herald kind with its five joins and its own walker file; doctrine_shifted returns ZERO hits in src and tests today, so the kind is wholly new. Fourth flag doctrineTapEnabled with the CQ5 trio. ⭐ THE CHARTER'S DARK-FOREVER CLAUSE IS STALE AND MUST NOT BE COPIED INTO THE CERT ROW: '(b) and (c) SHIP DARK-FOREVER UNTIL SP-D LANDS (errandSpineEnabled does not exist)' is false — errandSpineEnabled is registered at simulationRules.js:271, read by name at errandMint.js:76, and mintErrandSpine is imported by pactProposals.js:65 and envoyErrand.js:86. SP-D landed; waves.md already carries the erratum. All four arms are reachable the day the flag lights. Four cars, one per arm: the dossier with its fail-closed PLAYER-projection negative seeded with a real sheet and the absence-renders-as-absence pin; the tap with the ES vocabulary amendment and the executed tap-depth matrix; the plant with the plant-override exact-six-key negative and the writer-boundary scan executed as a mutant that tries the override road; the Herald kind with its five joins and walker.","dependsOn":"HB-6 (the sheet is the believed-doctrine axis's product) and HB-3 (there is nothing to show until something has learned). The ES legRef amendment lands in the same commit, so the ES lane serializes against it.","ownerGate":"None for landing — the fourth flag is virtual and the code ships dark. ⚠ LIGHTING it is a separate paid-surface question, because the tap is an ES product sold by access depth; that is a lighting decision, not this wave's.","outputMoving":"NO at landing. (a) and (d) are the arms that become reachable the day the flag lights; (b) and (c) are now reachable too, which is the correction above.","reuse":"settlementBeliefs.js and BeliefDivergenceBand.jsx both exist with a premium/elevated band already in place; reconcileBelief and the synthetic-report road are landed; the ES tap-product ladder has six worked members to add the seventh to; the SP-D errand spine is live.","workerKb":"ZERO to either worker for (a) and (d) — src/domain/display and src/components/map are main-thread. ~2 KB into the advance graph for (b)/(c). The generation worker is untouched, as for every HB wave."},{"wave":"HB-9 — the closes owed + the measure","status":"UNBUILT","carsLow":4,"carsHigh":8,"reason":"The widest band in the family, because its size is whatever the DEFER list still holds when it starts. Chartered: the OCCUPATION close (STATE_LADDER graded against the episode key, the pledge book carrying the open so no foreign schema change); the ROUTE close (arrival vs banditryLoss, applied as a DIVISOR on effectiveCost so the argmin is preserved and no draw is added to four callers); the ROUTE-CREATION CHARTER close and its load (corridor survival vs routeNetworkDecay retirement against the frozen seven-word CHARTER_VERDICTS, applied as a bounded multiplicand on score BEFORE the OBJECTIVE_BAR comparison, adding no draw and minting no vocabulary); the TRADE ROUTING close; plus the three closes the action-coverage law put on the books at the polish — the generosity verdict (site 2), the intervention engage/decline (site 10) and the sovereignty offer-posture (site 12), each with its closeOwed sentence already written. Then the dispositive literals for all four flags' certification rows, and the SIX HB envelopes (habit-row density per actor, class occupancy mix, hold-rung mix, doctrine-sheet coverage, plant share, lapse rate), each INTERVAL_WEEKS-derived with an executed mutant, plus Q3's same-input cross-run stock-identity check. THE MEASURED REALITY that sets the top of the band: the registry stands at 36 DEFER and 6 STAY across 42 rows with ZERO LEARN, and DEFER_CEILING is pinned exact in both directions at chooserTotality.walker.test.js:105, so every promotion lowers it in the same commit — while other families keep ADDING defer rows (HBF-33 IN-1c, HBF-34 WF-8a, HBF-36..42 ENC). The prose-pick family alone (HBF-07 / 33 / 34 / 42) is four rows that close together or not at all, and the nine strategy actions with no close — hold's ABSTENTION close in particular, a close-shape this estate has never minted — are named by the volume as the programme's largest honest gap. Roughly one car per close family plus one for the envelopes and the four cert literals; more if the prose-pick question is taken here.","dependsOn":"HB-3 (the grade machinery), HB-4 and HB-5 (the loads the closes grade), HB-8 (the doctrine-sheet-coverage and plant-share envelopes). Last in the spine.","ownerGate":"None as chartered — but TWO live tripwires. ⚠ Q1 RE-OPENS if the occupation close converts to a carried road, because that mints a THIRD owner-gated persisted field, occupationRecord.habitEpisode, which §7 rules deferred-and-visible precisely so it can neither inflate Q1 nor vanish from it. ⛔ Converting the route-creation ladder to a WEIGHTED DRAW stays owner-gated and is explicitly NOT taken — a scored bar is a preference, a legality guard is not.","outputMoving":"NO at landing — the route divisor and the charter multiplicand do change chooser behaviour, but every one of them rides habitConditioningEnabled. The DEFER list that survives is published in habitForkRegistry.js itself, which is a repo file a reader can grep, NOT in the volume's §7 rows.","reuse":"HB-3's grade-map machinery and the episode key family; the pledge book already carries every open, so the occupation close needs no foreign schema change; CHARTER_VERDICTS, STATE_LADDER and banditryLoss are all existing frozen vocabularies the closes grade against rather than author.","workerKb":"~4-6 KB into the advance graph across four or five existing host modules plus the envelope instrument. ZERO into the generation worker."}],"firstCar":"Land HB-1's promotion-shape car first: one gated commit over src/domain/worldPulse/habitForkRegistry.js and tests/lint/chooserTotality.walker.test.js that makes a DEFER→LEARN promotion structurally possible, because today it is not. Three defects, all measured at claude/composite-r4 @ a5876c0ea. First, fork() at habitForkRegistry.js:101 spreads the caller's row and then hard-codes `circumstanceClasses: Object.freeze([])` at line 109, so the field always wins as empty and no row can ever declare the classes it learns under — change it to pass a caller-supplied value through, defaulting to the frozen empty array, and keep the existing subset assertion against FORK_CLASS_VOCABULARY (which already aliases CIRCUMSTANCE_CLASSES at line 58) so the widening cannot admit an unknown token. Second, `dyadicActions` returns ZERO hits across src and tests although §3c makes it mandatory on every `per-action` row and rules that a list not derivable from an executed read of the emitter is a build STOP: add the field to fork()'s defaults, populate it on HBF-17 (settlementStrategy, the only per-action row) as ['deploy'], and add the walker arm that derives that list from enumerateMoves' own `move === 'deploy' && bestTargetId != null` conditionals rather than comparing a transcription — the same \"the emitter is the denominator, never a hand list\" discipline the STRATEGY_MOVES totality pin already applies. Third, HB-4's site 3 has no registry row at all: `coalitionRatification.castRatificationBallot` is live at src/domain/worldPulse/coalitionRatification.js:209 and called from envoyRatificationStage.js:224, yet `coalitionRatification` returns zero hits in the registry, and it is not discoverable by any of the four idiom signatures — so add it as a hand-maintained `discovery: 'checklist'` row with disposition DEFER, `arity: 'dyadic'` marked VERIFY-AT-BUILD (whether the term sheet exposes a stable offerer id at the load point is not executed anywhere), a written closeOwed of at least twenty characters, and raise DEFER_CEILING from 36 to 37 in the same commit with the reason recorded at chooserTotality.walker.test.js:105 — the ceiling is exact in both directions, so the commit reds if the number and the row disagree. Carry the arity-uniqueness assertion (`expect(symbolsWithTwoArities).toEqual([])`) and the three J-HB-23 assertions unchanged; they already pass and must keep passing. Land it CHECK-GIT-FIRST: the registry is a live shared file that IN-1c, WF-8a and ENC-4 have each minted rows into (HBF-33, HBF-34, HBF-36..42), so re-read HEAD immediately before staging and stage explicit paths only. Nothing here is output-moving, nothing rides a flag, nothing touches a persisted shape, and no owner line is needed — it is signed under the 14:4x grant. Raise one record debt vetoably at the same sitting rather than silently: the volume's HB-1 ruling to delete the `move === 'fortify' ||` disjunct was re-refuted at build, and src/domain/worldPulse/settlementPolitics.js:621 now records the token as a registered foreign move with two ratified same-seed goldens (1.02 dark / 1.0168 lit) held by tests/lint/strategyMoveVocabulary.walker.test.js's MOVE_TOKEN_BRANCHERS — the code is right and DESIGN_FP_ARCH_HB.md §4's HB-1 charter is stale, so the volume should be corrected rather than the code re-litigated."}}
{"type":"result","key":"v2:46acf5564157d531c9094b302a8b8bfff0d43412b0873e7a72ee09809bf4ece2","agentId":"a8cdffdba57fa0dbf","result":{"family":"INT","summary":"INT — the interior program, 8 waves, 7 of them unlanded. The compiled volume is /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a/docs/DESIGN_FP_ARCH_INT.md (739 lines; §1 substrate, §2 flags, §3 canonical model, §4 the waves, §5 seams, §6 the four chair questions). Family price: 25–38 cars. Three findings move the first pass's rows.\n\n(1) THE BINDING CONSTRAINT IS NOT THE LINE CEILING, IT IS THE LAZY ENGINE CHUNK, AND THE VOLUME NEVER PRICES IT. Every INT touchable has line headroom (warSeatBooks.js 492 eff / 810 raw against 800; settlementPolitics.js 560; relationshipMemory.js 477; npcLadderState.js 605; grievanceRead.js 51; coup.js 139; rulingPowerCoup.js 121; factionPairLedger.js 138; warPoliticalLoop.js 169; commonsVoiceKernel.js 214; deploymentReturn.js 333 — none is in scripts/.size-baseline.json any more). The byte ceilings are the problem. CONFIRMED by static import BFS at the slot: the generation worker (src/workers/generation.worker.js, 145-module static graph, ceiling WORKER_BUNDLE_CEILING_BYTES = 1,404,493 B monotone-down at tests/build/generationWorkerLazy.test.js:78) reaches ZERO worldPulse and ZERO rulingPower modules — INT adds nothing to it. All nine INT touchables ARE on the app graph behind a dynamic import, i.e. in the lazy `engine` chunk, whose assertion is `expect(size).toBeLessThan(676_000)` at tests/build/vendorPdfLazy.test.js:768 against a measured 675,323 B — about 680 B of headroom, and both prior raises (660k→673k, 673k→676k) were OWNER-RATIFIED, never a lane's edit. The INT family wants ~2,200 new effective lines of worldPulse code. It cannot land past wave one or two without an engine-chunk ceiling act. Worse for two waves: rulingPowerCoup.js and stressorsCore.js are in the EAGER first-paint closure (path: src/main.jsx → components/account/OperatorMessagesProvider.jsx → store/index.js → store/settlementSlice.js → domain/crisisLifecycle.js → worldPulse/stressorsCore.js → stressors.js → stressorDynamics.js → domain/rulingPowerCoup.js), whose CLOSURE_BUDGET_BYTES is 1,048,000 against a last-recorded 1,046,778 (vite.config.js) — roughly 1,222 B. INT-3's `decision` pressureKind sibling and INT-4's rally-margin read both land there.\n\n(2) INT-8 IS PART-BUILT, NOT UNBUILT, AND ITS PRICE DROPS. The first pass read \"`INT-8` 0\" and called it unbuilt, but its one named cure-commit (J-INT-9, \"the two cures + the habitat\") has already been paid by other waves. deploymentReturn.js:471's `Hold chance ${verdict.pHold}, roll ${verdict.roll}` is GONE — `grep -rn 'Hold chance' src` returns nothing; the banded `seatGripWordFor(pHold)` lives at :192 and is read at :592. relationshipMemory.js has ZERO `toFixed` hits (S18's second cure). And tests/helpers/proseNumericsWalk.js:230-236 now \"Index[es] `.push(...)` writes by the exact const-array binding they target… closes the common `const parts=[]; parts.push(sentence)` hop\" — the blind spot S18 called persistent is closed.\n\n(3) THREE OF THE VOLUME'S §1 PREMISES HAVE MOVED AND CHANGE PRICES. warSeatBooks.js has generalized itself past R1: SEAT_BOOK_KINDS at :540 is now FOUR kinds including `'foreign'` (R1 says a three-way realm/seat/patron split), PRIVATE_BOOK_KINDS at :551 and seatBooksPartition at :588 are new exports, and the books receipt vocabulary is already live and consumed (warTermination.js:917 `booksInterest`, :918 `booksDirection`; warRulingsNews.js:225/:291; warRulingsEvidence.js:39-40; settlementStrategy.js:521 reads all four keys). Nine src modules import it. INT-6's WOUND_TYPE_RE (grievanceRead.js:48) now carries ELEVEN alternatives including `contest_loss|contest_forestalled`, so the \"seven families partitioning the regex exactly\" must be re-derived, not inherited. And the flag bill's fourth obligation has moved twice: tests/domain/contributionLedgerShape.test.js:119 asserts `toHaveLength(35)` while its own it() title at :49 still says \"at 28\" — a flag-minting wave edits BOTH.\n\nThe flag bill, measured, paid by six of seven waves (INT-2 reuses the live `settlementPoliticsEnabled`, whose row is at subsystemRowsCompact.js:332, and INT-8 has no flag): the manifest key in src/domain/worldPulse/simulationRules.js (35 → 36); the authored certification row + its VIRTUAL_RULES twin + the LANE_LEAVES entry in subsystemRowsVirtual.js and tests/domain/subsystemRowsVirtual.test.js (three module-scope literal edits, bijection asserted at :651); the contributionLedgerShape.test.js pair above; the seven edge-shared bundle MODIFY rows plus `npm run build:edge-shared` (package.json:27); the acceptance file's literal `<flag>: true` drive; and the four-fence dormancy set with its lit-mutant control plus at least one by-name strict read. §92.2 already RULED that subsystemRowsInterior.js is NOT minted (it is absent at the slot, correctly). couplingRegistryInterior.js is also absent — §54.3's X5 is RESOLVED-NOT-PAID and the first cross-layer-reading INT wave pays that mint.\n\nNothing in this family is a push, deploy, migration, deletion or spend, so every wave is chair-signable under the 14:4x grant with two named exceptions: INT-6 mints the program's ONE new persisted key (`worldState.spatialLedgers.burials`), which is a persistence-shape act the owner signs and which #20's trailing OSR mint must swallow; and the engine-chunk ceiling raise, which is a family-level owner act, not a wave's. All seven land dark and move no same-seed output; the family becomes output-moving only at the second declared lighting, which is itself held on sitting item 1's §764.3 amendment.","waves":[{"wave":"INT-1 — the books generalized (flag `seatBooksEnabled`)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Zero deliverables exist: `grep seatBooksEnabled` returns ONE hit and it is a comment in strategicPosture.js:36 saying the books are unbuilt; seatBooksReceipt.js is absent. But this is a generalization over a live, pinned, 492-effective-line module, so the code half is small — the volume budgets +80 lines on warSeatBooks.js and a ≤200-line leaf. The cars are the bills, not the build. Car 1: the flag bill (six named paths above) plus the `booksStandingOf` band projection as new exports only, plus the four-fence dormancy set with the lit-mutant control. Car 2: seatBooksReceipt.js — the banded standing receipt, the agreement deadband and the change-gate memory read, mounted on the EXISTING booksInterest/booksDirection receipt lane rather than on warTermination (818, frozen) or the pulse mouths. Car 3: the ONE-EVALUATOR census — a comment-stripped source scan proving no second module computes a settlement/seat weight split, with an executed third-evaluator plant as its mutant — plus the consumer census (now NINE importers, not the volume's one or two) and the four reserved-seam tripwires of §5 rows 6-9. Car 4: the dossier line, which the volume marks VERIFY-AT-BUILD, so a UI car with its own round-trip pin. Car 5 is the contingency: if the receipt reads cross-layer, this wave pays couplingRegistryInterior.js's mint (§5 row 12's five-part act in the same commit), and it is the wave most likely to be the one that meets the engine-chunk ceiling first.","dependsOn":"No unbuilt predecessor. Rests on landings, not promises: R1 (§1b) — warSeatBooks.js exists as WR-5's module; SP-4 landed, so §5 seam 9's STOP is discharged and the pin is re-specced by symbol (seatBooksReceipt.js/warSeatBooks.js import NOTHING from strategicPosture.js; the landed reads are courtPostureOf/courtRiskAppetiteOf per CR-C4-1). Must not disturb warConvergenceContract's WAR_TERMINATION_DECIDING_TERM_KEYS (§5 seam 2) and must count sovereigntyIntent.js from day one (§5 seam 1). First in the family's build order (§4 Build order).","ownerGate":"None — chair-signed under the 14:4x grant. Not a push, deploy, migration, deletion or spend. The naming JUDGMENT (warSeatBooks.js IS the volume's seatBooks.js; no rename, no recombining facade) is vetoable and already recorded in §4.","outputMoving":"NO at landing — dark behind `seatBooksEnabled`, four-fence dormancy set, zero receipts and byte-identical goldens with the flag absent. YES only at the second declared lighting, which is held on sitting item 1.","reuse":"Very high, and higher than the volume knew. warSeatBooks.js (810 raw / 492 eff) already exports readWarSeatBooks (:618), authoritySignatureFor (:379), SEAT_BOOK_KINDS (:540, now FOUR kinds including 'foreign'), PRIVATE_BOOK_KINDS (:551) and seatBooksPartition (:588). The receipt VOCABULARY is already live and read by four modules: warTermination.js:917-918 writes booksInterest/booksDirection, settlementStrategy.js:521 lists booksInterest/booksDirection/booksReason/booksPublicReason, warRulingsNews.js:225/:291 and warRulingsEvidence.js:39-40 consume them. Both WR-5 divergence pin shapes exist war-side and are to be verified and reused, never duplicated. The census tripwire has a landed exemplar to copy in shape (§5 seam 3: sovereigntyBundle.js + tests/domain/sovereigntyBundleWr10.test.js).","workerKb":"ZERO into the generation worker — CONFIRMED: its 145-module static graph reaches no worldPulse or rulingPower module. ~280 new effective lines ≈ 10-12 KB source, roughly 4-6 KB minified, and it lands in the LAZY `engine` chunk (measured 675,323 B against `< 676_000` at tests/build/vendorPdfLazy.test.js:768 — about 680 B spare). This wave alone plausibly reds that ceiling. Nothing here touches the eager first-paint closure. It CAN live lazily behind its flag only if the receipt leaf is not statically imported by a module the engine chunk already pulls; otherwise the ceiling act is this wave's, not a later one's."},{"wave":"INT-2 — the positions wired (flag `settlementPoliticsEnabled`, exists dark)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"The flag and the bloc substrate are BUILT, but every deliverable of THIS wave is absent: blocCounsel.js and blocCounselNews.js do not exist, and `grep -n counsel src/domain/worldPulse/settlementPolitics.js` returns nothing. The cheapest of the flag-bearing waves because there is no flag bill — `settlementPoliticsEnabled` is already a manifest member and already carries an authored certification row (subsystemRowsCompact.js:332, with a 400+ character aliveness paragraph and a dormancy golden at tests/property/settlementPoliticsDormancyGolden.test.js). Car 1: blocCounsel.js (≤250) — the counsel margin read, the receipt mint and override detection — plus the same-flag four-fence completion, which must pin BOTH compositions dark AND both preset negatives (C-INTF-5 CONFIRMED at the slot: factionCompetitionEnabled: false appears TWICE, simulationRules.js:743 and :833; the volume's :477/:517 and the census's :345 have all rotted). Car 2: blocCounselNews.js (≤150) — the L6 five-join news-plane landing with WHAT_PHRASES, heraldRouting and its OWN walker file. Car 3: the pin battery — the property-shaped CLAMP negative over generated bloc states, the quiet-town negative seeded non-empty FIRST per the vacuity law, the override-handoff exactly-one entry pinned against mintFactionPairIncident's own dedup, the survival-bloc dissolution honesty pin, the JSON-alias round-trip (factions[].members[] ARE npcs[]), and the faction-card FOREIGN-COUNSEL dossier round-trip, which is VERIFY-AT-BUILD and may push this to a fourth car.","dependsOn":"INT-1 first, by §4's build order. The real coupling is the MINT SITE: INT-2's override handoff writes through mintFactionPairIncident (factionPairLedger.js:149) using INT-3's `*_decision` sibling type — the entry mints HERE and is consumed THERE, so both waves touch the same caller list and the volume resolves it by build order. SP-6's significance class is ABSENT, so the class assignment ships as a named constant with a TODO-tripwire pin that reds when the significance family lands without it registered. Collision: settlementStrategy.js is now 760 effective against the 800 layer ceiling with its baseline entry DELETED — only 40 lines of slack — so extending the move vocabulary there needs a leaf, not an inline edit.","ownerGate":"None — chair-signed. J-INT-12's law (the vocabulary never leads the events: per-verb loading rows are added only as each program's verbs land) is the chair's to hold, not the owner's.","outputMoving":"NO at landing — the flag is already dark and the subsystem is certified STRUCTURALLY INVISIBLE TO EVERY RECEIPT SHAPE THE ESTATE WRITES (subsystemRowsCompact.js:353). The declared degraded arm is the honest catch: with factionCompetitionEnabled false there are zero blocs and zero counsel while INT-3's re-read, demand and émigré arms still function, and the dormancy golden cannot see that — it is pinned separately.","reuse":"High. settlementPolitics.js (1,076 raw / 560 eff, 240 lines of headroom) already carries DECISION_LOAD_SPAN 0.3 (:145), leaderTiePosture (:333), coalitionConsolidation01 (:604), blocDecisionFactor (:628) and the clamp at :648-649; the chooser consumption is live in settlementStrategy. The dormancy golden, the certification row and the manifest membership are all paid. The news-plane machinery (WHAT_PHRASES, heraldRouting) is landed per S24.","workerKb":"ZERO into the generation worker (settlementPolitics.js is absent from its static graph; it is reached only from the advanceInterval worker and the app's lazy graph). ~400 new effective lines ≈ 14-18 KB source, roughly 6-8 KB minified into the lazy `engine` chunk against ~680 B of headroom. Not eager — nothing here is in the first-paint closure. The news leaf is the natural lazy-chunk candidate if a split is needed, since it is display-plane and reached only when a receipt is composed."},{"wave":"INT-3 — the interior veto completes (flag `interiorVetoEnabled`; INT-3b landed, 3a unbuilt)","status":"PART-BUILT","carsLow":4,"carsHigh":6,"reason":"INT-3b IS LANDED and the code reader confirms it against the first pass's own row: src/domain/worldPulse/emigreErrand.js exists (122 raw / 21 eff — the body is nearly all docblock), exports emigreErrandFor, is registered in envoyErrandVocabulary.js:360's ERRAND_CONSUMERS, is pinned by an eight-case acceptance matrix at tests/domain/emigreErrand.test.js and takes the exact-path INTERIOR layer home at tests/lint/couplingInclusion.walker.test.js:349. Its zero src callers are the wave's IDENTITY CLAIM, not an omission — 'Dormancy here is STRUCTURAL, not gated: with no caller, no seed, flag, preset or lifecycle path can reach this module at all.' The first pass's `:7 built:false` reads a stale registry sentence; the registry row is live. INT-3a is wholly unbuilt (`interiorVetoEnabled` zero hits, decisionJoinReceipt.js absent) and is the family's hardest wave. Car 1: the flag bill plus decisionPolitics.js as a sibling leaf (the volume's own JUDGMENT — keep warPoliticalLoop war-only and its WR-5 pins untouched) minting the five sibling incident types through mintFactionPairIncident, each only when its producing event exists. Car 2: THE JOIN RECEIPT (decisionJoinReceipt.js ≤180) — still absent per R4 and the wave's core deliverable — with its hardest pin, the JOIN NEGATIVE: a coup from pure legitimacy collapse with no live decision incident names no decision, seeded, with a positive control that seeds a live entry and proves the naming. Car 3: the `decision` pressureKind sibling, which is where the eager-closure cost lands. Car 4: the PACT re-read trigger, shipped behind the flag with the writer call dormant and a STOP-and-report if tempted to mint pact state. Car 5: the reversal-defusal, installed-successor-honour, bidirectional-re-read and ANDed-gate reachability pins on real generated corpora, plus the memoryWeave-composition pin. Car 6: the two pre-pins toward unbuilt neighbours (GRAMMAR's oath-holder clause dormant-but-shaped, FP-INFO's succession trigger consumed dormant).","dependsOn":"INT-2 first (shared mint site; build order resolves). Consumes WR-5's landed halves and extends them by SIBLING only, never by editing the war rows' type or shape (§5 seam 5): warPoliticalLoop.js:90 applyWarDecisionPolitics deposits `war_decision` with context.{decisionId, actualAction, desiredAction}; warPeaceDecision.js honours inheritedDemand; applyWorldPulse.js:943 carries carriedDemand; factionPairLedger.js:92 selectWarDecisionIncident is the read-back to generalize (≤ +40). R7's `memoryWeaveEnabled` is a REQUIRED conjunct of the landed grievance lane — a dormancy proof that ignores it proves the wrong conjunction. Pre-pins toward GRAMMAR (§5 seam 10) and FP-INFO (§5 seam 11, whose writer INFO owns). Q2 (sibling types vs one unified `decision` type) is a recorded recommendation the chair signs, not an open owner gate.","ownerGate":"None — chair-signed. Q2's ruling (SIBLING TYPES, because the landed rows are persisted state under THE PROMISE and a re-type buys only tidiness) is the chair's. One real STOP the wave must honour rather than decide: minting pact state from the interior, which belongs to SP-3.","outputMoving":"NO at landing — dark behind `interiorVetoEnabled`, with the memoryWeave-dark arm pinned to be exactly as dark as WR-5's so no new leak path opens. YES at the second lighting.","reuse":"Very high on the war half, which is the template: R3 and R4 record that the deposit lane, the inherited-demand override and the carriage through applyWorldPulse are all BUILT and pinned. R5 settles the home — worldState.factionPairStates[...].incidents[] through the ONE writer mintFactionPairIncident (factionPairLedger.js:149), four-flag gated, so J-INT-2's no-new-ledger spirit is satisfied by the landed lane and re-homing would mint the second memory plane it vetoes. Persist, undo and import paths for those entries are already pinned by WR-5's wave — verify, do not re-pin. FACTION_PAIR_TUNING (:43) and decayFactionPairStates (:197) exist.","workerKb":"ZERO into the generation worker. ~340 new effective lines ≈ 12-15 KB source, roughly 5-7 KB minified into the lazy `engine` chunk (~680 B spare). ⛔ AND THIS WAVE HAS AN EAGER SLICE THE VOLUME DOES NOT PRICE: the `decision` pressureKind sibling lands in stressorsCore.js (the coup_detat entry with `pressureKinds: ['legitimacy']` is at :236-239) and the join-margin receipt in rulingPowerCoup.js — BOTH are in the first-paint eager closure by the static path traced above, where the budget is CLOSURE_BUDGET_BYTES 1,048,000 against a last-recorded 1,046,778, about 1,222 B. Thirty effective lines there is roughly 0.6-1.2 KB minified, i.e. plausibly the whole remaining margin. The composer leaves must NOT be statically imported by rulingPowerCoup.js or they go eager with it; applyWorldPulse.js is frozen at 907 so the pressureKind mounts through the stressor lane's existing composition, never a new mouth edit."},{"wave":"INT-4 — the narrated middle (flag `strainAttributionEnabled`)","status":"UNBUILT","carsLow":3,"carsHigh":4,"reason":"Zero hits for `strainAttributionEnabled`; strainAttribution.js and strainAttributionNews.js are absent. Premise verified unchanged at the slot: stressorsCore.js:236-239 still makes coup_detat one-strain by construction, so this wave is ATTRIBUTION, NOT AMPLIFICATION — it adds WORDS, never weight, and the DOUBLE-COUNT guard is property-shaped with both flags lit. Car 1: the flag bill plus strainAttribution.js (≤250) — the co-presence read over live state at the pressure crossing (treaty installment streams, exhaustion scar, famine and occupation conditions, the corruption plane) on the closed burden vocabulary {tribute_strain, war_exhaustion, famine, occupation, corruption}, banded, band words only per L5. Car 2: strainAttributionNews.js (≤150) with the L6 five joins and its own walker, plus the commonsVoiceKernel treaty-burden term feeding the EXISTING composite through the existing writer — that file is live-lit, so the fence golden comes FIRST, and the term's reader resolves the treaty's own cadence marker (52-week current, legacy 12) rather than re-pricing it. Car 3: the pin battery — the CO-PRESENCE negative with its positive control (both states seeded), the CAUSAL-CLAUSE negative (only a matched LEGITIMACY_ARCHETYPES condition ever words as CAUSE, and corruption_exposed is the only §5 burden in that set), the dividend pin, the rally-margin negative, the DOUBLE-COUNT guard, the degraded-arm validity pin for economicCoupReadEnabled dark, and the no-decimal RUNTIME pin on composed output, which a source scan cannot see. Car 4: the town-page strain line, which the volume marks as NEW UI WORK owned by this wave with the power-structure card family as fallback and TreatyPanel.jsx as the cheap secondary landing.","dependsOn":"INT-3a first by build order, though the volume sanctions running INT-5 in parallel with this one by path — the only parallelism it allows. Its degraded arm is declared, not blocking: `economicCoupReadEnabled` exists VIRTUAL-dark (five hits at the slot) and is registered for the owner's lighting queue with NO code change owed by this wave; dark, fewer burdens are named and the receipts stay honest. Collision with INT-7 is by design and must be kept: INT-4 receipts at the STRESSOR crossing, INT-7 at the legitimacy BAND crossing — two distinct kinds, both pacing-registered, walker files separate per L6.","ownerGate":"None — chair-signed. J-INT-3 (no tribute→legitimacy write) is settled doctrine, re-verified at the slot, not a live question.","outputMoving":"NO at landing — dark behind `strainAttributionEnabled`. The one thing to watch is that the commonsVoiceKernel term feeds a LIVE composite, so the fence golden before the edit is what proves the dark arm byte-identical, not the dormancy golden after it.","reuse":"Good. commonsVoiceKernel.js (366 raw / 214 eff) carries the rung ladder, LEGIT_FLOOR and the existing composite writer; pressureModel's TRADE_ARCHETYPES and LEGITIMACY_ARCHETYPES partition is intact (S11); rulingPowerCoup.js's generic fallback stays the sub-margin arm so only a ≤+30 margin read is owed; TreatyPanel.jsx exists as a verified secondary landing (S23). The news-plane five-join pattern is landed and copied, not invented.","workerKb":"ZERO into the generation worker. ~430 new effective lines ≈ 15-19 KB source, roughly 6-8 KB minified into the lazy `engine` chunk (~680 B spare). ⛔ Like INT-3, it has an EAGER slice: the rally-receipt margin read sits in rulingPowerCoup.js, inside the first-paint closure (~1,222 B of margin). Keep it to the ≤+30 lines the volume budgets and do not let strainAttribution.js become a static import of that file, or the whole 250-line leaf goes eager. Everything else is comfortably lazy behind its flag."},{"wave":"INT-5 — the memory seam + the founding wound (flag `memoryHorizonSeamEnabled`)","status":"UNBUILT","carsLow":2,"carsHigh":3,"reason":"Zero hits for `memoryHorizonSeamEnabled`; foundingWound.js absent; `grep originHolder` still returns nothing, so S12's claim that INT-5's field is genuinely new holds. This is the family's CHEAPEST engine wave because the seam was pre-built for it: relationshipMemory.js exports RELATIONSHIP_MEMORY_HALF_LIFE_TICKS (:14) and RELATIONSHIP_MEMORY_MAX_LOOKBACK_TICKS (:15) and already THREADS both as options at :118-119, with the hard-zero at :129 before the half-life at :130 — the volume's ≤+40 lines is realistic. Car 1: the flag bill plus the seam itself, threading {halfLifeTicks, maxLookbackTicks} × memoryHorizonMultiplierOf through collectMemories' call sites, with the dark byte-identity golden taken FIRST because relationshipMemory is a LIT-PATH module (J-INT-6), plus the originHolderId stamp on npcLadderState. Car 2: foundingWound.js (≤180) — foundingWoundOf(pairKey) and grudgeLineageOf over stored {type, tick} rows. Car 3: the proof battery, which is the real cost — the BOTH-SIGNS × BOTH-CONSTANTS anti-ratchet pin (the band must lengthen bright rows exactly as it lengthens wounds, on the half-life AND the lookback, property-shaped over the weight function), THE FORTY-YEAR pin EXECUTED on a real generated corpus under an undying band, the fleeting negative, the asymmetric-pair fixture, chain-immutability across a second succession, the JSON round-trip with regen/undo/import per §3's lifecycle clause, and the memoryWeave composition pin. A tight implementer folds car 3 into cars 1 and 2; two is the floor, three the honest expectation.","dependsOn":"Runs in PARALLEL with INT-4 by path — the only parallelism §4's build order sanctions. Must land BEFORE INT-6, because INT-6's dig-up age pricing reads the seam-corrected clock, and the lighting order is memoryHorizonSeamEnabled before deliberateForgivenessEnabled. INT-8 consumes foundingWoundOf, so INT-5 precedes it. The revanchism consumers are memoryWeave-gated (R7), so the seam lit with the weave dark must move NOTHING the weave gates — that is a required pin, not a nicety.","ownerGate":"None — chair-signed. `originHolderId` is a field addition on an existing record, drop-when-absent and never null (T4), not a new persisted key, so it does not reach the persistence-shape gate INT-6 does. Req 13 is declared-empty WITH reason (a memory-clock law touches no alignment surface) — the declaration is the obligation, and it is already written.","outputMoving":"NO at landing — dark behind `memoryHorizonSeamEnabled`. ⚠ This is the wave where a mistake would be output-moving on a LIT path, because relationshipMemory sits in live code: the seam SCALES CONSTANTS ONLY and introduces no new transcendental (the existing Math.pow at :128 is pre-existing), and the dark byte-identity golden taken before the first edit is what proves it.","reuse":"The highest reuse-to-work ratio in the family. Both constants are exported AND already threadable as options — the module's own header documents the deferral and names the wiring point (the D5 SEAM comment). The band multiplier the seam adopts already exists: relationshipEvolution.js#memoryHorizonMultiplierOf. The stamp sites are two and are found by symbol: `inherited: true` at npcLadderState.js:566 and :583 — ⚠ the volume's own :555/:572 have rotted by eleven lines, and its warning that the drafted :264-266/:306/:386 are DECAY-CARRY sites mislabeled as inheritance sites is correct and still load-bearing.","workerKb":"ZERO into the generation worker (relationshipMemory.js and npcLadderState.js are both absent from its static graph, present on the app's lazy graph). ~235 new effective lines ≈ 8-10 KB source, roughly 3-4 KB minified into the lazy `engine` chunk (~680 B spare) — the smallest byte footprint of the seven, and the only one that might squeeze under the ceiling as it stands. Nothing eager. foundingWound.js is a clean lazy leaf behind the flag."},{"wave":"INT-6 — deliberate forgiveness (flag `deliberateForgivenessEnabled`)","status":"UNBUILT","carsLow":5,"carsHigh":7,"reason":"Zero hits for `deliberateForgivenessEnabled`; burialLedger.js absent. The family's largest wave, and the only one minting persisted state. The volume already splits it into two slices, and each is more than one car. Slice 1 (Car 1): woundFamilyOf(incidentType) as a single-writer typed classifier replacing WOUND_TYPE_RE at its call sites, with a behaviour-identical golden pinned in BOTH directions and a walker asserting TOTALITY over the incident-type inventory so a new type with no family reds. ⛔ RE-DERIVE, DO NOT INHERIT: grievanceRead.js:48's regex now reads /war|betray|tribute|conquest|occupation|sack|raid|seiz|impos|contest_loss|contest_forestalled/i — ELEVEN alternatives, two of them (contest_loss, contest_forestalled) added since the census, so the 'seven families partitioning the regex exactly' is a stale count and the partition must be re-measured before the classifier is authored. Slice 2 is cars 2-5: burialLedger.js (≤300, the ONE writer through which decree, dig-up and lapse all close); grievanceRead.js's two J-INT-14 arms (≤+50 — revanchism family-scoped EXACT suppression over typed rows, grievance a BANDED RECEIPTED DISCOUNT that is never zero), which must sit INSIDE the weave-gated read path or dark-weave worlds leak the discount; the DM verb arm through realmManifest plus approval routing as the REPUDIATE_TREATY twin (J-INT-5's both-arms law); and the autonomous plan-lane arm priced by INT-1's books read, with the domestic dividend and hold-dwell verdict. Cars 6-7 are the lifecycle and pin batteries, which are the heaviest in the family: the SUPPRESSION-NOT-DELETION two-arm pin (held ⇒ covered reads 0 WITH receipt AND every incident row byte-identical; dug up ⇒ reads RESUME plus a new betrayal wound), the unilateral-burial negative seeded counterpart-non-empty FIRST, the family-scope negative on a two-family fixture, the DOMESTIC VERDICT pin, the price-executes gate (insufficient stores binds NOTHING, receipted), the age-scaled dig-up price on INT-5's corrected clock, burial-gate reachability on real corpora, the writer/reader spelling pin and the v5 absence-is-evidence census join.","dependsOn":"INT-5 first (dig-up pricing reads the seam-corrected clock; the lighting order is explicit). INT-1 first (the autonomous arm is priced by the books read). INT-3 first (the `burial_decision` sibling mints through its lane). R7 binds hard here: the suppression must live inside the memoryWeave-gated read path. ⚠ Naming collision, verified: WR-6 landed a coalition-settlement action `'forgiveness'` with status `'forgiven'` (relationshipState.js:205-211) — DEBT forgiveness between partners, not a grudge burial — so INT-6's vocabulary uses burial/buried/dug_up EXCLUSIVELY and the word `forgiveness` never enters an INT-6 type, status or id, the flag name being the sole owner-authored exception. That collision-free spelling is load-bearing for greps and walkers.","ownerGate":"⭐ THE ONE REAL GATE IN THE FAMILY. This wave mints the program's ONLY new persisted key — worldState.spatialLedgers.burials, rows of {id, pairKey, woundFamily, decreedTick, decreedBy, price:{receiptId}, state} with woundFamily a closed seven-family enum and state held|dug_up|lapsed. Persistence shape is owner-gated by the estate's own list, so the shape is signed before the writer builds even though this is no migration (nothing applies). Two consequences to put on the sheet with it: #20's ONE trailing OSR mint must swallow this shape along with #32's batch, and spatialLedgers is a heavily-read live container (AdvanceReport.jsx, TravelersLayer.jsx, PlacementsLayer.jsx, RealmInspector.jsx, CauseWalkPanel.jsx, RumorsTab.jsx, RelationshipsTab.jsx, SteadingsSection.jsx, generalDeskRead.js, spatialSubstrateDerive.js all read it), so drop-when-empty at every level is what keeps an absent ledger byte-identical.","outputMoving":"NO at landing — dark behind `deliberateForgivenessEnabled`, and structurally dark besides: with no burial rows, drop-when-empty means the container never materializes and a world serializes byte-identically to the pre-burial engine (S25's law). The full six-path lifecycle clause lands BEFORE the writer builds: create on the EXECUTED price package only (a decree whose package fails binds NOTHING, receipted), read through grievanceRead suppression and the dossier neighbour-card line, persist through spatialLedgers serialization with a JSON round-trip pin, regen under THE PROMISE (held burials survive with the pair; a regen that removes a pair LAPSES its burials through the writer as `lapsed_with_the_pair` and never dangles), undo byte-exact with worldState, and import validating woundFamily against the closed enum so an unknown family drops to absent with a load receipt rather than becoming a free string.","reuse":"Moderate. grievanceRead.js is tiny (131 raw / 51 eff) with enormous line headroom and holds the only typing in the tree, so the classifier has exactly one habitat to occupy. The realm verb machinery exists and REPUDIATE_TREATY is the named twin to copy (realmManifest.js, realmVerbExecution.js). The price package reuses generosityReactions. The reframe lane stays UNTOUCHED — the organic lane and the deliberate one are separate by ruling. But the ledger, the writer, the classifier, both suppression arms and both verb arms are all new code.","workerKb":"ZERO into the generation worker. ~400+ new effective lines ≈ 14-18 KB source, roughly 6-8 KB minified into the lazy `engine` chunk (~680 B spare). Nothing eager — grievanceRead.js, relationshipState.js and the burial ledger are all lazy-graph. The DM verb arm reaches realmManifest, which is also lazy. This wave is a clean lazy-chunk citizen; its problem is the ceiling's arithmetic, not its placement."},{"wave":"INT-7 — legitimacy's row + the interior envelopes (flag `legitimacyCrossingsEnabled`)","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"Zero hits for `legitimacyCrossingsEnabled`; legitimacyCrossings.js absent; S21's gap is real and re-verified — `strategy_legitimacy` at subsystemRowsBaseline.js:94 is the ONLY hit in src and there is no standalone legitimacy rule row. The code half is small; the INSTRUMENT half is the price. Car 1: the flag bill plus legitimacyCrossings.js (≤200) — a detector reading the stock AFTER the existing writers, hysteresis on the existing band edges, one receipt per crossing, the cause-typed reseed case, and NEVER a second legitimacy writer. Car 2: the certification row in the subsystemRowsVirtual lane (⛔ subsystemRowsInterior.js is SUPERSEDED by §92.2/C-INTF-1 and is correctly absent at the slot) carrying the crossing-has-a-hit invariant, pointed at the typed-hit vocabulary from S1's writer census. Cars 3-5 are the envelopes, and they are why the range is wide: C-INTF-6's envelope-registration law means an INT envelope cannot ship a literal floor — the drafted 0.005/settlement-year is exactly what the landed machinery forbids. Each envelope must REGISTER an entry in tests/fixtures/distribution-envelopes.manifest.json with the full schema (id, file, n, baseRate, baseMeasuredAt, baseMeasurementN >= 400, direction, alpha, bound, margin, measurementContext, loosenPending, notes) AND join the frozen shrink-only totality roster in tests/lint/distributionEnvelopePower.test.js, which RE-DERIVES every entry from its measured base rate and reds if bound, margin or provenance does not hold. The manifest already holds 26 such entries, so the pattern is landed and copyable — but measuring base rates at n≥400 on generated corpora, for a TOTAL over all 20 declared endings tokens by family plus a TWO-PART tempo envelope, each with its own executed mutant negative control, is soak-corpus work priced in runs, not in lines.","dependsOn":"INT-4 first, and the separation from it must be kept: INT-4 receipts at the STRESSOR crossing, INT-7 at the legitimacy BAND crossing — two distinct kinds, both pacing-registered, separate walker files per L6. WR-9 has LANDED since the volume (9a repaired, 9c/9d closed), so the ADOPTION arm of the envelope-harness coordination note applies, not the standalone arm: VERIFY the WR-9 harness location at build and adopt its pattern rather than minting a second one. ⚠ Census trap the volume already names: subsystemRowsGrowth.js:59 carries the literal 'legitimacy_pressure' as an EMERGENT_EVENT_TYPES member, NOT a rule row — exclude it from any INT-7 census grep or the gap will read as already closed. The new lane must join the registry and the partition walker in the SAME commit.","ownerGate":"None — chair-signed. The envelope harness boots on a real soak corpus and the owner-ordered soak is the PROGRAM's acceptance gate, not this wave's: nothing in INT-7 runs one, and saying so is part of the wave's own declaration.","outputMoving":"NO at landing — dark behind `legitimacyCrossingsEnabled`, reading the stock after existing writers and writing none of its own. Req 13 is declared-empty with reason (an instrument bench).","reuse":"High on the instrument side, low on the content side. The envelope machinery is fully landed: the manifest, readEnvelope at tests/helpers/distributionEnvelope.js, the re-derivation power test, the loosenPending discipline (a derived bound looser than the live one keeps the live bound and goes to the owner queue — never loosen silently), and 26 worked entries to copy. The certification lane and the partition walker exist. The legitimacy stock, its band table (rulingPower.js's Endorsed ≥75 ×1.30 to Crisis <30 ×0.60), its reseed-on-transfer and its typed-hit writers are all live and stay the total writer set.","workerKb":"ZERO into the generation worker. ~200 new effective lines ≈ 7-9 KB source, roughly 3 KB minified into the lazy `engine` chunk (~680 B spare) — the envelope registrations are test-side and cost ZERO runtime bytes, which makes this the second-lightest wave on the byte ledger despite its car count. rulingPower.js IS eager (first-paint closure, via the same store→stressors chain), so the detector must be its OWN leaf reading the stock and must not be statically imported by rulingPower.js or rulingPowerSeat.js, or its 200 lines go eager against ~1,222 B of margin."},{"wave":"INT-8 — the interior voice (no flag of its own)","status":"PART-BUILT","carsLow":2,"carsHigh":4,"reason":"⭐ CORRECTS THE FIRST PASS, which read `INT-8` 0 and called it UNBUILT. Its one named commit — J-INT-9's 'two cures + the habitat' — has already been paid by other waves, and the code says so three ways. S17's defect is CURED: `grep -rn 'Hold chance' src` returns NOTHING, deploymentReturn.js's pHold/roll interpolation is gone, and the banded word function seatGripWordFor lives at :192 and is read in prose at :592 ('a grip that had looked ${seatGripWordFor(verdict.pHold)} did not survive its arrival') — dice in metadata, words in prose, exactly as specced. S18's second cure is CURED: relationshipMemory.js has ZERO toFixed hits. And the HABITAT is closed: tests/helpers/proseNumericsWalk.js:230-236 now reads 'Index `.push(...)` writes by the exact const-array binding they target. This closes the common `const parts=[]; parts.push(sentence); reason: parts` hop' — the push-indirection blind spot S18 called persistent no longer exists. What REMAINS is pool authoring only. Car 1: the NEW-KIND pools that ride their kinds' flags — books-standing, counsel, crossings, burial family, attribution, join receipts — on the landed warReceiptPools.js shape. Car 2: determinism and identity — same seed same sentence, per-entity keys `int8.<kind>.<entityId>`, draw-accounted, id-carry on every new push site, and the dossier/Herald round-trip proving every sentence's id walks back to its ledger row. Cars 3-4 are the L6 five-joins, since EACH kind completes them with its OWN walker file and there are six kinds — that is the cost driver, and it is what stretches this to four.","dependsOn":"Last in the build order, because every kind it voices must exist first: books-standing from INT-1, counsel from INT-2, join receipts from INT-3a, attribution from INT-4, foundingWoundOf from INT-5 (an explicit build-order collision in §4), burial family from INT-6, crossings from INT-7. The two cures it was to perform are no longer dependencies at all. ⚠ The prose ratchet baselines are SHARED surfaces under the receipt-vacuity and shared-ratchet laws — re-measure at the publishing commit and never inherit a per-file claim; and since the two debts this wave was to follow down are already down, verify at charter whether their rows have already been deleted rather than budgeting a shrink that has happened.","ownerGate":"Split, and the blocking half is already answered. NEW-KIND pools ride their kinds' flags and are chair-signed — build freely, dark. LIT-KIND upgrades (the coup 2-template inventory, the faction verb table, investiture) are J-INT-13 OWNER-GATED and proceed only against a ruling recorded in FABLE_VALIDATION_QUEUE.md BEFORE the wave starts — but that ruling EXISTS: the first pass records DESIGN_FP_ARCHITECTURE.md:2933 CR-FP-5 as RULED ('ACCEPT — pre-record the DARK arm now … the owner may later take the re-record arm'), which is Q3's recommended default. So INT-8 is NOT gated, and the first pass's 'chair-gated on Q4' note is correctly already annotated as not an open gate. Confirm the queue row is live at charter rather than assuming it.","outputMoving":"NO at landing for the new-kind pools — each rides a dark flag, so with the flags absent no sentence is composed and every existing golden is byte-identical. The lit-kind arm is the one that could move output, and the pre-recorded DARK arm keeps it from doing so: lit-kind pools ship behind a prose-version flag and light with the batch. Req 14 is the program's ONE real edit-verb story and the pin that discharges it is structural: pool prose lands ONLY on machine-owned receipt surfaces, and a walker asserts the write-target set so DM-editable fields (hooks, dossier free text) are never written.","reuse":"Higher than the volume assumed, on both halves. The two cures and the walker extension are DONE (receipts above) — that is most of a car the volume budgeted and this wave no longer owes. The pool shape is landed and large: src/domain/worldPulse/warReceiptPools.js is 767 lines / 54,251 bytes of war-grade pools to copy in shape. WHAT_PHRASES (display/settlementRumors.js:116, with the 63-kind canonical arm) and realm/heraldRouting.js exist per S24. The L6 five-join idiom and the per-kind walker-file convention are both landed patterns.","workerKb":"ZERO into the generation worker — but ⛔ THIS IS THE FAMILY'S REAL BYTE PROBLEM AND IT DWARFS THE OTHER SIX. warReceiptPools.js is 54 KB of source for the war kinds alone. Six new kinds authored at even a fifth of that grain is ~30-35 KB of source, roughly 12-20 KB minified, into a lazy `engine` chunk with about 680 B of headroom — twenty to thirty times over. Pool prose is also compression-hostile in a way code is not, so the gzip and Brotli closure figures deserve a look too. The answer is structural, not a ceiling raise: pool tables are pure data behind a kind's flag with no eager reader, which makes them the single best lazy-chunk candidate in the family — author each kind's pool as its own leaf reached by dynamic import at composition time, the way the living-content roster already splits out of the generation worker at 55,465 B. Price that split INTO the wave at charter rather than discovering it at the build gate."}],"firstCar":"INT-1 CAR 1 — THE FLAG AND THE GENERALIZED READ, IN ONE COMMIT. Mint `seatBooksEnabled` and land the generalized books-standing projection on the live evaluator, together, because the estate's WW-A precedent is that a flag joins the manifest in the SAME commit as its first real gate read and its certification row. Work at the build slot /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-headroom (claude/composite-r4 @ a5876c0ea), never on the ledger. Start by re-measuring, not by reading the compiled volume's numbers: `git log --oneline -5 -- src/domain/worldPulse/warSeatBooks.js` before the first edit, then re-derive the manifest count (it is 35 today at src/domain/worldPulse/simulationRules.js, not the volume's 20) and re-read warSeatBooks.js's export surface, which has grown past what §1b R1 describes — SEAT_BOOK_KINDS at :540 now carries FOUR kinds including `'foreign'`, and PRIVATE_BOOK_KINDS (:551) and seatBooksPartition (:588) did not exist when the volume was compiled. Add `booksStandingOf` to warSeatBooks.js as a NEW EXPORT ONLY, a pure banded projection over the weights readWarSeatBooks (:618) already returns; do not rename the module, do not add a shape-adapting facade (a facade that recombines weights is a second evaluator wearing a shim's clothing, and a rename breaks filename-anchored war pins), and stay well inside the file's real headroom — 492 effective lines against the 800 domain layer ceiling, with no baseline entry. The gate read is by name and strict (`=== true`), placed where the projection is consumed rather than buried in a frozen-list `.every()`, because a read only through `.every()` is invisible to the gate walker. Then pay the flag bill in the same commit, all six paths, each verified against the tree rather than the volume: the key in ENGINE_GATED_VIRTUAL_RULE_KEYS (35 → 36); the authored certification row plus its VIRTUAL_RULES twin plus the LANE_LEAVES entry in src/domain/certification/subsystemRowsVirtual.js and tests/domain/subsystemRowsVirtual.test.js, whose bijection is asserted at :651 — and NOT a new subsystemRowsInterior.js, which §92.2 ruled out and which is correctly absent; the fourth obligation at tests/domain/contributionLedgerShape.test.js, which is TWO literals, the `toHaveLength(35)` at :119 and the stale \"at 28\" in the it() title at :49; the seven edge-shared bundle MODIFY rows with `npm run build:edge-shared` (package.json:27); and the literal `seatBooksEnabled: true` drive in the acceptance file. Close with the four-fence dormancy set and its lit-mutant control — own-footprint golden, absent-vs-false differential, call-path spy, gate-polarity census — proving that with the flag absent there are zero receipts, zero new news kinds and byte-identical goldens. Two things this car must MEASURE and report rather than assume, because they decide whether the rest of the family is schedulable: run the build and read `dist/assets/engine-*.js` against the `< 676_000` assertion at tests/build/vendorPdfLazy.test.js:768 (it measured 675,323 B, leaving roughly 680 B, and both prior raises of that ceiling were owner-ratified), and confirm the generation worker is untouched at `WORKER_BUNDLE_CEILING_BYTES = 1404493` — it should be, since its 145-module static graph reaches no worldPulse module, and saying so with a build behind it retires the question for all seven waves. If the engine chunk reds, STOP and report with the measured figure: the ceiling act is the owner's, and it is better raised once, deliberately, at the family's first car than discovered seven times at the terminal gate. Leave the receipt composer, the consumer census and the dossier line to cars 2 through 4; this car's whole job is to put the flag and one honest read into the tree with every bill paid and the byte question answered."}}
{"type":"result","key":"v2:e49f79256dec9d8f0df6fe59802ec79ade8debf7ae53d87266f773836e05a1e3","agentId":"a28674c8ca3bfe988","result":{"family":"GR-CW","summary":"Seven waves to price: GR-4 and GR-5 part-built, GR-6/GR-7/CW-1/CW-2x/CW-3 unbuilt, with CW-3 the terminus of the nine-wave critical path and of the whole FP programme. Family total 23-40 cars (midpoint ~31). Four findings change the pricing against the record. (1) GR-4 is far more nearly done than \"PART\" suggests: eight slices landed (4a, 4b-alpha, 4b-ii-W2, 4b-iii-a/b, 4c, 4d, 4e) across seven worldPulse modules plus a display dossier and five test files; what is owed is two deferred receipt pools and one chair question, 1-3 cars, not a wave. (2) GR-5's own certification row is an itemised price list — `subsystemRowsCompact.js:469` states in words that GR-5A (the `worstObservedEver` monotone fold) is all that landed and enumerates the renewal window, the trigger producer, the mid-term demand, the conversion arm, every lineage act, every ending, every news kind, every dossier line and every band as NOT built, adding that \"every successor needs a chair-signed band under the value-derivation law before it can be written at all\" — so GR-5's first act is a chair band derivation, not code. (3) GR-6 is not merely unbuilt, it is PARKED FOR RE-CHARTER by an executed refutation (ODQ §35.2, 2026-08-14): the compiled occasion-1 mount is dead code because the war-opener's threshold operand is never evaluated for the ordered target, and the named re-mount is the per-settlement HOSTILE_CONFIDENCE gate — live today at `warDeployment.js:1031` with `HOSTILE_CONFIDENCE = 0.42` at `:172` — needing a chair mount ruling plus a chair-authored band before anything lands. (4) The couplings terminus is starved at the root and nobody has noticed in six weeks: `causedBy` adoption is still exactly the six files the 2026-08-04 CW compile measured (S11), with only `provenanceKernel.js` and `roadsKernel.js` writing it engine-side, and no walker anywhere enforces the SC-6 adoption contract (`tests/lint` holds `couplingDesk`, `couplingInclusion` and `provenanceStampSingleWriter` — none of them adoption). CW-1's depth->=3 detection and CW-3's `chainDepthDegenerate` envelope are instruments over an empty sky until that changes. Two record corrections the code wins: CW-1's composition seam is NOT over-ceiling and NOT baseline-frozen — `realmItemReadModel.js` measures 784 effective of an 800 layer ceiling with no `.size-baseline.json` entry, so the guarded one-line delegation fits without a decomposition slice; and SP-6a's `routine` band has LANDED (`bandFamilies.js`), so SC-3 is discharged and CW-1's only real gate is causedBy. The hard ceiling that does bind is `peaceTerms.js` at 797 effective of 800 (measured, matching `pactAmendment.js`'s own header) — every GR-5/GR-6 symbol must arrive as a writer-family leaf on an import edge the head already has. On the worker bundle: ZERO KB for all seven waves. A BFS from `src/workers/generation.worker.js` at the slot reaches 153 modules and not one of them is in `src/domain/worldPulse`, `src/domain/display`, `src/domain/realm` or `src/domain/certification`; GR lands in the advance path and CW in display, certification and `scripts/audit`, none of which the generation worker's rollup graph carries. That zero is load-bearing rather than incidental, because the worker measures 1,404,493 B against a `WORKER_BUNDLE_CEILING_BYTES` of 1,404,493 (`tests/build/generationWorkerLazy.test.js:78`) — one byte reds. No lazy chunk is needed by any wave in this family. Output-moving: six of seven are dark at landing behind a manifest-only flag or carry no flag and no engine path; the single exception is CW-2x, which has no flag of its own and extends a walk whose substrate flag `provenanceLedgerEnabled` is LIT in the ONE_REGEN cohort (`simulationRules.js:669`), so its sharpened horizon line lands on reader-visible Herald prose the day it ships.","waves":[{"wave":"GR-4 — the succession question (remainder)","status":"PART-BUILT","carsLow":1,"carsHigh":3,"reason":"Much nearer done than the record's \"PART\" reads. Landed at the slot: `treatySuccession.js` (15.5 KB) plus `treatySuccessionDecision.js`, `treatySuccessionVoice.js`, `treatySuccessionOpeningVoice.js`, `treatySuccessionReaffirmedVoice.js`, `treatySuccessionProposalApply.js`, `treatyBreachCredibility.js`, `display/treatySuccessionDossier.js` and five test files; slice markers 4a/4b-alpha/4b-ii-W2/4b-iii-a/4b-iii-b/4c/4d/4e all present, and GR-4c's credibility charge is live at `treatyBreach.js:238` through `advanceCredibility` under `oathHolderActive`. THREE things are owed, all named in code rather than inferred. (a) `honored_by_silence` — the HONOR terminal reached by SILENCE; `grammarReceiptPools.js:118` says its \"expiry road has no transition site to mount on\", so this car must first build a transition site on the proposal-expiry road (the actor-major surface that today expires to DECLINE, V-15) before the pool can be wired and re-derived by `grammarLifecycleKindPools.walker.test.js` from the governed annex. That is the car with real work in it. (b) `credibility_charge` — the prose pool for a charge whose producer already landed; a kind row in `grammarNews.js` plus a pool plus the annex edit, cheap, can ride (a). (c) the succession-road disposition write, refused at `treatyBreach.js:243-248` in words (\"a second state behind a second flag CONJUNCTION … a later slice's question\") — a chair ruling first, then a small car, or a permanent recorded deferral. Price 1 car if (b) rides (a) and (c) is deferred; 3 if the expiry-road transition site proves to need its own slice and (c) is built.","dependsOn":"Nothing unbuilt. GR-1 (`oathHolder.js`) and GR-4a-e are in tree. The `honored_by_silence` mount needs the actor-major proposal-expiry surface (`actorMajorApproval.js`, V-15, landed) to gain a transition site — the wave builds it, it does not wait for it. The pools are governed corpus (`docs/content/RECEIPT_POOLS_GRAMMAR.md`), so the car runs the corpus-gate ritual, not a plain gate.","ownerGate":"None — signed under the 14:4x grant. No push, deploy, migration, deletion or spend; no new persisted top-level key (the succession question rides `worldState.proposals` and the treaty record's chartered conditional extensions). Item (c) is a CHAIR ruling, not the owner's.","outputMoving":"NO at landing — dark behind `oathHolderEnabled`, which is manifest-only (`ENGINE_GATED_VIRTUAL_RULE_KEYS` at `simulationRules.js:313`, in no preset spread), strict `=== true`, four-fence dormancy set. Output moves only at the second declared lighting (sitting item 1's §764.3 amendment, the owner's).","reuse":"`treatySuccession.js` + six siblings; `treatyBreachCredibility.js`'s landed fold; `advanceCredibility` and `spatialLedgers.credibility` (`informationStatecraft.js`); `grammarNews.js`'s `grammarKindRow` registry idiom (the `reaffirmed` row at `:179` is the exact template); `grammarReceiptPools.js`'s twelve governed pools and the walker that re-derives them from the annex; `heraldRouting.js:258`'s treaty-cohort desk seat.","workerKb":"0 KB. `src/domain/worldPulse` is absent from the generation worker's 153-module graph (BFS from `src/workers/generation.worker.js` at the slot: zero worldPulse modules). No lazy chunk needed. The zero matters because the worker is at 1,404,493 B against a ceiling of 1,404,493."},{"wave":"GR-5 — renewal, renegotiation, conversion (remainder)","status":"PART-BUILT","carsLow":6,"carsHigh":9,"reason":"The largest wave in the family, and its own certification row prices it. GR-5A landed: `worstObservedEver` as the one-line monotone fold at the V-7 site, `worstObservedEverOf` and `treatyRenewalActive` in `pactAmendment.js:138/:~120`, the manifest entry at `simulationRules.js:422` and the cert row at `subsystemRowsCompact.js:449-469`. That row then enumerates, in words, everything NOT built: \"the renewal window, the renewal trigger producer, the mid-term renegotiation demand, the conversion arm, any lineage act, any ending, any provenance value, any news kind or Herald desk, any dossier line, any read-model key, and any band, threshold, cap or authored number — GR-5 as chartered is six behaviours and five bands, and every successor needs a chair-signed band under the value-derivation law before it can be written at all\". Decomposed into cars: (0) the chair band derivation — renewal window length, mid-term demand threshold, ask cap, conversion trust band, renewal-beat significance — each derived against measured substrate, not chosen; (1) the renewal window plus the fifth closed `renewal` trigger on `pactProposals`; (2) the re-draft at the current believed ratio through the two-picture contract, both sides re-appraising under their own `truthFor`; (3) acceptance as a `renewed` LINEAGE ACT through `pactAmendment` with GR-1 re-stamping; (4) the mid-term renegotiation demand, refusable, ask-capped, strain-on-refusal with no casus; (5) the accepted demand as a `renegotiated` act; (6) the conversion arm — `compelled_alliance` at window with `worstObservedEver === 'honored'` and trust above band proposes `allied` through the existing label lane and rewrites terms symmetrically as `converted`; (7) three endings joining the closed pact-endings vocabulary with their pools, kind rows and desks (a governed-annex edit, so the corpus-gate ritual); (8) the dossier round-trip on WarFaithTab, TreatyPanel and the PDF treaty chapter, plus the dormancy fences and the cert-row amendment. Six if cars fold in pairs and the dossier rides the acts; nine if the conversion arm and the annex each take their own sitting. The hard constraint on every one of them: `peaceTerms.js` measures 797 effective of a hard 800 (measured at the slot; `pactAmendment.js`'s own header says the same), so nothing lands in the head — every symbol arrives as a writer-family leaf reached by an import edge the head already has, the `peaceTermsSale.js`/`pactAmendment.js` pattern verbatim.","dependsOn":"GR-5A (landed, in tree). GR-2/GR-3 (landed — `pactProposals.js`, `pactFormation.js`, `pactAmendment.js`, `peaceTermsCatalog.js`). The chair-signed bands under the value-derivation law come FIRST and block every code car (the cert row says so by name). Conversion needs the relationship label-proposal lane (`relationshipRulesCore.js:776`, landed) and `compelled_alliance`. The three new endings need the governed annex `docs/content/RECEIPT_POOLS_GRAMMAR.md` and the corpus gate. GR-7's endings-mix envelope waits on this wave, not the reverse.","ownerGate":"None on the build — signed under the 14:4x grant. The five bands are CHAIR-signed under the value-derivation law (the chair may author them vetoably under the 09-13 and 09-14 grants). No new persisted top-level key: `provenance`, `lineage` and `worstObservedEver` are chartered conditional, drop-when-absent treaty-record extensions on the `sellerId`/`buyerId` precedent, and `PACT_PROVENANCE` at `pactAmendment.js:77` already freezes `converted` and `renewed`. Lighting is the owner's.","outputMoving":"NO at landing — dark behind `treatyRenewalEnabled`, manifest-only at `simulationRules.js:422`, in no preset spread, strict `=== true`, four-fence dormancy plus the lit-mutant control. The cert row's own claim is that the reader is exported and structurally unconsumed in src at this wave and that the pin reds the day the conversion gate wires it — that red IS this wave's handoff signal and must be discharged in the wiring car, not silenced.","reuse":"`pactAmendment.js` (the lineage-act writer, `stackingCellOf`, `PACT_PROVENANCE` already carrying all four values, `worstObservedEverOf`, `treatyRenewalActive`); `pactProposals.js` (the ledger and four of five closed triggers); `negotiationPictures.js` (the full two-picture API — `createNegotiationPicture`, `evaluateNegotiationPicture`, `compareOfferToResponderDraft`, `negotiateFromPictures`, `validateTermSheetAgainstPictures`; the volume's build-precondition is already satisfied); `oathHolder.js` for the re-stamp; `treatyClock.js`'s 52-week marker; `peaceTermsDocument.js`'s `frayingTermOf` and voice tables; `grammarNews.js`/`grammarReceiptPools.js` for the three endings.","workerKb":"0 KB. Same BFS receipt as GR-4 — the generation worker's graph carries no `src/domain/worldPulse`. No lazy chunk needed."},{"wave":"GR-6 — mediation generalized","status":"UNBUILT","carsLow":5,"carsHigh":8,"reason":"Zero code (`mediationGeneralizedEnabled` absent from `simulationRules.js`'s manifest; no `mediationPressure.js`), and it is PARKED FOR RE-CHARTER rather than merely unstarted — ODQ §35.2 (2026-08-14, chair, vetoable) ratified a full-train STOP because occasion 1's compiled mount was refuted BY EXECUTION: the war-opener's threshold operand is never evaluated for the ordered target (the short-circuit is live today at `warDeployment.js:1094`, with the file's own waiver note above it), so a bounded multiplier at the opener's soft gate would be dead code, and the band was literally blank. §35 names the re-mount candidate: the per-settlement `HOSTILE_CONFIDENCE` floor, pre-loop and coalition-guarded — live today at `warDeployment.js:1031` (`if (!coalitionDecision && fromStrength < HOSTILE_CONFIDENCE) continue;`) with `HOSTILE_CONFIDENCE = 0.42` at `:172`, `CONQUEST_MARGIN = 0.12` at `:173` and `WAR_FACTOR_W` in `REASON_TUNING`. §35's own words: \"a chair mount ruling plus a chair-authored band against the measured substrate. One future compile pass carries both; nothing lands until then.\" Cars: (0) the re-charter compile pass — the mount ruling, the band, and the banked TE5 re-measurements (direction WAR->GRAMMAR, the vacuous warIntent walker note discharged, CQ-7's reading surviving); (1) occasion 1 — the dependency-free `mediationPressure.js` leaf (<=120 effective) on the embassyLedger deposit-and-consume idiom, mounted at the re-ruled gate, with the 2-tick `warIntents` TTL DECLARED and the \"war that did not happen\" receipt minting when a pressured intent expires unopened; (2) occasion 2 — the fraying pass over `frayingTermOf`, reading `worstObservedEverOf(treaty) ?? 'honored'` by name so a once-strained record lowers the soften notch, proposing a renewal round where GR-5 is lit and softening strain accrual one banded notch where it is dark; (3) occasion 3 — the temple arm through `cohesionWeave.js` facet resolution, naming the institution and, where a seated priest resolves, the priest; (4) the closed four-member mediation ending vocabulary (`brokered_back`, `brokered_terms`, `declined_to_broker`, `brokerage_failed`) with pools, kind rows and desks — a governed-annex edit and the corpus gate; (5) the reach band and transit legs over `hopWeeks` (with V-27's standing hazard honoured: hopWeeks is calibrated per digest, a whole realm spans ~2-8 march weeks, so any band >= 9 weeks refuses nothing) plus the decline-posture arm priced in trust foregone; (6) the flag mint, manifest entry, certification row, four-fence dormancy set and the single-finder source-scan walker; (7) the dossier round-trip on the chronicle and the WarFaithTab \"the courts remember\" line, pinned across all three towns. Five if occasions 2 and 3 fold and the dossier rides the endings; eight if the re-charter, each occasion, the annex and the fences each take a sitting.","dependsOn":"The chair re-charter compile pass (ODQ §35.2) — a hard blocker, nothing builds before it. GR-2 (landed). GR-5 for occasion 2's renewal-round arm only; the dark arm (soften one notch) works without it, so GR-6 does not strictly wait on GR-5, but landing GR-5 first makes occasion 2 whole in one car. WR-7b's interceptor casting for the interested-broker counterface (the same cross-cut read, failed, casts the saboteur — no new state). The single-finder law binds: `findCrossPressuredMediator` (`peaceTermsGraph.js:103`) is the one finder and this wave adds occasions only.","ownerGate":"None — signed under the 14:4x grant. The mount ruling and the band are the CHAIR's by ODQ §35.2's own terms. No new persisted state at all: the occasions read existing ledgers and emit receipts. Lighting is the owner's.","outputMoving":"NO at landing — dark behind `mediationGeneralizedEnabled`, manifest-only, strict `=== true`, x1 exactly when dark/no-broker/no-intent so the composed result is byte-identical. This carries the sharpest fence in the family, because the mount is INSIDE the live war opener: the own-footprint golden must prove a byte-identical war layer dark, and the lit-mutant control must prove the fence is not vacuous. `warDeployment.js` measures 684 effective of 800, so the mount has room without a decomposition.","reuse":"`peaceTermsGraph.js:103` `findCrossPressuredMediator` (the ONE finder, never forked); `peaceTermsOverlay.js:84` `accrueMediationTrust` (reused, both edges, every occasion); `peaceTermsCatalog.js:53` `MEDIATION_SOFTEN: 0.2`; `warIntent.js` (`WAR_INTENT_LEDGER_KEY = 'warIntents'` at `:79`, TTL 2, 154 effective lines); `peaceTermsDocument.js:150` `frayingTermOf`; `pactAmendment.js`'s `worstObservedEverOf`; `spatial/cohesionWeave.js` facet resolution; `embassyLedger.js`'s deposit-and-consume header as the design law; `distanceRead.js`'s `hopWeeks`.","workerKb":"0 KB. `src/domain/worldPulse` (including `warDeployment.js`) is absent from the generation worker's graph. No lazy chunk needed."},{"wave":"GR-7 — grammar measurement + certification","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"No flag, no world state — instruments only, and the GR family's terminus. It extends `scripts/audit/war-convergence-collector.mjs` (547 lines, already importing `treatyPairKey` at `:74` and observing `treatyPairKeys` at `:297`) with a treaty section rather than minting a rival script: live-treaty count over time, compliance-state distribution, the formation-vs-dictation THESIS METRIC (structurally 0 before this programme), the nine-member endings mix with every producer named, term lifetimes on the 52-week clock, grain actually moved by stream terms at population scale, proposal refusal and no-overlap rates, mediation occasion counts and save rate, and the succession answer mix — that last one declaring its `routineMajorApproval: true` profile precondition in the row or being vacuous. Then the envelopes: a share envelope per ending member; a formation-endings mix over the closed four with a FLOOR on `no_overlap` (reserves must actually bite) and a nonzero `expired_unanswered` under lit transport; a mediation mix over GR-6's closed four with FLOORS on `declined_to_broker` and `brokerage_failed` (the broker must be able to refuse and to fail, or law L is decoration — the save rate alone cannot see this); the formation-trigger histogram keyed by TRANSPORT MODE; every envelope with an executed mutant negative control. The certification side is cheaper than the record implies: four of the five GR flags already carry rows (`subsystemRowsCompact.js:49` oathHolder, `:98` pactFormation, `:218` treatyLifecycleVoice, `:449` treatyRenewal), so GR-7 converts four pendings and the fifth row lands with GR-6, not here. Plus the out-of-order-lighting red and GR-7 sampling its OWN registry rows through `tests/helpers/couplingReceiptSample.js` (203 lines, the SC-9 obligation). Three cars if the collector section, the envelopes and the certification conversions each take one; five if the endings and formation/mediation mixes split and the mutant controls take their own sitting.","dependsOn":"GR-0 through GR-6 must all be LANDED — the endings mix names GR-0's `ran_its_term`/`hollowed_detected`/`hollowed_quiet`, GR-2's `broken_by_war`, WR-0c's `repudiated`, GR-4's `disavowed_by_succession`, GR-5's `renewed`/`renegotiated`/`converted` and GR-6's four mediation endings as producers, and an envelope over an absent producer is decoration. The thesis metric needs GR-2/GR-3 (landed). CW-0w's shared sampler (landed) is the SC-9 transport. GR-7 is strictly last in the family.","ownerGate":"None on the build — signed under the 14:4x grant, and it is a pure instrument act. The DONE-WHEN is the owner's: \"this wave closes the programme: DONE when the envelopes hold on an owner-ordered soak, and not before\". The chair builds, the owner runs.","outputMoving":"NO, ever. No world state, no flag, no engine path — envelopes, certification rows and receipt fields only. One standing hazard to honour: never declare a `wizard_news.*`-fed identity on a certification row (the moverFamily skew hazard), and the thesis-metric pin must be seeded non-empty rather than asserted on an empty harness.","reuse":"`scripts/audit/war-convergence-collector.mjs` (its own idiom, already treaty-aware); the four landed GR certification rows in `subsystemRowsCompact.js`; `couplingRegistryGrammar.js` (7 rows, the GR volume's registry leaf); `tests/helpers/couplingReceiptSample.js`; `treatyClock.js`'s `CURRENT_TREATY_TICKS_PER_YEAR`; `peaceTermsPrimitives.js`'s `treatyPairKey`; the WR-9 envelope shape and `subsystemRowsVirtual`'s certification pattern.","workerKb":"0 KB. `scripts/audit` is not bundled at all, and `src/domain/certification` is absent from the generation worker's 153-module graph. No lazy chunk needed."},{"wave":"CW-1 — the cascade governor","status":"UNBUILT","carsLow":2,"carsHigh":4,"reason":"`cascadeGovernorEnabled` has zero hits; no `cascadeBraid.js`. The build is a NEW pure leaf `src/domain/display/cascadeBraid.js` (~300 budget) doing detection, ancestor grouping by IDENTITY over recorded provenance edges (never similarity), layer counting through the registry join, braid assembly and damping marks, with zero engine imports proved by a census pin; plus a guarded one-line delegation at the composition seam; plus the braided view item — deterministic id from codepoint-ordered member receipt ids, full address chain over every member settlement, typed action `cascade`, reasons as the chain, and typed entity-ref slots so `heraldIndex` facets flip PENDING->available (SC-8). TWO RECORD CLAIMS FALL TO MEASUREMENT AND BOTH MAKE THIS CHEAPER. First, SC-3 is discharged: the CW volume blocks CW-1 on SP-6a because `'routine'` did not exist, but `bandFamilies.js` now carries the three-band scale and `'routine'` is in live use — the braid-class blocker is dead. Second, the seam is not the obstacle the volume feared: it says `realmItemReadModel.js` is \"1072 lines — OVER the 800 ceiling and baseline-frozen\", but the file measures 784 EFFECTIVE of an 800 layer ceiling and has NO entry in `scripts/.size-baseline.json`, so the one-line delegation fits with ~16 effective lines to spare and no decomposition slice is forced. What is NOT cheaper is the premise: causedBy adoption is still exactly what the 2026-08-04 compile measured — six files in `src/domain`, with only `provenanceKernel.js` and `roadsKernel.js` writing it engine-side (and roadsKernel's is the V-24d dark-gated edge) — so cross-layer chains of depth >= 3 essentially cannot exist and CW-1 lands as machinery proven only on seeded fixtures. Cars: (1) the leaf plus the seam plus the four-fence dormancy set and the lit-mutant control; (2) the fixture and pin set — two unrelated same-window misfortunes DO NOT braid (the false-causality negative, hardest, seeded with two live chains FIRST so the absence is not vacuous), a 2-layer chain never braids while the SAME fixture braids at 3, a `major` member is NEVER damped, damped members keep their ids and feed presence with count identity pre/post, the runtime no-decimal pin on rendered braid prose, address-law totality over members; plus the three executed mutants (ancestor-identity -> similarity, depth floor 3->2, damping that removes a member) in `mutation-coverage-manifest.json`. Four if the heraldIndex entity-ref wiring and the discourseKernel forward realization each take their own sitting.","dependsOn":"SP-6a (LANDED — `bandFamilies.js`, SC-3 discharged). The SC-6 causedBy adoption contract — NOT satisfied and unenforced; CW-1 can land on synthetic fixtures but is vacuous in production until the FP volumes adopt causedBy at their mint sites. `chronicleGraph.js`'s `buildRecordedEdges` over `spatialLedgers.provenance` and `discourseKernel.js` (landed, reused, never forked). `heraldIndex.js` (landed, 360 lines, PENDING facets waiting). The composition chokepoint must be VERIFY-AT-BUILD by the feed-assembly symbol rather than trusted from the volume's guess.","ownerGate":"None — signed under the 14:4x grant. ZERO new persisted keys (the programme's §3 is emphatic: `newPersistedKeys = []`, and a slice that finds itself writing worldState has left its spec and STOPs). The braided item's standing was ruled by the chair (Q4: view-item-with-receipt-discipline — deterministic composition-time id, never persisted, never in pulseRecord), vetoable. The bands — window length, member floor, depth threshold, member cap, braid-class derivation — are owner-signed at the lit soak per THE PROMISE.","outputMoving":"NO at landing — dark behind `cascadeGovernorEnabled`, joining `ENGINE_GATED_VIRTUAL_RULE_KEYS` in the same commit as its first by-name gate read, with fence 1 being a byte-identical composed Herald feed (same items, same order, same prose) and the lit-mutant control proving fences 1 and 3 trip when forced on.","reuse":"`discourseKernel.js` (206 effective — the forward prose realization, reused not rebuilt); `chronicleGraph.js` (237 effective); `provenanceKernel.js`'s ledger and `spatialLedgers.provenance`; `bandFamilies.js`'s three-band scale; `heraldIndex.js`'s typed-ref facets; `narrativeTempo.js` as the engine-side governor CW-1 must NOT duplicate (the census pin proves zero engine imports); the six coupling-registry leaves as the layer authority; `rumorHeraldLink.js`'s view-item idiom.","workerKb":"0 KB. `src/domain/display` and `src/domain/realm` are both absent from the generation worker's 153-module graph. No lazy chunk needed."},{"wave":"CW-2x — the cause-walk extension","status":"UNBUILT","carsLow":3,"carsHigh":5,"reason":"No `causeWalkCouplingJoin.js`. This is the cheapest unbuilt wave in the family because S9's refutation already paid most of it: the V-4 estate landed AFTER the volume was drafted, so the backward walk, the covert fail-closed truncation, root and dark-ledger honesty, the named-actor resolvers and the integrity register are all done and must NOT be rebuilt (building the volume's CW-2 as written would erect a second cause-walk — the second-telephone defect pointed at ourselves). Four slices, each a natural car. (1) THE REGISTRY JOIN — new `src/domain/display/causeWalkCouplingJoin.js` (~130): from a coupling receiptField on a town-page or Herald surface, resolve the receipt id and open the walk there, with the registry row's pairId/direction/owningVolume rendering as the link's LAYER TAG; `causeWalk.js` (130 effective of 306 raw — ample headroom) gains at most a tag pass-through. (2) HORIZON HONESTY SHARPENED — and this is the slice that carries the wave's real risk, see the output-moving cell: today `causeWalk.js:261` and `:304` render the SAME `NO_DEEPER_MEMORY` line for a true root and for a chain that simply ran out of ring, so the retention cut is currently indistinguishable from the end of the story; the slice must render a distinct line where the chain leaves the `MAX_HISTORY = 80` ring or hits a `collapsed_ancestor` edge, never a fabricated link, seeded by aging a fixture past `MAX_PROVENANCE_EDGES` with the un-aged fixture as negative control (two lines, two causes, never conflated). (3) CROSS-LAYER RENDERING — crossing count at the glance, chain at the table, per the LEGIBILITY law; `CauseWalkPanel.jsx` is 96 effective against a 600 components ceiling. (4) THE STORY HARNESS — a fixture format binding story -> expected chain shape -> owning-wave flag set, plus ONE synthetic multi-layer chain with a deliberate broken-chain variant that MUST red (a story harness that cannot fail is refused), with each of the six stories' real fixtures landing with their own final owning waves. Three cars if slices 1 and 3 fold; five if the horizon slice's aged-fixture machinery and the harness each take their own sitting.","dependsOn":"The chair's ratification of Q1 (the CW-2 -> CW-2x re-scope) — recommended \"ratify\" by the compiling chair and the reasoning is executed, but it is a chair word not yet given. The V-4 estate (LANDED: `causeWalk.js`, `chronicleGraph.js`, `heraldIntegrity.js`, `CauseWalkPanel.jsx`, `CausalityPopup.jsx`, `HeraldHeadline.jsx`, `discourseKernel.js`). CW-0w (LANDED — the registry and its walkers, 58 rows across six leaves). Layer tags derive ONLY from registry rows: no word-association inference (the moverFamilyOf hazard). Collision discipline: `causeWalk.js` and `chronicleGraph.js` are the VISION lane's — re-read before editing.","ownerGate":"None — signed under the 14:4x grant. Pure read over `spatialLedgers.provenance`, pulseHistory and registry data; no writes anywhere; zero new persisted keys.","outputMoving":"⚠ THE ONE WAVE IN THIS FAMILY THAT IS NOT DARK, AND IT MUST BE CLASSIFIED AT CHARTER RATHER THAN ASSUMED. CW-2x mints no flag, and its substrate flag `provenanceLedgerEnabled` is LIT — it sits in the ONE_REGEN preset-override spread at `simulationRules.js:669`, one of the eight chartered dark engines lit together at the declared golden boundary. So the walk is reachable on lit presets today, and `causeWalk.js` feeds `heraldCausalVoice.js` and `discourseKernel.js`, which produce Herald PROSE consumed by `HeraldHeadline.jsx`. Slice 2 replaces the line a retention cut renders — `NO_DEEPER_MEMORY` today at `causeWalk.js:261`/`:304` — with a distinct one, which is a same-seed reader-visible text change on a lit preset with no flag to hide behind. The volume's dormancy claim (\"inert where `spatialLedgers.provenance` is absent\") is true but is NOT the same as dark. PLAUSIBLE output-moving; Car 1 of this wave must measure it against the golden view model and declare the shift rather than let it ride. Slices 1, 3 and 4 are additive surfaces and are very likely non-moving.","reuse":"`causeWalk.js` (130 effective of 306 raw — `buildCauseWalk`, `REDACTED_HOP`, `NO_DEEPER_MEMORY`, `LEDGER_DARK_LINE`, the seesSecrets redaction extended byte-identically over the new join); `chronicleGraph.js`; `heraldIntegrity.js`'s five-state register; the six coupling-registry leaves (58 rows) as the layer authority; `provenanceKernel.js`'s `retentionClass: 'collapsed_ancestor'` and `MAX_PROVENANCE_EDGES`; `worldState.js:16`'s `MAX_HISTORY = 80`; `CauseWalkPanel.jsx`/`CausalityPopup.jsx` as landing pads.","workerKb":"0 KB. `src/domain/display` and `src/components` are absent from the generation worker's 153-module graph. No lazy chunk needed."},{"wave":"CW-3 — the coupling measure (closes FP)","status":"UNBUILT","carsLow":3,"carsHigh":6,"reason":"No `subsystemRowsCoupling.js`. The terminus of the nine-wave critical path and of the whole FP programme. Three instruments. (1) THE DIFFERENTIAL HARNESS — paired lit/dark same-seed soaks per coupling flag family, families ENUMERATED from registry rows' `flags` and never hand-listed, homed in `scripts/audit` on the `behavioral-observation.mjs` conventions. Its first consumer and guard-the-guard proof is confirmed still open at the slot: `subsystemRowsRegen.js:60` reads, verbatim, \"Half of that instrument now exists; the differential harness does not, so this row reads UNOBSERVED and says why\" — the harness must show that KNOWN divergence (belief lagging ground truth by more at equal distance, lit vs dark) before it certifies any unknown coupling, and the order is asserted. (2) ALIVENESS FLOORS — new `src/domain/certification/subsystemRowsCoupling.js` (~250 budget, a SIBLING file; `subsystemRowsWar.js` is 557 effective of 800 and additions there are forbidden by the volume): registry-keyed rows where every row must FIRE at or above a banded floor in the owner-signed lit soak or red as decoration, with rows for volumes still dark at measure time reading UNOBSERVED-and-say-why rather than borrowing evidence. The surface is real: 58 rows live today across six leaves (war 32, espionage 13, grammar 7, encounters 3, info 2, trade 1) under 13 chartered volume prefixes, and each needs a floor or an honest verdict — this is what pushes the wave past one car. (3) CASCADE ENVELOPES — chain-depth distribution over the lit soak's provenance ledger (most shallow, some deep, none unbounded; a chain still growing at the soak horizon reds), the six story shapes each observed at least once across the seed family (consuming CW-2x's harness fixtures), and the degenerate-depth red named `chainDepthDegenerate`. Every envelope carries an executed mutant negative control. Three cars if each instrument takes one; six if the 58-row aliveness surface splits by volume and the harness's guard-the-guard proof takes its own sitting. ⚠ The degenerate-depth red is not a hypothetical: measured today, causedBy has two engine-side writers, so a lit soak run now would fire `chainDepthDegenerate` loudly and correctly — the programme's premise really has not been adopted yet.","dependsOn":"The longest chain in the estate. CW-0w (LANDED). CW-2x's story-harness fixtures (instrument 3 consumes them). CW-1 (the braid the envelopes measure). ES-7, the last unbuilt member of the strictly serial nine-wave critical path SP-D -> ES-1..ES-7 -> CW-3 (six of nine done per the first pass; `subsystemRowsBelief.js:169` — \"the six Herald kinds are ES-7's, in ES-7's commit\"). And above all SC-6, the causedBy adoption contract, without which instrument 3 is an instrument over an empty sky and says so at soak in those words. CW-3 is genuinely last, and it should be: it measures everything above it.","ownerGate":"The BUILD is not gated — signed under the 14:4x grant; soak-side artifacts under `scripts/audit` output conventions, never worldState, never saves. But the wave's own ledger row must carry the boundary LOUDLY: CW-3 BUILDS; soak execution, lighting and band ratification are the owner/Fable terminal phase (standing directive 2026-08-02), and the FP programme is DONE when these envelopes hold on the owner-signed lit soak and not before. That soak is downstream of the second declared lighting, which is sitting item 1's §764.3 amendment and the owner's.","outputMoving":"NO — measurement, not news. No flag, no engine path, no save bytes; spine reqs 13/14 declared-empty with reason in the WR-9 wording. The INTERVAL_WEEKS denomination applies to the measured SERIES, not the instrument, and is stated rather than assumed.","reuse":"`subsystemRowsRegen.js:60`'s honest gap as the first consumer and the proof that the harness sees; the six coupling-registry leaves plus `couplingRegistrySchema.js` as the enumeration source; `CHARTERED_VOLUME_PREFIXES` at `tests/domain/couplingRegistry.test.js:108` (13 prefixes — point at it, never restate it; admission edits four sites in that one file); the `subsystemRows*` family idiom; `scripts/audit/behavioral-observation.mjs` conventions and the existing `cause-lifecycle-soak.mjs`; `tests/helpers/couplingReceiptSample.js`; `brokerageStamps.js`'s measured hop-count table as the evidence-grade precedent.","workerKb":"0 KB. `src/domain/certification` is absent from the generation worker's 153-module graph and `scripts/audit` is not bundled at all. No lazy chunk needed."}],"firstCar":"Land the SC-6 causedBy adoption contract as MACHINERY, before any other car in this family — a single `tests/lint/causedByAdoption.walker.test.js` on the `couplingInclusion.walker.test.js` precedent (1,618 lines, the estate's own inclusion-ratchet idiom), with the ratchet's cohort seeded from a measured census rather than a guess. The case for putting it first is that it is the one act that stops the family's two most expensive waves from being decoration: CW-1's depth->=3 braid detection and CW-3's `chainDepthDegenerate` envelope both rest on FP waves populating `causedBy` at their receipt mint sites, the CW compile named that as the reason CW-1/CW-2x/CW-3 sequence LAST, and six weeks later the measurement is unchanged — `grep -rl causedBy src/domain` returns exactly six files (`provenanceKernel.js`, `roadsKernel.js`, `roads/state.js`, `discourseKernel.js`, `heraldCausalVoice.js`, `realmItemReadModel.js`), of which only `provenanceKernel.js` and `roadsKernel.js` write it engine-side and roadsKernel's is the V-24d dark-gated edge, while `tests/lint` holds `couplingDesk`, `couplingInclusion` and `provenanceStampSingleWriter` walkers and nothing that enforces adoption. SC-6 has no in-tree spelling at all; it lives only in `docs/DESIGN_FP_ARCH_CW.md` §5b, which is exactly how an obligation written as prose quietly fails to be paid by the thirty-plus waves that were supposed to pay it. Build it as a two-way walker: enumerate the engine-side receipt mint sites (the `errandMint.js`/`appendPulseHistory` seam family, navigated by symbol and not by line), assert that every mint of a child receipt of a known outcome populates `causedBy` at the writer, and freeze today's compliant set as a ratchet that only shrinks — plus a positive control proving the walker SEES (a planted mint stripped of `causedBy` must red) and a negative control proving it does not fire on a root receipt that legitimately has no parent, because a walker that cannot fail is the vacuity defect this estate keeps re-finding. Land the adoption rule in the same commit as a one-paragraph obligation in the packet standard so every subsequent FP wave — GR-5's lineage acts and GR-6's brokerage receipts included — pays it at its own mint rather than leaving CW-3 to discover the debt at soak. The car is cheap and clean on every axis this family cares about: it needs no owner (no push, deploy, migration, deletion or spend; signed under the 14:4x grant), it adds ZERO bytes to the generation worker (the worker's 153-module graph carries no `tests/lint` and no `src/domain/worldPulse`, and the worker has literally zero headroom at 1,404,493 B against a ceiling of 1,404,493), it writes no world state and moves no same-seed output, and it is the one act in the family that can be executed today with nothing blocked ahead of it. Do it before GR-5's band car and before CW-1, because every wave that lands without it adds another mint site to a debt CW-3 will have to price at the far end of the critical path."}}
{"type":"result","key":"v2:f631fa2cee5df5fcbb43e3814dcca35eb31639815a7166ddf059fd8b6d12f9e1","agentId":"aeee29db4ed7352ac","result":{"family":"LG0 — LOGISTICS LG-0 and LG-1 (item #33, the family's first train)","summary":"The re-price against code changes LG-0's bill substantially, in both directions. What the volume chartered as \"build the circle network\" is already BUILT and lit: `src/domain/spatial/teleportEdges.js` (capability enumeration from the closed catalog, the magic gate MG-3a at :124-127, the clique builder at :193-213, the <2-holder dormancy floor at :197), mounted at `spatialDigest.js:630/:903`, opted in for every NEW canon at `campaignSpatialCanonize.js:124` (`teleport: true`), folded into routing at `distanceRead.js:361-443` and `:786-822`, carrying rumor at `rumorNetwork.js:673`, with 343 test lines across ten describes in `tests/domain/teleport.test.js`. Three of LG-0's chartered obligations are therefore already paid and must be struck from its price: the edge set itself; the \"circle capacity attribute\" (TELEPORT_EDGE_CAPACITY = 2 is already recorded per edge at `teleportEdges.js:75/:203` — only its READ is owed, and the volume puts that in LG-1); and Q9's feature minting (both `'Teleportation circle'` and `'Airship docking (high magic)'` are already in `src/data/institutionalCatalog.js:1419/:1987/:2248`). What remains is the thing the volume named and nobody built: the WILLINGNESS predicate. `teleportEdges.js:22-26` promises that \"the WILLINGNESS (non-hostile diplomacy) is a READ-TIME property … the diplomatic gate composes through the existing danger/relationship seams\" — and no such gate was ever written, at any consumer. The defect is live on every new-canon world with two circle-holders: `pathCost`'s fast path (`distanceRead.js:592-599`) and `routingAdjacency` (`:816-822`) gate on ENDPOINT MEMBERSHIP only, so a hostile faction routes through a target's own circle at collapsed cost, and `teleportNeighbourMap` hands it a zero-decay hi-fi intel channel into the target's bloc. The one architectural cost is that all four teleport reads are pure functions of the deep-frozen digest memoized on digest identity (TELEPORT_NODE_MEMO, TELEPORT_ADJ_MEMO, AUG_MEMO, CANDIDATE_MEMO), so a per-tick relationship predicate cannot be pushed inside them; L-LG-3 (\"the graph is recomputed from the relationship plane each pulse\") forces a per-tick filtered bloc view memo-keyed by (digest, bloc signature) rather than the cheaper reject-after-the-fact filter. Neither wave needs a migration, a persisted field or a new vocabulary: the digest slot exists, the predicate is read-time, the relationship rungs ('allied', 'trade_partner', 'patron', 'client', 'vassal', 'hostile') already exist, and a circle toll books into the closed eight-member TAX_FORMS via `market_tolls`. Two findings the compile must carry: the recon's \"28 literals\" for the fourth flag obligation is STALE at this slot — `tests/domain/contributionLedgerShape.test.js` now asserts 35/35 (line ~120; the test TITLE at :49 still says 28), so LG-0 moves 35→36; and the volume's graded substrate annex (`laneLGR-substrate.md` / `LG-SUBSTRATE.md`), which §6 makes every LG packet cite by sha and grade against, exists in NO preserve ref and in no commit in this repo — it was lost with the 09-12 scratchpad, so the first car must re-grade the substrate from code rather than cite it.","waves":[{"wave":"LG-0 — THE WILLINGNESS GATE (mints circleNetworkEnabled; carries the LG-0V volume fold)","status":"PART-BUILT","carsLow":4,"carsHigh":6,"reason":"Re-priced DOWN on three chartered obligations and UP on one. Struck as already built: the circle edge set, its dormancy floor, its magic gate and its routing fold (teleportEdges.js:193-213, distanceRead.js:361-443/:786-822, campaignSpatialCanonize.js:124, 343 test lines); the circle capacity ATTRIBUTE (teleportEdges.js:75/:203 — recorded, unread, and the read is LG-1's); and Q9's feature minting (institutionalCatalog.js:1419/:1987/:2248 — LG mints nothing, it consumes two existing entries). What is genuinely owed: (1) the LG-0V fold — docs/DESIGN_FP_ARCH_LG.md has NEVER existed build-side (ODQ §148.3: 'the fold is a CREATE'), and because the graded substrate annex is lost the same car must re-grade the ten Q-answers from code (Q2 and Q9 both change verdict); (2) the flag mint, whose bill is FOUR obligations and measured 15 files at the last mint (irregularForceEnabled touches simulationRules.js, a subsystemRows* certification row, one by-name === true gate read in the same commit, engineGatedRuleKeys.walker.test.js, subsystemRowsVirtual.test.js, coveringArrayCoverage.test.js, militaryStrength.test.js, a dormancy byte-identity suite, and the two contributionLedgerShape.test.js literals — 35→36 at this slot, NOT the recon's 28), plus the literal-drive law (mechanismLitCoverage grants credit only on a literal `circleNetworkEnabled: true`, never a computed member); (3) the predicate module itself plus the per-tick bloc view and the memo-key change in distanceRead.js — the one real design call, because L-LG-3's 'recomputed from the relationship plane each pulse' rules out the cheap reject-after-the-fact filter and requires the augmented adjacency to be keyed by (digest, bloc signature) so a hostile holder is routed AROUND rather than merely refused; (4) the two live defect sites wired to it (the routing fold and rumorNetwork.js:673's hi-fi carrier — a hostile bloc member must stop being a zero-decay intel neighbour); (5) the acceptance car — the defect reproduced then cleared with negative controls, dark-flag byte-identity, and the harder pin that a LIT flag over an all-allied bloc is also byte-identical. 4 cars if the predicate module and the memo-key change ride together; 6 if TTS convicts the adjacent pair on distanceRead.js (it is exactly the shape that convicts in this estate) and the flag bill escapes its car as it has six recorded times.","dependsOn":"Nothing in code — the graph, the catalog entries and the relationship vocabulary all exist. Doc-side it carries its own LG-0V member (ODQ §151.4: 'LG rides lg-0''s own LG-0V member') and must absorb the loss of the substrate annex (DESIGN_FP_ARCH_LG.md:17-20 and §6). Q10's band-walker prefix is NOT a blocker: LG-0 lands no band, and the compile must say so rather than leave it implied (§D makes any LG band wait on the prefix).","ownerGate":"None — signed under the 14:4x grant. No migration, no persisted-shape widening, no deletion, no spend: the digest slot already exists, the predicate is read-time, and §46's 'persisted-shape widening declared for the veto surface' clause binds the circle/dock FEATURES (already in the closed catalog) and LG-2's commitment ledger, not this wave. The one gated act is LIGHTING the flag afterwards — a declared same-seed shift held by name under §764.3, exactly as Q2 scheduled it.","outputMoving":"NO at landing — dark behind circleNetworkEnabled, byte-identical dormancy pinned. YES at lighting, and this must be declared loudly rather than discovered: the circle graph is ALREADY LIT for every new canon (campaignSpatialCanonize.js:124), so on any world with two circle-holders lighting the gate changes same-seed routing costs and rumor arrival — the doctrine-10 shift the volume's Q2 already calls 'scheduled, not smuggled' (dark worlds keep byte-identity; no golden moves before the declared batch).","workerKb":"~0 KB to the generation worker — CONFIRMED by an import-closure BFS over src/workers/generation.worker.js: 153 modules, zero under spatial/ or worldPulse/. WORKER_BUNDLE_CEILING_BYTES = 1,404,493 (monotone-down, generationWorkerLazy.test.js:78) is untouched, and teleportEdges.js is imported only by spatialDigest.js in the lazy canonize chunk. LG code lands in the advance/engine graph (advanceInterval.worker.js → worldPulse/advanceInterval.js), which carries no byte ceiling. The only first-paint cost is the virtual key's string in ENGINE_GATED_VIRTUAL_RULE_KEYS, which rides the eager store slice through normalizeSimulationRules — tens of bytes against the measured +252 B the nine WAVES cost. Nothing needs a lazy chunk.","reuse":"The injected-predicate idiom is the estate's own and fits exactly: supplyShipments.js:198-255 `routeIntercepted(path, isHostileToDestination)` and commodityFlow.js:418-533's `hostileToDestinationFor`, both supplied by supplyKernel.js:256 from `atWar(graph, gateId, destId)`. The relationship read needs no new vocabulary (Q1): relationshipTypeBetween at roads/embassyHazard.js:36-54 with the O(1)-amortized tickIndices.js:393 index, atWarWith at :413, and the existing rungs 'allied' / 'trade_partner' / 'patron' / 'client' / 'vassal' / 'hostile' (warCosts.js:422-430, anticipatedReactions.js:104). The dormancy and byte-identity pattern is seaLanes/M9c verbatim (activeTeleportEdges' version gate at distanceRead.js:361-367). The flag bill is readable off the last mint's diff — grep irregularForceEnabled for the 15 named surfaces, which is precisely the practice the contributionLedgerShape comment says works."},{"wave":"LG-1 — GRAPH FORMALIZATION (the predicate as an edge attribute, capacity as edge cost, the relay guarded, the §125.1 toll arm)","status":"PART-BUILT","carsLow":3,"carsHigh":5,"reason":"The transitive relay the owner named in §46 ('A marches to B, teleports B→C') is BUILT and derived, not special-cased: augmentedAdjacency (distanceRead.js:790-806) folds the teleport edges into the land graph and the deterministic Dijkstra at :833 produces the relay for free. LG-1 therefore does not build it; it GUARDS it, which is a pin plus a negative control (flip one relationship and the relay must disappear), and that is the volume's own wording. The genuinely owed cars: (1) the predicate promoted from LG-0's filtered view to a typed edge attribute on the bloc view ({between, cost, capacity, passage}), which is the shape the toll and the later §125a preemption arm both read — one car, and the thing that stops LG-5 from re-opening the graph; (2) capacity as edge cost — the FIRST consumer of TELEPORT_EDGE_CAPACITY, recorded at teleportEdges.js:75 and unread since M9c, mirroring navalLayer.js:316-340 (`seaLaneCapacityOf` + `convoyCapacityAvailable`) verbatim, which is the same recorded-but-unenforced-capacity problem already solved once; (3) the relay pin with its negative control, which can ride car 1 but which the volume names as its own obligation; (4) the toll arm — allies free, trading partners tolled, the host booking the income — which needs no new money surface: embattlement.js:156-162 `tollRateOf` and :354-357's pass-through toll term in `scoreRoute` are the seam, and the income head is the closed eight-member TAX_FORMS' `market_tolls` (treasury.js:269-272), so no ninth form is minted and FINITE-SEMANTICS holds. 3 cars if the relay pin rides the attribute car and the toll rate ships as a plain coefficient; 5 if the toll rate is expressed as a BAND (which drags in Q10) and the capacity throttle needs its own deferral-visible arm the way the convoy layer did.","dependsOn":"LG-0, hard — the flag and the per-tick bloc view are its substrate (volume §4 ladder; ODQ:3778 records the same ordering for lg-2, 'additionally sits behind LG-0 and the Q10 prefix'). The toll arm additionally depended on ODQ §125.1's first proposal, which was vetoable and never separately ruled; it is now signed at §931.6b item 5, so it is buildable. Two things to NAME and not build: §125a's wartime full-preemption is a dead arm until LG-5 has military traffic to preempt (it also has nothing to act on until this wave's capacity throttle exists), and any toll expressed as a BAND waits on Q10's band-walker prefix landing in the next infra act — an LG band landing before that prefix is a STOP (the pre-cured CR-HB0B-BANDGAP class).","ownerGate":"None — signed under the 14:4x grant, with §125.1 now signed at §931.6b item 5. No migration, no persisted shape, no new dial, no real-money surface: the toll is in-world income through an existing taxation head. One coefficient goes to the chair with its executed derivation under L-LG-7 / §42/§43 (a band without a derivation is a promotion STOP, never an implementer's number). The two standing code-side deferrals this wave inherits stay as built and are asked only if they bind: whether a ruined or besieged holder leaves the circle set (ruinFilterRoster.walker.test.js:188 records siege as node-starvation, not edge-severance, which teleportEdges.js:46-51 endorses), and navalLayer.js:69-73's AFFORD_MIN floor, which belongs to LG-2 rather than here.","outputMoving":"NO at landing — dark behind circleNetworkEnabled, and the toll arm is dark behind that flag AND the treasury surface, whose lighting is itself an open owner row, so it books nothing either way. At lighting it moves output twice over: the capacity throttle can defer a passage that today always succeeds, and the toll changes route CHOICE through scoreRoute's effectiveCost, which is a same-seed shift on any lit world with a tolled circle. Both ride the same declared batch as LG-0's gate rather than a second one.","workerKb":"~0 KB to the generation worker, same measurement and same reason as LG-0 (no spatial/ or worldPulse/ module is in the worker's 153-module closure; the ceiling is 1,404,493 B monotone-down). The capacity throttle and the toll term live in spatial/ and worldPulse/, i.e. the advance/engine graph. If the passage attribute is widened onto the FROZEN slot rather than the per-tick view it would also enter the lazy canonize chunk via teleportEdges.js — do not do that: the attribute is per-tick by L-LG-3, and freezing it would both break the recompute law and put politics into a canon that outlives it. Nothing needs a lazy chunk.","reuse":"navalLayer.js:316-340 is a line-for-line template for the capacity car — the same 'recorded but unenforced capacity gets its first consumer' problem, solved with a deferral-visible answer. The toll rides embattlement.js's existing pass-through toll (tollRateOf at :156-162, summed over intermediaries only at :354-357, deliberately not risk-discounted) and entrepots.js's self-balancing toll economics including its 'Customs House' institution at :114; treasury.js:269-272's TAX_FORMS gives the income head. The relay guard needs no new fixtures: tests/domain/teleport.test.js:180-204 already builds the land-isolated and siege-cut bloc cases the negative control inverts."}],"firstCar":"The first car is LG-0V, the volume fold with a re-graded substrate — docs only, no test movement, no output, and it is first because without it every later LG packet is defective by the volume's own §6. Create `docs/DESIGN_FP_ARCH_LG.md` on the build slot (`claude/composite-r4` at `a5876c0ea`): it has never existed build-side, so this is a CREATE, not a merge (ODQ §148.3 says so and §151.4 assigns the fold to lg-0's own member), and the 12,610-byte ledger blob at `a803dee7a` is the source, CLAIM_RE-checked against code as it is copied. Then do the part that makes this car necessary rather than ceremonial: the volume binds itself to a graded substrate annex, `laneLGR-substrate.md` §§1-11, to be landed as `docs/implementation/preverification/LG-SUBSTRATE.md`, and §6 makes every LG packet cite it by sha and grade any row its manifest touches — that file exists in NO preserve ref, in no commit in this repo, and `docs/implementation/preverification/` does not exist on either branch; it went with the 09-12 scratchpad. So write the annex fresh from the code that now exists, as a re-grade rather than a recovery, and record in it that the original is lost and this supersedes it. Six rows change verdict and each needs its citation: Q2's willingness gate is still owed but the graph beneath it is BUILT and LIT for every new canon (`teleportEdges.js:193-213`, `campaignSpatialCanonize.js:124`), so the defect it repairs is live today rather than hypothetical; Q9 is already satisfied, both features being in the closed catalog (`institutionalCatalog.js:1419/:1987/:2248`), so LG mints nothing; the capacity ATTRIBUTE of LG-0's third bullet is already recorded per edge (`teleportEdges.js:75/:203`) and only its read remains, in LG-1; the transitive relay of §46 is already derived through `augmentedAdjacency` and the Dijkstra at `distanceRead.js:790-806/:833`, so LG-1 guards it instead of building it; Q1's 'no new relationship vocabulary' is confirmed by the live rungs at `warCosts.js:422-430` read through `embassyHazard.js:36-54` and `tickIndices.js:393`; and the four-obligation flag bill's fourth surface is 35, not the 22 of §148.2 or the 28 the first pass recorded — `tests/domain/contributionLedgerShape.test.js` asserts `toHaveLength(35)` twice at about line 120 while its test title at :49 still reads 'at 28', which is itself worth a line in the annex because the title is what a reader greps. Close the car with the one architectural note LG-0's next car has to start from: every teleport read is a pure function of the deep-frozen digest memoized on digest identity, so the willingness predicate cannot live inside `candidateRoutes` or `augmentedAdjacency`, and L-LG-3's per-pulse recompute means the answer is a per-tick bloc view keyed by (digest, bloc signature) rather than the cheaper filter-after-the-fact that `supplyShipments.routeIntercepted` would suggest. Land it with the provenance row and the LG volume's sha, and nothing else moves."}}
{"type":"started","key":"v2:f8e975a512737995af3ef5a94d9dad9c720985d2a8dac25aad0b82b0fba3aa04","agentId":"a9cb1d82500244aa7","label":"refute:TR","phase":"Refute"}
{"type":"started","key":"v2:cdd0a2815f58bcdff8eb15b91a7d8c6a4210cc4cb3ed43e329f3ae44a934b135","agentId":"a21425ce437a3a426","label":"refute:WF — FAITH (docs/DESIGN_FP_ARCH_WF.md; FP program 5 of 6)","phase":"Refute"}
{"type":"started","key":"v2:0d6406c6fecbd55ebabec7100ba2cdbdfbdb1b4281bf6588d832fe8d6ba927fe","agentId":"a01fded516ce98f78","label":"refute:POP","phase":"Refute"}
{"type":"started","key":"v2:ab8a29b0e450de141a5c995c2918fc2fc7af5c85e0320191bcdb3e5abcd5eddf","agentId":"aa34b3f704e8c253c","label":"refute:WY — wayfare (the inhabited roads, the physical flows, the mirror law)","phase":"Refute"}
{"type":"started","key":"v2:5c2163fc8ff5388068d7d62eb959abe3dbf1941764d70e0aaa6510509180fb2d","agentId":"a166442fb62b831d9","label":"refute:IN — INFORMATION (docs/DESIGN_FP_ARCH_IN.md, governing volume DESIGN_FP_INFORMATION.md; SOL_QUEUE A2 row 13)","phase":"Refute"}
{"type":"started","key":"v2:f70ff8ff048f6e2265c86fc848becc21299e688e81d709018381ff18d202d2e2","agentId":"a1d56aa9d14a6aadb","label":"refute:WC — war circulation (DESIGN_FP_ARCH_WC.md, 431 KB, 17 waves in four arcs)","phase":"Refute"}
{"type":"result","key":"v2:0d6406c6fecbd55ebabec7100ba2cdbdfbdb1b4281bf6588d832fe8d6ba927fe","agentId":"a01fded516ce98f78","result":{"family":"POP","summary":"POP is 8 of 8 UNBUILT — re-confirmed independently at `a5876c0ea` (all six flags zero hits across src and tests; none of the eight named leaves nor `couplingRegistryPopulations.js` exists). Three of the reader's four headline corrections HOLD and I re-executed each: (1) POP-4's substrate gate IS discharged — `src/domain/worldPulse/populationDynamics.js:407` reads \"WAVE P4, THE RECONCILIATION (ODQ §219.3). THE DEFERRAL ABOVE IS DISCHARGED\" and :422-425 rules that under a lit engine this lane writes population ONLY as a conserved transfer, bare decline and growth belonging to demographicsKernel.js as \"the design's one writer\"; (2) no POP file is size-blocked — `scripts/.size-baseline.json` holds 19 entries and none is POP-touched, and my own comment-stripped measure reproduces the reader's figures (demographicsMigration 380/800, spatial/migration 247, commonsVoiceKernel 214, demographicsLadder 101, demographicsObservation 96, settlementRumors 555 — note the real path is `src/domain/display/settlementRumors.js`, not `src/display/`); (3) the flag-mint bill IS 35 while its own test title still lies — `tests/domain/contributionLedgerShape.test.js:49` says \"holds the triple bijection at 28\" and :119-120 assert `toHaveLength(35)` twice, and PROGRAMME-32-34.md:434 still tells every packet to carry \"the 28-literal fourth obligation\"; the pending doors are genuinely struck (`subsystemRowsVirtual.js:595` and walker `:288` both frozen empty) and the BACKLOG is full at exactly 17/17 with `commonsVoiceEnabled` and `assizeEnabled` in it. The worker reading is also right in substance: `WORKER_BUNDLE_CEILING_BYTES = 1404493` at `tests/build/generationWorkerLazy.test.js:78` governs `generation.worker.js`, which imports only generationRequest.js and the living-content seam; POP bytes ride `advanceInterval.worker.js`, which has no byte-ceiling test at all.\n\nBUT THE READER PRICED AGAINST A TREE THAT HAS MOVED UNDER TWO OF POP'S THREE LOAD-BEARING SURFACES, and both movements cut the same way — up. FIRST, THE COLUMN-CLASS SURFACE IS NO LONGER A TWO-MEMBER LIST. WC-0B landed `MILITARY_COLUMN_CLASSES` (`spatial/migration.js:504-506`, three members), the `COLUMN_CLASSES` totality union at :509, `isMilitaryColumn` at :521, and a widened release fork at :641 spelled `if (isDemographicColumn(col) || isMilitaryColumn(col)) { next[key] = col; continue; }`. A dedicated WAR-family pin file, `tests/domain/militaryColumnRelease.test.js`, asserts `DEMOGRAPHIC_COLUMN_CLASSES` EQUALS `['refugee','voluntary']` (:64), `COLUMN_CLASSES` by exact membership (:65), the totality identity (:73-74), and disjointness (:76) — and :106-107 SOURCE-SCANS the release fork's exact line to mutate it. POP-1's Q1 ruling (extend the closed set with 'returning') therefore reds a war-family suite no POP battery would think to name: the identical trap shape the reader itself caught in contributionLedgerShape, missed one surface over. Worse, THE CLASS IS NOW PART OF THE COLUMN KEY (`enqueueColumns`, :591: the key gains a `:${planClass}` suffix whenever the class is recognised, with the authored reason that an M4 and a P2 column on the same origin/dest/tick would otherwise collide), so the return column's mint must carry the suffix or collide with the outbound column it answers. And the three `isDemographicColumn` consumers inside `demographicsMigration.js` (:458, :505, :636) all begin seeing returning columns the moment the set widens — three deliberate rulings with pins, not a spread.\n\nSECOND, THE RUMOUR-PHRASE ESTATE THE READER PRICED AS \"ELEVEN AUTHORED STRINGS\" IS NOW A TWO-CORPUS MACHINE WITH EXACT CARDINALITY PINS ON BOTH HALVES. `src/domain/display/rumorPhrasePools.js` holds `WHAT_PHRASE_POOLS` for the §3 canonical arm's 63 kinds; `rumorFallbackPhrasePools.js` plus `rumorFallbackPhrasePoolsEvents.js` (a writer FAMILY) hold `FALLBACK_PHRASE_POOLS`, `FALLBACK_CANONICALS` and `FALLBACK_WIRED_KINDS` for §4's 107; `tests/domain/rumorFallbackPhrasePools.test.js:220-221` pins both `Object.keys(PARSED).length` and `FALLBACK_WIRED_KINDS.length` at exactly 107, with a byte-identity arm at :521. Promoting a kind into WHAT_PHRASES is now a MIGRATION BETWEEN TWO PINNED CORPORA — 107 and 63 both move in the same commit, the kind's authored CADENCE-floored fallback pool retires, a WHAT_PHRASE_POOLS entry is authored under the estate's four-variants/never-trim law, and CANONICAL-AT-ZERO must hold across the move. And the reader's specific mechanism claim is REFUTED at the slot: tier_change, steading_founded and steading_lost are in neither corpus and not in TRANSITION_KINDS (`settlementRumors.js:493-496`), so `whatPhrase` returns `UNREGISTERED_SUBJECT = 'a matter of some moment'` (:517, :571-576) — they are NOT \"de-underscored engine tokens\" today; the §4 strip arm is explicitly \"no longer reachable for an unregistered one\". Meanwhile two kinds the reader listed as unwritten, `population_decline` and `population_growth`, ARE already voiced with authored pools inside the 107, and two more the volume's S30 called absent — `settlement_resettled` and `settlement_terminal_death` — have LANDED WHAT_PHRASES rows at :474-475 (as has `steading_forced` at :477). POP-6 is output-moving at landing, as the reader says, but for a different reason and across a larger surface.\n\nThree smaller corrections. The WR-4 conscript-share read the reader cited at `warHomeCosts.js:354-362` is actually at `warCosts.js:370-377`; the levy BANK that POP-2's shortfall must arrive smaller at is `warHomeCosts.js:445-452` — matching the volume's own POP-F note, not the reader's line. POP-2's ledger row is REBUILT FROM SCRATCH every tick (`commonsVoiceKernel.js:305`, a fresh `const rec = {...}` with only `accusedNid` conditional), so `targetKind`/`targetRef` are ghost fields by a SECOND mechanism the volume's RF-1 never names — and the write is guarded by a JSON serialize-compare no-op guard at :352-358 whose authored purpose is to stop a standing grievance churning worldState every tick, so any per-tick-varying new field defeats no-op detection: POP-2 owes a fixed-point pin nobody has priced. And POP-5b's \"four calling surfaces\" are not four insertions in one family — the levy is WAR's, institution seeding is the `institutionLifecycle*` family, and trade/caravan assignment is the twenty-file `routeNetwork*` lane owned by FP-TRADE (which the volume itself says may have to be \"declared into FP-TRADE's seam — report, don't duplicate\"); only the promotion read at `demographicsResponses.js:296-297` is POP's own. Finally, DESIGN_FP_ARCHITECTURE.md's coupling table adds obligations the reader omits: row 31 (SC-6) makes every state-writing FP wave populate `causedBy` at the writer with CW-1 depth fixtures — POP-1, POP-3, POP-4 and POP-5a each owe it — and row 34 (SC-9) requires POP-7 to sample its OWN receipt-field rows or CW-3 reds them UNOBSERVED.\n\nSubstrate reuse is as strong as the reader reports and I re-verified the load-bearing pieces: `MIGRATION_REFUSALS` at demographicsMigration.js:107-109, the refusal mint at :231-237, `destinationMenuFor` still the one menu builder at :276 with viability and the spare/inbound read inside it, the 300-tick conservation identity at `tests/domain/demographicsMigration.test.js:209` with its leak-detecting negative control at :234, the 5x6 permit table and `moverPermitted` at `demographicsLadder.js:79/:209` with exactly one consumer at `demographicsResponses.js:244` and direct pins at `demographicsWorldsHand.test.js:533-585`, GR-3's terms live at `peaceTermsCatalog.js:250/:254` with the reader at `treatyEnforcement.js:301`, no POP leaf in the six-leaf coupling registry composition, and the POP prefix already inside the closed twelve-prefix alternation (DESIGN_FP_ARCHITECTURE.md:2645) so the four-edit-site admission ceremony is genuinely spared. Every wave still lands dark behind its own flag except POP-6.\n\nCORRECTED FAMILY PRICE: 32-45 cars, against the reader's 22-34. Seven of eight waves are TOO LOW; none is TOO HIGH; POP-6 is the single largest correction (4-6 to 6-9) and is the one wave whose verdict I moved for a reason the reader had backwards rather than merely under-counted.","verdicts":[{"wave":"POP-1 — THE BELIEVED ROAD (believedMigrationEnabled)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"The reader's shape is right and its RF-1/RF-2 reading is right, but it priced the Q1 class extension against the volume's two-member world. At the slot the column-class surface is three structures deep: DEMOGRAPHIC_COLUMN_CLASSES (spatial/migration.js:474), MILITARY_COLUMN_CLASSES (:504-506, WC-0B) and the COLUMN_CLASSES totality union (:509), with isMilitaryColumn at :521 and the release fork widened at :641. Adding 'returning' therefore reds tests/domain/militaryColumnRelease.test.js at :64 (DEMOGRAPHIC_COLUMN_CLASSES by exact equality), :65 (COLUMN_CLASSES by exact membership), :73-74 (the totality identity) and :76 (disjointness) — a WAR-family suite no POP packet names, the same trap class the reader found in contributionLedgerShape:49. That file also SOURCE-SCANS the release fork's exact line at :106-107, so the wrong-lane mutant must be re-cut against the widened predicate rather than the volume's. Two more costs the reader missed: the class is now part of the column KEY (enqueueColumns :591 appends ':<class>' for any recognised class, with the authored collision reason), so the return column's mint must carry the suffix or collide with the outbound column it answers; and widening the set turns on all three isDemographicColumn consumers inside demographicsMigration.js (:458 the stage-0 read, :505, :636) at once, each a deliberate ruling with its own pin, not a spread. Add SC-6's causedBy obligation at the new writer (DESIGN_FP_ARCHITECTURE.md coupling row 31) and SC-1's same-commit registry row for the new cross-layer import pair (row 27) on top of the coupling desk leaf the reader already prices. The reader's six-car split is sound as far as it goes; the class work is a car of its own that the split folds into car 1, and the mutant battery plus the full gate will not fit in the sixth. Everything else re-verified: reception machinery landed, destinationMenuFor still the one builder, the 300-tick identity and its :234 negative control live to be EXTENDED, the POP prefix already admitted."},{"wave":"POP-2 — THE COMMONS ARC (commonsArcEnabled)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"Two structural costs are absent from the pricing. (a) THE REFUSAL RUNG IS A STATE-MACHINE WIDENING, NOT A CONSTANT EDIT. nextRung is hard-clamped Math.max(0, Math.min(3, nextRung)) at commonsVoiceKernel.js:300, and RUNG_KIND and RUNG_WORD are parallel four-element frozen arrays at :184-185 indexed by the same rung; widening LEGIT_DIP from four slots to five (:63) without moving the clamp and both parallel arrays in lockstep produces a rung that can never be entered, and moving them changes headlineFor/summaryFor and the S19 jump-rule baseline the wave's own defining pin is written against. (b) THE PERSISTED FIELDS ARE GHOSTS BY A SECOND MECHANISM NOBODY HAS NAMED. The row is rebuilt from scratch every tick — :305 constructs a fresh `const rec = { rung, since, grievance, kind }` with only accusedNid conditional — so targetKind/targetRef vanish next tick unless explicitly carried from priorLedger. That is RF-1's hazard class at a site RF-1 does not cover, and the volume's '+ fields on the existing row shape' sentence hides it. Worse, the ledger write sits behind a JSON serialize-compare no-op guard at :352-358 whose authored purpose is to stop a standing grievance churning worldState every tick; a target field that varies per tick defeats no-op detection outright, so the wave owes a fixed-point pin (an answered petition must reach byte-identical serialization) that appears in neither the volume nor the reader. Also correcting the reader's VERIFY-AT-BUILD cite: the WR-4 conscript-share read is warCosts.js:370-377, and the levy bank the shortfall must arrive smaller at is warHomeCosts.js:445-452 — matching the volume's POP-F note, not the reader's :354-362. The rest of the reader's bill stands and is expensive on its own: commonsAnswer.js, both reversal arms (the corruption-only version is vacuous by S22), the three-walker registration for two new kinds, the wizard-news skew prohibition, the six-site flag mint, and every pin seeding live grievance state first. Reader's low of 3 is unreachable; its high of 5 is the realistic floor."},{"wave":"POP-3 — DEPARTURE MEMORY (departureMemoryEnabled)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":4,"why":"The reader's own description defeats its low. This wave adds a PERSISTED ledger key and then discharges the L4 bill on all six paths — CREATE at four feed sites through one writer, READ on two arms, PERSIST with JSON round-trip plus import validation of three closed sets (kinBand, causeClass, rememberedAs), REGENERATE as a zero-PRNG same-seed re-derivation, UNDO through the pulse ring, MIGRATE as drop-when-empty proved BYTE-ABSENT — and then adds freeze-at-death and reopen-at-resettlement extending S27, the kin-cap negative, the spent-tie convergence loop, the adoption double-count negative against traditions' live influx checkpoints, and the writer/reader spelling pin that boots the real writer. On top of that sits the flag-mint bill the reader itself measured at six exact edit sites in one commit, one of which is an authored certification row of the ~60-line conquestDoctrineEnabled shape (subsystemRowsVirtual.js:113-164 is the neighbour to copy), plus the four-fence dormancy set with its lit-mutant control, plus two executed manifest-entered mutants, plus SC-6's causedBy obligation at the new writer. That is not two sittings. One further correction: the reader calls POP-3 'independent of POP-1 and POP-2 and schedulable in parallel with either.' Its P2 CREATE site is the landing stage of demographicsMigration.js — the exact function POP-1 inserts THE ARRIVAL CLEARING into — so the two serialize by order on one function even though neither blocks the other logically, and POP-1's returning columns are a new causeClass/rememberedAs case POP-3 must rule rather than discover. The reuse claims are all real and re-verified (generic getSpatialLedger/setSpatialLedger with no key registry, free v5 census visibility per S32, traditions' ADOPTION_THRESHOLD checkpoints live), which is why the high stays at 4 rather than 5."},{"wave":"POP-4 — THE PLAGUE ARC (calamityArcEnabled)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":4,"why":"The reader's central finding is CONFIRMED and it is the best thing in the submission: I executed the read, and populationDynamics.js:407 now says 'WAVE P4, THE RECONCILIATION (ODQ §219.3). THE DEFERRAL ABOVE IS DISCHARGED', with :422-425 ruling that under a lit engine this lane writes population ONLY as a conserved transfer because bare decline and bare growth are deaths and births by another name and belong to demographicsKernel.js, 'the design's one writer'. The volume's STOP-and-report order has fired and its answer is the opposite of what the volume assumes, so the split-attribution arm genuinely collapses and the ZERO-NEW-MORTALITY pin gets easier to write honestly. But the reader then repriced the WHOLE WAVE on the strength of one arm. What the discharge removes is a set of attribution negatives; what it leaves untouched is the entire cost: a new spatialLedgers key carrying the same six-path L4 bill POP-3 carries (plus stamp-ordering import validation that openedTick <= crestTick <= closedTick, plus mid-arc terminal close through the lifecycle path, plus the prune-and-drop-when-empty arm), a new leaf running an open/crest/close/aftermath state machine, the burial-line generalization in demographicsHerald.js with THE BELLS PIN preserving the food gate for hunger, the first-market-day walk-backward fixture, the named-cast pin under arc conditions, the six-site flag mint with its authored certification row, the four-fence dormancy set, two executed mutants, SC-6's causedBy, and the FAITH omen coupling row. That is POP-3's bill plus a state machine plus display work, and POP-3 is not a two-car wave either. The evidence source needing no edit (pestilenceKernel.js as a genuine traveling front minting the one canonical disease_outbreak stressor) is real and is why the high stays at 4."},{"wave":"POP-5a — ROAD DRAMA (roadDramaEnabled)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":5,"why":"The reader correctly identifies why this wave is priced above its file count — a new PRNG fork family is the expensive kind of change in an estate where call order IS stream identity under THE PROMISE, so the draw accounting against the wave-E instrument is load-bearing rather than paperwork — and the re-route pin written as a route-choice assertion with populationLegCost byte-identical is exactly the right fence. Two things push the range up. First, the events[] carry-through does not compose onto the clean two-member whitelist the reader pictures: by the time POP-5a runs, columnOf is carrying a class drawn from a five-member union across two owner families, the key itself is class-suffixed at enqueueColumns:591, and the exact-equality and totality pins in tests/domain/militaryColumnRelease.test.js sit over all of it — so the RF-1 round-trip-through-enqueue pin for events must be written against a surface two lanes now share, and the split-column conservation mutant has to survive military columns in the same ledger. Second, SC-6's causedBy obligation (coupling row 31) lands here too, at the event writer, with CW-1 depth fixtures. The conservation bill the reader names is correctly sized — EXTENDED-UNWEAKENED with every kind closing into the identity, split sums exact, event fixtures added to the existing 300-tick pin, the leaked-half-column mutant mandatory — and the reuse is genuine (the conditional-spread idiom, the class- and direction-agnostic rumour carrier per S29, the keyed hash01 fallback already imported in demographicsPlans.js). The dependency call is right and I verified it: DESIGN_FP_ARCHITECTURE.md:2043-2044 slots WY-4 immediately after POP-1 precisely because the shared columnOf amendments compose BY ORDER, and POP-5a's events land on the same surface. Reader's low of 3 is the optimistic case with the fork accounting going first-try; 4-5 is the honest band."},{"wave":"POP-5b — THE PERMIT TABLE GOES LIVE (moverPermitsEnabled)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":5,"why":"'The cheapest wave in the family by construction' is right about the CODE and wrong about the WORK, because the reader counted four read insertions without asking whose files they are. Only one of the four is POP's own: the promotion gate at demographicsResponses.js:296-297, which today reads no_next_tier then nextTierHeadroom and gains a grade read beside them. The levy is WAR's (the bank at warHomeCosts.js:445-452, upstream of the conscript-share read at warCosts.js:370-377 — and the volume's warDeployment.js:1259 cite is dead, though the file itself survives at 1338 lines); institution seeding is the institutionLifecycle/institutionStatusLifecycle family; and trade/caravan assignment is the twenty-file routeNetwork* lane owned by FP-TRADE, which the volume itself hedges as 'or declared into FP-TRADE's seam if that program rebuilds it — report, don't duplicate'. A read insertion into a foreign family in this estate costs that family's own gate, its own dormancy fence at the site, and both-arm corpora proof on REAL corpora with a seeded failing-town fixture — three times over. On top: the J-POP-11 narrowing at destinationMenuFor's viable read (which must keep the dark path byte-identical), two treaty reads now REQUIRED-from-birth rather than declared-degraded (DESIGN_FP_ARCHITECTURE.md:2636 row 23 orders exactly that for the GR-3→POP-5b grant read, and GR-3 is landed at peaceTermsCatalog.js:250/:254 with the reader at treatyEnforcement.js:301), the levy/consent double-bind fixture producing ONE muster receipt naming TWO distinct reasons with its collapse mutant, the seatBooks declared-future-consumer row (coupling row 24), the six-site flag mint, and four CW-0 rows — CPL-3/CPL-8/CPL-17/CPL-18 — which cannot land without a POP coupling desk leaf, and no such leaf exists (couplingRegistry.js composes only War, Trade, Grammar, Info, Espionage, Encounters). The reader assigns that leaf to POP-1 but records no POP-1 dependency here, so scheduled ahead of POP-1 this wave owes the leaf itself. The reuse ratio genuinely is the family's best and I re-verified it: the 5x6 table and moverPermitted are authored, closed on both axes and already directly pinned at demographicsWorldsHand.test.js:533-585 with exactly one live consumer. The two struck ceremonies are correctly struck."},{"wave":"POP-6 — THE HOPEFUL HALF (rides its machinery's flags)","verdict":"TOO LOW","correctedLow":6,"correctedHigh":9,"why":"This is the largest correction in the family, and the reader has the mechanism backwards. It prices eleven WHAT_PHRASES rows as 'authored strings, expect well under 1.5 KB minified'. At the slot the rumour-phrase estate is a TWO-CORPUS MACHINE WITH EXACT CARDINALITY PINS ON BOTH HALVES: rumorPhrasePools.js holds WHAT_PHRASE_POOLS for the §3 canonical arm's 63 kinds; rumorFallbackPhrasePools.js plus rumorFallbackPhrasePoolsEvents.js (a writer FAMILY, not a file) hold FALLBACK_PHRASE_POOLS, FALLBACK_CANONICALS and FALLBACK_WIRED_KINDS for §4's 107; and tests/domain/rumorFallbackPhrasePools.test.js:220-221 pins Object.keys(PARSED).length AND FALLBACK_WIRED_KINDS.length at exactly 107, with a byte-identity arm at :521 and the 63 governed by rumorPhrasePools.test.js. Authoring a WHAT_PHRASES row is therefore a MIGRATION BETWEEN TWO PINNED CORPORA — both cardinality literals move in the same commit, the kind's CADENCE-floored fallback pool retires, a WHAT_PHRASE_POOLS entry is authored under the estate's four-variants/never-trim law, and CANONICAL-AT-ZERO must hold across the move, which means the SEEDED pool changes and not merely the canonical string. The reader's specific claim is refuted: tier_change, steading_founded and steading_lost are in NEITHER corpus and not in TRANSITION_KINDS (settlementRumors.js:493-496), so whatPhrase returns UNREGISTERED_SUBJECT = 'a matter of some moment' (:517, :571-576) — the §4 strip-and-de-underscore arm is explicitly 'no longer reachable for an unregistered one', so they are not printing engine slugs today. Conversely two kinds the reader classified as unwritten, population_decline and population_growth, ARE already voiced with authored pools inside the 107, so promoting them is a disclosed same-seed shift on kinds the reader filed as dark; and settlement_resettled (:474), settlement_terminal_death (:475) and steading_forced (:477) have LANDED rows since the volume's S30, shrinking the mint list while enlarging the per-row bill. The reader's conclusion that POP-6 is the family's one output-moving-at-landing wave SURVIVES and its J-POP-12 separate-commit discipline is right; the shift is simply larger and differently caused. R-28 is CONFIRMED still open and MOVED as the reader says — demographicsHerald.js:420, `quantityWords(departures || unplaced) + ' left ' + origin`, with the departures=0 arm still saying 'left', and a second instance at :422. Two new leaves, the editor wrapping demographicNewsEntries' RETURN so S3's :402 bypass stays intact, three-walker registrations, the flood pin, the dwindling-window arithmetic left as VERIFY-AT-BUILD, and honesty pins on every minted headline verb all stand on top."},{"wave":"POP-7 — CONVERGENCE INSTRUMENTATION (no flag)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":5,"why":"The reader's two structural clarifications are correct and worth keeping: C-POPF-1 did strike the pending door, so the six per-flag certification rows land in their own waves and not here (I confirmed both pending surfaces frozen empty at subsystemRowsVirtual.js:595 and walker :288), and the commonsArcEnabled row must declare stateKeys channels only because S20's wizard_news-shaped beat ids would skew any moverFamily claim. Q2's item is also confirmed live: the BACKLOG is full at exactly 17/17 shrink-only with commonsVoiceEnabled sitting in it beside assizeEnabled. But the low of 2 cannot survive contact with the bill. Endings-mix envelopes over seventeen producer-mapped tokens, six health metrics (rush-to-bust band, no-permanent-rush horizon, rung pyramid, THE PARITY METRIC, memory-decay stationarity, conservation UNCHANGED with every flag lit), and EVERY ONE of them carrying its own executed mutant negative control — an envelope that cannot fail is the false-instrument class this estate refuses — is not one sitting, it is the bulk of the wave and each control is a cp-backed, cmp-restored, manifest-entered mutant. Add the convergence certification rows with dispositive stateKeys channels, the commonsVoiceEnabled row and backlog shrink, the v5 rush/commons/arc censuses joining measureRealmDemography (live at demographicsObservation.js:103, 96 effective lines, genuinely roomy), and one obligation the reader omits entirely: DESIGN_FP_ARCHITECTURE.md coupling row 34 (SC-9) requires each convergence wave to sample its OWN receipt-field rows, with CW-3 redding never-sampled rows as UNOBSERVED — POP-7 is named in that row explicitly. The soak read-out and acceptance write-up is correctly called a sitting of its own if the envelopes do not hold first time. The reader's two gates are right and important — the lighting the envelopes require is the owner's §764.3 amendment and the soak is owner-ordered — as is its insistence that CR-FP-6 (RULED at DESIGN_FP_ARCHITECTURE.md:2938, 'ACCEPT NO — deliberately deferred beside J-POP-14... this goes on the owner queue, not into any wave') must be recorded as deferred here rather than quietly absorbed."}]}}
{"type":"started","key":"v2:675ffe40c34549bfabe8c9c9b9574125b624969a0f65484b90ef86edad5c1641","agentId":"a2e32bfaab3636a58","label":"refute:HB","phase":"Refute"}
{"type":"result","key":"v2:ab8a29b0e450de141a5c995c2918fc2fc7af5c85e0320191bcdb3e5abcd5eddf","agentId":"aa34b3f704e8c253c","result":{"family":"WY — wayfare (the inhabited roads, the physical flows, the mirror law)","summary":"SKEPTIC PASS, family WY, re-derived from docs/DESIGN_FP_ARCH_WY.md (ledger a803dee7a, 1,721 lines) against code at the NEW slot a5876c0ea (/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-headroom). VERDICT: 12 of 13 rows TOO LOW, 1 CONFIRMED, 0 WRONG DEPENDENCY. FAMILY 44–67 cars, not the reader's 34–55.\n\nWHAT THE READER GOT RIGHT (all re-measured, CONFIRMED): (1) All 13 rows UNBUILT — eight flags plus `kmScale` return 0 files across src and tests, and all seven greenfield modules are absent (`find src tests -name modeSpeeds|severityDurability|flowRegimeContract|moverTidings|armyCaravanSeizure|armySupply|*Projection` → empty). (2) ⛔ THE ROADSKERNEL RATCHET IS REAL AND HAS ZERO SLACK: `src/domain/worldPulse/roadsKernel.js` measures exactly 838 effective lines (skipBlankLines+skipComments, the eslint max-lines semantics) against `scripts/.size-baseline.json`'s frozen 838. Eight of WY-3's nine F3 stamps land there. (3) The F3 census is EXACT: writes at roadsKernel.js 487, 510, 706, 720, 731, 749, 812, 1028 plus `roads/state.js:189`; seaRoads.js holds three reads only (96/103/107). (4) ⛔ THE WORKER CLAUSE DOES NOT BIND WY — an independent static-import BFS from `src/workers/generation.worker.js` reaches 219 modules with ZERO `domain/worldPulse/` and ZERO `domain/spatial/`; `advanceInterval.worker.js` reaches 545 (371 worldPulse, 25 spatial) and carries no byte ceiling anywhere in tests/build. (My module counts differ from the reader's 227/560 by resolver strictness; the load-bearing zero is identical.) (5) WY is ALREADY in `CHARTERED_VOLUME_PREFIXES` at tests/domain/couplingRegistry.test.js:108 AND in the `toEqual` at :587 — §5b item 7 is PAID in both sites. (6) `hasMartialRecord` is exported at martialReadiness.js:256 and consumed at WarTab.jsx:48; the duplicate local survives at opportunism.js:122. (7) WarFaithTab.jsx is gone (WarTab.jsx / FaithTab.jsx / WarFaithDesk.jsx). (8) The WarFaithSection phantom census is still TWELVE src+test files — though membership drifted (FaithTab.jsx now carries the spelling WarFaithTab.jsx used to). (9) CONDITION_BANDS is module-local at armyStrength.js:133, re-exported only inside ARMY_STRENGTH_PHRASES at :237, whose one importer (forceComposition.test.js:12) reads STRENGTH_BANDS — near-free, not literally zero. (10) The F9 doc contradiction is live: slot DESIGN_FP_ARCHITECTURE.md:669 \"UNSIGNED\" vs ledger :669 \"⏱ SIGNED\", with the slot self-contradicting at :678/:1891. (11) travelersFilter is undeclared and `setLayerFilter` has no guard while `toggleLayer` does.\n\nFIVE CORRECTIONS THE READER OWES THE LEDGER. (a) DEFAULT_LAYERS carries **22** keys, not 23 — 17 boolean rows and 5 config keys (relationshipFilter, chainFilter, regionalChannelFilter, regionalImpactStatusFilter, regionalMinSeverity). The walker's anti-vacuity floor is 17, measured at build. (b) `setLayerFilter` is at mapSlice.js:**382-384**, not :328-330 — navigate by symbol. (c) `flows.js` is `src/domain/worldPulse/flows.js`, not spatial/; supplyWebWarfare measures **542** eff, not 538. (d) ⛔⛔ THE BYTE PINS THE READER LEANS ON ARE DIST-GATED AND SILENTLY SKIP. `tests/build/vendorPdfLazy.test.js:957` is `it.skipIf(!requireDistRead)` and `tests/build/generationWorkerLazy.test.js:267` is `describe.runIf(DIST_EXISTS)`. I CONFIRMED the constants (CLOSURE_BUDGET_BYTES = 1_048_000 at :565; WORKER_BUNDLE_CEILING_BYTES = 1404493 at :78) but the reader's ~1,047,205 first-paint figure is PLAUSIBLE ONLY — unverifiable read-only, and on a box without dist both ceilings report green without running. Every WY surface ledger row must quote a measured build, not a suite line. (e) WY-7's D9/D10 convergence is bigger than the volume's \"five eligibility spellings and four nameFor spellings\": at this HEAD I count 12+ distinct local `nameFor` spellings (pdf/lib/liveWorld.js:132, utils/generateWorldBook.js:462, utils/generateCampaignPDF.js:779, EngineSections.jsx:143, CampaignFolder.jsx:97, livingWorldSignals.js:189, AdminSimTuningPanel.jsx:124, HeraldBody.jsx:113, RelationshipChronicleSection.jsx:148, heraldWanderers.js:292/:320, PerspectiveStandings.jsx:60).\n\nTHE SYSTEMATIC PRICING ERROR. Six rows (WY-3, WY-4, WY-5, WY-6, WY-8a, WY-11) carry a `carsLow` BELOW the number of cars the reader's own `reason` field enumerates — WY-3 names five cars and prices low at 4; WY-4 names five and prices low at 3; WY-8a names five and prices low at 3. A car is one gated commit with its proofs; an enumerated car cannot be priced away. Separately the reader under-prices two estate-wide per-wave taxes: every flag-minting wave owes its certification row in `subsystemRowsVirtual.js` WITH its `tests/domain/subsystemRowsVirtual.test.js` twin in that wave's own commit (sovereigntyLightingContract.walker.test.js:4598 makes this binding on \"EVERY flag-minting packet from here on\"), and every executed mutant owes a `tests/lint/mutationCoverageManifest.test.js` entry. Eight WY flags means eight certification-row+twin obligations and roughly twenty-five mutants across the family.\n\nWHAT DID NOT MOVE. No WY row is a push, deploy, migration, deletion or spend — every field is drop-when-absent with MIGRATE: none, so the §2a batch clears the family under the 14:4x grant. Only WY-0 (car 2's deletions) and WY-7 (declared unifications, including disclosed PDF print shifts) move output at landing; WY-8b moves it by record presence; the other ten are dark behind their own flag or dark by data (WY-1's absent-datum arm is the strongest form). Two owner-visible items must be restated verbatim in their ledger rows: Q3's arrow deletion (veto window open until WY-0 lands) and the r3 chair translation binding WY-8a (attrition to the army's condition, never the realm's exhaustion). No engine wave adds a byte to the generation worker; WY's only real byte constraint is the eager first-paint closure through store/index.js:49 → mapSlice.js, and it must be measured against a built dist, not a skipped suite.","verdicts":[{"wave":"WY-0 the lawful canvas (surface, no flag)","verdict":"TOO LOW","correctedLow":2,"correctedHigh":3,"why":"The reader's carsLow of 1 is not reachable and one cost is unpriced. CONFIRMED at the slot: DEFAULT_LAYERS at mapSlice.js:88 carries 22 keys (NOT the reader's 23) — 17 boolean rows, 5 config keys, so the walker's anti-vacuity floor is 17; travelersFilter is genuinely undeclared (written by setLayerFilter at mapSlice.js:382-384, which unlike toggleLayer:377-380 has NO declared-key guard) and read at LayersPanel.jsx:95-103 + TravelersLayer.jsx:40; the deletion targets are real (chevronPoints imported at TravelersLayer.jsx:25 and rendered at :148, both angleDeg+180 flips at :87 and :126, the marker-end arrow machinery at WarFaithMapOverlay.jsx:152/:196 on a default-ON public-tier surface); both shipped test files exist (tests/components/travelersLayer.test.jsx, tests/domain/roadsTravelersGeometry.test.js); no touched file is in .size-baseline.json and all sit under the 600 component ceiling (LayersPanel 364 eff, MapOverlay 284, MapLegend 180, WarFaithMapOverlay 158, TravelersLayer 112). UNPRICED: after car 2, `chevronPoints` (travelersGeometry.js:84) has ZERO consumers tree-wide — the deletion owes a dead-export disposition, not just a deletion. Car 1 (walker + declared key + merge pin) and car 2 (both deletions + anchored negatives + moved tests + the dead export) are each a full sitting, and the Q3 half must stay revertible alone with its veto window named. 2 is the floor, 3 if the dead-export disposition or the sr-only/legend fallout is argued separately."},{"wave":"WY-1 the scale charter (engine, no flag — data-gated by spatialDigest.kmScale)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":4,"why":"The reader's three named cars omit a fourth obligation I measured. The reuse read is right: hopWeeks lives at distanceRead.js (468 eff, well under the 800 domain ceiling, so no leaf split), buildSpatialDigest at spatialDigest.js (415 eff), campaignSpatialCanonize.js is tiny (98 eff), and only modeSpeeds.js is greenfield. But the volume's 'spectrum-derivation lint census (shrink-only, over the named mover-estate consumers)' sits on a 37-FILE hopWeeks surface at this HEAD (measured: MapOverlay, TravelRingsLayer, TravelersLayer, edgeAnnotations, subsystemRowsCompact, settlementPestilence, seaRoads, roads/state, roads/verification, armyTransit, distanceRead, migration, navalLayer, pestilence, rumorNetwork, spatialArrival, supplyShipments, applyWorldPulse, armyTransitKernel, beliefMap, calamityKernel, candidateEvents, distancePricedNews, migrationKernel, npcCirculationTransit, pactProposals, pestilenceKernel, pulseKernel, realmVerbExecution, roadsKernel, routeNetworkCharter, routeNetworkConsumersTransit, routeNetworkFlowsObjective, seatIntervention, sovereigntyReach, spatialUsage, mapSlice). A shrink-only census over that surface, plus §1a.3's PER-MAP derivation obligation, is a car, not a bullet. Add §5b item 9's two-site amendment to DESIGN_WAR_RULINGS_ARCHITECTURE, which must land in the SAME fold commit or the two binding volumes disagree. Dependency read CONFIRMED (SP-E landed; unblocked today) and the dark-by-DATA framing is the strongest in the family — no correction there. 3 cars minimum: mechanism+carry-forward, the instrument family, the census+war-volume fold; 4 with the creation-flow disclosure screen."},{"wave":"WY-2 the durable truth (engine, severityDurableRumorsEnabled)","verdict":"CONFIRMED","correctedLow":2,"correctedHigh":3,"why":"The one row I cannot move. It mints NO persisted field (it appears in no §2a row), NO walker file, NO display seam and NO generator code, so the TOO-LOW default does not fire. Re-measured: rumorNetwork.js lives at src/domain/spatial/rumorNetwork.js and holds 364 eff against the 800 domain ceiling — the two-arm gate needs no decomposition and the file carries no .size-baseline.json entry; the keyed-fork idiom is estate-standard and already spelled in-tree (causeLifecycle.js:343-344 `forkKey`/`rng.fork`, townMapModel.js:362, supplyWebWarfare.js:487), so the fork-key-set census copies an existing discipline rather than inventing one; ES-1 is landed so the §5b PHASE-3 composition point has passed and the wave slots freely. Only severityDurability.js is greenfield. The reader's split (mechanism car / stream-identity instrument car, two mutants across both) is the right shape, and honouring the r2 deletion of the draw-count pin is correctly flagged as the trap. The one caution for the ledger row rather than the price: the wave still owes its certification row in subsystemRowsVirtual.js WITH the subsystemRowsVirtual.test.js twin in the same commit, which is what keeps the honest floor at 2 rather than below it."},{"wave":"WY-3 the mover bodies (engine, caravanBodiesEnabled; slices 3a/3b)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"The reader's own `reason` enumerates FIVE cars (decomposition, 3a, 3b, walkers+scans, pin family) and then prices carsLow at 4 — an enumerated car cannot be priced away. Every blocker premise CONFIRMED independently: roadsKernel.js measures EXACTLY 838 eff against the frozen 838 in scripts/.size-baseline.json (eslint.config.js:96 turns each baseline entry into max-lines with skipBlankLines+skipComments), zero slack, and the nine F3 writes are exactly where the r2 census put them (roadsKernel 487/510/706/720/731/749/812/1028 + roads/state.js:189; seaRoads reads only at 96/103/107). commodityFlow.js is 448 eff with room, MIN_SHIP: 2 is at commodityFlow.js:118, and the arrive-loop smuggle precedent is in-file. What the reader under-counts: the decomposition car is behaviour-identical extraction work on the estate's most contended pulse module and ships with a stripped-block declaration diff, a strict-neutral re-measure and a baseline LOWERING — that is a sitting by itself and it must land BEFORE any stamp; three §2a fields each owe a same-commit normalizer (RF-1); the tree-wide legArrivalTick pairing walker and the marker-truth source scan are two distinct instruments; four mutants each owe a mutationCoverageManifest entry; and the flag owes its certification row plus the subsystemRowsVirtual.test.js twin. Add the TRADE serialization on commodityFlow. 5 floor, 7 if the `;`-joined net-zero alternative fails across eight assignment sites and the extraction has to be re-cut."},{"wave":"WY-4 the population cargo + the fence (engine, flowMigrationPhysicalEnabled + migrationCargoEnabled)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"Same arithmetic fault: the reader enumerates FIVE cars and prices carsLow at 3. Substrate re-measured and the reuse claim holds — flows.js is 121 eff and migration.js 247 eff, both far under the 800 ceiling and neither in the size baseline, and the wave mints no module (flowRegimeContract arrives from WY-3). But that is exactly why its cost is instruments, and instruments do not compress: TWO flags means TWO certification rows plus TWO subsystemRowsVirtual.test.js twins; TWO §2a persisted fields (F4 faithCargo, F5 causeReceipt) on the MigrationColumn, each owing the THROUGH-ENQUEUE round-trip (enqueueColumns whole-ledger rebuild + releaseArrivals + the P2 stage-0 read, never save/load) rather than a plain serialize pin; a cross-family writer into religionState's writer family under the Q1 BLEND ruling with both legacy-mirror spellings; the PRODUCER-OVERLAP census measured at build because M4-shed and DISPLACEMENT_STRESSORS are not disjoint by construction; the dedup precedence pin; four-fence twice; and FOUR mutants with manifest entries. One correction to the reader's path naming: flows.js is src/domain/worldPulse/flows.js, not spatial/. Dependency read CONFIRMED — POP-1 is UNBUILT (believedMigrationEnabled 0 hits; the src `POP-1` hits are DS-POP-1 dossier ids) and stays the hard predecessor per §5b item 3 regardless of the first pass's #33 finding."},{"wave":"WY-5 the carried word (engine, moversCarryNewsEnabled)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":5,"why":"The reader's carsLow of 2 under-prices a NEW HERALD KIND in this estate. `linesForEntity` does not exist anywhere in src or tests — confirmed absent — so the dossier/popup read is greenfield too. A new kind (`no_word_from`) is not a line: it lands across the five-joins machinery (heraldJoinMolds.js, heraldIndex.js, heraldCausalVoice.js, rumorHeraldLink.js), must satisfy tests/lint/heraldRouting.walker.test.js and the kindPoolFloors/phrasedKindPools family (15 kind-pool walkers live in tests/lint), and the volume additionally demands the kind's OWN walker file per seam 5. That is a car on its own, on top of the ~150-eff moverTidings.js mechanism car and the pin car. The reader's mechanism analysis is otherwise sound and its sharpest point is right: WY-2 is a MECHANISM predecessor, not merely a queue-order one — building WY-5 first mints the second weathering spelling the r2 revision exists to forbid. Zero §2a fields is correct (fully derived). The BLACKOUT TELLABLE executed across WY-3's physical lane is a genuine cross-wave fixture and is what pushes the high end to 5, not 4. Certification row + twin owed."},{"wave":"WY-6 the prize arm + the pairs table (engine, caravanSeizureEnabled)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":5,"why":"Two greenfield artifacts plus a persisted field plus a Herald kind cannot be 2 cars. Verified at the slot: seizureTake exists at smuggle.js:316 and is already consumed at commodityFlow.js:613 (so §8's 'no new bands, reuse' is real and the reader is right that no tuning signature is owed — the single clean saving in the family); armyRecordOf is exported at src/domain/spatial/armyTransit.js:327 with armyTransit.js at 340 eff and wide headroom; espionageGauntlet.js is live at src/domain/worldPulse/espionage/ and `caught_spying` is a real outcome (foreignGuestHold.js:42, ransomClaim.js:56), so the E15-PRESENT pin's resolver-side scan PASSES rather than waits and the walker is genuinely born seeing fifteen. What the reader compresses: `tests/lint/encounterPairs.walker.test.js` is a both-direction closed-table walker over a fifteen-row registry with a sixteenth-arm mutant — its own car by the volume's own argument; F8 is a §2a persisted field owing the normalizer same-commit (RF-1) plus a round-trip; `caravan_seized` is a full Herald mint with five joins through the same kind-pool walker suite WY-5 pays; six pins and two mutants with manifest entries; certification row + twin. The TRADE collision on commodityFlow's stock-write seam serializes the home-credit car. 3 floor, 5 if the conservation/timing pins and the conscience arms split out."},{"wave":"WY-7 the projection spine (surface, no flag — behaviour-preserving except declared unifications)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"The reader's row for WY-7 was truncated before it reached me; from the family total (34-55) minus the nine rows I did receive (21-35), the four surface rows WY-7/8b/9/10 carry 13-20 between them, about 3.25-5 each. WY-7 alone is more than that and is the family's true monster. Priced from the volume against the slot: KIND_PREFIX is module-local at entityLinks.js:13 (CONFIRMED — 'share one registry' is literally unbuildable until it is exported); treatyProjection.js is greenfield decorating treatyDocument.js's `decorate` at :325 (192 eff) and consumed on THREE surfaces including PDF FaithWar.jsx (423 raw) and TreatyPanel.jsx (183 raw), with the scope/role/term shifts landing as DISCLOSED PRINT CHANGES that each owe before/after parity goldens; personProjection.js relocates heraldWanderers.js (332 raw) OUT of src/components/map into src/domain/display as a pure move across a layer boundary; whereaboutsDisplay.js (63 raw) becomes the ONE whereabouts vocabulary, a visible dossier wording shift; resolveOwningCampaign + settlementNameFor are both greenfield AND the census they collapse has DRIFTED WIDER than the volume's 'four nameFor spellings' — I count 12+ live local spellings (liveWorld.js:132, generateWorldBook.js:462, generateCampaignPDF.js:779, EngineSections.jsx:143, CampaignFolder.jsx:97, livingWorldSignals.js:189, AdminSimTuningPanel.jsx:124, HeraldBody.jsx:113, RelationshipChronicleSection.jsx:148, heraldWanderers.js:292/:320, PerspectiveStandings.jsx:60); tests/lint/wayfareMirror.walker.test.js is a new both-direction totality walker with a per-kind manifest and shared-renderer import identity; and the PHANTOM RETIREMENT is a twelve-file census (CONFIRMED still twelve: dossierLazyTabs, LivingWorldTab, FaithTab, faithPanelModel, livingWorldSignals, entityLinks, pdf/lib/liveWorld, pdf/sections/FaithWar, entityIndex.test, liveWorld.test, liveWorldBTrack.parity.test, summaryTabPhase2.test) whose membership has drifted — WarFaithTab.jsx no longer exists, FaithTab.jsx now carries the spelling — plus a retirement-completeness grep pin in the same commit. All of it under a MANDATORY quiet window on the dirtiest files in the tree. This is OUTPUT-MOVING at landing by declared unification, one of only two such rows plus WY-8b's record presence."},{"wave":"WY-8a the supply train (engine slice of WY-8; armySupplyEnabled, F9)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"The reader enumerates FIVE cars and prices carsLow at 3. Substrate CONFIRMED: src/domain/war/ does not exist so C-WYF-2's re-home beside supplyWebWarfare.js (542 eff by my measure, not the reader's 538; under the 800 ceiling, no baseline entry) is correct; accumulatedAttrition is already live across attrition.js, warArmyRecord.js, warCoalitionExpenditure.js and armyStrength.js so the coupling writes an existing quantity; armyRecordOf at armyTransit.js:327 is the normalizer to teach; commodityFlow's stock-write seam is the same door F8 uses. The doc catch-up is REAL and I reproduced both halves: slot docs/DESIGN_FP_ARCHITECTURE.md:669 reads 'F9 `supplyCargo` is UNSIGNED' while ledger :669 reads '⏱ SIGNED', and the slot contradicts itself at :678 and :1891 ('owner-SIGNED under the blanket sign-off but UNBUILT'). Under the 14:4x grant it is a catch-up, not a gate — correct. Why 4 and not 3: F9 is a §2a persisted field on a war-owned record with a SINGLE-WRITER source scan (its own instrument, with a second-writer plant), a conservation identity asserted EVERY tick, a round-trip through a consumption tick, a cross-program write into the war estate's occupation-resistance writer, three mutants with manifest entries, eight named pins, a certification row plus twin, and the r3 one-clause veto surface restated verbatim. WC-10 hard-gates on this BUILD, so it is on the WC critical path and cannot be the slice that gets compressed."},{"wave":"WY-8b the mirror (surface slice of WY-8; F6/F7)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"The reader's row was truncated before it reached me; the implied share from the family total is roughly 3.25-5 and this row exceeds it. Derived from the volume against the slot: armyProjection.js is greenfield grown from armyStrength.js (237 raw / 94 eff) with an audience parameter, the frozen five-ledger UNIT_SOURCES registry, the {band, source, complete} declared-absent provenance shape, the retreat JOIN across both records, the DM belief block and the commander-name anchored negative. It mints TWO §2a persisted fields (F6 on worldState.deployments[homeId] via warArmyRecord seed/refresh, F7 on spatialLedgers.armyTransit via armyTransitKernel's copy site) whose whole safety argument is byte-identity for every world without a martial record. It executes TWO measured cures in the same wave: the hasMartialRecord collapse (CONFIRMED exported at martialReadiness.js:256, consumed at WarTab.jsx:48, duplicate local still at opportunism.js:122) and C-WYF-3's three-layers-deep CONDITION_BANDS rename (CONFIRMED at armyStrength.js:133, re-exported inside ARMY_STRENGTH_PHRASES at :237 — near-free but not zero: forceComposition.test.js:12 imports the aggregate). It carries the supply band onto the popup facet table AND the mirror-law war tab in the SAME commit, and the host surface has moved under the volume — WarFaithTab.jsx is gone, so the consumption must be navigated by symbol into WarTab.jsx (407 eff) / FaithTab.jsx (198) / WarFaithDesk.jsx (92), all under the 600 component ceiling but all subject to the lazy-tab-body-leaf rule. Plus the B-track re-anchor (pdf/lib/liveWorld.js, 208 eff, with tests/pdf/liveWorldBTrack.parity.test.js), five pin families, the decoy mutant, and an owner tuning row for the condition ladder edges. Output moves at landing by record presence, not by flag."},{"wave":"WY-9 the clickable canvas (surface, store-gated)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"The reader's row was truncated; the implied share is roughly 3.25-5 and the covert half alone is wider than the volume's phrasing suggests. Measured at the slot: the census-1 covert hole is live at SIX map call sites, not the volume's implied handful — RoadScenePanel.jsx:61, HeraldRemembrance.jsx:212, TravelersLayer.jsx:51, TimelapseLayer.jsx:46, HeraldGazetteer.jsx:191 and HeraldWanderers.jsx:139 all pass `{ isOwner: !!auth?.user, authenticated: !!auth?.user }` and NONE supplies the id pair, while viewerSecrets.js already supports it (ownerUserId/viewerUserId documented at :34-35 and enforced at :56). That is a fail-closed tightening across six surfaces with a covert-leak mutant — its own car. On top: the four-sub-layer restructure (armies absorb navalTransit, persons widen to all three person ledgers, populations positioned from rec.departTick, the census-4 one-line cure); the §2c PERSISTED filter-value migration ('migrants'→'populations', 'envoys'→'persons') in the mapSlice merge, which is a lifecycle path on per-campaign persisted state and exactly the class this estate loses writes on; the whole §3d reach surface (pointer seam, hit-slop under counter-scale, drag-vs-click dead zone, tabIndex/role/Enter per marker, the sr-only summary widened to enumerate mover counts) with its own a11y row; the popup family over PortablePopup (68 eff, exists); and `linesForEntity` on heraldFeed.js (115 eff), which is CONFIRMED ABSENT tree-wide — greenfield. Note for the ledger row: the reader's first-paint headroom argument cannot be discharged by running the suite, because tests/build/vendorPdfLazy.test.js:957 is `it.skipIf(!requireDistRead)` and reports green without a dist."},{"wave":"WY-10 the flow layers (surface, store-gated)","verdict":"TOO LOW","correctedLow":2,"correctedHigh":4,"why":"The reader's row was truncated; the implied share is roughly 3.25-5, which for once may be close, but the floor is 2 rather than 1 and the structural-enforcement half is routinely the part that gets compressed. Derived: TWO greenfield projections (caravanProjection.js, populationProjection.js) land here as map-only-BY-OWNER-EXCEPTION, and 'map-only' is enforced STRUCTURALLY, not by comment — a contract test asserting that no pdf/, no export and no publicSafe-reachable module imports them, plus publicSafe contract rows, plus PUBLIC_TOPLEVEL_KEYS left untouched with the fail-closed seam already mirrored to SQL. That import census plus the mirror-walker rows carrying map-only exemptions is a car. The binding half (caravan sub-layer binds ONLY to F1/F2-bearing shipments; populations popup reads F4/F5; the honest legend per Q5) is a second, and it is genuinely cheap because WY-3's marker-truth source scan already fences re-solves from birth and a recordless shipment simply does not draw. Dependency read as the volume states it: last surface wave, blocked on engine WY-3 AND WY-4 (hence transitively on POP-1) and on WY-9's popup family. The lie-negative (a bodiless shipment renders nothing) is the executed guarantee and is cheap only because WY-3 paid for it."},{"wave":"WY-11 the lifeblood envelope (engine, caravanFloorEnabled + instrument)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":4,"why":"The reader names three distinct halves and then prices carsLow at 2. The floor law, the density envelope soak instrument and the certification-row AMENDMENTS are three different kinds of work and the third is the one C-WYF-5 exists to protect: WY-11 AMENDS eight rows its siblings minted with envelopes and dispositive channels and must NEVER land them as a batch — the batch shape is precisely what an implementer reaches for by default, and the estate makes the row-plus-twin binding on every flag-minting packet (tests/lint/sovereigntyLightingContract.walker.test.js:4598). The blockage read is CONFIRMED and is the family's worst: subsystemRowsVirtual.js is 284 eff with room so no decomposition is owed, dispatchEV.js (118 eff) is already in the pulse graph, but TR-9 is PART-BUILT (only 9c landed), TR-5 is UNBUILT, WY-3 is needed for density to be a real quantity, and all seven sibling flag-minting waves must precede it — so it is the family's last engine wave in practice, exactly as the reader says. The volume's own escape ('degraded arms declared if built earlier') is real and is what keeps the high end at 4 rather than higher, but a recorded-deferral car is still a car. The floor-never-beats-interdiction pin is the right executed guarantee and the ECONOMIC-never-PHYSICAL discipline is what keeps this wave from being much more expensive."}]}}
{"type":"started","key":"v2:825a41a7f57b004976d0eae9d1e6c341c1d73f750eb795fa61d680ca3b9ae661","agentId":"a6e66282d8234934c","label":"refute:INT","phase":"Refute"}
{"type":"result","key":"v2:5c2163fc8ff5388068d7d62eb959abe3dbf1941764d70e0aaa6510509180fb2d","agentId":"a166442fb62b831d9","result":{"family":"IN — INFORMATION (docs/DESIGN_FP_ARCH_IN.md, governing volume DESIGN_FP_INFORMATION.md; SOL_QUEUE A2 row 13)","summary":"THE READER IS AN UNUSUALLY GOOD MEASURER AND A SYSTEMATICALLY OPTIMISTIC PRICER. I re-executed its headline measurements at the slot (claude/composite-r4 @ a5876c0ea) and most survive exactly: the two hot heads are 781/800 and 781/800 to the line (my own block-comment-aware counter agrees digit for digit; eventProse 750, settlementStrategy 760, corruptionWeb 510, subsystemRowsBelief 252, brokerageServicesPlant 303, heraldRouting 273); IN-0 IS complete including IN-0c (`peaceTermsDisclosure.js` 89 lines, the credit live at `informationStatecraft.js:1442`, `couplingRegistryInfo.js:129-140` owningWave IN-0c, `treaty_disclosure_opened` routed at `heraldRouting.js:254`); IN-1a/1b/1c-a are all landed and mounted (`RelationshipsTab.jsx:11/:62`, cert row `subsystemRowsBelief.js:327`); IN-4's substrate is real (`errandMint.js:76` the one gate read, `mintErrandSpine` :257, and `envoyErrandVocabulary.js` carries `{consumer:'couriers', module:'src/domain/worldPulse/covertErrand.js', wave:'IN-4', built:false}` with the split rationale in source); the five plant key-lists are 4/7/6/9/10 as claimed, PLANT_REFUSALS is six, QUERY_REFUSALS five, ENVOY_PICTURE_PLANT_FIELDS ten; `INTERCEPT_ENDINGS` is ['read','refused'] with \"`caught` is IN-3's sweep, declared not built\" at :587; `sightPostures` has zero UI consumer and `projectPatronBindings`' `exposed` has zero src supplier; and I independently re-ran the worker BFS — 227 modules reached from `src/workers/generation.worker.js`, and NOT ONE is under domain/worldPulse, domain/realm, domain/display, domain/certification or components. 0 KB for every IN wave is CONFIRMED and is the cleanest fact in the family.\n\nFIVE THINGS THE READER GOT WRONG, TWO OF THEM LOAD-BEARING.\n\n(1) THE CAR BASE RATE IS COUNTED FROM HEADLINE COMMITS ONLY, AND THE FAMILY'S OWN HISTORY REFUTES IT. Under this task's definition — one gated commit with its proofs — IN-1c-a, the SMALLEST landed IN slice (a 182-line data leaf, a 62-line pool, one read-model join; no flag, no state, no numbers), cost FOUR code-touching gated commits: `22cedb1b0` (11 files / 750 insertions), `2d199df42` T1, `3f8fae006` CURE, `24c611907` T2a — plus two chair rulings (§55, §85) and two flip/collect commits. IN-0a needed a named repair car (`94d0c7989`, 6 files / 405). IN-1b STOPPED TWICE at chair walls (`8063366ea` \"STOPPED then cured: B13 measured 599/600\"; `889f0e9aa` \"STOPPED at two walls only a chair can move\"). Every IN slice except IN-0d needed a cure round. The reader's calibration (\"IN-1a = 1 car, IN-1b = 1 car, IN-0d = 1 car\") is the build commit with the cure rounds deleted. That is why I mark five of six waves TOO LOW rather than disputing their scope: the reader's scope enumerations are excellent and its multiplier is ~1.4×-low.\n\n(2) IN-5 HAS NO ES-6b DEPENDENCY — the arrow points the other way. `docs/DESIGN_FP_ARCH_ES.md:1641` states the ES spine verbatim: \"ES-0 (free) → ES-1 (SP-D) → ES-2 → ES-3 → ES-4 → ES-5a → ES-5b (Q1) → ES-6 → ES-7 (IN-5)\", and :1612 heads ES-7 \"final slice; after IN-5 for the knowledge desk\". ES-6b and IN-5 are PARALLEL predecessors of ES-7, not a chain. The first pass's \"critical path ES-6b → IN-5 → ES-7\" is a build ORDER for the ES family, and the reader read it as a blocking dependency. IN-5 can start the day the chair wants it; nothing in ES gates it. This is the single most consequential correction, because the reader used it to justify holding the family's most starvation-relieving wave.\n\n(3) IN-2'S \"SCARCITY/CONDITIONS STAY HARD-BLOCKED\" MEASURES THE WRONG SURFACE, AND THE TRIPWIRE IS BORN ALREADY-FIRED. The reader checked `CHANNEL_BELIEF_AXES.trade` (frozen empty — true) but the volume's block (S25: \"BELIEVED SCARCITY / CONDITIONS remain absent\") was premised on the BELIEF AXIS substrate, and SP-B landed it one day after the IN compilation: `src/domain/worldPulse/beliefAxisSubjects.js` (462 lines) ships `SCARCITY_BANDS`, `STORES_BANDS`, `ROUTE_POSITION_BANDS`, `PULL_BANDS`, `DEVOTION_BANDS`, `CONDITIONS_KEYS`, and `SUBJECT_AXIS_FIELDS = ['conditionsBands','devotionBand','scarcityBands']` folded onto the existing beliefRecord behind `believedScarcityEnabled` / `believedConditionsEnabled` / `believedDevotionEnabled` — all three IN the manifest, all three read by name at `beliefAxes.js:91-93`, already consumed by ES (`espionageProducts.js:454`, `espionageProductStage.js:684`). So IN-2's own pre-pin tripwire `axisFamiliesGrewSinceIn2()` fires on arrival, and the wave faces a live decision it cannot dodge: close the axis set at {strengthBand, faithLabel} by ruling against a tree that now holds three more families, or widen. A second wrinkle the reader could not see without this: `devotionBand` and `faithLabel` are now TWO devotion-shaped axes, and J-INA-3 types the bait on the older one. Note also that `scarcityBands`/`conditionsBands` are KEYED OBJECTS, not scalars — a plant asserting one needs an axis-plus-key, which the reader's `axis`/`assertedValue`/`trueValue` triple does not express.\n\n(4) `intelTradeEnabled` IS NOT AN UNRECORDED LIE — IT IS ROW 9 OF A GOVERNED 17-KEY REGISTER THE READER NEVER OPENED. `tests/lint/engineGatedRuleKeys.walker.test.js:193-210` holds `BACKLOG_RULE_KEYS`, asserted EXACT against the measured gated-but-uncensused set (:1027) and shrink-only with a ceiling at :1032. The walker's own docblock records the reason it has not been cured one key at a time: \"CR-WR10-C authorised four keys and four rows; landing seventeen more rows under a ruling that measured five keys would be a lane inventing its own scope… reported to the chair as the reopened §5 stragglers branch\". So the car is bigger than the reader says (manifest join + backlog row DELETE + ceiling lowered 17→16 + cert row + the four existing dormancy fences re-run) and it pre-empts a recorded chair fork rather than being a free unilateral cleanup. And the manifest is THIRTY-FIVE keys, not 24 (I extracted the array: it ends non-alphabetically at `contributionLedgerEnabled`… `envoyTaskCatalogEnabled`).\n\n(5) THE \"ONE-KEY DELTA PER COMMIT\" SEQUENCING ARGUMENT IS REFUTED AT THE SLOT. The reader's stated reason IN-4 car 1 must stand alone is that the walker makes a two-key commit physically impossible. `grep -ci delta tests/lint/engineGatedRuleKeys.walker.test.js` returns ZERO. The walker audits membership, rows, exemptions, backlog exactness and pending-key atomicity — nothing per-commit. \"Exactly the one-key delta\" is the ARCHITECTURE DOC's prescription (§2), not a live assertion, and it does not bar `reputationRaceEnabled` and the `intelTradeEnabled` cure from one commit. The first car is still the right first car; the reason given for it is not true.\n\nMINOR CORRECTIONS: EXACT_SECTION is 383 routed tokens, not 381 (war 91, trade 129, events 134, faith 19, divination 8, adjudication 2 — the reader dropped adjudication's two; the zero-knowledge-section diagnosis stands and strengthens). HERALD_SECTIONS has TWO real src importers (`heraldFeed.js`, and heraldRouting itself) plus seven test files — `brokerageStamps.js:336`'s hit is a COMMENT, though that file does hold a section→channel map asserted total both ways, so it is a real edit site; `heraldRouting.walker.test.js:125` pins the frozen six as a bare literal. My worker BFS reached 227 modules, not 153 — the dispositive conclusion is identical. CONFIRMED and worth keeping: the mirror's five input families are exactly plants / intelTransfers / secrecyPostures / envoy-interception evidence / our own belief record, with `shares: Object.freeze([])` — the WR-7b negotiation pictures are genuinely absent AND are not among CR-IN1-7's recorded deferrals, so J-INA-5's seventh family is owed, unrecorded, and the reader is right to price it.","verdicts":[{"wave":"IN-1c-b — the interim knowledge desk + the three certification-surface movements (the mirror's last slice)","verdict":"TOO LOW","correctedLow":2,"correctedHigh":3,"why":"The scope is right and the block is right: ODQ §55 Q3 rules 'the interim-desk decision rules at IN-1c-b's compile (an EXACT_SECTION row to events with a dated IN-5 re-point comment)' and Q5 rules 'the three certification-surface movements rule together WITH Q3 as one IN-1c-b charter'; RESUME_STATE.md:1677 confirms it is blocked on that charter and nothing else. What is wrong is the price, and the refutation is this wave's OWN SIBLING. The reader benchmarks against IN-0d (3 files / 523, one car). The correct benchmark is IN-1c-a — same wave, same registry family, no flag, no writer, no numbers, and strictly EASIER than 1c-b — which cost FOUR code-touching gated commits: 22cedb1b0 (11 files / 750 insertions, the registry and the governed standing line), 2d199df42 (T1, the ruled mutation-coverage cure), 3f8fae006 (the §75 option-2 cure landed ALONE and explicitly without the LANDED flip), 24c611907 (T2a, the two ruled registration rows), plus two chair rulings and two flip/collect commits. 1c-b adds an EXACT_SECTION edit — heraldRouting, which 1c-a 'touches NOT AT ALL' by the §55 ruling — and three certification-surface movements that the charter itself binds as one sitting. 2 cars is the honest floor (one build, one cure allowance on this family's measured rate); 3 if the three certification movements each want their own commit, which is exactly the shape 1c-a's T1/T2a split took. The 2-car upper arm the reader offers for J-INA-5's seventh input family is CORRECTLY IDENTIFIED but wrongly bundled: I read mirrorInputsAt at secondOrderBelief.js:160-231 and the families are plants, intelTransfers, secrecyPostures, envoy-errand interception evidence and our own belief record, with shares frozen empty — the WR-7b pictures are absent AND are NOT among CR-IN1-7's four recorded deferrals (the header names plants/HIDE, memoryless transfers, absent ally shares, deferred exposure receipts). An unrecorded owed input family with its own degradation pin and allow-list import fence is its own car, not the top of a range."},{"wave":"IN-2 — THE LURE (`infoLureEnabled`)","verdict":"TOO LOW","correctedLow":6,"correctedHigh":8,"why":"`infoLureEnabled` is 0 files in src, tests and scripts — UNBUILT confirmed. The reader's seven-obligation enumeration is the best part of its whole report and I verified every vocabulary figure (PAID_PLANT_KEYS 4, RECORD_KEYS 7, OVERRIDE_KEYS 6 including faithLabel, RECEIPT_KEYS 9, TARGET_KEYS 10 at disinformationPlant.js:181-197; ENVOY_PICTURE_PLANT_FIELDS ten at brokerageServicesPlant.js:95-106; CHANNEL_BELIEF_AXES.faith = ['faithLabel'], .trade frozen empty at brokerageServices.js:128-135). The ceiling re-price is also exactly right to the line: informationStatecraft 781/800, beliefMap 781/800, eventProse 750/800, so the fold-side axis law, the comparator and the NAMED LIE variant all go to leaves. TWO THINGS PUSH IT UP. FIRST, THE SUBSTRATE MOVED UNDER THE HARD BLOCK. SP-B landed beliefAxisSubjects.js (462 lines) with SUBJECT_AXIS_FIELDS = ['conditionsBands','devotionBand','scarcityBands'] folded onto the existing beliefRecord, gated by believedScarcityEnabled / believedConditionsEnabled / believedDevotionEnabled — all three in ENGINE_GATED_VIRTUAL_RULE_KEYS, read by name at beliefAxes.js:91-93, already consumed by ES at espionageProducts.js:454. The reader measured CHANNEL_BELIEF_AXES (the brokerage QUERY channel) and concluded the baits stay hard-blocked; the volume's block was premised on the BELIEF AXIS substrate, and that premise is now false exactly as faithLabel falsified the devotion half. So axisFamiliesGrewSinceIn2() is born already-fired, IN-2 must either rule the closure against a live three-family tree or widen, and scarcityBands/conditionsBands are KEYED OBJECTS — a plant asserting one needs axis-plus-key, which the reader's flat axis/assertedValue/trueValue triple cannot express. A second live problem: devotionBand and faithLabel are now two devotion-shaped axes and J-INA-3 types the bait on the older one, which is a coherence ruling this wave must take. SECOND, the DM LIE-COMMISSION VERB is priced as one car while being a full store-action lifecycle: a row in operationRegistry.js (640 lines, typed opType/klass/slice/targetScope/receiptRef/undoToken/undoState), a slice action, gen:compendium-data, an approval-routed typed AI proposal AND its UI — ES-7's equivalent verb (DISPATCH_CONFIRMATION_MISSION) is chartered in the ES record as a wave-scale act with its own judgment block. Add this family's measured cure rate and 4 is not a credible floor."},{"wave":"IN-3 — THE COUNTER-GAME (`counterIntelEnabled`)","verdict":"TOO LOW","correctedLow":7,"correctedHigh":9,"why":"`counterIntelEnabled` is 0 files — UNBUILT confirmed, and every consumption claim checks out: sendTwoDivergence.js (230) and envoyTestimony.js are built so the vet and send-two are consumers; INTERCEPT_ENDINGS is Object.freeze(['read','refused']) at brokerageServices.js:588 with the source comment at :587 reading verbatim '`caught` is IN-3's sweep, declared not built'; PLANT_REFUSALS is six at brokerageServicesPlant.js:90-92 so the pin asserts seven and QUERY_REFUSALS (five, brokerageServices.js:356) stays untouched; projectPatronBindings(bindings, { audience='dm', exposed=[] }) at brokeragePatronage.js:319 has NO src supplier of `exposed` (the only callers are tests/domain/brokeragePatronage.test.js:182/:185/:199); sightPostures is written at informationStatecraft.js:1321 and read by nothing outside statecraft and one certification row; corruptionWeb is 510/800 so the exposure producer has a home; the statecraft head's 19 lines force HIDE-as-answer into a leaf. All correct. The price is not. The reader's own enumeration lists SEVEN distinct deliverables and then offers 5 as the floor by folding the two DM surfaces into the house car and riding IN-2's store-action lane for the verb — but IN-2 may not land first (it has no ordering claim over IN-3; the volume's collision map only requires serialization on brokerageServicesPlant.js), so the verb cannot be assumed pre-paid. This is the family's largest wave by count of new mechanisms — five verbs, a suspicionOf derivation with an import pin plus a token scan plus guard-the-guard, a house sub-program with its own producer and the witch-hunt counterforce under a no-fates phrase scan, two DM components under the 600-effective component ceiling, and a DM verb — against a family whose SMALLEST flagless slice took four commits and whose component-shaped slice (IN-1b) STOPPED twice at chair walls, once at a measured 599/600. 7 is the floor and 9 is realistic; the reader's 5 assumes no cure round anywhere across seven mechanisms."},{"wave":"IN-4 — THE ROAD (couriers, double agents, the race) (`reputationRaceEnabled`)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"The substrate finding is excellent and I confirm all of it: reputationRaceEnabled 0 files; routeNetworkConsumersRace.js 364 phys / 146 eff with RACE_OUTCOMES frozen at :104 and reputationRace at :322 and no engine consumer; errandMint.js is 'THE GENERALIZED ERRAND MINT HEAD, and the errand spine's one gate' with errandSpineEnabled read exactly once at :76, mintErrandSpine at :257, errandSpineFields :141, covertFaceFor :189, covertMissionRefusal :221, and its header naming 'ES-1/IN-4's couriers' among the five volumes that mint through it; and envoyErrandVocabulary.js holds IN-4's row by name with covertErrand.js pre-declared and the source comment recording that the split happened so landing ES-1 'would have declared IN-4 built and pointed the registry at a file that does not exist'. This IS the cheapest unbuilt IN wave and it has no intra-family predecessor. But car 1 is bigger than priced and the reason given for isolating it is false. `intelTradeEnabled` is not merely absent from the manifest — it is row 9 of BACKLOG_RULE_KEYS (tests/lint/engineGatedRuleKeys.walker.test.js:193-210), a SEVENTEEN-key register asserted EXACT against the measured gated-but-uncensused set at :1027 and ceiling-asserted at :1032. Curing it means manifest join + backlog row DELETE + ceiling lowered to 16 + certification row + the existing dormancy fences re-run, and it takes one key out of a fork the walker's own docblock records as the chair's: 'landing seventeen more rows under a ruling that measured five keys would be a lane inventing its own scope… reported to the chair as the reopened §5 stragglers branch'. Meanwhile the stated impossibility argument is refuted — there is no 'delta' assertion anywhere in that walker (zero hits); the one-key-delta line is the architecture doc's prescription, not a live pin, so the cure and the flag mint COULD share a commit. Net: car 1 stands as the right first car on merit, cars 2-3 (the flag mint with ONE_REGEN-preset fences given distancePricedNewsEnabled: true at simulationRules.js:667, the race-consumer leaf, covertErrand on the built spine, the D-3 retirement dark-byte-identical, the JSON alias trap) are correctly scoped, and the family's cure rate plus the conditional WAR-file dock under Q2 puts the honest range at 4-6."},{"wave":"IN-5 — THE VOICE (the seventh Herald section; no flag)","verdict":"WRONG DEPENDENCY","correctedLow":4,"correctedHigh":6,"why":"THE BLOCKER IS FICTIONAL. The reader holds IN-5 behind ES-6b and calls it 'mid-critical-path ES-6b → IN-5 → ES-7'. The ES record states its own spine at docs/DESIGN_FP_ARCH_ES.md:1641 verbatim — 'ES-0 (free) → ES-1 (SP-D) → ES-2 → ES-3 → ES-4 → ES-5a → ES-5b (Q1) → ES-6 → ES-7 (IN-5)' — and heads ES-7 at :1612 as 'final slice; after IN-5 for the knowledge desk'. ES-6b and IN-5 are PARALLEL predecessors of ES-7, not a chain; ES-7 consumes IN-5's desk ('section authority — knowledge desk for confirmations/refutes'). Nothing in ES gates IN-5. The first pass's '§3j order: ES-6b → IN-5 → ES-7' is a sequencing convenience for one worktree, read here as a dependency. The real predecessor is IN-1c-b, and even that is soft: if IN-5 lands first its interim EXACT_SECTION row is not 'born stale', it is simply MOOT, because the beats route to the real desk. Everything else the reader says holds and I verified it: HERALD_SECTIONS is the frozen six at heraldRouting.js:64; belief_misjudgment: 'faith' at :143; intel_transfer: 'trade' at :192 with the authoring ruling at :120; the routed census is 383 (not 381 — the reader dropped adjudication's two) with ZERO knowledge section; the refile IS a same-seed mover because beliefsActive at beliefMap.js:204-209 is spatialCanonVersion AND infoMode !== 'omniscient', and infoMode 'full' ships at simulationRules.js:930 with 'perfect_delayed' at :857 — so car 4 is the family's one genuine §764.3 owner gate and the cars-1-3-dark-safe strategy is exactly right. The consumer census is slightly understated: brokerageStamps.js:336 is a COMMENT, but that file holds a section→channel map asserted total both ways, heraldRouting.walker.test.js:125 pins the frozen six as a bare literal, warCostKindPools and couplingDesk both iterate sections. Price rises to 4-6 on the cure rate and on that ten-site census, and — the important consequence — the wave can START NOW."},{"wave":"IN-6 — THE MEASURE (the acceptance harness; no flag)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"UNBUILT confirmed and every spec anchor is real: behavioral-observation.mjs exists (1233 lines) as the named edit site; knowledgeLaneEvidence.js:62 still holds the single dispositive literal KNOWLEDGE_LANE_EVENT_TYPES = ['belief_misjudgment'] and :161 the BELIEF_DIVERGENCE_RECEIPT_PATH the paired soak reads; subsystemRowsRegen.js:60 IS the differential harness's spec in words ('What would close the gap is a PAIRED run… Half of that instrument now exists; the differential harness does not') with soakEvidence: 'unobserved' at :85 (and at :180 and :228 — THREE unobserved rows in that file, not one, so the harness has more rows to flip than the reader counted); scripts/dormancy-bit-compare.mjs (252) is the committed comparator to copy; BeliefDivergenceBand.jsx (138) is the display join; subsystemRowsBelief.js at 252/800 has room for the rows. The gate-mutex false-green hazard the reader names is real and correctly designed around. The price is low for three compounding reasons. First, car 4's envelope cross-check is MECHANICAL AGAINST EVERY §4 Endings entry ACROSS THE WHOLE FAMILY — IN-2's bait endings, IN-3's five verbs and seven refusals, IN-4's four RACE_OUTCOMES, IN-5's court_sat_still — each with its own tuning-constant mutant that must be executed and proven to RED; that is not one car's worth of executed mutants. Second, the four certification rows at 25-40 effective lines each, naming each flag's dispositive literals, land against flags this wave did not author and must re-measure. Third, the paired-soak instrument is a new repo-level harness that must print its own case count to survive the gate-mutex hazard, over soaks that are the most expensive proofs in the estate. Against a family where the cheapest flagless slice needed four commits, 3 is not a floor for four instruments plus a family-wide mechanical cross-check. The terminus placement and the dependency on all four flags are correct."}]}}
{"type":"started","key":"v2:335ae9f6dfdb4da3010b3daabf0826bbb0aa40a670ca1cd13507c6190081bd6b","agentId":"a375d86717e3361fe","label":"refute:GR-CW","phase":"Refute"}
{"type":"result","key":"v2:f8e975a512737995af3ef5a94d9dad9c720985d2a8dac25aad0b82b0fba3aa04","agentId":"a9cb1d82500244aa7","result":{"family":"TR","verdicts":[{"wave":"TR-2 — THE HOUSE (merchantHousesEnabled; TR-2b the syndicate variant rides dark)","verdict":"WRONG DEPENDENCY","correctedLow":4,"correctedHigh":6,"why":"The reader writes \"No spine blocker: every substrate premise TR-2 names measured BUILT\" and ownerGate NONE. Both are refuted by code I read at a5876c0ea. (1) THE SP-4a ACTOR-CLASS GATE. TR-2's Req-13 in the volume is \"acts are posture-priced (SP-4a house instance)\". At the slot, src/domain/worldPulse/strategicPosture.js:97 carries POSTURE_ACTOR_KINDS = Object.freeze(['settlement']); :159 REFUSES an unknown kind and :162 says in the thrown message that widening it \"is an SP-4a\" amendment; the header at :90-95 says \"widening this set is an explicit SP-4a amendment at the constitution, never a volume-local mint\"; DESIGN_FP_ARCH_SP.md §8 seam 9 (line 866) repeats it; and tests/domain/strategicPosture.test.js:137 PINS the set to exactly ['settlement']. So TR-2 cannot price house acts through courtPostureOf without a SPINE-constitution amendment that is not a TR act and is nowhere in the reader's dependsOn. TR-2 must either wait on SP-4a or land with posture pricing DECLARED ABSENT and the Req-13 clause struck — a charter ruling, and the same blocker is inherited by TR-7's overreach term. (2) TR-2b's gate is not the reader's to clear as \"NONE\": SOL_QUEUE.md:346 and the first pass's B.1 row both say \"UNSCHEDULED until the owner sequences it\", and the first pass §D lists TR-2b's sequencing among the items awaiting an owner line. The 14:4x grant may well cover it, but that is a chair ruling to record vetoably, not a measured absence of a gate. (3) PRICE. The reader's own scope bundles TR-2b as \"car D\". On this estate's landing record that is not one car: every flag mint here costs a fence set, an AUTHORED certification row and, empirically, a repair commit — TR-1 itself landed in TWO commits (d7ea69a4b 18 files/+2,933, then 93c118b6d 4 files/+224 carrying the cert row and 143 lines of engineGatedRuleKeys walker repair), GR-2 in two (a18fdcfae +3,945, 91075d457 +719), SP-B in two, GR-3a in two. TR-2 alone is 3–4; TR-2b with its own syndicateHousesEnabled flag, manifest join, authored row, four-fence set, lit-mutant and the corruptionWeb patron widening is 1.5–2 more. (4) TWO BILLS THE READER OMITS, both measured: a new spatialLedgers key must be classified in src/lib/spatialUsage.js TRACKED_LEDGER_KEYS (:238) or EXEMPT_LEDGER_KEYS (:334) with authored prose plus its row in tests/lib/spatialUsage.test.js — TR-1's exemption paragraph is one of the longest strings in that file; and houseNews owes rows in commercialReceiptPools.js, which holds 40 entries in 386 eff, i.e. ~19 eff per kind, so ~8 house kinds spend ~150 of the 414 eff the reader calls \"the family's shared budget\". What IS confirmed: all four leaves absent under src; factionArchetypes.js (73 eff) is in the generation worker's 153-module graph and must be READ not edited; corruptionWeb.js measures 510 eff; settlementLifecycleKernel.js 733/800 is the affordable mount."},{"wave":"TR-3 — BELIEVED MARKETS (believedMarketsEnabled)","verdict":"CONFIRMED","correctedLow":2,"correctedHigh":3,"why":"The only row I can leave standing, and it is right for a reason the reader understated rather than the one they gave. SP-2 is not merely \"satisfied\" as a manifest membership — the believed-scarcity SUBJECT FAMILY IS FULLY BUILT AND ALREADY HAS A LIVE CONSUMER. src/domain/worldPulse/beliefAxisSubjects.js (179 eff) exports SUBJECT_AXIS_FIELDS including 'scarcityBands' (:130), mints the bands at :390 keyed by REGIONAL_GOOD_CATEGORIES with a four-rung ladder, and gates them through beliefAxes.subjectAxesActive (:91 reads believedScarcityEnabled === true by name); beliefMap.js:347 documents the optional field; subsystemRowsBelief.js:227-247 carries its authored certification row; and pactFormation.js:216/:228 ALREADY reads the truth side (scarcityGroundTruth) and the belief side (record.scarcityBands) through a believedBandsOf/observedBandsOf pair. So the volume's ~300-eff beliefScarcity.js is not a new belief family — it is a dispatch-facing read facade over machinery whose idiom is already precedent-set, and I would price that leaf at 100–150 eff, well under the volume's figure. Against that, everything the reader priced the second and third car on is real and stays: the K3 structural set per consumer, the no-merge arithmetic-token scan with the two-composer whitelist, the guard-the-guard seeded band-average, staleness across two bands, the T-1 tellable, four fences plus the lit-mutant, two named mutants. And TR-3 is the cheapest genuinely-new wave in the family for a structural reason the reader did not state: it is the ONLY unbuilt TR wave that adds NO persisted field, NO news kind, NO WHAT_PHRASES/heraldRouting rows, NO receipt-pool rows and NO own walker file for a kind — so the default \"TOO LOW when a wave adds a persisted field, a walker, a display seam and generator code\" does not fire here. Confirmed by measurement: beliefScarcity.js and dispatchDestination.js absent under src; dispatchEV.js is at src/domain/spatial/dispatchEV.js, 118 eff (the reader's path is wrong but the figure is right); beliefMap.js measures 781/800 exactly as the reader says (the volume's 771 and 778 are both stale), so the sibling-leaf law holds; believedScarcityEnabled is manifest member at simulationRules.js:252 joined by SP-B with the three-family comment at :243-249. ~0 KB worker: nothing in src/domain/worldPulse/** or src/domain/spatial/** is in the worker's traced graph."},{"wave":"TR-4 — THE GRAIN ROAD (foodCaravansEnabled; two slices; EARLY-ELIGIBLE)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"The reader's low end contradicts the reader's own enumeration. They list SIX distinct cars (0 CR-2, 1 the round-trip pin, 2 the leaves + offset + flag + fences, 3 the T7 walker + storageMonths census + sixth-writer plant, 4 calm-equivalence + three bypass arms + T-6, 5 slice 2's treaty fork + robbery claim) and then price the wave at 4–6. Car 0 cannot merge with anything: FABLE_VALIDATION_QUEUE.md:303-309 and SOL_QUEUE.md:342-345 both order the two storageMonths folds consolidated into the one applicator FIRST, behind a same-seed byte-identical pin, with an explicit STOP-and-report clause — that is a standalone no-behaviour-change commit by construction. So the floor is 5, and on this estate's measured record (TR-1 needed a repair commit; so did GR-2, SP-B and GR-3a) the realistic ceiling is 7. The CR-2 contradiction the reader found is REAL and I verified both sides verbatim — the queue rows order CONSOLIDATION while DESIGN_FP_ARCHITECTURE.md §5 #26 carries Q9's \"REPORTED-NOT-DEFECT; the queue row filed; the sixth-writer scan fences it\". Two different acts; the chair must strike one before an implementer starts. A SECOND WY-3 DEPENDENCY THE READER MISSED: they addressed only the round-trip pin, but §5 slots WY-3 \"in PHASE 4 IMMEDIATELY BEFORE #26 TR-4 so grain inherits the caravan body\", and WAYFARE is 12 of 12 UNBUILT with caravanBodiesEnabled at zero hits and unsigned §2a owner rows. Landing TR-4 first is defensible, but it means the volume's \"grain inherits the caravan body\" premise FAILS and grain arrivals light without a mover body — that consequence must be declared, not silently inherited, so \"dependsOn NOTHING UNBUILT\" is too clean. Substrate I re-measured and that holds: the storageMonths write set is five modules / six expressions — foodStockpile.js:406 and :479, generosityUpdates.js:65, applyWorldPulse.js:106 (the second fold; the volume's :322 and :92-110 are both rotted), magicBufferApply.js:357, and mutateWorld.js:1153 which is at src/domain/events/mutateWorld.js, NOT src/domain/worldPulse/ as the reader writes; commodityFlow.js is 448 eff at src/domain/spatial/; foodStockpile 253; generosityUpdates 90; treatyTransfer 49; supplyKernel 489/800; grain already canonical in goodsCatalog.js. The worker tripwire warning is sound and I would ADD ONE: src/data/tradeGoodsData.js is also in the worker's 153-module graph, and a grain-quantity constant is exactly the thing that drifts toward a goods-data file. applyWorldPulse is 907/907 with zero headroom, so CR-2's consolidation must be a net SHRINK there (which is the good news: it banks a lower baseline)."},{"wave":"TR-5 — THE PACT LANE (tradePactsEnabled)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"The reader's headline reuse claim is half wrong and it moves the price. They write \"TR-5 mints only grain_provision (zero hits tree-wide, measured)\". The volume's five commercial terms are market_access, toll_exemption, exclusivity/trade_exclusivity, grain_provision AND route_wardenship — DESIGN_FP_TRADE.md:857-859 names the fifth as \"route_wardenship (a patrol obligation priced against leg loss rates)\". GR-3 minted three (peaceTermsCatalog.js:285-287, confirmed, with display strings at :425-427); TR-5 mints TWO rows, not one, and both need executors and producers. Worse, route_wardenship is a LIVE COLLIDING TOKEN: TR-1 already spent it as a commercial-reason kind — commercialReasonTaxonomy.js:66 and :85 (route_predation ↔ route_wardenship), commercialReceiptPools.js:205/:410, settlementRumors.js:263 'a road kept safe by its neighbour', heraldRouting.js:241, and the pool walker at tests/lint/commercialKindPools.walker.test.js:72. Minting a TERM_CATALOG type on the same bare string is precisely the \"one catalog quietly becomes two\" failure the estate already records against a treaty term family (subsystemRowsBelief.js:247), and it is a chair adjudication the wave stalls at charter without. THE TWO RED-FLIPS ARE ALSO BIGGER THAN PRICED: I read both, and each asserts the commercial family by SET EQUALITY, not merely producer-lessness — peaceTermsGrantTerms.test.js:275 and sovereigntyBundleWr10.test.js:480 both run expect([...commercial].sort()).toEqual(['exclusivity','market_access','toll_exemption']). Two new rows red the set assertion in both pins in addition to the executor/producer clauses, and sovereigntyBundleWr10's arm is anchored on 'tribute' via expectAbsentWithAnchor, so its re-pointing has to keep the anchor honest. Add the eleven-file CR-WR10-B amendment sweep plus DESIGN_FP_ARCHITECTURE.md:2349, the five executors, tradeDemandTrigger, the realmPressure01 dead-consumer landing, the five-subsystem T-5 tellable plus its dark twin, the K3 absurdity twin, the lean-year fork, the INTERVAL_WEEKS identity pin, the authored certification row and the receipt-pool rows for any pact kinds. The nearest measured analogue is GR-2 — peacetime pact formation — which cost 3,945 + 719 insertions across two commits; TR-5 is strictly larger. CONFIRMED from the reader: peaceTerms.js measures EXACTLY 797/800 effective at this base (my own eff count, independent of theirs), so any head seam of ≥4 effective lines is leaf-first or the wave stops; peaceTermsCatalog.js is 115/800 with 26 frozen rows; negotiationPictures 303 eff; courtPostureOf has a real caller in pactFormation.js:110, so SP-C's recorded zero-callers is indeed stale; and none of the treaty estate is in the worker graph."},{"wave":"TR-6 — THE CORNER + FAMINE SPECULATOR (corneringEnabled)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"The reader prices the family's largest leaf load (~920 eff, ~1.7x TR-1) plus three loads no other wave carries at 4–5 — the same band they give TR-4, whose leaf load is less than half. The calibration refutes it. TR-1's ONE car landed 909 EFFECTIVE new src lines (commercialReasonTaxonomy 57 + commercialReasons 289 + commercialReasonsNews 161 + commercialReceiptPools 386 + couplingRegistryTrade 16) against the volume's predicted 540 — a 1.68x understatement — plus 840 effective lines of new test (commercialReasons.test 297, the two walkers 184 + 199, the dormancy fence 160), and it still needed a second commit. Scaling TR-6's 920 predicted eff by that same factor lands near 3,000 effective lines of authored code and proof before its five counterforce fixtures, and the estate's measured car size is 500–4,000 raw insertions with a median near 1,500. FIVE CARS IS THE FLOOR, NOT THE CEILING. Three specifics the reader underweights: (1) TR-6 AUTHORS the commodityFlow writer-family census from nothing — I confirmed C-TLF-3: no such shrink-only source scan exists under tests/lint (the only files there naming commodityFlow are couplingInclusion.walker, the trade-contract walker and two tuning-register JSONs), so the wave writes the census in the T4/storageMonths shape, with an executed third-writer plant, in the same commit as warehouseSeizure, the writer it admits. (2) THE RECEIPT-POOL CEILING BITES HERE. The reader says \"re-measure it here rather than assuming\" but does not price the consequence: commercialReceiptPools.js is 386/800 eff for TR-1's ~20 kinds at ~19 eff per kind, and TR-2, TR-5 and TR-7 spend that 414-eff headroom before TR-6 arrives with corner/monopoly/seizure/forced-sale/ruin kinds. On the volume's own leaf-first law that is a pool SPLIT — a second family pool leaf and the walker amendment to see both — which is a sub-car nobody has budgeted. (3) The lighting-declaration subtlety the reader did name is correct and I verified it: commercialReasons.js:349 carries a live scorer over seam_cornering that nothing writes, and :51 names TR-6 as the owed producer, so corneringEnabled lights TR-1's famine_profiteering family too and the second-lighting declaration must cover both flags. Also confirmed: all five leaves absent; commonsVoiceEnabled has ZERO hits in simulationRules.js (no manifest membership, no preset row) while commonsVoiceKernel.js (214 eff) is built; moralDrift.js 159 eff; smuggle.js 119; entrepots.js 151; and goodsCatalog.js (435 eff) plus tradeGoodsData.js are the worker tripwires the truth-side census must read rather than edit."},{"wave":"TR-7 — VENTURES (venturesEnabled)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":4,"why":"A low of 2 is below the structural floor the reader themselves set for TR-2, and TR-7 carries the same three cost drivers TR-2 does. Compare the two rows: TR-2 is 890 predicted eff with a persisted ledger, a news leaf and a lifecycle-normalizer and the reader prices it 3–4; TR-7 is 670 predicted eff with a persisted ledger (spatialLedgers.ventures), a news leaf (ventureNews.js) AND a regenerate-PRESERVES exception — and the reader prices it 2–3. Nothing in the substrate justifies the discount; the pin set is narrower than TR-2's but the state surface is the same shape. Three bills that are not in the reader's row: (1) the spatialUsage registration — a third spatialLedgers key must be argued into TRACKED_LEDGER_KEYS (src/lib/spatialUsage.js:238) or EXEMPT_LEDGER_KEYS (:334) with authored prose and a row in tests/lib/spatialUsage.test.js, exactly as TR-1's commercialReasons exemption paragraph did; (2) ventureNews's WHAT_PHRASES + heraldRouting + own walker file + receipt-pool rows, against a pool file the earlier waves have already eaten into (386/800 eff today); (3) regen-PRESERVES is a DISCLOSED EXCEPTION TO THE PROMISE — the J-TR-3 / J-WR-3 idiom — so it does not get a generic round-trip pin, it gets its own proof that in-flight ventures survive regeneration while everything derived does not, alongside the dangling-shipmentRef-closes-failed pin which is this user's most-bitten lifecycle class. On the estate's record (TR-1, GR-2, SP-B and GR-3a each needed a repair commit) 3 is the floor and 4 the working ceiling. Confirmed from the reader: ventureLedger.js / ventureLegs.js / ventureNews.js all absent; supplyShipments.js 210 eff and seaLanes.js 277 eff at src/domain/spatial/; commodityFlowEnabled lit in full_simulation; routeLifecycleEnabled measured false so new_route suppresses honestly; errandMint.js 87 eff with errandSpineEnabled manifested; demographicsPlans.js:21's \"must not become one\" fence is verbatim and TR-7 takes zero edits there. ONE CAUTION ON THE READER'S WORDING: they say \"seatBooks is UNBUILT and strategicPosture.js:36 says so in its own words\". :36 says `grep seatBooksEnabled src` → zero hits, which is about INT-1's FLAG; src/domain/worldPulse/warSeatBooks.js DOES exist (readWarSeatBooks, seatBooksPartition, consumed by armyTransitKernel.js:56). The conclusion — TR-7's books term is ABSENT-not-zero until INT-1 — survives, but a grep-driven implementer will hit that name collision, and TR-7's absence-then-presence pin pair must scope by module or it will certify the wrong books."},{"wave":"TR-8 — THE TRAVELING FACTOR (factorErrandsEnabled; two slices)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":5,"why":"The reader enumerates four cars (a commercialErrands + registry flip + fences + speed-floor extension, b the TR-5 suppression switch + the K.2 stale-arrival pin, c the compromised factor both arms, d capture→hold→ransom→return) and then prices the wave 3–4, so the low end is again below their own list; and the list omits the repair car every landed wave in this estate has taken. Slice 1 is genuinely cheap — SP-D's spine does the work and the landing surface is pre-reserved, which I verified verbatim: ERRAND_CONSUMERS at envoyErrandVocabulary.js:315-321 carries consumer 'factors', purposeClass 'commercial', module 'src/domain/worldPulse/factorErrand.js', wave 'TR-8', built: false, with a both-ways walker (tests/lint/errandConsumerRegistry.walker.test.js) that reds a minter without a row AND a row whose module does not mint; 'commercial' is already a member of PURPOSE_CLASS_SET (envoyErrandVocabulary.js:131); and envoyTaskCatalog.js:291-293 records the unblocking act in the exact words the reader quotes. The SPELLING COLLISION IS REAL and is a charter-stall: the registry pins factorErrand.js while the TR volume names commercialErrands.js, and the registry's spelling is quoted inside envoyTaskCatalog's evidence string, so either the leaf takes that name or the row plus its evidence prose move in the same commit. Slice 2 is where the price sits and it is a four-system round trip: the covert declared-vs-true purpose seam both arms, capture through foreignGuestHold.js (424 eff, one writer) with an import pin proving no second hold writer survives the wave, ransom on the I2 person-subject claim, and the return leg — plus the speed-floor walker extended by TOTALITY (every movement site through WR-7a's one transit kernel, not a sample). THE RANSOM GATE IS REAL AND THE READER SOFTENS IT: ransomClaim.js (178 eff) at :181 requires a home settlement as debtor, so an untied captured factor cannot be priced at all, and the ransom persistence ruling sits on the owner queue in OWNER-QUESTIONS-32-34.md. Declaring that arm degraded-and-dark is the right chair call, but it is a recorded ruling with a live defect underneath it, not an absence of a gate. Add the suppression-at-call-site pin proven in BOTH flag states at the TR-5 call site, the JSON-alias round-trip, the four-fence plus lit-mutant set, the authored certification row, and factor-errand news kinds with their WHAT_PHRASES/heraldRouting/walker/pool rows. Four is the floor. Both TR-5 and TR-7 must land first and both are unbuilt, so this stays the family's terminal build wave."},{"wave":"TR-9 — TRADE CONVERGENCE INSTRUMENTATION (no flag)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"The reader's row was truncated mid-sentence at the T-11 herald debt, but their 3–4 is refutable on the estate's own precedent for the identical instrument. THE WAR ANALOGUE TOOK FIVE COMMITS AFTER ITS CONTRACT MODULE: WR-9a (fd2222699, 4 files/+926), WR-9r (06c58f69e, +309), WR-9c (7a3c51ef7, 4 files/+1,303), WR-9d (a70c92845, 7 files/+1,203) and WR-9 F-1 (d91e3ea05), and warConvergenceContract.js ended at 515 eff. TR-9's remaining slices are strictly MORE than WR-9's in one respect and less in another, and the net is upward. LESS: the eight certification rows are gone from TR-9's bill, and the reader is right about why — VIRTUAL_PENDING_RULE_KEYS = Object.freeze([]) at subsystemRowsVirtual.js:595, pinned to [] by tests/domain/subsystemRowsVirtual.test.js:647, and subsystemRowsWaves.js:715-728 records the supersession in words: casusCommerciiEnabled \"joined this list on 2026-08-04 with FP wave TR-1 and LEFT IT THE SAME DAY … the TR architecture's Q4 recommendation … is superseded for any TRADE flag that manifests\". The stale comment the reader flags is live at simulationRules.js:256-258 and still asserts the pending door. MORE: (1) THE T-11 HERALD DEBT IS A NEWS-KIND WAVE IN ITSELF, not a slice — five route_* kinds (route_chartered routeNetworkCharter.js:138/:141; route_promoted, route_demoted, route_abandoned, route_revived routeNetworkDecay.js:123-137) each owing WHAT_PHRASES, heraldRouting, receipt-pool rows and a totality pin, and I confirmed settlementRumors.js carries ZERO of them (its only route_ rows are TR-1's commercial_route_predation/:262, commercial_route_wardenship/:263 and an unrelated route_disruption/:334); TR-1's comparable news surface cost 289 raw in the news leaf plus 460 raw in pools. (2) Four envelope families (house-Gini, corner, pact-mix, endings-share) each with an INTERVAL_WEEKS-derived window, a tuning table and an EXECUTED mutant negative control. (3) tickScanBudget lanes for flows/supplyKernel/commodityFlow/entrepotKernel plus every new TR kernel, extending tests/perf/tickScanBudget.test.js (294 lines). (4) Soak observation joins into scripts/audit/behavioral-observation.mjs (1,233 lines). (5) The kind-availability walker encoding §2's precondition table — and there the reader's own catch bites: commonsVoiceEnabled has ZERO hits in simulationRules.js, so the table names a key the manifest does not carry and the walker must be reconciled before it reds rather than after. 9c itself is confirmed exactly as described: tradeConvergenceContract.js measures 398 eff / 736 raw, landed at c7933e845 (4 files, +1,534) with its 804-line walker and five manifest-rowed mutants, and bands deliberately unauthored."}],"summary":"FAMILY VERDICT: the reader's 24–33 cars is TOO LOW. Re-derived from the volume and the slot at a5876c0ea: 31–44 for the eight priced waves, and 32–47 for the family once the two unpriced items below are added. One row survives (TR-3), one is wrongly gated (TR-2), six are underpriced.\n\nTHE CALIBRATION THE READER GOT WRONG, AND IT MOVES EVERY ROW. TR-1 did NOT land whole in one commit. It landed in TWO: d7ea69a4b (18 files, +2,933) and 93c118b6d (4 files, +224) — the \"TR-1 red repair\" carrying the authored certification row and 143 lines of engineGatedRuleKeys walker repair. That is not an anomaly, it is the estate's habit: GR-2 = a18fdcfae (+3,945) + 91075d457 (+719); SP-B = 4c0f2f38b (+2,009) + 2a71dee3a (+591); GR-3a = 40afbdd64 + 0be4800db. A \"one-car wave\" here empirically costs two commits. Second, the volume's per-wave effective-line estimates UNDERSTATE by ~1.68x: TR-1's predicted 540 eff of leaf landed as 909 eff of new src (taxonomy 57 + reasons 289 + news 161 + receiptPools 386 + couplingRegistryTrade 16) plus 840 eff of new test, because the receipt-pool surface is unpriced in every wave row of the volume.\n\nFOUR THINGS THE READER MEASURED RIGHT, INDEPENDENTLY RE-VERIFIED. (1) The worker bundle is a non-issue: I re-walked the graph from src/workers/generation.worker.js and got 153 modules with ZERO under src/domain/worldPulse/** or src/domain/spatial/**, and zero simulationRules/settlementRumors/heraldRouting/commercialReceiptPools/distanceRead/certification. Their three tripwires are exact — factionArchetypes.js, src/data/foodImportRates.js (7 eff) and region/goodsCatalog.js (435 eff) — and I add a FOURTH: src/data/tradeGoodsData.js is also in that graph, and a goods-taxonomy reach is exactly how TR-4 and TR-6 would pay bytes against a ceiling (1,404,493 B, monotone-down, tests/build/generationWorkerLazy.test.js:78) that had to be argued in the ledger for 245 B. (2) pulseKernel is 1581/1581 and applyWorldPulse 907/907 in scripts/.size-baseline.json, pulseKernel banked permanently by R-BLD-10 — S44's zero-edit rule is a necessity, not a preference. (3) The declared-pending door is struck and frozen. (4) TR-1's writer is unmounted: advanceCommercialReasons (commercialReasons.js:457) has no caller in src/, the only cross-module import is tradeWar.js:69's reader, and spatialUsage.js:349 says so in measured words.\n\nTWO CARS THE READER LEAVES OUT OF THE FAMILY TOTAL ENTIRELY. First, the TR-1 WIRING WAVE they recommend as \"car 2 of the family\" appears in no wave row and no total; it is more than \"one mount line\" — it flips the certification row from \"A DARK INSTRUMENT WITH NO MOUNT … every channel DELIBERATELY EMPTY\" (subsystemRowsCompact.js:293) to an owed stateKeys channel, which is ~30 lines of re-authored prose plus the v5 state census grading, so call it 1–2 cars. Second, THE RECEIPT-POOL CEILING: commercialReceiptPools.js is 386/800 eff for TR-1's ~20 kinds at ~19 eff per kind, and TR-2, TR-5, TR-6, TR-7 and TR-8 all owe pool rows against 414 eff of headroom. Those five waves will need roughly 600–900 eff between them. A second family pool leaf and its walker amendment is owed somewhere around TR-5/TR-6 — a sub-car nobody has budgeted and a live STOP risk if an implementer discovers it at the publishing commit.\n\nTHE SHARPEST SINGLE FINDING — TR-2's MISSING SPINE GATE. strategicPosture.js:97 freezes POSTURE_ACTOR_KINDS to ['settlement'], :159 refuses an unknown kind, :162's own error text and the header at :90-95 both say widening it is \"an SP-4a amendment at the constitution, never a volume-local mint\", DESIGN_FP_ARCH_SP.md §8 seam 9 repeats it, and tests/domain/strategicPosture.test.js:137 pins the set. TR-2's Req-13 (\"acts are posture-priced, the SP-4a house instance\") and TR-7's overreach term are therefore both blocked at this base by a SPINE-constitution act that is not a TRADE decision and appears nowhere in the reader's dependency columns. The chair must either obtain SP-4a or strike the Req-13 clause and land both waves with posture pricing declared absent.\n\nONE MORE PREMISE THAT IS SIMPLY WRONG: \"TR-5 mints only grain_provision\". The volume's five commercial terms include route_wardenship (DESIGN_FP_TRADE.md:857-859), so TR-5 mints TWO catalog rows — and route_wardenship already exists as a TR-1 commercial-reason KIND across five files, which is a live vocabulary collision requiring a chair adjudication before an implementer starts.\n\nRECORD CONTRADICTIONS STILL OPEN AND WORTH THE CHAIR'S PEN, all re-verified verbatim: CR-2 consolidate-first (FABLE_VALIDATION_QUEUE.md:303, SOL_QUEUE.md:342) versus §5 #26's REPORTED-NOT-DEFECT; WY-3 ordered immediately before TR-4 both for the round-trip pin AND for the caravan body, with WAYFARE 12 of 12 unbuilt on unsigned §2a rows; TR-8's factorErrand.js-vs-commercialErrands.js spelling; commonsVoiceEnabled named in TR-9's precondition table but absent from the flag manifest; and TR-2b's sequencing, which SOL_QUEUE.md:346 and the first pass both record as the owner's and the reader clears unilaterally — defensible under the 14:4x grant, but it is a ruling to record vetoably, not a measured absence of a gate. Every TR wave remains dark behind its own flag at landing; the one declared same-seed lit-path shift in the family is still TR-4's calm-equivalence band, and TR-6's lighting moves TR-1's output too."}}
{"type":"started","key":"v2:00339a883ecaa218e2fb090ad9d641cabeaf9a62412327c62cdf4d03f2680377","agentId":"a45a7b6ed273c1c37","label":"refute:LG0 — LOGISTICS LG-0 and LG-1 (item #33, the family's first train)","phase":"Refute"}
{"type":"result","key":"v2:f70ff8ff048f6e2265c86fc848becc21299e688e81d709018381ff18d202d2e2","agentId":"a1d56aa9d14a6aadb","result":{"family":"WC — war circulation (DESIGN_FP_ARCH_WC.md, 431 KB, 17 waves in four arcs)","verdicts":[{"wave":"WC-0 REGISTRATION (the vocabulary wave)","verdict":"CONFIRMED","correctedLow":0,"correctedHigh":1,"why":"Re-measured at the slot and every claim holds. The five implementer cars are real and their shapes are what the reader says: `511d2ceb7` (peopleLedger 316 raw / 131 eff + warStance 148 raw / 50 eff + 3 test files, 854 insertions, 5 files), `4c87bae45` (migration.js +49, 242 insertions), `cf7e64660` (lawBandModulation 118 raw / 33 eff + the lawBandTable walker, 528 insertions), `e47d67631` (the two TERM_CATALOG rows + the re-derived census, 277 insertions), `9602952ba` (contributionLedger 145 raw / 45 eff + both flags + the blocks[] arm + the cert rows, 588 insertions across 15 files of which 7 are generated). The sixth commit `05b43ec7b` is a fence car from LGT-P13-FENCES, not a WC implementer car. Landmarks verified by line: MILITARY_COLUMN_CLASSES migration.js:504, the frozen union :509, isMilitaryColumn :521, the three-way release guard :641 (`isDemographicColumn(col) || isMilitaryColumn(col)`); worldState.js:163-174 the fail-closed blocks[] arm; simulationRules.js:460/:484 both flag names; contributionLedger.js:78 the two-flag conjunction; subsystemRowsVirtual.js:305/:355 the pending cert rows. All four modules have zero importers. The residue claim is CONFIRMED from the other side, not merely asserted: `grep warContributions src/lib/spatialUsage.js` returns nothing, and the cert row's own text at subsystemRowsVirtual.js:323 names WC-1 as the wave that closes it ('WC-1 lands the writer … at which point this row gains a real stateKeys declaration'). The R28 premise is REFUTED as stated: errandMint.js is BUILT at src/domain/worldPulse/errandMint.js (294 raw / 87 eff), so WC-5's body text is wrong and a ledger-docs car is genuinely owed. 0-1 stands."},{"wave":"WC-1 THE ARRIVAL LEDGER","verdict":"TOO LOW","correctedLow":7,"correctedHigh":11,"why":"The reader priced by eff-rate; the binding caps here are per-packet SHAPE caps, and WC-1 breaks four of them at once. PACKET_STANDARD (docs/implementation/PACKET_STANDARD.md:441-454) allows one behavior family, ONE new persisted record family, ONE user-facing surface, at most two new leaves each ≤250 eff, at most twelve handwritten files, at most eight acceptance cases. WC-1 mints TWO persisted families (spatialLedgers.warContributions AND the relationship edge archive with its three symbols) — two packets by the one-per-packet rule alone. It carries THREE user-facing surfaces (the display credit chip; the Herald arrival + close beats with their K7 authored sentence families, 7.C rows wc_contribution_arrival and wc_contribution_close; the DM verb through the operationRegistry + gen:compendium-data lifecycle) — three more packets. contributionLedger.js goes 45 → ~300 eff, which is over the 250-eff leaf cap AND is a ~255-eff delta to an existing file, so it splits. Add contributionReads.js (~150 eff), CONTRIBUTION_DELIVERY_GRADES, the war_contribution obligation deposit at EVERY generosityReactions mutation site with the decay-single-owner pin (its own behavior family), and the WC-0 residue (the spatialUsage TRACKED row and the certification stateKeys, which must ride the first setSpatialLedger commit or spatialLedgerCoverage reds the other way). Five hardest-negative pin families, and the exact-once close is two pins with separately executed mutants. Seven is the floor by shape; nine to eleven is the realistic band. One correction to the reader's own row: 7.D puts forgiveContributionDebt at WC-5, not WC-1 — WC-1's only DM verb is annotateContributionRow."},{"wave":"WC-2 THE DERIVED STANCE","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"The reader's low end of 4 is not reachable and the top of 6 is tight. No persisted family and no flag, so the caps that bind are surfaces and consumers. Two user-facing surfaces (the stance chip plus war-document rows over the existing src/domain/display/warStatus.js and warResolve.js; and the crossing beat wc_stance_crossing with its authored family) are two packets. stanceScalars attach at THREE existing logic-bearing sites (warHomeCosts accrual, warFactorForCasusRead, peaceTermsCoalition.js:261's coalitionShares) — exactly at the three-existing-file cap, so one packet and no room to also carry the derivation. warStanceOf's ~120 new eff plus the totality/at-most-one-principal/commensurability pins and the two source-scan fences (DERIVED-NOT-DECLARED plus the no-anchor-key companion) is one to two more. The coalitionShares fork with its DIFFERENTIATED inverting fixture and the CR-WC-7 declaration is its own car because it is the one arm whose lit behavior changes. The K5 termsWeightRead plus priced shielding through peaceTermsAppraisal is one more. Five is the floor, seven realistic. Everything else in the reader's row — dependency WC-1 per 7.F, no landing gate, dark behind the conjunction, lit-only coalitionShares shift, eighteen tuning values at soak — re-derives correctly."},{"wave":"WC-3 THE RELAY","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"relayNetwork.js at ~250 eff sits exactly ON the per-leaf cap, so it cannot also absorb the conservation teaching to the shipment rebuild, and it holds at least four behavior families (staging composition, per-hop law-band efficiency, skim accounting, the troop/stores fork) against the one-family-per-packet rule. The conservation surface is unusually heavy: the per-consignment relay walker (debited = arrived + consumed + skimmed + destroyed, integer-exact, through at least one in-transit tick, never save/load only) is a walker with its bucket-drop mutant AND the identity taught to the shipment rebuild IN THE SAME COMMIT — that is one car by itself. PEOPLE-ARE-NOT-STORES carries two separate executed mutants; SKIM-ATTRIBUTION LAG needs a lagged fixture (skim at T, rumor at T+k), an omniscient twin, and the omniscient-attribution mutant. Two beats with authored families (wc_relay_shortfall with the typed reason `unknown_hands` as a toEqual set member, and wc_relay_skim as the naming beat) are one surface packet plus a lighting-census bill. Registering the RELAY CURVE ROW into WC-0's empty table can ride the efficiency car. Five is the floor. The reuse claim is CONFIRMED against code: distanceRead.js, entrepots.js, supplyShipments.js and lawWord.js are all present at src/domain/spatial and src/domain/worldPulse, so the wave composes rather than invents routing."},{"wave":"WC-4 STRATEGY AND COERCION","verdict":"TOO LOW","correctedLow":6,"correctedHigh":9,"why":"The volume itself says the coercion gradient is a SECOND behavior family that cannot share a packet, and R6's elective OVERSHOOT writes into THREE named payoffs — the K5 read, the WR-10 market appraisal, and RELEASE composed into occupation.js's stabilizationSuitability (:386, the existing input to advanceOccupationState) — which is over the two-direct-consumer cap, so coercion alone is two packets, not one. The chooser (contributionMoves.js ~150 eff) and the executor (contributionDispatch.js ~180 eff) are two behavior families and therefore two packets even though two new leaves are allowed per packet. The reservation arithmetic is a distinct single-writer state with four separate pins (musters strictly less, the cap holds, recall restores from the NEXT tick, a lapsed reservation emits no beat). The three-deliverer coercion pin with its standing-deleted and standing-into-regression mutants is a fixture car. The distance-weight pin is explicitly a DISTRIBUTION pin over the corpus, not single-seed. Seven tuning values, the second-densest cluster. Six is the floor, nine realistic; WC-4 has no beat in 7.C, which is the one thing keeping it off ten. The HB-1 discharge is CONFIRMED in code: src/domain/worldPulse/strategyMoves.js exists (110 raw / 22 eff) with tests/lint/strategyMoveVocabulary.walker.test.js, so WC-4 amends a landed leaf rather than minting a list — the reader is right that this is cheaper than the volume's text implies. ⚠ ADDITIONAL COST THE READER MISSED: WC-4 is the first mover of `muster` and `dispatch`, and the LANDED, FROZEN peopleLedger.js EVENT_SIGNATURES give BOTH a mandatory tag arm — `tagArm('veterans_first', ['cohort','free_lance'])` — over rows that live in spatialLedgers.residentCohorts, whose ONE WRITER does not land until WC-13. The module's own docstring is categorical ('A mover that debits census without running its arm diverges from this object and reds'). WC-4 therefore owes an explicit pre-pinned seam and a recorded 'tag arm vacuous until WC-13' deferral, or the arm ships unexercised for nine waves — precisely the phantom-garrison mutant WC-13's tag walker is built to catch."},{"wave":"WC-5 CALL-INS, EXITS, AND THE DOCTRINE PAGE","verdict":"TOO LOW","correctedLow":7,"correctedHigh":10,"why":"The reader read this as one leaf plus exits; 7.C shows FOUR news kinds land here (wc_call_answered, wc_call_refused, wc_debt_forgiven, wc_debt_conversion), each owing an authored sentence family under K7 and a pool extraction, plus the DM verb forgiveContributionDebt through the store-action lifecycle — that is two to three surface packets before any engine code. callInErrand.js at ~200 eff holds at least four behavior families (the CALL_IN_REQUEST record with its cap, the six-weight loaded answer fork, the lapse horizon with its 'silence is not refusal' grading, and the OUTCOME axis off readWarTerminations). K8's exits are three more independent families (the forgiveness gesture with its archive row through the one relationship writer; the debt-to-vassalage intent road through the WR-10 stages; the jubilee producer alone). The doctrine reliability page is a display seam plus a pre-pinned HABIT r5 seam with an executed red-capability proof. THE ONE-BALANCE PINS are two pins (a source scan plus a differentiated behavioral pin where forgiveness must move both the answer fork and the K5 weight in the same tick). Seven is the floor. The SP-D discharge is CONFIRMED in code: errandMint.js is present, tests/lint/errandConsumerRegistry.walker.test.js exists, and envoyErrandVocabulary.js is present — so the ERRAND_CONSUMERS row plus the same-commit built:true flip is genuinely the cheap path. The critical-path warning (WC-9 waits on this wave) re-derives correctly from 7.F."},{"wave":"WC-6 BLOCK ROSTERS AND THE CONSERVATION WALKER","verdict":"TOO LOW","correctedLow":11,"correctedHigh":16,"why":"Every structural cap in PACKET_STANDARD binds here simultaneously. The armyTransitEnvoy.js extraction is a pure-motion car with goldens both sides (armyTransitKernel.js measured at exactly 798 eff / 1,130 raw on the slot — the ceiling ledger's figure re-verified) and owes a size-baseline bill. warBlockRoster.js at ~320 eff is over the 250-eff leaf cap and splits. deployment.blocks[] with drawnCohorts, and the transit projection field, are persisted-shape changes — one per packet. The compositeArmiesEnabled CQ5 trio is a flag car that serializes against every other flag wave. The spatial arm of contributionDispatch over the first `reinforcement_column` carries the column key-collision pin. Share-integral accumulators at BOTH altitudes with the anchor→episode map is its own family. peopleLedger.js going live is another. THE CONSERVATION WALKER v1 spans nine events, each with its tag arm naming a TAG_SELECTIONS member, and its red-capability must be proven by executing a one-count-drop mutant on EVERY branch INCLUDING the birth source — that is realistically two cars, not part of one. The reconciliation pin carries THREE mutants (headcount-skew, missing-self-block, elected-row-written-into-the-levy-map) against a restated directional law whose old form is recorded UNPASSABLE. The BANK-MOVES pair runs on a CONSTRUCTED levy fixture with a non-emptiness assertion first, because war_levy is MEASURED SILENT — verified verbatim at src/domain/certification/subsystemRowsWar.js:55, 'war_levy fires ZERO times in every case while its siblings fire'. Then display/armyRoster.js (~150 eff, a surface) and the DM verb editBlockRosterNote (a second surface). Eleven is the floor; sixteen is credible for the volume's largest wave, and the estate has landed an 18-car wave before (ENC-3). CR-WC-9 is not an open landing gate but it is a chair ruling on persisted shape standing on the vetoable 14:4x delegation — the first pass still carries it as OWNER-QUESTIONS item 9 awaiting an owner line, and the reader's flat 'SIGNED, no landing gate remains' overstates its finality."},{"wave":"WC-7 REALIZED STANCE AND THE CROSSING","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"The cheapest wave in ARC B by bytes (~10 KB of new source) and the reader is right that the price is pins, not lines — but four is unreachable. Six distinct behavior families land: the time-integrated share feeding warStanceOf; the crossing edge-detect with the mission-creep beat; the dependency-risk forks over own-sheet truth on the outboundImpression template; the enemy's BELIEVED share as hop-worn composition news at crossings; and the ES composition-intelligence tap as a new PRODUCT row gated on the existing espionageEnabled conjunction (espionageMath.js is present at src/domain/worldPulse/espionage/ with TAP_LEVELS closed, so the reader's 'a new product, not a new level' reuse claim is sound). Two of those are user-facing surfaces (wc_stance_crossing and wc_composition_revealed in 7.C), each capped at one per packet and each owing a lighting-census bill. THE RUNG-SWAP PIN is a fixture car in its own right — it asserts that two rungs EXCHANGE and that all FOUR scalars re-derive for BOTH parties in the same tick, with the declarer-disjunct mutant that restores the priced-shielding hole. THE UPDATE-SET TOTALITY PIN is derived from EVENT_SIGNATURES with toEqual against a named list and carries two plants (the block-mover-that-skips and the dispatch-counted mutant). Add the event-updated-only integration pin, the crossing exact-once pin, the BELIEF-LAG pin with its omniscient twin, and the proseNumerics grep fence. Five is the floor."},{"wave":"WC-8 THE BLOCK FORKS","verdict":"TOO LOW","correctedLow":6,"correctedHigh":9,"why":"blockForks.js at ~300 eff is over the 250-eff leaf cap and splits on its own, before any surface or pin is counted — the reader acknowledged the over-cap and then still priced the wave from five. joinPreference.js (~120 eff) is minted here with its single-scorer source-scan fence and the second-scorer plant, which is one packet because the fence is the whole point (three consumers, one scorer, and WC-11 must be unable to grow a second). The four outcomes wired to people-ledger events are one family (stay; return = depart + arrive_home; defect = a conserved block transfer; resist = the return pair plus occupation.js's advanceResistance as a consumer input). THE DEFECT-TARGET PIN is declared by the volume to be the pin without which the wave is unbuildable — a deterministic receiving army from a closed candidate set, reasons pinned with toEqual, and an empty set that re-normalizes rather than silently staying — that is its own car. The NEWS-GATE pin constructs the in-flight window explicitly with an omniscient twin. The COHESION pin requires lawful single-valuedness, chaotic largest-remainder summation, and a non-member law word NULLing the row upstream. Occupied-block WATCHED taint plus occupied-home credit continuation is another family; the wc_block_fork beat with its typed loading reasons is a surface plus a bill. Six is the floor. The round-2 correction the reader cites (WC-8 must precede WC-11) is CONFIRMED verbatim in 7.F."},{"wave":"WC-9 THE SERVICE INTEGRALS","verdict":"TOO LOW","correctedLow":9,"correctedHigh":13,"why":"serviceIntegrals.js at ~290 eff is over the leaf cap and splits. It also mints spatialLedgers.serviceBonds — a FOURTH persisted ledger which, under the FINAL FOLD LAW, must land with its certification row and its spatialUsage manifest row in the SAME commit, and which carries four of its own pins (drop-when-empty, symmetric by key normalization, event-updated-only, and a source scan proving bondOf reads THIS ledger and not the relationship axes). This is the wave where four deferred rows all come due at once, each a behavior family: CONTRIBUTION_CLOSE_GRADES is MINTED here with a four-member dead-arm totality census; the relay comradeship term LIGHTS with RELAY_COMRADE_BONUS as a back-edit into WC-3's leaf; K5's termsWeightRead gains its comradeship term as a back-edit into WC-2's read; and the 1.6.4 COMRADE FIDELITY RIDER lands bounded by COMRADE_FIDELITY_MAX with THREE mutants (term deleted, cap removed, depth raised) and a pre-pinned HABIT r5 seam with its own executed red proof. Directive (h) demands FOUR separate consumer pins, one of which — the alliance-odds pin — must assert a REAL read through warCoalitionDecision.js:108-109 (the file is present and verified), and another of which is a WY-3 caravan seam row with a red proof for a consumer that does not exist. Betrayal-priced-by-intimacy lands inside EXISTING rule bodies, and the war-close fold writes both the relationship patch and the `shared_service` archive SUMMARY row. Ten tuning rows in 7.B — the densest cluster in the volume, every constant owing its in-file one-liner and owner signature at soak under THE PROMISE. Nine is the floor. The corrected three-edge dependency (WC-3, WC-5, WC-6) is CONFIRMED verbatim in 7.F."},{"wave":"WC-10 ENDURANCE AND CONCENTRATION","verdict":"WRONG DEPENDENCY","correctedLow":6,"correctedHigh":9,"why":"⛔ A THIRD MISSING EDGE, of exactly the class round 2 caught twice. 7.F reads `WC-10 <- WC-3, WC-6` and the reader repeated it. But §1.15 states PARTIAL WICK-APART's selector order verbatim as 'weakest comradeship BONDS, nearest homes, homes-under-threat per the block's BELIEF, longest-served / MOST-DRIFTED', and WC-10's own PINS require 'the selectors are a stated total order; the shuffle mutant reds'. The first selector reads the per-pair comradeship integral and the fourth reads the drift accumulator — and BOTH are minted at WC-9 (§1.6.1 sites spatialLedgers.serviceBonds and bondOf there, and §1.6.3 puts the drift accumulator there; 1.6.1 explicitly records that the pair integral 'had NO declared home anywhere in this volume before round 3'). Under the graph as written, WC-10's flagship ordering pin is authored over a four-member total order two of whose members cannot be computed — an ordering pin over uncomputable selectors is either unbuildable or vacuous, which is the same defect shape as the two edges round 2 restored. Corroborating evidence that this is an omission and not my misreading: §1.6.1's own four-consumer census for serviceBonds lists 1.7.2's affinity matrix, 1.3.2's relay term, 1.9.2's answer fork and 1.6.4's bondOf — and does NOT list 1.15's wick-apart selector, so the volume under-counted its own consumer the same way it under-counted the ledger's home. THE CORRECTED EDGE IS `WC-10 <- WC-3, WC-6, WC-9`. This moves WC-10 out of the slot that could run beside WC-7/WC-8 and pushes the whole free-companies arc right; WC-11's transitive closure is unchanged because it already depends on WC-9. Separately the reader's summary understates the WY-8a build gate's reach: armySupply.js is CONFIRMED ABSENT from the slot, and because WC-12 <- WC-11, WC-13 <- WC-11, WC-14 <- WC-13, WC-15 <- WC-13+WC-14 and WC-16 <- WC-15, that gate transitively hard-blocks SEVEN waves (WC-10 through WC-16), not two. On price: four is unreachable — the freeCompaniesEnabled CQ5 trio is a serializing flag car, the envelope carries a three-assertion monotonicity family, the carrying-capacity network read and the dueling-envelopes siege read with K4's siege document surface are separate families, the massing tell is an additive rumor-carrier entry with its own pin, and the supply-consequence one-clause veto is executed as a WRITE-SET assertion. Six to nine."},{"wave":"WC-11 FISSION AND FREE UNITS","verdict":"TOO LOW","correctedLow":9,"correctedHigh":13,"why":"freeUnits.js at ~370 eff is the largest leaf in the volume and is 48% over the 250-eff cap — two to three packets on its own (FREE_UNIT_STATES with unit records and normalizer; the comradeship-graph fission cutter; drift-weighted join preference consuming WC-8's leaf; the `merge` reconsolidation; forage escalation are five families). freeUnitKernel.js (~200 eff) is the estate's SECOND counts stepper and a separate family. THE COUNTS-MOVER MANIFEST is explicitly a wave OPENER and its own car: a second totality manifest with its own discovery signature over armyTransitKernel.js's stepArmyPosition sites and migrationKernel.js's releaseMigrationArrivals, gaining freeUnitKernel.js BEFORE the stepper lands, with pins proving the two censuses stayed DISJOINT (a planted named-person token must NOT enter it) — because widening law M would contradict CR-WC-2. THE AMNESTY PRODUCER AND ITS EXECUTOR land together with a vacuous-term negative and its positive both executed. THE MERGE PIN needs a differentiated should/should-not fixture asserting the older key survives, per-origin headcounts sum at the KEY level, and that the pool-level identity provably does NOT move. The war-ended-unknown pin needs a unit operating beyond news reach after termination. Two beats (wc_fission, wc_stand_down) plus the DM verb removeFreeUnit with its two dispositions are two surfaces. Nine is the floor. Inherits the WY-8a hard build gate through WC-10 (armySupply.js CONFIRMED absent) and blocks all of ARC C and ARC D."},{"wave":"WC-12 BRIGANDAGE AND THE EMBATTLED STATE","verdict":"UNTESTED","correctedLow":7,"correctedHigh":10,"why":"The reader's JSON was truncated mid-WC-11 and supplied no row for this wave, so there is nothing to confirm or refute — priced here from the volume and the code. brigandContest.js (~200 eff) holds at least five behavior families: nearest-settlement targeting by road and warmth with hostile-as-prey; the repel/embattled contest reading defenseLedger military and internal (src/domain/defenseLedger.js present); the embattled stressor injected through stressorsCore's normalizeStressor on the pestilenceKernel precedent; EMBATTLED_RESOLUTIONS with buy-off as a graded close; and embattled crime coupling (a new pressureModel CRIME_ARCHETYPES member plus the smuggling-to-besiegers loop, with espionage porosity composing through the EXISTING crime term under a worse-of source scan). NETWORK_CARRYING_CAPACITY going live is a sixth, and it carries THE TWO-CAPS COMPOSITION PIN — raising world mass with carried supply held constant must SHORTEN the army's envelope, with the network-cap-cuts-armies-directly mutant redding and a companion arm proving FISSION_SLACK is grace against the ENVELOPE alone. THREE beats (wc_brigand_arrival, wc_embattled, wc_buy_off) plus the DM verb resolveEmbattledState are two surface packets plus a bill. The contest pin is a dead-arm census whose band edges must be DERIVED from the measured defense distribution per §5.5's POINTS-GAP lesson, not authored. E16/E17 land with their resolvers and make WY §4's 'anti-vacuity at fifteen' stale — the one place this volume moves a sibling's guard, owing an erratum in WY's own §4 in the same commit. Inherits the WY-8a gate through WC-11."},{"wave":"WC-13 HOP-AND-SHED AND THE ONE ABSORPTION MARKET","verdict":"UNTESTED","correctedLow":10,"correctedHigh":13,"why":"No reader row (truncation). This is the second-heaviest wave in the volume and 7.E gives it six guard rows, more than any wave but WC-0 and WC-6. moverAbsorption.js at ~270 eff is over the leaf cap and splits (parking/capacity search; shed apportionment town by town; the host rejection fork loaded by density/relationship/law/fear; rejected-moves-on-hungrier escalation into WC-12's brigand branch). residentCohorts.js (~250 eff, AT the cap) lands here as THE ONE WRITER of a THIRD new persisted ledger — one persisted family per packet, plus its certification row and spatialUsage manifest row in the same commit. THE TAG-LEDGER TOTALITY WALKER covers THREE laws with FOUR executed mutants (a tag without its census credit; a census credit without its tag; a census DEBIT run without its tag arm — the phantom garrison that used to be the design; and a second overlay planted against LAW 3) — one to two cars, and it is the walker that makes WC-15 a read-only wave over people. The moverAbsorptionEnabled CQ5 trio is a serializing flag car. THE SHED EVENT in final form writes census and the cohort tag in ONE event. The `reclass` TAG EVENT producer (`veteran_return` → `shed_column`) lands with a before/after headcount identity pin and the reclass-as-drop-and-recreate mutant — without it WC-13's key-collision re-run would pass over a class nothing stamps. THE RAZED-DESTINATION PIN carries two mutants. Add the shared absorption market (military columns joining competeForDestinations), homecomingEffects.js (~120 eff), and two beats with their families. Ten is the floor."},{"wave":"WC-14 HOMECOMING AND THE PULSE","verdict":"UNTESTED","correctedLow":6,"correctedHigh":9,"why":"No reader row (truncation). demobilizationPulse.js (~180 eff) plus the lighting of homecomingEffects' double arithmetic. The distinguishing cost is that this wave writes into a SIBLING VOLUME's machinery twice: introduction attempts land as a FOURTH traditions relation pass through traditions/relations.js advanceRelations (adoptedFrom = origin mix), and faith exposure lands as a bounded nudge on traditionsKernel's sanctioned religionState share — each a separate behavior family with a foreign owner and each capped at one user-facing surface. THREE beats (wc_homecoming, wc_introduction_attempt, wc_institution_founded) plus the demographic-signature-of-a-peace receipts are a surface packet and a bill. The double-effect pin requires ONE call read twice with the two-computations mutant redding. The most-attempts-fail pin is a corpus-band assertion, not single-seed. The founding pin runs both directions (founds above the pooled threshold, decays through peace). THE ORPHAN PIN is re-aimed at the routing this volume now states: a destroyed-home block becomes a free unit, walks, and reaches a settlement ONLY through `shed` at exactly one addressable host, with the block-straight-to-free_lance mutant redding because that arm is no longer in the event table. Institution supply writes reach mercenaryMarket and the INT leaves. Six is the floor."},{"wave":"WC-15 RESIDENT COHORTS AND DIASPORA BONDS","verdict":"UNTESTED","correctedLow":8,"correctedHigh":11,"why":"No reader row (truncation). The wave is read-only over people — residentCohorts.js gains no new people arithmetic — but it is surface- and pin-heavy. The veteranCohortsEnabled CQ5 trio is a serializing flag car. The four bond expressions are FOUR read-time modulations (trade affinity, war-reluctance strategist weight, aid warmth, and the espionage-ease term composed into the existing infiltration read), which is over the two-direct-consumer cap and forces at least two packets. THE CR-WC-21 RULED PIN SET (Position B: calendar bands kept, RECENCY_WEIGHTS reclassified a BOUNDED FLOORED band modulation) is three pins with five mutants — the count is mortality-driven under a calendar-count-decay mutant; the floor is real under two mutants (a fourth lower band appended, the floor weight set to 0); mortality is the only zeroing road. THE SELF-ORIGIN PIN carries both halves in one fixture with two mutants, and settles a question two waves needed opposite answers to. Add the year-fold mortality debit pro-rata by the settlement's own realized death fraction (in the same fold as the `mortality` event it partitions), COHORT_AGE_BANDS stepping, the `untag` and `demobilize` tag events with `untag`'s kind set asserted EQUAL to RESIDENCY_TAGS and a planted fourth kind caught, the no-new-decay-law source-scan fence, the embodied-vs-institutional sixty-year fixture, and cohort chips plus tables plus the DM verb annotateCohort as two surfaces. Eight is the floor. CR-WC-21 is ruled and UNBLOCKS this wave (DESIGN_FP_ARCH_WC.md:29-30); its live gate is CR-WC-9 like the rest of ARC B onward, plus the transitive WY-8a build gate."},{"wave":"WC-16 VETERANS AND THE ORDER DIVIDEND","verdict":"UNTESTED","correctedLow":7,"correctedHigh":10,"why":"No reader row (truncation). veteranReads.js (~150 eff) is small, but the wave modifies the MUSTER ROAD — a hot war-layer path — and its pins are the volume's most structurally demanding. Veterans-first muster lands MUSTER_VETERAN_PREFERENCE with largest-remainder in codepoint origin order, the balance from untagged census, then pro-rata, AND the tag arm that debits the drawn cohort rows in the same event plus the `untag` of any drawn person's free_lance overlay — this is what finally makes the landed `veterans_first` TagArm in peopleLedger.js's frozen EVENT_SIGNATURES non-vacuous, nine waves after WC-4 first emitted a `muster`. THE FIELD-OR-GARRISON DIFFERENTIATED PIN asserts opposite home-defense deltas from one cohort across both fork arms, and the mutant that must red is the OLD DESIGN (a `muster` with its tag arm deleted), with a companion arm over the embattled contest. THE MUSTER-AND-RETURN ROUND-TRIP PIN asserts residentCohorts is BYTE-IDENTICAL PER ROW, not merely equal in total, and the composition-discarded mutant it catches is what §1.13.1 said before round 3 — LAW 1 closes throughout, so only a round trip can see it. Add the three-reads-one-cohort pin with its stale-read mutant, the LIVE-NOT-FROZEN source-scan fence (no veteran term touches defenseProfile.scores), the greener-armies pin over two consecutive seeded wars, comradeship reactivation seeded off WC-9's service-bond ledger and the shared_service archive rows, inherited drifted temperament riding veteran blocks, the wc_muster_fork beat, and the DM verb setVeteranPolicy. Seven is the floor."}],"summary":"SKEPTIC VERDICT: the reader's census is right, its dependency graph is right in fifteen of sixteen places, its worker-bundle finding is right and measured — and its PRICES are systematically LOW, by roughly a third, because it extrapolated a per-eff RATE from the one wave in the volume that carries none of the per-wave burdens. FAMILY TOTAL: 114-166 cars, against the reader's 85-124.\n\nWHAT I CONFIRMED BY EXECUTION. Every BUILT/ABSENT row: warStance.js (50 eff), peopleLedger.js (131), lawBandModulation.js (33), contributionLedger.js (45) present with zero importers; all twenty named WC-1..WC-16 writers ABSENT; armySupply.js ABSENT. WC-0's five implementer cars re-measured from their diffs (854 / 242 / 528 / 277 / 588 insertions). Every landmark line: migration.js:504/:509/:521/:641, worldState.js:163-174, simulationRules.js:460/:484, contributionLedger.js:78, subsystemRowsVirtual.js:305/:355. war_levy MEASURED SILENT verbatim at subsystemRowsWar.js:55. The residue deferral confirmed from both sides — spatialUsage.js carries no warContributions row, and the cert row's own text names WC-1 as the wave that closes it. errandMint.js BUILT (294 raw / 87 eff), so the volume's WC-5 absence premise is REFUTED as the reader says. The worker bundle: a BFS of src/workers/generation.worker.js reaches 153 modules, ZERO domain/worldPulse and ZERO domain/spatial — WC adds 0 bytes to WORKER_BUNDLE_CEILING_BYTES = 1,404,493.\n\nTHE ONE BYTE BUDGET THE READER NAMED WRONG, NOW MEASURED. It said WC spends zero of the first-paint budget and stopped there. The budget WC actually spends is the LAZY ENGINE CHUNK's own band at tests/build/engineChunkLazy.test.js:67 (>200,000 and <1,400,000 bytes), which it never mentioned. Measured in the slot's dist: engine-BkijXS8q.js is 675,099 B — 724,901 B of headroom against WC's ~150 KB bundled. The conclusion survives; the reasoning did not. Second-order: engine-core-lazy is 23,846 / 50,000 B, and WC's shared closed vocabularies are exactly the hoist-into-shared shape, so ~26 KB is the real slack there and the first WC leaf to acquire a second eager importer spends it.\n\nTHE FINDING THAT CHANGES THE QUEUE — A THIRD MISSING EDGE IN 7.F. WC-10 is a WRONG DEPENDENCY. The graph reads `WC-10 <- WC-3, WC-6`; §1.15 states wick-apart's selector order as \"weakest comradeship BONDS, nearest homes, homes-under-threat per the block's BELIEF, longest-served / MOST-DRIFTED\", and both the bond and the drift accumulator are minted at WC-9. WC-10's flagship ordering pin would be authored over a four-member total order two of whose members cannot be computed. Corroboration that this is the volume's omission and not my misreading: §1.6.1's own four-consumer census for serviceBonds does not list wick-apart — the same under-count that left the ledger homeless until round 3. The corrected edge is `WC-10 <- WC-3, WC-6, WC-9`, which is exactly the shape of the two edges round 2 restored. Related: the reader's summary says WY-8a hard-gates \"WC-10/WC-11\"; by transitive closure through WC-11 it gates SEVEN waves, WC-10 through WC-16.\n\nTHE COST THE READER MISSED INSIDE ARC A. The landed, frozen peopleLedger.js EVENT_SIGNATURES give `muster` and `dispatch` a MANDATORY tag arm — tagArm('veterans_first', ['cohort','free_lance']) — over rows whose one writer does not land until WC-13. WC-4 is the first mover of both, and the module's own docstring is categorical that a census-debiting mover without its arm \"diverges from this object and reds\". Between WC-4 and WC-13 that arm is declared and unexercised, which means WC-6's conservation walker runs with its tag-selection check vacuous for nine waves — the empty-harness class §5.6 warns about, and precisely the phantom-garrison mutant WC-13's tag walker exists to catch. WC-4 and WC-6 each owe a pre-pinned seam with an executed red proof and a recorded deferral, or residentCohorts' ledger moves earlier (the volume already moved it once, WC-15 → WC-13, for this same producer-and-subject reason).\n\nWHY THE PRICES ARE LOW, STRUCTURALLY. The reader's anchor — WC-0's ~52 eff of production per car — is measured on the only wave that carries NONE of §3's \"EVERY WAVE CARRIES\" list: no writer, no beats, no authored sentence families, no DM verbs, no display surface, no tuning, no rng parity. The binding constraint in this estate is not the 400-eff line cap; it is PACKET_STANDARD:441-454's SHAPE caps — one behavior family, ONE persisted record family, ONE user-facing surface, ≤2 new leaves at ≤250 eff each, ≤12 handwritten files including tests. Four leaves in the volume are over the 250-eff cap and split by arithmetic (warBlockRoster 320, blockForks 300, serviceIntegrals 290, freeUnits 370, plus contributionLedger's 45→300 growth and moverAbsorption's 270). Four new persisted ledger families plus the edge archive are five forced packet boundaries. 7.C lists twenty-two news kinds and 7.D seven DM verbs — twenty-nine surface obligations that cannot share a packet with each other or with engine code. Five CQ5 flag trios serialize. My count implies 31-45 eff per car against WC-0's 52, and that is the point: eff-per-car FALLS as behavior density rises, because the file and acceptance-case caps bind long before the line cap does. Cross-checked against real landed comparables — EP-3A (955 insertions, 15 files, a ledger + stamp + 8 re-roots + 2 walkers), HB-1 I1 (1,690 insertions, 12 files, 2 leaves + 2 walkers), D2-a (396 insertions, 2 files) — the estate's cars run 400-1,700 insertions and 5-15 files, which is the grain I priced to.\n\nTWO RECORD CORRECTIONS. (1) CR-WC-9 is not \"SIGNED, no landing gate remains\". It is a chair ruling on PERSISTED SHAPE standing on the vetoable 14:4x delegation, and the first pass still carries it as OWNER-QUESTIONS item 9 awaiting an owner line. The distinction matters for eleven waves. (2) 7.D puts forgiveContributionDebt at WC-5; WC-1's only DM verb is annotateContributionRow.\n\nWHAT THE READER GOT RIGHT AND I WOULD NOT RE-LITIGATE: WC-0 PART-BUILT and effectively complete at 0-1 cars; every wave dark at landing behind the two-flag conjunction, with the only two same-seed shifts (WC-2's coalitionShares split under CR-WC-7, WC-6's bank-moves-to-arrival) LIT-ONLY and declared; the reuse inventory, which I spot-verified module by module and found accurate; and the headline it already drew — WC alone exceeds the first pass's 100-200-car band for the whole of #32. At 114-166 that headline is not merely true, it is understated: one family of thirteen consumes most of the programme's entire estimate."}}
{"type":"result","key":"v2:cdd0a2815f58bcdff8eb15b91a7d8c6a4210cc4cb3ed43e329f3ae44a934b135","agentId":"a21425ce437a3a426","result":{"family":"WF — FAITH (docs/DESIGN_FP_ARCH_WF.md; FP program 5 of 6)","verdicts":[{"wave":"WF-0 — THE OBSERVATION FLOOR (no flag; instrument-side)","verdict":"TOO LOW","correctedLow":2,"correctedHigh":3,"why":"Substrate all CONFIRMED at a5876c0ea: `deityBearers` is a private helper at religiousContest.js:265 called once at :591; whole-world-soak.mjs builds its grid with `customContent: {}` at :248 AND :713; subsystemRowsBaseline.js (252 eff) carries FAITH_SPREAD_OTHER's verbatim UNOBSERVED verdict naming the exact missing observation; the five religion soaks exist under scripts/audit/. The 1-car floor is not reachable. Three separable acts, each of WF-1a/WF-1E's own grain: (i) the helper promotion + the receipt field + the bearer-count invariant on the two rows; (ii) the authored deity-bearing case, which is not a fixture edit but SEED-HUNTING against the ONE-SOAK harness — it must simultaneously satisfy pilgrimage.js:31-38's grand-observance bar (scaleBand>=3, act in {procession,offering,fair}) at the patron host, >=1 mapped reachable neighbour, two opposed-quadrant deities seated only through mutateEntities.js:820/:864, and a horizon where a patron seat actually changes, all inside whole-world-soak.mjs's governed grid (settlements 1..30, archetypes 1-4 byte-pinned) with rules in soakRules.mjs; (iii) re-grading the two certification rows off UNOBSERVED with the certify-subsystems receipt, which is its own act with its own prose. ONE CORRECTION TO THE READER, in their favour: the `deityBearers` receipt field is NOT the OSR's business and is NOT a schema bump — whole-world-soak.mjs:1105-1121 records the ruling verbatim (the `beliefDivergence` precedent followed five times; §909 measured the 5->6 bump and the chair WITHDREW it). OSR = the observed-shape register (the schema-9 re-freeze, 42e12a76f, 1998 findings/387 files) and it is fed by SRC READERS of persisted world shape, not by a soak receipt. One honesty note on their outputMoving answer: world goldens do stay byte-identical, but the certification/receipt SURFACE moves at landing (a new field plus two re-graded verdicts) — instrument-side, not player-side, and worth declaring rather than calling the wave never output-moving."},{"wave":"WF-2 — PILGRIMS + LEGATES (`pilgrimageEnabled`; SPREAD; slices 2a season, 2b movers)","verdict":"TOO LOW","correctedLow":7,"correctedHigh":10,"why":"⛔ ONE DEPENDENCY IS STALE AND THE READER INHERITED IT FROM THE VOLUME'S V40. 'SP-2 believed-devotion is ABSENT' is FALSE at the slot: `DEVOTION_BANDS` ships at beliefAxisSubjects.js:115 (['secular','lukewarm','observant','faithful','devout']), `devotionBand` is one of the three landed SUBJECT_AXIS_FIELDS at :130, and it is ALREADY CONSUMED — pactFormation.js:261 passes `self.devotion` / `believed.devotion` into scoreFaithCommunion. So 2a's belief leg reads a BUILT believed-devotion band, and the 'injectable beliefLegsFor seam so SP-2 can supply legs later' is machinery for a dependency that has already landed — build the read, not the seam. Everything else the reader measured is CONFIRMED (errandMint.js 87 eff; the two reserved ERRAND_CONSUMERS rows at envoyErrandVocabulary.js:322-335 with built:false and purposeClass religious/personal; foreignGuestHold.js 424 eff; pilgrimage.js 41 eff at src/domain/traditions/, deity-agnostic). The range is still too low. 2a is 3-4 cars (leaf + co-location gate; the ~5-line subsumption fork plus the lit-vs-dark disclosure pin; the three-way seeded negative; the six season/arc narration kinds; the FaithSection season line — and WF-1E proves a panel line alone was a whole car). 2b is 4-6: the walker forces module-plus-`built:true` in the SAME commit, so legateErrand.js and pilgrimErrand.js are TWO cars by construction, not one; `envoyErrands` is a top-level CONDITIONAL ARRAY with its own dedicated normalizer (cloneAdmittedEnvoyErrands via ensureWorldStateWithEnvoyNormalizer, ledgerOwnershipManifest.js:36) so two new purpose classes owe closed-record validation, bounding, sort and deep-clone round-trips both arms; then interception through foreignGuestHold plus the envoyDiplomacyEnabled-dark degraded arm; then seven mover narration kinds."},{"wave":"WF-3 — STANCE CONSEQUENCES (`faithStanceConsequencesEnabled`; SPREAD)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":5,"why":"The substrate reading is right and generous: deityStance.js 60 eff with the :160-174 deferral comment intact and `aggression`/`treatyDurability` genuinely unconsumed; deityStanceLane.js 235 eff; sacredClaim.js 85 eff; GR-2's pactFormation.js (426 eff) and pactProposals.js (161 eff) BUILT so the durability clock binds against real pacts. But a 2-car floor is refuted by this family's own landing record. WF-1 shipped SIX cars for a mechanism of comparable size, and two of those six were a realm beat (WF-1C) and a single panel sentence (WF-1E) — this estate does not fold a display seam into a mechanism car. WF-3 owes, separably: the stanceConsequences.js leaf with the flag, the manifest row (engineGatedRuleKeys.walker asserts the EXACT one-key delta) and the subsystemRowsVirtual shell; the G1c fence captured BEFORE wiring (the J1 precedent) plus the REVERSAL pin in both directions and the COLOUR-NEVER-DROWN cap; the departed-minister lifecycle and the guard-absoluteness regression; six corpus kinds with producers (only faith.pact.betrayed has an existing site); and the FaithSection stance line. Four is the honest centre. Note also that new dials must join STANCE_TUNING/STANCE_LANE_TUNING with EXECUTED derivations or the compile STOPs (§43 rationale-or-STOP), which the reader mentions for WF-5/WF-7 but not here."},{"wave":"WF-4 — OMEN READS (`omenReadsEnabled`; LOCAL)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"This is the reader's own stated default-to-TOO-LOW shape: a new PERSISTED sub-ledger, a walker, a display seam and eleven narration kinds priced at a 3-car floor. Measurements CONFIRMED (heraldRouting.js 273 eff at src/domain/realm/, its `divination` desk live at HERALD_SECTIONS:64; spatialLedgers is one conditional container at worldState.js:455 so the sub-ledger costs no new top-level key). TWO OBLIGATIONS THE READER MISSED, both mechanical: (i) the spatialLedgers COVERAGE MANIFEST — spatialUsage.js:228-260 requires every key written via setSpatialLedger to be classified TRACKED or EXEMPT with a reason, and tests/lib/spatialLedgerCoverage.walker.test.js reds until it is, so the new key is a registration event in the same commit; (ii) `omenReadings` must take its branch in the ensureWorldState loop with the byte-order consequence pinned and the WORLD_SNAPSHOT_HARD_DENY census re-derived (the deny-census walker reads CONDITIONAL_LEDGER_KEYS). On top: five reading kinds each needing its own SEEDED failed-prophecy arm (the volume says once per kind — five fixtures, not one), the razed-mid-expectation close path as its own lifecycle fixture, the NO-PHYSICS inward import pin, the damped-lens echo, and eleven producers. Five is the floor; eight is reachable if the per-kind arms fight."},{"wave":"WF-5 — SCHISM + UNDERGROUND (`faithSchismEnabled`; LOCAL + covert seam; slices 5a split, 5b underground)","verdict":"TOO LOW","correctedLow":8,"correctedHigh":12,"why":"The reader calls this the hardest wave in the family and then prices its floor at five — one below what WF-1, the SIMPLEST state wave in the family, actually cost. Substrate CONFIRMED (religionState.js 373 eff; religionLegitimacy.js 227 eff; institutionTolerance.js 123 eff; zero `covert` anywhere in religionState.js today; clandestineFacet.js 75 lines and criminal-only). The VERIFY-AT-BUILD surface is worse than 'verify': institutionFounding.js (26 eff) exports only INSTITUTION_FOUNDING_KINDS and a reader, institutionRoster.js (10 eff) only isLiveInstitution/liveInstitutions, and foundingCatalog.js (57 eff) is a frozen table — there is NO exported founding writer. The live runtime founding path is moralInstitutionPressure.js:481-530 minting a `candidate.institution.found.*` through the institutionPatch action lane, so 5a's 'reduced-ceremony founding call' is a new call on someone else's candidate lane, with that lane's own pins, not a call surface waiting to be used. Slice-by-slice: 5a is 3-4 (creedRef through the split founding; the creed-aware backing fork with the ANTI-ENTRENCHMENT pin against a pre-split baseline; the split/unsettled kinds). 5b is 4-6 (the covert field in place; four evaluators; the PREMIUM-ISOLATION audit across all four audience renders, which is a security-class pin battery, not a line; the banded closed vocabulary; never-prune-the-cellar sequenced against religionState.js:353-357's landed KEEP=3 codepoint prune; exposure-at-zero seeded lower bound). Then ten producers and a COVERT_CONGREGATION_TUNING table whose two clocks (520w/156w) must carry executed derivations or STOP. Two persisted shapes also bill the trailing OSR mint — that part the reader has right."},{"wave":"WF-6 — FAITH TERMS (`faithTermsEnabled`; SPREAD) — EARLY IN §3j ORDER","verdict":"WRONG DEPENDENCY","correctedLow":4,"correctedHigh":7,"why":"The reader priced WF-6 down on a half-measured record and then named the wrong remaining work. They say WF-6 'mints CONSUMERS and a FORMATION TRIGGER' and that 'the faith rows carry NO CLASS_TERM entry ... so nothing drafts them'. The CLASS_TERM half is true (peaceTermsCatalog.js:385 has no faith key) and IRRELEVANT: that is the WAR draft path. THE PEACETIME PRODUCER IS LANDED IN FULL. pactFormation.js:174-179 carries `PACT_DRAFT_LENS.faith_communion` as a four-rung ladder over ALL FIVE faith terms (shared_rite / pilgrimage_right+tolerance_guarantee / missionary_access / temple_restitution); pactTriggers.js:226 carries `scoreFaithCommunion` with its spread and floor guards; pactFormation.js:261 wires it to the believed-devotion bands. And tests/domain/peaceTermsGrantTerms.test.js:156 records the discharge in as many words — 'IT WAS TEN, AND NINE HAVE BEEN DISCHARGED. GR-3b's rung ladders gave the five faith, three population and one security rows a real producer' — with :231 asserting bucketOf('missionary_access') === ['produced']. So the formation trigger is NOT WF-6's to mint; faith terms form and sit inert today under pactFormationEnabled, and `temple_restitution` streams generically off `spec.stream` (peaceTerms.js:248/:721), so the reader's no-executor read holds. What WF-6 actually owes, and why the 5-ceiling still fails: FOUR grant readers with ZERO src consumers (treatyEnforcement.js:270/:278/:285/:293 — confirmed, only couplingRegistryGrammar.js:148/:166 docblocks name them) wired into engine behaviour, of which missionary_access moving the CHANNEL and never the share touches religiousContest.js (493 eff); the tolerance_guarantee BREACH arm, which needs a faith-side signal reaching the compliance evaluator whose writes are at peaceTerms.js:674/:707/:759 — and peaceTerms.js measures 797 of 800 effective, three lines of headroom, so that seam is a live STOP-plus-extraction-recipe risk and the extraction would be its own car (the war tranche's peaceTerms decomposition was exactly that); THIRTEEN corpus kinds, the largest count outside WF-8; the bundle-composition fixture and the herald sentence. Catalog re-measured: 26 rows / 13 families (not 24/11, not 12/8) — the reader's figure is right and the volume's and its own WF-F correction are both stale."},{"wave":"WF-7 — THE TITHE (`titheEnabled`; LOCAL)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"Every measurement CONFIRMED, including the one that matters most and that the record does not carry: warDeployment.js is 684 effective with the conserved sack arithmetic at :803-812, and it has fallen out of scripts/.size-baseline.json entirely (the frozen map now holds only App.jsx 650, explanation.js 827, applyWorldPulse.js 907, pulseKernel.js 1581, roadsKernel.js 838, warTermination.js 818, npcGenerator.js 1345, settlementSlice.js 824) — so the volume's STOP condition is green today. Also CONFIRMED: templeWealth has ZERO writers (only warFaith.generated.js:4604/:5574 titles and the two recording comments), generosityEV.js is 403 eff with faithTerm at :250-253, religionState.js 373 eff. The 3-car floor is what fails. Separable acts: tithe.js plus the persisted banded templeWealth through the one writer plus its lifecycle/round-trip/OSR bill; the 13-week fold off intervalWeeks.js:7 with the cadence mutant naming 13; the three reads, of which the war-family sack coupling is a CROSS-FAMILY import that WF-1d proves costs a COUPLING_REGISTRY row in the same commit and was itself a whole car; the coffers-at-the-fall disposition, both arms, against WF-1's landed ring; ten producers plus the FaithSection tithe line; and a TITHE_TUNING table with executed derivations or a STOP. One sharpening of their lighting note: DS-FTH-4 is on dossierMounts.js's DARK list and its own comment at :504-508 says lighting needs BOTH the banded temple stock AND `tenure` added to projectReligionStateOntoSettlement, and that only ONE of its pools still says something DS-FTH-1 does not — so 'four already-authored pools un-darken' overstates the prize."},{"wave":"WF-8 — NARRATION 3x (`faithNarrationEnabled`; LOCAL; lights LAST)","verdict":"TOO LOW","correctedLow":8,"correctedHigh":12,"why":"The landed evidence refutes a 5-car floor on its face. 47ea9c9ba cost 22 files / 1,771 insertions to land ONE governed row, and faithNews.js:97-101's own docblock concedes the arithmetic: 'the FAITH volume charters roughly twenty further narration kinds and each needs its own producer, so WF-8 is a multi-member train by arithmetic.' The registry family, its picker, faithReceiptPools.js (9 eff) and the kindPoolFloors exception all landed, so ROWS are now cheap — but the owed work is fifteen §K voices at FIFTEEN DISTINCT EXISTING FIRE SITES (entry, eviction, suppression, resurgence, three standing crossings, two legitimacy, two piety, niche, two tolerance, mandate), each needing a transition detector and a reachability pin, plus five new realm arcs, plus the Reformation detector, plus the >=3x census walker, plus the recorded SHRINK-BACK at faithNews.js:107-112. Corpus verified exactly: §K 15, §L 7, §M 8, and §C's per-wave counts (9/14/6/11/10/13/10/22/8 = 103) match the section bodies one for one. R2 re-confirmed: COMPOUND_SIGNATURES holds five keys, none a schism. Headroom re-measured: settlementRumors.js 555 eff (not 482), realmEvents.js 309 (not 283) — 245 lines for twenty-plus WHAT_PHRASES rows is tight enough that the extraction fallback is a live second car, not a footnote. ONE BACKLOG THE READER DOES NOT SEE: the train is not confined to §K+§L. `faith_last_altar_dark` is a §D WF-1 kind and it landed HERE, at 8A, because the family did not exist when WF-1 ran — and patronFall.js:76 froze PATRON_FALL_CAUSES at FOUR ('discredited','displaced','imposed','suppressed'), so §D's faith.fall.abandoned has no producer at all. Un-narrated kinds from earlier waves fall to this train by default unless each wave is made to pay its own."},{"wave":"WF-9 — FAITH CONVERGENCE INSTRUMENTATION (no flag; the acceptance harness)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":9,"why":"Absence CONFIRMED: src/domain/certification/ holds warConvergenceContract.js, warConvergenceForces.js, warEndingClassifier.js and tradeConvergenceContract.js and no faith sibling; the two templates measure 515 and 398 effective. That is the tell the reader read past — a 3-car floor for a module whose nearest sibling is 515 effective lines of envelopes, when this estate's cars run 270-1,800 insertions. The content list is nine separable instruments: the contract module itself; the eight-token endings walker with `faith_converted` qualified and the instrument-arc vocabularies held OUTSIDE the mix behind `roads_closed`/`plundered` floors; per-mechanism occurrence envelopes across SEVEN mechanisms including WF-2's subsumption delta and WF-4's per-kind reading ratios; the NINE-row counterforce audit plus the sack lure; the five-arc realm envelope; the invalid-config walker (a SPREAD flag lit while faithSpreadEnabled is dark must red); the tenure tail; WF-0's case grown to a grid inside the one-soak harness; and a mutant negative control on EVERY envelope (L7) with its mutationCoverageManifest row. The owner gate is read correctly and is the most important sentence in the reader's whole packet: WF-9 can be BUILT under the 14:4x grant but cannot be CLOSED — 'the program is DONE when these envelopes hold on the owner-ordered soak, and not before', and the walk plus one regen is owner-held under TAIL RULED 08-06. Do not let an implementer report FAITH complete on a green harness."}],"summary":"VERDICT: the reader's SUBSTRATE is excellent and its ARITHMETIC is not. I re-measured every figure they quote at a5876c0ea and essentially all of them land exactly — peaceTerms.js 797/800, warTermination.js 818, warDeployment.js 684 and out of the frozen map, religionState.js 373, religionLegitimacy.js 227, religiousContest.js 493, deityStance.js 60, deityStanceLane.js 235, sacredClaim.js 85, pilgrimage.js 41, errandMint.js 87, foreignGuestHold.js 424, faithNews.js 73, settlementRumors.js 555, realmEvents.js 309, heraldRouting.js 273, subsystemRowsBaseline.js 252, subsystemRowsVirtual.js 284, warConvergenceContract.js 515, tradeConvergenceContract.js 398, the catalog at 26 terms / 13 families, the four idle grant readers, the eight-flag census (only faithUnseatingEnabled exists, 9 src / 9 tests), and the corpus at 1,411 lines whose §C per-wave counts verify exactly against its own section bodies (9/14/6/11/10/13/10/22/8 = 103). THE WORKER-BUNDLE CLAIM IS CONFIRMED AND IS THE PACKET'S BEST WORK: I recomputed both closures independently — the generation worker's import graph (153 modules by my resolver, which counts .jsx; the reader's 127 differs in method, not in conclusion) contains ZERO faith, traditions, worldPulse, herald or rumor modules, while the advanceInterval worker's 559-module closure carries all of them, and WORKER_BUNDLE_CEILING_BYTES (1,404,493, generationWorkerLazy.test.js:78) is the only worker byte ceiling in tests/build/. Every WF wave adds 0 KB to it. Their reading that every wave lands DARK and is output-moving only at the owner's second lighting also holds. WHERE IT FAILS IS THE CAR COUNT, and the refutation is this family's own landing record, which the reader cites and then does not apply: WF-1 — the SIMPLEST state wave in the volume — shipped as SIX cars (66fda66d4 21 files/1,335 ins · 39d0ad7c8 5/341 · adb5faf3f 8/420 · 1a90b5639 8/271 · 3ac279db4 12/852 · 5d18b4a0c 7/532), and two of those six bought nothing but a realm beat and a single panel sentence. The reader then prices FIVE of the eight remaining waves with a ceiling at or below six, including WF-4 (new persisted sub-ledger), WF-5 (two persisted shapes plus a four-render premium-isolation audit) and WF-7 (persisted field plus a cross-family coupling) — every one of them strictly larger than WF-1 in state. All nine rows move up: 47-74 cars, centre ~60, against their 30-49 / ~38. THREE SUBSTANTIVE CORRECTIONS beyond the arithmetic. (1) WF-6's dependency is misstated in the cheap direction and the wave is still under-priced: GR-3b landed the whole PEACETIME PRODUCER — PACT_DRAFT_LENS.faith_communion at pactFormation.js:174-179 ladders all five faith terms, scoreFaithCommunion at pactTriggers.js:226 is the trigger, and tests/domain/peaceTermsGrantTerms.test.js:156 records the discharge verbatim ('nine have been discharged') with missionary_access asserted in the 'produced' bucket at :231. WF-6 mints no formation trigger; it owes consumers, thirteen producers, and a tolerance_guarantee breach seam that must reach a compliance writer inside a file with three lines of headroom. (2) WF-2 carries a stale absence: 'SP-2 believed-devotion is ABSENT' is false — DEVOTION_BANDS ships at beliefAxisSubjects.js:115, devotionBand is a landed SUBJECT_AXIS_FIELD, and pactFormation.js:261 already consumes it, so 2a should build the read rather than an injectable seam for a dependency that has landed. (3) WF-0's receipt field is NOT an OSR bill and NOT a schema bump — whole-world-soak.mjs:1105-1121 records the beliefDivergence precedent and §909's withdrawn 5->6 bump in as many words; the OSR is the observed-shape register (schema-9, 42e12a76f) that persisted WORLD shape feeds, which is where WF-4/5/7's fields really bill. Smaller misses: WF-4 also owes the spatialLedgers TRACKED/EXEMPT coverage manifest row (spatialUsage.js:228-260, walker-enforced); WF-5's 'reduced-ceremony founding call surface' does not exist to verify — there is no exported founding writer, only moralInstitutionPressure.js:481-530's candidate lane; WF-7's DS-FTH-4 prize is one pool plus a projection change, not four pools; and WF-8's backlog is wider than §K+§L, because faith_last_altar_dark was a WF-1 kind paid by a WF-8 car and PATRON_FALL_CAUSES froze at four, leaving §D's faith.fall.abandoned with no producer. Their firstCar recommendation stands and I would keep it: WF-0 is the only wave that mints no flag and no world state, every later wave's own-footprint fence is worded against a deity-BEARING world that no fixture supplies, and it can ship while sitting item 1's §764.3 amendment is unsigned."}}
{"type":"result","key":"v2:675ffe40c34549bfabe8c9c9b9574125b624969a0f65484b90ef86edad5c1641","agentId":"a2e32bfaab3636a58","result":{"family":"HB","summary":"SKEPTIC PASS ON HB, all measurements re-executed at claude/composite-r4 @ a5876c0ea (tree clean) against docs/DESIGN_FP_ARCH_HB.md §3a/§3c/§4/§7.\n\nWHAT THE READER GOT RIGHT, CONFIRMED BY MY OWN EXECUTION, not read off its report. (1) THE WORKER CLAIM HOLDS. I re-ran the import-graph BFS myself: src/workers/generation.worker.js reaches 153 modules and ZERO src/domain/worldPulse; src/workers/advanceInterval.worker.js reaches 559 of which 375 are worldPulse; a scan of all 153 generation-graph modules for non-literal dynamic imports returns one comment line and no live specifier, so the BFS is not under-reporting. src/domain/traditions/* is also absent from the generation graph. No HB wave adds a byte to the generation worker — CONFIRMED, and the reader is the first pricer in this estate to prove that by execution rather than assert it. (2) THE TWO UNHOMED PIECES ARE REAL: circumstanceClassOf returns exactly one hit in src and it is a DOCSTRING (habitVocabulary.js:23); habitFactorFor returns ZERO; and subsystemRowsVirtual.js:177 says in its own words \"the classifier has no lawful home yet, which is a blocking question for HB-4\". (3) THE THREE STALE-RECORD CATCHES REPRODUCE: scripts/.size-baseline.json's only settlementStrategy key is _te_ceil_settlement_strategy_2026_08_31, \"812 -> 760, ENTRY DELETED\", executed by lane CEIL, not HB-1; errandSpineEnabled is live (simulationRules.js:271, errandMint.js:76) so HB-8's dark-forever clause is indeed false; the registry stands at 42 rows, 36 DEFER + 6 STAY + ZERO LEARN, and DEFER_CEILING = 36 exact at chooserTotality.walker.test.js:105.\n\nFIVE THINGS THE READER MISSED, ALL MEASURED, AND FOUR OF THEM MOVE PRICE UPWARD.\n\n(A) ⛔ THE LEARN SET IS PINNED EMPTY AND THE FIRST CAR DOES NOT NAME IT. tests/lint/chooserTotality.walker.test.js carries, under the comment \"⛔ NOT ONE ROW SAYS LEARN AT THIS WAVE\": expect(HABIT_FORK_REGISTRY.filter((row) => row.disposition === 'LEARN')).toEqual([]). The reader's firstCar enumerates \"three defects, all measured\" and this is a fourth. Every DEFER→LEARN promotion in HB-4, HB-5 and HB-9 must first convert that assertion into a LEARN-row shape contract, and that conversion belongs in HB-1's promotion-shape car alongside the circumstanceClasses pass-through, or the promotion is structurally impossible for a second reason the brief never mentions.\n\n(B) ⛔⛔ THE OWNER GATE IS NOT TWO FIELDS. §3a close 6 reads, verbatim: \"⭐ CARRIED — the tradition record persists across the observance and the open stamps habitEpisode on it\". That is a THIRD foreign stamped field, and the volume waves it away in one clause (\"Three of the four carried closes (4, 6 and 8) carry on records the estate already persists and mutates at the close, so the stamped field costs nothing new\"). Closes 4 and 8 genuinely stamp nothing — the table says so. Close 6 stamps. And the receiving record is NOT the open bag the whole Q1 cheapness argument rests on: TraditionRec at src/domain/traditions/genesis.js:92-109 is a fully enumerated JSDoc typedef with seventeen @property lines, minted at four sites (genesis.js:320, relations.js:142, relations.js:279, customFounding.js:47). DeploymentRecord is `{[k:string]: any}` and TreatyRecord is Record<string, unknown>; TraditionRec is neither. §7's own instrument claims foreign_fields_deferred \"asserts that every deferred spelling is named HERE — so a fourth spelling appearing anywhere in the volume reds this clause until somebody dispositions it\". Close 6's spelling is not named in Q1. So either Q1 widens to three fields with a typedef edit on a closed record, or close 6 is re-roaded — and close 5 is a declared VERIFY-AT-BUILD whose named fallback is \"CARRIES on the same road as close 1\", which would make four. The reader inherited the two-field framing without re-measuring the third record.\n\n(C) ⛔ HB-8's \"SEVENTH ES legRef\" IS WRONG AT THE CODE AND THE READER REPEATED IT WITH AN INVENTED DENOMINATOR. ENVOY_COVERT_LEG_REFS at src/domain/worldPulse/envoyErrandVocabulary.js:207 has FIVE members — readiness, routePositionBand, storesBand, strength, tierBand. COVERT_LEG_REF_SET (line 436) is derived from it and ENFORCED BY A LIVE VALIDATOR at envoyErrandRecords.js:469 (`refs.some((ref) => !COVERT_LEG_REF_SET.has(ref))`) over a persistence DTO. The reader wrote \"the ES tap-product ladder has six worked members to add the seventh to\" — an unmeasured figure copied from a stale volume clause. Doctrine would be the SIXTH, and admitting it is a validated-vocabulary amendment with a persistence-DTO consequence, not a list append. This is a FOURTH stale-record place the reader's \"three places\" census missed.\n\n(D) ⚠ A SECOND CEILING-PINCHED FILE THE READER NEVER NAMES. src/domain/worldPulse/beliefMap.js measures 781 effective lines against the 800 domain ceiling (eslint.config.js:712, skipBlankLines + skipComments) with NO size-baseline entry to absorb an overflow — the baseline holds only eight entries and beliefMap is not one. HB-6 budgets 6 lines into it and HB-7's seam 2 IS the beliefMap injected closure. Two waves stacking into 19 lines of headroom is the same trap the reader correctly caught at settlementStrategy and missed here.\n\n(E) settlementStrategy.js measures 760 effective, not the reader's 775 — exactly the number the TE-CEIL note records. Headroom is 40 lines, not 25. This one cuts the reader's way and I record it because a wave brief built on 25 would over-price the extraction.\n\nCALIBRATION. The reader's 21-39 centred 27 is about 3 cars per remaining wave. HB-3 alone lands eight close-side hooks in eight different foreign host modules, each a CHECK-GIT-FIRST visit with its own seeded fixture and four-fence; HB-9 mints seven closes plus six envelopes plus four cert literals plus a close-shape this estate has never built. Landed waves in this estate ran 1 car (SP-C) to 18 (ENC-3). My re-derived family band is 35-61 cars, centred near 45. Six of nine waves price TOO LOW; three CONFIRM. Nothing in the family is output-moving at landing — all four flags are virtual and lit in no preset, which src/lib/spatialUsage.js:322 states in source — and the generation-worker figure is zero for every wave, both CONFIRMED.","verdicts":[{"wave":"HB-1 — the action vocabularies + the fork registry (residual: the promotion shape)","verdict":"TOO LOW","correctedLow":2,"correctedHigh":3,"why":"CONFIRMED: fork() at habitForkRegistry.js:101 spreads the caller's row then hard-codes `circumstanceClasses: Object.freeze([])` at line 109, so the field always wins empty; `dyadicActions` returns ZERO hits across src and tests; `coalitionRatification` returns zero hits in the registry while castRatificationBallot is live at coalitionRatification.js:209 (346 effective lines) and called from envoyRatificationStage.js:224. All three reader defects reproduce. But the residual is FIVE items, not three. FOURTH: chooserTotality.walker.test.js pins `expect(HABIT_FORK_REGISTRY.filter((row) => row.disposition === 'LEARN')).toEqual([])` under the comment '⛔ NOT ONE ROW SAYS LEARN AT THIS WAVE' — a DEFER→LEARN promotion needs that assertion rewritten into a LEARN-row shape contract, and that work is part of making promotion structurally possible, which is the car's whole identity. FIFTH: §4's HB-1 charter mints DEAD_CODE as the fourth disposition expressly 'for convergence.reactiveResponse: measured-uncalled, chair-owed report, never deleted by this program' — FORK_DISPOSITIONS at line 53 carries DEAD_CODE, and ZERO registry rows use it (measured: 36 DEFER + 6 STAY + 0 LEARN + 0 DEAD_CODE over 42 rows, and no `convergence` module appears in the registry's module census). That row is owed with its chair report. So the car carries TWO chair-authorized registry mints (coalitionRatification as discovery:'checklist' with arity VERIFY-AT-BUILD, and the DEAD_CODE row), each moving a count the walker pins exact in both directions, on a shared file IN-1c/WF-8a/ENC-1/ENC-4 have each minted into (29→30→31→35→36, recorded in the walker's own comment). Two mints plus the LEARN-shape contract plus the arity-derivation walker arm against enumerateMoves' own conditionals is more than one sitting's proofs. Low-risk path is 2; 3 if the coalitionRatification arity VERIFY-AT-BUILD comes back negative and the row needs a second shape."},{"wave":"HB-2 — the ledger + the flag (residual: the twelfth coupling prefix's leaf)","verdict":"CONFIRMED","correctedLow":1,"correctedHigh":2,"why":"Re-measured and the reader is right on every limb. `HB` IS present in CHARTERED_VOLUME_PREFIXES at tests/domain/couplingRegistry.test.js:108 (thirteen entries now — WR TR GR WF POP IN INT SP CW ES WY HB ENC — so the volume's 'ELEVEN at HEAD' is itself stale, and HB is the twelfth admitted, ENC the thirteenth). src/domain/certification/couplingRegistryHabit.js DOES NOT EXIST — the only hit for the name in the whole tree is a docstring at couplingRegistry.test.js:89 naming it as owed. Q4 is half-executed exactly as the reader says: the document act done, the leaf owed. Seven sibling leaves exist to copy the shape from. The habit substrate itself is fully landed and dark: habitLedger.js:255 writeHabits with zero src callers (the only non-test hits are its own docstring and emigreErrand.js:14 citing it as precedent), habitGate.js:62 the ONE by-name `habitConditioningEnabled === true` read, the flag at simulationRules.js:307, the TRACKED 'habits' row at spatialUsage.js with the honest-limit paragraph at :322, and the AUTHORED cert row at subsystemRowsVirtual.js:165-187. The reader's deferral of §1.4's median/p95 measured bound to HB-4 is correct and honest: grep for median|p95 in habitLedger.js returns ZERO, and the quantity is structurally unmeasurable while the writer has no caller. Deferred-and-documented, not a gap. One car; two only if the CPL row's direction argues."},{"wave":"HB-3 — the credit fold","verdict":"TOO LOW","correctedLow":6,"correctedHigh":11,"why":"The reader's four-car centre prices the modules and under-prices the HOSTS and the GATE. THE HOSTS: §3a's eight closes land eight hooks in eight DIFFERENT foreign modules — warDeployment.js (684 eff), treatyDisposition.js AND treatyBreach.js (close 2's map imports BOTH vocabularies), the coalition close, demographicsPlans, tradeWar.js/contestOverThirdParty (449 eff), traditionsKernel.js (484 eff), the espionage mission close, and npcLadderGoals.js (135 eff) — plus the treaty MINT (two paths, mintTreaty and mintTreatyFromCarriedSheet) and warIntent's stamp. Every one is a CHECK-GIT-FIRST visit with its own four-fence, its own seeded fixture, and either a carry ROUND-TRIP or a recompute-equality pin that §3a rules cannot substitute for each other; close 2 owes BOTH. Eight such visits is not four cars in this estate. THE GATE IS MIS-SCOPED, and this is the sharper finding: §3a close 6 says the open 'stamps habitEpisode on' the tradition record, which is a THIRD foreign stamped field the volume dismisses in one clause and Q1 never names. The receiving record is TraditionRec at src/domain/traditions/genesis.js:92-109 — a fully enumerated seventeen-@property JSDoc typedef minted at four sites — NOT the untyped bag Q1's whole cheapness argument rests on (DeploymentRecord `{[k:string]: any}`, TreatyRecord Record<string,unknown> declared once in peaceTermsCatalog.js, both re-verified today, both genuinely free). §7's own instrument claims foreign_fields_deferred 'asserts that every deferred spelling is named HERE'; close 6's is not. And close 5 is a declared VERIFY-AT-BUILD whose named fallback is 'CARRIES on the same road as close 1', which would make four. So the owner line this wave needs is two cheap fields PLUS a typedef edit on a closed persisted record, and the chair must put that third field in front of the owner or re-road close 6 — a decision, not a detail. Add the unhomed classifier (a credit row is keyed <class>.<action>; circumstanceClassOf exists nowhere), the authored complianceToSourceKind bridge with totality asserted on both sides, close 8's banding into SEVERITY_LADDER by HB-0 edges, the observedState omniscience mutant, the four-severity-rung both-directions reachability MEASUREMENT on real generated corpora, and the reader's own correctly-spotted sequencing hazard (the pledge OPENs live at HB-4/HB-5, two waves later, so every carry-road pin must seed its own open). Six is the floor of honest; eleven if the third field is signed and close 5 converts."},{"wave":"HB-4 — the chooser read, wave one: outside the war lane","verdict":"TOO LOW","correctedLow":4,"correctedHigh":7,"why":"The reader diagnosed this wave correctly and then priced it as if the diagnosis were free. Its own finding is right and I reproduce it: HBF-03 (demographicsResponses.selectResponse, the site the charter calls 'deliberately FIRST because it is the one site whose open→close join is already complete in the tree') carries a WRITTEN closeOwed — 'grade a chosen response against the overflow band it was meant to relieve, over the horizon the band takes to move' — so the close is OWED, not complete, and the charter contradicts the landed instrument. chooserTotality.walker.test.js enforces a ≥20-character closeOwed on every DEFER row, pins the DEFER list at exactly 36 in both directions, AND pins the LEARN set empty. So a promotion is a four-part edit per site: mint the close, lower the ceiling, rewrite the LEARN assertion, and satisfy the ACTION-COVERAGE LAW's walker (§3a: 'the union of every joined site's action vocabulary equals covered ∪ needsClose, exact set equality both directions, needsClose SHRINK-ONLY'). Three sites, three closes. On top of that the wave homes TWO pieces that do not exist: circumstanceClassOf, total over the 12-member CIRCUMSTANCE_PRECEDENCE with the sorted-vs-precedence permutation pin, and habitFactorFor, because habitCurve.js (88 effective) exports only the pure habitFactor. Plus the three pledge OPENs, the per-site same-seed lit/dark differentials, the stream-identity pins, the byte-identity fences, and HB-2's deferred median/p95 corpus measurement which can only land here. And site 3's registry row still does not exist unless HB-1's car minted it. With the three closes inside: 5-7. Slipped behind HB-9 with the closes recorded: 4. Two is not reachable on either road."},{"wave":"HB-5 — the chooser read, wave two: the war-chooser joint","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"The reader's headroom correction is right in direction and wrong in figure: the size-baseline entry IS deleted ('settlementStrategy.js 812 -> 760, ENTRY DELETED', executed by lane CEIL not by HB-1, with the pure-leaf method and settlementStrategyReads.js named), but the file measures 760 effective today, not 775 — so the room is 40 lines against the 800 domain ceiling, not 25. That correction favours the reader and I record it so a brief is not built on the wrong number; the extraction car still stands, because the baseline note names five instruments addressing the file by name and a naive addition moves a pinned symbol. What the reader OMITS is the wave's declared identity: §4 and §8.1 row 5 make this a JOINT landing with CR-ES-4's signed spy-before-decision wiring — 'both retrofit the SAME frozen surface; both add a gated read to the same candidate-scoring block' — and §8.1 row 5 adds HB-7's anticipation multiplicand at the same block under the same grant, 'three reads, one file, one negotiation'. The reader prices the habit read and books the spy wiring as free. It is a whole deferred ES arm with its own proofs. Add the extraction car, the post-softmax load with the positional scan and its PLANTED pre-softmax mutant, the pairwise odds-ratio pin at STRATEGY_K = 3.5 with the hand-walked [0.4815, 2.0769] fixture and the explicit STOP on the deleted per-probability spelling, the return_home hard-override anchored negative, the softmax stream-identity pin, warPeaceDecision (370 eff) with the gated-join anchored negative falsifying all four bilateralOffer disjuncts in their own arms plus the unconditional-stamp mutant, the npcLadderGoals slice, the WR-1 source scan asserting zero habit imports in warTermination.js (818 eff, still baselined, tolerance-zero), the beliefsActive orthogonality pin, the conservative-reinforcement measurement recorded in the header, and a four-fence re-run against the war lane's goldens. Five is the floor; eight on the estate's most contended module."},{"wave":"HB-6 — the believed-doctrine axis","verdict":"CONFIRMED","correctedLow":2,"correctedHigh":4,"why":"The reader's dependency read is CONFIRMED by measurement: SP-B is landed (beliefAxes.js imports subjectGroundTruth and foldSubjectAxes from beliefAxisSubjects.js; SUBJECT_AXIS_FIELDS at beliefAxisSubjects.js:130 is exactly three — conditionsBands, devotionBand, scarcityBands — so doctrineBands is genuinely the fourth member of a worked spine with three precedents), and doctrineSheetOf / DOCTRINE_ADOPT / doctrineBands all return ZERO hits, so the arm is wholly new but wholly conventional. beliefAxisSubjects.js measures 179 effective against a 90-line growth budget, which lands it at ~269 — comfortable. Independent of HB-3/4/5, so the reader is right that this is the family's best parallel lane, and right that the two band values are tuning under §8.2 rather than owner-gated. I widen the top by one for a risk the reader does not name: src/domain/worldPulse/beliefMap.js measures 781 EFFECTIVE against the 800 domain ceiling (eslint.config.js:712) with NO size-baseline entry to absorb an overflow — the baseline holds only eight files and beliefMap is not among them. This wave budgets 6 lines into it and HB-7's seam 2 is the beliefMap injected closure, so the two waves are stacking into 19 lines. HB-6's own 6 fit; the pair may not, and whichever wave crosses pays an extraction car. Otherwise the reader's 2-car shape — (1) the five joins, flag, cert row and derivation-executing walker; (2) the adopt-bar reachability measurement on generated worlds plus the per-observer WeakMap — is the right decomposition."},{"wave":"HB-7 — the anticipation term + the deviation damper","verdict":"CONFIRMED","correctedLow":2,"correctedHigh":4,"why":"The reader's read of the charter is accurate and its dependency chain is right: strictly after HB-5 (the dyadic product at the war chooser is the multiplicand's host) and HB-6 (the believed-doctrine axis is what the term reads), with seam 4 struck (convergence.reactiveResponse, forkless and uncalled) and seams 1/3 pre-pinned, 5/6 declared-deferred. Its Q4-corollary point is confirmed in the volume text: the leaf is named INTO the belief* spelling so LAYER_PATTERNS.INFO's existing /belief[A-Z]/ pattern claims it and no eighth family is owed. The counterWeight-tables-for-dyadic-vocabularies-only ruling is real and is what keeps the authored-cell census dischargeable. Bands genuinely NONE BY DESIGN. Two cars is the right shape — (1) the 260-line leaf, tables, flag and cert row; (2) the two seams plus the pin set including the ZERO-IMPORT scan with its guard-the-guard positive control, the counterWeight totality where a missing cell reds rather than defaulting, the damper's both-sides-live mutant, the infoStatecraftEnabled-dark degradation pin and the closure null ⇒ byte-identical anchor on the sightOf precedent. I widen the top to 4 for the same unnamed beliefMap ceiling: at 781 effective against 800, with HB-6 having already spent up to 6, this wave's injected-closure seam is the likely crosser and would pay a behaviour-identical extraction car of its own, exactly as TE-CEIL did in settlementStrategy."},{"wave":"HB-8 — the doctrine sheet: dossier + tap + plant","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"The reader's own erratum is CONFIRMED and important — errandSpineEnabled is registered at simulationRules.js:271, read by name at errandMint.js:76, imported by pactProposals.js and envoyErrand.js, so the charter's '(b) and (c) SHIP DARK-FOREVER UNTIL SP-D LANDS' is false and must not be copied into the cert row. But the reader then mis-measures the tap arm it just corrected. HB-8 mints 'doctrine as the SEVENTH ES legRef'; the reader restates that and adds 'the ES tap-product ladder has six worked members to add the seventh to'. MEASURED: ENVOY_COVERT_LEG_REFS at src/domain/worldPulse/envoyErrandVocabulary.js:207 has FIVE members — readiness, routePositionBand, storesBand, strength, tierBand. COVERT_LEG_REF_SET (line 436) is derived from it and enforced by a LIVE VALIDATOR at envoyErrandRecords.js:469 over a persistence DTO whose itinerary cap the same file mints. Doctrine is the SIXTH, the amendment is a validated-vocabulary change with a persistence consequence, and 'six worked members' is an invented denominator. That is a fourth stale-record place the reader's three-place census missed. On price, the instruction's own default applies squarely and I apply it: this wave adds a display seam (settlementBeliefs.js, 108 eff, plus BeliefDivergenceBand.jsx, 93 eff with the premium/elevated gate at :96 — both CONFIRMED present), a NEW WALKER FILE, a validated vocabulary with a persistence validator, a plant road on the disinfo lineage, and prose. Arm (d) alone is a car-plus: doctrine_shifted returns ZERO hits in src and tests, and §3g requires FIVE joins in the mint commit (annex-verbatim pool row · registry row with requiredSlots AND a slotless fallback, with the explicit 'PAIR-ONLY ≠ SLOTLESS' trap recorded · WHAT_PHRASES · EXACT_SECTION/sectionAuthority · the full address chain) plus its OWN walker file, never rows in a foreign walker, plus a proseNumerics re-record against an exact-equality-both-directions ceiling of 413. Four arms at one car each was the reader's own framing yet its band starts at 3, which is below its own decomposition. Five is the floor; eight with the ES amendment's validator work and the Herald walker."},{"wave":"HB-9 — the closes owed + the measure","verdict":"TOO LOW","correctedLow":8,"correctedHigh":14,"why":"The reader identifies the right pressures and then prices a car per close family, which is the error: the closes are SEVEN, not four. §4 charters the occupation close, the route close, the route-creation charter close and the trade-routing close, and then §4 and §3a add 'THE THREE CLOSES THE ACTION-COVERAGE LAW PUT ON THE BOOKS AT THE POLISH (R18)' — the generosity verdict (site 2), the intervention engage/decline (site 10) and the sovereignty offer-posture (site 12) — each with its closeOwed already written and each needing a grade map, an episode-key road, a load and its pins. All the vocabularies the closes grade against exist and are reusable, which I verified (STATE_LADDER 6 files, CHARTER_VERDICTS 1, banditryLoss 4, routeNetworkDecay 4, OBJECTIVE_BAR 3, SEVERITY_LADDER 3, INTERVAL_WEEKS 18) — that is genuine savings and the reader is right to bank it. What it does not price: the route close applies as a DIVISOR on effectiveCost across four callers so the argmin survives; the charter close applies as a bounded multiplicand on score BEFORE the OBJECTIVE_BAR comparison; hold's ABSTENTION close is a close-shape the estate has never minted and the volume names it 'the programme's largest honest gap'; the SIX HB envelopes are each INTERVAL_WEEKS-derived with an EXECUTED mutant; Q3's same-input cross-run stock-identity check rides here; and the four flags' dispositive cert literals land here. Then the ratchet: every promotion lowers DEFER_CEILING in the same commit while the LEARN-empty pin must already have been converted, and the walker's own comment records the ceiling moving 29→30→31→35→36 as other families mint INTO the list (HBF-33 IN-1c, HBF-34 WF-8a, HBF-36..42 ENC), so this wave is racing a rising floor on a shared file. And the reader's own tripwire is real and understated: §7 rules that if the occupation close converts to a carried road it mints occupationRecord.habitEpisode and 'Q1 RE-OPENS AT THAT WAVE' — an owner gate inside the last wave, which on my close-6 finding may already be the FOURTH such field rather than the third. Seven closes at roughly a car each, plus envelopes, plus cert literals, plus the abstention shape, plus the occupation carry-chain re-measurement and its re-gate: 8 is the floor, 14 if the prose-pick family (HBF-07/33/34/42, four rows that close together or not at all) is taken here."}]}}
{"type":"result","key":"v2:825a41a7f57b004976d0eae9d1e6c341c1d73f750eb795fa61d680ca3b9ae661","agentId":"a6e66282d8234934c","result":{"family":"INT","verdicts":[{"wave":"INT-1 — the books generalized (flag `seatBooksEnabled`)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"UNBUILT status CONFIRMED (`seatBooksEnabled` → 1 hit, the strategicPosture.js:36 comment saying the books are unbuilt; seatBooksReceipt.js absent). Substrate CONFIRMED: warSeatBooks.js 811 raw / 492 eff, SEAT_BOOK_KINDS at :540 is four kinds including 'foreign', PRIVATE_BOOK_KINDS :551, seatBooksPartition :588, and the receipt vocabulary is live (warTermination.js:917-918, settlementStrategy.js:521, warRulingsNews.js:225/:291, warRulingsEvidence.js:39-40). Nine real importers CONFIRMED by `grep -rn \"from '.*warSeatBooks.js'\" src` — but 27 src files MENTION it, including five certification lanes and couplingRegistryWar.js, so the census surface the wave must sweep is three times the import count. The floor of 3 is wrong for two reasons. (a) The reader files couplingRegistryInterior.js's mint as contingency car 5; it is not contingent — INT-1's entire subject is NON-WAR consumers reading a war-plane module, i.e. a cross-layer read, and §5 row 12 / §54.3 say the first cross-layer-reading INT wave pays the five-part CW-0 act IN THE SAME COMMIT. That mint is certain, not optional. (b) The dossier line is marked VERIFY-AT-BUILD UI work with its own round-trip pin, which cannot ride the census car. Cars: flag bill + booksStandingOf exports; seatBooksReceipt.js + change-gate/deadband; one-evaluator census + 9-importer consumer census + the four reserved-seam tripwires (§5 rows 6-9) + the CW-0 mint; the dossier car. Six if the receipt cannot mount on the existing booksInterest/booksDirection lane without touching warTermination (baselined 818, frozen). ⛔ REFUTED: the reader's claim that this is 'the wave most likely to meet the engine-chunk ceiling first'. See the summary — no INT code reaches the `engine` chunk. Worker bundle: ZERO into generation.worker (measured 1,323,512 B against WORKER_BUNDLE_CEILING_BYTES 1,404,493 — 80,981 B spare, and no INT vocabulary in it)."},{"wave":"INT-2 — the positions wired (flag `settlementPoliticsEnabled`, exists dark)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":5,"why":"Status and the no-flag-bill finding CONFIRMED: `settlementPoliticsEnabled` is a manifest member at simulationRules.js:349 and carries its authored row at subsystemRowsCompact.js:332 (with the 'STRUCTURALLY INVISIBLE TO EVERY RECEIPT SHAPE' paragraph at :353); blocCounsel.js and blocCounselNews.js absent; `grep -c counsel src/domain/worldPulse/settlementPolitics.js` = 0. Both preset negatives CONFIRMED at simulationRules.js:743 and :833 (the volume's :477/:517 have rotted). settlementPolitics.js 1076 raw / 560 eff, settlementStrategy.js 1295 raw / 760 eff with its size-baseline entry indeed deleted (the live baseline holds only 8 files: App.jsx, explanation.js, applyWorldPulse.js 907, pulseKernel.js 1581, roadsKernel.js 838, warTermination.js 818, npcGenerator.js 1345, settlementSlice.js 824). What pushes the floor to 4 is a MINT-SITE INVERSION the reader names and then dismisses: INT-2's override handoff writes an INT-3 `*_decision` sibling type through mintFactionPairIncident, but §4's build order puts INT-2 BEFORE INT-3, so INT-2 cannot consume a type that does not exist. It must either mint that type itself (paying part of INT-3's typed-incident bill plus the totality walker) or ship the handoff dormant with a tripwire and a recorded deferral — either is a car's worth of work the reader prices at zero. Add the SP-6 named-constant-plus-TODO-tripwire, and the news car is not cheap: an L6 kind gets its OWN walker file and the landed walker exemplars run 1,188 (chanceMeetingKindPools) to 1,618 (couplingInclusion) lines. Cars: blocCounsel.js + four-fence completion + both preset negatives; blocCounselNews.js + the five joins + its walker; the pin battery; the faction-card FOREIGN-COUNSEL dossier round-trip (VERIFY-AT-BUILD). Worker bundle: ZERO into generation.worker; settlementPolitics.js builds into advanceInterval.worker / grammarNews, not the engine chunk."},{"wave":"INT-3 — the interior veto completes (flag `interiorVetoEnabled`; 3b landed, 3a unbuilt)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":7,"why":"INT-3b LANDED is CONFIRMED and the reader's correction of the first pass is right for a better reason than the one given: envoyErrandVocabulary.js:360 carries `{consumer:'ambitious', module:'src/domain/worldPulse/emigreErrand.js', wave:'INT-3b', built: true}` — the `built:false` the first pass quoted is emigreErrand.js:7's own docblock quoting the OLD row. emigreErrand.js is 121 raw with a 212-line acceptance suite. INT-3a wholly unbuilt CONFIRMED (`interiorVetoEnabled` 0 hits; decisionJoinReceipt.js and decisionPolitics.js absent). Substrate CONFIRMED: stressorsCore.js:236-239 coup_detat `pressureKinds:['legitimacy']`; factionPairLedger.js 240/138 eff; warPoliticalLoop.js 233/169 eff. ⛔ THE READER'S EAGER-COST ANALYSIS IS HALF REFUTED. Running vite.config.js's OWN derivation (`EAGER_FIRST_PAINT_MODULES`, 263 modules) at the slot: stressorsCore.js EAGER — confirmed, so the `decision` pressureKind sibling IS a first-paint edit — but rulingPowerCoup.js is LAZY and so is coup.js, so the join-margin receipt is NOT eager. The reader's traced path breaks at its third hop: stressorsCore.js imports only ./stablePart.js and ./stressorSeverity.js — there is no stressorsCore → stressors.js → stressorDynamics.js → rulingPowerCoup.js edge. And the eager margin is 795 B, not 1,222: CLOSURE_BUDGET_BYTES = 1_048_000 (vendorPdfLazy.test.js:565, re-pinned :1706) against the last MEASURED closure of 1,047,205 (:539, the T13 build #4 — the file's own words are '795 B under this one'); the reader's 1,046,778 is vite.config.js:208's figure measured against the superseded 1,047,000 ceiling. stressorsCore.js is 559 raw / 392 eff against the 800 layer ceiling, so lines are free; the real discipline is that no new composer leaf may be statically imported by stressorsCore.js or it goes eager with it. Price: the floor of 4 cannot hold the flag bill AND five sibling incident types (each gated on its own producing event per J-INT-12) AND the join receipt with its seeded JOIN NEGATIVE AND the pressureKind AND the dormant PACT re-read AND the real-corpora pin battery. Six is the honest expectation; seven if the sibling types need their own totality walker."},{"wave":"INT-4 — the narrated middle (flag `strainAttributionEnabled`)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":5,"why":"UNBUILT CONFIRMED (`strainAttributionEnabled` 0 hits; strainAttribution.js and strainAttributionNews.js absent). Premise CONFIRMED unchanged at the slot: stressorsCore.js:236-239 still makes coup_detat one-strain, so attribution-not-amplification holds. commonsVoiceKernel.js 366 raw / 214 eff. ⛔ THE READER'S EAGER SLICE FOR THIS WAVE DOES NOT EXIST: rulingPowerCoup.js is LAZY by vite.config.js's own EAGER_FIRST_PAINT_MODULES derivation, so the rally-margin read costs zero first-paint bytes and the ≤+30-line budget is a line-ceiling matter only. That removes a constraint without changing the car count. The count is wrong at the floor for a reason the volume states in terms: the town-page strain line is 'NEW UI WORK owned here', which the reader itself makes car 4 — so its own decomposition gives a floor of 4, not 3. Cars: flag bill + strainAttribution.js; strainAttributionNews.js + five joins + own walker + the commonsVoiceKernel treaty-burden term (live-lit, fence golden FIRST); the seven-pin battery including the property-shaped double-count guard with both flags lit and the no-decimal RUNTIME pin a source scan cannot see; the town-page line. Five if the TreatyPanel.jsx secondary landing is taken as well. Worker bundle: ZERO into generation.worker."},{"wave":"INT-5 — the memory seam + the founding wound (flag `memoryHorizonSeamEnabled`)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":4,"why":"Every substrate claim CONFIRMED and the reader's line corrections are right: relationshipMemory.js 605 raw / 477 eff, RELATIONSHIP_MEMORY_HALF_LIFE_TICKS :14, MAX_LOOKBACK :15, both already threaded as options at :118-119, hard-zero at :129 before the half-life at :130; `originHolder` = 0 hits in src AND tests; npcLadderState.js `inherited: true` at :566 and :583 exactly as the reader re-derived (956 raw / 605 eff); foundingWound.js absent. This is genuinely the cheapest engine wave and the reader is right about the reuse ratio — but 2 is not a floor. The flag bill is a car in this estate on its own: the manifest key (ENGINE_GATED_VIRTUAL_RULE_KEYS, 35 members today — CONFIRMED by tests/domain/contributionLedgerShape.test.js:119 `toHaveLength(35)`, whose own it() title at :49 still reads 'at 28', so a minting wave edits both), the authored row plus its VIRTUAL_RULES twin plus the LANE_LEAVES entry (three module-scope literals, bijection asserted at subsystemRowsVirtual.test.js:646/:651), the seven edge-shared MODIFY rows plus `npm run build:edge-shared` (package.json:27), the acceptance-file drive, and the four-fence set with its lit-mutant control. Add a persisted-field addition on a LIT-PATH module that demands the dark byte-identity golden BEFORE the first edit, and a proof battery whose forty-year pin must be EXECUTED on a real generated corpus (a run, not lines) alongside JSON round-trip + regen/undo/import + chain-immutability across a second succession + the memoryWeave composition pin. Three cars minimum (flag bill + seam/stamp · foundingWound.js · the executed battery), four if the corpus work spills. Worker bundle: ZERO into generation.worker (relationshipMemory and npcLadderState build into their own lazy chunks, `relationshipMemory-*.js` and `npcLadderState-*.js`, with no byte ceiling on either)."},{"wave":"INT-6 — deliberate forgiveness (flag `deliberateForgivenessEnabled`)","verdict":"TOO LOW","correctedLow":6,"correctedHigh":8,"why":"UNBUILT CONFIRMED (`deliberateForgivenessEnabled` 0 hits; burialLedger.js absent). The reader's re-derivation warning is CONFIRMED and load-bearing: grievanceRead.js:48 is /war|betray|tribute|conquest|occupation|sack|raid|seiz|impos|contest_loss|contest_forestalled/i — ELEVEN alternatives, so 'seven families partitioning the regex exactly' is a stale count. grievanceRead.js is 131 raw / 51 eff (huge line headroom). The WR-6 naming collision is CONFIRMED at relationshipState.js:270 ('forgiveness' in the coalition-settlement action list) and :273 (status 'forgiven' in COALITION_SETTLEMENT_STATUSES) — the burial/buried/dug_up spelling discipline is real. The owner gate is correctly identified and is the family's only one. Two things push past 7. First, the reader's own decomposition already names seven distinct bodies of work (classifier · ledger · the two grievanceRead arms · the DM verb arm · the autonomous plan-lane arm · lifecycle battery · pin battery) and then prices them at 5-7; the flag bill is an eighth and is not free. Second, the drop-when-empty discipline is much wider than the ten components the reader lists: 171 src files mention spatialLedgers at the slot, so the absence-is-evidence census and the byte-identity argument sweep a far bigger surface than 'AdvanceReport, TravelersLayer, PlacementsLayer…' implies. Dependencies CONFIRMED as stated (INT-5 for the corrected dig-up clock, INT-1 for the books-priced autonomous arm, INT-3 for the `burial_decision` sibling, R7's weave-gated read path). Worker bundle: ZERO into generation.worker; grievanceRead's vocabulary builds into advanceInterval.worker (2,660,242 B, NO byte ceiling), applyWorldPulse-*.js, npcLadderState-*.js and pdfRender.worker — not the engine chunk."},{"wave":"INT-7 — legitimacy's row + the interior envelopes (flag `legitimacyCrossingsEnabled`)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"UNBUILT CONFIRMED (`legitimacyCrossingsEnabled` 0 hits; legitimacyCrossings.js absent). S21's gap re-verified: `strategy_legitimacy` at subsystemRowsBaseline.js:94 is the only hit in src, and the census trap is real — subsystemRowsGrowth.js:59 carries the literal 'legitimacy_pressure' as an EMERGENT_EVENT_TYPES member. subsystemRowsInterior.js and couplingRegistryInterior.js are both correctly ABSENT; the coupling registry is a head over eight per-volume leaves (war/trade/grammar/info/espionage/encounters/schema). ONE MEASURED CORRECTION: tests/fixtures/distribution-envelopes.manifest.json holds 25 entries, not 26. The floor of 3 contradicts the reader's own sentence 'cars 3-5 are the envelopes' — three cars would require the whole envelope arm to be one car. Cars: flag bill + legitimacyCrossings.js ≤200; the certification row in the virtual lane with the crossing-has-a-hit invariant, joining the registry and the partition walker in the same commit; then the envelopes — a TOTAL over all 20 declared endings tokens by family plus a two-part tempo envelope, each registering a full-schema manifest entry with baseMeasurementN ≥ 400 measured on generated corpora, each joining the frozen shrink-only roster in distributionEnvelopePower.test.js, each with an executed mutant negative control. That is priced in runs. rulingPower.js IS eager (CONFIRMED by vite.config's derivation, reached main.jsx → store/index.js → settlementSlice.js → events/mutate.js → events/mutateWorld.js → rulingPower.js — not the store→stressors chain the reader cites), so the reader's placement rule is right even though its path is wrong: the detector must be its own leaf and must not be statically imported by rulingPower.js or rulingPowerSeat.js. Worker bundle: ZERO into generation.worker; the envelope registrations are test-side and cost zero runtime bytes."},{"wave":"INT-8 — the interior voice (no flag of its own)","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"⭐ THE READER'S PART-BUILT CORRECTION IS CONFIRMED THREE WAYS AND THE FIRST PASS'S 'UNBUILT' ROW IS WRONG. `grep -rn 'Hold chance' src` returns nothing; deploymentReturn.js:192 exports seatGripWordFor and :592 reads it in prose ('a grip that had looked ${seatGripWordFor(verdict.pHold)} did not survive its arrival'); `grep -c toFixed src/domain/worldPulse/relationshipMemory.js` = 0; and tests/helpers/proseNumericsWalk.js:230-236 now carries collectArrayPushes with the header 'Index `.push(...)` writes by the exact const-array binding they target. This closes the common `const parts=[]; parts.push(sentence); reason: parts` hop'. J-INT-9's whole commit — both cures and the habitat — has been paid by other waves. warReceiptPools.js exists as the pool shape to copy. BUT THE REMAINING PRICE IS UNDERSTATED. The reader stretches to 4 on the grounds that six kinds each complete the L6 five joins with their OWN walker file — and that is exactly why 2 is impossible and 4 is the floor, not the ceiling. DESIGN_FP_ARCHITECTURE.md:150 spells the five joins out (annex-verbatim pool row · registry row with requiredSlots + slotless fallback · WHAT_PHRASES at settlementRumors.js:116 · EXACT_SECTION/sectionAuthority in heraldRouting.js · the full address chain) and states in terms that 'Kinds get their OWN walker file … never rows in a foreign walker'. Landed walker files in this estate run 1,188-1,618 lines. Six kinds × five joins + six walker files is three to four cars by itself, on top of the pool-authoring car and the determinism/identity car (per-entity keys `int8.<kind>.<entityId>`, draw accounting, id-carry, the dossier/Herald round-trip). The reader's note that the two ratchet rows may already be gone is right to check at charter rather than budget. Owner gate as stated: new-kind pools chair-signed; the lit-kind J-INT-13 question already answered by CR-FP-5 (DESIGN_FP_ARCHITECTURE.md:2933). Worker bundle: ZERO into generation.worker."}],"summary":"VERDICT ON THE READ: the wave-by-wave research is unusually good — every existence claim, every rotted line number, the INT-3b landing and the INT-8 cures all survive re-measurement — but the reader's single headline finding is FALSE, and every wave's floor is priced one car light. Family price corrected from 25–38 to 34–47 cars.\n\n⛔ FINDING (1) IS REFUTED: THE LAZY `engine` CHUNK IS NOT THE BINDING CONSTRAINT, BECAUSE NO INT CODE IS IN IT. Measured at the slot's dist (note: that dist is being rebuilt by a concurrent session — hashes and sizes moved 17:34 → 17:43 mid-read, so treat any single byte figure as a snapshot). Eight distinctive INT strings — contest_forestalled, coalitionConsolidation01, unseated, legitimacyFragility01, RELATIONSHIP_MEMORY, HEIR_INHERIT_FRACTION, war_decision, previousGovernments — all return ZERO occurrences in `engine-oZgV8RIu.js` (675,489 B). They live in `advanceInterval.worker-C23ez9o-.js` (2,660,242 B), `applyWorldPulse-*.js`, `npcLadderState-*.js`, `relationshipMemory-*.js`, `grammarNews-*.js` and `pdfRender.worker-*.js`. And the ONLY numeric byte ceilings in tests/build are: the engine chunk (`< 676_000` at vendorPdfLazy.test.js:768, plus engineChunkLazy's 200,000–1,400,000 sanity band), vendorPdf `< 3_000_000`, the generation worker `<= 1,404,493` (measured 1,323,512 — 80,981 B spare and zero INT vocabulary in it), and the first-paint closure. THERE IS NO BYTE CEILING ON advanceInterval.worker.js, which is where most INT code will land. Consequence: the reader's assertion that 'INT cannot land past wave one or two without an engine-chunk ceiling act' and that the raise is 'a family-level owner act' is a PHANTOM DEPENDENCY and a phantom owner gate. It should be struck from the sheet.\n\n⛔ THE EAGER ANALYSIS IS HALF WRONG, AND THE MARGIN IS TIGHTER THAN QUOTED. Running vite.config.js's own `computeEagerModuleGraph()` (263 modules, matching the file's '264' note): stressorsCore.js EAGER and rulingPower.js EAGER — but rulingPowerCoup.js and coup.js are LAZY. The reader's traced path dies at hop three: stressorsCore.js imports only ./stablePart.js and ./stressorSeverity.js. So INT-4 has no eager slice at all, and INT-3's eager exposure is the stressorsCore pressureKind row alone. The margin is 795 B, not 1,222: CLOSURE_BUDGET_BYTES = 1_048_000 (vendorPdfLazy.test.js:565, re-pinned :1706) against a last-measured 1,047,205 (:539) — the file says '795 B under this one'. The reader's 1,046,778 comes from vite.config.js:208 and was measured against the superseded 1,047,000 ceiling.\n\nWHAT SURVIVES, UNCHANGED: findings (2) and (3). INT-8's two cures and the walker habitat are all paid (receipts in its row). warSeatBooks' four-kind generalization, the eleven-alternative WOUND_TYPE_RE, and the contributionLedgerShape.test.js:119 `toHaveLength(35)` versus its :49 title 'at 28' are all CONFIRMED. So are the nine real warSeatBooks importers (though 27 src files mention it, so the census sweep is wider), both factionCompetitionEnabled:false presets (simulationRules.js:743 and :833), settlementPoliticsEnabled's manifest membership (:349) and its authored row (subsystemRowsCompact.js:332), the absence of subsystemRowsInterior.js and couplingRegistryInterior.js, and every effective-line count the reader quoted (warSeatBooks 492, settlementPolitics 560, relationshipMemory 477, npcLadderState 605, grievanceRead 51, settlementStrategy 760 of 800 with its baseline row deleted). Two small corrections: the envelope manifest holds 25 entries, not 26; and applyWorldPulse's frozen baseline is 907, not the volume's 941.\n\nWHY EVERY FLOOR MOVES UP BY ONE. Three costs the reader systematically under-prices. (a) The flag bill is a car on its own in this estate — manifest key + authored row + VIRTUAL_RULES twin + LANE_LEAVES entry + the two subsystemRowsVirtual.test.js literals + the contributionLedgerShape pair + seven edge-shared MODIFY rows + `npm run build:edge-shared` + the acceptance drive + four fences with a lit mutant — and the reader repeatedly folds it into a car that also ships a ≤250-line leaf. (b) An L6 news kind gets its OWN walker file (DESIGN_FP_ARCHITECTURE.md:150, in terms), and landed walkers here run 1,188–1,618 lines; INT-8 owes six of them. (c) The first cross-layer-reading INT wave pays couplingRegistryInterior.js's five-part mint in the same commit (§5 row 12, §54.3's X5), which is INT-1 by construction, not a contingency. Against the estate's own calibration — the first pass bands #32's ~88 unbuilt wave-units at 100–200 cars — the corrected 34–47 for eight INT waves is on the heavy end, which is right: INT is bill-dense, not code-dense.\n\nUNCHANGED AND CORRECT: all seven unlanded waves land DARK behind their flags and move no same-seed output; the family becomes output-moving only at the second declared lighting. The only real owner gate is INT-6's one new persisted key (worldState.spatialLedgers.burials), a persistence-shape act; nothing in the family is a push, deploy, migration, deletion or spend. Nothing was built, staged, or run — read-only throughout; the only writes were three analysis scripts under scratchpad/recon-32-34/int-skeptic/."}}
{"type":"result","key":"v2:335ae9f6dfdb4da3010b3daabf0826bbb0aa40a670ca1cd13507c6190081bd6b","agentId":"a375d86717e3361fe","result":{"family":"GR-CW","verdicts":[{"wave":"GR-4 — the succession question (remainder)","verdict":"TOO LOW","correctedLow":3,"correctedHigh":5,"why":"The eight-slice landing and the three owed items are CONFIRMED verbatim (grammarReceiptPools.js:117-120 defers `honored_by_silence` and `credibility_charge`; treatyBreach.js:243-248 refuses the succession-road disposition write in words; treatyBreach.js:238 charges credibility under `oathHolderActive`). What the reader priced as 'the car with real work in it' is not a car — it is a MOUNT PROBLEM of the same family ODQ §35 parked GR-6 for. The expiry road's only transition site is `expireStaleActorMajors` (actorMajorApproval.js:135), whose sole caller is `pulseKernel.js:347` as a bare `worldState = …` assignment with no news channel. pulseKernel.js measures 1581 effective against a `.size-baseline.json` entry of 1581 — ZERO headroom — under R-BLD-10, which bans raising it; so the beat cannot be composed at its natural site by adding a line, and the timestamp-derived alternative is refused in writing by the estate's own exactly-once law (treatySuccessionReaffirmedVoice.js header: 'no stamp is ever read back to decide whether to speak — which is exactly why … the EXPIRY road, having no transition site at all, is not part of this wave'). So (a) needs a chair mount ruling before an implementer sits, then a car for the site and a car for the pool, (b) is its own annex + EXPECTED-table + corpus-gate edit, and (c) is a chair ruling plus a small car or a recorded permanent deferral. One credit back to the reader: the kinds do NOT each need their own walker file — `tests/lint/grammarLifecycleKindPools.walker.test.js` is one shared census walker re-derived from the annex, so L6's 'own walker file per kind' is already consolidated in practice. 3 if (c) stays deferred and the mount ruling frees a one-commit site; 5 if the site needs its own slice and (c) is built."},{"wave":"GR-5 — renewal, renegotiation, conversion (remainder)","verdict":"TOO LOW","correctedLow":9,"correctedHigh":13,"why":"Every substrate figure the reader quoted re-measures true: peaceTerms.js 797 effective of a hard 800 (3 lines), pactAmendment.js 140, `PACT_PROVENANCE` at :77 already freezing `converted`/`renewed`, and the certification row at subsystemRowsCompact.js:449-469 whose `other:` field is verbatim the itemised NOT-BUILT list the reader quoted, including 'every successor needs a chair-signed band under the value-derivation law before it can be written at all'. The decomposition into behaviours is faithful. The PRICE is not, and the estate's own cadence is the refutation: GR-5A — a ONE-LINE monotone fold plus its gate, manifest entry, certification row and fences — took THREE implementer commits at the slot (`318a7fff7` I1, `85d19adf1` I1', `e93376ec1` I1'') before its train closed at `fc8451c44`. The remainder is eight behaviours, not one line: the renewal window and the fifth closed trigger; the two-picture re-draft; the `renewed` act; the mid-term demand; the `renegotiated` act; the conversion arm, which reaches into the LIT severity-gated label-proposal lane at relationshipRulesCore.js:776; three new Herald kinds each taking the five joins (grammarNews registry row, annex pool, settlementRumors WHAT_PHRASES, heraldRouting authority, chroniclersLetter) through one governed corpus-gate run; and the dossier round-trip across WarFaithTab, TreatyPanel and the PDF treaty chapter, plus the four-fence set, the lit-mutant, the K3 extension and the cert-row amendment. Car 0 (the five chair-signed bands) is a compile pass, not an implementer car, so it should not be counted in the range at all. At one car per behaviour with the flag-bearing repair overhead GR-5A actually paid, the floor is 9, not 6."},{"wave":"GR-6 — mediation generalized","verdict":"TOO LOW","correctedLow":7,"correctedHigh":11,"why":"The parked-for-re-charter finding is CONFIRMED at the source: ODQ §35.1 records that 'the war-opener's threshold operand is NEVER EVALUATED for the ordered target' and §35.2 that 'GR-6 is PARKED FOR RE-CHARTER … a chair mount ruling plus a chair-authored band against the measured substrate. One future compile pass carries both; nothing lands until then.' The named re-mount is live today with line rot only: `if (!coalitionDecision && fromStrength < HOSTILE_CONFIDENCE) continue;` at warDeployment.js:1031 (§35 said :1002), `HOSTILE_CONFIDENCE = 0.42` at :172, `CONQUEST_MARGIN = 0.12` at :173, and the refuted short-circuit at :1094 (§35 said :1065). `mediationGeneralizedEnabled`, `mediationPressure` and any GR-6 certification row are all grep-ZERO at the slot, and warDeployment.js measures 684 effective of 800, so the mount has room. The error is arithmetic, not analysis: the reader's own slice list is eight, one of which (car 0, the re-charter compile) is a CHAIR act rather than an implementer sitting, leaving seven implementer slices — and then prices the whole thing at a floor of five. CW-0w, a pure-instrument wave with no flag, no bands and no engine mount, cost exactly four cars at one per slice (`b3fb8f494`, `f7da6b607`, `03dee5fd5`, `e30770bd0`). A flag-bearing wave whose mount sits INSIDE the live war opener, carrying a closed four-member ending vocabulary through the governed annex, a reach band against V-27's calibrated hopWeeks hazard, the four-fence set with a lit-mutant, a single-finder source-scan walker and a three-town dossier pin cannot floor below seven."},{"wave":"GR-7 — grammar measurement + certification","verdict":"TOO LOW","correctedLow":4,"correctedHigh":7,"why":"The collector-extension reading is right — `scripts/audit/war-convergence-collector.mjs` is 547 lines and already treaty-aware (`treatyPairKey` imported :74, `treatyPairKeys` observed :297, `treatyWritten` :463) — and GR-7 is correctly last. Two of the reader's cheapness claims fail to measurement, and one whole obligation is missing. FIRST, 'GR-7 converts four pendings' is FALSE: `VIRTUAL_PENDING_RULE_KEYS = Object.freeze([])` (subsystemRowsVirtual.js:595) and all four landed GR rows are AUTHORED, not pending (subsystemRowsCompact.js:49 oathHolder, :98 pactFormation, :218 treatyLifecycleVoice, :449 treatyRenewal — the last one saying 'AUTHORED, NEVER PENDING' in its own comment). There is nothing to convert; the work is instead amending the GR-5A row and appending GR-6's row, and that append is NOT free — the file's header states '⛔ DO NOT APPEND A NEW ROW HERE — an append lands at index 20 and shifts eight later rows. New virtual rows append at the TAIL', under an exact ordered-equality pin. SECOND, a whole car is missing: `hollowed_quiet` is deferred BY NAME to this wave — grammarReceiptPools.js:24-28 says 'The PROSE lands with its ground-truth surface, which is GR-7's endings-mix instrument. Deferred BY NAME', and grammarLifecycleKindPools.walker.test.js's header pins that absence and names its owner, so GR-7 must land the DM-only ending's prose, flip the walker's absence assertion, and run the governed corpus gate. THIRD, the 'closed nine-member endings vocabulary' has no single in-tree home: `PACT_ENDINGS` is exported TWICE under the same name with different contents (pactAmendment.js:71 — broken_by_war, disavowed_by_succession, expired_unanswered, no_overlap, refused, signed; treatyLifecycleVoice.js:73 — ran_its_term, hollowed_detected, hollowed_quiet) and `reaffirmed`, which treatySuccessionReaffirmedVoice.js:118 actually writes as an ending, is in NEITHER. Reconciling that before an envelope can name every producer is chair-grade work this wave inherits."},{"wave":"CW-1 — the cascade governor","verdict":"TOO LOW","correctedLow":4,"correctedHigh":6,"why":"Both of the reader's record corrections survive in substance and one fails on its receipt. SC-3 IS discharged — `SIGNIFICANCE_CLASSES = Object.freeze(['routine','notable','major'])` at bandFamilies.js:75 with `routine` live on seventeen kind-row files — so the volume's CW-1 blocker is dead. The causedBy starvation IS real and unchanged: `grep -rl causedBy src/domain` returns exactly the six files the 2026-08-04 compile measured, and `tests/lint` holds only couplingDesk, couplingInclusion and provenanceStampSingleWriter. But the seam receipt is the WRONG FILE. The volume ordered 'VERIFY-AT-BUILD the exact composition chokepoint (realmItemReadModel vs the Herald page composer — navigate by the feed-assembly symbol, not this file's guess)', and the reader measured the guess instead of navigating the symbol. The Herald feed composer is `buildHeraldFeed` in `src/components/map/heraldFeed.js` — its own header: 'THE HERALD FEED SELECTOR … Turns a campaign's recorded pulse + wizard-news records into ONE normalized, section-filed feed of HeraldItems the six news doors render' — at 228 raw / 115 effective of an 800 components-.js ceiling. The conclusion (no decomposition slice forced) survives on a different file; the 784-of-800 measurement is a true figure about a file that is probably not the seam, and if it IS a seam its 16 effective lines are thin, not comfortable. On price: this is a new ~300-line detection leaf (ancestor grouping by identity, layer counting through the registry join, braid assembly, damping), a guarded seam, the braided view item with deterministic id + full address chain + SC-8 typed entity-refs flipping heraldIndex facets, seven pins whose hardest is a false-causality negative seeded with two live chains first, three executed mutants in the manifest, the four-fence set plus lit-mutant, and five owner-signed bands. One cost the reader did not name: cascadeBraid is a `src/domain/display` module importing `src/domain/certification` (the registry as layer authority) and `src/domain/worldPulse/bandFamilies.js` — genuine cross-layer edges owing SC-1 registry rows, and the inclusion walker CANNOT catch them because `CENSUS_SCOPE_RE = /^src\\/domain\\/(?:worldPulse|spatial)\\//` (couplingInclusion.walker.test.js:1054), so the rows must be paid deliberately or they vanish. Against CW-0w's measured four-cars-for-four-instrument-slices, a floor of two is not credible."},{"wave":"CW-2x — the cause-walk extension","verdict":"CONFIRMED","correctedLow":3,"correctedHigh":5,"why":"The price is right and the four-slice shape is faithful to §4. Verified: no `causeWalkCouplingJoin.js` at the slot; causeWalk.js 130 effective of 306 raw with ample headroom; `NO_DEEPER_MEMORY` at :33 rendered at BOTH :261 and :304 so a retention cut and a true root really are indistinguishable today; `CauseWalkPanel.jsx` 96 effective against a 600 components ceiling; the V-4 estate and CW-0w's six registry leaves (58 rows) all landed; and Q1's ratification is genuinely a chair word not yet given, which is a real blocker on slice 1. THE OUTPUT-MOVING MECHANISM IS MIS-STATED, and it should be corrected before Car 1 budgets for it. `provenanceLedgerEnabled: true` IS in the ONE_REGEN spread (simulationRules.js:669), so the walk is reachable on lit presets — that half is right. But `graceLine`, the field carrying `NO_DEEPER_MEMORY`, is consumed by exactly ONE surface in the whole tree: `CauseWalkPanel.jsx:122-124`. It never reaches heraldCausalVoice's registers, which read `walk.chain` hops and are separately gated by `heraldCausalVoiceEnabled` (heraldCausalVoice.js:115, read at :370 and :420), nor discourseKernel's realization, gated by `discourseProseEnabled`. So slice 2 changes a DM PANEL LINE, not composed Herald prose, and moves no news-stream golden. It is still reader-visible on a lit preset with no flag of its own and must be declared — the classification stands — but the wave should be budgeted against panel pins and a view-model check, not against a Herald-prose golden re-record. That correction makes 3-5 if anything slightly generous rather than tight, so the range holds."},{"wave":"CW-3 — the coupling measure (closes FP)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"Every measurement checks out and they collectively argue AGAINST the reader's own floor. No `subsystemRowsCoupling.js` at the slot. The first consumer is confirmed still open verbatim at subsystemRowsRegen.js: 'Half of that instrument now exists; the differential harness does not, so this row reads UNOBSERVED and says why, rather than borrowing evidence'. The registry surface is exactly 58 live rows across six leaves (war 32, espionage 13, grammar 7, encounters 3, info 2, trade 1) — I counted `couplingRow(` per leaf — and `subsystemRowsWar.js` measures 557 effective of 800, so the volume's 771 figure is stale and the sibling-file rule is a design law rather than a ceiling necessity. `tests/helpers/couplingReceiptSample.js` is 203 lines. The degenerate-depth warning is real: with only provenanceKernel.js and roadsKernel.js writing causedBy engine-side, a lit soak run today would fire `chainDepthDegenerate` correctly. The floor is the problem. Instrument 1 alone is a new paired lit/dark same-seed soak harness with its families enumerated from registry `flags`, homed on `behavioral-observation.mjs` conventions, and carrying an ORDER-ASSERTED guard-the-guard proof against a known divergence — that is not a third of a wave, it is one to two cars by itself. Instrument 2 is 58 registry-keyed rows each needing a banded floor or an honest UNOBSERVED-and-say-why verdict, which is the item the reader itself says 'pushes the wave past one car' and then prices at one. Instrument 3 is three envelopes, each with an executed mutant negative control, consuming CW-2x's fixtures. CW-0w — four walkers, no soak, no bands, no 58-row surface — cost four cars. Five is the honest floor here and eight the realistic ceiling."}],"summary":"Skeptic pass on GR-CW, re-derived from the two volumes (docs/DESIGN_FP_ARCH_GR.md, docs/DESIGN_FP_ARCH_CW.md, DESIGN_FP_GRAMMAR.md §GR-5/6/7, ODQ §35) and from code at the slot (claude/composite-r4 @ a5876c0ea). SIX OF SEVEN WAVES ARE UNDERPRICED; one is confirmed. Family total corrected from the reader's 23-40 (mid ~31) to 35-55 (mid ~45).\n\nWHAT THE READER GOT RIGHT, RE-MEASURED AND CONFIRMED. The worker-bundle answer is exactly right and is the single strongest cell in the pricing: a BFS from `src/workers/generation.worker.js` reaches 153 modules and ZERO of them live in `src/domain/worldPulse`, `src/domain/display`, `src/domain/realm` or `src/domain/certification`, so all seven waves add 0 KB and none needs a lazy chunk — and the zero is load-bearing because `WORKER_BUNDLE_CEILING_BYTES = 1404493` (tests/build/generationWorkerLazy.test.js:78) was re-minted from a build that measured exactly 1,404,493 B at dd0b68c0d, leaving literally one byte of nothing. Also confirmed: peaceTerms.js at 797 effective of 800; warDeployment.js 684/800; the GR-6 mount facts and ODQ §35.2's parking; the GR-5 certification row's itemised NOT-BUILT list; SC-3 discharged by `SIGNIFICANCE_CLASSES` at bandFamilies.js:75; causedBy adoption still exactly six files with no adoption walker anywhere in tests/lint; 58 registry rows across six leaves; subsystemRowsRegen's honest gap still open.\n\nTHE FIVE FINDINGS THAT MOVE THE PRICE. (1) GR-4's remaining `honored_by_silence` car is not a car, it is a MOUNT RULING: the expiry road's only transition site is `expireStaleActorMajors`, called bare from pulseKernel.js:347, and pulseKernel measures 1581 effective against a baseline of 1581 under R-BLD-10's no-raise law — zero headroom — while the timestamp-derived alternative is refused in writing by treatySuccessionReaffirmedVoice.js's own header. GR-4 is blocked the same way GR-6 is, and the reader classed it as buildable. (2) The estate's cadence refutes the GR-5 floor: GR-5A, a ONE-LINE monotone fold, took three implementer commits (318a7fff7, 85d19adf1, e93376ec1); the remainder is eight behaviours, one of which reaches into the lit label-proposal lane, all of them squeezed against peaceTerms.js's three remaining effective lines. (3) GR-7 has no pendings to convert — `VIRTUAL_PENDING_RULE_KEYS` is empty and all four GR rows are AUTHORED — and it silently owes `hollowed_quiet`'s prose, deferred BY NAME to GR-7 in grammarReceiptPools.js and pinned as an absence by grammarLifecycleKindPools.walker.test.js, plus a reconciliation of a `PACT_ENDINGS` constant that is exported TWICE under one name with different contents while `reaffirmed`, an ending the code actually writes, appears in neither. (4) CW-1's seam receipt measures the wrong file: the volume expressly ordered navigation by the feed-assembly symbol rather than its own guess, and the Herald feed composer is `buildHeraldFeed` in src/components/map/heraldFeed.js (115 effective of 800), not realmItemReadModel.js — the conclusion that no decomposition is forced survives, the receipt does not, and realmItemReadModel's 16 remaining effective lines are thin rather than comfortable if it turns out to be a seam after all. CW-1 also owes SC-1 registry rows for its own display→certification and display→worldPulse edges, which the inclusion walker cannot catch because its census scope is `src/domain/(worldPulse|spatial)` only. (5) CW-2x's output-moving mechanism is mis-stated: `graceLine` is consumed by CauseWalkPanel.jsx alone and never reaches heraldCausalVoice (itself gated by `heraldCausalVoiceEnabled`) or discourseKernel (gated by `discourseProseEnabled`), so slice 2 changes a DM panel line, not Herald prose, and re-records no news golden — the wave is still not dark and must declare the shift, but it should be budgeted against panel pins.\n\nON THE PROPOSED FIRST CAR. The SC-6 causedBy-adoption walker is the right first act and its premise is CONFIRMED (six files, two engine-side writers, one of them the V-24d dark edge, and no adoption walker in tests/lint). One design correction before it is built: the `couplingInclusion.walker.test.js` precedent it is told to copy carries `CENSUS_SCOPE_RE = /^src\\/domain\\/(?:worldPulse|spatial)\\//` at :1054, so an adoption walker inheriting that scope would be blind to mint sites outside those two directories — `src/domain/region/wizardNews.js` among them. Name the scope deliberately or the new walker reproduces the blind spot the CR-FP-11 amendment was written to remove.\n\nCalibration used throughout: CW-0w landed four instrument slices as exactly four cars (b3fb8f494, f7da6b607, 03dee5fd5, e30770bd0) — one car per slice with no repairs — while the flag-bearing GR-5A needed three cars for one line. Instrument waves price at one car per slice; flag-bearing, band-carrying, corpus-touching waves price above it."}}
{"type":"result","key":"v2:00339a883ecaa218e2fb090ad9d641cabeaf9a62412327c62cdf4d03f2680377","agentId":"a45a7b6ed273c1c37","result":{"family":"LG0 — LOGISTICS LG-0 and LG-1 (item #33, the family's first train)","verdicts":[{"wave":"LG-0 — THE WILLINGNESS GATE (mints circleNetworkEnabled; carries the LG-0V volume fold)","verdict":"TOO LOW","correctedLow":6,"correctedHigh":9,"why":"CONFIRMED and not re-litigated: the graph is built and lit (teleportEdges.js:193-213, dormancy :197, magic gate :124-127, capacity recorded :75/:203, campaignSpatialCanonize.js:124, distanceRead.js:361-443/:786-822, rumorNetwork.js:673); the willingness predicate is absent and the header at :22-26 promises it; the four reads are pure functions of the deep-frozen digest memoized on digest IDENTITY (TELEPORT_NODE_MEMO :372, TELEPORT_ADJ_MEMO :393, AUG_MEMO :788, CANDIDATE_MEMO :1018-ish) so no per-tick predicate fits inside them — I re-read candidateRoutes and it memo-keys on `${from}|${to}|${k}` with no tick term, so reject-after-the-fact genuinely fails and the filtered per-tick view is right; contributionLedgerShape.test.js asserts toHaveLength(35) twice at :119-120 while its title at :49 still says 'at 28' (the recon's 28 IS stale, 35→36); the last mint touched 15 files; the rungs exist (warCosts.js:422-430, anticipatedReactions.js:104, relationshipTypeBetween at embassyHazard.js:36-54 with the O(1) index at tickIndices.js:393-406) so no new vocabulary; and ~0 KB to the generation worker — I re-ran the import BFS over src/workers/generation.worker.js and got ZERO modules under spatial/ and ZERO under worldPulse/ (the load-bearing figure; my closure is 227 modules, not the reader's 153, so quote the zero and drop the 153). FIVE THINGS UNPRICED, and together they are worth 2-3 cars. (1) THE CONSUMER CENSUS IS MISSING, and it is the obligation that decides whether the gate is real or cosmetic. pathCost(digest,a,b,season) carries no worldState and no tick, so the predicate cannot reach it without either threading a parameter or a module-level mutable that breaks the module's own purity claim. Measured at the slot: hopWeeks has 35 consumer modules, chooseRoute 14, pathCost 10, candidateRoutes 6 — roughly 45 distinct files. The reader wires exactly two defect sites and never decides which of the 45 must see the view; SPV §1.2 requires the §38.4 consumer census PRE-EXECUTED, and the estate's own hazard law says PARTIAL is the status that hides. (2) THERE IS A DISPLAY SEAM AND THE READER SAYS THERE IS NONE: src/components/map/TravelersLayer.jsx:101 reads isTeleportNode to SUPPRESS drawing a land route for any teleport-involved pair — close an edge on the predicate and the map hides the road that is now the only road. (3) THE TTS OBLIGATION HAS NO TOOL AT THIS SLOT: volume §6 makes every LG packet run the TTS validator simulation, and scripts/validate-train-topology.mjs does not exist at a5876c0ea (verified by ls; DESIGN_PREVERIFICATION §2.1 says it lands with infra-3, and its seed survives only as ledger-side docs/preverification-tools/laneTE10-probe2.mjs.txt / -probe3). LG-0 either inherits a blocked compile obligation or lands the tool; the reader names neither. (4) THE ANNEX IS NOT 'DOCS ONLY, NO TEST MOVEMENT'. The reader's claim that docs/implementation/preverification/ exists on neither branch is FALSE at the build slot — it holds EP-SUBSTRATE.md (336 lines, 54 KB, 131 graded claims with quoted executed evidence and three focused vitest runs) and WF-SUBSTRATE.md (424 lines). LG-SUBSTRATE.md and laneLGR-substrate.md are genuinely absent everywhere, so the re-grade is real, but the landed standard is per-claim grading, and SPV §1.5 staffs the sweep as its own read-only lane at ~one lane-hour, not as car 1's side-work. (5) THE FLAG BILL IS SEVEN NAMED SURFACES, not four: contributionLedgerShape.test.js's note calls its pair 'the FOURTH obligation', coveringArrayCoverage.test.js:167 calls itself 'the sixth flag-bill surface', and the SEAT-78 note enumerates three-in-one-commit plus four downstream. The reader's file list is right and his label is the volume's stale §3 three-obligation count. On top of that no spatial or logistics certification leaf exists (16 subsystemRows*.js leaves, none), so the mint must pick a home or cut a new leaf under the ordered-equality pin. TWO DEPENDENCY CORRECTIONS the compile must carry rather than the reader's wording. First, 'Q10's band-walker prefix is NOT a blocker' contradicts the record's own ruled default order for #33: the §931 sitting item 19 reads 'in the order LG-0 · LG-1 · LG-2 · LG-3/4 · LG-5 · LG-6/7/8 · LG-9..12 · §125.4/§125.5, with the Q10 prefix first.' The volume §D's band-conditional wording and the sitting's flat ordering disagree; that is a chair question, not something LG-0 may settle in the cheap direction by assertion. Second, the ownerGate and outputMoving rows are one ruling stale: §764.3 was AMENDED at §931 item 1, so lighting is not a free-standing later keystroke — it must board THE SECOND DECLARED LIGHTING, one shared pre-freeze window it competes for with treasuryEnabled (item 3) and the three W-SEAT keys (item 14). Miss that window and the gate cannot light before the golden freeze at all. Everything else in the ownerGate row stands: no migration, no persisted widening (item 7 signed only LG-2's commitment ledger), dark at landing, byte-identical dormancy."},{"wave":"LG-1 — GRAPH FORMALIZATION (the predicate as an edge attribute, capacity as edge cost, the relay guarded, the §125.1 toll arm)","verdict":"TOO LOW","correctedLow":5,"correctedHigh":8,"why":"CONFIRMED and not re-litigated: the transitive relay of §46 is DERIVED, not built — augmentedAdjacency folds the teleport edges into the land graph at distanceRead.js:790-806 and the deterministic Dijkstra at :833 produces the A-marches-to-B-teleports-B-to-C relay for free, so LG-1 guards it rather than building it, and the negative-control fixtures already exist (teleport.test.js:180-204, the land-isolated and siege-cut bloc cases); TELEPORT_EDGE_CAPACITY = 2 is recorded and unread since M9c; the passage attribute must stay per-tick and OFF the frozen slot (freezing it would break L-LG-3's recompute law and put politics into a canon that outlives it — the reader is right and this is the sharpest thing in his LG-1 row); §125.1 IS signed and §125a IS confirmed (the §931 sitting's rulings paragraph, items 5 and 6). THREE PRICING ERRORS, all in the cheap direction. (1) THE TOLL'S INCOME PATH IS MISIDENTIFIED. The reader routes it through 'the closed eight-member TAX_FORMS' market_tolls (treasury.js:269-272), so no ninth form is minted and FINITE-SEMANTICS holds.' But TAX_FORMS is a TAXATION vocabulary whose rate is a BAND (TAX_RATE_BANDS at treasury.js:280) and whose feed is a LABEL map off generator-authored income sources (:344-345, 'Toll Revenue'/'Gate Tolls' → market_tolls). The landed toll-INCOME path is something else entirely: entrepots.js books toll takings as a PROSPERITY LIFT (tollProsperityOf :175, tollProsperityFor :192) inside a self-balancing centrality loop with four co-built brakes (:20-44). So 'the host booking the income' is either an extension of that feedback loop — in which case the coefficient must be derived so as not to destabilize four brakes, which is a chair derivation under L-LG-7 plus a stability proof — or a new per-passage treasury write, which is a new money path. It is not 'needs no new money surface'. (2) THE EXISTING TOLL TERM CANNOT EXPRESS THE RULING AS SIGNED. §125.1 is allies free, trading partners tolled, host books. tollRateOf(worldState, id) (embattlement.js:156-162) takes ONE settlement id and NO counterparty, so it cannot be relationship-conditional; and scoreRoute sums it over INTERMEDIARIES ONLY — `for (let i = 1; i < nodes.length - 1; i++) toll += tollRateOf(worldState, nodes[i]);` at :355 — so a circle passage that TERMINATES at the host books nothing, which is the commonest circle passage there is. LG-1 therefore mints a NEW edge-scoped, relationship-conditional toll kind that must compose with the node toll while leaving its byte-identity untouched, and scoreRoute is reached through chooseRoute's 14 consumer modules: the same threading problem as LG-0's predicate, at a second seam. That is a car on its own, not a coefficient hung on an existing term. (3) THE CAPACITY CAR CANNOT MIRROR navalLayer VERBATIM. convoyCapacityAvailable (navalLayer.js:333-343) counts ACTIVE CONVOY RECORDS out of the persisted spatialLedgers.navalTransit. A circle has no record to count: teleport is zero-latency, and the cardinality law forbids per-passenger records. The throttle needs a LOAD DERIVATION from the trade plane — the entrepots idiom of earned centrality from the real gate-crossings of the M2/M6a active shipments — before it can throttle anything, and the §931 sitting says the same from the other side at item 6: 'the circle edge has no capacity throttle yet, so preemption has nothing to act on until LG-1 adds one.' seaLaneCapacityOf is the right precedent for the recorded-but-unenforced SHAPE; it is not a template for the load source. DEPENDENCY CORRECTION: the reader lists the two standing code-side deferrals as 'asked only if they bind'. Item 21 of the sitting names LG-1's own car as exactly where (a) binds — 'ask only at the car that first makes them pay: (a) the toll/preemption car — the first car that makes bloc membership pay' — so LG-1 carries that chair interaction by name, not conditionally. AFFORD_MIN (navalLayer.js:69-73) is correctly LG-2's, as the reader says. The band caveat is also softer than the reader makes it: the route-cost toll is a scalar in median-hop units (tollRateOf's own contract), so it does NOT drag in Q10 by itself; what drags in Q10 is any LG BAND, and per the §931 item 19 ordering the prefix is meant to land ahead of the family regardless."}],"summary":"Both waves are TOO LOW: LG-0 moves 4-6 → 6-9, LG-1 moves 3-5 → 5-8, for a first train of 11-17 cars rather than the reader's 7-11. The reader's re-price is substantially right where it is destructive — the circle graph, its dormancy floor, its magic gate, its routing fold, the recorded capacity attribute and Q9's two catalog features are all genuinely built and must be struck from LG-0's bill; the willingness predicate genuinely was never written; the memoized-on-digest-identity architecture genuinely rules out an in-memo predicate and forces the per-tick filtered view; and the flag bill is 35→36, not 28. It is wrong where it is constructive, and always in the cheap direction. LG-0's single largest unpriced item is the consumer census: the four teleport reads sit behind a distanceRead API with roughly 45 consumer modules (hopWeeks 35, chooseRoute 14, pathCost 10, candidateRoutes 6, measured at the slot), and pathCost carries neither worldState nor tick, so the wave must decide and prove which consumers see the gate or ship a PARTIAL one — plus a display seam the reader says does not exist (TravelersLayer.jsx:101 suppresses land routes for teleport-involved pairs), a TTS tool that does not exist at a5876c0ea (scripts/validate-train-topology.mjs absent; only ledger-side .txt seeds), and a substrate annex whose two landed precedents are 336 and 424 lines of per-claim graded evidence rather than a docs-only fold. LG-1's toll arm is the reader's weakest claim: the income head he names (TAX_FORMS market_tolls) is a taxation band fed by generator-authored income labels, not a booking surface — the landed toll income is entrepots.js's prosperity lift inside a four-brake self-balancing loop — and the existing toll term is a per-node pass-through that takes no counterparty and excludes the destination, so it can express neither 'allies free' nor 'the host books'. Two record-level corrections the compile must carry rather than the reader's wording: 'Q10's prefix is NOT a blocker' contradicts the §931 sitting item 19's own ruled default order ('with the Q10 prefix first'), and §764.3 was amended at §931 item 1, so LG-0's lighting is not a free-standing later act but a seat in the one shared second-lighting window before the golden freeze, contested with treasuryEnabled and the three W-SEAT keys. Everything else in both ownerGate rows stands (no migration, no persisted widening, dark at landing) and the ~0 KB worker figure is confirmed by re-run BFS — zero spatial/ and zero worldPulse/ modules in the generation worker's closure, though the reader's '153 modules' does not reproduce (227 at a plain relative-import BFS), so quote the zero and not the count. Consequence for the programme: at 11-17 cars for two of eighteen members, the recon's ≈32 (28-36) for the whole family needs re-taking upward, or the family must accept that its first train is front-loaded by the once-per-family costs (the volume fold, the substrate sweep, the flag mint) and that LG-2 onward will not average what the remainder implies."}}
{"type":"started","key":"v2:8881172aacaf796cec090e7a713d6fc5f91942daded60a3df71a2d95e43b79b4","agentId":"a511c550bcb37190a","label":"fold","phase":"Fold"}
{"type":"result","key":"v2:8881172aacaf796cec090e7a713d6fc5f91942daded60a3df71a2d95e43b79b4","agentId":"a511c550bcb37190a","result":"1. #32 totals **401–610 cars (centre ≈500)** after the skeptics — every skepticked family moved UP ×1.3–1.6, none down; the first pass's 100–200 was short by ~3×. Excluding the seven WY-8a-blocked WC waves: 365–553. ES (7–13) and EP (11–17) are PLAUSIBLE (no skeptic); all other family rows CONFIRMED.\n2. Per family: TR 32–45 · WF 47–74 · POP 32–45 · WY 44–67 · IN 27–38 · ES 7–13 · WC 97–148 (67–100 CONFIRMED for WC-0..10) · HB 35–61 · EP 11–17 · INT 34–47 · GR-CW 35–55.\n3. #33 first train LG-0 + LG-1 re-priced **11–17** (LG-0 6–9, LG-1 5–8), front-loaded by the LG-0V fold, the lost substrate annex, the flag mint and the absent TTS tool.\n4. Critical path serial cost **15–25 cars** (SP-D landed; ES-6b ∥ IN-5 4–6 → ES-7 6–11 → CW-3 5–8); the IN skeptic refuted the ES-6b→IN-5 dependency (`DESIGN_FP_ARCH_ES.md:1641`), so IN-5 can start now, and CW-3 has a second spine (SC-6 walker → CW-1 → CW-2x) beside it.\n5. Lane 1: **TR-4** the grain road, 5–7 cars — CR-2 ruled to win over §5 #26 (consolidation first, byte-identical pin, STOP clause); TR-4 lands before WY-3, declared.\n6. Lane 2: **TR-5** the pact lane, 4–6 cars — TWO rows to mint (not one); `route_wardenship` is a live colliding token, ruled to the spelling `route_warden`; both red-flips assert by set equality.\n7. Lane 3: **POP-1** the believed road, 5–7 cars — the column-class surface is three structures deep (WC-0B) and pinned by the WAR-family `militaryColumnRelease.test.js`; the class is part of the column key.\n8. Lane 4: **WY-0 → WY-1**, 5–7 cars — WY-0 car 2 is output-moving at landing (Q3's veto window closes there); `DEFAULT_LAYERS` is 22 keys/17 boolean; the eager first-paint margin is 795 B, measured only with a built dist.\n9. Forced adjustments to §3j: WF-6 moves to lane 5 (peaceTerms.js 797/800 collision with TR-5); TR-3 (+TR-1's mount) boards before TR-6; POP-2 waits on POP-7's backlog clearance; WC-10 gains a WC-9 edge and WC-10..16 wait on WY-8a; TR-2 waits on an SP-4a SPINE amendment.\n10. Worker bundle: **0 KB to the ceilinged generation worker for all twelve families, CONFIRMED by nine independent BFS runs** (ceiling 1,404,493 B); the only exception is EP §7a row 3 (tens of bytes, PLAUSIBLE). ~250–400 KB minified lands in `advanceInterval.worker.js`/lazy chunks, which have no ceiling; none of it can or need live in a lazy chunk (runtime `=== true` reads).\n11. The two budgets that DO bind and were mis-priced: the eager first-paint closure (795 B; INT-3's stressorsCore row, WY-0/9, POP-6, and — derived — ~40 flag strings at ~30 B each exhaust it mid-programme) and the lazy `engine` chunk (677 B); both suites are dist-gated and skip silently.\n12. Files: `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/recon-32-34/PRICING-32-33.md` (45 KB: §A answer, §B 12-family per-wave table, §C 56-lane boarding order, §D what waits) and `briefs-932/brief-TR-4.md`, `brief-TR-5.md`, `brief-POP-1.md`, `brief-WY-0-WY-1.md` (13–15 KB each, every figure labelled CONFIRMED/PLAUSIBLE, grep traps from PROGRAMME §A named)."}
