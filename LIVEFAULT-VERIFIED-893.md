# §893 — THREE LIVE FAULTS RE-VERIFIED AT THE TRAIN TIP `62afd84d3`
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · executed greps, not recalled claims. The owner's
standing caution requires this check before building on any Opus artifact; two of the three gate a
lane I am about to dispatch.

| fault | verdict | the evidence, executed |
|---|---|---|
| ENC row 2 — `respect` into `BOND_KINDS` | **CONFIRMED still unexecuted** | `npcLadderState.js:407` → `BOND_KINDS = Object.freeze(new Set(['loyalty', 'gratitude', 'friendship']))`. No `respect`. |
| ENC row 3 — `rivalry` into `GRUDGE_KINDS` | **CONFIRMED still unexecuted** | `npcLadderState.js:383` → `GRUDGE_KINDS = Object.freeze(new Set(['contest_loss', 'contest_forestalled']))`. No `rivalry`. |
| RR-10 — `errandSpineEnabled` into `PREVIEW_OVERLAY` | **CONFIRMED still unexecuted** | `scripts/review/readerCorpus.mjs:116` → the overlay freezes exactly four keys: `warMemoryEnabled`, `espionageEnabled`, `demographicsEnabled`, `neutralNeighborsEnabled`. |

⭐ **THE SHAPE IS THE SAME IN ALL THREE: a ruling was made, recorded, and never reached the code**, and
the landed artifacts assert the opposite of the ledger. This is §892's E1 law at product scale — *an
amendment declared in a row is not an amendment; the clause text is the receipt* — and it is now
proven at four sites in two days (E1 itself, ENC-4's HOLE 0, and these three).

⚠ **A COLLATERAL CONFIRMATION FOR §893 RULING 3.** `PREVIEW_OVERLAY` sets `warMemoryEnabled: true`,
so **the review corpus the owner will walk deliberately turns war memory ON** — and O-5/RR-2 measured
that the flag writes records nothing can read. The walk would therefore meet silence exactly where the
preset promises a shift. That is independent evidence for FUNDING the Remembrance reader, and it
raises the item from *nice* to *the walk is degraded without it*.

⛔ **CONSEQUENCE FOR DISPATCH.** The ENC3 brief must state these as facts to be FIXED, not as
background — a lane that trusts the ledger and skips the two `Set` additions will build a leaf whose
kinds never resolve. `brief-ENC3.md` is amended accordingly.
