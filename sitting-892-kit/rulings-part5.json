[
  {"line": 2120, "id": "§883.4", "ruling": "*RULED (§892, Fable 5.1):* **ALL SIX CALLS AND BOTH CHAIR RULINGS RATIFIED; the deferral AMENDED as still-unbuilt (S10, the pre-cure and cured parsers EXECUTED in a scratch copy: unstaged-first refuses `ests/lint/…` under the old parser and reads clean under the new; staged-first and foreign-first behave exactly as the row says).** The trigger is dirty-UNSTAGED-and-first and the first refreeze itself creates that state — the refreeze was NOT REPEATABLE, exactly as the lane sharpened it. The `.trim()` is CONFINED (`gitRaw` untrimmed; `gitOut` trimmed with one caller, `rev-parse HEAD`) and `parsePorcelainPaths` asserts `line[2] === ' '` per record — stronger than the row states, since it also refuses a damaged SECOND record. The re-cut at `7f974e855` cost nothing (empty `diff --stat` on the target across the 28-commit gap). The chair's two orderings were EXECUTED as ruled: `20f089aaa` is pick 1 of 23 in HORIZON-INSTR ahead of the refreeze at pick 22, and ⭐ **the landing commit `d124eae45` is the closing receipt the row never cites — the second refreeze ran on the real landing tree with the register dirty-unstaged and first, REFROZE, and left the register blob byte-identical across both runs**; CURE-CAPSULE is absent from `7f974e855..f5a6c3bbf` and landed as pick 1 of 6 at §885. The base-state-capsule handover is exact at `7f974e855:431–434` (the prefix allowlist DROPS a mangled path — the false-CLEAN direction). **AMEND (3):** the deferral is right, but the battery it enabled (a canned-record battery over the pure parser) and the fold of the two same-named `parsePorcelainPaths` copies are BOTH still unbuilt at `4233031ba`, recorded only in receipt prose — docketed here as one hygiene car. Registers unmoved (lighting tuple re-probed at the CUREPORC dock; ratchet 34 tests before and after; OSR PLAUSIBLE, receipt-only); the four `voiceMechanics` reds are 4 of the 10 banked entries."},
  {"line": 2138, "id": "§883.5", "ruling": "*RULED (§892, Fable 5.1):* **The clause-S inversion, the three plants, the positional-index cure, the owed direction and the figures ALL RATIFIED; the receipts line AMENDED (S10, executed at `laneINSTRWRW-tree@e2967f2a1` — the lighting tuple re-probed EXACT at 2496/370/2126/22493/6075).** Clause W owns \"is it written\" with `identity ∈ dialGated` for the generation-dial reason only (`isDialRow` = reason AND door.kind, :332), refuses on a missing set (\"an absent measurement is not an acquittal\"), and clause S inverts for that reason (presence convicts as BANK THE WIN if LIT, else THE DIAL HAS ROLLED); D-dial and S both run after W, so no dial row reaches them unmeasured. The plants: (a) the one-bit narrowness plant convicts at W and passes when admitted (:809); (b) the `engine-internal` and `pending-surface` rows still convict under an invisible assignment — the widening did not leak past its reason; (c) a dial row with no measured set convicts, which is why `measure()` builds the corpora. The positional-index defect is real — `WRITER_DARK_REGISTER[0]` changed reason from `engine-internal` (355ff603b) to `dark-by-construction` (e2967f2a1), the lane's own first run failed exactly there (`walker-car4.log`, 1 failed | 55 passed) and the cured run is 56/56 — the row narrates it without quoting that log. The owed direction: 2 generation-dial rows against a frozen 31-entry `FROZEN_DIAL_GATED` roster with the reverse direction live (:1201–1204, :907); the chair's triage ruling stands. Gap carried: `compareDark`'s PRESENCE direction (`dialRolled: true`) has no plant — the walker asserts only the absent direction at :896 — docketed with the register's next car. **AMEND the receipts line:** the receipt holds R1–R9 only; Car 4 has NO §879-form row (its calls live in §11 prose) — \"plus Car 4's\" must read \"Car 4's calls are narrated in §11, not rowed\". ⚠ `laneINSTRWRW-probes/frozen-dialgated.txt` is a stale 34-entry pack-control roster, not the committed 31 — misleading to a reader, harmless to the tree."},
  {"line": 2154, "id": "§883.6", "ruling": "*RULED (§892, Fable 5.1):* **J-1, J-2 and the false-CLEAN direction RATIFIED; the consumer law RATIFIED and SHARPENED; the census denominator and the dock pointer AMENDED (S10, the battery EXECUTED 9/9 at the CURECAP dock, porcelain 0 → 0).** The hazard fails the OTHER way at `7f974e855:434`: `DIRTY_SCOPES` is a prefix ALLOWLIST, so a trimmed first record (`M scripts/x` → `ripts/x`) matches no scope, is DROPPED, and the generator stamps `BASE_STATE.json` at HEAD over an unmeasured tree; the committed D4 case encodes exactly that and now throws naming the record. **The consumer law, in its precise form:** what decides whether a mis-slice shouts or lies is the consumer's DEFAULT for an unrecognised record — fail-closed (an exclusion filter or equality test keeps the unknown record dirty) shouts; fail-open (an inclusion allowlist treats it as not mine) lies; the three named safe models (`worktree-inventory.mjs`, `implementation-session.mjs` with `-z`, the premortem readers) are fail-closed or delimiter-safe, so the law holds and generalises. J-1 (coverage extends an existing arm; ZERO registers moved; shape A priced at +2 titles) holds — 2 files, +141/−5, no new title, the battery 9 → 9. J-2 (the ratchet predicted unmoved, NOT executed, flagged PLAUSIBLE and priced) holds and was discharged by the §885 gate. The negative control and the sha-identical restore (`1ee9fb67…` matches the committed blob) hold on the receipt; the KNOWING second copy of the parser is right (coupling to an unlanded car would be worse) and the fold of the two copies remains owed (docketed with §883.4's hygiene car). **AMEND:** \"17 sites\" is not reproducible from the receipt's own table without an unstated counting convention (the ls-files class row says \"(5)\" but counts as 4) — state the convention or say 18; and the dock pointer is stale — `laneCURECAP-tree` now sits at the re-based `62be253f3` (§884.4), so `c422ee824` is read via the seal `refs/preserve/curecapsule-2026-09-02`, not the dock."}
]
