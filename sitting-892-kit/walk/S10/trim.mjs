import { readFileSync, writeFileSync } from 'node:fs';
const p = process.argv[2];
const j = JSON.parse(readFileSync(p, 'utf8'));
const E = {
"§883.4 CURE-PORCELAIN (HIGH — the base-state-capsule handover)":
"7f974e855 base-state-capsule.mjs :125 execFileSync(...) returned UNTRIMMED; :431-434 `porcelain.split('\\n').map((line) => line.slice(3).trim()).filter(Boolean).filter((path) => DIRTY_SCOPES.some((scope) => path === scope || path.startsWith(scope)))` — unexported (only :450 `(options.dirtyMeasuredPaths ?? dirtyMeasuredPaths)()`); injections :273,:275,:277,:288. c422ee824: `export function parsePorcelainPaths(row, raw)` with `if (line.length < 4 || line[2] !== ' ') { fail(row, …`; `export function dirtyMeasuredPaths(shell = shellOut)`; shellOut docblock `IT RETURNS STDOUT **UNTRIMMED**, AND THAT IS A CONTRACT`; D1–D7 present. My run: `Tests 9 passed (9)` TRUE_EXIT=0, porcelain [] before/after.",
"§883.5 INSTR-WRW Car 4 (HIGH — clause S INVERTS for generation-dial)":
"register :332 `const isDialRow = row.reason === 'dark-by-construction' && row.door.kind === 'generation-dial';` :361-370 `if (!dialGated) fail('W', '…An absent measurement is not an acquittal.'); if (!dialGated.has(row.identity)) fail('W', …BIT claim…)`; :389 `if (dial === lit) fail('D-dial', …already EQUALS…)`; :438-456 `if (isDialRow) { if (live && live.verdict === 'LIT') fail('S', …BANK THE WIN…); if (live) fail('S', 'the dial has ROLLED…'); } else { if (!live) fail('S', …); if (live.verdict !== 'DARK') fail('S', …) }`. compareDark: `isDialRow(row) ? liveJudged.has(row.identity) : !liveDark.has(row.identity)` + `dialRolled`. My run: `Tests 56 passed (56)` TRUE_EXIT=0, porcelain [] before/after.",
"§883.5 INSTR-WRW Car 4 (HIGH — the three plants)":
"(a) walker :809 `test('THE WIDENING IS NARROW: a generation-dial identity that is NOT dial-gated still convicts at clause W'` → `toContain('not in the measured dialGated set')`; admitted via `new Set([...live.dialGated, notGated.identity])` → writeProof `'dialGated membership (executed: present lit, absent dark)'`. (b) :848 `test('THE WIDENING IS REASON-SCOPED: an engine-internal row … still convicts at clause W'` + pending-surface twin :862-870. (c) :899 `test('a generation-dial row with NO measured dialGated set convicts — an absent measurement is not an acquittal'` … `expect(live.dialGated.size).toBe(31)`; measure(): `if (withDialCorpus) { … buildLitDialCorpus({ root }) … ({ root, rollDials: false }) … dialGated = dialGatedOf(…)`.",
"§883.5 INSTR-WRW Car 4 (MEDIUM — positional-index defect)":
"355ff603b walker :733 `const row = WRITER_DARK_REGISTER[0];` — register[0].reason `engine-internal` at 355ff603b (:81) vs `dark-by-construction` at e2967f2a1 (:85). walker-car4.log: `FAIL … the structural law refuses a malformed, duplicated, wrong-reason, or foreign-field row — AssertionError: expected [Function] to throw error matching /at least 80 characters/ but got 'writer dark register row \"customContentRoster on settlement\" (dark-by-construction) has noncanonical fields…'` (`1 failed | 55 passed (56)`); walker-car4b.log `56 passed (56)`. Cure :749-755 `WRITER_DARK_REGISTER.find((entry) => entry.identity === 'cultureProfileKey on config') … expect(row.reason).toBe('engine-internal');`.",
"§883.5 INSTR-WRW Car 4 (LOW — predicted→measured, baseline refreeze, gate, landing arithmetic)":
"Scratch probe at laneINSTRWRW-tree @e2967f2a1: `TUPLE {\"files\":2496,\"parked\":370,\"credited\":2126,\"titles\":22493,\"suiteTitles\":6075}` — EXACT. Baseline diff: only frozenAtSha, detectorDigest (e7243903…→74ab22c2…), registerDigest (8ae0f6e2…→ac473495…). test(/describe( counts: 6dcdb0784 39/5 · b2d4e961e 45/5 · 355ff603b 51/6 · e2967f2a1 56/6. lint-whole-car4tip.log FAILs = clampPrimitiveBaseline, sovereigntyLightingContract CENSUS, warCostKindPools ×3, warRulingKindPools; `Tests 6 failed | 1907 passed (1913)`. 4 commits over 9a0584f0f. Arithmetic: 22437+56=22493; 22491+56=22547; 6078+6=6084; 30788+56=30844; 2445+1=2446. d124eae45 used INSTR-WRW `+1/+0/+1/+56/+6` and landed exact.",
};
let n = 0;
for (const c of j.calls) if (E[c.row]) { c.evidence = E[c.row]; n++; }
writeFileSync(p, JSON.stringify(j, null, 2) + '\n');
console.log('replaced', n, 'maxEvidence', Math.max(...j.calls.map((c) => c.evidence.length)), 'maxNote', Math.max(...j.calls.map((c) => c.note.length)), 'calls', j.calls.length);
