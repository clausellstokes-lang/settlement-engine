+§887.2 · **THE HEARTBEAT CHECKPOINT: FIVE LANES HOME, NINE CARS SEALED, AND A QUIET-WINDOW GATE THE CHAIR BUILT THAT COULD NEVER OPEN.** All five lanes of the 09-03 wave reported and every car is sealed — enc2-enc6 `f052d1e77` · emdash `4da38b8d3` · emdash-annex `e46d4c3f1` · horizondark-cars12 `43e063a70` · determinism-wip `55d77a8d2`, atop the in-flight enc1-registers `f4cc5cb7d`. **Nine cars behind one landing, and every one of them was reachable from NO REF when its lane reported** — the §882.14 survival law paid out five separate times in a single afternoon, which is the strongest evidence yet that it is machinery and not ceremony. ⛔⛔ **THE ENC-1 TEST RATCHET REFUSED TWICE AND BOTH REFUSALS WERE CONTENTION THE CHAIR CREATED.** Each named `tests/simulation/centuryLegSoak.test.js` running **0 ms against a 900,000 ms budget** with ten tests returning as skips; load ended at **216.82**, then **276.47**. The chair read the load **at launch** both times (3.70, then 2.09) and the build lanes ramped up during the ~26-minute run. ⭐ **THE LAW: A FULL-SUITE INSTRUMENT NEEDS A QUIET WINDOW FOR ITS DURATION, NOT AT ITS START** — a start-time reading is a snapshot of a variable that moves, and §886's 'hold for quiet' had been read as a launch condition rather than a sustained one. ⚠ **THE DIAGNOSIS WAS WRONG IN BOTH DIRECTIONS BEFORE IT WAS RIGHT.** §886 banked the starvation signature as *'a DIFFERENT test failed each time — varied casualties on a starved box'*. Here the **same** test fell twice, which by that letter meant a real defect, and the chair said so. It is not: `centuryLegSoak` is the most expensive test in the tree, so under contention it is the **FIRST** casualty every time rather than a random one. ⭐ **The true signature is not 'varied casualties' — it is 'a RESOURCE failure rather than an ASSERTION failure, on a test FOREIGN to the consist'.** A test that ran 0 ms with no timeout signal, no assertion signal and no failure message did not fail; it never ran. The §886 row is amended accordingly. ⛔ **AND THE THIRD ERROR IS THE ONE WORTH THE OWNER'S TIME: THE CHAIR MECHANIZED THE LAW AND BUILT A GATE THAT COULD NEVER OPEN.** The waiter counted competing workers with `ps -ax -o command | grep -c 'vitest/dist/workers'` — a pattern that **matches its own grep** — so the count could never reach zero, and the streak sat at 0 while the load fell to 3.89. The chair had hit that exact self-matching trap **ninety minutes earlier**, by hand, named it in a report, and then wrote it into the machinery anyway. ⭐⭐ **A PREDICATE THAT CAN MATCH ITS OWN COMMAND LINE IS NOT A GATE**, and the bracket form `'[v]itest/dist/workers'` was proved to reach zero **before** it was trusted rather than after. **Also recorded, because a successor will meet it:** the 30-minute heartbeat template is **stale in two places** — it still names the §882/TAIL-F landing track, and it asks for a `Seat: Fable 5.1 — validated` trailer. The seat is **OPUS 5**, and a Fable trailer on an Opus act is precisely the unmarked-work class §685 exists to prevent, so the template is **overridden, not obeyed**. **Nothing was pushed, deployed, migrated or deleted; no tuning value was signed; no ceiling was raised; no register was refrozen by a lane.** (2026-09-03; SEAT: Opus 5 — Fable-unvalidated)
