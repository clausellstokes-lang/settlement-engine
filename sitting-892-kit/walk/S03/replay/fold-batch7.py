# -*- coding: utf-8 -*-
"""BATCH 7 — §16's PLAUSIBLE list amended (⟦G0-93⟧), then §17 G0 DISPOSITION, §18 THE INSTR CAR LIST
AS RE-CUT and §19 CHAIR ROWS appended; the header flipped to FOLDED.
"""
import time
import foldlib

f = foldlib.Folder()

# ── ⟦G0-93⟧ §16 — the "what remains PLAUSIBLE" list, after the five G0 probes ──
f.rep(
    "**What remains PLAUSIBLE after the panel** (§14, the rows the fold added): E41 the CI sum; E42 the headless tab render; E43 the worker bundle's measured size; E44 the interim 4 s cell's plateau before the wave; E45 `PIPELINE_REACHERS` tree-shaken; E46 the src-wide roster's completeness. Each names its experiment and its STOP; none is argued.",
    """**What remains PLAUSIBLE after the panel** (§14, the rows the fold added): E41 the CI sum; ~~E42 the headless tab render~~; ~~E43 the worker bundle's measured size~~; E44 the interim 4 s cell's plateau before the wave; ~~E45 `PIPELINE_REACHERS` tree-shaken~~; E46 the src-wide roster's completeness. Each names its experiment and its STOP; none is argued.

⟦G0-93⟧ **AND AFTER THE FIVE G0 PROBES the list is shorter and the survivors are named honestly.** SETTLED by the wave: E42 (all three tabs render — mechanism proven, a real surface still owed to Car 2), E43 (1,404,723 B, chunk count **1**), E45 (tree-shaken from the WHOLE build, stronger than priced), plus E15 · E17 · E18 · E19 · E20 · E21 · E22-solo · E24 · E25 · E27 · E28 · E30 · E32 · E33 · E34 · E35 · E36 · E5-WRW · E5-RR, and E16 REFUTED in three parts plus its method. **What is genuinely still PLAUSIBLE, with its owner:** E41 the CI sum (read from the last green CI run before boarding) · E44 the interim 4 s cell's plateau (the manual run after SOAKCHAIN Car 4) · E46 the src-wide roster's completeness (every src car's WHOLE lint/copy/docs) · **E37 — the one row a probe was chartered for and did NOT settle** (the regen census extension was not in Car 0's act list; S3.1 stays ARMED) · E38 (its named instrument measures a base two landings later) · E23's remaining clause (Car 3's real widening run) · **E22's PARALLEL leg** (a property of a 558-file run, not of a probe) · **the four DERIVED lighting figures** of READERREVIEW Car 1 (the walker fails fast on `files`; the refreeze refuses on a dirty tree) · **whether espionage would EVER write at a larger realm scale** (four horizons falsified at four settlements; settlement count not varied). ⭐ **The wave's own lesson, banked: five of the six things that changed the INSTR cars most were not on this list at all** — they were roster rows, method clauses and bill lines the volume asserted without ever entering them in §14. E47 and E48 are two such claims, entered and closed in one act; the standing cure is §17's law — *a draft's "confirmed" row is a HYPOTHESIS and belongs in the ledger the day it is written.*""",
)

TS = time.strftime('%H:%M')

APPEND = r'''
---

## §17 G0 DISPOSITION — every finding of all five probes, and where it landed

The G0 wave ran FIVE zero-byte/probe-build Car 0s at **C′ = `9a0584f0f`** (seal ref `refs/preserve/landing-TAILF-2026-09-02` verified equal at each), 2026-09-02 04:27–09:09 ET: **WRWALKER** (`$SP/laneG0WRW-receipt.md`, one STOP) · **CHARSET** (`$SP/laneG0CHARSET-receipt.md`, clean) · **COVERAGE** (`$SP/laneG0COV-receipt.md`, clean) · **WORKER** (`$SP/laneG0WORKER-receipt.md`, the probe build, one STOP) · **READERREVIEW** (`$SP/laneG0RR-receipt.md`, clean, nine probes). Every disposition below is FOLDED unless the row says otherwise; nothing is argued away.

**Severity:** BLOCKING = a car cannot be cut as written · MAJOR = a bill, proof or method is wrong · MINOR = a figure or a citation moves.

| id | lane | sev | the claim, and what the probe measured | disposition |
|---|---|---|---|---|
| W1 | WRWALKER | **BLOCKING** | the `web` closure would answer "is this key's name mentioned in three quarters of the codebase": kernel leak-through **86.96 %** (380 of 437 `src/domain/worldPulse` files inside `web`) against the > 50 % STOP; `web` is 1,562 of 2,143 src files | FOLDED ⟦G0-2⟧⟦G0-8⟧⟦G0-9⟧ — `web` SPLITS into `web-display` (counting) + `web-transitive` (reported), in §1.10's closed vocabulary and CHARSET's pinned mirror |
| W2 | WRWALKER | **BLOCKING** | F1: the title's `identityOf({key:k, shapes:[s]})` shorthand THROWS under `assertLegacyFinding` — it is prose, not code | FOLDED ⟦G0-1⟧ — Car 1's canonical six-field finding, with the refusal message named |
| W3 | WRWALKER | MAJOR | F2: **579 of the 880 DARK rows are not written code keys** (383 on `osr000…osr015` shapes minted from the corpus's own save ids, 257 data-valued) — the reviewable cohort is **301** | FOLDED ⟦G0-5⟧ — sized by a stated RULE, never a hand list; the cohort re-derived under the split |
| W4 | WRWALKER | MAJOR | F3: WRW-4's "tighten the red to grade R" cannot be taken — R-only makes dark **90.86 %**, tripping the 60 % STOP by 30 points | FOLDED ⟦G0-11⟧ — retracted for "N counts until the resolver's reach is raised" |
| W5 | WRWALKER | MAJOR | F4: §7.1's "the culture live join is expected EMPTY" is false — **six live rows** (`pdf/lib/viewModel.js` ×4, `summary/settlementQuickGuide.js` ×2) | FOLDED ⟦G0-12⟧ — Car 2's arm becomes LIVE and exact |
| W6 | WRWALKER | MAJOR | proof B REFUTED: emptying the STOP set moves dark 880 → 808 (−8 %), so the STOP is NOT what makes the cohort dark | FOLDED ⟦G0-14⟧ — proof B re-specified honestly |
| W7 | WRWALKER | MAJOR | timing: corpus **8.3 s solo** / **26.5 s at ×8 named load** (volume ~47 s; the 251,002 ms figure was contended) | FOLDED ⟦G0-15⟧ — the `beforeAll` budget re-priced `900_000 → 300_000`; §11.4 re-priced |
| W8 | WRWALKER | MAJOR | the six drafted DARK rows adjudicated: 1 CONFIRMED (`cultureProfileKey on config`), 1 HALF, four not in W or unobservable | FOLDED ⟦G0-13⟧ — WRW-2's list collapses to `isolationSupport` alone |
| W9–W13 | WRWALKER | MINOR | `PUBLIC_TOPLEVEL_KEYS` 38 / `NPC_PUBLIC_KEYS` 11 CONFIRMED · `corpusMeta` equal on all nine fields and the digest identical across 15 processes · `src/components` **565** (LIVE 544, orphans 21) · the `BUILTIN_MEMBERS` twin = OSR's 72 names byte-exact · the assumed baseline path RETIRED | FOLDED ⟦G0-6⟧⟦G0-7⟧⟦G0-8⟧⟦G0-10⟧⟦G0-16⟧ |
| C1 | CHARSET | MAJOR | `S` has THREE sizes by predicate — class-alone 191, **context-anchored whole pass 190**, bare whole pass 189; and `U+00A0` is silently collapsed by both paid renderers yet sits in no ban list | FOLDED ⟦G0-3⟧⟦G0-20⟧⟦G0-24⟧⟦G0-27⟧ — 190 adopted as `S`'s definition; NBSP joins `bans.invisible` |
| C2 | CHARSET | MAJOR | a new `.generated.js` under `src/` reds the OSR gate anyway — not on findings but on the execution-INPUT provenance digest; the `--write` absorb refuses until the files are committed | FOLDED ⟦G0-4⟧⟦G0-22⟧ — the **commit → `--write` → commit** ritual priced into EVERY minting car and named in §1.13 |
| C3 | CHARSET | MAJOR | `customContentManifest.js` has **fourteen** static importers at C′, not five; five entry-owned dynamic targets reach it; eager modules reaching it **0** | FOLDED ⟦G0-23⟧⟦G0-30⟧ — R2's `import()` cure is Car 1's DEFAULT, not a contingency |
| C4 | CHARSET | MAJOR | E30's "≈ 25–35 titles" reads HIGH: 22 authored → **+17 credited / +3 suiteTitles**; the CHARSET train's refreeze is **2499 / 371 / 2128** | FOLDED ⟦G0-22⟧⟦G0-31⟧ |
| C5 | CHARSET | MAJOR | E27: the jsPDF runtime WinAnsi table IS reachable (jspdf 4.2.1, 27 keys, `U+20AC → 128`, byte-identical to the dependency's decoded block) | FOLDED ⟦G0-28⟧⟦G0-29⟧ — §8.7 R1 RETIRED |
| C6–C9 | CHARSET | MINOR | `|C|` 218 · `|D|` 759 · union 959 · `D ∩ C` 217 · `W ⊂ D` · `U+00AD ∉ D` · the face roster exactly 8 TTFs and EQUAL to the `public/fonts` listing · the hoisted `s()` byte-identical to `generateCampaignPDF.js:88` (`generateWorldBook.js:61` differs by whitespace only) · probe 6's BYTE half declared OWED to the ONE G5 probe build (RATIFIED — no extra build) | FOLDED ⟦G0-20⟧⟦G0-21⟧⟦G0-25⟧⟦G0-26⟧ |
| V1 | COVERAGE | **BLOCKING** | roster C1: `status on activeChains` scores **0 of 7 canonical tokens over 1,029 rows** — the generator persists the LEGACY vocabulary and `canonicalSupplyChainStatus` maps at READ time | FOLDED ⟦G0-34⟧⟦G0-39⟧ + §14 **E47** — `read(s)` applies the canonical mapping; the legacy vocabulary is a WRWALKER/OSR handover |
| V2 | COVERAGE | MAJOR | roster C5/C6: `severityBand on activeConditions` is NOT zero-by-construction (**516/525** carry a condition) and Famine is reached on **36** rows — both drafted exemption blocks would red `exempt-but-reached` on their first gate | FOLDED ⟦G0-35⟧ + §14 **E48** — five exemptions DROPPED |
| V3 | COVERAGE | MAJOR | roster C4: `factions[].resources` has **no writer** — 0 of 1,323 factions | FOLDED ⟦G0-36⟧ — 5 `notInDenominator` rows + a WRWALKER handover; 15 phantom cells removed |
| V4 | COVERAGE | MAJOR | roster C3: `scars` does not exist and `magnitude on historicalEvents` is 0 of 3,049 — the real field is **`severity on historicalEvents`** with `catastrophic` (267 rows) outside `MAGNITUDE_BANDS` and `moderate` at 0 | FOLDED ⟦G0-36⟧⟦G0-41⟧ — recorded as `knownDefects.outOfVocabulary`, handed to the review's backlog as a FINITE-SEMANTICS finding |
| V5–V9 | COVERAGE | MAJOR/MINOR | C7 magic exports at `:468-471`, FOUR, each a `+absent` superset · C8 `CAPACITY_BANDS` already carries `'absent'` (6, not 5+1) · C9 the paths unions live at `isolationSupport.js:54`/`:56` · C2 the persisted key is `generationCoherenceReceipt` plus four unnamed sub-fields · C10/C11 `crossroads` 3 (at floor) and `Struggling` 36 | FOLDED ⟦G0-32⟧⟦G0-33⟧⟦G0-37⟧⟦G0-38⟧⟦G0-39⟧ |
| V10 | COVERAGE | MAJOR | the printed tuple — 66 fields · 329 cells · 146 ZERO · 8 under-floor · 175 at N≥3; manifest 67,779 B sha `c22d3fb2…`; `keyOf` 0 misses/525; carto 132/4/193 | FOLDED ⟦G0-42⟧ — **the register is minted from MEASUREMENT, never from the draft** |
| V11–V12 | COVERAGE | MAJOR | (f) **8,927 ms solo** — the 60 s switch does not fire · (e) Wealthy 0/42 default, 21/42 at 95, **0 in 300 seeds** | FOLDED ⟦G0-40⟧⟦G0-43⟧⟦G0-46⟧ + §14 E20/E22 |
| V13 | COVERAGE | MAJOR | §6.7 R2 under-priced ~2×: the genesis authored block is **76** findings (71 floored zeros + 5 under-floor), and **19 are cured outright by the carto corpus at 8.6 s**, not the ≈ 117 s J4 priced | FOLDED ⟦G0-45⟧⟦G0-46⟧ — a new fact for COV-4 |
| K1 | WORKER | MAJOR | **STOP S0.4 FIRED**: the `engine` chunk moves **+8 B** (13 → 14 export names; the new one `carryLockedRosterThroughGenerate`, forced static by topology T2), accounted 8 of 8 bytes; margin **236 → 228 B** | FOLDED ⟦G0-47⟧⟦G0-55⟧ — RULED ACCEPTED as the shape's own cost (§882.6); no ceiling raised |
| K2 | WORKER | MAJOR | closure **−6,366 B RAW** (entry −6,148, kernel −218), gzip −2,140, Brotli −1,624 — **S0.3 does not fire against either ceiling** | FOLDED ⟦G0-47⟧⟦G0-55⟧ — **Car 1 takes the FULL EXTRACTION shape**; the closure ends 6,704 B under 1,047,000 and `CLOSURE_BUDGET_BYTES` stays |
| K3 | WORKER | MAJOR | the worker bundle is **1,404,723 B raw / 452,384 gz / 355,591 br** and is exactly **ONE chunk** — Vite inlines a worker entry's dynamic imports | FOLDED ⟦G0-50⟧⟦G0-54⟧ — the count pin is **1**; S0.5 and the R13 counter-law are unreachable by construction |
| K4 | WORKER | MAJOR | the clone census is **529 rows**, not 527 (`REALM_SIZES` has three keys); `mapsSets=0`; `mutatesConfigInPlace=false` **but the resolved config differs from the caller's** | FOLDED ⟦G0-51⟧⟦G0-53⟧ + §14 E33 — analytics MUST read `resolvedConfig` |
| K5 | WORKER | MAJOR | the re-hash cascade is **370 of 484 shared stems**; only `pipeline-metadata` keeps its hash; Car 1's proof (9) as written cannot pass on a correct tree | FOLDED ⟦G0-48⟧⟦G0-56⟧ — the proof is RESTATED and the cascade QUOTED |
| K6 | WORKER | MAJOR | `__vite__mapDeps` **+3 entries / +125 B** (billed +1 / ~40 B), and **two EVICTIONS the bill does not name** — `anonGenCounter-*` and `densityCreateBoundary-*` become their own chunks | FOLDED ⟦G0-49⟧⟦G0-54⟧ |
| K7 | WORKER | MAJOR | the DEV timing: construct→ready **40.3 ms**, →first-step **41.5 ms** (12× inside E35's bound), →result **136.5 ms** (22×–73× under the signed reveal pacing) | FOLDED ⟦G0-57⟧ + §14 E35/E36 — the keep-alive follow-up is NOT owed; §9.7 R5/R6 closed |
| K8 | WORKER | MINOR | E45 stronger than priced: the why-string is absent from the entry, from all 8 closure files, and from **every** chunk in the build | FOLDED ⟦G0-58⟧ + §14 E45 — the fixture contingency does not fire |
| K9 | WORKER | MAJOR | **E37 NOT SETTLED** — the census extension is not in Car 0's act list and was not run | FOLDED ⟦G0-58⟧ + §14 E37 — **S3.1 stays ARMED and unpriced**; Car 3 runs it first or is dropped |
| K10 | WORKER | MINOR | the census instrument's own PLANT caught an early-return defect (it exited 1 naming nothing, because `DataCloneError` carries no path); `jsonLossless` is blind to Map/Set; `entropyRootCensus` was not run (Car 0 runs no vitest) | FOLDED ⟦G0-51⟧; the `entropyRootCensus` half is a **CAR CONSTRAINT** in §18 — Car 1 counts `generateSeed()` **call** sites with parens, since the lane body's shorthand is not a call |
| K11 | WORKER | MINOR | the golden test lives at `tests/property/generatorGoldenMaster.test.js` and `tests/property` is NOT one of the eight ENFORCER DIRS | **NO EDIT OWED** — the volume cites the correct path at the fold tip (checked); recorded as a Car 1 caution, since several receipt-era citations spell it `tests/generators/` |
| R1 | READERREVIEW | MAJOR | P0.1 YES — `renderToBuffer` runs OUTSIDE vitest, four variants, `canon_dossier` 347,870 B / **38 pages** vs a floor of 12; font re-registration needed and working — **but `vite-node` is absent from `package.json`, the lock and `node_modules`** | FOLDED ⟦G0-59⟧ + §14 E15 — the runner is `vite.createServer().ssrLoadModule()` (RATIFIED §882.8); the §5.5 fallback is NOT owed |
| R2 | READERREVIEW | MAJOR | P0.2 — eslint binds `scripts/**` weakly: 88 rules, only `no-console` off, **no `no-restricted-syntax`, no `max-lines`**; the determinism ban covers six layers, none of them `scripts/**` | FOLDED ⟦G0-62⟧ — a WRWALKER/READERREVIEW **handover row**; cured by the `--verify` bit, never by widening the ban; ⚠ neither block globs `.jsx` |
| R3 | READERREVIEW | **BLOCKING (method)** | P0.3 — "one lit MONTH per key" is measured INSUFFICIENT: `warMemoryEnabled` is silent at a month and reaches at a YEAR (102 new paths) | FOLDED ⟦G0-61⟧⟦G0-62⟧ + §14 E16 — the method is re-cut to **one lit YEAR minimum** |
| R4 | READERREVIEW | MAJOR | P0.3 — `neutralNeighbourEdges` is **not a ledger**; the reach lands in `regionalGraph.edges[].evidence[]`, and the flag/module spellings split US/UK | FOLDED ⟦G0-61⟧ — the detector is `evidence[].source === 'neutral_default'` |
| R5 | READERREVIEW | MAJOR | P0.3 — `characterDriftEnabled` is read by **no gate in `src/`** (five hits, not one a read) | FOLDED ⟦G0-60⟧ — `PREVIEW_OVERLAY` is the FOUR-key form (RULED §882.8) |
| R6 | READERREVIEW | **BLOCKING (design)** | P0.3 — `espionageEnabled` does not reach: a three-part conjunction needing `errandSpineEnabled`, and with the gate OPEN the belief maps read **268 lit = 268 dark over 30 years** | FOLDED ⟦G0-61⟧ (the overlay gains `errandSpineEnabled` — a chair act) + **CHAIR-ROW/OWNER: §19 RR-10**, the LIT-PREVIEW/OPERATIONS gap ⟦G0-73⟧ |
| R7 | READERREVIEW | MAJOR | P0.4 — one full 30-year campaign is **61.88 s**, not 14–19 min (peak RSS 787 MB; per-year cost FALLS) | FOLDED ⟦G0-63⟧⟦G0-71⟧⟦G0-72⟧ + §14 E19 — §5.7 R6 and §11.4 re-priced |
| R8 | READERREVIEW | MAJOR | P0.5 — the control hash is `sha256(JSON.stringify({worldState, regionalGraph, settlements}))` and the real soak at `--years 1` reproduces it exactly | FOLDED ⟦G0-64⟧⟦G0-71⟧ + §14 E17 — Car 1's equality proof is PRE-PAID; the second-hash remedy is unnecessary |
| R9 | READERREVIEW | MAJOR | P0.6 — exactly ONE new red, the billed lighting refreeze; the base red set is the FIVE banked ratchet rows (⚠ a bare `vitest run tests/lint` is not the gate) | FOLDED ⟦G0-69⟧ + §14 E18/E5 — §5.2's bill is complete as written |
| R10 | READERREVIEW | MAJOR | P0.7 — `files = 2497` MEASURED, the other four DERIVED: the walker fails fast on `files` and the refreeze REFUSES on a dirty tree | FOLDED ⟦G0-67⟧ — **a car constraint: Car 1 COMMITS its eight files first, then refreezes at the clean tip** (RULED §882.8) |
| R11 | READERREVIEW | MAJOR | P0.8 — **958‰** raw, but `depthHistogram = {0:3, 1:426, 2:0, 3:0, 4:0}`: the actor layer never resolves, so `fullyAddressed` is a THREE-part measure; and `MAX_ENTRIES = 240` **saturates at year 1** (429 minted, 240 kept) | FOLDED ⟦G0-65⟧⟦G0-66⟧ — the rubric must not quote the rate as the address-law score; **per-year capture is mandatory** (RULED §882.8) |
| R12 | READERREVIEW | MAJOR | P0.9 / E42 — all three tabs render, but as honest-absence shells (88–372 B); `PowerTab`'s default export is an OBJECT; `react`/`react-dom/server` must not go through `ssrLoadModule` | FOLDED ⟦G0-70⟧ + §14 E42 — the mechanism is proven, a real surface is Car 2's |

**REFUSED: none.** No probe finding was refused at its fold point. Two were folded with their scope narrowed rather than accepted whole, and both say so where they sit: W6 (proof B is re-specified, not deleted — the STOP set stays, it is simply not what makes the cohort dark) and K1 (S0.4's +8 B is ACCEPTED, not waved through — the edge is named, the margin is lowered, and no ceiling is raised).

⭐ **THE LAW THIS WAVE RE-TEACHES, banked in three places:** a register's roster, a car's bill and a probe's method are all **MINTED FROM MEASUREMENT, never transcribed from a design draft** — COVERAGE's eleven wrong rows, WORKER's four wrong byte cells and READERREVIEW's one-month method were each written as fact and each measured otherwise. A draft's "confirmed" row is a HYPOTHESIS until a probe prints it (§441 J7, now on the instrument side as well as the charter side).

---

## §18 THE INSTR CAR LIST AS RE-CUT — every car of HORIZON-INSTR, with its files, proofs, STOPs and predicted bill

**The absolutes at C′ = `9a0584f0f`** every bill below is predicted against (read them, never re-derive them): lighting **2495 / 370 / 2125 / 22437 / 6069** (files/parked/credited/titles/suiteTitles) · test ratchet **30731 / 2443** · OSR **1993 / 1409 / 388** · each-family ceiling **111** · `couplingInclusion` **28 / 179** · typecheck **173 / 1121**. Every "unchanged by G0" is a claim this fold makes deliberately, not a gap.

### §18.1 WRWALKER — Cars 1–3 (Car 0 discharged; Car 4 is the review phase's)

- **Car 1 — THE INSTRUMENT, THE REGISTER, THE GENESIS** (~1 day Opus). **Files:** `scripts/lib/writer-reach-scan.mjs`, `writer-dark-register.mjs`, `scripts/check-writer-reach.mjs`, `tests/lint/writerReach.walker.test.js`, `scripts/.writer-reach-baseline.json` (`--genesis --charter=§NNN`). **RE-CUT BY G0 in five places:** (1) the surface vocabulary is the SPLIT one — `web-display` counts, `web-transitive` is reported (⟦G0-2⟧), with the partition arm as a titled test; (2) the identity arm is built on a CANONICAL six-field legacy finding (⟦G0-1⟧, F1) — the shorthand throws; (3) the register carries **rows 3 and 6 ONLY** (⟦G0-13⟧); (4) the `beforeAll` budget is **`300_000`**, not `900_000` (⟦G0-15⟧); (5) the cohort is banked WHOLE and REPORTED by the `osr*`/data-valued RULE, so the reviewable number is **301** with `web` whole (⟦G0-5⟧). **Proofs:** the walker green at the committed tip, then `tests/lint` WHOLE with every red attributed; the digest identical twice and across processes; the `corpusMeta` cross-register equality; the `BUILTIN_MEMBERS` twin byte-exact against OSR's literal. **First act:** re-run the tally under the split — **prediction 547 LIT / 4,647 LIT-NAME / 1,326 DARK** (⟦G0-9⟧); a miss is investigated before the genesis. **STOPs:** dark > 60 % under the split; `judged < 3,000`; corpus + scan > 90 s (measured 8.8 s — ×10 of headroom). **Predicted bill:** lighting **files +1 · parked +0 · credited +1 · titles +≈ 43 · suiteTitles +5** over C′'s 2495/370/2125/22437/6069 → **2496 / 370 / 2126 / ≈ 22480 / 6074**; ratchet `--update` (+1 file, +≈ 43 tests) over 30731/2443; `mutationCoverageManifest` **+1 `rationale`** (`tests/lint` is an ENFORCER DIR and `*.walker` is a listed basename); OSR **1993/1409/388 unchanged** (a new `scripts/lib/*.mjs` cannot enrol — §14 E5, CONFIRMED by construction ⟦G0-19⟧); `couplingInclusion` **28/179 unchanged** (no `src/` byte); typecheck **173/1121 unchanged** (`scripts/` is outside `tsconfig.full.json`'s include — §14 E26); each-family **111 unchanged** (straight-line `it` only); `negativeAssertionAnchor` every `.not.` anchored on its own line; **zero `package.json` bytes; zero `src/` bytes.**
- **Car 2 — THE SURFACE-CLASS MAP, THE EXPORT ARM, THE CROSS-JOIN, THE HOOKS** (~half a day). **RE-CUT BY G0 in three places:** the CHARSET mirror pin asserts `CHARSET_SURFACES` = `SURFACE_CLASSES` minus `news` minus `web-transitive` (⟦G0-2⟧⟦G0-9⟧); **the culture cross-join arm is LIVE and exact — six rows, named** (⟦G0-12⟧, F4), beside the synthetic plant which stays as the discrimination proof; the web-only gap report is **26 identities under the split** (58 with `web` whole). **Proofs:** `--report` reproduces `surfaceReach` byte-for-byte; CHARSET's 16-row recon table cross-checked through `surfaceClassesOf`. **STOP:** `surfaceReach` JSON > 1 MB. **Predicted bill:** titles **+4**, no new file ⇒ lighting `files +0`; ratchet tests +4; **every other register unchanged by G0.**
- **Car 3 — THE LIT-DIAL ARM AND THE GENERATION-DIAL DOOR** (~half a day). **Unchanged by G0** except that rows 1–2 are confirmed as Car 3's (⟦G0-13⟧). **STOP:** `|dialGated| > 40`. **Predicted bill:** titles **+5**; `population` unchanged; lighting `files +0`; **all other absolutes unchanged.**

### §18.2 TUNEREG — cars 1–3 (car 4 optional and LAST; car 5 the owner-signed cull, HORIZON-DARK)

- **Car 1 — THE INVENTORY** (dark, byte-free). **Unchanged by G0** — no G0 probe measured a TUNEREG surface. **Files:** the lib, the CLI, `tests/lint/tuningRegister.walker.test.js` (arms A, B, C, F(30), G, H), `.tuning-inventory.json` genesis, `.tuning-register.json` scaffolded. **Proofs:** plants P-1…P-6 as titled; the hashed listing identical; wall-clock < 5 s solo. **STOP:** > 10 s → split P3. **Predicted bill:** lighting **files +1 · parked +0 · credited +1 · titles +≈ 42 · suiteTitles +8** → ≈ **2496 / 370 / 2126 / ≈ 22479 / 6077**; ratchet `--update`; `mutationCoverageManifest` **+1 rationale**; OSR **1993/1409/388 unchanged** (⚠ §14 E5's TUNEREG half is still OWED to this car's WHOLE `tests/lint` run — the WRWALKER half is CONFIRMED, the general claim is not); `couplingInclusion` 28/179 and typecheck 173/1121 **unchanged** (zero `src/` bytes).
- **Car 2 — DECLARATIONS** (dark). **Unchanged by G0.** Bill: titles only; ceilings banked DOWN; no new file.
- **Car 3 — THE SIGNING DOOR REHEARSAL + THE DESK SHEET** (dark). **Unchanged by G0.** ⚠ One G0-adjacent caution the fold carries in: `docs/tuning-signatures/README.md` must satisfy `tests/docs/enforcement-claims.test.js` — and READERREVIEW's P0.6 proved `tests/docs/*` scanners read root and `docs/**` markdown but NOT `docs/review/` (⟦G0-69⟧), so the doc-scanner surface is exactly as §3.5 assumed. Bill: `docs/` only; **lighting titles +≈ 12, files +0.**

### §18.3 SOAKCHAIN — Cars 0–4

- **Car 0 — THE INVARIANTS MODULE + THE MEASUREMENT.** **Unchanged by G0** in shape, and G0 hands it one usable figure: READERREVIEW P0.4 measured a 30-year 4-settlement `full_simulation` campaign at **61.88 s wall / 787 MB peak RSS with per-year cost FALLING 2,673 → 1,851 ms** (⟦G0-63⟧) — an ADVANCE-only figure on the reader runner's composer, **not** the soak's own per-year observation legs, so it does not discharge (a) but it bounds it from below and makes S0-a's "does 100 years × 2.1 fit 360 s?" answerable before the fixture is cut. **Predicted bill:** 1 new test file (`tests/soak-harness/soakInvariants.test.js`) ⇒ lighting **files +1 · titles +≈ 14 · suiteTitles +2**; ratchet `--update`; no manifest row (`tests/soak-harness` is not an enforcer dir); **OSR / couplingInclusion / typecheck / each-family unchanged.**
- **Car 1 — THE WATCHDOG'S FLOOR + THE REGISTRY ROW.** **Unchanged by G0.** Bill: **0 new test files**; the tripwire pin edit; the certification `sourceFingerprint` moves. STOP S1-a: any yearly hash differs.
- **Car 2 — THE CALLER.** **Unchanged by G0.** Bill: 1 new test file (`tripwireCaller.test.js`); `ci.yml` +4 lines; lighting **files +1**.
- **Car 3 — TIER 1, THE CENTURY LEG.** **Unchanged by G0 in shape; its CI arithmetic is re-priced twice by G0** — WRWALKER's second corpus build is **8.3 s solo / ≈ 26.5 s at ×8**, not the believed ~264 s (⟦G0-15⟧), and COVERAGE's census is **8.93 s solo** (⟦G0-40⟧), so the ⟦A23 E7⟧ cumulative pressure on the ONE 30-minute `check-tests` job is **materially lower than the volume priced** and the seed count may be set less conservatively. Bill: 1 new test file (`tests/simulation/centuryLegSoak.test.js`, **+1/+0/+1/+9/+1**); anchors at EXACT zero (`tests/simulation` is a generation-facing root); `check-tests` wall-clock **+≤ 6 min worst case**. STOPs S3-a…S3-d unchanged.
- **Car 4 — TIER 2, THE REGISTER AND THE SCHEDULE.** **Unchanged by G0.** Bill: the register genesis (UNFROZEN, two cells), `soakRegister.test.js`, the profiles, the workflow; lighting **files +1**; the `tests/ops` pin.

### §18.4 The three consist-level facts G0 changes for EVERY car of the train

1. **The gate wall-clock is smaller than the volume believed.** §11.4's headline moves from ≈ +13–14 min CPU to **≈ +9–10 min** (⟦G0-15⟧), with COVERAGE's leg CONFIRMED at 8.93 s solo (⟦G0-40⟧) and WRWALKER's at 8.8 s corpus + scan. The CI-budget question (E41) survives, but it is now the only live wall-clock risk in the train.
2. **A car that mints a generated artifact under `src/` owes the three-step provenance ritual** — commit → `--write` → commit (⟦G0-4⟧, CHARSET C2). No INSTR car mints one; every DARK-era car that does (CHARSET Cars 1–3) pays it, and §1.13 names it.
3. **No INSTR car adds a `src/` byte,** so `couplingInclusion` **28/179**, typecheck **173/1121**, the engine ceiling **675,764 / 676,000** and the first-paint closure **1,046,662 / 1,047,000** are untouched by the whole train **by construction** — the byte movement of the arc lives entirely in HORIZON-DARK (WORKER's −6,366 / +8, CHARSET's two generated leaves, CAPACITY's leaf).

---

## §19 CHAIR ROWS — the decisions this fold hands up, each with its recommended answer and its veto shape

**Chair rows (vetoable; the fold decided them within its delegation and records them so they can be overturned).**

| # | the decision | recommended answer | veto shape |
|---|---|---|---|
| CR-8 | COVERAGE Car 1's roster is rebuilt from Car 0's printed tuple, and the eleven drafted rows are corrected at cause rather than exempted | **REBUILD** — the register is minted from measurement (⟦G0-39⟧) | "keep the draft roster and exempt the zeros" → a walker born red on eleven false claims, and 24 phantom cells in the denominator forever |
| CR-9 | `status on activeChains` reads through `canonicalSupplyChainStatus` so the census counts customer-visible bands; the legacy persisted vocabulary becomes a WRWALKER/OSR handover row | **READ-TIME MAPPING** (⟦G0-34⟧, §882.5) | "keep it `persisted` with the LEGACY list" → the census then counts a vocabulary no customer sees, and the handover is lost |
| CR-10 | `severity on historicalEvents`'s `catastrophic` (267 rows) is recorded as `knownDefects.outOfVocabulary` and handed to the review's backlog, never silently added to `MAGNITUDE_BANDS` | **RECORD, DO NOT WIDEN** (⟦G0-41⟧) | "widen the catalog" → a FINITE-SEMANTICS breach performed by an instrument whose whole purpose is to refuse them |
| CR-11 | WORKER's S0.4 `engine` +8 B is ACCEPTED as the shape's own cost; the margin is recorded at 228 B and no ceiling is raised | **ACCEPT** (⟦G0-47⟧, §882.6) | "drop the extraction shape over 8 bytes" → forgoes −6,366 B of first paint to protect 8 |
| CR-12 | READERREVIEW's runners drive `vite.createServer().ssrLoadModule()`, not `npx vite-node` | **IN-LOCK RUNNER** (⟦G0-59⟧, §882.8) | "keep `vite-node`" → an unpinned network install on CI and a fresh clone, or a `package.json` byte change and a §349.2 mint |
| CR-13 | the reach method for every `PREVIEW_OVERLAY` key is **one lit YEAR minimum**, and `PREVIEW_OVERLAY` gains `errandSpineEnabled` | **YEAR + THE CONJUNCTION MEMBER** (⟦G0-61⟧⟦G0-62⟧) | "keep one month" → war memory reports as unreachable and the design chases a phantom |
| CR-14 | READERREVIEW Car 1 COMMITS its eight files, then refreezes the lighting census at the clean tip, then re-runs the walker plainly — three steps | **THE THREE-STEP ORDER** (⟦G0-67⟧, §882.8) | "refreeze from the working tree" → impossible; the refreeze refuses on a dirty tree by construction |
| CR-15 | Car 3's rubric scores the address law by `depthHistogram[2..4]`, not by `fullyAddressedRateMilli` | **THE HISTOGRAM** (⟦G0-65⟧) — the honest live figure is 0 of 429 | "quote 958‰" → the rubric reports a three-part measure as the four-part law and the actor gap stays invisible |
| CR-16 | §14 gains E47 and E48 — two claims the volume asserted and never ledgered, entered and refuted in one act | **ENTER THEM** (⟦G0-34⟧⟦G0-35⟧) | "leave them out" → the ledger keeps claiming completeness it does not have |

**⛔ OWNER ROWS — opened, not decided. Each is a class the chair may not settle alone.**

| # | the row | why it is the owner's | the evidence in hand |
|---|---|---|---|
| **WK-11** | **Bank WORKER's −6,366 B by LOWERING `CLOSURE_BUDGET_BYTES`, or keep it as margin?** The probe leaves first paint **6,704 B** under the unraised 1,047,000 (was 338 B) and the §880.8 raise to 1,048,000 stays banked and unspent | a BUDGET-class act on a signed ceiling (§882.6 named it and left it alone); the ratchet philosophy says bank a win, and the lighting wave's own cars (L-MAT, L-HOMES) may need the headroom | ⟦G0-55⟧; WORKER Car 0 §9.5(b); the chair's standing recommendation is **NOT before the lighting wave**, revisited at the tuning sitting |
| **RR-10** | **The LIT-PREVIEW corpus cannot make OPERATIONS readable.** With the full espionage conjunction lit and `espionageActive === true`, the layer writes ZERO records over thirty years in a four-settlement realm — so Q-OPS-1/Q-OPS-2 score `dark_by_flag` on the PREVIEW posture as well as on LAUNCH, defeating the preview corpus's stated purpose. **(a)** accept it and record OPERATIONS as unreadable-at-launch with this receipt as its evidence; **(b)** widen the preview realm or horizon until an accepted peace occurs (unbounded — 30 years already failed); **(c)** the preview corpus SEEDS a covert mission directly, which is a fixture, not a lighting overlay | it touches RR-8's charter, the §12 owner rows and the LIGHTING WAVE's R9(d) funding question; "accept unreadable" is a product judgment and "seed a fixture" changes what the review's evidence IS | ⟦G0-61⟧⟦G0-73⟧; READERREVIEW Car 0 §6 (`beliefMaps` 268 = 268 at 30 y, observers 4 = 4, zero espionage-named paths); the estate's own certification row still reads UNOBSERVED at C′. **The chair's minimum act is already folded and is not the owner's: `PREVIEW_OVERLAY` gains `errandSpineEnabled` or its espionage member is dead on arrival** |
| **(carried)** | the T13 closure-ceiling banking question of §882.6 is the same object as WK-11 and is answered once, not twice | — | — |

**Owed, and named so it is not re-found:** §14 **E37** is the one row a G0 probe was chartered for and did not settle — the regen clone census extension was not in WORKER Car 0's act list. **S3.1 stays ARMED and unpriced**; WORKER Car 3 either runs the extension before it is cut or is dropped, and either way the choice is recorded, not inherited silently.

---

*G0-FOLD lane, ⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ (Opus 5 seat, read-only validator/architect), session `58f0a8e2`, 2026-09-02, third dispatch. Dispatches 1 and 2 died at the session limit (~05:40, at ⟦G0 PARTIAL: 19⟧ by its last checkpoint but 31 by the volume's own markers) and at the Fable model limit (~09:1x); this one resumed from the FILE, trusting the markers over `fold.json`, and continued at ⟦G0-32⟧. Read-only on every tree: no vitest, no build, no npm script, no git write, no sub-agents; the ONE file written is this volume, by targeted in-place edits (`$ME/g0-fold/fold-batch3.py` … `fold-batch7.py`, every anchor asserted to match exactly once, each batch atomic, the header checkpointed after every batch). Sources read whole: the five Car 0 receipts (`$SP/laneG0WRW-receipt.md`, `laneG0CHARSET-receipt.md`, `laneG0COV-receipt.md`, `laneG0WORKER-receipt.md`, `laneG0RR-receipt.md`) and the chair's rulings at `docs/OWNER_DECISION_QUEUE.md` §882.3–§882.8. The pre-fold volume is preserved at `$ME/g0-fold/DESIGN_HORIZON.pre-g0.bak`; `$ME/g0-fold/fold.json` carries the machine-readable disposition.*
'''

f.rep(
    "\n---\n\n*FOLD lane (Fable 5.1 seat, architect), session `4fb807d8`",
    APPEND + "\n---\n\n*FOLD lane (Fable 5.1 seat, architect), session `4fb807d8`",
)

f.checkpoint(
    93, 93,
    flip='⟦G0 FOLDED 2026-09-02 ' + TS + ' ET · OPUS-AUTHORED — Fable retrovalidation OWED: the INSTR cars are cut; WRWALKER Car 1 on web-display⟧',
)
f.save('batch7 §17/§18/§19 + FLIP')
foldlib.fold_json(
    folded=['G0-93'],
    chair=[
        'CR-8 COVERAGE Car 1 roster rebuilt from the printed tuple (fold JUDGMENT)',
        'CR-9 chain status read through canonicalSupplyChainStatus (§882.5)',
        'CR-10 catastrophic recorded as knownDefects, never a catalog widening (§882.5)',
        'CR-11 WORKER engine +8 B ACCEPTED, margin 228, no raise (§882.6)',
        'CR-12 the reader runners use vite.ssrLoadModule, not npx vite-node (§882.8)',
        'CR-13 reach method = one lit YEAR; PREVIEW_OVERLAY gains errandSpineEnabled (fold JUDGMENT + §882.8)',
        'CR-14 READERREVIEW Car 1 commits, then refreezes at the clean tip (§882.8)',
        'CR-15 the address law is scored by depthHistogram[2..4], not fullyAddressedRateMilli (fold JUDGMENT)',
        'CR-16 §14 gains E47 + E48, two unledgered claims entered and refuted (fold JUDGMENT)',
        'OWNER WK-11 bank the -6,366 B by lowering CLOSURE_BUDGET_BYTES, or keep the margin (opened, not decided)',
        'OWNER RR-10 the LIT-PREVIEW corpus cannot make OPERATIONS readable — three options (opened, not decided)',
    ],
    verdict='FOLDED',
)
