# PREDICTION — the §891 train's seventh register act: `check-writer-reach.mjs --write` at 4233031ba
Written 2026-09-03 ~19:0x ET by the Fable 5.1 chair BEFORE the reproduction run. The previous chair ran the door at 18:26 with no prediction on record; this file is the prediction for the chair's own reproduction, which is taken WITHOUT touching the dock (the tool's `run()` is driven with a `readBaseline` override = the COMMITTED blob at 4233031ba and a `writeBaseline` override into this scratchpad).

## The mechanism (derived independently, from code, before this run)
- `scripts/lib/legacy-reader-shape-scan.mjs` `makeResolver`: a PARAMETER binding is resolved from its CALL SITES (`idx.callSites`); a JSX element is not a CallExpression, so a React component's destructured prop is UNGROUNDED; an ungrounded receiver's property access binds only if the key has a SINGLE HOME in the corpus.
- DESK CAR 1 (`a59e66e5a`) moved `eco.situationDesc` from EconomicsTab.jsx (where `eco = economicState || s?.economicState` grounds through the single-home key `economicState`) into EconomicsGlance.jsx (where `eco` is a bare JSX prop). `situationDesc` is not single-home. ⇒ the only web-display R read of `situationDesc on economicState` vanished; its dossier-pdf and web-transitive R reads remain. The identity stays LIT.
- EconomicsTab.jsx gained `String(s?._seed ?? s?.id ?? '')`; `s` grounds as the `settlement` root. ⇒ `_seed on settlement` gains a web-display R read.

## The figures (CERTAIN where derivable; the rest read off the 18:26 output and re-measured here)
- frozenAtSha `355ff603b…` → `4233031ba0d71216f652d1fa189a25ac2f429ba9` (CERTAIN: headSha of the dock).
- cohort `darkUnregistered`: UNCHANGED, same length and same members (CERTAIN: the 17-line diff touched no cohort row); `0 stale folded out`; `0 registered rows now lit`.
- surfaceReach: EXACTLY TWO identities move — `situationDesc on economicState` `dossier-pdf=R web-display=R web-transitive=R` → `dossier-pdf=R web-display=N web-transitive=R`; `_seed on settlement` `web-display=N` → `web-display=R` (other classes unchanged). Every other surfaceReach entry byte-identical.
- closureSizes: web-display 679→685 · web-transitive 883→887 · dossier-pdf 369→373 · campaign-pdf 73→74 · world-book 79→80 · foundry 317→321 · json-export 8→8 · news 28→28 (the probe at 19:0x already measured 685/887/373/74/80/321/8/28 — CONFIRMED before the write).
- scanStats: files 1563→1573 · sites 37419→37517 · propRead 35403→35494 · destructure 2005→2012 · rGrades 5633→5649 · nGrades 868513→869813 · resolvedReceivers 6406→6423.
- verdictDigest moves; shapesDigest, detectorDigest, registerDigest, pendingSurfaceCeiling, minRows, corpusMeta UNCHANGED.
- THE ACT'S OWN CLAIM: my reproduction's serialised output is BYTE-IDENTICAL to the previous chair's 18:26 file (`$SC/writer-reach-dirty-1826.json`). If it is not, that is a determinism finding about the instrument, not contention.

## What this act is, honestly
A SHRINK/ABSORB `--write` (no growth door needed). ⚠ The handoff card's sentence "DESK CAR 1 made situationDesc REACH the web display; the frozen register still says dark" is INVERTED: the frozen register says R, the live measurement says N, and the product DOES still render the string (EconomicsGlance.jsx line `{eco.situationDesc&&<p …>}` inside the web-display closure — probe-confirmed). The recorded N is a SCANNER-GROUNDING fact, not a display fact, and it is how the register already grades every prop-bound read in the estate (resolvedReceivers 6,423 of 37,517 sites). The alternative — a cure car re-routing the read through a groundable receiver — would sit AFTER six register acts and would move the OSR and prose-numerics registers (8 and 24 rows keyed on these two files), re-opening two taken acts; refused as the worse trade. DOCKETED instead: every desk-extraction car will darken web-display grades this way; the lighting wave's reader of this register must know the grade's meaning.
