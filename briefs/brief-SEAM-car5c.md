# Brief — SEAM train, car 5c: THE FOLD'S CURES — the provenance detector narrowed, the driver's options passed, two pins, two reserved keys, the headline counts pinned, the capture standing, and the receipt corrected (chair, 2026-09-08 19:0x; SITTING §R)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 5c (and the corrections 7–15 applied IN PLACE to the earlier sections, each marked `[corrected at 5c: …]`). Dock: `$SC/laneSEAM` at `b573bb5f4`. Read first, whole: `$SC/skeptic-seam/FOLD.md` (every section), SITTING §R (the rulings — they bind you), the three lens files (`skeptic-seam/seam.md`, `schema.md`, `arms-fences.md`) for the figures and the exact lines.

## The code cures, in this order (one commit each unless a ritual forces a split)
1. **Cure 1(i) — `src/domain/prose/moveGrammar.js:203-212`, the PROVENANCE detector narrowed to the twelve holder kinds** (SITTING §R c-16): drop the generic reporting-verb limb (`the (rolls|registers?|ledgers?|books?|records?) (say|…|has)`), drop `customs` and `tithe`, add `census`/`office`/`tradition`; the shipped docblock now TRUE. Arm: `tests/lint/proseMoveGrammar.walker.test.js` prints and asserts the split as integers — kind-only · generic-only · both — beside the total (expected 7 · 0 · 0 after narrowing; the fold's at-first reading was 7 · 11 · 0 of 18); U5 folded in: a scratch copy with the PROVENANCE row moved to last in `CLAUSE_DETECTORS`, `classifyMoves` re-run over the six leaves, the two outputs diffed (assert identical, or print the difference and STOP). Then the wiring census re-taken — a PRE-RULED row move: the 15 rows the generic limb alone moved return to their earlier `grammars`, THIN/COVERED follow; print the diff line count, the rows moved by name, and `git diff -U0 | grep -v grammars | grep -v count | grep -v THIN | grep -v COVERED | grep -v producerIndexFiles` (must be empty). Commit the JSON with the change.
2. **Cure 2 — `src/domain/prose/composedWalker.js:1010`, `reWalkBlock` passes its `options` through** (or refuses a block carrying a contrast shape with no `siblingKeys`). Arm: a block re-walk fixture whose units carry a contrast shape asserts A3 does NOT FAIL; a plant removing the pass-through reds by name.
3. **Cure 3 — `docs/content/prose-shift-register.json`: pin the BAND RULE** — `comparator-and-band-rule` gains a `source` pin over `bandOf` itself (`composeStateProse.js:454-458`: `departureBit + tension + change`) or a digest over the three signals; the register contract's recompute loop covers it, drift `[]`.
4. **Cure 4 — the register gives `seat` a recomputable pin**: `{kind:'integer', over:'pools carrying a seat key', value:0}`; the same loop.
5. **Cure 5 — `seatReason` / `seatRow` named RESERVED** in the register beside `seat` (and the composer's typedef says so); the projection contract's stray-key arm reads the reserved list.
6. **Cure 6 — `tests/lint/proseWiringCensus.walker.test.js` pins the holder census headline counts** as integers (rows 114 / 3 / 591; fields 191 / 5 / 415 — the DS-DEF-2 idiom applied to the totals). NOTE: cure 7 below moves the `standing` column and may move nothing in these counts (LICENSED/OFFICE/UNRESOLVED are source counts, not standing counts) — pin the figures at YOUR tip after cure 7 and say which they are.
7. **Cure 7 — THE CAPTURE STANDING (SITTING §R c-22):** `holderTable.js`/`wiringCensus.js` — the `source.standing` of the STATE'S OWN ORGANS (kinds `office`, `court`, `treasury`, `watch`) reads `powerStructure.criminalCaptureState` (`rulingStructure.js:797`; values none · adversarial · equilibrium · corrupted) and the faction `captureState` where present, and marks the holder INTERESTED when either is not `none`; every other kind (muster · parish · market · elders · road · toll-bar) keeps its own books and is NOT marked by the settlement-wide fact. The interested-fact figures per tier on the RATE corpus are re-measured under this rule and printed per tier and per kind (expected non-zero on the towns whose criminal capture state is not `none`, on the four kinds only); the standing vocabulary gains `INTERESTED` as a real value (A13's FAIL limb becomes reachable on real input — assert it with a real town, not a synthetic). This is a PRE-RULED row move (the `standing` column changes on the affected rows) — commit the re-taken JSON with the change, print the diff bounded as in cure 1.
8. **The receipt corrections 7–15** (FOLD §3(b)): apply each in place in `$SC/receipt-seam.md`, marked; the `church` line (6 citations / 3 files); the units (UTF-16 vs bytes, both instruments named); the consist span 24 · 57; the walk cost unpinned; `drawFace`'s keys named; steps 6-then-5 and the missing RESOLVED filter recorded as declared-and-harmless in the composer's docblock (a docblock edit, no behaviour); the STATE-KEY arm's floors made exact and its cannot-fail line removed (P5); P8 and P9 wording; the `fillSites` gap withdrawn.

## Discipline, fences, commits — as car 5b's, plus
The OSR: cures 1–7 regenerate no leaf; the OSR must read exact with no write (if a cure touches a scanned module and the dry read names it as a scanned-path delta with zero findings, print it and leave the door to the chair's §916 registers step — c-17). The lighting census by ritual on a clean tip. The runner count in its own shell. Commits `SEAM car 5c-1: the provenance detector narrowed to the holder kinds — 18 → N citing variants, the census re-taken` … `5c-8: the receipt corrected at the fold's fifteen lines`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the shas; the provenance split after narrowing and the census rows that moved back; the interested-fact figures per tier and per kind under the capture standing; A13's verdicts on a real interested town; the two pins and the reserved keys asserted; the headline counts pinned at your tip; the whole `tests/lint`, strict, typecheck, OSR figures at your tip; anything refused with its measurement. Porcelain 0 and runners 0 when you return.
