# LANE: L-UI-MAT — the small flips: three UI flags + the materialization dial (lighting POSITION 4)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · two cars, one gate · display-only shift (L-UI) + a persisted-shape act (L-MAT, O-11 SIGNED) · boards after L-DEFAULT⟧

READ FIRST: `$SC/briefs/_PREAMBLE.md` → PLAN.md §6 POSITION 4 (sealed `refs/preserve/light-plan-2026-09-05`) → the three
flags' promotion contracts (`warEconomySurfacing`, `handbookVoice`, `mobileSingleChrome` — all `false` today; O-13 ruled
them satisfied) → the MAT dial (`NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION = DEFAULT_LIVING_CONTENT_LAW_VERSION`, dormant)
and L-HOMES CAR 8's create boundary → §882.13's O-11 SIGN (the three persistence paths: publicSafe allowlist, account
import, and the third — re-derive from the row).

## CAR A — L-UI: three boolean flips
Flip the three product flags; re-record the `dossierFiveTabs`-class snapshots as DECLARED display shifts (list every
snapshot file and the cause). Same-seed shift: DISPLAY ONLY — prove the engine hash is unmoved (one fixed-seed run).

## CAR B — L-MAT: the dial + CAR 8's boundary + the THREE persistence paths in the SAME car, never optional
Turn on custom-content materialization for NEW worlds only; ⛔ STOP if `_livingContentLawVersion` is ever stamped on an
EXISTING world on regen (THE PROMISE). The three persistence paths (O-11 SIGNED) land with it or the car does not land.
`P12-PRELOAD` is MOOT (§889.3: residual 0) — do not build it; re-measure the engine chunk only if the chair asks.

## RESIDUES (named, not built here)
`L-DOORS (i)` charset `report → refuse` waits on HORIZON-B1 landing; `L-DOORS (ii)` `generationWorker` waits on WORKER
Car 1 (0 hits — nothing to flip). Both stay dark by name in the declaration.

## FENCES
No register acts; no new test file unless a snapshot family requires one (name it); commit per car; trailers
`Seat: Opus 5 — Fable-unvalidated` + `Lane: L-UI-MAT`; quiet-window law + mutex; receipt `$SC/receipt-l-ui-mat.md`.
