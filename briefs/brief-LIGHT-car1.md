# Brief — the LIGHT train, car 1: THE DIAL LIT AND LANDED — the loader's caller, the dial at 2, no migration, THE PROMISE re-proven, the three holes' arms at dial 2 (owner 2026-09-08 ~13:5x: "feel free to land everything lit on" — "Retroactively as well")

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-light.md`. Dock: `$SC/laneLIGHT`, cut at the §916 CAS `8961388ce` (`sh $SC/mkdock.sh laneLIGHT 8961388ce`) — this lane runs BESIDE the chair's taste + sitting and touches NOTHING under `src/domain/display/stateProse/`, `src/domain/prose/`, `src/data/dossierStateProse/` or the census. Read first, whole: `src/domain/content/livingContentLaw.js` (the four laws in its header: no migration deliberately — AMENDED by the owner; the dial is one line `:94`; materialization is not adoption), `livingContentLawVersion.js` (`materializesLivingContent`, the gate), `livingContentSeam.js` (`loadLivingContentRoster :92` — NO CALLER in src; `livingContentRosterFor` throws on a lit config without the builder), `livingContentRoster.js` header, `src/generators/generateSettlementPipeline.js:170-190` (the one call), `src/domain/density/densityCreateBoundary.js:360-390` (the two dial rows; `birthConfig` spreads the mint; DEF-2's clamp), the memory hazard verbatim in `$SC/briefs/LIGHT-HAZARD.md`, and the L-MAT-FIX receipt `$SC/receipt-l-mat.md` (cars 7–9: the three import-boundary holes — gallery import ingest, the reconciliation path, undo/versionHistory — and their arms).

## What to build, in this order (one commit each)
1. **The loader's caller.** `loadLivingContentRoster()` is awaited on the generation path BEFORE `generateSettlementPipeline` can reach `livingContentRosterFor` on a lit config — at the create boundary's async edge (name it; the dynamic `import(` form of the seam is load-bearing and stays), never in a React render body, never eagerly at module load (the roster closure must stay out of `engine-core`: prove with the two vite derivations' regexes and a chunk-membership read, no build — the byte ratchet's dry read only). Arm: a lit config with the builder unregistered used to throw `[livingContentSeam] v2 world, roster payload not loaded` — the plant that removes the caller must red; the DM-full arm's failure message updated to say the caller exists.
2. **The dial to 2** — `NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION = ROSTER_LIVING_CONTENT_LAW_VERSION` (one line), `newSettlementLivingContentLaw()` now mints `_livingContentLawVersion: 2` on every classified BIRTH and on no PREVIEW or re-derivation (the boundary's existing arms; re-run them).
3. **THE PROMISE re-proven, by bytes.** For the golden 525 configurations and the RATE 768 towns: generate each seed under law 1 and under law 2; assert EVERY field byte-identical except `settlement.customContentRoster` (absent under 1; a frozen record under 2, EMPTY when the run's reviewed environment holds no living-content definition). One other byte moving anywhere is a STOP — report, do not cure. Then the lifecycle paths with the dial at 2: create · read · same-seed regenerate · undo · persist/load · gallery import · reconciliation · versionHistory — each asserted byte-equal to itself across the path (the estate's existing lifecycle arms, run lit).
4. **NO MIGRATION — the owner's second ruling (2026-09-08 ~14:0x: "There are no true launched settlements or campaigns … All of those that exist were tests in which case inconsequential").** The module's law "THERE IS NO MIGRATION, DELIBERATELY" STANDS unchanged: a world born under law 1 stays under law 1 forever (dark, working, its record absent), a world born after the dial is lit under law 2, and nothing stamps a persisted config on any path. Do NOT write a migration, a read-path stamp, or a provenance line for one. Arm: a persisted law-1 world loaded, regenerated and undone on the lit build keeps `_livingContentLawVersion` absent and `customContentRoster` absent on every path (the PROMISE's own lifecycle arms, run lit). The header paragraph of `livingContentLaw.js` gains ONE dated sentence: the dial was lit on the owner's word with no migration because every pre-lighting world was test data. Every reader of `customContentRoster` treats absent as empty (assert with a plant that makes one reader throw on undefined).
5. **The three holes' arms at dial 2** — every arm from L-MAT-FIX cars 7–9 re-run with the dial lit (they were green at dial 1 for the wrong reason: the loader outage); DEF-2's clamp asserted: an imported config carrying the marker never overrides the birth's law; the security drop arms (`tests/security/livingContentRosterPublicDrop.test.js`) green lit; `writerReach` sees the new caller (the register door predicted).
6. **The lit walkers:** `mechanismLitCoverage` (worldPulse-scoped today — say what it does not see), the reference pack driven through a lit world once (the roster non-empty on that fixture; anti-vacuity), the DM-full arm on a lit world.

## Refusals you must make with the measurement
Any byte other than the roster moving under law 2 (step 3) · any eager edge that pulls the roster closure into first paint · a stamp on export or preview · a reader that consults the roster for a MECHANIC (adoption is not lit; the parity train reads it for TEXT only, later).

## Discipline, fences, commits — as SEAM's
Runner count in its own shell before every vitest; one focused file at a time; the whole `tests/lint` once at the tip; strict on its ceiling; voice E2; OSR/PN exact; typecheck; the cast walkers; eslint 0; no em dash / bang / toFixed in `src/` literals; no register `--write` but the lighting census ritual and the wiring census count-only re-take; never a build; explicit staging. Commits `LIGHT car 1a: the roster loader gains its caller …` / `1b: the dial lit — …` / `1c: THE PROMISE under law 2 — byte-identical but the roster on 525 + 768` / `1d: no migration — a law-1 world stays law-1 on every path, lit` / `1e: the three holes' arms at dial 2` / `1f: the lit walkers`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: LIGHT` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the shas; the byte-identity figures (525 · 768; the one excepted field); the lifecycle arms lit; the no-migration arm; the holes' arms; the eager-closure proof; anything refused with its measurement.
