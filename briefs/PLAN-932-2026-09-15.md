# PLAN §932 — THE DECLARED-SHIFT LANDING AND WAVE 4's FIRST LANES (drafted 2026-09-15 14:5x EDT by the Fable 5.1 chair, session f6ac0d98, under the owner's 14:4x grant)

The NEW build slot = `claude/composite-r4` after §931's CAS (the landing seal `landing-longtail-2026-09-15`). Every dock below is cut from it by `mkdock.sh <name> <slot-sha>`; the docks LT38 and LT37 stay cut at `f73bdbf16` and are cherry-picked by `compose.sh` + the Seat/Lane filter (`c931-f6ac0d98/seat-msg-filter.py`) — never rebased.

## LANE A0 — THE WORKER-HEADROOM CAR (`lane-932-headroom`, Opus; FIRST, because every generator car in this plan needs it): the generation worker bundle measures EXACTLY its ceiling 1,404,493 B (monotone-down, never raised). Buy ≥ 20 KB of headroom with ZERO output bytes moved: proven-unreachable code removed under the walkers, or a data table moved out of the worker chunk into a lazy import the pipeline awaits — measured by `npm run build && npm run verify:dist` under the mutex, the composed-instrument lines identical, the golden red SET identical; then LOWER `WORKER_BUNDLE_CEILING_BYTES` to the new measurement in the same car. LT17 car 2 (`b743a9cf2` on the sealed 67-car tip; the dock `lane-LT17-hygiene` `5da5fa8c9`) is cherry-picked ON TOP of the headroom car and measured again.

## LANE A — THE DECLARED-SHIFT LANE (`lane-932-declared`, one Opus implementer; output-moving by design, ONE priced window under the §764.3 amendment taken by delegation — §931.6b)
1. LT41b car 6 + 6b CHERRY-PICKED from the seal `lt41b-rulings-2026-09-15` (`a76fe8cdf`, `919c37da5`); the fence constant `PRE_COUPLING_CORPUS_SHA` in `tests/property/espionageDormancyFence.test.js` RE-RECORDED with the mover named in the golden header the way the last two windows did (read the file's header first); the prose-manifest cells carrying a stability label re-recorded by `node scripts/prose-manifest-cells.mjs --record` (the moved rows listed by pool key in the body); the aiGrounding bundle rebuilt (its input `simulationSpine.js` moves) by the clone → build → relink recipe.
2. LT17's five held output-moving cars (`brief-LT17-catalogue-hygiene.md`; the strings are in the lane's report) — implemented under the claim freeze, each priced by name.
3. #37 car 5's cure — ⚠ LT37 MEASURED the DailyLife food-deficit divergence NOT LIVE (band word identical 360/360; a double-rounding spread only) — so the cure is REFUSED on the measurement, recorded, not implemented; the held patch file stays as the record.
4. Every car: the declared-scope proof (the golden corpus dumped at base and tip by `c931-f6ac0d98/declared/__chair_dump.test.js`, every differing line classified by an extended `classify.py`; zero undeclared), `tests/property` + `tests/edgeFunctions` + `tests/security` + `tests/copy` + `tests/lint` + `tests/generators` whole, `check:quick`, the Seat/Lane trailer.
Gate: the fence green at the tip with its new constant; the golden master stays 525/525 red (the owner's GENESIS re-record); `GOLDEN_SHIFT_LEDGER.md` gains ONE entry naming every mover.

## LANE B — #34's FREE REMAINDER (`lane-LT34-remainder`, Opus; ≈12–14 cars, all dark or display; `PROGRAMME-32-34-2026-09-15.md` §C.3 lane 2 and §E items 3–7): the two W-OPS wiring cars · ENC-5's grudge fold · the drift-door car under TE-VIRT-1 (`characterDriftEnabled` manifest home taken by the chair, vetoable) · W-COIN-4 · WEAVE stragglers SEAM-4 · NAME-2 (behind `markovNamingEnabled`) · POLIS-5 · ST-1 (VAR-1b/VAR-2 after a classification note; VAR-1b is a declared shift → LANE A's window, not this lane) · SEAT-8 behind `irregularForceEnabled` (the `civilContests` shape GRANTED at §931.6b).

## LANE C — THE RECORD FOLD (docs only, on the LEDGER branch by `chair-commit.sh`; the chair's own act): the three counts into START_HERE §3j; the brief's #32 family list ("logistics" → SPINE); §148.2's 22 → 28; the WC-15 receipt; `subsystemRowsEncounters.js:54`'s stale prose is CODE (→ lane B); the slot copy of `DESIGN_FP_ARCHITECTURE.md` §4 F9 UNSIGNED → SIGNED is CODE-side docs (→ lane B or D).

## LANE D — THE Q10 BAND-WALKER PREFIX (`lane-Q10-prefix`, Opus; one infra car under `tests/lint`; §99.3) — the one hard blocker of #33's bands.

## LANE E — THE SCRIBE RE-CUT (`lane-932-scribe-recut`, Opus; LAUNCH-PATH — the owner 15:4x: "the scribe is pre-launch"): cherry-pick the Scribe's W0–W5b cars (the seal `scribe-w5b-2026-09-15` = `600771812`, on the rewrite dock) onto the NEW slot without the 57 rewrite cars; every car proven independent of rewrite content (the composed corpus at the slot is the SHIPPED corpus); chair-verified; the owner's five acts (key · db push 201–203 · the price · item 13 · the flag) stay the owner's after it. Also a read-only classification of #18's six display trains (rewrite-coupled → post-launch; uncoupled → the launch path) and the question of the DS-DEF-2 clarity re-cut to the owner.

## CONTINUING — LT38 (cars 5–16; car 11 the gate car) and LT37 (cars 5–6) resume the moment HOLD-VITEST lifts; they land at §933 with lanes A–D.

## THEN — #32's non-gated waves in §3j's order to the gates (TR-4 with its CR-2 pin first · TR-5 · WF-6 · POP-1 · WY-0 · WY-1 · IN-4 · TR-6 · the critical path ES-6b → IN-5 → ES-7 → CW-3 last), each wave priced at charter (lane zero of lane 3); the #32 field batch SIGNED by delegation (§931.6b item 9) so WC-6+, HB-3+, EP-4 and the WY rows are no longer stops; then #33 core-first (LG-0 re-priced against the built circle graph · LG-1 allied predicate + the toll · LG-2 the commitment ledger); then W-ARMS cars 1–3; then #18's UNCOUPLED trains (the rewrite and its coupled trains are POST-LAUNCH by the owner's 15:3x word); then #20; then the SECOND DECLARED LIGHTING (treasuryEnabled · the three W-SEAT keys · airField · the LG-0 willingness gate) → the GOLDEN FREEZE (the owner's signature) → the walk → #21.

## THE FOUR-LANE CAP (FOLD 57): A · B · D + LT38 = four build lanes; LT37 finishes first (two cars) and frees a slot; lane C is the chair's.
