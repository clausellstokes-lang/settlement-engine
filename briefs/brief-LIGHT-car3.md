# Brief — the LIGHT train, car 3: THE WORKER BUNDLE — the gate's two real reds cured at the build (chair, 2026-09-09 01:2x; SITTING §S c-5: the gate is the build, and it spoke)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-light.md` § CAR 3. Dock: `$SC/laneLIGHT` at `dd0b68c0d` (the sealed §917 tip: 16 cars; the gate at `gate-917.log` lines 959–976 is your brief's ground). Read first: `tests/build/generationWorkerLazy.test.js` whole (its header: the worker's production boundary; `WORKER_BUNDLE_CEILING_BYTES = 1404242`, "minted from a measured build at §900", monotone-down), `src/workers/generation.worker.js` (the `await loadGenerationLawPayloads()` car 1a added), `src/lib/generationClient.js:52` (the worker is a MODULE worker: `{ type: 'module' }`), `src/domain/content/livingContentSeam.js` (the loader; the dynamic `import(` form is load-bearing), `src/domain/density/densityCreateBoundary.js:334` (`loadGenerationLawPayloads`), `$SC/skeptic-light/FOLD.md` U2 and cure C5, `$SC/receipt-light.md` § CAR 1a (the eager-closure proof: module-graph only).

## THE TWO REDS (the gate's own words)
1. `exactly one generation.worker bundle is emitted, and it is a single chunk because vite inlines a worker entry's dynamic imports` — **expected [ 'import(' ] to deeply equal []**: the built worker `generation.worker-1YsDAmwr.js` now carries `import("./livingContentRoster-D2HjP6Oc.js")` — the seam's dynamic import reached the worker through car 1a's await, and vite did NOT inline it because the roster chunk is SHARED with the main graph. The test's premise (single chunk) is false the moment a shared lazy chunk is reachable from the worker; the worker is a module worker, so the dynamic import WORKS at runtime (the roster loads once per context, lazily — the boundary law kept).
2. `the generation worker bundle stays under its measured ceiling (monotone-down)` — **1,404,524 B against 1,404,242 B: +282 B** — the loader call and the boundary edge the worker gained.

## What to do, in this order
1. **Measure the composition of the +282 B.** THIS CAR MAY BUILD: `npx vite build` in the dock is lawful here (the gate built in this dock; the symlinked `node_modules` is the CORRECT state for a vite build — the clone hazard concerns the edge-shared esbuild step only, which you do not run). Build at the tip, record `generation.worker-*.js` bytes and its `import(` list; then try the shave: the worker awaits the SEAM's loader directly (`import { loadLivingContentRoster } from '../domain/content/livingContentSeam.js'` and `await loadLivingContentRoster()`) instead of the boundary's `loadGenerationLawPayloads` — the worker evaluates its own graph and needs only the roster registered, not the boundary's table — rebuild, record the bytes. Do the same for `customContentPreview.worker.js` (print its bytes before/after; it has no ceiling test — say so). Keep the boundary's `PIPELINE_REACHERS.payloadAwaitedBy` and the walker's ordering arm honest: the worker rows now name the seam's loader (the arm must accept either loader name for a worker row, by a declared table, never by loosening the regex).
2. **If the shave brings the bundle ≤ 1,404,242 B:** land it; the ceiling stands. **If not:** print the exact bytes and STOP on the ceiling — the re-mint is the CHAIR's (a declared row citing the lit dial as the capability; the test's header rewritten to name the new measured build), not yours; land everything else and report the number.
3. **The single-chunk arm becomes the declared-lazy-edge arm:** the worker bundle may carry EXACTLY the dynamic imports a frozen table names — today one, the roster chunk (match by the source module's basename in the emitted specifier, `livingContentRoster`), and the arm asserts (a) the count equals the table, (b) each named target exists in `dist/assets` as a chunk also referenced by the main graph (shared, not duplicated — a second copy of the roster inside the worker would be the byte regression the old arm existed to catch), (c) the worker is constructed with `type: 'module'` (read `generationClient.js`; a classic worker cannot execute `import(`). A plant that adds a second dynamic import reds by count; a plant that flips the worker to classic reds by name. The header rewritten to state the new law and why (a shared lazy chunk is not inlined; a module worker may import it).
4. **The eager-closure proof stays module-graph** — but now say in the receipt what the BUILD measured: the worker bytes before/after, the roster chunk shared, the first-paint total from the gate log (`STRICT DIST` lines) unchanged.

## Discipline, fences, commits — as car 2's, plus
The build is allowed (vite only; never `build-edge-shared`; never touch `node_modules`); `dist/` is not committed (check `.gitignore`; porcelain 0 after the build — if the build dirties the tree, say what and restore by `git show`, never the checkout family). The runner count in its own shell; one focused vitest at a time (`tests/build/generationWorkerLazy.test.js` needs a fresh `dist` — build first); the whole `tests/lint` at your tip; strict; typecheck; OSR exact; prose-numerics exact; no em dash / bang / toFixed in `src/` literals. The lighting census by ritual if a title moves. Commits `LIGHT car 3a: the worker awaits the seam's loader — the boundary edge leaves the worker bundle, <bytes>` / `3b: the worker's lazy edge declared — one shared chunk, a module worker, the count pinned` (+ a register car if a title moved); trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: LIGHT` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the shas; the worker bytes at the tip, after the shave, and the ceiling verdict (≤ or STOP with the number); the preview worker's bytes; the `import(` list; the new arm's plants; the whole `tests/lint`, strict, typecheck, OSR at your tip; anything refused with its measurement. Porcelain 0 and runners 0 when you return.
