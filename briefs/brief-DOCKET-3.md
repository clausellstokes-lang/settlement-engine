# LANE: DOCKET-3 — the six complexity band words (the chair's, verbatim) and the safetyProfile strain-label ruling
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## READ FIRST
1. `$SC/briefs/_PREAMBLE.md` — the hard rules. 2. `$SC/receipt-docket-2.md` — item 3 (the live 174/360 defect, J2, the `complexityBandOf` map with six nulls), and its note on `safetyProfile.js:218`. 3. `$SC/complexity-band-words.md` — THE CHAIR'S SIX WORDS. ⛔ Ship them verbatim; if one fails an instrument, STOP and report which — the chair rewrites, never the lane.

## YOUR DOCK
`$SC/laneDOCKET2`, detached at **`a6f01d757`** (DOCKET-2's car over product 5e28d5c83, sealed `docket-2-2026-09-05`). Build ON TOP; never rebase; the chair replays `laneDOCKET2:5e28d5c83` at §900. ⛔ Never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content.
⛔ **VITEST HOLD IS IN FORCE** until the chair sends RESUME: run NO vitest; module-evaluate with node; record every proof as OWED-UNTIL-RESUME with its exact command.

## THE WORK
1. **The six words into the map.** Fill `complexityBandOf`'s six `null` rows in `src/domain/display/labelBands.js` with the chair's words, keyed on the VERBATIM labels from `deriveEconomicComplexity` (re-read `src/generators/economy/prosperity.js:296–314`; if any label differs from the words file's key by a byte, STOP and report). Move `tests/lint/vocabularyTotality.walker.test.js`'s totality arm 5 → 11 with attribution in the comment. Prove by planting: a twelfth label in the producer reds the walker; a null re-inserted reds it. Prove the Summary tile now renders a band on the 174 settlements (re-run your 360-world census: whole-string renders 174 → 0).
2. **The fingerprint does not move.** `economyInputFingerprint` hashes `safetyLabel`/`foodLabel`; this car changes no label. Prove it: the fingerprint freshness assertion green over the same 360 worlds, byte-identical fingerprints before/after (a script, not a claim).
3. **`safetyProfile.js:218` — RULED: FIX IT.** The chair rules the latent bug (`'Dangerous — Plague Unrest'` is itself a strain label, so a non-first occurrence appends the wrong condition) is a repair, not a behaviour change of any world: prove the branch never fires over 360 worlds at base (0/360), fix it, plant a fixture that DOES reach the branch and show the wrong condition before / the right one after, and show 360/360 outputs byte-identical to base after the fix. If the branch DOES fire on any of the 360, STOP — that is a declared-shift question for the chair.

## FENCES
No persisted-shape change; no other label text changes; no register act (record deltas: lighting titles if any `it()` text moves, ratchet totals). Commit per step, `Seat: Opus 5 — Fable-unvalidated`, `Lane: DOCKET-3`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-docket-3.md`, PARTIAL header first, RETROVALIDATION ROW last, tip sha last.
