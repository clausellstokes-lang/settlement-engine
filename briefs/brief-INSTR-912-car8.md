# Brief — INSTR-912 car 8: THE WIRING CENSUS (the owner's 19:01 addendum, owed by car 1)

Seat: Opus 5 — implementer. The chair (Fable) rules; you measure and build. Receipt first, then the car. ⛔ DO NOT START until the chair tells you the §913 gate has finished and `$SC/HOLD-VITEST` is gone; if the file exists when you arrive, stop and say so.

## Where
- Dock: `$SC/laneINSTR` where `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad` (a detached-HEAD worktree; tip 74a1aa0e8 over the product 3b1c0eaa5 unless the chair says it was rebased — run `git log --format='%h %s' -8` first and record the tip). Never touch the main tree `/Users/cstokes/Desktop/settlement-engine` (its src is frozen) and never enter `laneLMAT` or `skepINSTR`.
- Read first, whole: `$SC/briefs/brief-INSTR-912.md` (the lane's charter, especially its ADDENDUM 19:01 — this car IS that addendum), `$SC/receipt-instr-912.md` (887 lines: what cars 1–7 built and how the lane measures), `$SC/prose-research/sweep/CLERK-LAWS.md`, `$SC/prose-research/sweep/MOVE-GRAMMAR.md` §4/§4.1, `$SC/prose-research/REGISTER-CARD.md`.
- Receipt: append a section `## CAR 8 — THE WIRING CENSUS` to `$SC/receipt-instr-912.md` in the receipt's own idiom (every figure from a command whose output you saw; refusals with their measurement).

## What the owner asked (verbatim from the addendum; this is the specification)
"Every variant is already tied to a state: its block's reading function, its POOL KEY's selecting predicate (the field and value that choose the pool), and the slots the composer's bag fills. Build the (block, pool) → {predicate, fields read, slots filled} census from the composers' key functions and bags and the annex headers; print it beside the composed-fill census; make it the source of truth for arm D and for C-pair/C-sibling. The BEFORE's prose is evidence of what the pool was written to say; the wiring is what it is entitled to say. A BEFORE claim the wiring does not license is a PRE-EXISTING unlicensed claim (NOTE/WITHHELD, banked for the wave), never a rewrite failure; a pool whose predicate is not recoverable is reported WIRING-UNRESOLVED, never inferred."

## What to build
1. `src/domain/prose/wiringCensus.js` (new; pure; no product import path may reach it — it is an instrument): for every (block, pool) the register loaders (car 3) enumerate — the kit's own count is 68 blocks / 708 pools / 2,266 variants (`$SC/s12-sitting/verify/slots.mjs`) — derive `{ block, pool, predicate, fieldsRead, slotsFilled, status }` where
   - `predicate` is the pool-key function's selecting condition as `{ field, op, value }` rows (or a small disjunction), recovered from the SOURCE of the 117 `…PoolKey(` functions in `src/domain/display/stateProse/*StateProse.js` (economy, power, general, defense, warFaith, stressors) — by static reading of the function body (the field paths it reads and the literal it returns per branch). Recover; NEVER infer from the prose. Where the branch cannot be recovered statically (computed keys, helpers you cannot follow, a key built from more than one reading), the row is `status: 'WIRING-UNRESOLVED'` with the reason — never a guess.
   - `fieldsRead` = every reading path the block's reading function and the key function touch (the composer's bag: read the composers to learn how the bag is filled; the receipt's car 3 names the loaders and `tests/helpers/dossierComposedFill.js` the composed-fill census — reuse them, do not re-implement).
   - `slotsFilled` = the `{slot}` names each variant's template carries, checked against what the bag provides (a slot with no provider is a finding, not a crash).
2. `tests/lint/proseWiringCensus.walker.test.js` (new): (a) TOTALITY — every (block, pool) the loaders enumerate has exactly one census row (the count must equal the loaders' pool count; assert the integer, not `> 0`); (b) the census prints beside the composed-fill census (one table; report-only for the corpus); (c) controls that MUST fire: a planted pool whose key function returns a key no branch selects → UNRESOLVED; a planted variant with a slot the bag never fills → finding; a planted predicate that reads a field the block's reading function never reads → finding; (d) an anti-vacuity arm — the walker reds if the census has zero RESOLVED rows or zero UNRESOLVED rows on a fixture built to have both; (e) the FENCE arm — no file under `src/` outside `src/domain/prose/` imports the census (grep by bytes).
3. Wire it as the source of truth: arm D of the grammar walker (`src/domain/prose/grammarWalker.js`, car 2) and C-pair / C-sibling of the entry walker (`src/domain/prose/entryWalker.js`, car 1) take the census as an INPUT for licensing (a claim the wiring does not license is NOTE/WITHHELD, "PRE-EXISTING unlicensed", never a rewrite failure). Keep the existing behaviour when no census is passed (the walkers' current callers), so nothing already green goes red by the wiring alone; add ONE control per walker proving the census changes a verdict when passed.
4. Print the census summary in the receipt: rows total; RESOLVED / UNRESOLVED counts by block; the ten largest UNRESOLVED reasons; slots with no provider; predicates over fields the block does not read. These figures are the chair's inputs for the wave — they must be reproducible by one command you name.

## Measurement discipline (the lane's own, from the receipt)
- Before every vitest run: `ls $SC/HOLD-VITEST` must fail, and the SPLIT-pattern runner count must be 0: `V=vit; V2=est; pgrep -fl "$V$V2" | grep -v gate-mutex | wc -l` (the unsplit pattern self-matches; receipt §0.1).
- Only FOCUSED runs: your new test, the two walker tests you touch (`tests/lint/proseMoveGrammar.walker.test.js`, `tests/lint/proseEntryContradiction.walker.test.js`), `tests/lint/proseRegisterLoaders.walker.test.js`, and — because you add a test file — `tests/lint/mutationCoverageManifest.test.js` (TOTALITY will red until you add the manifest entry: a `kind:'mutation'` plant in `scripts/mutation-sweep.sh` numbered after the lane's last, in the idiom of #74–#78, with its measured figures; never `uncovered`) and the lighting census in REPORT mode (its door refuses a dirty tree; refreeze by its own ritual as car 7b did, in a separate register commit after the code car, on a clean tree). Never the whole suite; never a build; never `--write` on writer-reach or the OSR.
- Every count asserted as an integer; every control executed with its output pasted; every refusal with its measurement.

## Fences
- You may add/change only: `src/domain/prose/wiringCensus.js`, `src/domain/prose/grammarWalker.js`, `src/domain/prose/entryWalker.js`, `tests/lint/proseWiringCensus.walker.test.js`, the two walker tests, `tests/fixtures/*` you need, `tests/helpers/dossierComposedFill.js` (extend, never change existing outputs), `scripts/mutation-sweep.sh`, `scripts/mutation-coverage-manifest.json` (EDIT BY TEXT; never re-serialize — a re-serialization is a 5,000-line diff), `tests/lint/.lighting-census-baseline.json` (register commit only, by the ritual), the receipt. No product surface, no composer, no pool text, no persisted shape, no seed input (THE PROMISE).
- Commits: one code car (`INSTR-912 car 8: the wiring census — <clause>`) then, if the census moved, one register car (`INSTR-912 car 8b: the lighting census refrozen …`). Explicit staging only (`git add <paths>`); porcelain 0 and zero untracked files after; no push, no stash, no rebase, no amend. Trailers, exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
- Return: the car sha(s), the census totals (rows; RESOLVED/UNRESOLVED; slots without a provider; predicates over unread fields), the command that reproduces them, the controls' outputs, the receipt section, and anything refused or left owed.

## ADDENDUM (owner, 2026-09-07 ~21:50; chair-ruled into this car, vetoable) — THE MAP READS BOTH WAYS, AND ITS GAPS SIZE THE AUTHORING WAVE
The owner's suggestion, adopted: the wiring census is ONE table indexed two ways. (1) pool → fact: what each pool is entitled to say (the licensing view above). (2) fact → text: for every fact the composers hold (the unrendered-facts census of car 5 counts 72 held, 13 rendered as a word, 59 key-only) and for every fact COMBINATION a key function conjoins (a key like "COMBINATION C2: a high rung on a narrow approach" is a predicate over two fields — recover the fields, not the prose), list the pools and the variant count and the grammar count that can speak to it. Print, beside the census, three tiers keyed to the dossier's blocks: MISSING (a held fact or a co-occurring fact pair with NO pool), THIN (a pool with one variant, or one grammar, or a slot set of {settlement} alone — the S12 finding: twenty of sixty-eight blocks carry only {settlement}), and COVERED. Measure co-occurrence by EXECUTION, not by reading: compose the 200-town sample car 2 already uses through the SHIPPED composers with real readings (never empty readings — the taste-sample hazard), record which key fired on which town, and count fact PAIRS that co-fire on many towns but have no pool keyed on both. Before building any of this, look for a prebuilt map and say what you found: the key STRINGS are human-readable predicates (use them as the label, never as the predicate), `dossierMounts.js` is the mount registry, car 3's loaders enumerate the pools, car 5's census counts the facts, `tests/helpers/dossierComposedFill.js` is the composed-fill census, and the annex headers carry pool descriptions — join those; do not re-derive by hand what one of them already prints, and do not invent one where none exists. The MISSING and THIN tiers are the authoring wave's input (§912.1: sized from a count of what the rewrite misses, coherent with each block's state and elements); every row must name the block, the field(s), the reading function, and the count that put it in its tier, so the chair can hand the wave a list and not a judgment. Refusals with measurement, as always.
