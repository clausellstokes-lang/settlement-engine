# SOAK-HONEST-909 — CAR 5 (chair-chartered from the skeptic pass wf_e092839f-8b1, 2026-09-07 ~04:10): the weak zero's last two floors, and the shipped citations made true
⟦Chair: Fable 5.1 (session b43943b4) · Lane: Opus 5 · dock `$SC/laneB6` at the car-4 tip `68c3a1618` (re-derive: `git -C $SC/laneB6 log --oneline 6ebe0ef3b..HEAD` must show FOUR cars: ec265d24c · 268d53605 · d58b489b8 · 68c3a1618) · product bytes in `scripts/soak/tripwires.mjs` + `tests/soak-harness/tripwireRegistry.test.js` + comment lines in `scripts/audit/whole-world-soak.mjs` · ⛔ no vitest while `$SC/HOLD-VITEST` exists (absent at dispatch; check before every run); focused files singly through `GATE_MUTEX_TIER=shared sh scripts/gate-mutex.sh --run -- npx vitest run <file>`⟧

Nine Opus skeptics read cars 1a–3 against their messages, their measurements and the laws (findings in `$SC/skeptic-909/` and the chair's digest below). Two findings are PRODUCT defects of exactly the class this lane exists to close; the rest are false or drifting statements in shipped comments. Every figure below is the skeptic's measurement at `d58b489b8` — re-derive each before editing (a brief figure is a hypothesis).

## THE TWO PRODUCT DEFECTS (fix, with tests; every new negative assertion ANCHORED per `tests/helpers/anchoredNegatives.js` and the walker's one-line marker rule — car 4 paid for this)
1. **`receiptCarries` admits an EMPTY ARRAY for a bare top-level path.** The emptiness law (`if (!Array.isArray(cursor) || !cursor.length) return false`) lives only inside the `[last]` branch; a bare name is admitted on `cursor[key] != null` alone (`tripwires.mjs:169-186` at d58b489b8). Executed by the skeptic: a lit receipt with `yearlyPopulations: []` and `yearlyDiedFlags: []` grades both series rows EXECUTABLE, their guards answer `[]`, and `fullInstrument` reads TRUE over two rows that could not observe — the weak zero, one floor down. Car 2's message ("an empty array is absent by the same law `null` is") is FALSE for a bare path; car 1a's comment ("told apart POSITIVELY by the rows' own `requires` channel") is false for an empty series. FIX: the emptiness law applies to EVERY segment, bare names included (an empty array is absent; `null`/`undefined` absent; a present-but-non-object reading where the row keys on an object — `realmDemography = 42` — is ABSENT too, not a silent clean: define "carried" as the shape the row reads). Tests: the empty-series receipt grades NOT-EXECUTABLE on both rows; the `42` reading grades `capacity_realm_load` NOT-EXECUTABLE; the real lit 30-year receipt (`$SC/soak909/fresh-30y-4s-lit.json`) still grades exactly as the receipt records. Re-run the grade census over the 32 `whole_world_soak` receipts on disk (the skeptic's wider census: `grep -rl whole_world_soak --include='*.json' $SC` — 26 real + fixtures) and declare every grade that moves (expected: none — an empty series exists in no real receipt; measure it).
2. **A row whose DETECTOR is unreadable skips its `requires` grading.** `tripwireFieldReach` hits `continue` on `read.unreadable` before the `requires` loop (`tripwires.mjs:757-762`), so the message's "grades every DECLARED requirement in its own channel (`unreachablePaths`)" and the header's "A NESTED PATH WITH NO NESTED SOURCE IS UNREADABLE, NEVER PASSED" are false for that case (a planted row with a computed `receipt[…]` detector and a bad `requires` path lands in `unreadable` and nowhere else). No live row is unreadable today (`unreadable []`), so this is a latent hole in the guard, not a live blindness. FIX: grade `requires` independently of the detector's readability (both channels, always); test with the planted row (a negative control restored by `cp`, never `git checkout`).

## THE HORIZON GUARDS — a chair JUDGMENT to implement (vetoable)
`capacity_plateau` (`pops.length < 150 → []`), `capacity_floor_thaw` (`< 100 → []`) AND the newly armed `capacity_envelope_30y` (`< 30` observed yearly rows → `[]`; the skeptic executed a 29-row receipt with share 0/116 → `[]`, absent from `notExecutable`) answer plain silence on a run too short to observe (the skeptic fed a deliberately FROZEN 30-row series; both stayed `[]`), so a 30-year receipt grades `fullInstrument: true` over two rows that could not say otherwise. RULING: a horizon-short row is COMPLETE-BUT-INCONCLUSIVE — it is not `notExecutable` (the instrument ran; the run is the thing that was short, and a 30-year `useful` receipt must not be refused for a property the design gives it), so it reports through the **observability** channel (the registry's third channel already exists: `findings · observability · notExecutable`) with the reason `horizon: 30 years observed, 150 required`, and `evaluate-receipt` prints it. Over "notExecutable" because that would make every useful-horizon receipt fail `fullInstrument` for a reason that is not an instrument gap; over "leave as is" because a silent zero the row could not have refused is the class this lane closes. Test: the fresh 30-year lit receipt shows the plateau and floor-thaw rows in `observability` with the horizon reason and the envelope row NOT (30 observed = its bar); a 29-row receipt shows all three; the archived 300-year receipt shows none (long enough); `deterministicFirings` unchanged on every receipt; `fullInstrument` unchanged on every receipt.

## THE SHIPPED STATEMENTS MADE TRUE (comment-only edits; quote nothing over twelve words of the old prose)
- `scripts/audit/whole-world-soak.mjs` ~:1093: "in their `requires` (`scripts/soak/tripwires.mjs:314,:355`)" — wrong at the commit that shipped it (319/361 at ec265d24c; 357/… at the tip). ~:1106-1109: "`behavioral-observation.mjs:1202`" (now 1207 at the tip — car 3 moved it); "five pinned fixtures" (17 files pin `schemaVersion: 5` — the skeptic's grep; state the measured count or drop the count). ~:1079-1088: the defect narrative describes a path the code could not take at the parent (both rows already declared `requires` there — the narrative must describe the M1-F1 blindness as it stood at 4243bdc61, before CAP-TRIP-907's `requires`, and say so).
- `scripts/soak/tripwires.mjs` :78, :431, :665: three citations of `behavioral-observation.mjs:1054` for the conditional spread — 1059 at the tip. :639-651: a JSDoc block that documents `receiptWriterFields` now heads `literalKeysFrom` (car 2 inserted the helper between them) — move the block back to its function or write `literalKeysFrom` its own.
- THE LAW FOR ALL OF THESE: **cite by SYMBOL, not by line** wherever a symbol exists (`the requires: declarations of capacity_plateau and capacity_floor_thaw`; `the conditional spread of realmDemography in behavioral-observation.mjs's yearly row`); keep a line number only beside a symbol and only as "at the time of writing". A stated line number that is wrong in the tree that ships it is the false-report class; this car removes the habitat.
- The receipt's SAME-SEED row states "303334 bytes each, identical serialization (host timings excluded)": two skeptics could not reproduce the byte figure (on-disk 539,995 each; seven host keys stripped → 305,121 vs 305,120; plus `isolatedWorker.timingsMs` → 304,847 = 304,847 identical; compact → 303,595) — the SUBSTANCE holds (`finalHash`, all thirty yearly hashes, the population and motion series equal). Restate the row as the exact strip it was taken with, or as the identical-after-strip figure the skeptic measured; a byte count nobody can reproduce is not a receipt.
- The third lifted constant, `CAMPAIGN_HORIZON_YEARS = 30` (car 3 lifted it beside the two named; value preserved; a spelling removed; declared in the commit body) — make the receipt's car-3 row name all THREE.
- The receipt's ORIENTATION row "written ~:1163 (pre-car-1; :1197 after)": :1197 is `if (RECEIPT_PATH) {`; the write is at :1200 at ec265d24c — correct the receipt, not the tree.

## NOT DONE, DECLARED (the chair's rulings)
- **Car 1a's commit message carries four false proof figures** (tripwireCaller 4/4 → 10/10, soakRegister 6/6 → 8/8, curveBandFreeze 10/10 → 4/4, demographicsEnvelope 9/9 → 6/6 — the alphabetical-order bug the receipt's R8 discloses). NOT amended: an amend rewrites every car above it and the sha is already cited; the receipt's R8 and the §910 ledger row carry the correction, and the per-file logs under `$SC/soak909/` are the receipts. Write the same correction into the receipt's CARS table beside car 1a.
- `scripts/audit/behavioral-observation.mjs` is outside the brief's enumerated footprint yet named by the brief's own car 3 (a former home of `MOTION_FLOOR_01`) — the brief's list was incomplete, not the lane; recorded.
- The nested half of the walker grades later path segments against a FLAT union of every `return {` key in the observer (79 keys), so a leaf the observer ships anywhere passes even where it cannot nest — the header admits it; a per-return-site nested walk is a chair follow-on for a later consist, not this car. Recorded.

## ADDED 04:27 FROM THE CAR-3 CODE LENS AND THE CRITIC (`$SC/skeptic-909/CRITIC.md`, G1–G10, J1–J4 — read it in full)
3. **THE LIFTED HORIZON'S TWIN IS UNGUARDED (G5/J1).** Car 3 lifted a THIRD constant, `CAMPAIGN_HORIZON_YEARS = 30` (`soakInvariants.mjs:163`), whose own comment concedes it equals `CERTIFICATION_HORIZONS.useful.years` (`behavioralContract.js:129` = 30) — and NO assertion anywhere reds when the two drift; `tests/soak-harness/soakInvariants.test.js` carries no arm for any of the three new constants. The commit's "the lift REMOVES a spelling" is refuted by count: 2 definitions before (`demographicsEnvelope.test.js:48`, `behavioralContract.js:129`) and 2 after (`soakInvariants.mjs:163`, `behavioralContract.js:129`) — the envelope suite's line became an ALIAS; the lift MOVED a spelling. CHAIR RULING: the lift is RATIFIED (the suite no longer types the figure) and the GUARD is ORDERED: one arm in `soakInvariants.test.js` asserting `CAMPAIGN_HORIZON_YEARS === CERTIFICATION_HORIZONS.useful.years`, and the same suite pins the three lifted values (0.05, 0.0025, 30) as CONTROLS read from the module (a value moved without a declared cause reds). Correct the soakInvariants comment (":143-145") and the receipt's car-3 row: MOVED, not removed; the twin is now guarded.
4. **THE FIXTURE'S YEAR-1 CLAIM IS FALSE AGAINST EVERY REAL RECEIPT (car-3 code lens; R13).** The derived `motion` block in `soakRegister.test.js:92-98` and R13 say year 1 "carries zero transitions — exactly what the observer produces". Measured on all three receipts the lane wrote: `behavioral.yearly[0].year = 1` with `motion.populationTransitions = 4`, and ZERO rows at zero transitions in 30 years and in 300. Read the observer's transition count (`behavioral-observation.mjs`, the loop around `if (!previous) continue;` ~:957-962) to learn why year 1 carries 4 (the first year's record IS a transition from the birth state, or the observer seeds `previous` — measure it), then derive the fixture's year 1 the way the observer does and correct the comment and R13. A fixture that claims the writer's shape must have it.
5. **THE TYPED `motion` PLANTS ARE UNDECLARED (G7).** Car 3 also planted `motion: { populationTransitions: 4, populationMoved: 4 }` — TYPED, not derived — into `tripwireRegistry.test.js`'s passing-corner receipt and the plant at ~:324. Declare both in a RETROVALIDATION row (the brief's law: a moved fixture is declared IN THE RECEIPT), and derive them from the fixture's own numbers if a derivation exists.
6. **STALE COUNTS INSIDE THE FILE THE CAR EDITED**: `tripwireRegistry.test.js` ~:324-326 still says "what ALL THREE require" (four rows declare `requires` now — executed: `notExecutable` lists four on a bare receipt); ~:240 "THREE SINCE §909"; ~:268-27x the passing-corner header ("every capacity row RAN on it: the third channel is empty, so this `[]` is a measurement") is overbroad for `capacity_envelope_30y` at `yearly.length = 1 < 30` — under the observability ruling above the passing corner now shows the horizon rows in `observability`; re-cut the arm and its header to the measured truth (`notExecutable []` AND the named horizon rows in `observability`).
7. **ONE MORE STALE CITATION**: `tripwires.mjs:296-298` cites `whole-world-soak.mjs:1050-1052` for `rules: runA.simulationRules` — measured at :1128 (already stale at 6ebe0ef3b: 1091). Inherited, not introduced; fix under the cite-by-symbol law with the rest. And `soakInvariants.mjs:134-135` cites the pre-lift `:957` (now 962; a historical cite — say "at 6ebe0ef3b" or drop the number).
8. **THE CENSUS SENTENCE (G1/J3).** The receipt's CAR-2 row claims a grade census over EVERY `whole_world_soak` receipt on disk and enumerates four; 32 exist (21 are the §905 L-PROBE battery under `lprobe-out-905/`). The critic graded the missing 21 at the dock HEAD (`$SC/skeptic-909/grade.mjs`): every one NOT APPLICABLE by gate (`demographicsEnabled` undefined on 18, false on 3), so the conclusion survives. Re-run the census over all 32 with the critic's script (or your own — cite it) AFTER your defect-1 fix and write the sentence as the set actually graded, with the count.
9. **A DEFERRAL LEDGER (G8, G9, J4).** Add a `## DEFERRED — documented, not a bug to re-find` section to the receipt: (a) `capacity_plateau` has never graded a SHIPPED 300-row series, only one rebuilt from `stateVectors` — deferred to CAP-HORIZON-909's 600-year run under the new writer, which is the first receipt that will carry the series at the horizon the row grades; (b) a DARK run certifies `fullInstrument: true` with all four capacity rows NOT APPLICABLE by gate — by design (the gate is a property of the run's lighting and the register's cell identity carries `lighting`), recorded as an open design question for the tuning sitting, not a defect; (c) the flat nested-walker union (79 keys) — a per-return-site nested walk is a later consist's car.
10. **THE COMMIT-MESSAGE FALSE FIGURES (G4).** Ruled above: NOT amended (the critic concurs: an amend rewrites three later cars and orphans any taken register). Write into the receipt, beside car 1a, that the durable record stands false in four figures and why it is left so; the §910 ledger row will say the same.

## THE CHAIR'S RULINGS ON R1–R16 (for your RETROVALIDATION ROW; the ledger row §910 carries them)
RATIFIED: R1, R2, R3 (the lane's best act), R4, R6, R7, R9 (strictly stronger than the equality it replaced), R10, R12, R14, R15, R16. RATIFIED WITH THE ORDERED GUARD: the CAMPAIGN_HORIZON_YEARS lift (J1 above). REOPENED AND CURED BY THIS CAR: the empty-array asymmetry (J2 = defect 1), the census sentence (J3 = item 8), the missing deferral ledger (J4 = item 9), R13's year-1 claim (item 4). R11 (the envelope row gated on `demographicsEnabled`): RATIFIED — a category error otherwise; reversible, and the chair keeps the wider net as an owner-visible option. R5: RATIFIED as to the two corrections it made; its class is re-cured by this car's cite-by-symbol law. R8: RATIFIED; the class is closed by reading each count from its own log by name — and this car's proof section obeys it.

## PROOF, RECEIPT, COMMIT
Focused files singly (`tripwireRegistry`, `tripwireCaller`, `soakRegister`, `soakScriptSeams`, `soakInvariants`, `evaluate`/`seams`/`curveBand`, `demographicsEnvelope`) — through `GATE_MUTEX_TIER=shared sh scripts/gate-mutex.sh --run -- npx vitest run <file> --maxWorkers=2` (the shared tier REFUSES a run without a worker cap — car 4's R16), exits captured in-shell, EACH COUNT READ FROM ITS OWN LOG BY NAME (the R8 lesson); the negative-assertion walker file singly (zero new un-anchored); eslint on touched files; `npm run typecheck:ratchet` (ceiling 173). Title counts per touched test file before and after (the census is predicted at 23664; if a new it() is needed, say so — the chair re-predicts). One commit, explicit paths, trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: SOAK-HONEST-909`; porcelain 0. Receipt re-written (CARS car 5; RETROVALIDATION rows for the horizon ruling and every judgment; the car-1a correction beside car 1a); every timestamp from `date`. Final message: the sha, every measured figure, the grade census, the title counts, every refusal with its measurement.
