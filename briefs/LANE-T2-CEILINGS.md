# LANE-T2-CEILINGS — the dispatch brief (Opus attribution-and-ruling lane; given 2026-09-19 ~14:2x EDT by the successor chair, session 7d3418f8)

Role: Opus lane for train EM-T2's terminal, the DIST half. The chair measured at `fefb2bb29` (`npm run build` + `npm run verify:dist`, log `$SP/chair-kit-923472dc/logs/consist-dist-measure-fefb2bb29.log`): STRICT DIST REFUSED, two build tests red —
- `tests/build/generationWorkerLazy.test.js` › *the generation worker bundle stays under its measured ceiling* — `generation.worker-D8YPzUzM.js is 1401131 B; the ceiling is 1399946 B` (+1,185 B).
- `tests/build/vendorPdfLazy.test.js` › *the engine chunk still exists (lazy) and remains large* — `expected 678134 to be less than 677000` (+1,134 B and the margin).

Run 14 was GREEN end to end at `ed9d99295` and the only `src/` paths that moved from there to the tip are EM-P0's two (`src/generators/pipeline.js`, `src/generators/steps/generatePopulation.js`, commit `429ceed54`), so the chair's READING is that every byte is EM-P0's pipeline seam. A reading is not a measurement. You MEASURE the attribution per module, and only if it confirms the reading do you APPLY the chair's ruling below. "A RISE IS NEVER A LANE'S EDIT: buy the bytes back, or take the ruling" — this brief IS the ruling, conditional on your measurement. The chair (Fable) rules and validates; it does not edit `src/` or `tests/` (the owner's seat law, restated 2026-09-19 14:11).

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

Worktree: `$SP/lane-em-b3b` ON `fixes-2026-09-18-consist` (the slot; `node_modules` present). It must be CLEAN and its tip must be the T2 cure lane's commit (subject begins `EM-T2's terminal reds cured`) on top of `fefb2bb29`; if not, STOP. Scratch: `$SP/lane-t2-ceilings-scratch/` (create it). You MAY create ONE extra worktree for the control build: `git -C $SP/lane-em-b3b worktree add --detach $SP/attrib-base-ed9d99295 ed9d99295` then `npm ci` inside it (log to your scratch); remove it with `git worktree remove` when done. FORBIDDEN: `$SP/consist` (the chair's worktree), the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the chair kit (read-only — copy the tool out of it), every other lane directory. Never `--ignore-other-worktrees`, never `git add -A`/`-u`/`.`, never stash/reset/clean/rebase/amend, never `--no-verify`, never push. zsh: explicit file arguments; quote variables. Never `npm run check`, `npm run test` or `test:ratchet`.

## Verbs, in order
1. **The control and the tip, attributed per module.** The tool is `$SP/chair-kit-923472dc/tools/attrib.config.mjs` (a one-off Vite config wrapper recording every module's `renderedLength` inside every emitted chunk; it MERGES across the worker's own Rollup pass). Copy it into the root of each tree as `attrib.config.mjs`, and in each tree run, through the EXCLUSIVE mutex with the long wait (`GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- …`): `ATTRIB_OUT=<scratch>/attrib-<base|tip>.json ATTRIB_DIR=dist-attrib npx vite build --config attrib.config.mjs`. Base = the control worktree at `ed9d99295`; tip = the slot. Delete the JSON before each run (the tool merges into an existing file). Afterwards REMOVE `attrib.config.mjs` and `dist-attrib/` from the slot — `git status --short` there must be empty again before verb 3.
2. **The table.** For the two chunks (`generation.worker-*.js` and the lazy `engine-*.js` — NOT `engine-core*`), list every module whose rendered length differs between base and tip: module, before → after, delta; plus each chunk's emitted size at base and tip. Write it to `<scratch>/T2-CEILINGS.attribution.md`. THE CONDITION: the moved modules are exactly `src/generators/pipeline.js` and/or `src/generators/steps/generatePopulation.js` in BOTH chunks (a module outside those two moving by more than 16 rendered bytes = STOP; report the table and edit nothing).
3. **The real measurement.** In the slot: `rm -rf dist`, then `GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npm run build` (⚠ `npm run build`, never bare `npx vite build` — the sitemap prebuild and the prerender postbuild are part of what `verify:dist` reads). Read the two emitted sizes in BYTES (`ls -l dist/assets`), call them W (worker) and E (engine).
4. **Apply the ruling (only if verb 2's condition held).**
   a. `tests/build/generationWorkerLazy.test.js`: `WORKER_BUNDLE_CEILING_BYTES = 1399946` → **W exactly** (zero slack, as every mint before it). Directly above the `export const`, after the existing `// 1,399,318 → 1,399,946 …` comment block and in its voice, add a block that says: `// 1,399,946 → <W> (2026-09-19, the chair, ODQ §934.19 addendum 2): +<delta> B minified,` then: ATTRIBUTED PER MODULE by a control build at train EM-T1's green terminal (ed9d99295) against the EM-T2 cure tip (<short sha>) — name each moved module with its before → after rendered bytes; the bytes are EM-P0's pipeline seam (ODQ §934.47, the owner's editor program): the runner hands `pins` to every step and refuses a partial pin, and generatePopulation consults a pin at each of its four choosers on its one stream; generation code in the generation worker, where it belongs; nothing eager re-merged; a buy-back inside those two files is the named alternative; the rise is offered for the owner's ratification like the mints it follows; MONOTONE-DOWN from here. Fill every figure from YOUR measurement; never copy a figure from this brief.
   b. `tests/build/vendorPdfLazy.test.js`: `expect(size).toBeLessThan(677_000)` → **`679_000`**, PROVIDED `E + 700 <= 679000` (the ~700 B cross-environment margin the three prior raises carried; if E is larger, STOP and report E). After the existing `// CEILING RAISE 676_000 -> 677_000 …` block and in its voice, add: `// CEILING RAISE 677_000 -> 679_000 (CHAIR RULING 2026-09-19, ODQ §934.19 addendum 2, OFFERED FOR RATIFICATION as the three above were):` measured <E> at the EM-T2 cure tip (<short sha>); the control build at train EM-T1's green terminal (ed9d99295) read <E at base>, so the +<delta> B are EM-P0's pipeline seam (ODQ §934.47) — name the moved modules with their rendered bytes — generation code, where it belongs; nothing eager re-merged: the first-paint closure arm above is green on the same build; the ~700 B margin is kept.
   ⛔ No other line of either file moves. No test TITLE changes; no test is added or removed (the lighting census tuple must not move). Do not touch `vite.config.js`, `src/`, or any other ceiling.
5. **Receipts.** (a) `GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 npm run verify:dist` — BARE, never wrapped (it takes the exclusive mutex itself and the mutex is not re-entrant); quote its verdict line and the strict-dist count (the last green read 538). A line with no count DID NOT RUN. (b) Shared tier, worker-capped, one directory per run: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint/sovereigntyLightingContract.walker.test.js tests/lint/mutationCoverageManifest.test.js` — green plain (a lighting red = STOP, never a refreeze). (c) `npx eslint tests/build/generationWorkerLazy.test.js tests/build/vendorPdfLazy.test.js` — 0 errors. (d) The monotone proof: temporarily set the worker constant to `W - 1`, run `VERIFY_DIST=1 npx vitest run --pool=threads --maxWorkers=2 tests/build/generationWorkerLazy.test.js` through the shared mutex, QUOTE the red, restore, re-read `git diff` (your cure, byte for byte).
6. **ONE commit ON the branch by EXPLICIT PATHSPEC of the two test files** (the chair grants commit authority for this landing): read `git diff --cached` whole first. Subject: `The generation worker and engine ceilings re-mint for EM-P0's pipeline seam, attributed per module (ODQ §934.19 addendum 2; vetoable)`. Body: the two reds verbatim, the attribution table, W and E with their deltas, the receipts with counts, the monotone proof's red line, "offered for the owner's ratification; a veto returns both constants and takes a buy-back inside the two files". Trailer: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. After it: `git show --stat HEAD` names exactly the two paths; `git status --short` empty; if the pre-commit hook rewrote either file, re-run 5a–5c and say so. Remove the control worktree (`git worktree remove $SP/attrib-base-ed9d99295`).
7. **Report** (also to `<scratch>/T2-CEILINGS.receipt.md`): the commit sha; the attribution table; W, E, both deltas; every receipt's count line quoted; every claim CONFIRMED (executed) or PLAUSIBLE; anything noticed and not touched — in particular any cheap buy-back you SAW while reading the two EM-P0 files (name it; do not make it).

STOP (uncommitted; write `<scratch>/T2-CEILINGS.STOP.md`): the slot is dirty or its tip is not the cure commit; a module outside the two moved; E + 700 > 679000; a lighting red; any edit needed outside the two test files; a gate prints no count; `npm ci` in the control tree fails twice.
