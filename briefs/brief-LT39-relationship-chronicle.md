# Brief — LONG TAIL #39: The relationship chronicle surface, and the chronicle's deploy
(recon by workflow wf_d358b43d-411, 2026-09-15, READ-ONLY; records on the ledger worktree, code read at the build slot `claude/composite-r4` = `f73bdbf16`)

## THE STATE OF THE RECORD

THE RECORD IS NARROW AND STILL ACCURATE ON ITS OWN CLAIM, AND ONE OF ITS TWO HALVES IS ALREADY CLOSED IN-REPO.

LEDGER docs/world-pulse-roadmap.md is 158 lines, read whole. Its 2026-06-11 status refresh (lines 7-24) is the operative block and says so itself ("Section bodies below are kept for design rationale; trust this block and the audit for status"): every phase is "implemented, wired, and tested green" EXCEPT 4d, and that explicitly includes "4a end-to-end (the `generate-chronicle` edge function exists in-repo with the client hook and credit cost)". The one genuinely-remaining row is lines 21-23: "Phase 4d (NPC/relationship chronicle UI — the data is captured; no surface reads `relationshipState.history`/`recentIncidents`)."

What Phase 4d names exactly, verbatim at lines 145-149 ("### 4d. NPC / relationship chronicle surface **[ui]**"): "`relationshipState.history`/`recentIncidents` are captured; NPCs have a thinner trail. A readable 'what this alliance survived / who this NPC betrayed' log turns the simulation's memory into table-usable narrative. Data mostly exists; the surface is UI." Phase 4a (lines 125-132) is the AI campaign chronicle and ends with the sentence the synthesis is quoting: "I can build the client hook + grounding payload; the edge function + deploy is your Codespace." The suggested order (lines 153-158) puts 4a/4d last.

A LATER SECTION CONFIRMS RATHER THAN OVERTURNS. LEDGER docs/REGIONAL_ENGINE_AUDIT.md:172-173 is the audit the refresh cites: "docs/world-pulse-roadmap.md is severely stale: 10 of 11 `[next]` items (plus 4a) are implemented, wired, and tested green; only 4d (NPC/relationship chronicle UI) is genuinely missing." docs/COHESION_REMEDIATION_PLAN.md:285 carries the unpaid chore "refresh the stale `world-pulse-roadmap.md` statuses" — the doc's own section bodies still read [next] on eleven landed items, so the file lies to a skim reader even though its header block does not.

THE DEPLOY HALF IS A RUNBOOK ROW, NOT A CODE ROW. LEDGER docs/DEPLOY.md:259 lists `npx supabase functions deploy generate-chronicle` in the verify_jwt=true block of the 33-function cutover, and :285 names ANTHROPIC_API_KEY as required "for generate-narrative + generate-chronicle + ai-analyst". The runbook is pinned against the function tree by tests/docs/deployRunbookFreshness.test.js. Nothing in the records says the deploy has happened; it is the owner's Supabase act and stays out of every car below.

THE LONG TAIL'S OWN ROW. kit/briefs/LONG-TAIL-2026-09-14.md:57 (table) and :445-454 (body) file this as item 39, display/UI, owed, M, "a screen; the edge function's deploy is yours", recorded at "START_HERE+ — world-pulse-roadmap, the 2026-06-11 status refresh ('Genuinely remaining: Phase 4d'); world-pulse-roadmap §4a and §4d". LEDGER docs/HANDOFF_CURRENT.md:21 names "the relationship chronicle" inside the long tail's debts-and-backlogs band (items 28-43). docs/OWNER_DECISION_QUEUE.md (32,530 lines) contains NO §-marked entry for Phase 4d, relationship chronicle, recentIncidents or relationshipState.history — grep returns nothing. So no chair ruling, charter, packet or deferral has ever been written against this item: the roadmap and the regional audit are the whole record.

## THE STATE OF THE CODE (read at the slot; no gate run)

MEASURED AT THE BUILD SLOT (claude/composite-r4 @ f73bdbf16) BY READING ONLY — no test suite, gate, build, vitest, deno test or npm script was run.

── 4a IS FULLY LANDED IN-REPO; ONLY THE DEPLOY IS OUTSTANDING ────────────────
The whole chain exists and is richer than the roadmap describes. supabase/functions/generate-chronicle/index.ts is 430 lines with a 462-line index.test.ts beside it; index.ts:31 `const CHRONICLE_COST = 2; // credits; pinned to src/config/pricing.js CHRONICLE_CREDIT_COST + spend_credits CASE 'chronicle' by tests/config/pricing.test.js`, with bot guard, JWT auth, payload cap (:42-43), daily capacity 503 (:249), per-user 429 (:268), atomic `spend_credits` RPC (:273), service-role `refund_credits` on failure (:303), and a token-cost line (:323). The pure grounding half is src/domain/worldPulse/chronicle.js (140 lines, `buildChronicleGrounding`, which has since grown siege-coalition and trade-war-commodity naming the roadmap never mentions). The client hook is src/lib/campaignChronicle.js (58 lines), invoking the function at :48. The surface is src/components/map/WizardNewsPanel.jsx:381 `generateChronicle()` and :480 the gold "Chronicle" button titled "Turn this tick's grounded news into a two-credit campaign chronicle". The reader is src/components/map/ChronicleScrollback.jsx (342 lines), which replaced the chronicles[0]-only article. NOTHING IS OWED HERE IN CODE.

── 4d: THE DATA IS FAR RICHER THAN THE RECORD SAYS, AND STILL UNREAD ─────────
The record names two archives; the slot carries SIX on every relationship edge, all bounded, all persisted, in src/domain/worldPulse/relationshipState.js `ensureRelationshipState`: recentIncidents cap 8 (:472, emitted :497), history cap 12 (:473, :498), turningPoints cap 24 (RELATIONSHIP_TURNING_POINT_CAP :198, emitted :499), allianceCalls cap 24 (:203, :500), coalitionSettlements cap 24 (:207, :501), hierarchyResolutions cap 6 (:502). turningPoints/allianceCalls/coalitionSettlements are explicitly the "longer-lived source than the rolling incident/history windows" (:192-197) — literally the alliance's survived-history and the refused-summons record ("a refused summons is a diplomatic fact", :199-201). 24 modules under src/domain/ name recentIncidents (22 in worldPulse, 1 spatial, 1 certification).

ZERO SURFACES READ ANY OF THE SIX. `grep -rln` for recentIncidents | history-archives | turningPoints | allianceCalls | coalitionSettlements | hierarchyResolutions across src/components/, src/domain/display/, src/hooks/ and src/lib/ returns NOTHING for all six. The roadmap's claim holds, and holds four fields wider than it knew.

A HUMAN-READABLE READ MODEL ALREADY EXISTS AND FEEDS ONLY THE AI. src/domain/worldPulse/relationshipMemory.js (604 lines) composes per-edge rows with posture, postureLabel (19 authored labels, :47-65), memoryScore, dailyLifeWeight, asymmetry, flowProfile, `recentMemory`, `reasons` and `practicalEffects` (authored table-facing sentences, :313-337) via `buildRelationshipPostures` (:380). `refreshRelationshipMemory` (:443) STAMPS that blob onto every relationshipState, and it runs every pulse — src/domain/worldPulse/applyWorldPulse.js:1130. Its only consumers are itself, the AI grounding path (src/store/aiSlice.js:116-137 → src/domain/aiGrounding.js:387/:442 → src/lib/ai.js:173), and tests. The prose exists; no human ever sees it.

⛔ THE TRAP IN REUSING IT. `memoryEntry` returns null when `weight <= 0` (relationshipMemory.js:133-137), and weight is zero for undated rows and for anything older than RELATIONSHIP_MEMORY_MAX_LOOKBACK_TICKS = 24 (:15, :114-130). So `recentMemory` is a 24-tick decay window, NOT a chronicle: a surface built on it would silently drop exactly the old alliance history the item asks for, while the durable turningPoints/allianceCalls/coalitionSettlements archives sit unread beside it.

THE PRESENT-TENSE HALF OF 4d IS ALREADY SHIPPED. src/components/map/PerspectiveStandings.jsx (141 lines, read whole; DESK-4) mounts in the Herald's War door (src/components/map/HeraldBody.jsx:236) and renders "The world as X knows it" — an observer picker plus one plain-language line per relation: sieges, coalition march-mates, treaty roles, and a DM-gated believed half. It reads warStatus, warAndRoadNames, treatyDocument and settlementBeliefs. It reads NO relationship state, NO posture, and NO archive. The PRESENT standing has a surface; the HISTORY has none. That is the sharpest statement of what is missing.

THE ONE PLACE A RELATIONSHIP CHANGE REACHES A SCREEN DOES IT ANONYMOUSLY. src/domain/display/chronicleReadModel.js `deltaFirst` (:203-244) collects relationship changes into `{ key, kind }` rows — kind ∈ war-declared | alliance-formed | peace-or-shift | relationship-change (:222-232) — and src/components/map/AdvanceReport.jsx:151-155 renders each as a chip containing ONLY `humanizeToken(r.kind)`. The relationshipKey is discarded and no settlement is named, so a GM reads "war declared" with no parties. ChronicleNode already carries `settlementIds` (chronicleGraph.js:125), so the names are one field away.

THE NPC HALF IS THINNER, EXACTLY AS THE RECORD SAYS. `ensureNpcStates` (src/domain/worldPulse/npcAgency.js:422-560) builds npcStates with no history/trail array at all. The durable NPC record is the ladder's: npcLadderState.js `normalizeContests` (:608), `sortedContests` (:645), `normalizeSeatTransitions` (:717), `normalizeRecord`/`sortedRecord` (:764/:846), plus GRUDGE_KINDS = contest_loss | contest_forestalled | rivalry (:387) and BOND_KINDS = loyalty | gratitude | friendship | respect (:414). Its only reader is the engine (src/domain/worldPulse/treatySuccession.js:247 reads `recordOf(ladder[settlementId]).seatTransitions`). A DM-gated NPC surface DOES exist — src/domain/display/npcInteriorityRead.js renders bonds, grudges and credibility into src/components/new/npcComponents.jsx behind `includeGroundTruth` — but it shows STANDING, never the dated contest/seat trail.

WORDS AND SEAMS THE IMPLEMENTER WILL HIT. `titleForType` (relationshipMemory.js:74-76) humanizes only by replacing underscores, so "coalition_betrayal" becomes "coalition betrayal" — fine for an AI payload, an engine token in front of a reader by this estate's own law; src/domain/display/humanizeEngineTokens.js (255 lines) has NO incident-type bucket. Incidents carry no visibility field, and the 24 writers include covert families (corruptionWeb, espionageGauntlet, informationStatecraft), so a player-facing render would leak DM truth; the estate's two fail-closed idioms are viewerSeesDmSecrets (src/domain/display/viewerSecrets.js:48, used by HeraldRemembrance.jsx) and includeGroundTruth (npcInteriorityRead.js, PerspectiveStandings.jsx:57-59). Finally, relationshipKeyFromEdge (relationshipState.js:363-369) returns `edge.id` when present, so the key is NOT reliably parseable into a settlement pair — names must come from the edge or the graph.

## ALREADY DONE (with evidence)

- PHASE 4a IS COMPLETE IN-REPO, DEPLOY EXCEPTED — and the record already says so (docs/world-pulse-roadmap.md:18-20). Evidence in the slot: supabase/functions/generate-chronicle/index.ts (430 lines) + index.test.ts (462 lines); the pure grounding src/domain/worldPulse/chronicle.js (140 lines); the client hook src/lib/campaignChronicle.js:48 invoking the function; the UI button at src/components/map/WizardNewsPanel.jsx:480 ('two-credit campaign chronicle') driving :381 generateChronicle(); the credit cost pinned at index.ts:31; and the deploy row plus secret already written into docs/DEPLOY.md:259/:285 behind tests/docs/deployRunbookFreshness.test.js. There is no code car for 4a.
- THE CHRONICLE READER GREW PAST WHAT THE RECORD DESCRIBES — src/components/map/ChronicleScrollback.jsx (342 lines) replaced the chronicles[0]-only article and scrubs the full chronicles[] + pulseHistory timeline, with an end-of-advance interval summary built off buildChronicleGrounding.
- THE PRESENT-TENSE HALF OF 4d SHIPPED AS DESK-4 — src/components/map/PerspectiveStandings.jsx (141 lines, mounted at HeraldBody.jsx:236) renders one observer's sieges, coalition march-mates, treaty roles and DM-gated beliefs in plain language with an observer picker. It reads no relationship state and no archive, so it closes the standing half and leaves the history half entirely open.
- A HUMAN-WORDED RELATIONSHIP READ MODEL ALREADY EXISTS AND IS COMPUTED EVERY PULSE — buildRelationshipPostures (relationshipMemory.js:380) with 19 authored posture labels (:47-65), reasons (:296-311) and table-facing practicalEffects (:313-337); refreshRelationshipMemory (:443) stamps the blob onto every relationshipState and runs at applyWorldPulse.js:1130. It is consumed today ONLY by the AI grounding path (src/store/aiSlice.js:116-137 -> src/domain/aiGrounding.js:387/:442 -> src/lib/ai.js:173). The prose half of 4d is already authored; the missing thing is a human reader, which is why the cars are read-only.
- PART OF THE NPC HALF SHIPPED SEPARATELY — src/domain/display/npcInteriorityRead.js renders DM-gated bonds, grudges and credibility into src/components/new/npcComponents.jsx (V-24c). It shows standing, not a dated trail, so car 5 is an extension of a landed surface rather than a new one.
- RELATIONSHIP CHANGES ALREADY REACH A SCREEN, ANONYMOUSLY — chronicleReadModel.js:222-232 classifies them (war-declared / alliance-formed / peace-or-shift) and AdvanceReport.jsx:151-155 renders a chip per change. Car 1 is a repair of a landed surface, not new capability.
- THE TEN OTHER ROADMAP ROWS (1a-1c, 2a-2d, 3a, 4b, 4c) — recorded green by the 2026-06-11 refresh and REGIONAL_ENGINE_AUDIT.md:172-173. I did not re-verify these; they are PLAUSIBLE on the record, not CONFIRMED by me, and no car touches them.

## THE CARS

### CAR 1 — Name the parties on the advance report's relationship chips

Today a relationship change reaches the GM as a bare chip reading "war declared" with no settlements named (chronicleReadModel.js:222-232 keeps only {key, kind}; AdvanceReport.jsx:151-155 renders humanizeToken(r.kind) alone). Carry the node's existing settlementIds (chronicleGraph.js:125) and the payload's fromType/toType onto the delta row, and render "Ashford and Calder: allied -> hostile" instead of "war declared". Resolve names with the resolveName already threaded into DeltaLead — never by parsing the relationshipKey, which is edge.id when the edge carries one (relationshipState.js:363-369). Pure display; no engine read, no new module, no new file.

*Files:* src/domain/display/chronicleReadModel.js, src/components/map/AdvanceReport.jsx

*Tests:* Extend tests/domain/chronicleReadModel.test.js (delta rows carry the pair; a keyed row with no settlementIds still renders and names nobody rather than inventing) and tests/components/advanceReport.test.jsx (the chronicle-delta chip names both settlements; the empty state is unchanged).

*Gate:* npx vitest run tests/domain/chronicleReadModel.test.js tests/components/advanceReport.test.jsx, then the full npm run check before landing (it is gate-mutex serialized via scripts/gate-mutex.sh — do not run it while another lane holds the lock).

### CAR 2 — The relationship chronicle read model

A new pure display leaf that answers "what has this relationship survived". One row per edge: the pair named, the present type and posture, and a DATED list of what happened, merged from all six durable archives on relationshipState (recentIncidents :497, history :498, turningPoints :499, allianceCalls :500, coalitionSettlements :501, hierarchyResolutions :502) plus the relationship-keyed outcomes in worldState.pulseHistory. ⛔ IT MUST NOT BE BUILT ON buildRelationshipPostures' recentMemory: memoryEntry drops every row older than 24 ticks or undated (relationshipMemory.js:133-137, :15), which is precisely the old alliance history the item wants. Reuse relationshipMemory's dedupe reasoning (:150-250 — one world event lands in up to three stores and must be counted once) but with no decay cutoff. Reuse its authored POSTURE_LABELS (:47-65) for the present-state line. Deterministic, codepoint-stable, inert-not-crash on garbage, no store, no rng, no wall clock, per the src/domain/display house rules. A new display -> worldPulse import pair needs its CW-0 registry row IN THE SAME COMMIT or tests/lint/couplingInclusion.walker.test.js reds (baseline tests/lint/.coupling-inclusion-baseline.json, walker :108).

*Files:* src/domain/display/relationshipChronicle.js, src/domain/certification/couplingRegistry.js, tests/lint/.coupling-inclusion-baseline.json

*Tests:* New tests/domain/display/relationshipChronicle.test.js: a fresh world yields an empty chronicle (byte-identical off-state); an event 40 ticks old that lives in turningPoints STILL appears (the negative control against the decay trap, which is the whole point of the car); one event written to incident + history + turningPoints appears exactly once; rows are deterministic across input reordering; garbage/absent relationshipStates return [] rather than throwing.

*Gate:* npx vitest run tests/domain/display/relationshipChronicle.test.js tests/lint/couplingInclusion.walker.test.js tests/domain/relationshipMemory.test.js; npm run typecheck:domain:strict; then npm run check before landing. A new test file bills the census — scripts/.test-ratchet-baseline.json is re-frozen with npm run test:ratchet:update as the LAST step of the landing, never first.

### CAR 3 — A total incident-type lexicon, bound to its writers by a walker

Car 2 prints incident types; the only humanizer in the tree is titleForType (relationshipMemory.js:74-76), which merely swaps underscores for spaces, so 'coalition_betrayal' would reach a reader as an engine token — against the estate's layman's-terms law and the NEWS ADDRESS LAW. Add an incident-type bucket to the DISPLAY_LEXICON in humanizeEngineTokens.js (it already carries displayLabel/humanizeIfToken at :222/:249 and has no incident coverage today), with an honest fallback sentence for an unmapped token, and bind it to the producers with a producer/consumer totality arm in the vocabularyTotality walker — the file's own stated contract, exact-set-both-ways, no dead arm and no silent miss (walker header :15-19). The producer set is the type literals the 24 src/domain writers of recentIncidents emit; if that set cannot be imported, bind it by source-presence exactly as the walker's curated safety-token precedent does (header :29-34) and say so in the file.

*Files:* src/domain/display/humanizeEngineTokens.js, src/domain/display/relationshipChronicle.js, tests/lint/vocabularyTotality.walker.test.js

*Tests:* Extend tests/lint/vocabularyTotality.walker.test.js with the incident contract; extend tests/domain/display/relationshipChronicle.test.js with an unknown-token row proving the fallback is a sentence, not a token.

*Gate:* npx vitest run tests/lint/vocabularyTotality.walker.test.js tests/domain/display/relationshipChronicle.test.js; then npm run check before landing.

### CAR 4 — The surface: what this relationship survived, in the War door

Mount car 2's read model as a section beside PerspectiveStandings in the Herald's War door (HeraldBody.jsx:229-251), reusing that file's own observer-picker idiom so the two read as one desk: PerspectiveStandings says what X's standings ARE, this says what they SURVIVED. Rows name both settlements through RealmEntityLink (the NEWS ADDRESS LAW, as HeraldRemembrance.jsx does), date each line with tickCalendarDetailLabel, and render NOTHING at all when the campaign has no recorded history — the estate's empty-state discipline (a young realm is news; a dormant system is not). ⛔ THE SECRETS SEAM IS THE DESIGN DECISION HERE: incidents carry no visibility field and the 24 writers include corruptionWeb / espionageGauntlet / informationStatecraft, so the covert rows must be gated fail-closed on the existing convention (PerspectiveStandings.jsx:57-59 / viewerSecrets.js:48), never rendered to a shared or gallery path. A new file spelling a raw premium-tier comparison REDS tests/lint/premiumGateSingleSource.test.js's exact census, so either import the single source or add the census row with its reason class in the same commit. Add no Herald door: the nine-door list and its conditional tenth are a ruled structure (RealmInspector.jsx:80-102, :113).

*Files:* src/components/map/RelationshipChronicleSection.jsx, src/components/map/HeraldBody.jsx, tests/lint/premiumGateSingleSource.test.js

*Tests:* New tests/components/relationshipChronicleSection.test.jsx: the section renders the pair's names and dated lines; the covert row is absent without ground truth and present with it (the fail-closed negative control); a campaign with no history renders nothing; extend tests/components/warFaithSurfacing.test.jsx so the War door still surfaces both sections.

*Gate:* npx vitest run tests/components/relationshipChronicleSection.test.jsx tests/components/warFaithSurfacing.test.jsx tests/lint/premiumGateSingleSource.test.js tests/build/heraldRegisterDoorsLazy.test.js tests/domain/display/visibilityAudit.test.js; then npm run check (which includes npm run build and verify:dist, the only place the first-paint and lazy-chunk budgets are honestly measured).

### CAR 5 — The NPC trail: the ladder's contests and seat transitions, DM-gated

The record's other half — "who this NPC betrayed" — has a durable archive nothing displays: npcLadderState's contests (:608/:645) and seatTransitions (:717), read today only by the engine (treatySuccession.js:247). npcStates themselves carry no trail (npcAgency.js:422-560), which is exactly the roadmap's "NPCs have a thinner trail", so DO NOT MINT ONE — render what is already persisted. Extend npcInteriorityRead's existing DM-truth block (it already composes bonds, grudges and credibility from the same sidecar, :68-106) with a dated contest/seat-transition trail, and render it in the block npcComponents.jsx already mounts. Same includeGroundTruth gate, fail-closed by default; zero new persisted state, which is the module's own stated constitution ("a READ-MODEL, never new state", :5-6).

*Files:* src/domain/display/npcInteriorityRead.js, src/components/new/npcComponents.jsx

*Tests:* Extend the existing npcInteriorityRead coverage: the trail appears only under includeGroundTruth; an NPC with an empty ladder record yields no trail section rather than an empty heading; ordering is deterministic.

*Gate:* npx vitest run tests/domain/display/ (the npcInteriority arm) plus the npcComponents component test; then npm run check before landing.

### CAR 6 — Refresh the roadmap record to what the tree now shows

Land LAST, after cars 1-5 are green. The file's eleven section bodies still read [next] on items its own header block (lines 7-24) and REGIONAL_ENGINE_AUDIT.md:172-173 say landed in June — the unpaid chore COHESION_REMEDIATION_PLAN.md:285 already names. Flip the section markers to [done] with the pins the header block already cites, mark 4d landed against the cars above, and leave 4a's status exactly as it stands: built in-repo, DEPLOY NOT DONE AND NOT OURS (docs/DEPLOY.md:259). Record the two things a later reader would otherwise re-find: that the durable archive is six fields and not the two 4d names, and that relationshipMemory's recentMemory is a 24-tick decay window that must never be mistaken for the chronicle.

*Files:* docs/world-pulse-roadmap.md, docs/COHESION_REMEDIATION_PLAN.md

*Tests:* None beyond the docs pins the gate already runs (tests/docs/deployRunbookFreshness.test.js is untouched by this car — no function directory changes).

*Gate:* npm run check (docs pins run inside it). No focused suite applies.

## OWNER-GATED, KEPT OUT

- THE DEPLOY ITSELF — `npx supabase functions deploy generate-chronicle` (docs/DEPLOY.md:259) and the 33-function cutover it belongs to, plus setting ANTHROPIC_API_KEY in the Supabase dashboard (:285). The function and its test are in the tree; pushing them to a project is the owner's Codespace act and no car touches it.
- THE PAID SURFACE'S BEHAVIOUR — CHRONICLE_COST = 2 credits (supabase/functions/generate-chronicle/index.ts:31, pinned to src/config/pricing.js and the spend_credits CASE by tests/config/pricing.test.js), the daily-capacity 503, the per-user 429 and the refund path. No car changes a price, a cap or a refund rule.
- ANY PERSISTENCE-SHAPE CHANGE TO relationshipState — adding a visibility marker to incidents, raising any of the six caps (8/12/24/24/24/6 at relationshipState.js:472-502), or minting a new durable chronicle archive. Every car is a READ of what is already persisted. If the secrets seam in car 4 turns out to need a per-incident visibility field, that is an owner decision and the car stops at the fail-closed gate instead.
- CONVERGING OR WIDENING ANY PREMIUM GATE — tests/lint/premiumGateSingleSource.test.js states in its own header that converging these surfaces 'would be a behaviour change on paid surfaces, which is owner-gated'. Car 4 adds a census row or imports the single source; it does not move who sees what on an existing surface.
- ADDING A HERALD DOOR — the nine-door list plus the conditional Wanderers door is a ruled structure with its own presence contract (RealmInspector.jsx:80-113). Car 4 mounts inside the existing War door deliberately.
- RAISING ANY SIZE CEILING — scripts/.size-baseline.json is shrink-only by its own header ('Never RAISE a number and never ADD a file without a decomposition-is-infeasible reason'). None of the touched files carries a baseline row today; if a car would push one past its layer ceiling, decompose or stop — never add a row.
- ANY MIGRATION APPLIED, ANY PUSH, ANY DATA DELETION, ANY TUNING SIGNATURE, AND THE GOLDEN FREEZE — untouched by every car; no engine byte, no rng call, no persisted write is proposed.

## RISKS

- ⛔ THE DECAY TRAP, THE ONE THAT WILL BITE. buildRelationshipPostures.recentMemory looks like the finished chronicle and is not: memoryEntry returns null when weight <= 0 (relationshipMemory.js:133-137), and weight is zero for undated rows and anything older than 24 ticks (:15, :114-130). An implementer who reuses it will ship a 'what this alliance survived' panel that silently forgets everything the alliance survived. Car 2's mandated negative control (a 40-tick-old turningPoint that MUST still render) is the guard.
- THE SECRETS SEAM IS UNMARKED IN THE DATA. Incidents carry no visibility field, and the 24 src/domain writers include corruptionWeb, espionageGauntlet and informationStatecraft. Anything rendered without a fail-closed gate leaks DM truth onto a share/gallery path. Use viewerSeesDmSecrets (viewerSecrets.js:48) or the includeGroundTruth convention, defaulting to hidden, and keep tests/domain/display/visibilityAudit.test.js in car 4's focused set.
- ENGINE TOKENS IN FRONT OF A READER. titleForType (relationshipMemory.js:74-76) only replaces underscores, and humanizeEngineTokens.js has no incident bucket. Without car 3 the surface prints 'coalition betrayal' and 'label proposal applied' as if they were English. The vocabularyTotality walker's own header (:15-19, :24-27) forbids curing this by deleting the guard.
- THE RELATIONSHIP KEY IS NOT A PAIR. relationshipKeyFromEdge returns edge.id when the edge has one (relationshipState.js:363-369), so 'rel.a.b' parsing works until it doesn't. Resolve settlement names from the edge or the regional graph, never from the key string.
- SAME-COMMIT REGISTRY OBLIGATIONS. A new src/domain/display -> src/domain/worldPulse import pair reds tests/lint/couplingInclusion.walker.test.js unless a CW-0 registry row lands in the SAME commit (walker :108; baseline tests/lint/.coupling-inclusion-baseline.json, which may only shrink). New test files bill the test census (scripts/.test-ratchet-baseline.json); re-freeze with npm run test:ratchet:update as the LAST step of a landing, and touch no package.json byte.
- A NEW PREMIUM SPELLING REDS AN EXACT CENSUS. tests/lint/premiumGateSingleSource.test.js freezes the exact set of files spelling a raw tier comparison, in BOTH directions — a new one reds, and a stale row reds too. Car 4 must either import src/lib/viewerAuthority.js's viewerCanAuthor or add its census row with a reason class, in the same commit.
- FIRST-PAINT AND LAZY-CHUNK BUDGETS. Herald door bodies mount through lazy() and are pinned by tests/build/heraldRegisterDoorsLazy.test.js; a static import of a new section into an eager chunk tips a first-paint ratchet, and those ratchets are only honestly measured by npm run check (build + verify:dist), not by a focused vitest run.
- THE GATE IS SERIALIZED AND ANOTHER LANE IS ON THIS MACHINE. Every test script routes through scripts/gate-mutex.sh; a full npm run check queues behind whatever holds the lock. Plan the landing around one mutex acquisition, not six.
- SCOPE CREEP INTO PERSISTENCE. The natural next thought — 'just add a visibility flag to incidents' or 'keep a proper chronicle archive' — is a persistence-shape change and therefore owner-gated. Every car here is a read; if the design needs a write, stop and record the question rather than minting a field.
- THE ROADMAP DOC LIES TO A SKIM READER TODAY (eleven [next] markers on landed work). Anyone who reads section bodies instead of the header block will re-open closed items. Car 6 exists to end that, and must land after the others so it is not a promise.

*Estimate:* M
