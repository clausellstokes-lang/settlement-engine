# Brief — SEAM train, car 3h: THE RUNG-4 KEYS EXPOSED — the defense desk's key tables made readable so DS-DEF-2 leaves the dark set (a wiring car; zero text)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 3h. Dock: `$SC/laneSEAM` after cars 3a–3g. Read first: SITTING §P.2-28 (the ruling: DS-DEF-2 is a WIRING car — one key function, one read set `["court","prison"]`, 22 of 26 pools on rung 4: 11 local-key-builder + 11 value-lookup), the MEASURE receipts §§ CAR 0e (refusal 1: the shape of the 22), the census module's four-rung ladder (`src/domain/prose/wiringCensus.js`: LITERAL · TEMPLATE · TABLE · everything else UNRESOLVED), ARCH §3.6 ("the 256 rung-4 keys need the composers to expose their key tables — a wiring car per desk, chair-decidable, zero text").

## What to build
1. In `src/domain/display/stateProse/defenseStateProse.js`, expose the key tables the five threat-assessment rows build locally (`beastsRowPoolKey`, `invasionRowPoolKey`, `internalRowPoolKey`, the economic band map, `disasterRowPoolKey`) as module-level FROZEN tables in the shapes the census's TABLE rung already reads (`Object.freeze({...})` or the pair-array form car 10 taught it), with the key functions reading those tables — BEHAVIOUR-IDENTICAL: every key the desk returns for every input is byte-identical before and after (an executed A/B over the RATE corpus's 768 towns AND the DRIFT corpus, the manifest drift `[]`). No key string moves; no pool moves; no text moves.
2. Re-run the census (`node scripts/wiring-census.mjs`, `--check`): DS-DEF-2's rows move UNRESOLVED → RESOLVED with a predicate each (expect 22 → up to 26 resolved on the block), the headline RESOLVED integer moves (318 → N; declared in the receipt and the walker's assertion updated as a declared row — the census's shrink-only UNRESOLVED ratchet moves DOWN, lawfully), `attach` coverage recomputed (DS-DEF-2 may leave the cannot-attach set — print). Every arm the census walker holds stays green; the eleven-module island fence untouched (the composer desk is product code and is NOT in the island — this car edits a composer file for the first time in the sequence: the manifest's zero-drift is the proof that no reader sees a change).
3. Proof: the manifest drift `[]`; the A/B key identity over 768 + 1,050 rows; the census walker with its declared-row update; the defense desk's own tests; the whole `tests/lint`; strict on its ceiling; voice E2; OSR/PN exact; typecheck; the byte ratchets (the desk is not a leaf; its effective lines printed against the 800 ceiling).

## Discipline, fences, commits — as car 3a's
Change only `defenseStateProse.js` (tables and the key functions' reads), `docs/content/wiring-census.json` (regenerated), `tests/lint/proseWiringCensus.walker.test.js` (the declared-row assertions), the defense desk's tests if a table is asserted, the receipt. Commit `SEAM car 3h: the defense desk's key tables exposed — DS-DEF-2 leaves the dark set, zero drift`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha; the A/B identity figure; the census line before → after; the manifest line; anything refused with its measurement.

## ADDENDUM (chair, 2026-09-08 14:1x, after cars 3b–3g's verification) — the tip, and the census rule generalised
- **Your tip is the chair's register car on top of `efb5111cd`** (the census re-taken after twelve stamped shas moved with no row) — read `git -C $SC/laneSEAM log --oneline -3` and start there.
- **THE CENSUS RULE, GENERALISED (vetoable):** a drift whose `git diff -U0 -- docs/content/wiring-census.json` lines (after dropping the `+++`/`---` header) are confined to the `stamp` object — `producerIndexFiles`, `candidateLeaves`, `stamp.files` shas — with NO row, fill, total, tier, grain or rate figure moved, is a lane's plain re-take at its tip: print the diff line count, the count outside `stamp` (must be 0), and the byte lengths in the receipt and the commit body, then `node scripts/wiring-census.mjs` and commit the JSON in the same commit as the change that moved the sha. A drift that moves ANY row or figure is the chair's (an INSTRUMENT shift ruled at the sitting) — REFUSE it with the diff. This car moves a desk (defense) and possibly the census module's rung classification: if 22 of 26 DS-DEF-2 pools move from rung 4, that IS a row move and it is the DECLARED purpose of this car — commit the census with the moved rows and print the before/after RESOLVED integer and the per-row rung deltas; that declared move is pre-ruled at SITTING §P.2-28 and needs no refusal.
- **Standing hazard from 3g:** `src/components/tabs/EconomicsTab.jsx` is at 596 effective lines against a hard 600 ceiling; this car does not touch it. `writerReach`'s inventory moved when OverviewTab's `stresses` was memoised (3g reverted it to a plain const) — do not memoise a value the writer-reach walker inventories; if you must, REFUSE with the measurement.
- The judgment calls of 3b–3g stand: the fill scanner reads the composed call shape (`tests/helpers/dossierComposedFill.js`); the six leaves' `readings` param widened to `…|null|undefined`; the 3f-0 fence (no desk mints a producer-index key).
