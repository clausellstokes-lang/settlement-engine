LANE 22 — the domain-side relationships reader + its governed migration (the LAST car; cut at the final consist tip; the chair FAST-FORWARDS)
Worktree: lane-gates-store; branch fix-relationships-domain-reader-2026-09-19 at EXACTLY the consist tip (given at launch).
Facts: lane 17 fed the PDF's relationships.network through src/components/new/relationshipsDeskRead.js (component-side) because a domain reader mints identities: `neighbourNetwork on settlement` is banked (explained, save-time-writer); `interSettlementRelationships` is written (7 ordinary rows since schema 4); `crossSettlementConflicts` has ZERO writers in src — conflicts are DERIVED at render by generateCrossSettlementConflictsDeterministic (src/generators/crossSettlementConflicts.js), never persisted.
The ruling: the reader moves to src/domain/display/stateProse/relationshipsDeskRead.js; it reads neighbourNetwork + interSettlementRelationships (persisted) and DERIVES conflicts by calling the deterministic generator (a domain→generator import is allowed? check layerBoundaries; if not, the reader takes the conflicts as an argument computed by the caller); printProse.js reads it directly (no component import); RelationshipsTab reads the same module; the component-side file is deleted. Then the register: the plain ratchet names what the domain reader minted; `neighbourNetwork` growth → the reasoned write (tagged); `interSettlementRelationships on settlement` → an explained-writer row (mechanism save-time-writer, writer = the file gate 0 proves, ruling 'ODQ §934.9(1)/§934.16') through the schema-22 migration bundle (the schema-21 chain as precedent: legacy-leaf → report → decisions → bundle → --write --migrate-schema --migration-review=<bundle>), subjectSha = the cure commit on this branch; NO read of crossSettlementConflicts as a persisted key (it is derived). Prove: plain exit 0 exact; observedShape* + bank literal + sentinel; statePrintParity (the saved-world fixture arm still non-vacuous); layerBoundaries; tests/pdf whole once. Two cars (the reader; the migration), parent = the consist tip, so the chair can fast-forward. If the validator refuses anything the brief does not answer, STOP and report.
