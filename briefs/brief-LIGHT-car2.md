# Brief — the LIGHT train, car 2: THE FOLD'S CURES — the reconciliation marker stripped, the census date, the ordering arm, the RATE arm made to assert, the dark-dial mint arm, and the receipt corrected (chair, 2026-09-08 22:5x; SITTING §S)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-light.md` § CAR 2 (and corrections B1–B8 applied IN PLACE, each marked `[corrected at car 2: …]`). Dock: `$SC/laneLIGHT` at `990a7860a`. Read first, whole: `$SC/skeptic-light/FOLD.md` (every section) and SITTING §S in `$SC/s12-sitting/SITTING-RULINGS-912.md` (the rulings bind you — C1 says STRIP), then the three lens files it cites for the exact lines.

## The code cures, in this order (one commit each)
1. **A1 — `src/lib/importReconciliationAdmission.js`, `dropReconciledSettlementContentRecords`:** strip `LIVING_CONTENT_LAW_CONFIG_KEY` from `entry.settlement.config` alongside `customContentProvenance` and `customContentRoster` (the destructure shape `importScrub.js:158-166` already ships — reuse it, do not mint a second). Arm: a new case in `tests/lib/importReconciliation.test.js` (today 0 marker assertions) driving a LIT-product-minted world (the wiring test's fixture from car 1e) through admission and asserting the marker ABSENT and the roster absent; a plant that removes the strip reds by name. Blast radius re-run: `importReconciliation` 18 → 19+, `accountImportSlice` 26, `accountSettlementContentPortability` 17, `importScrub` 13. The module's docblock names the rule and cites `importScrub.js:115-121`.
2. **A2 — `tests/lint/.lighting-census-baseline.json:29`** `"date": "2026-09-09"` → `"2026-09-08"` (the refreeze's true date per `measuredBy`, the commit body and the receipt); the walker stays 34 passed; the diff is one numeral.
3. **A3 — `tests/lint/densityCreateBoundary.walker.test.js`:** the payload arm becomes an ordering/live-branch check — for each of the seven awaiters the `await loadGenerationLawPayloads(` must precede the pipeline call (or the worker's `runGeneration`) inside the same function body, not merely appear in the file. Arm: a plant that MOVES the await below `runGeneration` in `settlementGenerateAction.js` reds by name; the clean tree passes at all seven.
4. **A4 — `tests/domain/livingContentPromiseBytes.test.js`:** the RATE arm asserts `verdict.v1` / `verdict.v2` per row and counts `v1Roster`, so the two RATE cells the receipt prints are measured by the file that prints them (5 → 6 tests or 5 → 5 with two assertions — say which; the §917 test-census prediction moves with it).
5. **The dark-dial mint arm:** the mint's `{}` branch is executed by nothing at the tip (both dark drives `vi.doMock` the function away). Add one arm that drives `newSettlementLivingContentLaw()` through the real function with the dial at the DEFAULT (a module-level constant you can vary only by re-import: use `vi.resetModules` + an env/argument seam if one exists — if the dial can only be flipped by editing the source line, REFUSE with the measurement and record the gap; do not add a runtime switch to the product).
6. **B1–B8** applied in place in `$SC/receipt-light.md`, marked; then a new § CAR 2 section with every arm's tail.

## Discipline, fences, commits — as car 1's
The runner count in its own shell (the TASTE mechanics lane runs beside you in `laneTASTE`); one focused file at a time; the whole `tests/lint` once at your tip; strict on its ceiling; typecheck; OSR exact, no write; prose-numerics exact; eslint 0; no em dash / bang / toFixed in `src/` literals; explicit staging; no register `--write` (the lighting census by ritual only if a title moves — A2 is a one-numeral edit of the baseline JSON by hand, committed as a register car with the reason, NOT a re-freeze); the writer-reach `--write` stays the chair's. NOTHING under the prose island, the state-prose desks, the leaves or the census. THE PROMISE re-run after A1 (`livingContentPromiseBytes` and `livingContentLawWiring`). Commits `LIGHT car 2a: the reconciliation boundary strips the foreign birth law …` / `2b: the lighting census date …` / `2c: the payload arm checks order …` / `2d: the RATE arm asserts what it prints` / `2e: the dark-dial mint arm` / `2f: the receipt corrected at the fold's eight lines`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: LIGHT` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the shas; A1's arm and blast-radius tails; the A3 plant; A4's count; the mint arm or its refusal; the corrections applied by number; the whole `tests/lint`, strict, typecheck, OSR at your tip; anything refused with its measurement. Porcelain 0 and runners 0 when you return.
