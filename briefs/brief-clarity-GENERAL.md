# LANE: THE GENERAL LEAF RE-CUT TO THE OWNER'S CLARITY STANDARD (rulings 42, 42a, 42b, 42c, 42d) — a LINE EDIT under the CLAIM FREEZE

DOCK: `$SC/kit/lane-clarity-GENERAL` (a worktree at 7992713d0 — CUT IT YOURSELF as above; porcelain 0 before every commit). NEVER PUSH. NEVER `git stash`/`checkout`/`reset --hard`. `/usr/bin/grep`.
THE CHECKPOINT LAW: append one line to `$SC/kit/RESUME-NOTE.md` just above the line starting
`**+ 09-14 04:0x — ✅ BATCH 4 OF 4 LANDED` after EVERY commit (sha · pools · figures); if you die, the next
chair reads it. Trailer on every commit: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
READ FIRST: `$SC/kit/briefs/brief-block-clarity-recut.md` (the standard, the freeze, 42d and its failure
mode) · `$SC/kit/briefs/brief-clarity-all-prose.md` (the programme and THE GATE) · the batch-4 commit body in
the DEF-2 dock, `git -C $SC/kit/laneRW-DEF2 show 7992713d0 --format=%B -s` (what went wrong and how it was
caught) · `$SC/kit/rewrite/clarity-sweep.mjs` (your instrument).

## THE MATERIAL
`### DS-GEN-1..23`, `### DS-HK-1`, `### DS-CND-1`, `### DS-SUP-1..3` — the general leaf. ⛔ DS-REL-1 and DS-REL-2 also live in the general leaf but were
ALREADY CUT by the warFaith lane in its own dock — DO NOT TOUCH THEM (the consist merges the docks); your `--section general` wave-gate diff will
show their rows unmoved. THE CHAIR'S COUNT (widened sweep): 23 blocks, 123 pools, ~452 spines, 36 of them in the inline form (GEN-5, GEN-6); ⚠ DS-GEN-3 shows 7 pools and ZERO spines to every instrument — a THIRD authoring form; find it on your first read, cut it, and name it in your report so the instruments can be widened. Count them yourself at your first read with the widened `clarity-sweep.mjs` (it now sees
the `[x · dm-only]` tag AND the inline pool form `**key** — 1. `[tag]` …`). Shipped v1 prose in the three stances with engine slots kept
byte-for-byte; some general blocks are the dossier's OVERVIEW tab — the most-read page in the product — so this leaf is the one the owner sees first.
The standard is the owner's, line by line: one fact per sentence, no clause explaining the one before it, one claim a reader can act on, plain modern
words, the speaker's stake in the present tense at the town's scope for [street]/[visitor]; a [ledger] spine is the archiver's bare fact.
⚠ LEARNED ON TWO LEAVES: CERTAINTY IS A CLAIM (a hedge made absolute is a moved claim — "very hard" is not "nothing", "generally" stays,
"meaningfully" stays, "not X so much as Y" is not "Y and not X", a capability is not an occurrence); a QUANTIFIER and a SCOPE BOUND are claims;
volitional `will not` is REFUSAL, write "refuses to"; a generic sentence may not become a claim about this town unless the previous sentence asserts
the antecedent; a simile struck for 42c is a 42b sharpening — declare it or keep a word that keeps both readings; where a 42a split would leave a
bare copula standing, rejoin inside the sentence; `at once` / `no longer` read as formerly-constructions to C3; `the record says` raises F25; a
42a split can move a pool's census `grammars` size (rejoin); the projection contract's DETERMINER-RUN positive control wants a determiner within
two words of a slot (a split near a slot can lower it — restore the phrase inside the split, never the floor). A READER AUDIT of every changed unit
follows your landing and the chair cures what it finds; the power leaf moved 32 claims in 189 with every instrument green — read each line back
against the shipped one asking: same certainty, same quantifier, same scope bound, same modality, same actor, same tense.

## THE CLAIM FREEZE, absolute
A re-cut spine makes the SAME CLAIM as the spine it replaces: same tense (habitual stays habitual), same
actor, same scope (the town stays the town), same quantity, same certainty. Words, shape, length and
vocabulary may change; the fact may not. The engine's move grammar is read from the words: run the sweep
after every pool and put back any CONSEQUENCE/ABSENCE the cut dropped (inside the sentence, not as the
explaining clause); never add PROVENANCE or HISTORY; never lose a LEVEL1 order id. 42b sharpenings (a vague
spine made definite) are the ONE exception — collect them in the commit body with old → new → the reading
and why the pool's own key supports it. NO SPEAKER DELETED where a stance carries one.

## HOW TO RUN IT
Batches of roughly twelve pools by block order (GEN-1 upward, then HK, CND, SUP); as many batches as that makes, READING THE WHOLE BATCH
before cutting a word, so the shapes vary across pools and not just within one. One commit per batch:
`REWRITE CLARITY general, BATCH n of N: <blocks> — …`. After each batch, THE GATE from the programme brief
(the sweep · both --check · the five suites · the wave gate `--section general --out $SC/kit/lane-clarity-GENERAL/.packets/wave-general-batch<n>.json`
diffed against a HEAD run you make FIRST at batch 1 · measure-block per block). A LEVEL1 order lost, a
PROVENANCE added, a census grammar moved, the pin moved, or owned FAIL/WITHHELD above 0 STOPS the batch until
it is cured by re-cutting the offending unit — never by re-recording anything.
REPORT per batch, in the commit body: units touched / left alone · the 42b list · words before/after · the
sweep's line (shifts, orders lost) · the proxy before/after · the wave gate's new-vs-gone findings by arm ·
the five figures per block · the ten lines you are least sure about. At the end: a one-paragraph verdict
on whether the power leaf now reads to the owner's standard, and what it still lacks.
