# Brief — the TASTE, car M-9: THE GATE JUDGES THE MODIFIER, NOT THE SPINE — per-pool verdicts scoped to the writers' own faces and the join; the spine's inherited findings reported apart (chair, 2026-09-09 01:5x)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-taste.md` § CAR M-9. Dock: `$SC/laneTASTE` at its HEAD (the draft-round commits sit on top of M-8d; read `git log --oneline -5` — do NOT touch the annex or the packets; your car is the harness and its walker only). Read first: `scripts/taste-measure.mjs` whole, `src/domain/prose/composedWalker.js` (how a finding names its SITE — spine piece vs modifier piece vs the joint), `$SC/receipt-taste.md` § M.7 and § M.10, and the three draft-gate verdicts the chair pulled from the workflow journal (`$SC/taste/gate-verdicts-r1-r3.txt`).

## THE FLAW (measured by the chair from the journal)
Round 3: the projector accepts every writer's row verbatim on all seven pools and the band-depth measures have cleared, yet every pool reads `inBand=false` because the composed walk's unit verdicts are WITHHELD by findings on the SPINE's own clause — the shipped sentences the writers cannot touch: arm Q "a trailing coordinate naming no second field" on `stone keeps itself, and wages do not.` (WALLED-STRAINED), on `built work stands on its own patience.` (WALLED-QUIET), on `whether that is confidence or thrift, the openness is itself a statement.` (UNWALLED-LARGE), on the DS-DEF-2 disaster spine; F25 "a cited record's content" on `the books say` (UNWALLED-LARGE); A3 on `rather than in the luck` / `rather than in what the hall issues`. Those are the pre-rewrite corpus's known state (car 5's sampled walk: FAIL 40 / WITHHELD 75 / PASS 85 of 200) and the REWRITE's job, not the taste's. A pool can never reach the bands while its spine is withheld, so the loop is dry by construction.

## What to build (one commit)
1. **Scope the per-pool verdict to what the writers own.** For each taste pool, `inBand` and `failing[]` count ONLY findings whose site is the modifier's own piece (its faces, its slots, its band measures, its sibling distance) or the JOINT (the connective, the seat, the thread between spine and modifier, A2's relation licence). Findings whose site is the SPINE's piece are collected under a new `inherited[]` per pool — printed with the arm id, the spine key and the quoted clause — and NEVER counted against the pool. The unit verdict line is printed in both grains: `unit: WITHHELD n` (the whole unit, as today) and `owned: FAIL a · WITHHELD b · PASS c` (the writers' grain the gate reads).
2. **A pool that has zero owned findings and every face inside every band is `inBand: true`** even while its spine carries inherited findings; the JSON says so on the row (`inheritedCount`, `spineWithheld: true`).
3. **Arm Q's vocabulary finding, printed not cured:** `stone keeps itself, and wages do not.` is the owner's exemplar line and is lawful — the spine reads the pay gate (`economicGates.military`) and `wages` names it; arm Q flags the coordinate as naming no second field because its field vocabulary does not map `wages` to the gate. Print, per inherited Q finding, whether the clause's nouns map to a field the spine's `reads` holds under the census's synonym table (if the census has none, say so) — a REPORT for the sitting, no arm changed (the arm is car 5's and the fix is the REWRITE's or a 5d; the chair rules).
4. **The walker arm:** a fixture unit whose spine carries a planted Q finding and whose modifier is clean reads `inBand: true` with `inheritedCount: 1`; the same fixture with a planted modifier finding reads `inBand: false` with the finding under `failing[]`; a joint finding counts as owned. The measure JSON's shape gains `inherited[]` and `owned` and nothing else moves (the `--dry`/`--check` interlocks and the M-10 first-table figures re-run byte-equal where the scope does not touch them).

## Discipline, fences, commits — as car M's
The runner count in its own shell (the LIGHT lane may be building beside you); never the whole suite; the whole `tests/lint` at your tip; strict; typecheck; OSR exact; no em dash / bang / toFixed in `src/` literals (the harness is `scripts/`, but the walker is `tests/`); explicit staging; no register `--write`. Touch ONLY `scripts/taste-measure.mjs`, its walker test, the receipt. Commit `TASTE car M-9: the gate judges the modifier and the joint, and reports the spine's findings as inherited`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: TASTE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha; the re-run of `--arm draft --round 3` at the current annex state with the seven pools' `owned` verdicts and `inBand` values; the inherited findings per pool with the Q-vocabulary report; the walker's plants; anything refused with its measurement. Porcelain 0 and runners 0 when you return.
