# LANE: DESK-TIMEBAND-2 — wire the chair's four duration-free variants into DS-GEN-9's silent event-type arms
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## READ FIRST
`$SC/briefs/_PREAMBLE.md` · `$SC/timeband-words.md` (THE CHAIR'S FOUR SENTENCES — ship them VERBATIM; if one fails an instrument, STOP and report which; the chair rewrites, never the lane) · `$SC/receipt-desk-timeband.md` (finding 5, the four silent arms, the measurement scripts under `$SC/tbwork/`) · `$SC/receipt-desk-gen2.md` (how GEN2 edited this annex and regenerated the pools).

## YOUR DOCK
`$SC/laneTIMEBAND`, detached at **`e2a135483`** (DESK-TIMEBAND's 2 cars over the desk consist tip `a2af55cde`, sealed `desk-timeband-2026-09-05`). Build ON TOP; never rebase; the chair replays `laneTIMEBAND:a2af55cde` at §900. ⛔ Never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content. ⛔ **VITEST HOLD IS IN FORCE** until the chair sends RESUME: run NO vitest; record every proof as OWED-UNTIL-RESUME with its exact command.

## THE ACT
1. In `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, DS-GEN-9's `event type:` blocks: append the chair's variant 5 to the FOUR pools named (economic, religious, political, demographic), in the block's own numbered form with its `[persona · anchored-dimension]` tag exactly as authored. Touch no other variant, no §0d table, no other annex. If the annex carries a per-pool `requiredSlots`/SLOTS line, extend it for variant 5 with `{event} {settlement}`.
2. Regenerate the pools the estate's way (the generator GEN2 used — find it by `grep -rl RECEIPT_POOLS_DOSSIER_STATE scripts/`; the `.generated.js` under `src/data/dossierStateProse/` must be produced by it, never hand-edited). Prove the regenerated file differs ONLY by the four new variants (a diff you paste into the receipt).
3. Move every pin the four variants move, with attribution: the walker's `requiredSlots.length === pool.length` (4 → 5 ×4), any pool-size census, any per-arm survivor count DESK-TIMEBAND pinned (`tbwork/measure3` — the four arms must now have ≥ 1 survivor on an old subject; re-run that measurement and pin the new survivor counts). Keep every `it()` title unchanged.
4. Prove the drawer honours the anchored dimension: an `anchored: true` record never draws a `not anchored` variant and the reverse (a plant on each side). Prove on the 48-town corpus that the four arms' darkness falls (report the before/after per arm; the `anchored === undefined` 3/48 stays dark by ruling — do not touch it).

## FENCES
No `{timeband_*}` fill; no ladder change; no persisted shape; no register act (record deltas: prose-numerics, lighting titles if any moved, sizeBaseline if the generated file crosses a ceiling — report, do not bank). Commit per step, `Seat: Opus 5 — Fable-unvalidated`, `Lane: DESK-TIMEBAND-2`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-desk-timeband-2.md`, PARTIAL header first, RETROVALIDATION ROW last, tip sha last.
