# LANE-EM-COMPILE-2 — compile a NEW member packet at the current tip, READY-able in one pass (Opus COMPILE lane; successor chair, session 7d3418f8, 2026-09-19)

**Seat:** Opus COMPILE. You compile ONE new member packet of the settlement-editor family to the estate's standard, measured against the CURRENT tip, so the chair can promote it without a separate pre-proof. You implement nothing and run no gate. **Standing law:** `briefs/LANE-EM-COMPILE.md` (chair kit) binds whole — its path law, "navigate by symbol", "a fact without a command is not verified", "exact contracts, no discretion", "never adjudicate: a refuted premise is BLOCKED with the smallest measured contradiction" — AND every numbered step of `briefs/LANE-EM-PREPROOF.md` (steps 1–10: the J-T1 facts at the tip; no stale absolutes — register predictions are DELTAS; registration obligations priced; ⭐ the BUNDLE BUDGETS priced, with the cure being the PLACEMENT before it is the ceiling; the edge-shared INPUT membership; cite the design and the charter by SECTION + ITEM, never by line; ⭐ SIMULATE THE POST-EDIT VALIDATION — a symbol the packet moves or deletes is a `retiredSymbols` row, and every other LANDED packet's rows for the same pair must be discharged). Substitutions:

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · kit = `$SP/chair-kit-923472dc` (READ-ONLY).
- **The tree you read (and may import from with plain `node`):** ONLY `$SP/read-tip-58fcfe614` (detached at `58fcfe614`, the build branch's tip; `node_modules` symlinked; confirm the HEAD). The family's authority documents are IN that tree: `docs/DESIGN_EDIT_MODE_AND_DECREES.md` (§12 governs; §20–§22.1 are today's rulings), `docs/ARCH_EDIT_MODE_AND_DECREES.md`, `docs/implementation/charters/EDIT-MODE-TRAIN.md` (your packet's row is in its amendments sections), `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256 `b90a95b7af484137ecf70bd15cde5054edf66b5db0d9f6e90c974d97b7caa5e1`; §P2 rows 10–11), `PACKET_STANDARD.md`, `PACKET_TEMPLATE.md`. The ledger's newest charter amendment (the sequencing of 2026-09-19 17:0x) is read via `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/implementation/charters/EDIT-MODE-TRAIN.md`.
- **You write ONLY** under `$SP/lane-em-compile-<ID>-scratch/` (create it): `<ID>.md` (DRAFT, version 1, the twelve template sections; Verified base and Last revalidated left as `__BASE__` with the revalidation sentence written for the chair; the preamble hash left as the chair's stamp but verified and quoted), `<ID>.manifest.json`, `<ID>.evidence.md`, `<ID>.compile.report.md`. FORBIDDEN: editing/staging/committing anywhere; `$SP/consist`, `$SP/lane-em-b3b` (a build lane holds it), other lane dirs; vitest, eslint --fix, npm scripts, builds. Plain `node` on scratch scripts is allowed (ONE process at a time). Write your files early and re-write them after every finished section.
- **Models to copy for shape:** the LANDED `docs/implementation/packets/settlement-editor/EM-B3a.md` (a small security/veil packet, version 3, with a priced-budgets subsection and a K-gate executed before the first edit) and the READY `EM-B1d.md` (a walker packet with a narrowed override and a measured matcher).

## Report (final message; also the report file)
DRAFT (READY-able) or BLOCKED; the file paths; the measured facts that shape the contract; the budget table; the bundle / edge-shared measurement; the predicted register moves as DELTAS; the requiredSymbols list with the post-edit simulation's verdict per row; every question only the chair can answer. Every claim CONFIRMED (quoted command) or PLAUSIBLE.

> CORRECTION (the chair, 2026-09-20, from the EM-R0c compile lane): the preamble's SHA-256 above is re-stamped to the live one (`b90a95b7…caa5e1`, row 12 present); re-measure it yourself at your tip and STOP if it differs. And every inherited harness you copy from the kit (`instrument.mjs` and kin) must take its TREE from an environment variable you set explicitly, with NO default — a dead lane's copy pointed at an older tip and a successor would have quoted its figures.

> THE EM-R FAMILY'S TOTALITY WALKERS (the chair, 2026-09-20, after EM-R0a's pre-proof): a `tests/lint` totality walker's declared sets are IMPORTED from their producer, never re-typed (`contractTestAntiVacuity` Rule 2 convicts a hardcoded exhaustive claim); every parked-file prediction prints `parkReasonsFor` and the packet bans a re-bound `it`/`test`/`describe` (the census credits only files whose opener is bound by their own `vitest` import); `contractTestAntiVacuity.walker.test.js` and `tests/lint` whole are §10 instruments of every member that CREATEs under `tests/lint`; the packet's `SURFACE_ROOTS` count is SIX.

> STEP 15 CORRECTION (the chair, 2026-09-20, from EM-R0d's pre-proof): `stamp.producerIndexFiles` in the wiring-census stamp is a NUMBER (1,171 at 141a1d775), not a list — 'count 0 of mine' is proved by reading the census's producer roster, never by counting a number's members. And the first-paint set is read by IMPORTING `vite.config.js`'s `EAGER_FIRST_PAINT_MODULES` (268 live); a replica seeded before the `ENGINE_SHARED_DOMAIN_EXCISIONS` reads 283 and is wrong.

> THE STATUS ROW (the chair, 2026-09-20, from EM-R0d's pre-proof): `parsePacketHeader` anchors `- **Status:** \`DRAFT\`` at END-OF-LINE — the value stands ALONE on its line (whitespace only after the closing backtick), and `status` is taken only when exactly one row matches. A stamp appended to that line (`BLOCKED …`, `READY-able …`) parses as `status: null` and fails validation at placement for a reason unrelated to content. Put every stamp on the CONTINUATION lines beneath the Status row, never on it; the chair's placement scripts write the Status value alone.

> GREP LAW (the chair, 2026-09-20, from EM-B1f's pre-proof): `git grep -E` silently ignores `\s` (no error, no match) — a population counted with `\s` under `-E` is undercounted. Use a POSIX class (`[[:space:]]`) or `git grep -P`; when a grep count contradicts a claim, re-run it with the other engine before calling the claim refuted.

> THREE LAWS (the chair, 2026-09-20, from EM-R0c's and EM-P2's reports): (1) SEED LOOPS — a `tests/domain` (or any `tests/`) CREATE never writes a bare `for (const seed of …) { expect(…) }`: `seedLoopTotality` convicts it and its habitat is shrink-only; collect (`for (const c of rows) …`) then assert once. (2) THE PREAMBLE HASH IS MEASURED, NEVER QUOTED — compute `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` at your read tip and cite THAT; a hash a brief quotes is stale the day the next amendment lands. (3) A HARNESS PRINTS THE TREE IT READ from its environment, never a literal label; and every unquoted shell expansion is a false-green risk (a `set -- $pair` in a verification loop printed UNCHANGED while `shasum` errored) — quote everything.
