# LANE: CAP-TRIP-907 — the capacity tripwires made HONEST (a not-executable channel, a reachability guard, one mis-refused row re-measured, one false header corrected)
⟦Chair: Fable 5.1 (session 405b5e7e) · Lane: Opus 5 (`model: "opus"`) · repair lane, product bytes in `scripts/soak/` + `tests/soak-harness/` + one test comment · dock `$SC/laneB6` @ 4243bdc61 (detached; clean; node_modules 453 symlinks — never npm install) · chartered at §907 from HORIZON-B6's receipt (`$SC/receipt-horizon-b6.md`, M1-F1 and M3-F1) and the skeptic's confirmation (`$SC/skeptic-m1f1/` and the chair's record below)⟧

## THE FINDING YOU ARE CURING (CONFIRMED twice, by independent execution)
`capacity_plateau` and `capacity_floor_thaw` (`scripts/soak/tripwires.mjs:239, :243, :276, :278`) key on `receipt.yearlyPopulations` / `receipt.yearlyDiedFlags`. NO written receipt carries either field: `scripts/audit/whole-world-soak.mjs` builds them per run (`:389-390`, `:489-490`, `:555-556`) and `receiptBody` (`:1013`, written at `:1163`) ships only `startPopulations`, `finalPopulations`, `finalDiedFlags` (`:1074`; the comment at `:1071-1073` says the flags "were computed and then discarded; now they ship" — the FINAL row ships, the series does not). Both detectors hit `Array.isArray(…) ? … : []`, take the `< 150` / `< 100` early return and answer `[]` — a silent clean. `evaluateTripwires` (`:364-375`) returns `{ findings, observability }` and has NO not-executable channel; the receipt-level `notExecutable` (`scripts/soak/evaluate.mjs:63`) is a writer-populated field, so the receipt grades `fullInstrument: true` while two of its three capacity rows are structurally blind. On the B6 receipt (`$SC/capacity/artifacts/research-lit-4s.cases/research-lit-4s-300y-4s-seed1.json`) the series rebuilt from `behavioral.yearly[].stateVectors[id].population` makes `capacity_plateau` convict all four settlements (8319@149 vs 10869@299; 202 vs 1017; 324 vs 244; 38 vs 159). The unit pin (`tests/soak-harness/tripwireRegistry.test.js:161, :172, :197`) plants the field on a synthetic receipt — the fixture is the ONLY writer of the shape; its comment at `:186-188` documents the silence it cannot tell from a pass.
ALSO MEASURED (the skeptic): `capacity_envelope_30y` was REFUSED at C3 (`d02c5acde`; `tripwires.mjs:225-230` says the receipt has "no `motion` block at all") — but the real receipt carries `behavioral.yearly[29].motion = { populationTransitions, populationMoved, … }`, written by `scripts/audit/behavioral-observation.mjs:1016-1018` since `37459391a` (2026-07-28), five weeks BEFORE C3. The refusal's mechanism was false when written.

## THE CARS (each its own commit in the dock, each with the trailers below; RE-DERIVE every line number above before editing — a brief figure is a hypothesis)
1. **CAR 1 — the NOT-EXECUTABLE channel.** `evaluateTripwires` returns a third channel (`notExecutable: [{ id, reason }]`); a detector whose keyed field is ABSENT from the receipt declares NOT-EXECUTABLE with the field named, never `[]`; `scripts/soak/evaluate.mjs` folds the evaluator's not-executables into the receipt-level ledger so `fullInstrument` is false when a deterministic row cannot run, and `mintRefusals` says so. Extend the registry pin: an absent field → NOT-EXECUTABLE (assert the reason text), a planted field → the existing behaviour. MEASURE on the B6 receipt before and after: `fullInstrument` true → false, `deterministicFirings` unchanged at 1, the two rows listed under notExecutable. No frozen register cell exists (both genesis cells UNFROZEN — confirm by reading `tests/soak-harness/.soak-register.json`), so no banked verdict moves; state that in the receipt.
2. **CAR 2 — the structural guard (the class, not the instance).** In `tripwireRegistryDefects` (or beside it) an arm that TEXT-READS each row's `detect` source for `receipt.<field>` / `receipt?.<field>` reads (and any documented accessor idiom you find) and asserts each field against the key set `receiptBody` writes (text-read from `whole-world-soak.mjs`, the estate's walker idiom — import no writer module). The two blind rows are BANKED as the arm's baseline (a ratchet that only shrinks: `unreachable: 2`; a third refused). Prove the arm with a negative control: plant a row keyed on `receipt.doesNotExist` in a test copy and watch it fail.
3. **CAR 3 — `capacity_envelope_30y` re-measured.** Correct the false refusal comment with the measured truth (the block exists; writer, sha, date). Then MEASURE what the row would say on the B6 receipt if armed (run it in a scratch copy first). Reinstate it ARMED only if its firing on the B6 receipt is explainable from the receipt's own figures and the roster becomes the design's predicted 12 rows / 10 deterministic; otherwise leave it refused with the corrected reason and hand the reinstatement to the chair with the measured firing. Either way the receipt states the before/after roster and `deterministicFirings`.
4. **CAR 4 — M3-F1.** `tests/domain/demographicsEnvelope.test.js:19-23`: replace the header's four figures with the measured 170 of 180 (share 0.9444), spread 0.4846, Elderfen year 12 — RE-MEASURE them yourself by running that one test file and quoting its output; if your run disagrees with the B6 receipt, report both and change nothing.

## LAWS
- ZERO product-behaviour bytes outside `scripts/soak/`, `scripts/audit/whole-world-soak.mjs` (comment/ledger fold only — you do NOT ship the series: that is the owner's schema row §907), `tests/soak-harness/`, and the one test header. No register `--write`. No tuning value moved. No golden re-recorded. STATE, NEVER FATE.
- Fast gate per car: the focused files (`npx vitest run tests/soak-harness/tripwireRegistry.test.js` and the soak-register/evaluate tests you touch; `tests/domain/demographicsEnvelope.test.js` for car 4). NO unfiltered `npx vitest run` (the chair's gate). Capture every exit in-shell before any pipe; a 143/137 exit is a harness kill.
- Commits in the dock: stage explicit paths, never `git add -A`/`-u`/`.`; never stash; each message ends with the trailers `Seat: Opus 5 — Fable-unvalidated` and `Lane: CAP-TRIP-907`. No push.
- Receipt `$SC/receipt-cap-trip-907.md`, created at arrival (STATUS: PARTIAL) and re-written at every car: the arrival check (HEAD 4243bdc61, porcelain 0, 453 packages), per car the diff summary, the fast-gate output quoted with its exit, the measured before/after figures, CONFIRMED/PLAUSIBLE per claim, and a RETROVALIDATION ROW listing every judgment call you made (chair ratifies or refuses at landing). Every timestamp from `date`.
- If a car proves impossible or its measurement contradicts this brief, refuse it WITH MEASUREMENT in the receipt and continue with the others.
