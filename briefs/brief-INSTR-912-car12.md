# Brief — INSTR-912 car 12 (and 12b if the census moves): the whole-suite proof's four new reds — forty-four em dashes inside the island's string literals, a dangling enforcer pointer, an untagged completeness claim, one un-anchored negative

Seat: Opus 5 — implementer. The chair (Fable) rules; you build and measure. Receipt: append `## CAR 12` to `$SC/receipt-instr-912.md`. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`.

## Where
- Dock: `$SC/laneINSTR2` at **983a37f26** (twelve commits over f3ab08f51; porcelain 0). Never touch any other dock or the main tree. `$SC/HOLD-VITEST` exists: you are EXEMPT for the focused runs below and for ONE whole `tests/lint` run at the end; never the whole suite.
- Read first: `$SC/whole-914.log` lines 34–200 (the six failed tests with their exact messages — three are banked and yours are the other three plus one), `$SC/s12-sitting/SITTING-RULINGS-912.md` §M/§N (nothing changes), the memory of the owner's law: NO EM DASH and no exclamation mark inside any string literal under `src/` (the Tier-2 voice ratchet `tests/copy/voiceMechanics.test.js` E2 counts them per file, shrink-only; the ceiling on files over baseline is a BANKED 2 — `labelBands.js` and `generalStateProse.js` — and cannot be raised).

## The four reds (the other two — the golden master's 525 drift and enforcement-claims' six banked naked claims — are known and not yours)
1. **voiceMechanics E2 — four island files over their per-file baseline:** `src/domain/institutions/institutionTable.js` em 13 · `src/domain/prose/entryGround.js` em 2 · `src/domain/prose/entryWalker.js` em 6 · `src/domain/prose/grammarWalker.js` em 23 (bang 0 everywhere). Rewrite every em dash INSIDE A STRING LITERAL in those four files (and check the other six island modules — `entryLexicons`, `moveGrammar`, `plantLedger`, `presenceMeasure`, `proseFingerprint`, `wiringCensus` — for any the arm did not list because they sit at their baseline of 0) with a colon, comma, full stop, semicolon or parenthesis; comments keep theirs (not counted). Measure with the test's own `stringLiteralContents` idiom before and after (em → 0 in each file; literal count unchanged so no literal was dropped). **Where a message text is ASSERTED by an arm** (a walker's finding label, a plant's expected title, a manifest label, a `check_caught` 4th argument, a control's expected string), update the assertion in the same car and say so — labels of the mutation plants are join keys: if a plant's LABEL carries an em dash, keep the label byte-identical (labels are in `scripts/`, not `src/`, and are not counted) and change only the `src/` string. ARM: `npx vitest run tests/copy/voiceMechanics.test.js` — the E2 per-file arm still fails (banked) but names exactly TWO files (`labelBands.js`, `generalStateProse.js`); paste its per-file lines.
2. **enforcedByExists:** `tests/lint/proseMeasures.walker.test.js:70 -> tests/lint/plantLedger.walker.test.js` — a header pointer to a file that does not exist (car 9 fixed the `src/` side; the test header kept the old name). Re-point to the real enforcer (the file itself, or whichever file carries the arm). ARM: `npx vitest run tests/docs/enforcedByExists.test.js` green.
3. **enforcement-claims, the per-claim freeze:** `src/domain/prose/plantLedger.js:10` — a completeness claim (the docblock's "0 problems" sentence) with no resolvable `@enforced-by`. Cure by tagging it with the arm that enforces it (`@enforced-by tests/lint/proseMeasures.walker.test.js`) or by rephrasing so it is no longer a completeness claim — read the test's docblock for what counts as one. ARM: `npx vitest run tests/docs/enforcement-claims.test.js` — the banked six-claim arm still fails, the per-claim freeze arm passes; paste both lines.
4. **negativeAssertionAnchor:** one NEW un-anchored negative assertion in the test corpus (the walker prints the file and line — read `whole-914.log` after line 163; it is almost certainly a car-11 or car-10 arm). Anchor it in the estate's idiom (`expectAbsentWithAnchor` or the file's existing anchored form). ARM: `npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js` 9/9.

## Discipline, fences, proof
Runner count 0 before every vitest run (`V=vit; V2=est; pgrep -fl "$V$V2" | grep -v gate-mutex | wc -l`; take the count in its own shell call). Focused files one at a time: the four arms above, then the seven lane suites (institutionTable · proseWiringCensus · proseEntryContradiction · proseMoveGrammar · proseMeasures · proseRegisterLoaders · mutationCoverageManifest — every tally must equal car 11's), then `npx tsc`/the typecheck ratchet, then the OSR dry and prose-numerics dry (both exact), then ONE `npx vitest run tests/lint` (whole directory) — paste its summary; the only lawful reds there are none (the DOOR 3 timing arm under contention is re-run alone if it appears). Change only the island's `src/` modules (string literals and the one docblock tag), the lane's test files (assertions that mirror changed strings; the anchor; the header pointer), `scripts/mutation-sweep.sh` and the manifest ONLY if a plant's expected-title argument mirrors a changed string (labels byte-untouched), the receipt. No product surface, composer, pool text, persisted shape, seed input, no register `--write` except the lighting door (12b, by the ritual, only if a title moved). One car `INSTR-912 car 12: the whole-suite proof's four reds — forty-four em dashes leave the island's strings, <clause>`; explicit staging; porcelain 0; the three trailers exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
Return: the car sha(s); per file the em count before → after and the literal count unchanged; the four arms' lines; the seven tallies; the whole-`tests/lint` summary; the OSR and PN dry lines; the lighting tuple if 12b; anything owed.
