# LANE: VOICE-JSX — the 34 em dashes in 15 mainline components (the Tier-3 voice debt no train owns)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-class reader-prose cure · display-only; a DECLARED text shift if any string reaches an engine hash⟧

## THE FINDING (PROSE-REBASE receipt §5, sealed `refs/preserve/prose-rebase-receipt-2026-09-05`)
`tests/copy/voiceMechanics.test.js`'s Tier-3 JSX arms are red on MAINLINE debt: 526 `.jsx` scanned, 17 with debt, **em 37
vs budget 6, 15 drifted files** (`AccountIdentitySection`, `FoundersHallPage`, `HeraldGazetteer`, `PerspectiveStandings`,
`WarTab`, …), byte-identical before and after the prose car. The committed Tier-3 baseline says em 3, so
`UPDATE_VOICE_BASELINE=1` REFUSES the rise and the refreeze half-applies. Two of the six known-failure census rows are
these arms.

## THE ACT
1. Enumerate the 34 with the test's own helper (`tests/helpers/jsxLiteralWalk.js`) — file:line:literal.
2. ⛔ **Grep for PARSERS before curing anything**: a `"<Band> — <Condition>"` string a consumer `split`s is a CONTRACT
   (see the prose STOP: 13 such strings were mis-cured and 186/360 tiles broke). Any literal that is parsed, hashed
   (`grep -rn Fingerprint src`), or used as a key is REFUSED with its site named.
3. Cure the rest in the house voice (`RECEIPT_POOLS_FAITH.md` §D: plain, concrete; em dash → period / colon / comma /
   a re-joined clause), one car; every cured literal listed in the message.
4. Prove: `voiceMechanics` Tier-3 arms go GREEN (em ≤ 6, drifted 0) WITHOUT setting any `UPDATE_*`; `proseLeak` green;
   the components' own tests; `dossierFiveTabs`-class snapshots — if a snapshot moves, it is a DECLARED display shift
   (list them; do not re-record — the chair does, at the landing); `tests/lint/` WHOLE; eslint.
5. Receipt `$SC/receipt-voice-jsx.md` with the refused list and a RETROVALIDATION ROW.

## FENCES
No register act. No `--amend`. Trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: VOICE-JSX`. Quiet-window + mutex.
