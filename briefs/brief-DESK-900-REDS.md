# LANE: DESK-900-REDS — attribute and cure the non-register reds of the §900 composition
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## THE MEASUREMENT (chair, whole suite, no filter, at the 54-car composition `8b9031f4c`, `whole-900a.log`): 49 failed arms in 26 files.
Register-class (the chair's doors cure these at the final tip — NOT yours; read them only to attribute): `writerReach` (13), `tuningRegister` (2), `proseNumerics` (2), `observedShapeReaders` (2), `sovereigntyLightingContract` (1), `dossierMountRegistry` (1, the mounts baseline), the three edge `freshness`/`reproducibility` suites (5, the re-mint ritual), `enforcement-claims` + `generatorGoldenMaster` (banked). `impactKindWalkers` (2) and `couplingInclusion` (1) are DESK-900-CARS's cars 1–2.
**YOURS — attribute each to the dock that introduced it (run the same file at each candidate dock's tip: `laneHOR2` 6d4a00039, `laneENC4B` 74a5cbeca, `laneSEAT78` 027cf668d, `laneCHARSET2` 3cd85c62c, `laneDOCKET2` 6097b4efd, `laneECON2` 333b1a83c, `laneDEF2` 468663f56, `laneINTEG-tree` a2af55cde, `laneROSTER` 7871d1622 — and at the product tip 38474a59e), then cure at cause or hand up with the reason:**
| file | arm | first suspicion (test it, do not trust it) |
|---|---|---|
| `tests/domain/treatyOrientationWr10g.test.js` | the lit arm changes no draw: the walker corpus is byte-identical beside the lit one | ⭐ HIGHEST: a seeded draw MOVED — the pool-length law (a pool grew) or a registry/kind change; find WHICH dock, and whether it is a same-seed shift THE PROMISE governs |
| `tests/build/engineChunkLazy.test.js` | pipeline metadata is loaded by the generation core, never through an eager import | HORIZON-B2's re-anchor (`laneHOR2`); may need the build the chair runs — say so if it does |
| `tests/store/settlementSlice.test.js` | generateSettlement snapshots all four live bags into the pipeline config | HORIZON-B2's generate action |
| `tests/store/toggleSlice.scope.test.js` | exactly ONE definition and FOUR call sites | HORIZON-B2 (a fifth call site?) |
| `tests/domain/contributionLedgerShape.test.js` | the triple bijection at 28 | SEAT-78's `irregularForceEnabled` flag (+1 → 29): a pin move with attribution, or a registry omission |
| `tests/domain/subsystemRowsVirtual.test.js` | the flag domain is ENUMERATED FROM THE REGISTRY, and its arithmetic closes | the same flag |
| `tests/domain/militaryStrength.test.js` | the dial-gated set is DOUBLY CONTROLLED | SEAT-78's dial |
| `tests/domain/economyStateProseDesk.test.js` | holds every desk fill table in the tree to its slot's declared shape | ECON2 / DOCKET item 8's casing class (`{chain}`/`{good}`) — DESK-900-CARS car 6 may be curing it; coordinate by reading its receipt, do not double-cure |
| `tests/data/dossierStateProseProjection.contract.test.js` | holds the three `canonical` variants against the live headlines they copy | a headline moved (DOCKET-3's band words? a desk?) |
| `tests/soak-harness/coveringArrayCoverage.test.js` | the producers ran and the write population is not empty | the OSR corpus pass or DOCKET's `--preset` |
| `tests/docs/enforcedByExists.test.js` | (read the log) | a doc anchor a lane moved |
| `tests/lint/negativeAssertionAnchor.walker.test.js` | no NEW un-anchored negative assertion; the exact per-file row | a lane's `.not.` without the `// anchored:` line IMMEDIATELY above (DOCKET-3 learned this) |
| `tests/copy/voiceMechanics.test.js` | per-file debt / per-file JSX debt exactly matches the baseline | ⛔ if it GREW it is a fix (an em dash in a new file), never a refreeze; if it FELL the chair banks it |

## YOUR DOCK
`$SC/laneDESKPROOF`, detached at **`8b9031f4c`** (clean; the 54-car composition). Build your fix cars there; the chair replays `laneDESKPROOF:8b9031f4c` onto the §900 dock. ⛔ Never rebase; never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content. Vitest through the mutex only; the whole `tests/lint/` at your tip at the end with every red attributed.

## FENCES
No register act (no `--update`, `--write`, `UPDATE_*`, refreeze doors). No cure of a register-class red. A cure that would change same-seed rendered output STOPS for the chair (THE PROMISE). One car per cause, `Seat: Opus 5 — Fable-unvalidated`, `Lane: DESK-900-REDS`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-desk-900-reds.md`, PARTIAL header first, an ATTRIBUTION TABLE (file → dock → cause → cure/handed-up), RETROVALIDATION ROW last, tip sha last.
