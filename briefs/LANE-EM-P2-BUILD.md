# LANE-EM-P2-BUILD — the dispatch brief (Opus build lane; given at 13:45 EDT 2026-09-19; re-use verbatim on a re-dispatch)

Role: Opus BUILD LANE for EM-P2 under SEALED DISPATCH. Worktree: `<SP>/lane-em-b3b`, checked out ON `fixes-2026-09-18-consist` (the slot; the chair's `<SP>/consist` is DETACHED). Scratch: `<SP>/lane-em-p2-scratch/`. Forbidden: `<SP>/consist`, the ledger checkout, the chair kit (read-only), every other lane dir; never `--ignore-other-worktrees`, never `git add -A`, never `git checkout --`/stash/reset. zsh: explicit file arguments.
Read whole first: the packet `docs/implementation/packets/settlement-editor/EM-P2.md` (version 2 — RE-PINNED to EM-P0's landed ONE-registration shape: `generatePopulation.js` holds one `registerStep`, module-local `drawPopulation`/`derivePopulation`, `chooseOrPin(pins, key, draw)` keyed on the RECORD PATH), `src/generators/steps/generatePopulation.js`, the preamble (verify its SHA-256 against the header), `PACKET_STANDARD.md`, `habitForkRegistry.js` + `chooserTotality.walker.test.js` (copied, NEVER edited), `entropyRootCensus.walker.test.js`, `mutationCoverage.shared.mjs`; the EM-P0 receipt in the kit's findings for the §12 shape.
Verbs in order: (1) `npm run implementation:dispatch -- EM-P2` (branch name must equal the verified branch; ancestry from d86aabae6); stop on any mismatch. (2) §8: CREATE `src/domain/generation/generationForkRegistry.js`, CREATE `tests/lint/generationForkRegistry.walker.test.js`, TEST-edit `tests/lint/entropyRootCensus.walker.test.js` (figures re-measured BY EXECUTION), REGISTER one row in `scripts/mutation-coverage-manifest.json` SURGICALLY (never re-serialise whole); `git status --short` = exactly those four paths. (3) §10 through the mutex, shared tier: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- <cmd>`; quote test counts; NEVER `npm run check`; never poll by hand. (4) The lighting census: DO NOT REFREEZE; run `sovereigntyLightingContract.walker.test.js` once separately, EXPECT RED, record the tuple (predicted 2647 / 383 / 2264 / … / …; any other delta = STOP). (5) Goldens: `UPDATE_GOLDEN`/`GOLDEN_SHIFT_SIGNED` FORBIDDEN; SHA-256 of both fixtures before/after, byte-identical. (6) `npm run check:packet -- EM-P2` then `npm run implementation:resume -- EM-P2`, both exit 0 BEFORE any commit. (7) ONE commit on the branch by explicit pathspec of the four paths — the chair grants commit authority for this landing; message `EM-P2: generation's choosers registered in a sibling registry keyed on the record path (train EM-T3, member 1)` + body (row count, scanned-set size, census figures before/after, goldens unmoved, the measured lighting delta) + `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; `git show --stat HEAD` names exactly the four paths.
STOP (uncommitted, `<scratch>/EM-P2.STOP.md`): any `src/generators` or existing-instrument edit needed; a golden moves; an `outputKey` that does not resolve; symbol resolution against exports; census figures not reproducible; `chooseOrPin` absent or a second `registerStep(`; a lighting delta other than +1/+0/+1; the sealed verbs refuse; §3 exceeded.
Judgment left to measurement: whether the scatter `rng.random()` in `linkFactions` (~`generatePopulation.js:101`) earns a `checklist` row keyed `factions[].powerFactionName` (`cardShape: 'faction'`); `relationships`/`conflicts` get NO row. Rows spell `forkId`/`module`/`symbol`/`reason` as `habitForkRegistry.js` does; `outputKey` = the RECORD PATH; `symbol` resolves against SOURCE (`pickFirst` in `npcGenerator.js` must resolve).
Report = the packet's §12 filled by execution, also written to `<scratch>/EM-P2.receipt.md`; every claim CONFIRMED (executed) or PLAUSIBLE.
