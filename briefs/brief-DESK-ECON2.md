# LANE: DESK-ECON2 — the economy desk's 8 dark blocks + the `{complexity}` fill (DESK-1 remainder, DESK-7, DESK-10, DESK-11)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-class · the eleven words are the CHAIR's, already authored — you wire them⟧

READ FIRST: `$SC/briefs/_PREAMBLE.md` → `$SC/briefs/_DESK-LAW.md` → `dossierMounts.js` WHOLE → `stateProseKernel.js` WHOLE →
**`economyStateProse.js` WHOLE (33 KB — the first desk; read `ACCESS_NOUN`, `bareCommonFill`, the `{faction}` finding, and
the `complexityDisplay` seam comment)** → `EconomicsTab.jsx` + `EconomicsGlance.jsx` → `tests/domain/economyStateProseDesk.test.js`
+ `tests/ui/economicsTabFlow.test.js` → the economy corpus leaf → the annex `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`
(§0c-3 THE COMPLEXITY BAND — OPEN, THE CHAIR'S; and the DS-ECO-8 reasoning) → **`$SC/desk7-words.md` — the chair's eleven
forms, the authority for `{complexity}`.**

## YOUR DOCK
`$SC/laneECON2` detached at **`940d161ca`**. Porcelain 0 at start. Sibling desk lanes edit `dossierMounts.js` in parallel —
one id per line, append at the end, your leaf only.

## THE FACTS (re-measure)
- Dark in the economy leaf at `940d161ca`: **DS-ECO-4, DS-ECO-5, DS-ECO-7, DS-ECO-10, DS-ECO-11 · DS-SUP-1, DS-SUP-2, DS-SUP-3**
  (8; the desk mounts DS-ECO-1, 2, 2, 3, 6, 8, 9, 12 — read each row's rung and why). DS-ECO-11 alone carries 17 pools / 53
  variants. EconomicsTab was 580 effective (20 free) at O2GATE — a Glance extraction is the expected shape.
- **DESK-7:** `{complexity}` is dark because `deriveEconomicComplexity` emits eleven title-cased display strings, none
  `bare-common`; DS-ECO-1's C1 pool collapses to 1 of 3 (every high-rung working settlement prints the same sentence).
  Wire `COMPLEXITY_NOUN` from `desk7-words.md` exactly (no edits to the words — veto goes to the chair), assert the map
  TOTAL both ways against the producer's own strings (import the function, enumerate its outputs over the input space or
  pin the eleven literals and prove the function emits nothing else), fill the slot, and measure the per-pool eligibility
  after (C1 → 3 of 3 expected). Close annex §0c-3 in the same car with the measured figures.
- **DESK-10:** DS-ECO-8 (the prosperity rung read on its own) GLANCES on its tile because DS-ECO-1 already speaks about
  prosperity in the header; its 21 variants are mute. It owes a `sentence` position on a page that is NOT economics.
  Chair's ruling on the page: the PDF is out of scope for this lane; choose among the router's tabs the one with headroom
  and no competing prosperity sentence (measure `overview`'s effective lines first — it already carries 8+3 sentences),
  give the reason, and mount it as a real draw. If no honest home exists, record it as still-deferred with the measurement.
- **DESK-11:** `EconomicsGlance.jsx`'s docblock says "all three positions are GLANCE rungs" — it carries FOUR and one is a
  SENTENCE (`{header?.sentence && …}`). Fix the docblock; ride it on your first car.

## THE ACT
Cars in this order: (1) DESK-7 + DESK-11 (small; the words + the docblock); (2) the 8 mounts in one or two cars (Glance
extraction first if EconomicsTab is at the line); (3) DESK-10's position. Each: real draws, `publicDossier`-gated
(EconomicsTab already threads it — extend the same object, never a second gate), rendered-text proofs + the public
negative, tests extended in `economyStateProseDesk.test.js` and `economicsTabFlow.test.js` (a new test file only if a new
tab is involved — name it). Proof per `_DESK-LAW.md`; plant-out; receipt `$SC/receipt-desk-econ2.md` with predicted
register deltas (mounts baseline fall; prose-numerics relocations; lighting census; writer-reach false-darks).

## ⛔ FENCES
As `_DESK-LAW.md`. `Lane: DESK-ECON2`. The eleven words are not yours to change — a word that cannot be wired is a
finding back to the chair, with the sentence it breaks.
