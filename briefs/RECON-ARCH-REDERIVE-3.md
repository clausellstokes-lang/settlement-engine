# RECON-ARCH-REDERIVE-3 — measure the MERGE and its honesty cost (Opus RECON lane; successor chair, session 7d3418f8, 2026-09-19)

**Seat:** Opus RECON. You EXECUTE a prototype and report measurements; you implement nothing in any tree, compile no packet, run no gate. **Why you exist:** the second re-derivation recon (kit `findings/RECON-ARCH-REDERIVE-2.report.md` — read it whole) prototyped design §22 and found that the generator's READINGS are path-dependent: holding the final roster at every writer lets an edit reach the economy but makes a NO-EDIT re-derivation move a city's food security; holding it at its last writer keeps the record but starves the edit. The chair ruled **design §22.1**: *the consequence of an edit is the DIFFERENCE between two re-derivations* — `R0 = rederive(record)`, `R1 = rederive(record + edit)`, both under the every-writer seam, and the new record is a THREE-WAY MERGE with `R0` as base (a reading identical in `R0` and `R1` keeps the record's own value; a reading that differs takes `R1`'s). Read §22 and §22.1: `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`. You execute it and say where it is wrong.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

**Tree (READ + IMPORT ONLY):** `$SP/read-tip-a41a0e109` (detached at `a41a0e109`; `node_modules` symlinked; the build branch has moved on — you measure at `a41a0e109` and say so). **WRITE ONLY** under `$SP/lane-rederive-recon-3-scratch/`. Toolkit: `$SP/chair-kit-923472dc/tools/rederive-prototype-2/*.mjs` (the second recon's whole seam: `hook3.mjs`, `seam.mjs`, its `p3`/`p4`/`p5` scripts, the 63-row sample) — copy and extend; re-run its inertness control first. FORBIDDEN: editing/staging/committing anywhere; `$SP/consist`, `$SP/lane-em-b3b`, other lane dirs; vitest, eslint, npm scripts. Plain `node` only, ONE process at a time. Write the report file early and re-write it after every section.

## Measure
**Q1 — the merge, built.** A pure `merge(record, R0, R1)`: leaf by leaf over the READINGS; collections that carry a stable key (say which key, per collection — id, name, type…) merged BY KEY (added · removed · changed), order taken from `R1`; held facts are taken from the edited record untouched; receipts (the power fingerprint; anything else you find that is a digest of other fields) recomputed LAST over the merged record. Say precisely which record keys you treated as held, as readings, as receipts, and which you could not class.
**Q2 — no edit.** Over the 63 rows: `merge(record, R0, R0)` equals the record in 63/63 by construction — CONFIRM it by execution (a merge that fails this is broken), including the trace.
**Q3 — the eight edits** of the second recon (rename via the cascade · role · add · remove an institution · power share · the governing seat · terrain → desert · culture → norse) on a village, a town, a city AND a metropolis: which readings move; ⛔ does ANY reading the edit did not touch move (the merge's whole claim); does any unedited held fact move; dangling joins as a delta BY KIND; does anything throw; and one plain sentence of what a DM would see.
**Q4 — THE HONESTY COST.** For every leaf that took `R1`'s value: did the record ALREADY differ from `R0` there (settling riding along with a real edit)? Count by edit and tier, and show the three worst cases in plain words (e.g. "the DM removed the granary; food security went Secure → Strained, where the edit alone explains Pressured → Strained"). This number decides how the delta card must word itself.
**Q5 — internal consistency of a merged record.** A merged reading mixes record-valued and `R1`-valued leaves: find every place the record's own invariants or derived labels would now disagree (a band label beside a score it no longer matches; a total beside parts that moved; a list whose count field is stale). Measure over Q3's trials; this is the merge's most likely failure.
**Q6 — sequences.** Two edits in a row: is `rederive(S1)` equal to the previous `R1` (the cache claim), and does `merge` applied twice equal the merge of both edits at once? Three trials.
**Q7 — the two items the second recon could not settle:** draw-SKIP versus draw-CONSUME inside `assembleInstitutions` (it spends 3,324 draws over the 63 rows — does a real skip of a held producer move anything later inside that step or after it?), and what a NEWLY ADDED NPC looks like when `enrichNpcCoherence` is skipped for a held roster (design §22 ruling 3's newcomer: which fields is the newcomer missing beside its enriched neighbours?).

## Report (final message; also `$SP/lane-rederive-recon-3-scratch/RECON-ARCH-REDERIVE-3.report.md`)
1. The control. 2. Q1's classification of record keys. 3. Q2. 4. Q3's table. 5. Q4's honesty cost. 6. Q5's inconsistencies. 7. Q6, Q7. 8. **Where §22.1 is WRONG or incomplete** — smallest measured contradiction first; do not soften; do not adjudicate. 9. What you could not measure. Every claim CONFIRMED (quoted command + output) or PLAUSIBLE.
