# Brief — SEAM train, ARCH car 3a: THE SEAM, UNREACHABLE — the composer exists and nothing calls it

Seat: Opus 5 — implementer. Receipt first: `$SC/receipt-seam.md` § CAR 3a. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`. Dock: `$SC/laneSEAM` — a fresh detached worktree at the §915 CAS (the chair fills: 3b22b5c569e5c93f709f2a6057e3a0c280ff18ac (short 3b22b5c56 — the §915 CAS, `landing-measure-2026-09-08`; the census JSON, the manifest fixture and the byte ratchets are in the tree)); node_modules symlinked; no build.

## Read first, whole
`$SC/arch-prose/ARCH-COMPOSED-PROSE-v2.md` **§2.1–§2.4** (the piece model; the sibling `poolMeta` map; the key table — every hash the model mints; `drawFace` with the no-hash short-circuit; the two-level ruling), **§2.6**, **§4.1** (where the composer lives: pure, headless, no clock, no RNG, no settlement access, no lexicon detector, no `localeCompare`/`Intl`/`toLocale*`; imports the kernel and the three FROZEN leaves only; the DESK supplies keys and typed flags, the CORPUS supplies relations and roles, the COMPOSER everything else; the candidates leaves `*StateProseCandidates.js` per desk because the general desk sits at 699 of 800 effective lines), **§4.2** (the ten steps), **§4.3** (salience: three integer signals; the comparator law), **§4.4** (the bound), **§4.5** (connective choice), **§4.6** (coherence at the freeze and the gate, never at the draw), **§4.7**, **§12 row 3a** (builds · files · EXECUTED proof · predicted reds · doors), **§16** (the chair's addendum: the branch grain; the relation table is EMPTY of joins at this tip so every joint is `addition`; the floors); `src/domain/display/stateProse/stateProseKernel.js` whole (the draw at :301–305; `eligibleVariants`; `fillSlots`; the hash pair `fnv1a32` + `avalanche32`); `tests/data/dossierStateProseProjection.contract.test.js`; `tests/lint/couplingInclusion.walker.test.js:15-30, 100-125` (a new module needs a LAYER_PATTERNS family row or an argued row in the same commit); the MEASURE receipt § CAR 1 (the manifest's base-side `pieces` synthesis car 3a must equal).

## What to build (ARCH §12 row 3a; every item an EXECUTED arm)
1. **`drawFace(variant, blockId, poolKey, seed)` in `stateProseKernel.js`** exactly as ARCH §2.6: `faces = 1 + wordings.length`; `faces === 1` ⇒ return 0 with NO hash; `!seed` ⇒ 0 (canonical-at-zero); else `avalanche32(fnv1a32(seed + '::' + blockId + '::' + poolKey + '::w')) % faces`. Today every variant has one face, so the kernel's output is byte-identical by construction. ARMS: a one-face variant provably never hashes (spy or counter on the hash pair); a planted four-face variant draws face 0 seedless and 25 % ± 2 SE per face over 10,000 seeds; the suffix key is `::w` (the P-F10 nit).
2. **`src/domain/display/stateProse/composeStateProse.js` (new):** the pure composer of §4.1–§4.2 taking `{spineKey, candidates: [{key, change}], turns: [{key}], slots, seed, audience, dimensions}` and returning `{blockId, poolKey, angle, text, pieces[]} | null`; with an EMPTY candidate list and no turns its `pieces` MUST equal the manifest's base-side synthesis (`[{role:'spine', key: poolKey, vid, index, face: 0}]`) and its `text` the kernel's own draw — asserted against car 1's recorder on the DRIFT corpus (the manifest byte-identical). Salience, the bound, the connective draw and the arrangement are implemented per §4.3–§4.6 and driven by FIXTURES only (no desk calls them yet): the comparator law (ascending hash; ties by code-unit comparison; never a locale API — a lint arm bans `localeCompare`, `Intl`, `toLocale*` in the composer); the fact budget `k ≤ 3 − |spine.reads|` read from `poolMeta` (absent ⇒ spine); the seats (the sentence seat; the clause seat behind S2 — SIGNED at SITTING §N.1, so implement it BEHIND the relation-row licence: a clause seats only with a `consequence` row in the spine→modifier direction, and the relations leaf is EMPTY of joins today (§16 item 9), so no clause can seat on the shipped corpus — assert that too); the position budget (≤ 2 modifier-bearing rungs per mount) as a pure arrangement function; the audience filter at the CANDIDATE stage (§4.2 step 2); the drop-and-continue on an unfillable slot; `provenance.pieces` inside the object `drawnAtMount` strips.
3. **The six `*StateProseCandidates.js` leaves (new, one per desk)** — each exporting an ORDERED candidate function list (empty today) built by explicit calls, never by iterating `pools`; and the coupling family row for the new module(s) in `couplingInclusion.walker.test.js` IN THE SAME COMMIT (the predicted red otherwise).
4. **The import fence:** a lint arm asserting the composer imports only the kernel and the three frozen leaves (`dossierConnectives`, `proseNorms`, `dossierRelations` — which do not exist until car 4: import them as optional/empty today and assert the fence on the import LIST), and that NOTHING in `src/` imports the composer yet (reachable from nothing — the seam is unreachable). A planted eager import from a desk must red; a planted `localeCompare` must red.
5. **Proof:** the six data leaves sha-identical to the base; the DRIFT manifest byte-identical (`npx vitest run tests/property/dossierProseManifest.test.js` drift `[]`); `composeStateProse.test.js` (new) with every arm above; the composer's `pieces` for an empty list equals car 1's synthesis on all 1,050 rows; the whole `tests/lint` once; strict ratchet on its ceiling (write strict-clean); voice E2 two banked files; OSR/PN exact; typecheck; the cast walkers; the ten-module (now eleven-module) byte fence untouched (the composer is PRODUCT code and lives outside the island — it is display code, in `src/domain/display/stateProse/`, and it is reachable from nothing at this car).

## Discipline, fences, commits
As MEASURE's (runner count in its own shell; focused one at a time; one whole tests/lint; no em dash/bang/toFixed in `src/`; never the whole suite; never a build; no register `--write` but the lighting door as 3a-b). Change only: `stateProseKernel.js` (the one function added), `composeStateProse.js` (new), the six candidates leaves (new), `tests/domain/composeStateProse.test.js` (new), `tests/domain/stateProseKernel.test.js` (extend), `tests/lint/couplingInclusion.walker.test.js` (the family row), a lint arm file for the fence, fixtures, the sweep + manifest by text (plants for the new arms), the receipt. NO change to any desk, pool leaf, `dossierMounts.js`, `legibilityRung.js`, persisted shape or seed input. Commit `SEAM car 3a: the seam, unreachable — <clause>` (+ 3a-b); explicit staging; porcelain 0; the three trailers (`Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`). Return: the sha(s); the arms' outputs; the manifest drift line; the fence plants' reds; the whole-tests/lint summary; the ratchet lines; anything refused with its measurement.
