# BATCH-COMPOSER — the shared brief for an Opus lane that composes a BATCH GATE (the chair, Fable 5.1, session 4a1823e2, 2026-09-20; the owner's directive of ~15:4x, ODQ §934.47 addendum 117; the launch message names the batch, its worktree, its base, its members in order and their special handling)

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · kit (READ-ONLY) `$SP/chair-kit-923472dc`.

**Why you exist.** The owner ruled that lanes queued for the machine's one gate are COMBINED: several lanes' finished work is composed onto ONE throwaway batch branch cut at the green integration tip, and ONE full check proves them all at the composed tip — instead of each lane re-running `tests/lint` and its directories at a stale base. You are the composer: you APPLY other lanes' finished work faithfully, one commit per unit, under THEIR prepared subjects and bodies. You are not an author: you never redesign a member's change, never "improve" it, never resolve a same-region conflict by invention. Seat law: Fable architects, manages, judges and validates; Opus surveys, recons, verifies and implements.

**Read first, whole:** `$SP/chair-kit-923472dc/briefs/LANE-PARALLEL.md` (every verb and every addendum at its foot binds you — mutex spelling, never pipe a gate, eslint bare, pathspec commits, the Opus trailer, never amend, never the checkout/stash/reset/clean family, the foreign stash is untouchable), then `$SP/chair-kit-923472dc/BATCH-LEDGER.md` (the fit test and your batch's row), then the forecast `$SP/lane-queue-recon-scratch/QUEUE-RECON.report.md` — the per-lane row of EACH of your members and section H for your batch — then each member's own resume note, whole, before you touch that member.

## Your worktree
The chair created it: a dedicated worktree on a throwaway branch `batch-<n>-2026-09-20` cut at the base the launch message names; `node_modules` symlinked. It is YOURS alone. You never touch `$SP/slot-2` (the integration branch's worktree), `$SP/consist` (the chair's terminal), or any member's worktree except to READ it (`git -C <member worktree> diff`, `show`, `status`, reading files) — a member's worktree, index and scratch are never modified by you. Scratch: `$SP/batch-<n>-scratch/` (create it); every exported patch, copied file list, log and the receipt live there.

## The composing loop, per member, in the order given
1. **Verify the member's state by command** against its note and the forecast: branch, HEAD, own commits, `git status --short`, and the note's per-file SHA-256 list where it has one. A mismatch is a STOP for THAT member (skip it, record why, carry on with the next) — never guess.
2. **Choose the apply method the launch message names.** COMMITTED work: `git cherry-pick <sha>` in order; afterwards prove FILE-SET-EQUAL and, where the integration branch has not touched those files since the member's base, BLOB-EQUAL to the member's commit (`git rev-parse <sha>:<path>` vs `HEAD:<path>`). UNCOMMITTED work: for each prepared commit unit, take exactly that unit's paths — when the forecast says ZERO BASE DRIFT for a path (`git diff --name-only <member base> <batch base> -- <path>` prints nothing), copy the member's version of the file byte-for-byte (from its worktree, or from its INDEX with `git -C <member worktree> show :<path>` when the note says the staged version is the unit's); otherwise export `git -C <member worktree> diff <member base> -- <paths>` to your scratch and `git apply --3way` it. A conflict marker, a rejected hunk or a same-region overlap is a STOP for that member — record the region, restore your tree to the last good commit by committing nothing and removing only the files YOU just wrote (copy back from `git show HEAD:<path>`), and move on. A file a member keeps PARKED in its scratch is copied in from there, exactly as its note says.
3. **Run the member's STILL-OWED focused red-first proofs in sequence** (the forecast and the note say which are owed and which are already executed and quoted): tests first → red (quote the failing title and count line) → the cure → green. Plants are restored BY COPY from a banked file, byte-identical (sha before and after). Focused means explicit files — NEVER a whole directory: the batch's one full check is the whole-directory proof, and it is the CHAIR's act, not yours. Every vitest line goes through the mutex, SHARED tier, worker-capped, the two exports inline, output to a file in your scratch, exit captured from the command itself; if the lock is held by the chair's full check your line simply waits. `npx eslint <touched files>` BARE after each member.
4. **Commit the unit by explicit pathspec** under the MEMBER'S prepared subject and body (from its message files where they exist; where a body has a line for whole-directory receipts, write `BATCH GATE RECEIPT: proven by the batch's full check — see the chair's ledger addendum for batch <n>`), adding one line `Composed-By: batch <n> composer, from <member worktree>@<member HEAD or 'uncommitted tree'>`; trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never amend. `git show --stat HEAD` names exactly the unit's paths; `git status --short` is empty before the next unit.
5. **Registers are not yours.** Never refreeze the lighting census, never run an observed-shape `--write`, never open a golden or `UPDATE_*` door (a member that needs a door applies its own work as a MEMBER AGENT after you — the launch message says who). Record each member's declared register deltas in your receipt so the chair can reconcile its one refreeze.

## What ends your turn
A receipt `$SP/batch-<n>-scratch/BATCH-<n>.receipt.md` and the same content as your final message (the chair reads only that): outcome first — the batch tip sha and which members are IN, which were SKIPPED and exactly why; per member: the commits (sha, subject), the apply method used, the file-set/blob proof, each focused red-first with its verbatim red and green count lines, eslint's exit; the summed declared register deltas (lighting files · parked · credited · titles · suiteTitles per member; mutation-coverage rows; any other register a member names); the goldens' hashes at your first and last command (`tests/fixtures/generator-golden-master.json`, `tests/fixtures/dossier-prose-manifest-golden.json` — unmoved, or STOP); `git status --short` empty; and ⛔ everything noticed and not touched, each item specific enough to slot. End with the single line `BATCH <n> COMPOSED — tip <sha>` (or `BATCH <n> COMPOSED WITH SKIPS — tip <sha>`).

**STOP for the whole batch** (write `BATCH-<n>.STOP.md`, end your turn with its path): a golden moves; your worktree is not on the named branch at the named base when you start; a gate line prints no count; anything owner-gated (schema or persisted shape, security posture, paid-surface behaviour, new capability) surfaces inside a member's work that its note did not declare.
