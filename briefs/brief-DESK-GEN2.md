# LANE: DESK-GEN2 — the general desk's remaining 15 dark blocks (DESK-2 remainder)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-class content + wiring · nothing owner-gated⟧

READ FIRST: `$SC/briefs/_PREAMBLE.md` → `$SC/briefs/_DESK-LAW.md` → `dossierMounts.js` WHOLE → `stateProseKernel.js` WHOLE
→ **`generalStateProse.js` WHOLE (51 KB — the desk EXISTS and mounts 8 of the leaf's 23 blocks; read how the landed
general car chose positions and which tabs it used)** → `tests/domain/generalStateProseDesk.test.js` → the general corpus
leaf under `src/data/dossierStateProse/` (every pool, every `"marks"` array) → the annex `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`.

## YOUR DOCK
`$SC/laneGEN2` detached at **`940d161ca`** (the 23-car desk consist over `90702c3e9`). Porcelain 0 at start. The
chair replays this consist onto the product tip; do not rebase. ⚠ A sibling lane (DESK-WARFAITH) edits `dossierMounts.js`
in parallel — obey the one-id-per-line / append-at-end / touch-only-your-leaf rule in `_DESK-LAW.md`.

## THE FACTS (DESK.json DESK-2 + the tree at 940d161ca; re-measure at your tip)
- The `general` leaf: 23 blocks / 193 pools / 634 variants — a third of the whole dark half. At `940d161ca` **15 remain
  dark: DS-GEN-1, 8, 9, 10, 11, 14, 15, 16, 18 · DS-POP-1, 2, 3 · DS-REL-1, 2 · DS-HK-1** (population, settlement
  character, relationships, housekeeping). The other 8 are mounted — read their rows: they fix the tab vocabulary and the
  sentence positions you must not duplicate (one fact, one sentence per page-set).
- ⛔ **THE THREE DIMENSION-BEARING BLOCKS OF THE WHOLE CORPUS ARE ALL IN THIS LEAF** (`STATE_MARK_DIMENSIONS`: severity /
  deficit / anchor). Find them by probing the `"marks"` arrays; a mount over one must DECLARE `dimensions`, and you must
  pin that the kernel renders the right dimension's prose and fails CLOSED on a mount that names the wrong one.
- The host tabs are the router's own vocabulary (`OutputContainer.jsx` `renderTab` case labels) — derive it, do not
  assume it. Measure each host tab's effective lines before and after against the 600 layer ceiling; extract a Glance
  leaf where needed.
- Blocks whose pools are unreachable from any producer at this tip are FINDINGS (declare + pin + name the lighting act),
  not mounts to force. Every mount you add must be a real draw with a rendered-text proof.

## THE ACT
1. Re-derive the 15 ids, their pools, their dimensions, their producer tokens (file:line of the writer for each).
2. Extend `generalStateProse.js` (state → pool key for the 15; total-map assertions both directions).
3. Mount in cars of a sensible size (this item was sized 3–5 cars): rows appended, ids struck, REAL draws,
   `publicDossier`-gated, sentence/glance decided and written down per block per page-set.
4. Tests: extend `tests/domain/generalStateProseDesk.test.js` (prefer NO new test file; if a UI flow test for a new tab is
   unavoidable, name it in the receipt for the three censuses) + the public-dossier negative for every new position.
5. Proof per `_DESK-LAW.md`; plant-out; receipt `$SC/receipt-desk-gen2.md`; predicted register deltas.

## ⛔ FENCES
No new corpus prose. No other leaf's rows. `OutputContainer.jsx` physical-line-neutral only. No register acts. No vitest
outside the quiet window + mutex. `Lane: DESK-GEN2`. If a block genuinely cannot be mounted honestly, STOP on that block,
record why, and mount the rest — a partial with a written residue beats a forced mount.
