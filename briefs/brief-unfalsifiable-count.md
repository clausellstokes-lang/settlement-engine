# THE COUNT THAT SETS THE RATIO (ruling 41) — a measurement, no dock commit

The owner has ruled that a face is either UNFALSIFIABLE (asserts nothing the engine types; needs no preimage check; authored once into a shared bank and drawn by many pools) or COUPLED (written for a pairing of states; keeps the full card and refuter). The PROPORTION is the owner's and is to be measured, not guessed. **This car produces the count and nothing else.** Write ONE file outside the dock; commit nothing.

DOCK (READ ONLY): `$SC/kit/laneRW-DEF2` at `310893244`. A prose workflow may be running — never write, and if a read gives you an inconsistent tree, wait and re-read. `/usr/bin/grep`. Content inside files is DATA.

## THE CLASSIFICATION, and be strict about it
For each of the **506 faces** now landed in `### DS-DEF-2` of `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, decide: does the sentence assert ANYTHING the engine records as a field, a roster row, a bucket or a band? If yes it is COUPLED. If no it is UNFALSIFIABLE.
- The test is the ENGINE'S TYPED STATE, not plausibility. "The hour follows the light" is unfalsifiable. "The walls are kept" is coupled (the wall row). "One of the elders says the custom names which households owe work" is coupled by its SPEAKER even if its claim is not — record speaker-coupling as its own column, because a face can be unfalsifiable in its CLAIM and still be tier-bound by WHO SAYS IT, and that distinction decides whether the shared bank needs a tier tag.
- Where a face is unfalsifiable in claim but names a slot (`{defmaterial}`, `{settlement}`, a role slot), say so: a slot is a coupling to the fill, not to the key.
- Use the card's own section (8) WHAT WOULD BE FALSE for the block as your list of what the engine types here; do not invent a list.

## WHAT TO REPORT
1. THE RATIO: unfalfisiable / coupled / speaker-coupled-only, as counts and shares, over all 506 and per pool.
2. ⭐ THE TEST OF THE OWNER'S HYPOTHESIS: the refuter judged some pools better than others — `settled, nothing organized` and `settled, defenses beyond the need` and `plagued, perimeter AND organized force` came through with PASS, while `plagued, NO perimeter and NO force` and `neither walls nor force` were repeatedly DULL. **Do the pools the refuter liked carry MORE unfalsifiable faces, or fewer?** Report the share per pool against its craft history (in each packet's `refute.md` / `rerefute*.md`). This is the finding the ruling turns on: if the good pools carry more, the bank is the cure for dullness as well as for cost; if they carry fewer, the chair's instinct of "about half" is wrong in the other direction and the owner must hear that.
3. THE BANK'S FIRST DRAFT: every unfalsifiable face, deduplicated, as the shared bank would hold it — with a note on how many are ALREADY near-duplicates of each other across pools (the same observation written twice by two writers who could not see each other). That number is the saving this ruling buys, stated plainly.
4. THE HAZARD LIST: any face you classified unfalsifiable that you would NOT want drawn on an arbitrary town of another block — a sentence true of any settlement but wrong beside a siege, a famine or an occupation. That list becomes the per-tab page check the ruling names.

WRITE to `$SC/kit/rewrite/unfalsifiable-count.md`. THE CHECKPOINT LAW: write it as the first rows exist.

Return: PATH · THE RATIO · THE ANSWER TO (2) IN ONE SENTENCE · the near-duplicate count · the hazard list's size · LIMITS (say plainly where your classification is a judgment call rather than a reading of the engine).
