# LANE 36 — cure browser pass 3's findings (cut at the consist tip 1d5c79a34)

**Seat:** Opus builder. **Chair:** Fable (session 923472dc). **Base:** `fixes-2026-09-18-consist` @ 1d5c79a34 (289 cars). **Branch:** `fix-pass3-2026-09-19`. **Worktree:** `$SP/lane-pass3` where `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` — create it with `git -C $SP/consist worktree add $SP/lane-pass3 -b fix-pass3-2026-09-19 1d5c79a34 && cd $SP/lane-pass3 && npm ci`.

## PATH LAW (absolute)
- Write ONLY inside `$SP/lane-pass3` and `$SP/lane36-scratch/`. NEVER touch `$SP/consist`, `$SP/lane-public-copy` (lane 35), or `/Users/cstokes/Desktop/settlement-engine`. Never `rm -rf` anything outside your two paths.
- Lane 35 owns (do not edit; if a cure needs one, write a patch file to scratch and say so in your report): `src/store/authSlice.js`, `src/config/tierFacts.js`, `src/components/generate/SeedField.jsx`, `src/components/home/HomeHero.jsx`, `src/components/home/LandingBelowFold.jsx`, `src/components/settlements/SettlementsPanel.jsx` (if that is where the fork→purchase modal lives), `src/pdf/sections/EconomicsTrade.jsx` (its magic line), the refusal walker test, `tests/store/previewPersona*.test.js`, the chart census docstring, `src/lib/refusalReasons.js`.

## GATE LAW (§934.33 pause-and-resume; the owner's normative practice)
- Finish EVERY edit for a car, stage it, then run its focused suites ONLY through the mutex: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run <files from ONE test directory> --maxWorkers=2`. One test directory per invocation (mixed directories silently drop files). NEVER `npm run check`, never the whole suite, never the exclusive tier.
- If the mutex is held (a poll message appears), do NOT wait: write `.lane-resume.md` in your worktree listing the exact gate commands still owed, commit what is staged as a car (message states the behaviour and that its proof is pending), and STOP with a report. The chair resumes you one gate at a time.
- Registers you may not move without the chair: golden master (never `UPDATE_GOLDEN`), prose-numerics ceilings (you may LOWER `REVIEWED_TOTAL_CEILING` and friends in tests/lint/proseNumerics.test.js exactly to the new counts and restate the +1 arm literal; never raise), OSR / writer-reach / wiring census / lighting census baselines (report movement; the chair mints). Any `.not.` assertion on absence needs `// anchored:` on the line DIRECTLY above (or `expectAbsentWithAnchor`).

## THE CARS (one behaviour family each; a commit per car with the proof named)
1. **Plot Hooks keeps proper names.** `src/components/new/tabs/PlotHooksTab.jsx:144` passes `hook.source` through `tokenCase`, so an NPC-sourced hook prints "Sita goswami" while Power prints "Yogesh Nayak, Mayor" (a regression from the §934.22 sentence-case work). Cure: token-case only tokens (snake/kebab/lower-case ids); a cased name with spaces passes through untouched. Test in the existing PlotHooks component suite (tests/components): "Sita Goswami" intact; "trade_route" → "Trade route".
2. **The PDF's institution names.** `src/pdf/sections/Services.jsx:226` maps `chain.processingInstitutions` through `humanize`, so the Canon Dossier prints "BY local resource Parish church + Monastery" (the catalogue's raw name). The screen's cure (lane 27, 481b21db0) goes through `src/domain/display/institutionDisplayName.js`. Cure: the PDF's Services and every other PDF site printing catalogue institution names (census `src/pdf` for processingInstitutions / instNames / institution renderers) route through the same dictionary. Test in tests/pdf: a Services render with `processingInstitutions: ['Parish church']` prints no "parish" (anchored). If a PDF corpus/golden-freeze register moves, STOP and report (the chair re-records by hand).
3. **Status case at every mount.** Pass 3 saw "Well-Defended" (World → Daily Life status strip; find the component through 'Daily Life' in src/components/dossier/dossierLazyTabs.js), "Import-Dependent" (Overview → Systems Health) and "Semi-Organized" (Defense). The labels are DATA (`defenseProfile.readiness.label`, `foodSecurity.label`, the defense organisation label) and the state-prose pools key on them (src/data/dossierStateProse/general.generated.js:2790), so cure at the MOUNTS with `statusCase` from src/domain/display/labelCase.js (lane 27's pattern, 2ac3c9c5f) — never at the data. Extend lane 27's status-case test/walker to these mounts.
4. **snake_case keys at six mounts.** Economics → Supply Chains "Needs imported warehouse_logistics" (chain ids from src/data/supplyChainData.js; print the chain's display name); Economics → Resource Exploitation (`hot_springs_mineral`, `mountain_timber`); Services tab (13 resource keys, e.g. `oasis_water`, `camel_herds`); Resources tab (`desert_salt`); Outlook (`south_asian`, a culture key; src/components/gallery/galleryUtils.js:12 lists the culture ids — find the canonical culture label); History (`external_threat`, an event kind). Cure with the existing dictionaries (`resourceDisplayName`, `institutionDisplayName`, the chain's name; a culture label; an event-kind label — add `src/domain/display/keyLabels.js` only if no dictionary exists, display-only). Extend lane 27's raw-key census walker (tests/lint — find it by `resourceDisplayName`) to these mounts, or add a component test per mount. Run the plain OSR check afterwards (find the exact command in package.json under observed-shape) and REPORT if it moves — do not write.
5. **The sacred house in layout lines.** `src/generators/spatialGenerator.js:245–262`: five village layout lines and the fallback say "church" outright, so a Mesoamerican village reads "Church and green above the tideline"; the desert line already derives the sacred house from what the settlement holds (`desertSacred`). Cure: every line derives the same way (church/cathedral/monastery held → "its church"; temple → "its temple"; shrine → "a wayside shrine"; nothing → no sacred word), and census the hamlet/town/city layout pools in the same file for the same literal. Setting-agnostic law: no real-world-religion noun unless the settlement holds that institution. GOLDEN: this moves same-seed text — DO NOT re-record; run the plain golden test (tests/property/generatorGoldenMaster.test.js), report the moved count, and STOP that car's proof there (the chair records the shift under §934.22 with a record). Also run the plain wiring census and prose-numerics tests and report.
6. **Raw coup weights.** Power → The Powers rows print "w 41.25", "w 26.4", "w 19". Find the renderer in src/components/new/tabs/PowerTab.jsx (grep the weight field). Cure: the raw float leaves the reader's view (the per-bucket "why" already explains); if a share must show, a whole-number percent through the estate's formatter. Prose-numerics counts should FALL — lower the ceilings exactly and restate the +1 arm literal; report the numbers.
7. **The dossier title strip on the phone.** `src/components/generate/WizardOutputToolbar.jsx:172` prints "{tier} · Pop. {n}" at 10 px at 375 wide; the estate's floor is 12 px for chrome (`PHONE_CHROME_FLOOR` — grep src; lanes 28/29/31 used it). Cure at ≤ 480 px; extend the phone-floor walker (tests/lint, lane 29's sweep) or the component's test.
8. **The library at 375.** `/settlements` renders the saved-settlements table (src/components/settlements/{SettlementCard,CampaignFolder,UnassignedLedger}.jsx) 547 px wide, so the page scrolls horizontally (scrollWidth 572 > 375) and the HEALTH/ACTIONS columns with the Open buttons run off-screen — against §934.26 (every control reachable on the phone; no horizontal page scroll). Cure: at ≤ 480 px the rows stack (name, health, actions) or the columns collapse; NO overflow-x band-aid that hides the Open control. Test: a jsdom render at 375 with the estate's matchMedia mock (lane 31's phone tests show the pattern) asserting the Open control renders and no element carries a min-width past the viewport.
9. **Save to Library under the persona.** Pass 3 saw no toast on save, and the exit prompt "This settlement hasn't been saved yet" (src/components/GenerateWizard.jsx:592; the history in src/store/settlementSlice.js:651) after saving, though the row appears in the library. Trace the save action's success path with a real signed-in user (auth.user present) versus the preview persona (role admin; user null?). If persona-only: the cure belongs in the persona (authSlice is lane 35's — write the patch to scratch and report). If real for signed-in users: cure the store path with a store test. Report which.

Out of scope (do not touch): the library's lack of pictures (a table, not a shelf), the dev server's 20 s paint, the HMR confound, every item pass 3 marked NOT OBSERVABLE.

## REPORT
Per car: sha, files, the focused suites run (with counts) or the gate commands owed in `.lane-resume.md`, and any register that moved. Say what you could not cure and why. No prose about effort; receipts only.
