# LANE-T2-TERMINAL-CURE — the dispatch brief (Opus cure lane; given 2026-09-19 ~14:1x EDT by the successor chair, session 7d3418f8)

Role: Opus CURE LANE for train EM-T2's terminal. Check 2 run 15 (bare `npm run check` on `fefb2bb29`) finished `exit 1` at `test:ratchet` with FOUR failing tests in TWO files, both introduced by EM-P0's landing commit `429ceed54`. You IMPLEMENT the chair's ruling below, PROVE it, and COMMIT it on the branch. The chair (Fable) rules and validates; it does not edit `src/` or `tests/` (the owner's seat law, restated 2026-09-19 14:11).

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

Worktree: `$SP/lane-em-b3b`, checked out ON `fixes-2026-09-18-consist` at `fefb2bb29`, clean (the slot; it has `node_modules`). Scratch: `$SP/lane-t2-cure-scratch/` (create it). FORBIDDEN: `$SP/consist` (the chair's worktree), the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the chair kit (read-only), every other lane directory. Never `--ignore-other-worktrees`, never `git add -A`/`-u`/`.`, never stash/reset/clean/rebase/amend, never `--no-verify`, never push. zsh: explicit file arguments only — no unquoted globs or unquoted variables in a vitest command (an unquoted glob once launched the whole suite). Never `npm run check`, never `npm run test`, never `test:ratchet`.

## The reds, verbatim (from `/var/folders/0l/_sz6gzvd11x6sthjy1jdj0_80000gp/T/test-ratchet-last-red.json`)
1. `tests/copy/voiceMechanics.test.js` › src/generators string-literal ratchet › *per-file generators debt exactly matches the baseline* — `src/generators/pipeline.js: baseline em:2 bang:0 → current em:3 bang:0`.
2. same file › *total generators debt never grows past its measured first freeze* — `expected 29 to be less than or equal to 28`.
3. `tests/lint/negativeAssertionAnchor.walker.test.js` › *no NEW un-anchored negative assertion anywhere in the test corpus* — `expected [ Array(1) ] to deeply equal []`.
4. same file › *the generation-facing quarantine is EXACT* — received `"tests/generators/pipelinePinnedMode.test.js": 2` beside the three quarantined rows.

## The chair's ruling on the cure's shape (implement exactly this; if you measure it wrong, STOP and report — do not improvise)
**Cure A — the em dash.** The third em dash is in the PARTIAL-PIN REFUSAL message in `src/generators/pipeline.js` (the `throw new Error(` inside `runPipeline`, the template literal ending `— pin every chooser of a step or none of them.`). Per `docs/VOICE_AND_TONE.md` §6 an em dash in a string literal becomes a period: the message's tail reads `only [<supplied>]. Pin every chooser of a step or none of them.` Nothing else in the message moves. The A4 arm of `tests/generators/pipelinePinnedMode.test.js` quotes the message as a `toThrow(` substring — update its expected string identically. The file's two BASELINED em dashes (the two `Pipeline strict:` messages) are NOT touched. ⛔ Never raise `EM_BUDGET_GENERATORS`, never edit the voice baseline file, never encode the character to dodge the walker. Grep `src tests scripts` for any other consumer of the message text first (the chair found only those two sites); the LANDED packet `docs/implementation/packets/settlement-editor/EM-P0.md` also quotes the message in its §-text — DO NOT edit the packet; name it in your report so the chair records the wording change.
**Cure B — the two bare negatives.** `tests/generators/pipelinePinnedMode.test.js`, arm A5: the two `expect(order).not.toContain('drawPopulation' | 'derivePopulation')` become `expectAbsentWithAnchor(order, '<name>', POPULATION_STEP, 'A5: no split registration')` from `tests/helpers/anchoredNegatives.js` (add the import beside the other helper import). THE ANCHOR IS THE ONE REGISTRATION: `generatePopulation` sits in the same live registry order the two retired half-step names would sit in, so it travels the same code path and the exclusion cannot go vacuous. A one-line comment saying so is welcome. ⛔ No `// anchored:` marker, no quarantine row, never edit the walker, `READMITTED_GENERATION_FACING` or `FROZEN_UNANCHORED_NEGATIVES`. ⛔ NO TEST TITLE MAY CHANGE and no test may be added or removed — the lighting census tuple must not move.
Read the enclosing scopes in full before editing (`runPipeline` whole; arms A4 and A5 whole; the helper whole).

## Verbs, in order
1. `git -C $SP/lane-em-b3b status --short` empty and `git log --oneline -1` = `fefb2bb29`, on the branch; else STOP.
2. REPRODUCE FIRST (the negative control), then cure. Every vitest run goes through the mutex in the SHARED tier, worker-capped, ONE DIRECTORY PER RUN (vitest drops files across directories): `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`. Expected before the cure: voiceMechanics `2 failed | 28 passed (30)`; the anchor walker `2 failed | 7 passed (9)`. ⛔ A gate line with no printed test count DID NOT RUN (the mutex's default gives up and exits 0 — that is why MAX_POLLS is set). Never poll the lock by hand.
3. Implement cures A and B. `git status --short` = exactly ` M src/generators/pipeline.js` and ` M tests/generators/pipelinePinnedMode.test.js`.
4. Focused receipts, same mutex form, quote every count:
   a. `tests/copy/voiceMechanics.test.js` — all green.
   b. `tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/sovereigntyLightingContract.walker.test.js tests/lint/mutationCoverageManifest.test.js` — all green; the lighting walker GREEN PLAIN is the proof no title moved (a red there = STOP, never a refreeze — the refreeze is the terminal's act, the chair's).
   c. `tests/generators/pipelinePinnedMode.test.js tests/generators/pipelineStrictMode.test.js` — all green (A4 now proves the new wording; A5 proves the anchored form).
   d. `tests/property/generatorGoldenMaster.test.js` PLAIN — green, goldens byte-identical. `UPDATE_GOLDEN` and `GOLDEN_SHIFT_SIGNED` are FORBIDDEN. `shasum -a 256 tests/fixtures/generator-golden-master.json` before and after: identical. (An error message is not generated output; this receipt proves it.)
   e. `npx eslint src/generators/pipeline.js tests/generators/pipelinePinnedMode.test.js` — 0 errors.
   f. A mutant, so the anchored form is proven to bite: temporarily point ONE `expectAbsentWithAnchor` anchor at a name that is not in the order (e.g. `'noSuchStep'`), run 4c's pinned-mode file, QUOTE the `LIVENESS ANCHOR` red, restore the line, re-run 4c green. The restored file must equal your cure byte-for-byte (`git diff` re-read).
5. ONE commit ON the branch by EXPLICIT PATHSPEC of the two paths (the chair grants commit authority for this cure): `git add src/generators/pipeline.js tests/generators/pipelinePinnedMode.test.js`, read `git diff --cached` whole (every hunk yours), commit. Subject: `EM-T2's terminal reds cured: the partial-pin refusal drops its em dash, and A5's two negatives are anchored on the one registration (check 2 run 15, 4 tests in 2 files)`. Body: the four reds by name, the ruling in one paragraph, the receipts with counts, the mutant's red line, "goldens byte-identical" with the sha, and that EM-P0.md's quoted message now differs in punctuation. Trailer: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. After the commit: `git show --stat HEAD` names exactly the two paths; `git status --short` empty (the pre-commit hook may re-stage under `--fix` — if it changed either file, re-run 4c and 4e and say so).
6. Report: the commit sha; each receipt's count line; the mutant's red; every claim labelled CONFIRMED (executed) or PLAUSIBLE; anything you noticed and did not touch. Also write the report to `$SP/lane-t2-cure-scratch/T2-CURE.receipt.md`.

STOP (uncommitted; write `$SP/lane-t2-cure-scratch/T2-CURE.STOP.md`): a third consumer of the message text in `src`/`tests`/`scripts`; a golden moves; the lighting walker reds; any edit needed outside the two paths; the helper's semantics differ from the ruling's reading; a gate prints no test count.
