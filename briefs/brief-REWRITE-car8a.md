# Brief — the REWRITE train, car 8a: THE KERNEL, THE INSTRUMENTS AND THE GATE — the index-stable draw, passage shapes as a licensed fourth draw, the taste's instruments landed, the wave's gate built from the taste harness with its four missing pieces and its one false grain cured, the arm-Q cure and the field-synonym table, the face-count ratchet; ZERO reader-facing bytes (chartered at SITTING §T.10, 2026-09-09; ARCH §12 row 8a; §N.2 the index-stable draw SIGNED; Part B §20–§23; the owner's rules of 09-08 verbatim)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-rewrite.md` (§ CAR 8a). Dock: `$SC/laneREWRITE`, cut at the §917 CAS `29ec62425` (`sh $SC/mkdock.sh laneREWRITE 29ec62425`). Read first, whole: `arch-prose/ARCH-COMPOSED-PROSE-v2.md` §4 (the algorithm; §4.3 the comparator), §8 (the pipeline: §8.1 sizing, §8.4 the gate table, §8.6 the SHIFT REGISTER), §12 row 8a, §13 row 22; `s12-sitting/SITTING-RULINGS-912.md` §N.2 and §T whole; `SITTING-AGENDA-car7.md` C″ C‴ C⁗ C′ §H; `taste/TASTE-TABLE.md` + `TASTE-TABLE.verify.md`; `receipt-taste.md` § CAR M, § CAR M-9 and the chair's rulings; the owner's rules in `RULES-V2-PART-B.md` §20–§23 and §21.1–§21.4; `REGISTER-CARD.md` with S2 and S3; the two docks `laneTASTE` (HEAD f07b98529, sealed `refs/preserve/taste-2026-09-09-armA`) and `laneTASTEB` (66d5b6e08, sealed `…-armB`) as READ-ONLY history. THE ARCHITECTURE BINDS (owner: architect, then implement): nothing here invents a mechanism the architecture does not name; where you must depart, refuse with the measurement.

## What this car is, and is not
8a is the REWRITE's first car and moves NO reader-facing byte: every leaf sentence is byte-identical before and after it (the manifest classifier prints `[]` at every commit), because the index-stable draw is a DRAW change proven by the classifier and signed as part of Shift 1, and every other piece is an instrument, a pin or a report. The first WRITING workflow (8b: the defense desk section) opens only after 8a lands, because its gate is built here. NEVER TRIM binds every commit: the variant count and the face count only rise.

## What to build, in this order (one commit each; porcelain 0 and runners 0 between)
1. **THE INDEX-STABLE DRAW (§N.2 SIGNED; ARCH §13 row 22; the SHIFT REGISTER row 1).** `stateProseKernel.js:303–307` draws `eligible[avalanche32(fnv1a32(seed::blockId::poolKey)) % eligible.length]`. Replace the modulus with the ARGMAX of `hashKey(seed::blockId::poolKey::v<vid>)` over the eligible set (`vid` = `poolMeta.vids`, the stable id pinned at SEAM car 4; the seedless case stays index 0 — law 4; the face draw at `:356–364` keeps its `::w` key and its modulus, untouched). Prove: (a) uniformity — on every three-variant pool over 10,000 seeds each vid within 2 SE of one third (the §N.2 figures 33.30 / 33.35 / 33.35 % are the reference); (b) APPEND-SAFETY — a planted fourth variant on a three-variant pool moves ≈ 1/4 of the reads and EVERY moved read moves TO the new vid, none between old ones (the shipped `% length` moves ≈ 3/4 — print both, that contrast is the row's ground); (c) repeat-call identity (A4). THE ONE-TIME RE-INDEX: run the classifier over the DRIFT run (`scripts/prose-manifest-diff.mjs base tip`) and print the RE-INDEXED share per cell class — this print is the owner's veto surface for Shift 1's draw half (§N.2: "the owner's veto stands until that record is signed"); the manifest fixture is NOT re-recorded by this car (the register is unfrozen; the freeze act is the REWRITE's last car) — the RE-INDEXED count is committed as a declared row in `docs/content/prose-shift-register.json` (row 1's `door`) and printed by the contract test (`tests/data/dossierStateProseProjection.contract.test.js:791–841`). ⛔ A FACT never moves: the eligible SET is unchanged; only which member is drawn. Assert by bytes on 525 + 768 that every drawn variant is a member of the same eligible set as before.
2. **PASSAGE SHAPES — a fourth seeded draw, LICENSED BY THE COMPOSITION, never rescued (SITTING §T.4 adopting agenda C⁗ as chartered; the owner's rulings a–e verbatim).** The closed set is THREE and no more: (1) spine then sentence (the shipped shape); (2) sentence then spine — lawful ONLY where the added face carries a noun INTO the spine (the thread arm, item 4) AND the relation is `addition` read as no cause (arm A2 convicts a joint that implies a relation with no row); (3) the clause seat — ONLY where a `consequence.clause` joint exists under S2's guards (none today: WITHHELD, printed). Chain: world → the facts → the LAWFUL SHAPE SET → a seeded selection among the lawful shapes on its own key (`seed::blockId::poolKey::shape`, no hash when one) — NEVER a seeded shape followed by a fallback; a refused shape is printed as refused. A new row on the SHIFT REGISTER with its pin (the shape-set digest per unit class), its door and its declared-row idiom. Shape is a surface like a face: no shape moves a fact between the record and the pen; the compiled passage stays audience-independent. THE DISTRIBUTION TABLE (owner ruling c) is this car's print and lands as `scripts/prose-shape-report.mjs`: over the RATE towns per pool — the duplicate-unit rate on VARIETY per shape policy (fixed vs licensed-draw) · the passage-shape distribution MARGINAL and CONDITIONAL on the lawful set · the face distribution (the uniformity arm per pool; corpus-level counts) · the connective distribution · the construction distribution via `composedOrderIdOf` · the thread verdicts · the band figures (item 5's corrected grain) · the claim/wiring census (A0b) after variation. The shipped draw stays shape (1) until the sitting rules on this print at 8a's fold — the report is a REPORT; nothing ships a second shape from this car.
3. **THE TASTE'S INSTRUMENTS LANDED, BY NAME, WITHOUT THE TASTE'S ANNEX ROWS.** Cherry-pick from `laneTASTE` the INSTRUMENT halves of: M-1 `3844a5d8f` + M-8d `2dd07e72a` (the licence card: `scripts/prose-licence-card.mjs`, `scripts/lib/prose-licence-card.mjs`, its walker); M-2 `1af673d07` INSTRUMENT HALF ONLY (`scripts/lib/dossier-annex-grammar.mjs` modifier rows and `[plain]`; `src/domain/prose/wiringCensus.js` the `annex` rung and the `role` column with every corpus figure over `spineRows`; `scripts/wiring-census.mjs`; the walker deltas) — NOT the seven typed lines in `RECEIPT_POOLS_DOSSIER_STATE.md`, NOT the regenerated leaves, NOT the census/shift-register data deltas those rows produced (re-take the census in the product dock after the code lands and stamp it by the census rule); M-3 `69eb79415` INSTRUMENT HALF (`legibilityRung.js` `pieces`; `tests/helpers/dossierManifest.js` the composed-unit reader; `scripts/taste-candidates.mjs`; `scripts/prose-rate-corpus.mjs`) — NOT the seven candidate functions (they land with their desk sections' rows); M-4 `b37df9ec3` INSTRUMENT HALF (the norm bit from the PREDICATE's rate in `prose-rate-corpus.mjs`; NOT the seven norm rows); M-5 `e5f3f6a98` whole (`scripts/lib/module-closure.mjs`, `tests/fixtures/tasteTowns.js`, the corruption walker); M-6 `c4c2468d2` whole (`scripts/taste-holders.mjs`, the interested walker); M-7 `f86bd505c` + M-9 `4c786100c` + M-9b `63d0a711e` (the harness and its walker — then item 5 generalises it); M-8b `93844e1c3` (the four landed instruments cured at cause). Every cherry-pick is `git cherry-pick -n` and re-staged BY FILE with the annex halves dropped; print per commit the files taken and the files refused. The taste's declared reds in the dock (the corpus-pinning walkers) must NOT appear in the product: with no annex row landed, `tests/lint` whole reads green at every commit — that is the acceptance, executed.
4. **THE THREAD ARM `armThread` (SITTING §T.4 adopting C″), REPORTED not gating until 8b's first batch:** subject continuity between adjacent sentences of a composed unit — a shared head noun or slot with the sentence before, or the LAST position (the one turn outward); the KINSHIP TIEBREAK inside a salience band in `composeStateProse.js` `compareSalience` (`:496–501`): the modifier sharing the spine's subject noun sits nearest the spine, and a subject shift is forced last regardless of band — two planted controls each (a mid-passage shift with nothing handed back reds; a carried noun passes; a last-position turn passes). The echo bound (A11) counts FACTS, not nouns: a deliberate noun echo for the thread is not an echo violation — assert with a plant.
5. **THE WAVE'S GATE — `scripts/prose-wave-gate.mjs` generalised from `taste-measure.mjs` for a DESK SECTION's spine pools and their faces (the REWRITE's unit), with the four missing pieces and the one false grain cured, each with a plant:** (a) ⛔ THE BAND GRAIN — `bandPositionOf` scores a single face against 21 TEXT-level exemplar metrics, 13 of which read zero on any one sentence, so every face reads the identical tuple (exceeded 13/21 · share 0.619 · mean 0.486 · deepest neighbourVariation 1.597 under; CONFIRMED by the chair's probe, SITTING §T.5): score the text-level metrics on the pool's RENDERED CORPUS (every unit of the pool over the RATE towns, joined as one text, per shape policy) and only the word-level and closer metrics on the face; print both grains; a plant of two faces of different length must now read DIFFERENT text-grain figures at the corpus grain and the same at the face grain only where the lexicon agrees; (b) a sibling DISTANCE measure on the estate's own ruler beside A5's synonym-swap count (ARCH §8.4: "A5 sibling distance on the estate's own ruler, floor measured on the VARIETY corpus") — the floor is REPORTED at 8a and set at 8b's fold; (c) the EXEMPLAR CITATION RATE per unit from the ten exemplar texts (the provenance budget's band; SITTING §T.4 set the budget ≤ 1 per unit on a LICENSED holder, provisional until this rate is read); (d) the FIXTURE SECTION: the interested fact composed BOTH WAYS IN BOTH RENDERINGS on `rate-9-2` (captured; holder Town hall, treasury) and the clean control `rate-3-0` — the player face as compiled, the dm-only face inline, and the DM's-pen line beside the passage with the compiled passage identical on both audiences — all four strings printed and the paired-town arm run on them; (e) the `--shapes` report of item 2 reachable from the gate; (f) the owned/inherited grains of M-9 kept; the packet/round protocol kept (`--arm draft --round N`, `--arm A|B --base`); the two-phase rule of Part B §21 encoded as the gate's keep-or-revert (a lawful set keeps only if still lawful; an unlawful set keeps if its failures fell with none new; a banked set is a refusal row, never trimmed; the banked count printed, shrink-only). The taste harness's own walker moves with it; the old script stays only as a thin alias or is removed with its walker in the same commit (never a second implementation).
6. **THE ARM-Q CURE AND THE FIELD-SYNONYM TABLE (SITTING §H; M-9b):** arm Q withholds a mapped reading (`the threat is on the town's books as plainly as the grain.` → `settlement.config.monsterThreat`) — cure at cause, with the convicting plant kept; the census gains a REPORT column `fieldSynonyms` (the nouns a field may be named by in prose — `wages`/`pay` → `economicGates.military`; `books`/`roll` → the holder kinds by `HOLDER_KINDS`) that arm Q, F25 and A3 read; a synonym is ratified like an alias — cited to the card, never to a comment or a prose string; DS-DEF-2's pools' NOT-EXECUTABLE standing on Q (their only census read is car 3h's synthetic table label) is printed as the wiring row it is, not cured by a synonym.
7. **THE FACE-COUNT RATCHET beside the variant ratchet (C′; Part B §22 d):** `tests/data/dossierStateProseProjection.contract.test.js:254` ratchets variants; add the face-count ratchet (sum of `faceCounts`, estate-wide and per pool, only ever rising) and the annex append-only assertion; name the single-face canonical rows bound to live engine strings one by one in the receipt (the seven `canonical` variants ARCH §16 item 5 re-measured: DS-ECO-3 ×3, DS-ECO-6 ×2, DS-ECO-7 ×2) and record their single-faced standing.
8. **T-F12's class list re-cut (SITTING §T.5):** the store/building conflation split (`store` vs the building that holds it), the polarity marker carried on the key as T-F3 requires, so the five waived refusals of the taste (stores: short · stores: import-fed on `granary AND hospital`; walled STRAINED; unwalled LARGE and SMALL) become lawful projections or stay refusals ON A TRUE GROUND — print each of the five with its new verdict.
9. **The connectives lists to the floors as PUBLIC-COPY DRAFTS (S12; row 27 signed at the walk):** addition ≥ 3 (the empty opener stays a member), consequence.clause ≥ 3, tension.sentence ≥ 3, contrast ≥ 3 — drafted in `dossierConnectives.generated.js`'s source annex, never `, which`, each joint's own comma and word; the lists are DATA behind the shipped draw and change no rendered byte until a `tension` or `consequence` joint exists (assert: manifest `[]`).
10. **The register car:** the census re-taken and stamped by the census rule; the lighting census by ritual; OSR shrink-only if a leaf regenerates (print before/after); the SHIFT REGISTER printed by the contract test with its two new rows; `tests/lint` whole green; strict on its ceiling; typecheck; the byte ratchets' dry reads (the instruments add no leaf bytes: declare each leaf's delta as 0).

## Discipline — as LIGHT's and SEAM's, plus
The runner count in its own shell before every vitest (`V=vit; V2=est; pgrep -fl "$V$V2" | grep -v gate-mutex | wc -l`, nothing else in that shell); one focused file at a time; never the whole suite except the acceptance runs named above; never a build (the gate is the build at the landing); explicit staging; no stash/amend/rebase/checkout/reset --hard; register doors only as named; ⚠ the interactive shell's `grep` is a FUNCTION that prints NOTHING on a file it deems binary (`scripts/taste-measure.mjs` is one) — use `/usr/bin/grep -a`. Commits `REWRITE car 8a-1: the index-stable draw …` … `8a-10`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: REWRITE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the shas; the classifier's RE-INDEXED print (the owner's veto surface); the uniformity and append-safety figures; the shape report's first table; the files taken and refused per cherry-pick; the gate's corrected band grain demonstrated on the two-length plant; every refusal with its measurement. Porcelain 0 and runners 0 when you return.

## After 8a (not this car): the skeptic pass (one dock per plant-driving lens) → cures → the §918.x landing chain (kit derived from the 917 set) → 8b THE FIRST WRITING WORKFLOW, the DEFENSE desk section (the wiring proven at SEAM 3h; the taste's pools' home; the worked blocks of ARCH §6.3), on the taste's pattern with the two phases of Part B §21 and the STAFFING RULED AT SITTING §T.3 row 26 (read it there; it is the owner's to veto).

## ADDENDUM 1 (chair, 2026-09-09 05:2x) — THE CHAIR'S RULINGS AFTER 8a-1 AND 8a-2 LANDED (tip `39c88b02d`); the next seat continues from 8a-3
The chair read `receipt-rewrite.md` § CAR 8a whole and re-ran `tests/data/dossierStateProseProjection.contract.test.js` + `tests/lint/prosePassageShapes.walker.test.js` at 39c88b02d (2 files, 79 passed, runners 0). 8a-1 `f4005cccd` and 8a-2 `39c88b02d` are ACCEPTED as landed. Rulings, each vetoable and carried to the §919 ledger row:
1. THE ONE-TIME RE-INDEX (RE-INDEXED 43,685 of 73,284 cells = 59.61 %; REPLACED 0 · WORDING-ONLY 0 · ADDITIVE 0) is SIGNED by the chair under the owner's total delegation of 2026-09-09 (SITTING §T.12). Nothing further is owed on it by the lane.
2. THE UNIFORMITY FLOOR: the lane's statistical repair (chi-square at the 0.001 critical value on df 2; a 0.15 pp absolute tolerance on the pooled shares; a 4.5 SE per-pool ceiling) is ACCEPTED as the brief's amendment — the "every pool within 2 SE" floor was mis-specified (its largest deviation over 1,641 shares is ≈ 3.4 SE by construction).
3. THE SHAPE TABLE ON THE SHIPPED CORPUS: the finding (`attach-set` 0 on all 708 pools; no unit has a modifier, so no unit has a shape question) is ACCEPTED; `scripts/prose-shape-report.mjs --corpus <f>` prints the finding on the product and is driven on a supplied corpus until 8b lands attach rows.
4. M-3's TWO CANDIDATE-PLUMBING FILES (`src/components/new/generalDeskRead.js` +7 and `generalStateProse.js`'s typedef +4) are REFUSED to 8b with the candidate function they serve. Do not land them at 8a-3.
5. `tests/lint/proseTasteAnnex.walker.test.js` is NOT landed (it tests annex rows that are not landed). Any arm of `proseTasteCandidates.walker.test.js` bound to the seven candidate functions is refused or lands NOT-EXECUTABLE — the lane's call per arm under the acceptance that `tests/lint` WHOLE reads green at every commit.
6. The `--taste` projector flag lands with a one-line comment that it is dark until annex rows land.
7. The lighting census refreeze stays at 8a-10 (the ritual refuses a dirty tree); the `voiceMechanics` E2 red is INHERITED and banked at the §917 tip (`labelBands` em 5 · `generalStateProse` em 3), not this car's — leave it.
8. Findings 2 and 3 of the receipt (`attach-set` 0; every DRIFT configuration sharing one `_seed`) are recorded for the fold and the §919 row; a second DRIFT seed sweep is a CAPACITY-train row, not this car's.
THE NEXT SEAT: continue from the dock tip `39c88b02d` with 8a-3 through 8a-10 in the brief's order, using the receipt's "RECON DONE FOR 8a-3" (drop a refused file with `git show HEAD:<path> > <path>`, never `git checkout --`; run `node scripts/generate-dossier-state-prose.mjs --check` first); a commit that cannot meet its executed acceptance lands as a measured refusal and the next begins; the receipt is updated after every commit; porcelain 0 and runners 0 between commits and at the return.
