# LANE: ENC-4 — the chance meeting's Herald line: the 7th registry family, the pools, the annex, the walker, the writer (ROAD B)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-class (the blocker was a PHANTOM, §887.1) · DARK by construction (`chanceEncountersEnabled` is off in every preset; it lights in the wave) · the WORDS are authored and sealed — you wire them, you do not write them⟧

## READ FIRST
1. `$SC/briefs/_PREAMBLE.md`.
2. **The words and the ruling**, sealed: `git show refs/preserve/enc5-words-2026-09-04:RECEIPT_POOLS_CHANCE_MEETING.md`
   (the chair's pools for `chance_meeting_recorded` — Herald `events`, significance notable, AUDIENCE public, six
   variants with per-variant required slots `{npc} {counterpart} {settlement} {home} {outcome_phrase}`, plus the band
   fills and the two noun phrases; the voice is `RECEIPT_POOLS_FAITH.md` §D — plain, concrete, understated, REPORTS
   never judges; zero em dashes, zero exclamation marks; "chance" and "meeting" never adjacent so no engine identifier
   reaches a townsperson's mouth (§886)) and `…:RULING-ENC5-ROAD-B.md` (ROAD B: the exact registry row, never the free
   prefix — `faithNews.js:26-29` calls the prefix "the cheap door and the trap"; the bill — a new registry family, an
   annex, a per-program walker, six ratchet figures in two files — is a MECHANISM the chair has NOT measured: re-derive
   it and report every divergence).
3. **The seam at the tip:** `src/domain/worldPulse/envoyChanceMeetingStage.js` (~:394–498): the stage takes an optional
   `heraldEntryFor(seed)` and returns `heraldSeeds` — "a visible outcome becomes ONE typed seed carrying the NEWS ADDRESS
   LAW's whole chain" (address chain + typed action + names + reason). Read the whole file and `envoyChanceMeeting.js`
   (1,001 lines; its docblock :18–:60 — the widest outcome is `compromised`, a compromise is a LEASH never a defection;
   STATE, NEVER FATE; every `CHANCE_MEETING_TUNING` value is a DRAFT and UNSIGNED — you move none).
4. **The registry precedent:** `src/domain/worldPulse/faithNews.js` (the `KIND_SECTION` / `EXACT_SECTION` rows; how a
   desk-bearing kind is registered so the estate-wide registered-minus-routed honesty check stays still) and the six
   existing families it names. Find where news pools live (`src/data/…`) and how the faith family's annex
   (`docs/content/RECEIPT_POOLS_FAITH.md`) is joined to its data leaf by a walker — that join is your walker's shape.

## YOUR DOCK
`$SC/laneENC4` at **`fd8b6df00`** (the landed tip). Porcelain 0. ⚠ PROSE-REBASE lands before you and cures reader
sentences across `src/domain` — the chair replays you onto the post-PROSE tip; touch no reader prose you do not own.

## THE ACT (cars in this order)
1. **The data leaf + the annex:** the pools from the sealed file into the news-pool data leaf (the family's shape), and
   `docs/content/RECEIPT_POOLS_CHANCE_MEETING.md` into the product tree VERBATIM from the seal (the annex is the
   citation; a string is a citation or a mint BY THE CLAIM — every pool sentence must be traceable to the annex).
2. **The 7th registry family `CHANCE_MEETING`** with the `chance_meeting_recorded` kind: the EXACT_SECTION row (desk +
   letter section per the faith precedent), significance, audience. ⛔ NEVER the free prefix.
3. **The walker** (per-program, `tests/lint/`, extending the family's join walker or a new file — name it): pins
   `requiredSlots.length === pool.length`, every slot present in every variant that names it, the voice fences (no em
   dash, no `!`, no adjacent "chance"/"meeting"), and the annex ⇄ leaf byte-join.
4. **The writer:** `heraldEntryFor` supplied at the stage's caller in the pulse, building the typed seed from the
   receipt (`{npc, counterpart, settlement, home, outcome_phrase}` from the meeting's typed outcome and the band fills);
   prove the seed carries the NEWS ADDRESS LAW's chain; prove the line reaches the Herald `events` feed with the flag
   FORCED on (the lit arm), and that with the flag off NOTHING is emitted and the golden is unmoved (0/525).
5. **The bill, re-derived:** which ratchet figures move (the ruling's guess: six, in two files — likely
   `tests/lint/.news-headline-contract-baseline.json` and `.news-voice-baseline.json`; measure) — record the deltas for
   the chair; take NO register act.

## PROOF
The stage + meeting suites; the news contract walkers (`tests/lint/newsHeadlineContract*`, `newsVoice*`); your walker;
`tests/lint/` WHOLE (exit + failing arms); `tests/copy/` (the voice arms — expect a shrink-only refusal if a magnitude
must RISE: that is a FINDING for the chair, not a re-record); `typecheck:domain:strict`; eslint; one fixed-seed run
before/after with the flag off (byte-identical) and with the flag on (the line appears); plant-out. Quiet-window law +
mutex before any vitest. Receipt `$SC/receipt-enc-4.md` with the bill table and a RETROVALIDATION ROW.

## ⛔ FENCES
No word changed (a word that cannot be wired is a finding back to the chair with the slot that breaks). No tuning value.
No flag flip. No register act. No `--amend`; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: ENC-4`.
