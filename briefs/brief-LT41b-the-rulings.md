# brief-LT41b — THE CHAIR'S RULINGS ON THE VOICE GUARD'S HELD ITEMS (2026-09-15 03:1x EDT; the owner: "I leave the rulings to you")

Dock: `kit/lane-LT41-voice` at `c351fdb44` (sealed `refs/preserve/lt41-voice-2026-09-15`; the four LT41 cars are already on the consist `laneCONSIST-930`). The new cars go on top; a second seal follows. The owner delegated these three rulings at 03:1x EDT in reply to the chair's two named items; the chair rules as follows. EVERY car below is a DECLARED SAME-SEED TEXT SHIFT (§764.3): the words of a generated sentence or label move; the facts, the draws, the roll counts, the indices and the structure do not. The generator golden master is already 525/525 red at the slot (the em-dash wave, §901); the code-to-code claim for these cars is "the red SET is identical row for row before and after" (run the golden at the base and at the tip in this dock and diff the failing-row sets), and the commit body DECLARES the shift in one sentence.

## RULING 1 — the eleven cross-settlement conflict sentences: CURED (car 4)
`src/generators/crossSettlementConflicts.js` lines 82, 86, 94, 100, 103, 107, 111, 115, 116, 134, 153. The em dash is a lexical tell in reader-facing prose (the voice law, zero tells); each sentence is RECAST without it under the CLAIM FREEZE (the clarity law: the words move, the claim does not — no new fact, no dropped fact, no comparative, no intensifier added). The chair's authored recasts (use these; do not re-author):
- :82  `… are locked in a ${nature}. Both claim the right to set terms for the shared corridor.`
- :86  `… have reached an impasse over a ${nature}. The alliance holds, but with open tension.`
- :94  `… over a ${nature}, and the client settlement needs better terms.`
- :100 `… of deliberately undercutting a ${nature}. The charge is probably true.`
- :103 `… of orchestrating a ${nature}. There is no proof, but the suspicion is corrosive.`
- :107 `… are on opposite sides of an active ${nature}. Formal violence is a matter of timing.`
- :111 `… is pressing ${npcA.name} over a ${nature}. The oath holds, but its terms are contested.`
- :115 `… are quietly at odds over a ${nature}, and neither can bring the matter to any authority.`
- :116 `A ${nature} has soured the arrangement between ${npcA.name} and ${npcB.name} of ${settB.name}. The network still runs, but trust is thin.`
- :134 `… are running parallel intelligence operations, and each knows the other knows.`
- :153 `… share routes and fences. The partnership is profitable, and neither side trusts it.`
Everything before the former dash is byte-identical, so `RelationshipsTab.jsx:82`'s dedupe key (`description.slice(0,40)`) cannot move — PROVE it anyway: generate the corpus the lane's own test uses and assert `min(indexOf(' — '))` was ≥ 40 at the base and that every description's first 40 characters are identical base-vs-tip for the same seed. `tests/generators/crossSettlementConflicts.test.js`: structure pins (same seed ⇒ same conflicts, same parties, same nature) stay green untouched; a text pin that quoted a dashed sentence moves WITH the sentence in the same commit and says so. The Tier-5 voice baseline SHRINKS (39 → 28) by the documented `UPDATE_VOICE_BASELINE=1` run in the same commit (a shrink re-freeze is plain --write); the allowlist is untouched (these were baseline debt, never allowlist).

## RULING 2 — the five origin-prose dashes: REFUSED (car 5 stays as it is)
`src/generators/narrative/settlementOriginProse.js:136,144,145,146` keep their dashes: lawful apposition under the annex's ratified R-DST-W4-f (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:5141-5150`), two of them inside the canonical-at-zero pin (`tests/generators/settlementOriginProse.test.js:62`). No code moves. The Tier-5 baseline keeps these five as DECLARED, RULED debt — write the ruling (this brief's name and the R-DST-W4-f cite) into the baseline's reason string or the tier's docstring so the next reader does not re-open it.

## RULING 3 — the stability label's three separator conventions collapse to ONE (car 6)
`src/generators/power/governanceNarrative.js` returns the persisted `powerStructure.stability` LABEL in THREE forms — `<Band> — <gloss>` (nine values: :250, :295, :298, :301, :304, :307, :313, :337 and the inner dash at :289), `<Band> (<gloss>)` (seven values: :249, :264, :268, :319 …) and `<Band>; <note>` (per `labelBands.js:3`). THE RULING: ONE convention, `<Band> (<gloss>)` — the form seven siblings already use, the form the display's own band recovery splits first, and the only one of the three that carries no em dash. The nine dash values become `Unstable (criminal governance)`, `Fractured (no stable governing authority)`, `Shaken (institutional trust collapsed)`, `Desperate (hunger is eroding order)`, `Anxious (disease is overriding normal authority)`, `Volatile (power is available to whoever moves first)`, `Strained (debt obligations constrain every decision)`, `Tense (regional monster threat)`; :289 becomes `Critical (active siege, survival priority)`; any `; ` value becomes the same parenthetical form. The BAND WORDS DO NOT CHANGE (`STABILITY_BANDS` in `labelBands.js` is a totality contract with the producer — `tests/lint/vocabularyTotality.walker.test.js` must stay green with NO entry added or removed). ⛔ THE LIFECYCLE: the label is PERSISTED in every existing save; readers must keep recovering the band from all three spellings forever — `labelBands.js`'s recovery stays tolerant of ` — ` and `; ` (add a test that feeds the OLD nine spellings through the recovery and gets the same band as the new ones); `dailyLifeLogic.js`'s `formatStability` and `aiLayer.js`'s documented trap likewise; NO migration, NO save rewrite (every existing world is test data — the owner's law). Consumers to census BEFORE editing (every one dispositioned in the commit body): `src/components/new/dailyLifeLogic.js`, `src/generators/servicesGenerator.js` (`institution: '(criminal governance)'` is not the label — leave it), `src/generators/aiLayer.js`, `src/domain/display/labelBands.js` (docs + tolerance), `src/domain/compendium/generated/compendiumData.generated.js` (REGENERATE by its own script, never hand-edit; if the regenerator is a mint trigger, HOLD and report), `tests/generators/governanceNarrative.test.js`, `tests/generators/powerAdversarialRemediation.test.js`, `tests/generators/aiLayer.test.js`, `tests/domain/powerStateProseDesk.test.js`, `tests/lint/vocabularyTotality.walker.test.js`, `tests/copy/voiceMechanics.test.js` (the `AUTHORED_VOCABULARY_ALLOWLIST` entries for this file RETIRE — the count pin drops — and the Tier-5 baseline shrinks by nine in the same commit). Any DS-* corpus row or desk that quotes a stability label verbatim (grep `docs/content` and `src/domain/display/stateProse` for the old spellings) is dispositioned too; if one is a corpus face, HOLD that face and report it rather than editing corpus prose from this lane.

## GATES (every car)
Focused suites for every file touched · `tests/generators/` whole · `tests/copy/` whole · `tests/lint/` whole (timeouts at load > 8 measured alone) · the golden red-SET diff base-vs-tip (identical) · `npm run check:quick` exit 0 · porcelain 0 · no package.json/lock byte · no ceiling moved. Each commit body declares the same-seed text shift and quotes every gate line. Second seal: `refs/preserve/lt41b-rulings-2026-09-15`.
