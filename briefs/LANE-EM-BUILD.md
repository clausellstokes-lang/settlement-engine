# LANE-EM-BUILD — the shared dispatch brief for an Opus BUILD LANE under SEALED DISPATCH (successor chair, session 7d3418f8, 2026-09-19; the launch message names the packet ID and its particulars)

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

**Role.** You build ONE implementation packet, `<ID>`, exactly as written, in the slot, and land it as ONE commit on the branch. The packet (`docs/implementation/packets/settlement-editor/<ID>.md`, status READY) is the authority for WHAT (§6 contracts, §7 change manifest), in what ORDER (§8), the focused COMMANDS (§10), the STOP conditions (§11) and the RECEIPT (§12). The chair (Fable) rules and validates; it does not edit `src/` or `tests/`.

**Worktree.** `$SP/lane-em-b3b` — checked out ON `fixes-2026-09-18-consist` (the slot; `node_modules` present); the chair's `$SP/consist` is DETACHED. Shell state does not persist between Bash calls: `cd "$SP/lane-em-b3b" && …` (re-set SP) or `git -C` every time. **Scratch:** `$SP/lane-<ID>-build-scratch/` (create it). **FORBIDDEN:** `$SP/consist`, `$SP/read-tip-*`, the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the chair kit (read-only: the briefs), every other lane dir. Never `--ignore-other-worktrees`, `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push. `git checkout --` only on a file YOU edited, to withdraw your own edit. zsh: quote variables; explicit file arguments; never an unquoted glob in a vitest command.

**Read whole first:** the packet; `docs/implementation/preambles/EM-PREAMBLE.md` (verify its SHA-256 equals the packet header's); `docs/implementation/PACKET_STANDARD.md`; every file in §5/§7 you will touch or must preserve, by symbol, in its full enclosing scope.

## Verbs, in order
1. **Seal.** `npm run implementation:dispatch -- <ID>` — capture its real exit status (no `| tail` on this one). Any refusal: STOP and report its exact stderr. The branch name must equal the packet's verified branch; HEAD must descend from its verified base.
2. **Build** ONLY the packet's §7 paths, in §8's order. `git status --short` must show exactly those paths and nothing else, at every step. Tests the packet says to write FAILING first are written failing first, and you quote the red.
3. **⛔ GATED RUNS ARE PAUSE-AND-RESUME (the owner's normative practice, restated 2026-09-19 17:05: the five seats are FOUR WORKING LANES + ONE GATE/SUITE SEAT — "once a build has gone to the point where it's waiting on the suite or a gate it is paused and waits until it's resumed for its turn").** A GATED RUN is anything that runs vitest, eslint, the typecheck, a build, `verify:dist` or `check:packet`. You NEVER sit waiting for the lock. When you reach a gated batch: (a) finish EVERY edit the batch needs; (b) write `$SP/lane-<ID>-build-scratch/<ID>.lane-resume.md` (in your SCRATCH, never in the worktree — `git status` must stay exactly §7) — the batch's exact commands in order (ONE TEST DIRECTORY PER INVOCATION), the paths edited so far, and what remains after the batch; (c) STOP: end your turn with the single line `PAUSED AT THE GATE — <the note's path>`. The chair resumes you ("the gate is yours") when the gate seat is free; you run the note's batch straight through and carry on, and you stop the same way at the next gated batch. A RED-FIRST proof is its own batch (tests written → pause → the red quoted → implement → pause → the green). Inside your window every vitest command still goes through the mutex, SHARED tier, worker-capped, because the chair's own terminal may hold it: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`. Quote every count line. ⛔ A gate line with no printed test count DID NOT RUN (the mutex's default gives up after 40 polls and EXITS 0 — never drop the two exports). NEVER `npm run check`, `npm run test`, `test:ratchet`. Ungated work (reading, editing, plain `node` probes, `git`, the seal of verb 1, `shasum`) never pauses.
4. **The standing instruments every build lane runs (they have each reddened a terminal):** `tests/lint/negativeAssertionAnchor.walker.test.js` (pair every negative with a liveness anchor via `tests/helpers/anchoredNegatives.js` — no bare `not.toContain/not.toMatch/not.toHaveProperty`), `tests/copy/voiceMechanics.test.js` (no em dash or exclamation point in a `src/` string literal), `tests/lint/mutationCoverageManifest.test.js`, and `npx eslint` on every touched file — all GREEN. **AND every directory under which your §7 CREATEs or RENAMEs a test file, WHOLE, once** (added 2026-09-20 after run 19: EM-B3c's new `tests/security` file carried sort fixtures shaped like migration names, and that directory's own SS4 meta walker — which no focused run and no `tests/lint` run reaches — read them as references; a lane's file opts into every walker that governs its directory; read those walkers' headers before you write a fixture, and declare a synthetic literal with the walker's executed marker rather than computing it past a regex). **Then `tests/lint` WHOLE, once** (added 2026-09-19: a lane's focused runs never reach the estate's walkers, and EM-B3a landed a red in the prose-numerics exact-identity arm that nobody saw for a day): its ONLY lawful reds are the lighting census (below) and the prose-numerics exact-identity arm for rows whose ADDRESS your edit shifted (the baseline addresses rows by file + line; it is outside your manifest, so you QUOTE the convicted rows in your receipt and the train's terminal cure re-addresses them) -- any other red is a STOP. `tests/lint/sovereigntyLightingContract.walker.test.js` run ONCE, separately: EXPECTED RED if the packet adds test files or titles — record the measured tuple and its delta against the packet's stated DELTA; any other delta = STOP. ⛔ NEVER refreeze it — the refreeze is the train's terminal act and the chair's.
5. **Goldens.** `UPDATE_GOLDEN` and `GOLDEN_SHIFT_SIGNED` are FORBIDDEN. `shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before the first edit and after the last: identical (reference: `7177cd6e…8f1e` and `921c51cf…db41`).
6. **Budgets, only where the packet's §7/§8 carries them:** a REAL `npm run build` through the EXCLUSIVE mutex (`GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npm run build`; never bare `npx vite build`), then the packet's own attribution and re-mint steps. `npm run verify:dist` is run BARE (it takes the mutex itself; the mutex is not re-entrant) with the long wait exported.
7. **Seal checks BEFORE the commit (part of your LAST gated batch — `check:packet` runs gated steps):** `npm run check:packet -- <ID>` then `npm run implementation:resume -- <ID>` — BOTH exit 0, statuses captured.
8. **ONE commit ON the branch by EXPLICIT PATHSPEC** of exactly the packet's §7 paths (the chair grants commit authority for this landing). Read `git diff --cached` whole first. Subject: `<ID>: <the packet's outcome in one clause> (train <T>, …)`. Body: what was built, every receipt with its count line, the goldens' hashes, the measured lighting tuple and delta, any budget figures, judgment calls (the packet says NONE unless you met one — then name it). Trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; if your harness adds its own, keep BOTH; never amend. After it: `git show --stat HEAD` names exactly those paths; `git status --short` empty; if the pre-commit hook rewrote a file, re-run the affected focused gates and say so.
9. **Report** = the packet's §12 receipt filled BY EXECUTION, also written to `<scratch>/<ID>.receipt.md`; every claim CONFIRMED (executed) or PLAUSIBLE; anything noticed and not touched.

**STOP** (uncommitted; `<scratch>/<ID>.STOP.md` with the smallest measured contradiction): any §11 condition; any edit needed outside §7; a golden moves; a contract cannot be met as written; a gate prints no count; the seal refuses. Do not improvise around a STOP — the chair rules.

> ZSH WARNING (from EM-B1d's build lane, 2026-09-19): NEVER put the mutex's env assignments in a shell variable and expand it unquoted (`env $M sh scripts/gate-mutex.sh ...`). zsh does not word-split an unquoted expansion: the command dies with `EXIT=2` and prints NO COUNT LINE -- which only the law 'a gate line with no printed test count DID NOT RUN' catches. Spell the assignments inline on every command, or `export` them first.

> ZSH WARNING, SECOND INSTANCE (FIX-K1's lane, 2026-09-20): the same non-splitting bites GIT — a path list held in an unquoted variable (`git diff -- $SRC`, `git checkout -- $SRC`, `git apply -R $PATCH`) is ONE pathspec: the diff prints nothing, the apply of a 0-byte patch is a no-op, and a red-first cycle then runs against the still-cured tree and reports GREEN. A red-first that comes back green is a STOP, not a pass: check `git status --short` names exactly the files you expect, and grep the tree for the pre-cure text before you believe any red. Spell every path inline.

> TRAILER RULING (the chair, 2026-09-20, after CURE-I): the commit trailer is the one YOUR harness prescribes — an Opus lane signs `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; signing as Fable would misattribute the implementation. Where an older sentence in this brief names the Fable trailer, this ruling replaces it. If your harness adds a second attribution line, keep both; never amend a commit to change a trailer.

> FIXTURE LAW FOR `tests/security` (the chair, 2026-09-20, CURE-I): a fixture that spells a migration-shaped name (`NNN_anything.sql`) carries `// synthetic-migration-name: <why this names no migration>` on its own line or the ONE line immediately above — the SS4 meta walker excludes a declared literal from its existence check AND asserts it absent, so the marker can hide nothing; an undeclared fixture reds the terminal (run 19).

> REPORT-ONLY ARMS (the chair, 2026-09-20, after FIX-C2): vitest 4.1.8's DEFAULT reporter DROPS a PASSING test's `console.log`, so a report-only arm written that way is silent. Write report-only output with `process.stdout.write` (it survives both reporters), and prove any existing report-only arm actually prints before you trust its silence.

> THREE LAWS (the chair, 2026-09-20, from EM-R0c's and EM-P2's reports): (1) SEED LOOPS — a `tests/domain` (or any `tests/`) CREATE never writes a bare `for (const seed of …) { expect(…) }`: `seedLoopTotality` convicts it and its habitat is shrink-only; collect (`for (const c of rows) …`) then assert once. (2) THE PREAMBLE HASH IS MEASURED, NEVER QUOTED — compute `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` at your read tip and cite THAT; a hash a brief quotes is stale the day the next amendment lands. (3) A HARNESS PRINTS THE TREE IT READ from its environment, never a literal label; and every unquoted shell expansion is a false-green risk (a `set -- $pair` in a verification loop printed UNCHANGED while `shasum` errored) — quote everything.

## Addendum 2026-09-20 08:51 — PATCH-AND-RESEAL, and a NAMED PRE-EXISTING RED (ODQ §934.47 addendum 84)
- A seal binds the HEAD it was taken at. When the chair must land a commit on the branch under your seal (a manifest cure, a CURE lane's composition), the protocol is PATCH-AND-RESEAL: save your uncommitted edits as a patch (`git diff -- <your paths, spelled inline> > <scratch>/<ID>.packet.patch`) with each file's SHA-256 recorded; `git restore -- <your paths>` (your own files only); prove `git status --short` EMPTY; end your turn `TREE CLEAN — awaiting <the chair's act>`. The chair commits in the clean worktree and resumes you; you re-run `implementation:dispatch` (a fresh seal at the new HEAD, the same packet), `git apply --index <patch>`, prove the SHA-256s equal, and continue. Never a WIP commit of packet work before the seal (the dispatch preflight refuses modified target files), never foreign bytes under a seal (`sealed foreign work drifted`).
- A red in a sealed check that run N of the full check already named at your tip, before any byte of yours, is a NAMED PRE-EXISTING RED: quote the title and the message, keep going through your remaining steps, never cure it yourself (a golden re-record is the owner's; a register refreeze is the chair's), and stop before the packet commit if it is still red when you reach batch 4 — the chair composes the cure first.
- The validator's retiree rule (`implementation-packets.mjs:844-848`) reds a packet whose own §7 burns the text its `retiredSymbols` row names (EM-B1f, 09-20). If your packet carries a `retiredSymbols` row whose symbol your §7 removes, STOP at the seal and name it — the chair withdraws or re-cuts the row (TOOL-19 cures the rule).
- **A RE-SEAL NEEDS THE CHAIR TO ROTATE THE OLD CAPSULE (EM-B1f, 09-20):** `implementation:dispatch` refuses when `<git-dir>/implementation-sessions/<ID>` exists (immutable evidence, keyed by packet id) and `check:packet`/`resume` refuse when that capsule's sealed HEAD drifted — a deadlock with no sanctioned door until TOOL-22 lands `dispatch --reseal`. The lane STOPS and names it; the chair moves the capsule to `<ID>.sealed-at-<head>` (nothing deleted), and the lane dispatches again ON A CLEAN TREE — `assertTargetPreflight` throws `non-CREATE target must be Git-clean` for any dirty manifest row, so the patch is re-applied only AFTER the fresh seal.
- ⭐ **LAW (ODQ §934.47 addendum 104): every lane that touches `src/` or `tests/` runs `tests/lint` WHOLE once before its commit — ALWAYS**, not only on a CREATE/RENAME: the walkers govern the whole tree (`controlBytes` walks `src` and `tests`; the citation, anchor and lighting walkers scan every file), and a focused batch never shows them your file — three of 2026-09-20's instrument reds were exactly that (FIX-P1b's NUL under `src/lib`; the negative-anchor counts; the citation drifts). The only permitted reds: the lighting census against a register that predates your base (quote what it evaluated) and the provenance title if swept.
