# Brief — car 6: THE TASTE — DS-DEF-11 Phase 1 as re-specified, in a dock, NOT landed (drafted 2026-09-08 13:0x; FINALISED after §916 — ⟦TIP⟧ and ⟦SKELETON⟧ tokens are filled by the chair then)

Seat: Opus 5 — implementer, in a dock cut at the §916 CAS `⟦TIP⟧` (`sh $SC/mkdock.sh laneTASTE ⟦TIP⟧`); nothing from this dock is ever landed — its artefacts are read by the sitting (car 7) and re-authored by the REWRITE under the ratified rules. Receipt: `$SC/receipt-taste.md`. Read first, whole: ARCH §0 (the walk on the walls block), §2.5 (the annex grammar), §4 (the algorithm), §5.2–5.3, §6.3–6.5 (the three worked blocks), §8.3 (the LICENCE CARD), §12 row 6, §16; `REGISTER-CARD.md` whole with S2 and S3; Part B §1, §16–§16.2, §18–§20; `MOVE-GRAMMAR.md` §1–§3, §4.4.1–§4.4.3; `CLERK-LAWS.md` §2.4.1, §2.6.1; SITTING §N, §O, §P, §Q; the SEAM receipt whole; the owner's rules verbatim: four wording faces per semantic variant, never trim, an unweighted seeded roll picks the face; the exemplar not the practical; no round past the second on any wording set.

## What the taste authors (annex rows only; the projector emits the leaves; no leaf byte by hand)
1. **DS-DEF-11 the walls** — the four pools of ARCH §6.3 (`country: pressed (walled)` / `(unwalled)`, `watch: bought (revealed)` / `(covert)`) with their `READS`, `NARROWS`, `RELATION`, `FORM`, `MOVE`, `ATTACH` lines from the LICENCE CARD printed FROM THE CENSUS; each with FOUR faces, each face inside the voice (the card) and the wording-family law (Part B §16); the covert pool `dm-only` on every variant.
2. **DS-DEF-2 the two stock pools** on `granary AND hospital` (after car 3h exposed the tables) and **DS-GEN-3's `purse: short`** at the sentence seat.
3. **The connective lists drafted to the floors** (addition ≥ 3; consequence.clause ≥ 3; tension.sentence ≥ 3) — public copy, the owner signs at the walk; the joints never `, which`.
4. **ONE INTERESTED FACT, BOTH WAYS (SITTING §Q.4 step 3, as re-ruled at §R c-22):** on a BIRTH town whose `powerStructure.criminalCaptureState` is `corrupted` (15 of 768 RATE towns; or `adversarial`, 194), over a fact held by one of the STATE'S ORGANS (office · court · treasury · watch — the only kinds a settlement-wide capture marks INTERESTED; SEAM car 5c's standing), the player face states the count as compiled and the DM face names the holder and its interest from typed facts only — a `dm-only` variant carrying the holder mark on the SAME pool; arm A13 classifies the citation; the sibling-distance arm reads both faces.
5. **The norm bits** for the new pools from the RATE print; **candidate functions** in the candidates leaves for these three blocks only.

## What the taste measures (every figure with its N and command; the sitting reads them by name — SITTING-AGENDA-car7 §E)
the fragment length distribution against ≤ 12 · the tie rate under the comparator · the per-entry walk cost → N with a stated confidence on the arms' fire rates (X-F8) · the per-set authoring cost and the round counts per wording set under the OWNER'S TWO-PHASE RULE (09-08 ~18:1x): the initial rounds until inside the bands (two dry rounds → a refusal row), then ONE refinement round by a different author with named targets, the gate re-run, a failed refinement reverting to the lawful draft — both phases priced separately · the composed-order share outside LEVEL1 ∪ LEVEL2 (T-F4) · the closure of `corruption.js` into the defense desk against the 293,079 B precedent (§5.3) · the paired-town arm on the corruption fixtures (the same seed, the impairment toggled: the two player pages byte-equal on every mount) · the classifier on every affected cell (ADDITIVE only; a REPLACED or RE-INDEXED cell is a STOP) · the duplicate-unit rate on VARIETY before and after · the citation rate per unit in the exemplar texts (the provenance budget's band) · the composed walk at N: zero FAIL, every WITHHELD listed.

## Discipline — as SEAM's, plus: NEVER land; never touch a leaf by hand; every composer call through the shipped desk-read recipe with every caller reading (the `{}` readings hazard); the dock's artefacts listed with shas in the receipt; the manifest diff `node scripts/prose-manifest-diff.mjs base.json tip.json` printed whole.
