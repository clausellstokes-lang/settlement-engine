# LANE: SOAK-HONEST-909 — the soak receipt ships the per-year series; the capacity rows become fully executable (chartered under the owner's 2026-09-07 delegation; ODQ §909)
⟦Chair: Fable 5.1 (session 405b5e7e) · Lane: Opus 5 (`model: "opus"`) · implementation lane, product bytes in `scripts/audit/whole-world-soak.mjs`, `scripts/audit/soakInvariants.mjs`, `scripts/soak/*`, `tests/soak-harness/*`, `tests/domain/demographicsEnvelope.test.js` · dock `$SC/laneB6` at the §908 CAS tip (re-derive: `git -C /Users/cstokes/Desktop/settlement-engine rev-parse claude/composite-r4`; the dock is fast-forwarded there by the chair before dispatch) · ⛔ no vitest while a chair gate runs (`$SC/HOLD-VITEST` present ⇒ wait)⟧

## WHAT IS OWED (the §907 owner row (b) and the CAP-TRIP follow-ons, decided by the chair under delegation)
1. **CAR 1 — SHIP THE SERIES.** `receiptBody` in `scripts/audit/whole-world-soak.mjs` (re-derive the line; ~:1013, written ~:1163) ships `yearlyPopulations` (per year, per settlement) and `yearlyDiedFlags` (per year, per settlement, booleans) from run A, beside the `finalPopulations`/`finalDiedFlags` it already ships; `schemaVersion` 5 → 6 with a one-line schema note in the writer's header naming §907 M1-F1 as the cause. Then measure: the reachability walker (car 2 of CAP-TRIP-907) reads 0 unreachable rows — BANK the win (the shrink-only baseline 2 → 0, the ratchet's own door); a fresh 30-year receipt (`--years 30 --settlements 4`, ~80 s) evaluated by `evaluate-receipt.mjs` grades `fullInstrument: true` with `notExecutable: []` and `capacity_plateau`/`capacity_floor_thaw` EXECUTED (their verdicts on a 30-year run are whatever they are — report them; the plateau row needs 150 years so it will declare its own horizon guard, not NOT-EXECUTABLE — read the row and say which). Archived receipts (schema 5) must still grade honestly: the two rows NOT-EXECUTABLE on them, `fullInstrument` false — prove it on the B6 receipt at `$SC/capacity/artifacts/research-lit-4s.cases/research-lit-4s-300y-4s-seed1.json`. The register's cell identity carries `receiptSchemaVersion` — read `scripts/soak/register.mjs` and the `_doc` of `tests/soak-harness/.soak-register.json`: if the UNFROZEN genesis cells' scaffolded identity must move 5 → 6, do it ONLY if the register's own door admits it on an unfrozen cell; otherwise hand the cell edit to the chair with the exact diff.
2. **CAR 2 — the nested field.** `capacity_realm_load` keys on `behavioral.yearly[last].realmDemography`; give `requires` a dotted-path form so an absent nested field declares NOT-EXECUTABLE (v4 receipts lacking `realmDemography`) instead of reading as silence; extend the reachability walker to resolve dotted paths against the writer (`behavioral-observation.mjs` writes `realmDemography` — measure where). Declare in the receipt which archived receipts change grade.
3. **CAR 3 — the constant lift + the row armed.** Lift `MOVING_SHARE_FLOOR` (0.05) and `MOTION_FLOOR_01` (0.0025, currently spelled twice: `behavioral-observation.mjs` ~:957 and `demographicsEnvelope.test.js` ~:45) into `scripts/audit/soakInvariants.mjs` beside `LIVENESS_FLOOR`; both former homes import from there (one spelling); then ARM `capacity_envelope_30y` importing the bar from there, exactly as `YEARLY_BYTES_PER_SETTLEMENT_CEILING` and `WALL_TIME_TREND` already are; correct the registry's refusal comment to "armed at §909". Roster 11 → 12 ids, 9 → 10 deterministic (the design's §2.5 C3 prediction). MEASURE its verdict on the B6 receipt (predicted SILENT: 110/120 = 0.9167 inside the customer horizon; no year below 0.05 in 300 — CAP-TRIP car 3's figures) and on the fresh 30-year receipt. The registry pin, the roster pin and `tripwireRegistryDefects` all move: update them with the measured figures.
4. Every car: the focused test files (`tripwireRegistry`, `soakRegister`, `evaluate`/`seams`/`curveBand` suites, `demographicsEnvelope`), one file at a time, exits captured before any pipe; eslint on touched files; `npm run typecheck:ratchet` once at the composed tip (ceiling 173).

## LAWS
- STATE, NEVER FATE: no tuning value moved, no golden re-recorded, no register `--write` on a frozen cell, no signature touched. A verdict that moves on an archived receipt is DECLARED in the receipt, never silent.
- Read the whole enclosing scope and every caller before editing (`grep -rn` the field names across scripts/ tests/ src/ docs/); re-derive every line number above.
- Commits in the dock: explicit paths, never `git add -A`/`-u`/`.`; never stash; no push; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: SOAK-HONEST-909`.
- Receipt `$SC/receipt-soak-honest-909.md` at arrival (STATUS: PARTIAL) and after every car; a RETROVALIDATION ROW of every judgment call; every timestamp from `date`; CONFIRMED/PLAUSIBLE per claim. If a car's measurement contradicts this brief, refuse it WITH the measurement and continue.
