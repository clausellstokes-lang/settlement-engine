# Brief — INSTR-912 REBASE: carry the nine code cars onto the product tip f3ab08f51 in a fresh dock, resolve the three known conflicts by the written recipe, regenerate the one register, prove the merged tree

Seat: Opus 5 — implementer. The chair (Fable) rules; you carry and measure. Receipt first (`$SC/receipt-instr-912-rebase.md`), then the commits. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`.

## Where
- SOURCE (read-only, never modified): `$SC/laneINSTR` at `ee403e8ab` — thirteen commits over 3b1c0eaa5.
- TARGET: `$SC/laneINSTR2` — a fresh detached worktree at the PRODUCT TIP `f3ab08f51` (full: `f3ab08f5194201d19f99f8905068783b62fbfaf8`; the §913 landing, twenty cars over 3b1c0eaa5), porcelain 0, node_modules SYMLINKED (leave them so; no vite build, no edge-bundle build in this brief).
- Read first, whole: `$SC/skeptic-instr2/FOLD.md` §5 (H1–H9), `$SC/s12-sitting/SITTING-RULINGS-912.md` §L.3 and §M.3 (the chair's rebase rulings; §M wins), `$SC/receipt-instr-912.md` § CAR 10's "Rebase deltas" paragraph (eleven manifest entries: 6 `invariants` + 5 `meta`; plants `#74`–`#84` → `#75`–`#85`; the join 96 = 96). Never enter `laneLMAT`, `skepINSTR`, `skepINSTR2` or the main tree.

## The recipe
1. **Carry the NINE code cars, in order, by cherry-pick into laneINSTR2:** `950c0c204` (1) · `d63f80207` (2) · `eb2c330dd` (3) · `37833b22e` (4) · `9d257ca7d` (5+6) · `e3e56f94a` (7) · `0b05e3a7a` (8) · `8c53ddef1` (9) · `c199f0189` (10). **SKIP the four register commits** (`74a1aa0e8` 7b, `27c24522c` 8b, `454d478a1` 9b, `ee403e8ab` 10b): the lighting census is regenerated ONCE at the end (step 4). Use `git cherry-pick -x <sha>` so each carried commit names its origin; keep each car's subject and body; the trailers stay as they are (Seat: Opus 5 — Fable-unvalidated · Lane: INSTR-912 · Co-Authored-By). Cars 1–6 should apply clean (additions only) — if any does not, STOP and report which file, do not improvise.
2. **The three conflict files, by TEXT, never by re-serialization:**
   - **H1 `scripts/mutation-coverage-manifest.json`** (car 7 re-serialized the whole file; cars 8, 9, 10 added entries): on every conflict take f3ab08f51's file as the base (2-space indent, insertion order; it already holds §913's one entry for `tests/security/livingContentRosterPublicDrop.test.js`) and ADD INSTR's entries by text at the END of the section they belong to (`invariants`: the five of car 7 + `#79`'s of car 8; `meta`: the three of car 9 + the two of car 10), each in the base's formatting. **ARM (executed, in the receipt):** parse base and result with python and diff the KEYS — `invariants` +6, `meta` +5, removed 0, changed 0, and every other top-level key byte-equal; then `npx vitest run tests/lint/mutationCoverageManifest.test.js` green (TOTALITY, LABEL JOIN one-to-one, SHRINK-ONLY 186, DIRTY-GUARD).
   - **H2 `scripts/mutation-sweep.sh`** (two hunks: the `MUTATED_FILES` insert near the top; the plants appended at the end): keep BOTH sides in both hunks — f3ab08f51's `MUTATED_FILES` entry (`src/domain/display/publicSafe.js`) and INSTR's six (incl. `src/domain/display/heraldIntegrity.js`); f3ab08f51's plant `# 74.` (§913 car 11, label `security/the living-content roster becomes an allowlisted public key`) stays `# 74.` and INSTR's plants `# 74.`–`# 84.` are RENUMBERED `# 75.`–`# 85.` — COMMENT NUMBERS ONLY, every label byte-untouched (labels are the join key; the numbers are not locators — the file carries duplicate numbers elsewhere already). **ARM:** `sh -n` exit 0; every INSTR label present exactly once (`grep -c`); §913's label present once; the manifest test's LABEL JOIN green.
   - **H3 `tests/lint/.lighting-census-baseline.json`**: on conflict take f3ab08f51's side unchanged (the four skipped register commits are the only INSTR edits to it); it is regenerated in step 4.
   - `scripts/.writer-reach-baseline.json`, `supabase/functions/_shared/*`, every other §913 file: f3ab08f51's side whole (INSTR never touched them — assert with `git diff --name-only 3b1c0eaa5..ee403e8ab` that none is listed).
3. **After the nine carries** (porcelain 0 between each): `git diff --name-status f3ab08f51..HEAD` must list exactly INSTR's files — 22 additions (the ten `src/` island modules, the six `tests/lint` walkers, four fixtures, two helpers) + 3 modifications (the manifest, the sweep, and NOTHING else — the lighting baseline is not yet touched) — paste the list. The ten-module byte fence and every island test must be green (step 5).
4. **The lighting census, regenerated by its own ritual as ONE register commit:** run the census door exactly as cars 7b/8b/9b/10b did (read their commit bodies for the ritual: `LIGHTING_CENSUS_REFREEZE=… npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js` under the gate mutex on a CLEAN tree), commit ONLY `tests/lint/.lighting-census-baseline.json` as `INSTR-912 rebase register: the lighting census refrozen at the composed tip — files N → M, titles …` with the door's own printed figures (never a hand number), the three trailers. Then the PLAIN walker run must be green.
5. **Prove the merged tree** (the split-pattern runner count must be 0 before every run; `$SC/HOLD-VITEST` is absent; the chair runs nothing meanwhile): (a) each of the six lane walkers, the institution table walker and the manifest test ONE AT A TIME — paste the tallies (expected: the same as car 10's — wiringCensus 26 · entryContradiction 27 · moveGrammar 50 · institutionTable 18 · registerLoaders 13 · measures 14 · mutationCoverageManifest 10); (b) **U6 — the whole `tests/lint` directory ONCE:** `npx vitest run tests/lint` (this is chartered; it is not the whole suite) — expected all green, or exactly the one known DOOR 3 timing red under contention, which you re-run alone to show it passes; paste the summary line; (c) `npx tsc --noEmit` or the repo's typecheck ratchet command (read package.json) — green at its ceiling; (d) `node scripts/check-observed-shape-readers.mjs` (dry) exit 0, 0 drift lines; `node scripts/check-writer-reach.mjs` (dry) exit 0; (e) the ten-module byte fence green (it is inside the wiringCensus walker); (f) NEVER the whole suite, never a build, never `--write`/`--update` on any register but the lighting door.
6. **Report:** the target dock's tip; the list of carried commits with their new shas; the three conflict resolutions with their arms' outputs; the register commit's figures; the U6 summary; the typecheck line; the OSR and writer-reach dry lines; `git rev-list --count f3ab08f51..HEAD` (expected 10: nine carries + one register); porcelain 0; and, for the chair's landing prediction, the lighting census tuple BEFORE and AFTER the door and the count of `it()` titles the consist adds (the door prints it).

## Fences
No edit to any file except through the cherry-picks and the three conflict resolutions and the one register commit; no product surface, composer, pool text, persisted shape, seed input; no push, no stash, no rebase -i, no amend, no `git add -A/-u/.`; the SOURCE dock is never modified. Trailers on the register commit exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
A claim without its executed tail is not a claim.
