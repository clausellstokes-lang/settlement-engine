# RESUME-PARALLEL — resuming a PAUSED parallel lane with a FRESH Opus agent (the chair, session a9df403c, 2026-09-20)

The four parallel lanes below paused at the gate on 2026-09-19 and their agents died with that session. Their WORK SURVIVED, STAGED in their worktrees; each wrote a resume note in its scratch. A fresh Opus agent resumes ONE lane at a time when the chair hands it the gate ("the gate is yours"). `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`; `KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19`.

**Read first, whole:** `$KIT/briefs/LANE-PARALLEL.md` (the shared parallel-lane brief — its verbs bind you), then YOUR lane's resume note (below), then `git -C <worktree> status --short` and `git -C <worktree> diff --cached --stat` — the staged work is the previous agent's; read every staged hunk before you run anything, and never withdraw a staged edit without stating why in your report.

| lane | worktree · branch · base | note | what remains |
|---|---|---|---|
| **FIX-G** (FIX-G2 the food card's bar named + caption corrected; FIX-G1 the viability summary counts the published list, built TO THE GOLDEN) | `$SP/lane-fix-g` · `fix-food-card-2026-09-19` · `ad7ddf2c9` | `$SP/lane-fix-g-scratch/FIX-G.lane-resume.md` | ⚠ ONE UNSTAGED hunk on `src/generators/economy/viability.js` beside the staged one (the previous agent was mid-edit: it collapses the long `buildViabilitySummary` comment to one line that points at the caller) — read it, confirm the note it points to exists in the STAGED hunks, then stage it or withdraw it with the reason; then the note's batch steps 1–11 in order (its steps 6 and 7 ALREADY RAN once at 20:35 — the golden suite red on exactly the predicted one row, `town\|germanic\|mountain\|mountain_pass\|civilized`, and `proseNumerics` red on rows 615→628 / 710→745 which are the BASE's own debt, cured on the consist by CURE-B — that red is LAWFUL at this base and is quoted, not cured); then TWO commits by pathspec (G2 first), the receipt, the report |
| **TOOL-2b** (the dist-gate guard's predicate tightened to `dist/index.html` + `dist/assets`; its tests in their own `tests/lint` file with a mutation row) | `$SP/lane-tool-a` · `tooling-a-2026-09-19` · `6fefb69e6` (TOOL-1 `52be5a1f2` + TOOL-2 `6fefb69e6` already committed; the branch's base is `ad7ddf2c9`) | `$SP/lane-tool-a-scratch/TOOL-A.lane-resume.md` (32 KB; read whole) | six files staged; the gated batch the note names; ONE commit by pathspec; the receipt (`$SP/lane-tool-a-scratch/TOOL-A.receipt.md` exists for TOOL-1/2 — append TOOL-2b's). The chair adds the standard's anti-vacuity sentence and the pre-proof brief's measured-negative sentence at COMPOSITION, not you |
| **FIX-F** (F2b: the `weight` → `weightWord` slot rename across the receipt pools, then `weight` joins the prose-numerics tokens; F1 committed `5c6c6df22`) | `$SP/lane-fix-f` · `fix-followups-2026-09-19` · `76be138a1` | `$SP/lane-fix-f-scratch/FIX-F.lane-resume.md` | ten files staged for F2b (measured NOT a stop: 40 sentences rendered under both slot names, 0 byte differences; no golden or cell key sees a slot name — re-run that measurement before the batch); the gated batch; ONE commit by pathspec; the receipt |
| **TOOL-3** (a census of every emitted worker and unguarded chunk; measured ceilings with a declared 4 KB headroom for `advanceInterval.worker` and `townSceneExport.worker`) | `$SP/lane-tool-3` · `tooling-3-2026-09-19` · `e5bdfd031` (a REAL `npm ci` install) | `$SP/lane-tool-3-scratch/TOOL-3.lane-resume.md`; evidence `TOOL-3.evidence.md`; the census `chunk-census.mjs` | `tests/build/workerBundleCeilings.test.js` staged; its window is HEAVY on the gate seat (two real builds through the EXCLUSIVE mutex + `verify:dist` bare) — it goes LAST in the gate order and only when the chair says the seat is free for a build; ONE commit by pathspec; the receipt with every measured ceiling and its attribution |

**The laws that bind a resume, restated:** PAUSE-AND-RESUME at every gated batch (the brief's verb 4 — you were handed the gate once; if your batch ends and more gated work remains, end your turn `PAUSED AT THE GATE — <note path>` again); every vitest line through the mutex, SHARED tier, worker-capped, the two exports spelled inline (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE test directory per invocation, a line with no printed count DID NOT RUN; `UPDATE_GOLDEN`, `GOLDEN_SHIFT_SIGNED` and every `UPDATE_*` baseline door FORBIDDEN; the lighting census is FROZEN at `2650·383·2267·25028·6677` (as of `1e5bcf83d` on the consist; your branch's base is OLDER, so the walker's red at your base is the base's own distance plus your delta — record the measured tuple and YOUR delta, never refreeze); pathspec commits only, one per car, the body carrying every count line; `git show --stat HEAD` names exactly your paths; the standing instruments (negativeAssertionAnchor, voiceMechanics, mutationCoverageManifest, eslint on every touched file). FORBIDDEN paths: `$SP/consist`, `$SP/slot-2`, every other lane's worktree and scratch, the read tips, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

**Your report:** the receipt filled by execution (every claim CONFIRMED with its count line, or PLAUSIBLE); the commit sha(s); the lighting tuple measured at your base and your delta; and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work). The COMPOSITION of your commits onto the integration branch is the chair's act at train EM-T4 (cherry-pick onto the then-tip; your focused suites re-run there) — you never touch the integration branch.
