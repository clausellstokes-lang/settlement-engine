# LANE-EM-B1A-REPIN — the dispatch brief (Opus COMPILE lane; given 2026-09-19 ~14:2x EDT by the successor chair, session 7d3418f8)

**Seat:** Opus COMPILE lane — you re-measure and re-write a DRAFT packet; you do not implement, and you run no gate. **Standing rules:** the shared compile brief `briefs/LANE-EM-COMPILE.md` in the chair kit binds whole (its PATH LAW, "navigate by symbol", "a fact without a command is not verified", "never adjudicate — a refuted premise is BLOCKED with the smallest measured contradiction"), with the two substitutions below.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · kit = `$SP/chair-kit-923472dc` (READ-ONLY to you).

**Substitution 1 — the tree you read.** READ ONLY `$SP/read-tip-023eda2ec` (a detached, read-only worktree of the build branch `fixes-2026-09-18-consist` at `023eda2ec2f8dd2d9286584d2496c6ac4ea5309e`; confirm `git -C` that path `rev-parse HEAD` first, STOP if it differs). It has NO node_modules: measure with `grep -n`, `git grep`, `sed -n`, `wc -l`, and `node -e` over plain ESM imports that need no package. FORBIDDEN: `$SP/consist`, `$SP/lane-em-b3b` (a build lane holds it), every other lane directory; the ledger checkout `/Users/cstokes/Desktop/settlement-engine` is readable ONLY through `git -C <it> show HEAD:<path>` (its working files are stale). Never edit, stage or commit anywhere.
**Substitution 2 — where you write.** WRITE ONLY under `$SP/lane-b1a-repin-scratch/` (create it). Start by COPYING the three inputs there: `$SP/chair-kit-923472dc/packets-waiting/EM-B1a.md`, `EM-B1a.manifest.json`, and `$SP/chair-kit-923472dc/findings/EM-B1a.evidence.md`. Your products are the re-pinned `EM-B1a.md`, `EM-B1a.manifest.json` (only if a row must move — say so either way) and an APPENDED section in `EM-B1a.evidence.md`. The chair places them; you never write into the kit.

## The order you are executing — design §19 ruling 6 (read it whole: `git -C /Users/cstokes/Desktop/settlement-engine show HEAD:docs/DESIGN_EDIT_MODE_AND_DECREES.md`, §18 and §19; also ruling 7's first sentence)
"Of the four predicates EM-B1a declared ABSENT: 'a plot in motion' is LIVE (`worldState.stressors.some(s => s.type === 'coup_detat')`); 'an open route' is TWO predicates — LIVE via `REGIONAL_CHANNEL_TYPES` `trade_route` with status `confirmed` (`region/graph.js`), and LIVE-GATED via `readRouteNetwork(ws).edges` behind `routeLifecycleEnabled` (off by default; the seal names which). 'A pending peace offer' and 'an envoy arrived at us' confirm as gaps — with the nuance that an envoy's arrival is LIVE on the SENDER's errand (readable across the campaign for a real neighbour) and absent only for a phantom. EM-B1a's `worldConditions.js` is re-pinned to this before promotion."

## The previous chair's verification at the old tip — HINTS, re-find every one BY SYMBOL at 023eda2ec and quote the command
- `COUP_STRESSOR_TYPE = 'coup_detat'` — `src/domain/worldPulse/coup.js` (~:51); `worldState.stressors` — `worldState.js` (~:318) ⇒ `plotInMotion` LIVE.
- `src/domain/region/graph.js`: `'trade_route'` (~:51 / :84) with status `'confirmed'` (~:68 / :626) ⇒ `openRoute` LIVE by the regional channel.
- `readRouteNetwork` — `routeNetworkLedger.js` (~:450) behind `routeLifecycleEnabled` ⇒ LIVE-GATED. ⚠ The survey cited `simulationRules.js:976` for the flag and that path does NOT exist at `src/domain/simulationRules.js` — LOCATE the file that declares `routeLifecycleEnabled` and its default before citing anything.
- `ENVOY_POSITION_BANDS` includes `'arrived'` — `envoyErrandVocabulary.js` (~:116), on the SENDER's errand ⇒ `envoyArrived` is readable for a real neighbour, absent for a phantom.
- `peaceReasons.js`: 0 hits for offer/pending ⇒ `pendingPeaceOffer` ABSENT confirmed.

## The chair's ruling on the SHAPE (so the lane does not have to adjudicate)
1. The roster stays TEN ids. `plotInMotion` flips to ✅ LIVE with its reader spelled by symbol (the stressor type through its exported constant, never the bare string, if the constant is exported — measure).
2. `openRoute` stays ONE id answered by TWO readers: the regional graph's confirmed `trade_route` channel (LIVE), and — only when `routeLifecycleEnabled` is on — the route network's edges (LIVE-GATED). The row carries, AS DATA, which reader answered (the ruling's "the seal names which"), in the same data position the draft already uses for the `source` note. Measure how a pure `(record, campaignState) => boolean` predicate reaches the rules flag; if it cannot without a third argument, that is a BLOCKED finding with the measurement — do not invent a signature.
3. `envoyArrived` and `pendingPeaceOffer` remain the TWO `source: 'EM-E4'` rows (E4 lands the inbound-envoy read over the sender's errand and the pending offer — ruling 7). `envoyArrived`'s note records the nuance (LIVE on the sender's errand for a real neighbour; absent for a phantom), by symbol.
4. The summary line becomes "eight live (one of them gated by `routeLifecycleEnabled` for its second reader), two honestly absent"; §16.2's count of `source: 'EM-E4'` rows, §16.4 and acceptance cases A2/A4 say TWO where they said four; every other place the draft says "four" of these predicates follows (grep the whole file for `four`, `EM-E4`, `six live`, `absent` — report each site you changed and each you left, with the reason).
5. RE-MEASURE the budget the change touches: `worldConditions.js`'s estimated effective lines (two more live readers) against the 250 leaf cap, and the packet's ≤400 total. Over budget = STOP AND SPLIT with the proposed line, never a squeeze.
6. The packet version becomes 2 with a one-paragraph "what version 2 changed and why" in the header's version line (the form EM-P2's header uses). Status stays DRAFT. The verified base stays as it is unless a requiredSymbols path moved between `d31af2cee` and `023eda2ec` — run `git -C $SP/read-tip-023eda2ec diff --stat d31af2cee 023eda2ec -- <every requiredSymbols path>` and report; the chair re-pins the base at promotion, not you.
7. New readers this re-pin names (`coup.js`, `region/graph.js`, `routeNetworkLedger.js`, the rules file) — decide BY THE STANDARD whether each is a `requiredSymbols` row (a symbol the deliverable must PRESERVE and that exists now): add the rows with the exact declaration text as the symbol, each proven by a quoted `grep -n`.

## Report (your final message; also `$SP/lane-b1a-repin-scratch/EM-B1a.repin.report.md`)
Status (DRAFT re-pinned, or BLOCKED with the smallest measured contradiction); the three file paths; a table of the ten predicates with verdict, reader symbol, file:line at 023eda2ec and the command that proved it; every site changed; the budget figures; the requiredSymbols delta; any question only the chair can answer. Every claim CONFIRMED (a quoted command) or PLAUSIBLE.
