# FOUR COMMITS IN ORDER: (A) the chair's cut 2 on DS-DEF-2 · (B) dock hygiene: the declared face-count grow · (C) CAR 8b-W-18l ATTRIBUTION BY ROLE · (D) CAR 8b-W-18m THE COMPROMISED ROLE

Dock: `$SC/kit/laneRW-DEF2` = /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneRW-DEF2 — HEAD `7761350e1`, porcelain clean at start (verify; if not clean STOP and report). Never touch the main tree /Users/cstokes/Desktop/settlement-engine. Never push. Stage explicit files. `/usr/bin/grep`, never bare `grep`. THE CHECKPOINT LAW: files to disk as soon as known; commit each step as soon as green; if cut off, the last line you write says what landed and what is on disk. The law in plain words: /Users/cstokes/Desktop/settlement-engine/docs/rewrite-retro-2026-09-12/THE-LAW-CONSOLIDATED-2026-09-13.md (§4, §6, §10); the binding text of rulings 25 and 26 with every edge: /Users/cstokes/Desktop/settlement-engine/docs/rewrite-retro-2026-09-12/verbatim/BRIEF-ADDENDUM-18.as-signed.md (search "25. ATTRIBUTION BY ROLE" and "26. A COMPROMISED ROLE"). Read both before (C) and (D). Cars 18i/18j/18k just landed (`git log -3`): read their commit bodies for the kernel's new shape (ARCHIVER_SOURCE, WEIGH_KIND, PAIR_JOINTS, joinPairFaces, eligibleFaces; section (2b) of the card instrument).

## (A) THE CHAIR'S CUT 2 — one commit, the annex only + regeneration
Apply EXACTLY the three replacements in /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/rewrite/DS-DEF-2/ds-def-2--invasion-war-walls-with-no-force/chair-cut-2.md to `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` under `### DS-DEF-2` → the bold pool line `Invasion & War: walls with NO force` (rows 1·3, spine 2, spine 3; every other byte of the pool unchanged). Then the census/generator sequence (`node scripts/wiring-census.mjs` → `node scripts/generate-dossier-state-prose.mjs` → census → both `--check`), the per-commit tests the previous gates ran (see `git show 569e01793` for the exact list), the wave gate `node scripts/prose-wave-gate.mjs --section defense --arm cure --round 1`. Commit: `REWRITE 8b DS-DEF-2 cure 2 (v3, the chair's cut by name): 1·3 disagrees on the dispute, not on who pays; spines 2 and 3 in the engine's own words` — body: the re-refute's floor-4 finding quoted (rerefute.md in the packet dir), the ground of the cut, the gate lines.

## (B) DOCK HYGIENE — the declared face-count grow (one commit)
The cure gate at `569e01793` landed the first SOURCED faces and pairs without re-freezing the register: `docs/content/prose-shift-register.json` row `face-count-per-variant` pins 2266 / largest 1 / digest `0fa317c1…` while the leaves measure 2275 / 4 / `e7ed6696…` (now different again after (A) — measure fresh), and the arms "the shipped leaves carry no sources and no pairs" (contract), "CARRIES NO SOURCED FACE AND NO PAIR" / "THE WHOLE SHIPPED CORPUS TAKES NO HASH" (kernel), "byte-identical to readStateProse" / "reproduces car 1's pieces[0]" / "ZERO SHIFT … with any roster" (composer) pin a zero-shift world that the first pool has lawfully ended. Re-freeze the register row as a GROW with the register's own `--write` path (find it: `/usr/bin/grep -rn "face-count-per-variant" scripts tests | head`), declared in the commit body as the one-time shift of the first v3 pool; re-pin the zero-shift arms to the new truth (the corpus carries N sourced faces and M pairs on DS-DEF-2 — assert the exact numbers from the leaf, and keep a zero-shift arm for every OTHER block). The copy-debt red (`labelBands.js` em 0→5, `generalStateProse.js` em 0→3) is NOT yours: prove it pre-exists at `569e01793` and name it in the body. After (B) every per-commit test the gates run is green; print the totals.

## (C) CAR 8b-W-18l — ATTRIBUTION BY ROLE, DRAWN ON A ROLL (ruling 25)
Mechanism: (1) `src/data/institutionRoles.js` — a table by CATALOGUE ROW NAME (the names in `src/data/institutionalCatalog.js`) with class defaults from `src/domain/factionBacking.js`'s INSTITUTION_CLASSES: each entry `{ role: 'a clerk in the hall', n: 'sg' }` / `{ role: 'dock workers', n: 'pl' }`; many per row (aim 4–8 for the common rows: town hall, chapel/church/cathedral, craft guilds, market, taverns, docks, town watch, garrison, court/meeting hall, burial ground; and the universal roads roster for the stranger: a traveller · a pedlar · a drover · a carter · a pilgrim …); THE NAMED OFFICES the tier's NPC roster prints (the mayor, the parish priest, the guard captain, the harbour master, …) as `{ role: 'the mayor', n: 'sg', office: true }` on their rows — find the roster in the generator (`/usr/bin/grep -rn "Guard Captain\|Parish Priest" src | head`) and key on it; REALM roles ('a subject of the king', 'a crown tenant') ONLY behind the engine's recorded form of rule (`/usr/bin/grep -rn "monarchy\|lordship" src/domain/causalState.js src/generators/power/rulingStructure.js`) — if no field records a monarchy, leave realm roles OUT and say so; custom rows may declare `roles` (schema/custom-content.manifest.json — ⚠ a manifest field REWRITES migration 185's frozen block via the manifest generator, an OWNER-GATED surface: do NOT add the manifest field; leave a TODO comment naming it and report it as OPEN). (2) The fill: beside `{defmaterial}` in `src/domain/display/stateProse/defenseStateProse.js` (`SLOT_FILL_TABLES`, `defmaterialFill`) add the SOURCE slots `{hall}` `{guild}` `{register}` `{tavern}` `{watch}` `{gate}` `{market}` `{court}` `{elders}` `{stranger}` `{muster}` `{garrison}` (the kernel's FACE_SOURCES minus archiver) filled at render from the roles of the town's resolved rows of that source's class (faceSources.js already maps rows → sources; reuse it), drawn by the seeded draw, and the NUMBER-AWARE VERB slot `{v:say}` `{v:hold}` `{v:report}` … rendering 'says'/'say' by the number of the most recent role slot in the same sentence (a small table of irregulars: is/are · has/have · does/do; else +s/+es); capitalise a role at a sentence head (after `. ` `? ` or at the face start). (3) NO REPEAT ON A PAGE: the composer keeps the roles already printed for this settlement in this render and the draw excludes them (a per-render set passed down; fall back to repetition only when the roster is exhausted). (4) The grammar: a source slot in a face must match the face's source tag (a `{tavern}` slot in a `[hall]` face is a refusal); a `{v:…}` with no preceding role slot in its sentence is a refusal; slots stay a subset of the spine's EXCEPT these source and verb slots, which are licensed on every face (amend `assertFaces`' subset rule by name). (5) Pins: the table's shape (every row's roles non-empty for the ten common rows; no minted proper names; no office marked where the roster does not print it); the fill (sg/pl verb agreement; the capital at a sentence head; the no-repeat set); the draw's determinism (seed → same role); the zero-text-shift proof (the shipped corpus has no source slots yet → generated files byte-identical). (6) THE RE-CUT OF THE FIRST POOL to slots: in the annex, DS-DEF-2 `walls with NO force`, replace the class-word attributions with slots and verb slots where a source speaks ('A clerk in the hall puts' → '{hall} {v:put}'; 'The guilds pay' → '{guild} {v:pay}'; 'At the tavern they name' → keep as is or '{tavern} {v:name}' — your call, recorded; 'The watch says' → '{watch} {v:say}'; 'Whoever holds the way in stops' → '{gate} {v:stop}'; the elders/court/stranger likewise) — the spine's survey lines unchanged; regenerate; --check; render `node scripts/prose-render-page.mjs DS-DEF-2 'Invasion & War: walls with NO force' --towns 2 --world-run --years 3` and paste the three drawn lines in the commit body so the roles are seen. One commit for the mechanism + one for the re-cut.

## (D) CAR 8b-W-18m — THE COMPROMISED ROLE (ruling 26 with edges (a)–(j))
Mechanism: (1) `compromisedSourcesOf(settlement)` beside `sourcesOf` in `src/domain/display/stateProse/faceSources.js`: from the engine's covert fields — find them: the birth capture / council capture (`/usr/bin/grep -rn "capture" src/generators/power/rulingStructure.js src/domain/display/stateProse/powerStateProse.js | head`), the covert criminal bloc (`blocs.some((b) => b?.covert === true)` in powerStateProse.js:804 and its source), the unexposed officer, a cult's hand on the register (the faith standing 'cult' with a covert flag if one exists — else omit and say so) → the source each compromises (hall · watch/court · its seat · register). (2) The tag `[<source> · compromised]` in the grammar (parseFaceRow/assertFaces): refused on a source no covert field can compromise (a closed table COMPROMISABLE = the sources (1) can return); at most one compromised face per source per variant. (3) The kernel: `COMPROMISED_SPEAKS = 0.7` with the chair's reasoning in the docblock (the 3-year table: 0.6 → silent at least once 78% / never speaks 6%; 0.7 → 66% / 3%; 0.8 → 49% / 1%; the owner's to tune); the speaks-or-silent roll seeded on (world seed, pool key, settlement id, CURRENT YEAR) — find the year the render sees (`/usr/bin/grep -rn "currentYear\|year" src/domain/display/stateProse/composeStateProse.js src/domain/prose/composedWalker.js | head`); the roll is used ONLY to decide whether the compromised source speaks, and the docblock states the principle: a year-seeded roll decides WHO SPEAKS, never WHAT IS TRUE. (4) The draw: on a pool whose SYMPTOM the covert field marks (a per-block table `COMPROMISED_SYMPTOM_POOLS` — for DS-DEF-2 mark the pools that touch the purse/accounts for the hall, the gate/watch pools for the watch; keep it small and recorded), when a compromised source is present AND the roll says speak: force that source into the unit (as the single face, or as one half of a `disagree` pair where the writers marked one) and prefer its `compromised` candidate; when the roll says silent: exclude that source from the unit's draw entirely. On every other pool the compromised source draws as any source. (5) The refuter's instrument: the card (prose-mark-card.mjs) prints a section "COVERT FIELDS AND THEIR SYMPTOMS ON THIS BLOCK" — per covert field, the source it compromises and the pools whose symptom it marks — so the refuter judges a compromised face under the inverted test (reassures/conceals on its own symptom; denies nothing else). (6) Pins: the roll's determinism and rate (over 10,000 seeded rolls the speak share within ±2 points of 0.7); the forcing on a symptom pool and the non-forcing elsewhere; the silence path (source absent from the unit); the audience gate (no compromised face carries a dm-only mark; the page text never contains the covert field's name); the grammar refusals; zero text shift on the shipped corpus (no compromised faces exist yet → byte-identical). One commit.

## Return, per commit in order
COMMIT · FILES · PINS · SHIFT (byte-identical yes/no; the declared grow in (B)) · GATE (exact lines + totals) · HAZARDS · OPEN. Then the final line: HEAD and `git status --short | wc -l`. If the session cuts you, the last line names what landed.
