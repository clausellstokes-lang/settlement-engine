# LANE CHARSET2 — the charset wall's wiring, the typed rejection surface, and the CS-9 RESTORE split
Dock: `$SP/laneCHARSET2-tree` (create it from the post-§891 product tip; the chair will name the sha at dispatch). Scratch + receipt: `$SC/charset2/`.

## ⭐ THE RULING THAT UNBLOCKS YOU (§893, the chair, vetoable)
**CS-9 is RULED: an account import of a user's OWN export is a RESTORE PATH, not an authoring wall.** Build the split. The import hydrates, binds and exports the pack, marking any entry the renderer cannot draw as `charset_legacy` and surfacing it for repair; it does NOT refuse the pack. The wall stays ABSOLUTE on the AUTHORING path. The ground is recorded in `$SC/RULINGS-893.md` §1 — read it, and if the code refutes it, STOP and bring evidence rather than building around it.

## WHAT TO BUILD (HORIZON-DARK's CHARSET Car 2 — the car list is `$SP/horizondark-dock-residue/HORIZON-DARK-CARLIST.md` §3 car 7; read that entry in full first)
Wire the charset wall (landed as CHARSET Car 1 in the §891 train — `src/domain/content/customContentCharset.js` + `.generated.js`) into the custom-content editor and the import path, and give a refusal a **typed, legible reason** instead of a silent mangling. Files the car list names: `customContentManifest.js`, `customContentCommands.js`, `customContentSliceRuntime.js`, `customContentCore.ts`, `aiOutputSchema.js`, `aiCharter.js` (+ the `build:edge-shared` re-mint), `CustomContentEditor.jsx`, `en.js`, and the edge twin.
⛔ **DO NOT FLIP THE POLICY.** `charsetPolicy.enforcement` stays `report`. The flip to `refuse` is the LIGHTING WAVE's act (L-DOORS), and it is gated on your CS-9 restore plant being green. Build the split and STOP before the flip.

## PROOFS THE CAR OWES
1. **The CS-9 restore plant** — the single STOP that decides whether the wall may ever flip: a legacy library carrying non-ASCII names still **hydrates, binds, exports and restores** with the wall active. Drive the REAL import path; a fixture tree under your scratch dir, never a plant into `src/`.
2. A typed refusal carries its reason to the surface (not a boolean, not a silent strip) — assert the reason token, not the message text.
3. A negative control: remove the split and the restore arm REDS. Restore byte-exactly, verify by sha256 (never `git checkout`), re-run green.
4. `build:edge-shared` re-mint: if you touch `aiOutputSchema.js`/`aiCharter.js`, the edge bundles must reproduce AT THE COMMITTED TREE. ⭐ The sequence, proved twice: materialise ONLY `immer` and `seedrandom` → build → commit the bundles → **RESTORE THE SYMLINKS**.

## REGISTERS — predict in writing, do not take
The car list predicts `.generated.js` moves the OSR execution-input digest and costs a governed absorb — **that is a landing act, not yours**. Predict the lighting census delta (files/credited from any new test file — prefer extending an existing file) and the OSR movement; measure what a single-file run can measure; hand the rest up.
