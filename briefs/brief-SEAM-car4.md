# Brief — SEAM train, ARCH car 4: THE SCHEMA, KEYS ADDED ONLY — `poolMeta`, `vid`, the three leaves with floors, the SHIFT REGISTER

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 4. Dock: `$SC/laneSEAM` at car 3g's tip. Read first, whole: ARCH **§2.3** (the schema: `poolMeta` RENDER half only — `role`, `variantCount`, `faceCounts`, `vids`, `relation?`, `form?`, `move?`, `attach`, `explains?`, `spines?`, `covers?`; `Variant.vid`, `wordings?`, `grammar?`; the three leaves `dossierConnectives.generated.js` (the FOUR reachable lists only; lengths PINNED in the header), `proseNorms.generated.js` (the departure BIT per pool — frozen at birth; from car 0's print at the 10 % line, SITTING §O.4), `dossierRelations.generated.js` (projected from the engine's sources (a)–(c) with direction — at this tip its JOIN is EMPTY: the leaf carries the 165 rows keyed on producer tokens AND an `aliases` section that is EMPTY until the sitting ratifies; no row joins a desk reading; assert it)), **§2.5** (the annex grammar: every typed line and its refusal), **§2.6** (the two-level ruling; `vid` = the annex row number at the freeze; the seven `canon 1 of 4` rows keep one face; NEVER TRIM), **§8.6** (the SHIFT REGISTER: every mechanism whose change re-rolls a drawn index, with its pin and door; `docs/content/prose-shift-register.json` printed by the contract test), **§12 row 4**, **§16** (the branch grain: `reads` transcribed from the census into the authoring half — the census JSON — never into `poolMeta`; the index-stable draw SIGNED at SITTING §N.2 — its `vid` identity is minted HERE but the draw formula flips only at Shift 1, car 8a); `scripts/generate-dossier-state-prose.mjs` whole (`:326-348` the typed-line branch to copy; `:406-411` the numbering guard; `:432-452` `assertNothingDropped`; `:533-554`; `:556-582` the variant shape; `:706-713` `--check`); `tests/data/dossierStateProseProjection.contract.test.js` whole; `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` (the annex; 585 KB; you ADD typed lines and touch NO sentence).

## What to build (every item an EXECUTED arm; the manifest byte-identical throughout)
1. **The projector learns §2.5's lines** (`ROLE` · `READS` · `NARROWS` · `RELATION` · `FORM` · `MOVE` · `ATTACH` · `EXPLAINS`/`SPINES`/`COVERS` · the `[grammar: Vn]` tag routed away from `marks` · the `[face]` sub-rows · the `§7b THE STATE CONNECTIVES` section) with every refusal row of the §2.5 table as a thrown error with a plant that proves it; today the annex gains ONLY what the migration needs: nothing per variant (no faces yet — Shift 1 is car 8), and `poolMeta` is EMITTED for every pool from the projector's own knowledge: `role: 'spine'`, `variantCount` (pinned at today's count), `faceCounts` (all 1), `vids` (the annex row numbers, stable), `attach: []`. The 68 STATE-KEY prose lines are transcribed into the census's AUTHORING half as `reads` only where they agree with the branch grain (a disagreement is printed, never resolved by the lane).
2. **`vid` on every variant** (the annex row number at the freeze) — a renumbering that would move an existing vid is a projector error (plant it).
3. **The three leaves with floors only:** connectives — `consequence.clause` and `tension.sentence` EMPTY lists with their floors (≥ 3) pinned in the header as OWED, `contrast.sentence` and `addition.sentence` exactly `['']`; norms — the departure bit per pool from car 0's RATE print (`node scripts/wiring-census.mjs --print` or the committed JSON; 70 of 267 fired pools read 1; unfired pools `null`); relations — the 165 rows with direction and an EMPTY `aliases` map (the join asserted 0 at this tip). All three in the lazy chunk by the existing `/src/data/` rule (car 2's chunk-membership arm must stay green).
4. **The SHIFT REGISTER** `docs/content/prose-shift-register.json` — every pinned mechanism (variant count per pool; face count per variant; vids; connective list lengths; pool-key rename; the norm bit; attach sets; the draw formula; the comparator, band rule, departure line, position budget; registry ids; instance keys) with its pin, its door and its declared-row idiom; the contract test PRINTS it and asserts every pin against the leaves.
5. **The contract-test arms:** `--check` green; the key-by-key leaf diff prints `0 ADDED / 0 REMOVED / 0 CHANGED pools`, only `poolMeta`/`vid` keys added; the variant ratchet unchanged at 2,266; every pin convicted by a plant (an appended variant; a fifth face; a renumbering; a fifth relation key; a numeric pool key `^\d+$`; an unmarked variant on a covert-source pool (car 0's `covert` column); a spanning attach set; a proper-slot-initial face; a digit in §7b; a longer connective list); the DRIFT manifest byte-identical (drift `[]`); the sha interlock between the census JSON and the leaves.
6. **Proof:** the whole `tests/lint` once; strict on its ceiling; voice E2 two files; OSR/PN exact (the leaves are data — confirm prose-numerics does not scan them, or that they carry no numerals); typecheck; car 2's byte ratchets (the leaves' bytes declared as rows); the lighting door by ritual.

## Discipline, fences, commits — as car 3a's
Change only: the generator script, the annex (typed lines only — a sentence-level diff must be EMPTY: assert `git diff --word-diff` shows no prose word moved), the six leaves (regenerated), the three new leaves, the shift register, the contract test, the byte baseline (declared rows), the sweep + manifest by text, the receipt. NO composer, desk, persisted shape or seed input. Commit `SEAM car 4: the schema, keys added only — <clause>` (+ 4b); trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha(s); the leaf diff line; the pins and their plants' reds; the manifest drift; the register printed; anything refused with its measurement.

## ADDENDUM (chair, 2026-09-08 12:2x) — the open schema row from car 3a: `readsCount`
Car 3a's composer bounds `k ≤ 3 − |spine.reads|` but the shipped `PoolMeta` carries no `reads` (ARCH §16: `reads` lives in the census's AUTHORING half, never in `poolMeta`), so the composer reads an absent `reads` as one fact. **Ruling:** the projector emits `poolMeta.readsCount` — an INTEGER, the branch grain's field count for that pool, read from the census JSON's authoring half at generation (the JSON is the truth; the projector never derives it) — absent where the census has no `reads` row (the composer keeps its one-fact default there, unchanged). The contract test asserts `readsCount` equals the census's count for every pool that has a row and is absent for every pool that has none; a plant that moves the census count without regenerating reds at the interlock. No text moves; no draw index moves (a render-half integer is not a mechanism of the SHIFT REGISTER — assert it there by name). Tip for this car: car 3g's, plus the census re-take rule in the 3b–3g addendum.

## ADDENDUM 2 (chair, 2026-09-08 14:4x) — your tip is `c45a46a78` (car 3h), and three facts from 3h that bind you
- The census at your tip has RESOLVED 340 (DS-DEF-2 26/26 on rungs 1 and `table`); `stamp.files` carries the six desks' and six candidate leaves' shas. Your projector and leaf changes touch stamped files (the projector is stamped? — read `STAMPED` in `scripts/wiring-census.mjs` and say which) — the generalised rule applies: a stamp-only drift is your plain re-take with the printed proof, in the same commit; a row move is REFUSED with the diff (car 4 adds keys only and moves no row).
- The island fence scans `src/` for the census module's bare name in RAW TEXT, comments included — cite the directory, never the module name, in any docblock you write.
- The alias draft's `docblock` evidence kind mints a `draft.rows` entry from English words on a comment line (3h's finding; cure chartered to 5b) — do not write a comment that pairs a producer token with a reading token on one line; if `draft.rows.length` moves under you, that is the cause, and you reword the comment.
