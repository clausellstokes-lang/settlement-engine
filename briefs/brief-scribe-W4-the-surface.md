# Brief — SCRIBE W4: THE SURFACE, DARK — redraw, the epoch record at advance time, daily life by default, the DM's survey notes
(chair Fable 5.1, 2026-09-14 ~20:5x; the owner 13:2x: "continue the scribe work to completion"; the Scribe is its own tier, dark behind `FLAGS.scribe`; the corpus is BANKED)

Same dock, laws and gate as W3a–W3d/W3c (read all five briefs, `SCRIBE-RULINGS.md` 1–36, the design ledger's §5b/§5c/§11 via `git show HEAD:docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md` from the main tree — READ ONLY there). Dock `$SC/kit/lane-scribe` at `f350d7ed5` (sealed `refs/preserve/scribe-w3c-2026-09-14`), porcelain 0. ⛔ EVERY BYTE OF THIS WAVE IS DARK: nothing renders, bills, or changes a shipped page while `FLAGS.scribe` is false; the FINITE-SEMANTICS walker, the fence tests and `tests/lint/sizeBaseline.test.js` (`settlementSlice.js` at tolerance 0 — put NOTHING in it) stay green. No `package.json` byte. No push, no migration apply, no data deletion.

## WHAT W2 LEFT (verified by the chair's read at `f350d7ed5`)
- The OPEN trigger exists (`src/hooks/useScribeOpenTrigger.js` → `src/store/scribeOpenTrigger.js` → `lib/scribeRenderer.js` → `src/store/scribeTransport.js`): renders every firing tab when the artefact is stale/absent for `saveId::advanceSeq::renderedFor`, once per key (`ATTEMPTED`).
- The AI-instructions box IS wired: `dossierNotes.aiGuidance` → `guidance` in the POST body → the volatile turn's last section (W3a). Keep; add a test that the open trigger passes the LIVE box text.
- The artefact supports `retireCurrent(prose, {state: 'redone'})` and the past lane (`src/lib/scribeArtefact.js`), but NO redraw action or button exists anywhere.
- The transport sends `record: null` on every render — RULING 21 (the epoch record is computed at advance time and sent at open) is NOT built.
- Daily life is still the OLD path (`DailyLifeTab.jsx` → `requestDailyLife`, five parallel narrative calls, a Generate/Regenerate button); the Scribe's `daily_life` tab today carries ONE pool (DS-ECO-8 `standingOfLiving`).
- The narrative overlay (`DossierNarrativeButtons/Banner`, `requestNarrative`, `showNarrative`) stays UNTOUCHED — W5, ruling 20.

## THE CARS (one commit each; the W3a gate list after each — bundle/static-card/exemplars/census --check, the scribe suites + fence + sizeBaseline + the corpus five with the pin at 2756, deno test/check with the flags, pin.mjs — plus `npx vitest run tests/store tests/hooks tests/components` for what you touch; quote every gate line)

### CAR 1 — REDRAW (design §5c rule 1)
`renderScribe(request)` gains `reason: 'open' | 'redo'`. On `redo`: `retireCurrent(prose, {state: 'redone', at, nonce})` FIRST (the prior render moves whole into the past lane marked redone — nothing is lost), then a full render of every firing tab from the CURRENT card and the stored record (a fresh draw; the model never sees the prior prose), bypassing `ATTEMPTED` for that key. Billed per render through the same SKU (no new pricing byte; `pricing.js` untouched). A DM-only button `Redraw the survey` in `src/components/dossier/DossierActionBand.jsx` (or a sibling the band composes), rendered ONLY when `FLAGS.scribe` is lit AND `saveId` exists AND the artefact has a `current`; disabled while a render is in flight; it names the cost from `getCost('dossierProse')`. Tests: the store action retires then renders; the button is absent dark; the redone epoch is in the past lane with `state: 'redone'`; a redo on an unsaved settlement is refused with the durable-home message.

### CAR 2 — THE EPOCH RECORD AT ADVANCE TIME (ruling 21, §5b)
Find where the campaign advance lands on a saved settlement (the epoch lane `src/store/scribeEpochLane.js` and the advance path it hooks; `advanceCampaignWorld` in `src/domain/worldPulse/`). At the moment the advance makes the artefact stale, compute `epochRecord(prose.current.card, townCard(newSettlement, {...}), campaignState)` while BOTH cards exist and store it as `prose.pendingRecord` (schema bump on the artefact, `SCRIBE_ARTEFACT_SCHEMA` +1, with `isArtefact` tolerant of its absence); the open-trigger render sends it as `record` and clears it on landing. Where `prose.current.card` is absent (a first render, or a compact past epoch) the record is null and the turn says nothing moved. ⚠ `townCard` needs the static card and the world: pass them as the trigger already does; a card built without the static table is lawful (`staticCardJoined: false`). Tests: a `one_season` advance through the REAL advance path (as `pilot.mjs --epochs 12` does) produces a `pendingRecord` whose `delta.counts.moved` > 0 on a town that moved; the next render's POST body carries it; landing clears it; the undo path (§5b UNDO) drops the pending record with the epoch it belongs to.

### CAR 3 — DAILY LIFE BY DEFAULT, AS THE SEVENTH CALL (§5c rule 4)
- `scribeBrief.js`: a DAILY-LIFE REGISTER section in the global brief (the five beats — dawn, the market, midday, the tavern, night — as the archiver's own observation of one ordinary day, every person a role the card seats, every place a row the card carries, no event, no forecast; the unit cap and every mechanical bar as elsewhere).
- The `daily_life` tab's card: alongside its one pool, the card carries five BEAT units under `blockId: 'DS-DAILY'`, `poolKey: 'dawn' | 'market' | 'midday' | 'tavern' | 'night'`, `vid: 0`, `faces: []`, whose corpus line (`unit.spine`) is the OFFLINE deterministic daily-life prose the tab already renders in local mode (`DailyLifeTab.jsx` "deterministic offline prose" — find its generator and reuse it as the fallback and the claim). `SCRIBE_OUTPUT_SCHEMA` is unchanged; the writer returns five more units; tier 0 refutes them like any unit (the referent scan binds hardest); tier 1 the same.
- `DailyLifeTab.jsx`: when `FLAGS.scribe` is lit and the artefact carries `DS-DAILY` units, render the five beats from the artefact (rendered by the kernel's slot fill like any Scribe unit; a FAIL falls back to the offline beat) and HIDE the Generate/Regenerate buttons (the survey-level Redraw covers it); dark, the tab is byte-identical to today. The old `requestDailyLife` path is NOT removed (W5's retirements).
- Tests: the daily_life card carries five beat units with offline spines; a render lands them; the tab reads them when lit and is unchanged when dark (a snapshot/markup control both ways).

### CAR 4 — THE DM'S SURVEY NOTES (ruling 6)
A DM-only, collapsed-by-default panel on the dossier (`DossierSessionNotices.jsx` or a sibling) named `Survey notes`, lit only with `FLAGS.scribe` and a `current` artefact: per tab, the pools whose unit was PATCHED (which seat, which arms), WITHHELD (the arms), or fell to the hand corpus (FAIL, the arms), read from `prose.current.blocks[*][*].verdicts` and the response's `dropped/patched` receipts as the artefact stores them; plain words, no em dash (the E2 ratchet), the arm names as the refuter prints them. Never on the player page (`playerView` / `publicDossier`). Tests: the panel is absent dark and on the player page; a PATCHED verdict row renders its seat and arms.

### AFTER THE CARS
Report per car: sha, files, gate lines verbatim, every JUDGMENT as "chose X over Y because Z", the pilot harness figures if you ran `pilot.mjs --dry --epochs 2` (it should now print a non-null record on epoch 1), and anything undone with the reason. The chair verifies, seals, and — since the surface cannot be exercised without a key — records W4 as BUILT DARK, UNVERIFIED LIVE.
