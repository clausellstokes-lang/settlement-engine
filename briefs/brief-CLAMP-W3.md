# LANE: CLAMP-W3 — the final wave: the ceiling car (62 → 69, seven recorded reasons) — one car, zero product bytes outside the register
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · executes `rulings/RULING-CLAMP-FINAL.md` — read it WHOLE first; it is the ruling, this is the act⟧
READ: `$SC/briefs/_PREAMBLE.md` → the ruling → `tests/lint/clampPrimitiveBaseline.test.js` WHOLE (the `DEF_RE`, `BASELINE_CEILING = 62`,
the CR-WR10-A(b) docblock at ~:40 — the ONE raise the ceiling ever took, its form is your form) → `scripts/.clamp-primitive-baseline.json`
(the 62-file sorted list) → the three sealed clamp receipts (`clamp-w1-rulings-2026-09-05`, `clamp-w2-receipt-2026-09-05`, `clamp-wave0-ruling-2026-09-04`).
DOCK: given at dispatch (the tip after the wave-2 cars land). THE ACT: (1) re-derive the census at your tip with the walker's own `DEF_RE` —
it must read exactly 69 files outside the kernel, the 62 + the seven named; if it reads anything else, STOP (a file moved under you);
(2) add the seven to the baseline list (sorted, the file's own order), raise `BASELINE_CEILING` to 69, and write the seven reasons into the
docblock in the CR-WR10-A(b) form with each recorded source named; (3) prove: the equality arm and the ceiling arm GREEN; the whole
`tests/lint/` (exit + arms — `clampPrimitiveBaseline` must now be green, and NOTHING else may change); plant one of the seven back OUT
of the list and prove the equality arm red; (4) receipt `$SC/receipt-clamp-w3.md` with the census table (69 named), predicted register
deltas (the known-failure census: the clampPrimitiveBaseline row retires at the landing — the chair's `--update`), a RETROVALIDATION ROW.
FENCES: no migration, no product edit, no `--amend`; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: CLAMP-W3`; quiet-window + mutex.
