# CHAIR ADDENDUM to brief-L-UI-MAT.md — Fable 5.1 chair, 2026-09-06 14:15 (session 19ace14d)

**Your dock:** `$SC/laneLUIMAT` (SC = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad`), cut at **dd5f13218** = the §903 CAS = the product tip `claude/composite-r4`; 453 symlinked packages, porcelain 0. Verify both on arrival. Scratch: `$SC/luimat-scratch/`. Receipt: `$SC/receipt-l-ui-mat.md` — write it FIRST as a PARTIAL header, update after every proof.

**READ-FIRST PATH FIX:** the brief's "PLAN.md §6 POSITION 4" is not in the tree. Read it from the sealed ref:
`git -C /Users/cstokes/Desktop/settlement-engine show refs/preserve/light-plan-2026-09-05:lightingwave/PLAN.md` (and `…:lightingwave/receipt-light-plan.md`).

**Where the wave stands:** §903 landed the lit DEFAULT (twenty-one virtual keys into `realistic_regional`, identity untouched) at dd5f13218. L-DEFAULT's hunks 2–7 were REFUSED with measurement and are owner rows (the successor preset id, `infoMode` on the paid surface) or rung-20 work — nothing in your two cars depends on them. If CAR B's dial or CAR A's flags interact with any of the twenty-one lit keys, measure it and say so; do not assume.

**The machine while you work:** EIGHT research workflows run beside you (load 30–70 is normal). No full-suite run is in flight and `$SC/HOLD-VITEST` is ABSENT, so single-file `npx vitest run <file>` is yours, through the mutex (`sh scripts/gate-mutex.sh --run -- …` where the repo's scripts use it). A quiet-window instrument (load < 4) will NOT open under this load — if your brief needs one, wait at most one hour, then report it OWED-TO-THE-CHAIR with the exact command. ⛔ Never bank a load-starved red: a timeout or a `SCOPE SENTINEL` line under this load is re-run singly before it is read into anything.

**Hazards that bite THIS brief (from the chair's memory; verify each against the tree, never trust the note):**
1. **A DISPLAY LABEL CAN BE A PERSISTED HASH INPUT.** CAR A's "display-only shift" is a claim until the fixed-seed engine-hash proof runs at BASE and at TIP and the two hashes match — quote both. If any flipped flag reaches a hash, a save, a golden or a witness, STOP and report; it is not display-only.
2. **A GOLDEN FIXTURE NOT NAMED `*golden*` ESCAPES THE FREEZE ROSTER**, and `it.each` is REFUSED by the each-family park ceiling — if a snapshot family needs re-recording, name every file, its cause, and whether it is enrolled in the freeze register; write the declared shift into `docs/GOLDEN_SHIFT_LEDGER.md` per the estate's pattern (see L-DEFAULT's car `f7a78711d` for the shape).
3. **CAR B touches persisted shape (`_livingContentLawVersion`).** Trace EVERY lifecycle path before the edit and prove each in the receipt: create (new world), read, persist, regenerate (regen must NOT stamp an existing world — THE PROMISE, the brief's STOP), undo/restore, clone, import (account import), and the publicSafe allowlist. A stamp that survives one path and ghosts another is worse than no stamp. The three persistence paths land in the SAME car or the car does not land.
4. **Any byte of `package.json` is a mint trigger** — do not touch it. Never materialise packages.
5. **Never `git show HEAD:<path> > <path>`**, never `git checkout --`, never `git stash` (preamble). Restore by inverse edit, verified with `cmp` against a backup.
6. **vitest prints ACTUAL first, then EXPECTED.**

**Cars, trailers, proofs:** one commit per car; every commit carries `Seat: Opus 5 — Fable-unvalidated` and `Lane: L-UI-MAT`. You OWE the `tests/lint/` directory run (through the mutex) before you report done — quote its exit and every failing arm even when green; if `src/` moved, also run the real `npm run typecheck:domain:strict` and `npm run typecheck` scripts. Predict every register figure IN WRITING before any instrument runs (the chair takes every register act at the landing). End the receipt with the RETROVALIDATION ROW (what was judged · what the Fable chair must re-derive · receipts by path · priority) and a DOCK TIP line (sha · cars over dd5f13218 · porcelain).

**Cap:** you are build lane 1 of 4. No subagents. A session can die without notice — commit each car the moment its proof is green; the receipt is the record.
