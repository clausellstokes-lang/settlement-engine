# LANE: DESK-TIMEBAND — fill `{timeband_age}` on the two desks whose declination went stale, and MEASURE the ladder's ceiling
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## READ FIRST
1. `$SC/briefs/_PREAMBLE.md` — the hard rules. 2. `$SC/briefs/_DESK-LAW.md` — the mount law. 3. `$SC/receipt-desk-gen2.md` FINDINGS 4 and 5 (your work order), and `$SC/rulings/RULING-FOUNDER-SLOT.md` (the chair's sibling rulings). 4. `$SC/receipt-desk-9.md` (ARM 3 — the public-dossier duty of every mount).

## YOUR DOCK
`$SC/laneTIMEBAND`, detached at **`a2af55cde`** (the desk consist tip: 26 cars over product `90702c3e9`, sealed `osr-schema17-2026-09-05`). Build ON TOP; never rebase; the chair replays `laneTIMEBAND:a2af55cde` into the §900 desk consist. ⛔ Never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content.
⛔ **VITEST HOLD IS IN FORCE** until the chair sends RESUME: run NO vitest; module-evaluate with node; record every proof as OWED-UNTIL-RESUME with its exact command.

## THE WORK
1. **Finding 4 — re-derive it.** `powerStateProse.js` (~line 900) and `stressorsStateProse.js` (~line 78) DECLINE `{timeband_age}` "for want of a duration former in the tree". GEN2 measured that `heraldCausalGrammar.js`'s `timeBandOf` / `timeBandWord` IS that former and a ZERO-IMPORT leaf. Confirm both claims by reading the three files (the declination's exact reason, the former's signature and its import graph — `node -e` the leaf). If the former's bands are the same vocabulary the desks' declined variants expect, fill the slot on BOTH desks through the former, keep-both mounts, one sentence mount per page-set, the public-dossier duty on each. If the vocabularies differ, STOP and report the difference — do not coin a mapping.
2. **Finding 5 — MEASURE, do not cure.** `{timeband_since}` is the ADVERBIAL column and the sixth band is PREDICATE-ONLY, so the slot is unfillable beyond a generation while 89 of 108 generated events are older. Re-derive the 89/108 on the same corpus GEN2 used; list every corpus variant that falls silent because of it (by pool id and variant number); state what an ADVERBIAL sixth band would need (a ladder entry + which variants would light). The WORDS are the chair's — write no adverbial form; hand the measurement up.

## FENCES
No new persisted shape; no corpus sentence changed; no register act (record deltas: mounts baseline, prose-numerics, writer-reach, lighting titles). Commit per step, `Seat: Opus 5 — Fable-unvalidated`, `Lane: DESK-TIMEBAND`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-desk-timeband.md`, PARTIAL header first, RETROVALIDATION ROW last, tip sha last.
