# LANE: OSR-SCHEMA18 — re-anchor the observed-shape register inside the product lineage (rung 18), restore the `--write`, retire what the rung retires
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own · 2026-09-05 evening⟧

## READ FIRST, IN THIS ORDER — the history is the brief
1. `$SC/briefs/_PREAMBLE.md` — the hard rules (no `git add -A`, no stash, no hook runs, capture every exit in-shell, seat trailers).
2. `$SC/briefs/brief-OSR-SCHEMA17.md` — the rung-17 brief: the four withdrawn/refused rulings it cites are the record of what NOT to do on this surface.
3. `$SC/receipt-osr-schema17.md` — how rung 17 was minted (three cars: the topology pass `d65329d58`, the rung `0742f8ff5`, the genesis `a2af55cde`), and the commands.
4. `$SC/rulings/RULING-OSR-900-NO-WRITE.md` and `$SC/rulings/RULING-OSR-SCHEMA17-DECISIONS.md` — WHY this lane exists: the genesis car was cherry-picked into the desk composition as `fea88e5b1` over a different base, so `migrationReview.subjectSha` (0742f8ff5) is not an ancestor of the product lineage; `validateBaselineHistory` reconstructs the receipt FROM THAT COMMIT'S TREE and refuses every CLI `--write` until the next rung re-anchors it. ⛔ A chair must not "fix" `subjectSha` by hand — the digests would not reconstruct and the receipt would become a lie.
5. The memory law (chair kit, verbatim in the ruling): a governed register genesis is a LINEAGE act — execute it (or re-execute its subject step) in the dock that LANDS.

## YOUR DOCK
`$SC/laneOSR18`, detached at **`b0cbc67a1`** = the §901 CAS (the product tip; 25 cars over 04bb92d19), porcelain **0**, `node_modules` 453 symlinks. ⛔ Never materialise packages, never `npm install`. ⚠ The product tip will NOT move under you tonight (the next landing is yours); still, read `git -C /Users/cstokes/Desktop/settlement-engine rev-parse claude/composite-r4` before you compose and refuse if it is not your base.

## THE SITUATION, MEASURED AT b0cbc67a1 (re-derive every line before you act — a brief's premise is a claim about a SHA)
- `scripts/.observed-shape-readers-baseline.json`: `schema` 17; `migrationReview.subjectSha` = `0742f8ff5997bb5ddb369c152cb31d587dde7121`; `corpusMeta.simulationFlagsLit` = 80.
- `git merge-base --is-ancestor 0742f8ff5 HEAD` → expected FALSE in this dock (that is the whole finding). If it is TRUE, STOP: the premise is dead and the lane is a different lane.
- `node scripts/check-observed-shape-readers.mjs` (plain) → expected exit 0 (the walker is exact at the tip; the whole-suite proof at d75e807a6 and the §901 gate had no OSR red). `--write` → expected REFUSED by history validation. Record both exits verbatim.
- `tests/lint/writerReach.walker.test.js` has TWO arms banked as declared debt in `scripts/.test-ratchet-baseline.json` and in `tests/lint/testRatchet.test.js`'s `WALKER_ROWS_OWED` ("corpusMeta.simulationFlagsLit must equal OSR's frozen value: expected 81 to be 80", capture magnitude ceiling 81; `OWED_CEILING` 5). They retire at THIS rung — but the retirement is a REGISTER act (the ratchet's remove-only `--update` at the landing's ratchet run) and belongs to the chair's register phase, not to your cars. Your job is to make both walkers read the same figure.
- The rung-17 decisions ruling left two items for "the next OSR write": (2) the ninth exemption's retirement (declared 9 → 8 with the banked count — MEASURE whether §900's DESK-900-CARS car already landed it: read the walker's exemption roster and the register's `manifests`/`rowTags`; if declared already equals banked, the item is DONE and you say so); (3) the register's `_doc` still describing an older schema — MEASURED by the chair at b0cbc67a1: `_doc[9]` reads "SCHEMA 10 = schema 9's topology, tag law and eight-identity M8/M9 bank, ALL UNCHANGED, …" — eight rungs stale; print every `_doc` line that names a schema number or a bank size and cure what is stale IN THE SAME WRITE as the genesis, never as a separate write. ⚠ The walker's own comments at `tests/lint/observedShapeReaders.walker.test.js:372` and `:1254` still say "EXPLAINED_WRITER_EXEMPTIONS, whose roster is still nine" at this tip — so either the §900 roster retirement never landed or the comment is stale: measure `EXPLAINED_WRITER_EXEMPTIONS.length` and the register's banked count and report which.
- L-HOMES-2 (`$SC/receipt-lgt-p2-manifest.md` §"the re-cut") kept the register identifier `ENGINE_GATED_VIRTUAL_RULE_KEYS` on the authored array because renaming it is a DETECTOR-SOURCE change the OSR refuses between rungs, and said "the honest rename rides rung 18". MEASURE before deciding: is the rename a pure identifier swap with ZERO identity movement in the observed-shape inventory? If yes, it may ride this rung as its own car BEFORE the genesis (so the genesis scans the renamed tree). If it moves any identity, REFUSE it with the measurement and leave it for the chair.

## THE ACT — in this order, each step proved before the next; one commit per step; every command's exit captured
0. **Re-derive every premise above** and write the figures into your receipt's first table. Any mismatch → STOP and report.
1. **(conditional) The rename car** — only under the zero-movement measurement above.
2. **Mint schema 18 through the governed migration path**, exactly as rung 17 did (`scripts/migrate-observed-shape-readers.mjs`; read its header and the 16 → 17 diff at `0742f8ff5`: the `LEAF_MIGRATION_PREDECESSOR` row, the predecessor-validator table entry, and the review template). The SUBJECT commit — the commit whose tree the receipt reconstructs from — MUST be a commit in THIS dock's lineage (your own car), so that `git merge-base --is-ancestor <subjectSha> HEAD` is true in the landing dock and stays true when the chair composes this lane's cars onto the product tip by REPLAY. ⚠ THE CHERRY-PICK HAZARD AGAIN: a replayed car gets a NEW sha. So either (a) the chair lands your cars by fast-forward (this dock IS at the product tip; if nothing else lands first, the chair fast-forwards `claude/composite-r4` to your tip and the subject sha survives) — the chair's preferred path, state it in your receipt — or (b) if anything lands ahead of you, the chair re-executes your subject step in the composing dock. Write the receipt so that (b) is a copy-paste of your commands.
3. **The genesis: execute the governed 17 → 18 migration and re-freeze**: the migration review (`--review`, `--json`, `--bundle` as the tool asks), then `node scripts/check-observed-shape-readers.mjs --write` (shrink-only). The `_doc` cure rides THIS write. Inspect EVERY row the write changes by name: `corpusMeta.simulationFlagsLit` must move 80 → 81 (the walker corpus lights SEAT-78's `irregularForceEnabled`; the W-OPS four are NOT lit in the walker corpus — the writer-reach register measured 81, not 85, at §901; if you measure anything else, STOP and explain the figure before you freeze it). A row that vanishes or appears for any reason other than the corpus seeing its writer is a FINDING, not a shrink.
4. **Prove the two walkers agree**: `sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/observedShapeReaders.walker.test.js tests/lint/writerReach.walker.test.js tests/lint/testRatchet.test.js` at your tip. Expected: the OSR walker green; writerReach's two banked arms now GREEN (81 = 81) — which means the ratchet's known-failure census will report two rows that no longer fail: that is the SIGNAL the chair's register phase retires (remove-only `--update`, `OWED_CEILING` 5 → 3, both `WALKER_ROWS_OWED` entries deleted with their comment paragraphs) — DO NOT retire them yourself; the ratchet meta-test may red on "a banked row passed" at your tip: record it as EXPECTED and leave it. ⛔ No unfiltered `npx vitest run` in a lane; no vitest at all if the chair announces a gate.
5. **The capsule**: `node scripts/base-state-capsule.mjs --runtime-tests=<N>` shells out to the OSR CLI and could not run since §900. Run it ONCE at your tip with `N` = the last landing's ratchet totalTests (31970) ONLY to prove the shell-out succeeds; do NOT commit the regenerated capsule (its `--runtime-tests` figure belongs to the NEXT landing's ratchet and the chair regenerates it as a chair car after the totals). Record the exit and the diff it would have made.
6. **Cost**: before/after wall-clock of one full OSR scan (rung 17 recorded +3 s / ≈ +20 %); a four-lane machine's timing is untrustworthy — say so and measure twice.

## ⛔ FENCES
- Zero rows MINTED by this rung (rung 17's law): a rung that adds an identity is refused unless the identity is a writer the corpus now genuinely sees; name it.
- No CONFIG added to the corpus (the 20-new-rows path, refused at rung 17). No `generated` change. No corpus topology change at all unless a premise forces one — then STOP and report before doing it.
- Never edit `subjectSha`, a digest, or any `migrationReview` field by hand.
- The register rename is ALL-OR-NOTHING and measured; a half-rename is the worst outcome.
- No `git add -A`; no stash; no `--no-verify` (no hook runs anyway; say "lint by hand" and do it: `npx eslint <files you touched>`).
- Every car: `Seat: Opus 5 — Fable-unvalidated` + `Lane: OSR-SCHEMA18` + `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; the chair's collection adds the retrovalidation row.

## REPORT — `$SC/receipt-osr-schema18.md`
The outcome table first (premise measured → cure or refusal → sha), then per step the exact commands with exits, the register diff by named row, the two-walker proof lines verbatim, the capsule shell-out exit, the cost, the dock tip and porcelain at the foot. CONFIRMED / PLAUSIBLE on every claim. If anything in this brief is wrong at the sha, say which line and what you measured — three lanes corrected the chair on this surface before you, each rightly.
