# Brief — INSTR-912 car 11 (and 11b if the census moves): the two register debts the merged tree exposed, plus the in-prose plant numbers

Seat: Opus 5 — implementer. The chair (Fable) rules; you build and measure. Receipt: append `## CAR 11` to `$SC/receipt-instr-912.md`. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`.

## Where
- Dock: `$SC/laneINSTR2` at **5af1a0566** (ten commits over the product tip f3ab08f51; porcelain 0; node_modules symlinked — no build). Never touch `laneINSTR`, `laneLMAT`, the skeptic docks or the main tree.
- Read first: `$SC/receipt-instr-912-rebase.md` (the two reds with their executed tails), `$SC/s12-sitting/SITTING-RULINGS-912.md` §L.2 item 62 and §M.2 (the institution-table closing rule: a column is `closed: true` only when EVERY source the spec names for it is read by the code — and, by the same law, a source that is NOT PRODUCED by any generator cannot be "read"), `$SC/prose-research/sweep/CLERK-LAWS.md` §1.2 and its two NOTES, the OSR docblock in `scripts/check-observed-shape-readers.mjs` (what a drift line is) and the prose-numerics register (`$SC/prose-numerics-rekey.mjs` and the baseline it reads — find how `floatInterpolation` and `twoDecimalScore` are counted and why they are shrink-only).

## The two reds (both measured at the source tip too — the consist's own debt, not the merge's)
1. **OSR, 2 of 44 drift lines:** `src/domain/institutions/institutionTable.js:415` reads `settlement?.treasury?.coinFlows?.taxed` — a key no generator writes (car 9's cure 6 declared it as `whatItCounts`'s third source). Decide by MEASUREMENT: grep every generator and pulse writer for `coinFlows`; if nothing writes it, the read is a phantom and the honest cure is to REMOVE it and let `whatItCounts` derive `closed: false` (two of three sources read) — OR, if the spec's third source has a produced twin (the fired income row at `economicState.js:215–233` was named in the fold; read it if it is written and it is the source §1.2 means), read THAT and keep the column closed. Either way: `node scripts/check-observed-shape-readers.mjs` must exit 0 with **0 drift lines** (never `--write`), the institution table walker green with its plants #80/#81/#84 still valid (re-run #84's plant if the third-source read changed; if the plant's anchor no longer exists, replace the plant with one that proves the new state — same label, or a new label with the manifest `meta` entry updated by text), and the receipt's correction row 27 re-stated on the measured ground.
2. **prose-numerics, 3 of 29 (total 227 > 225; floatInterpolation 149 > 148; twoDecimalScore 12 > 11):** `src/domain/prose/wiringCensus.js:1100` `(variants / total).toFixed(2)` leaks two rows. Cure: the census returns the raw integers (`variants`, `total`) and any formatted mean is produced OUTSIDE `src/` (in the walker test's print or a `$SC/instr-912` probe) — no `toFixed`, no float interpolation, no two-decimal literal in `src/domain/prose/`. `node $SC/prose-numerics-rekey.mjs $SC/laneINSTR2` (dry) must read `baseline=225 live=225 … exact=225 rekeyed=0 relocated=0 FELL=0 NEW=0`; never `--write`.
3. **The in-prose plant numbers** (the rebase's disclosed residue): the manifest `what` fields and sweep comment BODIES that still say `sweep #80`–`#84`, `plant #79`, `#77` beneath headers now numbered `# 75.`–`# 85.` — renumber the PROSE references to match the headers, by text; labels byte-untouched; `sh -n` green; the manifest test green (join one-to-one).

## Measurement discipline and fences
Runner count 0 before every vitest run (`V=vit; V2=est; pgrep -fl "$V$V2" | grep -v gate-mutex | wc -l`); focused files one at a time (the institution table walker, the wiring census walker, the manifest test, the OSR walker test `tests/lint/observedShapeReaders*.test.js` if one exists, the prose-numerics walker test if one exists); never the whole suite; never `--write`/`--update` on any register but the lighting door (11b, by the ritual, only if a title count moved — record the door's own figures). Change only: `src/domain/institutions/institutionTable.js`, `src/domain/prose/wiringCensus.js`, the two walker tests, `scripts/mutation-sweep.sh` (comment bodies; a plant only as item 1 requires), `scripts/mutation-coverage-manifest.json` (by text), `$SC/instr-912/*`, the receipt. No product surface, composer, pool text, persisted shape, seed input. One car (`INSTR-912 car 11: the two register debts the merged tree exposed — <clause>`) + 11b if needed; explicit staging; porcelain 0; the three trailers exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
Return: the car sha(s); the OSR dry line (0 drift) and the prose-numerics dry line (225 exact) verbatim; the institution-table decision with its measurement (phantom removed and the column opened, or a produced twin read and the column kept closed); the plants' state (#80/#81/#84); the walker tallies; the lighting tuple before/after if 11b; anything owed.

## CHAIR RULING ON ITEM 1 (appended 02:1x, before dispatch)
`coinFlows` is named in `src/domain/settlement.schema.js` and in `src/domain/worldPulse/treasury.js` — the PULSE may write it on an advanced world even though no GENERATOR writes it at birth. Measure both writers. (a) If `treasury.js` (or any pulse writer) writes `treasury.coinFlows.taxed`, the read is LEGITIMATE — a reader of a shape the world can carry — and the cure is the OSR register's own door: `sh scripts/gate-mutex.sh --run -- node scripts/check-observed-shape-readers.mjs --write` on a CLEAN tree, committing the refrozen inventory as a separate register commit `INSTR-912 car 11b: the observed-shape-reader inventory refrozen at the composed tip — <the door's own figures>` (the chair rules the door open for this one drift; record the two new inventory rows and that the plain walker is green after); keep `whatItCounts` closed only if the source is READ on the produced branch (the pulse's) and the `absent` branch is honest at birth — car 10's plant #84 must still red. (b) If nothing writes it anywhere, the read is a phantom: remove it, open the column, re-state row 27. Either way, print the grep that decided it.
