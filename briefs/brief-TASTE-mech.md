# Brief — the TASTE, car M (mechanics): the seven pools' typed lines, candidate functions, licence cards, fixtures and the measurement harness — so the writers meet a real gate (chair, 2026-09-08 21:1x; ARCH §12 row 6; SITTING §Q.4, §R c-22; the owner's two-phase rule and two-arm rule)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-taste.md` § CAR M. Dock: `$SC/laneTASTE` at the §916 product tip `8961388ce` (never landed; the sitting reads it). Read first, whole: `$SC/briefs/brief-SEAM-car6-taste.md` (the taste's charter and the two arms), ARCH §6.3–§6.5 (the three worked blocks — the seven pools below are theirs), §8.3 (the LICENCE CARD), §2.5 (the annex grammar's typed lines), §4 (the algorithm), §12 row 6; `$SC/receipt-seam.md` § CAR 5 (the arms; the sampled walk's baseline) and § CAR 5b/5c (the holder census, the capture standing, the 270); `scripts/generate-dossier-state-prose.mjs` and its contract test; `src/domain/display/stateProse/composeStateProse.js`, the six `*StateProseCandidates.js` leaves (empty `fired` lists), `src/domain/prose/composedWalker.js`, `scripts/prose-manifest-cells.mjs`, `scripts/prose-manifest-diff.mjs`, `scripts/prose-rate-corpus.mjs`, `tests/helpers/goldenMasterCorpus.js`, the census JSON.

## The seven pools (ARCH §6.3–§6.5; every one a MODIFIER on an existing block; zero re-key)
| block | pool | reads (branch grain) | predicate | relation · seat · move | attach |
|---|---|---|---|---|---|
| DS-DEF-11 | `country: pressed (walled)` | `config.monsterThreat` | `measuredMonsterFamily ∈ {plagued, frontier}` | addition · sentence · PRESENT | STRAINED (+ THREATENED/QUIET only under a NARROWS ruling — REFUSE, print) |
| DS-DEF-11 | `country: pressed (unwalled)` | the same | the same | addition · sentence · PRESENT | UNWALLED-SMALL, UNWALLED-LARGE |
| DS-DEF-11 | `watch: bought (revealed)` | `compromisedSecurityInstitutions().revealed` | `.length > 0` | addition · sentence · INSTITUTION | the three WALLED-* |
| DS-DEF-11 | `watch: bought (covert)` | `…().covert` | `.length > 0` | as above; `dm-only` on EVERY variant (T-F5) | the three WALLED-* |
| DS-DEF-2 | `stores: short` | `economicState.foodSecurity.label` | `∈ {Deficit, Deficit × Active Famine}` | addition · sentence · PRESENT | the `granary AND hospital` cell only (T-F12) |
| DS-DEF-2 | `stores: import-fed` | the same | `= Import-Dependent` | addition · sentence · PRESENT | the same cell |
| DS-GEN-3 | `purse: short` | `economicGates.military` | `present AND < 1` | addition (the (c) row runs modifier→spine ⇒ sentence seat) · PRESENT | the military rungs `scores.military: WEAK / CRITICAL` |

## What to build (one commit each, in this order)
1. **`scripts/prose-licence-card.mjs <block> <pool>`** — prints the LICENCE CARD of ARCH §8.3 FROM THE CENSUS ROW (reads · predicate · bag · relation · seat/form · move · attach with its derivation · echo · covert · may claim · may NOT · audience · the REFUSED COLUMNS), for any existing pool and for the seven new pools once their typed lines exist; `--all-taste` prints the seven. Arm: a plant that edits the census row's `reads` moves the card's line by name; the card for a shipped pool (DS-DEF-11 WALLED-STRAINED) matches the census row field for field.
2. **The typed lines in the annex** (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`) for the seven pools — `ROLE modifier`, `READS`, `RELATION`, `FORM sentence`, `MOVE`, `ATTACH` (derived, then the narrowing where the table says so), the covert pool's `dm-only` mark on every variant — each with ONE `[plain]` line carrying the text `⟦TO-AUTHOR⟧ <pool key>` and NO faces (the writers replace it; the projector must accept a variant whose text is the marker ONLY under a `--taste` flag that the shipped build never sets — assert both: with the flag the leaf projects, without it the projector REFUSES the marker by name). The census re-taken (a pre-ruled row move: seven new rows, `role: modifier`, their `reads` on the branch grain; nothing else moves — print the bounded diff). `readsCount` and `seat` projected for them (`seat: sentence`, `seatReason: 'no-row'` — the join is 0). The SHIFT REGISTER's `variant-count-per-pool` and `face-count-per-variant` pins move ONLY for the seven new pools (a declared ADDITIVE row; every existing pin unmoved — assert).
3. **The candidate functions** in `defenseStateProseCandidates.js` and `generalStateProseCandidates.js`: the seven pools become candidates when their predicate holds on the desk's readings (the shipped desk-read recipe with every caller reading — the `{}` hazard; the RATE corpus's callers are the model). Arm: on the RATE corpus (768) print, per pool, the towns on which it is a candidate and the cells it attaches to; a plant that inverts a predicate moves the count.
4. **The norm bits** for the seven from the RATE print at the 10 % line (`rateBp`, departure), written into the census row and the norms leaf by the existing path; print them.
5. **The `corruption.js` closure into the defense desk** measured against the 293,079 B precedent (§5.3; the module graph the candidates leaf pulls when it reads `compromisedSecurityInstitutions`) — REFUSE the read (print) if it exceeds the precedent; then the covert-source refusal asserted (the covert pool never a candidate on the player face — the paired-town arm: the same seed with the corruption impairment toggled, the two PLAYER pages byte-equal on every mount, on the corruption fixtures).
6. **The interested-fact fixture** (SITTING §R c-22): the RATE town `town|road|random_threat|germanic|hills` seed `rate-9-2` (`criminalCaptureState: corrupted`, holder `Town hall`) and the clean control `rate-3-0` — persisted as fixtures the writers' harness composes; the treasury-held DS-GEN-11 row's citing variant is the model for the DM face. The holder census's `standing` for these two towns printed (INTERESTED on the four organs; none on the clean town).
7. **`scripts/taste-measure.mjs`** — THE HARNESS the writing workflow calls after every round: (a) `generate-dossier-state-prose.mjs --taste --check` then the projection; (b) the composed walker over the seven pools' variants (exhaustive) and the sampled walk at N (N set from the per-entry cost with a stated confidence on the arms' fire rates — X-F8 — printed with its sha); (c) the manifest over DRIFT and the classifier against the §916 base cells (every affected cell ADDITIVE; a REPLACED or RE-INDEXED cell is a STOP); (d) the VARIETY corpus's duplicate-unit rate before and after (the base 9,771 bp at 4,200 towns); (e) per variant: the fragment/sentence length in words against the band, the four faces' sibling distance (A5's figures) and their band position (distance from the exemplar median per band from `RULES-V2-PART-B.md` §18's grains); (f) the tie rate under the comparator; (g) the PROVENANCE move count and A13's verdicts; (h) the per-set round counters read from the packet files under `$SC/taste/<pool>/` (`draft-round-N.md`, `refine-A.md`, `refine-B.md`) — the harness only READS them; (i) prints ONE TABLE per pool per arm, machine-readable JSON beside it (`$SC/taste/measure-<arm>.json`). Arm: the harness on the ⟦TO-AUTHOR⟧ state reports every pool WITHHELD with the marker named; a planted lawful face on one pool moves exactly that pool's row.
8. **The two docks:** commit the mechanics state in `laneTASTE` (the draft phase's base); the chair cuts `laneTASTEB` from that commit for arm B. Print the commit the chair must cut from.

## Discipline, fences, commits
As SEAM's: the runner count in its own shell before every vitest (the LIGHT lane runs beside you in `laneLIGHT` — the count is the law for both; one focused file at a time; never the whole suite; never a build); explicit staging; no stash/amend/rebase; register doors: the lighting census by ritual, the wiring census pre-ruled for the seven rows, the OSR shrink-only if a leaf regenerates (print 1972 before/after); no em dash/bang/toFixed in `src/` literals; strict on its ceiling; typecheck; the byte ratchets' dry reads (the seven pools add bytes: declare each leaf's delta; none raised). NEVER LANDED: nothing from this dock reaches the product branch; the taste is read by the sitting. Commits `TASTE car M-1: the licence card printed from the census` … `M-8: the draft base`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: TASTE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the shas; the seven licence cards verbatim; the candidate counts per pool on 768; the norm bits; the closure figure; the paired-town arm; the two fixtures' standings; the harness's first table on the ⟦TO-AUTHOR⟧ state; N and its confidence; the commit for `laneTASTEB`; the whole `tests/lint`, strict, typecheck at your tip; anything refused with its measurement. Porcelain 0 and runners 0 when you return.

## ADDENDUM (chair, 2026-09-08 21:2x) — the harness interface the writing workflow calls (`$SC/taste/taste-write.workflow.js`, written; launched after this car)
- `node scripts/taste-measure.mjs --arm draft --round <N>` writes `$SC/taste/measure-draft.json`; `--arm A` writes `$SC/taste/measure-A.json`; `--arm B` writes `$SC/taste/measure-B.json` (the gate copies `measure-draft.json` to `measure-draft-prev.json` between rounds and to `measure-draft-base.json` at the draft commit — the harness need not).
- The JSON carries, per pool: `inBand` (boolean), `failing[]` of `{measure, value, band}` (strings), `refusals[]`, and per face the word count, the position inside each band (signed distance from each edge — information only; the owner's rule: the band is a licence, not a target) and the directional measures (sibling distance, licensed-detail count, tic count), the sibling-distance figures, the provenance count and A13's verdict; plus a `fixture` section with the interested fact composed both ways (the player face and the dm-only face as strings) on rate-9-2 and the clean control on rate-3-0.
- The packet layout: `$SC/taste/<pool dir>/draft-round-<N>.md`, `refine-A.md`, `refine-B.md` — the dirs are `def11-country-walled`, `def11-country-unwalled`, `def11-watch-revealed`, `def11-watch-covert`, `def2-stores-short`, `def2-stores-importfed`, `gen3-purse-short` (create them empty in M-8 with a `README` naming the pool key).
- If your interface must differ, say exactly how in the receipt and the chair aligns the workflow's constants before launch.

## ADDENDUM 2 (chair, 2026-09-08 22:3x) — two owner rulings of the evening the harness must serve
- **THE DENSITY LAW (Part B §21.4):** the harness's read-aloud measure reports whether a line LANDS (the weight on the last word; the hinge; the breath after a long sentence), never whether it is plain; no "readability" score of any kind is printed.
- **PASSAGE SHAPES (agenda C⁗):** `scripts/taste-measure.mjs --shapes` composes each of the seven pools' units under the candidate shapes — spine-then-sentence (shipped), sentence-then-spine (only where the added face carries a noun INTO the spine: the thread arm decides), and the clause seat where a `consequence.clause` joint exists (none today: print WITHHELD) — and prints per shape the duplicate-unit rate on VARIETY, the thread verdicts, the band figures; a REPORT for the sitting, no shipped draw, no key minted. If the composer cannot be driven under an alternate shape without a code change, REFUSE with the measurement and describe the change (the sitting decides).

## ADDENDUM 3 (chair, 2026-09-08 22:4x) — the `--shapes` report is a DISTRIBUTION TABLE, and shapes are licensed, never rescued
`scripts/taste-measure.mjs --shapes` derives, per unit, the LAWFUL SHAPE SET from the composition (spine → sentence always; sentence → spine only where the added face carries a noun into the spine AND the relation is `addition` read as no cause — the thread arm and A2 decide; the clause only where a `consequence.clause` joint exists — none today: WITHHELD), then a seeded selection among the lawful shapes (no hash when one) — NEVER a seeded shape followed by a fallback; a shape refused by the composition is printed as refused. The report prints, over the seven pools on VARIETY: the duplicate-unit rate per shape policy (fixed vs licensed-draw) · the passage-shape distribution MARGINAL and CONDITIONAL on the lawful set · the face distribution (the uniformity arm per pool; the corpus-level counts) · the connective distribution · the construction distribution via `composedOrderIdOf` · the thread verdicts · the band figures · the read-aloud notes · the claim/wiring census (A0b) after the variation. No key is minted; nothing ships; the sitting rules.
