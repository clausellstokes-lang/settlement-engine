# LANE: L-DEFAULT (`LGT-C2-DEFAULT`) — the preset table lights, six hunks in one commit series (lighting POSITION 3)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · THE CENTRE OF MASS of the wave · the largest same-seed shift in the arc, DECLARED per cause · boards after L-HOMES lands⟧

READ FIRST: `$SC/briefs/_PREAMBLE.md` → `git show refs/preserve/light-plan-2026-09-05:PLAN.md` §6 POSITION 3 (the order
below, verbatim source) + §1.3 (the door denominator: 152 doors, 38 lit, 114 dark; classes A–G) + §5 (the OSR rung: the
25-key re-key is half (a) of `LGT-P15-EP1`; half (b), the EP-1 release, does NOT ride this car) + §8 (STOPs) → the §890
O-12 block in `simulationRules.js` (why lighting on a LEGACY preset id re-labels every installed campaign silently) →
`tests/simulation/simulationRulesPreset.stability.test.js` (the rosters) → `engineGatedRuleKeys.walker.test.js`.
The owner's word (09-02): **light everything, including the DEFAULT preset, as declared shifts; values are signed LAST.**

## THE ORDER WITHIN THE SERIES — fixed; re-verify each against the tree before its hunk
1. Class C + D into the lit default (`...WAVES`, `...ONE_REGEN`, `disasters`, `commodityFlow`, `allyIntelSharing`), with
   the stale `:527–529` comment repaired LINE-COUNT-NEUTRAL in the same hunk (prose-numerics rows are line-addressed).
2. O-1 in the BIRTH form — `NEW_CAMPAIGN_SIMULATION_PRESET_ID`, a lit SUCCESSOR id AFTER the legacy trio in key order;
   `:6`, `:38`, `:59`, `:136–145`, `:632` UNTOUCHED (re-derive the addresses; they move).
3. Class B into the lit SUCCESSORS — ⛔ NEVER onto a legacy id: `rulesMatchPreset` would miss, `presetIdForRules` fall
   through to `'custom'`, and every installed Dramatic Campaign would re-label itself at its next `ensureWorldState` with
   no receipt minted (THE PROMISE breach the O-12 block records).
4. O-2 `infoMode: 'perfect_delayed'` on the lit default and dramatic's successor — ⚠ every belief-conjoined record
   carries a PREMIUM sentence (`campaignWorldPulseSlice.js:391` refuses canonize to a non-premium tier): say what a
   free-tier world sees.
5. Class E `false → true` — ⛔ EXCEPT `demographicsEnabled`, which STOPs on HORIZON-DARK B6 (`{signed:false, lit:null}`):
   it stays dark and is the NAMED residue.
6. Classes F and G in dependency order across every lit preset — F needs L-HOMES CAR 2 (the manifest split); espionage
   needs CAR 4 + CAR 5; `neutralNeighborsEnabled` needs `LGT-P11` proven on `tickScanBudget`'s own fixture FIRST (if P11
   has not landed, `neutralNeighbors` stays dark and is named).
7. `LGT-P15-EP1` half (a) ONLY — the 25-key corpus re-key through the governed migration
   (`migrate-observed-shape-readers.mjs`) at the NEXT FREE RUNG after 17, at the END, after the chair's L-PROBE presence
   dump has been attributed key-by-key (a shape crossing `MIN_ROWS = 40` detonates the OSR ratchet; 188 growth rows from
   one key at §875). ⛔ Not the `advanceEpochEnabled` release.

## TESTS — extend, never add
`simulationRulesPreset.stability.test.js` rosters, `moverCompositionSmoke`, `emergentArcSoak`, `narrativeParity`,
`discourseParity`, `soakScriptSeams`, `engineGatedRuleKeys.walker`. ⛔ `narrativeParity`, `discourseParity`,
`emergentArcSoak`, `moverCompositionSmoke` run LIVE on `full_simulation`: a red there is a FINDING — reported, never
re-recorded. No new test file.

## THE SHIFT — measured, per preset, per cause
Every lit preset's 52-tick hash (CAR 9's witness) MUST move; an UNMOVED preset hash is a FINDING. An unmoved KEY is read
from the certification grade table only, NEVER inferred from a hash. Predicted: every pulse HASH golden moves 0 rows
(they drive literal rules objects); `generatorGoldenMaster` moves 0/525 on both arms — ⛔ a move on either is a STOP.
Record the evidence for the seven DECLARED-SHIFT sentences this car owes: (i) the world-alive stack · (ii) war/faith/
seasons · (iii) the belief stack wakes · (iv) the ceiling's eleven · (v) the 29→30 manifest keys + the split ·
(vi) the backlog seventeen · (xi) the shipped default mints epochs. The chair writes the declarations FROM your outputs.

## ⛔ FENCES
No flag names in comments in `simulationRules.js` beyond the keys you light (the lit-coverage walker regexes raw source).
No register acts. No re-record of a live-parity red. No `--amend`; one commit per hunk, in order; trailers
`Seat: Opus 5 — Fable-unvalidated` + `Lane: L-DEFAULT`. Quiet-window law + mutex. Receipt `$SC/receipt-l-default.md`.
