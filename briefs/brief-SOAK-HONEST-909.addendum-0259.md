# ADDENDUM — RE-DISPATCH of SOAK-HONEST-909 (chair session b43943b4, Fable 5.1, 2026-09-07 02:59) after the 02:53 death of the dispatching session 405b5e7e
1. **Arrival.** Dock `$SC/laneB6` HEAD `6ebe0ef3b` == product tip; porcelain 2 = your PREDECESSOR's uncommitted car-1 work: `scripts/audit/whole-world-soak.mjs` (+31: `yearlyPopulations` + `yearlyDiedFlags` shipped from run A beside `finalDiedFlags`, with the additive-envelope comment) and `tests/soak-harness/tripwireRegistry.test.js` (+69/−20: the shrink-only baseline banked 2 → 0 and 4 → 0, the legacy-writer CONTROL that reconstructs the pre-§909 writer by deleting the two shipped lines and proves the walker still finds the old 2 rows / 4 pairs, the non-vacuity check on the control, the body-only list reduced to `non_finite_ledger_figure`). Read `$SC/receipt-soak-honest-909.md`: its ORIENTATION table stands (re-derived at 02:50:57); its CARS section is STALE (car 1 was in flight). ⛔ Do NOT discard or rewrite the diff from scratch. First act: read both hunks whole in their enclosing scopes, re-prove them (`npx vitest run tests/soak-harness/tripwireRegistry.test.js` with the exit captured in-shell; eslint on both files), then COMMIT them as car 1a (explicit paths, lane trailers). Then continue car 1.
2. **CHAIR RULING on the schema bump (JUDGMENT, vetoable by the owner — record it in your RETROVALIDATION ROW as a chair ruling, not a lane refusal).** The brief's "schemaVersion 5 → 6" is WITHDRAWN. Your predecessor measured it and the chair ratifies the measurement: `SOAK_RECEIPT_SCHEMA_VERSION` has one home, `src/domain/certification/behavioralContract.js:117` (the certification contract, outside this lane's charter), with `SUPPORTED_SOAK_RECEIPT_SCHEMA_VERSIONS = [4, 5]` at :125; three stamps share it (`whole-world-soak.mjs` :519 checkpoint identity, :644, :1017 the receipt; `behavioral-observation.mjs` :1202); five test fixtures pin 5; and the constant's own header documents the ADDITIVE-v5 precedent (WR-9's `warConvergence`: "no existing field changes meaning"). The series ships ADDITIVE on v5. A pre-series receipt is told apart POSITIVELY by the rows' `requires` channel (NOT-EXECUTABLE on its face, §206.2b's third status) — the reading the freeze door (`register.mjs:347-354`) acts on; a version integer is not. The one-line schema note the brief asks for STILL goes into the writer's header — naming §907 M1-F1 as the cause and the additive-v5 precedent as the form; no version integer moves, no fixture moves, no `SUPPORTED_…` edit. Charter the permission, measure the mechanism: the permission (ship the series) stands; the mechanism (the bump) was measured as unnecessary and costlier.
3. **Car 1's remaining acceptance (from the brief, unchanged):** the walker reads 0 unreachable rows (banked in the diff — re-prove); a FRESH 30-year receipt (`--years 30 --settlements 4`, ~80 s; through `sh scripts/gate-mutex.sh --run -- …` if that is how the estate runs soaks — read `scripts/gate-mutex.sh` first; quiet-window law: load-1 < 4.0 and zero vitest workers before launch; output under `$SC/soak909/`) evaluated by `scripts/soak/evaluate-receipt.mjs`: `fullInstrument: true`, `notExecutable: []`, `capacity_plateau` / `capacity_floor_thaw` EXECUTED — report their verdicts, and say which guard the plateau row declares on a 30-year run (its own horizon guard, not NOT-EXECUTABLE); the B6 300-year receipt at `$SC/capacity/artifacts/research-lit-4s.cases/research-lit-4s-300y-4s-seed1.json` must STILL grade the two rows NOT-EXECUTABLE with `fullInstrument` false — prove it with the command and its output. The register cells: NO edit (your predecessor measured it: `receiptSchemaVersion` is computed at mint, `register.mjs:195`; both genesis cells scaffold 0).
4. **Cars 2 and 3 exactly as briefed** (the dotted-path `requires` + walker extension; the constant lift into `soakInvariants.mjs` + `capacity_envelope_30y` armed, roster 11 → 12 / 9 → 10, pins updated with MEASURED figures, its verdict measured on the B6 receipt and the fresh 30-year one). Every car: the focused test files one at a time (`tripwireRegistry`, `soakRegister`, `evaluate`/`seams`/`curveBand` suites, `demographicsEnvelope`), exits captured before any pipe; eslint on touched files; `npm run typecheck:ratchet` once at the composed tip (ceiling 173). ⛔ No vitest while `$SC/HOLD-VITEST` exists (absent at dispatch — check before every run). Receipt re-written after EVERY car (STATUS; CARS with shas; the RETROVALIDATION ROW; CONFIRMED/PLAUSIBLE per figure); every timestamp from `date`. Commits: explicit paths, never `git add -A`/`-u`/`.`; never stash; no push; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: SOAK-HONEST-909`.
5. **Report.** Your final message = the receipt's STATUS line, the car shas in order, the measured figures (the 30-year verdicts, the archived-receipt grades, the roster counts, the walker figures, the typecheck count), every refusal with its measurement, and the porcelain count at the end (must be 0).
