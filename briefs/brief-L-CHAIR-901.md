# BRIEF — L-CHAIR-901 (Opus lane; Seat: Opus 5 — Fable-unvalidated; Lane: L-CHAIR-901)
Chair: Fable 5.1, 2026-09-05. Dock: `$SC/laneLIGHTTRIAL` — the §901 composition, ALREADY COMPOSED by the chair 09-05 17:15: 18 cars over edef0b219 (the §900 tip; sealed `light-trial-2026-09-05`). The only replay conflict class (the four W-OPS cars vs SEAT-78's `irregularForceEnabled` on the flag manifest) was resolved by set-merge with the manifest re-measured 30 → **35** keys; LH8's densityCreateBoundary.js applied clean beside HORIZON-B2's EXECUTOR class (verify B2's row against LH8's arm 5 anyway). ⛔ Dispatch only after the §900 CAS has landed on edef0b219 (else the dock is re-based first). Lands as the chair cars of §901, BEFORE the registers.

## The eight items (each a measured finding from a lighting lane; the PERMISSION is chartered, the MECHANISM is yours to test — refuse with measurement where the tree disagrees)
1. **CAR 3's fences vs 35 keys** (L-HOMES-3 derived its no-fence set against 30 register keys; SEAT-78 added `irregularForceEnabled` at §900 and CAR 4 the four W-OPS keys — the composed manifest is 35, measured by `chair-tools/resolve-lighting-trial.py`): re-derive the no-fence set at the composition and prove the five new keys carry their source-level fence arms (or say which lack one and why the family gate is the fence).
2. **`missionDispatcher.js` header "NO FLAG IS MINTED HERE"** is false since CAR 4 (the door lives in the family gate, not the leaf — say exactly where the flag is minted now); correct the prose WITHOUT re-opening `espionageDormancyFence`'s reachability arm — run that arm before and after.
3. **`engineGatedRuleKeys.walker.test.js`'s "zero live instances of the computed-access class"** is STALE: `characterDrift.js:273` is one (L-HOMES-7). Re-state the claim with the measured instance; if the census must now count it, say what moves (a lighting title? the register?) and DON'T take the register act.
4. **`scripts/review/readerCorpus.mjs:113`** ("no gate anywhere reads it") is FALSE at the base (L-HOMES-7) — this is the OWNER'S WALK corpus: correct the sentence to the measured truth, cite the reader.
5. **`tests/domain/characterDrift.test.js:791`** over-broad `not.toContain` over the whole file — NARROW to the entry's own claim; never soften; anchor the negative (`// anchored:` on the line immediately above).
6. **`subsystemRowsCompact.js:420`** cites `tests/domain/institutionLifecycle.test.js` as the underways dark-vs-lit pin — zero hits (L-HOMES-3); re-cite the real pin (`underwaysOrganicFoundingDormancyFence.test.js`) and run the certification citation walker that convicts a dead citation.
7. **`densityCreateBoundary.walker.test.js`'s stripper: the NEWLINE half** (STRIPPER-UNIFY left it to L-HOMES-8; L-HOMES-8 cured the ORDER only): add `\n` to the quote classes (four characters), plant the two mutants (apostrophe-in-template; multi-line apostrophe), prove zero verdict flips over src/ (reuse `$SC/lh8/stripdiag.mjs` / `$SC/laneSTRIP`'s plant scripts).
8. **The golden roster is closed BY NAME** (L-HOMES-9: `goldenFreeze.walker.test.js` arm 2 matches `/golden/i` on the path): add the CONTENT-keyed arm — every register row's `path` must exist and be claimed, whatever its name, and every enrolled/excluded env spelling must map to a file — without changing the name arm; plant a mis-named fixture with a register row and prove the new arm reds where the old one is silent.
Also, from L-HOMES-9: write into the witness's header and `lprobe/pulse-hashes.mjs`'s header the chair's ruling that the two instruments are NOT comparable by design (preset-in-isolation vs corpus-shaped world), so nobody unifies them later.

## Laws
No `it.each` (each-family park ceiling 111/112). No new `*.test.js` unless an item cannot land otherwise (say so; a new file reds the lighting census and moves two registers). Every negative anchored. No register act (record deltas: lighting titles per new `it`; keep it small). vitest via `sh scripts/gate-mutex.sh --run -- npx vitest run <files>`; `tests/lint/` WHOLE at the end (139/139 expected except lighting figures your own titles move). No hook runs in this repo — `npx eslint` every file you commit and print the exit. Commit explicit files; message trailer `Seat: Opus 5 — Fable-unvalidated` / `Lane: L-CHAIR-901`, blank line, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-l-chair-901.md`: outcome table with shas; per item the measured premise (true/false at the composition), the cure or the refusal; register deltas; RETROVALIDATION ROW last; dock tip last; porcelain 0.
