# LANE: WARMS-CAR0 — W-ARMS Car 0 (the five probes), the re-measure (B3), and the skeptic pass (B2) — READ-ONLY
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · zero product bytes · zero repo writes · zero refs · NO vitest · NO `npm run` · NO build⟧

## WHY THIS LANE EXISTS
W-ARMS (house heraldry on the dossier and the PDF cover) is 14 items, **0 done**, and `$SC/SCOPE-MEASURE.md` §6.2 found
that **it is gated by nothing in the tree** — its "after the GOLDEN freeze" slot is a chair cleanliness ruling, not a
dependency. Three of its items are landable at zero critical-path cost and zero product bytes: **A1 Car 0** (the five
probes), **B3** (re-measure the design's point-in-time figures at the boarding base), **B2** (the independent skeptic
pass that §879.11 billed and §880.5 never ran). This lane does all three while a gate runs.

## READ FIRST
1. `$SC/briefs/_PREAMBLE.md` — the hard rules.
2. `$SC/SCOPE-MEASURE.md` §2 (W-ARMS: the 14 enumerated, the decayed figures table, "zero heraldry code at the tip")
   and §4 D-8…D-11 (the four W-ARMS blockers already REFUTED: H1 "Car 0 blocked by the freeze", H5, B1, UB-5).
3. **`DESIGN_W_ARMS.md` — WHOLE FILE (~55 KB). ⚠ It is NOT in your tree: B1 ("commit the volume") has NOT landed on the product branch — the handoff's "volume landed" is a claim this measurement refutes.** Read it with `git show "refs/preserve/train-warms-2026-09-05:docs/DESIGN_W_ARMS.md"` from /Users/cstokes/Desktop/settlement-engine (braces, zsh). Report B1 as OPEN and say which consist should carry it (a docs-only car; ⚠ a new docs file meets the `tests/docs/` scanner family — name the scanners).** It is the design: §3.1/§6/§8.2 rest on a first-paint
   closure figure that is at least six landings stale (D-11) — every byte figure in it is suspect until re-measured.
4. `git show refs/preserve/session-kit-893b-2026-09-04:warmsscope/items.md` (the 14) and
   `…:warmsscope/warms-plan.md` (the plan) — verify the ref with `git rev-parse --verify` first.
5. The charter rows on the LEDGER branch — ALWAYS `git show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md`
   (the working tree is dirty with mass docs deletions): §879.11 (the bill: "a short Fable design note + skeptic
   pass"), §880.5 (the note landed, the pass did not), the §9 taste rows (owner's, 11 of them — READ, do not rule).

## YOUR TREE
`$SC/laneANCH2`, detached at **`272dbd2da`** (clamp consist + ANCHORS over `90702c3e9`) — the freshest composed tree.
**READ-ONLY**; porcelain MUST be 0 when you finish; never `npm install`, never touch `node_modules`. Single-process
`node` probes are fine; anything that needs a BUILD (`npm run build`, the size/hash freeze, first-paint closure) or
vitest is **NOT RUN** — recorded as OWED with the exact command and the reason.

## THE ACT
**A1 — Car 0, the five probes.** The design names them (read it; do not take the count from this brief — if it names
four or six, say so). Run every probe that is read-only (git/grep/node). For each: the question it answers, the
command, the result, CONFIRMED. For any that needs a build: the command, OWED, and what the design would do with each
possible answer.
**B3 — re-measure at `272dbd2da`.** Every point-in-time figure the design quotes — sample counts
(`docs/samples/organic-craft/ornament/`, base 16), the golden snapshot line count (17), `public/map/charges/` (104),
the `src/design/organic/ornament/` file list (5), the D1 lighting-census figures (SCOPE-MEASURE measured
2521/371/2150/23178/6213 at `90702c3e9`; `node $SC/chair-tools/lighting-probe.mjs $SC/laneANCH2` gives the current),
the OSR schema (16 at the tip; ⚠ rung 17 is CLAIMED by the OSR-SCHEMA17 lane — say so wherever the design assumes a
rung), the seven voice ceilings (34/0/15/382/69/9/770 — ceilings CONFIRMED, "measured == ceiling" is PLAUSIBLE only:
do not run voiceMechanics). Mark each HOLDS / DECAYED with both values.
**B2 — the skeptic pass.** Attack the design the way a build lane would be bitten by it. For every claim a build car
would rely on — the byte budget it fits inside, the seam it hooks (`DossierHeaderRow`, `Cover.jsx`), the test file it
adds (⚠ a NEW test file reds THREE censuses: test-ratchet totalFiles, lighting census, known-failure file list), the
paid-surface exposure of Car 3 (the PDF is a PAID surface — its behaviour change is owner-visible, say what changes),
Car 1's sample count N (~24 vs base 16 — derive N from the design's own sample rule and the existing 16), the CC0
charge roster (A5, OUT by owner taste row §9.8) — write RATIFY / REFUTE / THIN with the evidence. Then the two questions
a skeptic pass exists for: **what would make this landing fail its gate**, and **what does it change for a user who
never asked for heraldry** (the default arms every house gets — is that a same-seed output shift? If the dossier header
renders differently for an existing save, that is a behaviour shift and must be DECLARED, never absorbed).

## ⛔ FENCES
No writes to any git tree. No vitest / `npm run` / build. One process at a time beside the running gate. Do not rule
on the eleven §9 taste rows or the one-word veto (C1) — they are the owner's; report where a build would need them.
Do not write product code — Car 1 is a separate lane on a separate brief.

## RECEIPT
`$SC/warms/CAR0.md` (the probes + B3 table), `$SC/warms/SKEPTIC.md` (B2, claim by claim), and
`$SC/warms/receipt-warms-car0.md` with the PARTIAL header FIRST, CONFIRMED/PLAUSIBLE per figure, and a RETROVALIDATION
ROW. Final message: outcome first; the single finding that most changes Car 1's brief; what is OWED to a build.
