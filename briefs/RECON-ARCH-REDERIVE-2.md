# RECON-ARCH-REDERIVE-2 — prototype the WHOLE seam (Opus RECON lane; successor chair, session 7d3418f8, 2026-09-19)

**Seat:** Opus RECON. You EXECUTE a prototype and report measurements. You implement nothing in any tree, compile no packet, run no gate. **Why you exist:** the first re-derivation recon (kit: `findings/RECON-ARCH-REDERIVE.report.md` — read it whole) showed that re-entering the generation pipeline from a saved record fails at specific WRITERS, and the chair ruled an architecture from it — **design §22, "HELD FACTS ARE FINAL"** (read it whole: `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`, §14, §21.5, §22). A ruling is a design until it is executed. You prototype ALL of §22 at once, through the in-memory override channel the first recon built, and tell the chair whether it holds — and where it does not.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

**Tree (READ + IMPORT ONLY):** `$SP/read-tip-a41a0e109` (detached at `a41a0e109`; `node_modules` symlinked; confirm the HEAD). ⚠ The build branch has moved on (EM-B3a landed; EM-P3 is building and will move `resolveConfig.js`) — that is fine: you measure at `a41a0e109`, and say so. **WRITE ONLY** under `$SP/lane-rederive-recon-2-scratch/` (create it). Your toolkit: `$SP/chair-kit-923472dc/tools/rederive-prototype/*.mjs` (the first recon's `hook2.mjs` + `enrich-loader.mjs` — in-memory wrappers with the `__FN_OVERRIDE__` channel that puts a pin consult INSIDE a function without touching the tree — plus its `x1`…`x7` scripts and the census instruments) — copy what you need. FORBIDDEN: editing/staging/committing anywhere; `$SP/consist`, `$SP/lane-em-b3b`, other lane dirs; vitest, eslint, every npm script. Plain `node` only, ONE process at a time. Write your report file early with its skeleton and re-write it after every finished section.

## Build the whole seam in scratch (overrides only), then measure
**P1 — the WRITER CENSUS.** For every HELD key of §22 ruling 1 (institutions, npcs, factions, relationships, conflicts, the power structure's held fields, the settlement's name, the world facts in config): every function that WRITES it during `generateSettlementPipeline` — its producer step, every mutating pass, the coherence enrichment's functions, `reconcilePowerStructure`'s replay, the name mint, the repair passes — by module and symbol, with HOW you proved it writes (the first recon's write-through method; a frozen-clone control). Also every ASSERT that compares a held fact with a fresh derivation (`assertPowerEconomyFreshness`, `assertStableGeneratedRoster`, any other you find). This table is the denominator of the whole family; a writer you miss is a flicker the DM sees.
**P2 — the prototype.** Through overrides: (a) pins deep-cloned on entry; (b) every writer in P1 consults the held bag and, when its key is held, returns the held value untouched (for a step that provides BOTH held and derived keys, say exactly how you split the consult and whether the step could simply be skipped); (c) the two asserts pin-aware — under held mode the freshness fingerprint is REFRESHED from the current readings, and the stable-roster assertion takes the held roster as its subject; (d) NOTHING else changes. With NO held bag every override must be inert: prove it against the committed golden hash (and a stride of the golden corpus), exactly as the first recon did.
**P3 — criterion 1: no edit.** Over the 63-row sample, held bag = the record's own held facts: which record keys reproduce, which do not, by tier. The target is every key but `simulationTrace`. For anything else that diverges: path, step, mechanism. In particular the "final value fed to an intermediate consumer" problem the first recon hit (restoring final `institutions` early made a city throw): does it survive once the freshness assert is pin-aware — and if the readings then SETTLE to values different from the record's (because the original run derived them from a pre-repair roster), how large is that settling shift, in which keys, at which tiers? That number decides whether "opening the editor and saving nothing" changes a dossier.
**P4 — criterion 2: the eight edits of X6, again, on a town AND a city AND a village:** rename an NPC (through `applyNpcRenameToSettlement`, the cascade — not by hand), change an NPC's role, add an institution from the tier's catalogue, remove an institution, change one faction's power share, seat a different governing faction, world fact terrain → desert, world fact culture → norse. Per edit: did it throw; which record keys moved and how many fields; did any UNEDITED held fact move (flicker); did any NEW dangling name-join appear (delta against the record's pre-existing dangles, by the first recon's X5 method); did the settlement keep its name; does the power share STICK; and one plain sentence of what a DM would see.
**P5 — the trace.** Do `simulationTrace` entries carry their producing step (or enough to attribute them)? Prototype ruling 8 (a held step re-emits nothing and its recorded entries are carried; a deriving step's entries are re-derived) and measure whether the no-edit trace then reproduces, and what an edit does to it.
**P6 — `stress` and the settlement's name.** With the name HELD, does a culture change still leave any prose speaking a different name? Is `stress` better HELD or re-derived under §22 (its summary is rendered from a `summaryRoll` token the record no longer carries) — measure both and report what each shows a DM.
**P7 — cost and size.** Wall-clock per re-derivation with the whole seam on; and, for the chair's packet planning, the list of `src/` functions the real implementation would have to touch (from P1), grouped as the charter's EM-R1…R6 propose (kit briefs cite the charter; read its newest amendments section via `git … show review-fixes-2026-07-08:docs/implementation/charters/EDIT-MODE-TRAIN.md`) — say where that grouping is wrong.

## Report (final message; also `$SP/lane-rederive-recon-2-scratch/RECON-ARCH-REDERIVE-2.report.md`)
1. The inertness control. 2. P1's writer census. 3. P3's reproduction table and the settling shift. 4. P4's edit table. 5. P5, P6. 6. **Where design §22 is WRONG or incomplete** — smallest measured contradiction first; do not soften. 7. P7. 8. What you could not measure and what would settle it. Every claim CONFIRMED (quoted command + output) or PLAUSIBLE. Do not adjudicate the design.
