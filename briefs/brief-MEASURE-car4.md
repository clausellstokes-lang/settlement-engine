# Brief — MEASURE car 4: the reader-corpus derivation arm — the golden key's single source, read from where it now lives

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-measure.md` § CAR 4. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`. Dock: `$SC/laneMEASURE` at **3f68a9978** (fifteen commits over 8522a17b2; porcelain 0). `$SC/HOLD-VITEST` exists; you are EXEMPT for the focused runs below and ONE whole `tests/lint` run.

## The red (the §915 whole-suite proof, `$SC/whole-915.log:643-658`; the other three reds are the banked ones)
`tests/scripts/readerCorpusManifest.test.js:79-83` — "the derivation is the golden test's own arrow, read from its source and compared field for field" — regex-reads `const keyOf = (c) => [...].join('|');` out of `tests/property/generatorGoldenMaster.test.js`'s SOURCE and compares the fields to `scripts/lib/golden-corpus-key.mjs`'s `GOLDEN_KEY_FIELDS`. MEASURE car 1 (`b74daab7a`) moved that arrow, with `TIERS`/`CULTURES`/`TERRAINS`/`TERRAIN_ROUTE`, into `tests/helpers/goldenMasterCorpus.js` and imports `{ goldenCorpus, keyOf }` from there — so the regex finds nothing and the arm reads `false`. The composition is still exact (the manifest's 525 rows key identically); only the SOURCE-READING arm lost its file.

## The cure — single source of truth, the arm reading where the truth lives
1. `tests/scripts/readerCorpusManifest.test.js`: the derivation arm reads the arrow from `tests/helpers/goldenMasterCorpus.js` (the file that now defines `keyOf`) — update `GOLDEN_TEST_PATH` (or add a `GOLDEN_KEY_SOURCE_PATH`) and its docblock sentence; keep the field-for-field comparison against `GOLDEN_KEY_FIELDS` byte-for-byte as it is; add ONE arm that asserts the golden test still IMPORTS `keyOf` from the helper (so the source-of-truth cannot silently fork back), and ONE that asserts the helper's `keyOf` output equals `goldenKeyOf` from `scripts/lib/golden-corpus-key.mjs` on three real rows. Do NOT re-add a second `keyOf` to the golden test.
2. If the helper's arrow is not spelled in the exact form the regex expects (`const keyOf = (c) => [c.a, c.b, …].join('|');` on one line), re-spell it so (no behaviour change — assert output identity on the 525 rows before/after) rather than widening the regex.
3. ARMS: `npx vitest run tests/scripts/readerCorpusManifest.test.js` all arms green; `tests/property/generatorGoldenMaster.test.js` unchanged in its banked shape (525 drift, the known row — do not touch it); `tests/property/dossierProseManifest.test.js` drift `[]`; the whole `tests/lint` once; strict on its ceiling; voice E2 the two banked files; OSR/PN exact; typecheck; the lighting door by ritual (4b) if a title moved (it will if you add arms).

## Discipline, fences, commits — as car 3's
Runner count 0 in its own shell call before every vitest run; focused one at a time; never the whole suite; no register `--write` but the lighting door. Change only: `tests/scripts/readerCorpusManifest.test.js`, `tests/helpers/goldenMasterCorpus.js` (spelling only, if needed), the receipt, the lighting baseline (4b). Commit `MEASURE car 4: the reader-corpus derivation arm reads the golden key where it now lives — single source, field for field`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: MEASURE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha(s); the arm's line; the 525-row identity; the whole-tests/lint summary; the lighting tuple if 4b.
