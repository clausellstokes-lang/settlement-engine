# LANE: DESK-WARFAITH — write the warFaith desk and mount its 9 blocks across two tabs (DESK-4)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-class content + wiring · nothing owner-gated⟧

READ FIRST: `$SC/briefs/_PREAMBLE.md` → `$SC/briefs/_DESK-LAW.md` → your tree's `src/domain/display/stateProse/dossierMounts.js`
WHOLE → `stateProseKernel.js` WHOLE → one finished desk end to end as the pattern (`economyStateProse.js`,
`src/components/new/tabs/EconomicsTab.jsx` + `EconomicsGlance.jsx`, `tests/domain/economyStateProseDesk.test.js`,
`tests/ui/economicsTabFlow.test.js`) → the corpus leaf `src/data/dossierStateProse/` for warFaith (every pool, every
`"marks"` array) → `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` (the annex; the one-fact-one-sentence law C3 / R-DST-A).

## YOUR DOCK
`$SC/laneWARFAITH` detached at **`940d161ca`** (the 23-car desk consist over `90702c3e9`; 36 blocks dark, 33 mounted).
Porcelain 0 at start. `node_modules` are symlinks incl. `pg`. This consist will be REPLAYED onto the product tip by the
chair; do not rebase.

## THE FACTS (DESK.json DESK-4, measured at 15c6368a6; re-measure at your tip)
- The `warFaith` leaf: **9 blocks — DS-WAR-1..5, DS-FTH-1..4 — 138 pools, 418 authored variants**, ALL dark. It is the
  ONLY leaf with NO desk module (`ls src/domain/display/stateProse/` has no `warFaithStateProse.js`; darkness proof:
  `PATRON: rankAxis` and `WALLED-QUIET` return zero src files — re-run that probe).
- Its blocks split across TWO host tabs (war footing / sieges / treaties → the war tab; creed standing → the faith tab),
  so it is the first real test of the one-fact-one-sentence law across a page-set boundary: each block gets at most ONE
  `sentence` rung per page-set; decide which tab SPEAKS each fact and give the other tab a `glance`, and write the reason.
- Host tab headroom was 367 / 147 effective at 15c6368a6 (DESK.json evidence) — the two figures were recorded against
  the 600 layer ceiling; RE-MEASURE both at your tip and quote them; extract a Glance leaf if a tab would cross 600.
- ⛔ **THE REVIEW CORPUS IS DEITY-FREE — 0 of 44 settlements carry a patron.** A faith mount cannot be WALKED until the
  owner's corpus act; it CAN be built and pinned on a fixture that carries a patron in the producer's real shape. Say in
  the receipt exactly what the faith positions render on a patron-less settlement (it must be honest and non-empty at the
  glance rung, and it must NOT invent a creed).
- **DEITY DOCTRINE (owner, constitutional):** faith is CULTURE — standing, institutions, observance, legitimacy — never a
  theological claim. Pool keys are producer tokens; the prose the corpus already holds is what renders; you write NO new
  corpus prose. If a pool is unreachable from any producer, that is a FINDING (declare, pin, name the one act that
  lights it), not a reason to widen the key.

## THE ACT
1. Re-derive: the 9 ids are in `UNMOUNTED_BLOCKS`; the pool/variant counts; the `"marks"` dimensions of every warFaith
   pool; the producer tokens each pool keys on and WHICH PRODUCER writes them (name the file:line for each token).
2. Write `warFaithStateProse.js` in the kernel's pattern (pure, headless; state → pool key; assert the map TOTAL in both
   directions as the mount law demands).
3. Mount the 9 blocks: rows appended to `DOSSIER_MOUNTS`, ids struck from `UNMOUNTED_BLOCKS`, REAL draw sites in the two
   tabs, `publicDossier`-gated. Sentence/glance split written down per block.
4. Tests: `tests/domain/warFaithStateProseDesk.test.js` (NEW — the one new test file this lane is allowed; name it in the
   receipt for the three censuses) and UI flow coverage for BOTH tabs, including the public-dossier negative.
5. Proof per `_DESK-LAW.md`; plant-out; receipt `$SC/receipt-desk-warfaith.md`; predicted register deltas.

## ⛔ FENCES
No new corpus prose. No edits to other leaves' rows. No `OutputContainer.jsx` growth (physical-line-neutral only). No
register acts. No vitest outside the quiet window + mutex. `Lane: DESK-WARFAITH`.
