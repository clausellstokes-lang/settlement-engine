# LANE: REMEMBRANCE — O-5 / RR-2, a reader for what `warMemoryEnabled` already writes
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · funded by §893 ruling 3.

## WHY THIS IS FUNDED, AND WHY IT GOT MORE URGENT AN HOUR AGO
`warMemoryEnabled` writes records that **nothing in the product can read**. A door that lights into a
void is not lit, and §881.4 asks every dark door to light as a DECLARED SHIFT.

⭐ **The escalating fact, measured at train tip `62afd84d3`:** `scripts/review/readerCorpus.mjs:116`
freezes `PREVIEW_OVERLAY` with **`warMemoryEnabled: true`**. So the review corpus **the owner will
actually walk** turns this flag ON. Without a reader, the walk meets silence at precisely the place
the preset promises a shift — and the owner's walk is one of only two things still owner-gated. This
is no longer a nice-to-have; it is a defect in the walk's own instrument.

## THE ACT
Build the reader surface for the war-memory records: find what is written, where, and in what shape,
then give it a home a player can see. **Establish the write side by reading the producer, never by
assuming the record's shape** — a unit-less or under-specified field gets a different reading at every
consumer, which is a recorded hazard of this program (§711.6).

## ⛔ DISCIPLINE
- **The car is DARK-INERT: it must build behind the flag and cost nothing when the flag is off.** That
  is what makes it droppable at ZERO product cost if it disappoints, which is the ground on which the
  chair funded it ahead of any further word.
- **Trace the full lifecycle before you design the surface:** create → read → persist → regenerate →
  undo → import. This program's most-bitten bug class is the write that survives one path and ghosts
  another. Prove survival by an actual round trip on each path, not by reading the code.
- Do NOT widen the flag's reach or change what it writes. This car adds a READER. If the records turn
  out to be insufficient to read from, **STOP and report that** — do not quietly change the producer.
- PREDICT every register figure in writing before any instrument runs. You take no register door; the
  chair takes every register act at the landing.
- ⚠ A new test FILE reds three censuses and the known-failure census is FULL at 10/10. Extend an
  existing test file.
