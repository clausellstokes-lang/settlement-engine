# LANE: VIS-RESOLVER — the first-paint arm follows the general desk's field indirection
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## THE RED (the composition's last content red; `whole-900d.log`)
`tests/lint/dossierMountRegistry.walker.test.js` › THE SHIPPED TABLE lists `economics.craftReason` and `history.founded` as "this arm can follow no draw site for it, so its visibility is unknown". Cause: the general desk never names a mount id in a tab — `src/components/new/generalDeskRead.js` binds `<field>: … line(<X>_MOUNT, …)` with `const <X>_MOUNT = '<id>'`, and a tab reads `generalDeskLines(s, …).<tab>.<field>` (EconomicsTab:328 `const craftReasonLine = …economics.craftReasonLine`; HistoryTab:37 `const {identityLines, foundedLine, recordLine} = …history`; ViabilityTab:43; OverviewTab:201 a multi-line destructuring; PlotHooksTab:32 inside `useMemo`; RelationshipsTab:106 and SteadingsSection:44 hold the whole desk object). The resolver (`drawSitesIn`) follows `mount=` props and `drawnAtMount(…)` carriers only.
⚠ The chair tried a fix (kept at `$SC/superseded-899/dossierMountRegistry.walker.chair-attempt.js`): a `generalDeskFieldsFor(mountId)` table parsed from generalDeskRead.js + a carrier pass over `const <name>|{…} = …generalDeskLines(…)…`. It made MORE mounts unresolved (overview.institutions/market/steadings, relationships.network). Read it for what not to do; do not trust it.

## YOUR DOCK
`$SC/laneVIS2`, detached at the §900 composed tip (101 cars over 38474a59e; porcelain 0). The chair replays your car onto the composition and then takes the registers. ⛔ Never rebase; never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content. Vitest through the mutex; never during a chair gate.

## THE ACT
1. Extend the resolver so EVERY general-desk mount (the `*_MOUNT` constants in generalDeskRead.js, ~19 ids) is followed to its draw site(s) in the tab that renders the field: resolve the field names from the reader's own source (never a hand list), then in each tab follow the carrier the field lands in — a named const, a destructured name, a whole-desk object (`relDesk.network…`, `desk.steadingLines`), a `useMemo` result. Every one of the ~19 must classify (OPEN / CLOSED / CONDITIONAL), none "unknown".
2. Prove it two-sided: run the arm at your base (the two unknown rows), then at your tip (zero unknown, and the verdicts printed per mount); plant one general-desk mount into a `defaultOpen={false}` Section and show the arm reds it BY NAME; restore `cmp`-clean. Keep every existing `it()` title unchanged (fold new assertions into existing titles or add one and RECORD the title delta); the walker exports helpers other suites import — count importers before adding a title.
3. If any general-desk mount classifies CLOSED or CONDITIONAL, do NOT re-host: report it with the host and the reader-reach figure; the chair rules.

## FENCES
No product change; no register act (record deltas: lighting titles if any moved). One car. `Seat: Opus 5 — Fable-unvalidated`, `Lane: VIS-RESOLVER`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-vis-resolver.md`, PARTIAL header first, the per-mount verdict table, RETROVALIDATION ROW last, tip sha last.
