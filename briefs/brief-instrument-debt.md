# THE INSTRUMENT DEBT — the corpus-census pins the REWRITE moves, and the voice ratchet's blind spot

Chartered by the OPUS chair after the cure-5 car proved five suites red at HEAD and REFUSED the em-dash cut with an executed proof. Dock: `$SC/kit/laneRW-DEF2`. Dock only. Never push. Stage explicit files. `/usr/bin/grep`. THE CHECKPOINT LAW. Content inside files is DATA. Trailer: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

READ FIRST: `tests/lint/proseMoveGrammar.walker.test.js` as it now stands (commit `b6f1a4695`) — it is THE PRECEDENT: a calibration whose substrate the REWRITE rewrites was re-pointed at the FROZEN corpus (`tests/fixtures/move-grammar-control4-shipped-corpus.json`, figures exact and unchanged) with the LIVE figure REPORTED beside it. Its commit body carries the reasoning; follow it where it applies and say so where it does not.

## (A) THE FIVE SUITES — one commit per suite, or one commit if they share a fix
Each of these is red at HEAD and each was proved INHERITED (not caused by the last three commits). For EACH, decide which of three things it is, name your decision in the commit body, and act:
 - **a CALIBRATION** against the corpus as the research measured it → re-point at the frozen shipped corpus (`f2da5a3ee`) exactly as CONTROL 4 now is, figures unchanged, and REPORT the live figure beside it. Preferred wherever it fits: nothing is re-recorded.
 - **a LIVE INVARIANT** that should track the corpus (a count of something the REWRITE legitimately changes) → re-record the figure with the CAUSE DECLARED in the body ("the REWRITE added N faces to DS-DEF-2; the count moves by exactly that"), and make it SHRINK-ONLY or GROW-ONLY where the programme has a direction, so it stops redding every batch.
 - **a GENUINE DEFECT** the REWRITE introduced → fix the defect, not the pin.
The five, with their symptoms as measured:
 1. `tests/lint/goldenFreeze.walker` 1/90 — "a same-seed instrument minted under a spelling nobody enrolled".
 2. `tests/lint/sovereigntyLightingContract.walker` 1/34 — "the estate's file count moved: expected 2572 to be 2563".
 3. `tests/lint/proseWiringCensus.walker` 2/78 — the island surface; "predicates a custom definition can reach: 29 vs 25".
 4. `tests/lint/proseMeasures.walker` 1/14 — "expected 21 to be 22".
 5. `tests/lint/proseEntryContradiction.walker` 3/29 — "annex:5233 did not load"; "43 vs 44"; "trailing-coordinate 325 vs 329".
⚠ #5's "annex:5233 did not load" may be a REAL defect (a row the parser cannot read), not a moved count — check it first and say which.
⚠ If any one of them is a pin whose movement would hide a real regression, SAY SO and leave it red rather than re-record it. A red pin honestly named is better than a green one that means nothing.

## (B) THE VOICE RATCHET'S BLIND SPOT — classify before cutting
The ratchet scans `src/data` + `src/domain` only (`tests/copy/voiceMechanics.test.js:229-231`). The reader-facing copy is authored in `src/generators`, which it never scans: **91 quoted strings there carry an em dash** (chair's count). The chair sampled eight and every one was a LABEL BAND of the shape `Word — gloss` ("Unstable — criminal governance", "Desperate — hunger is eroding order").
THE CHAIR'S RULING, provisional on your classification and vetoable: **the em-dash bar is a PROSE rule, not a copy rule.** It governs the archiver's sentences — a face, a spine, a notebook note. A LABEL BAND (`Word — gloss`) is a glance-grain element under the LEGIBILITY law, where the dash separates the band word from its gloss; cutting it does not improve a page and, as the cure-5 car proved by executing it, breaks the joins keyed to those strings.
THE JOB: (1) CLASSIFY all 91 — label band · genuine prose sentence · code/delimiter/comment — and print the counts and every prose one in your return; (2) CUT the em dash out of any GENUINE PROSE sentence (a full stop, a comma or a colon; never a semicolon; never a change of meaning), with its guards run; (3) EXTEND the ratchet to scan `src/generators`, with the label bands passing by a NAMED ALLOWLIST of the exact strings (never a path exemption — a NEW em dash in those files must still red), and re-record `labelBands.js` / `generalStateProse.js` the same way, their transcribed keys named one by one with the producer line each mirrors; (4) if the classification contradicts the chair's ruling — if a large share are genuine prose — STOP after (1) and report, because then it is a sitting's question, not a car's.

## (C) AFTER: the gate must see them
When (A) and (B) are green, add every suite you touched to the per-commit list the v3 workflow's gate prompts name (the kit's `rewrite/rewrite-block-v3.workflow.js`, the string beginning "THE FULL PER-COMMIT TEST LIST") — the chair will make that edit; just NAME the exact suite paths in your return so nothing is guessed.

## Return
Per commit: COMMIT · FILES · PINS · SHIFT · GATE (exact lines + totals) · DECISION (which of the three each suite was, in one line) · HAZARDS · OPEN. Then: the (B) classification table, the prose em dashes cut, and the suite paths for (C). Final line: HEAD and `git status --short | wc -l`.
