# L-HOMES — the lighting wave's dark-inert builds: NINE cars, one consist, one gate (lighting POSITION 2)
⟦Chair: Fable 5.1 · every car an Opus 5 lane (`model: "opus"`) · each lane is dispatched with ONE section of this file · boards AFTER the PROSE consist lands and AFTER the chair's L-PROBE dark arm is measured⟧

## COMMON LAW FOR EVERY L-HOMES CAR
Read `$SC/briefs/_PREAMBLE.md`, then `git show refs/preserve/light-plan-2026-09-05:PLAN.md` §1.1 (your row) + §6 POSITION 2
(the car table) + §4 (the golden-freeze machinery is PRESENT and UNFROZEN) + §8 (the instrument-and-STOP table), then the
sealed docket row `git show refs/preserve/session-kit-893-2026-09-04:pending/LIGHTING.json` (your `id`: `what`, `evidence`,
`blocked_by`). **Every car is DARK-INERT: it lands with every door it touches still dark, same-seed neutral — prove that
with one fixed-seed run before/after (byte-identical), and prove the machinery works with the door FORCED in a test.**
⛔ `simulationRules.js` is regexed as RAW SOURCE by the lit-coverage walker: **naming an unminted flag in a COMMENT mints it
into the denominator** — no flag names in prose anywhere in that file except the one you are minting. ⛔ A test file that
reads a golden-adjacent env spelling (`UPDATE_GOLDEN`, `GOLDEN_SHIFT_SIGNED`, …) must be ENROLLED in
`tests/fixtures/.golden-freeze-register.json` or EXCLUDED there in the SAME commit (`goldenFreeze.walker` runs on every
gate). ⛔ `subsystemRowsVirtual.js` sits at its 800-line size ceiling — new rows go to `subsystemRowsOps.js`, character
to `subsystemRowsLives.js`. Registers are the CHAIR's at the landing (lighting census, ratchet, voice, size, writer-reach,
the four censuses per new `src/domain` leaf, the golden-freeze register) — you RECORD predicted deltas. Quiet-window law
+ mutex before any vitest; `tests/lint/` WHOLE; `typecheck:domain:strict`; eslint; plant-out; commit per car; trailers
`Seat: Opus 5 — Fable-unvalidated` + `Lane: <id>`; receipt `$SC/receipt-<id>.md` with a RETROVALIDATION ROW. Dock: given
at dispatch (a fresh dock at the post-PROSE tip). The consist's STOP: any hashed-chunk listing diff over `(margin − 100 B)`
— the chair builds and reads it; you predict eager bytes = 0 and say why.

## CAR 1 — LGT-P1-BIRTH — fresh births resolve the LIT preset (first car; the structural one)
`NEW_CAMPAIGN_SIMULATION_PRESET_ID` = 0 files today. A default campaign is born by plain `normalizeSimulationRules()`
(`worldState.js` ~:317/:527/:530 — re-derive), so a lit preset table would light NOTHING for a fresh campaign. Touch
`buildNewCampaign`, `campaignImportedCreation`, `createDefaultWorldState`; add a sibling pin beside
`simulationRules.js:136–145`. ⛔ INVARIANT: `ensureWorldState` keeps plain `normalize()` — an INSTALLED campaign must never
acquire virtual keys (THE PROMISE: a seed is a starting world forever). Dark-inert because the lit preset resolves to the
same rules as today until L-DEFAULT edits the table. Deliver a REAL-birth fixture per preset (through `createCampaign` →
`buildNewCampaign` / `createImportedCampaign`, NEVER through `SIMULATION_RULE_PRESETS[id].rules`) — CAR 9 consumes them.

## CAR 2 — LGT-P2-MANIFEST — split the flat key list into a gated REGISTER and a derived VIRTUAL set
`ENGINE_GATED_VIRTUAL_RULE_KEYS` is one flat list of 30. ⛔ INVARIANT: a key may be LIT without being DELETED from the
register; today the two lighting contracts INVERT the moment a key lights. Re-point `tradeConvergenceContract.walker`
(~:478–489), `sovereigntyLightingContract.walker` (~:1690–1692), the five fences' virtuality arms, OSR clause 3,
`contributionLedgerShape` (~:90) — re-derive every address. The engine-gated walker's "bank wins" arm (~:789–794): the
backlog (17, at ceiling 17) must equal the measured gated-but-uncensused set EXACTLY — prove it before and after.

## CAR 3 — LGT-P13-FENCES — a dormancy fence per no-fence key (twelve)
Derive the twelve: keys in the gated register with no `*DormancyFence` arm (the five existing fence files under
`tests/property/` are the pattern). Shape per key: a DARK half + a LIT-MUTANT control, so the dark claim is provable AFTER
lighting (otherwise lighting destroys the only state in which darkness was exercised). ⚠ The fence FILES are themselves
surfaces in the golden-freeze register (`espionage-dormancy-fence` names the test file as its path) — enrol/exclude in the
same commit. A DORMANCY claim is a BIT claim (§713.2): the control is a hash, not a sentence.

## CAR 4 — LGT-P5-WOPS — the four W-OPS door mints (CR-WR10-C shape, by-name ==)
Four built subsystems with no door: `envoyTaskCatalog.js` (~:622 "NAMED, NOT MINTED"), `operationsVoice.js` (~:605),
`infiltrationDepth.js` (~:61 "NO FLAG IS MINTED"), `missionDispatcher.js` (~:66–68). Mint each flag DARK (false in every
preset), on the CR-WR10-C shape; rows to `subsystemRowsOps.js`. Must precede the espionage flip (L-DEFAULT hunk 6).

## CAR 5 — LGT-P6-ESPWIRE — the espionage Herald wiring
0 of 18 files under `src/domain/worldPulse/espionage/` write a news entry. Wire missions, captures and products into the
Herald/news seam under the NEWS ADDRESS LAW (address chain + typed action + names + reason). `narrativeParity.test.js`'s
CLAIMS-PARITY arm convicts a lit mechanism with no backing news — exercise it with the flag forced on. Dark-inert.

## CAR 6 — LGT-P4-READER — the Remembrance reader, RR-2 (O-5 RULED BUILD at §882.13)
`warMemoryEnabled` writes records nobody can read. Build the World Book realm chapter + the Herald war section, LAZY,
`PROSE_RENDER` only (CH-9). The ONE place a new UI test file is justified — it pays the censuses by name. Faith is
culture, never theology; a named character's fate is never written (STATE, NEVER FATE).

## CAR 7 — LGT-P7-DRIFTDOOR — the `characterDriftEnabled` door + funnel wiring
3 src files name the flag (`characterDrift.js`, `envoyChanceMeet…`, a subsystem row); NO manifest entry, no caller.
⚠ Shared with ENCOUNTERS by the amended O-9: ENCOUNTERS landed (§893.2) CONSUMING the flag without minting — so this
car OWNS the door. Read the ledger's history first: `git show review-fixes-2026-07-08:docs/HANDOFF_CURRENT.md` (THE DRIFT
DOOR — the §890.1 withdrawal; "the funnel door is reserved to TE-VIRT-1 by a LANDED test, and TE-VIRT-1 already ran") and
the §893 row's RULINGS §2 (three parts). If the landed test still reserves the funnel door, that is a STOP with the
file:line, not a test to soften. Mint DARK; `subsystemRowsLives.js` row; a fence (CAR 3's shape).

## CAR 8 — LGT-P8-MATBOUND — the materialization create boundary
The generate action is both BIRTH and REGEN today; a dial minted there would restamp lived worlds on their next regen —
a PROMISE breach (L-MAT's STOP: `_livingContentLawVersion` stamped on an EXISTING world on regen). Decide which store path
is BIRTH, make the boundary explicit, and pin: regen of an existing world stamps nothing. The dial stays dormant here.

## CAR 9 — LGT-P14-WITNESS — the per-preset NEW-CAMPAIGN witness (after CAR 1)
`tests/simulation/presetLightingWitness.test.js` + `tests/fixtures/preset-lighting-witness.json`: a 52-tick hash per
preset, born through CAR 1's REAL birth path (a fixture born any other way measures a world no customer receives). The
wave's ONLY bit-level witness: a later silent re-darkening of the default preset must red it. NEW test file — deliberate,
pays the censuses by name — and ⛔ the golden-freeze register row (or exclusion) in the SAME commit.
