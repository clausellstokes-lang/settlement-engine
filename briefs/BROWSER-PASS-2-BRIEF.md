# Browser pass 2 — the final consist (Opus verification lane)

Preview: `.claude/launch.json` entry `consist-preview` (port 5219, strictPort) on the consist worktree; use preview_start {name:"consist-preview"}. Desktop natural size AND phone (375×812) — the pane's screenshots after a scroll are unreliable: prefer read_page/find/javascript_tool probes and zoomed screenshots.
Forge THREE settlements (a village, a town, a metropolis; different cultures incl. a Mesoamerican-inspired one).
Walk, with receipts (DOM probes + screenshots):
1. The owner's five findings: NPC wants printed ONCE (no arrow duplicate); the Power tab's per-bucket "why" never repeats "(a commercial institution of this power)"; the threat assessment reads as sentences under their bars, not a stack; the Overview's description is ONE cohesive paragraph and sits second-highest; every "Tonight at the table" card is whole (no cut-off).
2. Phone: every dossier view's prose ≥ 14 px and chrome ≥ 12 px (probe computed styles on a sample of 30 elements across 6 views); no horizontal scroll; the header serves the painting as painted (NO parchment slip, NO plaque under the active word; the focus ring works with keyboard Tab).
3. Labels: status words in sentence case on screen; no ALL-CAPS field names outside eyebrows; "Arcane Support · Present/None" on Defense; the Overview magic row bands its score; Economic Backing at a score in 20–24 reads Underfunded (find one or force via a seed sweep of 10).
4. Institutions: "House of worship" everywhere (no "Parish church" on any surface, incl. the Services attribution, the Compendium catalogue, the institution card's title and sentence); "Burial grounds".
5. Food: the Economics tab's food sentences add up (production %, imports, magic; no "Agricultural surplus of 0%"); the Overview's Food Security band matches.
6. Anonymous draft: generate signed-out → refresh → the world is still on screen; sign-in path not required.
7. PDF: download the canon dossier for one town (the print prose present; Defense capability rows print band words, not digits; labels in sentence case; the relationships network prints on a LINKED world if one exists, else note it).
6b. Anonymous draft, the two-tab rule (lane 23): generate signed-out in tab A; open a second tab B at /realm and generate a DIFFERENT world; refresh A → A shows ITS world? (probe localStorage['settlementforge'] state.anonDraft / the envelope key: it holds B, the last writer; A re-adopts nothing foreign — report exactly what A shows, this is a check not a pin). Then in B: Clear/New (the editor emptied) → refresh B → the retired world must NOT come back (the slot is null). Rename a draft → refresh → the rename survives.
9. Brand derivatives (lane 24, §934.17): /favicon.ico and the <link rel=icon> set resolve (no 404 in read_network_requests), the favicon is the painted wax seal (zoom a screenshot of the tab strip is not possible — fetch the PNG bytes' size and confirm the image renders by loading its URL in a tab); the OG image (<meta property="og:image">) URL loads and shows the plaque + seal cut from the painting; the Founders' Hall page (/founders or the route in the nav) shows the small seal at its heading; Lora is the computed font-family on the landing hero, the dossier headings and the Founders' Hall (javascript_tool getComputedStyle on three headings); the in-app HouseDevice (the seal mark) renders where the geometric device used to.
10. PDF cover (lane 24): the downloaded dossier's first page carries the small seal beside the title (open the PDF in a tab, screenshot page 1, zoom the title block).
11. Relationships (lane 22, if composed by the time you run — check `git log --oneline -3` in the consist worktree for "relationships reader"): the Relationships tab renders for a world with neighbours; the PDF's relationships network section prints the same names as the tab.
8. Console: zero errors/warnings on /, /create, /realm, a dossier, at both widths.
Report ≤ 600 words: PASS/FAIL per item with the probe or screenshot path; anything else seen.
