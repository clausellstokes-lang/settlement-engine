# Brief — INSTR-912 car 10 (and 10b if a register moves): THE SECOND FOLD'S CURES — 14 code, 32 receipt, the firings re-run, the tiers re-printed

Seat: Opus 5 — implementer. The chair (Fable) has ruled; you build and measure. Receipt first (append `## CAR 10` to `$SC/receipt-instr-912.md`), then the car(s). `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`.

## Where
- Dock: `$SC/laneINSTR` (detached-HEAD worktree; tip **454d478a1** = eleven commits over 3b1c0eaa5; porcelain 0). `git log --format='%h %s' -12` first. Never touch the main tree; never enter `laneLMAT`, `skepINSTR`, `skepINSTR2`.
- Read whole, in this order: (1) `$SC/skeptic-instr2/FOLD.md` (273 lines: §3 the 46 cures, §4 the figures the ledger will carry, §5 the rebase hazards, §6 the U rows); (2) `$SC/s12-sitting/SITTING-RULINGS-912.md` **§M** (the chair's rulings; where a cure and §M differ, §M wins); (3) the three lens files under `$SC/skeptic-instr2/` (`census.md`, `cures.md`, `receipt-fences.md`) for each cure's exact figure and site; (4) the amended specs: `$SC/prose-research/RULES-V2-PART-B.md` §18, `$SC/prose-research/sweep/CLERK-LAWS.md` §2.6.1 + §1.2 NOTE 2, `$SC/prose-research/sweep/MOVE-GRAMMAR.md` §4.4.2; (5) `$SC/receipt-instr-912.md` §§ CAR 8 and CAR 9.

## What to build
1. **Code cures 1–14 of FOLD-2 §3a**, each with the ARM its line names, executed: (1) `guardAt` anchored to the LAST `if (` before the matched literal, with the four-`if` fixture composer; (2) `resolvedWithPredicate` and `resolvedWithCleanPredicate` published and ASSERTED as integers, with the plant that returns `predicate: []` unconditionally reddening the new integer while 310/398 stay green; (3) `spokenTo` normalised with `rootOf` and unioning `fieldsRead`, the two-fixture arm; (4) the table rung's synthetic `readField` excluded from the unheld-field arm; (5) THIN, COVERED, 65, 140 and 158 asserted as integers; (6) the pair-array table shape read (the four DS-POW-2 rows move UNRESOLVED → RESOLVED, present-then-absent); (7) double-quoted literals stripped before the key scan; (8) `prepared` keyed on `file::fn` with the same-named-pair fixture; (9) `walkPair`'s claim key carrying the finding's SITE and `notExecutable` the union of BEFORE and AFTER (CLERK-LAWS §2.6.1), the executed BEFORE-1/AFTER-2 case reading `added 1`; (10) the byte fence generalised to ALL TEN island modules (H8) with the non-blindness control; (11) cure 6's third source (`coinFlows.taxed`) guarded so a `NaN` read reds; (12) cure 2's arm — the four probe4 sentences as fixtures, reverting `locate()` must red; (13) cure 15's arm — `DS-POW-5::autocrat#2` WITHHELD asserted, stubbing the semicolon limb must red; (14) cure 12's arm — an `existsSync` on the docblock's named fixture path.
2. **The re-measurements the fold owes:** re-run `$SC/instr-912/firings.mjs 200` at the tip ON CAR 9'S CORRECTED SEQUENCE (every reading a caller passes, `politics` included; the general desk's bare strings harvested) and re-bank the pool-firing count with the absence-pool keys named (cure 22; FOLD-2 §6 row 3); after cures 1, 3, 6, 7 land, re-take §8.2's census command and print the corrected tier line beside car 8's (U4) — MISSING, THIN, COVERED, clean predicates, unheld-field predicates — every figure asserted as an integer (cure 5).
3. **Receipt corrections 15–46** as `## CAR 10 — CORRECTIONS TO CARS 8–9`: one row per item, the superseded sentence QUOTED (never described — P16), the measured one with its figure or command, the cure number; plus the sweep's `#79` and `#77` comment corrections (cure 20; P22), the md5 of the committed bytes (cure 24), "sixty" → 72 (cure 25), the DORMANT qualifier in every summary (cure 29), the U8 seed (cure 37), the duplicate `# NN.` numbers recorded (cure 40).
4. **Fences and register:** change only the island (`src/domain/prose/*`, `src/domain/institutions/institutionTable.js`, `tests/helpers/dossierCorpus.js`, `tests/helpers/dossierComposedFill.js`, the lane's `tests/lint/*.walker.test.js`, `tests/fixtures/*`), `scripts/mutation-sweep.sh` (comments and any new plant numbered after #82, label unique, FOUND BY LABEL), `scripts/mutation-coverage-manifest.json` (by text, the dock's current serialization), `tests/lint/.lighting-census-baseline.json` (10b only, by the ritual), `$SC/instr-912/*` (the probes), the receipt. No product surface, composer, pool text, persisted shape, seed input. The ten-module byte fence must be green at commit.
5. **Measurement discipline:** the split-pattern runner count 0 before every vitest run (`V=vit; V2=est; pgrep -fl "$V$V2" | grep -v gate-mutex | wc -l`); focused files one at a time; never the whole suite; never `--write`/`--update` on writer-reach or the OSR; every count an integer; every control's tail pasted; a refusal carries its measurement.
6. **Commits:** `INSTR-912 car 10: the second fold's cures — <clause>` and, only if the lighting census moved by its ritual, `car 10b`. Explicit staging; porcelain 0 and zero untracked after; no push, stash, rebase, amend. Trailers exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
7. **Return:** the car sha(s); a table of the 14 cures (cure → arm → executed tail in one line → DONE / REFUSED-with-measurement); the corrected census line (clean predicates · MISSING · THIN · COVERED · unheld · the pool-firing count with its absence-pool keys) beside car 8's; the count of receipt rows (must be 32: 15–46); the focused-suite tallies per file; anything owed. A claim without its executed tail is not a claim.
