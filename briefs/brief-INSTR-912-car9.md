# Brief — INSTR-912 car 9 (and 9b if a register moves): THE FOLD'S CURES — 18 code, 30 receipt — under the chair's rulings

Seat: Opus 5 — implementer. The chair (Fable) has ruled; you build and measure. Receipt first (append `## CAR 9` to `$SC/receipt-instr-912.md`), then the car(s). `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad`.

## Where
- Dock: `$SC/laneINSTR` (detached-HEAD worktree; tip **27c24522c** = nine commits over 3b1c0eaa5: cars 1–7, 7b, 8 the wiring census, 8b). `git log --format='%h %s' -10` first; porcelain must be 0. Never touch the main tree; never enter `laneLMAT` or `skepINSTR`.
- Read whole, in this order: (1) `$SC/skeptic-instr/FOLD.md` (242 lines — the 60 cures are its §3; the rebase hazards its §4; you do NOT rebase); (2) `$SC/s12-sitting/SITTING-RULINGS-912.md` **§L** (the chair's rulings on every spec item and the R11/R12 label — they decide how each cure is cut); (3) `$SC/prose-research/RULES-V2-PART-B.md` §18, `$SC/prose-research/sweep/CLERK-LAWS.md` §2.4.1 + the §1.2 NOTE, `$SC/prose-research/sweep/MOVE-GRAMMAR.md` §4.4.1 (the amended specs you build to); (4) the five lens files under `$SC/skeptic-instr/` when a cure's figure needs its source; (5) `$SC/receipt-instr-912.md` § CAR 8 (what car 8 built: `src/domain/prose/wiringCensus.js`, `walkPair`, the tiers, `$SC/instr-912/firings.mjs` and `probe-census8.mjs`).

## What to build
1. **Every code cure 1–18 of FOLD §3a**, BLOCKING and WAVE alike, each exactly as its "ARM:" line demands and as §L rules where the two differ (§L wins): cure 4 → the chronicle labels stay as the loader stamps them (R11 letter, R12 quiet) — the census heading becomes "chronicle (R11 · R12)" and the arm asserts every chronicle row carries exactly one of the two and every `letter.*` row R11; cure 6 → the three columns `closed: false` unless you read the named source in one import + one row and close on an executed arm, `holderRole` basis `'absent'`; cure 13 → arm D and C-pair/C-sibling keyed on car 8's census; cure 14 → arm E's fail channel, B2's firing control, BUDGET/SPREAD failable, NOT-EXECUTABLE rows for walls 4, 5, 7, 8, 9, 10, arm A's tag-gated split (§L item 67) with U5's fixture; cure 18 → FIRST check car 8's `firings.mjs`: if it already composes the six-desk 200-town sequence with the `politics` reading and harvests the general desk's bare strings, cure 18 is DISCHARGED by citation and you re-measure the V1 share and the run on it (write the two figures into Part B §18's blanks — the ONE spec edit you are allowed, by exact string replacement of `V1 share ____ · run ____`); cure 9 → de-duplicate the three nouns or withdraw the partition claim (module docblock + manifest label for plant #78) and replace the vacuous partition assertion with one that can fail; cure 10/72 → `openShare` in [0, 1] and the D8 FAIL arm behind a class-existence guard, both proven on fixtures; cure 71 → the three presence keys into `proseFingerprint.js`, fingerprints re-run where raw text exists under `$SC/prose-research/primary/`, `null` with the reason where it is gone (never fabricate).
2. **Every receipt correction 19–60 of FOLD §3b**, written as `## CAR 9 — CORRECTIONS TO CARS 1–8` in `receipt-instr-912.md`: one row per item — the section, the superseded sentence quoted, the measured sentence, the cure number. Never edit the earlier sections in place; strike by reference. Add to §10 the refusals the fold names (item 59) and to §11 the OWED rows (item 60), and the §L items the chair carries (the co-occurrence floor; the seven lost raw texts).
3. **The U rows** (FOLD §6): settle U4 before cure 3 (read PROBE_ALL's R4b source: does it exclude the three bare-string exports?); execute U1 (plant #77 under the sweep's own protocol) and U7 (dry-run one anchor cure on a scratch copy; expect the named red) and record both; U2 → name the seeds or withdraw the integers (cure 36); U3, U6, U8 → executed if cheap, else declared UNTESTED with what would test them.

## Measurement discipline (the lane's own)
Before EVERY vitest run: `ls $SC/HOLD-VITEST` must fail (it is absent now) and `V=vit; V2=est; pgrep -fl "$V$V2" | grep -v gate-mutex | wc -l` must print 0 (another lane's design workflow runs read-only agents on this machine — they run no vitest; if you see a runner, wait). Focused runs only; never the whole suite; never `--write` on writer-reach or the OSR; the lighting census by its own ritual in a separate register commit (9b) if any test file count or title moves. Every count an integer; every control executed with its tail pasted; every refusal with its measurement. Mutation plants you add are numbered after #79 (the dock's last) — the chair renumbers at the rebase; labels are the join key and must be unique. `scripts/mutation-coverage-manifest.json`: EDIT BY TEXT in the dock's current serialization; never re-serialize again.

## Fences
- Change only: `src/domain/prose/*` (the lane's modules), `src/domain/institutions/institutionTable.js`, `tests/helpers/dossierCorpus.js`, `tests/helpers/dossierComposedFill.js`, the lane's `tests/lint/*.walker.test.js`, `tests/fixtures/*` you need, `scripts/mutation-sweep.sh`, `scripts/mutation-coverage-manifest.json`, `tests/lint/.lighting-census-baseline.json` (9b only, by the ritual), `$SC/prose-research/primary/*.fingerprint.json` (the re-run), the ONE Part B blank, the receipt. No product surface, composer, pool text, persisted shape or seed input (THE PROMISE); the byte fence (car 8's arm) must still return exactly the lane's island.
- Commits: one code car (`INSTR-912 car 9: the fold's cures — <clause>`), a second (`car 9b`) only for a register that moved by its ritual. Explicit staging (`git add <paths>`); porcelain 0 and zero untracked after; no push, no stash, no rebase, no amend. Trailers exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
- Return: the car sha(s); a table of the 18 code cures (cure → arm → its executed tail in one line → DONE / DISCHARGED-BY-CITATION / REFUSED-with-measurement); the count of receipt rows written (must be 42: 19–60); the U rows' dispositions; the two K.2 figures; the final focused-suite tallies; anything owed.
