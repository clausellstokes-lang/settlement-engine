# LANE: DESK-GEN3 — the seven dormant-at-birth blocks GEN2 measured and did not reach, plus DS-GEN-18's route
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## READ FIRST, IN ORDER
1. `$SC/briefs/_PREAMBLE.md` — the hard rules. 2. `$SC/briefs/_DESK-LAW.md` — the mount law (keep-both mounts, complement-prune the dark list, module-evaluate, no duplicate sentence mount per page-set, the public-dossier gate as a DUTY of every mount — DESK-9's ARM 3 keys on importers).
3. `$SC/briefs/brief-DESK-GEN2.md` and `$SC/receipt-desk-gen2.md` — GEN2 mounted 6 of 15 and STOPPED on seven it measured MOUNTABLE (its RESIDUE section, lines ~236–262, is your work order; its FINDINGS 1–2 are already ruled: the router has 25 tabs; `{founder}` is a producer/annex disagreement the chair owns).
4. `$SC/receipt-desk-9.md` — the walker arm every mount must satisfy.

## YOUR DOCK
`$SC/laneGEN2`, detached at **`a4ce9f80f`** (GEN2's 4 cars over the desk consist base `940d161ca`, sealed `desk-gen2-2026-09-05`). Build ON TOP; never rebase; the chair replays `laneGEN2:940d161ca` into the §900 desk consist. ⛔ Never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content.

⛔ **VITEST HOLD IS IN FORCE** until the chair sends RESUME (a gate-class run is in progress). Do every non-vitest step first; record each proof as OWED-UNTIL-RESUME with its exact command (`sh scripts/gate-mutex.sh --run -- npx vitest run <files>`); run them when RESUME arrives. Capture every exit in-shell. In zsh an unquoted variable holding several paths is ONE word.

## THE WORK, in this order (each measured before it is built; each proved by planting)
1. **DS-GEN-8** (`lifecycleStatus`, dormant-at-birth, writer `settlementLifecycleFirstClass.js`; `history.ancientRuin` 2/48 opt-in; the steadings arm needs `spatialLedgers.satellites`). Mount under the DS-STR-2 precedent (a real writer off the generation path is a lawful dark mount); the opt-in arm renders only when its producer wrote.
2. **DS-GEN-15** (`urbanFabric`, writer `urbanFabricKernel.js`, read canonically through `fabricRead.js` — read through the canonical reader, never the raw field).
3. **DS-POP-1/2/3** (`populationHistory`, three worldPulse writers). ⚠ There is no `population` tab: the honest host is `overview`, where the population figure already renders; mount there, beside it, with ONE sentence mount per page-set.
4. **DS-REL-1** (`neighbourNetwork` etc., written by `SettlementsPanel.jsx` when a user links settlements). Host: `RelationshipsTab.jsx`, which already receives `publicDossier`.
5. **DS-GEN-18** — GEN2's route is measured (HOME-FED 24/24, STALLED 18/24, BOUGHT-IN 12–18/24, UNWORKED 13/24). Light the three unblocked pools. For STALLED, THE CHAIR RULES THE MECHANISM — **test it, do not trust it**: `{institution}` takes the NAME of one of the town's own `institutions[]` whose category matches the stalled chain's processing category (a singular, proper producer), and NEVER the plural category label `"Merchant guilds (3-8)"` (§0d's digit ban and the `proper` shape both refuse it, and the stripped form makes a number disagreement in front of a reader). If no institution of that category exists in the town, STALLED is WITHHELD for that town — silence, never a hole. Measure how many of the 18 STALLED towns have a matching institution before you build; if it is 0, STOP and report.

## FENCES
No new persisted shape; no change to any sealed corpus sentence (the words are the chair's — if a variant's slot cannot be filled honestly, WITHHOLD and report which); no register act (record deltas: mounts baseline, prose-numerics, writer-reach, lighting titles); no `{founder}` work (chair-owned); the `{institution}` producer cure touches ONLY the desk's producer path, never `activeChains[].processingInstitutions` itself. Every mount proved by planting the forbidden shape and watching the walker red.

## PROVE IT (OWED-UNTIL-RESUME where vitest)
Module-evaluate every touched module (`node -e "import('./src/…')"`), the three mount walkers + DESK-9's ARM 3, the desk suites, `tests/lint/` whole with every red attributed by re-running at base. Commit per step with `Seat: Opus 5 — Fable-unvalidated`, `Lane: DESK-GEN3`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-desk-gen3.md`, PARTIAL header first, RETROVALIDATION ROW last, tip sha last.
