# LANE: PANTHEON-ROSTER — one literal roster for the six registration freezes, so a minting commit can never fork them again
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## WHY (read `tests/domain/pantheon.test.js` A5's comment block first — it is the argument)
Twice now (WF-8a, then ENC-4) a kind-registering commit moved the four figures in `tests/lint/kindPoolFloors.walker.test.js` and NOT their hand-frozen twins in `pantheon.test.js` A5 (`registries`, the small-family list, `allRows`, `routedTokens`, `unvoiced`, the registered-minus-routed difference). The Fable chair moved them by attribution at §899 (chair car 4, `c6026a7a1`); ENC-4b/4c will move them again at §900. The test's own paragraph calls lifting the six into a shared roster "the strongest argument on record" and names it a chair act. **RULED (Fable 5.1, 09-05): the freezes stay LITERAL numbers (never derived from the tree — a derived freeze is an assertion that cannot fail) but live in ONE place.** That satisfies the earlier "literal by ruling" and removes the fork.

## YOUR DOCK
`$SC/laneROSTER`, detached at **`ec1b5e3e9`** (the §899 composed tip: 27 cars over product 5e28d5c83; porcelain 0). Do not rebase; the chair replays `laneROSTER:ec1b5e3e9` at §900. ⛔ Never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content. ⛔ **VITEST HOLD IS IN FORCE** until the chair sends RESUME: run NO vitest; do the build, module-evaluate with node, and record every proof as OWED-UNTIL-RESUME with its exact command.

## THE ACT
1. Read both tests' frozen figures and where each is asserted; read `KIND_REGISTRIES` and the helper beside it that the pantheon comment points to. Measure which of the six figures `kindPoolFloors.walker.test.js` also freezes (the comment says "the four figures"; re-derive the overlap — the roster holds the UNION).
2. Create ONE roster module under `tests/` (a test fixture, e.g. `tests/fixtures/kindRegistrationRoster.js` — a `Object.freeze`d literal, every figure a NUMBER or a literal list, with a docblock that says a registering commit moves THIS file and names the attribution rule: one registry row and one routed token per kind). Both tests import it and assert against it; no test derives a figure from the tree.
3. Keep every figure EXACTLY as it stands at your base (registries 12, the small families `['INFORMATION','FAITH','CHANCE_MEETING']`, allRows 114, routedTokens 380, unvoiced 274, difference 8 — re-read them from the files, do not trust this line). Zero behaviour change; the walkers must be green before and after with the identical figures.
4. Prove by planting: change ONE roster figure and show BOTH tests red (OWED-UNTIL-RESUME under vitest; under a plain-node dry run if you can drive the assertions without vitest). Restore, `cmp` clean.
5. Grep `tests/` for every other hand-frozen copy of any of the six figures (`grep -rnE '\b(113|114|379|380|274)\b' tests/lint tests/domain` and by name) and report each: folded into the roster, or provably a different population.

## FENCES
No `src/` change. No register act (record deltas: lighting titles if any `it()` text moves — keep titles unchanged; ratchet totalFiles +0 unless the roster is a test file — make it a fixture, not a `*.test.js`). No change to any frozen VALUE. Commit per step, `Seat: Opus 5 — Fable-unvalidated`, `Lane: PANTHEON-ROSTER`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-pantheon-roster.md`, PARTIAL header first, RETROVALIDATION ROW last, tip sha last.
