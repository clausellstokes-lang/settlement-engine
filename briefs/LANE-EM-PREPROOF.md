# LANE-EM-PREPROOF — the shared brief for an Opus PRE-PROOF lane (given 2026-09-19 ~15:1x EDT by the successor chair, session 7d3418f8)

**Seat:** Opus PRE-PROOF. The charter's law: "each packet is written to READY by an Opus pre-proof lane that verifies every verified-fact row against the tree (file:line), fills the change manifest and required symbols, and runs the sealed dispatch; the chair validates and cuts." You re-measure ONE already-compiled DRAFT packet against the CURRENT tip and hand the chair a packet it can promote without editing a fact. You implement nothing and run no gate. The shared compile brief `briefs/LANE-EM-COMPILE.md` (chair kit) binds whole — its path law, "navigate by symbol", "a fact without a command is not verified", "never adjudicate: a refuted premise is BLOCKED with the smallest measured contradiction" — with the substitutions below.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · kit = `$SP/chair-kit-923472dc` (READ-ONLY).

**The tree you read:** ONLY `$SP/read-tip-a41a0e109` (detached at `a41a0e109`, the build branch's tip; `node_modules` symlinked so `node -e` works; confirm `rev-parse --short HEAD` first). Your packet's DRAFT, its manifest entry (in `docs/implementation/PACKET_MANIFEST.json`) and its evidence file are IN that tree under `docs/implementation/packets/settlement-editor/`. **You write ONLY** under `$SP/lane-preproof-<ID>-scratch/` (create it): copy the packet, extract its manifest entry to `<ID>.manifest.json`, copy the evidence file, and edit the COPIES. FORBIDDEN: editing/staging/committing anywhere; `$SP/consist`, `$SP/lane-em-b3b`, other lane dirs; the ledger checkout's working files (read the ledger only via `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:<path>`); vitest, eslint --fix, every npm script.

## What "READY-able" means here — do all of it
1. **Every verified fact re-found BY SYMBOL at a41a0e109**, with the command and its output appended to the evidence copy (new numbered sections; never rewrite the old ones). Line numbers in the packet are refreshed to the tip. A fact that no longer holds = BLOCKED, smallest contradiction first.
2. **The J-T1 window:** `git -C $SP/read-tip-a41a0e109 diff --stat <packet's verified base> a41a0e109 -- <every change-manifest path and every requiredSymbols path>`; quote it. Every CREATE target absent; every requiredSymbols `symbol` present VERBATIM (`grep -c` → ≥1, say the count); by the standard, a symbol the deliverable CALLS and must find unchanged is a required symbol — add any that are owed, remove none silently.
3. **No stale absolutes (the chair's R11 rule).** Any lighting-census tuple, any other register figure, any "N of M" the packet quotes as an absolute at its old base is rewritten as the DELTA this packet causes, derived from its own CREATE/TEST rows and explained; the absolute is "stamped by the chair at promotion from the live baseline". Keep the red's SHAPE as a marked placeholder.
4. **Registration obligations priced, against the tip:** the mutation-coverage row if it CREATEs under an enforcer dir (⚠ `scripts/mutation-coverage-manifest.json` is held by whichever non-terminal packet names it — say whether yours does); the lighting delta; observed-shape / writer-reach movement if it adds a domain reader of a settlement field (say which instrument, shrink or mint); prose-numerics if it renders a number.
5. **⭐ THE BUNDLE BUDGETS, PRICED (new law: charter amendment of 2026-09-19, ODQ §934.19 addendum 2).** For every `src/` path the packet modifies or creates, MEASURE which emitted chunks it lands in — the generation worker (`tests/build/generationWorkerLazy.test.js`, `WORKER_BUNDLE_CEILING_BYTES`, EXACT — zero slack), the lazy engine (`tests/build/vendorPdfLazy.test.js`, `< 679_000`, ~870 B of margin today), the first-paint closure (`EAGER_FIRST_PAINT_MODULES` in `vite.config.js` and its budget test), or none. Use the repo's own closure tooling and import graph; say how you measured. If it lands in a ZERO-SLACK or near-full budget, the packet must CARRY the obligation: add the budget's test file(s) as TEST rows in the change manifest with a stated byte bound (your estimate of the minified growth ×2, stated as such), and write the build lane's step: a REAL `npm run build` through the exclusive mutex; the kit's per-module attribution (`tools/attrib.config.mjs` — per-module RENDERED lengths only; it reads main-graph chunk sizes 742 B low) showing ONLY this packet's own modules moved; growth inside the bound; then re-mint in the form `91d5f155b` used (worker: the constant to the exact measurement + a dated attribution comment; engine: the next round thousand keeping ~700 B) — else STOP for the chair. If it lands in no budgeted chunk, say so with the measurement.
6. **The header, made promotable but NOT promoted:** Status stays DRAFT; Packet version +1 with a one-paragraph "what this version changed and why"; Verified base and Last revalidated left for the chair (write the revalidation SENTENCE the chair will use, with the measured window facts, and a `__BASE__` placeholder); the preamble line's SHA-256 left as the chair's stamp (but VERIFY the preamble's current hash: `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` and quote it). `Depends on` re-checked against what has LANDED (EM-B3b, EM-P0 are LANDED; EM-P2 is STALE).
7. **Budget re-measured** (files, effective lines per leaf against 250, packet total against 400) wherever step 5 or a refreshed fact changed the plan. Over = STOP AND SPLIT with the proposed line.
8. **A dry read of the sealed dispatch's checks** (`scripts/implementation-session.mjs`: branch name, ancestry, substrate unchanged since the verified base, CREATE targets absent and git-clean): say, check by check, that your packet would pass at a41a0e109 once the chair sets the base to the tip.

9. **Cite the design and the charter by SECTION + ITEM, never by line number** (both documents move several times a day; a line citation went stale inside one pre-proof), and RE-RUN the J-T1 window in the same command that reads the tip — the branch moves under a lane.

10. **SIMULATE THE POST-EDIT VALIDATION (from EM-P3's build, 2026-09-19).** `requiredSymbols` is asserted VERBATIM at every status, including after the packet's own edits: for every row, ask whether the symbol text will still be present at that path once the packet is built. A symbol the packet MOVES, RENAMES or DELETES at a path is a `retiredSymbols` row (and its successor joins `requiredSymbols` only at the LANDED flip) — or the packet's contract must keep the verbatim text (a forwarding `export const X = …`). Check other LANDED packets' rows for the same (path, symbol) pair: a retirement must discharge them. Every check before `check:packet` runs against the PRE-edit tree, so nothing else will catch this.

## Report (final message; also `<scratch>/<ID>.preproof.report.md`)
READY-able or BLOCKED; the three file paths; the J-T1 window quoted; facts changed (old → new) with the proving commands; the requiredSymbols delta; step 5's chunk measurement and what you added because of it; the budget table; every question only the chair can answer. Every claim CONFIRMED (quoted command) or PLAUSIBLE.

11. **RUN THE PACKET'S OWN `checks` IN YOUR HEAD AND, WHERE A GENERATOR IS AMONG THEM, IN A THROWAWAY COPY (from EM-B1d's build STOP, 2026-09-19).** Every path a declared command WRITES is a change-manifest row. The one that bit: `npm run build:edge-shared` re-stamps `generatedAt` in ALL FIVE `supabase/functions/_shared/*.meta.json` in one build window (`tests/edgeFunctions/edgeSharedBundleReproducibility.test.js` pins the single window), so a packet that owes the edge-shared rebuild owes SEVEN generated paths when two bundles move (the two bundles, their two metas, and the three siblings' re-stamped metas) — and because the packet's sealed `checks` run that generator, a manifest that names fewer cannot pass its own seal (`sealed foreign work drifted`). You may not run npm scripts; establish the written set by reading the generator (`scripts/` — find what `build:edge-shared` runs) and from the precedent landings (`git log --stat -- supabase/functions/_shared | head`), and say which you did.
12. **SWEEP `tests/` AS WELL AS `src/`** for every symbol the packet changes and every literal it adds or retires: an EXACT PIN on a constant you widen (EM-B1d: `tests/generators/densityLaw.test.js` pinned `ROSTER_ABSENT_STATUSES` both directions) or a FIXTURE spelling a word you retire (`tests/domain/espionageMission.test.js` fixtured `status: 'imprisoned'`) is a declared TEST path with its one-token cure — found here, never at the build. Helper DEFAULT ARGUMENTS hide such fixtures from a `status:` grep; grep the literal itself. And ask the question EM-B1d's packet did not: when you add a member to a named set, read what that set's CONSEQUENCE is where it is consumed (a reversible status joined an irreversible consequence) — a set's name is a claim about its meaning, its consumers are the meaning.
13. **LINE-ADDRESSED BASELINES (from the FIX-F lane's finding, 2026-09-19).** `tests/lint/proseNumerics.test.js` allows rows by FILE + LINE; a packet that adds or removes lines ABOVE a baselined row in a file it modifies reds that walker's exact-identity arm at the landing, and the baseline file is outside the packet's manifest. For every `src/` file you MODIFY, list the prose-numerics baseline rows it carries (grep the baseline for the path) and say whether your edit sits above any: if so the packet's §12 receipt must QUOTE the convicted rows, and its train's terminal owes the re-address (CURE-B's shape). Check the same for any other register that addresses by line (say which you found).
14. **THE ORDER OF `checks`, AND WHAT A TITLE IS WORTH (from EM-B1d version 5's second STOP, 2026-09-19).** (a) A GENERATOR among a packet's `checks` (`npm run build:edge-shared` above all) goes LAST: it re-stamps declared paths mid-chain, the packet gate correctly invalidates every step after it, and `check:packet` can never reach them — EM-B1d's `focused-9..13` read `exit null, 0 ms`. (b) A new `it` is a TITLE in the lighting census ONLY if its file is CREDITED: the census sums `it` titles over CREDITED files only, and a PARKED file's titles count nowhere (EM-B1d priced a fifth title for an `it` added to `tests/generators/densityLaw.test.js`, a long-parked file, and measured `+4`). Before pricing `+N titles` for an edit to an EXISTING test file, run the walker's own `parkReasonsFor` over it (read it in `tests/lint/sovereigntyLightingContract.walker.test.js`; its reasons include an unresolved opener and registration that is not straight-line -- nesting, `.each`, loops, conditionals) and say what it prints; a NEW test file registers straight-line literal `it`s under ONE literal `describe`, or its promised `credited +1` lands as `parked +1`. CORRECTION (the chair, 2026-09-19 19:5x): an earlier text of this step said those files 'use vitest GLOBALS'. That was a build lane's unverified guess relayed as fact and it is FALSE -- 0 of 2,649 test files lack the `from 'vitest'` import. The rule stands; the cause of each parked file is whatever `parkReasonsFor` prints, never a guess. MEASURED the same evening by the cure lane: EM-B1e's file parked as `OPENER_UNRESOLVED:it` because `it` was bound a SECOND time -- an arrow PARAMETER named `it` (`settlements.map((it) => ...)`) copied from an existing suite -- and the census resolves an opener only where the module binds that word EXACTLY ONCE. So: in a test file never name a variable or parameter `it`, `test` or `describe`; 26 of the estate's 384 parked files park for that reason alone.
15. **STAMPED FILES (from the same build's `tests/lint` whole run).** `docs/content/wiring-census.json` stamps the bytes of producer files (`stamp.files` shas); a packet that MODIFIES a stamped file reds `tests/lint/proseWiringCensus.walker.test.js` (`stale-bytes`) at its landing unless it carries the plain re-take — EM-P3 moved `src/generators/steps/resolveConfig.js` and left that walker red on the integration tip for a day. For every `src/` file you modify: is it named in the census's `stamp.files`? Then the census JSON is a change-manifest row and the build lane runs the plain re-take (`node scripts/wiring-census.mjs`), lawful ONLY when `git diff -U0` on the JSON is confined to the `stamp` object (a moved ROW is the chair's).
16. **CITATIONS BY LINE (from CURE-D's stop, 2026-09-19).** Some `src/` files cite OTHER files by `path:line` — `src/domain/prose/holderTable.js` carries `cite: 'src/generators/steps/resolveConfig.js:195'`-style producer citations that `tests/lint/proseWiringCensus.walker.test.js` reads live, so a packet that inserts or removes lines ABOVE a cited line in the cited file reds that walker and owes the citation's re-address (addresses only) IN ITS OWN MANIFEST, together with the census re-take — EM-P3 shifted three cited lines and left the branch red for a day. For every `src/` file whose lines you shift: `git grep -n "<its path>:[0-9]"` across `src/`, `docs/content/` and `tests/`; every hit below your insertion point is a change-manifest row (the citing file) with the exact new address.

> ADDENDUM 2026-09-20 (the chair, after run 19 — runs 17, 18 and 19 were ONE family: a lane's new or renamed test file was read by a walker the lane never ran): a CREATE or RENAME of a test file under `tests/<dir>` opts into EVERY walker that governs `tests/<dir>`. Run that directory WHOLE once in your instruments (`tests/security` whole for a security file; `tests/lint` whole always); read the headers of the walkers governing the target directory for fixture-shape rules BEFORE drafting fixtures (the SS4 meta walker reads every quoted `NNN_x.sql` in `tests/security` as a migration reference); a synthetic literal shaped like a real artifact name is declared with the walker's executed marker, never computed past its regex.

> FIXTURE LAW FOR `tests/security` (the chair, 2026-09-20, CURE-I): a fixture that spells a migration-shaped name (`NNN_anything.sql`) carries `// synthetic-migration-name: <why this names no migration>` on its own line or the ONE line immediately above — the SS4 meta walker excludes a declared literal from its existence check AND asserts it absent, so the marker can hide nothing; an undeclared fixture reds the terminal (run 19).

> THE STATUS ROW (the chair, 2026-09-20, from EM-R0d's pre-proof): `parsePacketHeader` anchors `- **Status:** \`DRAFT\`` at END-OF-LINE — the value stands ALONE on its line (whitespace only after the closing backtick), and `status` is taken only when exactly one row matches. A stamp appended to that line (`BLOCKED …`, `READY-able …`) parses as `status: null` and fails validation at placement for a reason unrelated to content. Put every stamp on the CONTINUATION lines beneath the Status row, never on it; the chair's placement scripts write the Status value alone.

> GREP LAW (the chair, 2026-09-20, from EM-B1f's pre-proof): `git grep -E` silently ignores `\s` (no error, no match) — a population counted with `\s` under `-E` is undercounted. Use a POSIX class (`[[:space:]]`) or `git grep -P`; when a grep count contradicts a claim, re-run it with the other engine before calling the claim refuted.

> THREE LAWS (the chair, 2026-09-20, from EM-R0c's and EM-P2's reports): (1) SEED LOOPS — a `tests/domain` (or any `tests/`) CREATE never writes a bare `for (const seed of …) { expect(…) }`: `seedLoopTotality` convicts it and its habitat is shrink-only; collect (`for (const c of rows) …`) then assert once. (2) THE PREAMBLE HASH IS MEASURED, NEVER QUOTED — compute `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` at your read tip and cite THAT; a hash a brief quotes is stale the day the next amendment lands. (3) A HARNESS PRINTS THE TREE IT READ from its environment, never a literal label; and every unquoted shell expansion is a false-green risk (a `set -- $pair` in a verification loop printed UNCHANGED while `shasum` errored) — quote everything.
