# Brief — SEAM train, car 5b: THE HOLDER CENSUS — every fact's in-world source, derived and resolved, printed and asserted; zero text

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 5b. Dock: `$SC/laneSEAM` after car 5. Read first: `$SC/s12-sitting/SITTING-RULINGS-912.md` **§Q** (the ruling — this car is Q.4 step 1), `$SC/prose-research/REGISTER-CARD.md` amendment S3, `$SC/prose-research/sweep/MOVE-GRAMMAR.md` §4.4.3, the census module and JSON at the tip (`src/domain/prose/wiringCensus.js`, `scripts/wiring-census.mjs`, `docs/content/wiring-census.json` — the producer index from MEASURE car 3), `src/domain/institutions/institutionTable.js` (holders, capture, corruption, control as it derives them), the MEASURE receipts §§ CAR 0, 3.

## What to build
1. **A frozen HOLDER TABLE** (`src/domain/prose/holderTable.js`, in the island): producing system / field root → record-holder KIND from a closed list (treasury · muster · census · parish · toll-bar · market · watch · court · elders · tradition · road · the office itself) with the evidence for each mapping cited to the producer (file:line); a field with no mapping is SOURCE-UNRESOLVED, never guessed.
2. **The `source` column on every census row:** `{ kind, holder, standing }` where `holder` is THIS town's institution for that kind from the institution table (or `null` with the reason: no such institution in the roster; the office itself), and `standing` the typed facts a DM face may read (capture state, corruption, faction control — from the fields the engine holds; never inferred). Printed per field and per kind: LICENSED (holder resolved) / OFFICE (the office's own books) / SOURCE-UNRESOLVED counts; the list of holder kinds with no institution behind them in the shipped roster (the wiring debt the WAVE inherits); how many interested facts (a holder that is a power: captured, corrupt, or controlled) the RATE corpus shows per tier.
3. **Arms:** every mapping row convicts a fixture (a producer with no mapping → UNRESOLVED; a mapped kind with no institution → `holder: null` with the reason; a captured holder → `standing.captured`); the counts asserted as integers; a plant that maps every field to the office must red (the office may not be the source of the muster); the JSON `--check` interlock covers the new column; the island fence (thirteen modules now) green.
4. **Receipt:** the holder table with its citations; the counts; the per-tier interested-fact figures; what a cited face would need that the engine does not hold (the WAVE's list).

## Discipline, fences, commits — as car 3a's
Runner count in its own shell; focused one at a time; one whole `tests/lint`; strict on its ceiling (write strict-clean); voice E2; OSR/PN exact; typecheck; the cast walkers; no `toFixed`/em dash/bang in `src/`; never the whole suite; never a build; no register `--write` but the lighting door (5b-b). Change only the island (`holderTable.js` new, `wiringCensus.js`), `scripts/wiring-census.mjs`, `docs/content/wiring-census.json`, `tests/lint/proseWiringCensus.walker.test.js`, fixtures, the sweep + manifest by text, the receipt. NO composer, leaf, persisted shape or seed input. Commit `SEAM car 5b: the holder census — every fact's source derived and resolved, <counts>`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha(s); the counts LICENSED / OFFICE / UNRESOLVED; the kinds with no institution; the interested facts per tier; anything refused with its measurement.
