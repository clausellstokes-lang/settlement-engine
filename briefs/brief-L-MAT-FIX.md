# LANE: L-MAT-FIX — the thirteen cures the skeptic fold owes the L-MAT consist, BEFORE its proof and gate
⟦Chair: Fable 5.1 (session 8de5f153, drafted 2026-09-07 ~12:15 from `$SC/skeptic-912/FOLD.md`, the six-lens Opus skeptic pass `wf_a4deb4b0-511`) · Lane: Opus 5 (`model: "opus"`) · implementation lane, product bytes · dock: `$SC/laneLMAT` at its CURRENT tip `7d96e2b72` (six cars over the product tip 3b1c0eaa5) — continue from the tip; never cut a new dock⟧
SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad

## THE CHARTER, IN ONE BREATH
The L-MAT consist (cars 1–5b, receipt `$SC/receipt-l-mat.md`) is real work and every arithmetic figure in it reproduces. The skeptic fold found that the lane reached true conclusions through FALSE MECHANISMS and shipped the mechanisms: the roster is inert — but not because of the dial (the gate is `materializesLivingContent(settlement.config)`, the WORLD'S CONFIG, and nothing calls the roster loader, so lighting the dial takes generation DOWN); the store config IS hydrated from a saved settlement (`SettlementsPanel.jsx:102-103`) in three places that now say otherwise in shipped bytes; one test arm cannot fail; and THREE security holes are LIVE AT DIAL 1 through the import boundaries the lane never enumerated. This lane lands the thirteen cures the fold names — none needs a migration, none touches a golden — as THREE cars on top of `7d96e2b72`, then a whole-consist re-derivation of every claim the cars make. Read `$SC/skeptic-912/FOLD.md` IN FULL first (33 KB), then the six lens files it folds (`promise.md`, `security.md`, `import.md`, `walker.md`, `build.md`, `refusals.md` in the same directory — the executed probes are there), then `$SC/receipt-l-mat.md` and `$SC/briefs/brief-L-MAT.md`. Every file:line below is the fold's reading at `7d96e2b72` — RE-DERIVE each before editing (a cited line is a hypothesis).

## ⭐ CHAIR RULINGS (JUDGMENT, each vetoable by the owner; recorded in the §912 row)
- **R-F. DEF-3's PAIRING: cure BOTH records on the reconciliation path.** The roster AND `customContentProvenance` are remapped-or-dropped there by the two existing remappers (the lane's own argument that two records governed alike everywhere else must not disagree on one path — the better one). The inertness excuse is gone: the fold put a foreign roster into persistence through that path with no archive and no dial.
- **R-G. The DM-FULL drop's CLIENT HALF lands now; the SQL twin stays the owner's.** In `publicSafe.js`, `delete clone.customContentRoster` and `delete clone.customContentProvenance` beside the existing `delete clone.dmNotes` in the full path (defense in depth; no migration; nothing reds). The server half (`supabase/migrations/NNN_*.sql` re-issuing `_gallery_dm_full_json`) is a migration and stays on the owner's desk — write that into the DM-full arm's message. If the full path is meant to carry the roster to the DM's OWN other device (read the share model before editing: who receives a full share?), REFUSE with the measurement and say which reader would lose what.
- **R-H. DEF-5: a row whose `localUid` resolves and which carries no `customDefinitionId` is RESOLVED, not identity-incomplete** — that is the population R-B exists to protect. Add the fixture row the fold names so the arm can see the difference; keep the drop-with-warning for a genuinely unresolved row.
- **R-I. Car 6 / R-C stays REFUSED and UNBUILT in this lane.** Record in the receipt the fold's four options (wide · narrow · `{ lawVersion }` alone · re-cut the F2c escape filter), the refuted ground (the product's `makeContentRevision` is content-addressed, so the narrow digest is NOT blind in the product — the blindness belonged to the fixture), and the fact that outranks all four (`buildSettlementContentProvenance` returns `null` for a roster-only world, so no receipt exists to carry the marker). The chair's recommendation for the successor car is option 3; SHIPPING any key onto the hash-validated receipt is owner-gated. No bytes.
- **R-J. The generation OUTAGE is the consist's most consequential unrecorded fact and is RECORDED, not cured.** `loadLivingContentRoster` has no caller (`livingContentSeam.js:92` definition; a comment at `:64`); a lit config throws `[livingContentSeam] v2 world, roster payload not loaded`. It goes into `livingContentRoster.js`'s header, the wiring file's docblock, the DM-full arm's failure message, and a DEFERRED row in the receipt. It is the TRUE ground of every inertness claim; the dial is not.

## THE CARS (three, in this order; each with its focused proofs before the next begins)
**CAR 7 — BEHAVIOUR (DEF-1 · DEF-2 · DEF-3 · DEF-4 · DEF-5).**
- DEF-1 `src/store/galleryImportSettlement.js:73` + `src/lib/importScrub.js:38-45`: delete `customContentRoster` and `customContentProvenance` on the ingested settlement beside the existing scrub (the importer has no identity map — a DROP is the honest act), and add `_livingContentLawVersion` to `scrubImportedConfig`'s destructured drop list. One anchored arm (`expectAbsentWithAnchor`) per dropped key, in the importScrub / galleryImport test files — the negative-assertion per-file budgets are EXACT (`tests/lint/negativeAssertionAnchor.walker.test.js`: importScrub.test.js 1 · accountImport.test.js 6 · accountImportSlice.test.js 9): read the walker's rule before adding any bare negative; the helper is the anchored form.
- DEF-2 `src/domain/density/densityCreateBoundary.js:181-187`: clamp in `birthConfig` — destructure `LIVING_CONTENT_LAW_CONFIG_KEY` off the incoming config BEFORE spreading the mint, so a birth's law is the dial's law on EVERY path. At dial 1 this writes not one config byte (the key is simply absent) — PROVE it: the same-seed control and every dark arm in `livingContentLawWiring.test.js` must pass unchanged, and add one arm that feeds a LIT marker through the wizard-form path (`SettlementsPanel.jsx:102-103` → `configSlice` `key.startsWith('_')` → `birthConfig`) and asserts the born world carries NO marker and NO roster. Reproduce the fold's `probe-chain.mjs` (in `$SC/skeptic-912/`) as the negative control BEFORE the clamp (a roster minted on a dark build) and AFTER (none).
- DEF-3 `src/store/importReconciliationAdmission.js:570` → `importReconciliationExecution.js:53`: call `remapAccountSettlementLivingContentRoster` AND the provenance remapper on that path (R-F); one round-trip arm with a foreign roster + a foreign provenance receipt entering through reconciliation, asserting drop-or-remap on both.
- DEF-4 `src/store/accountImport.js:390-400` + `src/store/accountImportBody.js:467-477`: run the same remap-or-drop over every `versionHistory[i].settlement` (roster AND provenance) inside the existing car-5 loop; one arm: import → `revertToSnapshotAction` → the persisted roster is the destination's or absent, never the source's (`"src-secret-def"` must not survive).
- DEF-5 `src/lib/accountSettlementContentPortability.js:262-397` per R-H; the fold's P2/P2b probe as the control.
- Also DEF-2's chain closes the fold's deferral 4 (the sample fork) and the Library-Load leak — say so in the receipt, with the arm that proves it.

**CAR 8 — INSTRUMENTS (DEF-6 · DEF-7 · DEF-8 · DEF-9 · R-G).**
- DEF-6 `tests/lint/densityCreateBoundary.walker.test.js:503-505`: delete the tautological loop (`scanned` is built from `Object.keys(PIPELINE_REACHERS)` two statements above); keep the `:506-509` `toContain('src/store/settlementGenerateAction.js')` assertion; fix the `:513-514` stray message so it names what matched (mint vs dial).
- DEF-7 `tests/security/livingContentRosterPublicDrop.test.js:138-143`: derive `rosterKeys` from the real roster the file already generates two arms above (`livingContentRoster.js:166-172` copies 36 manifest-declared fields the hand literal misses); the `toBeGreaterThan(6)` becomes a count read off the real row.
- DEF-8 `tests/domain/livingContentLawWiring.test.js:221-253` (the ⛔ STOP arm): add the positive control (assert the regen actually changed the world) and keep the marker check; AND re-point the receipt's claim at the walker's source-text arm (`/settlement\.config \|\| config/` present, `/\bconfig \|\| settlement\.config/` absent) — the instrument that can actually fail. An arm that passes with and without the defect it names is not a proof.
- DEF-9 the same file's arms 2/3: convert `expect(KEY in pub).toBe(false)` to `expectAbsentWithAnchor(pub, 'config', …)` (the helper already imported), so `negativeAssertionAnchor.walker` can see them.
- R-G the client half of the DM-full drop in `publicSafe.js` (see the ruling; refuse with measurement if the share model contradicts it), with the DM-full arm in `livingContentRosterPublicDrop.test.js` re-cut to assert the DROP (default AND full) and to carry the SQL-twin sentence for the owner.
- L4's untaken plant (fold U1): plant `newSettlementLivingContentLaw` OFF the boundary (in a throwaway copy or with md5-verified `cp` backups, restored by `cp`, never `git checkout`) and show the `:251`/`:280` arms RED on it and GREEN restored — the cover for the new mint is measured, not reasoned. Record both digests.

**CAR 9 — TEXT (DEF-10 · DEF-11 · DEF-12 · DEF-13 · R-I · R-J).**
- DEF-10 `densityCreateBoundary.js:289` and `:326` (inside the executable `GENERATION_LAWS.livingContent` `why` string a walker reads): "every emitted dist file stayed byte-identical" → "no emitted dist file changed SIZE" (658 of 1,377 changed bytes; 0 changed size). The `442c7f988` commit message cannot be amended (the register was taken) — record the correction in the receipt.
- DEF-11 `densityCreateBoundary.js:48`, `tests/domain/livingContentLawWiring.test.js:299-300`, `src/store/settlementGenerateAction.js:117-120`: replace "nothing anywhere hydrates it from a saved settlement" with what is true — the Library's Load DOES hydrate the wizard form from `settlement._config` (`SettlementsPanel.jsx:102-103`, also reached from `SettlementDetail.jsx:605`), and DEF-2's clamp is why it is harmless.
- DEF-12 `tests/security/livingContentRosterPublicDrop.test.js:12`: the key has exactly one reader, the account-import remap (`livingContentRoster.js:37` names it) — one sentence.
- DEF-13 the same file `:176` and `:184`: re-anchor the premise and the failure message on the real barrier (R-J) — the lighting engineer will meet a total generation outage before any leak.
- R-I and R-J written where the brief says; plus the fold's X-corrections that are text: X8 (the writer-dark-register roster row has a SECOND assignment site at `accountImportBody.js:474` that `writerReach.walker` never reads — record it in the row's comment and in the receipt as a walker limitation, do not widen the walker in this lane), X12 (the `MINT_HOMES` deletion landed in car 1, not car 3 — receipt correction), X13 (the build-A timestamp typo).

**THE CONSIST PROOF at the composed tip (after car 9):** `npm run typecheck:ratchet` (ceiling 173 — if a car ships a type error, FIX THE SOURCE, never the baseline); eslint on every touched file; the focused files ONE AT A TIME with exits captured in-shell before any pipe: livingContentLawWiring, livingContentRosterPublicDrop, densityCreateBoundary.walker, accountSettlementContentPortability, accountImportSlice, accountImport, importScrub, galleryImport (find its test), negativeAssertionAnchor.walker, writerReach.walker, observedShapeReaders.walker, engineChunkLazy, livingContentSeamLazy, layerBoundaries; the negative-assertion per-file budgets EXACT; NO whole-suite run (the chair's proof and gate do that); NO build unless a car touches an import edge (car 7's DEF-2 is a body edit inside an existing function — measure whether the emitted lazy chunk changed size, by ONE build if you judge it needed, and report the first-paint closure against 1,042,086 raw).

## LAWS
- STATE, NEVER FATE. The dial stays at 1. No tuning value, no golden re-record (the same-seed control is a live CONTROL), no register `--write`, no signature, no `supabase/applied-head.json` byte, no migration file, no `ARCHITECTURE.md` edit.
- THE PROMISE re-proven by EXECUTION across create / read / persist / regenerate / undo / clone / import (account, gallery, reconciliation) / DM share / public share — the fold's lifecycle list is the trace; each path gets a sentence in the receipt naming the arm that proves it.
- Read the whole enclosing scope and every caller before editing; re-derive every line number above. Commits in the dock with EXPLICIT paths only (never `git add -A`/`-u`/`.`), never stash, never push, never touch the main tree or any other dock; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: L-MAT-FIX`. Every timestamp from `date`; every exit captured in-shell before a pipe; `$SC/HOLD-VITEST` checked before every vitest — if it appears, STOP (the chair's proof has begun). Two chair workflows may be running (S12) — they run no vitest.
- Receipt: APPEND to `$SC/receipt-l-mat.md` a section `## L-MAT-FIX (cars 7–9)` written FIRST as PARTIAL, updated after every car (shas; every DEF with its measured before/after; the RETROVALIDATION ROW for R-F…R-J as chair rulings and for every lane judgment; DEFERRED items incl. R-J's outage and the three walker holes the fold names at `settlementSlice.js:45` etc.; CONFIRMED/PLAUSIBLE per figure). If any cure contradicts a measurement — including any of R-F…R-J — REFUSE that item with the measurement and land the rest. The final message: STATUS, car shas, dock tip, porcelain, refusals one line each.
