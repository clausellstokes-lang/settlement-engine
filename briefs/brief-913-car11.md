# Brief — §913 car 11 (lane L-MAT-FIX): the mutation-coverage entry the new security test owes

Seat: Opus 5 — implementer. The chair (Fable) rules; you measure and build. Receipt first, then the car.

## Where
- Dock: `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad/laneLMAT` (a detached-HEAD git worktree; run `git log --format='%h %s' -3` first and record the tip you start from — it is the §913 register car 3 or later).
- The kit scratchpad is `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad`. Write your receipt to `$SC/receipt-913-car11.md` BEFORE you commit, and update it after.
- ⛔ Never touch the main tree `/Users/cstokes/Desktop/settlement-engine` (it is checked out on the ledger branch; its src is frozen). Work only in the dock.

## The red you are curing (measured by the chair in the §913 whole-suite proof, `$SC/whole-913.log`)
`tests/lint/mutationCoverageManifest.test.js` — arm "TOTALITY: every enumerated invariant file has a manifest entry" — reds because the consist added `tests/security/livingContentRosterPublicDrop.test.js` (car 5 of L-MAT) and `scripts/mutation-coverage-manifest.json` has no `invariants` entry for it. The test's own guidance (read its header, lines 1–45): add a `kind:'mutation'` entry backed by a planted regression in `scripts/mutation-sweep.sh`, or a `kind:'rationale'` entry with a written reason. Do NOT add it as `uncovered` — the baseline (186) only shrinks.

## What to build — a real plant, `kind: "mutation"` (preferred), rationale only if measurement defeats every plant
1. Read `tests/security/livingContentRosterPublicDrop.test.js` whole (284 lines) and `src/domain/display/publicSafe.js` whole. The test's six arms are titled at lines 116, 128, 155, 166, 218, 234. The ruling the file holds: `settlement.customContentRoster` (and the provenance receipt) never reach a public projection — dropped by the fail-closed root allowlist `PUBLIC_TOPLEVEL_KEYS` in the default projection, and by an explicit delete in the `full: true` DM-share projection (§912 R-G).
2. Candidate plants — TEST THESE, DO NOT TRUST THEM (the chair names mechanisms as hypotheses; the lane measures):
   - (i) PREFERRED: add `'customContentRoster'` to `PUBLIC_TOPLEVEL_KEYS` in `publicSafe.js`. Expected: the allowlist arm (line 116) and the ⭐ BEHAVIOURAL arm (line 128) red; the rest green.
   - (ii) strike the full-mode `delete clone.customContentRoster` (or whatever the R-G strip line reads) — expected: only the DM-FULL arm (line 234) reds.
   Choose (i) unless it fails to apply byte-exactly or fails to red. Record the measurement for whichever you ship: `planted => N red of 6 (titles)`, `restored => 6 passed`, verbatim vitest tails.
3. The plant goes into `scripts/mutation-sweep.sh` as the next numbered comment block — the dock's last is `# 73.` so yours is `# 74.` — in the exact idiom of `# 72.` and `# 73.` (a numbered comment stating the law, the failure the plant forecloses, and the measured figures; then ONE `perl -0pi -e '…'` whose anchor exists verbatim at HEAD; then ONE `check_caught "<label>" src/domain/display/publicSafe.js "npx vitest run tests/security/livingContentRosterPublicDrop.test.js" "<exact it() title that must red>"`). Read `check_caught()` at line 134 first: it refuses a plant whose `git diff --quiet` shows no change, demands a byte-identical restore via `git checkout --`, and with a 4th argument counts exactly ONE `FAIL … > <title>` line. Give the 4th argument (it makes the attribution exact); pick the title of the arm that reds under your plant — if two arms red, name one; the count is of lines ending in that title, so one title = one match.
   - Label: unique across the sweep (74 labels exist; `grep -c '^check_caught' scripts/mutation-sweep.sh`), in the `area/what` idiom, e.g. `security/the living-content roster becomes an allowlisted public key`. The label is the JOIN KEY to the manifest.
   - ⚠ Another dock (lane INSTR-912) also appended plants numbered 74–78 to this file; they will be renumbered at that lane's rebase. Do not care about them; do not read that dock.
4. The manifest entry: in `scripts/mutation-coverage-manifest.json` under `invariants`, keyed `"tests/security/livingContentRosterPublicDrop.test.js"`, `{"kind": "mutation", "label": "<your label>"}`, placed in the file's existing alphabetical order among the `tests/security/…` keys (it sorts before `tests/security/mapForkXssChain.test.js`). EDIT BY TEXT; never re-serialize the JSON (a re-serialization is a 5,000-line diff and a rebase hazard). `git diff --numstat` for the manifest must be ≤ 6 changed lines.
5. Measure, in this order, and paste each tail verbatim into the receipt:
   a. Count other runners first: `ps -axo command | grep -E "[v]itest" | grep -v gate-mutex | wc -l` must print 0 before EVERY vitest run (the chair holds the machine; `$SC/HOLD-VITEST` exists and you are EXEMPT for exactly the runs below — no whole-suite run, no other files).
   b. Apply the perl line by hand → `git diff --quiet -- src/domain/display/publicSafe.js; echo $?` must print 1 → `npx vitest run tests/security/livingContentRosterPublicDrop.test.js` → record which titles FAIL.
   c. `git checkout -- src/domain/display/publicSafe.js` → `git status --porcelain` must not list publicSafe.js → re-run the same file → all 6 pass.
   d. `npx vitest run tests/lint/mutationCoverageManifest.test.js` → all arms green (TOTALITY, LABEL JOIN — your label exists exactly once in the sweep and is claimed by exactly one entry — SHRINK-ONLY at 186, well-formed, guard-the-guard).
   e. `sh -n scripts/mutation-sweep.sh` → exit 0, and `[ -s scripts/mutation-sweep.sh ]`.
   f. Optionally run your plant THROUGH the sweep's own function to prove attribution end to end: `MUTATION_SWEEP_ALLOW_DIRTY= bash -c 'source <(sed -n "/^check_caught()/,/^}/p" scripts/mutation-sweep.sh); results=(); PASS=0; FAIL=0; <your perl line>; check_caught "<label>" src/domain/display/publicSafe.js "npx vitest run tests/security/livingContentRosterPublicDrop.test.js" "<title>"; printf "%s\n" "${results[@]}"'` — if the extraction idiom fights you, skip (f) and say so; (b)–(d) are the required proof.
6. Fences: you may change ONLY `scripts/mutation-sweep.sh` and `scripts/mutation-coverage-manifest.json`, plus the receipt in `$SC`. `publicSafe.js` is touched only transiently for the measurement and must be byte-identical at commit. Do not edit the test. Do not touch `supabase/`, `tests/lint/.lighting-census-baseline.json`, `scripts/.writer-reach-baseline.json` or any other register.
7. Commit ONE car in the dock with explicit staging (`git add scripts/mutation-sweep.sh scripts/mutation-coverage-manifest.json` — never `-A`, `-u` or `.`), porcelain empty afterwards, zero untracked files. No push, no stash, no rebase, no amend. Subject: `§913 car 11: the roster drop gets its planted regression — <one clause on what the plant proves>`. Body: what the red was, what the plant strikes, the measured figures (planted => N red of 6 naming the titles; restored => 6 passed; manifest test green; label join proven), and the receipt path. Trailers, exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: L-MAT-FIX
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
8. Return (as your final text): the car sha, the label, the plant's target line (quoted), the measured figures, the receipt path, and anything you could not do. A claim without its executed tail is not a claim.
