# Brief — SEAM train, ARCH cars 3b–3g: ONE DESK EACH THROUGH THE COMPOSER, WITH EMPTY CANDIDATES — zero drift after every stage

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 3b … § CAR 3g (one section per desk). Dock: `$SC/laneSEAM` at car 3a's tip. Read first: ARCH §2.7 (M1 staged per desk, smallest first), §4.1 (the 31 `readStateProse` call sites: general 11 · defense 9 · power 7 · stressors 2 · economy 1 · warFaith 1; `DeskLines` keyed on mount + index; the `useMemo` at the six un-memoised sites), §9's DM-page row (the eight framed blocks: `projectBesideDmField` module-wired on two, convention-held on six — name the route or the component line per block, X-F12), §12 row 3b–3g; the car-3a receipt.

## The order and the rule
Stressors (2 sites) → economy (1) → warFaith (1) → power (7) → defense (9) → general (11). Each stage: route that desk's `readStateProse` calls through `composeStateProse` with its (empty) candidates leaf, keep every desk's one law (a desk maps state to keys and nothing else), key `DeskLines` on mount + index where that desk's tab renders it (`tabs/EconomicsGlance.jsx:162`, `tabs/WarFaithDesk.jsx:122`, and the general desk's six render-body sites gain a `useMemo` keyed on `(settlement, seed, audience, publicDossier, playerView)` — X-F9 insurance), then run the DRIFT manifest: **drift `[]` after EACH stage, or STOP** — a drift is diagnosed against that one desk's diff, never six. Print each desk's effective line count with its headroom to 800 (the general desk is at 699). ONE commit per stage (`SEAM car 3b: the stressors desk through the composer — zero drift`, …), each with its manifest line in the body; lighting refreezes by ritual as needed.

## Proof per stage
`npx vitest run tests/property/dossierProseManifest.test.js` drift `[]`; the desk's own tests; the whole `tests/lint` once per stage; strict on its ceiling; voice E2; OSR/PN exact; typecheck; the DM-page acceptance for the framed blocks that desk owns (the route or the component line named, so the DM's-pen line stays adjacent). The six leaves sha-identical throughout. NO candidate function is written (empty lists stay empty); NO text moves.

## Discipline, fences, commits — as car 3a's
Change only the six desks, `DeskLines`' two callers, the six general-desk render sites (the `useMemo`), tests, the receipt. Trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return per stage: the sha, the manifest line, the line count, the DM-page acceptance, the tests; anything refused with its measurement.

## ADDENDUM (chair, 2026-09-08 12:2x, after car 3a's verification) — the tip is `84388a185`; three rulings that bind this car
1. **3b-0 comes first, its own commit:** `scripts/wiring-census.mjs:916` stamps the LITERAL sentence `candidateLeaves: 'none at this tip: car 3a lands …'`, which car 3a made false. Turn it into a MEASUREMENT (the sorted list of `src/domain/display/stateProse/*StateProseCandidates.js` found at the tip, each with its sha256 — six today; assert six by name in the walker), then re-take the census by plain `node scripts/wiring-census.mjs` and commit the JSON with the script. **The chair's ruling on the interlock:** a drift confined to `stamp.producerIndexFiles` and `stamp.candidateLeaves` (no census ROW moves; no `stamp.files` sha moves) is a lane's plain re-take at its tip, printed in the receipt with the exact delta (the chair re-measured car 3a's: one integer, 1145 → 1152); a drift that moves a row or a stamped sha stays the chair's (an INSTRUMENT shift at the sitting) — a lane REFUSES it with the measurement. Every later stage of this car that adds a file under the producer trees re-takes the same way, in the stage's own commit.
2. **The coupling walker owes no row.** Car 3a's refusal is accepted: no `LAYER_PATTERNS` family claims `src/domain/display/`, the scope regex excludes it, and the 152-pair inventory may never grow; the composer's fence is the island fence + `tests/lint/composeStateProseFence.test.js`. Do not add a row for a display leaf.
3. **Judgment calls accepted** as car 3a made them (`hashKey` as a kernel export; the three frozen leaves as a defaulted input; a declared `consequence` that cannot take the clause seats rides as an `addition`; `COMPOSITION_BOUNDS` one frozen record). The fact budget reading an absent `reads` as ONE fact stands until car 4 (see its addendum). No 3b–3g stage touches the composer's bounds.
