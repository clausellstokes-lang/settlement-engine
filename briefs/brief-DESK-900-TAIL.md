# LANE: DESK-900-TAIL — the three reds DESK-900-REDS handed up, cured by ruling
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## READ FIRST
`$SC/briefs/_PREAMBLE.md` · `$SC/receipt-desk-900-reds.md` (the attribution table; its two HANDED-UP rows and the `dossierMountRegistry` ARM 3 note) · the memory laws the chair cites below.

## YOUR DOCK
`$SC/laneTAIL`, detached at **`25a7fd0bf`** (the §900 composition, 84 cars over 38474a59e; porcelain 0). The chair replays `laneTAIL:25a7fd0bf`. ⛔ Never rebase; never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content. Vitest through the mutex; never during a chair gate (a sustained 7-worker run that is not yours means wait).

## THE THREE, each RULED — test the mechanism, do not trust it
1. **`tests/copy/voiceMechanics.test.js` Tier-2 per-file debt GREW** in `src/domain/display/labelBands.js` and `generalStateProse.js` (and SEAT-78's `bang 0→1`). REDS measured: the em dashes are PARSED DELIMITERS and label KEYS, not prose (`label.split('—')` at generalStateProse.js:286/:871; map keys equal to the producer's dashed labels; the `!` is `!==` inside a code quotation). RULED: the debt is real by the scanner's definition and the scanner is right — a src/domain file should not spell reader-facing punctuation in code. Cure WITHOUT touching the scanner and byte-identical in behaviour: (a) a delimiter literal is spelled `'—'` (the scanner reads source text; the runtime string is the same glyph) — prove `split('—')` returns the same array on every label; (b) the map keys reference the PRODUCER's own exported label constants (export them from `src/generators/economy/prosperity.js` if they are not already; the walker's `label.startsWith(band)` pin keeps holding) so no dashed literal lives in src/domain; (c) the `!==` quotation is re-worded without the bang or moved out of the string. Prove: the Tier-2 per-file arm reads EQUAL or LOWER than the frozen baseline for every touched file (no rise), the shipped output byte-identical over 360 worlds (a script), eslint 0. ⛔ If any step would change a rendered byte, STOP and report — see the memory law "a cure applied to a structural delimiter".
2. **`treatyOrientationWr10g`'s red is a SHIPPING DEFECT**: `warFaithStateProse.js:424-425` spells the war pair by hand, so a SALE treaty names no role. RULED: route through `treatyOrientationOf` — and because REDS measured this MOVES same-seed rendered text (a `victorId`-only document resolves to `unknown`; `warFaithStateProseDesk.test.js:269`), it is a DECLARED DISPLAY SHIFT under THE PROMISE's text-only class: measure exactly which worlds/documents change and how (a before/after table over the 48-town corpus and the desk suite's fixtures), write the shift entry DRAFT for `docs/GOLDEN_SHIFT_LEDGER.md` in the ledger's own format (the chair signs it at the landing — you do not edit the ledger), and make the desk suite's expectation follow the defect's cure with attribution. If the change moves anything that is not display text (a rules value, a persisted field, a hash input), STOP.
3. **`dossierMountRegistry.walker` ARM 3 is still red**: "A CORPUS READ IS UNGATED, or the publicDossier it is gated on never ARRIVED". REDS notes `WarFaithDesk`'s `publicDossier` default-closed gate has no test for the silent-dark direction. Measure which mount/tab the arm names (run it; read the failure), cure the gate or the prop plumbing at cause, and add the silent-dark test (the gate closed ⇒ no corpus byte reaches the DOM). Coordinate with lane DESK-VISIBILITY (its receipt at `$SC/receipt-desk-visibility.md` when it appears) — do not double-cure a Section-openness defect; this item is the GATE, theirs is the SECTION.

## FENCES
No register act (record deltas). No scanner change. One car per item, `Seat: Opus 5 — Fable-unvalidated`, `Lane: DESK-900-TAIL`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-desk-900-tail.md`, PARTIAL header first, the before/after tables, RETROVALIDATION ROW last, tip sha last.
