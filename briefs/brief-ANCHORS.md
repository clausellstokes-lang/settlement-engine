# LANE: ANCHORS — the twelve mutilated anchors
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · released by the §893 carve-out amendment.

## WHY THIS IS NO LONGER GATED — the gate was never the owner's
These twelve sat behind LEG-7, believed to be an owner ruling. **The §892 retrovalidation walk
proved LEG-7 is a LANE-authored wiring note**: a grep of the ledger returns no owner word. There was
never an owner decision here to protect, so the chair takes it on cost.

## THE ACT
Repair the twelve mutilated anchors and **re-point their pins in the same act** — an anchor repaired
without its pins moved is a worse state than the mutilation, because the pins then cite a target that
no longer reads as they claim.

## ⛔ THE HAZARD THAT WILL BITE YOU
**AN ANNEX HEADING IS A WALKER ANCHOR.** 2,250 of the annexes' em dashes live in HEADINGS that
walkers match by literal `indexOf('\n# TR-1 — …')`. A sed-style sweep SILENTLY RETARGETS those
walkers — the tests keep passing while the walker reads the wrong block. Additionally, 27 annex rows
are BYTE-TWINS of shipping `src/` template literals that `faithKindPools.walker.test.js:168` asserts
equal; those must be cured on BOTH sides in ONE act or the assert reds.

Establish the denominator before you edit: enumerate every anchor, every pin that cites it, and every
walker that matches it by string. Disposition each — repaired, exempt-with-reason, not-applicable —
and report the ledger. A sweep that hits eleven of twelve is a new inconsistency bug, not 92% of a fix.
