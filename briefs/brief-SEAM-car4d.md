# Brief — SEAM train, car 4d: THE SEAT LICENCE PROJECTED — the composer stops reading a primary field the schema never carried (chair, 2026-09-08 15:1x; car 4's open schema row)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 4d. Dock: `$SC/laneSEAM` at the chair's OSR register car over car 4c `802589718` (read `git log --oneline -3`). Read first: the car 4 receipt § CAR 4 whole (its nine judgment calls; the open row: `seatFor` reads `spineMeta.reads[0]` as the spine's PRIMARY FIELD and `PoolMeta` carries no `reads`), `src/domain/display/stateProse/composeStateProse.js` (`seatFor` and every read of `spineMeta`), `scripts/generate-dossier-state-prose.mjs` (the `poolMeta` emission from car 4; `readsCount` from the census), `src/data/dossierRelations.generated.js` (165 rows a→b; `aliases` = the three ratified rows; the join 0 of 165), ARCH §4.4–§4.5 (the clause seat behind a relation row in the spine→modifier direction; S2's guards), SITTING §N.1.

## The ruling to implement
The licence is computed WHERE THE CENSUS IS VISIBLE — at projection — never at render. The projector emits, per pool with `role: 'modifier'` (none today; the emission path is built and exercised on a fixture) and per (spine, modifier) pair the leaf can name, a resolved `seat: 'clause' | 'sentence'` and the relation row id that licensed it, reading the census's `reads` (branch grain), the relations leaf's rows and its `aliases`; a pool whose licence cannot be resolved gets `seat: 'sentence'` with `seatReason: 'no-row'` (printed). `composeStateProse.js`'s `seatFor` reads `poolMeta.seat` and NOTHING else — the `reads[0]` primary-field read is deleted, and a plant that restores it reds (the composer must not carry a field name it cannot license). Today's behaviour is preserved exactly: join 0 ⇒ every seat `sentence`, every joint `addition` — the manifest drift `[]`, the classifier UNCHANGED on 73,284 cells, the leaf diff 0/0/0 (a key added to `poolMeta` only), the SHIFT REGISTER names `seat` as NOT a mechanism until the first `consequence` row is licensed (then it is Shift-class and the register says so).

## Arms
Fixture: a synthetic census row + a synthetic relations row + one ratified alias ⇒ `seat: 'clause'` on the fixture pool; the same with the alias removed ⇒ `sentence` / `no-row`; the composer on the fixture seats the clause exactly where car 3a's clause path expects (the joint from the `consequence.clause` list — empty at the tip, so the composer's WITHHELD path fires and is asserted). The contract test asserts `seat` on every shipped pool is `sentence` with a reason, and that no shipped pool is `role: 'modifier'` yet.

## Discipline, fences, commits — as car 4's
Change only the composer, the projector, the contract test, the composer test, the SHIFT REGISTER JSON (a named non-mechanism row), the receipt. The generated leaves are regenerated by the projector (`--check` green); NO text moves (word-diff of every leaf's text fields prints nothing); the byte ratchets' dry reads (a small delta per leaf declared against its ceiling; none raised); the wiring census under the generalised stamp rule; the OSR: the six leaves' regeneration moves the OSR's INPUT set again — the tool prints "may absorb them; the instrument is NOT darkened" for generated inputs: that shrink-only `--write` is YOURS for this car only (the chair rules it a plain re-freeze on generated inputs, like the lighting census; print the before/after finding count — it must not move). One commit `SEAM car 4d: the seat licence projected — the composer reads poolMeta.seat, never a primary field`; trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha; the seat figures (sentence/no-row counts over 708); the fixture's clause proof; the leaf diff line; the manifest drift; the OSR finding count before/after; whole `tests/lint`, strict, typecheck at your tip; anything refused with its measurement.
