# LANE: ENC-4b — the §B kind (`chance_meeting_exposed`) ships where the approach is KNOWN (traveller × resident), with the words untouched
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · executes `rulings/RULING-ENC4-R1-R2-R9.md` R1 · builds ON TOP of ENC-4's nine cars⟧
READ: `$SC/briefs/_PREAMBLE.md` → the ruling (R1: one row now; §B deferred to THIS car; approacher only where the approach is known) →
ENC-4's receipt (`git show refs/preserve/enc4-receipt-2026-09-05:receipt-enc-4.md`, §F2 "THE §B KIND CANNOT BE WIRED, AND HERE IS THE
SLOT THAT BREAKS") → the sealed words (`refs/preserve/enc5-words-2026-09-04:RECEIPT_POOLS_CHANCE_MEETING.md` §B) → ENC-4's cars in your
dock (the registry family, the walker, the writer, `heraldSeedsOf`).
DOCK: `$SC/laneENC4B` at **`7be568047`** (ENC-4's tip; if the directory is missing, wait fifteen seconds — the chair is creating it).
THE ACT (one car, two if the annex rule needs its own): (1) `heraldSeedsOf` gains `approacherNid` ONLY for the traveller × resident kind
(the traveller approaches the resident's venue — derive that from the stage's own `kind` and the errand/transit that put the traveller
there; prove it from the stage's data, not from the census order); for traveller × traveller the seed carries NO approacher and §B is
not offered — the recorded row speaks. (2) Register §B (`chance_meeting_exposed`) in the `CHANCE_MEETING` family on its exact `events`
row for the approacher-known kind; the writer fills `{npc}` = the approacher and `{counterpart}` = the approached; the annex gains the
one-sentence rule "§B speaks only where the approach is known" beside the pool (no pool sentence changes). (3) The walker's totality
arms: `chooserTotality` DEFER → the kind is now ROUTED (re-derive `DEFER_CEILING` — it may drop back 36 → 35; lower it only if the
row's own accounting says so); `kindPoolFloors` figures move by exactly one kind (predict, then measure); `HBF-42` from DEFER to its
routed state per the registry's own vocabulary. (4) Proofs: the lit arm with the flag forced — a traveller × resident meeting emits
the §B line with the RIGHT parties (a fixture where the approacher is the traveller, and its mirror, asserting the sentence names them
in the right roles — the wrong-ROLE law); the dark arm byte-identical; the walkers; `tests/lint/` whole. Quiet-window law + mutex
before any vitest (a chair gate may be running); if the window never opens, do every node-reachable proof and report vitest as OWED.
FENCES: no word changed; no flag flip; no register act; no `--amend`; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: ENC-4b`.
Receipt `$SC/receipt-enc-4b.md` with a RETROVALIDATION ROW.
