# Brief — MEASURE train, ARCH car 0: THE CENSUS EXTENDED — every number the composed-prose wave is sized from, asserted, per tier, on a balanced sample, committed as data

Seat: Opus 5 — implementer. The chair (Fable) rules; you build and measure. Receipt first: `$SC/receipt-measure.md` § CAR 0. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`.

## Where
- Dock: `$SC/laneMEASURE` — a fresh detached worktree the chair cuts at the §914 product tip (the chair fills the sha here: 8522a17b20febb07ebe16bc9d739b9ebc54411c7 (short 8522a17b2 — the §914 CAS, `landing-instr-2026-09-08`)). The instruments (`src/domain/prose/*`, `src/domain/institutions/institutionTable.js`, the six `tests/lint` walkers, the helpers and fixtures) are IN the product tree at that tip. node_modules symlinked; no vite build, no edge-bundle build.
- Read first, whole and in this order: (1) `$SC/arch-prose/ARCH-COMPOSED-PROSE-v2.md` §3 (the census as built and what car 0 adds), §3.2–§3.6, §4.4 (the fact budget over `tests`; the occurrence bound as a Wilson lower bound), §5.2 (the relation table's four sources with direction), §6.1, §12 (the car-0 row: builds · files · EXECUTED proof · predicted reds · doors), **§16 items 1–8** (the chair's addendum: the provisional figures, the PER-TIER print, the `customReachable` column); (2) `$SC/receipt-instr-912.md` §§ CAR 8, CAR 10, CAR 11, CAR 12, CAR 13 (what the census is today and its corrected reader; the totals 708 / 318 / 390 / clean 185 / MISSING 34 / THIN 483 / COVERED 225); (3) `$SC/briefs/brief-INSTR-912-car8.md` with its ADDENDUM (the owner's two-way map and tiers); (4) `$SC/s12-sitting/SITTING-RULINGS-912.md` §L.4, §M, §N (the co-occurrence floor is the CHAIR'S — you print the distribution at the Wilson bound and set nothing; the RATE population is uniform-over-configuration with the wizard-default weighting as a REPORT); (5) the owner's rule memory in `$SC/arch-prose/ARCH-BRIEF.md` (four wordings, never trim; no em dash or bang in any `src/` string).

## What to build (ARCH §12 car 0, as amended by §16)
1. **`src/domain/prose/wiringCensus.js` EXTENDED** (the island; still no product importer — the ten-module byte fence must stay green): per row — `reads` (= `tests` unless a chair-ruled `NARROWS:` line exists; none exists today, so `reads === tests` on every row and the mechanism is proven by a fixture), `absent` per path (`measured` | `default` | `not-produced` — the generalised "default wearing a reading's clothes" test; ARCH §3.3), `covert` (any `reads` path on the COVERT-SOURCE list: `compromisedSecurityInstitutions().covert`, `npc.corrupt`, a covert impairment — the list published as a frozen constant), `objectClass` (the civic object a key names, from a CLOSED list the census emits; ARCH T-F12), the nearest-in-scope bag resolver re-checked on DS-DEF-4/-9/-11 and DS-POW-1 (read-facts §3.1's mis-credit), `sites` (mounts where the pool can speak, from `dossierMounts.js`), derived `attach` for a would-be modifier of each fact (every RESOLVED spine of the block whose `tests` exclude the field) with ATTACH COVERAGE per block, mounts-per-fact (spine and modifier), the k = 0 count (`3 − |reads| ≤ 0`) per block and estate-wide, **`customReachable`** (every (block, pool) whose predicate reads a fact a custom definition of some KIND can carry — read `src/domain/content/*` to enumerate the kinds and the typed facts a custom definition carries; print the count per kind and the list; refuse-with-measurement if the kinds are not enumerable from code), and the RELATION EXTRACTION from ARCH §5.2's sources (a) `canonicalAffectedSystems` (archetype → variable), (b) the `CAUSE_SIGNAL` rows, (c) the generator's recorded derivations (`economicGates.military` from `econOutput`; find every one by reading `src/generators/*` for a persisted gate/score derived from another field) — each row with DIRECTION; print the row count by direction between two fields a desk reads (ARCH: "before any joint is authored"); source (d) is the sitting's and is EMPTY today (assert it).
2. **`scripts/prose-rate-corpus.mjs` (new): THE RATE CORPUS** — a marginal-controlled grid: threat 4 × route 8 × tier 6 = 192 cells × 4 seeds (768 towns), culture and terrain swept where a predicate reads them; generated through the shipped pipeline (`generateSettlementPipeline`) with the composers called through their shipped desk-read recipes (every reading a caller passes — never `{}`; the `politics` reading included; the general desk's bare strings harvested — reuse car 9's sequence builder, `receipt-instr-912.md` §9.2); per (block, pool): `rateBp` (firing share), per TIER and pooled (§16 item 7: a pool silent at a tier where its block mounts is a per-tier finding; a hamlet with no walls is lawful silence — print both); the DEPARTURE bit computed from each pool's own firing rate (the 10 % line is a REPORT — ARCH E-F14a) and printed, NOT yet written into any leaf (the norm leaf is car 4's); the co-occurring fact pairs with no pool, with the Wilson 95 % interval per pair on N = 768 (ARCH E-F4: the bound is ≥ 51 of 768 at 5 %) — print the distribution and the pair list at the bound; the chair sets the floor from this print. A second weighting — the wizard-default configuration — printed as a REPORT column beside every rate (SITTING §N.3). Cost printed (seconds per town, total), never a threshold.
3. **`scripts/wiring-census.mjs` (new): the census COMMITTED AS DATA** — writes `docs/content/wiring-census.json` (every row and both indexes; the tiers; the rate columns; the relation table's row count; the custom-reachable list) stamped with the sha256 of the six composers, `dossierMounts.js` and the candidates leaves (none exist yet — stamp the composers and the mounts); `--check` refuses on a stale byte or a stale stamp exactly as `generate-dossier-state-prose.mjs --check` does; a `--print` mode for the receipt.
4. **`tests/lint/proseWiringCensus.walker.test.js` EXTENDED:** every new column convicted by a plant or a fixture that MUST fire (a default read as measured; an unmarked variant on a covert path; an attach set spanning two polarity classes; a same-object-class attach; a numeric pool key; a `NARROWS` line without a ruling id), totality 708 as an integer (then the grown count), RESOLVED 318 / clean 185 / UNRESOLVED 390 shrink-only, MISSING 34 · THIN 483 · COVERED 225 asserted (the corrected figures; a change is a declared row), the relation row count and the source-(d) emptiness asserted, the `customReachable` count asserted, the JSON `--check` interlock driven by a stale-stamp plant, the per-tier arm (a pool silent at every tier where it mounts is a finding, driven by a fixture). Mutation plants for the new arms numbered after `#85` (labels unique; the manifest by text).
5. **The receipt** prints, from ONE command each, reproducible: the census totals (as before, plus clean/absent/covert/objectClass/attach coverage/k = 0/mounts-per-fact/customReachable), the RATE corpus's marginals (192 cells × 4 seeds; the tier axis balanced), every rate per tier, the departure bits, the pair distribution with intervals and the count at the 5 % Wilson bound, the relation rows by direction, the cost. Every figure an integer or a ratio with its N; every count asserted; "n/a" is a refusal with its reason.

## Discipline, fences, proof
Runner count 0 in its own shell call before every vitest run; focused files one at a time; ONE whole `tests/lint` run at the end; `node scripts/check-domain-strict.mjs` exit 0 (the strict ratchet sits EXACTLY on its ceiling — a new strict error reds it; write strict-clean code from the first line); `npx vitest run tests/copy/voiceMechanics.test.js` E2 must name only the two banked files (no em dash or bang in any `src/` string literal); the OSR and prose-numerics dry reads exact (no `toFixed`, no float interpolation, no two-decimal literal in `src/`); the typecheck ratchet at or under its ceiling; the cast/annotation walkers green; the ten-module byte fence green (the census still has no product importer); the mutation manifest test green after your plants. Never the whole suite; never a build; never `--write`/`--update` on any register but the lighting door (0b, by the ritual, if a title moved — it will: your new arms add titles). Change only: `src/domain/prose/wiringCensus.js`, `scripts/wiring-census.mjs` (new), `scripts/prose-rate-corpus.mjs` (new), `docs/content/wiring-census.json` (new, committed), `tests/lint/proseWiringCensus.walker.test.js`, `tests/fixtures/*` you need, `scripts/mutation-sweep.sh` + the manifest (by text), `tests/lint/.lighting-census-baseline.json` (0b only), the receipt, `$SC/measure/*` probes. NO product surface, composer, pool text, persisted shape, seed input (THE PROMISE); no `poolMeta`, no leaf changes — those are cars 3–4. Commits: `MEASURE car 0: the census extended — <clause>` + `car 0b` for the lighting refreeze; explicit staging; porcelain 0; the three trailers exactly:
```
Seat: Opus 5 — Fable-unvalidated
Lane: MEASURE
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
Return: the car sha(s); the census line before → after (every new column's count); the RATE marginals and the per-tier silent-pool findings; the pair distribution at the bound (count ≥ 51 of 768) and the top twenty pairs with intervals; the relation rows by direction; the customReachable count per kind; the departure-bit summary; the cost; the plant list with their reds; the whole-tests/lint summary; the strict/voice/OSR/PN lines; anything refused with its measurement.
