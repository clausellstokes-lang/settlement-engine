# THE EIGHTEEN DEFECTS — content readers are being denied (the Opus chair)

`$SC/kit/rewrite/dark-pools.md` settled all 56 zero-firing pools by probe. The chair's hypothesis — that most were dead and could be struck for ~67M tokens — **did not survive**: only 12 are dead by construction (9 conservatively), 24 are out of the grid, and **18 are DEFECTS**. This car is not an efficiency car. It is the quality finding the efficiency question turned up, and it is ordered by what it costs a READER, not by what it costs the programme.

Dock `$SC/kit/laneRW-DEF2`; porcelain clean before each commit (a workflow may be running — verify, and wait rather than interleave). Never push. Stage explicit files. `/usr/bin/grep`. Every behaviour shift DECLARED in its commit body with the before/after figures; goldens re-recorded only with the cause stated. THE CHECKPOINT LAW. Trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## ORDER OF WORK — reader impact first

### (1) TWO POOLS NO READER CAN EVER SEE — `DS-POW-2`'s DOMINANT and NARROW share pools
The state exists on 132 and 116 of 768 towns and `stabilityLensPoolKey` returns first, on the instrument's path **and on `PowerTab`'s**, so the prose never composes for anyone. Establish whether the short-circuit is intended precedence or an ordering accident; if intended, the two pools are dead and say so; if not, fix the ordering. This is the only defect on the list that denies a reader content that already exists and is already written.

### (2) THE JOIN SPELLED WRONG — `DS-GEN-18 :: HOME-FED`
The docblock says the join is on the canonical token; the code does a bare `.toLowerCase()` across two different token shapes, so 0 towns join where 20 should. The docblock is the specification and the code disagrees with it. Fix the code, not the docblock, unless you can show the docblock is the thing that is wrong.

### (3) PROSE THAT COMPOSES TO NOTHING — `DS-CND-1 :: PROVENANCE: causes[] populated`
Keys correctly on 235/768 towns; all three variants name a `{reason}` slot nothing fills, so liveness drops every one and the pool renders empty on 235 towns. Either the slot gets a fill (the `{defmaterial}` pattern is the precedent) or the variants are re-cut not to need it — the latter is a WRITING act and belongs to a pool run, so if that is the answer, say so and stop.

### (4) THE INSTRUMENT NEVER ASKED THE RUNG — 14 pools of `DS-STR-1 :: CRISIS_POOL_OF`
`scripts/prose-rate-corpus.mjs:307-311` omits `crisisBannerRung`, so fourteen pools were never measured and recorded as zeros. Driving the shipped rung over the identical grid composes all fourteen at 6–26 towns each. **THE CHAIR PLANNED ON THIS INSTRUMENT AND IT WAS BLIND.** Add the rung, then RE-TAKE the whole 273-row fold at a current tip and refresh the census's `rateBp` column — which the rate measurement separately found to be a frozen 2026-09-08 artefact with 62 rows already moved. Declare the re-take: it also moves `bareSentences`, the pair distribution and the tier silences.

### (5) THE GRID IS BLIND ON ONE WHOLE AXIS — report, do not fix, unless the fix is one line
Three pools found it independently: the RATE grid pins all five priority sliders at 50, the stress-flag threshold computes to exactly 70, and every `fires(n)` call uses `n ≤ 65` — so all thirteen compound-stress flags are false on all 768 towns whatever the institutions say. Sweeping that axis would light pools in four blocks. Establish whether the 70-vs-65 is the grid's choice of sliders or a genuine off-by-one in the flag logic, and SAY WHICH; a real off-by-one in shipped flag logic is a product defect and outranks everything above it, so if that is what it is, stop and report before touching anything else.

## TWO THINGS RAISED OUTSIDE THE CAR, BOTH THE CHAIR'S
- **`DS-DEF-2`'s civic rows can assert a full legal chain about a RUINED court** — a floor-1 contradiction reachable with no authoring error at all, on the block this programme has just rewritten. Investigate and report; do not cure prose here, the block's own lane will.
- `generalStateProse.js:979-1032`'s docblock is stale at this tip. One commit, no behaviour.

## Return
Per commit: COMMIT · FILES · PINS · SHIFT (declared, with figures) · GATE · HAZARDS · OPEN. Plus: for (1) and (5), the VERDICT before the fix; for (4), the re-taken table's headline figures and how many pools moved. If any item turns out to be a decision rather than a defect, leave it and say so — the chair would rather have five settled than five forced.
