# Brief — MEASURE car 3: the fold's cures — 11 code, 13 receipt, the rate half re-folded, the manifest re-recorded as a declared instrument shift, the floors' lists re-printed, the alias table cut to three

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-measure.md` § CAR 3. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`. Dock: `$SC/laneMEASURE` at **fcd98a3db** (thirteen commits over 8522a17b2; porcelain 0). Never touch `skepMEASURE` or any other dock.

## Read first, whole, in this order
(1) `$SC/skeptic-measure/FOLD.md` (311 lines — §3 the 30 cures with their arms; §4 the figures; §5 the doors; §6 the U rows); (2) `$SC/s12-sitting/SITTING-RULINGS-912.md` **§P** (the chair's rulings — where a cure and §P differ, §P wins); (3) the three lens files under `$SC/skeptic-measure/` for each cure's site and figure; (4) `$SC/receipt-measure.md` §§ CAR 0–2 (what you correct); (5) ARCH §3.4, §3.7, §4.4, §5.2, §16.

## What to build
1. **Code cures 1–11 of FOLD §3a**, each with the ARM its line names, executed: (1) the economy desk called by its SHIPPED recipe in `scripts/prose-rate-corpus.mjs:206` — every reading `economyDeskRead` would default to `null` supplied as the tab supplies it (`foodBalance`, `granaryOutlook`, `flowDrift`, `impairedInstitution` — U1: thread the `ServicesTab.jsx:100` derivation too) and `playerView` from the audience; a walker arm asserting the economy desk's return DIFFERS between the two audiences on at least one golden town, and a fixture arm asserting `deskReturns` supplies every caller reading; (2) the same call in `tests/fixtures/composedReadingSequence.js:192`, same arms; (3) plant #80 re-anchored to include `branchReads: [],` — run the line standalone against the committed bytes, md5 must move, the walker must red, restore `cmp`-exact; (4) the membership arm split: the absence comparisons ungated, the presence limb and the causal carrier read behind `it.skipIf(!requireDistRead)`; plant B (eager import, no dist) still reds the absence arms; (5) ADDITIVE made reachable — drop the `textSha` equality conjunct or record a per-piece digest; the suite's ADDITIVE fixture rebuilt on a REAL addition; (6) the manifest's three refusals — `index >= 0` on every cell (must red today on 309), a mount-coverage arm against `DOSSIER_MOUNTS` (39 of 56 today) with every absence carrying a reason, the 36 one-sided positions in the leak check WITH direction (DM-only lawful; player-only a leak); (7) a seed flag on `prose-manifest-cells.mjs` so plant B reproduces from the tree (U4 re-run); (8) `absenceOf` distinguishing a fallback from a refusal guard, and `producerIndex` with the ES6-shorthand probe the estate spells at `src/domain/fieldManifest.js:373` — fixtures for both; (9) `objectClassOf` recording the SET of matching classes; the :872 arm restated to name the ambiguity; (10) `pairMemberClass` excluding JS method names (`.reduce` etc.) from the `fact` class — the usable-pair arm asserting the corrected counts with the method named; (11) a `sites` conviction (a plant on the mount read, or a must-fire fixture).
2. **The consequences, in the same car:** re-run the RATE corpus and re-fold the rate half of `docs/content/wiring-census.json` (`--check` green) — print fired (expect 270), silences (351 = 307 + 44), departure (72/198), and the USABLE pair distribution with `.reduce` excluded (expect ≈ 205 / ≈ 151 clearing) — these are the lists §P.2-26 sets the floors from: print them, set nothing; re-record `tests/fixtures/dossier-prose-manifest-golden.json` by the recorder (§P.2-29: a declared INSTRUMENT shift — the receipt names the 309 cells' change and the fixture's provenance names the tip that wrote it; add the arm that refuses a fixture whose provenance sha is not the writing tip); cut the alias draft's ratified set to the THREE identifier rows (§P.2-27) as a printed `ratifiedAliases` block in the census JSON (no leaf yet — SEAM car 4 carries it), and re-cut `generator-write` evidence to an AST-read assignment or key write (or refuse with the measurement if the AST cut exceeds this car — then the draft prints identifier rows only).
3. **Receipt corrections 12–25** as `## CAR 3 — CORRECTIONS TO CARS 0–2`: one row per item, the superseded sentence QUOTED, the measured one with its figure or command; the UNTESTED roster (U1–U5) with dispositions; plants #93/#94 executed standalone (U5) with md5 in and out.
4. **The register `note`:** if the lighting census moves (new titles), refreeze by its ritual as car 3b and write the corrected note ("two describes"); else leave it for the chair's register car.

## Discipline, fences, proof
Runner count 0 in its own shell call before EVERY vitest run (no batching); focused files one at a time; ONE whole `tests/lint` at the end; the strict ratchet on its ceiling (strict-clean code); voice E2 two banked files; OSR/PN exact; typecheck; the cast walkers; the island fence; the byte ratchets (no leaf byte moves — the census JSON and the manifest fixture are `docs/`/`tests/`, outside the leaves); the mutation manifest test after any new plant (numbered after the last; labels unique; by text). Never the whole suite; never a build; no register `--write` but the lighting door. Change only: `scripts/prose-rate-corpus.mjs`, `scripts/wiring-census.mjs`, `scripts/prose-manifest-cells.mjs`, `scripts/prose-manifest-diff.mjs`, `src/domain/prose/wiringCensus.js` (+ `wiringBranch.js` if needed), `tests/fixtures/composedReadingSequence.js`, `tests/property/dossierProseManifest.test.js`, `tests/helpers/dossierManifest.js`, `tests/build/vendorPdfLazy.test.js`, `tests/lint/proseWiringCensus.walker.test.js`, `tests/lint/proseCorpusBytes.test.js` (only if an arm mirrors a changed print), fixtures, `docs/content/wiring-census.json`, `tests/fixtures/dossier-prose-manifest-golden.json`, `scripts/mutation-sweep.sh` + the manifest by text, the receipt, `$SC/measure/*`. NO composer, desk, pool leaf, persisted shape, seed input. Commit `MEASURE car 3: the fold's cures — <clause>` (+ 3b); explicit staging; porcelain 0; the three trailers (`Seat: Opus 5 — Fable-unvalidated` / `Lane: MEASURE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`). Return: the sha(s); the eleven cures (cure → arm → tail → DONE/REFUSED-with-measurement); the corrected rate line (fired · silences · departure · usable pairs · clearing) beside the old; the manifest re-record's provenance and the 309 cells' disposition; the three ratified aliases; the whole-tests/lint summary; the ratchet lines; anything owed.
