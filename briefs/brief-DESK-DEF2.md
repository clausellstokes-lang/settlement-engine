# LANE: DESK-DEF2 — the defense desk's remaining 4 dark blocks (DESK-3 remainder)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-class content + wiring · nothing owner-gated⟧

READ FIRST: `$SC/briefs/_PREAMBLE.md` → `$SC/briefs/_DESK-LAW.md` → `dossierMounts.js` WHOLE → `stateProseKernel.js` WHOLE →
**`defenseStateProse.js` WHOLE (57 KB — the desk EXISTS and mounts 7 of the leaf's 11 blocks, all `sentence` on `defense`)**
→ `tests/domain/defenseStateProseDesk.test.js` → the defense corpus leaf under `src/data/dossierStateProse/` → the annex.

## YOUR DOCK
`$SC/laneDEF2` detached at **`940d161ca`**. Porcelain 0 at start; the chair replays onto the product tip. ⚠ Two sibling
lanes (DESK-WARFAITH, DESK-GEN2) edit `dossierMounts.js` in parallel — one id per line, append at the end, your leaf only.

## THE FACTS (re-measure at your tip)
- Dark: **DS-DEF-6, DS-DEF-7, DS-DEF-9, DS-DEF-10** (4 of 11; the landed defense car mounted DS-DEF-1,2,3,4,5,8,11 as
  `sentence` rungs on `defense`). Read the landed car's docblock in `dossierMounts.js` for WHY these four were left dark —
  if it names a reason (an unreachable pool, a missing producer, a size pinch on `DefenseTab.jsx` at 545 wc-lines against
  the 600 effective ceiling), that reason is your first re-derivation, not a fact to inherit.
- Seven `sentence` positions already speak on the defense tab: the one-fact-one-sentence law means each of your four must
  either be a distinct fact (a new `sentence`) or a `glance` where an existing sentence already covers the fact. Write
  the fact each block states and check it against the seven.
- `DefenseTab.jsx` is the host; measure its EFFECTIVE lines; extract a `DefenseGlance.jsx` leaf if it would cross 600.

## THE ACT
1. Re-derive the four ids, pools, dimensions (`"marks"`), producer tokens (file:line of each writer), and the landed
   car's stated reason for leaving them dark.
2. Extend `defenseStateProse.js`; mount what can be mounted honestly; a block whose pools no producer reaches is a
   FINDING (declare, pin, name the act that lights it) — mount the rest.
3. Real draws, `publicDossier`-gated, with rendered-text proofs and the public negative.
4. Tests: extend `tests/domain/defenseStateProseDesk.test.js`; UI flow coverage for the defense tab (extend an existing
   flow test if one exists; a new file is allowed — name it).
5. Proof per `_DESK-LAW.md`; plant-out; receipt `$SC/receipt-desk-def2.md` with predicted register deltas.

## ⛔ FENCES
As `_DESK-LAW.md`. `Lane: DESK-DEF2`. One car is the expected size; two if a Glance extraction is needed.
