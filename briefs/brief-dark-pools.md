# THE POOLS THAT NEVER FIRE — defect or dead? (the Opus chair)

`$SC/kit/rewrite/pool-rates.md` (measured 2026-09-14, every known `rateBp` reproduced EXACTLY, 0 mismatches over 271 rows) finds that of 361 resolved pools, **132 fire on ZERO towns of the 768-town RATE grid**, and separates them:
- **76 are NOT MEASURED, not measured-at-zero** — their blocks are unreachable because the RATE corpus generates world-less settlements (`generateSettlementPipeline(config, null, …)`); the shipped roster `MOUNTS_THE_CORPUS_DOES_NOT_REACH` in `tests/property/dossierProseManifest.test.js` names exactly those blocks. A reader may meet them often. NOT this car's.
- **56 are real zeros**: ~29 RUNG DARK (the block speaks; that key function never produced this key) and ~27 SILENT (the rung ran and chose another value class on all 768 towns).

At ~1.2M tokens a pool, writing those 56 costs ~67M tokens for prose no reader would ever see — IF they are truly unreachable. The chair will not assume that. **THE QUESTION THIS CAR ANSWERS, PER POOL: is the zero a PROPERTY OF THE ENGINE (the key combination cannot occur, so the pool is dead and its shipped rows stand untouched) or a DEFECT (the key should occur and something prevents it, so readers are being denied content)?**

THE CASE THAT PROVES THE QUESTION IS REAL — settle it FIRST and report it before the rest: `DS-DEF-2 :: Beasts & Monsters: plagued, NO perimeter and NO force` fires on ZERO towns, while its sibling `plagued, perimeter but NO force to hold it` fires on many. A plagued frontier thorp with neither walls nor force is the most ordinary settlement in the game as a reader imagines it. Either the grid cannot produce that combination (say exactly why, in fields), or something is wrong. The same block's `Internal Security: detention without process` and `Disasters & Famine: granary, NO medical provision` are the other two, and the chair's own reading is that both may be excluded BY TIER (a court resolves at every town-plus from a meeting hall, and `hasChurch` fires at every hamlet from a church in another settlement) — verify or refute that reading; do not adopt it.

METHOD, per zero pool: read its key function; determine the field combination it requires; then establish whether that combination is (a) impossible given the catalogue's `required` rows and tier gates — quote the rows; (b) possible but absent from the RATE grid's configuration space (the grid is threat 4 × route 8 × tier 6 × 4 seeds — a combination needing, say, a specific stress type or a world-level input the grid does not vary); or (c) possible and expected, and prevented by a defect — name the defect at file:line. Where a probe can settle it, probe: construct the settlement the key wants and see whether the rung produces the key.

DOCK: `$SC/kit/laneRW-DEF2`, READ ONLY unless you find a defect small and certain enough to fix, in which case fix it in its own commit with the probe as its proof and the behaviour shift declared. A workflow may be committing there — never edit while it is, and verify porcelain before and after anything you do write.

WRITE the findings to `$SC/kit/rewrite/dark-pools.md`: one row per zero pool — block · pool · SILENT/RUNG DARK · verdict (DEAD BY CONSTRUCTION / OUT OF GRID / DEFECT) · the fields or the file:line that settle it · what the chair should do (leave the shipped rows and never write it · write it after a corpus that reaches it · fix the defect then write it). Head it with the DS-DEF-2 three, since a batch waits on them.

Return: PATH · THE DS-DEF-2 THREE, each with its verdict and ground · the counts by verdict across all 56 · any DEFECT found, with its probe · how many of the 56 the chair can strike from the programme's remaining work, and how many tokens that is at 1.2M a pool · LIMITS.
