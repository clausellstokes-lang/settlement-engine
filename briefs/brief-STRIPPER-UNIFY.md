# BRIEF — STRIPPER-UNIFY (Opus lane; Seat: Opus 5 — Fable-unvalidated; Lane: STRIPPER-UNIFY)
Chair: Fable 5.1, 2026-09-05. Dock: `$SC/laneSTRIP` detached at 38474a59e (the §899 CAS = product tip). Lands at §901 or later.

## The finding you are curing (measured by L-HOMES-8 at 9f7378a55 in `$SC/laneLH8`; receipt `$SC/receipt-lgt-p8-matbound.md`)
`tests/lint/densityCreateBoundary*.walker` (name it from the receipt) had a `stripCommentsAndStrings` that ran its single-quote pass BEFORE its template-literal pass: an apostrophe inside a backtick string swallowed code to the next quote. 181 of 2,174 src files mis-stripped, 521,714 characters invisible, zero verdicts flipped by luck. L-HOMES-8 cured ITS walker (see its car; `git -C $SC/laneLH8 show 8776fae10 -- tests/lint`) and reported ELEVEN sibling instruments carrying the same ordering. Its list is in the receipt; MEASURE it yourself — grep every tests/lint and scripts/ file for a private stripper (`replace(/'(?:...)'/`, `stripComments`, `stripStrings`, `codeOnly`-like helpers) and classify each: (a) uses the shared `codeOnly` (the sov walker's export; note 12 suites import it from `sovereigntyLightingContract.walker.test.js`), (b) private, correct order, (c) private, WRONG order. Report the census with the denominator.

## The permission (chartered), the mechanism (test it, do not trust it)
Permission: cure class (c) at the habitat. The chair's suggested mechanism: lift ONE stripper into `tests/helpers/` (a plain module, no `it`), make every private stripper import it, delete the privates. ⚠ Measure before you follow it: importing a helper into a walker changes nothing in the lighting census (no titles move) but the sov walker's `codeOnly` export is a TEST-FILE export imported by 12 suites — moving it to a helper is legitimate only if no instrument pins its address (grep `codeOnly` across tests/ and scripts/ and docs/implementation/PACKET_MANIFEST.json). If a pin exists, keep the sov export as a re-export of the helper.
For EVERY stripper you touch: (1) measure the mis-strip over the tree before (files mis-stripped, chars lost — reuse L-HOMES-8's probe from its receipt) and after (must be 0 / 0); (2) plant an apostrophe-in-template-literal mutant fixture and assert the code after it stays visible; (3) run the instrument's own suite and prove ZERO verdict flips over the tree (list every verdict before/after; if one flips, that is a FINDING — report it, do not "fix" the instrument to make it stay green).

## Laws (all binding)
- No `it.each` (the each-family park ceiling is at 111 of 112). No new `*.test.js` unless unavoidable (a new file reds the lighting census and moves two registers; say so if you must). Every negative assertion anchored (`// anchored:` on the line immediately above).
- Every vitest via `sh scripts/gate-mutex.sh --run -- npx vitest run <files>`; run `tests/lint/` WHOLE at the end and report exit + counts; lighting red on `files`/`titles` is expected only if you added a file/title — otherwise 139/139.
- No register act (no `*_REFREEZE`, no `--update`/`--write`). Record deltas (expected: lighting titles + the number of new `it` titles you add; keep it small — prefer adding assertions inside existing titles).
- Never `git add -A`; no stash; dock's own node_modules symlink stays; commit with the pre-commit hook; each commit message ends with `Seat: Opus 5 — Fable-unvalidated` / `Lane: STRIPPER-UNIFY` (then a blank line, then `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`).
- Receipt at `$SC/receipt-stripper-unify.md`: outcome table with shas; the census (denominator, a/b/c); per-instrument before/after mis-strip figures; the mutant plants (killed, restored byte-identical by cmp); verdict flips (must be zero, or FINDINGS); register deltas; the RETROVALIDATION ROW last (what you judged, what the chair must re-derive); dock tip last. Porcelain 0 at the end.

## The eleven, as L-HOMES-8 named them (receipt §9.1) — your census must confirm or correct this list
`tests/lint/seedLoopTotality.walker.test.js` · `tests/lint/worldGenerationClockSeam.walker.test.js` · `tests/property/sovereigntyTradeDormancyFence.test.js` · `tests/property/oathHolderDormancyFence.test.js` · `tests/property/dispositionChannelsDormancyGolden.test.js` · `tests/domain/townCartographyDeterminism.test.js` · `tests/domain/npc/characterConsumers.test.js` · `tests/domain/npc/livedExperienceFunnel.test.js` · `tests/domain/npc/characterDrift.test.js` · `tests/domain/npc/paradigmAxisCatalog.test.js` · `tests/domain/npc/livedExperienceSources.test.js`. Two files already order it correctly (`infiltrationDrift.test.js`, `operationsVoice.test.js`) — the sound spelling exists in-house. L-HOMES-8's own probes: `$SC/lh8/blindspot.mjs`, `$SC/lh8/stripdiag.mjs` (reuse them; do not rewrite the measurement).
⚠ `characterDrift.test.js:791` is separately flagged as over-broad by L-HOMES-7 — do NOT widen or soften it while you are in the file; the stripper reorder only.
