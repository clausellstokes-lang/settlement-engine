# LANE 37 — cure check 2 run 9's remaining regressions (cut at the consist tip 87b87c406)

**Seat:** Opus builder. **Chair:** Fable (session 923472dc). **Base:** `fixes-2026-09-18-consist` @ 87b87c406 (290 cars). **Branch:** `fix-run9-2026-09-19`. **Worktree:** `$SP/lane-run9` where `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` — `git -C $SP/consist worktree add $SP/lane-run9 -b fix-run9-2026-09-19 87b87c406 && cd $SP/lane-run9 && npm ci`.

## PATH LAW
Write ONLY inside `$SP/lane-run9` and `$SP/lane37-scratch/`. NEVER touch `$SP/consist`, `$SP/lane-public-copy` (lane 35), `$SP/lane-pass3` (lane 36), or `/Users/cstokes/Desktop/settlement-engine`. Lane 36 owns (do not edit): `src/components/new/tabs/PlotHooksTab.jsx`, `src/pdf/sections/Services.jsx`, `src/generators/spatialGenerator.js`, `src/components/new/tabs/PowerTab.jsx`, `src/components/generate/WizardOutputToolbar.jsx`, `src/components/settlements/*`, the status-case and raw-key display seams. Lane 35 owns `src/store/authSlice.js`, `src/config/tierFacts.js`, `SeedField.jsx`, `HomeHero.jsx`, `LandingBelowFold.jsx`, `SettlementsPanel.jsx`, `src/lib/refusalReasons.js`. If a cure needs one of those files, write the patch to scratch and say so.

## GATE LAW (§934.33)
Finish every edit for a car, stage by explicit path, then run its focused suites ONLY through `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run <files from ONE test directory> --maxWorkers=2` (one directory per invocation). NEVER `npm run check`, the whole suite, the exclusive tier, a golden/baseline re-record, or a budget raise. If the mutex is held, write `.lane-resume.md` with the exact owed commands, commit what is staged, STOP and report. Any `.not.` assertion on absence needs `// anchored:` on the line directly above.

## THE CARS (run 9's ratchet verdict, verbatim failures in `$SP/consist-check-2-run9.log` lines 417–492)
1. **DS-DEF-2 / DS-DEF-6: the tier noun is the producer's (ODQ §934.22 addendum, RULED).** `tests/ui/defenseTabFlow.test.js` (6 arms) asserts the tab renders the EXACT sentence the desk produced; the tab now prints "The metropolis believes…" where the desk says "The town believes…" (and "What is in the store at Tyrholm…", "The town has grain in store…"). The §934.22 tier-noun cure (lane 27, car 348637896 `tierVoice`) landed at the TAB, so screen and desk disagree and the PDF still says "town". Cure: derive the tier noun ONCE in the desk's producer / the option bag both tabs and the PDF spell (the test's `/** The desk's own option bag */` at line 43 names it), remove the tab-side substitution, and let every surface render the desk's sentence. The six arms then pass UNCHANGED (do not edit their expectations). Check the PDF's defense section prints the same noun (tests/pdf). Report what the tier noun did to the goldens: run the plain golden test; if it moves, STOP that car's proof and report the count (the chair records the shift under §934.22).
2. **voiceMechanics (3 arms; ODQ §934.26 addendum, RULED).** `tests/copy/voiceMechanics.test.js` reddens on ONE new "!" in the en registry (the owner's verbatim gate-card copy "Access wars, religion, trade, the world!") and ONE new JSX debt (total 1 > budget 0; the per-file baseline `tests/copy/.voice-mechanics-jsx-baseline.json`). Ruling: the string stays as the owner ordered, in ONE place — the copy registry; if the JSX offender is the same sentence as a literal (browser pass 3 read it at `src/components/realm/RealmLockedGate.jsx:61-62` or wherever the gate card lives), it reads the registry key instead, so the JSX debt returns to 0; the registry arm gets a named, count-pinned allowlist row for that key citing "§934.26 (the owner's verbatim copy)". Budgets are NOT raised. If the JSX offender is a different string, rewrite it (no em dash, no exclamation).
3. **deepCraftKillList: tintedCallouts 161 > 160.** `tests/design/deepCraftKillList.test.js` counts a tinted-callout pattern across src/components; one new one landed in this consist (candidates: lane 28's RefusalNotice, lane 31's RealmPhoneNotice, lane 33's events card, lane 32's onboarding). Find it by running the test's own counter per file against the base fe021a487 (`git diff --stat fe021a487` narrows the files). Cure: re-express that notice in the dossier's own grammar (the owner's tome law: a rubric / a ruled note in the page's ink, not a tinted SaaS wash) so the count returns to 160. Never raise the ceiling.
4. **settlementWorkbench: the Entity Inspector's Escape returns focus to the invoking control.** `tests/components/settlementWorkbench.test.jsx:187` — after Escape, focus stays on the `<aside role="dialog">` instead of returning to the trigger button. Lane 32's `useDialogDismiss` / `DialogClose` (car 335db20fd) changed dismissal; trace the inspector's close path and restore focus to the opener (the hook already has a "focus goes home" arm — the inspector may not hand it the opener). Prove with the existing arm.
5. **CommandPalette: an option grew an interactive descendant.** `tests/ui/CommandPalette.test.jsx:130` — one `role=option` row contains an interactive element (a button/link), which ARIA forbids; find which (the failing assertion lists it) and move the control out of the option (or make it non-interactive) so the palette keeps one tab stop. Prove with the existing arm.

Out of scope (the chair's cars, do not touch): `isTierAllowedFailClosed` (lane 35 cured it), the docs counts (done, 87b87c406), the compendium regeneration, the edge-shared bundles, the wiring census re-cite.

## REPORT
Per car: sha, files, suites with printed counts (or the owed commands in `.lane-resume.md`), any register that moved. What you could not cure and why. Receipts only.
