# Brief — MEASURE cars 0e and 0f: the `reads` grain re-cut to the branch, and the relation-join MEASURED (then car 1 from its own brief)

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-measure.md` § CAR 0e / § CAR 0f. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`. Dock: `$SC/laneMEASURE` at **458eebb2f** (four cars over 8522a17b2; porcelain 0). Read first: `$SC/s12-sitting/SITTING-RULINGS-912.md` **§O** (the chair's rulings — they decide both cars), `$SC/receipt-measure.md` § CAR 0 (your predecessor's figures, refusals 3–4, findings F1 and F6), ARCH §4.4 and §5.2.

## Car 0e — the branch grain (§O.1, §O.5)
1. `reads` on every row carrying a predicate (185) = the fields of the SELECTING BRANCH: `predicate[].field` ∪ the guard fields on the path from the function entry to that branch (the `if` chain the guard reader already walks — reuse `guardAt`'s path); function-wide `fieldsRead` stays on the rows without a predicate (fail-closed), and the row says which grain it carries (`readsGrain: 'branch' | 'function'`). Recompute the fact budget (k = 0 count), derived `attach`, ATTACH COVERAGE per block and the modifier-eligible facts per tab on the branch grain; print BOTH grains side by side ONCE in the receipt (the measured cost of §O.1), then assert the branch-grain figures as integers. ARM: a fixture composer with two branches reading different fields must yield two rows with different `reads`; the DS-DEF-11 STRAINED row must read `{walls, gate}` (ARCH §6.3's worked value) and THREATENED/QUIET `{walls, gate, family}`; DS-DEF-11 and DS-DEF-2 must leave the cannot-attach set; a plant that returns function-wide `reads` on a predicate row must red.
2. The per-tier silences (347 rows): classify each as LAWFUL (the predicate's field cannot hold the firing value at that tier — read the field's domain from the generator or the schema: e.g. walls at a thorp) or MISSING-AT-TIER; print both counts and assert them; the MISSING-AT-TIER rows join the tier table as a fourth tier.
3. Regenerate `docs/content/wiring-census.json` (`--check` green), refreeze the lighting census by its ritual if a title moved (0e-b), plants for the new arms numbered after #88 (labels unique; the manifest by text).

## Car 0f — the relation join, MEASURED, nothing ratified (§O.2)
1. `scripts/wiring-census.mjs --join-draft`: for each of the 89 relation endpoints (producer tokens) and the 91 desk read roots, a candidate alias row `{endpoint, readRoot, evidence}` where the evidence is one of: the same identifier (case/underscore-insensitive) · a reading builder that reads the endpoint's field into the root (`generalDeskRead.js:176–243` and the desks' bags — cite file:line) · the generator writing the endpoint's field under the root's name (cite) · a docblock that names both. Print the draft table, the count of endpoints with ≥ 1 candidate, and the count of relation rows that WOULD join under the draft (by direction). NOTHING is written into any leaf or ratified — the aliases are a sitting act. ARM: a fixture with one endpoint and one root that share an identifier yields exactly one candidate; a plant that lowercases nothing must lose the case-insensitive match.
2. Receipt: the draft table (top forty candidates with evidence), the counts, and the list of endpoints with NO candidate (the wiring debt the SEAM/WAVE trains inherit).

## Then car 1
Continue in the same dock with `$SC/briefs/brief-MEASURE-car1.md` (read it whole; its enroll record; the DRIFT/VARIETY corpora; the classifier and its two plants; the paired-town and mixed-pool controls; the duplicate-unit baseline). Note car 0's RATE corpus builder and its shipped-recipe reading sequence — reuse, never re-implement.

## Discipline, fences, proof — as car 0's
Runner count 0 in its own shell call before EVERY vitest run (no batching of sibling walkers this time); focused files one at a time; ONE whole `tests/lint` at the end of EACH car; strict ratchet exactly on its ceiling; voice E2 two banked files; OSR/PN exact; typecheck; the cast walkers; the byte fence; the manifest test after plants; no `toFixed`/float interpolation/em dash/bang in `src/`. Never the whole suite; never a build; no register `--write` but the lighting door and, for car 1, the golden door through `recordGolden` with the chair's enroll record. Commits `MEASURE car 0e: …`, `car 0f: …`, `car 1: …` (+ `b` register commits by ritual); explicit staging; porcelain 0; the three trailers exactly (`Seat: Opus 5 — Fable-unvalidated` / `Lane: MEASURE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`). Return per car what its brief asks; for 0e the two-grain table and the new k = 0 / attach-coverage figures; for 0f the join counts and the no-candidate list. A claim without its executed tail is not a claim.
