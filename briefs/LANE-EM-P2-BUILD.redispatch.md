# LANE-EM-P2-BUILD — RE-DISPATCH (Opus build lane; successor chair, session 7d3418f8, 2026-09-19). The original brief `LANE-EM-P2-BUILD.md` binds VERBATIM; this file carries only what changed since 13:45.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

## What changed
1. **The first P2 lane died with its session at the 13:53 account switch, before writing a line.** The slot was clean; nothing of its work is in the tree. Its recon leftovers sit in `$SP/lane-em-p2-scratch/` (`scan.mjs`, `scan.mjs.bak`, `shape.mjs`, `goldens.before.txt`) — UNVERIFIED hints from a dead lane: you may read them, you may not trust them; re-measure everything by execution. Its sealed session was CLEARED by the chair (moved aside under the slot's git dir) so `implementation:dispatch` starts fresh.
2. **The tip moved, and EM-P2 was RE-PINNED to it.** Train EM-T2's terminal (check 2 run 15) went red on EM-P0's landing; two Opus lanes cured it on the branch — `023eda2ec` (the partial-pin refusal's punctuation in `src/generators/pipeline.js`; two anchored negatives in `tests/generators/pipelinePinnedMode.test.js`) and the ceilings re-mint in `tests/build/` — and the chair folded design §19, the charter's EM-E rows and the simulation survey onto the branch. None of it touches EM-P2's four change paths. EM-P2's verified base is now **`00fab686d31dd7bfaeb8363caa49921fb70c63e8`** (the packet header, the manifest entry and the index row all say so; `npm run validate:packets` printed `1 READY`). Where the original brief says "ancestry from d86aabae6", read **ancestry from `00fab686d`**; the branch tip you dispatch on is `f31ca0eb8`.
3. **The goldens' reference hashes** (re-take them yourself before the first edit; these are the chair's reading at the tip): `tests/fixtures/generator-golden-master.json` `7177cd6e89ebee404dec05d725d91e98ff59d2cfa124104a9a22515a7c8e8f1e`; `tests/fixtures/dossier-prose-manifest-golden.json` `921c51cf6799ffdfdbffa3715fb496f7d15ce44fff508864653d8ebf3bb4db41`.
4. **Trailers.** If your harness adds its own `Co-Authored-By`, keep BOTH trailers (the brief's and yours) — the chair's standing ruling; never amend.
5. **Scratch:** reuse `$SP/lane-em-p2-scratch/` (your receipt `EM-P2.receipt.md` and any `EM-P2.STOP.md` go there).

Everything else — the four paths and their order, the shared-tier mutex form with `GATE_MUTEX_MAX_POLLS=100000`, the lighting walker run ONCE separately and EXPECTED RED with the tuple recorded (never a refreeze), the goldens door FORBIDDEN, `check:packet` then `implementation:resume` both exit 0 BEFORE the one pathspec commit, the STOP list, the §12 report — is the original brief's, unchanged. The chair runs check 2 run 16 (the terminal, exclusive tier) in its own detached worktree while you build: your shared-tier runs will WAIT on it through the mutex's own long wait; that is by design — never poll the lock by hand, never shorten the wait.
