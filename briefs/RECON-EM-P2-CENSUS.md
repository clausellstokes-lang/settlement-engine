# RECON-EM-P2-CENSUS — the dispatch brief (Opus RECON lane; given 2026-09-19 ~15:1x EDT by the successor chair, session 7d3418f8)

**Seat:** Opus RECON. You EXECUTE a prototype and report measurements. You implement nothing in any tree, compile no packet, run no gate. **Why you exist:** EM-P2 version 2 died at its build lane's STOP because a design was argued instead of executed. The chair has ruled a replacement (design §21) and is applying that lesson to itself: before version 3 is compiled, YOU run the ruled measurement against the real pipeline and tell the chair what it actually shows — including where the ruling is wrong.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

**Tree (READ + IMPORT ONLY):** `$SP/read-tip-a41a0e109` — a detached worktree of the build branch at `a41a0e109`, with `node_modules` symlinked, so plain `node` can import `./src/...` from it (the chair smoke-tested `generateSettlementPipeline` there). Confirm `git -C` it `rev-parse --short HEAD` = `a41a0e109` first. **WRITE ONLY** under `$SP/lane-p2-recon-scratch/` (create it): your `.mjs` scripts, their outputs, your report. FORBIDDEN: editing/staging/committing anywhere; `$SP/consist`, `$SP/lane-em-b3b`, every other lane dir, the ledger checkout's working files; vitest, eslint, every npm script (plain `node <script>.mjs` only — no gate, so no mutex; keep ONE node process at a time). zsh: quote variables.

## Read whole first
1. Design §21 — `$SP/read-tip-a41a0e109/docs/DESIGN_EDIT_MODE_AND_DECREES.md` (and §14 for "root means CHOSEN, not computed").
2. The STOP that caused this — `$SP/lane-em-p2-scratch/EM-P2.STOP.md` (S1–S5).
3. `src/generators/pipeline.js` whole (the runner: `rng.fork(name)` per step, `setActiveRng(stepRng)`, `onStep`, `onStrictViolation`, `getStepMeta`, the partial-pin refusal), `src/generators/generateSettlementPipeline.js`, `src/kernel/prng.js` (`fork: (label) => createPRNG(...)`), `src/kernel/rngContext.js`.
4. `tests/generators/pipelinePinnedMode.test.js` — the counting-stream idiom (`countingStream`, `instrumentedRoot`, `withAmbientCounter`) and how it builds `initialContextFor` / `runHeadless`; `tests/helpers/goldenMasterCorpus.js` (`goldenCorpus()`, `keyOf`).

## What to build in scratch and measure (each figure from an executed script whose command and output you quote)
**M1 — the instrument, with its own control.** A counting root PRNG: every `fork` (recursively, any depth) returns a counting proxy that records, per STEP (the top-level fork label) — raw `random()` calls, other method calls, and the sub-fork labels used. Prove the AMBIENT channel lands in it (the runner sets the step's stream active; show an ambient helper's draw incrementing the step's count). CONTROL: reproduce EM-P0's own facts with your instrument — `generatePopulation` draws > 0 unpinned and exactly 0 under full pins (`npcs`, `relationships`, `factions`, `conflicts` from the run's own record); if your instrument cannot reproduce that, it is wrong — fix it before measuring anything else.
**M2 — which steps draw.** Over the golden corpus: per step, in how many rows it draws and the min/median/max draw count. Start with a stride sample (every 13th row), report its wall-clock, then run the FULL corpus if it costs under ~5 minutes; say which you ran.
**M3 — drawn or pure, per (step, key).** For every step that draws, re-run with ONLY that step's stream perturbed (the root's `fork(S)` returns `base.fork(S + '::perturbed')`; every other step forks normally, so S's inputs are identical) and capture, via `onStep`, S's returned patch AND the post-step value of every key S declares in `mutates`. A key whose serialized value differs from the unperturbed run is DRAWN in that row. Table: step · key · provides|mutates · rows moved / rows run. Also list: steps that draw but move NO declared key (then run with `onStrictViolation` and report which undeclared keys they write), and steps that move a key they only MUTATE.
**M4 — reaches the record.** For each of the 57 `provides` keys (the chair measured 22 steps / 57 keys at f31ca0eb8 — re-measure): is its value present on the assembled `settlement`, under what path (same name? nested? renamed?), or ABSENT (the STOP found `powerIntent` absent). Do this by value where names differ, and say how you matched.
**M5 — entropy the step stream does NOT carry.** Inside the pipeline's transitive import closure (compute it from `generateSettlementPipeline.js`'s imports; give the file count): every `createPRNG(`, hash-keyed draw (`hash01` and siblings), `Math.random`, `Date`/`performance` read. For each hit say whether it EXECUTES during `generateSettlementPipeline` (instrument or argue — label which) and whether the perturbation in M3 can see it. This is the hole most likely to falsify §21's "every channel funnels through the stream's `random()`".
**M6 — field-level sensitivity (Tier 2's arm), on EM-A1's draft fields.** Read the root fields EM-A1 declares (`$SP/chair-kit-923472dc/packets-waiting/EM-A1.md`, its declarations table — ten fields by the chair's notes; count what is there). For each: does the path RESOLVE on a generated settlement (n = how many elements), which step's perturbation moves it, and does ANY step other than its declared home move it.
**M7 — cost and the sample the packet should pin.** Wall-clock per corpus row for M2 and for M3; recommend the smallest corpus sample (size + how chosen) on which M2/M3's classification equals the full corpus's (or the largest you ran), with the evidence.

## Report (your final message; also `$SP/lane-p2-recon-scratch/RECON-EM-P2-CENSUS.report.md`)
1. The instrument control (M1) — pass/fail with the quoted numbers. 2. Tables M2–M6. 3. **Where design §21 is WRONG or incomplete** — every premise the execution contradicts, smallest measured contradiction first (do not soften; the chair amends the design from this). 4. The Tier-1 literal as measured (step · key · draws · drawn|pure · onRecord path), paste-ready. 5. M7's recommendation. 6. What you could not measure and what would settle it. Every claim CONFIRMED (quoted command + output) or PLAUSIBLE (reasoning, with the settling experiment named).
