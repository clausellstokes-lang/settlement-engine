# LANE: SEAT-78 — the irregular-force topology (`irregularForceEnabled`), its OWN consist beside the lighting wave (LGT-P10)
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · chair-ruled FUND under O-4 (§882.1; TIME IS NOT THE CONSTRAINT) · ≈4 cars · the arc's LONGEST POLE — it, not the wave, sets the freeze date, so it runs from day one⟧

## READ FIRST
1. `$SC/briefs/_PREAMBLE.md`.
2. **The sealed SEAT-7a car** `refs/preserve/seat7a-newstrain-2026-09-04` = `ce9910469` (one car over `6c49ecbbf`) — read
   its WHOLE message and diff (9 files). It mints `irregularForceEnabled` WITH its first gate read and its authored
   certification row in ONE commit (simulationRules.js's own law; the lit-coverage walker regexes RAW SOURCE, so the
   mint and every mention must land together); lights exactly one thing (`resolveCoupVerdict` gains
   `forceRatioFactor`, multiplicative on the incumbent's `share`, wired at BOTH verdict callers — coup.js and
   deploymentReturn.js); proves ALIVENESS end-to-end (dark → `coup_suppressed`, lit → `coup_succeeded`; a pHold
   differential; a monotone participation ladder; the un-gated-incumbent dead-arm trap written into the suite header);
   refuses F3's sixth additive adj (the five live adjs sum to 0.815 of a DECLARED_BUDGET 0.82 — raising it is a tuning-
   signature act) — JUDGMENT, vetoable; defers F3's `civilContests` ledger (a NEW PERSISTED SHAPE) with F2/F5; records a
   FALSE charter premise (`governanceLedger` has 0 hits; the read was re-homed).
3. The ledger: `git show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md` — **§745.2 (the D10/D11 mini-panel FOLD
   that must run FIRST)**, the W-SEAT charter rows (D10, D11, F2–F5, Q-S3 "two-thirds lit"), §882.1 O-4 (FUND), and every
   later SEAT-7 mention (`grep -n 'SEAT-7'`). Re-derive what D11 (the ABOVE-threshold half) and Q-S3's third leg are.
4. The lighting plan's row: `refs/preserve/light-plan-2026-09-05:PLAN.md` §1.1 LGT-P10 and §6 "IN PARALLEL".

## YOUR DOCK
Given at dispatch — a fresh dock at the post-PROSE product tip. ⚠ SEAT-7a overlaps the mainline on
`src/domain/rulingPowerCoup.js` (mainline +47/−0 since `6c49ecbbf`; the PROSE car also cures 2 lines there) and
`src/domain/worldPulse/simulationRules.js`. Replay `ce9910469` FIRST as its own car, resolving both by keeping the
mainline AND the car's law; prove `rulingPowerCoup.js` by module evaluation and by its suite; prove the mint survives the
walker (`sovereigntyLightingContract.walker` + `engineGatedRuleKeys.walker`).

## THE ACT
0. **The D10/D11 mini-panel FOLD (§745.2)** — if it is a document act the chair owes, STOP and say so with the row; if it
   is a lane act (folding panel findings into the design), do it as car 0 and record every fold.
1. Replay SEAT-7a (above). 2. SEAT-7b — the above-threshold half (D11), on the SAME shape as 7a: minted dark, one gate
   read, certification row, aliveness proven end-to-end through the real writer, both callers. 3. SEAT-8 — Q-S3's third
   leg, whatever the charter names it (re-derive; do not guess from this brief). 4. The per-key dormancy FENCE (L-HOMES
   CAR 3's shape: a dark half + a lit-mutant control; a DORMANCY claim is a BIT claim) — ⚠ fence files are surfaces in
   the golden-freeze register: enrol/exclude in the same commit.
Every car dark-inert and same-seed neutral (one fixed-seed run before/after, byte-identical). No persisted shape (F3's
ledger stays deferred — owner-gated class). No tuning value moved (the additive slot stays OPEN).

## PROOF
The coup/deployment suites; the commons dormancy golden (5/5); the two walkers; `tests/lint/` WHOLE; `tests/property/`
fences; `typecheck:domain:strict`; eslint; plant-out of each mint. Quiet-window law + mutex. Receipt
`$SC/receipt-seat-78.md` (PARTIAL header first; a RETROVALIDATION ROW; the OPEN additive slot recorded).

## ⛔ FENCES
No flag name in a comment anywhere in `simulationRules.js` but the ones you mint in the same commit. No register acts.
No new persisted shape. No tuning signature. No `--amend`; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: SEAT-78`.
