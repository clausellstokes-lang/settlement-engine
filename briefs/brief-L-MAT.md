# LANE: L-MAT — the living-content law is WIRED (dark), and the three O-11 persistence paths are made honest; the dial stays at 1
⟦Chair: Fable 5.1 (session b43943b4, drafted 2026-09-07 ~03:25 from the four-reader map `$SC/lmat-map.md`, workflow wf_37a1566c-e8f) · Lane: Opus 5 (`model: "opus"`) · implementation lane, product bytes · dock: a FRESH dock cut from the product tip at dispatch (`sh $SC/mkdock.sh laneLMAT <tip>` — re-derive: `git -C /Users/cstokes/Desktop/settlement-engine rev-parse claude/composite-r4`; expected the §910 CAS or later) · ⛔ no vitest while `$SC/HOLD-VITEST` exists · ⛔ builds only under the quiet-window law and through `sh scripts/gate-mutex.sh --run -- npm run build` (a build is CPU-heavy; two are owed)⟧

## THE CHARTER, IN ONE BREATH
ODQ §904 owner row 2 ("the MAT dial's public-safe path") was DELEGATED to the chair on 2026-09-07 01:45 and ruled BUILD IT (ruling 8). The L-UI-MAT lane (§904, receipt `receipt-l-ui-mat.md` R2) measured that the dial flip is a NO-OP (the mint has no caller), that the wiring is build-gated, and that O-11's three persistence paths are unprepared. This lane WIRES the law (dark: the dial stays at `DEFAULT_LIVING_CONTENT_LAW_VERSION = 1`, so every world is still born v1 and no persisted byte moves), and makes the three O-11 paths honest so that the day the owner rolls the dial, nothing leaks, nothing lies and nothing is lost. **Lighting the dial is NOT this lane's** — it moves same-seed output and kills a dark-register row; it is a separate owner act on its own consist.

## ⭐ CHAIR RULINGS (JUDGMENT, each vetoable by the owner; recorded in memory `owner-decision-2026-09-07-delegated-rows-chair-rulings` as the 03:25 amendment to ruling 8)
- **R-A. O-11 path 1 = the public projection DROPS the roster, PINNED — NO migration, NO allowlist entry.** Ruling 8's mechanism ("the allowlist including `customContentRoster`") is WITHDRAWN on measurement: (i) the ledger's own O-11 tracing line reads the public path as DROP ("publicSafe allowlist drops the roster from DM-shares", ODQ ~:32260); (ii) `customContentRoster` has NO reader in `src/` (`livingContentRoster.js:33-34` says so), so nothing on any public surface would render it — allowlisting it is pure exposure with zero product value; (iii) the roster records EVERY living-content definition in scope for the run, adopted or not (`livingContentRoster.js:184`) — an author's unadopted homebrew library; (iv) every row carries the five stable account-scoped identifiers (`customDefinitionIdentityProjection.js:16`) that would let an observer correlate one private definition across two published worlds; (v) NO token of `PRIVATE_KEY_RE` (`publicSafe.js:101`) matches any roster key, so the deeper denylist would strip nothing. Consequence: **L-MAT needs no SQL migration** (the living-content law has none by design, `livingContentLaw.js:22-24`; the provenance receipt has no server mirror; the only migration ever contemplated was the allowlist twin). The lane is therefore chair-class end to end (persisted shape, pre-launch, §882.13's own boundary) and lands dark-inert. Charter the permission (the public-safe path is honest), measure the mechanism (the migration was the wrong one).
- **R-B. O-11 path 2 = RESOLVE-OR-DROP on account import, mirroring the provenance seam three lines above it.** The roster and the receipt are minted side by side (`generateSettlementPipeline.js:153` and `:185`) and must be treated alike on import: remap the four identity fields through the archive-backed identity map exactly as `remapAccountSettlementContentProvenance` does; on any miss, DROP the roster and push a per-settlement warning (the estate's stated law for an unresolvable foreign content identity, `accountImportBody.js:439-443`); per-SETTLEMENT failure grain (the roster is an exactness record of scope, like the receipt). Over "drop always" because a user moving their own estate between accounts keeps a true record; over "resolve per row" because a half-resolved scope record is a false one. ⚠ FIRST trace regeneration-after-import (the reader's open question): if a regen re-mints the roster from the importer's own `customContent`, say so in the receipt and keep the remap anyway (the un-regenerated saved world is the one that must not lie).
- **R-C. O-11 path 3 = the provenance receipt widens by ONE OPTIONAL core key, OMITTED WHEN DORMANT.** Measured by the reader: an always-present key (even `null`) moves 100 % of existing receipt hashes (4adb7702… → e3ee3f18…); an omitted-when-dormant key reproduces the dormant digest byte for byte, so ZERO existing receipts move (THE PROMISE). The key is present iff the world is LIT (law version 2), so a lit world with zero roster rows is not mistaken for a dark one; its value = `{ lawVersion, rosterHash }` where `rosterHash` is a digest of the IDENTITY-ONLY projection (definitionId / revisionId / contentHash triples per row, sorted) — what is immutable — never the authored presentation prose (an epithet edit must not move a tamper-evident receipt). Admission becomes required-plus-optional (never exact-wider: `hasExactKeys` would reject every six-key receipt and the import path answers a failed admit by DELETING the receipt, `accountImportBody.js:439`). The same widening lands in the portability remap's own core (`accountSettlementContentPortability.js:182`) or its output fails its own admit. Wired WITHOUT a new import: build the roster BEFORE the receipt in the pipeline and pass the digest through the existing context bag, so `settlementContentProvenance.js` (excised, pinned `engine-core-lazy`) acquires no new edge. The pin at `livingContentMaterialization.test.js:560` ("the provenance receipt is untouched by the roster") is RE-CUT deliberately to the narrower truth: byte-identical for every dark world (the control), moved for a lit one. `schemaVersion` stays 1.
- **R-D. `config._livingContentLawVersion` may reach the public projection** (it rides `settlement.config`, which is allowlisted; no denylist token matches). It is a law-version marker of the same kind as `schemaVersion` and `generatorVersion`, not a secret; RECORDED, not changed. Also PIN `customContentProvenance` absent from the allowlist in the same idiom (it already ships on every run with a pack and has no negative pin) — the same price, the same law.
- **R-E. The wiring mirrors the WIRED precedent exactly** (the density law: `birthConfig` spreads the mint, every classified BIRTH calls `birthConfig`, the EXECUTOR class never mints — `densityCreateBoundary.js:120-126`): the living-content mint is spread inside `birthConfig`, main-thread, never on the worker/engine far side. Over "a declared LAZY mint home in the two BIRTH callers" (which avoids first paint entirely but breaks the one-mint-in-birthConfig architecture and the header at :176-186 records that adding a wiring state already cost one red).

## THE CARS (one consist; every figure below is a READER'S MEASUREMENT at `ec265d24c` to RE-DERIVE before any edit — a brief figure is a hypothesis)
**CAR 1 — take the boundary module off first paint (the measured lever).** `src/store/settlementSliceHelpers.js:44` `export { birthConfig } from '../domain/density/densityCreateBoundary.js'` is the SOLE eager edge into `densityCreateBoundary.js` (its only consumer `src/store/settlementGenerateAction.js:47`; the other importer `composeInstantWorld.js:56` is lazy). Both BIRTH callers are already lazy (`settlementGenerateAction.js` reached only via `settlementSlice.js:59`'s dynamic import; `composeInstantWorld.js` not eager). Replace the re-export by a direct import in `settlementGenerateAction.js`; measured counterfactual: the main.jsx closure drops 239 → 238 modules and `densityCreateBoundary.js` leaves the eager set (`densityLaw.js` stays eager on its own path — the density cost is unchanged). Check the `settlementSlice.js` size ratchet (tolerance 0, the reason the helpers file exists, :40-41) and whether `settlementGenerateAction.js` carries a ratchet of its own; the walker's `MINT_HOMES` row for the helpers file (`densityCreateBoundary.walker.test.js:48-52`) is removed in the same car or kept as an allowlist row — say which and why. PIN `densityCreateBoundary.js` in `manualChunks` (an orphan that is neither ESD nor eager is co-located into the big lazy engine chunk — the FP-G11/FP-G17 incident class). BUILD receipt: `npm run build` at base and at this car; the hashed-chunk listing diff (count, per-chunk bytes, content hashes); `EAGER_FIRST_PAINT_MODULES` membership; raw / gzip / Brotli against `1_048_000` / `337_000` / `283_000` (`vendorPdfLazy.test.js:565,:595,:596`; margins are three-figure and owner-signed — a car that costs bytes has no room). ⚠ THE LARGEST UNPRICED RISK: does de-eagering the boundary re-parent the big lazy `engine` chunk into `composeInstantWorld`'s chunk? Only the two-build diff answers it; if it does, STOP and report.
**CAR 2 — WIRE.** Static import `densityCreateBoundary.js` → `src/domain/content/livingContentLaw.js` (⚠ the law files live under `src/domain/content/`, NOT `src/domain/density/` as older notes say) and spread `newSettlementLivingContentLaw()` inside `birthConfig` beside the density mint; flip the `GENERATION_LAWS.livingContent` row to `wiring: 'WIRED'` (:279) — the :470 arm requires the mint NAMED in executable code in `MINT_HOME`, which the import satisfies; state the edge cost in the row as the register demands. The dial stays at 1, so the mint returns `{}` and writes ZERO config bytes (`livingContentLaw.js:94-95, :154-162`) — prove it with a same-seed golden at base and tip (0 of N rows moved). `livingContentLaw.js` becomes non-ESD, non-eager: PIN it in `manualChunks`. DROP the header's `ENGINE_SHARED_DOMAIN_EXCISIONS` instruction (`densityCreateBoundary.js:249-251`): measured inert — `livingContentLaw.js` is not an ESD member (68 members, seeded from `src/generators` only, `vite.config.js:58`), so the row would be a `.delete()` of a non-member. Two guards must stay green with NO build: `engineChunkLazy.test.js:238` (the orphan-excision arm over the main.jsx walk) and `livingContentSeamLazy.test.js:134` (`livingContentLaw.js` absent from `EAGER_FIRST_PAINT_MODULES`). BUILD receipt again (the second build): listing diff, ceilings, membership.
**CAR 3 — close the walker hole + correct the header.** The ⭐ arm at `densityCreateBoundary.walker.test.js:429` scans `[...reachers, MINT_HOME]` (:452); the measured reacher set is five files and EXCLUDES `src/store/settlementGenerateAction.js` — the estate's own birth-mint home — so a per-caller mint placed there would land silently green. Extend the scanned set to the declared BIRTH rows and their `reachesVia` executors; prove it with a negative control (a temporary mint in the generate action reds the arm; restored by `cp` from an md5-verified backup, never `git checkout`). Correct the two stale header facts with the measurement beside each: the closure is 239 modules at this tip, not 237 (:231); "the lane awaits `loadEngine()`" (:246-248) is false — the lane calls `runGeneration` and dynamically imports `workers/generationRequest.js` (:61). Quote no more than twelve consecutive words of the old prose in the new.
**CAR 4 — O-11 path 1 (PIN the drop; R-A, R-D).** In the townMapEditsPublicDrop idiom but the STRONGER anchored form the estate requires (`tests/helpers/anchoredNegatives.js:111`; `lifecycleRoundTrip.test.js:1203` precedent): `expectAbsentWithAnchor([...PUBLIC_TOPLEVEL_KEYS], 'customContentRoster', 'config', …)` and the same for `customContentProvenance`; plus a BEHAVIOURAL arm that generates with LIT_CONFIG + the reference pack (`livingContentMaterialization.test.js:106-109, :214` shapes), asserts the roster key IS written, and asserts `toPublicSafe` drops it while allowlisted siblings survive — every existing allowlist-contract arm generates a DORMANT world (`gallerySanitizeAllowlist.contract.test.js:124`, `customContent: {}`) and proves nothing about the roster. No migration; `PUBLIC_TOPLEVEL_KEYS` untouched; the SQL twin untouched; `supabase/applied-head.json` untouched (121). Measure and RECORD (not change) that `config._livingContentLawVersion` reaches the public projection on a lit world.
**CAR 5 — O-11 path 2 (R-B).** `remapAccountSettlementLivingContentRoster(rawRoster, identityMap)` in `src/lib/accountSettlementContentPortability.js`, modelled beat for beat on the provenance remapper (:107-128: the four lookups `definitionIds` / `revisionIds` / `revisionContentHashes` / `localUids`, plus `revisionNumbers` for `customDefinitionVersion` when it is a revision number — drop the field when it is not resolvable); RE-DERIVE `customDefinitionFingerprint` from the destination hash (the projection falls it back to the content hash, `customDefinitionIdentityProjection.js:93-99` — a remap that rewrites the hash and carries the fingerprint leaves the SOURCE hash under a second key); keep `source` / `isCustom` / `customDefinitionCategory` and the authored fields verbatim; RE-SORT each bucket with `compareCodepoint` over the NEW id (`livingContentRoster.js:189-194` — the source order was keyed on the id just changed). Wire it in `accountImportBody.js` immediately after the provenance block (:424-445), same `if (preparedResult.ok === true)` guard: on failure `delete …settlement.customContentRoster` + a `settlementContentWarnings` row. ⚠ THE PHASE-4/PHASE-8 ORDERING: on legacy content-PACK envelopes the identity map is still the empty `archiveBacked:false` default at Phase 4 (:186, replaced only at :631 inside Phase 8), so the remap resolves nothing there and MUST degrade to the documented drop-with-warning — prove both paths. Amend the roster module's "ONE additive key … ONE writer" header (`livingContentRoster.js:23, :33`) and check the WRITER_DARK_REGISTER row (`scripts/lib/writer-dark-register.mjs:81`) and the MAT retro-control (`writerReach.walker.test.js:1164-1167`): with the dial at 1 no shipped world carries the key, so both stay as they are — say so with the measurement. Declare the roster on `importReconciliationAdmission.js`'s boundary the way `campaignState` / `aiData` / `versionHistory` are, or measure that the reconciliation path cannot reach it. Proof: sibling arms in `accountImportSlice.test.js`'s `export→import round-trip` suite (the mocked identity map at :81-110; the provenance arm at :749-767 is the template) + unit arms + the negative control (an UNMAPPED roster is removed with a warning). Trace regen-after-import FIRST and record the answer.
**CAR 6 — O-11 path 3 (R-C).** `settlementContentProvenance.js`: the optional core key (:382-388 the five-key core; :21 `RECEIPT_KEYS`; :109-111 `hasExactKeys` → required-plus-optional; :412 the strict schemaVersion pin stays at 1); the identical widening in the remap's core (`accountSettlementContentPortability.js:182-196`); the pipeline builds the roster BEFORE the receipt and passes the digest via the context bag (:147 vs :180 today — the receipt is built first; verify `finalCtx.settlement.config` at :182 is the same law version as the config in scope at :147); the identity-only digest; re-cut the :560 pin with the dark control kept; the three downstream instruments that MOVE for different reasons and must be re-derived, never carried: `writerReach.walker.test.js:911` `dialGated.size` 30 (a new dial-gated persisted key), the observed-shape-readers inventory row for the module (nine entries; a read of `customContentRoster on settlement` adds one — `scripts/.observed-shape-readers-baseline.json`), the tuning inventory count of 1 for the module (`tests/lint/.tuning-inventory.json:7406` — a new numeric bound moves it). MEASURE: zero existing receipts move (the dormant digest byte-identical at base and tip over the whole fixture set); `customContentTierGates.test.js:245` PIN 5 (receipt equality lit vs without) — say what it measures now and whether it must be re-cut; the neutrality instrument's `changedMechanicsPaths` (`customContentPresentationClaims.test.js:452`) stays unaffected while it generates dark — record it.
**Every car:** the focused test files one at a time, exits captured in-shell before any pipe; eslint on touched files; `npm run typecheck:ratchet` once at the composed tip (ceiling 173); the negative-assertion per-file budgets are EXACT (`negativeAssertionAnchor.walker.test.js`: accountImport.test.js 6 · importScrub.test.js 1 · accountImportSlice.test.js 9; the marker is read on the assertion's line and the ONE line above — never wrap it); the lighting census is exact equality and refuses a dirty tree — the chair takes it at the landing; `ARCHITECTURE.md` and the migration train are UNTOUCHED (no migration in this lane).

## LAWS
- STATE, NEVER FATE. The dial stays at 1. No tuning value moved, no golden re-recorded (the same-seed golden is a CONTROL, read at base and tip), no register `--write`, no signature touched, no `supabase/applied-head.json` byte, no migration file.
- THE PROMISE re-proven by EXECUTION inside the wiring car across create / read / regenerate / undo / clone / import: an EXISTING (markerless) world regenerates as v1 (`regenSection` takes `settlement.config || config`, settlement first — `densityCreateBoundary.js:52`, `settlementGenerateAction.js:113`); `resolveLivingContentLawVersion` reads the config and NEVER the dial (`livingContentLawVersion.js:97-107`); `updateConfig` drops unknown keys; `geographyLockedConfig` overlays geography keys only. The STOP the L-UI-MAT brief set (a marker stamped on an existing world at regen) becomes fireable the day the mint has a caller — this lane is that day; fire it deliberately in a test and prove it does not fire in the product.
- Read the whole enclosing scope and every caller before editing; re-derive every line number above (the map was read at `ec265d24c`; your dock is later). Never edit a running script. Commits in the dock: explicit paths, never `git add -A`/`-u`/`.`; never stash; no push; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: L-MAT`.
- Receipt `$SC/receipt-l-mat.md` at arrival (STATUS: PARTIAL; the three arrival lines) and after EVERY car (CARS with shas; the two build listings; the RETROVALIDATION ROW of every judgment call incl. the chair's R-A…R-E as chair rulings; CONFIRMED/PLAUSIBLE per figure); every timestamp from `date`. If a measurement contradicts this brief — including any of R-A…R-E — REFUSE with the measurement and continue; a refusal is a result.
