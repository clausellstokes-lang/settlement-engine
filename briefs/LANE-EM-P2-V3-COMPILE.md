# LANE-EM-P2-V3-COMPILE — the dispatch brief (Opus COMPILE lane; successor chair, session 7d3418f8, 2026-09-19)

**Seat:** Opus COMPILE. You compile ONE member packet — **EM-P2 version 3** — to the estate's implementation-packet standard. You implement nothing in any tree and run no gate. The shared compile brief `briefs/LANE-EM-COMPILE.md` (chair kit) binds whole — its path law, "navigate by symbol", "a fact without a command is not verified", "exact contracts, no discretion", "never adjudicate: a refuted premise is BLOCKED with the smallest measured contradiction" — with the substitutions below.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · kit = `$SP/chair-kit-923472dc` (READ-ONLY)

**The tree you read and may IMPORT from:** ONLY `$SP/read-tip-a41a0e109` (detached at `a41a0e109`; its `src/` and `tests/` are byte-identical to the build branch's tip — only docs moved since; `node_modules` is symlinked, so plain `node` runs the pipeline from it). **You write ONLY** under `$SP/lane-em-p2-v3-scratch/` (create it). FORBIDDEN: editing/staging/committing anywhere; `$SP/consist`, `$SP/lane-em-b3b` (a build lane holds it), other lane dirs; the ledger checkout's working files — read the ledger ONLY through `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:<path>`, and the build branch's newest docs through `git -C … show fixes-2026-09-18-consist:<path>`. No vitest, no eslint --fix, no npm script, no build. Plain `node` on scripts in your scratch IS allowed (one process at a time) — the recon's prototype is yours to re-run.

## Why version 3 exists — read these whole, in this order
1. `findings/EM-P2.v2.STOP.md` (kit) — version 2's build lane stopped with zero edits: a STATIC scan of three spellings is a proper subset of generation's draw vocabulary.
2. The ledger's `docs/DESIGN_EDIT_MODE_AND_DECREES.md` §14 (as amended), §21 and **§21.5** — the chair's ruling and what the execution forced; and `docs/implementation/charters/EDIT-MODE-TRAIN.md`'s three "Amendments of 2026-09-19" sections (the EM-P2 version 3 bullets are your charter row).
3. `findings/RECON-EM-P2-CENSUS.report.md` (597 lines — every figure with its command), `findings/RECON-EM-P2-TIER1-LITERAL.md` (the 75 rows, paste-ready) and `tools/census-prototype/*.mjs` (the two instruments, the controls, the sample selection, the off-corpus probes). These are MEASUREMENTS by a recon lane: re-run what you rest a contract on, and say so.
4. The STALE version 2 in the tree (`docs/implementation/packets/settlement-editor/EM-P2.md` + its manifest entry) — for what survives: §0's reasons (1), (3), (4) for a SIBLING register still stand; the row shape, the scan, `forkKey`, A1–A4 and the entropy-census TEST row do not.
5. The build branch's `docs/implementation/preambles/EM-PREAMBLE.md` (it gained §P2 rows 10–11 at `4da740b52`: edge-shared input membership; the byte budgets priced at pre-proof), `PACKET_STANDARD.md`, `PACKET_TEMPLATE.md`; `packets-waiting/EM-A1.md` (kit) — the consumer that joins on `(cardShape, outputKey)`.

## The shape the chair has ruled (design §21.5) — compile exactly this, or report BLOCKED with the measured contradiction
- **Tier 1** — one row per `(step, key)` for every key a step `provides` or `mutates` (75 at this tip), both-directions equal to `getStepMeta()` (static, by construction). Per row, MEASURED BY EXECUTION and committed as reviewed literals: `via: 'provides' | 'mutates'`; `draws` (does the step draw — rows drawn / rows run); `class: 'drawn' | 'pure' | 'label'` where `drawn` REQUIRES the step to draw AND the key to move under that step's perturbed stream, `label` is a key that moves under a zero-draw step (it stores a stream label — `powerIntent`), and every `pure` carries the row count behind it; `onRecord: 'absent' | 'same' | 'transformed'`. Fork labels are observed DATA of the census, never a row key. No static idiom scan anywhere.
- **Tier 2** — EM-A1's editable root fields, re-cut against the LIVE record (the power card is `powerStructure.governingName` + `factions[].isGoverning`; `seats[].holder` exists nowhere): `{ forkId, step, key, module, symbol, outputKey (the RECORD PATH), cardShape, origin: 'drawn' | 'computed' }`. Arms: the path resolves on generated settlements; the holding `(step, key)` exists in Tier 1; `origin` equals the measurement; `symbol` resolves against SOURCE by a resolver that understands `registerStep('<name>'` hosts (do NOT copy the model's resolver where it is wrong; do NOT edit the model). ⛔ Editability is NOT conditioned on entropy (§14 as amended): four of the ten fields are `computed` and stay editable.
- **The census corpus** — the recon's 63-row sample (every tier x terrain pair once + all 21 non-grid golden rows; it reproduces the full 525-row classification exactly) PLUS an off-corpus block exercising the wizard's random modes (`settType:'random'`, `culture:'random_culture'`, `_randomizePriorities`), so the five `resolveConfig` keys that are pure over the golden corpus are classified over configs that draw them.
- **The other channels, censused by execution:** stream MINTS (`createPRNG`: 35 per run against 8 static spellings) and the HASH channel (`kernel/proseHash.js#pickVariant` — 96 choices per settlement — plus the `fnv1a32` sites the recon named). ⚠ THE INSTRUMENT INSIDE VITEST is yours to design and PROVE: the recon used a `node --import` loader hook, which a vitest file cannot assume. Measure what works here (`vi.mock` with `importOriginal` wrapping `createPRNG` / `pickVariant`? a test-only injection seam?) WITHOUT any edit to `src/kernel/**` or `src/generators/**` — and its control is non-negotiable: with the instrument installed the golden hash of a generated settlement is unchanged, `generatePopulation` draws > 0 unpinned and exactly 0 under full pins. If no in-vitest instrument can be proven without a `src/` edit, that is a BLOCKED finding with what you tried.
- **The mismatch arm PRINTS the fresh literal and FAILS; it never writes** (the `negativeAssertionAnchor` walker's regeneration idiom).
- **Where things live:** C `src/domain/generation/generationForkRegistry.js` (the two tiers' literals; if 75 + ~10 rows cannot fit one leaf's 250 effective lines, a two-leaf split `…Tier1.js` / `…Tier2.js` is PRE-APPROVED — measure, do not squeeze); the static arms under `tests/lint/` (it owes its mutation-coverage row — REGISTER, surgical); the census by execution under `tests/generators/`, its instrumentation under `tests/helpers/`. Measure the census test's wall-clock from the prototype (the recon: ~23 s for the 63 rows) and give it an explicit budget with the measured figure.
- **Preamble rows 10–11:** measure that the new registry module has NO production importer (so it lands zero bytes in the worker, the engine, the eager set and every edge-shared closure) and say how; if anything you create IS an input of a budgeted closure, the packet carries the obligation as row 11 describes.
- **What this packet must NOT do:** edit `src/generators/**` or `src/kernel/**`; touch `pipeline.js`'s partial-pin rule (ARCH-REDERIVE owns it); re-key the three name-keyed choice sites (EM-P1b owns them); assert anything about `rederive`.

## Produce (in your scratch)
`EM-P2.md` (version 3, DRAFT, the twelve template sections; the header's Verified base and Last revalidated left as `__BASE__` for the chair, the preamble hash left as the chair's stamp but VERIFIED and quoted; `Depends on: EM-P0 (LANDED 429ceed54)`), `EM-P2.manifest.json` (the manifest entry: exact paths, requiredSymbols that EXIST now each proven by a quoted command, ≤8 acceptance cases, `checks` as argv arrays one directory per vitest array), `EM-P2.evidence.md` (new — every command and output), and `EM-P2.compile.report.md`.

## Report (final message; also the report file)
DRAFT or BLOCKED; the file paths; the budget table (files, effective lines per leaf, packet total); the in-vitest instrument you chose with its control's quoted numbers; the Tier-1 / Tier-2 literals' row counts and any row where your re-run DISAGREES with the recon; the predicted register moves as DELTAS (lighting: files/titles; mutation coverage); the bundle measurement; every question only the chair can answer. Every claim CONFIRMED (quoted command) or PLAUSIBLE.
