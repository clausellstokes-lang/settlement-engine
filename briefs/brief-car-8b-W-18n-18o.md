# ⚠ AMENDED 2026-09-13 13:3x BY RULING 40 (the owner: THE FAIR COPY DOES NOT CITE ITSELF) — READ THIS BLOCK FIRST, IT OVERRIDES THE TEXT BELOW WHERE THEY DIFFER
- (B) item 1 SURVEY_FRAMES and the `{survey}` slot are STRUCK: do not build them. The spine is a PLAIN STATEMENT of the engine's fact in the archiver's hand; variety comes from the OPENER CLASSES alone. (B) items 2–5 stand.
- (A) the observed face is a BARE OBSERVATION in the passive with no observer named ('No soldier has been seen on the wall at night'); no 'in the survey's time here' / 'it is observed that'. The `observed` mark and its rules stand.
- (B′) item 5 NO FORECAST: the same E2 arm ALSO refuses, on a PLAYER face (the notebook exempt), the strings 'the survey', 'this office', 'the record has', 'entered as', 'set down here', 'so far as the survey' (case-insensitive) — the fair copy never cites itself.
- (C) the first pool's spines: write them as bare statements ('The walls are kept and no soldiers of the town's own stand behind them.' with `{settlement}` kept in spine 1 as the one unit) — no frame slot; the observed face as a bare passive.
- The register row `passage-shape` (slice E #15) is re-pointed to the opener classifier, not to a frames table.

# THREE COMMITS IN ORDER: (A) CAR 8b-W-18n THE ARCHIVER AS WITNESS + THE PUBLIC · (B) CAR 8b-W-18o SURVEY FRAMES + OPENER AND CLOSE CLASSES + THE UNIT CAP · (C) the first pool re-cut under 18n/18o/30

Dock: /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneRW-DEF2 — HEAD `48b58031a`, porcelain clean at start (verify; else STOP). Never touch the main tree except to READ the law. Never push. Stage explicit files. `/usr/bin/grep`. THE CHECKPOINT LAW (files early, commit each step green, the last line names what landed). Read first: the law in plain words /Users/cstokes/Desktop/settlement-engine/docs/rewrite-retro-2026-09-12/THE-LAW-CONSOLIDATED-2026-09-13.md; the binding rulings 27, 28, 29, 30 with their edges in /Users/cstokes/Desktop/settlement-engine/docs/rewrite-retro-2026-09-12/verbatim/BRIEF-ADDENDUM-18.as-signed.md; the last six dock commits (`git log -6`) — cars 18i/18j/18k/18l/18m changed the kernel (ARCHIVER_SOURCE, WEIGH_KIND, PAIR_JOINTS, ROLE_SLOTS, agreeVerb, drawRole, COMPROMISED_*), the grammar, the composer, faceSources, institutionRoles, the card instrument (sections 2b, 2c). Build on them; do not re-derive.

## (A) CAR 18n — THE WITNESS AND THE PUBLIC (rulings 27, 28)
1. `[archiver · observed]`: the `archiver` token (18i) gains the mark `observed` under which it DRAWS ALONE, eligible on every town (`eligibleFaces`); the grammar allows at most ONE observed face per variant and refuses `observed` on any other source; an observed face carries no role slot (the archiver is not a source: it prints in the archiver's own frame — the writers write 'In the survey's time here…' / 'It is observed that…' literally). 2. `public` joins FACE_SOURCES as a SECOND UNIVERSAL token (draws everywhere like `stranger`; `eligibleFaces`; never compromisable — add it to a `NEVER_COMPROMISABLE` set the grammar and `COMPROMISABLE_SOURCES` respect); `institutionRoles.js` gains a `public` roster BY TIER, plural only ('the townsfolk' · 'the people of the town' · 'anyone in the town' · 'every household here' at town+; 'anyone in the village' · 'the whole village' at village; 'the whole hamlet' / 'the whole thorp' below), never an office; `{public}` is a role slot like the others with `n: 'pl'` throughout. 3. The card (prose-mark-card.mjs) prints, beside (2b), the line "THE OBSERVER'S AND THE PUBLIC'S LIST: what neither may claim to have seen" = the same rows as (2b) with that heading (mechanical, no new logic). 4. Pins: the observed face draws alone and never as a pair half; one per variant; public draws on every tier; public never compromisable; the role slot fills plural and the verb agrees; zero text shift on the shipped corpus (byte-identical generated files). One commit.

## (B) CAR 18o — SURVEY FRAMES, OPENER AND CLOSE CLASSES, THE UNIT CAP (rulings 29, 30; and the reconciliation's close-kind arm)
1. `SURVEY_FRAMES`: a closed table in the kernel of frames for the engine's own fact — at least twelve, each a template with a `{fact}` hole, e.g. 'The survey finds {fact}' · 'Entered as {fact}' · 'The record has {fact}' · 'Nothing in the survey puts {contrary}' (skip any frame needing a second hole) · 'So far as the survey can find, {fact}' · 'On the survey's visit {fact}' · 'Set down here: {fact}' · 'By the survey's reading {fact}' · 'The survey has {fact}' · 'Kept, by the survey's reading, {fact-rest}' (only frames whose hole takes the SAME clause; drop the rest) — the writer writes a spine as `{survey} the walls kept and no soldiers of the town's own behind them` and the fill draws a frame seeded on (seed, pool, settlement) with the PAGE-LEVEL NO-REPEAT (reuse 18l's printedRoles set: a `printedFrames` set on the same read); capitalisation at the head; the frame never carries a count or magnitude (a test over the table). 2. OPENER CLASS: a mechanical classifier `openerClassOf(text)` in the kernel — closed, total, pinned: `attributed` (starts with a role/survey/public slot or a known attribution frame) · `place` (a preposition of place: At/In/On/By/Along/Outside…) · `time` (On the nights/In the survey's time/Since/After/Before/When…) · `fronted` (an object noun phrase followed by a subject+verb — approximate: a noun phrase then a comma or a determiner+noun+verb) · `expletive` (It is/There is/There are) · `entry` (Entered/Set down/Kept,/Recorded) · `subject` (default). Computed at generation for every face and stored on the leaf (`opener: 'place'`); the composer keeps the last drawn unit's class per page (a `lastOpener` on the read, like printedRoles) and the draw PREFERS a face of a different class (fall back when none). 3. CLOSE CLASS (the reconciliation, slice D: the veto list is all about how a face ends and has no instrument): `closeClassOf(text)` — closed, total, pinned: `which-tail` (a final clause beginning ', which') · `summary` (a final clause beginning 'and that is' / 'which is to say' / 'so ' as a conclusion) · `antithesis` (a final ', and not X' / ', never X' / 'X and not Y' pattern) · `reassurance` (final 'all is well' / 'nothing to fear' / 'in good order' / 'as it should be') · `question` (a final clause with no mark that reads as a question — approximate: starts with 'whether'/'who'/'what'/'where' after a comma) · `plain` (default). Stored on the leaf; REPORTED at block grain by measure-block.py (add a `closes` histogram) and by the wave gate as information; the E2 ratchet refuses NOTHING new here (ruling 1) — except: `reassurance` is refused by the grammar on any face NOT tagged `compromised` (ruling 26's edge (a) made it lawful only there) — implement that one refusal with a pin. 4. THE UNIT CAP (ruling 30): the grammar refuses a PAIR whose half has more than one sentence (`faceSentenceCount`, 18i); the composer asserts every drawn unit at ≤ 3 sentences (a thrown error in dev, a pin); a per-block `SIMPLE_POOLS` mark (record the first pool as NOT simple; the mark's shape only) asserting ≤ 2. 5. Pins for all; zero text shift on the shipped corpus. One commit.

## (B′) ADDED BY THE RESEARCH RECONCILIATION (rulings 31–39 and the architecture's risk; ride in (B) as the same commit or a fourth commit — your call, recorded)
1. THE PAGE-LEVEL STATE IS ONE SET PER PAGE RENDER (slice E): the no-repeat sets (printedRoles, printedFrames, lastOpener) must be threaded ONCE per page render across desks (today `withFaceSources` mints them per desk entry and generalDeskLines is called at six sites per page-set — the same role can print six times); a role/frame commits to its set ONLY WHEN THE PIECE LANDS (today roleRawOf mutates before fillSlots can drop the piece); determinism: the state is a function of (seed, year, the page's FIXED traversal order) — pin all three (same seed+year → same page over 200 seeds; a dropped piece consumes nothing; the per-page set is shared across the desks of one page).
2. THE SHIFT REGISTER gains two rows: the ROLE draw (`::r<at>::<source>`, car 18l) and the compromised YEAR roll (18m), the year one flagged as the first draw input beyond seed and pool identity.
3. KERNEL LAW 2 re-cut in the docblock and its arm: "the page never STATES a covert fact" (not "nothing a covert piece does may be observable"); the PAIRED-TOWN byte-equality arm becomes the audience-gate arm 18m already added.
4. `COMPROMISED_SYMPTOM_POOLS` moves out of the composer into pool metadata (the generator's poolMeta from an annex mark `<!-- symptom: hall -->` or a census column — your call) so the composer reads a leaf, not a literal.
5. NO FORECAST (ruling 33): the E2 voice ratchet gains a mechanical arm — `\b(will|shall)\s+\w` in a face or note is a refusal ('will' as a noun is rare; document the false-positive path and allow an escape only by the chair's override).
6. MEASURES (ruling 35), in `$SC/kit/rewrite/measure-block.py` (a kit script, not the dock — but the gate prints its output; extend it): within-pool sentence-length spread, same-opener rate, pet-word rate (the research's ration list, `prose-research/PROBE_ALL.md` names it), presence (concrete sensory nouns per hundred words from a closed lexicon — reuse the research's lexicon in `prose-research/` if it exists, else a small one you declare), the close-class histogram (from (B)'s classifier — export it or re-implement in python), attributions per sentence, a perfection flag; and the OPEN SHARE per block (notes/weighings that leave the matter open vs settle it — approximate by the presence of 'may be'/'matter of debate'/'holds no view' forms; report only).
7. THE CARD prints the key's SIBLING RUNGS (the neighbouring values of the fields the key discriminates on, from the census/preimage — section 2d, mechanical).
8. THE CAP'S ARITHMETIC (ruling 37): the grammar refuses a pair whose halves total more than THREE sentences with a weighing, or a half over two sentences; a 2+1 pair is lawful and NOT compoundable (the joint guard already refuses it) and carries no weigh row (refuse a weigh on a 2+1 pair); `simple` caps at two.
9. BYTES: after (B), re-run the leaf byte measurement at the new pin (the architecture's X-F6: bytes per face; the three first-paint ceilings in the size baseline test) and REPORT the numbers in the commit body — change nothing; the ceilings are owner-signed.

## (C) THE FIRST POOL RE-CUT under (A)/(B) and ruling 30
In the annex (DS-DEF-2 → walls with NO force): (i) the hall/guild pair's halves are two sentences each — re-cut each to ONE sentence keeping its sense (hall: 'A man who sits on the council puts the walls' keeping under the military purse and calls it undisputed at the hall.' → use the slot form as the pool already does; guild: one sentence) so the pair is compoundable; (ii) spines 1–3: write the survey fact through the `{survey}` frame slot where the sentence allows (spine 1 keeps `{settlement}` — one unit); (iii) add ONE `[archiver · observed]` face to variant 2 ('On the nights the survey kept, the gate was barred by whoever was nearest and opened by whoever was asked.' — check it against the card's (2b): the Town watch's Gate duty is NOT denied by it; if you judge it is, use 'In the survey's time here no soldier has been seen on the wall.') and ONE `[public]` face to variant 3 ('{public} {v:have} seen the wall kept and nobody on it, and {v:take} it that nobody is coming.' — check verb agreement with the plural roster); (iv) regenerate, census sequence, --check, the full per-commit list (`git show 95760e212` names the suites), the wave gate; render `node scripts/prose-render-page.mjs DS-DEF-2 'Invasion & War: walls with NO force' --towns 2 --world-run --years 3` and paste the drawn lines. The register: the face count grows (4 → 5 on two variants) — re-freeze `face-count-per-variant` by hand (the register is hand-edited; the contract test recomputes) and DECLARE the grow in the body. One commit.

## Return, per commit
COMMIT · FILES · PINS · SHIFT · GATE (exact lines + totals) · HAZARDS · OPEN; for (C) the drawn lines. Final line: HEAD and `git status --short | wc -l`.
