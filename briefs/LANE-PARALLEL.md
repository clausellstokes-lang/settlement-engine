# LANE-PARALLEL — the shared dispatch brief for an Opus lane that works BESIDE the slot, in its own worktree (successor chair, session 7d3418f8, 2026-09-19; the launch message names the lane, its worktree, its branch, its base and its work)

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

**Why you exist.** The owner's lane law (restated 2026-09-19 17:05): the five seats are FOUR WORKING LANES + ONE GATE/SUITE SEAT. The sealed SLOT (`$SP/lane-em-b3b`, on the integration branch `fixes-2026-09-18-consist`) builds ONE editor packet at a time; you are one of the lanes that keeps the other seats working. Your work is NOT an implementation packet (no sealed dispatch, no `implementation:dispatch`): it is a fix, a tooling cure or a measurement the chair has sequenced beside the train (charter `docs/implementation/charters/EDIT-MODE-TRAIN.md`, the amendments section "THE DEFERRED WORK, SEQUENCED"). The chair (Fable) rules and validates; it never edits `src/` or `tests/`; you implement, prove and commit ON YOUR OWN BRANCH; the chair composes your commits onto the integration branch at a train boundary.

**Your worktree and branch** are named in the launch message (the chair has created them, cut from the named base, with `node_modules` installed or symlinked — confirm `git -C <worktree> log -1 --oneline` and `git -C <worktree> branch --show-current` first). Shell state does not persist between Bash calls: `cd "<worktree>" && …` or `git -C` every time. **Scratch:** `$SP/<lane-name>-scratch/` (create it). **FORBIDDEN:** `$SP/consist` (the chair's — terminals run there), `$SP/lane-em-b3b` (the slot), `$SP/read-tip-*`, the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the chair kit (read-only), every other lane's directory — never use another lane's path even as a scratch name. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, `--ignore-other-worktrees`, push, or a checkout of any other branch. `git checkout --` only on a file YOU edited, to withdraw your own edit. zsh: quote variables; explicit file arguments; never an unquoted glob in a vitest command. ⛔ A `VITE_*` switch in `.env.local` is read by vitest AND inlined by `vite build` — never create one.

## The work loop
1. **Read before you touch:** every file you will change, by symbol, in its full enclosing scope; the tests that pin it; the instrument or census that watches it. Navigate by SYMBOL, never by a line number from a brief. A fact without a command is not verified.
2. **Measure first where the launch message says "measured first":** write the measurement to `<scratch>/<lane>.evidence.md` BEFORE the first edit. If the measurement refutes the work's premise, STOP with the smallest measured contradiction — do not adjudicate; the chair rules.
3. **Implement** the smallest change that does the work, matching the surrounding code's idiom. One coherent unit = one commit. Tests that prove a cure are written to FAIL against the uncured tree first (a planted mutant or the pre-cure behaviour), and you quote that red.
4. **⛔ GATED RUNS ARE PAUSE-AND-RESUME.** A GATED RUN is anything that runs vitest, eslint, the typecheck or a build. You NEVER sit waiting for the lock. When you reach a gated batch: (a) finish EVERY edit the batch needs and STAGE your files by explicit path; (b) write `<scratch>/<lane>.lane-resume.md` — the batch's exact commands in order (ONE TEST DIRECTORY PER INVOCATION), the files staged, what remains after the batch; (c) STOP: end your turn with the single line `PAUSED AT THE GATE — <the note's path>`. The chair resumes you ("the gate is yours") when the gate seat is free; you run the batch straight through and carry on; you stop the same way at the next gated batch (a red-first proof is its own batch). Inside your window every vitest command goes through the mutex, SHARED tier, worker-capped: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`. Quote every count line. ⛔ A gate line with no printed test count DID NOT RUN (the mutex's default gives up after 40 polls and EXITS 0 — never drop the two exports). NEVER `npm run check`, `npm run test`, `test:ratchet`, a bare `npx vite build`. Ungated work (reading, editing, plain `node`, `git`, `shasum`) never pauses.
5. **The standing instruments every lane runs in its LAST batch (each has reddened a terminal):** `tests/lint/negativeAssertionAnchor.walker.test.js`, `tests/copy/voiceMechanics.test.js` (no em dash or exclamation point in a `src/` string literal), `tests/lint/mutationCoverageManifest.test.js`, `npx eslint` on every touched file — all GREEN; plus the WHOLE of the directory your change lives under when it is `tests/lint/` (a lane's focused runs never reach the walkers otherwise). `tests/lint/sovereigntyLightingContract.walker.test.js` run ONCE, separately: EXPECTED RED if you add test files or titles — record the measured tuple and YOUR delta; ⛔ NEVER refreeze it (the refreeze is a train's terminal act and the chair's).
6. **Goldens.** `UPDATE_GOLDEN` and `GOLDEN_SHIFT_SIGNED` are FORBIDDEN. `shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before the first edit and after the last: identical. A change that would move a golden is a STOP.
7. **Commit ON YOUR BRANCH by EXPLICIT PATHSPEC** (`git commit -- <paths>`), after reading `git diff --cached` whole. Subject: `<LANE-ID>: <the outcome in one clause>`. Body: what changed and why, every receipt with its count line, the goldens' hashes, the lighting tuple and your delta, judgment calls named (decision · alternative rejected · why · how to reverse). Trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; if your harness adds its own, keep BOTH; never amend. After it: `git show --stat HEAD` names exactly your paths; `git status --short` is empty; if the pre-commit hook rewrote a file, re-run the affected gates and say so.
8. **Report** (final message; also `<scratch>/<lane>.receipt.md`): outcome first; the commit sha(s); every claim CONFIRMED (executed; command + output quoted) or PLAUSIBLE; registers your change will move when composed (as DELTAS); anything noticed and not touched; anything that belongs to the OWNER (schema or persisted shape, security posture, a `src/domain` re-grade, paid-surface behaviour, new capability) reported as a proposal, never built.

**STOP** (uncommitted; `<scratch>/<lane>.STOP.md` with the smallest measured contradiction): the premise is refuted; the work needs a file the launch message did not scope and the chair has not ruled; a golden moves; a gate prints no count; anything owner-gated. Do not improvise around a STOP — the chair rules.

> ZSH WARNING (from EM-B1d's build lane, 2026-09-19): NEVER put the mutex's env assignments in a shell variable and expand it unquoted (`env $M sh scripts/gate-mutex.sh ...`). zsh does not word-split an unquoted expansion: the command dies with `EXIT=2` and prints NO COUNT LINE -- which only the law 'a gate line with no printed test count DID NOT RUN' catches. Spell the assignments inline on every command, or `export` them first.

> ADDENDUM 2026-09-20 (the chair, after run 19 — runs 17, 18 and 19 were ONE family: a lane's new or renamed test file was read by a walker the lane never ran): a CREATE or RENAME of a test file under `tests/<dir>` opts into EVERY walker that governs `tests/<dir>`. Run that directory WHOLE once in your instruments (`tests/security` whole for a security file; `tests/lint` whole always); read the headers of the walkers governing the target directory for fixture-shape rules BEFORE drafting fixtures (the SS4 meta walker reads every quoted `NNN_x.sql` in `tests/security` as a migration reference); a synthetic literal shaped like a real artifact name is declared with the walker's executed marker, never computed past its regex.

> ZSH WARNING, SECOND INSTANCE (FIX-K1's lane, 2026-09-20): the same non-splitting bites GIT — a path list held in an unquoted variable (`git diff -- $SRC`, `git checkout -- $SRC`, `git apply -R $PATCH`) is ONE pathspec: the diff prints nothing, the apply of a 0-byte patch is a no-op, and a red-first cycle then runs against the still-cured tree and reports GREEN. A red-first that comes back green is a STOP, not a pass: check `git status --short` names exactly the files you expect, and grep the tree for the pre-cure text before you believe any red. Spell every path inline.

> TRAILER RULING (the chair, 2026-09-20, after CURE-I): the commit trailer is the one YOUR harness prescribes — an Opus lane signs `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; signing as Fable would misattribute the implementation. Where an older sentence in this brief names the Fable trailer, this ruling replaces it. If your harness adds a second attribution line, keep both; never amend a commit to change a trailer.

> FIXTURE LAW FOR `tests/security` (the chair, 2026-09-20, CURE-I): a fixture that spells a migration-shaped name (`NNN_anything.sql`) carries `// synthetic-migration-name: <why this names no migration>` on its own line or the ONE line immediately above — the SS4 meta walker excludes a declared literal from its existence check AND asserts it absent, so the marker can hide nothing; an undeclared fixture reds the terminal (run 19).

> REPORT-ONLY ARMS (the chair, 2026-09-20, after FIX-C2): vitest 4.1.8's DEFAULT reporter DROPS a PASSING test's `console.log`, so a report-only arm written that way is silent. Write report-only output with `process.stdout.write` (it survives both reporters), and prove any existing report-only arm actually prints before you trust its silence.

> GREP LAW (the chair, 2026-09-20, from EM-B1f's pre-proof): `git grep -E` silently ignores `\s` (no error, no match) — a population counted with `\s` under `-E` is undercounted. Use a POSIX class (`[[:space:]]`) or `git grep -P`; when a grep count contradicts a claim, re-run it with the other engine before calling the claim refuted.

> THREE LAWS (the chair, 2026-09-20, from EM-R0c's and EM-P2's reports): (1) SEED LOOPS — a `tests/domain` (or any `tests/`) CREATE never writes a bare `for (const seed of …) { expect(…) }`: `seedLoopTotality` convicts it and its habitat is shrink-only; collect (`for (const c of rows) …`) then assert once. (2) THE PREAMBLE HASH IS MEASURED, NEVER QUOTED — compute `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` at your read tip and cite THAT; a hash a brief quotes is stale the day the next amendment lands. (3) A HARNESS PRINTS THE TREE IT READ from its environment, never a literal label; and every unquoted shell expansion is a false-green risk (a `set -- $pair` in a verification loop printed UNCHANGED while `shasum` errored) — quote everything.

## Addendum 2026-09-20 08:51 — four hazards (ODQ §934.47 addendum 84)
- `--reporter=basic` is NOT a vitest reporter in this tree: the run dies in a startup error AFTER taking the shared lock and prints NO count line (FIX-P5). Default reporter only; an invalid flag burns a mutex acquisition — the no-count law catches it, re-run.
- A FOREIGN STASH exists in the shared tree (`stash@{0}: On analytics-intelligence-layer`). It is someone's work. No lane lists, applies, drops or clears it; `git stash` in any form stays forbidden.
- The prose manifest golden's provenance pins its RECORDER by file identity (`MANIFEST_RECORDER_FILES` in `tests/helpers/dossierManifest.js`: `dossierManifest.js`, `goldenMasterCorpus.js`, `scripts/prose-rate-corpus.mjs`). Until CURE-J composes (it makes the identity comment-insensitive), ANY edit to those three — a citation re-address included — reds `tests/property/dossierProseManifest.test.js` and forces a golden re-record you may not perform. Leave them alone; record the citation you left with the reason.
- A governed detector source (`scripts/check-observed-shape-readers.mjs`, `scripts/lib/legacy-reader-shape-scan.mjs`, every entry of `scannerToolFiles()`) refuses a single comment byte outside the governed migration bundle (`DETECTOR SOURCE changed since the schema-22 instrument was governed`; TOOL-13b). Do not edit them; a change there is a bundle member the chair mints.
- **eslint runs BARE, never through the mutex** (FIX-L1, 09-20): the shared tier refuses a command that declares no worker cap (`SHARED tier REFUSED — the command declares no worker cap`), and a refused line prints no count and did not run. The mutex is for vitest; `npx eslint <files>` runs on its own.

## Addendum 2026-09-20 12:20 — a FIX lane that edits an EDGE-SHARED INPUT owes the regeneration (ODQ §934.47 addendum 98)
- The EM preamble's row 10 binds parallel lanes too: before you stage, measure every `src/` path you changed against the five bundle metas' own `inputs` lists (`supabase/functions/_shared/*.meta.json`); if any is a member, run `npm run build:edge-shared` and commit the SEVEN `_shared` paths it re-stamps (the two bundles whose inputs moved and ALL FIVE metas — the single-build-window pin forbids restoring the siblings) WITH your change, and quote `tests/edgeFunctions/*.freshness.test.js` + `edgeSharedBundleReproducibility.test.js` green. FIX-P5's F14 changed `src/domain/display/humanizeEngineTokens.js` (an input of `aiCharterBundle` and `aiOutputSchemaBundle`) without it, and run 22 reded four titles after the composition (cured by the chair as CURE-K). A JSDoc-only edit stales a bundle too — freshness hashes raw source text.
- **`git grep -E '\x00'` matches the TEXT `\x00`, not the byte** (TOOL-25, 09-20, proved on a fixture repo: `-E` matched the file containing the four characters and missed the raw NUL; `-P` matched only the raw byte). A byte search is `git grep -P '\x00'` or a node read; never trust `-E` for control bytes. And `tests/lint/controlBytes.test.js` already walks `src` and `tests` whole-C0 — a NUL that reaches a commit there means the lane never ran `tests/lint` whole; run it.
- ⭐ **LAW (ODQ §934.47 addendum 104): every lane that touches `src/` or `tests/` runs `tests/lint` WHOLE once before its commit — ALWAYS**, not only on a CREATE/RENAME: the walkers govern the whole tree (`controlBytes` walks `src` and `tests`; the citation, anchor and lighting walkers scan every file), and a focused batch never shows them your file — three of 2026-09-20's instrument reds were exactly that (FIX-P1b's NUL under `src/lib`; the negative-anchor counts; the citation drifts). The only permitted reds: the lighting census against a register that predates your base (quote what it evaluated) and the provenance title if swept.

### Addendum (the chair, 2026-09-20 13:4x — from TOOL-25's and FIX-D9's receipts; ODQ §934.47 addendum 106)
- **A PIPELINE MASKS A GATE'S EXIT CODE.** `node scripts/check-hazard-registry.mjs | tail -5` reports `tail`'s status, so a fail-closed gate printed under a pipe reads as exit 0. Never pipe a gate: redirect to a file and read the file (`cmd > "$S/x.log" 2>&1; echo "exit $?"; tail -5 "$S/x.log"`), or in zsh read `$pipestatus[1]`. Same family as the no-count law: an exit line you did not capture from the command itself is not a receipt.
- **DETECTION IS NOT A CONTRACT; DECLARATION IS.** Git classifying a file binary by content is a symptom (a raw NUL makes a source file "binary"); `.gitattributes` is the declaration a census may trust. When you add a binary exhibit class (`*.glb`), declare it; when a text file reads as binary, that is a defect to cure, never a row to allow.
- **A LANDED RECORD IS ANNOTATED, NEVER REWRITTEN — and a citation inside one is HISTORICAL.** A survey/audit/review row that records a past measurement is not re-addressed when the source moves; it carries an as-of mark (FIX-C2d's rule) or sits in the records roster. Re-addressing it falsifies the record.
- **ASSIGNED SCOPE IS NOT SCOPE CREEP.** A lane widens nothing on its own (FIX-D9 was right to refuse); the chair may assign an adjacent one-token cure as a further commit at the composition, and the directory of every file the new commit touches then runs WHOLE once before it.
- **AN INSTRUMENT'S OWN COST IS MEASURED (FIX-C2c, 13:5x).** A `tests/lint` whole run died with `FATAL ERROR: Zone Allocation failed - process out of memory` and printed NO count line — the lane correctly claimed nothing from it and read its own diff: a helper re-evaluated `citing.join('\n')` inside a match loop (a fresh megabyte per match) and another sentence-spanned 91,823 lines for a set of ~169. Cured behaviour-identical; the directory then completed in 353 s. The laws: a run without a count line DID NOT RUN, whatever killed it; a walker that reads a document reads it ONCE outside its loops; report the whole-directory wall-clock in the receipt, and say out loud when your instrument moved it.
- **A CLEAN APPLY IS DRY-RUN, NEVER REASONED (FIX-P1, 14:0x).** A lane predicting that its commit "applies clean" at the tip dry-runs the cherry-pick in a throwaway detached worktree (`git worktree add --detach <scratch> <tip>`; link `node_modules`; pick; compare file sets and blobs; remove the worktree) and quotes the result. Both of FIX-P1's conflicts were the same shape — an import block two lanes widened independently — and a read of the hunks called them clean.

### Addendum (the chair, session 4a1823e2, 2026-09-20 15:1x — from FIX-P9's STOP; ODQ §934.47 addendum 114)
- **THE OWNER'S LANE LAW AS RESTATED 2026-09-20 ~14:2x: FOUR lanes live, and ONE lane (or chair terminal) holds the gate at a time.** Where an older sentence in this brief speaks of a shared tier with two holders, this replaces it: you are handed the gate alone, you run your batch straight through, and you release it in your final line. The mutex spelling is unchanged (SHARED tier, worker-capped, the two exports inline).
- **A DERIVED-ARTIFACT DOOR THAT THE FREEZE REGISTER EXCLUDES BY NAME IS NOT A GOLDEN DOOR — BUT A LANE STILL NEEDS THE CHAIR'S RULING BY NAME TO USE ONE.** `tests/fixtures/.golden-freeze-register.json` carries `excludedEnvSpellings`: the env spellings that are NOT world fingerprints, each with its written reason (`UPDATE_ORGANIC_SAMPLES` — 'a build-artifact byte golden of the docs/samples HTML. It pins a RENDERED PAGE, not a generated world; it moves when the sample builder moves'). The blanket 'every `UPDATE_*` door is FORBIDDEN' protects frozen goldens and shrink-only baselines; an excluded spelling is a regeneration by the artifact's own writer, the same class as `gen:compendium-data` or `build:edge-shared`. **RULED for FIX-P9 (`UPDATE_ORGANIC_SAMPLES` only), lawful under six conditions:** (1) RED-FIRST — with the source change applied and WITHOUT the door, the drift guard reds naming exactly the expected sample; a green is a STOP; (2) the door runs for that ONE test file, through the mutex; (3) the artifact's diff is proven to be EXACTLY the expected substitutions, COUNTED (nothing else moved; every sibling sample byte-identical; `git status --short` names only the expected files); (4) the plain re-run is green and the directory whole is green; (5) the refreshed artifact is committed in the SAME commit as its source change, so no commit is ever red on the drift guard; (6) the two §6 goldens re-hashed unmoved, and the commit body names the door, the register row that excludes it, and the substitution counts. Any OTHER excluded spelling is asked of the chair by name first.
- **A NOTE'S PREDICTION IS RE-MEASURED BY ITS SUCCESSOR (FIX-P9, 15:0x).** The dead agent's note predicted a grep would return one line after the cut; measured, RENDERED sites went 1 → 0 while matching LINES went 4 → 6, because the cure's own comments quote the old glyph. Count the thing the claim is about (rendered sites), never lines that merely mention it.

### Addendum (the chair, session 4a1823e2, 2026-09-20 15:2x — RUN 23's two causes; ODQ §934.47 addendum 114)
- **FIND THE TESTS THAT NAME YOUR FILE, AND RUN THEIR DIRECTORIES WHOLE.** RUN 23 reddened on a walker in `tests/data` that convicts `src/lib/anonForkSalt.js` — the lane had run `tests/lib`, `tests/components` and `tests/lint` whole and never met it, because a walker can live in ANY directory. Before every commit that touches `src/`: `git grep -l -F '<your file's basename without extension>' -- tests` (and once more for each exported symbol you changed the CALL SHAPE of), and run the WHOLE directory of every hit once. `tests/lint` whole stays mandatory; it is no longer sufficient.
- **A GOLDEN'S RE-RECORD OWES A CENSUS OF EVERY LITERAL COPY OF THE OLD HASH.** CURE-J's signed, provenance-only re-record moved the prose-manifest golden's bytes; EM-B1e's acceptance arm A4 carried its own literal of the old sha and reddened three compositions later. The freeze register (`tests/fixtures/.golden-freeze-register.json`) is the ONE pin; a test that must assert a frozen artefact's bytes READS the register's row for that surface. Any lane that moves a golden through the owner's door greps `tests/`, `src/` and `scripts/` for the first 12 hex of the OLD hash before it closes.
- **A RED `&&` CHAIN BLINDS EVERYTHING AFTER IT — THE CHAIR MEASURES THE BLINDED STEPS SEPARATELY.** RUN 23 stopped in `test:ratchet`; `build` and `verify:dist` were then run bare in the consist at the same tip (green: `STRICT DIST OK — 59 files, 543 tests`; the generation worker measured 1,401,208 B against a ceiling of 1,401,208 — ZERO slack, so any packet adding a generator byte must buy its own headroom).

### Addendum (the chair, session 4a1823e2, 2026-09-20 15:4x — THE OWNER'S BATCH GATES; ODQ §934.47 addendum 117)
- **THE OWNER (verbatim): "can you not combine some different areas into one gate if they are queued in line to preserve time?" — YES: BATCH GATES.** When the chair tells you your lane is a MEMBER OF BATCH N, you do NOT run your own whole-directory batch at your stale base. An Opus COMPOSER lane applies your work onto a throwaway batch branch cut at the green integration tip (your commits by cherry-pick; your uncommitted work by an exported patch and `git apply --3way`, one commit per lane under YOUR prepared subject and body), runs any FOCUSED red-first proof your note still owes in sequence (tests first → red → cure → green), and the chair then proves the WHOLE batch with ONE full check at the composed tip. Your note therefore must always carry, ready for a composer who has never met you: the exact pathspec per commit, the commit messages as files in your scratch, which focused red-firsts are ALREADY executed and quoted and which are STILL OWED, the registers you move as deltas, and every file you staged with its SHA-256. A lane that gates ALONE (a sealed Edit-Mode build; a golden-door lane; a lane re-cut after base drift) still follows verb 4's pause-and-resume exactly as before.
