# LANE: CAP-HORIZON-909 — two measurements the chair commissioned under the owner's 2026-09-07 delegation (zero product bytes)
⟦Chair: Fable 5.1 (session 405b5e7e) · Lane: Opus 5 (`model: "opus"`) · measurement lane · dock `$SC/laneB6` at the SOAK-HONEST-909 landing tip (the receipt must carry the per-year series so `capacity_plateau` can grade it — do NOT run before that landing; the chair dispatches you after it) · ⛔ CPU-heavy: quiet-window law; both runs through `sh scripts/gate-mutex.sh --run -- …`, DETACHED (nohup; TRUE_EXIT captured in-shell to a log; pid file), never as a foreground tool call⟧

## M1 — THE 600-YEAR HORIZON (§907 owner row (c), decided: an instrument reading of a transient until measured)
Run the `research-lit-4s` shape at 600 years (`whole-world-soak.mjs --years 600 --settlements 4 --seed realm-scale-research-lit-4s-600y-4s-seed1 --lighting demographicsEnabled=true`, output under `$SC/capacity-horizon/`; ~52 min at the measured 0.635 s/settlement-year — state the measured rate you get). Then: the realm's `loadRatio01` by decade to year 600; the settlement shapes (`settlementShapeOf`) and `capacity_plateau`'s verdict per settlement at 600; whether the realm ever enters the row's window [0.6, 1.05] and stays; runaway/floor/bifurcation figures. Answer ONE question with figures: does the fixture SETTLE (where, and when), or is 0.4787 at year 300 a point on a curve still moving at 600? Do not touch any dial; do not rule model-vs-window — report which the figures support and why.

## M2 — THE 12-SETTLEMENT TIMING PROBE (§907 owner row (d), decided: re-price by measurement)
Run `--years 30 --settlements 12` under the same overlay (~4 min if the cost is linear in settlements), capture run A's wall clock per settlement-year, compare with the 4-settlement figure (0.635 s) and with the 300-year run's 762.3 s, and state the measured superlinearity exponent (PLAUSIBLE if from two points; say so). Then price the terminal 300 y × 12 s LIT cell and its DARK twin from the measurement, with the two-point caveat. That figure replaces the design's 35 h estimate in the chair's §909 row.

## LAWS
Zero product bytes; nothing signed, nothing frozen, no register `--write`. Receipt `$SC/receipt-cap-horizon-909.md` (STATUS at arrival; re-written at each milestone; CONFIRMED/PLAUSIBLE per figure; a RETROVALIDATION ROW); raw figures under `$SC/capacity-horizon/`. Quiet-window law before each launch (load-1 < 4.0, zero vitest workers, three consecutive minutes; samples in the receipt). A 143/137 exit is a harness kill, not a verdict. Every timestamp from `date`.
