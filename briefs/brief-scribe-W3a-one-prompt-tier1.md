# Brief — SCRIBE W3a: ONE PROMPT, THE CARD THAT ANSWERS THE REFUTER, TIER 1 WIRED
(chair Fable 5.1, 2026-09-14 ~12:3x; the owner: "fix them comprehensively and exhaustively"; the corpus is PAUSED and the Scribe is the only lane)

You are an implementer in the dock `$SC/kit/lane-scribe` (SC = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad`). It is a worktree of the SettlementForge repo at `544ae0339` on the Scribe lineage. Porcelain is 0 at dispatch and must be 0 after every car. NEVER `git stash`, `git checkout -- <file>`, `reset --hard`, never push, never touch `package.json` (a mint trigger), never add a dependency, `deno` only with `--no-lock`. Commit each car by explicit pathspec (`git add <files>` then `git commit -F msg`), and run `git diff --cached --stat` before every commit. Read the WHOLE of every file you edit before editing it.

READ FIRST: `docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md` §3, §4, §6, §10 (the contract; `git show HEAD:` if the checkout is stale — in this dock it is not); `$SC/kit/briefs/SCRIBE-RULINGS.md` (rulings 1–24); `$SC/kit/briefs/SCRIBE-SIM-2026-09-14.md` (THE SIMULATION THIS BRIEF CURES — read it whole); `supabase/functions/scribe-render/{scribeCore.ts,index.ts,voice.ts}`; `src/domain/prose/{townCard.js,refuteUnit.js,scribePage.js,moveGrammar.js}`; `scripts/scribe-bundle.mjs`; `$SC/kit/scribe-harness/{lib/brief.mjs,render-town.mjs,pilot.mjs,simulate.mjs}`; the tests `tests/domain/townCard.test.js`, `tests/domain/refuteUnit.test.js`, `tests/lint/scribeBundle.walker.test.js`, `tests/lint/scribeFiniteSemantics.walker.test.js`, `supabase/functions/scribe-render/scribeCore.test.ts`, `index.test.ts`.

## THE FINDING THIS CURES, IN ONE BREATH
The product (`scribeCore.ts`) and the pilot harness (`lib/brief.mjs`) build TWO DIFFERENT PROMPTS with two different output schemas. The simulation ran the harness's fork; the product's prompt has never been exercised. Under the harness's prompt, an Opus seat wrote 15/15 lawful units of which 5 fell to the corpus — 4 for a move-order rule the model was never told (the card carries the spine but not its LEVEL1 order id) and 1 for a true C4 totality; the Q arm withheld 6 of 15 second sentences because the card ground gives it no field paths to license them; both an Opus and a Sonnet seat added a MECHANISM the card does not hold (a fine and a debtor's cell; a court backlog) which no tier-0 arm can see; and the brief left the model guessing on stance, the DM register, slot fill, and whether to imitate a corpus line that breaks the law. The tier-1 checklist that would catch the mechanism class exists (`buildTier1Checklist`) and is NOT called anywhere.

## THE CARS, IN ORDER (one commit each; the gate after each; stop and report if a gate stays red)

### CAR 1 — ONE BRIEF BUILDER, SHARED (the fork cured)
- NEW `src/domain/prose/scribeBrief.js`, a PURE domain leaf (no Node API, no file read, no clock, no randomness — it is bundled for Deno and imported by a browser-adjacent test). It exports:
  - `SCRIBE_OUTPUT_SCHEMA` — MOVE the product's schema here verbatim (`units[{blockId, poolKey, vid, spine, faces[], notebook[]}]`, closed at every level). The harness's flat `{stance, source, pair, text}` schema is DELETED; the design's §3.2 shape is this one.
  - `buildScribeBrief({voice, exemplars})` — THE GLOBAL CACHED BLOCK, byte-stable given its two inputs: the framing lines; the VOICE; the MECHANICAL BARS; the UNIT RULES (the product's, amended per CAR 2's wording); ⭐ THE PLAUSIBLE-ADDITION BAR (new): "Add no mechanism, practice, cause, procedure, custom, price or arrangement the card does not name. If the card says a court and a prison, you may not say a fine, a debt, a backlog or a bribe. A line that explains HOW a thing works where the card only says THAT it stands is refused by the second reader and falls to the corpus."; ⭐ THE TWO-LADDERS NOTE (new, static truth): "The posture badge word (Fortress · Well-Defended · Defensible · Lightly Defended · Vulnerable · Undefended) and the readiness band (STRONG · ADEQUATE · WEAK · CRITICAL) are TWO ladders over ONE score and both are the engine's. Write to the pool key's band; never name the badge word and never reconcile the two. A funding note reading 'Upkeep underfunded … at 97%' beside 'Economic Backing: Well-funded' is likewise two true readings of two fields; write to the pool key and neither number."; ⭐ THE CORPUS LINE'S STANDING (new): "The corpus line given with a pool is the CLAIM you must keep and the line that ships if yours is refused. It is not a model of the law: where it breaks a bar above (a which-clause close, a colon, a number error in a fill), do not imitate the breach."; then the EXEMPLAR PACK under its own heading; then `cachePadding` is no longer needed (the pack clears the floor) — keep the function, assert in a test that the built brief exceeds `CACHE_MIN_PREFIX_TOKENS` without padding.
  - `buildTownBlock(card)` — THE PER-SETTLEMENT CACHED BLOCK: `card.town` as pretty JSON under "THE TOWN, WHICH DOES NOT CHANGE BETWEEN TABS" (the harness's hoist, now in the product too), plus the roster rule sentence the harness had.
  - `buildScribeUserTurn({card, record, guidance})` — THE VOLATILE TURN: the product's shape (epoch, then the record if any, then THE LINES TO WRITE as `poolBrief` rows, then the game master's instructions LAST and ranked below the law, verbatim from the product), with `poolBrief` extended per CAR 2 and with the town REMOVED from the turn (it is in the town block).
  - `buildTier1Checklist(units, card)` — MOVED here from scribeCore.ts and extended per CAR 4.
  - `TIER1_ANSWER_SCHEMA` — per CAR 4.
- `scripts/scribe-bundle.mjs`: add `scribeBrief.js` to `SCRIBE_ENTRIES`; rebuild; `--check` green; the bundle walker test's freshness and agreement arms extended to the new export.
- `supabase/functions/scribe-render/scribeCore.ts`: DELETE its own schema, MECHANICAL_BARS, UNIT_RULES, `buildScribeBrief`, `poolBrief`, `buildScribeUserTurn`, `buildTier1Checklist`; import them from `../_shared/proseKernel.bundle.js` and re-export for `index.ts`. Keep `cachePadding`, `parseScribeUnits`, `judgeUnits`, the interfaces.
- THE EXEMPLAR PACK IN THE PRODUCT: the edge function cannot read the kit. Add `docs/content/scribe-exemplar-pack.md` = a copy of `$SC/kit/rewrite/recut/EXEMPLAR-PACK.md` ⛔ AFTER YOU HAVE READ IT WHOLE AND CONFIRMED it quotes no passage of any named author's copyrighted text (author NAMES as guideposts are fine; a quoted passage is not — strip any such block and say so in the commit). Then `supabase/functions/scribe-render/exemplars.ts` = `export const SCRIBE_EXEMPLARS = "<the md as a JS string>"` + `SCRIBE_EXEMPLARS_CHARS`, GENERATED by a new `scripts/scribe-exemplars.mjs` (with `--check`, byte-equal, the voice.ts idiom), and a Deno test that the length pin holds. The harness reads the same md from the dock (`${DOCK}/docs/content/scribe-exemplar-pack.md`), never the kit copy.
- `index.ts` `callAnthropic`: `system: [ {text: brief, cache_control: {type:'ephemeral', ttl:'1h'}}, {text: townBlock, cache_control: {type:'ephemeral', ttl:'1h'}} ]` — two breakpoints (the API allows four). The docblock says why.
- THE HARNESS (`$SC/kit/scribe-harness/`, kit-only, never the product): `lib/brief.mjs` keeps ONLY the file readers (`voiceText` from the workflow, `exemplarPack` from the DOCK's md, `staticCard`, `DOCK`/`KIT`) and re-exports the builders from `${DOCK}/src/domain/prose/scribeBrief.js`; `render-town.mjs`'s `buildRender` returns `{brief, townBlock, turn, card, record}`; `callModel` sends the two system blocks and passes `output_config.format: {type:'json_schema', schema: SCRIBE_OUTPUT_SCHEMA}` via `client.beta.messages.create` (drop zod and `zodOutputFormat`; validate with `parseScribeUnits` from the dock); `refuteAll` becomes `judgeUnits` from the dock's `scribeCore` logic — since scribeCore.ts is Deno TypeScript, MOVE `judgeUnits` and `parseScribeUnits` into `scribeBrief.js` too (pure JS) so the harness and the edge function run ONE judge; `simulate.mjs build` writes `brief.md`, `town.md`, `turn.md`, `schema.json`, `card.json`; `simulate.mjs judge --response` runs the one `judgeUnits`. Add a harness check `node lib/pin.mjs` that asserts the dock's `voice.ts` string === the workflow's VOICE (the two transcriptions cannot drift) and prints both lengths.
- GATE: `node scripts/scribe-bundle.mjs --check`; `npx vitest run tests/lint/scribeBundle.walker.test.js tests/lint/scribeFiniteSemantics.walker.test.js tests/domain/townCard.test.js tests/domain/refuteUnit.test.js tests/lib/scribeArtefact.test.js tests/lint/composeStateProseFence.test.js`; `deno test --no-lock supabase/functions/scribe-render/`; `deno check --no-lock supabase/functions/scribe-render/index.ts`; then `node simulate.mjs build --seed render-town --tab defense` in the harness prints the three files' sizes. Record every figure in the commit body.

### CAR 2 — THE CARD ANSWERS WHAT THE REFUTER ASKS
- `townCard.js` `poolRow`: under `unit`, add `order: {id, moves, licences}` = `orderIdOf(classifyMoves(spine))` and `LEVEL1_ORDERS[id]` (`''`/`[]`/`''` where none) — import from `moveGrammar.js`, which is already in the bundle's reach (verify with the bundle's `inputs`). Key-sorted, byte-stable.
- `poolBrief` (now in `scribeBrief.js`) prints per pool: the stance (`angle`), the marks, the slots with their FILLS on this town ("write the fill, not the token: {settlement} is Spitzplatz here"), THE FIELDS with their values (`fields[]`: "the second sentence of a unit, if there is one, must rest on one of these fields and name it in its own word, or it is withheld"), THE ORDER: "the corpus spine realises the move order `<id>` — `<moves joined by ' then '>` (`<licences>`); keep that order in your spine", each face position's source AND the roster it may draw from (`faceRoles`), the compromised roll if it speaks, and the corpus line (spine + faces + notebook rows if `unit` carries any dm-only rows — check what `variant.wordings`/`sources` carry for dm-only faces and print them under `notebook`).
- Answer the four guesses in the UNIT RULES: STANCE — "`spine` is always the pool's stated fact in the archiver's own hand; a `face` speaks through the source named at its position; where the card lists no face, `faces` is `[]`"; THE DM REGISTER — "audience `dm`: `notebook` carries the archiver's private rows, one per dm-only face the card lists, in the notebook register the VOICE describes; audience `player`: `notebook` is `[]` and nothing private appears anywhere"; SLOT FILL — "write the rendered words with the card's fills applied; name the town at most once per unit and never as the first word of two units on one page"; THE CORPUS LINE — the standing sentence from CAR 1.
- Tests: `townCard.test.js` — a pin that every pool's `unit.order.id` equals `orderIdOf(classifyMoves(unit.spine))` over the test's towns, and that the card stays byte-stable across two builds; update any fixture the new key moves and SAY which.
- GATE as CAR 1.

### CAR 3 — THE REFUTER'S CARD GROUND
- `refuteUnit.js` `groundOfCard`: the Q arm's `reads` are today `pool.static.reads` — WIRING STRINGS like `scoreBand(readinessScore) (via READINESS_ROW_POOL in defenseStateProse.js)`, which `claimsField` can never match to a word of prose. Add the pool's FIELD PATHS (`pool.fields[].field`) to the entry's `reads` and pass `fieldSynonymsFor`'s vocabulary (`src/domain/prose/fieldSynonyms.js`, already in the estate; check it is bundle-reachable and pure) as `entry.vocabulary`, exactly as `proseEntryContradiction.walker.test.js:798` and `prose-wave-gate.mjs:1712` do for the corpus. The wiring strings stay for A13/C3 (they read them). Docblock: the arm asked the corpus's question with none of the corpus's inputs.
- CORPUS-DIFF order-loss STAYS `FAIL`. Add to its finding description the order the corpus realised and the licence, so a reader of the verdict sees what was asked.
- Tests in `refuteUnit.test.js`: a POSITIVE control (a second sentence naming the pool's field in its synonym passes Q) and a NEGATIVE control (a second sentence naming nothing is withheld with `reads [<the field paths>]; none claimed` in its detail).
- GATE as CAR 1 PLUS the corpus's five per-commit suites (`tests/data/dossierStateProseProjection.contract.test.js tests/lint/proseComposed.walker.test.js tests/lint/proseMoveGrammar.walker.test.js tests/domain/stateProseKernel.test.js tests/domain/composeStateProse.test.js` — the recorder-blind pin must read EXACTLY 2756) and `node scripts/wiring-census.mjs --check`, because `refuteUnit.js` shares walkers with the corpus gate.

### CAR 4 — TIER 1 WIRED
- `buildTier1Checklist(units, card)` (in `scribeBrief.js`): the five questions plus **6. MECHANISM: does it describe how a thing works — a practice, a procedure, a cause, a custom, a price, a debt, a fine, a backlog — where the facts only say that the thing stands?** and **7. SAME PAGE: does it contradict a machine line on this page?** (the card's `page` rows of kind `machine`/`badge`/`row` are printed as THE PAGE). The facts printed are `card.town`, `card.epoch`, and per line the pool's `fields` with values. Lines are numbered per (unit, row) so an answer maps back to a unit.
- `TIER1_ANSWER_SCHEMA`: `{answers: [{n: integer, certainty: 'yes'|'no', quantifier, scope, actor, forecast, mechanism, samePage}]}` closed at every level.
- `applyTier1(units, answers)` (pure, in `scribeBrief.js`): any `yes` on any row of a unit drops the unit with arms `T1-CERTAINTY` … `T1-SAMEPAGE`; returns `{kept, verdicts, dropped}` in `judgeUnits`'s shape so verdict rows merge.
- `index.ts` `callModel`: after tier-0 leaves `kept.length > 0`, a SECOND `callAnthropic` with the SAME two cached system blocks and the checklist as the user turn, `output_config.format: {type:'json_schema', schema: TIER1_ANSWER_SCHEMA}`, `max_tokens` 4000, adaptive thinking; usage summed into the same `usage` totals (meter both calls as one event; add `calls: 2` and `tier1Dropped` to the response). A tier-1 provider error is NOT a render failure: log it, keep tier-0's `kept`, and set `tier1: 'skipped'` in the response — the design says tier 0 is the floor and tier 1 the second reader, and a dossier is not blanked because the second reader was unavailable. Both calls inside the one 55 s budget; the timer wraps both.
- The harness: `render-town.mjs` runs tier 1 live when a key exists; `simulate.mjs` gains `tier1 --response <units.json>` which writes `tier1.md` (the checklist prompt) and `tier1-schema.json`, and `judge --response <units.json> --tier1 <answers.json>` which applies both readers and prints the merged verdicts.
- Deno tests: the checklist asks seven questions; a `yes` on question 6 drops the unit; a tier-1 provider 500 leaves tier-0's kept units and marks `tier1: 'skipped'` (fake `anthropicFetch`); the metering sums two calls. Vitest: `applyTier1` positive/negative controls.
- GATE as CAR 1.

### AFTER THE CARS
Report per car: sha, files, the gate lines VERBATIM (test counts, the pin, the bundle line, the deno lines), the built prompt sizes from `simulate.mjs build` (brief / town / turn chars and the 4-chars estimate), and every judgment you made in the form `JUDGMENT: chose X over Y because Z`. Anything you could not do: name it and why. The chair re-runs the simulation on the unified prompt after you report; do not seat model agents yourself.

## LAWS THAT BIND (from the estate; do not restate them from memory, read them)
FINITE-SEMANTICS (prose is an OUTPUT; the `scribeFiniteSemantics.walker` fences what may read `settlement.prose`); the fence test (`composeStateProseFence`, HEADLESS_READERS); no product `package.json` byte; `settlementSlice.js` is size-baselined at tolerance 0; `deno` with `--no-lock` only; the bundle's `--check` is byte equality; the VOICE is transcribed, never paraphrased.
