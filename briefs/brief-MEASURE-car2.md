# Brief — MEASURE train, ARCH car 2: THE BYTE RATCHETS — the prose leaves get their own monotone size guard; the lazy chunk and first paint are pinned where the estate already measures them

Seat: Opus 5 — implementer. Receipt first: `$SC/receipt-measure.md` § CAR 2. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`. Dock: `$SC/laneMEASURE` at car 1's tip.

## Read first
`$SC/arch-prose/ARCH-COMPOSED-PROSE-v2.md` **§10** (where the bytes live; the three first-paint ceilings that already exist — raw 1,048,000 / gzip 337,000 / Brotli 283,000 in `tests/build/vendorPdfLazy.test.js`; the STALE-DIST POLICY: size assertions read only under `VERIFY_DIST=1` after a build; `scripts/.size-baseline.json` is a max-LINES instrument whose `ceilingFor()` is null for `src/data/**` — NO byte row may go there), §12 row 2, §16; `tests/build/vendorPdfLazy.test.js:35-90, 545-610`; `tests/lint/sizeBaseline.test.js`; `eslint.config.js:79-100`; `vite.config.js:865-882` (the chunk rule `if (id.includes('/src/data/')) return isEagerData(id) ? 'data' : 'data-lazy'`).

## What to build
1. **`scripts/.prose-byte-baseline.json` (new) + `tests/lint/proseCorpusBytes.test.js` (new):** raw and gzip bytes per SOURCE leaf — the six `src/data/dossierStateProse/*.generated.js` and the causal leaf — a source read, lawful pre-build; MONOTONE (shrink-only unless a declared row names the car and the delta); the ceiling per leaf set from ARCH §10's CEILING arithmetic (≈ 2.8 MB raw total at the ceiling, apportioned per leaf by today's share — print the apportionment); a planted 1 KB appended to a leaf copy must red; the baseline's docblock carries the declared-row idiom.
2. **`tests/build/vendorPdfLazy.test.js` EXTENDED under `requireDistRead` / `VERIFY_DIST`:** a row for the `data-lazy` chunk's raw and gzip bytes with its own ceiling, and a CHUNK-MEMBERSHIP assertion — every `src/data/dossierStateProse/*` module resolves into a `data-lazy-*` chunk and NONE into `index-*`, `data-*` (eager) or any first-paint chunk (the eight closure files); a planted eager import (in a copy) must red in the absence half that runs without a build. The three existing ceilings byte-untouched (assert their literals).
3. **Assert the two consumers are untouched:** `scripts/.size-baseline.json` and `tests/lint/sizeBaseline.test.js` byte-identical to the base (a test-time `git diff --quiet` or a sha pin in the receipt).
4. **The receipt:** per leaf raw/gzip today and the ceiling; the chunk membership at a fresh build in the dock — ⚠ RELINK LAW: before ANY vite build in a dock, every `node_modules/<pkg>` must be a SYMLINK (a cloned package bundles twice and moves first-paint bytes — memory `a-docks-symlinked-node-modules-poisons-the-edge-bundle-build`); print `ls -la node_modules/immer node_modules/seedrandom` before the build; the closure figure must read the §914 tip's (1,042,122 raw at f3ab08f51; re-measure); the plants' reds.

## Discipline, fences, proof
As car 0's (runner count; focused; ONE whole `tests/lint`; strict ratchet on its ceiling; voice E2; OSR/PN; typecheck; cast walkers). ONE vite build is chartered for the chunk-membership measurement (`npx vite build` in the dock, links verified first); never the whole suite; never `--write` on any register but the lighting door (2b) if a title moved. Change only the four files named, the receipt, `$SC/measure/*`. No leaf byte moves (this car pins; it does not grow). Commits `MEASURE car 2: the byte ratchets — <clause>` + `car 2b`; explicit staging; porcelain 0 (a `dist/` build is untracked/ignored — confirm `git status --porcelain -uall` is empty after); the three trailers (`Seat: Opus 5 — Fable-unvalidated` / `Lane: MEASURE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`). Return: the shas; the per-leaf table; the chunk row; the closure figure; the plants' reds; the consumers' sha pins; the whole-tests/lint summary; anything refused.
