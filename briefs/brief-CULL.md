# LANE: CULL — TUNEREG Car 5 (HD-22), the ~113 zero-consumer tuning tables
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · released by the §893 carve-out amendment.

## WHY YOU EXIST, AND WHY THIS IS NO LONGER GATED
This car was owner-gated as "data deletion". The owner amended the carve-out line on
2026-09-04 and the chair re-cut it on ONE test: **can a later Fable retrovalidation actually
undo this?** Deleting dead tables from a versioned repository is undone by `git revert`; no
customer data exists; the tree is pre-launch. So it is TAKEN. Deferring it is strictly worse,
because it would put a src-moving train inside the frozen window.

## THE ACT
Delete the tuning tables that TUNEREG Car 1's `dependents` column measured at **zero consumers**,
so that the tuning surface the owner eventually signs is the surface that actually drives the world.

## ⛔ THE DISCIPLINE THAT MAKES THIS SAFE — read before your first deletion
1. **MEASURE PER-EXPORT, NEVER PER-FILE.** §882.15b's J-6 correction exists because a file-level
   dead-set is wrong whenever one live export shares a file with dead siblings. Your denominator is
   EXPORTS, not files.
2. **RE-MEASURE; DO NOT TRUST CAR 1's LIST.** It is a claim, and a claim decays. Six landings have
   happened since. For every candidate, search well past the symbol name: string spellings, aliases,
   re-exports, dynamic/computed access, test-only references, annex and doc citations, and the
   walker/scanner families that read prose. A table cited only by a walker is NOT dead.
3. **THE RECEIPT NAMES EVERY TABLE REMOVED**, with its measured consumer count and the searches that
   established it. A cull whose receipt says "113 tables" and not which ones is not reviewable and
   will be reverted wholesale.
4. **STOP AND REPORT rather than delete anything whose deadness you cannot demonstrate.** An
   under-cull is a docket row; an over-cull is a silent capability loss the walk may not catch.
5. Expect register consequences and PREDICT THEM IN WRITING before any instrument runs: the tuning
   inventory is a MEASURED freeze and only a LANDING refreezes it — you do not take that act.
   ⚠ The tuning REGISTER is a different, hand-authored file; do not conflate them.

## SCOPE FENCE
You delete dead tables and nothing else. No tuning VALUE changes — values remain the owner's and are
signed last. No consolidation (that is Car 4). No renames.
