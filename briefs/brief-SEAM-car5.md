# Brief — SEAM train, ARCH car 5: THE NEW ARMS on the landed instruments — A0a/A0b, A1, A2, A3, A4 + the lint ban, A5, A6, A8, A9, A11, A12, C7, `composedOrderIdOf`, the composed walker, `check-pair` in `scripts/`

Seat: Opus 5 — implementer. Receipt: `$SC/receipt-seam.md` § CAR 5. Dock: `$SC/laneSEAM` at car 4's tip. Read first, whole: ARCH **§8.4** (the gate table — every property, the existing instrument, the new arm, the channel), **§4.6** (the coherence pass: the object-class guard; A1; C7 with two planted convictions; the echo bound A11; the position budget A12), **§4.3** (A4 determinism; the locale ban), **§2.5**/**§2.6** (A6 claim-equality with the LONGER arm of `check-pair` SUPPRESSED for faces; A9 the seam contract; A5 sibling distance on the estate's own ruler with a REPORT → FAIL floor measured on VARIETY), **§5.3** (A8 the registry — `TURN_KEY_REGISTRY` tiers 1a–1c as a frozen list in the generator; an `EXPLAINS` outside it throws), **§12 row 5**, **§16**; the island (`src/domain/prose/*` — the walkers your arms extend), `$SC/prose-research/check-pair.mjs` (lands in `scripts/` with LONGER switchable), the MEASURE receipts (the corpora your arms run over: DRIFT for the composed walk, VARIETY for the sequences and the sibling floor).

## What to build
Every arm in ARCH §8.4's "new or extended" column, each with: a planted control it must convict, a clean control it must pass, NOT-EXECUTABLE where its input is absent (never `[]`), and its channel (FAIL / WITHHELD / REPORT → FAIL) as the table states. In particular: **A0a** (projector: `reads ⊆ tests`) and **A0b** (walker: the text claims every declared field and no other; three plants — over-claim, under-claim, the implicit negation on `invasionRowPoolKey`'s `walls with citizen militia`); **A1** (`typedFactsOf` overlap across pieces = restatement; disjoint classes on one noun = conflict); **A2** (a `consequence`/`tension` joint against the relations leaf — the spine's PRIMARY field as an endpoint, direction read; with the join EMPTY today every such joint is WITHHELD — assert that state); **A3** (wall 5 as an arm); **A4** (repeat-call identity over VARIETY; the lint ban on `localeCompare`/`Intl`/`toLocale*` in the composer); **A5** (sibling distance — the floor measured on VARIETY: REPORT until the first wording sets exist, with the arm's shape proven on a fixture family of four); **A6** (parent → face claim-equality via `check-pair` with LONGER suppressed; slot/mark byte-equality); **A8**; **A9**; **A11** (echo: ≤ 1 modifier mount per fact per page-set, none where it spines — over car 0's fact index); **A12** (the position budget as a desk test); **C7** (cross-block sibling agreement on a page-set; two plants; REPORT before FAIL); **`composedOrderIdOf`** (LEVEL1 ∪ LEVEL2 over `spine.order ++ modifier moves`; the share outside both printed); **`src/domain/prose/composedWalker.js`** (the composed unit as the walked entry; the exhaustive block re-walk at a modifier landing; the sampled composed walk with N and the sample sha printed — N set at car 6, so print at a placeholder N with its cost); **`scripts/check-pair.mjs`** moved from the kit with its arms and a wrapper test; the composed anti-vacuity control (a manufactured C2 duty+exemption pair from two innocent pieces must red).
Proof: `tests/lint/proseComposed.walker.test.js` (new) with every arm; the walkers' existing tallies unchanged; the manifest byte-identical; the whole `tests/lint` once; strict on its ceiling; voice E2; OSR/PN exact; typecheck; the cast walkers; the island fence (now twelve modules) green; the manifest test after your plants (numbered after the last); the lighting door by ritual.

## Discipline, fences, commits — as car 3a's
Change only: the island modules named, `tests/lint/proseComposed.walker.test.js` (new), `scripts/check-pair.mjs` (new, from the kit), fixtures, the generator's registry constant (car 4's file — the frozen list only), the sweep + manifest by text, the receipt. NO composer behaviour change (the composer is car 3a's; an arm that needs a hook in it is a REFUSAL with the measurement), no leaf text, no persisted shape. Commit `SEAM car 5: the new arms — <clause>` (+ 5b); trailers `Seat: Opus 5 — Fable-unvalidated` / `Lane: SEAM` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Return: the sha(s); a table of every arm (arm → planted red → clean pass → channel); the composed walk's cost at its placeholder N; anything refused with its measurement.

## ADDENDUM (owner "Do it", 2026-09-08 ~11:5x; SITTING §Q) — ARM A13, THE PROVENANCE MOVE
Add **A13**: the PROVENANCE move (MOVE-GRAMMAR §4.4.3) is classified by `classifyMoves` (a sentence that names the holder of a record — the vocabulary from the holder table's kinds: "the treasury's books", "the muster roll", "the watch's own count", "the parish register", "the toll roll", "the elders", "from the road"), counted per pool and per register with a REPORT channel until the sitting sets its budget from the bands; a cited claim on a piece whose census `source` column is SOURCE-UNRESOLVED or OFFICE is WITHHELD (`P/a cited holder the census does not license`); the DM-face variant of an interested fact carries a `holder` mark the audience filter reads (a `dm-only` mark today; the projector's mark vocabulary gains nothing new until car 4's schema says so — if the mark must be new, REFUSE with the measurement and the chair takes it to the sitting). Fixtures: a lawful citation (the holder resolved) passes; a citation on the office's own books is a finding; a citation with no source is WITHHELD. Car 5b (the holder census) lands AFTER this car and supplies the column; until then A13 reads the column as absent and is NOT-EXECUTABLE, printed.

## ADDENDUM (chair, 2026-09-08 15:3x, after car 4) — three standing rules for this car
1. **Your tip is the dock's HEAD when you are dispatched** — read `git log --oneline -5` and the receipt's last chair section; the dispatch prompt names the sha.
2. **The observed-shape readers baseline:** if your car regenerates any `src/data/*.generated.js` leaf, the OSR dry read names those inputs and prints "the instrument is NOT darkened" — that shrink-only `--write` is yours (ruled at car 4), with the finding count printed before and after (it must stay 1972; any other drift, or any detector source named, is REFUSED with the output). If your car regenerates no leaf, the OSR must read exact with no write.
3. **The runner count is its own tool call** — a one-line shell with nothing else in it; `pgrep -fl` matches any command line that spells the runner's name (a heredoc carrying test source read as 36 runners at car 4); list the matches before waiting on a non-zero count.
