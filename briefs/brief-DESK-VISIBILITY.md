# LANE: DESK-VISIBILITY — a lit sentence a reader cannot see is dark: measure every landed mount's Section, add the walker arm
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## THE FINDING (DESK-GEN3, `receipt-desk-gen3.md`, "three things worth your eye" #1)
`src/components/new/Primitives.jsx:114` renders a collapsible Section as `{open && children}`. A corpus sentence mounted inside a Section that is `collapsible` and `defaultOpen={false}` produces NO bytes for a reader — and the three mount walkers, DESK-9's ARM 3 and the registry law are all green over it. Only a UI flow test noticed. GEN3 checked its own four mounts; **nobody has checked the mounts landed before it** (the desk consist's 23 cars, WARFAITH, DEF2, ECON2, DESK9, GEN2).

## YOUR DOCK
Given at dispatch (the §900 composed dock's tip, AFTER DESK-900-CARS and DESK-900-REDS have reported and been replayed). ⛔ Never rebase; never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content. Vitest through the mutex; never during a chair gate.

## THE ACT
1. **Measure with a denominator.** For EVERY row of `DOSSIER_MOUNTS` (read the registry at your tip — ~50 rows), find the component that renders the mount (the tab file + the Section/primitive it sits in) and classify: OPEN (rendered on first paint), CLOSED (inside a `collapsible defaultOpen={false}` or any `{open && …}` / conditional that hides it on first paint), CONDITIONAL-ON-DATA (rendered only when the datum exists — lawful), UNRESOLVED (say why). Do it by reading the JSX, then CONFIRM by rendering: one fixed-seed dossier per tab through the existing UI test harness (`tests/ui/*TabFlow.test.jsx` are the pattern), asserting the sentence text is in the DOM on first paint. Report the table.
2. **The walker arm.** Add to the mount walker family (the file DESK-9 extended) an arm that reds any sentence mount whose host is CLOSED on first paint unless the registry row declares `visibility: 'closed-section'` with a reason. Prove by planting (a mount moved into a closed Section reds; a declared one passes).
3. **Cure the CLOSED ones** the honest way: open the Section by default where the sentence is the block's point, or re-host the mount beside the datum it explains — never by deleting the collapsible. Each cure is a car with a before/after DOM proof. If opening a Section changes the tab's first-paint size past its ceiling, STOP and report.

## FENCES
No register act (record deltas: lighting titles for new UI tests; mounts baseline if rows move). No corpus sentence changed. Commit per step, `Seat: Opus 5 — Fable-unvalidated`, `Lane: DESK-VISIBILITY`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-desk-visibility.md`, PARTIAL header first, the CLASSIFICATION TABLE, RETROVALIDATION ROW last, tip sha last.
