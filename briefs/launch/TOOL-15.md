# TOOL-15 — the unpoliced figure-in-a-symbol shape: a manifest row that quotes a FIGURE (`const UNDISPOSITIONED_CEILING = 7`, `"uncoveredBaseline": 186`) goes stale the day the figure burns and reds a sibling packet's window; a walker refuses the shape in `requiredSymbols`, `retiredSymbols` and every numeric manifest row, and the loaded instances are re-spelled figure-free (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 79, 80, 84)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then the three instances on the record: EM-B1k2's LANDED `requiredSymbols` row that quoted the ceiling figure (re-spelled figure-free by the chair at `fff247009` — read that commit: `git show fff247009`), EM-B1f's `retiredSymbols` row that quoted `= 7` (withdrawn at `6a3e8089f`; `$KIT/findings/EM-B1f/EM-B1f.STOP.md`), and EM-B1k2's `"uncoveredBaseline": 186` (a numeric manifest row — find it: `git grep -n 'uncoveredBaseline' -- docs/implementation/PACKET_MANIFEST.json`), and, in your worktree, WHOLE: `docs/implementation/PACKET_MANIFEST.json`'s structure (the `requiredSymbols` / `retiredSymbols` / `changeManifest` row shapes; every key whose value is a bare number), `scripts/implementation-packets.mjs` (`validate`, `parsePacketHeader`, how rows are matched by `grep -cF`-style exact text), `docs/implementation/PACKET_STANDARD.md` §"Change manifest" and §"Required verified tree contract", and the nearest manifest walker under `tests/lint` (`git grep -l 'PACKET_MANIFEST' -- tests/lint` — extend it, never a second walker; if none exists, a new file under `tests/lint` owes its mutation-coverage row and the `tests/lint` directory whole).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b tool-15-figure-in-symbol-2026-09-20 $SP/lane-tool-15 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-15/node_modules` (no build). Scratch `$SP/lane-tool-15-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The shape, and why it is a defect
A `requiredSymbols` row is matched as exact text in the tree at placement and at every revalidation. When the text carries a FIGURE that the estate is designed to move (a ceiling that burns, a baseline that shrinks, a count that a census re-takes), the row is true only until the next lawful burn; then a sibling packet's window reds on a symbol that was never the point (the SYMBOL is the point; the figure is the estate's to move). The same holds for `retiredSymbols` and for any numeric manifest row copied from a register (`uncoveredBaseline`). The law (TE-26): a symbol row names a DECLARATION, never a value.

## The two commits, in order
1. **MEASURE the population, then re-spell.** Over every packet entry in `PACKET_MANIFEST.json` (LANDED included — a landed packet's stale row still reds a sibling's window at revalidation): every `requiredSymbols`/`retiredSymbols` `symbol` whose text contains an `=` followed by a number, a number in a comparison, or a bare number that a `git grep -n` finds in a register (`tests/lint/.*baseline*.json`, `scripts/*.json`, `*-baseline.json`); every numeric manifest row that mirrors a register's figure. A table: packet · row · the figure · the register it mirrors · lawful-to-move? For each row that is a figure-in-a-symbol, re-spell it figure-free (the declaration's stable prefix, e.g. `const UNDISPOSITIONED_CEILING`), exactly as `fff247009` did, and prove the re-spelled text resolves once in the tree (`grep -cF` = 1) — a re-spelling that matches zero or two places is a STOP. Numeric mirror rows: replace with the register's PATH + KEY (the manifest names where the figure lives, never the figure), unless the row is a packet's own declared delta (then it stays — say which). `npm run validate:packets` must print `valid:` before and after with the same counts. Landed packets' rows may be re-spelled (the chair's precedent at fff247009: a LANDED row re-spelled figure-free is a repair of the record, not a change of the landing) — the commit message lists every packet touched.
2. **THE WALKER ARM.** In the nearest manifest walker: every `requiredSymbols`/`retiredSymbols` symbol is refused if it carries `=` followed by a numeric literal, a numeric comparison, or a bare integer ≥ 2 (a `0`/`1` literal is admitted — measure how many symbols would be convicted by each threshold and choose the one that convicts exactly the figure class and nothing else; quote the census); every numeric manifest row is refused unless its key is in an ADMITTED list of packet-own deltas you measured in commit 1. Counterforce: plant one `= 7` symbol in a throwaway copy of the manifest → red with the packet and row named; restore. The walker's header names TE-26 and the three instances.

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/lint` WHOLE once if you touch or create a walker there (the ONLY permitted red is the lighting census — FROZEN at the tuple the chair names at dispatch; run it once, record your delta, never refreeze); the mutation-coverage row for a new `tests/lint` file, added surgically; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-15-scratch/TOOL-15.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-15-scratch/TOOL-15.receipt.md`: the population table; every re-spelling with its `grep -cF` = 1 proof; the validator's before/after line; the threshold census; the counterforce; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.

## Addendum (the chair, 2026-09-20 13:3x — from TOOL-19's receipt): the `requiredSymbols` sibling arm has a THIRD instance loaded — EM-B1k2's `"uncoveredBaseline": 186`. TOOL-19 was forbidden to widen; it is yours: the same shape as the retiree rule (a figure in a symbol row is a trap in a PRESENCE row), measured before the cure.
