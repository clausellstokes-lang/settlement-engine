# RESUME — EM-R6 VERSION 3.1: the chair's nine rulings applied as a fold (ODQ §934.47 addendum 34; the same lane resumed; read-only on read-tip-32602dc60; write only $SP/lane-em-compile-EM-R6-scratch/)

Version 3 is ACCEPTED (in the kit's packets-waiting/; v2 superseded). Version 3.1 = version 3 + these rulings, each measured, written as EM-R6.md / EM-R6.manifest.json / EM-R6.evidence.md / EM-R6.compile.report.md (keep v3's files as EM-R6.v3.*):
- Q1 CONFIRMED: `economicState.institutionalServices[].institutions[]` cascades under `label` with its own LOWERCASED rewrite (each declared label path carries its own rewrite form; never a case-folded REFERENCE).
- Q2, Q3 CONFIRMED as compiled (frozen-stamp; catalogue).
- Q4 FIX-D5 RIDES HERE: the twelve-spelling criminal-service sentinel vocabulary as a declared list (the dangle denominator excludes sentinels by that list, never by `startsWith('(')`), with a test arm pinning the live/dark split (3 live: `(arcane underground)` 246 · `(smuggling)` 173 · `(covert)` 2; 9 dark) so a future generator edit cannot silently light one. Price the +5 effective lines and +266 B in the budget table.
- Q5 CONFIRMED read-allowed: `NON_CASCADED_SURFACES` rows gain a `readable: true|false` field (this packet is the first reader — orphan kind 1 reads the matched-pattern path); the rule is written into `factionRename.js`'s ledger header: "never touched" means never WRITTEN.
- Q6 CONFIRMED as compiled (CASCADED under the exact-match guard, report-only on removal). Q7 CONFIRMED: the measured floors (≥40 shapes · ≥1 catalogue path · ≥4 normalised paths · ≥1 red-control finding), raise-only. Q8 unchanged (§3a stays cuttable). Q9: EM-B1c is NOT gated on FIX-D3; A2 keeps asserting the 175 residue by count as the declared price.
- Item 4: A1's failure message gains the sentence that a `catalogId` path crossing the 50 % threshold is a catalogue-shape change, not a missing cascade row. Item 6: re-measure `availableServices.legal[].name` (the 0-of-703 figure is version 1's) and assert the property directly in an A6 arm. Item 8: one line wherever corpus coverage is asserted: the corpus emits 231 of the catalogue's 280 names; both collision scans (231 emitted, 280 catalogue) are empty. Item 12: aliased stamps declare once per path by design — an `alias` field on the row or the sentence in the walker's failure message; choose one and say why.
Everything else stands. Report as before: status, the four paths, the budget table, deltas, the §7 ⟷ JSON proof, requiredSymbols by grep, questions, anything noticed.
