# TOOL-17 — the engine chunk's static importer set gets a shrink-only inventory ratchet: no instrument counts which emitted chunks import the 678 kB engine, so the set grows silently (38 at ec0a30da2); freeze it from a measured build AFTER FIX-B2 lands (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 83, TOOL-12's item 5)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-12's report §3(a) (the import graph instrument `graph.mjs` and the 38-chunk set, twelve members explaining all of it) and FIX-B2's receipt (`$KIT/findings/FIX-B2/FIX-B2.receipt.md` — the post-move set N, the anchors that left; if that receipt is not in the kit, FIX-B2 has not landed and you STOP — this lane runs after it) and, in your worktree, WHOLE: `tests/build/vendorPdfLazy.test.js` (how the engine chunk is discovered — reuse that discovery, never a second rule), `tests/build/engineChunkLazy.test.js` (its shape; a sibling arm may live here), the size-baseline / inventory-ratchet idiom the estate already uses for frozen sets (`git grep -l -E 'shrink-only|SHRINK-ONLY|inventory ratchet' -- tests/build tests/lint scripts` — read the nearest precedent whole: how a frozen set is stored, how a shrink is banked, how growth reds with the offender named), and `EM-PREAMBLE.md` §P2 row 11.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch (it INCLUDES FIX-B2's composed commits): `git -C $SP/slot-2 worktree add -b tool-17-engine-importers-2026-09-20 $SP/lane-tool-17 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-17/node_modules`. Scratch `$SP/lane-tool-17-scratch/`. YOU BUILD ONCE in your own worktree through the mutex's default (exclusive) tier with the two exports INLINE (`GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npm run build`) — the frozen set is minted from YOUR measured build at the tip the chair names. TOOL-12's `graph.mjs` is READ-ONLY at its home — copy it. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The two commits
1. **MEASURE, then FREEZE.** From your build: the set of emitted chunks (by their SOURCE identity — the chunk's facade module or manual-chunk name, never the hashed file name, which changes every build) that statically import the engine chunk; reproduce FIX-B2's N exactly or STOP and say which chunk differs. ⛔ THE CHAIR'S RE-CUT (addendum 98, FIX-B2's finding): the frozen set counts EDGES, not Rollup's grouping — an importer is in the set only if its own static closure reaches an ENGINE MEMBER (FIX-B2's `needs.mjs` walk, stopping at the engine boundary); a chunk that imports the engine only because Rollup grouped an unrelated module into it (`generateWorldBook` at FIX-B2's tip: a 90-module closure reaching no engine member) is ADMITTED BY NAME with the reason, never counted, so the ratchet cannot red on a grouping decision the estate does not control. FIX-B2's measured five at `fd4be7c07`: `SettlementsPanel` (the refused `structuralValidator` closure), `composeInstantWorld` / `generationRequest` / `instantWorldBody` (the engine's own door — admitted), `generateWorldBook` (grouping — admitted); the frozen EDGE set is therefore expected to be ONE (`SettlementsPanel`) — measure it, never assume it. Store the set in a register beside the test in the estate's frozen-set idiom (a JSON list with `measuredAtSha`, `measuredBy`, `date`, `note`, sorted, one entry per line so a diff names the offender). The arm: RED when a chunk JOINS the set (the joiner named, with the engine member it imports — from the graph, so the message says WHICH member to pin, the FIX-B2 cure), GREEN when the set is equal; a chunk LEAVING the set is a win the BELOW arm makes you bank (the register shrinks in the same commit that removed the edge — shrink-only, never re-serialised whole beyond the one line). Gated on `requireDistRead` like its siblings, never skipping silently (a skipped byte arm is not a pass — the receipt quotes the executed set size).
2. **THE COUNTERFORCE, then the header.** Prove the arm by counterforce: in a throwaway, add one import of an engine member to a small UI module (TOOL-12's `livingContentLaw` shape), build, run the arm — RED with the joiner and the member named; restore the exact SHA-256, rebuild? NO — two builds are the price of one counterforce; instead prove the arm on a SYNTHETIC dist fixture (a copied `dist/` with one hand-edited import edge — say exactly what you edited) so the counterforce costs no second build, and say so in the header. The header carries: the set size at the mint, the tip, the date, the twelve-member finding (TOOL-12) and FIX-B2's cure as the prescribed fix for any red, and the sentence that the count is SHRINK-ONLY.

## Laws
Not one byte under `src/` or `vite.config.js`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line; `tests/build` WHOLE once against your dist with dist reads forced (a CREATE under `tests/build` — the directory-whole rule; the lighting census FROZEN at the tuple the chair names at dispatch; run the walker once, record your delta, never refreeze); eslint on every touched file; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: the build and the `tests/build` run are GATED — do everything ungated first, write `$SP/lane-tool-17-scratch/TOOL-17.lane-resume.md` (build → measure → freeze → counterforce on the fixture → tests/build whole → commit), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-17-scratch/TOOL-17.receipt.md`: the frozen set (N entries, by source identity) with the tip; the counterforce quoted (the joiner and the member named); every count line with the executed arm's figure; the lighting delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
