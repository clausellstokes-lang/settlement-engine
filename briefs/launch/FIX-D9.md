# FIX-D9 — the dead-generator disposition: three `src/generators/density` modules reachable from nothing yet carrying mutation-plant obligations, and one ESD excision that landed in no chunk (measure-first; the disposition written, nothing deleted; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 83, TOOL-12's items 1 and 8)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-12's report §3(a) and its ⛔ items 1 and 8 (`$KIT/findings/TOOL-12/TOOL-12.report.md`), `docs/DEAD_CODE_DISPOSITION.md` WHOLE (the estate's disposition idiom — its rows are the home for what you find), EM-B1k2's packet §13 Q1 and its roster row naming `titularSuccession.js` as "the dark caller the chair named" (`docs/implementation/packets/settlement-editor/EM-B1k2.md`, grep the name), and `scripts/mutation-coverage-manifest.json:253`'s row (the TE-DENSITY-1 rationale that begins "THIS ROW IS OWED BY A RULE THAT DID NOT EXI…" — read it whole).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b fix-dead-generators-2026-09-20 $SP/lane-fix-d9 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-d9/node_modules` (no build). Scratch `$SP/lane-fix-d9-scratch/`. TOOL-12's graph instrument (`$SP/lane-tool-12-scratch/tools/graph.mjs`) is READ-ONLY at its home — copy it. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The four modules
`src/generators/density/densityAscension.js`, `src/generators/density/successionGrammar.js`, `src/generators/density/titularSuccession.js` — TOOL-12 measured ZERO static and ZERO dynamic importers under `src/` and all three ABSENT from the emitted engine chunk (tree-shaken), while at least one carries a mutation-plant obligation. `src/domain/region/foldTradeCategories.js` — an UNPINNED ESD excision (an `ENGINE_SHARED_DOMAIN` excision in `vite.config.js`) that is neither among the engine's 114 modules nor matched by any `manualChunks` rule: dead, or co-located somewhere unexamined. A `git grep -l` for each name finds 2–3 files under `src/` — the module itself plus comment mentions or a real importer; you settle which BY THE IMPORT GRAPH, never by grep alone.

## The measurement, then the disposition (two commits)
1. **MEASURE (no edit).** For each of the four: (a) every `src/` importer, static and dynamic, by the real import graph from every entry (`src/main.jsx`, each worker entry in `vite.config.js`, `api/**` if it imports `src/`) — reproduce TOOL-12's zero or refute it; (b) every `tests/` importer (which test files import the module, and what they assert — a test of dead code is a claim about nothing); (c) every `scripts/` and `docs/` mention with its meaning (the mutation-coverage row, the mutation-sweep plant, a packet naming it as a future seam); (d) for `titularSuccession.js`, EM-B1k2's ruling: it reads the roster only through `factionRosterOf` and was named a DARK CALLER — whether a chartered packet (the density programme, EM-B1h/B1i, the simulation architecture) plans to wire it; (e) for `foldTradeCategories.js`, whether Rollup emitted it anywhere (search every chunk of the chair's dist copy `$SP/lane-tool-12-scratch/dist-ec0a30da2/` for a string unique to the module) and, if nowhere, which excision rule names it and why. Quote every figure.
2. **THE DISPOSITION, WRITTEN — NOTHING DELETED.** The chair's rule: deleting shipped code is an owner-visible act (§934.70 is asked with your table); this lane writes the disposition so nothing is re-found. For each module, ONE of: `DARK-BY-DESIGN` (a named packet will wire it — the packet cited; a `// dark-until: <packet>` header line added to the module) · `RETIRE-PROPOSED` (no consumer planned; the tests that import it named; the owner's decision point) · `LIVE` (TOOL-12's zero refuted — the importer named and the report corrected). The rows go into `docs/DEAD_CODE_DISPOSITION.md` in its own idiom, and a walker arm (find the disposition doc's existing walker, if any — `git grep -l 'DEAD_CODE_DISPOSITION' -- tests`; extend it, never a second walker; if none exists, STOP and say so — the chair decides whether one is minted) pins each `DARK-BY-DESIGN` module's header line so a future reader cannot mistake it for an orphan. The mutation-coverage row at `:253` is left as it is unless its rationale is false on measurement — then the sentence is corrected, never the row removed.

## Laws
Not one logic byte under `src/` (a header comment line is the only permitted src edit; measure the built module bytes unchanged — a comment does not render). Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line; `tests/lint` WHOLE once if you touch a walker (the ONLY permitted red is the lighting census — FROZEN at the tuple the chair names at dispatch; run it once, record your delta, never refreeze); eslint on every touched file; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-d9-scratch/FIX-D9.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-d9-scratch/FIX-D9.receipt.md`: the four-row measurement table (src importers by graph / tests / scripts / docs / emitted?) with receipts; the disposition per module and its reason; the walker arm's counterforce (a planted orphan header, red, restored, green); every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
