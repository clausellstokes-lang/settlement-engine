# TOOL-24 — three shrink-only prose-byte TOTALS over the code roots (generators · domain-product · certification), measured by a real parse, with the existing declared-row door for a raise and a non-asserted per-file diagnostic on the red; and the `src/data` prose the leaf ratchet misses named as its first row (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 88 — TOOL-18's shape (b), synthesised)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-18's report WHOLE (`$KIT/findings/TOOL-18/TOOL-18.report.md` — §1 the population by the M1 rule and its M3 correction (templates included; M3 − M1 = 75,073 B), §2 the reach today (zero instruments over the two roots; `proseLeavesOnDisk()` misses 234,998 B under `src/data`), §3 the growth by group (a file split nets to zero), §4–§5 shape (b) synthesised, §6 the ten items — 1, 3, 4, 10 are yours), and, in your worktree, WHOLE: `tests/lint/proseCorpusBytes.test.js` (the RAW-exact / GZIP-band idiom, `proseLeavesOnDisk()` at `:103-109`, the declared-row door, the chain that must terminate on the committed number at `:303`), `scripts/.prose-byte-baseline.json` (`_instrument`, `_ceilingArithmetic`, `_declaredRowIdiom`, `_newLeafCeilingArithmetic`, the ten `leaves`), and TOOL-18's parser instrument (`$SP/lane-tool-18-scratch/` — READ-ONLY; copy it: it is the M3 rule with acorn, 0 parse failures over 1,172 files).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b tool-24-prose-totals-2026-09-20 $SP/lane-tool-24 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-24/node_modules` (no build). Scratch `$SP/lane-tool-24-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The ruling you implement (addendum 88)
Shape (a) — forty RAW-exact rows on files that move on every fix — is DECLINED; shape (c) — moving the prose into `src/data` — is refuted as an accounting cure. Shape (b), synthesised: assert THREE shrink-only TOTALS of authored prose bytes, one per root — `src/generators/**`; `src/domain/**` minus `src/domain/certification/**`; `src/domain/certification/**` (an engineering register that ships 0 % of its 416,965 B — kept visible, never confused with a product cost) — measured by TOOL-18's M3 rule (a real parse; string, template AND template-quasi text; the prose predicate stated exactly as TOOL-18 states it, with its sampled false-positive check reproduced), with the estate's DECLARED-ROW door for a raise (the same `_declaredRowIdiom` the leaf ratchet uses: a raise names its cause, its rows and its ceiling arithmetic; a shrink is banked by lowering the number in the same commit that shrank it), and a NON-ASSERTED per-file diagnostic (the top twenty files by prose bytes and their delta against the last banked map) printed on the RED via `process.stdout.write` (never `console.log` — vitest 4.1.8 drops a passing test's log, and a diagnostic that only prints on the red must be proved to print). The totals are a CEILING that only falls; the ARCH-COMPOSED-PROSE citation is NOT to be repeated (its document does not exist — DOC-1 materialises it; cite `scripts/.prose-byte-baseline.json`'s `_ceilingArithmetic` by path until then).

## The three commits, in order
1. **The instrument, red-first.** A new file `tests/lint/proseRootTotals.test.js` (a CREATE under `tests/lint`: its `scripts/mutation-coverage-manifest.json` row added surgically beside its siblings; the `tests/lint` directory runs WHOLE in your gate; your lighting delta recorded — the walker asserts its five figures in order and stops at the first miss; never refreeze) plus a register `scripts/.prose-root-totals.json` (three rows: `root`, `prose`, `measuredAtSha`, `measuredBy`, `date`, `rule: "M3"`, and the `_declaredRowIdiom` paragraph copied from the leaf baseline with its authority path). The arm: measured total ≤ the register's number → green; measured > number → RED with the diagnostic; measured < number → RED ("bank the shrink: lower the row"), the leaf ratchet's BELOW idiom. Red-first: plant a 200-character prose literal in a throwaway copy of one `src/generators` file → red with the file named in the diagnostic; restore the exact SHA-256; plant a prose DELETION → the BELOW arm reds; restore. Then mint the three numbers from YOUR measurement at the tip (quote each; reproduce TOOL-18's 1,169,148 B M1 population and explain every byte of difference from your M3 figure).
2. **The `src/data` leak named.** `proseLeavesOnDisk()` derives its roster from `readdirSync('src/data/dossierStateProse')` + the named leaves, so `src/data/institutionDescVariants.js` (66,835 B), `institutionServices.js` (47,868 B) and their siblings (TOOL-18 item 1: 234,998 B in all) are prose the ratchet never names. Do NOT widen the leaf ratchet's RAW-exact rows over them (the chair's ruling: those files are hand-authored, not generated); add a FOURTH total row — `src/data/**` minus the ten declared leaves — to your register, so the leak is bounded by the same shrink-only idiom, and correct `proseCorpusBytes.test.js`'s header where it claims the corpus is fully declared (a comment; measure the claim first — quote it).
3. **The docs receipt.** One paragraph in `docs/implementation/` beside the instrument's authority (the file the chair names at dispatch, or the leaf baseline's `_instrument` doc string extended) stating the four totals, the rule, the door, and that the diagnostic is report-only. Nothing else.

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; the ONLY permitted reds in `tests/lint` whole are the lighting census and, if swept, `tests/property/dossierProseManifest.test.js`'s provenance title (owner-gated; never re-record); the three prose-manifest recorder files are off limits; the instrument's wall-clock measured (a parse of 1,172 files — if over 30 s in the directory run, say so and cache by file sha); quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-24-scratch/TOOL-24.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-24-scratch/TOOL-24.receipt.md`: the four minted totals with the rule and the M1/M3 reconciliation; both red-firsts quoted; the diagnostic's printed proof; the mutation row; every count line; the lighting delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
