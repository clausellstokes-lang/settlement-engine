# LAUNCH — COMPILE EM-R0b: `recordInvariants` — the town's own invariants as a module (second member of the re-entry family; design §22.2 item 3)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md` steps 1–10). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-R0b-scratch/`. **Authority:** design §22.2 (item 3 above all; items 8 and 10 for the power total and the referential checks) — `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`; the charter's EM-R table (same ref). **Measured ground:** kit `findings/RECON-ARCH-REDERIVE-3.report.md` §6 and `tools/rederive-prototype-3/invariants.mjs` + `q5a-probe.mjs`, `q5b-validate.mjs`, `q5c-flags.mjs`, `q5d-flagvec.mjs` — copy them to your scratch and RE-RUN them at your tip (plain `node`, one process at a time); every figure you quote is one you re-measured.

**The packet's outcome (the chair's ruling on the shape):** a PURE module under `src/domain/edit/` — `recordInvariants(record) → violations[]`, each violation `{ id, path, message, kind }` with `kind ∈ arithmetic · count · prose-count · referential · band · flag-vector` — promoted from the recon's instrument under these laws: (1) EVERY check ships with ITS OWN CONTROL: it must be clean over plain generated records of the golden corpus at test time (the recon REJECTED two candidates by exactly that control — `criticalIssueCount = issues.length` and `foodRatio = production/need` — record both in the packet as REJECTED WITH THEIR CONTROLS so nobody re-adds them); (2) the ONE pre-existing generator inconsistency the control found (`V-SUMMARY-DEPS` on `town|germanic|mountain|mountain_pass`: the summary says six dependencies, the list holds five) is pinned as KNOWN — a named, dated, single-row expectation that REDS when the defect is cured (so FIX-G1's landing must remove the pin) — never a blanket tolerance; (3) the LEARNED flag-vector check (which label may sit beside which flags) must be DECLARED DATA in the module, derived by a script you give the chair, with the corpus that taught it named — not learned at runtime; say how it behaves on a label/flag pair the corpus never showed (the chair's default: an unseen pair is a violation only if the label was seen with a DIFFERENT vector in every observation — quote the recon's rule and say if it differs); (4) band checks are sandwich tests against declared thresholds read FROM THE PRODUCER'S OWN TABLE by import where one is exported (never a copied number); (5) the power total (`V-POWER-100`) and the referential checks (a reading naming a held entity that does not exist) are in; they are what gate design §22.2 items 8 and 10. The module is consumed by the merge (EM-R0c) at RUNTIME and by the corpus ratchet (EM-R7); this packet builds neither. Measure its cost per record (µs–ms) at each tier — it runs after every edit.
**Tests:** the control over the corpus stride (say rows and wall-clock); a planted-mutant table — for EACH check, the smallest mutation of a real record that must trip exactly that check and no other (the mutation-coverage manifest will want these; measure whether `tests/domain/` or the directory you choose is an enforcer directory and price the row). Bundle placement as for EM-R0a: it must reach neither the generation worker nor the first-paint set; prove by the import graph; name the first importer-to-be.
