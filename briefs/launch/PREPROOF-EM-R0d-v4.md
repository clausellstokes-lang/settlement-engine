# PREPROOF-EM-R0d-v4 — EM-R0d's pre-proof RE-RUN at the composed tip: its condition (FIX-C2 composed) is satisfied, but the first composition sitting moved THREE of its paths after its pre-proof tip `141a1d775` — `tests/components/dossierLabelCase.test.jsx` +95 (FIX-P5's badge-capitals law), `src/domain/prose/holderTable.js` 2 lines and `tests/build/domainGeneratorsBoundary.test.js` 4 lines (citation re-addresses) — so its TEST row over the first must be re-cut and every claim re-measured before the chair places it at EM-R0a's flip (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 72, 93–101)

You are an Opus PRE-PROOF lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys and verifies. You EDIT NOTHING in the tree; you re-execute the packet's claims at the tip the chair names and write the revalidated packet + manifest as COPIES into your scratch — the chair validates and places. Read `$KIT/briefs/LANE-EM-PREPROOF.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then the current packet and manifest WHOLE (`$KIT/packets-waiting/EM-R0d.md`, `EM-R0d.manifest.json` — version 3, pre-proofed at `141a1d775`, READY-able CONDITIONAL on FIX-C2 composing; its `_pending` rows; its three drafted cures in §7; the addresses-only `holderTable.js` row that addendum 72 Q1 ruled OUTSIDE the three-logic-file cap), the pre-proof's report and evidence under `$KIT/evidence/` (`EM-R0d.*`), FIX-P5's receipt (`$KIT/findings/FIX-P5/FIX-P5.receipt.md` — commit D `ec7d8a363`, composed as `7dea316c7`: the badge-capitals law in `tests/components/dossierLabelCase.test.jsx`, thirteen sites cured, the three-letter legend codes below the four-letter bar with a control both ways), and FIX-C2's / FIX-C2b's receipts (the citation re-addresses in `holderTable.js` and `domainGeneratorsBoundary.test.js`).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR TREE: a read tip at the tip the chair names at dispatch (`git -C $SP/slot-2 worktree add --detach $SP/read-tip-r0d-v4 <tip>`; `ln -s $SP/slot-2/node_modules $SP/read-tip-r0d-v4/node_modules`); no build; every gated run through the mutex SHARED tier with the two exports INLINE, ONE directory per invocation, a printed count on every line (default reporter only), and PAUSE-AND-RESUME at the first gated step (write `$SP/lane-preproof-EM-R0d-v4-scratch/EM-R0d-v4.lane-resume.md`, end `PAUSED AT THE GATE — <path>`); `git status --short` at the read tip EMPTY at start and end. Scratch `$SP/lane-preproof-EM-R0d-v4-scratch/`; your outputs are COPIES there: `EM-R0d.md` (version 4), `EM-R0d.manifest.json`, `EM-R0d-v4.report.md`, `EM-R0d-v4.evidence.md`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree (EM-R0a's sealed build), every `$SP/lane-*` beyond your own scratch, the ledger checkout beyond `git show`, the kit.

## What version 4 must do (the same seventeen steps as every EM pre-proof, plus)
1. **The window, quoted.** `git diff --stat 141a1d775 <tip>` over every change-manifest AND `requiredSymbols` path: exactly the three movers above (name any other); for the two citation movers prove COMMENT-ONLY (every changed line a comment or string address) and admit them as expected movers; for `dossierLabelCase.test.jsx` read FIX-P5's +95 WHOLE.
2. **The TEST row re-cut.** Version 3's edits to `dossierLabelCase.test.jsx` were drafted against the pre-law file. Re-cut them over the composed file: every case R0d adds obeys the badge-capitals law (no shouted word typed into content; capitals belong to the style) and the three-letter/four-letter legend-code control; if R0d's cases would themselves red P5's census, the packet's cases change, never the law; quote the re-cut diff against the composed file.
3. **Every other claim re-executed at the tip** (the seventeen steps): the eleven `requiredSymbols` rows resolve (`grep -cF` = 1 each — `BASELINE_EDGES` and `HOLDER_SOURCES` moved files; re-measure their lines); the wiring census's producer-file count (R0d CREATEs `src/data/bandLadders.js` — under `src/data`, NOT `src/domain`/`src/generators`, so preamble row 13's census row does NOT fire — measure by running `node scripts/wiring-census.mjs --check` at the tip and say whether `src/data` is walked); the byte budgets (the worker: `WORKER_BUNDLE_CEILING_BYTES` zero slack — R0d modifies three generators inside the worker's closure; the fourth amendment's law: a worker byte has NO placement cure, so R0d's bytes there must be bought back or bounded — re-measure with the kit's esbuild idiom; the lazy engine: FIX-B2 bought 35,079 B of headroom, re-measure R0d's engine bytes against it); the edge-shared metas' `inputs` (do the three generators or `holderTable.js` sit in a bundle closure? then `npm run build:edge-shared` and the seven paths join §7 — row 10/12); the lighting delta by `parkReasonsFor` over every test home (the register at the tip is `2664·359·2305·25501·6812` or later — read it); the preamble hash at the tip (`docs/implementation/preambles/EM-PREAMBLE.md` — the FOURTH amendment, `c9f33c8d…6675`, or later if the chair amended again; measure); the goldens' hashes (the prose manifest golden CHANGES when CURE-J's re-record composes — read the register and the fixture at your tip and quote both; R0d's golden claim is 'unmoved by this packet', measured against the tip's golden, whatever it is).
4. **The placement statement for the chair:** the window sentence with the three movers named, the re-cut row, the `_pending` rows' state, `retiredSymbols: []` (never `null`), the preamble hash, and the index rows' texts (no status word but READY).

## Your report
`EM-R0d-v4.report.md`: the window; the re-cut diff; every re-executed claim with its count; the budget figures; the lighting delta; every claim CONFIRMED by execution or PLAUSIBLE, labelled; the stamp read from `date`; ⛔ everything noticed and not touched, each item specific enough to slot. If a command errors on a rate limit, stop and say so. End your turn with the report's path and one line: READY-ABLE at `<tip>` or BLOCKED with the reason.
