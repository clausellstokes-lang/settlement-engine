# LAUNCH — COMPILE EM-B1j: THE PULSE'S CLOSE-FATE AND REMNANT VOCABULARIES — `LIFECYCLE_CLOSE_FATES` priced against the fifteen closure fates, the ageing of a rise or standing fate ruled by measurement, the readers of `'remnant'` censused, and ONE exported remnant writer (train EM-T6, after EM-B1h and EM-B1i; launch by the chair, session a9df403c, 2026-09-20)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, SIXTEEN steps), then every 2026-09-20 addendum at the foot of `LANE-EM-COMPILE-2.md`, `LANE-EM-PREPROOF.md` and `LANE-PARALLEL.md` (the directory-whole rule; the zsh warnings; the trailer ruling; the `tests/security` fixture law; the report-only-arm rule; the family's totality-walker line; the step-15 correction; the Status-row parser rule). The tree you read is named in the dispatch message (a detached read tip; never edited). Scratch: `$SP/lane-em-compile-EM-B1j-scratch/`. Outputs are COPIES in your scratch (`EM-B1j.md`, `EM-B1j.manifest.json`, `EM-B1j.compile.report.md`, `EM-B1j.evidence.md`) — never into the kit, never into any worktree's docs.

**The authority, in order (read each WHOLE):** the charter rows and rulings that chartered this member — `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/implementation/charters/EDIT-MODE-TRAIN.md | grep -n 'EM-B1j\|EM-B1l\|LIFECYCLE_CLOSE_FATES\|remnant'` and the same grep over `docs/OWNER_DECISION_QUEUE.md` (the ledger checkout is READ-ONLY, via `git show` only; every returned row binds); the landed and accepted siblings at the read tip and in the kit — `EM-B1e.md` (LANDED: the pulse's ONE ruin writer `ruinInstitution(inst, { reason, fate })`, both required), `EM-B1d.md` (LANDED: `NPC_UNAVAILABLE_STATUSES`; `ROSTER_ABSENT_STATUSES` untouched), `EM-B1h` (kit `packets-waiting/EM-B1h.*`, version 2 ACCEPTED: the fate vocabulary closed — THREE kinds derived from what each writer does to the record: `closure` 15, `standing` 1 `demoted_by_disaster`, `rise` 2; the kind map the single authored source; the guard `assertWorldPulseFate` in the leaf); `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256 `16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195` — the second amendment; cite it); `docs/implementation/PACKET_STANDARD.md`; the two landed models `EM-B3c.md` and `EM-B1k2.md`.

**The packet's outcome (the chair's ruling on the shape — this is its index; each part is MEASURED before it is contracted):**
1. **`LIFECYCLE_CLOSE_FATES` PRICED, not widened blindly.** Today it counts THREE words (a mirror born 2026-06-10, never re-examined); EM-B1h's closure kind holds FIFTEEN fates. Measure what widening three to fifteen would MOVE — every campaign's damping, the preset witness golden (`tests/fixtures/preset-lighting-witness-golden.json`), the dormancy goldens (`tests/property/*DormancyGolden*.test.js`) and the generator/prose goldens — executed over the estate's own corpora in plain node (the witness's own driver; the dormancy suites' fixtures), with the exact rows that move counted. THE RULE: a golden that moves is the OWNER's door (§934.58's shape), so the packet is written as TWO halves — (a) the behaviour-neutral half that lands (the mirror derived from EM-B1h's kind map so it cannot drift again, asserted equal to the three words TODAY by a pin that reds the day the derivation and the literal disagree), and (b) the widening itself as a measured, priced, owner-signed door with its rows named — never landed quietly, never left unpriced.
2. **Does a `rise` or `standing` fate AGE OUT?** A closure fate is cleared by its own lifecycle; a rise (2) or standing (1) fate is never cleared today. Measure what the record carries after N ticks for each (the witness driver; a synthetic campaign through the shipped pulse), what the dossier and Wizard News READ from it (every reader, dispositioned), and what ageing it out would change (a lived-behaviour change: a golden or a receipt row that moves is the owner's door). Put the price and the two shapes (age out at the tick's own cadence; never clear) to the chair — recommend, do not decide.
3. **THE READERS OF `'remnant'`.** `tierOutcomeApply.js:137-149` writes `status: 'remnant'` to institutions in six of seven branches, OUTSIDE the `EntityStatus` typedef the status-union walker pins at five. Census every reader of that status (by literal and by the typedef's consumers; the denominator stated); decide by measurement whether `'remnant'` is a sixth member of the same vocabulary (then this packet admits it to the typedef and the walker, with every reader dispositioned) or a SEPARATE vocabulary (then **EM-B1l** is chartered from your measurement — write its charter row in your report; this packet does not build it).
4. **ONE EXPORTED REMNANT WRITER (FIX-T1's finding, addendum 68).** The remnant/shell shape has FOUR writers and no single writer — `institutionLifecycle.js:1021, :1063`, `magicRegimeLifecycle.js:358`, `tierOutcomeApply.js:166` — each spelling `remnantReason` independently, with hand-built replicas in five tests (`magicForms:304`, `institutionStatusModel:60`, `institutionStatusLifecycle:389`, `foodStockpile:385`, `evaluateInstitutionLifecycle:165`). EM-B1e's ruling applied one door over: ONE exported writer (`remnantInstitution(inst, { reason, fate })`, both required, in the same leaf family as `ruinInstitution`), the four sites re-pointed with their exact records byte-identical (a pin per site: the record the writer returns equals the record the site wrote before), the five test replicas re-pointed (or exempt with a reason at the site, per FIX-T1's marker idiom), and FIX-T1's replica walker taught the remnant shape too (read its shape off the new writer, as it reads the ruin shape off `ruinInstitution`).

**What you must MEASURE before contracting (and put in the report with the command and its output):** the three-word mirror's consumers and the fifteen-fate kind map's members at the read tip; the witness and dormancy corpora's movement under the widening (rows named); the ageing question's readers; the `'remnant'` reader census with its denominator; the four writers' exact records and the byte-identity of the re-pointed sites; the byte budgets under the amended §P2 row 11 (the generation worker's zero-slack closure — is the calamity/lifecycle leaf family in it? measure; the eager first-paint set by importing `vite.config.js`'s `EAGER_FIRST_PAINT_MODULES`; the edge-shared metas — if any `_shared` bundle imports the leaf, row 10's rebuild and ALL FIVE metas as GENERATED rows are owed); the lighting delta against the frozen `2653·383·2270·25052·6680`, credited files only; the mutation register rows (a CREATE test file wants a row; the writer's suite a `mutation` row with a real plant that the shared-tree sweep can revert by cp — read TOOL-11's charter note).

**Budget and split:** this member has four parts; if the honest packet exceeds 250 effective lines per leaf, 400 total or 12 files, SPLIT at the natural seam (the remnant writer + its replicas as EM-B1j; the fate pricing + ageing as EM-B1j′) and say so. Register predictions as DELTAS (`it` titles only, in CREDITED files; never a variable named `it`/`test`/`describe`); `__BASE__` for the chair; `acceptanceCases` as `{id, case}` objects, at most eight; the §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS`; `requiredSymbols` verbatim strings that exist at the read tip; `checks` as the build lane's focused commands (the writer's suite, the replica walker, the status-union walker, the golden suites, `tests/lint/contractTestAntiVacuity.walker.test.js` if any CREATE carries the `contract` token). The Status row carries its value ALONE; every stamp goes on the continuation lines. Nine questions or fewer for the chair, each with your recommendation.

**Laws:** FORBIDDEN — `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*`, the other read-only lanes' scratch dirs, the ledger checkout beyond `git show`, the kit (read-only). No edit to any tracked file; `git status --short` at the read tip EMPTY at start and end. Plain `node` probes are ungated (the witness and dormancy drivers run in node); any vitest line is GATED and PAUSE-AND-RESUME (the mutex SHARED tier, worker-capped, the two exports INLINE, one directory per invocation; write `$SP/lane-em-compile-EM-B1j-scratch/EM-B1j.lane-resume.md` and end `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours"). Stamps from `date` in the same call. If a command errors on a rate limit, stop and say so. Your report: the measurements with their commands, the two prices (the widening; the ageing) with the rows that move, the `'remnant'` verdict (same vocabulary or EM-B1l's charter row), the writer's byte-identity pins, the budget figures, the questions, and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
