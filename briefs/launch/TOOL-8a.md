# TOOL-8a — the dark-guard lint: a REGISTRY + EXACT-VOCABULARY walker over `src/generators/**`, built BEFORE the cures (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 54)

You are an Opus PARALLEL TOOLING lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you — verb 4, PAUSE-AND-RESUME at every gated batch; the 2026-09-20 addendum: the directory-whole rule), then TOOL-8's report `$KIT/findings/TOOL-8/TOOL-8.report.md` WHOLE (READ-ONLY; its instruments live in `$SP/lane-tool-8-scratch/` — COPY what you reuse: `scan.mjs` (the acorn AST scan), `vocab.mjs` (the exact field vocabularies), `candidates.json`, `dark-classified.json`). §5.2–§5.5 of that report is your design; the chair's rulings below bind where they differ.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b tooling-8a-2026-09-20 $SP/lane-tool-8a <the branch tip the chair names at dispatch>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-8a/node_modules` (no build). Scratch `$SP/lane-tool-8a-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The chair's rulings (they bind you)
- **R1 — the shape.** `tests/lint/darkGuardCensus.walker.test.js` + a registry `src/generators/darkGuardRegistry.js` (or the estate's home for generator-side registries — find the precedent, e.g. `habitForkRegistry.js`, and match it; say why): one row per guard site `{ id, file, line, literal, shape, field, normalization, disposition, why }`. A SOURCE ARM re-runs the acorn scan over `src/generators/**` — every site whose shape matches (`STRMETH`, `REGEX`, `PRED`, `NEWREGEX`; NOT `EQ`) must carry a registry row and every row must resolve to a site (totality both ways, the `chooserTotality` idiom); a new unregistered guard REDS. A CORPUS ARM runs ONE uninstrumented pass over the golden corpus (`tests/helpers/goldenMasterCorpus.js`; the full 525 rows cost 8.49 s — measure your own figure and keep the test inside the default 20,000 ms timeout; if you must stride, say so and prove the vocabularies are unchanged by the stride) and derives each field's EXACT vocabulary (instNames · svcNames · resKeys · facNames · secrets · incomeSrc · goods · npcRoles · histDesc); for each row `eligible = |vocabulary(field)|`, `matched = |{v : predicate(normalize(v), literal)}|`; `matched === 0 && eligible > 0 && disposition not in the declared-dark set` → RED with the counter-example quoted. Patch NOTHING on any prototype (the report's §5.2 reasons: global mutation in a shared vitest worker; a capped window INVENTS darkness — `garrison` ×2,987,768).
- **R2 — precedence-shadowed arms are registered BY CHAIN, never individually red:** a row may declare `precededBy` (the earlier arm(s) that always claim the value); the walker reds only when NO arm of the chain matches. `serviceClassifier.js`'s ~160-arm ternary (303 dark arms) is the proof case.
- **R3 — a CLOSED `DECLARED_DARK` vocabulary**, asserted by the walker the way `FORK_CLASS_VOCABULARY` is in `habitForkRegistry.js`: `defensive-escape` · `idempotent-normaliser` · `negated-dedupe` · `corpus-absent` · `precedence-shadowed` · `catalogue-candidate` (rows whose guard reads catalogue candidates validate against the catalogue, not the corpus) · `KNOWN_DARK_PENDING_CURE`. Every row's `why` is a sentence. Extensible only by a row with a reason.
- **R4 — THE EIGHT are born as `KNOWN_DARK_PENDING_CURE` rows the walker ASSERTS are still dark** (D1 `npcGenerator.js:1362`; D2 `spatialGenerator.js:162`; D3 `:186`; D4 `:74`; D5 `:87`; D6 `:168`; D7 `:142`; D8 `:132` — the report §4.1 has each literal, field and the spelling it cannot reach). The walker lands GREEN today and REDS the day FIX-D4 or FIX-D6 cures a site without flipping its row (the FIX-P5 exception idiom: an executed exception that self-retires). The 545 `corpus-absent` and 173 `precedence-shadowed` rows are born classified from TOOL-8's `dark-classified.json` — re-derive them at your base, do not copy blindly; report any row whose class changed and why.
- **R5 — the mutation proof is the NEGATIVE CONTROL, never a golden-moving plant:** a synthetic registry row whose literal is known-absent must RED (the `worldGenerationClockSeam` idiom), and a synthetic row whose literal is known-present must CLEAR; the file's manifest entry is a `self-proving-meta` rationale row in `scripts/mutation-coverage-manifest.json` (read that file's existing rows of that kind and match their shape). NO `MUTATED_FILES` entry for `npcGenerator.js`; the sweep plants nothing in `src/`.
- **R6 — scope wave 1 to `src/generators/**` and DECLARE the blind spot** in the walker's header chooser-totality style: `src/domain`'s 409 never-evaluated sites need a pulse/dossier driver corpus (TOOL-8b). The header also records N14 — a capped or strided execution instrument invents darkness; this walker derives from exact vocabularies for that reason.
- **R7 — the two exemplars of honest darkness** (`generationCoherence.js:330`, `goodsCatalog.js:278` — the latter is `src/domain`, so it is out of wave 1's scan scope: name it in the header as the exemplar, do not register it) and the three families of §5.3 (`arcaneInstitutionIdentity.js:165`; the four `/^_+|_+$/g` trims; `economicState.js:405…497`'s negated dedupe guards) are your allowlist's worked rows.

## Laws
No `src/` edit beyond the registry file itself (a CREATE; if the registry's home demands anything else in `src/`, STOP and say what). Goldens untouched (hashes before/after: `7177cd6e…8f1e`, `921c51cf…db41`). The lighting census is FROZEN (the tuple the chair names at dispatch) — your CREATE file adds titles: run the walker ONCE separately, record the tuple and your delta, NEVER refreeze. The directory-whole rule: your CREATE under `tests/lint` runs `tests/lint` WHOLE once (its lawful reds: the lighting census by your delta; nothing else). The standing instruments (negativeAssertionAnchor — every `.not.` anchored on the immediately preceding line; voiceMechanics; mutationCoverageManifest; eslint on every touched file). Every vitest line through the mutex SHARED tier, worker-capped, ONE directory per invocation, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN. PAUSE-AND-RESUME at every gated batch (`PAUSED AT THE GATE — <note path>`; the chair resumes you with "the gate is yours"). ONE commit by pathspec (a second only if the registry's home forces a separate car — say why); `git show --stat HEAD` names exactly your paths.

## Your report
The receipt by execution (the walker's count lines; the negative control red and the positive control green, quoted; the corpus arm's wall-clock; `tests/lint` whole; the lighting tuple + delta; eslint 0; the goldens' hashes); the registry's census (rows by disposition, with the eight named); any row whose class moved from TOOL-8's; the commit sha; ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
