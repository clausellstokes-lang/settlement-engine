# FIX-G4 — THE GOLDEN DOOR LEARNS A NULL REGISTER ROW'S `proofForm` AT FIRST USE (trust-on-first-use), with the instrument the field has never had; the shift-records README's two false sentences corrected; TOOL-21's remaining noticed items given their homes (launch brief; the chair, Fable 5.1, session 4a1823e2, 2026-09-20; ODQ §934.76 — DECIDED YES under the owner's in-session deferment of 15:3x, verbatim: "I leave all decisions to you and give you all deferment regarding signatures and golden door signings"; ODQ §934.47 addenda 113, 116)

You are an Opus PARALLEL BUILD lane for SettlementForge. Seat law: Fable architects, manages, judges and validates; Opus surveys, recons, verifies and implements. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, including the three addenda the chair appended on 2026-09-20 afternoon at its foot: ONE gate holder at a time; the derived-artifact door ruling; find the tests that NAME your file; a re-record owes a census of the old hash's literals), then TOOL-21's report WHOLE — it is this lane's measurement and its §4(d) is this lane's design: `$SP/lane-tool-21-scratch/TOOL-21.report.md` (its harnesses sit beside it and are yours to re-use from YOUR scratch, each taking its tree from an environment variable you set explicitly).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=$SP/chair-kit-923472dc  (READ-ONLY)

YOUR WORKTREE: named by the chair at dispatch (already created, `node_modules` symlinked). Scratch `$SP/lane-fix-g4-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`, every other `$SP/lane-*`, the read tips, the ledger checkout beyond `git show`, the kit, the memory directory. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists — never touch it.

## What is true today (TOOL-21, CONFIRMED by execution — re-measure each at your base before you build on it)
`tests/fixtures/.golden-freeze-register.json` holds 50 rows; 47 carry `proofForm: null`; the door (`tests/helpers/goldenRecordDoor.js` — find every refusal by its code) compares a shift record's `surfaces[].proofForm` to the row's ONLY when the row's is set, so `PROOF_FORM_MISMATCH` is unreachable for 47 surfaces; the door's write set on a successful re-record is exactly `sha256`, `rows`, `ownerRow` — it never writes `proofForm`; no arm of `tests/lint/goldenFreeze.walker.test.js` reads the field; NO ruling anywhere assigns a form to any surface (so nothing may be back-filled); the chartered option "refuse when the record carries a form and the row is null" SEALS all 47 surfaces shut and is refused. The replay of all ten real shift records: zero refused today, zero refused under trust-on-first-use.

## The ruling you build (§934.76)
**When the register row's `proofForm` is null and an owner-signed shift record carries a LAWFUL form for that surface, a successful re-record WRITES `row.proofForm` beside the three fields it already writes. When the row's `proofForm` is set, the existing equality check governs, unchanged.** No value is ever invented; protection only grows; nothing is back-filled by hand; the frozen register is edited by the door's own verb and by nothing else.

## The commits, in order (measure first; one coherent unit each)
1. **THE INSTRUMENT FIRST (red-first by construction).** A walker arm in `tests/lint/goldenFreeze.walker.test.js` (read the file WHOLE; match its idiom; import the declared sets from their producer, never re-type them): every register row's `proofForm` is either null or a member of the door's lawful-form vocabulary (import it from the door); and a PURE helper exported by the door module — the function that decides what a successful re-record writes — is pinned by three cases: a null row + a lawful record form → the write set INCLUDES `proofForm`; a set row + an equal form → the write set EXCLUDES it (unchanged row); a set row + a different form → `PROOF_FORM_MISMATCH` (the existing refusal, unmoved). Written to FAIL against the uncured door (case one); quote that red.
2. **THE DOOR LEARNS.** The smallest change in `tests/helpers/goldenRecordDoor.js` that makes commit 1 green: the write set gains `proofForm` under exactly the ruled condition; every other refusal code, its order and its message are byte-unchanged (prove it: drive the report's §2 discrimination controls again from your scratch and quote them). PROVE END TO END IN A HERMETIC SANDBOX, exactly as TOOL-21 did (a scratch register, a scratch fixture, a scratch record, the signature variable set ONLY on `process.env` inside one node process rooted in your scratch): first use writes the form; second use with the same form passes and leaves the row byte-identical apart from sha/rows/ownerRow; second use with a different form refuses and writes NOTHING. ⛔ You never run the door against the real register, a real fixture or a real record, and you never set `GOLDEN_SHIFT_SIGNED` in a shell.
3. **THE README TELLS THE TRUTH.** `docs/shift-records/README.md`: the sentence at about `:62` ("Must equal the register row's `proofForm`") becomes the law as built (equal when the row carries one; LEARNED at first use when the row carries none); the sentence at about `:118` ("the register pins each one by hash") is corrected to what is measured (find the real count at your base — TOOL-21 measured 1 of 11 records pinned) with the consequence stated plainly. Navigate by the sentences, not the line numbers. A landed shift RECORD is never edited — only the README.
4. **THE REPLAY, AS A STANDING ARM.** The historical replay becomes executable: every committed `docs/shift-records/*.json` surface entry, run through the door's pure predicate against the register AS IT WOULD HAVE BEEN, refuses nothing (zero false positives) — a walker arm or a `tests/helpers` suite, whichever home the estate's idiom prefers (measure: where do the door's own tests live?). This is what keeps a future door change honest.

## Items TOOL-21 noticed that are NOT yours to build — report what you measure about each, touch nothing
The six fixtureless dormancy suites still owed the `proofForm` ruling the genesis note names; `espionage-rider-dormancy-fence`'s owner STOP enrolled as ordinary door traffic; `pdf-golden-view-model-snapshot`, where the door can never refuse. For each: one paragraph — what is true at your base, and the smallest ruling that would close it.

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files or ONE directory>`), output to a file, exit code captured from the command itself; eslint BARE. The directories that run WHOLE in your gated batch: `tests/lint`, `tests/helpers` if it holds suites, the home of the door's own tests, `tests/property` (the goldens — they must be byte-unmoved: hash `tests/fixtures/generator-golden-master.json` and `tests/fixtures/dossier-prose-manifest-golden.json` before your first edit and after your last), plus the directory of every test that NAMES `goldenRecordDoor` (`git grep -l -F goldenRecordDoor -- tests scripts`). The lighting census is FRESH at your base — record your exact delta (files · parked · credited · titles · suiteTitles); never refreeze. `UPDATE_GOLDEN`, `GOLDEN_SHIFT_SIGNED`, `LIGHTING_CENSUS_REFREEZE` and every `UPDATE_*` door are FORBIDDEN in any shell. PAUSE-AND-RESUME: one lane holds the gate at a time and it is not you yet — do everything ungated (reading, the sandbox proofs in plain `node`, the edits staged by explicit path), write `$SP/lane-fix-g4-scratch/FIX-G4.lane-resume.md`, commit NOTHING under `tests/` before `tests/lint` whole has run, and end your turn `PAUSED AT THE GATE — <the note's path>`. Trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never amend.

## Your report
`$SP/lane-fix-g4-scratch/FIX-G4.receipt.md` and your final message (the chair reads only that): outcome first; the pure helper's name and home; the three-case pin and its quoted red; the sandbox transcript (first use writes; equal second use is inert; unequal second use refuses and writes nothing); the unchanged refusal codes proven; the README's two corrected sentences quoted before and after; the replay arm's count; every count line; your lighting delta; ⛔ everything noticed and not touched, each item specific enough to slot.
