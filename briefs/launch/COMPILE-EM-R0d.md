# LAUNCH — COMPILE EM-R0d: three band LADDERS get one domain-layer home, so the town's invariants can read the PRODUCER'S OWN thresholds (re-entry family; built BEFORE EM-R0b; design §22.3 item 6)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, THIRTEEN steps — 13 is new today: line-addressed baselines). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-R0d-scratch/`.

**Why it exists.** `recordInvariants` (EM-R0b; its compiled draft and evidence are in kit `packets-waiting/EM-R0b.*` and `evidence/EM-R0b.*` — read its report's "four shape decisions" and chair question 1) checks that a band LABEL sits beside a SCORE its band contains. The chair's law: a band check reads THE PRODUCER'S OWN TABLE by import, never a copied number and never an envelope learned from a corpus (a learned envelope is narrower than the truth and would convict a DM's honest edit). Measured by that compile: only `severityBand()` (`src/domain/activeConditions.js`) is importable from the domain layer today; `legitimacyBandFor` is exported but lives under `src/generators`, and importing it from `src/domain/edit/` is a NEW domain→generators edge that `tests/build/domainGeneratorsBoundary.test.js` refuses (its own message: "invert, don't widen"); the READINESS ladder (`defenseProfile.readiness.label`) and the FOOD-SECURITY ladder (`economicState.foodSecurity.label` — and its per-flag vector) are exported by NOTHING.
**The packet's outcome (the chair's ruling on the shape — the EM-P3 shape; read `docs/implementation/packets/settlement-editor/EM-P3.md` whole, above all its PLACEMENT ruling and its byte pricing):** each of the three ladders' threshold TABLE moves to ONE domain-layer home as frozen exported DATA plus a tiny pure band function, and its generator imports it from there — so the value has one home and both the generator and `recordInvariants` read it. GOLDEN-NEUTRAL BY CONSTRUCTION (same numbers, same comparisons, same order of evaluation): prove it — `generator-golden-master`, the prose manifest and the preset witness must not move, and the build lane executes the hashes.
**Measure, do not assume:** (1) for each ladder: the producer (module, symbol), the exact thresholds and comparison operators (`>` vs `>=` matters at a boundary — extract them by reading, then CONFIRM by executing the producer at each boundary value ±1), every OTHER place the same numbers are spelled (a second copy is a finding: the Compendium's documented rungs, the Overview's colour bands, the PDF — a recon today found the `Surplus` rung is unreachable in 525 towns and that the food card's WORD and BAR are different facts; read kit `findings/RECON-G/RECON-G.report.md` §1 so you do not re-derive it), and whether the label is computed ONCE or re-computed per surface; (2) ⭐ PLACEMENT against the ZERO-SLACK generation worker (`WORKER_BUNDLE_CEILING_BYTES = 1,401,208` EXACT): the three producers are generators, so they ARE in that worker — moving a table out of a generator file into a domain leaf the generator imports adds a MODULE to the worker's closure (EM-P3 measured +634 B for the naive placement and +80 B after the placement cure: values in `src/data/`, a re-export in `src/domain/`). Find the placement that costs the fewest bytes, price it by minifying before/after over the real files, give the bound, and write the build lane's re-mint step with per-module attribution if any byte lands; (3) the eager first-paint set and the lazy engine chunk (`< 679_000`) the same way; edge-shared INPUT membership of every file you touch (SEVEN generated paths if any bundle moves — step 11); (4) the domain→generators boundary test's live edge set (4 files / 5 edges per EM-R0b's measurement) must not grow — your direction is the inversion it asks for: say whether your packet REMOVES an edge; (5) step 12 and 13: pins and fixtures on the moved symbols in `tests/`, and prose-numerics baseline rows (addressed by file + line) in every file whose lines you shift — quote them for the train's terminal.
Register predictions as DELTAS; `__BASE__` for the chair; the §7 table and the JSON manifest set-equal with actions from `PACKET_ACTIONS` (print the equality); requiredSymbols with the post-edit simulation (a symbol you MOVE is not a required symbol at its old path — EM-P3's build lane solved exactly this with a forwarding export: read how, and contract it).
