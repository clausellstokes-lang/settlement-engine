# FIX-D6 — ONE shared lowercase institution predicate: TOOL-8 localised the whole case-drift class to the one module whose predicate does not lowercase (`spatialGenerator.js:21`, `const has = (keyword) => instNames.some(n => …)`) — seven of the eight real FIX-D4 members are that helper; the predicate is written once, every site routed through it, and the golden re-recorded through the owner's door (signed under the standing grant of 2026-09-20) (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 54, 98 — §934.65 SIGNED; after TOOL-8a composes)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-8's report WHOLE (`$KIT/findings/TOOL-8/TOOL-8.report.md` — §1 the FIX-D4 anchor; §4.1 THE EIGHT: every real FIX-D4 member in `src/generators`, seven of eight ONE helper; the finding that `hasInst` in `npcGenerator.js:937` DOES lowercase (a vocabulary-coverage defect, not a case bug) while `spatialGenerator.js:21`'s `has` does NOT; §5.2 the registry + exact-vocabulary design; §5.3 the declared-dark allowlist; §5.4 the false-positive classes; the `candidates.json` / `dark-classified.json` beside it), TOOL-8a's receipt (`$KIT/findings/TOOL-8a/` — the registry and the exact-vocabulary lint it built; its declared-dark allowlist is the register your cure must keep true) and the shift record precedent (`docs/shift-records/2026-09-20-cure-j-provenance.json` — the form; CURE-J's receipt for the door's exact verb and its throw-by-design).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include TOOL-8a's composed commits (STOP if the registry is absent): `git -C $SP/slot-2 worktree add -b fix-d6-shared-predicate-2026-09-20 $SP/lane-fix-d6 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-d6/node_modules` (no build). Scratch `$SP/lane-fix-d6-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists — never touch it.

## The three commits, in order
1. **MEASURE, then the predicate.** Re-execute TOOL-8's instrument at your tip (its scripts are in its findings; the golden corpus 525 rows; the `PRED` shape's 272 sites over 11 helpers) — reproduce THE EIGHT or explain each difference. Then ONE predicate in ONE home (`src/generators/lib/institutionPredicates.js` or the home TOOL-8a's registry already names — never a second spelling): it lowercases BOTH sides (the names through `nativeSemanticNames(...)` and the keyword), takes the exact vocabulary from TOOL-8a's registry, and is what every `has`/`hasInst`/`hasInstNamed`/`hasIncomeSource`/`hasRes` site calls. Route `spatialGenerator.js:21` first (the seven), then `npcGenerator.js:937`'s `hasInst` (already lowercasing — the routing is a no-op by construction; prove it). Red-first: TOOL-8's D2 (`has('craft')` missing `Craft guilds (…)`'s lowercase arm) reproduced on the pre-cure tree — the Artisan Quarter gate's decision for a named golden seed — then flipped by the cure.
2. **THE GOLDEN, THROUGH THE DOOR.** The cure moves the generator golden (`tests/fixtures/generator-golden-master.json`) and possibly the prose manifest — a LEGITIMATE one-time output change the owner signed (§934.65 under the standing grant; the words verbatim: "I sign everything. Anything that you need for a signature now or in the future I give."). Write the shift record `docs/shift-records/2026-09-20-fix-d6-shared-predicate.json` in the estate's form (`ownerWords` verbatim, `ownerDate` 2026-09-20, `odqRow` §934.65, the cause, the seat, `surfaces[]` with `action: 're-record'`, `predictedRows` from the register, `proofForm` per surface), COMMIT it, commit the predicate, then run the door per surface (`GOLDEN_SHIFT_SIGNED=<record> <the surface's record verb>` — CURE-J's receipt has the exact spelling; the door writes fixture + register in one act and THROWS BY DESIGN — that throw is the success line); diff the golden: every moved row named and attributed to a site the cure changed (a row that moved for any other reason is a STOP); `predictedRows` must equal the moved count. TOOL-8's PREDICTION IS THE BAR: D2's Artisan Quarter and the seven's decisions move; the 173 PRECEDENCE_SHADOWED and the 545 INPUT_ABSENT move nothing.
3. **The prevention.** TOOL-8a's exact-vocabulary lint gains the arm that no generator calls `.some(n => n.includes(` on institution names outside the one predicate (counterforce: plant a raw `has` in a throwaway, red with the site named, restore); the declared-dark allowlist re-measured (a guard the cure lit is no longer dark — its row leaves the allowlist in this commit, shrink-only).

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/generators` whole, `tests/property` whole (the goldens), `tests/lint` whole (the lint moved); the lighting census FROZEN at the tuple the chair names at dispatch (record your delta; never refreeze); the edge-shared metas' `inputs` measured for every `src/` path you touch (regenerate and commit the seven if any is a member); the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; NUL-safe messages; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-d6-scratch/FIX-D6.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-d6-scratch/FIX-D6.receipt.md`: THE EIGHT reproduced; the predicate's one home and every routed site; the red-first; the shift record's path; the door's throw quoted per surface; the golden diff attributed row by row with `predictedRows` equal; the lint arm's counterforce; the allowlist's shrink; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
