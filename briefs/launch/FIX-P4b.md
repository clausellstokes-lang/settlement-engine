# FIX-P4b — two shipped comments describe a flag that does not exist; 49 unguarded `console.*` calls in `public/map/**` (our four files) channelled or frozen (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 83 — FIX-P4's hand-off)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-P4's receipt (`$KIT/findings/FIX-P4/FIX-P4.receipt.md`, read-only — its F15 walker `tests/lint/embeddedMapConsoleGate.walker.test.js`, the flag block in `public/map/main.js`, its noticed items 1 and 2) and, in your worktree, WHOLE: `src/components/PipelineReveal.jsx`, `src/application/commands/settlementGenerateAction.js` (the paragraph near `:581-583`), `src/lib/flags.js`, `src/components/GenerateWizard.jsx` (grep `flag(` — measure, do not assume), `tests/lint/embeddedMapConsoleGate.walker.test.js` (its header says what it does NOT cover), and the four `public/map` files that are ours: `sf-bridge.js`, `main.js`, `cloud.js`, `load.js`.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch (it must INCLUDE FIX-P4's two composed commits `3b7eb66ee` and `268d0846c` — if the tip does not, STOP and say so): `git -C $SP/slot-2 worktree add -b fix-p4b-2026-09-20 $SP/lane-fix-p4b <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-p4b/node_modules` (no build). Scratch `$SP/lane-fix-p4b-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The two commits, in order (each by explicit pathspec; each proved before the next)
1. **The phantom flag.** `PipelineReveal.jsx:35` says "Flag: `pipelineReveal`, default on in prod" and `settlementGenerateAction.js:581-583` says "Gated by the flag at the consumer site (GenerateWizard)"; measured by FIX-P4, there is no `pipelineReveal` in `src/lib/flags.js` and no `flag(` call in `GenerateWizard.jsx` — the reveal is unconditional. THE CHAIR RULED: CORRECT THE COMMENTS TO THE TRUTH (the reveal is unconditional; say since when if `git log -S pipelineReveal` tells you), never invent the flag — a new flag is new capability and the owner's; if you find the reveal IS gated somewhere else, STOP and report the site. Comment-only edits; no logic byte; the two files' behaviour byte-identical (measure: the built module bytes or the focused tests unchanged). Then grep the estate for every other comment naming `pipelineReveal` (`git grep -n pipelineReveal -- src tests docs`) and correct or record each.
2. **The console calls in our four map files.** FIX-P4's walker guards the flag block; 49 unguarded `console.*` calls remain in `public/map/**` — 22 in vendored libs (NOT yours: record them, touch nothing under a vendored path; say which paths are vendored and how you know), and ours: `sf-bridge.js` 9, `main.js` 6, `cloud.js` 2, `load.js` 1. For each of the 18: channel it through the existing deployed-host guard the F15 cure established (read how `main.js` mutes on a deployed host and reuse that exact idiom — no second mechanism), or, where the call is a genuine error report the operator must see, leave it with a one-line `// console: kept —` reason. Then widen the walker's arm to the four files (its header currently says it covers only the flag block — the widening is the point; the header changes with it) with a counterforce measured (plant an unguarded `console.log` in one of the four in a throwaway, see the walker red, restore the exact SHA-256, green). No behaviour change on a non-deployed host (the dev console keeps every line). `addLakesInDeepDepressions` in `main.js` stays owner-gated per the fork runbook — untouched.

3. **The walker is CREDITED, not parked (addendum 98; COMPOSE-REPROOF-1's finding).** `tests/lint/embeddedMapConsoleGate.walker.test.js` is PARKED by the lighting walker's grammar — `TEST_TABLE_UNPROVEN:test.each()` ×3, because `test.each(MUTED_CHANNELS)` passes an Identifier where `staticTableRows` requires an `ArrayExpression` — so its 16 titles / 4 suites contribute nothing to the sovereignty census, and it consumed the last slot of the `eachOnly` ceiling (111 of 111) and sits beside `literalTableStillParked` at 50 of 50. Rewrite the three `test.each(MUTED_CHANNELS)` registrations as a literal table (the array spelled inline at the call) or as body loops inside plain `it` titles — whichever the walker credits (run its `parkReasonsFor` over the file until it prints `[]`; quote it); the arms' assertions unchanged. Expected lighting delta from this commit: `parked −1 · credited +1 · titles +16 · suiteTitles +4` (record what the walker evaluates; never refreeze) and `eachOnly` 111 → 110 (quote the ceiling arm's count). ⚠ The obvious reformat is convicted by the same arm — read `eachOnly`'s and `literalTableStillParked`'s arms whole before you write.

## Laws
Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line; `tests/lint` WHOLE once (the ONLY permitted red is the lighting census; the register is FROZEN at the tuple the chair names at dispatch — run the walker once, record your delta, never refreeze); eslint on every touched file; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-p4b-scratch/FIX-P4b.lane-resume.md` (the gated batches in order), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-p4b-scratch/FIX-P4b.receipt.md`: the phantom-flag census (every comment, corrected or recorded); the 49-call ledger (vendored / channelled / kept-with-reason); the walker's widening and its counterforce quoted; every count line; the goldens' hashes; the lighting delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
