# FIX-C2d — the 209 remaining ARM 3 document leads over 45 documents, cured READ-BY-READ through FIX-C2c's harness (never unread: the sampled lead-invalidity is 5%, the symbol-attribution error 20% — curing blind writes ~10 wrong addresses over correct ones), with the RECORD-CLASS annotated and never re-addressed (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 91, 101, 105–106 — after FIX-C2c composes)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-C2c's receipt WHOLE (`$KIT/findings/FIX-C2c/FIX-C2c.receipt.md` — its two measured rates, its twenty-sample method, its cure harness `cure-citations.mjs` (single-match + line-count-invariant), its "noticed" items 1–2 which ARE this lane, and item 6's MISPATHED class), FIX-C2b's receipt (`$KIT/findings/FIX-C2b/` — the ruling on `WC-PREAMBLE.md:70`, the record-class precedent) and the roster `$KIT/findings/FIX-C2c/ARM3-REMAINING-ROSTER.txt` (209 rows over 45 documents; top: GEOPOLITICAL_WAR_LAYER 18 · DESIGN_FP_ARCH_GR 16 · FABLE_VALIDATION_QUEUE 13 · DESIGN_FP_ARCH_SP 12 · DESIGN_FP_ARCH_TR 12 · EP-SUBSTRATE 11).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-C2c's three composed commits (STOP if `tests/lint/sourceCitationIntegrity.shared.mjs` lacks ARM 5): `git -C $SP/slot-2 worktree add -b fix-c2d-doc-citations-2026-09-20 $SP/lane-fix-c2d <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-c2d/node_modules` (no build). Scratch `$SP/lane-fix-c2d-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists — never touch it.

## The chair's two rulings (addendum 106) — they bind this lane
- **R1 — READ, THEN CURE.** Every one of the 209 rows is read against LIVE source before its cure: the cited symbol or construct is found at the new address by eye (quote the line), or the row is classified (below). A row cured without a quoted live line is a defect of this lane. The harness's single-match + line-count-invariant rule stays; a row it refuses is classified, never forced.
- **R2 — THE RECORD-CLASS IS ANNOTATED, NEVER RE-ADDRESSED.** A row that RECORDS a past measurement (a survey refutation — "REFUTED AT THE SURVEY", "NO exported `alignmentOf` exists anywhere" — an audit PASS row quoting an expression that no longer exists, a plan row whose range names a catalog) is a historical record: re-addressing it falsifies the record. Such a row gets an inline as-of mark the walker honours — the spelling `(as of <12-hex sha>)` immediately after the address, the sha being the commit at which the record was true (find it with `git log -S` / `git blame` on the document row; the row's own commit is the default) — and the walker's ARM 3 skips any citation carrying the mark. Additionally a `records` roster in `sourceCitationIntegrity.shared.mjs` names WHOLE documents that are records (`docs/TEMPORAL_AUDIT.md`, `docs/COMPREHENSIVE_REVIEW*.md`, `docs/ASSESSMENT.md`, `docs/shift-records/**`, every `docs/archive/**`) — excluded from ARM 3 by path, the same mechanism as the LANDED-packet rule. FIX-C2c's four named rows (`DESIGN_FP_ARCH_CW.md:44`, `DESIGN_FP_ARCH_SP.md:142`, `TEMPORAL_AUDIT.md:99`, `COHESION_REMEDIATION_PLAN.md:114`) are the first four marks; the lane finds the rest by reading.

## The four commits, in order
1. **THE INSTRUMENT'S TWO NEW RULES.** The `records` roster and the as-of mark in `sourceCitationIntegrity.shared.mjs`; ARM 3 prints the skipped-by-mark and skipped-by-roster counts as its own report lines. Red-first: plant a marked citation in a throwaway document → ARM 3 counts it under `skipped-by-mark` and does not report it; remove the mark → reported. Counterforce for the roster likewise. The LANDED rule untouched.
2. **THE RECORD-CLASS, MARKED.** Every record-class row in the 209 receives its as-of mark (the sha quoted in the receipt row by row with the `git log -S` line that dates it); the four named first. No address changes in this commit.
3. **THE CURES, READ-BY-READ.** The remaining rows through the harness, ONE document per harness invocation, each row's quoted live line in the receipt; a row the harness refuses or the read cannot place is classified: `UNRESOLVABLE` (the construct is gone — the citation becomes prose "(formerly at …)", never a guessed address), `MISPATHED` (FIX-C2c item 6 — the file moved; the path is re-cut when the symbol is at the new path, quoted), `AMBIGUOUS` (two candidates — the row names both in a footnote and stays; ARM 5's extent/directory/symbol rules decide where they can). `git diff --numstat` +N/−N on every document (line-count invariant).
4. **THE MEASUREMENT.** ARM 3 re-run over live docs: the count from 209 → the residue, every residue row classified in the receipt; the twenty-sample re-drawn from the CURED rows (lead validity and symbol attribution both re-measured — the bar is 20/20 on a fresh systematic sample; below that, the sample's misses are re-read and cured before the commit).

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/lint` WHOLE (the instrument moved), `tests/docs` WHOLE (its readers of the documents you edit), the two `tests/domain` and two `tests/property` filesystem readers FIX-C2c's resume note names (EXPLICIT files — never `tests/property` whole); the lighting census FROZEN at the tuple the chair names at dispatch (record your delta; never refreeze); the three prose-manifest recorder files are off limits; LANDED packet bodies are never edited (they are frozen evidence — the LANDED rule excludes them; a lead inside one is a STOP with the row quoted); quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; NUL-safe messages (spell control characters in words); the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-c2d-scratch/FIX-C2d.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-c2d-scratch/FIX-C2d.receipt.md`: the roster's disposition row by row (cured with the quoted live line · marked with the dating sha · UNRESOLVABLE · MISPATHED · AMBIGUOUS), the counts per class summing to 209; both counterforces; the fresh twenty-sample's two rates; ARM 3's before/after; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
