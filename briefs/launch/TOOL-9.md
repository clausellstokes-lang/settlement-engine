# TOOL-9 — the lighting walker's READ-ONLY PRINT MODE: the whole tuple, every moved figure, nothing written (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 51's charter row)

You are an Opus PARALLEL TOOLING lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you — PAUSE-AND-RESUME at every gated batch; the 2026-09-20 addenda: the directory-whole rule, the zsh-pathspec warning, the trailer ruling), then, in your worktree, WHOLE: `tests/lint/sovereigntyLightingContract.walker.test.js` (the census arm — how `measureCensus` derives `files · parked · credited · titles · suiteTitles`, the CREDITING grammar, the assertion ORDER that short-circuits at the first moved figure, and the refreeze door `LIGHTING_CENSUS_REFREEZE` / `LIGHTING_CENSUS_NOTE` that writes `tests/lint/.lighting-census-baseline.json`), the register file itself, and the kit's `logs/lighting-refreeze-3-door.log` + `lighting-refreeze-3-plain.log` (what the door prints today).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b tooling-9-2026-09-20 $SP/lane-tool-9 5a3380e8d`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-9/node_modules` (no build). Scratch `$SP/lane-tool-9-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The problem (measured by every lane tonight)
The census arm asserts the five figures in order and STOPS at the first that moved, so a lane whose change moves `titles` can record only `titles` — `suiteTitles` after it is unreached, and every lane's delta on the later figures is PLAUSIBLE, never measured (CURE-I: "suiteTitles was not reached (short-circuit)"; FIX-K1, TOOL-7a, EM-B1k2 the same). The full tuple is readable today only through the refreeze door, which WRITES the register — the chair's act, never a lane's.

## The ruling (the chair; vetoable)
1. **`LIGHTING_CENSUS_PRINT=1`** — a READ-ONLY mode of the same walker file: it runs `measureCensus` exactly as the census arm does, prints ALL FIVE measured figures beside the register's five and the per-figure delta (e.g. `files 2652 (=) · parked 383 (=) · credited 2269 (=) · titles 25056 (+13) · suiteTitles 6681 (+2)`), and prints the parked-roster delta by file if `parked` moved; it WRITES NOTHING (the register's bytes identical before and after — asserted by the mode itself, sha256 both sides); the process exits the way vitest exits (the census arm still asserts in this mode, so the run is red when the tuple moved — the point is the print, not a green). The normal mode's assertion ORDER is UNCHANGED (the short-circuit is the ratchet's law; do not reorder). The refreeze door is UNCHANGED.
2. **One place derives the figures.** If the print mode and the census arm would compute anything twice, extract the one function; the door, the arm and the print mode all call it (a print that re-implemented the count would print a different census).
3. **Self-tests, in the file's own idiom:** the print mode writes nothing (register sha256 equal before/after a print); the print names all five figures and their deltas; a synthetic register with one figure moved prints that figure's delta and no other. Pair every `.not.` with its `// anchored:` marker on the immediately preceding line.
4. **The receipt idiom for lanes, written into the walker's header:** "a lane records the tuple `LIGHTING_CENSUS_PRINT=1` prints, and its delta per figure; it never refreezes". The chair will put the same sentence into the lane briefs at composition.

## Laws
No `src/` edit. Goldens untouched (`shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before and after). The register `tests/lint/.lighting-census-baseline.json` is NOT a path of this lane — if any step would change its bytes, STOP. Your own titles move the census: run the walker ONCE separately (and once in print mode, which is the proof of your own work), record the tuple and your delta against the FROZEN `2652·383·2269·25043·6679`, NEVER refreeze. The directory-whole rule: your edit under `tests/lint` runs `tests/lint` WHOLE once (lawful red: the lighting census by your own delta; nothing else). The standing instruments (negativeAssertionAnchor, voiceMechanics, mutationCoverageManifest — the walker's existing row stays true; eslint on the file). Every vitest line through the mutex SHARED tier, worker-capped, ONE directory per invocation, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN. Spell every path inline. ONE commit by pathspec of the one file (`TOOL-9: …`; the body carrying the print mode's own output over the live tree); the trailer your harness prescribes. PAUSE-AND-RESUME: the gate is shared two at a time and both runs are taken now — do everything ungated, write `$SP/lane-tool-9-scratch/TOOL-9.lane-resume.md` (batches in order), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
The receipt by execution: the print mode's full output over the live tree (five figures, five deltas), the sha256 of the register before and after, the self-tests' count lines, `tests/lint` whole, eslint 0, the goldens' hashes, the commit sha; ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
