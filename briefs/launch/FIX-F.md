# LAUNCH — FIX lane F: three recorded follow-ups of the 2026-09-18 fixes chain (FIX-F1 · FIX-F2 · FIX-F3), one commit each

Read `briefs/LANE-PARALLEL.md` (chair kit) WHOLE and obey it. Worktree, branch and base are named in the dispatch message. Scratch: `$SP/lane-fix-f-scratch/`. Lane id: `FIX-F`. ⚠ The chair recorded these three from the previous chair's ledger and has NOT re-verified them against the code: for EACH, your first act is to MEASURE whether the defect still exists at your base (a follow-up already cured by a later car is reported as CURED-ALREADY with the commit that cured it, and you move on).

## FIX-F1 — the eight tabs still on the tier-noun backstop
The ruling (read it whole): `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md | grep -n "934.22 addendum"` — §934.22 addendum ("THE TIER NOUN BELONGS TO THE PRODUCER": the noun is derived ONCE, in the desk's producer, from a `tierNoun` the caller's option bag carries, and every surface renders the desk's exact sentence) and addendum 2 ("THE TIER NOUN LANDED IN THE DESK'S BAG; EIGHT TABS STILL LEAN ON THE BACKSTOP" — it names the eight and the backstop; lane 37's car `79b143dae` is the model cure for the Defense, Viability and Overview tabs). **Work:** each of the eight passes `tierNoun` in its option bag exactly as the three cured tabs do, so the backstop is reached by NO caller; then the backstop's fate is the CHAIR's ruling — report whether it can be deleted (no caller) and what pins it; do not delete it. **Proof:** the screen sentence equals the desk's sentence for a metropolis AND a village on each of the eight (extend the existing DS-* / tab suites; their expectations for already-cured tabs stay unchanged). ⛔ NEITHER GOLDEN MOVES (lane 37's first cut moved 4,214 of the prose manifest's rows by deriving the noun inside the desk reader — that is the trap; the noun travels in the caller's bag).

## FIX-F2 — the prose-numerics detector's missing `weight` token
The estate's prose-numerics law: no raw engine numeral or mechanic word reaches reader-facing prose; a detector (find it: the prose-numerics walker / register under `tests/lint/` or `tests/copy/`) carries a token vocabulary, and `weight` is missing from it. **Measured first:** the detector's vocabulary and how tokens are matched (word boundary? case?); how many CURRENT hits adding `weight` produces across the scanned surface, each classified — a real leak (an engine weight spoken to a reader), or a legitimate English use ("the weight of the crown", a cargo weight that IS the fact). **Work:** add the token with the narrowest matcher that catches the leaks; every real leak is cured at its producer in the estate's voice (no em dash, no exclamation point); a legitimate use gets the detector's own reasoned allow-list row, never a blanket skip. If the real leaks number more than FIVE, STOP after the detector change is drafted and report the list — the cure of many producers is its own lane and the chair sequences it. ⛔ A prose change can move the dossier prose golden: a leak whose cure moves `tests/fixtures/dossier-prose-manifest-golden.json` is a STOP with the rows named (a golden shift is signed by the owner's door, never by a lane).

## FIX-F3 — the two uppercase sites kept because their screen twins still shout
Lane 15 (typography) deferred two sites — `Services` (near line 161 at the time) and `SupplyChainFlow` (near line 208) — that render an UPPERCASE label because the matching on-screen twin still renders uppercase, and changing one alone would make screen and print disagree. Find both by symbol and by the `uppercase` / `textTransform` / `toUpperCase` spelling; find each one's TWIN (the other surface that renders the same label: the PDF, the print sheet, or the sibling tab). **The chair's ruling on the shape (owner law 2026-09-19: the dossier is a tome; its chrome speaks the scribe's grammar — rubrics and small capitals, never a dashboard's shouting):** cure BOTH twins of each pair together so they agree, in the typographic idiom the lane-15 typography car established for the labels it DID cure (find that car on the branch: `git log --oneline --grep="typograph" -i` and read its diff for the model). If a twin lives in a surface with a byte ceiling or a snapshot, price it and say so. If the pair cannot be cured together inside this lane's scope, report why and leave both untouched.
