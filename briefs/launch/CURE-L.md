# CURE-L — RUN 23's three reds, two causes, two commits, IN THE SLOT ON THE BRANCH, holding the gate (the chair, Fable 5.1, session 4a1823e2, 2026-09-20 15:2x; ODQ §934.47 addendum 114)

RUN 23 (`npm run check` in the consist at `e45c4738b`) exited 1 at 15:13:42 in `test:ratchet` with THREE failing tests outside the frozen census (which is zero). Validators, both typecheck ratchets and lint were green; the `&&` chain stopped, so `build` and `verify:dist` were NOT reached (the chair runs those two bare in the consist while you work). Log: `<SP>/consist-check-2-run23.log` lines 418–436.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · `SLOT=$SP/slot-2` (ON `fixes-2026-09-18-consist`, tip `e45c4738b` = the eighth lighting refreeze `2665·359·2306·25529·6815`, tree clean) · scratch `$SP/lane-cure-l-scratch/` (create it). `$SP/chair-kit-923472dc/briefs/LANE-PARALLEL.md` binds WHOLE, including the addenda the chair appended today at its foot. You hold THE SLOT AND THE GATE for this window: every vitest line through the mutex, SHARED tier, worker-capped, the two exports inline, ONE directory or explicit file list per invocation, output to a file in your scratch, the exit code captured from the command itself (never through a pipe); eslint BARE.

## CAUSE A — two titles, `tests/data/sampleSettlements.test.js` (the fork-door walker of REVIEW-P F1) convicts FIX-P1d's own measuring call
```
the fork doors — the forker is resolved, never assumed › every CALL of forkSeedFor passes an identity resolved by forkIdentity
  AssertionError: a fork door passed a bare id: a signed-out reader then falls back to the constant 'anon' and shares one world with every other visitor (REVIEW-P F1). Wrap it: forkSeedFor(sample, forkIdentity(authUserId)) — lib/anonForkSalt.js: expected [ Array(1) ] to deeply equal []
the fork doors — the forker is resolved, never assumed › every door that calls forkSeedFor imports forkIdentity
  AssertionError: expected [ 'src/lib/anonForkSalt.js' ] to deeply equal []
```
FIX-P1d (`ee204c827`) made `src/lib/anonForkSalt.js` measure the address ceiling by RUNNING `forkSeedFor` at the widest suffix — a CALL of `forkSeedFor` inside a module the walker reads as a door. The lane proved `tests/lib`, `tests/components` and `tests/lint` whole and never ran `tests/data`, where this walker lives.

**The chair's ruling on the shape.** The walker's claim stays TRUE and UNWEAKENED for every real door: after your cure, a planted bare-id call in a real door (or in the fixture the walker reads) must still red BOTH arms. Preferred shape, MEASURE BEFORE BUILDING: (i) the measuring call passes an identity RESOLVED BY `forkIdentity` — a DECLARED synthetic signed-in id whose suffix is the widest the door can be handed — if and only if that path is PURE at module evaluation (no storage read, no clock, no RNG, no `window`): read `forkIdentity` whole and prove it; (ii) arm 2 learns that the module which DEFINES `forkIdentity` owes no import of it — a definition is stronger than an import — the smallest honest amendment, written into the walker's header with its reason and a counterforce arm (a module that neither defines nor imports it still convicts). `FORK_SEED_MAX` stays **26**; the OSR checker stays exit 0 with NO new row for `src/lib/anonForkSalt.js` (that was the point of FIX-P1d); no golden moves. If (i) is impure, or moves `FORK_SEED_MAX`, or re-mints the OSR row, STOP with the measurement and the alternative priced (the ceiling measured in the module that owns `forkSeedFor`, imported by `lib`) — do not improvise a third shape, and never exempt `anonForkSalt.js` from the walker by filename.

**Red-first and proofs.** Both titles RED at `e45c4738b` (quote the run), GREEN after; the plant still reds both arms and is restored byte-identically (sha before and after). Then `tests/data` WHOLE, `tests/lib` WHOLE, `node scripts/check-observed-shape-readers.mjs` (exit captured), `npx eslint` bare on every touched file.

## CAUSE B — one title, `tests/domain/ruinInstitution.test.js` (EM-B1e's acceptance arm A4) pins a STALE literal of a golden that moved through the owner's door
```
EM-B1e — the pulse ruin shape has exactly one writer › A4 the pulse history does not move: both goldens and the preset witness are bytewise unchanged
  AssertionError: expected '88983938ddcf28341031e186d855fef950e6b…' to be '921c51cf6799ffdfdbffa3715fb496f7d15ce…'
```
`921c51cf…` is the prose-manifest golden's sha BEFORE CURE-J's signed, provenance-only re-record (`docs/shift-records/2026-09-20-cure-j-provenance.json`, ODQ §934.71; rows 1050 → 1050); `88983938…` is the file today and the freeze register's pin. The arm kept its own literal copy of the hash, so a LAWFUL re-record reddened it.

**The chair's ruling on the shape.** A test never carries its own literal copy of a frozen golden's sha: `tests/fixtures/.golden-freeze-register.json` is the ONE pin, and the owner's door updates it at every signed re-record. CENSUS FIRST, with its denominator: every literal occurrence, under `tests/`, `src/` and `scripts/`, of any current or former sha256 of `tests/fixtures/generator-golden-master.json`, `tests/fixtures/dossier-prose-manifest-golden.json` and the preset lighting witness (`git grep -n -P` on the first 12 hex of each hash you can establish from the register's rows and from `git log -p` of the register — `7177cd6e89eb`, `88983938ddcf`, `921c51cf6799`, the witness's, and their predecessors). Disposition every site: (a) an ARM whose claim is "this artefact's bytes are what the estate pins" → re-pointed to READ the register's row for that surface by its surface id and compare the file's live sha to it — a signed re-record can never red it again, an unsigned movement still does; (b) a comment or record that states a past hash as history → untouched (annotate as-of only if it reads as live); (c) the register, the shift records, packet bodies → untouched. A4 is class (a) for each artefact it pins that has a register row; if the preset witness has no register row, say so and leave that one literal with a comment naming the door that would move it. Read `tests/lint/negativeAssertionAnchor.walker.test.js` and the mutation-coverage manifest for any row that names A4 or a site you touch.

**Red-first and proofs.** A4 RED at `e45c4738b` (quote), GREEN after; and PROVE THE RE-POINTED ARM STILL BITES without touching the real golden or the real register: a scratch copy of the arm (or a pure helper you extract for it) fed a scratch register whose pin is wrong must red. Then `tests/domain` WHOLE (report its wall-clock) and the WHOLE directory of every other file the census makes you touch.

## Both
Two commits on the branch in the slot, by explicit pathspec, after reading `git diff --cached` whole — `CURE-L: the fork-door walker reads a measurement as a measurement, and still convicts a bare id` and `CURE-L: a test reads a frozen golden's pin from the freeze register, never from its own literal` (adjust the clause to what you actually built); bodies carry every count line, the census with its denominator, the plants, the goldens' hashes before and after (`7177cd6e…` / `88983938…`, unmoved); trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; never amend. LAST: `tests/lint` WHOLE at your tip — the lighting register is FRESH at `e45c4738b`, so the lighting walker is GREEN unless you added or removed a title: if you did, its red is EXPECTED — quote the figure it stopped at and state your exact delta (files · parked · credited · titles · suiteTitles); ⛔ NEVER refreeze, the chair does. `UPDATE_GOLDEN`, `GOLDEN_SHIFT_SIGNED`, `LIGHTING_CENSUS_REFREEZE` and every `UPDATE_*` door are FORBIDDEN. FORBIDDEN paths: `$SP/consist`, every lane worktree and scratch but your own, the read tips, the ledger checkout, the kit, the memory directory. Finish with `git -C "$SLOT" status --short` EMPTY and `git show --stat` of each commit naming exactly its paths.

## Your report (the chair reads only your final message)
Outcome first; the two shas and the new tip; each proof CONFIRMED with its verbatim count line and exit code, or FAILED with the verbatim assertion and your attribution; the census table for cause B; your lighting delta; ⛔ everything noticed and not touched, each item specific enough to slot — in particular any OTHER walker or pin living outside `tests/lint` that a `src/` lane would not meet by running its own directories. End with `SLOT AND GATE RELEASED — tip <sha>`.
