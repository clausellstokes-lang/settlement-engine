# COMPOSE-REPROOF-1 — the first composition sitting's re-proof: seven lanes' commits cherry-picked onto the integration branch after EM-B1f's landing; every lane's declared count lines re-executed ONCE at the composed tip, consolidated by directory (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 93–94)

You are an Opus VERIFY lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus verifies. You EDIT NOTHING and COMMIT NOTHING; you re-execute and report. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then each composed lane's receipt (read-only; the count lines each declared are your denominator): `$KIT/findings/FIX-L1/FIX-L1.receipt.md`, `FIX-P3/FIX-P3.receipt.md`, `FIX-P4/FIX-P4.receipt.md`, `FIX-P5/FIX-P5.receipt.md`, `FIX-P1/` (FIX-P1 and FIX-P1b), `FIX-C2b/FIX-C2b.receipt.md`, `TOOL-13b/TOOL-13b.receipt.md`.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR TREE: a read tip at the COMPOSED tip the chair names at dispatch (`git -C $SP/slot-2 worktree add --detach $SP/read-tip-compose1 <tip>`; `ln -s $SP/slot-2/node_modules $SP/read-tip-compose1/node_modules`; no build). Scratch `$SP/lane-compose-reproof-1-scratch/`. `git status --short` at the read tip EMPTY at start and end; no git mutation. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*`, the ledger checkout, the kit.

## What you prove (each line executed, its count printed, nothing inferred)
The composed tip carries, over the sixth refreeze `fb39b1ad6`: FIX-L1 (2 commits) · FIX-P3 (5) · FIX-P4 (2) · FIX-P5 (5) · FIX-P1 + FIX-P1b (2) · FIX-C2b (4) · TOOL-13b (1). Reproduce each lane's declared receipts at THIS tip, consolidated so each directory runs ONCE (the union of what the seven lanes declared — a lane's pin that was green on its own branch must be green here, and a count that differs from the lane's is reported with the difference explained by the composition, never smoothed):
1. `tests/lint` WHOLE — expected reds: the lighting census (the composition moved the register's figures: L1 −25 parked / +25 credited / +330 titles / +108 suites; P3 +5 files / +36 titles / +10 suites; P4 +1 file; P5 +1 file / +16 titles / +3 suites; P1 +1 file / +27 titles / +5 suites; C2b +3 titles; 13b +1 title — the walker asserts its five figures in order and stops at the first miss, so quote what it evaluated and say the chair's seventh refreeze measures the rest) and `tests/property/dossierProseManifest.test.js`'s provenance title if swept (owner-gated, §934.71). Any OTHER red is a composition defect: name it, the lane whose commit introduced it (bisect by cherry-pick order in your scratch — the seven lanes' shas are in their receipts), and STOP that section.
2. `tests/components` WHOLE (P3's 297 files / 2,086; P4's postGenCoach 22; P5's dossier suites 52 and the two censuses; C2b's none) · `tests/ui` WHOLE (P5's 162 / 1,076; P3's 73) · `tests/pdf` WHOLE (P5's 46 / 458) · `tests/lib` WHOLE (P1b's 174 / 1,797) · `tests/domain` WHOLE (L1's 277 + B1f's landed suites) · `tests/domain/autonomy` (L1's 14) · `tests/generators` (L1's 2) · `tests/property` WHOLE (L1's 34; the goldens; EM-B1f's npcs.property) · `tests/copy` WHOLE (P3's 151 / P5's 155 — say which count the tip prints and why) · `tests/data` (P1b's 22; C2b's dossierStateProseProjection 79) · `tests/store` (P3's 14; P1b's 6) · `tests/config` (P5's 44) · `tests/design` (P5's 73) · `tests/build` ×6 files as C2b listed (56 passed | 42 skipped — a skipped dist arm is a skip, say so) · `tests/scripts/implementationSession` (C2b's 10) · `tests/security` if any lane touched it.
3. `npx eslint` BARE over the union of every composed lane's touched files (`git diff --name-only fb39b1ad6..<tip> -- '*.js' '*.jsx' '*.mjs'`), exit 0.
4. `npm run validate:packets` (`valid: 194 packets (0 READY)`); `node scripts/wiring-census.mjs --check`; `node scripts/check-observed-shape-readers.mjs` plain (`exactly matching the frozen inventory`); `node scripts/check-writer-reach.mjs` if any lane's receipt names it; both goldens' SHA-256 (`7177cd6e…8f1e`, `921c51cf…db41` — byte-identical to the landing).
5. The composition's file-set proof: for each of the 21 cherry-picked commits, `git show --name-only` at the tip equals the source commit's set (the chair verified this at composition; re-verify), and no file was touched by two lanes in a way that lost either's hunks — for the two files two lanes touched (`tests/components/phoneChromeFloor.census.test.js`; `scripts/mutation-coverage-manifest.json`) prove both lanes' rows are present.

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <one directory or explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only; a line with no count DID NOT RUN — re-run it); eslint BARE; FIX-B2's exclusive builds share the mutex with you — wait your turn through it; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; a foreign stash exists in the shared tree — never touch it. You are granted the gate at dispatch (the composition's re-proof is the train's next step). If a command errors on a rate limit, stop and say so.

## Your report
`$SP/lane-compose-reproof-1-scratch/COMPOSE-REPROOF-1.receipt.md`: every count line by directory with the lane(s) it discharges; the expected reds quoted and the walker's evaluated figures; any unexpected red with its bisect; the eslint set and exit; the five script lines; the goldens' hashes; the file-set proof; the stamp read from `date`; ⛔ everything noticed and not touched. End your turn with the receipt's path and one line: GREEN except the named expected reds, or RED with the lane named.
