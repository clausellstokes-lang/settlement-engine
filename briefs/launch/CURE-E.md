# LAUNCH — CURE-E: run 17's ONE red — the trade-route token floor in `tests/domain/humanizeEngineTokens.test.js` (measure first; the chair rules the cure's shape; written by the chair, session a9df403c, 2026-09-20)

**Seat:** an Opus CURE lane — measure, then PAUSE for the chair's ruling, then cure on the branch under pause-and-resume. Scratch: `$SP/lane-cure-e-scratch/`. `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`.

**The red (the ratchet's own JSON report of run 17, taken at 02:25 EDT on the tip `1e5bcf83d`; 33,580 tests, 33,578 passed, ONE failed):** `tests/domain/humanizeEngineTokens.test.js` › "the authored display lexicon — typed buckets, closed and enumerable › covers EVERY trade-route token the generator can actually produce" — `AssertionError: expected 3 to be greater than or equal to 5` at `humanizeEngineTokens.test.js:163:27`. Run 16 was green end to end at `f31ca0eb8` (33,555 tests). Since then the branch gained EM-B3a (`668d87512`), EM-P3 (`f4e5b64c5` — the world-fact option sets' one home; it moved `src/generators/steps/resolveConfig.js`), EM-B1e (`8f714cf3c`), EM-B1d (`95e494bdb`), the five cures (`08cb42e31`…`32602dc60`) and docs; every landing's receipt shows the two goldens byte-identical.

**MEASURE FIRST, ungated (plain `node`; no vitest until the chair hands you the gate):** (1) read the test whole — what SAMPLE does the arm draw (fixed seeds? the golden corpus? a random draw?), what is a "trade-route token", and where does the generator produce them (find the producer by symbol); (2) replicate the arm's sample and count in a plain-node probe at each of these read-only tips — `$SP/read-tip-a41a0e109` (the green era), `$SP/read-tip-58fcfe614` (after B3a + P3), `$SP/read-tip-ad7ddf2c9` (after B1e), `$SP/read-tip-e5bdfd031` (after B1d), `$SP/read-tip-32602dc60` (after the cures; the code tip — the later commits are docs and a register) — every tip's `git status --short` must be EMPTY before and after, and you WRITE NOTHING in any of them; (3) say whether the count is DETERMINISTIC at a tip (run the probe three times) and which landing moved it, quoting the tokens produced before and after; (4) if a landing moved it, read that landing's diff for the token producer and name the mechanism; if the sample is random, say so — the floor is then flaky by construction and the cure is a deterministic sample, never a lowered floor. ⛔ Write `$SP/lane-cure-e-scratch/CURE-E.measurement.md` and END YOUR TURN with `PAUSED FOR THE CHAIR'S RULING — <path>`. The chair rules the cure's shape; you then build it on the branch in the worktree the chair names, red-first, under the mutex's shared tier with the two exports inline, pausing at each gated batch, and commit by pathspec with the count lines in the body.

FORBIDDEN: `$SP/consist` (the chair's terminal worktree), `$SP/slot-2` (a sealed build), every `$SP/lane-*` but your scratch, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push. Report anything noticed and not touched, each item specific enough to slot.
