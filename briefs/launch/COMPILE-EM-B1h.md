# LAUNCH — COMPILE EM-B1h: the pulse's FATE vocabulary is CLOSED before a ninth spelling can land (train EM-T6, after EM-B1f and BEFORE EM-B1a)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, TWELVE steps). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-B1h-scratch/`.

**Where it came from (read both):** EM-B1e (LANDED in your tree: `docs/implementation/packets/settlement-editor/EM-B1e.md` — its §6, its §13 R1/R2 and the chair's answers at the end) gave the pulse's ruin shape ONE exported writer, `ruinInstitution(inst, { reason, fate })`, with `fate` a REQUIRED argument — and its pre-proof measured that **`worldPulseFate` has NO closed vocabulary**: eight writers spell fates as free string literals, and EM-B1a's decree will add a ninth (`ruined_by_decree`). The estate's FINITE-SEMANTICS law (typed buckets, never free text; memory and design both state it) makes an open vocabulary a defect in waiting: a ninth spelling lands unnoticed, and every reader that tests truthiness or a literal silently mis-reads it. The charter's row: `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/implementation/charters/EDIT-MODE-TRAIN.md` (search `EM-B1h`).

**The packet's outcome (the chair's ruling on the shape):** ONE frozen exported vocabulary — `WORLD_PULSE_FATES` — in the domain layer beside the writer it serves (propose the home by MEASUREMENT: `calamityKernel.js` holds `ruinInstitution`; but the eight writers live in several kernels — find every one by executing a scan with a comment-only strip, the technique EM-B1e's A7 uses, NOT the shared `codeOnly` helper, which blanks string contents and returns a vacuous zero); `ruinInstitution` REFUSES a `fate` outside it (the throw idiom it already has); and a TOTALITY WALKER in `tests/lint/` modelled on EM-B1d's `statusUnionTotality.walker.test.js` (read its version-5 packet in your tree — `EM-B1d.md` §6: the R6′ matcher, the derived trigger on the DISCRIMINATING members, the homonym table, the roster with a checked `spelling` field, the empty inline exemption register, the `CANNOT-CATCH:` header): every site that WRITES `worldPulseFate` spells a member or reads the vocabulary; every site that READS it and enumerates is total or carries a reasoned omission. `ruined_by_decree` joins the vocabulary IN THIS PACKET (declared, unproducible until EM-B1a) so that EM-B1a's op lands against a closed set.
**Measure, do not assume:** (1) the eight writers — module, symbol, the literal each writes, and whether any writes a COMPUTED fate (a template or a variable) that a literal scan cannot see; (2) every READER of `worldPulseFate` and what it does (EM-B1e measured one real consequence: criminal leashes sever on a truthy fate) — is any reader a display surface (then the fate is reader-facing vocabulary and the voice law applies)? (3) homonyms: which fate words also belong to other declared vocabularies (the walker's trigger must be the discriminating subset, as EM-B1d's is); (4) PERSISTED SHAPE: fates are already on saved records — closing the vocabulary must not reject a value an OLD save carries: sweep fixtures, goldens and the preset witness for every fate value that exists in data, and any that no current writer produces (a retired spelling) is a finding for the chair; (5) goldens: the packet is golden-neutral by construction (it validates, it does not re-spell) — prove it, the preset witness above all (`tests/fixtures/preset-lighting-witness-golden.json`), and note that EM-B1e's test file carries a SECOND pin on that witness's digest; (6) bundles: every kernel you touch is in `advanceInterval.worker` (no ceiling yet; TOOL-3 will mint one with 4 KB headroom ratcheted per train) — price the bytes; is any touched file in the zero-slack generation worker, first paint or an edge-shared closure (seven generated paths if so: step 11)? (7) the mutation-coverage row (`tests/lint` is an enforcer directory; `"uncovered"` is unavailable — `"rationale"` or `"mutation"`).
Register predictions as DELTAS; `__BASE__` for the chair; the §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS` (print the equality); requiredSymbols with the post-edit simulation (a symbol the packet re-spells is NOT a required symbol — the chair cut exactly such a row from EM-B1d version 5 today).
