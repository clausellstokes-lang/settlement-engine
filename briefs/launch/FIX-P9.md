# FIX-P9 — the two tier-word surfaces FIX-P5 registered but could not cure: the Compendium's `titleCase(id)` label baked into a byte-pinned generated artifact, and the gallery's facet-wide `capitalize`; plus the taste-approved sample's `†`; each cured through the authority it names, and the lane's executed census rows deleted the day each lands (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 86, 89 — FIX-P5's hand-off; after FIX-P5 composes)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-P5's receipt WHOLE (`$KIT/findings/FIX-P5/FIX-P5.receipt.md` — commit C `a5575107a` F14 one tier word; the census `tests/copy/tierWord.census.test.js` and its two REGISTERED rows with the authority each names; noticed items 1–3; the `tierFacts.js:35-38` declaration: the spellings were UNIFIED to the wizard's "Thorpe", only the machine token stays `thorp`) and, in your worktree, WHOLE: `tests/copy/tierWord.census.test.js` (the registered rows are EXECUTED — the compendium row requires the shipped artifact to still say "Thorp", so it reds the day the artifact is cured and must be deleted in the same commit), `src/config/tierFacts.js` (the declaration), the Compendium's generated artifact and its generator (find them: `git grep -n 'titleCase' -- src/domain/content src/components/compendium scripts`; the artifact is byte-pinned — read the pin and the regeneration verb; the estate's rule for a generated artifact is REGENERATE through its own generator and commit the regenerated file with the change, never a hand edit), the gallery's `capitalize` (`git grep -n "textTransform: 'capitalize'" -- src/components` — three files per FIX-P5; it governs EVERY facet the gallery draws, not only the tier), and `src/components/organic/samples/PricingSample.jsx` (the `†`).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-P5's five composed commits (STOP if not): `git -C $SP/slot-2 worktree add -b fix-tier-word-2-2026-09-20 $SP/lane-fix-p9 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-p9/node_modules` (no build). Scratch `$SP/lane-fix-p9-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The three commits, in order
1. **The Compendium's label, through its generator.** The label is `titleCase(id)` baked into a byte-pinned generated artifact. Change the GENERATOR so the tier label comes from the shipped tier-word table (`tierFacts.js`'s label, never `titleCase` of the token), regenerate the artifact through its own verb, and commit both with the pin re-minted in the estate's idiom for a regenerated artifact (read how the pin is re-minted — if it needs an owner door, STOP and say so). Red-first: the census's compendium row goes RED the moment the artifact says "Thorpe" — quote it — and that row is DELETED in this commit (its cause is cured), with the census's header noting the deletion. Every other facet label the generator emits must be byte-identical before and after (diff the regenerated artifact: only the tier labels move; quote the count).
2. **The gallery's `capitalize`, tier-aware.** `textTransform: 'capitalize'` over raw facet tokens prints "Thorp". The cure that keeps every other facet as it is: the tier facet renders the tier-word table's label and the CSS capitalize stays for the facets it was written for — measure which facets the three files draw and prove each non-tier facet's rendered text is unchanged (a DOM snapshot per facet before/after). The census's gallery row goes red then is deleted in this commit. No new mechanism: if a shared tier-label helper already exists from FIX-P5's commit C, import it; never a second spelling.
3. **The sample's `†`.** `PricingSample.jsx` is the taste-approved painted sample; measure whether the shipped pricing surface still uses `†` anywhere (`git grep -n '†' -- src/components/pricing src/data`); if the product dropped it, the sample follows (one character, with the taste-approval's ODQ row cited in the commit); if the product still uses it, record the row as true and CLOSE it in your receipt with the citation.

## Laws
Goldens byte-identical except the regenerated Compendium artifact if it is itself a golden (measure: if `generatorGoldenMaster` or the prose manifest moves, STOP — that is the owner's). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/copy` whole and `tests/components` whole (the census and the gallery/compendium suites live there), the compendium's contract tests, `tests/lint` whole if you touch a walker; the lighting census FROZEN at the tuple the chair names at dispatch (no new test file; record your title delta; the walker asserts its five figures in order and stops at the first miss; never refreeze); the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-p9-scratch/FIX-P9.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-p9-scratch/FIX-P9.receipt.md`: the generator change and the artifact diff count; the pin's re-mint; the census rows' red-then-deleted proof; the per-facet DOM snapshots; the `†` finding; every count line; the goldens' hashes; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
