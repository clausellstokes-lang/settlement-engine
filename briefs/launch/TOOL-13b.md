# TOOL-13b — the observed-shape scanner reports its DENOMINATOR: the rate says what it measures, the vacuous M12 filter is settled by measurement, the header names only the roots it binds, and an erasure guard reds any widening that drops a banked row (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 82)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-13's report WHOLE (`$KIT/findings/TOOL-13/TOOL-13.report.md` — especially §1 what "read" and "resolved" mean, §5 THE UNRESOLVED READS THAT MATTER and its denominator anatomy, §6.4 THE NON-MONOTONICITY HAZARD, §7.2 the recommendation, §8 the twelve items) and, in your worktree, WHOLE: `scripts/check-observed-shape-readers.mjs` (the `--report` path near `scan reached`, `assertHealthyScanProvenance` and its call, the M12 language-surface filter and its comment block, the frozen-inventory read/compare), `scripts/lib/legacy-reader-shape-scan.mjs` (its header; `makeResolver` and the `roots.has(n.text)` return; `scanReaders`), the scanner's own tests (find them: `git grep -l 'check-observed-shape-readers\|legacy-reader-shape-scan' -- tests scripts`), and `tests/lint/observedShapeReaders.walker.test.js`.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b tool-13b-2026-09-20 $SP/lane-tool-13b c33446830` (the branch tip: the fifth lighting refreeze); `ln -s $SP/slot-2/node_modules $SP/lane-tool-13b/node_modules` (no build). Scratch `$SP/lane-tool-13b-scratch/`. TOOL-13's own instruments live at `$SP/lane-tool-13-scratch/` — READ-ONLY; copy what you reuse into your scratch. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The ruling you implement (addendum 82; TOOL-13 §7.2)
The 7.6 % resolution figure is overwhelmingly a DENOMINATOR ARTEFACT: 20,103 reads (15.7 %) are rooted at a host/runtime global (`Object.freeze` alone 7,473) and can never be domain reads; on the 51,095 reads of the corpus vocabulary the rate is 16.76 %. The cure is NOT more resolution (every vocabulary widening measured ERASES frozen rows through the `objects.length === 1` ambiguity guard, which `assertClassADebtPreserved` does not cover — the ratchet would show STALE ROW: the right red, the wrong diagnosis). The cure is that the rate REPORTS ITS DENOMINATOR, mints ZERO findings, erases ZERO rows and needs NO migration. The byte-frozen artifact is untouched: `assertHealthyScanProvenance` pins `reads === resolved + unresolved`, so everything you add is an ADDITIONAL field computed outside the frozen blob.

## The four commits, in order (each by explicit pathspec; each proved before the next)
1. **The denominator field.** `--report` (and the plain run's summary line if it prints the rate) states, from a NEW computed object that is never written into the frozen blob: total reads; host/runtime-global-rooted reads (with the top five roots and their counts — reproduce TOOL-13's `Object.freeze` 7,473 to the read, or say why the figure moved); UI breakpoint-token reads (`.sm/.xs/.md/.xxs` — TOOL-13's 4,716); uninitialised-`let` reads (265); the CORPUS-VOCABULARY reads and the resolved rate over THEM (TOOL-13's 8,562 / 51,095 = 16.76 %); and the "reads that matter" figure printed AS A CEILING with its two false-mattering examples named in the comment (`Float32Array.from`, `CONTROL_CHARACTERS.test`). Spell the line the way §7.2 proposes; keep every existing printed figure byte-identical (a test may pin the old line — find it before you change the format; if a pin exists, EXTEND the line after the pinned prefix or add a second line, never move a pinned figure). Prove: `node scripts/check-observed-shape-readers.mjs --report` before and after — the finding count and the `scan reached N/M` figures identical; the frozen inventory's hash identical (`shasum -a 256` before/after); the plain run prints `exactly matching the frozen inventory`.
2. **M12 settled by measurement.** The language-surface filter prints `cleared 0 read(s) across 0 identit(ies)`. Measure WHY (which keys, which reads it would have matched, whether M11/BUILTIN_MEMBERS already cleared them) and then either RETIRE it (comment block kept as a one-paragraph obituary with the measurement) or RE-SCOPE it to the reads it was written for — whichever the measurement says; if either choice changes ANY finding or the frozen inventory's hash, STOP and report (the chair rules). Quote the before/after counts.
3. **The header correction.** `scripts/lib/legacy-reader-shape-scan.mjs` says the root prior binds `settlement`, `worldState`, `save`, `campaign`; measured, only `save` and `settlement` bind under `minRows = 40` (`campaign` 1 row, `worldState` 25, `pulseResult` 12, `wizardNews` 13). Correct the comment to the roots the code binds TODAY and state the mechanism (a walk root is thin by construction; the record threshold filters it) with a pointer to TOOL-13a (the governed widening, chartered, not this lane). No code change in this commit.
4. **The erasure guard.** A test (in the scanner's own test home; if it must be a NEW file under `tests/lint/`, it owes its `scripts/mutation-coverage-manifest.json` row added surgically beside its siblings, and its lighting delta recorded) that runs the shipped resolver configuration IN MEMORY against the frozen inventory and asserts EVERY banked row survives (0 erased under the shipped config — true today). Its COUNTERFORCE, measured and quoted in the receipt (the §P6 written-contract rule: an arm that already holds at the base is proved by its counterforce, never red-first): run the same arm under TOOL-13's root-widening configuration in a throwaway (the six roots admitted) and observe it RED with the 13 erased rows named; then the shipped config green. The arm must be cheap enough to live in the directory's whole run (measure its wall-clock; over 20 s → STOP and say so).

## Laws
Not one byte under `src/`. Goldens untouched (hash the two goldens before and after anyway). The frozen inventory byte-identical across all four commits (its hash quoted per commit). Every vitest line through the mutex SHARED tier with the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; ONE directory per invocation; a line with no printed count DID NOT RUN; the directory-whole rule for any CREATE (its `tests/<dir>` whole once — the ONLY permitted red in `tests/lint` is the lighting census; the register is FROZEN at `2656·383·2273·25074·6684`, run the walker once, record your delta, never refreeze); eslint on every touched file; `node scripts/check-observed-shape-readers.mjs` (the plain run) green after every commit; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; report-only arms write with `process.stdout.write`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-13b-scratch/TOOL-13b.lane-resume.md` (the gated batches in order), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-13b-scratch/TOOL-13b.receipt.md`: per commit the count lines and the inventory hash; the denominator line as printed; M12's measurement and its fate; the header's before/after; the erasure arm's counterforce quoted (the 13 rows); the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
