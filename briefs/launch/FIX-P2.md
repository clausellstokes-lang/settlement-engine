# FIX-P2 — the realm layer keyed on `settlement.id`: MEASURE FIRST under the persona, then STOP for the chair's ruling (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.63 row FIX-P2)

You are an Opus RECON + MEASUREMENT lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, recons and verifies. This lane EDITS NO `src/` OR `tests/` FILE: it measures and reports; the chair rules the cure's shape (a persisted key is the owner's class if it turns out to be one). Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first for the laws that bind every lane (forbidden paths, no polling, stamps read from `date`), then REVIEW-P's report `$KIT/findings/REVIEW-P/REVIEW-P.report.md` — its F1 receipt and its F2 row (the finding you are measuring), and `$KIT/briefs/launch/RECON-ID.md` (the id recon F2 cites). REVIEW-P's captures are in `$SP/lane-review-p-scratch/` (READ-ONLY).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

## The finding (REVIEW-P F2, CONFIRMED in part)
Anonymous forks collide on `settlement.id` (F1's receipt). The library and gallery do NOT collide — `src/lib/saves.js:467-487` inserts the row with no `id` (the DB mints one; `settlement.id` travels only inside `data`) and `src/lib/gallery.js:62` publishes by that save-row id. The REACHABLE hazard is WITHIN ONE REALM: `src/domain/regionalGraph.js:302` (`centerId = settlement.id`), `src/domain/resourceSites.js:142`, `src/domain/townMap/mapDress.js:101,167` key on it, so two saves grown from one seed and placed in one campaign share a node key. `src/domain/realm/realmItemReadModel.js:245` already reads `save.id ?? settlement.id`. The review's second arm — two same-seed saves in one realm — needed an account and was not walked. The cure shapes REVIEW-P named: give the realm/graph layer the SAVE id as its node key, or make `settlement.id` fold the resolved config the way `contentId` already does.

## Your worktree and the preview
A READ TIP, detached, never edited: `git -C $SP/slot-2 worktree add --detach $SP/read-tip-5a3380e8d 5a3380e8d` (if it already exists, use it), then `ln -s $SP/slot-2/node_modules $SP/read-tip-5a3380e8d/node_modules`. Scratch: `$SP/lane-fix-p2-scratch/` (create it). The persona (the dev-mode signed-in account) lives in `.env.development.local` — copy it READ-ONLY out of `$SP/consist/.env.development.local` into your read tip (never write into `$SP/consist`; never into `.env.local`, which vitest reads and `vite build` inlines). Run your own Vite dev server in the read tip on port **5233** (`npx vite --port 5233 --strictPort`, backgrounded with nohup, its PID recorded in your scratch; kill ONLY that PID when done; never `pkill vite`). Drive it with the in-app browser tools (`mcp__Claude_Browser__*`: navigate, read_page, find, computer, read_console_messages, javascript_tool for READ-ONLY inspection). FORBIDDEN: `$SP/consist` (except the one read of the persona file), `$SP/slot-2`'s working tree, every `$SP/lane-*`, the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the kit (read-only). No gated run (no vitest, eslint, build); no `git` mutation anywhere.

## The measurement, in order
1. **The lifecycle of the key (code, whole scopes).** Read in full: how `settlement.id` is minted in generation (find the writer; is it seed-derived?), every consumer that keys on it (start from the four sites above; `grep -rn 'settlement\.id\b\|centerId\|nodeId' src/domain src/lib src/components` and disposition every hit: keys-a-structure / display-only / not-applicable), and — the crux — whether any structure keyed by it is PERSISTED (the campaign/realm save, `src/lib/saves.js`, the realm store, the observed-shape register's writers) or is re-derived on every read. Map the paths: create · read · persist · regenerate · undo · clone/fork · migrate/import. Say for each whether the key crosses it.
2. **The live reproduction under the persona.** Generate one seed twice (the same tier/culture/terrain), save BOTH to the library, place BOTH in ONE realm/campaign, then observe and capture (screenshots to your scratch; the read model's keys via read-only JS): does the realm show one node or two; do the resource sites and the map dress of the second save collide with or overwrite the first's; what does `realmItemReadModel` key the two by; what does the campaign save (if any) persist as their keys. Then fork one of them (the product's fork path) and repeat the observation.
3. **The verdict.** CONFIRMED or REFUTED, with the receipt (what you clicked, what rendered, what the keys were). If REFUTED anonymously-reachable-only, say what makes it unreachable and whether that is by design or by accident.
4. **The two cure shapes, priced — and STOP.** For each of REVIEW-P's two shapes (save-id node key; `settlement.id` folding the resolved config): the exact chokepoint(s), the blast radius over every lifecycle path from step 1 (especially: does either change a PERSISTED key — if so, name the saves it would orphan and the compatibility read that would be needed), the goldens/pins that would move (grep the fixtures for `settlement.id`-derived values), and the tests that pin today's behaviour. Give the chair a recommendation with the reason. Do NOT implement. The chair rules; if the ruled shape touches a persisted key it is an OWNER decision point (ODQ) before any build.

## Your report
`$SP/lane-fix-p2-scratch/FIX-P2.measure.md`: the lifecycle map; the reproduction receipt (CONFIRMED / REFUTED, each claim CONFIRMED by execution or PLAUSIBLE); the two shapes priced with blast radius; the recommendation; the stamp read from `date`; the server's PID and its kill confirmed; and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work). End your turn with the report's path.
