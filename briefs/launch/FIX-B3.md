# FIX-B3 — `ENGINE_SHARED_DOMAIN` is seeded from the REACHABLE graph, not from every file under `src/generators/`: the dark trio (and every other unreachable generator module) stops charging first paint by construction; measured with a real build; the first-paint budget's honest headroom re-measured (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 87 — FIX-B2's family, after FIX-B2 lands)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-D9's receipt (`$KIT/findings/FIX-D9/` — the finding: `ENGINE_SHARED_DOMAIN` is seeded by walking every file under `src/generators/`; `computeEagerModuleGraph()` seeds from ESD; so "is it eager?" answers yes by construction; with the dark trio skipped ESD 51 → 49 and EAGER 268 → 266; `titularSuccession.js` alone holds `factionLifecycle.js` and `cohesionWeave.js` in the eager set), TOOL-23's report if it has landed (`$KIT/findings/TOOL-23/` — the full unreachable-generator set and the ESD/eager figures with all of them skipped), FIX-B2's receipt (`$KIT/findings/FIX-B2/` — the chunk rules it landed; its instruments `needs.mjs`, the graph tool; the importer set N), and, in your worktree, WHOLE: `vite.config.js` (the ESD seed walk, `ENGINE_SHARED_DOMAIN_EXCISIONS`, `computeEagerModuleGraph()`, `EAGER_FIRST_PAINT_MODULES`, the `@consumed-by` annotation), `tests/build/vendorPdfLazy.test.js` (the first-paint closure arithmetic and its 8-file pin; `entryStaticClosure()`), `tests/build/engineChunkLazy.test.js`, `tests/build/domainGeneratorsBoundary.test.js` (the boundary the ESD encodes), and the FP-G17 orphan guard (find it: `git grep -n 'FP-G17' -- tests vite.config.js`).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-B2's composed commits (STOP if not): `git -C $SP/slot-2 worktree add -b fix-esd-reachable-2026-09-20 $SP/lane-fix-b3 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-b3/node_modules`. Scratch `$SP/lane-fix-b3-scratch/`. YOU BUILD in your own worktree, every build through the mutex's default (exclusive) tier with the two exports INLINE (`GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npm run build`), once per commit. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The two commits, in order
1. **MEASURE, then the seed.** Reproduce FIX-D9's figures at your tip by executing the shipped derivations (ESD and EAGER sizes; the eager members that only unreachable modules hold). Then change the ESD seed to walk the REACHABLE graph from the build's real entries (the same seven FIX-D9 enumerated — derive them from the config and `index.html`, never a hand list; static and dynamic imports; workers' own graphs included as entries) intersected with `src/generators/**` — so an unreachable generator module cannot enter ESD and therefore cannot enter EAGER. Prove by execution: ESD before/after (51 → the measured figure), EAGER before/after (268 → the measured figure), the members that left, and that NO reachable module left (the difference is exactly the unreachable set). Update the `@consumed-by` annotation and the seed's docblock to the measured truth. `EAGER_FIRST_PAINT_MODULES`'s import sites (`tests/build/vendorPdfLazy.test.js`, the pricing lanes' probes) keep working — they import the set, never a list.
2. **THE BUILD PROOF and the honest headroom.** A real build; the first-paint closure by the test's own arithmetic before/after (raw 1,039,267 / 1,048,000 with margin 8,733 at ec0a30da2; gzip; brotli; the 8-file pin) — the moved modules ABSENT from first paint; the engine chunk and the worker unchanged or explained; `tests/build` WHOLE against your dist with dist reads forced (a skipped byte arm is not a pass); the FP-G17 orphan guard green (it proves an excision is placed when first-paint reaches it — say what it proves about modules first-paint no longer reaches); pins updated SHRINK-ONLY, never a raised ceiling. Then the headroom table the chair owes the owner under §934.68: first paint, the lazy engine, the worker — before FIX-B2, after FIX-B2, after this. ⭐ §934.68 IS SIGNED (the owner's standing grant of 2026-09-20 12:2x; addendum 98): the lazy-engine ceiling's two chair-ruled raises are RATIFIED — record it in `tests/build/vendorPdfLazy.test.js`'s header in one sentence beside the number's history (the raises named by their commits, "ratified by the owner 2026-09-20 (ODQ §934.47 addendum 98)"), a comment only; the number does not move (FIX-B2's placements left the chunk at 643,221 B, 35,079 B under it).

## Laws
Not one byte under `src/` (the change is `vite.config.js`'s seed + `tests/build` pins; if a src edit seems needed, STOP). Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; the lighting census FROZEN at the tuple the chair names at dispatch (titles in existing `tests/build` homes — no new file; record your delta; the walker asserts its five figures in order and stops at the first miss; never refreeze); quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: the builds and the `tests/build` runs are GATED — do everything ungated first, write `$SP/lane-fix-b3-scratch/FIX-B3.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-b3-scratch/FIX-B3.receipt.md`: ESD/EAGER before/after with the members that left and the proof no reachable module left; the first-paint arithmetic before/after; the three-budget headroom table; every count line; the pins moved; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
