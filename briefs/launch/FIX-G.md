# LAUNCH — FIX lane G: FIX-G2 (the Overview's food card says what each of its two facts is) and FIX-G1 (the viability summary counts what the list shows — built TO THE GOLDEN, then STOP)

Read `briefs/LANE-PARALLEL.md` (chair kit) WHOLE and obey it. Worktree, branch and base are named in the dispatch message. Scratch: `$SP/lane-fix-g-scratch/`. Lane id: `FIX-G`. **The measured ground — read both whole before anything:** kit `findings/RECON-G/RECON-G.report.md` and `findings/RECON-G/FIX-G1.measurement.md` (an Opus recon over all 525 golden rows, 2026-09-19). The chair's rulings: ledger ODQ §934.57 and the charter section "RECON-G's findings" — `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md | grep -n "934.57"`.

## FIX-G2 — DISPLAY ONLY: zero goldens, zero saved-shape, zero `src/domain` / `src/generators` bytes
**What a DM meets today:** the Overview's food card prints a WORD ("Secure", "Pressured", "Import-Dependent", "Deficit", "Active Famine" — cut from the food DEFICIT at generation and never re-graded) beside an unnamed BAR (`resilienceScore`: granary months, source diversity, import independence, adequacy — re-graded at every tick), under a caption that promises "Food Security is re-judged as the campaign advances". A bar 73 % full sits beside "Import-Dependent"; after one quiet month 43 of 63 towns show a word its own thresholds contradict. They are two facts shown as one.
**The chair's ruling on the shape (vetoable; the owner has been asked whether the WORD should become a lived reading — build for what the code DOES today, and word it so that answer can change one sentence later):** (1) the bar gets its OWN NAME and its own short gloss saying what it measures, in the dossier's voice — the tome's grammar (owner law 2026-09-19: rubrics, marginalia, a scribe's plain nouns; never a dashboard's jargon, no em dash, no exclamation point in a `src/` string literal — `tests/copy/voiceMechanics.test.js` enforces the last two). Propose the name and gloss from the four things the score is made of (read its producer by symbol; the recon names it); give the chair TWO candidate wordings in your evidence file with your pick, then build your pick. (2) the caption stops promising what the code does not do: it says the STORES AND SUPPLY reading moves as the campaign advances and that the WORD is the land's condition as surveyed. (3) every surface that shows the pair gets the same cure TOGETHER — the recon lists them (the Overview tab, the PDF, the AI/narrative context, exports): find each by symbol; a surface that shows only one of the two facts is left alone and listed. (4) prose-numerics law: no raw engine numeral newly reaches reader-facing prose; the bar's percentage is already rendered — do not add figures.
**Proof:** the tab's existing suite extended (the name and the gloss render; the old caption is gone — pair the negative with its anchor); if the PDF or a snapshot pins the old caption, that pin is a declared edit with its reason. ⛔ A change that moves `tests/fixtures/generator-golden-master.json` or `tests/fixtures/dossier-prose-manifest-golden.json` is a STOP — this cure is display copy in `src/components/**` (and the PDF's renderer if it repeats the caption), nothing else. Price the first-paint / lazy-chunk bytes if `OverviewTab.jsx` is in a budgeted chunk (say which, by the import graph) and STOP if a ceiling test would red.

## FIX-G1 — built TO THE GOLDEN, then STOP for the owner's signed door
**The defect (1 of 525 rows; `town|germanic|mountain|mountain_pass|civilized`):** `src/generators/economy/viability.js` counts dependencies over `[...issues, ...warnings]` for the summary sentence while it publishes `dependencies` from `warnings` alone; two `foodBalance.js` arms push a DEPENDENCY into `issues`. THE LIST IS RIGHT (`metrics.dependencyCount` agrees with it on 525/525). **The cure is ONE SITE:** the sentence counts what the list shows (derive the count from the same filtered collection the list is published from — read the function whole and choose the form that makes a future divergence impossible, e.g. one computed `dependencies` array used by both). **Red-first:** a test on that config asserting the sentence's count equals `economicViability.dependencies.length` — red before, green after; plus the general arm over a bounded stride (every row: stated count == list length).
⛔ **THE GOLDEN:** the cure moves exactly ONE row of `tests/fixtures/generator-golden-master.json` and ZERO rows of the prose manifest (measured). `UPDATE_GOLDEN` and `GOLDEN_SHIFT_SIGNED` are FORBIDDEN to you. Build the cure and its tests, run your gated batch with the golden-master suite EXPECTED RED on exactly that one row (quote it: one row, that config, the sentence's 6 → 5 and nothing else — ANY other row or key moving is a STOP), commit the cure on YOUR branch with the red named in the body, and report. The re-record rides EM-P1's signed golden door at train EM-T4, executed by the chair; this generator file is in the ZERO-SLACK generation worker (`1,401,208` bytes EXACT) — measure the minified byte delta of your edit and say whether it is negative, zero or positive (a positive delta is a re-mint the chair must attribute: prefer the form that does not grow).
