# FIX-L3 — `tests/edgeFunctions/contracts.test.js` stays PARKED after FIX-L1 because it carries three park-reason families beyond `OPENER_UNRESOLVED`; each family measured against the lighting walker's own `parkReasonsFor`, each cured in the walker's crediting grammar, the file CREDITED (measure-first; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 85 — FIX-L1's hand-off; low priority, after FIX-L2)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-L1's receipt (`$KIT/findings/FIX-L1/FIX-L1.receipt.md` — the `OPENER_UNRESOLVED` class closed at 0 by renaming 41 shadowing bindings; `contracts.test.js` named as needing three OTHER reason families; CURE-A3's lesson that a mask can hide imports) and, in your worktree, WHOLE: `tests/lint/sovereigntyLightingContract.walker.test.js` (the crediting grammar: `RUNNING_SUITE_MODIFIERS`, `staticTableRows`, `TEST_TABLE_UNPROVEN`, the template-title rule, the conditional-registration rule, `parkReasonsFor` — the ONE authority on what parks a file), `tests/edgeFunctions/contracts.test.js` WHOLE (every `describe`/`it`/`test` registration and how it is built), and the `eachOnly` / `literalTableStillParked` ceiling arms (both at their ceilings at the first composition's tip — 111/111 and 50/50 — read their arms whole: un-parking a file that uses an `each` shape LOWERS `eachOnly`, which is the direction the ceiling wants; the reformat the arm convicts is the one that keeps the `each` while pretending it is a table).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b fix-l3-contracts-park-2026-09-20 $SP/lane-fix-l3 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-l3/node_modules` (no build). Scratch `$SP/lane-fix-l3-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The commits (one per reason family; measure first)
0. **MEASURE.** Run the walker's `parkReasonsFor` over `contracts.test.js` at your tip (the pre-proof lanes' idiom — import the walker's export or lift its call) and print every reason with its line; group them into families; for each family say what the crediting grammar wants instead (a literal table for a `test.each` over an Identifier; a plain string for a template title; an unconditional registration for a conditional one; a body loop for a `describe.each` over runtime values — the walker's own comments name each shape). Quote the file's title and suite counts (they become credited titles when the file un-parks — your predicted delta).
1–3. **One family per commit.** Rewrite the registrations in the family the walker credits, assertions UNCHANGED (every arm still asserts the same contract — diff the assertions; a changed expectation is a STOP); after each commit `parkReasonsFor` prints one family fewer; after the last it prints `[]`. Red-first per commit: the file's own suite green before and after (`tests/edgeFunctions` is its directory — run the file, then the directory whole once at the end); the walker run once at the end: your delta `parked −1 · credited +1 · titles +N · suiteTitles +M` recorded against the register the chair names, never refrozen; `eachOnly`'s count before/after quoted (it should fall by the number of `each` shapes you removed).

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/lint` WHOLE once at the end (the only permitted reds: the lighting census against a register that predates your base; the provenance title if swept); the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-l3-scratch/FIX-L3.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-l3-scratch/FIX-L3.receipt.md`: the reasons by family with lines; per commit the registrations rewritten and the assertion diff (empty); `parkReasonsFor` after each; every count line; the lighting delta and `eachOnly`'s before/after; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
