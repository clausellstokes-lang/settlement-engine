# FIX-D10 — the reachability roster RETIRED BY CLASS after TOOL-23's census: the walker first learns the three edge classes the seven-entry scan cannot see (edge-bundle inputs · the committed-artifact producer closure · JSDoc type edges), then the eight ORPHANS, then the disposition vocabulary and headers for the 205 that KEEP, then the 36 TEST-ONLY-FIXTURE modules classified against the design docs — the seven register-moving or owner-gated rows named LAST and left with their reasons (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 87, 107; dispatched AFTER RUN 23 at a clean composed tip)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-23's report WHOLE (`$KIT/findings/TOOL-23/TOOL-23.report.md` + `table.md` — §2's premise correction, §3's classes and their discriminators, §5's price sheet measured from `git show --stat 3a5618ed8`, §5's three cheapest and the seven named LAST, §6's twelve items), FIX-D9's receipt and compose plan (`$KIT/findings/FIX-D9/` — the retirement door it executed: the module, its test, any `vite.config.js` excision row, one counter in `docs/content/wiring-census.json` through the `--dry`-then-write door, in the forced order; the OSR baseline NOT touched), and `docs/DEAD_CODE_DISPOSITION.md` WHOLE (its 2026-07-14 blanket ruling and its OWNER-VERIFY holds; its proposed ratchet at lines 115–122).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-D9's four composed commits and the eighth lighting refreeze (STOP if `tests/lint/deadCodeDisposition.walker.test.js` is absent): `git -C $SP/slot-2 worktree add -b fix-d10-reachability-roster-2026-09-20 $SP/lane-fix-d10 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-d10/node_modules` (no build). Scratch `$SP/lane-fix-d10-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists — never touch it.

## The chair's rulings (addendum 107) — they bind this lane
- **R1 — THE INSTRUMENT BEFORE THE KNIFE.** No module is retired until the walker can prove it unreachable from EVERY shipped runtime and EVERY reader: the seven Vite entries, the five `supabase/functions/_shared/*.meta.json` bundle inputs (the Deno runtime), the committed-artifact producer closure (the eleven `scripts/` producers of `public/landing-maps/**` and the generated prose/compendium/CSS outputs), and JSDoc `import()` type edges (`tsc` resolves what Rollup erases). TOOL-23 measured 65 modules that a seven-entry scan would have falsely convicted.
- **R2 — RETIRE ONLY WHAT NO DESIGN DOC CLAIMS.** A TEST-ONLY-FIXTURE module named by a live `docs/DESIGN_*.md` or a charter as a planned surface is KEPT with the header `BUILT, UNMOUNTED — awaiting <doc §>`; one named by nothing is retired. The un-mounted feature set is the owner's future, not dead mass, until a document says otherwise.
- **R3 — THE SEVEN NAMED LAST STAY.** `usePricingMoment.js` (PAID SURFACE — an OWNER-VERIFY hold; owner-gated), `computeNetworkEcho.js` (a named denominator member of a single-home scan; a two-plant proof), `InteriorView.jsx` (a registered artwork plate), `edgeAnnotations.js` (a walker allowlist row), `tabPalette.js` (cited by two reachable modules), `regionWakeReplay.js` (the `.domain-any-baseline.json` row), `heraldIndex.js` (the second plant row) — each gets a header line stating the register that holds it and is NOT retired by this lane.
- **R4 — THE TE-STRIP TAIL IS AN OWNER DECISION POINT (§934.75), NOT THIS LANE'S KNIFE.** `src/lib/townScene/**` (5 modules) are the clients of two workers that are still build entries — a half-finished strip or a live surface with a dead door; the chair recommends finishing the strip and awaits the word. You measure and header them (`STRIPPED CLIENT — TE-STRIP tail, §934.75`), nothing more.

## The five commits, in order (measure first; every retirement through FIX-D9's door)
1. **THE WALKER LEARNS THE EDGES.** `tests/lint/deadCodeDisposition.walker.test.js` gains a reachability arm built from TOOL-23's instruments (its entries enumerated from `vite.config.js` — never a hand list; the four exemption classes of R1 as EXECUTED edges, each with a counterforce: drop a meta input, a producer, a typedef edge in a throwaway → the walker reports the module reachable/unreachable correctly). It asserts the ROSTER (the 249 minus the fates this lane lands) shrink-only from a committed baseline, never grows, and prints every class count. The disposition doc's proposed ratchet (lines 115–122) is re-cut to this design in the same commit. Red-first against the seven-entry design: 65 false convictions, quoted.
2. **THE ORPHANS.** The clean ORPHANS (TOOL-23: 8, four of them gated or registered → those four go to commit 3's headers): `src/store/deitySnapshot.js` (a proven SUPERSEDED shim), `src/components/organic/index.js` (a dead barrel), `src/hooks/usePosture.js` (zero references; its header's false importer claim dies with it), and the fourth clean one from the table — each through the door: the module, its test if any (none, executed), any excision row, the census counter if under `src/generators`/`src/domain` (26 of the 44 candidates cost no census regeneration — measure per module), the manifest/plant rows. Goldens byte-identical (TOOL-23's path and prose-literal scans both zero — re-execute).
3. **THE VOCABULARY AND THE HEADERS.** `docs/DEAD_CODE_DISPOSITION.md` gains the classes TOOL-23 measured (LIVE-EDGE-RUNTIME · LIVE-AUTHORING · TYPE-PINNED · ORACLE-INSTRUMENT · TEST-ONLY-FIXTURE · TEST FACADE · STRIPPED CLIENT) with their discriminators; one directory-level `@non-product` note for `src/domain/certification/**` (29 modules, the estate's evidence apparatus); `src/domain/worldPulse/index.js` headed TEST FACADE; the 57 LIVE-AUTHORING producers headed (or one directory note per producer directory — measure which is fewer lines); the three TYPE-PINNED and five LIVE-EDGE-RUNTIME headed; the seven of R3 headed with their registers; the five of R4 headed; `src/data/dossierCausalProse.generated.js` (210,260 B) has its PRODUCER named by execution (which generator writes it; whether any browser path reads it — quote the reader or its absence) and is classed accordingly. The walker of commit 1 reads the headers and reports a module whose header class disagrees with its measured class.
4. **THE TEST-ONLY-FIXTURE 36, CLASSIFIED UNDER R2.** For each: the design doc or charter that names it (quoted) → KEPT + header; none → RETIRED through the door, its test file (25 test files total across the class), its plant rows (2), its `tests/lint/.tuning-inventory.json` rows (7 — measure first whether that register is shrink-only or frozen; a frozen register is a STOP with the row quoted). ESD/eager re-run after (TOOL-23: 51 → 49, 269 → 267 with all unreachable generators skipped — quote your figures; `cohesionWeave.js` is an edge-bundle input and stays).
5. **THE MEASUREMENT.** The roster after: class counts, bytes, effective lines; the walker's baseline committed at the shrunken figure; `computeNetworkEcho.js`'s mutation-manifest rationale re-measured (its standing plant was ruled INELIGIBLE on 2026-07-27 pending the wave's fold — the wave folded: if the two targets are now committed and clean, LAND the standing sweep plant in this commit with its rationale row re-cut, red-first through the mutation harness; if not, quote the blocker — no phantom lane holds it).

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (a run that ends without a count line DID NOT RUN); eslint runs BARE; `tests/lint` WHOLE, `tests/build` WHOLE, `tests/domain` WHOLE (retirements under `src/domain`), `tests/ui` and `tests/components` WHOLE when a test file there is deleted, the wiring census `--check` after every census-moving commit; typecheck (`npm run typecheck`, BARE — the TYPE-PINNED class is proved by it); the lighting census FROZEN at the tuple the chair names at dispatch (record your delta; a deleted test file LOWERS files/titles — record it, never refreeze); the observed-shape baseline NEVER touched (FIX-D9's precedent proves a retirement does not move it); the edge-shared metas' `inputs` measured for every `src/` path you touch; the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); an unquoted zsh list is ONE pathspec; `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; control characters spelled in words in any tool-written text; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-d10-scratch/FIX-D10.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-d10-scratch/FIX-D10.receipt.md`: the 65 false convictions of the seven-entry design quoted at red-first; every retirement with its door steps and figures; every KEPT module with the document that names it; the roster before/after by class; ESD/eager figures; the tuning-inventory ruling; every count line; the commit shas; ⛔ everything noticed and not touched, each specific enough to slot.
