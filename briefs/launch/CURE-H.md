# LAUNCH — CURE-H: run 18's ONE red — A8 of EM-B1k's (renamed) suite derives the settlement-clock chain from its PRODUCER instead of a hardcoded literal (the contract-test anti-vacuity walker's Rule 2; written by the chair, session a9df403c, 2026-09-20)

**Seat:** an Opus CURE lane in the SLOT (`$SP/slot-2`, ON the branch, unsealed between builds — the chair says when) under PAUSE-AND-RESUME. Scratch `$SP/lane-cure-h-scratch/`. `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`.

**The red (run 18, 03:51 EDT; 33,587 tests, ONE failed):** `tests/lint/contractTestAntiVacuity.walker.test.js` › "Rule 2 — no exhaustive claim is backed by a local literal in a non-deriving file": `literal-union vacuity: tests/store/participationWriteBase.contract.test.js::A8 — the settlement_clock chain returns the roster BY REFERENCE at every step, over corpus towns — "every…" claim over a hardcoded literal; derive it from its producer`. CAUSE: CURE-G (`c71782e7f`) renamed EM-B1k's suite with the `contract` token so the mutation register would enumerate it — and the token has a SECOND enforcer, this walker, whose Rule 2 refuses an exhaustive claim ("every step") backed by a literal list. A8 hardcodes the four clock-stage functions (`advanceTime` → `advanceFoodStockpile` → `applyBlockadeTransportImpairment` → `advanceTreasury`); the producer is `src/domain/worldPulse/pulseKernel.js`'s settlement-clock chain.

**RULED (ODQ §934.47 addendum 49):** (1) READ the walker whole first — its Rule 2, what it accepts as "deriving" (a `// derives:` marker? an import from the producer? a source read?) and how a file declares itself deriving; obey ITS grammar, never invent one. (2) A8's step population is DERIVED from the producer: either import a declared chain from `pulseKernel.js` (⛔ there is none, and that file sits at its 1581-line ceiling with zero headroom — adding an export is a STOP) or READ `pulseKernel.js`'s source and extract the chain by the found-and-ordered idiom CURE-E established (locate the clock stage by a declared anchor asserted FOUND, parse the ordered call sequence inside its span, assert the count and order match what the test then drives — a future fifth stage reds the pin instead of silently escaping it); the test's "every step" claim then rests on the derived list. (3) NO `src/` file moves; NO assertion of A8 weakens; the roster-by-reference claim stays exactly as strong. (4) Red-first: the walker RED on the file (quote it), then GREEN, plus the suite itself (5 passed) and `tests/lint/contractTestAntiVacuity.walker.test.js` green; eslint on the file. (5) The census: no title moves (A8 is edited, not added) — record the tuple unchanged; never refreeze. ONE pathspec commit `CURE-H: …` on the branch with the count lines. FORBIDDEN: `$SP/consist`, every read tip's working tree, every other lane's worktree; never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push. Every vitest line through the mutex's SHARED tier with the two exports inline; a line with no printed count did not run; pause at the gate boundary and let the chair resume you.

**Also measured, for TOOL-6b's brief (report, do not cure):** the `contract` token is consumed by at least TWO walkers — enumerate every consumer of the token (`git grep -n "contract" -- tests/lint scripts | grep -i "pattern\|token\|basename"`), so the `tests/store` admission dock knows every law a renamed file inherits.
