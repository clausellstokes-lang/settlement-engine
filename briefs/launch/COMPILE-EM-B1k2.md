# LAUNCH — COMPILE EM-B1k2: the irreversible consumers read the RAW roster explicitly, the two unreachable permanent roster writers are cured with the class, and the participation ratchet widens to `src/domain/density` (defense in depth behind EM-B1k; same train, immediately behind it; ODQ §934.60 addendum 2, Q1; written by the chair, session a9df403c, 2026-09-20)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, SIXTEEN steps). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-B1k2-scratch/`.

**The measured ground — read it WHOLE first:** EM-B1k version 2 as PLACED (`docs/implementation/packets/settlement-editor/EM-B1k.md` in the read tree if your tip carries `1b381485f`; otherwise the kit's `packets-waiting/EM-B1k.md` plus the chair's five cuts recorded in its Last-revalidated row), its evidence `$SP/lane-em-compile-EM-B1k-scratch/EM-B1k.evidence.md` and `EM-B1k.evidence.addendum.md` (§12 the reader census; §13 the base-swap corpus proof; §15 the clock-chain closure), and the RECON-STAGE report (`$KIT/findings/RECON-STAGE/` and `$SP/lane-em-compile-EM-B1f-scratch/RECON-STAGE.report.md`: the consumer table — what each reader of the filtered view writes, reversible or not; the population at risk). Its probes (`reader-census.mjs`, `base-swap-corpus.mjs`, `cure-simulation.mjs`) are read-only models: copy them to your scratch and re-run at your tip; every figure you quote is one you re-measured.

**What EM-B1k did and did not do (the chair's ruling, §934.60 addendum 2):** EM-B1k makes the tick's WRITE BASE the raw save roster — two single-line replacements in `pulseKernel.js` — and that alone was measured to stop BOTH defects (the erased person; the dissolved sole-member house), because `factionDensityKernel`'s `stillEmpty` confirmation (`:258-262`) was right all along and failed only because its `fresh` reading was filtered. But `tickStart` (`factionDensityKernel.js:704`) still READS the filtered participation view, so the dissolution reaction is still PRODUCED and merely REFUSED at the confirmation: the class can return through a future edit to that guard. EM-B1k2 removes the habitat.

**The packet's outcome (the chair's ruling on the shape):**
1. **The irreversible consumers read RAW, explicitly.** `factionDensityKernel`'s lifecycle read at `tickStart` (`:704` → `factionRosterOf`) and its confirmation read the RAW roster by construction (the snapshot item's `save.settlement.npcs`, `saveSettlement`'s exact meaning), not the participation view — so the irreversible decision is computed from truth, never from a projection, whatever a later packet does to the guard. Contract EXACTLY which reads become RAW: only the reads that feed an IRREVERSIBLE consequence (the dissolution decision and its confirmation). Every REVERSIBLE reading in that kernel (arrivals, emergence founders, marks) keeps the participation view — the roads design's one chokepoint is not reopened (design `DESIGN_THE_ROADS.md` §8). Measure by execution that the pulse goldens, the dormancy goldens and the preset witness do not move (no fixture holds `stasis` or `whereabouts`; generation writes neither) — the same measurement EM-B1k made, re-run here with the consumer edits in an in-memory prototype (the override technique; never edit the tree).
2. **The two currently-unreachable permanent roster writers are cured with the class BEFORE either is lit:** `settlementLifecycleFirstClass.js:611` (dispersal must carry the off-stage soul, never drop it) and `successorNpc.js:66`. Measure WHY each is unreachable today (a dark flag? a dead branch? an unregistered process?) and what "lit" would mean, so the cure is contracted against the real trigger and not a guess; ⚠ `successorNpc.js` is in the EAGER first-paint graph while every sibling is not — price its first-paint delta against the closure budget (name the test that pins it; a rise is attributed per module and re-minted only under a ruling, never quietly).
3. **The participation ratchet widens to `src/domain/density`.** `tests/domain/roadsParticipation.test.js:308` greps `src/domain/worldPulse` and `src/domain/spatial` (41 files); EM-B1k's compile measured that adding `src/domain/density` convicts EXACTLY ONE new file, `src/domain/density/factionLifecycle.js` — re-measure; that file takes ONE disposition row, and `factionDensityKernel.js`'s existing census row gains the real disposition **irreversible ⇒ RAW** in place of the inherited blanket. ⚠ COLLISION: EM-B1k's TEST row edits the same file (A3, A5 in the master-gate describe) — so this packet's `Collision group` names EM-B1k, and it is PLACED ONLY AFTER EM-B1k IS LANDED, with its pre-proof re-run at that tip. Say so in the header.
4. **`roadsKernel`'s `fullRoster` compensation (`roadsKernel.js:1094-1102`) becomes redundant** under a raw base; the CODE stays (harmless, self-consistent), the COMMENT is re-pointed at EM-B1k so the next reader does not take it for the estate's only defence. ⚠ BUDGET: that comment-only edit is a FOURTH existing production file beside `factionDensityKernel.js`, `settlementLifecycleFirstClass.js` and `successorNpc.js`, and the standard allows ≤3 — measure it and put it to the chair as a numbered question with your recommendation (a comment-only row at +0 effective lines, or dropped from this packet and slotted where `roadsKernel.js` is next edited); never a quiet renegotiation.
5. **Every hot file and every ceiling priced:** `factionDensityKernel.js` and `successorNpc.js` against `scripts/.size-baseline.json` and the standard's hot-file list (read `PACKET_STANDARD.md`'s table as it stands at your tip — `institutionLifecycle.js` joined it on 2026-09-20); the pulse worker (`advanceInterval.worker`, TOOL-3's 4 KB per-train headroom — a DELTA, no per-packet re-mint); the zero-slack generation worker (must be 0 B: prove by the import graph); first paint (item 2); edge-shared membership of every file you touch (SEVEN generated paths and a generator LAST in `checks` if any is an input).
6. **Tests:** the red-first reproduction that the class is CLOSED — a shelved sole member is never even PROPOSED for dissolution (`tickStart`'s read contains them), not merely refused; the dispersal and successor arms driven at their real triggers (or, if a trigger cannot be lit today, an arm at the function boundary with the reason stated); the widened ratchet's one new conviction as a declared row; the participation guarantees unchanged (EM-B1k's A5 shape).

Register predictions as DELTAS (`it` titles only, in CREDITED files; never name a variable `it`/`test`/`describe`); `__BASE__` for the chair; `acceptanceCases` as `{id, case}` objects, at most eight; the §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS` (print it) — ⛔ a measured NEGATIVE is a §7.1 ledger row, NEVER a table row (TOOL-1's arm reads a table-only path as drift); any generator among `checks` LAST; requiredSymbols with the post-edit simulation (name EM-B1k's post-edit lines, not the pre-edit ones, where the seam is cited).
