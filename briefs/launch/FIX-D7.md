# FIX-D7 — generator hygiene from TOOL-8's census: eight measured items, each BEHAVIOUR-PRESERVING with goldens byte-identical, or moved to FIX-D6's signed door (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 54)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with its 2026-09-20 addenda: the directory-whole rule, the zsh-pathspec warning, the trailer ruling, the `tests/security` fixture law), then TOOL-8's report `$KIT/findings/TOOL-8/TOOL-8.report.md` §4.2–§4.3 and §8 (READ-ONLY; its instruments in `$SP/lane-tool-8-scratch/` — `vocab.mjs`, `vocab.json`, the 525-row corpus pass — are yours to COPY into your scratch and re-run at your base; the exact field vocabularies are the measurement every item below rests on).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b fix-generator-hygiene-2026-09-20 $SP/lane-fix-d7 <the branch tip the chair names at dispatch>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-d7/node_modules` (no build). Scratch `$SP/lane-fix-d7-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The law of this lane
Every item is MEASURED FIRST against the exact corpus vocabularies at your base, then cured only if the cure is BEHAVIOUR-PRESERVING over the 525-row corpus — goldens byte-identical (`shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before the first edit and after the last: `7177cd6e…8f1e`, `921c51cf…db41`; the golden suites in your gated batch are the proof, quoted). An item whose honest cure would move any golden is NOT cured here: it is written up with its measurement and moved to **FIX-D6's signed door** (ODQ §934.65) — say so per item. Never `UPDATE_GOLDEN`, never `GOLDEN_SHIFT_SIGNED`. Do NOT touch the eight dark guards (D1–D8) — they are FIX-D4's and FIX-D6's, and TOOL-8a's registry asserts they are still dark; a cure here would red that walker.

## The eight items (TOOL-8 §8; each cited by its number there)
- **N2 — `src/generators/npcGenerator.js:613-615`.** `{commodity}` is a token no `NPC_FACTION_GOALS` string carries (`grep -c "{commodity}" src/data/npcData.js` → 0 — re-measure); the third link `.replace(/grain/g, …)` is unbounded (no `\b`). Cure: retire the dead substituter if the token is absent from every string it reads (prove the population), and bound the `grain` replacement with `\b` — both behaviour-preserving over the corpus (no goal string contains `grain` inside a longer word: prove it from the vocabulary). A pin per change.
- **N3 — `src/generators/services/serviceClassifier.js`.** The header claims `SERVICE_CATEGORY_MAP` "covers all 260 known services unambiguously"; measured: 280 keys, 124 of 375 corpus service names covered, 251 falling through to the keyword heuristic, which is therefore LOAD-BEARING. Cure: the header corrected to the measured figures and the load-bearing fact; a pin that derives the coverage figure from the map and the corpus (so the comment cannot rot again) — asserting a floor, not an exact count. The 303 dead arms stay (they are TOOL-8a's `precedence-shadowed` chain rows, not yours).
- **N4 — `src/generators/narrativeGenerator.js:500,516`.** `hasInst('professional guard')` and `hasInst('healer')` match 0 of 231 corpus institution names; the corpus generates `Professional city watch` (caught by the LATER `city watch` rung) — measure the corpus's healer spelling(s) first. If a rung can be retired with the output unchanged (the later rung already catches every row), retire it with a pin; if honouring a real spelling would change output for any row, it is FIX-D6's — write it up.
- **N5 — `src/generators/economy/foodBalance.js:221`.** The comment at `:216` declares a de-slug contract (`'mountain_pass'` → `'mountain pass trade'`) that fired 0 times over the two `mountain_pass` rows. Measure WHY (the token arrives de-slugged already? `effectiveRoute` is never the raw token?): either pin the contract where it actually holds, or retire the comment and the dead branch with the measurement in the commit body. Behaviour-preserving by construction if the branch never fires — prove it.
- **N7 — `src/generators/historyGenerator.js:471`.** `.replace(/\b(.{4,40}) vs \1\b/g, '$1')` ("remove accidental doubled nouns") never fires. Measure whether the doubling was cured upstream (find the producer) or the corpus merely misses the shape; if a fossil, retire it with the receipt; if the shape is reachable, keep it and register it as a `defensive` dark guard in TOOL-8a's vocabulary (a note for that lane, not an edit here).
- **N8 — `src/generators/power/economyReconciliation.js:62`.** `.replace(/\s+\(dominant\)$/, '')` — 0 of 31 corpus faction names carry `(dominant)`. Measure the layer: is the suffix added at read time (so the generator-side normaliser is in the wrong layer) or no longer produced anywhere? Retire it or move it, behaviour-preserving, with the producer named.
- **N9 — `src/generators/institutionProbability.js:82`.** `inst.includes('armory')` — American only, beside the estate's `armory|armoury` (`militaryStrength.js:88`, `entities.js:165`); neither spelling in the 231-name corpus vocabulary or in `src/data/institutionalCatalog.js`. Cure: `armou?ry`, behaviour-preserving today (no corpus row matches either); a pin with both spellings. The same file's `fortif`, `bandit`, `levy`, `harbor`, `feudal`, `planar trader` are 0 of 231 — measure each; a spelling that the catalogue could produce under a variant (`harbour`) gets the same treatment; the rest are TOOL-8a's `corpus-absent` rows, not yours.
- **N13 — `Woodcutter's camp`.** In `src/data/institutionalCatalog.js`, 0 of 231 generated names contain `Woodcutter`, so `spatialGenerator.js:151-160`'s `Woodcutters' Ground` quarter is unreachable for the whole corpus. Measure WHY (a zero weight? a terrain gate no corpus row satisfies? a selection bug?). If it is a selection DEFECT whose cure changes output, it is FIX-D6's signed door — write it up with the mechanism; if it is a legitimate rarity, record it as `corpus-absent` for TOOL-8a and do nothing.

## Laws
No golden moves (above). The lighting census is FROZEN (the tuple the chair names at dispatch) — your pins add titles: run the walker ONCE separately, record the tuple and your delta, NEVER refreeze (or use `LIGHTING_CENSUS_PRINT=1` if TOOL-9 has landed by then — it prints the whole tuple and writes nothing). The directory-whole rule for any test file you CREATE; `tests/lint` whole once; the standing instruments (negativeAssertionAnchor — every `.not.` anchored on the immediately preceding line; voiceMechanics — no em dash or `!` in a `src/` string; mutationCoverageManifest — a new test FILE wants a row; eslint on every touched file). NO BUILD in this lane (the chair measures the dist at composition; these files are on the generation worker's path — say in your receipt which files you touched so the chair's attribution reads them). Every vitest line through the mutex SHARED tier, worker-capped, ONE directory per invocation, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN; paths spelled inline. ONE commit per coherent item group by pathspec (the trailer your harness prescribes); `git show --stat HEAD` names exactly your paths each time. PAUSE-AND-RESUME: do everything ungated (the measurements, the cures, the pins), write `$SP/lane-fix-d7-scratch/FIX-D7.lane-resume.md` (batches in order), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
The receipt by execution: per item — MEASURED (the figure, the instrument), CURED (behaviour-preserving, the pin) or MOVED TO FIX-D6 (the mechanism and what would shift), or CLOSED (the reason); the golden suites' count lines and the two hashes; the lighting tuple + delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
