# LAUNCH — COMPILE EM-R0f: THE ECONOMY FINGERPRINT MOVES DOWN A LAYER — `fingerprintPowerEconomyInput` leaves `src/generators/` for `src/data/economyFingerprint.js`, a pure, golden-neutral move (the EM-R family's fourth member; chartered ODQ §934.47 addendum 35; launch by the chair, session a9df403c, 2026-09-20)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, SIXTEEN steps), then the kit's 2026-09-20 addenda at the foot of `LANE-PARALLEL.md` (the directory-whole rule; the zsh warnings; the trailer ruling; the `tests/security` fixture law; the report-only-arm rule). The tree you read is named in the dispatch message (a detached read tip; never edited). Scratch: `$SP/lane-em-compile-EM-R0f-scratch/`. Outputs are COPIES in your scratch (`EM-R0f.md`, `EM-R0f.manifest.json`, `EM-R0f.compile.report.md`, `EM-R0f.evidence.md`) — never into the kit, never into any worktree's docs.

**The authority, in order (read each WHOLE):** the charter row for EM-R0f and the ruling that chartered it (`git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md | grep -n 'EM-R0f'` — read every returned row whole; the ledger checkout is READ-ONLY, via `git show` only); design §22–§22.3 (`… show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`) for the family's place of this member (R0a · R0d · R0b v3 · **R0f** · R0c · R1–R5 · R6 · R7 — R0c's merge reads the fingerprint from its new home, so R0f lands before R0c); `docs/implementation/preambles/EM-PREAMBLE.md` at the read tip (its SHA-256 is `16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195` — the second amendment; cite it); `docs/implementation/PACKET_STANDARD.md`; the two EM packets that are the models of a small landed member — `EM-B3c.md` (LANDED) and `EM-B1k2.md` (LANDED) — for the §7 table (TOOL-1's arm: one full path per row, actions from `PACKET_ACTIONS`, table == JSON as `(action, path)` sets) and the §12 receipt.

**The packet's outcome (the chair's ruling on the shape — this is its index):** a PURE MOVE. `fingerprintPowerEconomyInput` (today in `src/generators/power/economyReconciliation.js` — confirm by symbol at the read tip; if it has already moved or been renamed, STOP and report) is CREATED verbatim in `src/data/economyFingerprint.js` (the data layer: a leaf with no `src/generators` or `src/domain` import — measure its imports; if the function's body imports from generators, the move needs the chair's ruling on the split: report it as a question, do not improvise a second leaf); every importer re-pointed to the new home (the consumer census with its denominator: `git grep -n 'fingerprintPowerEconomyInput' HEAD -- src tests scripts`, each site dispositioned: re-pointed / kept because it is the old home's re-export / exempt with reason); the old site keeps NOTHING but, if the estate's boundary idiom requires it, a one-line re-export with a `// moved: EM-R0f` marker (measure whether a re-export is needed for any importer the packet cannot re-point in the same commit; prefer none). **The proof of golden-neutrality is the packet's own acceptance case:** the fingerprint over ALL 525 golden rows is byte-identical before and after the move (`sha256` of the concatenated fingerprints, computed by a plain-node probe in your scratch at the read tip and written into the packet as the measured figure the build lane must reproduce). The domain→generators boundary ratchet (`tests/lint/domainGeneratorsBoundary*.js` — find it by symbol; read its header and its frozen set): the frozen set SHRINKS by exactly the rows this move retires and NEVER grows — the packet declares the exact rows and the walker's header gains EM-R0f's citation (a `MODIFY` row). No golden moves (`generator-golden-master.json`, `dossier-prose-manifest-golden.json`, the preset witness — all byte-identical; `UPDATE_*` doors forbidden).

**What you must MEASURE before contracting:** (1) the function's body and its imports at the read tip (effective lines by eslint's `Linter`, `max-lines` with `skipBlankLines`+`skipComments`); (2) the consumer census with its denominator (src / tests / scripts) and any test that pins the OLD path by literal (the `tests/` sweep by literal — every such pin is a declared TEST row with its cure, never a silent survivor); (3) the boundary walker's frozen set rows that name `economyReconciliation.js`'s fingerprint and what else in that file the rows cover (a row that covers more than the moved function does NOT retire — say so); (4) the byte budgets under the preamble's amended §P2 row 11: the generation worker's closure (`WORKER_BUNDLE_CEILING_BYTES`, exact, zero slack — a move within the worker's closure should be byte-neutral: measure, never assume), the eager first-paint closure (import `vite.config.js`'s `EAGER_FIRST_PAINT_MODULES` and test both the old and the new module's membership — a data-layer leaf the eager graph now reaches is a first-paint cost the packet must price), the edge-shared metas (does any `supabase/functions/_shared` bundle import the fingerprint? if yes, the packet owes row 10's rebuild and ALL FIVE metas as GENERATED rows — §P2 row 12); (5) the lighting delta counted from your CREATE/title rows against the frozen `2653·383·2270·25052·6680`; (6) the mutation register: a CREATE test file wants a row; a pure move of a function usually wants a `rationale` row citing the golden-neutrality proof — read the register's shape.

**Budget and split:** this is a SMALL member; if the honest packet exceeds 120 effective lines of change or five files, report why before compiling further. Register predictions as DELTAS (`it` titles only, in CREDITED files; never name a variable `it`/`test`/`describe`); `__BASE__` for the chair; `acceptanceCases` as `{id, case}` objects, at most six; the §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS`; `requiredSymbols` verbatim strings that exist at the read tip; `checks` as the build lane's focused commands (the boundary walker, the new suite if any, the golden suites, `tests/lint/contractTestAntiVacuity.walker.test.js` if any CREATE carries the `contract` token). Nine questions or fewer for the chair, each with your recommendation.

**Laws:** FORBIDDEN — `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*`, the other read-only lanes' scratch dirs, the ledger checkout beyond `git show`, the kit (read-only). No edit to any tracked file; `git status --short` at the read tip EMPTY at start and end. Plain `node` probes are ungated; any vitest line is GATED and PAUSE-AND-RESUME (the mutex SHARED tier, worker-capped, the two exports INLINE, one directory per invocation; the chair resumes you with "the gate is yours"). Stamps from `date` in the same call. If a command errors on a rate limit, stop and say so. Your report: the measurements above with their commands, the consumer census with its denominator, the golden-neutrality figure, the budget figures, the questions, and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
