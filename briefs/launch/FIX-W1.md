# FIX-W1 — the doubled article in four Wizard News beat composers ("the The Chandlers"): a falsehood the DM reads; the four composers cured at ONE article helper, measure-first, and whatever golden or dormancy pin carries the doubled text re-recorded through the owner's door (signed under the standing grant of 2026-09-20) (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 65, 98 — §934.65 SIGNED)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then the ledger's FIX-W1 charter (`git -C /Users/cstokes/Desktop/settlement-engine show HEAD:docs/OWNER_DECISION_QUEUE.md | grep -n 'FIX-W1'` — read each row whole: the four composers, the doubled-article shape, the rule that the cure MOVES whatever pin carries the doubled text and is measured first), the shift record precedent (`docs/shift-records/2026-09-20-cure-j-provenance.json`; CURE-J's receipt for the door's exact verb and its throw-by-design), and, in your worktree, WHOLE: the Wizard News beat composers (`git grep -n -E 'the \\$\\{|\\bthe \\+ ' -- src/domain/worldPulse src/generators | head` is only a start — find the FOUR by the charter's names and by a real instrument: render the Wizard News corpus over the golden seeds and grep the OUTPUT for `\\bthe The\\b|\\bthe the\\b|\\ba An\\b` — the population is measured on rendered text, never on source), the article helper if one exists (`git grep -n -i 'withArticle\\|articleFor\\|indefinite' -- src`), and the goldens / dormancy pins that carry Wizard News text (`tests/property/*`, `tests/fixtures/*`, the dormancy goldens — find them by grepping the fixtures for a doubled article).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b fix-w1-doubled-article-2026-09-20 $SP/lane-fix-w1 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-w1/node_modules` (no build). Scratch `$SP/lane-fix-w1-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists — never touch it.

## The three commits, in order (measure first)
1. **THE POPULATION, ON RENDERED TEXT.** Over the golden corpus (525 seeds) and the dormancy corpora, render every Wizard News beat and count the doubled articles by composer and by shape (`the The X` where X is a proper noun already carrying its article — `The Chandlers` — is the charter's; also `a An`, `an A`, `the the`); name the four composers by file and line, and any fifth the instrument finds (the charter's four came from a read, the instrument decides). Which goldens/pins carry the doubled text today (grep the fixtures; count the rows). Red-first: an arm over the rendered corpus that reds on any doubled article (report-only until the cure; then asserted).
2. **THE CURE AT ONE HELPER.** The proper-noun names that carry their own article (`The Chandlers`) are data; the composers prepend `the` blindly. Cure at ONE article helper (`articleFor(name)` / `withArticle(name)` — the existing one if the estate has it; else one new export in the composers' shared home), which returns the name unchanged when it already begins with an article (case-insensitive, word-bounded) and prepends otherwise; every one of the four composers routed through it; no composer keeps its own `'the ' +`. Behaviour elsewhere: names without an article render exactly as before (a corpus diff over every beat: only the doubled-article rows change — quote the count). The arm from commit 1 becomes ASSERTED with a counterforce (a planted `'the ' + 'The Chandlers'` in a throwaway → red).
3. **THE PINS, THROUGH THE DOOR.** The moved rows live in a golden or a dormancy pin — a LEGITIMATE one-time output change the owner signed (§934.65 under the standing grant; the words verbatim: "I sign everything. Anything that you need for a signature now or in the future I give."). Write the shift record `docs/shift-records/2026-09-20-fix-w1-doubled-article.json` (the estate's form; `predictedRows` = the count from commit 1 per surface; `proofForm` per surface), COMMIT it and the cure, then the door per surface (CURE-J's exact verb; the throw-by-design is the success line); diff each fixture: every moved row is a doubled-article row and nothing else (a row moved for any other reason is a STOP); `predictedRows` equal.

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/property` whole (the goldens), the composers' own suites, `tests/copy/voiceMechanics.test.js`; the lighting census FROZEN at the tuple the chair names at dispatch (record your delta; never refreeze); the edge-shared metas' `inputs` measured for every `src/` path you touch; the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; NUL-safe messages; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-w1-scratch/FIX-W1.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-w1-scratch/FIX-W1.receipt.md`: the rendered population by composer and shape; the helper's one home and the four (or more) routed sites; the corpus diff count; the shift record's path; the door's throw per surface; each fixture's diff attributed row by row; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
