# FIX-T1 — the 28 hand-built replicas of the ruin shape in `tests/` re-pointed at the ONE writer; EM-B1e's A4 second pin made to read the place a lawful shift re-records (launch brief; the chair, session a9df403c, 2026-09-20; charter row FIX-T1 = EM-B1e R7)

You are an Opus PARALLEL TEST-SIDE lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with its 2026-09-20 addenda: the directory-whole rule, the zsh-pathspec warning, the trailer ruling, the `tests/security` fixture law), then, in your worktree, WHOLE: EM-B1e's packet `docs/implementation/packets/settlement-editor/EM-B1e.md` (its R7 and A4 rows; its §12 receipt), its landing `git show 8f714cf3c` (the writer `ruinInstitution(inst, { reason, fate })` in `src/domain/worldPulse/calamityKernel.js` — both arguments REQUIRED — and its suite `tests/domain/ruinInstitution.test.js`), and `tests/simulation/presetLightingWitness.test.js` with its golden `tests/fixtures/preset-lighting-witness-golden.json` (the byte golden over the whole pulse and the ONE place a lawful pulse shift re-records).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b fix-ruin-replicas-2026-09-20 $SP/lane-fix-t1 <the branch tip the chair names at dispatch>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-t1/node_modules` (no build). Scratch `$SP/lane-fix-t1-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The class (EM-B1e's R7, measured at its pre-proof)
The pulse's ruin shape has ONE exported writer since `8f714cf3c`, so a DM's decree and a disaster leave the same record. But 28 hand-built replicas of that shape live in `tests/` (~18 files: `ruinFilter.probe.test.js:27` calls itself "the exact calamityKernel.ruin stamp"; `magicForms.test.js:307` spells all three flag keys) and NONE imports the writer — so the next lawful change to the shape (EM-B1h's closed fate vocabulary is chartered) silently forks 28 fixtures from the one record the product writes, and every suite built on them keeps passing against a shape the product no longer produces. Boundary sentence: this lane re-points every replica of the ruin shape at the writer and fixes A4's second pin; it changes no `src/` byte and weakens no assertion.

## The work
1. **THE CENSUS, with its denominator.** Find every test that hand-builds the ruin shape — search by the SHAPE, not the name: the three flag keys the writer stamps (read them off `ruinInstitution`'s body; never a literal list from this brief), the `reason`/`fate` fields, and every spelling of the ruined status the writer emits — over `tests/**` (a found-and-ordered read of the writer decides the keys). Report `found N / re-pointed / exempt-with-reason` — EM-B1e counted 28 in ~18 files at its base; yours is the live count (say what moved).
2. **RE-POINT each replica at the writer** (`import { ruinInstitution } from '…/calamityKernel.js'`, both arguments supplied by the test with the meaning it already intended — read the enclosing test whole to know which `reason` and `fate` it meant; a test that intended no particular fate takes the writer's documented default ONLY if one exists, else the fate its assertions imply, stated in a comment). A replica that DELIBERATELY builds a malformed or partial ruin to prove a guard is EXEMPT with the reason written at the site (and a one-line `// replica-exempt: <why>` marker so the census below can see it). No assertion weakened; no title changed unless a title named the hand-built shape (say so).
3. **THE CENSUS AS A WALKER, so the class cannot return:** `tests/lint/ruinShapeReplica.walker.test.js` — the same shape-search, over `tests/**`, convicting any hand-built ruin stamp outside the writer's own file and its suite, with the `// replica-exempt:` marker as the declared, executed exemption (the marker's reason is asserted non-empty; an exempt site must still be a ruin-shaped literal, else the marker lies). Three adversarial self-tests in the estate's idiom (fires on a planted replica; clears on a writer call; cannot be forged from a shape quoted in a string or comment). A `mutation`/`rationale` row in `scripts/mutation-coverage-manifest.json` (read the existing walkers' rows; a `self-proving-meta` rationale if the self-tests are the proof); the directory-whole rule: `tests/lint` whole once.
4. **A4's SECOND PIN.** EM-B1e's `tests/domain/ruinInstitution.test.js` A4 asserts the preset-witness digest (`tests/fixtures/preset-lighting-witness-golden.json`'s digest) as a literal outside the freeze register. Make it READ the golden file's digest at run time (one place re-records; the pin follows) — or, if the witness suite's own law forbids a second reader, NAME the second pin in `presetLightingWitness.test.js`'s header ("a second pin at ruinInstitution.test.js:<line>; a lawful shift re-records both") — prefer the first; say why if the second.

## Laws
No `src/` edit (STOP and say which if one is needed). Goldens untouched (`shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json tests/fixtures/preset-lighting-witness-golden.json` before and after — all three identical). The lighting census is FROZEN (the tuple the chair names at dispatch) — your CREATE file and any title moves change it: run the walker ONCE separately (or `LIGHTING_CENSUS_PRINT=1` if TOOL-9 has landed), record the tuple and your delta, NEVER refreeze. Every re-pointed suite runs in your gated batch, ONE directory per invocation (`tests/domain`, `tests/store`, `tests/generators`, `tests/simulation`, … as the census finds them); the standing instruments (negativeAssertionAnchor — every `.not.` anchored on the immediately preceding line; voiceMechanics; mutationCoverageManifest; eslint on every touched file); `tests/lint` whole once. Every vitest line through the mutex SHARED tier, worker-capped, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN; paths spelled inline. TWO commits by pathspec — (A) the re-points + A4's pin; (B) the walker + its register row — the trailer your harness prescribes; `git show --stat HEAD` names exactly your paths each time. PAUSE-AND-RESUME: do everything ungated (the census, the re-points, the walker, the self-tests), write `$SP/lane-fix-t1-scratch/FIX-T1.lane-resume.md` (batches in order), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
The receipt by execution: the census ledger (found / re-pointed / exempt-with-reason, by file:line); every re-pointed suite's count line; the walker's self-tests; `tests/lint` whole; the three goldens' hashes; the lighting tuple + delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
