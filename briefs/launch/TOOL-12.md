# TOOL-12 — the engine chunk's headroom hunt: ~181 bytes stand between the lazy-engine budget and the next generator byte; find the bytes the chunk never reads and price their move (measure-first, read-only; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 76)

You are an Opus RECON lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, recons and verifies. This lane EDITS NOTHING under `src/`, `tests/` or `vite.config.js`; it measures and reports; the chair rules the moves. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first for the laws that bind every lane, then, in your read tip, WHOLE: `vite.config.js` (the chunking — `ENGINE_SHARED_DOMAIN`, its EXCISIONS, `EAGER_FIRST_PAINT_MODULES`, the lazy engine's manual chunk and the worker's), `tests/build/vendorPdfLazy.test.js` (the lazy-engine budget `679,000` and how the chunk is found and measured), `tests/build/engineChunkLazy.test.js` and `tests/build/generationWorkerLazy.test.js` (the other two budgets), and the kit's per-module attribution instrument `$KIT/tools/attrib.config.mjs` (read its header for how it attributes rendered bytes per module — the chair's re-mint receipts use it).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; the scratch copy is `$SP/chair-kit-923472dc`)

YOUR TREE AND YOUR DIST: create the read tip `git -C $SP/slot-2 worktree add --detach $SP/read-tip-ec0a30da2 ec0a30da2` (EM-P2's landing) and `ln -s $SP/slot-2/node_modules $SP/read-tip-ec0a30da2/node_modules`; the chair's REAL build of that exact tip is already copied for you at `$SP/lane-tool-12-scratch/dist-ec0a30da2/` (179 MB, 547 chunks; `STRICT DIST OK — 538 tests` at 07:56) — you run NO build and NO gate; you read that dist. Scratch `$SP/lane-tool-12-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*` beyond your own, the ledger checkout beyond `git show`, the kit. No `git` mutation; `git status --short` at the read tip EMPTY at start and end.

## The finding (EM-R0f's compile, 2026-09-20)
The lazy engine budget (`tests/build/vendorPdfLazy.test.js`, ceiling 679,000 B) has ~181 B of effective headroom after EM-R0f's bound (679,000 − 677,935 − 184 − the kept ~700 B cross-environment margin). It is the tightest of the four budgets and the next member that lands any generator byte hits it. The estate's own law (EM-PREAMBLE §P2 row 11): THE CURE IS THE PLACEMENT BEFORE IT IS THE CEILING — bytes a bundle never reads are moved out of its import closure, not paid for (EM-P3's precedent: +634 B of citation data the worker never read, moved).

## The measurement, in order
1. **Reproduce the figure** on the copied dist: which asset IS the lazy engine chunk (by the test's own discovery rule), its exact byte size, the budget, the margin the test keeps, the headroom — quoted from the test's own arithmetic, not re-derived by a different rule. Same for the worker (`WORKER_BUNDLE_CEILING_BYTES`, zero slack) and the eager first-paint set, so the report carries all three side by side.
2. **Attribute the chunk per module** with the kit's `attrib.config.mjs` idiom (rendered bytes per source module inside the chunk — read how the chair's re-mint receipts did it and reproduce that exactly; if the instrument needs the vite metafile, say whether the copied dist carries one and what you used instead). A table: module → rendered bytes → share, sorted, the top forty and the long tail summed.
3. **Find the bytes the engine never reads**, by three independent instruments, each with its receipt: (a) the import graph — modules in the chunk's closure reachable only through an import the engine's entry paths never execute (a `node` walk of the real import graph from the chunk's entries, with dynamic imports classed separately); (b) data literals — large frozen tables, prose pools, catalogues inside the chunk whose consumers are elsewhere (the EM-P3 shape); (c) duplication — modules rendered into more than one chunk (the engine and the worker; the engine and first paint) where a shared chunk or a boundary move would remove a copy. For each candidate: its rendered bytes, its consumers (all of them, by `git grep` — POSIX classes, never `\s`), and whether moving it is a PLACEMENT (behaviour-neutral by construction: the same bytes served from another chunk on the same path) or a CHANGE (a chunk boundary the tests pin, a first-paint cost, a worker-closure cost).
4. **Price the top ten moves**: for each, the headroom it would buy in the engine chunk, the bytes it would cost elsewhere (a move into first paint or the worker is a cost the other budgets must absorb — measure against their headroom), the pins that would move (`tests/build/*` ratchets, the size baseline, the chunk-shape pins), and the risk class. Rank by headroom-per-risk. Name the move you would make first and the two you would refuse.
5. **The verdict**: the honest headroom reachable by placement alone (no behaviour change), and whether the ceiling itself was set with a rationale the estate still holds (read the test's header: the number's origin) — never recommend raising a ceiling; recommend moves.

## Your report
`$SP/lane-tool-12-scratch/TOOL-12.report.md`: the three budgets side by side with the test's own arithmetic; the attribution table; the candidates by instrument with receipts; the priced top ten with the first move named; the verdict; the stamp read from `date`; and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work). Every claim CONFIRMED by execution or PLAUSIBLE, labelled. If a command errors on a rate limit, stop and say so. End your turn with the report's path.
