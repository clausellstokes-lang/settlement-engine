# LAUNCH — COMPILE EM-R0c: THE MERGE — the consequence of an edit is the difference of two re-derivations, applied to the record by consistency group (the re-entry family's largest member; design §22.1, §22.2, §22.3)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, SIXTEEN steps). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-R0c-scratch/`.

**The authority, in order (read each WHOLE):** design §22, §22.1, ⭐ §22.2 (twelve items: the merge's law), §22.3 (the receipt split by path; groups as `{ id, root, members }`; cross-entry totals ATOMIC; cross-key checks ship), §22.4 (the identity table: the merge keys `npcs` by `id`, `institutions` and `factions` by `name`) — `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`; the charter's EM-R family table and its later amendments (same ref, `docs/implementation/charters/EDIT-MODE-TRAIN.md`). **The measured ground:** kit `findings/RECON-ARCH-REDERIVE-3.report.md` (the merge BUILT and executed as a prototype: `tools/rederive-prototype-3/merge.mjs` + `seam3.mjs`, `q2-noedit.mjs`, `q3-merge-edits.mjs`, `q5*.mjs`, `q6-sequences.mjs` — copy them to your scratch; every figure you quote is one you re-measured at your tip). **The siblings you consume (compiled drafts in kit `packets-waiting/`, evidence in `evidence/`):** `EM-R0a.md` version 2 — THE CLASS REGISTER (`src/domain/edit/recordRegister.js`: 47 keys classed; KEYED 48 / ATOMIC 15; six consistency groups incl. a per-entry group; the receipt MERGED, NEVER RECOMPUTED; `history.eventsTimeline` / `historicalEvents` are ORDER-BEARING — slotted to YOU); `EM-R0b.md` (version 2 is being compiled now — `recordInvariants(record) → violations[]`, 29 checks incl. six cross-key); `EM-R0d.md` version 2 (the band ladders' one home in `src/data/`). You IMPORT the register and the invariants; you re-declare nothing they own.

**The packet's outcome (the chair's rulings on the shape — design §22.2 is the contract; this is its index):** a PURE module family under `src/domain/edit/` — `mergeConsequence(record, R0, R1, { edit }) → { record′, delta }` — where: (1) NODE three-way with the subtree short-circuit and ABSENT as a value (identity with no edit: 63/63); (2) a declared GROUP untouched (`R0 ≡ R1` on it) keeps the record's group WHOLE; touched, it takes `R1`'s WHOLE; (3) KEYED collections merge by their DECLARED key, ATOMIC collections and arrays of scalars as one value, NOTHING by position; (4) ORDER is a reading merged three-way (the record's order stands unless the edit moved the common entries' relative order; the second arm COUNTED in the delta — measure over the full edit corpus how often it fires; the two order-bearing history collections are its real test); (5) MIRRORS (`factions[].members[]`, a complete copy of the NPC by id — 572/572) are RECOMPUTED after the merge, never merged; (6) the RECEIPT (`powerStructure.economyInputFingerprint`) is recomputed LAST over the merged record; HISTORY (`generationCoherenceReceipt.repairs`) and AUTHORED keys are untouched by CLASS; (7) THE RUNTIME GUARD: `recordInvariants(record′)` — a violation that neither the pre-edit record nor `R1` carries, and that is not the edit's own, ESCALATES deterministically (the enclosing declared group from `R1` → its top-level key(s) — BOTH keys for a cross-key check → every reading from `R1`), each escalation recorded in the delta; it terminates because `R1` is a generated world; (8) THE DELTA carries, per moved reading, THREE values — what it read BEFORE (the record), what it reads NOW (the merge), what THE EDIT ALONE accounts for (`R0 → R1`) — plus `orderMoved`, `escalations`, and the REMOVAL cascade's `orphaned` entries when the edit is a removal (EM-R6's vocabulary — read its draft); (9) ⛔ THE CHAIN IS THE SEMANTICS: there is NO batch entry point — the module's public surface takes ONE edit; a caller with several applies them in order, each against the previous `record′`, and `R0` for the next edit is the previous `R1` (the cache claim: byte-identical, 3/3) — say how the surface makes a batch call impossible to write by accident.
**What you must MEASURE before contracting (the recon measured four rows per edit; you own the corpus):** the eight edits × ALL SIX tiers over the 63-row sample through the prototype seam — identity 63/63; zero MIXED objects outside EM-R0a v2's six groups (any MIXED object is a missing group: report it to the chair with its leaves, do not invent a group); `recordInvariants` clean with ZERO escalations (run EM-R0b v1's instrument, the kit's `invariants.mjs`, until v2 exists; say which); the order rule's second arm's count; the honesty figures (leaves that took `R1` where the record had already settled: the recon's 43 % was a four-row figure). Anything that escalates or mixes on the corpus is a FINDING that goes to the chair with its smallest reproduction — EM-R7 (the corpus ratchet, the family's terminal proof) will hold the family to zero.
**Budget and split:** this is the family's largest member. Measure the prototype's effective lines honestly; if it exceeds 400 effective lines, 250 per leaf or 12 files, SPLIT — the natural seams are (a) the pure tree merge (node / group / keyed / atomic / order), (b) the post-passes (mirrors, receipt, the guard and its escalation), (c) the delta model — and say which consumer waits on which (EM-B2a, EM-B2b, EM-R7, the delta card in EM-D*). **Placement:** `src/domain/edit/` must reach NEITHER the zero-slack generation worker NOR first paint — prove by the import graph who imports it first (EM-B2a's store path?) and that the import is dynamic; price the bytes. **It moves no golden and persists no new key** (it is a function nobody calls until EM-B2a does): prove it.
Register predictions as DELTAS (`it` titles only, in CREDITED files; never name a variable `it`/`test`/`describe`); `__BASE__` for the chair; `acceptanceCases` as `{id, case}` objects, at most eight; the §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS` (print it); any generator among `checks` LAST; requiredSymbols with the post-edit simulation.
