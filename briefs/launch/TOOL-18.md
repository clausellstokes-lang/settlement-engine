# TOOL-18 — quoted prose inside CODE modules: 26.5 % of the emitted engine chunk is authored text living in `src/generators` (`npcStructure.js` 67.5 % by literal mass), beyond the prose-byte baseline's reach — measure the population and propose the reach (measure-first, read-only; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 83, TOOL-12's item 12; low priority)

You are an Opus RECON lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, measures and reports. This lane EDITS NOTHING; it measures and proposes; the chair rules. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first, then TOOL-12's report §2.2 (the attribution table), §3(b) (the data-literal instrument `literals.mjs` and `span.mjs`) and its ⛔ item 12 (`$KIT/findings/TOOL-12/TOOL-12.report.md`), and, in your read tip, WHOLE: `tests/lint/proseCorpusBytes.test.js` and `scripts/.prose-byte-baseline.json` (its `_instrument`, `_ceilingArithmetic`, `_declaredRowIdiom`, the ten `leaves` under `src/data/**`, the 2,800,000 raw ceiling for the six state leaves — ARCH §10's arithmetic, not the file's), `docs/ARCHITECTURE.md` §10 (or wherever the composed-prose corpus's sizing law lives — find it by the ceiling's own citation), and the memory of the prose programme in `docs/OWNER_DECISION_QUEUE.md` §934.9 (the defense leaf's shrink idiom, 154593 → 154563).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR TREE: a read tip at the branch tip the chair names at dispatch (`git -C $SP/slot-2 worktree add --detach $SP/read-tip-tool18 <tip>`; `ln -s $SP/slot-2/node_modules $SP/read-tip-tool18/node_modules`); TOOL-12's instruments (`$SP/lane-tool-12-scratch/tools/`) and its dist copy (`$SP/lane-tool-12-scratch/dist-ec0a30da2/`) are READ-ONLY — copy the tools into `$SP/lane-tool-18-scratch/`. No build, no gate, no git mutation; `git status --short` at your read tip EMPTY at start and end. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*` beyond your own scratch, the ledger checkout beyond `git show`, the kit.

## The measurement
1. **The population, by SOURCE (not by the chunk).** Over every `.js` under `src/generators/**` and `src/domain/**` (the two producer roots the wiring census walks), measure per file: total bytes; bytes inside string and template literals; of those, bytes that are PROSE (a literal of ≥ 40 characters containing at least one space and ending in sentence punctuation or a comma — state your rule exactly and its false-positive check on twenty sampled literals); the prose share. A table sorted by prose bytes: the top forty, the long tail summed, the estate total. Reproduce TOOL-12's `npcStructure.js` 26,939 of 39,921 rendered (or explain the difference between rendered and source bytes for it).
2. **The reach today.** Which of those files, if any, `scripts/.prose-byte-baseline.json` names (none is expected — its ten leaves are `src/data/**` generated files); which the wiring census's pool/variant counts cover (a `rollFrom` pool is counted as variants, not bytes); which the prose-numerics or the composed-prose manifest goldens (`tests/property/dossierProseManifest.test.js`) reach by rendering. The gap: prose bytes that no instrument bounds.
3. **Growth.** By `git log` over the last sixty days, the ten files whose prose bytes grew most (measure at two tips: the branch tip and the tip sixty days earlier — `git rev-list -1 --before='60 days ago' <branch>`), so the chair sees whether the corpus is growing inside code modules or merely sitting there.
4. **The proposal (the chair rules; you propose three shapes with a price each):** (a) extend `.prose-byte-baseline.json`'s row-by-row declaration to the top-N code modules by prose share (the same RAW-exact / GZIP-band idiom; price: N rows, a new leaf-set ceiling from ARCH §10's arithmetic or a new one the owner would sign); (b) a shrink-only per-root ceiling on TOTAL prose bytes inside `src/generators` + `src/domain` (one figure, no per-file rows; price: coarse, cannot say which file grew); (c) move the prose out of code into `src/data` leaves (the estate's own direction for the composed corpus — the state leaves are `.generated.js`; price: a src refactor per module, goldens byte-identical by construction only if the rendered text is unchanged — a build-and-golden question, not this lane's). Say which you would recommend and why, in one paragraph, and what each would cost the engine chunk's ledger (TOOL-12's 365 B margin) — a declaration costs zero bytes; a move (c) is a placement question for FIX-B2's family.

## Your report
`$SP/lane-tool-18-scratch/TOOL-18.report.md`: the population table with the rule and its sampled check; the reach today and the gap; the growth table; the three shapes priced with a recommendation; every claim CONFIRMED by execution or PLAUSIBLE, labelled; the stamp read from `date`; ⛔ everything noticed and not touched, each item specific enough to slot. If a command errors on a rate limit, stop and say so. End your turn with the report's path.
