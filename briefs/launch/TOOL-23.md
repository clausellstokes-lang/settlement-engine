# TOOL-23 — the reachability census: 248 of 2,247 `src/` modules are reachable from no build entry (FIX-D9's resolved-path scan over seven entries); classify every one, price the dead mass and the mutation obligations it carries, and put the retire/keep table to the owner (measure-first, read-only; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 87)

You are an Opus RECON lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, measures and reports. This lane EDITS NOTHING; it measures and classifies; the chair rules and the owner decides retirements (§934.70's class). Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first, then FIX-D9's receipt and evidence WHOLE (`$KIT/findings/FIX-D9/` — the resolved-path scanner over `src`, `tests`, `e2e`, `scripts`, `api`, `supabase` (static, dynamic, `require`, `vi.mock`), the SEVEN build entries including `src/utils/pdfRender.worker.js`, the 248 figure, the disposition idiom in `docs/DEAD_CODE_DISPOSITION.md`, the `ENGINE_SHARED_DOMAIN` seeding finding), TOOL-12's §3(a) (`$KIT/findings/TOOL-12/`), and, in your read tip, WHOLE: `docs/DEAD_CODE_DISPOSITION.md`, `vite.config.js`'s entries and worker block, `scripts/mutation-sweep.sh` + `scripts/mutation-coverage-manifest.json` (which modules carry plant obligations), and `tests/lint/deadCodeDisposition.walker.test.js` if FIX-D9's walker has composed by your tip (its arm is the register your table feeds).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR TREE: a read tip at the branch tip the chair names at dispatch (`git -C $SP/slot-2 worktree add --detach $SP/read-tip-tool23 <tip>`; `ln -s $SP/slot-2/node_modules $SP/read-tip-tool23/node_modules`); FIX-D9's scanner (`$SP/lane-fix-d9-scratch/`) and TOOL-12's graph tool (`$SP/lane-tool-12-scratch/tools/`) are READ-ONLY — copy them into `$SP/lane-tool-23-scratch/`. No build, no gate, no git mutation; `git status --short` at the read tip EMPTY at start and end. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*` beyond your own scratch, the ledger checkout beyond `git show`, the kit.

## The census
1. **Reproduce the 248** at your tip with FIX-D9's scanner (the seven entries enumerated by reading `vite.config.js`, `index.html` and every `new Worker(`/`.worker.js` construction — never a hand list; say if the count of entries differs from seven and why). For each unreachable module: bytes; effective lines; its importers by class (none / tests only / scripts only / docs mentions); its exports and whether any is re-exported by a barrel that IS reachable (a barrel re-export that nothing consumes is still dead — measure consumers, not exports); whether it carries a mutation-plant obligation (`mutation-sweep.sh`, the coverage manifest) or a disposition row already; its last commit touching it and the commit that removed its last production importer (`git log -S`, `--diff-filter`).
2. **Classify every one of the 248** into: `SUPERSEDED` (a successor ships its behaviour — name it, FIX-D9's `foldTradeCategories` shape); `DARK-BY-DESIGN` (a signed-off future seam — name the register or packet; Register VII's un-wired half is the precedent); `TEST-ONLY-FIXTURE` (imported by a test that tests nothing shipped — the test named); `ORPHAN` (no importer of any class); `BARREL-DEAD` (reachable only through a re-export nothing consumes). A table sorted by class then bytes; the totals per class; the mutation obligations per class (plants aimed at dead code are budget spent on nothing).
3. **The ESD/eager cost.** FIX-D9 found `ENGINE_SHARED_DOMAIN` is seeded by walking every file under `src/generators/` (the dark trio charging first paint by construction; ESD 51 → 49, EAGER 268 → 266 without it). Re-run both shipped derivations with ALL unreachable generator modules skipped and report ESD and EAGER sizes — the figure FIX-B3 will build against — and which reachable modules the unreachable ones alone hold in the eager set.
4. **The proposal for the owner (§934.70's class — you propose, the owner decides):** per class, the recommended fate (retire with its test / keep dark with a header / re-home as a fixture) and the price of each (bytes, tests, plant rows, walker rows), with the three cheapest safe retirements named first and the modules whose deletion would move a golden or a register named last (measure: does any unreachable module's text appear in a golden fixture, a frozen register, or a shift record?).

## Your report
`$SP/lane-tool-23-scratch/TOOL-23.report.md`: the entries as enumerated; the 248-row table with classes and totals; the ESD/eager figures; the proposal with prices; every claim CONFIRMED by execution or PLAUSIBLE, labelled; the stamp read from `date`; ⛔ everything noticed and not touched, each item specific enough to slot. If a command errors on a rate limit, stop and say so. End your turn with the report's path.
