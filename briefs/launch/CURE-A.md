# LAUNCH — CURE-A: the model walker's `codeOnly` regex (an Opus CURE lane IN THE SLOT; closes train EM-T3 before the lighting refreeze and run 17)

Read `briefs/LANE-PARALLEL.md` (chair kit) WHOLE and obey it with these substitutions: **your worktree is THE SLOT** `$SP/lane-em-b3b`, checked out ON `fixes-2026-09-18-consist` (confirm the branch and that `git status --short` is EMPTY before you touch anything; if it is not, STOP); you commit ON that branch by explicit pathspec — the chair grants commit authority for this one cure. Scratch: `$SP/lane-cure-a-scratch/`. Lane id: `CURE-A`.

**The defect (verified by the chair at `58fcfe614`; green today, so nothing forces it):** `tests/lint/chooserTotality.walker.test.js` defines a file-local `codeOnly(src)` that takes the shared `blankCommentsAndStrings` (`tests/helpers/codeOnlySource.js`) and then BLANKS import/export-from lines with `/^\s*(?:import|export)\b[^\n]*?from\s*['"][^'"]*['"];?[^\n]*$/gm`, replacing the match with `' '.repeat(m.length)`. Under the MULTILINE flag `^` matches at every line start and `\s` matches a NEWLINE, so when a blank line precedes an import/export-from line the match BEGINS at the blank line, swallows its newline(s) and the next line's indentation, and the replacement turns those NEWLINES INTO SPACES: the blanked text keeps its length but LOSES LINES, and everything after it reads at a shifted line number. **The chair's ruling on the shape:** horizontal whitespace only — `^[ \t]*` for the indentation and `from[ \t]*` (or the narrowest equivalent that keeps a match on ONE line; note `[^\n]*?` already stays on a line); nothing else in the walker changes; the shared helper is NOT touched.

**Measured first (write `<scratch>/CURE-A.evidence.md` before the first edit):** (1) the pattern as it stands and every call site of the file-local `codeOnly`; whether any call site maps a finding back to a LINE NUMBER or relies on line structure after the blanking; (2) eleven sibling files under `tests/lint/` also spell `codeOnly` — for each, say whether it carries its OWN copy of this import-blanking pattern with `\s` (LIST them; do not fix them: the chair rules on scope after your report); (3) a plain-`node` script in scratch (no vitest) that re-implements the file-local `codeOnly` in BOTH forms (current and cured) on top of the real shared helper, runs both over EVERY file the walker scans (derive its scanned set from the walker's own rule, by reading it), and reports: the files where the two outputs differ (expect: exactly the files with a blank line before an import/export-from line — the difference should be NEWLINES ONLY), and — the proof the chair wants — that THE WALKER'S FINDINGS (the chooser sites it extracts, per file) are IDENTICAL under both forms. Identical = the cure is behaviour-preserving today; proceed. A DIFFERENCE in findings is the finding: STOP and report it with the smallest example.
**Red-first:** one new `it` INSIDE the existing walker test file (no new test file) that feeds `codeOnly` a fixture with a blank line before an indented `import … from '…'` line and asserts the NEWLINE COUNT of the output equals the input's (and that the import text is blanked); it must FAIL against the current pattern (quote the red) and pass after the cure. State your lighting delta (one title, no file).
**Gated batches:** (A) the red; (B) the green: the walker file, then `tests/lint/` WHOLE (one directory per invocation), the standing instruments of the brief's verb 5, eslint on the touched file. Lighting: EXPECTED RED already at this tip (un-refrozen landings); record the measured tuple and YOUR delta only; NEVER refreeze.
**Commit subject:** `CURE-A: the model walker blanks import lines by horizontal whitespace only — line structure kept, the scanned findings identical before and after (train EM-T3's close)`.

---
## CURE-B (SAME LANE, SAME SITTING, its own commit) — the prose-numerics baseline RE-ADDRESSED for this train's landings
**The red (found by the FIX-F lane, 2026-09-19; proved against the base blob):** `tests/lint/proseNumerics.test.js`'s EXACT-IDENTITY arm is ALREADY RED on the integration tip. Its baseline addresses each allowed row by FILE + LINE; EM-B3a (`668d87512`) added 13 lines above a row in `src/domain/display/worldSnapshotPublic.js` (committed at line 615, scans at 628) and never re-addressed it — a build lane's focused runs never reach this walker, and the baseline file is outside every packet's manifest, so a packet's seal could not have carried the cure. Every landing of this train that shifted lines above a baselined row has the same debt: EM-P3 (`f4e5b64c5`), EM-B3a (`668d87512`), EM-B1e (`8f714cf3c`: +41 raw lines in `src/domain/worldPulse/calamityKernel.js`) and EM-B1d version 5 (its landing precedes you: `npcs.js`, `factionLifecycle.js`, `successors.js`, `envoyCasting.js`, `magicFormsPractitioner.js`).
**The work:** in your gated window run `tests/lint/proseNumerics.test.js` alone FIRST and quote every row the exact-identity arm convicts; for each, prove by reading that the row's CONTENT is unchanged and only its ADDRESS moved (the same token, the same sentence, at the new line) — a row whose content changed is NOT yours to re-address: STOP and list it; re-address the moved rows in the baseline file (find it from the test's own imports; change `line` values ONLY — show the diff greps to nothing but `"line"` values, as the FIX-F lane did for its own rows); re-run the walker GREEN; then `tests/lint` WHOLE. Commit subject: `CURE-B: the prose-numerics baseline re-addressed for train EM-T3's landings — <N> rows, addresses only, no row's content moved`.
**Also in this sitting, because it is the same walker file and the FIX-F lane measured it:** `tests/lint/proseNumerics.test.js` near line 464 cites a function `namesAScalar` that no longer exists (the regex it means lives unnamed inside `hasTwoDecimalToken` in `proseNumericsWalk.js`) — LEAVE IT: it is slotted to the FIX-F lane's next car (FIX-F2b), which edits that detector; two lanes must not touch one comment.

---
## CURE-C (SAME LANE, SAME SITTING, its own commit) — EM-B1e's test file is PARKED; make it CREDITED before the chair refreezes
Measured whole at `ad7ddf2c9` by EM-B1d's build lane: the live lighting tuple is `2649 · 384 · 2265 · 25015 · 6675` — EM-B1e's landed `tests/domain/ruinInstitution.test.js` moved `+1 files / +1 PARKED / +0 credited / +0 titles`, where its packet promised `credited +1, titles +7`. A parked file's pins prove nothing to the lighting contract, and a refreeze must never bank a parked file a packet promised credited. Find the park reason with the walker's own `parkReasonsFor` (`tests/lint/sovereigntyLightingContract.walker.test.js`; the likeliest reason is `OPENER_UNRESOLVED` — the file uses vitest GLOBALS instead of `import { describe, it, expect } from 'vitest'`) and make the file's registration statically provable WITHOUT changing a single assertion (the seven arms' titles and bodies byte-identical; prove it with a diff that touches only the import line, or whatever the park reason demands). Re-run the file GREEN 7/7, then measure the census: `parked −1 · credited +1 · titles +7 · suiteTitles +1`. ⛔ Do NOT touch `tests/generators/densityLaw.test.js` (112 `it`s parked for the same reason): crediting the estate's opener-parked files is its own slotted lane (FIX-L1), not a terminal cure.

## CURE-D (SAME LANE, SAME SITTING, its own commit) — the prose-wiring census re-taken for EM-P3's move of `resolveConfig.js`
`tests/lint/proseWiringCensus.walker.test.js` is RED at pristine HEAD (`3 failed | 75 passed`, `stale-bytes`, citing `src/generators/steps/resolveConfig.js:195/198/203`): EM-P3 (`f4e5b64c5`) edited that stamped file and its manifest carried no re-take. The estate's standing ruling (2026-09-08): a drift CONFINED TO THE `stamp` OBJECT of `docs/content/wiring-census.json` (the file count, `candidateLeaves`, the `stamp.files` shas) with NO row / fill / total / tier / grain / rate figure moved is a lane's PLAIN RE-TAKE — `node scripts/wiring-census.mjs` — printed with the diff's line count, the count of changed lines OUTSIDE `stamp` (must be 0) and the byte lengths; a moved ROW is the CHAIR's: REFUSE with the diff and stop. Re-take it AFTER every other cure of this sitting and after EM-B1d's landing is in your tree (its five `src/` files may be stamped too), so the census is taken once at the tree the terminal will check. If the prose leaf must regenerate in a forced order with the census (the estate has such a ritual — read the walker's header and `scripts/wiring-census.mjs`'s), follow that order and say so.

> CORRECTION TO CURE-C (the chair, 2026-09-19 19:5x): the 'likeliest reason is `OPENER_UNRESOLVED` -- the file uses vitest GLOBALS' above is REFUTED -- `tests/domain/ruinInstitution.test.js:32` and `tests/generators/densityLaw.test.js:49` both import from `'vitest'`, and 0 of 2,649 test files lack it. What is measured: the live tuple `2649 . 384 . 2265 . 25015 . 6675` against the frozen `2646 . 383 . 2263 . 25005 . 6671`, i.e. ONE of the three test files landed since the freeze (`ruinInstitution.test.js`, `tests/lib/editTravel.test.js`, `tests/store/decreeRegistryPersistence.test.js`) is parked for a reason `parkReasonsFor` must print. CURE-C = measure which and why; cure that file's registration without changing an assertion; REPORT `densityLaw.test.js`'s park reasons and do not touch it.
