# LAUNCH — RECON-ID: what IS an entity's identity in this estate, and what would "stable identity" have to change? (an Opus RECON lane; READ + plain `node` ONLY; the design is the chair's, the persisted-shape questions are the OWNER's)

**Seat:** Opus RECON. You measure and report; you edit, stage and commit NOTHING anywhere; no vitest, eslint, npm scripts or builds; plain `node` on scratch scripts only, ONE process of yours at a time. `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`. **Tree (READ + IMPORT ONLY):** named in the dispatch message. **WRITE ONLY** under `$SP/lane-recon-id-scratch/` — through ABSOLUTE paths (the kit's `tools/*/instrument.mjs` does `process.chdir(TREE)` at import; a relative write lands in the read tree). FORBIDDEN: every other lane's directory, `$SP/consist`, `$SP/slot-2`, the ledger checkout's working files (read the ledger ONLY via `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:<path>`).

**Why you exist.** The packet EM-P1 ("stable identity: every NPC, institution and faction gets a permanent id") was BLOCKED at its pre-proof (kit `findings/EM-P1-preproof/EM-P1.preproof.report.md` — read it WHOLE, §2, §5, §7, §8 and ⭐ §9). What it found: NPC ids ALREADY exist and are persisted (`npc_<n>`; the chair has ruled their spelling never changes); created NPCs carry a second namespace (`npc.<slug>_<hash>`); INSTITUTIONS already have a regen-stable anchor (`src/domain/townMap/anchors.js`: `anchorForInstitution` = `cat:<catalogId>` → `uid:<localUid>` → `name:<slug>`, consumed by seven modules); generated FACTIONS carry NO id, simulation-time houses mint `faction.<slug>` (written into REPLAYED event chains — lived history, immutable), nine readers take `id || name` (`factionRefOf`), `factionResponses.js` hand-rolls a slug at four sites, and the PERSISTED `linkedFactionIds` stores whatever handle `factionRefOf` returns — so minting a faction id silently flips persisted values from names to ids. NOTHING before the editor's first door needs an institution or faction id, so EM-P1 has been moved behind train EM-T7 and behind YOU: the chair designs "stable identity" from your table, not from the packet's premise.

## Measure
**Q1 — THE NAMESPACE TABLE.** Every identity an NPC, an institution, a faction (and, for completeness, a settlement, a relationship, a conflict, an active condition, a decree/dm-layer entry if present) carries or is addressed by: the key, its spelling(s), WHERE it is minted (module, symbol, and on which event: generation · simulation-time creation · DM creation · import · save migration), whether it is PERSISTED, whether it is STABLE under (a) same-seed regeneration, (b) a rename, (c) a roster reorder, (d) a save → load round trip, (e) fork/clone, (f) export → import, (g) the public veil. `anchorForInstitution`, `catalogId`, `localUid` and `seatKey` (the faction DISPLAY-NAME join `factionAffiliation` uses) are rows. Say for each entity kind: is there ALREADY a stable identity, and is it TOTAL (does every institution resolve to `cat:` or `uid:`, or do some fall to `name:` — count over the 525-row golden corpus by tier; a `name:` anchor is not rename-stable)?
**Q2 — THE HANDLE CENSUS.** Every place a reference TO an entity is STORED on a record or a save (not computed): `linkedFactionIds`, `factionAffiliation`, `governingFaction`, relationship endpoints (`npc1Id`/`npc2Id`?), conflict parties, institution names inside services / chains / defense buckets / history / trade dependencies, power-structure faction rows, AI overlays and authored markers, pulse ledgers keyed `${saveId}:${npc.id}` … For each: what it stores (id · name · anchor · slug), the resolver that reads it, and what happens to it TODAY when the entity is RENAMED (the existing cascades: `applyNpcRenameToSettlement`, `factionRenameChanges` / `src/domain/factionRename.js`'s join list — is each handle on a cascade's list? a handle that is NOT is a live rename bug: list it with a reproduction) and when the entity is REMOVED.
**Q3 — WHAT "MINT A FACTION ID" WOULD MOVE.** Prototype in memory (the override technique in kit `tools/rederive-prototype*/`; never edit the tree): give generated factions an id at `factionGrouping.js` before the sort, in the three candidate spellings — (a) `faction.<slug(name)>` (the simulation-time idiom: ONE namespace for all factions, and exactly what `factionResponses.js`'s four sites already assume when the id is absent), (b) a counter `fac_<n>` in draw order (the generated-NPC idiom), (c) NONE — keep names as the identity and make the rename cascade total instead. For each: which record keys change over a bounded stride, which PERSISTED handle values change, whether `resolveFactionRef`-style resolvers still resolve OLD name-handles after ids exist (mixed handles in one save), how many goldens move (generator master rows; prose manifest rows; the pulse/dormancy goldens), and what a rename then does. (c) is a real candidate: measure how far the existing name-join + cascade already is from total.
**Q4 — DOES AN INSTITUTION NEED A NEW ID AT ALL?** Given the anchor: what would an `id` give that `anchorForInstitution` does not? Count the institutions whose anchor is `name:` (rename-unstable) and say what it would take to make the anchor TOTAL and rename-stable for them (stamp a `localUid` at generation? — is `localUid` already stamped anywhere, by whom?). The observed-shape register convicts 33 readers asking for `id on factions` / `id on institutions` that no writer produces: for each of the 28 files, is the reader a real consumer that would work given an id, or dead code?
**Q5 — PINNED MODE AND THE MIGRATION.** `chooseOrPin` returns a pinned record without running the chooser, so a mint inside a chooser never runs under a pin; the save migration mints on first read keyed on stored array order. Under each Q3 candidate, where must the ONE mint function be called so that generator, migration and pin boundary agree, and what is the reorder ghost-write's smallest reproduction?
**Q6 — WHAT THE EDITOR ACTUALLY NEEDS.** Design §22–§22.3 (`git … show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`): the merge keys `npcs` by `id`, `institutions` and `factions` by `name`; a newcomer NPC is enriched on a stream keyed by its stable id; renames must not re-roll prose; a removal sweeps the rename-surface list. For each need, say which EXISTING identity already serves it and which need has NO stable identity today. The smallest "stable identity" that serves every need is the recommendation the chair wants — it may be much smaller than EM-P1.

## Report (final message; also `$SP/lane-recon-id-scratch/RECON-ID.report.md`, written early, re-written after every section)
Outcome first: for each entity kind, "already stable: yes / partly / no", and the smallest change that makes it total. Then Q1–Q6 with their tables. Then, in plain words for the OWNER, the one persisted-shape question that remains (if any) with what a DM with an existing save would see under each answer. Then noticed-not-touched (the owner's law: nothing is deferred — each item specific enough to slot). Every claim CONFIRMED (quoted command + output) or PLAUSIBLE. Do not adjudicate; do not build.
