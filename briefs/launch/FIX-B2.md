# FIX-B2 — the twelve engine members with a consumer in another chunk (FP-G11's `formatNumber` class, still live ×12): 38 emitted chunks statically import the 678 kB engine; cut the anchored ones by placement, proved by real builds (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 76, 82, 83)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-12's report WHOLE (`$KIT/findings/TOOL-12/TOOL-12.report.md` — §1 the three budgets by the tests' own arithmetic, §2 the attribution, §3(a) the import graph and its 38 importers, §4 the priced moves and the two refusals, §5 the verdict, the ⛔ items) and, in your worktree, WHOLE: `vite.config.js` (the manual chunks — `ENGINE_SHARED_DOMAIN`, its EXCISIONS, the engine and `engine-core-lazy` rules, the `narrativeData` rule near `:878`, the SEAMCYCLE comment at `:214-218`, `EAGER_FIRST_PAINT_MODULES`), `tests/build/vendorPdfLazy.test.js`, `tests/build/engineChunkLazy.test.js`, `tests/build/generationWorkerLazy.test.js` and every other `tests/build/*` pin that names a chunk, a chunk count or a chunk's importers, and `scripts/gate-mutex.sh`'s header (the tier names). TOOL-12's instruments (`$SP/lane-tool-12-scratch/tools/graph.mjs`, `attrib.mjs`, `span.mjs`, `literals.mjs`) are READ-ONLY at their home — copy them into your scratch and run them from there; its dist copy `$SP/lane-tool-12-scratch/dist-ec0a30da2/` is the BEFORE picture (built at ec0a30da2; your worktree is four docs/register commits later with no src change, so the engine's bytes are the same — prove it with your own first build anyway).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b fix-engine-anchors-2026-09-20 $SP/lane-fix-b2 c33446830`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-b2/node_modules`. Scratch `$SP/lane-fix-b2-scratch/`. YOU BUILD in your own worktree (its `dist/` is yours), every build through the mutex's DEFAULT (exclusive) tier with the two exports INLINE — `GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npm run build` — never wrapping `npm run check`; a build is ~5 minutes and the chair's full check (run 21) and a sealed build share the machine, so batch the moves (below) and build once per commit. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The ruling you implement (addenda 82–83; TOOL-12 §4–§5)
Twelve engine members have a consumer living in another chunk, so 38 emitted chunks statically import the 678 kB lazy engine — a 173-byte `livingContentLaw` chunk drags 677,935 B. That is FP-G11's `formatNumber` defect, still live, twelve times. THE CURE IS THE PLACEMENT (EM-PREAMBLE §P2 row 11): a member whose consumers live outside the engine is pinned to its own small lazy chunk (or joins `engine-core-lazy` where TOOL-12 says so), so the UI chunk imports the small chunk and STOPS importing the engine. THE CHAIR REFUSED (recorded, vetoable): move 1 (`src/data/narrativeData.js` — its consumers are all in-engine, the generation fetch is byte-identical, only the ledger shrinks: a ledger move is not a cure), move 7 (`lib/prebuiltResourceChains.js` alone — ledger-only) and the `structuralValidator` closure (HIGH; a seventh of the chunk re-shaped for a byte problem). You make the ANCHORED moves only; the engine's ledger headroom is a side effect you MEASURE, never the goal you report.

## The three commits, in order (each by explicit pathspec; each proved by a real build before the next)
1. **The LOW leaves — TOOL-12's moves 4, 5, 6, 10:** `content/livingContentLawVersion.js` + `content/livingContentSeam.js` (anchors: `livingContentLaw`, `densityCreateBoundary`, `importScrub`, `StructuredCampaignReconciliation`); `domain/customCategories.js` (anchor `CompendiumPanel`; own chunk or `engine-core-lazy` — take TOOL-12's recommendation and say which); `lib/narrativeMutations.js` (anchor `factionRename`); `generators/stressPriority.js` (anchor `generalDeskRead`). In the same commit: the SEAMCYCLE comment at `vite.config.js:214-218` corrected to the measured truth ("one emitted importer" is refuted — five, in four chunks; name them from your graph run), and the byte-vs-character question settled for the instrument that measures the engine chunk (TOOL-12: 677,935 bytes but 677,828 characters — read how `vendorPdfLazy.test.js` measures; if it measures a string's `.length`, fix it to bytes and quote both figures; if it measures bytes, add the one-line comment that says so and why).
2. **The MED pair — moves 2 and 3:** `computeActiveChains.js` + `chainMagicSubstitution.js` + `lib/prebuiltResourceChains.js` as one chunk (anchor `realmManifest`, 104,632 B, stops importing the engine); `generators/helpers.js` + `generators/priorityHelpers.js` (a cycle — they move together; anchors `DailyLifeTab` and GenerateWizard's helpers edge; 25 in-engine importers re-point at the new chunk — count them before and after).
3. **The partials — moves 8 and 9:** `domain/magicFilter.js` → `engine-core-lazy` (GenerateWizard keeps other engine edges — say which remain); `generators/terrainHelpers.js` (anchors `ConfigurationPanel` and `realmManifest`'s second edge).

## The proof, per commit (every figure by execution, quoted)
- The BEFORE/AFTER importer list: from your graph run over the built dist, the set of emitted chunks that statically import `engine-*.js` — the named anchor chunks must LEAVE the set; the count 38 → N; no chunk JOINS it.
- The engine chunk's byte size before/after (bytes, `stat`), the new chunk's size and its importers; the engine's rendered attribution shows ONLY the moved modules left (TOOL-12's attrib idiom).
- THE FIRST-PAINT CLOSURE UNCHANGED: 8 files; raw/gzip/brotli figures within budget and the moved modules ABSENT from it (a move that lands bytes in first paint is a STOP). THE WORKER BUNDLE UNCHANGED at 1,401,208 (zero slack; `worker.rollupOptions` is unset so `manualChunks` never runs for workers — prove the byte figure anyway).
- `tests/build` WHOLE green against YOUR dist with dist reads FORCED (find the `requireDistRead` switch; a `skipIf(!requireDistRead)` arm that skipped is not a pass — quote the executed arm's figure). Chunk-shape pins that name a count or a member set are updated SHRINK-ONLY with the measurement; NEVER raise a ceiling, budget or baseline (the lazy-engine ceiling 679,000 and its two unratified raises are the owner's question §934.68 — you do not touch the number).
- `npx eslint vite.config.js <touched tests>` exit 0. No `src/` byte in any commit (the moves are `vite.config.js` chunk rules + `tests/build` pins only; if a move needs a src edit, STOP and report which).
- Goldens untouched (hash the two before and after anyway). The lighting census is FROZEN at `2656·383·2273·25074·6684` (register at c33446830): no test file is created here; if you must create one, run the walker once, record your delta, never refreeze.

## Laws
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line; quote every shell expansion (`"$VAR"` — zsh does not word-split, git swallows a list as one pathspec); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; commit by explicit pathspec with the trailer your harness prescribes. PAUSE-AND-RESUME: the builds and the `tests/build` runs are GATED — do everything ungated first (read, plan the three rule sets, stage nothing you have not measured), write `$SP/lane-fix-b2-scratch/FIX-B2.lane-resume.md` (the gated batches in order: build 1 → tests/build → commit 1 → build 2 → …), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours". If a command errors on a rate limit, stop and say so.

## Your report
`$SP/lane-fix-b2-scratch/FIX-B2.receipt.md`: per commit the importer set before/after with the anchors named, the engine and new-chunk bytes, the first-paint and worker figures, every count line, the pins moved (shrink-only), the commit shas; the two refusals restated; ⛔ everything noticed and not touched, each item specific enough to slot (TOOL-12's items 1–12 that are not this lane's — the three dark generator modules, the three unbudgeted worker bundles — are already slotted by the chair; do not re-find them, do add anything new).
