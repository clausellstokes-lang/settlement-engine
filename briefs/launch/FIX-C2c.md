# FIX-C2c — the citation walker's third act: ARM 3 reaches packet headers (the docs scope), the 170 ambiguous-basename citations stop being silently skipped, the fourth UNRESOLVED sub-class (a target that existed only in a pinned uncommitted tree) is named, and the five stale docs citations FIX-C2b recorded are re-addressed by symbol (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 91 — FIX-C2b's hand-off)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-C2's and FIX-C2b's receipts (`$KIT/findings/FIX-C2/`, `$KIT/findings/FIX-C2b/FIX-C2b.receipt.md` — the four arms, `CODE_TREES`, the 29 archival files, ARM 4 report-only by the chair's acceptance, the ten items: 1, 3, 6, 7, 8, 9 are yours) and, in your worktree, WHOLE: `tests/lint/sourceCitationIntegrity.walker.test.js` and `tests/lint/sourceCitationIntegrity.shared.mjs` (every arm, the HISTORICAL roster, the `.source-citation-baseline.json` idiom now at `rows: []`, the ⛔ recorder-file refusal in the header), and `docs/README.md`'s index (the HISTORICAL banner convention).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-C2b's four composed commits (`0bc7503b8`, `a7cd336bb`, `7330a89e9`, `b1c1ac182` cherry-picked onto the branch; if the tip does not carry them, STOP and say so): `git -C $SP/slot-2 worktree add -b fix-citations-2c-2026-09-20 $SP/lane-fix-c2c <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-c2c/node_modules` (no build). Scratch `$SP/lane-fix-c2c-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it. ⛔ The three prose-manifest recorder files (`tests/helpers/dossierManifest.js`, `tests/helpers/goldenMasterCorpus.js`, `scripts/prose-rate-corpus.mjs`) are OFF LIMITS until CURE-J's re-record is signed (§934.71) — a citation inside them is recorded, never edited.

## The four commits, in order (each by explicit pathspec; each proved before the next)
1. **ARM 3 reaches the docs scope, measure-first.** ARM 3 (the symbol arm, report-only) is scoped to `CODE_TREES`, so an in-range stale address in a packet header is unseen. Widen its walk to the docs-live roots the EOF arm already covers; measure the population BEFORE printing (how many docs citations ARM 3 would flag; its false-positive rate on a sample of twenty read against live source — the `wiring-census.mjs:349` class); it stays REPORT-ONLY (the chair's standing ruling on ARM 3 and ARM 4: a heuristic over a half-false set never gates) and prints via `process.stdout.write`. In the same commit: the five in-range stale `implementation-packets.mjs` addresses frozen inside LANDED packets stay frozen (a landed packet is a historical record) — the arm's HISTORICAL rule must exclude LANDED packets' bodies by reading `PACKET_MANIFEST.json`'s status, never a hand list; prove it excludes them and still sees a READY packet's header.
2. **The 170 ambiguous-basename citations (29 distinct basenames) are no longer silently skipped.** Today a citation whose basename resolves to more than one file is dropped without a word. Resolve by the nearest path evidence in the same sentence or file (a directory fragment; an import in the citing file; the cited symbol's home when the sentence names one), and print the rest as a named class `AMBIGUOUS-BASENAME` with the candidates — report-only, counted, never a finding that gates; a ledger of the 170 by basename with resolved / unresolved counts.
3. **The fourth UNRESOLVED sub-class named.** FIX-C2b proved `writers.js` never entered history — the atlas pinned an UNCOMMITTED tree. The walker's absent-target classification gains `PINNED-UNCOMMITTED` (the target is cited by a document that records a tree hash or a date, and `git log --all -- <path>` is empty), distinct from DELETED and RENAMED; the citing documents are listed; the citations are struck or moved under HISTORICAL by the same rule FIX-C2b applied to the town-map files, with the reason.
4. **The five docs citations FIX-C2b recorded, re-addressed by symbol:** `COHESION_REMEDIATION_PLAN.md:120` → `safetyProfile.js:234` (in-range, stale); `docs/DESIGN_FP_COUPLINGS.md:510` and `docs/DESIGN_FP_INTERIOR.md:187` → `coup.js:113-114`; and any others ARM 3's widened walk finds in LIVE (non-HISTORICAL) documents at your tip, each read against live source before the edit and each recorded with its before/after. NOT yours: `WC-PREAMBLE.md:70`'s `:1360`/`:1366` (a family preamble; editing it re-stamps a family — DOC-3, the chair's), `ES-5D.md:677` (a LANDED packet's ratified deviation record — frozen), `MF-UC1.md:178` (ARM 4's documented false-positive family).

## Laws
No `src/` byte outside citation TEXT (comments and string literals only — a citation in code is a STOP). Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/lint` WHOLE once (the only permitted reds: the lighting census; the provenance title if swept); the lighting census FROZEN at the tuple the chair names at dispatch (run the walker once, record your delta — it asserts its five figures in order and stops at the first miss; never refreeze); FIX-C2b's two laws bind you: writing ABOUT a struck address in citation form re-creates it, and a docs edit that adds a line rots every line-addressed anchor below it (append at the end or re-address what you shift); quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-c2c-scratch/FIX-C2c.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-c2c-scratch/FIX-C2c.receipt.md`: ARM 3's docs population and sampled false-positive rate; the 170-by-basename ledger; the PINNED-UNCOMMITTED list; the five (or more) re-addressings with before/after; every count line; the lighting delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
