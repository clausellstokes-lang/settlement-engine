# TOOL-13 — the observed-shape scanner resolves 7.6 % of reads: WHY the other 92 % go unresolved, and what a resolution pass costs (measure-first, read-only; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 76)

You are an Opus RECON lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, recons and verifies. This lane EDITS NOTHING under `src/` or `tests/`; it measures and reports; the chair rules the cure's shape. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first for the laws that bind every lane (forbidden paths, no polling, stamps read from `date`, the report-only-arm rule), then the memory the chair's index carries on this register (`the-observed-shape-register-is-content-addressed-and-history-bound…` — read it from `/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/`, READ-ONLY) and, in your read tip, WHOLE: `scripts/check-observed-shape-readers.mjs` (the scanner and its `scanStats`), `scripts/.observed-shape-readers-baseline.json` (the register's shape: `manifests.{executionTree,scanTree,sourceTree}.entries[N]`, the 1,964 findings the terminal pins), and every helper the scanner imports.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR TREE: a READ TIP the chair names at dispatch (detached; never edited; `git status --short` EMPTY at start and end; `node_modules` symlinked from `$SP/slot-2`). Scratch `$SP/lane-tool-13-scratch/` (create it). FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*`, the ledger checkout beyond `git show`, the kit (read-only). No gated run (the scanner is a plain `node` script — run it in your read tip with its output redirected to your scratch; never `npm run check`). No `git` mutation anywhere.

## The finding (EM-R0f's compile, 2026-09-20)
The scanner's `scanStats` at `141a1d775` report **9,766 of 128,176 reads resolved** — 118,410 unresolved, a 7.6 % resolution rate — on the register that is supposed to make a reader-without-a-writer identity visible. It did not affect R0f (it adds no tracked shape read), but a register that cannot classify 92 % of what it reads is a guard whose "no explained-writer drift" verdict rests on 8 % of the surface.

## The measurement, in order
1. **Reproduce the figure** at the read tip with the scanner's own stats (the command and its output verbatim); confirm the denominators (what is a "read" to the scanner; what "resolved" means — a receiver bound to a shape in the register).
2. **Classify the 118,410 unresolved reads by CAUSE**, with an instrument you write in your scratch (an AST pass beside the scanner's own, reusing its parser): dynamic reads (`obj[expr]`), member chains longer than the scanner follows, aliased receivers (destructuring, re-binding), reads on parameters of untyped functions, reads inside callbacks the scanner does not enter, reads on values from `JSON.parse`/`structuredClone`/spread copies, test/fixture reads that should not count, and the remainder — each class COUNTED with three quoted examples (file:line) and the share of the 118,410 it explains. The classes must sum to the total (a remainder class is honest; "other" without a count is not).
3. **Which unresolved reads MATTER:** of the unresolved, how many read a field that the register knows as a WRITTEN shape (a field some writer stamps) — those are the reads the guard exists for and cannot see. Count them; quote ten.
4. **Price a resolution pass per class:** for each cause class, what the scanner would need (a binding pass; following one more chain link; entering arrow callbacks; a type-directed pass from the JSDoc typedefs the estate carries) and the measured cost of a prototype for the two largest classes (wall-clock over the tree; the resolution rate it reaches) — built in your scratch, never in `scripts/`. Report the rate the estate would reach and what the register's 1,964 findings would become (does the frozen inventory move? by construction it should not for reads that were unresolved; measure).
5. **The verdict:** is the 7.6 % a structural limit of a source-text scanner, a fixable gap, or a mix — with the cheapest honest step named (a walker that reports the resolution rate as a floor? a resolution pass in `scripts/`? a typedef-directed pass?). Recommend; do not build.

## Your report
`$SP/lane-tool-13-scratch/TOOL-13.report.md`: the reproduced figure with its command; the cause table summing to the total with examples; the reads-that-matter count; the priced pass per class with the prototype's measured rate and cost; the verdict and recommendation; the stamp read from `date`; and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work). Every claim CONFIRMED by execution or PLAUSIBLE, labelled.
