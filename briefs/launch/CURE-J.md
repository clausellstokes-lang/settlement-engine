# CURE-J — run 21's one red: the prose manifest's provenance pins its RECORDER by raw file sha, so FIX-C2's citation re-address in `scripts/prose-rate-corpus.mjs` (a recorder file) forced a golden refusal with zero rows moved; the recorder identity becomes comment-insensitive and the fixture is re-recorded ONCE with its rows proven byte-identical (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 84)

You are an Opus CURE lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then, in your worktree, WHOLE: `tests/property/dossierProseManifest.test.js` (the provenance arms at `:170-215` — `⭐ THE PROVENANCE REFUSES A FIXTURE ITS RECORDER DID NOT WRITE` compares `file._provenance.recorder` to `recorderShas()` and pins `shift === 'PROSE'`, the ruling text, the row count and `rowsSha`), `tests/helpers/dossierManifest.js` (`MANIFEST_RECORDER_FILES` = three files at `:335`; `recorderShas()` at `:345` hashes each file's RAW bytes; `manifestBytes`; `sha256`), `scripts/prose-manifest-cells.mjs` WHOLE (`MANIFEST_PROVENANCE` at `:61` — `shift`, `ruling`, `car`, `recordedOverSha` "DECLARATIVE", `note`; the `--record` path at `:116` writes `{...MANIFEST_PROVENANCE, recorder: recorderShas()}`), `tests/fixtures/.golden-freeze-register.json` and whatever test governs it (`git grep -l 'golden-freeze-register' -- tests scripts` — read the register's law: if it pins the golden FILE's sha, a re-record moves it and the register is updated in its own idiom in the same commit; if its update needs an owner door, STOP and say so), FIX-C2's receipt (`$KIT/findings/FIX-C2/FIX-C2.receipt.md` — the 32 re-addressed files include `scripts/prose-rate-corpus.mjs`), and the estate's comment-stripping idiom (`tests/domain/contributionLedgerShape.test.js:43 codeOnly`, `tests/config/pageBackgrounds.test.js:90`) — note FIX-P3's finding that `codeOnly` BLANKS STRING CONTENTS: your stripper must remove comments and KEEP strings, templates and regex literals.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b cure-j-2026-09-20 $SP/lane-cure-j c33446830` (the fifth refreeze; run 21's tip); `ln -s $SP/slot-2/node_modules $SP/lane-cure-j/node_modules` (no build). Scratch `$SP/lane-cure-j-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The finding, CONFIRMED by the chair
Run 21 (`npm run check` at c33446830) exited 1 on exactly one title: `tests/property/dossierProseManifest.test.js :: … THE PROVENANCE REFUSES A FIXTURE ITS RECORDER DID NOT WRITE` — "a fixture whose recorder has moved since it was written is REFUSED". Of the three `MANIFEST_RECORDER_FILES`, exactly one moved in `141a1d775..c33446830`: `scripts/prose-rate-corpus.mjs` (3 changed lines; FIX-C2's citation re-address — measure the diff yourself and say whether every changed line is comment or string text; if any is CODE, STOP and report). The rows did not move (the lane that composed FIX-C2 hashed the golden byte-identical) — the refusal is the IDENTITY, not the corpus. This is the marker-slice class (memory fold 112): a raw-sha pin on a source file is blind to the difference between a comment edit and a behaviour edit, so every citation re-address in a recorder file forces a golden re-record. FIX-C2b is re-addressing citations now; without this cure it recurs.

## The two commits, in order (each by explicit pathspec; each proved before the next)
1. **The recorder identity becomes comment-insensitive.** `recorderShas()` hashes each recorder file's COMMENT-STRIPPED source (line and block comments removed; string, template and regex literals kept byte-for-byte; whitespace otherwise untouched — state the stripper's exact rule and prove it on an inline fixture containing each shape: a `//` comment, a `/* */` block, a string containing `//`, a template literal containing `/*`, a regex literal containing `//`). The helper's docblock says why (this cure, the date, the class). A test arm beside the provenance arm — or in the helper's own test home if one exists (`git grep -l 'recorderShas' -- tests`) — proves BOTH directions by counterforce, measured in a throwaway and quoted: a comment appended to a recorder file does NOT move `recorderShas()`; a one-token code edit to the same file DOES; the file restored to its exact SHA-256 after each. The message in the provenance arm gains "(comment-insensitive since 2026-09-20)". No src byte.
2. **The one re-record, honest.** With the identity now comment-insensitive, the stored `_provenance.recorder` (raw shas from 09-17) cannot match — a one-time re-record is owed. Before running it: copy the fixture aside; run `node scripts/prose-manifest-cells.mjs --record`; diff the two files — `rows` MUST be byte-identical and `rowsSha` unchanged (quote both; if a single row moved, STOP — that is a prose shift and the owner's, not this lane's). `MANIFEST_PROVENANCE` is amended in the script — `shift` and `ruling` UNCHANGED (the test pins them; they name the last owner-signed PROSE shift, which is still the truth of the rows), `recordedOverSha` → `c33446830` (the field's own meaning: the base this record was built on), and `note` gains one sentence at its end: "2026-09-20 (CURE-J): re-recorded with ZERO rows moved (rowsSha unchanged) after the recorder identity became comment-insensitive — FIX-C2's citation re-address in scripts/prose-rate-corpus.mjs had moved the raw sha of a recorder file." Then the re-record proper, the fixture diff quoted (only `_provenance.recorder`, `recordedOverSha` and `note` change), the golden-freeze register updated in its own idiom if it pins the file (say exactly what moved in it and why that is lawful), and the generator golden untouched (`7177cd6e…` before and after).

## Gates (PAUSE-AND-RESUME as every lane; the chair grants this lane the gate at dispatch because it blocks the train — run your batch once your edits are staged, no pause, and say so)
Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (a line with no count DID NOT RUN; an invalid vitest flag such as `--reporter=basic` burns the lock and prints no count — use the default reporter). Batch: (1) RED-FIRST at your base: `tests/property/dossierProseManifest.test.js` red on the one title (quote it); (2) after commit 1, before the re-record: still red (the identity changed, the stored map is raw) — quote; (3) after commit 2: `tests/property/dossierProseManifest.test.js` + `tests/property/generatorGoldenMaster.test.js` (the control) + every test file that imports `tests/helpers/dossierManifest.js` (measure the set: `git grep -l "helpers/dossierManifest" -- tests`), in as few invocations as the one-directory rule allows — all green with counts; (4) the golden-freeze register's own test, if any, green; (5) `npx eslint` on every touched file, exit 0. Goldens: the generator golden byte-identical; the prose manifest golden's `rows` byte-identical (the whole-file hash moves by the provenance only — say the old and new whole-file hashes and the unchanged `rowsSha`). The lighting census is FROZEN at `2656·383·2273·25074·6684`; you add titles to existing files (no new test file) — record your title/suite delta, never refreeze. Quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; commit by explicit pathspec with the trailer your harness prescribes; `git show --stat HEAD` names only your files.

## Your report
`$SP/lane-cure-j-scratch/CURE-J.receipt.md`: the moved recorder file's diff classified; the stripper's fixture proof; the counterforce both directions; the fixture diff (provenance-only) with `rowsSha` unchanged and both whole-file hashes; the freeze register's change and its law; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot. End your turn with the receipt's path and the two shas.
