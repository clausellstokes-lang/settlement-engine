# LAUNCH — COMPILE EM-R0a: THE CLASS REGISTER of a generated record (first member of the re-entry family; design §22.2 items 2, 4, 5, 7)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md` steps 1–10). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-R0a-scratch/`. **Authority:** design §22, §22.1 and ⭐ §22.2 — `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/DESIGN_EDIT_MODE_AND_DECREES.md`; the charter's newest EM-R table (same ref, `docs/implementation/charters/EDIT-MODE-TRAIN.md`, the amendments section "ARCH-REDERIVE version 3"). **Measured ground:** kit `findings/RECON-ARCH-REDERIVE-3.report.md` (§2 the classification; §6 the MIXED-object census) and its scripts `tools/rederive-prototype-3/` (`q1-keys.mjs`, `q1b-shapes.mjs`, `q2-noedit.mjs`, `merge.mjs`, `invariants.mjs`, `seam3.mjs`) — you may copy them into your scratch and RE-RUN them at your tip with plain `node` (one process at a time); a figure you quote must be one you re-measured at YOUR tip.

**The packet's outcome (the chair's ruling on the shape):** ONE pure-data module under `src/domain/edit/` (propose the file name; a leaf must stay under 250 effective lines — split the DATA across sibling leaves if it does not fit) that declares, for a generated settlement record: (a) the CLASS of every top-level key and of every sub-path whose class differs from its parent's — `HELD · WORLD · CONSTANT · READING · MIRROR · RECEIPT · HISTORY · AUTHORED` exactly as design §22.2 item 7 rules them (`factions[].members[]` MIRROR of the NPC's role/title; `powerStructure.economyInputFingerprint` RECEIPT; `generationCoherenceReceipt` HISTORY; `userCanon`, `aiOverlays`, `dmLayer`, `decrees` AUTHORED); (b) the declared KEY of each keyed collection (the recon lists 41, with three TRAPS: `activeConditions` → `archetype` not `id`; the generation receipt's `repairs[]` is moot now that the receipt is HISTORY — say so; `relationships`/`conflicts` have no unique field and are safe only because HELD — declare them UNMERGEABLE so a future reclassification reds); (c) the keyless object collections declared ATOMIC (the recon lists them; `resourceAnalysis.gaps` is keyed on `chain` but NOT unique in 29 arrays — rule it ATOMIC and say why); arrays of scalars ATOMIC; (d) THE CONSISTENCY GROUPS — seeded from the MIXED census's nine object paths outside the trace and the receipt: for EACH, propose the SMALLEST object that closes the invariant (e.g. `economicState.foodSecurity` — label + the three flags + the ratio; a viability summary with the lists it counts), with the invariant named and the recon row that convicted it. Groups must not be so wide that an unrelated edit moves a card (design's test: adding a tavern never moves the food card). Put every group you are unsure of to the chair as a QUESTION with the measurement beside it.
**Its walker (the packet's test):** TOTALITY — generate a stride of the golden corpus at test time (say how many rows and how long it takes; the recon's 63-row sample is the floor) and assert every key path observed in a generated record is classed (a NEW record key reds until classed), every object collection observed is either keyed (and the key is present and UNIQUE in every observed array) or ATOMIC, and every declared path is observed at least once (no dead declarations — pair each negative with a liveness anchor). State the walker's `CANNOT-CATCH:` list in its header (a key that only appears at a tier or config the stride does not reach).
**This packet is DATA + one walker. It does NOT build the merge (EM-R0c), the invariants (EM-R0b) or any seam.** Its `requiredSymbols` are the generator entry it calls to produce records and nothing else. Price the bundles: the register is imported by the merge, which lives in the editor's lazy path — it must reach NEITHER the generation worker NOR the first-paint set; prove the placement by the import graph (who imports `src/domain/edit/` today?) and say which chunk it will land in when a consumer exists (today: none — a module with no importer is in NO chunk; say so and name the packet that will first import it).
