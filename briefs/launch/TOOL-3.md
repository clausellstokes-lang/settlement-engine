# LAUNCH — TOOL-3: every emitted WORKER has a byte ceiling; the two that have none get a measured one with a declared headroom, ratcheted per TRAIN

Read `briefs/LANE-PARALLEL.md` (chair kit) WHOLE and obey it. Worktree, branch and base are named in the dispatch message — ⭐ your worktree has a REAL `node_modules` (`npm ci`), because you BUILD: never replace it with a symlink (a symlinked install poisoned a hashed artifact in a sibling slot today). Scratch: `$SP/lane-tool-3-scratch/`. Lane id: `TOOL-3`.

**Why.** The generation worker has an EXACT, zero-slack byte ceiling (`WORKER_BUNDLE_CEILING_BYTES = 1401208`, asserted in `tests/build/generationWorkerLazy.test.js` — read that test and the ODQ §934.19 family on the ledger: `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md | grep -n "934.19"`), so nothing grows there unnoticed. Two other emitted workers have NO ceiling at all: `advanceInterval.worker` (the pulse/simulation worker — a 549-module closure that almost every domain packet touches; `grep -rn advanceInterval tests/build/` returns nothing; `tests/domain/advanceWorkerByteIdentity.test.js` is a structured-clone DETERMINISM pin, not a bundle ceiling) and `townSceneExport.worker` (whose closure includes `publicSafe.js`). Wave 3 of the editor program (the tick packets) will land bytes in the pulse worker; today two landed packets already have (EM-B1e +437 B minified; EM-B1d's three files), unpriced because no ceiling exists.
**The chair's ruling on the STYLE (charter, amendments of 2026-09-19; ODQ §934.47 addendum 22):** these two workers get a MEASURED ceiling with a DECLARED HEADROOM of 4 KB (`ceiling = measured bytes at your base + 4096`, the two numbers spelled as named constants with the date and the base sha beside them), ratcheted DOWN by the chair at every TRAIN terminal with per-module attribution — growth is noticed and named once per train, not re-minted once per packet. The generation worker's exact ceiling is NOT touched.
**Measured first (`<scratch>/TOOL-3.evidence.md` before any edit):** (1) a CENSUS of every worker the build emits — read `vite.config.js`'s worker config and `src/workers/`; run ONE real `npm run build` in your gated window (the exclusive mutex; never a bare `npx vite build`) and list every emitted worker chunk with its byte size, and every other emitted chunk that has NO ceiling test anywhere in `tests/build/` (so the chair sees the whole unguarded surface, not only the two named); (2) how the existing ceiling test FINDS the generation worker's file in `dist/assets` (by name pattern? by manifest?) and whether the same finder works for the other two — copy its idiom exactly; (3) reproducibility: build twice and say whether each worker's size is byte-stable across builds (if not, by how much — the headroom must exceed the noise by a wide margin and you say the measured noise); (4) the kit's per-module attribution tool (`$SP/chair-kit-923472dc/tools/attrib.config.mjs` — read its header: it records each module's rendered length in Vite's worker pass) — run it for the pulse worker at your base so the FIRST per-train attribution has its baseline table (write it to `<scratch>/advanceInterval.attribution.json`); name in it the modules EM-B1e and EM-B1d touched (`src/domain/worldPulse/calamityKernel.js`, `src/domain/entities/npcs.js`, `src/domain/density/factionLifecycle.js`, `src/domain/worldPulse/envoyCasting.js`) with their rendered bytes.
**Build:** the ceiling tests for the two workers, in the existing build-test file if its shape allows or a sibling beside it (dist-gated exactly as its neighbours are — and note a new guard landing in the composition train: a build test NAMED in a focused run with no `dist/` now REFUSES; in your own gated batch build first). Red-first: each new ceiling test must FAIL when you plant a lower ceiling (quote the red) and pass at the minted figure. The new tests register straight-line literal `it`s under ONE literal `describe` (the lighting census credits only statically registered files); state your lighting delta as `files / parked / credited / titles (it only) / suiteTitles (describe only)`; price the mutation-coverage row if the file you create is in an enforcer directory (`tests/build`: measure with `tests/lint/mutationCoverage.shared.mjs`'s `ENFORCER_DIRS` and `NAME_PATTERN`; `"uncovered"` is unavailable — `uncoveredBaseline` is asserted exactly).
**Gate use is heavy (builds): plan ONE gated window** — do every ungated thing first (reading, the census script, the test files written), then `PAUSED AT THE GATE` with the batch: build → measure → (edit the two constants from the measurement — that one edit is allowed inside your window) → build tests → `verify:dist` BARE → the standing instruments. Commit subject: `TOOL-3: the pulse worker and the scene-export worker get measured byte ceilings with a declared 4 KB headroom — <bytes> and <bytes> at <base sha>`.
