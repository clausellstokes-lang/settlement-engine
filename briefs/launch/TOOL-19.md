# TOOL-19 — the packet validator's retiree rule learns the seal: a non-terminal packet's `retiredSymbols` row must be PRESENT before READY, but a SEALED build's own §7 lawfully burns it (EM-B1f's STOP, the third shape of §7.4's trap); the rule distinguishes the two states (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 84)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then EM-B1f's STOP note and partial receipt (`$KIT/findings/EM-B1f/EM-B1f.STOP.md`, `EM-B1f.receipt-partial.md` — the three states the lane proved: ceiling 7 + row → EXIT 0; ceiling 6 + row → EXIT 1 `already absent … before READY`; ceiling 6, row removed → EXIT 0) and, in your worktree, WHOLE: `scripts/implementation-packets.mjs` (the `validate` verb; the retiree rule at `:844-848` and its message; how `dispatch` records a seal — the sealed dispatch bundle, `sealDigest`, where the seal state lives on disk and how `check:packet` reads it; `parsePacketHeader`), `docs/implementation/PACKET_STANDARD.md` §"Sealed dispatch and handoff lifecycle" and §"Change manifest" (the `retiredSymbols` contract), and the validator's own tests (`git grep -l 'retiredSymbols' -- tests/scripts tests/lint` — read the nearest whole).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b tool-19-retiree-seal-2026-09-20 $SP/lane-tool-19 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-19/node_modules` (no build). Scratch `$SP/lane-tool-19-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The defect and the cure (two commits)
1. **MEASURE the rule's states, then write the fixture.** Reproduce the three states on a synthetic packet in a fixture tree (the validator's tests already build packet fixtures — reuse their harness; never a real packet): (a) READY, retiree present → valid; (b) READY, retiree absent, NO seal → the existing refusal (correct: a placement whose retiree is already gone is stale); (c) READY, retiree absent, a LIVE SEAL for that packet on disk → today EXIT 1 (the defect), after the cure VALID with a printed note (`retiree burned under seal <digest>`). Say exactly how a live seal is detected (the dispatch artefact's path and the fields you read — `sealDigest`, the packet id, the HEAD it bound) and what happens when the seal's HEAD is not an ancestor of the tree's HEAD (a stale seal is NOT a licence: the refusal stands, with a message naming the seal's HEAD).
2. **THE CURE, minimal.** The retiree rule gains the one branch: present → ok; absent and no live seal → refuse (unchanged message); absent under a live seal whose bound HEAD is an ancestor → ok with the note. No other validator behaviour moves: run the validator's whole test home before and after and quote identical counts plus your new titles; `npm run validate:packets` on the real tree prints the same `valid:` line before and after. The standard's "Sealed dispatch and handoff lifecycle" section gains one sentence stating the rule (docs, the same commit). Counterforce: state (c) with the seal's HEAD forged to a non-ancestor → refused.

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; the lighting census FROZEN at the tuple the chair names at dispatch (no new test file — titles in the validator's existing test home; record your delta, never refreeze); quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-19-scratch/TOOL-19.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-19-scratch/TOOL-19.receipt.md`: the three states before/after with exit codes and messages; the seal-detection mechanism named; the counterforce; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
