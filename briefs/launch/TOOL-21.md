# TOOL-21 — 47 of 50 frozen golden-register rows carry `proofForm: null`, so the door's `PROOF_FORM_MISMATCH` refusal is structurally unreachable and ODQ §555.8's map-family protection is inert on `town-map-golden`, `town-map-v2-golden` and 45 more: measure which rows should carry a proof form, what the door would then refuse, and whether the register edit is the owner's door or the chair's mint (measure-first, read-only; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 86 — CURE-J's second noticed item)

You are an Opus RECON lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, measures and reports. This lane EDITS NOTHING; the register is frozen and the door's verbs are the owner's or the chair's. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first, then CURE-J's receipt (`$KIT/findings/CURE-J/CURE-J.receipt.md` — the door's mechanics it measured: `recordGolden` under `GOLDEN_SHIFT_SIGNED`, the shift record's `surfaces[].proofForm`, `goldenRecordDoor.js:49-53`, the frozen register's row for `dossier-prose-manifest` with `proofForm: 'derived-artefact'`; the finding that 47 of 50 rows carry `null`) and, in your read tip, WHOLE: `tests/fixtures/.golden-freeze-register.json` (every row: `surface`, the pinned sha, `proofForm`, `frozenAt`, and what else), `scripts/lib/goldenRecordDoor.js` (or wherever the door lives — find it by `PROOF_FORM_MISMATCH`; every refusal code and the condition that reaches it), `tests/lint/goldenFreeze.walker.test.js` (`:421-433` and the whole file — what the walker asserts about `proofForm`), `docs/shift-records/*.json` (every record: which `proofForm` values the owner has ever signed), and `docs/OWNER_DECISION_QUEUE.md` §555.8 (the map-family protection's text — `git -C <ledger> show HEAD:docs/OWNER_DECISION_QUEUE.md | grep -n '§555.8'`, read the section whole) plus every other ODQ ruling that names `proofForm` (`grep -n 'proofForm'`).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)
LEDGER=/Users/cstokes/Desktop/settlement-engine  (read by `git show HEAD:<path>` ONLY — its checkout is stale)

YOUR TREE: a read tip at the branch tip the chair names at dispatch (`git -C $SP/slot-2 worktree add --detach $SP/read-tip-tool21 <tip>`; `ln -s $SP/slot-2/node_modules $SP/read-tip-tool21/node_modules`). Scratch `$SP/lane-tool-21-scratch/`. No build, no gate, no git mutation; `git status --short` at the read tip EMPTY at start and end. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*` beyond your own scratch, the ledger checkout beyond `git show`, the kit.

## The measurement
1. **The register's rows, classified.** All 50 rows: surface · family (map / dossier / generator / prose / other — by the surface's name and its test home) · `proofForm` (`null` or the value) · the row's pinned sha and whether it equals the file on disk today (a drifted pin is its own finding) · `frozenAt`. Which of the 47 `null` rows belong to a family that §555.8 protects (the map family at least), and what §555.8 says the protection IS.
2. **The door's reach, executed in a throwaway.** With the door's code read: for a `null` row, which refusals CAN fire (the sha pin; the spent-record rule; the action) and which cannot (`PROOF_FORM_MISMATCH`); simulate a shift record for `town-map-golden` with `proofForm: 'derived-artefact'` against the `null` row and show the door's exact behaviour (accepts? refuses on another code? ignores the field?) — in a scratch copy of the register and a scratch record, never the tree's; the door's own dry-run mode if it has one, else a node harness that imports the door's predicate.
3. **What a filled `proofForm` would refuse.** For each family, the proof form the owner has signed before (from the shift records) and what a MISMATCH would have caught historically — replay every past shift record against a register where the rows carry the family's expected form: which past re-records would the door have refused, and would any refusal have been a false positive (a correct re-record refused for a wrong form)?
4. **The proposal (the chair rules; the owner signs anything that changes what the door refuses):** (a) fill the 47 rows with the family's expected form in ONE register migration (the price: the freeze register is FROZEN — how is it lawfully edited? the walker's `frozenAt` law; is a refreeze verb the chair's, or does it need the owner's word?); (b) make `PROOF_FORM_MISMATCH` fire when the RECORD carries a form and the ROW is `null` (the door learns "unspecified ≠ any"); (c) leave it and record §555.8 as inert. Say which you recommend and why; price each against the owner's signature and the frozen register's law.

## Your report
`$SP/lane-tool-21-scratch/TOOL-21.report.md`: the 50-row table; the door's reach with the simulation quoted; the historical replay; the three shapes priced with a recommendation; every claim CONFIRMED by execution or PLAUSIBLE, labelled; the stamp read from `date`; ⛔ everything noticed and not touched, each item specific enough to slot. If a command errors on a rate limit, stop and say so. End your turn with the report's path.
