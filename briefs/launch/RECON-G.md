# LAUNCH — RECON-G: the Overview's `resilienceScore` against its band label — measure what the 2026-07-22 re-grade would move (an Opus RECON lane; READ + plain `node` ONLY; the decision is the OWNER's)

**Seat:** Opus RECON. You measure and report; you edit, stage and commit NOTHING anywhere; no vitest, eslint, npm scripts or builds; plain `node` on scratch scripts only, ONE process at a time. `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`. **Tree (READ + IMPORT ONLY):** named in the dispatch message (a detached `read-tip-*` worktree with `node_modules` symlinked; confirm its HEAD). **WRITE ONLY** under `$SP/lane-recon-g-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/lane-em-b3b`, every other lane dir, the ledger checkout's working files (read the ledger only via `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:<path>`).

**The report you are settling:** the 2026-09-18 fixes chain REPORTED to the owner that the Overview tab shows a `resilienceScore` beside a BAND LABEL the score does not match, traced to a domain re-grade of 2026-07-22 (the bands were re-cut and one side was not carried). A `src/domain` re-grade is the OWNER's decision, so nothing was changed. The chair has NOT re-verified any of this. Find the record first: `git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/OWNER_DECISION_QUEUE.md | grep -n -i "resilien"` and the kit's `ORDER-QUEUE.md` / `REPORT-2026-09-19-the-fixes-consist.md`; then `git log -S` for the band thresholds around 2026-07-22 on the tree's history.

## Measure
1. **The two sides, by symbol:** where `resilienceScore` is computed (module, function, inputs, range), where its BAND LABEL is computed (the thresholds table; is the label derived from the SAME number, a different number, or a stored field?), and every surface that prints either (the Overview tab, the PDF, the AI/narrative context, exports). Is there ONE producer both surfaces read, or two derivations that can disagree?
2. **The disagreement, by execution:** generate the golden corpus's configs (or a stride of at least 63 rows across all tiers, the same sample the re-derivation recons used: kit `tools/rederive-prototype-2/` has the 63-row sample) and tabulate score → label: for each label, the min/max score observed; every row where the label's band, as the thresholds table defines it, does NOT contain the score. Count by tier.
3. **What the 2026-07-22 re-grade changed:** the commit(s), the thresholds before and after, and which side was not carried.
4. **What each candidate cure would MOVE:** (a) re-grade the label to the current thresholds; (b) re-grade the thresholds to the label's old cut; (c) derive the label from the score at ONE producer and render it everywhere. For each: how many of the sample's rows change their printed label, does either golden (`tests/fixtures/generator-golden-master.json`, `tests/fixtures/dossier-prose-manifest-golden.json`) carry the label or the score (so: would a golden MOVE — count the rows), does any prose read the band, does a saved world PERSIST the label (a persisted label means old saves keep the old word — say what a DM with a saved world would see).
5. **The proposal for the OWNER** in plain words: what a DM sees today (one concrete example: town, score, label), what they would see under each cure, what it costs (goldens, saves), and the lane's recommendation — marked as a recommendation; the owner decides.

## Report (final message; also `$SP/lane-recon-g-scratch/RECON-G.report.md`, written early and re-written after every section)
Outcome first (is the defect real at this tip: yes/no, how often); then sections 1–5. Every claim CONFIRMED (quoted command + output) or PLAUSIBLE. Do not adjudicate; do not build.

---
## PART 2 (same lane, same rules) — FIX-G1's MEASUREMENT: a viability summary that counts six dependencies beside a list of five
The third re-derivation recon's invariant instrument (kit `tools/rederive-prototype-3/invariants.mjs`; report `findings/RECON-ARCH-REDERIVE-3.report.md` §6) was clean on 62 of 63 PLAIN generated records; the residual is a PRE-EXISTING generator inconsistency: `town|germanic|mountain|mountain_pass|…` — `economicViability.summary` says **6 operational dependencies** while `economicViability.dependencies.length = 5` (check `V-SUMMARY-DEPS`). Measure: (1) reproduce it on that row at your tree's tip; (2) the producer of the summary sentence and the producer of the list, by symbol — where do the two counts come from, and which one is right (is a dependency counted and then filtered, de-duplicated by title, or capped)? (3) how often over the FULL golden corpus (all 525 rows of `tests/fixtures/generator-golden-master.json`'s configs, or as many as plain `node` can generate in reasonable time — say how many you ran) does ANY summary count disagree with its list (dependencies, warnings, plot hooks, suggestions — the instrument's `V-SUMMARY-*` family); (4) what the smallest cure is (count from the final list at ONE site), and what it MOVES: which golden rows' prose would change (count them — the dossier prose manifest carries the summary?), whether a saved world persists the sentence. Report it as a proposal: a cure that moves a golden goes through the owner's signed door, so the report must give the owner the before/after sentence for the one town and the number of rows that would re-record. Write this part to `$SP/lane-recon-g-scratch/FIX-G1.measurement.md` as well as the main report.
