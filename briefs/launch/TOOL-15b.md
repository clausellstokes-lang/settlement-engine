# TOOL-15b — the figure-in-a-symbol predicate at the GATE: TOOL-15's walker refuses the shape in `tests/lint`, but the validator (`scripts/implementation-packets.mjs`, HK-3/HK-5's home) is what a placement, a seal and the pre-commit hook actually run — the same predicate lands there, and `tests/scripts/implementationPackets.test.js`'s three live-estate census arms come under the guard-of-guards (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 94 — TOOL-15's proposal, accepted; after TOOL-15 composes)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-15's receipt WHOLE (`$KIT/findings/TOOL-15/TOOL-15.receipt.md` — the population 194 packets / 2,595 symbol rows; the four instances and the six total; ⭐ TE-26's DIRECTION LAW: a figure is a TRAP in a PRESENCE row (every `requiredSymbols` row; a `retiredSymbols` row before its packet lands) and LOAD-BEARING in a terminal ABSENCE row (GVF-1's `const BUDGET = 1405` against a live `1303`); the measured threshold — a name-bound value with |v| ≥ 2 convicts exactly the class, every wider spelling refused by census; bare `<`/`>` excluded; the admitted numeric key set with reasons) and, in your worktree, WHOLE: `tests/lint/packetSymbolFigureCensus.walker.test.js` (TOOL-15's walker — its predicate is the ONE spelling; you IMPORT or lift it byte-for-byte, never a second implementation that can drift), `scripts/implementation-packets.mjs` (HK-3 and HK-5 — find them by their tags; the `validate` verb's error path; how a refusal prints), `tests/scripts/implementationPackets.test.js` (its three live-estate census arms), and `tests/lint/mutationCoverage.shared.mjs` (`ENFORCER_DIRS`, the `NAME_PATTERN` tokens — the rule that makes a test file visible to the guard-of-guards).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include TOOL-15's composed commits (STOP if `tests/lint/packetSymbolFigureCensus.walker.test.js` is absent): `git -C $SP/slot-2 worktree add -b tool-15b-validator-figure-2026-09-20 $SP/lane-tool-15b <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-15b/node_modules` (no build). Scratch `$SP/lane-tool-15b-scratch/`. ⚠ `scripts/implementation-packets.mjs` is check index 11 of every sealed packet's `checks` — if a packet's dispatch is LIVE in the slot when you would compose (the chair tells you), your change lands after that packet lands; you build and prove on your lane regardless. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The two commits, in order
1. **The predicate at the gate.** The validator gains one refusal beside HK-3/HK-5: a `requiredSymbols` row, or a `retiredSymbols` row of a NON-terminal packet, whose `symbol` carries a name-bound figure (TOOL-15's exact predicate — imported from a shared module you extract from the walker, so the walker and the validator run ONE function; the extraction moves bytes, not meaning, and the walker's own arms stay green), is refused with the packet, the row index and the figure-free spelling the row should carry; terminal packets' `retiredSymbols` are admitted by shape (the direction law) with the same shared function deciding. The population is zero at your tip (TOOL-15 cured the four), so `npm run validate:packets` prints the same `valid:` line before and after. Counterforce, measured and quoted: plant `= 7` into one READY-or-LANDED `requiredSymbols` row in a throwaway copy of the manifest → refused with the row named; plant it into a LANDED packet's `retiredSymbols` row → admitted; restore the exact SHA-256. The pre-commit hook path: prove (read `.husky/pre-commit` and `lint-staged`'s config) whether a manifest edit runs the validator at commit time; if it does not, say so — the gate (`npm run check`'s `validate:packets` step) is where it binds, and the receipt says which.
2. **The guard-of-guards sees `tests/scripts/implementationPackets.test.js`.** Its three live-estate census arms are invisible to `mutationCoverageManifest.test.js` because `tests/scripts` is not an `ENFORCER_DIR` and the basename matches no `NAME_PATTERN` token. Measure the two cures: (a) add `tests/scripts` to `ENFORCER_DIRS` — count how many files that admits and how many rows the manifest would then owe (every one needs a rationale row; if more than a handful, this cure is a STOP for the chair's ruling, priced); (b) rename nothing, but add a `NAME_PATTERN` token that matches this one file — measure what else it matches. Take the cheaper measured cure that admits exactly this file's arms (or STOP with both priced); its manifest row added surgically beside its siblings; the guard's own test green.

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/lint` WHOLE once (you touch a walker: the only permitted reds are the lighting census if the register predates your base and the provenance title if swept); `tests/scripts` whole; the lighting census FROZEN at the tuple the chair names at dispatch (no new test file; record your title delta; the walker asserts its five figures in order and stops at the first miss; never refreeze); `npm run validate:packets` identical before and after; the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-15b-scratch/TOOL-15b.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-15b-scratch/TOOL-15b.receipt.md`: the shared predicate's extraction (bytes moved, the walker's arms green); both counterforces quoted; the hook-path finding; the two guard-of-guards cures priced and the one taken; every count line; the lighting delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.

## Addendum (the chair, 2026-09-20 13:3x — ODQ §934.47 addendum 106): THE FIRST LIVE INSTANCE OF THE CLASS
FIX-D9's lane, walking every commit between its base and the slot tip `5dd5e8e68`, found that FIX-B2's three composed commits (`05bd8d074` 912 → 1040; `bb1f15dd0` → 1110; `ede9b5300` → 1138) moved `vite.config.js`'s `testTimeout` line while two citers still said `vite.config.js:912` (`tests/lint/siteCoherenceRatchet.test.js:69`, `tests/scripts/implementationSession.test.js:187`) — stale by 226 lines, and the symbol arm of `sourceCitationIntegrity` saw it only as `codeSym 3 → 5`, REPORT-ONLY, so three composed commits and their gates were green over it. FIX-D9's lane re-addresses the two at its composition (FIX-B2b, a fourth commit). YOUR bar: the symbol arm becomes ASSERTED against a baseline that is born at ZERO after FIX-B2b and FIX-C2c compose (a counterforce: move `testTimeout` by one line in a throwaway and the arm reds naming both citers), so the next such move is red at the lane's own gate, not found by a later lane's read.
- **THE THIRD INSTANCE, a different vintage (FIX-D9's sweep, 13:4x):** `tests/data/dossierStateProseProjection.contract.test.js:1509` cites `vite.config.js:877-878` for the `/src/data/ → data-lazy` rule; at the composed tip those lines are unrelated prose, and at FIX-D9's base (`6a3e8089f`) they were the `narrativeData` force-route — imprecise BEFORE FIX-B2. The class is a standing property of line-addressed citations into a churning config, not a FIX-B2 artefact. YOU cure this one (a one-token re-address after quoting the live rule's line, line-neutral; `tests/data` WHOLE once before the commit) as part of bringing the asserted symbol arm to a zero baseline; the cure's shape is the chair's ruling: the symbol arm stops being report-only for `CODE_TREES` (citations there are gate-wired with no baseline), AND any citation into `vite.config.js` may name the rule's exported symbol or a `@consumed-by` marker instead of a line — the arm accepts either form.
