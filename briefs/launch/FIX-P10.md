# FIX-P10 — the phone-floor family's leftovers after FIX-P5: five prose-shaped 12–13 px lines on lane 28's surfaces; `phoneChromeFloor`'s stale `/create` and `/settlements` rows re-measured shrink-only; `WizardOutputToolbar`'s sticky offset against the painted header's real height; `dossierPhoneFloorAllViews.test.jsx` extended to `/pricing` (measure-first; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 89 — FIX-P5's hand-off; after FIX-P5 composes)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-P5's receipt WHOLE (`$KIT/findings/FIX-P5/FIX-P5.receipt.md` — the prose floor (`proseFontSize`, the 14 px phone PROSE floor), the `publicChromeFloor` roster and `PROSE_FLOOR_ROSTER`, `phoneChromeFloor`'s `ROUTE_BASELINE` idiom (floored / ruled / bare per route; shrink-only; an owner row removed by the registry's own law), F7's measurement of the painted slip (12 px cannot fit — §934.64 is the OWNER's and NOT this lane's), noticed items 4–7) and, in your worktree, WHOLE: `tests/components/publicChromeFloor.census.test.js`, `tests/components/phoneChromeFloor.*.test.js` (find by name), `tests/components/dossierPhoneFloorAllViews.test.jsx`, `src/App.jsx` (`:916`, `:946` and the other three lines FIX-P5 names — read each in its enclosing component), `src/components/wizard/WizardOutputToolbar.jsx` (or wherever `HEADER_H` is pinned — `git grep -n 'HEADER_H' -- src`), and the painted header's real height source (the plate's geometry FIX-P5 measured: 83.92 × 22.47 at 375).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-P5's five composed commits (STOP if not): `git -C $SP/slot-2 worktree add -b fix-phone-floor-2-2026-09-20 $SP/lane-fix-p10 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-p10/node_modules` (no build). Scratch `$SP/lane-fix-p10-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The four commits, in order (measure first; each proved before the next)
1. **The five prose-shaped lines.** For each (`src/App.jsx:916`, `:946`, and the three others FIX-P5 names — re-locate them by content, never by the line number, which has moved): is it PROSE (a sentence a phone reader reads) or CHROME (a label, a legend code, a control)? Prose goes through `proseFontSize` (the floor); chrome is recorded with its reason and left. The `publicChromeFloor` census's `PROSE_FLOOR_ROSTER` row for each touched surface moves by exactly the lines you floored (shrink-only in `bare`, up in `floored` — quote before/after); red-first by planting one line back to its literal size.
2. **`phoneChromeFloor`'s stale rows.** `/create` and `/settlements` read stale against 63e40fe57 (FIX-P5's finding). Re-measure both routes with the registry's own instrument at your tip; update the rows to the measured figures under the registry's shrink-only law (`bare` never rises; if `bare` would rise, that is a regression on the route — STOP, name the offending element, and cure it in this commit instead of raising the row); an `owner:` field removed only by that registry's own law.
3. **`WizardOutputToolbar`'s sticky offset.** It pins `top: HEADER_H` (22.47 px at 375) while the painted header is taller (measure the real rendered height of the header at 320 / 375 / 391 / 430 in the existing phone-width harness); if the toolbar overlaps the plate, derive the offset from the header's measured height (one source of truth — the same constant the header itself uses, never a second literal) and add the arm to the existing toolbar test that pins the relation at the four widths; if it does not overlap, record the measurement and CLOSE it.
4. **`dossierPhoneFloorAllViews.test.jsx` sees `/pricing`.** It renders the dossier views; `/pricing` is a public surface outside its roster. Extend its roster to `/pricing` through the same harness (measure the added suite's wall-clock; over 20 s → say so); the census figures it prints for `/pricing` must equal `publicChromeFloor`'s (`floored 17 / ruled 3 / bare 0` at FIX-P5's tip — re-measure).

## Laws
Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/components` whole and `tests/ui` whole (the floor censuses live there); the lighting census FROZEN at the tuple the chair names at dispatch (no new test file — arms in existing homes; record your title delta; the walker asserts its five figures in order and stops at the first miss; never refreeze); §934.64 (the painted slip's 10 px) is the OWNER's — touch nothing there; the three prose-manifest recorder files are off limits; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-p10-scratch/FIX-P10.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-p10-scratch/FIX-P10.receipt.md`: the five lines classified with reasons; the two routes' before/after rows; the header/toolbar measurements at four widths; the `/pricing` census equality; every count line; the goldens' hashes; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
