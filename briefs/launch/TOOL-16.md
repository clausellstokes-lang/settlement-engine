# TOOL-16 — three worker bundles larger than the budgeted one have no ceiling at all: the monotone-down ceiling idiom extended to `advanceInterval`, `pdfRender` and `customContentPreview`, each minted from a measured build (measure-first; launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 83, TOOL-12's items 6–7)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus measures, implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-12's report §1 (budget 3), §3(c) and its ⛔ items 6–7 (`$KIT/findings/TOOL-12/TOOL-12.report.md`), and, in your worktree, WHOLE: `tests/build/generationWorkerLazy.test.js` (the header's history — the per-entry worker build, the `import(` premise that was wrong, the duplicate-on-disk paragraph; `WORKER_BUNDLE_CEILING_BYTES` "MINTED FROM A MEASURED BUILD at the §900 composition", EXACT with zero slack; how the emitted worker file is discovered; `requireDistRead`), `vite.config.js`'s `worker:` block and each worker entry (`src/**/*.worker.js` — enumerate them: `git grep -l -E 'new Worker\(|\.worker\.js' -- src vite.config.js`), the re-mint precedent (`git show 91d5f155b --stat` and its message: the form a ceiling re-mint takes), and `EM-PREAMBLE.md` §P2 row 11 (the byte-budget law).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b tool-16-worker-ceilings-2026-09-20 $SP/lane-tool-16 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-16/node_modules`. Scratch `$SP/lane-tool-16-scratch/`. YOU BUILD ONCE in your own worktree through the mutex's default (exclusive) tier with the two exports INLINE (`GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npm run build`; read `scripts/gate-mutex.sh`'s header for the tier names) — the ceilings are minted from YOUR measured build, at the tip the chair names, and the receipt says so. The chair's dist copy of ec0a30da2 at `$SP/lane-tool-12-scratch/dist-ec0a30da2/` (READ-ONLY) is a cross-check: `advanceInterval.worker` 2,656,872 B, `pdfRender.worker` 2,303,334 B, `customContentPreview.worker` 1,627,922 B by `ls -la` there (TOOL-12's table printed 2,656,422 and 1,626,660 for two of them — settle the discrepancy by `statSync` at your build and say which instrument was off and why). FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The measurement, then the mint (two commits)
1. **MEASURE (no edit).** After your build: for each of the four workers (the three unbudgeted and the budgeted `generation.worker` as the control, which must reproduce 1,401,208 exactly or you STOP — the tip has no src change since ec0a30da2): bytes by `statSync`, module count, the ten largest modules by rendered length (the kit's `tools/attrib.config.mjs` idiom — a worker build is its own rollup build, so attribution comes from your build's `generateBundle`; if the shipped config emits no metafile, say what you used), whether the worker carries the engine (`advanceInterval` should — it advances the world) and which other worker carries it too (the duplicate-on-disk paragraph's figure re-measured), and whether `worker.rollupOptions` is still unset (so `manualChunks` never runs for workers — TOOL-12's finding; the reason a worker ceiling has no placement cure).
2. **THE MINT.** Three ceilings in the idiom `WORKER_BUNDLE_CEILING_BYTES` already uses — EXACT at the measured byte figure, zero slack, monotone-down (a shrink re-mints the figure down in the re-mint form of `91d5f155b`; a rise is a STOP with attribution) — each with its own test arm beside the existing one (extend `tests/build/generationWorkerLazy.test.js` if its shape admits siblings, else a sibling file `tests/build/workerBundleCeilings.test.js` — a CREATE, so the `tests/build` directory runs WHOLE and your lighting delta is recorded), each arm gated on `requireDistRead` the same way and never skipping silently (the preamble's rule: a skipped byte arm is not a pass — the receipt quotes the executed figures). The header carries the four figures, the tip, the date, the instrument, and the sentence that a worker byte has NO PLACEMENT CURE (buy-back inside the worker's own files only). Cross-check: `tests/build` WHOLE against your dist with dist reads forced — every arm executed, every figure printed; then delete your `dist/`? NO — leave it; the chair's landing build is separate.

## Laws
Not one byte under `src/` or `vite.config.js` (a ceiling changes no behaviour; if a measurement suggests a placement, REPORT it — FIX-B2 owns placements). Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line; the lighting census FROZEN at the tuple the chair names at dispatch (run the walker once if you CREATE a test file, record your delta, never refreeze); eslint on every touched file; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: the build and the `tests/build` run are GATED — do everything ungated first (read; enumerate the workers; draft the arms), write `$SP/lane-tool-16-scratch/TOOL-16.lane-resume.md` (build → measure → mint → tests/build whole → commit), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-16-scratch/TOOL-16.receipt.md`: the four-worker table (bytes by `statSync`, modules, top ten, engine carried?, the control reproduced), the discrepancy settled, the three minted figures with the tip, every count line with the executed arms' figures, the lighting delta, the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
