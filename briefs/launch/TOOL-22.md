# TOOL-22 — a sanctioned re-seal: `implementation:dispatch --reseal <ID>` archives the packet's prior session capsule under `<ID>.sealed-at-<head>` and seals afresh on a clean tree, so a branch moving under a paused sealed build no longer deadlocks the lane (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 87)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then EM-B1f's re-seal STOP note (`$KIT/findings/EM-B1f/EM-B1f.BLOCKED-reseal.md` — the deadlock measured: `dispatch` refuses because the capsule exists, `check:packet`/`resume` refuse because its sealed HEAD drifted; the chair's hand rotation `mv <git-dir>/implementation-sessions/EM-B1f EM-B1f.sealed-at-c33446830`) and, in your worktree, WHOLE: `scripts/implementation-session.mjs` (the capsule at `:284` — `join(identity.gitDir, 'implementation-sessions', packetId)`; `dispatch.json`, `seal.json`, `state.json`, `runs/`, `run.lock`; immutability at `:79`; the refusals at `:358` and `:425`), `scripts/implementation-packets.mjs` (the `dispatch` verb, `assertTargetPreflight` at `:202-215` — `non-CREATE target must be Git-clean`; how the CLI parses its arguments), `docs/implementation/PACKET_STANDARD.md` §"Sealed dispatch and handoff lifecycle", and the session scripts' own tests (`git grep -l 'implementation-session\|implementation-sessions' -- tests` — read the nearest whole; they build capsules in fixture git dirs).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b tool-22-reseal-2026-09-20 $SP/lane-tool-22 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-22/node_modules` (no build). Scratch `$SP/lane-tool-22-scratch/`. ⛔ NEVER touch `$SP/slot-2`'s git dir or any real capsule — every capsule you create or rotate lives in a FIXTURE git dir under your scratch. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The door (two commits)
1. **`--reseal`, minimal and evidence-preserving.** `implementation:dispatch --reseal <ID>`: refuses unless a capsule exists for `<ID>` (a plain dispatch is the door when none exists); reads the capsule's `seal.json` payload head; refuses if the tree is not clean or the packet is not READY (the same preflight as dispatch — reuse it, never a second copy); ARCHIVES the capsule by rename to `<ID>.sealed-at-<short head>` (if that name exists, `-2`, `-3`… — never overwrite, never delete); then runs the ordinary dispatch, which seals afresh at HEAD. The state written into the new `dispatch.json` names the archived capsule (`resealedFrom: "<ID>.sealed-at-<head>"`) so the evidence chain is explicit. `check:packet`, `resume` and every other verb stay untouched. The CLI's usage line and the standard's "Sealed dispatch and handoff lifecycle" section gain the one sentence (docs, same commit).
2. **The fixture proof.** In the session scripts' test home: (a) dispatch → capsule exists; (b) a second plain dispatch → refused (unchanged message); (c) the fixture branch advances by one commit; `check:packet` → refused `sealed HEAD or branch drifted` (unchanged); (d) `--reseal` → the old capsule archived under its head, a new seal at the new HEAD, `resealedFrom` set; (e) `--reseal` on a DIRTY tree → refused before any rename (the archive must not happen if the seal cannot follow — prove the capsule is untouched after the refusal); (f) `--reseal` with no capsule → refused with a message pointing at plain dispatch. Counterforce: mutate the rename target to overwrite → the `-2` arm reds. Every arm prints its count.

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; the lighting census FROZEN at the tuple the chair names at dispatch (titles in the existing test home — no new file; record your delta; the walker asserts its five figures in order and stops at the first miss); `npm run validate:packets` prints the same `valid:` line before and after; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-22-scratch/TOOL-22.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-22-scratch/TOOL-22.receipt.md`: the six fixture arms with their messages; the counterforce; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.

## Addendum (the chair, 2026-09-20 13:3x — from TOOL-19's receipt; ODQ §934.47 addendum 107)
TOOL-19 landed the sealed-burn exemption reading the seal's HEAD binding but deliberately NOT `authority.packetSha256` — so today a seal still licenses a retiree burn after the packet's contract text was rewritten under it. YOUR `dispatch --reseal` is the door that must close this: a re-seal re-binds `packetSha256` to the packet at HEAD, and the validator's burn exemption (TOOL-19's `readDispatchSeal` path in `scripts/implementation-packets.mjs`) gains the arm that the seal's `packetSha256` equals the packet's sha at HEAD — refused otherwise with the plain sentence naming both. Counterforce: rewrite one byte of a sealed packet in a throwaway, the burn is refused. Also from TOOL-19: a seal licenses the burn ONLY in the worktree that dispatched it — a packet left READY with a burned retiree on a pushed branch reds CI; the charter's landing order gains "never push with a packet READY" (the chair's sentence, not yours).
