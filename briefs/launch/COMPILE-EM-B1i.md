# LAUNCH — COMPILE EM-B1i: a STANDING institution must not read as DESTROYED — `institutionDestroyed` tests the fate's declared KIND, not its truthiness (train EM-T6, right after EM-B1h, before EM-B1a)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, SIXTEEN steps). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-B1i-scratch/`.

**The defect (LIVE, pre-existing, found by EM-B1h's compile — read its version-2 draft and report WHOLE: kit `packets-waiting/EM-B1h.md`, `evidence/EM-B1h.compile.report.md`, `evidence/EM-B1h.evidence.md`):** `worldPulseFate` is stamped on institutions by thirteen write sites with eighteen values (seventeen live + `ruined_by_decree`, declared by EM-B1h). EM-B1h classifies each by what its writer DOES to the record: `closure` 15 (every one sets `_worldPulseInactive` and a non-active status) · `standing` 1 (`demoted_by_disaster`: renamed in place, no status, no flag) · `rise` 2 (`founded_by_flourishing`, `upgraded_by_reconstruction`). The ONE reader that acts on the fate, `causeLifecycle.institutionDestroyed` (`src/domain/worldPulse/causeLifecycle.js` — find it by symbol), tests the fate's TRUTHINESS — so a FLOURISHING academy, a RECONSTRUCTED guild and a DEMOTED-but-standing wizard's tower all read as DESTROYED, and a criminal leash on them SEVERS. EM-B1h is golden-neutral by design (it freezes the vocabulary and declares the kinds as data nobody reads); THIS packet is the first reader of the kind and the cure.
**The packet's outcome (the chair's ruling on the shape):** `institutionDestroyed` is true only for a fate whose declared kind is `closure` (import the kind map from EM-B1h's leaf `src/domain/worldPulse/worldPulseFates.js` — it does not exist at your tip: treat EM-B1h's §6 as applied, say so in the dependency row, and contract the exact import). An UNKNOWN fate (a value not in the vocabulary, as an old save might hold) — measure what old data can hold (EM-B1h swept it: no retired spelling in the repo's history) and contract the conservative reading: an unknown truthy fate keeps TODAY's behaviour (reads destroyed) so no old save changes meaning silently; say what that costs.
**MEASURE FIRST — this is a LIVED-BEHAVIOUR change, so its price is the point:** (1) every caller of `institutionDestroyed` and what each does on `true` (the criminal-leash severing is the one EM-B1e's pre-proof named; find them all); (2) by execution through the pulse (plain `node`; the recon harnesses in kit `tools/` drive `simulateCampaignWorldPulse` — ⚠ every write through an ABSOLUTE path; their `instrument.mjs` does `process.chdir(TREE)` at import): how often the three standing/rise fates are stamped over a bounded multi-tick run of the preset scenarios, and how often a criminal leash actually sits on such an institution — i.e. how often the bug FIRES today; (3) ⭐ THE GOLDENS: which pulse goldens, dormancy goldens and the preset witness (`tests/fixtures/preset-lighting-witness-golden.json`) would MOVE under the cure — execute the witness's scenario before and after an in-memory prototype of the cure (the override technique; never edit the tree) and count the differing records. If ANY golden moves, the packet carries the OWNER'S SIGNED DOOR (read how `tests/helpers/goldenRecordDoor.js` works: one cause per record, `predictedRows` stated before the write, executed by the chair) with the before/after in a DM's words ("the academy the town raised last season still answers to the guild's fence"); if NONE moves, say so with the execution beside it. (4) `LIFECYCLE_CLOSE_FATES` (3 of the 15 closure fates: only the ECONOMIC closures feed `priorLifecycleCounts`' damping): is that restriction DELIBERATE (read its introduction: `git log -S LIFECYCLE_CLOSE_FATES`, the commit's message and any design note) — report what you find; do NOT derive it from the kind in this packet (3 counted words → 15 would be a second lived-behaviour change): put it to the chair as a question with the measurement. (5) the `vocabularyTotality` walker row EM-B1h said this packet owes (producer → consumer), and every other register: the usual sixteen steps.
Register predictions as DELTAS (`it` titles only, in CREDITED files; never name a variable `it`/`test`/`describe`); `__BASE__` for the chair; `acceptanceCases` as `{id, case}` objects, at most eight; the §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS` (print it); any generator among `checks` LAST; requiredSymbols with the post-edit simulation; `causeLifecycle.js`'s bundle membership priced (the pulse worker's ceiling is being minted with a 4 KB headroom; is the file in first paint, the lazy engine, the zero-slack generation worker, or an edge-shared closure — SEVEN generated paths if so).
