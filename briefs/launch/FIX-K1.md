# FIX-K1 — the `in`-on-a-literal convictions in `src/` cured with `Object.hasOwn` (launch brief; the chair, session a9df403c, 2026-09-20 04:3x EDT; ODQ §934.47 addendum 52)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you — especially verb 4, PAUSE-AND-RESUME at every gated batch), then TOOL-7's report `$SP/lane-tool-7-scratch/TOOL-7.report.md` (READ-ONLY; copy any instrument you reuse into YOUR scratch): §1 (the executed primitives) and §2.2 (the convictions, each with its file:line) — that report is your finding.

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: create it — `git -C $SP/slot-2 worktree add -b fix-hasown-2026-09-20 $SP/lane-fix-k1 5a3380e8d` (the live tip; TOOL-7 measured one commit behind CURE-E, which touched no `src/` file — re-measure at YOUR base first), then `ln -s $SP/slot-2/node_modules $SP/lane-fix-k1/node_modules` (no build). Scratch: `$SP/lane-fix-k1-scratch/` (create it). FORBIDDEN: `$SP/consist` (the chair's terminal runs there), `$SP/slot-2`'s working tree (a sealed build), every other `$SP/lane-*`, the read tips, the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`. Shell state does not persist between Bash calls.

## The class
`x in LIT` where LIT is an object literal (frozen, plain, or `Object.fromEntries`) and `x` can carry a string from outside LIT's own vocabulary: a prototype name (`constructor`, `toString`, `valueOf`, `hasOwnProperty`, `__proto__`, `isPrototypeOf`, `propertyIsEnumerable`, `toLocaleString`) reads as PRESENT and `LIT[x]` hands back a function (or an object for `__proto__`). `Object.freeze` does not close that door; `Object.hasOwn(LIT, x)` does (178 uses in the tree already; `tests/lint/vocabularyTotality.walker.test.js:408-415` records the estate's first bite of this class, `complexityBandOf`). The cure is behaviour-IDENTICAL for every own key and turns a leak into an absence for prototype keys — that is the whole change. Boundary sentence: this lane cures every `src/` site TOOL-7 convicted; it touches no test-side site (TOOL-7a/7c own those) and no site whose LHS is bounded by an internal constant.

## The sites (re-measure the population at YOUR base with the report's `scanA2.mjs` copied into your scratch; report Found / changed / exempt-with-reason)
1. ⭐ THE TIER-BAND PAIR, RED-FIRST: `src/domain/traditions/genesis.js:143` and `src/domain/traditions/politics.js:99` (`if (raw && raw in TIER_INDEX) …` — `raw` is `settlement.tier`, a SAVED-CAMPAIGN string; politics's own comment says it "mirrors genesis.resolveTierBand exactly"). Write the pins FIRST, red: a settlement whose `tier` is `'constructor'` (and one with `'toString'`) resolves to the unknown/default band with a NUMBER (or the documented default) for `tierIndex`, never a function — for BOTH resolvers; quote the red. Then cure. Measure whether `politics.js` can import genesis's resolver without an import cycle (follow the imports with a small node probe); if acyclic, ONE resolver and politics calls it (the copied family collapses — say so); if a cycle, both lines cured with `Object.hasOwn` plus a cross-pin that the two agree on every key of `TIER_INDEX` and on the eight prototype names.
2. `src/domain/worldPulse/commercialReasonTaxonomy.js:138-139` — BOTH lines (`in` on 138; `?? null` on 139 does not catch a function); the JSDoc's `string | null`, "total on both sides by construction", becomes true; a pin with `'constructor'` and `'toString'` in both directions.
3. `src/lib/pulseFingerprint.js:160` — the `Object.fromEntries` accumulator: `Object.hasOwn(familyCounts, t)`; a pin that a token `'toString'` mints no key and the payload carries no function text.
4. `src/lib/flagRegistry.js:177` — `flag('constructor')` warns as unknown and reads false; a pin.
5. `src/domain/worldPulse/espionage/espionageDoctrine.js:170` — `Object.hasOwn`; a cheap pin (the producer is bounded today; the habitat is not).
6. `src/components/admin/AdminTrendsCharts.jsx:108` — `Object.hasOwn(idx, p.x)`.
7. `src/lib/constructionUsage.js:112-113` — `Object.hasOwn(deg, a)` (or seed `deg` from `Object.create(null)` — the smaller diff, and say why).
8. THE ACCUMULATOR-GUARD FAMILY — `src/domain/spatial/commodityFlow.js:500,505`, `src/domain/worldPulse/generosityKernel.js:1041,1094,1121`, `src/domain/worldPulse/routeNetworkLedger.js:554`, `src/domain/worldPulse/traditionsKernel.js:448`: `if (k in writes) continue` SKIPS A WRITE on a prototype-named key (the EM-B1k shape: a write that ghosts). `Object.hasOwn` at each; confirm each accumulator is a plain object (if any is a Map, `.has()` — and say so). One pin for the family on one representative site; the others are the same line.
9. NOT CONVICTABLE — UNTOUCHED: `src/domain/worldPulse/beliefMap.js:1127`, `src/domain/worldPulse/disinformationPlant.js:162`, `src/domain/worldPulse/espionage/espionageProducts.js:311` (the LHS is an internal frozen constant — TOOL-7's false-positive class 8). Do not "fix" them; list them as exempt with that reason.
10. `eslint.config.js:192` — `no-prototype-builtins` is `'warn'`. Measure its population at your base with eslint itself (quote the command and the count). If ZERO, flip the level to `'error'` (one line) and quote the zero — its own commit; if not zero, leave the level, list every site in your report (FIX-K2's population) and say so.

## Laws
Goldens: `shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before the first edit and after the last — identical (`7177cd6e…8f1e`, `921c51cf…db41`); `UPDATE_GOLDEN` / `GOLDEN_SHIFT_SIGNED` / every `UPDATE_*` door FORBIDDEN. No seed carries a prototype-named tier or token, so every same-seed output is byte-identical — the golden suites in your gated batch are the proof; quote them. NO BUILD in this lane (the chair measures the dist at composition). The lighting census is FROZEN at `2652·383·2269·25043·6679` (register at your base): your pins add titles — run `tests/lint/sovereigntyLightingContract.walker.test.js` ONCE, separately, record the measured tuple and YOUR delta, NEVER refreeze. Pathspec commits only — ONE commit for FIX-K1 (a second only for the eslint level); `git show --stat HEAD` names exactly your paths. The standing instruments: `tests/lint/negativeAssertionAnchor.walker.test.js` (pair every `.not.` with a liveness anchor via `tests/helpers/anchoredNegatives.js`; an `// anchored:` marker sits on the IMMEDIATELY preceding line), `tests/copy/voiceMechanics.test.js` (no em dash or `!` in a `src/` string literal), `tests/lint/mutationCoverageManifest.test.js` (a new test FILE wants a row; pins inside existing suites do not), `npx eslint` on every touched file; then `tests/lint` WHOLE once (lawful reds: the lighting census by your delta; the prose-numerics exact-identity arm for rows whose ADDRESS your edit shifted — quote them; anything else is a STOP). Every vitest line through the mutex, SHARED tier, worker-capped, ONE test directory per invocation, the two exports spelled INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN.

⛔ THE GATE IS HELD by the chair's terminal (run 19) and SIX lanes wait ahead of you. Do every ungated thing now (read whole scopes, the scanner re-run, the pins and cures written), write `$SP/lane-fix-k1-scratch/FIX-K1.lane-resume.md` — batches in order: the RED-FIRST batch is its own (the pins run red against the uncured code: keep the cures in a patch in your scratch until the red is quoted, or plant/restore — say which); then the greens + every consumer suite of the touched files (find them by import; one directory per invocation); then the instruments; then `tests/lint` whole; then the lighting walker — and end your turn with the single line `PAUSED AT THE GATE — <the note's path>`. The chair resumes you with "the gate is yours".

## Your report
The receipt (`$SP/lane-fix-k1-scratch/FIX-K1.receipt.md`) filled by execution — every claim CONFIRMED with its count line or PLAUSIBLE; the population ledger (found / changed / exempt-with-reason at YOUR base); the commit sha(s); the lighting tuple and delta; the goldens' hashes; and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
