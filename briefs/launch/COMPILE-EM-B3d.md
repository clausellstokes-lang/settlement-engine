# LAUNCH — COMPILE EM-B3d: THE IMPORT SCRUB STRIPS THE TWO EDITOR KEYS — a settlement-level drop of `dmLayer` and `decrees` in `src/lib/importScrub.js`, beside `scrubImportedTreasury`, on all three import paths; defense in depth, the deferral EM-B3a's K4 recorded (train EM-T7's first member; chartered in the charter's 2026-09-19 17:0x sequencing, "THE DEFERRED WORK, SEQUENCED"; launch by the successor chair, Fable 5.1, session 4a1823e2, 2026-09-20)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`), then the kit's 2026-09-20 addenda at the foot of `LANE-PARALLEL.md` (the directory-whole rule; the zsh warnings; the trailer ruling; the `tests/security` fixture law; the report-only-arm rule; never pipe a gate). Seat law: Fable architects, manages, judges and validates; Opus surveys, recons, verifies and implements. You are a COMPILE seat: you implement nothing, you edit no tracked file, you run no gate.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad` · kit = `$SP/chair-kit-923472dc` (READ-ONLY).
**The tree you read (and may import from with plain `node`):** ONLY `$SP/read-tip-em-t7` (detached at `ee204c827` = the slot tip after today's six picks and FIX-P1d; `node_modules` symlinked; confirm the HEAD and that `git status --short` is EMPTY at start and end). Where `LANE-EM-COMPILE-2.md` names an older read tip or an older preamble hash, THIS message replaces it: measure `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` at your read tip and cite that (the chair measured `c9f33c8d…6675`, the fourth amendment; STOP if yours differs). Scratch: `$SP/lane-em-compile-EM-B3d-scratch/` (create it). Outputs are files in your scratch ONLY (`EM-B3d.md`, `EM-B3d.manifest.json`, `EM-B3d.compile.report.md`, `EM-B3d.evidence.md`) — never into the kit, never into any worktree.

**The authority, in order (read each WHOLE):** (1) the LANDED packet `docs/implementation/packets/settlement-editor/EM-B3a.md` at the read tip — its K4 ruling, its §2 non-goals, its §12 out-of-scope note and its lifecycle table's import row are this packet's charter in miniature: the strip was DELIBERATELY DEFERRED there with the measured reason (the gallery source is already veiled by the fail-closed allowlist; the account source is an export that omits both keys), and the three import call sites are named there (`src/store/galleryImportSettlement.js`, `src/store/galleryImportMap.js`, `src/lib/accountImport.js` — confirm each BY SYMBOL at the read tip; line numbers in a landed packet are historical); (2) the charter's rows for EM-B3d and train EM-T7 (`git -C /Users/cstokes/Desktop/settlement-engine show review-fixes-2026-07-08:docs/implementation/charters/EDIT-MODE-TRAIN.md | grep -n 'EM-B3d'` — read every returned row whole; the ledger checkout is READ-ONLY, via `git show` only); (3) design §12.4 (the two keys never travel) in `docs/DESIGN_EDIT_MODE_AND_DECREES.md`; (4) `docs/implementation/preambles/EM-PREAMBLE.md` and `docs/implementation/PACKET_STANDARD.md` at the read tip; (5) the models of a small landed member for the §7 table and the §12 receipt: `EM-B3a.md` and `EM-B3c.md`.

**The packet's outcome (the chair's ruling on the shape — this is its index):** ONE behaviour family — an imported settlement arrives WITHOUT the editor's two private keys, whatever its source claims. A pure, exported, settlement-level scrub beside `scrubImportedTreasury` in `src/lib/importScrub.js` (name it in the estate's idiom — measure the sibling's spelling; prefer ONE function that drops both keys), REFERENCE-IDENTICAL when there is nothing to strip (the sibling's own contract; `tests/lib/importScrub.test.js` holds the precedent arm by title), called on ALL THREE import paths at the same seam where the treasury scrub is called. It writes nothing else, reads no world fact, consumes no PRNG, and touches neither `partializeStoreState` nor the public projection (EM-B3a owns those; they stay unmodified). No golden moves; every `UPDATE_*` door is forbidden. If the measurement shows the honest home is a different seam (for example one shared caller through which all three paths already pass), report it as a question with your recommendation — do not improvise a second shape.

**What you must MEASURE before contracting:** (1) `scrubImportedTreasury`'s body, contract and doc at the read tip, and the exact call expression at each of the three import sites (by symbol; quote each line) — and whether any FOURTH import path exists today (`git grep -n "scrubImportedTreasury" -- src` is the denominator; a path that imports a settlement without the treasury scrub is a finding in itself: report it, do not fold it in silently); (2) what `dmLayer` and `decrees` are at the read tip: every writer and reader of each key on a settlement (`git grep -nP "\bdmLayer\b|\bdecrees\b" -- src`), so the packet can state truthfully whether any shipped path can put either key on an imported settlement today (EM-B3a measured "no"; re-measure — the persona slicer's `decrees` read and the observed-shape register's `decrees on settlement` row are known); (3) the observed-shape instrument's view: whether a new READ of `dmLayer`/`decrees` in `src/lib/importScrub.js` mints a new identity or exceeds a frozen ceiling (`node scripts/check-observed-shape-readers.mjs` is plain node and read-only without `--write`; capture its exit code from the command itself, never through a pipe) — a `delete`/destructure-omit that reads no value may be the shape that mints nothing: measure both spellings on a scratch COPY, never in the read tip; (4) EM-B3a's RUNTIME travel tests (find them by title) — state which arms this packet extends and which it leaves, so the strip is proven on a settlement that really carries both keys (a hand-built fixture is lawful here; say so); (5) the byte budgets under the preamble's §P2 row 11: is `src/lib/importScrub.js` in the eager first-paint closure (import `vite.config.js`'s `EAGER_FIRST_PAINT_MODULES`) or the generation worker's closure, and is it an edge-shared INPUT (the five `supabase/functions/_shared/*.meta.json` `inputs` lists)? A member of any of the three prices its cost in the packet; (6) the lighting delta as DELTAS from your CREATE/title rows (the register at the read tip predates today's compositions — never quote absolutes); (7) the mutation-coverage register's expectation for a new exported function in `src/lib`; (8) `tests/security` governance: if any arm lands under `tests/security`, the SS4 fixture law applies.

**Budget and split:** this is a SMALL member — if the honest packet exceeds 80 effective lines of change or six files, report why before compiling further. Register predictions as DELTAS; `__BASE__` for the chair; `acceptanceCases` as `{id, case}` objects, at most six; the §7 table and the JSON manifest set-equal with actions from `PACKET_ACTIONS`; `requiredSymbols` verbatim strings that exist at the read tip; `retiredSymbols` normalised (an empty list, never `null`); `checks` as the build lane's focused commands (the import suites by file, `tests/lib/importScrub.test.js`, the travel tests, `tests/lint` whole, the observed-shape walker, the goldens plain); the Status row's value ALONE on its line. Nine questions or fewer for the chair, each with your recommendation.

**Laws:** FORBIDDEN — `$SP/consist`, `$SP/slot-2`, every `$SP/lane-*` worktree, other lanes' scratch dirs, the ledger checkout beyond `git show`, the kit (read-only), the memory directory. No edit to any tracked file anywhere. Plain `node` probes are ungated (ONE process at a time); any vitest line is GATED and PAUSE-AND-RESUME — you should need none; if you believe you do, end your turn `PAUSED AT THE GATE — <note path>` and the chair rules. Stamps from `date` in the same call. Never type a control-character escape in a tool parameter; spell control characters in words. If a command errors on a rate limit, stop and say so.

**Your report (final message; also `EM-B3d.compile.report.md`):** DRAFT (READY-able) or BLOCKED; the file paths; the measurements above with their commands and quoted output; the import-path census with its denominator; the budget table; the predicted register moves as DELTAS; the `requiredSymbols` list with the post-edit validation simulated per row; your questions with recommendations; and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work). Every claim CONFIRMED (quoted command) or PLAUSIBLE.
