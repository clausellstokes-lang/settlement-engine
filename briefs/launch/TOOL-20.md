# TOOL-20 — the 51 local comment strippers in `tests/` (27 string-blind, 11 of those also trailing-blind) hoisted onto the two canonical instruments — counters onto FIX-T2's offset-preserving `commentsOnly`, identities onto CURE-J's digest-grade `stripComments` — string-blind ones first; plus the reciprocal cross-reference sentence CURE-J's header still owes (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 86, 90, 92 — after FIX-T2 and CURE-J compose)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-T2's receipt and its ledger (`$KIT/findings/FIX-T2/FIX-T2.receipt.md`; `$SP/lane-fix-t2-scratch/local-strippers.ledger.md`, READ-ONLY — the 51 copies tagged, the 27 string-blind, the 11 trailing-blind (`^\s*//` never removes an end-of-line comment — the exact shape that bit FIX-P3), the two contracts) and CURE-J's receipt (`$KIT/findings/CURE-J/`), and, in your worktree, WHOLE: `tests/helpers/codeOnlySource.js` (`codeOnly` — blanks strings by design, 22 importers; `commentsOnly` — comments blanked in place, strings/templates/regex/JSX kept, offsets preserved; the header's contract sentences) and `tests/helpers/dossierManifest.js` (`stripComments` — digest-grade: comments removed, whitespace collapsed, `${…}` comments kept, offsets destroyed; ⛔ a prose-manifest RECORDER file: its identity is now comment-insensitive, so a docblock edit is lawful, but read CURE-J's rule 3 before touching a byte of it and never touch its code).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch — it MUST include FIX-T2's composed commits and CURE-J's `c127cdfb2` (STOP if `commentsOnly` or `stripComments` is absent): `git -C $SP/slot-2 worktree add -b tool-20-stripper-hoist-2026-09-20 $SP/lane-tool-20 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-tool-20/node_modules` (no build). Scratch `$SP/lane-tool-20-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`; a foreign stash exists in the shared tree — never touch it.

## The commits (one per batch of hoists; each proved before the next)
1. **The reciprocal sentence, and the ledger re-measured.** One docblock sentence in `dossierManifest.js`'s `stripComments` header naming `commentsOnly`, its contract and the pick-by-question rule (the mirror of FIX-T2's sentence) — a comment-only edit to a recorder file: prove `recorderShas()` is unchanged by it (run the helper before and after; the digest is comment-insensitive by CURE-J's design — this is the first live proof of that property and the receipt says so). Then re-measure FIX-T2's 51-file ledger at your tip (files may have moved; a copy may have been hoisted by another lane): the population, the string-blind set, the trailing-blind subset, and for each copy WHICH question its caller asks — a COUNT (line-addressed; offsets matter) or an IDENTITY (a digest; offsets do not) — the routing table.
2. **The string-blind counters hoisted** (the 27, counters first): each caller imports `commentsOnly` and its local stripper is deleted; for each, prove the caller's counts are IDENTICAL before and after on the real tree (a count that changes is a finding: the local copy was wrong, the estate's figure moves, and the pin's shrink-only law applies — quote it, lower the pin, never raise); the file's own tests green. Batches of ~9 by directory so each commit's gate is one directory.
3. **The remaining copies** (identities onto `stripComments`; the string-safe counters onto `commentsOnly`), same proof per caller; ⛔ INCLUDING `tests/lint/negativeAssertionAnchor.walker.test.js`'s MATCHER COUNTER (addendum 98, FIX-B2's finding): it scans RAW lines with no comment stripping, so a file that merely NAMES `not.toContain`/`not.toMatch`/`not.toHaveProperty` in a comment convicts itself against its frozen exact count (a draft comment moved `engineChunkLazy`'s count 7 → 9 with no assertion added) — route that counter through `commentsOnly` (the `// anchored:` MARKER arm stays raw by design — it counts comments on purpose; keep the two arms' inputs distinct and say so in the header); prove by counterforce: a comment naming a matcher no longer moves the count; a real bare negative still does; ALSO (addendum 101, FIX-T2's items): `tests/design/deepCraftKillList.test.js` — the precedent for the rule FIX-T2 overturned — still reads RAW lines against `CEILINGS` (the same cure, one import; its ceilings shrink-only if any match lived in a comment); `tests/helpers/sourceContract.js:245` brace-counts raw source (route the count through `commentsOnly`; prove the brace counts unchanged over the tree, or name the file where a comment brace moved them and lower the affected pin); and `significanceMigration.census.test.js` passes a SHARED `/g` regex object to `tally()` where its neighbour uses a factory and says why — make it ONE idiom (the factory; a shared `/g` object carries `lastIndex` across calls and lies the moment anyone switches to `exec`); the two left-as-partial baselines FIX-T2 named (`clampPrimitiveBaseline`, `slugifyIdiomBaseline`) included. At the end: `git grep -n -E 'replace\(/\\/\\\*\[\\s\\S\]\*\?\\\*\\//' -- tests` and its siblings print ZERO local strippers outside the two helpers — or the survivors are named with the reason (a stripper that deliberately keeps comments, e.g. an `// anchored:` scanner, is not a copy).

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line (default reporter only); eslint runs BARE; `tests/lint` WHOLE once if any hoisted file lives there (the only permitted reds: the lighting census if the register predates your base; the provenance title if swept); the lighting census FROZEN at the tuple the chair names at dispatch (no new test file; record any title delta; the walker asserts its five figures in order and stops at the first miss; never refreeze); the mutation-coverage rows of touched `tests/lint` files stay true; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-tool-20-scratch/TOOL-20.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-tool-20-scratch/TOOL-20.receipt.md`: the routing table; the `recorderShas()` proof; per hoist the before/after counts (and any pin lowered); the final grep at zero or the named survivors; every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
