# LAUNCH — REVIEW-P: the PUBLIC-PATH REVIEW — one read-only Opus walk of a new visitor's first ten minutes (ODQ §934.24 item 1, approved whole by the owner 2026-09-19; slotted "the moment run 17 exits"; written by the chair, session a9df403c, 2026-09-20)

**Seat:** an Opus REVIEW lane — read-only on the tree, findings with receipts; you cure nothing (every finding becomes a law with a walker in a later fix lane, as the 2026-09-18 dossier review did). **Chair:** Fable 5.1. Scratch: `$SP/lane-review-p-scratch/` (create it; every file you write goes here, through ABSOLUTE paths).

**The tree you walk:** a detached read-only worktree at the tip named in the dispatch message (`$SP/read-tip-<sha>`; `node_modules` symlinked — a Vite dev server runs from it). You NEVER edit, stage or commit anything in any worktree; you never run vitest, eslint, tsc or a build; you never touch `$SP/consist`, `$SP/slot-2`, any `$SP/lane-*` or the ledger checkout.

**The persona must be OFF.** The estate carries a DEV-ONLY preview persona (`src/store/authSlice.js`, find it by the symbol that reads `VITE_PREVIEW_ROLE`; `src/components/AccountMenu.jsx` renders a `PREVIEW PERSONA` badge when it is active) — an anonymous visitor's walk is meaningless under it. Start your OWN server from the read tree with the role unset — `cd "$SP/read-tip-<sha>" && VITE_PREVIEW_ROLE= node_modules/.bin/vite --port 5231 --strictPort --host 127.0.0.1` (background it; the read tree has no `.env.development.local`, so no file sets the role — verify with `ls -la .env*`), then confirm in the rendered DOM that the persona badge is ABSENT and the account menu offers Sign In; if the persona shows, STOP and report. Stop the server when you are done (`kill` your own PID only).

**The instrument:** the repo's own Playwright (`npx playwright --version`; the e2e suite under `tests/e2e/` or `e2e/` is the model for driving chromium headless — read one spec whole for the launch shape). Two viewports for EVERY step: phone 375×812 (the estate's mobile breakpoint; a touch user agent) and desktop 1440×900. Every receipt is a screenshot in your scratch (`<step>-<viewport>.png`) plus the DOM measurement that grounds the finding (computed font sizes, overflow widths, the exact copy, the console's errors and warnings captured per step). A finding without a receipt is not a finding.

**THE WALK (ODQ §934.24 item 1, verbatim in order): landing → create → the forge wait → the first dossier → the save nudge → the library → a sample fork → pricing → sign-in** — as an ANONYMOUS visitor, never signing in (the sign-in step ends at the sign-in surface; you enter no credentials). At each step record: what the visitor sees first (above the fold on both viewports), every control's copy and where it leads, every refusal or gate the anonymous visitor meets and whether its notice names the reason (§934.24 item 2: no gate refuses silently), the phone floors (prose ≥ 14 px, chrome ≥ 12 px; §934.24 item 4), horizontal overflow at 375, the painted header's phone form (§934.26: arrow + plaque + seal + Sign In only; the bottom bar Create, Library | Compendium, Gallery, About; no Realm on the phone), any console error or warning, any copy that promises what the product does not do (§934.30's honesty line for the landing's events section), any raw key or placeholder, any status word outside the tokenCase ladder, any number rendered as a bare float. Time the forge wait and say whether its pacing copy matches what the owner ruled (the 3–10 s forge is deliberate pacing; never concede speed — the positioning law). The sample fork: the anonymous daily cap does NOT apply to a curated sample (§934.24 item 3) — measure that it does not. Pricing under the purchase lock: the Founder card is absent from /create and the locked-state copy prints no price that does not resolve (§934.24 addendum).

**Also measured, because the owner's identity recon flagged it (§934.47 addendum, RECON-ID):** `settlement.id` is a hash of the SEED alone (516 of 525 corpus rows share one id) and crosses the public veil — measure who keys on it on the public path (gallery rows, realm placement, forks): can two published worlds grown from one seed collide in the library, the gallery or a fork? A reproduction with two forks of the same sample is the receipt.

**What you deliver** (`$SP/lane-review-p-scratch/REVIEW-P.report.md`; also your final message): a table of findings, each with: the step, the viewport, the receipt file, the exact copy or measurement, the law it breaks (cite the ODQ row or design section by number) or "NEW — no law yet", a severity (a visitor is misled / blocked / confused / a polish), and the SMALLEST cure shape you can name (never built here). Every claim CONFIRMED (a receipt) or PLAUSIBLE (say why you could not measure it). Then ⛔ EVERYTHING NOTICED AND NOT WALKED, each specific enough to slot — the owner's law: there is never deferred work; the chair gives every item a fate the turn it arrives. Label anything you could not reach (a route behind a real account, a purchase) as unreached with the reason.
