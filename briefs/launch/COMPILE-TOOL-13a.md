# COMPILE-TOOL-13a — the observed-shape scanner's schema-23 rung: the dark root prior admitted through the governed migration bundle, the thirteen erased rows triaged before the write, P1a carried as a second car, the stale `scanStats` re-frozen, and TOOL-13b's three refuted commits (M1 the denominator field, M2 the M12 docblock, M3 the header) carried as bundle members — COMPILED, not built (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 82, 84)

You are an Opus COMPILE lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus surveys, compiles and verifies. You COMPILE the rung — the bundle's exact members, their bytes, the triage table, the pre-proof of every claim — and you BUILD NOTHING: the mint is the CHAIR's act, on a branch cut at the integration tip, through the migration-bundle door (the register is content-addressed and history-bound: a plain `--write` goes dark, and a migration cherry-picked from a lane refuses on the consist — "genesis is not a committed ancestor"). Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then TOOL-13's report WHOLE (`$KIT/findings/TOOL-13/TOOL-13.report.md`), TOOL-13b's receipt and STOP note WHOLE (`$KIT/findings/TOOL-13b/` — §1.1 the fold: M1–M3 with the two exact refusal lines and the counter-control; the thirteen erased rows' triage table; the `minRows` 8-vs-40 candidate; the `scanStats` drift), and, in your read tip, WHOLE: `scripts/check-observed-shape-readers.mjs` (the governance branch at `:3044-3071`, `scannerToolFiles()`, `isDetectorSourcePath()`, `assertHealthyScanProvenance`, the `--report` path, M12), `scripts/lib/legacy-reader-shape-scan.mjs` (`makeResolver`, `roots.has(n.text)`, `scanReaders`'s `minRows` default), `scripts/lib/observed-shape-governance.mjs` (`assertGovernedLegacyDetectorSource` at `:127`; how a governed blob is recorded; what a migration bundle IS — its files, its manifest, the `genesis` ancestry rule), the schema-22 rung's own commit(s) as the precedent (`git log --all --grep 'schema-22' --oneline`; read the bundle it landed whole — that is the form you compile into), `tests/lint/observedShapeReaders.walker.test.js` (with TOOL-13b's erasure guard once it lands — read its arm), and `CLASS_A_PROTECTED_IDENTITIES` wherever it lives (TOOL-13b: 11 of the 13 erased rows are the single shape `stressors`, one vocabulary step from four protected rows).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR TREE: a read tip at the branch tip the chair names at dispatch (`git -C $SP/slot-2 worktree add --detach $SP/read-tip-tool13a <tip>`; `ln -s $SP/slot-2/node_modules $SP/read-tip-tool13a/node_modules`); TOOL-13's and TOOL-13b's instruments (`$SP/lane-tool-13-scratch/`, `$SP/lane-tool-13b-scratch/`) are READ-ONLY — copy what you reuse into `$SP/lane-tool-13a-scratch/`. No git mutation; `git status --short` at the read tip EMPTY at start and end. Scans run in memory or write only into your scratch (a governed `--write` is the chair's; never run it). FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every `$SP/lane-*` beyond your own scratch, the ledger checkout beyond `git show`, the kit.

## What you compile (the rung's members, each priced)
1. **The root widening.** Admit the six roots the header names (`settlement`, `worldState`, `save`, `campaign`, `pulseResult`, `wizardNews`) without a record threshold over walk roots (a walk root is thin BY CONSTRUCTION — filtering it through `minRows` is a category error). Re-execute TOOL-13's figures at your tip (`9,780 → 12,368`, +45 identity rows, the erased 13) — reproduce or explain every difference (TOOL-13b re-measured reads 128,225 / files 2,232 at c33446830).
2. **The thirteen erased rows, TRIAGED ONE BY ONE before the write** — TOOL-13b's table: `WorldPulseData.js` (institutionName, outcome, settlementIds), `heraldFeed.js` (covert, impactKind, outcome, significance), `chronicleGraph.js` (applyMode, proposalPayload on raw), `heraldRouting.js` (impactKind, outcome, section, sectionAuthority). For each: why the widening erases it (the `objects.length === 1` ambiguity guard — which second object appears), whether the read is still written in source (all thirteen are, so none is a repair), and the rung's disposition: KEPT by an explicit carry (the bundle re-asserts the row so the loss is not banked silently) or RELEASED with the reason. The `stressors` family's proximity to `CLASS_A_PROTECTED_IDENTITIES` is weighed in writing.
3. **P1a — destructuring bindings — as a second car** (+207, zero erasure per TOOL-13): the detector change, its figure re-executed, its erasure re-measured at your tip (zero or STOP).
4. **M1–M3 from TOOL-13b's fold:** M1 the `--report` denominator field computed OUTSIDE the byte-frozen blob (`assertHealthyScanProvenance` pins `reads === resolved + unresolved`), its figures taken from the rung's OWN post-widening scan, never carried forward; M2 the M12 docblock corrected (the live population DA-B2 cured; the key absent from `BUILTIN_MEMBERS`; the filter capable; 22 reads across 21 files upstream of it); M3 the resolver's header naming only the roots it binds and the mechanism. Each as exact bytes (a unified diff in your scratch), each priced 0 findings minted / 0 rows erased by construction.
5. **The `minRows` 8-vs-40 candidate:** `scanReaders`'s default 8 against the gate's 40 — behaviour, not prose; measure what changes if the gate's value becomes the default (findings, erasure, wall-clock) and RECOMMEND include/exclude with the figures; the chair rules.
6. **The `scanStats` re-freeze** (`2228/128176/9766` frozen vs live) as the rung's write, in the bundle's own form.
7. **The bundle itself:** the file list, the manifest, the `genesis` it must be cut from (the integration tip at mint), the exact chair command sequence to mint it on a branch cut at that tip, the tests that must run after (the walker whole; the plain scanner printing `exactly matching the frozen inventory`; TOOL-13b's erasure guard GREEN under the new shipped config with the thirteen dispositions honoured), and every pin that moves (the frozen inventory's hash; the `scanStats` sentinel; `assertClassADebtPreserved`).

## Your report
`$SP/lane-tool-13a-scratch/TOOL-13a.compile.md` + the diffs in your scratch: every member with its bytes and price; the triage table with dispositions; the recommendation on `minRows`; the mint sequence for the chair; every claim CONFIRMED by execution or PLAUSIBLE, labelled; the stamp read from `date`; ⛔ everything noticed and not touched, each item specific enough to slot. No build, no gate, no write. If a command errors on a rate limit, stop and say so. End your turn with the compile's path.
