# FIX-C2b — the citation walker's second act: the certification file's whole citation set by symbol; the docs-live baseline burned to zero; the absent targets measured against history; the bare `:NNN` form measured (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addenda 44, 63, 70, 72, 79)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-C2's receipt and report (`$KIT/findings/FIX-C2/FIX-C2.receipt.md`, `FIX-C2.report.md`, read-only — the walker's three arms, its roster of 29 archival files, the 47-row docs-live baseline, the 113 absent targets, the symbol arm's false-positive rate) and, in your worktree, WHOLE: FIX-C2's walker under `tests/lint/` (find it by its header; read its arms, its baseline file and the HISTORICAL-banner roster arm), `src/domain/certification/subsystemRowsWar.js` (or wherever the certification rows live — FIX-C2's report names the file), and `docs/README.md`'s index (the HISTORICAL banner convention).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b fix-citations-2b-2026-09-20 $SP/lane-fix-c2b <the branch tip the chair names at dispatch — it INCLUDES FIX-C2's two composed commits>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-c2b/node_modules` (no build). Scratch `$SP/lane-fix-c2b-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The four commits, in order (each by explicit pathspec; each proved before the next)
1. **The certification file's whole citation set by SYMBOL.** `subsystemRowsWar.js` carries ~33 further in-range stale addresses beyond FIX-C2's seven (the `warDeployment.js:1280–1291` flag-read block is off by exactly 811 after the WR-7b decomposition; `computeAllyRelief` and `composeDefenderWillScore` moved to `warCapacityReads.js:74` and `warSiegeVerdict.js:121`) — shipped certification evidence the EOF arm structurally cannot see. Re-address EVERY citation in that file by the symbol it names (the walker's symbol arm is report-only because of its false-positive rate — FIX-C2's `wiring-census.mjs:349` lesson: a true citation with a backticked neighbour was convicted; so you read each candidate's live source before changing it, and you record every citation you left alone with the reason). A pin: the file's citations resolve under the walker's arms at your tip.
2. **The docs-live baseline burned to zero, and two false exactness claims corrected.** The 47 rows across 25 documents (`GEOPOLITICAL_WAR_LAYER.md` alone 12) are re-addressed or, where the cited symbol no longer exists, re-cut to the symbol's new home or struck with a one-line note; the baseline file shrinks to `[]` (shrink-only: the arm forbids growth). `docs/DESIGN_FP_ARCH_INT.md:31` claims "(exact)" for an address off by 25 and `docs/implementation/preverification/WF-SUBSTRATE.md:192` claims "ALL EIGHT, EACH AT AN EXACT ANCHOR" while `pantheon.js:405` is off by 71 — each corrected to the measured truth or the claim withdrawn. Also in this commit, from the pre-proofs' findings: packet headers under `docs/implementation/**` cite `scripts/**` by `path:line` OUTSIDE the walker's declared roots (two off-by-ones survived in EM-R0a's header) — `docs/implementation/**` JOINS the walker's docs-live arm (measure the population first; a row that is a packet's own frozen evidence of a past tip stays under the HISTORICAL rule — say which); `vite.config.js`'s `EAGER_FIRST_PAINT_MODULES` `@consumed-by` annotation names one test while the pricing lanes now import it — widen the annotation (a comment line); the `~22.7k LOC` figure in `tests/build/domainGeneratorsBoundary.test.js:9` and any header quoting it (`git grep -n '22.7k'`) corrected to the measured figure with its stamp (`src/domain/worldPulse` was 183,678 lines / 443 files at `141a1d775`); `proseEntryContradiction:393`'s `where: factionDynamics.js:466` and `RECEIPT_POOLS_DOSSIER_STATE.md:5295`'s citation of a chain EM-R0d deletes — re-addressed by symbol (these move again when R0d lands; a symbol survives, a line does not).
3. **The 113 absent target paths measured against history** (357 citations; `SettlementMapPane.jsx` ×31, `useTownMapPresentation.js` ×28, `writers.js` ×27): for each target, `git log --all --diff-filter=D --name-only -- <path>` and `git log --all --follow` for renames — deleted, renamed, or on another branch; a citation to a RENAMED file is re-pointed; to a DELETED file, struck with the deleting commit named or moved under the HISTORICAL rule if the document is itself archival; a target on another branch is recorded by name and left (with the branch). The top three by hand; the rest by the same rule with a table in your receipt.
4. **The bare `:NNN` citation form, measure-first.** A line number inheriting its path from earlier in the sentence is invisible to the walker; FIX-C2 re-addressed one by hand. Measure the population (a sentence-scoped scan: a path literal followed within the same sentence by one or more bare `:NNN` tokens); if it is more than a handful, add a fourth arm under the same shrink-only baseline idiom; if a handful, re-address them in this commit and record the count.

## Laws
No `src/` byte outside the certification file's citation TEXT (comments and string literals only — if a citation is code, STOP and say so); goldens byte-identical (measure the two hashes before and after; the certification file may be pinned — measure). The lighting census is FROZEN (the tuple the chair names at dispatch): run the walker once separately, record the tuple and your delta, never refreeze. The directory-whole rule (`tests/lint` whole once — your edits live there); the standing instruments (negativeAssertionAnchor, voiceMechanics, mutationCoverageManifest — the walker's row stays true, eslint on every touched file); every vitest line through the mutex SHARED tier, worker-capped, ONE directory per invocation, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN; quote every path inline; a report-only arm writes with `process.stdout.write`; `git grep -E` ignores `\s` — use `[[:space:]]`; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-c2b-scratch/FIX-C2b.lane-resume.md` (the batches in order), and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
The receipt by execution: per commit the count lines; the certification file's citation ledger (re-addressed / left with reason); the docs-live baseline's before/after; the absent-target table; the bare `:NNN` population; the goldens' hashes; the lighting tuple + delta; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
