# TOOL-7a — Act 1 of the silent-failure-idiom lint: the marker-slice class cured through ONE throwing helper, Rule 5 of the anti-vacuity walker, the brace matchers made one family (launch brief; the chair, session a9df403c, 2026-09-20 04:3x EDT; ODQ §934.47 addendum 52)

You are an Opus PARALLEL TOOLING lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you — verb 4, PAUSE-AND-RESUME at every gated batch), then TOOL-7's report `$SP/lane-tool-7-scratch/TOOL-7.report.md` WHOLE (READ-ONLY; its instruments — `scanB2.mjs`, `timewalk.mjs` — are there: COPY what you reuse into your own scratch). Then, in your worktree, WHOLE: `tests/lint/contractTestAntiVacuity.walker.test.js` (its header — the "tests that lie" class and the ACCEPTED GAP at lines 38-39; `inScope`/`inFoldScope`; `codeSkeleton` at :127 with the `'>'`-after-arrow fix at :152; the `SELF` exclusion at :55/:65; Rules 1–4; the adversarial self-tests at :427-526), `tests/helpers/sourceContract.js`, `tests/helpers/codeOnlySource.js` (its header records why importing from a `.test.js` file re-evaluates that module), `tests/lint/vocabularyTotality.walker.test.js` :60-120 and every consumer of `fnBody` / `objectLiteralKeys` inside it, and `git show 9f3455b84` (CURE-E: the found-and-ordered idiom and the `declarationBody` it minted in `tests/domain/humanizeEngineTokens.test.js`; its second site in `tests/components/faithPanelModel.test.js`).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

YOUR WORKTREE: `git -C $SP/slot-2 worktree add -b tooling-7a-2026-09-20 $SP/lane-tool-7a 5a3380e8d` (the live tip — CURE-E/F/G/H are in it), then `ln -s $SP/slot-2/node_modules $SP/lane-tool-7a/node_modules` (no build). Scratch: `$SP/lane-tool-7a-scratch/` (create it). FORBIDDEN: `$SP/consist` (the chair's terminal), `$SP/slot-2`'s working tree (a sealed build), every other `$SP/lane-*`, the read tips, the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`. Shell state does not persist between Bash calls.

## The chair's rulings (ODQ §934.47 addendum 52) — they bind you
- **R1 — one throwing helper, every live B1 site through it.** In `tests/helpers/sourceContract.js`: `spanBetween(src, startAnchor, endAnchor, { label })` (match the file's vocabulary if a better name lives there — say why): THROWS when either anchor is absent (`indexOf < 0`) or inverted (`end <= start`); never returns `''`; the walker's Rule 1b `EXTRACTOR_CALLEE_RE` learns its name. RE-MEASURE the B1 population at YOUR base first with a splitter that runs on the STRIPPED source and balances parentheses (the report's own splitter mis-read `tests/domain/peaceTermsGrantTerms.test.js:510` because its anchor literal contains a paren — false-positive class 9); report Found / changed / exempt-with-reason. Re-route EVERY live B1 site through the helper — the report's §3.3 list (`tests/edgeFunctions/surveyorByok.test.js` :50 :138 :165 :174 :633; `tests/edgeFunctions/aiProviderAbstraction.test.js` :234 :286; `tests/security/byokNeverLogged.test.js:89`; `tests/ui/uiA11yWave5.test.jsx:189`; `tests/domain/aiAnalyst.test.js:461`; `tests/domain/razingWitnessWr8.test.js:439`; `tests/domain/simulationRulesPreset.stability.test.js:637`; `tests/domain/peaceTermsGrantTerms.test.js:510`; `tests/store/autoplacementStore.test.js:101`; `tests/components/arrowHeader.test.jsx:235`; `tests/domain/interview.test.js:172`; `tests/lint/postureNameCollision.walker.test.js:246`; `scripts/check-observed-shape-readers.mjs:2090`) plus anything your re-measure finds — AND CURE-E's two cured sites, retiring its `declarationBody` (the third spelling of one job). If a re-routed test THROWS at its green run, that test was passing vacuously: a finding — quote it, fix the ANCHOR (never weaken the assertion), name it in your report.
- **R2 — the brace matchers become one family, throwing and strip-aware.** `fnBody` and `objectLiteralKeys` move into `sourceContract.js`: unbalanced input throws, a missing declaration throws, and they count braces on `codeSkeleton`'s output, never raw text (the file's own header promises exactly that); `statementWindowAt` (:245) gets the same strip. The six hand-rolled matchers collapse to the family: `fnBody` ×3 (`vocabularyTotality.walker.test.js:75`, `tests/lib/saves.metaProjection.test.js:30`, `tests/lib/saves.galleryOptIns.test.js:33`), `objectLiteralKeys` ×2 (`vocabularyTotality:88`; `tests/store/savedSettlementPatchKeysWalker.test.js:168` has a different signature — reconcile, or keep it with a written reason), `declarationBody` ×1. `vocabularyTotality.walker.test.js` IMPORTS them and its `export` of the two goes away (zero importers today — measured by TOOL-7; re-measure). If any of its arms depended on a silent-empty return, that is vacuity: quote it and cure it there. To make `codeSkeleton` importable without importing a test file, MOVE it into `tests/helpers/codeOnlySource.js` as a second, DECLARED export (the strict strip: comments, strings, templates AND regex literals), the walker importing it from there; `codeOnly`'s bytes and behaviour UNCHANGED (23 importers). Write both purposes in that file's header — the chair closed TOOL-7's Q6 as "two strips with declared purposes in one file", not as a convergence.
- **R3 — Rule 5.** In `contractTestAntiVacuity.walker.test.js`: convict any `.slice(` whose ARGUMENT EXPRESSION contains two DISTINCT finder calls (`indexOf(` / `lastIndexOf(` / `search(`) — the B1 shape — over a NEW, DECLARED scope for Rule 5 only: `tests/**` + `scripts/**` (the file's other rules keep their scopes; the header's own words at lines 78-80 are the reason: a rule that cannot see the directories where its class occurred is vacuity one level up). EMPTY allowlist — the file's law is that a rule lands only against a triaged population, which is R1. The shape is parsed on `codeSkeleton`'s output with a balanced splitter; the `SELF` exclusion applies; the message names `spanBetween` as the cure and CURE-E `9f3455b84` as the precedent (NOT `tradeRouteSemantics.test.js`, whose `match`+anchors form is a different, sound idiom). THREE adversarial self-tests in the file's existing pattern: it FIRES on a planted inline two-finder slice; it CLEARS on the bound-and-asserted form; it CANNOT BE FORGED from the shape quoted inside a string or a comment (join the existing test at :514 if it generalises). No new mutation row — the file's row is `self-proving-meta`; the three self-tests ARE its mutation proof. Rule 5's red-first is its own gated batch: plant one inline two-finder slice in one real in-scope file, run the walker, quote the red naming the file:line, restore with the inverse edit. B2/B3/B4 (the single-anchor shapes, 93 sites) are NOT Rule 5's — they are TOOL-7b's; do not widen.
- **R4 —** the walker's OWN three gates at `:357, :369, :382` (`!(key in EXEMPT…)`) → `Object.hasOwn`, so TOOL-7c's arm A can land later without this file redding itself.
- **R5 — budget.** `tests/lint/negativeAssertionAnchor.walker.test.js:670` holds an EXACT-EQUALITY `.not.` count for `vocabularyTotality.walker.test.js`: any `.not.` you add anywhere carries its `// anchored:` marker on the IMMEDIATELY preceding line (a wrapped marker reads as un-anchored and moves a frozen row — CURE-E's commit records it).

## Laws
Goldens untouched (`shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before and after; `7177cd6e…8f1e`, `921c51cf…db41`). NO `src/` edit (if one is needed, STOP and say which). TWO commits by pathspec: (A) the helper family + `codeSkeleton`'s move + the re-routed sites; (B) Rule 5 + R4. `git show --stat HEAD` names exactly your paths each time. The standing instruments (negativeAssertionAnchor, voiceMechanics, mutationCoverageManifest, eslint on every touched file), then `tests/lint` WHOLE once (lawful reds: the lighting census by your delta; prose-numerics address rows your edits shifted, quoted), then the lighting walker ONCE separately (FROZEN `2652·383·2269·25043·6679`; record the tuple and your delta; NEVER refreeze). Every vitest line through the mutex, SHARED tier, worker-capped, ONE test directory per invocation, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN.

⛔ THE GATE IS HELD by the chair's terminal (run 19) and six lanes wait ahead of you. Do everything ungated now (reading, the re-measure, the helper, the re-routes, Rule 5 and its self-tests written), write `$SP/lane-tool-7a-scratch/TOOL-7a.lane-resume.md` (batches in order: the re-routed sites' own suites, one directory per invocation — `tests/edgeFunctions`, `tests/security`, `tests/ui`, `tests/domain`, `tests/components`, `tests/store`, `tests/lib`, `tests/lint`; commit A; Rule 5's red-first plant; the green; commit B; the instruments; `tests/lint` whole; the lighting walker), and end your turn with the single line `PAUSED AT THE GATE — <the note's path>`. The chair resumes you with "the gate is yours".

## Your report
The receipt (`$SP/lane-tool-7a-scratch/TOOL-7a.receipt.md`) by execution — every claim CONFIRMED with its count line or PLAUSIBLE; the B1 population ledger at your base; the commit shas; the lighting tuple and delta; the vacuity findings (tests that threw once made honest); and ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
