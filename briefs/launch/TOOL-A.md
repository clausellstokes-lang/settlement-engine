# LAUNCH — TOOL lane A: TOOL-1 (the validator compares §7 with the JSON change manifest) + TOOL-2 (the dist-gated silent skip: MEASURE AND PROPOSE ONLY)

Read `briefs/LANE-PARALLEL.md` (chair kit) WHOLE and obey it. Worktree, branch and base are named in the dispatch message. Scratch: `$SP/lane-tool-a-scratch/`. Lane id: `TOOL-A`.

## TOOL-1 — build it
**The hole (measured at EM-B1d's pre-proof):** a packet's Markdown §7 change table named EIGHT files while its JSON `changeManifest` in `docs/implementation/PACKET_MANIFEST.json` named FIVE, and `validate:packets`, `check:packet` and the sealed dispatch all passed — the seal reads the JSON, a human and a build lane read the Markdown, and nothing compares them. **The chair's ruling on the shape:** the packet validator (find it from `package.json`'s `validate:packets` script; read it whole) gains ONE arm — for every packet whose status is NON-TERMINAL (`DRAFT`, `READY`, `STALE`; the validator already knows `TERMINAL = {LANDED, SUPERSEDED}`), the set of paths in the Markdown §7 table must EQUAL the set of `changeManifest` paths, and each path's action (CREATE / MODIFY / TEST / DELETE — use the validator's own vocabulary) must agree. Terminal packets are EXEMPT (history is not re-litigated; a LANDED packet's table may cite moved files). 
**Measured first (`<scratch>/TOOL-A.evidence.md`):** how many packets in the whole estate (every family under `docs/implementation/packets/`) carry a §7 table the arm can PARSE, by status; what table shapes exist (column order, backticked paths, a path with a parenthetical, a row spanning several paths, generated artifacts listed in prose instead of rows); exactly which non-terminal packets at your base would RED under the arm and why. The arm must land GREEN on the estate as it stands: if a non-terminal packet at your base genuinely disagrees with its manifest, that is a FINDING for the chair (report it; do not edit the packet) and the arm ships with that packet fixed by the chair first or, if the chair so rules on resume, a dated waiver row — never a silent skip. A §7 table the arm cannot parse in a NON-TERMINAL packet is a RED with a message naming the packet and the row (an unparseable table is how the hole re-opens).
**Red-first:** the validator's own test suite (find it) gains a fixture packet whose §7 names a path absent from its manifest, and one whose action disagrees — both must fail BEFORE the arm exists (quote the red) and the arm's message must name packet, path and side. Extend the EXISTING test file where one exists (a new test file moves the lighting census and may owe a mutation-coverage row — state your deltas either way).
⚠ `PACKET_STANDARD.md` / `PACKET_TEMPLATE.md`: if the standard does not already SAY that §7 and the JSON manifest must agree, add the one sentence to the standard (docs are in your scope for this lane) and name it in the commit.

## TOOL-2 — MEASURE AND PROPOSE; build nothing until the chair rules
**The hazard:** the build tests guard themselves with `describe.runIf(DIST_EXISTS)` (find every spelling): with no `dist/` they SKIP and the run prints green unless `VERIFY_DIST=1` — a lane that names a build test in a focused run without having built reads a skip as a pass. **Measure:** every dist-gated suite (file, number of tests gated), the helper(s) that compute `DIST_EXISTS`, where in `npm run check`'s order the unit step meets these files (before or after the build?), what `verify:dist` sets, what a focused run prints today with and without `dist/` (read the reporter's output shape from a run you are entitled to make in your gated window — or reason from the code and mark it PLAUSIBLE). **Propose** the smallest cure under which (i) `npm run check` on a clean tree stays green with no `package.json` byte changed (⛔ any `package.json` byte is a MINT TRIGGER in this estate — a cure that needs one must say so loudly and price it), (ii) CI is unaffected, and (iii) a lane that NAMES a dist-gated file in a focused run with no `dist/` gets a RED or an unmissable non-green it cannot quote as a count line. Give two candidate shapes with their costs and your recommendation; then PAUSE (`PAUSED FOR THE CHAIR'S RULING — <path to the proposal>`), exactly as you would at a gate.

**Commits:** TOOL-1 is one commit: `TOOL-1: validate:packets compares a non-terminal packet's §7 table with its JSON change manifest — <N> packets parsed, 0 red at the base`. TOOL-2, once ruled, is its own commit.
