# FIX-T2 — source-text ratchets that count their own literal in prose: `loadingNarrationRatchet.test.js` counts `fallback={null}` and `Loading` across raw file text, so a COMMENT reds the gate; the counters read code, not comments (launch brief; the chair, session a9df403c, 2026-09-20; ODQ §934.47 addendum 83 — FIX-P3's hand-off; TOOL-7's family)

You are an Opus PARALLEL BUILD lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its verbs bind you, with every 2026-09-20 addendum at its foot), then FIX-P3's receipt item (c) (`$KIT/findings/FIX-P3/FIX-P3.receipt.md`: a comment naming `fallback={null}` cost the lane a cycle) and TOOL-7 / TOOL-7a's findings (`$KIT/findings/TOOL-7/`, `TOOL-7a/` — the marker-slice family this belongs to) and, in your worktree, WHOLE: `tests/lint/loadingNarrationRatchet.test.js` (its `walk`, `count`, `bareLoadingRe`, `nullFallbackRe`, the two pins `BARE_LOADING_PIN = 32` and `NULL_FALLBACK_PIN = 38` with their LOWERED history and the BELOW arm that banks a win), and the estate's comment/string helpers (`git grep -n 'codeOnly\|stripComments\|withoutComments' -- tests scripts` — FIX-P3 found that `codeOnly` BLANKS STRING CONTENTS by design; read its home whole, because the `Loading` regex matches a STRING prefix and must survive the stripping while comments must not).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19  (READ-ONLY; scratch copy `$SP/chair-kit-923472dc`)

YOUR WORKTREE: cut at the branch tip the chair names at dispatch: `git -C $SP/slot-2 worktree add -b fix-ratchet-comments-2026-09-20 $SP/lane-fix-t2 <tip>`; `ln -s $SP/slot-2/node_modules $SP/lane-fix-t2/node_modules` (no build). Scratch `$SP/lane-fix-t2-scratch/`. FORBIDDEN: `$SP/consist`, `$SP/slot-2`'s working tree, every other `$SP/lane-*`, the read tips, the ledger checkout, the kit. Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`.

## The two commits, in order
1. **MEASURE THE POPULATION, then cure the one ratchet.** (a) How many of the 32 + 38 counted matches today sit INSIDE comments (line or block) — per file, quoted; if any do, the shipped pins are counting confessions that do not render, and the honest count is lower. (b) Make both counters read CODE ONLY: comments stripped, string and JSX text KEPT (the `Loading` arm matches `['"\`>]Loading` — a string or JSX-text prefix — so a helper that blanks strings is WRONG here; either use the estate's helper in a comments-only mode if it has one, or write the smallest comment stripper that handles `//`, `/* */` and JSX `{/* */}` and leaves template literals, strings and regex literals intact — prove it on a fixture that contains each shape). (c) If (a) found comment-resident matches, LOWER the affected pin to the measured code-only count — shrink-only, banked with a LOWERED line in the pin's own history idiom ("LOWERED N → M on 2026-09-20 by FIX-T2: k matches lived in comments"); never raise. (d) RED-FIRST by counterforce: plant a comment `// fallback={null}` in a throwaway copy of one `src/` file, run the ratchet before your cure (red: the count rises past the pin — quote it), after (green: the comment is invisible), restore the exact SHA-256.
2. **THE SWEEP — every other source-text ratchet in `tests/lint` that counts a literal over raw file text.** Enumerate the denominator: `git grep -n -E 'readFileSync\([^)]*\)\.match\(|\.match\((mk|make)?[A-Za-z]*Re\(\)' -- tests/lint` and every `tests/lint/*Ratchet*.test.js`; for each counter, say whether comments can reach it (a regex over raw text with no stripping = YES) and whether any current match sits in a comment (measured). Cure the ones that count RENDERED or BEHAVIOURAL literals the same way (comment-stripped); LEAVE the ones that deliberately count comment markers (an `// anchored:` scanner counts comments by design — say which). A ledger: found N counters; cured k; left j with reasons.

## Laws
Not one byte under `src/`. Goldens byte-identical (hash before/after). Every vitest line through the mutex SHARED tier with the two exports INLINE (`GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`), ONE directory per invocation, a printed count on every line; `tests/lint` WHOLE once (the ONLY permitted red is the lighting census — FROZEN at the tuple the chair names at dispatch; run it once, record your delta, never refreeze; you create no test file — a fixture for the stripper lives inside the test or under `tests/fixtures/`, and a fixture file is not a test file, say which); the mutation-coverage row of any touched `tests/lint` file stays true (read the manifest's row for each file you edit); eslint on every touched file; quote every shell expansion (`"$VAR"`); `git grep -E` ignores `\s` — use `[[:space:]]`; stamps from `date` in the same call; the trailer your harness prescribes. PAUSE-AND-RESUME: do everything ungated, write `$SP/lane-fix-t2-scratch/FIX-T2.lane-resume.md`, and end your turn `PAUSED AT THE GATE — <the note's path>`; the chair resumes you with "the gate is yours".

## Your report
`$SP/lane-fix-t2-scratch/FIX-T2.receipt.md`: the comment-resident population per pin; the stripper's fixture proof; the counterforce quoted (red past the pin, green after); the pins' before/after; the sweep ledger (N / k / j with reasons); every count line; the commit shas; ⛔ everything noticed and not touched, each item specific enough to slot.
