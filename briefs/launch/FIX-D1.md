# LAUNCH — FIX-D1 (MEASUREMENT FIRST): the dangling references a generated town ALREADY carries, and one name spelled two ways (an Opus RECON lane; READ + plain `node` ONLY; cures that move a golden are the OWNER's door)

**Seat:** Opus RECON. You measure, classify and propose; you edit, stage and commit NOTHING anywhere; no vitest, eslint, npm scripts or builds; plain `node` on scratch scripts, ONE process of yours at a time. `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`. **Tree (READ + IMPORT ONLY):** named in the dispatch message. **WRITE ONLY** under `$SP/lane-fix-d1-scratch/`, through ABSOLUTE paths (the kit's `instrument.mjs` copies `process.chdir(TREE)` at import). FORBIDDEN: every other lane's directory, `$SP/consist`, `$SP/slot-2`, the ledger checkout's working files.

**The measured ground (read both whole):** kit `findings/RECON-ID/RECON-ID.report.md` (Q2: the handle census; the pre-existing dangles) and kit `evidence/EM-R6.compile.report.md` + `packets-waiting/EM-R6.md` §5–§6 (the institution cascade's compile, which needed a dangling-join BASELINE to gate against and therefore counted what a freshly generated town already carries): **7,399 dangling institution references over the 525-row corpus — medians thorp 11 · hamlet 11 · village 8 · town 15 · city 23 · metropolis 18 — concentrated in five paths:** `resourceAnalysis.resourceChains[].processingInstitutions[]` 2,885 (10.2 % of its values match the roster) · `economicState.activeChains[].processingInstitutions[]` 2,525 (76.0 %) · `resourceAnalysis.exploitation.fullyExploited[]…` 869 (16.4 %) · `…partiallyExploited[]…` 699 (18.2 %) · **`availableServices.criminal[].institution` 421 (67.2 %) — the different one: a scalar reference, not a loose list.** And one name spelled two ways: `"Thieves' Guild"` 88 times beside the catalogue's `"Thieves' guild"`. The harness is in kit `tools/em-r6/` and `tools/recon-id/` — copy and re-run at your tip; every figure you quote is one you re-measured.

## Measure, per path
1. **WHAT the dangling value IS:** for each of the five paths, the producer (module, symbol), and what the non-matching values are — a catalogue label for an institution TYPE the town does not have (a statement of what COULD process the resource: then the field is a catalogue list, not a reference, and "dangling" is the wrong word), a name that was on the roster at an EARLIER pipeline step and was removed or renamed by a later pass (a real stale reference), a case or punctuation near-miss of a roster name (a join that fails by accident), or something else. Classify EVERY non-matching value over the corpus into those classes with counts (a bounded, cached 525-row run).
2. **WHO READS each path, and what a DM SEES:** which tab, PDF section, AI context or export renders it; for one concrete town per class, the sentence or list a DM meets and why it is wrong (or why it is fine). A catalogue list rendered as "processed by: X, Y" when the town has no X is a reader-visible falsehood even if it is not a broken join.
3. **`availableServices.criminal[].institution`, separately:** 421 services crediting an institution the town does not have — which generator writes them, from what (a faction? an absent guild?), and what the Services tab shows.
4. **The case near-miss:** every producer that spells `"Thieves' Guild"`, what joins on it, whether the two spellings ever co-exist in ONE town, and what a rename or a removal of the catalogue-spelled institution would leave behind. Sweep the catalogue for OTHER case/punctuation near-misses the same way (a normalised-name collision scan over every institution name the generator can emit).
5. **What each cure would MOVE:** for every class you judge a real defect, the smallest cure at its producer, and by execution where plain `node` allows: how many of the 525 generator-golden rows change, whether any prose-manifest row changes, whether a saved world persists the value (old saves keep it). A cure that moves a golden goes through the owner's signed door — give the owner the before/after in a DM's words and the row counts.

## Report (final message; also `$SP/lane-fix-d1-scratch/FIX-D1.report.md`, written early, re-written per section)
Outcome first: of 7,399, how many are REAL stale references, how many are catalogue lists mis-read as references, how many are near-miss joins. Then sections 1–5. Then the proposal, split into (A) cures that move no golden — buildable by a parallel fix lane at once, (B) cures that move goldens — for the owner's door, each with its before/after sentence, (C) reclassifications (a path that is a catalogue list should be DECLARED one, so the institution cascade's totality walker and the merge's referential invariants stop counting it as a join). Noticed-not-touched, each item specific enough to slot. Every claim CONFIRMED (quoted command + output) or PLAUSIBLE. Do not adjudicate; do not build.
