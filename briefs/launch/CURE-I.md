# CURE-I — run 19's one red: EM-B3c's walker plants synthetic migration names that the SS4 meta walker reads as references; the cure is a DECLARED, EXECUTED synthetic-name marker (launch brief; the chair, session a9df403c, 2026-09-20 04:4x EDT)

You are an Opus CURE lane for SettlementForge (chair: Fable 5.1, session a9df403c). Seat law: Fable architects, manages, judges and validates; Opus implements and verifies. Read `$KIT/briefs/LANE-PARALLEL.md` WHOLE first (its laws bind every lane), then, whole: `tests/security/migrationRefIntegrity.meta.test.js` (the SS4 meta walker), `tests/security/galleryScannerMirrorTotality.test.js` (EM-B3c's walker, landed `ae4a643f7`), `tests/helpers/anchoredNegatives.js` and the header of `tests/lint/negativeAssertionAnchor.walker.test.js` (how the estate reads a line marker), and `scripts/mutation-sweep.sh`'s area 12 (the meta walker's mutation proof).

SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad
KIT=/Users/cstokes/Desktop/settlement-engine-kits/chair-kit-923472dc-2026-09-19

## The red (run 19 = train EM-T3's terminal at `5a3380e8d`; 33,595 tests, ONE failed)
`tests/security/migrationRefIntegrity.meta.test.js` :: "every referenced migration file exists under supabase/migrations/" — `expected [ …(5) ] to deeply equal []`: `tests/security/galleryScannerMirrorTotality.test.js` references `089_a.sql` (×1), `202_b.sql` (×2) and `1000_z.sql` (×2), none of which exists. They are that walker's own SORT FIXTURES — the "net-current owner under both sorts" self-test, where numeric order must beat lexical order, which needs a four-digit name the directory does not have — not `runIf` gates. The meta walker scans every `tests/security/*.test.js` for `NNN_….sql` literals (`MIG_REF_RE`) and requires each to exist; it excludes only ITSELF (its docblock: "its detector self-test below plants a deliberately-fake literal"). The product is sound; migration 203's pin is intact; two instruments disagree about what a quoted literal means. Reproduce it first at your base and quote the five names.

## The ruling (the chair; vetoable) — a synthetic fixture is DECLARED, and the declaration is EXECUTED
NOT: computing the names to dodge the regex (the meta walker's own declared CANNOT-CATCH gap — an evasion). NOT: a second file-level exclusion in the meta walker (a blanket exemption behind which a real vacated gate could hide). NOT: replacing the fixtures with real filenames (the sort property needs the four-digit name). Build:
1. **The meta walker learns a line marker.** Match the estate's marker idiom — read how `// anchored:` is placed (the IMMEDIATELY PRECEDING line) and read by the anchored-negatives walker, and use the same placement; name it `// synthetic-migration-name: <reason>`. Gathering stays as today; a literal whose line is marked is a SYNTHETIC ref: EXCLUDED from the existence check AND asserted NOT to exist — a marked literal that names a real migration is a RED with its own message ("the marker lies: a real migration hides behind a synthetic marker"). The synthetic set is asserted non-empty whenever any file carries the marker (liveness), and the existing "non-vacuous" floors stay. THREE adversarial self-tests in the file's existing style: an unmarked fake fires (today's behaviour, kept); a marked fake clears; a marked REAL name fires with the lie message. The detector self-test's planted literal stays as it is (self-exclusion by file, as today). Pair every new `.not.` with its `// anchored:` marker on the immediately preceding line.
2. **B3c's walker marks its fixtures.** Mark the lines carrying `089_a.sql`, `202_b.sql`, `1000_z.sql` with the marker and the reason (sort fixtures; numeric-vs-lexical), or hoist the three names to ONE marked const if that is the smaller, clearer diff — say which. No assertion changes; no title changes there unless unavoidable (say so).
3. **The registers.** Both files hold rows in `scripts/mutation-coverage-manifest.json` (B3c's walker's row; the meta walker's proof is mutation-sweep area 12 — keep it true: run the plant's command and quote that it still reds). `tests/lint/mutationCoverageManifest.test.js` green.

## Where it lands
The slot `$SP/slot-2` is SEALED by EM-B1k2's build (its batches are running on the gate now). You work in YOUR OWN worktree first: `git -C $SP/slot-2 worktree add -b cure-i-2026-09-20 $SP/lane-cure-i 5a3380e8d`; `ln -s $SP/slot-2/node_modules $SP/lane-cure-i/node_modules` (no build); scratch `$SP/lane-cure-i-scratch/` (create it). Prove the cure there — the gated batch: the two files' suites; `tests/security` WHOLE once (the directory the meta walker governs; ONE directory per invocation); `tests/lint/mutationCoverageManifest.test.js`; `tests/lint/negativeAssertionAnchor.walker.test.js`; `npx eslint` on both files; `tests/lint/sovereigntyLightingContract.walker.test.js` ONCE separately (your self-tests add titles — record the measured tuple and your delta against the FROZEN `2652·383·2269·25043·6679`; NEVER refreeze). Then ONE commit on YOUR branch by explicit pathspec of the two files (`CURE-I: …`, the body carrying every count line and the five names), `git show --stat HEAD` naming exactly them. Then `git format-patch -1 --stdout HEAD > $SP/lane-cure-i-scratch/CURE-I.patch` with its sha256, write `$SP/lane-cure-i-scratch/CURE-I.lane-resume.md` (your commit sha, the patch path + sha256, the exact commands to land and re-prove in the slot), and end your turn with the single line `PAUSED FOR THE SLOT — <the note's path>`. When EM-B1k2 has LANDED the chair says "the slot and the gate are yours": then in `$SP/slot-2` ON `fixes-2026-09-18-consist` (verify `git status --short` is EMPTY first — if not, STOP and report), `git cherry-pick <your sha>` onto the branch, re-run the two suites + `tests/security` whole there (shared tier), confirm `git show --stat HEAD` names exactly the two files and `git status --short` is empty, and report the slot's sha. B1k2 touches neither file; if the cherry-pick conflicts anyway, STOP and report.

## Laws
The gate is FREE but SHARED — EM-B1k2's batches run beside you on the shared tier by the chair's judgment. Every vitest line through the mutex, SHARED tier, worker-capped, the two exports INLINE: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 <explicit files>`; a line with no printed count DID NOT RUN. NEVER `npm run check`, `npm run test`, `test:ratchet`. Goldens untouched (`shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` before and after; `7177cd6e…8f1e`, `921c51cf…db41`). No `src/` edit. FORBIDDEN: `$SP/consist` (the chair's terminal), `$SP/slot-2`'s working tree UNTIL the chair's word, every other `$SP/lane-*`, the read tips, the ledger checkout `/Users/cstokes/Desktop/settlement-engine`, the kit (read-only). Never `git add -A`/`-u`/`.`, stash, reset, clean, rebase, amend, `--no-verify`, push, `--ignore-other-worktrees`. Stamps are read from `date` in the same call.

## Your report
The receipt by execution: the red reproduced at your base with the five names quoted; the green; the three self-tests' count lines; `tests/security` whole; area 12's plant still redding; eslint 0; the lighting tuple + delta; the goldens' hashes; your commit sha (and later the slot's); ⛔ everything noticed and not touched, each item specific enough to slot (the owner's law: there is never deferred work).
