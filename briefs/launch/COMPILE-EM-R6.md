# LAUNCH — COMPILE EM-R6: an INSTITUTION gets the rename cascade a faction already has, and a REMOVAL sweeps the same list (MOVED UP: it lands BEFORE EM-B1c, whose institution rename op has no cascade to call; design §22.1 ruling 7, §22.2 item 10)

Read `briefs/LANE-EM-COMPILE-2.md` (chair kit) WHOLE and obey it (it binds `LANE-EM-COMPILE.md` and `LANE-EM-PREPROOF.md`, THIRTEEN steps). The tree you read is named in the dispatch message. Scratch: `$SP/lane-em-compile-EM-R6-scratch/`.

**The measured ground — read it WHOLE first:** kit `findings/RECON-ID/RECON-ID.report.md` (an Opus recon over the full 525-row corpus, 2026-09-19) and its executable census in kit `tools/recon-id/` (`census.mjs` is the DENOMINATOR: copy it to your scratch and re-run it at your tip; every figure you quote is one you re-measured). What it found: FACTIONS are already total — 11 stored handle paths, all on `src/domain/factionRename.js`'s cascade, 0 of 15 handles stale after a rename on real pipeline data; NPCs — 4 of 6 paths cascaded, 2 stale BY WRITTEN DECISION; ⛔ INSTITUTIONS — **42 stored paths hold an institution's exact name (33 strict + 9 polysemous) and NO institution rename or removal writer exists anywhere**: renaming "Mages' guild" leaves 9 of 10 handles stale (Services still lists its two magic services, Defense still credits it, two NPCs' postings and their faction member chips still read it, the quarter's landmarks name it, a supply chain's processor and a trade dependency name a house no longer on the roster) — nothing throws, nothing logs; REMOVING one institution created 9 new dangling joins. Mean 2.57 strict handles per institution, max 10; 98 % of institutions are referenced elsewhere.
**The packet's outcome (the chair's ruling on the shape):** `src/domain/institutionRename.js` (propose the name by the estate's idiom) MODELLED ON `factionRename.js` — read that module whole and copy its architecture exactly: ONE declared surface list (each row: the record path, how the name is matched, how it is rewritten), a pure `institutionRenameChanges(settlement, oldName, newName)` that returns the change set, an apply function, and a `NON_CASCADED` ledger with a written reason per row. (1) The 33 STRICT paths are cascade rows. (2) The 9 POLYSEMOUS paths (a string that may or may not be the institution: 492 roster matches against 7,840 non-matching) each need a per-path ruling — put EACH to the chair with its counts and your recommendation (cascade with an exact-match guard · NON_CASCADED with reason); two of them are RECEIPTS and take the ruling `simulationTrace[].causes[].reason` already has — find that ruling and cite it. (3) `generators/factionRoles.js` stores an institution NAME in `linkedInstitutionIds` and its own comment calls it rename-sensitive — a cascade row. (4) MIRRORS: `factions[].members[].institution` mirrors `npcs[].institution` (design §22.2 item 7: a mirror is recomputed, never cascaded twice) — say how the cascade and the mirror recompute compose so the roster copy and the member-chip copy cannot disagree after a reload (the recon saw them disagree). (5) **REMOVAL SWEEPS THE SAME LIST:** `institutionRemovalChanges(settlement, name)` — for each surface row, what removal does: drop the entry (a service offered only by that house), null the reference, or REFUSE (a supply chain whose only processor is that house — is that the op's guard's business, EM-C2's?); measure each row's removal semantics on real records and put the ones that are not obvious to the chair. (6) THE GATE IS A DELTA BY KIND: generated records already carry dangling joins (town median 17, city median 23, concentrated in five paths) — the packet's acceptance is "a rename or a removal adds ZERO new dangling joins of any kind", measured against the record's own baseline, never an absolute. (7) ⚠ THE ANCHOR: 12 % of institutions (2,083 of 17,361 — every one the cascade pass or the coherence repair adds AFTER the `catalogId` stamp) anchor on `name:<slug>`, so a rename MOVES their map anchor (`anchorKey`: map pins, interior deltas, fog sessions, the glyph seed — 20 consuming modules). The OWNER is being asked whether to re-stamp `catalogId` after the last roster mutation (a separate, owner-gated packet). Compile EM-R6 so it is CORRECT UNDER BOTH ANSWERS: under YES nothing more is needed; under NO the cascade must carry the `anchorKey`-keyed edits from the old anchor to the new one — measure what that would take (which stores hold `anchorKey`-keyed state, whether they are inside the settlement record or beside it in the save) and write it as a CONDITIONAL section the chair can cut.
**It adds no persisted key and moves no golden** (it is a function nobody calls until an op does) — prove the golden-neutrality by the import graph (who would import it: EM-B1c's rename op, EM-B1a's removal op, EM-R0c's merge for the removal sweep) and price its bytes against every ceiling by PLACEMENT (it must not enter the zero-slack generation worker or first paint; `factionRename.js`'s own placement is the model — measure where that module sits today).
Tests: the cascade's totality walker (every path in the census that holds an institution name is either a cascade row or a NON_CASCADED row with a reason — the census is generated at test time over a corpus stride, so a NEW path reds until it is listed; say the stride and its wall-clock) and executed rename + removal arms on real pipeline records with the delta-by-kind gate. Register predictions as DELTAS; `__BASE__` for the chair; §7 table and JSON manifest set-equal with actions from `PACKET_ACTIONS` (print it); requiredSymbols with the post-edit simulation.
