# Brief — MEASURE train, ARCH car 1: THE MANIFEST AND THE CORPORA — byte-identity before any text moves, and a classifier that names what any change did to every cell

Seat: Opus 5 — implementer. Receipt first: `$SC/receipt-measure.md` § CAR 1. `$SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit`. Dock: `$SC/laneMEASURE` at car 0's tip (read `git log -3`).

## Read first, whole
`$SC/arch-prose/ARCH-COMPOSED-PROSE-v2.md` §3.4 (the three corpora — DRIFT · RATE · VARIETY — and why one sample cannot serve three instruments), **§3.7** (the manifest: the per-row roll-up sha fixture, the on-demand per-cell table, the classifier's four verdicts, the base-side normalisation, the golden door `recordGolden` under `GOLDEN_SHIFT_SIGNED`, the two controls no one-audience manifest can see — the mixed-pool audience arm and the PAIRED-TOWN arm — and the seedless control), §7 (the repeat census and the DUPLICATE-UNIT RATE on VARIETY), §12 row 1 (builds · files · EXECUTED proof · predicted reds · doors), §16; `tests/helpers/goldenRecordDoor.js` and `tests/lint/goldenFreeze.walker.test.js` (the door and the register a new carrier must be enrolled in); `tests/property/generatorGoldenMaster.test.js:760-900` (the 525-row builder, `keyOf(config)`, the `120_000` override idiom); `src/domain/display/legibilityRung.js` (the provenance shape `{blockId, poolKey, angle}` today); the car-0 receipt (the RATE corpus builder you reuse).

## What to build
1. **`tests/property/dossierProseManifest.test.js` + `tests/fixtures/dossier-prose-manifest.json` + `tests/helpers/dossierManifest.js`:** the DRIFT corpus (the golden's 525 configurations, `keyOf(config)`, its four seeds) × the six composers through the shipped desk-read readings, at `audience: dm` AND `audience: player`; the fixture a per-row roll-up sha (≈ 1,050 rows) for the fast drift arm; the base-side normalisation (`face: 0`; `pieces: [{role:'spine', key: poolKey, vid, index, face: 0}]` synthesised — car 3a will assert equality); a `120_000` per-test override in the golden's own idiom; the cost PRINTED (N, seconds, runner), never a threshold. Every write through `recordGolden` (`GOLDEN_SHIFT_SIGNED` naming the chair's ENROLL record — the record text the chair supplies below), the row in `tests/fixtures/.golden-freeze-register.json`, the surface name `dossier-prose-manifest`; `goldenFreeze.walker.test.js` green with the carrier ENROLLED, never excluded.
2. **`scripts/prose-manifest-cells.mjs --out <file>`:** the per-cell table `${keyOf(config)}::${audience}::${mount}::${rungIndex}` → `{pool, vid, index, face, angle, pieces[], textSha}` regenerated on demand at any sha (≈ 52,500 rows; never committed). **`scripts/prose-manifest-diff.mjs base.json tip.json`:** the CLASSIFIER printing per cell `UNCHANGED · WORDING-ONLY (same pool, same vid, text moved) · ADDITIVE (a piece added beside an unchanged spine) · RE-INDEXED (same pool, vid moved) · REPLACED (a different pool or a turn)` with per-class counts and the towns per class. ARMS (executed): a plant that edits one variant's text must be classified WORDING-ONLY on exactly its cells and nothing else; a plant that appends a variant to a two-variant pool must be classified RE-INDEXED on ≈ 2/3 of that pool's cells (the P-F1 arithmetic) — both plants applied to a COPY and restored `cmp`-exact.
3. **The two controls:** (a) the mixed-pool audience arm — `eligibleVariants` applies `variantIsAudible` before the modulus, so on the 12 mixed pools (`dm-only` + unmarked variants) the player's drawn INDEX may differ: the manifest pins the COUNT of such cells and asserts a difference on any other cell is a leak; (b) the PAIRED-TOWN arm — for every sampled town compose the player face twice, as is and with every `covert: true` pool suppressed at the candidate stage (car 0's `covert` column), and assert byte-equality across all 525; and on the corruption fixtures the same seed with the impairment toggled. (c) the seedless control: `galleryImportSettlement.js:67` nulls `_seed` ⇒ index 0 everywhere — assert.
4. **`tests/helpers/proseVarietyCorpus.js` + `scripts/prose-duplicate-units.mjs`:** the VARIETY corpus (the 525 configs re-seeded `prose-${i}`, 8 seeds, 4,200 towns) as a per-car script never per-CI; the DUPLICATE-UNIT RATE — the share of (position, text) pairs seen on more than one town, pooled over every cell — printed as the BASELINE before any text moves, with N and the seed count; the repeat census per mount against the chance floor, NOT-EXECUTABLE below the stated pair count (ARCH §7's power table). Cost printed.
5. **The receipt:** the drift arm's `[]` three times; the two classifier plants' outputs; the mixed-pool count; the paired-town byte-equality on 525; the seedless control; the duplicate-unit baseline (a number with its N and seeds); the wall-clock of each corpus.

## The chair's enroll record (paste verbatim into the door's record file in the estate's idiom)
`ENROLL dossier-prose-manifest — chair Fable 5.1, 2026-09-08, ARCH-COMPOSED-PROSE-v2 §3.7 / MEASURE car 1: a new golden carrier for the dossier's composed text (DRIFT corpus 525 × 2 audiences); the generator golden master cannot see a dossier sentence; every later change to this carrier is a declared shift on the SHIFT REGISTER, signed per cell class by the classifier's print.`

## Discipline, fences, proof
As car 0's (runner count 0 in its own shell; focused one at a time; ONE whole `tests/lint`; strict ratchet exactly on its ceiling — strict-clean code; voice E2 two banked files; OSR/PN exact; typecheck; cast walkers; the byte fence; the manifest test after your plants). Never the whole suite; never a build; no register `--write` but the lighting door (1b) and the golden door through `recordGolden` with the enroll record. Change only the files named plus fixtures, the sweep + manifest by text, the receipt, `$SC/measure/*`. No composer, pool text, persisted shape, seed input. Commits `MEASURE car 1: the manifest and the corpora — <clause>` + `car 1b`; explicit staging; porcelain 0; the three trailers (`Seat: Opus 5 — Fable-unvalidated` / `Lane: MEASURE` / `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`). Return: the shas; the drift line; the two plants' classifier outputs; the controls' figures; the duplicate-unit baseline; the costs; the whole-tests/lint summary; the ratchet lines; anything refused with its measurement.
