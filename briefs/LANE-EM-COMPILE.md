# EM COMPILE LANES — wave 1 of the settlement editor's packet train (the shared brief)

**Seat:** Opus COMPILE lane. You compile member packets to the estate's implementation-packet standard; you do not implement, and you run no gate. **Chair:** Fable (session 923472dc). **Program:** ODQ §934.36–§934.42 — the owner ordered the editor built now; the charter is `docs/implementation/charters/EDIT-MODE-TRAIN.md`.

**Base (the packets' verified base):** branch `fixes-2026-09-18-consist` at **d31af2ceebf643818201b2e2ab4a556765d2fc7c** (short d31af2cee), worktree `$SP/consist` with `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`. Confirm `git -C $SP/consist rev-parse HEAD` prints that sha before you measure anything; if it differs, STOP and report.

## PATH LAW (absolute)
- READ the consist worktree and the ledger checkout (`/Users/cstokes/Desktop/settlement-engine`, branch review-fixes-2026-07-08, for `docs/OWNER_DECISION_QUEUE.md` §934.36–§934.42 only). WRITE ONLY under `$SP/lane-em-<your letter>-scratch/`. Never edit, stage, commit, or create files in the consist or the ledger; never run vitest, eslint --fix, npm run check, or any script that writes. Measurements only: `grep -n`, `git grep`, `sed -n`, `node -e "import('…').then(m => console.log(Object.keys(m)))"`, `node -e` with eslint's Linter for `max-lines` if a hot file is named, `wc -l`.

## AUTHORITY (read in this order, whole)
1. `docs/implementation/PACKET_STANDARD.md` — the law of a packet (statuses; the hard scope budget; the verified tree contract; the exact contracts; the change manifest with `requiredSymbols` naming ONLY symbols present in the tree now; the registration obligations priced at compile; the STOP conditions; the completion receipt).
2. `docs/implementation/PACKET_TEMPLATE.md` — the twelve sections every member packet carries.
3. Two exemplars: `docs/implementation/packets/catalog-hygiene/MF-CH6.md` (a landed member) and one greenfield packet with CREATE rows — find one: `grep -l '"action": "CREATE"' -r docs/implementation/PACKET_MANIFEST.json` is the manifest; pick a LANDED packet whose changeManifest has CREATE rows under `src/domain/` and read its Markdown.
4. `docs/implementation/charters/EDIT-MODE-TRAIN.md` — your packets' rows (outcome, CREATE/MODIFY/TEST, required symbols, collision group).
5. `docs/ARCH_EDIT_MODE_AND_DECREES.md` (the module map, the JSDoc types, the instruments, the catalogues) and `docs/DESIGN_EDIT_MODE_AND_DECREES.md` — **§12 GOVERNS** wherever an earlier section or the ARCH disagrees.
6. The manifest's entry shape: `docs/implementation/PACKET_MANIFEST.json` — keys `id, status, packetPath, verifiedBase, changeManifest[{action,path,symbol?}], requiredSymbols[{path,symbol}], acceptanceCases, checks[[argv…]]`.

## WHAT YOU PRODUCE, PER PACKET, in your scratch root
1. `<ID>.md` — the member packet at the template, **status DRAFT**, packet version 1, verified base = the full sha above, `Depends on` (the charter's dependencies, as packet IDs until they land), `Collision group` (files shared with sibling packets), `Commit authority: edits only; the chair commits`, `Baseline posture: measured` (name what you measured). The header's preamble citation line reads `Preamble: docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR)`.
2. `<ID>.manifest.json` — the manifest entry (status DRAFT; `packetPath: docs/implementation/packets/settlement-editor/<ID>.md`; changeManifest rows with exact paths; requiredSymbols ONLY for symbols that EXIST at the base — every one proven by a quoted command; `checks` as argv arrays `["npx","vitest","run","--pool=threads","--maxWorkers=2","<exact test file>", …]`, one directory per array).
3. `<ID>.evidence.md` — every command you ran to prove a verified fact, with its output (the receipt for the chair's promotion). A fact without a command is not verified.

## THE RULES THAT SHAPE A WAVE-1 PACKET
- **One behaviour family; the default hard scope budget binds** (≤12 handwritten files; ≤400 new/changed effective production lines; each new leaf ≤250; ≤2 new logic leaves; ≤3 existing logic files modified; ≤8 acceptance cases). If the charter's row cannot fit, the packet says STOP AND SPLIT with the proposed split — never a quiet renegotiation.
- **Navigate by symbol, never by inherited line number.** Every verified-fact row names path + symbol + the fact + the command that proved it.
- **`requiredSymbols` names only what the deliverable must PRESERVE and that exists now.** Symbols the packet CREATES are named nowhere in the manifest until the flip to LANDED.
- **Exact contracts, no discretion:** signatures, return shapes, absence vs empty vs null, ordering, determinism (hash/fork keys; no-draw behaviour), lifecycle (create/read/persist/reload/regenerate/undo/import/migrate/public veil), receipts. Words like "as appropriate", "if useful", "choose the best approach" are forbidden.
- **Registration obligations priced at compile, in the manifest:** a new `tests/lint/` file → its `scripts/mutation-coverage-manifest.json` row (REGISTER); a new `src/domain/**` file or any new test file/title → the sovereignty-lighting census moves: state the DELTA you cause (`files / parked / credited / titles / suiteTitles`) and NEVER an absolute tuple; the terminal refreeze is the chair's (EM-PREAMBLE §P2 row 1 governs); a new domain reader of a save-time key (`dmLayer`, `decrees`) → the observed-shape register's `EXPLAINED_WRITER_EXEMPTIONS` row through the migration-bundle door (a CHAIR act, priced as a named REGISTER row the chair executes); new readers of settlement fields under `src/domain/edit/**` → say whether `scripts/check-writer-reach.mjs` and `scripts/check-observed-shape-readers.mjs` can move and how (a shrink is `--write`; growth is a mint); any rendered float → prose-numerics. Wave 1 is HEADLESS: no component, no store surface except where the charter names the store (B3's persistProjection rows), no feature flag.
- **Golden posture: UNCHANGED for every wave-1 packet.** `tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js` must not move; B2's isolation property test is the proof.
- **THE PROMISE and the design's rulings are not yours to reopen:** names are join keys (`free-cascade` runs the existing rename cascade — find it: `src/domain/factionRename.js` and its join list); the layer is applied ON WRITE (the record carries the edited values; `dmLayer` records which fields are the DM's); phantoms are minimal save records; guards FOLD; the OSR door is the exemptions bundle; edits do not travel (fork/import/gallery). Where a fact in the tree refutes a premise, the packet is **BLOCKED** with the smallest measured contradiction — never adjudicated by you.
- **Hot files:** check the standing list in the standard; a wave-1 packet should name none. If one is unavoidable, open with an executed headroom measurement and shape the edit to net zero.
- **Test precedent:** name one or more existing tests whose SHAPE the packet's tests copy (a domain unit test, a property test, a persistence round-trip, a lint walker), by path and describe/it name.

## YOUR ASSIGNMENT
Compile the packets named in your launch message, in the order given. Read the charter's rows for them and the ARCH's §1 module map, §2 types, §3 schema, §8 instruments and §9 catalogues, then MEASURE the tree for every claim (the ARCH's file:line hints are HINTS; re-find by symbol). Report per packet: status (DRAFT or BLOCKED), the three file paths in scratch, the budget table's figures, the predicted register moves, the collision group, and any question only the chair can answer (list; do not wait).

## WHAT YOU NEVER DO
Implement anything. Run vitest, eslint --fix, npm run check, or any writing script. Edit the consist or the ledger. Raise or invent a budget. Name a symbol that does not exist. Guess a fact you could have measured.

> CORRECTION (the chair, 2026-09-19, from the EM-B1h compile lane's finding): wherever this brief and `EM-PREAMBLE.md` section P2 row 1 disagree about the LIGHTING CENSUS, THE PREAMBLE IS RIGHT -- a member never edits `tests/lint/.lighting-census-baseline.json` and never quotes an absolute tuple; it states the DELTA it causes, and the train's terminal refreeze is the chair's act.
