# LANE: ENC-4c — wire the host-offered half of §B with the chair's five sentences
⟦Chair: Fable 5.1 · Lane: Opus 5 (`model: "opus"`) · Fable retrovalidation of this act is the chair's own⟧

## READ FIRST
1. `$SC/briefs/_PREAMBLE.md` — the hard rules.
2. `$SC/receipt-enc-4b.md` — F2 (the §B words are honest on only half the kind), F3, F4, and R2–R4. ENC-4b SHIPS THE CASE, NOT THE KIND: the writer withholds the line when the host's own notable offered.
3. `$SC/enc4c-host-words.md` — THE CHAIR'S WORDS for the withheld half. ⛔ Ship them verbatim; you may not rewrite a sentence. If a sentence fails an instrument (voice, digit, slot, adjacency), STOP and report which instrument and which sentence — the chair rewrites, never the lane.
4. The §B block ENC-4b wired: `src/domain/worldPulse/envoyChanceMeetingReceiptPools.js` (`chance_meeting_exposed`), `envoyChanceMeetingNews.js` (the withhold gate), `tests/lint/chanceMeetingKindPools.walker.test.js`, `tests/lint/kindPoolFloors.walker.test.js`, and the annex line ENC-4b added.

## YOUR DOCK
`$SC/laneENC4B`, detached at **`9d9686c12`** (ENC-4b's tip, 4 cars over `7be568047`, sealed `enc4b-2026-09-05`). Build ON TOP of it; do not rebase; the chair replays. ⛔ Never materialise `node_modules`, never `npm install`, never `git stash`, never `git checkout --` on foreign content.

## THE ACT
1. **Re-derive the premise**: at your tip, show by the writer's own gate that the host-offered case produces NO line (the withhold). Count how often it fires on the same 600-world corpus ENC-4b used (the receipt names the seeds/configs); if it never fires, STOP and report — words for a case that cannot occur are a mint.
2. **Wire the second pool** for the same kind, keyed on the CASE (the approacher is of the host town), with the chair's five sentences verbatim, the per-variant required slots as listed, and the §C noun phrase as the reason limb exactly as §B does. The writer selects the pool by the case it already computes; it withholds NOTHING for this kind after your car (both cases have words). Keep the dispatch door and the significance unchanged.
3. **The annex**: add the block under the governed `## §B ENC-4` section the way ENC-4b added its one line, and correct the walker docblock's seal statement in the same commit, as ENC-4b did.
4. **Instruments**: every figure in `chanceMeetingKindPools.walker.test.js` and `kindPoolFloors.walker.test.js` that your pool moves, moved with attribution in the comment (pool length, required slots, the eligible set). ⛔ `tests/domain/pantheon.test.js` A5's six freezes: ENC-4b moved them to 115/381 in its car 4 — a second POOL for the same kind adds NO registry row and NO routed token; prove that they do NOT move, and say so in the receipt.
5. **Proofs** under the mutex when the window opens (`sh scripts/gate-mutex.sh --run -- npx vitest run <files>`; NO vitest while a chair gate runs): the two walkers, the writer's suite, pantheon, `tests/lint/` whole. If the window never opens, record the exact commands as OWED.

## FENCES
No register act. No widening to traveller × traveller (fenced by the ruling on the prose ground). No reason clause authored. No change to the sealed §A/§B sentences. Commit per step, `Seat: Opus 5 — Fable-unvalidated`, `Lane: ENC-4c`, `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Receipt `$SC/receipt-enc-4c.md`, PARTIAL header first, RETROVALIDATION ROW last, tip sha last.
