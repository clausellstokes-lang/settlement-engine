{
  "appliedHead": 203,
  "appliedAt": "2026-09-20",
  "verifiedAt": "2026-09-21T01:11:35Z",
  "verification": "Applied to the linked production project (uhozyhcdccbhigvlacdu) by the owner's attended `supabase db push` of 2026-09-20 (~21:10 EDT), run from a clean worktree at f76ddf0ab (PR #53's pushed tip; 203 migration files, contiguous). The `--dry-run` immediately before it listed exactly 201, 202 and 203 as pending, which is the remote history's own statement that 001 through 200 were present; the push then printed `Applying migration 201_staff_unlock_surveyor_entitlement.sql...`, `Applying migration 202_edit_registry_public_denylist.sql...`, `Applying migration 203_gallery_scanner_client_mirror_totality.sql...` and `Finished supabase db push.` with no error (read by the chair from the owner's terminal). All three are `create or replace function` (201 also re-states its revoke, grant and comment); no table or data statement. `supabase migration list` was NOT run afterwards; `SUPABASE_MIGRATION_HEAD=203 node scripts/check-migration-head.mjs` printed `deploy probe OK: live head 203 == repo head 203`. An earlier attempt the same evening from the stale main checkout (136 migration files on disk) was REFUSED by the CLI (`Remote migration versions not found in local migrations directory`); the `migration repair --status reverted 137 … 200` it suggested was NOT run and must never be.",
  "note": "Last migration number applied to the PRODUCTION Supabase project. Bump this in the SAME commit/PR as the `supabase db push` that applies new migrations. The gate cross-checks it (scripts/check-migration-head.mjs + tests/docs/migrationAppliedHead.test.js): appliedHead must reference a real, contiguous migration file and may never exceed the repo migration head. When the repo head is AHEAD of appliedHead, `validate:migration-head` surfaces the pending (undeployed) migrations so the 'code ahead of deployed DB' state is visible in the gate output rather than tribal knowledge. The authoritative live check remains the SUPABASE_MIGRATION_HEAD deploy probe in scripts/check-migration-head.mjs."
}
