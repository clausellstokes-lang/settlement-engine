# `settlement editor / wave 1` — EM-B1f: status-based absence at THE ONE PARTICIPATION CHOKEPOINT

- **Status:** `DRAFT` — ⭐ **READY-able. THE BLOCK IS LIFTED BY EXECUTION.** Version 2 was
  `BLOCKED` on EM-B1k; EM-B1k's cure is in the tree at `19c4cb853`, and this lane re-ran the DM
  reproduction and the sole-member-house reproduction at that tree **with this packet's own arm
  applied**: the arm erases nobody from the save and dissolves no house (§13).
- **Packet version:** `3`
- **Verified base:** `__BASE__`
- **Last revalidated:** `__BASE__`
- **Depends on:** **EM-B1d, LANDED at `95e494bdb`** (version 5) · **EM-B1k, LANDED** — flipped at
  `63e40fe57` while this lane was compiling; ⭐ **both dependencies are terminal and placement is
  FREE** (§7.1).
- **Collision group:** ⭐ **NONE at `96036427f`** — measured: the register's only non-terminal
  packet is EM-B3c (READY), which shares none of this packet's twelve paths. ⚠ **EM-B1k2** (DRAFT
  in the kit, not yet in the register) will reserve `tests/domain/roadsParticipation.test.js` when
  it is placed; the validator permits ONE non-terminal owner, so the ORDER is the chair's (Q3).
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured at `19c4cb853` — the chokepoint's body; the eight required
  symbols by `grep -cF`; **the DM reproduction and the house reproduction re-run in seven cells
  through the shipped pulse, with and without the arm, at the cured and the pre-cure kernel**; the
  ninth roster row simulated **through the walker's own `offencesOf`**; the four test homes
  through the lighting walker's own `parkReasonsFor`; the three forms' minified bytes; the four
  chunk closures and the FORM B import edge; the edge-shared write set by generator AND by
  precedent; the full 525-row golden corpus; every `.status` writer in `src/`; the line-addressed
  and stamped registers
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)
  — ⓘ measured live at `19c4cb853`:
  `b90a95b7af484137ecf70bd15cde5054edf66b5db0d9f6e90c974d97b7caa5e1`, identical to the brief's
  stamp.

> **THE CHAIR'S REVALIDATION SENTENCE, ready to use at promotion:**
> *"Revalidated at `<tip>`: `git diff --stat e5bdfd031 <tip>` over all twelve change paths and all
> seven `requiredSymbols` paths moved ONE file — `tests/domain/roadsParticipation.test.js`
> (+118, EM-B1k's A3 and A5) — and nothing else; all eight `requiredSymbols` rows resolve
> `grep -cF` = 1 at their recorded lines (157 · 151 · 105 · 92 · 618 · 94 · 113 · 124);
> `isOffStage` still reads no `.status`; `NPC_UNAVAILABLE_STATUSES` is still
> `['dead','exiled','jailed','removed']` and frozen; the derived TRIGGER is still
> `['dead','exiled','retired']` and `FLAGGED === DECLARED_FLAGGABLE`; the preamble hashes to
> `b90a95b7…caa5e1`; and the §13 promotion measurement was re-executed at the cured kernel."*

## 0. WHAT VERSION 3 CHANGED, AND WHY

Version 2 was compiled at `e5bdfd031` and **BLOCKED** because the participation filter reached an
irreversible consumer and the tick's written settlement was born from the filtered view: a shelved
person was erased from the save, and a sole-member house was dissolved for good. **EM-B1k cured
exactly that** (`19c4cb853`: `pulseKernel.js`'s two seam lines), and version 3 answers the gating
measurement **by execution rather than by inheritance** — §13 is now a seven-cell table, not a
block. Nothing else in the packet's shape changed, but five facts were re-measured and **two v2
claims are corrected**:

1. ⛔ **v2's chunk row "eager first paint — NO (`roads/state.js` absent; BOTH IMPORTERS absent)"
   is REFUTED in its second half.** `src/domain/entities/npcs.js` **IS** in the eager first-paint
   closure (`main.jsx → store/index.js → settlementSlice.js → events/mutateEntities.js →
   entities/npcs.js`). It does not change this packet's price — `roads/state.js` is still absent
   from that closure and FORM B adds **zero** module edges — but the sentence was wrong and is
   struck (§3.2).
2. ⛔ **v2's "no writer in `src/` can assign `exiled` or `removed` to `.status`" needed one
   qualifier.** Two writers DO write `status: 'removed'` — `settlementLifecycleFirstClass.js:601`
   and `tierOutcomeApply.js:143` — and **both write an INSTITUTION**, never an NPC
   (`demotionFateForInstitution`; `EntityStatus` carries `removed` too). The claim now reads *no
   writer assigns any of the three to an **NPC's** `.status`*, which is what the arm is about (§6).
3. ⭐ **The ninth roster row's shape is no longer argued — it is executed.** The walker's own
   `offencesOf` was run over the post-edit source: `omits: ALL_BUT_DEAD` is LAWFUL, and **the
   charter's own ruling text (an omission naming `active`, `dead`, `missing`, `retired`) is
   CONVICTED TWICE** (§7, Q1).
4. The four test homes are CREDITED by the lighting walker's own `parkReasonsFor` (executed) —
   the `+4 titles` delta is measured, not assumed.
5. `checks` gains `tests/property/roadsDormancyGolden.test.js`, copying EM-B1k's own landed
   `checks` row: A5's dormancy claim is precisely what that golden pins.
6. ⭐ **THE BRANCH MOVED UNDER THE LANE, AND IT MOVED IN THIS PACKET'S FAVOUR.** While version 3
   was being compiled the tip advanced `19c4cb853 → 63e40fe57` (**EM-B1k flipped to `LANDED`**) →
   `9f3455b84` (CURE-E) → `96036427f` (CURE-F). `19c4cb853` is an ancestor of `96036427f`, and
   `git diff --stat` over all twelve change paths and all seven `requiredSymbols` paths across that
   window is **EMPTY** — so every measurement below stands unchanged, and the placement that was
   blocked at the compile base is **free at the tip** (§7.1).

---

## 1. Reconciled authority

1. **The owner, design §15 (ODQ §934.46):** *"A jailed or exiled holder cannot keep a seat."*
2. **The charter, amendments of 2026-09-19 17:36** (EM-B1f's six rulings; ODQ §934.47 addendum 22)
   — Q1 the arm is `exiled · jailed · removed`, never `missing`/`retired` (R18); Q2 the lazy
   engine's ~700 B margin is a raise-time convention and the literal assertion is the law; Q3
   price `advanceInterval.worker` as a DELTA, no per-packet re-mint; Q4 **FORM B, derived from
   `NPC_UNAVAILABLE_STATUSES` minus `dead`**; Q5 the ninth roster row is this packet's; Q6
   discharged; Q7 `envoyCasting`'s double refusal is the settled idiom.
3. **The charter, amendments of 2026-09-19 19:42 and 20:03–20:34** (ODQ §934.60 and its two
   addenda) — the two live defects; **EM-B1k cures them FIRST; EM-B1f lands after it WITH ALL
   THREE MEMBERS and does not narrow.**
4. **The charter, amendments of 2026-09-20 02:0x and 02:53** (ODQ §934.62, §934.47 addendum 39) —
   EM-B1k version 2 placed and built; **EM-B1k2** follows it as defence in depth and owns the
   participation ratchet's widening to `src/domain/density`.
5. **The charter's train table:** **EM-T6** — EM-B1f **FIRST**, because it ⛔ **BLOCKS EM-B1a's
   promotion** (B1a's `set-npc-status` is what makes `jailed` producible).
6. **EM-B1d version 5's own law, inherited verbatim** (`entities/npcs.js` header above
   `NPC_UNAVAILABLE_STATUSES`): *"`jailed` is UNAVAILABLE but still ON the roster… **Participation
   is a THIRD question, cured once at `isOffStage` (roads/state.js §8) by EM-B1f.**"*
7. **`DESIGN_THE_ROADS.md` §8** and **§1 law 5** — travel is narrative, captivity is mechanical.
8. **`EM-PREAMBLE.md` §P2 rows 10–11**; the pre-proof brief's steps 11–16.

### §1.1 · Resolved contradictions — five, each measured

- v1's *"`ROSTER_ABSENT_STATUSES` gains `jailed`"* → **withdrawn**: EM-B1d v5 left that constant
  untouched (`['dead','exiled','removed']`, `factionLifecycle.js:87`) and minted
  `NPC_UNAVAILABLE_STATUSES` instead. Evidence §4.
- v1's four `REGENERATE` rows → **invalid**: `PACKET_ACTIONS` is
  `['CREATE','DOC','MODIFY','REGISTER','TEST']` (`implementation-packets.mjs:30-36`). Seven
  `MODIFY` rows. Evidence §11.
- ⛔ **the charter's ruling (3) text — an omission naming `active`, `dead`, `missing`, `retired` —
  is REFUTED BY THE WALKER.** Executed: `'active'` is convicted as *"omits a word that is not a
  member of the live union"* (the check is against `NON_ACTIVE`, not `NpcStatus`), and dropping
  `exiled`/`jailed`/`removed` from `omits` is convicted as *"enumerates the union without exiled,
  jailed, removed"*. The lawful row is `omits: ALL_BUT_DEAD`. Evidence §7. **Measured, not chosen.**
- ⛔ **v2's "both importers absent from eager first paint"** → refuted; see §0 item 1. Evidence §9.
- ⛔ **v2's "no writer can assign `removed`"** → qualified; see §0 item 2. Evidence §13.

The implementer does not read other documents to reinterpret this packet.

---

## 2. Outcome

**Observable result:** an NPC whose `status` is `exiled`, `jailed` or `removed` is OFF-STAGE for
every participation read, so a jailed or exiled holder cannot keep a seat.

**Definition of done:** `isOffStage` returns `true` for those three; the seven existing consumers
inherit it unchanged; `readWarSeatBooks` reads `unseated` for a jailed ruler exactly as it already
does for a dead, shelved or hostage one; and **no irreversible consumer acts on the widening**,
which §13 now proves by execution at the cured tree rather than assumes.

In scope: ONE arm on ONE predicate; integration by *inheritance* (no consumer is edited); the
prevention guard is EM-B1d's walker, which this packet joins as its ninth roster row.

Explicit non-goals: ⛔ `missing` and `retired` never join the arm (Q1; A2 pins both directions).
⛔ No writer — `set-npc-status` is EM-B1a's. ⛔ No holder guard, op, store surface, component or
flag. ⛔ No edit to `NPC_UNAVAILABLE_STATUSES`, `ROSTER_ABSENT_STATUSES` or any EM-B1d file.
⛔ No edit to `pulseKernel.js` or anything else EM-B1k or EM-B1k2 owns.

---

## 3. Hard scope budget

| Limit | Packet budget | This packet |
|---|---:|---:|
| Behavior families | `1` | **1** |
| New persisted record families · Named state writers · Feature flags · User-facing surfaces | `0 or 1` | **0 · 0 · 0 · 0** |
| Direct consumers | `<=2` | **0 new** (seven inherit) |
| New logic-bearing production leaves | `<=2` | **0** |
| Existing logic-bearing production files modified | `<=3` | **1** |
| Additional registration-only files | `<=3` | **1** |
| Handwritten files total | `<=12` | **5** |
| New/changed effective production lines | `<=400` | **≤6** |
| Delta in a shared/hot file | `<=15` | n/a — §3.1 |
| Acceptance cases | `<=8` | **7** |

Overrides approved before dispatch: `NONE`.

### §3.1 · Hot files — measured at `19c4cb853`; the one file edited is NOT hot

`src/domain/roads/state.js` = **288 effective lines** (596 raw) under eslint's own `Linter`
(`configType: 'flat'`, `max-lines` with `skipBlankLines` and `skipComments`) against the
`src/domain/**` ceiling of **800** (`eslint.config.js:712`): **512 lines of headroom**, no per-file
override, **no `scripts/.size-baseline.json` entry**, and absent from `PACKET_STANDARD.md`'s
standing hot list — which the chair re-measured at `c740ded8b` and which now carries six rows
(`convergence.js` corrected 798 → 764; `institutionLifecycle.js` joined at 798).

### §3.2 · ⭐ THE BUNDLE BUDGETS, PRICED (§P2 row 11)

| budget | ceiling | member? | cost | obligation |
|---|---:|---|---:|---|
| generation worker `WORKER_BUNDLE_CEILING_BYTES` | `1401208` — ZERO slack (`generationWorkerLazy.test.js:159`) | **NO** (220-module closure, measured) | **0 B** | none |
| eager first paint `EAGER_FIRST_PAINT_MODULES` | closure budget | **NO** — `roads/state.js` absent ⚠ **but `entities/npcs.js` IS eager** (v2's sentence corrected) | **0 B** | none — the edge points the other way |
| lazy `engine` chunk | `< 679_000` (`vendorPdfLazy.test.js:787`) | ⭐ **YES** | **+184 B measured**, bound **≤370 B** | §8's byte step |
| `advanceInterval.worker` | ⛔ none today; **TOOL-3 mints one with a 4 KB per-TRAIN headroom** | ⭐ **YES** (549-module closure) | **a +184 B DELTA against that headroom** | ⛔ **no per-packet re-mint (Q3)** |
| `townSceneExport` (125) · `townScene` · `customContentPreview` workers | — | **NO** | **0 B** | none |
| edge-shared bundles | INPUT membership | ⭐ **YES — input of both** | 7 paths | §3.3 |

**The bytes, RE-MEASURED at `19c4cb853`** (each form written over the real file, minified with the
repo's own esbuild 0.28.1, `--minify --format=esm --target=es2022`): base **7,727 B**;
**FORM A** (three literals spelled here) **7,885 B = +158 B**; **FORM B as a second import
statement** 7,946 B = +219 B; ⭐ **FORM B as a SPECIFIER on the import `state.js` already has**
**7,911 B = +184 B — CONTRACTED.** The base file is byte-identical to version 2's
(SHA-256 `23ffc63b…676481` both times), so every figure reproduces exactly.

⭐ **THE IMPORT IS NOT A NEW EDGE.** `state.js:36` already reads
`import { importanceWeight } from '../entities/npcs.js';`. Measured: `entities/npcs.js` is already
in the `advanceInterval.worker` closure; **its own closure is 2 modules** (itself and
`src/kernel/slugify.js`) and **does not reach** `roads/state.js` — no cycle; **0 modules are added
to any closure.** FORM B's whole cost over FORM A is **+26 B of code**, and it buys a single
source of truth for the vocabulary.

⚠ **HONEST LABEL:** `+184 B` is **CONFIRMED** as a minified-source delta, **PLAUSIBLE** as the
rendered-chunk delta (esbuild is not Rollup's renderer). Hence the ×2 bound.

Under **Q2** the lazy engine's `< 679_000` literal is the law between re-mints; the build lane
measures at this packet's own base and asserts growth ≤370 B with only `roads/state.js` moved.

### §3.3 · ⛔ THE EDGE-SHARED OBLIGATION — SEVEN PATHS, established two ways

`src/domain/roads/state.js` is an INPUT of both `aiCharterBundle` (**114** inputs) and
`aiOutputSchemaBundle` (**115**) — membership, never entry-hood (§P2 row 10) — and of neither
`aiGroundingBundle` (74), `analyticsEventsBundle` (2) nor `intentAtlasBundle` (2).
`build:edge-shared` runs `scripts/build-edge-shared.mjs`, whose **five** `ENTRIES` (`:33-39`) each
write `generatedAt: new Date().toISOString()` (`:83`) inside one `for` loop (`:108`), so a run that
moves two bundles writes **7 paths**: 2 bundles + their 2 metas + the 3 siblings' re-stamped metas.
⭐ **Confirmed by precedent, exactly:** EM-B1d's own landing `95e494bdb` shows those SEVEN paths
and no others; `ee8ac6c3c` shows the same seven. Both methods were used, as step 11 requires.

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-B1f
```
Check by check, at a base set to the tip: **branch/ancestry** — `head === verifiedBase`, the
substrate arm returns early; **substrate** — no `REGISTER` row is declared, so no declared-substrate
file sits in the window; **CREATE targets absent** — there is **no CREATE row**, so the check is
vacuous; **non-CREATE targets clean** — twelve pre-existing paths; **required symbols resolve** —
the eight rows of §5, each `grep -cF` = 1 at `19c4cb853`.

⭐ **`node scripts/implementation-packets.mjs validate` PASSES ON PLACEMENT at `96036427f`.**
`reservesChangePaths = !TERMINAL_PACKET_STATUSES.has(status)` (`:676`) with
`TERMINAL = {LANDED, SUPERSEDED}` (`:43`). At `19c4cb853` EM-B1k was still `READY` and would have
produced *"duplicate change path across packets: tests/domain/roadsParticipation.test.js (EM-B1k,
EM-B1f)"*; **the chair flipped it to `LANDED` at `63e40fe57`, while this lane was compiling**, and
the register's only remaining non-terminal packet is EM-B3c, which shares nothing. Re-measured —
§7.1.

---

## 5. Verified tree contract — every row re-found by symbol at `19c4cb853`

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| **The chokepoint** | `src/domain/roads/state.js` | `export function isOffStage` (`:157`) | `isInStasis(npc) \|\| whereabouts.state === 'hostage'`; **never reads `.status`**. Pure, total, absence-safe (`isOffStage(null)` and `isOffStage(undefined)` both `false`, executed). | THE one place the arm is added. |
| **Inherited predicate** | `src/domain/npc/npcOps.js` | `export function isInStasis` (`:151`) | its vocabulary is `STASIS_REASONS`, a per-layer fact, **not** `NpcStatus`. | Preserve; build beside it. |
| **The availability vocabulary** | `src/domain/entities/npcs.js` | `export const NPC_UNAVAILABLE_STATUSES` (`:105`) | `Object.freeze(['dead','exiled','jailed','removed'])`; its header names EM-B1f as the cure for the participation question. | ⭐ **The arm DERIVES from it** (FORM B). Never re-spelled. |
| **Sole seat read** | `src/domain/worldPulse/warSeatBooks.js` | `function rosterNpcById` (`:92`) — ⛔ **module-private** | `:102` pairs `=== 'dead'` with `isOffStage`. | Preserve UNEDITED — the widening ARRIVES here. |
| **Exported route to it** | `src/domain/worldPulse/warSeatBooks.js` | `export function readWarSeatBooks` (`:618`) | calls it at `:633`; `!rulerId \|\| !ruler` returns `securityBand:'unseated'`, `seatWeight01:0`, omits `rulerId`. | A1 observes through it. |
| **Master gate** | `src/domain/worldPulse/worldSnapshot.js` | `export function buildWorldSnapshot` (`:94`) | `:127-129` filters off-stage NPCs out of the settlement every pulse kernel reads; the RAW save roster is untouched. | A4 observes through it. ⭐ **And EM-B1k now makes the tick's write base the RAW roster, so this is a READ PROJECTION and nothing else** (§13). |
| **The (formerly) irreversible consumer** | `src/domain/density/factionLifecycle.js` | `export function factionRosterOf` (`:113`) | `:117` filters `settlement.npcs` by `isOnRoster`, whose vocabulary is `ROSTER_ABSENT_STATUSES = ['dead','exiled','removed']` (`:87`); an empty roster still dissolves the house (`:130`) — but at `19c4cb853` it is fed the RAW roster. | ⛔ Named so §13 cannot be lost. Not edited. |
| **Prevention guard** | `tests/lint/statusUnionTotality.walker.test.js` | `const CONSUMER_ROSTER` (`:124`) | eight rows; `spelling` is CHECKED, not trusted; its `CHOKEPOINT` constant (`:121`) already says *"status-based absence joins that chokepoint in EM-B1f, not here"*. | ⛔ Gains this packet's ninth row, **APPENDED** (§7). |
| **Test precedent** | `tests/domain/warSeatBooks.test.js` | `it('never lets a dead roster holder keep deciding through a stale top rung')` | asserts `interestKind:'realm'`, `settlementWeight01:1`, `seatWeight01:0`, `securityBand:'unseated'`, `rulerId` undefined. | ⭐ A1 copies this shape verbatim with `'jailed'`. |
| **Test precedent** | `tests/domain/roadsState.test.js` | `describe('roads state — isOffStage …')` (`:30`) | four straight-line `it`s. | A2/A3 extend it; no new `describe`. |

Forbidden alternatives: no second participation predicate or status vocabulary; ⛔ **no edit to any
of the seven consumers**; ⛔ no edit to any EM-B1d, EM-B1k or EM-B1k2 file; no new `worldState` key,
PRNG stream, time source or writer; no files outside the manifest.

---

## 6. Exact contracts

### The predicate, after this packet — the whole diff, verbatim (FORM B, the chair's)

```js
// at :36 — a SPECIFIER on the import this file already has; not a new module edge.
// ⭐ PRECEDENT, BYTE FOR BYTE: src/domain/worldPulse/envoyCasting.js:50 already reads
//    `import { NPC_UNAVAILABLE_STATUSES, importanceWeight } from '../entities/npcs.js';`
import { NPC_UNAVAILABLE_STATUSES, importanceWeight } from '../entities/npcs.js';
```
```js
/**
 * The statuses that put a person OFF-STAGE, DERIVED from the availability vocabulary so this
 * file never spells the union a third time (entities/npcs.js owns it; EM-B1d v5's law).
 * `dead` is subtracted because the three seat/ladder consumers pair `=== 'dead'` themselves.
 */
export const OFF_STAGE_STATUSES = Object.freeze(
  NPC_UNAVAILABLE_STATUSES.filter((s) => s !== 'dead'),
);

export function isOffStage(npc) {
  if (isInStasis(/** @type {Parameters<typeof isInStasis>[0]} */ (npc))) return true;
  const o = npc && typeof npc === 'object' ? /** @type {Record<string, unknown>} */ (npc) : null;
  if (o && OFF_STAGE_STATUSES.includes(String(o.status || '').toLowerCase())) return true;
  const w = o ? o.whereabouts : null;
  return !!(w && typeof w === 'object' && /** @type {Record<string, unknown>} */ (w).state === 'hostage');
}
```
⛔ **The declaration line `export function isOffStage(npc) {` is unchanged byte-for-byte.**
⛔ **`OFF_STAGE_STATUSES` evaluates to `['exiled','jailed','removed']` and is frozen — executed
in seven probe cells at the cured tree.** A member added to `NPC_UNAVAILABLE_STATUSES` later joins
the arm automatically; that is the point of deriving, and A2 pins the current value so the widening
is never silent.

### Inputs, outputs, absence — every row below EXECUTED in §13's probes

- **absent `status` key** ⇒ `''` ⇒ **on-stage** — the case the corpus made load-bearing (4,884 of
  5,171 generated NPCs carry no `status` key);
- **`null` / `undefined` npc** ⇒ `false`, exactly as today;
- **non-string `status`** ⇒ `String(...)` coerces; `0` ⇒ `''` ⇒ on-stage (executed);
- **`'JAILED'`** ⇒ lowercased ⇒ off-stage, matching `warSeatBooks:102` / `npcLadderState:205` /
  `npcLadderKernel:660` (executed);
- **unknown token (`'sabbatical'`) / empty string** ⇒ on-stage (executed). The predicate never
  guesses.

### Ordering · determinism · flag · dormancy · golden posture

Stasis first (unchanged), then status, then hostage — a disjunction, so the order is not
observable; fixed so a future arm is one block. No hash/fork key, no draw, no rounding. **Flag:
NONE** — the arm is deliberately outside `roadsActive`, as `isInStasis` already is. **Dormancy:
presence-driven** — a world with no such NPC evaluates one `includes` on a 3-member frozen array
and behaves identically, including `buildWorldSnapshot` returning the SAME settlement reference
(A5), which `tests/property/roadsDormancyGolden.test.js` pins across the dormant corpus.
**Golden posture: UNCHANGED**, re-measured by execution at `19c4cb853`: across the FULL 525-row
corpus, 5,171 generated NPCs carry only `<ABSENT>` (4,884) or `'active'` (287) — **zero** of the
arm's three members.

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| nothing | a READ of an existing union member | nothing | unchanged | unchanged — generation writes none (525/525 measured) | unchanged | ⛔ unchanged: an imported or forked save reads identically, the predicate being pure over the record | ⛔ unchanged — the veil reads the SAVE, not the participation view, so the jailed person still appears in the dossier as jailed |

### Receipts, privacy, alignment, edit story

No receipt; no rendered figure (⇒ prose-numerics NOT owed, and the baseline does not name this file
— measured); no DM-only field; projection unchanged. Alignment **DECLARED EMPTY**. Edit story
**ENGINE-ONLY** — the DM's route is EM-B1a's.

### ⛔ The behaviour change, stated so it can be refused

A world holding an NPC stored as `exiled` or `removed` changes its **participation** reading the
moment this lands. **Measured at `19c4cb853`: no writer in `src/` assigns any of the three to an
NPC's `.status`.** The NPC writers are `killNpc` (`npcs.js:186`, `'dead'`), `createNpc`
(`npcs.js:277`, `'active'`), `mutateEntities.js:230/241/410`, `undoEvent.js:459`,
`magicFormsPractitioner.js:220`, `factionRoles.js:251`, `lineageMemberBirth.js:303`,
`densityAscension.js:145` and `factionDensityKernel.js:610` (all `'active'`), and `addNpc`
enumerates its fields without forwarding `event.payload.status`. ⛔ **THE ONE CORRECTION TO
VERSION 2:** two `src/` sites DO write `status: 'removed'` —
`settlementLifecycleFirstClass.js:601` (`{ ...inst, … }`) and `tierOutcomeApply.js:143`
(inside `demotionFateForInstitution`) — and **both write an INSTITUTION**, whose `EntityStatus`
union (`entities/status.js:23`) carries `removed` as a homonym. Neither can reach an NPC record.
⇒ **inert by construction on every world that exists.** `jailed` becomes producible only with
EM-B1a, which this packet blocks.

⭐ **AND THE EXCEPTION THAT BLOCKED VERSION 2 IS GONE — §13 proves it by execution.**

---

## 7. Exact change manifest — TWELVE paths, set-equal with the JSON

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/domain/roads/state.js` | `isOffStage` (`:157`), import (`:36`) | **+6 eff** | §6 verbatim. Declaration line byte-identical. ⛔ Stales two edge bundles — see the seven rows. |
| `MODIFY` | `tests/lint/statusUnionTotality.walker.test.js` | `CONSUMER_ROSTER` (`:124`) | **+2 eff** | ⛔ **THE NINTH ROW, APPENDED AT THE END** (never inserted: `A3` drives three synthetic probes off `CONSUMER_ROSTER[3]`, the `envoyCasting` row — executed). Shape FORCED by the walker, executed in evidence §7: `{ file: 'src/domain/roads/state.js', symbol: 'isOffStage', spelling: 'literals', enumerator: false, omits: ALL_BUT_DEAD, why: <≥40 chars> }`. The `why` must say: *"THE PARTICIPATION CHOKEPOINT ITSELF. It spells only `'dead'`, and only to SUBTRACT it: the members it acts on — exiled, jailed, removed — are DERIVED from NPC_UNAVAILABLE_STATUSES and never spelled here, so `omits` lists words this file does not WRITE rather than words it ignores. `missing` and `retired` are genuinely excluded: a reversible absence keeps the place (R18)."* ⛔ Nothing else in the walker is touched. |
| `TEST` | `tests/domain/roadsState.test.js` | the `isOffStage` describe (`:30`) | `n/a` | A2 + A3 — **one** new straight-line `it`. No new `describe`. ⛔ Never name a variable or parameter `it`, `test` or `describe`. |
| `TEST` | `tests/domain/warSeatBooks.test.js` | beside the dead-holder case | `n/a` | A1 — **one** new straight-line `it`. |
| `TEST` | `tests/domain/roadsParticipation.test.js` | the master-gate describe (`:33`) | `n/a` | A4 + A5 + A7 — **two** new straight-line `it`. ⛔ Do not touch the `.npcs` inventory ratchet (`:187`), its `EXPECTED` list or `UNDISPOSITIONED_NPCS_READERS` — those are EM-B1k2's. ⛔ Do not disturb EM-B1k's two `it`s at `:65` and `:108`. |
| `MODIFY` | `supabase/functions/_shared/aiCharterBundle.js` | whole artifact | `n/a` | ⛔ Written by `npm run build:edge-shared`, **LAST** in `checks`. Never hand-edited. |
| `MODIFY` | `supabase/functions/_shared/aiCharterBundle.meta.json` | whole artifact | `n/a` | same run |
| `MODIFY` | `supabase/functions/_shared/aiOutputSchemaBundle.js` | whole artifact | `n/a` | same run |
| `MODIFY` | `supabase/functions/_shared/aiOutputSchemaBundle.meta.json` | whole artifact | `n/a` | same run |
| `MODIFY` | `supabase/functions/_shared/aiGroundingBundle.meta.json` | `generatedAt` | `n/a` | ⛔ A SIBLING re-stamped by the same window (step 11). Declared so the seal does not drift. |
| `MODIFY` | `supabase/functions/_shared/analyticsEventsBundle.meta.json` | `generatedAt` | `n/a` | same |
| `MODIFY` | `supabase/functions/_shared/intentAtlasBundle.meta.json` | `generatedAt` | `n/a` | same |

**Generated artifacts:** ⛔ **SEVEN**, all by `npm run build:edge-shared`, never by hand.
⛔ **No CREATE row. No REGISTER row. Exactly four new `it` and no new `describe`.**
⛔ **`REGENERATE` is not a `PACKET_ACTIONS` member** — every generated row is `MODIFY`.
⛔ **A measured NEGATIVE never enters this table** — the four are in §7.2 (the chair's rule of
2026-09-20, from EM-B1k v2's fifth cut).

### §7.1 · Placement — measured TWICE: at `19c4cb853`, and again at `96036427f`

`TERMINAL_PACKET_STATUSES = {LANDED, SUPERSEDED}` (`implementation-packets.mjs:43`);
`reservesChangePaths = !TERMINAL.has(status)` (`:676`); a duplicate path across two non-terminal
packets is an ERROR (`:694-700`).

- **At `19c4cb853`** (this packet's compile base): 191 entries — 187 `LANDED`, 2 `SUPERSEDED`,
  **2 `READY`** — and EM-B1k, whose code had just landed, was **still `READY`** and reserved
  `tests/domain/roadsParticipation.test.js`. Placement would have been refused.
- ⭐ **At `96036427f`** (the branch tip as this lane closed; `19c4cb853` is its ancestor and
  **none of this packet's nineteen measured paths moved between them**): 191 entries —
  **188 `LANDED`, 2 `SUPERSEDED`, 1 `READY`.** EM-B1k was flipped to `LANDED` at `63e40fe57`.
  **The only non-terminal packet in the register is EM-B3c, and it shares none of the twelve.
  PLACEMENT IS FREE.** ✓
- ⚠ **EM-B1k2 (DRAFT, kit `packets-waiting/`, not yet in the register):** its change manifest
  declares `TEST tests/domain/roadsParticipation.test.js`. **Two non-terminal packets cannot both
  hold it**, so whichever is placed second waits for the first to flip. See Q3.
- **No packet in the register declares `src/domain/roads/state.js` as a change path.** ✓
- All eight `requiredSymbols` re-confirmed `grep -cF` = 1 at `96036427f`.

### §7.2 · The registration ledger — measured negatives live HERE, never in the table

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — titles only** | ⭐ EXECUTED through the walker's own `parkReasonsFor` at `19c4cb853`: all four homes return `[]` (CREDITED); the classifier is proved live on two planted parks. ⛔ NO ABSOLUTE QUOTED. DELTA: **`+0 files / +0 parked / +0 credited / +4 titles / +0 suiteTitles`**. The chair stamps the absolute. |
| P2.2 | mutation-coverage row | **NOT OWED** | The packet CREATEs nothing under `ENFORCER_DIRS`; the walker's row is EM-B1d's and already landed. |
| P2.3 | observed-shape exemption | **NOT OWED** | No save-time key read. ⚠ the scanner is in `checks`; **motion is a STOP.** |
| P2.4 | writer-reach | ⚠ **MEASURE, DO NOT ASSUME** | Baseline captured at step 1; a shrink is `--write`, **growth is a mint and a CHAIR act**. |
| P2.5 · P2.7 | fork / prose-numerics | **NOT OWED** | No draw; no figure. `tests/lint/.prose-numerics-baseline.json` does **not** name `src/domain/roads/state.js` (`grep -c` → 0). |
| P2.10 | edge-shared freshness | ⛔ **OWED — §3.3** | Seven paths, by generator AND by precedent. |
| P2.11 | byte budgets | **PRICED — §3.2** | +184 B into the lazy engine; a DELTA against TOOL-3's 4 KB per-train headroom for `advanceInterval.worker`; zero into the worker, the three other workers and first paint. |
| **step 13** | line-addressed registers | **MEASURED NEGATIVE — not a row** | `tests/lint/.tuning-inventory.json:967` carries `"src/domain/roads/state.js#ROADS_TUNING": { "line": 222, … }` and this edit sits ABOVE it, so the LINE goes stale — but `tuningRegister.walker.test.js:406` keys on **`spanDigest`** and `:428` explicitly pins *"a comment inserted above the table must leave the digest where it was"*. No manifest row; §12.1 records it. ⭐ And the register's other two touches are inert by construction: `TABLE_DEF_RE` only matches `*_TUNING`, `NAMED_NUMERIC_CONST_RE` only `= <number>;`, `BARE_DECIMAL_RE` only `\d+\.\d+` — `OFF_STAGE_STATUSES` matches none. |
| **step 15** | stamped files | **MEASURED NEGATIVE — not a row** | `docs/content/wiring-census.json` `stamp.files` has 7 entries, all under `src/domain/display/stateProse/`, and **does not name this file**. No re-take. |
| **step 16** | citations by line | **MEASURED NEGATIVE — not a row** | `git grep -nE "src/domain/roads/state\.js:[0-9]+"` finds **nine hits, all in `docs/**` prose** (`DESIGN_FP_ARCH*`, `SETTLEMENT_CAPABILITY_ATLAS.md`, `WF-SUBSTRATE.md`, the ES-5B/5C packets). ⛔ **None is read live by a walker** — the census's live `cite:` reads are `src/domain/prose/holderTable.js`'s, and this file carries none. Two addresses go stale (`:319` `riskToleranceOf`, `:145-149` `isOffStage`); `:122` `WHEREABOUTS_STATES` sits ABOVE the insertion and does not move. §12.1 records it. |

---

## 8. Ordered coding sequence

0. Dispatch and seal; stop on any preflight mismatch. ⓘ EM-B1k's manifest row reads `LANDED` as of
   `63e40fe57`; if a later lane has placed EM-B1k2 ahead of this packet, `validate` will refuse on
   `tests/domain/roadsParticipation.test.js` — re-check before dispatch (§4, §7.1).
1. **Capture the baselines before the first edit:** `check-writer-reach`,
   `check-observed-shape-readers`, the engine chunk's size from a real build through the exclusive
   mutex, and the kit's per-module attribution.
2. **Reproduce and add the failing tests.** ⭐ Run A1's fixture FIRST and observe
   `securityBand:'holding'`, `seatWeight01:0.3744`, `rulerId:'a:ruler'` for a jailed ruler — this
   lane executed it at `19c4cb853` (evidence §6). If it does not reproduce, **STOP**.
3. **Implement the arm** — §6 exactly, in `src/domain/roads/state.js`, and nothing else.
4. **APPEND the walker's ninth row** and its reasoned omission only. Confirm green; delete the row
   locally and confirm A3's set-equality REDS by name; restore.
5. **Run focused verification** (§10), then the byte step: a real build through the mutex,
   per-module attribution showing **only `src/domain/roads/state.js` moved**, growth ≤370 B, the
   engine chunk `< 679_000`.
6. ⛔ **LAST: `npm run build:edge-shared`**, then confirm exactly SEVEN `_shared` paths moved and
   record both `sourceHash` transitions. It re-stamps declared paths, so nothing may follow it.
7. Write the completion receipt.

```text
1. isInStasis(npc)                                            ⇒ off-stage
2. o := (npc is a non-null object) ? npc : null
3. OFF_STAGE_STATUSES.includes(String(o?.status||'').toLowerCase())  ⇒ off-stage
4. w := o ? o.whereabouts : null ; w?.state === 'hostage'      ⇒ off-stage
5. otherwise                                                   ⇒ on-stage
```

---

## 9. Acceptance matrix

| ID | Case | Required observation | Test home |
|---|---|---|---|
| **A1** | ⭐ **THE SEAT LAW, EXECUTED (design §15).** | A jailed ruler through `readWarSeatBooks` → `interestKind:'realm'`, `settlementWeight01:1`, `seatWeight01:0`, `securityBand:'unseated'`, `rulerId` undefined — identical to the landed `dead` case. ⛔ **Anchored:** an `active` ruler in the same arm reads `holding`, `0.3744`, `a:ruler`. Both halves already executed at `19c4cb853` (evidence §6). | `tests/domain/warSeatBooks.test.js` |
| **A2** | **THE ARM, EXACT AND DERIVED.** | `OFF_STAGE_STATUSES` equals `['exiled','jailed','removed']` and `Object.isFrozen` is true — so a widening of `NPC_UNAVAILABLE_STATUSES` reds here rather than changing behaviour silently. `isOffStage` TRUE for the three and for `'JAILED'`; ⛔ **FALSE for `active`, `dead`, `missing`, `retired`, `'sabbatical'`, `''`, a non-string (`0`), and a record with NO `status` key.** The `missing`/`retired` rows are what pin Q1. | `tests/domain/roadsState.test.js` |
| **A3** | **REGRESSION ANCHOR** — travel is still narrative. | The four existing `it`s read as before. **No new title.** | `tests/domain/roadsState.test.js` |
| **A4** | ⛔ **ABSENCE FROM PARTICIPATION IS NOT ABSENCE FROM THE RECORD.** | `buildWorldSnapshot` excludes a jailed NPC from the participation view **and** `save.settlement.npcs` still contains them by id. | `tests/domain/roadsParticipation.test.js` |
| **A5** | **DORMANCY** — zero allocation. | The existing `plain` fixture still yields the **same settlement reference**. **No new title**; `tests/property/roadsDormancyGolden.test.js` is the corpus-wide half, run via `checks`. | `tests/domain/roadsParticipation.test.js` |
| **A6** | ⛔ **NOTHING MOVES.** | Both golden suites bytewise unchanged. Proved, not argued: **0 of 5,171 corpus NPCs across all 525 rows** carry any of the three (executed at `19c4cb853`), and no `src/` writer can assign one to an NPC. Run via `checks`; no new title. | `tests/property/…` |
| **A7** | ⭐ **THE WALKER'S NINTH ROW, GUARDED.** | The walker is green with the appended row; **deleting it REDS by name** (post-edit `state.js` is FLAGGED — it spells `'dead'` at the `filter` — and would be absent from `DECLARED_FLAGGABLE`, which A3 asserts set-equal); the guard-the-guard arm passes on `'dead'`. Simulated through the walker's own `offencesOf` (evidence §7). | `tests/domain/roadsParticipation.test.js` drives it; the walker's own suite proves the red |

Seven of ≤8.

---

## 10. Verification commands

```sh
npx eslint src/domain/roads/state.js tests/domain/roadsState.test.js \
  tests/domain/warSeatBooks.test.js tests/domain/roadsParticipation.test.js \
  tests/lint/statusUnionTotality.walker.test.js
npm run typecheck:ratchet && npm run typecheck:domain:strict

npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/roadsState.test.js tests/domain/roadsParticipation.test.js \
  tests/domain/warSeatBooks.test.js tests/domain/warSeatTermination.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/statusUnionTotality.walker.test.js
# ⛔ GOLDEN POSTURE — must not move (roadsDormancyGolden copies EM-B1k's own landed row)
npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js \
  tests/property/roadsDormancyGolden.test.js
# ⛔ THE R18 FAMILY — the density lane's own pins, run because this packet feeds it
npx vitest run --pool=threads --maxWorkers=2 \
  tests/generators/densityLaw.test.js tests/domain/advanceWorkerByteIdentity.test.js

node scripts/check-observed-shape-readers.mjs      # motion is a STOP
node scripts/check-writer-reach.mjs                # shrink = --write; growth = a chair act
node scripts/implementation-packets.mjs validate

# ⛔ ABSOLUTELY LAST — it re-stamps declared paths; nothing may follow it
npm run build:edge-shared
```

---

## 11. Mandatory STOP conditions

1. ⛔ Any non-terminal packet holds `tests/domain/roadsParticipation.test.js` at the dispatch tip
   (EM-B1k2 above all) — `validate` refuses (§4, §7.1).
2. ⛔ The defect does not reproduce at step 2 (a jailed ruler already reads `unseated`).
3. ⛔ `NPC_UNAVAILABLE_STATUSES` is not `['dead','exiled','jailed','removed']` at the base.
4. ⛔ Either golden suite moves, or `roadsDormancyGolden` or `tests/generators/densityLaw.test.js`
   reds.
5. ⛔ `check-writer-reach` or `check-observed-shape-readers` **grows**.
6. ⛔ Engine growth exceeds 370 B, any module other than `src/domain/roads/state.js` moved, the
   engine lands ≥ `679_000`, or any worker ceiling reds.
7. ⛔ The walker demands anything beyond one appended roster row, or the derived `TRIGGER` is no
   longer `['dead','exiled','retired']`.
8. ⛔ Any consumer needs an edit — the whole premise is inheritance through one chokepoint.
9. ⛔ Fewer or more than SEVEN `_shared` paths move.
10. ⛔ The delta in `state.js` exceeds 6 effective lines, or a file outside the manifest must change.
11. ⛔ **The §13 reproduction does not hold at the build's own tip** — re-run the two probes
    (`v3-dm-repro.mjs`, `v3-house-repro.mjs` in the compile scratch) before the first edit.

---

## 12. Completion receipt

Record, with executed output: the step-2 reproduction both ways; the seven consumer sites
re-confirmed **unedited** by diff; both golden suites, the roads dormancy golden and the density
suite; the seven `_shared` paths and both `sourceHash` transitions; writer-reach and observed-shape
before/after; the byte step (engine before/after, per-module attribution, worker readings); the
lighting DELTA actually caused (`+4 titles` predicted) **as a delta**; the walker's red when the
ninth row is removed; **and §13's four post-arm cells re-executed at the built tip.**

### §12.1 · NOTICED, NOT TOUCHED — each specific enough to slot

1. ⛔ **THE CHARTER'S OWN RULING TEXT FOR THE NINTH ROW IS REFUTED BY THE WALKER** (§1.1, Q1).
   Slot: **the chair corrects the amendment of 2026-09-19 17:36 ruling (3)** in the same sitting
   that promotes this packet — the omission names `exiled, jailed, missing, removed, retired`,
   never `active`.
2. ⚠ **`tests/domain/roadsParticipation.test.js` is wanted by EM-B1f AND EM-B1k2** (EM-B1k's claim
   retired when it flipped to `LANDED` at `63e40fe57`). Slot: **the chair fixes the placement
   order** before the second of the two is placed (Q3).
3. ✓ **CLOSED, owned elsewhere:** the participation ratchet's scan roots are still only
   `src/domain/worldPulse` and `src/domain/spatial` (`roadsParticipation.test.js:428`), so
   `src/domain/density/factionLifecycle.js:117` is still outside them — **EM-B1k2 owns the
   widening to `src/domain/density`, and its pre-proof adds `src/generators/density`** (charter,
   2026-09-20 02:53). Not this packet's.
4. ⚠ **`src/domain/worldPulse/tierOutcomeApply.js:143-149` writes `status: 'remnant'` to
   institutions, and `'remnant'` is NOT a member of the `EntityStatus` typedef**
   (`src/domain/entities/status.js:23` = `active|impaired|removed|destroyed|vacant`), which
   `statusUnionTotality.walker.test.js` A1 pins at exactly five. Six of that function's seven
   branches write it. Slot: **a totality question for EM-B1h's family** (the `worldPulseFate` /
   entity-status vocabularies) — measure whether any reader tests `status === 'remnant'` and
   whether the typedef or the writer is wrong.
5. ⚠ **`tests/lint/.tuning-inventory.json`'s `line: 222` for `ROADS_TUNING` goes stale** with this
   edit. Not asserted (the walker keys on `spanDigest`), so it re-takes with that file's next
   regeneration. No action owed.
6. ⚠ **Nine `docs/**` prose citations address `src/domain/roads/state.js` by line**; two
   (`:319` `riskToleranceOf`, `:145-149` `isOffStage`) go stale by ~+9. No walker reads them.
   Slot: **whoever next edits those volumes** re-addresses; recorded here so it is not re-found.
7. ⚠ **`src/generators/density/titularSuccession.js` is DARK** — it calls `factionRosterOf` at
   `:177`/`:231` with no importer in `src/`. ✓ Now owned: **EM-B1k2's pre-proof gives it its
   disposition row** (charter, 2026-09-20 02:53). Recorded so it is not re-slotted.
8. ⚠ **`tests/property/npcs.property.test.js:40`** asserts the status is one of five, omitting
   `removed` and `jailed`; it cannot red because its arbitrary never generates a `status`. A
   vacuous arm worth one line when someone is next in that file.

---

## 13. ⭐⭐ THE PROMOTION MEASUREMENT — ANSWERED BY EXECUTION AT THE CURED TREE

Version 2's block was: *the participation filter is the base the tick's writes are built from, so
adding `jailed` to it would erase a person from the save and dissolve their house.* **EM-B1k
changed the write base** (`pulseKernel.js` `:184` and `:579`, landed at `19c4cb853`). This lane
re-ran **both** reproductions at that tree, driving the packet's own arm through an in-memory
override of the read tip, and the answer is unambiguous.

**How it was driven, stated so it can be refused:** the probes run inside
`$SP/read-tip-32602dc60`, whose `src/` differs from `19c4cb853` **only** in
`src/domain/worldPulse/pulseKernel.js`'s two seam lines (`git diff --stat 32602dc60 19c4cb853`
prints `pulseKernel.js | 4 ++--` and two test files and docs — quoted in evidence §1). A Node
loader replaces that ONE module's source with the landed text at its own URL, and — when
`ARM_MODE=on` — replaces `roads/state.js` with §6's exact post-edit text. Both overrides throw if
their anchors stop matching, so a vacuous run is impossible.

### §13.1 · The DM's own reproduction — a generated seven-person town, the shipped shelve writer, one tick with `commit: true`, the real store commit

| cell | kernel | arm | subject | store roster | DB roster | `return-npc` | verdict |
|---|---|---|---|---|---|---|---|
| 1 | **pre-cure** | off | DM-shelved | **6 of 7** | **6 of 7** | `npc_target_missing` | ⛔ **ERASED** (v1's finding, reproduced) |
| 7 | **pre-cure** | ⭐ **ON** | `status:'jailed'` | **6 of 7** | **6 of 7** | `npc_target_missing` | ⛔ **ERASED** — *exactly the harm version 2 refused to ship* |
| 2 | **landed** | off | DM-shelved | 7 of 7 | 7 of 7 | `{"ok":true}` | ✓ SURVIVES |
| 3 | **landed** | off | `status:'jailed'` | 7 of 7 | 7 of 7 | `npc_not_in_stasis` | ✓ SURVIVES (arm absent ⇒ inert) |
| **4** | **landed** | ⭐ **ON** | `status:'jailed'` | **7 of 7** | **7 of 7** | `npc_not_in_stasis` | ⭐ **SURVIVES** |
| **5** | **landed** | ⭐ **ON** | `status:'exiled'` | **7 of 7** | **7 of 7** | — | ⭐ **SURVIVES** |
| **6** | **landed** | ⭐ **ON** | `status:'removed'` | **7 of 7** | **7 of 7** | — | ⭐ **SURVIVES** |

⭐ **Cell 7 against cell 4 is the whole answer:** the same arm, the same fixture, the same tick —
erasure before EM-B1k, no erasure after. **Adding `jailed`, `exiled` or `removed` to the filter can
no longer erase a person from the save.**

### §13.2 · The sole-member house — the R18 reproduction, eight statuses, four cells

At the **landed** kernel, `ARM_MODE=off` and `ARM_MODE=on` produce **byte-identical rows for all
eight statuses**:

| sole Weaver | pre-cure, arm off | pre-cure, ⭐arm on | **landed, arm off** | **landed, ⭐arm on** |
|---|---|---|---|---|
| `active` | stands | stands | stands | stands |
| `stasis` | ⛔ dissolved, person gone | ⛔ dissolved | ✓ stands, person kept | ✓ stands, person kept |
| `hostage` | ⛔ dissolved, person gone | ⛔ dissolved | ✓ stands, person kept | ✓ stands, person kept |
| **`jailed`** | stands | ⛔ **dissolved, person gone** | stands | ⭐ **stands, person kept** |
| `exiled` | dissolved, **person kept** | dissolved, person kept | dissolved, person kept | **dissolved, person kept — UNCHANGED** |
| `removed` | dissolved, person kept | dissolved, person kept | dissolved, person kept | **dissolved, person kept — UNCHANGED** |
| `missing` | stands | stands | stands | stands |
| `retired` | stands | stands | stands | stands |

⭐ **THE ARM CHANGES NOTHING IN THIS CONSEQUENCE SPACE AT THE CURED TREE** — the `landed, arm off`
and `landed, arm on` columns are identical, row for row.

⛔ **And the reason `exiled` and `removed` dissolve at all is NOT this packet.** It is
`ROSTER_ABSENT_STATUSES = Object.freeze(['dead','exiled','removed'])`
(`factionLifecycle.js:87`) read by `isOnRoster` (`:96-99`) — a **pre-existing** R18 reading that
fires identically with the arm absent, and that keeps the person in the record. The charter's
sentence *"every reading in the estate already counts `removed` absent"* is confirmed by
execution. `jailed` is the reversible one, and it now dissolves nothing.

### §13.3 · What this does NOT claim

- ⚠ **EM-B1k2's sharper finding still stands and is not this packet's:** `fresh` falls back to
  `tickStart` when an update entry carries no `.settlement` (`factionDensityKernel.js:721-723`), so
  EM-B1k's refusal rests on an invariant nothing pins. **EM-B1k2 removes that dependency** and is
  chartered behind EM-B1k. This packet does not rely on the unpinned invariant for its own claim —
  cells 4–6 are executed end-to-end through the shipped entry point — but the chair should know
  the defence in depth is not yet in the tree (Q2).
- The probes drive ONE generated town and ONE two-house fixture, not the corpus. The corpus-wide
  claim this packet makes is the golden one (§6), and it is measured over all 525 rows.

### §13.4 · The questions that remain — numbered for the chair, not waited on

- **Q1 — the charter's ruling (3) text is refuted; does the chair amend it?** The walker convicts
  `omits: ['active','dead','missing','retired']` twice. The packet carries the lawful
  `ALL_BUT_DEAD` row. **The chair amends the ledger or overrules the walker; the packet does not
  adjudicate.**
- **Q2 — ✓ ANSWERED WHILE THIS LANE RAN.** EM-B1k was flipped to `LANDED` at `63e40fe57`, and
  CURE-E (`9f3455b84`) and CURE-F (`96036427f`) landed behind it. None of this packet's nineteen
  measured paths moved. **Placement is free; no chair act is owed.**
- **Q3 — the placement order on `tests/domain/roadsParticipation.test.js`**, now a two-way question:
  EM-B1f and EM-B1k2 (DRAFT, not yet in the register) both declare it, and the validator permits
  ONE non-terminal owner. The cheapest order measured: place and build EM-B1f, flip it, **then**
  place EM-B1k2 and re-run its pre-proof at that tip — which is also the charter's own order
  (EM-B1f is FIRST in train EM-T6 because it blocks EM-B1a). The reverse works too, at the cost of
  one extra re-base.
- **Q4 — the walker's `omits` still reads backwards under FORM B** (it lists the words the arm
  ACTS ON, because they are derived rather than spelled). Lawful, and the `why` says so. Should
  the walker gain a `spelling: 'derived'`? Evidence §7. **A tooling question, not this packet's.**
