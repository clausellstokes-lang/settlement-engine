# EM / EM-R0b — `recordInvariants`: THE TOWN'S OWN INVARIANTS AS A MODULE (the re-entry family)

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR — verified LIVE at `e5bdfd031` as `b90a95b7af484137ecf70bd15cde5054edf66b5db0d9f6e90c974d97b7caa5e1`, R-2; §P2 rows 10–12)

- **Status:** `DRAFT`
  ⚠ The status value stands ALONE on its line because `parsePacketHeader` anchors the status row at end-of-line.
- **Packet version:** 2

  **What version 2 changed and why.** The chair's rulings of 2026-09-19 18:30 and 19:29 (design §22.3;
  charter amendments) answered all five of version 1's questions, and a new tree `e5bdfd031` was measured.
  (1) ⭐ **The four band checks now read THE PRODUCER'S OWN TABLES by import** — EM-R0d gives the
  legitimacy, readiness and food-security ladders a `src/data/` home and this packet imports it DIRECTLY
  (the 19:29 ruling 2: no `src/domain/` re-export). **Every learned envelope is deleted.** Version 1's
  sandwich was narrower than the truth and would have convicted a DM's honest edit.
  (2) ⭐ **STOP-3b is STRUCK and the five evidence checks stay LIVE.** §22.3 item 1 splits the receipt BY
  PATH: only `repairs[]` and `authoredTensions`-class facts about the ACT of generation are HISTORY;
  `checks[]` and `judgments[]` with their evidence are READINGS, merged like any other. Version 1's
  contradiction is resolved by the design's own by-path law, so no exemption is needed.
  (3) **The six cross-key checks SHIP** (§22.3 item 5), and the `stress ≡ stressors` identity JOINS as a
  thirtieth check — measured byte-identical on **525 of 525**, not merely 63.
  (4) **The food band check is TOLERANT of rounding** (EM-R0d's STOP-2, ruled): the record publishes
  `deficitPct`/`surplusPct` rounded while the label was computed unrounded. §6 states the rule and A5
  proves it on a constructed boundary.
  (5) **The flag table is taught on the FULL 525 rows; the module's own test controls on the 63-row
  stride** (§22.3 item 6). A second measured rounding hazard forced this: **24 of 525 rows would be
  convicted by a check that DERIVED the flags from the published number** (R-31), so the module declares
  what the generator DOES and derives nothing.
  (6) The second leaf is renamed `recordInvariantFlags.js` — with the envelopes gone, the only declared
  data left is the flag table and the KNOWN pin.
  (7) `V-STRESS-NAME` and the recon's band pair 4 stay DROPPED (version 1's controls, re-confirmed).

- **Verified base:** `__BASE__`
  ⭐ **The chair's revalidation sentence:** *"Re-measured by an Opus COMPILE lane at `e5bdfd031`
  (evidence R-1…R-34). The control is UNCHANGED from version 1's tree: 1 violating row of 525, and it is
  the pin (R-1b). All three change-manifest targets are ABSENT (R-17); all five `requiredSymbols` rows
  resolve VERBATIM (R-18); the packet CREATEs only, so `retiredSymbols` is EMPTY (R-19). THE BUDGETS, BY
  THE IMPORT GRAPH: the generation worker's closure and `EAGER_FIRST_PAINT_MODULES` are BOTH unmoved —
  220 → 220 and 283 → 283 modules, with EM-R0d's leaf present in the overlay (R-14) — and the
  counterfactual in which a generator imports this leaf moves both by **+2** (it drags the ladder leaf in
  with it). The lighting census moves by this packet's own delta alone (§7); the mutation-coverage
  manifest does NOT move (R-16); this packet MODIFIES no `src/` file, so the line-addressed and stamped
  registers (steps 13 and 15) are untouched (R-32)."*
- **Last revalidated:** `__BASE__` — stamped by the chair at promotion.
- **Depends on:** ⭐ **EM-R0d** (it exports the three ladders this packet reads; its second version drops
  the `src/domain/` re-export and this packet imports `src/data/bandLadders.js` directly — charter 19:29
  ruling 2) and **EM-R0a** (its group register is what these invariants CLOSE; §5.4 is the table its
  register is checked against). The charter's order is `EM-R0a (v2) · EM-R0d · EM-R0b (v2)`.
  ⚠ EM-R0d's four ladder symbols are **not yet in the tree**, so by the standard they are NOT
  `requiredSymbols` rows here; the manifest carries a `_pending` note naming exactly the four the chair
  adds at promotion, once the base is at or after EM-R0d's landing.
- **Collision group:** **NONE.** No packet of the manifest's 189 entries reserves any of this packet's
  three paths (R-20). ⚠ EM-A2a also CREATEs into `src/domain/edit/` (`pools.js`) — a different file;
  CREATE targets are checked per FILE, not per directory.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** **measured, at `e5bdfd031`.** Measured: the control re-run at the new tree and
  widened to 525; the TRUE carrier count of every check; an isolating planted mutant for all 30; the four
  ladders by execution at every boundary; the rounding rule on the real corpus and on constructed
  boundaries; the readiness-producer question; the flag table at 525 and at 63; the emitted-chunk
  placement with EM-R0d's leaf in the overlay; the domain→generators ratchet; the lighting, mutation,
  observed-shape, line-addressed and stamped registers. Every figure carries its command in
  `EM-R0b.evidence.md`.

---

## 1. Reconciled authority

1. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §22.3** (the chair, 2026-09-19 18:30; ODQ §934.47 addendum
   25) — item 1 splits the generation receipt BY PATH and keeps the five evidence invariants LIVE; item 5
   ships the cross-key checks and adds `stress ≡ stressors`; item 6 sends the three ladders to EM-R0d and
   rules *"Envelopes LEARNED from a corpus are narrower than the truth and would convict a DM's honest
   edit"*, with the flag data DECLARED and taught on the FULL 525-row corpus, the module's own test
   controlling on the 63-row stride.
2. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §22.2 item 3** — the module's commission. Items 8 and 10 are
   what it gates; item 12 is the delta's numbers, not this packet's.
3. **`docs/implementation/charters/EDIT-MODE-TRAIN.md`**, amendment of 2026-09-19 19:29 — EM-R0d's ruling:
   the three ladders CONFIRMED by execution at every boundary; **ruling 2, no `src/domain/` re-export**;
   and for THIS packet, *"the food-security band check takes the record's ROUNDED inputs, so it accepts a
   label that ANY unrounded value within half a unit of the published one would yield"* and *"it measures
   which producer's label lands on `defenseProfile.readiness.label`"*.
4. **The charter's amendment of 2026-09-19 18:30** — the family order `R0a (v2) · R0d · R0b (v2)`.
5. `docs/implementation/PACKET_STANDARD.md`, `EM-PREAMBLE.md` §P1–§P9 (§P2 rows 10–12).
6. The measured ground, re-run at `e5bdfd031` — `EM-R0b.evidence.md`.

Resolved contradictions, each measured:

- ⭐⭐ **THE READINESS PRODUCERS CANNOT DISAGREE, AND THE REASON IS NOT WHAT THE NAME SUGGESTS.** EM-R0d
  found five readiness labels in `src/generators/power/rulingStructure.js:733-742`. Measured: that file
  contains **ZERO lines naming `readiness`** (R-22). Its labels are `_provDefLabel`, a PROVISIONAL defence
  label used only as a FALLBACK INPUT to the legitimacy score —
  `const legitimacyDefenseLabel = projection.defenseLabel || _provDefLabel;` (`:744`) — and it never
  reaches `defenseProfile.readiness.label`. Confirmed end to end:
  `defenseProfile.readiness.label === readinessBandOf(score)` on **525 of 525** rows, **0 disagreements**
  (R-22). → `V-BAND-READINESS` ships as an exact check. ⚠ `_provDefLabel` is nonetheless a SIXTH,
  undeclared spelling of the readiness vocabulary — and it omits `Fortress`, so the fallback can never say
  it. That is §12's noticed-not-touched row 1, slotted to EM-R0d's second version.

- ⭐⭐ **THE ROUNDING HAZARD IS REAL AND HAS TWO INSTANCES, ONE OF WHICH NOTHING HAD NAMED.**
  (a) The LABEL: the record publishes `deficitPct` rounded while the label was computed unrounded. On the
  corpus today no row sits exactly on a cut, so an exact check passes 525/525 — but on a constructed
  boundary an exact check **CONVICTS an honest record**: published `deficitPct = 15` with label
  `Import-Dependent` (unrounded 15.4) and published `5` with `Pressured` (unrounded 5.3) both red under an
  exact ladder and pass under the tolerant one, while `Deficit` at 15 and `Import-Dependent` at 5 still red
  under both (R-23). §6 states the rule; A5 is that table.
  (b) ⭐ **THE FLAGS, which nothing had measured.** The food card runs TWO ladders on the same number: the
  LABEL cuts at 15 (open below), the FLAGS cut at 20 (`isDeficit: d > 20`, `isPressured: d > 5 && d <= 20`
  — `foodGenerator.js:475-478`). Of the 36 rows with published `deficitPct` in `(15, 20]`, **12 published
  at 19 carry `isPressured` and 24 published at 20 carry `isDeficit`** — because the flag ladder read an
  unrounded value above 20 (R-31). **A check that derived the flags from the published number would
  convict 24 of 525 honest rows.** → The module DECLARES the observed label→flags table and derives
  nothing, exactly as §22.3 item 6 rules. The 15-versus-20 disagreement itself is the OWNER'S decision
  point (ODQ §934.57), not this packet's.

- ⭐ **THE FIVE EVIDENCE CHECKS NEED NO EXEMPTION, AND VERSION 1'S STOP-3b IS STRUCK.** Version 1 measured
  that carrying `generationCoherenceReceipt` untouched as HISTORY makes five checks convict a consistent
  record, leaving item 3's escalation no legal step. §22.3 item 1 resolves it by the same by-path law item
  7 itself states: `checks[]` and `judgments[]` with their evidence are **READINGS**, merged like any
  other, each judgment atomic. They relate a READING to held facts; the escalation's first step is lawful.
- ⭐ **AND THE RECOMPUTE HAZARD CANNOT REACH THIS MODULE.** EM-R0a v2 measured that the receipt is MERGED,
  NEVER RECOMPUTED, because a recompute moves
  `judgments[id=confidence_and_provenance].evidence[path=simulationTrace]` on 63 of 63 records. Measured
  here: **525 of 525** rows carry that evidence entry, its `evidence` VALUE is `undefined`, and **none of
  the five checks' matchers matches it** — three key on `e.path` being `history.currentTensions[i]`,
  `stress[0]` or `conflicts[i]`, two on `e.evidence` being a numeric string (R-24). The module ABSTAINS on
  that entry under every merge policy. → No contract change; recorded so EM-R0c need not re-derive it.

- The two candidates the recon REJECTED, `V-STRESS-NAME` and the recon's band pair 4 stay dropped with
  their controls (§5.3), re-confirmed at this tree.

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** a PURE module `src/domain/edit/recordInvariants.js` exporting
`recordInvariants(settlement) → violations[]`, each `{ id, path, message, kind }` with
`kind ∈ arithmetic · count · prose-count · referential · band · flag-vector`, **clean on every plain
generated record of the golden corpus except one dated, named, single-row KNOWN pin**, whose four band
checks read THE PRODUCERS' OWN TABLES by import and which declares **no threshold and no learned
envelope**. It lands in neither the generation worker nor the first-paint set.

**Definition of done:** thirty checks, each with its own control and a planted mutant that trips it and
nothing else; the rejected and dropped candidates recorded WITH their controls; the only declared data is
the label→flags table (taught on 525, controlled on 63) and the KNOWN pin.

In scope: the instrument as a pure module; its control and mutant table; nothing else.

Explicit non-goals:

- **the merge, the escalation and the delta model** — EM-R0c's. This packet ships the predicate.
- **the class register and the groups** — EM-R0a's. §5.4 hands it the table its register is checked against.
- **the ladders** — EM-R0d's. This packet IMPORTS them and defines none.
- **the corpus ratchet** — EM-R7's.
- **FIX-G1**, and the food card's 15-versus-20 flag question (the owner's, ODQ §934.57).
- ⛔ This packet is a READER. It writes nothing and changes no generator.
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget |
|---|---:|
| Behavior families | `1` |
| New persisted record families · Named state writers · Feature flags · User-facing surfaces | `0` |
| Direct consumers | `0` at landing (EM-R0c is the first; the acceptance test is the only caller) |
| New logic-bearing production leaves | `1` — `recordInvariants.js`. `recordInvariantFlags.js` is pure declared data, no branch |
| Existing logic-bearing production files modified | `0` |
| Additional registration-only files | `0` |
| Handwritten files total | `3` — the cap is 12 |
| New/changed effective production lines | `≤ 300` |
| Effective lines per new leaf | `≤ 215` (`recordInvariants.js`) · `≤ 40` (`recordInvariantFlags.js`, smaller than v1's 60 — the envelopes are gone) — the cap is 250 |
| Delta in a shared/hot file | `n/a` — no hot file is named |
| Acceptance cases | `8` — the cap |
| ⭐ Generation-worker byte delta | **`0 B`** — not in the worker's closure, 220 → 220 (R-14) |
| ⭐ First-paint delta | **`0`** — `EAGER_FIRST_PAINT_MODULES` 283 → 283 (R-14) |

Overrides approved before dispatch: `NONE`.

⚠ **IDENTITY POSTURE: byte-identical output.** This packet adds a reader and changes no generator. Neither
golden appears in §7 and neither may move.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-R0b
```

Expected: all three targets ABSENT; every `requiredSymbols` row resolving verbatim. **Read dry at
`e5bdfd031` (R-21)**: status supplied by promotion; manifest validates (3 change rows, actions in
`PACKET_ACTIONS`, 8 acceptance cases as `{id, case}` objects, no duplicate change path across 189
packets); base an ancestor of HEAD; the substrate check EXCLUDES CREATE rows so the substrate is the five
`requiredSymbols` paths plus the TEST row, none of which this packet writes; CREATE targets absent and
Git-clean.

⚠ **`checks` ORDER (brief step 14a):** neither declared command is a generator — both are plain
`npx vitest run` — so no step re-stamps a declared path mid-chain and `check:packet` reaches every step.
The boundary ratchet is a `checks` command and a `requiredSymbols` row, deliberately **not** a manifest
row (EM-P3's ruling Q3): this packet must leave `BASELINE_EDGES` byte-identical, and a lane that moved it
between the stamp and the dispatch makes this packet refuse.

## 5. Verified tree contract

### 5.1 ⭐ THE FOUR BAND LADDERS — all read by import, none copied

| reading | the table this packet imports | form | control | evidence |
|---|---|---|---|---|
| `activeConditions[].severityBand` | `src/domain/activeConditions.js#severityBand` (`:541`) — a pure `0..1 → band` function | **EXACT**, over EVERY entry | 525/525 clean, 516 conditions judged | R-5, R-8 |
| `powerStructure.publicLegitimacy.label` | `src/data/bandLadders.js#legitimacyBandOf` (EM-R0d) — cuts 75/60/45/30, CLOSED below | **EXACT** | 525/525 clean | R-25 |
| `defenseProfile.readiness.label` | `src/data/bandLadders.js#readinessBandOf` (EM-R0d) — cuts 76/55/38/24/12, CLOSED below | **EXACT** | 525/525 clean | R-22, R-25 |
| `economicState.foodSecurity.label` | `src/data/bandLadders.js#foodSecurityBandOf` + `FOOD_SECURITY_CUTS` (EM-R0d) — 40/15/5 on the deficit, 40 on the surplus, **OPEN below**, famine outranking | **EXACT, TOLERANT OF ROUNDING** (§6) | 525/525 clean | R-23 |

⛔ **No envelope is learned and no threshold is written in this packet.** A4 asserts it by scanning the
module's own source for a numeric literal outside the declared flag table.

### 5.2 The rest of the verified contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| The one domain producer | `src/domain/activeConditions.js` | `export function severityBand` | `:541`; the module has **ZERO imports of its own**, 43,420 B, and is already in both budgeted graphs (R-13) | IMPORT it. ⛔ Never copy its thresholds |
| The ladder leaf | `src/data/bandLadders.js` | `legitimacyBandOf` · `readinessBandOf` · `foodSecurityBandOf` · `FOOD_SECURITY_CUTS` | ⚠ **CREATED BY EM-R0d; ABSENT at this compile's tip** (R-18b). Imported at the `src/data/` address, NOT a `src/domain/` re-export (charter 19:29 ruling 2; ninety-six domain files already import `src/data` directly) | IMPORT the `src/data/` address |
| The layering ratchet | `tests/build/domainGeneratorsBoundary.test.js` | `const BASELINE_EDGES` | 4 importing files / **5 live edges**; cardinality guard `≤ 6`; a per-file arm that REDS on any NEW importer regardless of the headroom (R-6) | These leaves import **no** `src/generators` module. A8 asserts it |
| Worker ceiling | `tests/build/generationWorkerLazy.test.js` | `WORKER_BUNDLE_CEILING_BYTES` | `= 1401208`; this packet's leaves are **not in the worker's closure** (R-14) | ⛔ READ ONLY, not a manifest row. EM-R0d carries the ceiling row for the ladder leaf |
| First-paint closure | `vite.config.js` | `EAGER_FIRST_PAINT_MODULES` · `computeEngineSharedDomain` | 283 modules; the seed is ONLY `src/generators` imports of `src/domain` (R-14) | ⛔ READ ONLY. Delta 0 |
| Corpus helper | `tests/helpers/goldenMasterCorpus.js` | `goldenCorpus` · `keyOf` | *"THE 525-ROW CONFIGURATION CORPUS, ONE SPELLING"* | The test's corpus. ⛔ Never a second spelling |
| Generator entry | `src/generators/generateSettlementPipeline.js` | `generateSettlementPipeline` (`:77`) | the pipeline the control runs | The test calls it; the MODULE never does |
| Census instrument | `tests/lint/sovereigntyLightingContract.walker.test.js` | `TEST_FILES` · `measureCensus` · `parkReasonsFor` | `titles`/`suiteTitles` sum over **credited** files only (`:602-605`); `parkReasonsFor = (src) => classify(src).reasons` (`:1670`). ⛔ **NO ABSOLUTE TUPLE IS QUOTED** — the live register is measuredBy EM-P0 and already behind three landings (R-15) | §7's delta; the chair stamps the absolute |
| Mutation-coverage set | `tests/lint/mutationCoverage.shared.mjs` | `ENFORCER_DIRS` · `NAME_PATTERN` | the eight dirs (`:36-45`); a file outside them is picked only if its BASENAME matches `NAME_PATTERN` (`:65-75`); `recordInvariants.test.js` matches neither (R-16) | §7's zero row, and the naming pin below |
| Test precedent (corpus) | `tests/property/generatorGoldenMaster.test.js` | `it('every config produces byte-identical output to the golden master')` | a whole-corpus generate-and-assert loop, literal titles | Copy this shape for A1 |
| Test precedent (unit) | `tests/domain/activeConditions.test.js` | its `describe`/`it` shape | a plain domain unit test beside the module this packet imports | Copy this shape for A3–A8 |
| Golden posture | `tests/property/generatorGoldenMaster.test.js` · `tests/property/dossierProseManifest.test.js` | — | neither appears in §7 | UNCHANGED; motion is a STOP |

Forbidden alternatives:

- ⛔⛔ **NO `src/generators/**` MODULE MAY IMPORT EITHER NEW LEAF.** Measured on an overlay:
  `EAGER_FIRST_PAINT_MODULES` **283 → 285** and the worker closure **220 → 222**, gaining BOTH this leaf
  and EM-R0d's ladder leaf (R-14). EM-P3's P-20g, reproduced and now doubled.
- ⛔⛔ **THE MODULE IMPORTS NOTHING FROM `src/generators/**`.** Even a correct exported ladder there is a
  new `src/domain → src/generators` edge against a shrink-only ratchet whose own message says *"invert it
  … rather than widening the baseline"* (R-6). That refusal is why EM-R0d exists.
- ⛔ **NO THRESHOLD IS WRITTEN AND NO ENVELOPE IS LEARNED.** A4 scans the module's source. The domain-side
  copies of the legitimacy ladder (`rulingPower.js#rebandLegitimacy`, `timeProgression.js`) are
  module-local and are not the producer; reading one would be a copied number laundered through a third
  module. EM-R0e re-points them at the leaf; this packet touches neither.
- ⛔ **NO FLAG IS DERIVED FROM A PUBLISHED NUMBER.** Measured: that convicts 24 of 525 honest rows (R-31).
  The label→flags table is DECLARED.
- ⛔ **NO CHECK SHIPS WITHOUT A PLANTED MUTANT** (`V-STRESS-NAME`'s class).
- ⛔ **NO LEARNING AT RUNTIME.** `recordInvariants(settlement)` takes ONE record. A second parameter is STOP-1.
- ⛔ **NO BLANKET TOLERANCE FOR THE KNOWN DEFECT** — one named row with its date and its cure's packet id,
  asserted STILL PRESENT. Not a skip, not a filter, not a widened epsilon.
- ⛔ **THE TEST FILE'S BASENAME MAY NOT CARRY A `NAME_PATTERN` TOKEN** (`…contract.test.js` and
  `…walker.test.js` both MATCH and would silently owe a mutation-coverage row — R-16).
- ⛔⛔ **THE TEST FILE NEVER BINDS `it`, `test` OR `describe` AS A VARIABLE OR PARAMETER** (brief step 14b:
  the census resolves an opener only where the module binds that word EXACTLY ONCE; 26 of the estate's 384
  parked files park for that reason alone, EM-B1e among them). Straight-line literal `it`s under ONE
  literal `describe` — no `.each`, no loops, no nesting, no conditionals — or the promised `credited +1`
  lands as `parked +1` and the `titles +8` counts nowhere.
- no new top-level key; no persisted shape; no flag; no PRNG; no clock; no store; no files outside the manifest.

### 5.3 ⭐ THE REJECTED AND THE DROPPED — recorded so nobody re-adds them

| candidate | why it may not convict | the control that refused it |
|---|---|---|
| `criticalIssueCount = issues.length` | counts only the CRITICAL issues | REJECTED by the recon: false on **6 of 63** plain records |
| `foodRatio = production / need` | the ratio counts imports and magic | REJECTED by the recon: false on plain thorps and hamlets |
| `V-STRESS-NAME` | **it cannot convict** — zero violation return sites, the only check of which that is true | DROPPED at v1, re-confirmed: 7 mutations, 7 abstentions, 0 convictions (R-4) |
| the recon's band pair 4 | the **same number** as pair 3 on 525/525; the only cross-key band pair | DROPPED at v1 as redundant (R-9) |
| ⭐ **every LEARNED band envelope** (v1's three) | **narrower than the truth; they would convict a DM's honest edit** — and the corpora disagree (readiness `Well-Defended` `[56..75]` at 63 vs `[55..75]` at 525) | STRUCK by §22.3 item 6; replaced by EM-R0d's tables read by import |
| ⭐ **deriving the food flags from the published `deficitPct`** | the flags were computed from the UNROUNDED value and cut at 20 where the label cuts at 15 | REFUSED by its own control: it convicts **24 of 525** honest rows (R-31) |

### 5.4 ⭐ FOR EM-R0a — every check, and the group or keys that close it

EM-R0a v2's six groups are `G1 food-security` · `G2 food-balance` · `G3 viability-counts` ·
`G4 isolation-support` · `G6 condition-severity` · `G7 defense-readiness`. Every check below names what
closes it; **this table is what EM-R0a's register is checked against.**

| check | kind | record paths related | closed by |
|---|---|---|---|
| `V-DEPCOUNT` | count | `economicViability.metrics.dependencyCount` · `.dependencies` | **G3** (member list) |
| `V-WARNCOUNT` | count | `economicViability.metrics.warningCount` · `.warnings` | **G3** |
| `V-SUMMARY-DEPS` | prose-count | `economicViability.summary` · `.dependencies` | **G3** |
| `V-SUMMARY-HOOKS` | prose-count | `economicViability.summary` · `.plotHooks` | **G3** |
| `V-FOOD-RAW` · `V-FOOD-DEF` · `V-FOOD-PCT` | arithmetic | `economicViability.metrics.foodBalance.*` | **G2** (whole) |
| `V-FOODSEC-RAW` · `V-FOODSEC-DEF` · `V-FOODSEC-PCT` | arithmetic | `economicState.foodSecurity.*` | **G1** (whole) |
| `V-FOODSEC-CHAINS` | count | `economicState.foodSecurity.{activeChainsCount,activeChains,chains}` | **G1** |
| `V-FOODSEC-FLAGS` | flag-vector | `economicState.foodSecurity.{label,is*}` | **G1** |
| `V-BAND-FOODSEC` | band | `economicState.foodSecurity.{deficitPct,surplusPct,label}` | **G1** |
| `V-ISOLATION` | arithmetic | `isolationSupport.{deficit,requiredCapacity,capacity}` | **G4** |
| `V-COND-BAND` | band | `activeConditions[].{severity,severityBand}` | **G6** (per-entry) |
| `V-BAND-READINESS` | band | `defenseProfile.readiness.{score,label}` | **G7** (whole) |
| `V-INCOME-100` | arithmetic | `economicState.incomeSources[].percentage` | **ATOMIC**, not a group (§22.3 item 3) |
| `V-POWER-100` | arithmetic | `powerStructure.factions[].power` | **ATOMIC** + HELD (§22.2 item 8: the OP keeps the total) |
| `V-LEGIT-SUM` | arithmetic | `powerStructure.publicLegitimacy.{score,breakdown}` | **HELD-internal** — the merge never merges a HELD key, so no group could fire |
| `V-LEGIT-FLAGS` | flag-vector | `powerStructure.publicLegitimacy.{label,is*}` | **HELD-internal** |
| `V-BAND-LEGITIMACY` | band | `powerStructure.publicLegitimacy.{score,label}` | **HELD-internal** |
| `V-GOVERNING` | count | `powerStructure.factions[].isGoverning` · `.governingName` | **HELD-internal** |
| `V-FLAGVEC` | flag-vector | `economicState.foodSecurity.*` (**G1**) · `powerStructure.publicLegitimacy.*` (**HELD-internal**) | both, per arm |
| ⛔ `V-EVIDENCE-ROSTER` | prose-count | `generationCoherenceReceipt.judgments[]` (READING) · `npcs` · `relationships` | **CROSS-KEY** — escalates at both keys |
| ⛔ `V-EVIDENCE-EVENTS` | prose-count | `…judgments[]` · `history.historicalEvents` | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-TENSION` | referential | `…judgments[]` · `history.currentTensions` | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-STRESS` | referential | `…judgments[]` · `stress.label` | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-CONFLICT` | referential | `…judgments[]` · `conflicts` | **CROSS-KEY** |
| ⛔ `V-DEFENSE-INST` | referential | `defenseProfile.institutions` · `institutions` | **CROSS-KEY** (and EM-R6's removal sweep) |
| ⛔ `V-STRESS-IDENTITY` ⭐NEW | referential | `stress` · `stressors` | **CROSS-KEY** — two top-level keys, byte-identical 525/525 (R-26) |

**Thirty checks.** Seven are cross-key and ship under §22.3 item 5. ⭐ Every group G1–G7 is exercised by at
least one check, and no check names a group EM-R0a does not declare.

## 6. Exact contracts

```js
// src/domain/edit/recordInvariants.js — PURE. No store, PRNG, clock, locale, I/O, no mutation
// of the argument. ⛔ NO src/generators IMPORT.
import { severityBand } from '../activeConditions.js';
import { legitimacyBandOf, readinessBandOf, foodSecurityBandOf } from '../../data/bandLadders.js';
import { FLAGS_EVER_TRUE, FLAG_FIELDS, KNOWN_VIOLATIONS, FLAG_CORPUS } from './recordInvariantFlags.js';

/**
 * @typedef {'arithmetic'|'count'|'prose-count'|'referential'|'band'|'flag-vector'} InvariantKind
 * @typedef {{ id: string, path: string, message: string, kind: InvariantKind }} Violation
 * @param {Record<string, any>} settlement  ONE record. Never a corpus.
 * @returns {Violation[]}  EMPTY when consistent. Declared check order, then array index.
 *                         Never null, never undefined, never throws.
 */
export function recordInvariants(settlement) { /* … */ }
```

- The parameter is **`settlement`** — the estate's receiver prior, so every read grounds against the shape
  the observed-shape corpus histograms (§7).
- Three outcomes per check: a violation, `true`, or **ABSTAIN**. Absent or mistyped inputs ABSTAIN;
  abstention is never reported.
- `violation.path` is the record path of the **derived** leaf that disagrees.

### The band checks

Three are a plain call: `legitimacyBandOf(score).label === label`, `readinessBandOf(score).label === label`,
`severityBand(c.severity) === c.severityBand` — for EVERY `activeConditions` entry, not index 0.

⭐ **The fourth is TOLERANT OF ROUNDING, and this is a contract, not a slack.**

```text
V-BAND-FOODSEC, given published deficitPct d, published surplusPct s, and the famine flag f:
  ADMISSIBLE = { foodSecurityBandOf(f, d', s') : d' in {d-0.5, d, d+0.5}, s' in {s-0.5, s, s+0.5} }
  CONVICT iff the record's label is not in ADMISSIBLE.
```

The record publishes both percentages ROUNDED; the label was computed from the UNROUNDED values, so any
unrounded input within half a unit of the published one could honestly have produced the label. Measured
(R-23) — on the corpus both forms pass 525/525, and on constructed boundaries:

| published | label | EXACT | TOLERANT | what it is |
|---|---|---|---|---|
| `d=15, s=0` | `Import-Dependent` | ⛔ CONVICTS | ✓ passes | honest: unrounded 15.4 (the ladder is OPEN below) |
| `d=15, s=0` | `Pressured` | ✓ | ✓ | honest: unrounded 14.6 |
| `d=15, s=0` | `Deficit` | ⛔ | ⛔ **still reds** | genuinely wrong, two rungs away |
| `d=5, s=0` | `Pressured` | ⛔ CONVICTS | ✓ passes | honest: unrounded 5.3 |
| `d=5, s=0` | `Secure` | ✓ | ✓ | honest: unrounded 4.8 |
| `d=5, s=0` | `Import-Dependent` | ⛔ | ⛔ **still reds** | genuinely wrong |

The tolerance is **exactly half a unit and never more**: it admits the two labels adjacent across the one
cut the published value sits on, and nothing further.

### The flag-vector check

```text
for each declared (objectPath, flags[]) and the object's own `label`:
  1. the label was NEVER observed  -> ABSTAIN ENTIRELY.
  2. a flag that is FALSE          -> NEVER judged.
  3. a TRUE flag in the label's observed set -> pass.
  4. a TRUE flag in NO observation of that label -> CONVICT.
```

⭐ **Against the chair's stated default:** the per-FLAG rule and *"a violation only if the label was seen
with a DIFFERENT vector in every observation"* are **the same rule for a TRUE flag**. They differ in one
direction only, and the recon's is narrower: a flag always TRUE beside a label but FALSE in the record is
not convicted here — that case is `V-LEGIT-FLAGS` and `V-FOODSEC-FLAGS`. The two halves are complementary.

⛔ **THE WHOLE-VECTOR FORM IS REFUSED** — `Import-Dependent` carries two live vectors and `Endorsed`
carries `isApproved` as well (`isApproved: score >= 60` has no upper bound), so vector equality would
convict 12 legitimate records outright (R-27).

⭐ **THE TABLE IS WHAT THE GENERATOR DOES, NOT A RECONCILIATION** (§22.3 item 6), taught on the **FULL 525
rows** (`FLAG_CORPUS` names it in the module's header):

| object | label → flags EVER true |
|---|---|
| `economicState.foodSecurity` | `Secure`→`isSecure` · `Pressured`→`isPressured` · **`Import-Dependent`→`isDeficit`(196)+`isPressured`(12)** · `Deficit`→`isDeficit` · `Deficit — Active Famine`→`isDeficit` |
| `powerStructure.publicLegitimacy` | `Endorsed`→`isEndorsed`+`isApproved` (23) · `Approved`→`isApproved` · `Tolerated`→`isTolerated` · `Contested`→`isContested` · `Legitimacy Crisis`→`isLegitimacyCrisis` |

The `Import-Dependent` second vector is not noise: it is the **12 rows published at `deficitPct = 19`**,
where the flag ladder's cut of 20 has not yet been crossed (R-31). `isSurplus` is declared and true beside
no label (`Surplus` appears on 0 of 525 rows), so a `Surplus` record ABSTAINS by rule 1.

⚠ **Every label present at 525 is present in the 63-row stride** (R-28), so the module's own test controls
on 63 without losing a row of the table.

### Absence, ordering, determinism, lifecycle

- absent reading/object, non-number score, non-string label, label outside the table: **ABSTAIN**.
- `violations`: an empty array. ⛔ Never `null`, never `undefined`.
- a check that throws: ⛔ forbidden — total over any input by construction. A7 is the proof.
- **Order:** declared check order, then array index. No content sort.
- ⚠ **The arithmetic checks carry a `|difference| ≤ 1` tolerance** (the generator rounds); `V-ISOLATION`
  carries none. A mutant of `+1` on a tolerant check trips NOTHING, measured (R-12a) — A3's mutants are
  sized to clear each check's own tolerance.
- Hash/fork key `NONE`; no PRNG, clock, locale or `Intl`.
- **Lifecycle:** module load, frozen, read-only. Never persisted or projected; every column `n/a`.
- Flag `NONE`. Golden posture **UNCHANGED**. Alignment `DECLARED EMPTY`.
  Edit story: `ENGINE-ONLY — EM-R0c's delta card is where a violation becomes words.`
- Closed kinds: the six §22.2 item 3 names, and no seventh. DM-only fields `NONE`; a violation is an
  engine diagnostic. ⛔ EM-R0c must not print one to a player surface without its own projection rule.

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/recordInvariants.js` | `recordInvariants` | `≤ 215` | The thirty checks of §5.4. ⛔ Import ONLY `../activeConditions.js`, `../../data/bandLadders.js` and `./recordInvariantFlags.js`. ⛔ No `src/generators` import, no `src/components` import, no numeric threshold, no corpus. Parameter named `settlement`. |
| `CREATE` | `src/domain/edit/recordInvariantFlags.js` | `FLAGS_EVER_TRUE`, `FLAG_FIELDS`, `KNOWN_VIOLATIONS`, `FLAG_CORPUS` | `≤ 40` | Pure frozen data, §6's table verbatim. **Import nothing.** The header names the corpus (525 rows, `goldenCorpus`), the measuring sha, and that the table records what the generator DOES across its two ladders. |
| `TEST` | `tests/domain/recordInvariants.test.js` | A1–A8 | `n/a` | ⛔ **Straight-line literal `it`s under ONE literal `describe`; never bind `it`, `test` or `describe` as a variable or parameter** (§5.2). No `.each`, loops, nesting or conditionals. `// anchored:` above every negative assertion. The basename may gain no `NAME_PATTERN` token. |

Generated artifacts: `NONE`. Neither leaf is in an edge-shared bundle closure (the five entry modules are
`src/domain/{aiCharter,aiGrounding,aiOutputSchema,intentAtlas}.js` and `src/lib/analyticsEvents.js`,
none of which reaches `src/domain/edit/`). **No declared `checks` command writes any path** (step 11):
both are `npx vitest run` on files this manifest already names.

No other file may be edited.

**Predicted register moves, priced here rather than discovered at the terminal:**

| Register | Moves? | Predicted delta | Door |
|---|---|---|---|
| Lighting census | **YES** | `files +1` · `credited +1` · `parked +0` · `titles +8` · `suiteTitles +1`. ⚠ `credited +1` is conditional on §5.2's registration pin — a bound `it`/`test`/`describe` makes it `parked +1` and the titles count nowhere (step 14b). ⛔ No absolute is restated; the live register is measuredBy EM-P0 and behind three landings (R-15) | interior red; re-derived at the terminal by the chair |
| Mutation-coverage | **NO** | zero — `tests/domain` is not an `ENFORCER_DIR` and the basename matches no `NAME_PATTERN` token (R-16). ⚠ Both halves matter |
| ⚠ Observed-shape readers | **PRICED, RED-FIRST AT THE BUILD** | a NEW file has ceiling 0. Evidence for zero: the inventory's 9 findings naming a key this module reads are **all on the wrong receiver**, and this module reads each at its correct home (R-16b) — evidence, not proof | §8 step 7: scan before and after; **STOP for the chair** on any identity. The lane never mints |
| ⭐ Prose-numerics (line-addressed, step 13) | **NO** | zero — this packet **MODIFIES no `src/` file**, so no baselined row can shift above or below. Its own new files carry no baseline rows (R-32) |
| ⭐ Wiring census (stamped, step 15) | **NO** | zero — `stamp.files` holds **7** entries and none is under `src/domain/edit/` or `src/data/bandLadders.js`; and this packet modifies no stamped file (R-32) |
| Writer-reach · edge-shared · decision-fork · bundle ceilings | **NO** | zero — no record key is written, no chunk delta (R-14) |

## 8. Ordered coding sequence

0. Dispatch and seal. **Re-run R-6's live-edge scan** and confirm EM-R0d is LANDED with its four symbols
   present; if either has moved, STOP and re-compile.
1. **Capture the baseline, RED FIRST.** Run the observed-shape reader scan in report mode and record the
   new leaves' absence. Record the live lighting tuple from a throwaway `git archive` probe outside the
   worktree.
2. Add the failing arms for A1–A8 against modules that do not exist yet.
3. Create `src/domain/edit/recordInvariantFlags.js` — the table **transcribed from A6's own measurement**,
   not from this packet's prose.
4. Create `src/domain/edit/recordInvariants.js` — the checks in §5.4's declared order, the four band
   checks calling the imported ladders.
5. No wiring. ⛔ **Import this module from nothing.** EM-R0c is the first importer.
6. Run focused verification (§10).
7. ⭐ **THE REGISTER STEP.** Re-run the observed-shape scan. Any new finding identity on either leaf is a
   **STOP for the chair** with the identities quoted — do not mint, do not rename a variable to dodge the
   heuristic, do not widen the inventory.
8. Write the completion receipt, quoting A1's row count and wall-clock, A3's per-check verdicts, A5's
   boundary table, and the observed-shape scan's before/after.

Bounded algorithm:

```text
1. violations = []
2. For each EXACT check in declared order: evaluate; ABSTAIN -> next; consistent -> next;
   otherwise push { id, path, message, kind }.
3. V-COND-BAND: for every activeConditions entry, severityBand(c.severity) vs c.severityBand.
4. V-BAND-LEGITIMACY / V-BAND-READINESS: the imported ladder's label vs the record's.
5. V-BAND-FOODSEC: the record's label must be in the ADMISSIBLE set of §6.
6. V-STRESS-IDENTITY: stress and stressors must be byte-identical when both are present.
7. V-FLAGVEC: for each declared flagset, every TRUE flag absent from the label's observed set.
8. Return violations. Never null. Never throw.
```

## 9. Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | ⭐ THE CONTROL | the 63-row stride (`sample63`), generated plainly | `recordInvariants` returns `[]` on 62 rows; `town\|germanic\|mountain\|mountain_pass\|civilized\|golden-master-v3` returns exactly the pinned `V-SUMMARY-DEPS`. The violating-row SET equals `KNOWN_VIOLATIONS`, both directions. ⚠ The DECLARED table is the 525-row one (§22.3 item 6); this arm is the module's own control on the stride | `tests/domain/recordInvariants.test.js` |
| A2 | THE PIN BITES BOTH WAYS | `KNOWN_VIOLATIONS` | the pinned row is asserted **STILL PRESENT** with its exact message, so curing the generator REDS this arm and FIX-G1 must remove the pin. ⛔ Not a skip, not a filter, not a widened tolerance | same |
| A3 | ⭐ GUARD THE GUARD | for each of the 30 checks, the smallest mutation of a real record (§5.4, R-12, R-29) | each mutation returns **exactly that check's id and no other**, each step clearing its check's own tolerance. ⚠ `V-FLAGVEC`'s foodSecurity arm needs an `Import-Dependent` carrier — `V-FOODSEC-FLAGS` covers only four labels and ABSTAINS on `Import-Dependent` and `Deficit — Active Famine`, so on a `Secure` carrier both checks fire (R-30) | same |
| A4 | ⭐ NO THRESHOLD, NO ENVELOPE | the module's own source text | the four band checks call `legitimacyBandOf` / `readinessBandOf` / `foodSecurityBandOf` / `severityBand`, and `recordInvariants.js` contains no numeric literal outside its tolerance constants | same |
| A5 | ⭐ THE ROUNDING RULE | constructed records at `deficitPct = 15` and `= 5` | both adjacent labels pass at each cut; a label two rungs away REDS. §6's six-row table, executed | same |
| A6 | THE FLAG DATA IS DECLARED, NOT DERIVED | the 63-row stride | the shipped `FLAGS_EVER_TRUE` covers every label the stride shows, and a flag DERIVED from the published `deficitPct` is shown to disagree with the record on the `(15,20]` rows — the reason the table is declared | same |
| A7 | ABSENCE, UNSEEN LABEL, PURITY, IDEMPOTENCY | a record with `economicViability`, `isolationSupport` and `activeConditions` deleted; a record whose `foodSecurity.label` is unknown; one real record | `[]` for the first two; the record deep-equal to a pre-call clone; two calls equal | same |
| A8 | THE LAYERING BOUNDARY HOLDS | the live `src/domain` → `src/generators` edge scan | the live set equals `BASELINE_EDGES`; neither new leaf appears | `tests/build/domainGeneratorsBoundary.test.js` (run, not edited) |

## 10. Verification commands

```sh
npx eslint src/domain/edit/recordInvariants.js src/domain/edit/recordInvariantFlags.js \
  tests/domain/recordInvariants.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/recordInvariants.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/build/domainGeneratorsBoundary.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/property/generatorGoldenMaster.test.js \
  tests/property/dossierProseManifest.test.js --maxWorkers=2

node scripts/check-observed-shape-readers.mjs --report   # report mode NEVER writes the baseline
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-R0b
npm run check:tail
```

⚠ **`gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0** — a gate line with no printed test count DID NOT
RUN. The env prefix above is the long-poll spelling; the receipt must print each run's own count.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`, stop if:

- the dispatch seal is missing, invalid, or belongs to another worktree state;
- resume reports authority, HEAD, foreign-work or receipt-integrity drift;
- **STOP-1 — a second argument.** Any pressure to make `recordInvariants` take a corpus, a second record
  or an observation set. A runtime learner is what §22.2 item 3 and §22.3 item 6 both refuse;
- **STOP-2 — EM-R0d is not LANDED, or its four symbols are spelled otherwise than §5.2 names them.** This
  packet cannot build against absent ladders, and it may not define one to proceed;
- **STOP-3 — the food ladder's cuts or its OPEN-below operator change in EM-R0d.** The rounding rule of §6
  is written against `>` at 40/15/5; a closed-below ladder needs the admissible set re-derived;
- **STOP-4** — the observed-shape scan gives either leaf any finding identity (§8 step 7);
- **STOP-5** — either golden moves, or `tests/build/domainGeneratorsBoundary.test.js` reds;
- **STOP-6** — any check ships without a planted mutant that isolates it.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the next
wave. ⛔ **This packet may not cure the generator defect it pins**, and it may not reconcile the food
card's two ladders — that is the owner's decision point (ODQ §934.57).

## 12. Completion receipt

- Base SHA · dispatch bundle and seal identity · final commit or working-tree state:
- Exact changed files and effective-line deltas:
- Acceptance cases; focused commands, exits and counts (⚠ each with its own printed test count):
- A1's row count and wall-clock; A3's per-check verdict table; A5's boundary table:
- Observed-shape reader scan, before and after:
- Lighting census tuple, measured in a throwaway probe:
- Sealed per-step receipt and exact-state resume status; both typecheck configurations:
- Wave-end gate stages actually executed; base-versus-wave failure identity diff:
- Dormancy/golden result; bundle/first-paint result (predicted: worker +0, eager +0):
- Generated artifacts: `NONE` · Deviations: `NONE | STOP` · Judgment calls: `NONE`

**Noticed, not touched — each slotted, nothing deferred (the owner's law of 2026-09-19 18:10):**

1. ⭐ `src/generators/power/rulingStructure.js:733-742` `_provDefLabel` is a **SIXTH, undeclared spelling
   of the readiness vocabulary**, derived from institutions with no score and feeding the legitimacy
   score — and it omits `Fortress`, so the fallback can never say it. → **EM-R0d's second version**: it
   reads the leaf's labels, and the leaf's header names the fallback as a declared consumer.
2. `V-FOODSEC-FLAGS`'s own map covers **four of the six** food labels (`Secure`, `Pressured`, `Deficit`,
   `Surplus`) and silently ABSTAINS on `Import-Dependent` and `Deficit — Active Famine`, where `V-FLAGVEC`
   is the sole check (R-30). It is correct as it stands but the asymmetry is undocumented in the recon's
   instrument. → **this packet's own §6**, where it is now written down; no separate slot needed.
3. The `V-EVIDENCE-*` checks' TRUE carrier counts range from **242 to 525 of 525** — `V-EVIDENCE-CONFLICT`
   is exercised by fewer than half the corpus (R-25). → **EM-R7's corpus ratchet**: it records the carrier
   count beside each check so a check that stops being exercised is visible.
4. `judgments[id=confidence_and_provenance].evidence[path=simulationTrace]` carries an `evidence` value of
   **`undefined` on 525 of 525 rows** — the visible symptom of EM-R0a's finding that the receipt is built
   before the trace is propagated (R-24). No shipped check reads it. → **EM-R0c's compile**, which already
   owns the receipt-recompute finding.
