# EM / EM-R0b — `recordInvariants`: THE TOWN'S OWN INVARIANTS AS A MODULE (the re-entry family)

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR — ⭐ **re-verified LIVE at `141a1d775` as
`16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195`** by the pre-proof lane;
the version-3 compile's `b90a95b7…` is SUPERSEDED — §P2 row 11 took its second amendment in the
window; §P2 rows 10–12)

- **Status:** `DRAFT`
  ⚠ The status value stands ALONE on its line because `parsePacketHeader` anchors the status row at end-of-line.

  ⭐ **READY-able at `141a1d775`** — every verified fact re-executed at that tip by the Opus
  pre-proof lane (evidence P-1…P-10), ONE acceptance contract open for the chair (§12 question 1:
  the stride is undefined in the tree).
- **Packet version:** 4

  **What version 3 changed and why.** The chair's ruling of 2026-09-20 on EM-R0c's compile report
  (questions 2 and 4) sent three things here, and a new tree `32602dc60` was measured.
  (1) ⭐ **`CHECK_META` SHIPS** — an exported, total, per-check table naming the record KEYS each
  check relates, and **`Violation` gains a `keys` field**. EM-R0c's escalation ladder requires BOTH
  keys of a cross-key violation at step 1 (design §22.2 item 3, §22.3 item 5) and version 2 exported
  nothing it could read; EM-R0c's STOP-4 is what this discharges. ⭐ The table's own derivation
  reproduces version 2's **seven** cross-key checks exactly, by a rule version 2 stated in prose but
  never encoded (V3-2b).
  (2) ⭐ **THREE NEW CHECKS, 30 → 33.** Measuring the chair's question 4 found a third shape neither
  document had named: **one fact stored at TWO record paths**, on 525 of 525 rows, which the merge
  can put out of step **63/63 with a clean control** (V3-3d). `V-DEFENSE-MAGICDEP`,
  `V-DEFENSE-TRADITIONS` and `V-HISTORY-AGE` close it. Each has its control (63/63 clean) and an
  isolating mutant (convicts 63/63, trips nothing else).
  (3) ⭐ **THE CHAIR'S QUESTION 4 IS ANSWERED, AND THE ANSWER IS NO, TWICE.** No invariant ties the
  leaves of `defenseProfile.scores` (every candidate identity holds 0/525; `disaster` is REFUTED as a
  function of the other five) and none ties the leaves of `history.founding` (`overcoming` is REFUTED
  as a function of both `initialChallenge` and `reason`). **Both of EM-R0c's MIXED readings are
  LAWFUL and no group is owed for either.**
  (4) **Version 2's rulings on the food card are UNTOUCHED** — the rounding tolerance and the
  DECLARED flag table stand exactly as ruled, because every fact that forced them re-measures
  identically at this tip (V3-4).
  (5) Every version-2 figure was RE-EXECUTED at `32602dc60` in the command that quotes it. ⚠ Version
  2's harness pointed at `read-tip-e5bdfd031`; nothing was carried.
  (6) A2 is RE-CUT, not added: the pin's both-directions arm folds into A1, which already asserted the
  violating-row SET equals `KNOWN_VIOLATIONS` in both directions. The cap of eight holds.

  **What version 4 changed and why (the Opus PRE-PROOF lane, 2026-09-20 07:0x EDT, at `141a1d775`).**
  No premise is refuted and no figure moved: every version-3 headline was RE-EXECUTED at the new tip
  and every one holds to the row (P-4). Four things changed.
  (1) ⭐ **THE LIGHTING DELTA IS CORRECTED, `titles +8` → `titles +7`.** Version 3 priced eight
  titles from its eight ACCEPTANCE CASES, but §9 homes **A8 in `tests/build/domainGeneratorsBoundary.test.js`
  — "run, not edited"** — so it lands no `it` in the new file. The census sums `liveTitlesIn` over
  CREDITED files, and an unedited file contributes nothing (P-6). Inherited from version 2, which
  made the same count.
  (2) ⭐⭐ **"THE 63-ROW STRIDE" IS UNDEFINED IN THE TREE** and no arithmetic stride over the 525-row
  corpus yields 63 (P-7). A1, A3, A6 and A7 all name it and A1's "62 rows clean" is its cardinality.
  It is §12 question 1, with a measured menu and a recommendation; the figure is a MARKED PLACEHOLDER
  until the chair rules.
  (3) The preamble hash is re-stamped and §P2 row 11's second amendment is priced (P-8); the §12
  receipt's bundle row now says HOW `+0` is established, because a skipped byte arm is no longer a pass.
  (4) The three references to "§12 question 1" are re-cut: the chair ALREADY RULED it at ODQ §934.47
  addendum 38 ruling 1 (**BOTH** — the check here, the `history` group at EM-R0a), and §12 carried no
  numbered questions at all (P-9).

- **Verified base:** `__BASE__`
  ⭐ **The chair's revalidation sentence:** *"Re-measured by an Opus COMPILE lane at `32602dc60`
  and RE-EXECUTED WHOLE by an Opus PRE-PROOF lane at `141a1d775` (evidence V3-1…V3-7, then P-1…P-10).
  The J-T1 window over every change-manifest and every requiredSymbols path is EMPTY — no path this
  packet names moved in the 24 commits between the two tips (P-1) — and the generator's 224-module
  import closure contains none of the five `src/` files that did move, so every corpus figure carries
  by construction and was re-executed anyway (P-3, P-4). The control is UNCHANGED: 1 violating row of
  525, and it is the pin (P-4). The three duplication identities hold 525/525 at this tip, D1 525/525,
  D3 zero, D4 36 rows / 12 `isPressured` / 24 `isDeficit`, D5 reproducing the label→flags table to the
  row including `Import-Dependent → isDeficit(196) + isPressured(12)`, and D7 525/525 undefined (P-4).
  `CHECK_META` is TOTAL over the 33 declared checks in both directions and every one of the 14
  top-level keys it names is a key EM-R0a v2 classes, with no path heading at a key its own row omits
  (V3-2; CONDITIONAL on EM-R0a's landing, which is the family's order). The three new checks each
  carry a control clean on 63/63 plain records and an isolating mutant that convicts 63/63 and trips
  nothing else (V3-3e). The budgets are unmoved — `EAGER_FIRST_PAINT_MODULES` measured BY IMPORTING
  THE SET holds 268 modules and 0 `src/domain/edit/**`, and nothing anywhere in `src/` imports that
  directory (P-5); this packet MODIFIES no `src/` file, so the line-addressed and stamped registers
  are untouched (P-5). The lighting delta is `files +1 · credited +1 · parked +0 · suiteTitles +1 ·
  titles +7` — SEVEN, not eight, because A8 is homed in an unedited file (P-6)."*
- **Last revalidated:** `__BASE__` — stamped by the chair at promotion.
  ⚠ **THE BASE IS NOT FREE.** `assertAncestorAndSubstrate` builds its substrate from the non-CREATE
  change rows **UNION every `requiredSymbols` path**, and refuses when `git diff <base>..<HEAD>`
  touches any of them. At promotion the chair moves the five `_pending` rows into `requiredSymbols`,
  which puts `src/data/bandLadders.js` and `src/domain/edit/recordRegister.js` into the substrate —
  files EM-R0d and EM-R0a CREATE. **So the base must be stamped AT OR AFTER both landings**, or the
  dispatch throws `verified-base descendant changed declared substrate`. Quoted and proved at P-10.
- **Depends on:** ⭐ **EM-R0d** (it exports the three ladders this packet reads; this packet imports
  `src/data/bandLadders.js` directly — charter 19:29 ruling 2) and **EM-R0a** (its group register is
  what these invariants CLOSE; §5.4 is the table its register is checked against). The family's order
  is now `EM-R0a · EM-R0d · EM-R0b v3 · EM-R0f · EM-R0c · R1–R5 · R6 · R7` (the chair, 2026-09-20).
  ⚠ EM-R0d's four ladder symbols are **not yet in the tree**, so by the standard they are NOT
  `requiredSymbols` rows here; the manifest carries a `_pending` note naming exactly the four the
  chair adds at promotion.
- **Collision group:** **NONE.** No packet reserves any of this packet's four paths. ⚠ EM-A2a also
  CREATEs into `src/domain/edit/` (`pools.js`) and EM-R0c CREATEs two more there — all different
  files; CREATE targets are checked per FILE, not per directory.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** **measured, at `32602dc60`.** The control re-run and widened to 525; the
  `CHECK_META` totality and key-membership arms; the two MIXED objects' functional-dependence tests;
  the three-way split falsifier and its control; an isolating mutant for each new check; every
  version-2 headline figure re-executed. Every figure carries its command in `EM-R0b.evidence.md`.

---

## 1. Reconciled authority

1. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §22.2 item 3** — the module's commission, AND the
   escalation ladder's shape: *"the enclosing declared group from `R1`, then its top-level key from
   `R1` — BOTH keys for a cross-key check"*. Version 3 is what makes that readable.
2. **§22.3 item 1** (the receipt split BY PATH, the five evidence checks LIVE), **item 5** (the
   cross-key checks SHIP; `stress ≡ stressors` joins), **item 6** (the ladders read by import; the
   flag data DECLARED and taught on the FULL 525 rows).
3. **The chair, 2026-09-20**, on EM-R0c's compile report: question 2 — *"EM-R0b goes to VERSION 3"*,
   exporting the per-check key table; question 4 — the two new MIXED objects are measured here.
4. **`docs/implementation/charters/EDIT-MODE-TRAIN.md`**, amendments of 2026-09-19 18:30 and 19:29.
5. `docs/implementation/PACKET_STANDARD.md`, `EM-PREAMBLE.md` §P1–§P9 (§P2 rows 10–12).
6. The measured ground, re-run at `32602dc60` — `EM-R0b.evidence.md`.

Resolved contradictions, each measured:

- ⭐⭐ **"SEVEN CROSS-KEY CHECKS" AND "EIGHT CHECKS NAMING TWO KEYS" ARE BOTH TRUE, AND THE
  DIFFERENCE IS THE CONTRACT.** A naive `keys.length > 1` reads **8**; version 2's §5.4 says
  **seven**; version 2 is right. `V-FLAGVEC` names `economicState` and `powerStructure` and relates
  **neither to the other** — it is two independent arms under one id. Escalating it at both keys
  would take `powerStructure` from `R1` because the FOOD card disagreed, the opposite of the minimal
  move item 3 requires. **RULED: a row may declare `arms`; a violation carries the keys of the arm
  that FIRED, never the union; and `CROSS_KEY_CHECKS` is derived as "no `arms` and more than one
  key" — a derivation that reproduces version 2's seven exactly** (V3-2b).
- ⭐⭐ **THE CHAIR'S QUESTION 4, ANSWERED NO TWICE — AND THE MEASUREMENT FOUND A THIRD SHAPE.** No
  invariant ties `defenseProfile.scores`' own leaves (every candidate arithmetic identity holds
  **0/525**; `disaster` REFUTED as a function of the other five, 4 ambiguous tuples) and none ties
  `history.founding`'s (`overcoming` REFUTED as a function of `initialChallenge`, 4 of 9, and of
  `reason`, 9 of 17). Both MIXED readings are LAWFUL. **But three leaves hold ONE fact at TWO paths
  on 525/525 — `defenseProfile.scores.magicDependency` ≡ `defenseProfile.magicDependency`,
  `scores.traditions` ≡ `defenseProfile.traditions`, `history.founding.age` ≡ `history.age` — and
  the merge splits all three 63/63 with a control that splits nothing** (V3-3d). They ship as checks.
- ⭐ **A GROUP CANNOT CLOSE TWO OF THE THREE.** EM-R0a v2's walker forbids a group root nested inside
  another's, and `G7 defense-readiness` is rooted at `defenseProfile.readiness`; a group rooted at
  `defenseProfile` would nest it. A group rooted at `history` WOULD be legal — and ⭐ **THE CHAIR HAS
  RULED IT: BOTH** (ODQ §934.47 addendum 38, ruling 1, 2026-09-20 02:47 EDT). `V-HISTORY-AGE` DETECTS
  here; the `history` consistency group PREVENTS the split and is **EM-R0a's row, added at EM-R0a's
  pre-proof with this measurement cited**. Nothing is open. The two defence pairs keep detection only,
  by the division §22.2 item 3 draws (V3-3f).
- ⭐ **THE READINESS PRODUCERS CANNOT DISAGREE** — `rulingStructure.js` contains **0 lines naming
  `readiness`** and `defenseProfile.readiness.label === readinessBandOf(score)` on **525 of 525**
  (V3-4 D2, D3). Version 2's finding, re-executed. Its `_provDefLabel` remains EM-R0d's row.
- ⭐ **THE TWO ROUNDING HAZARDS ARE UNCHANGED AT THIS TIP** — 36 rows publish `deficitPct ∈ (15,20]`,
  **12 carrying `isPressured` and 24 carrying `isDeficit`** (V3-4 D4), and the label→flags table
  reproduces version 2's to the row including `Import-Dependent → isDeficit(196) + isPressured(12)`.
  The tolerance rule of §6 and the DECLARED table stand exactly as ruled.
- The candidates the recon REJECTED and version 1 DROPPED stay dropped with their controls (§5.3).

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** a PURE module `src/domain/edit/recordInvariants.js` exporting
`recordInvariants(settlement) → violations[]`, each `{ id, path, message, kind, keys }`, **and
`CHECK_META`** — a total, frozen, per-check table naming the record paths and top-level keys each
check relates, so EM-R0c's escalation ladder can take BOTH keys of a cross-key violation at step 1.
Clean on every plain generated record of the golden corpus except one dated, named, single-row KNOWN
pin; its four band checks read THE PRODUCERS' OWN TABLES by import; it declares **no threshold and no
learned envelope**. It lands in neither the generation worker nor the first-paint set.

**Definition of done:** thirty-three checks, each with its own control and a planted mutant that
trips it and nothing else; a `CHECK_META` row for every one of them, proved total in both directions,
with every key it names present in EM-R0a's register; the rejected and dropped candidates recorded
WITH their controls; the only declared data is the label→flags table (taught on 525, controlled on
63), the KNOWN pin and `CHECK_META`.

In scope: the instrument as a pure module; its per-check metadata; its control and mutant table.

Explicit non-goals:

- **the merge, the escalation and the delta model** — EM-R0c's. This packet ships the predicate AND
  the metadata the ladder reads; it does not walk the ladder.
- **the class register and the groups** — EM-R0a's. §5.4 hands it the table its register is checked
  against, and the `history` consistency group the chair RULED at ODQ §934.47 addendum 38 ruling 1 is
  **EM-R0a's row**, not this packet's.
- **the ladders** — EM-R0d's. This packet IMPORTS them and defines none.
- **the corpus ratchet** — EM-R7's.
- **FIX-G1**, and the food card's 15-versus-20 flag question (the owner's, ODQ §934.57).
- ⛔ This packet is a READER. It writes nothing and changes no generator.

## 3. Hard scope budget

| Limit | Packet budget | Measured |
|---|---:|---|
| Behavior families | `1` | — |
| New persisted record families · Named state writers · Feature flags · User-facing surfaces | `0` | — |
| Direct consumers | `0` at landing (EM-R0c is the first) | V3-6 |
| New logic-bearing production leaves | `1` — `recordInvariants.js`. The two data leaves are frozen tables that take no branch | cap 2 |
| Existing logic-bearing production files modified | `0` | — |
| Additional registration-only files | `0` | — |
| Handwritten files total | `4` | cap 12 |
| New/changed effective production lines | `≤ 340` | cap 400 |
| Effective lines per leaf | `≤ 230` `recordInvariants.js` · `≤ 40` `recordInvariantFlags.js` · `≤ 70` `recordInvariantMeta.js` (**measured 64**) | cap 250 |
| Delta in a shared/hot file | `n/a` — no hot file is named | — |
| Acceptance cases | `8` — the cap; A2 is RE-CUT, not added | §9 |
| ⭐ Generation-worker byte delta | **`0 B`** — 0 `src/domain/edit/**` files in its 220-module closure | V3-6 |
| ⭐ First-paint delta | **`0`** — 0 in `EAGER_FIRST_PAINT_MODULES`' 268 modules | V3-6 |

Overrides approved before dispatch: `NONE`.

⚠ **IDENTITY POSTURE: byte-identical output.** This packet adds a reader and changes no generator.
Neither golden appears in §7 and neither may move.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-R0b
```

Expected: all four targets ABSENT; every `requiredSymbols` row resolving verbatim. Read dry at
`32602dc60`: status supplied by promotion; manifest validates (4 change rows, actions in
`PACKET_ACTIONS`, 8 acceptance cases as `{id, case}` objects, no duplicate change path); base an
ancestor of HEAD; the substrate check EXCLUDES CREATE rows; CREATE targets absent and Git-clean.

⚠ **`checks` ORDER:** **no declared command is a generator** — `npx eslint`, `npm run typecheck:*`
and `npx vitest run` only — so no step re-stamps a declared path mid-chain and `check:packet` reaches
every step. The boundary ratchet is a `checks` command and a `requiredSymbols` row, deliberately
**not** a manifest row: this packet must leave `BASELINE_EDGES` byte-identical.

## 5. Verified tree contract

### 5.1 ⭐ THE FOUR BAND LADDERS — all read by import, none copied

| reading | the table this packet imports | form | control | evidence |
|---|---|---|---|---|
| `activeConditions[].severityBand` | `src/domain/activeConditions.js#severityBand` — a pure `0..1 → band` function | **EXACT**, over EVERY entry | 525/525 clean | V3-1 |
| `powerStructure.publicLegitimacy.label` | `src/data/bandLadders.js#legitimacyBandOf` (EM-R0d) — cuts 75/60/45/30, CLOSED below | **EXACT** | 525/525 clean | V3-1 |
| `defenseProfile.readiness.label` | `src/data/bandLadders.js#readinessBandOf` (EM-R0d) — cuts 76/55/38/24/12, CLOSED below | **EXACT** | **525/525, 0 disagreements** | V3-4 D2 |
| `economicState.foodSecurity.label` | `src/data/bandLadders.js#foodSecurityBandOf` + `FOOD_SECURITY_CUTS` (EM-R0d) — 40/15/5 on the deficit, 40 on the surplus, **OPEN below**, famine outranking | **EXACT, TOLERANT OF ROUNDING** (§6) | 525/525 clean | V3-4 D4 |

⛔ **No envelope is learned and no threshold is written in this packet.** A4 asserts it by scanning
the module's own source for a numeric literal outside the declared tolerance constants.

### 5.2 The rest of the verified contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| The one domain producer | `src/domain/activeConditions.js` | `export function severityBand` | the module has ZERO imports of its own and is already in both budgeted graphs | IMPORT it. ⛔ Never copy its thresholds |
| The ladder leaf | `src/data/bandLadders.js` | `legitimacyBandOf` · `readinessBandOf` · `foodSecurityBandOf` · `FOOD_SECURITY_CUTS` | ⚠ **CREATED BY EM-R0d; ABSENT at this compile's tip.** Imported at the `src/data/` address, NOT a `src/domain/` re-export | IMPORT the `src/data/` address |
| The class register | `src/domain/edit/recordRegister.js` | `RECORD_CLASSES` | ⚠ CREATED BY EM-R0a; ABSENT at this tip. **A2 reads it to prove every `CHECK_META` key is a classed key** | IMPORT it in the TEST only; ⛔ the module imports it NOT |
| The layering ratchet | `tests/build/domainGeneratorsBoundary.test.js` | `const BASELINE_EDGES` | 4 importing files / 5 live edges, live set measured 4 | These leaves import **no** `src/generators` module. A8 asserts it |
| Worker ceiling | `tests/build/generationWorkerLazy.test.js` | `WORKER_BUNDLE_CEILING_BYTES` | these leaves are **not in the worker's 220-module closure** | ⛔ READ ONLY, not a manifest row |
| First-paint closure | `vite.config.js` | `EAGER_FIRST_PAINT_MODULES` | 268 modules; 0 `src/domain/edit/**` | ⛔ READ ONLY. Delta 0 |
| Corpus helper | `tests/helpers/goldenMasterCorpus.js` | `goldenCorpus` · `keyOf` | the 525-row corpus, ONE spelling; it exports `goldenCorpus` and `keyOf` and **NOTHING ELSE** — ⛔ **no stride, sample or slice helper exists anywhere in `tests/`** (P-7) | The test's corpus. ⛔ Never a second spelling |
| ⭐⭐ The stride | — | `__STRIDE_RULE__` | ⛔ **OPEN — §12 question 1.** "The 63-row stride" that A1/A3/A6/A7 name is defined in NO tree artifact, and **no arithmetic stride over 525 yields 63** (measured k∈[2,12], P-7). It is an artifact of EM-R0c's kit prototype carried into a SHIPPING test's contract. The pin sits at corpus index **510** | The rule the chair picks is written HERE as one line of source, so the suite's own sample is reproducible from the tree alone |
| Generator entry | `src/generators/generateSettlementPipeline.js` | `generateSettlementPipeline` | the pipeline the control runs | The test calls it; the MODULE never does |
| Mutation-coverage set | `tests/lint/mutationCoverage.shared.mjs` | `ENFORCER_DIRS` · `NAME_PATTERN` | `recordInvariants.test.js` matches neither | §7's zero row, and the naming pin below |
| Test precedent (corpus) | `tests/property/generatorGoldenMaster.test.js` | its whole-corpus generate-and-assert loop | literal titles | Copy this shape for A1 |
| Test precedent (unit) | `tests/domain/activeConditions.test.js` | its `describe`/`it` shape | a plain domain unit test | Copy this shape for A2–A8 |
| Golden posture | `tests/property/generatorGoldenMaster.test.js` · `tests/property/dossierProseManifest.test.js` | — | neither appears in §7 | UNCHANGED; motion is a STOP |

Forbidden alternatives:

- ⛔⛔ **NO `src/generators/**` MODULE MAY IMPORT ANY OF THE THREE LEAVES.**
- ⛔⛔ **THE MODULE IMPORTS NOTHING FROM `src/generators/**`** — a new `src/domain → src/generators`
  edge against a shrink-only ratchet. That refusal is why EM-R0d exists.
- ⛔ **`recordInvariants.js` IMPORTS `recordRegister.js` NOT.** `CHECK_META` names keys as STRINGS;
  only the TEST reads EM-R0a's register, to prove the names resolve. A runtime import would couple
  the predicate to the register for nothing and enlarge EM-R0c's closure.
- ⛔ **NO THRESHOLD IS WRITTEN AND NO ENVELOPE IS LEARNED.** A4 scans the module's source.
- ⛔ **NO FLAG IS DERIVED FROM A PUBLISHED NUMBER** — measured, it convicts 24 of 525 honest rows.
- ⛔ **NO CHECK SHIPS WITHOUT A PLANTED MUTANT** that isolates it (`V-STRESS-NAME`'s class).
- ⛔ **NO CHECK SHIPS WITHOUT A `CHECK_META` ROW** — A2's totality arm is the red.
- ⛔ **NO LEARNING AT RUNTIME.** `recordInvariants(settlement)` takes ONE record. A second parameter
  is STOP-1.
- ⛔ **NO BLANKET TOLERANCE FOR THE KNOWN DEFECT** — one named row with its date and its cure's
  packet id, asserted STILL PRESENT.
- ⛔ **THE TEST FILE'S BASENAME MAY NOT CARRY A `NAME_PATTERN` TOKEN** (`…contract.test.js` and
  `…walker.test.js` both MATCH and would silently owe a mutation-coverage row).
- ⛔⛔ **THE TEST FILE NEVER BINDS `it`, `test` OR `describe` AS A VARIABLE OR PARAMETER.**
  Straight-line literal `it`s under ONE literal `describe` — no `.each`, no loops, no nesting, no
  conditionals — or the promised `credited +1` lands as `parked +1`.
- no new top-level key; no persisted shape; no flag; no PRNG; no clock; no store; no files outside
  the manifest.

### 5.3 ⭐ THE REJECTED AND THE DROPPED — recorded so nobody re-adds them

| candidate | why it may not convict | the control that refused it |
|---|---|---|
| `criticalIssueCount = issues.length` | counts only the CRITICAL issues | REJECTED by the recon: false on 6 of 63 plain records |
| `foodRatio = production / need` | the ratio counts imports and magic | REJECTED by the recon: false on plain thorps and hamlets |
| `V-STRESS-NAME` | **it cannot convict** — zero violation return sites | DROPPED at v1, re-confirmed |
| the recon's band pair 4 | the same number as pair 3 on 525/525 | DROPPED at v1 as redundant |
| ⭐ every LEARNED band envelope | narrower than the truth; they would convict a DM's honest edit | STRUCK by §22.3 item 6; replaced by EM-R0d's tables read by import |
| ⭐ deriving the food flags from the published `deficitPct` | the flags were computed UNROUNDED and cut at 20 where the label cuts at 15 | REFUSED by its own control: **24 of 525** honest rows (V3-4 D4/D5) |
| ⭐ **NEW at v3** — `disaster` as a function of the other five defence scores | the same five scores yield different `disaster` values | REFUTED: **4 ambiguous tuples of 82** (V3-3b) |
| ⭐ **NEW at v3** — `overcoming` as a function of `initialChallenge` or of `reason` | the founding prose is drawn independently | REFUTED: 4 of 9 and 9 of 17 ambiguous (V3-3c) |
| ⭐ **NEW at v3** — any arithmetic identity over `defenseProfile.scores` | four candidates tried | each holds **0 of 525** (V3-3b) |

### 5.4 ⭐ FOR EM-R0a — every check, and the group or keys that close it

EM-R0a v2's six groups are `G1 food-security` · `G2 food-balance` · `G3 viability-counts` ·
`G4 isolation-support` · `G6 condition-severity` · `G7 defense-readiness`. **This table is what
EM-R0a's register is checked against, and it is now also the source of `CHECK_META`.**

| check | kind | record paths related | keys | closed by |
|---|---|---|---|---|
| `V-DEPCOUNT` · `V-WARNCOUNT` · `V-SUMMARY-DEPS` · `V-SUMMARY-HOOKS` | count / prose-count | `economicViability.{metrics.*,dependencies,warnings,summary,plotHooks}` | `economicViability` | **G3** |
| `V-FOOD-RAW` · `V-FOOD-DEF` · `V-FOOD-PCT` | arithmetic | `economicViability.metrics.foodBalance.*` | `economicViability` | **G2** |
| `V-FOODSEC-RAW` · `V-FOODSEC-DEF` · `V-FOODSEC-PCT` · `V-FOODSEC-CHAINS` · `V-FOODSEC-FLAGS` · `V-BAND-FOODSEC` | arithmetic / count / flag-vector / band | `economicState.foodSecurity.*` | `economicState` | **G1** |
| `V-ISOLATION` | arithmetic | `isolationSupport.{deficit,requiredCapacity,capacity}` | `isolationSupport` | **G4** |
| `V-COND-BAND` | band | `activeConditions[].{severity,severityBand}` | `activeConditions` | **G6** (per-entry) |
| `V-BAND-READINESS` | band | `defenseProfile.readiness.{score,label}` | `defenseProfile` | **G7** |
| `V-INCOME-100` | arithmetic | `economicState.incomeSources[].percentage` | `economicState` | **ATOMIC**, not a group |
| `V-POWER-100` | arithmetic | `powerStructure.factions[].power` | `powerStructure` | **ATOMIC** + HELD |
| `V-LEGIT-SUM` · `V-LEGIT-FLAGS` · `V-BAND-LEGITIMACY` · `V-GOVERNING` | arithmetic / flag-vector / band / count | `powerStructure.{publicLegitimacy.*,factions[],governingName}` | `powerStructure` | **HELD-internal** |
| `V-FLAGVEC` | flag-vector | `economicState.foodSecurity.label` · `powerStructure.publicLegitimacy.label` | `economicState` · `powerStructure` | ⭐ **TWO ARMS, each single-key — NOT cross-key** |
| ⛔ `V-EVIDENCE-ROSTER` | prose-count | `generationCoherenceReceipt.judgments[].evidence` · `npcs` · `relationships` | 3 keys | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-EVENTS` · `V-EVIDENCE-TENSION` | prose-count / referential | `…judgments[].evidence` · `history.*` | 2 keys | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-STRESS` | referential | `…judgments[].evidence` · `stress.label` | 2 keys | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-CONFLICT` | referential | `…judgments[].evidence` · `conflicts` | 2 keys | **CROSS-KEY** |
| ⛔ `V-DEFENSE-INST` | referential | `defenseProfile.institutions` · `institutions` | 2 keys | **CROSS-KEY** (and EM-R6's removal sweep) |
| ⛔ `V-STRESS-IDENTITY` | referential | `stress` · `stressors` | 2 keys | **CROSS-KEY** — byte-identical 525/525 |
| ⭐ `V-DEFENSE-MAGICDEP` **NEW** | referential | `defenseProfile.scores.magicDependency` · `defenseProfile.magicDependency` | `defenseProfile` | **NOTHING** — no legal group (a root at `defenseProfile` would nest G7); the check IS the closure |
| ⭐ `V-DEFENSE-TRADITIONS` **NEW** | referential | `defenseProfile.scores.traditions` · `defenseProfile.traditions` | `defenseProfile` | **NOTHING** — same reason |
| ⭐ `V-HISTORY-AGE` **NEW** | referential | `history.founding.age` · `history.age` | `history` | the check **AND** a `history` group at EM-R0a — the chair RULED BOTH (ODQ §934.47 addendum 38 ruling 1) |

**Thirty-three checks. SEVEN are cross-key** and ship under §22.3 item 5 — `V-FLAGVEC` is not among
them, by the `arms` rule of §6. Every group G1–G7 is exercised by at least one check, and no check
names a group EM-R0a does not declare.

## 6. Exact contracts

```js
// src/domain/edit/recordInvariants.js — PURE. No store, PRNG, clock, locale, I/O, no mutation
// of the argument. ⛔ NO src/generators IMPORT. ⛔ NO recordRegister.js IMPORT.
import { severityBand } from '../activeConditions.js';
import { legitimacyBandOf, readinessBandOf, foodSecurityBandOf } from '../../data/bandLadders.js';
import { FLAGS_EVER_TRUE, FLAG_FIELDS, KNOWN_VIOLATIONS, FLAG_CORPUS } from './recordInvariantFlags.js';

export { CHECK_META, CHECK_IDS, CROSS_KEY_CHECKS } from './recordInvariantMeta.js';

/**
 * @typedef {'arithmetic'|'count'|'prose-count'|'referential'|'band'|'flag-vector'} InvariantKind
 * @typedef {{ id: string, path: string, message: string, kind: InvariantKind,
 *             keys: string[] }} Violation
 * @param {Record<string, any>} settlement  ONE record. Never a corpus.
 * @returns {Violation[]}  EMPTY when consistent. Declared check order, then array index.
 *                         Never null, never undefined, never throws.
 */
export function recordInvariants(settlement) { /* … */ }
```

```js
// src/domain/edit/recordInvariantMeta.js — PURE FROZEN DATA. Imports NOTHING.
/**
 * @typedef {{ kind: InvariantKind, paths: string[], keys: string[],
 *             arms?: { id: string, keys: string[] }[] }} CheckMeta
 */
export const CHECK_IDS = Object.freeze([ /* the 33 of §5.4, in declared order */ ]);
export const CHECK_META = Object.freeze({ /* one row per id */ });
export const CROSS_KEY_CHECKS = Object.freeze(
  CHECK_IDS.filter(id => !CHECK_META[id].arms && CHECK_META[id].keys.length > 1),
);
```

- `paths` — the record paths the check relates, VERBATIM from §5.4. `keys` — the distinct top-level
  keys those paths head at, lexicographic. **Every `paths` entry must head at a key its own `keys[]`
  carries; A2 asserts it.**
- ⭐ **`arms`** — present ONLY where a check has INDEPENDENT arms. **A violation carries the keys of
  the ARM THAT FIRED, never the union.** `V-FLAGVEC` is the only such row today.
- ⭐ **`CROSS_KEY_CHECKS` is DERIVED, never hand-listed:** no `arms` and more than one key. The
  derivation reproduces §5.4's seven exactly; hand-listing would drift the first time a check is
  added.
- `violation.keys` is what EM-R0c's escalation ladder reads at step 1 (design §22.2 item 3): the
  enclosing declared group, or — for a cross-key violation — **both keys from `R1`**.
- The parameter is **`settlement`** — the estate's receiver prior.
- Three outcomes per check: a violation, `true`, or **ABSTAIN**. Absent or mistyped inputs ABSTAIN;
  abstention is never reported.
- `violation.path` is the record path of the **derived** leaf that disagrees.

### The band checks

Three are a plain call: `legitimacyBandOf(score).label === label`, `readinessBandOf(score).label ===
label`, `severityBand(c.severity) === c.severityBand` — for EVERY `activeConditions` entry.

⭐ **The fourth is TOLERANT OF ROUNDING, and this is a contract, not a slack** — UNCHANGED from
version 2, and the facts that forced it re-measure identically (V3-4).

```text
V-BAND-FOODSEC, given published deficitPct d, published surplusPct s, and the famine flag f:
  ADMISSIBLE = { foodSecurityBandOf(f, d', s') : d' in {d-0.5, d, d+0.5}, s' in {s-0.5, s, s+0.5} }
  CONVICT iff the record's label is not in ADMISSIBLE.
```

| published | label | EXACT | TOLERANT | what it is |
|---|---|---|---|---|
| `d=15, s=0` | `Import-Dependent` | ⛔ CONVICTS | ✓ passes | honest: unrounded 15.4 |
| `d=15, s=0` | `Pressured` | ✓ | ✓ | honest: unrounded 14.6 |
| `d=15, s=0` | `Deficit` | ⛔ | ⛔ **still reds** | genuinely wrong |
| `d=5, s=0` | `Pressured` | ⛔ CONVICTS | ✓ passes | honest: unrounded 5.3 |
| `d=5, s=0` | `Secure` | ✓ | ✓ | honest: unrounded 4.8 |
| `d=5, s=0` | `Import-Dependent` | ⛔ | ⛔ **still reds** | genuinely wrong |

The tolerance is **exactly half a unit and never more**.

### The flag-vector check — UNCHANGED from version 2

```text
for each declared (objectPath, flags[]) and the object's own `label`:
  1. the label was NEVER observed  -> ABSTAIN ENTIRELY.
  2. a flag that is FALSE          -> NEVER judged.
  3. a TRUE flag in the label's observed set -> pass.
  4. a TRUE flag in NO observation of that label -> CONVICT.
```

⛔ **THE WHOLE-VECTOR FORM IS REFUSED.** ⭐ **THE TABLE IS WHAT THE GENERATOR DOES**, taught on the
FULL 525 rows and re-measured at this tip to the row (V3-4 D5):

| object | label → flags EVER true |
|---|---|
| `economicState.foodSecurity` | `Secure`→`isSecure`(117) · `Pressured`→`isPressured`(151) · **`Import-Dependent`→`isDeficit`(196)+`isPressured`(12)** · `Deficit`→`isDeficit`(13) · `Deficit — Active Famine`→`isDeficit`(36) |
| `powerStructure.publicLegitimacy` | `Endorsed`→`isEndorsed`+`isApproved` · `Approved`→`isApproved` · `Tolerated`→`isTolerated` · `Contested`→`isContested` · `Legitimacy Crisis`→`isLegitimacyCrisis` |

⚠ **Every label present at 525 is present in the 63-row stride** (V3-4 D6: 5 labels, 0 lost).

### ⭐ The three duplication checks (NEW at version 3)

```text
V-DEFENSE-MAGICDEP    defenseProfile.scores.magicDependency === defenseProfile.magicDependency
V-DEFENSE-TRADITIONS  defenseProfile.scores.traditions      ≡  defenseProfile.traditions   (deep)
V-HISTORY-AGE         history.founding.age                  === history.age
```

Each ABSTAINS unless both paths are present. Each holds on **525 of 525** plain records, and the
merge can put the two copies out of step on **63 of 63** constructed trials with a control that
splits nothing (V3-3d). They are the ONLY instrument available for the two defence rows: a
consistency group rooted at `defenseProfile` would NEST `G7 defense-readiness`, which EM-R0a's own
walker forbids.

### Absence, ordering, determinism, lifecycle

- absent reading/object, non-number score, non-string label, label outside the table: **ABSTAIN**.
- `violations`: an empty array. ⛔ Never `null`, never `undefined`.
- a check that throws: ⛔ forbidden — total over any input by construction. A7 is the proof.
- **Order:** declared check order (`CHECK_IDS`), then array index. No content sort.
- ⚠ **The arithmetic checks carry a `|difference| ≤ 1` tolerance**; `V-ISOLATION` carries none; the
  three duplication checks carry none (they are exact identities). A3's mutants clear each check's
  own tolerance.
- Hash/fork key `NONE`; no PRNG, clock, locale or `Intl`.
- **Lifecycle:** module load, frozen, read-only. Never persisted or projected; every column `n/a`.
- Flag `NONE`. Golden posture **UNCHANGED**. Alignment `DECLARED EMPTY`.
  Edit story: `ENGINE-ONLY — EM-R0c's delta card is where a violation becomes words.`
- Closed kinds: the six §22.2 item 3 names, and no seventh.

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/recordInvariants.js` | `recordInvariants` | `≤ 230` | The thirty-three checks of §5.4 in `CHECK_IDS` order, each violation carrying `kind` and `keys` from `CHECK_META` (the arm's keys where the row declares arms). ⛔ Import ONLY `../activeConditions.js`, `../../data/bandLadders.js`, `./recordInvariantFlags.js` and `./recordInvariantMeta.js`. ⛔ No `src/generators` import, no `src/components` import, **no `recordRegister.js` import**, no numeric threshold, no corpus. Parameter named `settlement`. Re-export `CHECK_META`, `CHECK_IDS` and `CROSS_KEY_CHECKS` so EM-R0c's compiled import is satisfied verbatim. |
| `CREATE` | `src/domain/edit/recordInvariantMeta.js` ⭐ NEW | `CHECK_IDS`, `CHECK_META`, `CROSS_KEY_CHECKS` | `≤ 70` | Pure frozen data, §5.4's table transcribed. **Import nothing.** `CROSS_KEY_CHECKS` is DERIVED from `CHECK_META`, never hand-listed. The header names the design sections the `keys` field serves (§22.2 item 3, §22.3 item 5) and states the `arms` rule. |
| `CREATE` | `src/domain/edit/recordInvariantFlags.js` | `FLAGS_EVER_TRUE`, `FLAG_FIELDS`, `KNOWN_VIOLATIONS`, `FLAG_CORPUS` | `≤ 40` | Pure frozen data, §6's table verbatim. **Import nothing.** The header names the corpus (525 rows, `goldenCorpus`), the measuring sha, and that the table records what the generator DOES across its two ladders. |
| `TEST` | `tests/domain/recordInvariants.test.js` | A1–A8 | `n/a` | ⛔ **Straight-line literal `it`s under ONE literal `describe`; never bind `it`, `test` or `describe` as a variable or parameter.** No `.each`, loops, nesting or conditionals. `// anchored:` above every negative assertion. The basename may gain no `NAME_PATTERN` token. |

Generated artifacts: `NONE`. No leaf is in an edge-shared bundle closure. **No declared `checks`
command writes any path.** No other file may be edited.

**Predicted register moves, priced here rather than discovered at the terminal:**

| Register | Moves? | Predicted delta | Door |
|---|---|---|---|
| Lighting census | **YES** | ⭐ **`files +1` · `credited +1` · `parked +0` · `titles +7` · `suiteTitles +1`** — **CORRECTED AT PRE-PROOF from version 3's `titles +8`.** The census sums `liveTitlesIn` over CREDITED files only; §9 homes **A8 in `tests/build/domainGeneratorsBoundary.test.js`, run and NOT edited**, so it lands no `it` here and an unedited file contributes nothing. SEVEN arms (A1–A7) land in the new file under ONE literal `describe` (P-6). ⚠ `credited +1` is conditional on §5.2's registration pin. ⛔ **NO ABSOLUTE IS RESTATED** — the live tuple is the chair's to stamp at promotion from `tests/lint/.lighting-census-baseline.json` | interior red; re-derived at the terminal by the chair |
| Mutation-coverage | **NO** | zero — `tests/domain` is not an `ENFORCER_DIR` and the basename matches no `NAME_PATTERN` token. ⚠ Both halves matter |
| ⚠ Observed-shape readers | **PRICED, RED-FIRST AT THE BUILD** | a NEW file has ceiling 0. ⭐ v3 adds a THIRD leaf, so the scan covers three | §8 step 7: scan before and after; **STOP for the chair** on any identity. The lane never mints |
| ⭐ Prose-numerics (line-addressed) | **NO** | zero — this packet **MODIFIES no `src/` file** |
| ⭐ Wiring census (stamped) | **NO** | zero — no stamped file is modified |
| Writer-reach · edge-shared · decision-fork · bundle ceilings | **NO** | zero — no record key is written, no chunk delta (V3-6) |

## 8. Ordered coding sequence

0. Dispatch and seal. **Re-run the live-edge scan** and confirm EM-R0d is LANDED with its four
   symbols present, and EM-R0a LANDED with `RECORD_CLASSES` (A2 reads it). If either has moved, STOP.
1. **Capture the baseline, RED FIRST.** Run the observed-shape reader scan in report mode and record
   the three new leaves' absence. Record the live lighting tuple from a throwaway probe.
2. Add the failing arms for A1–A8 against modules that do not exist yet.
3. Create `src/domain/edit/recordInvariantMeta.js` — §5.4's table transcribed, `CROSS_KEY_CHECKS`
   derived. ⭐ **Build this leaf FIRST**: A2's totality arm is what keeps the other two honest.
4. Create `src/domain/edit/recordInvariantFlags.js` — the table transcribed from A6's own measurement.
5. Create `src/domain/edit/recordInvariants.js` — the checks in `CHECK_IDS` order, the four band
   checks calling the imported ladders, every violation carrying `kind` and `keys`.
6. No wiring. ⛔ **Import this module from nothing.** EM-R0c is the first importer.
7. Run focused verification (§10).
8. ⭐ **THE REGISTER STEP.** Re-run the observed-shape scan. Any new finding identity on any leaf is a
   **STOP for the chair** with the identities quoted — do not mint, do not rename a variable to dodge
   the heuristic, do not widen the inventory.
9. Write the completion receipt.

## 9. Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | ⭐ THE CONTROL, AND THE PIN BOTH WAYS | the stride `__STRIDE_RULE__`, generated plainly | `recordInvariants` returns `[]` on `__STRIDE_N_MINUS_1__` rows; `town\|germanic\|mountain\|mountain_pass\|civilized\|golden-master-v3` (corpus index **510**) returns exactly the pinned `V-SUMMARY-DEPS` with its exact message. **The violating-row SET equals `KNOWN_VIOLATIONS`, both directions**, so curing the generator REDS this arm and FIX-G1 must remove the pin. ⛔ Not a skip, not a filter, not a widened tolerance. ⚠ **THE CARDINALITY IS A MARKED PLACEHOLDER** until §12 question 1 is ruled; the SHAPE — "all clean but the one pin, set-equal both ways" — is what this arm asserts and it is EXECUTED at `141a1d775` on the full 525 (1 of 525, and it is the pin) and on the recommended stride (1 of 65) — P-4, P-7. ⛔ A stride that MISSES index 510 makes this arm VACUOUS and `tests/lint/contractTestAntiVacuity.walker.test.js` is the estate's guard for exactly that class | `tests/domain/recordInvariants.test.js` |
| A2 | ⭐⭐ `CHECK_META` IS TOTAL, ITS KEYS RESOLVE, AND A VIOLATION CARRIES THEM | `CHECK_IDS`, `CHECK_META`, EM-R0a's `RECORD_CLASSES`, the stride | (i) every id in `CHECK_IDS` has a `CHECK_META` row and every row names a declared id — **both directions**; (ii) every key any row names is a key `RECORD_CLASSES` classes, and **every `paths` entry heads at a key its own `keys[]` carries**; (iii) `CROSS_KEY_CHECKS` is exactly the rows with no `arms` and >1 key, and equals the SEVEN of §5.4 — ⛔ asserted against the literal seven ids, so a hand-edit that adds an eighth reds; (iv) every violation the stride produces carries `kind` and non-empty `keys`, and `V-FLAGVEC`'s carries **one** key, the arm's | same |
| A3 | ⭐ GUARD THE GUARD | for each of the 33 checks, the smallest mutation of a real record | each mutation returns **exactly that check's id and no other**, each step clearing its check's own tolerance. ⚠ `V-FLAGVEC`'s foodSecurity arm needs an `Import-Dependent` carrier. ⭐ The three duplication mutants are measured isolating (convict 63/63, trip nothing else) | same |
| A4 | ⭐ NO THRESHOLD, NO ENVELOPE | the module's own source text | the four band checks call `legitimacyBandOf` / `readinessBandOf` / `foodSecurityBandOf` / `severityBand`, and `recordInvariants.js` contains no numeric literal outside its tolerance constants | same |
| A5 | ⭐ THE ROUNDING RULE | constructed records at `deficitPct = 15` and `= 5` | both adjacent labels pass at each cut; a label two rungs away REDS. §6's six-row table, executed | same |
| A6 | THE FLAG DATA IS DECLARED, NOT DERIVED | the 63-row stride | the shipped `FLAGS_EVER_TRUE` covers every label the stride shows, and a flag DERIVED from the published `deficitPct` is shown to disagree with the record on the `(15,20]` rows | same |
| A7 | ⭐ ABSENCE, UNSEEN LABEL, PURITY, IDEMPOTENCY, **AND THE DUPLICATION PAIRS** | a record with `economicViability`, `isolationSupport` and `activeConditions` deleted; a record whose `foodSecurity.label` is unknown; one real record; and each duplication pair with ONE side deleted | `[]` for the first two; the record deep-equal to a pre-call clone; two calls equal; ⭐ **each duplication check ABSTAINS when either side is absent** — it may never convict a partial record | same |
| A8 | THE LAYERING BOUNDARY HOLDS | the live `src/domain` → `src/generators` edge scan | the live set equals `BASELINE_EDGES` (measured at `141a1d775`: **4 importing files / 5 edges**, P-5); none of the three leaves appears | `tests/build/domainGeneratorsBoundary.test.js` ⛔ **RUN, NOT EDITED — so it lands NO `it` and contributes NOTHING to the lighting delta** (this is why the delta is `titles +7`, not `+8`; P-6) |

```text
CANNOT-CATCH:
1.  Whether `readiness.score` is a function of the six defence scores. Not refuted over 86 distinct
    tuples, but no exact relation was found, so nothing may convict. -> EM-R7's corpus ratchet.
2.  Whether a THIRD duplicated leaf exists that the 525-row corpus never separates. The three were
    found by inspecting two objects the merge happened to mix; the sweep was not exhaustive.
3.  A DM edit outside the eight-edit corpus. The runtime guard is what covers it, by design.
4.  ~~Whether the `history` group is preferable to `V-HISTORY-AGE`.~~ ⭐ **CLOSED by the chair at
    ODQ §934.47 addendum 38 ruling 1: BOTH.** The check detects here; the group prevents, at EM-R0a.
7.  ⭐ **NEW at the pre-proof — TWO OF THE READINESS LADDER'S FIVE CUTS ARE NOT EXERCISED BY THE
    CORPUS.** `V-BAND-READINESS` holds 525/525 and the six observed label ranges do not overlap
    (P-4), but the corpus's 41 distinct readiness scores carry an **11-wide hole at 34..44** that
    swallows the declared `38` cut whole, and the `24` cut is witnessed by a single score (24) with
    nothing at 21–23 beneath it. So A1's green proves the ladder AGREES WITH THE RECORD on every
    score the corpus draws; it does NOT prove the 38 cut is at 38, and it would not distinguish any
    cut in `(33, 45]`. -> **EM-R0d's pre-proof owns the ladder's cuts** and EM-R7's corpus ratchet
    records the witnessed set. ⛔ Nobody may read this arm as proof of the cut positions.
8.  ⭐ **NEW at the pre-proof — the stride's cardinality.** Until §12 question 1 is ruled, A1's row
    count is a placeholder; the arm's SHAPE is executed and holds (P-7).
5.  The escalation itself. This packet ships the metadata; EM-R0c walks the ladder and EM-R7 proves
    the family reaches zero escalations.
6.  ⛔ A VACUOUS SPLIT FALSIFIER. The first draft of V3-3d mutated R0 at BOTH paths, so both landed
    on the record's own value and the arm printed SPLIT=0 while proving nothing. The construction in
    §6 and V3-3d is the corrected one; any re-derivation must move ONE path and keep the other.
```

## 10. Verification commands

```sh
npx eslint src/domain/edit/recordInvariants.js src/domain/edit/recordInvariantMeta.js \
  src/domain/edit/recordInvariantFlags.js tests/domain/recordInvariants.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/recordInvariants.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/build/domainGeneratorsBoundary.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/property/generatorGoldenMaster.test.js \
  tests/property/dossierProseManifest.test.js --maxWorkers=2

node scripts/check-observed-shape-readers.mjs --report   # report mode NEVER writes the baseline
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-R0b
npm run check:tail
```

⭐⭐ **AND `tests/lint` WHOLE — ADDED AT THE PRE-PROOF (the chair's addendum of 2026-09-20, after runs
17, 18 and 19: "a CREATE or RENAME of a test file under `tests/<dir>` opts into EVERY walker that
governs `tests/<dir>` … run that directory WHOLE once in your instruments; `tests/lint` whole
ALWAYS").** This packet CREATEs `tests/domain/recordInvariants.test.js`, and **nothing above reaches
the walkers that will read it**: `npm run check` runs `test:ratchet` (a counting script) and `lint`
(eslint), never the `tests/lint` vitest suite — measured from `package.json` at `141a1d775`. Runs 17,
18 and 19 were one family precisely because a lane's new test file was read by a walker the lane
never ran.

```sh
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint
```

⚠ **THIS IS A §10 VERIFICATION COMMAND, NOT A SEALED `checks` ROW, AND THE DISTINCTION IS
DELIBERATE.** `tests/lint/sovereigntyLightingContract.walker.test.js` is EXPECTED to red on its
interior arm — the packet adds a credited file and the census is an exact tuple, re-derived at the
terminal by the chair (§7). A guaranteed-red suite inside the sealed `checks` would fail the packet's
own gate. The lane RUNS it, QUOTES the census red with its printed tuple, and confirms **every other
`tests/lint` arm is green** — that is what proves no OTHER walker was surprised by the new file.

⚠ **`gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0** — a gate line with no printed test count
DID NOT RUN. The env prefix above is the long-poll spelling; the receipt must print each run's count.
⛔ **No command above is a generator**, so no declared path is re-stamped mid-chain.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`, stop if:

- the dispatch seal is missing, invalid, or belongs to another worktree state;
- resume reports authority, HEAD, foreign-work or receipt-integrity drift;
- **STOP-1 — a second argument.** Any pressure to make `recordInvariants` take a corpus, a second
  record or an observation set;
- **STOP-2 — EM-R0d is not LANDED, or its four symbols are spelled otherwise than §5.2 names them**;
- **STOP-3 — the food ladder's cuts or its OPEN-below operator change in EM-R0d.** §6's rounding rule
  is written against `>` at 40/15/5; a closed-below ladder needs the admissible set re-derived;
- **STOP-4** — the observed-shape scan gives any leaf a finding identity (§8 step 8);
- **STOP-5** — either golden moves, or `tests/build/domainGeneratorsBoundary.test.js` reds;
- **STOP-6** — any check ships without a planted mutant that isolates it;
- ⭐ **STOP-7 — any check ships without a `CHECK_META` row, or a row names a key EM-R0a does not
  class.** A2 is the red. ⛔ Never add the key to `CHECK_META` to make the arm pass — a key outside
  the register means either the register or the check is wrong, and both are the chair's;
- ⭐ **STOP-8 — `CROSS_KEY_CHECKS` is hand-listed rather than derived**, or its membership stops
  equalling §5.4's seven. Either is a silent change to EM-R0c's escalation.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the
next wave. ⛔ **This packet may not cure the generator defect it pins**, and it may not reconcile the
food card's two ladders — that is the owner's decision point (ODQ §934.57).

## 12. Completion receipt

- Base SHA · dispatch bundle and seal identity · final commit or working-tree state:
- Exact changed files and effective-line deltas (four files):
- Acceptance cases; focused commands, exits and counts (⚠ each with its own printed test count):
- A1's row count and wall-clock; A2's totality and key-membership output; A3's per-check verdict
  table for all 33; A5's boundary table:
- Observed-shape reader scan, before and after, over THREE leaves:
- Lighting census tuple, measured in a throwaway probe; the delta restated as
  ⭐ `files +1 · credited +1 · parked +0 · suiteTitles +1 · titles +7` with NO absolute — and the
  file's `it` count QUOTED, so `+7` is a measurement and not a promise:
- Sealed per-step receipt and exact-state resume status; both typecheck configurations:
- Dormancy/golden result; bundle/first-paint result (predicted: worker +0, eager +0).
  ⭐ **SAY HOW `+0` IS ESTABLISHED** (§P2 row 11's second amendment, ODQ §934.47 addendum 39): it is
  established STRUCTURALLY — `EAGER_FIRST_PAINT_MODULES` imported and measured to hold 0
  `src/domain/edit/**`, and nothing anywhere in `src/` importing that directory, so the leaves are in
  neither closure and no build is needed. ⛔ **A `skipIf(!requireDistRead)` byte arm that SKIPPED IS
  NOT A PASS** and may not be quoted as one; if such an arm runs, quote its executed figure and the
  dist it read, or say plainly that it skipped:
- Generated artifacts: `NONE` · Deviations: `NONE | STOP` · Judgment calls: `NONE`

## ⭐ §12 QUESTIONS FOR THE CHAIR (added at the pre-proof; §5.2 and §9 cite this section by number)

**1. ⛔⛔ WHAT IS "THE 63-ROW STRIDE"?** It is named by A1, A3, A6 and A7, and A1's "62 rows clean"
is its cardinality — but **nothing in the tree defines it**. `tests/helpers/goldenMasterCorpus.js`
exports `goldenCorpus` and `keyOf` and nothing else; no stride, sample or slice helper exists
anywhere under `tests/`; and **no arithmetic stride over 525 yields 63** (every k in 2..12 measured,
P-7). The 63 is EM-R0c's kit-prototype sample leaking into a shipping test's contract. The pin sits
at corpus index **510**, and ⛔ **`i % 8 === 0` — the obvious rule — MISSES it**, which would make
A1's pin arm vacuous.
**RECOMMENDED: `rows.filter((_, i) => i % 8 === 6)` — 65 rows, carrying index 510 by construction.**
EXECUTED at `141a1d775`: 64 clean, 1 violating, and it is the pin (P-7). A1 then reads "64 clean +
the pin". One line of source in the test, reproducible from the tree alone, and the cheapest rule
that keeps the arm honest. The alternative is the FULL 525 (11 s generation, also executed, 1 of 525)
if the chair prefers no sampling at all — that is the only other rule this lane would defend.

**Noticed, not touched — each slotted, nothing deferred (the owner's law of 2026-09-19 18:10):**

1. ⭐ `src/generators/power/rulingStructure.js` `_provDefLabel` is a **SIXTH, undeclared spelling of
   the readiness vocabulary** and omits `Fortress`. Re-confirmed at this tip: that file contains
   **0 lines naming `readiness`** (V3-4 D3). → **EM-R0d's second version**, already slotted.
2. `V-FOODSEC-FLAGS`'s own map covers **four of the six** food labels and silently ABSTAINS on
   `Import-Dependent` and `Deficit — Active Famine`, where `V-FLAGVEC` is the sole check. → written
   down in §6; no separate slot.
3. The `V-EVIDENCE-*` checks' TRUE carrier counts range from **242 to 525 of 525**. → **EM-R7's
   corpus ratchet**: it records the carrier count beside each check.
4. `judgments[id=confidence_and_provenance].evidence[path=simulationTrace]` carries an `evidence`
   value of **`undefined` on 525 of 525 rows** (V3-4 D7). No shipped check reads it. → EM-R0c's
   compile, which owns the receipt finding; **ANSWERED there** (the receipt is merged, never
   recomputed).
5. ⭐ **NEW at v3 — the duplication SWEEP was not exhaustive.** Three duplicated leaves were found by
   inspecting the two objects EM-R0c's merge happened to mix. A whole-record sweep for "one fact at
   two paths" over the 525-row corpus is a bounded, mechanical measurement nobody has run. →
   **SLOT: EM-R7's corpus ratchet carries it** — it already walks every record path, and a
   duplication census is one more pass over the same walk.
6. ⭐ **NEW at v3 — `readiness.score` is not refuted as a function of the six defence scores** (86
   distinct tuples, 0 ambiguous) but no exact relation was found, so no check may ship. If a relation
   exists it is a real invariant the guard is missing. → **SLOT: EM-R7's ratchet records the
   observation count**, so the evidence either strengthens into a check or is recorded as
   coincidence.
7. ⭐⭐ **NEW at the pre-proof — THE READINESS LADDER'S `38` CUT IS UNWITNESSED BY THE CORPUS.** The
   525 rows carry 41 distinct readiness scores spanning 11..81 with a hole at **34..44**, so no row
   exercises the `38` boundary and the `24` cut is witnessed only from above (a single score at 24,
   nothing at 21–23). `V-BAND-READINESS` is still EXACT and still 525/525 — this bounds what its
   green PROVES, not whether it passes. → **SLOT: EM-R0d's pre-proof**, which owns the four ladders,
   states each cut's witnessed/unwitnessed status beside it; **EM-R7's corpus ratchet** records the
   witnessed score set so a corpus change that starts exercising 34..44 is visible. §9 CANNOT-CATCH 7.
8. ⭐ **NEW at the pre-proof — TWO TEST HEADERS ASSERT IN PROSE THAT `src/domain/edit/**` DOES NOT
   EXIST.** `tests/store/decreeRegistryPersistence.test.js:17` ("which does not exist at this
   commit") and `tests/lib/editTravel.test.js:15`. Both are COMMENT-ONLY — neither carries a runtime
   assertion, so **nothing reds** (measured) — but both become false the moment the first packet
   CREATEs under that directory. Under the family order that is **EM-R0a**, not this packet. →
   **SLOT: EM-R0a's pre-proof carries the two-line prose re-address**, since it lands first; if
   EM-A2a's `pools.js` lands earlier, it inherits the row.
9. ⭐ **NEW at the pre-proof — a SECOND `bandLadders.js` basename arrives with EM-R0d.**
   `src/domain/compendium/bandLadders.js` already exists (exporting `buildBandLadders`), is read by
   `scripts/generate-compendium-data.mjs`, `src/domain/compendium/searchIndex.js` and two test files,
   and carries **three rows in `scripts/.observed-shape-readers-baseline.json`**. EM-R0d creates
   `src/data/bandLadders.js`. The addresses are distinct so no import is ambiguous and this packet's
   §5.2 spells the `src/data/` address explicitly — but any instrument that addresses by BASENAME
   will now see two. → **SLOT: EM-R0d's pre-proof names the collision and states which instruments
   address by basename**; this packet's §8 step 8 scan is already red-first over its own three leaves.
