# EM-B1j COMPILE REPORT — Opus COMPILE lane, 2026-09-20, read tip `141a1d775`

**Outcome: DRAFT (READY-able) for part 4 ONLY, after a forced split; parts 1–3 ruled by measurement
and chartered. Two contracted premises in the launch brief are REFUTED BY EXECUTION.**

Files: `EM-B1j.md` · `EM-B1j.manifest.json` · `EM-B1j.evidence.md` · this report, all in
`$SP/lane-em-compile-EM-B1j-scratch/`. Probes: `probe-witness.mjs`, `patch-hook.mjs`,
`census-hook.mjs`, `probe-witness-census.mjs`, `remnant-census.mjs`, `inactive-vocab.mjs`,
`replica-census2.mjs`, `byte-identity.mjs`, `byte-identity2.mjs`, `headroom.mjs`,
`worker-closure.mjs`.

## The two refuted premises

1. **"ONE exported writer `remnantInstitution(inst, { reason, fate })`, both required, the four
   sites re-pointed byte-identical."** Executed: that signature is byte-identical for **1 of 4**
   sites. It moves key order at the economic close, and it stamps `_worldPulseEconomyClosed` onto
   the ABOLISH and DEMOTION records — which `institutionStatusModel.js:334` reads as `isShell`, a
   state that file's own header (`:319-321`) declares a moral abolition must never enter. The
   corrected signature (`closure` required from a closed set; `outcomeId` optional and OMITTED when
   absent) is byte-identical for **3 of 4**, key order included, with all four refusals proved.
   The fourth (`deactivateForDemotion`) is out of class on measurement, not on taste.
2. **"five test replicas."** The brace-scoped census reads **15 stamps in 11 files** (plus 3 probes
   and 6 assertions). FIX-T1 named five from an incidental sweep and never censused the class.

## The split (forced, not chosen)

The honest four-part packet reads 16+ handwritten files against a cap of 12. The seam:

- **EM-B1j** (compiled here) — the writer + the three byte-identical sites + the walker taught. **5 files.**
- **EM-B1j′** (chartered) — the 15 stamp replicas in 11 `tests/domain/` files. TEST-only, 0 production lines.
- **EM-B1j″** (chartered) — the `LIFECYCLE_CLOSE_FATES` derived mirror + its equality pin, with the widening and the ageing as priced owner doors.
- **EM-B1l** (chartered from the measurement, below).

⭐ **EM-B1j MUST PRECEDE EM-B1j″**, measured: `institutionLifecycle.js` is 798/800 with **2 lines of
headroom**; EM-B1j's re-points RECLAIM ~12 effective lines there, and EM-B1j″'s derived mirror costs
+1/+2 which those 2 lines cannot safely absorb first.

## The two prices

**(a) Widening `LIFECYCLE_CLOSE_FATES` 3 → 15.** The counter reads `institutionHistory[].fate`
(17 live values = 12 shared + 5 its own, re-derived by execution). **Nine of the twelve added words
are live there** — `abolished`, `disbanded`, and all seven demotion fates via
`tierOutcomeApply.js:391`. `closes` rises, `closeChance` divides by `1 + 0.6 × priorCloses`, a seeded
roll flips. **It is a lived-behaviour change: CONFIRMED.** Rows that move:
- preset witness golden — **0 of 8 rows, 0 of 112 fields**, but that zero is worthless: the census
  hook proves `priorLifecycleCounts` is called **0 times** in the whole witness run (its realm sits
  in the neutral dead band). Reported as a blind instrument, not a clean bill.
- generator + prose goldens — **cannot move**: `generatorGoldenMaster.test.js` imports no world-pulse module.
- committed fixtures — **cannot move**: 0 of `tests/fixtures/` carry `institutionHistory`; 0 carry any of the twelve words.
- dormancy goldens — **the only corpus that could move**; PLAUSIBLE, and the executed proof is a
  GATED `tests/property` run scheduled as EM-B1j″'s build-lane first batch.

**(b) Ageing out a rise or standing fate.** `worldPulseFate` has exactly two readers.
`causeLifecycle.institutionDestroyed` reads TRUTHINESS (EM-B1i's subject).
`rosterProvenance.institutionLastLifecycle` **early-returns on `standing` before it ever reads the
fate**, and its one consumer (`OverviewTab.jsx:57`) destructures only `{ created }`. Wizard News,
the dossier prose and the dossier readers name `worldPulseFate` in **zero** files. ⇒ after EM-B1i
lands, a stale rise/standing fate has **no reader that branches on it**. **RECOMMEND: NEVER CLEAR.**
Ageing it out would move stored bytes (and any world-state hash) to buy nothing.

## The `'remnant'` verdict — SEPARATE VOCABULARY; EM-B1l is chartered

Denominator: 37 occurrences in 18 files; 10 comments; 27 in code; 1 a different field
(`heraldRegister.js:250` `kind`). **Nine write sites across three files** (the chair's correction,
confirmed verbatim), four writer functions. **Sixteen reader sites in fifteen files**, of which two
have a different subject (`lineageClaim` reads a settlement; `entryWalker` filters prose text).

`'remnant'` is **not** a sixth `EntityStatus`. The union is pinned at five and `'ruined'` — stamped
by EM-B1e's own landed writer — is outside it too, as are `'defunct'`, `'closed'`, `'disbanded'`,
`'abolished'`. Every `EntityStatus` occurrence in `src/` is a comment or a JSDoc annotation: the
union has **no runtime consumer**. Widening it would move nothing and prove nothing.

> **EM-B1l CHARTER ROW (written from this measurement; this packet does not build it).**
> **EM-B1l — ONE inactive-institution status vocabulary.** The estate spells "this institution is
> not live" **seven different ways across fifteen reader sites**: `{destroyed,remnant,removed,ruined}`
> ×7 · `{remnant,removed,ruined}` ×2 (`calamity.js:306`, `tierOutcomeApply.js:211`) ·
> `{destroyed,remnant,removed}` ×2 (`foodStockpile.js:125`, `institutionLifecycle.js:1051`) ·
> `{remnant,ruined}` ×1 (`institutionLifecycle.js:1095`) · plus `causeLifecycle.js:134`'s
> eight-word `NONSTANDING_STATUS` and `entryWalker.js:682`'s seven-word PROSE vocabulary (a
> different subject, declared out). EM-B1l mints ONE exported frozen vocabulary in the same leaf
> family, re-points the thirteen same-subject readers, and adds a totality walker that reds on a
> fourteenth hand-rolled spelling — the `NPC_UNAVAILABLE_STATUSES` idiom. ⛔ It does **not** widen
> `EntityStatus`: that union is documentation with no runtime consumer, and `'ruined'` is outside
> it as well. ⚠ Three suspected divergences were checked and are NOT defects — `foodStockpile` and
> `institutionLifecycle:1051` both test `_worldPulseInactive` first, and `calamity.js:306`'s missing
> `'destroyed'` is honest-dead because no writer ever sets an institution's status to `'destroyed'`.
> EM-B1l is therefore **hygiene with a guard, not a bug fix**, and it must say so.

## Budgets

| Row | Measured |
|---|---|
| Handwritten files | **5** of 12 |
| Existing logic files modified | **3** of 3 (at the cap) |
| New production lines | **net negative** (~+12 in `calamityKernel.js`, ~−16 across the three sites) |
| Hot file | `institutionLifecycle.js` **798/800, headroom 2** — this member RECLAIMS ~12 |
| Generation worker (zero slack) | **untouched**: 228-module closure, **0** worldPulse members |
| Eager first paint | **268** members; none of the three modified files is one (`tierOutcomeApply.js` is, and is not touched) |
| Edge-shared | **0 of 5** metas name any of the four files — no rebuild, §P2 row 12 does not fire |
| Lighting | `+0 files / +0 parked / +0 credited / +N titles / +0 suiteTitles`; no file created |
| Mutation-coverage | **not owed** (no `tests/lint/` CREATE) |
| Acceptance cases | 6 of 8 |

## Dependencies, measured

`tests/lint/ruinShapeReplica.walker.test.js` **ABSENT** at `141a1d775` (FIX-T1 at its gate).
`src/domain/worldPulse/worldPulseFates.js` **ABSENT** (EM-B1h not landed — EM-B1j″ needs it).
`tests/domain/ruinInstitution.test.js` **PRESENT** (EM-B1e landed).

## Questions for the chair (7, each with a recommendation)

1. **The corrected signature.** `remnantInstitution(inst, {reason, fate, closure, outcomeId})` with
   `closure` required and `outcomeId` omitted-when-absent. **Recommend: accept** — it is the only
   shape proved byte-identical for three sites, and the required `closure` is what stops an
   abolition being read as a shell.
2. **`deactivateForDemotion` out of class.** It writes a COMPUTED status (`'remnant'` ×6,
   `'removed'` ×1), sets no closure flag, carries `removedReason`. **Recommend: exempt with a
   reason at the site**, FIX-T1's marker idiom, population one.
3. **The three-way split and its forced order (EM-B1j → EM-B1j″).** **Recommend: accept**; the hot
   file's 2 lines of headroom make the order a measurement, not a preference.
4. **`LIFECYCLE_CLOSE_FATES` widening = the owner's door.** **Recommend: put it to the owner with
   the nine live words named**, and land only the behaviour-neutral half (the derived mirror + a
   pin that reds the day derivation and literal disagree) in EM-B1j″.
5. **Ageing a rise/standing fate.** **Recommend: NEVER CLEAR, and close the question with the
   reason** (no reader branches on it after EM-B1i) rather than leaving it open.
6. **EM-B1l's grade.** It is hygiene-with-a-guard, not a defect cure — three suspected divergences
   were refuted. **Recommend: charter it at low priority, behind the editor train.**
7. **`tests/lint/provenanceStampSingleWriter.walker.test.js:300`** pins `LIFECYCLE_CLOSE_FATES`'s
   three words by exact equality and sits outside every EM manifest. **Recommend: EM-B1j″ declares
   it a TEST row**; it is the one file that reds the moment the mirror is derived.

## ⛔ Noticed and NOT touched — each specific enough to slot

1. **`LIFECYCLE_BUILD_FATES` is 2 words against `institutionHistory`'s 5 rise-ish words.** `built`
   and `reopened` damp builds; `founded` (×2 sites), `reactivated` and `added` damp nothing. A
   patron-founded institution is invisible to `priorBuildPenalty`. Same class as the close question,
   never asked. → **SLOT: EM-B1j″'s second measurement.**
2. **`priorLifecycleCounts` uses `else if`.** A fate in both sets would count as a build, silently.
   With both sets derived from one kind map the overlap becomes possible. → **SLOT: EM-B1j″, a
   disjointness assertion in the derived-mirror pin.**
3. **The 24-entry `institutionHistory` cap silently bounds the damping.** `appendInstitutionHistory`
   slices to 24; widening the close set makes the cap bite sooner, because demotion fates crowd out
   older closes. Un-modelled today. → **SLOT: a named paragraph in EM-B1j″'s price, for the owner.**
4. **`tierOutcomeApply.js` is in the eager first-paint closure** (268 members) while the other three
   worldPulse files are not. Any future member that edits it pays first-paint bytes. → **SLOT: a
   hazard row in the memory fold; not a packet.**
5. **`institutionStatusModel.js` exports `INSTITUTION_STATUSES = ['operational','impaired','shell']`
   — a THIRD status vocabulary**, disjoint from both `EntityStatus` and the inactive set, over the
   same records. → **SLOT: EM-B1l's §1 must name it and declare it out of scope, or the walker will
   collide with it.**
6. **`heraldRegister.js:250` spells `kind: 'remnant'`** on a dead-settlement register row — the same
   word for an unrelated concept, one grep away from a false conviction. → **SLOT: EM-B1l's walker
   declares the path excluded with this reason.**
7. **The preset lighting witness cannot see the institution lifecycle at all** (`priorLifecycleCounts`
   CALLS=0 over all 8 rows). It is cited across the family as the behaviour-neutrality instrument.
   → **SLOT: a line in the witness suite's header stating what it does NOT witness; owner-free,
   cheap, and it stops the next lane reading a 0-diff as a clean bill.**
8. **`docs/implementation/charters/EDIT-MODE-TRAIN.md:323` says `LIFECYCLE_CLOSE_FATES` is "3 of the
   15 closure fates (only the ECONOMIC closures damp)".** Measured, that rationale is wrong:
   `abolished` and `disbanded` are non-economic AND already land in `institutionHistory`, so the
   present set is not "the economic ones" — it is the three `closureFateForInstitution` returns, and
   nothing else was ever considered. → **SLOT: a charter text correction at the next amendment.**
