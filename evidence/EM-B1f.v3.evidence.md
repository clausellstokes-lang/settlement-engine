# EM-B1f VERSION 3 — EVIDENCE

Every fact in the packet, with the command that proved it and its output. Compiled by an Opus
COMPILE lane, 2026-09-20, at `19c4cb85393e73404caa3bac1fe7800a48bfd39e`.
**Version 1's and version 2's evidence files are preserved beside this one** (`EM-B1f.v1.evidence.md`,
`EM-B1f.evidence.v2.md`) and are not rewritten; this file is version 3's own numbered record.

Substitutions used throughout:
`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`,
`SC=$SP/lane-em-compile-EM-B1f-scratch`, `T=$SP/read-tip-32602dc60`.

---

## §1 · THE TREES, AND WHY A PROBE MAY BE DRIVEN IN THE READ TIP

```
$ git -C $SP/slot-2 rev-parse HEAD
19c4cb85393e73404caa3bac1fe7800a48bfd39e
$ git -C $SP/slot-2 status --short          # (empty, before AND after this lane)
$ git -C $SP/slot-2 rev-parse --abbrev-ref HEAD
fixes-2026-09-18-consist
$ git -C $SP/read-tip-32602dc60 rev-parse HEAD
32602dc607b7423838249cf57d73baf08feb047d
$ git -C $SP/read-tip-32602dc60 status --short   # (empty, before AND after)
$ git -C $SP/slot-2 diff --stat 32602dc60 19c4cb853
 docs/implementation/INDEX.md                       |   2 +
 docs/implementation/PACKET_MANIFEST.json           | 185 ++++++++++
 docs/implementation/PACKET_STANDARD.md             |   5 +-
 .../packets/settlement-editor/EM-B1d.md            |   2 +-
 .../packets/settlement-editor/EM-B1k.md            | 408 +++++++++++++++++++++
 src/domain/worldPulse/pulseKernel.js               |   4 +-
 tests/domain/roadsParticipation.test.js            | 118 ++++++
 tests/lint/.lighting-census-baseline.json          |  16 +-
 tests/store/participationWriteBase.test.js         | 298 +++++++++++++++
 9 files changed, 1026 insertions(+), 12 deletions(-)
```

⭐ **`src/` differs in ONE FILE and two lines.** No probe in this evidence reads the read tip's
`pulseKernel.js` without replacing it. The other two moved files are tests, which no probe imports.

```
$ git -C $SP/slot-2 diff 32602dc60 19c4cb853 -- src/domain/worldPulse/pulseKernel.js
@@ -181,7 +181,7 @@ function buildSettlementMap(snapshot, localSettlements) {
-      settlement: localSettlements.get(String(item.id)) || item.settlement,
+      settlement: localSettlements.get(String(item.id)) || item.save?.settlement || item.settlement,
@@ -576,7 +576,7 @@ export function simulateCampaignWorldPulse(...)
-    localSettlements.set(String(item.id), vaulted.settlement);
+    localSettlements.set(String(item.id), item.save?.settlement?.npcs && item.save.settlement.npcs !== /** @type {{npcs?: unknown}} */ (vaulted.settlement)?.npcs ? { ...vaulted.settlement, npcs: item.save.settlement.npcs } : vaulted.settlement);

$ git -C $SP/slot-2 diff 32602dc60 19c4cb853 -- src/domain/worldPulse/pulseKernel.js \
    | grep -E "^\+" | grep -cE "'(dead|exiled|jailed|missing|removed|retired|active)'"
0
```
⇒ the two seam lines add **no** quoted status literal, so every walker figure derived from `src/`
(TRIGGER, FLAGGED, DISCOVERED_ENUMERATORS) is identical at both trees. **CONFIRMED.**

**The override, declared.** `$SC/v3-loader.mjs` is a Node loader that (a) stubs
`src/lib/supabase.js` (the one module reading `import.meta.env`, inherited verbatim from version 1's
`stub-loader.mjs`), (b) with `PULSE_MODE=landed` returns `19c4cb853`'s `pulseKernel.js` **as the
source of the read tip's own URL** so every relative import still resolves inside the read tip, and
(c) with `ARM_MODE=on` returns §6's exact post-edit `roads/state.js`. Both replacements **throw** if
their textual anchors stop matching, so a vacuous run is impossible. Tree and file paths come from
environment variables with **no defaults** (the chair's correction of 2026-09-20).

## §2 · ⭐⭐ THE PROMOTION MEASUREMENT, PART 1 — THE DM'S OWN REPRODUCTION

`$SC/v3-dm-repro.mjs`: a town generated by `generateSettlementPipeline`, the DM's own
`applyNpcOp('stasis-npc')` writer (or a directly-set `.status`), one tick through
`simulateCampaignWorldPulse({commit:true})`, then the **real** store commit
`applyWorldPulseResultToState`.

```
$ cd $T && PULSE_MODE=pre ARM_MODE=off SUBJECT_SHELVE=1 \
    node --experimental-loader $SC/v3-loader.mjs $SC/v3-dm-repro.mjs
══ PULSE_MODE=pre ARM_MODE=off subject=SHELVED(stasis)
   OFF_STAGE_STATUSES exported: null
   DM applyNpcOp stasis-npc -> {"ok":true,"status":"applied","reason":null}
   subject: Dorothea Eindriðason | isOffStage(them): true | roster ON THE SAVE: 7 of 7
   AFTER ONE TICK (commit:true) + the REAL store commit
     store  savedSettlements[0].settlement.npcs : 6 of 7 | subject present: false
     DB     persistUpdates[0].settlement.npcs   : 6 of 7 | subject present: false
     un-shelve afterwards  applyNpcOp return-npc -> {"ok":false,"status":"failed","reason":"npc_target_missing"}
     VERDICT: ⛔ ERASED
```
⇒ version 1's finding reproduces at the pre-cure kernel. **The harness is live.** CONFIRMED.

```
$ PULSE_MODE=pre ARM_MODE=on SUBJECT_STATUS=jailed node --experimental-loader … v3-dm-repro.mjs
══ PULSE_MODE=pre ARM_MODE=on subject=jailed
   OFF_STAGE_STATUSES exported: ["exiled","jailed","removed"]
     store … : 6 of 7 | subject present: false
     DB   … : 6 of 7 | subject present: false
     un-shelve afterwards -> {"ok":false,"status":"failed","reason":"npc_target_missing"}
     VERDICT: ⛔ ERASED
```
⇒ ⛔ **exactly the harm version 2 refused to ship**, reproduced with this packet's own arm.
CONFIRMED.

```
$ PULSE_MODE=landed ARM_MODE=off SUBJECT_SHELVE=1  …   → store 7 of 7 | DB 7 of 7 | return-npc {"ok":true,"status":"applied"} | SURVIVES
$ PULSE_MODE=landed ARM_MODE=off SUBJECT_STATUS=jailed …→ isOffStage false | 7 of 7 | 7 of 7 | SURVIVES  (arm absent ⇒ inert)
$ PULSE_MODE=landed ARM_MODE=on  SUBJECT_STATUS=jailed …→ OFF_STAGE_STATUSES ["exiled","jailed","removed"] | isOffStage TRUE | 7 of 7 | 7 of 7 | SURVIVES
$ PULSE_MODE=landed ARM_MODE=on  SUBJECT_STATUS=exiled …→ isOffStage TRUE | 7 of 7 | 7 of 7 | SURVIVES
$ PULSE_MODE=landed ARM_MODE=on  SUBJECT_STATUS=removed…→ isOffStage TRUE | 7 of 7 | 7 of 7 | SURVIVES
```
⇒ ⭐⭐ **THE BLOCK IS LIFTED. Adding `jailed`, `exiled` or `removed` to the filter cannot erase a
person from the save.** CONFIRMED by execution through the shipped entry point and the real store
commit.

## §3 · ⭐⭐ THE PROMOTION MEASUREMENT, PART 2 — THE SOLE-MEMBER HOUSE

`$SC/v3-house-repro.mjs` (version 1's `pulse-repro.mjs`, re-driven): two houses, The Weavers with
exactly one member, the density law LIT (`DENSITY_LAW_CONFIG_KEY`), one tick with `commit:true`.

```
$ PULSE_MODE=pre ARM_MODE=off  node --experimental-loader $SC/v3-loader.mjs $SC/v3-house-repro.mjs
  A · ACTIVE sole Weaver (control)   houses=["The Crown","The Weavers"] | beat=(none)
  B · STASIS sole Weaver             houses=["The Crown"] | roster=["Factor of The Crown[active]"] | beat=faction_dissolved
  C · HOSTAGE sole Weaver            houses=["The Crown"] | roster=["Factor of The Crown[active]"] | beat=faction_dissolved
  D · status:'jailed'                houses=["The Crown","The Weavers"] | beat=(none)
  E · status:'exiled'                houses=["The Crown"] | roster=[…,"Factor of The Weavers[exiled]"] | beat=faction_dissolved
  F · status:'removed'               houses=["The Crown"] | roster=[…,"Factor of The Weavers[removed]"] | beat=faction_dissolved
  G · status:'missing'               houses=["The Crown","The Weavers"] | beat=(none)
  H · status:'retired'               houses=["The Crown","The Weavers"] | beat=(none)

$ PULSE_MODE=pre ARM_MODE=on  …
  D · status:'jailed'                houses=["The Crown"] | roster=["Factor of The Crown[active]"] | beat=faction_dissolved   ⛔

$ PULSE_MODE=landed ARM_MODE=off …
  B · STASIS  houses=["The Crown","The Weavers"] | roster keeps the person | beat=(none)   ✓ EM-B1k's cure
  C · HOSTAGE houses=["The Crown","The Weavers"] | roster keeps the person | beat=(none)   ✓
  D · jailed  houses=["The Crown","The Weavers"] | beat=(none)
  E · exiled  houses=["The Crown"] | roster keeps the person | beat=faction_dissolved
  F · removed houses=["The Crown"] | roster keeps the person | beat=faction_dissolved

$ PULSE_MODE=landed ARM_MODE=on  …
  ⭐ ALL EIGHT ROWS BYTE-IDENTICAL TO THE ARM-OFF RUN ABOVE.
```
⇒ ⭐ **At the cured tree the arm changes nothing in this consequence space**, and the `exiled` /
`removed` dissolutions belong to a pre-existing reading:

```
$ sed -n '85,99p' $SC/work3/src/domain/density/factionLifecycle.js
export const ROSTER_ABSENT_STATUSES = Object.freeze(['dead', 'exiled', 'removed']);
const ABSENT = new Set(ROSTER_ABSENT_STATUSES);
export function isOnRoster(npc) {
  if (!npc) return false;
  return !ABSENT.has(String(npc.status || 'active').toLowerCase());
}
```
⇒ the charter's *"every reading in the estate already counts `removed` absent"* is CONFIRMED by
execution; `jailed` is the reversible member and it now dissolves nothing.

## §4 · THE EIGHT REQUIRED SYMBOLS, AT `19c4cb853`

```
$ for each (path, symbol):  git show 19c4cb853:<path> | grep -cF "<symbol>"   (and -nF for the line)
1  line 157     src/domain/roads/state.js                     :: export function isOffStage
1  line 151     src/domain/npc/npcOps.js                      :: export function isInStasis
1  line 105     src/domain/entities/npcs.js                   :: export const NPC_UNAVAILABLE_STATUSES
1  line  92     src/domain/worldPulse/warSeatBooks.js         :: function rosterNpcById
1  line 618     src/domain/worldPulse/warSeatBooks.js         :: export function readWarSeatBooks
1  line  94     src/domain/worldPulse/worldSnapshot.js        :: export function buildWorldSnapshot
1  line 113     src/domain/density/factionLifecycle.js        :: export function factionRosterOf
1  line 124     tests/lint/statusUnionTotality.walker.test.js  :: const CONSUMER_ROSTER
```
All eight resolve exactly once, at the same lines version 2 recorded at `e5bdfd031`. **CONFIRMED.**

```
$ git show 19c4cb853:src/domain/roads/state.js | sed -n '157,161p'
export function isOffStage(npc) {
  if (isInStasis(/** @type {Parameters<typeof isInStasis>[0]} */ (npc))) return true;
  const w = npc && typeof npc === 'object' ? /** @type {Record<string, unknown>} */ (npc).whereabouts : null;
  return !!(w && typeof w === 'object' && /** @type {Record<string, unknown>} */ (w).state === 'hostage');
}
$ git show 19c4cb853:src/domain/roads/state.js | grep -n "^import"
34:import { isInStasis } from '../npc/npcOps.js';
35:import { detPow } from '../../kernel/detPow.js';
36:import { importanceWeight } from '../entities/npcs.js';
…
$ git show 19c4cb853:src/domain/entities/npcs.js | grep -n "NPC_UNAVAILABLE_STATUSES"
105:export const NPC_UNAVAILABLE_STATUSES = Object.freeze(['dead', 'exiled', 'jailed', 'removed']);
```
⇒ the chokepoint still never reads `.status`; the availability vocabulary is unchanged. **CONFIRMED.**

## §5 · THE J-T1 WINDOW, AND THE PREAMBLE HASH

```
$ git -C $SP/slot-2 diff --stat e5bdfd031 19c4cb853 -- <all 12 change paths> <all 7 requiredSymbols paths>
 tests/domain/roadsParticipation.test.js | 118 ++++++++++++++++++++++++++++++++
 1 file changed, 118 insertions(+)
$ git -C $SP/slot-2 merge-base --is-ancestor e5bdfd031 19c4cb853 && echo YES
YES
$ git -C $SP/slot-2 log --oneline e5bdfd031..19c4cb853
19c4cb853 EM-B1k: the tick's settlement is born from the raw save roster …
07cc2efb6 PACKETS: EM-B1k's §7 change table made set-equal with its JSON manifest …
1e5bcf83d REGISTER: the lighting census re-derived at train EM-T3's terminal …
1b381485f PACKETS: EM-B1k placed and READY at c740ded8b (version 2) …
c740ded8b DOC: the chair's owed corrections on the branch …
32602dc60 CURE-D: the prose-wiring census re-taken …
145acdb75 CURE-C: EM-B1e's acceptance file is statically registered …
8955b67a8 CURE-B: the prose-numerics baseline re-addressed …
2d72c8b7a CURE-A2: the move-vocabulary walker's twin mask …
08cb42e31 CURE-A: the model walker blanks import lines …

$ git -C $SP/slot-2 show 19c4cb853:docs/implementation/preambles/EM-PREAMBLE.md | shasum -a 256
b90a95b7af484137ecf70bd15cde5054edf66b5db0d9f6e90c974d97b7caa5e1  -
```
⇒ the preamble hashes to the brief's stamp exactly; ONE packet path moved in the window, and it is
`roadsParticipation.test.js` (+118 = EM-B1k's A3 at `:65` and A5 at `:108`). **CONFIRMED.**

## §6 · A1's REPRODUCTION AND ITS POST-ARM VERDICT, AT `19c4cb853`

`$SC/v3-seat-probe.mjs`, fixtures copied verbatim from `tests/domain/warSeatBooks.test.js`.

```
$ PULSE_MODE=landed ARM_MODE=off node --experimental-loader $SC/v3-loader.mjs $SC/v3-seat-probe.mjs
══ ARM_MODE=off | OFF_STAGE_STATUSES=null
  ACTIVE (control)          | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  status: dead              | isOffStage false | unseated | 0      | (absent)  | realm | 1
  stasis imprisoned         | isOffStage true  | unseated | 0      | (absent)  | realm | 1
  whereabouts hostage       | isOffStage true  | unseated | 0      | (absent)  | realm | 1
  status: 'jailed'          | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256   ⛔ THE DEFECT
  status: 'JAILED'          | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  status: 'exiled'          | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256   ⛔
  status: 'removed'         | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256   ⛔
  status: 'missing'         | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  status: 'retired'         | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  no status key at all      | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  unknown token 'sabbatical'| isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  non-string status (0)     | isOffStage false | holding  | 0.3744 | a:ruler   | seat  | 0.6256
  null/undefined npc -> isOffStage: false false

$ PULSE_MODE=landed ARM_MODE=on  …
══ ARM_MODE=on | OFF_STAGE_STATUSES=["exiled","jailed","removed"] | frozen=true
  ACTIVE (control)          | false | holding  | 0.3744 | a:ruler  | seat  | 0.6256
  status: dead              | false | unseated | 0      | (absent) | realm | 1
  status: 'jailed'          | TRUE  | unseated | 0      | (absent) | realm | 1   ⭐ design §15
  status: 'JAILED'          | TRUE  | unseated | 0      | (absent) | realm | 1   ⭐ lowercasing
  status: 'exiled'          | TRUE  | unseated | 0      | (absent) | realm | 1   ⭐
  status: 'removed'         | TRUE  | unseated | 0      | (absent) | realm | 1   ⭐
  status: 'missing'         | false | holding  | 0.3744 | a:ruler  | seat  | 0.6256   ⛔ Q1 PINNED
  status: 'retired'         | false | holding  | 0.3744 | a:ruler  | seat  | 0.6256   ⛔ Q1 PINNED
  no status key at all      | false | holding  | 0.3744 | a:ruler  | seat  | 0.6256
  unknown token 'sabbatical'| false | holding  | 0.3744 | a:ruler  | seat  | 0.6256
  non-string status (0)     | false | holding  | 0.3744 | a:ruler  | seat  | 0.6256
  null/undefined npc -> isOffStage: false false
```
⇒ every row of A1, A2 and A3 is executed, in both directions, at the tree the build starts from.
**CONFIRMED.**

## §7 · ⭐⭐ THE NINTH ROSTER ROW, SIMULATED THROUGH THE WALKER'S OWN `offencesOf`

`$SC/v3-walker-loader.mjs` stubs `vitest` and appends ONE export line to the walker's own source;
not a byte of its logic is changed and nothing is reimplemented. `$SC/v3-walker-probe.mjs` builds
the post-edit `state.js` by the same textual patch the runtime probes use, and throws if the anchor
stops matching.

```
$ cd $T && node --experimental-loader $SC/v3-walker-loader.mjs $SC/v3-walker-probe.mjs
NPC_STATUS            : ["active","dead","exiled","jailed","missing","removed","retired"]
NON_ACTIVE            : ["dead","exiled","jailed","missing","removed","retired"]
TRIGGER (derived)     : ["dead","exiled","retired"]
ALL_BUT_DEAD          : ["exiled","jailed","missing","removed","retired"]
SRC_FILES scanned     : 2246
CONSUMER_ROSTER rows  : 8
FLAGGED == DECLARED   : true
DISCOVERED_ENUMERATORS: ["src/domain/density/factionLifecycle.js","src/domain/entities/npcs.js","src/domain/worldPulse/magicFormsPractitioner.js"]
state.js currently FLAGGED? false
CONSUMER_ROSTER[3] is : src/domain/worldPulse/envoyCasting.js # rosterPersonAvailable

── THE POST-EDIT FILE, THROUGH THE WALKER'S OWN READS ──
non-active members the POST-EDIT file spells: ["dead"]
trigger words it spells (⇒ FLAGGED)         : ["dead"]
hits                                        : ["158:dead"]
is it a DISCOVERED_ENUMERATOR (>=2)?        : false
BASE file spells (control)                  : []

── offencesOf(row, POST-EDIT source) ──
  ✓ LAWFUL     ⭐ ALL_BUT_DEAD (the three chokepoint rows' own shape)
  ⛔ CONVICTED  THE CHAIR'S RULING TEXT: ['active','dead','missing','retired']
        src/domain/roads/state.js: omits a word that is not a member of the live union: active
        src/domain/roads/state.js: enumerates the union without exiled, jailed, removed
  ⛔ CONVICTED  ruling text minus 'active': ['dead','missing','retired']
        src/domain/roads/state.js: enumerates the union without exiled, jailed, removed
  ✓ LAWFUL     ALL_BUT_DEAD + dead (also lawful?)
  ⛔ CONVICTED  union-read (the form the chair asked about)
        src/domain/roads/state.js: declared a union READ but spells dead at 158:dead
  ⛔ CONVICTED  ALL_BUT_DEAD but a SHORT why (<40 chars)
        src/domain/roads/state.js: omits exiled, jailed, missing, removed, retired with no written reason

── the guard-the-guard: DELETING the ninth row ──
  post-edit state.js is FLAGGED (spells a trigger word): true
  with NO ninth row it is absent from DECLARED_FLAGGABLE, so A3's set-equality reds by name: true

── A7 inertness arm (scans src/ only; the packet writes no src/ status) ──
  does the POST-EDIT state.js trip the jailed-writer matcher? false
```

**What this establishes, read off the walker's own code:**
- `offencesOf`'s stale check is `row.omits.filter((m) => !NON_ACTIVE.includes(m))`, and
  `NON_ACTIVE` excludes `'active'` **by construction** ⇒ ⛔ **`'active'` can never lawfully appear
  in an `omits` list.** The charter's ruling (3) text is refuted.
- `missing = NON_ACTIVE.filter((m) => !listed.includes(m) && !row.omits.includes(m))` ⇒ `omits`
  must cover every non-active member the file does not SPELL. Post-edit that set is exactly
  `ALL_BUT_DEAD`.
- A3 asserts `FLAGGED` set-equal to `DECLARED_FLAGGABLE`, which is built from rows with
  `spelling === 'literals'` ⇒ **`spelling: 'literals'` is forced**, and `union-read` is convicted
  outright.
- A2/A4 asserts `DISCOVERED_ENUMERATORS` (≥2 distinct non-active members) equals the declared
  `enumerator && literals` files ⇒ **`enumerator: false` is forced** (the post-edit file lists one).
- A3 drives three synthetic probes off `CONSUMER_ROSTER[3]` ⇒ ⛔ **APPEND, never insert.**
- A7's jailed-writer matcher scans `src/` only and the post-edit file does not trip it.

**CONFIRMED** — every clause above is the walker's own executed output, not a reading of it.

## §8 · THE LIGHTING DELTA, THROUGH THE WALKER'S OWN `parkReasonsFor`

```
$ cd $T && node --experimental-loader $SC/v3-lighting-loader.mjs $SC/v3-lighting-probe.mjs
✓ CREDITED  tests/domain/roadsState.test.js       | reasons: [] | literal it( : 29 | describe( : 12
✓ CREDITED  tests/domain/warSeatBooks.test.js     | reasons: [] | literal it( : 7  | describe( : 1
✓ CREDITED  tests/domain/roadsParticipation.test.js| reasons: [] | literal it( : 6 | describe( : 3
✓ CREDITED  tests/lint/statusUnionTotality.walker.test.js | reasons: [] | literal it( : 0 | describe( : 1

guard-the-guard — a file with no vitest import: ["OPENER_UNRESOLVED:it"]
guard-the-guard — a rebound `it` parameter:     ["OPENER_UNRESOLVED:it"]
```
⇒ all four homes are CREDITED and the classifier is proved live in both directions. The packet adds
four straight-line `it`s across three credited files and no `describe`:
**DELTA `+0 files / +0 parked / +0 credited / +4 titles / +0 suiteTitles`.** No absolute is quoted
(EM-PREAMBLE §P2 row 1). **CONFIRMED.**

## §9 · THE CHUNK CLOSURES (the static-edge resolver copied from `vite.config.js`)

```
$ TARGET_REL=domain/roads/state.js node $SC/v3-closure.mjs
### GENERATION WORKER            modules 220  | roads/state.js IN CLOSURE: NO
### EAGER FIRST PAINT            modules 251  | roads/state.js IN CLOSURE: NO
### ADVANCE INTERVAL WORKER      modules 549  | roads/state.js IN CLOSURE: YES
      src/workers/advanceInterval.worker.js -> worldPulse/advanceInterval.js
      -> worldPulse/pulseKernel.js -> worldPulse/worldSnapshot.js -> roads/state.js
### TOWN SCENE EXPORT WORKER     modules 125  | NO

$ TARGET_REL=domain/entities/npcs.js node $SC/v3-closure.mjs      # (the label string is hard-coded; the TARGET is npcs.js)
### GENERATION WORKER            modules 220  | IN CLOSURE: NO
### EAGER FIRST PAINT            modules 251  | IN CLOSURE: ⛔ YES
      src/main.jsx -> store/index.js -> store/settlementSlice.js
      -> domain/events/mutateEntities.js -> domain/entities/npcs.js
### ADVANCE INTERVAL WORKER      modules 549  | IN CLOSURE: YES
```
⛔ **Version 2's parenthetical "both importers absent" is REFUTED** — `entities/npcs.js` IS eager.
It does not change the price: `roads/state.js` is absent from that closure, and the FORM B edge
points **from** `state.js` **to** `npcs.js`, so nothing is pulled into first paint.

```
$ node <the same resolver, seeded at entities/npcs.js>
npcs.js closure size: 2
  src/domain/entities/npcs.js
  src/kernel/slugify.js
reaches roads/state.js (would be a CYCLE): false
```
⇒ **zero modules are added to any closure** by FORM B. **CONFIRMED.**

The three ceilings, read at `19c4cb853`:
```
$ git show 19c4cb853:tests/build/generationWorkerLazy.test.js | grep -n WORKER_BUNDLE_CEILING_BYTES
159:export const WORKER_BUNDLE_CEILING_BYTES = 1401208;
$ git grep -n "679_000" 19c4cb853 -- tests/
19c4cb853:tests/build/vendorPdfLazy.test.js:787:    expect(size).toBeLessThan(679_000);
```

## §10 · THE BYTES, RE-MEASURED

```
$ STATE_JS_SOURCE=$SC/work3/src/domain/roads/state.js BYTES_DIR=$SC/bytes3 node $SC/v3-bytes.mjs
base         raw  32364 B
formA        raw  32849 B
formBspec    raw  32901 B
formBstmt    raw  32939 B
$ $T/node_modules/.bin/esbuild --version
0.28.1
$ for f in base formA formBspec formBstmt; do esbuild $f.js --minify --format=esm --target=es2022 …
base           7727 B (base)
formA          7885 B delta +158 B
formBspec      7911 B delta +184 B     ⭐ CONTRACTED
formBstmt      7946 B delta +219 B
$ shasum -a 256 $SC/bytes/base.js $SC/bytes3/base.js
23ffc63bb6f39944defe147241f7f9ddbc75966509efb1bcaa4176b789676481  (both)
```
⇒ the version 2 figures reproduce exactly because the base file is byte-identical. `+184 B` is
**CONFIRMED** as a minified-source delta and **PLAUSIBLE** as a rendered-chunk delta (esbuild is not
Rollup's renderer) — hence the ×2 bound of ≤370 B.

```
$ TREE=$T EFF_FILES=src/domain/roads/state.js node $SC/v3-efflines.mjs
   288 effective lines   src/domain/roads/state.js  | raw 596
$ git show 19c4cb853:eslint.config.js | sed -n '712p'
      'max-lines': ['error', { max: 800, skipBlankLines: true, skipComments: true }],
$ git show 19c4cb853:scripts/.size-baseline.json | grep -c "roads/state"
0
```
⇒ 288 / 800, 512 lines of headroom, no baseline row, and absent from `PACKET_STANDARD.md`'s
standing hot list (re-measured by the chair at `c740ded8b`: six rows; `convergence.js` 798 → 764,
`institutionLifecycle.js` joined at 798). **CONFIRMED.**

## §11 · THE EDGE-SHARED SEVEN, BOTH WAYS

```
$ for each meta: git show 19c4cb853:supabase/functions/_shared/<m>.meta.json | (count inputs)
aiCharterBundle          inputs: 114   | roads/state.js: true
aiOutputSchemaBundle     inputs: 115   | roads/state.js: true
aiGroundingBundle        inputs:  74   | roads/state.js: false
analyticsEventsBundle    inputs:   2   | roads/state.js: false
intentAtlasBundle        inputs:   2   | roads/state.js: false

$ git show 19c4cb853:scripts/build-edge-shared.mjs | sed -n '33,39p'
const ENTRIES = [
  { label: 'aiGrounding',     … }, { label: 'analyticsEvents', … }, { label: 'aiCharter', … },
  { label: 'intentAtlas',     … }, { label: 'aiOutputSchema',  … },
];
$ …| sed -n '82,84p'
  writeFileSync(META_FILE, JSON.stringify({
    generatedAt: new Date().toISOString(),
$ …| sed -n '108,110p'
for (const e of ENTRIES) { await buildOne(e); }

$ git log --oneline --stat -3 19c4cb853 -- supabase/functions/_shared
95e494bdb EM-B1d: jailed joins NpcStatus … (version 5)
 aiCharterBundle.js | aiCharterBundle.meta.json | aiGroundingBundle.meta.json
 aiOutputSchemaBundle.js | aiOutputSchemaBundle.meta.json
 analyticsEventsBundle.meta.json | intentAtlasBundle.meta.json
 7 files changed, 15 insertions(+), 9 deletions(-)
ee8ac6c3c … 7 files changed
```
⇒ SEVEN paths, established by reading the generator **and** by EM-B1d's own precedent landing —
exactly the seven this packet declares. **CONFIRMED.**

```
$ git show 19c4cb853:scripts/implementation-packets.mjs | sed -n '30,36p'
export const PACKET_ACTIONS = Object.freeze(['CREATE','DOC','MODIFY','REGISTER','TEST']);
```
⇒ `REGENERATE` is not a member; every generated row is `MODIFY`. **CONFIRMED.**

## §12 · THE GOLDEN POSTURE, RE-MEASURED OVER ALL 525 ROWS

```
$ git -C $SP/slot-2 diff --stat e5bdfd031 19c4cb853 -- tests/helpers/goldenMasterCorpus.js src/generators/
(empty)
$ TREE=$T node $SC/v3-corpus-census.mjs 1
corpus rows total: 525 | stride: 1 | settlements generated: 525
NPCs seen: 5171 | records with NO status key: 4884
status histogram: { "<ABSENT>": 4884, "active": 287 }
NPCs the EM-B1f arm would newly make off-stage (jailed+exiled): 0
```
⇒ ZERO of `exiled`, `jailed`, `removed` anywhere in the corpus; no golden can move.
**CONFIRMED.** (9.3 s user; the generator path is byte-identical since `e5bdfd031`.)

## §13 · EVERY `.status` WRITER IN `src/` — AND THE ONE CORRECTION TO VERSION 2

```
$ grep -rnE "status: ?'(active|dead|exiled|jailed|missing|removed|retired)'" $T/src | grep -v '\.test\.'
src/domain/entities/npcs.js:186           status: 'dead'      ← killNpc          (NPC)
src/domain/entities/npcs.js:277           status: 'active'    ← createNpc        (NPC)
src/domain/events/mutateEntities.js:230/241/410   'active'                       (NPC/inst/faction)
src/domain/events/undoEvent.js:459        status: 'active'                       (NPC, undo)
src/domain/worldPulse/magicFormsPractitioner.js:220  'active'                    (NPC)
src/generators/factionRoles.js:251        'active'                               (NPC)
src/domain/worldPulse/lineageMemberBirth.js:303     'active'                     (NPC)
src/generators/density/densityAscension.js:145      'active'                     (NPC)
src/domain/worldPulse/factionDensityKernel.js:610   'active'                     (NPC)
… plus institution/faction/edge writers, all 'active'
⛔ src/domain/worldPulse/settlementLifecycleFirstClass.js:601  status: 'removed'
⛔ src/domain/worldPulse/tierOutcomeApply.js:143               status: 'removed'

$ sed -n '598,602p' $T/src/domain/worldPulse/settlementLifecycleFirstClass.js
    // Institutions clear — deactivated as archaeology, never erased from the record.
    const institutions = (Array.isArray(settlement.institutions) ? settlement.institutions : [])
      .map((inst) => (inst && inst.status !== 'removed' ? { ...inst, status: 'removed', … } : inst));
$ sed -n '137,144p' $T/src/domain/worldPulse/tierOutcomeApply.js
function demotionFateForInstitution(inst) { … return { fate: 'abandoned', status: 'removed' }; }

$ grep -rn "status: ?'exiled'" $T/src        → (nothing)
$ grep -rln "'exiled'" $T/src
src/domain/worldPulse/magicFormsPractitioner.js
src/domain/density/factionLifecycle.js
src/domain/entities/npcs.js
```
⇒ ⛔ **Version 2's "no writer can assign `exiled` or `removed`" needed one qualifier:** two writers
DO write `status: 'removed'`, and **both write an INSTITUTION** (the function is literally named
`demotionFateForInstitution`; `EntityStatus` at `entities/status.js:23` carries `removed` as a
homonym). **No writer assigns any of the three to an NPC's `.status`.** That is the claim the
packet now makes. **CONFIRMED.**

## §14 · THE LINE-ADDRESSED AND STAMPED REGISTERS (steps 13, 15, 16)

```
$ git show 19c4cb853:tests/lint/.tuning-inventory.json | grep -n -A4 "roads/state.js#ROADS_TUNING"
967:    "src/domain/roads/state.js#ROADS_TUNING": { "exported": true, "idiom": "literal",
       "keys": 76, "line": 222, "spanDigest": "4022da79…811cc", …
$ git grep -n "spanDigest moved" 19c4cb853 -- tests/lint/tuningRegister.walker.test.js
406:      if (was.spanDigest !== now.spanDigest) problems.push(`${id}: spanDigest moved`);
428:      expect(planted.spanDigest, 'a comment inserted above the table must leave the digest
         where it was …').toBe(baseline);
$ git show 19c4cb853:scripts/lib/tuning-inventory.mjs | sed -n '63,67p'
export const TABLE_DEF_RE = /^\s*(export\s+)?const\s+([A-Za-z0-9_]*_TUNING)\b/;
export const NAMED_NUMERIC_CONST_RE = /^(export\s+)?const\s+([A-Z][A-Z0-9_]+)\s*=\s*(-?\d+(?:\.\d+)?)\s*;/;
export const BARE_DECIMAL_RE = /(?<![\w.])\d+\.\d+(?![\w.])/g;
```
⇒ the `line: 222` address goes stale but is **not asserted**; and `OFF_STAGE_STATUSES` matches none
of the three discovery regexes, so the tuning register cannot move. **MEASURED NEGATIVE — §7.1.**

```
$ git show 19c4cb853:tests/lint/.prose-numerics-baseline.json | grep -c "roads/state.js"
0
$ git show 19c4cb853:docs/content/wiring-census.json | (read stamp.files)
stamp.files entries: 7  — all under src/domain/display/stateProse/  | names roads/state.js: false
$ git grep -nE "src/domain/roads/state\.js:[0-9]+" 19c4cb853 -- src docs tests scripts
docs/DESIGN_FP_ARCHITECTURE.md:335              …state.js:319  (riskToleranceOf)
docs/DESIGN_FP_ARCH_GR.md:145                   …state.js:319
docs/DESIGN_FP_ARCH_SP.md:144                   …state.js:319
docs/DESIGN_FP_ARCH_WF.md:173, :770             …state.js:319
docs/SETTLEMENT_CAPABILITY_ATLAS.md:7374        …state.js:145-149  (isOffStage)
docs/implementation/packets/foreign-policy/ES-5B.md:97, :366   …state.js:122  (WHEREABOUTS_STATES)
docs/implementation/packets/foreign-policy/ES-5C.md:435        …state.js:122
docs/implementation/preverification/WF-SUBSTRATE.md:344        …state.js:319
```
⇒ nine hits, **all `docs/**` prose, none read live by a walker** (the census's live `cite:` reads
are `src/domain/prose/holderTable.js`'s, and this file carries none). `:122` sits ABOVE the
insertion and does not move; `:319` and `:145-149` go stale by ~+9. **MEASURED NEGATIVE — §7.1 and
§12.1 item 6.**

## §15 · PLACEMENT AND THE DISCHARGE SWEEP, OVER THE 191-ENTRY REGISTER

```
$ node <sweep over git show 19c4cb853:docs/implementation/PACKET_MANIFEST.json>
entries: 191   | status histogram: { LANDED: 187, SUPERSEDED: 2, READY: 2 }
EM-B1f present: false
NON-TERMINAL COLLISION: EM-B1k READY TEST tests/domain/roadsParticipation.test.js
EM-B1d LANDED · EM-B1e LANDED · EM-B1k READY · EM-B3c READY
EM-B3c paths: tests/security/galleryScannerMirrorTotality.test.js ·
  supabase/migrations/203_… · scripts/ops/migrationRehearsalCore.mjs · tests/ops/migrationRehearsal.test.js ·
  scripts/mutation-coverage-manifest.json · docs/DEPLOY.md · ARCHITECTURE.md · docs/CURRENT_STATE.md
  ⇒ shares NONE of this packet's twelve
EM-B1k paths: MODIFY src/domain/worldPulse/pulseKernel.js · CREATE tests/store/participationWriteBase.test.js
             · TEST tests/domain/roadsParticipation.test.js

$ git show 19c4cb853:scripts/implementation-packets.mjs | sed -n '43p;676p;694,700p'
43: const TERMINAL_PACKET_STATUSES = new Set(['LANDED', 'SUPERSEDED']);
676:    const reservesChangePaths = !TERMINAL_PACKET_STATUSES.has(String(status));
694:      if (reservesChangePaths) { … addError(errors,
         `duplicate change path across packets: ${row.path} (${priorOwner…}, ${idLabel})`); }

$ node <sweep: any other packet naming my eight (path,symbol) pairs>
  requiredSymbols EM-B1d LANDED | src/domain/worldPulse/warSeatBooks.js :: function rosterNpcById
  requiredSymbols EM-B1k READY  | src/domain/worldPulse/worldSnapshot.js :: export function buildWorldSnapshot
  requiredSymbols EM-B1k READY  | src/domain/density/factionLifecycle.js :: export function factionRosterOf
  (no retiredSymbols row anywhere names one of them)
$ node <sweep: any packet declaring src/domain/roads/state.js as a change path>
  (none)

$ node -e '<EM-B1k2 from the kit>'
EM-B1k2 status DRAFT | MODIFY factionDensityKernel.js · settlementLifecycleFirstClass.js ·
  successorNpc.js · roadsKernel.js · CREATE tests/domain/irreversibleRawRoster.test.js ·
  ⛔ TEST tests/domain/roadsParticipation.test.js
```
⇒ ⛔ **EM-B1k's code is landed at `19c4cb853` but its manifest row still reads `READY`**, so
`validate` would refuse this packet with *"duplicate change path across packets:
tests/domain/roadsParticipation.test.js (EM-B1k, EM-B1f)"*. One chair act clears it. ⚠ EM-B1k2
wants the same path. Nothing to discharge in `retiredSymbols`. **CONFIRMED.**

## §16 · `envoyCasting`'s DOUBLE REFUSAL AND THE SUCCESSOR READER (the chair's Q7/ruling 6)

```
$ git show 19c4cb853:src/domain/worldPulse/envoyCasting.js | grep -n "import\|UNAVAILABLE\|rosterPersonAvailable"
50:import { NPC_UNAVAILABLE_STATUSES, importanceWeight } from '../entities/npcs.js';
51:import { isOffStage } from '../roads/state.js';
76:const UNAVAILABLE_STATUSES = new Set([...NPC_UNAVAILABLE_STATUSES, 'missing']);
108:export function rosterPersonAvailable(npc) {
109:  if (isOffStage(npc)) return false;
110:  const status = text(npc.status).toLowerCase();
111:  if (UNAVAILABLE_STATUSES.has(status)) return false;
150:    if (!rosterPersonAvailable(npc)) continue;
```
⇒ ⭐ **Line 50 is this packet's proposed `state.js:36` line, byte for byte, already landed in
another file** — the FORM B specifier form is the estate's own idiom, not an invention. The double
refusal (`isOffStage` then the union) is settled by EM-B1d's vocabulary and needs no edit from this
packet: after the arm, both halves refuse the same three, which is the belt-and-braces the chair
ruled (Q7). The successor reader (`src/domain/entities/successors.js#inferSuccessors`) is the
walker's other `union-read` row and is likewise untouched — the walker's own A5/A6 arm drives both
through the real functions.

---

## §17 · ⛔ HONEST LABELS — what is CONFIRMED and what is not

| claim | label |
|---|---|
| The eight required symbols resolve once each at `19c4cb853` | **CONFIRMED** (§4) |
| The J-T1 window moved one file | **CONFIRMED** (§5) |
| The arm erases nobody from the save at the cured tree | **CONFIRMED** (§2, executed through the shipped entry + the real store commit) |
| The arm dissolves no sole-member house at the cured tree | **CONFIRMED** (§3, eight statuses × four cells) |
| The ninth row's lawful shape, and the charter text's refutation | **CONFIRMED** (§7, the walker's own `offencesOf`) |
| All four test homes are credited ⇒ `+4 titles` | **CONFIRMED** (§8, the walker's own `parkReasonsFor`) |
| `+184 B` as a minified-source delta | **CONFIRMED** (§10) |
| `+184 B` as a RENDERED-chunk delta | ⚠ **PLAUSIBLE** — esbuild is not Rollup's renderer; the ×2 bound (≤370 B) and the build lane's real `npm run build` are the answer |
| Zero modules added to any closure | **CONFIRMED** (§9 — the import statement already exists; `npcs.js` closure = 2, no cycle) |
| Seven `_shared` paths | **CONFIRMED** twice (§11, generator + precedent) |
| Golden neutrality over 525 rows | **CONFIRMED** (§12) |
| No `src/` writer assigns the three to an **NPC's** `.status` | **CONFIRMED** (§13, with version 2's claim corrected) |
| The tuning, prose-numerics, wiring-census and line-citation registers cannot move | **CONFIRMED** (§14, by reading the discovery regexes and the asserted keys) |
| EM-B1k2's `fresh`-fallback finding (`factionDensityKernel.js:721-723`) | ⚠ **not re-measured here** — it is EM-B1k2's, chartered; this packet's own claim does not rest on it (§13.3 of the packet) |
| The probes cover the corpus | ⛔ **NO** — §2 and §3 drive one generated town and one two-house fixture; the corpus-wide claim is the golden one in §12 |

---

## §18 · ⭐ THE BRANCH MOVED DURING THE LANE — RE-MEASURED, AND IT MOVED IN THIS PACKET'S FAVOUR

The compile read `19c4cb853` throughout. As the lane closed, a routine cleanliness check found the
slot worktree at a NEW head and dirty with another lane's work. ⛔ **This lane wrote nothing there:**
every command against `$SP/slot-2` was `git show` / `git diff` / `git log` / `git rev-parse`, and
every file this lane created is under `$SC`.

```
$ git -C $SP/slot-2 rev-parse HEAD
96036427f1f69294dad30db8612296be0cacb1f5
$ git -C $SP/slot-2 merge-base --is-ancestor 19c4cb853 96036427f && echo YES
YES
$ git -C $SP/slot-2 log --oneline 19c4cb853..96036427f
96036427f CURE-F: the tick's seed line compares against the roster it was HANDED, not the one the
          clock chain returned — so a clock-stage roster write survives a dormant tick
9f3455b84 CURE-E: the trade-route token pin reads its producer's DECLARATION, not a second marker
          — and the marker-slice class is closed at both its instances
63e40fe57 PACKETS: EM-B1k LANDED at 19c4cb853 (version 2) — the participation view is never the
          write base … the seventh landing; its §5 roadsKernel hint corrected (:395 → :425)

$ git -C $SP/slot-2 diff --stat 19c4cb853 96036427f -- <all 12 change paths> <all 7 requiredSymbols paths> supabase/functions/_shared/
(empty)

$ git -C $SP/slot-2 show 96036427f:docs/implementation/PACKET_MANIFEST.json | grep -A1 '"id": "EM-B1k"'
      "id": "EM-B1k",
      "status": "LANDED",

$ node <the placement sweep, at 96036427f>
entries: 191   | histogram: {"LANDED":188,"SUPERSEDED":2,"READY":1}
EM-B1f present: false
✓ NO non-terminal packet reserves any of the twelve paths — placement is FREE
  EM-B1k LANDED · EM-B3c READY · EM-B1d LANDED
non-terminal packets: EM-B3c(READY)

$ for each of the eight (path, symbol): git show 96036427f:<path> | grep -cF "<symbol>"
1 · 1 · 1 · 1 · 1 · 1 · 1 · 1
```

⇒ **Chair question 2 is answered by the chair's own act**: EM-B1k is `LANDED`, placement is free,
and no measurement in this evidence needs re-taking — the window over every path this packet names
is empty. The chair may stamp `19c4cb853` or `96036427f`; **`96036427f` is recommended**, because
only there does `validate` pass.

⚠ **In-flight working-tree state observed (another lane's, untouched):**
```
$ git -C $SP/slot-2 status --short
 M docs/implementation/PACKET_MANIFEST.json
 M scripts/mutation-coverage-manifest.json
 M tests/scripts/baseStateCapsule.test.js
R  tests/store/participationWriteBase.test.js -> tests/store/participationWriteBase.contract.test.js
```
EM-B1k's landed CREATE target is being renamed to `…contract.test.js` and registered in
`scripts/mutation-coverage-manifest.json` (TOOL-6's family). It touches none of this packet's
paths, but the chair should know a rename of a LANDED packet's declared path is in flight.
