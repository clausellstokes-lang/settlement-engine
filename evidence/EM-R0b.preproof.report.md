# EM-R0b — PRE-PROOF REPORT (Opus PRE-PROOF lane, session a9df403c, 2026-09-20 07:0x–07:2x EDT)

## STATUS: **DRAFT, READY-able at `141a1d775`** — no premise refuted; ONE acceptance contract open.

Every version-3 verified fact was re-found BY SYMBOL and RE-EXECUTED at the read tip, and every one
holds to the row. The packet is promotable once the chair answers question 1 (the stride) and stamps
the base at or after EM-R0a's and EM-R0d's landings.

**Files** (all under `$SP/lane-preproof-EM-R0b-scratch/`, the kit untouched):

- `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad/lane-preproof-EM-R0b-scratch/EM-R0b.md`
- `…/EM-R0b.manifest.json` · `…/EM-R0b.evidence.md` (V3-1…V3-8 untouched, P-1…P-12 appended) · `…/EM-R0b.preproof.report.md` · `…/EM-R0b.lane-resume.md`
- Harnesses: `closure.mjs` · `r0b-preproof.mjs` · `gap.mjs` · `stride.mjs` · `stride2.mjs` (each takes `TREE` from an env var with **no default**)

**Tree:** `$SP/read-tip-141a1d775`, `status --short` EMPTY at start (07:01:41 EDT) and end. Nothing
edited, staged or committed in any worktree, the kit or the ledger. Plain `node` only — no vitest,
eslint CLI, tsc, build or npm script. **No gate was taken and none is owed** (see the resume note).

---

## 1 · THE J-T1 WINDOW

```
$ git -C $T diff --stat 32602dc60 141a1d775 -- <all 4 change rows> <all 5 live requiredSymbols paths>
                                               <both _pending paths>
(exit 0 — NO OUTPUT)
$ git -C $T rev-list --count 32602dc60..141a1d775
24
```

⭐ **EMPTY.** Twenty-four commits moved 31 files; not one is a path this packet names. The five `src/`
files that moved are all `src/domain/worldPulse/**`, and `generateSettlementPipeline`'s **224-module
import closure contains none of them** (P-3, with two positive controls proving the walker resolves).
So every 525-row figure carries by construction — and was re-executed anyway.

## 2 · EVERY VERIFIED-FACT ROW, WITH ITS VERDICT AND COMMAND

Executed by `TREE=$T node r0b-preproof.mjs` unless noted; 525 rows generated in 11 s.

| row | v3 at `32602dc60` | pre-proof at `141a1d775` | verdict |
|---|---|---|---|
| THE CONTROL (the KNOWN pin) | 1 violating row of 525, and it is the pin | `rows=525 violating rows=1` — `town\|germanic\|mountain\|mountain_pass\|civilized\|golden-master-v3` (summary says 6, `dependencies.length=5`) | ✓ **CONFIRMED** |
| `V-DEFENSE-MAGICDEP` identity | 525/525 | `same=525 diff=0 absent=0` | ✓ **CONFIRMED** |
| `V-DEFENSE-TRADITIONS` identity | 525/525 | `same=525 diff=0 absent=0` | ✓ **CONFIRMED** |
| `V-HISTORY-AGE` identity | 525/525 | `same=525 diff=0 absent=0` | ✓ **CONFIRMED** |
| D1 `stress ≡ stressors` | 525/525 | `same=525 diff=0` | ✓ **CONFIRMED** |
| D2 readiness band | 525/525, 0 disagreements | 6 label ranges, **0 overlaps** | ✓ **CONFIRMED** (but see finding D) |
| D3 `readiness` in `rulingStructure.js` | 0 lines | `0` | ✓ **CONFIRMED** |
| D4 the `(15,20]` window | 36 rows / 12 / 24 | `36 rows — isPressured=12 isDeficit=24 neither=0` | ✓ **CONFIRMED** |
| D5 label→flags EVER true | 5 labels, `Import-Dependent → isDeficit(196)+isPressured(12)` | reproduced **to the row** | ✓ **CONFIRMED** |
| D6 labels at 525 | 5 | `5` | ✓ **CONFIRMED** |
| D7 `simulationTrace` evidence | 525 entries, 525 `undefined` | `525 / 525` | ✓ **CONFIRMED** |
| `BASELINE_EDGES` | 4 files / 5 edges | 4 files / 5 edges, quoted | ✓ **CONFIRMED** |
| `EAGER_FIRST_PAINT_MODULES` | 268, 0 `src/domain/edit/**` | **imported**: `size: 268`, `src/domain/edit` → 0; control member present | ✓ **CONFIRMED** |
| worker delta | `0 B` | 0 importers of `src/domain/edit` anywhere in `src/` | ✓ **CONFIRMED** (structural; no dist read) |
| mutation-coverage | no row | enumeration rule executed, `enumerated=false` ×4, 2 controls bite | ✓ **CONFIRMED** |
| prose-numerics · wiring-census · `path:line` | nil | all nil, each measured | ✓ **CONFIRMED** |
| `CHECK_META` totality / key membership | 33/33, 14 keys, 0 bad | **CONDITIONAL** — `RECORD_CLASSES` is EM-R0a's and still absent | ⚠ **CONDITIONAL** (family order) |
| the four ladder symbols | absent | still absent (`src/data/bandLadders.js` FILE ABSENT ×4) | ⚠ **CONDITIONAL** (EM-R0d) |

**requiredSymbols delta: NONE.** All five live rows re-resolve (`grep -cF` → 1, except
`severityBand` → 2, re-confirmed as the `severityBands()` prefix artefact at `:1009`). The five
`_pending` rows stay pending. `retiredSymbols` stays `[]` — the packet CREATEs only and touches no
file any row names, so the post-edit simulation is clean and no LANDED packet's `(path, symbol)` pair
is disturbed. All four CREATE targets ABSENT; `src/domain/edit/` does not exist as a directory.

## 3 · WHAT CHANGED IN THE PACKET (old → new), each with its proving command

**A. ⭐⭐ THE LIGHTING DELTA: `titles +8` → `titles +7`.**
The census sums `liveTitlesIn` over CREDITED files only (`measureCensus`, walker `:601-605`). §9 homes
**A8 in `tests/build/domainGeneratorsBoundary.test.js` — "run, not edited"** — so it lands no `it`.
Seven arms (A1–A7) land in the new file. Version 3 priced eight by counting acceptance CASES; version
2 made the same count, so this is inherited, not a regression.
⇒ **`files +1 · credited +1 · parked +0 · titles +7 · suiteTitles +1`**, no absolute restated. The
live tuple is frozen at `2653 · 383 · 2270 · 25052 · 6680` and is the chair's to stamp.

**B. ⭐⭐ "THE 63-ROW STRIDE" IS DEFINED NOWHERE — §12 question 1.** See §4 below.

**C. THE PREAMBLE HASH.** `b90a95b7…` → **`16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195`**
(`shasum -a 256`, matching the launch override). §P2 row 11's second amendment priced: membership
re-measured **by importing** the set, and the §12 receipt row now says HOW `+0` is established, with
"a skipped `skipIf(!requireDistRead)` arm is not a pass" written in.

**D. THE STALE `§12 question 1` CITATIONS.** Cited 4× but §12 carried no questions — and the chair had
already RULED it: **ODQ §934.47 addendum 38 ruling 1, BOTH** (the check here, the `history` group at
EM-R0a). All four sites re-cut; §12 now carries a real numbered question section.

**E. §10 GAINS `tests/lint` WHOLE.** Measured: `npm run check` runs `test:ratchet` (a counting script)
and `lint` (eslint) — **it never runs the `tests/lint` vitest suite**, and neither does `check:tail`.
The packet CREATEs a test file and ran none of the walkers that read it: the run-17/18/19 family
exactly. Added as a §10 VERIFICATION command, deliberately **not** a sealed `checks` row (the census
arm is expected to red on its interior; a guaranteed-red suite in the sealed chain fails the gate).

**F. THE BASE STAMP IS CONSTRAINED, and now proved.** `assertAncestorAndSubstrate` (`:185-196`) builds
substrate = non-CREATE change rows **∪ every `requiredSymbols` path**. At promotion the five `_pending`
rows join, adding `src/data/bandLadders.js` and `src/domain/edit/recordRegister.js` — files EM-R0d and
EM-R0a CREATE. **A base before either landing makes the dispatch throw.** (`head === verifiedBase`
skips the substrate diff entirely — the cleanest promotion.)

## 4 · THE STEP-5 CHUNK MEASUREMENT AND THE BUDGET TABLE

| budget | membership, measured | delta | what the packet carries because of it |
|---|---|---:|---|
| Generation worker (`WORKER_BUNDLE_CEILING_BYTES = 1401208`, EXACT/zero slack) | the three leaves are in no closure — **0 importers of `src/domain/edit` anywhere in `src/`** | **`0 B`** | **NOTHING.** No TEST row, no re-mint, no build step |
| Lazy engine (`vendorPdfLazy.test.js`, `< 679_000`) | same — not reachable | **`0 B`** | nothing |
| First paint (`EAGER_FIRST_PAINT_MODULES`) | **imported**: `Set`, 268 members, 0 `src/domain/edit/**`; control member present | **`0`** | nothing |
| Edge-shared metas | no leaf is an input to any bundle closure | none | nothing |

⭐ **It lands in NO budgeted chunk**, and the proof is structural rather than a dist byte read:
`src/domain/edit/` has zero importers in the whole tree (the only two mentions are prose in test
headers). That is *stronger* than a skipped `skipIf` arm, which §P2 row 11 now forbids quoting as a
pass. **Nothing was added to the manifest for budgets.**

Scope budget re-checked and unmoved: 4 handwritten files (cap 12) · `≤ 340` effective production
lines (cap 400) · per-leaf `≤ 230` / `≤ 40` / `≤ 70` (cap 250) · 8 acceptance cases (cap 8) · 1 new
logic-bearing leaf (cap 2). **No split is owed.**

## 5 · THE SEALED DISPATCH, READ DRY

| check | verdict at `141a1d775` |
|---|---|
| branch exists / matches capsule | ⚠ needs the worktree ON the verified branch; this read tip is detached and would correctly refuse |
| base is an ancestor of HEAD | passes for any base on the branch |
| **substrate unchanged since base** | ⛔ **constrains the base stamp** — see 3F |
| CREATE targets absent + Git-clean | ✓ all four absent; the directory does not exist |
| manifest validates | ✓ re-parsed after this lane's edits: 4 change rows, no duplicate path, 8 `{id, case}` cases, 5 `checks`, **no generator among them**, `retiredSymbols` `[]` |

## 6 · QUESTIONS FOR THE CHAIR (3 — each with a recommendation)

**1. ⛔⛔ WHAT IS "THE 63-ROW STRIDE"?** A1, A3, A6 and A7 name it; A1's "62 rows clean" IS its
cardinality; **nothing in the tree defines it**. `goldenMasterCorpus.js` exports `goldenCorpus` and
`keyOf` and nothing else; no stride/sample/slice helper exists under `tests/`; and **no arithmetic
stride over 525 yields 63** (every k in 2..12 measured). It is EM-R0c's kit-prototype sample
(`504 = 8 × 63`) carried into a shipping test's contract. The pin is at corpus index **510**, and the
obvious rule `i % 8 === 0` **MISSES it** — which would make A1's pin arm vacuous, the very class
`contractTestAntiVacuity.walker.test.js` exists to catch.
**RECOMMENDED: `rows.filter((_, i) => i % 8 === 6)` — 65 rows, carries index 510 by construction,
EXECUTED at this tip to 64 clean + 1 violating, and it is the pin.** A1 then reads "64 clean + the
pin". One line of source, reproducible from the tree alone. The only alternative I would defend is the
FULL 525 (also executed, 11 s, 1 of 525) if the chair prefers no sampling at all. Until ruled, A1's
cardinality is a marked placeholder (`__STRIDE_RULE__`, `__STRIDE_N_MINUS_1__`) in both the packet and
the manifest.

**2. Does the corrected lighting delta `titles +7` stand?** It follows mechanically from the walker's
own counting rule plus §9's test homes, and I could not execute it (the file does not exist, and a
decisive run needs a throwaway-copy census behind the gate — see the resume note). If the chair
prefers certainty before promotion, that run is the one gated step this packet would benefit from.
**RECOMMENDED: accept `+7` and let the terminal's re-derivation confirm it** — the census is
re-derived by the chair at the terminal anyway (§7), and `+7` versus `+8` costs only a one-line
re-read there, not a re-freeze.

**3. Should EM-R0d's pre-proof own the unwitnessed ladder cut before EM-R0b builds?** The `38` cut is
swallowed by an 11-wide corpus hole at 34..44, so `V-BAND-READINESS`'s 525/525 green does not prove
the cut positions. It does not block this packet — the check passes and is EXACT.
**RECOMMENDED: no re-order. EM-R0d's pre-proof states each cut's witnessed status beside it**, and
this packet's §9 CANNOT-CATCH 7 already records the bound so nobody misreads A1's green.

---

## 7 · ⛔ EVERYTHING NOTICED AND NOT TOUCHED — each specific enough to slot

1. ⭐⭐ **The `38` readiness cut is UNWITNESSED by the 525-row corpus** (41 distinct scores, an 11-wide
   hole at 34..44; the `24` cut witnessed only from above, one score at 24 with nothing at 21–23).
   A1's green bounds nothing about cut positions. → **SLOT: EM-R0d's pre-proof** states each of the
   four ladders' cuts with witnessed/unwitnessed beside it; **EM-R7's corpus ratchet** records the
   witnessed score set. Written into this packet as §9 CANNOT-CATCH 7 and §12 noticed 7.
2. ⭐ **Two test headers assert in prose that `src/domain/edit/**` does not exist** —
   `tests/store/decreeRegistryPersistence.test.js:17` ("which does not exist at this commit") and
   `tests/lib/editTravel.test.js:15`. Both COMMENT-ONLY, **nothing reds** (measured), but both become
   false at the first CREATE under that directory. Under the family order that is **EM-R0a**. →
   **SLOT: EM-R0a's pre-proof carries the two-line prose re-address**; if EM-A2a's `pools.js` lands
   first it inherits the row. Written into this packet as §12 noticed 8.
3. ⭐ **A second `bandLadders.js` basename arrives with EM-R0d.**
   `src/domain/compendium/bandLadders.js` already exists (`buildBandLadders`), read by
   `scripts/generate-compendium-data.mjs`, `src/domain/compendium/searchIndex.js`,
   `src/components/compendium/CatalogTabs.jsx` and two test files, with **three rows in
   `scripts/.observed-shape-readers-baseline.json`**. Addresses are distinct so no import is
   ambiguous. → **SLOT: EM-R0d's pre-proof names the collision and says which instruments address by
   basename.** §12 noticed 9.
4. ⭐ **`npm run check` / `check:tail` never run the `tests/lint` vitest suite** — only
   `test:ratchet` (a counting script) and eslint. Any packet that CREATEs a test file and relies on
   `check:tail` to exercise the test-estate walkers is exposed to the run-17/18/19 family. → Cured
   for THIS packet in its §10; **SLOT: the compile/pre-proof briefs should say so once**, so every EM
   member that creates a test file adds the directory run rather than each lane rediscovering it.
5. **`docs/implementation/PACKET_STANDARD.md`'s hot-file list moved in the window** (+`institutionLifecycle.js`
   798/800, +`App.jsx` 575/600, +`SettlementsPanel.jsx` 577/600; `convergence.js` re-measured 764).
   None is a file this packet touches. → No slot; recorded so the next pre-proof reading §3's
   "Delta in a shared/hot file" knows the list moved under it.
6. **`scripts/mutation-coverage-manifest.json` moved in the window** (+15: EM-B1k2's `mutation` row
   and EM-B3c's `rationale` row). This packet does not name the file, so the brief's "held by
   whichever non-terminal packet names it" warning does not reach here. → No slot; recorded.
7. **Design §22.3 item 5 says "six" cross-key checks where §5.4 says "seven"** — RECONCILED, not
   drift: the design's sentence continues "`stress` and `stressors` … that identity joins the module
   as a check", i.e. six **plus** `V-STRESS-IDENTITY`. → No slot; recorded in P-9 so no successor
   reads it as a contradiction.
8. **The version-2 harness's dead-tree hazard** (`read-tip-e5bdfd031` baked into three scripts) —
   this lane's harnesses all `throw` when `TREE` is unset, with no default. The compile lane slotted
   the kit-wide cure. → Already owned; recorded as executed practice here.
