# RULING — four corrections the scope measurement forced, and CS-9's status
2026-09-05 · Opus 5 · ⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · source: `$SC/SCOPE-MEASURE.md`

## 1. ⛔ CS-9 HAS DECAYED TO CHAIR-CLASS. RULED.
The lane found CS-9 recorded as **"BLOCKING on the lighting wave's critical path"** and correctly
refused to rule it — applying the chair's own amendment to a specific row is a chair act.

**Applying it now:** §893's carve-out amendment leaves **exactly two** gated acts — every
`git push`/deploy, and the owner's WALK. CS-9 (own-export account import as a RESTORE path,
recommended) is neither. It is reversible in a pre-launch tree with no installed users, and
retrovalidation can undo it. ⇒ **CS-9 is chair-class and no longer gates anything.** It unblocks
CHARSET Car 2, which gates roughly half of `L-DOORS`.
⭐ This is the general shape the amendment predicted: rows recorded as owner-gated under the OLD
class-based line stay recorded that way and are **obeyed in silence** long after the line moved.
**A gate is a claim and claims decay** — re-derive every "owner-gated" label against the amendment
before obeying it.

## 2. ⛔ THE WAVE'S SCHEMA RUNG IS SPENT — the charter is now WRONG on its face
CONFIRMED by direct read: `scripts/.observed-shape-readers-baseline.json` `schema` is **15** at
`ca651d54b` and **16** at `43d49a3a0` and the tip. The lighting charter's CH-8 ruling and
`LGT-P15-EP1` both name *"the wave's ONE schema-16 migration"* — **that rung was minted by the
ENCOUNTERS landing at §893.2 and does not belong to the wave any more.** A lane reading the charter
will either believe the wave's migration is done or mint a colliding rung.
⇒ **The charter row must be corrected before the wave dispatches**, and the next free rung is **17**
— which is also the rung the deferred OSR act needs, so there is no collision between them.

## 3. ⛔ A PLAN'S HEADLINE ORDERING RESTED ON A CONSTRAINT THAT NEVER EXISTED
`SCOPE_FLOOR_RATIO = 0.9`, and `check-test-ratchet.mjs:1119` reads `rows.length < floor`.
**`totalFiles`/`totalTests` are FLOORS that red on COLLAPSE, not ceilings** — verified at source.
So **a new test file could never have breached them**, not even when the census was believed full at
10/10. `wave-plan.md`'s D-1/D-2/D-3 ordering argument — that the wave's witness, the reader, the
fences car and the desk leaves were competing for census slots — **was never true**. The plan is
free to reorder on merit.

## 4. THE THREE FIGURES ARE CORRECTED
| item | quoted | MEASURED |
|---|---|---|
| lighting wave | "6 cars + 16 prerequisites" (22) | **31** — 0 done, 4 partial, 27 open |
| W-ARMS | "14 items" | **14** ✅ correct — 0 done, and CONFIRMED zero heraldry code at the tip |
| HORIZON-DARK | "5 cars" | **6** — "5" silently drops B6, which has the longest wall-clock and gates `demographicsEnabled` |

⚠ The two lists **double-count**: `LGT-P9-CS9` ≡ HORIZON-DARK B1 and `LGT-P3-CAPSIG` ≡ B6, so the
union is **at most 35 distinct, not 37**.
⚠ **The wave grew and nobody billed it:** `chanceEncountersEnabled` joined the gated keys (29 → 30),
so the doors are **152 / 38 lit / 114 dark**, not 151/113. ENCOUNTERS added a lighting cost.

## 5. ⛔⛔ A PERMANENT LOSS, RECORDED SO NOBODY HUNTS FOR IT
**`LIGHTING-INVENTORY.md` is GONE** — 169,776 B / 436 lines — proved by two independent negatives
(`git rev-list --all --objects` finds nothing; the `825f209c-…` session dir no longer exists).
**§8, the bill's instrument-and-STOP table, is reproduced by no survivor.** What makes the wave
reconstructible anyway: the §882.1 charter row (all 23 rulings verbatim), `pending/LIGHTING.json`'s
31 items, and the LIGHTINGSCOPE artifacts — all sealed. `docs/DESIGN_HORIZON.md` is **alive** at
728,943 B; it only looked missing because the ledger worktree is dirty with mass `docs/` deletions.

## 6. TAKEABLE NOW, AT ZERO CRITICAL-PATH COST
W-ARMS is gated by **neither** of the other two — its "after the GOLDEN freeze" slot is a chair
cleanliness ruling, not a technical dependency. Three of its items are zero-byte: commit the
`DESIGN_W_ARMS.md` volume (55,812 B, sealed in `session-kit-893b-2026-09-04`, md5 `58e1e8dc…`,
byte-identical — housekeeping, not a rescue), the skeptic pass, and Car 0's probes.

## NOT RULED
The prose-car rebase cost ("2 of 200 overlapping paths") was measured **six landings and ninety cars
ago**. ⛔ **Treat it as EXPIRED, not small.** Re-deriving it is the cheapest measurement that would
improve the plan, and it was rightly refused at load 21.76 with a ratchet running.
