# RECEIPT — lane OSRCORPUS · Opus 5 · 2026-09-05

## ⛔ PARTIAL — THE CORPUS WAS NOT WIDENED. STOPPED ON A FENCE THE BRIEF NAMES.

The ruled act is **"widen the corpus, then take the governed `--write` shrink re-freeze."**
Both halves are refused, and the refusals are measured, not argued:

1. ⛔ **`scripts/lib/observed-shape-corpus.mjs` is a governed DETECTOR SOURCE.** Editing it does not
   merely make `--write` throw — **it darkens the whole gate**, which returns 1 without scanning at
   all. Passing it needs a **schema-17 mint**. The brief's fence: *"no schema rung is minted … stop
   and report."*
2. ⛔ **Widening is NOT a pure shrink.** The ruling's load-bearing premise — *"a widened corpus can
   only ever reduce 'no writer produces this' findings"* — is **REFUTED at baseline grade**: the
   briefed widening clears 33 rows and **mints 20 brand-new ones across 15 files**. The brief's
   fence: *"no identity is added."*
3. ⚠ The baseline's own frozen `corpusMeta` moves in six of nine fields.

**Nothing was landed. The dock is byte-identical to what I received** — `HEAD=940d161ca`,
porcelain **0**, `scripts/lib/observed-shape-corpus.mjs` at sha256 `c964bb9f…a3e7`, and the OSR
gate's output is **diff-identical** to the run I took before touching anything.

⭐ **But the diagnosis is CONFIRMED and the cure is real** — and I measured a *better* version of it
that is a **pure shrink**. §5 is a ready-to-cut mint packet for the chair.

---

## 1 · THE DIAGNOSIS — CONFIRMED, RE-DERIVED, NOT INHERITED

The brief's figures are the previous lanes'. I re-derived every one that this act rests on.

| claim | how | verdict |
|---|---|---|
| corpus is 4 seeds × 4 configs | read `SEEDS`/`CONFIGS` | ✅ `['osr-a','osr-b','osr-c','osr-d']` × 4 |
| **no config loads stress** | executed generation over the real corpus grid | ✅ **0 of 118** `incomeSources` rows carry `isCriminal` |
| the corpus therefore cannot see the writer | folded through the real `foldCorpus` | ✅ observed keys = `desc, percentage, priorityNote, source, weight` — **exactly the exemption's recorded list, key for key** |
| a stress config makes it observable | same harness, stress added | ✅ key enters the shape; **one** config × 4 seeds suffices (measured for three different stress sets) |
| the writer's gate | read at source | ✅ `economicState.js:319` `if (safetyProfile.blackMarketCapture > 10)`; `safetyProfile.js:607-619` `blackMarketCapture = min(80, baseShadowPercent + stressShadowBonus)`, and `stressShadowBonus` is **0 with no stress flag** (insurgency +18, famine +15, wartime +8, tier-scaled) |

**So the finding IS a false positive of corpus reach.** That half of the ruling stands, and is now
measured rather than asserted.

### And the cure genuinely clears the finding
Driving the **real** detector over the **real** widened corpus, the subject file's findings go from
`isCriminal@328, display@598, detail@599` to `display@598, detail@599`. **`isCriminal on
incomeSources` is gone.** CONFIRMED.

---

## 2 · ⛔ BLOCKER 1 — THE CORPUS IS A DETECTOR SOURCE, AND EDITING IT DARKENS THE GATE

Executed, not read off a docstring:

```
isDetectorSourcePath('scripts/lib/observed-shape-corpus.mjs')  ->  true
DETECTOR_INPUT_PATHS = ["tests/fixtures/spatialPackFixtures.js"]      # the ONLY absorbable one
```

Then the instrument's **own** classifier, run over **real on-disk manifests** (not injected):

```
frozen  detectorDigest = 313ada0b…37b
live    detectorDigest = f725e091…413
drift.detectorSources  = ["scripts/lib/observed-shape-corpus.mjs"]
drift.inputs           = []
=> --write branch taken = THROW (detector source changed)
```

And the gate itself, verbatim, exit **1**:

> `observed-shape DETECTOR SOURCE changed since the schema-16 instrument was governed
> (scripts/lib/observed-shape-corpus.mjs); an ordinary gate/write cannot migrate the instrument.
> Build and review the governed migration bundle instead.`

⛔⛔ **The sharp edge the ruling did not have: this is not just a `--write` refusal.** The plain gate
takes the same branch and returns 1 **before scanning anything**. A widened corpus left in the tree
does not trade one finding for a re-freeze — **it turns the entire reader-with-no-writer ratchet
off**. Leaving it in is strictly worse than the finding it cures.

⚠ `--write` on an *uncommitted* edit throws one guard earlier
(`observed-shape current detectorTree is not the exact committed HEAD input tree`), so a lane that
only tried `--write` dirty would have mis-diagnosed the wall. The detector-source refusal above is
from the plain gate, which does not take the clean-tree guard.

**This is the same wall that killed the withdrawn mint**, reached by a different road. Baseline
schema is 16, `BASELINE_SCHEMA` is 16; there is no rung 17.

---

## 3 · ⛔ BLOCKER 2 — WIDENING IS NOT A SHRINK. THE PREMISE IS REFUTED.

I reproduced the gate's **entire declared post-scan chain** (M6 → M11 → M12 → M8/M9 → M13) so the
comparison is at **baseline grade**, not raw. The reproduction is validated three ways:

| check | mine | the artifact | |
|---|---|---|---|
| findings after the full chain | **1994** | `--report` reported **1994** (lane OSRMINT) | ✅ |
| identities | **1410** | frozen inventory **1409** + the 1 NEW `isCriminal` row | ✅ |
| corpusMeta, all 9 fields | configs 4 / gens 16 / originCount 8558 / shapeCount 1299 / steadings 12 / transitions 14490 / pulse 12 / flags 80 / seeds 4 | the baseline's frozen `corpusMeta`, **field for field** | ✅ |

Then, the briefed widening (**one stress config appended to `CONFIGS`**), same grade:

| | BASE | WIDENED |
|---|---|---|
| identities | 1410 | 1397 |
| rows that **vanish** | — | **33** (32 of them frozen rows) |
| rows that **appear** | — | **20 — every one a NEW row, none in the frozen inventory, none in any exemption** |
| surviving rows with a raised count | — | 0 |

**The 20 new rows**, across 15 files:

```
tierChange on outcome        ×6   applyWorldPulse, applyWorldPulsePrimitives, tierOutcomeApply,
                                  tierResourceDynamics, worldPulseFeedCuration, pulseFingerprint
institutionPatch on outcome  ×4   applyWorldPulse, applyWorldPulsePrimitives,
                                  institutionLifecycle, worldPulseFeedCuration
covert on impairments        ×4   corruption, entities/status, beliefMap, institutionStatusLifecycle
… on originContext           ×3   components/map/WorldPulseData (formerSponsorSettlementId,
                                  primaryInstigatorId, supporterIds)
… on good                    ×2   display/deityEffects (effect), region/goodsCatalog (criticality)
recordMode on outcome        ×1   pulseHelpers
```

**MECHANISM, and it is a recorded hazard firing exactly as recorded.** Appending a config makes 20
saves instead of 16, which **re-rolls producer 2's entire pulse**: shapeCount 1299 → 1578 (+279),
originCount 8558 → 10466, transitions 14490 → 17733. Shapes such as `outcome`, `impairments`,
`good` and `originContext` cross **`MIN_ROWS = 40`** and become judgeable, so reads that were
previously *too thin to judge* become findings. The program's own memory row —
*"a shape crossing MIN_ROWS=40 DETONATES the OSR ratchet"* — is precisely this.

⭐ The clean signature of the mechanism: `relationshipPatch` **loses** 11 rows (its key union grew)
while `outcome` **gains** 12. Both directions, one cause.

**`--write`'s law is "may only lower or delete ordinary debt."** Twenty added identities are
inventory GROWTH, which the schema-11 docstring records as the exact thing a shrink-only re-freeze
*cannot express*. Even with the detector door open, this act does not fit through it.

---

## 4 · ⚠ BLOCKER 3 — THE FROZEN `corpusMeta`

The baseline pins the corpus's own shape. The briefed widening moves **six of nine** fields:
`configs 4→5`, `generations 16→20`, `steadingsMinted 12→16`, `shapeCount 1299→1578`,
`originCount 8558→10466`, `transitionCount 14490→17733`. Only a governed migration re-records it.

---

## 5 · ⭐ THE COST, AND A STRICTLY BETTER CURE — A READY-TO-CUT MINT PACKET

### The generation cost the chair asked for

| corpus | build (ms, every run) | vs BASE |
|---|---|---|
| **BASE** (4×4) | 8367 · 8462 · 8469 · 8507 · 8550 | — (~8.5 s) |
| **A — briefed:** +1 stress CONFIG (5×4) | 10764 · 10781 · 10787 · 10815 | **+2.3 s, +27 %** |
| **B — topology-only stress pass** | 8550 | **inside the BASE band; ≈ +1 %, free** |

Full-tree scan is unaffected (BASE 3014/3030 ms; A 3122/3124 ms). Whole gate wall-clock: **16 s**.

### Option B, measured: same cure, PURE SHRINK, no pulse re-roll

Push the four stress-loaded settlements into `roots` **but not into `generated`**, so producer 2's
pulse corpus is untouched:

```js
  // stress-loaded generations, TOPOLOGY ONLY.
  for (const seed of SEEDS) {
    roots.push({ name: 'settlement', value: generateSettlementPipeline(
      { ...CONFIGS[0], stressTypes: ['insurgency', 'famine'] }, null, { seed, customContent: {} }) });
  }
```

| | Option A (briefed) | **Option B** |
|---|---|---|
| clears `isCriminal on economyStateProse.js` | ✅ | ✅ |
| **new rows minted** | **20** | **0** |
| frozen rows cleared | 32 | **12** |
| ceilings breached | 0 | **0** |
| corpusMeta fields moved | **6 of 9** | **2 of 9** (`originCount` +2, `transitionCount` +6) |
| corpus cost | +27 % | **≈ free** |

**Option B is the act the chair actually wants**: it is a pure shrink, it costs nothing, it moves
almost nothing, and it clears twelve banked rows as well as the target — including both sibling
`isCriminal on incomeSources` reads at `EconomicsTab.jsx` and `treasury.js`. Its full reconciliation:

```
DELETED (12 frozen rows) : EconomicsTab.jsx isCriminal on incomeSources · treasury.js isCriminal on
  incomeSources · rulingPower.js / applyWorldPulseOccupationAuthority.js / occupation.js /
  pdf/lib/viewModel.js  modifiers on factions · beliefMap.js / factionCompetition.js /
  npcLadderKernel.js  captureState on factions · historyGenerator.js message on issues ·
  pdf/lib/viewModel.js + viewModelBodySlices.js  priorityNote on issues
ADDED    : none.        RAISED : none.        corpusMeta : originCount +2, transitionCount +6.
```

⛔ **It still needs a schema-17 mint** — it edits the same detector source. That call is the
chair's, not mine, and the brief forbids me to cut it. Precedent exists and is exact: **schema 15
was a rung cut for a change in this very file**, whose docstring says *"the shrink-only `--write`
refuses a moved detector BY DESIGN … a rung is the only lawful way past it."*

---

## 6 · MEASURED STATE OF THE DOCK — ALL CAPTURED IN-SHELL AS `CMD; E=$?`

| gate | exit | result |
|---|---|---|
| `node scripts/check-observed-shape-readers.mjs` | **1** | one NEW row, `isCriminal on incomeSources` — **`diff` against my pre-probe run: IDENTICAL** |
| `npx vitest run tests/lint/observedShapeReaders.walker.test.js` | **1** | **2 failed / 42 passed** — exactly the two briefed arms (`SHRINK-ONLY`, `A1/A7`) |
| `npx vitest run tests/lint/` | **1** | **3 failed / 2128 passed**; beyond the two OSR arms, **only `clampPrimitiveBaseline`** |
| three desk suites | **0** | **136 passed (3 files)** |
| `npm run typecheck:domain:strict` | **0** | `✓ no strict-type regressions (1121 errors, ceiling 1121)` — the real script |

`HEAD=940d161ca155ab2be76c9d15b9a8207d9c2f7c2f` · porcelain **0** · `git diff` empty ·
`scripts/lib/observed-shape-corpus.mjs` sha256 `c964bb9f491af50eac289d9f07b7119d5c3a98231aac77d417cee3232fb4a3e7`,
`cmp`-identical to the backup taken before the first probe.

---

## 7 · WHAT I DID NOT DO

No corpus widening landed. No `--write`, no `--migrate-schema`, no baseline edit, no ceiling moved,
no identity added, no schema rung minted, no read deleted, no commit, no ref write, no rebase, no
push, no `git checkout`, no `git show … > path`. Every probe edit was applied and reversed by
inverse edit **and verified byte-for-byte with `cmp` against a pre-taken backup**.

⚠ **One self-correction, and it is a hazard row.** One inverse edit removed its inserted block but
left **a stray blank line** — `git status` said "modified", but nothing in the diff *looked* wrong
and a lane trusting its own inverse edit would have shipped it. `cmp` against the pre-taken backup
is what caught it. **Never treat an inverse edit as self-verifying; diff it against a byte copy.**

---

## 8 · MEMORY ROWS FOR THE CHAIR TO FILE
*(index is chair-only; these are handed up rather than written as orphan topics)*

1. ⛔⛔ **A CORPUS WIDENING IS A DETECTOR CHANGE, AND IT TURNS THE GATE OFF.**
   `observed-shape-corpus.mjs` is one of the eleven `scannerToolFiles()` and is **not** in
   `DETECTOR_INPUT_PATHS` (whose only member is `spatialPackFixtures.js`). Editing it makes the
   **plain gate** exit 1 without scanning, not merely `--write` throw. Widening the corpus is a
   schema-rung act, always.
2. ⛔⛔ **"A WIDER CORPUS CAN ONLY SHRINK FINDINGS" IS FALSE.** Measured at baseline grade: a
   4→5 config widening clears 33 rows and **mints 20 new ones**, because more generations re-roll
   the pulse and push shapes across `MIN_ROWS = 40` into being judgeable. Widen the corpus's
   *topology* without widening its *pulse* and it is a pure shrink instead.
3. ⭐ **THE PULSE IS THE BLAST RADIUS, NOT THE GENERATION.** Feeding stress settlements into `roots`
   but not `generated` costs ~1 % and moves 2 corpusMeta fields; appending a `CONFIG` costs 27 %
   and moves 6.
4. ⚠ **AN INVERSE EDIT IS NOT SELF-VERIFYING** — a block removal left a stray blank line. Take a
   `cp` backup first and `cmp` against it.
5. ⭐ **THE GATE'S FILTER CHAIN IS REPRODUCIBLE OUT-OF-BAND** and this is the way to measure a
   corpus change that the gate itself refuses to scan under: `scanReaders` →
   `applyShapeFamilyFilter` → `applyDomGlobalReceiverFilter` → `applyLanguageSurfaceFilter` →
   `applyExplainedWriterFilter` → `applyVirtualDormantWriterFilter`. Validated: it reproduces
   `--report`'s 1994 findings and the frozen `corpusMeta` field for field.

---

## RETROVALIDATION ROW

⟦**OPUS-AUTHORED — Fable retrovalidation OWED**⟧ · lane OSRCORPUS · 2026-09-05 · Seat: Opus 5.

**CONFIRMED (executed evidence, quoted above):** the corpus observes no `isCriminal` (0/118 rows);
one stress config makes it observable; the widened corpus clears the target finding;
`isDetectorSourcePath` → `true`; the live `provenanceDriftOf` names the corpus as a detector source
with zero inputs; the gate's verbatim detector-source refusal at exit 1; the 20 new rows / 33
vanished at baseline grade under a filter chain validated against `--report`'s own 1994/1410 and the
frozen `corpusMeta`; Option B as a 0-added / 12-deleted pure shrink; every cost figure; and the
dock's byte-identical final state with all five gates re-measured.

**PLAUSIBLE (reasoning only):** that a schema-17 mint built on Option B would reconcile exactly as
§5 predicts. The reconciliation was measured out-of-band through the reproduced chain, not through
the governed migration bundle, which no lane may build. The experiment that would settle it is the
chair cutting the rung and running `--migrate-schema=17` with the review bundle.

No product bytes changed. There is nothing to retro-validate but the measurements and the refusal.
