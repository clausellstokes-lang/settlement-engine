# SKEPTIC LENS — THE EIGHTEEN CODE CURES (car 9), against FOLD §3a's ARM lines and §L

Seat: Opus 5 — Fable-unvalidated (the refuter). Pinned dock `$SC/skepINSTR2` @ `454d478a1`,
read-only. `git status --porcelain | wc -l` = **0 before, 0 after**. Seven plants applied and
restored (`cmp` byte-identical, md5 re-checked each time): #80, #81, #82, #77, plus three of
my own (the coinFlows read, the arm-E fail channel, the cure-15 limb, the cure-2 locator).
Every figure below came from a command I ran in this session.

## THE HEADLINE
**Fifteen of the eighteen cures reproduce exactly. Three are PARTLY, and one receipt sentence
is REFUTED.** The sharpest finding is a class, not a single row: **cures 2 and 15 shipped the
CODE without the ARM their fold lines named**, and the receipt's verdict column reads DONE for
both in the same shape as the sixteen that do carry arms. I proved the gap by reverting each
cure on a copy — the six-walker gate stays **24/24 green** either way.

## PER-CURE

| # | verdict | what I measured |
|---|---|---|
| 1 | **CONFIRMED** | roster **35 → 249**, candidates 37, absent **25 → 12** (`bailiff` among them); census row `C2 · an office the world does not hold` is **absent from the census = 0**. NOT over-broad: all 13 newly-resolved nouns hit a real held role (12 exact; `abbess` via `Abbot/Abbess`), and seven fabricated offices (archon, vizier, margrave, exarch, tribune, podesta, landgrave) stay ABSENT. A real office breach is still caught. |
| 2 | **PARTLY** | the code cure is real: `"The wall is old, and all souls are counted here."` reads **FAIL `C4/a totality over an open column`**. But the ARM line ("the four probe4 sentences as fixtures") was **never built** — the sentence exists only in a comment in `entryWalker.js:195`. Reverting `locate()` to `masked.indexOf(q)` and `t.indexOf(phrase)` leaves the walker **24/24 green**. |
| 3 | **CONFIRMED** | R4b **53** = `heraldIntegrity.js` 18 + `causeLifecycleVocabulary.js` 32 + `causeWalk.js` 3, asserted as an exact per-file map. |
| 4 | **CONFIRMED** | chronicle 85 rows; **R11 11 · R12 74**; every R11 row is a `letter.*` row; census heading now `chronicle (R11 · R12)`. |
| 5 | **CONFIRMED** | the third fail-closed path is DRIVEN — a `toThrow` case beside the two existing ones. |
| 6 | **PARTLY** | `COLUMN_SOURCES` published; `closed` derived; `whatItCounts` 3/3, `whatItDoes` **1 of 6**, `whatItDoesNotDo` 3 of 4, `holderRole.basis` `'absent'`. Plants **#80 → 2 red of 17** and **#81 → 1 red of 17**, reproduced exactly. ⛔ **BUT THE THIRD SOURCE IS UNGUARDED.** `coinFlows.taxed` is declared `read: true` and reaches only a `basis` STRING no test asserts; on every settlement the absent branch is taken. I replaced the read with `NaN` and the walker stayed **17/17 green**. `whatItCounts.closed === true` — the over-licensing direction §L.2/62 forbids — rests on a source whose removal no arm can see. |
| 7 | **CONFIRMED** | **23** service rows dropped over **30** settlements, naming `(arcane underground) · (informal) · (lawless) · (smuggling) · (street gang)`; plant **#82 → 1 red of 17**. |
| 8 | **CONFIRMED** | one `treatyTerms` toll row ⇒ `values ['toll exemption on the salt road']`, `closed true`, `nullEverywhere false`; without it empty and open; `whoIsCounted.closed` false either way. |
| 9 | **CONFIRMED** | `SENSORY_NOUNS` **166 entries / 166 distinct** (sight 82 · hearing 26 · smell 16 · touch 23 · taste 19). No figure moved: R1 sensory/100w **1.198**, textured **24 %**, spread **0.79 bits**. The "resolver already read them as sight" argument checks out — `SENSE_OF_NOUN` reverses before `fromEntries`, so the FIRST-declared bucket wins. |
| 10 | **CONFIRMED** | `closed` counts a both-channel plant once; `both.openShare` 0 (was −1), `both.closed` 1, `lawful.openShare` 0, `namedOpenShare` 0.5. The cross-seed limb and the class-existence guard are code, both driven. |
| 11 | **CONFIRMED** | `plantLedger.js` `@enforced-by` → `proseMeasures.walker.test.js`; `presenceMeasure.js` gains one; both targets asserted to EXIST. |
| 12 | **PARTLY** (LOW) | the header now names `tests/fixtures/grammarControls.js`, `HAND_TAGGED`, and the walker imports that file (so a bad path would red at import). The ARM asked for a **path-existence assertion in the docblock check**; there is none, so the header STRING is unguarded. |
| 13 | **CONFIRMED** | two pools of one block resolve differently, keyed on `(block, pool)`; gap (a) per pool: register total **103** · pools with any **89 of 132** · pools with more than one **14** · max **2**. |
| 14 | **CONFIRMED** | arm E FAILS **298 / 708 (42.1 %) uniformGrammar · 407 / 708 (57.5 %) uniformSegments · 79 / 708 (11.2 %) repeatedOpener** against 0.30 / 0.40 / 0.030 — to the unit. B2 silent on a fair draw and fires above the chance floor. BUDGET driven in **all three** outcomes (fail / note / silent). SPREAD asserted on content. Walls **4, 5, 7, 8, 9** emit NOT-EXECUTABLE with `Wanted:`; wall **10 is detected**, present-then-absent. **I stubbed arm E's fail channel: 2 red of 49** — the ARM line's demand is met. |
| 15 | **PARTLY** | behaviour and figure both reproduce: `power.generated.js::DS-POW-5::autocrat#2` reads **WITHHELD** with `Q/a trailing coordinate naming no second field`, and the Q class is **890 over 2,266 R1 entries** (330 trailing + 560 second-sentence). ⛔ But the ARM the fold named — an assertion in `proseEntryContradiction.walker.test.js` — was **never built**: `DS-POW-5::autocrat` appears nowhere in that file. I stubbed the whole semicolon limb and the walker stayed **24/24 green**. ⚠ Also a wording correction: 890 is a count of FINDINGS, not of variants; the trailing limb is carried by **329** entries. |
| 16 | **PARTLY, with one REFUTED sentence** | anchor 6 is built and passes. The 44 = 44 explanation is **right and stronger than asserted**: I checked set IDENTITY over the 3,132-entry corpus — notExecutable {44} and fails {44} are the **same 44 entries**, 0 either way (the gate asserts only the two cardinalities). ⛔ **REFUTED:** the receipt and the code comment both say `:2247` *fires no arm of this walker at all* at any flag value. Loaded from the annex, it fires **five arms at every flag value** (2 WITHHELD `Q`, 3 NOTE — C1 ×2, C4), verdict WITHHELD. What it fires is **no FAIL and no C3** — which is all the gate actually asserts (`.fails` empty). The refusal's substance stands; its sentence does not. |
| 17 | **CONFIRMED** | the three-step procedure is in the docblock; `FAILING_FLOOR = 4` is named and USED (`expect(failing).toBeGreaterThan(FAILING_FLOOR)`); `armCandidates()` prints live replacements per arm (93 · 10 · 2). |
| 18 | **PARTLY** | **14 composers · 39 blocks · 287 lines** over 3 towns reproduce, politics IS passed (`settlementBlocs` into `powerStateProse`) and the general desk's bare strings ARE harvested (85 of the 287). ⛔ But the fold's ARM demanded **0 DORMANT draws** on the re-run: the shipped sequence reads **DORMANT 3** over 3 towns and **200** over 200 towns. Zero is reached only under a synthetic materialised ledger. The receipt discloses this in §9.2; the §9.1 tail and the lane's headline read "3 → 0" as though the arm were met on the sequence itself. |

## THE K.2 RE-MEASURE — REPRODUCED TO THE UNIT
I copied `reading-sequence-9.mjs` into my own directory, repointed its dock at `skepINSTR2`
(never entering `laneINSTR`), and ran it at 200:
```
towns 200 of 200 (generator throws 0) [fresh world — no politics ledger]
desk throws: none
composers reached: 14
provenance rungs 13505 · bare general-desk sentences 5542 · LINES 19047
distinct blocks fired: 39 · draws from the DORMANT pool key: 200 · distinct orders n = 29
V1 share 0.8650 (16475 of 19047) · run rate 0.7525 (14332 of 19046 pairs)
```
**V1 0.8650 · run 0.7525 · 19,047 lines · n = 29 — CONFIRMED, every digit.**

## AUXILIARY CLAIMS I EXECUTED
- **the six lane walkers = 138** — CONFIRMED by addition of seven separate runs: entry 24 · grammar 49 · loaders 13 · measures 14 · wiring 21 · institution 17 = **138**.
- **car 8's census** re-reads at the tip: pools 708 · RESOLVED 310 · UNRESOLVED 398 · variants 2,266 · rungs 398/167/74/69 · TIERS MISSING 58 · THIN 483 · COVERED 225. CONFIRMED.
- **plant #77 (U1)** — **4 red of 17**, `whoIsCounted is OPEN on every tier` by name. Matches the receipt's U1 row exactly. ⚠ the sweep's own comment still reads "1 red of 12" (stale, cosmetic; `check_caught` only needs a non-zero exit).
- **car 9b's register** — `measuredAtSha` → `8c53ddef1`, titles **23,717 → 23,730**, suiteTitles **6,352 → 6,354**, files 2,549 and parked 375 **unmoved**. The gate is green at the tip (**34 passed**).
- **zero product bytes outside the island** — CONFIRMED: no file under `src/` outside `src/domain/prose/` + `institutionTable.js` names any of the ten lane modules.
- **"329 passed over sixteen files"** — **UNTESTED.** My fence permits one focused file at a time, so I cannot reproduce a sixteen-file tally.

## WHAT I WOULD PUT TO THE CHAIR
1. **Cures 2 and 15 are code without arms** (MEDIUM). Both behaviours are right today and
   nothing reds if either regresses. The receipt should say so, or the two arms should be
   built — the fold named the file and the fixture for each.
2. **`whatItCounts` closes on an unguarded source** (MEDIUM). The honesty rule is that
   `closed` implies every source read; the gate cannot detect the third source ceasing to be
   read. A one-line assertion on the `basis` string's present branch would close it.
3. **The `:2247` sentence is false as written** (LOW, but it is the refusal's own ground).
   "Fires no arm at all" → "produces no FAIL and no C3 finding at any flag value".
4. **Cure 18's DORMANT figure needs its qualifier carried into every summary** (MEDIUM). A
   reader taking "DORMANT 3 → 0" from the cure table will believe the shipped sequence draws
   no dormant key; on a fresh world it draws one per town.

Seat: Opus 5 — Fable-unvalidated
