# SKEPTIC FOLD 2 — LANE INSTR-912, CARS 8 · 8b · 9 · 9b, at the pinned tip `454d478a1`

**CONFIRMED 46 · REFUTED 7 · PARTLY 26 · UNTESTED 5** — porcelain, every lens: census `0 → 0`
(one plant, #79, applied and restored `cmp`-identical, md5 `ac78a0ef4d78271cd0a4b18ce683f1a7`)
· cures `0 → 0` (seven plants — #80, #81, #82, #77 and three of the lens's own — each applied
and restored `cmp`-identical) · receipt-fences `0 → 0` (one plant on `institutionTable.js`, md5
`12401cb8bae0a859204ec67910182925` both sides) · this fold `0 → 0` (read-only; no plant).

Seat: Opus 5 — Fable-unvalidated (the folder). Dock `$SC/skepINSTR2` @ `454d478a1`, eleven
commits over `3b1c0eaa5`, verified by my own `git log`. Every figure below is either a lens
file's or one I re-took in this session; the ones I re-took are marked **[folder]**.

**THE HEADLINE, IN ONE SENTENCE.** The lane's *structural* figures survive intact and reproduce
to the unit on three independent readings; its *semantic* figures do not — **"RESOLVED" does not
mean the predicate was recovered, "MISSING 58" is overstated by 41 %, "158" is 74 parts the
instrument's own bookkeeping, and "V1 0.8650" is a statement about rendered text, not about the
dossier's grammar.** Two of the eighteen cures shipped code without the arm their fold line
named, and one arm closes a column on a source no test can see go dark. Nothing found here
moves a product byte, and nothing found here blocks the rebase — but four numbers the authoring
wave is being sized from are wrong or unpinned, and they are all fixable inside the island.

---

## (2) EVERY REFUTED AND PARTLY VERDICT

### REFUTED (7)

| # | the claim | the figure | sev | lens |
|---|---|---|---|---|
| **R1** | §8.2/§8.11: "pools whose selecting predicate was **RECOVERED** — 310" | 310 reached a rung, but **143 carry `predicate: []`** and **14 of the 167** that carry a row hold a code fragment the guard reader bled across statement boundaries (`guardAt`'s `/\bif\s*\(([\s\S]*?)\)…/` takes the LEFTMOST `if`) **[folder: regex at `wiringCensus.js:270`]**. Clean, usable predicates **≈ 153 of 708** | **HIGH** | `census.md` §a |
| **R2** | §8.3/§8.11: MISSING **58** — the figure §912.1 condition one is discharged with | **24 of the 58 are read by a pool-key function** (`spokenTo` sees only `predicate[].field`, and 143 of those are empty) and **3 more** are spoken to via a deeper path with no `rootOf` normalisation — unlike `censusSummary`'s own unread-field arm at `:862`, which DOES normalise **[folder: `spokenTo` at `:683`, no `rootOf`]**. Corrected **≤ 34**, a **41 % overstatement** | **HIGH** | `census.md` §c |
| **R3** | §8.2/§8.11: "predicates over a field no composer holds: **158**" — and refusal 3's claim that the bookkeeping version of this arm was cured | **74 of the 158 are the table rung's own synthetic label** — `readField = "<reader> (via <TABLE> in <file>)"` written into BOTH `predicate[].field` and `fieldsRead`, whose `rootOf` is the whole string and can never be in a held set **[folder: `wiringCensus.js:534-543`, comment and all]**. The comparison moved from `fieldsRead` to `held`; the artefact moved with it. Usable **84** | **HIGH** | `census.md` "THE 158" |
| **R4** | §9.3 refusal 1 / cure 16's comment: `RECEIPT_POOLS…:2247` "fires **no arm of this walker at all**" at any flag value | Loaded from the annex it fires **five arms at every flag value** (2 WITHHELD `Q`, 3 NOTE — C1 ×2, C4), verdict WITHHELD. What it fires is **no FAIL and no C3** — which is all the gate asserts. The refusal's substance stands; its sentence is false | LOW | `cures.md` 16 |
| **R5** | §9.2/§11(f): the Part B §18 edit is the **one licensed exact string replacement**, and its OWED row names a stale trailing parenthetical | The text written is the swap **plus a 27-word parenthetical** **[folder: word count = 27]** naming a sha the car could not know before committing. And the OWED row's phrase *"(blank until measured — a blank is not a number)"* occurs **0 times** in `RULES-V2-PART-B.md` **[folder: `grep -c` = 0]** — the row points at nothing | **MED** | `receipt-fences.md` §D |
| **R6** | §9.2: "V1 **dominates the whole dossier**" — 0.8650 read as a fact about the prose's grammar | **V1 is the classifier's FALLBACK class** — `LEVEL1_ORDERS.V1 = { order: ['PRESENT'] }`, the one-move default returned when nothing is recognised **[folder: `moveGrammar.js:91`]**; `SLOT_RE` needs literal braces, and every composed line is already rendered. On the SAME 2,266 authored R1 variants: markers intact **V1 0.6633, n = 108**; the same texts rendered **0.8124, n = 60**. Rendering alone moves the share **+0.149** and collapses n from 108 to 60 | **HIGH** | `receipt-fences.md` §G |
| **R7** | Part B §18 §16.2: the six-desk 200-town sequence is *"(INSTR car **8**'s probe)"* | **[folder]** The bullet still reads `(INSTR car 8's probe)` — but §9.2 is car 9 REFUSING car 8's probe (it passes no `politics` and cannot see the general desk's bare strings) and building its own sequence. The spec now credits the measurement to the probe the receipt disqualified | **MED** | *(new — folder)* |

### PARTLY (26)

| # | the claim | the figure | sev | lens |
|---|---|---|---|---|
| **P1** | §8.2: "key functions **118**", asserted | `prepared.set(fn.name, …)` keys on the BARE name **[folder: `wiringCensus.js:436`]**, and two desks both export `foodSecurityPoolKey` (`economyStateProse.js:393`, `generalStateProse.js:353`) **[folder]** — so **118 collapse to 117 consulted** while the walker asserts 118 at `:444-445` **[folder]**. Harmless today; the next same-named pair loses real keys silently | MED | `census.md` latent 1 |
| **P2** | §8.2: the two UNRESOLVED reasons, "there are TWO, and they are **exhaustive**" | **8 of 256** mounted-UNRESOLVED rows carry an affirmatively FALSE reason: **4** (all DS-POW-2) whose key IS in a module-level `Object.freeze([['critical','critical matched'],…])` pair-array table `moduleKeyTables` cannot read, and **4** (DS-FTH-3 ×2, DS-ECO-11, DS-GEN-8) lost to a tokenizer desync — an apostrophe inside a double-quoted literal opens a phantom string and eats every later single-quoted key. 3.1 %: a shortfall, not a lie | MED | `census.md` §a |
| **P3** | §8.7 refusal 2 / the LABEL RULE: no predicate is taken from a pool-key STRING | Held for the **FIELD** on all three rungs. But the **TEMPLATE rung's `value` IS read off the key string** (`bindTemplate` binds `posture ${status}` against `posture peace` → `'peace'`) — **69 pools**. Defensible (the composer wrote the template) but it must be said plainly | LOW | `census.md` §b |
| **P4** | §8.9 + the sweep's #79 comment: plant #79 ⇒ "5 red of 21 — anti-vacuity, c1, d, D/wiring, **C-sibling's premise gate**" | **The count is right; the roster is wrong by one.** The five that red are anti-vacuity, **(c1)**, **(c1c)**, **(d)** and the grammar walker's arm D. **C-sibling stayed green; c1c is unnamed** | LOW | `census.md` §f |
| **P5** | §8.4: "TIERS with the pair rows: MISSING **163**", printed directly under `floor 200: 91` | 163 is the **floor-180** figure (58 + 105 = 163); floor 200 gives 149. §8.11's row says "at the 90 % floor" and is correct; §8.4's layout is not | LOW | `census.md` §e |
| **P6** | §8.2/§8.3: the fenced blocks are the output of the command §8.2 names | **Edited, not verbatim.** Five printed `DS-DEF-4 :: capture …` lines are compressed into one and the `(via … in defenseStateProse.js)` tails dropped (the count 65 is right); `facts a key function conjoins: 44` is **asserted at `proseWiringCensus.walker.test.js:283`** and never printed **[folder: assertion confirmed]**; the 19-block settlement-only list is not in the walker's output | LOW | `census.md` latent 3 · `receipt-fences.md` §F |
| **P7** | §8.4/§8.11: "**181** of 708 pools fire" | Reproduces from `firings.json` — but **the probe was never re-run after car 9 found its two defects.** Car 9's corrected sequence reports lines/V1/run and **no corrected pool-firing count**. The block half (39 of 68) is stable across both runs; the key half is untested after the fix | MED | `census.md` §d |
| **P8** | §8.6: `walkPair` implements CLERK-LAWS §2.6's C-pair | It does. But `claimKey` is `` `${klass}|${arm}|${column}` `` — no text, no offset **[folder: `entryWalker.js:974`]**, so an AFTER that adds a genuinely NEW fault of a class the BEFORE already carried reads **`added 0`** and inflates the debt count 1 → 2 (executed). §2.6's rule is "a rewrite may not ADD a FAIL"; this composition cannot see one. `notExecutable` is the AFTER's only **[folder]** | MED | `census.md` §h |
| **P9** | §9.1 cure **2**: verdict DONE | The **code** cure is real (`"The wall is old, and all souls are counted here."` reads FAIL `C4/a totality over an open column`). The **ARM** the fold named — the four probe4 sentences as fixtures — was never built; the sentence exists only in a comment at `entryWalker.js:195`. Reverting `locate()` leaves the walker **24/24 green** | MED | `cures.md` 2 |
| **P10** | §9.1 cure **6**: `whatItCounts` closed, 3 of 3 sources read | Two of three sources are guarded (plants #80 ⇒ 2 red of 17, #81 ⇒ 1 red of 17, reproduced). **The third is not**: `coinFlows.taxed` is declared `read: true` and reaches only a `basis` STRING no test asserts. Replacing the read with `NaN` leaves the walker **17/17 green** — `closed === true`, the over-licensing direction §L.2/62 forbids, rests on a source whose removal no arm can see | MED | `cures.md` 6 |
| **P11** | §9.1 cure **12**: the hand-tagged sample path | The header names `tests/fixtures/grammarControls.js` / `HAND_TAGGED` and the walker imports the file (a bad path would red at import). The ARM asked for a **path-existence assertion in the docblock check**; there is none, so the header STRING itself is unguarded | LOW | `cures.md` 12 |
| **P12** | §9.1 cure **15**: verdict DONE; "the class reads **890 of 2,266** R1 variants" | Behaviour and figure both reproduce (`DS-POW-5::autocrat#2` WITHHELD `Q/a trailing coordinate…`). But the ARM — an assertion in `proseEntryContradiction.walker.test.js` — was never built; `DS-POW-5::autocrat` appears nowhere in that file, and stubbing the whole semicolon limb leaves **24/24 green**. And **890 is a count of FINDINGS**, not of variants (330 trailing + 560 second-sentence); the trailing limb is carried by **329 entries** | MED | `cures.md` 15 |
| **P13** | §9.1 cure **16** / §9.3: "the SAME 44 … the same 44 the flagless walk reports not-executable" | **Right, and stronger than asserted** — set IDENTITY over the 3,132-entry corpus: notExecutable {44} and fails {44} are the same 44 entries, 0 either way. But **the gate asserts only the two cardinalities**, so the identity the receipt claims is not the thing the arm proves | LOW | `cures.md` 16 |
| **P14** | §9.1 cure **18**: "DORMANT draws **3 → 0**"; the ARM demanded 0 DORMANT draws on the re-run | 14 composers · 39 blocks · 287 lines over 3 towns reproduce; politics IS passed; the bare strings ARE harvested (85 of 287). But the **shipped** sequence reads **DORMANT 3 of 3 towns** and **200 of 200 towns**. Zero is reached only under a synthetic materialised ledger. §9.2 discloses it; the §9.1 tail and the lane headline read "3 → 0" as if the arm were met on the sequence itself | MED | `cures.md` 18 |
| **P15** | §9.8: "the fold's **sixty** cures are discharged: 18 code · 42 receipt · 12 spec" | Those sum to **72**, not 60. The error is inherited: FOLD §3b's heading says "RECEIPT CORRECTIONS (30)" while its items run 19–60 (42 items). The car-9 brief already carried the right number | LOW | `receipt-fences.md` §A |
| **P16** | the 42 corrections: "the superseded sentence **quoted**" | 43 rows written (19–60 with no gap, plus 32b) — coverage CONFIRMED. But **14 of 43 describe rather than quote** (22, 23, 25, 31, 38, 40, 41, 43, 47, 53, 54, 57, 59, 60). Seven point at a LIST or TABLE no single sentence can carry; **five (25, 31, 40, 41, 57) paraphrase where a quote was available** | LOW | `receipt-fences.md` §A |
| **P17** | §9.4: one md5 for all four plants, `a1670cfbb22b894e2e051d60992ff2aa` | The committed file's md5 at `8c53ddef1`/`454d478a1` is **`12401cb8bae0a859204ec67910182925`**. The receipt's value is of an intermediate working copy (car 9's own anti-vacuity cure landed after the plants ran), so **the quoted md5 cannot serve a successor as the restore check it looks like** | MED | `receipt-fences.md` §B |
| **P18** | §8.2/§8.11: the census figures the wave is sized from | **`THIN 483` and `COVERED 225` are `console.log` only** — as are 65, 140 and 158. Asserted as integers: 708, 2266, 310, 398, 44, MISSING 58 **[folder: greps of `proseWiringCensus.walker.test.js`]**. Part B §18 now sizes the authoring wave from "MISSING 58 · THIN 483 · COVERED 225", so **two of the three can drift silently under the rebase H6 says must re-measure everything** | MED | `receipt-fences.md` §F |
| **P19** | §9.5 U8: "**159** entries fail on both, and ZERO verdicts flip" | The load-bearing half CONFIRMED — **0 verdicts flip**. The companion integer does **not** reproduce (**148** under the lens's construction), and the receipt never names the settlement its ground came from, so the two are not comparable | LOW | `receipt-fences.md` §B |
| **P20** | §9.1 cure **12**: the "rename" arrow `grammarHandTagged.js → grammarControls.js` | No file was renamed: a one-line reference fix in `moveGrammar.js` (2 ±) plus 47 added lines in `grammarControls.js`. The arrow notation oversells it | LOW | `receipt-fences.md` §E |
| **P21** | §8.5 control (e) / §8.10: THE FENCE | The arm is genuine and non-blind — `expect(hits).toEqual(['src/domain/prose/wiringCensus.js'])` plus a length check **[folder: read at `:261-276`]**. But **it fences `wiringCensus` alone**; the other **nine** island modules have no such arm. Clean today by two independent scans; a wave car could wire one and no gate would say so | MED | `receipt-fences.md` §D |
| **P22** | the sweep's own comment on plant #77 | Still reads "1 red of 12"; the executed figure is **4 red of 17**. Stale and cosmetic (`check_caught` needs only a non-zero exit), but it is the roster a successor reads | LOW | `cures.md` aux |
| **P23** | K.6 cure 1: the union roster is not over-broad | Measured NO over-breadth (finding 2 below). But three of the thirteen newly-held nouns — **`sheriff`, `constable`, `sergeant`** — are held **only** by `ROLE_CATEGORY_KEYWORDS`, a categorisation vocabulary, and by no instantiated role list. No corpus entry turns on them today; a wave car authoring a constable will pass C2 on a **keyword** rather than on an office the world instantiates | LOW | `receipt-fences.md` §C |
| **P24** | FOLD §4's own file count at `74a1aa0e8`: 20 files / 17 additions | Measured **21 / 18** — the fold undercounted the additions by one. At the tip: **25 files, 22 additions, 3 modifications** **[folder: `git diff --name-status`]** | LOW | `receipt-fences.md` §D |
| **P25** | FOLD's join-arm figure: "89 sweep labels, 79 manifest mutation entries", a 1-and-11 asymmetry | Counting `check_caught` **and** `check_caught_planted` against `invariants` **and** `meta`, the join is **exact at every sha: 85 = 85 (base) · 90 = 90 (`74a1aa0e8`) · 94 = 94 (tip)**. The asymmetry was an artefact of reading `invariants` only and missing `meta` — the very section car 9 then used. **[folder: invariants 661 → 666 → 667; meta 12 → 12 → 15]** | MED | `receipt-fences.md` §H1 |
| **P26** | §L.3's renumbering ruling: "renumber 74–79 → 75–80" | That ruling **predates car 9**. The consist now runs **#74 … #82** (nine plants, nine distinct labels), so a chair applying §L.3 verbatim leaves **#80–#82 unrenumbered and colliding**. See H2b | MED | `receipt-fences.md` §H2b |

---

## (3) THE CURES OWED BEFORE THE REBASE AND §914 — 46

### (a) CODE — 14

1. **`src/domain/prose/wiringCensus.js:270`** — `guardAt` must anchor to the **LAST** `if (` before the matched literal, not the leftmost (`String.match` non-global). **ARM:** a fixture composer in `tests/fixtures/wiringFixtures.js` with FOUR `if` branches before the literal (the shipped `shadowEconomyPoolKey` shape); assert the recovered row is `{field:'safetyProfile.blackMarketCapture', op:'>=', value:'3'}`, and keep c1b's one-branch control beside it so both shapes are exercised. Today c1b's single-`if` control **cannot fail on the estate's shape** — that is why R1 was invisible.
2. **same file + `tests/lint/proseWiringCensus.walker.test.js`** — publish and **assert** `resolvedWithPredicate` (and `resolvedWithCleanPredicate`) so "RESOLVED" can never again be read as "recovered". **ARM:** a plant making the literal rung return `predicate: []` unconditionally must red the new integer while 310/398 stay green — the exact false green plant #79's split cannot see.
3. **`wiringCensus.js:683`** — `spokenTo` must normalise with `rootOf` (as `:862` already does) **and** union each row's `fieldsRead`, not only `predicate[].field`. **ARM:** a fixture where a fact is spoken to only via a deeper path (`readings.exportPosture.status`) and a second keyed by a function whose guard did not parse; assert MISSING excludes both, present-then-absent by removing the pool.
4. **`wiringCensus.js:534-543`** — the table rung's synthetic `readField` must not be countable as "a predicate over a field no composer holds". **ARM:** `expect(summary.predicatesOverUnreadFields.filter(r => r.rung === 'table')).toHaveLength(0)`, beside the existing (c3) blind/named/licensed triple.
5. **`tests/lint/proseWiringCensus.walker.test.js`** — **assert THIN, COVERED, 65, 140 and 158 as integers.** The brief's own rule was "every count asserted as an integer", and Part B §18 sizes the wave from two figures that are currently `console.log`. **ARM:** the assertions themselves; #79 already proves the split non-vacuous.
6. **`wiringCensus.js`, `moduleKeyTables`** — read the pair-array table shape (`Object.freeze([['critical','critical matched'],…])`) as well as `{ 'v': 'key' }`. **ARM:** a pair-array fixture table; the four DS-POW-2 rows must move UNRESOLVED → RESOLVED, present-then-absent by striking the shape reader.
7. **`wiringCensus.js`, `keyForms`** — strip DOUBLE-quoted literals before the single-quote key scan (`codeOnly` does not). **ARM:** a fixture composer returning `"NICHE: the patron's niche carries a contestant"` followed by two single-quoted keys; assert both keys resolve; present-then-absent.
8. **`wiringCensus.js:436`** — key `prepared` on `file + '::' + fn.name`. **ARM:** `expect(prepared.size).toBe(census.functions)` with a fixture carrying a same-named pair in two files (the shipped `foodSecurityPoolKey` collision).
9. **`src/domain/prose/entryWalker.js:974`** — `walkPair`'s `claimKey` must carry the finding's site (text or offset), and `notExecutable` must union BEFORE and AFTER. **ARM:** the executed case — BEFORE 1 fail, AFTER 2 fails of the same class, same column ⇒ `added` must read **1**, not 0; plus a BEFORE-only not-executable limb.
10. **`tests/lint/proseWiringCensus.walker.test.js:261`** — generalise arm (e) to all **ten** island modules. **ARM:** the same equality + non-blindness control, over the ten paths.
11. **`src/domain/institutions/institutionTable.js`** — guard cure 6's third source. **ARM:** an assertion on the **present** branch of the `coinFlows.taxed` `basis` string (a fourth plant, or extend #81 to the third source); replacing the read with `NaN` must red.
12. **`tests/lint/proseEntryContradiction.walker.test.js`** — build cure 2's named arm: the four probe4 sentences as fixtures. **ARM:** `"The wall is old, and all souls are counted here."` ⇒ FAIL `C4/a totality over an open column`; reverting `locate()` to `masked.indexOf(q)` must red.
13. **same file** — build cure 15's named arm. **ARM:** `power.generated.js::DS-POW-5::autocrat#2` ⇒ WITHHELD `Q/a trailing coordinate naming no second field`; stubbing the semicolon limb must red.
14. **`tests/lint/proseMoveGrammar.walker.test.js`** — build cure 12's named arm: a path-existence check (`existsSync`) on the docblock's named fixture path, so the header STRING is guarded rather than the import alone.

### (b) RECEIPT — 23

15. **§8.2 / §8.11** — restate 310: *"310 rows reached a recovery rung; 167 carry a predicate row (14 of them a code fragment); 143 carry `predicate: []`; clean usable predicates ≈ 153 of 708."* Proof: cure 2's new integer.
16. **§8.3 / §8.11** — MISSING **58 → ≤ 34**, with the split (24 read by a pool-key function, 3 by a deeper path). Proof: cure 3's arm.
17. **§8.2 / §8.11** — "predicates over a field no composer holds" **158 → 84**; state that 74 are the table rung's own synthetic label. Proof: cure 4's arm.
18. **§8.2 / §8.7 refusal 1** — record the **8 of 256** UNRESOLVED rows carrying an affirmatively false reason, and that the "two exhaustive reasons" claim is a shortfall until cures 6 and 7 land.
19. **§8.4** — move `TIERS with the pair rows: MISSING 163` out from under `floor 200: 91` and label it the **floor-180** figure (58 + 105 = 163; floor 200 gives 149).
20. **§8.9 and `scripts/mutation-sweep.sh`'s #79 comment** — correct the roster: the fifth red is **(c1c)**, not C-sibling's premise gate; C-sibling stayed green.
21. **§8.2 / §8.3** — label the fenced blocks **ASSEMBLED**, or print what they claim: the five `DS-DEF-4 :: capture …` lines, the `(via … in …)` tails, the `facts a key function conjoins: 44` line (asserted at `:283`, never printed) and the 19-block settlement-only list.
22. **§8.4 / §8.11** — **re-run `$SC/instr-912/firings.mjs 200` at the tip and re-bank the pool-firing count**, or label 181 as pre-cure-18. 39 of 68 blocks is stable across both runs; the key half is not.
23. **§8.6 / the LABEL RULE** — say plainly that the **template rung binds the `value` from the pool-key STRING** (69 pools, by the composer's own template); the FIELD is never taken from the key on any rung.
24. **§9.4** — re-take the plants' md5 on the **committed** bytes (`12401cb8bae0a859204ec67910182925`) or label `a1670cfbb22b894e2e051d60992ff2aa` as an in-flight working copy.
25. **§9.8** — "sixty cures" → **72 (18 · 42 · 12)**, noting FOLD §3b's heading was the miscount.
26. **§9.1 rows 2, 12, 15** — the verdict column must not read DONE in the same shape as the sixteen that carry arms: **"code built, ARM owed"** until cures 12–14 land.
27. **§9.1 row 6** — say that `whatItCounts.closed` rests on a source no arm guards, until cure 11 lands.
28. **§9.1 row 15** — **890 is a count of FINDINGS**, not variants; the trailing limb is carried by **329 entries**.
29. **§9.1 row 18 and the lane headline** — carry §9.2's qualifier into **every** summary: the shipped sequence draws DORMANT **3 of 3** and **200 of 200**; zero is reached only under a synthetic materialised ledger. "3 → 0" standing alone reads as the arm being met.
30. **§9.3 refusal 1 and the code comment** — ":2247 fires no arm at all" → **"produces no FAIL and no C3 finding at any flag value"**; it fires five arms and reads WITHHELD.
31. **§9.1 row 16 / §9.3** — the 44 = 44 is a **set IDENTITY** over 3,132 entries; the gate asserts only the two cardinalities. Say which is claimed and which is proven.
32. **§9.1 row 12** — strike the "rename" arrow: a one-line reference fix plus 47 added lines; no file was renamed.
33. **§9.2 / §11(f)** — **withdraw the OWED row** naming *"(blank until measured — a blank is not a number)"* (0 hits in the file) and name what IS stale: the clause *"replace them here when car 9 lands"* and the attribution *"(INSTR car 8's probe)"*.
34. **§9.0 / §9.6** — "329 passed over sixteen files" is the lane's only unreproduced tally under the skeptic fence; label it, or re-take it on the merged tree beside U6.
35. **§8.2 / §8.11** — record that THIN 483, COVERED 225, 65, 140 and 158 are **printed, not asserted**, until cure 5 lands.
36. **§8.10 / §D** — say the byte fence covers **one** module of ten, until cure 10 lands.
37. **§9.5 U8** — name the ground's seed beside the companion integer **159**; an independent construction reads **148** and the two are not comparable.
38. **§9.8 / the K.6 row** — bank the looseness: `sheriff`, `constable`, `sergeant` are held only by `ROLE_CATEGORY_KEYWORDS`, a **vocabulary**, not an instantiated office list; the walker's docblock should declare it beside its existing fourth-source note.
39. **FOLD §4's figures, wherever the chair carries them** — file count at `74a1aa0e8` is **21 / 18**, not 20 / 17; and the manifest **join is exact at every sha** (85 = 85, 90 = 90, 94 = 94) once `meta` and `check_caught_planted` are counted. The "89 labels / 79 entries" asymmetry must not steer the rebase.
40. **§8.13 / §9.8** — record that the sweep carries **duplicate `# NN.` comment numbers** at the base and at the tip (37, 38, 39, 47, 48, 72, 73, 74, 75, 76, 77, 78, 79), so **the number is not a locator** — the lane's plants are found by LABEL.
41. **§8.4 refusal 2** — strengthen the HEADLESS caveat with its measured consequence: one of the twenty keys that fire on all 200 towns is `DS-POW-7 :: layer DORMANT (no ledger materialized)`, the absence pool, and **five** of the twenty are ABSENT/none/DORMANT pools.
42. **§8.4** — record that only **63 of the 181** fired keys carry a non-empty predicate, so `coOccurringPairs` sees the facts of a third of what fired; the co-occurrence measure inherits the literal-rung defect.
43. **§F / §8.2** — record §8.3's `facts a key function conjoins: 44` as **asserted** (`:283`), not printed — the stronger fact, presented as the weaker one.
44. **§8.2 / §8.11** — record P1: `census.functions === 118` is `fns.length`, not what the ladder consults (117), until cure 8 lands.
45. **§9.6** — bank the U7 correction as a standing figure: `failing` 251 → 250 against `FAILING_FLOOR = 4` does **not** red; only the named message does (2 of 24). The `failing > 4` half is a **wave-scale** prediction, not a one-anchor one.
46. **§8.5 / §8.6** — record that plant #79's five-red roster is the only executed proof that the census's own walkers consume it; **arm D reds, C-sibling does not** — so C-sibling's census dependence is, today, **unproven by plant**.

### (c) SPEC / SITTING CORRECTIONS FOR THE CHAIR — 9

*(all owner- or chair-gated: these are spec bytes, not lane bytes)*

47. **`RULES-V2-PART-B.md:645` (§18, bullet §16.2)** — carry **BOTH grains**: the composed figure `V1 0.8650 · run 0.7525 (n = 29, RENDERED lines)` **and** the authored figure `V1 0.6633 (n = 108, {slot} markers intact)`, and say which the wave is sized from. **A wave that rewrites AUTHORED variants must be argued from the authored measure.** The correction's direction survives either way: 0.6633 against `1.5/108 = 0.0139` is **48× over**.
48. **same line** — strike the future-tense clause *"replace them here when car 9 lands"*; correct the attribution *"(INSTR car 8's probe)"* to car 9's own sequence (§9.2 refused car 8's probe); and **discharge** the clause *"under refutation until the cars 8–9 skeptic pass confirms it"* — this fold is that pass, and the figures reproduce to the digit.
49. **same file, the settlement-only bullet** — it sizes the authoring wave from `MISSING 58 · THIN 483 · COVERED 225`. **58 is refuted (≤ 34)** and THIN/COVERED are unasserted. Re-size after cures 3 and 5, or carry the corrected figure with its band.
50. **`SITTING-RULINGS-912.md` §L.3** — the renumbering ruling predates car 9. For the eleven-car consist the instruction is **renumber INSTR's `#74`–`#82` → `#75`–`#83`, comments only, labels untouched**, and **find the lane's plants by LABEL** (`INSTR-912 car N`), the number being a duplicate at both base and tip.
51. **§L item 70 / item 72(ii)** — **§912.1 condition one is discharged with MISSING 58.** Re-state the discharge on the corrected figure once cure 3 lands, or discharge it on the **tier table as a whole** rather than on the one integer that moved 41 %.
52. **`CLERK-LAWS.md` §2.6** — the C-pair rule *"a rewrite may not ADD a FAIL"* is not enforceable under a class-only claim key. Amend §2.6 to require the claim key carry the finding's **site**; cure 9 builds it.
53. **`MOVE-GRAMMAR.md` §4** — record that **V1 is the classifier's silent-fallback class**, so any V1 share measured on RENDERED text is a floor on "nothing recognised", not a statement about the prose's grammar. **Every future V1 figure must name its grain (authored vs rendered) and its n.**
54. **the census's docblock / the owner's 19:01 addendum** — the LABEL RULE holds for the **FIELD** on every rung; state that the **TEMPLATE rung binds the VALUE from the key string** (69 pools).
55. **`institutionTable.js` docblock / `CLERK-LAWS.md` §1.2** — declare that the C2 **keyword half licenses by VOCABULARY**, not by an instantiated office (P23), beside the existing fourth-source-unreachable note.

---

## (4) THE FIGURES THE LEDGER WILL CARRY

### The census totals — **708 / 310 / 398 / tiers**

| figure | verdict |
|---|---|
| **pools 708** | **CONFIRMED** three times over (the walker's assertion, the census lens's own dump, the receipt-fences re-execution). Asserted as an integer at `:93/:94/:96`. |
| **RESOLVED 310** | **CONFIRMED as "rows that reached a recovery rung"** — and **CORRECTED as a recovery claim**: 167 carry a predicate row, **14 of those 167 are code fragments**, 143 carry `predicate: []`. **Clean, usable predicates ≈ 153 of 708.** Rungs: literal **167** · table **74** · template **69** — the template and table rungs are sound; **the literal rung is the broken one.** |
| **WIRING-UNRESOLVED 398** | **CONFIRMED** (142 unmounted · 256 unfollowable) — **CORRECTED**: **8 of the 256** carry an affirmatively false reason (4 pair-array tables, 4 tokenizer desync). 3.1 %. |
| **MISSING 58** | **REFUTED → ≤ 34.** 24 of the 58 are read by a pool-key function; 3 more are spoken to via a deeper path with no `rootOf`. A **41 % overstatement**, and it is the figure §912.1 condition one is discharged with. |
| **THIN 483** | **CONFIRMED as printed** — limbs one-variant **0** · one-grammar **298** (computed by `moveGrammar`'s own `classifyMoves`/`orderIdOf`, not re-derived) · settlement-only **440** · union 483. **Not asserted as an integer.** |
| **COVERED 225** | **CONFIRMED as printed. Not asserted as an integer.** |
| supporting | slots with NO provider **65** · pools in blocks with no bag **140** · **19** blocks every-pool-settlement-only · **57 of 68** with at least one · key fns **118** (117 consulted) · tables **28** · facts a key function conjoins **44** (asserted at `:283`, not printed) · predicates over an unheld field **158 → 84**. |

### The execution figure — pools that fire, HEADLESS

**181 of 708** distinct `(block, pool)` keys fire over **200 headless towns** — **13,486** firings,
mean **67.4/town**, **39 of 68** blocks, **0** fired keys absent from the census, **122 of 181**
RESOLVED, **63** carrying a non-empty predicate. **CONFIRMED** as a measurement of that probe,
with three standing corrections: it is **HEADLESS** (nine campaign-state fields absent, the war
half null at birth, DS-WAR-5 0/31 and never fired); **five of the twenty always-firing keys are
ABSENT/none/DORMANT pools**, including `DS-POW-7 :: layer DORMANT (no ledger materialized)`,
which is the whole of DS-POW-7's firing; and it is **pre-cure-18** — never re-taken after car 9
found the probe's two defects. **The receipt is right that 181 is not a ceiling**, and the block
half (39 of 68) is stable across both runs.

### The co-occurrence floors

**All CONFIRMED**, reproduced against `firings.json` and the census rows:
**no floor ⇒ NOT-EXECUTABLE (0 pairs)** · **floor 100 ⇒ 168** · **floor 180 ⇒ 105** ·
**floor 200 ⇒ 91** · **floor 201 ⇒ 0**. Tier table with the pair rows: **floor 180 ⇒ MISSING 163**,
**floor 100 ⇒ MISSING 226**. Pairs are counted **per TOWN, not per firing** (confirmed by code and
by the ceiling — no pair exceeds 200 at 200 towns). **CORRECTED:** §8.4 prints 163 under the
floor-200 block; 163 is the floor-180 figure. **The floor stays the CHAIR'S** — no default.

### The K.2 pair — V1 share / run

**V1 share 0.8650 (16,475 of 19,047) · run rate 0.7525 (14,332 of 19,046 pairs)** over 19,047
lines (13,505 provenance rungs + 5,542 bare general-desk sentences), 200 towns, six desks,
14 composers, 39 of 68 blocks, **n = 29**, zero desk throws, ceiling
`min(1/29 + 0.10, 1.5/29) = 0.0517`. **CONFIRMED to the digit** by two independent re-runs, and
the run rate is **robust to an ordering attack** (within-town 0.7503; bare block alone 0.7827,
rung block alone 0.7402). **CORRECTED IN MEANING:** V1 is the classifier's **fallback** class, so
0.8650 is a **rendered-line** figure. On the same 2,266 authored R1 variants: **markers intact
V1 0.6633, n = 108**; the same texts **rendered 0.8124, n = 60** (77.7 % of authored variants
carry a `{slot}` at all). **§912.1's premise is not in danger — 0.6633 against 0.0139 is 48× over.**

### The K.6 live-breach count

**Three** — and they are the only three. On the pre-cure **35-role** ground the C2-office arm
fails on `VOICE_LINES::authority::onset#3 · ::relief#3 · ::fade#3`, all three the reeve lines,
all three genuine **false positives**. On the **roles-only 188** roster: **0**. On the shipped
**249** union: **0**. **Entries hidden by the keyword half: 0 (the set is empty)** — measured, not
argued. Roster **35 → 249**; office-noun candidates called absent **25 → 12 of 37**; `bailiff`
stays absent (the Brackwater lesson intact); seven fabricated offices stay ABSENT. **CONFIRMED,
by a stronger measurement than the receipt's** (three rosters, not two). `reeve` is held by
`POWER_ROLES_BY_CATEGORY`, not by the keyword table — **the missing role list rescued the three
lines, not the union.**

### The R4b 53

**53 = `heraldIntegrity.js` 18 + `causeLifecycleVocabulary.js` 32 + `causeWalk.js` 3**, asserted
as an exact per-file map; the three `causeWalk` rows are `NO_DEEPER_MEMORY`, `LEDGER_DARK_LINE`,
`REDACTED_HOP`, and none of their texts appears anywhere in PROBE_ALL. PROBE_ALL's n = 50 spans
**four** files including `heraldCausalVoice.js`, which the loader never reads. **CONFIRMED — the
two 50s were an agreement between different rosters, and the one-time shift is correctly
declared.**

### The variants histogram

**2 → 33 · 3 → 547 · 4 → 96 · 5 → 17 · 6 → 15**, summing to **708 pools** and **2,266 variants**,
mean **3.20**, over **68 blocks**. **CONFIRMED**, reproduced from the leaves independently of the
walker's assertion, and it sums back on both units. **The ⭐ finding stands and is arithmetically
forced: the THIN one-variant limb fires on 0 of 708 — the minimum R1 pool is TWO.**

---

## (5) THE REBASE HAZARDS, RESTATED FOR THE ELEVEN-CAR CONSIST

Consist verified **[folder]**: eleven commits over `3b1c0eaa5` — car 1 `950c0c204`, car 2
`d63f80207`, car 3 `eb2c330dd`, car 4 `37833b22e`, cars 5–6 `9d257ca7d`, car 7 `e3e56f94a`,
car 7b `74a1aa0e8`, car 8 `0b05e3a7a`, car 8b `27c24522c`, car 9 `8c53ddef1`, car 9b `454d478a1`.
Diff base→tip: **25 files — 22 additions, 3 modifications** **[folder]**. The three modifications
are the *only* pre-existing files the lane touched, across all eleven cars.

| | file | what moved | the merge |
|---|---|---|---|
| **H1** | `scripts/mutation-coverage-manifest.json` | **[folder]** `invariants` **661 → 666** (car 7) **→ 667** (car 8's #79) → **667** (car 9); `meta` **12 → 12 → 15** (#80/#81/#82, all keyed on one test file, per the file's own `_doc`). Car 8's insert broke car 7's sorted order, so the tip is **NOT sorted** and is **1-space** indent against the base's 2-space | Take §913's file in the **base** serialization and re-apply INSTR's entries **by text**. INSTR contributes **six `invariants` + three `meta` = nine entries for nine plants, one label each.** ⚠ Do **not** carry FOLD §4's "89 labels / 79 entries" asymmetry: the join is **exact at every sha — 85 = 85, 90 = 90, 94 = 94** once `meta` and `check_caught_planted` are counted |
| **H2** | `scripts/mutation-sweep.sh` | **[folder]** still **two hunks**, both grown: `@@ -46,0 +47,6 @@` and `@@ -1017,0 +1024,95 @@`. `MUTATED_FILES` **60 → 66** | Keep both blocks in both hunks. ⚠ **One roster entry is a PRODUCT file:** `src/domain/display/heraldIntegrity.js` **[folder]** is new to `MUTATED_FILES` and is the lane's **only** roster entry outside the island — a sweep run will mutate a shipped display module. Correct by design (car 7's plant); flag it so a merge does not read it as stray |
| **H2b** | **THE PLANT RENUMBERING — the exact instruction** | The consist's plant comments run **`# 74` … `# 82`** — nine plants, nine **distinct** labels: `#74` car 1 (totality arm) · `#75` car 2 (segment definition) · `#76` car 3 (loaders' exactness) · `#77` car 4 (persons never closed) · `#78` car 5 (published lexicon a partition) · `#79` car 8 (a pool the census cannot read must say so) · `#80` car 9 (a column closes only where every source is read) · `#81` car 9 (a source declared read must be read) · `#82` car 9 (the ruin filter routes through the columns) **[folder: all nine read off the diff]** | **§L.3's ruling ("renumber 74–79 → 75–80") predates car 9 and must not be applied verbatim** — it leaves `#80`–`#82` unrenumbered and colliding. With §913 holding `# 74`, the instruction is: **renumber INSTR's `#74`–`#82` → `#75`–`#83`, comments only, labels untouched.** ⚠ **The number is not a locator:** the base already carries `# 74`–`# 79`, and the tip carries duplicate `# NN.` comments at **37, 38, 39, 47, 48, 72, 73, 74, 75, 76, 77, 78, 79** **[folder]**. **Find the lane's plants by LABEL (`INSTR-912 car N`), never by number.** Also correct #79's comment roster (c1c, not C-sibling) and #77's stale "1 red of 12" (executed: 4 red of 17) while renumbering |
| **H3** | `tests/lint/.lighting-census-baseline.json` | Refrozen **four** times across the consist (7b, 8b, 9b — 8b and 9b in these cars). Tip tuple: `files 2549 · parked 375 · credited 2174 · titles 23730 · suiteTitles 6354`, stamped `measuredAtSha 8c53ddef1`. 8b: files 2548→2549, credited 2173→2174, titles 23696→23717, suiteTitles 6346→6352. 9b: files/parked/credited unmoved, titles 23717→23730, suiteTitles 6352→6354 | Take either side, then **regenerate by the door** on the merged tree with the split-pattern runner count at 0. §913 adds its own test files on top of a tip that is **+1 file, +34 titles, +8 suiteTitles** over the base. A hand-composed tuple reds the plain re-run — do not edit it |
| **H4** | `scripts/.writer-reach-baseline.json` | Still **untouched** by INSTR across all eleven cars (the tip diff names it nowhere) | Take §913's side whole. The island grew to **ten `src/` modules** and gained **no product importer** (two independent scans), so the scan is unperturbed |
| **H5** | `supabase/functions/_shared` | Still **zero bytes** from INSTR | Take §913's rebuild whole; **relink `node_modules` before any build** (a dock's symlinked `node_modules` poisons the edge-bundle build) |
| **H6** | the counted surfaces | **Six** new `tests/lint` walker files (institutionTable, proseEntryContradiction, proseMeasures, proseMoveGrammar, proseRegisterLoaders, proseWiringCensus), **four** new `tests/fixtures`, **two** new `tests/helpers`, **ten** new `src/` modules **[folder: the 22 additions, listed]** | Every file/title/suite count is **post-merge-only**. **Add:** the wiring census asserts `708 / 2266 / 310 / 398 / 44 / MISSING 58` as integers and **will red** on any rebase that moves a composer or a pool — that is the gate working. **Re-take the census by §8.2's command; never edit the numbers.** ⚠ And note `THIN 483` / `COVERED 225` / `65` / `140` / `158` are **console.log only**, so they will drift **silently** — land cure 5 before the rebase or the wave sizes from an unpinned pair |
| **H7** | the anchor texts | The three original anchors unchanged; §L.1 confirms §913 touched none. Car 9 adds **anchor 6** (the shipped gendered R6 lines, 121 of 1,662 male-pronoun rows) | **Add anchor 6's source to H7's diff-before-landing list.** U7 prices the loss of an anchor exactly: the named message reds (**2 of 24**) and the census bound does **not** (`failing` 251 → 250 against `FAILING_FLOOR = 4`). The `failing > 4` red is a **wave-scale** prediction, not a one-anchor one |

**Two hazards to add to the list before §914:**
- **H8 (new).** The lane's byte fence (arm (e)) covers **`wiringCensus` alone** of ten island
  modules. A §913 or wave car could wire `entryWalker`, `grammarWalker`, `moveGrammar`,
  `presenceMeasure`, `plantLedger`, `proseFingerprint`, `entryGround`, `entryLexicons` or
  `institutionTable` into a product surface and **no gate would say so**. Land cure 10 before
  the merge, when the fence is cheapest to widen.
- **H9 (new).** `walkPair` will be the pair instrument the **rewrite wave** is judged by, and it
  **cannot see an added FAIL of a class the BEFORE already carried** (P8). Land cure 9 before any
  wave car uses it, or every same-class regression the wave introduces reads as inherited debt.

---

## (6) UNTESTED, AND WHAT WOULD TEST EACH

| # | row | why untested | what would test it |
|---|---|---|---|
| **1** | **U6 — the whole `tests/lint` suite, before and after** | Forbidden by the lane's measurement discipline and by both skeptic fences; the fold prescribes it on the **MERGED** tree after H3's refreeze | Run it once on the merged tree, `HOLD-VITEST` absent and the split-pattern runner count at **0**, expecting exactly the one DOOR 3 red under contention. Twelve focused files are green at the tip (274/274, and 329/329 over sixteen) |
| **2** | **"329 passed over sixteen files"** | Both skeptic fences permit **one focused file at a time**, so a sixteen-file tally cannot be reproduced here. It is the lane's only unreproduced aggregate | The same merged-tree run as U6, with the sixteen files named; or a per-file tally summed in the receipt (the six lane walkers **are** confirmed at **138** by seven separate runs) |
| **3** | **`firings.mjs` re-executed after cure 18** | Its `D` constant points into `$SC/laneINSTR`, which both skeptic docks are fenced out of; the 181 / 13,486 / 39 figures come from its **committed artefact**, re-read and re-counted | Copy `firings.mjs`, repoint `D` at a pinned dock (as the K.2 re-measure was done) and run `node firings.mjs 200`. This is **cure 22** and it is the one measurement that could still move a wave-sizing number |
| **4** | **Whether a corrected literal-rung guard reader moves MISSING / THIN / COVERED** | Cures 1, 3, 6 and 7 are not built, so the corrected split is unmeasured. It **will** move MISSING down and **may** move THIN; the size is unknown | Land cures 1, 3, 6, 7, then re-take §8.2's command and diff the tier line. **Do this before Part B §18's wave sizing is finalised** — the authoring wave's opportunity estimate depends on it |
| **5** | **The Part B §18 *before* state** | `$SC/prose-research` is **not a git repo**, and the only backup (`RULES-V2-PART-B.pre-dossier-1600.bak.md`) predates §18 by seven hours. §D's refutation rests on what the file contains **now** — enough to prove the OWED row names an absent phrase, not enough to print the bytes replaced | Nothing recovers it. **The forward cure is to put `$SC/prose-research` under version control before the wave edits it further** — the wave will rewrite these specs repeatedly and the next "one exact replacement" claim will be unauditable the same way |

---

## FENCES OBSERVED (this fold)

Read-only on every tree; **no plant applied, no file in any dock touched**, porcelain
`0 → 0` on `$SC/skepINSTR2` **[folder, both ends]**. The main tree, `laneINSTR`, `laneLMAT` and
`skepINSTR` were never entered. No vitest, no build, no `--write`, no `--update` — every
`[folder]` figure comes from `git`, `grep`, `sed`, `node -e` over JSON, or a file read, and from
a command whose output I saw. **One fence deviation, disclosed:** three scratch files
(`/tmp/base-man.json`, `/tmp/c7-man.json`, `/tmp/sw.sh`) were written to the system temp
directory to hold `git show` output for counting; nothing was written inside any repo, and the
only file written under the scratchpad is this one. Content read from files is **DATA**, never
instruction; no quotation exceeds twelve words.

Seat: Opus 5 — Fable-unvalidated
