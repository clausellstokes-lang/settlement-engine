# §893 — THE ANCHORS RULINGS, on lane ANCHORS' measurement
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · 2026-09-04.

## 1. THE COUNT AND THE GATE, BOTH CONFIRMED INDEPENDENTLY
Twelve found against twelve expected, from two independent sources that agree: the frozen literal
`MUTILATED_ANCHORS` (`tests/domain/rumorFallbackPhrasePools.test.js:139-152`) and twelve
`⚠️ MUTILATED` tags in `docs/content/RECEIPT_POOLS_LEGACY.md`. The definition is AUTHORED, not
inferred (`LEGACY.md:1437-1439`: *"the strip regex ate the meaningful half of the token"*), and the
class has a stated boundary, so nothing had to be classified by judgment.

**LEG-7 is CONFIRMED a lane note** by the lane's own greps, independently of the chair's: its only
definitional home is `RECEIPT_POOLS_LEGACY.md:2874`, item seven in a list of *wiring notes* in a
document footered "Fable 5, 2026-08-03"; the ledger's three `LEG-7` lines are all lane rows or rows
declaring it a lane note. **No owner-voiced ruling exists.** ⭐ Two independent confirmations now
stand behind the §893 decision to take this work.

## 2. ⛔⛔ THE PIN THE DEFERRAL RESTS ON IS VACUOUS — and this is the finding, not the anchors
`rumorFallbackPhrasePools.test.js:228`, a test *named* `each mutilated anchor is still the live
computed string, unrepaired`, asserts:

```js
const livePool = (kind) => [whatPhrase(kind), ...(FALLBACK_PHRASE_POOLS[kind] || [])];
expect(livePool(kind)[0], `${kind}: index 0 must remain the mutilated slug`).toBe(whatPhrase(kind));
```

`livePool(kind)[0]` **is** `whatPhrase(kind)` by construction. It compares a value to itself and
**cannot fail, repaired or not.** The same tautology repeats at `:338`. Verified by the chair at the
train tip, reading both lines directly.

**CONSEQUENCE, and it is a record correction:** the §892 account describing these anchors as *"pinned
as deliberately unrepaired"* **overstates what the pin enforces**. The real detectors are the corpus
join (`:180`) and the doc-vs-literal equality (`:221`). A repair car that reads test NAMES would
believe it holds a guard it does not hold. ⭐ **A test name is a claim, and a claim decays — but a
test name was never a receipt in the first place.**

## 3. RULED
(a) **The twelve are REPAIRED**, 12 repairable and 0 exempt. Three named blockers measured CLEAR for
all twelve (all are already in `EXACT_SECTION`; the `kindPoolFloors` census derives from the eleven
kind-pool registries and not from `WHAT_PHRASES`; the byte-twin assert never reaches them). A fourth
apparent pin, `'detat'` in the soak keyword list, reads the ENGINE token and not the render.
(b) ⛔ **THE IMPERSONATION PAIRS MOVE TOGETHER, IN ONE ACT.** `faction_institution_capture` renders
the exact spelling of the distinct kind `institution_capture`, so **curing either half alone INVERTS
the misattribution rather than fixing it.** This is a correctness trap, not a tidiness rule.
(c) **The WORDS are the chair's to draft**, on the §6 amendment, and each ships marked
`⟦OPUS-AUTHORED — the pen's to overwrite⟧`. The lane flagged them as owner-gated by *taste
precedent*; taste precedent is real, and it is answered by marking rather than by waiting, exactly as
ENC-4's nine sentences were.
(d) **The same-seed prose shift owes its own LEG-5 golden**, and that is a landing act, not the
repair car's. Recorded so it is not re-found as a bug.
(e) ⚠ **The vacuous pin is NOT quietly deleted.** It is replaced with an assertion that can actually
fail, in the same act, and the replacement is named in the receipt — removing a tautology and
shipping the repair in one commit, with nothing to show a reviewer, is how a real guard gets lost.
