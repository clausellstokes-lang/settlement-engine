# WF / WF-1D — the war-dissolution join that names the fall (stage 4 of the `wf-1` split promotion)

- **Status:** LANDED
- **Compile note:** compiled by lane TC-WF1D under ODQ §291.5/§320; BUILT AND VERIFIED by lane
  TE-WF1D under ODQ §321. ⚠ The status value above stands ALONE on its line because
  `parsePacketHeader` (`scripts/implementation-packets.mjs`) anchors the status row at
  end-of-line — trailing prose there leaves the status unparsed and makes the manifest disagree
  with the Markdown (J-TEWF1B-1, paid once already).
- **Verified base:** `claude/composite-r4` at `cdfe5a96716250af71f4e91305beb8716987e4f3`
- **Preamble:** `docs/implementation/preambles/WF-PREAMBLE.md`, cited **BY SHA-256
  `f4cd39fee756c3a192710ab9aa3fc208e18ef1f08ea9e9073bcdc8bfb55d871c`** (780 lines, 58,883 bytes).
  ⚠ **THIS LANDING IS WHAT MOVED THAT HASH.** The same commit that opened this packet amended
  §P1 R-WF-7 and §P7 stop-condition 8 to enumerate the ZERO-HEADROOM class at four members —
  the chair-pre-cleared preamble batch discharging WF-1C's RAISED-A and this packet's RAISED-2
  (ODQ §324.3 / §314.2) — and re-stamped the citation in **WF-1B** and **WF-1C**, the only two
  packets that cite the file by SHA (executed `grep`). Every refutation, disposition, register,
  STOP and census law in that file binds here and is not restated.
- **Substrate annex:** `docs/implementation/preverification/WF-SUBSTRATE.md`. ⛔ **No absolute is
  inherited.** The compile ran at `2cdb87fa`; **every figure it carried was RE-DERIVED at
  `cdfe5a96` before the first edit** (§11.0). Where the live tree disagreed with the compile,
  the compile was corrected and the correction is recorded — four times, §13.
- **Design authority:** `docs/DESIGN_FP_ARCH_WF.md` §4-WF-1 (the NET-ZERO join rule) and §5 seam
  **5** (the WR-1 dissolution join) · `docs/DESIGN_FP_FAITH.md` §5-WF-1 (the
  `pin:dissolution-names-the-fall` fixture and the legacy arm) ·
  `docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md` §1/§10 · `docs/DESIGN_FP_SPINE.md` reqs 13/14.
- **Train:** `wf-1`, family **WF**, member **d**. WF-1A, WF-1B and WF-1C are all LANDED on the
  branch; WF-1C's landing (`cdfe5a96`) is this packet's base, so the two figures that moved in it
  were read from the live tree rather than predicted across it.
- **Member cap:** WF is **STAMPED** (ODQ §78.1) ⇒ engine trains cap at EIGHT. This member uses
  none of the headroom.
- **Census authority:** ODQ **§321** (this landing's dispatch), under the §320 compile dispatch
  and §299.4's binding-forward rule.
- **Constitutional law binding every line below:** the DEITY DOCTRINE — faith is CULTURAL, never
  theological; no premade deity pool; typed buckets per FINITE-SEMANTICS; AI is clerk, never
  writer. This join makes a war receipt say **why the believers left the seat** — a political and
  social fact about people — and never that a god fell, failed or departed. See §5.

---

## §0 · WHAT THIS MEMBER IS, AND WHAT IT REFUSES

`WF-1A` §0 defers three deliverables to WF-1d. This member takes ONE of them and refuses the
other two on arithmetic, in both cases with the measurement rather than the assertion:

- **THE JOIN — this packet.** The `warTermination` receipt names the typed fall.
- **THE FAITHSECTION CAUSE-CHAIN LINE — DEFERRED to a sibling member (§7).** With the join it
  breaks **three** `PACKET_STANDARD` hard caps at once and carries a DS-FTH-1 corpus amendment
  the join carries not at all. Compile seed: `laneTCWF1D-WF-1E-seed.md`. **RAISED-3.**
- **THE CHRONICLE OBITUARY ROW — REFUSED ON A MEASUREMENT (§4).** Both candidate producers were
  re-filed out from under it by later chair rulings, and WF-1d's own output is a **receipt**, not
  an `impactKind`, so `KIND_SECTION` cannot key on it. **RAISED-1.**

⭐⭐ **THE JOIN LANDS AT NET ZERO, AND §2 IS THE EXECUTED MEASUREMENT RATHER THAN THE ARGUMENT.**
`warTermination.js` goes **818 → 818 EXACT** on eslint's own `Linter` and **1045 → 1045** on
`wc -l`, `eslint` clean, with the four landed war suites green.

---

## §1 · THE BEHAVIOUR THIS MEMBER ADDS

Today a holy war can outlive the faith that raised it and **the receipt can only say that it did,
never why**. `warTerminationCauseTables.js` carries one clause for `sacred_claim` —
*"a god named when the banners rose is no longer worshipped from the same throne"* — and it says
the same words whether the creed was evicted by a garrison, lost a contest, or simply lost the
pews.

WF-1a built the typed record that knows the difference (`religionStates[cid].patronFalls` and the
pure read `fallCauseFor`) and left it deliberately unread. Preamble **§P2.11** reserves the first
caller to this member. **Executed at this base: `fallCauseFor` has exactly ONE definition
(`patronFall.js:146`) and ZERO callers in `src`** — the prohibition held through WF-1b and WF-1c.
This member takes the caller the law reserved for it, and WF-1a's producer-first identity claim is
discharged rather than re-priced.

Lit on both flags, with a ring that records the fall of the anchor this war actually pinned, the
receipt now carries the typed token and the reader sentence says it in the house voice. Dark — and
lit with nothing recorded — the receipt is **byte-identical**, proved at the byte level in §9.

---

## §2 · THE CURE

### 2.1 THE ZERO-HEADROOM CLASS, RE-MEASURED AT THIS BASE

Re-executed with **eslint's own `Linter`** under `max-lines {skipBlankLines, skipComments}` — the
same engine `tests/lint/sizeBaseline.test.js` uses — on BOTH sides of the edit. Never `wc -l`
alone, never inherited.

| file | before | after | frozen literal | verdict |
|---|---:|---:|---:|---|
| `src/domain/worldPulse/warTermination.js` | **818** | **818** | **818** | ⛔ **EXACT — NET ZERO HELD** |
| `src/domain/worldPulse/warTerminationCauseTables.js` | 71 | **83** | — | +12 against an 800 layer ceiling |
| `src/domain/certification/couplingRegistry.js` | 109 | **113** | — | +4 |
| `src/domain/certification/couplingRegistryWar.js` | 383 | **403** | — | +20 |
| `src/domain/worldPulse/patronFall.js` | 28 | **28** | — | comment-only, effective UNMOVED |
| `src/domain/worldPulse/pulseKernel.js` | **1581** | **1581** | **1581** | UNTOUCHED — not named by this member |
| `src/domain/worldPulse/applyWorldPulse.js` | **941** | **941** | **941** | UNTOUCHED — not named by this member |

⭐ **`wc -l` AGREES INDEPENDENTLY: `warTermination.js` 1045 → 1045.** The physical diff is `-4 +2`
in the import block and `+2` statement lines, so the file does not move by either measure. This is
the discipline the amended §P1 R-WF-7 now REQUIRES of every zero-headroom edit, and this member is
the first to pay it under the new rule.

⭐ **NEITHER KERNEL FILE IS TOUCHED, STATED AFFIRMATIVELY.** The manifest names no line in either;
**no PRNG call is added, moved or removed and the fork order is untouched**, so the stream identity
THE PROMISE protects is not in play.

### 2.2 THE MECHANISM: THE PROSE SELECTOR PAYS FOR THE FAITH IMPORT

`warTermination.js` read `DISSOLVED_CAUSE_PROSE` at **exactly one site** (`:431`) and did **not**
re-export it (only `WAR_CAUSE_DISSOLUTION` is re-exported, `:94`). Replacing that raw-table read
with a `dissolvedClauseFor(type, fallCause)` selector living in `warTerminationCauseTables.js`
collapses the four-line import block to two, which buys the `patronFall.js` import outright and
leaves exactly one line of change budget. Everything else is a key or a parameter appended to a
line that already exists.

⭐ **THE EXTRACTION HOME WAS BUILT FOR THIS PRESSURE.** `warTerminationCauseTables.js`'s own header
says it exists so the WR family could move tables DOWN out of a frozen file rather than raise a
frozen number. WF-1d takes the door the WR family already cut, which is why this is a **seam** and
not a squeeze.

### 2.3 THE EXACT EDIT — seven sites

| # | site | edit | Δ eff |
|---|---|---|---:|
| 1 | the cause-table import block | 4 lines become 2; the `patronFall.js` import IS one of the two | **−2** |
| 2 | `readWarTerminations`, above `const deployments = …` | the BY-NAME strict flag gate, materializing the ring map | **+1** |
| 3 | `terminationReason` parameter list, the `dissolvedCauseTypes,` line | append `fallCause = null,` | 0 |
| 4 | `terminationReason`, the `dissolvedCase` line | `DISSOLVED_CAUSE_PROSE[type]` → `dissolvedClauseFor(type, fallCause)` | 0 |
| 5 | above `const attackerName = …` | the per-deployment lookup, one statement on one line | **+1** |
| 6 | the `terminationReason({…})` argument line | append `fallCause,` | 0 |
| 7 | the receipt's `decidingTerm,` line | append `...(fallCause ? { patronFallCause: fallCause } : {}),` | 0 |
| | | **TOTAL** | **0** |

⛔ **THE DISCIPLINE THAT MAKES THIS A SEAM.** Exactly ONE edit (#1) reformats existing code, and it
reformats an import whose specifier list genuinely shrinks by a member no longer read. Edits #2 and
#5 are ordinary one-statement lines, each on its own line — **there is no two-statement cramming
anywhere in this member.** #3/#6/#7 append a key or a parameter to a line that already exists.

⚠ **THE FLAG GATE IS SPELLED AS A GATED MAP RATHER THAN A BOOLEAN** — J-TEWF1D-1. It is still one
BY-NAME strict `=== true` read; see §13.

### 2.4 THE TYPED CLAUSE

`warTerminationCauseTables.js` gains a closed four-member table and one selector. ⛔ The vocabulary
is the landed one and is **not widened**: four keys, exactly `PATRON_FALL_CAUSES`, in codepoint
order. `abandoned` is absent because WF-1a CUT it on a measurement. A6 asserts the two vocabularies
are the same set, so a later fifth cause reds here rather than rendering a blank clause.

⛔ **EVERYTHING BUT A TYPED `sacred_claim` FALL READS EXACTLY TODAY'S CLAUSE, BYTE FOR BYTE** — the
other fifteen casus types and a `sacred_claim` with no recorded fall all return
`DISSOLVED_CAUSE_PROSE[type]` unchanged. §9's executed transcript proves it by SHA over the whole
control block rather than by argument.

⭐ **BOTH the typed token AND the sentence land**, and the reason is FINITE-SEMANTICS: the receipt
carries `patronFallCause`, a closed bucket a later reader can switch on, and the reader sentence
carries the house prose. ⚠ The conditional spread is a BYTE claim — a receipt with no named fall
carries **no key**, absent and never `null`. A3 and MUTANT (d) both hold it there.

### 2.5 THE COUPLING IS THE CHARTERED ONE, AND ITS BILL WAS EXECUTED

`DESIGN_FP_FAITH.md` §5-WF-1 declares FAITH×WAR in terms. Running
`tests/lint/couplingInclusion.walker.test.js` against the wired tree **before** the row landed
reproduced the walker's own message verbatim:

```
AssertionError: expected [ Array(1) ] to deeply equal []
+ "src/domain/worldPulse/warTermination.js imports src/domain/worldPulse/patronFall.js (FAITH→WAR)
   — add its couplingRegistry row in THIS commit …"
 Test Files  1 failed (1)      Tests  1 failed | 15 passed (16)   TRUE_EXIT=1
```

- **(a) PAIR half — INCURRED.** Executed at this base: `COUPLING_REGISTRY` held **49** rows, of
  which **ZERO** carried `direction: 'FAITH→WAR'` and **ZERO** named `warTermination.js`. **M3 is
  the estate's first `FAITH→WAR` registry row**; the registry now holds **50**.
- **(b) UNLAYERED-MODULE half — NOT INCURRED.** No module is created. `UNLAYERED_BASELINE_CEILING`
  (**179**) and `ARGUED_ROSTER_CEILING` (**20**) both stand still — proved by the walker's own
  green at the wired tip, not by assertion. `warTermination.js` is claimed by `LAYER_PATTERNS.WAR`
  and `patronFall.js` by FAITH's landed **exact-path** row (`:164`, read in full).

⛔ **THE ROW COSTS THREE FILES, NOT ONE, AND THE VALIDATOR SAYS SO.**
`couplingRegistrationGaps` (`implementation-packets.mjs:137`) requires a packet naming a coupling
LEAF to also name the composing head and the registry pin file: *"five waves running named only the
first"*. §8 names all three. This corrected the compile — see §13 correction **C-2**.

### 2.6 THE LEAF'S OWN HEADER SAYS THIS WAVE FALSIFIES IT

`patronFall.js` declared it sat *"beside its ONLY importer (`religiousContest.js`)"* so
`scanCrossLayerPairs` would yield zero pairs. This member gives the leaf its **second** importer,
in another layer. Both ceilings still stand still — because the lawful cure for a new pair is a
LICENSING ROW, not a baseline bump — but that paragraph now describes a tree that no longer
exists. **M4 re-words it in the same commit**, on WF-1C's M2 precedent.

### 2.7 THE LIFECYCLE CLAUSE (§78 F-4) — SHORT, BECAUSE NOTHING PERSISTS

1. **ZERO new persisted material.** `patronFallCause` rides a `war_termination_read` receipt, a
   per-tick projection. `readWarTerminations` **mutates nothing** — re-asserted across this
   landing by A5, which freezes the whole `worldState` to JSON and compares after the read.
2. **ZERO new top-level keys** ⇒ `CONDITIONAL_LEDGER_KEYS` does not move and the
   array-order-IS-serialized-key-order law is untouched. **Asserted, not assumed** (A5).
3. **No dedicated normalizer is owed and none is added.** The dead "no load-time normalizer"
   premise is used nowhere in this packet.
4. **The `{ to: 2 }` `religionStates` migration is untouched.** This member READS `patronFalls`
   through the landed pure `fallCauseFor` and writes nothing there.
5. **REGEN / UNDO.** The receipt is derived: a regen re-runs the fold and re-derives the clause
   from the ring it rebuilds; the pulse undo ring restores `preWorldState` wholesale. ⭐ And the
   derivation is honest across the boundary — a regen that DROPS `religionStates` drops the ring
   with it, at which point the join finds nothing and the receipt falls back to today's clause,
   which is **exactly the A3 arm, executed**, rather than a defect.
6. **IMPORT.** `scrubImportedConfig` drops `faithProfile` outright and `religionStates` is
   worldState rather than settlement config ⇒ this member adds no import-validation surface.
7. **THE VEIL, asserted rather than assumed.** `religionStates` is in `WORLD_SNAPSHOT_HARD_DENY`
   and **not** in `WORLD_SNAPSHOT_PUBLIC_LEDGER_ALLOWLIST`. The receipt carries a four-token
   bucket and **no deity ref**. A5 asserts both halves.
8. **§103.2 — ungated persistence: NOT INCURRED.** No normalizer, no cleanup arm.

---

## §3 · SCOPE AND BOUNDARY

WF-1D gives `fallCauseFor` its chartered first production caller, joins the typed fall to the war
dissolution at net-zero effective lines in a frozen file, lands the closed reader clause in the
already-built cause-table leaf, and licenses the FAITH×WAR coupling the faith volume declares.

**It does not** (named affirmatively, never by silence): touch `pulseKernel.js`,
`applyWorldPulse.js`, `peaceTerms.js`, `religionState.js`, `religiousContest.js`, `realmEvents.js`,
`pantheon.js`, `settlementRumors.js`, `heraldRouting.js`, `chroniclersLetter.js`,
`simulationRules.js`, `subsystemRowsVirtual.js`, `worldState.js` or **any component**; mint a flag,
a news kind, an `impactKind`, a Herald desk row, a kind REGISTRY, a receipt pool, a `WHAT_PHRASES`
row, an `EXACT_SECTION` row, a `KIND_SECTION` row (**refused on a measurement — §4**), a
`spatialLedgers` sub-ledger, an errand row, a persisted field, a top-level key, a normalizer, a
migration arm, or a test FILE; author a threshold, weight, rate, window or curve of any kind; add a
`LAYER_PATTERNS` row or an `ARGUED_UNLAYERED` entry; add or move a PRNG draw; touch `package.json`
or `package-lock.json`; light either flag anywhere; or move a same-seed golden.

---

## §4 · THE CHRONICLE OBITUARY ROW IS REFUSED, AND THE REFUSAL IS ARITHMETIC

| measured | result |
|---|---|
| the Chronicle's row mechanism | `KIND_SECTION` (`chroniclersLetter.js:80`), keyed on **`impactKind`** |
| its faith rows today | `pantheon_ascendancy`, `pantheon_twilight`, `pantheon_extinction` — all `'traditions'` |
| candidate producer (a) — the SETTLEMENT extinction obituary | ⛔ **RE-FILED TO WF-8 at ODQ §309** |
| candidate producer (b) — the REALM last-seat beat | ⛔ **TAKEN BY WF-1C's OWN M4** — `pantheon_extinction` is live at this base, verified by A6 |
| what WF-1d itself produces | a **receipt** — `kind: 'war_termination_read'`, **not an `impactKind`** |

⇒ **A WF-1d `KIND_SECTION` ROW WOULD BE A REGISTRATION ROW FOR A KIND NOTHING MINTS** — the
consumerless-row shape the estate forbids (ODQ §73.3 F-1).

⛔⛔ **AND IT WOULD LAND SILENTLY.** `heraldRouting.walker.test.js` requires every `KIND_SECTION`
key to be routed, but **no arm requires a key to have a MINTER**, and a `faith_*`/`pantheon_*` key
routes for free through the prefix rule. ⭐ **A6 asserts the absence instead** — over a table first
proved populated — so the refusal is structural rather than a sentence in prose. **RAISED-1.**

---

## §5 · DEITY-DOCTRINE AND SPINE COMPLIANCE

- **Faith is cultural, never theological — and this is where the doctrine does the most work in the
  family, because the surface written to is a WAR receipt.** The four clauses this member adds
  continue the existing register exactly: a contest lost, devotion turned, a garrison seated, a
  rite put out of the light. ⛔ **No clause says a god fell, failed, died, departed or was
  defeated**, and none reads a deity's authored axes. `imposed` records what the garrison did,
  never what the god failed to do.
- **No premade deity pool.** This member mints no deity, reads no deity catalogue, and **prints no
  deity name at all** — the clause names the act, and the deity name it might have wanted comes
  from nowhere.
- **Typed buckets (FINITE-SEMANTICS).** `patronFallCause` is exactly one member of the landed
  closed four-token vocabulary; `FALL_CAUSE_PROSE` is a frozen four-key table whose keys A6 asserts
  EQUAL `PATRON_FALL_CAUSES`.
- **AI is clerk, never writer.** No AI surface reads or writes any of it, and no edge-shared bundle
  moves (§6).
- **Spine req 13 — alignment-EMPTY, WITH REASON.** The join is a lookup of a landed token by a ref
  a landed anchor comparison already computed; no arm reads a deity's authored axes.
- **Spine req 14 — RECORDED, engine-only.** The clause is a derived reading of history. The DM's
  lever is the EXISTING `SET_PRIMARY_DEITY` verb, which creates an `imposed` fall through WF-1a's
  classifier and can therefore now change what a war receipt says — the verb story is the existing
  verb, with a longer reach.
- ⚠ **DS-FTH corpus (§P2.7) — NOT REACHED.** DS-FTH-1 binds `faithPanelModel`'s return signature,
  which is exactly why the FaithSection line is a separate member ⇒ **no corpus row is amended and
  `npm run gen:dossier-prose` is NOT run.**

---

## §6 · PRICED OBLIGATIONS

| obligation | verdict |
|---|---|
| **§49/§50/§148 — the flag mint** | ⛔ **NOT INCURRED — THIS MEMBER MINTS NO FLAG.** It rides `faithUnseatingEnabled` (LANDED, WF-1a) and `warTerminationEnabled` (LANDED). No registry row moves. ⚠ The by-name read census over `src` moves ONE: this member adds a read in `warTermination.js`, strict `=== true`, not reached through a frozen-list `.every()` |
| **§104.4 — edge-shared bundle closures** | **NOT INCURRED.** No path this packet names appears in any committed bundle meta's `inputs` — derived at compile and re-checked here precisely because M4 is a COMMENT-ONLY edit, the trigger shape §104.4 fires on |
| **test census (the lighting contract)** | **INCURRED, TITLES ONLY. AS LANDED: `2488/364/2124/20649/5776` → `2488/364/2124/20655/5776` (+6 titles, nothing else).** ⛔⛔ **THE FIGURE MOVED THREE TIMES AND WAS MEASURED EVERY TIME, NEVER ADDED.** (1) At the build base `cdfe5a96` it read `2485/364/2121/20618/5768` → `2485/364/2121/20624/5768`; the STARTING figure there was RE-READ from the walker's own live constant, NOT inherited, because the compile had predicted it by adding WF-1C's +7 across another member's landing and the live constant already contained that +7 — applying it twice would have landed 20,625. (2) At the first CAS slot MF-T2B landed +2 files, +2 credited, +7 titles and +2 suite titles. (3) At the second CAS slot TE-NOTICES landed +1 file, +1 credited, +24 titles and +6 suite titles. ⛔ At each slot the tuple line was set to a deliberate PLACEHOLDER so the SEQUENCED walker had to convict on every figure in turn, and each value was taken from its own assertion message — arithmetic would have produced the same tuple and would not have been evidence. ⭐ This member mints NO test file: six straight-line `it()` calls join an EXISTING describe. Authorizing decision **ODQ §321** |
| **§102.2 / §P3b — mutation coverage** | **NOT INCURRED.** No file is added under `tests/lint/` |
| **§85.4 — seeded chooser / pool registry** | **NOT INCURRED.** The lookup and the selector are total functions of already-computed values and draw nothing. **Zero new PRNG streams, dark AND lit** |
| **size baseline / hot files** | ⛔⛔ **INCURRED, AND IT IS THIS MEMBER'S SHARPEST CONSTRAINT.** `warTermination.js` held at **818/818** across the edit on BOTH instruments (§2.1). No `PACKET_STANDARD` hot-file is named |
| **`negativeAssertionAnchor`** | **INCURRED, CEILING ZERO.** ⚠⚠ **PAID TWICE BEFORE IT PASSED, and the lesson is now executed rather than inherited**: a `// anchored:` marker counts only on the assertion's OWN line or the SINGLE line directly above it, so a marker that WRAPS onto a continuation line does not count. Five sites reddened; all five now carry a same-line trailing marker on this file's landed `:263` idiom. Walker green, 9/9 |
| **coupling registry — BOTH halves** | ⛔ **INCURRED, PAIR HALF ONLY.** §2.5. The UNLAYERED half is NOT incurred |
| **hand-keyed address rot** | **NOT INCURRED.** `.prose-numerics-baseline.json` holds **413** rows; executed, **ZERO** key any file this packet names |
| **wizard-news authoring walker** | **NOT INCURRED.** This member authors no news entry and no `impactKind` |
| **declared shift** | **NOT INCURRED.** Dark is byte-identical by construction and §9 proves it at the byte level. No preset declares `faithUnseatingEnabled`, so no same-seed golden can reach the lit path |
| **chair value signature (§42/§43, §78 F-5)** | **NOT INCURRED.** This member authors **no number** |
| **§77 premise map** | **INCURRED.** Rows in §10 |
| **`docs/**.md` naked-claim debt** | **INCURRED — PER CLAIM.** The live claim regex was run over this packet's bytes and over the amended preamble and both re-stamped packets before committing: **no match** in any of the four. ⚠ The suite is RED at this base on an arm that is NOT this member's — §12 item 5 |

---

## §7 · THE SPLIT, PRICED

`PACKET_STANDARD` binds: *"If the work cannot fit, the agent stops and proposes the smallest
split."* Shape 1 (join + FaithSection line in one member) breaks **three** hard caps
simultaneously — existing logic files 6 > 3, handwritten 14 > 12, acceptance ≥10 > 8 — so the
member is the join alone.

⭐ **THE SPLIT LINE IS NOT ARBITRARY**: the two halves share **no production path**, no data
structure, no flag read site and no obligation. D1 is a WAR-file seam owing a coupling row; D2 is a
display projection owing a DS-FTH-1 corpus amendment and a generator run.

**What the deferred member owes**, recorded so it is not re-discovered at a terminal: DS-FTH-1
binds `faithPanelModel`'s return signature verbatim ⇒ a corpus edit and `gen:dossier-prose` in the
SAME commit; `FaithSection.jsx` carries **TWELVE** hand-keyed `.prose-numerics-baseline.json` rows
at lines `49/53/174/176/179/188`, and the render belongs in the existing `<Cause>` block BELOW all
of them, so **adding no line above `:174` makes the bill zero — RE-ADDRESS such rows, NEVER delete
them**; `pulseKernel.js` is net zero by packing onto its existing single-line call; and ⚠ the dark
fence for D2 is **not** the ring's presence — gating on `state.patronFalls` alone would render the
line in a world lit once and then darkened.

---

## §8 · EXACT CHANGE MANIFEST

⚠ **Change-path collision check, EXECUTED at `cdfe5a96`:** `PACKET_MANIFEST.json` holds **132**
rows, **131 LANDED + 1 SUPERSEDED — ZERO non-terminal.** No live packet reserves any path below.

### Handwritten (9 files)

| # | action | path | Δ eff | instruction |
|---|---|---|---:|---|
| M1 | MODIFY | `src/domain/worldPulse/warTermination.js` | **0 — NET ZERO, MANDATORY** | ⛔⛔ **ZERO-HEADROOM FILE, 818 against a frozen literal of 818, pinned EXACT IN BOTH DIRECTIONS. GROWTH HAS NO LAWFUL CURE.** The seven sites of §2.3, measured on both instruments on both sides |
| M2 | MODIFY | `src/domain/worldPulse/warTerminationCauseTables.js` | +12 (71→83) | `FALL_CAUSE_PROSE` and `dissolvedClauseFor`, placed ABOVE `TERMINATION_PEACE_PROSE` so the three module-load totality assertions keep their position at the file's foot, and given their OWN docblocks so no existing export's JSDoc is orphaned |
| M3a | MODIFY | `src/domain/certification/couplingRegistryWar.js` | +20 (383→403) | ⛔ **ONE row, in the SAME COMMIT as its live reader.** `CPL-23.FAITH_TO_WAR.WF-1d.dissolution_names_the_fall`; `read` and `counterforce` BOTH addressed into `warTermination.js` |
| M3b | MODIFY | `src/domain/certification/couplingRegistry.js` | +4 (109→113) | The composing head: import, compose, and re-export by name. ⚠ The import block and the re-export block are SEPARATE — a re-export creates no local binding |
| M4 | MODIFY | `src/domain/worldPulse/patronFall.js` | **0 — COMMENT ONLY** | Correct the falsified "ONLY importer" paragraph; name both importers and say the cross-layer read is LICENSED |
| M5a | TEST | `tests/domain/warTermination.test.js` | — | The six acceptance cases of §9 as six straight-line `it()` calls inside the EXISTING `WR-1 cause dissolution` describe |
| M5b | TEST | `tests/domain/couplingRegistry.test.js` | — | The exact-list roster pin gains the new spread. ⛔ Required by `couplingRegistrationGaps` |
| M6 | TEST | `tests/lint/sovereigntyLightingContract.walker.test.js` | — | The WHOLE five-figure tuple re-derived by execution |
| M7 | DOC | `docs/implementation/packets/fp/WF-1D.md` | — | This packet. ⛔ The path carries **no `-I` suffix** (J-TEWF1B-2) |

### Generated

**NONE.** No generator runs. `npm run gen:dossier-prose` is NOT run.

### Budget reconciliation

| limit | this packet |
|---|---|
| behavior families | **1** |
| new persisted record families | **0** |
| named writers for state that changes | **0 — nothing is persisted** |
| feature flags | **0 new, 0 new conjunctions** |
| user-facing surfaces | **0** |
| direct production consumers | **1** of 2 |
| new logic-bearing production leaves | **0** |
| existing logic-bearing production files modified | **3** of 3 (AT CAP: M1, M2, M4) |
| registration-only production files | **2** of 3 (M3a, M3b) |
| handwritten files total | **9** of 12 |
| new/changed effective production lines | **≤40** of 400 |
| acceptance cases | **6** of 8 |

**Overrides approved before dispatch: NONE — and none was needed.**

⚠ **THE LANDING ALSO CARRIES A THREE-FILE PREAMBLE BATCH** (`WF-PREAMBLE.md`, `WF-1B.md`,
`WF-1C.md`) under ODQ §324.3. That is a chair act discharging two RAISED rows, not a member
deliverable, and it is counted separately here rather than folded into the budget above so the
chair can rule on the accounting.

---

## §9 · ACCEPTANCE (closed denominator — 6 cases)

⚠⚠ **THE FIXTURE WAS RUN AND ITS OUTPUT PRINTED BEFORE ANY PIN WAS WRITTEN**, once against a
PRISTINE `cdfe5a96` worktree and once against the wired tree, with the same harness, so the two
transcripts are directly comparable. The executed result:

```
ring seeded through recordPatronFall: {"a":{"patronFalls":[{"ref":"sun-old","cause":"discredited","atTick":7}]}}

A1  lit+lit, ring names the pinned ref  -> patronFallCause "discredited"
    reason "…no longer worshipped from the same throne — the creed lost its rightful
            claim in the town it was named from. Still, …"
A2a dark / A2b false / A3 lit-no-fall / A4 legacy   -> patronFallCause KEY ABSENT

BYTE COMPARISONS   (pre-wiring -> wired)
  A2a dark === A2b false            : true  -> true
  A3  lit-no-fall === A2a dark      : true  -> true
  A1  lit-with-fall === A2a dark    : true  -> FALSE     <- the negative control FIRED
```

⭐⭐ **EXACTLY ONE ARM MOVED ACROSS THE LANDING, AND IT WAS PROVED BY DIGEST RATHER THAN BY EYE.**
The A2a, A2b, A3 and A4 transcript blocks are SHA-identical before and after, as is the whole
fifteen-casus control block — which is the byte control on M2's selector refactor.

| id | case |
|---|---|
| A1 | **MAIN BEHAVIOUR, and the LIT-MUTANT CONTROL.** Lit on both flags with a ring recording the pinned patron's fall, the receipt carries `patronFallCause: 'discredited'` AND the typed clause. ⛔ The **LITERAL** `faithUnseatingEnabled: true` spelling is load-bearing for `mechanismLitCoverage` AUTO credit. The ring is seeded THROUGH `recordPatronFall` and asserted before it is used |
| A2 | **ABSENT vs FALSE vs LIT, at the byte level** — `{}` and `{faithUnseatingEnabled:false}` serialize identically, and only the literal lit drive moves a byte. Also the **CASUS-GUARD pin** (see MUTANT (c)) and the control that the other fifteen clauses are unchanged |
| A3 | **NO INVENTED CAUSE, AND NO KEY.** Lit, with a ring recording nothing for the pinned ref, the receipt carries **NO `patronFallCause` key** (absent, never `null`) and serializes identically to dark. **anchored** |
| A4 | **THE CHARTERED LEGACY ARM.** A deployment predating the anchors reaches `causeState: 'anchor_unavailable'` with `dissolvedCauseTypes: []` and is named nothing. *"Missing history is not evidence"*, honoured verbatim. **anchored** |
| A5 | **LIFECYCLE AND THE VEIL.** The whole `worldState` is frozen to JSON and compared after the read (no mutation); `CONDITIONAL_LEDGER_KEYS` is unchanged; `religionStates` is in `WORLD_SNAPSHOT_HARD_DENY` and not in the public allowlist. **anchored** |
| A6 | **REGISTRATION AND VOCABULARY TOTALITY, WITH THE REFUSED ROW ASSERTED AS AN ABSENCE.** `Object.keys(FALL_CAUSE_PROSE)` EQUALS `PATRON_FALL_CAUSES`; the FAITH→WAR pair is licensed by exactly ONE row whose read AND counterforce modules are `warTermination.js`; and `KIND_SECTION` gains NO key — asserted over a table first proved to carry `pantheon_extinction`. **anchored** |

---

## §10 · §77 PREMISE-MAP ROWS

| id | premise | grade at `cdfe5a96` | how it dies |
|---|---|---|---|
| P-1 | `warTermination.js` is 818 effective against a frozen literal of 818 | EXECUTED, both sides | any landing moves it ⇒ re-measure; **never inherit** |
| P-2 | `DISSOLVED_CAUSE_PROSE` has exactly ONE reader in `warTermination.js` and is not re-exported from it | EXECUTED — one hit at `:431` | a second reader appears ⇒ the import swap stops being net-zero ⇒ **STOP** |
| P-3 | `fallCauseFor` has ZERO production callers | EXECUTED — one definition, zero `src` callers | another wave calls it first ⇒ §P2.11 breached |
| P-4 | `COUPLING_REGISTRY` carries ZERO `FAITH→WAR` rows | EXECUTED — 49 rows enumerated | a row appears first ⇒ M3 becomes an amendment |
| P-5 | no exact-count pin bounds `COUPLING_REGISTRY.length` | EXECUTED — **but a deep-equal ROSTER pin does**, and it reddened. Correction **C-3** | a further pin lands ⇒ re-read the live arms |
| P-6 | no path this packet names is in any edge-shared closure | EXECUTED over the committed metas' `inputs` | a bundle's inputs grow ⇒ §104.4 becomes INCURRED |
| P-7 | the lighting tuple starts at `2485/364/2121/20618/5768` at the BUILD base, at `2487/364/2123/20625/5770` after MF-T2B, and at `2488/364/2124/20649/5776` after TE-NOTICES | EXECUTED at all three — read from the walker's live constant, then re-derived figure by figure after each rebase | ⛔ **re-read at the implementing tip; inheriting either packet's number is the recorded failure** |
| P-8 | `PACKET_MANIFEST.json` has ZERO non-terminal rows | EXECUTED — 132 rows, 131 LANDED + 1 SUPERSEDED | a sibling DRAFT opens ⇒ re-run the collision check |
| P-9 | `.prose-numerics-baseline.json` keys none of this packet's files | EXECUTED — 413 rows, zero matches | a landing adds one ⇒ re-address it, NEVER delete it |
| P-10 | `KIND_SECTION` has no arm requiring a key to have a minter | EXECUTED | such an arm lands ⇒ §4 gains a second, machine reason |
| P-11 | `warTermination.js` carries a ZERO any-cast allowance (absent from the baseline) | EXECUTED — `.domain-any-baseline.json` names it nowhere | a hole lands ⇒ cure by TYPING, never by widening |

---

## §11 · PREFLIGHT — EXECUTED, WITH ITS RESULTS

0. ⛔ **THE STEP-8 STOP.** `git rev-parse HEAD` = `cdfe5a96`, WF-1C's landed tip. **Every figure
   the compile carried was re-derived here before the first edit.** All seven size figures, the
   census tuple, the 49-row registry, both coupling ceilings, the schema version, the 413-row
   prose baseline and the preamble hash reproduced EXACTLY; four *reasoning* rows did not, and are
   corrected at §13.
1. §31 ANCHOR PREFLIGHT, bare, in-shell: **9 passed, TRUE_EXIT=0** at the pristine base, and
   **9 passed, TRUE_EXIT=0** again at the wired tip after the marker placement was corrected.
2. `BASE_STATE.json` admissibility RE-READ and RE-DISCHARGED at this member's own tip; every
   figure re-executed regardless.
3. Packet status DRAFT → READY → LANDED, `validate:packets` green at every transition.
4. All production targets clean; no foreign dirt. Nothing staged with `-A`/`-u`/`.`.
5. Every §15 symbol resolves in the live tree.
6. `npm run validate:packets` exits 0 at the tip; the §8 collision check re-run.
7. ⛔⛔ **RE-MEASURED BEFORE AND AFTER — 818 = 818 = the live literal**, plus `wc -l` 1045 = 1045,
   plus `pulseKernel.js` 1581 and `applyWorldPulse.js` 941 confirmed UNTOUCHED.
8. ⛔ **THE LIGHTING TUPLE RE-READ FROM THE LIVE CONSTANT** — see §6.
9. ⛔ **The own-footprint golden is the committed deity-BEARING master**
   (`tests/fixtures/worldpulse-golden-master.json` via `tests/property/worldpulseDeityGolden.test.js`).
   It predates this landing in the tree, so its green at the wired tip IS the pre-wiring
   comparison, and it is stronger than a self-captured golden because this lane never wrote it.
10. ⛔⛔ **THE FIXTURES WERE RUN AND PRINTED BEFORE THE PINS WERE WRITTEN** — §9. ⚠⚠ **THE
    MEMBER'S OWN FIXTURE HAZARD WAS REPRODUCED RATHER THAN TAKEN ON TRUST**: a LIT world whose
    ring records no fall for the PINNED ref is byte-identical to dark, so a fixture that lights
    the flag and never records a matching fall produces base == cure and a silently vacuous pin.
    The transcript shows `A3 === A2a dark : true` on BOTH sides of the landing.

---

## §12 · CHECKS

```
npx vitest run tests/domain/warTermination.test.js tests/domain/warTerminationPulse.test.js \
  tests/domain/warCostsIntegration.test.js tests/domain/warCoalitionWr6.test.js \
  tests/domain/patronFall.test.js tests/domain/couplingRegistry.test.js \
  tests/domain/couplingReceiptSample.test.js tests/domain/subsystemRowsWar.test.js \
  tests/domain/religionDormancy.byteIdentity.test.js
npx vitest run tests/lint/couplingInclusion.walker.test.js tests/lint/couplingDesk.walker.test.js \
  tests/lint/heraldRouting.walker.test.js tests/lint/sizeBaseline.test.js \
  tests/lint/domainAnyCastBaseline.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/property/worldpulseDeityGolden.test.js
npx eslint src/domain/worldPulse/warTermination.js
npm run typecheck:ratchet ; npm run typecheck:domain:strict
```

1. ⚠ **Gate-reading law.** ⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** — it
   self-deadlocks and **exit 3 is the mutex giving up, not a red.** Run `check:tail` **bare**, from
   a fresh shell, with an in-shell exit capture, and **outlast it in your own turn.**
2. ⚠ **Trust no exit status you did not capture**, and read only SELF-NAMED logs. ⚠ This lane
   reproduced a variant of that hazard on itself: `${PIPESTATUS[0]}` is a **bash**-ism and this
   shell is **zsh**, so a piped check reported an EMPTY exit status. Re-run bare, it reported 0.
3. **Both typecheck configurations, at their exact floors:** `typecheck:ratchet` **173/173** and
   `typecheck:domain:strict` **1134/1134**, both TRUE_EXIT=0. ⚠ M2 adds a new exported function
   and M1 widens a receipt literal — the exact shape that produced nine strict errors against a
   GREEN ratchet at WF-1B's landing. It did not recur here because the selector's return and its
   table are both explicitly typed rather than inferred.
4. ⚠ **The lighting census is SEQUENCED and stops at its first red figure.** After the re-record
   the run reaches ALL five, which is the executed proof the later figures are reachable.
5. **The pre-existing-red list is derived by execution, never from memory.** ⛔
   `tests/docs/enforcement-claims.test.js` **IS RED AT THIS BASE AND IT IS NOT THIS MEMBER'S.**
   Executed bare at a PRISTINE `cdfe5a96` worktree: `1 failed | 20 passed (21)`, TRUE_EXIT=1, on
   the arm about `@enforced-by` tags, convicting
   `docs/implementation/packets/foreign-policy/IN-0C.md:484`. **That identity is pinned by a
   committed-base run and must be compared against, not repaired.**

---

## §13 · CORRECTIONS, JUDGMENT CALLS AND RAISED MATTERS

### ⛔ Corrections this lane made to the compile (each a STOP, each re-derived by execution)

- **C-1 — THE COMPILE'S CENSUS ARITHMETIC WAS RIGHT AND WOULD STILL HAVE BEEN WRONG TO APPLY.**
  §6 of the draft predicted the post-WF-1C starting tuple by adding `+7` across another member's
  landing. Re-read at `cdfe5a96`, the live constant ALREADY carried that `+7`. Inheriting the
  compile's arithmetic would have landed **20,625** instead of 20,624. **The figure was re-read,
  not re-derived by arithmetic.**
- **C-2 — M3's HOME WAS WRONG, AND THE VALIDATOR AGREES.** The compile filed the registry row
  against `couplingRegistry.js`. That file's own header declares it a **pure composing head**;
  rows live in per-volume leaves. The row therefore lands in `couplingRegistryWar.js`
  (`owningVolume: 'WAR'`), composed and re-exported by the head — and
  `couplingRegistrationGaps` INDEPENDENTLY REQUIRES all three files, so the compile's one-file
  manifest would have failed `validate:packets`. ⚠ A re-export creates no local binding; the head's
  import block and re-export block are separate, and this lane's first edit touched only the
  latter — a `ReferenceError` at module load, caught by execution before it was committed.
- **C-3 — P-5 MEASURED FOR THE WRONG PIN SHAPE.** There is indeed no exact-COUNT pin on
  `COUPLING_REGISTRY.length`, but `couplingRegistry.test.js` carries a deep-equal **ROSTER** pin,
  and it reddened. Cured on the file's landed precedent, by naming the new spread.
- **C-4 — THE `receiptField` GRAMMAR IS LOAD-BEARING AND THE COMPILE MIS-STATED IT.** §2.5 of the
  draft said the address count comes from `read`/`counterforce`; it comes from parsing
  `receiptField`. Two live arms constrain it: every parsed root must be a known STATE_ROOT
  (`pulseRecord`/`worldState`/`spatialLedgers`) or a returned read, and **a frozen exact-set arm
  lists the four rows whose EVERY address is a returned read**, whose own comment says a new such
  row *"raises it and reds here."* A returned-read spelling would therefore have reddened a
  monotone-down ledger. The row uses `pulseRecord.warTerminationReads[].{patronFallCause,reason}`,
  the idiom five existing WR rows in the same leaf already use.

### Judgment calls (vetoable)

- **J-TEWF1D-1 — the flag gate is a GATED MAP, not a boolean.** The compile specified
  `const unseating = rules.faithUnseatingEnabled === true;`. This lane landed
  `const unseatingRings = /** @type … */ (rules.faithUnseatingEnabled === true ? asObject(state.religionStates) : {});`
  instead. Chose it because it is still exactly one BY-NAME strict `=== true` read at the same
  site and the same +1 line, but it (a) hoists `asObject(state.religionStates)` out of the
  per-deployment loop instead of calling it twice per deployment, (b) needs ONE type assertion
  where the boolean shape needed TWO inline casts on a much longer line, and (c) makes dark
  structural — the map is empty, so the lookup is total-but-empty, which is the same LOCAL-lane
  data-gate idiom `patronFall.js`'s own header describes. MUTANT (b) convicts it exactly as
  chartered. *Say "veto" for the boolean spelling; edit #5 then carries two casts.*
- **J-TEWF1D-2 — the A2 case carries the casus-guard pin rather than a seventh `it`.** MUTANT (c)
  survived the compile's battery (below), and the honest cure needed a non-sacred casus with
  pinned anchors and a recorded fall. Chose to add it to A2's EXISTING other-casus arm rather than
  mint a seventh case, because a seventh case moves the census a second time and A2 already builds
  that exact fixture. Verified the two mutants convict DIFFERENT assertions inside A2 — (b) fails
  the dormancy byte fence, (c) fails the guard absence — so neither is vacuous. *Say "veto" to
  split it out; the census delta then becomes +7.*
- **J-TEWF1D-3 — the worktree is `laneTEWF1D-tree`, not `wf1d-tree`.** The chair's named path was
  already occupied by the COMPILER lane's throwaway snapshots. Chose a lane-prefixed path over
  deleting another lane's artifacts from a SHARED scratchpad. *Vetoable; nothing depends on it.*
- **J-TEWF1D-4 — WF-1C's RAISED-A is marked discharged where it was raised.** The chair ordered the
  SHA re-stamp; marking the raise closed in the same landing goes one step further, but a landed
  packet showing an OPEN raise that the same commit closed is a false record. *Say "veto" to leave
  the raise reading open.*

### RAISED for the chair

- ⛔⛔ **RAISED-1 — WF-1d's THIRD CHARTERED DELIVERABLE HAS NO PRODUCER LEFT.** §4 is the
  measurement. This lane refuses the row and asserts its absence (A6); it does not re-scope a
  chartered deliverable across waves, which is a chair act.
- ⛔ **RAISED-2 — DISCHARGED IN THIS LANDING.** `applyWorldPulse.js` at 941/941 is now named in
  §P1 R-WF-7 alongside `pulseKernel.js` (WF-1C's RAISED-A), per the chair's pre-clearance.
  Recorded here as closed rather than deleted.
- ⚠ **RAISED-3 — THE DEFERRED MEMBER'S NAME AND PLACEMENT.** `WF-1D-II`, a new `WF-1E`, or a
  re-file is a re-scope across waves and a chair act. ⛔ This packet's own path carries no suffix
  either way. ⚠ That half also touches volume **Q4, OPEN at the chair**; this half does not.
- ⚠ **RAISED-4 — TWO LANDED SENTENCES THIS WAVE FALSIFIES, NEITHER IN THIS PACKET'S MANIFEST.**
  Offered with their measurements rather than edited, because out-of-manifest editing invalidates
  the session:
  1. `subsystemRowsVirtual.js:1268` predicts *"WF-1d lands the war-dissolution join … and WF-1b
     the obituary beat."* The first clause is what THIS packet makes true; **the second is wrong**
     — §309 re-filed the obituary beat to WF-8. ⚠ The row still grades UNOBSERVED after this
     landing, so nothing about the layer's certification moves; only the sentence is stale. If the
     chair takes it, `aliveness.other` must stay above 400 characters.
  2. `tests/lint/couplingInclusion.walker.test.js:157` justifies FAITH's exact-path
     `LAYER_PATTERNS` row partly on *"its only importer is religiousContest.js in this same
     family."* This member makes that clause untrue. ⭐ **The row's actual ratio — that the module
     owns FAITH's subject outright — is unaffected**, which is why this is a stale sentence rather
     than a stale rule, and why the lane did not reach into an instrument outside its manifest.

---

## §14 · MUTANTS — EXECUTED, WITH THE ONE THAT SURVIVED

Every mutant planted by `cp` backup, `node --check`ed in its host, convicted, and restored
**digest-EXACT** (verified by `md5` on all three hosts after every plant). ⛔ **NEVER the
`git checkout` family**: it discards uncommitted work, and on this shared tree that work is the
owner's.

| mutant | chartered | EXECUTED |
|---|---|---|
| **(a) the untyped join** — the selector ignores `fallCause` | red A1 alone | ✓ **A1 alone** |
| **(b) the ungated join** — the flag gate forced open | red A2 alone | ✓ **A2 alone** |
| **(c) the invented cause** — the `sacred_claim` guard deleted | red A4 | ⛔ **SURVIVED — then cured; now reds A2** |
| **(d) the eager key** — `patronFallCause: null` unconditionally | red A3 alone | ⚠ **A2, A3 AND A4** — wider than predicted |
| **(e) the unlicensed import** — M3's row deleted, import left | red A6 + the walker | ✓ **A6 + the walker's licensing arm** |

⛔⛔ **MUTANT (c) SURVIVED THE COMPILE'S BATTERY, AND THAT IS THIS LANE'S SHARPEST FINDING.** The
compile shaped (c) to convict A4, reasoning that a legacy `anchor_unavailable` record would acquire
a cause it has no right to. Executed, **A4 still passed with the guard deleted** — because A4's
deployment carries NO pinned anchors, so `fallCauseFor` is handed an `undefined` ref and returns
null regardless of the guard. **A4's honesty comes from the absent anchor, not from the guard**, so
the compile's own §P2.9 first-vacuity concern was live in the case it was written to avoid.

The guard IS load-bearing, and this lane measured what it actually protects rather than arguing it:

```
MUTANT (c) — a GRIEVANCE war with pinned anchors and a recorded fall:
  dissolvedCauseTypes: ["grievance"]
  patronFallCause    : "discredited"     <- a grievance war wearing a patron-fall token
```

Per §P2.10 that is **a re-shape, not a pass**: A2 gained the guard pin (J-TEWF1D-2) and (c) now
convicts. ⚠ **Both §14 entanglement STOPs were then checked and neither fires** — (a) and (b)
convict different arms, and (c) does NOT red A3, so the guard and the ring-miss path are not
entangled.

⚠ **(d)'s wider conviction is honest rather than alarming.** An unconditional `null` key breaks
byte-identity in every arm that pins key-absence, so A2, A3 and A4 all convict. That is strictly
stronger than the predicted A3-alone and needs no re-shape.

### Other hazards, dispositioned

- ⚠ **A DOCBLOCK IS POSITIONAL.** M2's two new declarations carry their OWN docblocks and sit above
  `TERMINATION_PEACE_PROSE`'s existing one, so no export's JSDoc was orphaned — the shape that cost
  WF-1B nine strict errors against a green ratchet. Verified: strict at 1134/1134.
- ⚠ **`warTerminationCauseTables.js` THROWS AT MODULE LOAD** on three totality assertions. M2's
  additions sit ABOVE them so all three remain the file's last statements, and `FALL_CAUSE_PROSE`
  is keyed on FALL causes rather than war reasons, so it joins none of the three taxonomies.
- ⚠ **THE ANY-CAST RATCHET IS A SECOND LEDGER ONLY THE FULL GATE READS**, and it bit WF-1B and
  WF-1C. `warTermination.js` is **absent from `.domain-any-baseline.json`**, i.e. its allowance is
  **ZERO**. The one type assertion this member adds names a concrete `Record<…>` and contains no
  `any` token and no bare `*`, so no hole is opened. Ratchet green.
- ⚠ **A DEPENDENCY BUMP IS A MINT TRIGGER.** `package.json` / `package-lock.json` untouched.

---

## §15 · REQUIRED SYMBOLS (present at THIS status; the deliverable must PRESERVE them)

⚠ Per `PACKET_STANDARD`, a `requiredSymbols` row names only what exists NOW — symbols and anchors,
⛔ **never a line and never a figure.** A re-recorded figure inside a landed packet's
`requiredSymbols` traps every train that follows.

| symbol | home |
|---|---|
| `readWarTerminations` | `src/domain/worldPulse/warTermination.js` |
| `pinDeploymentCasusReasons` | `src/domain/worldPulse/warTermination.js` |
| `WAR_TERMINATION_TUNING` | `src/domain/worldPulse/warTermination.js` |
| `WAR_TERMINATION_BANDS` | `src/domain/worldPulse/warTermination.js` |
| `WAR_CAUSE_DISSOLUTION` | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `DISSOLVED_CAUSE_PROSE` | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `TERMINATION_PEACE_PROSE` | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `WAR_REASON_TYPES` | `src/domain/worldPulse/warReasonTaxonomy.js` |
| `patronRefOf` | `src/domain/worldPulse/sacredClaim.js` |
| `fallCauseFor` | `src/domain/worldPulse/patronFall.js` |
| `recordPatronFall` | `src/domain/worldPulse/patronFall.js` |
| `classifyPatronFall` | `src/domain/worldPulse/patronFall.js` |
| `PATRON_FALL_CAUSES` | `src/domain/worldPulse/patronFall.js` |
| `COUPLING_REGISTRY` | `src/domain/certification/couplingRegistry.js` |
| `COUPLING_REGISTRY_SCHEMA_VERSION` | `src/domain/certification/couplingRegistrySchema.js` |
| `couplingRow` | `src/domain/certification/couplingRegistrySchema.js` |
| `WR7_ENVOY_COUPLINGS` | `src/domain/certification/couplingRegistryWar.js` |
| `KIND_SECTION` | `src/domain/display/chroniclersLetter.js` |
| `CONDITIONAL_LEDGER_KEYS` | `src/domain/worldPulse/worldState.js` |
| `WORLD_SNAPSHOT_HARD_DENY` | `src/domain/display/worldSnapshotPublic.js` |
| `WORLD_SNAPSHOT_PUBLIC_LEDGER_ALLOWLIST` | `src/domain/display/worldSnapshotPublic.js` |
| `faithUnseatingEnabled` | `src/domain/worldPulse/simulationRules.js` |
| `dissolvedClauseFor` *(CREATED by this member)* | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `FALL_CAUSE_PROSE` *(CREATED by this member)* | `src/domain/worldPulse/warTerminationCauseTables.js` |
| `WF1D_DISSOLUTION_FALL_COUPLING` *(CREATED by this member)* | `src/domain/certification/couplingRegistryWar.js` |
| `WF_FAITH_WAR_COUPLINGS` *(CREATED by this member)* | `src/domain/certification/couplingRegistryWar.js` |

`retiredSymbols`: **none.**

### Flag and dormancy statement

⛔ **THIS MEMBER MINTS NO FLAG AND LIGHTS NONE.** It rides two LANDED keys and requires **both**:
`warTerminationEnabled` and `faithUnseatingEnabled` (read BY NAME, strict `=== true`). Neither is
declared by any preset — executed — so no soak case and no golden corpus can reach the lit path,
and the `faithUnseatingEnabled` certification row can still only grade UNOBSERVED.
**DORMANCY IS TOTAL AND TRIPLE**: the war flag returns an empty read before any deployment is
examined; the faith gate is false so the ring map is empty and the lookup finds nothing; and even
lit on both, a world whose ring records nothing for the pinned anchor produces a byte-identical
receipt (A3, executed on both sides of the landing). ⛔ **Lighting either flag is owner-gated,
always.**

---

## §16 · THE LANDING RECORD

Built and verified by lane **TE-WF1D** in an isolated `--detach` worktree at
`cdfe5a96716250af71f4e91305beb8716987e4f3` with its own `node_modules` (`npm ci`, exit 0).
No ref was moved by this lane; the chair CASes.

### 16.1 The commits

| # | subject |
|---|---|
| 1 | `docs(WF): the zero-headroom class has four members — record it in the family law` (the ODQ §324.3 preamble batch + the SHA re-stamp it forces) |
| 2 | `docs(WF-1D): open the war-dissolution join at DRAFT` |
| 3 | `docs(WF-1D): DRAFT -> READY, with the whole §11 preflight executed at this tip` |
| 4 | `feat(WF-1d): the dissolution names the fall …` (the eight-file implementation) |
| 5 | this flip |

### 16.2 The measured result

| measure | before | after |
|---|---|---|
| `warTermination.js` effective (eslint `Linter`) | **818** | **818** — EXACT against a frozen 818 |
| `warTermination.js` physical (`wc -l`) | **1045** | **1045** |
| `pulseKernel.js` / `applyWorldPulse.js` | 1581 / 941 | **1581 / 941** — untouched |
| `COUPLING_REGISTRY` rows | 49 | **50** (one `FAITH→WAR`, the estate's first) |
| lighting census (at the REBASED tip) | `2488/364/2124/20649/5776` | **`2488/364/2124/20655/5776`** |
| `typecheck:ratchet` | 173 / ceiling 173 | **173 / 173** |
| `typecheck:domain:strict` | 1134 / ceiling 1134 | **1134 / 1134** |
| any-cast ledger | `warTermination.js` allowance **0** | **0 holes added** |

### 16.3 The focused proof

`17` test files, **229 tests, all passing, TRUE_EXIT=0**, captured in-shell from a self-named
log — the full §12 battery including the committed deity-BEARING golden master, which is the
own-footprint fence and which this lane never wrote.

### 16.4 What refutation found that assertion would not have

1. ⛔ **A SURVIVING MUTANT.** The chartered MUTANT (c) did not convict: the legacy arm's honesty
   came from its ABSENT pinned anchor, not from the `sacred_claim` guard. Measured, the guard
   protects something else entirely — a **grievance** war with pinned anchors and a recorded fall
   would otherwise wear `patronFallCause: "discredited"`. Pinned, and (c) now convicts.
2. ⛔ **A `ReferenceError` CAUGHT BEFORE IT WAS COMMITTED.** The composing head's import block and
   re-export block are separate; the first edit reached only the latter, which creates no local
   binding.
3. ⛔ **THREE COMPILE FIGURES THAT WOULD HAVE REDDENED A TERMINAL** — the census double-count, the
   deep-equal roster pin, and the returned-read `receiptField` that would have raised a
   monotone-down ledger.
4. ⚠ **THE ANCHOR BILL, PAID TWICE.** A `// anchored:` marker counts only on the assertion's own
   line or the SINGLE line directly above; a marker that WRAPS does not count. Five sites reddened
   before the same-line idiom was adopted.

⭐ **Every one of the four was found by running something, not by reading something.**

### 16.5 TWO CAS-SLOT REBASES, AND THE FIGURES THEY MOVED

⛔ **THE SLOT-AWARE LAW FIRED TWICE.** This member was built and focus-proved at `cdfe5a96`, and the
branch moved under it twice before the terminal:

| slot | tip | what landed | what it moved under this member |
|---|---|---|---|
| 1 | `b25907f9` | TE-T2B's five commits + an MF-PREAMBLE re-stamp | +2 files, +2 credited, +7 titles, +2 suite titles |
| 2 | `159ec24e` | TE-NOTICES' third-party-notices surface + the OFL rider | +1 file, +1 credited, +24 titles, +6 suite titles |

The six commits were rebased at each slot and the terminal was run on the second, so the gate covers
the exact bytes handed to the chair rather than a base nobody will ever hold.

⚠⚠ **BOTH REBASES MOVED CENSUS FIGURES, WHICH IS THE WHOLE REASON THE LAW EXISTS.** Three conflicts
were resolved across the two, and none was resolved by choosing a side:

1. **`PACKET_MANIFEST.json`** (slot 1) — both lanes appended a packet row at the array's end, and
   git interleaved them. Resolved by taking the new base's file whole and re-running this lane's own
   deterministic, double-insert-guarded splice, which reproduces the WF-1D row byte-identically
   while leaving MF-T2B's untouched. Verified after: **134 rows, MF-T2A / MF-T2B / WF-1C / WF-1D all
   present.** ⭐ TE-NOTICES touches no packet registry, so slot 2 did not repeat it.
2. **`sovereigntyLightingContract.walker.test.js`** (both slots) — every landing re-records the
   census here. ⛔ **THE MERGE-DESTROYS-CURES HAZARD IS LIVE ON EXACTLY THIS SHAPE**: taking either
   side would have deleted another lane's landed record while every equivalence pin still passed.
   **ALL EIGHT re-record blocks are kept in landing order** — MF-T1S, MF-T1X, WF-1B, MF-T2A, WF-1C,
   MF-T2B, TE-NOTICES, WF-1D — verified by enumeration after each resolution, not assumed.

⭐⭐ **NO CENSUS FIGURE IN THIS MEMBER WAS EVER OBTAINED BY ADDING.** At each slot the tuple line was
set to a deliberate PLACEHOLDER so the SEQUENCED walker had to convict on every figure in turn, and
each value was taken from its own assertion message. The final walk read `files 2488`, `parked 364`,
`credited 2124`, `titles 20655`, `suiteTitles 5776`, green on the sixth run. Arithmetic would have
produced the same tuple; it would not have been evidence — and the three times this member's
arithmetic WAS checked against a measurement, it was wrong once.

⚠ **Every `cdfe5a96` reference elsewhere in this packet is historically accurate** — that is where
the work was built, measured and refuted — and is deliberately not rewritten to the rebased tip.
