L-CHAIR-901 fix car A: the mission dispatcher's header named the operations-voice leaf by file, and the leaf's own namer census counted a fourth

The composed-tip proof (whole-901.log at d75e807a6; 9 failed of 32,429) reddened
`tests/domain/operationsVoice.test.js` › "no module under src names this leaf, and the
census is executed over the tree": the mention roster measured FOUR namers against an
exact roster of three — `src/domain/worldPulse/operations/missionDispatcher.js` joined the
certification row, the family door and the manifest. The token entered at `f2ab6c802`
(L-CHAIR-901 item 2), whose cure of the "NO FLAG IS MINTED HERE" header cited
`espionage/operationsVoice.js` by path in the very paragraph that explains why the
espionage door module must not be named by path. Same class, one module over: the namer
census is a RAW-SOURCE regex over src/, and a comment is a mention.

Cure: the citation is written in words ("the operations voice leaf's own door prose"),
one line, same line count; the file now carries zero occurrences of `operationsVoice`,
`espionageGate.js` and `espionageEnabled`. Executed under the mutex at this tree:
tests/domain/operationsVoice.test.js + tests/property/espionageDormancyFence.test.js +
tests/domain/missionDispatcher.test.js — 3 files, 128 tests, exit 0 (fixA-901.log).

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
