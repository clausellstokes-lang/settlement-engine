# DW-0 RULING DOCKET — chair's decision sheet

**Basis.** Six lenses, refuted; 47 findings survived; merged here to 14 blockers, 7 rulings, 6 owner-gated items, 18 recorded amendments. **No test was executed by any lens** (three live lanes, serial mutex) — every "reds the walker" below is read off shipped test source, not off a run. Every engine citation was re-resolved at the declared slot `79b78881c`.

**Two facts the chair needs before reading anything else, both measured this session:**

- The compiled charter is **unlanded**. `7c3377cd4` is contained in **no branch** — only in `refs/preserve/holding-dw0` — and `docs/DW0-CHARTER-COMPILED.md` is MISS at `review-fixes-2026-07-08`, at `master`, and at `79b78881c`. Nothing has to be un-landed; the blockers below can be fixed before the document ever exists on a branch.
- One root cause generates six of the fourteen blockers: **the charter declares base `79b78881c` and reconciles against `029268fe5`, an orphan preserve ref that is not its ancestor.** `git merge-base 029268fe5 79b78881c` exits 1 with no output. Six cars landed in between and the charter knows about none of them.

---

## 1. BLOCKERS — ranked by consequence

Each names the earliest car it stops. Items 1, 5 and 11 stop **every** car.

**B1 — The prerequisite reconciliation was run at the wrong ref, so the charter bills landed work as pending and inherits a decayed absence.** *(merges internal-consistency L3-02, dependency-scope L4-10/L4-14, omission-hunt M4, engine-grounding L2-10)*
CG-1 (`d78011665`), CG-1b (`3e9d2d888`), CH-1 (`b2852ccc3`), CH-2A (`17fe89763`), UC-5 (`f4df874ce`) and MP-1 (`9e5059cec`) are all ancestors of `79b78881c`, yet §F.2 bills "2 prerequisite cars (CG-1, CG-2) / 47 commits", §F.4 draws "CG-1 → CG-2", §H G1 hands a landed CH-1 a work order, and §G's CG row quotes "Today: 16 of 48 (32 throw — 29 on the binding cap)" — a baseline CG-1b cured to 0 of 504 before the charter's own base commit was written.
**Fix:** add a §F.0 "WHAT IS ALREADY LANDED AT THIS BASE" table with a sha per row (LANDED: CG-1, CG-1b, CH-1, CH-2A, UC-5, MP-1; HALF: CH-2 — CH2B holds at DRAFT per §520.5; OUTSTANDING: CG-2, CH-3, CH-4, CH-5); re-derive §F.2's count (**1** remaining CG car, **46** dispatched); base-stamp or re-measure every "Today" integer; and add the standing compile rule that **an absence claim inherited from a frozen dossier is re-measured at the slot, because absence is the one claim that decays silently.** Blocks: all.

**B2 — UC-5 exists. The charter asserts three times, in the present tense, that it does not, and builds a car on the assertion.** *(merges L2-01, L3-01, L4-9 — three lenses independently)*
`git cat-file -s 79b78881c:src/domain/undercity/connectivity.js` → **28261**. The charter says at lines 561, 628-629 and 689 that it "does not exist at any ref", calls that a *Standing fact*, and the AMENDMENT RECORD's list of nine facts re-verified at the slot does not include it.
**Fix:** strike all three assertions. **Delete DW-4b's honesty clause and the acceptance arm that greps for it** — it would ship a literal false statement into every plan and pin it with a test. Re-disposition **E7** against the shipped `CONNECTION_CLASSES = ['NATIVE','ADJACENCY_BREACH','FUNDED_LINK']` (`:128`) and the `ADJACENCY_BREACH / SHARED_QUARTER_WITH_MOTIVE` return at `:397` — it is DISCHARGED or genuinely open, never "ACCEPTED-PARKED on a module that does not exist". Re-home **D-6** (the ROOF and WATER-LANDING joint kinds): UC-5 was the undercity train's "SIXTH and LAST car", so the train it was deferred to has no remaining dispatch vehicle. Blocks: DW-4.

**B3 — MP-1 landed *in the charter's own base commit* and the charter names it zero times, leaving DW-6d pointed at work a shipped single-writer leaf already owns.** *(merges L2-02, L4-1)*
`cartographyProperty.js` (9,141 B) exports `buildPropertyLineIndex`/`propertyLineForAnchor`/`openGroundOf`, is `@enforced-by tests/domain/townCartographyProperty.test.js` (13,688 B), and is mounted at `SettlementMapPane.jsx:601`. Its header states the DW integration contract verbatim: when DW-1/DW-2's typed `CompoundMember` rows land, `membersOf` reads them **instead** and "the RETURN SHAPES below do not move". Both frozen sources carry the boundary — architecture `:1579` gates "DW-6d (after MP-1 has landed its identity edit)", draft §8.4 is a whole section titled "What belongs to MP-1". A node scan of all 716 lines returns ABSENT for `MP-1`, `cartographyProperty`, `§495`, `membersOf`, `openGroundOf`.
**Fix:** name the module, the test and MP-1 in §C.6 and the file map; re-cut DW-6d as a **consumer** (see ruling R7); add "DW-1/DW-2 emit typed `CompoundMember` rows with `membersOf`/`openGroundOf` return shapes unchanged" as a named DW-1f/EST-3 exit criterion, which no car currently carries. Blocks: DW-6d, and the DW-1f exit text.

**B4 — `PlanDelta` is chartered as a durable user-authored write with one of the three laws its shipped sibling carries; the missing one silently corrupts a paying user's authored map.** *(omission-hunt M1, M2 merged; two limbs of each struck by the refuter)*
`src/domain/interior/interiorEdits.js` (10,238 B) states three laws — DORMANCY, the KEY-NAMING TRAP, and **EDITS-DELTA**: pins key on a *stable id* so a re-derived model "re-applies every pin whose anchor still resolves and DROPS the dangling ones (never a throw, never a ghost)". DW carries only dormancy. The contract is `{ buildingId, path, op, value, editedAt }` with **`path` nowhere defined**, and DW-S re-derives every plan at every soak epoch. `interiorEdits`, `mapEdits`, `PUBLIC_TOPLEVEL`, `PRIVATE_KEY`, `dangling` and `re-apply` are all **0 occurrences across all three documents**.
**Fix:** rule `PlanDelta` into the sidecar family: keyed on a stable cell/fixture anchor id, never a structural path; a dangling anchor **drops**, and DW-7's exit carries that case with its mutant. Second, state the KEY-NAMING TRAP for DW's keys — `chronicle` and `seed` are both `PRIVATE_KEY_RE` tokens and DW emits both, so a matching key is **silently stripped** from every public projection with no arm to see it. (Struck: the leak narrative — the allowlist is fail-closed; and the migration limb — `MIGRATIONS` holds one no-op entry and both shipped sidecars landed additively at `SCHEMA_VERSION = 1`.) Separately: **undo is 0 occurrences in all three documents** while the map pane beside DW-7a ships `canUndoEdits`/`editHistory.undo` (`useTownMapPresentation.js:237`) — a paying Cartographer would get edit-without-undo on the plan. Blocks: DW-7b; the shape decision is owner-gated (§3, O3).

**B5 — "The fourteen grammar requests R1–R14 are already dispositioned by the draft" and "the sixteen engine gaps E1–E16 … the draft already carries them" are false for ten items, and §H forbids re-litigating them.** *(research-fidelity L5-5 — the largest loss-of-item finding on the docket)*
Node substring counts over both frozen sources: `apertureFor` 0/0, `strengthFor` 0/0, `two-door` 0/0, `apertureUse` 0/0, `PROCESS_LOOP` 0/0, `prospect` 0/0, `seasonalOccupancy` 0/0, `permanence` 0/0. Anchored E-counts in the draft: **E6 0, E7 0, E8 0, E9 0, E10 0, E12 0** (and 0 in the architecture). Each is routed to a car whose draft §5.2 row is about something else — R12 → DW-1c (the parti enum), R7 → DW-1e (StorageCell + polarities), R11 → DW-2c (parti placement), R10 → DW-1a (cell kinds).
**Fix:** replace the sentence with a three-column table — request · where the draft carries it (section/line) · UNCARRIED — and give each UNCARRIED item a car with a contract or a recorded deferral with a reason. "Already dispositioned" with no citation is the §441 J7 phantom home applied to ten items at once. Blocks: all — the paragraph closes items across DW-1, DW-2 and DW-4.

**B6 — EST-5 is held on a measurement that reported before the compile ran, and the real prerequisite (CH-4) is in no list; its flag home is also wrong.** *(merges L4-2, L4-6; L4-5's A7 half moved to B12)*
ODQ §517 (2026-08-23 21:52 CDT, ~5h before the compile) is the TC-B8-RECON verdict: the loop is buildable, but `noble` is **unreachable** — 0 of 10 quarters can classify it, because `CATEGORY_PATTERNS` (`districtProfile.js:105-117`) tests merchant at index 2 and criminal at index 8 (matching `Resi**den**tial`) before residential at 11, and `inferCategory` returns on first match. "RULED: it joins the catalog-hygiene train as CH-4." Charter greps: `CH-4` 0, `§517` 0, `noble` 0, `inferCategory` 0. Separately, §E.2 homes the whole ESTATE wave on `townCartographyEnabled`, but the loop's measured home is `urbanFabricKernel.js` (884 lines) behind **`urbanFabricEnabled`** — a different, preset-gated ONE_REGEN flag the charter never names (`urbanFabricKernel` 0, `urbanFabricEnabled` 0).
**Fix:** replace D2 with §517's verdict — EST-5 is unblocked on measurement and **BLOCKED on CH-4**; add CH-4 to §F.4 and §F.2; carry §517.1's three named pieces into EST-5's contract (per-district-instance resolution, a persisted ownership ledger the cartography reads but never writes, a displacement pool); and split ESTATE out of the `townCartographyEnabled` row so an EST-5 lane applying the bill's grep-the-gate rule does not mint a persistence shape it has no authority to mint. Blocks: EST-5.

**B7 — Five catalog rows are filed in the wrong CH-3 class, and the disposition built on it is false permanently, not provisionally.** *(merges L4-3, L5-1)*
`draft-CATALOG-HYGIENE-PLAN.md` §3.1: "311 rows, 36 carry `minTier`, **26 REDUNDANT** … **10 ABOVE-BLOCK**, **0 BELOW-BLOCK**" — disjoint by definition. R-INST-6 D6-1 and R-INST-5 G2's four rows are ABOVE-BLOCK rows #1–#5. J-CH-3-1 (approved at ODQ §538.5) **keeps** them and legitimises "author here, gate there", deleting only the 26 no-ops. The charter says they "are members of CH-3's '26 redundant `minTier` declarations' class. Both may be wholly absorbed", and adds "not measurable until CH-3 lands" — false at the same frozen ref, where `CH-mintier-audit.mjs` measured it and `SKEP-mintier.mjs` re-confirmed it. The charter also contradicts itself two sentences apart, putting the same four rows among "the 10 city rows that vanish from the UI".
**Fix:** restate the class split; delete the "not measurable" clause; and give DW-R2 the affirmative rule that **for an above-block row the firing tier is the `minTier`, permanently**, so its (a) denominator excludes them at the authoring block. Otherwise DW-R2 authors up to ten phantom city-tier `ProgramMinimum` rows and reds its own (a)=(b) equality, and the standard repair widens (a), destroying B1's no-tier-holes guarantee. Blocks: DW-R2.

**B8 — DW-1's only observable exit number is a splice of two sources that disagree, and four cars would red at their own acceptance.** *(merges L2-03, L3-05)*
Charter: `+6/+4/+2/+2/+2/+3` = 19. Draft §5 table: `+1/+1/+2/+2/+2/+3` = 11. Architecture: `+6/+4/+5/+5/+5/+5` = 30, where **each figure equals that car's declared acceptance-arm count**. The compile takes the architecture's (post-AR-7) figure for 1a/1b and the stale draft figure for **1c, 1d, 1e and 1f** — twelve unbudgeted titles across four cars. §F.3's five divergences contain no census row and §I carries no conflict for it. A DW-1c lane that builds the architecture's five arms — which §A says STAND — measures +5 against a stated +2, and the charter's own swallowed-titles warning does not even cover that direction.
**Fix:** open C-16 and rule the source (see R3), then restate all six. (Verified clean and *not* a problem: the same cell's banked-failure sentence is correct — `CEILING = 17` at `tests/lint/testRatchet.test.js:181` with 11 banked entries, and that headroom is for banked failures, not titles.) Blocks: DW-1c/d/e/f.

**B9 — §F.4 dispatches DW-1a and DW-1b in parallel into the same source file, against an explicit serialize rule the architecture states and the slot confirms.** *(engine-grounding L2-04)*
Architecture `:1567`: "DW-1a -> DW-1b (both touch `interiorTemplates.js`: serialize them)". Charter line 480 reproduces the draft's uncorrected graph — "DW-1a,b,c,d,e (parallel) → DW-1f" — with no divergence row. At the slot, DW-1a's target `ROOM_KINDS` is at `interiorTemplates.js:35` and DW-1b's `FURNISHING_KINDS` at `:50`. Two lanes in a shared tree collide on the file, and their member-count tuples conflict on rebase — the banked TE-STACK-3 shape, where the tuple resolves to the wrong side. Second limb: DW-2c is gated "(2a, R2)" where the architecture gates it "EST-1a..1d → DW-2c (2c needs the program table)".
**Fix:** adopt the architecture's dispatch list for both and record it as divergence D6. (Struck: the DW-2b/2d/2e limb — the charter already writes them as a serial arrow chain.) Blocks: DW-1a.

**B10 — DW-1a and DW-1b move a byte-identity-pinned generated artifact and change a shipped public count, inside a wave whose exit declares "Declared shift: none".** *(merges owner-fidelity F6, L2-06, L3-14)*
`compendiumData.generated.js` header: "GENERATED FILE … Source of truth: the engine registries (… `interiorTemplates` …) … Pinned by `tests/docs/compendiumDataFreshness.test.js` (byte-identity + parity). **THE REGISTRY-RENDER LAW**". `roomKinds` at `:409` carries both `stall` and `dais`; `furnishingKinds` at `:410`. The reader is `scripts/generate-compendium-data.mjs:603` (`roomKinds: [...ROOM_KINDS]`), and the public render is `src/components/compendium/CatalogHubs.jsx:66-67` — a "Room kinds: N" card plus the joined list. So `28 − 2 + 57 = 83` moves a shipped public count from 28 to 83 and rewrites a shipped public enumerable. The charter never names `compendium`, `gen:compendium-data`, `freshness` or `CatalogHubs`, and neither frozen source mentions them either — this is a claim the compile volunteered, not one it inherited.
**Fix:** add the artifact and `npm run gen:compendium-data` to DW-1a/DW-1b's changeManifest, change DW-1's declared shift from "none" to a stated one naming the Compendium's `roomKinds`/`furnishingKinds`, and add an arm that the regenerated `roomKinds` contains neither `stall` nor `dais`. Depends on ruling R6. Blocks: DW-1a, DW-1b.

**B11 — The vocabulary collision check was run only "against the existing 28", and fifteen of the 57 new cell kinds are already members of the storage vocabulary.** *(omission-hunt M7)*
`pantry`, `larder`, `dairy`, `scullery`, `still_room`, `drying_loft`, `coach_house`, `loft`, `garret`, `undercroft`, `dungeon`, `cistern`, `arsenal_floor`, `buttery`, `porch` appear in **both** §2.2's ten add-groups and §2.5's storage sub-forms; `PORCH` and `BARBICAN` appear as both circulation sub-forms and partis/cells. The draft's own sentence is "57 distinct backticked names …, zero duplicates, zero collisions **with the existing 28**". This is the exact shape the draft itself says hid the dead `dais` member — "a second dead member hidden by a cross-vocabulary name collision" — with `pantry` now having two homes and the validator two ways to size, reach and shed the same room.
**Fix:** run the cross-vocabulary uniqueness check **before DW-1a**, across {PARTIS, ROOM/CELL_KINDS, CIRCULATION sub-forms, STORAGE sub-forms, FIXTURE_KINDS}; where a name deliberately lives in two, the charter states which vocabulary owns it and the other is a reference; add the check as a DW-1d/1e arm. Blocks: all of DW-1.

**B12 — The soak leg's exit drops the constitutional arms and builds its one surviving arm to a specification the owner's own recon superseded.** *(merges L3-06 corrected, L4-5)*
Draft §6.1 lists arms A1–A9; §G's DW-S row names only A7 plus the B8 loop and reduces the Observable to "Four numbers and a boolean, per epoch". Dropped: **A4** ("same epoch, same seed ⇒ byte-identical plan — THE PROMISE, and the precondition of every other arm"), **A8** ("no reverse motion rewrites what an earlier year recorded"), **A9** ("the constitutional arm: the mechanical statement of 'lived history is immutable'"). Separately, ODQ §517.5 states "**DW-S's ownership arm is re-specified accordingly**" and names the discriminating measures — saturation year and residential floor, 0.376 vs 2.066, a 5.5× separation against only 2× on ownership share. Charter: `saturation` 0, `residential floor` 0, `§517` 0.
**Fix:** restore A1–A9 with A4/A8/A9 named as the leg's **blocking** arms, and re-spec A7 to §517.5's two measures with ownership share reported as secondary. Cross-listed owner-gated (§3, O5). Blocks: DW-S.

**B13 — Four §G criteria cannot be executed as written, in a section whose own rule is that every criterion names what it counts and what value it must take.** *(merges L3-15, M7's second limb)*
(i) DW-2's exit is conditioned on "the cell areas summing to the envelope **within the declared tolerance**" — one occurrence in the document, and no tolerance is declared anywhere, nor in the architecture's DW-2b arm. (ii) DW-1's pin ends in "**the storage set**", whose only figure is the approximate "~35 sub-forms"; circulation's "~55" is likewise unpinnable — so the two vocabularies the owner personally made first-class at §453 (closets and pantries) are the only ones with no ratchet. (iii) DW-6's "The PDF chapter paginates multi-floor / The Foundry export carries wall data / the halo returns a polygon set" carry no counted value. (iv) §A stops the amendment record at AR-12; the draft runs to **AR-13**, which is precisely the row fixing the band count to 20 with BAND ZERO — hence §C:92's "eighteen bands plus BAND ZERO" (19) against a band list that carries 20.
**Fix:** declare the tolerance as a number; recount `~55` and `~35` mechanically and pin both as integers with mutants; convert the three DW-6 clauses to counts; correct AR-12 → AR-13 and the band count. Also add the missing **Declared shift** rows: only DW-1, DW-2 and ESTATE carry one, while DW-7's click surface and CG's lit goldens both declare YES in their sources. Blocks: DW-1e, DW-2, DW-6.

**B14 — Three owner-signed bands have no car in the draft's §5 table the compile says "stands" and no exit criterion in §G, so all 41 cars can land green without them.** *(merges F9 corrected, L2-14, M6)*
**B4** the seven-rung prosperity ladder, **B6** the shedding order, **B11** non-contiguous holdings — each appears only in §C.8's ratification list (lines 330, 333, 335) and returns zero hits across §F–§G (434-595) and zero across the draft's §5 span. B4 is the sharp one: its own ratification text says it "repairs a live defect", and the defect is live — `interiorModel.js:79-87`'s `prosperityScore` has three regexes and a `return 0.5` fall-through, and matching them against the authored ladder (`constants.js:93`) shows **two** rungs fall through, `Moderate` **and** `Subsistence`, both scoring the same as Comfortable. The architecture separately declares `interiorModel.js` "not grown" and restricts every DW car touching it to composition calls, so the repair is architecturally barred as well as unassigned. It becomes user-visible at exactly DW-7a.
**Fix:** add a §C.8 closure table — one row per ratified band, naming the car and the integer or differential that proves it; an empty row is either a deferral recorded in §K with a reason or a missing car. B4's disposition is owner-gated (§3, O4). (Struck from the original finding: B7 and B12 *are* in the draft car table, at DW-2b `:1931` and DW-1f `:1906`; and B21 is not "present as CG-1b" — `CG-1b` appears nowhere in the charter.) Blocks: nothing directly — which is exactly why it must be ruled now, since no lane will ever red on it.

---

## 2. RULINGS THE CHAIR MUST MAKE

**R1 — `owns` / `occupies` and what an Estate is anchored to.** *(the first known-open fork; merges F3, F4, L2-11, L5-9(c))*

> **Recommend: split the ANCHOR from the RELATION.** Keep §519's signed relation rule verbatim — `owns` is a POWER, `occupies` is everyone else — and rule that **estate membership is not carried by `owns`**. An Estate is a set under one **anchor**, and the anchor may be a POWER, a non-power institution, or `ANONYMOUS_FABRIC` (which is where §498.1's "or individual" actually lives, and is exactly what B8's wealth placement needs). Give `ANONYMOUS_FABRIC` its own first-class arm with acceptance, not residual status.

This preserves **both** owner sentences. The draft's own §2.6(c) quotes the owner verbatim — "belonging to one singular institution **or individual**" — and defines the relations as *how the owner came by the building* ("`owns` … when the owner built or bought it"; "`occupies` … a building it did not build — the lent palace, the colonised ruin, the criminal front"). §519's POWER scoping is a statement about *who drives consolidation and whose holdings the tier-2 halo lights*, which is what §519.5's own sentence says.

**Alternative — the literal reading the charter adopted** (`Estate.ownerRef` is POWER-typed, full stop). Cost, all measured: all three of the owner's own §498.2 worked examples become unbuildable as estates — "the abbey with an outlying grange, the merchant with a house on one street and a warehouse on the quay, the innkeeper with the stable-block across the lane" (ODQ 20549-20551), none of them a POWER. THE POWERS is bounded at four (`rulingPowerCoup.js:106` `.slice(0, 3)` plus the seat), so estates per settlement cap at four regardless of tier and B8's concentration limb has at most four beneficiaries in a metropolis — a consequence §C.2 does not note. And §C.6's tier-2 worked example ("hover the merchant's house") becomes unbuildable 27 lines before §C.7 rules merchants cannot own, so DW-6d's arm passes on a ruling-seat fixture while the feature is dead for the case that justifies it — the vacuous arm §G's own rule at line 502 exists to forbid.

**This ruling is not the chair's to make alone** — `ownerRef`'s type is persistence shape. Put it to the owner as one question. Two things to fix regardless of which way it goes: (i) drop the "unrecordable occupant" worry — `buildingRef` → `building.anchorKey` → institution is already shipped at `hoverModel.js:31-36`; (ii) say which contract `Estate.ownerRef` is *not*, because `ownerRef` already means "the paying account that owns a saved record" in **17 src files and 11 test files** (`canonEventApply.js:171`, `commandContext.js:50-54`), so a lane greping the name lands in command-envelope owner scoping. Consider `heldBy`/`anchorRef`. And note L5-9's live consequence: §C.7 orders DW-1f's sizing "re-read on that basis" and then §G freezes it at +3 titles unchanged.

**R2 — Where the compiled charter lands.** *(the second known-open fork)*

> **Recommend: land the CORRECTED charter at `docs/DW0-CHARTER-COMPILED.md` on `review-fixes-2026-07-08`, cross-linked from `docs/implementation/INDEX.md` — and not before the blockers above are fixed.**

Measured basis. The doc gate is clear: the enforcement-claims corpus is "every root-level `*.md` and `docs/**/*.md`" (`enforcement-claims.test.js:42-47`), the charter's `CLAIM_RE` hit count is **0** (re-measured with node against the shipped regex, not with ugrep), `WINDOW = 3` at `:79`, and `docCounts.test.js` pins no doc-file count. So `docs/` is safe and is where lanes look. Landing on the ledger branch puts it beside the ODQ it cites eight times.

Two conditions attach. First, **the document must not land as written** — it currently asserts three false absences (B2) and would make them citable ground truth for every DW lane. Second, its primary sources live at `029268fe5`, an **orphan** preserve commit unreachable from any branch, and the charter itself currently exists only at `refs/preserve/holding-dw0`. §A must name both refs **by name**, not only by sha, and the chair should confirm both preserve refs are protected before anything cites them.

**Alternative — `charters/DW0-CHARTER-COMPILED.md`.** Benefit: sits beside the sources it rules over and leaves the CLAIM_RE corpus entirely, so a later amendment adding a claim-vocabulary phrase cannot red the doc gate. Cost: `charters/` does not exist on any branch (`git ls-tree review-fixes-2026-07-08 -- charters` is empty), lanes do not read it, and leaving the CLAIM_RE gate is a loss — that gate is precisely the phantom-claim guard this panel exists to enforce.

**R3 — Which source is authoritative for DW-1's per-car census deltas.**

> **Recommend: the architecture — 6/4/5/5/5/5 = 30.** Each figure equals that car's declared acceptance-arm count, and §A already rules the architecture STANDS. Restate §G's Observable from the arm counts and open C-16 recording both sides.

**Alternative — the draft's 1/1/2/2/2/3 = 11.** Cost: four cars' acceptance packets must be cut from five arms to two, discarding named convicting mutants that the architecture supplies and §G's own rule requires. Note either way that the banked-failure headroom sentence is correct and unaffected (`CEILING = 17`, 11 banked) — that headroom is not a title budget.

**R4 — §G's granularity, and the 24 cars with no architecture packet.**

> **Recommend: keep §G per-wave (J-DW0-5 already rules this deliberately and per-car arms belong in the architecture), and add to §A the coverage fact plus a hard gate: the architecture covers 17 of the 41 DW cars, and DW-R, DW-3, DW-4, DW-5, DW-6, DW-7 and DW-S — 24 cars, more than half the program — are owed a `changeManifest` and named mutants before their wave dispatches.**

**Alternative — write per-car criteria into §G.** Cost: duplicates the architecture, lengthens the ruling layer, and creates a second place a car's contract can drift. The real defect is not the granularity; it is that §A presents the architecture as universal without saying it stops at 17.

**R5 — The `ProgramMinimum` depth policy (C-5).**

> **Recommend: rule HONEST into §C.1 now** — "Policy tier = HONEST (unstruck by the signature, marked TASTE); coverage = no tier holes (signed)" — and add the tier to DW-R's exit so a row authored at SPARE or GENEROUS is a detectable defect.

**Alternative — leave it as §I's unruled recommendation.** Cost: `HONEST` appears on exactly one line of the whole charter (the conflict row), §C.1 rules B1 without it, and unlike EST-5 (BLOCKED on TC-B8-RECON) DW-R2 is not marked gated on C-5 — so a lane can dispatch and size roughly 311×N minimum cell sets on its own taste, on the program's long-pole table.

**R6 — Does DW-1a REPLACE `ROOM_KINDS` or SHADOW it?**

> **Recommend: replace.** `28 − 2 + 57 = 83` describes an edit to the real symbol, DW-1's exit already demands the retired-2 intermediate pin, and a shadow leaves two disagreeing room vocabularies in `src/` with the public Compendium under-reporting by 55 — the second-truth shape J-DW0-1 exists to prevent. Price it as B10 specifies: regenerate the artifact, declare the shift, name the public count move 28 → 83.

**Alternative — shadow in a new module.** Cost: as above, plus an arm is then owed proving the two lists stand in a stated containment relation, so a reader can see they are not two truths.

**R7 — Is DW-6d a PRODUCER or a CONSUMER of the property-line polygon set?**

> **Recommend: consumer.** MP-1's leaf already wrote the integration contract (`cartographyProperty.js:29-35`): DW-1f mints `CompoundMember`, EST-3 supplies the typed rows, `membersOf`/`openGroundOf` are re-pointed at a new **input** with return shapes unmoved. DW-6d's deliverable becomes the typed member polygons plus the tier-2 overlay, not a halo.

**Alternative — DW-6d builds its own producer.** Cost: two writers of "what a property line is" against a mounted single-writer leaf, so the hover halo and the DW projection can disagree about the same property. Also decide (see B2) whether DW-4 is a producer or a consumer of surface joints, given `connectivity.js:13-14`'s "ONE PRODUCER PER JOIN" law.

---

## 3. OWNER-GATED — not the chair's under any delegation

**O1 — The `interiors` deferred ladder row's return cells, and the free-sample teaser.** *Paid-surface behaviour.* The charter names DW-7a "the car that makes that row real" (line 239) without reconciling the ledger's **recorded** return cells — `free: '1 sample per settlement'`, `cartographer: true`, `axis: 'viewing'` (`entitlementLadder.js:173-180`) — against its own §C.4 ruling that viewing is "FREE at every level, anonymous included" (line 204) and its own DW-7 exit case "view-free" (line 582). Those cells make `claims()` (`enforcement-claims.test.js:**73**`) true; returning the row as stored reds rule F (`:249-254`), and the cheapest green is the fourth ledger entry §C.4 forbids, which also reds the exact-equality pin at `:260`. The charter parks the sitting at line 584 but never tells the owner that **the stored cells are the thing being ruled on**. *Chair's recommendation to carry:* return as `{ axis:'viewing', free:true, cartographer:true, enforcement:'parity' }` — which is what signed B16b and ODQ §523.2 require — and state plainly that this strikes the 2026-07-17 "ONE free sample interior per settlement" teaser. That single sentence is the only place DW touches paid-surface behaviour. (Note: rule F cannot red today; `DEFERRED_LADDER_ROWS` is outside `ROWS`, and the ledger pins the return to "map activation D3b/P6", a phrase with **zero** hits in the charter.)

**O2 — `Estate.ownerRef`'s type (R1).** *Persistence shape.* The single largest contract change in the program. See R1 for the recommendation and the cost of each branch.

**O3 — `PlanDelta`, `EstateEvent`, and EST-5's persisted ownership ledger.** *Persistence shape.* Three new durable artefacts on the settlement record, one of them user-authored on a paid surface. The chair may rule the *laws* they must carry (B4); whether they exist and in what shape is the owner's. Flag explicitly that an EST-5 lane misled by the wrong flag home (B6) is one step from minting a persistence shape on its own.

**O4 — B4's seven-rung prosperity ladder.** *Tuning signature under THE PROMISE.* `prosperityScore`'s three regexes are a tuning constant; the memory's standing law is that tuning is owner-signed and the tuning pass is last. Two rungs currently fall through to 0.5, so any repair moves interior areas on more settlements than the "roughly a quarter" figure covers. Present it as: the owner signed B4 and the program as chartered does not deliver it — is the repair owner-signed now, or deferred to the tuning pass and recorded in §K?

**O5 — Shipping DW-S without A4, A8 and A9 (B12).** *THE PROMISE.* Restoring the arms is the chair's; deciding to certify the soak leg green with same-seed determinism and lived-history immutability unmeasured is not.

**O6 — Already discharged; do NOT put it back on the owner.** The three viewing paywalls. ODQ §523.1 records the owner's contemporaneous delegation ("i leave all judgements to you") of exactly this class, and §523.3 ruled all three — `change-view` FREE, `map-chains` FREE, `fog-table` PAID — at 2026-08-23 22:42 CDT, roughly four hours before the compile committed (2026-08-24 02:45:59 -0500). The charter cites §523/§524 at lines 207/209 for the export trichotomy and then carries the three as unresolved at §C.4 (233-234), §J-DW0-6 (669) and §K.6 (687-688): a **partial read**, not a clean miss, and the AMENDMENT RECORD's input list never names the ODQ at all. Strike them from §K's open list.

---

## 4. FIX-AND-PROCEED — recorded amendments; the charter can land with these

1. **Rewrite §C.4 / §J-DW0-6 / §K.6 against §523/§524** and add **WEB-8b** to §F.4's prerequisite line. DW-6's arm is already correctly written as no-growth/shrink-only and will not red when WEB-8b shrinks the ledger to 1 — do not convert it to a literal length. *(F2, L4-11)*
2. **Give B16's anonymous case an observable.** The charter rules it correctly at line 204 but §F–§G's only `/anon/` hit is `ANONYMOUS_FABRIC` at line 545, and DW-7's observable names four cases with no anon case. `anon` is a distinct resolved tier with a distinct gate row (`maxTier:'town', maxSaves:0` vs free's `capital`/3, `authSlice.js:48`). Make it five cases and add a DW-6 arm that the map block is present in the dossier payload at tier `anon`. *(F5)*
3. **Re-derive the ESTATE lit-file fence at the slot and base-stamp it.** `git grep -ln townCartographyEnabled 79b78881c -- tests` returns **nine**; the charter names eight. `tests/domain/townCartographyProperty.test.js` lights the stage (`LIT_RULES` at `:38`) and proves parcel membership against a real manifest, so EST-2a/2b/EST-3 move it by construction and the "A ninth moving file is a STOP" rule fires on a lawful landing. `tests/domain/townCartographyCalibration.test.js` is a probable tenth via the lit corpus fixture. Note also that the draft called the eight "a PLAUSIBLE architectural reading, not a measurement" and the compile hardened it into a fence with a hard stop. (Not a lit file: `settlementMapPropertyLine.test.jsx`.) *(L3-08, L4-8)*
4. **Add a near-threshold control to the ESTATE stop rule.** Subject max squared deviation 0.4999, control min 291.30 — so the tolerance can be widened from 1 to anything below 291.3 and both fractions still read 1,056/1,056 and 0/528 while the lattice bound (0.5) the arm rests on is already void. Assert `max < 0.51`, or plant a perturbed chord at ~1.2 that must fail. Keep the vertex-crossing quad as a structural control; it is a legitimate synthetic negative. *(L3-07)*
5. **Add the ninth row to `SIGNED-BANDS-2026-08-23.md`** carrying §518's superseded ruling and §519's correction, as §519.5 already directs. The sheet is 4,989 B, exists at **exactly one ref** (`refs/preserve/research-dossiers-2026-08-23`), was written once and never amended, and carries eight rows: B16, B16b, B13, B15, B1, B8, B11, B21. Until then the charter should read "§519 RULED IN THE ODQ, signature-sheet row pending" rather than "Signed". §519's *authority* is not in doubt — §519.1 quotes the owner directly. *(F8, L5-9(a))* **Do this before EST-4 dispatches.**
6. **Record B21.** It is one of the eight owner-signed rows, appears **0 times** in all three documents, and its constant is live at `cartographyTuning.js:95` (`CARTOGRAPHY_HEADROOM_PERMILLE = 1600`) under a header reading "THE DECLARED HEADROOM (BAND 21, owner-signed 2026-08-23)". §C's framing — "the draft's §7 asked eighteen bands … as QUESTIONS" — structurally cannot reach it, since B21 is not a draft band. Add a §C row naming CG-1b as its consumer and the value as landed and tuning-pass-only. *(L3-09; M4's "no record anywhere" limb is struck — MF-CG1b's packet does carry it)*
7. **Split the CG exit row.** CG-1/CG-1b's throw-rate criterion is DISCHARGED at `3e9d2d888` with its own receipt (287/504 → 0/504); **CG-2's identical-footprint criterion is the only live half**, and `MF-CG2` has no landing at any ref. Also name the constant correctly: the charter says "TC-3 byte band"; the constant is `TC4_ROW_BYTES_BAND`. *(L3-03)*
8. **Fix DW-R2's Observable — it compares three different research predicates as one NO_BUILDING count.** Only the criminal 11 is a NO_BUILDING count. R-INST-4's NO_BUILDING count is **11 of 40**, not 14 (14 is NO_BUILDING-**or-HOSTED** at every tier), and R-INST-2 publishes no verdict census at all (the token "verdict" occurs once in 581,595 B), so "~30 of 124 at the floor" can never be matched by any ProgramMinimum verdict total. Split into three named predicates with the tranche's own wording. *(L5-2)*
9. **Record G1's residual.** J-CH-1 hands `Warden's Lodge` back to DW in writing — "DW-1 assigns the real kind" — and DW-0 records neither the return nor an owning car. The gap is the **interior kind**, not the parti (`MOATED_PLATFORM` is already a DW-1 member): DW-1's five chartered vocabularies contain no kind vocabulary and the draft makes `interiorKind` a consumed input to S2, so nothing in the program mints it. `Warden` occurs once in the charter, inside §H's own restatement. *(L5-3)*
10. **Rewrite G5 and G6 against CH's measured home table, not R-INST-5's prose.** CH H19 refuted G6 by execution — `Dragon resident` appears **0 times** at `magicExists:false` and 0 at `pm0` across 840 settlements — and H18 refuted G5's dead-magic half; CH §2(b) states "CH-2 should not 'fix' either". The charter references `draft-CATALOG-HYGIENE-PLAN.md` exactly twice and never reads the home table. DW's `occupies` stake in G6 is correct and survives. *(L5-7)*
11. **Close G8 by one fixture.** `ring table` is named in §H's own missing list and is absent from the 60-member add list DW-1b's walker will pin at 82 (0 hits in the draft and architecture; 8 in R-INST-5, inside what the dossier calls its best-sourced passage). Either add it and restate as 22 + 61 = 83 with the one-time figure move declared, as AR-7 did for 81 → 82, or record a reasoned refusal. Check `LEDGE` and `WINDOW_SILL_BENCH` from the same line while there. *(L5-6)*
12. **Amend §8.3, and fix "four `high magic` rows".** G4's shelf half is assigned to the CONTENT train only inside §H; the draft's §8.3 scope list — the one surface a content lane reads — is never amended and sits outside the "§5.0–§5.10 stands" range (`8.3` = 0 occurrences in the charter). Separately, `(high magic)` returns **three** rows at the slot (`institutionalCatalog.js:2192, 2201, 2210`); the fourth is `Dragon resident` (`:2167`) with `tags: []` and `magicLicense: 'none'`. R-INST-5's own header says only "FOUR ROWS" — the "high magic" qualifier is the charter's addition. *(L5-10)*
13. **Flag R-INST-6's second self-contradiction.** "Eleven rows get no cell anywhere" is contradicted by its own §Σ.4 table on rows 5, 20 and 21 (a store, a yard, an unlit degree-one cell, a document cell), all three of which also name a host. The charter quotes the sentence in its opening line and flags only the sibling 16-vs-11 as C-8. Its NO_BUILDING contract (ground + fixtures[] + reason, no cells) leaves those cells nowhere to go when DW-5b certifies the rows zero-cell. Make DW-5b's arm read the table's cells column, not the prose. *(L5-8)*
14. **Correct §B's CH-3 row 2 and add the known red.** The plan measures that deleting a redundant `minTier` "cannot change the gate's verdict" — so "which rows are generator-eligible at which tier" is exactly what it *cannot* move — while the two real effects go unrecorded: the key is spread by `assembleInstitutions`, so the record and corpus digest move; and CH-3's **reader fix** will red `tests/generators/metropolisCatalogReachable.test.js`'s ratchet arm. Name that test in the charter as a known CH-3 red DW must not attempt to fix. *(L4-4)*
15. **Restore the missing mutant and fix two citations.** DW-1's producer arm ("no file under `src/` produces `room('stall'`/`room('dais'`") is already green at the base — 0 producers measured — and is the one arm of the five whose convicting mutant is missing from line 527; the architecture's is "add a `room('dais', …)` call anywhere". `CLAIM_RE` is at `enforcement-claims.test.js:**40**`, not `:39`, in a citation the AMENDMENT RECORD certifies as directly re-read. §D's "the §452 **six**" should read **seven** (the draft's §2.4 enumerates seven; 7 + 2 = the 9 the same row pins). *(L2-05, L2-12, L2-08)*
16. **Give D-EXPORT-1 back its second carrier.** ODQ §524.5 assigned the pin to **both** WEB-8b's arm and **DW-6's acceptance**; `D-EXPORT`, `hasDrawableMap`, `townMapPlate` and `SettlementPDF` are all 0 across all three documents, so the deferral silently loses one of two carriers. No mapless-PDF failure follows today (no DW car modifies `src/pdf/**`, and CG-1b took the throw rate to 0 of 504), but if WEB-8b slips the pin has no home. *(M5)*
17. **Cite the AD charter that already exists.** ODQ §526 records `draft-AD-CHARTER.md` at 122,346 B / 1,377 lines, **5 waves / 17 cars**, and names `cartographyPaint.js` as the DW-6 seam; the compile presents AD as still to be chartered and returns 0 for `AD-CHARTER`, `townMapStyles`, `cartographyPaint`. The file is **unreachable at every ref and on disk** — get it into a preserve ref before DW-6a/6c build an unnamed art slot beside a free, landed, geometry-reshaping illustrated lens. Ordering is sound: §514.4 puts AD strictly downstream of DW-S. (The "slot present and empty, fixture rendered through it" observable is not self-contradictory — it is the identity-pass-through pattern.) *(L4-12)*
18. **Editorial residues.** §C.6's tier-2 illustration is §518-era ("hover the merchant's house"); §519.5 replaced it with "a power's holdings" — one-word fix so a skim-reader does not carry the wrong noun *(L4-7)*. §C.7 says "four exotic witnesses"; the draft's §3.3 lists five *(L5-9(b))*. Three namespace hazards worth a note: "C-5" is a substring of "UC-5" (five of seven textual hits are UC-5); three simultaneous D-namespaces run (divergences D1–D5, deferrals D-3/D-6/D-10, R-INST-6's D6-1); and §F.4's EST block reuses `2a`/`2b` ambiguously — inherited verbatim from the draft, not introduced here.

---

## 5. WHAT THE PANEL COULD NOT CHECK

**Nothing was executed against the engine.** No vitest, no `npm run check*`, no worktree, no write. Every runtime claim is read off shipped source.

**Every corpus integer is unverified by execution.** 48/48 and 16/48; 1,098 parcels and 14.8%; 1,056/1,056 and 528/528; 3,168, 4,224, 0.4999, 291.3 and 3,183.6; 183 identical-footprint groups over 369 of 2,839 rows; 81/97/108 of 420; 287/504. Two lenses traced each to the frozen draft with node string counts and every one is present there, and the arithmetic *relations* were confirmed against shipped carve constants (`PARCEL_EDGE_DIVISIONS = 3`, four cut points, three wedges ⇒ 528 × 2 = 1,056; ×3 = 3,168; ×4 = 4,224). **The relations are confirmed; the underlying measurements are not.** Several are PROVISIONAL-ON-CH3 by the charter's own §B in any case.

**Named gaps, each of which could hide a further finding:**

- **CG-2 × CG-1b is unmeasured by anyone.** CG-1b changed the caps; whether 183 / 369 / 2,839 still hold afterwards was checked by no lens and by no prior train.
- **The six R-INST tranches and DWR1A were not walked in full** (2.2 MB combined). Only the citations under audit were resolved. C-11's evidence, B2's 3/28, ~30/124 and 14/40, and the sixteen ProgramMinimum exemplars were opened by nobody — and AR's own closing paragraph says the amendment did not re-derive them either, so they remain **the largest untested body under the charter**.
- **The taxonomy counts were recounted only against the draft's §2 tables**, which are the charter's own restatement of the tranches. 48 partis, 57 cell adds and 60 fixture adds recount cleanly there (and ROOM_KINDS = 28 was verified member-by-member at the slot); a tranche member that never reached §2 is invisible to that method.
- **§E.2's six-surface flag bill was verified only for symbol existence.** `ENGINE_GATED_VIRTUAL_RULE_KEYS` (`simulationRules.js:185`), `subsystemRowsVirtual.js`, `contributionLedgerShape.test.js` and `coveringArrayCoverage.test.js` all exist; the `toHaveLength` literals and the three lockstep figures were not counted.
- **`draft-AD-CHARTER.md` is unreachable** — absent from every ref (full `git for-each-ref` sweep) and from disk — so AD's 17 cars could not be walked for a reverse dependency on DW. L4-12's ordering conclusion rests on §514.4's amended sequence, not on AD's own car graph.
- **The ODQ was read at HEAD `cea5e8a7f`.** Three build lanes are live; §539 and later may already exist and may already correct some of this. Only §452/§453/§494/§495/§498/§514–§526/§538 were read in full — roughly 520 other paragraphs were not, so a directive outside that set could have an owner-with-no-car that nobody would have seen.
- **CH's refutations (H18/H19) were executed at base `f1e4d5150`, not at the DW slot**, and `draft-CATALOG-HYGIENE-PLAN.md` is a PLAN, not a landed packet.
- **CH's own §3.1 blast radius says "the UI catalog at city loses 10 rows" while its table lists 9 city rows plus 1 village row.** DW-0 repeats CH's 10 faithfully. Have CH re-check before DW-R2 hardens on that figure.
- **Absence claims are token greps.** Sound for the exact tokens (and run through node, never ugrep, given the complexity-limit trap that prints to stderr and reads as a clean absence), but a synonym nobody thought to try would not appear.
- **Not audited at all:** §H's defect rows beyond G1–G8; §I's conflicts other than C-2, C-5, C-6, C-8, C-9, C-13; §J beyond the resolution of J-DW0-1..8; and the architecture's per-car convicting mutants for the 17 covered cars. Whether `draft-CATALOG-HYGIENE-PLAN.md` line 5 says what C-13 claims was checked by no one.

**My own judgment on what is most under-examined.** The **DW-3, DW-4, DW-5 and DW-7 waves** got the least scrutiny of anything on this docket — they are inside the 24 cars with no architecture packet (R4), they carry no named mutants, and no lens went deep on them. Everything the panel found in DW-1, DW-2 and ESTATE it found because those cars *had* packets to check against. Treat the quiet in those four waves as absence of evidence.

**Verified clean, and worth recording as such so nobody re-checks it:** J-DW0-8 holds (the charter's `CLAIM_RE` hit count is **0**, re-measured with node against the shipped regex; `docCounts.test.js` pins no doc-file count, so the landing does not red the doc gate). §F.4 contains **no cycle** — every stated edge was walked and no car's prerequisite lands later than the car. The car arithmetic closes (41 + 3 + 1 = 45), so C-6's ruling against the draft's 44 is right; the 47 fails only because it bills a landed CG-1. C-8's "take 11" ruling is correct and needs no change. The §524 export ruling and the $2.99 / `SINGLE_DOSSIER.priceLabel` figures are faithful to the owner's words. G3 and G7 are correctly dispositioned, and the `ARCANE_INST_TAGS` hazard is handed over accurately. `PLAN_UNIT_CM_BY_TIER` (`:99-108`, read at `:286`, validated at `manifestContract.js:216`, consumed at `cartographyBuildings.js:333`), `powerStrata.js:73-82`, `rulingPowerCoup.js:65/:106`, `entitlementLadder.js:135-139`, `simulationRules.js:783`, `InteriorView.jsx:58`, `interiorModel.js:83-86`, `interiorTemplates.js:140/:166`, `JOINT_KINDS` as the closed five, and every document byte-size in §A and C-12 all resolve exactly where the charter says. **C-12 is the strongest-grounded section in the document** — it correctly catches and corrects the chair's own 237-KB mis-homing. Of 99 discrete citations resolved at the slot, **83 were clean**; the 16 defective ones cluster where the charter reasoned from the frozen sources instead of the slot, which is the single pattern this docket is about.