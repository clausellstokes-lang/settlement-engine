| MF-CH2B | [`MF-CH2B`](./packets/catalog-hygiene/MF-CH2B.md) | **READY**; built by lane TE-CH-2, `[OPUS-RUN · FABLE-VALIDATION OWED]`, ODQ §484; the SECOND half of a serial pair — it is built ON TOP of `MF-CH2A` and is minted at that car's landing, because both cars re-record the generator golden and two non-terminal packets may not name one path (§471) | Third car of the catalog-hygiene train, and the car that makes the licence live. All five shelf-reading gates stop deciding a catalog row by its display bucket: **P1** the magic multiplier and **P3** the exotic scaler in `institutionProbability.js`, **P5** the world law in `generationContext.js`, and the **UI** grid in `magicFilter.js` — **P4** costs zero lines here, because `MF-CH2A` routed the canonical detector through the licence and `institutionProbability.js:302` reads it unchanged, which is the "one read" the charter asked for. ⭐⭐ THE LARGEST DECLARED SHIFT IN THE CH TRAIN, PRICED BEFORE THE FIRST EDIT: **1,025 of 2,520** same-seed settlements change their institution roster (504 grid × five magic cases), and it is legible in three numbers — the DEFAULT world moves **3 of 504**; the shift is concentrated in magic-free worlds (356 of 504) and at the dial's extremes; and the collateral has ONE mechanism, that the world law returns BEFORE the `rng.chance` draw at `assembleInstitutions.js:268`, so a row it newly ALLOWS consumes a draw it used to skip and re-sequences the step. ⭐ WHAT A MAGIC-FREE WORLD GETS BACK: `Alchemist quarter` 0 → 92, `Great library` 0 → 84, `Warden's Lodge` 0 → 66, `Alchemist shop` 0 → 9, `Adventurers' charter hall` 23 → 32 — R-INST-5's central finding, that a chemical trade and a repository of books are buildings a world with no magic in it still builds. ⚠ THE ALTERNATIVE IS PRICED, NOT ASSERTED: keeping the unanchored `ARCANE_INST_KW` veto inside the world law (Design B) costs **545 of 2,520** instead of 1,025 — executed, not estimated — and it is refused because it leaves the world law deciding by an unanchored substring, which is the residual `DESIGN_REALM_MAGIC_TOGGLE.md:514-519` recorded as "Chair to schedule" and which this car DISCHARGES. ⭐⭐ AND A SIXTH SURFACE, FOUND BY EXECUTION AND NOT BY THE CHARTER: `generationCoherence.js` walks EVERY string in a finished settlement — including its TAXONOMY fields — and asks `allowsMagicClaim` about each, and `textAssertsFunctionalMagic` is a PROSE detector, so the bare strings `Magic`, `arcane` and `magic` read as CLAIMS. The car's own thesis turned back on it: the moment a magic-free world lawfully keeps a Magic-shelf row, the settlement's own `world_law_magic` certification convicts it FOR THE NAME OF THE SHELF — 2 of 5 mundane members failed before the cure, and Design B does not avoid it because the charter hall enters under B too. Cured inside a file this car already owns: `allowsMagicClaim` no longer convicts a candidate whose ENTIRE text is one token of the closed classification vocabulary, a predicate with exactly ONE consumer in `src/`, so it narrows that certification and nothing else and the golden does not move by it (0 of 525). J-TECH2-10, vetoable. ⭐ G7's literal cure only: three keywords leave `hiMagicInsts` (`magical banking` 0 matches, `enchanting quarter` 0, `magic item consignment` 0 — the last a member of `ARCANE_GOODS`, a goods vocabulary inside an institution gate), and `NON_MAGIC_EXOTICS` dies with the shelf test it patched. The P2 LICENCE form is split out of the train entirely and carried to the chair as J-TECH2-4. ⚠ G4 was REFUTED at row level and the arm re-cut: the grid and the world law ALREADY agreed on every Magic/Exotic row, so what B4 pins is that they STILL agree after both stop reading the shelf — both sides move by 9 rows and the disagreement count stays at 5, all five content-profile denials named. Eight acceptance arms plus B7b for the sixth surface; B1 proves shelf-independence BEHAVIOURALLY over all 311 rows × four gates rather than by grep alone, and B2's grep arm distinguishes a bucket read from an authored-tag read and pins per-file marker counts. No file in this car is an input to any edge bundle, so no `build:edge-shared` is owed. Four ratchets outside the acceptance were paid at the source and every one ATTRIBUTED BY EXECUTION (each green at the MF-CH2A tip): CH-2A's own arm A7 RE-POINTED to the seven licence-`none` rows the world law now admits; the reader-with-no-writer corpus 1300/8607/14586 → 1302/8656/14644 (up, and up is the safe direction here, with the findings inventory PROVED unmoved at the same 159-byte `c5b67844abe51226…`); the mundane realm's RECORDED-CENSUS 134/22/193 → 139/24/195 in both places its own docstring names; and this car's new walker's bare `not.toContain`, anchored at the source. ⚠ The charter's claim that CH-2 DISCHARGES the `DESIGN_REALM_MAGIC_TOGGLE.md:514-519` residual is corrected in that file: the residual's SURFACE shrinks to the 283 unlicensed rows, its ANCHORING does not change, and it stays open. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | the catalog field, the ladder and the reader, which are `MF-CH2A`'s; the P2 `hiMagicInsts` licence form (J-TECH2-4 — it decides what a low-magic city CONTAINS, which is content work); an rng-preserving side stream for the newly-allowed rows, refused because the chair already called that shape a probe rather than a design (§503.4) and here it would need a hard-coded row list; `filterServicesForMagic`, deliberately left on the keyword vocabulary because it is handed service names, no shelf read has ever reached it, and it has zero production callers; `institutionCatalogArcaneTag`; CH-3's data slips; any generation-path write, any flag, any migration, any persisted field, any store verb, any user-visible surface beyond the grid's own filter, any tuning constant, a dependency bump |
