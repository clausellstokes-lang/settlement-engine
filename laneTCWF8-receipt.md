# lane TC-WF8 — VERIFICATION RECEIPT (compile lane; the chair reviews, TE-WF8 executes)

**OUTCOME, FIRST.** WF-8A — the settlement extinction obituary member — is compiled in full at
`<scratchpad>/draft-WF-8.md`, and its central compile result is measured, not reasoned:
**the beat needs ZERO kernel wiring.** The fold's outcomes already flow through
`pulseKernel.js:1237/:1249` into `applyWorldPulse.js:1071`'s generic
`newsEntryForOutcome`, which stamps `impactKind: outcome.candidateType || outcome.type` — so a
fold-local `outcomes.push(faithReceipt(...))` reaches the feed with **none of the four
zero-headroom files touched** (all four re-measured EXACT at this base). The WF-1E
sixth-argument packing precedent the dispatch pointed at is NOT invoked, and that is the
finding: the transport the estate already built makes the net-zero law moot for this member.

- **Compile base:** `claude/composite-r4` @ `27c250f94bb7c5c799693a62985c5ddecd4b6c7c` —
  re-read at compile start and unchanged at every probe (`git log --oneline -1` re-run at the
  measurement window). TE-WF1F is in flight and will move the branch before TE-WF8 dispatches;
  the packet's step-8 STOP covers it (zero shared paths measured — WF-1F's four files appear
  nowhere in WF-8A's manifest).
- **Preamble SHA-256, computed live:**
  `f4cd39fee756c3a192710ab9aa3fc208e18ef1f08ea9e9073bcdc8bfb55d871c` (780 lines) — NOT the
  WF-1D-era `ca02c8a1…` (757 lines); the file was re-stamped by WF-1D's landing (the
  four-member zero-headroom class). The packet instructs the executor to recompute again.
- **Seat compliance:** ODQ §291.5 / §347.1(2). **No git write, no ref move, no repo edit, no
  full gate.** Every probe ran in a throwaway `git archive 27c250f9` tree
  (`<scratchpad>/laneTCWF8-tree`, node_modules symlinked); every plant was restored and proven
  `cmp`-exact against the git blob before the green control ran.

---

## §1 · WHAT WAS EXECUTED (all CONFIRMED; self-named logs in the scratchpad)

1. **Effective lines, eslint's own `Linter`** (`laneTCWF8-measure-base.log`): the four
   zero-headroom files reproduce EXACT — `warTermination.js` 818/818, `pulseKernel.js`
   1581/1581, `applyWorldPulse.js` 941/941, `peaceTerms.js` 797 vs the 800 layer ceiling.
   Manifest files: `religiousContest.js` 470/800 · `heraldRouting.js` 268/800 ·
   `chroniclersLetter.js` 220/800 · `settlementRumors.js` 509/800 · `habitForkRegistry.js`
   245/800. Templates: `informationNews.js` 50 · `informationReceiptPools.js` 13.
2. **The C2 plant probe** (`laneTCWF8-c2-plant.log` / `laneTCWF8-herald-plant.log` /
   `laneTCWF8-c2-green.log`): a planted dead `KIND_SECTION` key reds
   `tests/domain/chroniclersLetter.test.js` C2 (`expected [ 'zz_dead_row' ] to deeply equal
   []`, 1 failed | 24 passed, TRUE_EXIT=1) AND two arms of
   `tests/lint/heraldRouting.walker.test.js` (2 failed | 17 passed, TRUE_EXIT=1). Restore
   `cmp`-exact; green control 25/25 TRUE_EXIT=0. **This is the §321.2a design's conviction
   evidence and the S-1 staleness finding in one.**
3. **The transport chain, link by link:** the seam live and DISCARDED at
   `religiousContest.js:795` (WF-1B's promise verified); `outcomes`/`nameFor`/`unseating`/
   `stablePart`/snapshot names all in scope at the mint point; `newsEntryForOutcome`'s
   candidateType promotion read in source; `curationClass: 'transition'` verified existing
   vocabulary (`pulseHelpers.js:138`); the voice classifier verified EXACT-token (no `faith_*`
   prefix matching) so the candidateType walker's null-voice arm holds unedited.
4. **Registers at base:** census tuple `2492/364/2128/20677/5780` (walker `:5062`);
   kindPoolFloors 112/378/274/divergence 8/small-family `['INFORMATION']`; PACKET_MANIFEST 137
   rows, zero `WF-8*`, path free; `.prose-numerics-baseline.json` 413 rows with **ZERO keying
   any touched file**; all five edge-bundle metas (110/66/111/2/2 inputs) **zero hits** ⇒
   §104.4 NOT INCURRED; `chooserTotality.walker` `CLASSIFIED toEqual DISCOVERED` with
   DEFER_CEILING 30 untouched by a classified-at-landing row.
5. **Validator header parse simulated** on the draft (the §P10.4 / TTS §2.2 obligation):
   heading parsed; statusRows 1 `['DRAFT']`; baseRows 1; verifiedBase
   `27c250f9…`; verifiedBranch `claude/composite-r4`. TRUE_EXIT=0.
6. **CLAIM_RE prescan** with the pattern EXTRACTED from
   `tests/docs/enforcement-claims.test.js` (never transcribed): `draft-WF-8.md` hits=0; this
   receipt re-scanned after writing — see the note at §6.

## §2 · THE MEMBER, IN ONE PARAGRAPH

Four acts: (1) the PRODUCER — one outcome per deleted creed, minted in the fold behind the
hoisted `unseating` read, headline picked by the CURED `hash01` keyed pick over an authored
4-variant pool; (2) the lawful RESURRECTION — `KIND_SECTION['faith_last_altar_dark']:
'traditions'` lands in the same commit as its literal `candidateType:` minter, exactly the
condition §321.2(a) set; (3) the §321.2a MINTER-TOTALITY ARM joins `tests/lint/`
(heraldRouting.walker) with the written-ruling escape (`KIND_SECTION_MINTER_RULINGS`, empty at
mint) and hygiene arms, with C2's substance RELOCATED there (declared re-record; single truth);
(4) the A6 FLIP — comment-only declared re-record at `warTermination.test.js:455-467`, with the
assertions measured UNMOVED (the obituary's key collides with none of A6's absences, so the pin
flips in JUSTIFICATION, not in assertion — stated plainly rather than dressed up).

## §3 · HOW A6 FLIPS LAWFULLY

A6's three absence assertions (`war_termination_read` absent; `patron_fall*` absent;
`pantheon_extinction` present as the anchor) are all STILL TRUE after WF-8A — verified against
the landed test text at `:455-467`. What the resurrection falsifies is A6's COMMENT — "a
KIND_SECTION row would have no minter and nothing would red on it" — whose second clause my
plant probe proved false even at WF-1D's own era. The flip is therefore a comment-only
re-record citing §309/§321.2a in the diff and the packet, with the executor proving the file
green and byte-stable in its assertions pre/post. Census-neutral; title kept.

## §4 · THE MINTER-TOTALITY ARM AND ITS PLANTED-DEFECT CONVICTIONS

Design: the arm lives in `tests/lint/heraldRouting.walker.test.js` (the KIND_SECTION walker's
home — §321.2a's "walker estate" by the letter), ONE `it`: every key minted
(candidateType/impactKind literal scan over `src/domain`, non-vacuity floor) OR ruled
(`KIND_SECTION_MINTER_RULINGS`); no stale ruling (ruled∧minted reds); no orphan ruling; ruling
values spell §-refs; the letter's frozen-six section vocabulary check rides along. The
predicate is a local pure helper with TWO in-suite §75 controls executed every run (a dead key
convicts; a dead-but-ruled key clears). Executor plants: P1 dead key → the arm reds (compile
already executed this shape: the C2/herald convictions above); P2 dead key + ruling → the dead
check CLEARS (the escape's proof); P3 ruling on `pantheon_extinction` → the stale-ruling arm
reds alone. C2's block in `tests/domain/chroniclersLetter.test.js` is removed with a pointer in
the same commit — the relocation that keeps one truth in one home.

## §5 · LINE BUDGET AND THE KERNEL ACCOUNTING (measured basis)

- Production: faithNews ≤80 + faithReceiptPools ≤40 + fold ≤14 + three registration rows
  (+1/+~5-with-rulings-map/+1) + habitForkRegistry +~3 ⇒ **~143 effective against the 400
  cap**, every target file's headroom measured in §1.1.
- Kernel wiring: **ZERO.** The four zero-headroom files are non-goals with their EXACT figures
  restated; the packet's §P7.8 STOP still binds the executor to before/after readings on both
  instruments for every file it DOES touch.
- Caps: two override rows are REQUIRED before dispatch — handwritten 12→17 (16 counted M-rows
  + the packet under the stricter accounting) and registration-only 3→4 (habitForkRegistry,
  the §85.4a estate row). Both enumerated per file in packet §6.2/§6.3; both are standing-law
  obligations rather than design acts; droppable rows only shrink the count.

## §6 · STALENESS FINDINGS (each measured; none silently absorbed)

- **S-1** ⛔⛔ TC-WF1D §4.1's "a dead row would have sat in a frozen table with every walker
  green" is **REFUTED BY EXECUTION** — C2 (`tests/domain/chroniclersLetter.test.js`, since
  `1477c284`) reds it, and heraldRouting.walker reds it twice more. §321.2(a)'s ordered arm is
  therefore PART-BUILT; the genuine gap is the ruling escape + hygiene + tests/lint residence,
  which is exactly what WF-8A lands. RAISED R-2 gives the chair the veto on the discharge
  shape.
- **S-2** The seed's §85.4 pricing named both obligations but its manifest omitted their FILES
  (habitForkRegistry.js, mechanism-lit-coverage-baseline.json) — IN-1C-A's "the member
  actually cost FIFTEEN" lesson applied; both are M-rows now, and they are what push the two
  overrides.
- **S-3** The seed's six `.prose-numerics-baseline.json` re-address rows are GONE at
  `27c250f9` (413 rows, zero keying any touched file) — obligation NOT INCURRED, recorded so
  the executor does not hunt for it.
- **S-4** The seed's N10 (`impactKindWalkers` EXPECTED_VOICE) is NOT INCURRED under the
  candidateType mint idiom — that walker's own header routes promoted candidateTypes to
  `tests/domain/candidateTypeVoicePhrasing.walker.test.js`, whose two arms this member
  satisfies with zero edits (voice classifier verified exact-token).
- **S-5** The seed's WF-1B-I dependency/reservation clauses are DISCHARGED (WF-1B landed as a
  single member; the seam verified live and unconsumed).

## §7 · RAISED (chair rules before TE-WF8 dispatches; packet §7 carries the full rows)

1. **R-1 — the second-small-family review is INCURRED at this member** (§309.3's economics do
   not survive §347's obituary-first sequencing). Recommended: take it; shrink-back recorded
   against the next WF-8 member; alternatives priced.
2. **R-2 — the §321.2a discharge shape** (relocation + escape, recommended) vs a tests/lint
   twin (duplicate truth with a foreclosed escape, priced, not recommended).
3. **R-3 — the ONE authored value**, `LAST_ALTAR_SEVERITY: 0.5`, asks for its §42/§43
   signature at this review; derivation stated (the fold's no-record conversion default;
   deliberately below the 0.72 major-entry line — kind cadence stays major, pool floor 4).
4. **R-4 — naming** (`WF-8A`, stage 1 of the multi-member WF-8 train; `faithNarrationEnabled`
   untouched, still lights LAST).

## §8 · JUDGMENT CALLS (vetoable; full grounds in packet §8)

J-TCWF8-1 outcome transport over direct news mint (the zero-zero-headroom result) ·
J-TCWF8-2 `candidateType` literal at the mint · J-TCWF8-3 pre-prune authored-name capture with
`deityDisplayNameFromRef` fallback · J-TCWF8-4 C2 relocation · J-TCWF8-5 no dormancy slot ⇒
`religionState.js` untouched · J-TCWF8-6 the cured `hash01` pick copied verbatim, the nine
stale sites left to `CR-IN1C-DRIFT`.

## §9 · WHAT THIS LANE DID NOT DO, AFFIRMATIVELY

- No git write, no ref move, no repo edit, no full gate, no typecheck-ratchet runs (the
  executor's windows), no mutant planted in the LIVE tree (all plants in the archive tree,
  restored `cmp`-exact).
- The acceptance fixtures were NOT run to their pins — the seed's executed bar figures
  (prune at 4, per-creed multiplicity, the 5-tick organic flip, the `nicheOf` hand-authoring
  vacuity) are carried as fixture-first STOPs, and the packet orders the print-before-pin run
  at the executor's own base.
- `deriveDecisionTier`/`collectFaithDeltas` tolerance of the new outcome type was traced in
  source but NOT executed — named as explicit print-and-verify steps in B7's fixture-first
  run, not assumed.
- The WF-1B-era mutant (g) (the off-by-one prune bar) is deliberately NOT taken — another
  member's landed branch, under its own battery; recorded in packet §6.5.
- ⚠ One self-caught process note: this lane's first measurement invocation piped through
  `tee` and the harness echoed TRUE_EXIT=0 over a crashed node — the recorded wrapper hazard,
  reproduced and corrected in-lane (all subsequent exits captured in-shell, logs self-named).

## §10 · HELPER INVENTORY (all under `laneTCWF8-*`, left for chair verification)

`laneTCWF8-tree/` (pristine `git archive 27c250f9`, node_modules symlink; C2 plant restored
`cmp`-exact) · `laneTCWF8-measure.mjs` (+ the in-tree copy) · `laneTCWF8-measure-base.log` ·
`laneTCWF8-c2-plant.log` · `laneTCWF8-herald-plant.log` · `laneTCWF8-c2-green.log` ·
`laneTCWF8-chroniclersLetter.PRISTINE.js` · `laneTCWF8-ODQ-snapshot.md` (ledger ODQ at
`0ab3d655`) · `laneTCWF8-src-WF-PREAMBLE.md` / `laneTCWF8-src-WF-1B.md` /
`laneTCWF8-src-WF-1D.md` / `laneTCWF8-src-WF-1E.md` / `laneTCWF8-src-DESIGN_FP_ARCH_WF.md`
(binding docs extracted at the base). CLAIM_RE: draft hits=0 (executed); this receipt scanned
after write — the chair may re-run `laneTCWF8-tree` extraction one-liner to confirm hits=0.
