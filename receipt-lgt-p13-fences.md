# RECEIPT — LGT-P13-FENCES (L-HOMES CAR 3) — ✅ COMPLETE

_(This file was written as a PARTIAL header before the first edit and updated after every proof, per the preamble.)_

**Lane** LGT-P13-FENCES · **Seat** Opus 5 — Fable-unvalidated · **Chair** Fable 5.1 · **Date** 2026-09-05
**Dock** `$SC/laneLH3`, detached at `38474a59e` — verified on arrival: HEAD
`38474a59eba460f30d6596dcb65efda3a446738a` (== `refs/preserve/train-composed-2026-09-05` ==
`refs/preserve/landing-composed-2026-09-05`), `git status --porcelain` = 0 lines,
`node_modules/*` symlinked to `/Users/cstokes/Desktop/settlement-engine/node_modules/*` (spot-checked
`vitest`, `react`). No rebase, no ref write, no register act, no `src/` byte touched.

## STATUS
Four fence files authored, **green (34/34)**, and **committed** — dock tip
`1506d657d13dd979e7fa1866977fca1259b771e9`. eslint 0 · `typecheck:domain:strict` 0.
Plant-out control executed: **all four fences RED under their own mutants (10/34), restored byte-exact
by `cmp`, and GREEN again (34/34)**. `tests/lint/` WHOLE: **1 failed / 138 passed**, and the one
failure is the predicted lighting-census `files` figure moving by exactly +4. **THE CAR IS COMPLETE;
the register acts are the chair's.**

---

## 1 · PREMISE RE-DERIVATION — THREE BRIEF FIGURES REFUTED BY MEASUREMENT

The brief and `PLAN.md` §6 POSITION 2 both state the derivation as *"the five existing
`*DormancyFence` files under `tests/property/`"* and *"twelve keys"*. All three load-bearing figures
are wrong at this tip, and the corrected derivation yields **five keys in four files**, not twelve.

### 1.1 ⛔ "the FIVE existing fence files" — measured: **NINETEEN**

```
$ ls tests/property/*DormancyFence.test.js | wc -l
19
```
advanceEpoch · believedWorldAxes · casusCommercii · chanceEncounters · errandSpine ·
espionageDoctrine · espionage · espionageGauntlet · espionageMission · espionageProducts ·
espionageRider · faithField · oathHolder · pactFormation · secondOrderBelief · secrecyTrade ·
sovereigntyTrade · strategicPosture · treatyLifecycleVoice.

### 1.2 ⛔ the docket's named twelve contains **THREE KEYS THAT ARE NOT IN THE REGISTER**

`pending/LIGHTING.json`'s `LGT-P13-FENCES.size` names *"Eleven names / TWELVE keys: conquestDoctrine,
faithUnseating, habitConditioning, treatyRenewal, undercityHighWater, underwaysOrganicFounding,
warCirculation/contributionLedger, warMemory, lineageClaim, coalitionLedger, magicEconomy"*.

Measured against the live register (`ENGINE_GATED_VIRTUAL_RULE_KEYS`, **30 members** — the brief's
"30" CONFIRMED, enumerated by importing the module rather than by regex):

- **`lineageClaimEnabled`, `coalitionLedgerEnabled`, `magicEconomyEnabled` are NOT members.** Zero
  hits each in the register.
- and the docket omits **five keys that ARE members** and have no `*DormancyFence` file:
  `migrationRumorsEnabled`, `settlementPoliticsEnabled`, `treasuryEnabled`, `foreignSeatEnabled`,
  `legitimacyUpheavalEnabled`.

Applying the brief's own rule literally — *register minus keys with a `*DormancyFence` arm* — gives
**FOURTEEN**, not twelve.

### 1.3 ⛔ NINE of those fourteen already carry an equivalent instrument, one of them by CHAIR RULING

The car exists so that *"the dark claim is provable AFTER lighting"*. The testable property is:
**some test constructs the dark state EXPLICITLY (a literal `false`, an absent key, or a named DARK
constant — never inherited from the default), asserts a bit-level dark claim, and pairs it with a lit
control on the same fixture.** A test built that way survives lighting; a test that relied on the
default being dark does not. Measured file-by-file:

| key | its dark proof today | verdict |
|---|---|---|
| `habitConditioningEnabled` | `tests/property/habitNeutralIdentity.test.js` — *"the four-fence dormancy set, and the lit-mutant control"*; `:114` "THE GATE-POLARITY CENSUS drops each conjunct one at a time, with the LIT MUTANT control" | **fenced** |
| `migrationRumorsEnabled` | `tests/property/migrationRumorsDormancyGolden.test.js` — dormancy proof + LIT WALKTHROUGH | **fenced** |
| `settlementPoliticsEnabled` | `tests/property/settlementPoliticsDormancyGolden.test.js` — dormancy golden + "THE LIT-PATH ANTI-VACUITY" | **fenced** |
| `treasuryEnabled` | `tests/domain/treasuryDormancy.byteIdentity.test.js` — raw-byte identity, ANTI-VACUITY arms, truthy-non-true census | **fenced** |
| `foreignSeatEnabled` | `tests/domain/foreignSeatDormancy.byteIdentity.test.js` — same family | **fenced** |
| `legitimacyUpheavalEnabled` | `tests/domain/legitimacyUpheavalDormancy.byteIdentity.test.js` — same family | **fenced** |
| `warMemoryEnabled` | `tests/domain/concludedWarsWriter.test.js:49–75` — "DARK MEANS BYTE-IDENTICAL", "ANTI-VACUITY CONTROL: the SAME comparator sees the lit run diverge", plus the truthy census at `:52` | **fenced** |
| `undercityHighWaterEnabled` | `tests/domain/undercityColonization.test.js` A6 — absent ≡ false byte identity through the real `ensureFactionStates` seam, dark projection identity, anti-vacuity lit control, monotone fold, null-never-zero | **fenced** (see §1.4) |
| `treatyRenewalEnabled` | `tests/domain/treatyRenewalMemory.test.js` | ⛔ **fenced AND chair-ruled** |

⛔⛔ **`treatyRenewalEnabled` IS A STOP, NOT A GAP.** Its file's header carries a landed chair
judgment that forbids exactly what this brief would have had me build:

> `tests/domain/treatyRenewalMemory.test.js:12–15` — *"THE FOUR-FENCE DORMANCY SET LIVES HERE AS
> CASES (chair judgment J-TC11-2), not in a separate `tests/property/` file: a second credited file
> would break both the standing GR shape and the eight-case denominator. Fence 1 own-footprint
> golden (A2), fence 2 absent-vs-false differential (A3), fence 3 call-path spy (A4), fence 4
> gate-polarity census (A5)."*

I did not build it, and I did not soften the ruling. **The Fable chair owns any reversal.**

### 1.4 The one judgment call inside the derivation (chair-vetoable)

`undercityHighWaterEnabled`'s A6 arm is complete except for the **truthy-non-true probe** (it drives
`absent`, `false`, `true`, never `1`/`'true'`). I classed it FENCED and did **not** mint a new census
file for one missing probe: a new `tests/property/` file costs the lighting census `files`, `titles`
and `suiteTitles` EXACTLY, and duplicating a strong instrument to add one loop is the "elaborate way
of saying nothing" shape. **If the chair wants uniformity, the cheaper cure is three lines added to
A6, not a file.** Same reasoning would apply to any other PASS key the chair wants hardened.

### 1.5 ⇒ THE CORRECTED DERIVATION: **FIVE KEYS, FOUR FILES**

| # | key | why it was genuinely unfenced | new file |
|---|---|---|---|
| 1 | `underwaysOrganicFoundingEnabled` | **the real gate was never executed in both states.** Its only lit/dark pair, `tests/domain/worldPulseLitBurndown.test.js:157–166`, RE-IMPLEMENTS the gate in the test (`const litFlag = rules.underwaysOrganicFoundingEnabled === true;` then passes `underwaysFoundingLit` straight to `detectInstitutionGaps`). The engine's own read at `institutionLifecycle.js:672` was executed by nothing. | `underwaysOrganicFoundingDormancyFence.test.js` |
| 2 | `warCirculationEnabled` | gate arms exist in `contributionLedgerShape.test.js` but **no bit claim anywhere**: nothing asserted that lighting the lane moves no world byte | `warCirculationDormancyFence.test.js` |
| 3 | `contributionLedgerEnabled` | same gate, same conjunction — one file, because splitting gives each key half a conjunction and neither the cell where the other is dark | (same file) |
| 4 | `conquestDoctrineEnabled` | the LAYER gate is fenced (`vengeanceLicenseWr8.test.js:106` "DARK MINTS NOTHING, and dormancy is the same reference"), but the **COMPOSED gate is an EIGHT-flag conjunction and nothing ever dropped its conjuncts one at a time** — the shape in which a deleted guard hides behind a surviving one | `conquestDoctrineDormancyFence.test.js` |
| 5 | `faithUnseatingEnabled` | explicit dark/lit pairs exist in four files, but **no SITE CENSUS** over its four `=== true` reads (`pulseKernel:1637`, `warTermination:538`, `religionState:649`, `religiousContest:692`) and **no truthy-non-true probe anywhere** | `faithUnseatingDormancyFence.test.js` |

### 1.6 A REFUTED CERTIFICATION ROW, found in passing

`src/domain/certification/subsystemRowsCompact.js:420` claims the underways layer is *"pinned where
its bodies are readable instead: `tests/domain/institutionLifecycle.test.js`, where a dark evaluate
is compared against a lit one with the clandestine gap present."*

```
$ grep -c "underways\|clandestine" tests/domain/institutionLifecycle.test.js
0
```
**The named address carries zero hits.** The real (and gate-re-implementing) pin is in
`worldPulseLitBurndown.test.js`. Reported, not cured — a certification-row edit is a `src/` act this
car has no mandate for.

---

## 2 · WHAT WAS BUILT

Four new files under `tests/property/`, no `src/` byte touched, no fixture added, no register acted on.

1. **`tests/property/underwaysOrganicFoundingDormancyFence.test.js`** — 4 suites / 6 tests.
   Drives the REAL `evaluateInstitutionLifecycle` past the build gate on a vice-bearing town with
   NOTHING else to build, so the single candidate the lit arm produces can only have come through
   the clandestine door. Both raw-rules receivers exercised (`context.simulationRules` **and**
   `worldState.simulationRules`). Measured before authoring: dark (absent/false/`1`/`'true'`) → 0
   candidates; lit → 1 × `institution_build` "Underground network".
2. **`tests/property/warCirculationDormancyFence.test.js`** — 4 suites / 10 tests.
   The two-flag conjunction census (5×5 grid + the half-lit rows + non-object bags), then the
   **wave-identity bit claim**: six weekly ticks of the real pulse, both flags LIT, byte-identical to
   both dark with the rules object set aside — anchored by a sibling flag (`treasuryEnabled`) driven
   through the identical harness and shown to move the world. Fence 3 gives the structural reason:
   `contributionLedger.js` has **zero importers in `src/`** (measured) and makes zero
   `setSpatialLedger` calls, each with a guard-the-guard. ⭐ **Fence 2 is a declared HANDOFF
   TRIPWIRE: it REDS the day WC-1 lands the writer, and that red is the signal.**
3. **`tests/property/conquestDoctrineDormancyFence.test.js`** — 4 suites / 9 tests.
   The eight-conjunct census, each conjunct dropped absent **and** in all eight not-true spellings;
   a closed-vocabulary pin transcribing the ruling's eight names rather than reading them off the
   constant they guard (the self-referential-pin class); the layer gate's own strict census; and the
   mint's **same-reference** dormancy plus a real lit mint (holders `['greyford','stonemere']`,
   CR-WR8-A refusing the stranger) hashed against the dark ledger.
4. **`tests/property/faithUnseatingDormancyFence.test.js`** — 4 suites / 9 tests.
   The projection gate driven over seven dark spellings incl. `null`/`1`/`'true'`/`{}`/`[]`; the
   **flag-and-ring** law driven in both directions (a FULL ring under a dark flag projects nothing;
   an EMPTY ring under a lit flag projects nothing); and the four-site source census with a
   sibling-key guard-the-guard, whose red message EXPOSES ("a fifth file is either a new gate site
   owing a roster entry in the same commit, or prose that should be reworded") rather than judges.

### Design rules obeyed in all four, and why
- ⛔ **No 64-hex string literal anywhere.** `tests/lint/goldenFreeze.walker.test.js:222` builds
  `CONSTANT_CARRIERS` from `tests/property/` files whose source pins a 64-hex literal, and `:481`
  reds any such file with no `.golden-freeze-register.json` row; `:358` reds a recorded value while
  the register is UNFROZEN. Every hash here is computed in-file from two live runs, so **these four
  files owe the golden-freeze register NOTHING — neither a row nor a written exclusion** (the
  exclusion roster is keyed on env spellings, and these files read no `process.env`). See §3.
- ⛔ **No `not.toContain` / `not.toMatch` / `not.toHaveProperty`** except through
  `tests/helpers/anchoredNegatives.js`: `negativeAssertionAnchor.walker.test.js` holds
  `tests/property/` at **EXACT zero** un-anchored sites.
- ⛔ **Every flag drive spelled as a LITERAL**, never a computed member.
- ⛔ **Every rules object is built in the file**, so no arm inherits a default — each file's FENCE 4
  is the pin that says so, which is the whole point of the car.

---

## 3 · PREDICTED REGISTER DELTAS (recorded BEFORE any instrument ran; every register act is the chair's)

Base tuple, `tests/lint/.lighting-census-baseline.json` at `38474a59e` (`measuredAtSha`
`35b027458`, `measuredBy` "Fable 5.1 chair — §899 composed landing"):
`files 2523 · parked 371 · credited 2152 · titles 23204 · suiteTitles 6217`.

| register | predicted delta | predicted after | derivation |
|---|---|---|---|
| **lighting census** `files` | **+4** | **2527** ⭐ **MEASURED — the walker itself reports `expected 2527 to be 2523`** | four new `tests/` files |
| **lighting census** `parked` | **0** | **371** | all four import from `'vitest'` and bind `describe`/`test` normally — credited, not parked |
| **lighting census** `credited` | **+4** | **2156** | files − parked |
| **lighting census** `titles` | **+34** | **23238** | 6 + 10 + 9 + 9, counted by `grep -cE "^\s*(test\|it)\("` per file |
| **lighting census** `suiteTitles` | **+16** | **6233** | 4 + 4 + 4 + 4 top-level `describe(` |
| **test ratchet** `totalFiles` | **+4** | (refreeze, never a breach) | `SCOPE_FLOOR_RATIO = 0.9` — a 90 % COLLAPSE FLOOR |
| **test ratchet** `totalTests` | **+34** | (refreeze, never a breach) | same floor |
| **known-failure census** `entries` | **0** | **6 of 17** | every arm lands green or the car does not land; `--update` can only REMOVE |
| ⭐ **golden-freeze register** | **0 rows, 0 exclusions** | 48 surfaces / 43 env carriers / 3 constant carriers, unchanged | **measured, not assumed**: the only three fences enrolled today (`espionageDormancyFence`, `espionageMissionDormancyFence`, `espionageRiderDormancyFence`) are enrolled *because they pin 64-hex constants* (`proofForm: "in-file corpus constant"`, `constantSites` 1 / 11 / 11). The other sixteen fence files carry none and are not enrolled. These four carry none and read no golden-adjacent `process.env` spelling ⇒ they enter neither `ENV_CARRIERS` nor `CONSTANT_CARRIERS`. **The brief's "twelve new fence files is exactly the class this walker exists for" is REFUTED: the class is 64-hex constants, not fence files.** |
| **mechanism lit-coverage** | **0** | baseline unchanged (23 mechanisms / 1 flag) | `NON_LIT_RE = /(dormanc\|byteidentity)/i` (`mechanismLitCoverage.test.js:67`) excludes every `*Dormancy*` filename from `litEligible`, so no literal `<flag>: true` in these files grants or moves credit on either axis |
| **`sizeBaseline`** | **0** | unchanged | no `src/` byte touched; `subsystemRowsVirtual.js` untouched at its 800-line ceiling |
| **writer-reach / OSR / voice / prose-numerics / tuning** | **0** | unchanged | no `src/` byte, no prose string, no tuning value, no observed-shape reader |
| **engine-gated walker backlog** | **0** | 17 at ceiling 17 | no key added to or removed from the register |
| ⭐ **EAGER BYTES** | **0 B** | — | **why:** every byte of this car is under `tests/`. `vite` builds from `src/` + `index.html`; no `src/` module was created, edited, renamed or deleted, no import edge moved, and no `src/` module gained or lost a consumer. There is no path by which a `tests/` file enters a hashed chunk. The listing diff remains the chair's proof, not this sentence. |

---

## 4 · PROOFS EXECUTED — every exit captured in-shell (`CMD; E=$?`), never read off a notification

| # | gate | exit | result, quoted |
|---|---|---|---|
| P1 | `npx eslint <4 new files>` | **0** | no output |
| P2 | `npm run typecheck:domain:strict` | **0** | `[domain-strict] ✓ no strict-type regressions (1120 errors, ceiling 1120).` — unchanged, as predicted (no `src/` byte) |
| P3 | `sh scripts/gate-mutex.sh --run -- npx vitest run <4 new files>` | **0** | `Test Files 4 passed (4) · Tests 34 passed (34)` — and the **34** independently CONFIRMS the predicted `titles` delta |
| P4 | **plant-out, RED arm** — four `src/` mutations planted at once, one per fence | **1** (required) | `Test Files 4 failed (4) · Tests 10 failed \| 24 passed (34)` — see §4.1 |
| P5 | **plant-out, RESTORE** — inverse edit, then `cmp` against backups taken BEFORE the plant | **0** | `CMP OK institutionLifecycle.js / contributionLedger.js / conquestDoctrineStage.js / religionState.js`, `CMP_STATUS=0`; `git status --porcelain` empty |
| P6 | plant-out, GREEN arm (the same four files, restored tree) | **0** | `Test Files 4 passed (4) · Tests 34 passed (34)` |
| P7 | `sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/` (WHOLE dir, at the dock tip; the runner REFUSES a dirty tree before it measures) | **1** | `Test Files 1 failed \| 138 passed (139) · Tests 1 failed \| 2145 passed (2146)`. **The failing-arm list is exactly one arm, and it is the predicted register act — see §4.3.** |

### 4.3 The one `tests/lint/` red, attributed without a base re-run

```
FAIL tests/lint/sovereigntyLightingContract.walker.test.js
  > THE CENSUS IS AN ASSERTION, NOT A SENTENCE — every stated figure is executed
AssertionError: the estate's file count moved — re-measure, do not re-word:
  expected 2527 to be 2523
```
⚠ **vitest prints ACTUAL first**: the tree measures **2527**, the frozen register says 2523. The
delta is **+4 — exactly this car's four files, and nothing else.** That equality IS the attribution:
had the base been anything other than green on this figure, the move would not have landed on 4. An
independent count agrees: `find tests -type f | grep -E '\.test\.(js|jsx)$' | wc -l` = **2527**.

⛔ **THE RECORDED HAZARD BIT, AS PREDICTED, AND IT MATTERS TO THE CHAIR.** The walker asserts `files`
FIRST and throws (`:7445–7446`), so `parked`, `credited`, `titles` and `suiteTitles` were **never
evaluated in this run** — their predicted values in §3 remain this seat's arithmetic, corroborated
only indirectly (`titles +34` by P3's `Tests 34 passed`; `suiteTitles +16` by a grep count). Only
`files` is measured. **The refreeze writes all five or none; do not read this red as a one-figure
move.**

The other 138 files / 2145 tests of the scanner family are GREEN at the dock tip — reported as owed,
even though green.

Every vitest run went through `scripts/gate-mutex.sh --run` and waited out a sibling lane's
`tests/lint/` gate (laneLH2, ~17 min) rather than running beside it — the quiet-window law. Nothing
was ever run during another lane's quiet-wait.

### 4.1 The plant-out control — each fence proven able to RED, by its own mutant

Every plant asserts its anchor occurs **exactly once** and re-reads the mutated VALUE, not just the
anchor match (the recorded method note: an inert plant reds for the wrong reason). No
`git checkout --`, no `git show HEAD:<path> >`, no `git stash` — restoration is an inverse edit
verified by `cmp` against a pre-plant `cp` backup.

| plant (in `src/`) | mutant | what reddened |
|---|---|---|
| `institutionLifecycle.js` | the gate's `=== true` loosened to `== true` | underways FENCE 1 (both receivers) + FENCE 2 — the truthy-non-true arm caught `1 == true` |
| `contributionLedger.js` | the two-flag conjunction turned into a disjunction | warCirculation FENCE 1 — the half-lit cells |
| `conquestDoctrineStage.js` | one conjunct of `CONQUEST_REQUIRED_RULES` renamed | conquest FENCE 1 — **both** the all-eight-true lit-mutant control and the closed-vocabulary pin, which is exactly the pair the self-referential-pin class needs |
| `religionState.js` | the strict read dropped to a truthy read | faith FENCE 1 (both arms) **and** FENCE 3's site census, which noticed the spelling had left the module |

10 of 34 red under the plants; 34 of 34 green once restored. **No fence is a green about nothing.**

### 4.2 One finding produced by the fences' own harness, worth recording

Driving the underways rules through the **`worldState` receiver** rather than the `context` receiver
makes the four dark footprints DIFFER from one another, while through the `context` receiver they are
identical. That is not a leak: it is the CR-WR10-C virtual-key byte cost made visible — a world that
persists `{ underwaysOrganicFoundingEnabled: false }` carries a key a world persisting `{}` does not,
which is the whole reason the key is virtual. The fence's byte-identity arm therefore uses the
`context` receiver and the `worldState` receiver carries only the candidate-emission arm. Stated here
because a later lane widening the fence would otherwise rediscover it as a bug.

---

## 5 · RETROVALIDATION ROW

**What this Opus seat JUDGED (and the Fable chair must re-derive):**

1. ⭐⭐ **THE CAR'S SCOPE WAS CUT FROM TWELVE KEYS TO FIVE, ON MEASUREMENT.** The chair must
   re-derive §1: (a) there are **19** fence files, not five; (b) three of the docket's twelve keys
   (`lineageClaim`, `coalitionLedger`, `magicEconomy`) **are not in the register at all**; (c) nine
   of the fourteen keys the brief's own rule selects already carry a dark-half-plus-lit-control
   instrument. Receipts: `tests/property/*DormancyFence.test.js` (19 files),
   `ENGINE_GATED_VIRTUAL_RULE_KEYS` (30 members, enumerated by import), and the nine addresses
   tabled in §1.3. **Priority: HIGHEST — it is the whole shape of the car.**
2. ⛔ **A LANDED CHAIR JUDGMENT WAS OBEYED, NOT SOFTENED.** `treatyRenewalEnabled` was NOT given a
   `tests/property/` file, because `tests/domain/treatyRenewalMemory.test.js:12–15` records chair
   judgment **J-TC11-2** forbidding exactly that. If the Fable chair now wants the fence family
   uniform, that is a reversal of a landed ruling and belongs to the chair, not to a lane.
   **Priority: HIGH.**
3. ⭐ **THE GOLDEN-FREEZE OBLIGATION THE BRIEF ASSERTED DOES NOT EXIST FOR THESE FILES.** The brief
   states "twelve new fence files is exactly the class this walker exists for". Measured: the class
   is **64-hex string literals**, not fence files — the only three enrolled fences carry
   `proofForm: "in-file corpus constant"` with `constantSites` 1/11/11, and the other sixteen fence
   files are absent from the register. These four author none and read no golden-adjacent
   `process.env` spelling, so they owe **neither a row nor a written exclusion**. The chair should
   re-derive this before the composition, since it changes what the landing owes.
   **Priority: HIGH.**
4. **The one discretionary call inside the derivation** (§1.4): `undercityHighWaterEnabled` was
   classed FENCED although its A6 arm lacks the truthy-non-true probe. I judged three lines added to
   A6 cheaper and more honest than a new census file. **Priority: MEDIUM — a chair may simply say
   "add the file" and it is one commit.**
5. **A refuted certification row** (§1.6): `subsystemRowsCompact.js` cites
   `tests/domain/institutionLifecycle.test.js` as the underways dark-vs-lit pin; that file has zero
   hits for either token. Reported, not cured — the fix is a `src/` prose edit this car has no
   mandate for. **Priority: MEDIUM.**
6. **Every predicted register figure in §3** is this seat's arithmetic, not an executed refreeze. The
   `titles +34` half is now corroborated by P3's `Tests 34 passed`; `files +4` and `suiteTitles +16`
   are counted, not measured by the walker. **The chair takes every register act.**

**Receipts by path:** this file · `$SC/laneLH3-scratch/fences.out` (P3) ·
`$SC/laneLH3-scratch/plants-red.out` (P4) · `$SC/laneLH3-scratch/plant-restore.log` (P5) ·
`$SC/laneLH3-scratch/plants-green.out` (P6) · `$SC/laneLH3-scratch/lint.out` (P7) ·
`$SC/laneLH3-scratch/typecheck.out` (P2) · `$SC/laneLH3-scratch/plants.py` (the plant definitions) ·
`$SC/laneLH3-scratch/backups/` (the pre-plant `cp` backups the `cmp` ran against).

**Commits (dock `$SC/laneLH3`, detached, never pushed, never rebased):**

| step | sha | file |
|---|---|---|
| 1 | `93d76e022` | `tests/property/underwaysOrganicFoundingDormancyFence.test.js` |
| 2 | `d518da6ff` | `tests/property/warCirculationDormancyFence.test.js` |
| 3 | `c9b0330b8` | `tests/property/conquestDoctrineDormancyFence.test.js` |
| 4 | `1506d657d` | `tests/property/faithUnseatingDormancyFence.test.js` |

**DOCK TIP: `1506d657d13dd979e7fa1866977fca1259b771e9`** (base `38474a59e`, four commits, porcelain 0).
