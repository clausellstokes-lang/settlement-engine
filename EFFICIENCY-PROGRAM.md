# THE EFFICIENCY PROGRAM — the owner's "Do all of this" (2026-09-20 ~16:0x EDT, session 4a1823e2; ODQ §934.47 addendum 118)

The owner asked "Is there anything else to make all of this more efficient without dropping quality?", the chair listed twelve measures, and the owner answered "Do all of this". THE QUALITY FLOOR accepted with the list: the full check at the composed tip · red-first proofs · one cause per moved golden fixture · walkers and ratchets at full strength · no byte ceiling raised for convenience · the handoff kit kept current.

| # | measure | state | where |
|---|---|---|---|
| 1 | Edit-Mode packets that cannot collide build in PARALLEL worktrees under their own seals and land at one train terminal | SURVEY RUNNING (PACKET-PARALLEL-SURVEY, Opus, read-only) → the chair rules → a standard amendment + a tooling lane for the seal's branch binding | `lane-packet-parallel-scratch/PACKET-PARALLEL.report.md` |
| 2 | TOOL-26 — a shrink-only roster of every test file that reads `src/` by path, and the governed directories per `src/` area — FRONT of the tooling queue | CHARTERED (addendum 117); brief + dispatch NEXT free seat; rides batch B2 | `briefs/launch/TOOL-26.md` (to write) |
| 3 | The owner's deploy runsheet + the one-time `VERCEL_DEPLOY_HOOK_URL` secret | WRITTEN; goes to the owner with the PR link | `OWNER-RUNSHEET-DEPLOY-2026-09-20.md` |
| 4 | The chair's full-check runner: independent stages side by side, the test ratchet alone, no stage blinded by an earlier red | WRITTEN, syntax-checked; FIRST USE = batch B1's one full check (compare its wall-clock with RUN 24's ~20 min) | `tools/chair-check.sh` |
| 5 | Batch cap 6 → 8 where the forecast gives an attribution key; composers run each member's governing tests as focused proofs | ADOPTED in the batch ledger; the composer brief gains the governing-tests step when TOOL-26 lands (until then: `git grep -l -F <basename> -- tests`) | `BATCH-LEDGER.md`, `briefs/BATCH-COMPOSER.md` |
| 6 | An Opus VERIFIER seat per batch reviews the batch diff adversarially while the full check runs | BRIEF TO WRITE; first use on B1 | `briefs/BATCH-VERIFIER.md` (to write) |
| 7 | No literal golden or census pins in lane notes — compare to the freeze register, read the census live | LAW in the consolidated lane brief (item 11); every hand-off until then carries the live pins from QUEUE-RECON | `briefs/LANE-PARALLEL.md` |
| 8 | MEASURE moving `tests/lint` whole from each sealed packet build to its train terminal; bring the measurement to the owner BEFORE amending | RIDES in item 1's survey (its ITEM 8) | same report |
| 9 | A standing drift forecast at every tip move | WRITTEN and run once; TO ADD: a composed-by-patch-id filter and an ignore list for finished worktrees | `tools/drift-forecast.sh` |
| 10 | One scripted gate acquisition per window (all focused runs in one script; no agent blocked on the lock) | LAW in the consolidated lane brief (item 11); already the composer brief's practice | `briefs/LANE-PARALLEL.md` |
| 11 | LANE-PARALLEL consolidated law-for-law into one deduplicated brief, an Opus law census proving nothing was dropped | TO DO during the next full check | `briefs/LANE-PARALLEL.md` → v2 |
| 12 | SURVEY-ONLY seats may run on a cheaper model tier (the owner's amendment of the seat law) | IN FORCE: read-only censuses and enumerations → `model: "sonnet"`; recon that classifies or forecasts, every implementation / composition / cure / verification seat stays Opus; architecture, rulings and validation stay Fable | memory: the seat-law directive |

Also adopted with the list (the chair's own four): a WIP LIMIT — idle seats go to composers, re-cuts, compiles and verifiers, not to new fix lanes, until the paused queue drains; a lane that gates ALONE takes the EXCLUSIVE tier uncapped (the two-worker cap wastes six of eight cores under the one-gate law); every hand-off carries the live pins; ledger addenda at GATE events, not lane events.
