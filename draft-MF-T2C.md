# Town cartography / MF-T2C — the embedder extended onto the ABI (BigInt, components, degeneracy, point location)

- **Status:** DRAFT — compiled by lane TC-T2C for the chair; promotion, base-pinning and dispatch
  are chair acts
- **Packet version:** 1
- **Verified base:** `STOP: RE-DERIVE AT BASE`. Compiled against `claude/composite-r4` at
  `b25907f94c5581969fe169fed553b23b1293516b` (pinned for every read below), with three landings in
  flight the same day. The promoting chair pins the real base; the executing lane re-executes the
  whole §4 preflight there. ⛔ Carrying any figure from this compile across a moved tip is the
  program's most recently banked hazard (MF-T2B's CAS-slot titles trap) — every base figure below
  is therefore a re-derivation instruction, not a number.
- **Depends on:** `MF-T2A` (the single-declaration law — LANDED, terminal) and `MF-T2B` (the ABI +
  exact-geometry core — LANDED). Both bind this member; neither is re-dispatched.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **computed from the file at
  the compile tip `b25907f9`**, not copied from any packet. ⚠⚠ The preamble was re-stamped TWICE
  today (§315, §324.5); **the executor re-computes this hash at ITS OWN base before any edit** — a
  later landing may re-stamp again, and a mismatch means the family law moved under this draft:
  re-read the preamble, then re-stamp this line in the same act that promotes the packet. Its §P1
  refutations, §P2 hazard dispositions, §P3 anchor preflight, §P5 census law, §P6 mutant hygiene,
  §P7 STOP set and §P8 capsule law bind this packet and are not restated.
- **Collision group:** `d3a-port` — this member reserves ONE shared D3a change path,
  `tests/lint/sovereigntyLightingContract.walker.test.js`. ⭐ It deliberately reserves NO path on
  `src/domain/townMap/fabric/index.js` (no barrel edit — §7). Staged promotion, never simultaneous
  with another non-terminal D3a member on the census path (`CR-HB2B-SPLITP`; plan §9.3, preamble
  §P7.11).
- **Commit authority:** the executing lane commits on its own detached ref; the chair moves the
  branch.
- **Baseline posture:** `STOP: RE-DERIVE AT BASE`, every row, per §4. For orientation only, at the
  compile tip: `dcelEmbedding.js` and `dcel.js` are byte-identical to `9d851fae` (empty
  `git diff --stat`, executed), so the preamble §P9 figures **169** and **89** effective transfer
  to `b25907f9`; the fabric read 20 files / 128 distinct exported names / 0 duplicated at
  MF-T2B's landing. The capsule remains inadmissible unless re-stamped at the pinned base (§P8).

> **`censusAuthorization`:** this packet moves the test census by two credited files, eight
> titles and two suite titles. Its authorizing decisions are **ODQ §312** (the D3a port dispatch
> that commissions this member) and **the executing seat's own dispatch section, appended here at
> promotion** (the MF-T2B precedent: §312 + §315), under the wave charter at **§310.4** and
> §299.4's binding-forward rule that a packet moving any census names its authorizing decision in
> the packet body. The family's stamp is **GRANTED** at ODQ §312.2b (the stamped engine-train
> column; the plan's cap-of-four topology predates the grant).

> ⛔ **PORT SOURCE PROVENANCE (preamble §P1 R-MF-4).** The sandbox is not a git repository; the
> source is named by path and SHA-256, re-hashed by the implementing lane before any edit — **a
> mismatch is a STOP, not a merge.**
>
> | source (sealed W3 tip, `…/laneMFW3F-tip/src/domain/townMap/fabric/`) | SHA-256 |
> |---|---|
> | `fabricDcel.js` — the extended-contract reference: BigInt orientation, per-component outer faces, `DEGENERATE` typing, the face-kind vocabulary, the locate rules | `01da024a8655ce9e468a75a53ffc49a4e28508d2d7a9400cbe2dbf1b6d81197b` |
>
> The exact-geometry closure (`pointLocateRing`, `bounds`) is consumed from THIS TREE's
> `exactGeometry.js`, landed by MF-T2B — no second port of `fabricGeometry.js` occurs here. The
> 12.7× traversal repair's source is the D3a compile receipt (`laneTCD3A-receipt.md` §1.2), an
> algorithmic patch proven output-identical at four sizes — not a sandbox module.

---

## 1. Reconciled authority

1. **ODQ §303.5** — the port boundary law: *adopt the codex slice's record shapes and its
   embedder's algorithm; do not adopt its arrangement compiler, census, or parcel registry; on
   the integer wall D1's versioned ABI wins.* This member extends the ADOPTED algorithm in place;
   the four sandbox divergences it carries are exactly the places §303.5 records that the codex
   contract cannot express the fabric's real shapes.
2. **Preamble §P1 R-MF-2** — every module deciding topology, ordering or identity from a product
   of ABI quanta uses BigInt, with the reason in the file. ⭐⭐ **EXECUTED AT THE COMPILE TIP,
   END-TO-END:** on the triangle `a=[0,0] b=[m−1,m] c=[m,m+1]`, `m=1286630001`, the LANDED kernel
   throws `'DCEL vertex has an angular tie'` on valid ground — `Number` cross `0`, `BigInt` cross
   `−1n`. MF-T2B's widened wall admits these coordinates today; this member is what makes the
   arithmetic survive what the wall now admits. The false throw wears the message of a condition
   that did not occur; A2 carries the exact triple.
3. **Preamble §P1 R-MF-1** — a port member prices complexity against real leaf sizes. The
   dominant term is `[...unvisited].sort(…)[0]` inside the face-traversal loop; the pre-sorted
   cursor repair (12.7×, output-identical at four sizes, receipt-proven) rides here.
4. **ODQ §287.8 / SPEC §10.15** — D1 proves face/adjacency/point-location; both codex packets
   name point location an explicit non-goal, so §287.8's third leg is MINTED at this member,
   under the landed `COORDINATE_ABI.boundaryRule: 'CLOSED'`.
5. **ODQ §287.16 / §290.1/§290.4** — dormant explicit input; live seeds byte-identical; the
   existence of a conceivable edge case is not authority to enlarge the tranche.
6. **Live code decides.** Compiled from executed probes at `b25907f9` (this lane's receipt §1):
   the landed kernel's open-chain throw, the two-EXTERIOR/one-field component defect, the sealed
   face record's exact-key pin, and the sandbox reference's behavior on the same fixtures.

**Resolved contradictions:**

- *The sealed PLANAR_DCEL face record is pinned by exact keys* (`['boundaryHalfEdgeId','faceId',
  'faceKind']`) and the sealed dcel's key list pins singular `outerFaceId` — vs *the port brings
  the extended face shape in.* **RESOLUTION: the extended contract lives on the IN-MEMORY
  embedding; `dcel.js` seals a legacy projection, and the sealed artifact does not move by one
  byte.** The v1 artifact-schema shift (sealing the extended shape) is a later declared-shift
  wave, named as a carry, not taken. Executed basis: `sealCanonicalArtifact` digests the whole
  record, so ONE added key would move every PLANAR_DCEL digest; and no test imports the kernel
  directly — `dcel.js` is its only consumer in the entire tree.
- *The sandbox spells the outer kind `OUTER_CYCLE`* — vs *the live artifact and its pins spell it
  `EXTERIOR`.* **RESOLUTION: the app keeps `EXTERIOR`** and adopts `HOLE_CYCLE`/`DEGENERATE`
  unchanged; the locate verdict `ENCLOSED_BY_OUTER_CYCLE` is spelled `ENCLOSED_BY_EXTERIOR`.
  Renaming the live kind moves sealed bytes; renaming the port's new tokens costs nothing now
  and never again. **JUDGMENT, vetoable.**
- *The sandbox locate takes map units and converts via `worldQ`* — vs *the kernel's published
  coordinate space is ABI integer quanta.* **RESOLUTION: `locateFace(embedding, [xQ, zQ])` takes
  quanta — the ABI wins; unit conversion stays the caller's.** **JUDGMENT, vetoable.**
- *The codex `compareRay` THROWS on an angular tie; the sandbox breaks collinear ties
  canonically.* **RESOLUTION: the throw is PRESERVED, converted to exact arithmetic.** It is the
  detector for the one overlap shape the pairwise atomic check structurally passes (two collinear
  segments sharing one endpoint); BigInt confines it to TRUE ties. The tie rule is not one of
  §303.5's four divergences, so the codex contract stands. **JUDGMENT, vetoable.**
- *Plan §3.2(a) reads MF-T2C's A5 as the run that settles the extrapolated wall clock* — vs the
  estate's flake law. **RESOLUTION: no credited test asserts wall clock.** A8 pins
  correctness-at-scale (the Euler identity on a synthetic ~2k-boundary grid); the completion
  receipt records the measured time as evidence. Named divergence from the plan's letter.

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** the app-side planar embedder survives the coordinate range the landed ABI
already admits and expresses the shapes the fabric actually publishes — exact BigInt orientation
(no false angular tie at ABI scale), one outer face per connected component (`outerFaceIds`),
zero-area cycles typed `DEGENERATE` instead of thrown, the hole-capable face-record shape
(`innerBoundaryHalfEdgeIds`, declared), and point location under `boundaryRule: 'CLOSED'` — while
the sealed PLANAR_DCEL artifact, its digests, and every live byte stay exactly where they are.

**Definition of done:** `derivePlanarDcelEmbedding` computes `orient`/`compareRay`/`area2` in
BigInt; the face traversal uses the pre-sorted cursor; the embedding returns the extended
contract of §6; `locateFace` and `PLANAR_EMBEDDING_FACE_KINDS` are exported;
`compileOrthogonalCrossPlanarDcel` seals the legacy projection with its census made BigInt-safe;
and the digest-pin suites pass with not one literal edited.

**In scope:**

1. **One primary behavior** — the extended exact planar embedding (BigInt + divergences 2/3/4
   are ONE behavior: without exact arithmetic the extended kernel throws falsely on real ground,
   so they ship together or not at all; plan §4 says exactly this).
2. **One necessary integration path** — the `dcel.js` seal path: the BigInt-safe census and the
   legacy projection, which is what keeps the only existing consumer byte-identical.
3. **One prevention guard** — the untouched-pin control (§10): every digest-pin suite green,
   unmodified, after the edit; plus A1's degenerate-extension pin proving the new fields are
   inert on the admitted input.

**Explicit non-goals, named affirmatively:**

- ⛔ **No sealed-artifact change.** `PLANAR_DCEL_SCHEMA_VERSION`, `PLANAR_DCEL_LAW_VERSION`,
  `embeddingKind: 'XZ_LEFT_FACE_V1'`, the sealed key lists and every digest stay frozen. The
  extended-shape seal is a named carry for a later declared-shift wave.
- ⛔ **No consumer is wired.** No module outside `dcel.js` gains an import of the kernel; the
  barrel is untouched; nothing reaches the entry closure.
- ⛔ **No containment filling.** `innerBoundaryHalfEdgeIds` ports as the sandbox publishes it —
  declared and empty everywhere (verified over the whole reference). Containment is answered by
  `locateFace`'s smallest-containing-cycle rule; face-containment machinery exists in neither
  source and §290.4 forbids the enlargement here.
- ⛔ **No noder, no arrangement change** — that is MF-T2D. This member's kernel still requires
  the atomically-noded input the codex contract states.
- ⛔ **No arrangement-quantum rung, no tuning constant.** `BOUNDARY_EPS_Q = 0.5` is the ABI's
  half-quantum rounding radius, a structural constant of `boundaryRule: 'CLOSED'` ported verbatim
  from the reference's locate call — argued as ABI-structural, RAISED-4 if the chair reads it as
  tuning surface.
- ⛔ **No reachability claim for `HOLE_CYCLE` or `ENCLOSED_BY_EXTERIOR`** (RAISED-1): both port
  as defensive vocabulary with the finding in the module docblock; no pin asserts they fire.
- ⛔ **No y-flip, no height, no `FABRIC_COORDINATE_ABI` motion, no dependency bump.**
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` — the extended embedding is in-memory; nothing new is sealed or persisted | ≤1 |
| Named state writers | `0` | ≤1 |
| Feature flags | `0` | ≤1 |
| User-facing surfaces | `0` | ≤1 |
| Direct consumers | `0` new (the existing `dcel.js` edge is preserved, not added) | ≤2 |
| New logic-bearing production leaves | `0` | ≤2 |
| Existing logic-bearing production files modified | `2` (`dcelEmbedding.js`, `dcel.js`) | ≤3 |
| Additional registration-only files | `0` — deliberately no barrel edit | ≤3 |
| Handwritten files total | `6` (2 src · 2 acceptance · 1 census re-record · this packet) | ≤12 |
| New/changed effective production lines | **≤ `220`** (compile estimate 120–160; measured at implementation with eslint's Linter) | ≤400 |
| Effective lines per new leaf | `n/a` — no new leaf; `dcelEmbedding.js` stays far under the plain 800 ceiling | ≤250 |
| Delta in a shared/hot file | `0` — `STOP: RE-DERIVE AT BASE` that no manifest path has joined the hot-file list | ≤15 |
| Acceptance cases | `8` | ≤8 |

Overrides approved before dispatch: `NONE`.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- MF-T2C
```

Then, before any edit — every row is one this manifest touches, so §P8's re-execution law binds
and no capsule covers them. **Every expected value is re-derived here, none inherited:**

```sh
# 0) The family preamble hash — recompute; a mismatch is a family-law move, not a typo
shasum -a 256 docs/implementation/preambles/MF-PREAMBLE.md

# 1) The port source is unchanged (a mismatch is a STOP, not a merge)
shasum -a 256 <sealed-tip>/src/domain/townMap/fabric/fabricDcel.js   # MUST match the header table

# 2) The two target files: effective lines with eslint's own Linter (never wc -l), and the
#    sweep-plant anchor line present at column zero, byte-exact
node <measure-eff> <HEAD> src/domain/townMap/fabric/dcelEmbedding.js src/domain/townMap/fabric/dcel.js
grep -n '^export function derivePlanarDcelEmbedding(input) {' src/domain/townMap/fabric/dcelEmbedding.js
grep -c 'CURRENT_MAP_TRADITION_ID' src/domain/townMap/fabric/dcelEmbedding.js   # MUST be 0

# 3) MF-T2A's law green, and the arriving names absent from the live tree
npx vitest run tests/lint/townMapFabricSingleDeclaration.walker.test.js ; echo TRUE_EXIT=$?
git grep -n 'locateFace\|PLANAR_EMBEDDING_FACE_KINDS' -- src tests                # MUST be empty

# 4) The untouched-pin control, captured BEFORE the first edit: the 12 digest-pin suites AND
#    every other fabric-touching test file — counts recorded for the §10 after-capture
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapPlanarDcel.test.js tests/domain/townMapBoundaryArrangement.test.js \
  tests/domain/townMapFabricRoot.test.js tests/domain/townMapParcelRegistry.test.js \
  tests/domain/townMapFantasyConstruction.test.js tests/domain/townMapSettlementFabric.test.js \
  tests/domain/townMapMassingRoster.test.js tests/domain/townMapMassingPersistence.test.js \
  tests/domain/townMapStreetGeometry.test.js tests/domain/townMapMassingProjection.test.js \
  tests/domain/townMapLandform.test.js tests/domain/townMapStreetGraph.test.js

# 5) The census tuple AT THIS BASE — read the five figures out of the walker's CENSUS object and
#    record them as the member's before-tuple. STOP: RE-DERIVE AT BASE; the compile carries none.

# 6) Both typecheck floors AT THIS BASE — run and record; STOP: RE-DERIVE AT BASE
npm run typecheck:ratchet    ; echo TRUE_EXIT=$?
npm run typecheck:domain:strict ; echo TRUE_EXIT=$?

# 7) §P2.9: run the acceptance fixtures against the UNEDITED kernel and print what it does —
#    the ABI triangle and the open chain MUST throw with the exact messages A2/A3 name; the
#    two-component and nested fixtures MUST embed with two EXTERIOR faces each. A pin is written
#    only after this capture.
```

**A base where the ABI triangle does NOT throw, or where either target file's effective count
disagrees with a fresh Linter measurement, is a STOP** — it means a sibling landed into this
member's surface between compile and dispatch.

## 5. Verified tree contract

Facts verified at the compile tip `b25907f9`; the executor re-verifies each at its base.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| The kernel being extended | `src/domain/townMap/fabric/dcelEmbedding.js` | `export function derivePlanarDcelEmbedding` | 169 effective; sole consumer `dcel.js`; `orient`/`compareRay`/shoelace in `Number`; face traversal sorts inside the loop | extend IN PLACE; the exported signature line survives byte-identical (sweep anchor) |
| ⛔ The sweep-plant anchor | `scripts/mutation-sweep.sh` | entry **28a**: the `perl -0pi -e "s/^export function derivePlanarDcelEmbedding\(input\) \{/…/m"` plant, its `check_caught "town-map/fabric duplicate export declaration"`, and the `MUTATED_FILES` row naming the host | the plant applies to the CURRENT file text | ⛔ the edit preserves the anchor line at column zero AND keeps `CURRENT_MAP_TRADITION_ID` un-imported and un-declared in the host — else MF-T2A's plant silently stops applying or turns into the J-TET2A-1 parse-error class |
| The seal path | `src/domain/townMap/fabric/dcel.js` | `compileOrthogonalCrossPlanarDcel`, `validateCensus` | 89 effective; seals `faces: embedding.faces` and `outerFaceId` directly; census compares `metrics.area2s` in `Number` | project the legacy face triple at the seal; make the census BigInt-safe; extend its invariants (§6) |
| The frozen artifact shape | `tests/domain/townMapPlanarDcel.test.js` | the exact-key pins: face keys `['boundaryHalfEdgeId','faceId','faceKind']`, the sealed dcel key list with singular `outerFaceId`; the digest/vertex/area2 pin rosters | the largest holder of the family's literal digest pins | ⛔ must stay green UNTOUCHED — the member's own no-op control; a moved pin is a STOP, never a re-record |
| The ABI authority | `src/domain/townMap/fabric/coordinateAbi.js` | `COORDINATE_ABI.boundaryRule === 'CLOSED'`, `MAX_WORLD_UNITS` | landed by MF-T2B | A6 asserts the locate contract against the ABI record, not against a comment |
| The geometry core | `src/domain/townMap/fabric/exactGeometry.js` | `pointLocateRing(poly, px, py, eps)` → `'INSIDE'|'BOUNDARY'|'OUTSIDE'`; `bounds` | landed by MF-T2B | the locate substrate; called with `eps = BOUNDARY_EPS_Q` |
| The widened wall | `src/domain/townMap/fabric/foundation.js` | `requireCanonicalInt` defaults `±MAX_WORLD_UNITS` | landed by MF-T2B | untouched here; it is WHY the ABI-scale fixtures reach the kernel |
| The standing law | `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | the fabric single-declaration law | green at zero duplicates | the arriving export names collide with nothing (verified: zero hits tree-wide) |
| Census | `tests/lint/sovereigntyLightingContract.walker.test.js` | `const CENSUS = Object.freeze({` | the five-figure tuple, SEQUENCED | re-record all five together with cause + authorization; `STOP: RE-DERIVE AT BASE` for the before-tuple |
| Test precedent | `tests/domain/townMapCoordinateAbi.test.js` + `tests/property/townMapCoordinateAbiDeterminism.test.js` | MF-T2B's domain matrix + determinism companion | the family's two-file shape | **copy this proof shape** |
| Anchor helper | `tests/helpers/anchoredNegatives.js` | `expectAbsentWithAnchor` | the negative-assertion route | by name on the same line, per §P3 |

**Forbidden alternatives:**

- no second face list, no parallel "extendedFaces" export, no second embedding function;
- no change to any schema/law/version string, `embeddingKind`, or sealed key list;
- no import of the sealed sandbox tree from any app file, ever;
- no barrel (`fabric/index.js`) edit; no import of the kernel from any new module;
- no `test.each`, no `describe.runIf`, no loop-generated tests (census law §P5);
- no files outside the manifest.

## 6. Exact contracts

### The extended embedding contract (in-memory; the sealed artifact is NOT this)

```js
// derivePlanarDcelEmbedding(input) — input contract UNCHANGED. Returns:
{
  vertices,                       // unchanged shape
  halfEdges,                      // unchanged shape
  faces: [{ faceId, faceKind, boundaryHalfEdgeId,
            innerBoundaryHalfEdgeIds,   // NEW — declared, [] in this era (port-faithful)
            component }],               // NEW — the connected component index
  outerFaceId,                    // preserved: outerFaceIds[0], or undefined when none exists
  outerFaceIds,                   // NEW — one per component that has a negative cycle,
                                  //       each that component's most-negative cycle,
                                  //       ordered by component index
  componentCount,                 // NEW
  faceIndex: [{ ring, bounds, area2 }], // NEW — aligned index-for-index with faces[];
                                  //       ring in ABI quanta; bounds [minX,minY,maxX,maxY]|null;
                                  //       area2 a BigInt
  metrics: { area2s,              // NOW BigInt values, discovery order (order unchanged)
             cycleLengths, degrees,      // unchanged
             degenerateFaces, holeCycles } // NEW counts
}
export const PLANAR_EMBEDDING_FACE_KINDS =
  Object.freeze(['BOUNDED', 'EXTERIOR', 'HOLE_CYCLE', 'DEGENERATE']);
export function locateFace(embedding, pointQ)   // [xQ,zQ] in ABI quanta — see below
```

- **Face kinds.** A positive cycle is `BOUNDED`; a component's most-negative cycle is
  `EXTERIOR`; any OTHER negative cycle of that component is `HOLE_CYCLE`; a zero-area cycle is
  `DEGENERATE` and is REPORTED, never thrown. ⚠ The module docblock records the executed
  finding: under the kernel's own noded-input precondition each component carries exactly one
  negative walk, so `HOLE_CYCLE` is defensive vocabulary — reachable only if a later wave
  relaxes the precondition — and no test may claim it fires (RAISED-1).
- **Determinism of components.** Component indices are assigned by a DFS over vertices in
  codepoint order of the vertex keys (the kernel's existing `stableSceneStringify` keys), edges
  followed via twins; `outerFaceIds` is ordered by component index. No hash order anywhere.
- **BigInt law (R-MF-2).** `orient`, `compareRay`'s cross, and the shoelace accumulate in
  BigInt; comparisons against `0n`; published `area2s` are BigInt. The `compareRay` tie
  (`cross === 0n`) still THROWS `'DCEL vertex has an angular tie'` — now only on TRUE collinear
  ties, which are the overlap shape the pairwise atomic check structurally passes.
- **The traversal cursor (R-MF-1).** All half-edge ids are sorted ONCE by codepoint before the
  face walk; a cursor advances past visited ids to yield each start. The selected start is the
  codepoint-minimum unvisited id — the same value the per-iteration sort produced — and the
  output is invariant because the cycle SET does not depend on discovery order, each cycle is
  rotated to its minimum, and faces are sorted by `faceId`. The receipt cites the four-size
  output-identity proof; the member's step-1 capture re-proves it on this tree.
- **The zero-area path (divergence 4).** The throw
  `'DCEL face must have nonzero signed area'` is REMOVED; a zero-area cycle produces a face
  typed `DEGENERATE` (counted in `metrics.degenerateFaces`), excluded from `outerFaceIds` and
  from locate candidacy. A degenerate-only component contributes NO `outerFaceIds` entry
  (executed against the reference: open chain → `outerFaceIds: []`).

### Point location — `locateFace(embedding, pointQ)`

```text
input : pointQ = [xQ, zQ], ABI integer quanta (the embedding's own space)
refusal: either coordinate not a safe integer within ±MAX_WORLD_UNITS
         -> { kind: 'OUTSIDE_ABI', faceId: null }
scan  : every non-DEGENERATE face with a ring of >= 3 (via faceIndex), bounding box first,
        then pointLocateRing(ring, xQ, zQ, BOUNDARY_EPS_Q)   // BOUNDARY_EPS_Q = 0.5, internal
pick  : the smallest containing cycle by |area2|; at equal |area2| a BOUNDED face wins
        (a closed ring's two walks have identical magnitude — picking by iteration order would
        make the verdict depend on hash order; the region is the answer)
verdicts:
  INSIDE                { kind, faceId, faceKind: 'BOUNDED', faces: <every containing faceId> }
  ENCLOSED_BY_EXTERIOR  { …same shape… }   // best containing cycle not BOUNDED — defensive,
                                           // no reachability pin (RAISED-1)
  BOUNDARY              { kind, faceId, faceKind, faces }   // boundaryRule 'CLOSED': a point ON
                                           // a boundary is its own verdict, folded into neither side
  OUTER                 { kind: 'OUTER', faceId: outerFaceIds[0] ?? null, faces: [] }
```

### The seal path — `dcel.js`

- `validateCensus` compares `metrics.area2s` BigInt-safely (`0n` literals; the
  positives-sum-equals-negation identity in BigInt) and gains three invariants on the admitted
  input: `outerFaceIds` deep-equals `[outerFaceId]`, `metrics.degenerateFaces === 0`,
  `metrics.holeCycles === 0`, `componentCount === 1`.
- The seal writes `faces: embedding.faces.map(({ faceId, faceKind, boundaryHalfEdgeId }) =>
  ({ faceId, faceKind, boundaryHalfEdgeId }))` — the legacy triple, key-for-key. The stable
  serializer sorts keys, so the projected records serialize byte-identically to today's and
  every contentHash stays put. Nothing else in the sealed record moves.

### Absence rules

- `outerFaceId` when no component has a negative cycle: `undefined` (the codex's own `?.` shape
  today), and `outerFaceIds` is `[]` — never `null`, never a throw.
- `locateFace` on non-ABI input: the `OUTSIDE_ABI` refusal record, never `NaN`, never a throw.
- `innerBoundaryHalfEdgeIds`: always present, always an array, `[]` in this era.

### Determinism

- No hash order anywhere: cycle starts by codepoint-minimum id; components by codepoint-first
  vertex key; `outerFaceIds` by component index; faces sorted by `faceId` (unchanged).
- Pure functions; no clock, no RNG, no locale, no `Intl` — the determinism companion scans the
  edited file's raw text with a positive control, exactly as MF-T2B's does.
- BigInt-vs-Number: every published Number stays a Number (`cycleLengths`, `degrees`); every
  BigInt is exact (`area2s`, `faceIndex[].area2`). No BigInt crosses the seal.

### Flag and dormancy

- Flag: `NONE`. Golden posture: **unchanged and PROVEN unchanged** — the untouched-pin control
  plus the before/after equality over every fabric-touching test file (§4 step 4 vs §10).

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| pure derivation, in-memory | `dcel.js` (sole consumer) | ⛔ nothing persisted; the sealed artifact is unchanged | n/a | n/a | n/a | the sealed v1 artifact is untouched, so no migration exists to declare | n/a — no runtime surface |

### Receipts and privacy

- Closed kinds: `NONE`. DM-only fields: `NONE`. Player/public projection: unaffected; nothing
  runs at runtime and no live surface imports the fabric barrel's dcel path any differently.

### Alignment and edit story

- Alignment: `DECLARED EMPTY: an exact-geometry kernel has no alignment surface.`
- Edit story: `ENGINE-ONLY: pure derivation; nothing is DM-editable and nothing is proposed.`

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/domain/townMap/fabric/dcelEmbedding.js` | `orient`, `compareRay`, the traversal loop, the cycle/face assembly, + new `locateFace`, `PLANAR_EMBEDDING_FACE_KINDS`, internal `BOUNDARY_EPS_Q` | `+150 / −15` eff | The §6 contract, ported against `fabricDcel.js` at the header SHA. ⛔ The exported signature line `export function derivePlanarDcelEmbedding(input) {` survives byte-identical at column zero; `CURRENT_MAP_TRADITION_ID` is neither imported nor declared here; the input contract does not change |
| `MODIFY` | `src/domain/townMap/fabric/dcel.js` | `validateCensus` + the seal's `faces:` line | `+15` eff | BigInt-safe census, the three added invariants, the legacy-triple projection. ⛔ No other sealed field moves |
| `CREATE` | `tests/domain/townMapDcelEmbeddingExtension.test.js` | `describe('MF-T2C planar-embedding extension …')`, A1–A6, A8 | `n/a` | The domain matrix: ONE literal `describe`, seven straight-line `test()` calls, string-literal titles, fixtures built inside named tests (SP-D idiom) |
| `CREATE` | `tests/property/townMapDcelEmbeddingDeterminism.test.js` | `describe('MF-T2C planar-embedding determinism …')`, A7 | `n/a` | The replay companion, matching MF-T2B's property-file shape |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | the `CENSUS` object | `+12` | ONE re-record block naming its cause and the `censusAuthorization` refs; all five figures re-derived together at the base. `STOP: RE-DERIVE AT BASE` |
| `DOC` | `docs/implementation/packets/town-cartography/MF-T2C.md` | this packet | `n/a` | The packet, its status transitions, and its §12 landing record |

Generated artifacts: `NONE`. No other file may be edited. ⚠ The two acceptance files are
`CREATE` rows, not `TEST` rows — a `TEST` path must exist at every status (the MF-T2B measured
precedent). ⚠ No `tests/lint/**` file is minted, so §P3b's mutation-coverage row is **NOT
INCURRED**; no registration file is touched, and §P4's four-name template is satisfied by its
"reason none exists" arm: the kernel is directory-internal, the barrel deliberately does not
export it, and the registry test paths that bind this member are the single-declaration walker
and the entry-closure fence.

**Symbols the deliverable CREATES** (`locateFace`, `PLANAR_EMBEDDING_FACE_KINDS`, the two
describe titles) join `requiredSymbols` **at the flip to LANDED, never at READY** — the
validator resolves rows against the live tree at every status.

## 8. Ordered coding sequence

0. Dispatch and seal; run every §4 preflight. STOP on any mismatch — the source SHA, the
   preamble hash, the plant anchor, a fabric file changed underfoot, or an unexpected census.
1. **Capture the golden/dormancy evidence BEFORE the first edit:** the §4 step-4 battery counts;
   the census tuple; and the §P2.9 unedited-kernel fixture captures (the two throws, the
   two-EXTERIOR embeddings) — the pins of A2/A3/A4 are written FROM these captures.
2. Add the acceptance tests A1–A8 (red where they drive the not-yet-written extension).
3. Implement `dcelEmbedding.js` per §6 — arithmetic first (BigInt orient/compareRay/shoelace),
   then the cursor, then components/kinds/`outerFaceIds`/`DEGENERATE`, then `faceIndex` and
   `locateFace`.
4. — (no writer, no lifecycle seam)
5. — (no new consumer; `dcel.js`'s edit is step 6's guard-side work)
6. `dcel.js`: the BigInt-safe census, its three added invariants, the seal projection.
7. Focused verification (§10), including the untouched-pin control, the before/after equality
   over every fabric-touching test file, and the anchor preflight.
8. Wave-end gate and the completion receipt (§12).

**Bounded algorithm — the cursor repair (the one hot-loop change):**

```text
1. sortedIds := [...edgeById.keys()].sort(compareCodepoint)        // ONCE
2. cursor := 0
3. while unvisited.size:
4.   while cursor < sortedIds.length and !unvisited.has(sortedIds[cursor]): cursor += 1
5.   start := sortedIds[cursor]        // == codepoint-minimum unvisited, as today
6.   walk the cycle exactly as the landed loop does
```

**Bounded algorithm — components and outer faces:**

```text
1. for each vertex key in codepoint order: if unassigned, DFS via outgoing→twin→origin,
   stamping componentIndex; componentCount := number of DFS roots
2. each cycle's component := component of its first half-edge's origin
3. per component: outer := the cycle with the minimum (most negative) BigInt area2 among its
   negative cycles, if any
4. faceKind := area2 > 0n ? BOUNDED : area2 < 0n ? (outer of its component ? EXTERIOR
   : HOLE_CYCLE) : DEGENERATE
5. outerFaceIds := outers ordered by componentIndex; outerFaceId := outerFaceIds[0] (undefined
   when the list is empty)
```

## 9. Acceptance matrix

Every negative below carries a positive control in the same test; every fixture was executed at
compile against the landed kernel or the sandbox reference (receipt §1.7–§1.8), so each pin
documents an OBSERVED result, not a predicted one. The executor re-runs each fixture before
pinning (§8 step 1).

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| **A1** | Main behavior — the extension is INERT on the admitted input | the admitted orthogonal-cross fixture through `compileOrthogonalCrossPlanarDcel` (the fixtures file's own maker) plus the raw embedding | the sealed artifact's face keys are exactly the legacy triple and its key list still carries singular `outerFaceId`; on the embedding, `outerFaceIds` deep-equals `[outerFaceId]`, every `innerBoundaryHalfEdgeIds` deep-equals `[]`, `componentCount` is 1, `degenerateFaces` and `holeCycles` are 0 — with the positive control FIRST: the extended fields EXIST (not `undefined`), `faceIndex` aligns 1:1 with `faces` and its rings are non-empty | domain |
| **A2** | ⭐⭐ The named historical regression (R-MF-2) — exact arithmetic at ABI scale, with the convicting counterexample | the triangle `a=[0,0] b=[m−1,m] c=[m,m+1]`, `m = 1286630001`, as three canonical boundaries — the EXACT triple the compile executed | the extended kernel EMBEDS it: 2 faces, one `BOUNDED` one `EXTERIOR`, Euler `V−E+F = 2` — where the landed kernel threw the false angular tie (captured at step 1). In the same test, the counterforce with its control: `Number` cross of the two rays is `0` while the BigInt cross is `−1n` (they DISAGREE at ABI scale), and on the same triangle translated to the fixture-era range both spellings agree — the divergence is the SCALE, not an arbitrary disagreement. And the throw's own positive control: a TRUE collinear tie (two collinear boundaries `[0,0]→[10,0]` and `[0,0]→[20,0]`) still throws the angular-tie message — the detector is confined to real ties, not removed | domain |
| **A3** | Divergence 4 — a zero-area cycle is typed, never thrown | the two-segment open chain `[0,0]→[10,0]→[10,10]` | one face, `faceKind: 'DEGENERATE'`, `metrics.degenerateFaces === 1`, `outerFaceIds` deep-equals `[]` and `outerFaceId` is `undefined`; positive control in the same test: CLOSING the chain into a triangle yields zero `DEGENERATE`, one `BOUNDED`, one `EXTERIOR` | domain |
| **A4** | Divergence 3 — one outer face PER COMPONENT | two disjoint axis-aligned squares (8 boundaries) | `componentCount === 2`; `outerFaceIds` has length 2, one per component, ordered by component index, each the most-negative cycle of its own component (asserted against `faceIndex[].area2`); both are typed `EXTERIOR`; `outerFaceId === outerFaceIds[0]`; positive control: the single-square half of the fixture alone yields `outerFaceIds` of length 1 | domain |
| **A5** | Divergence 2's real shape — a nested (fossil) ring is a second COMPONENT, and containment is the locate rule's | a 100×100 square with a disjoint 20×20 square nested inside | `componentCount === 2`, `holeCycles === 0` (the kind is defensive — asserted with the anchor comment naming RAISED-1), both negative cycles typed `EXTERIOR`, each its component's outer; `locateFace` at a point inside the inner ring names the INNER `BOUNDED` face while listing all four containing cycles in `faces` (the smallest-containing-cycle rule), and at an annulus point names the OUTER `BOUNDED` face | domain |
| **A6** | ⭐ Point location under the ABI's own boundary rule | the nested fixture of A5 plus the ABI record | `COORDINATE_ABI.boundaryRule === 'CLOSED'` asserted as the authority; a point ON the inner ring's edge returns `kind: 'BOUNDARY'` (folded into neither side); a point far outside every ring returns `kind: 'OUTER'` with `faceId === outerFaceIds[0]`; `locateFace(embedding, [0.5, 0])` and non-integer/overflow inputs return the `OUTSIDE_ABI` refusal with `faceId: null`; positive control: the INSIDE verdict of A5 re-asserted here so the refusal arms sit beside a verdict that fires | domain |
| **A7** | Idempotency / replay + purity | every fixture above run twice; the edited file's raw text | byte-identical results both times — the sealed artifact compared by `contentHash`, the extended fields by BigInt-safe deep equality; and the source scan of `dcelEmbedding.js` finds no `Date`, `Math.random`, `Intl`, `toLocale`, `performance` or `crypto`, with a positive control proving the scan sees a planted occurrence (MF-T2B's A5 shape) | property |
| **A8** | Correctness at scale — the repaired traversal on a real-leaf-sized input | a deterministic synthetic grid (~30×30 cells ⇒ ~1.8k boundaries), built inside the test | the kernel completes and the census closes exactly: Euler `V − E + F === 1 + C` with `C === componentCount`, every half-edge in exactly one cycle, `outerFaceIds` length equals `componentCount`, zero `DEGENERATE`; ⛔ NO wall-clock assertion — the elapsed time is recorded in the receipt as evidence, never pinned (flake law; divergence from plan §3.2a named in §1) | domain |

**Anchor law (§P3):** A1, A3, A5, A6 and A7 assert absences (`[]`, zero counts, no forbidden
identifier, `undefined`). Each routes through `tests/helpers/anchoredNegatives.js` by name on
the same line, or carries `// anchored: <why this cannot go vacuous>` on the assertion line or
the line immediately above; for a multi-line comment only the LAST line counts.

**Predicted census motion — the family's domain-matrix + determinism-companion shape:**

```
BEFORE: STOP: RE-DERIVE AT BASE (the five figures read from the CENSUS object at dispatch)
DELTA : +2 files · +0 parked · +2 credited · +8 titles · +2 suites
        (7 titles in the domain matrix, 1 in the property companion — derived from the
         test() calls above, NOT copied from the plan, whose +6 estimate is superseded)
```

⛔ **`parked` MUST NOT move.** One literal `describe` per file, straight-line `test()` calls,
string-literal titles; loops live INSIDE named tests. ⚠ **NAMED INTERIOR RED:** the census arm
is an exact equality against a recorded constant, so this member reds at its own implementation
commit until the tuple is re-recorded — the census is SEQUENCED, so the `files` red is itself
proof the later figures had not yet been read. A delta smaller than the titles added is
attributed by reverting one test file at a time, never accepted as arithmetic.

## 10. Verification commands — the gate section (skeleton; exits and counts recorded at execution)

```sh
# Anchor preflight — MANDATORY per new test file, BEFORE the member proof is declared green
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?

# Focused static
npx eslint src/domain/townMap/fabric/dcelEmbedding.js src/domain/townMap/fabric/dcel.js \
           tests/domain/townMapDcelEmbeddingExtension.test.js \
           tests/property/townMapDcelEmbeddingDeterminism.test.js
npm run typecheck:ratchet ; echo TRUE_EXIT=$?          # floor: STOP: RE-DERIVE AT BASE (§4.6)
npm run typecheck:domain:strict ; echo TRUE_EXIT=$?    # floor: STOP: RE-DERIVE AT BASE (§4.6)

# Effective-line ledger against §3 — eslint's own Linter, never wc -l
node <measure-eff> <HEAD> src/domain/townMap/fabric/dcelEmbedding.js src/domain/townMap/fabric/dcel.js

# Focused tests — one slot, one command
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapDcelEmbeddingExtension.test.js \
  tests/property/townMapDcelEmbeddingDeterminism.test.js \
  tests/lint/townMapFabricSingleDeclaration.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js

# ⭐ THE UNTOUCHED-PIN CONTROL — the §4 step-4 battery, re-run AFTER the edit; counts must equal
# the before-capture file-for-file, and `git diff` over the twelve pin files must be EMPTY
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapPlanarDcel.test.js tests/domain/townMapBoundaryArrangement.test.js \
  tests/domain/townMapFabricRoot.test.js tests/domain/townMapParcelRegistry.test.js \
  tests/domain/townMapFantasyConstruction.test.js tests/domain/townMapSettlementFabric.test.js \
  tests/domain/townMapMassingRoster.test.js tests/domain/townMapMassingPersistence.test.js \
  tests/domain/townMapStreetGeometry.test.js tests/domain/townMapMassingProjection.test.js \
  tests/domain/townMapLandform.test.js tests/domain/townMapStreetGraph.test.js

# ⛔ Dormancy: POST-BUILD ONLY (§P2.2). A pre-build run self-skips and is not a discharge.
npm run build   # then:
VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js ; echo TRUE_EXIT=$?
# …plus the forensic zoom: a string literal unique to this member ('OUTSIDE_ABI',
# 'ENCLOSED_BY_EXTERIOR', the false-tie docnote) appears in NONE of the entry chunks and in a
# lazy chunk elsewhere in dist/ — measured against the entry closure's own chunk list.

# ⭐ THE REMOVING-POWER SWEEP — eight mutants, each planted in a host it PARSES in, each
# convicting a NAMED arm, each restored by byte copy verified with cmp (never git checkout --).
# The sweep opens by asserting the PRISTINE tree is green (the T2B reporter lesson).
#   M1 orient/compareRay reverted to Number arithmetic        -> A2 (the false tie returns)
#   M2 the DEGENERATE typing reverted to the zero-area throw  -> A3
#   M3 outer selection reverted to the first negative cycle
#      in discovery order (per-component logic deleted)       -> A4 (+A5)
#   M4 the BOUNDARY verdict branch collapsed into INSIDE
#      (locateFace treats 'BOUNDARY' as containment)          -> A6 (the on-edge verdict flips)
#   M5 the cursor's advance-past-visited guard deleted
#      (a start may name an already-visited id)               -> A1 (the disjoint-cycle throw
#      fires on the very first fixture; note the CANONICAL-MINIMUM property itself is NOT
#      load-bearing — §6 says why — so the mutant targets the guard branch, not the ordering)
#   M6 innerBoundaryHalfEdgeIds omitted from the face record  -> A1 (existence control)
#   M7 the dcel.js seal projection removed (extended faces
#      sealed whole)                                          -> townMapPlanarDcel.test.js
#      (the exact-key pin and digest pins red — the guard proven able to red)
#   M8 the at-equal-|area| tie INVERTED to prefer the
#      non-BOUNDED cycle                                      -> A5/A6 (INSIDE flips to
#      ENCLOSED_BY_EXTERIOR deterministically; a plain deletion could pass by iteration-order
#      luck, so the mutant inverts the branch instead)
# ⚠ M7 is the untouched-pin control's own conviction: it proves a moved sealed byte is VISIBLE.

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- MF-T2C
npm run implementation:resume -- MF-T2C

# Wave-end — BARE, fresh shell, never piped, never wrapped in gate-mutex, and OUTLAST it
npm run check:tail ; echo TRUE_EXIT=$?
```

⛔⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** — `test:ratchet` re-acquires and
self-deadlocks; **exit 3 is the mutex giving up, not a red.** ⚠ Trust no exit status you did not
capture. ⚠ Report both typecheck configurations by name and window against the floors captured
at §4 step 6 — a ratchet being green is evidence only about that ratchet. Expected: every
command exits `0`; report actual counts, never this packet's.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and preamble §P7:

- **any digest pin, exact-key pin, or pinned roster in the twelve suites moves** — the seal
  projection exists precisely so they cannot; a move means the member did something other than
  extend in memory. Re-record nothing; STOP and report the moved pin;
- any fabric-touching test file changes its result between the §4 and §10 captures;
- `PLANAR_DCEL_SCHEMA_VERSION`, `PLANAR_DCEL_LAW_VERSION`, `embeddingKind`,
  `FABRIC_COORDINATE_ABI` or any stamped version string would need to move;
- the port source's SHA-256 does not match the header table, or the family preamble's hash does
  not match a fresh computation at the base (re-read, re-stamp, re-promote — never edit past it);
- the `export function derivePlanarDcelEmbedding(input) {` line would change in ANY byte, or
  `CURRENT_MAP_TRADITION_ID` would become imported or declared in the host — the MF-T2A sweep
  plant's applicability is part of this member's contract;
- the ABI-scale triangle does NOT throw at the §4 step-7 capture (the base moved under the
  member), or ANY acceptance fixture's unedited-kernel behavior differs from the §1.7 captures;
- the bare name `clipHalfPlane` would be declared; the single-declaration walker reds; an
  arriving export name collides with a live one;
- a wall-clock assertion would be needed to make any credited test meaningful;
- a reachability pin would be needed for `HOLE_CYCLE` or `ENCLOSED_BY_EXTERIOR` (RAISED-1 —
  those kinds are defensive vocabulary and their firing is not claimable);
- the census moves by other than `+2 / +0 / +2 / +8 / +2` against the tuple captured at §4
  step 5, or `parked` moves at all;
- `tests/build/townMapLazy.test.js` reds POST-BUILD, or the forensic zoom finds a member-unique
  literal in an entry chunk;
- another D3a member is simultaneously non-terminal on
  `tests/lint/sovereigntyLightingContract.walker.test.js`;
- any ratchet, baseline, budget or ceiling would need raising; any tuning constant (including
  reading `BOUNDARY_EPS_Q` as one — RAISED-4) would be touched beyond what §6 declares.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into
the next wave.

### RAISED — for the chair at promotion (compiled recommendations; each vetoable)

1. **`HOLE_CYCLE` / `ENCLOSED_BY_EXTERIOR` are defensive dead vocabulary** under the kernel's
   noded-input precondition (executed: zero occurrences across every probe, including nested
   rings — a hole is a separate COMPONENT whose negative cycle is its own outer). Recommendation:
   **port them faithfully** with the finding in the docblock and no reachability pin; they become
   live only if a later wave relaxes the precondition. Alternative: strip to the executed
   vocabulary (smaller, less faithful).
2. **`innerBoundaryHalfEdgeIds` ports declared-and-empty** — the reference never fills it;
   containment is the locate rule's. Filling it is new machinery in neither source (§290.4).
3. **The census `titles` delta is +8, not the plan's +6** — derived from the actual test calls.
4. **`BOUNDARY_EPS_Q = 0.5`** is compiled as ABI-structural (the half-quantum rounding radius of
   `boundaryRule: 'CLOSED'`), not tuning surface. If the chair rules otherwise, this member is
   BLOCKED on the tuning-signature docket.
5. **The wall-clock divergence from plan §3.2(a)** — measured time goes in the receipt; no
   credited test asserts it.

## 12. Completion receipt — TEMPLATE (executed by the implementing lane; nothing below is filled at compile)

- Base SHA and lifecycle commits; working-tree state at the LANDED tip.
- Preamble hash recomputed at the base; port-source SHA re-check verbatim, at the base AND the
  DRAFT tip.
- Changed files and effective lines against §3, measured with eslint's Linter.
- The §4 step-7 unedited-kernel captures, verbatim (the two throws; the two-EXTERIOR
  embeddings), then A1–A8 executed with counts and TRUE_EXIT per command.
- The untouched-pin control: before/after counts file-for-file, and the empty `git diff` over
  the twelve pin suites.
- The mutant table: eight rows — planted digest, `node --check`, the NAMED convicted arm,
  restore digest, `cmp` verdict — plus the pristine-green and clean-re-run controls.
- Census before → after (all five, re-derived together), `parked` unmoved, the interior red
  observed and cleared, the suiteTitles proof.
- Both typecheck configurations by name, window, and floor (as captured at §4 step 6).
- Dormancy: the POST-BUILD run's executed counts and the forensic-zoom chunk listing.
- The measured wall clock of the A8 grid and of the ABI-triangle embed, as receipt evidence.
- Generated artifacts (`NONE` expected); deviations (`NONE` or a STOP); out-of-scope
  observations without investigation; judgment calls, each vetoable.
