
=== RT2.1-04
**noun:** `{defwork}` (the slot) ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY only — the town's own wall by recorded name; refuses any name outside the four wall words and drops the variant ‖ **reads per layer (pools):** DS-DEF-11 ×3 name it ‖ **safe class word:** — (it IS the derived fill) ‖ **source:** `defenseStateProse.js:1003`, `:1027-1035`, bag line `:1086` (the survey's `:1084` is `const slots = {`)

=== RT2.1-06
**noun:** barracks ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (garrison bucket); a SECURITY body ‖ **reads per layer (pools):** via `forces.garrison.present` ‖ **safe class word:** the garrison ‖ **source:** `defenseInstitutionBuckets.js:89`; `corruption.js:630`; catalogue `Barracks` (town, 0.3)

=== RT2.1-07
**noun:** professional guard ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY keyword with NO shipped row ‖ **reads per layer (pools):** none ‖ **safe class word:** — (a keyword the world never builds) ‖ **source:** `defenseInstitutionBuckets.js:89`; `threatDefensePolicy.js:38` (`Professional guard (hundreds)`, no catalogue row)

=== RT2.1-11
**noun:** magicDef bodies (wizard · mages' guild · academy · alchemist · golem workforce) ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: the SEVENTH bucket (ADDENDUM 11 lists six); flag `hasMagicInst` ‖ **reads per layer (pools):** DS-DEF-5 `arcane defense PRESENT/ABSENT` reads `forces.magicDef.present` in CODE (`arcaneDefensePoolKey`); the CENSUS lists only `magicWorks` — a census under-report (M-12) ‖ **safe class word:** arcane provision ‖ **source:** `defenseInstitutionBuckets.js:105-108`; `priorityHelpers.js:68`; `defenseStateProse.js:1319-1321`, `:1389`

=== RT2.1-12
**noun:** `force` (the token) ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: muster kind; on this desk `force = garrison \|\| militia` — EXCLUDES the watch, the mercenary, the charter ‖ **reads per layer (pools):** the `force` arg of `beastsRowSituation` (DS-DEF-2 Beasts ×7) ‖ **safe class word:** the muster (garrison-or-militia only) ‖ **source:** `holderTable.js:193`; `defenseStateProse.js:655`, `:429-440`

=== RT2.1-14
**noun:** court · courthouse ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: flag `hasCourtSystem` — satisfied by `town hall` / `city hall`, both REQUIRED, so nearly every town+ is `hasCourtSystem` through its HALL. HOLDER: court kind, a STATE ORGAN; its field tokens are the SEAT's (`govMultiplier · blocs · stability · termLines …`). POWER-eligible: a SECURITY body (`Courthouse`, `Multiple courthouses`, `Multiple court buildings`) ‖ **reads per layer (pools):** DS-DEF-2 Internal Security ×4 (`court ; prison`, SOURCE-UNRESOLVED); DS-DEF-6 Legal Infrastructure ×4 (WIRING-UNRESOLVED) ‖ **safe class word:** the court (body) only where a courthouse row resolves; otherwise the function word ("the town's law") ‖ **source:** `priorityHelpers.js:55`; `holderTable.js:78-81`, `:115`, `:215-223`; `corruption.js:630`; `defenseStateProse.js:657-659`

=== RT2.1-16
**noun:** treasury ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: treasury kind, a STATE ORGAN — no defence body ‖ **reads per layer (pools):** DS-DEF-8 `viability threatened` / `intact` (card: `treasury · a STATE ORGAN`) ‖ **safe class word:** the purse · the treasury ‖ **source:** `holderTable.js:78-81`, `:115`, `:166-169`; census (`economicViability.viable` → treasury)

=== RT2.1-17
**noun:** office ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: office kind (the record itself), a STATE ORGAN; a citation on it is a FINDING, never a licence ‖ **reads per layer (pools):** none on this desk ‖ **safe class word:** the record — and no citation ‖ **source:** `holderTable.js:84`, `:115`, `:237-239`, `:362-363`, `:488-490`

=== RT2.1-19
**noun:** hospital · infirmary ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: flag `hasHospital` — catches `monastery`, `healer`, `friary` (so "hospital present" can be a friary) ‖ **reads per layer (pools):** DS-DEF-2 Disasters ×5; DS-DEF-6 Medical ×3 ‖ **safe class word:** the infirmary · somewhere for the sick ‖ **source:** `priorityHelpers.js:64`; shipped `Healer (divine, 1st level)`, `Monastery or friary`, `Multiple monasteries`

=== RT2.1-22
**noun:** navy · fleet ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: flag `hasNavy` (`navy`, `major port`) — CONDITIONAL: the defense refuter's enumeration of `institutionalCatalog.js` finds NO shipped member; the general refuter's sweep of `institutionServices.js` finds a `Navy` row (X-D1, §6) ‖ **reads per layer (pools):** DS-DEF-6 Naval Defense reads `blockaded ; navy ; port` ‖ **safe class word:** the ships ‖ **source:** `priorityHelpers.js:62`, `:45`; `foodStockpile.js:417`

=== RT2.1-23
**noun:** road ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: road kind (tokens `terrainType`, `monsterThreat`) — CONDITIONAL: it resolves to institutions (`Listening post` / `Waystation`), so the survey's "a body word for nothing" is wrong as a holder and right as a defence bucket ‖ **reads per layer (pools):** DS-DEF-11 WALLED-THREATENED / QUIET (card: `muster + road`) ‖ **safe class word:** the road (organ) · the country ‖ **source:** `holderTable.js:78-81`, `:233-234`, `:349-355`

=== RT2.1-25
**noun:** the ruling structure ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER (class word) ‖ **reads per layer (pools):** DS-DEF-4 capture ×5 ‖ **safe class word:** the ruling structure · the seat ‖ **source:** `rulingStructure.js:797`

=== RT2.1-33
**noun:** practitioner(s) ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE (plural, a standing condition) — under a MUSTER holder (`magicDependency`) ‖ **reads per layer (pools):** DS-DEF-9 ×6; DS-DEF-6 Magical Capability v2 ‖ **safe class word:** a practitioner · the people it calls on ‖ **source:** `holderTable.js:194`; `defenseInstitutionBuckets.js:105-108`; annex `:3213`, `:2983`

=== RT2.1-34
**noun:** `Town Mayor` · `Elected Reeve` · `Feudal Appointee` ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE words entering as the POWER through `{seat}` (the refuter's A-8) ‖ **reads per layer (pools):** DS-DEF-4 ×5 at render ‖ **safe class word:** — (the register car owes a `seatKind`, or the class word replaces the slot) ‖ **source:** `rulingStructure.js:284`, `:452`

=== RT2.1-36
**noun:** army (the enemy's; the town's abroad) ‖ **layers (HOLDS / CONDITIONAL with condition):** EXTERNAL BODY (`besiegedBy`, muster-kind tokens) or a hypothetical; `{counterpart}` unfilled ‖ **reads per layer (pools):** 6 occurrences ‖ **safe class word:** the besieger (through `{counterpart}` once wired) ‖ **source:** `warStatus.js:289-297`; `holderTable.js:196-197`; annex `:2562`, `:2652`, `:2658`, `:3138`, `:3144`, `:3287`

=== RT2.1-37
**noun:** the line ‖ **layers (HOLDS / CONDITIONAL with condition):** ONE spelling, TWO layers: walls-BODY in DS-DEF-2/5; the MANNED FRONT (a force word) in DS-DEF-7 — rule 3's case inside one desk ‖ **reads per layer (pools):** 19 occurrences ‖ **safe class word:** the wall (DEF-2/5) — never "the line" where the pool reads a force ‖ **source:** annex `:2601-2656`, `:2883`, `:3092-3124`

=== RT2.1-43
**noun:** the purse ‖ **layers (HOLDS / CONDITIONAL with condition):** treasury ORGAN word — on a court/prison read at `:2667` it names an organ the read does not reach ‖ **reads per layer (pools):** DS-DEF-2 `:2667`; DS-DEF-11 fence `:5969` ‖ **safe class word:** the purse (treasury reads only) ‖ **source:** `holderTable.js:166-169`

=== RT2.1-44
**noun:** the town's law · the law ‖ **layers (HOLDS / CONDITIONAL with condition):** a FUNCTION word (safe), sometimes made an agent ("reaches for the purse") ‖ **reads per layer (pools):** `:2661`, `:2667`, `:2763` ‖ **safe class word:** enforcement · the law (function, never agent) ‖ **source:** `safetyProfile.js:281`

=== RT2.1-46
**noun:** the sick-house · infirmary · the parish ‖ **layers (HOLDS / CONDITIONAL with condition):** hospital flag (may be a friary) / parish ORGAN ‖ **reads per layer (pools):** `:2708`, `:2723`, `:3017` ‖ **safe class word:** as hospital / parish rows ‖ **source:** `priorityHelpers.js:64`; `holderTable.js:296-302`

=== RT2.1R-03
**assignment tried (survey):** "the engine's own prose never bakes 'the watch'" ‖ **refuter's verdict:** REFUTED ‖ **evidence:** `safetyProfile.js:274` bakes "The adventurers' charter hall handles threats the watch cannot." under `hasCharterHall` with no `hasWatch` guard; `:281`, `:288`, `:297`, `:307` bake `'The watch'` under `hasWatch`

=== RT2.1R-08
**assignment tried (survey):** "a law expensive at the ROLE layer costs this desk nothing" ‖ **refuter's verdict:** REFUTED ‖ **evidence:** A-8: the desk's only power slot `{seat}` renders `Town Mayor` / `Elected Reeve` (`rulingStructure.js:284`, `:452`) — a role word reaches five DS-DEF-4 pools

=== RT2.1R-11
**assignment tried (survey):** law item 5 as drafted ("until cured a face never uses 'the watch' for a pay-gate read") ‖ **refuter's verdict:** REFUTED as the rule stands ‖ **evidence:** follows the holder table against the gate's producer (M-1) and the estate's synonym instrument; on a walled Town-watch-only town (no Barracks 0.7 × no Citizen militia 0.4 ≈ a quarter of walled towns) it bars the only true body word and licenses "the muster" for a roll the town does not keep (M-3). The refuter's safe form until the `{watchname}` fill lands: the person-plural class word or the gate's own nouns (`wages · pay · purse`), never either institution name

=== RT2.1R-13
**assignment tried (survey):** "INTERESTED reaches the four organs" only ‖ **refuter's verdict:** CONDITIONAL ‖ **evidence:** `holderTable.js:707-708`: `interested` is ALSO true on `corrupt` / `corruptImpairment` for ANY kind — a muster row is interested where its Citizen militia carries a corruption impairment

=== RT2.1R-14
**assignment tried (survey):** 7.1 the `watch PRESENT` citation "licensed only while the town is not captured" ‖ **refuter's verdict:** CONDITIONAL — stricter than stated ‖ **evidence:** `capturedRulingStructure` counts `adversarial` and `equilibrium` as captured (`holderTable.js:141`): the citing player face FAILS from rung 1, not from `corrupted`; and fails where the holder carries any corruption impairment (`:707-708`)

=== RT2.2-07
**noun:** the court ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: `hasCourtSystem` = `courthouse · court buildings · democratic assembly · city hall · town hall`. HOLDER: court kind (services `Criminal trials · Civil disputes · Notary services …`, `dutyNamed: 0`); its FIELD tokens are the SEAT's (`govMultiplier · governanceFractured · breakdown · blocs · stability · recentConflict · termLines · fraying · yearsRemaining`). POWER-eligible: STATE ORGAN; SECURITY body ‖ **reads per layer (pools):** none (DS-GEN-17 ADMINISTERED reads the flag pair) ‖ **safe class word:** the seat where the row is a hall; the court only where the row is a courthouse ‖ **source:** `priorityHelpers.js:55`; `defenseGenerator.js:232`; `holderTable.js:115`, `:215-223`, `:325-329`; `corruption.js:630`

=== RT2.2-09
**noun:** the magistrate · the constable ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER-eligible only (members of `SECURITY_INSTITUTION_RE`) — ZERO shipped rows; unreachable on this desk ‖ **reads per layer (pools):** none ‖ **safe class word:** — ‖ **source:** `corruption.js:630`; roster sweep (263 keys): 0

=== RT2.2-12
**noun:** the reeve · the headman · the steward · the appointee ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (institution rows) that are ALSO holders: `Village reeve` → TREASURY (`Tax collection`, on: false, p 0.7); `Village headman` → ELDERS + TREASURY; `Lord's appointee` → TREASURY; `Lord's steward` in no holder cite — a role word here is a BODY word; there is no person behind it (CONDITIONAL as a general law: `officesOf` also mints role words off `npcs[].role/.title`) ‖ **reads per layer (pools):** DS-GEN-17 (indirectly, the booleans match the rows) ‖ **safe class word:** the body word itself ‖ **source:** `institutionServices.js:20-24`, `:1030-1032`, `:1035`, `:1346-1349`; `holderTable.js:277`, `:336`; `institutionTable.js:377-396`

=== RT2.2-13
**noun:** the treasury ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: treasury kind (STATE ORGAN); roster `Village headman · City administration · Weekly market · City-state government · Lord's appointee · Village reeve · Town hall` ‖ **reads per layer (pools):** DS-GEN-3 viable ×2; DS-GEN-11 ×5 (LICENSED, "a citation of this holder is licensed where the provenance budget allows") — CONDITIONAL flip to INTERESTED only where the town instantiates a treasury holder AND (`criminalCaptureState !== 'none'` or any faction's `captureState`) ‖ **safe class word:** the purse · the treasury ‖ **source:** `holderTable.js:115`, `:166-169`, `:273-277`, `:724-756`

=== RT2.2-14
**noun:** the office ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: office kind (the compiling record); standing ALWAYS `OFFICE`, never LICENSED — a citation "would be citing the speaker"; its `Public records` service registers "deeds, wills, births, and freemen" (a population register the census kind does not hold). STATE ORGAN — and an OFFICE row can NEVER flip to INTERESTED (`sourceOfForTown` returns early) ‖ **reads per layer (pools):** DS-GEN-7 structuralViolations/Suggestions ×2; DS-REL-2 prominentRelationship ×1 ‖ **safe class word:** the record — no citation ‖ **source:** `holderTable.js:84`, `:115`, `:357-363`, `:489`, `:563-575`, `:724-729`; `institutionServices.js:45`

=== RT2.2-15
**noun:** the toll-bar ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: toll-bar kind (`Toll collection · Customs brokerage · Market charter and tolls`; roster `Gates (if walled) · Major Port · Town council`) ‖ **reads per layer (pools):** DS-GEN-6 crossroads/isolated/road ×3; DS-GEN-13 ×4 (`market + toll-bar`) ‖ **safe class word:** the toll ‖ **source:** `holderTable.js:183-185`, `:304-308`; `institutionServices.js:199`, `:1551`

=== RT2.2-16
**noun:** the census ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: census kind (`Citizen registration · Noble registration`; roster `Democratic assembly · Royal seat`) — CONDITIONAL: both services are `on: false, p: 0.5` (a roll on the two carriers); `Democratic assembly` is ALSO a `hasCourtSystem` member ‖ **reads per layer (pools):** NO general-desk pool reads it — though DS-POP-1/-2/-3 are the population blocks; `HOLDER_SOURCES` carries no `population · migration · births · deaths` token at all ‖ **safe class word:** the roll (see the §8.3 question in §6) ‖ **source:** `holderTable.js:164-240`, `:290-294`; `institutionServices.js:891-895`, `:1231-1233`; `priorityHelpers.js:55`

=== RT2.2-17
**noun:** the tradition ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: tradition kind — `rosterBacked: false`, "THE ONE KIND WITH NO INSTITUTION ANYWHERE IN THE SHIPPED ROSTER" ‖ **reads per layer (pools):** none ‖ **safe class word:** nothing citable ‖ **source:** `holderTable.js:339-346`

=== RT2.2-18
**noun:** the road (holder) ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: road kind (`Road register` under `Listening post`, on: true; `Way-bill registration` under `Waystation`, on: false, p 0.6); tokens `terrainType`, `monsterThreat` ‖ **reads per layer (pools):** DS-GEN-12 ×5 (LICENSED) ‖ **safe class word:** the way ‖ **source:** `holderTable.js:233-234`, `:350-354`; `institutionServices.js:405-408`, `:1652-1653`

=== RT2.2-21
**noun:** the granary · the storehouse ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: `hasGranary` (substring `granar`); census civic classes split `store` (the STOCK) from `storehouse` (the BUILDING) ‖ **reads per layer (pools):** DS-GEN-17 PROVISIONED (`hasGranary \|\| hasHospital`, a DISJUNCTION) ‖ **safe class word:** the building or what is in it — never both in one word ‖ **source:** `priorityHelpers.js:63`; `wiringCensus.js:1309`, `:1322`

=== RT2.2-22
**noun:** the hospital · the healer ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: `hasHospital` (`hospital · monastery · healer · friary`; a monastery ALSO sets `hasChurch`) ‖ **reads per layer (pools):** DS-GEN-17 PROVISIONED ‖ **safe class word:** a house that takes in the sick ‖ **source:** `priorityHelpers.js:64`; `defenseGenerator.js:204`; `wiringCensus.js:1315`

=== RT2.2-28
**noun:** `{steading}` · `{ruin}` ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (a satellite place, a fallen one); no holder ‖ **reads per layer (pools):** DS-GEN-8 ×4 ‖ **safe class word:** the named place itself ‖ **source:** `generalStateProse.js:1417`, `:1442`

=== RT2.2-31
**noun:** the mayor · the lord · the sexton · the warden (missed) ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE words inside BODY rows and service descs (`Mayor and council` — NEITHER an elders nor a toll-bar holder; `Lord's appointee`; the sexton in `Register of the dead`'s desc; `Warden's Lodge`); none is an NPC record on this desk ‖ **reads per layer (pools):** none ‖ **safe class word:** — ‖ **source:** `institutionServices.js:491`, `:1030`, `:1035`, `:1069`, `:1413`

=== RT2.2-38
**noun:** `link` + `localRelationshipRole` (`patron` / `client`) ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE — the role THIS TOWN holds in an asymmetric tie, withheld where absent; `overlord`/`vassal` are real values with NO pool; "patron" is ALSO a brokerage POWER and a member of the census civic class `temple` (which is why the pool classes as a temple pool) ‖ **reads per layer (pools):** DS-REL-1 patron / client ‖ **safe class word:** the tie's own word ‖ **source:** `generalStateProse.js:1477-1494`; `canonicalRelationship.js:54-57`, `:143`, `:302-307`; `brokeragePatronage.js:60`; `wiringCensus.js:1312`

=== RT2.2R-02
**assignment tried (survey):** the magistrate · the constable · **the barracks** "exist ONLY as members of `SECURITY_INSTITUTION_RE`; unreachable" ‖ **refuter's verdict:** REFUTED for the barracks ‖ **evidence:** `Barracks` is a shipped row (`institutionServices.js:1555`), a garrison-bucket keyword (`:89`), a `hasGarrison` member (`:46`) and a glyph member (`glyphAssign.js:98`)

=== RT2.2R-04
**assignment tried (survey):** "For those ten [STATE-ORGAN] pools, `sourceOfForTown` will return INTERESTED on any town whose ladder is not `none` … roughly a third of towns" ‖ **refuter's verdict:** REFUTED as stated; CONDITIONAL as corrected ‖ **evidence:** (a) the three OFFICE rows can NEVER flip (`holderTable.js:724-729` returns early on a non-LICENSED base); (b) the seven treasury rows flip only where the town instantiates a treasury holder (`holdersOf` `:590-607`; else `absent: ['holder …']` `:746-756`); (c) the faction check is over ANY faction entry (`:133-137`)

=== RT2.2R-05
**assignment tried (survey):** "A ROLE WORD IN THIS ENGINE IS A BODY ROW, NOT A PERSON" (as a general law) ‖ **refuter's verdict:** CONDITIONAL — holds for the seven named rows on the seven named reads only ‖ **evidence:** `hasInst` is a substring (`defenseGenerator.js:89-90`) reaching `Elder Grove Council` and `Informal elder consensus`; `officesOf` mints role words from `npcs[].role/.title` (`institutionTable.js:377-396`, `COLUMN_SOURCES.office` `read: true` `:124-127`)

=== RT2.3-01
**noun:** `{institution}` on DS-ECO-11 (exploitation lens) ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY only — a chain house from `leading.row.processingInstitutions[0]` (16 distinct: Alchemist shop · Apothecary · Butcher · Caravanserai · Fishmonger · Glassblower · Market · Mill · Mine · Potter · Salt works · Sawmill · Stone quarry · Tannery · Vintner · Weavers' guild) — CONDITIONAL on `leadingExploitation` returning a row and `properFill` accepting it. Overlaps: `Alchemist shop` is a `magicDef` DEFENCE-bucket keyword; `Weavers' guild` fires `hasGuild` (which also fires on `Thieves' guild`); `Market` collides with the market KIND and civic class ‖ **reads per layer (pools):** DS-ECO-11 EXPLOITATION (the census reads `leading.bucket`, `leading.row`) ‖ **safe class word:** the workings · the house ‖ **source:** `economyStateProse.js:746-767`, `:320-332`, `:991`; `resourceGenerator.js:175-177`; `resourceChains.js` (mineralHot); `defenseInstitutionBuckets.js:110`; `priorityHelpe

=== RT2.3-05
**noun:** the granary ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: `hasGranary`; the chain house `Town granary` / `Granary`; the `storehouse` class — vs a READING that is not a body (the STOCK band; three capacity rows keyed on the name). A `granary` holder row was WITHDRAWN. The BUILDING is never read by the desk ‖ **reads per layer (pools):** DS-ECO-2's reads are `granary.band` etc — the STOCK (layer NONE) ‖ **safe class word:** the stores · what the town holds back (the `store` class) ‖ **source:** `dossierViewModel.js:336`; `priorityHelpers.js:63`; `foodStockpile.js:185-195`; `wiringCensus.js:1309`, `:1322`; `holderTable.js:157-159`

=== RT2.3-09
**noun:** the hall · the halls ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (the `hall` civic class) + HOLDER (court kind's `Courthouse`; `Town hall` = treasury + office) + POWER (the seat, in "dictate to the hall") ‖ **reads per layer (pools):** DS-ECO-12 v3's read is `eco.incomeSources` → TREASURY, a different organ ‖ **safe class word:** the town's own reckoning (treasury); the ruling structure (the seat) ‖ **source:** `wiringCensus.js:1314`; `holderTable.js:272-279`, `:325-331`, `:357-364`

=== RT2.3-13
**noun:** the warehouses · the workshops · the yard · the stalls · the house · the quarter · the workings · the stores ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (`storehouse`, `craft`, `care` civic classes; chain houses) — `stall` is the MARKET kind's own ratified noun (HOLDER); `healing` is a CATEGORY key; `the workings` has no engine row (deliberately); `the stores` is the `store` class (a stock word) ‖ **reads per layer (pools):** DS-ECO-12 v3 / DS-ECO-10 (`stall`, market kind); DS-SUP-3 (`readings.notableAbsences` — CATEGORY keys, layer NONE); the rest unwired ‖ **safe class word:** the workings · the stores (already the desk's safe generics) ‖ **source:** `wiringCensus.js:1309`, `:1315`, `:1316`, `:1322`; `fieldSynonyms.js:83`; `servicesDisplay.js:15-21`; `economyDeskRead.js:108`

=== RT2.3-14
**noun:** the books · the accounts · the ledgers · the rolls · the returns · the duties · fees / a licence · the survey · the counts · the purse / the chest (record words) ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER-layer SURFACES against the engine's own ratified table: **books** = treasury · parish · toll-bar · market · road · office; **rolls** = muster · census · watch · court (ON-ORGAN on DS-ECO-6 ≥15 v1, the watch kind's `roll · rolls · watch`); **stall** = market; **toll** = toll-bar; **counts** = census; **purse · chest** = treasury (UNUSED on the desk). NOT ratified for any kind: **accounts** (9 lines), **returns**, **ledgers** (`ledger` WITHDRAWN as a token), **duties** (NOT in `DUTY_SERVICE_KINDS`; the only catalog "Duties" is `Customs house`'s "Duties and tariffs", a house in no `HOLDER_RECORDS` cite — the survey's route REFUTED, the conclusion stands). **survey** = the office kind (a citation is a FINDING, arm A13's OFFICE limb). The `[ledger]` ANGLE names the

=== RT2.3-15
**noun:** a stranger (×42) · the crier (×5) · a clerk / the clerks (×3) · the men · Merchants · [elder] ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE-shaped words with no read behind them: *a stranger* is the `[visitor]` ANGLE's standpoint, not a role (OWED Q2); *the crier* is an authored COPY table (`CRIER_FRAMES`), not an office; *the clerks* the `[ledger]` persona (at `:1080`, `:1122`, `:1649` — the survey's DS-ECO-6 ≥30 v1 location is wrong); *the men* a force-register idiom on ratepayers; *Merchants* a FROZEN component literal (`EconomicsTab.jsx:664`, not `economicDragDesc`), unmarked, player-audible; `[elder]` an ANGLE and separately a holder kind. NONE of ADDENDUM 11's specimen role words appears in the desk's 355 variant lines ‖ **reads per layer (pools):** none — no economy read touches `settlement.npcs` ‖ **safe class word:** — ‖ **source:** annex `:122`, `:124`, `:125`, `:1080`, `:1116`, `:1122`, `:1649`; `marketPrices.js:228-239`; `EconomicsTab.jsx:664`; `economy.

=== RT2.3-16
**noun:** the road · the roads (missed noun, 31 occurrences) ‖ **layers (HOLDS / CONDITIONAL with condition):** as a PLACE word no layer; but the ROAD holder kind's NAME (nouns `book · books`), the `road` civic class, and the `{access}` value one hop from a toll-bar field — on the seven TERRAIN pools (road-kind after the `terrainKey` repair) and DS-ECO-1, a refuter needs a ruling: route (safe) or record (a citation) ‖ **reads per layer (pools):** DS-ECO-11 TERRAIN ×7; DS-ECO-1 ×5 ‖ **safe class word:** the road as a route only ‖ **source:** `holderTable.js:233-234`, `:350-356`; `fieldSynonyms.js:88`; `wiringCensus.js:1313`

=== RT2.3-17
**noun:** the hands (as a POWER) · one name ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER named through an idiom to dodge the dark `{faction}` ‖ **reads per layer (pools):** DS-SUP-2 CAPTURED v2 `:1337`, v3 `:1338` (NO PRODUCER) ‖ **safe class word:** — ‖ **source:** annex `:1335-1338`

=== RT2.3-19
**noun:** `rank` · `foodBalance.*` · `granary.*` (band) · `readings.flowDrift.band` · `text(terrainKey)` · `readings.notableAbsences` · `leading.bucket` ‖ **layers (HOLDS / CONDITIONAL with condition):** layer NONE — no institution row, no holder kind (the survey's BODY tag on these is REFUSED); `text(terrainKey)` becomes HOLDER (road) after a one-token repair (`terrainKey` → `terrainType`) ‖ **reads per layer (pools):** DS-ECO-1 ×5; DS-ECO-2 ×7; DS-ECO-3; DS-ECO-11 ×7 + ×4; DS-SUP-3 ×6 ‖ **safe class word:** no institution-class noun of any layer ‖ **source:** `economyStateProse.js:961`, `:976`; `holderTable.js:157-159`, `:233`; `economyDeskRead.js:108`; census `source.kind ''`

=== RT2.3-22
**noun:** `eco.foodSecurity.stockpile.blockaded` · `.blockadeBypass` ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER (toll-bar) — not a body, NOT an organ (`toll-bar ∉ STATE_ORGAN_KINDS`); CONDITIONAL rider: ANY holder can be INTERESTED world-run through a corruption impairment written on an NPC's home institution of any kind (O-R4) ‖ **reads per layer (pools):** DS-ECO-9 ×2 ‖ **safe class word:** `book · books · toll` ‖ **source:** `foodStockpile.js:417-418`; `holderTable.js:115`, `:304-310`, `:660-661`, `:671`, `:685`; `corruptionImpair.js:111`; `mutateEntities.js:831`

=== RT2.3-23
**noun:** `readings.exportPosture.status` · `analysis.economicStrengths` · `.strategicValue` · `eco.isEntrepot` · `.localProduction` · `.primaryExports` · `.primaryImports` ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER (market) — not a body, not an organ; the same O-R4 rider ‖ **reads per layer (pools):** DS-ECO-10 ×6; DS-ECO-11 ×3; DS-ECO-12 ×5 ‖ **safe class word:** `book · books · stall` ‖ **source:** `holderTable.js:170-180`, `:311-317`

=== RT2.3R-03
**assignment tried (survey):** "BODY, cited to an organ" on `stockpile.blockaded/.blockadeBypass`, `exportPosture.status`, `economicStrengths`, `strategicValue`, `eco.*` ‖ **refuter's verdict:** REFUTED on both words — HOLDER (toll-bar / market), which are NOT state organs ‖ **evidence:** `holderTable.js:115`; `standingOf` prints "toll-bar is not one of the state's own organs" (`:685`)

=== RT2.4-07
**noun:** the court ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY: `Courthouse`, `Multiple court buildings`. HOLDER: court kind, STATE ORGAN (holders measured = those two). POWER-eligible: SECURITY body; civic class `law`. **The word means two things on this desk**: the law court (the holder) and the political ASSEMBLY (DS-POW-7 `a fully divided court`, `objectClass: law`) — the engine types only the law court ‖ **reads per layer (pools):** HOLDER: 12 of the desk's 14 LICENSED rows (court 10, court + watch 2) · BODY: none ‖ **safe class word:** at the assembly sense the `{seat}` / the hall's own combination; at the record sense a CITATION, never a subject ‖ **source:** `institutionServices.js:89`, `:1117`; `holderTable.js:78`, `:115`, `:325-330`; `corruption.js:630`; `wiringCensus.js:1311`

=== RT2.4-09
**noun:** the workshops · the tables · the clearinghouse · the temple / `{institution}` · the prison / the gaol ‖ **layers (HOLDS / CONDITIONAL with condition):** the workshops: BODY (`Craft Guild District`; class `craft`) on pools DARK by declaration; the tables: BODY (`Gambling halls`) on a `name` read; the clearinghouse: NO ENGINE ROW ANYWHERE (an invention); the temple / `{institution}`: the §0c slot named by ONE variant of forty and never filled (`slotsWithoutProvider`); the prison: BODY (`Small prison/stocks`, `Large prison`; class `law`), not rendered — and `'Prison Warden'` is an emitted power ROLE ‖ **reads per layer (pools):** none (the economicBase pools read `[]`; `unlicensed revenue` v3 reads `name`; DS-POW-5 theocracy drops) ‖ **safe class word:** the role label; none for the rest ‖ **source:** `institutionServices.js:134`, `:941`, `:1635`, `:1640`; `powerStateProse.js:665-701`; `wiringCensus.js:1311`, `:1316`; `historyData.js:148`; `npcGenerator.js:1465`

=== RT2.4-10
**noun:** the muster ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER-ORGAN ONLY: muster kind; exactly ONE roster holder (`Citizen militia`, "the sharpest wiring debt this table found"); its FIELDS are walls · garrison · militia · mercenary · charter · force · economicGates · besiegedBy · besiegingTargets · ticksToDeploy · stretchedThin. The desk's single occurrence is the VERB (DS-POW-4 "what {faction} can muster for itself") — a reverse alias trap ‖ **reads per layer (pools):** NONE (no muster-kind read on the desk) ‖ **safe class word:** the muster (as a noun, on a muster-kind read only) ‖ **source:** `holderTable.js:78`, `:188-199`, `:280-288`; `institutionServices.js:835`

=== RT2.4-11
**noun:** the rolls · the records · the accounts · the books · the returns · the manifests · the minutes · the writ ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER-layer SURFACES (the office's own formula); `office` is "the compiling record itself" and a citation on it is a FINDING; the `ledger` token WITHDRAWN. *the writ* (DS-POW-1 Endorsed v3, Contested v2) is a court/office INSTRUMENT surface with no engine row ‖ **reads per layer (pools):** DS-POW-1 (rolls ×3, records, accounts ×3, returns), DS-POW-2 (rolls), DS-POW-4 (rolls ×3, records ×3), DS-POW-5 (books), DS-POW-6 (rolls, record, returns, books, manifests, minute) — leaf counts roll 8 · record 7 · account 6 · return 2 · book 2 · manifest 1 · minute 1 ‖ **safe class word:** the surface is lawful (R-vi / W7); the roll as an AGENT-SOURCE is not ‖ **source:** `holderTable.js:74-77`, `:159-161`, `:271-364`, `:361-362`

=== RT2.4-13
**noun:** the combination · the bloc · the majority ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER: the `Bloc` typedef (`{id, members[], glue{type,detail}, end, strain, sinceTick, covert?}`); the glue `patronage` is PEOPLE-HELD and dies at succession — a ROLE-layer fact inside a POWER row ‖ **reads per layer (pools):** `politics.blocs` — DS-POW-7 `layer DORMANT` and `an opposition bloc forms COVERT…` (the only two RESOLVED of twenty) ‖ **safe class word:** the combination (the shipped word: names the typed bloc without claiming an institution) ‖ **source:** `settlementPolitics.js:63-67`, `:423-457`, `:461`, `:549`, `:604`, `:792-803`

=== RT2.4-15
**noun:** the office (missed noun, 5 uses) ‖ **layers (HOLDS / CONDITIONAL with condition):** FIVE registers: a HOLDER KIND; the `'OFFICE'` register STANDING; a civic-class member of `hall`; the seat-as-office in the desk's own prose (DS-POW-4 Contested v1, Critical v1; DS-POW-7 end seats v1, end doctrine v1); a faction-name word (`Conversion Enforcement Office`) ‖ **reads per layer (pools):** none rowed ‖ **safe class word:** — (the desk's most layer-loaded unrowed noun) ‖ **source:** `holderTable.js:80`, `:87`; `wiringCensus.js:1314`; `stressFactions.js:148`

=== RT2.4-16
**noun:** the clerks · the officials · an operator · a leader · the person who speaks for {faction} · the incumbent / the challenger · a ruler ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE words — the survey tested them against the roster and the holder kinds (the BODY/HOLDER instrument), not against `officesOf` (the ROLE instrument): the engine EMITS `'City Clerk'`, `'Corrupt Official'` (a role NAME that bakes a corruption STANDING into a public roster string), `'Kingpin'`/`'Lieutenant'`, and `leaderNpcId`/`leaderName` are typed and UNREAD by the desk; `contenders.incumbent{name, gated, govMultiplier}` / `challengers[]{name, archetype, power, weight}` arrive as a caller READING; *a ruler* is the generic of the `{seat}` fill (a maxim frame when stated as "a ruler with the town behind them can do things") ‖ **reads per layer (pools):** none by field: the ACT charges (record · name · chase · agreeable · speaks) stand under law 1 regardless ‖ **safe class word:** a ROLE states a

=== RT2.4-18
**noun:** the reeve · the headman · the mayor · the priest · elder · the captain ‖ **layers (HOLDS / CONDITIONAL with condition):** inside `{seat}` fills, never as bare nouns (`'village reeve' → 'Elected Reeve'`, `"Headman's Authority"`, `'Priestly Guidance'`, `'Town Mayor'`) — AND emitted NPC ROLES (`'Reeve'`, `'Mayor'`, `'Elder'` the thorp/hamlet mandated role); *elder* is ALSO an angle tag and a holder kind (four registers); **the captain**: zero occurrences on the desk BUT the engine EMITS `'Guard Captain'`, `'Watch Captain'` (with a typed institution link), `'City Watch Chief'`, `'Garrison Commander'` — ADDENDUM 11's specimen IS an engine role word; *the factor* is not emitted ‖ **reads per layer (pools):** `{seat}` on DS-POW-1/2/4/6/7 ‖ **safe class word:** — (a role word can arrive INSIDE the body slot) ‖ **source:** `rulingStructure.js:164`, `:259-260`, `:284`; `historyData.js:21`, `:119`; `npcGenerator.js:1512-1536`; `factionRoles.js:45`, `:215-233`; `roleCategory.js:33`, `:38

=== RT2.4R-03
**assignment tried (survey):** "the captain · the factor — not part of this desk's vocabulary; captain matches no roster row and no holder kind" ‖ **refuter's verdict:** REFUTED for the captain (the engine emits it) ‖ **evidence:** `'Guard Captain'`, `'Watch Captain'` (typed link), `'City Watch Chief'`, `'Garrison Commander'` (`npcGenerator.js:1514-1536`; `factionRoles.js:45`); `captain` in `roleCategory.js:38`

=== RT2.5-04
**noun:** soldiers · officers (1 face) ‖ **layers (HOLDS / CONDITIONAL with condition):** `soldier` a `force`-class member; no roster row spelled "soldier"; "officers" NO engine row (though `sergeant`, `commander`, `marshal` are role words the engine matches) ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the muster ‖ **source:** `wiringCensus.js:1303`; `npcGenerator.js:1018-1025`

=== RT2.5-06
**noun:** army (2) ‖ **layers (HOLDS / CONDITIONAL with condition):** no institution row; `NAME_RULES` MILITARY includes `army`; the condition template `army_deployed` says "standing army … home garrison" — CONDITIONAL: the engine speaks of a standing army as a DESCRIPTION string, never a row ‖ **reads per layer (pools):** NONE ‖ **safe class word:** — (a campaign is DS-WAR-1's) ‖ **source:** `factionArchetypes.js:79`; `activeConditions.js:342`

=== RT2.5-12
**noun:** treasury (2) ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: treasury kind, STATE ORGAN (fields `incomeSources · viable · criticalIssueCount · economicViability`) ‖ **reads per layer (pools):** NONE — rendered twice, once as an AGENT ("a drowning treasury", which is VERBATIM the record's own `reason` string) ‖ **safe class word:** the treasury (organ) — never as an agent ‖ **source:** `holderTable.js:78`, `:115`, `:166-169`; `stressorDynamics.js:844`

=== RT2.5-14
**noun:** roads · road (2) · routes · arteries (missed) ‖ **layers (HOLDS / CONDITIONAL with condition):** the `road` class (`route` a token); HOLDER: road kind (`Road register · Way-bill registration`) ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the road ‖ **source:** `wiringCensus.js:1313`; `holderTable.js:78`, `:234-235`, `:349-355`; annex 4143, 4280, 4451-4452

=== RT2.5-15
**noun:** workshops (1) · workroom (missed) ‖ **layers (HOLDS / CONDITIONAL with condition):** the `craft` class; a `Cartographer's workshop` row; the arcane BODY class is BUILDINGS (`sanctum · college · conclave …`) and the `magicDef` bucket ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the workshops ‖ **source:** `wiringCensus.js:1316`; `stressorDynamics.js:53`; `defenseInstitutionBuckets.js:105-108`

=== RT2.5-17
**noun:** houses (merchant sense, 2) ‖ **layers (HOLDS / CONDITIONAL with condition):** `NAME_RULES` NOBLE (`\bhouse\s+[a-z]`) and MERCHANT (`bank`, `broker`, `guild`); a brokerage HOUSE has a typed PATRON — a different object ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the merchant power (the archetype class word) ‖ **source:** `factionArchetypes.js:81`, `:84`; `brokeragePatronage.js:60`

=== RT2.5-18
**noun:** institutions · institution (4) ‖ **layers (HOLDS / CONDITIONAL with condition):** the generic for `settlement.institutions[]` ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the institutions (asserts no row) ‖ **source:** —

=== RT2.5-19
**noun:** healers (1) ‖ **layers (HOLDS / CONDITIONAL with condition):** the `care` class names `healer` as a CIVIC OBJECT — AND `'healer'` is an NPC role token, `Healer (divine, 1st level)` a village catalog row, and `'Healer'` the stress-mandated role for famine / plague / migration (the survey's "no engine row" REFUTED) ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the infirmary (body) ‖ **source:** `wiringCensus.js:1315`; `npcGenerator.js:1035`, `:1523`, `:1529`, `:1533`; `institutionalCatalog.js:845`

=== RT2.5-21
**noun:** rolls (3) · registers (verb) · books (2) · accounts (2) · ledgers (1) · seal(s) (2) · writ (1) · licence / permission (2) · tax / levy / levies (6) · quarantine (1) · lists / tally / bill (missed) ‖ **layers (HOLDS / CONDITIONAL with condition):** RECORD words of organs the desk never resolves: rolls → census (`DUTY_SERVICE_KINDS` names `rolls?`); books / accounts → treasury / office; `ledger` a WITHDRAWN token; tax / levy → treasury AND toll-bar; `quarantine` no record at all (a Defense-tab display posture); lists → the watch's `Missing persons` / the office's `Record filing` ‖ **reads per layer (pools):** NONE (standing SOURCE-UNRESOLVED on every row) ‖ **safe class word:** the read's own noun, never a register ‖ **source:** `holderTable.js:157-162`, `:274`, `:289-295`, `:305`, `:319`, `:358`; `institutionTable.js:106`; `defenseDisplay.js:25-42`; annex 4075-4455

=== RT2.5-22
**noun:** "another power" / "an outside power" (2) ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER, class word only — the positive exemplar P1 ‖ **reads per layer (pools):** `token === occupied` (a condition of the town) ‖ **safe class word:** **"another power"** IS the safe class word ‖ **source:** annex 4087, 4102

=== RT2.5-23
**noun:** authority (5) ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER generally; `stability` → court kind HOLDS; `publicLegitimacy` → court REFUTED (not a holder token; a `PowerStructure` typedef property) ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the ruling structure ‖ **source:** `holderTable.js:215-224`; `rulingPower.js:90`

=== RT2.5-24
**noun:** government / governments (7) · the ruling power (1) · the seat (12) ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER: `powerStructure.government` / `governingName`; GOVERNMENT archetype; `governingFactionOf` — the NAME slot exists and is UNREAD by every desk pool ‖ **reads per layer (pools):** NONE ‖ **safe class word:** the ruling structure · the seat ‖ **source:** `rulingPower.js:88`, `:131-133`, `:224-229`; `factionArchetypes.js:54`, `:87`

=== RT2.5-25
**noun:** ruler / rulers (2) ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER typed by NAME on the coup record (`incumbent.name`, stamped at birth) and UNREAD; the engine's own hook says "Officers loyal to the **seat**" — the face's "ruler" is the writer's; a PERSON-shaped word for a FACTION-typed object ‖ **reads per layer (pools):** `variant === barracks_coup` (`:4340`), `temple_putsch` (`:4349`) ‖ **safe class word:** the seat ‖ **source:** `stressorDynamics.js:690`, `:894`; `rulingPowerCoup.js:118-123`

=== RT2.5-26
**noun:** occupier (2) ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER — CONDITIONAL BY RECORD: DS-STR-1 `occupied` is a BIRTH token and `worldState.occupations` (the only source of `occupierName`) is written ONLY by the world run — at birth there is NO name to wire (the survey's "one field away" REFUTED for DS-STR-1; CONDITIONAL for DS-CND-1's occupation family); the occupier is a SETTLEMENT id, not a faction (B-11) ‖ **reads per layer (pools):** `token === occupied`; `FAMILY: occupation layer` (dark) ‖ **safe class word:** "the occupier" is safe as a CLASS word even where the name is null ‖ **source:** `stressGenerator.js:46-58`; `occupationStatus.js:87-96`; `pulseKernel.js:1150`

=== RT2.5-27
**noun:** creditor / creditors (2) · sponsor (2) · faction / factions (2) · religious authority (1) · the houses that finance (1) · neighbour (missed) · conspiracy / conspirators / the plot (missed) · resistance / armed movement (missed) ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER words: creditor — no typed field on the DESK's records (a world-run `creditorId` exists elsewhere); sponsor — `sponsorSettlementId` typed and UNREAD; factions — `powerStructure.factions[]` / `nameOf`, UNREAD; religious authority / the houses — the archetype reaching the variant through `COUP_VARIANT_BY_ARCHETYPE`, the NAME `contenders[0].name` UNREAD; neighbour — a SETTLEMENT (`hostileNeighborsOf`; `attackerSettlementId`), the `{counterpart}` shape; the conspiracy — the coup's challenger FACTION, typed as `originContext.contenders[]` (the engine's own hooks say "the conspirators"); the resistance — a SCALAR on the occupations ledger and the `occupation_resistance` archetype, NO institution row ‖ 

=== RT2.5-28
**noun:** clerks (3) · collector(s) (3) · officers (1) · administrators (1) · scouts (1) · handler / asset (1) ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE words with NO engine row: not in `FACTION_ROLES`, `ROLE_FACTION_MAP`, `NAME_RULES` or any class list (`administrators` — the BODY `City administration` exists; scouts and the handler are the engine's own HOOK / REASON strings) ‖ **reads per layer (pools):** NONE ‖ **safe class word:** — (none safe on this desk) ‖ **source:** `npcGenerator.js:1002-1060`; `factionRoles.js:40-60`; `npcProfile.js:158`; `stressorDynamics.js:679`, `:763`

=== RT2.5-29
**noun:** overseers (2) · officials (1) · healers (1) · mages / casters / hedge wizards (4) · captain · reeve · elder · priest · factor (0 uses) ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE words the engine EMITS or MATCHES (the survey's "none" REFUTED): `'overseer'`, `'official'`, `'healer'`, `'factor'`, `'reeve'`, `'captain'`, `'priest'`, `'elder'`, `'wizard' · 'mage' · 'archmage' · 'sorcerer' · 'hedge wizard'` are all `ROLE_FACTION_MAP` tokens; `NAME_RULES` MILITARY carries `captain\|sheriff\|warden\|ranger\|sentinel`, GOVERNMENT `reeve\|steward\|elder`. None binds an institution (`holderRole` null stands); the AGENCY given them is the finding, not the word ‖ **reads per layer (pools):** NONE ‖ **safe class word:** — ‖ **source:** `npcGenerator.js:64-72`, `:1004`, `:1006`, `:1015`, `:1017`, `:1027`, `:1035`, `:1040-1049`, `:1054-1055`; `factionArchetypes.js:79`, `:87`

=== RT2.5-30
**noun:** heir (missed) · the person responsible · somebody it had reason to trust · Somebody … answers elsewhere (missed) ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE (heir: no row; nearest record `dominant_npc_removed` "succession is unresolved") and PERSON referents (the second and third explicit persons beside F18) ‖ **reads per layer (pools):** `token === succession_void`; `RECENTLY BETRAYED`; `INFILTRATED` ‖ **safe class word:** — ‖ **source:** `activeConditions.js:306-308`; annex 4114, 4122, 4137

=== RT2.5-31
**noun:** collaborators / patriots · commons / households / population / people (23) · the men · hands · the street / the crowd · badge · the works ‖ **layers (HOLDS / CONDITIONAL with condition):** collective-person agents; "a totality over persons" is refused on every card; "The streets rose on their own" is the engine's own `popular_revolt` reason ‖ **reads per layer (pools):** NONE ‖ **safe class word:** — ‖ **source:** annex 4079, 4091, 4151, 4160-4162, 4178, 4330, 4335-4336, 4356, 4364, 4369, 4374; `stressorDynamics.js:850`

=== RT2.5-36
**noun:** the other reads: `token (via CRISIS_POOL_OF)` ×15 · ARITY ×2 · `worldStressor.lifecycleStage` ×5 · COUNTERFORCE / SYNERGY (dark) ×10 · `condition.severity` ×4 · DIRECTION ×2 · PROVENANCE ×2 · DURATION ×1 · FAMILY (dark) ×6 ‖ **layers (HOLDS / CONDITIONAL with condition):** a CONDITION, a BAND, a STAGE, a WINDOW of a record — no institution, organ, power or role in the READ (CONDITIONAL: the record's `description` / `hooks` / `reason` strings carry the engine's own body and person words; `{reason}` on COUNTERFORCE will print `${key} institutions` in the world's `INSTITUTION_CLASSES` vocabulary once wired — B-9) ‖ **reads per layer (pools):** 47 pools ‖ **safe class word:** the condition's own word ‖ **source:** `stressorsStateProse.js:98-117`, `:160`, `:170`, `:338-352`, `:388-397`; `stressorsCore.js:34-42`, `:314-322`; `activeConditions.js:493`, `:541-547`, `:636-643`, `:670`; `stressorDynamics.js:47-55`, `:386-391`

=== RT2.5R-04
**assignment tried (survey):** `hasWatch` "town tier and up only (`:48`)" ‖ **refuter's verdict:** CONDITIONAL ‖ **evidence:** `:48` is a NAME list; the tier fact is the catalog's seating (`institutionalCatalog.js:1348`, `:1918`); a custom roster can seat a watch at any tier

=== RT2.5R-08
**assignment tried (survey):** warehouse "no body row" · schools / taverns "no roster row" · healers "no engine row" · overseers / officials / factor / mages "none" ‖ **refuter's verdict:** REFUTED ‖ **evidence:** `priorityHelpers.js:60`; `institutionalCatalog.js:845`, `:981`, `:1002`, `:1527`; `npcGenerator.js:1015`, `:1035`, `:1040-1049`, `:1054-1055`, `:1523-1533`

=== RT2.5R-13
**assignment tried (survey):** O-1 "at thorp/hamlet/village the word names nothing at all" ‖ **refuter's verdict:** CONDITIONAL — refuted as a totality ‖ **evidence:** `safetyProfile.js:30-35` prints `'local watch'` below town whenever `hasWatch` resolves

=== RT2.5R-17
**assignment tried (survey):** F1, F5, F6, F12, F21, F22, F26 as "invented" body/role words ‖ **refuter's verdict:** CONDITIONAL — the layer mismatch stands; the WORDS are the record's own hook / reason strings ‖ **evidence:** `VARIANT_HOOKS` `stressorDynamics.js:657-729` (`:679`, `:689`, `:690`, `:701`, `:705`, `:706`, `:717`), rides on the record as `originContext.hooks[]` (`:738`); `reason` (`:844`)

=== RT2.5R-18
**assignment tried (survey):** F10 (c) "patrols is not a watch service" ‖ **refuter's verdict:** HOLDS but incomplete ‖ **evidence:** `patrol` IS a `force`-class civic token (`wiringCensus.js:1303`) — B-2

=== RT2.6-01
**noun:** the army · the field force ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER-adjacent only (whose army); NO BODY row; `army` not in the `force` civic class ‖ **reads per layer (pools):** `war.status.besiegingTargets.length` → `statusLabel: On campaign` (holder muster) ‖ **safe class word:** the muster ‖ **source:** `holderTable.js:197`; `wiringCensus.js:1303`

=== RT2.6-02
**noun:** the soldiers · the men ‖ **layers (HOLDS / CONDITIONAL with condition):** a person-plural; the card's refused column "a totality over persons" ‖ **reads per layer (pools):** `war.occupation` (`occupation.pays *`), `war.occupierPosition` ‖ **safe class word:** the muster (ours); **"the occupier's men" / "the soldiers"** (theirs — W12); NOT "{counterpart}'s garrison" (REFUSED) ‖ **source:** `occupationStatus.js:95-103`; ADDENDUM 13 W12

=== RT2.6-04
**noun:** the banner ‖ **layers (HOLDS / CONDITIONAL with condition):** the engine's own word for the `relational` clause (`'the compelled banner'`) — CONDITIONAL: the 24 `<family> · <state>` rows carry NO reads and are SOURCE-UNRESOLVED; the court licence sits on the four `fraying` / `document-level` rows ONLY ‖ **reads per layer (pools):** `relational · <state>` (unread) ‖ **safe class word:** the banner on a `relational` term — licensed by no read on the family pools ‖ **source:** `treatyDocument.js:57-61`; the census dump

=== RT2.6-10
**noun:** the watch ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (bucket) AND HOLDER (the watch kind, a STATE ORGAN; roster `Professional city watch` / `Town watch`) AND SECURITY body AND `force` class AND `military` role keyword (`watch chief`, `city watch`) AND a `FACTION_ROLES` key; `professional city watch` in both buckets ‖ **reads per layer (pools):** NONE — no warFaith row resolves the watch kind; the noun is ABSENT from the desk's corpus (the three "the watch" hits are "the watching") ‖ **safe class word:** no safe use on this desk ‖ **source:** `defenseInstitutionBuckets.js:90`, `:95-97`; `holderTable.js:79`, `:115`, `:202-212`, `:322`; `corruption.js:630`; `wiringCensus.js:1303`; `roleCategory.js:39`; `factionRoles.js:44`; `warFaith.generated.js:2447`, `:2461`

=== RT2.6-16
**noun:** the ruling structure · the government ‖ **layers (HOLDS / CONDITIONAL with condition):** POWER — `mandateGovWeight`'s regex (theocracy · monarch / feudal / autocra / imperial / … / king / queen / emperor); the reader is GOVERNMENT-SCOPED and returns `null` for a merchant council or republic; the class word and the faction NAME are the same string ‖ **reads per layer (pools):** `faith.mandate.phrase` → the three `MANDATE:` pools ‖ **safe class word:** the ruling structure ‖ **source:** `religionState.js:686-691`, `:789`; `rulingStructure.js:787`, `:792`; `warFaithStateProse.js:580-583`

=== RT2.6-21
**noun:** the rolls · the books · the ledger · the records ‖ **layers (HOLDS / CONDITIONAL with condition):** the `[ledger]` ANGLE is a STANDPOINT ("the clerk's view — what the books, rolls and counts show"); `ledger` WITHDRAWN as a holder token; the angle palette's own names (`ledger`, `elder`) are HOLDER-kind vocabulary, which is how the shipped prose slides from the standpoint into the organ (F-7) ‖ **reads per layer (pools):** every pool carries an angle ‖ **safe class word:** the record / the books as a STANDPOINT only — never as a holder ‖ **source:** annex `:122`; `holderTable.js:79-80`, `:157-161`

=== RT2.6-22
**noun:** the elders ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: elders kind (`Record of custom`; roster `Household elder · Village elder · Village headman · Town council`); a `government` ROLE keyword; the `[elder]` ANGLE (20 variants on this desk); the engine EMITS "the village elders" / "the household heads" as a council label on small tiers (elsewhere) ‖ **reads per layer (pools):** NONE (the angle only) ‖ **safe class word:** the `[elder]` angle only — never "the elders" as a body ‖ **source:** `holderTable.js:79`, `:333`, `:336`; `roleCategory.js:34`; annex `:125`; `governanceNarrative.js:101-105`

=== RT2.6-23
**noun:** the parish ‖ **layers (HOLDS / CONDITIONAL with condition):** HOLDER: parish kind (`Register of the dead · Central register · Records`; roster `Parish burial grounds · Cemetery network · Parish church`); `parish` also a `temple`-class word; `Parish church`'s `Records` is on: false p 0.5 and `Parish churches (2-5)` offers `Record keeping`, which is DELIBERATELY in no kind's list — CONDITIONAL: "a town-tier town holds the parish record only through its burial grounds" is unverified per tier (PLAUSIBLE) ‖ **reads per layer (pools):** `faith.piety.trend` + `faith.unaffiliated` → the two `SINK:` pools (LICENSED) ‖ **safe class word:** the parish — for a piety / sink read only ‖ **source:** `holderTable.js:79`, `:226-227`, `:257-259`, `:296-302`; `institutionServices.js:1161-1170`, `:1411-1428`; `wiringCensus.js:1312`

=== RT2.6-26
**noun:** the term · the clause (`{term}`) ‖ **layers (HOLDS / CONDITIONAL with condition):** an OBLIGATION on the instrument (catalog types labelled at `:398-430`; 13 families; the corpus authored 7 + a floor — CONDITIONAL: the count 26 not re-counted); the LABEL TABLE fills the proper slot with `installed seat`, `temple restitution`, `pilgrim's road`, `right of mission`, `shared rite` — a seat word, a temple word and a road word arriving THROUGH the slot (A-4) ‖ **reads per layer (pools):** `term`, `term.family`, `term.complianceState` → the 24 `<family> · <state>` pools ‖ **safe class word:** the clause / the term ‖ **source:** `peaceTermsCatalog.js:159-322`, `:328`, `:398-430` (`:408`, `:416-418`, `:420`); `warFaithStateProse.js:323-326`, `:379-389`, `:798`

=== RT2.6-30
**noun:** the parish church · the graveyard / burial ground / cemetery network ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY and (coin-flip) ORGAN: `Parish church`'s `Records` on: false p 0.5; the burial rows are the RELIABLE parish holders (`Register of the dead` on: true p 0.8; `Central register` on: true p 0.9) ‖ **reads per layer (pools):** the resolution of a `parish` citation on the two SINK pools ‖ **safe class word:** the parish register ‖ **source:** `institutionServices.js:1161-1166`, `:1411-1416`, `:1423-1428`

=== RT2.6-32
**noun:** the congregation · the faithful · the households ‖ **layers (HOLDS / CONDITIONAL with condition):** a person-plural (the card's refused column); banded by `faith.ranks[].share`; `whoIsCounted` is OPEN BY LAW ("the population is a NUMBER and no roll of persons exists anywhere in the engine") ‖ **reads per layer (pools):** `shareBandLabel` ‖ **safe class word:** the following · the share of the town (a band, never a body) ‖ **source:** `faithPanelModel.js:60-65`; `institutionTable.js:211-212`

=== RT2.6-33
**noun:** the house · the benches · the building · the roof · the stonework · the fabric ‖ **layers (HOLDS / CONDITIONAL with condition):** BODY (the fabric of `{institution}`, which never fills); no built-fabric field (`MOVES.OBJECT` licences one) ‖ **reads per layer (pools):** NONE ‖ **safe class word:** none ‖ **source:** `warFaithStateProse.js:131-132`; `moveGrammar.js:42`

=== RT2.6-35
**noun:** cults[] · the lesser rites ‖ **layers (HOLDS / CONDITIONAL with condition):** a faith standing (`config.cultDeitySnapshots` — every NON-PATRON embedded deity); `cult` also a local `standing` and a pantheon tier ‖ **reads per layer (pools):** `faith.cults.length > 0` → `CULTS:` ‖ **safe class word:** the lesser rites ‖ **source:** `faithPanelModel.js:192`; `religionState.js:150-155`; `pantheon.js:79-80`

=== RT2.6-36
**noun:** the ruler ‖ **layers (HOLDS / CONDITIONAL with condition):** the MANDATE phrases' own word, licensed ONLY where `mandateGovWeight > 0` — CONDITIONAL: the engine types nothing named "the ruler"; the regex admits PERSON-shaped words (monarch · king · queen · emperor), so the referent is the government CLASS; safe ONLY inside the authored phrase the block "surrounds, never rewrites"; a writer's own "the ruler" outside it is a role word the engine does not emit ‖ **reads per layer (pools):** `faith.mandate.phrase` → the three `MANDATE:` pools ‖ **safe class word:** the ruling structure ‖ **source:** `religionState.js:686-691`, `:789`, `:791-793`; annex `:4517-4518`

=== RT2.6-38
**noun:** the captain · the priest / high priestess · the reeve · the elder · the factor · the magistrate · the mayor / lord mayor · the guildmaster · the clerk(s) · the sexton · the bearers · the shepherd / observers / watchers ‖ **layers (HOLDS / CONDITIONAL with condition):** ROLE words: `FACTION_ROLES` (`Watch Captain` `linkToInst: /watch\|garrison\|barracks\|militia/` — spans the MUSTER's roster row, A-8; `High Priestess`; `Guildmaster`; `Lord Mayor`); `ROLE_CATEGORY_KEYWORDS`; `Priest (resident)` and `Village reeve` are institution ROWS named by role words (the priest row keeps NONE of the parish services; the reeve row is a treasury holder); `magistrate` also a SECURITY body and a `law`-class word; the clerk a STANDPOINT word; the sexton AND the bearers appear only inside service DESCRIPTION strings (`Register of the dead`; `Funeral procession`) ‖ **reads per layer (pools):** NONE — no warFaith read reaches a role; the ANGLE `[elder]` only ‖ **safe class word:** none safe on thi

=== RT2.6R-01
**assignment tried (survey):** "{counterpart}'s garrison" as the safe word for the occupier's soldiers; the garrison row (a) "the OCCUPIER's soldiers — `settlementOccupation`" ‖ **refuter's verdict:** REFUTED ‖ **evidence:** `occupationStatus.js:95-103` returns `{occupierName, statePhrase, resistancePhrase, pays, burdened}` — NO garrison field; W12 "an occupation never entails a garrison present"; row (a) is the corpus's wording (`:3451`)

=== RT2.6R-10
**assignment tried (survey):** "the banner" licensed on the family pools (holder court) ‖ **refuter's verdict:** CONDITIONAL ‖ **evidence:** the 24 family rows carry no reads; the court licence is on the four doc/fraying rows only

=== RT2.6R-13
**assignment tried (survey):** `statusLabel: Occupied` a muster-licensed BODY-adjacent read ‖ **refuter's verdict:** CONDITIONAL — incidental licence ‖ **evidence:** the keyed branch is `if (occupation)` (`:203`), a POWER standing; the muster tokens are the sibling branches' (`sourceOfRow` `:510-514`)

=== RT2.6R-15
**assignment tried (survey):** "the ruler is the ruling STRUCTURE, not a person" ‖ **refuter's verdict:** CONDITIONAL ‖ **evidence:** the regex admits person-shaped words (`religionState.js:689`); safe only inside the authored phrase
