Desk landing (register car 2): the mounts baseline and the writer-reach register re-take at the composed tip — the growth door opens once, for two identities, by ruling

Mounts baseline: the walker's own UPDATE door at 7ef501df9 (29/29 plain). Writer-reach: `--rebank --charter=§900` — the ONE governed growth door, chartered for the MIN_ROWS flood class and used here, declared, for TWO identities: `forcedByConfig on stress` and `forcedByConfig on stressors`, written by `resolveStress.js:66` since c1ea091f7 and read by nothing (their twin `source: 'forced'` is the field downstream reads); pre-existing at a2af55cde, handed up by DESK-TIMEBAND. The three doors were weighed in `rulings/RULING-WR-FORCEDBYCONFIG.md` (chair kit): lighting them is a words + capability car; a register row has no honest reason (no dormancy bit, no reader); the growth door records the admission in `rebankHistory` with the pending surface named (a stressors-desk sentence, DS-STR). The same act folds out the two STALE members the desks lit (`hasMilitaryInst on inst`, `hasWatch on inst`): cohort 1322 → 1322. corpusMeta re-measured (originCount 8560), the shapes digest and the new LIT identity `stressTypes on _config` frozen. Plain re-run green.

Owner veto by name: "light forcedByConfig" (a stress-desk sentence + a capability) or "strike the key" (a persisted-shape change; moves the OSR).

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
