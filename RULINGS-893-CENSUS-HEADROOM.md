# §893 — CENSUS HEADROOM. ⛔ THIS FILE'S FIRST VERSION WAS BACKWARDS; THIS IS THE CORRECTED RULING.
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · lane CENSUSHEADROOM, 2026-09-04.

## 0. ⛔⛔ THE CHAIR'S ERROR, FIRST, BECAUSE EVERYTHING BELOW DEPENDS ON IT
The chair ruled that the four war-kind slots were **STALE PINS** — that *"extra families were minted
into the war-cost pools without amending the registry pin"* and the cure was a re-pin. **That is
exactly backwards, and the mistake was a misread of an assertion's argument order.**

**Vitest prints ACTUAL then EXPECTED.** So `expected [ …(5) ] to deeply equal [ …(6) ]` means
**src = 5 and the ANNEX = 6.** The POOLS hold exactly five — proved by a **GREEN sibling arm**,
`expect(row.pool).toHaveLength(5)`. The extras were minted into the **ANNEX DOCUMENTS**. The source
is **MISSING AUTHORED CONTENT**, not over-minted. The `-`-prefixed diff lines are present in Expected
and ABSENT from Received.

⚠ **The census's own `cause` prose carries the same inversion**, which is how it propagated into the
chair's ruling. **A stale, wrong CAUSE sentence is more dangerous than a stale figure: a figure gets
refused by a gate, a sentence gets believed.** ⭐ The chair had a memory recording this exact
inversion and **folded it to an archive four hours before repeating it**; the row has been un-folded.

## 1. RULED: ZERO OF THE FOUR ARE STALE PINS. All four are live, correct instruments.
Nothing is re-pinned. **The cure is to WIRE content that was authored and never wired.**

## 2. RULED: ALL EIGHT EXTRA FAMILIES ARE LEGITIMATE — wire them, remove nothing
`git log -S` on each of the eight sentences returns **exactly one** commit: `1e8bf8a87`, *"THE
CHRONIC-TIER DEEPENING"* — **the owner's own**, a 13-agent run with three adversarial verifiers and
chair rulings A-16..A-19. `git show --stat` proves it touched **nine `docs/` files and ZERO `src/`**:
**a pure corpus commit whose WIRING STEP WAS NEVER TAKEN.** The extra depths land exactly on the
frequency-floor table (notable→6, routine→10 ≥ 8) — the deepening was authored *to* the floor.
And the direction is **already ruled**: `kindPoolFloors.walker.test.js` records that *"the fixed-five
class chair ruling CR-FP-7 closed by countersigning the cap-raise arm."* D-W3 asked *cap raised or
corpus trimmed*; **CR-FP-7 answered.** Nothing should be removed.

⭐⭐ **THIS IS THE FOURTH INSTANCE TONIGHT OF ONE PATTERN: a ruling was made, the content was authored,
and the WIRING STEP WAS NEVER TAKEN.** `respect` into `BOND_KINDS`; `rivalry` into `GRUDGE_KINDS`;
`errandSpineEnabled` into `PREVIEW_OVERLAY`; and now eight annex families into `WAR_RECEIPTS`. It is
no longer a coincidence; **it is the shape of this program's debt** and deserves a standing check.

## 3. RULED: THE ORDER IS MECHANICS, NOT CONVENTION — and it is proved from the instrument
- **Cure FIRST** → the gate stays **green** with a `RATCHET DOWN … bank the win` advisory
  (`check-test-ratchet.mjs:1435`, `return 0`).
- **Remove the census row first** → the still-failing test becomes a **REGRESSION** (`:1292`) and
  **reds the gate**.
- ⛔ **ALL-OR-NOTHING PER ROW.** A partial deepening breaks the frozen magnitude pattern's hard-coded
  `(5)` and yields `unmeasured`, which **fails closed**.
The only door anywhere is `npm run test:ratchet:update`, remove-only — **named, not run**, and the
chair's at a landing.

## 4. ⚠ A DISCLOSED BEHAVIOUR SHIFT, STATED BECAUSE IT MUST NEVER RIDE SILENTLY
`templateIndex = fnv1a32(seed) % pool.length`. Growing a pool **re-maps every seed.** Same-seed
outputs change. This is a legitimate one-time shift caused by wiring authored content, and it is
recorded as such — never let a distribution move without saying so.

## 5. THE CORRECTED HEADROOM LEDGER — 6 of 10 reachable, four in ONE act
| slots | rows | the actual cure |
|---|---|---|
| **4** | war-kind (3 warCost + 1 warRuling) | **WIRE** 8 authored families into `WAR_RECEIPTS`; extend `requiredSlots` on four `eventProse.js` rows; relax the hard-coded fives in both walkers; extend `EXPECTED_SLOTS` and the `unsupported` regexes; widen `warCostsNews.test.js:145`'s `[1-5]`; **and, COUPLED IN THE SAME COMMIT**, strike four `LEGACY_UNDER_FLOOR` rows and take `kindPoolFloors`' literal `28 → 24` — that test is green today and **reds in BOTH directions if the halves separate** |
| **1** | `enforcement-claims` | ⭐ **half of it is an INSTRUMENT DEFECT**: `CLAIM_RE`'s `0 problems` has no left boundary, so it matches inside **"30 problems"** — all three `FABLE_VALIDATION_QUEUE` hits are lines reporting *30 problems / 3 errors*, **the opposite of a completeness claim**. A `(?<![\d.])` guard takes naked 6 → 3. The other three are real untagged claims. |
| **1** | `clampPrimitiveBaseline` | fully-current REAL debt, re-verified by replicating its scan: 78 live / 62 baseline / **0 stale** / 16 growth. 16 behaviour-touching migrations — **schedule LAST**. |
| 2 | voice Tier-3 (`.jsx`) | ⛔ still **NO ROAD**; the prose car touches zero `.jsx` |
| 2 | voice Tier-2 | the prose car, after its rebase |

## 6. A FIFTH INSTANCE, GENUINELY DARK, WORTH ZERO SLOTS AND LIT ANYWAY
`war_culture_suppressed` is **src = 5 / annex = 10** with the identical defect and is **NOT in the
census at all**, because `phrasedKindPools.walker.test.js` **reads no annex**. It buys no headroom.
**RULED: light it in the same act** — an instrument that cannot see a defect is not evidence the
defect is absent, and leaving a known-dark twin beside four cured siblings is how it gets re-found.
