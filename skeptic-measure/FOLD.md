# SKEPTIC FOLD — LANE MEASURE, THE WHOLE TRAIN (cars 0 · 0b–0g · 1 · 1b · 2 · 2b)

**CONFIRMED 33 · REFUTED 11 · PARTLY 16 · UNTESTED 9 — census-rate 0->0 · manifest 0->0 · bytes-fences 0->0 (porcelain before->after, every lens).**

Seat: Opus 5 — Fable-unvalidated (the folder). Pinned dock
`$SC/skepMEASURE` @ `fcd98a3dbb3178adccb37b6f3f103bb5dc03af63`, base `8522a17b2`, 13 commits.
Re-verified by me at fold time: `git rev-parse HEAD` = `fcd98a3db…`, `git status --porcelain | wc -l`
= **0**, `git rev-list --count 8522a17b2..HEAD` = **13**.

Grading, stated so the counts mean one thing: **CONFIRMED** = the claim reproduced on a command
someone ran. **REFUTED** = a figure or a claim the receipt states is wrong as stated. **PARTLY** =
the claim holds but is narrower, weaker or blinder than the receipt says. **UNTESTED** = neither,
with the test named in §6. Sixty claims are graded; nine untested rows are listed apart. Lens C's
own header counts 20 claims (15 C / 4 P / 1 U / 0 R); this fold regrades its F2 as REFUTED — a
wrong number is now committed in a frozen baseline — and moves its untested claim to §6.

**WHAT I RE-EXECUTED MYSELF** (read-only, in the pinned dock), rather than taking on the lens's
word: `totals` and the whole rate half of `docs/content/wiring-census.json`; the pair table's
class histogram and its JS-method members; the `default` / `not-produced` / `covert` cells;
`CIVIC_OBJECT_CLASSES` multi-match; `COVERT_SOURCES` reach; the per-tier silence rungs;
`customReachable`'s rows, kinds, limbs and matched tokens; plant #80 executed on a copy;
`economyDeskRead`'s options and `deskReturns`' call; `classifyCell`'s ADDITIVE conjunct;
`DOSSIER_MOUNTS`' size; the membership arm's gating; the five byte ceilings; the variety baseline.
Every one agreed with its lens. Two figures I sharpened and both are noted in place (the
single-pool rungs are **11**, of which 7 are undeclared; the 503 exclusion is a union of 437 and
108 overlapping on 42).

---

## 1. THE HEADLINE, IN ONE PARAGRAPH

The train's *instruments* are sound and its *arithmetic* is exact — the byte lens tested twenty
claims and refuted none, every headline census integer re-derives from the committed JSON, and
the Wilson bound, the apportionment and the closure margins all reproduce to the byte. What does
not survive is a single wiring defect and its shadow: **`deskReturns` calls the economy desk with
`{seed, audience}` and that desk reads `options.playerView` and four caller-supplied readings it
defaults to `null`.** One line — `scripts/prose-rate-corpus.mjs:206` — makes the rate corpus
under-fire three FOOD pools, makes the manifest single-audience on 16.3 % of its cells, records
309 player cells carrying DM-only prose at `index: -1`, and blanks three registered mounts. It
moves three of the five figures the SITTING's rulings are set from. Behind it sit two arms that
cannot fail (the rate half certifies itself; ADDITIVE is unreachable as coded) and one plant that
mutates nothing.

---

## 2. EVERY REFUTED AND PARTLY VERDICT

### REFUTED (11)

| # | the claim | the figure | severity | lens |
|---|---|---|---|---|
| R1 | the rate corpus calls the six desks "through the SHIPPED desk-read recipes, never with `{}` readings" (`prose-rate-corpus.mjs:35-40`) | false for the economy desk: `deskReturns:206` passes `{seed, audience}` and `economyDeskRead` defaults `foodBalance`/`granaryOutlook`/`flowDrift`/`impairedInstitution` to `null`. `deriveFoodBalance.available` is **768 of 768**. Fired **267 → 270**; departure **70/197 → 72/198**; silences **347 (303/44) → 351 (307/44)**; pairs 729/590/226/170 **unmoved** | ⛔⛔ HIGH | census-rate §1 |
| R2 | receipt §13's plants-must-mutate lesson holds for this lane's files | plant **#80** (`mutation-sweep.sh:1117`) was broken by car 0e's `branchReads: [],` insert (`wiringCensus.js:699-702`). Executed on a copy at fold time: md5 `0a28c398a1b6cff56183212bed8fa7fd` **before and after** — it mutates nothing. The sweep's `check_caught` reds it as BROKEN GAP; the receipt never mentions it | ⛔ MED-HIGH | census-rate §3 |
| R3 | `default 20` / `default 10` is "the finding class" | `absenceOf` (`wiringCensus.js:1258`) cannot tell `x \|\| fallback` from `!x \|\| typeof x !== 'object'`. Of 3 paths / 10 cells: `readings.inst` (5) and `link` (2) are refusal guards. **default 10 → default 3** | ⛔ MEDIUM | census-rate §5 |
| R4 | "503 (a table-rung synthetic label on 437, an unrooted bare parameter on 108)" | not a partition: 437 + 108 = **545**; the union is 503 with **42** pairs in both. Both integers right, the phrasing wrong | ⛔ LOW | census-rate §8 |
| R5 | "customReachable rows 22 over 8 kinds" as a count of pools reaching a custom kind | at the tip it is **21 hit rows over 9 distinct (block,pool) pairs**; 22 is car 0's function-grain figure and "8 kinds" is the kind ROSTER (5 carry a hit). Limbs value 19 / bucket 2; matched tokens `{none 4, critical 4, metropolis 3, city 3, thorp 3, moderate 2, factions 2}` — **19 of 21 are string collisions** on tier and severity words | ⛔ MEDIUM | census-rate §10 |
| R6 | the four "RELATION ROWS THAT WOULD JOIN" rest on generator writes | **5 of the 9 cited `generator-write` lines are not writes of world state**: `structuralValidator.js:588` (a `reason:` prose string), `defenseGenerator.js:608` (a comment), `narrativeText.js:53` (a template string), `stressNarrative.js:80` and `:83` (arrow params of a prose-template table). All four joins rest on one of those or on a docblock; **not one uses two `identifier`-grade aliases**, so the draft's strength (3 identifier rows) and its four joins are disjoint | ⛔ MEDIUM | census-rate §11 |
| R7 | 0e.2's "what the branch grain moved" list is complete | two printed tables also moved and are absent: ATTACH COVERAGE `facts`/`meanReach` (DS-STR-2 3→2, DS-CND-1 8→5, DS-DEF-5 10→8, DS-WAR-1 14→10, DS-ECO-11 7→5, DS-GEN-6 10→9, DS-FTH-1 6→4) and the MOUNTS-PER-FACT top twelve (`readings.tradeRouteAccess` left it, `readings.primaryStress` entered). A chair reading car 0 §2 beside the tip gets two disagreeing tables | ⛔ LOW | census-rate §13 |
| R8 | "THE MANIFEST IS RECORDED AT TWO AUDIENCES" | at two audiences on **five** desks and at the DM face twice on the sixth. **11,792 of 72,160 cells (16.3 %)** sit on the economy desk; on all **5,371** both-face positions the faces are identical **by construction**. **309 player cells carry `index: -1`** — `DS-ECO-6 :: TIER: minor shadow activity (≥3)` drawn at `vid` 2/3, both `dm-only`. Only 2 of the 12 mixed pools fire at all, and the recipe blinds one of them; had the audience reached the desk the pinned figure would be **345 divergent positions, not 36** | ⛔⛔ HIGH | manifest M-1 |
| R9 | the classifier's ADDITIVE verdict convicts a real addition | `classifyCell` (`prose-manifest-diff.mjs:61-62`) reaches ADDITIVE only when `base.textSha === tip.textSha`; a modifier beside a spine adds WORDS, so the cell's single `textSha` necessarily moves. Driven: real addition → **ADDITIVE 0 / WORDING-ONLY 1**; the suite's fixture (textSha held) → ADDITIVE 1. ARCH §12 accepts the taste (car 6) and the AUTHORING wave (car 9) on "every affected cell ADDITIVE" — **unreachable as coded** — and the REWRITE wave (car 8a) on WORDING-ONLY, which an accidental attach would satisfy | ⛔⛔ HIGH | manifest M-2 |
| R10 | the drift manifest records the composed surface | **39 of the 56 registered `DOSSIER_MOUNTS`** (56 re-counted by me at fold time). `economics.foodTile`, `economics.seasonTile`, `economics.tradeFlow` carry **0 cells**, and `foodTile` returns real prose under the tab recipe and `null` under the manifest's — the absence is the recipe's, not the corpus's. A mount the manifest never records is a mount whose prose can move with the drift arm green | ⛔⛔ HIGH | manifest M-3 |
| R11 | "eleven arms in THREE new describes" (car 2 §1, §10) | the diff adds exactly **two** `describe(` lines (base 5 → tip 7); the arm count 41 → 52 is right. The wrong word is now committed in `tests/lint/.lighting-census-baseline.json`'s `note`, where a later reader takes it as the record. Every NUMBER in that baseline is correct (2552→2553, 2177→2178, 23816→23833, 6372→6376, parked 375 unmoved) | ⛔ LOW | bytes-fences F2 |

### PARTLY (16)

| # | the claim | the figure | severity | lens |
|---|---|---|---|---|
| P1 | the walker's new RATE arms convict the rate figures (receipt §7) | **no gate can refute any rate figure.** `wiring-census.mjs:1065` re-injects the committed rate half before `--check` compares; the walker (`proseWiringCensus.walker.test.js:683`) builds `committed` the same way, then asserts 267 (:1241), 347 (:1247), 303 (:1281), 729 (:1335), 226 (:1341), 170 (:1342) and the departure report (:1344-6) **against the data those integers came from**. The census half IS genuinely re-derived. The blindness is declared in the script's docblock (:20-25) and **not** in the receipt | ⛔ HIGH | census-rate §2 |
| P2 | F5: "`not-produced` on 96/64 read paths is a real measurement, not a blind index" | `producerIndex` (`wiring-census.mjs:104-105`) matches only `key:` and `.key =`, so ES6 shorthand writes are invisible: **6 of the 64 cells carry a wrong verdict** (`safetyProfile.js:684`, `foodStockpile.js:418`, `narrativeGenerator.js:1113` — all three lines confirmed present by me). The estate already spells the cure at `fieldManifest.js:373`. **30 of 64** sit on a bare key-function parameter (guaranteed by construction, not measured); 1 is a JS array method | ⛔ MEDIUM | census-rate §4 |
| P3 | `objectClass` named on 99 of 708 from a closed list of 10 | closure holds (asserted, walker :874). But the class is read off the pool key's **authored English**, the trap `tradeFlowPoolKey`'s own docblock forbids, and **12 of the 99 match more than one class** with the object-literal order deciding silently (measured by me: `DS-DEF-2` granary+hospital → store/care, `DS-DEF-6` Clergy care → temple/care, …). T-F12 would admit a `care` modifier beside a spine already naming the hospital. **Every pool key the wave rewrites can move its class and no gate would say so** | ⛔ MEDIUM | census-rate §6 |
| P4 | the `covert` column over-marks by design and 4 is a floor (refusal 3) | **0 read paths match the six-entry `COVERT_SOURCES` list** (verified by me); the only live limb is `chain.split('.').includes('covert')`, 4 hits. The frozen constant is inert on the shipped corpus and exercised only on synthetic strings (:862-864). The receipt records the OVER-marking side; the **UNDER-marking side is fail-open and unrecorded**, and car 1's paired-town arm is vacuous for the same reason | ⛔ MEDIUM | census-rate §7 |
| P5 | the USABLE distribution (226 / 170) is the one to set the floor from | `pairMemberClass` (`prose-rate-corpus.mjs:445-448`) calls anything containing a `.` a `fact`. **21 of the 226 usable pairs and 19 of the 170 clearing pairs (11 %) carry `eco.incomeSources.reduce`** — `Array.prototype.reduce`, not a reading of the world (re-measured by me). Four of the receipt's twenty most-frequent usable pairs carry it | ⛔ MEDIUM | census-rate §8 |
| P6 | the 44 MISSING-AT-TIER rows are the authoring wave's (§O.5) | the LAWFUL limb requires another pool of the SAME RUNG to have fired there, so a **single-pool rung can never be lawful**. Measured by me: **11 of the 44** sit on one — `originTierPoolKey` 4 (already declared split-ladder), `contractedForcePoolKey` 4, `operationRolePoolKey` 3 — leaving **7 undeclared rows that are not wave work**. `contractedForcePoolKey`'s own docblock says why it is single-pool by design. A third coarseness beside the declared 123 block-grain and 12 split-ladder rows | ⛔ MEDIUM | census-rate §9 |
| P7 | every new column is convicted by a plant or a fixture that MUST fire (brief car-0 item 4) | `sites` is the exception: `grep -c "566\|mountedRows" tests/lint/proseWiringCensus.walker.test.js` → **0**. `totals.mountedRows` 566 is covered only by the blunt byte-identity arm, a change-detector | ⛔ LOW | census-rate §12 |
| P8 | the base-side normalisation is car 3a's equality target | field-for-field and in order what ARCH §3.7 specifies; `face !== 0` on **0 of 72,160**. But the coordinate's DOMAIN is unguarded: `index = audible.indexOf(chosen)` is **−1 on 309 cells** and nothing asserts `index >= 0`, so the target contains 309 rows no composer can reproduce. The base-normalisation arm compares `pieces[0].index` to `cell.index` — both −1, so it agrees | ⛔ HIGH | manifest (b) |
| P9 | "36 audience-divergent cells", every one on a mixed pool, leaks `[]` | the count is pinned and a non-mixed difference does fail — but the label says **cells** and the number counts **positions**: 36 positions = **72 cells**. The leak check runs only over both-face positions, skipping the 36 one-sided (all DM-only today, player-only 0 — lawful now, unguarded later). And it cannot see a leak that renders IDENTICALLY on both faces, the shape R8 produces 309 times | ⛔ MEDIUM | manifest (d) |
| P10 | the paired-town covert arm | it does **not** suppress at the candidate stage and does **not** assert byte-equality: it filters recorded player cells by the census's covert set and asserts the filtered count equals the full count — a post-draw membership test. Vacuous: 4 covert rows, `rateBp: null` on all four, **0 dm and 0 player cells** on the drift corpus. The receipt says all of this out loud; the fold confirms the vacuity, not a concealment | ⛔ MEDIUM | manifest (e) |
| P11 | the seedless control | the ordering form reproduces exactly (1,071 cells · 202 above index 0 · 18 below every probe) and the kernel law is driven beside it. But `economyDeskRead` overrides the caller's seed from the settlement, so `seedOverride: ''` never reaches that desk: **152 of the 1,071 (14.2 %) are economy-desk cells that were never seedless**, and the twelve probes cannot move them either, so the arm passes over them testing nothing | ⛔ MEDIUM | manifest (f) |
| P12 | "a re-record without `GOLDEN_SHIFT_SIGNED` must throw" | not testable for this surface. The door refuses `NO_SIGNATURE` (`goldenRecordDoor.js:243-249`) but `recordGolden` is **never called** for `dossier-prose-manifest` — there is no write path, so the fixture is updated by hand-edit today and nothing refuses that. Everything else about the door CONFIRMED: enrolled not excluded, all measured fields null, the chair's record quoted **verbatim** (character-for-character match), the `-golden` glob really claims it, `goldenFreeze.walker` 90 passed | ⛔ MEDIUM | manifest (g) |
| P13 | the repeat census implements ARCH §7's refusal | §7 makes the arm NOT-EXECUTABLE below a stated **pair count** and asks for N, expected, observed and **power**. The implementation refuses on independent **DRAWS** (`MIN_DRAWS = 8`) or a one-variant pool and prints no power figure; `MIN_DRAWS` equals the corpus's seed count exactly, so "EXECUTABLE" reduces to "seen under all eight seeds". Arguably the better criterion — but not the specified one, and the 285 refusals are not §7's | ⛔ LOW | manifest (h) |
| P14 | the chunk-membership extension lands "under `requireDistRead`" | **2 of the 11 new arms** are `it.skipIf(!requireDistRead)`. The membership arm (`vendorPdfLazy.test.js:1585`, verified by me) sits under `describe.runIf(distExists)` only and carries a PRESENCE limb (`if (!carriers.length) …`), which the file's own STALE-DIST POLICY (:51-68) rules must be gated. The fingerprint is re-derived from SOURCE every run, so a rewrite moves it while the stale chunk carries the old text — executed: fingerprint changed true, old chunk contains new fingerprint false ⇒ carriers `[]` ⇒ RED. `npm run check` runs `test` before `build`, so **the authoring wave reds on every leaf whose prose moved**, naming two wrong causes | ⛔ MEDIUM | bytes-fences F1 |
| P15 | "Both bundled packages are SYMLINKS, so the dock cannot bundle either twice" | two of at least five named: `vite.config.js:531-545` also bundles **zustand**, and the closure carries `vendor-react` (react, react-dom) and `vendor-icons`. The law's OUTCOME is confirmed twice over — 453 of 456 top-level entries are symlinks in a peer dock, and the closure landed byte-exactly on §914's figure | ⛔ LOW | bytes-fences F3 |
| P16 | §5's block is command output | it is an assembly: `car2-dist.mjs` prints a per-leaf `fp(NNNB) "…"` line above each carriers line and §5 drops all seven with no elision mark, unlike §2/§3/§8. Every figure faithful; the seven lengths reappear in §6. Sibling: §8's sha-pin table labels its middle column `tip 710ef8e08` (car 1b) rather than car 2's tip — the shas are identical at `fcd98a3db` either way | ⛔ LOW | bytes-fences F4 |

---

## 3. THE CURES OWED BEFORE §915 — 30, each a named file with the arm that proves it

### (a) CODE — 11

1. **`scripts/prose-rate-corpus.mjs:206` — call the economy desk by its shipped recipe.**
   `economyDeskRead(s, { ...opts, playerView: opts.audience === 'player', foodBalance: deriveFoodBalance(s), granaryOutlook: deriveGranaryOutlook(s), flowDrift, impairedInstitution })`, or call
   `economyStateProse` directly with `opts` as the other five desks do. Re-run the corpus, re-fold
   with `--rates`, re-commit the rate half. **ARM:** a walker arm asserting the economy desk's
   return DIFFERS between `audience:'player'` and `audience:'dm'` on at least one golden town
   (today identical by construction, `true`), plus a fixture arm asserting `deskReturns` supplies
   every reading `economyDeskRead` would default to `null`. **This one edit discharges R1 and R8,
   moves R10's three zero-cell mounts, and un-blinds P11's 152 cells.**
2. **`tests/fixtures/composedReadingSequence.js:192`** carries the identical defective call (car 0's
   `deskReturns` took it from there). Fix in the same commit or the next car re-inherits it.
   **ARM:** the same two-audience difference arm, over the fixture.
3. **`scripts/mutation-sweep.sh:1117` — re-anchor plant #80** to include `branchReads: [],`.
   **ARM:** run the line standalone against the committed bytes; md5 MUST move (today
   `0a28c398a1b6cff56183212bed8fa7fd` → itself), then the walker must red.
4. **`tests/build/vendorPdfLazy.test.js:1585` — split the membership arm.** Keep the
   stray / closure / eager-data / entry comparisons ungated (true absence checks the policy wants
   ungated); move the `!carriers.length` presence limb and the causal leaf's carrier read behind
   `it.skipIf(!requireDistRead)`. **ARM:** plant B (eager import, no dist) must still red both
   absence arms; a source-rewrite-against-stale-dist must NOT red.
5. **`scripts/prose-manifest-diff.mjs:61-62` — make ADDITIVE reachable.** Drop the
   `base.textSha === tip.textSha` conjunct, or record a per-piece digest so a spine's own text can
   be held while the unit's moves. **ARM:** rebuild the suite's ADDITIVE fixture on a REAL addition
   (text moves) — the arm must read ADDITIVE, and the current textSha-held fixture must become
   unreachable. **Cars 6 and 9 cannot be accepted on their stated criterion until this lands.**
6. **`tests/property/dossierProseManifest.test.js` — three refusals.** Assert `index >= 0` on every
   cell; add a mount-coverage arm asserting the recorded mount set against `DOSSIER_MOUNTS` (39 of
   56 today) with every absence carrying a reason; add the 36 one-sided positions to the leak check
   **with their direction** (DM-only lawful, player-only a leak). **ARM:** each must red today.
7. **`scripts/prose-manifest-cells.mjs` — expose a seed flag** so plant B (RE-INDEXED at ≈ 2/3) is
   reproducible from the tree by someone other than its author. **ARM:** U4's re-run.
8. **`src/domain/prose/wiringCensus.js:1258` (`absenceOf`) and `scripts/wiring-census.mjs:104-105`
   (`producerIndex`).** Distinguish `x || fallback` from the refusal guard `!x || typeof x !== 'object'`;
   add the ES6 shorthand probe the estate already spells at `src/domain/fieldManifest.js:373`.
   **ARM:** a fixture carrying a refusal guard that must NOT be called `default`, and one carrying a
   shorthand write that must NOT be called `not-produced`; plant #87 already convicts the verdict.
9. **`src/domain/prose/wiringCensus.js:1215` (`objectClassOf`) — record every matching class, not the
   first**, and let T-F12 refuse on the SET. **ARM:** a fixture on "granary AND hospital" asserting
   both `store` and `care`; the walker's :872 arm restated to name the ambiguity.
10. **`scripts/prose-rate-corpus.mjs:445-448` (`pairMemberClass`) — exclude JS method names** from the
    `fact` class (the docblock names two bookkeeping shapes; this is a third). **ARM:** the usable-pair
    arm asserts 226 → 205 and 170 → 151 with `eco.incomeSources.reduce` named in the message.
11. **A `sites` conviction** — a plant on the mount read, or a walker arm asserting
    `totals.mountedRows` 566 against a fixture that must fire. **ARM:** the plant reds the column.

### (b) RECEIPT — 13 (all in `$SC/receipt-measure.md`)

12. §7 (and 0e/0f's arm lists): state that **the RATE arms cannot fail** — `--check` and the walker
    both re-inject the committed rate half. The script's docblock declares it; the receipt must too.
13. Car 0 §3 F5: restate as "**64 cells over 24 paths, of which 30 are bare key-function parameters
    and 6 carry a wrong verdict on an ES6 shorthand write**".
14. Car 0 §4 / F4: "503 (… on 437, … on 108)" → "**437 ∪ 108, overlapping on 42 = 503**".
15. Car 0 §2 and the column table: **`customReachable` is 21 hit rows over 9 distinct pools** at the
    tip; the "22 over 8 kinds" line is car 0's function grain and the 8 is the kind ROSTER.
16. 0e.2: add the **two moved printed tables** (attach-coverage `facts`/`meanReach`; mounts-per-fact
    top twelve) to the "what moved" list, or mark car 0 §2's tables superseded.
17. 0e.6 / §O.5: declare the **third coarseness** — 11 of the 44 MISSING-AT-TIER rows sit on a
    single-pool rung where the LAWFUL limb is unreachable, 7 of them beyond the 12 split-ladder rows.
18. 0f.2: state that the four "WOULD JOIN" rows and the three identifier rows are **DISJOINT**, and
    that 5 of the 9 `generator-write` citations are a comment, a prose string, a template string or
    an arrow parameter.
19. Car 1 §1.2: "36 audience-divergent **cells**" → "**36 POSITIONS = 72 cells**"; add that the leak
    check skips the 36 one-sided positions.
20. Car 1 §1.3: record that **PLANT B is not reproducible from the tree** (no committed command
    produces the 8 × 60 corpus).
21. Car 2 §1 and §10: "**three new describes**" → **two**; and re-stamp
    `tests/lint/.lighting-census-baseline.json`'s `note` (its numbers are all correct).
22. Car 2 §5: mark the block as an assembly (or restore the seven `fp(NNNB)` lines); §8's middle
    column relabelled to car 2's tip.
23. §13: record that **plant #80 was broken by car 0e and is a shipped RED**, with its md5 proof.
24. Car 1 §1.1 / §1.5: record that the manifest is **two-audience on five desks**, that 16.3 % of its
    cells are identical by construction, and that 309 player cells carry `index: -1` and DM-only prose.
25. Add an UNTESTED roster naming §6's nine rows, so a successor does not re-find them as gaps.

### (c) RULINGS FOR THE CHAIR — 6

26. **THE TWO FLOORS (the co-occurrence floor, SITTING §L.4/§O.3; the departure line, ARCH E-F14a).**
    The distribution they are read from **moves under cure 1**: departure 70/197 → 72/198, fired
    267 → 270, per-tier silences 347 → 351; the pair counts do not. And **19 of the 170 licensed pairs
    rest on `.reduce`** until cure 10 lands. **RULING OWED:** set both floors only after cures 1 and 10
    are re-run, on the corrected USABLE distribution, never on the committed one. Setting them today
    sets them on a corpus that omitted a caller reading.
27. **THE ALIAS DRAFT (SITTING §O.2).** Its four joins rest on comments and prose strings; its real
    strength is three `identifier` rows, one of which is ARCH §5.2's own worked edge
    (`economicGates.military` ↔ `settlement.defenseProfile.economicGates.military`).
    **RULING OWED:** ratify the three identifier rows only, or re-cut `generator-write` to require a
    real assignment or key write read from an AST rather than a line regex. Until then **ARCH §4.5's
    `consequence` and `tension` seats have no licensed row** and car 5's arm A2 refuses every joint —
    the shipped join is STRICT 0 and by-leaf 0, unmoved (re-read by me from `totals`).
28. **THE `reads` GRAIN (refusal 4 / SITTING §O.1).** Shipped: BRANCH on **287** rows, function-wide
    fail-closed on **421** — a deliberate widening of §O.1's letter ("every row carrying a predicate",
    185) in service of ARCH §6.3's worked example, which the shipped rows reproduce to the field.
    **RULING OWED:** ratify the 287/421 split (all 185 predicate rows do carry `readsGrain: 'branch'`,
    0 exceptions), and rule separately on DS-DEF-2's rung-4 keys — 26 pools, 4 RESOLVED, ONE key
    function, ONE read set `["court","prison"]`, so **no grain frees that block**; it is a wiring car
    that moves the census's headline RESOLVED integer.
29. **THE MANIFEST'S RE-RECORD.** Cure 1 changes 309 player cells' variant. That is a declared
    **INSTRUMENT shift, not a prose shift**, and `tests/fixtures/dossier-prose-manifest-golden.json`
    (1,050 rows / 141,855 B) must be re-recorded and banked as such **before car 3a pins `pieces`
    against it**. Note P12: the register is unfrozen and there is no write path, so today that
    re-record is a hand-edit nothing refuses.
30. **THE GENERATOR GOLDEN MASTER's 525-row drift** (receipt §1.6) stands at the §914 tip and is
    **not this lane's** (`git diff --stat 8522a17b2..fcd98a3db -- src/generators src/data src/store
    src/kernel src/components` is empty; the only `src/` motion is the fenced census island). The
    composed-prose fixture was therefore recorded against generator output already in an undeclared
    same-seed shift. **RULING OWED (owner-signed):** re-record, or rule that the manifest fixture is
    re-taken after the re-record. Plus the §915 collision on the lighting baseline — see §5 row 1.

---

## 4. THE FIGURES THE LEDGER WILL CARRY

* **The census columns — CONFIRMED** (read by me from `docs/content/wiring-census.json` `totals` at
  `fcd98a3db`): pools **708** · blocks **68** · variants **2266** · RESOLVED **318** · UNRESOLVED
  **390** · with a predicate **185** · clean **185** · key functions **118** (consulted 118) · key
  tables **29** · TIERS MISSING **34** / THIN **483** / COVERED **225** / MISSING-AT-TIER **44** ·
  `reads = tests` on **708 of 708** · narrowed **0** · NARROWS refused **0** · covert **4** ·
  objectClass **99** · mounted **566** · absent **377 / 10 / 64**. **Two column CORRECTIONS:**
  `default` **10 → 3** true defaults (7 are refusal guards, R3); `not-produced` **64 cells over 24
  paths**, of which **6** carry a wrong verdict and **30** are bare parameters (P2).
* **k = 0 on the branch grain — CONFIRMED: 49 of 318 RESOLVED** (`totals.zeroK` 49), against **121**
  function-wide; branch grain on **287** rows, function-wide on **421**; k histogram branch
  `-2:11 · -1:12 · 0:26 · 1:79 · 2:190`.
* **Attach coverage — CONFIRMED: 22 blocks can attach nothing** on the branch grain (26
  function-wide); DS-DEF-11 · DS-DEF-9 · DS-ECO-9 · DS-POW-3 freed; DS-DEF-11 leaves at 10000 bp
  reached, DS-DEF-2 does not and no grain frees it. **CORRECTION:** car 0 §2's printed per-block
  `facts`/`meanReach` table is STALE at the tip (R7).
* **267 of 708 — CONFIRMED as the committed figure** (`rate.rows.length` = 267) and **CORRECTED to
  270** under the shipped desk-read recipe (R1, cure 1).
* **The per-tier silences — CONFIRMED: 347 = 303 LAWFUL + 44 MISSING-AT-TIER** at 128 towns per
  tier; **CORRECTED to 351 = 307 + 44** under cure 1. Three coarsenesses, two declared and one new:
  **123** rows on the BLOCK grain, **12** on a split ladder, **11** on a single-pool rung (7 of them
  undeclared and not wave work, P6).
* **The pair counts — CONFIRMED exactly and unmoved by cure 1: 729 distinct · 590 clearing · 226
  usable · 170 usable-clearing**, Wilson bound ≥ **51 of 768** (re-derived: `wilson(50,768).lo =
  0.049730`, `wilson(51,768).lo = 0.050866`). **CORRECTIONS:** the 503 exclusion is **437 ∪ 108,
  overlap 42** (R4); **21 of the 226 and 19 of the 170 carry `eco.incomeSources.reduce`** (P5).
* **The relation rows — CONFIRMED: 165 = 131 (a) + 28 (b) + 6 (c), all `a->b`, source (d) 0 and
  asserted empty; the JOIN STRICT 0 and by-leaf 0** over 85 desk read roots
  (`totals.relationRows` 165, `totals.relationRowsJoinable` 0). The alias draft's **4** "would join"
  rows are not a join and rest on 5 unsound citations (R6).
* **customReachable 22 — CORRECTED to 21 hit rows over 9 distinct pools** (byKind services 6 ·
  resources 6 · institutions 5 · tradeGoods 2 · factions 2 · stressors/deities/traditions 0), limbs
  value **19** / bucket **2**, matched tokens `{none 4, critical 4, metropolis 3, city 3, thorp 3,
  moderate 2, factions 2}` — **19 of 21 are string collisions**. §O.6's DIRECTION survives (the true
  reach is smaller, not larger); the quoted number is not a count of reachable pools (R5).
* **The DRIFT row count — CONFIRMED: 1,050 rows / 141,855 B = 525 configurations × 2 audiences over
  four seed tokens; 72,160 cells; unresolved 0, ambiguous 0; recomputation disagreement 6,275;
  seedless 1,071 / 202 / 18.** **CORRECTIONS:** two-audience on five desks only, 11,792 cells
  (16.3 %) identical by construction, **309** player cells at `index: -1` carrying DM-only prose,
  **39 of 56** registered mounts recorded, and the pinned "36 audience-divergent cells" is **36
  POSITIONS = 72 cells** — **345 positions** once cure 1 lands (R8, R10, P8, P9).
* **The duplicate-unit baseline — CONFIRMED: 9,774 bp at N = 4,200 towns over 8 seeds and 525
  configurations** (read by me from the committed baseline): unit instances **532,689**, duplicated
  **520,632**, distinct (position,text) pairs **51,396**, seen twice **39,339**, groups **632** /
  EXECUTABLE **347** / NOT-EXECUTABLE **285**, sub-floor groups **2**. Cost 85 s on re-measure
  against the receipt's printed 74 s — a printed cost on a loaded machine, not a threshold. The
  refusal criterion is DRAWS, not ARCH §7's pair count, and carries no power figure (P13).
* **The byte ceilings — ALL CONFIRMED** (literals re-read by me at the tip): `CLOSURE_BUDGET_BYTES`
  **1_048_000** · `CLOSURE_GZIP_BUDGET_BYTES` **337_000** · `CLOSURE_BROTLI_BUDGET_BYTES` **283_000**
  · `DATA_LAZY_RAW_CEILING_BYTES` **3_098_110** · `DATA_LAZY_GZIP_CEILING_BYTES` **771_897**.
  Measured closure **1,042,122 raw / 330,822 gzip / 277,755 brotli** over 8 files — margins **5,878 /
  6,178 / 5,245**. Six state leaves **641,410 raw / 121,630 gzip, ratio 5.2735**; causal leaf
  **210,260 / 36,700** with zero headroom and reaching **no emitted chunk at all**; apportioned
  ceilings sum to exactly **2,800,000** (delta 0) and **622,222** gzip, inside the 500,000–650,000
  band; headroom **4.3654×** on every leaf. **ARCH §10's 1,042,086 / 5,914 is 36 B low** — §10 is the
  number to correct, not the brief's.

---

## 5. WHAT THE §915 REGISTER DOORS WILL MOVE

This consist lands on `8522a17b2` directly with **no rebase**, and `run-registers-915.sh` names the
same `BASE=8522a17b2`, so the two trains are siblings on one parent.

1. **`tests/lint/.lighting-census-baseline.json` — the one real collision.** Both trains rewrite it.
   §915's script says its lighting door is NOT run ("the census frozen at `62f201796` still matches");
   MEASURE's car 2b refroze it at `55d7f696b` with **files 2553 · parked 375 · credited 2178 · titles
   23833 · suiteTitles 6376**. Whichever lands second takes a **textual conflict**. The counts survive
   either order (§915's car 12 moved no test file; MEASURE's two new test files are additive).
   **CURE: keep MEASURE's tuple, re-stamp `measuredAtSha` and `note` — fixing "three describes" → two
   in the same act (R11) — then re-run the plain walker.**
2. **`tests/lint/.prose-numerics-baseline.json`** — the PN door runs `--write`. MEASURE contributes no
   drift (`baseline=225 live=225 parseErrors=0 · exact=225 rekeyed=0 relocated=0 FELL=0 NEW=0`), so it
   should not move for this consist.
3. **`scripts/.writer-reach-baseline.json`** — the writer-reach door runs `--write` for provenance.
   MEASURE's dry run exits 0 (WRWALKER HOLD, the standing state) and no new writer entered.
4. **`tests/lint/.dossier-mounts-baseline.json`** — predicted no change; MEASURE moved no mount.
5. **`scripts/.observed-shape-readers-baseline.json` (OSR) will NOT move.** MEASURE adds a new `src/`
   file (`wiringBranch.js`), the case that would normally admit a reader — but the scan reports
   **1972 findings exactly matching the frozen inventory**, and the inventory names neither census file.
6. **Not a door, but owed:** the base-state capsule and the census-totals register are stale by the
   lane's new assertions and one `docs/content/` file. A chair's capsule car will see `totalTests`,
   `totalFiles`, `stampedAt` and `lightingCensus` move.
7. **If the cures land before §915:** `docs/content/wiring-census.json` (the rate half) and
   `tests/fixtures/dossier-prose-manifest-golden.json` both move; `scripts/.prose-byte-baseline.json`
   does not, because no leaf byte moves. The two size consumers (`scripts/.size-baseline.json`,
   `tests/lint/sizeBaseline.test.js`) and `eslint.config.js` / `vite.config.js` / `package.json` are
   sha-identical at base, tip and worktree and must stay so.

---

## 6. THE UNTESTED ROWS, AND WHAT WOULD TEST THEM

| # | the row | what would test it |
|---|---|---|
| U1 | `impairedInstitution`'s effect on the rate figures — `ServicesTab.jsx:100` derives it tab-locally, so it is the fourth omitted caller reading and its delta is unknown | thread the ServicesTab derivation into `deskReturns` with cure 1 and re-run `prose-rate-corpus.mjs`; the delta on fired / silences / departure is the answer |
| U2 | 0e.5's "**50** status flips · **7** guards · **8** atoms" — the probe scripts live in `$SC/laneMEASURE`, fenced from the verifier. The **mechanism** is CONFIRMED (`MONSTER_FAMILY_OF` maps `heartland -> 'settled'`, `defenseStateProse.js:216-221`), so the refusal's ground is sound | re-run `probe-alias.mjs` and `probe-delta.mjs` from an unfenced copy of the dock at `fcd98a3db` |
| U3 | Whether the **full mutation sweep** is otherwise green at this tip — no whole sweep was run, and plant #80 is now known broken | `bash scripts/mutation-sweep.sh` at the tip under the runner gate; expect at least one BROKEN GAP until cure 3 lands |
| U4 | **PLANT B** (RE-INDEXED at ≈ 2/3; 6,685 bp against the arithmetic's exact 6,667, seed grain 43 of 60, Wilson [5923, 8149]) — no committed command produces the 8 configs × 60 seeds corpus, so the car's second classifier plant cannot be re-run by anyone but its author | cure 7's seed flag on `prose-manifest-cells.mjs`, then re-run the plant and the diff |
| U5 | **Plants #93 and #94** — registered in `mutation-coverage-manifest.json` with `meta:` rows and in `mutation-sweep.sh` by text, each naming its measured reds; not re-executed (a sweep is not a focused file) | extract each line with `sed` and run it standalone against the committed bytes, as cars 0 / 0e / 0f did — md5 must move, the named arms must red, restore `cmp`-identical |
| U6 | **`scripts/check-test-ratchet.mjs`** — it spawns the whole suite, which the verifier's fence forbade; `scripts/.test-ratchet-baseline.json` is not in the consist diff, so the ratchet is CLAIMED unmoved | run it once under the runner gate (`HOLD-VITEST` absent, runner count 0) |
| U7 | **The whole `tests/lint` run at 147 files / 2,386 assertions** — confirmed statically (146 at base → 147 at tip) and the two focused suites the claim turns on were executed | the full `npx vitest run tests/lint` under the gate |
| U8 | **The "ten-module byte fence"** — the instrument could not be identified by that name in the tree; the ELEVEN-module island fence (`proseWiringCensus.walker.test.js:473-479`) was read and holds, and no `src/` file outside the island names any of them | the chair names the instrument, or the receipt's reference is corrected to the eleven-module fence |
| U9 | **The one chartered `vite build`** — not re-run (forbidden). The dist figures are re-derived from the receipt's own printed per-file rows, which sum correctly, and from `$SC/measure/car2-dist.log`; the chunk hash `data-lazy-pqPyi0JA` matches §10's own dock | one `npx vite build` in a relinked dock (clone the bundled packages first — the symlink hazard) and re-run `car2-dist.mjs` |

---

Porcelain BEFORE **0** · AFTER **0** · HEAD unmoved at `fcd98a3db`. Nothing outside
`$SC/skeptic-measure/FOLD.md` was written.

Seat: Opus 5 — Fable-unvalidated · the MEASURE skeptic fold
