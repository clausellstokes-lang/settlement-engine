Seat: Opus 5 — Fable-unvalidated (the verifier, SECOND CUT). Findings, never a pass; the chair rules on every row.

# REFUTATION OF THE SECOND CUT (`TASTE-SAMPLE-912-v2.md`), pairs v2-1 · v2-2 · v2-3

**Corpus.** The product tip in the dock `laneB6` at **3b1c0eaa51f77561a036ae7ec54682c39856192c** (`3b1c0eaa5`). The main working tree was never read for a corpus fact (tree law). Clock by `date`: 2026-09-07 18:05:56 EDT.
**Porcelain on the dock: 0 BEFORE, 0 AFTER** (`git -C <laneB6> status --porcelain | wc -l`, executed twice). Nothing was written outside `MY/`; scratch under `MY/refuter2-scratch/`.
**Method.** Every figure below comes from a command executed in this session and whose output I saw. `DS-POW-5`, `DS-GEN-18` and their pools were brace-matched out of the leaves to JSON (`MY/refuter2-scratch/block.py`); the pair mechanics were recomputed with `check-pair.mjs`'s own regexes re-implemented (`MY/refuter2-scratch/measure.py`); `check-pair.mjs` was re-run on the v2 pairs file and on my cures with cwd under `MY/refuter2-scratch/`; `gen-probe2.mjs` was re-run READ-ONLY for both seeds from a copy under `MY/refuter2-scratch/`; the kernel's draw was re-derived independently in Python from its own `fnv1a32` (`stateProseKernel.js:92`) and `avalanche32` (`:107`); and 48 settlements were generated read-only through the product's own `generateSettlementPipeline` to measure the founding claim rather than inherit it.

**Headline.** The cut repairs most of what the first refutation found: the manner clause is restored verbatim, the body/name identity claim of cut 1 is gone from the assertion, the agent is restored to the [street] variant, the V4 tag is withdrawn on the right ground, and the two absence classes are declared instead of demonstrated. The mechanical receipt reproduces character-for-character and no AFTER is longer. **What does not survive contact with the code is the sample's own §1 generalisation** — that on today's composed fill sets an R1 STATE block licenses at most two orders — which is false at this tip on a block I can name, and it is the finding the owner is meant to take away. Two smaller things are wrong on measurement: the article-opener figure, and the claim that the walker's arm D reads the composed fill (the file's own §5 lists that arm as owed). And v2-1's AFTER, rendered on seed A's fills, garden-paths into the very conflation cut 1 was withdrawn for.

---

## v2-1 — DS-POW-5 [ledger] index 0 — labelled **V5 INSTITUTION → PRESENT (tense: present throughout)**

BEFORE (located in the leaf by exact string; `power.generated.js` `DS-POW-5`, pool `governing body name: a SLOT, never a baked noun`, index 0, angle `ledger`):
*What governs {settlement} calls itself the {seat}, and the name is precise about the town's size and pretensions in a way outsiders routinely mistake.*
AFTER: *The {seat} is the name what governs {settlement} gives itself, precise about the town's size and pretensions in a way outsiders routinely mistake.*

Measured (my own re-implementation of check-pair's normalisations): words **24 → 23** · sentences **1 → 1** · opener `what governs` → `the {}` · slots `{seat, settlement}` → `{seat, settlement}` · close `mistake` → `mistake` · ration **0 → 0** · contrast shape **0 → 0** · DURATION added/lost **none** · COUNT added/lost **none**.

### (a) C-pair

| test | verdict | severity | evidence |
|---|---|---|---|
| the reflexive self-designation is carried by the same agent | **PARTLY** | **MEDIUM-HIGH** | The AGENT survives intact — *what governs {settlement}* is verbatim in both, and the reflexive *itself* is kept. The ACT does not. *Calls itself X* is self-naming in USE; *gives itself X* is self-CONFERRAL — a body can be given a name it never uses, and a body can use a name no act conferred. The pool's own key is about the name being **in use** ("a SLOT, never a baked noun"), and the sample's §1 states the pool's claim as the town using it. **The same file refuses this exact substitution two entries later:** v2-2's rule field says the first cut's *gives … any other title* "widened the act from address to conferral" and calls that the reason for the KEEP. One verb, two rulings, one document. |
| no identity claim added; the name kept apart from the body | **CONFIRMED** on the intended parse | — | *The {seat} is the name X gives itself* is a specificational identity between the slot and a NAME, not between the slot and the GOVERNING BODY. Cut 1's defect (*The {seat} is what governs {settlement}*, with *a title* apposed to it) is genuinely cured: nothing in the AFTER predicates governing of the {seat}. |
| the appositive *precise about …* attaches to the name, not to `{seat}`/`what governs` | **PARTLY** | MEDIUM | The supplementive after the comma has no overt host. Its two available controllers are the matrix subject (*The {seat}*, co-referential with its specificational complement *the name* — the right reading) and the nearest nominal before the comma (*itself*, i.e. *what governs {settlement}* — the wrong one, which would make the BODY precise about the town's size). The BEFORE had no ambiguity at all: *the name is precise* is an independent clause with an explicit subject. The pair's rule field asserts that "*the name … precise*" keeps body and name apart — **that clause does not exist in the AFTER**; *the name* is a predicate nominal and *precise* is detached. |
| the manner clause preserved with the same object (the MANNER of the precision) | **CONFIRMED** | — | *in a way outsiders routinely mistake* is byte-identical and still modifies *precise*, so what outsiders mistake is the manner, not the title. The first refutation's second REFUTED item is fully cured, and the typed claim list now says so explicitly rather than hiding it behind a pronoun. |
| the rendered sentence delivers that reading to a reader | **REFUTED** | **MEDIUM-HIGH** | Rendered on seed A's fills (`Breitturm`, `Guild Council`, from the re-run probe): **BEFORE** — *What governs Breitturm calls itself the Guild Council, and the name is precise about the town's size and pretensions in a way outsiders routinely mistake.* **AFTER** — *The Guild Council is the name what governs Breitturm gives itself, precise about the town's size and pretensions in a way outsiders routinely mistake.* The string *the name what governs* is the nonstandard relative (*name what* = *name which*), and that is the most available first parse: a reader takes **"the Guild Council is a name that governs Breitturm"**, strands *gives itself*, and has to back up. The parse a reader takes on the first pass is therefore the body/name conflation cut 1 was withdrawn for. On the artefact whose entire job is to show the owner that the prose is excellent rather than adequate, a sentence that must be re-read is the failure the sample exists to disprove. |
| no manufactured particular; no band, modality or threat class moved | **CONFIRMED** | — | No noun enters that the BEFORE did not hold; measured, the DURATION and COUNT sets are identical, the ration is 0 → 0, no will/shall/would either side. |

### (b) U1–U12
U1 n/a (contrast shape 0 → 0, measured, both directions). **U2 CONFIRMED** (1 sentence). **U3 CONFIRMED** (all nine rationed patterns 0 → 0, measured). U4 n/a. U5 neutral — the close is unchanged (`mistake`) and no close rule is cited, so neither a breach nor a delivery. **U6 CONFIRMED** (no cause fronted). U7 n/a (no future indicative added, no subjunctive removed). U8 n/a. **U9 CONFIRMED** (no gloss cut or created). U10 — measured on the pool, see (b) of the report body below. **U11 CONFIRMED** (band sets identical). **U12 CONFIRMED** (no historical, spatial, capacity or actor clause added).

### (c) the label and the A3 note
**PARTLY, MEDIUM.** The ORDER claim is honest as far as it goes: the AFTER does open on `{seat}` and the standing fact is second, and the tense is present throughout on both clauses (the tag now prints the tense, which is the first refutation's cure 7 adopted and it works).
Two corrections bind. (i) **V5's licence is a SLOT here, not a row.** MOVE-GRAMMAR §1.2 row 5 licenses INSTITUTION by a row of the institution TABLE (`{settlement, office, holderRole, whatItDoes, whoItCounts, whoIsExempt, sustainerKind, sustainerName, closed}`); CLERK-LAWS §1 specifies that table as an instrument to be BUILT, and no such row exists at 3b1c0eaa5. The chair's own A12 substitutes the slot ("{seat} on 7 (an INSTITUTION licence, V5)"), and the sample declares "licensed by `{seat}`", so the substitution is visible rather than hidden — I record it **WITHHELD**, a rule question for the chair, not a defect of the cut.
(ii) **The A3 note is honest — CONFIRMED.** `{seat}` is filled by `properFill(text(power.governingName))` (`powerStateProse.js:852`), and `properFill` (`:177–184`) admits only a value with an initial capital, no digits, no dash, no sentence punctuation and no snake_case — a `proper` shape. A3 bars a `proper` fill after an article; the BEFORE already does it mid-sentence (*calls itself the {seat}*) and the AFTER does it once, at the opening. Count 1 → 1, position moved. "Relocated, not created, declared" is exactly right.

**Worst finding: the rendered AFTER garden-paths into the conflation the first cut was withdrawn for, and the naming verb moves from use to conferral — the substitution this same file refuses in v2-2. REFUTED, MEDIUM-HIGH.**

---

## v2-2 — DS-POW-5 [visitor] index 1 — **V1 PRESENT — KEPT AS WRITTEN**

| test | verdict | severity | evidence |
|---|---|---|---|
| BEFORE == AFTER byte-for-byte | **CONFIRMED** | — | Compared programmatically: identical. Measured 17 → 17 words, 1 → 1 sentence, opener `a stranger` unchanged, close `immediately` unchanged. The pair is a true null edit. |
| the sample's typed claims for it are the BEFORE's | **PARTLY** | LOW | The text says *by **any other** title*; the claim list says *by another title*. *Any other* quantifies universally over the complement set; *another* is existential. Under CLERK-LAWS §2.2 C4 a quantifier is a claim-class item. No text moves, so no claim moves here — but the typed set that arm C would compare is written looser than the sentence, which is precisely the looseness the first refutation named on ts-2 and which is what let cut 1's widening pass unremarked. The claim list should carry *any other*. |
| a KEEP is a lawful member of the sample | **CONFIRMED** | — | The 09-05 taste sample carried nine KEEPs (its own refutation grades them as a class). The sample's declaration that this variant is already in the plain-fact grammar and that no cut earns its risk is a real finding, and it is stated rather than dressed as a demonstration. |
| U1–U12 | **CONFIRMED / n/a** | — | Trivially clean on every arm: a null edit cannot add a rationed word, a sentence, a band, a modality or an opener. U10 is untouched by this pair. |

**Worst finding: the typed claim list under-quantifies the sentence it types (*another* for *any other*). PARTLY, LOW.**

---

## v2-3 — DS-POW-5 [street] index 2 — **V1 PRESENT; the V4 tag WITHDRAWN**

BEFORE: *The town at {settlement} calls the hall by its proper title in public and something shorter everywhere else, and both usages are precise.*
AFTER: *The town at {settlement} uses the hall's proper title in public and something shorter everywhere else. Both usages are precise.*
Measured: words **23 → 20** · sentences **1 → 2** · opener `the town` → `the town` (unchanged) · slots `{settlement}` → `{settlement}` · close `precise` → `precise` · ration 0 → 0 · contrast 0 → 0 · DURATION/COUNT identical.
The AFTER is the first refutation's cure 3 adopted verbatim (compared string-for-string).

| test | verdict | severity | evidence |
|---|---|---|---|
| the agent is kept | **CONFIRMED** | — | *The town at {settlement}* opens both. The first refutation's REFUTED item (the agent dropped and the usage generalised) is fully cured, and the opener is now unchanged rather than swapped. |
| *calls the hall by its proper title* == *uses the hall's proper title* | **PARTLY** | MEDIUM | Under the first refuter's own address/usage distinction the object of the act moves. *The town calls THE HALL by T* names a referring relation between the town and the hall and restricts T's use to that relation. *The town uses THE HALL'S T* names a usage of a title, with the hall demoted from the object of the act to the possessor of the title; that the town is naming the hall becomes implicature rather than assertion. Nothing widens (the possessive keeps the hall as the title's owner, exactly as the BEFORE's *its* did), and nothing reaches the sibling's prohibition — so this does not repeat cut 1's failure. But it is not claim-neutral either, and a strictly claim-neutral cut exists at 22 words that keeps the R-DA-03 gain (cure 3 below). |
| *Both usages are precise* is a QUALIFY move, not a summarising second sentence | **CONFIRMED** | — | *Precise* is a second predicate about the two usages named in sentence one, not a restatement of them, so it is R-DA-03's qualification and not MOVE-GRAMMAR §1.3's non-existent MEANING move. Wall 6 holds (not a *which* tail, not a third sentence) and U2's ceiling holds (2 sentences, measured). |
| the same sentence as a taste residual | **WITHHELD** | LOW | The first refutation logged a WITHHELD on this exact sentence — whether the anaphoric *Both usages* reads as a clerk's second fact or as a summarising beat, near the register's measured second-sentence tic. The sentence did not change between cuts; the WITHHELD did not carry forward into v2. A WITHHELD is a finding, so it is re-raised here rather than allowed to lapse by omission. |
| no claim widens or narrows elsewhere; the sibling is not contradicted | **CONFIRMED** | — | Slots, band sets and ration identical; the shorter form stays the TOWN's own, so the [visitor] variant's correction of a stranger is untouched. |
| the V4 withdrawal | **CONFIRMED** | — | Right conclusion, right ground. The composer's only DS-POW-5 reader is `line5` (`powerStateProse.js:894–898`) and its bag is `{ settlement: town, seat: governing }` — no object slot is ever offered on this block, so V4 has no licence here. Measured: `{good}`, `{route}` and `{institution}` each appear in exactly **one** variant of the block's **forty** (in `economicBase: craft`, `economicBase: trade_hub` and `theocracy` respectively), never in this pool. The file's own comment beside the reader says as much. |
| the replacement tag **V1 PRESENT** | **PARTLY** | MEDIUM | V1 in MOVE-GRAMMAR §2.1 is `PRESENT` — the state key alone, ONE move. The sample's own claim list types the second sentence as "a QUALIFY move under R-DA-03". A variant cannot be V1 and carry two moves. No member of the closed set V1–V8 covers PRESENT → QUALIFY, which is the honest description; §2.1 needs either that member or a stated rule that a QUALIFY sentence does not count as a move. (Inherited: the BEFORE carries the same second fact as a tail, so the cut does not create the mis-tag — it only makes it visible.) |

**Worst finding: the tag V1 is contradicted by the pair's own claim list, which names a second QUALIFY move. PARTLY, MEDIUM.**

---

## (b) U10 IN FULL, MEASURED ON THE POOL (not asserted)

Pool `governing body name: a SLOT, never a baked noun`, k = 3 (ledger [0] · visitor [1] · street [2]). Openers use `check-pair.mjs`'s own normalisation (first two words, slots → `{}`, lowercased, stripped).

| axis | BEFORE | AFTER | verdict |
|---|---|---|---|
| two-word openers | `what governs` / `a stranger` / `the town` | `the {}` / `a stranger` / `the town` | three distinct both sides — **CONFIRMED** (no shared opener created, no spread gained on this axis) |
| sentence counts | 1 / 1 / 1 | 1 / 1 / 2 | a spread **gained** — **CONFIRMED**, and the receipt's A11 line reports it |
| variants opening on the `{settlement}` token | **0** | **0** | **CONFIRMED** — R-DA-17's per-pool limb is untouched; cut 1's DS-GEN-14 breach has no analogue here |
| close kinds | `mistake` (a verb) / `immediately` (an adverb) / `precise` (an adjective) | `mistake` / `immediately` / `precise` | **CONFIRMED unchanged** — no flattening, unlike cut 1's gap pool |
| variants opening on an article | **2** (`a stranger`, `the town`) | **3** (`the {}`, `a stranger`, `the town`) | **REFUTED** — see (d)15 |

---

## (c) DOES THE POOL SHOW TWO MOVE ORDERS OR ONE? — **PARTLY, MEDIUM**

Plainly, in three parts.

**First, the first refuter's specific charge is CURED.** Cut 1's costume was that two of the three AFTERs opened on nominals denoting the SAME referent — *The {seat}* and *The hall*, both the governing body — so the "two grammars" were one shape with two type labels. Cut 2 restores *The town* as [street]'s opener, a genuinely different referent, so that objection does not survive.

**Second, reduced to move order, the pool has one sentence-level skeleton.**

| pair | opening nominal | what it denotes | predicate | sequence |
|---|---|---|---|---|
| v2-1 AFTER | *The {seat}* | the governing body / its name | *is the name … gives itself* + a detached standing fact | INSTITUTION → PRESENT |
| v2-2 (unchanged) | *A stranger who …* | a visitor | *is corrected …* | PRESENT |
| v2-3 AFTER | *The town at {settlement}* | the town | *uses …* ; then *Both usages are precise.* | PRESENT → QUALIFY |

Every one is [a subject NP] + [a present predicate]. What varies is the referent-type of the opening nominal and, in v2-3, a sentence break. That is a real and reportable variation under §2.1's own member definitions — V1 and V5 differ precisely by whether an institution move precedes the state — but it is a thinner thing than "two grammars" suggests to a reader who has not read §2.1.

**Third, the sample's own count is not accurate.** "V5 once, V1 twice" is false of v2-3 by the sample's own claim list (a QUALIFY second move). The honest count is: **two members of the closed set are tagged (V5, V1), and the three variants realise three distinct sequences, one of which (PRESENT → QUALIFY) is not a member of V1–V8 at all.** The sample is stronger, not weaker, for saying that.

---

## (d) THE SAMPLE'S OTHER CLAIMS

| # | claim | verdict | severity | evidence |
|---|---|---|---|---|
| 1 | the three slot numbers are 8 / 5 / 2 | **CONFIRMED** | — | Declared `slots` field on `DS-POW-5`: `[settlement, seat, faction, counterpart, good, route, band, institution]` = **8**. Per-variant union over the block (declared lists + `{}` tokens in text): `{good, institution, route, seat, settlement}` = **5**. The composer's fill: `line5`'s bag `{ settlement: town, seat: governing }` = **2**. All three measured. |
| 2 | the fill literal is at `powerStateProse.js:895` | **REFUTED** | LOW | `:894` is `const line5 = (poolKey) => (poolKey`; `:895` is the `readStateProse(CORPUS, 'DS-POW-5', …)` call; the quoted `slots: { settlement: town, seat: governing }` is at **`:896`**. The citation is right for "the DS-POW-5 read" and wrong for the literal it quotes. Cite by symbol (`line5`'s bag), per the standing line-number hazard. |
| 3 | "{institution}, {route}, {good} … one variant each of forty" | **CONFIRMED** | — | `DS-POW-5` holds **40** variants in **12** pools; `{good}` in 1 (`economicBase: craft` [1] street), `{route}` in 1 (`economicBase: trade_hub` [1] street), `{institution}` in 1 (`theocracy` [3] visitor). The composer's comment (`:890–892`) is accurate. |
| 4 | the admissible level-1 set on the composed fill is **{V1, V5}**, with V2/V3/V4/V6/V7/V8 excluded for the reasons given | **CONFIRMED** | — | V3: `grep -rn "none-exists\|noneExists\|none_exists"` over the dock's `src` returns **0** hits. V4: no object slot is in `line5`'s bag (row 4's licensing list is `good, assetId, resource, a built-fabric field, a plant id`). V2, V6, V8: the chair's own A12 census measured no slot licensing any of the three anywhere on the state leaves. V7: §2.1 marks it R2-only and this is R1. |
| 5 | V5's licence on this block | **WITHHELD** | MEDIUM | §2.1 licenses V5 by "an institution row + the state key" and §1.2 row 5 names the institution TABLE's columns; that table is specified in CLERK-LAWS §1 and does not exist at this tip. A12 reads the `{seat}` SLOT as the licence. The sample declares the slot reading, so nothing is hidden — but if §2.1's letter binds, this block's admissible set is `{V1}` alone and the sample's two orders collapse to one. A chair question, not a defect. |
| 6 | "the walker's arm D reads the third [reading]" | **REFUTED** | MEDIUM | MOVE-GRAMMAR §4.1 item 2 has the walker read, per variant, "the fields the leaf exposes (the licensing set)"; §4.3 arm D reds when a tag names a member "whose licensing fields the block does not hold". Neither reads the composer's bag. **The sample contradicts itself:** its own §5 lists as an OWED instrument gap "(e) a licence arm that reads the composer's fill set, not the leaf's slot list". §1 asserts the arm already does what §5 says is owed. |
| 7 | **"On today's composed fill sets an R1 STATE block licenses at most two orders"** | **REFUTED** | **HIGH** | False at 3b1c0eaa5, on a block whose composed bag I read. **`DS-GEN-18`** has exactly ONE reader (`generalStateProse.js:1861`) and ONE bag, `craftSlots` (`:1841–1859`): it fills `{settlement}` and `{institution}` unconditionally, plus `{resource}` on UNWORKED/HOME-FED and `{good}` on BOUGHT-IN. That is an INSTITUTION licence and an OBJECT licence on one block's composed fill — **V1 + V5 + V4 = three orders**. The leaf agrees: DS-GEN-18's declared slots are exactly `[settlement, institution, resource, good]`, and its `HOME-FED [0] ledger` variant's own text carries `{settlement} {resource} {institution}` together. **`DS-ECO-11`** is the second instance and it is the chair's OWN named fallback block (SITTING §F.1): its lens-D bag (`economyStateProse.js:978–984`, read at `:1047`) supplies `{resource}`, `{good}` and `{institution}` over the shared bag. **What this costs:** the sentence is the sample's §1 finding for the owner and it is repeated in §G-shaped terms — that the third grammar waits on FIELDS the blocks do not hold and therefore on an authoring or schema wave. On the composed reading the sample itself insists on, the third grammar is available today, on at least two blocks, one of which the sitting had already nominated. The true statement is about DS-POW-5, not about R1 STATE blocks. |
| 7b | (a refinement the three-readings frame misses) | **NOTE** | LOW | The composed fill is per CALL, not per block. DS-POW-5 has one reader and one bag, so "the block's fill" is well defined there. DS-ECO-11 has four readers and two different bags, so a FOURTH reading — the per-POOL composed fill — is the one arm D would actually have to take. |
| 8 | both seeds draw DS-POW-5 at indices 0 and 1 under the corrected probe | **CONFIRMED** | — | Re-ran `gen-probe2.mjs` read-only from a copy under `MY/refuter2-scratch/`. Seed A → **Breitturm**, pop 2921, `DS-POW-5 :: governing body name … [ledger]` = index **0**. Seed B → **Langburg**, pop 2198, `[visitor]` = index **1**. Both outputs compare identical to the shipped `probe2-*.json`. Independently re-derived in Python from the kernel's own hash pair: `chair-taste-2026-09-07` → 0, `chair-taste-2026-09-07-B` → 1. |
| 9 | `foundedPoolKey(s.history)` = FOUNDED-OLD for both | **CONFIRMED** | — | Printed by the re-run for both seeds (founding ages 265 and 244). The first cut's GAP block was indeed never theirs. |
| 10 | the power desk's readings cannot change WHICH `governingTitle` line is drawn | **CONFIRMED** | — | `powerStateProse.js:966`: `const namedKey = governing ? GOVERNING_NAME_POOL : null` — a module constant (`:708`), gated only on `power.governingName`, which is the SETTLEMENT's field and not a reading. `:990` calls `line5(namedKey)`; the draw is a function of seed, blockId and poolKey alone (`stateProseKernel.js:303–304`), and eligibility is audience plus slot-anchoring (`eligibleVariants`). The pool's three variants carry **no `marks`** (measured — their only keys are `angle`, `slots`, `text`), so the audience cannot filter them either. Readings reach only `rulingKey` (`:965`), the OTHER DS-POW-5 rung. |
| 11 | the probe builds the power desk's readings "as `PowerTab.jsx:200` builds them" | **PARTLY** | LOW | PowerTab's bag (`:200–214`) is `{contenders, riskLabel: coupRiskLabel(contenders), structuralLens, politics}`. The probe builds `{contenders, structuralLens}`. Its own header declares only the `politics` omission and states that only DS-POW-7 goes silent — **that is false**: with `riskLabel` undefined, `riskPoolKey` (`:508–511`) returns null and DS-POW-4's `successionRisk` rung goes silent too. Nothing here touches DS-POW-5, so the two-seed claim is unaffected; the probe's self-description is what is inaccurate. |
| 12 | the draw formula and the canonical-at-zero line | **CONFIRMED** | — | `stateProseKernel.js:304` is the quoted expression verbatim; `:303` is `if (!seed) return eligible[0];`. |
| 13 | no AFTER is longer (24→23, 17→17, 23→20) | **CONFIRMED** | — | Measured exactly those three. |
| 14 | the §4 receipt reproduces | **CONFIRMED** | — | Re-ran `check-pair.mjs` against the dock with cwd under `MY/refuter2-scratch/`: the output is **identical character-for-character** to `check-pair-pow5-v2.out` and to the fenced block inside the sample, including `corpus loaded: 2734 variants in 1020 pools`. |
| 15 | "article-openers 2 → 2 (the first cut's 2 → 3 undone)" | **REFUTED** | MEDIUM | Measured with check-pair's own opener normalisation: BEFORE `what governs` / `a stranger` / `the town` = **2** article-initial; AFTER `the {}` / `a stranger` / `the town` = **3**. On a definite-article-only reading it is **1 → 2**. On no reading is it 2 → 2. What cut 2 undid was cut 1's `the hall` — the REFERENT — not the article count; the count is unchanged from cut 1. No rule caps article-openers per pool, so this is a mis-reported figure rather than a breach — but the sample's own §4 stands on receipts stating what they measured, and this figure is in the C-sibling paragraph a reader will take as measured. |
| 16 | "settlement-token openers 0 → 0" | **CONFIRMED** | — | No variant opens on the `{settlement}` token in either state. v2-1's new `the {}` opener is `{seat}`, not `{settlement}`, so R-DA-17's per-pool limb is untouched. |
| 17 | the pool's openers are three distinct | **CONFIRMED** | — | Three distinct before AND after; no spread was gained or lost on this axis. |
| 18 | "six of thirteen tabs hold one or two mounts today" | **CONFIRMED** | — | Counted from `dossierMounts.js`: overview 14 · economics 10 · defense 8 · power 7 · faith 4 · war 3 · history 3 · viability 2 · services 1 · resources 1 · relationships 1 · plot_hooks 1 · daily_life 1 = 13 tabs, 56 mounts, **6** at ≤ 2. Reproduces the sitting's A10 exactly. |

---

## (e) C-SIBLING FOR THE POOL AFTER THE CUT

**PARTLY, MEDIUM.**

1. **Do the three cohere in structural fact? Not quite, and the divergence is the corpus's.** [0] and [1] name the governing BODY through the `{seat}` slot; [2] names *the hall*. The composer's own comment (`powerStateProse.js:854–864`) states that `{seat}` carries two incompatible roles across these blocks — the governing BODY in DS-POW-1, and in DS-POW-2 the HALL, a place the body occupies — and `:890` states that DS-POW-5 takes the BODY role. So [2]'s *the hall's proper title* is a title of the building, and the sample's C-sibling sentence ("the three variants assert one governing body, named `{seat}`") is **not true of [2] as written**. Under CLERK-LAWS §2.2 C5 the institution named is a structural fact, so this is a genuine sibling divergence rather than a small particular.
2. **Is variant [2]'s lack of a `{seat}` slot a defect the corpus already holds? CONFIRMED — the fact, named from the leaf:** at `DS-POW-5`, pool `governing body name: a SLOT, never a baked noun`, index 2, angle `street`, the variant's **declared `slots` list is `["settlement"]`** and its text carries the `{settlement}` token only. That is true of the BEFORE and of the AFTER alike; the cut neither creates nor sharpens it. The sample declares the fact and hands the question to the refuter, which is the honest handling.
3. **Is the [visitor]/[street] tension pre-existing and unsharpened? CONFIRMED.** Both the BEFORE and the AFTER keep the TOWN as the user of the shorter form, and both attribute the proper title to the hall by a possessive (*its proper title* → *the hall's proper title*). Cut 1's sharpening — the hall generally *going by* a shorter form, which is what the sibling says draws a correction — is fully undone. No count, exemption, band, geography, calendar or outcome moved.

---

## (f) THE ABSENCE SECTION

**CONFIRMED, with one attribution correction.**

- **LACK (V3) NOT-EXECUTABLE — CONFIRMED.** `grep -rn "none-exists\|noneExists\|none_exists"` over the dock's `src` returns **0** hits. A schema act precedes any LACK sentence, exactly as the sample says.
- **GAP (V8) NOT-EXECUTABLE — CONFIRMED.** `generalStateProse.js:623` is `const key = !history.founding ? 'GROWN-UNRECORDED' : …` — the key is computed from the FALSINESS of the field, which R-DA-08's own Obeys (5) refuses as a licence ("never by the block merely lacking one"), and the chair's A12 measured no slot licensing V8 anywhere on the state leaves. The sample's withdrawal of cut 1's two GAP pairs as a demonstration is correct and is stated rather than buried.
- **The 48-of-48 figure — CONFIRMED, and now measured rather than inherited.** The leaf's own comment at `:615` asserts it; the sample re-asserts it with no citation. I measured it: 48 settlements generated read-only through the product's `generateSettlementPipeline` (4 settlement types × 3 cultures × 4 terrains, my own seeds) — **48 of 48 carry `history.founding`**; `foundedPoolKey` returned FOUNDED-OLD 44, FOUNDED-YOUNG 4, **GROWN-UNRECORDED 0**. **PARTLY, LOW** on the presentation only: attribute the figure to the leaf comment or print a measurement, per the citation law.
- The sample's statement that both absence classes wait on the schema acts and on the walker's arm I is a declaration, not a claim, and I do not grade it.

---

## SCORE

Counts are of tests **explicitly graded** above (n/a and NOTE rows are not counted).

| section | CONFIRMED | REFUTED | PARTLY | WITHHELD | worst finding | severity |
|---|---|---|---|---|---|---|
| **v2-1** | 9 | 1 | 3 | 1 | the rendered AFTER garden-paths into the body/name conflation cut 1 was withdrawn for; and *calls itself* → *gives itself* moves the act from use to conferral — the substitution this same file refuses in v2-2 | **MEDIUM-HIGH** |
| **v2-2** | 8 | 0 | 1 | 0 | the typed claim list writes *another* where the sentence writes *any other* | LOW |
| **v2-3** | 9 | 0 | 2 | 1 | the tag V1 is contradicted by the pair's own claim list, which names a second QUALIFY move | MEDIUM |
| **(c) move orders** | 1 | 0 | 1 | 0 | "V5 once, V1 twice" is not accurate; three sequences are realised and one is not a member of V1–V8 | MEDIUM |
| **(d) claims** | 12 | 4 | 1 | 1 | "an R1 STATE block licenses at most two orders on today's composed fill sets" is false — DS-GEN-18 licenses three | **HIGH** |
| **(e) C-sibling** | 2 | 0 | 1 | 0 | the C-sibling sentence is not true of [2], which names the hall, not the `{seat}` body | MEDIUM |
| **(f) absence** | 4 | 0 | 1 | 0 | the 48-of-48 figure is re-asserted without its source | LOW |
| **TOTAL** | **45** | **5** | **10** | **3** | | |

**The single worst finding: (d)7.** The sample's §1 tells the owner that the record's blocks cannot yet carry a third structure, and that the deeper variety therefore waits on an authoring or schema wave. On the composed-fill reading the sample itself adopts, that is false at this tip: `DS-GEN-18`'s single composed bag offers `{institution}` together with `{resource}` or `{good}`, and `DS-ECO-11` — the block the chair's own sitting named as the fallback — offers `{resource}`, `{good}` and `{institution}` on its exploitation lens. Three level-1 orders are licensed on today's data. The finding is true of DS-POW-5 and was generalised to the register without a receipt. It is the one row in this sample that would change what the owner decides.

---

## CURES (a cure never adds a claim)

**Every text cure below was EXECUTED through `check-pair.mjs` against the dock at 3b1c0eaa5, with cwd under `MY/refuter2-scratch/`, and passes every mechanical arm** (`MY/refuter2-scratch/cures-v2.json`; receipt at the end of this section). Word counts are measured, not counted by eye. Offered as "test this, don't trust it" — the chair rules.

### Text cures

1. **v2-1 (MEDIUM-HIGH) — keep the V5 opener, keep *calls itself*, kill the garden path.**
   **Cure 1 (24 → 23 words, PASS):** *"The {seat} is what {settlement}'s government calls itself, a name precise about the town's size and pretensions in a way outsiders routinely mistake."*
   Rendered on seed A: *"The Guild Council is what Breitturm's government calls itself, a name precise about the town's size and pretensions in a way outsiders routinely mistake."* It opens on the institution as V5 requires; it keeps the BEFORE's own verb, so the act stays self-naming-in-use and the conferral shift is not spent; it restores *a name* as an overt appositive host, so body and name are apart and *precise* has one unambiguous controller; the manner clause is verbatim; there is no *name what* to re-parse. `{settlement}` is a `proper` fill and takes a genitive, not an article, so A3's only breach remains the relocated *The {seat}*, declared exactly as the sample declares it. The one thing it introduces is the definite description *{settlement}'s government* for *what governs {settlement}* — the same referent, and `government` is the estate's own field name for it (`power.government`, filled into `{seat}` at `defenseStateProse.js:658`), so it is a varied noun rather than a new particular.
   **Cure 1-alt (24 → 24 words, PASS):** *"The {seat} is the name what governs {settlement} calls itself by, precise about the town's size and pretensions in a way outsiders routinely mistake."* Keeps *what governs {settlement}* verbatim and restores *calls*, at equal length; it is the more strained sentence and it does not remove the *name what* adjacency, so I offer it only as the form for a chair who will not admit *government*.
   **Fallback:** the first refutation's cure 1a (*"What governs {settlement} calls itself the {seat}, a name precise about …"*, 24 → 22, already passed) preserves every claim exactly and delivers no V5 opener. If the chair rules that the V5 opener is not worth a new noun, 1a is the honest outcome and the pool then shows one order — which is a finding, not a failure.
2. **v2-3 (MEDIUM) — take the R-DA-03 gain without moving the act (23 → 22 words, PASS).**
   *"The town at {settlement} calls the hall by its proper title in public and something shorter everywhere else. Both usages are precise."*
   The BEFORE's verb and its object survive untouched, so the address relation between the town and the hall stays asserted rather than implied; the tail still becomes its own sentence, so the A11 sentence spread still goes 1/1/1 → 1/1/2 and the R-DA-03 move is still shown; the agent is still the town, so the sibling is still safe; and it is one word shorter than the BEFORE. It costs two words against the adopted cure 3 and buys back the only claim that pair moves.

### Declaration cures (no text moves)

3. **(d)7 (HIGH) — restate the finding as DS-POW-5's, and name the counter-examples.** Replace "on today's composed fill sets an R1 STATE block licenses at most two orders" with the measured form: *on DS-POW-5's composed fill the admissible set is {V1, V5} — two members for three variants*; and add that at least two R1 blocks DO license three today (`DS-GEN-18`, whose one bag `craftSlots` fills `{institution}` with `{resource}` or `{good}`; `DS-ECO-11`, whose exploitation lens fills `{resource}`, `{good}` and `{institution}`). The owner's takeaway changes with it: the third grammar is not blocked by the data everywhere, only here — and the sitting's own fallback block was one of the places it is available. This makes the licensing-filter demonstration *stronger*, because it shows a filter that removes members on one block and admits them on another.
4. **(d)6 (MEDIUM) — do not credit arm D with the reading it does not have.** §1's "the walker's arm D reads the third" contradicts §5's own owed item (e). Say instead that arm D as specified reads the LEAF's fields (§4.1 item 2) and that the composed-fill arm is owed; the sample is the evidence for why it is owed.
5. **(d)15 (MEDIUM) — correct the article-opener figure to 2 → 3** (or 1 → 2 on the definite-only reading), and say what was actually undone: cut 1's referent (`the hall` → `the town`), not the article count. Note in the same line that no rule caps article-openers per pool, so nothing is breached — a corrected figure costs the sample nothing and an uncorrected one costs it a receipt.
6. **v2-1 (MEDIUM-HIGH) — reconcile the two rulings on one verb.** Either adopt cure 1 (which removes the question), or state in v2-1's rule field why *calls itself → gives itself* is licensed on [ledger] while *addresses … by → gives* is refused on [visitor]. As the file stands, the same widening is a reason to KEEP one variant and a reason to CUT another.
7. **v2-3 (MEDIUM) — tag the sequence, not a member it is not.** Either print `PRESENT → QUALIFY` and note that §2.1 holds no member for it, or add the member. "V1" beside a claim list naming two moves is a tag a reader falsifies by counting.
8. **v2-2 (LOW) — write *any other* in the typed claim.** The sentence quantifies universally; the claim list should, or arm C will compare a looser set than the one the corpus ships.
9. **(d)2 (LOW) — cite by symbol.** The quoted `slots:` literal is `line5`'s bag at `:896`; `:895` is the `readStateProse` call. A line number in a document is wrong at its own commit sooner than a symbol is.
10. **(f) (LOW) — attribute the 48-of-48**, to the leaf comment at `generalStateProse.js:615`, or print the measurement (mine is in this file: 48 of 48, four types × three cultures × four terrains, GROWN-UNRECORDED 0).
11. **v2-3 (LOW) — carry the first refutation's WITHHELD forward.** The taste residual on *Both usages are precise* attaches to a sentence that did not change between cuts; a WITHHELD that lapses by omission is a finding lost.
12. **(d)11 (LOW) — correct the probe's own header.** `gen-probe2.mjs` omits `riskLabel` as well as `politics`, so DS-POW-4's `successionRisk` goes silent too; "only DS-POW-7 goes silent" is not true of the probe as written. Either add `riskLabel: coupRiskLabel(contenders)` or declare both omissions.

### Instrument gaps this cut exposes (for the walker lane, in addition to the five the sample already lists)

13. (f) **an ARTICLE-opener and OPENING-SHAPE arm per pool** — the two-word string test reports three distinct openers while all three open on an article; the shape the reader meets is the one the sample tried to report and mis-counted. (g) **a per-POOL composed-fill resolver**, not a per-block one — DS-ECO-11 shows the bag differing between lenses of one block, so arm D's licence set is keyed on (block, pool), not on block. (h) **a garden-path / re-parse arm is NOT proposed** — that is a reader's judgment and belongs to the owner's walk; what an instrument can hold is the adjacency of a bare relative to a head noun (`name what`, `title what`), which is cheap and would have caught this one.

### The cures' receipt (executed)

```
$ node check-pair.mjs <laneB6> cures-v2.json
#cure-v2-1 PASS(mechanical) DS-POW-5 :: governing body name: a SLOT, never a baked noun [ledger]
    · A11 SPREAD (sentences, pool "governing body name: a SLOT, never a baked noun" DS-POW-5, angles ledger/visitor/street): 1/1/1 → 1/1/1
#cure-v2-1-alt PASS(mechanical) DS-POW-5 :: governing body name: a SLOT, never a baked noun [ledger]
    · A11 SPREAD (sentences, pool "governing body name: a SLOT, never a baked noun" DS-POW-5, angles ledger/visitor/street): 1/1/1 → 1/1/1
#cure-v2-3 PASS(mechanical) DS-POW-5 :: governing body name: a SLOT, never a baked noun [street]
    · A11 SPREAD (sentences, pool "governing body name: a SLOT, never a baked noun" DS-POW-5, angles ledger/visitor/street): 1/1/1 → 1/1/2

corpus loaded: 2734 variants in 1020 pools; 3 pass mechanically, 0 WITHHELD (R4-BAND — the band half is the refuter's, not a FAIL), 0 fail (a mechanical pass is NOT a claim-preservation verdict — that is the refuter's job)
```
Word counts, measured: cure-v2-1 24 → 23 · cure-v2-1-alt 24 → 24 · cure-v2-3 23 → 22. None longer. **A mechanical pass is not a claim-preservation verdict here either** — these are the refuter's proposals and the chair rules on their claims exactly as it rules on the sample's.

---

*Read-only throughout. No tree was edited, committed, checked out, stashed or reset; no vitest and no npm was run; nothing was written inside the dock, the main tree or `K`. `check-pair.mjs` and `gen-probe2.mjs` were executed with absolute paths or from a copy, with cwd under `MY/refuter2-scratch/`; the only files written are this one and the scripts, probe outputs and receipts beneath `MY/refuter2-scratch/`.*
*Dock porcelain: **0 before, 0 after**. Product **3b1c0eaa5** unchanged.*
