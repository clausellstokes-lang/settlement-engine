Seat: Fable 5.1 — the chair's authoring, THIRD AND FINAL CUT (session 5540cfd2, 2026-09-07 18:12 EDT); mechanically checked at laneB6 3b1c0eaa5; two Opus refutations behind it (`refute-taste-912.md`, `refute-taste-912-v2.md`); the chair's rulings on the second are §7; a third, targeted refutation is appended at §8 when it returns

# THE TASTE SAMPLE, v3 — one block, one pool, all its variants, in the shapes its composed fields license; the same block under two seeds

**What this is, in plain words.** One block of the town record, its three lines rewritten (or kept) so that the facts do not change and the shape does where the data allows it. Two adversaries have tried to break it; what they found is applied or ruled on below, and every number here is one they or the chair measured. Nothing is applied to the product; the corpus at 3b1c0eaa5 is unchanged. A mechanical pass is not a claim-preservation verdict (`check-pair.mjs:136`).

## 1. The block, the pool, the fields
- **Block** `DS-POW-5` (power.generated.js). Its slot set has THREE readings: the leaf's declared `slots` field lists **8**; the union over its forty variants' text is **5** (`{settlement} {seat} {institution} {route} {good}` — the last three in one variant each, never in this pool); the shipped composer's bag for this block — `line5` in `powerStateProse.js` (the literal at `:896`) — fills **2**: `{settlement}` and `{seat}`. The walker's arm D, as MOVE-GRAMMAR §4.1 specifies it, reads the LEAF's fields; the arm that reads the composer's bag is OWED (§5), and this sample is the evidence for why — and the bag is per CALL, so on a block with several readers (DS-ECO-11 has four) the licence set is keyed on (block, pool), not on block.
- **Pool** `governing body name: a SLOT, never a baked noun` — three variants (ledger [0] · visitor [1] · street [2]); the pool's claim: the governing body's name is the `{seat}` slot and the town uses it.
- **The admissible level-1 shapes on THIS block's composed fill:** **V1** PRESENT and **V5** INSTITUTION → PRESENT (licensed by `{seat}` — a SLOT naming the institution; the chair RULES, §7, that at this tip a composed slot naming the institution is a lawful V5 licence, the institution table being a derived projection of exactly these fields). Not drawable here: V4 (no object slot in the bag), V2 (no structural-consequence field), V3 (no `none-exists` field anywhere), V6 (no unresolved STATE field — only display labels), V8 (no not-held provenance), V7 (R2 only). **Two members for three variants — on DS-POW-5.** That is NOT true of the register: **at least two R1 blocks license THREE shapes today** — `DS-GEN-18`, whose one composed bag (`craftSlots`, `generalStateProse.js:1841–1859`) fills `{institution}` with `{resource}` or `{good}`, and `DS-ECO-11`, whose exploitation lens (`economyStateProse.js:978–984`) fills `{resource}`, `{good}` and `{institution}`. The licensing filter therefore removes members on one block and admits them on another, which is the demonstration; and twenty of sixty-eight blocks compose only `{settlement}` (SITTING §A12), where it admits one.
- **The two seeds, through the SHIPPED readings path** (`gen-probe2.mjs`: the general desk through `generalDeskLines`; the power desk with `structuralLensOf(s)`, `coupContenders(s)` and `coupRiskLabel` as `PowerTab.jsx:200` builds them; only the worldState `politics` projection omitted): `chair-taste-2026-09-07` → **Breitturm** draws index **0** [ledger] (the V5 line); `chair-taste-2026-09-07-B` → **Langburg** draws index **1** [visitor] (the V1 line). The second refuter re-derived both from the kernel's own hash pair. The `governingTitle` pool key is a module constant gated only on the settlement's `power.governingName`, so no reading can move this draw. The draw is `eligible[avalanche32(fnv1a32(`${seed}::${blockId}::${poolKey}`)) % eligible.length]` (`stateProseKernel.js:304`; `:303` returns index 0 for a falsy seed — canonical-at-zero). The seed that picks a variant picks its shape; no new mechanism; nothing an installed world draws moves.
- **Level 2 is UNSHOWN** (owner-gated under THE PROMISE; six of thirteen tabs hold one or two mounts).

## 2. The three variants (pairs file `pairs-pow5-v3-2026-09-07.json`) — the tense printed beside every tag
### v3-1 — `DS-POW-5 :: governing body name: a SLOT, never a baked noun` [ledger] index 0 — **V5 INSTITUTION → PRESENT (tense: present throughout)**
BEFORE: What governs {settlement} calls itself the {seat}, and the name is precise about the town's size and pretensions in a way outsiders routinely mistake.
AFTER:  The {seat} is what {settlement}'s government calls itself, a name precise about the town's size and pretensions in a way outsiders routinely mistake.
CLAIMS (the typed set, identical before and after — C-pair): the governing body calls ITSELF the {seat} (the BEFORE's own verb; self-naming in use, the agent the body — '{settlement}'s government' is the same referent as 'what governs {settlement}', a varied noun and the estate's own field name, never a new particular); the name is precise about the town's size; the name is precise about the town's pretensions; outsiders routinely mistake the MANNER of that precision ('in a way outsiders routinely mistake' verbatim)
RULE: V5: opens on the institution ({seat}) and ends on the present state; the second refuter's cure 1 ADOPTED by the chair — it keeps 'calls itself' (the v2 'gives itself' moved the act from use to conferral, the same widening the chair refused on the visitor variant), restores 'a name' as an overt host so body and name stay apart and 'precise' has one controller, and removes the 'name what governs' garden path the v2 sentence had; 24 → 23 words; A3 note: the proper fill {seat} after the article at the opening — relocated, not created, declared; {settlement}'s takes a genitive, not an article

### v3-2 — `DS-POW-5 :: governing body name: a SLOT, never a baked noun` [visitor] index 1 — **V1 PRESENT (tense: present) — KEPT AS WRITTEN**
BEFORE: A stranger who addresses the {seat} at {settlement} by any other title is corrected, politely and immediately.
AFTER:  A stranger who addresses the {seat} at {settlement} by any other title is corrected, politely and immediately.
CLAIMS (the typed set, identical before and after — C-pair): a stranger who addresses the {seat} by ANY OTHER title is corrected (the universal quantifier as the sentence has it); the correction is polite; the correction is immediate
RULE: KEEP: the variant is already in the plain-fact grammar; the first cut ('gives … any other title') widened the act from address to conferral (the refuter's PARTLY) and bought nothing measurable — no cut earns its risk here; a KEEP is a lawful member of the sample (the 09-05 sample's own convention)

### v3-3 — `DS-POW-5 :: governing body name: a SLOT, never a baked noun` [street] index 2 — **V1 PRESENT + a QUALIFY second sentence (R-DA-03) — the sequence PRESENT → QUALIFY; QUALIFY is not an order member of V1–V8 (the chair rules it a licensed second sentence inside any member, carrying a second fact of the same field); tense: present**
BEFORE: The town at {settlement} calls the hall by its proper title in public and something shorter everywhere else, and both usages are precise.
AFTER:  The town at {settlement} calls the hall by its proper title in public and something shorter everywhere else. Both usages are precise.
CLAIMS (the typed set, identical before and after — C-pair): the town calls the hall by its proper title in public (the BEFORE's verb and object kept — the address relation asserted, not implied); the town uses a shorter form everywhere else (the town's own — the agent kept); both usages are precise (a second fact, its own sentence — a QUALIFY move under R-DA-03)
RULE: the second refuter's cure 2 ADOPTED: 'calls the hall by its proper title' kept verbatim (the v2 'uses the hall's proper title' demoted the address relation to implicature), the ', and both usages' tail becomes its own sentence (R-DA-03), the agent stays the town so the [visitor] sibling is not contradicted; 23 → 22 words; the V4 tag stays withdrawn (the composer's DS-POW-5 bag — `line5` at powerStateProse.js:896 — fills {settlement, seat} only); the corpus fact that this variant carries no {seat} slot and names the body by its hall is declared as a pre-existing sibling divergence for the wave

**What the pool shows, plainly.** Two members of the closed set are realised (V5 once, V1 twice), and three SEQUENCES: INSTITUTION → PRESENT · PRESENT · PRESENT → QUALIFY. Every variant is a subject and a present predicate; what varies is whether an institution move precedes the state, whether a second fact takes its own sentence, and the referent that opens (the body · a stranger · the town). That is the variation §2.1 defines and it is thinner than the word "grammars" suggests; the fuller variation is on blocks whose bags carry an object or a consequence, and on the fields the §912.1 authoring wave adds.
**C-sibling.** The three cohere in band, office, status, quantifier, geography, calendar and outcome, and differ in standpoint. One pre-existing divergence, the corpus's not the cut's: [2] names the body by its HALL (a building; its declared slots are `["settlement"]` only) while [0] and [1] name it by `{seat}` — a finding for the wave, declared. The [visitor]/[street] tension is pre-existing and unsharpened (the shorter form stays the town's own). Openers: "the {}" · "a stranger" · "the town" — three distinct, before and after; settlement-token openers 0 → 0; **article-openers 2 → 3** (no rule caps them); close kinds unchanged (`mistake` · `immediately` · `precise`); sentence counts 1/1/1 → 1/1/2.

## 3. The absence classes — declared, not demonstrated
- **LACK (V3): NOT-EXECUTABLE at this tip** — no `none-exists` field exists in `src` (0 hits).
- **GAP (V8): NOT-EXECUTABLE at this tip** — the one pool naming an unrecorded founding, `DS-GEN-14 :: GROWN-UNRECORDED`, is keyed by `generalStateProse.js:623` on the mere falsiness of `history.founding` (R-DA-08 refuses "the block merely lacking" a field as a licence); the leaf's comment at `:615` says the generator writes a founding on 48 of 48 settlements, and the second refuter MEASURED it (48 generated across four types, three cultures, four terrains: FOUNDED-OLD 44, FOUNDED-YOUNG 4, GROWN-UNRECORDED 0). The first cut's GAP pairs are withdrawn as a demonstration.

## 4. The mechanical receipt (executed; verbatim)
```
$ node check-pair.mjs $SC/laneB6 pairs-pow5-v3-2026-09-07.json
#v3-1 PASS(mechanical) DS-POW-5 :: governing body name: a SLOT, never a baked noun [ledger]
    · A11 SPREAD (sentences, pool "governing body name: a SLOT, never a baked noun" DS-POW-5, angles ledger/visitor/street): 1/1/1 → 1/1/1
#v3-2 PASS(mechanical) DS-POW-5 :: governing body name: a SLOT, never a baked noun [visitor]
    · A11 SPREAD (sentences, pool "governing body name: a SLOT, never a baked noun" DS-POW-5, angles ledger/visitor/street): 1/1/1 → 1/1/1
#v3-3 PASS(mechanical) DS-POW-5 :: governing body name: a SLOT, never a baked noun [street]
    · A11 SPREAD (sentences, pool "governing body name: a SLOT, never a baked noun" DS-POW-5, angles ledger/visitor/street): 1/1/1 → 1/1/2

corpus loaded: 2734 variants in 1020 pools; 3 pass mechanically, 0 WITHHELD (R4-BAND — the band half is the refuter's, not a FAIL), 0 fail (a mechanical pass is NOT a claim-preservation verdict — that is the refuter's job)
```
Word counts: v3-1 24 → 23 · v3-2 17 → 17 (KEPT) · v3-3 23 → 22. WITHHELD 0.

## 5. OWED and declared
- The B-GRAMMAR walker's receipt (order histogram; arm D against the leaf's fields; the negative controls) — the instrument lane's first product; the C-sibling extractor.
- Instrument gaps the two refutations exposed, for the walker lane: (a) a settlement-token-opener count per pool; (b) a close-KIND arm; (c) the CONTRAST regex extended to "never X so much as Y" and trailing coordinate negation; (d) a TENSE arm; (e) a licence arm reading the composer's bag, keyed on (block, pool); (f) an article-opener / opening-shape arm; (g) a bare-relative adjacency check (`name what`, `title what`).
- The Herald / ladder / chronicle demonstrations of MOVE-GRAMMAR §6 items 4–5.

## 6. The gate that remains
A third, targeted Opus refutation over the two changed sentences (v3-1, v3-3) and the corrected claims of §1–§2; its findings and the chair's rulings are §8.

## 7. THE CHAIR'S RULINGS ON THE SECOND REFUTATION (`refute-taste-912-v2.md`: 45 CONFIRMED · 5 REFUTED · 10 PARTLY · 3 WITHHELD) — all upheld
1. (d)7 HIGH UPHELD — "an R1 STATE block licenses at most two orders" was false; restated as DS-POW-5's fact with DS-GEN-18 and DS-ECO-11 named (verified by the chair at `generalStateProse.js:1841–1859` and `economyStateProse.js:978–984`). The sitting's §A12 ("at most three on the richest, one on twenty") stands; the v2 document's sentence was the error.
2. v2-1 REFUTED UPHELD — cure 1 adopted (the BEFORE's verb kept; the garden path removed); the chair's v2 sentence had refused a widening on [visitor] and committed it on [ledger].
3. v2-3 PARTLY UPHELD — cure 2 adopted; the sequence tagged PRESENT → QUALIFY; **RULED: QUALIFY (R-DA-03's second fact of the same field, its own sentence) is a licensed second sentence inside any V-member, not an order member** — MOVE-GRAMMAR §2.1 gains the note (the sitting's superseded-passages appendix carries it).
4. WITHHELD (V5's licence is a slot, not a table row) — **RULED: at this tip a composed slot naming the institution (`{seat}`, `{institution}`) is a lawful V5 licence**; the institution table is a derived projection of these fields and the slot is the row's name column; when the table exists, the row replaces the slot.
5. (d)6, (d)15, (d)2, (f) UPHELD — corrected above (arm D reads the leaf; article-openers 2 → 3; `:896` / by symbol; the 48-of-48 attributed and measured).
6. (e)1 UPHELD — [2] names the hall; declared as the corpus's divergence, for the wave.
7. (d)11 UPHELD — the probe now supplies `riskLabel` and declares the `politics` omission.
8. WITHHELD (the taste residual on "Both usages are precise") — carried to the owner's walk as a reader's judgment; not a gate.
9. v2-2 LOW UPHELD — the claim list reads "any other".

Product 3b1c0eaa5 UNCHANGED. Porcelain 0 → 0 at every check.
