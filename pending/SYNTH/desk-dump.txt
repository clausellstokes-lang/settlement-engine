### not_covered: This source is the DESK remainder only — the dossier mount registry, the desks that feed it, and what a mount costs. It does NOT cover: (1) the §891 LANDING itself (pre-gate, gate, chair-verify, CAS, seal, §893 collection) — the ordering source owns that, and DESK-8's refreeze is one line inside it; (2) the HORIZON-DARK remainder (5 of 11 chair-class cars, 3 of them owner-gated: TUNEREG Car 5 THE CULL, CHARSET Car 3, the COVERAGE vocabulary exports); (3) the LIGHTING WAVE proper — its scope is flag doors, HORIZON's three named doors, the MAT dial and product flags, and its own inventory states that explicitly; the only overlap is DESK-7, which §892 docketed INTO that wave as a content car; (4) the GOLDEN freeze (⚠ its seven cars are UNLANDED and 197+ commits behind), W-ARMS, the owner's walk, and the exhaustive review; (5) the ENCOUNTERS program and the src-prose car; (6) the CAUSAL dossier register (causalDossierProse.js) and dmFieldProjection.js — sibling stateProse modules with their own darkness, which I did NOT census because the source is the STATE-prose mount registry; a separate reader should price them, since the receipt's CHECKPOINT 2 finding ('the whole stateProse subsystem has zero runtime consumers') was true of them too and DESK CAR 1 lit only the economy half; (7) the §0c-4 SHAPE RESIDUE — three named deferrals in the same annex that are NOT desk work but will bite a desk lane: the 20-site sentence-initial lowercase seam (cure is ~3 lines in fillSlots but the kernel is a SHARED seam and needs ONE consist), the {band} slot serving six incompatible roles across 26 blocks (one wave or none), and {reason}'s determiner split. Any desk car mounting a block that uses those slots inherits the degradation.

### notes: ['OPUS-AUTHORED. READ-ONLY pass; nothing written outside $SC/pending/DESK/. The O2GATE dock was never touched — every product read went through `git -C /Users/cstokes/Desktop/settlement-engine show <sha>:<path>` against the main repo.', "⛔ THE CAR LIST WAS STALE AND I CAUGHT IT. The brief and $SP/LANE-QUEUE.md:46-49 both describe the paid-surface gap as open. It is NOT: O2GATE committed and sealed while I was reading — `refs/preserve/train891-o2gate-2026-09-04` = 450f7dbb7, one car over 15c6368a6, 3 files (+98/-8). The economy desk's state prose no longer renders on the public dossier. The §891 train is 19 cars, not 18. I therefore report NO pending item for the paid-surface leak, only the missing INSTRUMENT behind it (DESK-9).", "THE ARITHMETIC RE-DERIVED, AND THE QUEUE'S FIGURE IS OFF BY ONE CAR IN THE WRONG DIRECTION. $SP/LANE-QUEUE.md:46-49 says '4 mounts cost one new 113-line component ⇒ roughly 17 more desks'. 17 = 68/4, which counts the car already built. The remainder is 64 dark blocks, so at DESK CAR 1's observed rate it is SIXTEEN more cars, not seventeen. But the per-car rate is the wrong unit anyway: DESK CAR 1 delivered 4 blocks over 5 POSITIONS in one tab, and the real unit is one corpus leaf per host tab. By leaf the remainder is SIX dispatchable desk items (DESK-1..DESK-6), which I estimate at 8-13 cars total, not 16 — because five of the six host tabs have 233-453 effective lines of headroom and will need no forced extraction, whereas DESK CAR 1 spent a whole component leaf buying room in a tab that sat at exactly 600/600.", "MEASURED CORPUS CENSUS (own probe, $SC/pending/DESK/CENSUS.txt, parsed from the six generated leaves at 15c6368a6): 68 blocks · 708 pools · 2,266 variants. Per leaf — defense 11/126/383 · economy 15/105/329 · general 23/193/634 · power 7/79/256 · stressors 3/67/246 · warFaith 9/138/418. The 64 DARK blocks carry 681 pools and 2,185 variants. Cross-check: the economy leaf's own generator header says '15 blocks, 329 variants' — exact agreement, so the parse is sound.", 'WHAT \'DARK\' COSTS THE PRODUCT, stated plainly: 2,185 authored variants of settlement prose exist in the shipping bundle\'s lazy data chunk and reach no reader. DESK CAR 1 put the corpus in the product for the first time (`grep -rl "COMBINATION C1" dist/assets` went exit 1 / 0 files → exit 0 / 2 files) at a cost of +64,046 B in data-lazy and +6,424 B in the EconomicsTab chunk, with ZERO first-paint and ZERO engine growth — $SP/receipt-deskwiring.md:249-268. So the remaining desk work is byte-cheap by construction: it lands in lazy tab chunks, and the corpus bytes are ALREADY shipping.', '⛔ THE ONE TRAP A DESK LANE MUST NOT FALL INTO, and it is written into the walker by design: the REACHABILITY arm only checks that a mount id appears as a string literal at exactly ONE site under src/components. It cannot tell a real draw from a decorative literal. Planting 64 bare literals would PASS THE GATE AND LIE (THE CITATION LAW). Evidence: tests/lint/dossierMountRegistry.walker.test.js lines 281-284 (`unreachableMounts` counts sites) and $SP/receipt-deskwiring.md:39-44.', "SIZE HEADROOM AT THE O2GATE TIP, to the 600-effective-line src/components/*.jsx LAYER ceiling (no .size-baseline.json entry for any of them, so the layer number is the ceiling). Figures are eslint-Linter measurements from $SP/receipt-deskwiring.md:71-79, and `git diff --stat ca651d54b 450f7dbb7 -- src/components/new/tabs/` proves only EconomicsTab/EconomicsGlance moved: EconomicsTab 580 (20 free) · EconomicsGlance 50 (550) · OverviewTab 322 (278) · DefenseTab 314 (286) · PowerTab 300 (300) · WarTab 367 (233) · FaithTab 147 (453) · HistoryTab 287 (313) · OutputContainer 600 (ZERO free — from 450f7dbb7's commit body).", "NOTHING IN THIS SOURCE IS OWNER-GATED except by adjacency. The desk work is chair-class content and wiring throughout. Three owner facts touch it: the faith mounts cannot be WALKED until the review corpus stops being deity-free (0 of 44 settlements carry a patron — the owner's act, $SP/LANE-QUEUE.md:35-36); every push and the walk itself stay the owner's; and the paid-surface DEFAULT that O2GATE implemented came from an owner carve-out by nature, so any future desk car that would SHOW state prose on a public dossier is an owner question, not a chair one.", 'A DESK CAR OWES FOUR REGISTER ACTS BEYOND ITS CODE, all measured by DESK CAR 1 and all of them landing-owned: the mounts baseline (a fall, refreeze to bank it) · prose-numerics (path-and-line addressed, so ANY edit to a file carrying frozen rows moves it — 225 rows before, 225 after, net zero, all relocations) · the lighting census (a title count asserted EXACTLY; DESK CAR 1 had to FOLD a new test into an existing one rather than spend a title) · the observed-shape-readers register (DESK CAR 1 hit a genuine red and cured it at cause by renaming a return key that named an engine record it was not). $SP/receipt-deskwiring.md:271-289 and :330-343.']

--- DESK-1 [chair-class] landed=PARTIAL conf=CONFIRMED
    SIZE: 1-2 cars. Anchor = DESK CAR 1 (a59e66e5a): 6 files, +270/-48, delivering 4 blocks over 5 mount positions, of which a new 115-raw-line (50 effective) component leaf. Files: dossierMounts.js (rows + strike from UNMOUNTED_BLOCKS, same commit), economyStateProse.js (+1 pool-key resolver per block), 1-2 component leaves, tests/lint/dossierMountRegistry.walker.test.js, tests/lint/economyReadModelCoverage.walker.test.js.
    NAME: ECONOMY DESK REMAINDER — mount the 11 dark DS-ECO/DS-SUP blocks
    WHAT: The economy desk module already exists and lights 4 of its leaf's 15 blocks. Eleven more authored blocks (income mix, markets, supply chains, treasury, trade routes) sit dark: the reader gets tiles and numbers where the corpus already holds 78 pools / 249 authored variants of prose about the same facts.
    BLOCK: NOTHING external — this is the cheapest next car (the desk module, the router seam `drawnAtMount`, the position-leaf pattern and the classification precedents all exist). The one real constraint is SIZE: EconomicsTab.jsx is at 580/600 effective lines (eslint layer ceiling, no .size-baseline.json entry) so only ~20 effective lines of headroom remain in the host tab; the positions must land in EconomicsGlance.jsx (50/600, ~550 free) or in a second extracted leaf. Also owes the O2GATE public-dossier gate for any NEW draw site (see DESK-11).
    EV: MEASURED at the train tip. `git show 15c6368a6:src/domain/display/stateProse/dossierMounts.js` — DOSSIER_MOUNTS = 5 rows (lines 120-136), UNMOUNTED_BLOCKS = 64 entries (lines 152-176) of which DS-ECO-3,4,5,6,7,10,11,12 + DS-SUP-1,2,3 = 11 economy. Per-block pool/variant census (own probe over `git s
--- DESK-2 [chair-class] landed=NO conf=CONFIRMED
    SIZE: 3-5 cars. 23 blocks / 193 pools / 634 variants. A single desk module at economyStateProse's density (123 eff lines for 4 blocks / 27 pools) would PLAUSIBLY exceed the 800-effective-line src/domain ceiling and need splitting across two or three modules. Plus one position leaf per host tab.
    NAME: GENERAL DESK — the largest unwritten desk (23 blocks, 193 pools)
    WHAT: Writes the state-to-pool-key desk for the `general` corpus leaf (DS-POP, DS-GEN, DS-REL, DS-HK — population, settlement character, relationships, housekeeping) and mounts its 23 blocks. It is a third of the whole dark half and the only leaf carrying dimension-partitioned blocks.
    BLOCK: The desk module does not exist. Three of its blocks are DIMENSION-BEARING and the walker's honesty arm refuses a mount over one that declares no dimension, while the kernel fails CLOSED (renders nothing) on an unanswered dimension: DS-GEN-1 (severity), DS-GEN-6 (deficit), DS-GEN-9 (anchor). So this desk must both DECLARE the dimension on the mount row and ANSWER it from live state, which the economy desk never had to do — there is no worked precedent in the tree. DS-GEN-6 additionally has the registry's one written exception (a tier overlay composed after the route line). Host tabs must be chosen (Overview 322 eff, History 287, Relationships ~251, DailyLife ~289 — all with real headroom under 600).
    EV: Block/pool/variant counts: own probe, $SC/pending/DESK/CENSUS.txt (`general 23 193 634`). Dimension census: own probe over the leaf's `"marks"` arrays against `STATE_MARK_DIMENSIONS` (`git show 15c6368a6:src/domain/display/stateProse/stateProseKernel.js` lines 192-196: severity/deficit/anchor) — exa
--- DESK-3 [chair-class] landed=NO conf=CONFIRMED
    SIZE: 1-2 cars. 11 blocks / 126 pools / 383 variants. New files: 1 desk module under src/domain/display/stateProse/ (PLAUSIBLY 250-400 effective lines by scaling economyStateProse's 123 eff / 4 blocks / 27 pools; 800 ceiling), 1 position leaf under src/components/new/tabs/ (~115 raw at DESK CAR 1's rate). Modified: dossierMounts.js, DefenseTab.jsx, the mount walker.
    NAME: DEFENSE DESK — write it and mount its 11 blocks
    WHAT: Writes the desk for the `defense` corpus leaf (DS-DEF-1..11 — walls, garrison, readiness, threat posture) and mounts its 11 blocks on DefenseTab, so a settlement's defensive standing reads as sentences rather than only as band words and counts.
    BLOCK: The desk module does not exist — the receipt PROVED the darkness by probing eight distinctive pool keys across src/: `readiness STRONG` returns ZERO src files. Nothing else blocks it; DefenseTab has 286 effective lines of headroom under the 600 ceiling.
    EV: Census: $SC/pending/DESK/CENSUS.txt (`defense 11 126 383`). Darkness proof: $SP/receipt-deskwiring.md:107-113 (the eight-pool-key probe). DefenseTab 314 effective lines: $SP/receipt-deskwiring.md:74 (eslint Linter), unchanged through 450f7dbb7. Dark ids DS-DEF-1..11: dossierMounts.js lines 153-155 a
--- DESK-4 [chair-class] landed=NO conf=CONFIRMED
    SIZE: 1-2 cars. 9 blocks / 138 pools / 418 variants. WarTab has 233 effective lines of headroom, FaithTab 453 — both comfortable, so this may need no extraction leaf at all (unlike DESK CAR 1, whose extraction was forced by EconomicsTab sitting at 600/600).
    NAME: WAR + FAITH DESK — write it and mount its 9 blocks across two tabs
    WHAT: Writes the desk for the `warFaith` leaf (DS-WAR-1..5, DS-FTH-1..4 — war footing, sieges, treaties, creed standing) and mounts its 9 blocks. It is the only leaf whose blocks split across two host tabs, so it is also the first real test of the C3 one-fact-one-sentence law across a page-set boundary.
    BLOCK: The desk module does not exist (`PATRON: rankAxis` and `WALLED-QUIET` return zero src files). ALSO: the review corpus is DEITY-FREE — 0 of 44 settlements carry a patron — so a faith mount cannot be walked or judged until the roster widens, which the queue records as the OWNER's act. Build it anyway; the walk-readiness is the owner's blocker, not the build's.
    EV: Census: $SC/pending/DESK/CENSUS.txt (`warFaith 9 138 418`). Darkness proof: $SP/receipt-deskwiring.md:110-112. Tab sizes 367 / 147 effective: $SP/receipt-deskwiring.md:77-78. The deity-free corpus: $SP/LANE-QUEUE.md:35-36 (OWNER'S DESK item 5). Dark ids: dossierMounts.js lines 173-176 at 15c6368a6.
--- DESK-5 [chair-class] landed=NO conf=CONFIRMED
    SIZE: 1 car. 7 blocks / 79 pools / 256 variants — the closest in scale to DESK CAR 1 (4 blocks / 27 pools) and therefore the best second car to dispatch after the economy remainder. New: 1 desk module + 1 position leaf (or none, given the headroom). Modified: dossierMounts.js, PowerTab.jsx, the mount walker.
    NAME: POWER DESK — write it and mount its 7 blocks
    WHAT: Writes the desk for the `power` leaf (DS-POW-1..7 — who rules, how contested, how legitimate) and mounts its 7 blocks on PowerTab, so the power structure reads as prose about its own state.
    BLOCK: The desk module does not exist. Nothing else — PowerTab has 300 effective lines of headroom, the most of any candidate host tab after Faith.
    EV: Census: $SC/pending/DESK/CENSUS.txt (`power 7 79 256`). PowerTab 300 effective: $SP/receipt-deskwiring.md:76. Dark ids DS-POW-1..7: dossierMounts.js lines 169-171 at 15c6368a6. Note a live position leaf already exists in that tab's dir (`src/components/new/tabs/power/PowerStrata.jsx`), so the extrac
--- DESK-6 [chair-class] landed=NO conf=CONFIRMED
    SIZE: 1 car. 3 blocks / 67 pools / 246 variants. The pool-key resolvers are the cost here, not the block count: 22 pools per block against economy's ~7.
    NAME: STRESSORS DESK — write it and mount its 3 blocks
    WHAT: Writes the desk for the `stressors` leaf (DS-STR-1, DS-STR-2, DS-CND-1 — what is going wrong and how bad) and mounts its 3 blocks. Smallest by block count but NOT by content: it is the densest leaf in the corpus at 22 pools per block.
    BLOCK: The desk module does not exist, and the HOST TAB IS UNDECIDED — stressors have no dedicated tab in the router's switch. A position must be chosen (Overview, History, or the Viability tab), and the mount id is spelled `<tab>.<position>` where the tab must be a case label of OutputContainer's own renderTab, machine-checked by the walker's `routerTabs()`. That choice is the car's first judgment call.
    EV: Census: $SC/pending/DESK/CENSUS.txt (`stressors 3 67 246`). The tab vocabulary is derived from the router: `git show 15c6368a6:tests/lint/dossierMountRegistry.walker.test.js` lines 129-137 (`routerTabs`) and 371 ('the tab vocabulary is derived from the router that uses it'). Dark ids: dossierMounts.
--- DESK-7 [chair-class] landed=NO conf=CONFIRMED
    SIZE: Small — 1 car, ~2 files. Eleven authored phrases plus a lookup table in economyStateProse.js (`SLOT_FILL_SHAPES`/`bareCommonFill` seam) and the annex row at docs/content/RECEIPT_POOLS_DOSSIER_STATE.md §0c-3 closed. Article stored per value (they are transcriptions into a second register, not new concepts).
    NAME: {complexity}'s eleven in-sentence forms — the content car that unblocks DS-ECO-1's C1 pool
    WHAT: Authors a closed vocabulary of bare common-noun phrases and a total map from `deriveEconomicComplexity`'s eleven title-cased display strings onto it, so the `{complexity}` slot can be filled. Until it exists, every variant naming that slot is ineligible under anchored liveness and the DS-ECO-1 pools collapse — C1 to ONE variant of three, which means every high-rung, working-approach settlement in a world prints the SAME sentence. That is the exact outcome the corpus was authored to prevent.
    BLOCK: Nothing technical — it is words, and the words are the chair's to read and veto. Two mechanical shapes were tried and REFUSED on evidence: lower-casing yields 'a agricultural surplus with trade links'; splitting at the em dash yields adjectives no seam can take. This was RATIFIED as a free deferral at §892 and then RE-RULED a BLOCKER in the same act, because DESK CAR 1 wired the desk: it stops being free the day the §891 train lands.
    EV: Annex: `git show 15c6368a6:docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` line 179 (the slot row) and lines 219-236 (§0c-3, 'OPEN, THE CHAIR'S', with the per-pool arithmetic C1 1/3 · C2 2/3 · C3 2/3 · C4 3/3 · C5 2/3). Producer: `git show 15c6368a6:src/generators/economy/prosperity.js` lines 292-314 —
--- DESK-8 [machinery-debt] landed=NO conf=CONFIRMED
    SIZE: Trivial — one file, one number, inside the landing's register batch: `UPDATE_MOUNT_BASELINE=1 npx vitest run tests/lint/dossierMountRegistry.walker.test.js`.
    NAME: Refreeze the dossier-mounts darkness baseline 68 → 64 at the §891 landing
    WHAT: Banks the win DESK CAR 1 already paid for. The registry's shrink-only baseline still records 68 dark blocks while the tree has 64, so the ratchet is currently slack by four: a later car could re-park four blocks and no gate would notice.
    BLOCK: The §891 landing — refreezes belong to the landing, not the lane. The walker's `<=` arm is already GREEN at 64 against a committed 68, so nothing reds; this is a bank, not a repair. The guarded writer (`writeShrinkOnlyBaseline`) throws on a raise, so running it is safe.
    EV: `git show 15c6368a6:tests/lint/.dossier-mounts-baseline.json` and `git show 450f7dbb7:...` both = {"UNMOUNTED_BLOCKS":{"blocks":68}} while UNMOUNTED_BLOCKS.length = 64 (counted at both shas). The arm and the refreeze door: tests/lint/dossierMountRegistry.walker.test.js lines 548-566. Predicted by th
--- DESK-9 [machinery-debt] landed=NO conf=CONFIRMED
    SIZE: Small — 1 car. An arm in tests/lint/dossierMountRegistry.walker.test.js (or a sibling) asserting that every component site naming a mount id also reads a public/audience condition, with a planted mutant proving it convicts. Do NOT add a new test FILE (a new file reds three censuses); extend an existing one.
    NAME: A walker arm that makes the public-dossier gate a DUTY of every desk car, not a habit
    WHAT: O2GATE cured the paid-surface leak on EconomicsTab by threading `publicDossier` into one tab. Nothing in the tree stops the NEXT desk car from drawing a corpus sentence on the anonymous gallery dossier the same way — the leak was found by a chair reading, not by an instrument, and five more desk cars are queued behind it.
    BLOCK: Nothing. It is buildable today and is best built BEFORE DESK-2..DESK-6 rather than after, since each of those cars is a fresh chance to repeat the omission. Note the size pinch it must respect: OutputContainer.jsx sits at EXACTLY 600/600 effective lines with zero headroom, so threading a public flag into a SECOND tab is a line-neutral edit or a decomposition.
    EV: The gate exists at exactly one site: `git show --stat 450f7dbb7` = 3 files (OutputContainer.jsx 2 lines, EconomicsTab.jsx 14, tests/ui/economicsTabFlow.test.js 90) — no lint/walker arm. The original omission and its shape: $SC/briefs/brief-O2GATE.md 'THE FINDING' section, and 450f7dbb7's own commit 
--- DESK-10 [chair-class] landed=NO conf=CONFIRMED
    SIZE: Small — folds naturally into whichever desk car lands the chosen host tab's positions (Overview, i.e. part of DESK-2's general work). One mount row + one draw site + one walker-visible literal.
    NAME: DS-ECO-8 owes a SENTENCE position on a page that is not the economics tab
    WHAT: DS-ECO-8 (the prosperity rung read on its own) currently GLANCES on its tile because DS-ECO-1 already speaks about prosperity in the header a few inches above, and R-DST-A forbids a page saying two things about one fact. Its speaking position is genuinely owed elsewhere — the Overview tab or the PDF chapter opener — and the block's 21 authored variants are otherwise mute.
    BLOCK: A page decision (which non-economics surface hosts it), then a mount row plus a real draw site there. Deliberately deferred and written down by DESK CAR 1 — not a bug to re-find.
    EV: $SP/receipt-deskwiring.md:120-127 (JUDGMENT 1, the DS-ECO-8 reasoning) and :319-321 ('Also deferred and written down: DS-ECO-8 owes a SENTENCE position on a page that is not the economics tab'). The row itself and its reasoning are in the shipped table's docblock: `git show 15c6368a6:src/domain/disp
--- DESK-11 [machinery-debt] landed=NO conf=CONFIRMED
    SIZE: Trivial — one docblock in one file, ride it on the next desk car.
    NAME: EconomicsGlance.jsx's docblock is stale on the count AND the rung it ships
    WHAT: The file's own 'WHAT IS DELIBERATELY NOT DRAWN HERE' section says 'All three positions in this file are GLANCE rungs, so none of them prints a corpus sentence today.' The file carries FOUR positions and one of them — economics.prosperityHeader, DS-ECO-1 — is a SENTENCE rung that the file renders at `{header?.sentence && ...}`. A reader auditing the paid-surface boundary from this docblock would conclude the file cannot speak, which is exactly the wrong conclusion and is how the O2GATE leak was nearly mis-scoped to the food line alone.
    BLOCK: Nothing. O2GATE identified it in its own commit body and did not fix it, because both of its source edits had to be physical-line-neutral (OutputContainer at 600/600, prose-numerics rows frozen by exact line). EconomicsGlance has 550 effective lines of headroom, so this cure is free there.
    EV: `git show 450f7dbb7:src/components/new/tabs/EconomicsGlance.jsx` line 2 ('the first four positions') vs line 32 ('All three positions in this file are GLANCE rungs'); the sentence draw is in the same file's header block (`{header?.sentence && <p ...>}`). Confirmed unchanged by O2GATE: `git diff --st
--- DESK-12 [machinery-debt] landed=PARTIAL conf=CONFIRMED
    SIZE: Trivial to record; recurring ~1 register act per desk car thereafter.
    NAME: Every desk car darkens the writer-reach register's web-display grades — absorb, never 'cure'
    WHAT: The writer-reach scanner resolves a component PARAMETER from its CALL SITES, and a JSX element is not a CallExpression, so a read moved behind a JSX prop becomes ungrounded and grades N. DESK CAR 1's extraction of the prosperity header into EconomicsGlance did exactly that to `situationDesc` — a FALSE DARK for an identity the product still renders. Five more desk cars will each do it again. Without a standing rule the next seat will 'fix' the component to satisfy the scanner, which is a real product change made to appease a false reading.
    BLOCK: Nothing to build; it is a rule to write down where a lane will find it (the desk-car brief template, or the register's own docblock) plus the per-car register act: absorb via plain `--write`, predicting BOTH moved entries in advance.
    EV: $SC/RESUME-NOTE.md:33 (the mechanism, CONFIRMED by reading scripts/lib/legacy-reader-shape-scan.mjs makeResolver plus a probe), :10 (the DESK CAR 1 instance), :79 and :142 (LIVE-FAULT #7 — 'every desk extraction (a read moved behind a JSX prop) darkens it; absorb with plain --write, predict BOTH ent
