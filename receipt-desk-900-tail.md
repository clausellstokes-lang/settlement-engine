# RECEIPT — lane DESK-900-TAIL
## ✅ COMPLETE — four cars landed and proved; the owed whole-`tests/lint/` directory run is executed and every arm attributed.
⟦Seat: Opus 5 — Fable-unvalidated · Chair: Fable 5.1 · Lane: DESK-900-TAIL · 2026-09-05⟧
⟦Dock `$SC/laneTAIL`, **verified on arrival**: HEAD `25a7fd0bf3f18d9ab53677aa8acbf43ef0e361ec`, porcelain **0**, detached, `git rev-list --count 38474a59e..HEAD` = **84** (the brief's figure, CONFIRMED), `node_modules` = **453 package symlinks** (correct form; no `npm install`, no materialisation, no rebase, no stash, no `checkout --`, no register door, no `git show HEAD:<path> >`)⟧

## OUTCOME
| item | outcome | sha |
|---|---|---|
| 1 · `voiceMechanics` Tier-2 debt | **PARTLY CURED** — 2 of 4 files GREEN at cause; 2 handed up with the blocking arithmetic; all three ruled mechanisms REFUTED | `38ef6e42a` |
| 1b · `voiceMechanics` JSX arm (chair's addition) | **CURED** — the one GROWTH gone; JSX totals em 1 → 0; the two remaining rows are pure falls | `38ef6e42a` |
| 2 · `treatyOrientationWr10g` | **CURED at cause; the ruled DISPLAY SHIFT DOES NOT EXIST** (measured differentially) | `fce136262` |
| 3 · `dossierMountRegistry` ARM 1 (chair's addition) | **CURED — and a SECOND leak the composition could not see** | `930fe8d14` + `7e1273490` |
| 3 · `dossierMountRegistry` ARM 3 | **CURED at cause** — one offender, a gate that was correct and invisible | `7e1273490` |
| 3 · the silent-dark proof | **BUILT** — `tests/ui/warFaithDeskFlow.test.js`, 7 arms, negative control executed | `930fe8d14` |

**Both walker arms are GREEN.** `dossierMountRegistry.walker.test.js` does not appear in the
final failing-file list at all (it carried 2 failed arms in the chair's own 78-car
whole-suite log).

---

## ⛔ FIVE RULING / BRIEF ERRORS, each refuted by measurement

| the chair (or REDS) ruled | measured at this tip | verdict |
|---|---|---|
| 1(a) spell the delimiter so the scanner does not read it as prose | Tier 2 has been an **espree AST walk** since lane PARSER-3 (2026-09-03) and pushes `node.value` / `quasis[].value.cooked`. An escaped em dash counts **exactly the same** and the two runtime strings are `===`. Tier 3 (`jsxLiteralWalk.js`) pushes `n.value.cooked` too. The only spelling that dodges is `String.fromCharCode(0x2014)`, which the walker's own budget comment refuses as ratchet-gaming. | ⛔ **REFUTED** |
| 1(b) key the maps off the producer's constants in `src/generators/economy/prosperity.js` | **THREE files forbid it in writing.** `labelBands.js:50` — "⛔ ZERO IMPORTS, and keep it that way — this leaf is reachable from the eager first-paint graph". `economyStateProse.js` — "the desk does NOT import the generator: a display leaf that reached into src/generators would drag the economy generator into every tab chunk that draws". `prosperity.js`'s own header records the estate ALREADY re-homing `priorityToCategory` **out of** that generator **into** a dependency-free domain leaf "so presentation can read the exact same bands without importing the economy generator and its transitive graph". A vocabulary moves TOWARDS domain. | ⛔ **REFUTED** |
| 2 route through `treatyOrientationOf`; it MOVES same-seed text ⇒ a declared display shift | `peaceTermsDocument.js:216-218` — the **only** producer of that `doc` — already routes through `treatyOrientationOf` and writes `victorId = orientation.receiverId`, `loserId = orientation.giverId`. Its typedef says the two fields carry "the buyer and the seller on a WR-10 sale" and `treatyOrientationWr10g.test.js:595` pins it. | ⛔ **REFUTED — no shift exists** |
| REDS #1: a `victorId`-only document resolves to `unknown`, so rendered text moves | the producer writes **both slots or neither**. The victorId-only shape at `warFaithStateProseDesk.test.js:269` is a **hand-built fixture the producer cannot emit** (the recorded "a fixture can be the only writer of the shape" hazard). | ⛔ **REFUTED** |
| 3 ARM 1 is the `history` tab | ARM 1 stops at its FIRST offender. With history threaded the loop reaches **`plot_hooks`** — a SECOND live leak of the identical shape, invisible in `whole-900b.log` because the first red hid it. | ⭐ **WIDER THAN RULED** |

---

## ITEM 1 — the voice rows

### Tier 2 (`src/data` + `src/domain` string literals, espree)
| file | frozen | at the composition | after car 1 | state |
|---|---|---|---|---|
| `src/domain/certification/subsystemRowsSeat.js` | em 9 · bang 0 | em 15 · bang 1 | **em 9 · bang 0** | ✅ GREEN, exactly the baseline |
| `src/domain/display/stateProse/economyStateProse.js` | absent (0/0) | em 5 | **em 0** | ✅ GREEN |
| `src/domain/display/labelBands.js` | absent (0/0) | em 5 | em 5 — unchanged, **no rise** | ⛔ HANDED UP |
| `src/domain/display/stateProse/generalStateProse.js` | absent (0/0) | em 3 | em 3 — **untouched** | ⛔ HANDED UP |
| **TOTALS** | em 311 · bang 8 | em 330 · bang 9 | **em 319 · bang 8** | budgets 670 / 15 |

Final arm text, quoted: `labelBands.js: baseline em:0 bang:0 → current em:5 bang:0` and
`generalStateProse.js: baseline em:0 bang:0 → current em:3 bang:0` — exactly the two rows
handed up, and nothing else.

### Tier 3 (`src/**/*.jsx`) — the chair's addition
| file | frozen | at the composition | after car 1 |
|---|---|---|---|
| `SummaryTab.jsx` | em 2 | em 0 (FELL) | em 0 — a win to bank |
| `tabs/OverviewTab.jsx` | em 1 | em 0 (FELL) | em 0 — a win to bank |
| `tabs/WarFaithDesk.jsx` | absent | **em 1 (GREW)** | **em 0** ✅ |
| **TOTALS** | em 3 | em 1 | **em 0** (budget 6) |

The growth was `WarFaithDesk.jsx:233`, a template segment `" — "` between a glance band word
and a sentence. **Dead at that rung** — `drawnAtMount` strips the sentence for a `glance`
row, so the `&&` branch has never rendered — and the line above already uses the file's own
` · `. Rewritten to §6's label separator (a colon). No shipped byte moves.

### The six cured tells in `subsystemRowsSeat.js` (all SEAT-78's D10 rows, all line-internal)
| line | tell | §6 replacement |
|---|---|---|
| 175 | `world — not after … to be 1 — and` | parentheses (appositive) |
| 180 | `competing with it — a lopsided contest…` | period (two complete ideas) |
| 181 | `STRICTLY LOWER — a differential…` | colon (clarification) |
| 185 | `at 0.3 weight inverted — the fight…` | colon |
| 186 | `would be vacuous — the recorded hazard.` | colon |
| 165 | **the bang: `a !== true early return`** | "an early return on the NEGATED strict comparison" |
File line count unmoved at **197**, so no line-addressed register row relocates. NO component
under `src/components` consumes these rows (measured: zero grep hits) and `VOICE_AND_TONE.md:89`
puts developer-only text out of the reader's rule — the ratchet counts it anyway.

### ⛔ THE BANK IS BLOCKED — the arithmetic the chair needs
`writeShrinkOnlyBaseline` **throws** when a metric TOTAL would rise. Frozen em **311**; the
tree now measures **319**. Banking the surviving 8 em (labelBands 5 + generalStateProse 3)
would take the committed total to 319 ⇒ **the documented refreeze REFUSES until 8 more em are
burned from the 311 already banked.** The bang total is now EQUAL (8 = 8) and no longer
blocks. **The JSX refreeze is legal now** (3 → 0, a fall).

### ⛔ A CURE I REFUSED, killed by measurement
Routing `generalStateProse.js`'s two `split('—')[0]` sites through `labelBands.safetyBandOf`
is the estate's own idiom for exactly this defect and is **NOT equivalent**: `SAFETY_POOL_OF`
carries the key `Secure`, which is **not a member of `SAFETY_BANDS`** — provably different
closed sets — so the routing returns `null` where the split returns a head word and the
`{Secure, Controlled, Quarantined}` pool goes dark. Left untouched rather than half-cured.
⭐ Separately: `SAFETY_POOL_OF.Secure` is a **read of a key no writer produces** —
`safetyProfile.js` emits no label opening with `Secure`. DECLARED, not cured here.

---

## ITEM 2 — the war pair

### What the red actually is (measured line by line)
The census convicts on `readsLedger && spellsRawPair`. On this file:
- `readsLedger` fires on `war.treaties` (:337) and `war?.treaties` (:380) — an array of
  already-built `TreatyDocument`s. `/\.treaties\b/` cannot tell that from a ledger read.
- `spellsRawPair` fires on `doc.victorId` / `doc.loserId` (:424-425) — the read-model's
  **resolved** slots, read back exactly as `concludedWars.js` reads back its own artifact,
  which is the written reason `concludedWars.js` is on the allowlist.

### The cure — routing, not widening
`treatyOrientation.js` gains `documentSideOf(doc, partyId) → 'receiver' | 'giver' | null`:
the same lookup at the address that owns the vocabulary ("ONE reader, many consumers"). The
census allowlist is **UNTOUCHED** (DESKCURE-2: "cured by typing and by routing, never by
widening"). Offenders after: `['worldPulse/concludedWars.js', 'worldPulse/peaceTerms.js']` —
exactly `WAR_PAIR_WRITERS`.

### Byte-identity, differentially measured over 5,832 shapes
| comparison | divergences | what they are |
|---|---|---|
| old vs **this car** | 402 | 306 where `settlementId` is not a string; **96 where it is the literal `"undefined"`** — the CR-WR10-G defect itself (`String(doc.victorId)` on an absent slot names a town called `undefined` the victor). The cure REMOVES a false positive and creates none. |
| old vs **the chair's ruled mechanism** | **630** | every extra one is a document LOSING its side |
No producible document reaches a divergent branch. ⇒ **no world's rendered text moves; NO
entry is owed to `docs/GOLDEN_SHIFT_LEDGER.md`**, and the desk suite's expectations
(`warFaithStateProseDesk.test.js:268-270`, `:517`, `:519`) are correct as written and were
NOT touched. `tests/domain/warFaithStateProseDesk.test.js` and
`tests/domain/treatyOrientationWr10g.test.js` both **GREEN**.

### ⛔ The defect that survives — handed up
The two pool keys still read "the town is the VICTOR side" / "the LOSER side", and the
producer fills the receiver slot with the **BUYER**. A town that bought a holding is told it
is the victor; the seller is told it is the loser. DS-WAR-2 has no seller/buyer pool ⇒ the
cure is a **corpus authoring act**; gating on `orientationKind` would silence a sale's
document entirely (a reader losing a sentence rather than gaining a true one). Named at cause
in the desk's docblock.

---

## ITEM 3 — the gate

### ⛔ TWO LIVE PAID-SURFACE LEAKS (ARM 1), not one
| tab | what it did | why it leaked |
|---|---|---|
| `history` (car 3) | `case 'history'` passed no `publicDossier`/`playerView` | `HistoryTab` declares `publicDossier = false`, and **false is the SPEAKING path** (`generalDeskLines` reads `=== true`) ⇒ GEN2's history chapter rendered corpus prose to a free anonymous gallery viewer |
| `plot_hooks` (car 4) | `case 'plot_hooks'` passed nothing at all | `PlotHooksTab` declares `publicDossier = false` — identical shape; DS-HK-1's hook framing leaked the same way |
⚠ **DECLARED DISPLAY CHANGE, one direction only:** on the PUBLIC gallery dossier the history
chapter's and the plot-hooks framing's corpus sentences stop rendering. Nothing moves for an
owning DM. This is a leak closing to the already-ruled §885.3 policy, not a policy choice.
⭐ The tree already knew: `tests/ui/generalDeskTabFlow.test.js:300` and `:392` have been red
on exactly these two. Both are GREEN now.

### ⛔ ARM 3 — a gate that was correct and INVISIBLE
Single offender, quoted from the arm: *"`src/components/new/economyDeskRead.js`:
`economyStateProse()` reads the corpus in a statement that never mentions publicDossier"*.
The gate was a GUARD CLAUSE one statement above the read
(`if (options.publicDossier || !settlement) return SILENT_ECONOMY_DESK;`). It worked;
nothing could see it working — and a gate no instrument can see is the one the next edit
deletes silently, which is how the two hand-fitted gates before it were lost. Restructured to
the `warFaithDeskRungs` idiom: `return options.publicDossier ? SILENT_ECONOMY_DESK :
economyStateProse(…)`. Same truth table, one statement, visible.
`tests/ui/economicsTabFlow.test.js` **GREEN**.

### SIZE, measured before and after (eslint `Linter`, skipBlank + skipComments)
`src/components/OutputContainer.jsx` = **600 / 600 effective lines BEFORE and AFTER** both
router edits (physical 1,049 → 1,049). Every edit is character-level on an existing case line.

### THE SILENT-DARK PROOF — `tests/ui/warFaithDeskFlow.test.js` (NEW FILE, 7 arms)
DESK-4 cited `tests/ui/warTabFlow.test.js` and `faithTabFlow.test.js`; DESK-900-REDS measured
that NEITHER HAS EVER EXISTED and withdrew the claim (car 4), leaving the gate's closed half
provably unproved. This is that proof: WAR and FAITH positions rendered twice on the same
settlement through `expectPresentThenAbsent`; SILENT-DARK asserted on the **container**
(`''`, never a hollow `<div data-testid>`) with a non-vacuity arm; the SILENT record's key set
proved equal to the open one (**23 = 23**, measured); both host tabs and the router pinned.

**⭐ NEGATIVE CONTROL EXECUTED.** The gate planted out (`return publicDossier ?` →
`return false ?`): `PLANT_EXIT=1`, **4 of 7 arms RED** — the four DOM/gate arms; the three
source-pin arms correctly stayed green. Restored by **inverse edit** and verified
`cmp` byte-identical against a backup taken BEFORE the plant
(`$SC/desk900tail/WarFaithDesk.PREPLANT.jsx`). Porcelain 0 after.

---

## EXECUTED PROOFS — every exit captured in-shell, every vitest run through `scripts/gate-mutex.sh`
| run | scope | exit | result |
|---|---|---|---|
| `run1.log` | walker + 4 desk/UI/copy files, after cars 1-3 | `RUN1_EXIT=1` | 6 failed / 109 passed. ARM 1 had moved from `history` to `plot_hooks`; ARM 3 named one offender |
| ⛔ **owed whole-`tests/lint/` run** (+ 8 named files), after car 4 | **148 files** | **`RUN2_EXIT=1`** | **8 failed / 140 passed; 24 red arms — all attributed below** |
| `plant.log` | the new UI file, gate planted out | **`PLANT_EXIT=1`** | 4 failed / 3 passed — the negative control |
| `typecheck.log` | **`npm run typecheck:domain:strict`** (the REAL script) | **`TYPECHECK_EXIT=0`** | "no strict-type regressions (1120 errors, ceiling 1120)" — ceiling unmoved |
| eslint | every touched file, each car | **0** | `subsystemRowsSeat.js`, `labelBands.js`, `economyStateProse.js`, `WarFaithDesk.jsx`, `treatyOrientation.js`, `warFaithStateProse.js`, `OutputContainer.jsx`, `economyDeskRead.js`, `warFaithDeskFlow.test.js` |

### THE OWED DIRECTORY RUN — every failing arm attributed
`RUN2_EXIT=1` · 8 failed | 140 passed (148) · 24 failed | 2291 passed (2315).

| file | arms | attribution |
|---|---|---|
| `writerReach.walker.test.js` | 13 | **register-class — the chair's door** (13 at the composition too) |
| `observedShapeReaders.walker.test.js` | 2 | **register-class — the chair's door**; the chair's own 4→5 pin car is on ITS dock, not replayed here |
| `proseNumerics.test.js` | 2 | **register-class — the chair's door** |
| `significanceMigration.census.test.js` | 2 | **PRE-EXISTING** — 2 failed in the chair's own `whole-900b.log:65` |
| `sovereigntyLightingContract.walker.test.js` | 1 | **register-class — THE LIGHTING CENSUS, and this lane owes exactly +1** (below) |
| `tuningRegister.walker.test.js` | 1 | **register-class — the chair's door** (2 at the composition; the chair's IRREGULAR_TUNING draft-row car closed one) |
| `voiceMechanics.test.js` | 2 | **this lane's, HANDED UP** — the two rows above, and the two JSX falls to bank |
| `generalDeskTabFlow.test.js` | 1 | **PRE-EXISTING, NOT MINE** — the DS-GEN-11 viability LIVENESS anchor, red in the chair's `whole-900b.log:3856` at the 78-car state with the identical message. Its other two arms (history, plot_hooks) were red there and are **GREEN here.** |
| `dossierMountRegistry.walker.test.js` | **0** | **CURED** (2 arms red in `whole-900b.log:10`) |
| `couplingInclusion.walker.test.js` | **0** | DESK-900-CARS' cars, now in the composition |

---

## REGISTER LAW — no act of any kind taken; every delta PREDICTED IN WRITING then measured
No `--update` / `--write` / `--genesis` / `--rebank`, no `UPDATE_*` / `*_REFREEZE`. No
register-class red cured.

| register | predicted | measured | note |
|---|---|---|---|
| **lighting census** (`sovereigntyLightingContract`) | **+1**, my one new test file | `expected 2536 to be 2523` here against `expected 2535 to be 2523` in the chair's 78-car log ⇒ **exactly +1** | ⛔ the chair re-takes it. The ONE new test file is **`tests/ui/warFaithDeskFlow.test.js`** |
| `tests/copy/.voice-mechanics-baseline.json` | 2 rows to lower/strike, 2 to bank | as predicted | ⛔ **the bank is BLOCKED**: totals would rise 311 → 319 |
| `tests/copy/.voice-mechanics-jsx-baseline.json` | 2 rows to strike | as predicted | **legal now** (3 → 0, a fall) |
| `scripts/.observed-shape-readers-baseline.json` | manifest rows drift for 3 touched files | **ALREADY STALE at the composition** — frozen sizes 40139 / 33306 / 51740 against live 50812 / 57038 / 112624 | no new drift class |
| `tests/lint/.prose-numerics-baseline.json` | no row for any file I touch | confirmed: 0 matches for all six | unmoved by this lane |
| test-ratchet collapse floors | raised, never breached, by one new test file | consistent with WARMS-CAR0's 2026-09-05 correction | chair's act |
| `npm run typecheck:domain:strict` | ceiling unmoved | **1120 / 1120, exit 0** | — |

---

## RETROVALIDATION ROW (for the Fable chair)

**WHAT WAS JUDGED — six calls this Opus seat made that a Fable chair must re-derive:**

1. ⭐ **HIGHEST — I declared the item-2 DISPLAY SHIFT NON-EXISTENT and wrote no ledger entry.**
   Re-derive: does `peaceTermsDocument.js:216-218` really fill `victorId`/`loserId` from
   `treatyOrientationOf(treaty).receiverId`/`.giverId`, so that a SALE's document already
   names both sides and a victorId-only document is unproducible? **If that premise fails, a
   shift entry IS owed and my refusal was wrong.** (`$SC/desk900tail/item2diff.mjs`,
   `item2class.mjs`.)
2. ⭐ **The two ROUTER cures DO move shipped output on the public gallery dossier** — the
   history chapter's three corpus sentences and the plot-hooks framing sentence stop
   rendering there. I treated that as a LEAK CLOSING under an already-ruled policy (§885.3)
   rather than a paid-surface behaviour change needing an owner signature. **A chair who
   reads it the other way should hold both cars for the owner.**
3. **I refused all three of item 1's ruled mechanisms and cured only 2 of 4 files.**
   Re-derive the espree cooked-value measurement and the three in-tree import prohibitions.
   If either falls, the remaining 8 em are curable and the block on the refreeze lifts.
4. **`documentSideOf` was placed in `treatyOrientation.js` rather than widening the census's
   `WAR_PAIR_WRITERS` allowlist.** The alternative is a one-line allowlist row with a written
   reason. I judged routing correct on DESKCURE-2's rule; a chair who prefers the allowlist
   reverts one file and adds one line.
5. **`labelBands.js` gained an exported `COMPLEXITY_LABEL` record** so `economyStateProse`
   stops transcribing the same eleven authored strings. That edits the chair's own band-word
   file (its em count is UNCHANGED at 5). If the chair wants that file frozen, the
   de-duplication moves to `economyStateProse` instead and its 5 em come back.
6. **A NEW TEST FILE was added** (`tests/ui/warFaithDeskFlow.test.js`). Judged the honest home
   under the desk law's own allowance; it costs exactly +1 on the lighting census.

**WHAT THE CHAIR MUST RE-DERIVE, BY PATH** (all under `$SC/desk900tail/`):
- `run1.log` (`RUN1_EXIT=1`, 6 arms) · **`run2.log` — the owed directory run** (`RUN2_EXIT=1`,
  24 arms, all attributed) · `plant.log` (`PLANT_EXIT=1`, the negative control) ·
  `typecheck.log` (`TYPECHECK_EXIT=0`, 1120/1120)
- `voicecount.mjs` / `voiceshow.mjs` / `jsxcount.mjs` — the Tier-2 and Tier-3 counters,
  replicating the walker's own logic verbatim
- `census.mjs` — the CR-WR10-G consumer census, replicated statically
- `item2diff.mjs` / `item2class.mjs` — the 5,832-shape differential and its producibility classifier
- `*.BEFORE.js` / `WarFaithDesk.PREPLANT.jsx` — the pre-edit and pre-plant backups
- the four commit messages, each carrying its own measurement

**PRIORITY:** (1) the item-2 no-shift finding — it overturns a ruling and cancels a ledger
entry · (2) the TWO paid-surface leaks, which were shipping · (3) the item-1 refutations and
the **8-em block on the voice refreeze** · (4) the surviving sale-names-a-victor corpus defect
· (5) `SAFETY_POOL_OF.Secure`, a pool key no writer produces · (6) the lower-stakes calls above.

---

## DOCK TIP
`laneTAIL` detached at **`7e1273490d2d2238e2f14d092c50a397ff9a55e8`** (4 cars above
`25a7fd0bf`), porcelain **0**.
Replay `laneTAIL:25a7fd0bf..7e1273490d2d2238e2f14d092c50a397ff9a55e8` onto the §900 dock.
