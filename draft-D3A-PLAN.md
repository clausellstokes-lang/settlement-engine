# D3a — THE PORT WAVE · compiled plan (DRAFT for the FABLE chair)

**Lane:** TC-D3A, OPUS COMPILE seat (ODQ §291.5). Compiled read-only. No git write, no gate run,
no repo edit. Every figure traces to `laneTCD3A-receipt.md` §1, which names the command.

**Charter:** ODQ §310.4 dispatches this compile under §304.4's charter, carrying §303.5's
adopt/replace split, §299.3's three obligations, §306.4a's dossier-surface batch, §287.16's
dormant-explicit-input massing scope and §290.1's core-first slice.

**Bases:** BUILD `claude/composite-r4` @ `9d851fae` · SANDBOX code of record
`…/a244e7a3-…/scratchpad/laneMFW3F-tip` (the sealed W3 tip) · LEDGER worktree `c5ba15e4`.

---

## §0 · THE PLAN IN ONE PAGE

D3a is the port's **opening tranche**, not the whole port. The sandbox fabric measures
**14,230 effective production lines across 54 modules** (executed, receipt §1 / `laneTCD3A-measure-files.mjs`).
Against `PACKET_STANDARD`'s cap of 400 new-or-changed effective production lines per packet, a
complete port is **≥ 36 members**. §290.4 forbids enlarging the first tranche, so this plan ports
only what §290.1's core-first slice and §287.16's massing scope actually require, and names the
remainder as later work rather than smuggling it in.

What D3a lands, in order:

| # | member | what it does | class |
|---|---|---|---|
| 0 | **MF-T2A** | the §310.3(7) first act: a same-name/different-contract export scan over `src/domain/townMap/fabric/**`, asserting **exactly zero** | guard only |
| 1 | **MF-T2B** | the versioned integer ABI + the exact-geometry core it rests on | port |
| 2 | **MF-T2C** | the codex embedder extended onto the ABI — BigInt, the four divergences, point location, and the measured 12.7× repair | port |
| 3 | **MF-T2D** | the arrangement compiler REPLACED (codex jig out, sandbox noder in) | port / replace |
| 4 | **MF-T2E** | `MassPartQ` / `SolidPartQ` — the typed functional-volume vocabulary | massing foundation |
| 5 | **MF-T2F** | exact solid legality extended from area to VOLUME (§299.3a) | obligation |
| 6 | **MF-T2G** | the §288-nonconforming light profile **STRIPPED** (§299.3b) | obligation |
| 7 | **MF-T2H** | the spatial-receipt seam (§287.4) | port |
| 8 | **MF-T2J** | the executable S0–S23 stage manifest and its source-derived walker | port |
| 9 | **MF-T2K** | support-relative solids and component slots | massing foundation |
| 10 | **MF-T2L** | vegetation: durable trunk/canopy mass + separate nonpersistent instance field | massing foundation |
| 11 | **MF-T2M** | architectural-history operations + deterministic fixtures | massing foundation |
| 12 | **MF-T2N** | dossier surface: a resource **LOCATION** | §306.4a, engine-side |
| 13 | **MF-T2P** | dossier surface: a dated **CONSTRUCTION OPERATION** | §306.4a, engine-side |
| 14 | **MF-T2Q** | dossier surface: an institution **FOUNDING YEAR** | §306.4a, engine-side |
| 15 | **MF-T2R** | dossier surface: the **high-water evidence channels** | §306.4a, engine-side |
| — | **MF-T2S** | §299.3c operation-vocabulary merge — **REFUSED TO COMPILE NOW**, two shapes stated | ⛔ chair |

**The dormancy boundary, stated once and true of every member above:** nothing lights. No member
adds a consumer to a live surface, no member changes a generated byte, and **live seeds stay
byte-identical**. The proof is per-member and structural, not incidental — §2.4.

**Three things the chair must rule before dispatch** (full docket at §11): the family preamble
and its **un-stamped** member cap; the §310.3(7) rename's target tree; and whether "the port's
seal" in §304.4 means D3a's seal or the port's completion.

---

## §1 · THE ADOPT / REPLACE SPLIT, RESOLVED AGAINST BOTH TREES

§303.5 is the binding instruction: *"adopt the codex slice's RECORD SHAPES and its embedder's
algorithm; do NOT adopt its arrangement compiler, census, or parcel registry (a single-fixture
jig that throws on 8 of 17 real leaves). On the integer wall: **D1's versioned ABI wins**."*

Resolved module by module against the 18 codex modules at `9d851fae` and the 54 sandbox modules
at the sealed tip:

| codex module | eff | §303.5 verdict | D3a member |
|---|---:|---|---|
| `dcelEmbedding.js` | 169 | **ADOPT the algorithm**, extend the contract | MF-T2C |
| `foundation.js` | 143 | **ADOPT the record shapes**; re-parameterise the 0..1000 wall | MF-T2B |
| `boundaryArrangement.js` | 247 | ⛔ **DO NOT ADOPT** — replaced by the sandbox noder | MF-T2D |
| `dcel.js` | 89 | ⛔ **DO NOT ADOPT** — the census; superseded | MF-T2D |
| `parcelRegistry.js` | 216 | ⛔ **DO NOT ADOPT** — the single-fixture jig | later wave |
| `settlementFoundation.js` | — | ADOPT the record shapes; the fixture plan is not the input | untouched in D3a |
| `shapes.js`, `building.js`, `massingRoster.js` | — | **ADOPT** — the explicit-mass record shapes §287.16 extends | MF-T2E/K/L |
| `projection.js` | 374 | ADOPT, **minus** the light profile | MF-T2G |
| `operations.js` | 278 | ⛔ **second vocabulary** — merges or dies (§299.3c) | MF-T2S ⛔ |
| `content.js`, `frontage.js`, `fabricRoot.js`, `streetGeometry.js`, `streetGraph.js`, `massingProjection.js`, `index.js` | — | ADOPT as-is; untouched by this tranche | — |

**And the reciprocal direction — what the SANDBOX supplies that the codex has no answer for.**
The port is not one-way: the sandbox's algorithms REPLACE codex machinery in three places, each
because the codex contract cannot express a shape the fabric actually publishes. The four
divergences are `laneMFD1-receipt.md` §6.2's, re-verified here against both sources:

| # | the codex rule (read at `9d851fae`) | why it fails on real ground | the replacement |
|---|---|---|---|
| 1 | `requireCanonicalInt` defaults to `0 ≤ v ≤ 1000` | ABI quanta reach ~1.3e9 | quantize through the ABI — **and convert the arithmetic to BigInt**, see §3.2 |
| 2 | face record is `{faceId, faceKind, boundaryHalfEdgeId}` — one boundary, singular | a demoted circuit nested inside a live one **is** a hole | `faces[].innerBoundaryHalfEdgeIds` + a `HOLE_CYCLE` kind |
| 3 | one `outerFaceId`, the first negative cycle in hash order | with a hole or a second component it can name the **hole** | `outerFaceIds`, one per connected component |
| 4 | `throw 'DCEL face must have nonzero signed area'` on any open chain | a half-ring bankside circuit **is** an open chain — **8 of 17 leaves** | a zero-area cycle is typed `DEGENERATE` and reported |

⭐ Plus one facility the codex names as an explicit non-goal in both its packets: **point
location**. §287.8's third leg is minted in the port, under the ABI's `boundaryRule: 'CLOSED'`.

---

## §2 · WHAT THIS TRANCHE IS BOUNDED BY

### 2.1 · §290.1's core-first slice is the outer fence

§290.4: *"Build order remains foundation/D1 → fresh W3/frontage → D3a → D4 → the parity vertical
slice. Stop there... The existence of a conceivable edge case is not by itself authority to
enlarge the first tranche."* Every member below is inside D3a's own box. Nothing in this plan
advances D4, the parity slice, or A0–A5.

### 2.2 · The full port does not fit, and the plan says so rather than discovering it

Measured (receipt §1, `laneTCD3A-measure-files.mjs` over the sealed tip):

```
14,230 effective lines · 54 modules
   22 modules exceed the 250-eff-per-new-leaf cap outright
    5 exceed 650: buildFabric 811 · substrate 715 · streets 713 · parcels 685 · fabricGeometry 681
```

`PACKET_STANDARD`: *"If the work cannot fit, the agent stops and proposes the smallest split. The
agent may not quietly renegotiate the budget."* This plan is that split. **The generator half of
the sandbox — `buildFabric`, `streets`, `parcels`, `substrate`, `fields`, `institutions`,
`habitation`, `walls`, `waterWorks` and their neighbours, ~9,900 effective lines — is NOT in
D3a.** It is the port's later tranches, and §304.4's twin-life retirement fires when they land,
not when D3a seals. See RAISED-5.

### 2.3 · The port's dependency spine is small, and that is why D3a can start

The D1 foundations do not drag the generator. Measured transitive closure inside the sandbox's
681-effective-line `fabricGeometry.js`, seeded with exactly the symbols the five D1 foundation
modules import:

```
$ node laneTCD3A-geomcore.mjs <fabricGeometry.js> TOPOLOGY_PLACES TRIG_N q6 absArea bounds \
      polygonIntersectionArea triangulateSimple triangulationIsSound offsetLine \
      pointLocateRing properCross segIntersect
module total effective : 681
top-level declarations : 57
CLOSURE                : 19 declarations, 167 effective lines
```

⭐ **The exact-geometry core the whole foundation layer needs is ~170 effective lines of 681** —
`triangulateSimple`(30) · `polygonIntersectionArea`(16) · **`clipHalfPlane`(15)** · `offsetLine`(12) ·
`pointInPolygon`(11) · `triPairArea`(11) · `bounds`(10) · `segIntersect`(10) · `area`(8) ·
`crossParams`(8) · `distToSegment`(8) · `pointLocateRing`(8) · `triangulationIsSound`(7) ·
`inTriangleStrict`(6) · `properCross`(5) · `TOPOLOGY_PLACES` · `CROSS_EPS` · `absArea` · `q6`.

⚠ Four of those measure 0 effective under `max-lines` because they are single-line bodies, so the
true figure is **~171**, not 167. Stated rather than rounded away.

⭐⭐ **AND `clipHalfPlane` IS INSIDE THAT CLOSURE** — which is precisely why MF-T2A goes first.
See §7.

### 2.4 · The dormancy boundary — how each member PROVES it, not asserts it

§287.16: *"D3a may land explicit-input support-relative solids, typed functional volumes,
component slots, architectural-history operations and deterministic fixtures **while leaving live
seeds byte-identical**."* Three structural facts make that provable rather than hopeful, each
measured:

1. **The app fabric has no input producer.** CX-1 established and this lane re-checked: the only
   construction of the admitted plan kind in the whole tree is a hand-typed test fixture. A
   settlement cannot reach this code. A module that lands here therefore cannot move a seed
   because nothing calls it with a seed.
2. **Zero consumers outside the directory.** Every D3a member's `requiredSymbols` closure stays
   inside `src/domain/townMap/fabric/**` plus the barrel; **no member adds a live consumer**, and
   each member's acceptance matrix carries the consumer-absence arm explicitly.
3. **The barrel is import-graph-safe and already gated.** `src/domain/townMap/index.js` re-exports
   the fabric and is imported by 14 live modules, but nothing eager imports the barrel; the entry
   static closure is unmoved and `tests/build/townMapLazy.test.js` is the standing enforcement.
   ⛔ **Every D3a member runs that build test focusedly** — an added leaf that pulls a new
   transitive import into the entry closure is exactly how a dormant port stops being dormant.

⛔ **The dormancy claim is a per-member EXECUTED claim, never inherited from this section.** Each
packet names its own golden-capture step (implementation order step 1) and quotes the result.

### 2.5 · What D3a explicitly does NOT do — named affirmatively, never by silence

- **No D3b activation.** §287.16: empirical/cultural activation waits for `AMP-1` reliability,
  clustered range and the separate 18% sealed holdout. No occurrence distribution, frequency,
  prevalence or activation weight is minted; those stay `NOT_IDENTIFIED` / `NONE`.
- **No D4 light.** §299.3b is discharged by **stripping** a non-conforming profile, not by
  building a conforming one. §288.8 holds implementation dormant and puts fixed survey after D4.
- **No authorship A0–A5.** §287.12 puts them last, and only through the operation seam after
  canonical identities and frontage exist.
- **No parcel/frontage equivalence.** §310.1 states plainly that S9 frontage/parcel-DCEL
  equivalence *"is NOT BUILT"* and is carried to the D3a **era**. This plan carries it forward
  again, to the tranche that ports `parcels.js`, and records the carry rather than dropping it.
- **No tuning.** The arrangement-quantum ladder's three rungs `[1000, 5000, 25000]` are on the
  TUNING-SIGNATURE docket (§310.3(9), owner). ⛔ **No D3a member picks a rung**; the port carries
  rung 0 unchanged as the ported value, and says so.
- **No non-European map tradition.** `EUROPEAN_FANTASY_BASE` stays stamped and asserted for
  equality at every ported compiler, exactly as the codex slice already does.
- **No corpus byte moves, no plate re-record, no soak.** §305.4's RS-5 law: a soak fires at the
  next exposure that changes engine behaviour. D3a's map members change none. ⚠ **The §306.4a
  members DO** — see §5.3.

---

## §3 · THE MEASURED SUBSTRATE (executed at `9d851fae`; re-execute what you touch)

Nothing in this section is a budget. These are stamped facts about the base, in the WC-preamble's
§P9 sense; the member packets re-execute every row their own manifest touches.

### 3.1 · The sandbox is not in git, and that changes how a port packet cites its source

```
$ git -C laneMFW3F-tip rev-parse --show-toplevel
fatal: not a git repository (or any of the parent directories): .git
```

⛔ **There is no sandbox commit to name.** A port packet cannot write "ported from `<sha>`". The
plan's rule, binding on every port member:

> A port member's change manifest names each source module by **path plus its SHA-256 at compile
> time**, recorded in the packet body. The implementer re-hashes before editing; a mismatch is a
> STOP, not a merge.

This also means the sealed tip is the program's **only** copy of that evidence. ⛔ **No lane
edits it.** That is decisive for §7.

### 3.2 · The two convictions that shape MF-T2C

Both executed in this lane against the codex blobs at `9d851fae` (receipt §1.2, §1.3):

**(a) The kernel is quadratic, and one line is 84% of it.** `derivePlanarDcelEmbedding` measured
`759.4 ms → 4,258.6 ms → 13,634.9 ms` at 2,112 / 4,704 / 8,320 boundaries — fitted exponents 1.99
and 2.04. The dominant term is
`const start = [...unvisited].sort(compareCodepoint)[0]` **inside** the face-traversal loop:
`O(F · E log E)`. Amortising it to a pre-sorted cursor gives **13,634.9 ms → 1,070.8 ms, 12.7×**,
and the patched build is **SHA-identical to the landed one at four sizes**. Extrapolated to real
leaves: town ≈ **26.5 s**, metropolis ≈ **59.7 s** (PLAUSIBLE — arithmetic over a measured
exponent; MF-T2C's A5 is the run that settles it).

**(b) At ABI scale the kernel does not draw a wrong map — it THROWS on valid geometry.** The
codex `orient`/`compareRay` multiply in `Number`; at ~1.3e9 quanta each product reaches ~1.66e18,
**184× past `MAX_SAFE_INTEGER`**. On `a=[0,0] b=[m−1,m] c=[m,m+1]`, `m = 1286630001`:

```
Number orient = 0     BigInt orient = -1     *** the SIGN FLIPPED ***
compareRay would THROW 'DCEL vertex has an angular tie' on a NON-tie
```

⛔ **This is the sharpest finding of the compile.** The failure wears the message of a condition
that did not occur, so it would be debugged as a geometry defect for as long as it took someone
to suspect the arithmetic. MF-T2C carries this exact triple as a named acceptance case.

### 3.3 · The estate registers, and which ones are free here

| register | instrument | cost to a `townMap/fabric` member |
|---|---|---|
| test census | `tests/lint/sovereigntyLightingContract.walker.test.js` | the five-figure tuple, per member |
| **coupling / layer census** | `tests/lint/couplingInclusion.walker.test.js` | ⭐ **ZERO** |
| **size ratchet** | eslint `max-lines` + `scripts/.size-baseline.json` | ⭐ **ZERO** |
| **hot files** | the `PACKET_STANDARD` standing list | ⭐ **ZERO** |
| mutation coverage | `tests/lint/mutationCoverageManifest.test.js` | one row per new `tests/lint/**` file |
| anchor walker | `tests/lint/negativeAssertionAnchor.walker.test.js` | per new test file, ceiling zero |
| build/lazy fence | `tests/build/townMapLazy.test.js` | per member, focused (§2.4) |

The coupling census is free because its own standing cannot-catch list says so, quoted at
`9d851fae`: *"The census is total over `src/domain/worldPulse` and `src/domain/spatial` only; a
layer leaf that lands in a third directory is unclaimed and uncounted until the scope widens."*
The size ratchet is free because no `townMap` path carries a `.size-baseline.json` entry and the
plain `src/domain/**` ceiling is **800** effective lines under `max-lines {skipBlankLines,
skipComments}` — against which `dcelEmbedding.js` sits at **169**.

⛔ **All four zeros end at the §306.4a members.** A `worldPulse` leaf is inside the coupling
census, owes a `LAYER_PATTERNS` family home or an `ARGUED_UNLAYERED` entry **in the same commit as
the leaf**, and `ARGUED_ROSTER_CEILING` is an exact `.toBe(20)` in both directions. Priced at §5.

### 3.4 · The test census tuple, and this family's two known price shapes

```
$ git show 9d851fae:tests/lint/sovereigntyLightingContract.walker.test.js | sed -n '4848p'
    files: 2484, parked: 364, credited: 2120, titles: 20598, suiteTitles: 5767,
```

The thirteen codex packets re-recorded this in place, each naming its cause. Read as a price list
the family has exactly two shapes, and **every one of the thirteen kept `parked` at 364**:

| shape | delta `files/parked/credited/titles/suiteTitles` | precedent |
|---|---|---|
| domain matrix + determinism companion | **+2 / 0 / +2 / +6 / +2** | T1G, T1N, T1A, T1D, T1P, T1F |
| single credited A1–A6 file | **+1 / 0 / +1 / +6 / +1** | T1M, T1V, T1S |

⛔ **Every D3a member is held to one of these two shapes.** One literal `describe`, straight-line
`it` calls, string-literal titles. **No `.each`, no `runIf`, no nesting** — a `test.each` case is
invisible to the census by construction and a `describe.runIf` parks the file whole, and either
keeps the arithmetic closing while `credited` silently does not move.

⛔⛔ **AND EVERY MEMBER NAMES ITS AUTHORISING DECISION IN THE PACKET BODY.** §299.4 ruled the
codex slice's thirteen self-authorised re-records an AUTHORITY breach — retroactively authorised,
but *"BINDING FORWARD: a packet that moves any census/ratchet NAMES ITS AUTHORIZING DECISION in
the packet body."* Every D3a member carries a `censusAuthorization` block citing **ODQ §310.4**
(the decision that dispatches this wave). A member without one is defective on its face.

⚠ The census is **SEQUENCED and stops at its first red figure**: when `files` reds, `parked` /
`credited` / `titles` / `suiteTitles` never execute, and a green there is a green that never ran.

### 3.5 · The base-state capsule is NOT citable at this base

`BASE_STATE.json` is stamped `b8946403` (2026-08-16); the base is `9d851fae` and the window is
not docs-only. Per the capsule's own `consumptionLaw` it is **worthless for citation here**, and
this lane cited none of it. ⛔ **Every member measures its own rows from scratch** until a
capsule is regenerated at a D3a base.

---

## §4 · THE PORT MEMBERS — priced

Budgets are `PACKET_STANDARD`'s defaults unless a row says otherwise: ≤2 new logic-bearing
production leaves · ≤3 existing logic files modified · ≤12 handwritten files · ≤400 new/changed
effective production lines · ≤250 effective per new leaf · ≤8 acceptance cases.

### MF-T2A · the §310.3(7) first act — the same-name/different-contract scan

Full draft: `draft-MF-T2A.md`. Guard only; **zero production lines move.**

| | |
|---|---|
| new leaves | 0 |
| existing logic modified | 0 |
| handwritten files | 3 (`tests/lint/**` walker · mutation-coverage row · census re-record) |
| production eff | **0** |
| census | +1 / 0 / +1 / +6 / +1 |
| standing obligation | ⛔ a new `tests/lint/**` file owes its **mutation-coverage row**, priced here |

The scan asserts **exactly zero** duplicated export names in `src/domain/townMap/fabric/**` —
measured at 0 of 94 today (receipt §1.5). It is the census-burn law's *victory assertion*: the
population is exactly zero, the message names what is forbidden, and **any future member reds on
arrival** rather than being banked into a census.

### MF-T2B · the versioned integer ABI and the exact-geometry core

Full draft: `draft-MF-T2B.md`.

| | |
|---|---|
| new leaves | 2 — `fabric/coordinateAbi.js` (~95 eff) · `fabric/exactGeometry.js` (~175 eff) |
| existing logic modified | 1 — `fabric/foundation.js`, bounds threaded through `requireCanonicalInt` |
| REGISTER | `fabric/index.js` (+6) |
| production eff | **≈ 285 of 400** · per-leaf **175 / 250** and **95 / 250** |
| census | +2 / 0 / +2 / +6 / +2 (the domain-matrix + determinism-companion shape) |

⭐ The ABI wall is a **default parameter**, not a constant —
`requireCanonicalInt(value, label, min = 0, max = 1000)` — so this is argument threading at the
call sites, not a validator rewrite. That is the single measurement that makes MF-T2B affordable.

⚠ **`coordinateAbi.js`'s `angleTableSize: TRIG_N` would drag `trigTable.js` (270 eff, two
1024-entry tables) and blow the budget.** The port declares `ANGLE_TABLE_SIZE = 1024` locally
with an equality pin against the sandbox constant, and the table itself ports with the geometry
that needs it. **JUDGMENT, vetoable** — the alternative (port the table now) costs a third leaf
and 270 effective lines for a value nothing in this tranche reads.

### MF-T2C · the embedder extended onto the ABI

Full draft: `draft-MF-T2C.md` is **not** in this batch; the shape is fixed here and the packet
compiles when MF-T2B lands (`PACKET_STANDARD`'s primary scope-control rule: *"Compile the next
packet only when its dependencies are landed and its live substrate is measurable"*).

| | |
|---|---|
| new leaves | 0 (extends the adopted algorithm in place) |
| existing logic modified | 2 — `fabric/dcelEmbedding.js` (169 → ~305 eff) · `fabric/dcel.js` |
| production eff | **≈ 150 of 400** |
| carries | BigInt conversion · divergences 2/3/4 · point location · the 12.7× repair |
| census | +2 / 0 / +2 / +6 / +2 |

⛔ **The BigInt conversion and the four divergences are ONE behaviour**, not five: without BigInt
the extended kernel throws a false angular tie on real ground (§3.2b), so a member that lands the
divergences without the arithmetic lands a kernel that cannot run. They ship together or not at
all.

⚠ **The 12.7× repair is a REPAIR, not a declared shift** — proved output-identical at four sizes
(receipt §1.2). It carries no golden motion and needs no shift declaration. Saying so here is
what keeps a later reader from re-deriving it as one.

### MF-T2D · the arrangement compiler replaced

| | |
|---|---|
| new leaves | 1 — `fabric/boundaryNoder.js` (the sandbox `buildBoundaryArrangement`, ~180 eff) |
| existing logic modified | 1 — `fabric/boundaryArrangement.js`, retired to a thin adapter or deleted |
| `retiredSymbols` | the codex arrangement compiler; `requiredSymbols` names the **successor**, and only once it exists (at the flip to LANDED) |
| production eff | ≈ 200 of 400 |
| census | +2 / 0 / +2 / +6 / +2 |

⛔ **A packet that names one symbol in both `requiredSymbols` and `retiredSymbols` is refused**,
and a symbol the deliverable CREATES cannot be named at `READY`. Both bite here; the packet's
status sequence is simulated before dispatch (§9.3).

### MF-T2E · `MassPartQ` / `SolidPartQ` — the typed functional-volume vocabulary

The record family §287.16 charters and §299.3a's volume arm needs. One new persisted record
family — the budget's exact allowance of one.

| | |
|---|---|
| new leaves | 1 — `fabric/massPart.js` (~140 eff) |
| production eff | ≈ 150 of 400 |
| census | +1 / 0 / +1 / +6 / +1 |
| ⛔ | half-open vertical intervals `[baseQ, topQ)` on a **NAMED support surface** — the rule D1 declared in advance so D3a would inherit it rather than mint a second one |

### MF-T2F · exact solid legality, extended from area to VOLUME — §299.3a

D1 built the predicate and left the extension point explicit: *"`VOLUME` with `sharedVolumeQ` —
the same call, once D3a supplies `[baseQ, topQ)`."*

| | |
|---|---|
| new leaves | 1 — `fabric/solidLegality.js` ported (~110 eff) + the volume arm |
| existing logic modified | 1 — `fabric/exactGeometry.js`, `polygonIntersectionArea` re-exported for the volume path (+0 eff, already public) |
| production eff | ≈ 150 of 400 |
| census | +2 / 0 / +2 / +6 / +2 |
| ⛔ discriminated result, never a boolean | `REFUSED/SAME_SOLID_ID` · `REFUSED/DIFFERENT_SUPPORT_SURFACE` · `PLANAR_ONLY` · `VOLUME` |
| ⭐ the pinned negative | two solids stacked `[0,5)` and `[5,9)` share **100 units of area and ZERO volume** |
| ⭐ the pinned conviction | the interval predicate §287.5 forbids by name says `OVERLAPPING` on the L-and-notch pair where the exact predicate returns `sharedAreaQ: 0` |

⚠ **The incumbent `groundLaw.overlapping` is NOT convicted and must not be reported as such.**
D1 measured `legacyFalsePositive = 0`: §17's boolean is not wrong, it is *unquantified*. No D3a
member changes it and nothing in the generation path calls the new predicate.

### MF-T2G · the light profile STRIPPED — §299.3b

§299.3b: the codex's published profile *"CONFORMS OR IS STRIPPED at D1"*, and §288.8 says
*"IMPLEMENTATION IS DORMANT"* with fixed survey after D4. CX-1 measured what the profile carries:
`FIXED_SURVEY_LIGHT_V1`, `mode: 'FIXED_SURVEY'`, `directionQ: [2,1]`,
`shadowRunDenominatorQ: 4` — and **no `GLOBAL_CELESTIAL` source, no azimuth/elevation pair, no
Sun/Moon typing, no receiver-separation or clamp-law record**, all four of which §288.2/§288.3
require of *"every published dimensional light profile."*

**STRIP, not conform. JUDGMENT, vetoable.** Conforming means building §288.2–§288.5's whole
contract, which is D4 scope and would breach §290.4's stop. Stripping is one deletion inside a
dormant module and leaves `PLANAR_V1`'s own rule intact: *"exposes no probe rather than
fabricating height."*

| | |
|---|---|
| new leaves | 0 |
| existing logic modified | 2 — `fabric/projection.js` · `fabric/index.js` |
| production eff | **negative** (a deletion); ≤ 40 changed |
| census | +0 / 0 / +0 / +2 / +0 — two arms added to an already-credited file |
| ⛔ | the DM/PUBLIC projection digests **must move** here; that motion is the member's declared, expected result and is captured as a golden before the edit, per implementation-order step 1 |

⚠ ⛔ **This is the ONLY D3a member whose bytes are expected to move at all, and they are
*artifact* bytes in a dormant module, never *seed* bytes.** A member that finds a live seed moved
STOPS.

### MF-T2H · the spatial-receipt seam — §287.4

| | |
|---|---|
| new leaves | 2 — `fabric/spatialReceipt.js` (~130 eff) · `fabric/fabricRng.js` (~85 eff) |
| production eff | ≈ 220 of 400 |
| census | +2 / 0 / +2 / +6 / +2 |
| ⛔ the three rules are **unrepresentable**, not forbidden | `effectiveAt` strictly greater than every source time (same-pass feedback cannot be *built*) · the dependency roster closed by kind and temporal branch, in order · a no-effect result is a `noEffectDiagnostic`, never an empty receipt |
| ⭐ | the digest is built from a **named field roster**, not from the object — which is what makes §10.1's refusal structural |

### MF-T2J · the executable S0–S23 stage manifest

| | |
|---|---|
| new leaves | 1 — `fabric/stageManifest.js` (~220 eff) |
| handwritten | + the 12-arm source-derived walker as a `tests/lint/**` file ⇒ ⛔ **owes a mutation-coverage row**, priced here |
| production eff | ≈ 230 of 400 |
| census | +2 / 0 / +2 / +6 / +2 |

⚠ The manifest's own headline result travels with it and must not be quietly dropped: the module
graph is acyclic, and the **STAGE** graph carries one SCC (`S2·S3·S4·S6·S13`), closed by two named
imports. The port preserves the SCC as published data; it does not "fix" it. That is W4's work.

### MF-T2K / MF-T2L / MF-T2M · the remaining §287.16 massing foundations

Shapes fixed, packets compiled when their substrate lands. Each is a single-behaviour member on
the `+1 / 0 / +1 / +6 / +1` census shape.

- **MF-T2K — support-relative solids and component slots.** §287.5: *"terrain-mounted parts name
  a support surface."* The `DIFFERENT_SUPPORT_SURFACE` refusal MF-T2F ships is this member's
  consumer, so the order is F → K, not K → F.
- **MF-T2L — vegetation.** §287.5 requires **both** halves: a durable selectable trunk/canopy mass
  **and** a separate deterministic *nonpersistent* instance field. ⛔ A member that lands only the
  durable mass has landed half a contract and made the other half harder; both or neither.
- **MF-T2M — architectural-history operations and deterministic fixtures.** ⛔ **Blocked behind
  MF-T2S**: an "architectural-history operation" is an operation, and §299.3c forbids a second
  vocabulary. This member cannot compile until the door question is ruled.

---

## §5 · THE §306.4a DOSSIER-SURFACE MEMBERS — engine-side, and priced differently

§306.4a adopts three missing dossier surfaces plus the high-water evidence gap as W8
prerequisites *"routed into the D3a/A0–A5 charter as THE DOSSIER-SURFACE BATCH."*

### 5.1 · The batch splits into a PRODUCER half and a CONSUMER half, and only one is expensive

```
$ grep -rn 'deriveHighWater' laneMFW3F-tip/src
  fabric/tierGrammar.js:574   export function deriveHighWater(settlement)
$ git grep -l 'deriveHighWater' 9d851fae
(empty)
```

⛔ **`deriveHighWater` is sandbox-only.** §306.2a's finding — *"deriveHighWater's three evidence
channels are empty on 16 of 17 leaves"* — is therefore **not a defect in the reader**. It is the
statement that the app-side engine never produces the facts the reader looks for. The same holds
for a resource LOCATION, a dated CONSTRUCTION OPERATION and an institution FOUNDING YEAR: each is
a **producer** gap in `src/domain/worldPulse/**` and `resourceAnalysis`, and the consumer arrives
free with the port.

**Consequence for the order: the producers go LAST in D3a and are independent of the port
members.** They unblock W8 and the benchmark's direction arms; they do not unblock any port
member. This is exactly what makes them a lawful pivot target under R1's dual-family ready queue
(§9.2).

### 5.2 · What a `worldPulse` leaf costs — every zero of §3.3 turns on

| obligation | instrument | form |
|---|---|---|
| a layer home **or** a written argument, **in the commit that creates the leaf** | `tests/lint/couplingInclusion.walker.test.js` | `LAYER_PATTERNS` family regex, or an `ARGUED_UNLAYERED` entry |
| the argued-roster ceiling | same file | `ARGUED_ROSTER_CEILING` `.toBe(20)` — ⛔ **exact in BOTH directions** |
| the unlayered census | same file | `expect([...UNLAYERED_BASELINE].sort()).toEqual([...LIVE_UNLAYERED].sort())` |
| a `spatialLedgers` row, if the leaf writes one | `tests/lib/spatialLedgerCoverage.walker.test.js` | `expect(classified).toEqual(written)` — the row lands **with** its writer, never ahead |
| the flag-mint three obligations, **if** a flag is minted | `engineGatedRuleKeys.walker` + `subsystemRowsVirtual` | manifest row + first by-name gate read + certification row are **ONE ATOMIC ACT** |

⚠ **A `LAYER_PATTERNS` addition is safe while its target is absent; an `ARGUED_UNLAYERED` addition
is NOT** — the argued arm asserts `expect(DOMAIN_MODULES, '<module> vanished — re-aim the
exclusion').toContain(module)`, so an argued entry **must** land in the same commit as its leaf.

⭐ **And the choice is not "pick the cheap name."** The roster's own comment reserves the argued
door for *"modules that own NO subject and are spoken by every port."* A leaf that owns a subject
takes a **family home**. Each of MF-T2N/P/Q/R owns a subject — resource siting, construction
dating, institution founding, settlement high-water — so each takes a family home, and the plan
predicts a `LAYER_PATTERNS` edit rather than an argued row for all four. **JUDGMENT, vetoable.**

### 5.3 · ⛔ These four ARE engine-behaviour members, and the soak law fires

| | |
|---|---|
| §305.4 | *"RS-5 fires at the NEXT build-branch exposure that changes engine behaviour"* |
| the port members | change no engine surface — **no soak owed** |
| **MF-T2N/P/Q/R** | write new dossier facts into world state — **a soak IS owed at their exposure** |

⛔ **This is the single largest cost item in the plan and it is not a packet cost.** A 162-cell
soak is a program act, not a member's wave-end gate. **The plan therefore recommends that
MF-T2N/P/Q/R ride their own train, exposed separately from the port members**, so the port's
seal is not held hostage to a soak. See RAISED-4.

⚠ And each of the four must be checked for §306.2's **live input trap**: writing terrain to
`config.terrainOverride` — the field the exemplar harness itself passes — is a **silent no-op**
(0 of 409 components moved). A producer whose fact lands in a field nothing reads is exactly the
same class of defect wearing a different name. **Each of the four proves its fact reaches a
consumer by an executed counterfactual, not by a write-then-read of its own field.**

### 5.4 · The four members

| member | fact | producer home (predicted) | consumer |
|---|---|---|---|
| **MF-T2N** | resource **LOCATION** | `src/domain/worldPulse/**`, `resourceAnalysis` — ⚠ §287.9 folded the live-source correction: *resources live under `resourceAnalysis`* | the port's siting reads |
| **MF-T2P** | dated **CONSTRUCTION OPERATION** | the §287.7 domain door — ⛔ **blocked behind MF-T2S** for the same reason as MF-T2M |
| **MF-T2Q** | institution **FOUNDING YEAR** | `src/domain/worldPulse/**` beside the institution lifecycle | the port's institution reads; ⭐ §306.4a proves the need — *the same cathedral appears on the year-18 leaf* |
| **MF-T2R** | the **high-water evidence channels** | `src/domain/worldPulse/**` | `deriveHighWater`'s three channels, empty on 16 of 17 leaves |

⚠ **MF-T2R has a second, separate defect behind it that this member does NOT cure**, and the
distinction must survive into the packet: §306.2a found that beyond the starved high-water law,
*"the ring re-traces against drawn fabric even at fixed extent"* — a §297.3a retrospective-partition
gap now carrying a causal receipt, ordered into **W4's** generative-epoch work. Feeding the
channels will move the population-cut arm partway and **will not** make it pass. A member that
predicts a passing arm has mis-predicted.

---

## §6 · §299.3's THIRD OBLIGATION — ⛔ REFUSED TO COMPILE NOW

§299.3c: a second operation vocabulary was minted outside the §287.7 domain door; *"it MERGES
into the one registry; a second vocabulary is a second truth and is refused."*

**Located, at `9d851fae`:**

| side | path | eff |
|---|---|---|
| the second vocabulary | `src/domain/townMap/fabric/operations.js` | 278 |
| the §287.7 door | `src/store/operationRegistry.js` | 244 |
| | `src/store/operations.js` | 25 |
| | `src/store/actionResult.js`, `src/store/outbox.js` | — |

⛔ **The merge crosses `src/domain/**` → `src/store/**`.** That is a layer-direction question, not
a refactor, and `PACKET_STANDARD`'s authority order is explicit: *"If two higher authorities still
disagree, the packet is BLOCKED. A coding agent never adjudicates the disagreement."* This lane
does not adjudicate it either. The two candidate shapes, stated so the chair rules on shapes
rather than on adjectives:

**Shape A — the fabric vocabulary becomes a REGISTRATION in the store's registry.**
`fabric/operations.js` keeps its pure compilers and stops declaring `artifactKind:
'CANONICAL_SPATIAL_OPERATION'` and its own gate; the store's `operationRegistry` gains the rows,
the authority check and the `ActionResult`/outbox path. *For:* one registry, one truth, exactly
what §287.7 says. *Against:* the domain leaf must be reachable from the store, and D3a would be
minting store rows for operations nothing can yet execute.

**Shape B — the fabric vocabulary is STRIPPED to a proposal, as §287.7's own AI clause allows.**
*"AI proposes canonical operations and may execute only declared low-risk document verbs."* The
fabric emits a typed *proposal* record; the door stays the store's and gains nothing in D3a.
*For:* strictly smaller, no layer crossing, and it matches the fabric's actual status (headless,
nothing accepted). *Against:* the merge is deferred rather than done, and §299.3c said merge.

⭐ **This lane's recommendation is Shape B for D3a with Shape A docketed for the tranche that
lands a real executor** — because §290.2's release-blocking list is not triggered by either, and
Shape A mints store rows for operations that no path can reach, which is the "registration ahead
of its door" defect the estate has already recorded twice. **RAISED-6; the chair decides.**

⛔ **MF-T2M and MF-T2P are both blocked behind this ruling** and are marked BLOCKED, not DRAFT —
`PACKET_STANDARD`: *"BLOCKED — instructions are intentionally withheld pending a named dependency,
ruling, attestation, or substrate decision."*

---

## §7 · THE RAISED-7 RENAME MICRO-ITEM — THE FIRST ACT, AND WHY IT IS NOT A RENAME

§310.3(7): *"two same-named `clipHalfPlane` exports with OPPOSITE conventions: the rename
micro-item is ORDERED into the next map wave's first act, plus a source scan refusing
same-name/different-contract exports (the D1 walker family's next arm)."*

**The measurement that reshapes the order** (receipt §1.4): `clipHalfPlane` has **zero
occurrences on the build branch**. Both exports live only in the sealed sandbox tip:

```
fabric/groundLaw.js:88        clipHalfPlane(poly, qx, qy, nx, ny)   keeps (p−q)·n ≥ 0
fabric/fabricGeometry.js:116  clipHalfPlane(poly, ox, oy, nx, ny)   keeps (p−o)·n ≤ 0
```

The conventions are **complementary half-planes**, and `groundLaw.js:701` says so in the tree:
*"the 'obvious' reuse silently keeps the complement of the ground it was asked for."*

**So a rename executed today would edit a tree that (a) is not in git, (b) is the program's only
copy of the sealed W3 evidence, and (c) retires at the port's seal.** It buys nothing durable and
risks the seal. The order's two halves therefore split, and only one of them is D3a's first act:

1. ⭐ **THE SOURCE SCAN LANDS NOW, AS MF-T2A** — app-side, over
   `src/domain/townMap/fabric/**`, asserting **exactly zero** duplicated export names. Measured
   at **0 of 94** today, so it lands green at zero with no baseline. A whole-`src/` scan is
   un-landable at **92** offenders / **199** pairs; a `src/domain/townMap/**` scan needs a
   one-row baseline for the pre-existing `TOWN_MAP_OVERLAY_VERSION` fork. The fabric scope needs
   neither, which is why it is the scope.
2. ⭐⭐ **THE RENAME IS THEN DISCHARGED BY THE GUARD, AT THE MOMENT THE GEOMETRY PORTS.**
   `clipHalfPlane` is inside MF-T2B's 19-declaration geometry core (§2.3), so **one** of the two
   arrives at MF-T2B. Its opposite twin lives in `groundLaw.js` (362 eff), which ports in a later
   tranche — and on the day it does, the guard reds unless the two arrive under distinct names.
   The cure is structural: the habitat is closed **before** the sweep, not patched after it.
3. ⛔ **THE SANDBOX-SIDE RENAME IS REFUSED, WITH ITS REASON RECORDED** — not dropped. The sealed
   tip is evidence, not code of record for the app; editing it breaks a seal to fix a defect that
   the guard catches on arrival anyway.

**This is a deliberate divergence from the literal order and it is flagged, not smuggled:
RAISED-2.** If the chair prefers the literal reading, the sandbox rename is a two-file edit that
does not touch this plan's members; nothing here depends on the refusal.

---

## §8 · THE FAMILY AND ITS PREAMBLE

**The family is `town-cartography`** — twenty packets at `9d851fae` (thirteen `MF-*`, seven
`TC-*`). D3a's members continue the `MF-T1x` series as **`MF-T2x`**.

**No preamble exists for it.** Seven do (`GR`, `HB`, `IN`, `INFRA`, `INT`, `WC`, `WF`); none is
`MF` or `TC`. MF-T1X recorded the gap and declined to fill it:

> *"town-cartography has no family preamble, and the immediate MF-T1M, MF-T1V and MF-T1S
> predecessors were promoted and landed in the same three-governance-file form. That measured
> local precedent controls MF-T1X. The absent family preamble is not a dispatch blocker and this
> packet does not invent a fourth governance file."*

⭐ **That precedent was right for one packet and is wrong for sixteen.** MF-T1X was a single
member; D3a is a multi-member, multi-train wave whose members share hazards, a census law, a
dormancy proof and a STOP set. `DESIGN_BUILD_EFFICIENCY.md` §4's whole argument applies exactly
here: one signed file, cited **by SHA-256**, so that *"drift between a family's members and their
shared law is structurally impossible rather than merely discouraged."*

**So this lane compiled one: `draft-MF-PREAMBLE.md`, WC-shape, §P0–§P11, volume =
`map-corpus/docs/GENERATION-SPEC.md`.** It is a DRAFT for the chair to sign; a lane may not
author governance (§299.5 struck exactly that), so it lands only by a chair act.

⛔⛔ **AND THE PREAMBLE'S MOST CONSEQUENTIAL LINE IS THE ONE IT CANNOT INVENT: THE FAMILY IS
UN-STAMPED.** `PACKET_STANDARD`'s differential member caps:

| the family's Fable-round stamp | engine train | prose/docs train |
|---|---:|---:|
| stamped | 8 | 10 |
| **un-stamped** | **4** | **4** |

> ⛔ *"The stamp is a precondition, never a default… An un-stamped family stays at four however
> light its waves look, and no compiler may read a light wave class as a stamp. A family's own
> preamble carries its recorded stamp status; that line, not this table, is where a compiler
> reads which column it is in."*

**town-cartography carries no §70.4 Fable-round stamp.** The compiled preamble records
`STAMP: NONE` and **the cap is 4**. This plan's train topology is built on 4, not 8. See
RAISED-1: the chair may stamp the family, which would re-plan §9 at 8 — but a compiler may not
assume it, and this one did not.

⚠ **The volume is on the LEDGER branch only** (`map-corpus/docs/GENERATION-SPEC.md`, 12,667 lines;
absent from `9d851fae`), and the worktree copy is **dirty** relative to ledger HEAD — established
by `git show HEAD:… | cmp`, never by a diff listing, because the shared index's staged
`map-corpus/` deletions poison diffs here:

```
stdin map-corpus/docs/GENERATION-SPEC.md differ: char 360486, line 4084
worktree blob aa613cb8… · ledger HEAD 7dcd70ab…
```

The preamble cites `aa613cb8…` — **the blob it actually read** — and says so. RAISED-3.

---

## §9 · ORDER, TRAINS AND THE VALIDATOR SEQUENCE

### 9.1 · Dependency order

```
MF-T2A ─┐                                                (guard; blocks nothing, protects all)
        └─ MF-T2B ── MF-T2C ── MF-T2D ─┬─ MF-T2H ── MF-T2J
                                       └─ MF-T2E ── MF-T2F ── MF-T2K ── MF-T2L
MF-T2G  (independent — a deletion inside a dormant module)
MF-T2M  ⛔ BLOCKED on MF-T2S
MF-T2N · MF-T2Q · MF-T2R  (engine-side; independent of every port member)
MF-T2P  ⛔ BLOCKED on MF-T2S
MF-T2S  ⛔ chair ruling (§6)
```

### 9.2 · Trains, at the un-stamped cap of four

| train | members | class | rationale |
|---|---|---|---|
| `mf-t2a` | MF-T2A · MF-T2B · MF-T2C · MF-T2D | engine | the foundation spine; A first so the guard precedes the pour |
| `mf-t2b` | MF-T2E · MF-T2F · MF-T2G · MF-T2H | engine | massing vocabulary + the two discharged obligations |
| `mf-t2c` | MF-T2J · MF-T2K · MF-T2L | engine | manifest + the remaining massing foundations |
| `mf-t2d` | MF-T2N · MF-T2Q · MF-T2R | engine | ⛔ **exposed separately — a soak is owed (§5.3)** |

**R1, the dual-family ready queue, binds and is not satisfiable inside this family.** The compile
pipeline must keep the executor queue's next TWO slots compiled and drawn from **at least two
distinct families**. Every train above is `town-cartography`. ⛔ **So each D3a train must be
queued against a non-map family's train** — the WF family is the live sibling — and
`premise-map.mjs queue-check` enforces it. Recorded here so the coordinator does not discover it
at dispatch.

⚠ **Interior reds must be named before they exist.** Every member above adds at least one test
file, and the census arm is an exact equality against a recorded constant, so **every member
forces an interior red by construction**. Each train plan names them with predicted figures; a
plan declaring zero interior reds for these trains has mis-declared.

### 9.3 · ⛔⛔ THE SHARED-CHANGE-PATH PROBLEM — the sharpest scheduling finding

A change path is reserved at **every non-terminal status, DRAFT included**. Two live packets
naming one path red `validate:packets` until one reaches a terminal status.

**Every D3a member names at least two shared paths:**

| shared path | why every member touches it |
|---|---|
| `tests/lint/sovereigntyLightingContract.walker.test.js` | the census tuple moves per member |
| `src/domain/townMap/fabric/index.js` | the barrel REGISTER row, per new leaf |

⛔ **Therefore no two D3a members may be promoted simultaneously**, and a four-member train
cannot be promoted as a block. The measured local precedent confirms it: the codex slice landed
its thirteen packets strictly one at a time — promote → land → terminalize — and
`validate:packets` read **0 READY** at every measurement.

**Two lawful shapes, and the plan recommends the second:**

- **(a) Serial single-member dispatch** — the codex precedent. Correct, and it pays the full gate
  and preflight **sixteen times**, which is exactly the cost model §28's train law exists to kill.
- **(b) ⭐ Staged promotion inside the train (`CR-HB2B-SPLITP`)** — members are promoted one at a
  time as the chain is built on the private ref, so shared paths are never reserved by two
  non-terminal packets at once, and the train still amortises **one** bare terminal gate across
  four members. `PACKET_STANDARD` names this exact cure: *"a staged-promotion pair — two trains
  split precisely so that shared change paths are never reserved by two non-terminal packets at
  once — occupies ONE slot."*

⛔ **Never the third shape**: editing the validator to permit the overlap. The standard forbids it
in as many words — *"The cure is `CR-HB2B-SPLITP` staged promotion, never a validator edit."*

⛔ **And the status sequence is EXECUTED before dispatch, not reasoned about.** Every intermediate
state the chain passes through, in order, simulated against a synthetic manifest — *"a plan that
only checks its final state cannot see that."*

---

## §10 · STANDING STOP CONDITIONS FOR EVERY D3a MEMBER

A D3a wave STOPS — reports, never repairs — on any of:

1. A live **seed** byte moves. (Artifact bytes moving inside MF-T2G's dormant module is that
   member's declared, expected result and is not this condition.)
2. A new consumer appears outside `src/domain/townMap/fabric/**`, or
   `tests/build/townMapLazy.test.js` reds — the dormancy boundary breached.
3. A source module's SHA-256 does not match the packet's recorded hash (§3.1).
4. `parked` moves off **364**, or a census figure moves by other than the member's predicted
   delta. ⚠ A delta **smaller** than the titles added is not arithmetic to accept — a parked file
   swallows its titles silently. Attribute by reverting one test file at a time.
5. A census/ratchet move without a `censusAuthorization` ref (§3.4).
6. Any arrangement-quantum rung is chosen, or any tuning constant of any kind is touched.
7. A `LAYER_PATTERNS`/`ARGUED_UNLAYERED` edit lands in a different commit from its leaf (§5.2).
8. A flag is declared without its first by-name gate read in the same commit.
9. A `spatialLedgers` row without a live `setSpatialLedger` writer.
10. Two members of one train reserve a shared `changeManifest` path at once (§9.3).
11. An interior red that was not NAMED in the train plan before it existed.
12. A `requiredSymbols` row naming a symbol the deliverable creates, at `READY`.
13. Any hard scope limit would be exceeded, or a ratchet, baseline, budget or ceiling would need
    raising.
14. A frontage/parcel equivalence claim — §310.1 states it is NOT BUILT and it is not D3a's.

---

## §11 · ⛔ RAISED — every judgment-dense call, for the chair at signing

| # | the call | this lane's recommendation |
|---|---|---|
| **1** | **The family preamble, and the un-stamped cap of 4.** `draft-MF-PREAMBLE.md` is compiled but a lane may not author governance (§299.5). And the cap is **4**, not 8, because no §70.4 stamp exists for `town-cartography`. | **Sign the preamble** as a chair act. **Rule the stamp explicitly** — grant it on the codex slice's and W3's executed evidence (which would re-plan §9 at 8 members and halve the terminal-gate count), or record `STAMP: NONE` and keep 4. ⛔ A compiler may not read a light wave class as a stamp, and this one did not. |
| **2** | **The §310.3(7) rename's target tree.** `clipHalfPlane` is sandbox-only; the sandbox is not in git, is the sealed W3 evidence, and retires. This plan lands the **guard** as the first act and lets it discharge the rename when the geometry ports (§7). | Adopt the split. If the literal order is preferred, the sandbox rename is a two-file edit that touches no member here. |
| **3** | **The volume's dirty blob.** `GENERATION-SPEC.md` differs from ledger HEAD at line 4084; the preamble cites the worktree blob `aa613cb8…`. | Confirm the worktree copy is the intended code of record, **or** name the ledger blob `7dcd70ab…` and the preamble re-stamps. Either way the citation must be a blob, not a path. |
| **4** | **The §306.4a members owe a SOAK (§5.3).** They change engine behaviour; §305.4's RS-5 fires at their exposure. That is a program cost, not a member cost. | **Expose `mf-t2d` separately from the port trains**, so the port's seal is not held by a 162-cell soak. Alternatively route the four to a successor wave — §306.4a's own words route them to *"D3a/A0–A5"*, so a successor is within the ruling. |
| **5** | **What "the port's seal" means in §304.4.** The full port is **14,230 effective lines / ≥36 members**; D3a as chartered is ~16. If the twin-life retirement fires at D3a's seal, the sandbox retires while ~9,900 lines of generator have not ported — and the program loses its geometry code of record. | Rule that **"the port's seal" is the PORT's completion, not D3a's**, and that D3a is the port's opening tranche. Record the remaining tranches as a named carry, so the retirement obligation is not silently deferred. |
| **6** | **§299.3c's operation-vocabulary merge (§6).** The merge crosses `src/domain/**` → `src/store/**`. This lane refused to compile it and stated two shapes. | **Shape B for D3a** (strip to a proposal; the door stays the store's), **Shape A docketed** for the tranche that lands a real executor. MF-T2M and MF-T2P stay **BLOCKED** until ruled. |
| **7** | **MF-T2G strips rather than conforms.** Building §288.2–§288.5's contract is D4 scope and would breach §290.4's stop. | **Strip.** It is the smaller act and it restores §288.8's stated dormancy. The DM/PUBLIC digest motion is declared and captured as a golden before the edit. |
| **8** | **`ANGLE_TABLE_SIZE` declared locally rather than porting `trigTable.js`** (270 eff, two 1024-entry tables, for one integer this tranche reads once). | **Declare locally with an equality pin**; the table ports with the geometry that reads it. |
| **9** | **The §306.4a producers predict `LAYER_PATTERNS` family homes, not `ARGUED_UNLAYERED` rows** (§5.2) — each owns a subject, and the roster's argued door is reserved for modules that own none. | Adopt. ⚠ The exact regexes are compiled per member against the live roster, never a `<noun>[A-Z]` prefix — a prefix silently widens a frozen family. |
| **10** | **Scheduling: R1 cannot be satisfied inside this family** (§9.2). Every D3a train is `town-cartography`. | Queue each D3a train against a **WF** train, and bind the queue block to the plan's own train via `premise-map.mjs queue-check`. |

---

## §12 · WHAT LANDING THIS PLAN UNBLOCKS, AND WHAT IT DOES NOT

**Unblocks:** MF-T2A dispatch immediately (it depends on nothing and protects everything);
MF-T2B on the same base; the shape of MF-T2C through MF-T2L fixed so each compiles when its
substrate lands; the §306.4a batch priced and its soak obligation surfaced before it is
discovered at an exposure.

**Does NOT unblock:** MF-T2M, MF-T2P and MF-T2S (chair ruling, §6); the generator tranches
(~9,900 effective lines); D3b activation; D4 light; the parity vertical slice; A0–A5; frontage /
parcel equivalence; the arrangement-quantum ladder (owner tuning signature); and the twin-life
retirement itself, which RAISED-5 asks the chair to place.
