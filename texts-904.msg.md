## SUBJECT
the L-UI-MAT landing lands at six cars — two of O-13's three product flags light as a declared, proven display-only shift, the third is retired because its reader was deleted in June, and the MAT dial's public-safe path is refused with measurement as an owner row

## BODY
Lane L-UI-MAT (Opus) landed one car over the §903 CAS: warEconomySurfacing and handbookVoice flip to true, proven display-only structurally (no engine module imports the flag modules; 36 importers, all render-time) and by execution (the 525-row golden-master aggregate unmoved, 0/525, a planted negative control convicting); the handbookVoice contract re-polarised with two vacuity guards. Car B (the MAT dial's public-safe path) refused on three grounds, chiefly a SQL migration under the gallery allowlist — owner-gated by nature.

The chair car (lane CHAIR-904, Opus) retired the mobileSingleChrome registry entry: six hits in four files and none a read; src/App.jsx lost all three read sites at 8bf493d05 (2026-06-22); FLAG_DEFAULTS and the descriptions 39 -> 38 paired; the eager core 11,528 -> 10,734 B; the docs corrected to the measured truth. Every premise re-derived; one brief mechanism contradicted (the soak census is the simulation-rules domain, disjoint from the UI registry — the conclusion held). Chair-verified in the dock.

The proof at 89ff7b03b (proof-904.sh, in the dock itself behind a quiet-window probe, two research runs alive): typecheck ratchet 173 at ceiling, exit 0; the whole suite with no filter — 2543 files, 32429 tests: 3 failed / 32315 passed / 111 skipped, and the three reds are EXACTLY the three banked known failures (voiceMechanics E2, the enforcement-claims meta-pin, the generator golden master 525/525 — the §898 row), classified by file against scripts/.test-ratchet-baseline.json's entries (proof-904.verdict GREEN-AT-BANK); no load timeout, no register arm, no edge-bundle arm this time. Registers, taken AFTER the proof to written predictions (run-registers-904.sh), every prediction held: lighting — the farmed probe and the register identical to the digit (files 2543 / parked 373 / credited 2170 / titles 23653 / suiteTitles 6333), the door re-took provenance as register car 1 21b95511f (plain walker 34/34); observed-shape — the plain read 1972 findings exactly matching the frozen inventory, zero drift lines, NO write, walker 44/44; mounts — 56 / 15 dark, the door's porcelain empty; prose-numerics — baseline 225 = live 225, 0 re-keyed; writer-reach — nothing stale (judged 6532 · LIT 560 · LIT-NAME 4646 · DARK 1326), --write moved frozenAtSha alone (a7ca033a6 → 21b95511f; cohort 1322 → 1322), register car 2 35638df4b; tuning — plain walker 75/75, no door.

Owner rows: rebuild or bury the single-chrome mobile nav (the entry is retired as bookkeeping, vetoable by revert; the feature survives only in two doc sentences); L-MAT's public-safe path waits on the SQL migration and the security boundary. Ratified, vetoable: the retirement anchor as the next chair car (negative-assertion baseline 1 -> 2). L-UI-MAT's R3 closed: the golden-master red is the banked §898 row. R6 owed: a visibility arm on the two lit surfaces before the walk. Research runs under the owner's 17:40 cap (two sweeps + one implementation lane); next: the lighting plan's chair tail, the sweeps' dry-checks, S12, the taste sample, the walk.

## ENROLS
R67 (L-UI-MAT) — enrolled; R1 ruled and landed at §904, R3 closed, R2 an owner row, R6 owed. R68 (CHAIR-904) — enrolled; R1 verified and R2 answered at §904, R3 conceded, R4 the next chair car. The register, totals and capsule cars are Fable-validated acts and enrol nothing.
