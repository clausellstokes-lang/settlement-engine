OSR rung-18 landing (register car 2): the writer-reach register re-takes at the tip — the identity set and every figure UNCHANGED; frozenAtSha and the detector digest move, because the observed-shape register is one of this walker's detector inputs

The prediction said "--write must change nothing" and was wrong on exactly two lines: frozenAtSha (cea826821 -> 21cf7dff0) and
detectorDigest, which hashes the walker's detector inputs and therefore moves when the observed-shape register changes — which
the rung-18 genesis did (schema 17 -> 18; corpusMeta.simulationFlagsLit 80 -> 81 on the OSR side). No identity row changed (the
diff is 2 lines each way); cohort 1322 -> 1322, 0 stale folded out, 0 rows newly lit; the dry run raised no flag and the plain
re-run is the receipt (WR_PLAIN_EXIT=0). Both registers now carry simulationFlagsLit 81, which is what retires the two banked
ratchet rows at this landing's ratchet.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
