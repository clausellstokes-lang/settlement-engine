PROSE landing fix #3: the chip-title separator takes the cure after all — both sides of the V-26a contract move together, and the per-file voice arm is green again

Fix #2 restored `<Town> — <Section>` in stateSlicers.js to satisfy the V-26a pin, AFTER the voice Tier-2
refreeze had been taken — so the per-file arm read that file at em:1 against a baseline of em:0, a rise the
shrink-only door cannot bank. The pin reads the title's SHAPE and nothing parses the dash (ids are
@member:-namespaced), so the honest cut is the cure on both sides in one car: the composer emits
`<Town>: <Section>` and the pin reads it. Registers are the LAST cars; this car precedes the re-taken totals.
Proof under the mutex: interviewCampaignScope 13/13; voiceMechanics — only the two banked JSX arms red.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
