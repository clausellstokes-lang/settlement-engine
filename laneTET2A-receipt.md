# lane TE-T2A — MF-T2A full packet lifecycle receipt (D3a Track 0)

> **THE TIP FOR THE CHAIR'S CAS:**
> ## `7567a2c7eeadb35c947f3f5b9c190f1ae70ac2dd`
> Detached HEAD in
> `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/6298872d-53af-4c8a-99e5-139bfc5bfc1f/scratchpad/t2a-tree`,
> **four** commits on top of `claude/composite-r4 @ f20b9faa`. Working tree CLEAN. **The worktree is
> LEFT IN PLACE for chair verification.** A second worktree,
> `…/scratchpad/t2a-baseproof`, holds the pristine base `f20b9faa` for failure-identity comparison
> and is also left in place (its `node_modules` is a symlink into the lane's own tree).

## OUTCOME, FIRST

**MF-T2A is built, landed and terminalized, and THE TERMINAL IS FULLY GREEN — all seventeen gate
steps plus `smoke:boot`, every one exit 0.** The §310.3(7) source scan exists, is green at zero, and
is proven REMOVABLE by execution rather than by argument. The census arithmetic closed on the
prediction in all five figures, zero production lines moved, and both typecheckers sit at their
exact floors.

⭐⭐ **This is the first terminal in this sequence that needed no inherited-red argument.** TE-WF1B's
terminal was red at step 15 and wrote a one-line cure out for the chair; the chair executed it, and
this run confirms it holds — that arm did not fire, and the MF-PREAMBLE landing did not reintroduce
it.

- ⭐ **The guard convicts, and the conviction is attributable.** A real second declaration planted
  into the live fabric directory reds **THE LAW arm alone** — 1 failed / 4 passed — and the message
  names the identifier and BOTH declaring files. Restored digest-exact; the walker goes green again.
  That is the sweep's own mutated-red / clean-green attribution control, executed by hand because a
  build lane may not run the sweep on this tree (preamble §P3b).
- ⭐ **Two of the compiled packet's claims were REFUTED by execution before a single pin was
  written**, and both are recorded as vetoable judgment calls rather than quietly cured — §6 below.
  Run-the-fixture-first is what found them; reading would not have.
- ⭐ **The census prediction held exactly**: `+1 / +0 / +1 / +5 / +1`, `parked` unmoved at 364, and
  the sequenced walker reached and cleared `suiteTitles` (33/33).
- ⭐ **The live failing set EQUALS the frozen banked set, member for member** — 11 known failures of
  28,651 tests against a ceiling of 11. The named flake did not fire even under load average 142,
  and nothing was banked. §12.1, §12.2.
- ⚠ **One preamble instruction is not executable as written**, found by running it rather than by
  reading it: the dormancy fence SKIPS on a fresh worktree and reports exit 0 on zero executed
  tests. This lane refused to report that as a green, built `dist/`, and ran it for real — **3
  passed under `VERIFY_DIST=1`**. §7, RAISED-2.
- ⛔ **The family preamble contradicts itself about its own status**, and every MF member inherits
  it: it still forbids the SHA-256 citation that this packet and the brief both require. One-line
  chair fix, wanted before MF-T2B cites the same hash. **RAISED-1.**

## 1 · WHAT LANDED, COMMIT BY COMMIT

| sha | what |
|---|---|
| `5475110c` | The packet lands at **DRAFT** at `docs/implementation/packets/town-cartography/MF-T2A.md`, its `PACKET_MANIFEST.json` row spliced as a scoped text APPEND (**140 insertions, 0 deletions**) and its `INDEX.md` row inserted above MF-T1X |
| `ba6b0c68` | **DRAFT → READY** after re-executing the whole §4 preflight at that tip |
| `37fb6916` | **The implementation** — the walker, the sweep plant + its guard row, the coverage row, the census re-record |
| **`7567a2c7`** | **READY → LANDED**, the four post-cure symbols join `requiredSymbols`, and §12/§13 (the executed receipt and the landing record) are written into the packet. **This is the tip.** |

Diffstat against the base: **7 files, 830 insertions, 2 deletions** — one new test file, two
`scripts/` files, one census walker, and three packet-system files (`MF-T2A.md`,
`PACKET_MANIFEST.json`, `INDEX.md`). **Zero `src/` bytes**; `git diff --stat f20b9faa HEAD -- src/`
is empty. No `package.json` / `package-lock.json` motion.

**Handwritten files against the packet's budget of 5:** the walker, `mutation-sweep.sh`,
`mutation-coverage-manifest.json`, the census walker, and the packet itself. `INDEX.md` and
`PACKET_MANIFEST.json` are the coordinator's own surfaces rather than member manifest rows —
RAISED-4.

## 2 · PREFLIGHT, EVERY FIGURE THIS LANE'S OWN

| check | result |
|---|---|
| `git rev-parse HEAD` at start | `f20b9faa828469fed6f3fccae19f7c08508a5213` — **CONFIRMED** |
| `MF-PREAMBLE.md` SHA-256 | `7a39a28ee7a54799489601a9b7ea9333b33ce83158c7e8c4aec878c89a204de8` — matches the brief's binding citation exactly; the packet header cites this value |
| duplicate-export baseline | `18 files · 94 distinct exported names · 0 duplicated · 0 offending pairs` — **re-executed at `f20b9faa` AND again at `5475110c`** before promotion; identical both times |
| duplicate-export baseline, PACKET's stricter regex | the compile measured with a leading-whitespace-tolerant regex; re-measured with the packet's own line-anchored one: **same 18 / 94 / 0**. The stricter regex cannot exceed the looser one, so the direction was safe, but it was measured rather than assumed |
| `git grep clipHalfPlane -- src tests scripts` | exit **1** (absent). Whole-tree grep also exit **1** — the identifier does not exist anywhere in this repository |
| `BASE_STATE.json` admissibility | **NOT CITABLE**, per its own `consumptionLaw` and preamble §P8: stamped `b8946403`, and this base is not that sha. Every figure here was measured from scratch; nothing was inherited from the capsule or from the compile |
| `validate:packets` at the base | `valid: 130 packets (0 READY)`, TRUE_EXIT=**0** |
| path-reservation collision check | **zero** non-terminal packets in the whole manifest (129 LANDED + 1 SUPERSEDED), so no other packet reserved any of this member's five change paths at any point |
| `npm ci` | TRUE_EXIT=**0**, captured in-shell |

## 3 · THE FIXTURES, RUN AND PRINTED BEFORE ANY PIN WAS WRITTEN

Executed against the live fabric directory and against every synthetic fixture the acceptance
matrix specifies, before the walker existed (`laneTET2A-probe.log`):

```
=== A1/A2 · LIVE ===        files: 18 · distinct names: 94 · duplicates: []
=== A3 · COUNTERFORCE ===   [{"name":"sharedName","files":["alpha.js","beta.js"]}]
=== A4 · RE-EXPORT / STAR / DEFAULT ===   declarations: []   duplicates: []
=== A4b · TEMPLATE LITERAL AND COMMENT ===
indented-in-template    -> []
column-zero-in-template -> ["fromTemplate"]
line-comment            -> []
block-comment           -> ["fromBlockComment"]
=== A5 · clipHalfPlane ===  [{"name":"clipHalfPlane","files":["fabricGeometry.js","groundLaw.js"]}]
```

**This probe is what refuted the packet's §8 template-literal claim** — see §6. It also enumerated
which fabric files could host a lawful plant, which refuted the second claim.

## 4 · PER-ACCEPTANCE-CASE RESULT

All five pass. `tests/lint/townMapFabricSingleDeclaration.walker.test.js`: **5 passed (5)**,
TRUE_EXIT=**0**.

| case | what it proves | result |
|---|---|---|
| A1 | THE LAW: zero duplicated declarations across the live fabric directory, asserted as a victory assertion with no baseline, allow-list or exemption door | **PASS** |
| A2 | `guard-the-guard`, asserted FIRST: ≥18 files and ≥94 distinct names, so an emptied directory, a rotted scan root or a silently broken regex reds here instead of letting A1 pass on nothing | **PASS** |
| A3 | Counterforce: a synthetic two-file duplicate is convicted by name with BOTH files listed | **PASS** |
| A4 | The scan's boundary in BOTH directions — the four lawful non-declarations contribute nothing, and the two over-convicting spellings are pinned as positives | **PASS** (rebuilt to what executes — §6) |
| A5 | The named §310.3(7) regression: two `clipHalfPlane` declarations with opposite half-plane conventions, as a FIXTURE (the sandbox is not in this tree and must not be reachable from a test) | **PASS** |

**Focused battery:** this walker · `mutationCoverageManifest` · `sovereigntyLightingContract` ·
`negativeAssertionAnchor` · `townMapMassingSilhouette` → **5 files passed, 59 tests passed**,
TRUE_EXIT=**0**.

## 5 · THE MUTANT — PLANTED, CONVICTED, RESTORED BY DIGEST

The packet's §11 makes it a STOP if the plant cannot red the walker, so the plant was executed by
hand on the live tree rather than inferred from the sweep script.

| | |
|---|---|
| label | `town-map/fabric duplicate export declaration` (sweep entry **28a**) |
| host | `src/domain/townMap/fabric/dcelEmbedding.js` |
| plant | a second `export const CURRENT_MAP_TRADITION_ID` declaration (`perl -0pi -e`, anchored on `export function derivePlanarDcelEmbedding`) |
| pre-plant digest | `2612d04d225f64d65297f9e9259e9c32` |
| planted digest | `7ee2c61a1d2d98c4a879d5b24cf78a84` |
| planted `node --check` | TRUE_EXIT=**0** — the plant PARSES, which is the whole point of J-TET2A-1 |
| planted walker | **1 failed \| 4 passed (5)**, TRUE_EXIT=**1**, failing arm = `THE LAW` **alone** |
| message | `CURRENT_MAP_TRADITION_ID  <-  dcelEmbedding.js, foundation.js`, plus the TO-COMPLY instruction |
| restore | byte copy, **never** the `git checkout --` family the shared-tree protocol forbids |
| restored digest | `2612d04d225f64d65297f9e9259e9c32` · `cmp` **identical** · `git diff` over the fabric directory **empty** |
| clean re-run | **5 passed**, TRUE_EXIT=**0** |

⭐ **The conviction is attributable, not blanket.** Exactly one arm fell and the other four —
including the `guard-the-guard` arm — stayed green, so the plant proves the LAW's removing power
specifically rather than proving the file can be broken.

## 6 · TWO COMPILED CLAIMS REFUTED BY EXECUTION — both recorded, neither smuggled

### J-TET2A-1 · the sweep's plant host had to move, `dcel.js` → `dcelEmbedding.js`

The compiled draft named `src/domain/townMap/fabric/dcel.js` as the plant host. **`dcel.js` already
imports `CURRENT_MAP_TRADITION_ID`** (from `foundation.js`), so a planted second declaration there is
a redeclaration in one module scope:

```
SyntaxError: Identifier 'CURRENT_MAP_TRADITION_ID' has already been declared
node --check dcel.js + plant          TRUE_EXIT=1
node --check dcelEmbedding.js + plant TRUE_EXIT=0
```

The walker would still have reddened — it reads source text and imports nothing — but it would have
been proven against a **parse error** instead of against the defect class under test: two
syntactically valid same-named implementations. `dcelEmbedding.js` neither declares nor imports the
name. **Both exits were executed before the choice was made.** Vetoable.

### J-TET2A-2 · A4 pins the scan's over-convict direction instead of an immunity it does not have

The compiled draft's §8 asserted that an `export` inside a template literal or a comment "cannot
fire" because the regex is line-anchored. Executed, that is true only of the **indented** spellings.
A **column-zero** `export const` inside a template literal or inside a block comment **does** fire.

The cure is not to widen the walker into a parser — the packet's non-goals forbid that, and a
hand-rolled comment stripper is itself foolable. The cure is to pin what is true and say which
direction it errs in: **an extra text match can only ADD an identifier to the map and never remove
one, so the scan can over-convict loudly and can never under-convict silently.** A false red is a
stop a human reads; a false green would be a duplicate that landed. A4 now asserts both directions
as positives, so neither can be discovered later as a surprise. Vetoable.

## 7 · ⚠ THE DORMANCY FENCE IS NOT ASSERTABLE BEFORE THE BUILD — AND IT PASSES AFTER IT

Preamble §P2.2 instructs that **every** MF member runs `tests/build/townMapLazy.test.js` focusedly.
Run at this tip on a fresh worktree, before the build, it reports:

```
Test Files  1 skipped (1)
     Tests  3 skipped (3)      TRUE_EXIT=0
```

**A skip, never a pass.** The file is `describe.runIf(distExists)` and a fresh worktree has no
`dist/`, so the whole suite is gated off; a second arm is additionally
`it.skipIf(!process.env.VERIFY_DIST)`. **`TRUE_EXIT=0` on a fully-skipped suite is exactly the shape
the estate's "a green that never ran" law names**, so this lane refused to report it as a green.

**Then the gate built `dist/`, and the fence was run again for real:**

```
$ VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js
Test Files  1 passed (1)
     Tests  3 passed (3)       TRUE_EXIT=0
```

⭐ **The dormancy boundary is EXECUTED, not argued.** All three arms ran, including the anti-vacuity
arm that only runs under `VERIFY_DIST` (it proves the town-map fingerprint DOES exist somewhere in
`dist` — a lazy chunk — so the "not in the entry closure" assertion is not passing on an absent
fingerprint). Independently, this member cannot move the entry closure at all: it changes zero
`src/` bytes.

⛔ **This matters much more for the next MF member than for this one.** MF-T2B onward add real
production leaves, and a barrel hop that drags a new transitive import into the entry closure is the
precise way a dormant port stops being dormant. Such a member gets its answer **only after a build** —
a focused pre-build run of the fence tells it nothing. **RAISED-2.**

## 8 · OBLIGATIONS RE-VERIFIED AT THE TIP

| obligation | verdict |
|---|---|
| test census | **INCURRED, PAID** — §9 |
| §102.2 / §P3b mutation coverage | **INCURRED, PAID** — one `tests/lint/**` file added, one `kind: 'mutation'` row with a live sweep label, plus the `MUTATED_FILES` guard row the plant obliges. `mutationCoverageManifest.test.js` **8 passed** |
| `negativeAssertionAnchor` | **INCURRED, ceiling zero, DISCHARGED AT ZERO SITES** — the walker names none of the three scanned negative matchers, in code or in prose (the walker counts a matcher named inside a comment as a site). Walker **9 passed** |
| entry-closure fence | **INCURRED, DISCHARGED POST-BUILD** — 3 passed under `VERIFY_DIST=1`; §7 |
| the single-declaration law itself | **MINTED HERE**, green at zero, and proven removable |
| coupling / layer census | **NOT INCURRED** — scope is `worldPulse` + `spatial` only; no `townMap` path is in it (preamble §P4). Green in the gate |
| size ratchet / hot files | **NOT INCURRED** — no `townMap` path carries a `.size-baseline.json` entry, no manifest path is on the hot-file list, and zero production lines moved |
| flag mint (§49/§50/§148) | **NOT INCURRED** — no flag; a guard is never flag-gated |
| `spatialLedgers` | **NOT INCURRED** — no writer, no row |
| §104.4 edge-shared closures | **NOT INCURRED** — `validate:edge` TRUE_EXIT=0 at the terminal, confirmed by the gate rather than by reading the metas |
| declared shift / goldens | **NOT INCURRED** — no golden, plate, corpus byte or seed byte is touched; the member has no runtime surface at all |
| tuning / chair value signature | **NOT INCURRED** — the member authors no number; no quantum rung, band or constant is touched |
| stamped version strings | **NOT INCURRED** — `FABRIC_COORDINATE_ABI` and every other stamped string unmoved |
| `docs/**.md` naked-claim debt | **INCURRED, PAID** — every docs file this lane wrote was CLAIM_RE pre-scanned to **0 hits** before the write and re-scanned after. The scanner was proved non-vacuous against a planted control, which also reproduced the recorded `0 problems` / `30 problems` trap |
| `package.json` / `package-lock.json` | **UNTOUCHED** — no dependency bump, so no schema-mint trigger |

## 9 · CENSUS ARITHMETIC — IT CLOSES EXACTLY, AND ON THE PREDICTION

**`2,484 / 364 / 2,120 / 20,606 / 5,767` → `2,485 / 364 / 2,121 / 20,611 / 5,768`**

| figure | before | after | delta | why |
|---|---:|---:|---:|---|
| files | 2484 | 2485 | **+1** | one new `tests/lint/**` walker |
| parked | 364 | 364 | **+0** | no parked-file rule, parser door or existing classification moved |
| credited | 2120 | 2121 | **+1** | the new file is CREDITED, not parked — one literal `describe`, straight-line `test()` calls, string-literal titles, no `.each`, no `runIf`, no nesting, no loop-registered case |
| titles | 20606 | 20611 | **+5** | the five acceptance cases, one `test()` each |
| suiteTitles | 5767 | 5768 | **+1** | exactly one new `describe` |

**The interior red fired first, exactly as the packet named it:** `expected 2485 to be 2484` on
`files`, TRUE_EXIT=1, `1 failed | 32 passed (33)`. Because the census is SEQUENCED, that red is
itself the executed proof that `parked`, `credited`, `titles` and `suiteTitles` had **not yet been
read**. After the whole tuple was re-recorded **together** — never one figure at a time — the walker
reached and cleared `suiteTitles`: **33 passed (33)**, TRUE_EXIT=**0**.

⚠ **The parked-file swallow was checked, not assumed:** the titles delta EQUALS the five titles
added, so no title was swallowed by a parked classification. And `credited` moved, which is the
figure that lies when a file is generated, looped or parked.

**Authorization:** ODQ **§312 / §314** (this dispatch), under the **§310.4** wave charter and
§299.4's binding-forward rule. Named in the packet header's `censusAuthorization` block, in the
packet's §7 change-manifest row, and in the re-record comment block itself.

## 10 · THE PACKET LIFECYCLE, EXECUTED AT EVERY TRANSITION

`validate:packets` ran at every status, never only at the endpoints — the standard's status-sequence
simulation law applied to a real chain rather than a synthetic one:

| transition | result | TRUE_EXIT |
|---|---|---:|
| base | `valid: 130 packets (0 READY)` | 0 |
| → **DRAFT** | `valid: 131 packets (0 READY)` | 0 |
| → **READY** | `valid: 131 packets (1 READY)` | 0 |
| → **LANDED** | `valid: 131 packets (0 READY)` | 0 |

**Shared change paths were never reserved twice.** The manifest carried zero non-terminal packets
before this member, so the staged-promotion law (`CR-HB2B-SPLITP`, plan §9.3, preamble §P7.11) had
nothing to bite. **It will bite the moment MF-T2B is compiled**, because MF-T2B shares
`sovereigntyLightingContract.walker.test.js` with this member and with every later one. **MF-T2A is
now TERMINAL, so it reserves nothing and MF-T2B is free to promote.**

**The `requiredSymbols` discipline was honoured in both directions.** At DRAFT and READY the row set
named only symbols already present in the live tree; the four symbols the deliverable CREATES — the
walker's `describe` title, the sweep's `check_caught` label, its `MUTATED_FILES` guard row, and the
coverage manifest's matching label — were added **at the flip to LANDED**, where they make every
later deletion gate-detectable. **No figure was placed in `requiredSymbols`**, which is the recorded
trap that turns a LANDED packet into a trap for every later train.

## 11 · EVERY MANIFEST-ADJACENT EDIT WAS A PURE INSERT

| file | diffstat | note |
|---|---|---|
| `docs/implementation/PACKET_MANIFEST.json` (DRAFT) | **140 insertions, 0 deletions** | a scoped text APPEND onto the end of the `packets` array; never re-serialised |
| `scripts/mutation-coverage-manifest.json` | **4 insertions, 0 deletions** | a surgical insert beside its sibling row; `uncoveredBaseline` untouched and absent from the diff |
| `scripts/mutation-sweep.sh` | **13 insertions, 0 deletions** | entry 28a AND the `MUTATED_FILES` row for its host |
| `tests/lint/sovereigntyLightingContract.walker.test.js` | **13 insertions, 1 deletion** | the single deletion is the tuple line itself — the re-record, not a re-serialisation |

⚠ **The anchor-assert caught a real mis-transcription.** The first attempt to insert the coverage row
anchored on a label spelled `massing silhouette totality…`; the live label is
`massing/silhouette totality…`. The script asserted its anchor was uniquely locatable and **refused
to write** rather than inserting in the wrong place or silently doing nothing. Recorded because
"assert the anchor, never trust the anchor" is what turned a would-be silent defect into a stop.

## 12 · THE GATE — ALL SEVENTEEN STEPS PLUS `smoke:boot`, EVERY ONE GREEN

Run **BARE** in the lane's own worktree, **never** through `gate-mutex` (`test:ratchet` re-acquires
and self-deadlocks; exit 3 would be the mutex giving up, not a red). Each of the seventeen steps ran
**separately** with its own in-shell `TRUE_EXIT` captured into its own self-named log, so a red step
could not black out the steps after it the way the real `&&` chain would.

Run at the tip **`7567a2c7`**, started `17:45:54Z`, finished `18:09:08Z`.

| # | step | TRUE_EXIT | note |
|---:|---|---:|---|
| 1 | `validate:hazard-registry` | **0** | |
| 2 | `validate:premortem` | **0** | |
| 3 | `validate:packets` | **0** | `valid: 131 packets (0 READY)` |
| 4 | `validate:data` | **0** | |
| 5 | `validate:custom-content-manifest` | **0** | |
| 6 | `validate:migration-head` | **0** | |
| 7 | `validate:edge` | **0** | §104.4 NOT INCURRED, confirmed by the gate |
| 8 | `validate:map` | **0** | |
| 9 | `validate:tuning-bands` | **0** | the member authors no number; no band moves |
| 10 | `validate:foundry-module` | **0** | |
| 11 | `validate:mcp-server` | **0** | |
| 12 | `typecheck:ratchet` | **0** | `tsconfig.full.json` — **173 errors, ceiling 173**, exactly at floor |
| 13 | `typecheck:domain:strict` | **0** | `tsconfig.domain-strict.json` — **1134 errors, ceiling 1134**, exactly at floor |
| 14 | `lint` | **0** | 29 problems, **0 errors**, 29 warnings |
| 15 | `test:ratchet` | **0** | **11 known failures of 28,651 tests, ceiling 11** — exactly at the frozen ceiling |
| 16 | `build` | **0** | 314 static route documents prerendered |
| 17 | `verify:dist` | **0** | `STRICT DIST OK — 51 file(s), 409 test(s), zero failed/non-run/uncollected/missing/extra/duplicate` |
| — | **`smoke:boot`** (outside the chain) | **0** | `524/524 chunks initialised`, shell mounted with 31,706 B of markup, **PASS — the built bundle boots** |

⭐⭐ **THE TERMINAL IS FULLY GREEN.** This is a change from the immediately preceding lane: TE-WF1B's
terminal was red at step 15 on `enforcement-claims :: the banked naked-claim debt is frozen PER
CLAIM`, and it wrote the one-line cure out for the chair rather than editing chair-signed law. **The
chair executed that cure** (`f9bd533f`, the WF-PREAMBLE paraphrase), and it holds: that arm did not
fire here. **The MF-PREAMBLE landing at `f20b9faa` did not reintroduce it** — its commit message's
"CLAIM_RE pre-scanned clean" is corroborated by this green.

### 12.1 · The base-versus-wave failure identity diff — ZERO new failures

**No separate pristine-base run was needed, and this is a statement about what the ratchet IS, not a
shortcut.** `scripts/.test-ratchet-baseline.json` is a PER-TEST census of named, attributed failures,
frozen at `4deb4f026644cba500b0efc1e051fdea2ff96041`, and `check-test-ratchet.mjs` fails closed on
any failing test ABSENT from it. The gate reported **11 known failures against a banked set of
exactly 11 entries** — so the live failing set EQUALS the frozen set, member for member. A wave that
introduced any new deterministic failure could not have produced that number.

The 11 banked entries, all pre-existing and none this lane's: four `voiceMechanics` ratchet arms, one
`enforcement-claims` `@enforced-by` arm, one `metronomeCooldownLint` arm, one `clampPrimitiveBaseline`
arm, three `warCostKindPools` phrased-kind arms, one `warRulingKindPools` arm.

### 12.2 · The known flake — FOUR-LEG CLASSIFICATION, and it did NOT fire

`tests/components/npcAuthoringScope.test.jsx` is the program's named flake and is deliberately **not**
banked (the CG-1 precedent). Classification for this run:

1. **It is absent from the banked set** — confirmed by reading `entries`, not assumed.
2. **It did not appear.** Had it failed it would have been a twelfth failure against a ceiling of 11
   and `test:ratchet` would have exited non-zero. It exited **0**.
3. **The machine was under genuine load** — `uptime` during step 15 read load averages
   `142.51 / 102.51 / 62.22`, the RS-5 soak's load as briefed. The flake had every opportunity and
   did not take it.
4. **It is unreachable by this member regardless.** This packet touches no component, no `.jsx`, no
   dossier surface and no read model; its change manifest names zero component paths.

**Nothing was banked, and nothing needed to be.**

## 13 · RAISED

1. ⛔⛔ **THE MF-PREAMBLE CONTRADICTS ITSELF ABOUT ITS OWN STATUS, AND EVERY MF MEMBER INHERITS IT.**
   At `f20b9faa` the file's header line 18 still reads:
   `- **Status:** `DRAFT` — awaiting chair signature. **No packet may cite it by SHA-256 until it lands.**`
   But the file HAS landed (that commit is the landing), the commit message says chair-signed, and
   the file's own STAMP LINE records the §312.2b grant as executed in the past tense. **MF-T2A cites
   it by SHA-256 — which that sentence forbids.** This lane followed the brief and the STAMP LINE
   (both of which treat the preamble as landed law) and did not edit chair-signed governance to
   resolve it. **It is a one-line chair fix** — set the status to a landed value and drop the
   citation prohibition — and it should happen before MF-T2B cites the same hash, because every
   later member reproduces the contradiction. The SHA-256 itself is correct and verified.
2. ⚠ **`tests/build/townMapLazy.test.js` CANNOT DISCHARGE §P2.2 FOCUSEDLY.** It is
   `describe.runIf(distExists)`, so on a fresh worktree it SKIPS and reports `TRUE_EXIT=0` on zero
   executed tests. The preamble tells every MF member to run it focusedly; done literally, that
   instruction yields a green that never ran. **The real discharge is post-build**, which this lane
   executed (3 passed under `VERIFY_DIST=1`). Worth a one-line preamble amendment so MF-T2B — the
   first member that actually adds production leaves, and therefore the first that could really
   breach the entry closure — does not bank a skip as a pass.
3. **The compiled drafts still carry the two claims execution refuted (§6).** `draft-MF-T2A.md` §7
   names `dcel.js` as the plant host and §8 asserts template-literal immunity; `draft-D3A-PLAN.md`
   §4's MF-T2A row inherits the same shape. The LANDED packet is correct, but if later members are
   compiled from those drafts the errors can propagate — particularly the "pick any file for the
   plant" habit, which is wrong for any identifier the host already imports. Worth a correction pass
   on the drafts, or an explicit note that the landed packet supersedes them.
4. **This member's change manifest does not name `INDEX.md` or `PACKET_MANIFEST.json`**, following
   the landed WF-1B precedent exactly (its manifest names neither, though TE-WF1B edited both) and
   `PACKET_STANDARD`'s rule that updating `INDEX.md` is a coordinator act. `validate:packets` is
   green at every status either way. Recorded so the chair can rule differently if they would rather
   see the packet-system surfaces named in each member's manifest.
5. **The packet's own §12 says the dormancy fence "executes for real at the terminal" rather than
   quoting the post-build result**, because the executed result only existed after the gate had
   already sealed the tip. Amending it would require re-running the full gate to keep the tip
   gate-proven, and **this lane judged a proven-green terminal worth more than a prose improvement**
   — disturbing it invites a flake on a load-142 machine for no structural gain. The executed figure
   lives in §7 above. Chair may direct the amendment plus a re-gate if they prefer it in the packet.
6. **A foreign stash sits in the shared repository:** `stash@{0}: On analytics-intelligence-layer:
   generation-tuning fixes`. It is not this lane's and was not touched. `lint-staged` created and
   correctly cleaned up its own backup stash during the implementation commit; this one is
   pre-existing. Noted only so it is not mistaken for wave debris.
7. ⭐ **The stale `index.lock` TE-WF1B raised is GONE.** `/Users/cstokes/Desktop/settlement-engine/.git/index.lock`
   no longer exists. That RAISED item is discharged.

## 14 · WHAT THE CHAIR SHOULD DO

1. **CAS `claude/composite-r4` to `7567a2c7eeadb35c947f3f5b9c190f1ae70ac2dd`.** The tree is clean,
   the packet is LANDED and validating, all seventeen gate steps plus `smoke:boot` are green, and the
   live failing set equals the frozen banked set exactly. **No inherited-red argument is needed for
   this terminal** — it is simply green.
2. **Fix the MF-PREAMBLE's status line** (RAISED-1) before MF-T2B cites the same hash. One line, in
   chair-signed law, and it unblocks the citation contract for the whole family.
3. **Optionally amend the preamble's §P2.2 dormancy instruction** (RAISED-2) so MF-T2B does not bank
   a skip as a pass — this is the member where it starts to matter.
4. **MF-T2B is free to promote.** MF-T2A is terminal, so it reserves none of the three shared D3a
   change paths. The staged-promotion law binds from MF-T2B onward, one non-terminal member at a
   time.
