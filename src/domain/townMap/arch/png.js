/**
 * Compatibility export for the original architecture-spike import path.
 *
 * The deterministic encoder is a general kernel primitive now that both the
 * dark architecture laboratory and the production town-scene exporter use it.
 */
export { encodePng } from '../../../kernel/deterministicPng.js';
