/**
 * domain/certification/subsystemRowsSeat.js — THE W-SEAT FAMILY'S CERTIFICATION ROWS.
 *
 * A NINTH ROW LEAF, AND IT EXISTS FOR A MEASURED STRUCTURAL REASON RATHER THAN TASTE.
 * `subsystemRowsVirtual.js` sat at 788 effective lines against its layer's 800-line
 * `max-lines` ceiling — the previous flag mint (W-COIN-1a's `treasuryEnabled`) took it to
 * within twelve lines of the wall. A certification row is not small by accident: the
 * contract asserts `aliveness.other` past 400 characters and at least one falsifiable
 * invariant, so an honest row costs ~35 effective lines and the NEXT mint after treasury
 * was always going to red the ceiling. Compressing a row to fit would have landed the file
 * at exactly 800/800 and handed the wall to whichever lane touched it next.
 *
 * The W-SEAT program alone charters THREE virtual keys (the foreign seat here; the domestic
 * §736 upheaval scaling with SEAT-2b; the irregular-force physics with SEAT-7), so this is a
 * habitat problem rather than a one-row problem, and the estate had already solved it once:
 * the certification registry is ALREADY lane-split eight ways (baseline / growth / people /
 * place / regen / virtual / war / waves). This leaf is the ninth cut of that same seam.
 *
 * ⛔ IT DELIBERATELY DOES NOT JOIN `subsystemCertification.js`'s import list. The rows spread
 * back into `VIRTUAL_SUBSYSTEM_ROWS`, which stays the ONE export every consumer, walker and
 * bijection already reads — so the composition, the totality audit and the ordered
 * rule-name contract are untouched and no other lane's surface moves. The whole cost to the
 * file this relieves is one import and one spread.
 */

/** @typedef {import('./subsystemCertification.js').SubsystemRow} SubsystemRow */

/**
 * The W-SEAT family's authored rows (the sibling row leaves' idiom verbatim).
 * @type {ReadonlyArray<SubsystemRow>}
 */
export const SEAT_SUBSYSTEM_ROWS = Object.freeze([
  Object.freeze({
    rule: 'foreignSeatEnabled',
    title: 'The foreign seat (the occupier or overlord who looms over the ruler)',
    module: 'src/domain/rulingPowerSeat.js,src/domain/worldPulse/stressorGates.js,src/domain/worldPulse/warSeatBooks.js,src/domain/worldPulse/foreignPrimacy.js,src/domain/worldPulse/seatIntervention.js,src/domain/worldPulse/occupation.js,src/domain/worldPulse/anticipatedReactions.js',
    aliveness: Object.freeze({
      // DELIBERATELY EMPTY: SEAT-1 mints no `candidateType` literal. The seat is a pure
      // read and the one lit behaviour is a GATE refusing a birth, which is the absence of
      // a candidate rather than a new one.
      eventTypes: Object.freeze([]),
      // DELIBERATELY EMPTY, and a COMMITMENT rather than an omission: SEAT-1 mints ZERO
      // news kinds. The news bill is priced by name into SEAT-2's charter (volume D8 — the
      // seat-override, posture-change, seat-backed-challenge, reaction-held and liberation
      // kinds, each paying the full V6-B3 authoring/herald/pool bill). Declaring a mover
      // family here would grade this row ALIVE off another lane's beat, the recorded
      // moverFamily hazard.
      moverFamilies: Object.freeze([]),
      // DELIBERATELY EMPTY, and this is the row's honest weakness: the seat OWNS NO STATE
      // AT ALL. It is derived per call from three ledgers other subsystems own
      // (`worldState.occupations`, `worldState.relationshipStates`,
      // `spatialLedgers.treaties`), on the hegemonyRead template — "A PATTERN, NEVER AN
      // ENTITY … ZERO persisted state". There is no `spatialLedgers.*` this subsystem owns
      // and there never will be; the single persisted field the whole design allows (the
      // CHOSEN posture, Q-S2) lands on the occupations record at SEAT-3, not here.
      stateKeys: Object.freeze([]),
      other: 'THE COUNTERPART SEAT: the owner directive (ODQ 735.2) that an occupied or vassal settlement\'s ruling power has "an equal or greater counterpart in all decision-makings derived from the overlord or occupier". ONE GATE AT THIS CAR: stressorGates.occupierGovernsHere reads foreignSeatEnabled by name with the strict === true idiom, off snapshot.worldState.simulationRules rather than the normalized rules because a virtual key has no DEFAULT_SIMULATION_RULES entry to normalize (the brokerageEffectsActive idiom). WHAT THE FLAG LIGHTS AT SEAT-1, EXACTLY ONE THING: the coup spawn gate\'s occupied test widens from the STRESSOR spelling to the UNION of the stressor spelling and the occupations LEDGER. That is D4\'s named move and A1.1.2 rules it must land flag-gated rather than as plumbing, because the two spellings were MEASURED to diverge in BOTH directions - a war-layer conquest mints a ledger row at occupation.js:966 and never a type occupation stressor, so today the gate OPENS and a coup spawns in a town already held at spearpoint, while a generation-authored occupied stress has no ledger row at all and must keep blocking. The lit predicate is their UNION, so it can only ever block MORE coups than today and never fewer, and the DARK path keeps the stressor spelling VERBATIM. WHAT SEAT-1 SHIPS UNREACHED, AND THAT IS THE DESIGN: foreignSeatOf, the one seat resolver, consults the three substrates in precedence order - the occupations ledger at a rung BELOW vassalized (regime occupation, carrying PRIMACY), a vassal relationship edge on which this settlement is the junior or the vassalized rung itself (regime vassalage, carrying a banded scalar weight), and subordinating treaty ties alone (capped at the present band, because influence without a compact is pressure and not a seat). It has NO production consumer at this car and its greens are therefore REGRESSION-grade, not discovery-grade; SEAT-2a wires it into the seat books and the decision choke and carries the discovery proof. THE VASSALIZED RUNG IS RULED OUT OF OCCUPATION (A1.1.8): the ladder\'s top rung never exits the ledger because the sovereignty-sale machinery needs the row, so a presence-only predicate would keep a matured vassal under occupation PRIMACY and under W-COIN\'s zero-tax rule forever, making cultivate the road to permanent maximal domination and inverting the owner\'s design. PRIMACY IS ORDERING, WEIGHT IS SCALAR (law 2.3): occupation grants override rights on a TYPED decision-class list and never depends on a tuned number staying bigger, so no tuning pass can invert "always just greater". ONE RESOLVER PER QUANTITY: the duplicate vassalOverlordOf collapsed here - two functions of one name read different substrates from different argument shapes, the dead edge-walking export in occupation.js is deleted and the ledger question has one home. GRIP_SCALE is its OWN named table and is never folded into occupation\'s STATE_BENEFIT_SCALE: one is a POLITICAL unit and the other an ECONOMIC one, and they diverge at the bottom of the ladder on purpose. WHAT IT NEVER DOES: no persisted seat state, no rng, no wall clock, no writes of any kind, no fifth governing-truth fork - it CONSUMES governingFactionOf rather than re-deriving it, and it mints no 11-by-11 culture table because the II.5-2 derived-similarity composite is constitutional. The lane is pinned at tests/domain/foreignSeatResolver.test.js and tests/domain/foreignSeatDormancy.byteIdentity.test.js. SEAT-2a GIVES THE RESOLVER ITS FIRST NINE PRODUCTION CONSUMERS AND THE FLAG ITS SECOND GATE, so this row greens stop being regression-grade and become discovery-grade. WHAT SEAT-2a ADDS TO WHAT THE FLAG LIGHTS: readWarSeatBooks gains a FOURTH weight, foreignWeight01, and the three weights become an enumerated PARTITION read through seatBooksPartition. The gate is inside the producer, once, rather than at each of the nine consumer files - nine gates would be nine chances to forget one and the ninth would be a silent partial lighting. THE DEFECT THIS CURED, which ODQ 823 ordered cured FIRST: warTermination blendBooksTerm re-normalized by its OWN computed total, so a fourth weight it did not know about neither threw nor produced a NaN - it SILENTLY REDISTRIBUTED that weight mass across the three books it did know, moving cause, cost_to_continue and cost_to_stop and therefore the deciding term and the disposition itself, with no receipt anywhere admitting it. A fail-open normalizer is indistinguishable from a correct answer, which is why the most defensive line in the family was the most dangerous one. THE CURE IS STRUCTURAL, NOT A PATCH: no file outside warSeatBooks.js may enumerate book weight FIELD NAMES, a fifth book joins by widening SEAT_BOOK_KINDS, and a source scan in tests/domain/warSeatBooksPartition.test.js enforces it with exactly two licensed exceptions - warSeatBooks.js which owns the partition, and conquestFeasibility.js whose own module header declares no imports at all, an older constraint that wins. DORMANCY IS BIT IDENTITY RATHER THAN CLOSENESS: with no foreign book the producer rescale factor is exactly 1 and the blend fourth product carries weight exactly 0, and x times 1 and y plus 0 are exact for every finite double, so a dark run reproduces the previous arithmetic to the LAST BIT. The domestic private mass is summed once and multiplied once rather than distributed into two products, because distributing it changes the last bit in a MEASURED 26.5 percent of a 39601-cell sweep and looks identical in the other 73.5 - a single hand-picked sample of that claim is a coin flip weighted three to one toward a false pass, and the first draft of the pin failed exactly that way. THE INTEREST LABEL GAINS A FOURTH VALUE, foreign, AND THE RULE THAT PICKS IT IS ORDERING NOT MAGNITUDE (law 2.3): an occupier wins the label because occupation confers PRIMACY, never because its scalar out-weighs anything, so no tuning pass can invert the owner always-just-greater. A vassal overlord holds a scalar that competes honestly and leaves the label domestic. WHAT IT STILL DOES NOT DO, DECLARED: no reader-facing NEWS kind is minted for a foreign-carried decision. The war-ruling evidence writer ABSTAINS from both suing kinds rather than mis-attributing - sued_for_peace_realm would claim the realm own position decided and sued_for_peace_seat names the LOCAL ruler on the settlement behalf, the wrong person for the right event. The kind is chartered in the volume D8 bill, five kinds minted together paying the full V6-B3 census; until it lands the fact is receipted on the termination read itself as booksInterest, foreignSeatId, foreignSeatBand and booksReason. The partition is pinned at tests/domain/warSeatBooksPartition.test.js. SEAT-2a ALSO LANDS D2 PRIMACY AXIS, AND IT IS THE FLAG THIRD GATE: a second, flag-gated pass in candidateEvents placed AFTER the autonomy routing, returning the SAME ARRAY REFERENCE when dark so the choke own byte-identity pin survives by construction rather than by arithmetic. WHERE IT LANDS AND WHY NOT WHERE THE VOLUME PUT IT: authorityFor sees only rules - no settlement, no snapshot, no worldState - so it cannot know which town is deciding, and A1.1.6 struck the one edit serving thirty families claim. The axis lands at the choke where the context is. THE CONTRACT IS TYPED AND DISTINCT FROM applyMode: a candidate marked proposal because the campaign runs dm_only and one marked proposal because a foreign army holds the town are indistinguishable if routing is the only record, so the RULING is the contract - a record naming the seat, regime, rung, band and decision class - and the routing is one of its two dispositions. REFERRED means the decision leaves the local court autonomous lane carrying its ruling; the court still proposes and no longer decides. VETOED means the local court may not take the decision at all and the candidate is refused. Both dispositions are live. THE REGISTER IS KEYED ON candidateType AND NOT ON ruleFamily, MEASURED: ruleFamily is coarse - every tier candidate carries tier and every resource candidate resource, while the whole relationship family is PRESSURED-ONLY in the coverage table - so keying on the family would have silently swept in decisions the table rules OUT of force reach, and a coarser match never reds. AND EVERY SPELLING WAS MEASURED AGAINST THE EMITTERS: CHANGE_AUTHORITY_POLICY names tier_change and NO producer emits that string - tierResourceDynamics emits tier_promotion and tier_demotion - so a register copied from the policy table would have matched nothing, done nothing, and passed every existence census while doing it. The pin scans the emitters. PRIMACY, NOT PRESENCE: the pass requires seat.primacy true, so a VASSAL overlord reaches none of it. Reading mere seat existence would have handed every matured vassal the occupier powers, which is A1.1.8 inversion arriving through a second door, and the pin asserts the same candidate list is untouched at the vassalized rung and moves at stabilized. The state-only bypass the choke installs survives the pass by name, so a mechanical condition refresh never becomes a proposal-budget casualty. WHAT THE AXIS DOES NOT YET CARRY: the OVERRIDE BURN (A1.2.14) - the typed legitimacy price on the occupier home court plus the durable scar into the resistance-target inputs - is SEAT-2b work by the volume own split, because it is a cross-settlement legitimacy WRITE that must ride applyLegitimacyDeltasToUpdates with the re-band and this is a read-and-route layer. Nor the deterministic direct-site majors (mobilization posture, levy and garrison) which have no policy key and no choke passage; they are named per-site edits A1.1.6 charters and are recorded as owed rather than silently skipped. The axis is pinned at tests/domain/foreignPrimacyAxis.test.js. SEAT-2c ADDS D9 WAVE 1, AND ITS SCOPE IS AMENDMENT A1.2.13 AND NOT THE VOLUME BODY: the body promised a PHYSICAL army act through armyTransit with upkeep and a coin sink, plus a rebellion term in successPressure and an insurgency term in stabilizationSuitability; A1.2.13 struck the physical half because it contradicted two recorded vetoable judgments (interventions ride a deliberately transit-ISOLATED ledger, and initiation is deliberately live-state rather than belief-gated) and re-specified wave 1 as exactly four items - conserved columns, a hop-priced MARCH-DELAY term, an eligibility rule of relationship edge OR foreign seat, and derived presence-consequence machinery for uninvited entry. THE OTHER TWO LEGS ARE NOT MERELY DEFERRED, THEY HAVE NO SITE OF THE SHAPE THE BODY ASSUMED, and that is measured rather than argued: successPressure exists at exactly ONE place tree-wide and is a vassal-RELATIONSHIP candidate scorer emitting severity and probability rather than a verdict taking a signed adj, stressorGates carries only a rebellion BIRTH gate, stabilizationSuitability is an OCCUPATION state-machine input whose garrison term is already a flat boolean, and no insurgency RESOLVER exists in the worldPulse family at all. WHAT THE FLAG NOW ALSO LIGHTS, EXACTLY THREE THINGS. First, THE ELIGIBILITY RULE: the mover could only ever see relationship EDGES, so an overlord whose compact lives in another substrate was structurally invisible to the contest it had every claim to enter. The widening admits the foreign seat, and its live population is NARROW AND MEASURED - of the four bases foreignSeatOf resolves, the two vassal-edge bases are edge-backed and already eligible so the array comes back by REFERENCE, and the occupations-ledger basis is largely unreachable because SEAT-1 own cure refuses a coup birth in a ledger-occupied town while this flag is lit, which leaves precisely the two EDGE-LESS bases: the vassalized rung whose relabel vassalizationOutcomes skipped, and subordinating treaty ties. THE ADMITTED SUITOR IS LABELLED vassal AND THAT IS NOT A FABRICATED EDGE: the scorer reads relType only to answer whether a compact binds the incumbent and whether the column was invited, both admitted bases ARE compacts read from a substrate that carries the fact without carrying an edge, and law 2.1 exists precisely because those three substrates answer one question. An invented label would have scored NO motive at all and the arm would have passed every existence census while admitting a court that can never act. Second, THE MARCH, AND IT IS A REPAIR RATHER THAN A FEATURE: pulseKernel says of interventionAdjFor that the arriving army tilts the verdict it reaches in time and that a column arriving after the verdict marched to yesterday coup, and there was NO TIME TERM IN THAT FUNCTION AT ALL - every committed column tilted at full share the instant it committed, at any distance, and the only thing making that sentence partly true was a one-tick stage ordering identical for a neighbour and for an empire six weeks away. The delay is hop-priced through armyMarchWeeks, the estate ONE march derivation, never a second one, and it is a STEP rather than a ramp because an army is at the contest or on the road and there is no fractional column. An unreachable or unmapped pair yields ZERO march, which is hopDelayTicks own no-path-no-surcharge discipline verbatim, because the alternative would silently delete an intervention the mover lawfully committed. Third, THE WELCOME, which closes a measured hole: interventionLegitimacy computes, rounds and documents a legitimacyCost whose ONLY consumer tree-wide is a news TAG reading its sibling flag, so LEGIT_COST_UNINVITED priced nothing and an army that came uninvited paid exactly what an invited one paid. An uninvited column now keeps less of its strength share, because a court that must be seen not to have called an army carries less of its weight into the contest. THE CONSEQUENCE IS A SHARE KEEP AND DELIBERATELY NOT A LEGITIMACY WRITE: a cross-court legitimacy debit is A1.2.14 override-burn class, it must ride applyLegitimacyDeltasToUpdates with the re-band, and SEAT-1 census already found EIGHT near-identical copies of that applicator so a ninth is forbidden - the burn stays SEAT-2b, and the town own resentment blunting an unasked-for army is expressible with zero persisted bytes. THE KEEP CONSTANT IS DECLARED AND NEVER DERIVED FROM THE LEGITIMACY PRICE even though one minus that price equals it today: a legitimacy PRICE is a standing debit on a court and a tilt KEEP is a dimensionless factor on a strength share, 711.6 law is that one number wearing two units acquires a different meaning at every consumer and nothing ever reds, and the coincidence is stated on settlementPolitics own precedent with a source scan forbidding the leaf to so much as name the convergence tuning. WHAT SEAT-2c DELIBERATELY DOES NOT DO: it adds NO sixth signed term to the coup clamp. Five terms sum to 0.815 against a DECLARED budget of 0.82 that the coup-adj suite asserts is within 0.05 of the worst case, and the sixth slot is chartered to SEAT-7 forceRatioAdj on a file declared SERIAL SEAT-2 to SEAT-7 - so D9 rides the ALREADY-WIRED interventionAdj and changes what a column SHARE is worth, never how many terms the clamp carries. AND THE GATE IS THE SEAT KEY RATHER THAN THE INTERVENTION KEY, BY RULING: interventionEnabled is a WAVES PRESET flag lit today in dramatic_campaign, living_realm and full_simulation, and amendment A2 forbids widening autonomous minting behind it because that is a declared LIT shift the owner has not seen. Every arm therefore AND-composes with the virtual foreignSeatEnabled and returns its IDENTITY value when dark - the same array reference, or exactly 1 - so the dark share is the pre-D9 expression LITERALLY rather than an arithmetic equivalent, and the hashed intervention dormancy golden, whose lit arm sets interventionEnabled and never the seat key, does not move in either arm. The lane is pinned at tests/domain/seatIntervention.test.js. SEAT-3 ADDS THE POSTURE, THE ONE PERSISTED FIELD THE WHOLE SEAT DESIGN ALLOWS (Q-S2 GRANTED). The ladder conflates how much CONTROL an occupier has with how it TREATS the town - extractive is a rung the machine climbs through, not a policy anyone picked - and the owner directive asks for a mixed responsibility to cultivate and exploit, which is a CHOICE. A closed posture of exploit, administer or cultivate now rides the occupations record, biasing the two ladder inputs it feeds and never forking a second state machine. THE GATE LIVES AT EXACTLY ONE SITE AND THAT IS WHAT MAKES A1.2.11 DARK CLAUSE LITERAL: occupationPostureOf returns the constant BEFORE it looks at the field at all, so a world that was lit, chose a posture and was unlit again behaves exactly like a world that never chose one, and the present-field-dark fixture the amendment demands proves it rather than asserting it. THE DECISION IS OCCASIONED ONLY BY A RUNG CLIMB to extractive or better - below that the occupier is still fighting for control and has no yield to have a policy about - so a court is asked at most three times in an occupation whole life and never on a quiet tick, which closes the amendment oscillation worry STRUCTURALLY rather than by tuning a hysteresis onto a second axis. A SOLD HOLDING DOES NOT INHERIT THE SELLER POSTURE, a deliberate divergence from the unrelated-fields-ride-across precedent: posture is the one field on that record encoding a DECISION, and a buyer inheriting the seller policy is precisely the fabricated un-chosen policy A1.2.11 forbids arriving through a door the amendment did not close. AND A THRESHOLD WAS DEAD ON ARRIVAL AND A PROBE CAUGHT IT BEFORE ANY TEST EXISTED: the rich line was first a round 0.6, but swept over the whole tier-by-prosperity-by-legitimacy grid the corpus produces, occupiedUsefulness spans 0.3177 to 0.6060 with p75 0.5114 - so a 0.6 gate sat above the ninety-ninth percentile and the exploit arm would have fired for essentially nothing while passing every existence census that asked only whether the branch was reachable. Both thresholds are now measured quantiles and the distributions are recorded at the constants. The posture is pinned at tests/domain/occupationPosture.test.js. SEAT-4 ADDS D5, THE ANTICIPATED-REACTION FORECAST, AND IT RIDES THIS KEY BY THE Q-S3 RULING rather than minting a fourth. Before a court takes a MAJOR decision it asks how the powers that matter will answer, and it asks its OWN believed picture of them. THE VOCABULARY IS THE ONE THE BELIEF RECORD CAN ACTUALLY COMPUTE (A1.1.3): the volume five bands became four - supportive, indifferent, wary, opposed - because the record holds only readiness, strengthBand, allianceLabel, faithLabel and confidence, and a five-band vocabulary would have been four bands of fiction over one band of data. The casus flag is TRUE treaty structure only, on the lord-knows-his-vassals precedent, because a casus computed from a stale picture would let a court talk itself out of an obligation it demonstrably has. A CONFLICT BETWEEN THE AMENDMENT AND THE ESTATE OWN LAW WAS FOUND AND RULED: A1.1.3 says an absent entry reads indifferent at low confidence, while readBeliefRelationship returns GROUND TRUTH on unknown because the declared relationship is public and absence is non-paranoid. The ESTATE wins on the LABEL and the AMENDMENT wins on the CONFIDENCE - a court cannot forget whether it is sworn to a neighbour, but it does not know that neighbour STATE - and for the neutral labels that dominate a real map the two rules then agree. THE BOUNDED SET IS THREE SUBSTRATES AND NOT FOUR: the volume lists neighbours-by-graph separately, but in this tree the regional graph edges ARE the relationship edges, so listing them twice would have made the set look wider than it is. THE FORECAST CHANGES AN OUTCOME AT THE CHOKE, beside the primacy pass and after it, returning the SAME ARRAY REFERENCE when dark. A decision is HELD BACK only when a power is opposed AND holds a live subordinating compact over the decider AND the court is confident enough to act - all three load-bearing, because dropping the casus condition lets every hostile neighbour veto everything and dropping the confidence condition makes a court act on a rumour it has half-forgotten, which is the failure the info-starvation tragedy exists to model rather than to cause. A held decision does not vanish: it routes to the authority lane carrying a typed record of whose answer was feared, which is the owner 741 payoff in one field. WHY THE CHOOSER TERM IS NOT BUILT, MEASURED: settlementStrategy.js is size-baselined at EXACTLY 812 effective lines with ZERO tolerance in BOTH directions and cannot take one line - the same wall that already blocks SEAT-2a ruled-but-unbuilt kernel term, so one decomposition unblocks both and that is recorded as a single chartered follow-on rather than two. AND SEAT-4 GARBAGE-LEDGER ARM FOUND A CRASH IN SEAT-1 OWN RESOLVER: foreignSeatOf reached normalizeRelationshipEdge with a null array element, whose default fires only on undefined, so a null edge threw against this leaf own INERT-NOT-CRASH header promise. SEAT-1 shipped it with one caller; the resolver now has three, so a throw there takes a whole pulse down rather than degrading one read. Cured and pinned. The forecast is pinned at tests/domain/anticipatedReactions.test.js.',
    }),
    // The gate the flag reaches runs on every candidate stressor birth, so the cadence is
    // the pulse's own — even though the lit difference is a refusal rather than a beat.
    expectedTempo: 'per_tick',
    invariants: Object.freeze([
      Object.freeze({
        name: 'dark_is_the_stressor_spelling_verbatim',
        description: 'THE PROMISE, and it is narrower than the usual dark claim because this flag lights a PREDICATE rather than a layer: with the key dark, `occupierGovernsHere` returns `activeTypesAt(snapshot, sid).has(\'occupation\')` and nothing else is evaluated, so the coup gate behaves byte-for-byte as it did before this commit. The strict `=== true` read refuses every truthy non-true value, so a string "true" or a 1 lights nothing.',
        check: 'NOT expressible from a receipt — a gate refusal leaves no receipt row by construction. Pinned behaviourally in tests/domain/foreignSeatDormancy.byteIdentity.test.js against the RAW comparator: the same seeded world advanced the same ticks with the key absent and with it explicitly false, serialized bytes compared with JSON.stringify and no normalizer (A1.19 — normalising would launder the very bytes the bar exists to compare), plus a truthy-refusal arm and a lit ANTI-VACUITY drive so a green cannot mean the instrument compared nothing.',
      }),
      Object.freeze({
        name: 'the_lit_predicate_only_ever_widens',
        description: 'The lit form is the UNION of the stressor spelling and the ledger predicate, never a swap. A settlement the stressor spelling calls occupied is STILL occupied when the flag is lit, so no coup that is blocked today becomes reachable. This is what makes the widening safe to light mid-campaign: it can refuse births that used to happen, and it can never resurrect one that did not.',
        check: 'Expressible from source and asserted that way, plus behaviourally: tests/domain/foreignSeatResolver.test.js drives the gate over the four-cell matrix (neither / ledger-only / stressor-only / both) at both flag states and asserts the lit column is a superset of the dark column, with the stressor-only cell blocked in BOTH.',
      }),
      Object.freeze({
        name: 'the_seat_is_derived_and_never_persisted',
        description: 'ZERO persisted state (law 2.2, on the hegemonyRead template). `foreignSeatOf` reads three ledgers other subsystems own and writes none of them; it holds no cache, mints no id, and creates no key anywhere. The single persisted field the design allows — the CHOSEN posture, Q-S2 GRANTED — is a SEAT-3 field on the existing occupations record and is deliberately absent from this car, so nothing in SEAT-1 can put a byte on disk whether the flag is lit or dark.',
        check: 'Expressible from source, and asserted that way in tests/domain/foreignSeatResolver.test.js: the leaf\'s source contains no assignment into `worldState`, no `setSpatialLedger`, no `Math.random`, and no `Date`; and behaviourally, that a lit resolver call over a frozen world returns the same view twice and leaves the input serializing identically.',
      }),
      Object.freeze({
        name: 'one_resolver_per_quantity',
        description: 'The resolver collapse this car owes (law 2.1). `vassalOverlordOf` existed TWICE under one name over two substrates with two argument shapes — an edge-walking snapshot reader in occupation.js (a dead export, measured zero callers tree-wide) and a ledger reader in traditions/relations.js (module-private, two live callers). The ledger question now has exactly one exported home and relations.js consumes it; the edge question was never a separate public quantity and lives as one arm of foreignSeatOf. GRIP_SCALE is likewise kept distinct from occupation\'s STATE_BENEFIT_SCALE rather than shared, because a shared numeric table with two meanings is the 711.6 failure by construction.',
        check: 'Expressible from source, and asserted that way in tests/domain/foreignSeatResolver.test.js: exactly one exported `vassalOverlordOf` exists under src/, its output equals the pre-collapse relations.js semantics over a matrix of rungs including the absent record and the non-vassalized rungs, and the leaf\'s mirrored OCCUPATION_RUNGS equals occupation.js\'s own STATE_LADDER so the second spelling can never drift.',
      }),
    ]),
    // No channel at all, so the row can never be graded ALIVE from a receipt and says so.
    // UNOBSERVED for this lane's standing reason — the soak builds its rules from the
    // full_simulation spread, which declares no virtual key — and for a second reason of
    // its own: the lit behaviour at this car is a gate REFUSAL, which leaves no receipt
    // row even on a lit run. The observation window belongs to SEAT-2a, where the seat
    // first moves a decision that receipts.
    soakEvidence: 'unobserved',
  }),
]);
