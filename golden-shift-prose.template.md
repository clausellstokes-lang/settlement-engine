# GOLDEN SHIFT LEDGER — LIGHTING POSITION 0 "THE SRC/ PROSE CAR" (§__N__, 2026-09-__DD__; SEAT: Fable 5.1 — validated)

## HEADLINE RESULT — a DECLARED same-seed shift on EMITTED TEXT; no rules value, no preset, no flag moved
The sealed prose car (`refs/preserve/srcprose-2026-09-03` = `8f4d5c648`, merge-base `30c1667bc`) was rebased onto
`__BASE__` by lane PROSE-REBASE (Opus 5) and lands as its own consist at `__CAS_SHA__`. It cures 1,001 reader sentences
across 200 paths; 113 of those paths are `src/domain/` modules outside `display/` that EMIT text, so **emitted text moves
on __N_PATHS__ engine-side paths** for the same seed. This entry is the declaration; the ODQ row §__N__ is the authority.

## Why this is a shift and not a repair of a defect
__WHY__ (the cures are sentence-level: em dashes, agreement, register — the FACTS a sentence states are unchanged; the
BYTES are not. THE PROMISE is kept: a saved world's lived history is immutable; what moved is how the reader is told it.)

## The eight overlapping paths, resolved by keeping the mainline change AND the cure
| path | prose side | mainline side | resolution |
|---|---|---|---|
__RESOLUTION_ROWS__

## Goldens / driven-corpus constants re-recorded — every one named, with its cause
| file | assertions | before | after | cause |
|---|---|---|---|---|
__RERECORD_ROWS__
Register state: `tests/fixtures/.golden-freeze-register.json` is UNFROZEN (`frozenAt` null); no shift-record file is
therefore signed for this act — the ledger row + this entry are the form (LIGHT-PLAN §4). Fingerprinted fixtures touched:
__FINGERPRINTED__ (expected: none).

## Non-golden registers moved at the landing (chair acts, at the composed tip)
- voice magnitudes (`UPDATE_VOICE_BASELINE=1`): __VOICE_BEFORE__ → __VOICE_AFTER__ (a FALL — shrink-only, banked)
- writer-reach: plain `--write`, __WR_DELTA__ (never `--genesis`)
- test ratchet: totalTests __TT_BEFORE__ → __TT_AFTER__, totalFiles unchanged, entries 6
- prose-numerics: __PN_RELOCATIONS__ relocations, net zero rows
- lighting census: __LIGHTING__ (no new test file; expected +0)

## Conditions under which a FUTURE golden will legitimately shift (for the next session)
L-PROBE (POSITION 1) measures its dark-arm control AFTER this landing, so this car's text movement is never mis-attributed
to lighting. Any later same-seed text movement on these __N_PATHS__ paths needs its own declaration.

## The DECLARED-SHIFT sentence owed to LGT-REG-DECL (cause (0), verbatim into the eleven-declarations record)
> "1,001 reader sentences across 200 paths were cured; emitted text moves on __N_PATHS__ engine-side paths; no rules
> value, no preset and no flag moved."
