# THE UNDERCITY TRAIN — MF-UC0 … MF-UC5 (compiled charter, RULED at ODQ §441)

**RULED at ODQ §441 (2026-08-22); panel-verified at b10ed1a1.** Before ruling, the chair ran
three read-only skeptic lenses (doctrine fidelity · estate reality · packet/gate law) over the
§433-collected draft at b10ed1a1 — 23 findings, 40 solid confirmations, every finding with
executed file:line evidence (panel record: successor scratchpad `tasks/w7ectvopq.output`). §441
ruled every finding; lane **TC-UNDERCITY-R2** (chair-tier compile seat, §441.9/§442.4) applied
the ruling IN PLACE: every ruled item is an explicit edit tagged `§441.x` where the text changed,
and the §3 table was RE-WALKED at b10ed1a1 with file:line receipts before any row kept the word
CONFIRMED. The pre-ruling text is preserved beside this file as `draft-UNDERCITY-PLAN.pre-R2.md`;
the edit ledger is `laneTCUNDERCITYR2-receipt.md`. One later chair law that names these cars by
name (§443's pulse-written-key reader law) is folded in as a tagged addendum.

**Lane:** TC-UNDERCITY, chair-tier COMPILE seat (ODQ §343.1; dispatched at ODQ §431 by owner
order — "can you start the undercity train?"). Compiled read-only: no repo write, no ref, no
gate run, no vitest of any kind (a landing gate and executor lanes share the box; the suite's
method laws are §432's, carried in §10 — §441.5(h)). Receipts: `laneTCUNDERCITY-receipt.md`
(the compile) and `laneTCUNDERCITYR2-receipt.md` (the ruling applied).

**Charter authority:** ODQ **§311–§311.9** (the undercity doctrine — owner design directive +
chair refinements at delegation, quoted verbatim in §1 below) · **§359.3** (OB-6 FOLDED into
this charter) · **§359.4** (OB-7 revisits at this charter — ruled in §6) · **§360/§361**
(CT-4 consumes this train's engine facts; the fact contract is §7) · **§431** (this dispatch) ·
**§423/§423.1** (the domain-door law) · **§424** (the TC-PRODUCERS findings that bind the
fabric-vs-engine split) · `draft-PRODUCERS-PLAN.md` (the priced predecessor whose architecture
this charter follows).

**Bases (rev-parsed fresh; bumped by TC-UNDERCITY-R2 at the ruling's application):**
- BUILD `claude/composite-r4` @ **`84e06412`** (T2H landed, §442.1; the compile read
  `ec8f3359`, the §424 H8B landing). **Every file:line in this charter was RE-WALKED at
  `b10ed1a1`** — the chair's shared baseproof worktree (`chair-baseproof-b10ed1a1`, §440.4),
  the tree the skeptic panel also walked. That reading is EQUIVALENT at 84e06412 for every
  cited path: `git diff --name-only b10ed1a1 84e06412` is exactly MF-T2H's landing —
  `src/domain/townMap/fabric/spatialReceipt.js` + `fabricRng.js` (new), their two tests, the
  packet meta files (INDEX/MANIFEST/MF-T2H.md) and the two census walkers
  (`sovereigntyLightingContract` · `entropyRootCensus`) — none of which this charter cites by
  line (the census TUPLE moved, which is why no tuple is written here; §441.5(e)). ⚠ The
  landing cascade keeps MOVING this branch (WEB-1 → the map stack → the producers); every
  executor re-derives every figure at its OWN base. Figures here are compile-time facts,
  named by tree.
- LEDGER `review-fixes-2026-07-08` — ODQ §441 read from the live working tree at the §443
  commit (`b0f710ec`); the compile read §431 at `2f5d607a`. GENERATION-SPEC read at the same
  tree: `map-corpus/docs/GENERATION-SPEC.md`.
- PRODUCER lane tips consumed by name, all UNLANDED (§441.5(f)): T2R `src/domain/highWater.js`
  at lane tip `387a3b34` (member `23e0bcd9`; packet BLOCKED → READY pending §443's C-a
  re-proof); T2Q `src/domain/institutionFounding.js` at lane tip `b3e84008`; T2N has no
  commit, ref or packet — its path is PREDICTED.
- OB-7's lost spec recovered from the MF-R2 lane transcript
  (`~/.claude/projects/…/a244e7a3…/subagents/agent-aa87a4ba77846140b.jsonl` — the
  subagent-transcripts-survive-purges route of record), quoted verbatim in §6.

**Family and cap (RULED §441.7 — supersedes the compile's new-family reading and J-8):** the
six cars file under the **STAMPED town-cartography family** as **MF-UC0 … MF-UC5**, the §433 C2
precedent (T2N/T2Q/T2R are the same domain-root-deriver shape under MF). Every packet cites
`docs/implementation/preambles/MF-PREAMBLE.md` by SHA-256 **unchanged** —
`0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` at b10ed1a1 (re-hash at
the executor's base; expect the same digest) — and **ODQ §311** as the doctrine citation. **NO
new family, NO UC-PREAMBLE, no stamp question:** the family's stamp is GRANTED at §312.2b
(eight members per engine train) and every train here carries ≤3. The roster is six cars →
**THREE trains**: **T-UC1 = UC-0 / UC-3 / UC-1** · **T-UC2 = UC-4 alone** (the flag-minting
wave rides alone, §441.4) · **T-UC3 = UC-2 / UC-5**. Reversal recorded by the chair: found the
`undercity` family with its own preamble only when undercity GEOMETRY (D5) or the DW program
needs a distinct law substrate. Fabric-side undercity GEOMETRY is the same family's D5 strata
wave — and is a NON-GOAL here (§2), deferred whole.

---

## §0 · THE TRAIN IN ONE PAGE

The §311 doctrine compiles into **six engine-side cars, every one golden-inert by
construction**: five pure derivers at the domain root (the ageBands/T2N/T2R precedent —
generation never imports them, the generator golden never moves) plus one pulse-seam member
riding EXISTING registered machinery (the T2Q precedent — no new door verb). **No car writes
one byte on the generation path; no car mints a store verb; no car ships a user-visible
surface.** Exposure arrives later by two already-chartered routes: CT-4 (dossier prose, §361)
and the map program's D5 strata wave (the −1 leaf, dormant fabric) — each with its own gate.

| car (packet id) | train | the fact it produces | shape | same-seed motion |
|---|---|---|---|---|
| **UC-0** (`MF-UC0`) | T-UC1 #1 — compiles NOW | the underground SHEET existence gate + the license-home audit (§311.1, §311.8.5) + the `institutionSubstructure` facet kind (§441.5(a)) + the walls accessor walk (§441.5(d)) | pure deriver `src/domain/undercity/strataExistence.js` (+ inference rows in `cohesionWeave.js`) | NONE (structural; the facet rows PROVEN golden-inert at S0) |
| **UC-3** (`MF-UC3`) | T-UC1 #2 — compiles NOW | the STATIC components — caverns, terrain-licensed, epoch-invariant (§311.8.2a); wells/cisterns MOVED to UC-1 (§441.5(b)) | pure deriver `src/domain/undercity/staticComponents.js` | NONE |
| **UC-1** (`MF-UC1`) | T-UC1 #3 — after T2R lands | the sanitation LADDER rung under the §441.1 roster rule + per-quarter coverage + winning causes (§311.7) + wells/cisterns, the second surface-coupled component | pure deriver `src/domain/undercity/sewerDerivation.js` | NONE |
| **UC-4** (`MF-UC4`) | T-UC2 ALONE — after T2R lands | the UNDERCITY EPOCH layer — colonization extent, smugglers' tunnels (§441.5(c)), fossils, flood/seal state (§311.3/.4, §311.8.2d/.3/.4) | deriver + ONE pulse-seam high-water signal behind a NEW virtual flag — the FLAG-MINTING wave and the FIRST builder of the §359.5 persisted-signal shape (§441.4) | pulse-path only, DARK-BY-DEFAULT (zero writes until R-5 lights the flag); measured at S0 |
| **UC-2** (`MF-UC2`) | T-UC3 #1 — after T2N + T2Q land | the MONOTONE components — crypts/ossuaries, cellars/undercrofts (incl. the smuggler CELLAR subset), mines/quarries (§311.8.2b) | pure deriver `src/domain/undercity/monotoneComponents.js` | NONE |
| **UC-5** (`MF-UC5`) | T-UC3 #2 — after UC-1..UC-4 | the CONNECTIVITY graph — classes, postures, typed joints, isolation facts (§311.9) | pure deriver `src/domain/undercity/connectivity.js` | NONE |

**The compile's central inheritance (from §424, binding):** the generator golden hashes the
WHOLE settlement (`tests/property/generatorGoldenMaster.test.js` per the TC-PRODUCERS
measurement), so any generation-path field flips every hash and dormant-until-consumed is
structurally unavailable there. This charter therefore prefers the pulse-side/pure-deriver
shapes everywhere the doctrine permits — and the doctrine permits it everywhere: every §311.8
license reads facts that already exist (or that the producer train mints), and §311.7.3's
seed-keyed middle-band draw reads the settlement's EXISTING seed, minting nothing.

**OB-6 (folded here per §359.3):** recommendation **DECLINE-AS-DEFAULT** — the doctrine needs
no new catalog institutions; an optional owner-gated expansion car is priced in §5 with its
full declared-shift bill. **OB-7 (revisits here per §359.4):** spec recovered verbatim;
**STAYS DEFERRED** with the revisit condition narrowed — §6.

**Queue position (RULED §441.7; the compile's "both trains behind the producer train" is
superseded — J-5):** the producer-free cars no longer wait. **UC-0 and UC-3 compile NOW** (no
producer input; the chair dispatches them as the first two buildable cars at the R2 ratification,
§441.9). **UC-1 compiles after T2R lands** (its high-water cause reads `deriveHighWater`).
**UC-4 compiles after T2R lands** (the vertical mark composes with the same reader). **UC-2 and
UC-5 compile after T2N + T2Q land** (UC-2 consumes T2N's resource sites and T2Q's
`institutionFoundingOf`; UC-5 consumes UC-1..UC-4's outputs). One truth per fact, never a second
derivation. The dependency is carried as a prose **"Depends on:"** header line in each packet
(the MF-T2G/MF-T2Q form) — `dependsOn` is not a validator key — and **no UC packet carrying a
producer symbol in `requiredSymbols` is minted, even DRAFT, before that producer's landing sha**
(`requiredSymbols` resolve against the live tree at EVERY status; §441.5(f)). Undercity surfaces
are disjoint from townMap/fabric and the website cars, so the trains interleave with D3a
landings exactly as the CT cars do (§361.2's slotting precedent); only the standing meta files
overlap, governed by the standing rebase laws.

---

## §1 · THE BINDING LAW, QUOTED

The component law, §311.8.1, verbatim — binding on every car:

> **THE GENERAL FORM, binding on every component**: LICENSE (the facts that permit it) +
> ANCHOR (the surface feature it sits under/beside) + EXTENT DRIVER (the quantity that grows
> it) + TEMPERAMENT + CAUSED PORTALS (its surface joins). Nothing exists without its license,
> sits off its anchor, or grows by anything but its driver.

The temperament vocabulary, §311.8.2, verbatim (closed):

> **THE FOUR TEMPERAMENTS ARE CLOSED VOCABULARY**: (a) STATIC terrain fact (caverns — never
> grow nor diminish); (b) MONOTONE excavation (crypts/ossuaries by burial demand ×
> institution age, with plague-year surge pits; cellars/undercrofts by quarter trade volume,
> the smuggler subset by criminal share at gates/waterfronts; mines/quarries at the dossier's
> resource sites while worked, ABANDONED not erased after); (c) SURFACE-COUPLED
> infrastructure (sewers per §311.7; wells/cisterns by water need, tier-scaled); (d)
> DEMAND-DRIVEN colonization (the undercity proper per §311.4; smugglers' tunnels ONLY where
> a wall or toll makes the dig worth it — a bypass needs something to bypass). Every future
> component declares one temperament.

The existence gate, §311.1, verbatim:

> **THE EXISTENCE GATE (owner):** the underground SHEET appears ONLY if the settlement
> actually has qualifying subterranean institutions — sewers, crypts, cellars and their kin.
> No qualifying seed → no sheet. This is the licensing rule for §287.4's fact-licensed
> StrataExistencePlan: seed classes are TYPED sources with dossier homes (sanitation
> infrastructure at the tiers that earn it; crypt-bearing institutions; cellar-bearing
> commerce; mine/quarry workings where terrain licenses them).

The causal law, §311.2, verbatim (added at §441.5(b) — it names the CAUSED PORTALS every
component row now carries as `surfaceJoins[]`):

> **CAUSAL, NEVER COSMETIC (owner):** every underground element maps IN RELATION TO THE
> SURFACE — a sewer that makes no sense for the settlement's layout is decoration and cannot
> be navigated or plotted. Chair's engineering note: sewers are derivable TODAY — under the
> drawn street web, along the relief substrate's fall lines, draining to the placed water;
> crypts under the institutions that bury; cellars under the commerce that stores; every
> surface↔underground access (church stair, well-house, sewer grate, cellar door) is a §287.5
> two-endpoint portal with a surface cause.

The two-level reading, §311.4 (owner wording preserved): *"the SHEET exists with any
qualifying seed (the honest mundane network, however modest); the UNDERCITY additionally
requires the criminal-economic drivers that grew it."* Proportional fill is *"PROPORTIONAL to
the economy, to the criminal share of that economy, and to crime-syndicate power standings —
typed dossier facts, a DERIVATION never a dial, THE PROMISE intact."*

The epoch law, §311.3: the undercity *"REQUIRES at least one/some mundane seeds and GROWS
from them"*; *"the mundane network is epoch zero; the inertia law holds (new digging never
re-rolls old crypts); and the HIGH-WATER LAW runs underground — dug is forever."*

Lawfulness and rosters (§311.6): sewers — all cities/metropolises, only SOME towns, below
that none; sewers expand only as the surface expands (one-way, acyclic). **§441.1 refines
§311.6.1 by the dossier's own roster (chair refinement under the §311 delegation, vetoable):**
the FULL WEB rung exists EXACTLY where the roster carries 'Sewage system' (or a custom
institution declaring the sanitation/drains facet); cities and metropolises carry a FLOOR of
QUARTER NETWORK (the owner's "all cities/metropolises have sewers" kept as drains in the
planned quarters); towns derive by the causes and cannot reach full web without the
institution (the owner's "only SOME towns"). One truth: the rung and the roster never
disagree about whether the sewer institution exists. Crypts — zoned
around religious places, cemeteries, residential quarters, outskirts; monotone. Caverns —
static, seed-permanent, epoch-invariant. Connectivity (§311.6.4) — *"encouraged but NOT
required"*; isolated pieces are lawful; *"any new linking passage is a dug act with a cause
and a payer … never an assumed corridor."*

Boundaries (§311.9): three connection classes + the refusal class (NATIVE / ADJACENCY BREACH
/ FUNDED LINK; vaults and dungeons connect to nothing by default — a breach into one is a
HEIST EVENT); the seven laws (motive · route value from surface causes · the security
gradient OPEN/GUARDED/SEALED with the stricter side's TYPED JOINT · sewers are the spine
where they exist, else an ARCHIPELAGO undercity · connectivity is state (sever without
delete) · links persist (high-water on edges) · the honesty contract — isolation VISIBLE on
the sheet). And §311.9.3: *"the underground never causes the surface; routes feed
crime/espionage outcomes ONLY through the §287.4 receipt seam, never same-pass."*

Routing law L2 (§311.5, §311.7.5, §311.8.5, §311.9.3): the GENERATION-SPEC §13/§168 fold
rides the next spec touch as one edit — a compile lane writes nothing, so that fold is
carried as a DEFERRED ROW in §9 (deliberately deferred, documented). §311.8.5 also orders the
license-home audit — executed as UC-0's first obligation (§4).

---

## §2 · THE FABRIC-VS-ENGINE SPLIT (chair scope, ruled by this compile)

**Engine-side (THIS train):** typed FACTS on settlements — sheet existence, ladder rung,
component roster with license/anchor/extent/temperament, epoch/colonization state, the
component-level connectivity graph. Golden-relevant if placed on the generation path;
therefore NOT placed there (see the §424 inheritance in §0). All six cars are domain-root
derivers or pulse-seam readers.

**Fabric-side (NOT this train — affirmative non-goal):** all undercity GEOMETRY — the −1
leaf, galleries, chamber cuts, drawn mains, the registration ghost, portal placement as
drawn joints, `SUBTERRANEAN_UNDER` solids. The GENERATION-SPEC already architects the whole
stratum (read at the ledger: `LeafIndex = -2 | -1 | 0 | 1`; G-27 "THE UNDERGROUND STRATUM
(§13/§168) — unbuilt, twice deferred, first-class in wave nine"; G-54 connection points; the
D5 strata wave row "leaf roster; connection points; undercity leaf gains 2.5D · 0 orphan
connections"; the registration-ghost law; the §166 2.5D amendment). That work is
town-cartography family under the §312.2b stamp, lands DORMANT (fabric is dormant until
D3b), and consumes THIS train's facts through the §287.4 `StrataExistencePlan` seam — UC-0's
deriver IS the fact-licensed plan §287.4 names. Duplicating any of it engine-side would mint
a second truth.

**The §423 door, complied with by construction:** no car mints an operation verb or a payload
grammar. §311.9's FUNDED LINK ("a payer, a route motive, and a DATE: a dug act in the event
stream") compiles as DERIVED PROVENANCE — the same register MF-R2's C5 ruled for new-sewer
events — not as a store operation; nothing here is a player-initiated act. If a later feature
lets players commission digs, THAT feature owes the §423 two-layer shape (verb in
`src/store/operationRegistry.js`, payload in the domain grammar) and is out of scope here.
Option-D binds every draft: a third vocabulary at either level is refused at review.

---

## §3 · THE LIVE ESTATE (surveyed at ec8f3359; RE-WALKED at b10ed1a1 by TC-UNDERCITY-R2 — what exists, what is missing)

**Already landed (the D6 UNDERWAYS program; breadth re-derived at each base by
`grep -rhoi underways src tests | wc -l` — the compile's bare "119 mentions" figure is DROPPED
per §441.5(j): the instrument was unnamed and the count reads differently at every tree):**
- `src/domain/worldPulse/clandestineFacet.js` — the covert-couplings substrate:
  `UNDERWAYS_TUNING` (bounded owner-retunable coefficients), `hasClandestineFacet` /
  `settlementHasUnderways` resolving through the facet-law chokepoint (`facetOf`), dormant
  absent the facet. **This is the precedent for both the tuning-constant shape (§8 R-3) and
  the facet-resolved license reads.** ⚠ **FOUND ESTATE DEFECT (panel, estate lens F3; recorded
  here, NOT cured by this train — D-8):** `clandestineFacet.js:50` reads
  `facetOf(i, 'institutionFunction') === 'clandestine'`, but the catalog declares
  `facets: { clandestine: 'clandestine', subterranean: 'subterranean' }` (:886/:1446/:1965) and
  `declaredFacet` (`cohesionWeave.js:281-294`) requires `facetKind in facets` — so the catalog's
  own 'Underground network' rows resolve `null` for that kind and do not count; the precedent's
  tests pass on a self-supplied fixture spelling. UC-0 therefore PINS the key spelling it reads
  with a positive arm against the REAL catalog row (§441.5(a)) and copies the precedent's
  chokepoint, never its key.
- Catalog (`src/data/institutionalCatalog.js`): 'Underground network' at THREE tiers
  (village `baseChance 0.08` … lines 880/1440/1959) with
  `facets: { clandestine, subterranean }` and `forbiddenResources: ['marshlands',
  'fertile_floodplain']` ("tunnels flood" — §311.3's high-water law already whispered in the
  data); 'Underground city' + 'Black market bazaar' at metropolis (lines 2383+); 27
  criminal-priority entries; 'Communal root cellar'.
- `underwaysOrganicFoundingEnabled` (virtual flag, ABSENT from defaults):
  `institutionLifecycle.js` can FOUND an excavated tunnel network organically — the
  mine-founds-itself pattern applied to crime (`subsystemRowsVirtual.js:967-1013`).
- Couplings into espionage, siege/supply (`FOOD_TRICKLE`, `INTERDICTION_RELIEF`),
  `stressInstitutionEffects.js` ("the underways" secret), geography tunnel-affinity comments
  (`geographyData.js:268,577`), `tests/data/underwaysInstitutionParity.test.js`.
- `historyGenerator.js:713` — a DEFERRED-with-reason underways founding mention (a recorded
  deferral this train does NOT resurrect; generation-path, golden-shifting).
- The epoch substrate: `worldPulse/urbanFabricKernel.js` (district deposit ledgers, stressor
  scars, catastrophe rebirth — the surface memory the vertical epoch law composes with);
  `calamityHistory` stamps carrying `year` + `tick`; `factionArchetypes.js` A.CRIMINAL;
  `corruption.js` criminal-faction power vocabulary (`criminalCaptureState`).
- Dossier prose already speaks the register (crypt/undercroft/cellar lines in
  `dossierCausalProse.generated.js`, `faithReceiptPools.js`, `institutionDescVariants.js`) —
  CT-4 refines this corpus against §7's facts, not from scratch.

**The license-home audit (UC-0 re-executes at its base; §311.8.5's order).** Every row below
was RE-WALKED at b10ed1a1 by TC-UNDERCITY-R2 with the file:line quoted (the compile's
ec8f3359 reading held for the rows it got right and was WRONG for three — founding kind,
criminal share, walls — which §441 rules; a compile's CONFIRMED is a hypothesis until a second
instrument walks it, §441 J7). CONFIRMED means the home was read at the line named; HOME OWED
AT UC-0 means the walk found candidates but no single accessor — UC-0 names it; NO TYPED HOME
means the doctrine named a field that does not exist; PRODUCER-TRAIN DEPENDENCY names the
unlanded producer and its real symbol:

| license input (§311.7.2/§311.8) | home at b10ed1a1 (holds at 84e06412 — header note) | status |
|---|---|---|
| subterranean/clandestine institutions | declared catalog facets `facets: { clandestine: 'clandestine', subterranean: 'subterranean' }` at `institutionalCatalog.js:886/:1446/:1965` (the three 'Underground network' rows; `grep -n 'facets:'` → exactly those three), resolved through `facetOf` (`cohesionWeave.js:315-319`: declared ?? inferred ?? null). The key UC-0 reads is **`facetOf(i, 'subterranean')`** (the catalog's own spelling), pinned by a positive arm against the real row — never the precedent's `'institutionFunction'` key (D-8) | CONFIRMED (§441.5(a)) |
| crypt-bearing / cellar-bearing / sanitation / mine seed classes (§311.1's other three) | NO facet kind exists today: `FACET_INFERENCE` (`cohesionWeave.js:258-275`) carries only `institutionNature` (faith/security/trade/craft/learning/vice/civic) and `institutionFunction` (heals/feeds/arms/judges). UC-0 mints ONE inference-only kind **`institutionSubstructure`** (`crypt \| cellar \| sewer \| mine \| none`) in that table — domain-side, never a catalog `facets` addition (`assembleInstitutions.js:296-299/:355` spread the whole entry onto the hashed settlement → R-2) | HOME MINTED AT UC-0 (§441.5(a)) |
| **'Sewage system'** (the sewer institution the roster already generates) | `institutionalCatalog.js:2225-2231` — `required: false, baseChance: 0.4, desc: 'Underground drainage. Rare but critical for health.', tags: ['sanitation'], priorityCategory: 'infrastructure'`, inside the **city** block (`city: {` at :1539; `metropolis: {` at :2249). Rolled at METROPOLIS too: `src/generators/steps/assembleInstitutions.js:243` `mergeCatalogs(institutionalCatalog['city'], institutionalCatalog['metropolis'])` — the chair's own correction of the panel's "no metropolis entry" | CONFIRMED (§441.1) |
| **'Aqueduct or water system'** | `institutionalCatalog.js:2239-2245` — `required: true, baseChance: 1, exclusiveGroup: 'waterSupply', tags: ['essential', 'water'], priorityCategory: 'infrastructure'`, city block (so present at city+ by construction; note its tags are water/essential, NOT 'sanitation' — it licenses the WATER side of §311.8.2(c), wells/cisterns, not the drain rung) | CONFIRMED (§441.1) |
| **'Public bathhouse'** | `institutionalCatalog.js:978-984` — `required: false, baseChance: 0.3, tags: ['trade', 'sanitation'], priorityCategory: 'government'`, inside the **town** block (`town: {` at :891). A sanitation-tagged institution at town tier: contributes to the causes, never to the full-web ceiling | CONFIRMED (§441.1) |
| founding kind (planned/charter/military) | **the field §311.7.2 names does not exist.** `genArrivalDetail` (`narrativeGenerator.js:236`, returns at :432-446) yields `{ age: null, reason: pick(reasonPool), foundedBy: pick(FOUNDERS_BY_TIER[tier]…), initialChallenge, overcoming, stressNote }` — prose picks only, no `kind`; `historyGenerator.js:825` stores it as `founding`; `grep -rn 'founding\.kind\|foundingKind' src` → ZERO hits; `viewModel.js:389-392` projects the same prose fields. The only "military" signal is a name-substring heuristic (`historyGenerator.js:128` `includes('wall')`-class), a generator-side idiom a domain-root deriver may not reach (`tests/build/domainGeneratorsBoundary.test.js` freezes the domain→generators edge set). Regex-matching the prose `reason` string is REFUSED by name (the name-string idiom) | **NO TYPED HOME (§441.2)** — UC-1 builds with FIVE live causes; the slot stays typed at ZERO WEIGHT marked `NO_TYPED_HOME`; a typed `founding.kind` on the generation path is R-7 |
| prosperity band | settlement prosperity, normalized 0..1 by `corruption.js` (`spawnCorruptionChance` :320-322 reads `prosperity` beside `crime`/`security`; the tuning table :106-114 names the weights) — UC-1 reads the same band the estate already reads | CONFIRMED |
| civic capacity | government/civic institutions: catalog `priorityCategory: 'government'` (57 rows at b10ed1a1) and the `institutionNature` 'civic' inference (`cohesionWeave.js:268` `/\bhall\b\|court\|assembly\|council\|magistrat/`) — UC-0 pins WHICH read (facet-first, the clandestineFacet law) | CONFIRMED |
| gradient + outfall / terrain | `geographyData.js` terrain classes; the `[D6 THE UNDERWAYS]` tunnel-affinity comments at :268 (mountain rock) and :577 (hill slopes) name the lawful ground | CONFIRMED (relief detail is fabric-side; the engine reads terrain class) |
| plague/fire REBUILD events | `calamityHistory` stamps: `calamityKernel.js:73` typedef `calamityHistory?: CalStamp[]` (`{ type, name, year, tick, deaths, exodus, k, targets }` :77-78), read :203, appended :459 (ring of 8); + historicalEvents. ⚠ **§443 LAW:** `calamityHistory` is a PULSE-written key the observed generation corpus never carries — every NEW reader routes through the banked `src/domain/display/calamityLedger.js` `buildCalamityLedger(settlement)` (:62), NEVER raw (the reader-with-no-writer ratchet mints a row at ceiling 0 otherwise — T2R's second red) | CONFIRMED (read via calamityLedger, §443) |
| high-water population | T2R's `deriveHighWater(settlement)` in `src/domain/highWater.js` (:173 at lane tip; exports `HIGH_WATER_CHANNELS`, `HIGH_WATER_GAPS`, `DEMOTION_THRESHOLD`) — ABSENT at b10ed1a1 (`ls` → No such file); lane tip `387a3b34`, UNLANDED. Read AS RULED at §434 (peakTier precedence; the calamity-path gap a typed UNDERSTATEMENT — never invented) and, per §443, through calamityLedger for channel 3 with the DISPLAY-LAZY law inherited (its consumers dormant or lazy) | PRODUCER-TRAIN DEPENDENCY (T2R; §441.5(f)/(g)) |
| burial demand × institution age | demographics' dead (walk owed at UC-0) × institution age from T2Q's **`institutionFoundingOf(inst)`** in `src/domain/institutionFounding.js` (:109 at lane tip `b3e84008`; `INSTITUTION_FOUNDING_KINDS` :44) → `{ kind: 'FOUNDED', year, tick } \| { kind: 'PRE_SEED' }` — ABSENT at b10ed1a1, UNLANDED. **PRE_SEED age rule (§441.5(g)):** age = the settlement's history age for PRE_SEED institutions, the FOUNDED year otherwise — one rule, pinned in UC-2 | PRODUCER-TRAIN DEPENDENCY (T2Q) |
| quarter trade volume | M6d windowed throughput: `urbanFabricKernel.js:283` (the header row) and :405-406 `// 5. TRADE VOLUME — the M6d windowed throughput (dark ⇒ 0)` / `getSpatialLedger(worldState, 'tradeFlow')` | CONFIRMED (dark ⇒ 0) |
| resource sites (mines/quarries) | T2N's `src/domain/resourceSites.js` — a **PREDICTED path** from draft-PRODUCERS-PLAN: no commit, ref or packet exists (`git log --all` has no T2N; §433 C4 dispatches N at the next free seat) | PRODUCER-TRAIN DEPENDENCY (T2N; path PREDICTED, §441.5(f)) |
| criminal share of the economy | **a stored home EXISTS** — the compile's "NO stored scalar" row and J-10 are REFUTED by measurement (§441.3). `src/generators/safetyProfile.js:619` `const blackMarketCapture = Math.min(80, baseShadowPercent + stressShadowBonus);` (0–80, generation-time; prose :623 "captures an estimated N% of economic activity"; stored on the profile :665), already read as the criminal share by `economicState.js:319-321`, `EconomicsTab.jsx:619`, `DefenseTab.jsx:73`, `viewModelBodySlices.js:99-101/:168`, and — decisively — **`src/domain/corruption.js:539-541`**, which derives the 0..1 crime driver with `compound.criminalEffective` precedence: `const crimEff = Number(sp?.compound?.criminalEffective); const crime = Number.isFinite(crimEff) ? n01(crimEff / 100) : Number.isFinite(sp.blackMarketCapture) ? n01(sp.blackMarketCapture / 80) : 0.3;` | CONFIRMED (§441.3) — home `safetyProfile.blackMarketCapture` with `safetyProfile.compound.criminalEffective` precedence EXACTLY as corruption.js:539-541 reads it; UC-4 consumes that mapping (import or re-export corruption.js's read), never a parallel weighting |
| syndicate power standings | faction power, A.CRIMINAL archetype: `src/domain/factionArchetypes.js:40` `CRIMINAL: 'criminal'`, the classifying regex :69; `corruption.js:418` reuses the engine's `criminalCaptureState` vocabulary — the SEPARATE input to colonization (kept distinct from the criminal share above) | CONFIRMED |
| walls/tolls (smuggler license) | **no engine-side accessor found.** `src/domain/siteGenesis.js` does not exist; the file is `src/domain/townMap/siteGenesis.js` — map-side, importing only `kernel/math.js`, its only wall mentions a `mode === 'fortify'` label (:281) and comments (:267/:385). Engine-side spellings to walk: `src/domain/worldPulse/demographicsObservation.js:106` `{ granary: 0, walls: 0 }`, `warDeployment.js` (siege-at-the-walls prose :355/:435/:498/:561 — no wall FACT), `src/generators/safetyProfile.js:254-292` `inst.hasWalls` ← produced by `priorityHelpers.js:52` `hasWalls: hasAny(names, ['walls','citadel','gates (if walled)',…])` (a name-list idiom) and `historyGenerator.js:128/:170`. ⛔ **`src/domain/undercity/**` may NOT import `src/domain/townMap/**`** — `tests/build/townMapLazy.test.js` guards townMap out of the first-paint closure (§441.5(d)) | **HOME OWED AT UC-0 (§441.5(d))** — UC-0 names the engine-side accessor (and records the facet/name-list idiom it rests on); a STOP if none resolves |

**UC-0 STOP condition:** a license input whose field-level walk finds NO live home and no
lawful derivation is a STOP back to this charter — never an invented default (§311.8.5: every
license has a live home before the stratum compiles). The founding-kind slot is NOT a STOP —
§441.2 ruled it a zero-weight typed slot until R-7.

**The anchor-key law (§441.5(k)):** every component/graph row anchors by the CANONICAL
institution key — `id` when present (`src/domain/settlement.schema.js:744`: `@property {string}
[id] Stable id (e.g. "institution.town_watch"). Optional today; required after entity-registry
migration.`) — else the registry's stable NAME key; never the display name, never a fabric
building id (buildings are fabric-minted, not engine facts). The §437 carry note reads
accordingly (end of file).

**The first-paint closure law (§441.5(d)):** `src/domain/undercity/**` never imports
`src/domain/townMap/**` (the `townMapLazy` guard) — and, by §443's inheritance, any car that
imports `highWater.js` or `calamityLedger.js` inherits their DISPLAY-LAZY law (imported only
from lazy display/dossier surfaces, never the first-paint entry closure; enforced by
`tests/build/vendorPdfLazy.test.js`). Every UC deriver is reached from dormant or lazy
consumers (CT-4 prose, D5 fabric, the pulse seam) — each packet states this and its S0
entry-closure arm proves it.

---

## §4 · THE CARS

Common standard, every car (stated once, priced in each packet):
- **Caps:** ≤400 new/changed effective production lines per wave; each new leaf ≤250
  effective; shared/hot-file delta ≤15 effective — measured with the validated instrument
  (eslint `max-lines`, `skipBlankLines`+`skipComments`; the TET2K/TET2J efflines method),
  executor re-measures at its base.
- **Acceptance:** ≤8 cases per car, closed denominator, argv-form focused checks; the G-43
  directional arms below count inside the eight.
- **Mutants:** branch mutants convicted digest-exact with a clean-tree control; a
  guard-the-guard arm proving the collection live AND the key right (the TE-T2B lesson: an
  anchor-green sweep can still miss an importer — prove by key, not by anchor count); any
  equivalent mutant is pinned as a stated property, then re-planted in a convicting spelling
  (§417's standard).
- **Census (REWRITTEN to T2Q's shape, §441.5(e)):** each car predicts its FIVE-FIGURE delta
  (`+1 files / +0 parked / +1 credited / +N titles / +1 suiteTitles`) in a `censusAuthorization`
  PROSE block of its packet citing **ODQ §311 (the doctrine) + §359.3 (the OB-6 fold) + §431
  (this dispatch) + §441 (the ruling)** — the validator has no such key; it is MF-PREAMBLE
  law. The census walker path (`tests/lint/sovereigntyLightingContract.walker.test.js`) is in
  NO car's `changeManifest`; the row's exact text is deferred VERBATIM to the chair's landing
  act (§417, the T2J/T2Q shape) and never re-stamped across a rebase. A new test file reds TWO
  censuses — the lighting walker's exact tuple AND the negative-assertion anchor walker at
  ceiling 0 — so every negative assertion in the new file carries its `// anchored:` line. The
  compile's "ceiling motion" wording, its C-3 and its "census-ceiling motion unavailable" STOP
  are STRUCK: the test-ratchet debt baseline is a monotone-down CEILING that a new PASSING file
  never touches (a different instrument; never raised to finish a packet), and the memory claim
  that the census "sits AT its ceiling" was re-checked at §363.2 (11 entries, 6 headroom).
  Loop-registered/`test.each` titles are refused (invisible to the census); no parked files.
- **Packet rows (§441.5(i)):** a car's NEW test file is a `CREATE` row; `TEST` is reserved for
  files that already exist (the validator refuses a non-CREATE row whose path does not exist,
  at EVERY status).
- **Dependencies (§441.5(f)):** carried as a prose **"Depends on:"** header line (MF-T2G/MF-T2Q
  form); `dependsOn` and `censusAuthorization` are NOT validator keys. No UC packet that names
  a producer symbol in `requiredSymbols` is minted — not even DRAFT — before that producer's
  landing sha is on the branch.
- **The general form, whole (§441.5(b)):** every component row of every car carries
  `temperament` (the closed 4-enum: `STATIC | MONOTONE | SURFACE_COUPLED | DEMAND_DRIVEN`) and a
  geometry-free `surfaceJoins[]` — each join = a joint kind from the closed vocabulary
  (`grate | stair | sealed_door | sluice | breach`, §311.9.2(iii)) + the anchoring SURFACE
  feature it joins through (church stair, well-house, sewer grate, cellar door — §311.2's
  caused portals). The vocabulary is UC-5's law; its DATA module
  (`src/domain/undercity/jointVocabulary.js`, a frozen list, ≤20 effective) is minted by UC-0
  as T-UC1's first car so that every later row imports one truth — J-R2-2.
- **Pulse-written keys (§443 LAW, post-ruling addendum):** a UC deriver reads
  `calamityHistory` (UC-2's plague-year pits, UC-4's flood/seal state, UC-1's rebuild cause)
  ONLY through `buildCalamityLedger` (`src/domain/display/calamityLedger.js:62`), never raw —
  the observed-shape reader walker refuses a new raw reader of a key only the pulse writes.
- **Purity:** derivers are pure, deterministic, seed-keyed draws only off the settlement's
  existing seed (the district-affinity idiom §311.7.3); `deterministicSort` for any
  iteration; no Date.now, no rng.
- **Anchors:** every packet cites **MF-PREAMBLE** by SHA-256 at its own base (expect
  `0706aad6…` unchanged; §441.7) and ODQ §311 as the doctrine citation; edits into existing
  files use unique anchors (the per-file-exact anchor-walker is a full-gate ratchet — sweep
  `vitest run tests/lint tests/build tests/ops` before the gate, §408's amended law).
- **Any-cast ledger:** monotone-down ceilings refuse widening — cure by typedef (the
  WF-1B/WF-1C law); the file's own idiom decides the spelling that reds.

### UC-0 (`MF-UC0`, T-UC1 #1) · THE EXISTENCE GATE + LICENSE-HOME AUDIT + THE SUBSTRUCTURE FACET
**Depends on:** nothing — compiles NOW (§441.7).
`src/domain/undercity/strataExistence.js` (new leaf, est. ~180 effective) +
`src/domain/undercity/jointVocabulary.js` (frozen data, ≤20 effective — J-R2-2) + inference
rows in `src/domain/spatial/cohesionWeave.js` (shared hot file; MODIFY row) +
`tests/domain/undercityStrataExistence.test.js` (CREATE row).
**Scope:** §311.1 compiled: `deriveStrataExistence(settlement)` → `{ exists, seeds: [...typed
seed rows] }` where seed classes are exactly the doctrine's four (sanitation at earning
tiers; crypt-bearing institutions; cellar-bearing commerce; mine/quarry where terrain
licenses) — each seed row carries its licensing fact and dossier home (the receipts CT-4
cites). Resolves institutions through `facetOf`, never name strings (custom-content parity,
the clandestineFacet law — its CHOKEPOINT, not its key; §3's D-8 defect). Also executes the
§3 audit table at its base, field-level, and records it in the packet — including the
**walls accessor walk (§441.5(d))**: name the engine-side wall fact UC-4's smugglers'-tunnel
license will read (candidates `demographicsObservation.js:106`, `warDeployment.js`,
`safetyProfile.js` `inst.hasWalls` via `priorityHelpers.js:52`), never a `townMap/**` import.
**The facet kind, PRICED here (§441.5(a)):** the §311.1 seed classes have no facet today
(`FACET_INFERENCE` = `institutionNature` + `institutionFunction` only, `cohesionWeave.js:258-275`).
UC-0 mints ONE new INFERENCE-ONLY kind **`institutionSubstructure`** with the closed values
`crypt | cellar | sewer | mine | none` as regex rows in `FACET_INFERENCE` (e.g. crypt:
`/temple|church|cathedral|ossuary|cemetery|crypt|monaster|abbey/i`; cellar:
`/warehouse|cellar|granary|merchant|storehouse|undercroft/i`; sewer: `/sewage|sewer|drain/i`;
mine: `/mine|quarry|pit\b/i` — the executor fixes the rows against the real catalog names at
its base). Constraints: (1) measured against the **≤15 effective shared/hot-file cap** on
cohesionWeave.js (headroom measured with the validated instrument BEFORE the edit); (2)
**golden-inert PROVEN, not asserted, at UC-0's S0** — generation never reads facet kinds, and
the generator golden run at S0 is the proof; (3) **catalog `facets` additions are REFUSED as
R-2 golden-shift** (`assembleInstitutions.js:296-299/:355` spread the entry onto the hashed
settlement — any `facets` key becomes settlement bytes); (4) the KEY SPELLING UC-0 reads is
pinned with a POSITIVE arm against the REAL catalog row: `facetOf(<the :886 'Underground
network' row>, 'subterranean') === 'subterranean'`, and the `institutionSubstructure` values
against real catalog names — never a self-supplied fixture spelling; (5) covered by
`tests/domain/cohesionWeave.test.js`'s existing inference pins plus the new file.
**The sanitation seed reads the SAME roster as UC-1 (§441.1):** a sanitation seed exists
where the roster carries 'Sewage system' (full-web class) or a custom institution declaring
the sanitation/drains facet, or where UC-1's derived rung is ≥ culvert — §311.7.1's
"cesspits contribute no sheet" stated as **rung ≥ culvert**. UC-0 and UC-1 share ONE read of
the roster institution; they can never disagree about whether the sewer institution exists.
**Non-goals:** no undercity-proper predicate (UC-4's), no geometry, no sheet rendering, no
catalog edit.
**Acceptance (≤8):** no-seed → no sheet · one modest seed → sheet exists (the two-level
reading's first level) · facet-declared custom institution counts (incl. a custom
sanitation/drains facet) · marsh/floodplain forbiddance honored · deterministic same-seed ·
a seed-class enumeration pin (closed vocabulary) · G-43 arm: remove the last qualifying
institution → exists flips false · the POSITIVE KEY arm: the real catalog 'Underground
network' row resolves `'subterranean'` through `facetOf`, and a roster carrying 'Sewage
system' yields a sanitation seed of full-web class (one truth with UC-1).

### UC-1 (`MF-UC1`, T-UC1 #3) · THE SEWER LADDER + WELLS/CISTERNS (the surface-coupled car)
**Depends on:** T2R LANDED (`deriveHighWater`, `src/domain/highWater.js`) — compiles after its
landing sha is on the branch; no packet minted before it (§441.5(f)). Reads T2R's reader AS
RULED at §434 (peakTier precedence first, the sealed tier-vs-popToTier read as residual; the
calamity-path gap a typed UNDERSTATEMENT the reader never invents) and per §443 through
calamityLedger for its channel 3 — so a demoted-then-recovered town's rung is honestly
conservative (§441.5(g)).
`src/domain/undercity/sewerDerivation.js` (new leaf, est. ~240 effective — wells/cisterns add
~10 over the compile's 230) + `tests/domain/undercitySewerDerivation.test.js` (CREATE row).
**Scope:** §311.7 compiled whole: the four-rung ladder (cesspits → culvert/market drain →
quarter network → full web) under the **§441.1 one-truth rule** — **FULL WEB iff the roster
carries 'Sewage system' (`institutionalCatalog.js:2225`, city block, rolled at city AND
metropolis via `assembleInstitutions.js:243`) or a custom institution declaring the
sanitation/drains facet; cities and metropolises carry a FLOOR of QUARTER NETWORK (the
owner's "all cities/metropolises have sewers" kept as drains in the planned quarters); towns
derive by the causes between cesspits / culvert / quarter network and are CAPPED BELOW full
web without the institution** (the owner's "only SOME towns"). The compile's "top rung
ALWAYS" is struck: it contradicted the dossier roster in ~60% of cities (baseChance 0.4) — a
second truth. The output names its `rungSource` (`ROSTER_FULL_WEB | TIER_FLOOR | DERIVED`).
**Causes (§441.2):** UC-1 builds with **FIVE live typed causes** with stated weights —
prosperity band · civic capacity · gradient+outfall (a flat dry town honestly gets cesspits) ·
plague/great-fire REBUILD in the immutable event log (read via calamityLedger, §443) ·
high-water population past the cesspit-failure line (T2R's reader) — plus the
planned/charter/military founding slot kept in the typed weight table at **ZERO WEIGHT,
marked `NO_TYPED_HOME`** (§3: the field §311.7.2 named does not exist). **Regex-matching the
prose `founding.reason` string is REFUSED by name** — the name-string idiom this charter
forbids everywhere else. The slot wakes when R-7 lands a typed `founding.kind` on the
generation path (the endgame's ONE REGEN is its natural vehicle). CERTAIN above the derived
threshold, ABSENT below the floor, weighted-still-diced seed-keyed in the declared middle
band. Per-quarter coverage flags keyed to the district 12-enum (`districtProfile.js:33-37`,
PINNED not imported — the urbanFabricKernel's zero engine→display-coupling law); mains extend
only under rings whose vintage postdates the works (reads surface epoch vintage where lit;
dark ⇒ tier default, honestly coarse).
**Wells/cisterns (MOVED here from UC-3, §441.5(b)):** §311.8.2(c)'s second SURFACE-COUPLED
component — license: water need (the 'Aqueduct or water system' row at city+, `:2239`,
required; tier-scaled need below it); anchor: the quarter/well-house; extent: tier-scaled
water need; `temperament: SURFACE_COUPLED`; `surfaceJoins: [{ kind: 'stair', feature:
'well-house' }]`-class rows. They do not ladder, but they are coupled to the surface's water
placement, which is why they live in the surface-coupled car and not the static one.
**Every ladder/well row carries `temperament` + `surfaceJoins[]`** (sewer grate joins for the
network rows; §311.2).
**The §311.6.1 compile-time question is ANSWERED by §311.7.2's cause table AND §441.1's roster
rule together** — no causeless coin flip survives, and no rung contradicts the roster.
**Constants (§441.6):** the weights land as ONE frozen bounded table, `SEWER_DERIVATION_TUNING`,
the `UNDERWAYS_TUNING` precedent — typed, bounded, owner-retunable, flagged as a TUNING-PASS
INPUT (§8 R-3); no free scalar. **PROVISIONAL until the §362.4 tuning signature;
EXPOSABLE-PROVISIONAL** (§7 term 5): CT-4 may compile and expose rung/cause sentences against
these defaults exactly as every other pre-signature surface does under §337 and §341. The
packet NAMES THE DEFAULTS' PROVENANCE — who chose each weight and from what measurement (the
executor's own seed-sweep over the corpus at its base, recorded in the packet; never a
remembered number). The owner may veto to "dark until signed" by a word.
**Acceptance (≤8):** the roster rule — 'Sewage system' in the roster → FULL WEB; a city or
metropolis without it → QUARTER NETWORK (the floor; never full web); a town without it →
capped below full web (§441.1) · flat-dry-town honestly gets cesspits (owner's own example) ·
G-43 arm: plague-at-year-N rebuild → drains appear in the rebuilt quarter · the HONEST
founding arm (replaces the compile's founding-kind flip, §441.2): the `NO_TYPED_HOME` slot at
zero weight is a pinned NO-OP — perturbing its input moves nothing — while each of the five
live causes moves the rung MONOTONE in its declared direction · organic faubourg stays
uncovered while the planned quarter drains (per-quarter law) · middle-band determinism (same
seed, same rung) · threshold/floor boundary cases · wells/cisterns: tier-scaled extent and the
water-need license, every row with temperament + surfaceJoins.

### UC-2 (`MF-UC2`, T-UC3 #1) · THE MONOTONE COMPONENTS
**Depends on:** T2N LANDED (`src/domain/resourceSites.js` — a PREDICTED path until T2N's
packet names it; no commit, ref or packet exists at b10ed1a1) AND T2Q LANDED
(`institutionFoundingOf`, `src/domain/institutionFounding.js`). No packet minted before BOTH
landing shas are on the branch (§441.5(f)).
`src/domain/undercity/monotoneComponents.js` (new leaf, est. ~250 effective — AT the leaf
cap; if the executor's measurement crosses it, split crypts from cellars/mines as two leaves
inside the same car) + `tests/domain/undercityMonotoneComponents.test.js` (CREATE row).
**Scope:** §311.8.2(b) compiled: crypts/ossuaries (license: burying institutions + cemetery
zoning; anchor: the owning institution/quarter; extent: burial demand × institution age;
plague-year surge pits from `calamityHistory`); cellars/undercrofts (license: storing
commerce; anchor: the commerce quarter; extent: quarter trade volume, dark ⇒ 0; the smuggler
subset licensed by criminal share AT gates/waterfronts); mines/quarries (license+anchor:
T2N's resource sites; extent: worked-while-worked, ABANDONED not erased — the monotone
high-water case). Institutional vaults/dungeons per §311.8.3: under the owning institution,
licensed by its type, rare event-driven growth, posture SEALED. **Every row carries
`temperament: MONOTONE` and `surfaceJoins[]`** (church stair for crypts, cellar door for
undercrofts, the pit-head for mines — joint kinds from `jointVocabulary.js`; §441.5(b)) and
anchors by the canonical institution KEY (§3's anchor-key law). Smugglers' TUNNELS are NOT
here — they are UC-4's demand-driven row (§441.5(c)); the smuggler CELLAR subset of (b) stays.
**The PRE_SEED age rule (§441.5(g)), one rule, pinned:** institution age = the settlement's
history age for institutions whose `institutionFoundingOf(inst)` is `{ kind: 'PRE_SEED' }`;
the FOUNDED `year` otherwise — the founding-era cathedral is as old as the town, never
ageless and never invented. Plague-year surge pits read `calamityHistory` ONLY through
`buildCalamityLedger` (§443).
**Consumes** T2N's and T2Q's derivers — never re-derives their facts (one truth per fact).
**Acceptance (≤8):** G-43 arm: move the resource site → the mine moves · monotonicity pin:
advancing time never shrinks crypt extent · abandoned-not-erased on resource exhaustion ·
plague year mints the surge pit · cellar extent tracks trade volume with dark ⇒ 0 · vault
exists only under its licensed institution type · smuggler-cellar subset requires
gate/waterfront + criminal share · the PRE_SEED age rule: a PRE_SEED temple's crypt extent
equals a FOUNDED-at-year-0 temple's (one rule, two spellings, same number).

### UC-3 (`MF-UC3`, T-UC1 #2) · THE STATIC COMPONENTS
**Depends on:** UC-0's `jointVocabulary.js` (same train, earlier car) — otherwise nothing;
compiles NOW (§441.7).
`src/domain/undercity/staticComponents.js` (new leaf, est. ~100 effective — wells/cisterns
left) + `tests/domain/undercityStaticComponents.test.js` (CREATE row).
**Scope:** §311.6.3/§311.8.2(a): caverns as seed-permanent terrain facts — licensed by the
ground (`geographyData` terrain classes; the tunnel-affinity comments at 268/577 name the
lawful ground), keyed off the settlement seed, epoch-invariant, never grow nor diminish.
Every row carries `temperament: STATIC` and `surfaceJoins[]` (a cave mouth is a `breach`-kind
join to the terrain feature it opens from; an isolated cavern carries an EMPTY list and is
lawful — §311.6.4). **Wells/cisterns are NOT here (§441.5(b)):** the compile filed a
SURFACE-COUPLED component in the STATIC car, so an executor would have recorded the wrong
temperament; they moved to UC-1 as the second surface-coupled component.
**Acceptance (≤8):** epoch invariance (same caverns at year 0 and year 300) · terrain
licensing (no cavern in the floodplain) · seed determinism · every row declares
`temperament: STATIC` + a `surfaceJoins[]` drawn from the closed joint vocabulary (empty
allowed, wrong kind refused) · G-43 arm: terrain-class flip → cavern license flips.

### UC-4 (`MF-UC4`, T-UC2 ALONE) · THE UNDERCITY EPOCH LAYER (colonization + the vertical high-water law) — THE FLAG-MINTING WAVE
**Depends on:** T2R LANDED (the vertical mark composes with `deriveHighWater`'s channels and
inherits its §443 lazy law); UC-0 landed (seeds) — compiles after T2R's landing sha is on the
branch; no packet minted before it (§441.5(f)). **Rides ALONE as T-UC2 (§441.4):** a
flag-minting wave is a train boundary (PACKET_STANDARD: first member or alone), and no other
UC car is a no-flag slice of this flag.
`src/domain/undercity/colonization.js` (new leaf, est. ~250 effective) + ONE pulse-seam
signal behind ONE NEW virtual flag + `tests/domain/undercityColonization.test.js` (CREATE row)
+ the four-surface flag bill below.
**Scope:** the second level of §311.4's two-level reading: `deriveUndercity(settlement)`
computes the colonization of the seeded spaces PROPORTIONAL to (economy, criminal share,
syndicate standings) — a derivation, never a dial. **`drivers.criminalShare` CONSUMES the
estate's one stored truth (§441.3):** `safetyProfile.blackMarketCapture` with
`safetyProfile.compound.criminalEffective` precedence EXACTLY as `corruption.js:539-541`
reads it (`crimEff` finite ⇒ `n01(crimEff/100)`, else `blackMarketCapture` finite ⇒
`n01(blackMarketCapture/80)`, else `0.3`) — by importing or re-exporting corruption.js's read,
NEVER a parallel weighting (the compile's institution-weight + faction-power derivation is
STRUCK with J-10). A.CRIMINAL faction power remains the SEPARATE `syndicates[]` input.
Colonization ORDER per
§311.8.4: from the seeds nearest the surface causes (shadows district, waterfront, gates),
every colonized piece bound to a surface cover by the UNIVERSAL FRONT (the §175.1 truth
roster: REAL institutions seated below — criminal floors under their fronts; anonymous
fabric otherwise; NO minted clickable truth). Consequence states per §311.8.3: deep workings
near the water table FLOOD; plague years and collapses SEAL — use recedes, dug space never
does. Fossils (abandoned galleries, sealed doors, flooded sections) derive from RECESSION:
syndicate standing now vs its high-water mark. Flood/seal state reads `calamityHistory` ONLY
through `buildCalamityLedger` (§443).
**Smugglers' tunnels — THIS car's demand-driven component row (§441.5(c)):** temperament (d)
DEMAND-DRIVEN; license = the wall/toll fact (UC-0's named engine-side accessor, §441.5(d)) +
the criminal share; anchor = gate / waterfront; extent = the contraband flow (composed from
trade volume × criminal share); `surfaceJoins[]` at the gate-side cellar door / the waterfront
sluice. The §311.8.5 arm **remove the wall → the smugglers' tunnel loses its license**
registers on THIS row; UC-5's edge arm (the tunnel's link loses its motive) is its
CONSEQUENCE, never a second cause. Every component row carries `temperament` + `surfaceJoins[]`
and anchors by the canonical institution key.
**The one non-deriver piece:** the vertical high-water MARK cannot be derived from a ring
buffer that forgot it — exactly MF-D5's finding, already ACCEPTED IN PRINCIPLE at §359.5
(the signal persists as its own field, concrete shape CHAIR-REVIEWED at the member that
builds it). **UC-4 IS the first builder of the §359.5 persisted-signal shape (§441.4)** — no
MF-D5 packet exists (0 hits in INDEX.md/PACKET_MANIFEST.json at b10ed1a1), so there is no
idiom to consume; the compile's "consume MF-D5's idiom if landed first" branch is STRUCK.
UC-4's packet carries the CONCRETE shape (field name, the ring-independent monotone write,
its reader) to the chair for the §359.5 mechanical review — **C-2 at its packet**.
Pulse-seam only, behind the dormancy idiom — **a NEW virtual flag ABSENT from
`DEFAULT_SIMULATION_RULES`** (candidate name `undercityHighWaterEnabled`; the packet fixes it),
read exactly as the precedent reads its own: `(context.simulationRules ||
worldState?.simulationRules || {}).<flag> === true` (`institutionLifecycle.js:661`); dark ⇒
zero writes ⇒ byte-identical; S0 measures the dark path with the two-part reading (§397).
**THE FLAG BILL, PRICED (§441.4 — four surfaces + the bundle; validator and walker teeth on
each):**
1. `src/domain/worldPulse/simulationRules.js` — one row in `ENGINE_GATED_VIRTUAL_RULE_KEYS`
   (:185; the precedent key at :351). A shared hot file: one line, inside the ≤15 cap.
   **`npm run build:edge-shared` (package.json:27) runs in the same commit whenever this file
   moves** — it sits in two closures; a stale edge-shared bundle is a landing red.
2. `src/domain/certification/subsystemRowsVirtual.js` — the AUTHORED certification row for
   the flag, **in the SAME commit** as the manifest row (the TR-1 incident:
   `engineGatedRuleKeys.walker` :17-28/:46-47 requires every gated key to be manifest-listed,
   gate-read in src, and authored in `VIRTUAL_SUBSYSTEM_ROWS`, pending nowhere;
   `tests/domain/subsystemRowsVirtual.test.js` reds otherwise). The precedent row
   (`underwaysOrganicFoundingEnabled`, :989-1013) is ~60 source lines of prose data.
   **EXEMPT from the ≤15 shared-file delta as AUTHORED DATA** in a rows module that grows by
   exactly one row per flag (§441.4 J4; vetoable). **Reversal:** if the chair withdraws the
   exemption, the row lands as its own rows file admitted by the module, and the cap applies
   to the admitting edit.
3. `tests/domain/subsystemRowsVirtual.test.js` — the `VIRTUAL_RULES` ordered-equality list
   (:176 onward, AUTHORING ORDER, an exact ordered equality) gains the key at its section
   position; `tests/lint/engineGatedRuleKeys.walker.test.js` (:705-707 pattern) sees the gate
   read. An existing-file `TEST` row.
4. The acceptance file drives the flag LITERALLY — `<flag>: true` written in
   `undercityColonization.test.js` for the lit arms (the mechanism-lit coverage walker,
   `tests/property/mechanismLitCoverage.test.js`, reads the literal).
Budget: the leaf ≤250 + the four surfaces; the ≤3-modified-production-files cap holds
(simulationRules.js, subsystemRowsVirtual.js, the pulse-seam file).
**DARK-BY-DEFAULT (§441.4):** lighting the flag in `DEFAULT_SIMULATION_RULES` is same-seed
PULSE motion — the class §434 docketed to the owner as §433-O2's first instances — and is
**R-5**. Until lit, `F5.fossils[]` and `drivers.highWater` are DARK on every live world: the
packet says so, §7 F5 carries the caveat, and CT-4's contract term 3 FORBIDS asserting fossils
on the dark path (the quiet-world vacuity class: a dark fence holds over an empty list).
**§423 compliance:** the signal rides existing registered pulse machinery (a payload
enrichment at an existing seam, the T2Q pattern) — no new verb; an S0 arm proves no door
verb is needed.
**Acceptance (≤8):** no criminal drivers → sheet without undercity (two-level reading) ·
colonization proportional-monotone in syndicate power, with `drivers.criminalShare` EQUAL to
corruption.js's read for the same profile (one truth, §441.3) · receding syndicate leaves
fossils — on the LIT path (`<flag>: true` literal); the dark path pins the ABSENCE honestly,
never a fossil · inertia: new digging never re-rolls old crypts (compose with UC-2's output,
read-only) · flood/seal sever-not-delete · dark-flag byte-identity (S0, two-part reading) ·
UNIVERSAL FRONT: every colonized piece names its surface cover · G-43 arm: remove the wall →
the smugglers' tunnel row loses its license (§441.5(c)).

### UC-5 (`MF-UC5`, T-UC3 #2) · THE CONNECTIVITY GRAPH
**Depends on:** UC-1, UC-2, UC-3, UC-4 LANDED (it consumes their component rows and their
`surfaceJoins[]`), hence after T-UC1, T-UC2 and UC-2 — the last car of the program.
`src/domain/undercity/connectivity.js` (new leaf, est. ~220 effective) +
`tests/domain/undercityConnectivity.test.js` (CREATE row). The typed-joint VOCABULARY is
UC-5's law (§311.9.2(iii)) but its data module was minted by UC-0 (`jointVocabulary.js`,
J-R2-2) so every earlier component row could carry it — UC-5 imports it, never re-declares.
**Scope:** §311.9 compiled at COMPONENT level (geometry-free): the three connection classes
(NATIVE free by construction; ADJACENCY BREACH licensed by measured proximity + an actor
with a motive; FUNDED LINK with payer + route motive + DATE as a derived-provenance row) and
the REFUSAL class (vaults/dungeons connect to nothing by default; keepers resist
sewer↔crypt joins). The seven laws as graph invariants: motive law (motiveless joins are
not dug — sparsity) · route value from surface causes (remove the wall → the tunnel loses
its motive) · the security gradient with the stricter side's TYPED JOINT (grate, stair,
sealed door, sluice, breach — the vocabulary rows land here as data; their DRAWN joints are
D3a-era portal work, fabric-side) · sewers-as-spine vs ARCHIPELAGO character by composition
with UC-1's rung · connectivity-is-state (flood/seal sever edges without deleting; dated
restore acts) · links persist (high-water on edges) · the honesty contract (isolation is a
first-class visible fact: the counting-house cellar NOT meeting the sewers is itself the
game). One-way seal restated: outputs feed crime/espionage ONLY through the §287.4 receipt
seam, never same-pass.
**Acceptance (≤8):** vault refusal default · funded link carries payer+motive+date ·
remove-the-wall EDGE arm — the tunnel's link loses its motive as the CONSEQUENCE of UC-4's
component-row license loss (UC-4 registers the primary arm; §441.5(c)) · archipelago-vs-spine
follows UC-1's rung (a 'Sewage system' roster ⇒ spine; §441.1) · sever-not-delete round trip
· isolation visibility pin · cross-posture joint takes the stricter side · graph rows anchor
by the canonical institution key and consume each component's `surfaceJoins[]` as the portal
truth (one producer per join).

**Interior reds, all three trains, named now (§441.5(e)):** each car's new test file moves the
lighting census's exact tuple (and the anchor walker at ceiling 0 if any negative assertion
lacks its `// anchored:` line) until the chair's landing act inserts the deferred row — the
predicted five-figure delta is in each packet's `censusAuthorization` prose block; no other
interior red is authorized. Truncation to a green prefix is a complete lawful train.

---

## §5 · OB-6 — THE ROSTER-EXPANSION DECISION (folded here; a second truth refused)

**Source:** §175.3 ("OB-6 — expanding the undercity's REAL roster (engine-side generation
ask, optional)"), from MF-R2's escalation #1: *"More real entries below = owner-gated
escalation #1"* — the audit's cure C3 having established the truth roster (real institutions
seated below; criminal floors under fronts; anonymous fabric otherwise; a clickable
institution the dossier never generated would be fabric-invented truth).

**THE RECOMMENDATION (owner-visible, since it changes what players see): DECLINE AS
DEFAULT.** The §311.8 components are not institutions — they are underground structure
anchored to SURFACE institutions the catalog already generates; the undercity-proper
colonizes with REAL criminal institutions that already exist (27 criminal-priority entries,
'Underground network' at three tiers, 'Underground city' at metropolis). The doctrine
compiles completely with zero new catalog rows, and the truth-roster law renders everything
else as honest anonymous fabric. Expanding the roster adds owner-visible content the
doctrine does not require.

**The optional car, priced for the owner's word (UC-R, rides NONE of the three trains — its
own gated wave):** new catalog entries (candidates: an ossuary/catacomb entry at city+ tiers; a
bathhouse/cistern-works entry) are a **G2-track golden-shifting act** — the catalog header's
own class. Bill: the declared-shift machinery in full (§72.3 key-by-key re-record with a
zero-added/zero-removed diff on non-target keys, the §110.3 three-part proof, the soak, the
owner signature); census motion for the parity tests
(`underwaysInstitutionParity.test.js` widens); dossier-home rows for each entry (desc
variants, vocabulary, services); est. one car, ≤300 effective across catalog+data files.
On "yes" it schedules as a flag-free single-member wave at a train boundary; on silence it
stays parked HERE, and no later lane re-derives the question (§359.3's one-truth order).

---

## §6 · OB-7 — RULED: STAYS DEFERRED, SPEC RECOVERED AND NARROWED

**The lost spec, recovered verbatim this sitting** (MF-R2 lane transcript, §2/R-escalations
of `laneMFR2-reconciliation.md`):

> **Escalations (both optional, both owner-gated, neither blocking):** #1 real undercity
> institutions beyond the surface roster (R2); #2 engine-recorded relocation events IF the
> owner wants marginalia quoting engine history instead of derived provenance (R5).

**RULING (chair-tier, explicit per §431's rule-or-defer order): OB-7 REMAINS DEFERRED,**
now with its condition narrowed from "revisits at the undercity train's charter" to a
concrete trigger: it activates ONLY if the owner asks for map/dossier marginalia that QUOTE
engine event history for institution relocations. The undercity train does not need it —
§359.5's canonical-truth doctrine (derive projections, never store parallel state) and
MF-R2's own C5 register (new-sewer/tunnel events as derived provenance rows) are the ruled
cures this charter follows; UC-4/UC-5 date their acts from derived provenance and
`calamityHistory`, which existing machinery already records. The spec text above is now IN
the ledgered record via this charter, so the loss of the lane doc no longer degrades the
decision. No car depends on it; no re-derivation is owed.

---

## §7 · THE CT-4 FACT CONTRACT (the clerk-never-writer interface)

CT-4 (the content train's undercity prose chapter, §361.1) compiles AFTER this train and may
ground a sentence ONLY in these typed facts (§360.1's law 2: only-explain-what-the-engine-
knows; mechanisms the engine does not simulate are never asserted as this-town facts). The
contract, fact by fact — name · producing car · derivation inputs · what prose may say:

| # | typed fact (shape) | car | inputs | the sentence class it licenses |
|---|---|---|---|---|
| F1 | `strataExistence: { exists, seeds[] }` — each seed: class + licensing fact + dossier home (the sanitation seed names the roster institution — 'Sewage system' or the sanitation-facet custom — or the derived rung ≥ culvert; §441.1) | UC-0 | institutions (facet-resolved through `facetOf`, incl. the `institutionSubstructure` kind), the sanitation roster, terrain | "why this town has an underground at all" — cite the actual seeds; where the seed is the sewer institution, cite THAT institution (the dossier already lists it) |
| F2 | `surfaceCoupled: { sewerLadder: { rung, rungSource: ROSTER_FULL_WEB\|TIER_FLOOR\|DERIVED, causes[], perQuarterCoverage }, wells[] }` — causes = the five live typed causes with weights + the `NO_TYPED_HOME` zero-weight slot; every row with temperament + surfaceJoins | UC-1 | the roster rule (§441.1), the five typed causes (§441.2), T2R's reader, water need | "why the drains exist / stop at the faubourg" — when `rungSource` is ROSTER_FULL_WEB cite the 'Sewage system' institution; when TIER_FLOOR say the planned quarters drain; when DERIVED cite the winning cause(s) and the uncovered quarters. **PROVISIONAL until the tuning signature — exposable-provisional (term 5)** |
| F3 | `monotoneComponents[]` — kind, license, anchor (canonical institution KEY — id when present, else the registry's stable name key; §441.5(k)), extent, abandoned?, `temperament: MONOTONE`, `surfaceJoins[]` | UC-2 | burial demand, `institutionFoundingOf` (PRE_SEED rule), trade volume, resource sites | "whose crypt, whose cellar, which seam" — anchored to REAL institutions by key |
| F4 | `staticComponents[]` — caverns with terrain license, `temperament: STATIC`, `surfaceJoins[]` (wells MOVED to F2, §441.5(b)) | UC-3 | terrain, seed | "what the ground gave" |
| F5 | `undercity: { present, colonizationShare, drivers { economy, criminalShare, syndicates[], highWater }, components[] (incl. smugglers' tunnels — `temperament: DEMAND_DRIVEN`, surfaceJoins), fossils[] }` — fossils: kind (abandoned/sealed/flooded) + cause + date. **`criminalShare` = corruption.js:539-541's read (§441.3). `fossils[]` and `drivers.highWater` are DARK-BY-DEFAULT (§441.4): empty on every live world until R-5 lights the flag** | UC-4 | the stored criminal share, faction standings, the flag-gated high-water signal, calamityHistory via calamityLedger (§443), the wall/toll fact | "who grew it, and what receded" — the epoch story; dated fossils ONLY on the lit path (term 3). On the dark path CT-4 compiles a grounded residue (the colonization story without recession claims) |
| F6 | `connectivity: { character: spine\|archipelago, edges[], isolations[] }` — edges: class + posture + joint type (from `jointVocabulary.js`) + live/severed + date; rows anchored by canonical institution key; consumes each component's `surfaceJoins[]` | UC-5 | UC-1..UC-4 outputs, the wall/toll fact, proximity | "what meets what, and what pointedly does not" — the honesty contract in prose |

Contract terms, binding on all three trains and on CT-4's compile:
1. Every fact is same-seed deterministic at a fixed world state; CT-4's section-sliced
   agreement pins compare against these derivers' outputs, never against prose (the
   doc-agreement vacuity law: slice the section, compare ordered lists).
2. Fact names and shapes above are the INTERFACE — a UC executor changing a shape after
   CT-4 compiles owes CT-4 a re-compile flag (recorded in the UC packet as a consumer).
3. Prose NEVER invents an underground element outside F1–F6, and never asserts geometry
   (routes, distances, plans) — geometry is D5's, not compiled yet. Setting-agnostic per
   §360.1(3): mechanisms travel, geography does not. **CT-4 NEVER asserts a fossil, an
   abandonment, a sealing or a high-water recession on the DARK path (§441.4):** while the
   UC-4 flag is absent from defaults, `F5.fossils[]` is structurally empty on every live world,
   and a sentence grounded in an empty list is the quiet-world vacuity — CT-4's pins for F5
   run on BOTH paths and the dark-path pin asserts the absence.
4. The fabric's D5 wave consumes the SAME facts through §287.4's StrataExistencePlan — one
   producer, two consumers, zero second truths.
5. **Tuning exposure (§441.6, chair-ruled, vetoable; flagged as a graze of §362.4):**
   `SEWER_DERIVATION_TUNING` and the UC-2/UC-4 extent coefficients are PROVISIONAL until the
   tuning signature and EXPOSABLE-PROVISIONAL — CT-4 may compile and expose rung/cause
   sentences against the provisional defaults exactly as every other pre-signature surface
   does under §337 (the soak gates nothing) and §341 (tuning is LAST and signs everything at
   once). Each UC packet names the defaults' PROVENANCE (who chose them, from what measurement).
   The owner may veto to "dark until signed" by a word, and CT-4's F2/F5 sentences then wait.
6. **Anchors (§441.5(k)):** every F3/F5/F6 row anchors by the canonical institution key — `id`
   when present (optional today, `settlement.schema.js:744`), else the registry's stable name
   key; never the display name; never a fabric building id. The fabric resolves the key to its
   building at D5; CT-4 resolves it to the dossier's institution entry.

---

## §8 · THE RAISED SPLIT

**OWNER docket (§441.8 — parked, each with the chair's recommendation as ruled; nothing in
the six cars' DEFAULT build triggers any of them):**
- **R-1 OB-6** — the roster expansion recommendation + the priced optional car (§5).
  Chair's recommendation (§441.8): **DECLINE** (unchanged).
- **R-5 LIGHT the undercity high-water flag in `DEFAULT_SIMULATION_RULES`** (§441.4) — a
  same-seed PULSE shift (§434's class, §433-O2); bill = the soak + the declared-shift proof
  (§72.3/§110.3). Chair's recommendation (§441.8): **light it in the tuning pass with the map
  leg** (§341) — until then F5's fossils are dark and CT-4 says nothing of them.
- **R-6 make 'Sewage system' `required: true` at city+** (the catalog alternative to §441.1's
  roster rule) — a G2 golden-shifting catalog edit + the metropolis roster. Chair's
  recommendation (§441.8): **NOT recommended** — the §441.1 one-truth rule makes the roster
  honest as it stands (a city without the institution has quarter drains, not a full web).
- **R-7 a typed `founding.kind` on the generation path** (§441.2) — wakes UC-1's sixth cause
  and the G-43 founding-kind flip; a generation-path field = the full declared-shift bill.
  Chair's recommendation (§441.8): **bundle into the endgame's ONE REGEN as a declared shift**,
  so UC-1's sixth cause wakes then; until then the slot sits typed at zero weight.
- **R-2 Same-seed generation motion** — the charter's design has NONE (all cars
  deriver/pulse-side). If any executor finds a car inexpressible without a generation-path
  write, that is a STOP and the declared-shift bill (golden re-record §72.3, §110.3
  three-part proof, soak, signature) goes to the owner BEFORE the car builds. The
  generation-side underways founding mention (`historyGenerator.js:713`) stays deferred —
  resurrecting it is R-2 by definition.
- **R-3 Tuning-flavored constants** — `SEWER_DERIVATION_TUNING` and the UC-2/UC-4 extent
  coefficients land as bounded typed tables with defaults (the UNDERWAYS_TUNING precedent,
  landed law), and are REGISTERED AS TUNING-PASS INPUTS beside the 300y-runaway and §341 map
  leg; the tuning signature act itself is constitutionally owner (§362.4) and is not
  pre-signed by the §362 grant. Chair's recommendation (§441.8): **unchanged**; §441.6 rules
  the defaults EXPOSABLE-PROVISIONAL in the interim (§7 term 5), owner-vetoable to dark.

**Standing owner CLASSES (not docket items; they fire only if an executor trips them):**
- **R-4 New user-facing surface** — this train ships none. The two exposure routes (CT-4
  prose; D5 strata leaf) carry their own gates; any interim surfacing of F1–F6 in a live
  dossier panel would be a new-surface act → owner.

**CHAIR (ruled at §441 unless marked):**
- **C-1 RULED (§441.7)** — the fabric-vs-engine split (§2) and the six-car roster stand; the
  topology is THREE trains (T-UC1 = UC-0/UC-3/UC-1 · T-UC2 = UC-4 alone · T-UC3 = UC-2/UC-5)
  with the producer coupling per car (§0) and the queue interleave unchanged. Vetoable.
- **C-2 OPEN at UC-4's packet (§441.4)** — UC-4's persisted high-water signal concrete shape:
  UC-4 IS the first builder of the §359.5 shape; there is no MF-D5 idiom to consume; the packet
  carries the concrete shape to the chair for the mechanical review before its GO.
- **C-3 STRUCK (§441.5(e))** — there is no census "ceiling motion"; the census bullet in §4
  carries the T2Q shape (predicted delta in prose; row text deferred to the landing act).
- **C-4 RULED (§441.7)** — no stamp question: the cars file as MF-UC0…MF-UC5 under the
  stamped town-cartography family, citing MF-PREAMBLE by SHA unchanged + ODQ §311; NO
  UC-PREAMBLE; J-8 superseded. Reversal named in the header.
- **C-5** The GENERATION-SPEC §13/§168 fold — rides the chair's next spec touch (§9).
- **C-6 (§441.1, vetoable)** — the roster rule refining §311.6.1 is a chair refinement under
  the §311 delegation; the owner's two sentences ("all cities/metropolises"; "only SOME
  towns") are both honored by floor + ceiling.

---

## §9 · DEFERRED ROWS (deliberately deferred — documented, not bugs to re-find)

| row | what | wakes when |
|---|---|---|
| D-1 | GENERATION-SPEC §13/§168 fold (the §311.5 L2 routing) — one edit at the next spec touch, ledger-side | the chair next touches map-corpus/docs/GENERATION-SPEC.md |
| D-2 | OB-7 (§6, spec recovered) | the owner asks for engine-quoted relocation marginalia |
| D-3 | UC-R the OB-6 optional car (§5, priced) | the owner's word |
| D-4 | Fabric-side strata geometry (G-27/G-54/D5; the −1 leaf, registration ghost, drawn joints) | the map program's D5 wave, town-cartography family, dormant until D3b |
| D-5 | Player-commissioned digs as store operations (§423 two-layer shape) | a product ask; none exists |
| D-6 | The typed-joint DRAWN vocabulary joining the D3a-era portal work (§311.9.3's routing) | D3a portal members; UC-5's data rows are the source of truth |
| D-7 | `historyGenerator.js:713` underways founding mention (pre-existing deferral, generation-path) | only with R-2's owner bill |
| D-8 | FOUND ESTATE DEFECT (§3): `clandestineFacet.js:50` reads facet kind `'institutionFunction'` for `'clandestine'` while the catalog declares the key `clandestine` — the precedent's own 'Underground network' rows resolve null; its tests pass on a fixture spelling | its own micro-fix car (never cured silently by this train; UC-0 pins its OWN key spelling instead) — J-R2-1 |
| D-9 | The `pulse-time-writer` exemption mechanism (§443 C-b) that would let `highWater.js`/UC cars read `calamityHistory` raw | the trailing OSR mint (§384.2); until then every UC reader routes through `buildCalamityLedger` |

---

## §10 · EXECUTION LAWS CARRIED (the fresh estate, so no executor rediscovers them)

- **§408 sweep:** pre-gate sweep is `vitest run tests/lint tests/build tests/ops`; a new
  migration owes a new reviewed wave in the rehearsal manifest — **this train mints NO
  migration** (affirmative non-goal; the pulse signal is world-state, not a DB table).
- **§397 two-part S0:** an S0 reading is the exit AND the printed staleness line — an exit
  alone convicts nothing (ERR_MODULE_NOT_FOUND wears the envelope verdict's code).
- **§414/§416/§420 terminal pattern:** the landing terminal fires DETACHED in its own
  session with a silent-death guard; a liveness probe (ps on the lock's pid) precedes ANY
  mutex reclaim — the kill tell is a DEAD pid + a header-only GATE log; a frozen small log
  mid-run is NORMAL (gate-tail streams its body to $TMPDIR); heartbeat-on-every-sample is
  the watcher standard; start the terminal under load only after outlasting the gate in
  your own turn.
- **§416.1 identity arm:** any close-out check that compares base-vs-tip asks "is the new
  tip MY tip?" before crying alarm — a change-only check reports success as failure.
- **§417 deferred rows:** stacked members sharing a walker/census path defer the shared
  row's exact text verbatim to the landing act (the T2J shape); a lane never asserts
  another member's completion and never edits the validator.
- **§410 retrospective mints:** enter at the terminal status, never DRAFT (retiredSymbols
  is a promise at non-terminal statuses).
- **§420 instrument lies:** git rev-parse echoes unresolvable args to stdout before its
  non-zero exit · macOS sed has no \b · vitest positional full-path filters collect nothing
  and exit 1 (a nothing-ran wearing a red) — capture TRUE_EXIT in-shell, self-named logs.
- **Validator status-sequence simulation** runs at each train plan against a synthetic
  manifest — a change path is reserved at every non-terminal status, DRAFT included; the
  shared-path check runs against the LIVE manifest's D3a/producer/website packets at the
  executor's base. No UC packet is minted before its producers' landing shas (§441.5(f)).
- **§432 (REPLACES the compile's §355 row — the hunt is CLOSED; §441.5(h)):** the suite's
  method laws are §432's: a SHORT `TMPDIR` on every terminal (a long lane TMPDIR is a
  test-statistic hazard — `readerShapeResolver`'s probe path derives `maxReadTokenLength`);
  "quiet" is MEASURED LOAD with `ps -r` attribution, never a process count; a sentinel
  report and a regressions list are DIFFERENT DOCUMENTS — never compared across kinds; a stray
  still classifies-then-ONE-re-runs, and a second stray ESCALATES TO THE CHAIR (there is no
  hunt to file to). The `prngSeedEntropy` tolerance cure rides a housekeeping car — never
  edited unilaterally. Per §440.2, every mutexed battery exports
  `GATE_MUTEX_LOCK_DIR=/tmp/settlementforge-vitest-gate.lock` (the short per-lane TMPDIRs
  otherwise defeat the cross-lane lock).
- **§443 pulse-written keys:** a new reader of a key only the pulse writes (`calamityHistory`)
  mints an observed-shape-reader row at ceiling 0 and the governed `--write` refuses new files
  — route through the banked `calamityLedger` read; the DISPLAY-LAZY law transfers by
  inheritance to every importer.

## §11 · JUDGMENT TABLE (vetoable by a word)

| # | call | the crux |
|---|---|---|
| J-1 | All six cars golden-inert (deriver/pulse-side); zero generation-path writes | §424's measured trap + §431's stated preference; the doctrine's licenses all read existing/producer facts |
| J-2 | OB-6: recommend DECLINE; price the optional car anyway | the truth-roster law covers the doctrine; expansion is owner-visible content, not a requirement |
| J-3 | OB-7: DEFER with recovered spec + narrowed trigger | derived provenance is the ruled cure (MF-R2 C5, §359.5); the blocking ambiguity was the LOST spec, now recovered |
| J-4 | Fabric geometry wholly deferred to D5 | the spec already architects it (G-27 "first-class in wave nine"); building any of it here mints a second truth in an un-chartered slot |
| J-5 | ~~Both trains queue behind the producer train~~ **SUPERSEDED by §441.7** — UC-0/UC-3 compile NOW; each later car waits only for ITS producer | the producer-free cars were parked behind T2N for no dependency reason (panel, packet-law F6) |
| J-6 | Connectivity compiles component-level, geometry-free | §311.9's laws are graph invariants; routes/joints-as-drawn are D3a/D5 work |
| J-7 | The §311.6.1 "some towns" question closed by §311.7.2's six causes | the chair already refined it at the owner's delegation; re-opening it would be a second truth |
| J-8 | ~~Family founded un-stamped, cap four, two trains~~ **SUPERSEDED by §441.7** — MF-UC0…MF-UC5 under the stamped town-cartography family, three trains, no UC-PREAMBLE | the producers are the same domain-root-deriver shape under MF (§433 C2); a new family owes a preamble + census law for no gain (panel, packet-law F10) |
| J-9 | Funded links as derived provenance, not store verbs | §423's cheap lawful path; no player act exists to verb |
| J-10 | ~~The criminal-share scalar is DERIVED, not stored~~ **STRUCK (§441.3)** and replaced: **one truth — `blackMarketCapture` / `criminalEffective` per `corruption.js:539-541`** | the compile's greps missed the estate's spelling; a parallel weighting would have minted a second truth for the §311.4 driver |

**The ruling's judgments (ODQ §441, vetoable by a word) — carried here so no executor reads
them second-hand:**

| # | call | the crux |
|---|---|---|
| §441-J1 | Refine §311.6.1 by the ROSTER (§441.1) over a catalog bill | the catalog already generates 'Sewage system' at city (0.4) and metropolis; "always top rung" contradicted it in ~60% of cities |
| §441-J2 | FIVE causes now, the founding-kind slot typed at ZERO weight (§441.2) | `founding.kind` has no typed home; prose-regex is the name-string idiom; R-7 wakes the slot |
| §441-J3 | Consume corruption.js's read for the criminal share (§441.3) | one stored home exists; never a parallel weighting |
| §441-J4 | UC-4 ALONE as T-UC2 + the certification-row cap exemption (§441.4) | a flag mint is a train boundary with a four-surface bill; the authored row is data that grows one row per flag (reversal: a new rows file) |
| §441-J5 | Exposable-provisional tuning (§441.6) | §337/§341: every pre-signature surface exposes against provisional defaults; the owner may veto to dark |
| §441-J6 | MF family, three trains (§441.7) | §433 C2 precedent; the 8-cap holds every train at ≤3; reversal: an `undercity` family when D5 geometry or DW needs its own law substrate |
| §441-J7 | A skeptic panel before EVERY charter ruling from here | the §311.7.2 home error and the J-10 refutation were invisible to a single reader — a compile's CONFIRMED is a hypothesis until a second instrument walks it |

**R2's own judgments (the application seat; vetoable):**

| # | call | the crux |
|---|---|---|
| J-R2-1 | The clandestineFacet.js key mismatch is RECORDED (§3, D-8) and routed to its own micro-fix car, not cured by UC-0 | the panel found it while walking §441.5(a)'s key-spelling rule; curing a sibling's key inside a charter car is a silent second change; UC-0 pins its OWN spelling against the real catalog row instead |
| J-R2-2 | The closed joint vocabulary's DATA module (`jointVocabulary.js`) is minted by UC-0, the first car, though the vocabulary is UC-5's law | every component row from UC-3 onward carries `surfaceJoins[]` (§441.5(b)) and UC-5 lands LAST — without one data module each car would hardcode the strings (second truths) or depend on an unbuilt car |
| J-R2-3 | §443's pulse-written-key law is folded in as a tagged addendum beyond the brief's 24 items | the ruling names UC-2 and UC-4 by name as LAW; leaving it out would hand the executors a charter that reds on arrival (T2R's second red) |

**Budget (compile estimate, re-cut to three trains):** T-UC1 (UC-0 ~180+20+facet rows /
UC-3 ~100 / UC-1 ~240) ≈ ~540 effective + tests; T-UC2 (UC-4 ~250 + the four flag surfaces)
≈ ~320 effective + tests; T-UC3 (UC-2 ~250 / UC-5 ~220) ≈ ~470 effective + tests; ~11–17
lane-hours across the three, CT-car pricing precedent; UC-R (owner-gated) ~1 car more.
STOPs: §3's missing-home STOP (the founding slot is NOT one — §441.2) · R-2's generation-path
STOP (incl. any catalog `facets` addition) · a second stray → ESCALATE TO THE CHAIR (§432) ·
validator simulation showing a shared-path reservation no split promotion cures · a UC-0 S0
golden that moves under the facet rows (the inference-only claim would be false).

**The final train table (§441.7):**

| train | cars (packet ids) | compiles when | flag |
|---|---|---|---|
| T-UC1 | UC-0 (`MF-UC0`) → UC-3 (`MF-UC3`) → UC-1 (`MF-UC1`) | UC-0/UC-3 NOW; UC-1 after T2R lands | none |
| T-UC2 | UC-4 (`MF-UC4`) alone | after T2R lands | the NEW undercity high-water flag (absent from defaults; R-5 lights it) |
| T-UC3 | UC-2 (`MF-UC2`) → UC-5 (`MF-UC5`) | after T2N + T2Q land (UC-5 also after T-UC1 and T-UC2) | none |

*Compiled read-only at the named bases; RULED at ODQ §441 and applied in place by
TC-UNDERCITY-R2 with the §3 table re-walked at b10ed1a1. Every figure above is either quoted
from a named ODQ row/file line or marked as an estimate the executor re-derives. CLAIM_RE
(`tests/docs/enforcement-claims.test.js:40`) and the C0 scan swept clean over this file after
the ruling was applied (the R2 receipt's scan section).*


## ⭐ CARRY NOTE (chair, ODQ §437): A FUTURE CONSUMER OF THE CONNECTIVITY GRAPH
The DW (Dwellings) program's law 7 projects this train's connectivity graph
into per-building floor plans (basements/hatches/sealed arches render from
the graph's own rows). REQUIREMENT carried now: graph rows stay PER-BUILDING
ADDRESSABLE — **anchor → the canonical institution KEY the fabric resolves to
its building** (§441.5(k): `id` when present, else the registry's stable name
key; buildings are fabric-minted and hold no engine id) — so the future
projection needs no re-shape. No other change; the executor records this in
the graph member's packet as a stated future consumer.
