# TC-CATALOG-HYGIENE (CH) — charter compile brief (prepared by the chair 2026-08-23 §492; dispatch at the first free seat, Opus)

You are lane **TC-CH-COMPILE** for SettlementForge `[OPUS-RUN · FABLE-VALIDATION OWED]` (ODQ §484). Compile the **CATALOG-HYGIENE TRAIN charter** ruled at ODQ §491: three repair cars (CH-1 · CH-2 · CH-3) that land BEFORE the dwellings compile DW-0. You are a chair-tier COMPILE lane, SOLO (lane cap TWO, no sub-agents, no Workflow). Load `anthropic-skills:deep-work` (apply §5's verification core — `adversarial-verify` is not registered), `anthropic-skills:judgment-ledger`, `anthropic-skills:structural-prevention`.

## Hard rules
- READ-ONLY on the repo `/Users/cstokes/Desktop/settlement-engine` except a private read-only worktree you create at `$SP/laneTCCH-tree` from `claude/composite-r4` as read NOW (re-read it; `df -k /` ≥ 3,000,000 KB first; `npm ci` exit captured in-shell; `ls node_modules | wc -l` ≥ 400). You may RUN generators/tests there (mutexed: `export GATE_MUTEX_LOCK_DIR=/tmp/settlementforge-vitest-gate.lock; export GATE_MUTEX_MAX_POLLS=480; export TMPDIR=/tmp/chc; sh scripts/gate-mutex.sh --run -- npx vitest run <files>; echo TRUE_EXIT=$?`) but you COMMIT NOTHING and edit nothing under `src/`, `docs/`, `tests/` — the deliverable is the charter in the scratchpad. Never any `npm run check*`.
- `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/6298872d-53af-4c8a-99e5-139bfc5bfc1f/scratchpad`. Write ONLY `$SP/draft-CATALOG-HYGIENE-PLAN.md` (the charter), `$SP/laneTCCH-receipt.md`, and `$SP/CH-*` working files. FIRST ACT: the receipt with `STARTED <date -u captured in the same command>`, the mark, the base sha; `## RESUME POINT <UTC>` at every proof boundary / ≤30 min ((a) PROVEN (b) IN FLIGHT (c) NEXT). C0 scan (`LC_ALL=C grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'` → 0) on every authored file before every RESUME POINT. No emojis.
- Every "home" you write is a MEASUREMENT (file:line read this lane, quoted), never the research's claim — §441 J7: a charter's CONFIRMED home is a hypothesis until the compile reads it (UC-4 and UC-2 each found the undercity charter's homes wrong). Every same-seed shift is MEASURED, not predicted.

## The inputs (read whole, in order)
1. ODQ `docs/OWNER_DECISION_QUEUE.md` §491 (the ruling), §488.2 (R-INST-5's G1–G7 + finding 4), §485.2 finding 5 (R-INST-4's FACET_INFERENCE gap), §476 (R-INST-3: `religiousCenter` exclusivity; three learning rows), §462 (R-INST-2: L1670 `priorityCategory: 'military'`; L1112 "MUST be downstream"), §488.3 (HK-6: the three "Eberron" strings), §464 (the grant; carve-outs by nature), §481/§484 (seat model; the mark).
2. The dossiers at `refs/preserve/research-dossiers-2026-08-23` (or the scratchpad copies): `draft-R-INST-5-MAGICAL.md` §Σ (the G1–G7 proofs with their file:line claims and §L item 45/53), `draft-R-INST-4-HOSPITALITY-POVERTY-UTILITY.md` §0 engine recon + §Σ item 20 (the name-only simulation over 38 names), `draft-R-INST-3-FAITH-LEARNING.md` §Σ engine gaps (religiousCenter; catalog learning rows), `draft-R-INST-2-TRADE-CRAFTS.md` §Σ (the two catalog slips).
3. Live code at your base (measure, quote): `src/domain/spatial/cohesionWeave.js` `FACET_INFERENCE` (~:258) and `facetOf` (~:300) — the whole table, every regex, which facets it infers; `src/domain/arcaneInstitutionIdentity.js` (the estate's own ruling that the TAG is the semantics and the bucket is a shelf — the precedent CH-2 extends), `src/domain/magicFilter.js` (`ARCANE_GOODS`, the hard-zero keywords, the three gate paths), `src/domain/magicProfile.js` + `src/data/constants.js` (`getMagicLevel` and its four tokens), `src/generators/steps/resolveConfig.js`, `src/generators/helpers.js`, `src/components/InstitutionalGrid.jsx` (the UI strip), the probability path R-INST-5 G5 names (`institutionProbability` — find it), `src/data/institutionalCatalog.js` (the 34 `minTier: 'metropolis'` rows — classify each: authored in the metropolis block (consistent) vs authored in the CITY block (the G2 slips; R-INST-5 counted five); L1670; L1112; L2183 "Eberron-style"; the `religiousCenter` rows), `src/data/institutionDescVariants.js:242-243`, the `religiousCenter` exclusivity reader(s) under `src/domain` (git grep), the sibling ANCHORED detector the estate already wrote (grep `anchored`/`\\b` in cohesionWeave.js and its tests — the precedent CH-1 carries across), `src/domain/interior/interiorTemplates.js` (the templates a facet selects), `tests/` readers of FACET_INFERENCE / interior kinds / magicFilter / the catalog (git grep — these are the pins each car must re-record or extend; list them ALL, the §489.3 arm applies at charter time too).
4. The packet form: `docs/implementation/PACKET_STANDARD.md`, `PACKET_TEMPLATE.md`, a recent LANDED packet (`docs/implementation/packets/town-cartography/MF-UC1.md`) and `scripts/implementation-packets.mjs` (action vocabulary CREATE|DOC|MODIFY|REGISTER|TEST; change paths reserved at every non-terminal status; a TEST path must exist at every status; requiredSymbols never pin the migration head). Existing packet FAMILIES under `docs/implementation/packets/` — choose or mint `catalog-hygiene` and say how the validator admits a new family (read the script).
5. Memory hazards under `/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/`: `sizebaseline-exact-ceiling-hazard.md`, `pulsekernel-sits-at-its-exact-size-baseline-ceiling.md` (check every file a car modifies against `scripts/.size-baseline.json` headroom — a file at its ceiling refuses one added line), `modify-host-in-edge-bundle-input-roster-owes-regen.md` (grep the five `supabase/functions/_shared/*.meta.json` rosters for every file a car modifies — `cohesionWeave.js` is in THREE bundles; `institutionalCatalog.js`? measure), `undercity-cars-three-ratchets-every-roster-reader-trips.md`, `regen-edit-loss-hazard.md`, `the-promise-ratified.md` (a seed is a STARTING world forever — same-seed shifts are DECLARED one-time shifts, never silent), `test-census-ceiling-forecloses-new-rows.md`, `naked-claim-debt-is-now-per-claim-and-reds-a-green-test.md` (a comment with completeness vocabulary in a .js reds a green test — UC-2's landing hit it), `flag-mint-bill-is-five-surfaces-not-four.md` + its §489 sixth surface (CH mints NO flag — confirm, or price the bill), `owner-fix-philosophy.md` (bold architecture over patches), `finite-semantics-law.md`.

## What the charter must contain (draft-CATALOG-HYGIENE-PLAN.md)
§0 the base sha and the measured home table (every claim from the research re-read at file:line; mark each CONFIRMED / CORRECTED (with the true home) / REFUTED).
§1 **CH-1 — inference honesty**: (a) the anchoring: every `FACET_INFERENCE` regex word-bounded, with the exact before/after table over ALL catalog names (run the table over the full catalog at base and after your proposed regexes in the worktree — a script, not reasoning — and list every row whose inferred facet CHANGES); (b) the per-entry `interiorKind` (or facet) OVERRIDE field — where it lives (catalog row field vs a side table), how `facetOf` reads it (declared ?? inferred ?? null is the landed precedent — quote it), which rows get one in THIS car (only the rows the research proved mis-typed; never a bulk re-type — that is DW-1's vocabulary work); (c) the walker: no catalog name matches more than one rule per facet, and every override names an existing kind; (d) the DECLARED SHIFT: run the generator over a seed sweep (reuse UC-1's 420-settlement harness idiom from `$SP/laneTEUC1-receipt.md`) at base vs with the change and report how many settlements change an interior, which goldens/parity pins red, and the exact re-record list; (e) acceptance ≤8 arms with a convicting mutant each; (f) ≤3 production files, the bundle regen owed (cohesionWeave.js IS a bundle input), size-baseline headroom per file.
§2 **CH-2 — the magic license**: (a) the field `magicLicense: NONE|LOW|MEDIUM|HIGH` on the 32 Magic/Exotic rows (+ any other row a gate currently treats by shelf) — the VALUES come from R-INST-5's verdict table (quote it) and you mark each as the dossier's proposal; (b) the three gate paths rewritten to read the license (quote each path before/after; the Adventurers' charter hall shelf straddle G4 resolved; the Great library G5; Dragon resident G6 generation-vs-UI divergence closed by ONE read); (c) G7: the goods vocabulary out of the institution gate — what replaces it; (d) the walker: every Magic/Exotic row declares a license; no gate reads `shelf`/`category === 'Magic'` (grep-proven); (e) the DECLARED SHIFT measured the same way (which settlements at magic=0 keep the 11 mundane entries; which change at LOW/MEDIUM); (f) acceptance, files, headroom, bundles.
§3 **CH-3 — the data slips**: each of the G2 city-block `minTier: 'metropolis'` rows resolved row by row WITH the reasoning (the tier block is the structural truth — but measure whether any reader depends on minTier vs block; quote); `religiousCenter` exclusivity at city (what the reader does, what R-INST-3 says it should do, the fix); L1670; L1112 (the prose is rewritten to what the negation search supports); HK-6's three "Eberron" strings rewritten setting-free (the variants file is golden/parity-pinned — list the pins and the re-record); the declared shift per item.
§4 the TRAIN: order CH-1 → CH-2 → CH-3 (or argue another), each car's packet stub in PACKET_STANDARD form (id, family, status DRAFT, changeManifest with actions, TEST rows existing at every status, requiredSymbols as symbols never counts), the shared-path de-duplication (the validator reserves change paths at every non-terminal status — no two cars may list the same path until the earlier is LANDED; if they must, serial-mint), the census arithmetic each car owes (a new test file reds TWO censuses), the grep-driven arm file list per car (§489.3), the sweep trees, the bundle regen set.
§5 anti-scope: what CH does NOT do (no new interior KINDS — DW-1; no bulk re-typing; no catalog rows added — the three learning rows are a CONTENT-train item, say where; no flag).
§6 judgments (vetoable, each with the rejected alternative and the reversal) + deferrals + open questions.
§Σ a ten-line summary for the chair.

FINAL in the receipt: charter size, C0, the measured-home tally (CONFIRMED/CORRECTED/REFUTED), the declared-shift figures per car, the list of pins each car re-records, and anything that needs a chair ruling BEFORE CH-1 can build. Return the FINAL as your final message. A skeptic lane follows you (§441 J7) before the chair rules.
