§909 CAR 5: the weak zero's last two floors close, and a run too short to conclude stops reading like a world that behaved

Nine skeptics and a critic read cars 1a-3 against their own messages. Two findings
were PRODUCT defects of exactly the class this lane exists to close, and both are
reproduced at the car-4 tip before being cured.

1. `receiptCarries` admitted an EMPTY ARRAY for a bare top-level path. The emptiness
   law lived inside the `[last]` branch alone, so a lit receipt carrying
   `yearlyPopulations: []` and `yearlyDiedFlags: []` graded both series rows
   EXECUTABLE, let them take their own `pops.length < 150` early return, answered
   `[]`, and certified `fullInstrument: true` over two rows that could not observe —
   under a shipped comment claiming an empty array was already absent by the same law
   `null` is. Measured at 68c3a1618: notExecutable [], findings []. The law now holds
   at EVERY segment, and "carried" is the SHAPE the row reads: `[last]` is a non-empty
   array walked to its final row, `{}` a non-array object with a reading in it. The
   four capacity rows declare their shapes, so `yearlyPopulations: 42`,
   `realmDemography = 42` and `realmDemography = {}` are absences rather than silent
   cleans. The shape is DECLARED, never inferred — a law refusing every primitive
   terminal would make a future row keyed on a scalar permanently NOT-EXECUTABLE, a
   false report bought to close a true one.

2. `tripwireFieldReach` skipped a row's `requires` grading whenever its DETECTOR was
   unreadable. A planted row with a computed `receipt[...]` detector and a requirement
   naming a path no writer produces landed in `unreadable` and NOWHERE ELSE, while the
   walker's header claimed every declared requirement was graded in its own channel.
   Both channels are independent now; the read channel is still refused rather than
   graded on a partial key set.

THE HORIZON GUARD (a chair ruling, implemented). `capacity_plateau` (150y),
`capacity_floor_thaw` (100y) and `capacity_envelope_30y` (30y) answered plain silence
on a run too short to observe, so a 30-year receipt graded fullInstrument true over
rows that could not have said otherwise. A horizon-short row is COMPLETE BUT
INCONCLUSIVE: it is not notExecutable (the instrument ran; the RUN was short, and a
useful-horizon receipt must not fail for a property the design does not give it), so
it reports through the observability channel with both figures on its face and
`evaluate-receipt` prints it apart from a host-observability note. Measured over the
33 receipt documents on disk: deterministicFirings and notExecutable IDENTICAL on
every one, and exactly the four lit 30-year receipts gain the two long-window rows.

THE ORDERED TWIN GUARD. `CAMPAIGN_HORIZON_YEARS` and `CERTIFICATION_HORIZONS.useful.years`
were a stated twin with nothing asserting they are equal; `soakInvariants.test.js` now
carries the equality and pins all three lifted bars as controls read from the module.
Car 3's "the lift REMOVES a spelling" is corrected by count: two definitions before and
two after — the lift MOVED it, and what it bought is that the envelope suite stopped
typing the figure.

THE FIXTURE'S YEAR 1. `soakRegister.test.js` derived its `motion` block from a
previous-YEAR record and claimed year 1 "carries zero transitions — exactly what the
observer produces". The observer compares each year's own before- and after-state, so
year 1 is not exempt: measured `populationTransitions: 4` at `yearly[0].year === 1` on
all three receipts this lane wrote, and zero rows at zero in 30 years and in 300. The
fixture now derives it the observer's way.

SHIPPED STATEMENTS MADE TRUE, under one law: CITE BY SYMBOL. Seven line citations were
wrong in the tree that ships them — three the lane wrote and staled itself, four
inherited and measured stale here (the remnant law was 313 lines from its cite). The
schema note's "five pinned fixtures" is re-derived as seventeen. The M1-F1 narrative now
describes the defect as it stood at 4243bdc61 and says so. A JSDoc block that car 2
separated from `receiptWriterFields` is back on its function.

NO VALUE MOVED: 150, 100, 30, 0.05 and 0.0025 keep their exact values; the two capacity
horizons are hoisted to one spelling each WITHIN this file, where the band prose and the
detector had spelled them twice.

PROOF (each count read from its own log by name): tripwireRegistry 11/11, soakInvariants
17/17, soakRegister 8/8, tripwireCaller 10/10, soakScriptSeams 8/8, demographicsEnvelope
6/6, demographicsCure 4/4, curveBandFreeze 4/4, realmScaleCertification 10/10,
determinismUnderWorkers 4/4, centuryLegSoak 9/9, negativeAssertionAnchor.walker 9/9 —
every EXIT=0. eslint on all nine files EXIT=0, zero bytes. typecheck:ratchet 173/173,
EXIT=0. Title counts: tripwireRegistry 12 -> 12, soakRegister 9 -> 9, soakInvariants
18 -> 19 (the ordered twin guard is one new arm; the census moves 23664 -> 23665).

Seat: Opus 5 — Fable-unvalidated
Lane: SOAK-HONEST-909
