# RECON-ARCH-REDERIVE-2 — the report (Opus RECON lane, session 7d3418f8, 2026-09-19 16:4x EDT)

**Tree read and imported:** `$SP/read-tip-a41a0e109`; `git rev-parse HEAD` =
`a41a0e109bdee8fe3a0df082bf35b36d2399301e`, `git status --porcelain` = 0 lines, measured at the END
of the lane (CONFIRMED). The build branch has moved on (EM-B3a landed, EM-P3 building) — **every
figure below is measured at `a41a0e109`**. Nothing was edited, staged or committed anywhere; every
script and output lives under `$SP/lane-rederive-recon-2-scratch/`. No vitest, no eslint, no npm
script, no gate; one `node` process at a time.

`SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/923472dc-319b-4b73-9e42-fa911739df78/scratchpad`

Scripts (new or extended in scratch; the kit's originals were copied, never edited):
`enrich-loader2.mjs` + `hook3.mjs` (the first recon's loader EXTENDED with the writers §22 names
that it did not wrap — the two ASSERTS, the power projector, `mergeNPCLists`,
`buildPoliticalNarrative`), `c0-control.mjs`, `p1-census.mjs`, `p1b-writers.mjs`,
**`seam.mjs` — the whole §22 seam**, `p2-inert.mjs`, `p3-noedit.mjs`, `p4-edits.mjs`,
`p4b-mirror.mjs`, `p4c-w4.mjs`, `p5-trace.mjs`, `p5b-placement.mjs`, `p5c-order.mjs`,
`p6-stress-name.mjs`, `p6b-stress.mjs`, `p7-cost.mjs`.

**The headline, in one breath.** §22's seam, prototyped whole, **runs all eight edits on a village,
a town and a city with nothing thrown** — the teleported port runs, the seat change sticks, the power
share sticks, the town keeps its name. But ruling 1's *own* words, applied literally, break two
things the design did not anticipate (the name mint's draws; the faction member mirror), and
**ruling 1 and ruling 9 are in direct contradiction at `institutions`**: "final at every writer"
reproduces 6/63 with NO edit and flips a city's food security from Secure to Pressured, while "final
at the LAST writer" reproduces 60/63 and leaves an institution removal invisible to the economy.
That contradiction is the one thing the chair must rule, and it is not curable inside §22 as written.

---

## 1. The inertness control

```
$ node --import ./hook3.mjs c0-control.mjs
=== C0.a — the committed golden CONTROL row, with RECON-2's loader installed ===
row      = town|germanic|plains|road|civilized|golden-master-v3
manifest = b77b5909009112855bad4dacf881847d06e43385f64a15f4b528e0d30c91c3ce
measured = b77b5909009112855bad4dacf881847d06e43385f64a15f4b528e0d30c91c3ce
EQUAL    = true

=== C0.b — a stride of the 525-row golden corpus (every 21st row) ===
rows=25  identical=25/25  wall=559 ms
INERT = true
```

And the WHOLE SEAM installed with an EMPTY held bag — every `__FN_OVERRIDE__` guard and every
restore placement exercised, nothing held (this is P2's ruling-(d) arm):

```
$ node --import ./hook3.mjs p2-inert.mjs
control row = town|germanic|plains|road|civilized|golden-master-v3
manifest    = b77b5909009112855bad4dacf881847d06e43385f64a15f4b528e0d30c91c3ce
seam, bag={} = b77b5909009112855bad4dacf881847d06e43385f64a15f4b528e0d30c91c3ce
EQUAL       = true
golden stride (every 21st of 525): identical=25/25
63-row sample, seam(bag={}) vs plain run: identical=63/63
INERT = true
```

CONFIRMED: the extended loader and the whole seam change no generated byte with no held bag.

---

## 2. P1 — the writer census

### 2.1 STEP-level writers, declared and measured three ways

```
$ node --import ./hook3.mjs p1-census.mjs
=== P1.A — DECLARED writers (getStepMeta), 22 steps ===
institutions     provides=[assembleInstitutions]  mutates=[subsumptionPass, cascadePass, isolationPass, factionCorrelationPass, coherenceRepairPass]
npcs             provides=[generatePopulation]  mutates=[corruptionPass]
factions         provides=[generatePopulation]  mutates=[corruptionPass]
relationships    provides=[generatePopulation]  mutates=[]
conflicts        provides=[generatePopulation]  mutates=[]
powerStructure   provides=[generatePower]  mutates=[neighbourFactions, powerEconomyReconcilePass, assembleSettlement]
effectiveConfig  provides=[resolveConfig]  mutates=[resolveResources, resolveStress, resolveNeighbour, isolationPass, stressConfirmPass, generateEconomy, coherenceRepairPass]
culture          provides=[resolveConfig]  mutates=[]

=== P1.B — MEASURED value change, per (step, held key), over 63 rows ===
step|key                                  rows    declared?   step draws (63-row total)
assembleInstitutions|institutions         63/63   provides    3324
generatePower|powerStructure              63/63   provides    0
generatePopulation|{npcs,factions,relationships,conflicts}  63/63  provides   19748
cascadePass|institutions                  55/63   mutates     1162
corruptionPass|{npcs,factions}            42/63   mutates     276
coherenceRepairPass|institutions          20/63   mutates     0
powerEconomyReconcilePass|powerStructure  11/63   mutates     0
subsumptionPass|institutions               9/63   mutates     0
isolationPass|institutions                 2/63   mutates     2
(+ resolveConfig|{effectiveConfig,culture} 63/63 and the six effectiveConfig mutators)

=== P1.C — MEASURED WRITE-THROUGH (the caller's object mutated) ===
cascadePass|institutions 55/63 · corruptionPass|npcs 42/63 · corruptionPass|factions 42/63 ·
coherenceRepairPass|institutions 20/63 · powerEconomyReconcilePass|powerStructure 11/63 ·
subsumptionPass|institutions 9/63 · isolationPass|institutions 2/63
(+ the four effectiveConfig write-throughs, 63/63 each)
```

CONFIRMED: **no undeclared step-level writer of any held key exists.** All three instruments agree
on the seven held keys. (The first recon's undeclared write-throughs were on `simulationTrace`,
`economicState` and `generationRepairs` — none of them a held key.) `generatePower` provides
`powerStructure` with **0 draws in 63/63**; §21.5 ruling 3 reproduces, and the replay's entropy is
spent on a privately minted `createPRNG(intent.rngSeed)` the step's own stream never sees.

### 2.2 FUNCTION-level writers inside `assembleSettlement` — the denominator that matters

```
$ node --import ./hook3.mjs p1b-writers.mjs     (5 rows: thorp, village, town, city, metropolis)
symbol                          calls  draws  what it does to a HELD key
generateSettlementName             5     10   ⛔ MINTS `name` — 2 draws on the STEP's own ambient stream
renderStressSummary               10      0   (stress only)
generatePressureSentence           5      5   reading — reads the name, 2 draws AFTER the mint
generateArrivalScene               5     13   reading — reads the name
generateDefenseProfile             5      0   reading over institutions
assertPowerEconomyFreshness       15      0   ⛔ ASSERT (three call sites per run)
projectPowerGenerationIntent      15      0   REPLAYS the power roster (private PRNG)
assertStableGeneratedRoster       10      0   ⛔ ASSERT (inside reconcilePowerStructure, twice per run)
reconcilePowerStructure           10      0   WRITES powerStructure IN PLACE — 0 paths at thorp/town/city,
                                              2 at village [publicLegitimacy.score, breakdown.defense],
                                              22 at metropolis [factions[].power×4,
                                              factionRelationships[].ratio×4, …+11 shapes]
refreshPowerGenerationTraces      10      0   writes simulationTrace (1 path village, 5 metropolis)
resizePoliticalRoster              5      0   REPLACES powerStructure (dormant under the default config)
buildPoliticalNarrative           51    179   re-rolls each npc's secret/goal/hooks   (in the substream)
mergeNPCLists                      5     13   MERGES the roster (adds, re-orders)     (in the substream)
disperseNamedRoster                5      0   rung-band dispersal                     (in the substream)
enrichNpcCoherence                 5    192   the three above + enrichNPCsWithStructure
relinkFactionMembers               5      0   WRITES factions[].members[] ← the enriched npcs
generateCoherence                  5    332   ⛔ the transformer: npcs 1045 leaf paths, factions 200
ensureFactionStructuralNpcs        5      0   WRITES npcs + factions (ret===arg0 at ≤city; a delta at metropolis)
buildGenerationCoherenceReceipt    5      0   reading
normalizeSettlement                5      0   adds canonical containers (no held-key path)
promoteStressorsToConditions       5      0   activeConditions only
reapplyEventConditions             5      0   ret===arg0

per-row draw split:
  generateSettlementName   thorp=2 village=2 town=2 city=2 metropolis=2
  generatePressureSentence thorp=1 village=1 town=1 city=1 metropolis=1
  generateArrivalScene     thorp=2 village=2 town=3 city=3 metropolis=3
  enrichNpcCoherence       thorp=12 village=23 town=34 city=51 metropolis=72
```

⭐ **The draw column is the finding.** Of the nine writers of a held key inside assembly, exactly
TWO spend entropy: `generateSettlementName` (2 draws, on the STEP's own ambient stream, before every
other assembly writer) and `enrichNpcCoherence` (12–72 draws, inside its own named child stream
`npc-enrichment`). The second can be skipped because `inAssemblySubstream` forks it; **the first
cannot.** Section 3 prices getting that wrong at 63/63 rows.

### 2.3 The census, as the packet's denominator

| held key | writers, by module and symbol | how proved |
|---|---|---|
| `institutions` | `steps/assembleInstitutions.js` (provides) · `steps/subsumptionPass.js` · `steps/cascadePass.js` · `steps/isolationPass.js` · `steps/factionCorrelationPass.js` · `steps/coherenceRepairPass.js` (all `mutates`, all write THROUGH) | P1.A declared; P1.B value change 63/9/55/2/—/20; P1.C frozen-clone write-through |
| `npcs` | `steps/generatePopulation.js#chooseOrPin` (the ONE existing consult) · `steps/corruptionPass.js` (write-through 42/63) · `narrativeGenerator.js#generateCoherence` → `#enrichNpcCoherence` → {`#buildPoliticalNarrative`, `npcGenerator.js#mergeNPCLists`, `density/applyDensityLaw.js#disperseNamedRoster`, `#enrichNPCsWithStructure`} · `factionRoles.js#ensureFactionStructuralNpcs` | P1.B/C; P1.D/E |
| `factions` | `generatePopulation` · `corruptionPass` · `narrativeGenerator.js#relinkFactionMembers` · `factionRoles.js#ensureFactionStructuralNpcs` | same |
| `relationships` | `generatePopulation` ONLY — no other step moves it in 63/63 | P1.B |
| `conflicts` | `generatePopulation` ONLY + the assembly literal's DUAL WRITE into `powerStructure.conflicts` (`assembleSettlement.js:135`) | P1.B; source |
| `powerStructure` | `steps/generatePower.js` (provides, 0 draws) · `steps/neighbourFactions.js` · `steps/powerEconomyReconcilePass.js` → `power/economyReconciliation.js#reconcilePowerStructure` (write-through 11/63) · the assembly literal's spread · `#reconcilePowerStructure` again (with the defense label) · `density/applyDensityLaw.js#resizePoliticalRoster` | P1.A/B/C; P1.D |
| `name` | `npcGenerator.js#generateSettlementName` (`assembleSettlement.js:112`) — **2 draws, ambient** | P1.E |
| world facts | `steps/resolveConfig.js` (provides `effectiveConfig` + `culture`) + SEVEN mutating steps; the record's `config` is `{...effectiveConfig}` and `_config` is `{...ctx.config}` (`assembleSettlement.js:164,170`) | P1.A/B |

**Every ASSERT that compares a held fact with a fresh derivation — the complete list, and it is 2:**

| assert | module:line | call sites per run | subject |
|---|---|---|---|
| `assertPowerEconomyFreshness` | `power/economyReconciliation.js:353` | **3** — `steps/powerEconomyReconcilePass.js:41`, `steps/assembleSettlement.js:204`, `:223` | the held `powerStructure.economyInputFingerprint` vs a fresh FNV-1a over `[version, tier, prosperity, safetyLabel, foodLabel]` |
| `assertStableGeneratedRoster` | `power/economyReconciliation.js:191` (module-private) | **2** — inside `reconcilePowerStructure` (`:242`) | the held generated-faction identity multiset vs the REPLAY's, keyed `factionIntentKey` = `'governing-seat'` for the seat, else `category::name` |

`git grep -n "export function assert\|^function assert" -- src/generators src/domain` finds no third
assertion in the generator that compares a held fact with a fresh derivation. `assertIntent` (`:141`)
only checks the intent's version. `buildGenerationCoherenceReceipt` is NOT an assert — it is a
receipt that RECORDS disagreement, and it moves (§3).

---

## 3. P3 — reproduction with no edit, and the settling shift

Held bag = the record's own held facts (all seven keys), over the 63-row structured sample.

```
$ node --import ./hook3.mjs p3-noedit.mjs
V1 ruling-1 literal (name SKIP, factions final, replay skipped, asserts pin-aware)
    reproduces=0/63  threw=0  caller record mutated=0/63  16.2 ms
    diverge: pressureSentence×63 arrivalScene×63 simulationTrace×57 economicState×20
             availableServices×20 economicViability×20 generationCoherenceReceipt×20
             powerStructure×9 history×9 settlementReason×6 defenseProfile×6 activeConditions×5
V2 V1 + the name mint CONSUMES its draws
    reproduces=6/63  threw=0  caller record mutated=0/63  15.8 ms
    diverge: (the same list minus pressureSentence and arrivalScene)
V3 V2 + relinkFactionMembers runs           — identical to V2 in every figure
V4 V2 with institutions NOT held
    reproduces=13/63  threw=0  diverge: simulationTrace×49 history×3
```

⛔ **FINDING A — ruling 1's LITERAL reading destroys the prose in 63 of 63 rows.**
"Every writer consults the pin FIRST and, when the key is held, leaves it as it is", applied to
`generateSettlementName`, means the mint does not run — so its **2 draws on the assembly step's own
ambient stream** do not happen, and `generatePressureSentence` and `generateArrivalScene`, which read
that same stream 1–3 draws later, produce different prose in **63/63 rows**. Letting the mint RUN and
discarding its result restores them (0/63 → 6/63). §22 has no rule about stream position, and the
difference between "skip the writer" and "run the writer and discard its result" is the whole
difference here.

⛔ **FINDING B — ruling 9's criterion 1 is NOT met, and the gap is the SETTLING SHIFT.**

```
=== P3.b — THE SETTLING SHIFT, per diverging key (V2), leaf paths summed over 63 rows ===
key                           rows  leaf paths  shapes
simulationTrace                 57       14115  [].causes[].reason×2258 [].targetId×1807 …
availableServices               20        2415  equipment[].name×264 equipment[].desc×264
                                                equipment[].institution×254 legal[].name×161 …(+49)
economicState                   20         189  foodSecurity.foodRatio×20 foodSecurity.deficitPct×20
                                                foodSecurity.dailyProduction×20 …(+11)
economicViability               20         159  metrics.foodBalance.*
history                          9          64  legacyAnnotations[].annotation×15 …
generationCoherenceReceipt      20          52  repairs[]×32 repairs.length×20
powerStructure                   9           9  economyInputFingerprint×9
settlementReason                 6           7  []×7
defenseProfile                   6           6  scores.disaster×6
activeConditions                 5           5  [].severity×5
```

**The mechanism, CONFIRMED by V4 and by the run order.** Dropping `institutions` from the held bag
takes the diverging list from ten keys to two and reproduction from 6/63 to 13/63. `institutions` has
**six writers at run-order indices 5, 6, 7, 8, 13, 14** and **twelve readers, seven of them at or
before index 14**:

```
$ node --import ./hook3.mjs p5c-order.mjs
 5  assembleInstitutions   PROVIDES     6 subsumptionPass MUTATES·reads   7 cascadePass MUTATES·reads
 8  isolationPass MUTATES  9 stressConfirmPass reads   10 generateEconomy reads   11 generatePower reads
13  factionCorrelationPass MUTATES·reads   14 coherenceRepairPass MUTATES·reads
17  structuralValidationPass reads  18 generatePopulation reads  19 corruptionPass reads
20  generateNarratives reads  21 assembleSettlement reads
```

A held `institutions` is the record's **FINAL** (post-`factionCorrelationPass`,
post-`coherenceRepairPass`) roster. Under "final at every writer" it reaches `generateEconomy` (10)
and `generatePower` (11), which originally read the roster as it stood BEFORE the repairs at 13/14.
This is the first recon's "final value fed to an intermediate consumer", and **making the freshness
assert pin-aware does not remove it — it only stops the throw; the readings settle somewhere else
instead.**

⛔ **FINDING C — the settling shift is DM-VISIBLE, and it flips a food-security label.**

```
=== P3.c — per-row settling, V2, one row per tier ===
thorp        IDENTICAL
hamlet       simulationTrace 0+/4~/0-
village      economicState 0+/7~ · availableServices 3+/77~/3- · settlementReason 0+/1~
             · economicViability 4+/11~ · generationCoherenceReceipt 0+/1~/1- · simulationTrace 17+/182~/21-
town         simulationTrace 21+/251~/21-   (everything else identical)
city         economicState 0+/13~ · availableServices 7+/131~/5- · powerStructure 0+/1~
             · settlementReason 0+/2~ · economicViability 0+/6~ · defenseProfile 0+/1~
             · generationCoherenceReceipt 0+/1~/2- · simulationTrace 30+/305~/32-
   prosperity Prosperous→Prosperous | safety Moderate→Moderate | ⛔ food Secure→Pressured |
   defense Well-Defended→Well-Defended |
   fingerprint MOVED power-economy-v1:d657f444 → power-economy-v1:6cbd04d0
metropolis   the same shape; ⛔ food Secure→Pressured;
   fingerprint MOVED power-economy-v1:c57b7e1d → power-economy-v1:7a00b6f3
```

**This is the number the brief asked for.** Opening the editor on a `city` or a `metropolis` and
saving NOTHING moves the settlement's **food security from Secure to Pressured**, rewrites 131–186
leaf paths of `availableServices` (equipment, legal and healing lists change names, descriptions and
providing institutions), moves `defenseProfile.scores.disaster`, and re-stamps the power structure's
economy fingerprint. A DM would see a different town.

⭐ **FINDING D — ruling 4 is load-bearing; CONFIRMED, and it buys its cure with a lie.**

```
=== P3.d — the same runs with the invariants NOT pin-aware (ruling 4 withdrawn) ===
rows=63  threw=9  ran=54
    ×9  Power/economy freshness invariant failed: the final power st…
```

With ruling 4 the seam throws **0/63 with no edit**; without it, **9/63 rows throw with no edit at
all**. But it buys that by letting `powerStructure.economyInputFingerprint` MOVE in 9/63 — the held
power structure is re-stamped as having consumed an economy it never consumed.

⭐ **FINDING E — `relinkFactionMembers` running or not is invisible with an unedited bag** (V2 ≡ V3
on every figure, and the no-edit control is identical in 3/3 rows of `p4b-mirror.mjs`). The two
readings of ruling 1 separate only under an edit; section 4 separates them.

CONFIRMED: **0/63 caller-record mutations in every variant** — ruling 6's `structuredClone` on entry
holds (the first recon measured 36/63 with an aliased bag).

### 3.1 ⭐ THE PLACEMENT EXPERIMENT — the decisive measurement of this lane

Ruling 1 says "final at EVERY writer". The first recon's X7 measured that "the LAST production"
is the one the record holds. Prototyped both, with ruling 8's trace partition (§5) on top:

```
$ node --import ./hook3.mjs p5b-placement.mjs
=== P5.b2 — placement × trace partition, no edit, 63 rows ===
W1 every writer (ruling 1 literal), no trace partition
    reproduces  6/63  diverging: simulationTrace×57 economicState×20 availableServices×20
                      economicViability×20 generationCoherenceReceipt×20 powerStructure×9
                      history×9 settlementReason×6 defenseProfile×6 activeConditions×5
W2 every writer + ruling 8 trace partition
    reproduces 11/63  (the same key list minus 7 trace rows)
W3 LAST writer only, no trace partition
    reproduces 13/63  diverging: simulationTrace×49 history×3
W4 LAST writer only + ruling 8 trace partition
    reproduces 60/63  diverging: history×3          ⭐
```

⭐ **W4 meets ruling 9's criterion 1 in 60 of 63 rows**, with a single residual key. And that
residual is attributable:

```
$ node --import ./hook3.mjs p6-stress-name.mjs      (P6.a)
town|germanic|coastal|port|…   ALL HELD: history ⛔ · drop powerStructure: history OK · every other drop: ⛔/THREW
town|germanic|plains|none|…    identical pattern
town|germanic|plains|road|frontier|… identical pattern
```

**The `history×3` residual is the SAME class as Finding B, for `powerStructure`.**
`generateNarratives` (index 20) declares `reads: powerStructure` and mints
`history.legacyAnnotations` from it; the RECORD's `powerStructure` is the post-ASSEMBLY value (after
the assembly's second `reconcilePowerStructure` with the real defense label and after the
`conflicts` dual-write), not the value step 20 saw. Holding the record's value at
`powerEconomyReconcilePass` (16) hands step 20 a structure it never had. 3/63 is the rate at which
`deriveLegacyAnnotations` is sensitive to it; the mechanism is universal.

⛔ **But W4's cost is that the edit stops reaching the readings.**

```
=== P5.b3 — DOES THE EDIT STILL REACH THE READINGS under placement=last? ===
town: ADD "Town granary Annexe"
   every writer : keys moved vs control = economicState:4 institutions:2
   LAST writer  : keys moved vs control = institutions:2            ⛔ the economy never sees it
town: REMOVE "Market square"
   every writer : institutions:386 availableServices:167 economicState:155 simulationTrace:106
                  resourceAnalysis:30 spatialLayout:22 generationCoherenceReceipt:12 history:9
                  economicViability:6 structuralViolations:3 powerStructure:1 settlementReason:1
                  defenseProfile:1 activeConditions:1
   LAST writer  : institutions:386 availableServices:155 resourceAnalysis:30 spatialLayout:22
                  generationCoherenceReceipt:9 structuralViolations:3    ⛔ economicState untouched
```

and the consequence is a real dangling reference (§4): under `last`, removing an institution at town
tier leaves **`economicState.tradeDependencies[].institution="Market square"`** pointing at a thing
that no longer exists, because `generateEconomy` (10) ran before the pin landed at 14.

The readers that DO and DO NOT see an institution edit under `last`:

```
READERS BEFORE the last writer (they see the FRESH roster): subsumptionPass, cascadePass,
   stressConfirmPass, generateEconomy, generatePower, factionCorrelationPass, coherenceRepairPass
READERS AFTER  the last writer (they see the EDITED roster): structuralValidationPass,
   generatePopulation, corruptionPass, generateNarratives, assembleSettlement
```

---

## 4. P4 — the eight edits

### 4.1 On the seam that reproduces (W4: last-writer placement, `relink=true`, ruling-8 trace)

```
$ node --import ./hook3.mjs p4c-w4.mjs
-- village  control reproduces=true  baseline dangles=8
-- town     control reproduces=true  baseline dangles=14
-- city     control reproduces=true  baseline dangles=30
```

| # | edit | threw | keys moved (vs the no-edit control) | flicker | new dangles (Δ) | name | does it STICK |
|---|---|---|---|---|---|---|---|
| 1 | rename an NPC via `applyNpcRenameToSettlement`, then re-derive (ruling 7) | **no**, 3/3 | village `relationships:5 prominentRelationship:2 npcs:1 factions:1` · town `relationships:5 npcs:1 factions:1` · city `relationships:4 npcs:1 factions:1` | NONE | **0** on 3/3 | KEPT | renamed npc present: yes 3/3 |
| 2 | change an NPC's role | **no**, 3/3 | `npcs:1 factions:1` on 3/3 | `factions:1` (the member MIRROR — see 4.2) | 0 | KEPT | `role="Harbourmaster"`, **mirror=["Harbourmaster"]** 3/3 |
| 3 | add an institution from the tier catalogue | **no**, 3/3 | village `availableServices:5 economicState:4 institutions:2` · town `availableServices:38 institutions:2` · city `economicState:40 availableServices:37 institutions:2 economicViability:1` | NONE | 0 | KEPT | present in the output: yes 3/3 |
| 4 | remove an institution | **no**, 3/3 | village `institutions:219 economicState:62 availableServices:36` · town `institutions:386 availableServices:155 resourceAnalysis:30 spatialLayout:22 …` · city `institutions:330 economicState:97 availableServices:25 …` | NONE | village 0 · **town 1** (`economicState.tradeDependencies[].institution="Market square"`) · city 0 | KEPT | absent from the output: yes 3/3 |
| 5 | change one faction's power share | **no**, 3/3 | `generationCoherenceReceipt:7 powerStructure:1` on 3/3 | NONE | 0 | KEPT | **STICKS=true 3/3** (12/12, 9/9, 7/7) |
| 6 | seat a different governing faction | **no**, 3/3 | `powerStructure:7 / :8 / :10` | NONE | 0 | KEPT | **STICKS=true 3/3** |
| 7 | world fact: terrain → desert | **no**, 3/3 | village `economicState:205 resourceAnalysis:203 simulationTrace:147 …` · town `simulationTrace:630 economicState:525 resourceAnalysis:210 history:70 …` · city `simulationTrace:627 economicState:420 resourceAnalysis:148 …` | **`powerStructure:1`** (`economyInputFingerprint` — ruling 4's own refresh) 3/3 | village 3 · town 6 · city 2 — ALL of the pre-existing free-party-prose / catalogue-label kinds | **KEPT** 3/3 | terrain=desert 3/3 |
| 8 | world fact: culture → norse | **no**, 3/3 | village `culturalIdentity:11 config:2 economicState:1 culturalNotes:1 _config:1 arrivalScene:1` · town `economicState:50 simulationTrace:46 culturalIdentity:11 …` · city `simulationTrace:73 culturalIdentity:11 …` | NONE | 0 | **KEPT** 3/3 | culture=norse 3/3 |

⭐ **Every refusal the first recon measured is CURED, and nothing new throws — 24 of 24 edit×row
trials ran.** The seat change throws no more (the replay does not run). The power share is no longer
silently discarded (the replay does not overwrite). Terrain→desert — **§14's flagship, the teleported
port** — runs on a village, a town AND a city. The culture change **no longer renames the
settlement**: `Schwarzwalde` stays `Schwarzwalde` in 3/3, where the first recon measured
`Schwarzwalde → Stormtun`.

In plain words, per edit, what a DM would see:
1. **Rename.** The person is renamed everywhere that names them by key; no reference dangles.
2. **Role.** The role changes on the person's card and on their faction chip, and nothing else moves.
3. **Add an institution.** The new house appears; the legal/equipment service lists gain its entries and the income mix re-weights.
4. **Remove an institution.** The house is gone, its services vanish, the economy's compound lists shrink — but at town tier one trade dependency still names it (the `last`-placement cost).
5. **Power share.** The share sticks at the number the DM typed, and the coherence receipt notices the political shape changed.
6. **Seat.** The new faction governs; the old one stops.
7. **Terrain → desert.** The town keeps its name, its people, its houses and its factions, and its economy, resources and viability say what a desert town honestly says — 205–525 leaf paths of consequence.
8. **Culture → norse.** The cultural identity's seven dimensions, the notes and the arrival scene re-voice; the town keeps its name.

### 4.2 The member mirror — ruling 1's second literal break

```
$ node --import ./hook3.mjs p4b-mirror.mjs
village / town / city, npcs[0] "Vilbirg …" (id=npc_1), a member of 1 faction group
  edit npcs[0].role = "Harbourmaster"   (record role="Mayor", mirror ["Mayor"])
     relink=false (ruling 1 literal: factions is FINAL): role="Harbourmaster" mirror=["Mayor"] ⛔ MIRROR STALE
     relink=true  (the mirror follows the held roster): role="Harbourmaster" mirror=["Harbourmaster"] MIRROR FOLLOWS
  edit npcs[0].title = "The Drowned Hand"
     relink=false: ⛔ MIRROR STALE   ·   relink=true: MIRROR FOLLOWS
  no-edit control: relink=false ≡ relink=true ? true         (3/3 rows)
```

⛔ **Ruling 1 makes `factions` a held key, and `relinkFactionMembers` is one of its writers — so under
the literal reading the faction member chip keeps the OLD role, OLD title, OLD anything-but-name, on
every tier.** The rename escapes only because `applyNpcRenameToSettlement` walks the grouping home
itself. `relinkFactionMembers` is a MIRROR writer, not a fact writer; exempting it costs exactly
nothing with no edit (identical in 3/3) and is the difference between a coherent dossier and a
stale one.

### 4.3 The same eight edits on the ruling-1-literal placement (`every`), for the contrast

`$ node --import ./hook3.mjs p4-edits.mjs` — all eight also run with nothing thrown, and the
edits stick identically. The differences are only:
- the no-edit control does NOT reproduce (village 6 keys settled, town 1, city 8), so **every edit's
  diff is measured against a control that already differs from the record**;
- the institution ADD at town reaches `economicState:4` (under `last` it reaches nothing but the roster);
- town's institution REMOVE produces a different single new dangle (`availableServices.*[].institution="(smuggling)"`,
  a catalogue label, not a reference to the removed house).

---

## 5. P5 — the trace; P6 — `stress` and the name

### 5.1 P5.a — do trace entries carry their producing step? **YES, 95/95. §22's "MEASUREMENT OWED" is answered.**

```
$ node --import ./hook3.mjs p5-trace.mjs
entries=95
fields: causes×95 downstreamEffects×95 targetType×95 targetId×95 step×95 result×95 ts×95
entries carrying a `step`: 95/95  (TOTAL)
by step: assembleInstitutions=46 economyReconcilePass=20 cascadePass=9 generatePower=7
         resolveResources=6 generatePopulation=2 generateNarratives=2 resolveConfig=1
         resolveStress=1 corruptionPass=1
```

Every entry carries `step` AND a monotone clock `ts`, so a partition by step re-orders exactly.

### 5.2 P5.b — which steps' entries diverge with the whole seam and no edit

```
step                      rows whose entries diverge   rows whose entry COUNT differs
generatePopulation             57/63                          0/63
cascadePass                    55/63                         50/63
generatePower                  52/63                          0/63
economyReconcilePass           50/63                          0/63
generateNarratives             50/63                          0/63
corruptionPass                 39/63                          0/63
coherenceRepairPass            20/63                         20/63
subsumptionPass                 9/63                          9/63
isolationPass                   7/63                          2/63
trace identical with no edit: 6/63
```

⚠ **The four steps whose entry COUNT changes are the institution REPAIR passes.** Under a held
roster they find nothing to repair, so they emit fewer entries. They are DERIVING steps by §22's own
classification (they produce receipts) yet their entries are ABOUT a held key.

### 5.3 P5.c — ruling 8 prototyped

```
H1 the four roster+power+institution producers
    trace reproduces  7/63 · whole record  7/63
H2 H1 + every institution MUTATOR (the whole held-institutions chain)
    trace reproduces 13/63 · whole record 11/63
H3 H2 + powerEconomyReconcilePass + neighbourFactions + assembleSettlement
    trace reproduces 13/63 · whole record 11/63     (H3 adds nothing over H2)
```

and with the settling shift removed (placement=last), **H2's partition takes the whole record to
60/63** (§3.1, W4). So ruling 8 works — but its denominator is not "held step vs deriving step"; it
is **"the step's entries are about a held key"**, which is what H2 encodes and what H1 misses.

### 5.4 P5.d — what an edit does to the partitioned trace

```
  village    / add an institution   trace leaf paths moved=  0  entries 60→60  steps moved: none
  village    / change an NPC role   trace leaf paths moved=  0  entries 60→60  steps moved: none
  village    / terrain → desert     trace leaf paths moved=179 entries 60→58  steps moved: resolveResources,economyReconcilePass,generateNarratives
  town       / add an institution   0 · 95→95 · none
  town       / change an NPC role   0 · 95→95 · none
  town       / terrain → desert     618 · 95→87 · resolveResources,resolveStress,economyReconcilePass,generateNarratives
  city       / add an institution   0 · 95→95 · none
  city       / change an NPC role   0 · 95→95 · none
  city       / terrain → desert     707 · 95→90 · resolveResources,resolveStress,economyReconcilePass,generateNarratives
```

CONFIRMED: with ruling 8 on, a root edit moves the trace by **zero** and a world-fact edit moves it
only at DERIVING steps. That is precisely what ruling 8 promises.

### 5.5 P6 — the settlement's name under a culture change

```
$ node --import ./hook3.mjs p6-stress-name.mjs      (P6.b)
village / town / city:
  record name="Schwarzwalde" · a FREE culture→norse generation would mint "Stormtun"
  · held re-derivation name="Schwarzwalde" (KEPT)
  leaves speaking the OTHER name "Stormtun": 0
  leaves speaking the HELD name: 4 [stress.summary, stressors.summary, pressureSentence, arrivalScene]
```

⭐ CONFIRMED on 3/3 tiers: **with the name HELD, a culture change leaves NO prose speaking a
different name.** The first recon's `summaryRoll` hazard is neutralised — the held `stress.summary`
speaks the held name, which is now the right name.

### 5.6 P6 — `stress`: HELD or RE-DERIVED?

```
$ node --import ./hook3.mjs p6b-stress.mjs
rows=63  stress entries on the records=54  entries carrying `summaryRoll`=0   (deleted at assembly)
rows where a FREE (re-derived) stress MOVES under an edit: 0/126 trials
terrain→desert: rows where HELD stress differs from RE-DERIVED stress: 0/63
culture→norse : rows where HELD stress differs from RE-DERIVED stress: 0/63
```

Three measured facts:
1. **No record carries a `summaryRoll`** (0 of 54 entries over 63 rows) — the input of the summary is
   gone, confirming the first recon at scale.
2. **Held and re-derived `stress` are byte-identical in 126/126 trials**, because the NAME is held
   and the F8 re-render therefore renders the same name either way. Holding `stress` buys nothing.
3. ⛔ **Neither a terrain change nor a culture change MOVES `stress` at all (0/126).** §14 lists
   stressors among the world facts, but no world-fact edit reaches them through `config′`. A stressor
   edit would have to be a direct edit of a held `stress`, which is exactly the one key that cannot
   be re-rendered. **What each shows a DM:** re-derived shows the settlement's own stressor, named
   correctly; held shows the same thing today, and would show the OLD settlement name the moment the
   name stopped being held. RE-DERIVED is the safe arm, and it must be named as re-derived rather
   than left inside "the world facts".

---

## 6. Where design §22 is WRONG or incomplete — smallest measured contradiction first

1. **Ruling 1's "leaves it as it is" is ambiguous for a writer that DRAWS, and the literal reading
   costs 63/63 rows.** `generateSettlementName` spends 2 draws on the assembly step's shared ambient
   stream before every other assembly writer. Skip it and `pressureSentence` and `arrivalScene` move
   in 63/63 (V1 = 0/63); run it and discard and they do not (V2 = 6/63). §22 says nothing about
   stream position. **The rule §22 needs and does not have:** *a writer whose key is held CONSUMES its
   draws and discards its result when it draws on a stream shared with later code, and is skipped only
   when it draws inside its own named child stream* (`enrichNpcCoherence` is the second case, and the
   comment at `assembleSettlement.js:53-68` is why).

2. **Ruling 1 makes `factions` held, which leaves the faction member MIRROR stale on every non-name
   NPC edit.** Measured on role and title, 3/3 tiers: `relink=false` → `mirror=["Mayor"]` while the
   person's card says `Harbourmaster`. `relinkFactionMembers` is a MIRROR writer, not a fact writer.
   Exempting it is free with no edit (identical 3/3). §22 needs a named exemption class: *a writer
   that copies a held fact from one home to another is not a writer of that fact.*

3. ⛔ **THE CENTRAL ONE: ruling 1 ("final at EVERY writer") and ruling 9 ("with NO edit, re-derivation
   reproduces every record key") CONTRADICT EACH OTHER AT `institutions`, AND NEITHER HORN IS FREE.**
   - `every` writer: 6/63 reproduce (11/63 with ruling 8). No-edit changes a **city's food security
     from Secure to Pressured**, rewrites 131–186 leaf paths of `availableServices`, moves
     `defenseProfile.scores.disaster`, re-stamps the power fingerprint.
   - `last` writer: **60/63** reproduce — but `generateEconomy` and `generatePower` never see the
     edit, so an institution ADD at town moves nothing but the roster and a REMOVE leaves
     `economicState.tradeDependencies[].institution="Market square"` dangling.
   - The cause is structural: the record holds the FINAL roster and seven of the twelve institution
     readers originally consumed an INTERMEDIATE one. **There is no third option without the record
     carrying the pre-repair roster as well** — a persisted-shape change. §22 ruling 5 refuses a
     schema change for `powerIntent` and is silent about this one, which is the bigger of the two.

4. **Ruling 1's held set omits `history`, yet holding `powerStructure` moves `history.legacyAnnotations`
   in 3/63 with NO edit** — attributed by dropping one held key at a time (drop `powerStructure` →
   `history OK`, 3/3 rows). Same class as (3): `generateNarratives` (index 20) reads `powerStructure`,
   and the record's value is the post-ASSEMBLY one. The contradiction is therefore not confined to
   `institutions`; it is a property of every held key whose record value is made after its last
   pre-assembly reader.

5. **Ruling 4 makes the power structure's receipt LIE, and ruling 9 convicts it for doing so.**
   `economyInputFingerprint` moves in 9/63 with no edit and in 3/3 world-fact edits. It is a field
   INSIDE a held key, and ruling 1 says "the power structure's held FIELDS" without enumerating them.
   Two things are owed: the enumeration (which powerStructure fields are held and which derived),
   and an exemption in ruling 9's flicker gate for the fields ruling 4 refreshes — otherwise
   "no UNEDITED held fact moved" fails on every world-fact edit, at every tier.

6. **Ruling 4's second half is unreachable as written.** `assertStableGeneratedRoster` runs ONLY
   inside `reconcilePowerStructure`, which ruling 1 turns into a no-op under held mode. So "takes the
   held roster as its subject" never fires. The measured cure for the seat edit is **the replay not
   running**, not a pin-aware assert. Ruling 4 should say that, or EM-R3 will build an unreachable
   branch and a walker will pass over it.

7. **Ruling 8's partition key is misnamed.** It says "a HELD step re-emits nothing". The steps whose
   entries must be carried are not the held-key PRODUCERS (H1 = 7/63) but every step whose entries are
   ABOUT a held key, including the four institution repair passes whose entry COUNT changes in
   50/63, 20/63, 9/63 and 2/63 because a held roster needs no repair (H2 = 13/63, and 60/63 with the
   `last` placement). The "MEASUREMENT OWED" it names is ANSWERED: entries carry `step` 95/95 and a
   clock `ts` 95/95, so the partition is exact.

8. **§14 lists STRESSORS among the world facts and ruling 1 holds "the world facts", but `stress`
   cannot be held.** 0 of 54 stress entries on 63 records carry the `summaryRoll` the summary is
   rendered from. Measured: held and re-derived `stress` are identical in 126/126 trials *only
   because the name is held*; the staleness returns the moment it is not. And neither world-fact edit
   moves `stress` at all (0/126), so the stressor half of §14's world-fact card is not reachable
   through `config′` in any case.

9. **Ruling 9's "no NEW dangling reference" cannot be a plain delta.** terrain→desert adds 2–6 new
   dangles on 3/3 tiers, every one of them of the free-party-prose (`history.currentTensions[].factions[]`)
   or catalogue-label (`economicState.activeChains[].processingInstitutions[]`) kinds the first recon
   named as pre-existing by design. The gate must be a delta **by KIND**, and it must still catch the
   one genuine new dangle the `last` placement produces (a reference to a REMOVED institution).

10. **Ruling 7's institution cascade is still entirely absent and is now the blocker it was predicted
    to be.** `git grep -ln "INSTITUTION_RENAME\|applyInstitutionRename" -- src` returns nothing at
    `a41a0e109`.

11. **Ruling 2's "reported in the delta card as the reading's movement" is not implementable until
    (3) is ruled** — the same institution ADD moves `economicState:4` under `every` and nothing under
    `last`. The delta card would tell the DM two different stories depending on a decision §22 does
    not name.

**CONFIRMED CORRECT, without qualification:**
- **Ruling 5** — NO schema change is needed for the power card. Share and seat both stick on 3/3
  tiers because the replay does not run. `powerIntent` stays unpersisted.
- **Ruling 6** — pins deep-cloned on entry: 0/63 caller mutations in every variant, at 1.0–1.7 % of
  a re-derivation.
- **Ruling 7's first half** — renames go through the cascade: `applyNpcRenameToSettlement` + the
  re-derivation gives **0 new dangling references on 3/3 tiers**, where the first recon measured 4–5
  with the rename applied to the pins.
- **Ruling 1's core claim** — the roster consult inside `enrichNpcCoherence` reproduces `npcs` and
  `factions` in 63/63 under every variant; they never appear in a diverging-key list.
- **§22's ⛔ PUT-TO-THE-OWNER paragraph is now OUT OF DATE in the DM's favour.** §14's flagship —
  change a world fact and watch the consequences — **RUNS** under the prototype, on a village, a town
  and a city, keeping the name, the people, the houses and the factions, and moving 205–525 leaf
  paths of economy, resources and viability. What remains to put to the owner is not "the flagship
  does not run" but item (3): *whether a no-edit re-derivation may change the dossier, or whether an
  institution edit may fail to reach the economy* — with a persisted pre-repair roster as the only
  measured way to have both.

---

## 7. P7 — cost, and the packet grouping

```
$ node --import ./hook3.mjs p7-cost.mjs
tier         bag bytes   plain generation ms   re-derivation ms   structuredClone ms   clone %
thorp            15171                  6.6                5.4                0.082   1.52%
hamlet           25294                  8.6                7.2                0.123   1.71%
village          37546                 17.2               15.6                0.171   1.10%
town             59447                 24.9               22.7                0.231   1.02%
city             84341                 27.4               24.5                0.327   1.33%
metropolis      107762                 31.7               29.0                0.423   1.46%

63 re-derivations in 1136 ms  (18.0 ms each, instrumented)
```

⭐ **A re-derivation with the whole seam on is CHEAPER than a plain generation at every tier**
(5.4 vs 6.6 ms at thorp, 29.0 vs 31.7 at metropolis), because the enrichment, the power replay and
the repair work do not run. The clone is 1.0–1.7 % of it. **There is no cost argument anywhere near
this design.** The owner's positioning law ("generation is milliseconds") is safe: an edit is
5–29 ms.

### 7.1 The `src/` functions the real implementation must touch, against the charter's EM-R1…R6

| charter row | what the measurement says it must touch | where the grouping is WRONG |
|---|---|---|
| **EM-R1** pins cloned on entry + the writer census as a walker | `generators/pipeline.js` (the `structuredClone` at the pin merge, `runPipeline` ~line 178) + the census table of §2.3 | ⛔ "every writer of a held key consults the pin" **cannot be the walker's predicate**: `relinkFactionMembers` must NOT (item 6.2) and `generateSettlementName` must consult it only AFTER drawing (item 6.1). The walker needs two declared exemption classes — MIRROR writers and SHARED-STREAM drawers — or it will convict the correct implementation. |
| **EM-R2** the roster is final inside `enrichNpcCoherence`; `corruptionPass` leaves a held roster alone | `generators/narrativeGenerator.js#enrichNpcCoherence`, `generators/steps/corruptionPass.js` | ⛔ INCOMPLETE. `generators/factionRoles.js#ensureFactionStructuralNpcs` also writes `npcs` AND `factions` (`ret===arg0` at ≤city, a real delta at metropolis) and appears in NO row. `#relinkFactionMembers` needs an explicit EXEMPTION, which no row grants. |
| **EM-R3** the power structure is final in the replay; the two invariants pin-aware | `generators/power/economyReconciliation.js#reconcilePowerStructure`, `#assertPowerEconomyFreshness`, `#assertStableGeneratedRoster` | ⛔ INCOMPLETE. `generators/density/applyDensityLaw.js#resizePoliticalRoster` REPLACES `settlement.powerStructure` (`assembleSettlement.js:253`) and is in no row; `steps/neighbourFactions.js` and `steps/powerEconomyReconcilePass.js` are step-level writers in no row. And `#assertStableGeneratedRoster` is UNREACHABLE once the replay is skipped (item 6.6) — the row prices work that will not exist. |
| **EM-R4** the settlement's name and the world facts held through assembly | `generators/npcGenerator.js#generateSettlementName` (`assembleSettlement.js:112`) | ⚠ The world-fact HALF IS NOT A PACKET. World facts are held by being the INPUT (`config′` through `resolveConfig`); the eight-edit table shows terrain and culture edits already flowing with no pin at all. R4 should be the name mint alone, with item 6.1's consume-and-discard rule as its whole content. |
| **EM-R5** the trace's partition by step | `generators/pipeline.js` (`_LEDGER_KEYS`), `steps/assembleSettlement.js:297` (the copy-through), and each step's `recordTrace` call sites | ⚠ The partition set is H2 (producers **and** the four institution repair passes), not the producers (item 6.7). Denominator CONFIRMED: `step` 95/95, `ts` 95/95. |
| **EM-R6** the institution rename-surface list and its cascade | `domain/factionRename.js` | CONFIRMED still absent; 30 of the 56 exact joins point at an institution by name. |
| ⛔ **MISSING ROW — call it EM-R0** | the PLACEMENT decision of item 6.3: `generators/pipeline.js`'s pin channel and every one of the twelve institution readers | This is the largest single decision in the family and the charter has no row for it. It is owner-gated if the answer is "persist the pre-repair roster". |

Files the measurement says are touched, in full: `generators/pipeline.js`; twelve step files
(`assembleInstitutions`, `subsumptionPass`, `cascadePass`, `isolationPass`, `factionCorrelationPass`,
`coherenceRepairPass`, `generatePopulation`, `corruptionPass`, `generatePower`, `neighbourFactions`,
`powerEconomyReconcilePass`, `assembleSettlement`); five non-step generator modules
(`narrativeGenerator.js`, `npcGenerator.js`, `factionRoles.js`, `density/applyDensityLaw.js`,
`power/economyReconciliation.js`); and `domain/factionRename.js`. **Nineteen files**, against the
charter's "six to nine packets in `src/generators/**`".

---

## 8. What I could not measure, and what would settle it

1. **The `every`-vs-`last` decision (item 6.3).** I measured both horns exactly; I am not ruling it,
   and it cannot be ruled inside §22 as written. **Settles it:** whether the record may carry the
   pre-repair institution roster (and the pre-assembly power structure) — a persisted-shape question,
   owner-gated. A one-script prototype would be: snapshot `ctx.institutions` after `isolationPass`,
   carry it in the held bag as `institutionsIntermediate`, restore THAT for readers 6–14 and the
   final one for readers 17–21, and re-run `p5b-placement.mjs`. I did not run it because it presumes
   the schema answer.
2. **The 525-row golden proof of each override.** I proved inertness on the committed control row,
   a 25-row stride of the 525 (`i % 21`), and 63/63 against plain runs. The packet must prove the
   full 525 under the real (tree) implementation; my loader proves only that the SEAM is inert, not
   that a tree edit expressing it would be.
3. **The draw-skip vs draw-consume difference at the STEP level.** My step-level stand-in is
   `onStep` restore-after, which CONSUMES the step's draws. The real `chooseOrPin` SKIPS them
   (`generatePopulation`: 315 → 0). `assembleInstitutions` spends **3324 draws over 63 rows** and
   `cascadePass` 1162; if the real implementation skips them, everything later **inside those steps**
   moves. **Settles it:** a `__FN_OVERRIDE__` on `assembleInstitutions`' own internal builder (it has
   named module functions) returning the held roster without drawing, re-run against `p3-noedit.mjs`.
   About one hour.
4. **Ruling 3 — adding a PERSON.** Not measured at all. **Settles it:** push a `dm:`-id npc into the
   held roster with no relationships and measure whether `relationships`, `conflicts` and
   `factions[].members[]` stay put and whether the enrichment's skipped structural fields leave the
   newcomer visibly poorer than their neighbours (they will: `enrichNpcCoherence` is overridden
   wholesale, so a minted npc gets no `structuralPosition`/`factionAffiliation`).
5. **The hash channel under an edit.** §21.5 ruling 7 named `pickVariant` keying on display names.
   With the roster held, the description prose never re-runs, so a rename's effect on those 96
   choices is still unmeasured. **Settles it:** `__PROSEHASH_CENSUS__` (already in the hook) switched
   on around the P4 rename, comparing the seed multiset.
6. **`corruptionPass` double-corruption under an EDITED roster.** The pass writes through in 42/63;
   under the seam it is restored away. Whether an edited-but-uncorrupted npc would be corrupted twice
   is unmeasured.
7. **thorp and hamlet under the eight edits.** P4 ran village, town and city per the brief. The
   thorps are where a held bag accidentally works (6/63 without the placement fix), so they are the
   least informative, but they are also where the density law is dormant.
8. **Whether `settlementReason` (7 leaf paths, 6/63) and `activeConditions[].severity` (5/63) are
   DM-visible.** They settle under the `every` placement and vanish under `last`; I did not trace
   them to a surface.
