# RECEIPT — LGT-P2-MANIFEST (L-HOMES CAR 2) — ✅ COMPLETE

**Lane** L-HOMES-2 / `LGT-P2-MANIFEST` · **Seat** Opus 5 — Fable-unvalidated · chair Fable 5.1
**Dock** `$SC/laneLH2`, opened detached at `38474a59eba460f30d6596dcb65efda3a446738a`.
**Arrival verified:** HEAD == `38474a59e` ✅ · `git status --porcelain --untracked-files=all` = **0 lines** ✅ ·
`node_modules` = 453 per-package SYMLINKS + 1 real dir (NOT materialised) ✅ · detached ✅.
**Dock tip: `4a2e1110cfaf30991fd00f495a8cb61a0cba69b3`** · porcelain **0** at close.
No rebase, no push, no ref write, no register act, no `git stash`, no `git checkout --`,
no `git show HEAD:<path> > <path>`, no `npm install`. CAR 1's constant was neither needed nor duplicated.

---

## 1 · PREMISES RE-DERIVED BEFORE ACTING (a brief figure is the chair's claim, not a fact)

| premise as the brief states it | measured at `38474a59e` | verdict |
|---|---|---|
| `ENGINE_GATED_VIRTUAL_RULE_KEYS` is one flat list of **30** | `simulationRules.js:185–488`; module read **30**; `new Set().size === 30`; the last four members are NOT in alphabetical position | ✅ HOLDS |
| the two lighting contracts INVERT the moment a key lights | cause located: `engineGatedRuleKeys.walker.test.js:753–761` DEMANDS the deletion; effect located: `tradeConvergenceContract.walker.test.js:478` and `sovereigntyLightingContract.walker.test.js:1749` read build state off the list. **Executed as plant M1** | ✅ HOLDS — and proven, not argued |
| `tradeConvergenceContract.walker` ~:478–489 | the R6 arm spans `:459–509`; the join is `:478`, the partition guard `:503–509` | ✅ address holds |
| `sovereigntyLightingContract.walker` ~:1690–1692 | the FLAG_MANIFEST measure is at **`:1746–1751`**, its guard at `:1760`, its import at `:501` | ⚠ **AMENDED — off by ~56 lines** |
| "the five fences' virtuality arms" | exactly **5** of 19 `tests/property/*DormancyFence*` read the list: advanceEpoch `:404`, believedWorldAxes `:412`, casusCommercii `:231`, espionage `:446`, secondOrderBelief `:314` | ✅ HOLDS, count exact |
| OSR clause 3 at `check-observed-shape-readers.mjs:1697` | clause 3 is at **`:1886–1893`**; `:1697` addresses nothing | ⚠ **AMENDED** |
| `contributionLedgerShape` ~:90 | the joins are `:50`, `:100`, `:106`, `:108`; `:90` is inside a comment | ⚠ **AMENDED** |
| the backlog is **17, at ceiling 17** | `BACKLOG_RULE_KEYS` = **17** entries; `expect(…).toBeLessThanOrEqual(17)` at `:794` | ✅ HOLDS exactly |
| the "bank wins" arm ~:789–794 | `:776–794`; the EXACT equality is `:789` | ✅ HOLDS |
| **the walker's own docblock: "this walker's TEN importers"** | **TWELVE** files `import { codeOnly } from '…engineGatedRuleKeys.walker.test.js'` | ⚠ **STALE BY 2 — and it changed this car's design (§4.2)** |
| ⛔ **"re-point OSR clause 3"** | ⛔ **REFUTED — the OSR scripts are governed DETECTOR SOURCES (§2)** | ⛔ **REFUSED WITH MEASUREMENT** |

## 2 · ⛔ THE BRIEF ERROR THAT RE-CUT THE CAR — THE OSR SCRIPTS ARE GOVERNED DETECTOR SOURCES

I first built the split the way the brief's re-point list implies: the authored list renamed to
`ENGINE_GATED_RULE_KEYS` (the register), the legacy identifier reassigned to the derived set, and all ~20
consumers re-pointed across 38 files. eslint **0**, `typecheck:domain:strict` **0**. Then:

```
$ npm run check:observed-shape-readers
observed-shape DETECTOR SOURCE changed since the schema-16 instrument was governed
(scripts/check-observed-shape-readers.mjs, scripts/lib/observed-shape-baseline.mjs,
 scripts/migrate-observed-shape-readers.mjs); an ordinary gate/write cannot migrate the
instrument. Build and review the governed migration bundle instead.
EXIT=1
```

**Cause, located:** `scannerToolFiles()` (`check-observed-shape-readers.mjs:190–204`) names those three files
and `:2812–2839` refuses the whole gate on ANY byte moving in them. That is PLAN §5.3's
"crosses `isDetectorSourcePath` ⇒ **very large**" class: a governed migration bundle at the next free rung —
an act this lane may not take and one the chair has not authorised for CAR 2.

**And the rename cannot dodge it.** Clause 3 finds the register by scanning `simulationRules.js`'s RAW TEXT for
the literal `ENGINE_GATED_VIRTUAL_RULE_KEYS = Object.freeze([` (`:1886`); clause 4a slices its defaults window
at `indexOf('ENGINE_GATED_VIRTUAL_RULE_KEYS')` (`:1896`). Move that identifier off the authored array and
clause 3 finds nothing (`indexOf` → −1, `includes` → false) and **throws for every `VIRTUAL_DORMANT_WRITERS`
row**. So the brief's spelling is red WITH the migration unbuilt and red WITHOUT it.

**The re-cut, and it is strictly better than the brief's:** keep the authored array's identifier and position
exactly as they are — documented now as **THE REGISTER** — and publish the derived subset as
`ENGINE_GATED_DORMANT_RULE_KEYS`. Then

- **zero detector-source bytes move**; clause 3 and clause 4a are byte-identical; OSR stays green;
- clause 3 (registration) and clause 4 (lighting) stop double-firing on one event — which is precisely the
  separation the brief wanted. The old code's own comment said the two "meet at that removal"; after the
  re-cut **there is no removal**;
- the two lighting contracts, the fences' membership arms, `contributionLedgerShape`'s length-and-bijection
  pins, `subsystemRowsVirtual`'s bijection, the certification census union, `base-state-capsule`'s
  `flagManifestRows`, `coveringArrayCoverage` and `characterDrift.test` **need no edit at all** — they
  already read the register, and a lit key is never deleted from it.
  **⇒ 7 files changed instead of 38, and no consumer can be left mis-pointed.**

⚠ **The residual cost, stated rather than hidden:** the register's identifier still contains the word VIRTUAL
while the register may hold a lit key. The name is pinned by a governed instrument, so the meaning is carried
by the docblock, which now opens "THE ENGINE-GATED RULE-KEY REGISTER" and states the invariant in its second
paragraph. **A chair row: an honest identifier rides the OSR migration bundle, never a lane.**

## 3 · WHAT LANDED — one commit, seven files

| step | outcome | sha |
|---|---|---|
| CAR 2 — the register / dormant split, its walker arm, and the five fences | ✅ green | **`4a2e1110c`** |

- `src/domain/worldPulse/simulationRules.js` — the register's docblock re-cut to REGISTER semantics + the
  invariant; `deriveDormantRuleKeys(register, defaults, presets)` (pure, input-taking, drivable) and
  `ENGINE_GATED_DORMANT_RULE_KEYS`, published after the preset catalog because it derives from it.
  ⛔ **No key added, removed or reordered. No `<x>Enabled` token added** (control executed, §5).
- `tests/lint/engineGatedRuleKeys.walker.test.js` — the deletion-demanding arm replaced by the census read;
  THE SPLIT block (two-engine cross-check + both synthetic polarities + the containment) folded into the
  same title (§4.2). One anchored negative via `expectPresentThenAbsent`.
- the five dormancy fences — membership stays on the REGISTER; each virtuality arm now also reads
  `ENGINE_GATED_DORMANT_RULE_KEYS`, so the estate's derived answer must agree with the fence's own
  measurement. `advanceEpoch`'s vacuous preset read cured (§4.1).

## 4 · FINDINGS

### 4.1 ⛔ `advanceEpochDormancyFence`'s VIRTUALITY arm was VACUOUS
At HEAD `:397`: `Object.keys(SIMULATION_RULE_PRESETS[name] || {}).includes(FLAG)` enumerates the CATALOG
ENTRY (`id`, `label`, `rules`) and never the rules object, so `declaring` was `[]` for **every possible
tree** — the arm would have stayed green through the very preset light it exists to catch. Cured here
(`?.rules`) with a non-vacuity floor. **Executed both ways** with the key genuinely lit in two presets:

```
THE PRE-FIX (vacuous) READ  -> []
THE FIXED READ              -> ["quiet_local","narrative_campaign"]
```
The other four fences read `preset.rules` correctly.

### 4.2 ⛔ ONE NEW TITLE IN THAT WALKER COSTS THIRTEEN
`tests/lint/engineGatedRuleKeys.walker.test.js` is imported for `codeOnly` by **12** other suites, and
importing a symbol from a `.test.js` re-evaluates the module and RE-REGISTERS its suites inside each
importer. **Measured:** a 13-file battery moved 264 → 269 tests for a single added title (this file plus the
4 importers in that battery) ⇒ tree-wide one title is **1 + 12 = 13** runtime titles on the lighting census
and on the ratchet's `totalTests`. The walker's own docblock says **TEN** importers; the tree carries
**TWELVE**. I therefore FOLDED the split assertions into the existing title: **net test-title delta 0**, and
`tests/lint/` still measures exactly 2146 tests before and after.

### 4.3 `simulationRules.js`'s WAVES comment is stale on its byte claim
It says the preset catalog "rides the eager store slice … a measured +252 B to the first-paint closure", but
the landed pin `tests/build/campaignRuntimeLazy.test.js:145` asserts `simulationRules.js` is **NOT** in
`sourceStaticClosure('src/main.jsx')`. Not this car's to cure; recorded.

### 4.4 Left verbatim, deliberately
`scripts/mutation-coverage-manifest.json` rows dated 2026-08-05/06 describe plants made against the list
under its pre-re-cut meaning. They are historical records of executed controls and were not rewritten.

## 5 · RECEIPTS, EVERY EXIT CAPTURED IN-SHELL

| instrument | exit | figure |
|---|---|---|
| **`tests/lint/` WHOLE** (standalone, final tip) | **0** | `Test Files 139 passed (139) · Tests 2146 passed (2146)` — **zero failing arms**, lighting census walker GREEN |
| consumers battery (12 files incl. all 5 fences, both bijections, `campaignRuntimeLazy`, preset stability) | **0** | `Test Files 12 passed (12) · Tests 200 passed | 3 skipped` |
| `npm run typecheck:domain:strict` | **0** | `no strict-type regressions (1120 errors, ceiling 1120)` |
| `npm run check:observed-shape-readers` | **0** | `1993 finding(s), exactly matching the frozen inventory` (unmoved from base) |
| `node scripts/check-writer-reach.mjs` | **0** | judged 6520 · LIT 550 · LIT-NAME 4644 · DARK 1326 — **identical to base** |
| `node scripts/count-tuning-inventory.mjs` | **0** | 233 / 2110 / 535 / 6985 / 8 / 147 — **identical to base** |
| `npx eslint` (all 7 files, and each file singly) | **0** | zero problems |
| `node scripts/count-domain-any.mjs` | — | `simulationRules.js` absent from the report; the diff adds no `any` |
| base battery (13 files) BEFORE the car | **0** | `13 passed · 264 passed` — establishes the "bank wins" arm was GREEN at base ⇒ backlog == measuredGap == the 17 |
| ⚠ intermediate `tests/lint/` run | **1** | ONE red, MINE: `negativeAssertionAnchor.walker` convicted an un-anchored `not.toContain` at my line 822. Cured with `expectPresentThenAbsent`; the final run above is 0. |

**THE "BANK WINS" ARM, BEFORE AND AFTER.** `expect(Object.keys(BACKLOG_RULE_KEYS).sort()).toEqual(measuredGap)`
(`:789`) is a set EQUALITY, so its green IS the measurement. GREEN at base (264/264) and GREEN at the tip
(2146/2146 in the dir run) ⇒ the backlog equals the measured gated-but-uncensused set **exactly**, both times,
at **17 of ceiling 17**: `assize · commonsVoice · contestedGoals · corruptionWeb · discourseProse ·
economicCoupRead · heirs · heraldCausalVoice · intelTrade · ladderPoliticalWindows · memoryWeave ·
migrationCorruptionPush · neutralNeighbors · npcCredibility · seaRoads · thirdPartyRansom · upswingHazardRead`.
Plant M1 proves the arm is live rather than vacuous: it reds there when the census shifts.

## 6 · PLANT-OUT — four mutants, all killed, all restored byte-identical

Harness refuses a non-unique anchor and a no-op mutation; restore is `cp` from a backup taken BEFORE the
plant, verified by `cmp` in the same shell. The `git checkout` family was never used.

| # | mutant | result |
|---|---|---|
| **M1** | ⭐ **THE INVERSION**: obey HEAD's demand — delete `'pactFormationEnabled'` from the register | **5 failed / 63 passed across 4 files.** `tradeConvergenceContract.walker` verbatim: *"pactFormationEnabled: its row names gate evidence (src/domain/worldPulse/pactProposals.js) but the flag is NOT in ENGINE_GATED_VIRTUAL_RULE_KEYS — either the CQ5 manifest arm was missed or the evidence is fabricated"*; plus `engineGatedRuleKeys` ×2, `contributionLedgerShape`, `subsystemRowsVirtual` |
| **M2** | ⭐⭐ **THE CURE, WHICH IS THE WHOLE CAR**: light a REGISTERED key (`espionageEnabled`) in a preset override spread, register untouched | module-level: **register stays 30 and keeps the key; dormant falls to 29 and drops it.** Suite-level: `engineGatedRuleKeys` GREEN, `tradeConvergenceContract` GREEN, `contributionLedgerShape` GREEN, `subsystemRowsVirtual` GREEN — **1 failed / 88 passed**, the single red being `espionageDormancyFence` at `:452`, my dormant read, while the register read at `:447` PASSED in the same test. The split, in one file, in one run |
| **M3** | the derivation blinded to the presets surface | **1 failed / 6 passed** — *"REMOVAL [a preset light leaves the dormant set]: the member survived the operation … expected [ 'advanceEpochEnabled', …(29) ] to not include 'advanceEpochEnabled'"* |
| **M4** | the pre-fix vacuous preset read, driven with the key genuinely lit in two presets | old read `[]` vs fixed read `["quiet_local","narrative_campaign"]`; the fence's VIRTUALITY arm **reds** (2 failed / 22 passed) |

**Restore proof:** `simulationRules.js` md5 `ebd5577de1fdb2d3c301706a8f51b586` — live == backup after every
plant; `git status --porcelain --untracked-files=all` empty of anything but the seven intended files; the
seven pre-commit md5s are **identical** to the seven post-commit md5s, so the `lint-staged` pre-commit hook
edited and re-staged nothing.

## 7 · DARK-INERTNESS AND EAGER BYTES

**Same-seed shift: NONE.** No key was added, removed or reordered; no rules value, preset, flag or default
moved. `ENGINE_GATED_DORMANT_RULE_KEYS` equals the register exactly, in register order (`JSON.stringify`
equal, 30 == 30), measured before and after the plant series. The flag DENOMINATOR is unmoved — control
executed rather than asserted: `enumerateFlags`'s regex over `simulationRules.js` at HEAD vs the worktree
returns **90 tokens on both sides, added `[]`, removed `[]`**. No flag was minted by a comment.

**Eager bytes = 0, with the reason.** `tests/build/campaignRuntimeLazy.test.js:145` asserts
`sourceStaticClosure('src/main.jsx')` does **not** contain `src/domain/worldPulse/simulationRules.js`; this
car adds **no import edge** to that file (the only new import in the whole car is a test-side helper). That
pin is GREEN in the final run. ⇒ predicted first-paint delta **0 B**. The chair's hashed-chunk listing diff
remains the proof; this is the reason.

## 8 · PREDICTED REGISTER DELTAS — recorded, NOT taken

| register | delta | how |
|---|---|---|
| lighting census `files` / `parked` / `credited` / `titles` / `suiteTitles` | **0 / 0 / 0 / 0 / 0** | MEASURED — `sovereigntyLightingContract.walker` is GREEN inside the `tests/lint/` WHOLE run at the tip, and the census is compared EXACTLY. No file added; net test-title delta 0 (one rename); no describe added |
| test ratchet `totalTests` | **0** | MEASURED — `tests/lint/` measures 2146 tests at base and at the tip |
| test ratchet `totalFiles` | **0** | no file added or deleted |
| test ratchet `entries` (known failures) | **0** | no new known failure; every car arm lands green |
| golden-freeze register | **0** | no new test file, no fixture, and **no golden-adjacent env spelling** anywhere in the diff (`grep -E "UPDATE_|GOLDEN|REFREEZE|process\.env|import\.meta\.env"` over the added lines = NONE) ⇒ neither a row nor an exclusion is owed |
| four censuses per new `src/domain` leaf | **0** | no new leaf; the only `src` edit is to an existing module |
| `sizeBaseline` | **0** | none of the seven files appears in `scripts/.size-baseline.json` (8 baselined paths, checked by name); `simulationRules.js` effective lines 316 → 334 against a layer ceiling of 800, and `tests/lint/sizeBaseline.test.js` is GREEN inside the whole-dir run |
| voice magnitudes | **0** | no user-facing string added; `tests/copy/` untouched and the lint family green |
| OSR · writer-reach · tuning · domain-any | **0** | each measured above, exit 0, figures identical to base |
| **eager / first-paint bytes** | **0 B** | §7 |

## 9 · RETROVALIDATION ROW (owed to the Fable 5.1 chair)

| # | what an Opus seat JUDGED | what the chair must RE-DERIVE | receipts by path | priority |
|---|---|---|---|---|
| 1 | **The brief's spelling of the split was refused.** The legacy identifier STAYS on the authored register and the derived set takes a new name, because the OSR scripts are governed detector sources and clause 3 pins that literal | that the register keeping the word VIRTUAL is acceptable, and that an honest identifier is deferred to a governed OSR migration bundle rather than owed by this consist | `$SC/lh2/after-osr.log` (the refusal, verbatim) · `check-observed-shape-readers.mjs:190–204`, `:1886–1896`, `:2812–2839` · the derived export's docblock | **HIGH — it re-cuts the brief** |
| 2 | the derived list is named `ENGINE_GATED_DORMANT_RULE_KEYS` and its live readers are the five dormancy fences plus the walker | that DORMANT is the right house word (vs UNLIT / STILL_VIRTUAL), and that CAR 3's twelve new fences should read it too | `simulationRules.js` derived-export docblock · the five fence diffs | MEDIUM |
| 3 | **no reader-roster walker arm was built.** A consumer left on the derived set would invert at L-DEFAULT — but I measured that such an inversion is LOUD (the trade walker's R6 reds), not silent, so a roster arm's value did not carry its cost | that a loud red at L-DEFAULT is an acceptable guard, or that CAR 3/9 should add the roster | plant M1's five-arm red · §2 | MEDIUM |
| 4 | the split assertions RIDE the existing test title rather than a new one, on the measured 13× re-registration multiplier | that a zero-title landing was the right trade against one title's legibility | §4.2 · the fold's own comment in the walker | MEDIUM |
| 5 | `advanceEpochDormancyFence`'s vacuous preset read was CURED inside this car rather than reported and left | that curing a neighbouring fence's vacuity belongs in this car (it is one of the five the brief names) | §4.1 · plant M4 | LOW |
| 6 | src prose elsewhere naming the former single-purpose list was left untouched, since the register is the same object and those sentences still read correctly | that no prose sweep is owed | §2 | LOW |

**Dock tip: `4a2e1110cfaf30991fd00f495a8cb61a0cba69b3`.** Porcelain 0.
