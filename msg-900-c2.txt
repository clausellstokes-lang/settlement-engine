Desk composition (chair car): the chance-meeting writers look the presentation up through a typed narrowing — ENC-4c's strict-typecheck bill, paid

envoyChanceMeetingNews.js indexed the two-band CHANCE_MEETING_PRESENTATION table with the registry's four-value significance union at two sites (TS7053, a baseline-0 file: +2), which blocked npm run check at the composed tip. presentationFor() returns the notable or major band and null for routine / n-a — the same null the two sites already guarded with 'return null' — so the runtime is byte-identical and the checker sees the narrowing. Proof: typecheck:domain:strict exit 0 (the real script); the writer's suite, ENC-4's walker and the tuning walker under the mutex; eslint 0.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
