Desk landing (fix car B): the two writer-reach arms the OSR lineage keeps red enter the census as DECLARED DEBT, ledgered, with a magnitude — OWED_CEILING 3 → 5, both retire at rung 18

`the frozen corpusMeta EQUALS the observed-shape register corpusMeta` and `the frozen shapesDigest EQUALS the in-process digest` both assert corpusMeta.simulationFlagsLit against the OSR register: writer-reach, re-taken at this tip, measures 81 (the walker corpus lights every simulation flag and SEAT-78 minted `irregularForceEnabled`); the OSR register still carries 80 and cannot be re-taken here — its schema-17 migration receipt's subjectSha lies outside the composed lineage after the cherry-pick replay and the CLI `--write` refuses (RULING-OSR-900-NO-WRITE, chair kit). The rows carry their full attribution (subsystem, cause, introducedAt = the replayed genesis fea88e5b1, class debt) and a `capture` magnitude on the lit count (ceiling 81) so a SECOND new flag reds by figure; the meta-test's WALKER_ROWS_OWED ledger carries both with the retirement named (rung 18, lane OSR-SCHEMA18), and OWED_CEILING moves 3 → 5 with them. Meta-test 94/94.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
