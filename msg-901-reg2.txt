Lighting landing (register car 2): the writer-reach register refreshes at the composed tip — 31 identity rows gain the world-book surface term (30 as N, one as R), cohort 1322 unchanged, simulationFlagsLit stays 81

Taken by run-registers-901.sh at cea826821 (after the lighting census car), under the gate mutex, with every
figure predicted in writing before the doors ran (the PREDICTIONS line in reg901.run.log):

  · mounts baseline — the walker was green in the whole-suite proof; the door changed NOTHING (changed: []). As predicted.
  · prose-numerics — baseline 225 = live 225, rekeyed 0, relocated 0, fell 0, new 0. As predicted.
  · tuning inventory — the refreeze door changed nothing (75 arms skipped under the refreeze env; the plain
    walker was green in the proof and no lighting car adds a table). As predicted.
  · writer-reach — the ONE owed refresh: the proof's surfaceReach round-trip arm named
    "manpower on _timePressure is missing from the frozen surfaceReach"; --write re-derived the register
    (cohort 1322 → 1322; 0 stale folded out; 0 registered rows now lit). READ FROM THE DIFF, every line:
    31 identity rows changed, 0 added, 0 removed (the identity sets before and after are equal) —
    30 of them gain `world-book=N` and ONE, `tier on config`, gains `world-book=R` (the world-book
    surface READS it: before `"campaign-pdf=N dossier-pdf=R foundry=R json-export=N news=R web-display=R web-transitive=R world-book=N"`, after `"campaign-pdf=N dossier-pdf=R foundry=R json-export=N news=R web-display=R web-transitive=R world-book=R"`).
    The register's own corpus figures moved with the surface: files 1593 → 1594, sites 38585 → 38614,
    propRead 36534 → 36563, rGrades 5906 → 5912, nGrades 880233 → 881663, resolvedReceivers 6674 → 6680,
    web-display readers 695 → 696, world-book 81 → 92; verdictDigest re-derived; frozenAtSha 0de65c4ce → cea826821.
    The plain re-run is the receipt (WR_PLAIN_EXIT=0). corpusMeta.simulationFlagsLit is 81, the figure the proof's
    LIVE arm measured, so the two ratchet rows banked at §900 keep magnitude 81 ≤ ceiling 81 and the 81-vs-80
    disagreement stays declared debt until OSR rung 18 — nothing is re-banked.
  · OSR — skipped by ruling (RULING-OSR-900-NO-WRITE). Wizard-news — green in the proof; untouched.

(Message amended once before any gate: the first wording counted 30 world-book rows and stopped reading the diff
there; E4 says a figure is derived, never guessed, and the whole diff is the figure.)

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
