# §893 — THE GOLDEN RULINGS. Seven cars are landable, and the recorded blocker was never the blocker.
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · lane GOLDENPLAN, 2026-09-04.

## 1. ⭐⭐ THE "197 COMMITS BEHIND" FEAR IS DEAD, MEASURED TWICE
**7 of 7 cars still needed, 0 superseded.** All six added paths are absent from the product tip;
`recordGolden` scores 0 hits on product against 45 on golden, and that POSITIVE CONTROL is what makes
the zeros trustworthy rather than a broken search. `dormancyOracle.js` on product is byte-identical to
the merge base, so car 4's bit arm has no other road.

⭐ **Better than "not superseded": the product is ALREADY WRITTEN AGAINST this consist.**
`tests/lint/tuningRegister.walker.test.js` carries live references to `docs/shift-records/` and the
freeze register. All three arms traced and land-safe with no edit owed.

**The one colliding file conflicts on NOTHING.** `tests/property/generatorGoldenMaster.test.js`: the
product edits lines 18–131 (a T13 docblock), car 6 edits ~650 and ~761. `git merge-tree` returns a
clean tree with zero conflict markers; car 6's hunks shift +108. **No judgment call exists.**
⚠ **Honest hedge, and it is the lane's own:** the sequential rebase REPLAY could not be simulated
(Apple Git 2.39.5 lacks `merge-tree --merge-base`), so *"the rebase is also clean"* is **PLAUSIBLE,
not CONFIRMED**. The replay is the first act of the landing and it is watched, not assumed.

## 2. ⛔ R11 IS A MISGRADE — RULED, and the live-fault list is corrected
The card carries *"`commitTrailerRefusal` unwired (R11, HIGH)"* and says the GOLDEN freeze's pre-flight
requires it. **Both halves are wrong.** It lives at `goldenRecordDoor.js:210` with zero non-test
callers, and **that is the documented design**: its own docblock states *"NO GIT HOOK IS INSTALLED BY
THIS LANE, and that is deliberate rather than unfinished,"* and the walker header calls it *"merely
advisory"* with the in-tree walker as the load-bearing half. No support exists anywhere in the tree
for the pre-flight claim. **RULED: regrade HIGH → INFO/ACCEPTED.** It blocks nothing.

## 3. ⛔⛔ THE REAL BLOCKER WAS NEVER ON THE LIST — and it would have cost an 11th red
In the 197 intervening commits, `0fdbc53a0` minted `process.env.UPDATE_MOUNT_BASELINE` in
`tests/lint/dossierMountRegistry.walker.test.js:74`. **Car 2's walker enumerates by
`/^UPDATE_[A-Z_]+$/` across the whole `tests/` tree**, and that spelling is in neither the 43 enrolled
values nor the 7 exclusions. **Arm 2 REDS on landing.** The census that found it reproduces the
register's own base figures exactly (`UPDATE_GOLDEN = 40`, 43 carriers), which is what makes it a
finding rather than a scan artifact.

There is **no lawful disposition for an 11th red**: the census is full at 10 and `check-test-ratchet.mjs`
states plainly that a new regression is never baselined. **RULED: one `excludedEnvSpellings` entry,
folded into CAR 1** — a chair act touching no measured field.

⭐ **THE PATTERN, for the third time tonight:** *the recorded blocker was a phantom and the real
blocker was unrecorded.* A docket is a claim, and a claim decays in both directions — it keeps
frightening you with what is fixed while going quiet about what broke since.

## 4. RULED — the landing order and what it owes
**Order: 1 → 3 → 6 → 4 → 7 → 5 → 2**, the existing parent chain. Car 2 is last despite its name
because it reads what all six others create.
**OWED AND NOT CARRIED BY THE CONSIST:** the lighting census (`files` 2510 → 2511) and one
`mutation-coverage-manifest.json` rationale entry. ⭐ The lane **REFUSED to name**
`parked`/`credited`/`titles`/`suiteTitles`, which are underivable without executing the instrument and
forbidden to hand-edit. That refusal is E4 working exactly as intended and is recorded as compliance,
not as a gap.
**CANNOT MOVE, and this is provable rather than hoped:** every `src`-scoped register — `sizeBaseline`,
writer-reach, prose numerics, OSR, dossier mounts — because the consist touches **zero `src/` files and
zero `package.json`**. `negativeAssertionAnchor` is not owed.
