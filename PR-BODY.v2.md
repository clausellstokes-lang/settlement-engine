# The 2026-09-18 site review implemented, the data-loss cure, and the Edit-Mode train's first headless packets — `fixes-2026-09-18-consist` → `master`

**Read this first: the order of operations is not "merge, then migrate".** Vercel's ignore-build gate (`scripts/vercel-ignore-build.mjs`) skips the production build while the repo's migration head (203) is ahead of `supabase/applied-head.json` (200). So migrations 201–203 are applied FIRST (the owner's hand: `supabase db push`, then `supabase migration list`), `applied-head.json` is bumped to 203 on this branch with its verification text, and only then does the merge deploy. All three migrations are `create or replace function` only, re-runnable, non-destructive, and safe to apply before this client ships (202 says so in its own header: the server may refuse a key before a client token for it exists, never after).

**Why production is stale today.** Master's CI run on `b9c494afe` (2026-09-16) had every gate green except `Coverage floors`, which was cancelled at its 45-minute cap; that one cancellation skipped both deploy jobs. This branch raises that cap to 60 and the e2e job's cap from 15 to 25 (its suite more than doubles here). This merge is the cure for the stuck deploy.

**What was ordered.** 2026-09-18: "impliment every fix" over the site review (the owner's five findings and the chair's list). 2026-09-19: the five deferrals, the two residuals, the food deficit math and the band visual, the brand derivatives, and the owner's second walk (§934.20–§934.35): the food bar and every chart, name lists in columns, no clamped text, the content leaks, the create page and the fork, the phone front, onboarding and dialogs, the staff unlock and a development-only preview persona, the landing rebuilt around a regenerated Cnocby, the tier stock pictures, the export-image control withheld. 2026-09-19/20: pause the push, build the editor first (§934.42); "there is never any deferred work" (§934.56).

**What landed.** 525 commits over `master` (`b9c494afe`, the exact merge base), composed from Opus lanes under one Fable chair, every lane proven on its own branch and re-proven at the composed tip:
- **The site-review fixes** (about 330 commits): the painted arrow header and a consistent footer on every page; the phone front and its floors; dialogs and gates (29 popups gain close controls and Escape); the food arithmetic and band visuals; generated-content contradiction cures (a town is no longer "Stable" during an active crisis; a trade-fed town is no longer told it has a surplus; the Religious Quarter's vocabulary agrees with itself); two anonymous visitors can no longer fork the same world; the landing rebuilt around a regenerated Cnocby; tier stock paintings as the share image.
- **THE DATA-LOSS CURE (EM-B1k `19c4cb853`, EM-B1k2).** Shelving an NPC (or holding a roads hostage) and committing one tick erased the person from the save, and a house whose sole member was off-stage dissolved permanently: the tick's update was born from the filtered participation view and landed wholesale. The ruling: a READ projection is never the WRITE base. ⚠ The cure stops new loss; it does NOT heal a save already damaged. ⚠ A client ROLLBACK after this deploy re-opens the defect (the old client rebuilds its write base from the filtered view), so a rollback is destructive, not neutral.
- **The Edit-Mode train's first headless packets** (twelve landings; `docs/implementation/charters/EDIT-MODE-TRAIN.md`). None is reachable as an editor surface: the editor does not exist yet (all sixteen architected modules are absent). Three of the landings are pure denials that ship (the public projections and migrations 202/203 refuse the editor's two private save keys before any client can write them).
- **Tooling, gates and registers** (about 110 commits): citation walkers, the dead-code disposition made executable, ratchet baselines, the control-byte pin widened to every tracked file, the golden freeze's door.

**Declared, signed output shifts.** Nine signed shift records under `docs/shift-records/` (content coherence 525/525 golden rows; the pressure-sentence class 36/525; the food arithmetic 525/525; the parish-church text 453/525; the Religious Quarter's text 453/525; the dossier-contradiction cures; the sacred-house layout lines; the preset-lighting witness; CURE-J's provenance-only re-record, zero content rows). Said once, plainly: a seed generated on production today will not reproduce byte-identically after this deploy. Every shift is individually owner-worded and recorded; saved settlements keep their stored records.

**What the owner should know before merging.**
- The purchases lock (`src/lib/launchGate.js`, `VITE_PURCHASES_OPEN`) does not exist on `master`; this merge is what first obeys the 2026-09-16 "purchases locked until launch" order. Opening sales later is a Vercel environment change, not a code change.
- World locks are removed (`LockControls.jsx` deleted): a coup in an existing save that had locked its incumbent now falls without an approval queue.
- Anonymous visitors are narrowed by owner order: no thorpe, no pre-generation options. A new browser key `anonDraft` keeps an anonymous visitor's dossier across a refresh (it repairs the promise "your first dossier is yours to keep").
- Migration 201 lets staff (`developer` / `admin`) hold the Surveyor entitlement, including the provider-key write door (owner-ordered §934.28). It is the one genuine loosening in the delta; `profiles.role` is not self-writable.
- `WORKER_BUNDLE_CEILING_BYTES` stands at 1,401,208 and the generation worker measures exactly that: zero slack. The ceiling's rises are recorded in `tests/build/generationWorkerLazy.test.js` with their measured causes.

**The owner's hand.** `supabase login` / `link` / `db push` / `migration list` (201–203) before the merge; after it, a Vercel Redeploy on the merge commit if neither deploy secret is set; the redeploy of the eight edge functions whose shared bundles moved (`generate-narrative`, `custom-content`, `interpret-session`, `surveyor-autonomy`, `construct-realm`, `construct-settlement`, `surveyor-byok`, `ai-analyst`) from a CLEAN checkout of the merge commit; `npm run ops:post-deploy` for the release receipt.

**Supersedes** PR #52 (`arrow-header-2026-09-16`) and PR #51 (`fix-stray-config-seed-2026-09-16`): both tips are ancestors of this branch; close them after the merge. PR #48 (the ledger branch) is never merged and is closed.

**Verification.** RUN 24 (exit 0, 2026-09-20 16:06 EDT: 33,770 tests with zero regressions against a frozen census of zero failing tests; the build; `STRICT DIST OK` on 59 files / 543 tests) at `7a4ea48e9`: the full local check (`npm run check`: eleven validators, both typecheck ratchets, lint, the test ratchet with a frozen census of zero failing tests, the build, the strict dist verification). CI covers five jobs the local check does not (npm audit, e2e, performance, coverage floors, the Deno edge tests and the hostile-locale golden run); this PR's own checks are their first honest read. Three browser walks were run on earlier tips of this branch with the development-only preview persona.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
