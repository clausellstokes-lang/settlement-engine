# RULING — CLAMP W1's two refutations are ACCEPTED, and the import-pin widening stands
2026-09-05 · Opus 5 · ⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧

## BOTH REFUSALS ACCEPTED. Neither is a preference; each rests on a landed pin.

**`conquestIntent.js` — ACCEPTED, and it is the `sovereigntyAppraisal` case exactly.**
`envoyK3BeliefSeam.test.js:356` pins the file at `[]` imports and asserts it at :714; the file's own
header says so. Migrating it deletes a belief pin to satisfy a clamp ratchet, which is trading a
load-bearing guard for a bookkeeping one. ⭐ This is the identical position to
`sovereigntyAppraisal.js` — **the one and only file the ceiling was ever raised for**, under chair
ruling CR-WR10-A(b), for exactly this reason. The numerical proof was clean (144 combinations
exhaustive, zero divergences), so the blocker is structural, not arithmetic.
⛔ **OPEN, and NOT taken here:** whether the ceiling rises 62 → 63 to bank a row for it, as it rose
61 → 62 for its twin. That is a ruling of the CR-WR10-A kind and it should be made against the
FINAL wave's arithmetic, not in the middle.

**`conquestExecution.js` — ACCEPTED.** Its header declares "NO IMPORTS AT ALL, exactly like its
belief siblings", and `warSeatBooksPartition.test.js:313` already rules that such a header
declaration is "an older constraint that **wins**". That is the estate's own precedent, and it is
the same ground on which `conquestFeasibility` — excluded by name in the wave-0 ruling — keeps its
copy. Overruling it here would make the exclusion list arbitrary.
⭐ Worth recording because it is the better proof: the lane showed `1 - months/capacity` **cannot**
be `-0` (IEEE: `x−y` is `-0` only when `x = -0, y = +0`, and here `x` is the literal `1`), so this
was the numerically cleanest of the eight and was still refused on doctrine. **A file can be
provably safe and still not free to move.**

## THE IMPORT PIN — the widening STANDS
`dispositionChannels.test.js:187` pinned `dispositionProfile`'s imports as an exact set; the lane
widened it by one row for the kernel primitive only, left the reach assertion untouched, documented
it in the test, and named its own revert (`7548ee495` alone). Grounds accepted:
`sovereigntyIntentWr10.test.js` already pins an exact set that OPENS with that same specifier, so
the kernel import is not foreign to this class of pin. ⭐ The lane flagging its own single-commit
revert is what makes this cheap to overturn later.

## THE ARITHMETIC, CORRECTED
**72, not the plan's 70** — six migrated, two refused. Ten copies remain against a ceiling of 62.
Of those ten, four are now spoken for (`warAllianceRisk` and `conquestFeasibility` excluded at wave
0; `conquestIntent` and `conquestExecution` refused here), so **the final wave must resolve at least
four files that currently have a reason not to move.** The ceiling question cannot be dodged
forever, and it should be answered once, with the whole picture, at the end.

## TWO HAZARDS THE LANE SURFACED — recorded, not fixed
1. ⛔ `conquestDoctrineStage`'s division is safe **only because `storageCapacityMonths` has a floor
   of 1.5 — undocumented and unpinned** — and `foodStockpile.js` / `foodGenerator.js` already
   disagree on their last branch (2.0 vs 1.0). Anyone reconciling that drift toward the generator
   erodes the margin toward zero and reintroduces a division hazard with no edit to the clamp file.
2. ⛔ `warCoalitionDecision` now depends **implicitly** on `relationshipState.js` keeping COERCE
   semantics. Migrating `relationshipState` would change this file's behaviour with no edit to it.
Both are written into the source and the receipt. Neither is a blocker; both are traps for a later
lane that reads only one file.
